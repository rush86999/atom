# Property-Based Testing & Fuzzy Testing Dependencies
# For Phase 0: Foundation Setup

# Property-Based Testing (already in requirements.txt)
# hypothesis>=6.92.0,<7.0.0

# Fuzzy Testing
atheris>=2.2.0  # Coverage-guided fuzzing for Python
# python-fuzz>=0.1.0  # NOTE: Package doesn't exist on PyPI, removed

# Mutation Testing
mutmut>=2.4.0  # Mutation testing tool

# Chaos Engineering
# chaos-toolkit>=0.23.0  # NOTE: Package doesn't exist on PyPI, removed

# Parallel Test Execution
pytest-xdist>=3.6.0,<4.0.0  # Parallel pytest execution (constrain to avoid pytest 9)

# Coverage & Quality
pytest-cov>=4.1.0  # Coverage reporting (already in requirements.txt)
coverage[toml]>=7.0.0  # Enhanced coverage with TOML support

# Performance Testing
pytest-benchmark>=4.0.0  # Benchmarking tests
locust>=2.15.0  # Load testing

# Additional Testing Utilities
pytest-mock>=3.12.0  # Mocking utilities (already in requirements.txt)
pytest-timeout>=2.2.0,<3.0.0  # Test timeout enforcement
pytest-randomly>=3.15.0,<4.0.0  # Randomize test execution order
freezegun>=1.4.0,<2.0.0  # Time freezing for tests
factory-boy>=3.3.0  # Test data factories
faker>=22.0.0  # Fake data generation for tests
