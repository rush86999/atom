---
phase: 303-quality-focused-stub-test-fixes
plan: 02
type: execute
wave: 2
depends_on: [303-01]
files_modified:
  - backend/tests/test_workflow_versioning_system.py
  - backend/core/workflow_versioning_system.py
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - test_workflow_versioning_system.py rewritten from 6 stub tests to 15 proper AsyncMock tests
    - Tests actually import and test classes from core.workflow_versioning_system
    - Target coverage of 25-30% achieved for workflow_versioning_system.py (from 0%)
    - Test pass rate 95%+ (following Phase 297-298 patterns)
    - Backend coverage increases by +0.3-0.4pp
  artifacts:
    - path: backend/tests/test_workflow_versioning_system.py
      provides: Proper AsyncMock tests for workflow versioning system
      min_lines: 400
      coverage_target: 25-30%
      tests_estimated: 15
    - path: .planning/phases/303-quality-focused-stub-test-fixes/303-02-SUMMARY.md
      provides: Plan execution summary with coverage impact
  key_links:
    - from: backend/tests/test_workflow_versioning_system.py
      to: backend/core/workflow_versioning_system.py
      via: import and patch at module level
      pattern: from core.workflow_versioning_system import|patch('core.workflow_versioning_system'
---

## Objective

Rewrite 6 stub tests in test_workflow_versioning_system.py to be proper AsyncMock tests that actually import and test classes from core.workflow_versioning_system, following Phase 297-298 quality patterns.

**Purpose**: Fix second critical quality issue discovered in Phase 302 where stub tests don't import target modules, contributing 0% to coverage goals.

**Output**: 15 proper AsyncMock tests for workflow_versioning_system.py with 25-30% coverage and 95%+ pass rate.

---

## Execution Context

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/303-quality-focused-stub-test-fixes/303-01-SUMMARY.md
@.planning/phases/302-services-wave-2-next-3-files/302-01-SUMMARY.md

@backend/core/workflow_versioning_system.py
@backend/tests/test_atom_meta_agent.py (Phase 297 quality reference)

---

## Context

### Phase 302 Critical Discovery (Continued)

**Stub Test Problem**: 6 tests in test_workflow_versioning_system.py are stubs:

```python
# Current stub test (test_workflow_versioning_system.py)
class TestVersionCreation:
    def test_create_version(self):
        version = {
            "id": "v1",
            "workflow_id": "wf-001",
            "created_at": datetime.now(timezone.utc).isoformat(),
            "changes": ["Initial version"]
        }
        assert version["id"] == "v1"  # Tests dict, not WorkflowVersion!
```

**Problem**: Tests validate dict operations but never import or test actual classes from `core.workflow_versioning_system` like:
- `VersionType` (Enum)
- `ChangeType` (Enum)
- `WorkflowVersion` (BaseModel)
- `VersionDiff` (BaseModel)
- `Branch` (BaseModel)
- `ConflictResolution` (BaseModel)

**Impact**: 0% coverage despite having 96 lines of test code

### Plan 303-01 Success Pattern

Plan 303-01 will have demonstrated:
1. How to rewrite stub tests to import actual classes
2. Proper AsyncMock patching at module level
3. Pydantic model validation testing
4. 25-30% coverage achievement
5. 95%+ pass rate

Apply the same patterns to workflow_versioning_system.py.

### Target File Analysis

**File**: `core/workflow_versioning_system.py` (1,283 lines, 0% current coverage)

**Key Classes**:
- `VersionType` (Enum: MAJOR, MINOR, PATCH)
- `ChangeType` (Enum: ADD, REMOVE, MODIFY, RENAME)
- `WorkflowVersion` (BaseModel)
- `VersionDiff` (BaseModel)
- `Branch` (BaseModel)
- `ConflictResolution` (BaseModel)

**Target Coverage**: 25-30% (~320-385 lines from 1,283 total)
**Estimated Impact**: +0.3-0.4pp backend coverage (320-385 lines / 91,078 total lines)

---

## Tasks

<task type="auto">
  <name>Task 1: Analyze workflow_versioning_system.py structure and identify testable components</name>
  <files>backend/core/workflow_versioning_system.py</files>
  <action>
    Read and analyze core/workflow_versioning_system.py to understand:
    1. Class hierarchy and inheritance
    2. Key methods and their signatures
    3. External dependencies (database, workflow engine)
    4. Business logic that should be tested

    Extract class definitions:
    - VersionType enum values (MAJOR, MINOR, PATCH)
    - ChangeType enum values (ADD, REMOVE, MODIFY, RENAME)
    - WorkflowVersion fields and validation
    - VersionDiff fields and diff calculation logic
    - Branch fields and branch management
    - ConflictResolution fields and conflict handling

    Identify testable scenarios:
    - Model validation (Pydantic BaseModel)
    - Version creation and increment logic
    - Diff calculation between versions
    - Branch creation and merging
    - Conflict resolution strategies
    - Error handling (invalid versions, merge conflicts)

    Output: Analysis document listing all classes, methods, and recommended test scenarios (15 tests total).
  </action>
  <verify>
    <automated>grep -E "^class |^def |^async def " backend/core/workflow_versioning_system.py | head -30</automated>
  </verify>
  <done>Analysis document created with 15 test scenarios identified</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite test_workflow_versioning_system.py with proper AsyncMock imports (15 tests)</name>
  <files>backend/tests/test_workflow_versioning_system.py</files>
  <action>
    Complete rewrite of test_workflow_versioning_system.py following Plan 303-01 patterns:

    1. **Import actual classes** from target module:
       ```python
       from core.workflow_versioning_system import (
           VersionType,
           ChangeType,
           WorkflowVersion,
           VersionDiff,
           Branch,
           ConflictResolution,
       )
       ```

    2. **Test structure (15 tests, ~400 lines)**:
       - TestVersionTypeEnum (3 tests): test_enum_values, test_major_version, test_minor_patch_versions
       - TestChangeTypeEnum (3 tests): test_change_types, test_add_remove_modify, test_rename_change
       - TestWorkflowVersion (4 tests): test_version_creation, test_version_increment, test_version_validation, test_version_metadata
       - TestVersionDiff (3 tests): test_diff_calculation, test_no_diff, test_diff_formatting
       - TestBranchAndConflict (2 tests): test_branch_creation, test_conflict_resolution

    3. **Use AsyncMock for external dependencies**:
       - Patch database operations: `patch('core.workflow_versioning_system.SessionLocal')`
       - Mock workflow storage/retrieval queries
       - Patch workflow engine: `patch('core.workflow_versioning_system.WorkflowEngine')`

    4. **Cover success + error paths**:
       - Valid inputs → successful model creation/validation
       - Invalid inputs → Pydantic validation errors
       - Edge cases → empty diffs, version conflicts, merge failures

    5. **Follow Plan 303-01 success patterns**:
       - Class-based test organization
       - Descriptive test names (`test_version_increment_minor`)
       - Proper assertions (`assert version.version_type == VersionType.MINOR`)
       - Mock patches at import level

    Delete existing stub tests and replace with proper implementation.
  </action>
  <verify>
    <automated>cd backend && python3 -m pytest tests/test_workflow_versioning_system.py -v --tb=short</automated>
  </verify>
  <done>15 tests passing, tests import from core.workflow_versioning_system</done>
</task>

<task type="auto">
  <name>Task 3: Measure coverage and create Phase 303-02 summary</name>
  <files>.planning/phases/303-quality-focused-stub-test-fixes/303-02-SUMMARY.md</files>
  <action>
    1. Run coverage report for workflow_versioning_system.py:
       ```bash
       cd backend && python3 -m pytest tests/test_workflow_versioning_system.py --cov=core.workflow_versioning_system --cov-report=term-missing --cov-report=json
       ```

    2. Extract coverage metrics from coverage.json:
       - Total lines in workflow_versioning_system.py
       - Covered lines
       - Coverage percentage
       - Missing lines (top 10 areas to expand)

    3. Calculate backend coverage impact:
       - Baseline after Plan 303-01: ~26.1-26.2%
       - Lines covered in this plan: covered lines from workflow_versioning_system.py
       - New backend coverage: baseline + (lines covered / 91,078)
       - Target increase: +0.3-0.4pp (cumulative +0.6-0.8pp for Phase 303)

    4. Create 303-02-SUMMARY.md with:
       - Executive summary (stub tests → proper AsyncMock tests)
       - Test file rewrite (6 stub tests → 15 proper tests)
       - Test execution results (pass rate, coverage achieved)
       - Coverage analysis (per file and backend impact)
       - Combined Phase 303 impact (both files fixed)
       - Before/after comparison (0% → 25-30% coverage for both files)
       - Recommendations for Plan 03 (audit bulk-created tests)

    5. Update STATE.md with Plan 303-02 completion status

    Success criteria:
       - 15 tests passing (from 6 stub tests)
       - 25-30% coverage for workflow_versioning_system.py
       - Backend coverage increased to ~26.4-26.6% (cumulative for Phase 303)
       - Test pass rate 95%+
  </action>
  <verify>
    <automated>cd backend && python3 -c "import json; data = json.load(open('coverage.json')); f = 'core/workflow_versioning_system.py'; cov = data['files'].get(f, {}).get('summary', {}); print(f'Coverage: {cov.get(\"covered_lines\", 0)}/{cov.get(\"num_statements\", 1)} = {round(cov.get(\"covered_lines\", 0) / cov.get(\"num_statements\", 1) * 100, 1)}%')"</automated>
  </verify>
  <done>303-02-SUMMARY.md created with coverage impact documented</done>
</task>

---

## Threat Model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → Production code | Test mocks and patches may not reflect real-world behavior |
| Pydantic model validation | Tests verify model validation logic, not business logic |
| Database operations | Tests use mocked sessions, not real database queries |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-303-02-01 | S | Test fixture mocks | mitigate | Verify mocks match production interface signatures |
| T-303-02-02 | T | Version conflict handling | mitigate | Test conflict resolution logic with mock data |
| T-303-02-03 | I | Diff calculation integrity | mitigate | Test diff calculation with known inputs/outputs |
| T-303-02-04 | D | Test data exposure | accept | No PII in test data, synthetic version data only |
| T-303-02-05 | E | Exception handling in tests | mitigate | Test both valid and invalid inputs, verify validation errors |

---

## Verification

### Overall Plan Checks

1. **Stub Tests Eliminated**: Original 6 stub tests deleted and replaced with 15 proper tests
2. **Module Imports**: Tests actually import from core.workflow_versioning_system
3. **Test Quality**: Tests follow Plan 303-01 AsyncMock patterns
4. **Test Pass Rate**: 95%+ tests passing (14+/15)
5. **Coverage Targets**: workflow_versioning_system.py achieves 25-30% coverage
6. **Backend Coverage Impact**: Combined increase of +0.6-0.8pp for Phase 303
7. **Documentation**: 303-02-SUMMARY.md created with before/after comparison

### Success Metrics

| Metric | Target | Measurement Method |
|--------|--------|-------------------|
| Total Tests | 15 tests (from 6 stubs) | `pytest --collect-only` |
| Test Pass Rate | 95%+ (14/15) | `pytest -v` |
| workflow_versioning_system.py coverage | 25-30% | `pytest --cov-report=json` |
| Phase 303 cumulative coverage increase | +0.6-0.8pp | (lines covered / 91,078) × 100 |
| New backend coverage | ~26.4-26.6% | 25.8% + cumulative increase |

---

## Success Criteria

1. ✅ test_workflow_versioning_system.py rewritten from 6 stub tests to 15 proper AsyncMock tests
2. ✅ Tests actually import and test classes from core.workflow_versioning_system
3. ✅ Target coverage of 25-30% achieved for workflow_versioning_system.py (from 0%)
4. ✅ Test pass rate 95%+ (following Plan 303-01 patterns)
5. ✅ Backend coverage increases by +0.6-0.8pp cumulative for Phase 303 (25.8% → ~26.4-26.6%)
6. ✅ 303-02-SUMMARY.md created with before/after comparison documented
7. ✅ STATE.md updated with Plan 303-02 completion status

---

## Output

After completion, create `.planning/phases/303-quality-focused-stub-test-fixes/303-02-SUMMARY.md`

**Summary Template**:
- Executive summary (stub tests → proper AsyncMock tests)
- Before/after comparison (6 stub tests → 15 proper tests, 0% → 25-30% coverage)
- Test execution results (pass rate, coverage achieved)
- Coverage analysis (per file and Phase 303 cumulative impact)
- Combined Phase 303 results (both files fixed, stub test problem eliminated)
- Recommendations for Plan 03 (audit remaining bulk-created tests)
