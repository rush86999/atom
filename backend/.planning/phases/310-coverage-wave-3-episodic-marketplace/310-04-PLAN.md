---
phase: 310-coverage-wave-3-episodic-marketplace
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_episode_retrieval_service.py
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "All 3 failing tests in test_episode_retrieval_service.py pass"
    - "API parameter mismatch fixed (task_context removed)"
    - "Mock query configured to return iterable list"
    - "Test pass rate increases from 87.5% to 100%"
  artifacts:
    - path: "tests/test_episode_retrieval_service.py"
      provides: "Fixed test file with correct API expectations"
      min_lines: 550
  key_links:
    - from: "tests/test_episode_retrieval_service.py"
      to: "core.episode_retrieval_service.retrieve_contextual"
      via: "current_task parameter"
      pattern: "retrieve_contextual\\([^)]*current_task="
    - from: "tests/test_episode_retrieval_service.py"
      to: "mock query result"
      via: "iterable return value"
      pattern: "return_value=\\["
---

# Phase 310 Plan 04: Fix API Mismatches in test_episode_retrieval_service.py

**Fix 3 failing tests by correcting API parameter name and configuring mock to return iterable.**

## Objective

Fix the 3 failing tests in `test_episode_retrieval_service.py` by:
1. Removing non-existent `task_context` parameter from `retrieve_contextual()` calls
2. Configuring mock query to return an iterable list instead of a Mock object

Purpose: Align test expectations with actual production API and ensure mocks return proper data structures.

Output: 3 tests fixed (pass rate increases from 87.5% to 100%), test_episode_retrieval_service.py compliant with production API.

## Context

@.planning/phases/310-coverage-wave-3-episodic-marketplace/310-01-VERIFICATION.md
@.planning/phases/310-coverage-wave-3-episodic-marketplace/310-01-SUMMARY.md

### Gap 3: API Parameter Mismatch and Mock Configuration (3 tests failing)

**Issue 1: Parameter Name Mismatch (2 tests)**
- Tests call `retrieve_contextual(agent_id, task_context=...)`
- Actual signature: `retrieve_contextual(agent_id, current_task, limit, require_canvas, require_feedback)`
- The parameter is named `current_task`, not `task_context`

**Issue 2: Mock Configuration (1 test)**
- Mock query returns Mock object instead of iterable list
- Causes "TypeError: object is not iterable" when test tries to iterate results

## Tasks

<task type="auto">
  <name>Task 1: Verify actual API signature in episode_retrieval_service.py</name>
  <files>core/episode_retrieval_service.py</files>
  <action>Verify the exact method signature for `retrieve_contextual()` at lines 273-300:
```python
async def retrieve_contextual(
    self,
    agent_id: str,
    current_task: str,  # <-- Note: current_task, not task_context
    limit: int = 5,
    require_canvas: bool = False,
    require_feedback: bool = False
) -> Dict[str, Any]:
```

Confirm:
- First parameter after self is agent_id
- Second parameter is current_task (NOT task_context)
- Returns Dict[str, Any] with episodes in "episodes" key</action>
  <verify>grep -A 10 "def retrieve_contextual" core/episode_retrieval_service.py</verify>
  <done>Confirmed API signature: current_task parameter, not task_context</done>
</task>

<task type="auto">
  <name>Task 2: Fix retrieve_contextual parameter name mismatches</name>
  <files>tests/test_episode_retrieval_service.py</files>
  <action>Find test methods that call `retrieve_contextual(agent_id, task_context=...)` and rename parameter:
- Old: `await service.retrieve_contextual(agent_id, task_context="...", ...)`
- New: `await service.retrieve_contextual(agent_id, current_task="...", ...)`

Search for: `task_context=`
Replace with: `current_task=`

This should fix tests at approximately:
- Line 438: `task_context="Generate monthly sales dashboard"` → `current_task="Generate monthly sales dashboard"`
- Line 467: `task_context="data analysis"` → `current_task="data analysis"`</action>
  <verify>grep -n "task_context=" tests/test_episode_retrieval_service.py</verify>
  <done>All task_context parameters renamed to current_task</done>
</task>

<task type="auto">
  <name>Task 3: Fix mock query to return iterable list</name>
  <files>tests/test_episode_retrieval_service.py</files>
  <action>Find the test that has mock query returning non-iterable Mock object. Look for:
```python
mock_query = Mock()
# Missing: return_value configuration
```

Fix by adding return_value with a list:
```python
mock_query = Mock()
mock_query.return_value = []  # or list of mock episodes
# Or configure the specific method that should return a list
mock_lancedb.search.return_value = []
```

Search for patterns like:
- `mock_query = Mock()` without `return_value`
- Tests that iterate over mock results (for loops, list comprehensions)

The issue is likely in a semantic retrieval test where LanceDB search results are mocked.</action>
  <verify>grep -n "mock_query\|Mock()" tests/test_episode_retrieval_service.py | head -20</verify>
  <done>Mock query configured to return iterable list</done>
</task>

<task type="auto">
  <name>Task 4: Verify all tests pass</name>
  <files>tests/test_episode_retrieval_service.py</files>
  <action>Run the full test suite for test_episode_retrieval_service.py to verify all tests pass. Expect 100% pass rate (24/24 tests passing).</action>
  <verify>
    <automated>pytest tests/test_episode_retrieval_service.py -v --tb=short</automated>
  </verify>
  <done>All 24 tests pass (pass rate increases from 87.5% to 100%)</done>
</task>

## Threat Model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production code | API signature verification |
| test → mock | Mock return value validation |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-310-04-01 | Tampering | API parameter mismatch | mitigate | Verify all method calls match production signatures |
| T-310-04-02 | Spoofing | non-iterable mock | mitigate | Ensure mock return values match production data types (lists, dicts) |

## Verification

1. All 24 tests in test_episode_retrieval_service.py pass
2. No TypeError about non-iterable objects
3. No API signature errors (unexpected keyword argument)
4. Test pass rate increases from 87.5% to 100%

## Success Criteria

- [ ] 3 failing tests fixed
- [ ] task_context parameter renamed to current_task
- [ ] Mock query configured with iterable return value
- [ ] No regression in previously passing tests
- [ ] 100% pass rate achieved (24/24 tests)

## Output

After completion, create `.planning/phases/310-coverage-wave-3-episodic-marketplace/310-04-SUMMARY.md` with:
- Test results before and after (3 failed → 0 failed)
- List of fixes applied
- Coverage impact
- Commit details
