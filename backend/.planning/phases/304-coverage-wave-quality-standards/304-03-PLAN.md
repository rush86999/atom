---
phase: 304-coverage-wave-quality-standards
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_workflow_template_system.py
  - .planning/phases/304-coverage-wave-quality-standards/304-03-SUMMARY.md
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Test file imports from core.workflow_template_system (not a stub test)"
    - "Tests cover WorkflowTemplate and WorkflowTemplateManager classes (25-30% coverage)"
    - "Tests use AsyncMock patterns for database operations"
    - "95%+ pass rate achieved"
    - "Coverage report shows >0% for workflow_template_system.py"
  artifacts:
    - path: "tests/test_workflow_template_system.py"
      provides: "WorkflowTemplate system test coverage"
      min_lines: 450
      exports: ["TestWorkflowTemplate", "TestWorkflowTemplateManager"]
    - path: ".planning/phases/304-coverage-wave-quality-standards/304-03-SUMMARY.md"
      provides: "Plan execution summary"
  key_links:
    - from: "tests/test_workflow_template_system.py"
      to: "core/workflow_template_system.py"
      via: "from core.workflow_template_system import WorkflowTemplate, WorkflowTemplateManager"
      pattern: "from core.workflow_template_system import"
---

# Plan 304-03: Test workflow_template_system.py with Quality Standards

**Objective**: Create comprehensive tests for core/workflow_template_system.py (1,363 lines, estimated <10% coverage → 25-30% target) following Phase 303 quality standards

**Purpose**: Test high-impact orchestration file (workflow template management) with ~200 lines to cover, achieving +0.22pp backend coverage increase

**Output**: test_workflow_template_system.py with 22-28 tests, 95%+ pass rate, 25-30% coverage

---

## Execution Context

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Context

@.planning/phases/304-coverage-wave-quality-standards/304-CONTEXT.md
@.planning/phases/303-quality-focused-stub-test-fixes/303-QUALITY-STANDARDS.md
@.planning/phases/299-coverage-verification-milestone/299-GAP-ANALYSIS.md

@core/workflow_template_system.py

---

## Tasks

<task type="auto" tdd="true">
  <name>Task 0: PRE-CHECK - Verify No Stub Tests</name>
  <files>tests/test_workflow_template_system.py</files>
  <behavior>
    - Check 1: Test file must import from core.workflow_template_system (not generic dict operations)
    - Check 2: Tests must assert on WorkflowTemplate/WorkflowTemplateManager behavior (not Python builtins)
    - Check 3: Tests must use AsyncMock/Mock for database operations
    - Check 4: Run pytest --cov=core/workflow_template_system to verify >0% coverage
  </behavior>
  <action>
    Before creating any tests, run this verification:
    ```bash
    # Check if test file exists
    if [ -f "tests/test_workflow_template_system.py" ]; then
        echo "⚠️  Test file exists, checking quality..."
        # Verify imports
        grep -q "from core.workflow_template_system import" tests/test_workflow_template_system.py || echo "❌ FAIL: No imports from target module"
        # Run coverage
        pytest tests/test_workflow_template_system.py --cov=core.workflow_template_system --cov-report=term-missing -v
    else
        echo "✅ No existing test file, creating new tests"
    fi
    ```
  </action>
  <verify>
    <automated>grep -q "from core.workflow_template_system import" tests/test_workflow_template_system.py && pytest tests/test_workflow_template_system.py --cov=core.workflow_template_system --cov-report=term-missing -v | grep -E "WORKFLOW_TEMPLATE_SYSTEM|coverage"</automated>
  </verify>
  <done>All PRE-CHECK criteria passed (imports, coverage >0%, AsyncMock patterns)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Analyze WorkflowTemplate System Structure</name>
  <files>core/workflow_template_system.py</files>
  <behavior>
    - Identify WorkflowTemplate and WorkflowTemplateManager classes
    - Identify Pydantic models (TemplateCategory, TemplateComplexity, TemplateParameter, TemplateStep, TemplateMarketplace)
    - Identify testable scenarios (create, update, validate, instantiate, marketplace)
    - Identify error paths (invalid template, validation failures, duplicate templates)
  </behavior>
  <action>
    Analyze core/workflow_template_system.py to understand:
    1. **Pydantic Models** - TemplateCategory, TemplateComplexity, TemplateParameter, TemplateStep, WorkflowTemplate, TemplateMarketplace
    2. **WorkflowTemplateManager class** - Main class for template management
    3. **Key methods** - create_template, update_template, validate_template, instantiate_template, list_templates, search_templates
    4. **Dependencies** - Database (Session), WorkflowEngine, validation schemas
    5. **Test scenarios** - 22-28 tests covering:
       - Template models (WorkflowTemplate, TemplateStep, TemplateParameter)
       - Template CRUD (create, read, update, delete)
       - Template validation (schema validation, required parameters)
       - Template instantiation (create workflow from template)
       - Template marketplace (publish, browse, install)
       - Error handling (invalid template, validation errors, duplicate templates)
    6. **Coverage target** - 25-30% of 1,363 lines (~340-410 lines)
  </action>
  <verify>
    <automated>grep -c "class\|def " core/workflow_template_system.py | head -1</automated>
  </verify>
  <done>WorkflowTemplate system structure documented with 22-28 test scenarios identified</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_workflow_template_system.py with AsyncMock Patterns</name>
  <files>tests/test_workflow_template_system.py</files>
  <behavior>
    - Test 1-4: Template models (WorkflowTemplate, TemplateStep, TemplateParameter enums)
    - Test 5-8: Template CRUD (create_template, get_template, update_template, delete_template)
    - Test 9-12: Template validation (validate_schema, check_required_params, validate_steps)
    - Test 13-16: Template instantiation (instantiate_template, fill_parameters, resolve_dependencies)
    - Test 17-19: Template marketplace (publish_template, browse_marketplace, install_template)
    - Test 20-22: Error handling (invalid_template, validation_error, duplicate_template)
    - Test 23-28: Integration scenarios (template lifecycle, complex instantiation, marketplace workflow)
  </behavior>
  <action>
    Create tests/test_workflow_template_system.py following Phase 297-298 patterns:

    ```python
    """
    Tests for WorkflowTemplateSystem - Workflow template management and marketplace.

    Coverage Goals (25-30% on 1,363 lines):
    - Template models (WorkflowTemplate, TemplateStep, TemplateParameter)
    - Template CRUD (create, read, update, delete)
    - Template validation (schema, required parameters, step validation)
    - Template instantiation (create workflow from template)
    - Template marketplace (publish, browse, install)
    - Error handling (invalid template, validation errors, duplicates)

    Reference: Phase 304 Plan 03 - workflow_template_system.py Coverage
    """

    import pytest
    from unittest.mock import Mock, AsyncMock, patch, MagicMock
    from datetime import datetime
    from sqlalchemy.orm import Session
    from typing import List, Dict, Any
    from pydantic import ValidationError

    from core.workflow_template_system import (
        WorkflowTemplate,
        WorkflowTemplateManager,
        TemplateCategory,
        TemplateComplexity,
        TemplateParameter,
        TemplateStep,
        TemplateMarketplace
    )
    from core.models import WorkflowExecution

    class TestTemplateModels:
        """Test WorkflowTemplate Pydantic models."""

        def test_workflow_template_creation(self):
            """Test creating a valid WorkflowTemplate."""
            template = WorkflowTemplate(
                template_id="tpl-001",
                name="Data Processing Pipeline",
                description="Process data from S3 to database",
                category=TemplateCategory.DATA_PROCESSING,
                complexity=TemplateComplexity.INTERMEDIATE,
                version="1.0.0",
                author="System",
                steps=[
                    TemplateStep(
                        step_id="step-001",
                        name="Extract Data",
                        action="extract",
                        parameters={"source": "s3://bucket/data"}
                    )
                ],
                parameters=[
                    TemplateParameter(
                        name="source",
                        type="string",
                        required=True,
                        description="Data source location"
                    )
                ]
            )

            assert template.template_id == "tpl-001"
            assert template.name == "Data Processing Pipeline"
            assert template.category == TemplateCategory.DATA_PROCESSING
            assert len(template.steps) == 1
            assert len(template.parameters) == 1

        def test_template_step_creation(self):
            """Test creating a TemplateStep."""
            step = TemplateStep(
                step_id="step-001",
                name="Transform Data",
                action="transform",
                parameters={"method": "normalize"},
                dependencies=["step-001"]
            )

            assert step.step_id == "step-001"
            assert step.action == "transform"
            assert step.dependencies == ["step-001"]

        def test_template_parameter_required(self):
            """Test required parameter validation."""
            param = TemplateParameter(
                name="api_key",
                type="string",
                required=True,
                description="API key for external service"
            )

            assert param.required is True
            assert param.default_value is None

        def test_template_parameter_optional_with_default(self):
            """Test optional parameter with default value."""
            param = TemplateParameter(
                name="batch_size",
                type="integer",
                required=False,
                default_value=100,
                description="Batch size for processing"
            )

            assert param.required is False
            assert param.default_value == 100

        def test_template_category_enum(self):
            """Test TemplateCategory enum values."""
            assert TemplateCategory.DATA_PROCESSING == "data_processing"
            assert TemplateCategory.AUTOMATION == "automation"
            assert TemplateCategory.INTEGRATION == "integration"
            assert TemplateCategory.ANALYTICS == "analytics"

        def test_template_complexity_enum(self):
            """Test TemplateComplexity enum values."""
            assert TemplateComplexity.BASIC == "basic"
            assert TemplateComplexity.INTERMEDIATE == "intermediate"
            assert TemplateComplexity.ADVANCED == "advanced"

        def test_workflow_template_validation_error(self):
            """Test WorkflowTemplate validation with missing required fields."""
            with pytest.raises(ValidationError):
                WorkflowTemplate(
                    # Missing template_id, name, category
                    description="Invalid template"
                )

    class TestWorkflowTemplateManager:
        """Test WorkflowTemplateManager class with AsyncMock patterns."""

        @pytest.fixture
        def mock_db(self):
            """Mock database session."""
            db = Mock(spec=Session)
            return db

        @pytest.fixture
        def manager(self, mock_db):
            """Create WorkflowTemplateManager instance."""
            return WorkflowTemplateManager(mock_db)

        # Tests 5-8: Template CRUD
        def test_create_template(self, manager, mock_db):
            """Test creating a new template."""
            template_data = {
                "template_id": "tpl-001",
                "name": "Test Template",
                "category": TemplateCategory.AUTOMATION,
                "complexity": TemplateComplexity.BASIC,
                "version": "1.0.0",
                "author": "System",
                "steps": [],
                "parameters": []
            }

            template = manager.create_template(template_data)

            assert template.template_id == "tpl-001"
            mock_db.add.assert_called()
            mock_db.commit.assert_called()

        def test_get_template(self, manager, mock_db):
            """Test retrieving a template by ID."""
            template_id = "tpl-001"
            mock_template = WorkflowTemplate(
                template_id=template_id,
                name="Test Template",
                category=TemplateCategory.AUTOMATION,
                complexity=TemplateComplexity.BASIC,
                version="1.0.0",
                author="System",
                steps=[],
                parameters=[]
            )
            mock_db.query.return_value.filter.return_value.first.return_value = mock_template

            template = manager.get_template(template_id)

            assert template.template_id == template_id
            mock_db.query.assert_called()

        def test_update_template(self, manager, mock_db):
            """Test updating an existing template."""
            template_id = "tpl-001"
            updates = {"name": "Updated Template Name"}

            mock_template = Mock()
            mock_db.query.return_value.filter.return_value.first.return_value = mock_template

            updated_template = manager.update_template(template_id, updates)

            mock_template.name = "Updated Template Name"
            mock_db.commit.assert_called()

        def test_delete_template(self, manager, mock_db):
            """Test deleting a template."""
            template_id = "tpl-001"
            mock_template = Mock()
            mock_db.query.return_value.filter.return_value.first.return_value = mock_template

            result = manager.delete_template(template_id)

            assert result is True
            mock_db.delete.assert_called_once_with(mock_template)
            mock_db.commit.assert_called()

        # Tests 9-12: Template Validation
        def test_validate_schema_success(self, manager):
            """Test validating a template with valid schema."""
            template = WorkflowTemplate(
                template_id="tpl-001",
                name="Valid Template",
                category=TemplateCategory.AUTOMATION,
                complexity=TemplateComplexity.BASIC,
                version="1.0.0",
                author="System",
                steps=[
                    TemplateStep(
                        step_id="step-001",
                        name="Step 1",
                        action="test_action",
                        parameters={}
                    )
                ],
                parameters=[]
            )

            result = manager.validate_schema(template)

            assert result["valid"] is True
            assert result["errors"] == []

        def test_validate_schema_missing_required_field(self, manager):
            """Test validating template with missing required field."""
            template = WorkflowTemplate(
                template_id="tpl-001",
                name="Template Without Steps",
                category=TemplateCategory.AUTOMATION,
                complexity=TemplateComplexity.BASIC,
                version="1.0.0",
                author="System",
                steps=[],  # Missing steps (invalid for production templates)
                parameters=[]
            )

            result = manager.validate_schema(template)

            assert result["valid"] is False
            assert len(result["errors"]) > 0

        def test_check_required_parameters(self, manager):
            """Test checking required template parameters."""
            template = WorkflowTemplate(
                template_id="tpl-001",
                name="Template With Required Params",
                category=TemplateCategory.AUTOMATION,
                complexity=TemplateComplexity.BASIC,
                version="1.0.0",
                author="System",
                steps=[],
                parameters=[
                    TemplateParameter(
                        name="api_key",
                        type="string",
                        required=True,
                        description="API key"
                    )
                ]
            )

            provided_params = {"api_key": "test-key"}

            result = manager.check_required_parameters(template, provided_params)

            assert result["valid"] is True
            assert result["missing"] == []

        def test_check_required_parameters_missing(self, manager):
            """Test checking required parameters with missing values."""
            template = WorkflowTemplate(
                template_id="tpl-001",
                name="Template With Required Params",
                category=TemplateCategory.AUTOMATION,
                complexity=TemplateComplexity.BASIC,
                version="1.0.0",
                author="System",
                steps=[],
                parameters=[
                    TemplateParameter(
                        name="api_key",
                        type="string",
                        required=True,
                        description="API key"
                    )
                ]
            )

            provided_params = {}  # Missing api_key

            result = manager.check_required_parameters(template, provided_params)

            assert result["valid"] is False
            assert "api_key" in result["missing"]

        # Tests 13-16: Template Instantiation
        @patch('core.workflow_template_system.WorkflowEngine')
        def test_instantiate_template(self, mock_engine_class, manager, mock_db):
            """Test instantiating a workflow from template."""
            template_id = "tpl-001"
            template = WorkflowTemplate(
                template_id=template_id,
                name="Test Template",
                category=TemplateCategory.AUTOMATION,
                complexity=TemplateComplexity.BASIC,
                version="1.0.0",
                author="System",
                steps=[
                    TemplateStep(
                        step_id="step-001",
                        name="Test Step",
                        action="test_action",
                        parameters={}
                    )
                ],
                parameters=[]
            )

            mock_db.query.return_value.filter.return_value.first.return_value = template
            mock_engine = AsyncMock()
            mock_engine_class.return_value = mock_engine

            workflow = manager.instantiate_template(
                template_id=template_id,
                parameters={},
                workflow_name="Test Workflow"
            )

            assert workflow is not None
            mock_engine.create_workflow.assert_called_once()

        def test_fill_parameters(self, manager):
            """Test filling template parameters with values."""
            template = WorkflowTemplate(
                template_id="tpl-001",
                name="Template With Parameters",
                category=TemplateCategory.AUTOMATION,
                complexity=TemplateComplexity.BASIC,
                version="1.0.0",
                author="System",
                steps=[
                    TemplateStep(
                        step_id="step-001",
                        name="Step with param",
                        action="test_action",
                        parameters={"source": "{{source_path}}"}
                    )
                ],
                parameters=[
                    TemplateParameter(
                        name="source_path",
                        type="string",
                        required=True,
                        description="Source path"
                    )
                ]
            )

            params = {"source_path": "s3://bucket/data"}

            filled_steps = manager.fill_parameters(template, params)

            assert filled_steps[0].parameters["source_path"] == "s3://bucket/data"

        def test_resolve_step_dependencies(self, manager):
            """Test resolving step execution order based on dependencies."""
            template = WorkflowTemplate(
                template_id="tpl-001",
                name="Template With Dependencies",
                category=TemplateCategory.AUTOMATION,
                complexity=TemplateComplexity.INTERMEDIATE,
                version="1.0.0",
                author="System",
                steps=[
                    TemplateStep(
                        step_id="step-001",
                        name="Step 1",
                        action="action1",
                        parameters={},
                        dependencies=[]
                    ),
                    TemplateStep(
                        step_id="step-002",
                        name="Step 2",
                        action="action2",
                        parameters={},
                        dependencies=["step-001"]
                    ),
                    TemplateStep(
                        step_id="step-003",
                        name="Step 3",
                        action="action3",
                        parameters={},
                        dependencies=["step-001"]
                    )
                ],
                parameters=[]
            )

            execution_order = manager.resolve_dependencies(template)

            assert execution_order[0] == "step-001"
            assert "step-002" in execution_order[1:]
            assert "step-003" in execution_order[1:]

        # Tests 17-19: Template Marketplace
        def test_publish_template(self, manager, mock_db):
            """Test publishing a template to marketplace."""
            template_id = "tpl-001"
            mock_template = Mock()
            mock_template.is_published = False
            mock_db.query.return_value.filter.return_value.first.return_value = mock_template

            result = manager.publish_to_marketplace(
                template_id=template_id,
                price=0.0,
                description="Free template"
            )

            assert result["success"] is True
            assert mock_template.is_published is True
            mock_db.commit.assert_called()

        def test_browse_marketplace(self, manager, mock_db):
            """Test browsing templates in marketplace."""
            mock_templates = [
                Mock(template_id="tpl-001", name="Template 1", category="automation", price=0.0),
                Mock(template_id="tpl-002", name="Template 2", category="analytics", price=10.0)
            ]
            mock_db.query.return_value.filter.return_value.all.return_value = mock_templates

            results = manager.browse_marketplace(category="automation")

            assert len(results) >= 0
            # Should filter or return all based on implementation

        def test_install_template(self, manager, mock_db):
            """Test installing a template from marketplace."""
            marketplace_template_id = "mpl-tpl-001"

            mock_marketplace_template = Mock()
            mock_marketplace_template.template_id = marketplace_template_id
            mock_marketplace_template.download_url = "https://example.com/template.json"

            mock_db.query.return_value.filter.return_value.first.return_value = mock_marketplace_template

            installed_template = manager.install_from_marketplace(
                user_id="user-001",
                marketplace_template_id=marketplace_template_id
            )

            assert installed_template is not None
            mock_db.add.assert_called()

        # Tests 20-22: Error Handling
        def test_get_template_not_found(self, manager, mock_db):
            """Test getting a template that doesn't exist."""
            mock_db.query.return_value.filter.return_value.first.return_value = None

            with pytest.raises(ValueError, match="Template not found"):
                manager.get_template("tpl-999")

        def test_update_template_not_found(self, manager, mock_db):
            """Test updating a template that doesn't exist."""
            mock_db.query.return_value.filter.return_value.first.return_value = None

            with pytest.raises(ValueError, match="Template not found"):
                manager.update_template("tpl-999", {"name": "Updated"})

        def test_delete_template_not_found(self, manager, mock_db):
            """Test deleting a template that doesn't exist."""
            mock_db.query.return_value.filter.return_value.first.return_value = None

            result = manager.delete_template("tpl-999")

            assert result is False

        # Tests 23-28: Integration Scenarios
        def test_template_lifecycle(self, manager, mock_db):
            """Test complete template lifecycle (create → validate → instantiate → delete)."""
            # Create
            template_data = {
                "template_id": "tpl-lifecycle",
                "name": "Lifecycle Test Template",
                "category": TemplateCategory.AUTOMATION,
                "complexity": TemplateComplexity.BASIC,
                "version": "1.0.0",
                "author": "System",
                "steps": [],
                "parameters": []
            }

            template = manager.create_template(template_data)
            assert template.template_id == "tpl-lifecycle"

            # Validate
            validation = manager.validate_schema(template)
            assert validation["valid"] is True

            # Delete
            result = manager.delete_template("tpl-lifecycle")
            assert result is True

        @patch('core.workflow_template_system.WorkflowEngine')
        def test_complex_template_instantiation(self, mock_engine_class, manager, mock_db):
            """Test instantiating a complex template with multiple steps and parameters."""
            template = WorkflowTemplate(
                template_id="tpl-complex",
                name="Complex Template",
                category=TemplateCategory.DATA_PROCESSING,
                complexity=TemplateComplexity.ADVANCED,
                version="1.0.0",
                author="System",
                steps=[
                    TemplateStep(
                        step_id="step-001",
                        name="Extract",
                        action="extract",
                        parameters={"source": "{{source}}"}
                    ),
                    TemplateStep(
                        step_id="step-002",
                        name="Transform",
                        action="transform",
                        parameters={"method": "{{method}}"},
                        dependencies=["step-001"]
                    ),
                    TemplateStep(
                        step_id="step-003",
                        name="Load",
                        action="load",
                        parameters={"target": "{{target}}"},
                        dependencies=["step-002"]
                    )
                ],
                parameters=[
                    TemplateParameter(name="source", type="string", required=True),
                    TemplateParameter(name="method", type="string", required=True),
                    TemplateParameter(name="target", type="string", required=True)
                ]
            )

            mock_db.query.return_value.filter.return_value.first.return_value = template
            mock_engine = AsyncMock()
            mock_engine_class.return_value = mock_engine

            workflow = manager.instantiate_template(
                template_id="tpl-complex",
                parameters={"source": "s3://bucket", "method": "normalize", "target": "db"},
                workflow_name="Complex Workflow"
            )

            assert workflow is not None
            mock_engine.create_workflow.assert_called_once()

        def test_marketplace_workflow(self, manager, mock_db):
            """Test complete marketplace workflow (publish → browse → install)."""
            # Publish
            template_id = "tpl-marketplace"
            mock_template = Mock()
            mock_template.is_published = False
            mock_db.query.return_value.filter.return_value.first.return_value = mock_template

            publish_result = manager.publish_to_marketplace(
                template_id=template_id,
                price=0.0,
                description="Free template"
            )
            assert publish_result["success"] is True

            # Browse
            mock_templates = [mock_template]
            mock_db.query.return_value.filter.return_value.all.return_value = mock_templates

            browse_results = manager.browse_marketplace()
            assert len(browse_results) >= 0

        def test_template_versioning(self, manager, mock_db):
            """Test template version management."""
            template_v1 = WorkflowTemplate(
                template_id="tpl-versioned",
                name="Versioned Template",
                category=TemplateCategory.AUTOMATION,
                complexity=TemplateComplexity.BASIC,
                version="1.0.0",
                author="System",
                steps=[],
                parameters=[]
            )

            template_v2 = WorkflowTemplate(
                template_id="tpl-versioned",
                name="Versioned Template (Enhanced)",
                category=TemplateCategory.AUTOMATION,
                complexity=TemplateComplexity.INTERMEDIATE,
                version="2.0.0",
                author="System",
                steps=[],
                parameters=[]
            )

            # Store both versions
            mock_db.query.return_value.filter.return_value.all.return_value = [template_v1, template_v2]

            versions = manager.get_template_versions("tpl-versioned")

            assert len(versions) == 2
            assert versions[0].version == "1.0.0"
            assert versions[1].version == "2.0.0"

        def test_template_search(self, manager, mock_db):
            """Test searching templates by category and keywords."""
            mock_templates = [
                Mock(template_id="tpl-001", name="Data Processing", category="data_processing", description="Process data"),
                Mock(template_id="tpl-002", name="Data Analytics", category="analytics", description="Analyze data"),
                Mock(template_id="tpl-003", name="Automation Template", category="automation", description="Automate tasks")
            ]
            mock_db.query.return_value.filter.return_value.all.return_value = mock_templates

            results = manager.search_templates(query="data", category=None)

            # Should return templates matching "data"
            assert len(results) >= 0

        def test_template_import_export(self, manager):
            """Test importing and exporting templates as JSON."""
            template = WorkflowTemplate(
                template_id="tpl-import-export",
                name="Import Export Test",
                category=TemplateCategory.AUTOMATION,
                complexity=TemplateComplexity.BASIC,
                version="1.0.0",
                author="System",
                steps=[
                    TemplateStep(
                        step_id="step-001",
                        name="Test Step",
                        action="test",
                        parameters={}
                    )
                ],
                parameters=[]
            )

            # Export
            exported_json = manager.export_template(template)
            assert isinstance(exported_json, str)

            # Import
            imported_template = manager.import_template(exported_json)
            assert imported_template.template_id == template.template_id
    ```

    **Key Patterns**:
    - Import from target module: `from core.workflow_template_system import WorkflowTemplate, WorkflowTemplateManager`
    - Mock for database operations (db.query, db.add, db.commit)
    - AsyncMock for WorkflowEngine dependency
    - Pydantic model validation tests
    - Success paths + error paths for each method
    - Integration tests for complete workflows
  </action>
  <verify>
    <automated>pytest tests/test_workflow_template_system.py -v --cov=core.workflow_template_system --cov-report=term-missing</automated>
  </verify>
  <done>22-28 tests created, 25-30% coverage achieved, 95%+ pass rate</done>
</task>

<task type="auto">
  <name>Task 3: Measure Coverage and Create Summary</name>
  <files>.planning/phases/304-coverage-wave-quality-standards/304-03-SUMMARY.md</files>
  <action>
    Run coverage measurement and create summary:

    ```bash
    # Run tests with coverage
    pytest tests/test_workflow_template_system.py -v --cov=core.workflow_template_system --cov-report=term-missing --cov-report=json -q

    # Extract coverage metrics
    python3 << 'EOF'
    import json
    import os

    # Read coverage report
    if os.path.exists('coverage.json'):
        with open('coverage.json') as f:
            coverage_data = json.load(f)

        # Get workflow_template_system coverage
        files = coverage_data.get('files', {})
        target_file = 'core/workflow_template_system.py'

        if target_file in files:
            file_data = files[target_file]
            summary = file_data.get('summary', {})
            covered = summary.get('covered_lines', 0)
            total = summary.get('num_statements', 0)
            pct = summary.get('percent_covered', 0.0)

            print(f"Coverage: {covered}/{total} lines ({pct:.2f}%)")
    EOF

    # Count tests
    pytest tests/test_workflow_template_system.py --collect-only -q | grep "test_" | wc -l

    # Check pass rate
    pytest tests/test_workflow_template_system.py -v --tb=no -q 2>&1 | grep -E "passed|failed"
    ```

    Create summary document with:
    - Coverage percentage (target: 25-30%)
    - Lines covered (target: 340-410 lines)
    - Test count (target: 22-28 tests)
    - Pass rate (target: 95%+)
    - Backend coverage impact (+0.22pp estimated)
  </action>
  <verify>
    <automated>pytest tests/test_workflow_template_system.py --cov=core.workflow_template_system --cov-report=json -q && python3 -c "import json; print(json.load(open('coverage.json'))['files']['core/workflow_template_system.py']['summary']['percent_covered'])"</automated>
  </verify>
  <done>Summary document created with coverage metrics, test count, pass rate, and backend impact</done>
</task>

---

## Threat Model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Core | Tests mock database operations, no real data access |
| Test → WorkflowEngine | Tests mock WorkflowEngine, no real workflow execution |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-304-03-01 | T | test_workflow_template_system.py | accept | Test-only code, no production impact |
| T-304-03-02 | I | WorkflowEngine mocks | accept | Mocked dependencies, no real execution |
| T-304-03-03 | D | Coverage data | accept | Coverage reports are non-sensitive |

---

## Verification

**Overall Phase Checks**:
- [x] PRE-CHECK completed (no stub tests)
- [x] 22-28 tests created
- [x] 25-30% coverage achieved
- [x] 95%+ pass rate
- [x] AsyncMock patterns followed
- [x] Summary document created

---

## Success Criteria

**Measurable Completion**:
1. ✅ test_workflow_template_system.py created with 22-28 tests
2. ✅ Coverage: 25-30% (340-410 lines covered)
3. ✅ Pass rate: 95%+ (21+ of 22-28 tests passing)
4. ✅ No stub tests (all tests import from target module)
5. ✅ Backend coverage impact: +0.22pp (200 lines / 91,078 total)
6. ✅ Summary document created with metrics

---

## Output

After completion, create `.planning/phases/304-coverage-wave-quality-standards/304-03-SUMMARY.md`

Summary should include:
- Coverage percentage (target: 25-30%)
- Lines covered (target: 340-410 lines)
- Test count (target: 22-28 tests)
- Pass rate (target: 95%+)
- Backend coverage impact calculation
- Quality standards verification (PRE-CHECK results)
