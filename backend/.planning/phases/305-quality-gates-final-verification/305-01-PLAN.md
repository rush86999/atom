---
phase: 305-quality-gates-final-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/test_workflow_engine.py, tests/test_atom_agent_endpoints.py, tests/test_agent_world_model.py]
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03, QUAL-04]

must_haves:
  truths:
    - "Phase 300 tests achieve 95%+ pass rate (49 failures fixed)"
    - "All tests import from target modules (already verified)"
    - "All tests achieve >0% coverage (already verified)"
    - "Model attributes match production code"
    - "Method signatures match production code"
  artifacts:
    - path: "tests/test_workflow_engine.py"
      provides: "Fixed tests for workflow_engine.py (46 tests, 95%+ pass rate)"
      min_lines: 880
    - path: "tests/test_atom_agent_endpoints.py"
      provides: "Fixed tests for atom_agent_endpoints.py (40 tests, 95%+ pass rate)"
      min_lines: 700
    - path: "tests/test_agent_world_model.py"
      provides: "Fixed tests for agent_world_model.py (20 tests, 95%+ pass rate)"
      min_lines: 400
  key_links:
    - from: "tests/test_workflow_engine.py"
      to: "core/workflow_engine.py"
      via: "import statements and model assertions"
      pattern: "from core.workflow_engine import"
    - from: "tests/test_atom_agent_endpoints.py"
      to: "core/atom_agent_endpoints.py"
      via: "import statements and endpoint assertions"
      pattern: "from core.atom_agent_endpoints import"
    - from: "tests/test_agent_world_model.py"
      to: "core/agent_world_model.py"
      via: "import statements and model assertions"
      pattern: "from core.agent_world_model import"
---

<objective>
Fix Phase 300 test failures (49 failing tests) by aligning test expectations with actual production code

Purpose: Improve test pass rate from 54% to 95%+ by fixing model attribute mismatches, method signature mismatches, and fixture issues

Output: 106 tests with 95%+ pass rate (vs current 54%)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/305-quality-gates-final-verification/305-CONTEXT.md
@.planning/phases/303-quality-focused-stub-test-fixes/303-QUALITY-STANDARDS.md
@.planning/phases/304-coverage-wave-quality-standards/304-PHASE-SUMMARY.md
@.planning/phases/300-orchestration-wave-1-top-3-files/300-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read production code to understand actual APIs</name>
  <files>core/workflow_engine.py, core/atom_agent_endpoints.py, core/agent_world_model.py</files>
  <action>Read the actual production code to identify:
1. Model fields (WorkflowExecution, WorkflowStep, AgentRequest, AgentResponse, BusinessFact, WorldModel)
2. Method signatures (execute_workflow, validate_step, stream_agent_response, create_agent, query_facts, add_fact)
3. Enum values (WorkflowState, AgentStatus, FactStatus)
4. Constructor parameters and required fields

Document the actual API in a comment block for each file to guide test fixes.</action>
  <verify>grep "ACTUAL API" tests/test_workflow_engine.py tests/test_atom_agent_endpoints.py tests/test_agent_world_model.py</verify>
  <done>Actual API documented in comments for all 3 files</done>
</task>

<task type="auto">
  <name>Task 2: Fix test_workflow_engine.py test failures</name>
  <files>tests/test_workflow_engine.py</files>
  <action>Fix 46 tests in test_workflow_engine.py by:
1. Reading actual production code (workflow_engine.py) first
2. Updating model field references to match actual model definitions
3. Updating method signature calls to match actual signatures
4. Fixing fixture mocks to match actual dependencies
5. Updating assertions to match actual return values

Follow Phase 303 quality standards:
- Import from target module (already done)
- Use AsyncMock for external dependencies (database, LLM)
- Test success + error paths
- Achieve >0% coverage (already done)
- Target 95%+ pass rate</action>
  <verify>pytest tests/test_workflow_engine.py -v --tb=short | grep -E "(PASSED|FAILED|ERROR)" | wc -l</verify>
  <done>46 tests in test_workflow_engine.py with 95%+ pass rate (44+ passing)</done>
</task>

<task type="auto">
  <name>Task 3: Fix test_atom_agent_endpoints.py test failures</name>
  <files>tests/test_atom_agent_endpoints.py</files>
  <action>Fix 40 tests in test_atom_agent_endpoints.py by:
1. Reading actual production code (atom_agent_endpoints.py) first
2. Updating model field references to match actual model definitions
3. Updating method signature calls to match actual FastAPI endpoint signatures
4. Fixing fixture mocks (Mock(spec=Request), database sessions)
5. Updating assertions to match actual response formats

Follow Phase 303 quality standards:
- Import from target module (already done)
- Use AsyncMock for external dependencies
- Test success + error paths
- Achieve >0% coverage (already done)
- Target 95%+ pass rate</action>
  <verify>pytest tests/test_atom_agent_endpoints.py -v --tb=short | grep -E "(PASSED|FAILED|ERROR)" | wc -l</verify>
  <done>40 tests in test_atom_agent_endpoints.py with 95%+ pass rate (38+ passing)</done>
</task>

<task type="auto">
  <name>Task 4: Fix test_agent_world_model.py test failures</name>
  <files>tests/test_agent_world_model.py</files>
  <action>Fix 20 tests in test_agent_world_model.py by:
1. Reading actual production code (agent_world_model.py) first
2. Updating model field references to match actual model definitions (BusinessFact, WorldModel)
3. Updating method signature calls to match actual signatures (query_facts, add_fact)
4. Fixing fixture mocks (world model queries, fact retrieval)
5. Updating assertions to match actual return values

Follow Phase 303 quality standards:
- Import from target module (already done)
- Use AsyncMock for external dependencies
- Test success + error paths
- Achieve >0% coverage (already done)
- Target 95%+ pass rate</action>
  <verify>pytest tests/test_agent_world_model.py -v --tb=short | grep -E "(PASSED|FAILED|ERROR)" | wc -l</verify>
  <done>20 tests in test_agent_world_model.py with 95%+ pass rate (19+ passing)</done>
</task>

<task type="auto">
  <name>Task 5: Run PRE-CHECK verification</name>
  <files>tests/test_workflow_engine.py, tests/test_atom_agent_endpoints.py, tests/test_agent_world_model.py</files>
  <action>Run Phase 303 PRE-CHECK verification to ensure:
1. All tests import from target modules (verify with grep)
2. All tests achieve >0% coverage (run pytest --cov)
3. All tests have 95%+ pass rate (run pytest -v)
4. No stub tests detected (coverage >0%)

Document results in a PRE-CHECK report.</action>
  <verify>grep "PRE-CHECK COMPLETE" tests/test_workflow_engine.py || echo "PRE-CHECK report exists"</verify>
  <done>PRE-CHECK verification passed for all 3 files (imports, coverage, pass rate)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production | Test code reads production code to verify actual API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-305-01 | T (Tampering) | Test assertions could be weakened to pass failing tests | mitigate | Do not remove assertions, fix test data to match actual API |
| T-305-02 | I (Information disclosure) | Test code may expose implementation details | accept | Test code is not production, acceptable |
| T-305-03 | D (Denial of service) | Long-running tests could block CI/CD | accept | Unit tests should complete in <60 seconds |
</threat_model>

<verification>
## Phase-Level Verification

1. **Test Pass Rate Check**:
   ```bash
   pytest tests/test_workflow_engine.py tests/test_atom_agent_endpoints.py tests/test_agent_world_model.py -v --tb=short | grep -E "(passed|failed|error)"
   ```
   Expected: 95%+ pass rate (101+ of 106 tests passing)

2. **Coverage Check**:
   ```bash
   pytest tests/test_workflow_engine.py tests/test_atom_agent_endpoints.py tests/test_agent_world_model.py --cov=core.workflow_engine --cov=core.atom_agent_endpoints --cov=core.agent_world_model --cov-report=term-missing
   ```
   Expected: >0% coverage for all 3 modules

3. **Import Check**:
   ```bash
   grep -h "^from core\." tests/test_workflow_engine.py tests/test_atom_agent_endpoints.py tests/test_agent_world_model.py | head -10
   ```
   Expected: All files import from target modules

4. **No Stub Tests**:
   ```bash
   pytest tests/test_workflow_engine.py tests/test_atom_agent_endpoints.py tests/test_agent_world_model.py --cov=core.workflow_engine --cov=core.atom_agent_endpoints --cov=core.agent_world_model --cov-report=json | jq '.totals.percent_covered'
   ```
   Expected: Coverage >0% for all modules
</verification>

<success_criteria>
- [ ] 106 tests fixed (46 + 40 + 20)
- [ ] 95%+ pass rate achieved (101+ tests passing)
- [ ] All tests import from target modules (verified)
- [ ] All tests achieve >0% coverage (verified)
- [ ] Model attributes match production code
- [ ] Method signatures match production code
- [ ] PRE-CHECK verification passed
- [ ] No stub tests detected
- [ ] 305-01-SUMMARY.md created with deviation documentation
</success_criteria>

<output>
After completion, create `.planning/phases/305-quality-gates-final-verification/305-01-SUMMARY.md`
</output>
