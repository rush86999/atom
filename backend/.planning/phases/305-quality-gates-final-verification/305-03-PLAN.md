---
phase: 305-quality-gates-final-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [tests/test_workflow_debugger.py, tests/test_hybrid_data_ingestion.py, tests/test_workflow_template_system.py]
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03, QUAL-04]

must_haves:
  truths:
    - "Phase 304 tests achieve 95%+ pass rate (41 failures fixed)"
    - "All tests import from target modules (already verified)"
    - "All tests achieve >0% coverage (already verified)"
    - "Model attribute mismatches resolved"
    - "Method signature mismatches resolved"
    - "Enum value mismatches resolved"
  artifacts:
    - path: "tests/test_workflow_debugger.py"
      provides: "Fixed tests for workflow_debugger.py (30 tests, 95%+ pass rate)"
      min_lines: 600
    - path: "tests/test_hybrid_data_ingestion.py"
      provides: "Fixed tests for hybrid_data_ingestion.py (24 tests, 95%+ pass rate)"
      min_lines: 500
    - path: "tests/test_workflow_template_system.py"
      provides: "Fixed tests for workflow_template_system.py (21 tests, 95%+ pass rate)"
      min_lines: 450
  key_links:
    - from: "tests/test_workflow_debugger.py"
      to: "core/workflow_debugger.py"
      via: "import statements and model assertions"
      pattern: "from core.workflow_debugger import"
    - from: "tests/test_hybrid_data_ingestion.py"
      to: "core/hybrid_data_ingestion.py"
      via: "import statements and service assertions"
      pattern: "from core.hybrid_data_ingestion import"
    - from: "tests/test_workflow_template_system.py"
      to: "core/workflow_template_system.py"
      via: "import statements and model assertions"
      pattern: "from core.workflow_template_system import"
---

<objective>
Fix Phase 304 test failures (41 failing tests) by resolving model attribute, API signature, and enum value mismatches

Purpose: Improve test pass rate from 45.3% to 95%+ by aligning test expectations with actual production code

Output: 75 tests with 95%+ pass rate (vs current 45.3%)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/305-quality-gates-final-verification/305-CONTEXT.md
@.planning/phases/303-quality-focused-stub-test-fixes/303-QUALITY-STANDARDS.md
@.planning/phases/304-coverage-wave-quality-standards/304-PHASE-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read production code to understand actual APIs</name>
  <files>core/workflow_debugger.py, core/hybrid_data_ingestion.py, core/workflow_template_system.py</files>
  <action>Read the actual production code to identify:
1. Model fields (WorkflowBreakpoint, DebugVariable, ExecutionTrace, HybridIngestionConfig, WorkflowTemplate, TemplateCategory)
2. Method signatures (set_breakpoint, step_execution, inspect_state, sync_integration_data, get_usage_summary, create_template, validate_template)
3. Enum values (TemplateCategory actual values: AUTOMATION, DATA_PROCESSING, AI_ML, BUSINESS, INTEGRATION, MONITORING, REPORTING, SECURITY, GENERAL)
4. Constructor parameters and required fields

Document the actual API in a comment block for each file to guide test fixes.

Critical issue from Phase 304 summary:
- "TemplateCategory.ANALYTICS - doesn't exist (actual values: AUTOMATION, DATA_PROCESSING, AI_ML, BUSINESS, INTEGRATION, MONITORING, REPORTING, SECURITY, GENERAL)"</action>
  <verify>grep "ACTUAL API" tests/test_workflow_debugger.py tests/test_hybrid_data_ingestion.py tests/test_workflow_template_system.py</verify>
  <done>Actual API documented in comments for all 3 files (including actual enum values)</done>
</task>

<task type="auto">
  <name>Task 2: Fix test_workflow_debugger.py test failures</name>
  <files>tests/test_workflow_debugger.py</files>
  <action>Fix 15 failing tests in test_workflow_debugger.py (currently 50% pass rate) by:
1. Reading actual production code (workflow_debugger.py) first
2. Fixing model attribute mismatches: "WorkflowBreakpoint.is_active - attribute doesn't exist on model"
3. Fixing field naming inconsistencies: "Production code uses node_id parameter but model has step_id column"
4. Updating model field references to match actual model definitions
5. Updating method signature calls to match actual signatures (set_breakpoint, step_execution, inspect_state)

Critical issues from Phase 304 summary:
- "15 test failures due to WorkflowBreakpoint/DebugVariable model attributes not matching production code usage"
- "Production code uses node_id parameter but model has step_id column"
- "Field naming inconsistencies between models and production code"

Follow Phase 303 quality standards:
- Import from target module (already done)
- Use AsyncMock for external dependencies
- Test success + error paths
- Achieve >0% coverage (27.70% already achieved)
- Target 95%+ pass rate</action>
  <verify>pytest tests/test_workflow_debugger.py -v --tb=short | grep -E "(PASSED|FAILED|ERROR)" | wc -l</verify>
  <done>30 tests in test_workflow_debugger.py with 95%+ pass rate (29+ passing)</done>
</task>

<task type="auto">
  <name>Task 3: Fix test_hybrid_data_ingestion.py test failures</name>
  <files>tests/test_hybrid_data_ingestion.py</files>
  <action>Fix 9 failing tests in test_hybrid_data_ingestion.py (currently 62.5% pass rate) by:
1. Reading actual production code (hybrid_data_ingestion.py) first
2. Fixing method signature mismatches: "sync_integration_data() doesn't accept mode parameter"
3. Fixing return value mismatches: "get_usage_summary() returns different keys than expected"
4. Fixing return value issues: "_check_auto_enable_sync() returns None instead of bool"
5. Updating test expectations to match actual API

Critical issues from Phase 304 summary:
- "9 test failures due to actual API differing from expected signatures"
- "sync_integration_data() doesn't accept mode parameter"
- "get_usage_summary() returns different keys than expected"
- "_check_auto_enable_sync() returns None instead of bool"

Follow Phase 303 quality standards:
- Import from target module (already done)
- Use AsyncMock for external dependencies
- Test success + error paths
- Achieve >0% coverage (41.0% already achieved)
- Target 95%+ pass rate</action>
  <verify>pytest tests/test_hybrid_data_ingestion.py -v --tb=short | grep -E "(PASSED|FAILED|ERROR)" | wc -l</verify>
  <done>24 tests in test_hybrid_data_ingestion.py with 95%+ pass rate (23+ passing)</done>
</task>

<task type="auto">
  <name>Task 4: Fix test_workflow_template_system.py test failures</name>
  <files>tests/test_workflow_template_system.py</files>
  <action>Fix 17 failing tests in test_workflow_template_system.py (currently 19% pass rate) by:
1. Reading actual production code (workflow_template_system.py) first
2. Fixing enum value mismatches: Replace "TemplateCategory.ANALYTICS" with actual enum values
3. Using actual enum values: AUTOMATION, DATA_PROCESSING, AI_ML, BUSINESS, INTEGRATION, MONITORING, REPORTING, SECURITY, GENERAL
4. Updating model field references to match actual model definitions
5. Updating method signature calls to match actual signatures (create_template, validate_template, instantiate_template)

Critical issues from Phase 304 summary:
- "17 test failures due to enum values not matching actual code"
- "TemplateCategory.ANALYTICS - doesn't exist"
- "Actual values: AUTOMATION, DATA_PROCESSING, AI_ML, BUSINESS, INTEGRATION, MONITORING, REPORTING, SECURITY, GENERAL"

Follow Phase 303 quality standards:
- Import from target module (already done)
- Use AsyncMock for external dependencies
- Test success + error paths
- Achieve >0% coverage (48.6% already achieved)
- Target 95%+ pass rate</action>
  <verify>pytest tests/test_workflow_template_system.py -v --tb=short | grep -E "(PASSED|FAILED|ERROR)" | wc -l</verify>
  <done>21 tests in test_workflow_template_system.py with 95%+ pass rate (20+ passing)</done>
</task>

<task type="auto">
  <name>Task 5: Run PRE-CHECK verification</name>
  <files>tests/test_workflow_debugger.py, tests/test_hybrid_data_ingestion.py, tests/test_workflow_template_system.py</files>
  <action>Run Phase 303 PRE-CHECK verification to ensure:
1. All tests import from target modules (verify with grep)
2. All tests achieve >0% coverage (run pytest --cov)
3. All tests have 95%+ pass rate (run pytest -v)
4. No stub tests detected (coverage >0%)

Document results in a PRE-CHECK report.</action>
  <verify>grep "PRE-CHECK COMPLETE" tests/test_workflow_debugger.py || echo "PRE-CHECK report exists"</verify>
  <done>PRE-CHECK verification passed for all 3 files (imports, coverage, pass rate)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production | Test code reads production code to verify actual API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-305-07 | T (Tampering) | Test assertions could be weakened to pass failing tests | mitigate | Do not remove assertions, fix test data to match actual API |
| T-305-08 | I (Information disclosure) | Test code may expose implementation details | accept | Test code is not production, acceptable |
| T-305-09 | D (Denial of service) | Long-running tests could block CI/CD | accept | Unit tests should complete in <60 seconds |
</threat_model>

<verification>
## Phase-Level Verification

1. **Test Pass Rate Check**:
   ```bash
   pytest tests/test_workflow_debugger.py tests/test_hybrid_data_ingestion.py tests/test_workflow_template_system.py -v --tb=short | grep -E "(passed|failed|error)"
   ```
   Expected: 95%+ pass rate (71+ of 75 tests passing)

2. **Coverage Check**:
   ```bash
   pytest tests/test_workflow_debugger.py tests/test_hybrid_data_ingestion.py tests/test_workflow_template_system.py --cov=core.workflow_debugger --cov=core.hybrid_data_ingestion --cov=core.workflow_template_system --cov-report=term-missing
   ```
   Expected: >0% coverage for all 3 modules

3. **Import Check**:
   ```bash
   grep -h "^from core\." tests/test_workflow_debugger.py tests/test_hybrid_data_ingestion.py tests/test_workflow_template_system.py | head -10
   ```
   Expected: All files import from target modules

4. **No Stub Tests**:
   ```bash
   pytest tests/test_workflow_debugger.py tests/test_hybrid_data_ingestion.py tests/test_workflow_template_system.py --cov=core.workflow_debugger --cov=core.hybrid_data_ingestion --cov=core.workflow_template_system --cov-report=json | jq '.totals.percent_covered'
   ```
   Expected: Coverage >0% for all modules
</verification>

<success_criteria>
- [ ] 75 tests fixed (30 + 24 + 21)
- [ ] 95%+ pass rate achieved (71+ tests passing)
- [ ] All tests import from target modules (verified)
- [ ] All tests achieve >0% coverage (verified)
- [ ] Model attribute mismatches resolved (WorkflowBreakpoint.is_active, node_id vs step_id)
- [ ] Method signature mismatches resolved (sync_integration_data, get_usage_summary, _check_auto_enable_sync)
- [ ] Enum value mismatches resolved (TemplateCategory actual values)
- [ ] PRE-CHECK verification passed
- [ ] No stub tests detected
- [ ] 305-03-SUMMARY.md created with deviation documentation
</success_criteria>

<output>
After completion, create `.planning/phases/305-quality-gates-final-verification/305-03-SUMMARY.md`
</output>
