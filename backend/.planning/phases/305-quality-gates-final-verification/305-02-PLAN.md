---
phase: 305-quality-gates-final-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [tests/test_byok_handler.py, tests/test_lancedb_handler.py, tests/test_episode_segmentation_service.py]
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-03, QUAL-04]

must_haves:
  truths:
    - "Phase 301 tests achieve 95%+ pass rate (48.6 failures fixed)"
    - "All tests import from target modules (already verified)"
    - "All tests achieve >0% coverage (already verified)"
    - "Fixture issues resolved (load_config, missing db argument)"
    - "Integration test assumptions converted to unit tests"
  artifacts:
    - path: "tests/test_byok_handler.py"
      provides: "Fixed tests for byok_handler.py (40 tests, 95%+ pass rate)"
      min_lines: 605
    - path: "tests/test_lancedb_handler.py"
      provides: "Fixed tests for lancedb_handler.py (7 tests, 95%+ pass rate)"
      min_lines: 105
    - path: "tests/test_episode_segmentation_service.py"
      provides: "Fixed tests for episode_segmentation_service.py (7 tests, 95%+ pass rate)"
      min_lines: 105
  key_links:
    - from: "tests/test_byok_handler.py"
      to: "core/llm/byok_handler.py"
      via: "import statements and handler assertions"
      pattern: "from core.llm.byok_handler import"
    - from: "tests/test_lancedb_handler.py"
      to: "core/llm/lancedb_handler.py"
      via: "import statements and handler assertions"
      pattern: "from core.llm.lancedb_handler import"
    - from: "tests/test_episode_segmentation_service.py"
      to: "core/episode_segmentation_service.py"
      via: "import statements and service assertions"
      pattern: "from core.episode_segmentation_service import"
---

<objective>
Fix Phase 301 test failures (48.6 failing tests) by resolving fixture issues and converting integration tests to unit tests

Purpose: Improve test pass rate from 10% to 95%+ by fixing incorrect patching, missing db arguments, and integration test assumptions

Output: 54 tests with 95%+ pass rate (vs current 10%)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/305-quality-gates-final-verification/305-CONTEXT.md
@.planning/phases/303-quality-focused-stub-test-fixes/303-QUALITY-STANDARDS.md
@.planning/phases/304-coverage-wave-quality-standards/304-PHASE-SUMMARY.md
@.planning/phases/301-services-wave-1-byok-vector-episodes/301-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read production code to understand actual APIs</name>
  <files>core/llm/byok_handler.py, core/llm/lancedb_handler.py, core/episode_segmentation_service.py</files>
  <action>Read the actual production code to identify:
1. Model fields (BYOKConfig, LLMProvider, LanceDBConfig, Episode, EpisodeSegment)
2. Method signatures (get_provider, validate_config, search, insert, segment_episode, detect_gaps)
3. Constructor parameters and required fields
4. Database session usage patterns

Document the actual API in a comment block for each file to guide test fixes.</action>
  <verify>grep "ACTUAL API" tests/test_byok_handler.py tests/test_lancedb_handler.py tests/test_episode_segmentation_service.py</verify>
  <done>Actual API documented in comments for all 3 files</done>
</task>

<task type="auto">
  <name>Task 2: Fix test_byok_handler.py test failures</name>
  <files>tests/test_byok_handler.py</files>
  <action>Fix 40 tests in test_byok_handler.py (currently 0% passing) by:
1. Reading actual production code (byok_handler.py) first
2. Fixing incorrect patching: `patch('core.byok_handler.load_config')` → `patch('core.byok_handler.SessionLocal')` (load_config doesn't exist)
3. Updating model field references to match actual model definitions
4. Updating method signature calls to match actual signatures
5. Fixing fixture mocks (database sessions, LLM providers)

Critical issue from Phase 301 summary: "All tests fail due to incorrect patching (load_config doesn't exist)"

Follow Phase 303 quality standards:
- Import from target module (already done)
- Use AsyncMock for external dependencies
- Test success + error paths
- Achieve >0% coverage (already done)
- Target 95%+ pass rate</action>
  <verify>pytest tests/test_byok_handler.py -v --tb=short | grep -E "(PASSED|FAILED|ERROR)" | wc -l</verify>
  <done>40 tests in test_byok_handler.py with 95%+ pass rate (38+ passing)</done>
</task>

<task type="auto">
  <name>Task 3: Fix test_lancedb_handler.py test failures</name>
  <files>tests/test_lancedb_handler.py</files>
  <action>Fix 7 tests in test_lancedb_handler.py (currently 43% passing, 4 failures) by:
1. Reading actual production code (lancedb_handler.py) first
2. Converting integration tests to unit tests (integration test assumptions identified in Phase 301)
3. Mocking LanceDB connections instead of using real connections
4. Updating method signature calls to match actual signatures (search, insert)
5. Fixing fixture mocks (LanceDB client, vector operations)

Follow Phase 303 quality standards:
- Import from target module (already done)
- Use AsyncMock for external dependencies
- Test success + error paths
- Achieve >0% coverage (already done)
- Target 95%+ pass rate</action>
  <verify>pytest tests/test_lancedb_handler.py -v --tb=short | grep -E "(PASSED|FAILED|ERROR)" | wc -l</verify>
  <done>7 tests in test_lancedb_handler.py with 95%+ pass rate (7+ passing)</done>
</task>

<task type="auto">
  <name>Task 4: Fix test_episode_segmentation_service.py test failures</name>
  <files>tests/test_episode_segmentation_service.py</files>
  <action>Fix 7 tests in test_episode_segmentation_service.py (currently 0% passing) by:
1. Reading actual production code (episode_segmentation_service.py) first
2. Adding missing db argument to all test methods (critical issue from Phase 301)
3. Updating method signature calls to match actual signatures (segment_episode, detect_gaps)
4. Fixing fixture mocks (database sessions, episode queries)
5. Updating assertions to match actual return values

Critical issue from Phase 301 summary: "All tests fail due to missing db argument in fixture"

Follow Phase 303 quality standards:
- Import from target module (already done)
- Use AsyncMock for external dependencies
- Test success + error paths
- Achieve >0% coverage (already done)
- Target 95%+ pass rate</action>
  <verify>pytest tests/test_episode_segmentation_service.py -v --tb=short | grep -E "(PASSED|FAILED|ERROR)" | wc -l</verify>
  <done>7 tests in test_episode_segmentation_service.py with 95%+ pass rate (7+ passing)</done>
</task>

<task type="auto">
  <name>Task 5: Run PRE-CHECK verification</name>
  <files>tests/test_byok_handler.py, tests/test_lancedb_handler.py, tests/test_episode_segmentation_service.py</files>
  <action>Run Phase 303 PRE-CHECK verification to ensure:
1. All tests import from target modules (verify with grep)
2. All tests achieve >0% coverage (run pytest --cov)
3. All tests have 95%+ pass rate (run pytest -v)
4. No stub tests detected (coverage >0%)

Document results in a PRE-CHECK report.</action>
  <verify>grep "PRE-CHECK COMPLETE" tests/test_byok_handler.py || echo "PRE-CHECK report exists"</verify>
  <done>PRE-CHECK verification passed for all 3 files (imports, coverage, pass rate)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production | Test code reads production code to verify actual API |
| test → external services | LanceDB, LLM providers (must be mocked) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-305-04 | T (Tampering) | Test assertions could be weakened to pass failing tests | mitigate | Do not remove assertions, fix test data to match actual API |
| T-305-05 | I (Information disclosure) | Test code may expose LLM API keys | mitigate | Ensure all tests mock external services, no real API calls |
| T-305-06 | D (Denial of service) | Integration tests could hang on external service calls | mitigate | Convert all integration tests to unit tests with mocked dependencies |
</threat_model>

<verification>
## Phase-Level Verification

1. **Test Pass Rate Check**:
   ```bash
   pytest tests/test_byok_handler.py tests/test_lancedb_handler.py tests/test_episode_segmentation_service.py -v --tb=short | grep -E "(passed|failed|error)"
   ```
   Expected: 95%+ pass rate (51+ of 54 tests passing)

2. **Coverage Check**:
   ```bash
   pytest tests/test_byok_handler.py tests/test_lancedb_handler.py tests/test_episode_segmentation_service.py --cov=core.llm.byok_handler --cov=core.llm.lancedb_handler --cov=core.episode_segmentation_service --cov-report=term-missing
   ```
   Expected: >0% coverage for all 3 modules

3. **Import Check**:
   ```bash
   grep -h "^from core\." tests/test_byok_handler.py tests/test_lancedb_handler.py tests/test_episode_segmentation_service.py | head -10
   ```
   Expected: All files import from target modules

4. **No Stub Tests**:
   ```bash
   pytest tests/test_byok_handler.py tests/test_lancedb_handler.py tests/test_episode_segmentation_service.py --cov=core.llm.byok_handler --cov=core.llm.lancedb_handler --cov=core.episode_segmentation_service --cov-report=json | jq '.totals.percent_covered'
   ```
   Expected: Coverage >0% for all modules

5. **No Integration Tests**:
   ```bash
   grep -r "lancedb.connect\|OpenAI(" tests/test_byok_handler.py tests/test_lancedb_handler.py tests/test_episode_segmentation_service.py
   ```
   Expected: No external service calls (all mocked)
</verification>

<success_criteria>
- [ ] 54 tests fixed (40 + 7 + 7)
- [ ] 95%+ pass rate achieved (51+ tests passing)
- [ ] All tests import from target modules (verified)
- [ ] All tests achieve >0% coverage (verified)
- [ ] Fixture issues resolved (load_config → SessionLocal, db argument added)
- [ ] Integration tests converted to unit tests
- [ ] No external service calls (all mocked)
- [ ] PRE-CHECK verification passed
- [ ] No stub tests detected
- [ ] 305-02-SUMMARY.md created with deviation documentation
</success_criteria>

<output>
After completion, create `.planning/phases/305-quality-gates-final-verification/305-02-SUMMARY.md`
</output>
