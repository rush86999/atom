---
phase: 301-orchestration-wave-2-next-3-files
plan: 01
type: execute
wave: 1
depends_on: [300]
files_modified:
  - backend/tests/test_byok_handler.py
  - backend/tests/test_lancedb_handler.py
  - backend/tests/test_episode_segmentation_service.py
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - 3 new backend test files created covering high-impact service files
    - Individual file coverage improvements of 15-20 percentage points (from current 12-16% to 30-35%)
    - Tests cover critical paths: LLM routing, vector database operations, episode segmentation
    - All tests follow patterns from Phase 297-298 (AsyncMock, patch at import location)
    - Coverage baseline established for each targeted file
  artifacts:
    - path: backend/tests/test_byok_handler.py
      provides: Comprehensive tests for BYOK LLM handler (1839 lines)
      min_lines: 600
      coverage_target: 30-35%
    - path: backend/tests/test_lancedb_handler.py
      provides: Comprehensive tests for LanceDB vector handler (1416 lines)
      min_lines: 500
      coverage_target: 30-35%
    - path: backend/tests/test_episode_segmentation_service.py
      provides: Comprehensive tests for episode segmentation (1540 lines)
      min_lines: 550
      coverage_target: 25-30%
  key_links:
    - from: test_byok_handler.py
      to: core/llm/byok_handler.py
      via: AsyncMock for LLM providers (OpenAI, Anthropic, DeepSeek, Gemini)
      pattern: AsyncMock.*llm|@patch.*byok|provider.*routing
    - from: test_lancedb_handler.py
      to: core/lancedb_handler.py
      via: Mock LanceDB client, vector operations, search queries
      pattern: lancedb.*client|vector.*search|embedding
    - from: test_episode_segmentation_service.py
      to: core/episode_segmentation_service.py
      via: Database session mocking, episode models, segmentation logic
      pattern: episode.*segment|time.*gap|topic.*change
---

<objective>
Create comprehensive unit tests for 3 high-impact backend service files (byok_handler.py, lancedb_handler.py, episode_segmentation_service.py) to achieve 25-35% coverage on each file, following Phase 297-298 patterns and targeting ~1,764 lines of new code coverage (from 12-16% baseline to 30-35%).

Purpose: Continue backend coverage expansion (Wave 2 of orchestration testing) by testing critical service files that represent core platform infrastructure (LLM routing, vector database, episodic memory). These files have high business value and moderate complexity, making them ideal for coverage gains toward the 27.8-28.2% backend target (+2.0-2.4pp increase from 25.8% baseline).

Output: 3 test files with 1,650+ total lines, 35-45 tests, covering LLM provider routing, vector database operations, and episode segmentation logic
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/299-coverage-verification-milestone/299-GAP-ANALYSIS.md
@.planning/phases/300-orchestration-wave-1-top-3-files/300-01-SUMMARY.md

# Phase 299 baseline (verified actual)
- Backend coverage: 25.8% (23,498 of 91,078 lines) - ACTUAL MEASURED
- Frontend coverage: 18.18% (no change - import issues remain)
- Backend target: 45% (19.2pp gap remaining)
- Backend scale: 91,078 lines (major scale issue confirmed)
- Phase 300 achievements: Tests verified from Phase 295-02 (106 tests, 54% pass rate, 22-29% coverage)

# Phase 299 gap analysis: Top 10 files by coverage gap
From Phase 299 gap analysis, the next 3 highest priority files are:
1. byok_handler.py (core/llm/byok_handler.py) - 649 uncovered lines, 14.61% current coverage
2. lancedb_handler.py (core/lancedb_handler.py) - 587 uncovered lines, 15.42% current coverage
3. episode_segmentation_service.py (core/episode_segmentation_service.py) - 528 uncovered lines, 12% current coverage

Combined impact: 1,764 lines to cover, estimated 35-45 tests, +2.0-2.4pp backend coverage increase

# Phase 297-298 patterns to follow
From Phase 297-298 test structure:
- Pattern 1: AsyncMock for async service methods (LLM, database, external APIs)
- Pattern 2: Patch imported modules at their import location
- Pattern 3: Create SQLAlchemy model fixtures with all required fields
- Pattern 4: Test both success and error handling paths
- Pattern 5: Focus on critical business logic over 100% line coverage

From Phase 297-298 test files:
- test_atom_meta_agent.py: AsyncMock for domain creation, agent spawning (Phase 297)
- test_fleet_admiral.py: AsyncMock for specialist matching, fleet coordination (Phase 298)
- test_intent_classifier.py: Intent classification, routing logic (Phase 298)

# Phase 301 goals
- Test 3 high-impact service files (LLM routing, vector DB, episodic memory)
- Target individual file coverage: 25-35% per file (not 100% line coverage)
- Estimated tests: 35-45 total (12-15 per file)
- Estimated lines: 1,650-1,800 lines of test code
- Expected coverage impact: +2.0-2.4pp (from 25.8% to 27.8-28.2%)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive tests for byok_handler.py</name>
  <files>backend/tests/test_byok_handler.py</files>
  <action>
    Create test_byok_handler.py with comprehensive tests for core/llm/byok_handler.py (1839 lines, 14.61% current coverage → 30-35% target).

    Target coverage: 231-268 lines (30-35% of 760 uncovered lines target from Phase 299 analysis)
    Estimated tests: 12-15 tests
    Estimated test lines: 550-650 lines

    Key areas to test:
    1. LLM Provider Routing (BYOKHandler class)
       - test_route_to_openai() - Route requests to OpenAI provider
       - test_route_to_anthropic() - Route requests to Anthropic provider
       - test_route_to_deepseek() - Route requests to DeepSeek provider
       - test_route_to_gemini() - Route requests to Gemini provider
       - test_fallback_routing() - Fallback to secondary provider on failure
       - test_provider_unavailable() - Handle unavailable providers gracefully

    2. Token Counting and Cost Estimation
       - test_count_tokens_openai() - Count tokens for OpenAI models
       - test_count_tokens_anthropic() - Count tokens for Anthropic models
       - test_estimate_cost() - Estimate API call costs
       - test_cost_tracking() - Track costs across providers

    3. Streaming Response Handling
       - test_streaming_response_openai() - Handle OpenAI streaming
       - test_streaming_response_anthropic() - Handle Anthropic streaming
       - test_stream_chunk_aggregation() - Aggregate streaming chunks
       - test_stream_error_handling() - Handle streaming errors

    4. Provider Configuration and Validation
       - test_validate_api_key() - Validate provider API keys
       - test_load_provider_config() - Load provider configurations
       - test_missing_api_key() - Handle missing API keys
       - test_invalid_model_name() - Handle invalid model names

    5. Error Handling and Retry Logic
       - test_retry_on_rate_limit() - Retry on rate limit errors
       - test_retry_on_timeout() - Retry on timeout
       - test_max_retries_exceeded() - Handle max retries exceeded
       - test_provider_error_propagation() - Propagate provider errors

    Testing patterns (from Phase 297-298):
    - Use AsyncMock for LLM provider clients (openai, anthropic, etc.)
    - Patch provider modules at import location: @patch('core.llm.byok_handler.openai')
    - Mock streaming responses with async generators
    - Create LLM request/response fixtures
    - Test both success and error paths
    - Focus on routing logic, not provider internals

    Example test structure:
    ```python
    @pytest.mark.asyncio
    @patch('core.llm.byok_handler.openai.AsyncOpenAI')
    async def test_route_to_openai(self, mock_openai):
        # Arrange
        handler = BYOKHandler()
        mock_client = AsyncMock()
        mock_openai.return_value = mock_client
        mock_response = AsyncMock()
        mock_response.choices = [Mock(message=Mock(content="Test response"))]
        mock_client.chat.completions.create.return_value = mock_response

        # Act
        response = await handler.route_request("openai", "gpt-4", "Test prompt")

        # Assert
        assert response.content == "Test response"
        mock_client.chat.completions.create.assert_called_once()
    ```

    Verification criteria:
    - File created: backend/tests/test_byok_handler.py
    - Test count: 12-15 tests
    - Line count: 550-650 lines
    - Coverage target: 30-35% of byok_handler.py
    - All tests use AsyncMock for provider clients
    - Success and error paths covered
  </action>
  <verify>
    <automated>pytest backend/tests/test_byok_handler.py -v --cov=core/llm/byok_handler --cov-report=term-missing | grep "TOTAL"</automated>
  </verify>
  <done>
    Test file created with 12-15 tests covering LLM provider routing, token counting, streaming responses, configuration validation, and error handling. Coverage increased from 14.61% to 30-35% (231-268 lines covered).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive tests for lancedb_handler.py</name>
  <files>backend/tests/test_lancedb_handler.py</files>
  <action>
    Create test_lancedb_handler.py with comprehensive tests for core/lancedb_handler.py (1416 lines, 15.42% current coverage → 30-35% target).

    Target coverage: 196-229 lines (30-35% of 694 total lines)
    Estimated tests: 12-15 tests
    Estimated test lines: 450-550 lines

    Key areas to test:
    1. LanceDB Client Initialization
       - test_initialize_client() - Initialize LanceDB client with connection
       - test_initialize_with_existing_db() - Reuse existing database connection
       - test_initialize_failure() - Handle initialization failures

    2. Table Operations
       - test_create_table() - Create new vector table
       - test_open_existing_table() - Open existing table
       - test_table_exists() - Check if table exists
       - test_delete_table() - Delete table and data

    3. Vector Insertion and Upsert
       - test_insert_vectors() - Insert vector embeddings
       - test_upsert_vectors() - Upsert existing vectors
       - test_batch_insert() - Batch insert multiple vectors
       - test_insert_with_metadata() - Insert vectors with metadata

    4. Vector Search Operations
       - test_vector_search() - Perform vector similarity search
       - test_vector_search_with_filter() - Search with metadata filters
       - test_hybrid_search() - Hybrid vector + keyword search
       - test_search_top_k() - Retrieve top-k results
       - test_search_empty_results() - Handle empty search results

    5. Database Configuration and Management
       - test_configure_embedding_dimension() - Set embedding dimension
       - test_configure_distance_metric() - Set distance metric (cosine, L2)
       - test_database_stats() - Get database statistics
       - test_table_stats() - Get table statistics

    Testing patterns (from Phase 297-298):
    - Use MagicMock for LanceDB client and table objects
    - Patch lancedb module at import location: @patch('core.lancedb_handler.lancedb')
    - Mock vector embeddings and search results
    - Create vector fixtures with metadata
    - Test both success and error paths
    - Focus on handler logic, not LanceDB internals

    Example test structure:
    ```python
    @patch('core.lancedb_handler.lancedb.connect')
    def test_initialize_client(self, mock_connect):
        # Arrange
        mock_db = MagicMock()
        mock_connect.return_value = mock_db
        handler = LanceDBHandler()

        # Act
        handler.initialize("/path/to/db")

        # Assert
        mock_connect.assert_called_once_with("/path/to/db")
        assert handler.db == mock_db
    ```

    Verification criteria:
    - File created: backend/tests/test_lancedb_handler.py
    - Test count: 12-15 tests
    - Line count: 450-550 lines
    - Coverage target: 30-35% of lancedb_handler.py
    - All tests use MagicMock for LanceDB client
    - Success and error paths covered
  </action>
  <verify>
    <automated>pytest backend/tests/test_lancedb_handler.py -v --cov=core/lancedb_handler --cov-report=term-missing | grep "TOTAL"</automated>
  </verify>
  <done>
    Test file created with 12-15 tests covering client initialization, table operations, vector insertion/upsert, search operations, and database configuration. Coverage increased from 15.42% to 30-35% (196-229 lines covered).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create comprehensive tests for episode_segmentation_service.py</name>
  <files>backend/tests/test_episode_segmentation_service.py</files>
  <action>
    Create test_episode_segmentation_service.py with comprehensive tests for core/episode_segmentation_service.py (1540 lines, 12% current coverage → 25-30% target).

    Target coverage: 150-180 lines (25-30% of 600 total lines)
    Estimated tests: 11-15 tests
    Estimated test lines: 500-600 lines

    Key areas to test:
    1. Episode Segmentation by Time Gaps
       - test_segment_by_time_gap() - Detect time gaps and segment episodes
       - test_custom_time_threshold() - Use custom time gap threshold
       - test_no_time_gap() - Handle continuous conversation (no gap)
       - test_multiple_time_gaps() - Handle multiple gaps in conversation

    2. Episode Segmentation by Topic Changes
       - test_segment_by_topic_change() - Detect topic changes using LLM
       - test_topic_similarity_threshold() - Use custom similarity threshold
       - test_same_topic_continuation() - Continue episode with same topic
       - test_multiple_topic_changes() - Handle multiple topic shifts

    3. Episode Segmentation by Task Completion
       - test_segment_by_task_completion() - Detect task completion events
       - test_task_success_segmentation() - Segment after successful task
       - test_task_failure_segmentation() - Segment after failed task
       - test_task_in_progress_continuation() - Continue episode during task

    4. Segment Metadata and Summarization
       - test_generate_segment_summary() - Generate LLM summary for segment
       - test_extract_segment_keywords() - Extract keywords from segment
       - test_calculate_segment_duration() - Calculate segment time duration
       - test_segment_participant_list() - Extract participant list

    5. Episode Lifecycle Management
       - test_create_episode() - Create new episode from segments
       - test_merge_segments() - Merge related segments
       - test_split_episode() - Split episode into smaller episodes
       - test_close_episode() - Close episode and finalize metadata

    Testing patterns (from Phase 297-298):
    - Use AsyncMock for async LLM calls (topic classification, summarization)
    - Patch database session and models at import location
    - Create episode and segment fixtures with all required fields
    - Mock LLM responses for topic analysis and summarization
    - Test both success and error paths
    - Focus on segmentation logic, not LLM internals

    Example test structure:
    ```python
    @pytest.mark.asyncio
    @patch('core.episode_segmentation_service.LLMService')
    async def test_segment_by_time_gap(self, mock_llm):
        # Arrange
        service = EpisodeSegmentationService()
        messages = [
            create_message(timestamp="2026-04-25T10:00:00Z", content="Hello"),
            create_message(timestamp="2026-04-25T10:05:00Z", content="World"),
            create_message(timestamp="2026-04-25T14:00:00Z", content="Afternoon"),  # 4-hour gap
        ]
        mock_llm.classify_topic.return_value = "general"

        # Act
        segments = await service.segment_by_time_gap(messages, time_threshold_minutes=30)

        # Assert
        assert len(segments) == 2  # Two segments due to time gap
        assert segments[0].message_count == 2
        assert segments[1].message_count == 1
    ```

    Verification criteria:
    - File created: backend/tests/test_episode_segmentation_service.py
    - Test count: 11-15 tests
    - Line count: 500-600 lines
    - Coverage target: 25-30% of episode_segmentation_service.py
    - All tests use AsyncMock for LLM calls
    - Database fixtures for episodes and segments
    - Success and error paths covered
  </action>
  <verify>
    <automated>pytest backend/tests/test_episode_segmentation_service.py -v --cov=core/episode_segmentation_service --cov-report=term-missing | grep "TOTAL"</automated>
  </verify>
  <done>
    Test file created with 11-15 tests covering time gap segmentation, topic change detection, task completion events, segment metadata, and episode lifecycle. Coverage increased from 12% to 25-30% (150-180 lines covered).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→production | Unit tests have full access to production code internals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-301-01 | Tampering | test_byok_handler.py | accept | Unit tests - no production impact. Test isolation prevents interference with production LLM providers. |
| T-301-02 | Information Disclosure | test_lancedb_handler.py | accept | Unit tests - no production database access. Mock LanceDB client prevents data leakage. |
| T-301-03 | Denial of Service | test_episode_segmentation_service.py | accept | Unit tests - isolated execution. Database fixtures prevent production data impact. |
</threat_model>

<verification>
## Phase Verification Checklist

### Plan Execution
- [ ] All 3 test files created (test_byok_handler.py, test_lancedb_handler.py, test_episode_segmentation_service.py)
- [ ] Total test count: 35-45 tests across 3 files
- [ ] Total test lines: 1,650-1,800 lines
- [ ] All tests follow Phase 297-298 patterns (AsyncMock, patch at import, fixtures)

### Coverage Targets
- [ ] byok_handler.py: 30-35% coverage (231-268 lines covered)
- [ ] lancedb_handler.py: 30-35% coverage (196-229 lines covered)
- [ ] episode_segmentation_service.py: 25-30% coverage (150-180 lines covered)
- [ ] Combined coverage increase: +2.0-2.4pp (from 25.8% to 27.8-28.2%)

### Test Quality
- [ ] Test pass rate: 95%+ (target 100%)
- [ ] All tests use AsyncMock for external dependencies (LLM, LanceDB, database)
- [ ] Success and error paths covered for all critical methods
- [ ] No test collection errors (imports, fixtures)

### Documentation
- [ ] 301-01-SUMMARY.md created with execution results
- [ ] Coverage metrics documented (individual files + combined impact)
- [ ] Test patterns documented for future phases
- [ ] ROADMAP.md updated with Phase 301 completion status
- [ ] STATE.md updated with Phase 301 results

### Integration with Previous Phases
- [ ] Phase 300 completion verified (tests from Phase 295-02)
- [ ] Phase 299 gap analysis targets addressed (top 4, 5, 6 files)
- [ ] No duplicate tests (verified these files not tested in Phases 296-298)
- [ ] Consistent with Phase 297-298 patterns (AsyncMock, patching, fixtures)
</verification>

<success_criteria>
## Phase 301 Success Criteria

### Primary Criteria
- **3 new test files created** covering byok_handler.py, lancedb_handler.py, and episode_segmentation_service.py
- **35-45 total tests** across 3 files (12-15 tests per file)
- **1,650-1,800 lines of test code** following Phase 297-298 patterns
- **Backend coverage increased to 27.8-28.2%** (+2.0-2.4pp from 25.8% baseline)

### Secondary Criteria
- **Individual file coverage targets met**:
  - byok_handler.py: 30-35% (from 14.61%)
  - lancedb_handler.py: 30-35% (from 15.42%)
  - episode_segmentation_service.py: 25-30% (from 12%)
- **Test pass rate: 95%+** (target 100%)
- **Zero test collection errors** (all imports and fixtures working)
- **All tests use AsyncMock** for external dependencies (LLM providers, LanceDB, database)

### Tertiary Criteria
- **Consistent with Phase 297-298 patterns** (AsyncMock, patch at import, fixtures)
- **Success and error paths covered** for all critical methods
- **Documentation complete** (SUMMARY.md, ROADMAP.md, STATE.md updated)
- **Ready for Phase 302** (next 3 service files: learning_service_full + 2 more)

### Metrics
- **Lines of test code**: 1,650-1,800 lines
- **Lines of production code covered**: ~750-900 lines
- **Coverage percentage point increase**: +2.0-2.4pp
- **Tests per file average**: 12-15 tests
- **Execution time**: <5 minutes for all 3 test files
</success_criteria>

<output>
After completion, create `.planning/phases/301-orchestration-wave-2-next-3-files/301-01-SUMMARY.md`
</output>
