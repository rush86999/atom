---
phase: 220
plan: 220-01
type: fix
wave: 1
depends_on: []
files_modified:
  - core/industry_workflow_endpoints.py
  - tests/unit/test_industry_workflow_endpoints.py
autonomous: true
must_haves:
  - All 17 industry workflow tests passing with 0 failures
  - Duplicate test file removed from tests/api/services/
  - Real template IDs used in test fixtures (healthcare_patient_onboarding, retail_inventory_management, manufacturing_quality_control)
  - ROI request validation fixed (template_id removed from request body)
  - No regressions in other tests (overall pass rate >= 98%)
---

<objective>
Fix all 5 failing tests in the industry workflow endpoint test suite by removing duplicate test file, fixing ROI request validation, and updating test fixtures to use real template IDs.

Purpose: Tests are failing due to (1) duplicate test file with incorrect mocking, (2) Pydantic 224 validation errors when template_id appears in both path parameter and request body, (3) placeholder template IDs that don't exist in actual templates.

Output: 17/17 passing tests with 0 failures, overall pass rate >= 98%
</objective>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@backend/core/industry_workflow_endpoints.py
@backend/core/industry_workflow_templates.py
@backend/tests/unit/test_industry_workflow_endpoints.py
</context>

<tasks>

<task type="auto">
  <name>Remove duplicate test file and fix ROI request model</name>
  <files>
    DELETE: tests/api/services/test_industry_workflow_endpoints.py
    MODIFY: core/industry_workflow_endpoints.py
  </files>
  <action>
    1. Delete duplicate test file: `rm tests/api/services/test_industry_workflow_endpoints.py`

    2. Fix ROICalculationRequest Pydantic model in core/industry_workflow_endpoints.py (lines 21-23):
       - Remove the `template_id: str` field from the request body
       - The endpoint is `POST /api/v1/templates/{template_id}/roi` so template_id is already in the path
       - Keep only `hourly_rate: float` field with default value 50.0

    After fix, the model should look like:
    ```python
    class ROICalculationRequest(BaseModel):
        hourly_rate: float = Field(50.0, description="Hourly rate for time value calculation")
    ```

    This fixes the Pydantic 422 validation error caused by template_id being in both path and request body.
  </action>
  <verify>
    - File `tests/api/services/test_industry_workflow_endpoints.py` no longer exists
    - ROICalculationRequest model has only `hourly_rate` field (grep -A2 "class ROICalculationRequest")
  </verify>
  <done>
    Duplicate test file is deleted. ROI request model no longer has template_id field, resolving Pydantic 422 validation errors.
  </done>
</task>

<task type="auto">
  <name>Update test fixtures to use real template IDs</name>
  <files>
    MODIFY: tests/unit/test_industry_workflow_endpoints.py
  </files>
  <action>
    Replace placeholder template IDs with real template IDs that exist in core/industry_workflow_templates.py:

    1. Verify real template IDs exist: `grep -E 'id="(healthcare_patient_onboarding|retail_inventory_management|manufacturing_quality_control)"' core/industry_workflow_templates.py`

    2. In tests/unit/test_industry_workflow_endpoints.py, find and replace:
       - "test_template_1" -> "healthcare_patient_onboarding"
       - "test_template_2" -> "retail_inventory_management"
       - "test_template_3" -> "manufacturing_quality_control"
       - Any other placeholder IDs -> appropriate real template ID

    3. Update test data to match real template structure:
       - Use proper industry values (healthcare, retail, manufacturing)
       - Match complexity levels (Beginner, Intermediate, Advanced)
       - Include appropriate integration lists

    This fixes "template not found" errors in failing tests like test_get_supported_industries_empty, test_search_templates_no_filters.
  </action>
  <verify>
    - No instances of "test_template_" placeholder IDs remain in test file
    - Template IDs used (healthcare_patient_onboarding, retail_inventory_management, manufacturing_quality_control) exist in core/industry_workflow_templates.py
  </verify>
  <done>
    All placeholder template IDs replaced with real template IDs. Test data matches actual template structure, eliminating "template not found" errors.
  </done>
</task>

<task type="auto">
  <name>Fix exception handling test and ROI not found test</name>
  <files>
    MODIFY: tests/unit/test_industry_workflow_endpoints.py
  </files>
  <action>
    Fix the exception handling test that's not properly triggering endpoint error handling:

    1. For `test_endpoint_exception_handling` (around lines 345-353):
       - The endpoint uses FastAPI dependency injection: `engine: IndustryWorkflowEngine = Depends(get_industry_workflow_engine)`
       - Mock at the dependency level: `patch('core.industry_workflow_endpoints.get_industry_workflow_engine')`
       - Configure the mock to raise exception: `mock_get_engine.return_value.get_all_industries.side_effect = Exception("Test error")`

    2. For `test_calculate_roi_template_not_found`:
       - Verify expected status code is 404 (FastAPI default for HTTPException)
       - Check that assertion uses correct error response format
       - Ensure test uses a real template_id (like "nonexistent_template") not a placeholder

    3. Verify error response assertions match FastAPI error format: `{"detail": "error message"}`

    The key insight: exception handling test must mock the dependency injection return value, not the method directly.
  </action>
  <verify>
    - test_endpoint_exception_handling passes with correct mock path
    - test_calculate_roi_template_not_found expects 404 status code
    - Error response assertions match FastAPI format
  </verify>
  <done>
    Exception handling test properly triggers endpoint error handling. ROI not found test has correct expected status codes and assertion logic.
  </done>
</task>

<task type="auto">
  <name>Run tests and verify all pass</name>
  <files>
    NO FILES - verification only
  </files>
  <action>
    Run industry workflow test suite and verify all tests pass:

    1. Run industry workflow tests: `pytest tests/unit/test_industry_workflow_endpoints.py -v`
    2. Verify output shows 17 passed, 0 failed
    3. Run full test suite to check for regressions: `pytest tests/ -x --tb=short`
    4. Confirm overall pass rate >= 98%

    If any failures remain, diagnose root cause and fix before marking complete.
  </action>
  <verify>
    - Command `pytest tests/unit/test_industry_workflow_endpoints.py -v` shows 17/17 passed
    - Full test suite pass rate >= 98%
    - 0 failures in industry workflow tests
  </verify>
  <done>
    All 17 industry workflow tests pass with 0 failures. No regressions introduced in other tests. Overall test pass rate >= 98%.
  </done>
</task>

</tasks>

<verification>
- [ ] All 17 industry workflow tests pass (0 failures)
- [ ] Duplicate test file `test_industry_workflow_endpoints.py` removed from tests/api/services/
- [ ] Template ID mismatches fixed (using real template IDs like "healthcare_patient_onboarding")
- [ ] ROI request validation fixed (template_id removed from request body)
- [ ] No regressions in other tests
- [ ] Overall test pass rate >= 98%
</verification>

<success_criteria>
1. pytest tests/unit/test_industry_workflow_endpoints.py -v shows 17 passed
2. tests/api/services/test_industry_workflow_endpoints.py does not exist
3. ROICalculationRequest model has no template_id field
4. All test placeholders use real template IDs from industry_workflow_templates.py
5. Overall test suite pass rate >= 98%
</success_criteria>

<output>
After completion, create `.planning/phases/220-fix-industry-workflow-test-failures/220-01-SUMMARY.md`
</output>
