# HubSpot Integration Environment Configuration Template
# Copy this file to .env.hubspot and fill in your actual credentials

# Required for OAuth 2.0 Authentication
HUBSPOT_CLIENT_ID=your-oauth-client-id
HUBSPOT_CLIENT_SECRET=your-oauth-client-secret
HUBSPOT_REDIRECT_URI=http://localhost:5058/auth/hubspot/callback

# Optional - Environment Selection
HUBSPOT_ENVIRONMENT=production  # production or qa (for testing)

# Optional - Token Authentication (for private apps)
HUBSPOT_ACCESS_TOKEN=your-access-token
HUBSPOT_REFRESH_TOKEN=your-refresh-token
HUBSPOT_PRIVATE_APP_TOKEN=your-private-app-token

# HubSpot API Documentation:
# https://developers.hubspot.com/docs/api/overview

# Getting Started:
# 1. OAuth 2.0 Setup (Recommended):
#    - Go to https://developers.hubspot.com/apps
#    - Create new app and select OAuth 2.0
#    - Configure redirect URI
#    - Set required scopes
#    - Fill in HUBSPOT_CLIENT_ID, HUBSPOT_CLIENT_SECRET, and HUBSPOT_REDIRECT_URI
#
# 2. Private App Setup (Alternative):
#    - Go to https://app.hubspot.com/settings/private-apps
#    - Create private app
#    - Set required permissions and scopes
#    - Generate access token
#    - Fill in HUBSPOT_PRIVATE_APP_TOKEN and optional HUBSPOT_ACCESS_TOKEN
#
# 3. Required Scopes:
#    - contacts: Contact management
#    - crm.objects.companies.read/write: Company management
#    - crm.objects.contacts.read/write: Contact management
#    - crm.objects.deals.read/write: Deal management
#    - marketing.campaigns.read/write: Marketing campaigns
#    - crm.lists.read/write: Lead list management
#
# 4. Testing Configuration:
#    - Use .env.hubspot.local for development
#    - Use .env.hubspot.production for production
#    - Never commit actual credentials to version control

# Advanced Configuration:
# - Multiple portal support for multi-account operations
# - Webhook configuration for real-time updates
# - Batch operations for improved performance
# - Custom fields and metadata support
# - Marketing automation workflows
# - Lead nurturing campaigns
# - Email marketing templates