# Zendesk Integration Environment Configuration Template
# Copy this file to .env.zendesk and fill in your actual credentials

# Required for basic authentication (API Token)
ZENDESK_SUBDOMAIN=your-subdomain
ZENDESK_EMAIL=your-email@example.com
ZENDESK_TOKEN=your-api-token

# Optional - OAuth 2.0 Configuration (for user authentication)
ZENDESK_CLIENT_ID=your-oauth-client-id
ZENDESK_CLIENT_SECRET=your-oauth-client-secret
ZENDESK_REDIRECT_URI=http://localhost:5058/auth/zendesk/callback

# Optional - OAuth Scopes (comma-separated)
ZENDESK_SCOPES=tickets:read tickets:write users:read organizations:read

# Zendesk API Documentation:
# https://developer.zendesk.com/api-reference/ticketing/tickets

# Getting Started:
# 1. Basic Authentication (simpler):
#    - Go to Admin > Channels > API
#    - Create new API token
#    - Fill in ZENDESK_SUBDOMAIN, ZENDESK_EMAIL, and ZENDESK_TOKEN
#
# 2. OAuth 2.0 (recommended for production):
#    - Go to Admin > Channels > API > OAuth Clients
#    - Create new OAuth client
#    - Fill in ZENDESK_CLIENT_ID, ZENDESK_CLIENT_SECRET, and ZENDESK_REDIRECT_URI
#    - Configure the redirect URI to match your application

# Testing Configuration:
# - Use .env.zendesk.local for development
# - Use .env.zendesk.production for production
# - Never commit actual credentials to version control