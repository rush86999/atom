[pytest]
# Override parent conftest for unit tests
# Don't use pytest-playwright for these unit tests
testpaths = .
python_files = test_*.py
addopts =
    --no-cov
    -v
    --tb=short
    -p no:playwright
