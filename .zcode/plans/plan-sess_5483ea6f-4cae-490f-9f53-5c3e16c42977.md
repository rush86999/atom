## Goal
Update all docs to reflect the latest features (esp. Swarm Coordination) and fix the broken setup instructions that block new users. Ensure **every environment variable is documented with a working default** in both Quick Start and Regular Start paths. Canonical ground truth:
- Entrypoint = `main_api_app:app` (NOT `main:app` — `backend/main.py` doesn't exist)
- Backend dev port = `8001` (per README + Makefile)
- A real `docker-compose-personal.yml` needs to be created

## Work items

### A. Create the missing `docker-compose-personal.yml` (root)
Write a fresh, correct personal-edition compose (the archived one in `archive/2026-07-consolidation/` launches the non-existent `main:app` and references removed Phase 66 / Spotify / Hue integrations — not salvageable). New file:
- **SQLite** backend (no Postgres), single-user, zero external services by default
- Launches `uvicorn main_api_app:app` (the full app), port `8001:8000`
- Frontend on `:3001`, `NEXT_PUBLIC_API_URL=http://localhost:8001`
- Auto-generates/reads `SECRET_KEY`, `JWT_SECRET_KEY`, `BYOK_ENCRYPTION_KEY` from `.env` (required, no insecure defaults — matches the existing full-stack compose's `${VAR:?...}` pattern)
- Optional Ollama passthrough via `ATOM_LOCAL_ONLY` + `OLLAMA_BASE_URL`
- Browser-node + piece-engine commented out by default (personal edition doesn't need them)

### B. Reconcile `backend/.env.example` and root `.env.example`
- Audit both so **every** var referenced in `backend/core/config.py` appears with a real default value (not `your-X-here` placeholders for non-secret vars).
- Group logically, mark secret vars clearly (`# REQUIRED` / `# OPTIONAL`).
- Add the vars missing from the templates but present in code: `OLLAMA_BASE_URL`/`OLLAMA_MODEL`, `ADMIN_PASSWORD`, `DISABLE_AUTH_RATE_LIMIT`, `ATOM_LOCAL_ONLY`, `ATOM_LEARNING_ROUTER`, `TAVILY_API_KEY`, `BRAVE_SEARCH_API_KEY`, `NODE_ENGINE_URL`, etc.
- De-duplicate the `MARKETPLACE_*` vs `ATOM_SAAS_*` divergence (both are read by code — keep both, cross-reference).

### C. New reference doc: `docs/reference/ENVIRONMENT_VARIABLES.md`
A single canonical table of every env var: name, default, required?, what it does, which file to set it in (backend/.env vs frontend .env.local vs root .env for Docker). This becomes the link target for "all env vars."

### D. Fix all primary onboarding docs (entrypoint, port, compose file)
Rewrite launch commands to `main_api_app:app` + port `8001`, point Docker at the new `docker-compose-personal.yml`, add Swarm Coordination to feature lists:
- `README.md` — Quick Start already correct (main_api_app + 8001); fix Docker section (`docker-compose-personal.yml` now exists), add Swarm Coordination to Key Features + Advanced docs, add env-var reference link.
- `CONTRIBUTING.md` — fix `main:app`→`main_api_app:app`, port 8000→8001, add venv step, drop non-existent `CHANGELOG.md` reference.
- `docs/getting_started/quick-start.md` — full rewrite: `main_api_app:app`, port 8001, fix `docker-compose-personal.yml` reference, update "Last Updated."
- `docs/getting_started/README.md` — same fixes; fix Docker option.
- `docs/getting_started/run-with-ollama.md` — fix `main:app`, port, compose ref; clarify `dev.sh` runs minimal smoke app (full app = `make backend`).
- `docs/getting_started/TROUBLESHOOTING.md` — **critical**: lines 50-57 actively tell users `main_api_app:app` is "broken" and to use `main:app` — this is exactly backwards. Rewrite to correct entrypoint + port 8001.
- `docs/getting_started/INSTALLATION.md` — fix the stale `docker-compose.yml` snippet (references non-existent `ATOM_DMM_LEVEL5_ENABLED`, wrong image tags) → point at the new personal compose + canonical commands.
- `docs/guides/QUICKSTART.md` — fix `main:app`, port 8000→8001, compose ref; add env-var link.
- `docs/RELEASE_NOTES.md` — add Swarm Coordination section; fix `docker-compose-personal.yml` ref; note `dev.sh` = minimal.

### E. Fix secondary docs (same entrypoint/port/compose corrections + Swarm Coordination where feature lists exist)
- `docs/operations/personal-edition.md` (~30 refs to personal compose) — update to real file + canonical commands.
- `docs/operations/README.md`, `docs/platform/README.md`, `docs/marketplace/README.md`, `docs/deployment/README.md` — fix compose refs.
- `docs/guides/MARKETPLACE_QUICKSTART.md`, `docs/guides/OAUTH_SETUP_CHECKLIST.md`, `docs/guides/OAUTH_QUICK_SETUP_GUIDE.md` — fix `main:app`.
- `docs/SHOWCASE.md`, `benchmarks/RESULTS.md` — note `dev.sh` runs minimal; full app = `make backend`.
- `docs/operations/performance.md`, `docs/testing/e2e-guide.md`, `docs/testing/STRESS_TESTING_CI_CD.md`, `docs/testing/QUALITY_ASSURANCE.md` — fix `main:app`/port.
- `docs/architecture/SYSTEM_OVERVIEW.md` — fix non-existent `./start-dev.sh` → `scripts/dev.sh` (or `make backend`).
- `backend/tests/{browser_discovery,load,stress}/README.md` — fix `main:app`→`main_api_app:app`.

### F. Fix archive/legacy + planning docs (all markdown, per your "all markdown files" choice)
- `docs/archive/legacy/*`, `docs/archive/implementation/*`, `docs/archive/oauth/*`, `.archive/**`, `.planning/**`, `archive/2026-07-consolidation/**` — mechanical `main:app`→`main_api_app:app` and `docker-compose-personal.yml` path corrections. These are historical/internal but you asked for repo-wide, so I'll correct the references that would mislead (entrypoint + compose file). Lower-effort per file.

### G. Not touched
- `scripts/dev.sh` — per your decision, left as-is (still launches `minimal_app`). I'll add a one-line note in docs that it's a smoke check.
- `node_modules/`, `backend/venv/`, `backend/piece-engine/node_modules/` — vendored, excluded.

## Verification
- `grep -rn "main:app" --include=*.md` → only legitimate refs (e.g. the separate `infra/atomic-docker/.../main.py` worker, and notes explaining the distinction).
- `grep -rn "docker-compose-personal.yml" --include=*.md` → all resolve to the new real file.
- New `docker-compose-personal.yml` passes `docker compose -f docker-compose-personal.yml config` (syntax validation only — won't actually build images).
- Env-var reference doc cross-checked against `backend/core/config.py` getenv calls.

## Notes / trade-offs
- I will NOT run the apps or build Docker images (too slow / network-dependent). Verification is static: grep sweeps + `docker compose config` syntax check.
- I will not change runtime behavior of any script other than creating the new compose file.
- I'll commit nothing unless you ask.