# Plan: Add Kimi K3, GPT 5.6 Sol, Claude Mythos 5 to BYOK System

## Background

Three new models need registration across the BYOK system. **Fable (claude-fable-5) already exists** in the registry as `fable-5` (learning_llm_router.py:612) — no changes needed for it. The three NEW models are:

| Model | ID | Provider | Cost (In/Out per 1M) | Context | ELO | Quality (0-1) | Speed |
|-------|-----|----------|----------------------|---------|-----|--------------|-------|
| Kimi K3 | `kimi-k3` | moonshot | $2.90 / $14.00 | 1M | 1485 | 0.94 | 0.75 |
| GPT 5.6 Sol | `gpt-5.6-sol` | openai | $5.00 / $30.00 | 1.05M | 1484 | 0.98 | 0.72 |
| Claude Mythos 5 | `claude-mythos-5` | anthropic | $10 / $50 (avg $30/M) | 1M | 1508 | 0.99 | 0.60 |

All use **existing providers** (moonshot/openai/anthropic) — no new provider config, base_url, or CACHE_CAPABILITIES entries needed.

## Automation note

The system already has a `dynamic_benchmark_fetcher.py` that pulls quality scores from LMSYS / Artificial Analysis / Benchmark.moe at runtime. The static entries below are **fallbacks** for when the dynamic fetcher fails (cold start, API down). Once these model names appear in the upstream benchmark APIs, the dynamic path auto-updates their scores — no code change needed.

## Changes (7 files)

### 1. `core/learning_llm_router.py` — Add 3 ModelSpec entries
Add to `_initialize_model_registry`, following the existing `kimi-k2.6` / `gpt-5.5` / `fable-5` patterns:
- `kimi-k3`: moonshot, capabilities {CODE, REASONING, VISION, LONG_CONTEXT, HIGH_QUALITY}, cost 8.45 (avg), quality 0.94, speed 0.75, ctx 1M, cache=True, tier=premium, api_region=international
- `gpt-5.6-sol`: openai, capabilities {CODE, REASONING, TOOL_USE, LONG_CONTEXT, HIGH_QUALITY}, cost 17.50 (avg), quality 0.98, speed 0.72, ctx 1050000, cache=True, tier=premium
- `claude-mythos-5`: anthropic, capabilities {VISION, CODE, REASONING, LONG_CONTEXT, HIGH_QUALITY}, cost 30.00 (avg), quality 0.99, speed 0.60, ctx 1000000, cache=True, tier=premium

### 2. `core/llm/byok_handler.py` — Update COST_EFFICIENT_MODELS
Update the complexity→model mappings for the three providers to point at the new top-tier models:
- moonshot: COMPLEX/ADVANCED → `kimi-k3` (was `kimi-k2.6`)
- openai: ADVANCED → `gpt-5.6-sol` (was `o3`)
- anthropic: ADVANCED → `claude-mythos-5` (was `claude-4-opus`)

### 3. `core/benchmarks.py` — Add MODEL_QUALITY_SCORES entries
Static fallback scores (0-100 scale):
- `"kimi-k3": 94`
- `"gpt-5.6-sol": 98`
- `"claude-mythos-5": 99`

### 4. `core/llm/cognitive_tier_service.py` — Update TIER_MODELS
Add the new models to the appropriate CognitiveTier recommendations (COMPLEX tier for the frontier models). No new `_model_to_provider` prefix branches needed — `kimi`, `gpt`, `claude` prefixes already exist.

### 5. `core/cost_config.py` — Add MODEL_COSTS static entries
Per-token fallback pricing:
- `"kimi-k3": {"input": 0.0000029, "output": 0.000014}`
- `"gpt-5.6-sol": {"input": 0.000005, "output": 0.00003}`
- `"claude-mythos-5": {"input": 0.00001, "output": 0.00005}`

### 6. `core/llm/context/token_counter.py` — Add MODEL_CONTEXT_LIMITS
- `"kimi-k3": 1000000`
- `"gpt-5.6-sol": 1050000`
- `"claude-mythos-5": 1000000`

### 7. `core/dynamic_pricing_fetcher.py` — Add pricing fallbacks + vision keywords
- Add `kimi-k3` to the `_kimi_models` fallback list
- Add `gpt-5.6-sol` and `claude-mythos-5` to the vision keyword list (they support vision)

## What's NOT needed
- **Fable**: already registered as `fable-5` / `claude-fable-5` (line 612). No changes.
- **New provider config**: all three use existing providers (moonshot/openai/anthropic). Base URLs, API key env vars, CACHE_CAPABILITIES already configured.
- **`byok_endpoints.py` provider catalog**: the providers (openai/anthropic/moonshot) already have catalog entries. The new models are accessible via existing provider configs — users select the provider and the model name.
- **DB migration**: models are static registry entries, not DB rows.

## Tests
- Verify the 3 new models appear in `_model_registry` with correct specs
- Verify `get_quality_score` returns the static fallback for each
- Verify `COST_EFFICIENT_MODELS` routes to the new models for their providers
- Run existing routing + BPC test suites to confirm no regressions

## Run + push
- Run pytest routing + BPC suites
- Branch, commit, merge to main, push
