# Android Keystore Configuration
# Copy this file to keystore.properties and fill in your values

# Keystore file path (relative to android/app directory)
KEYSTORE_FILE=../atom-keystore.jks

# Keystore password
KEYSTORE_PASSWORD=your-keystore-password

# Key alias
KEY_ALIAS=atom-key-alias

# Key password
KEY_PASSWORD=your-key-password

# ============================================================================
# IMPORTANT SECURITY NOTES
# ============================================================================

# 1. NEVER commit keystore.properties to version control
# 2. Add keystore.properties to .gitignore
# 3. Store the keystore file (atom-keystore.jks) securely
# 4. Keep backups of the keystore file and passwords
# 5. If you lose the keystore, you cannot update your app

# ============================================================================
# Keystore Management Best Practices
# ============================================================================

# - Use a strong, unique password (20+ characters)
# - Store passwords in a secure password manager
# - Keep the keystore file in a secure, backed-up location
# - Use different keystores for development and production
# - Document keystore location and passwords (securely)

# ============================================================================
# Development vs Production
# ============================================================================

# Development: Use debug keystore (Android SDK creates this automatically)
# Production: Use release keystore (create with: keytool -genkeypair...)

# To create a production keystore:
# keytool -genkeypair -v -keystore atom-keystore.jks \
#   -keyalg RSA -keysize 2048 -validity 10000 \
#   -alias atom-key-alias

# To verify keystore contents:
# keytool -list -v -keystore atom-keystore.jks
