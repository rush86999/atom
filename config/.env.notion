# ATOM Development Environment Configuration
# This file provides PostgreSQL configuration for Notion integration

# -------------------------------------------------------------------------
# DATABASE CONFIGURATION (PostgreSQL)
# -------------------------------------------------------------------------

# PostgreSQL Database URL
DATABASE_URL=postgresql://atom_user:atom_password@localhost:5432/atom_development

# -------------------------------------------------------------------------
# NOTION INTEGRATION CONFIGURATION
# -------------------------------------------------------------------------

# Notion OAuth Configuration
NOTION_CLIENT_ID=your_notion_client_id
NOTION_CLIENT_SECRET=your_notion_client_secret
NOTION_REDIRECT_URI=http://localhost:5058/api/auth/notion/callback

# -------------------------------------------------------------------------
# ENCRYPTION & SECURITY
# -------------------------------------------------------------------------

# OAuth Token Encryption
ATOM_OAUTH_ENCRYPTION_KEY=your-32-byte-base64-encryption-key-here

# Flask Secret Key
FLASK_SECRET_KEY=atom-dev-secret-key-change-in-production

# -------------------------------------------------------------------------
# API SERVICE CONFIGURATION
# -------------------------------------------------------------------------

# Python API Service
PYTHON_API_SERVICE_BASE_URL=http://localhost:5058

# -------------------------------------------------------------------------
# INSTRUCTIONS
# -------------------------------------------------------------------------

# 1. Make sure PostgreSQL is running:
#    brew services start postgresql  (macOS)
#    or
#    docker run --name postgres-dev -e POSTGRES_PASSWORD=atom_password -e POSTGRES_USER=atom_user -e POSTGRES_DB=atom_development -p 5432:5432 -d postgres:latest

# 2. Create database and user:
#    createdb atom_development
#    createuser atom_user

# 3. Update NOTION_CLIENT_ID and NOTION_CLIENT_SECRET with actual values
#    from https://www.notion.so/my-integrations

# 4. Generate encryption key:
#    openssl rand -base64 32
#    Replace ATOM_OAUTH_ENCRYPTION_KEY with the generated key