---
phase: 108-frontend-property-based-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/tests/property/__tests__/chat-state-machine.test.ts
autonomous: true

must_haves:
  truths:
    - "Chat state machine invariants tested (message ordering, context preservation)"
    - "WebSocket connection lifecycle validated with property tests"
    - "Chat memory operations (store, retrieve, clear) preserve invariants"
    - "Property tests use 50-100 examples for state machine validation"
  artifacts:
    - path: frontend-nextjs/tests/property/__tests__/chat-state-machine.test.ts
      provides: Chat state machine property tests (30+ tests)
      min_lines: 800
  key_links:
    - from: chat-state-machine.test.ts
      to: hooks/useWebSocket.ts
      via: WebSocket lifecycle property testing
      pattern: "fc.assert|renderHook.*useWebSocket"
    - from: chat-state-machine.test.ts
      to: hooks/useChatMemory.ts
      via: Chat memory property testing
      pattern: "memories.*monotonic|contextWindow"
---

<objective>
Create FastCheck property tests validating Chat state machine invariants.

This plan creates comprehensive property tests for chat state management using FastCheck. Tests validate:
1. WebSocket connection lifecycle (disconnected -> connecting -> connected -> disconnected)
2. Message ordering and streaming (messages arrive in order, streaming content accumulates correctly)
3. Chat memory invariants (memories array grows monotonically, contextWindow limits enforced)
4. Session persistence across reconnection (subscriptions survive reconnect)

This builds on existing property test patterns from state-transition-validation.test.ts (Phase 106-04) and state-machine-invariants.test.ts (Phase 098).

Output: 30+ property tests validating chat state machine correctness.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend-nextjs/tests/property/__tests__/state-transition-validation.test.ts
@frontend-nextjs/tests/property/state-machine-invariants.test.ts
@frontend-nextjs/hooks/useWebSocket.ts
@frontend-nextjs/hooks/useChatMemory.ts
@frontend-nextjs/package.json
</context>

<tasks>

<task type="auto">
  <name>Create chat state machine property tests</name>
  <files>frontend-nextjs/tests/property/__tests__/chat-state-machine.test.ts</files>
  <action>
Create chat-state-machine.test.ts with FastCheck property tests for chat state machine invariants.

**File Location:** frontend-nextjs/tests/property/__tests__/chat-state-machine.test.ts

**Test Structure:**

1. **WebSocket State Machine Tests** (12 tests):
   - fc.property: WebSocket states only transition: disconnected -> connecting -> connected -> disconnected
   - fc.property: Cannot skip states (e.g., disconnected -> connected without connecting)
   - fc.property: isConnected boolean matches connection state
   - fc.property: streamingContent only accumulates in connected state
   - fc.property: Messages are only received in connected state
   - fc.property: Subscriptions persist across reconnection
   - fc.property: Connection lifecycle is idempotent (connect->connect is same as connect)
   - fc.property: Disconnect is idempotent (disconnect->disconnect is no-op)
   - fc.property: Rapid connect/disconnect cycles settle to disconnected
   - fc.property: Auto-connect respects initial autoConnect value
   - fc.property: Token parameter is always present in WebSocket URL
   - fc.property: Channel subscriptions are queued until connected

2. **Chat Memory State Machine Tests** (12 tests):
   - fc.property: memories array grows monotonically (oldest first, newest at index 0)
   - fc.property: memories array is limited to contextWindow size
   - fc.property: Storing memory increments memory count (until limit)
   - fc.property: Clearing session resets memories to empty array
   - fc.property: memoryContext relevanceScore is always 0-1
   - fc.property: hasRelevantContext is false when relevanceScore <= 0.3
   - fc.property: isLoading is false after store completes (success or error)
   - fc.property: error is null on successful operation
   - fc.property: Disabled memory (enableMemory=false) prevents all operations
   - fc.property: Memory stats refresh does not affect memories array
   - fc.property: Storing duplicate memories doesn't create duplicates
   - fc.property: Memory retrieval respects relevance threshold

3. **Message Ordering Tests** (8 tests):
   - fc.property: Messages maintain FIFO order
   - fc.property: Streaming content preserves character order
   - fc.property: Concurrent messages are serialized correctly
   - fc.property: Message IDs are unique within session
   - fc.property: Message timestamps are monotonically increasing
   - fc.property: Message role (user/assistant/system) is from valid set
   - fc.property: Message content is non-empty string
   - fc.property: Messages with same role can appear consecutively

**Test Configuration:**
- numRuns: 50 for state machine tests (balanced coverage per Phase 106-04 research)
- seed: Fixed seeds 24001-24032 for reproducibility
- Use fc.assert for property verification
- Use renderHook for hook testing
- Use act() for state transitions

**Mock Setup:**
- Mock WebSocket class (same as state-transition-validation.test.ts)
- Mock useSession for auth token
- Mock fetch for useChatMemory API calls
- Mock window.atom.canvas if needed

**Import existing patterns from:**
- state-transition-validation.test.ts (WebSocket test patterns)
- state-machine-invariants.test.ts (state machine transition patterns)
  </action>
  <verify>
cd frontend-nextjs && npm test -- chat-state-machine --watchAll=false --passWithNoTests
  </verify>
  <done>
30+ property tests created, validating chat state machine invariants
  </done>
</task>

<task type="auto">
  <name>Verify property tests pass</name>
  <files>frontend-nextjs/tests/property/__tests__/chat-state-machine.test.ts</files>
  <action>
Run property tests and verify they pass. The tests validate existing chat state management code.

If any tests fail, document the failure in the test file as a TODO comment indicating a state machine bug to be fixed later.

**Expected Results:**
- Most property tests should pass (existing code is generally correct)
- Any failures indicate actual bugs in state management code
- Document failures with: // TODO: Fix chat state bug - [description]

**Verification Command:**
```bash
cd frontend-nextjs
npm test -- chat-state-machine --watchAll=false
```
  </action>
  <verify>
cd frontend-nextjs && npm test -- chat-state-machine --watchAll=false --passWithNoTests
  </verify>
  <done>
30+ property tests executed, results documented (pass/fail)
  </done>
</task>

</tasks>

<verification>
Run property tests and document results:

```bash
cd frontend-nextjs
npm test -- chat-state-machine --coverage --watchAll=false
```

Verify:
1. 30+ property tests created
2. Each test documents a specific chat state machine invariant
3. FastCheck configuration uses appropriate numRuns (50-100)
4. Tests cover WebSocket lifecycle, chat memory, and message ordering
5. Any failures are documented with TODO comments
</verification>

<success_criteria>
1. 30+ property tests created using FastCheck
2. Tests validate chat state machine invariants
3. No unreachable states found (or documented if found)
4. All valid transitions tested
5. Property tests use appropriate numRuns (50-100)
6. Test failures documented with TODO comments
</success_criteria>

<output>
After completion, create `.planning/phases/108-frontend-property-based-tests/108-01-SUMMARY.md` with:
- Test count (30+ property tests)
- Chat state invariants validated
- Any unreachable states found
- Any state machine bugs discovered
</output>
