---
phase: 108-frontend-property-based-tests
plan: 04
type: execute
wave: 3
depends_on:
  - 108-01
  - 108-02
  - 108-03
files_modified:
  - .planning/phases/108-frontend-property-based-tests/FRONTEND_STATE_MACHINE_INVARIANTS.md
autonomous: true

must_haves:
  truths:
    - "All state machine invariants documented with formal specifications"
    - "State transition diagrams for Chat, Canvas, and Auth state machines"
    - "Property test patterns documented for reusability"
    - "Documentation links invariants to specific test files"
  artifacts:
    - path: .planning/phases/108-frontend-property-based-tests/FRONTEND_STATE_MACHINE_INVARIANTS.md
      provides: Comprehensive state machine invariant documentation
      min_lines: 800
  key_links:
    - from: FRONTEND_STATE_MACHINE_INVARIANTS.md
      to: tests/property/__tests__/chat-state-machine.test.ts
      via: Documentation references chat tests
      pattern: "chat.*invariant.*test"
    - from: FRONTEND_STATE_MACHINE_INVARIANTS.md
      to: tests/property/__tests__/canvas-state-machine.test.ts
      via: Documentation references canvas tests
      pattern: "canvas.*invariant.*test"
    - from: FRONTEND_STATE_MACHINE_INVARIANTS.md
      to: tests/property/__tests__/auth-state-machine.test.ts
      via: Documentation references auth tests
      pattern: "auth.*invariant.*test"
---

<objective>
Create comprehensive documentation for frontend state machine invariants.

This plan documents all state machine invariants tested in Plans 01-03, creating a reference guide for:
1. State transition specifications (formal state machine definitions)
2. Invariant rules (what must always be true)
3. Property test patterns (how to test state machines with FastCheck)
4. Integration patterns (how state machines interact)

This follows the pattern from Phase 103 (backend invariants documentation) and Phase 098 (frontend property test patterns).

Output: Comprehensive state machine invariant documentation.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-backend-property-based-tests/103-04-PLAN.md
@frontend-nextjs/tests/property/state-machine-invariants.test.ts
@frontend-nextjs/tests/property/__tests__/state-transition-validation.test.ts
</context>

<tasks>

<task type="auto">
  <name>Create state machine invariants documentation</name>
  <files>.planning/phases/108-frontend-property-based-tests/FRONTEND_STATE_MACHINE_INVARIANTS.md</files>
  <action>
Create FRONTEND_STATE_MACHINE_INVARIANTS.md with comprehensive state machine documentation.

**File Location:** .planning/phases/108-frontend-property-based-tests/FRONTEND_STATE_MACHINE_INVARIANTS.md

**Documentation Structure:**

1. **Introduction** (purpose, scope, relationship to Phase 103 INVARIANTS.md)

2. **Chat State Machine** (200+ lines):
   - State diagram: disconnected -> connecting -> connected -> disconnected
   - States: disconnected, connecting, connected, error
   - Events: connect(), disconnect(), message(), error()
   - Invariants (12 invariants from Plan 01)
   - Test file reference: chat-state-machine.test.ts
   - Integration with: useWebSocket hook, useChatMemory hook

3. **Canvas State Machine** (200+ lines):
   - State diagram: null -> initialized -> updating -> closed
   - States: null, initialized, updating, closed, error
   - Events: register(), update(), close(), error()
   - Invariants (10 invariants from Plan 02)
   - Canvas types: generic, docs, email, sheets, orchestration, terminal, coding
   - Test file reference: canvas-state-machine.test.ts
   - Integration with: useCanvasState hook, canvas components

4. **Auth State Machine** (200+ lines):
   - State diagram: guest -> authenticating -> authenticated -> guest
   - States: guest, authenticating, authenticated, error
   - Events: login(), logout(), refresh(), error()
   - Invariants (8 invariants from Plan 03)
   - Session fields: user, expires, accessToken, refreshToken
   - Test file reference: auth-state-machine.test.ts
   - Integration with: next-auth, permission system

5. **Property Test Patterns** (150+ lines):
   - FastCheck configuration (numRuns: 50-100, seed management)
   - Mock patterns (WebSocket, useSession, window.atom.canvas)
   - renderHook patterns for React hooks
   - act() patterns for state transitions
   - Test file structure (describe blocks, invariant documentation)

6. **Cross-State Machine Integration** (100+ lines):
   - Chat depends on Auth (requires session token)
   - Canvas independent of Auth (works in guest mode)
   - WebSocket reconnection preserves subscriptions
   - Session expiration affects all authenticated state

7. **Invariant Categories** (reference section):
   - Lifecycle invariants (state transition rules)
   - Type invariants (field types, enum values)
   - Ordering invariants (FIFO, monotonic growth)
   - Consistency invariants (state synchronization)
   - Error handling invariants (error recovery)

**Pattern Reference:**
- Follow structure from Phase 103 INVARIANTS.md (backend invariants)
- Reference Phase 098 state-machine-invariants.test.ts patterns
- Include seed ranges for reproducibility (24001-24077)

**Key Points:**
- Each invariant links to specific test file and test number
- Include FastCheck arbitraries used for testing
- Document any VALIDATED_BUG findings
- Include state diagrams in ASCII art
  </action>
  <verify>
test -f .planning/phases/108-frontend-property-based-tests/FRONTEND_STATE_MACHINE_INVARIANTS.md && wc -l .planning/phases/108-frontend-property-based-tests/FRONTEND_STATE_MACHINE_INVARIANTS.md
  </verify>
  <done>
Documentation created with 800+ lines covering all state machine invariants
  </done>
</task>

</tasks>

<verification>
Verify documentation completeness:

```bash
wc -l .planning/phases/108-frontend-property-based-tests/FRONTEND_STATE_MACHINE_INVARIANTS.md
grep -c "INVARIANT:" .planning/phases/108-frontend-property-based-tests/FRONTEND_STATE_MACHINE_INVARIANTS.md
grep -c "test.ts" .planning/phases/108-frontend-property-based-tests/FRONTEND_STATE_MACHINE_INVARIANTS.md
```

Verify:
1. Documentation is 800+ lines
2. All 3 state machines documented (Chat, Canvas, Auth)
3. Each invariant references test file
4. State diagrams included
5. Property test patterns documented
6. Cross-state machine integration described
</verification>

<success_criteria>
1. Documentation created with 800+ lines
2. All state machines from Plans 01-03 documented
3. Invariants linked to specific test files
4. Property test patterns documented
5. State diagrams included
6. Integration patterns described
</success_criteria>

<output>
After completion, create `.planning/phases/108-frontend-property-based-tests/108-04-SUMMARY.md` with:
- Documentation size (800+ lines)
- State machines documented (3)
- Invariants documented (30+)
- Test file references included
</output>
