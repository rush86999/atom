---
phase: 108-frontend-property-based-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/tests/property/__tests__/canvas-state-machine.test.ts
autonomous: true

must_haves:
  truths:
    - "Canvas state machine invariants tested (component lifecycle, state consistency)"
    - "Canvas state transitions validated (null -> state -> updates)"
    - "Canvas type validation (7 valid types: generic, docs, email, sheets, orchestration, terminal, coding)"
    - "Property tests use 50-100 examples for state machine validation"
  artifacts:
    - path: frontend-nextjs/tests/property/__tests__/canvas-state-machine.test.ts
      provides: Canvas state machine property tests (25+ tests)
      min_lines: 700
  key_links:
    - from: canvas-state-machine.test.ts
      to: hooks/useCanvasState.ts
      via: Canvas state property testing
      pattern: "fc.assert|renderHook.*useCanvasState"
    - from: canvas-state-machine.test.ts
      to: components/canvas/types/index.ts
      via: Canvas type validation
      pattern: "canvas_type.*constantFrom"
---

<objective>
Create FastCheck property tests validating Canvas state machine invariants.

This plan creates comprehensive property tests for canvas state management using FastCheck. Tests validate:
1. Canvas state lifecycle (null -> state -> updates)
2. Canvas type validation (7 valid types: generic, docs, email, sheets, orchestration, terminal, coding)
3. State consistency across multiple canvas subscriptions
4. Global canvas subscription behavior (all states tracked)

This builds on existing property test patterns from state-transition-validation.test.ts (Phase 106-04) and state-machine-invariants.test.ts (Phase 098).

Output: 25+ property tests validating canvas state machine correctness.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend-nextjs/tests/property/__tests__/state-transition-validation.test.ts
@frontend-nextjs/tests/property/state-machine-invariants.test.ts
@frontend-nextjs/hooks/useCanvasState.ts
@frontend-nextjs/components/canvas/types/index.ts
@frontend-nextjs/package.json
</context>

<tasks>

<task type="auto">
  <name>Create canvas state machine property tests</name>
  <files>frontend-nextjs/tests/property/__tests__/canvas-state-machine.test.ts</files>
  <action>
Create canvas-state-machine.test.ts with FastCheck property tests for canvas state machine invariants.

**File Location:** frontend-nextjs/tests/property/__tests__/canvas-state-machine.test.ts

**Test Structure:**

1. **Canvas State Lifecycle Tests** (8 tests):
   - fc.property: Canvas state is always null before first subscription callback
   - fc.property: allStates array grows monotonically (canvases added, never removed except explicitly)
   - fc.property: State updates preserve canvas_id immutability
   - fc.property: getState returns null for unregistered canvas
   - fc.property: getState returns valid state for registered canvas
   - fc.property: Multiple canvas subscriptions are independent
   - fc.property: Changing canvasId unsubscribes from previous, subscribes to new
   - fc.property: Unsubscribing stops state updates for that canvas

2. **Canvas Type Validation Tests** (7 tests):
   - fc.property: Canvas types are from allowed set (7 valid types)
   - fc.property: Each canvas type has valid structure for that type
   - fc.property: Generic canvas has required fields
   - fc.property: Docs canvas has document-specific fields
   - fc.property: Email canvas has email-specific fields
   - fc.property: Sheets canvas has spreadsheet-specific fields
   - fc.property: Invalid canvas types are rejected

3. **Canvas State Consistency Tests** (6 tests):
   - fc.property: Canvas state contains required fields (canvas_type, canvas_id, timestamp)
   - fc.property: Canvas timestamp is ISO 8601 format
   - fc.property: Canvas data field preserves structure through updates
   - fc.property: Canvas metadata is non-null after state initialization
   - fc.property: Canvas state transitions are atomic (no partial updates)
   - fc.property: Concurrent canvas updates are serialized correctly

4. **Global Canvas Subscription Tests** (5 tests):
   - fc.property: Global subscription receives all canvas updates
   - fc.property: getAllStates returns consistent snapshot
   - fc.property: Global unsubscribing stops all canvas updates
   - fc.property: Global and specific subscriptions can coexist
   - fc.property: Global subscription doesn't interfere with specific subscriptions

**Test Configuration:**
- numRuns: 50 for state machine tests (balanced coverage per Phase 106-04 research)
- seed: Fixed seeds 24033-24057 for reproducibility
- Use fc.assert for property verification
- Use renderHook for hook testing
- Use act() for state transitions

**Mock Setup:**
- Mock window.atom.canvas API (same as state-transition-validation.test.ts)
- Mock canvas states with proper structure
- Mock subscribers for testing subscription lifecycle

**Import existing patterns from:**
- state-transition-validation.test.ts (Canvas test patterns)
- state-machine-invariants.test.ts (Canvas state machine patterns)
  </action>
  <verify>
cd frontend-nextjs && npm test -- canvas-state-machine --watchAll=false --passWithNoTests
  </verify>
  <done>
25+ property tests created, validating canvas state machine invariants
  </done>
</task>

<task type="auto">
  <name>Verify property tests pass</name>
  <files>frontend-nextjs/tests/property/__tests__/canvas-state-machine.test.ts</files>
  <action>
Run property tests and verify they pass. The tests validate existing canvas state management code.

If any tests fail, document the failure in the test file as a TODO comment indicating a state machine bug to be fixed later.

**Expected Results:**
- Most property tests should pass (existing code is generally correct)
- Any failures indicate actual bugs in state management code
- Document failures with: // TODO: Fix canvas state bug - [description]

**Verification Command:**
```bash
cd frontend-nextjs
npm test -- canvas-state-machine --watchAll=false
```
  </action>
  <verify>
cd frontend-nextjs && npm test -- canvas-state-machine --watchAll=false --passWithNoTests
  </verify>
  <done>
25+ property tests executed, results documented (pass/fail)
  </done>
</task>

</tasks>

<verification>
Run property tests and document results:

```bash
cd frontend-nextjs
npm test -- canvas-state-machine --coverage --watchAll=false
```

Verify:
1. 25+ property tests created
2. Each test documents a specific canvas state machine invariant
3. FastCheck configuration uses appropriate numRuns (50-100)
4. Tests cover canvas lifecycle, type validation, and consistency
5. Any failures are documented with TODO comments
</verification>

<success_criteria>
1. 25+ property tests created using FastCheck
2. Tests validate canvas state machine invariants
3. All 7 canvas types validated
4. Property tests use appropriate numRuns (50-100)
5. Test failures documented with TODO comments
</success_criteria>

<output>
After completion, create `.planning/phases/108-frontend-property-based-tests/108-02-SUMMARY.md` with:
- Test count (25+ property tests)
- Canvas state invariants validated
- Any canvas type bugs discovered
- Any state machine bugs discovered
</output>
