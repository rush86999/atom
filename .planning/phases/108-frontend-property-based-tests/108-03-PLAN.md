---
phase: 108-frontend-property-based-tests
plan: 03
type: execute
wave: 2
depends_on:
  - 108-01
  - 108-02
files_modified:
  - frontend-nextjs/tests/property/__tests__/auth-state-machine.test.ts
autonomous: true

must_haves:
  truths:
    - "Auth state machine invariants tested (session validity, permission checks)"
    - "Auth state transitions validated (guest -> authenticating -> authenticated/error)"
    - "Session expiration handling validated"
    - "Property tests use 50-100 examples for state machine validation"
  artifacts:
    - path: frontend-nextjs/tests/property/__tests__/auth-state-machine.test.ts
      provides: Auth state machine property tests (20+ tests)
      min_lines: 600
  key_links:
    - from: auth-state-machine.test.ts
      to: next-auth/react
      via: Auth session state testing
      pattern: "fc.assert|useSession"
    - from: auth-state-machine.test.ts
      to: hooks/auth (or equivalent)
      via: Auth state machine testing
      pattern: "authenticating.*authenticated.*guest"
---

<objective>
Create FastCheck property tests validating Auth state machine invariants.

This plan creates comprehensive property tests for authentication state management using FastCheck. Tests validate:
1. Auth state lifecycle (guest -> authenticating -> authenticated/error)
2. Session validity checks (token expiration, refresh flow)
3. Permission state transitions (unauthorized -> authorized)
4. Session persistence across page refreshes

This builds on existing property test patterns from state-transition-validation.test.ts (Phase 106-04) and state-machine-invariants.test.ts (Phase 098).

Output: 20+ property tests validating auth state machine correctness.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend-nextjs/tests/property/__tests__/state-transition-validation.test.ts
@frontend-nextjs/tests/property/state-machine-invariants.test.ts
@frontend-nextjs/package.json
</context>

<tasks>

<task type="auto">
  <name>Create auth state machine property tests</name>
  <files>frontend-nextjs/tests/property/__tests__/auth-state-machine.test.ts</files>
  <action>
Create auth-state-machine.test.ts with FastCheck property tests for auth state machine invariants.

**File Location:** frontend-nextjs/tests/property/__tests__/auth-state-machine.test.ts

**Test Structure:**

1. **Auth State Lifecycle Tests** (8 tests):
   - fc.property: Auth states: guest -> authenticating -> authenticated -> guest (logout)
   - fc.property: Cannot skip states (e.g., guest -> authenticated without authenticating)
   - fc.property: loading is only true during state transitions
   - fc.property: session is null when status='guest' or 'unauthenticated'
   - fc.property: session is non-null when status='authenticated'
   - fc.property: error clears on successful state transition
   - fc.property: Login failure returns to guest with error set
   - fc.property: Logout from guest is safe no-op

2. **Session Validity Tests** (6 tests):
   - fc.property: Session expiration transitions to guest
   - fc.property: Token refresh maintains authenticated state
   - fc.property: Invalid token causes transition to guest
   - fc.property: Session has required fields (user, expires, accessToken)
   - fc.property: Session timestamps are valid ISO 8601 dates
   - fc.property: Session user object has required fields (name, email)

3. **Permission State Tests** (6 tests):
   - fc.property: Permissions are computed from session roles
   - fc.property: Unauthorized state has no permissions
   - fc.property: Permission checks are deterministic for same session
   - fc.property: Permission changes trigger state update
   - fc.property: Multiple permission checks don't cause state thrashing
   - fc.property: Admin role has all permissions

**Test Configuration:**
- numRuns: 50 for state machine tests (balanced coverage per Phase 106-04 research)
- seed: Fixed seeds 24058-24077 for reproducibility
- Use fc.assert for property verification
- Use renderHook for hook testing (if using useSession hook)
- Use act() for state transitions

**Mock Setup:**
- Mock next-auth useSession hook (same as state-transition-validation.test.ts)
- Mock session data with proper structure
- Mock token expiration logic

**Import existing patterns from:**
- state-transition-validation.test.ts (Auth test patterns)
- state-machine-invariants.test.ts (Auth flow state machine patterns)
  </action>
  <verify>
cd frontend-nextjs && npm test -- auth-state-machine --watchAll=false --passWithNoTests
  </verify>
  <done>
20+ property tests created, validating auth state machine invariants
  </done>
</task>

<task type="auto">
  <name>Verify property tests pass</name>
  <files>frontend-nextjs/tests/property/__tests__/auth-state-machine.test.ts</files>
  <action>
Run property tests and verify they pass. The tests validate existing auth state management code.

If any tests fail, document the failure in the test file as a TODO comment indicating a state machine bug to be fixed later.

**Expected Results:**
- Most property tests should pass (existing code is generally correct)
- Any failures indicate actual bugs in state management code
- Document failures with: // TODO: Fix auth state bug - [description]

**Verification Command:**
```bash
cd frontend-nextjs
npm test -- auth-state-machine --watchAll=false
```
  </action>
  <verify>
cd frontend-nextjs && npm test -- auth-state-machine --watchAll=false --passWithNoTests
  </verify>
  <done>
20+ property tests executed, results documented (pass/fail)
  </done>
</task>

</tasks>

<verification>
Run property tests and document results:

```bash
cd frontend-nextjs
npm test -- auth-state-machine --coverage --watchAll=false
```

Verify:
1. 20+ property tests created
2. Each test documents a specific auth state machine invariant
3. FastCheck configuration uses appropriate numRuns (50-100)
4. Tests cover auth lifecycle, session validity, and permissions
5. Any failures are documented with TODO comments
</verification>

<success_criteria>
1. 20+ property tests created using FastCheck
2. Tests validate auth state machine invariants
3. Session expiration handling validated
4. Property tests use appropriate numRuns (50-100)
5. Test failures documented with TODO comments
</success_criteria>

<output>
After completion, create `.planning/phases/108-frontend-property-based-tests/108-03-SUMMARY.md` with:
- Test count (20+ property tests)
- Auth state invariants validated
- Any session handling bugs discovered
- Any state machine bugs discovered
</output>
