---
phase: 248-test-discovery-documentation
plan: 02
type: execute
wave: 2
depends_on: [248-01]
files_modified:
  - backend/TEST_FAILURE_REPORT.md
  - backend/TESTING.md
autonomous: true

must_haves:
  truths:
    - "Full test suite runs and produces results (pass/fail counts)"
    - "All test failures are documented with stack traces and reproduction steps"
    - "Test failures are categorized by severity (critical/high/medium/low)"
    - "Test execution process is documented in TESTING.md"
  artifacts:
    - path: "backend/TEST_FAILURE_REPORT.md"
      provides: "Comprehensive test failure documentation"
      min_lines: 100
      contains: "test failure list, severity categories, reproduction steps"
    - path: "backend/TESTING.md"
      provides: "Test execution guide"
      min_lines: 50
      contains: "how to run tests, interpret results, common issues"
  key_links:
    - from: "TEST_FAILURE_REPORT.md"
      to: "pytest output"
      via: "test failure parsing"
      pattern: "FAILED|ERROR"
    - from: "TESTING.md"
      to: "developer workflow"
      via: "documentation"
      pattern: "pytest|coverage"
---

# Phase 248 Plan 02: Run Test Suite and Document Failures

## Objective

Run the full test suite and document all failures with evidence, categorization, and prioritization. Create TESTING.md for test execution workflow.

## One-Liner

Execute full test suite (400+ tests), document all failures in TEST_FAILURE_REPORT.md with severity categorization, and create TESTING.md guide.

## Context

@/Users/rushiparikh/projects/atom/.planning/phases/248-test-discovery-documentation/248-01-SUMMARY.md
@/Users/rushiparikh/projects/atom/backend/BUILD.md

After Plan 01, all syntax errors are fixed and test collection works. This plan runs the full suite and documents failures.

## Severity Categories

- **CRITICAL**: Core functionality broken (agents, workflows, API endpoints)
- **HIGH**: Important features broken (integrations, canvas, database)
- **MEDIUM**: Secondary features broken (analytics, reporting)
- **LOW**: Cosmetic issues, edge cases, non-critical paths

## Tasks

<task type="auto">
  <name>Task 1: Run full test suite and capture output</name>
  <files>test-results.txt</files>
  <action>
    1. Navigate to backend directory: cd /Users/rushiparikh/projects/atom/backend
    2. Run full test suite with verbose output:
       python3 -m pytest -v --tb=short > test-results.txt 2>&1
    3. Capture both stdout and stderr
    4. Record total tests collected, passed, failed, skipped
    5. Record execution time
  </action>
  <verify>test-results.txt exists and contains "test session starts" and summary</verify>
  <done>Full test suite executed with results captured</done>
</task>

<task type="auto">
  <name>Task 2: Parse test results and create TEST_FAILURE_REPORT.md</name>
  <files>backend/TEST_FAILURE_REPORT.md</files>
  <action>
    Create TEST_FAILURE_REPORT.md with:
    1. **Executive Summary**: Total tests, pass rate, execution time
    2. **Critical Failures**: List all CRITICAL test failures with:
       - Test name and file
       - Error message
       - Stack trace (abbreviated)
       - Affected component
    3. **High Priority Failures**: List all HIGH test failures (same format)
    4. **Medium/Low Failures**: Summarized list
    5. **Categories by Component**: Group failures by subsystem
    6. **Reproduction Steps**: How to reproduce each failure
    7. **Fix Priority**: What to fix first (impact-based ranking)

    Format:
    ```markdown
    # Test Failure Report - Phase 248

    ## Executive Summary
    - Total Tests: XXX
    - Passed: XXX (XX%)
    - Failed: XXX (XX%)
    - Skipped: XXX (XX%)
    - Execution Time: XXX seconds

    ## Critical Failures (X tests)

    ### [AGENT-001] Agent Creation Failed
    - **Test**: tests/api/test_agent_control_routes.py::test_create_agent
    - **Error**: AssertionError: Expected 201, got 500
    - **Stack**: ...
    - **Component**: Agent Control Routes
    - **Reproduction**: pytest tests/api/test_agent_control_routes.py::test_create_agent -v
    ...
    ```
  </action>
  <verify>TEST_FAILURE_REPORT.md exists and has at least 100 lines</verify>
  <done>All test failures documented with categorization and reproduction steps</done>
</task>

<task type="auto">
  <name>Task 3: Create TESTING.md with test execution guide</name>
  <files>backend/TESTING.md</files>
  <action>
    Create TESTING.md with:
    1. **Quick Start**: How to run all tests
    2. **Test Categories**: Unit, integration, E2E, property tests
    3. **Running Specific Tests**: By file, class, name
    4. **Interpreting Results**: What pass/fail/skip means
    5. **Common Issues**: Solutions to frequent test problems
    6. **Coverage Reports**: How to generate and read them
    7. **CI/CD Integration**: How tests run in CI

    Include actual commands from this project:
    - Run all tests: python3 -m pytest -v
    - Run specific file: python3 -m pytest tests/api/test_agent_control_routes.py -v
    - Run with coverage: python3 -m pytest --cov=core --cov-report=html
  </action>
  <verify>TESTING.md exists and has at least 50 lines</verify>
  <done>Comprehensive testing guide created</done>
</task>

## Verification

### Done Criteria Met
- [ ] Full test suite executed (400+ tests)
- [ ] TEST_FAILURE_REPORT.md created with all failures documented
- [ ] Failures categorized by severity (CRITICAL/HIGH/MEDIUM/LOW)
- [ ] TESTING.md created with execution guide
- [ ] Reproduction steps provided for each failure

### Success Criteria Met
- [ ] Test failure report is comprehensive (no missing failures)
- [ ] Prioritization is clear (what to fix first)
- [ ] Testing documentation is actionable (developers can use it)

## Output

After completion, create `.planning/phases/248-test-discovery-documentation/248-02-SUMMARY.md`

## Notes

- **First-time execution**: This is the first full test run after fixing syntax errors
- **Expect many failures**: The codebase has known issues; this phase documents them
- **Prioritization matters**: Focus on CRITICAL and HIGH failures for Phase 249
- **Documentation value**: TESTING.md will be used by all developers going forward
