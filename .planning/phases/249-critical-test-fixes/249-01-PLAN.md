---
phase: 249-critical-test-fixes
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/api/agent_routes.py
  - backend/tests/api/test_dto_validation.py
autonomous: true

must_haves:
  truths:
    - "AgentRunRequest has agent_id field with proper validation"
    - "AgentUpdateRequest has agent_id field with proper validation"
    - "Required field validation works (empty AgentRunRequest raises ValidationError)"
    - "Optional fields can be omitted without error"
    - "Tests fail before fix and pass after fix (TDD cycle verified)"
  artifacts:
    - path: backend/api/agent_routes.py
      provides: AgentRunRequest, AgentUpdateRequest DTOs
      contains: "class AgentRunRequest", "class AgentUpdateRequest"
    - path: backend/tests/api/test_dto_validation.py
      provides: DTO validation tests
      exports: ["TestAgentDTOValidation"]
  key_links:
    - from: backend/tests/api/test_dto_validation.py
      to: backend/api/agent_routes.py
      via: "from api.agent_routes import AgentRunRequest, AgentUpdateRequest"
      pattern: "AgentRunRequest\\(.*agent_id"
---

<objective>
Fix Pydantic v2 DTO validation issues for agent request/response models

Purpose: Core agent functionality depends on properly validated DTOs. Current DTOs are missing required fields (agent_id) and validation is broken.

Output: Working AgentRunRequest and AgentUpdateRequest DTOs with proper field definitions and Pydantic v2 validation
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/TEST_FAILURE_REPORT.md

# Test Failure Context
From TEST_FAILURE_REPORT.md - Critical failures DTO-001, DTO-002, DTO-003:

- DTO-001: test_agent_request_dto_required_fields - "DID NOT RAISE ValidationError"
- DTO-002: test_agent_request_dto_optional_fields - "'AgentRunRequest' object has no attribute 'agent_id'"
- DTO-003: test_agent_response_dto_all_fields - "'AgentUpdateRequest' object has no attribute 'agent_id'"

Root cause: AgentRunRequest only has `parameters: Dict[str, Any] = {}` field, missing `agent_id` field.
</context>

<feature>
  <name>Agent DTO Pydantic v2 Validation</name>
  <files>backend/api/agent_routes.py, backend/tests/api/test_dto_validation.py</files>
  <behavior>
    AgentRunRequest must:
    - Have required `agent_id: str` field
    - Have optional `parameters: Dict[str, Any]` field (default empty dict)
    - Raise ValidationError when instantiated without agent_id
    - Allow instantiation with just agent_id (parameters uses default)

    AgentUpdateRequest must:
    - Have required `agent_id: str` field
    - Have optional `name`, `description`, `status` fields
    - Allow accessing agent_id attribute after instantiation

    Pydantic v2 compatibility:
    - Use Field() for constraints (not direct assignments)
    - Proper type hints for all fields
    - ConfigDict for model configuration (not Config class)
  </behavior>
  <implementation>
    1. RED: Run failing tests to confirm failures (pytest tests/api/test_dto_validation.py::TestAgentDTOValidation)
    2. GREEN: Update DTOs in backend/api/agent_routes.py:
       - Add agent_id: str field to AgentRunRequest (required, no default)
       - Keep parameters: Dict[str, Any] = {} as optional default
       - Add agent_id: str field to AgentUpdateRequest (required)
       - Ensure Pydantic v2 syntax (no Config class, use model_config)
    3. VERIFY: Run tests again - all 3 DTO tests should pass
    4. COMMIT: "fix(249-01): add agent_id field to AgentRunRequest and AgentUpdateRequest DTOs"
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: RED - Confirm DTO test failures</name>
  <files>backend/tests/api/test_dto_validation.py</files>
  <action>
    Run the failing DTO validation tests to confirm current failures:
    cd backend && source venv/bin/activate && pytest tests/api/test_dto_validation.py::TestAgentDTOValidation -v

    Expected failures:
    - test_agent_request_dto_required_fields: DID NOT RAISE (AgentRunRequest has no required fields)
    - test_agent_request_dto_optional_fields: AttributeError (agent_id missing)
    - test_agent_response_dto_all_fields: AttributeError (agent_id missing)

    Document exact error messages for reference.

    Test quality verification:
    - Verify test assertions are specific and will catch the bug (not just pass silently)
    - Check test uses proper pytest.raises() for exception testing
  </action>
  <verify>pytest output shows 3 failures with expected error messages</verify>
  <done>Three test failures documented with exact error output</done>
</task>

<task type="auto">
  <name>Task 2: GREEN - Add agent_id field to AgentRunRequest DTO</name>
  <files>backend/api/agent_routes.py</files>
  <action>
    Update AgentRunRequest class in backend/api/agent_routes.py (around line 36):

    Current code:
    ```python
    class AgentRunRequest(BaseModel):
        parameters: Dict[str, Any] = {}
    ```

    Change to:
    ```python
    class AgentRunRequest(BaseModel):
        agent_id: str
        parameters: Dict[str, Any] = Field(default_factory=dict)
    ```

    This makes:
    - agent_id a required field (no default)
    - parameters an optional field with empty dict default
    - Uses Field(default_factory=dict) for mutable default (Pydantic v2 best practice)
  </action>
  <verify>grep -A 3 "class AgentRunRequest" backend/api/agent_routes.py shows agent_id: str as first field</verify>
  <done>AgentRunRequest has agent_id: str field, parameters: Dict[str, Any] = Field(default_factory=dict)</done>
</task>

<task type="auto">
  <name>Task 3: GREEN - Add agent_id field to AgentUpdateRequest DTO</name>
  <files>backend/api/agent_routes.py</files>
  <action>
    Update AgentUpdateRequest class in backend/api/agent_routes.py (around line 39):

    Current code:
    ```python
    class AgentUpdateRequest(BaseModel):
        name: Optional[str] = None
        description: Optional[str] = None
    ```

    Change to:
    ```python
    class AgentUpdateRequest(BaseModel):
        agent_id: str
        name: Optional[str] = None
        description: Optional[str] = None
    ```

    This makes agent_id a required field while keeping name and description optional.
  </action>
  <verify>grep -A 4 "class AgentUpdateRequest" backend/api/agent_routes.py shows agent_id: str as first field</verify>
  <done>AgentUpdateRequest has agent_id: str as first field, followed by optional name and description</done>
</task>

<task type="auto">
  <name>Task 4: VERIFY - Run tests to confirm fixes</name>
  <files>backend/tests/api/test_dto_validation.py</files>
  <action>
    Run the DTO validation tests to confirm all pass:
    cd backend && source venv/bin/activate && pytest tests/api/test_dto_validation.py::TestAgentDTOValidation -v

    Expected result: All 3 tests now pass:
    - test_agent_request_dto_required_fields: PASS (ValidationError raised for empty AgentRunRequest)
    - test_agent_request_dto_optional_fields: PASS (agent_id accessible)
    - test_agent_response_dto_all_fields: PASS (agent_id accessible in AgentUpdateRequest)
  </action>
  <verify>pytest output shows 3/3 tests passing in TestAgentDTOValidation</verify>
  <done>All three AgentDTOValidation tests pass (test_agent_request_dto_required_fields, test_agent_request_dto_optional_fields, test_agent_response_dto_all_fields)</done>
</task>

<task type="auto">
  <name>Task 5: VERIFY - Run full DTO test suite</name>
  <files>backend/tests/api/test_dto_validation.py</files>
  <action>
    Run the entire DTO validation test file to ensure no regressions:
    cd backend && source venv/bin/activate && pytest tests/api/test_dto_validation.py -v

    Check for any new failures caused by the DTO changes. The test file includes:
    - TestAgentDTOValidation (our fixes)
    - TestCanvasDTOValidation
    - TestBrowserDTOValidation
    - TestAuthDTOValidation
    - TestDTOEdgeCases
    - TestDTOOpenAPIAlignment (may still fail - tracked separately)
    - TestDTOValidationErrors
    - TestDTOCoercion
    - TestDTODefaults

    Document any remaining failures for later plans.
  </action>
  <verify>pytest tests/api/test_dto_validation.py shows improved pass rate (at least 3 more tests passing)</verify>
  <done>Full DTO test suite executed; pass rate improved; remaining failures documented</done>
</task>

</tasks>

<verification>
Overall verification checks:
1. AgentRunRequest requires agent_id field (empty instantiation raises ValidationError)
2. AgentRunRequest.agent_id is accessible after instantiation
3. AgentUpdateRequest.agent_id is accessible after instantiation
4. Optional parameters field still works with default empty dict
5. No regressions in other DTO test classes
</verification>

<success_criteria>
1. test_agent_request_dto_required_fields passes (ValidationError raised)
2. test_agent_request_dto_optional_fields passes (agent_id accessible)
3. test_agent_response_dto_all_fields passes (agent_id in AgentUpdateRequest)
4. AgentRunRequest has agent_id: str as first field
5. AgentUpdateRequest has agent_id: str as first field
</success_criteria>

<output>
After completion, create `.planning/phases/249-critical-test-fixes/249-01-SUMMARY.md` with:
- Tests fixed (DTO-001, DTO-002, DTO-003)
- Changes made (added agent_id to both DTOs)
- Test results before/after
- Any remaining issues found
</output>
