---
phase: 183-core-services-coverage-skill-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_skill_adapter.py
autonomous: true

must_haves:
  truths:
    - "CLI skills parse command arguments correctly (--port, --host, --workers)"
    - "Python packages are extracted from skill metadata and passed to CommunitySkillTool"
    - "npm packages are parsed correctly (scoped packages, version specifiers)"
    - "NodeJsSkillAdapter handles npm governance checks before installation"
    - "Test coverage on skill_adapter.py exceeds 75%"
  artifacts:
    - path: "backend/tests/test_skill_adapter.py"
      provides: "CLI skills, Python packages, npm packages test coverage"
      min_lines: 400
    - path: "backend/tests/test_skill_adapter_cli.py"
      provides: "Dedicated CLI skills test file"
      min_lines: 250
    - path: "backend/tests/test_skill_adapter_npm.py"
      provides: "Dedicated npm skills test file"
      min_lines: 300
  key_links:
    - from: "backend/tests/test_skill_adapter.py"
      to: "backend/core/skill_adapter.py"
      via: "imports CommunitySkillTool, NodeJsSkillAdapter, create_community_tool"
      pattern: "from core.skill_adapter import|CommunitySkillTool|NodeJsSkillAdapter"
    - from: "backend/tests/test_skill_adapter_cli.py"
      to: "backend/core/skill_adapter.py"
      via: "tests _parse_cli_args and _execute_cli_skill methods"
      pattern: "_parse_cli_args|_execute_cli_skill|atom-"
    - from: "backend/tests/test_skill_adapter_npm.py"
      to: "backend/core/skill_adapter.py"
      via: "tests NodeJsSkillAdapter npm package handling"
      pattern: "NodeJsSkillAdapter|install_npm_dependencies|_parse_npm_package"
---

<objective>
Add comprehensive test coverage for skill adapter CLI skills and npm package support.

**Purpose**: Close coverage gaps in skill_adapter.py (751 lines) by testing CLI skill argument parsing and npm package handling added in Phases 25 and 36.

**Context**: Existing test_skill_adapter.py (297 lines, 25 tests) covers basic prompt and Python skills but misses:
- CLI skills (Phase 25): _parse_cli_args(), _execute_cli_skill()
- Python packages (Phase 35): _execute_python_skill_with_packages()
- npm packages (Phase 36): NodeJsSkillAdapter class (200+ lines untested)

**Output**:
- Extended test_skill_adapter.py with package tests
- New test_skill_adapter_cli.py (CLI argument parsing)
- New test_skill_adapter_npm.py (npm package support)
- 75%+ line coverage on skill_adapter.py

Phase 182 patterns: Module-level docker mocking, subprocess mocks, AsyncMock for async.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/183-core-services-coverage-skill-execution/183-RESEARCH.md
@.planning/phases/182-core-services-coverage-package-governance/182-01-SUMMARY.md

@backend/core/skill_adapter.py
@backend/tests/test_skill_adapter.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Extend test_skill_adapter.py with Python package tests</name>
  <files>backend/tests/test_skill_adapter.py</files>
  <action>
    Extend test_skill_adapter.py with Python package support tests (Phase 35 coverage):

    **Test Class: TestPythonPackageSkills (6 tests)**
    1. test_packages_attribute_stored - Verify packages list is stored on tool creation
    2. test_package_execution_workflow - Test _execute_python_skill_with_packages() calls PackageInstaller
    3. test_package_installation_failure_handling - Test error message when install_packages fails
    4. test_vulnerability_logging - Test vulnerabilities are logged but execution proceeds
    5. test_code_extraction_for_packages - Test _extract_function_code() adds execution wrapper
    6. test_package_skill_without_sandbox_raises - Verify RuntimeError when sandbox_enabled=False

    **Mocking Pattern** (from Phase 182):
    - Mock PackageInstaller at module level before importing skill_adapter
    - Mock install_packages() to return {"success": True, "image_tag": "test:latest"}
    - Mock execute_with_packages() to return "Execution result"
    - Use patch('core.package_installer.PackageInstaller') for import-time mocking

    **Code Pattern**:
    ```python
    # Module-level mocking
    sys.modules['core.package_installer'] = MagicMock()
    from core.skill_adapter import CommunitySkillTool

    # In test
    skill = create_community_tool({
        "name": "test_skill",
        "skill_type": "python_code",
        "packages": ["numpy==1.21.0"],
        "skill_content": "def execute(query): return query"
    })
    assert skill.packages == ["numpy==1.21.0"]
    ```

    Follow Phase 182-01 pattern: use MagicMock for module-level mocking, AsyncMock for async methods.
  </action>
  <verify>
    Run: pytest backend/tests/test_skill_adapter.py::TestPythonPackageSkills -v --cov=backend.core.skill_adapter --cov-report=term-missing
    Expected: 6 new tests passing, coverage increases on _execute_python_skill_with_packages
  </verify>
  <done>
    - 6 tests for Python package support added to test_skill_adapter.py
    - Packages attribute stored correctly on tool creation
    - PackageInstaller.install_packages() called in execution workflow
    - Installation failures return error messages
    - Vulnerabilities logged but execution proceeds
    - Code extraction adds execution wrapper for package skills
  </done>
</task>

<task type="auto">
  <name>Create test_skill_adapter_cli.py for CLI skills</name>
  <files>backend/tests/test_skill_adapter_cli.py</files>
  <action>
    Create new test file test_skill_adapter_cli.py for CLI skill argument parsing (Phase 25 coverage):

    **File Structure**:
    - Module-level imports: sys, pytest, unittest.mock, backend.core.skill_adapter
    - Mock docker.errors at module level (Phase 182 pattern)
    - Mock execute_atom_cli_command from tools.atom_cli_skill_wrapper

    **Test Class 1: TestCliSkillDetection (4 tests)**
    1. test_atom_prefix_triggers_cli_execution - skill_id starting with "atom-" calls _execute_cli_skill
    2. test_non_atom_prefix_uses_normal_execution - regular skills use prompt/code execution
    3. test_cli_skill_bypasses_sandbox_check - CLI skills don't require sandbox
    4. test_cli_skill_command_extraction - Verify command name extracted from skill_id (e.g., "atom-daemon" -> "daemon")

    **Test Class 2: TestCliArgumentParsing (8 tests)**
    1. test_port_flag_parsing - "port 3000" -> ["--port", "3000"]
    2. test_port_equals_parsing - "port=3000" -> ["--port", "3000"]
    3. test_host_flag_parsing - "host 0.0.0.0" -> ["--host", "0.0.0.0"]
    4. test_workers_flag_parsing - "workers 4" -> ["--workers", "4"]
    5. test_boolean_flags - "host mount" -> ["--host-mount"], "dev" -> ["--dev"]
    6. test_multiple_flags_combined - "port 3000 with dev mode" -> all flags parsed
    7. test_config_show_daemon_flag - atom-config with "show daemon" -> ["--show-daemon"]
    8. test_no_arguments_returns_none - Empty query returns None from _parse_cli_args

    **Test Class 3: TestCliSkillExecution (5 tests)**
    1. test_cli_skill_success_path - execute_atom_cli_command returns success -> formatted output
    2. test_cli_skill_error_path - execute_atom_cli_command fails -> error message returned
    3. test_cli_skill_stderr_included - stderr included in success output
    4. test_cli_skill_exception_handling - Exception caught and formatted as ERROR message
    5. test_cli_skill_logged - Verify logger.info called with command details

    **Mocking Pattern**:
    ```python
    # Module-level mocking for subprocess
    sys.modules['tools.atom_cli_skill_wrapper'] = MagicMock()
    from tools.atom_cli_skill_wrapper import execute_atom_cli_command
    ```

    Follow test_atom_cli_skills.py pattern from Phase 25 for subprocess mocking.
  </action>
  <verify>
    Run: pytest backend/tests/test_skill_adapter_cli.py -v --cov=backend.core.skill_adapter --cov-report=term-missing
    Expected: 17 new tests passing, coverage on _parse_cli_args and _execute_cli_skill
  </verify>
  <done>
    - test_skill_adapter_cli.py created (250+ lines, 17 tests)
    - CLI skill detection tested (atom- prefix triggers CLI path)
    - Argument parsing tested for all flags (--port, --host, --workers, boolean flags)
    - Edge cases tested (no arguments, multiple flags, special cases)
    - Execution path tested (success, error, exception, stderr handling)
  </done>
</task>

<task type="auto">
  <name>Create test_skill_adapter_npm.py for npm packages</name>
  <files>backend/tests/test_skill_adapter_npm.py</files>
  <action>
    Create new test file test_skill_adapter_npm.py for npm package support (Phase 36 coverage):

    **File Structure**:
    - Module-level imports with docker.errors mocking (Phase 182 pattern)
    - Mock NpmPackageInstaller, PackageGovernanceService, NpmScriptAnalyzer
    - Mock SQLAlchemy for db_session (use raw SQL for PackageRegistry if needed)

    **Test Class 1: TestNpmSkillAdapterBasics (5 tests)**
    1. test_npm_skill_adapter_initialization - Verify skill_id, code, node_packages stored
    2. test_package_manager_default - Default package_manager is "npm"
    3. test_package_manager_custom - Can set package_manager to "yarn" or "pnpm"
    4. test_lazy_installer_loading - NpmPackageInstaller loaded on first access
    5. test_lazy_governance_loading - PackageGovernanceService loaded on first access

    **Test Class 2: TestNpmPackageParsing (6 tests)**
    1. test_regular_package_parsing - "lodash@4.17.21" -> ("lodash", "4.17.21")
    2. test_scoped_package_parsing - "@babel/core@^7.0.0" -> ("@babel/core", "^7.0.0")
    3. test_scoped_no_version - "@scope/name" -> ("@scope/name", "latest")
    4. test_no_version_specifier - "express" -> ("express", "latest")
    5. test_caret_version - "package@^1.0.0" -> ("package", "^1.0.0")
    6. test_tilde_version - "package@~2.0.0" -> ("package", "~2.0.0")

    **Test Class 3: TestNpmGovernanceChecks (6 tests)**
    1. test_governance_check_all_packages - Each package checked via governance.check_package_permission
    2. test_governance_denied_stops_installation - Permission denied returns error
    3. test_npm_package_type_in_check - Verify package_type="npm" passed to check
    4. test_agent_id_passed_to_governance - agent_id parameter passed correctly
    5. test_db_session_passed_to_governance - db parameter passed for audit logging
    6. test_continues_on_parse_error - Invalid package spec logged but continues

    **Test Class 4: TestNpmScriptAnalysis (4 tests)**
    1. test_script_analyzer_called - NpmScriptAnalyzer.analyze_package_scripts called
    2. test_malicious_scripts_block_install - malicious=True returns error
    3. test_warnings_logged - Suspicious scripts logged but installation continues
    4. test_package_manager_passed - Package manager passed to analyzer

    **Test Class 5: TestNpmInstallation (5 tests)**
    1. test_installer_install_packages_called - NpmPackageInstaller.install_packages called
    2. test_install_failure_returns_error - Failed install returns error dict
    3. test_vulnerabilities_logged - Vulnerabilities in result logged
    4. test_image_tag_returned - Successful install returns image_tag in result
    5. test_skill_id_used_for_image - skill_id passed to installer (format: "skill-{skill_id}")

    **Test Class 6: TestNpmExecution (4 tests)**
    1. test_execute_with_packages_called - installer.execute_with_packages called
    2. test_inputs_passed - inputs dict passed to execution
    3. test_execution_error_handling - Exception returns error message
    4. test_execution_logged - Success logged with skill_id

    **Mocking Pattern** (from Phase 182-01 npm governance tests):
    ```python
    # Module-level mocking
    sys.modules['docker'] = MagicMock()
    sys.modules['docker.errors'] = MagicMock()
    sys.modules['core.npm_package_installer'] = MagicMock()
    sys.modules['core.package_governance_service'] = MagicMock()
    sys.modules['core.npm_script_analyzer'] = MagicMock()

    # Create mock instances
    mock_installer = MagicMock()
    mock_installer.install_packages.return_value = {
        "success": True,
        "image_tag": "atom-skill:testing-v1"
    }
    ```

    Follow Phase 182-01 pattern: raw SQL for PackageRegistry if SQLAlchemy issues arise.
  </action>
  <verify>
    Run: pytest backend/tests/test_skill_adapter_npm.py -v --cov=backend.core.skill_adapter --cov-report=term-missing
    Expected: 30 new tests passing, coverage on NodeJsSkillAdapter class
  </verify>
  <done>
    - test_skill_adapter_npm.py created (300+ lines, 30 tests)
    - npm package parsing tested (scoped, versioned, default versions)
    - Governance checks tested (permission denied, package_type, agent_id)
    - Script analysis tested (malicious detection, warnings)
    - Installation workflow tested (success, failure, vulnerabilities)
    - Execution tested (inputs, error handling, logging)
  </done>
</task>

<task type="auto">
  <name>Measure skill_adapter coverage and verify 75% target</name>
  <files>backend/tests/test_skill_adapter.py</files>
  <action>
    Measure line coverage on skill_adapter.py and verify 75% target achieved:

    **Coverage Measurement**:
    ```bash
    cd backend
    pytest tests/test_skill_adapter.py \
           tests/test_skill_adapter_cli.py \
           tests/test_skill_adapter_npm.py \
           --cov=core.skill_adapter \
           --cov-report=term-missing \
           --cov-report=html \
           -v
    ```

    **Verify Coverage**:
    1. Check line coverage percentage exceeds 75%
    2. Review missing lines - acceptable if:
       - Exception handlers requiring real subprocess failures
       - Edge cases requiring Docker daemon
       - Import-time side effects
    3. Document any uncovered lines in SUMMARY.md

    **If coverage < 75%**: Add targeted tests for missing lines
    - Focus on high-value paths (not import errors or Docker daemon checks)
    - Use mocks to trigger exception handlers if possible

    **Update Phase Status**:
    - Create 183-01-SUMMARY.md with coverage results
    - Document test counts (original + new)
    - List any deviations from plan
  </action>
  <verify>
    Run: pytest backend/tests/test_skill_adapter*.py --cov=core.skill_adapter --cov-report=term-missing -o addopts=""
    Expected: Coverage >= 75% on skill_adapter.py
  </verify>
  <done>
    - Coverage measured on skill_adapter.py (751 lines)
    - Line coverage >= 75% achieved
    - Missing lines documented (if any)
    - 183-01-SUMMARY.md created with results
  </done>
</task>

</tasks>

<verification>
## Phase 183-01 Verification Criteria

1. **Test Files Created/Extended**:
   - [ ] test_skill_adapter.py extended with Python package tests
   - [ ] test_skill_adapter_cli.py created (250+ lines)
   - [ ] test_skill_adapter_npm.py created (300+ lines)

2. **Test Count**:
   - [ ] 6 new tests for Python packages
   - [ ] 17 new tests for CLI skills
   - [ ] 30 new tests for npm packages
   - [ ] Total: 53 new tests

3. **Coverage Target**:
   - [ ] skill_adapter.py coverage >= 75%
   - [ ] CLI skill methods covered (_parse_cli_args, _execute_cli_skill)
   - [ ] npm package handling covered (NodeJsSkillAdapter class)

4. **Test Execution**:
   - [ ] All new tests passing
   - [ ] No test isolation failures
   - [ ] Module-level mocking works correctly

5. **Documentation**:
   - [ ] 183-01-SUMMARY.md created
   - [ ] Coverage results documented
   - [ ] Deviations documented (if any)
</verification>

<success_criteria>
**Phase 183-01 Complete When**:
- 53 new tests created covering CLI skills, Python packages, npm packages
- skill_adapter.py line coverage >= 75%
- All tests passing (100% pass rate)
- 183-01-SUMMARY.md documents coverage results
</success_criteria>

<output>
After completion, create `.planning/phases/183-core-services-coverage-skill-execution/183-01-SUMMARY.md`
</output>
