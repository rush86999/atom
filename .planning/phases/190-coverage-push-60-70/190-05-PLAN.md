---
phase: 190-coverage-push-60-70
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/tests/core/auth/test_enterprise_auth_coverage.py
autonomous: true

must_haves:
  truths:
    - "Enterprise auth and bulk operations files achieve 75%+ line coverage"
    - "Authentication flow tests cover login, MFA, SSO, token refresh"
    - "Bulk operations tests cover batch processing, error handling, progress tracking"
    - "Execution state manager tests cover state transitions, persistence, recovery"
  artifacts:
    - path: "backend/tests/core/auth/test_enterprise_auth_coverage.py"
      provides: "Comprehensive coverage tests for auth and operations services"
      min_lines: 900
      exports: ["test_enterprise_auth_service", "test_bulk_operations_processor", "test_enhanced_execution_state_manager"]
  key_links:
    - from: "backend/tests/core/auth/test_enterprise_auth_coverage.py"
      to: "core/enterprise_auth_service.py"
      via: "Import and test EnterpriseAuthService class methods"
      pattern: "from core.enterprise_auth_service import EnterpriseAuthService"
---

<objective>
Achieve 75%+ coverage on enterprise auth and operations files (878 statements total) using parametrized tests.

**Purpose:** Target enterprise_auth_service (300 stmts), bulk_operations_processor (292 stmts), enhanced_execution_state_manager (286 stmts) for +658 statements = +1.40% overall coverage gain.

**Output:** Comprehensive test file with ~80 tests achieving 75%+ coverage across 3 auth/operations files.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
@/Users/rushiparikh/projects/atom/backend/core/enterprise_auth_service.py
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Phase 189 patterns - proven parametrized test approach
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md

# Research - Top 30 zero-coverage files
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create coverage tests for enterprise_auth_service.py (300 stmts, target 75%)</name>
  <files>backend/tests/core/auth/test_enterprise_auth_coverage.py</files>
  <action>
    Add comprehensive tests for EnterpriseAuthService class:

    1. Parametrized auth method tests:
       ```python
       @pytest.mark.parametrize("auth_method,credentials,expected_result", [
           ("password", {"username": "user", "password": "pass"}, "success"),
           ("sso", {"sso_token": "token"}, "success"),
           ("api_key", {"api_key": "key"}, "success"),
           ("oauth", {"oauth_code": "code"}, "success"),
       ])
       def test_authenticate_with_method(self, auth_method, credentials, expected_result):
           service = EnterpriseAuthService(db_session)
           result = service.authenticate(auth_method, credentials)
           assert result.status == expected_result
       ```

    2. User authentication tests:
       - test_authenticate_with_password
       - test_authenticate_with_mfa
       - test_authenticate_with_sso
       - test_authenticate_with_oauth

    3. Token management tests:
       - test_generate_access_token
       - test_generate_refresh_token
       - test_validate_token
       - test_refresh_expired_token
       - test_revoke_token

    4. Session management tests:
       - test_create_user_session
       - test_extend_session
       - test_validate_session
       - test_terminate_session
       - test_terminate_all_sessions

    5. Permission tests:
       - test_check_user_permission
       - test_check_role_permission
       - test_check_resource_access
       - test_evaluate_policy

    Target 75% coverage: ~225 covered statements.
  </action>
  <verify>pytest backend/tests/core/auth/test_enterprise_auth_coverage.py::TestEnterpriseAuthServiceCoverage -v --cov=core/enterprise_auth_service --cov-report=term-missing</verify>
  <done>enterprise_auth_service.py achieves 75%+ coverage with ~25 tests passing</done>
</task>

<task type="auto">
  <name>Create coverage tests for bulk_operations_processor.py (292 stmts, target 75%)</name>
  <files>backend/tests/core/auth/test_enterprise_auth_coverage.py</files>
  <action>
    Add comprehensive tests for BulkOperationsProcessor class:

    1. Parametrized operation type tests:
       ```python
       @pytest.mark.parametrize("operation,status,expected_result", [
           ("create", "pending", "queued"),
           ("update", "running", "executed"),
           ("delete", "completed", "finalized"),
           ("bulk", "failed", "retried"),
           ("import", "pending", "processing"),
       ])
       def test_handle_bulk_operation_status(self, operation, status, expected_result):
           processor = BulkOperationsProcessor(db_session)
           result = processor.handle_status(operation, status)
           assert result.action == expected_result
       ```

    2. Bulk operation creation tests:
       - test_create_bulk_operation
       - test_schedule_bulk_operation
       - test_validate_operation_items
       - test_estimate_operation_duration

    3. Batch processing tests:
       - test_process_batch
       - test_process_batch_with_chunking
       - test_handle_batch_progress
       - test_track_batch_completion

    4. Error handling tests:
       - test_handle_operation_failure
       - test_retry_failed_items
       - test_skip_invalid_items
       - test_log_operation_errors

    5. Progress tracking tests:
       - test_get_operation_progress
       - test_update_operation_status
       - test_notify_operation_complete
       - test_generate_operation_report

    Target 75% coverage: ~219 covered statements.
  </action>
  <verify>pytest backend/tests/core/auth/test_enterprise_auth_coverage.py::TestBulkOperationsProcessorCoverage -v --cov=core/bulk_operations_processor --cov-report=term-missing</verify>
  <done>bulk_operations_processor.py achieves 75%+ coverage with ~25 tests passing</done>
</task>

<task type="auto">
  <name>Create coverage tests for enhanced_execution_state_manager.py (286 stmts, target 75%)</name>
  <files>backend/tests/core/auth/test_enterprise_auth_coverage.py</files>
  <action>
    Add comprehensive tests for EnhancedExecutionStateManager class:

    1. Parametrized state transition tests:
       ```python
       @pytest.mark.parametrize("current_state,action,expected_next_state", [
           ("idle", "start", "running"),
           ("running", "pause", "paused"),
           ("paused", "resume", "running"),
           ("running", "complete", "completed"),
           ("paused", "cancel", "cancelled"),
           ("running", "fail", "failed"),
       ])
       def test_state_transition(self, current_state, action, expected_next_state):
           manager = EnhancedExecutionStateManager(db_session)
           result = manager.transition(current_state, action)
           assert result == expected_next_state
       ```

    2. State initialization tests:
       - test_initialize_execution_state
       - test_load_existing_state
       - test_reset_execution_state
       - test_validate_state_structure

    3. State persistence tests:
       - test_save_state_to_database
       - test_load_state_from_database
       - test_update_state_snapshot
       - test_delete_state_snapshot

    4. State recovery tests:
       - test_recover_from_checkpoint
       - test_resume_after_failure
       - test_rollback_to_previous_state
       - test_handle_state_corruption

    5. State query tests:
       - test_get_current_state
       - test_get_state_history
       - test_get_state_transitions
       - test_get_execution_metrics

    Target 75% coverage: ~215 covered statements.
  </action>
  <verify>pytest backend/tests/core/auth/test_enterprise_auth_coverage.py::TestEnhancedExecutionStateManagerCoverage -v --cov=core/enhanced_execution_state_manager --cov-report=term-missing</verify>
  <done>enhanced_execution_state_manager.py achieves 75%+ coverage with ~30 tests passing</done>
</task>

</tasks>

<verification>
1. Run all auth/operations tests: pytest backend/tests/core/auth/test_enterprise_auth_coverage.py -v
2. Check coverage: pytest --cov=core/enterprise_auth_service --cov=core/bulk_operations_processor --cov=core/enhanced_execution_state_manager --cov-report=term-missing
3. Verify each file achieves 75%+ coverage (covered_statements / total_statements >= 0.75)
4. Verify --cov-branch flag is enabled for all tests
5. Check test count: ~80 tests total (25+25+30)
</verification>

<success_criteria>
1. enterprise_auth_service.py achieves 75%+ coverage (225/300 stmts)
2. bulk_operations_processor.py achieves 75%+ coverage (219/292 stmts)
3. enhanced_execution_state_manager.py achieves 75%+ coverage (215/286 stmts)
4. Combined coverage gain: +659 statements = +1.40% overall (659/47,106)
5. All tests use parametrized patterns proven in Phase 189
6. Authentication flows (password, MFA, SSO, OAuth) are tested
7. Bulk operation batch processing is tested
8. State transition logic is comprehensively tested
9. Branch coverage measured with --cov-branch flag
</success_criteria>

<output>
After completion, create `.planning/phases/190-coverage-push-60-70/190-05-SUMMARY.md` with:
- Coverage achieved for each of 3 files (statement counts and percentages)
- Total tests created and pass rate
- Combined coverage impact on overall percentage
- Auth methods tested (password, MFA, SSO, OAuth, API key)
- Bulk operation types tested
- State transitions tested
- Deviations from planned coverage targets
</output>
