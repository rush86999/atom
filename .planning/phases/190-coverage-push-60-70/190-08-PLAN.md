---
phase: 190-coverage-push-60-70
plan: 08
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/tests/core/validation/test_validation_coverage.py
autonomous: true

must_haves:
  truths:
    - "Validation, optimization, and dashboard files achieve 75%+ line coverage"
    - "Validation service tests cover schema validation, rule validation, custom validators"
    - "AI optimizer tests cover optimization strategies, cost calculation, execution planning"
    - "Integration dashboard tests cover data display, real-time updates, user interactions"
  artifacts:
    - path: "backend/tests/core/validation/test_validation_coverage.py"
      provides: "Comprehensive coverage tests for validation and optimization services"
      min_lines: 800
      exports: ["test_validation_service", "test_ai_workflow_optimizer", "test_integration_dashboard"]
  key_links:
    - from: "backend/tests/core/validation/test_validation_coverage.py"
      to: "core/validation_service.py"
      via: "Import and test ValidationService class methods"
      pattern: "from core.validation_service import ValidationService"
---

<objective>
Achieve 75%+ coverage on validation, optimization, and dashboard files (777 statements total) using parametrized tests.

**Purpose:** Target validation_service (264 stmts), ai_workflow_optimizer (261 stmts), integration_dashboard (252 stmts) for +583 statements = +1.24% overall coverage gain.

**Output:** Comprehensive test file with ~75 tests achieving 75%+ coverage across 3 validation files.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
@/Users/rushiparikh/projects/atom/backend/core/validation_service.py
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Phase 189 patterns - proven parametrized test approach
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md

# Research - Top 30 zero-coverage files
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create coverage tests for validation_service.py (264 stmts, target 75%)</name>
  <files>backend/tests/core/validation/test_validation_coverage.py</files>
  <action>
    Add comprehensive tests for ValidationService class:

    1. Parametrized validation type tests:
       ```python
       @pytest.mark.parametrize("validation_type,data,expected_valid", [
           ("schema", {"name": "test"}, True),
           ("schema", {}, False),
           ("rule", {"value": 5, "min": 0, "max": 10}, True),
           ("rule", {"value": 15, "min": 0, "max": 10}, False),
           ("format", {"email": "test@example.com"}, True),
           ("format", {"email": "invalid"}, False),
       ])
       def test_validate_by_type(self, validation_type, data, expected_valid):
           validator = ValidationService()
           result = validator.validate(validation_type, data)
           assert result.is_valid == expected_valid
       ```

    2. Schema validation tests:
       - test_validate_json_schema
       - test_validate_required_fields
       - test_validate_field_types
       - test_validate_nested_schema

    3. Rule validation tests:
       - test_validate_min_value
       - test_validate_max_value
       - test_validate_range
       - test_validate_pattern
       - test_validate_custom_rule

    4. Custom validator tests:
       - test_register_custom_validator
       - test_execute_custom_validator
       - test_handle_validator_error
       - test_chain_validators

    5. Validation result tests:
       - test_collect_validation_errors
       - test_format_validation_report
       - test_get_field_level_errors
       - test_get_summary_stats

    Target 75% coverage: ~198 covered statements.
  </action>
  <verify>pytest backend/tests/core/validation/test_validation_coverage.py::TestValidationServiceCoverage -v --cov=core/validation_service --cov-report=term-missing</verify>
  <done>validation_service.py achieves 75%+ coverage with ~25 tests passing</done>
</task>

<task type="auto">
  <name>Create coverage tests for ai_workflow_optimizer.py (261 stmts, target 75%)</name>
  <files>backend/tests/core/validation/test_validation_coverage.py</files>
  <action>
    Add comprehensive tests for AIWorkflowOptimizer class:

    1. Parametrized optimization strategy tests:
       ```python
       @pytest.mark.parametrize("strategy,workflow_type,expected_improvement", [
           ("parallel", "data_processing", "speed"),
           ("sequential", "approval", "accuracy"),
           ("batch", "notification", "throughput"),
           ("adaptive", "general", "balanced"),
       ])
       def test_optimization_strategy(self, strategy, workflow_type, expected_improvement):
           optimizer = AIWorkflowOptimizer(db_session)
           result = optimizer.optimize(strategy, workflow_type)
           assert result.improvement_type == expected_improvement
       ```

    2. Workflow analysis tests:
       - test_analyze_workflow_structure
       - test_identify_bottlenecks
       - test_estimate_execution_time
       - test_calculate_resource_usage

    3. Optimization tests:
       - test_optimize_for_speed
       - test_optimize_for_cost
       - test_optimize_for_accuracy
       - test_optimize_parallel_steps

    4. Cost calculation tests:
       - test_estimate_execution_cost
       - test_compare_optimization_costs
       - test_calculate_roi
       - test_budget_validation

    5. Planning tests:
       - test_generate_execution_plan
       - test_validate_feasibility
       - test_estimate_completion_time
       - test_allocate_resources

    Target 75% coverage: ~196 covered statements.
  </action>
  <verify>pytest backend/tests/core/validation/test_validation_coverage.py::TestAIWorkflowOptimizerCoverage -v --cov=core/ai_workflow_optimizer --cov-report=term-missing</verify>
  <done>ai_workflow_optimizer.py achieves 75%+ coverage with ~25 tests passing</done>
</task>

<task type="auto">
  <name>Create coverage tests for integration_dashboard.py (252 stmts, target 75%)</name>
  <files>backend/tests/core/validation/test_validation_coverage.py</files>
  <action>
    Add comprehensive tests for IntegrationDashboard class:

    1. Parametrized dashboard widget tests:
       ```python
       @pytest.mark.parametrize("widget_type,data_source,expected_output", [
           ("chart", "metrics", "visualization"),
           ("table", "logs", "tabular"),
           ("status", "integrations", "status_list"),
           ("timeline", "events", "chronological"),
       ])
       def test_render_dashboard_widget(self, widget_type, data_source, expected_output):
           dashboard = IntegrationDashboard(db_session)
           widget = dashboard.render_widget(widget_type, data_source)
           assert widget.type == expected_output
       ```

    2. Dashboard data tests:
       - test_get_integration_metrics
       - test_get_recent_events
       - test_get_system_status
       - test_get_error_summary

    3. Real-time update tests:
       - test_subscribe_to_updates
       - test_push_dashboard_update
       - test_handle_websocket_connection
       - test_broadcast_changes

    4. User interaction tests:
       - test_handle_filter_request
       - test_handle_sort_request
       - test_handle_pagination
       - test_export_dashboard_data

    5. Dashboard configuration tests:
       - test_save_dashboard_config
       - test_load_dashboard_config
       - test_reset_to_default
       - test_customize_layout

    Target 75% coverage: ~189 covered statements.
  </action>
  <verify>pytest backend/tests/core/validation/test_validation_coverage.py::TestIntegrationDashboardCoverage -v --cov=core/integration_dashboard --cov-report=term-missing</verify>
  <done>integration_dashboard.py achieves 75%+ coverage with ~25 tests passing</done>
</task>

</tasks>

<verification>
1. Run all validation tests: pytest backend/tests/core/validation/test_validation_coverage.py -v
2. Check coverage: pytest --cov=core/validation_service --cov=core/ai_workflow_optimizer --cov=core/integration_dashboard --cov-report=term-missing
3. Verify each file achieves 75%+ coverage (covered_statements / total_statements >= 0.75)
4. Verify --cov-branch flag is enabled for all tests
5. Check test count: ~75 tests total (25+25+25)
</verification>

<success_criteria>
1. validation_service.py achieves 75%+ coverage (198/264 stmts)
2. ai_workflow_optimizer.py achieves 75%+ coverage (196/261 stmts)
3. integration_dashboard.py achieves 75%+ coverage (189/252 stmts)
4. Combined coverage gain: +583 statements = +1.24% overall (583/47,106)
5. All tests use parametrized patterns proven in Phase 189
6. Validation covers schema, rules, and custom validators
7. Optimization covers all strategies (parallel, sequential, batch, adaptive)
8. Dashboard covers all widget types
9. Branch coverage measured with --cov-branch flag
</success_criteria>

<output>
After completion, create `.planning/phases/190-coverage-push-60-70/190-08-SUMMARY.md` with:
- Coverage achieved for each of 3 files (statement counts and percentages)
- Total tests created and pass rate
- Combined coverage impact on overall percentage
- Validation types tested
- Optimization strategies tested
- Dashboard widgets tested
- Deviations from planned coverage targets
</output>
