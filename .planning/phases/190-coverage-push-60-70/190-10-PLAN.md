---
phase: 190-coverage-push-60-70
plan: 10
type: execute
wave: 2
depends_on: ["190-02"]
files_modified:
  - backend/tests/api/workflow/test_workflow_analytics_endpoints_coverage.py
autonomous: true

must_haves:
  truths:
    - "Workflow analytics and message analytics endpoints achieve 75%+ line coverage"
    - "Analytics dashboard tests cover KPI queries, timeline data, error metrics"
    - "Analytics endpoint tests cover summary, sentiment, response times, activity metrics"
    - "Message analytics tests cover message aggregation, filtering, trend analysis"
  artifacts:
    - path: "backend/tests/api/workflow/test_workflow_analytics_endpoints_coverage.py"
      provides: "Comprehensive coverage tests for workflow analytics API endpoints"
      min_lines: 700
      exports: ["test_workflow_analytics_endpoints", "test_message_analytics_engine"]
  key_links:
    - from: "backend/tests/api/workflow/test_workflow_analytics_endpoints_coverage.py"
      to: "core/workflow_analytics_endpoints.py"
      via: "Import and test workflow analytics API endpoints"
      pattern: "from core.workflow_analytics_endpoints import workflow_analytics_router"
---

<objective>
Achieve 75%+ coverage on workflow analytics API endpoints and message analytics (552 statements total) using parametrized tests.

**Purpose:** Target workflow_analytics_endpoints (333 stmts), message_analytics_engine (219 stmts) for +414 statements = +0.88% overall coverage gain.

**Output:** Comprehensive test file with ~55 tests achieving 75%+ coverage across 2 analytics files.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
@/Users/rushiparikh/projects/atom/backend/core/workflow_analytics_endpoints.py
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Phase 189 patterns - FastAPI TestClient patterns
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md

# Research - Top 30 zero-coverage files
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create coverage tests for workflow_analytics_endpoints.py (333 stmts, target 75%)</name>
  <files>backend/tests/api/workflow/test_workflow_analytics_endpoints_coverage.py</files>
  <action>
    Add comprehensive tests for workflow analytics API endpoints:

    1. Import and setup:
       ```python
       import pytest
       from fastapi.testclient import TestClient
       from core.workflow_analytics_endpoints import workflow_analytics_router
       from main import app

       app.include_router(workflow_analytics_router)
       client = TestClient(app)
       ```

    2. Parametrized endpoint tests:
       ```python
       @pytest.mark.parametrize("endpoint,method,status_code", [
           ("/analytics/workflows/kpis", "GET", 200),
           ("/analytics/workflows/timeline", "GET", 200),
           ("/analytics/workflows/errors", "GET", 200),
           ("/analytics/workflows/alerts", "GET", 200),
           ("/analytics/workflows/summary", "GET", 200),
           ("/analytics/workflows/performance", "GET", 200),
       ])
       def test_analytics_endpoint(self, endpoint, method, status_code):
           response = client.request(method, endpoint)
           assert response.status_code == status_code
       ```

    3. KPI endpoint tests:
       - test_get_workflow_kpis
       - test_get_kpis_with_date_filter
       - test_get_kpis_with_workflow_filter
       - test_handle_invalid_kpi_request

    4. Timeline endpoint tests:
       - test_get_execution_timeline
       - test_get_workflow_events
       - test_get_step_durations
       - test_get_completion_rates

    5. Error endpoint tests:
       - test_get_error_summary
       - test_get_error_by_type
       - test_get_recent_errors
       - test_get_error_trends

    6. Alert and summary tests:
       - test_get_active_alerts
       - test_get_performance_metrics
       - test_generate_analytics_report
       - test_export_analytics_data

    Target 75% coverage: ~250 covered statements.
  </action>
  <verify>pytest backend/tests/api/workflow/test_workflow_analytics_endpoints_coverage.py::TestWorkflowAnalyticsEndpointsCoverage -v --cov=core/workflow_analytics_endpoints --cov-report=term-missing</verify>
  <done>workflow_analytics_endpoints.py achieves 75%+ coverage with ~30 tests passing</done>
</task>

<task type="auto">
  <name>Create coverage tests for message_analytics_engine.py (219 stmts, target 75%)</name>
  <files>backend/tests/api/workflow/test_workflow_analytics_endpoints_coverage.py</files>
  <action>
    Add comprehensive tests for MessageAnalyticsEngine class:

    1. Parametrized aggregation tests:
       ```python
       @pytest.mark.parametrize("aggregation_type,time_range,expected_output", [
           ("count", "1h", {"total_messages": 100}),
           ("average", "24h", {"avg_per_hour": 4.17}),
           ("sum", "7d", {"total": 700}),
           ("trend", "30d", {"direction": "up"}),
       ])
       def test_aggregate_messages(self, aggregation_type, time_range, expected_output):
           engine = MessageAnalyticsEngine(db_session)
           result = engine.aggregate(aggregation_type, time_range)
           assert result["type"] == aggregation_type
       ```

    2. Message aggregation tests:
       - test_count_messages_by_type
       - test_count_messages_by_sender
       - test_count_messages_by_channel
       - test_aggregate_by_time_interval

    3. Filtering tests:
       - test_filter_by_content
       - test_filter_by_metadata
       - test_filter_by_date_range
       - test_combine_filters

    4. Trend analysis tests:
       - test_analyze_volume_trend
       - test_analyze_sentiment_trend
       - test_analyze_response_time_trend
       - test_detect_anomalies

    5. Analytics calculation tests:
       - test_calculation_engagement_rate
       - test_calculation_response_rate
       - test_calculation_peak_hours
       - test_generate_summary_stats

    Target 75% coverage: ~164 covered statements.
  </action>
  <verify>pytest backend/tests/api/workflow/test_workflow_analytics_endpoints_coverage.py::TestMessageAnalyticsEngineCoverage -v --cov=core/message_analytics_engine --cov-report=term-missing</verify>
  <done>message_analytics_engine.py achieves 75%+ coverage with ~25 tests passing</done>
</task>

</tasks>

<verification>
1. Run all analytics tests: pytest backend/tests/api/workflow/test_workflow_analytics_endpoints_coverage.py -v
2. Check coverage: pytest --cov=core/workflow_analytics_endpoints --cov=core/message_analytics_engine --cov-report=term-missing
3. Verify each file achieves 75%+ coverage (covered_statements / total_statements >= 0.75)
4. Verify --cov-branch flag is enabled for all tests
5. Check test count: ~55 tests total (30+25)
</verification>

<success_criteria>
1. workflow_analytics_endpoints.py achieves 75%+ coverage (250/333 stmts)
2. message_analytics_engine.py achieves 75%+ coverage (164/219 stmts)
3. Combined coverage gain: +414 statements = +0.88% overall (414/47,106)
4. All tests use FastAPI TestClient patterns proven in Phase 189
5. Analytics endpoints cover KPIs, timeline, errors, alerts
6. Message analytics covers all aggregation types
7. Trend analysis and anomaly detection are tested
8. Branch coverage measured with --cov-branch flag
</success_criteria>

<output>
After completion, create `.planning/phases/190-coverage-push-60-70/190-10-SUMMARY.md` with:
- Coverage achieved for each file (statement counts and percentages)
- Total tests created and pass rate
- Combined coverage impact on overall percentage
- Analytics endpoints tested
- Aggregation types tested
- Trend analysis tests included
- Deviations from planned coverage targets
</output>
