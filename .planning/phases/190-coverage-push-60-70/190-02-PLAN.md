---
phase: 190-coverage-push-60-70
plan: 02
type: execute
wave: 2
depends_on: ["190-01"]
files_modified:
  - backend/tests/core/workflow/test_workflow_system_coverage.py
autonomous: true

must_haves:
  truths:
    - "Top 5 workflow system files (auto_document_ingestion, workflow_versioning_system, advanced_workflow_system, workflow_marketplace, proposal_service) achieve 75%+ line coverage"
    - "Parametrized tests cover document type handling, version operations, workflow states, marketplace actions, and proposal statuses"
    - "Coverage-driven test development targets specific uncovered lines identified by coverage.json"
    - "All tests use --cov-branch flag for accurate branch coverage measurement"
  artifacts:
    - path: "backend/tests/core/workflow/test_workflow_system_coverage.py"
      provides: "Comprehensive coverage tests for workflow system files"
      min_lines: 1200
      exports: ["test_auto_document_ingestion", "test_workflow_versioning_system", "test_advanced_workflow_system", "test_workflow_marketplace", "test_proposal_service"]
  key_links:
    - from: "backend/tests/core/workflow/test_workflow_system_coverage.py"
      to: "core/auto_document_ingestion.py"
      via: "Import and test AutoDocumentIngestion class methods"
      pattern: "from core.auto_document_ingestion import AutoDocumentIngestion"
    - from: "backend/tests/core/workflow/test_workflow_system_coverage.py"
      to: "core/workflow_versioning_system.py"
      via: "Import and test WorkflowVersioningSystem class methods"
      pattern: "from core.workflow_versioning_system import WorkflowVersioningSystem"
---

<objective>
Achieve 75%+ coverage on top 5 workflow system zero-coverage files using parametrized tests and coverage-driven development.

**Purpose:** Target highest-impact zero-coverage files (479 + 477 + 473 + 354 + 342 = 2,125 statements) to achieve ~1,594 covered statements (+3.4% overall coverage gain).

**Output:** Comprehensive test file with 120+ tests achieving 75%+ coverage across 5 workflow system files.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
@/Users/rushiparikh/projects/atom/backend/core/auto_document_ingestion.py
@/Users/rushiparikh/projects/atom/backend/core/workflow_versioning_system.py
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Phase 189 patterns - proven parametrized test approach
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md

# Research - Top 30 zero-coverage files
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create coverage tests for auto_document_ingestion.py (479 stmts, target 75%)</name>
  <files>backend/tests/core/workflow/test_workflow_system_coverage.py</files>
  <action>
    Add comprehensive tests for AutoDocumentIngestion class:

    1. Import and setup:
       ```python
       import pytest
       from core.auto_document_ingestion import AutoDocumentIngestion, IngestionResult
       from unittest.mock import Mock, MagicMock, AsyncMock
       ```

    2. Parametrized document type tests (cover lines 50-150):
       ```python
       @pytest.mark.parametrize("doc_type,expected_parser", [
           ("pdf", "PDFParser"),
           ("docx", "DOCXParser"),
           ("txt", "TextParser"),
           ("csv", "CSVParser"),
       ])
       def test_select_document_parser(self, doc_type, expected_parser):
           service = AutoDocumentIngestion(db_session)
           parser = service.get_parser_for_type(doc_type)
           assert parser.__class__.__name__ == expected_parser
       ```

    3. Ingestion flow tests (cover lines 150-250):
       - test_ingest_document_success
       - test_ingest_document_file_not_found
       - test_ingest_document_invalid_format
       - test_ingest_document_metadata_extraction

    4. Document processing tests (cover lines 250-350):
       - test_extract_text_from_pdf
       - test_extract_text_from_docx
       - test_extract_tables_from_csv
       - test_process_document_pages

    5. Error handling tests (cover lines 350-450):
       - test_handle_corrupt_file
       - test_handle_large_file
       - test_handle_encrypted_file
       - test_log_ingestion_errors

    Use mock patterns from Phase 189 for external dependencies (file system, parsers).
    Target 75% coverage: ~359 covered statements.
  </action>
  <verify>pytest backend/tests/core/workflow/test_workflow_system_coverage.py::TestAutoDocumentIngestionCoverage -v --cov=core/auto_document_ingestion --cov-report=term-missing</verify>
  <done>auto_document_ingestion.py achieves 75%+ coverage with ~25 tests passing</done>
</task>

<task type="auto">
  <name>Create coverage tests for workflow_versioning_system.py (477 stmts, target 75%)</name>
  <files>backend/tests/core/workflow/test_workflow_system_coverage.py</files>
  <action>
    Add comprehensive tests for WorkflowVersioningSystem class:

    1. Parametrized version operation tests:
       ```python
       @pytest.mark.parametrize("operation,initial_version,expected_version", [
           ("create", None, "v1.0.0"),
           ("patch", "v1.0.0", "v1.0.1"),
           ("minor", "v1.0.0", "v1.1.0"),
           ("major", "v1.0.0", "v2.0.0"),
       ])
       def test_version_bump(self, operation, initial_version, expected_version):
           service = WorkflowVersioningSystem(db_session)
           result = service.bump_version(initial_version, operation)
           assert result == expected_version
       ```

    2. Version creation tests:
       - test_create_workflow_version
       - test_create_version_with_description
       - test_create_version_with_tags
       - test_version_inheritance

    3. Version comparison tests:
       - test_compare_versions
       - test_check_version_compatibility
       - test_get_latest_version
       - test_list_all_versions

    4. Version rollback tests:
       - test_rollback_to_version
       - test_rollback_with_validation
       - test_rollback_preserves_history

    5. Version diff tests:
       - test_get_version_diff
       - test_get_version_changes
       - test_merge_version_conflicts

    Target 75% coverage: ~358 covered statements.
  </action>
  <verify>pytest backend/tests/core/workflow/test_workflow_system_coverage.py::TestWorkflowVersioningSystemCoverage -v --cov=core/workflow_versioning_system --cov-report=term-missing</verify>
  <done>workflow_versioning_system.py achieves 75%+ coverage with ~25 tests passing</done>
</task>

<task type="auto">
  <name>Create coverage tests for advanced_workflow_system.py (473 stmts, target 75%)</name>
  <files>backend/tests/core/workflow/test_workflow_system_coverage.py</files>
  <action>
    Add comprehensive tests for AdvancedWorkflowSystem class:

    1. Parametrized workflow state tests:
       ```python
       @pytest.mark.parametrize("state,action,expected_next_state", [
           ("draft", "submit", "pending_review"),
           ("pending_review", "approve", "approved"),
           ("pending_review", "reject", "rejected"),
           ("approved", "start", "running"),
           ("running", "complete", "completed"),
       ])
       def test_workflow_state_transition(self, state, action, expected_next_state):
           system = AdvancedWorkflowSystem(db_session)
           result = system.transition_state(state, action)
           assert result == expected_next_state
       ```

    2. Advanced workflow creation tests:
       - test_create_conditional_workflow
       - test_create_parallel_workflow
       - test_create_nested_workflow
       - test_create_workflow_with_dependencies

    3. Workflow execution tests:
       - test_execute_workflow_step
       - test_handle_branching_logic
       - test_execute_parallel_steps
       - test_aggregate_parallel_results

    4. Workflow validation tests:
       - test_validate_workflow_structure
       - test_check_circular_dependencies
       - test_validate_required_inputs
       - test_validate_output_schema

    5. Error handling tests:
       - test_handle_execution_failure
       - test_retry_failed_step
       - test_handle_timeout
       - test_log_execution_metrics

    Target 75% coverage: ~355 covered statements.
  </action>
  <verify>pytest backend/tests/core/workflow/test_workflow_system_coverage.py::TestAdvancedWorkflowSystemCoverage -v --cov=core/advanced_workflow_system --cov-report=term-missing</verify>
  <done>advanced_workflow_system.py achieves 75%+ coverage with ~25 tests passing</done>
</task>

<task type="auto">
  <name>Create coverage tests for workflow_marketplace.py (354 stmts, target 75%)</name>
  <files>backend/tests/core/workflow/test_workflow_system_coverage.py</files>
  <action>
    Add comprehensive tests for WorkflowMarketplace class:

    1. Parametrized marketplace action tests:
       ```python
       @pytest.mark.parametrize("action,workflow_exists,expected_result", [
           ("publish", True, "published"),
           ("publish", False, "not_found"),
           ("unpublish", True, "unpublished"),
           ("download", True, "downloaded"),
           ("rate", True, "rated"),
       ])
       def test_marketplace_action(self, action, workflow_exists, expected_result):
           marketplace = WorkflowMarketplace(db_session)
           result = marketplace.perform_action(action, "test-workflow")
           assert result.status == expected_result
       ```

    2. Workflow publication tests:
       - test_publish_workflow_to_marketplace
       - test_validate_published_workflow
       - test_set_workflow_price
       - test_add_workflow_tags

    3. Workflow discovery tests:
       - test_search_workflows_by_tag
       - test_search_workflows_by_category
       - test_get_trending_workflows
       - test_get_recommended_workflows

    4. Workflow download tests:
       - test_download_workflow_template
       - test_verify_workflow_compatibility
       - test_install_downloaded_workflow
       - test_handle_version_conflicts

    5. Rating and review tests:
       - test_rate_workflow
       - test_submit_review
       - test_get_workflow_ratings
       - test_moderate_review

    Target 75% coverage: ~266 covered statements.
  </action>
  <verify>pytest backend/tests/core/workflow/test_workflow_system_coverage.py::TestWorkflowMarketplaceCoverage -v --cov=core/workflow_marketplace --cov-report=term-missing</verify>
  <done>workflow_marketplace.py achieves 75%+ coverage with ~20 tests passing</done>
</task>

<task type="auto">
  <name>Create coverage tests for proposal_service.py (342 stmts, target 75%)</name>
  <files>backend/tests/core/workflow/test_workflow_system_coverage.py</files>
  <action>
    Add comprehensive tests for ProposalService class:

    1. Parametrized proposal status tests:
       ```python
       @pytest.mark.parametrize("status,action,expected_next_status", [
           ("draft", "submit", "pending_approval"),
           ("pending_approval", "approve", "approved"),
           ("pending_approval", "reject", "rejected"),
           ("approved", "implement", "implementing"),
           ("implementing", "complete", "completed"),
       ])
       def test_proposal_status_transition(self, status, action, expected_next_status):
           service = ProposalService(db_session)
           result = service.transition_status(status, action)
           assert result == expected_next_status
       ```

    2. Proposal creation tests:
       - test_create_proposal
       - test_create_proposal_with_attachments
       - test_create_proposal_with_reviewers
       - test_validate_proposal_content

    3. Proposal approval tests:
       - test_submit_for_approval
       - test_approve_proposal
       - test_reject_proposal_with_feedback
       - test_request_revision

    4. Proposal implementation tests:
       - test_start_implementation
       - test_track_implementation_progress
       - test_mark_proposal_complete
       - test_handle_implementation_failure

    5. Notification tests:
       - test_notify_reviewers
       - test_notify_status_change
       - test_send_reminder
       - test_log_proposal_activity

    Target 75% coverage: ~257 covered statements.
  </action>
  <verify>pytest backend/tests/core/workflow/test_workflow_system_coverage.py::TestProposalServiceCoverage -v --cov=core/proposal_service --cov-report=term-missing</verify>
  <done>proposal_service.py achieves 75%+ coverage with ~20 tests passing</done>
</task>

</tasks>

<verification>
1. Run all workflow system tests: pytest backend/tests/core/workflow/test_workflow_system_coverage.py -v
2. Check coverage for each file: pytest --cov=core/auto_document_ingestion --cov=core/workflow_versioning_system --cov=core/advanced_workflow_system --cov=core/workflow_marketplace --cov=core/proposal_service --cov-report=term-missing
3. Verify each file achieves 75%+ coverage (covered_statements / total_statements >= 0.75)
4. Verify --cov-branch flag is enabled for all tests
5. Check test count: ~120 tests total (25+25+25+20+20)
</verification>

<success_criteria>
1. auto_document_ingestion.py achieves 75%+ coverage (359/479 stmts)
2. workflow_versioning_system.py achieves 75%+ coverage (358/477 stmts)
3. advanced_workflow_system.py achieves 75%+ coverage (355/473 stmts)
4. workflow_marketplace.py achieves 75%+ coverage (266/354 stmts)
5. proposal_service.py achieves 75%+ coverage (257/342 stmts)
6. Combined coverage gain: +1,595 statements = +3.39% overall (1,595/47,106)
7. All tests use parametrized patterns proven in Phase 189
8. Branch coverage measured with --cov-branch flag
</success_criteria>

<output>
After completion, create `.planning/phases/190-coverage-push-60-70/190-02-SUMMARY.md` with:
- Coverage achieved for each of 5 files (statement counts and percentages)
- Total tests created and pass rate
- Combined coverage impact on overall percentage
- Any import blockers or issues encountered
- Deviations from planned coverage targets
</output>
