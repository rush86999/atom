---
phase: 190-coverage-push-60-70
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/workflow_debugger.py
  - core/models.py
autonomous: true

must_haves:
  truths:
    - "workflow_debugger.py can be imported without ImportError for missing models"
    - "All 4 missing models (DebugVariable, ExecutionTrace, WorkflowBreakpoint, WorkflowDebugSession) are created in models.py"
    - "Tests can import from workflow_debugger.py and instantiate WorkflowDebugger class"
    - "Import blocker is resolved, enabling future test coverage for workflow debugging features"
  artifacts:
    - path: "core/models.py"
      provides: "Missing database models for workflow debugging"
      contains: ["class DebugVariable", "class ExecutionTrace", "class WorkflowBreakpoint", "class WorkflowDebugSession"]
    - path: "core/workflow_debugger.py"
      provides: "Workflow debugging service"
      min_lines: 527
  key_links:
    - from: "core/workflow_debugger.py"
      to: "core/models"
      via: "Import statements for DebugVariable, ExecutionTrace, WorkflowBreakpoint, WorkflowDebugSession"
      pattern: "from core.models import (DebugVariable|ExecutionTrace|WorkflowBreakpoint|WorkflowDebugSession)"
---

<objective>
Fix critical import blockers in workflow_debugger.py by creating missing database models, enabling test coverage for workflow debugging features in Phase 190.

**Purpose:** workflow_debugger.py has 4 missing model imports (CRITICAL VALIDATED_BUG from Phase 189) that prevent any test execution. This blocker must be resolved before any coverage can be achieved on this 527-statement file.

**Output:** 4 new database models in models.py, updated imports in workflow_debugger.py, verification test confirming imports work.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
@/Users/rushiparikh/projects/atom/backend/core/workflow_debugger.py
@/Users/rushiparikh/projects/atom/backend/core/models.py
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Phase 189 Aggregate Summary - VALIDATED_BUG documentation
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md

# Research - Top 30 zero-coverage files
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create DebugVariable model in models.py</name>
  <files>backend/core/models.py</files>
  <action>
    Add DebugVariable database model to core/models.py:
    - Table name: workflow_debug_variables
    - Fields: id (String, primary_key), workflow_execution_id (String, ForeignKey), variable_name (String), variable_value (JSON), timestamp (DateTime), captured_at (DateTime, default=datetime.now(timezone.utc))
    - Index: idx_workflow_execution_id
    - Relationship: workflow_execution relationship to WorkflowExecution or WorkflowExecutionLog
    - Position: Add near other workflow-related models (around line 4500-4600 where WorkflowExecutionLog is defined)

    Use SQLAlchemy 2.0 syntax with Mapped[] types matching existing model patterns in models.py.
  </action>
  <verify>grep -n "class DebugVariable" backend/core/models.py</verify>
  <done>DebugVariable model exists with all required fields and relationships</done>
</task>

<task type="auto">
  <name>Create ExecutionTrace model in models.py</name>
  <files>backend/core/models.py</files>
  <action>
    Add ExecutionTrace database model to core/models.py:
    - Table name: workflow_execution_traces
    - Fields: id (String, primary_key), workflow_execution_id (String, ForeignKey), step_id (String), trace_type (String: info/debug/error), message (Text), metadata (JSON), timestamp (DateTime, default=datetime.now(timezone.utc))
    - Index: idx_workflow_execution_trace, idx_trace_type
    - Relationship: workflow_execution relationship to WorkflowExecution or WorkflowExecutionLog
    - Position: Add after DebugVariable model

    Use SQLAlchemy 2.0 syntax with Mapped[] types matching existing model patterns.
  </action>
  <verify>grep -n "class ExecutionTrace" backend/core/models.py</verify>
  <done>ExecutionTrace model exists with all required fields and relationships</done>
</task>

<task type="auto">
  <name>Create WorkflowBreakpoint model in models.py</name>
  <files>backend/core/models.py</files>
  <action>
    Add WorkflowBreakpoint database model to core/models.py:
    - Table name: workflow_breakpoints
    - Fields: id (String, primary_key), workflow_id (String), step_id (String), condition (String, nullable), enabled (Boolean, default=True), hit_count (Integer, default=0), created_by (String), created_at (DateTime, default=datetime.now(timezone.utc))
    - Index: idx_workflow_breakpoint, idx_step_breakpoint
    - Position: Add after ExecutionTrace model

    Use SQLAlchemy 2.0 syntax with Mapped[] types matching existing model patterns.
  </action>
  <verify>grep -n "class WorkflowBreakpoint" backend/core/models.py</verify>
  <done>WorkflowBreakpoint model exists with all required fields and indexes</done>
</task>

<task type="auto">
  <name>Create WorkflowDebugSession model in models.py</name>
  <files>backend/core/models.py</files>
  <action>
    Add WorkflowDebugSession database model to core/models.py:
    - Table name: workflow_debug_sessions
    - Fields: id (String, primary_key), workflow_execution_id (String, ForeignKey), session_type (String: interactive/automated), status (String: active/paused/completed), breakpoints (JSON, default=list), current_step (String, nullable), started_at (DateTime, default=datetime.now(timezone.utc)), ended_at (DateTime, nullable)
    - Index: idx_debug_session_execution, idx_debug_session_status
    - Relationships: workflow_execution relationship to WorkflowExecution or WorkflowExecutionLog, breakpoints relationship to WorkflowBreakpoint
    - Position: Add after WorkflowBreakpoint model

    Use SQLAlchemy 2.0 syntax with Mapped[] types matching existing model patterns.
  </action>
  <verify>grep -n "class WorkflowDebugSession" backend/core/models.py</verify>
  <done>WorkflowDebugSession model exists with all required fields and relationships</done>
</task>

<task type="auto">
  <name>Verify workflow_debugger.py imports work correctly</name>
  <files>backend/core/workflow_debugger.py</files>
  <action>
    1. Verify workflow_debugger.py imports the 4 new models from models.py
    2. If imports are already present but commented/wrong, fix them
    3. Create a simple verification test: backend/tests/core/workflow/test_debugger_models_exist.py
    4. Test imports WorkflowDebugger class and creates a mock instance
    5. Run: pytest backend/tests/core/workflow/test_debugger_models_exist.py -v

    The verification test should:
    - Import all 4 models from core.models
    - Import WorkflowDebugger from core.workflow_debugger
    - Assert models have expected attributes (id, workflow_execution_id, etc.)
    - Confirm no ImportError occurs
  </action>
  <verify>
    PYTHONPATH=backend python -c "from core.models import DebugVariable, ExecutionTrace, WorkflowBreakpoint, WorkflowDebugSession; from core.workflow_debugger import WorkflowDebugger; print('All imports successful')"
  </verify>
  <done>All 4 models import successfully, WorkflowDebugger imports without errors, verification test passes</done>
</task>

</tasks>

<verification>
1. Run import verification: PYTHONPATH=backend python -c "from core.models import DebugVariable, ExecutionTrace, WorkflowBreakpoint, WorkflowDebugSession; print('Models OK')"
2. Run workflow_debugger import: PYTHONPATH=backend python -c "from core.workflow_debugger import WorkflowDebugger; print('WorkflowDebugger OK')"
3. Run verification test: pytest backend/tests/core/workflow/test_debugger_models_exist.py -v
4. Check for no ImportError in workflow_debugger.py imports
5. Verify models have correct table names and relationships
</verification>

<success_criteria>
1. All 4 missing models (DebugVariable, ExecutionTrace, WorkflowBreakpoint, WorkflowDebugSession) exist in models.py
2. workflow_debugger.py can import all 4 models without ImportError
3. WorkflowDebugger class can be instantiated without import errors
4. Verification test passes confirming models exist and are importable
5. Import blocker is resolved, enabling Plan 190-02 to add test coverage for workflow_debugger.py
</success_criteria>

<output>
After completion, create `.planning/phases/190-coverage-push-60-70/190-01-SUMMARY.md` with:
- Models created (4 total with field counts)
- Import verification results
- Test file created for verification
- Any deviations or additional fixes needed
</output>
