---
phase: 190-coverage-push-60-70
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/tests/core/ingestion/test_hybrid_ingestion_coverage.py
autonomous: true

must_haves:
  truths:
    - "Top 3 ingestion files (hybrid_data_ingestion, formula_extractor, integration_data_mapper) achieve 75%+ line coverage"
    - "Parametrized tests cover data sources, extraction types, and mapping strategies"
    - "Hybrid ingestion (LanceDB + PostgreSQL) is tested with mock patterns"
    - "Formula extraction edge cases are tested with various formula types"
  artifacts:
    - path: "backend/tests/core/ingestion/test_hybrid_ingestion_coverage.py"
      provides: "Comprehensive coverage tests for ingestion services"
      min_lines: 900
      exports: ["test_hybrid_data_ingestion", "test_formula_extractor", "test_integration_data_mapper"]
  key_links:
    - from: "backend/tests/core/ingestion/test_hybrid_ingestion_coverage.py"
      to: "core/hybrid_data_ingestion.py"
      via: "Import and test HybridDataIngestion class methods"
      pattern: "from core.hybrid_data_ingestion import HybridDataIngestion"
---

<objective>
Achieve 75%+ coverage on top 3 ingestion zero-coverage files (965 statements total) using parametrized tests and mock patterns.

**Purpose:** Target hybrid_data_ingestion (314 stmts), formula_extractor (313 stmts), integration_data_mapper (338 stmts) for +724 statements = +1.54% overall coverage gain.

**Output:** Comprehensive test file with ~85 tests achieving 75%+ coverage across 3 ingestion files.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
@/Users/rushiparikh/projects/atom/backend/core/hybrid_data_ingestion.py
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Phase 189 patterns - proven parametrized test approach
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md

# Research - Top 30 zero-coverage files
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create coverage tests for hybrid_data_ingestion.py (314 stmts, target 75%)</name>
  <files>backend/tests/core/ingestion/test_hybrid_ingestion_coverage.py</files>
  <action>
    Add comprehensive tests for HybridDataIngestion class:

    1. Parametrized source/format tests:
       ```python
       @pytest.mark.parametrize("source,format,expected_parser", [
           ("local_file", "pdf", "PDFParser"),
           ("local_file", "csv", "CSVParser"),
           ("s3", "json", "JSONParser"),
           ("url", "html", "HTMLParser"),
           ("database", "sql", "SQLParser"),
       ])
       def test_select_parser_for_source(self, source, format, expected_parser):
           service = HybridDataIngestion(db_session)
           parser = service.select_parser(source=source, format=format)
           assert parser.__class__.__name__ == expected_parser
       ```

    2. Hybrid storage tests (LanceDB + PostgreSQL):
       - test_ingest_to_postgres
       - test_ingest_to_lancedb
       - test_dual_storage_ingestion
       - test_sync_hybrid_storage

    3. Data processing tests:
       - test_chunk_file_processing
       - test_process_large_file
       - test_handle_binary_data
       - test_extract_metadata

    4. Error handling tests:
       - test_handle_unsupported_format
       - test_handle_corrupt_data
       - test_handle_storage_failure
       - test_retry_failed_ingestion

    Target 75% coverage: ~236 covered statements.
  </action>
  <verify>pytest backend/tests/core/ingestion/test_hybrid_ingestion_coverage.py::TestHybridDataIngestionCoverage -v --cov=core/hybrid_data_ingestion --cov-report=term-missing</verify>
  <done>hybrid_data_ingestion.py achieves 75%+ coverage with ~25 tests passing</done>
</task>

<task type="auto">
  <name>Create coverage tests for formula_extractor.py (313 stmts, target 75%)</name>
  <files>backend/tests/core/ingestion/test_hybrid_ingestion_coverage.py</files>
  <action>
    Add comprehensive tests for FormulaExtractor class:

    1. Parametrized formula type tests:
       ```python
       @pytest.mark.parametrize("formula_type,formula,expected_result", [
           ("arithmetic", "A1+B1", {"type": "binary", "op": "+"}),
           ("function", "SUM(A1:A10)", {"type": "function", "name": "SUM"}),
           ("reference", "Sheet2!A1", {"type": "reference", "sheet": "Sheet2"}),
           ("conditional", "IF(A1>0,B1,C1)", {"type": "conditional"}),
           ("lookup", "VLOOKUP(A1,B:C,2,FALSE)", {"type": "lookup"}),
       ])
       def test_extract_formula_type(self, formula_type, formula, expected_result):
           extractor = FormulaExtractor()
           result = extractor.parse_formula(formula)
           assert result["type"] == expected_result["type"]
       ```

    2. Formula parsing tests:
       - test_parse_simple_formula
       - test_parse_nested_formula
       - test_parse_array_formula
       - test_parse_complex_expression

    3. Dependency extraction tests:
       - test_extract_cell_dependencies
       - test_extract_range_dependencies
       - test_detect_circular_reference
       - test_build_dependency_graph

    4. Formula validation tests:
       - test_validate_formula_syntax
       - test_check_function_arguments
       - test_detect_invalid_reference
       - test_validate_data_types

    5. Edge case tests:
       - test_handle_empty_formula
       - test_handle_malformed_formula
       - test_handle_very_long_formula
       - test_handle_unicode_in_formula

    Target 75% coverage: ~235 covered statements.
  </action>
  <verify>pytest backend/tests/core/ingestion/test_hybrid_ingestion_coverage.py::TestFormulaExtractorCoverage -v --cov=core/formula_extractor --cov-report=term-missing</verify>
  <done>formula_extractor.py achieves 75%+ coverage with ~30 tests passing</done>
</task>

<task type="auto">
  <name>Create coverage tests for integration_data_mapper.py (338 stmts, target 75%)</name>
  <files>backend/tests/core/ingestion/test_hybrid_ingestion_coverage.py</files>
  <action>
    Add comprehensive tests for IntegrationDataMapper class:

    1. Parametrized mapping strategy tests:
       ```python
       @pytest.mark.parametrize("source_schema,target_schema,mapping_strategy", [
           ("salesforce", "postgres", "field_mapping"),
           ("hubspot", "postgres", "transform_mapping"),
           ("stripe", "postgres", "enrichment_mapping"),
           ("custom", "postgres", "custom_mapping"),
       ])
       def test_select_mapping_strategy(self, source_schema, target_schema, mapping_strategy):
           mapper = IntegrationDataMapper(db_session)
           strategy = mapper.get_strategy(source_schema, target_schema)
           assert strategy == mapping_strategy
       ```

    2. Field mapping tests:
       - test_map_fields_direct
       - test_map_fields_with_transform
       - test_map_fields_with_default
       - test_map_nested_fields

    3. Data transformation tests:
       - test_transform_date_format
       - test_transform_currency
       - test_transform_phone_number
       - test_transform_custom_function

    4. Batch processing tests:
       - test_map_batch_records
       - test_handle_batch_errors
       - test_track_mapping_progress
       - test_resume_failed_batch

    5. Validation tests:
       - test_validate_mapped_data
       - test_check_required_fields
       - test_validate_data_types
       - test_detect_mapping_conflicts

    Target 75% coverage: ~254 covered statements.
  </action>
  <verify>pytest backend/tests/core/ingestion/test_hybrid_ingestion_coverage.py::TestIntegrationDataMapperCoverage -v --cov=core/integration_data_mapper --cov-report=term-missing</verify>
  <done>integration_data_mapper.py achieves 75%+ coverage with ~30 tests passing</done>
</task>

</tasks>

<verification>
1. Run all ingestion tests: pytest backend/tests/core/ingestion/test_hybrid_ingestion_coverage.py -v
2. Check coverage: pytest --cov=core/hybrid_data_ingestion --cov=core/formula_extractor --cov=core/integration_data_mapper --cov-report=term-missing
3. Verify each file achieves 75%+ coverage (covered_statements / total_statements >= 0.75)
4. Verify --cov-branch flag is enabled for all tests
5. Check test count: ~85 tests total (25+30+30)
</verification>

<success_criteria>
1. hybrid_data_ingestion.py achieves 75%+ coverage (236/314 stmts)
2. formula_extractor.py achieves 75%+ coverage (235/313 stmts)
3. integration_data_mapper.py achieves 75%+ coverage (254/338 stmts)
4. Combined coverage gain: +725 statements = +1.54% overall (725/47,106)
5. All tests use parametrized patterns proven in Phase 189
6. Hybrid storage (LanceDB + PostgreSQL) is properly mocked
7. Formula extraction edge cases are tested
8. Branch coverage measured with --cov-branch flag
</success_criteria>

<output>
After completion, create `.planning/phases/190-coverage-push-60-70/190-04-SUMMARY.md` with:
- Coverage achieved for each of 3 files (statement counts and percentages)
- Total tests created and pass rate
- Combined coverage impact on overall percentage
- Hybrid storage mock patterns used
- Formula types tested (arithmetic, function, reference, conditional, lookup)
- Deviations from planned coverage targets
</output>
