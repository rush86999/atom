---
phase: 190-coverage-push-60-70
plan: 12
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/tests/core/agents/test_embedding_service_coverage.py
autonomous: true

must_haves:
  truths:
    - "Embedding service and atom meta agent world model files achieve 75%+ line coverage"
    - "Embedding service tests cover vector generation, caching, batch operations"
    - "World model tests cover knowledge storage, retrieval, fact verification"
    - "FastEmbed local embeddings are tested with mock patterns"
  artifacts:
    - path: "backend/tests/core/agents/test_embedding_service_coverage.py"
      provides: "Comprehensive coverage tests for embedding and world model services"
      min_lines: 700
      exports: ["test_embedding_service", "test_agent_world_model"]
  key_links:
    - from: "backend/tests/core/agents/test_embedding_service_coverage.py"
      to: "core/embedding_service.py"
      via: "Import and test EmbeddingService class methods"
      pattern: "from core.embedding_service import EmbeddingService"
---

<objective>
Achieve 75%+ coverage on embedding service and world model files (648 statements total) using parametrized tests.

**Purpose:** Target embedding_service (317 stmts), agent_world_model (331 stmts) for +486 statements = +1.03% overall coverage gain.

**Output:** Comprehensive test file with ~60 tests achieving 75%+ coverage across 2 embedding files.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
@/Users/rushiparikh/projects/atom/backend/core/embedding_service.py
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Phase 189 patterns - proven parametrized test approach
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md

# Research - Top 30 zero-coverage files
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create coverage tests for embedding_service.py (317 stmts, target 75%)</name>
  <files>backend/tests/core/agents/test_embedding_service_coverage.py</files>
  <action>
    Add comprehensive tests for EmbeddingService class:

    1. Parametrized embedding model tests:
       ```python
       @pytest.mark.parametrize("model_name,embedding_dim", [
           ("BAAI/bge-small-en-v1.5", 384),
           ("sentence-transformers/all-MiniLM-L6-v2", 384),
           ("custom-model", 768),
       ])
       def test_load_embedding_model(self, model_name, embedding_dim):
           service = EmbeddingService(model_name=model_name)
           assert service.embedding_dim == embedding_dim
       ```

    2. Embedding generation tests:
       - test_generate_embedding_for_text
       - test_generate_embedding_for_short_text
       - test_generate_embedding_for_long_text
       - test_handle_empty_text

    3. Batch embedding tests:
       - test_generate_batch_embeddings
       - test_handle_batch_size_limit
       - test_process_large_batch
       - test_track_batch_progress

    4. Caching tests:
       - test_cache_embedding_result
       - test_retrieve_cached_embedding
       - test_invalidate_cache
       - test_cache_hit_rate_tracking

    5. Vector operations tests:
       - test_calculate_similarity
       - test_calculate_cosine_similarity
       - test_find_nearest_neighbors
       - test_batch_similarity_search

    6. Error handling tests:
       - test_handle_model_load_failure
       - test_handle_encoding_error
       - test_handle_timeout
       - test_retry_with_fallback

    Target 75% coverage: ~238 covered statements.
  </action>
  <verify>pytest backend/tests/core/agents/test_embedding_service_coverage.py::TestEmbeddingServiceCoverage -v --cov=core/embedding_service --cov-report=term-missing</verify>
  <done>embedding_service.py achieves 75%+ coverage with ~30 tests passing</done>
</task>

<task type="auto">
  <name>Create coverage tests for agent_world_model.py (331 stmts, target 75%)</name>
  <files>backend/tests/core/agents/test_embedding_service_coverage.py</files>
  <action>
    Add comprehensive tests for AgentWorldModel class:

    1. Parametrized knowledge storage tests:
       ```python
       @pytest.mark.parametrize("knowledge_type,data,expected_result", [
           ("fact", {"fact": "test", "source": "doc"}, "stored"),
           ("rule", {"rule": "if X then Y", "priority": "high"}, "stored"),
           ("entity", {"name": "Test", "type": "organization"}, "stored"),
           ("relationship", {"from": "A", "to": "B", "type": "uses"}, "stored"),
       ])
       def test_store_knowledge(self, knowledge_type, data, expected_result):
           model = AgentWorldModel(db_session)
           result = model.store(knowledge_type, data)
           assert result["status"] == expected_result
       ```

    2. Knowledge retrieval tests:
       - test_retrieve_by_fact
       - test_retrieve_by_entity
       - test_retrieve_by_relationship
       - test_retrieve_by_context

    3. Fact verification tests:
       - test_verify_fact_with_citation
       - test_check_fact_source
       - test_validate_fact_timestamp
       - test_handle_conflicting_facts

    4. Knowledge graph tests:
       - test_build_knowledge_graph
       - test_traverse_relationships
       - test_find_shortest_path
       - test_detect_cycles

    5. World model query tests:
       - test_query_world_state
       - test_get_agent_context
       - test_get_environment_state
       - test_infer_missing_information

    6. World model update tests:
       - test_update_fact
       - test_retract_fact
       - test_merge_knowledge
       - test_sync_with_external_source

    Target 75% coverage: ~248 covered statements.
  </action>
  <verify>pytest backend/tests/core/agents/test_embedding_service_coverage.py::TestAgentWorldModelCoverage -v --cov=core/agent_world_model --cov-report=term-missing</verify>
  <done>agent_world_model.py achieves 75%+ coverage with ~30 tests passing</done>
</task>

</tasks>

<verification>
1. Run all embedding tests: pytest backend/tests/core/agents/test_embedding_service_coverage.py -v
2. Check coverage: pytest --cov=core/embedding_service --cov=core/agent_world_model --cov-report=term-missing
3. Verify each file achieves 75%+ coverage (covered_statements / total_statements >= 0.75)
4. Verify --cov-branch flag is enabled for all tests
5. Check test count: ~60 tests total (30+30)
</verification>

<success_criteria>
1. embedding_service.py achieves 75%+ coverage (238/317 stmts)
2. agent_world_model.py achieves 75%+ coverage (248/331 stmts)
3. Combined coverage gain: +486 statements = +1.03% overall (486/47,106)
4. All tests use parametrized patterns proven in Phase 189
5. Embedding service covers all models (BAAI, sentence-transformers, custom)
6. Batch operations are tested
7. World model covers all knowledge types (fact, rule, entity, relationship)
8. Branch coverage measured with --cov-branch flag
</success_criteria>

<output>
After completion, create `.planning/phases/190-coverage-push-60-70/190-12-SUMMARY.md` with:
- Coverage achieved for each file (statement counts and percentages)
- Total tests created and pass rate
- Combined coverage impact on overall percentage
- Embedding models tested
- Knowledge types tested
- Vector operations tested
- Deviations from planned coverage targets
</output>
