---
phase: 190-coverage-push-60-70
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/tests/core/workflow_validation/test_workflow_validation_coverage.py
autonomous: true

must_haves:
  truths:
    - "Workflow validation and UI endpoints files achieve 75%+ line coverage"
    - "Parameter validation tests cover type checking, constraint validation, schema validation"
    - "Template endpoint tests cover CRUD operations, versioning, deployment"
    - "Advanced workflow endpoint tests cover execution, monitoring, debugging"
  artifacts:
    - path: "backend/tests/core/workflow_validation/test_workflow_validation_coverage.py"
      provides: "Comprehensive coverage tests for workflow validation and endpoints"
      min_lines: 900
      exports: ["test_workflow_parameter_validator", "test_workflow_template_endpoints", "test_advanced_workflow_endpoints"]
  key_links:
    - from: "backend/tests/core/workflow_validation/test_workflow_validation_coverage.py"
      to: "core/workflow_parameter_validator.py"
      via: "Import and test WorkflowParameterValidator class methods"
      pattern: "from core.workflow_parameter_validator import WorkflowParameterValidator"
---

<objective>
Achieve 75%+ coverage on workflow validation and endpoint files (837 statements total) using parametrized tests.

**Purpose:** Target workflow_parameter_validator (286 stmts), workflow_template_endpoints (276 stmts), advanced_workflow_endpoints (275 stmts) for +628 statements = +1.33% overall coverage gain.

**Output:** Comprehensive test file with ~80 tests achieving 75%+ coverage across 3 workflow files.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
@/Users/rushiparikh/projects/atom/backend/core/workflow_parameter_validator.py
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Phase 189 patterns - proven parametrized test approach
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md

# Research - Top 30 zero-coverage files
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create coverage tests for workflow_parameter_validator.py (286 stmts, target 75%)</name>
  <files>backend/tests/core/workflow_validation/test_workflow_validation_coverage.py</files>
  <action>
    Add comprehensive tests for WorkflowParameterValidator class:

    1. Parametrized type validation tests:
       ```python
       @pytest.mark.parametrize("param_type,value,expected_valid", [
           ("string", "hello", True),
           ("string", 123, False),
           ("integer", 42, True),
           ("integer", "42", False),
           ("float", 3.14, True),
           ("float", "3.14", False),
           ("boolean", True, True),
           ("boolean", "true", False),
           ("array", [1,2,3], True),
           ("array", "not_array", False),
           ("object", {"key": "value"}, True),
           ("object", "not_object", False),
       ])
       def test_validate_parameter_type(self, param_type, value, expected_valid):
           validator = WorkflowParameterValidator()
           result = validator.validate_type(param_type, value)
           assert result.is_valid == expected_valid
       ```

    2. Constraint validation tests:
       - test_validate_required_parameter
       - test_validate_optional_parameter
       - test_validate_min_max_values
       - test_validate_min_max_length
       - test_validate_pattern_regex
       - test_validate_enum_values

    3. Schema validation tests:
       - test_validate_parameter_schema
       - test_validate_nested_schema
       - test_validate_array_schema
       - test_validate_object_schema

    4. Cross-parameter validation tests:
       - test_validate_parameter_dependencies
       - test_validate_mutually_exclusive_params
       - test_validate_conditional_required

    5. Error reporting tests:
       - test_generate_validation_error
       - test_collect_all_validation_errors
       - test_format_error_message

    Target 75% coverage: ~215 covered statements.
  </action>
  <verify>pytest backend/tests/core/workflow_validation/test_workflow_validation_coverage.py::TestWorkflowParameterValidatorCoverage -v --cov=core/workflow_parameter_validator --cov-report=term-missing</verify>
  <done>workflow_parameter_validator.py achieves 75%+ coverage with ~30 tests passing</done>
</task>

<task type="auto">
  <name>Create coverage tests for workflow_template_endpoints.py (276 stmts, target 75%)</name>
  <files>backend/tests/core/workflow_validation/test_workflow_validation_coverage.py</files>
  <action>
    Add comprehensive tests for workflow template endpoints:

    1. Parametrized endpoint tests:
       ```python
       @pytest.mark.parametrize("endpoint,method,status_code", [
           ("/templates", "GET", 200),
           ("/templates", "POST", 201),
           ("/templates/{id}", "GET", 200),
           ("/templates/{id}", "PUT", 200),
           ("/templates/{id}", "DELETE", 204),
           ("/templates/{id}/versions", "GET", 200),
           ("/templates/{id}/deploy", "POST", 200),
       ])
       def test_template_endpoint(self, endpoint, method, status_code):
           client = TestClient(app)
           response = client.request(method, endpoint)
           assert response.status_code == status_code
       ```

    2. Template CRUD tests:
       - test_create_template
       - test_get_template_by_id
       - test_list_templates
       - test_update_template
       - test_delete_template

    3. Template versioning tests:
       - test_create_template_version
       - test_list_template_versions
       - test_get_template_version
       - test_compare_template_versions
       - test_revert_to_version

    4. Template deployment tests:
       - test_deploy_template
       - test_validate_template_before_deploy
       - test_get_deployment_status
       - test_rollback_deployment

    5. Template import/export tests:
       - test_import_template_from_file
       - test_export_template_to_file
       - test_validate_import_format
       - test_handle_import_errors

    Target 75% coverage: ~207 covered statements.
  </action>
  <verify>pytest backend/tests/core/workflow_validation/test_workflow_validation_coverage.py::TestWorkflowTemplateEndpointsCoverage -v --cov=core/workflow_template_endpoints --cov-report=term-missing</verify>
  <done>workflow_template_endpoints.py achieves 75%+ coverage with ~25 tests passing</done>
</task>

<task type="auto">
  <name>Create coverage tests for advanced_workflow_endpoints.py (275 stmts, target 75%)</name>
  <files>backend/tests/core/workflow_validation/test_workflow_validation_coverage.py</files>
  <action>
    Add comprehensive tests for advanced workflow endpoints:

    1. Parametrized execution tests:
       ```python
       @pytest.mark.parametrize("endpoint,method,expected_status", [
           ("/workflows/execute", "POST", "running"),
           ("/workflows/{id}/status", "GET", "success"),
           ("/workflows/{id}/pause", "POST", "paused"),
           ("/workflows/{id}/resume", "POST", "running"),
           ("/workflows/{id}/cancel", "POST", "cancelled"),
           ("/workflows/{id}/logs", "GET", "success"),
       ])
       def test_workflow_execution_endpoint(self, endpoint, method, expected_status):
           client = TestClient(app)
           response = client.request(method, endpoint)
           assert response.json()["status"] == expected_status
       ```

    2. Advanced execution tests:
       - test_execute_conditional_workflow
       - test_execute_parallel_workflow
       - test_execute_nested_workflow
       - test_execute_scheduled_workflow

    3. Workflow monitoring tests:
       - test_get_workflow_metrics
       - test_get_execution_history
       - test_get_performance_stats
       - test_get_error_summary

    4. Workflow debugging tests:
       - test_set_breakpoint
       - test_get_execution_trace
       - test_inspect_variables
       - test_step_through_workflow

    5. Error handling tests:
       - test_handle_execution_timeout
       - test_handle_execution_failure
       - test_retry_failed_workflow
       - test_get_error_details

    Target 75% coverage: ~206 covered statements.
  </action>
  <verify>pytest backend/tests/core/workflow_validation/test_workflow_validation_coverage.py::TestAdvancedWorkflowEndpointsCoverage -v --cov=core/advanced_workflow_endpoints --cov-report=term-missing</verify>
  <done>advanced_workflow_endpoints.py achieves 75%+ coverage with ~25 tests passing</done>
</task>

</tasks>

<verification>
1. Run all workflow validation tests: pytest backend/tests/core/workflow_validation/test_workflow_validation_coverage.py -v
2. Check coverage: pytest --cov=core/workflow_parameter_validator --cov=core/workflow_template_endpoints --cov=core/advanced_workflow_endpoints --cov-report=term-missing
3. Verify each file achieves 75%+ coverage (covered_statements / total_statements >= 0.75)
4. Verify --cov-branch flag is enabled for all tests
5. Check test count: ~80 tests total (30+25+25)
</verification>

<success_criteria>
1. workflow_parameter_validator.py achieves 75%+ coverage (215/286 stmts)
2. workflow_template_endpoints.py achieves 75%+ coverage (207/276 stmts)
3. advanced_workflow_endpoints.py achieves 75%+ coverage (206/275 stmts)
4. Combined coverage gain: +628 statements = +1.33% overall (628/47,106)
5. All tests use parametrized patterns proven in Phase 189
6. Parameter validation covers all types (string, integer, float, boolean, array, object)
7. Template endpoints cover CRUD and versioning
8. Advanced endpoints cover execution, monitoring, debugging
9. Branch coverage measured with --cov-branch flag
</success_criteria>

<output>
After completion, create `.planning/phases/190-coverage-push-60-70/190-06-SUMMARY.md` with:
- Coverage achieved for each of 3 files (statement counts and percentages)
- Total tests created and pass rate
- Combined coverage impact on overall percentage
- Parameter types tested
- Template operations tested (CRUD, versioning, deployment)
- Advanced operations tested (execution, monitoring, debugging)
- Deviations from planned coverage targets
</output>
