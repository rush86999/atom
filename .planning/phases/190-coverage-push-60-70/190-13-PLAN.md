---
phase: 190-coverage-push-60-70
plan: 13
type: execute
wave: 3
depends_on: ["190-01", "190-02", "190-03", "190-04", "190-05", "190-06", "190-07", "190-08", "190-09", "190-10", "190-11", "190-12"]
files_modified:
  - backend/tests/core/workflow/test_workflow_debugger_coverage.py
autonomous: true

must_haves:
  truths:
    - "workflow_debugger.py achieves 75%+ line coverage (527 statements, currently 0% due to import blockers)"
    - "Debugger tests cover breakpoint management, execution tracing, variable inspection"
    - "Debug session tests cover session lifecycle, state management, replay functionality"
    - "Import blockers fixed in Plan 190-01 enable full test coverage"
  artifacts:
    - path: "backend/tests/core/workflow/test_workflow_debugger_coverage.py"
      provides: "Comprehensive coverage tests for workflow debugging service"
      min_lines: 700
      exports: ["test_workflow_debugger", "test_debug_sessions", "test_execution_traces"]
  key_links:
    - from: "backend/tests/core/workflow/test_workflow_debugger_coverage.py"
      to: "core/workflow_debugger.py"
      via: "Import and test WorkflowDebugger class methods"
      pattern: "from core.workflow_debugger import WorkflowDebugger"
---

<objective>
Achieve 75%+ coverage on workflow_debugger.py (527 stmts) after fixing import blockers in Plan 190-01.

**Purpose:** workflow_debugger.py was 0% in Phase 189 due to 4 missing models. After Plan 190-01 fixes, target 75% coverage (+395 stmts = +0.84% overall).

**Output:** Comprehensive test file with ~55 tests achieving 75%+ coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
@/Users/rushiparikh/projects/atom/backend/core/workflow_debugger.py
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# Phase 189 patterns - workflow debugger tests created but blocked
@.planning/phases/189-backend-80-coverage-achievement/189-AGGREGATE-SUMMARY.md

# Plan 190-01 - Import blocker fixes
@.planning/phases/190-coverage-push-60-70/190-01-PLAN.md

# Research - Top 30 zero-coverage files
@.planning/phases/190-coverage-push-60-70/190-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create coverage tests for workflow debugger breakpoint management</name>
  <files>backend/tests/core/workflow/test_workflow_debugger_coverage.py</files>
  <action>
    Add comprehensive tests for WorkflowDebugger breakpoint management (cover lines 1-175):

    1. Import and setup (after Plan 190-01 fixes):
       ```python
       import pytest
       from core.workflow_debugger import WorkflowDebugger
       from core.models import WorkflowBreakpoint, DebugVariable, ExecutionTrace, WorkflowDebugSession
       from unittest.mock import Mock, AsyncMock
       import pytest_asyncio
       ```

    2. Parametrized breakpoint tests:
       ```python
       @pytest.mark.parametrize("step_id,condition,enabled", [
           ("step-1", None, True),
           ("step-2", "value > 100", True),
           ("step-3", "status == 'error'", True),
           ("step-4", None, False),
       ])
       def test_set_breakpoint(self, step_id, condition, enabled):
           debugger = WorkflowDebugger(db_session)
           breakpoint = debugger.set_breakpoint(step_id, condition, enabled)
           assert breakpoint.step_id == step_id
           assert breakpoint.enabled == enabled
       ```

    3. Breakpoint CRUD tests:
       - test_set_breakpoint_with_condition
       - test_clear_breakpoint
       - test_clear_all_breakpoints
       - test_list_breakpoints
       - test_enable_breakpoint
       - test_disable_breakpoint

    4. Breakpoint hit tests:
       - test_check_breakpoint_hit
       - test_evaluate_breakpoint_condition
       - test_increment_hit_count
       - test_get_breakpoint_hits

    5. Variable inspection tests:
       - test_capture_variable
       - test_inspect_variable
       - test_get_all_variables
       - test_compare_variable_values

    Target 75% coverage: ~132 covered statements.
  </action>
  <verify>pytest backend/tests/core/workflow/test_workflow_debugger_coverage.py::TestWorkflowDebuggerBreakpoints -v --cov=core/workflow_debugger --cov-report=term-missing</verify>
  <done>Breakpoint management achieves 75%+ coverage with ~20 tests passing</done>
</task>

<task type="auto">
  <name>Create coverage tests for execution tracing</name>
  <files>backend/tests/core/workflow/test_workflow_debugger_coverage.py</files>
  <action>
    Add comprehensive tests for execution tracing (cover lines 175-350):

    1. Parametrized trace type tests:
       ```python
       @pytest.mark.parametrize("trace_type,message,metadata", [
           ("info", "Step started", {"step": "step-1"}),
           ("debug", "Variable value", {"var": "x", "value": 10}),
           ("error", "Step failed", {"error": "ValueError"}),
           ("warning", "Slow execution", {"duration_ms": 5000}),
       ])
       def test_log_execution_trace(self, trace_type, message, metadata):
           debugger = WorkflowDebugger(db_session)
           trace = debugger.log_trace(trace_type, message, metadata)
           assert trace.trace_type == trace_type
       ```

    2. Trace logging tests:
       - test_log_info_trace
       - test_log_debug_trace
       - test_log_error_trace
       - test_log_warning_trace

    3. Trace retrieval tests:
       - test_get_traces_for_execution
       - test_get_traces_by_type
       - test_get_traces_by_step
       - test_get_traces_in_time_range

    4. Trace filtering tests:
       - test_filter_traces_by_level
       - test_search_trace_messages
       - test_aggregate_trace_counts
       - test_generate_trace_summary

    5. Trace analysis tests:
       - test_analyze_execution_flow
       - test_detect_loops_in_trace
       - test_find_long_running_steps
       - test_identify_error_patterns

    Target 75% coverage: ~132 covered statements.
  </action>
  <verify>pytest backend/tests/core/workflow/test_workflow_debugger_coverage.py::TestWorkflowDebuggerTraces -v --cov=core/workflow_debugger --cov-report=term-missing</verify>
  <done>Execution tracing achieves 75%+ coverage with ~20 tests passing</done>
</task>

<task type="auto">
  <name>Create coverage tests for debug sessions and replay</name>
  <files>backend/tests/core/workflow/test_workflow_debugger_coverage.py</files>
  <action>
    Add comprehensive tests for debug sessions (cover lines 350-527):

    1. Parametrized session type tests:
       ```python
       @pytest.mark.parametrize("session_type,initial_state", [
           ("interactive", "paused"),
           ("automated", "running"),
           ("replay", "running"),
       ])
       def test_create_debug_session(self, session_type, initial_state):
           debugger = WorkflowDebugger(db_session)
           session = debugger.create_session(session_type, initial_state)
           assert session.session_type == session_type
       ```

    2. Session lifecycle tests:
       - test_create_session
       - test_start_session
       - test_pause_session
       - test_resume_session
       - test_end_session
       - test_abandon_session

    3. Session state tests:
       - test_get_session_state
       - test_get_current_step
       - test_get_session_variables
       - test_update_session_context

    4. Step-by-step execution tests:
       - test_step_into
       - test.step_over
       - test_step_out
       - test_continue_execution
       - test_skip_to_step

    5. Replay functionality tests:
       - test_start_replay_session
       - test_replay_to_breakpoint
       - test_replay_with_modifications
       - test_compare_executions

    Target 75% coverage: ~131 covered statements.
  </action>
  <verify>pytest backend/tests/core/workflow/test_workflow_debugger_coverage.py::TestWorkflowDebuggerSessions -v --cov=core/workflow_debugger --cov-report=term-missing</verify>
  <done>Debug sessions achieve 75%+ coverage with ~15 tests passing</done>
</task>

</tasks>

<verification>
1. Run all workflow debugger tests: pytest backend/tests/core/workflow/test_workflow_debugger_coverage.py -v
2. Check coverage: pytest --cov=core/workflow_debugger --cov-report=term-missing
3. Verify workflow_debugger.py achieves 75%+ coverage (covered_statements / total_statements >= 0.75)
4. Verify Plan 190-01 models are importable
5. Check test count: ~55 tests total (20+20+15)
</verification>

<success_criteria>
1. workflow_debugger.py achieves 75%+ coverage (395/527 stmts)
2. Coverage gain: +395 statements = +0.84% overall (395/47,106)
3. All tests use parametrized patterns proven in Phase 189
4. Breakpoint management covers all operations
5. Execution tracing covers all trace types
6. Debug sessions cover interactive, automated, and replay modes
7. Import blockers from Plan 190-01 are resolved
8. Branch coverage measured with --cov-branch flag
</success_criteria>

<output>
After completion, create `.planning/phases/190-coverage-push-60-70/190-13-SUMMARY.md` with:
- Coverage achieved for workflow_debugger.py (statement counts and percentages)
- Total tests created and pass rate
- Coverage impact on overall percentage
- Breakpoint operations tested
- Trace types tested
- Session types tested
- Import blocker resolution confirmation
- Deviations from planned coverage targets
</output>
