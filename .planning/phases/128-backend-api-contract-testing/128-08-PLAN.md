---
phase: 128-backend-api-contract-testing
plan: 08
type: execute
wave: 3
depends_on: [128-06, 128-07]
files_modified: [.github/workflows/contract-tests.yml, backend/docs/API_CONTRACT_TESTING.md]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Contract violations block merge with specific failure details"
    - "CI fails on contract violations (continue-on-error: false)"
    - "Tests rely on Schemathesis validation, not loose assertions"
    - "Documentation reflects proper Schemathesis usage patterns"
  artifacts:
    - path: .github/workflows/contract-tests.yml
      provides: CI workflow that fails on contract violations
      contains: "continue-on-error: false"
    - path: backend/docs/API_CONTRACT_TESTING.md
      provides: Documentation for Schemathesis contract testing
      contains: "@schema.parametrize"
  key_links:
    - from: .github/workflows/contract-tests.yml
      to: backend/tests/contract/
      via: pytest contract test execution
      pattern: "pytest tests/contract/"
    - from: .github/workflows/contract-tests.yml
      to: contract test failure blocking merge
      via: continue-on-error: false configuration
      pattern: "continue-on-error:\\s*false"
---

<objective>
Update CI workflow and documentation to enforce strict schema validation via Schemathesis.

Purpose: Fix Gap 3 where loose assertions hide contract violations, and document proper usage.

Gap Source: 128-VERIFICATION.md - "CI workflow has continue-on-error: false BUT tests use loose assertions (accepting 401, 403, 404, 422, 500)"

Output: Updated CI workflow and documentation reflecting proper Schemathesis testing patterns.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

@.planning/phases/128-backend-api-contract-testing/128-VERIFICATION.md
@.planning/phases/128-backend-api-contract-testing/128-04-SUMMARY.md
@.planning/phases/128-backend-api-contract-testing/128-06-PLAN.md
@.github/workflows/contract-tests.yml
@backend/docs/API_CONTRACT_TESTING.md
</execution_context>

<context>
## Gap Context

From 128-VERIFICATION.md:

**Gap 3 (WARNING):** Loose assertions hide violations

**Evidence:**
- Tests accept too many status codes: `[200, 400, 401, 403, 404, 422, 500]`
- Schemathesis `case.call_and_validate()` would automatically validate response schemas
- CI has `continue-on-error: false` but tests bypass contract validation

**Issue:** The CI workflow configuration is correct (`continue-on-error: false`), but the tests themselves don't enforce schema validation because:
1. They use manual TestClient calls instead of `case.call_and_validate()`
2. Loose status code assertions pass even when response schema is wrong

**Resolution:** After plan 128-06 fixes the tests to use `@schema.parametrize()` and `case.call_and_validate()`, this plan ensures:
1. CI workflow properly enforces contract violations as blocking
2. Breaking change detection failures block merge (not informational)
3. Documentation reflects the correct patterns

## CI Workflow Status

From contract-tests.yml line 59:
```yaml
continue-on-error: false  # Already correct!
```

The CI configuration is already correct - this plan focuses on:
1. Verifying breaking change detection blocks merge (remove `--allow-breaking` if present)
2. Updating documentation to reflect proper Schemathesis usage
3. Adding comments explaining contract enforcement
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and enhance CI workflow contract enforcement</name>
  <files>.github/workflows/contract-tests.yml</files>
  <action>
Verify `.github/workflows/contract-tests.yml` properly enforces contract violations:

**Check line 114-117 (breaking-change-detection step):**

Current code:
```yaml
python tests/scripts/detect_breaking_changes.py \
  --base backend/openapi.json \
  --current backend/openapi_current.json \
  --allow-breaking || echo "Breaking changes detected"
```

**Issue:** The `--allow-breaking` flag causes the script to exit 0 even when breaking changes are found, AND the `|| echo` swallows failures.

**Replace with:**
```yaml
python tests/scripts/detect_breaking_changes.py \
  --base backend/openapi.json \
  --current backend/openapi_current.json
```

**Remove the `|| echo "Breaking changes detected"` fallback** - the script itself handles exit codes:
- Exit 0: No breaking changes
- Exit 1: Breaking changes detected (CI fails)

**Also add a comment before the step to clarify enforcement:**
```yaml
      - name: Detect breaking changes
        id: diff
        # This step will FAIL the build if breaking changes are detected
        # To update baseline after intentional changes, use: --update-baseline
        working-directory: ./backend
        env:
          BASE_REF: $'\''${{ github.base_ref || '\'''\''main'\'' '\''}}'\''
        run: |
          # ... (existing git fetch code)

          if [ -f backend/openapi.json ]; then
            echo "Comparing against baseline from $BASE_REF"
            # NOTE: Breaking changes will FAIL the build
            # For approved breaking changes, update baseline manually
            python tests/scripts/detect_breaking_changes.py \
              --base backend/openapi.json \
              --current backend/openapi_current.json

            # Capture exit code for reporting
            EXIT_CODE=$?
            if [ $EXIT_CODE -ne 0 ]; then
              echo "has_breaking_changes=true" >> $GITHUB_OUTPUT
            else
              echo "has_breaking_changes=false" >> $GITHUB_OUTPUT
            fi
          else
            echo "No baseline found - skipping breaking change check"
            echo "has_breaking_changes=false" >> $GITHUB_OUTPUT
          fi
```

DO NOT use `--allow-breaking` in CI - this defeats the purpose of breaking change detection.
  </action>
  <verify>
# Verify the workflow is valid YAML:
cat .github/workflows/contract-tests.yml | grep -A5 "Detect breaking changes"

# Verify no --allow-breaking flag in CI:
grep -n "allow-breaking" .github/workflows/contract-tests.yml
# Should return no results (or only in comments)

# Verify continue-on-error: false is present:
grep "continue-on-error: false" .github/workflows/contract-tests.yml
  </verify>
  <done>CI workflow fails build on breaking changes (no --allow-breaking flag)
</done>
</task>

<task type="auto">
  <name>Task 2: Update API_CONTRACT_TESTING.md with Schemathesis patterns</name>
  <files>backend/docs/API_CONTRACT_TESTING.md</files>
  <action>
Update `backend/docs/API_CONTRACT_TESTING.md` to reflect proper Schemathesis usage patterns:

**Find and update the "Test Structure" section** (around lines 50-80):

**Replace any manual TestClient examples with:**
```python
"""Contract tests using Schemathesis property-based testing."""
import pytest
from conftest import schema
from hypothesis import settings

class TestHealthEndpoints:
    """Contract tests for health check endpoints."""

    @schema.parametrize(endpoint="/health")
    @settings(max_examples=10, deadline=None)
    def test_health_endpoint_contracts(self, case):
        """Test /health endpoint conforms to OpenAPI spec."""
        response = case.call_and_validate()
        # Schemathesis automatically validates:
        # - Response status code matches OpenAPI spec
        # - Response body matches schema definition
        # - Required headers are present
        # - Content-Type matches spec
        # We only add business logic assertions:
        assert response.status_code in [200, 503]
```

**Add a "Common Mistakes" section:**
```markdown
## Common Mistakes to Avoid

### ❌ Manual TestClient Calls
```python
# WRONG: Manual HTTP calls bypass Schemathesis validation
def test_health_endpoint(self):
    with TestClient(app) as client:
        response = client.get("/health")
        assert response.status_code in [200, 400, 401, 403, 404]  # Too loose!
```

### ✅ Schemathesis Property-Based Testing
```python
# CORRECT: Schemathesis validates schema automatically
@schema.parametrize(endpoint="/health")
@settings(max_examples=10, deadline=None)
def test_health_endpoint_contracts(self, case):
    response = case.call_and_validate()  # Validates schema!
    assert response.status_code in [200, 503]  # Business logic only
```

**Key differences:**
1. Use `@schema.parametrize()` decorator - generates diverse test cases
2. Use `case.call_and_validate()` - automatic schema validation
3. Remove loose status code assertions - Schemathesis handles it
4. Hypothesis generates edge cases you wouldn't think of
```

**Update the "Running Contract Tests" section:**
```bash
# Run all contract tests
pytest tests/contract/ -v -m contract

# Run specific test file
pytest tests/contract/test_core_api.py -v

# Run with more examples (slower but more thorough)
pytest tests/contract/ -v -m contract --hypothesis-max-examples=100
```

DO NOT document manual TestClient patterns - they are anti-patterns for contract testing.
  </action>
  <verify>
# Verify documentation contains proper Schemathesis examples:
grep -c "@schema.parametrize" backend/docs/API_CONTRACT_TESTING.md
# Should be > 0

# Verify documentation warns against manual TestClient:
grep -c "Manual TestClient Calls" backend/docs/API_CONTRACT_TESTING.md
# Should be > 0
  </verify>
  <done>API_CONTRACT_TESTING.md documents proper Schemathesis usage patterns
</done>
</task>

<task type="auto">
  <name>Task 3: Add pre-commit hook recommendation to documentation</name>
  <files>backend/docs/API_CONTRACT_TESTING.md</files>
  <action>
Add a "Pre-commit Hook" section to `backend/docs/API_CONTRACT_TESTING.md`:

**Add at the end of the document:**
```markdown
## Pre-commit Hook (Recommended)

To catch breaking changes before pushing, add a pre-commit hook:

Create `.git/hooks/pre-commit`:
```bash
#!/bin/bash
# Run contract tests before committing
cd backend
pytest tests/contract/ -v -m contract --maxfail=5
EXIT_CODE=$?

if [ $EXIT_CODE -ne 0 ]; then
    echo ""
    echo "❌ Contract tests failed. Commit aborted."
    echo "   Fix the issues or skip with: git commit --no-verify"
    exit 1
fi

# Detect breaking changes
python3 tests/scripts/detect_breaking_changes.py
if [ $? -ne 0 ]; then
    echo ""
    echo "❌ Breaking changes detected. Commit aborted."
    echo "   To update baseline: python3 tests/scripts/generate_openapi_spec.py -o openapi.json"
    echo "   To skip: git commit --no-verify"
    exit 1
fi
```

Make it executable:
```bash
chmod +x .git/hooks/pre-commit
```

This ensures contract violations never leave your development machine.
```

DO NOT make this mandatory - some developers may prefer CI-only enforcement.
  </action>
  <verify>
# Verify documentation mentions pre-commit hooks:
grep -c "pre-commit" backend/docs/API_CONTRACT_TESTING.md
# Should be > 0
  </verify>
  <done>Documentation includes pre-commit hook recommendation for local enforcement
</done>
</task>

</tasks>

<verification>
After completing this plan:

1. Verify CI workflow enforces breaking changes:
   ```bash
   grep -A10 "Detect breaking changes" .github/workflows/contract-tests.yml | grep -E "(allow-breaking|\\|\\| echo)"
   # Should return no results (no bypass mechanisms)
   ```

2. Verify contract tests fail build:
   ```bash
   grep "continue-on-error" .github/workflows/contract-tests.yml
   # Should show: continue-on-error: false
   ```

3. Verify documentation patterns:
   ```bash
   grep "@schema.parametrize" backend/docs/API_CONTRACT_TESTING.md
   # Should show Schemathesis examples
   ```

4. Verify pre-commit hook documented:
   ```bash
   grep "pre-commit" backend/docs/API_CONTRACT_TESTING.md
   # Should show hook setup instructions
   ```
</verification>

<success_criteria>
1. CI workflow fails build on breaking changes (no --allow-breaking flag)
2. CI workflow fails build on contract test violations (continue-on-error: false)
3. Documentation shows @schema.parametrize() pattern as the standard
4. Documentation warns against manual TestClient anti-pattern
5. Documentation includes pre-commit hook for local enforcement
</success_criteria>

<output>
After completion, create `.planning/phases/128-backend-api-contract-testing/128-08-SUMMARY.md`
</output>
