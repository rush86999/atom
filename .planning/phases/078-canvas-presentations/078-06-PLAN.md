---
phase: 078-canvas-presentations
plan: 06
type: execute
wave: 3
depends_on: [078-01, 078-02, 078-03]
files_modified:
  - backend/tests/e2e_ui/tests/test_canvas_dynamic_content.py
autonomous: true

must_haves:
  truths:
    - "Dynamic canvas content loads with auto-waiting"
    - "WebSocket updates trigger canvas refresh"
    - "Async data loads without race conditions"
    - "Canvas updates preserve form data when possible"
    - "Loading indicators show during async operations"
    - "Error states display correctly when data fails to load"
  artifacts:
    - path: "backend/tests/e2e_ui/tests/test_canvas_dynamic_content.py"
      provides: "E2E tests for dynamic content loading and auto-waiting"
      min_lines: 120
      contains: "test_canvas_websocket_update|test_async_data_loading"
  key_links:
    - from: "test_canvas_dynamic_content"
      to: "WebSocket canvas:update messages"
      via: "page.evaluate() WebSocket simulation"
      pattern: "canvas.*update|websocket.*canvas"
    - from: "Auto-waiting strategies"
      to: "Playwright wait_for_load_state and wait_for_selector"
      via: "Explicit waits in page object methods"
      pattern: "wait_for.*|wait_for_load_state"
---

<objective>
Create E2E tests for dynamic canvas content loading with auto-waiting.

Purpose: Verify dynamic content loads correctly, WebSocket updates work, and auto-waiting prevents flaky tests
Output: Dynamic content tests covering WebSocket updates, async loading, and error states
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/078-canvas-presentations/078-01-SUMMARY.md
@.planning/phases/078-canvas-presentations/078-02-SUMMARY.md
@.planning/phases/078-canvas-presentations/078-03-SUMMARY.md
@backend/tools/canvas_tool.py
@backend/api/canvas_routes.py
@frontend-nextjs/components/chat/canvas-host.tsx
@backend/tests/e2e_ui/pages/page_objects.py

# Dynamic Content Context
- Canvas updates via WebSocket: type="canvas:update", action="update"
- Async data sources: API calls for chart data, form options, etc.
- Auto-waiting strategies: wait_for_selector(), wait_for_load_state("networkidle")
- Loading states: Show spinner/skeleton during data fetch
- Error states: Display error message when data fails to load
- Canvas update function: update_canvas() for partial updates without re-presentation
- WebSocket manager: core.websockets.manager for broadcasting updates
</context>

<tasks>

<task type="auto">
  <name>Create dynamic content loading E2E tests</name>
  <files>backend/tests/e2e_ui/tests/test_canvas_dynamic_content.py</files>
  <action>
    Create test_canvas_dynamic_content.py with:

    1. Helper functions:
       - simulate_websocket_update(page, canvas_id: str, updates: dict) -> None
       - simulate_async_data_load(page, delay_ms: int, data: dict) -> None
       - wait_for_canvas_update(page, canvas_id: str, timeout: int = 5000) -> bool
       - trigger_async_canvas_with_loading(page, component_type, data_url: str) -> str

    2. WebSocket update tests:
       - test_canvas_websocket_update():
         - Trigger initial canvas
         - Get canvas_id
         - Simulate WebSocket update with new data
         - Verify canvas content updates
         - Verify title changes if updated

       - test_canvas_update_action_vs_present():
         - Trigger canvas with action="present"
         - Verify canvas appears
         - Send update with action="update"
         - Verify canvas refreshes without closing
         - Verify existing canvas_id preserved

       - test_multiple_canvas_updates():
         - Trigger canvas
         - Send 3 rapid updates
         - Verify final state reflects last update
         - Verify no flickering or intermediate states visible

    3. Async data loading tests:
       - test_async_chart_data_loading():
         - Trigger canvas that loads data from API
         - Wait for loading indicator to appear
         - Wait for loading indicator to disappear
         - Verify chart renders with data
         - Verify wait_for_load_state("networkidle") succeeds

       - test_async_form_options_loading():
         - Trigger form with async select options
         - Verify dropdown disabled during load
         - Verify loading indicator visible
         - Wait for options to load
         - Verify dropdown enabled with options

       - test_auto_waiting_prevents_flaky_tests():
         - Run 5 iterations of async canvas load
         - Each iteration: trigger, wait, verify
         - Verify all iterations pass (no flaky timeouts)
         - Use consistent timeout values

    4. Loading indicator tests:
       - test_loading_indicator_displays():
         - Trigger async canvas
         - Immediately check for loading state
         - Verify spinner or skeleton visible
         - Verify user sees loading feedback

       - test_loading_indicator_hides_after_load():
         - Wait for data to load
         - Verify loading indicator disappears
         - Verify canvas content visible

    5. Error state tests:
       - test_async_load_error_display():
         - Trigger canvas with failing data source
         - Wait for timeout
         - Verify error message displays
         - Verify canvas shows error state, not blank

       - test_error_state_allows_retry():
         - Trigger canvas that fails to load
         - Verify error message visible
         - Trigger retry (simulate)
         - Verify success if retry succeeds

    6. Form data preservation tests:
       - test_form_data_preserved_during_update():
         - Trigger form canvas
         - Fill form fields
         - Send update that doesn't affect fields
         - Verify form data still present
         - Verify values unchanged

       - test_form_data_cleared_on_schema_change():
         - Trigger form canvas
         - Fill form fields
         - Send update with new schema
         - Verify form data reset or preserved appropriately
         - Verify validation state recalculated

    7. Race condition prevention tests:
       - test_rapid_canvas_updates_no_race():
         - Send 10 rapid updates
         - Verify final state is consistent
         - Verify no JavaScript errors in console
         - Verify no partial states visible

       - test_concurrent_canvas_operations():
         - Trigger two different canvases
         - Update both simultaneously
         - Verify each updates independently
         - Verify no cross-contamination

    Use page.wait_for_load_state() for auto-waiting
    Use page.wait_for_selector() before asserting element state
    Include timeout handling in all wait operations
    Use unique IDs to prevent cross-test interference
  </action>
  <verify>
    # Check test file exists with dynamic content tests
    grep -c "def test_.*dynamic\|def test_.*async\|def test_.*websocket" backend/tests/e2e_ui/tests/test_canvas_dynamic_content.py
    grep -c "wait_for\|loading\|update" backend/tests/e2e_ui/tests/test_canvas_dynamic_content.py
  </verify>
  <done>
    test_canvas_dynamic_content.py exists with 10+ test cases covering dynamic content
  </done>
</task>

</tasks>

<verification>
1. WebSocket update tests verify action="update" behavior
2. Async loading tests use proper wait strategies
3. Loading indicator tests verify user feedback
4. Error state tests handle failures gracefully
5. Form preservation tests verify data integrity
6. Race condition tests verify stability under rapid updates
7. All tests use auto-waiting to prevent flakiness
</verification>

<success_criteria>
- test_canvas_dynamic_content.py with 10+ test cases
- Tests cover: WebSocket (3), async loading (3), loading indicators (2), error states (2), form preservation (2), race conditions (2)
- All tests use wait_for_* methods for stability
- Tests handle both success and failure scenarios
</success_criteria>

<output>
After completion, create .planning/phases/078-canvas-presentations/078-06-SUMMARY.md
</output>
