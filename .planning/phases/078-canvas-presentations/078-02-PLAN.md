---
phase: 078-canvas-presentations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/e2e_ui/pages/page_objects.py
  - backend/tests/e2e_ui/tests/test_canvas_charts.py
autonomous: true

must_haves:
  truths:
    - "Line chart renders with correct data points"
    - "Bar chart displays categories and values correctly"
    - "Pie chart shows segments with proper labels"
    - "Chart tooltips display on hover"
    - "Chart axes have proper labels"
    - "Chart legend displays series information"
  artifacts:
    - path: "backend/tests/e2e_ui/pages/page_objects.py"
      provides: "CanvasChartPage Page Object with chart-specific locators and methods"
      min_lines: 100
      contains: "class CanvasChartPage"
    - path: "backend/tests/e2e_ui/tests/test_canvas_charts.py"
      provides: "E2E tests for canvas chart rendering"
      min_lines: 150
      contains: "test_line_chart_renders|test_bar_chart_renders|test_pie_chart_renders"
  key_links:
    - from: "CanvasChartPage.get_chart_data()"
      to: "Recharts chart components"
      via: "data-testid on SVG elements"
      pattern: "recharts.*wrapper|chart.*container"
    - from: "test_canvas_chart_rendering"
      to: "canvas_tool.py present_chart function"
      via: "WebSocket canvas:update message"
      pattern: "chart_type.*line_chart|present_chart"
---

<objective>
Create Chart Page Objects and E2E tests for canvas chart rendering.

Purpose: Verify all chart types (line, bar, pie) render correctly with proper data, axes, legends, and tooltips
Output: CanvasChartPage Page Object and chart rendering tests
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/078-canvas-presentations/078-01-SUMMARY.md
@backend/tools/canvas_tool.py
@frontend-nextjs/components/canvas/LineChart.tsx
@frontend-nextjs/components/canvas/BarChart.tsx
@frontend-nextjs/components/canvas/PieChart.tsx
@backend/tests/e2e_ui/pages/page_objects.py

# Chart Component Context
- Line Chart: LineChartCanvas with timestamp/value data, X/Y axes, tooltip, legend
- Bar Chart: BarChartCanvas with name/value data, category X axis, value Y axis
- Pie Chart: PieChartCanvas with name/value segments, labels on slices, colors array
- All charts use Recharts library with ResponsiveContainer (200px height)
- Charts register with window.atom.canvas.getState() API
- Chart data format: [{name: string, value: number, label?: string}]
</context>

<tasks>

<task type="auto">
  <name>Create CanvasChartPage Page Object with chart locators</name>
  <files>backend/tests/e2e_ui/pages/page_objects.py</files>
  <action>
    Add CanvasChartPage class to page_objects.py with:

    1. Locators (Recharts-specific CSS selectors):
       - chart_container - Recharts ResponsiveContainer wrapper
       - line_chart_svg - SVG element for line charts
       - bar_chart_svg - SVG element for bar charts
       - pie_chart_svg - SVG element for pie charts
       - chart_title - Chart title element (h4 tag)
       - chart_tooltip - Tooltip element (visible on hover)
       - chart_legend - Legend container
       - chart_x_axis - XAxis element with labels
       - chart_y_axis - YAxis element with labels
       - data_points - Individual data point elements (dots, bars, slices)
       - grid_lines - CartesianGrid lines

    2. Methods:
       - is_loaded() -> bool - Check if any chart is visible
       - get_chart_type() -> str - Detect chart type (line, bar, pie)
       - get_title() -> str - Get chart title text
       - get_data_point_count() -> int - Count visible data points
       - get_x_axis_label() -> str - Get X axis label text
       - get_y_axis_label() -> str - Get Y axis label text
       - hover_data_point(index: int) -> None - Hover over data point to show tooltip
       - get_tooltip_text() -> str - Get tooltip content if visible
       - has_legend() -> bool - Check if legend is displayed
       - get_legend_items() -> list[str] - Get legend item labels
       - get_chart_colors() -> list[str] - Extract chart colors from SVG

    3. Chart-specific helper methods:
       - verify_line_chart_data(expected_data: list) -> bool
       - verify_bar_chart_data(expected_data: list) -> bool
       - verify_pie_chart_data(expected_data: list) -> bool

    Follow BasePage pattern
    Use SVG-specific selectors for Recharts components
    Include docstrings with examples
  </action>
  <verify>
    # Check file has CanvasChartPage class
    grep -c "class CanvasChartPage" backend/tests/e2e_ui/pages/page_objects.py
    grep -c "chart_svg\|chart_tooltip\|chart_legend" backend/tests/e2e_ui/pages/page_objects.py
  </verify>
  <done>
    CanvasChartPage class exists with 100+ lines including chart-specific methods
  </done>
</task>

<task type="auto">
  <name>Create chart rendering E2E tests</name>
  <files>backend/tests/e2e_ui/tests/test_canvas_charts.py</files>
  <action>
    Create test_canvas_charts.py with:

    1. Helper functions:
       - create_test_chart_data(chart_type, point_count) -> list[dict]
       - trigger_chart_canvas(page, chart_type, data, title) -> str (returns canvas_id)
       - wait_for_chart_render(page, timeout: int = 3000) -> None

    2. Line chart tests:
       - test_line_chart_renders():
         - Trigger line chart with 5 data points
         - Verify line_chart_svg is visible
         - Verify chart type detection returns "line"

       - test_line_chart_data_points():
         - Create chart with known data values
         - Verify data point count matches
         - Verify timestamps on X axis

       - test_line_chart_tooltip():
         - Hover over data point
         - Verify tooltip appears with correct value
         - Verify tooltip formatting

    3. Bar chart tests:
       - test_bar_chart_renders():
         - Trigger bar chart with category data
         - Verify bar_chart_svg is visible
         - Verify bars are rendered

       - test_bar_chart_categories():
         - Create chart with named categories
         - Verify X axis shows category names
         - Verify Y axis shows numeric values

       - test_bar_chart_colors():
         - Verify bars have fill color
         - Verify color matches expected value

    4. Pie chart tests:
       - test_pie_chart_renders():
         - Trigger pie chart with segment data
         - Verify pie_chart_svg is visible
         - Verify all segments present

       - test_pie_chart_labels():
         - Verify labels appear on/near segments
         - Verify labels show "name: value" format
         - Verify COLORS array cycles correctly

       - test_pie_chart_legend():
         - Verify legend displays
         - Verify legend items match data names

    5. Common chart tests:
       - test_chart_title_displays():
         - Test all chart types with titles
         - Verify title appears above chart

       - test_chart_responsive():
         - Verify chart uses ResponsiveContainer
         - Verify chart fits container width

    Use unique IDs for test data to prevent collisions
    Use page.evaluate() to simulate WebSocket canvas:update messages
    Include data-testid attributes if charts don't have them (use class selectors as fallback)
  </action>
  <verify>
    # Check test file exists with chart tests
    grep -c "def test_.*chart" backend/tests/e2e_ui/tests/test_canvas_charts.py
    grep -c "line_chart\|bar_chart\|pie_chart" backend/tests/e2e_ui/tests/test_canvas_charts.py
  </verify>
  <done>
    test_canvas_charts.py exists with 10+ test cases covering all chart types
  </done>
</task>

</tasks>

<verification>
1. CanvasChartPage class handles all three chart types
2. SVG selectors work correctly for Recharts components
3. Tests verify data rendering, not just visibility
4. Tooltip tests use hover() action correctly
5. Tests use unique data to prevent cross-test pollution
</verification>

<success_criteria>
- CanvasChartPage Page Object with 100+ lines
- test_canvas_charts.py with 10+ test cases
- Tests cover: line chart (3), bar chart (3), pie chart (3), common (1+)
- All tests verify actual data values, not just DOM presence
</success_criteria>

<output>
After completion, create .planning/phases/078-canvas-presentations/078-02-SUMMARY.md
</output>
