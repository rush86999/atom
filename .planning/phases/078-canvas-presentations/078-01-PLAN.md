---
phase: 078-canvas-presentations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/e2e_ui/pages/page_objects.py
autonomous: true

must_haves:
  truths:
    - "User can create new canvas presentation from agent chat interface"
    - "Canvas host component renders when presentation is triggered"
    - "Canvas close button hides the canvas presentation"
    - "Canvas title displays correctly in header"
  artifacts:
    - path: "backend/tests/e2e_ui/pages/page_objects.py"
      provides: "CanvasHostPage Page Object with canvas locators and interaction methods"
      min_lines: 80
      contains: "class CanvasHostPage"
    - path: "backend/tests/e2e_ui/tests/test_canvas_creation.py"
      provides: "E2E tests for canvas creation workflow"
      min_lines: 100
      contains: "test_canvas_presented_from_chat"
  key_links:
    - from: "CanvasHostPage.is_loaded()"
      to: "canvas host container div"
      via: "absolute positioned canvas div"
      pattern: "canvas.*host|canvas.*container"
    - from: "test_canvas_presented_from_chat"
      to: "WebSocket canvas:update event"
      via: "canvas_tool.py present_chart function"
      pattern: "canvas.*update|present_chart"
---

<objective>
Create Canvas Page Objects and E2E tests for canvas presentation creation.

Purpose: Enable testing of canvas creation workflow from agent chat and direct API calls
Output: CanvasHostPage Page Object and canvas creation tests
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/077-agent-chat-streaming/077-01-SUMMARY.md
@backend/tools/canvas_tool.py
@backend/api/canvas_routes.py
@frontend-nextjs/components/chat/canvas-host.tsx
@backend/tests/e2e_ui/pages/page_objects.py

# Canvas Creation Context
- Canvas presentations triggered via WebSocket message: type="canvas:update", action="present"
- Backend function: present_chart(), present_markdown(), present_form(), present_status_panel()
- Frontend component: CanvasHost in frontend-nextjs/components/chat/canvas-host.tsx
- Canvas components: markdown, code, chart, form, status_panel, eval, snapshot, browser_view
- Canvas state includes: id, visible, component, title, data, version
</context>

<tasks>

<task type="auto">
  <name>Create CanvasHostPage Page Object class</name>
  <files>backend/tests/e2e_ui/pages/page_objects.py</files>
  <action>
    Add CanvasHostPage class to page_objects.py with:

    1. Locators (CSS selectors - canvas uses absolute positioning):
       - canvas_host - Main canvas container (absolute positioned div with z-50)
       - canvas_close_button - X button to close canvas
       - canvas_title - Canvas title display in header
       - canvas_component_badge - Component type badge (e.g., "markdown", "form")
       - canvas_version - Version number display
       - canvas_content - Canvas content area (Monaco editor, charts, etc.)
       - save_button - Save changes button (for editable canvases)
       - history_button - Version history button
       - preview_mode_button - Preview/Edit mode toggle (for markdown)

    2. Methods:
       - is_loaded() -> bool - Check if canvas host is visible
       - get_title() -> str - Get canvas title text
       - get_component_type() -> str - Get component type badge text
       - get_version() -> str - Get canvas version number
       - close_canvas() -> None - Click close button to hide canvas
       - is_visible() -> bool - Check if canvas is currently displayed
       - wait_for_canvas_visible(timeout: int = 5000) -> None - Wait for canvas to appear
       - wait_for_canvas_hidden(timeout: int = 5000) -> None - Wait for canvas to disappear

    Follow BasePage pattern from existing page_objects.py
    Canvas is absolute positioned, so use page.locator() not get_by_test_id()
    Include docstrings with Google-style Args/Returns
  </action>
  <verify>
    # Check file has CanvasHostPage class
    grep -c "class CanvasHostPage" backend/tests/e2e_ui/pages/page_objects.py
    grep -c "canvas_host\|canvas_close_button\|canvas_title" backend/tests/e2e_ui/pages/page_objects.py
  </verify>
  <done>
    CanvasHostPage class exists with 80+ lines including all required locators and methods
  </done>
</task>

<task type="auto">
  <name>Create canvas creation E2E tests</name>
  <files>backend/tests/e2e_ui/tests/test_canvas_creation.py</files>
  <action>
    Create test_canvas_creation.py with:

    1. Helper functions:
       - create_test_user_with_canvas(db_session, email) -> User
       - create_authenticated_page_for_canvas(browser, user, token) -> Page
       - trigger_canvas_presentation(page, component_type, data) -> None (simulate WebSocket)

    2. Test cases:
       - test_canvas_presented_from_chat():
         - Navigate to chat, send message that triggers canvas
         - Verify CanvasHostPage.is_loaded() returns True
         - Verify canvas title and component badge display

       - test_canvas_close_button():
         - Trigger canvas presentation
         - Click close button
         - Verify canvas is hidden (is_visible() == False)

       - test_canvas_title_displays():
         - Trigger canvas with specific title
         - Verify title matches expected value
         - Verify title truncates if too long (max-w-[250px])

       - test_canvas_component_badge():
         - Trigger different canvas types (markdown, form, chart)
         - Verify component type badge shows correct type
         - Verify badge styling matches component type

       - test_canvas_version_display():
         - Trigger canvas with version data
         - Verify version number displays in header
         - Verify format "v{number}"

       - test_canvas_save_button_visibility():
         - Trigger editable canvas (markdown, code, sheet)
         - Verify save button is visible
         - Trigger read-only canvas (snapshot, browser_view)
         - Verify save button is not visible

    Use authenticated_page fixture from auth_fixtures.py
    Use page.evaluate() to simulate WebSocket canvas:update messages
    Include unique IDs in test data to prevent collisions
  </action>
  <verify>
    # Check test file exists with tests
    grep -c "def test_canvas" backend/tests/e2e_ui/tests/test_canvas_creation.py
    grep -c "CanvasHostPage" backend/tests/e2e_ui/tests/test_canvas_creation.py
  </verify>
  <done>
    test_canvas_creation.py exists with 6+ test cases covering CANVAS-01 requirements
  </done>
</task>

</tasks>

<verification>
1. CanvasHostPage class added to page_objects.py
2. All locators handle absolute positioned canvas correctly
3. Test file includes helper functions for user/auth setup
4. Tests simulate WebSocket messages via page.evaluate()
5. All tests use unique IDs to prevent data collisions
</verification>

<success_criteria>
- CanvasHostPage Page Object with 80+ lines
- test_canvas_creation.py with 6+ test cases
- Tests cover: creation, close, title, badge, version, save button
- Tests use page.evaluate() to simulate WebSocket triggers
</success_criteria>

<output>
After completion, create .planning/phases/078-canvas-presentations/078-01-SUMMARY.md
</output>
