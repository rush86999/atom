---
phase: 078-canvas-presentations
plan: 04
type: execute
wave: 2
depends_on: [078-01]
files_modified:
  - backend/tests/e2e_ui/tests/test_canvas_state_api.py
autonomous: true

must_haves:
  truths:
    - "window.atom.canvas.getState(canvas_id) returns canvas state object"
    - "getState returns null for non-existent canvas_id"
    - "getAllStates() returns array of all canvas states"
    - "Canvas state includes: canvas_id, component, timestamp, data"
    - "Chart canvas state includes data_points, axes_labels, title"
    - "Form canvas state includes form_schema, form_data, validation_errors"
  artifacts:
    - path: "backend/tests/e2e_ui/tests/test_canvas_state_api.py"
      provides: "E2E tests for canvas state API (window.atom.canvas)"
      min_lines: 120
      contains: "test_get_state_returns_canvas_data|test_get_all_states"
  key_links:
    - from: "test_canvas_state_api"
      to: "window.atom.canvas global object"
      via: "page.evaluate() JavaScript execution"
      pattern: "window\.atom\.canvas\.getState|window\.atom\.canvas\.getAllStates"
    - from: "Canvas state registration"
      to: "LineChartCanvas, BarChartCanvas, PieChartCanvas, InteractiveForm"
      via: "useEffect hook in components"
      pattern: "atom\.canvas.*getState|registerState"
---

<objective>
Create E2E tests for Canvas State API (window.atom.canvas.getState).

Purpose: Verify the global canvas state API returns correct data structures for all canvas types
Output: Canvas state API tests covering getState, getAllStates, and state object structure
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/078-canvas-presentations/078-01-SUMMARY.md
@frontend-nextjs/components/canvas/LineChart.tsx
@frontend-nextjs/components/canvas/BarChart.tsx
@frontend-nextjs/components/canvas/PieChart.tsx
@frontend-nextjs/components/canvas/InteractiveForm.tsx
@frontend-nextjs/hooks/useCanvasState.ts
@backend/tests/e2e_ui/tests/test_canvas_forms.py

# Canvas State API Context
- Global API: window.atom.canvas.getState(canvasId), getAllStates(), subscribe(), subscribeAll()
- State registration happens in useEffect hooks of chart and form components
- Chart state structure: {canvas_type, canvas_id, timestamp, component, chart_type, data_points, axes_labels, title, legend}
- Form state structure: {canvas_type, canvas_id, timestamp, component, form_schema, form_data, validation_errors, submit_enabled, submitted}
- API allows AI agents to read canvas content without OCR
</context>

<tasks>

<task type="auto">
  <name>Create canvas state API E2E tests</name>
  <files>backend/tests/e2e_ui/tests/test_canvas_state_api.py</files>
  <action>
    Create test_canvas_state_api.py with:

    1. Helper functions:
       - get_canvas_state(page, canvas_id: str) -> dict (wrapper for page.evaluate())
       - get_all_canvas_states(page) -> list[dict] (wrapper for page.evaluate())
       - trigger_canvas_and_get_id(page, component_type, data) -> str
       - wait_for_state_registration(page, canvas_id: str, timeout: int = 2000) -> bool

    2. API availability tests:
       - test_canvas_api_exists():
         - Navigate to any page
         - Verify window.atom exists
         - Verify window.atom.canvas exists
         - Verify getState is a function
         - Verify getAllStates is a function
         - Verify subscribe is a function
         - Verify subscribeAll is a function

       - test_get_state_returns_null_for_invalid_id():
         - Call getState with non-existent canvas_id
         - Verify returns null or undefined

    3. Line chart state tests:
       - test_line_chart_state_structure():
         - Trigger line chart canvas
         - Get state via getState(canvas_id)
         - Verify structure has all required fields
         - Verify component == "line_chart"
         - Verify chart_type == "line"

       - test_line_chart_data_points():
         - Create line chart with known data
         - Get state and verify data_points array
         - Verify each data point has x, y, label
         - Verify data point count matches input

       - test_line_chart_axes_labels():
         - Get state for line chart
         - Verify axes_labels.x exists
         - Verify axes_labels.y exists

    4. Bar chart state tests:
       - test_bar_chart_state_structure():
         - Trigger bar chart canvas
         - Verify component == "bar_chart"
         - Verify chart_type == "bar"

       - test_bar_chart_data_points():
         - Create bar chart with category data
         - Verify data_points have correct x (category) and y (value)

    5. Pie chart state tests:
       - test_pie_chart_state_structure():
         - Trigger pie chart canvas
         - Verify component == "pie_chart"
         - Verify chart_type == "pie"

       - test_pie_chart_data_points():
         - Create pie chart with segment data
         - Verify data_points structure

    6. Form state tests:
       - test_form_state_structure():
         - Trigger form canvas
         - Verify component == "form"
         - Verify form_schema exists
         - Verify form_data exists
         - Verify validation_errors exists

       - test_form_state_updates():
         - Fill form field
         - Get state and verify form_data updated
         - Trigger validation error
         - Verify validation_errors populated

    7. Multiple canvas tests:
       - test_get_all_states_returns_multiple():
         - Trigger line chart, bar chart, form
         - Call getAllStates()
         - Verify returns array with 3+ items
         - Verify each item has canvas_id and state

       - test_get_state_filters_by_id():
         - Create multiple canvases
         - Verify getState(id) returns correct canvas
         - Verify IDs are unique

    8. Timestamp tests:
       - test_state_timestamp_is_iso_string():
         - Trigger canvas
         - Get state
         - Verify timestamp is valid ISO 8601 string
         - Verify timestamp is recent (within 10 seconds)

    Use page.evaluate() to call JavaScript API
    Use unique canvas IDs to prevent collisions
    Include assertions for all required state fields
  </action>
  <verify>
    # Check test file exists with state API tests
    grep -c "def test_.*state" backend/tests/e2e_ui/tests/test_canvas_state_api.py
    grep -c "getState\|getAllStates\|window\.atom\.canvas" backend/tests/e2e_ui/tests/test_canvas_state_api.py
  </verify>
  <done>
    test_canvas_state_api.py exists with 10+ test cases covering state API
  </done>
</task>

</tasks>

<verification>
1. Tests verify window.atom.canvas API exists
2. All canvas types tested for state structure
3. Data point verification included for charts
4. Form state updates tested correctly
5. Multiple canvas state retrieval tested
</verification>

<success_criteria>
- test_canvas_state_api.py with 10+ test cases
- Tests cover: API availability (2), line chart (3), bar chart (2), pie chart (2), form (2), multiple (1+), timestamp (1)
- All tests use page.evaluate() for JavaScript access
- Tests verify complete state structure, not just presence
</success_criteria>

<output>
After completion, create .planning/phases/078-canvas-presentations/078-04-SUMMARY.md
</output>
