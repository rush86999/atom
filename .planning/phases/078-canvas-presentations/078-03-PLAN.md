---
phase: 078-canvas-presentations
plan: 03
type: execute
wave: 2
depends_on: [078-01]
files_modified:
  - backend/tests/e2e_ui/pages/page_objects.py
  - backend/tests/e2e_ui/tests/test_canvas_forms.py
autonomous: true

must_haves:
  truths:
    - "User can fill out canvas form fields"
    - "Form validation shows errors for invalid input"
    - "Form submission creates submission record"
    - "Required field validation works correctly"
    - "Success feedback displays after successful submission"
    - "Submit button is disabled during submission"
  artifacts:
    - path: "backend/tests/e2e_ui/pages/page_objects.py"
      provides: "CanvasFormPage Page Object with form interaction methods"
      min_lines: 120
      contains: "class CanvasFormPage"
    - path: "backend/tests/e2e_ui/tests/test_canvas_forms.py"
      provides: "E2E tests for canvas form submission and validation"
      min_lines: 150
      contains: "test_form_submit_success|test_form_validation_error"
  key_links:
    - from: "CanvasFormPage.fill_form_field()"
      to: "InteractiveForm component inputs"
      via: "name attribute selectors"
      pattern: "input.*name=|select.*name=|form.*field"
    - from: "test_form_submission"
      to: "/api/canvas/submit endpoint"
      via: "POST request with form_data"
      pattern: "canvas/submit|form_submission"
---

<objective>
Create Form Page Objects and E2E tests for canvas form submission and validation.

Purpose: Verify forms render correctly, validate input, show errors, and submit successfully
Output: CanvasFormPage Page Object and form submission tests
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/078-canvas-presentations/078-01-SUMMARY.md
@backend/tools/canvas_tool.py
@backend/api/canvas_routes.py
@frontend-nextjs/components/canvas/InteractiveForm.tsx
@backend/tests/e2e_ui/pages/page_objects.py

# Form Component Context
- InteractiveForm component supports: text, email, number, select, checkbox field types
- Form schema includes: name, label, type, placeholder, defaultValue, required, validation
- Validation rules: pattern (regex), min/max (numbers), custom error messages
- Form state tracked in window.atom.canvas.getState() with form_data and validation_errors
- Submit endpoint: POST /api/canvas/submit with {canvas_id, form_data, agent_execution_id, agent_id}
- Form submission creates CanvasAudit record and AgentExecution for governance
- Success feedback: Green checkmark with "Submitted successfully!" message
</context>

<tasks>

<task type="auto">
  <name>Create CanvasFormPage Page Object with form locators</name>
  <files>backend/tests/e2e_ui/pages/page_objects.py</files>
  <action>
    Add CanvasFormPage class to page_objects.py with:

    1. Locators (form field selectors):
       - form_container - Form container div
       - form_title - Form title (h3 element)
       - form_field - Generic form field locator (by name)
       - form_input_text - Text input fields
       - form_input_email - Email input fields
       - form_input_number - Number input fields
       - form_select - Select dropdown fields
       - form_checkbox - Checkbox fields
       - form_submit_button - Submit button
       - form_error_message - Field-level error (with AlertCircle icon)
       - form_success_message - Success message (with Check icon)
       - form_label - Field label elements
       - required_indicator - Required field asterisk (*)

    2. Methods:
       - is_loaded() -> bool - Check if form is visible
       - get_title() -> str - Get form title
       - get_field_count() -> int - Count form fields
       - fill_text_field(name: str, value: str) -> None
       - fill_email_field(name: str, value: str) -> None
       - fill_number_field(name: str, value: float) -> None
       - select_option(name: str, value: str) -> None
       - set_checkbox(name: str, checked: bool) -> None
       - get_field_value(name: str) -> str - Get current field value
       - get_field_error(name: str) -> str - Get error message for field
       - has_field_error(name: str) -> bool
       - click_submit() -> None
       - is_submit_enabled() -> bool
       - is_submitting() -> bool - Check if button shows "Submitting..."
       - is_success_message_visible() -> bool
       - get_success_message() -> str
       - wait_for_submission(timeout: int = 5000) -> None

    3. Form state helpers:
       - get_form_data() -> dict - Extract current form data from all fields
       - is_field_required(name: str) -> bool - Check if field has required asterisk
       - get_field_label(name: str) -> str - Get label text for field

    Follow BasePage pattern
    Use name attribute selectors for fields (most reliable)
    Include docstrings with validation behavior notes
  </action>
  <verify>
    # Check file has CanvasFormPage class
    grep -c "class CanvasFormPage" backend/tests/e2e_ui/pages/page_objects.py
    grep -c "form_field\|form_submit\|form_error" backend/tests/e2e_ui/pages/page_objects.py
  </verify>
  <done>
    CanvasFormPage class exists with 120+ lines including form interaction methods
  </done>
</task>

<task type="auto">
  <name>Create form submission and validation E2E tests</name>
  <files>backend/tests/e2e_ui/tests/test_canvas_forms.py</files>
  <action>
    Create test_canvas_forms.py with:

    1. Helper functions:
       - create_test_form_schema(field_configs) -> dict
       - trigger_form_canvas(page, schema, title) -> str
       - mock_canvas_submit_api(page, response) -> None (intercept POST /api/canvas/submit)

    2. Form rendering tests:
       - test_form_renders_with_title():
         - Trigger form with title
         - Verify form title displays
         - Verify all fields render

       - test_form_field_types():
         - Create form with all field types (text, email, number, select, checkbox)
         - Verify each field type renders correctly
         - Verify field labels display

       - test_form_required_fields():
         - Create form with required fields
         - Verify required asterisk (*) appears
         - Verify count matches required field count

    3. Form validation tests:
       - test_form_required_field_validation():
         - Submit form without filling required fields
         - Verify error messages appear
         - Verify form does not submit

       - test_form_email_validation():
         - Enter invalid email format
         - Verify pattern validation error
         - Enter valid email and verify error clears

       - test_form_number_min_max_validation():
         - Enter number below min
         - Verify min validation error
         - Enter number above max
         - Verify max validation error
         - Enter valid number and verify error clears

       - test_form_validation_summary():
         - Trigger multiple validation errors
         - Verify all errors display simultaneously
         - Verify each error shows correct field

    4. Form submission tests:
       - test_form_submit_success():
         - Fill all required fields with valid data
         - Mock successful API response
         - Click submit
         - Verify success message displays
         - Verify canvas audit created in DB

       - test_form_submit_button_disabled_during_submission():
         - Click submit
         - Verify button shows "Submitting..."
         - Verify button is disabled during submit
         - Verify button re-enables after response

       - test_form_submit_with_agent_context():
         - Submit form with agent_id and agent_execution_id
         - Verify governance check passes
         - Verify AgentExecution record created
         - Verify CanvasAudit record links to execution

       - test_form_submit_governance_blocked():
         - Submit form with STUDENT agent (below SUPERVISED)
         - Verify governance blocks submission
         - Verify error message shows maturity requirement

    5. Form state API tests:
       - test_form_state_api():
         - Fill form fields
         - Call window.atom.canvas.getState(canvas_id)
         - Verify form_data matches input values
         - Verify validation_errors empty when valid

    Use authenticated_page fixture
    Use page.route() to intercept /api/canvas/submit for mocking
    Verify DB records for governance and audit trail
    Include unique IDs in form field names
  </action>
  <verify>
    # Check test file exists with form tests
    grep -c "def test_.*form" backend/tests/e2e_ui/tests/test_canvas_forms.py
    grep -c "validation\|submit\|governance" backend/tests/e2e_ui/tests/test_canvas_forms.py
  </verify>
  <done>
    test_canvas_forms.py exists with 10+ test cases covering validation and submission
  </done>
</task>

</tasks>

<verification>
1. CanvasFormPage handles all form field types
2. Form validation tests cover all validation rules
3. Submission tests verify API integration and governance
4. Tests mock API responses appropriately
5. Database verification included for audit trail
</verification>

<success_criteria>
- CanvasFormPage Page Object with 120+ lines
- test_canvas_forms.py with 10+ test cases
- Tests cover: rendering (3), validation (4), submission (3), state API (1+)
- Tests verify DB records for governance and audit
</success_criteria>

<output>
After completion, create .planning/phases/078-canvas-presentations/078-03-SUMMARY.md
</output>
