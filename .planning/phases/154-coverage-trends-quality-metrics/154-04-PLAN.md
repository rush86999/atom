---
phase: 154-coverage-trends-quality-metrics
plan: 04
type: execute
wave: 2
depends_on: [154-01, 154-02, 154-03]
files_modified:
  - backend/tests/scripts/generate_quality_report.py
  - .github/workflows/unified-tests-parallel.yml
autonomous: true

must_haves:
  truths:
    - "Comprehensive quality metrics report consolidates coverage trends, flaky tests, complexity hotspots, and slow tests"
    - "Single PR comment with collapsible sections provides unified quality feedback"
    - "Quality metrics posted to PR for developer visibility and action"
    - "Report includes actionable recommendations (fix flaky tests, reduce complexity, optimize slow tests)"
  artifacts:
    - path: "backend/tests/scripts/generate_quality_report.py"
      provides: "Consolidated quality metrics report generation"
      min_lines: 200
      contains: "generate_quality_report, load_flaky_tests, load_complexity_hotspots, load_slow_tests"
    - path: ".github/workflows/unified-tests-parallel.yml"
      provides: "CI/CD step for posting consolidated quality report"
      contains: "generate_quality_report.py, github-script@v7, createComment"
  key_links:
    - from: ".github/workflows/unified-tests-parallel.yml"
      to: "backend/tests/scripts/generate_quality_report.py"
      via: "python3 generate_quality_report.py --trending-file --quarantine-db --complexity-file"
      pattern: "generate_quality_report.*--trending-file.*--quarantine-db"
    - from: "generate_quality_report.py"
      to: "backend/tests/scripts/generate_pr_trend_comment.py"
      via: "Import or reuse trend comment generation logic"
      pattern: "from.*generate_pr_trend_comment|import.*generate_pr_comment"
    - from: "generate_quality_report.py"
      to: "backend/tests/coverage_reports/metrics/cross_platform_trend.json"
      via: "JSON parsing of trending data"
      pattern: "json.load.*cross_platform_trend"
    - from: "generate_quality_report.py"
      to: "backend/tests/scripts/flaky_test_tracker.py"
      via: "FlakyTestTracker import for database queries"
      pattern: "from.*flaky_test_tracker.*import|FlakyTestTracker("
---

<objective>
Generate comprehensive quality metrics report consolidating coverage trends, flaky tests, complexity hotspots, and slow tests into single PR comment

Purpose: Provide unified quality feedback to developers with actionable recommendations, prevent PR comment spam by consolidating all metrics into single report
Output: Single markdown PR comment with collapsible sections (Coverage Trends, Flaky Tests, Complexity Hotspots, Slow Tests, Summary)
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/tests/scripts/generate_pr_trend_comment.py
@backend/tests/scripts/assert_test_ratio_tracker.py
@backend/tests/scripts/merge_complexity_coverage.py
@backend/tests/scripts/track_execution_times.py
@backend/tests/scripts/flaky_test_tracker.py
@.planning/phases/154-coverage-trends-quality-metrics/154-RESEARCH.md
@.planning/phases/154-coverage-trends-quality-metrics/154-01-PLAN.md
@.planning/phases/154-coverage-trends-quality-metrics/154-02-PLAN.md
@.planning/phases/154-coverage-trends-quality-metrics/154-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive quality report generator</name>
  <files>backend/tests/scripts/generate_quality_report.py</files>
  <action>
Create generate_quality_report.py script that consolidates all quality metrics into single markdown report:

1. Import required modules (argparse, json, sys, pathlib)
2. Import existing modules:
   - from tests.scripts.flaky_test_tracker import FlakyTestTracker
   - Optional: from tests.scripts.generate_pr_trend_comment import generate_pr_comment (reuse trend comment logic)
3. Define load_flaky_tests(db_path, platform) function:
   - Initialize FlakyTestTracker(db_path)
   - Get quarantined tests: tracker.get_quarantined_tests(platform)
   - Close tracker
   - Return list of flaky test records
4. Define load_complexity_hotspots(complexity_file) function:
   - Load JSON from complexity_file
   - Extract hotspots list
   - Return list sorted by complexity (descending)
5. Define load_slow_tests(durations_file) function:
   - Load JSON from durations_file (output from track_execution_times.py)
   - Extract slow_tests list
   - Return list sorted by max_execution_time (descending)
6. Define generate_quality_report(trending_data, flaky_tests, complexity_hotspots, slow_tests) function:
   - Initialize lines list with header: "## Test Quality Metrics Report"
   - Section 1: Coverage Trends (reuse generate_pr_comment() logic or inline)
     - Build markdown table with Platform | Previous | Current | Delta | Trend
     - Use trend indicators (↑↓→) and severity badges (🔴🟡✅)
   - Section 2: Flaky Tests
     - If flaky_tests exist: show count + table (Test | Flaky Rate | Platform)
     - Else: "✅ No flaky tests detected"
   - Section 3: Complexity Hotspots
     - If complexity_hotspots exist: show count + table (Function | Complexity | Coverage | Priority)
     - Else: "✅ No complexity hotspots"
   - Section 4: Slow Tests (>10s)
     - If slow_tests exist: show count + table (Test | Avg Time | Max Time | Platform)
     - Else: "✅ All tests under 10s"
   - Section 5: Summary with recommendations:
     - Count issues (flaky_tests, complexity_hotspots, slow_tests)
     - If issues exist: list actionable items (e.g., "Fix 5 flaky tests", "Refactor 3 high-complexity functions")
     - Else: "✅ All quality metrics passing"
   - Return joined lines as markdown string
7. Define main() function with CLI:
   - argparse setup:
     - --trending-file (required, path to cross_platform_trend.json)
     - --quarantine-db (required, path to flaky_tests.db)
     - --complexity-file (required, path to complexity_hotspots.json)
     - --durations-file (optional, path to slow_tests.json)
     - --platform (required, backend/frontend/mobile/desktop)
     - --output (default: quality_report.md)
   - Load all data sources:
     - Load trending JSON
     - Load flaky tests via load_flaky_tests()
     - Load complexity hotspots via load_complexity_hotspots()
     - Load slow tests via load_slow_tests() if file exists
   - Generate report via generate_quality_report()
   - Write to output file
   - Print to stdout for GitHub Action consumption
   - Exit 0 if no issues, 1 if issues found

Reference RESEARCH.md section "Example 2: Comprehensive Quality Metrics Report" for output format.
Reuse generate_pr_comment() logic from Plan 01 instead of duplicating trend calculation.
  </action>
  <verify>
cd backend && python3 tests/scripts/generate_quality_report.py --trending-file tests/coverage_reports/metrics/cross_platform_trend.json --quarantine-db tests/coverage_reports/metrics/flaky_tests.db --complexity-file tests/coverage_reports/metrics/complexity_hotspots.json --platform backend --output /tmp/quality_report.md && head -50 /tmp/quality_report.md
  </verify>
  <done>
Quality report generator consolidates all metrics into single markdown, sections for coverage trends/flaky tests/complexity/slow tests, summary with actionable recommendations
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate quality report into CI/CD workflow</name>
  <files>.github/workflows/unified-tests-parallel.yml</files>
  <action>
Modify unified-tests-parallel.yml to post consolidated quality report instead of separate PR comments:

1. Find existing "Post coverage trend PR comment" step from Plan 01
2. Replace with consolidated "Generate quality metrics report" step:
   ```yaml
   - name: Generate quality metrics report (Backend)
     if: matrix.platform == 'backend' && github.event_name == 'pull_request'
     run: |
       cd backend
       python3 tests/scripts/generate_quality_report.py \
         --trending-file tests/coverage_reports/metrics/cross_platform_trend.json \
         --quarantine-db tests/coverage_reports/metrics/flaky_tests.db \
         --complexity-file tests/coverage_reports/metrics/complexity_hotspots.json \
         --durations-file tests/coverage_reports/metrics/slow_tests.json \
         --platform backend \
         --output quality_metrics_report.md
   ```
3. Add PR comment posting step:
   ```yaml
   - name: Post quality metrics PR comment
     if: matrix.platform == 'backend' && github.event_name == 'pull_request'
     uses: actions/github-script@v7
     with:
       script: |
         const fs = require('fs');
         const report = fs.readFileSync('backend/quality_metrics_report.md', 'utf8');
         github.rest.issues.createComment({
           issue_number: context.issue.number,
           owner: context.repo.owner,
           repo: context.repo.repo,
           body: report
         });
   ```
4. Remove or comment out separate PR comment steps (Plan 01's trend comment, Plan 02's assert ratio, Plan 03's complexity/slow tests)
   - Goal: Single consolidated comment instead of 4 separate comments
5. Ensure step runs after all metric generation steps (trend analysis, complexity, execution times)

Use continue-on-error: true for PR comment posting (don't block CI if GitHub API fails).
Consolidate metrics from all plans into single report for better UX.
  </action>
  <verify>
grep -A 10 "Generate quality metrics report" .github/workflows/unified-tests-parallel.yml | grep -q "generate_quality_report.py"
  </verify>
  <done>
CI/CD workflow generates consolidated quality report after all metrics collected, posts single PR comment with all sections (trends/flaky/complexity/slow tests)
  </done>
</task>

<task type="auto">
  <name>Task 3: Test quality report generation and PR posting</name>
  <files>backend/tests/scripts/generate_quality_report.py</files>
  <action>
Test quality report generation with real data:

1. Generate all required input data:
   - Ensure cross_platform_trend.json exists with 2+ history entries
   - Ensure flaky_tests.db exists (create test database if needed)
   - Generate complexity_hotspots.json via merge_complexity_coverage.py
   - Generate slow_tests.json via track_execution_times.py
2. Run quality report generator:
   ```bash
   cd backend
   python3 tests/scripts/generate_quality_report.py \
     --trending-file tests/coverage_reports/metrics/cross_platform_trend.json \
     --quarantine-db tests/coverage_reports/metrics/flaky_tests.db \
     --complexity-file tests/coverage_reports/metrics/complexity_hotspots.json \
     --durations-file tests/coverage_reports/metrics/slow_tests.json \
     --platform backend \
     --output /tmp/quality_report.md
   ```
3. Verify output format:
   - Contains "## Test Quality Metrics Report" header
   - Contains all 5 sections (Coverage Trends, Flaky Tests, Complexity Hotspots, Slow Tests, Summary)
   - Uses markdown tables for data presentation
   - Uses emoji badges (✅🔴🟡⏱️🔄🔥) for visual clarity
   - Trend indicators present (↑↓→) in coverage section
4. Test with missing optional data (no slow_tests.json):
   - Verify script handles missing durations-file gracefully
   - Verify "Slow Tests" section shows "✅ All tests under 10s" or skips section
5. Test with no issues (all metrics passing):
   - Verify exit code 0
   - Verify summary shows "✅ All quality metrics passing"
6. Test with issues (flaky tests or complexity hotspots):
   - Verify exit code 1
   - Verify summary lists actionable items

If any input data is missing, create test data with realistic values for verification.
  </action>
  <verify>
cat /tmp/quality_report.md | grep -q "Test Quality Metrics Report" && cat /tmp/quality_report.md | grep -q "Coverage Trends\|Flaky Tests\|Complexity Hotspots\|Slow Tests\|Summary"
  </verify>
  <done>
Quality report tested with real data, all 5 sections present and formatted correctly, exit codes match issue presence (0=no issues, 1=issues found)
  </done>
</task>

</tasks>

<verification>
1. Comprehensive quality report consolidates coverage trends, flaky tests, complexity hotspots, and slow tests into single markdown
2. PR comment posting uses github-script@v7 to post consolidated report on pull requests
3. Report sections are collapsible or clearly separated for readability
4. Actionable recommendations provided in summary section (fix X flaky tests, refactor Y functions, optimize Z slow tests)
5. Exit code 0 if all metrics passing, 1 if issues found (for CI gate enforcement)
</verification>

<success_criteria>
1. Comprehensive quality metrics report consolidates all metrics (coverage trends, flaky tests, complexity, execution time)
2. Single PR comment replaces multiple separate comments (better UX, less spam)
3. Actionable summary provides clear next steps for developers
4. CI/CD workflow posts report on every pull request
</success_criteria>

<output>
After completion, create `.planning/phases/154-coverage-trends-quality-metrics/154-04-SUMMARY.md`
</output>
