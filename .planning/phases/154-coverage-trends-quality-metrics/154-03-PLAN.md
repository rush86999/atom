---
phase: 154-coverage-trends-quality-metrics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/scripts/merge_complexity_coverage.py
  - backend/tests/scripts/track_execution_times.py
  - backend/tests/scripts/flaky_test_tracker.py
  - backend/requirements-testing.txt
  - .github/workflows/unified-tests-parallel.yml
autonomous: true

must_haves:
  truths:
    - "Code complexity metrics (cyclomatic complexity) tracked alongside coverage"
    - "High-complexity, low-coverage functions identified as technical debt hotspots"
    - "Flaky test detection tracks execution time (avg/max) in quarantine database"
    - "Slow tests (>10s) flagged for optimization to prevent CI/CD slowdown"
  artifacts:
    - path: "backend/tests/scripts/merge_complexity_coverage.py"
      provides: "Merge radon complexity with pytest coverage data"
      min_lines: 150
      contains: "identify_hotspots, load_radon_complexity, load_coverage_data"
    - path: "backend/tests/scripts/track_execution_times.py"
      provides: "Parse pytest --durations output and update flaky_test_tracker"
      min_lines: 100
      contains: "parse_durations_output, update_execution_times, get_slow_tests"
    - path: "backend/tests/scripts/flaky_test_tracker.py"
      provides: "Extended schema with avg_execution_time and max_execution_time"
      contains: "avg_execution_time REAL, max_execution_time REAL"
    - path: "backend/requirements-testing.txt"
      provides: "radon dependency for cyclomatic complexity analysis"
      contains: "radon>=6.0"
  key_links:
    - from: ".github/workflows/unified-tests-parallel.yml"
      to: "backend/tests/scripts/merge_complexity_coverage.py"
      via: "radon cc backend/core -a --json | python3 merge_complexity_coverage.py"
      pattern: "radon cc.*merge_complexity_coverage"
    - from: ".github/workflows/unified-tests-parallel.yml"
      to: "backend/tests/scripts/track_execution_times.py"
      via: "pytest --durations=10 | python3 track_execution_times.py"
      pattern: "pytest.*--durations.*track_execution_times"
    - from: "track_execution_times.py"
      to: "backend/tests/scripts/flaky_test_tracker.py"
      via: "FlakyTestTracker.update_execution_time()"
      pattern: "update_execution_time.*avg_execution_time"
---

<objective>
Track code complexity and test execution time alongside coverage to identify technical debt hotspots

Purpose: Combine complexity analysis (radon) with coverage data to flag high-complexity, untested functions; track execution time to prevent CI slowdown
Output: Complexity hotspot report (high complexity + low coverage), slow test list (>10s), flaky test tracker with execution time columns
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/tests/scripts/flaky_test_tracker.py
@backend/tests/scripts/flaky_test_detector.py
@backend/tests/scripts/ci_quality_gate.py
@.planning/phases/154-coverage-trends-quality-metrics/154-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend flaky test tracker schema with execution time</name>
  <files>backend/tests/scripts/flaky_test_tracker.py</files>
  <action>
Extend flaky_test_tracker.py to track execution time for slow test detection:

1. Modify _create_schema() to add execution time columns:
   - Add avg_execution_time REAL DEFAULT 0.0 after flaky_rate
   - Add max_execution_time REAL DEFAULT 0.0 after avg_execution_time
   - Use ALTER TABLE if schema exists (migration pattern):
     ```python
     # Check if column exists
     cursor = self.conn.execute("PRAGMA table_info(flaky_tests)")
     columns = [row[1] for row in cursor.fetchall()]
     if 'avg_execution_time' not in columns:
         self.conn.execute("ALTER TABLE flaky_tests ADD COLUMN avg_execution_time REAL DEFAULT 0.0")
     if 'max_execution_time' not in columns:
         self.conn.execute("ALTER TABLE flaky_tests ADD COLUMN max_execution_time REAL DEFAULT 0.0")
     ```
2. Modify record_flaky_test() signature to accept execution_times parameter:
   - Add execution_times: Optional[List[float]] = None
   - Calculate avg_time = sum(execution_times) / len(execution_times) if provided else 0.0
   - Calculate max_time = max(execution_times) if provided else 0.0
   - Pass avg_execution_time and max_execution_time to INSERT/UPDATE statement
3. Add new method update_execution_time():
   - Signature: update_execution_time(test_path: str, platform: str, execution_times: List[float])
   - Calculate avg and max from execution_times
   - UPDATE flaky_tests SET avg_execution_time=?, max_execution_time=?, updated_at=datetime('now') WHERE test_path=? AND platform=?
4. Add new method get_slow_tests():
   - Signature: get_slow_tests(min_time: float = 10.0, platform: Optional[str] = None, limit: int = 50) -> List[Dict]
   - SELECT * FROM flaky_tests WHERE max_execution_time >= ?
   - Optional platform filter: AND platform = ?
   - ORDER BY max_execution_time DESC LIMIT ?
   - Return list of slow test records sorted by max time

Reference existing record_flaky_test() implementation for schema pattern.
Do NOT modify existing flaky test detection logic - only add time tracking columns.
  </action>
  <verify>
cd backend && python3 -c "from tests.scripts.flaky_test_tracker import FlakyTestTracker; import tempfile; from pathlib import Path; db = Path(tempfile.mktemp(suffix='.db')); t = FlakyTestTracker(db); conn = t.conn; cursor = conn.execute('PRAGMA table_info(flaky_tests)'); cols = [r[1] for r in cursor.fetchall()]; print('avg_execution_time' in cols and 'max_execution_time' in cols)"
  </verify>
  <done>
Flaky test tracker schema extended with avg_execution_time and max_execution_time columns, update_execution_time() and get_slow_tests() methods implemented
  </done>
</task>

<task type="auto">
  <name>Task 2: Create complexity and coverage merge script</name>
  <files>backend/tests/scripts/merge_complexity_coverage.py</files>
  <action>
Create merge_complexity_coverage.py script to identify high-complexity, low-coverage hotspots:

1. Import required modules (json, sys, pathlib, argparse)
2. Define load_radon_complexity(complexity_file) function:
   - Load JSON output from radon cc --json
   - Radon format: {"module/path": {"class": {"method": complexity_score}}}
   - Return nested dict structure
3. Define load_coverage_data(coverage_file) function:
   - Load pytest coverage.json report
   - Extract files data: coverage_data["files"]
   - Return dict mapping file paths to coverage percentages
4. Define identify_hotspots(complexity_data, coverage_data) function:
   - Initialize hotspots = []
   - Iterate through complexity_data items:
     - For each (module_path, classes) pair:
       - For each (class_name, methods) in classes:
         - For each (method_name, complexity) in methods:
           - Get file_coverage from coverage_data.get(module_path, {}).get("summary", {}).get("percent_covered", 0.0)
           - If complexity > 10 AND file_coverage < 80:
             - Determine priority: "high" if complexity > 20 else "medium"
             - Append hotspot dict: {file, class, function, complexity, coverage, priority}
   - Sort hotspots by complexity (descending)
   - Return sorted list
5. Define main() function with CLI:
   - argparse setup:
     - --complexity (required, path to radon JSON output)
     - --coverage (required, path to coverage.json)
     - --output (default: quality_metrics.json)
     - --min-complexity (default: 10, minimum complexity to flag)
     - --max-coverage (default: 80, maximum coverage to flag)
   - Load complexity and coverage data
   - Identify hotspots via identify_hotspots()
   - Generate output JSON:
     ```json
     {
       "hotspots": [...],
       "summary": {
         "total_hotspots": N,
         "high_priority": M,
         "medium_priority": P
       }
     }
     ```
   - Write output to file
   - Print summary to stdout
   - Exit 0 if no hotspots, 1 if hotspots found

Reference RESEARCH.md section "Pattern 3: Cyclomatic Complexity Tracking with Radon" for implementation details.
  </action>
  <verify>
radon cc backend/core -a --json > /tmp/complexity.json 2>/dev/null && python3 backend/tests/scripts/merge_complexity_coverage.py --complexity /tmp/complexity.json --coverage backend/tests/coverage_reports/metrics/coverage.json --output /tmp/quality_metrics.json && cat /tmp/quality_metrics.json | jq .
  </verify>
  <done>
Complexity/coverage merge script created, identifies high-complexity (>10) low-coverage (<80%) functions, outputs JSON with hotspots sorted by priority
  </done>
</task>

<task type="auto">
  <name>Task 3: Create execution time tracking script</name>
  <files>backend/tests/scripts/track_execution_times.py</files>
  <action>
Create track_execution_times.py script to parse pytest --durations and update flaky test tracker:

1. Import required modules (re, json, sys, pathlib, argparse)
2. Import FlakyTestTracker: from tests.scripts.flaky_test_tracker import FlakyTestTracker
3. Define parse_durations_output(durations_text) function:
   - Parse pytest --durations output format:
     ```
     10.01s call     tests/test_foo.py::test_bar
     5.23s  call     tests/test_baz.py::test_qux
     ```
   - Use regex: r'(\d+\.\d+)s\s+(?:call|setup|teardown)\s+([^\s]+)'
   - Return dict mapping test_path -> execution_time (float)
4. Define update_execution_times(durations_dict, platform, db_path) function:
   - Initialize FlakyTestTracker(db_path)
   - For each (test_path, execution_time) in durations_dict:
     - Check if test exists in flaky_tests table
     - If exists: UPDATE avg_execution_time and max_execution_time
       - Calculate new avg as weighted average: (existing_avg * existing_runs + execution_time) / (existing_runs + 1)
       - Update max_execution_time if execution_time > existing_max
     - If not exists: Skip (only track times for tests already in quarantine)
   - Close tracker connection
5. Define main() function with CLI:
   - argparse setup:
     - --durations-file (required, path to pytest durations output)
     - --quarantine-db (required, path to flaky_tests.db)
     - --platform (required, backend/frontend/mobile/desktop)
     - --slow-threshold (default: 10.0, flag tests >10s)
     - --output (optional JSON for slow tests list)
   - Read durations file
   - Parse durations via parse_durations_output()
   - Update tracker via update_execution_times()
   - Get slow tests via tracker.get_slow_tests(min_time=slow_threshold, platform=platform)
   - Print summary:
     - Total tests tracked, Average execution time, Slow test count
     - List top 10 slow tests with times
   - If --output specified, write slow tests to JSON
   - Exit 0 if no slow tests, 1 if slow tests found

Reference RESEARCH.md section "Pattern 4: Flaky Test Quarantine with Execution Time" for implementation details.
  </action>
  <verify>
cd backend && pytest tests/ --durations=10 2>/dev/null | tee /tmp/durations.txt && python3 tests/scripts/track_execution_times.py --durations-file /tmp/durations.txt --quarantine-db tests/coverage_reports/metrics/flaky_tests.db --platform backend
  </verify>
  <done>
Execution time tracking script parses pytest --durations output, updates flaky test tracker with avg/max execution times, reports slow tests (>10s threshold)
  </done>
</task>

<task type="auto">
  <name>Task 4: Add radon and execution time tracking to CI/CD</name>
  <files>.github/workflows/unified-tests-parallel.yml backend/requirements-testing.txt</files>
  <action>
Add complexity analysis and execution time tracking to unified-tests-parallel.yml:

1. Add radon to backend/requirements-testing.txt:
   - Add: `radon>=6.0  # Cyclomatic complexity analysis`
2. In backend test job, add complexity analysis step after pytest:
   ```yaml
   - name: Analyze code complexity (Backend)
     if: matrix.platform == 'backend'
     run: |
       cd backend
       # Analyze only changed files (git diff)
       CHANGED_FILES=$(git diff --name-only origin/main...HEAD | grep '\.py$' || echo "")
       if [ -n "$CHANGED_FILES" ]; then
         radon cc $CHANGED_FILES -a --json > tests/coverage_reports/metrics/complexity.json
       else
         # Fallback: analyze core modules
         radon cc backend/core -a --json > tests/coverage_reports/metrics/complexity.json
       fi
   ```
3. Add complexity/coverage merge step:
   ```yaml
   - name: Identify complexity hotspots (Backend)
     if: matrix.platform == 'backend'
     run: |
       cd backend
       python3 tests/scripts/merge_complexity_coverage.py \
         --complexity tests/coverage_reports/metrics/complexity.json \
         --coverage tests/coverage_reports/metrics/coverage.json \
         --output tests/coverage_reports/metrics/complexity_hotspots.json
   ```
4. Modify existing pytest step to enable durations:
   - Add: `--durations=10` to pytest command
   - Pipe output to file: `| tee tests/coverage_reports/metrics/test_durations.txt`
5. Add execution time tracking step after pytest:
   ```yaml
   - name: Track execution times (Backend)
     if: matrix.platform == 'backend'
     run: |
       cd backend
       python3 tests/scripts/track_execution_times.py \
         --durations-file tests/coverage_reports/metrics/test_durations.txt \
         --quarantine-db tests/coverage_reports/metrics/flaky_tests.db \
         --platform backend \
         --slow-threshold 10.0 \
         --output tests/coverage_reports/metrics/slow_tests.json
     continue-on-error: true  # Don't block CI on slow tests
   ```
6. Upload complexity and execution time reports as artifacts

Do NOT block PRs on complexity or execution time in Phase 1 (use as warnings only).
Run radon only on changed files for performance (avoid analyzing entire codebase).
  </action>
  <verify>
grep -q "radon>=6.0" backend/requirements-testing.txt && grep -A 3 "Analyze code complexity" .github/workflows/unified-tests-parallel.yml | grep -q "radon cc"
  </verify>
  <done>
radon added to requirements, CI/CD workflow analyzes complexity on changed files, merges with coverage data, tracks execution times from pytest --durations, uploads reports as artifacts
  </done>
</task>

</tasks>

<verification>
1. Flaky test tracker schema extended with avg_execution_time and max_execution_time columns
2. Complexity analysis (radon) identifies high-complexity (>10), low-coverage (<80%) functions
3. Execution time tracking parses pytest --durations output and updates quarantine database
4. Slow tests (>10s) flagged and reported with execution time metrics
5. CI/CD workflow runs complexity and execution time analysis on every backend test run
</verification>

<success_criteria>
1. Code complexity metrics (cyclomatic complexity) tracked alongside coverage
2. High-complexity, low-coverage functions identified as technical debt hotspots
3. Flaky test detection tracks execution time (avg/max) in quarantine database
4. Slow tests (>10s) flagged for optimization to prevent CI/CD slowdown
</success_criteria>

<output>
After completion, create `.planning/phases/154-coverage-trends-quality-metrics/154-03-SUMMARY.md`
</output>
