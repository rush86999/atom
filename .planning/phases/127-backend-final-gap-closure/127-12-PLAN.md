---
phase: 127-backend-final-gap-closure
plan: 12
type: execute
wave: 3
depends_on: ["127-11"]
files_modified:
  - backend/tests/test_browser_tool_integration.py
  - backend/tests/test_device_capabilities_integration.py
  - backend/tests/coverage_reports/metrics/phase_137_device_coverage.json
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Coverage increases measurably from baseline"
    - "Integration tests for browser tool increase coverage"
    - "Integration tests for device capabilities increase coverage"
  artifacts:
    - path: "backend/tests/test_browser_tool_integration.py"
      provides: "Browser tool integration tests"
      min_lines: 150
    - path: "backend/tests/test_device_capabilities_integration.py"
      provides: "Device capabilities integration tests"
      min_lines: 130
    - path: "backend/tests/coverage_reports/metrics/phase_127_device_coverage.json"
      provides: "Device coverage measurement after tests"
      contains: "before/after coverage comparison"
  key_links:
    - from: "test_browser_tool_integration.py"
      to: "tools/browser_tool.py"
      via: "BrowserTool class method calls"
      pattern: "browser_tool.*BrowserTool.*(navigate|scrape|screenshot)"
    - from: "test_device_capabilities_integration.py"
      to: "tools/device_tool.py"
      via: "DeviceTool class method calls"
      pattern: "device_tool.*DeviceTool.*(get_location|camera|notifications)"
---

# Phase 127: Backend Final Gap Closure - Plan 12

## Objective

**Add 40-45 integration tests for Device and Browser automation tools** to continue closing the gap to 80% coverage target.

**Purpose:** tools/browser_tool.py and tools/device_tool.py are high-impact files. Integration tests that call actual class methods will measurably increase overall coverage.

**Output:** 40-45 integration tests, measurable coverage increase (+3-5 pp target), coverage report documenting improvement.

## Context

@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

@.planning/phases/127-backend-final-gap-closure/127-VERIFICATION.md
@.planning/phases/127-backend-final-gap-closure/127-11-SUMMARY.md

@backend/tools/browser_tool.py
@backend/tools/device_tool.py
@backend/tests/conftest.py

## Gap Being Closed

**Blocker 1 [scope_sanity]:** Insufficient test coverage to achieve 80% target. This plan adds 40-45 more integration tests for device and browser tools.

## Tasks

<task type="auto">
  <name>Task 1: Create Browser Tool Integration Tests (20-22 tests)</name>
  <files>backend/tests/test_browser_tool_integration.py</files>
  <action>
    Create integration tests for tools/browser_tool.py:

    ```python
    # backend/tests/test_browser_tool_integration.py
    """
    Integration coverage tests for tools/browser_tool.py.
    """
    import pytest
    from unittest.mock import Mock, AsyncMock, patch
    from tools.browser_tool import BrowserTool, BrowserSession
    from core.models import SessionLocal

    @pytest.fixture
    def browser_tool(db_session):
        return BrowserTool(db_session)

    class TestBrowserToolSessionManagement:
        """Tests for browser session management."""

        def test_create_session(self, browser_tool):
            """Test creating a new browser session."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_browser = Mock()
                mock_playwright.chromium.launch.return_value = mock_browser
                mock_browser.new_context.return_value = Mock()
                mock_browser.new_page.return_value = Mock()

                result = browser_tool.create_session(
                    user_id="test_user",
                    headless=True
                )
                assert result["success"] is True
                assert "session_id" in result

        def test_create_session_with_options(self, browser_tool):
            """Test creating session with custom options."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_browser = Mock()
                mock_playwright.chromium.launch.return_value = mock_browser
                mock_browser.new_context.return_value = Mock()
                mock_browser.new_page.return_value = Mock()

                result = browser_tool.create_session(
                    user_id="test_user",
                    headless=False,
                    viewport={"width": 1920, "height": 1080}
                )
                assert result["success"] is True

        def test_get_session_status(self, browser_tool, db_session):
            """Test getting session status."""
            # Create a session record first
            session = BrowserSession(
                session_id="test_session_123",
                user_id="test_user",
                status="active"
            )
            db_session.add(session)
            db_session.commit()

            result = browser_tool.get_session_status("test_session_123")
            assert result is not None
            assert result["status"] == "active"

        def test_close_session(self, browser_tool):
            """Test closing a browser session."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_browser = Mock()
                mock_playwright.chromium.launch.return_value = mock_browser

                result = browser_tool.close_session(session_id="test_session")
                assert result["success"] is True

        def test_list_sessions(self, browser_tool, db_session):
            """Test listing all sessions for a user."""
            # Create multiple sessions
            for i in range(3):
                session = BrowserSession(
                    session_id=f"session_{i}",
                    user_id="list_test_user",
                    status="active"
                )
                db_session.add(session)
            db_session.commit()

            result = browser_tool.list_sessions(user_id="list_test_user")
            assert len(result) >= 3

    class TestBrowserToolNavigation:
        """Tests for browser navigation."""

        def test_navigate_to_url(self, browser_tool):
            """Test navigating to a URL."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_page = Mock()
                mock_page.goto.return_value = "ok"

                result = browser_tool.navigate(
                    session_id="test_session",
                    url="https://example.com"
                )
                assert result["success"] is True

        def test_navigate_with_wait(self, browser_tool):
            """Test navigating with wait for selector."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_page = Mock()
                mock_page.goto.return_value = "ok"
                mock_page.wait_for_selector.return_value = Mock()

                result = browser_tool.navigate(
                    session_id="test_session",
                    url="https://example.com",
                    wait_for_selector=".content"
                )
                assert result["success"] is True

        def test_navigate_invalid_url(self, browser_tool):
            """Test navigating to invalid URL."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_page = Mock()
                mock_page.goto.side_effect = Exception("Invalid URL")

                result = browser_tool.navigate(
                    session_id="test_session",
                    url="not-a-url"
                )
                assert result["success"] is False

        def test_go_back(self, browser_tool):
            """Test going back in browser history."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_page = Mock()
                mock_page.go_back.return_value = Mock()

                result = browser_tool.go_back(session_id="test_session")
                assert result["success"] is True

        def test_go_forward(self, browser_tool):
            """Test going forward in browser history."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_page = Mock()
                mock_page.go_forward.return_value = Mock()

                result = browser_tool.go_forward(session_id="test_session")
                assert result["success"] is True

        def test_refresh_page(self, browser_tool):
            """Test refreshing the page."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_page = Mock()
                mock_page.reload.return_value = Mock()

                result = browser_tool.refresh(session_id="test_session")
                assert result["success"] is True

    class TestBrowserToolScraping:
        """Tests for web scraping functionality."""

        def test_scrape_page_text(self, browser_tool):
            """Test scraping text content from page."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_page = Mock()
                mock_page.inner_text.return_value = "Page content here"

                result = browser_tool.scrape_text(
                    session_id="test_session",
                    selector="body"
                )
                assert result["success"] is True
                assert "content" in result

        def test_scrape_page_html(self, browser_tool):
            """Test scraping HTML content from page."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_page = Mock()
                mock_page.inner_html.return_value = "<div>HTML content</div>"

                result = browser_tool.scrape_html(
                    session_id="test_session",
                    selector=".content"
                )
                assert result["success"] is True
                assert "html" in result

        def test_scrape_multiple_elements(self, browser_tool):
            """Test scraping multiple elements."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_page = Mock()
                mock_page.query_selector_all.return_value = [
                    Mock(text_content="Item 1"),
                    Mock(text_content="Item 2")
                ]

                result = browser_tool.scrape_elements(
                    session_id="test_session",
                    selector=".item"
                )
                assert result["success"] is True
                assert len(result["elements"]) == 2

        def test_scrape_attributes(self, browser_tool):
            """Test scraping element attributes."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_element = Mock()
                mock_element.get_attribute.return_value = "attribute_value"
                mock_page.query_selector.return_value = mock_element

                result = browser_tool.scrape_attributes(
                    session_id="test_session",
                    selector="#test",
                    attributes=["href", "title"]
                )
                assert result["success"] is True
                assert result["attributes"]["href"] == "attribute_value"

    class TestBrowserToolScreenshots:
        """Tests for screenshot functionality."""

        def test_take_screenshot(self, browser_tool):
            """Test taking a screenshot."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_page = Mock()
                mock_page.screenshot.return_value = b"fake_image_data"

                result = browser_tool.screenshot(
                    session_id="test_session",
                    full_page=False
                )
                assert result["success"] is True
                assert "screenshot_path" in result

        def test_take_full_page_screenshot(self, browser_tool):
            """Test taking a full page screenshot."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_page = Mock()
                mock_page.pdf.return_value = b"fake_pdf_data"

                result = browser_tool.screenshot(
                    session_id="test_session",
                    full_page=True
                )
                assert result["success"] is True

        def test_take_element_screenshot(self, browser_tool):
            """Test taking screenshot of specific element."""
            with patch('tools/browser_tool.playwright') as mock_playwright:
                mock_element = Mock()
                mock_element.screenshot.return_value = b"element_image"
                mock_page.query_selector.return_value = mock_element

                result = browser_tool.screenshot(
                    session_id="test_session",
                    selector="#hero"
                )
                assert result["success"] is True

    class TestBrowserToolForms:
        """Tests for form interaction."""

        def test_fill_form_field(self, browser_tool):
            """Test filling a form field."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_page = Mock()
                mock_page.fill.return_value = Mock()

                result = browser_tool.fill(
                    session_id="test_session",
                    selector="#email",
                    value="test@example.com"
                )
                assert result["success"] is True

        def test_click_element(self, browser_tool):
            """Test clicking an element."""
            with patch('tools.browser_tool.playwright') as mock_playwright:
                mock_page = Mock()
                mock_page.click.return_value = Mock()

                result = browser_tool.click(
                    session_id="test_session",
                    selector="#submit-button"
                )
                assert result["success"] is True

        def test_select_option(self, browser_tool):
            """Test selecting an option from dropdown."""
            with patch('tools/browser_tool.playwright') as mock_playwright:
                mock_page = Mock()
                mock_page.select_option.return_value = Mock()

                result = browser_tool.select_option(
                    session_id="test_session",
                    selector="#country",
                    value="US"
                )
                assert result["success"] is True

        def test_upload_file(self, browser_tool):
            """Test uploading a file."""
            with patch('tools/browser_tool.playwright') as mock_playwright:
                mock_page = Mock()
                mock_page.set_input_files.return_value = Mock()

                result = browser_tool.upload_file(
                    session_id="test_session",
                    selector="#file-input",
                    file_path="/path/to/file.txt"
                )
                assert result["success"] is True

        def test_submit_form(self, browser_tool):
            """Test submitting a form."""
            with patch('tools/browser_tool.playwright') as mock_playwright:
                mock_page = Mock()
                mock_page.query_selector.return_value = Mock()
                result = browser_tool.submit_form(
                    session_id="test_session",
                    selector="#login-form"
                )
                assert result["success"] is True
    ```

    Target: 20-22 tests covering browser session management, navigation, scraping, screenshots, and forms.
  </action>
  <verify>
    - pytest tests/test_browser_tool_integration.py -v passes
    - Tests import BrowserTool and call its methods
    - Coverage report shows browser_tool.py coverage increased
    - At least 20 tests created
  </verify>
  <done>
    test_browser_tool_integration.py exists with 20+ integration tests. All tests pass and browser_tool.py coverage shows measurable increase.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Device Capabilities Integration Tests (20-22 tests)</name>
  <files>backend/tests/test_device_capabilities_integration.py</files>
  <action>
    Create integration tests for tools/device_tool.py:

    ```python
    # backend/tests/test_device_capabilities_integration.py
    """
    Integration coverage tests for tools/device_tool.py.
    """
    import pytest
    from unittest.mock import Mock, patch
    from tools.device_tool import DeviceTool, DeviceSession
    from core.models import SessionLocal

    @pytest.fixture
    def device_tool(db_session):
        return DeviceTool(db_session)

    class TestDeviceToolSessionManagement:
        """Tests for device session management."""

        def test_create_session(self, device_tool):
            """Test creating a new device session."""
            result = device_tool.create_session(
                user_id="test_user",
                device_type="mobile"
            )
            assert result["success"] is True
            assert "session_id" in result

        def test_get_session_info(self, device_tool, db_session):
            """Test getting session information."""
            session = DeviceSession(
                session_id="device_session_123",
                user_id="test_user",
                device_type="mobile",
                capabilities=["camera", "location"]
            )
            db_session.add(session)
            db_session.commit()

            result = device_tool.get_session("device_session_123")
            assert result is not None
            assert result["device_type"] == "mobile"

        def test_close_session(self, device_tool):
            """Test closing a device session."""
            result = device_tool.close_session(session_id="test_session")
            assert result["success"] is True

        def test_list_active_sessions(self, device_tool, db_session):
            """Test listing active sessions."""
            for i in range(3):
                session = DeviceSession(
                    session_id=f"device_{i}",
                    user_id="list_user",
                    device_type="mobile",
                    status="active"
                )
                db_session.add(session)
            db_session.commit()

            result = device_tool.list_sessions(user_id="list_user")
            assert len(result) >= 3

    class TestDeviceToolCamera:
        """Tests for camera capabilities."""

        def test_request_camera_permission(self, device_tool):
            """Test requesting camera permission."""
            with patch('tools.device_tool.request_permission') as mock_perm:
                mock_perm.return_value = {"granted": True}
                result = device_tool.request_permission(
                    session_id="camera_session",
                    capability="camera"
                )
                assert result["granted"] is True

        def test_take_photo(self, device_tool):
            """Test taking a photo."""
            with patch('tools.device_tool.capture_photo') as mock_capture:
                mock_capture.return_value = {
                    "success": True,
                    "photo_path": "/tmp/photo.jpg"
                }
                result = device_tool.take_photo(
                    session_id="camera_session"
                )
                assert result["success"] is True

        def test_start_video_recording(self, device_tool):
            """Test starting video recording."""
            with patch('tools.device_tool.start_recording') as mock_record:
                mock_record.return_value = {
                    "success": True,
                    "recording_id": "rec_123"
                }
                result = device_tool.start_recording(
                    session_id="camera_session"
                )
                assert result["success"] is True

        def test_stop_video_recording(self, device_tool):
            """Test stopping video recording."""
            with patch('tools.device_tool.stop_recording') as mock_record:
                mock_record.return_value = {
                    "success": True,
                    "video_path": "/tmp/video.mp4"
                }
                result = device_tool.stop_recording(
                    session_id="camera_session",
                    recording_id="rec_123"
                )
                assert result["success"] is True

        def test_check_camera_available(self, device_tool):
            """Test checking if camera is available."""
            with patch('tools.device_tool.check_available') as mock_check:
                mock_check.return_value = {"available": True}
                result = device_tool.check_capability("camera")
                assert result["available"] is True

    class TestDeviceToolLocation:
        """Tests for location capabilities."""

        def test_request_location_permission(self, device_tool):
            """Test requesting location permission."""
            with patch('tools.device_tool.request_permission') as mock_perm:
                mock_perm.return_value = {"granted": True}
                result = device_tool.request_permission(
                    session_id="location_session",
                    capability="location"
                )
                assert result["granted"] is True

        def test_get_current_location(self, device_tool):
            """Test getting current location."""
            with patch('tools.device_tool.get_location') as mock_location:
                mock_location.return_value = {
                    "latitude": 37.7749,
                    "longitude": -122.4194,
                    "accuracy": 10.5
                }
                result = device_tool.get_location(
                    session_id="location_session"
                )
                assert result["latitude"] == 37.7749

        def test_watch_location_changes(self, device_tool):
            """Test watching for location changes."""
            with patch('tools.device_tool.watch_location') as mock_watch:
                mock_watch.return_value = {
                    "success": True,
                    "watch_id": "watch_123"
                }
                result = device_tool.watch_location(
                    session_id="location_session"
                )
                assert result["success"] is True

        def test_clear_location_watch(self, device_tool):
            """Test clearing location watch."""
            with patch('tools.device_tool.clear_watch') as mock_clear:
                mock_clear.return_value = {"success": True}
                result = device_tool.clear_location_watch(
                    watch_id="watch_123"
                )
                assert result["success"] is True

    class TestDeviceToolNotifications:
        """Tests for notification capabilities."""

        def test_request_notification_permission(self, device_tool):
            """Test requesting notification permission."""
            with patch('tools.device_tool.request_permission') as mock_perm:
                mock_perm.return_value = {"granted": True}
                result = device_tool.request_permission(
                    session_id="notif_session",
                    capability="notifications"
                )
                assert result["granted"] is True

        def test_send_notification(self, device_tool):
            """Test sending a notification."""
            with patch('tools.device_tool.send_notification') as mock_send:
                mock_send.return_value = {"success": True}
                result = device_tool.send_notification(
                    session_id="notif_session",
                    title="Test Notification",
                    body="Test body content"
                )
                assert result["success"] is True

        def test_schedule_notification(self, device_tool):
            """Test scheduling a notification."""
            with patch('tools.device_tool.schedule_notification') as mock_sched:
                mock_sched.return_value = {
                    "success": True,
                    "notification_id": "notif_123"
                }
                result = device_tool.schedule_notification(
                    session_id="notif_session",
                    title="Scheduled Notification",
                    body="Scheduled content",
                    scheduled_time="2024-01-01T10:00:00Z"
                )
                assert result["success"] is True

        def test_cancel_notification(self, device_tool):
            """Test canceling a scheduled notification."""
            with patch('tools.device_tool.cancel_notification') as mock_cancel:
                mock_cancel.return_value = {"success": True}
                result = device_tool.cancel_notification(
                    notification_id="notif_123"
                )
                assert result["success"] is True

        def test_list_notifications(self, device_tool):
            """Test listing active notifications."""
            with patch('tools.device_tool.list_notifications') as mock_list:
                mock_list.return_value = [
                    {"id": "n1", "title": "Notification 1"},
                    {"id": "n2", "title": "Notification 2"}
                ]
                result = device_tool.list_notifications(
                    session_id="notif_session"
                )
                assert len(result) == 2

    class TestDeviceToolSystem:
        """Tests for system-level device capabilities."""

        def test_get_device_info(self, device_tool):
            """Test getting device information."""
            with patch('tools.device_tool.get_device_info') as mock_info:
                mock_info.return_value = {
                    "platform": "iOS",
                    "version": "15.0",
                    "model": "iPhone 13"
                }
                result = device_tool.get_device_info(
                    session_id="device_session"
                )
                assert result["platform"] == "iOS"

        def test_get_battery_level(self, device_tool):
            """Test getting battery level."""
            with patch('tools.device_tool.get_battery_level') as mock_battery:
                mock_battery.return_value = {
                    "level": 0.75,
                    "charging": False
                }
                result = device_tool.get_battery_level(
                    session_id="device_session"
                )
                assert result["level"] == 0.75

        def test_get_network_info(self, device_tool):
            """Test getting network information."""
            with patch('tools.device_tool.get_network_info') as mock_network:
                mock_network.return_value = {
                    "type": "wifi",
                    "connected": True,
                    "signal_strength": 0.85
                }
                result = device_tool.get_network_info(
                    session_id="device_session"
                )
                assert result["connected"] is True

        def test_vibrate_device(self, device_tool):
            """Test vibrating the device."""
            with patch('tools.device_tool.vibrate') as mock_vibrate:
                mock_vibrate.return_value = {"success": True}
                result = device_tool.vibrate(
                    session_id="device_session",
                    pattern="short"
                )
                assert result["success"] is True
    ```

    Target: 20-22 tests covering device capabilities (camera, location, notifications, system info).
  </action>
  <verify>
    - pytest tests/test_device_capabilities_integration.py -v passes
    - Tests import DeviceTool and call its methods
    - Coverage report shows device_tool.py coverage increased
    - At least 20 tests created
  </verify>
  <done>
    test_device_capabilities_integration.py exists with 20+ integration tests. All tests pass and device_tool.py coverage shows measurable increase.
  </done>
</task>

<task type="auto">
  <name>Task 3: Measure Coverage Improvement for Device System</name>
  <files>backend/tests/coverage_reports/metrics/phase_127_device_coverage.json</files>
  <action>
    Run coverage measurement for device system:

    ```bash
    cd /Users/rushiparikh/projects/atom/backend
    pytest tests/test_browser_tool_integration.py tests/test_device_capabilities_integration.py \
        --cov=tools.browser_tool \
        --cov=tools.device_tool \
        --cov-report=json:tests/coverage_reports/metrics/phase_127_device_coverage.json \
        --cov-report=term-missing:skip-covered \
        -v
    ```

    Generate comparison report:
    ```python
    import json
    from datetime import datetime, timezone

    with open('tests/coverage_reports/metrics/phase_127_baseline.json') as f:
        baseline = json.load(f)
    with open('tests/coverage_reports/metrics/phase_127_device_coverage.json') as f:
        device_cov = json.load(f)

    baseline_pct = baseline['files']['total']['summary']['percent_covered']
    device_pct = device_cov['files']['total']['summary']['percent_covered']
    improvement = device_pct - baseline_pct

    report = {
        "timestamp": datetime.now(timezone.utc).isoformat(),
        "plan": "127-12",
        "baseline_percentage": baseline_pct,
        "plan_percentage": device_pct,
        "improvement_percentage_points": round(improvement, 2),
        "tests_added": 42,
        "files_covered": ["browser_tool.py", "device_tool.py"],
        "target_80_gap_remaining": round(80.0 - device_pct, 2)
    }

    with open('tests/coverage_reports/metrics/phase_127_device_summary.json', 'w') as f:
        json.dump(report, f, indent=2)
    ```
  </action>
  <verify>
    - phase_127_device_coverage.json exists with coverage data
    - phase_127_device_summary.json shows improvement
    - Improvement is > 0
  </verify>
  <done>
    Device coverage report exists showing measurable improvement. Summary documents 42 tests added for browser_tool.py and device_tool.py.
  </done>
</task>

## Verification

After completing all tasks:

1. **Coverage increased** measurably from baseline
2. **42 integration tests created:**
   - 20-22 browser tool tests
   - 20-22 device capabilities tests
3. **Coverage improvement** target: +3-5 percentage points
4. **Device system** (browser_tool.py, device_tool.py) show measurable coverage increases

## Success Criteria

1. Overall backend coverage shows measurable increase
2. 42+ integration tests created
3. browser_tool.py and device_tool.py coverage increased
4. Improvement documented in coverage report

## Output

After completion, create `.planning/phases/127-backend-final-gap-closure/127-12-SUMMARY.md` with:
- Coverage improvement achieved
- Tests added (42 total)
- Device system covered
- Remaining gap to 80% target

---

*Gap closure plan for Phase 127 Blocker 1: Device system integration tests (42 tests)*
