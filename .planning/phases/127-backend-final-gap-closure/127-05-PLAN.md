---
phase: 127-backend-final-gap-closure
plan: 05
type: execute
wave: 3
depends_on: [127-02]
files_modified: [backend/tests/test_atom_agent_endpoints_coverage.py, backend/tests/coverage_reports/metrics/phase_127_endpoints_coverage.json]
autonomous: true

must_haves:
  truths:
    - "Integration tests added for core/atom_agent_endpoints.py"
    - "FastAPI agent endpoints tested with TestClient"
    - "Streaming responses tested for agent execution"
    - "Error handling tested for invalid requests"
    - "Coverage for atom_agent_endpoints.py increased measurably"
  artifacts:
    - path: "backend/tests/test_atom_agent_endpoints_coverage.py"
      provides: "Integration tests for core/atom_agent_endpoints.py"
      contains: "TestClient tests for agent endpoints, streaming, errors"
    - path: "backend/tests/coverage_reports/metrics/phase_127_endpoints_coverage.json"
      provides: "Coverage measurement after endpoints tests"
      contains: "before/after coverage comparison"
  key_links:
    - from: "test_atom_agent_endpoints_coverage.py"
      to: "core/atom_agent_endpoints.py"
      via: "FastAPI TestClient integration testing"
      pattern: "TestClient.*agent.*endpoints"
---

<objective>
Add integration tests for core/atom_agent_endpoints.py using FastAPI TestClient to cover agent chat, streaming, and error handling paths.

**Purpose:** atom_agent_endpoints.py is a high-impact file (736 lines, +368 potential coverage). Integration tests with TestClient efficiently test HTTP endpoints and their responses.

**Output:** Integration test file with TestClient + coverage measurement showing improvement
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/127-backend-final-gap-closure/127-02-SUMMARY.md
@.planning/phases/127-backend-final-gap-closure/127-RESEARCH.md
@backend/core/atom_agent_endpoints.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Integration Tests for Agent Endpoints</name>
  <files>backend/tests/test_atom_agent_endpoints_coverage.py</files>
  <action>
    Create integration tests for atom_agent_endpoints.py using FastAPI TestClient:

    ```python
    # backend/tests/test_atom_agent_endpoints_coverage.py
    """
    Integration coverage tests for core/atom_agent_endpoints.py.

    These tests use FastAPI TestClient to cover:
    - Agent chat endpoints
    - Streaming responses
    - Error handling
    - Request validation
    """
    import pytest
    import json
    from unittest.mock import Mock, AsyncMock, patch
    from fastapi.testclient import TestClient
    from fastapi import FastAPI

    # Try to import the app and endpoints
    try:
        from main import app
        APP_AVAILABLE = True
    except ImportError:
        # Create minimal app for testing
        from fastapi import FastAPI
        app = FastAPI()
        APP_AVAILABLE = False

    class TestAgentChatEndpointsCoverage:
        """Integration tests for agent chat endpoints."""

        def test_chat_endpoint_with_valid_request(self, test_client):
            """Test chat endpoint with valid request."""
            # Mock agent execution
            with patch('core.atom_agent_endpoints.execute_agent') as mock_execute:
                mock_execute.return_value = {
                    "response": "Test response",
                    "agent_id": "test-agent",
                    "execution_id": "exec-123",
                }

                response = test_client.post(
                    "/api/agent/chat",
                    json={
                        "agent_id": "test-agent",
                        "message": "Hello, agent!",
                        "context": {}
                    }
                )

                # Should succeed
                assert response.status_code in [200, 202]

        def test_chat_endpoint_with_streaming(self, test_client):
            """Test chat endpoint with streaming response."""
            with patch('core.atom_agent_endpoints.execute_agent_stream') as mock_stream:
                # Mock streaming response
                async def mock_gen():
                    yield "data: {\"chunk\": \"Hello\"}\n\n"
                    yield "data: {\"chunk\": \" world\"}\n\n"
                    yield "data: [DONE]\n\n"

                mock_stream.return_value = mock_gen()

                response = test_client.post(
                    "/api/agent/chat",
                    json={
                        "agent_id": "test-agent",
                        "message": "Hello!",
                        "stream": True
                    }
                )

                # Should accept streaming request
                assert response.status_code in [200, 202]

        def test_chat_endpoint_with_missing_agent_id(self, test_client):
            """Test chat endpoint with missing agent_id."""
            response = test_client.post(
                "/api/agent/chat",
                json={
                    "message": "Hello!"
                }
            )

            # Should return validation error
            assert response.status_code == 422

        def test_chat_endpoint_with_empty_message(self, test_client):
            """Test chat endpoint with empty message."""
            response = test_client.post(
                "/api/agent/chat",
                json={
                    "agent_id": "test-agent",
                    "message": ""
                }
            )

            # Should return validation error
            assert response.status_code == 422

        def test_chat_endpoint_with_invalid_context(self, test_client):
            """Test chat endpoint with invalid context type."""
            response = test_client.post(
                "/api/agent/chat",
                json={
                    "agent_id": "test-agent",
                    "message": "Hello!",
                    "context": "invalid"  # Should be object, not string
                }
            )

            # Should return validation error
            assert response.status_code == 422

    class TestAgentExecutionEndpointsCoverage:
        """Integration tests for agent execution endpoints."""

        def test_execute_agent_endpoint(self, test_client):
            """Test execute agent endpoint."""
            with patch('core.atom_agent_endpoints.execute_agent') as mock_execute:
                mock_execute.return_value = {
                    "execution_id": "exec-123",
                    "status": "completed",
                    "result": {"output": "test output"}
                }

                response = test_client.post(
                    "/api/agent/execute",
                    json={
                        "agent_id": "test-agent",
                        "action": "test_action",
                        "parameters": {"param1": "value1"}
                    }
                )

                assert response.status_code in [200, 202]

        def test_execute_agent_with_timeout(self, test_client):
            """Test execute agent with timeout parameter."""
            with patch('core.atom_agent_endpoints.execute_agent') as mock_execute:
                mock_execute.return_value = {
                    "execution_id": "exec-456",
                    "status": "completed"
                }

                response = test_client.post(
                    "/api/agent/execute",
                    json={
                        "agent_id": "test-agent",
                        "action": "test_action",
                        "timeout_seconds": 30
                    }
                )

                assert response.status_code in [200, 202]

        def test_get_execution_status(self, test_client):
            """Test get execution status endpoint."""
            with patch('core.atom_agent_endpoints.get_execution_status') as mock_status:
                mock_status.return_value = {
                    "execution_id": "exec-123",
                    "status": "running",
                    "progress": 0.5
                }

                response = test_client.get(
                    "/api/agent/execution/exec-123/status"
                )

                assert response.status_code == 200

        def test_cancel_execution(self, test_client):
            """Test cancel execution endpoint."""
            with patch('core.atom_agent_endpoints.cancel_execution') as mock_cancel:
                mock_cancel.return_value = {
                    "execution_id": "exec-123",
                    "status": "cancelled"
                }

                response = test_client.post(
                    "/api/agent/execution/exec-123/cancel"
                )

                assert response.status_code in [200, 202]

    class TestAgentManagementEndpointsCoverage:
        """Integration tests for agent management endpoints."""

        def test_list_agents(self, test_client):
            """Test list agents endpoint."""
            with patch('core.atom_agent_endpoints.list_agents') as mock_list:
                mock_list.return_value = {
                    "agents": [
                        {"id": "agent-1", "name": "Agent 1", "status": "active"},
                        {"id": "agent-2", "name": "Agent 2", "status": "active"}
                    ]
                }

                response = test_client.get("/api/agent")

                assert response.status_code == 200
                data = response.json()
                assert "agents" in data

        def test_get_agent_by_id(self, test_client):
            """Test get agent by ID endpoint."""
            with patch('core.atom_agent_endpoints.get_agent') as mock_get:
                mock_get.return_value = {
                    "id": "agent-1",
                    "name": "Agent 1",
                    "description": "Test agent",
                    "status": "active"
                }

                response = test_client.get("/api/agent/agent-1")

                assert response.status_code == 200

        def test_get_agent_not_found(self, test_client):
            """Test get agent with non-existent ID."""
            with patch('core.atom_agent_endpoints.get_agent') as mock_get:
                mock_get.side_effect = ValueError("Agent not found")

                response = test_client.get("/api/agent/nonexistent")

                assert response.status_code == 404

        def test_create_agent(self, test_client):
            """Test create agent endpoint."""
            with patch('core.atom_agent_endpoints.create_agent') as mock_create:
                mock_create.return_value = {
                    "id": "new-agent-1",
                    "name": "New Agent",
                    "status": "created"
                }

                response = test_client.post(
                    "/api/agent",
                    json={
                        "name": "New Agent",
                        "category": "testing",
                        "module_path": "test.module",
                        "class_name": "TestClass"
                    }
                )

                assert response.status_code in [200, 201]

        def test_update_agent(self, test_client):
            """Test update agent endpoint."""
            with patch('core.atom_agent_endpoints.update_agent') as mock_update:
                mock_update.return_value = {
                    "id": "agent-1",
                    "name": "Updated Agent",
                    "status": "active"
                }

                response = test_client.put(
                    "/api/agent/agent-1",
                    json={
                        "name": "Updated Agent",
                        "description": "Updated description"
                    }
                )

                assert response.status_code == 200

        def test_delete_agent(self, test_client):
            """Test delete agent endpoint."""
            with patch('core.atom_agent_endpoints.delete_agent') as mock_delete:
                mock_delete.return_value = {
                    "id": "agent-1",
                    "status": "deleted"
                }

                response = test_client.delete("/api/agent/agent-1")

                assert response.status_code in [200, 202]

    class TestAgentStreamingEndpointsCoverage:
        """Integration tests for streaming endpoints."""

        def test_sse_streaming_format(self, test_client):
            """Test SSE (Server-Sent Events) streaming format."""
            with patch('core.atom_agent_endpoints.stream_agent_response') as mock_stream:
                async def mock_gen():
                    yield "event: message\n"
                    yield "data: {\"text\": \"Hello\"}\n\n"
                    yield "event: done\n"
                    yield "data: [DONE]\n\n"

                mock_stream.return_value = mock_gen()

                response = test_client.get(
                    "/api/agent/stream/agent-123",
                    params={"message": "Hello!"}
                )

                assert response.status_code == 200

        def test_websocket_streaming(self, test_client):
            """Test WebSocket streaming for real-time updates."""
            # Note: TestClient doesn't support WebSocket, so we test the endpoint setup
            with patch('core.atom_agent_endpoints.websocket_endpoint') as mock_ws:
                mock_ws.return_value = None  # WebSocket handler

                # The WebSocket route should be registered
                # This test verifies the route exists
                routes = [route.path for route in app.routes]
                assert any("ws" in route or "websocket" in route.lower() for route in routes)

    class TestAgentErrorHandlingCoverage:
        """Integration tests for error handling."""

        def test_agent_execution_failure(self, test_client):
            """Test agent execution failure handling."""
            with patch('core.atom_agent_endpoints.execute_agent') as mock_execute:
                mock_execute.side_effect = Exception("Agent execution failed")

                response = test_client.post(
                    "/api/agent/execute",
                    json={
                        "agent_id": "failing-agent",
                        "action": "fail_action"
                    }
                )

                # Should handle error gracefully
                assert response.status_code in [500, 202]

        def test_invalid_agent_id_format(self, test_client):
            """Test with invalid agent ID format."""
            response = test_client.get("/api/agent/invalid-id-with spaces!")

            # Should return 400 or 404
            assert response.status_code in [400, 404]

        def test_rate_limiting(self, test_client):
            """Test rate limiting on agent endpoints."""
            with patch('core.atom_agent_endpoints.check_rate_limit') as mock_check:
                mock_check.return_value = False  # Rate limit exceeded

                response = test_client.post(
                    "/api/agent/chat",
                    json={"agent_id": "test", "message": "test"}
                )

                # Should return 429
                assert response.status_code == 429

        def test_missing_content_type(self, test_client):
            """Test request without proper content-type."""
            response = test_client.post(
                "/api/agent/chat",
                data="not json",
                headers={"Content-Type": "text/plain"}
            )

            # Should return error
            assert response.status_code == 415

    class TestAgentContextEndpointsCoverage:
        """Integration tests for context-related endpoints."""

        def test_set_agent_context(self, test_client):
            """Test setting agent context."""
            with patch('core.atom_agent_endpoints.set_agent_context') as mock_set:
                mock_set.return_value = {
                    "agent_id": "agent-1",
                    "context": {"key": "value"}
                }

                response = test_client.put(
                    "/api/agent/agent-1/context",
                    json={"key": "value", "user_id": "user-123"}
                )

                assert response.status_code == 200

        def test_get_agent_context(self, test_client):
            """Test getting agent context."""
            with patch('core.atom_agent_endpoints.get_agent_context') as mock_get:
                mock_get.return_value = {
                    "agent_id": "agent-1",
                    "context": {"key": "value", "session_id": "sess-1"}
                }

                response = test_client.get("/api/agent/agent-1/context")

                assert response.status_code == 200

        def test_clear_agent_context(self, test_client):
            """Test clearing agent context."""
            with patch('core.atom_agent_endpoints.clear_agent_context') as mock_clear:
                mock_clear.return_value = {
                    "agent_id": "agent-1",
                    "status": "cleared"
                }

                response = test_client.delete("/api/agent/agent-1/context")

                assert response.status_code == 200

    class TestAgentValidationEndpointsCoverage:
        """Integration tests for validation endpoints."""

        def test_validate_agent_request(self, test_client):
            """Test agent request validation."""
            with patch('core.atom_agent_endpoints.validate_request') as mock_validate:
                mock_validate.return_value = {
                    "valid": True,
                    "errors": []
                }

                response = test_client.post(
                    "/api/agent/validate",
                    json={
                        "agent_id": "agent-1",
                        "action": "test_action",
                        "parameters": {"test": "value"}
                    }
                )

                assert response.status_code == 200

        def test_validate_request_with_errors(self, test_client):
            """Test validation that returns errors."""
            with patch('core.atom_agent_endpoints.validate_request') as mock_validate:
                mock_validate.return_value = {
                    "valid": False,
                    "errors": [
                        {"field": "parameters", "message": "Missing required field"}
                    ]
                }

                response = test_client.post(
                    "/api/agent/validate",
                    json={"agent_id": "agent-1", "action": "test"}
                )

                assert response.status_code == 200
                data = response.json()
                assert data["valid"] is False
                assert len(data["errors"]) > 0

    class TestAgentMetricsEndpointsCoverage:
        """Integration tests for metrics endpoints."""

        def test_get_agent_metrics(self, test_client):
            """Test getting agent metrics."""
            with patch('core.atom_agent_endpoints.get_agent_metrics') as mock_metrics:
                mock_metrics.return_value = {
                    "agent_id": "agent-1",
                    "total_executions": 100,
                    "success_rate": 0.95,
                    "average_duration_ms": 1500
                }

                response = test_client.get("/api/agent/agent-1/metrics")

                assert response.status_code == 200

        def test_get_agent_history(self, test_client):
            """Test getting agent execution history."""
            with patch('core.atom_agent_endpoints.get_agent_history') as mock_history:
                mock_history.return_value = {
                    "agent_id": "agent-1",
                    "executions": [
                        {"id": "exec-1", "timestamp": "2024-01-01T00:00:00Z"},
                        {"id": "exec-2", "timestamp": "2024-01-02T00:00:00Z"}
                    ],
                    "total": 2
                }

                response = test_client.get("/api/agent/agent-1/history")

                assert response.status_code == 200
    ```
  </action>
  <verify>
    # Run tests and verify they pass
    cd /Users/rushiparikh/projects/atom/backend
    pytest tests/test_atom_agent_endpoints_coverage.py -v --tb=short
  </verify>
  <done>
    Integration test file created with 25+ test methods covering agent endpoints (chat, execute, management, streaming, errors, context, validation, metrics). All tests use TestClient.
  </done>
</task>

<task type="auto">
  <name>Task 2: Measure Coverage Improvement for atom_agent_endpoints.py</name>
  <files>backend/tests/coverage_reports/metrics/phase_127_endpoints_coverage.json</files>
  <action>
    Run coverage measurement specifically for atom_agent_endpoints.py:

    ```bash
    cd /Users/rushiparikh/projects/atom/backend
    pytest tests/test_atom_agent_endpoints_coverage.py \
        --cov=core.atom_agent_endpoints \
        --cov-report=json:tests/coverage_reports/metrics/phase_127_endpoints_coverage.json \
        --cov-report=term-missing:skip-covered \
        -v
    ```

    Then extract and compare coverage:
    ```python
    # backend/tests/scripts/compare_endpoints_coverage.py
    import json

    # Load baseline
    with open("tests/coverage_reports/metrics/phase_127_baseline.json") as f:
        baseline = json.load(f)

    # Load new endpoints coverage
    with open("tests/coverage_reports/metrics/phase_127_endpoints_coverage.json") as f:
        new_coverage = json.load(f)

    # Extract atom_agent_endpoints.py coverage
    baseline_file = "backend/core/atom_agent_endpoints.py"
    baseline_ep = baseline["files"].get(baseline_file, {})
    new_ep = new_coverage["files"].get(baseline_file, {})

    baseline_pct = baseline_ep.get("summary", {}).get("percent_covered", 0)
    new_pct = new_ep.get("summary", {}).get("percent_covered", 0)

    improvement = new_pct - baseline_pct

    result = {
        "file": "core/atom_agent_endpoints.py",
        "baseline_coverage": baseline_pct,
        "new_coverage": new_pct,
        "improvement": round(improvement, 2),
        "lines_covered_before": baseline_ep.get("summary", {}).get("covered_lines", 0),
        "lines_covered_after": new_ep.get("summary", {}).get("covered_lines", 0),
        "lines_added": new_ep.get("summary", {}).get("covered_lines", 0) - baseline_ep.get("summary", {}).get("covered_lines", 0),
    }

    # Write result
    with open("tests/coverage_reports/metrics/phase_127_endpoints_improvement.json", "w") as f:
        json.dump(result, f, indent=2)

    print(f"core/atom_agent_endpoints.py coverage: {baseline_pct:.2f}% → {new_pct:.2f}% ({improvement:+.2f} pp)")
    print(f"Lines covered: {result['lines_covered_before']} → {result['lines_covered_after']} ({result['lines_added']:+d})")
    ```

    Run comparison:
    ```bash
    python3 tests/scripts/compare_endpoints_coverage.py
    ```
  </action>
  <verify>
    # Verify improvement was recorded
    python3 -c "
    import json
    with open('backend/tests/coverage_reports/metrics/phase_127_endpoints_improvement.json') as f:
        data = json.load(f)
    assert data['improvement'] > 0
    print(f\"atom_agent_endpoints.py improved by {data['improvement']:.2f} percentage points\")
    "
  </verify>
  <done>
    Coverage JSON created showing before/after comparison for atom_agent_endpoints.py with measurable improvement (lines covered increased by X, percentage increased by Y pp).
  </done>
</task>

</tasks>

<verification>
After completing both tasks, verify:
1. test_atom_agent_endpoints_coverage.py exists with 25+ test methods
2. All tests pass: `pytest tests/test_atom_agent_endpoints_coverage.py -v`
3. Coverage for atom_agent_endpoints.py increased measurably
4. Improvement JSON documents the gain

Commands:
- `pytest backend/tests/test_atom_agent_endpoints_coverage.py -v`
- `python3 -c "import json; print(open('backend/tests/coverage_reports/metrics/phase_127_endpoints_improvement.json').read())"`
</verification>

<success_criteria>
- 25+ integration test methods added for atom_agent_endpoints.py
- All tests passing with TestClient
- atom_agent_endpoints.py coverage increased by at least 5 percentage points
- Improvement measured and documented
- Test file and metrics committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/127-backend-final-gap-closure/127-05-SUMMARY.md` with:
- Number of tests added for atom_agent_endpoints.py
- Coverage before and after
- Percentage point improvement
- Lines of code covered
</output>
