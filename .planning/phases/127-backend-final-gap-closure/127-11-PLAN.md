---
phase: 127-backend-final-gap-closure
plan: 11
type: execute
wave: 3
depends_on: ["127-10"]
files_modified:
  - backend/tests/test_canvas_tool_integration.py
  - backend/tests/test_canvas_routes_integration.py
  - backend/tests/coverage_reports/metrics/phase_127_canvas_coverage.json
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Coverage increases measurably from baseline"
    - "Integration tests for canvas tool increase coverage"
    - "Integration tests for canvas routes increase coverage"
  artifacts:
    - path: "backend/tests/test_canvas_tool_integration.py"
      provides: "Canvas tool integration tests"
      min_lines: 160
    - path: "backend/tests/test_canvas_routes_integration.py"
      provides: "Canvas routes integration tests"
      min_lines: 140
    - path: "backend/tests/coverage_reports/metrics/phase_127_canvas_coverage.json"
      provides: "Canvas coverage measurement after tests"
      contains: "before/after coverage comparison"
  key_links:
    - from: "test_canvas_tool_integration.py"
      to: "tools/canvas_tool.py"
      via: "CanvasTool class method calls"
      pattern: "canvas_tool.*CanvasTool.*(present|submit|update)"
    - from: "test_canvas_routes_integration.py"
      to: "api/canvas_routes.py"
      via: "FastAPI endpoint integration tests"
      pattern: "canvas_routes.*TestClient.*post.*get"
---

# Phase 127: Backend Final Gap Closure - Plan 11

## Objective

**Add 40-45 integration tests for Canvas system** (canvas tool and canvas routes) to continue closing the gap to 80% coverage target.

**Purpose:** tools/canvas_tool.py and api/canvas_routes.py are high-impact files. Integration tests that call actual class methods and API endpoints will measurably increase overall coverage.

**Output:** 40-45 integration tests, measurable coverage increase (+3-5 pp target), coverage report documenting improvement.

## Context

@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

@.planning/phases/127-backend-final-gap-closure/127-VERIFICATION.md
@.planning/phases/127-backend-final-gap-closure/127-10-SUMMARY.md

@backend/tools/canvas_tool.py
@api/canvas_routes.py
@backend/tests/conftest.py

## Gap Being Closed

**Blocker 1 [scope_sanity]:** Insufficient test coverage to achieve 80% target. This plan adds 40-45 more integration tests for canvas system.

## Tasks

<task type="auto">
  <name>Task 1: Create Canvas Tool Integration Tests (20-22 tests)</name>
  <files>backend/tests/test_canvas_tool_integration.py</files>
  <action>
    Create integration tests for tools/canvas_tool.py:

    ```python
    # backend/tests/test_canvas_tool_integration.py
    """
    Integration coverage tests for tools/canvas_tool.py.
    """
    import pytest
    from tools.canvas_tool import CanvasTool, CanvasComponent, CanvasFormData
    from core.models import SessionLocal, CanvasAudit

    @pytest.fixture
    def canvas_tool(db_session):
        return CanvasTool(db_session)

    @pytest.fixture
    def sample_canvas_data():
        return {
            "canvas_id": "test_canvas_123",
            "component_type": "chart",
            "title": "Test Chart",
            "data": {"labels": ["A", "B"], "values": [1, 2]}
        }

    class TestCanvasToolPresentation:
        """Tests for canvas presentation functionality."""

        def test_present_chart_canvas(self, canvas_tool, sample_canvas_data):
            """Test presenting a chart canvas."""
            result = canvas_tool.present(
                canvas_id=sample_canvas_data["canvas_id"],
                component_type="chart",
                user_id="test_user",
                data=sample_canvas_data
            )
            assert result["success"] is True
            assert result["canvas_id"] == sample_canvas_data["canvas_id"]

        def test_present_form_canvas(self, canvas_tool):
            """Test presenting a form canvas."""
            form_data = CanvasFormData(
                fields=[
                    {"name": "email", "type": "email", "required": True},
                    {"name": "message", "type": "text", "required": False}
                ]
            )
            result = canvas_tool.present(
                canvas_id="form_canvas_123",
                component_type="form",
                user_id="test_user",
                data={"form_data": form_data}
            )
            assert result["success"] is True

        def test_present_markdown_canvas(self, canvas_tool):
            """Test presenting a markdown canvas."""
            result = canvas_tool.present(
                canvas_id="markdown_canvas_123",
                component_type="markdown",
                user_id="test_user",
                data={"content": "# Test Markdown\n\nThis is a test."}
            )
            assert result["success"] is True

        def test_present_sheets_canvas(self, canvas_tool):
            """Test presenting a sheets canvas."""
            result = canvas_tool.present(
                canvas_id="sheets_canvas_123",
                component_type="sheets",
                user_id="test_user",
                data={"rows": [{"A": 1, "B": 2}], "columns": ["A", "B"]}
            )
            assert result["success"] is True

        def test_present_with_invalid_component_type(self, canvas_tool):
            """Test presenting with invalid component type."""
            with pytest.raises(ValueError):
                canvas_tool.present(
                    canvas_id="invalid_123",
                    component_type="invalid_type",
                    user_id="test_user",
                    data={}
                )

    class TestCanvasToolSubmission:
        """Tests for canvas form submission."""

        def test_submit_form_success(self, canvas_tool, db_session):
            """Test successful form submission."""
            # First create a form canvas
            canvas_tool.present(
                canvas_id="form_submit_123",
                component_type="form",
                user_id="test_user",
                data={"fields": [{"name": "email", "type": "email"}]}
            )

            # Submit the form
            result = canvas_tool.submit(
                canvas_id="form_submit_123",
                user_id="test_user",
                data={"email": "test@example.com"}
            )
            assert result["success"] is True

        def test_submit_form_validation(self, canvas_tool):
            """Test form validation on submission."""
            result = canvas_tool.submit(
                canvas_id="form_validation_123",
                user_id="test_user",
                data={"email": "invalid_email"},  # Should fail validation
                validate=True
            )
            # Either success with validation errors, or failure
            assert "validation" in result or result["success"] is False

        def test_submit_form_with_required_field(self, canvas_tool):
            """Test form submission with missing required field."""
            result = canvas_tool.submit(
                canvas_id="form_required_123",
                user_id="test_user",
                data={},  # Missing required fields
                validate=True
            )
            assert result["success"] is False

        def test_submit_nonexistent_form(self, canvas_tool):
            """Test submitting to nonexistent form."""
            result = canvas_tool.submit(
                canvas_id="nonexistent_form",
                user_id="test_user",
                data={"test": "data"}
            )
            assert result["success"] is False

    class TestCanvasToolUpdate:
        """Tests for canvas update functionality."""

        def test_update_canvas_data(self, canvas_tool, sample_canvas_data):
            """Test updating canvas data."""
            # Create canvas first
            canvas_tool.present(
                canvas_id=sample_canvas_data["canvas_id"],
                component_type="chart",
                user_id="test_user",
                data=sample_canvas_data
            )

            # Update it
            updated_data = sample_canvas_data.copy()
            updated_data["title"] = "Updated Chart"
            result = canvas_tool.update(
                canvas_id=sample_canvas_data["canvas_id"],
                user_id="test_user",
                data=updated_data
            )
            assert result["success"] is True

        def test_update_adds_component(self, canvas_tool):
            """Test updating canvas by adding component."""
            result = canvas_tool.update(
                canvas_id="update_components_123",
                user_id="test_user",
                action="add_component",
                data={"component": {"type": "button", "label": "Click"}}
            )
            assert result["success"] is True

        def test_update_removes_component(self, canvas_tool):
            """Test updating canvas by removing component."""
            result = canvas_tool.update(
                canvas_id="remove_component_123",
                user_id="test_user",
                action="remove_component",
                data={"component_id": "comp_123"}
            )
            assert result["success"] is True

    class TestCanvasToolRetrieval:
        """Tests for canvas data retrieval."""

        def test_get_canvas_data(self, canvas_tool, sample_canvas_data):
            """Test retrieving canvas data."""
            # Create canvas first
            canvas_tool.present(
                canvas_id=sample_canvas_data["canvas_id"],
                component_type="chart",
                user_id="test_user",
                data=sample_canvas_data
            )

            # Get it
            result = canvas_tool.get(canvas_id=sample_canvas_data["canvas_id"])
            assert result is not None
            assert result["canvas_id"] == sample_canvas_data["canvas_id"]

        def test_list_user_canvases(self, canvas_tool):
            """Test listing all canvases for a user."""
            # Create multiple canvases
            for i in range(3):
                canvas_tool.present(
                    canvas_id=f"user_canvas_{i}",
                    component_type="chart",
                    user_id="list_test_user",
                    data={"test": i}
                )

            # List them
            result = canvas_tool.list(user_id="list_test_user")
            assert len(result) >= 3

        def test_get_canvas_audit_log(self, canvas_tool, sample_canvas_data):
            """Test retrieving canvas audit log."""
            canvas_tool.present(
                canvas_id=sample_canvas_data["canvas_id"],
                component_type="chart",
                user_id="test_user",
                data=sample_canvas_data
            )

            audit_log = canvas_tool.get_audit_log(
                canvas_id=sample_canvas_data["canvas_id"]
            )
            assert len(audit_log) >= 1
            assert audit_log[0]["action"] == "present"

    class TestCanvasToolDeletion:
        """Tests for canvas deletion."""

        def test_close_canvas(self, canvas_tool, sample_canvas_data):
            """Test closing a canvas."""
            canvas_tool.present(
                canvas_id=sample_canvas_data["canvas_id"],
                component_type="chart",
                user_id="test_user",
                data=sample_canvas_data
            )

            result = canvas_tool.close(
                canvas_id=sample_canvas_data["canvas_id"],
                user_id="test_user"
            )
            assert result["success"] is True

        def test_delete_canvas(self, canvas_tool, db_session):
            """Test deleting a canvas permanently."""
            canvas_tool.present(
                canvas_id="delete_canvas_123",
                component_type="chart",
                user_id="test_user",
                data={}
            )

            result = canvas_tool.delete(
                canvas_id="delete_canvas_123",
                user_id="test_user"
            )
            assert result["success"] is True

            # Verify deletion
            retrieved = canvas_tool.get(canvas_id="delete_canvas_123")
            assert retrieved is None or retrieved.get("deleted") is True
    ```

    Target: 20-22 tests covering canvas presentation, submission, update, retrieval, and deletion.
  </action>
  <verify>
    - pytest tests/test_canvas_tool_integration.py -v passes
    - Tests import CanvasTool and call its methods
    - Coverage report shows canvas_tool.py coverage increased
    - At least 20 tests created
  </verify>
  <done>
    test_canvas_tool_integration.py exists with 20+ integration tests. All tests pass and canvas_tool.py coverage shows measurable increase.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Canvas Routes Integration Tests (20-22 tests)</name>
  <files>backend/tests/test_canvas_routes_integration.py</files>
  <action>
    Create integration tests for api/canvas_routes.py using FastAPI TestClient:

    ```python
    # backend/tests/test_canvas_routes_integration.py
    """
    Integration coverage tests for api/canvas_routes.py.
    """
    import pytest
    from fastapi.testclient import TestClient
    from unittest.mock import Mock, patch
    from main import app

    @pytest.fixture
    def test_client():
        return TestClient(app)

    class TestCanvasPresentationEndpoints:
        """Tests for canvas presentation endpoints."""

        def test_present_canvas(self, test_client):
            """Test presenting a canvas."""
            with patch('api.canvas_routes.canvas_tool.present') as mock_present:
                mock_present.return_value = {"success": True, "canvas_id": "test_123"}
                response = test_client.post(
                    "/api/canvas/present",
                    json={
                        "canvas_id": "test_123",
                        "component_type": "chart",
                        "user_id": "test_user",
                        "data": {"test": "data"}
                    }
                )
                assert response.status_code == 200

        def test_present_canvas_with_validation_error(self, test_client):
            """Test presenting canvas with validation error."""
            with patch('api.canvas_routes.canvas_tool.present') as mock_present:
                mock_present.side_effect = ValueError("Invalid component type")
                response = test_client.post(
                    "/api/canvas/present",
                    json={"component_type": "invalid"}
                )
                assert response.status_code == 422

        def test_present_missing_required_field(self, test_client):
            """Test presenting with missing required field."""
            response = test_client.post(
                "/api/canvas/present",
                json={"component_type": "chart"}  # Missing canvas_id
            )
            assert response.status_code == 422

    class TestCanvasFormEndpoints:
        """Tests for canvas form endpoints."""

        def test_submit_form(self, test_client):
            """Test submitting form data."""
            with patch('api.canvas_routes.canvas_tool.submit') as mock_submit:
                mock_submit.return_value = {"success": True, "submission_id": "sub_123"}
                response = test_client.post(
                    "/api/canvas/submit",
                    json={
                        "canvas_id": "form_123",
                        "user_id": "test_user",
                        "data": {"email": "test@example.com"}
                    }
                )
                assert response.status_code == 200

        def test_submit_form_with_files(self, test_client):
            """Test submitting form with file uploads."""
            with patch('api.canvas_routes.canvas_tool.submit') as mock_submit:
                mock_submit.return_value = {"success": True}
                response = test_client.post(
                    "/api/canvas/submit",
                    data={
                        "canvas_id": "form_123",
                        "user_id": "test_user",
                        "file": ("test.txt", "file content", "text/plain")
                    }
                )
                assert response.status_code in [200, 201]

        def test_validate_form_field(self, test_client):
            """Test validating individual form field."""
            with patch('api.canvas_routes.canvas_tool.validate_field') as mock_validate:
                mock_validate.return_value = {"valid": True}
                response = test_client.post(
                    "/api/canvas/validate",
                    json={
                        "canvas_id": "form_123",
                        "field_name": "email",
                        "value": "test@example.com"
                    }
                )
                assert response.status_code == 200

    class TestCanvasUpdateEndpoints:
        """Tests for canvas update endpoints."""

        def test_update_canvas(self, test_client):
            """Test updating canvas data."""
            with patch('api.canvas_routes.canvas_tool.update') as mock_update:
                mock_update.return_value = {"success": True}
                response = test_client.put(
                    "/api/canvas/test_123",
                    json={"title": "Updated Title"}
                )
                assert response.status_code == 200

        def test_update_canvas_component(self, test_client):
            """Test updating specific canvas component."""
            with patch('api.canvas_routes.canvas_tool.update') as mock_update:
                mock_update.return_value = {"success": True}
                response = test_client.patch(
                    "/api/canvas/test_123/components/comp_123",
                    json={"label": "Updated Label"}
                )
                assert response.status_code == 200

        def test_batch_update_canvases(self, test_client):
            """Test batch updating multiple canvases."""
            with patch('api.canvas_routes.canvas_tool.batch_update') as mock_batch:
                mock_batch.return_value = {"updated": 3}
                response = test_client.post(
                    "/api/canvas/batch-update",
                    json={
                        "canvas_ids": ["c1", "c2", "c3"],
                        "updates": {"theme": "dark"}
                    }
                )
                assert response.status_code == 200

    class TestCanvasRetrievalEndpoints:
        """Tests for canvas retrieval endpoints."""

        def test_get_canvas(self, test_client):
            """Test getting canvas by ID."""
            with patch('api.canvas_routes.canvas_tool.get') as mock_get:
                mock_get.return_value = {
                    "canvas_id": "test_123",
                    "component_type": "chart"
                }
                response = test_client.get("/api/canvas/test_123")
                assert response.status_code == 200

        def test_list_canvases(self, test_client):
            """Test listing canvases."""
            with patch('api.canvas_routes.canvas_tool.list') as mock_list:
                mock_list.return_value = [
                    {"canvas_id": "c1", "title": "Canvas 1"},
                    {"canvas_id": "c2", "title": "Canvas 2"}
                ]
                response = test_client.get("/api/canvas?user_id=test_user")
                assert response.status_code == 200
                data = response.json()
                assert len(data) == 2

        def test_list_canvases_with_filters(self, test_client):
            """Test listing canvases with filters."""
            with patch('api.canvas_routes.canvas_tool.list') as mock_list:
                mock_list.return_value = []
                response = test_client.get("/api/canvas?user_id=test_user&type=chart")
                assert response.status_code == 200

        def test_get_canvas_audit_log(self, test_client):
            """Test getting canvas audit log."""
            with patch('api.canvas_routes.canvas_tool.get_audit_log') as mock_audit:
                mock_audit.return_value = [
                    {"action": "present", "timestamp": "2024-01-01T00:00:00Z"}
                ]
                response = test_client.get("/api/canvas/test_123/audit")
                assert response.status_code == 200

    class TestCanvasActionEndpoints:
        """Tests for canvas action endpoints."""

        def test_close_canvas(self, test_client):
            """Test closing a canvas."""
            with patch('api.canvas_routes.canvas_tool.close') as mock_close:
                mock_close.return_value = {"success": True}
                response = test_client.post("/api/canvas/test_123/close")
                assert response.status_code == 200

        def test_delete_canvas(self, test_client):
            """Test deleting a canvas."""
            with patch('api.canvas_routes.canvas_tool.delete') as mock_delete:
                mock_delete.return_value = {"success": True}
                response = test_client.delete("/api/canvas/test_123")
                assert response.status_code in [200, 204]

        def test_clone_canvas(self, test_client):
            """Test cloning a canvas."""
            with patch('api.canvas_routes.canvas_tool.clone') as mock_clone:
                mock_clone.return_value = {
                    "success": True,
                    "new_canvas_id": "cloned_123"
                }
                response = test_client.post("/api/canvas/test_123/clone")
                assert response.status_code == 201

        def test_export_canvas(self, test_client):
            """Test exporting canvas data."""
            with patch('api.canvas_routes.canvas_tool.export') as mock_export:
                mock_export.return_value = {
                    "data": "exported_data",
                    "format": "json"
                }
                response = test_client.get("/api/canvas/test_123/export?format=json")
                assert response.status_code == 200

    class TestCanvasErrorHandling:
        """Tests for endpoint error handling."""

        def test_handle_canvas_not_found(self, test_client):
            """Test handling of canvas not found error."""
            with patch('api.canvas_routes.canvas_tool.get', side_effect=ValueError("Canvas not found")):
                response = test_client.get("/api/canvas/nonexistent")
                assert response.status_code == 404

        def test_handle_unauthorized_access(self, test_client):
            """Test handling of unauthorized access."""
            from api.canvas_routes import CanvasAccessDeniedError
            with patch('api.canvas_routes.canvas_tool.get', side_effect=CanvasAccessDeniedError("Unauthorized")):
                response = test_client.get("/api/canvas/restricted_canvas")
                assert response.status_code == 403

        def test_handle_invalid_json(self, test_client):
            """Test handling of invalid JSON in request."""
            response = test_client.post(
                "/api/canvas/present",
                data="invalid json",
                headers={"Content-Type": "application/json"}
            )
            assert response.status_code == 422
    ```

    Target: 20-22 tests covering canvas API endpoints (present, submit, update, retrieval, actions, errors).
  </action>
  <verify>
    - pytest tests/test_canvas_routes_integration.py -v passes
    - Tests use TestClient for canvas endpoints
    - Coverage report shows canvas_routes.py coverage increased
    - At least 20 tests created
  </verify>
  <done>
    test_canvas_routes_integration.py exists with 20+ integration tests. All tests pass and canvas_routes.py coverage shows measurable increase.
  </done>
</task>

<task type="auto">
  <name>Task 3: Measure Coverage Improvement for Canvas System</name>
  <files>backend/tests/coverage_reports/metrics/phase_127_canvas_coverage.json</files>
  <action>
    Run coverage measurement for canvas system:

    ```bash
    cd /Users/rushiparikh/projects/atom/backend
    pytest tests/test_canvas_tool_integration.py tests/test_canvas_routes_integration.py \
        --cov=tools.canvas_tool \
        --cov=api.canvas_routes \
        --cov-report=json:tests/coverage_reports/metrics/phase_127_canvas_coverage.json \
        --cov-report=term-missing:skip-covered \
        -v
    ```

    Generate comparison report:
    ```python
    import json
    from datetime import datetime, timezone

    with open('tests/coverage_reports/metrics/phase_127_baseline.json') as f:
        baseline = json.load(f)
    with open('tests/coverage_reports/metrics/phase_127_canvas_coverage.json') as f:
        canvas_cov = json.load(f)

    baseline_pct = baseline['files']['total']['summary']['percent_covered']
    canvas_pct = canvas_cov['files']['total']['summary']['percent_covered']
    improvement = canvas_pct - baseline_pct

    report = {
        "timestamp": datetime.now(timezone.utc).isoformat(),
        "plan": "127-11",
        "baseline_percentage": baseline_pct,
        "plan_percentage": canvas_pct,
        "improvement_percentage_points": round(improvement, 2),
        "tests_added": 42,
        "files_covered": ["canvas_tool.py", "canvas_routes.py"],
        "target_80_gap_remaining": round(80.0 - canvas_pct, 2)
    }

    with open('tests/coverage_reports/metrics/phase_127_canvas_summary.json', 'w') as f:
        json.dump(report, f, indent=2)
    ```
  </action>
  <verify>
    - phase_127_canvas_coverage.json exists with coverage data
    - phase_127_canvas_summary.json shows improvement
    - Improvement is > 0
  </verify>
  <done>
    Canvas coverage report exists showing measurable improvement. Summary documents 42 tests added for canvas_tool.py and canvas_routes.py.
  </done>
</task>

## Verification

After completing all tasks:

1. **Coverage increased** measurably from baseline
2. **42 integration tests created:**
   - 20-22 canvas tool tests
   - 20-22 canvas routes tests
3. **Coverage improvement** target: +3-5 percentage points
4. **Canvas system** (canvas_tool.py, canvas_routes.py) show measurable coverage increases

## Success Criteria

1. Overall backend coverage shows measurable increase
2. 42+ integration tests created
3. canvas_tool.py and canvas_routes.py coverage increased
4. Improvement documented in coverage report

## Output

After completion, create `.planning/phases/127-backend-final-gap-closure/127-11-SUMMARY.md` with:
- Coverage improvement achieved
- Tests added (42 total)
- Canvas system covered
- Remaining gap to 80% target

---

*Gap closure plan for Phase 127 Blocker 1: Canvas system integration tests (42 tests)*
