---
phase: 138-mobile-state-management-testing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/__tests__/integration/stateHydration.test.tsx
  - mobile/src/__tests__/helpers/storageTestHelpers.ts
autonomous: true

must_haves:
  truths:
    - "Auth state hydrates from SecureStore and AsyncStorage on app startup"
    - "Device state hydrates from AsyncStorage on app startup"
    - "WebSocket state does not persist (fresh connection on each app start)"
    - "State hydration happens correctly when tokens exist but are expired"
    - "State hydration handles corrupted storage data gracefully"
  artifacts:
    - path: mobile/src/__tests__/integration/stateHydration.test.tsx
      provides: "State hydration integration tests"
      min_lines: 500
    - path: mobile/src/__tests__/helpers/storageTestHelpers.ts
      provides: "Storage test helper utilities"
      min_lines: 200
  key_links:
    - from: mobile/src/__tests__/integration/stateHydration.test.tsx
      to: mobile/src/contexts/AuthContext.tsx
      via: "AuthProvider mounting with stored tokens"
      pattern: "stateHydration tests AuthContext initialization"
    - from: mobile/src/__tests__/integration/stateHydration.test.tsx
      to: mobile/src/contexts/DeviceContext.tsx
      via: "DeviceProvider mounting with stored state"
      pattern: "stateHydration tests DeviceContext initialization"
    - from: mobile/src/__tests__/helpers/storageTestHelpers.ts
      to: mobile/src/__tests__/integration/stateHydration.test.tsx
      via: "import { mockStorageState, clearAllStorage }"
      pattern: "test helpers imported by integration tests"
---

<objective>
Create state hydration integration tests to verify state restoration from persistent storage on app startup.

**Purpose:** State hydration is critical for user experience - users should stay logged in and device state should persist across app restarts. This plan tests the complete hydration flow.

**Output:** 500+ line integration test file with storage helper utilities.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-mobile-state-management-testing/138-RESEARCH.md
@mobile/src/contexts/AuthContext.tsx
@mobile/src/contexts/DeviceContext.tsx
@mobile/src/contexts/WebSocketContext.tsx
@mobile/src/__tests__/contexts/AuthContext.test.tsx
@mobile/jest.setup.js
</context>

<tasks>

<task type="auto">
  <name>Create storage test helper utilities</name>
  <files>mobile/src/__tests__/helpers/storageTestHelpers.ts</files>
  <action>
Create storage test helper file at mobile/src/__tests__/helpers/storageTestHelpers.ts (200+ lines):

1. **Storage state helpers** (80 lines):
   - mockSecureStoreState(tokens: Partial<SecureStoreState>): Mock SecureStore items
   - mockAsyncStorageState(items: Record<string, string>): Mock AsyncStorage items
   - mockMMKVState(items: Record<string, any>): Mock MMKV items
   - getAllStoredState(): Get all currently stored state
   - clearAllStorage(): Clear all storage (SecureStore, AsyncStorage, MMKV)
   - createMockAuthState(overrides?): Create valid mock auth state
   - createMockDeviceState(overrides?): Create valid mock device state

2. **Token helpers** (50 lines):
   - createValidToken(expiryHours?: number): Create valid JWT mock
   - createExpiredToken(): Create expired JWT mock
   - createExpiringSoonToken(): Create token expiring in <5 minutes
   - parseTokenExpiry(token): Extract expiry from token mock

3. **User data helpers** (40 lines):
   - createMockUser(overrides?): Create mock user object
   - createMockDevice(overrides?): Create mock device info
   - createMockCapabilities(overrides?): Create mock capabilities

4. **Scenario helpers** (30 lines):
   - setupAuthenticatedState(): Setup full authenticated user state
   - setupUnauthenticatedState(): Clear all auth state
   - setupRegisteredDevice(): Setup registered device state
   - setupPartialAuthState(): Setup partial/expired auth state

Export all helpers for use in integration tests.
  </action>
  <verify>
File created with 15+ exports for storage mocking and test scenarios
  </verify>
  <done>
Storage helper utilities file exists with comprehensive mocking functions
  </done>
</task>

<task type="auto">
  <name>Create AuthContext hydration tests</name>
  <files>mobile/src/__tests__/integration/stateHydration.test.tsx</files>
  <action>
Create state hydration integration test file at mobile/src/__tests__/integration/stateHydration.test.tsx with AuthContext hydration tests (250+ lines):

1. **Setup** (new integration test pattern):
   - Import storage helpers from storageTestHelpers.ts
   - Import AuthProvider, useAuth
   - Import all three providers (Auth, Device, WebSocket) for full integration
   - Use render() from @testing-library/react-native

2. **AuthContext hydration tests**:
   - test('should hydrate authentication state from valid tokens')
     - Setup: Create valid access token and user data in storage
     - Action: Mount AuthProvider
     - Verify: isAuthenticated=true, user data loaded

   - test('should refresh token when expiring soon (<5 minutes)')
     - Setup: Create token expiring in 4 minutes
     - Action: Mount AuthProvider with mock refresh API
     - Verify: Refresh API called, new token stored

   - test('should clear state when token expired and refresh fails')
     - Setup: Create expired token
     - Action: Mount AuthProvider with failing refresh API
     - Verify: isAuthenticated=false, tokens cleared

   - test('should handle missing SecureStore tokens gracefully')
     - Setup: Only user data in AsyncStorage, no tokens
     - Action: Mount AuthProvider
     - Verify: isAuthenticated=false, no errors thrown

   - test('should handle corrupted user data gracefully')
     - Setup: Invalid JSON in atom_user_data
     - Action: Mount AuthProvider
     - Verify: isAuthenticated=false or handled gracefully

   - test('should load device info from storage')
     - Setup: Device ID in AsyncStorage
     - Action: Mount AuthProvider
     - Verify: deviceInfo loaded with stored device ID

   - test('should generate new device ID if none stored')
     - Setup: Empty AsyncStorage
     - Action: Mount AuthProvider
     - Verify: New device ID generated and stored

   - test('should complete hydration before setting isLoading=false')
     - Setup: Valid tokens in storage
     - Action: Mount AuthProvider
     - Verify: isLoading transitions true→false, isAuthenticated becomes true

3. **Hydration timing tests**:
   - test('should not block UI render during hydration')
   - test('should show loading state during hydration')

Use act() and waitFor() for async hydration flows.
  </action>
  <verify>
npm test -- stateHydration.test.tsx passes with 10+ AuthContext hydration tests
  </verify>
  <done>
10+ tests passing for AuthContext state hydration scenarios
  </done>
</task>

<task type="auto">
  <name>Create DeviceContext and multi-provider hydration tests</name>
  <files>mobile/src/__tests__/integration/stateHydration.test.tsx</files>
  <action>
Add to stateHydration.test.tsx with DeviceContext and multi-provider tests (250+ lines):

1. **DeviceContext hydration tests**:
   - test('should hydrate device state from AsyncStorage')
     - Setup: Device ID, token, capabilities, last sync in storage
     - Action: Mount DeviceProvider
     - Verify: All device state loaded correctly

   - test('should initialize with default state when no storage')
     - Setup: Empty AsyncStorage
     - Action: Mount DeviceProvider
     - Verify: Default state (not registered, no capabilities)

   - test('should parse capabilities from JSON storage')
     - Setup: Capabilities JSON string in storage
     - Action: Mount DeviceProvider
     - Verify: Capabilities object parsed correctly

   - test('should parse last sync date from ISO string')
     - Setup: last_sync as ISO string in storage
     - Action: Mount DeviceProvider
     - Verify: Date object created from ISO string

   - test('should handle corrupted device state gracefully')
     - Setup: Invalid JSON for capabilities
     - Action: Mount DeviceProvider
     - Verify: Default capabilities used, no crash

2. **Multi-provider hydration tests**:
   - test('should hydrate Auth and Device states in correct order')
     - Setup: Valid auth and device state in storage
     - Action: Mount AuthProvider > DeviceProvider
     - Verify: Both states loaded, auth available to device

   - test('should handle auth available but device unregistered')
     - Setup: Auth tokens but no device state
     - Action: Mount both providers
     - Verify: Authenticated but device not registered

   - test('should handle device registered but auth missing')
     - Setup: Device state but no auth tokens
     - Action: Mount both providers
     - Verify: Not authenticated, device state loaded but registration flag checked

3. **WebSocketContext hydration tests**:
   - test('should not persist WebSocket state across app restarts')
     - Setup: Previous WebSocket connection (no storage expected)
     - Action: Mount WebSocketProvider
     - Verify: Fresh connection, no persisted state

   - test('should re-join rooms after reconnection')
     - Setup: Rooms in AsyncStorage from previous session
     - Action: Mount WebSocketProvider with mock Socket.IO
     - Verify: Rooms re-joined after connection

4. **Edge case hydration tests**:
   - test('should handle partial storage (some keys missing)')
   - test('should handle storage access errors gracefully')
   - test('should handle rapid provider mount/unmount cycles')
   - test('should handle concurrent provider initialization')

Create test scenarios that match real-world app startup flows.
  </action>
  <verify>
npm test -- stateHydration.test.tsx passes with 15+ additional tests (25+ total)
  </verify>
  <done>
25+ tests passing for complete state hydration across all providers
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Run state hydration tests:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- stateHydration.test.tsx
   ```
   Expected: 25+ tests passing

2. Run all integration tests:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- integration/
   ```
   Expected: All integration tests passing

3. Check hydration coverage:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- --coverage --collectCoverageFrom="contexts/*.tsx"
   ```
   Expected: Increased coverage for initialization flows

4. Verify storage helpers:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- helpers/storageTestHelpers.test.ts
   ```
   Expected: Helper utilities tested and working
</verification>

<success_criteria>
- stateHydration.test.tsx created with 500+ lines
- storageTestHelpers.ts created with 200+ lines
- 25+ tests covering all hydration scenarios
- 100% test pass rate
- All context providers tested for hydration
- Edge cases (corrupted data, partial storage) handled
</success_criteria>

<output>
After completion, create `.planning/phases/138-mobile-state-management-testing/138-03-SUMMARY.md` with:
- Tests created count
- Hydration scenarios covered
- Any timing/async issues encountered
</output>
