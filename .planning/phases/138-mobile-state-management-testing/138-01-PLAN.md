---
phase: 138-mobile-state-management-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/__tests__/contexts/WebSocketContext.test.tsx
  - mobile/src/__tests__/helpers/websocketMocks.ts
autonomous: true

must_haves:
  truths:
    - "WebSocket connection states are tested (connecting, connected, disconnected, error)"
    - "WebSocket reconnection logic is tested with retry attempts and max limits"
    - "WebSocket streaming messages are tested with chunk, complete, and error callbacks"
    - "WebSocket room joining/leaving is tested with AsyncStorage persistence"
    - "WebSocket heartbeat/connection quality is tested with latency measurement"
  artifacts:
    - path: mobile/src/__tests__/contexts/WebSocketContext.test.tsx
      provides: "WebSocket context provider tests"
      min_lines: 600
    - path: mobile/src/__tests__/helpers/websocketMocks.ts
      provides: "Socket.IO mock utilities"
      min_lines: 200
  key_links:
    - from: mobile/src/__tests__/contexts/WebSocketContext.test.tsx
      to: mobile/src/contexts/WebSocketContext.tsx
      via: "import { WebSocketProvider, useWebSocket }"
      pattern: "WebSocketContext.test.tsx imports WebSocketContext"
    - from: mobile/src/__tests__/helpers/websocketMocks.ts
      to: socket.io-client
      via: "jest.mock"
      pattern: "mock socket.io-client module"
---

<objective>
Create comprehensive WebSocketContext tests with connection state management, reconnection logic, streaming callbacks, and room persistence.

**Purpose:** WebSocketContext is the third context provider (after AuthContext and DeviceContext) but currently lacks comprehensive test coverage. This plan establishes testing for real-time communication features.

**Output:** 600+ line WebSocketContext test file with Socket.IO mock utilities.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-mobile-state-management-testing/138-RESEARCH.md
@mobile/src/contexts/WebSocketContext.tsx
@mobile/src/__tests__/contexts/AuthContext.test.tsx
@mobile/src/__tests__/contexts/DeviceContext.test.tsx
@mobile/jest.setup.js
</context>

<tasks>

<task type="auto">
  <name>Create WebSocket mock utilities in websocketMocks.ts</name>
  <files>mobile/src/__tests__/helpers/websocketMocks.ts</files>
  <action>
Create Socket.IO mock utilities file at mobile/src/__tests__/helpers/websocketMocks.ts with:

1. **MockSocket class** (150 lines):
   - Constructor with url and options
   - Properties: connected, id, rooms
   - Methods: connect(), disconnect(), emit(), on(), off(), once()
   - Event handlers storage in Map structure
   - Automatic 'connect' event simulation after connection

2. **Socket.IO client mock** (50 lines):
   - jest.mock('socket.io-client') with factory function
   - Returns MockSocket instances
   - Supports auth option passing
   - Exports: createMockSocket(), getMockSocket(), resetMockSockets()

3. **Helper functions**:
   - simulateConnection(socketId): Trigger connect event
   - simulateDisconnection(socketId, reason): Trigger disconnect
   - simulateEvent(socketId, event, data): Emit event to listeners
   - getAllEmittedEvents(): Get all emitted events for verification

Follow patterns from existing test files (AuthContext.test.tsx, DeviceContext.test.tsx) for mock structure.
  </action>
  <verify>
File created with exports: MockSocket, createMockSocket, getMockSocket, resetMockSockets, simulateConnection, simulateDisconnection, simulateEvent, getAllEmittedEvents
  </verify>
  <done>
Mock utilities file exists with 8+ exports for Socket.IO testing
  </done>
</task>

<task type="auto">
  <name>Create WebSocketContext connection state tests</name>
  <files>mobile/src/__tests__/contexts/WebSocketContext.test.tsx</files>
  <action>
Create WebSocketContext test file at mobile/src/__tests__/contexts/WebSocketContext.test.tsx with connection state tests (200+ lines):

1. **Setup** (following AuthContext.test.tsx pattern):
   - Import WebSocketProvider, useWebSocket
   - Mock fetch for auth token
   - Mock AsyncStorage for room persistence
   - Mock expo-constants for SOCKET_URL

2. **TestComponent**:
   - Consumes useWebSocket hook
   - Renders connection state testIDs: isConnected, isConnecting, connectionError, connectionQuality

3. **Connection state tests** (describe block):
   - test('should initialize with disconnected state')
   - test('should connect when authenticated')
   - test('should set connecting state during connection')
   - test('should update to connected after successful connection')
   - test('should handle connection errors')
   - test('should disconnect on logout')
   - test('should update connection quality based on latency')

4. **Reconnection tests**:
   - test('should attempt reconnection after disconnect')
   - test('should respect MAX_RECONNECT_ATTEMPTS limit')
   - test('should increment reconnect attempts counter')
   - test('should stop reconnecting after max attempts')
   - test('should reconnect after successful reconnection')

Use act() for state updates and waitFor() for async assertions.
  </action>
  <verify>
npm test -- WebSocketContext.test.tsx passes with 12+ connection state tests
  </verify>
  <done>
12 tests passing for connection state, reconnection logic, and error handling
  </done>
</task>

<task type="auto">
  <name>Create WebSocket streaming and room tests</name>
  <files>mobile/src/__tests__/contexts/WebSocketContext.test.tsx</files>
  <action>
Add to WebSocketContext.test.tsx with streaming and room tests (250+ lines):

1. **Streaming message tests**:
   - test('should send streaming message with callbacks')
   - test('should call onChunk callback when agent:streaming received')
   - test('should call onComplete callback when agent:streaming_complete received')
   - test('should call onError callback when agent:streaming_error received')
   - test('should return unsubscribe function from sendStreamingMessage')
   - test('should clean up callbacks after completion')
   - test('should queue messages when not connected')

2. **Stream subscription tests**:
   - test('should subscribe to stream updates')
   - test('should receive stream chunks via subscription')
   - test('should unsubscribe from stream updates')

3. **Room management tests**:
   - test('should join room and persist to AsyncStorage')
   - test('should leave room and remove from AsyncStorage')
   - test('should re-join rooms after reconnection')
   - test('should store multiple rooms independently')
   - test('should handle room join when not connected')

4. **Typing indicator tests**:
   - test('should subscribe to typing indicators')
   - test('should receive typing updates')
   - test('should unsubscribe from typing indicators')

5. **Helper tests**:
   - test('should emit events to server')
   - test('should register event listeners')
   - test('should unregister event listeners')

Use mock helpers from websocketMocks.ts for Socket.IO simulation.
  </action>
  <verify>
npm test -- WebSocketContext.test.tsx passes with 20+ additional tests (32+ total)
  </verify>
  <done>
32+ total WebSocketContext tests passing covering all streaming, room, and event features
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Run WebSocketContext tests:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- WebSocketContext.test.tsx
   ```
   Expected: 32+ tests passing

2. Run all context tests:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- contexts/
   ```
   Expected: AuthContext (42), DeviceContext (41), WebSocketContext (32+) = 115+ tests passing

3. Check test coverage for WebSocketContext.tsx:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- --coverage --collectCoverageFrom="contexts/WebSocketContext.tsx"
   ```
   Expected: >70% coverage for WebSocketContext
</verification>

<success_criteria>
- WebSocketContext.test.tsx created with 600+ lines
- websocketMocks.ts helper file created with 200+ lines
- 32+ tests covering connection, reconnection, streaming, and rooms
- 100% test pass rate
- WebSocketContext coverage >70%
- All tests follow existing patterns from AuthContext.test.tsx and DeviceContext.test.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/138-mobile-state-management-testing/138-01-SUMMARY.md` with:
- Tests created count
- Coverage percentage achieved
- Any issues encountered with Socket.IO mocking
</output>
