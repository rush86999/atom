---
phase: 138-mobile-state-management-testing
plan: 05
type: execute
wave: 2
depends_on: [138-01, 138-02]
files_modified:
  - mobile/src/__tests__/contexts/ContextIntegration.test.tsx
autonomous: true

must_haves:
  truths:
    - "AuthProvider and DeviceContext integrate correctly (device requires auth)"
    - "AuthProvider and WebSocketContext integrate correctly (socket requires auth)"
    - "DeviceContext and WebSocketContext work together (device rooms in socket)"
    - "All three providers work together in a full app scenario"
    - "Provider nesting order is correct (Auth > Device > WebSocket)"
  artifacts:
    - path: mobile/src/__tests__/contexts/ContextIntegration.test.tsx
      provides: "Multi-provider integration tests"
      min_lines: 400
  key_links:
    - from: mobile/src/__tests__/contexts/ContextIntegration.test.tsx
      to: mobile/src/contexts/AuthContext.tsx
      via: "AuthProvider wrapping other providers"
      pattern: "ContextIntegration tests AuthContext as base provider"
    - from: mobile/src/__tests__/contexts/ContextIntegration.test.tsx
      to: mobile/src/contexts/DeviceContext.tsx
      via: "DeviceProvider inside AuthProvider"
      pattern: "ContextIntegration tests DeviceContext depends on AuthContext"
    - from: mobile/src/__tests__/contexts/ContextIntegration.test.tsx
      to: mobile/src/contexts/WebSocketContext.tsx
      via: "WebSocketProvider inside AuthProvider"
      pattern: "ContextIntegration tests WebSocketContext depends on AuthContext"
---

<objective>
Create multi-provider integration tests to verify context providers work together correctly.

**Purpose:** Context providers have dependencies (Auth → Device, Auth → WebSocket). Integration tests ensure these dependencies work correctly and providers can share state.

**Output:** 400+ line integration test file with multi-provider scenarios.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-mobile-state-management-testing/138-RESEARCH.md
@mobile/src/contexts/AuthContext.tsx
@mobile/src/contexts/DeviceContext.tsx
@mobile/src/contexts/WebSocketContext.tsx
@mobile/src/__tests__/contexts/AuthContext.test.tsx
@mobile/src/__tests__/contexts/DeviceContext.test.tsx
@mobile/src/__tests__/helpers/storageTestHelpers.ts
</context>

<tasks>

<task type="auto">
  <name>Create Auth + Device integration tests</name>
  <files>mobile/src/__tests__/contexts/ContextIntegration.test.tsx</files>
  <action>
Create multi-provider integration test file at mobile/src/__tests__/contexts/ContextIntegration.test.tsx with Auth + Device tests (200+ lines):

1. **TestApp component** (40 lines):
   - Wraps AuthProvider > DeviceProvider
   - Renders test components that consume both contexts
   - Provides testIDs for verification: authStatus, deviceStatus, deviceRegistered

2. **Auth + Device integration tests**:
   - test('should allow device registration only when authenticated')
     - Setup: Not authenticated
     - Action: Try to register device
     - Verify: Registration fails with "Not authenticated" error

   - test('should allow device registration after authentication')
     - Setup: Authenticate user
     - Action: Register device
     - Verify: Registration succeeds, device registered

   - test('should clear device state on logout')
     - Setup: Authenticated and device registered
     - Action: Call logout()
     - Verify: Device state cleared, unregistered

   - test('should use auth token for device registration')
     - Setup: Authenticated with access token
     - Action: Register device
     - Verify: API called with Bearer token from auth

   - test('should share user info between contexts')
     - Setup: Authenticated user
     - Action: Access user from DeviceContext
     - Verify: DeviceContext can access user from AuthContext

   - test('should handle auth state changes while device registered')
     - Setup: Device registered
     - Action: Token expires and refreshes
     - Verify: Device still registered after token refresh

   - test('should handle device registration failure when auth expires')
     - Setup: Auth token expires during device registration
     - Action: Attempt device registration
     - Verify: Registration fails gracefully

3. **Provider nesting tests**:
   - test('should require DeviceProvider inside AuthProvider')
   - test('should throw error when useDevice used without DeviceProvider')
   - test('should throw error when useAuth used without AuthProvider')

Use nested provider renders matching actual app structure.
  </action>
  <verify>
npm test -- ContextIntegration.test.tsx passes with 10+ Auth+Device tests
  </verify>
  <done>
10+ tests passing for Auth + Device integration
  </done>
</task>

<task type="auto">
  <name>Create Auth + WebSocket and multi-provider integration tests</name>
  <files>mobile/src/__tests__/contexts/ContextIntegration.test.tsx</files>
  <action>
Add to ContextIntegration.test.tsx with Auth + WebSocket and full integration tests (200+ lines):

1. **Auth + WebSocket integration tests**:
   - test('should connect WebSocket only when authenticated')
     - Setup: Not authenticated
     - Action: Mount WebSocketProvider
     - Verify: No connection attempt

   - test('should connect WebSocket after authentication')
     - Setup: Authenticate user
     - Action: Mount WebSocketProvider
     - Verify: Connection attempted with auth token

   - test('should disconnect WebSocket on logout')
     - Setup: Authenticated and WebSocket connected
     - Action: Call logout()
     - Verify: WebSocket disconnected

   - test('should use auth token for WebSocket connection')
     - Setup: Authenticated with access token
     - Action: WebSocket connects
     - Verify: Socket auth option includes token

   - test('should reconnect WebSocket after token refresh')
     - Setup: WebSocket connected with token
     - Action: Token refreshes
     - Verify: WebSocket reconnects with new token

   - test('should handle WebSocket connection without auth gracefully')
     - Setup: No auth token available
     - Action: WebSocket tries to connect
     - Verify: Connection error set, no crash

2. **Full three-provider integration tests**:
   - test('should mount all providers in correct order')
     - Setup: Auth > Device > WebSocket
     - Action: Render TestApp with all providers
     - Verify: All providers mount without errors

   - test('should pass auth state to all dependent providers')
     - Setup: Authenticate in TestApp
     - Action: Check all providers
     - Verify: Device and WebSocket receive auth state

   - test('should handle logout across all providers')
     - Setup: All providers active, user authenticated
     - Action: Call logout()
     - Verify: Auth cleared, device unregistered, WebSocket disconnected

   - test('should handle concurrent state changes')
     - Setup: All providers active
     - Action: Trigger login, device registration, WebSocket connect simultaneously
     - Verify: All operations complete correctly

   - test('should handle rapid auth state changes')
     - Setup: All providers active
     - Action: Login → logout → login rapidly
     - Verify: All providers handle changes, no corruption

3. **Performance tests**:
   - test('should render all providers within acceptable time')
     - Setup: Measure mount time
     - Action: Mount all providers
     - Verify: Mount completes in <100ms

   - test('should handle context updates without performance degradation')
     - Setup: All providers mounted
     - Action: Trigger 100 state updates
     - Verify: No significant slowdown

Create realistic full-app scenarios with all three providers.
  </action>
  <verify>
npm test -- ContextIntegration.test.tsx passes with 15+ additional tests (25+ total)
  </verify>
  <done>
25+ tests passing for complete multi-provider integration
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Run context integration tests:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- ContextIntegration.test.tsx
   ```
   Expected: 25+ tests passing

2. Run all context tests:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- contexts/
   ```
   Expected: All context tests passing (Auth, Device, WebSocket, Integration)

3. Check overall context coverage:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- --coverage --collectCoverageFrom="contexts/*.tsx"
   ```
   Expected: >75% combined coverage for all contexts

4. Verify provider order:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- --testNamePattern="order"
   ```
   Expected: Provider nesting order tests passing
</verification>

<success_criteria>
- ContextIntegration.test.tsx created with 400+ lines
- 25+ tests covering all provider combinations
- 100% test pass rate
- Provider dependencies verified
- Performance tests passing
- >75% combined context coverage
</success_criteria>

<output>
After completion, create `.planning/phases/138-mobile-state-management-testing/138-05-SUMMARY.md` with:
- Tests created count
- Provider combinations tested
- Any provider ordering issues found
- Coverage percentages
</output>
