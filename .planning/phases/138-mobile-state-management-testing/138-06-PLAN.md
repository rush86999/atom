---
phase: 138-mobile-state-management-testing
plan: 06
type: execute
wave: 3
depends_on: [138-01, 138-02, 138-03, 138-04, 138-05]
files_modified:
  - mobile/.github/workflows/mobile-coverage.yml
  - mobile/.planning/phases/138-mobile-state-management-testing/138-COVERAGE.md
autonomous: true

must_haves:
  truths:
    - "State management coverage report shows >=80% for contexts"
    - "State management coverage report shows >=75% for storage services"
    - "Coverage gaps are documented with specific files and line numbers"
    - "CI/CD workflow enforces coverage thresholds for state management"
    - "Phase summary includes recommendations for Phase 139"
  artifacts:
    - path: mobile/.github/workflows/mobile-coverage.yml
      provides: "CI/CD workflow with coverage enforcement"
    - path: mobile/.planning/phases/138-mobile-state-management-testing/138-COVERAGE.md
      provides: "Coverage report and gap analysis"
      min_lines: 300
  key_links:
    - from: mobile/.github/workflows/mobile-coverage.yml
      to: mobile/src/contexts
      via: "coverage collection"
      pattern: "CI/CD measures context coverage"
    - from: mobile/.github/workflows/mobile-coverage.yml
      to: mobile/src/services/storageService.ts
      via: "coverage collection"
      pattern: "CI/CD measures storage service coverage"
    - from: mobile/.planning/phases/138-mobile-state-management-testing/138-COVERAGE.md
      to: .planning/phases/139-mobile-platform-specific-testing
      via: "recommendations section"
      pattern: "Phase 138 findings inform Phase 139 planning"
---

<objective>
Generate state management coverage report, enforce coverage thresholds in CI/CD, and document findings with recommendations.

**Purpose:** Validate that Phase 138 achieved its coverage targets (80% for contexts, 75% for storage), enforce going forward, and provide handoff to Phase 139.

**Output:** Coverage report document, updated CI/CD workflow, and phase summary.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-mobile-state-management-testing/138-RESEARCH.md
@.planning/phases/137-mobile-navigation-testing/137-06-PLAN.md
@mobile/src/contexts/AuthContext.tsx
@mobile/src/contexts/DeviceContext.tsx
@mobile/src/contexts/WebSocketContext.tsx
@mobile/src/services/storageService.ts
</context>

<tasks>

<task type="auto">
  <name>Generate state management coverage report</name>
  <files>mobile/.planning/phases/138-mobile-state-management-testing/138-COVERAGE.md</files>
  <action>
Create coverage report at mobile/.planning/phases/138-mobile-state-management-testing/138-COVERAGE.md (300+ lines):

1. **Coverage Summary Section**:
   - Run coverage: `npm test -- --coverage --coverageReporters="json-summary" --collectCoverageFrom="contexts/**/*.tsx" --collectCoverageFrom="services/storageService.ts"`
   - Extract coverage from coverage-summary.json
   - Create table with files, statements, branches, functions, lines coverage
   - Calculate average coverage: (sum of all coverage / number of files)

2. **Per-File Analysis**:
   - AuthContext.tsx: Coverage percentage, covered lines, uncovered lines
   - DeviceContext.tsx: Coverage percentage, covered lines, uncovered lines
   - WebSocketContext.tsx: Coverage percentage, covered lines, uncovered lines
   - storageService.ts: Coverage percentage, covered lines, uncovered lines

3. **Gap Analysis**:
   - List uncovered line numbers for each file below threshold
   - Categorize gaps: Error handling, Edge cases, Async flows, Provider init
   - Identify high-priority gaps (critical paths uncovered)
   - Note low-priority gaps (debug code, unused features)

4. **Test Inventory**:
   - Count tests by context: AuthContext (42 existing + new), DeviceContext (41 existing + new), WebSocketContext (new)
   - Count integration tests: Hydration, Lifecycle, Multi-provider
   - Count service tests: StorageService (new)
   - Total test count and breakdown by type

5. **Test Quality Metrics**:
   - Pass rate: (passing tests / total tests) * 100
   - Coverage per test: (coverage percentage / test count)
   - Test execution time

6. **Comparison to Baseline**:
   - Pre-Phase 138 coverage (if available)
   - Coverage improvement in percentage points
   - Tests added this phase

7. **Phase 139 Recommendations**:
   - Any remaining coverage gaps for state management
   - Platform-specific state testing needs (iOS vs Android)
   - Performance testing for state updates
   - Memory leak testing for context providers

Use code fence formatting for all tables and code examples.
  </action>
  <verify>
Coverage report created with 7 sections, 300+ lines
  </verify>
  <done>
138-COVERAGE.md exists with complete coverage analysis
  </done>
</task>

<task type="auto">
  <name>Create CI/CD coverage enforcement workflow</name>
  <files>mobile/.github/workflows/mobile-coverage.yml</files>
  <pattern>create new file</pattern>
  <action>
Create mobile CI/CD workflow at mobile/.github/workflows/mobile-coverage.yml (or update existing):

1. **Coverage enforcement job**:
   - Run tests with coverage: `npm test -- --coverage --coverageReporters="json-summary" --coverageReporters="lcov"`
   - Set thresholds: 80% for contexts, 75% for storage services
   - Fail build if thresholds not met
   - Comment on PR with coverage trends

2. **State management specific checks**:
   - Check AuthContext.tsx coverage >=80%
   - Check DeviceContext.tsx coverage >=80%
   - Check WebSocketContext.tsx coverage >=80%
   - Check storageService.ts coverage >=75%
   - Check aggregate contexts coverage >=80%

3. **PR comment template**:
   ```yaml
   - name: Comment Coverage
     if: always()
     run: |
       gh pr comment ${{ github.event.pull_request.number }} << EOF
       ## State Management Coverage Report

       | File | Statements | Branches | Functions | Lines |
       |------|------------|----------|-----------|-------|
       ... (table)

       **Threshold**: Contexts >=80%, Storage >=75%
       **Status**: ${{ job.status }}
       EOF
   ```

4. **Artifact upload**:
   - Upload coverage/lcov.info as coverage artifact
   - Retention: 30 days for trend tracking

5. **Trend tracking** (if time permits):
   - Compare to previous run
   - Show +/-% change
   - Warn if coverage decreased

Follow pattern from Phase 137-06 navigation coverage checks.
  </action>
  <verify>
mobile-coverage.yml exists with coverage thresholds and PR comments
  </verify>
  <done>
CI/CD workflow enforces 80% contexts, 75% storage coverage
  </done>
</task>

<task type="auto">
  <name>Create Phase 138 final summary document</name>
  <files>mobile/.planning/phases/138-mobile-state-management-testing/138-SUMMARY.md</files>
  <action>
Create phase summary at mobile/.planning/phases/138-mobile-state-management-testing/138-SUMMARY.md (400+ lines):

1. **Phase Overview**:
   - Phase: 138 Mobile State Management Testing
   - Duration: [start] - [end]
   - Plans executed: 6/6
   - Status: COMPLETE

2. **Plans Executed**:
   - 138-01: WebSocketContext tests (32 tests, 600+ lines)
   - 138-02: StorageService tests (50 tests, 800+ lines)
   - 138-03: State hydration tests (25 tests, 500+ lines)
   - 138-04: App lifecycle tests (23 tests, 500+ lines)
   - 138-05: Context integration tests (25 tests, 400+ lines)
   - 138-06: Coverage verification and CI/CD

3. **Coverage Achieved**:
   - AuthContext.tsx: XX% coverage
   - DeviceContext.tsx: XX% coverage
   - WebSocketContext.tsx: XX% coverage
   - storageService.ts: XX% coverage
   - Aggregate contexts: XX% coverage

4. **Tests Created**:
   - Total tests: 155+
   - Context tests: 140+ (Auth 42+, Device 41+, WebSocket 32+, Integration 25+)
   - Integration tests: 48+ (Hydration 25+, Lifecycle 23+)
   - Service tests: 50+ (StorageService)

5. **Key Findings**:
   - WebSocket reconnection logic fully tested
   - State hydration verified for all providers
   - App lifecycle transitions handled correctly
   - Multi-provider integration verified
   - MMKV and AsyncStorage operations covered

6. **Issues Encountered**:
   - Socket.IO mocking complexity (resolved with mock utilities)
   - Async timing in hydration tests (resolved with flushPromises)
   - AppState mocking challenges (resolved with custom mock)
   - Any remaining issues for Phase 139

7. **Handoff to Phase 139**:
   - Remaining coverage gaps (if any)
   - Platform-specific testing needs (iOS vs Android)
   - Performance optimization opportunities
   - Recommended test infrastructure improvements

8. **Success Criteria Verification**:
   - [ ] Context providers tested with provider value and updates: YES (140+ tests)
   - [ ] AsyncStorage/MMKV persistence tested: YES (50+ tests)
   - [ ] State hydration tested: YES (25+ tests)
   - [ ] State sync tested across app lifecycle: YES (23+ tests)
   - [ ] Coverage thresholds met: YES (80%/75%)

9. **Files Created**:
   - List all test files created with line counts
   - List all helper files created
   - List documentation files created

10. **Recommendations for Future Phases**:
    - Phase 139: Platform-specific testing
    - Consider: Performance testing for large state objects
    - Consider: Memory profiling for long-running sessions
    - Consider: Stress testing for rapid state updates
</action>
  <verify>
138-SUMMARY.md created with 10 sections, 400+ lines
  </verify>
  <done>
Phase summary complete with all verification criteria met
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Verify coverage report exists:
   ```bash
   ls -la /Users/rushiparikh/projects/atom/mobile/.planning/phases/138-mobile-state-management-testing/138-COVERAGE.md
   ```

2. Verify CI/CD workflow:
   ```bash
   ls -la /Users/rushiparikh/projects/atom/mobile/.github/workflows/mobile-coverage.yml
   ```

3. Verify phase summary:
   ```bash
   ls -la /Users/rushiparikh/projects/atom/mobile/.planning/phases/138-mobile-state-management-testing/138-SUMMARY.md
   ```

4. Check all phase deliverables:
   ```bash
   ls -la /Users/rushiparikh/projects/atom/mobile/.planning/phases/138-mobile-state-management-testing/
   ```
   Expected: 6 PLAN files, 1 COVERAGE, 1 SUMMARY

5. Verify coverage meets thresholds:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- --coverage --collectCoverageFrom="contexts/**/*.tsx"
   ```
   Expected: >=80% coverage for contexts
</verification>

<success_criteria>
- 138-COVERAGE.md created with complete coverage analysis
- CI/CD workflow enforces 80%/75% thresholds
- 138-SUMMARY.md documents all phase outcomes
- All success criteria verified
- Handoff to Phase 139 prepared
</success_criteria>

<output>
After completion, create `.planning/phases/138-mobile-state-management-testing/138-06-SUMMARY.md` with:
- Final coverage percentages
- CI/CD workflow status
- Phase 138 completion confirmation
- Phase 139 handoff notes
</output>
