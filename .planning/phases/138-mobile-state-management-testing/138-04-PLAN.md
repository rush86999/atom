---
phase: 138-mobile-state-management-testing
plan: 04
type: execute
wave: 2
depends_on: [138-01, 138-03]
files_modified:
  - mobile/src/__tests__/integration/appLifecycle.test.tsx
autonomous: true

must_haves:
  truths:
    - "Auth state persists when app goes to background and returns to foreground"
    - "Device state syncs when app goes to background"
    - "WebSocket reconnects when app returns to foreground"
    - "State is saved before app background transition"
    - "Multiple background/foreground transitions are handled correctly"
  artifacts:
    - path: mobile/src/__tests__/integration/appLifecycle.test.tsx
      provides: "App lifecycle state persistence tests"
      min_lines: 500
  key_links:
    - from: mobile/src/__tests__/integration/appLifecycle.test.tsx
      to: mobile/src/contexts/AuthContext.tsx
      via: "AppState 'change' event simulation"
      pattern: "appLifecycle tests AuthContext state persistence"
    - from: mobile/src/__tests__/integration/appLifecycle.test.tsx
      to: mobile/src/contexts/DeviceContext.tsx
      via: "AppState 'change' event simulation"
      pattern: "appLifecycle tests DeviceContext sync on background"
    - from: mobile/src/__tests__/integration/appLifecycle.test.tsx
      to: mobile/src/contexts/WebSocketContext.tsx
      via: "AppState 'change' event simulation"
      pattern: "appLifecycle tests WebSocket reconnection on foreground"
---

<objective>
Create app lifecycle integration tests to verify state persistence across app background/foreground transitions.

**Purpose:** Mobile apps frequently go to background (user switches apps, locks phone). State must persist and connections must recover properly. This plan tests real-world app lifecycle scenarios.

**Output:** 500+ line integration test file with comprehensive lifecycle testing.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-mobile-state-management-testing/138-RESEARCH.md
@mobile/src/contexts/AuthContext.tsx
@mobile/src/contexts/DeviceContext.tsx
@mobile/src/contexts/WebSocketContext.tsx
@mobile/src/__tests__/integration/stateHydration.test.tsx
@mobile/src/__tests__/helpers/storageTestHelpers.ts
</context>

<tasks>

<task type="auto">
  <name>Create AppState mock utilities and AuthContext lifecycle tests</name>
  <files>mobile/src/__tests__/integration/appLifecycle.test.tsx</files>
  <action>
Create app lifecycle integration test file at mobile/src/__tests__/integration/appLifecycle.test.tsx with AuthContext lifecycle tests (250+ lines):

1. **AppState mock utilities** (in test file, 60 lines):
   - createMockAppState(): Create React Native AppState mock
   - simulateAppStateChange(state: 'active' | 'background' | 'inactive'): Trigger app state change
   - waitForAppStateChange(timeout): Wait for AppState change handlers
   - getAppStateChangeHandlers(): Get all registered AppState listeners
   - resetAppStateListeners(): Clear all listeners

2. **AuthContext lifecycle tests**:
   - test('should persist auth state when app goes to background')
     - Setup: User authenticated
     - Action: Simulate app → background
     - Verify: Tokens still in storage, state preserved

   - test('should restore auth state when app returns to foreground')
     - Setup: Authenticated, app → background
     - Action: Simulate app → foreground
     - Verify: isAuthenticated still true, state restored

   - test('should not refresh token on brief background transition')
     - Setup: Authenticated with valid token
     - Action: Brief background (<5 min), then foreground
     - Verify: No token refresh API called

   - test('should handle token expiry during extended background')
     - Setup: Token expires while app in background
     - Action: Extended background (>1 hour), then foreground
     - Verify: Token refresh attempted or user logged out

   - test('should save device state before background transition')
     - Setup: Device registered with capabilities
     - Action: Simulate app → background
     - Verify: Device state saved to AsyncStorage

3. **Test helpers for lifecycle simulation**:
   - Create TestApp component that wraps all providers
   - Create lifecycle simulation functions
   - Add background/foreground assertion helpers

Use flushPromises() and waitFor() for async state updates.
  </action>
  <verify>
npm test -- appLifecycle.test.tsx passes with 8+ AuthContext lifecycle tests
  </verify>
  <done>
8+ tests passing for AuthContext app lifecycle scenarios
  </done>
</task>

<task type="auto">
  <name>Create DeviceContext and WebSocketContext lifecycle tests</name>
  <files>mobile/src/__tests__/integration/appLifecycle.test.tsx</files>
  <action>
Add to appLifecycle.test.tsx with DeviceContext and WebSocketContext lifecycle tests (250+ lines):

1. **DeviceContext lifecycle tests**:
   - test('should sync device state when app goes to background')
     - Setup: Device registered
     - Action: Simulate app → background
     - Verify: syncDevice API called, lastSync updated

   - test('should handle sync failure when app goes to background')
     - Setup: Device registered, sync API returns error
     - Action: Simulate app → background
     - Verify: Error handled gracefully, state still saved locally

   - test('should update device token on foreground if changed')
     - Setup: Push token changed while app backgrounded
     - Action: Simulate app → foreground with new token
     - Verify: Device token updated in storage and API

   - test('should re-check capabilities on foreground')
     - Setup: Camera permission granted while app backgrounded
     - Action: Simulate app → foreground
     - Verify: Capabilities re-checked and updated

2. **WebSocketContext lifecycle tests**:
   - test('should disconnect WebSocket when app goes to background')
     - Setup: WebSocket connected
     - Action: Simulate app → background
     - Verify: Socket disconnect called

   - test('should reconnect WebSocket when app returns to foreground')
     - Setup: WebSocket was connected before background
     - Action: Simulate app → foreground
     - Verify: Socket reconnect attempted

   - test('should not reconnect if not authenticated')
     - Setup: Not authenticated
     - Action: Simulate app → foreground
     - Verify: No reconnect attempt

   - test('should re-join rooms after foreground reconnection')
     - Setup: Was in room 'user:123' before background
     - Action: Simulate background → foreground → reconnection
     - Verify: Room re-joined after reconnection

   - test('should handle connection error on foreground return')
     - Setup: Network unavailable when returning to foreground
     - Action: Simulate foreground with connection error
     - Verify: Error state set, reconnect attempts scheduled

3. **Multi-provider lifecycle integration tests**:
   - test('should handle all providers during background transition')
     - Setup: Authenticated, device registered, WebSocket connected
     - Action: Simulate app → background
     - Verify: Auth persisted, device synced, socket disconnected

   - test('should restore all providers after foreground return')
     - Setup: All providers active before background
     - Action: Simulate background → foreground
     - Verify: Auth restored, device capabilities checked, socket reconnected

   - test('should handle rapid background/foreground cycles')
     - Setup: All providers active
     - Action: Simulate 5 rapid background/foreground transitions
     - Verify: No state corruption, all transitions handled

   - test('should handle memory cleanup during extended background')
     - Setup: All providers active
     - Action: Simulate extended background (>30 min)
     - Verify: Appropriate cleanup, no memory leaks

Create realistic app lifecycle scenarios matching actual usage patterns.
  </action>
  <verify>
npm test -- appLifecycle.test.tsx passes with 15+ additional tests (23+ total)
  </verify>
  <done>
23+ tests passing for complete app lifecycle across all providers
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Run app lifecycle tests:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- appLifecycle.test.tsx
   ```
   Expected: 23+ tests passing

2. Run all integration tests:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- integration/
   ```
   Expected: All integration tests passing (hydration + lifecycle)

3. Check context coverage:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- --coverage --collectCoverageFrom="contexts/*.tsx"
   ```
   Expected: >70% coverage for all context providers

4. Verify no memory leaks:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- --detectLeaks
   ```
   Expected: No detected memory leaks
</verification>

<success_criteria>
- appLifecycle.test.tsx created with 500+ lines
- 23+ tests covering all lifecycle scenarios
- 100% test pass rate
- All context providers tested for lifecycle behavior
- AppState mock utilities created for reuse
- Memory leak detection passing
</success_criteria>

<output>
After completion, create `.planning/phases/138-mobile-state-management-testing/138-04-SUMMARY.md` with:
- Tests created count
- Lifecycle scenarios covered
- Any AppState mocking issues
- Memory leak detection results
</output>
