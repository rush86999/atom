---
phase: 138-mobile-state-management-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/__tests__/services/storageService.test.ts
autonomous: true

must_haves:
  truths:
    - "MMKV storage operations are tested (set, get, delete for string, number, boolean, object)"
    - "AsyncStorage operations are tested with proper async handling"
    - "Storage quota management is tested with warning and enforcement thresholds"
    - "Storage cleanup is tested with LRU strategy and old data removal"
    - "Storage migration from AsyncStorage to MMKV is tested"
  artifacts:
    - path: mobile/src/__tests__/services/storageService.test.ts
      provides: "StorageService comprehensive tests"
      min_lines: 800
  key_links:
    - from: mobile/src/__tests__/services/storageService.test.ts
      to: mobile/src/services/storageService.ts
      via: "import { storageService, setString, getString, setObject }"
      pattern: "storageService.test.ts tests all storage methods"
    - from: mobile/src/__tests__/services/storageService.test.ts
      to: react-native-mmkv
      via: "jest.mock"
      pattern: "mock MMKV for in-memory testing"
---

<objective>
Create comprehensive StorageService tests for MMKV and AsyncStorage operations, quota management, and cleanup strategies.

**Purpose:** StorageService is the persistence layer for all mobile state (auth tokens, user data, capabilities, offline queue). Current tests are incomplete. This plan ensures full coverage of storage operations.

**Output:** 800+ line StorageService test file with 50+ tests.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-mobile-state-management-testing/138-RESEARCH.md
@mobile/src/services/storageService.ts
@mobile/jest.setup.js
@mobile/src/__tests__/contexts/AuthContext.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Create MMKV and AsyncStorage basic operation tests</name>
  <files>mobile/src/__tests__/services/storageService.test.ts</files>
  <action>
Create comprehensive StorageService test file at mobile/src/__tests__/services/storageService.test.ts with basic storage tests (300+ lines):

1. **Setup** (following existing service test patterns):
   - Mock MMKV with jest.mock('react-native-mmkv')
   - Mock AsyncStorage from jest.setup.js
   - Create in-memory mock instances for both

2. **MMKV string operation tests**:
   - test('should set string value in MMKV')
   - test('should get string value from MMKV')
   - test('should return null for non-existent MMKV key')
   - test('should handle MMKV errors gracefully')

3. **MMKV number operation tests**:
   - test('should set number value in MMKV')
   - test('should get number value from MMKV')
   - test('should return null for non-existent number key')

4. **MMKV boolean operation tests**:
   - test('should set boolean value in MMKV')
   - test('should get boolean value from MMKV')
5. test('should handle false boolean correctly')

6. **AsyncStorage operation tests**:
   - test('should set string value in AsyncStorage')
   - test('should get string value from AsyncStorage async')
   - test('should remove value from AsyncStorage')
   - test('should handle AsyncStorage errors')

7. **Object operation tests**:
   - test('should set object as JSON string')
   - test('should get and parse object from JSON')
   - test('should return null for invalid JSON')
   - test('should handle circular reference errors')

Each test should verify both the storage API call and the returned value.
  </action>
  <verify>
npm test -- storageService.test.ts passes with 20+ basic operation tests
  </verify>
  <done>
20+ tests passing for MMKV and AsyncStorage basic operations
  </done>
</task>

<task type="auto">
  <name>Create storage quota and cleanup tests</name>
  <files>mobile/src/__tests__/services/storageService.test.ts</files>
  <action>
Add to storageService.test.ts with quota management and cleanup tests (300+ lines):

1. **Storage quota tests**:
   - test('should calculate storage quota correctly')
   - test('should return quota usage ratio')
   - test('should detect when quota approaching warning threshold (80%)')
   - test('should detect when quota at enforcement threshold (95%)')
   - test('should track storage breakdown by entity type')
   - test('should update quota after operations')

2. **Storage cleanup tests**:
   - test('should clear all cached data while preserving auth tokens')
   - test('should cleanup old data using LRU strategy')
   - test('should remove smallest entries first during cleanup')
   - test('should stop cleanup when enough space freed')
   - test('should return bytes freed from cleanup')
   - test('should handle cleanup errors gracefully')

3. **Cache compression tests**:
   - test('should identify compressible cache items')
   - test('should calculate potential compression savings')
   - test('should compress large cache items')
   - test('should respect compression threshold')

4. **Storage quality metrics tests**:
   - test('should return storage statistics')
   - test('should calculate average item size')
   - test('should track cache hit rate')
   - test('should return compression ratio')

5. **Clear all tests**:
   - test('should clear all MMKV keys')
   - test('should clear all AsyncStorage keys')
   - test('should handle clear errors gracefully')

6. **Get all keys tests**:
   - test('should return all keys from MMKV and AsyncStorage')
   - test('should deduplicate keys if overlap exists')
   - test('should handle empty storage')

Mock quota calculations to test threshold logic.
  </action>
  <verify>
npm test -- storageService.test.ts passes with 25+ quota and cleanup tests
  </verify>
  <done>
25+ tests passing for quota management, cleanup, and metrics
  </done>
</task>

<task type="auto">
  <name>Create storage migration and edge case tests</name>
  <files>mobile/src/__tests__/services/storageService.test.ts</files>
  <action>
Add to storageService.test.ts with migration and edge case tests (200+ lines):

1. **Migration tests**:
   - test('should migrate key from AsyncStorage to MMKV')
   - test('should migrate multiple keys successfully')
   - test('should count successful and failed migrations')
   - test('should remove migrated keys from AsyncStorage')
   - test('should handle migration errors gracefully')
   - test('should only migrate MMKV_KEYS (not ASYNC_STORAGE_KEYS)')

2. **Key type checking tests**:
   - test('should check if key exists in MMKV')
   - test('should check if key exists in AsyncStorage')
   - test('should return false for non-existent keys')
   - test('should handle has() errors gracefully')

3. **Delete operation tests**:
   - test('should delete key from MMKV')
   - test('should delete key from AsyncStorage')
   - test('should return true on successful delete')
   - test('should return false on delete error')

4. **Edge case tests**:
   - test('should handle very large string values')
   - test('should handle special characters in keys and values')
   - test('should handle unicode characters correctly')
   - test('should handle null and undefined values')
   - test('should handle empty string values')
   - test('should handle negative numbers')
   - test('should handle floating point numbers')

5. **Convenience function tests**:
   - test('setString should call storageService.setString')
   - test('getString should call storageService.getStringAsync')
   - test('setObject should serialize and call setString')
   - test('getObject should call getStringAsync and parse')
   - test('setNumber should convert to string for AsyncStorage')
   - test('getNumber should parse string to number')
   - test('setBoolean should convert to string for AsyncStorage')
   - test('getBoolean should parse string to boolean')
   - test('deleteKey should call storageService.delete')
   - test('hasKey should call storageService.has')
   - test('clearStorage should call storageService.clear')

Test convenience functions delegate correctly to storageService methods.
  </action>
  <verify>
npm test -- storageService.test.ts passes with 25+ migration and edge case tests (50+ total)
  </verify>
  <done>
50+ tests passing covering all StorageService functionality
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Run StorageService tests:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- storageService.test.ts
   ```
   Expected: 50+ tests passing

2. Check storage coverage:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- --coverage --collectCoverageFrom="services/storageService.ts"
   ```
   Expected: >80% coverage for storageService.ts

3. Run all service tests:
   ```bash
   cd /Users/rushiparikh/projects/atom/mobile && npm test -- services/
   ```
   Expected: All service tests passing
</verification>

<success_criteria>
- storageService.test.ts created with 800+ lines
- 50+ tests covering MMKV, AsyncStorage, quota, cleanup, and migration
- 100% test pass rate
- StorageService coverage >80%
- All convenience functions tested
</success_criteria>

<output>
After completion, create `.planning/phases/138-mobile-state-management-testing/138-02-SUMMARY.md` with:
- Tests created count
- Coverage percentage achieved
- Any MMKV mocking issues encountered
</output>
