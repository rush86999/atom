---
phase: 095-backend-frontend-integration
plan: 07
type: execute
wave: 5
depends_on: [095-01, 095-02, 095-03, 095-04, 095-05, 095-06, 095-08]
files_modified:
  - .planning/phases/095-backend-frontend-integration/095-VERIFICATION.md
autonomous: true

must_haves:
  truths:
    - "All 6 success criteria from ROADMAP.md are verified as TRUE"
    - "Unified coverage aggregator produces combined report with per-platform breakdown"
    - "Frontend integration tests cover component interactions with 90%+ coverage"
    - "API contract validation tests verify all frontend-backend communication"
    - "FastCheck property tests validate state management invariants (10-15 properties)"
    - "CI/CD orchestration runs tests in parallel and aggregates coverage"
    - "Frontend test pass rate is 100% (all existing failing tests fixed via Plan 08)"
  artifacts:
    - path: .planning/phases/095-backend-frontend-integration/095-VERIFICATION.md
      provides: Phase verification results
      contains: success_criteria, verification_results, metrics, blockers
  key_links:
    - from: 095-VERIFICATION.md
      to: all plan SUMMARY.md files
      via: verification cross-references
      pattern: 095-\d+-SUMMARY
    - from: 095-VERIFICATION.md
      to: 095-08-SUMMARY.md
      via: SUCCESS-06 verification
      pattern: 100% pass rate
---

<objective>
Verify Phase 095 completion against all success criteria and create verification summary.

**Purpose:** Confirm all plans are complete and success criteria met. Create verification report documenting achievements, metrics, and any remaining items for future phases.

**Output:** Verification report with success criteria validation, coverage metrics, test counts, and phase completion status.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
</execution_context>

<context>
@.planning/phases/095-backend-frontend-integration/095-01-PLAN.md
@.planning/phases/095-backend-frontend-integration/095-02-PLAN.md
@.planning/phases/095-backend-frontend-integration/095-03-PLAN.md
@.planning/phases/095-backend-frontend-integration/095-04-PLAN.md
@.planning/phases/095-backend-frontend-integration/095-05-PLAN.md
@.planning/phases/095-backend-frontend-integration/095-06-PLAN.md
@.planning/phases/095-backend-frontend-integration/095-08-PLAN.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Verify success criteria and collect metrics</name>
  <files>.planning/phases/095-backend-frontend-integration/095-VERIFICATION.md</files>
  <action>
    Create verification report with the following sections:

    **1. Phase Overview:**
    - Phase number and name
    - Plans completed (01-07)
    - Execution dates
    - Overall status (COMPLETE/PARTIAL/BLOCKED)

    **2. Success Criteria Verification (from ROADMAP.md):**

    For each of the 6 success criteria, document:
    - Criterion text
    - Status: TRUE/FALSE/PARTIAL
    - Evidence: Links to files, commands to verify, metrics
    - Notes: Any deviations or partial completions

    Criteria to verify:
    1. Unified coverage aggregator script exists and produces combined report
    2. Frontend integration tests cover component interactions with 90%+ coverage
    3. API contract validation tests verify all frontend-backend communication
    4. FastCheck property tests validate state invariants (10-15 properties)
    5. CI/CD orchestration runs tests in parallel and aggregates coverage
    6. Frontend tests pass rate 100% (existing failing tests fixed in Plan 08 - canvas-accessibility-tree empty suite, api-backend-helper encryption mock)

    **3. Metrics Summary:**
    - Backend coverage percentage (from pytest)
    - Frontend coverage percentage (from Jest)
    - Overall unified coverage percentage
    - Backend test count and pass rate
    - Frontend test count and pass rate
    - Property test count (frontend FastCheck)
    - Integration test count (frontend)

    **4. Artifacts Created:**
    - List all files created/modified with brief descriptions
    - Include file paths for reference

    **5. Requirements Coverage:**
    - FRONT-01 through FRONT-06 status
    - INFRA-01 through INFRA-02 status
    - Indicate which requirements are COMPLETE/PARTIAL/PENDING

    **6. Blockers and Open Items:**
    - Any blockers preventing full completion
    - Items deferred to future phases (96-99)
    - Known issues or limitations

    **7. Recommendations:**
    - Recommendations for Phase 96 (Mobile Integration)
    - Technical debt items to address
    - Process improvements

    Use markdown formatting with tables for criteria and metrics.
  </action>
  <verify>cat .planning/phases/095-backend-frontend-integration/095-VERIFICATION.md | grep -E "(Criterion|Status|Metrics|COMPLETE)"</verify>
  <done>095-VERIFICATION.md created with all 6 success criteria verified and metrics documented</done>
</task>

<task type="auto">
  <name>Run final validation commands</name>
  <files>.planning/phases/095-backend-frontend-integration/095-VERIFICATION.md</files>
  <action>
    Execute validation commands and add results to verification report:

    **1. Backend Coverage:**
    ```bash
    cd backend && pytest tests/ --cov=core --cov=api --cov=tools --cov-report=json --cov-report=term -q
    ```
    Extract coverage percentage from output

    **2. Frontend Coverage:**
    ```bash
    cd frontend-nextjs && npm run test:ci -- --coverage --no-coverage
    ```
    Extract coverage percentage from output

    **3. Unified Coverage:**
    ```bash
    python backend/tests/scripts/aggregate_coverage.py
    ```
    Extract overall coverage percentage

    **4. Frontend Test Count:**
    ```bash
    cd frontend-nextjs && npm test -- --listTests 2>/dev/null | wc -l
    ```

    **5. Property Test Count:**
    ```bash
    cd frontend-nextjs && npm test -- tests/property/ --listTests 2>/dev/null | wc -l
    ```

    **6. Integration Test Count:**
    ```bash
    cd frontend-nextjs && npm test -- tests/integration/ --listTests 2>/dev/null | wc -l
    ```

    Add a "Validation Results" section to 095-VERIFICATION.md with:
    - Command executed
    - Output/metrics obtained
    - Pass/fail determination for each criterion
  </action>
  <verify>cd backend && python tests/scripts/aggregate_coverage.py --format text 2>&1 | head -20</verify>
  <done>Verification report updated with actual metrics from test execution</done>
</task>

</tasks>

<verification>
After completion:
1. Read 095-VERIFICATION.md and verify all 6 success criteria have status values
2. Check that metrics section contains actual numbers (not placeholders)
3. Verify artifacts created section lists all files from plans 01-06
4. Ensure requirements coverage table shows FRONT-01 through FRONT-06, INFRA-01 through INFRA-02
5. Confirm blockers section is accurate (empty if no blockers)
</verification>

<success_criteria>
1. All 6 success criteria from ROADMAP.md have status (TRUE/FALSE/PARTIAL)
2. Metrics section contains actual coverage percentages and test counts
3. Artifacts created section lists all files created in plans 01-06
4. Requirements coverage shows status for FRONT-01 to FRONT-06, INFRA-01 to INFRA-02
5. Overall phase status is indicated (COMPLETE/PARTIAL/BLOCKED)
6. Recommendations section provides input for Phase 96
</success_criteria>

<output>
After completion, finalize `.planning/phases/095-backend-frontend-integration/095-VERIFICATION.md` with:
- Final verification results
- Phase completion determination
- Handoff to Phase 96 (Mobile Integration)
- Summary of achievements
</output>
