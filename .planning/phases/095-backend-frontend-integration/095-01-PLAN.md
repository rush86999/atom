---
phase: 095-backend-frontend-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/jest.config.js
  - frontend-nextjs/package.json
  - frontend-nextjs/tests/setup.ts
autonomous: true

must_haves:
  truths:
    - "Frontend test runner (Jest) executes tests and produces coverage JSON output"
    - "Test configuration includes jsdom environment and proper path aliases for imports"
    - "Coverage collection is configured for components, pages, and lib directories"
    - "npm test command runs tests and produces coverage/coverage-final.json"
    - "npm run test:ci command exists for CI environment with --ci and --coverage flags"
  artifacts:
    - path: frontend-nextjs/jest.config.js
      provides: Jest test configuration with coverage settings
      contains: testMatch, collectCoverageFrom, coverageReporters
    - path: frontend-nextjs/package.json
      provides: npm scripts for test execution
      contains: test, test:watch, test:coverage, test:ci scripts
    - path: frontend-nextjs/tests/setup.ts
      provides: Test setup with jsdom mocks and global configuration
      min_lines: 50
  key_links:
    - from: package.json
      to: jest.config.js
      via: jest --config jest.config.js
      pattern: jest.*config
    - from: jest.config.js
      to: tests/setup.ts
      via: setupFilesAfterEnv
      pattern: setupFilesAfterEnv
---

<objective>
Fix Jest configuration and enable coverage JSON output for frontend tests.

**Purpose**: Frontend tests need proper configuration to run in CI and produce coverage reports in JSON format for unified aggregation. Currently only 2 test files exist, and coverage output format needs standardization.

**Output**: Working Jest configuration with JSON coverage reporter, npm scripts for CI execution, proper test setup with jsdom mocks.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
</execution_context>

<context>
@frontend-nextjs/jest.config.js
@frontend-nextjs/package.json
@frontend-nextjs/tests/setup.ts
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Update Jest configuration for coverage JSON output</name>
  <files>frontend-nextjs/jest.config.js</files>
  <action>
    1. Add coverageReporters array to include 'json' and 'json-summary' formats
    2. Set coverageDirectory to 'coverage' (default)
    3. Ensure collectCoverageFrom includes all relevant directories (components, pages, lib, hooks)
    4. Add coverageThreshold with global branches 80, lines 80, functions 80, statements 75
    5. Configure moduleNameMapper to handle @/, @pages/, @layouts/, @components/, @lib/ aliases
    6. Set transformIgnorePatterns to handle ES modules from node_modules
    7. Ensure testMatch includes patterns for tests/, components/**/__tests__/, lib/**/__tests__/

    DO NOT change testEnvironment from jsdom (required for React Testing Library)
  </action>
  <verify>cd frontend-nextjs && npm test -- --showConfig | grep -A5 coverageReporters</verify>
  <done>jest.config.js produces coverage JSON at coverage/coverage-final.json when tests run with --coverage flag</done>
</task>

<task type="auto">
  <name>Add npm scripts for test execution</name>
  <files>frontend-nextjs/package.json</files>
  <action>
    1. Verify test script exists: "test": "jest"
    2. Verify test:watch script exists: "test:watch": "jest --watch"
    3. Verify test:coverage script exists: "test:coverage": "jest --coverage"
    4. Ensure test:ci script exists or add it: "test:ci": "jest --ci --watchAll=false --coverage --passWithNoTests"
    5. Add test:silent script: "test:silent": "jest --ci --watchAll=false --coverage --no-coverage"
    6. Add --maxWorkers=2 to test:ci to prevent OOM in CI environments

    If scripts exist, verify they have correct flags. If missing, add them to scripts section.
  </action>
  <verify>cd frontend-nextjs && npm run test:ci -- --help 2>&1 | head -5</verify>
  <done>package.json has test, test:watch, test:coverage, test:ci, and test:silent scripts with correct Jest flags</done>
</task>

<task type="auto">
  <name>Enhance test setup with additional jsdom mocks</name>
  <files>frontend-nextjs/tests/setup.ts</files>
  <action>
    1. Verify existing mocks: scrollIntoView, matchMedia, ResizeObserver, IntersectionObserver, URL.createObjectURL/revokeObjectURL, clipboard
    2. Add missing mock for IntersectionObserverEntry if not present
    3. Add mock for window.scrollTo if not present
    4. Add mock for localStorage and sessionStorage using jest.fn() implementation
    5. Ensure fetch mock uses cross-fetch or node-fetch for consistency
    6. Add mock for Next.js router if not present (use next/router jest mock)

    Keep existing mocks (got module, console methods, useToast, AgentAudioControlContext, WakeWordContext)
  </action>
  <verify>cd frontend-nextjs && npm test -- --no-coverage 2>&1 | head -20</verify>
  <done>tests/setup.ts provides comprehensive jsdom mocks for browser APIs (storage, router, fetch, observers)</done>
</task>

</tasks>

<verification>
After completion:
1. Run `cd frontend-nextjs && npm run test:ci` - tests should execute and produce coverage/coverage-final.json
2. Verify coverage JSON format: `cat frontend-nextjs/coverage/coverage-final.json | jq . | head -20`
3. Check that test configuration includes all required reporters and path mappings
</verification>

<success_criteria>
1. Jest runs tests successfully from `npm run test:ci` command
2. Coverage JSON file exists at frontend-nextjs/coverage/coverage-final.json after test run
3. Coverage JSON contains valid coverage data with per-file metrics
4. All existing tests (2 API route tests) pass with the new configuration
5. test:ci script produces JSON output suitable for unified aggregation
</success_criteria>

<output>
After completion, create `.planning/phases/095-backend-frontend-integration/095-01-SUMMARY.md` with:
- Configuration changes made
- Coverage JSON output location
- Test execution results
- Any issues encountered and resolutions
</output>
