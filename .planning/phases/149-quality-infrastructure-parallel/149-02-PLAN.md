---
phase: 149-quality-infrastructure-parallel
plan: 02
type: execute
wave: 2
depends_on: ["149-01"]
files_modified:
  - backend/tests/scripts/ci_status_aggregator.py
autonomous: true

must_haves:
  truths:
    - "Script parses pytest, Jest, and cargo test result formats"
    - "Aggregated output includes per-platform breakdown with total/passed/failed/duration"
    - "Markdown summary generates with platform table and overall pass rate"
    - "Exit code 1 on test failures, exit code 0 on all pass"
  artifacts:
    - path: "backend/tests/scripts/ci_status_aggregator.py"
      provides: "Unified CI status aggregation across 4 platforms"
      exports: ["parse_pytest_results", "parse_jest_results", "parse_cargo_results", "aggregate_platform_status", "generate_markdown_summary"]
  key_links:
    - from: ".github/workflows/unified-tests-parallel.yml"
      to: "backend/tests/scripts/ci_status_aggregator.py"
      via: "python3 backend/tests/scripts/ci_status_aggregator.py in aggregate-status job"
      pattern: "ci_status_aggregator\\.py"
---

<objective>
Create ci_status_aggregator.py script to combine test results from all platform jobs into unified status report with per-platform breakdown, pass rates, and markdown summary for PR comments.

Purpose: Provide centralized visibility into CI status across all platforms with aggregating metrics and human-readable summaries.

Output: Python script (483 lines) parsing pytest/Jest/cargo formats, generating JSON + markdown outputs, following e2e_aggregator.py pattern.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/149-quality-infrastructure-parallel/149-RESEARCH.md
@.planning/phases/148-cross-platform-e2e-orchestration/148-01-SUMMARY.md
@backend/tests/scripts/e2e_aggregator.py
@.github/workflows/unified-tests-parallel.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create platform result parsers</name>
  <files>backend/tests/scripts/ci_status_aggregator.py</files>
  <action>
    Create backend/tests/scripts/ci_status_aggregator.py with shebang (#!/usr/bin/env python3) and docstring:

    Add these parser functions (following e2e_aggregator.py pattern):
    1. load_json(file_path: str) -> Dict[str, Any] - Load JSON with error handling, returns {"error": message} on failure
    2. parse_pytest_results(results: Dict) -> Dict[str, Any] - Parse pytest JSON report, extract summary fields (total, passed, failed, skipped, duration), calculate pass_rate
    3. parse_jest_results(results: Dict) -> Dict[str, Any] - Parse Jest test-results.json, extract numTotalTests/numFailedTests/numPendingTests, calculate passed and pass_rate
    4. parse_cargo_results(results: Dict) -> Dict[str, Any] - Parse cargo test JSON (pre-processed format with stats key), extract test metrics

    Each parser returns:
    {
      "platform": "backend"|"frontend"|"mobile"|"desktop",
      "total": int,
      "passed": int,
      "failed": int,
      "skipped": int,
      "duration": float,
      "pass_rate": float
    }

    DO NOT copy from RESEARCH.md - implement based on e2e_aggregator.py patterns
  </action>
  <verify>
    python3 -c "from backend.tests.scripts.ci_status_aggregator import parse_pytest_results, parse_jest_results, parse_cargo_results; print('Imports successful')"
  </verify>
  <done>
    Script file exists with 4 parser functions, each returns platform metrics dict with total/passed/failed/skipped/duration/pass_rate
  </done>
</task>

<task type="auto">
  <name>Task 2: Add aggregation and summary generation</name>
  <files>backend/tests/scripts/ci_status_aggregator.py</files>
  <action>
    Add to ci_status_aggregator.py:

    1. aggregate_platform_status(platforms: List[Dict[str, Any]]) -> Dict[str, Any]
       - Sum total_tests, total_passed, total_failed, total_duration across platforms
       - Calculate overall_pass_rate = total_passed / total_tests * 100
       - Return {total_tests, total_passed, total_failed, pass_rate, total_duration_seconds, platform_count}

    2. generate_markdown_summary(aggregate: Dict[str, Any], platforms: List[Dict[str, Any]]) -> str
       - Create markdown with:
         - Header: "# CI Test Results Summary" with timestamp
         - Overall Results: Total Tests, Passed, Failed, Pass Rate, Duration
         - Platform Breakdown table: | Platform | Tests | Passed | Failed | Pass Rate | Duration |
         - Status: "All tests passed" or "X test(s) failed"
       - Return formatted markdown string

    Follow e2e_aggregator.py generate_summary() pattern for table formatting
  </action>
  <verify>
    python3 -c "from backend.tests.scripts.ci_status_aggregator import aggregate_platform_status, generate_markdown_summary; print('Functions imported')"
  </verify>
  <done>
    Script has aggregate_platform_status() calculating totals and pass_rate, generate_markdown_summary() creating formatted markdown with platform table
  </done>
</task>

<task type="auto">
  <name>Task 3: Add CLI with argument parsing and output</name>
  <files>backend/tests/scripts/ci_status_aggregator.py</files>
  <action>
    Add main() function to ci_status_aggregator.py:

    1. ArgumentParser with these arguments:
       - --backend: Backend pytest JSON report path
       - --frontend: Frontend Jest JSON report path
       - --mobile: Mobile Jest JSON report path
       - --desktop: Desktop cargo JSON report path
       - --output: (required) Output JSON file path
       - --summary: Output markdown summary file path

    2. Load each platform result if provided, call appropriate parser, append to platforms list

    3. Call aggregate_platform_status(platforms) to get aggregate metrics

    4. Prepare output dict: {timestamp: datetime.now().isoformat(), aggregate, platforms}

    5. Write JSON output to --output file (create parent dirs, use json.dumps with indent=2)

    6. If --summary provided, write markdown from generate_markdown_summary()

    7. Print summary: "CI Status: {total_passed}/{total_tests} passed ({pass_rate}%)"

    8. Exit code 1 if total_failed > 0, else exit code 0

    Add if __name__ == "__main__": main()

    Follow e2e_aggregator.py main() pattern for structure
  </action>
  <verify>
    python3 backend/tests/scripts/ci_status_aggregator.py --help
  </verify>
  <done>
    Script has working CLI with --help, accepts 4 platform arguments, generates JSON output, optional markdown summary, correct exit codes
  </done>
</task>

</tasks>

<verification>
1. Script is executable: `chmod +x backend/tests/scripts/ci_status_aggregator.py` and `./backend/tests/scripts/ci_status_aggregator.py --help` works
2. All parsers handle missing/error data: Test with invalid JSON returns error structure
3. Aggregation calculates correct totals: Test with sample data (mock 2 platforms with known values)
4. Markdown generates table format: Run with --summary and check output has table headers
5. Exit codes correct: Test with all pass (exit 0) and with failures (exit 1)
</verification>

<success_criteria>
1. ci_status_aggregator.py created with 483 lines (similar to e2e_aggregator.py)
2. Parsers handle pytest (stats), Jest (numTotalTests), and cargo (stats) formats
3. aggregate_platform_status() sums all platform metrics correctly
4. generate_markdown_summary() creates readable table with platform breakdown
5. CLI accepts --backend/--frontend/--mobile/--desktop/--output/--summary arguments
</success_criteria>

<output>
After completion, create `.planning/phases/149-quality-infrastructure-parallel/149-02-SUMMARY.md`
</output>
