---
phase: 149-quality-infrastructure-parallel
plan: 03
type: execute
wave: 3
depends_on: ["149-01", "149-02"]
files_modified:
  - .github/workflows/platform-retry.yml
  - backend/tests/scripts/platform_retry_router.py
autonomous: true

must_haves:
  truths:
    - "Retry workflow triggers on unified-tests-parallel completion with failure conclusion"
    - "Detect-failures job identifies which platforms failed by parsing test result artifacts"
    - "Retry jobs only run for failed platforms (conditional on platform-specific output)"
    - "Platform retry router extracts failed test names for targeted re-run"
  artifacts:
    - path: ".github/workflows/platform-retry.yml"
      provides: "Platform-specific retry jobs triggered on initial test failures"
      contains: "workflow_run trigger, detect-failures job, conditional retry jobs"
    - path: "backend/tests/scripts/platform_retry_router.py"
      provides: "Failed test extraction from platform result artifacts"
      exports: ["extract_failed_tests", "generate_retry_command"]
  key_links:
    - from: ".github/workflows/platform-retry.yml"
      to: ".github/workflows/unified-tests-parallel.yml"
      via: "workflow_run trigger listening for unified-tests-parallel completion"
      pattern: "workflow_run.*workflows.*unified-tests-parallel"
    - from: ".github/workflows/platform-retry.yml"
      to: "backend/tests/scripts/platform_retry_router.py"
      via: "python3 backend/tests/scripts/platform_retry_router.py in detect-failures job"
      pattern: "platform_retry_router\\.py"
---

<objective>
Create platform-retry.yml workflow and platform_retry_router.py script to detect failed platforms and trigger targeted re-runs (only failed platform tests, not full suite) for 80% time savings vs full re-runs.

Purpose: Avoid wasting CI time re-running passing platform tests when only one platform fails, enabling faster feedback on flaky tests.

Output: New workflow with detect-failures job parsing artifacts, conditional retry jobs per platform, Python script extracting failed test names.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/149-quality-infrastructure-parallel/149-RESEARCH.md
@.planning/phases/148-cross-platform-e2e-orchestration/148-01-SUMMARY.md
@backend/tests/scripts/e2e_aggregator.py
@backend/tests/scripts/detect_flaky_tests.py
@.github/workflows/unified-tests-parallel.yml
@.github/workflows/platform-retry.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create platform retry router script</name>
  <files>backend/tests/scripts/platform_retry_router.py</files>
  <action>
    Create backend/tests/scripts/platform_retry_router.py with shebang and docstring:

    Add these functions (following detect_flaky_tests.py pattern):
    1. load_json(file_path: str) -> Dict[str, Any] - Load JSON with error handling
    2. extract_failed_tests(results: Dict, platform: str) -> List[str]
       - Parse pytest format: extract from summary.failed field (list of test names)
       - Parse Jest format: extract from testResults (find tests with status: 'failed')
       - Parse cargo format: extract from testResults (find tests with passed: false)
       - Return list of failed test names

    3. generate_retry_command(failed_tests: List[str], platform: str) -> str
       - Backend: return "pytest tests/ -v " + " ".join(failed_tests)
       - Frontend/Mobile: return "jest --testNamePattern=\"" + "|".join(escaped_names) + "\""
       - Desktop: return "cargo test " + " ".join(test_names)
       - Return empty string if no failed tests

    4. main() with ArgumentParser:
       - --platform: Required (backend|frontend|mobile|desktop)
       - --results-file: Required (path to test results JSON)
       - --output-file: Required (path to write retry command)
       - Load results, extract failed tests, generate command, write to output file
       - Exit code 0 if tests to retry, exit code 3 if no failures (special code for workflow)
    </action>
    <verify>
    python3 backend/tests/scripts/platform_retry_router.py --help
  </verify>
  <done>
    Script exists with extract_failed_tests() handling pytest/Jest/cargo formats, generate_retry_command() creating platform-specific commands, CLI accepts --platform/--results-file/--output-file
  </done>
</task>

<task type="auto">
  <name>Task 2: Create retry workflow skeleton with detect-failures job</name>
  <files>.github/workflows/platform-retry.yml</files>
  <action>
    Create .github/workflows/platform-retry.yml with:
    - Trigger: workflow_run on workflows: [unified-tests-parallel], types: [completed], branches: [main, develop]
    - detect-failures job:
      - runs-on: ubuntu-latest
      - if: ${{ github.event.workflow_run.conclusion == 'failure' }}
      - outputs: backend-failed, frontend-failed, mobile-failed, desktop-failed
      - Steps:
        1. Download workflow artifacts using actions/github-script@v7 (list artifacts from workflow_run.id, download each platform's test-results)
        2. Run platform_retry_router.py for each platform (if artifact exists), set output to "true" if failed tests found, "false" otherwise
        3. Upload retry commands as artifacts for downstream jobs

    Use github-script to list and download artifacts from the triggering workflow run

    DO NOT call reusable workflows - this workflow must detect failures and trigger re-runs independently
  </action>
    <verify>
    grep -E "workflow_run:|detect-failures:|github.event.workflow_run.conclusion" .github/workflows/platform-retry.yml
  </verify>
  <done>
    Workflow triggers on unified-tests-parallel failure, detect-failures job downloads artifacts and runs platform_retry_router.py for each platform
  </done>
</task>

<task type="auto">
  <name>Task 3: Add conditional platform retry jobs</name>
  <files>.github/workflows/platform-retry.yml</files>
  <action>
    Add 4 retry jobs to platform-retry.yml (retry-backend, retry-frontend, retry-mobile, retry-desktop):
    - Each job:
      - needs: [detect-failures]
      - if: ${{ needs.detect-failures.outputs.{platform}-failed == 'true' }}
      - runs-on: ubuntu-latest
      - timeout-minutes: 20 (shorter than initial run since fewer tests)
      - Steps:
        1. Checkout code (actions/checkout@v4)
        2. Setup platform (Python/Node.js/Rust based on platform)
        3. Download retry command artifact
        4. Run retry command (execute command from artifact)
        5. Upload retry results artifact
        6. Comment on PR with retry results (if original run was from PR)

    For backend: use same setup as unified-tests-parallel.yml backend job
    For frontend: use npm ci and npm run test:ci with retry command
    For mobile: use npm ci and npm run test:ci with retry command
    For desktop: use cargo test with specific test names

    Use matrix if jobs share identical structure (can simplify 4 jobs into 1 with matrix)
  </action>
  <verify>
    grep -E "retry-(backend|frontend|mobile|desktop):|needs.*detect-failures|if.*outputs.*failed == 'true'" .github/workflows/platform-retry.yml
  </verify>
  <done>
    Four conditional retry jobs (or 1 matrix job with 4 platforms), each runs only if its platform failed, executes targeted re-run of failed tests
  </done>
</task>

</tasks>

<verification>
1. Workflow syntax valid: `yamllint .github/workflows/platform-retry.yml`
2. Platform retry router handles all formats: Test with sample pytest/Jest/cargo JSON
3. Detect-failures job sets correct outputs: Check workflow sets {platform}-failed to true/false
4. Retry jobs conditional on failed platform: Verify `if: needs.detect-failures.outputs.{platform}-failed == 'true'`
5. Retry commands are targeted: Verify commands only include failed tests (not full suite)
</verification>

<success_criteria>
1. platform-retry.yml workflow triggers on unified-tests-parallel failure
2. detect-failures job identifies failed platforms by parsing artifacts
3. platform_retry_router.py extracts failed test names and generates platform-specific retry commands
4. Conditional retry jobs only run for failed platforms
5. Retry jobs execute targeted test re-runs (not full suite)
</success_criteria>

<output>
After completion, create `.planning/phases/149-quality-infrastructure-parallel/149-03-SUMMARY.md`
</output>
