---
phase: 200-fix-collection-errors
plan: 04
type: execute
wave: 2
depends_on: [200-01, 200-02, 200-03]
files_modified:
  - backend/pytest.ini
autonomous: true

must_haves:
  truths:
    - "pytest collection completes with zero errors"
    - "Test count is documented and stable"
    - "All exclude patterns are documented in pytest.ini comments"
  artifacts:
    - path: backend/pytest.ini
      provides: documented pytest configuration
      contains: comments explaining each --ignore pattern
  key_links:
    - from: pytest collection
      to: zero errors
      via: proper configuration
      pattern: "0 errors in"
---

<objective>
Verify and document zero collection errors with complete pytest.ini configuration comments.

**Purpose:** After Plans 01-03 exclude problematic tests, verify that pytest collection truly achieves zero errors. Add comments to pytest.ini explaining each --ignore pattern for future maintainability. Document the final test count and confirm the configuration is stable.

**Output:** pytest.ini with documented configuration, verified zero collection errors, documented test count.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/200-fix-collection-errors/200-RESEARCH.md
</execution_context>

<context>
@backend/pytest.ini
@.planning/phases/200-fix-collection-errors/200-01-SUMMARY.md
@.planning/phases/200-fix-collection-errors/200-02-SUMMARY.md
@.planning/phases/200-fix-collection-errors/200-03-SUMMARY.md

# Expected State After Plans 01-03:
# - tests/contract/ excluded (Schemathesis hook incompatibility)
# - 3 duplicate test files deleted (import file mismatch)
# - 5 problematic test files excluded (issubclass/import errors)
# - Collection errors: 0 (from 10)
# - Tests collected: ~22,500 (stable)

# pytest.ini addopts should contain:
# --ignore=archive/ (legacy project structure)
# --ignore=frontend-nextjs/ (frontend tests, separate run)
# --ignore=scripts/ (script tests, separate run)
# --ignore=tests/contract/ (Schemathesis hook incompatibility)
# --ignore=tests/api/test_api_routes_coverage.py (import error)
# --ignore=tests/api/test_feedback_analytics.py (import error)
# --ignore=tests/api/test_feedback_enhanced.py (import error)
# --ignore=tests/core/test_agent_governance_service_coverage_extend.py (import error)
# --ignore=tests/core/test_agent_governance_service_coverage_final.py (import error)
</context>

<tasks>

<task type="auto">
  <name>Document pytest.ini ignore patterns with comments</name>
  <files>backend/pytest.ini</files>
  <action>
    Add comments to backend/pytest.ini explaining each --ignore pattern in the addopts line.

    Create a new comment section above the addopts line that documents each ignore pattern:

    # Test Collection Ignore Patterns (Phase 200: Fix Collection Errors)
    # --ignore=archive/: Legacy project structure, deprecated tests
    # --ignore=frontend-nextjs/: Frontend tests run separately via Next.js test runner
    # --ignore=scripts/: Utility scripts with their own test infrastructure
    # --ignore=tests/contract/: Schemathesis hook incompatibility (deprecated @schemathesis.hook names)
    # --ignore=tests/api/test_api_routes_coverage.py: Import-time issubclass() error during full collection
    # --ignore=tests/api/test_feedback_analytics.py: Import-time issubclass() error during full collection
    # --ignore=tests/api/test_feedback_enhanced.py: Import-time issubclass() error during full collection
    # --ignore=tests/core/test_agent_governance_service_coverage_extend.py: Import-time error during full collection
    # --ignore=tests/core/test_agent_governance_service_coverage_final.py: Import-time error during full collection

    Keep all existing --ignore patterns in the addopts line.
    Do NOT modify any other parts of pytest.ini.
  </action>
  <verify>grep -c "ignore" backend/pytest.ini | grep -q "^9"</verify>
  <done>All 9 ignore patterns are documented with comments in pytest.ini</done>
</task>

<task type="auto">
  <name>Verify zero collection errors stable across multiple runs</name>
  <files>backend/pytest.ini</files>
  <action>
    Verify that pytest collection consistently achieves zero errors across multiple runs.

    Run collection 3 times and verify each shows 0 errors:
    1. python3 -m pytest backend/tests/ --collect-only -q 2>&1 | tail -1
    2. python3 -m pytest backend/tests/ --collect-only -q 2>&1 | tail -1
    3. python3 -m pytest backend/tests/ --collect-only -q 2>&1 | tail -1

    Expected for all runs: "XXXX tests collected, 0 errors in X.XXs"

    Also verify test count is stable (should not vary between runs):
    The test count (XXXX) should be identical across all 3 runs.

    If test count varies, there may be a non-deterministic test discovery issue.
  </action>
  <verify>for i in 1 2 3; do python3 -m pytest backend/tests/ --collect-only -q 2>&1 | tail -1; done</verify>
  <done>All 3 runs show identical test count and 0 errors</done>
</task>

<task type="auto">
  <name>Document final test count and configuration</name>
  <files>.planning/phases/200-fix-collection-errors/200-04-PLAN.md</files>
  <action>
    Create a summary document with the final test collection state.

    Create (or append to) a summary with:
    1. Final test count (from collection output)
    2. Confirmation of 0 collection errors
    3. List of all ignore patterns in pytest.ini
    4. Files excluded and why

    Run: python3 -m pytest backend/tests/ --collect-only -q 2>&1

    Capture and document:
    - Total tests collected
    - Collection time
    - Error count (should be 0)

    This documentation serves as the baseline for future coverage work.
  </action>
  <verify>python3 -m pytest backend/tests/ --collect-only -q 2>&1 | grep "errors in"</verify>
  <done>Summary document created with final test count and 0 errors confirmed</done>
</task>

</tasks>

<verification>
1. pytest.ini has comments for all 9 ignore patterns
2. pytest --collect-only shows 0 errors consistently across 3 runs
3. Test count is stable (identical across all runs)
4. Summary document created with final configuration state
</verification>

<success_criteria>
- pytest.ini ignore patterns fully documented with comments
- Zero collection errors achieved and stable
- Test count documented and stable across runs
- Phase 200 configuration baseline established
</success_criteria>

<output>
After completion, create `.planning/phases/200-fix-collection-errors/200-04-SUMMARY.md`
</output>
