---
phase: 200-fix-collection-errors
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_atom_agent_endpoints_coverage.py
  - backend/tests/core/systems/test_embedding_service_coverage.py
  - backend/tests/core/systems/test_integration_data_mapper_coverage.py
autonomous: true

must_haves:
  truths:
    - "Duplicate test files are removed from tests/ and tests/core/systems/ directories"
    - "pytest collection no longer reports import file mismatch errors"
    - "Test files exist only in their canonical locations (core/agents/, core/integration/)"
  artifacts:
    - path: backend/tests/test_atom_agent_endpoints_coverage.py
      provides: duplicate file deleted (canonical in core/agents/)
      exists: false
    - path: backend/tests/core/systems/test_embedding_service_coverage.py
      provides: duplicate file deleted (canonical in core/agents/)
      exists: false
    - path: backend/tests/core/systems/test_integration_data_mapper_coverage.py
      provides: duplicate file deleted (canonical in core/integration/)
      exists: false
  key_links:
    - from: pytest collection
      to: unique test files
      via: file deletion
      pattern: no duplicate module errors
---

<objective>
Remove duplicate test files causing import file mismatch errors during pytest collection.

**Purpose:** Four test files exist in multiple directories, causing pytest to report "import file mismatch" errors. The canonical locations are: core/agents/ for agent endpoint and embedding tests, core/integration/ for integration mapper tests, and core/agent_endpoints/ for the newer agent endpoints test. Removing the older duplicates eliminates 4 collection errors.

**Output:** Deleted duplicate test files, pytest collection shows 5 errors (down from 9).
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/200-fix-collection-errors/200-RESEARCH.md
</execution_context>

<context>
@backend/tests/
@backend/tests/core/agents/
@backend/tests/core/agent_endpoints/
@backend/tests/core/systems/
@backend/tests/core/integration/

# Duplicate Files Found:
# 1. test_atom_agent_endpoints_coverage.py:
#    - tests/test_atom_agent_endpoints_coverage.py (DUPLICATE - DELETE)
#    - tests/core/agents/test_atom_agent_endpoints_coverage.py (KEEP)
#    - tests/core/agent_endpoints/test_atom_agent_endpoints_coverage.py (KEEP - newer)
#
# 2. test_embedding_service_coverage.py:
#    - tests/core/systems/test_embedding_service_coverage.py (DUPLICATE - DELETE)
#    - tests/core/agents/test_embedding_service_coverage.py (KEEP)
#
# 3. test_integration_data_mapper_coverage.py:
#    - tests/core/systems/test_integration_data_mapper_coverage.py (DUPLICATE - DELETE)
#    - tests/core/integration/test_integration_data_mapper_coverage.py (KEEP)

# Error Messages:
# ERROR collecting tests/core/agents/test_atom_agent_endpoints_coverage.py
# import file mismatch:
# imported module 'test_atom_agent_endpoints_coverage' has this __file__ attribute:
#   /Users/rushiparikh/projects/atom/backend/tests/core/agent_endpoints/test_atom_agent_endpoints_coverage.py
</context>

<tasks>

<task type="auto">
  <name>Delete duplicate test_atom_agent_endpoints_coverage.py from tests/ root</name>
  <files>backend/tests/test_atom_agent_endpoints_coverage.py</files>
  <action>
    Delete the duplicate test file from the tests/ root directory.

    File to delete: backend/tests/test_atom_agent_endpoints_coverage.py

    The canonical versions are in:
    - tests/core/agents/test_atom_agent_endpoints_coverage.py (older, keep)
    - tests/core/agent_endpoints/test_atom_agent_endpoints_coverage.py (newer, keep)

    Command: rm /Users/rushiparikh/projects/atom/backend/tests/test_atom_agent_endpoints_coverage.py

    Verify deletion: ls backend/tests/test_atom_agent_endpoints_coverage.py should return "No such file or directory"
  </action>
  <verify>ls backend/tests/test_atom_agent_endpoints_coverage.py 2>&1 | grep -q "No such file"</verify>
  <done>File deleted, pytest collection no longer shows import mismatch for this module</done>
</task>

<task type="auto">
  <name>Delete duplicate test_embedding_service_coverage.py from systems/</name>
  <files>backend/tests/core/systems/test_embedding_service_coverage.py</files>
  <action>
    Delete the duplicate test file from tests/core/systems/.

    File to delete: backend/tests/core/systems/test_embedding_service_coverage.py

    Canonical version: tests/core/agents/test_embedding_service_coverage.py

    Command: rm /Users/rushiparikh/projects/atom/backend/tests/core/systems/test_embedding_service_coverage.py

    Verify deletion: ls backend/tests/core/systems/test_embedding_service_coverage.py should return "No such file or directory"
  </action>
  <verify>ls backend/tests/core/systems/test_embedding_service_coverage.py 2>&1 | grep -q "No such file"</verify>
  <done>File deleted, pytest collection no longer shows import mismatch for this module</done>
</task>

<task type="auto">
  <name>Delete duplicate test_integration_data_mapper_coverage.py from systems/</name>
  <files>backend/tests/core/systems/test_integration_data_mapper_coverage.py</files>
  <action>
    Delete the duplicate test file from tests/core/systems/.

    File to delete: backend/tests/core/systems/test_integration_data_mapper_coverage.py

    Canonical version: tests/core/integration/test_integration_data_mapper_coverage.py

    Command: rm /Users/rushiparikh/projects/atom/backend/tests/core/systems/test_integration_data_mapper_coverage.py

    Verify deletion: ls backend/tests/core/systems/test_integration_data_mapper_coverage.py should return "No such file or directory"
  </action>
  <verify>ls backend/tests/core/systems/test_integration_data_mapper_coverage.py 2>&1 | grep -q "No such file"</verify>
  <done>File deleted, pytest collection no longer shows import mismatch for this module</done>
</task>

<task type="auto">
  <name>Verify collection errors reduced</name>
  <files>backend/pytest.ini</files>
  <action>
    Verify that the import file mismatch errors are resolved by running pytest collection.

    Run: python3 -m pytest backend/tests/ --collect-only -q 2>&1 | tail -1

    Expected: "XXXX tests collected, 5 errors in X.XXs" (errors reduced from 9 to 5)

    Also verify no import file mismatch errors remain:
    Run: python3 -m pytest backend/tests/ --collect-only 2>&1 | grep -i "import file mismatch"

    Expected: No output (no import file mismatch errors)
  </action>
  <verify>python3 -m pytest backend/tests/ --collect-only -q 2>&1 | tail -1</verify>
  <done>Collection shows 5 errors (down from 9), and no import file mismatch errors appear</done>
</task>

</tasks>

<verification>
1. backend/tests/test_atom_agent_endpoints_coverage.py deleted
2. backend/tests/core/systems/test_embedding_service_coverage.py deleted
3. backend/tests/core/systems/test_integration_data_mapper_coverage.py deleted
4. pytest --collect-only shows 5 errors (down from 9)
5. No "import file mismatch" errors appear in collection output
</verification>

<success_criteria>
- All three duplicate test files deleted
- Import file mismatch errors eliminated from collection
- No regression in test collection (canonical files still collect)
- Collection error count reduced by 4
</success_criteria>

<output>
After completion, create `.planning/phases/200-fix-collection-errors/200-02-SUMMARY.md`
</output>
