---
phase: 200-fix-collection-errors
plan: 05
type: execute
wave: 2
depends_on: [200-04]
files_modified:
  - backend/coverage.json
  - .planning/phases/200-fix-collection-errors/200-05-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "Coverage is measured accurately with zero collection errors"
    - "Baseline coverage documented for comparison"
    - "Coverage gain from Phase 200 is calculated"
  artifacts:
    - path: backend/coverage.json
      provides: actual coverage measurement
      contains: overall percent_covered value
    - path: .planning/phases/200-fix-collection-errors/200-05-SUMMARY.md
      provides: coverage analysis and baseline documentation
  key_links:
    - from: zero collection errors
      to: accurate coverage measurement
      via: pytest --cov
      pattern: coverage.py successfully measures all collectible tests
---

<objective>
Measure actual coverage with zero collection errors and establish baseline for future phases.

**Purpose:** With collection errors fixed, coverage.py can now accurately measure backend coverage. Run a full coverage measurement to establish the true baseline, calculate any gains from unblocking tests, and document the gap to the 85% target.

**Output:** coverage.json with actual coverage percentage, documented baseline, gap analysis to 85% target.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/200-fix-collection-errors/200-RESEARCH.md
</execution_context>

<context>
@backend/pytest.ini
@backend/coverage.json
@.planning/phases/200-fix-collection-errors/200-04-SUMMARY.md

# Baseline (Before Phase 200):
# - Collection errors: 10
# - Overall coverage: 74.6%
# - Tests collected: 22,595 (but with errors, measurement may be incomplete)

# Expected (After Phase 200):
# - Collection errors: 0
# - Overall coverage: 75-76% (slight gain from excluding broken tests)
# - Tests collected: ~22,500 (stable, all collectible)

# Note: The coverage gain from Phase 200 is expected to be minimal (0-1%)
# because we're excluding problematic tests rather than fixing them.
# The main benefit is accurate coverage measurement going forward.
</context>

<tasks>

<task type="auto">
  <name>Run full coverage measurement with zero errors</name>
  <files>backend/coverage.json</files>
  <action>
    Generate a complete coverage report with pytest-cov.

    Run: python3 -m pytest backend/tests/ --cov=backend --cov-branch --cov-report=json --cov-report=term-missing -q

    This will:
    1. Run all collectible tests (~22,500 tests)
    2. Measure line and branch coverage
    3. Generate coverage.json report
    4. Display missing lines in terminal output

    Capture the terminal output, especially:
    - Overall coverage percentage (e.g., "TOTAL 75.3%")
    - Coverage by module
    - Any warnings or issues

    The coverage.json file will be generated in the current working directory
    (/Users/rushiparikh/projects/atom/backend/coverage.json or project root).
  </action>
  <verify>cat coverage.json | python3 -c "import json,sys; d=json.load(sys.stdin); print(f'Coverage: {d[\"totals\"][\"percent_covered\"]}%')"</verify>
  <done>coverage.json generated with valid percent_covered value</done>
</task>

<task type="auto">
  <name>Analyze coverage and document baseline</name>
  <files>.planning/phases/200-fix-collection-errors/200-05-SUMMARY.md</files>
  <action>
    Analyze the coverage results and create a baseline documentation.

    From coverage.json, extract and document:
    1. Overall coverage percentage (totals.percent_covered)
    2. Lines covered vs total (totals.covered_lines / totals.num_statements)
    3. Branch coverage if available (totals.percent_covered_display)
    4. Coverage by key module (agent_governance_service, trigger_interceptor, etc.)

    Compare to Phase 199 baseline (74.6%) and calculate:
    - Coverage gain/loss from Phase 200
    - Gap to 85% target (85% - current_coverage)

    Document findings in 200-05-SUMMARY.md:
    ```markdown
    # Phase 200 Coverage Baseline

    ## Overall Coverage
    - Current: XX.X%
    - Phase 199 baseline: 74.6%
    - Change: +/-X.X%
    - Gap to 85% target: X.X%

    ## Key Modules
    - agent_governance_service.py: XX%
    - trigger_interceptor.py: XX%
    - [other key modules]

    ## Collection Errors
    - Before Phase 200: 10
    - After Phase 200: 0

    ## Next Steps
    - Phase 201: Target remaining X.X% gap to 85%
    - Focus areas for coverage improvement
    ```

    If coverage.json is in backend/ subdirectory, adjust path accordingly.
  </action>
  <verify>ls -la .planning/phases/200-fix-collection-errors/200-05-SUMMARY.md</verify>
  <done>Summary document created with coverage baseline and gap analysis</done>
</task>

<task type="auto">
  <name>Generate coverage by module report</name>
  <files>backend/coverage.json</files>
  <action>
    Generate a per-module coverage breakdown to identify low-coverage areas.

    From coverage.json, extract coverage for each file and rank by:
    1. Lowest coverage (priorities for improvement)
    2. Most lines uncovered (high-impact targets)

    Create a list of modules below 85% coverage with their current percentage
    and gap to target. Add this to the 200-05-SUMMARY.md.

    Format:
    ```
    ## Modules Below 85% Target (Prioritized)

    | Module | Coverage | Gap | Priority |
    |--------|----------|-----|----------|
    | file.py | 45.2% | 39.8% | HIGH |
    | ...
    ```

    This prioritization will guide Phase 201 coverage improvement work.
  </action>
  <verify>grep "Modules Below" .planning/phases/200-fix-collection-errors/200-05-SUMMARY.md</verify>
  <done>Module coverage breakdown included in summary document</done>
</task>

</tasks>

<verification>
1. coverage.json exists with valid data
2. Overall coverage percentage extracted and documented
3. 200-05-SUMMARY.md created with baseline analysis
4. Module-level coverage breakdown included
5. Gap to 85% target calculated
</verification>

<success_criteria>
- Coverage measured accurately with zero collection errors
- Baseline coverage documented for comparison
- Gap to 85% target calculated
- Module-level priorities identified for next phase
- Phase 200 achievements summarized
</success_criteria>

<output>
After completion, create `.planning/phases/200-fix-collection-errors/200-05-SUMMARY.md`
</output>
