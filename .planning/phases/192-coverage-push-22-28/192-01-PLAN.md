---
phase: 192-coverage-push-22-28
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "core/workflow_engine.py"
  - "tests/core/workflow/test_workflow_engine_coverage_fix.py"
autonomous: true
must_haves:
  truths:
    - "WorkflowEngine import blocker fixed (WorkflowStepExecution -> WorkflowExecutionLog)"
    - "Coverage increases from 0% to 60%+ (1,163 statements target ~700 covered)"
    - "Tests cover initialization, validation, step conversion, and error handling"
    - "No ImportError when importing workflow_engine module"
  artifacts:
    - path: "tests/core/workflow/test_workflow_engine_coverage_fix.py"
      provides: "WorkflowEngine coverage tests with import fixes"
      min_lines: 350
      tests: "~30 tests covering initialization, validation, step conversion"
    - path: "core/workflow_engine.py"
      contains: "WorkflowExecutionLog import (not WorkflowStepExecution)"
  key_links:
    - from: "tests/core/workflow/test_workflow_engine_coverage_fix.py"
      to: "core/workflow_engine.py"
      via: "Direct import after fix"
      pattern: "from core.workflow_engine import WorkflowEngine"
---

# Plan 192-01: Fix WorkflowEngine Import Blocker & Coverage

## Objective
Fix the critical import blocker preventing WorkflowEngine from being tested (WorkflowStepExecution should be WorkflowExecutionLog), then create comprehensive coverage tests to achieve 60%+ coverage on this 1,163-statement file.

## Purpose
WorkflowEngine is the largest zero-coverage file (1,163 statements) and has a critical import bug that blocks all testing. Fixing this blocker and adding coverage tests will provide the biggest single-file coverage impact in Phase 192.

## Output
- Fixed import in workflow_engine.py (line 30)
- 350+ lines of coverage tests
- ~30 tests covering initialization, validation, step conversion, error handling
- 60%+ coverage achieved on workflow_engine.py

## Context
@.planning/phases/192-coverage-push-22-28/192-RESEARCH.md
@.planning/phases/192-coverage-push-22-28/coverage_actual.json

# Prior Work
@tests/core/workflow/test_workflow_engine_coverage_extend.py - Previous attempt at coverage with VALIDATED_BUG documented
@tests/core/workflow/test_workflow_system_coverage.py - Related workflow system tests

## Tasks

<task type="auto">
  <name>Task 1: Fix WorkflowEngine Import Blocker</name>
  <files>core/workflow_engine.py</files>
  <action>
    Fix the import error on line 30 of workflow_engine.py:
    - Current (incorrect): from .models import WorkflowStepExecution
    - Correct: from .models import WorkflowExecutionLog

    This is a documented VALIDATED_BUG from previous test attempts. The correct model is WorkflowExecutionLog, not WorkflowStepExecution.

    Steps:
    1. Open core/workflow_engine.py
    2. Find line 30 with the WorkflowStepExecution import
    3. Replace with WorkflowExecutionLog
    4. Search for any other references to WorkflowStepExecution in the file and replace with WorkflowExecutionLog
    5. Verify the fix by running: python3 -c "from core.workflow_engine import WorkflowEngine; print('Import successful')"
  </action>
  <verify>python3 -c "from core.workflow_engine import WorkflowEngine; print('Import successful')"</verify>
  <done>WorkflowEngine imports without ImportError, no references to WorkflowStepExecution remain</done>
</task>

<task type="auto">
  <name>Task 2: Create WorkflowEngine Coverage Test File</name>
  <files>tests/core/workflow/test_workflow_engine_coverage_fix.py</files>
  <action>
    Create tests/core/workflow/test_workflow_engine_coverage_fix.py with comprehensive coverage tests:

    File structure (350+ lines):
    - Module docstring explaining coverage target (0% -> 60%+)
    - TestWorkflowEngineCoverageFix class
    - Parametrized tests for workflow status handling
    - Tests for initialization with various configs
    - Tests for node-to-step conversion (linear, diamond, complex graphs)
    - Tests for conditional connection detection
    - Tests for dependency validation
    - Tests for condition evaluation
    - Tests for parameter resolution
    - Tests for value extraction from paths
    - Tests for schema validation
    - Edge case tests (empty workflows, invalid configs, circular dependencies)

    Test pattern (from Phase 191):
    ```python
    class TestWorkflowEngineCoverageFix:
        '''Coverage-driven tests for workflow_engine.py (0% -> 60%+ target)'''

        def test_engine_initialization_default(self, db_session):
            '''Cover lines 37-43: Default initialization'''
            from core.workflow_engine import WorkflowEngine
            engine = WorkflowEngine()
            assert engine.max_concurrent_steps == 5
            assert engine.semaphore._value == 5

        @pytest.mark.parametrize("max_concurrent,expected_value", [
            (1, 1), (5, 5), (10, 10), (20, 20)
        ])
        def test_engine_initialization_custom_concurrency(self, max_concurrent, expected_value):
            '''Cover initialization with custom concurrency'''
            engine = WorkflowEngine(max_concurrent_steps=max_concurrent)
            assert engine.max_concurrent_steps == expected_value
    ```

    Coverage target areas:
    - Lines 37-100: Initialization and config
    - Lines 100-250: Validation methods
    - Lines 250-400: Step conversion logic
    - Lines 400-600: Error handling paths
    - Lines 600-800: State management

    Note: Skip _execute_workflow_graph async method (261 statements) due to complexity.
    Focus on testable synchronous methods.
  </action>
  <verify>pytest tests/core/workflow/test_workflow_engine_coverage_fix.py -v --cov=core/workflow_engine --cov-report=term-missing</verify>
  <done>30+ tests passing, 60%+ coverage achieved on workflow_engine.py</done>
</task>

<task type="auto">
  <name>Task 3: Verify WorkflowEngine Coverage & Generate Report</name>
  <files>tests/core/workflow/test_workflow_engine_coverage_fix.py</files>
  <action>
    Run coverage measurement and verify the 60%+ target is achieved:

    1. Run pytest with coverage:
       pytest tests/core/workflow/test_workflow_engine_coverage_fix.py \
         --cov=core/workflow_engine \
         --cov-branch \
         --cov-report=term-missing \
         --cov-report=html \
         -v

    2. Check coverage percentage is 60% or higher

    3. If below 60%, identify uncovered line ranges from terminal output and add targeted tests

    4. Generate coverage report snippet for phase summary:
       - Document starting coverage (0%)
       - Document ending coverage (achieved %)
       - Document number of statements covered
       - Document number of tests added

    Acceptable outcomes:
    - 60%+ coverage: Plan complete
    - 50-59% coverage: Add more tests for missing lines
    - <50% coverage: May need to accept complex async methods uncovered
  </action>
  <verify>pytest tests/core/workflow/test_workflow_engine_coverage_fix.py --cov=core/workflow_engine --cov-report=term | grep -E "TOTAL|workflow_engine.py"</verify>
  <done>Coverage 60%+ achieved, all tests passing, coverage report generated</done>
</task>

## Verification

### Coverage Measurement
```bash
pytest tests/core/workflow/test_workflow_engine_coverage_fix.py \
  --cov=core/workflow_engine \
  --cov-branch \
  --cov-report=term-missing \
  --cov-report=json:.planning/phases/192-coverage-push-22-28/192-01-coverage.json
```

### Success Criteria
- [ ] ImportError fixed (WorkflowStepExecution -> WorkflowExecutionLog)
- [ ] 30+ tests created and passing
- [ ] 60%+ coverage achieved on workflow_engine.py
- [ ] Coverage report generated at 192-01-coverage.json
- [ ] No test collection errors

### Quality Checks
- [ ] Tests use proper fixtures (db_session, mocker)
- [ ] Tests follow Phase 191 patterns (parametrization, coverage-driven naming)
- [ ] Edge cases tested (empty configs, invalid inputs, circular dependencies)
- [ ] Complex async methods documented as skipped if necessary

## Success Criteria
- WorkflowEngine import blocker fixed
- 60%+ coverage achieved (700+ of 1,163 statements covered)
- 30+ tests created following Phase 191 patterns
- All tests passing with no collection errors

## Output
After completion, create `.planning/phases/192-coverage-push-22-28/192-01-SUMMARY.md` with:
- Starting coverage (0%)
- Ending coverage (achieved %)
- Tests created count
- Import fix details
- Any complex methods that remain uncovered
