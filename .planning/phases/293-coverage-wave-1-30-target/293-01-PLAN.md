---
phase: 293-coverage-wave-1-30-target
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_workflow_analytics_endpoints.py
  - backend/tests/test_workflow_parameter_validator.py
  - backend/tests/test_maturity_routes.py
  - backend/tests/test_supervisor_learning_service.py
  - backend/tests/test_feedback_service.py
autonomous: true
requirements:
  - COV-B-02
user_setup: []
must_haves:
  truths:
    - "Backend Tier 1 high-priority files have test coverage >= 30%"
    - "All new tests pass without requiring external services (mock-based)"
    - "Coverage trend tracker shows improved Tier 1 file coverage"
  artifacts:
    - path: "backend/tests/test_workflow_analytics_endpoints.py"
      provides: "Tests for workflow_analytics_endpoints.py (0% coverage, 314 lines, priority 1570)"
      min_assertions: 20
    - path: "backend/tests/test_workflow_parameter_validator.py"
      provides: "Tests for workflow_parameter_validator.py (0% coverage, 286 lines, priority 1430)"
      min_assertions: 15
    - path: "backend/tests/test_maturity_routes.py"
      provides: "Tests for maturity_routes.py (0% coverage, 214 lines, priority 1070)"
      min_assertions: 15
    - path: "backend/tests/test_supervisor_learning_service.py"
      provides: "Tests for supervisor_learning_service.py (0% coverage, 212 lines, priority 1060)"
      min_assertions: 15
    - path: "backend/tests/test_feedback_service.py"
      provides: "Tests for feedback_service.py (2.28% coverage, 219 lines, priority 326)"
      min_assertions: 15
  key_links:
    - from: "backend/tests/test_workflow_analytics_endpoints.py"
      to: "backend/core/workflow_analytics_endpoints.py"
      via: "Test client against router endpoints"
      pattern: "from core.workflow_analytics_endpoints import"
    - from: "backend/tests/test_workflow_parameter_validator.py"
      to: "backend/core/workflow_parameter_validator.py"
      via: "Direct unit tests with pure function calls"
      pattern: "from core.workflow_parameter_validator import"
    - from: "backend/tests/test_maturity_routes.py"
      to: "backend/api/maturity_routes.py"
      via: "Test client against router endpoints"
      pattern: "from api.maturity_routes import"
    - from: "backend/tests/test_supervisor_learning_service.py"
      to: "backend/core/supervisor_learning_service.py"
      via: "Direct unit tests with mocked db session"
      pattern: "SupervisorLearningService"
    - from: "backend/tests/test_feedback_service.py"
      to: "backend/core/feedback_service.py"
      via: "Direct unit tests with mocked db session"
      pattern: "FeedbackService"
---

<objective>
Backend Tier 1 high-impact file tests for workflow_analytics_endpoints, workflow_parameter_validator, maturity_routes, supervisor_learning_service, and feedback_service.

Purpose: These 5 files are the highest-priority backend files from Phase 292's Tier 1 list (all at 0-2.28% coverage, >200 lines). Testing them builds confidence in critical code paths and closes the largest backend coverage gaps.

Output: 5 new test files in backend/tests/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
Existing test patterns from backend/tests/conftest.py:
- MockLLMProvider, MockEmbeddingService, MockCacheService, MockWebSocket in tests/fixtures/mock_services.py
- pytest-asyncio via plugin registration
- Session mocking via MagicMock/AsyncMock
- Path insertion: sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))

Common test patterns found in existing tests (grep confirms):
- `from unittest.mock import MagicMock, AsyncMock, patch, PropertyMock`
- `with patch.object(ClassName, 'method_name') as mock_method`
- `db = MagicMock(spec=Session)` for database mocking
- `pytest.mark.asyncio` for async test functions
- `TestClient` from FastAPI for route testing: `from fastapi.testclient import TestClient`

Source files under test (read before writing tests):
- backend/core/workflow_analytics_endpoints.py - FastAPI router with workflow analytics endpoints
- backend/core/workflow_parameter_validator.py - Parameter validation logic
- backend/api/maturity_routes.py - FastAPI router for maturity level routes
- backend/core/supervisor_learning_service.py - SupervisorLearningService class with SQLAlchemy
- backend/core/feedback_service.py - FeedbackService class with SQLAlchemy
- backend/core/models.py - Database models used by these services
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests for workflow_analytics_endpoints and workflow_parameter_validator</name>
  <files>
    backend/tests/test_workflow_analytics_endpoints.py
    backend/tests/test_workflow_parameter_validator.py
  </files>
  <read_first>
    /Users/rushiparikh/projects/atom/backend/core/workflow_analytics_endpoints.py
    /Users/rushiparikh/projects/atom/backend/core/workflow_parameter_validator.py
    /Users/rushiparikh/projects/atom/backend/core/models.py (AgentExecution, Dashboard, DashboardWidget models)
    /Users/rushiparikh/projects/atom/backend/tests/test_api_agent_endpoints.py (reference for FastAPI route testing pattern)
    /Users/rushiparikh/projects/atom/backend/tests/test_agent_execution_service.py (reference for service class unit test pattern)
  </read_first>
  <action>
    Create two test files following existing patterns:

    **test_workflow_analytics_endpoints.py** (core/workflow_analytics_endpoints.py):
    - Import router via `from core.workflow_analytics_endpoints import router`
    - Use `from fastapi.testclient import TestClient`
    - Create a mock app: `from fastapi import FastAPI` then `app = FastAPI(); app.include_router(router)`
    - Mock the `get_current_user` dependency to return a test user object with `{"sub": "test-user", "role": "admin"}`
    - Mock `get_db` dependency to return a MagicMock Session
    - Test at least: `POST /metrics/query` (MetricsQuery endpoint), `GET /metrics/summary/{workflow_id}`, `POST /alerts/configure` (AlertRequest endpoint), `POST /dashboards/create` (DashboardCreate endpoint)
    - Each test should verify: HTTP 200/201/4xx status, response JSON structure with expected fields, proper error handling for invalid inputs
    - Test the `serialize_alert()` helper function with both valid alerts and edge cases (missing fields, empty fields)
    - Use `MagicMock` for WorkflowAnalyticsEngine dependency

    **test_workflow_parameter_validator.py** (core/workflow_parameter_validator.py):
    - Import classes and functions from the module
    - Test parameter validation functions: valid parameter values, invalid parameter types, missing required parameters, boundary conditions (min/max ranges, empty strings, None values)
    - Test type coercion (string to int, string to bool, numeric overflow)
    - Test error handling: malformed parameter definitions, circular dependencies, invalid JSON schema
    - Use plain asserts (no db mocking needed if validation is pure logic)
    - If the validator uses external services, mock them with `@patch`
  </action>
  <behavior>
    - test_workflow_analytics_endpoints:
      - Test 1: POST /metrics/query with valid body returns 200 and expected JSON structure
      - Test 2: POST /metrics/query with missing required fields returns 422
      - Test 3: GET /metrics/summary/{workflow_id} returns 200 with summary structure
      - Test 4: POST /alerts/configure with valid AlertRequest returns 201 and alert_id
      - Test 5: POST /alerts/configure with invalid severity returns 422
      - Test 6: POST /dashboards/create with valid DashboardCreate returns 201
    - test_workflow_parameter_validator:
      - Test 1: validate_parameters() with valid params returns success
      - Test 2: validate_parameters() with missing required param returns error
      - Test 3: validate_parameters() with type mismatch returns validation error
      - Test 4: Boundary condition test (empty string, None, out-of-range) returns typed errors
  </behavior>
  <acceptance_criteria>
    - `backend/tests/test_workflow_analytics_endpoints.py` exists and contains `def test_workflow_analytics_metrics_query_success(` and `def test_workflow_analytics_alert_configure_success(` and `def test_workflow_analytics_dashboard_create_success(` and `def test_serialize_alert(`
    - `backend/tests/test_workflow_parameter_validator.py` exists and contains `def test_parameter_validator_valid_params(` and `def test_parameter_validator_missing_param(` and `def test_parameter_validator_type_mismatch(` and `def test_parameter_validator_boundary_conditions(`
    - `pytest backend/tests/test_workflow_analytics_endpoints.py -v --no-header 2>&1 | tail -20` shows all tests passed (exit code 0)
    - `pytest backend/tests/test_workflow_parameter_validator.py -v --no-header 2>&1 | tail -20` shows all tests passed (exit code 0)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rushiparikh/projects/atom/backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_workflow_analytics_endpoints.py tests/test_workflow_parameter_validator.py -v --no-header --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    Both test files exist, all tests pass (exit 0), test names include success and error cases
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests for maturity_routes, supervisor_learning_service, and feedback_service</name>
  <files>
    backend/tests/test_maturity_routes.py
    backend/tests/test_supervisor_learning_service.py
    backend/tests/test_feedback_service.py
  </files>
  <read_first>
    /Users/rushiparikh/projects/atom/backend/api/maturity_routes.py
    /Users/rushiparikh/projects/atom/backend/core/supervisor_learning_service.py
    /Users/rushiparikh/projects/atom/backend/core/feedback_service.py
    /Users/rushiparikh/projects/atom/backend/core/models.py (SupervisorPerformance, SupervisorRating, SupervisorComment, FeedbackVote, SupervisionSession, AgentProposal models)
    /Users/rushiparikh/projects/atom/backend/tests/test_api_agent_endpoints.py (reference for route mocking patterns)
    /Users/rushiparikh/projects/atom/backend/tests/test_agent_graduation.py (reference for async DB service testing patterns)
  </read_first>
  <action>
    Create three test files:

    **test_maturity_routes.py** (api/maturity_routes.py):
    - Import router via `from api.maturity_routes import router`
    - Use TestClient pattern from Task 1
    - Mock `get_current_user` and `get_db` dependencies
    - Test: GET maturity tiers listing, GET maturity level for an agent, PUT update maturity level, POST request maturity evaluation
    - Test error cases: invalid agent ID returns 404, invalid maturity level returns 422, unauthorized user returns 401/403
    - Use `MagicMock` for any database/engine dependencies

    **test_supervisor_learning_service.py** (core/supervisor_learning_service.py):
    - Import `SupervisorLearningService` class
    - Create `db = MagicMock(spec=Session)` instance
    - Create `service = SupervisorLearningService(db=db_mock)`
    - Mock db.query().filter().first() to return a MagicMock SupervisorPerformance object
    - Test: `process_feedback_for_learning()` with "rating" type (test all 5 rating levels 1-5 and verify confidence adjustments), "vote" type, "intervention_outcome" type
    - Test: unknown feedback_type logs warning and returns error dict
    - Test: `_get_or_create_performance()` creates new record when none exists
    - Test: `_process_rating()` applies correct RATING_BOOST values
    - Verify db.commit() is called after mutations

    **test_feedback_service.py** (core/feedback_service.py):
    - Import `FeedbackService` class
    - Create `db = MagicMock(spec=Session)` instance
    - Create `service = FeedbackService(db=db_mock)`
    - Test: `rate_supervisor()` with valid rating (1-5) returns SupervisorRating, with invalid rating (0, 6) raises ValueError
    - Test: `add_comment()` creates threaded comment with correct parent_id
    - Test: `vote_feedback()` (thumbs up/down) with valid vote_type returns FeedbackVote
    - Test: `get_supervisor_metrics()` aggregates ratings correctly (avg, count, distribution)
    - Test: `get_performance_trends()` returns time-series data
    - Test error handling: missing session_id, duplicate vote, invalid rating_category
  </action>
  <behavior>
    - test_maturity_routes:
      - Test 1: GET maturity tiers returns 200 with list of tier definitions
      - Test 2: GET maturity level for valid agent returns 200 with level data
      - Test 3: PUT maturity level with valid level returns 200
      - Test 4: PUT maturity level with invalid level returns 422
      - Test 5: GET maturity level for missing agent returns 404
    - test_supervisor_learning_service:
      - Test 1: process_feedback "rating" with rating=5 increases confidence by 0.05
      - Test 2: process_feedback "rating" with rating=1 decreases confidence by 0.05
      - Test 3: process_feedback "vote" updates performance
      - Test 4: process_feedback "intervention_outcome" updates performance
      - Test 5: process_feedback with unknown type returns error and logs warning
      - Test 6: _get_or_create_performance creates new record when not found
    - test_feedback_service:
      - Test 1: rate_supervisor with valid 1-5 rating creates SupervisorRating
      - Test 2: rate_supervisor with rating=0 raises ValueError
      - Test 3: add_comment creates comment with correct session_id
      - Test 4: vote_feedback creates FeedbackVote
      - Test 5: get_supervisor_metrics returns dict with avg_rating and total_ratings keys
  </behavior>
  <acceptance_criteria>
    - `backend/tests/test_maturity_routes.py` exists and contains `def test_maturity_routes_get_tiers(` and `def test_maturity_routes_get_agent_level(` and `def test_maturity_routes_update_level_success(` and `def test_maturity_routes_update_level_invalid(`
    - `backend/tests/test_supervisor_learning_service.py` exists and contains `def test_supervisor_learning_rating_boost(` and `def test_supervisor_learning_rating_penalty(` and `def test_supervisor_learning_intervention_outcome(` and `def test_supervisor_learning_unknown_type(`
    - `backend/tests/test_feedback_service.py` exists and contains `def test_feedback_service_rate_valid(` and `def test_feedback_service_rate_invalid(` and `def test_feedback_service_add_comment(` and `def test_feedback_service_get_metrics(`
    - `pytest backend/tests/test_maturity_routes.py backend/tests/test_supervisor_learning_service.py backend/tests/test_feedback_service.py -v --no-header --tb=short 2>&1 | tail -30` shows all tests passed (exit code 0)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rushiparikh/projects/atom/backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_maturity_routes.py tests/test_supervisor_learning_service.py tests/test_feedback_service.py -v --no-header --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
    All three test files exist, all tests pass (exit 0), test names cover happy path and error cases
  </done>
</task>

<task type="auto">
  <name>Task 3: Run all tests and verify backend coverage impact</name>
  <files>
    backend/tests/coverage_reports/metrics/coverage.json
    backend/tests/coverage_reports/metrics/coverage_trend_v5.0.json
  </files>
  <read_first>
    /Users/rushiparikh/projects/atom/backend/tests/coverage_reports/metrics/coverage_trend_v5.0.json
    /Users/rushiparikh/projects/atom/backend/tests/scripts/validate_coverage_structure.py
  </read_first>
  <action>
    1. Run full backend test suite to verify no regressions:
    ```
    cd /Users/rushiparikh/projects/atom/backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_workflow_analytics_endpoints.py tests/test_workflow_parameter_validator.py tests/test_maturity_routes.py tests/test_supervisor_learning_service.py tests/test_feedback_service.py -v --no-header --tb=short 2>&1
    ```

    2. Run combined coverage to verify no regressions in the broader suite:
    ```
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_workflow_analytics_endpoints.py tests/test_workflow_parameter_validator.py tests/test_maturity_routes.py tests/test_supervisor_learning_service.py tests/test_feedback_service.py --cov=core.workflow_analytics_endpoints --cov=core.workflow_parameter_validator --cov=api.maturity_routes --cov=core.supervisor_learning_service --cov=core.feedback_service --cov-report=term --no-header 2>&1
    ```
    Verify each target file shows coverage > 10% (indicating meaningful test execution).

    3. Update the trend tracker if it exists: add an entry noting Tier 1 tests added in Phase 293.
    Format:
    ```json
    {
      "phase": "293-01",
      "timestamp": "2026-04-24T...",
      "files_added": ["test_workflow_analytics_endpoints", "test_workflow_parameter_validator", "test_maturity_routes", "test_supervisor_learning_service", "test_feedback_service"],
      "note": "Tier 1 high-priority backend files now tested"
    }
    ```
  </action>
  <acceptance_criteria>
    - All 5 test files pass (exit 0) when run together
    - Each of the 5 target files shows coverage > 10% in `--cov=... --cov-report=term` output
    - coverage_trend_v5.0.json (if it exists) has a Phase 293-01 entry
    - No new test collection errors introduced
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rushiparikh/projects/atom/backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_workflow_analytics_endpoints.py tests/test_workflow_parameter_validator.py tests/test_maturity_routes.py tests/test_supervisor_learning_service.py tests/test_feedback_service.py -v --no-header --tb=short --cov=core.workflow_analytics_endpoints --cov=core.workflow_parameter_validator --cov=api.maturity_routes --cov=core.supervisor_learning_service --cov=core.feedback_service --cov-report=term 2>&1 | tail -40</automated>
  </verify>
  <done>
    All test suites pass with exit 0, each target file shows >10% coverage in cov-report output, trend file updated if it exists
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks -> production code | Mocked dependencies may diverge from real service behavior |
| Coverage JSON -> trend tracker | Incorrect coverage JSON structure could corrupt trend history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-293-01 | Spoofing | Test mocks | accept | Test mocks are in-memory only, no network exposure. Low risk in test-only code. |
| T-293-02 | Information Disclosure | coverage.json | accept | Coverage data exposes file structure but no secrets. Existing production coverage.json already public in repo. |
| T-293-03 | Repudiation | Test behavior | mitigate | All new tests follow existing patterns (MockLLMProvider, MagicMock for db). Verifiable via pytest exit 0. No test data is committed to production database. |
</threat_model>

<verification>
1. `pytest` all 5 test files together exits 0
2. Each of the 5 target files shows >= 10% coverage in per-file `--cov=` report
3. All test names match expected patterns (success + error cases per file)
4. No new collection errors or import failures
</verification>

<success_criteria>
1. Backend Tier 1 files: workflow_analytics_endpoints, workflow_parameter_validator, maturity_routes, supervisor_learning_service, feedback_service all have test files with >=15 assertions each
2. All new test files pass pytest (exit 0) with no import errors
3. Backend overall coverage maintains >30% (already at 36.72% from Phase 292 baseline)
4. Coverage trend tracker updated
</success_criteria>

<output>
After completion, create `.planning/phases/293-coverage-wave-1-30-target/293-01-SUMMARY.md`
</output>
