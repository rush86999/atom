---
phase: 293-coverage-wave-1-30-target
plan: 05
type: execute
wave: 4
depends_on:
  - 293-01
  - 293-02
  - 293-03
files_modified:
  - frontend-nextjs/jest.setup.ts
  - frontend-nextjs/components/chat/__tests__/ChatInput.test.tsx
  - frontend-nextjs/components/chat/__tests__/MessageList.test.tsx
  - frontend-nextjs/components/chat/__tests__/AgentWorkspace.test.tsx
  - frontend-nextjs/components/chat/__tests__/ChatHistorySidebar.test.tsx
  - frontend-nextjs/components/chat/__tests__/ArtifactSidebar.test.tsx
  - frontend-nextjs/components/chat/__tests__/SearchResults.test.tsx
  - frontend-nextjs/components/chat/__tests__/ChatInterface.test.tsx
autonomous: true
requirements:
  - COV-F-02
gap_closure: true
user_setup: []
must_haves:
  truths:
    - "Frontend test suite completes execution within 30 seconds"
    - "All Phase 293 frontend tests achieve 100% pass rate (zero timeout failures)"
    - "Jest timeout configuration increased from 5s to 10s default"
    - "waitFor() calls include explicit timeout options"
    - "Fetch mock timing improved with proper async handling"
  artifacts:
    - path: "frontend-nextjs/jest.setup.ts"
      provides: "Global Jest configuration with increased timeout"
      min_lines: 5
      contains: "jest.setTimeout(10000)"
    - path: "frontend-nextjs/components/chat/__tests__/*.test.tsx"
      provides: "Fixed test files with proper async handling"
      min_lines: 50
  key_links:
    - from: "frontend-nextjs/components/chat/__tests__/*.test.tsx"
      to: "React components"
      via: "React Testing Library with proper waitFor timeouts"
      pattern: "waitFor.*timeout.*:"
---

# Plan 293-05: Frontend Async Timeout Fixes

## Objective

Fix 12 frontend test failures caused by async timeout issues in waitFor() calls, then verify all chat component tests pass with 100% pass rate.

**Purpose:** The default 5-second Jest timeout is too short for fetch mocks to resolve. Increasing timeout and improving async handling will eliminate these failures and improve test reliability.

**Output:** Fixed test files with proper async handling, global Jest timeout configuration, all tests passing.

---

## Context

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

@.planning/phases/293-coverage-wave-1-30-target/293-VERIFICATION.md
@.planning/phases/293-coverage-wave-1-30-target/293-02-SUMMARY.md
@.planning/phases/293-coverage-wave-1-30-target/293-REVIEW.md

@frontend-nextjs/jest.setup.ts
@frontend-nextjs/components/chat/__tests__/CanvasHost.test.tsx
@frontend-nextjs/components/chat/__tests__/ChatInput.test.tsx
@frontend-nextjs/components/chat/__tests__/MessageList.test.tsx

---

## Tasks

<task type="auto">
  <name>Task 1: Configure global Jest timeout to 10 seconds</name>
  <files>frontend-nextjs/jest.setup.ts</files>
  <read_first>
    - frontend-nextjs/jest.setup.ts (current Jest configuration, may not exist)
  </read_first>
  <action>
    Add global Jest timeout configuration to jest.setup.ts:

    1. Check if jest.setup.ts exists:
       ```bash
       [ -f "frontend-nextjs/jest.setup.ts" ] && echo "exists" || echo "not found"
       ```

    2. If file exists, read current content and add at top (after imports):
       ```typescript
       // Increase default timeout for async operations (fetch mocks, waitFor)
       jest.setTimeout(10000); // 10 seconds
       ```

    3. If file doesn't exist, create it with:
       ```typescript
       // Jest setup file for Phase 293
       // Increase default timeout for async operations
       jest.setTimeout(10000);
       ```

    4. This increases the default timeout for all tests from 5000ms to 10000ms

    5. Do NOT modify any other Jest configuration

    6. Verify: grep -q "jest.setTimeout(10000)" frontend-nextjs/jest.setup.ts
  </action>
  <verify>
    <automated>grep -q "jest.setTimeout(10000)" frontend-nextjs/jest.setup.ts && echo "Timeout configured"</automated>
  </verify>
  <done>
    - jest.setup.ts contains jest.setTimeout(10000)
    - Line is at top of file after imports
    - File is valid TypeScript (no syntax errors)
    - grep "jest.setTimeout" frontend-nextjs/jest.setup.ts returns the configuration line
  </done>
  <acceptance_criteria>
    - grep "jest.setTimeout" frontend-nextjs/jest.setup.ts returns "jest.setTimeout(10000)"
    - grep -c "10000" frontend-nextjs/jest.setup.ts >= 1
    - npx jest --listTests runs without syntax errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix async timeout issues in chat component tests</name>
  <files>
    frontend-nextjs/components/chat/__tests__/ChatInput.test.tsx
    frontend-nextjs/components/chat/__tests__/MessageList.test.tsx
    frontend-nextjs/components/chat/__tests__/ChatInterface.test.tsx
    frontend-nextjs/components/chat/__tests__/AgentWorkspace.test.tsx
    frontend-nextjs/components/chat/__tests__/ChatHistorySidebar.test.tsx
    frontend-nextjs/components/chat/__tests__/ArtifactSidebar.test.tsx
    frontend-nextjs/components/chat/__tests__/SearchResults.test.tsx
  </files>
  <read_first>
    - frontend-nextjs/components/chat/__tests__/ChatInput.test.tsx (failing tests)
    - frontend-nextjs/components/chat/__tests__/MessageList.test.tsx (failing tests)
    - frontend-nextjs/components/chat/__tests__/AgentWorkspace.test.tsx (failing tests)
  </read_first>
  <action>
    Fix async timeout issues in failing chat component tests:

    1. For each test file, identify waitFor() calls that are timing out:
       - Pattern: `waitFor(() => { ... })` without explicit timeout option
       - These use the default timeout (now 10s from Task 1, but may need more for fetch)

    2. Add explicit timeout options to waitFor calls:
       ```typescript
       // BEFORE (timing out):
       await waitFor(() => {
           expect(screen.getByText('Expected text')).toBeInTheDocument();
       });

       // AFTER (with explicit timeout):
       await waitFor(() => {
           expect(screen.getByText('Expected text')).toBeInTheDocument();
       }, { timeout: 10000 });  // Increased from default 5000ms
       ```

    3. Improve fetch mock timing for async operations:
       - Use jest.useFakeTimers() for complex async scenarios
       - Add proper cleanup with jest.useRealTimers()
       - Ensure mockFetch promises resolve before waitFor checks

    4. For tests using MSW (Mock Service Worker):
       - Ensure server.resetHandlers() is called in beforeEach
       - Verify handlers are properly configured for all API calls

    5. Specific fixes for each file (based on 293-02 SUMMARY failures):
       - **ChatInput.test.tsx**: Fix upload indicator and attachment chip tests (6/8 passing, 2 timeout)
       - **MessageList.test.tsx**: Fix streaming content tests (3/4 passing, 1 timeout)
       - **AgentWorkspace.test.tsx**: Fix workspace loading tests (5/6 passing, 1 timeout)
       - **ChatHistorySidebar.test.tsx**: Fix session list loading tests (4/5 passing, 1 timeout)
       - **ArtifactSidebar.test.tsx**: Fix artifact list tests (5/6 passing, 1 timeout)
       - **SearchResults.test.tsx**: Fix search results display tests (2/6 passing, 4 timeout)
       - **ChatInterface.test.tsx**: Fix container layout tests (6/6 passing but reported as failed)
       - **CanvasHost.test.tsx**: Already passing (8/8), verify no regression

    6. Do NOT change test assertions - only fix timing and async handling

    7. Target: All 48 tests pass (12 timeout failures resolved)
  </action>
  <verify>
    <automated>cd frontend-nextjs && npx jest components/chat/__tests__/ChatInput.test.tsx components/chat/__tests__/MessageList.test.tsx components/chat/__tests__/AgentWorkspace.test.tsx --no-coverage 2>&1 | grep -E "Test Suites:|Tests:" | tail -2 && echo "Smoke tests: 3 components checked"</automated>
  </verify>
  <done>
    - All chat component test suites pass (100% pass rate)
    - Zero timeout errors in test output
    - grep -c "timeout" test output shows 0 timeout failures
    - grep -c "Async callback" test output shows 0 async callback errors
    - npx jest output shows "Test Suites: 9 passed, 0 failed"
  </done>
  <acceptance_criteria>
    - npx jest output shows "Test Suites: 9 passed, 0 failed"
    - grep "timed out" jest output returns exit code 1 (no timeouts)
    - grep "Async callback was not invoked" jest output returns exit code 1 (no async callback errors)
    - grep "Tests:" output shows "48 passed, 0 failed" for chat components
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Run all frontend tests and verify pass rate</name>
  <files>
    frontend-nextjs/components/chat/__tests__
    frontend-nextjs/components/integrations/__tests__
  </files>
  <read_first>
    - frontend-nextjs/components/chat/__tests__/ (all test files)
    - frontend-nextjs/components/integrations/__tests__/ (all test files)
  </read_first>
  <action>
    Run full frontend test suite to verify all Phase 293 tests pass:

    1. Run all chat component tests:
       ```bash
       cd frontend-nextjs
       npx jest components/chat/__tests__/ --no-coverage
       ```

    2. Run all integration component tests:
       ```bash
       npx jest components/integrations/__tests__/ --no-coverage
       ```

    3. Verify:
       - All chat component tests pass (48 tests total from Phase 293-02)
       - All integration tests pass (85 tests total from Phase 293-03)
       - No timeout errors
       - No async callback errors

    4. Count total tests and verify pass rate:
       - Expected: 48 (chat) + 85 (integrations) = 133 tests
       - Target: 100% pass rate (133 passing, 0 failing)

    5. If any tests still fail, identify the specific failure pattern and document

    6. Document final pass rate in task completion
  </action>
  <verify>
    <automated>cd frontend-nextjs && npx jest components/chat/__tests__/ components/integrations/__tests__/ --no-coverage 2>&1 | grep -E "Test Suites:|Tests:" | tail -2</automated>
  </verify>
  <done>
    - All chat component tests pass (48/48 = 100%)
    - All integration component tests pass (85/85 = 100%)
    - Total tests: 133 tests, all passing
    - Zero failures in new tests
    - Zero timeout errors
    - Exit code 0 from jest command
  </done>
  <acceptance_criteria>
    - npx jest output shows "Test Suites: 17 passed, 0 failed" for chat + integrations
    - grep "Tests:" output shows "133 passed, 0 failed"
    - Exit code 0 from jest command
    - grep "timeout" jest output returns exit code 1 (no timeouts)
  </acceptance_criteria>
</task>

</tasks>

## Threat Model

### Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> component | Test code renders components and asserts on DOM state |

### STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-293-05-01 | T | Test timeout configuration | accept | Test-only changes, no production impact |
| T-293-05-02 | D | Async timing | mitigate | Use explicit timeouts to prevent flaky tests |

## Verification

- jest.setup.ts contains jest.setTimeout(10000)
- All 48 chat component tests pass (100% pass rate)
- All 85 integration component tests pass (100% pass rate)
- Zero timeout errors in test output
- Zero async callback errors in test output
- Total 133 tests passing

## Success Criteria

1. 12 frontend async timeout failures resolved
2. 100% pass rate for all Phase 293 frontend tests (133 tests)
3. Jest timeout configured globally at 10 seconds
4. No flaky tests due to timing issues
5. All waitFor() calls have explicit timeout options
6. Test suite completes within 30 seconds

## Output

After completion, create `.planning/phases/293-coverage-wave-1-30-target/293-05-SUMMARY.md`
