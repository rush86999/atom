---
phase: 099-cross-platform-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/e2e_ui/tests/cross-platform/
  - backend/tests/e2e_ui/pages/cross_platform_objects.py
  - backend/tests/e2e_ui/fixtures/cross_platform_fixtures.py
  - frontend-nextjs/components/agent/AgentChat.tsx
  - frontend-nextjs/components/canvas/CanvasContainer.tsx
  - frontend-nextjs/components/auth/LoginForm.tsx
  - mobile/app/screens/AgentChatScreen.tsx
  - frontend-nextjs/src/lib/testIds.ts
autonomous: true

must_haves:
  truths:
    - "Cross-platform integration tests verify shared workflows (authentication, agent execution, canvas presentations) work identically across web, mobile, and desktop platforms"
    - "Feature parity tests confirm all platforms support expected features (agent chat streaming, history, feedback, canvas types, skill execution)"
    - "API response consistency tests verify backend APIs return identical data structures regardless of client platform"
  artifacts:
    - path: "backend/tests/e2e_ui/tests/cross-platform/test_shared_workflows.py"
      provides: "Shared workflow E2E tests for web platform"
      exports: ["TestSharedWorkflows"]
      min_lines: 100
    - path: "backend/tests/e2e_ui/tests/cross-platform/test_feature_parity.py"
      provides: "Feature parity tests verifying cross-platform consistency"
      exports: ["TestFeatureParity"]
      min_lines: 80
    - path: "backend/tests/e2e_ui/pages/cross_platform_objects.py"
      provides: "Page objects for cross-platform workflow testing"
      exports: ["SharedWorkflowPage", "FeatureParityPage"]
      min_lines: 60
  key_links:
    - from: "backend/tests/e2e_ui/tests/cross-platform/test_shared_workflows.py"
      to: "backend/tests/e2e_ui/pages/page_objects.py"
      via: "Page Object inheritance"
      pattern: "from.*page_objects import"
    - from: "backend/tests/e2e_ui/tests/cross-platform/test_feature_parity.py"
      to: "backend/core/models.py"
      via: "API contract validation"
      pattern: "requests\\.(get|post)"
---

<objective>
Create cross-platform integration tests that verify shared workflows (authentication, agent execution, canvas presentations) work identically across web, mobile, and desktop platforms. This is the foundation for Phase 099, establishing the test patterns that mobile and desktop E2E tests will follow.

Purpose: Cross-platform consistency is critical for user experience — users should be able to switch between web, mobile, and desktop apps with identical functionality. These tests verify that the backend APIs support all platforms equally and that shared workflows produce consistent results.

Output: Cross-platform integration test suite with shared workflow tests, feature parity validation, and API consistency checks. These tests run on web (Playwright) during this plan, then serve as templates for mobile (Detox) and desktop (tauri-driver) E2E tests in subsequent plans.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/099-cross-platform-integration/099-RESEARCH.md

# Existing E2E Infrastructure (Phase 075-080)
@backend/tests/e2e_ui/conftest.py
@backend/tests/e2e_ui/pages/page_objects.py
@backend/tests/e2e_ui/fixtures/auth_fixtures.py
@backend/tests/e2e_ui/fixtures/database_fixtures.py
@backend/tests/e2e_ui/fixtures/api_fixtures.py

# Example Test Pattern
@backend/tests/e2e_ui/tests/test_agent_chat.py
@backend/tests/e2e_ui/tests/test_canvas_forms.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cross-platform test directory and shared workflow page objects</name>
  <files>backend/tests/e2e_ui/tests/cross-platform/__init__.py, backend/tests/e2e_ui/pages/cross_platform_objects.py</files>
  <action>
Create directory structure and page object base classes for cross-platform testing:

1. Create `backend/tests/e2e_ui/tests/cross-platform/` directory with `__init__.py`

2. Create `backend/tests/e2e_ui/pages/cross_platform_objects.py` with:
   - `SharedWorkflowPage` base class extending `BasePage`
   - Methods for authentication workflow (login, logout, session validation)
   - Methods for agent execution workflow (send message, receive streaming response, request canvas)
   - Methods for canvas presentation workflow (present sheets/charts/forms, verify rendering, submit forms)
   - `FeatureParityPage` class for validating feature availability
   - All methods use data-testid selectors for resilience
   - Documentation comments explaining expected behavior per platform

3. Add `cross_platform_objects.py` import to `pages/__init__.py`

Use existing `BasePage` and page object patterns from `page_objects.py` as reference.
</action>
  <verify>python -c "from backend.tests.e2e_ui.pages.cross_platform_objects import SharedWorkflowPage, FeatureParityPage; print('Import successful')"</verify>
  <done>SharedWorkflowPage and FeatureParityPage classes exist with authentication, agent execution, and canvas presentation methods</done>
</task>

<task type="auto">
  <name>Task 2: Create shared workflow E2E tests for web platform</name>
  <files>backend/tests/e2e_ui/tests/cross-platform/test_shared_workflows.py</files>
  <action>
Create `test_shared_workflows.py` with E2E tests verifying shared workflows work correctly on web platform:

1. Import SharedWorkflowPage, fixtures (authenticated_page, db_session, setup_test_project)

2. Create `TestSharedWorkflows` class with tests:
   - `test_agent_execution_workflow`: Navigate to agent chat, send message, verify streaming response, request canvas, verify canvas renders
   - `test_canvas_presentation_workflow`: Present each canvas type (generic, docs, email, sheets, orchestration, terminal, coding), verify rendering
   - `test_authentication_workflow`: Login with email/password, verify session, logout, verify session cleared
   - `test_skill_execution_workflow`: Navigate to skills, install skill, execute skill, verify output
   - `test_data_persistence_workflow`: Create project, modify data, refresh page, verify data persisted

3. Each test follows pattern: Setup → Action → Verify (AAA), uses authenticated_page fixture, uses SharedWorkflowPage methods

4. Add docstrings explaining what the workflow tests and why it's cross-platform critical

Reference existing test patterns from `test_agent_chat.py` and `test_canvas_forms.py`.
</action>
  <verify>cd backend && pytest tests/e2e_ui/tests/cross-platform/test_shared_workflows.py -v --tb=short 2>&1 | head -50</verify>
  <done>All 5 shared workflow tests pass (agent execution, canvas presentation, authentication, skill execution, data persistence)</done>
</task>

<task type="auto">
  <name>Task 3: Create feature parity and API consistency tests</name>
  <files>backend/tests/e2e_ui/tests/cross-platform/test_feature_parity.py</files>
  <action>
Create `test_feature_parity.py` with tests verifying feature parity and API consistency:

1. Import FeatureParityPage, requests library

2. Create `TestFeatureParity` class with tests:
   - `test_agent_chat_feature_parity`: Verify web supports all expected agent chat features (streaming, history, feedback, canvas_presentations, skill_execution)
   - `test_canvas_type_parity`: Verify web supports all 7 canvas types (generic, docs, email, sheets, orchestration, terminal, coding)
   - `test_api_response_consistency`: Call `/api/v1/agents`, verify response structure (id, name, maturity, capabilities fields present)
   - `test_workflow_trigger_parity`: Verify web supports all trigger types (manual, scheduled, event-based)
   - `test_settings_parity`: Verify web supports all expected settings (theme, notifications, preferences)

3. Define feature lists as class constants:
   - `AGENT_CHAT_FEATURES = ["streaming", "history", "feedback", "canvas_presentations", "skill_execution"]`
   - `CANVAS_TYPES = ["generic", "docs", "email", "sheets", "orchestration", "terminal", "coding"]`
   - `WORKFLOW_TRIGGER_TYPES = ["manual", "scheduled", "event_based"]`

4. Use `requests` for direct API calls to validate response structure

5. Add docstrings explaining these are "contract tests" that mobile/desktop must also pass
</action>
  <verify>cd backend && pytest tests/e2e_ui/tests/cross-platform/test_feature_parity.py -v --tb=short 2>&1 | head -50</verify>
  <done>All 5 feature parity tests pass (agent chat features, canvas types, API consistency, workflow triggers, settings)</done>
</task>

<task type="auto">
  <name>Task 4: Add data-testid attributes to shared UI components</name>
  <files>frontend-nextjs/components/agent/AgentChat.tsx, frontend-nextjs/components/canvas/CanvasContainer.tsx, frontend-nextjs/components/auth/LoginForm.tsx, mobile/app/screens/AgentChatScreen.tsx, frontend-nextjs/src-tauri/src/components.rs</files>
  <action>
Add `data-testid` attributes to shared UI components for E2E test selector resilience:

1. Review frontend components and add data-testid to key interactive elements:
   - `frontend-nextjs/components/agent/AgentChat.tsx`: Add `data-testid="agent-chat-input"`, `data-testid="send-message-button"`, `data-testid="agent-response"`, `data-testid="streaming-indicator"`, `data-testid="history-button"`, `data-testid="execution-history-list"`, `data-testid="history-item-0"` (or dynamic index)
   - `frontend-nextjs/components/canvas/CanvasContainer.tsx`: Add `data-testid="canvas-container"`, `data-testid="canvas-type-{type}"` (generic, docs, email, sheets, etc.), `data-testid="close-canvas-button"`
   - `frontend-nextjs/components/canvas/forms/`: Add `data-testid="form-field-{name}"` to form inputs, `data-testid="form-submit-button"`, `data-testid="form-success-message"`

2. Review auth components:
   - `frontend-nextjs/components/auth/LoginForm.tsx`: Add `data-testid="login-email-input"`, `data-testid="login-password-input"`, `data-testid="login-submit-button"`, `data-testid="login-error-message"`, `data-testid="logout-button"`

3. Review mobile components:
   - `mobile/app/screens/AgentChatScreen.tsx`: Add `testID` prop to match web data-testid values (React Native uses `testID` instead of `data-testid`)
   - `mobile/app/screens/CanvasScreen.tsx`: Add `testID` props matching web pattern
   - `mobile/app/components/auth/LoginScreen.tsx`: Add `testID` props for email/password/submit/error elements

4. Review desktop Tauri components (if using separate components):
   - `frontend-nextjs/src-tauri/src/components.rs` or relevant files: Ensure data-testid attributes are included in HTML rendering

5. Create a shared test ID constants file if not exists:
   - `frontend-nextjs/src/lib/testIds.ts`: Export constants for all test IDs (avoids typos, ensures consistency)
   - `mobile/src/constants/testIds.ts`: Mirror web test IDs for mobile

6. Document in each component's JSDoc: "data-testid attributes used by E2E tests - do not remove without updating test plans"

Note: If any of the specified files don't exist or use different component structures, add data-testid to the equivalent files/components. The key is ensuring E2E tests have stable selectors.
</action>
  <verify>cd frontend-nextjs && grep -r "data-testid" components/ --include="*.tsx" --include="*.ts" | grep -E "(agent-chat-input|canvas-container|login-email-input)" | head -10</verify>
  <done>Key UI components have data-testid attributes for E2E selectors: agent chat, canvas, auth forms, mobile equivalents</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. Cross-platform test directory exists with 2 test files
2. `pytest tests/e2e_ui/tests/cross-platform/ -v` runs all tests successfully
3. Tests use data-testid selectors (not CSS classes vulnerable to styling changes)
4. Shared workflow tests cover: authentication, agent execution, canvas presentations, skill execution, data persistence
5. Feature parity tests define expected features that mobile/desktop must also support

Run: `cd backend && pytest tests/e2e_ui/tests/cross-platform/ -v --co` to list discovered tests
Run: `cd backend && pytest tests/e2e_ui/tests/cross-platform/ -v` to execute tests
</verification>

<success_criteria>
- Cross-platform integration tests created (2 test files, 10 tests total)
- All tests pass on web platform (Playwright)
- Tests use existing E2E infrastructure (fixtures, page objects, conftest)
- Shared workflow tests document expected behavior for mobile/desktop
- Feature parity tests define contract that mobile/desktop must satisfy
- Tests can run in CI: `pytest tests/e2e_ui/tests/cross-platform/ -v`
</success_criteria>

<output>
After completion, create `.planning/phases/099-cross-platform-integration/099-01-SUMMARY.md`
</output>
