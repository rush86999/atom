---
phase: 141-desktop-coverage-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/src-tauri/tests/coverage/mod.rs
  - frontend-nextjs/src-tauri/coverage.sh
  - .planning/phases/141-desktop-coverage-expansion/141-01-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "Baseline coverage percentage measured and documented"
    - "Coverage gaps identified in main.rs (1757 lines)"
    - "Platform-specific coverage breakdown available"
    - "Test execution completes without tarpaulin errors"
    - "Baseline JSON created in coverage-report/"
  artifacts:
    - path: "frontend-nextjs/src-tauri/coverage-report/baseline.json"
      provides: "Baseline coverage data with percentage, lines, commit SHA"
      min_lines: 10
    - path: "frontend-nextjs/src-tauri/coverage-report/index.html"
      provides: "HTML coverage report for visual inspection"
    - path: "frontend-nextjs/src-tauri/tests/coverage/mod.rs"
      provides: "Enhanced baseline tracking with per-file breakdown"
      contains: "fn generate_baseline_with_breakdown"
  key_links:
    - from: "frontend-nextjs/src-tauri/coverage.sh"
      to: "cargo tarpaulin"
      via: "CLI invocation with tarpaulin.toml config"
      pattern: "cargo tarpaulin --config tarpaulin.toml"
    - from: "frontend-nextjs/src-tauri/tests/coverage/mod.rs"
      to: "coverage-report/baseline.json"
      via: "JSON serialization of CoverageBaseline struct"
      pattern: "serde_json::to_string"
---

<objective>
**Measure and document baseline desktop coverage** for main.rs (1757 lines) to identify coverage gaps and establish starting point for Phase 141 expansion.

**Purpose:** Phase 140 established infrastructure but baseline measurement is pending. Cannot plan coverage expansion without knowing current percentage and specific gaps.

**Output:**
- Baseline coverage percentage documented
- Per-file coverage breakdown
- Coverage gap analysis (which functions/lines need tests)
- Baseline JSON for trend tracking
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-desktop-coverage-baseline/140-03-SUMMARY.md
@.planning/phases/140-desktop-coverage-baseline/140-RESEARCH.md
@docs/DESKTOP_COVERAGE.md
@frontend-nextjs/src-tauri/tarpaulin.toml
@frontend-nextjs/src-tauri/tests/coverage/mod.rs
@frontend-nextjs/src-tauri/tests/platform_specific/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance baseline tracking with per-file breakdown</name>
  <files>frontend-nextjs/src-tauri/tests/coverage/mod.rs</files>
  <action>
Add `generate_baseline_with_breakdown()` function to create detailed baseline:

1. Add `FileCoverage` struct with path, covered, total, percentage fields
2. Add `CoverageBreakdown` struct with overall percentage and files vector
3. Implement `generate_baseline_with_breakdown()` that:
   - Runs `cargo tarpaulin --out Json --output-dir coverage-report`
   - Parses JSON output to extract per-file coverage
   - Calculates percentage per file: (covered / total) * 100
   - Identifies files below 50% coverage (high priority gaps)
   - Returns CoverageBreakdown with sorted files (lowest coverage first)
4. Add unit test `test_generate_baseline_with_breakdown_success()`:
   - Mocks tarpaulin JSON output with sample file data
   - Verifies correct calculation of file percentages
   - Verifies sorting (lowest coverage files first)
5. Modify `generate_baseline()` to call breakdown version internally

Do NOT modify existing `parse_coverage_report()` or `compare_with_baseline()` functions.
  </action>
  <verify>
```bash
cd frontend-nextjs/src-tauri
cargo test --test coverage_baseline_test test_generate_baseline_with_breakdown_success
```
  </verify>
  <done>
`generate_baseline_with_breakdown()` creates CoverageBreakdown with sorted files, unit test passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Run baseline coverage measurement</name>
  <files>frontend-nextjs/src-tauri/coverage.sh</files>
  <action>
Run baseline coverage measurement and capture results:

1. Update `coverage.sh` to add `--baseline-breakdown` flag that:
   - Calls `cargo tarpaulin` with tarpaulin.toml config
   - Outputs to coverage-report/ directory (HTML + JSON)
   - Extracts percentage from terminal output
   - Calls baseline tracking module to generate JSON
2. Execute `./coverage.sh --baseline-breakdown`:
   - Capture overall coverage percentage
   - Capture per-file breakdown from JSON
   - Note any tarpaulin warnings (ARM, timeout, exclusions)
3. Parse tarpaulin JSON output:
   - Extract files with lowest coverage (<30%)
   - Extract files with medium coverage (30-60%)
   - Extract fully covered files (>90%)
4. Create `coverage-report/baseline.json` with:
   - baseline_coverage: X.XX (overall percentage)
   - measured_at: ISO 8601 timestamp
   - total_lines: N (from main.rs)
   - covered_lines: M
   - platform: "windows" | "macos" | "linux"
   - arch: "x86_64" | "arm64"
   - commit_sha: current git SHA
   - files_breakdown: [{path, covered, total, percentage}]
   - high_priority_gaps: [files below 30%]

If tarpaulin fails on ARM Mac, document error and skip (CI/CD will measure on x86_64).
  </action>
  <verify>
```bash
cd frontend-nextjs/src-tauri
./coverage.sh --baseline-breakdown
ls -la coverage-report/baseline.json
cat coverage-report/baseline.json | jq '.baseline_coverage'
```
  </verify>
  <done>
Baseline JSON exists with coverage percentage, per-file breakdown, and high-priority gaps identified
  </done>
</task>

<task type="auto">
  <name>Task 3: Document baseline results and gap analysis</name>
  <files>.planning/phases/141-desktop-coverage-expansion/141-01-SUMMARY.md</files>
  <action>
Create comprehensive baseline summary documenting current state:

1. Document overall baseline percentage:
   - Section "Baseline Coverage Results" with percentage
   - Comparison to target (80%): gap = 80 - baseline
   - Note if baseline is unexpected (very low or very high)

2. Document per-file breakdown (top 10 lowest coverage):
   - For main.rs sections: file dialogs, device capabilities, IPC commands
   - Note line ranges with zero coverage
   - Note functions with partial coverage

3. Create gap analysis table:
   | Category | Lines | Coverage | Gap | Priority |
   |----------|-------|----------|-----|----------|
   | File Dialogs | 24-165 | X% | 100-X% | High |
   | Device Capabilities | 200-450 | X% | 100-X% | High |
   | IPC Commands | 700-1200 | X% | 100-X% | Medium |
   | Error Handling | throughout | X% | 100-X% | High |

4. Identify platform-specific gaps:
   - Windows: cfg(windows) blocks with zero coverage
   - macOS: cfg(macos) blocks with zero coverage
   - Linux: cfg(linux) blocks with zero coverage

5. Create 141-01-SUMMARY.md with:
   - Baseline metrics (percentage, covered/total lines)
   - Per-file coverage breakdown (sorted by coverage ascending)
   - High-priority gaps (files below 30% coverage)
   - Platform-specific gap analysis
   - Recommendations for Plans 02-06 (which areas to test first)

Format matches 140-03-SUMMARY.md structure for consistency.
  </action>
  <verify>
```bash
cat .planning/phases/141-desktop-coverage-expansion/141-01-SUMMARY.md | grep -A 5 "Baseline Coverage Results"
cat .planning/phases/141-desktop-coverage-expansion/141-01-SUMMARY.md | grep -A 10 "gap analysis"
```
  </verify>
  <done>
141-01-SUMMARY.md documents baseline percentage, per-file breakdown, gap analysis table, platform-specific gaps, and recommendations for subsequent plans
  </done>
</task>

</tasks>

<verification>
**Overall Phase 141-01 Verification:**

1. Baseline measurement completed:
   ```bash
   cd frontend-nextjs/src-tauri
   ./coverage.sh --baseline-breakdown
   cat coverage-report/baseline.json | jq '.baseline_coverage'
   ```

2. Per-file breakdown generated:
   ```bash
   cat coverage-report/baseline.json | jq '.files_breakdown | length'
   ```

3. Gap analysis documented:
   ```bash
   cat .planning/phases/141-desktop-coverage-expansion/141-01-SUMMARY.md | grep -c "Priority"
   ```

4. Tests pass:
   ```bash
   cargo test test_generate_baseline_with_breakdown_success
   ```

**Acceptance Criteria:**
- Baseline coverage percentage is a number between 0-100
- Per-file breakdown includes at least main.rs
- High-priority gaps list is non-empty (baseline < 80%)
- 141-01-SUMMARY.md includes gap analysis table
</verification>

<success_criteria>
1. **Baseline Measured:** Coverage percentage documented in baseline.json
2. **Breakdown Generated:** Per-file coverage breakdown shows which files need tests
3. **Gaps Identified:** High-priority gaps (files <30% coverage) listed for targeting
4. **Platform Analysis:** Windows/macOS/Linux cfg blocks assessed for coverage
5. **Recommendations Ready:** Plans 02-06 can use gap analysis to prioritize test areas
</success_criteria>

<output>
After completion, create `.planning/phases/141-desktop-coverage-expansion/141-01-SUMMARY.md` with baseline metrics, per-file breakdown, gap analysis, and recommendations for subsequent plans.
</output>
