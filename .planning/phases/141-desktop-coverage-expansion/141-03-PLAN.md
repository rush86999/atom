---
phase: 141-desktop-coverage-expansion
plan: 03
type: execute
wave: 2
depends_on: ["141-01"]
files_modified:
  - frontend-nextjs/src-tauri/tests/platform_specific/macos.rs
  - frontend-nextjs/src-tauri/tests/platform_specific/mod.rs
  - .planning/phases/141-desktop-coverage-expansion/141-03-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "macOS-specific test file created with cfg(target_os = \"macos\")"
    - "Menu bar operations tested (creation, menu items, event handling)"
    - "Path handling tested for macOS (forward slash separator)"
    - "Temp directory operations tested for macOS (/var/folders or /tmp)"
    - "Tests compile successfully on macOS (or skipped gracefully on other platforms)"
  artifacts:
    - path: "frontend-nextjs/src-tauri/tests/platform_specific/macos.rs"
      provides: "macOS-specific test suite for menu bar, dock, and platform utilities"
      min_lines: 150
      contains: "test_macos_menu_bar_structure"
      contains: "test_macos_temp_directory"
      contains: "test_macos_path_separator"
    - path: "frontend-nextjs/src-tauri/tests/platform_specific/mod.rs"
      provides: "macOS module declaration with cfg gate"
      contains: "#[cfg(target_os = \"macos\")]"
      contains: "pub mod macos;"
  key_links:
    - from: "tests/platform_specific/macos.rs"
      to: "src/main.rs"
      via: "Testing menu bar creation, file dialog default paths, temp directory operations"
      pattern: "cfg!.*target_os.*macos"
    - from: "tests/platform_specific/macos.rs"
      to: "std::env::temp_dir()"
      via: "macOS temp directory access (/var/folders or /tmp)"
      pattern: "std::env::temp_dir|/var/folders|/tmp"
---

<objective>
**Create macOS-specific test suite** covering menu bar operations, path handling, and temp directory access to increase coverage of macOS-specific code paths in main.rs.

**Purpose:** macOS menu bar (lines 500-650) and file dialogs use platform-specific behavior. macOS temp directories (/var/folders or /tmp) and path separators (/) require dedicated tests.

**Output:**
- 15-20 macOS-specific tests for menu bar, dock, path handling, temp operations
- Coverage increase of ~5-8 percentage points for macOS code paths
- Test file with cfg(target_os = "macos") for compile-time platform filtering
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-desktop-coverage-expansion/141-01-PLAN.md
@.planning/phases/140-desktop-coverage-baseline/140-02-SUMMARY.md
@.planning/phases/140-desktop-coverage-baseline/140-RESEARCH.md
@docs/DESKTOP_COVERAGE.md
@frontend-nextjs/src-tauri/src/main.rs
@frontend-nextjs/src-tauri/tests/platform_specific/mod.rs
@frontend-nextjs/src-tauri/tests/helpers/platform_helpers.rs
@frontend-nextjs/src-tauri/tests/menu_bar_test.rs
@frontend-nextjs/src-tauri/tests/menu_unit_test.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create macOS-specific test file structure</name>
  <files>frontend-nextjs/src-tauri/tests/platform_specific/macos.rs</files>
  <action>
Create `macos.rs` test file with module-level documentation and cfg guard:

1. Add module documentation explaining:
   - macOS-specific tests for menu bar, dock, path handling, temp operations
   - Tests only compile and run on macOS (cfg(target_os = "macos"))
   - Focus on main.rs lines 500-650 (menu bar) and macOS-specific paths

2. Import required modules:
   - `use std::fs;` for file operations
   - `use std::path::PathBuf;` for path handling
   - `use std::env;` for environment variables
   - `use crate::helpers::platform_helpers::*;` for helper utilities

3. Create helper function `get_macos_temp_dir()`:
   - Calls `std::env::temp_dir()`
   - Returns PathBuf to macOS temp directory
   - Logs temp dir path for debugging (typically /var/folders/... on macOS)

4. Create helper function `verify_unix_path_format()`:
   - Takes path: &str
   - Returns true if path contains forward slashes
   - Returns true if path starts with "/" (absolute path)
   - Returns false if path contains backslashes
   - Used to verify Unix/Mac path format in tests

5. Add module-level test `test_macos_platform_detection()`:
   - Calls `get_current_platform()` and asserts "macos"
   - Calls `is_platform("macos")` and asserts true
   - Calls `cfg_assert("macos")` to verify platform
   - Ensures tests only run on macOS (will panic on other platforms)

Do NOT add actual test implementations yet (Task 2).
  </action>
  <verify>
```bash
cd frontend-nextjs/src-tauri
# Verify file compiles (will skip on non-macOS)
rustc --test tests/platform_specific/macos.rs 2>&1 || true
# Check file exists
ls -la tests/platform_specific/macos.rs
```
  </verify>
  <done>
macos.rs file exists with module documentation, helper functions, and platform detection test
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement macOS path handling tests</name>
  <files>frontend-nextjs/src-tauri/tests/platform_specific/macos.rs</files>
  <action>
Add tests for macOS path handling and temp directory operations:

1. `test_macos_temp_directory_format()`:
   - Get temp dir via `get_macos_temp_dir()`
   - Assert path starts with "/": `assert!(temp_path.starts_with("/"))`
   - Assert path does NOT contain backslashes: `assert!(!temp_path.contains("\\"))`
   - Assert temp directory exists: `assert!(temp_dir.exists())`

2. `test_macos_temp_directory_writable()`:
   - Create test file in temp dir
   - Write "test content" to file
   - Read back and verify content matches
   - Cleanup: `let _ = fs::remove_file(&test_file);`

3. `test_macos_path_separator()`:
   - Call `get_platform_separator()` and assert equals "/"
   - Create PathBuf with forward slashes: "/Users/test/file.txt"
   - Verify `file_name()` returns "file.txt"
   - Verify `extension()` returns "txt"
   - Verify `parent()` returns "/Users/test"

4. `test_macos_home_directory()`:
   - Get `HOME` environment variable
   - Assert path exists and starts with "/Users/"
   - Create test path: `$HOME/test_atom_file.txt`
   - Verify path components parse correctly

5. `test_macos_appdata_directory()`:
   - Test common macOS app data locations:
     - ~/Library/Application Support/
     - ~/Library/Preferences/
     - ~/Library/Caches/
   - Verify paths start with home directory
   - Verify paths exist or can be created

6. `test_macos_absolute_vs_relative_paths()`:
   - Create absolute path: "/Users/test/document.pdf"
   - Create relative path: "test/document.pdf"
   - Verify `is_absolute()` returns correct values
   - Verify `file_name()` returns same value for both
   - Verify `extension()` returns "pdf" for absolute path

Each test should be annotated with `#[test]` and `#[cfg(target_os = "macos")]`.
  </action>
  <verify>
```bash
cd frontend-nextjs/src-tauri
cargo test --test platform_specific::macos::test_macos_temp_directory 2>&1 | head -20
```
  </verify>
  <done>
6 macOS path handling tests added with cfg(target_os = "macos") guard
  </done>
</task>

<task type="auto">
  <name>Task 3: Add macOS menu bar and system info tests</name>
  <files>frontend-nextjs/src-tauri/tests/platform_specific/macos.rs</files>
  <action>
Add tests for macOS menu bar, dock, and system information:

1. `test_macos_menu_bar_structure()`:
   - Verify menu bar JSON structure from main.rs
   - Expected structure: { title, items: [{ label, action }] }
   - Verify menu items include expected labels (File, Edit, View, etc.)
   - Note: Tests expected structure, not actual Tauri Menu creation

2. `test_macos_dock_integration()`:
   - Verify dock icon bouncing is configured (if applicable)
   - Test dock menu structure (if implemented)
   - Verify dock-related event emissions (dock-icon-clicked)

3. `test_macos_system_info_structure()`:
   - Mock `get_system_info()` command response structure
   - Verify response contains "platform": "macos"
   - Verify response contains "architecture" (x64 or arm64 for Apple Silicon)
   - Verify response contains tauri_version

4. `test_macos_cfg_detection()`:
   - Test `cfg!(target_os = "macos")` returns true
   - Test `cfg!(target_os = "windows")` returns false
   - Test `cfg!(target_os = "linux")` returns false
   - Test `cfg!(target_arch)` returns correct value (x86_64 or aarch64)

5. `test_macos_spotlight_integration()`:
   - Verify Spotlight metadata configuration (if applicable)
   - Test file indexing for common document types
   - Note: This tests configuration structure, not actual Spotlight integration

6. `test_macos_file_operations_roundtrip()`:
   - Create temp file with macOS-specific path
   - Write content with Unix line endings (LF only)
   - Read back and verify content matches
   - Verify no CRLF conversion issues
   - Cleanup temp file

7. `test_macos_unix_permissions()`:
   - Create temp file
   - Verify file is readable
   - Verify file is writable
   - Test file mode (if accessible via std::fs::metadata)
   - Cleanup temp file

Note: Tests 1-5 test the expected structures and platform detection. Tests 6-7 test Unix-style file operations.
  </action>
  <verify>
```bash
cd frontend-nextjs/src-tauri
cargo test --test platform_specific::macos 2>&1 | grep -E "test macos|running|passed"
```
  </verify>
  <done>
7 macOS menu bar and system info tests added covering menu structure, dock, system info, cfg detection, and file operations
  </done>
</task>

</tasks>

<verification>
**Overall Phase 141-03 Verification:**

1. macOS test file exists and compiles:
   ```bash
   cd frontend-nextjs/src-tauri
   ls -la tests/platform_specific/macos.rs
   rustc --crate-type lib tests/platform_specific/macos.rs --edition 2021
   ```

2. Module declaration added to mod.rs:
   ```bash
   grep -A 1 'target_os = "macos"' tests/platform_specific/mod.rs
   ```

3. Tests can be listed:
   ```bash
   cargo test --list 2>&1 | grep macos
   ```

4. Platform-specific tests run only on macOS:
   ```bash
   # On non-macOS, tests should be skipped (not compiled)
   cargo test platform_specific::macos 2>&1 | grep -i "skipped\|no tests"
   ```

**Acceptance Criteria:**
- macos.rs file exists with 15-20 tests
- All tests have #[cfg(target_os = "macos")] guard
- mod.rs includes macos module declaration with cfg gate
- Tests compile without errors (on any platform, thanks to cfg)
- Tests cover menu bar, path handling, temp operations, system info
</verification>

<success_criteria>
1. **macOS Test File Created:** tests/platform_specific/macos.rs with 15-20 tests
2. **Platform Guard Applied:** #[cfg(target_os = "macos")] on all tests and module declaration
3. **Menu Bar Coverage:** Tests for menu structure, dock integration, system info
4. **Path Handling Tests:** Temp directory, path separators, home directory, app data
5. **Compilation Success:** File compiles on all platforms (tests skipped on non-macOS)
</success_criteria>

<output>
After completion, create `.planning/phases/141-desktop-coverage-expansion/141-03-SUMMARY.md` with test count, coverage increase estimated, macOS-specific patterns documented, and handoff to Plan 04 (Linux tests).
</output>
