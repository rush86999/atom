---
phase: 141-desktop-coverage-expansion
plan: 02
type: execute
wave: 2
depends_on: ["141-01"]
files_modified:
  - frontend-nextjs/src-tauri/tests/platform_specific/windows.rs
  - frontend-nextjs/src-tauri/tests/platform_specific/mod.rs
  - .planning/phases/141-desktop-coverage-expansion/141-02-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "Windows-specific test file created with cfg(target_os = \"windows\")"
    - "File dialog operations tested (open, save, folder picker)"
    - "Path separator handling tested for Windows (backslash separator)"
    - "Temp directory operations tested for Windows (%TEMP% environment variable)"
    - "Tests compile successfully on Windows (or skipped gracefully on other platforms)"
  artifacts:
    - path: "frontend-nextjs/src-tauri/tests/platform_specific/windows.rs"
      provides: "Windows-specific test suite for file dialogs and platform utilities"
      min_lines: 150
      contains: "test_windows_file_dialog_open"
      contains: "test_windows_temp_directory"
      contains: "test_windows_path_separator"
    - path: "frontend-nextjs/src-tauri/tests/platform_specific/mod.rs"
      provides: "Windows module declaration with cfg gate"
      contains: "#[cfg(target_os = \"windows\")]"
      contains: "pub mod windows;"
  key_links:
    - from: "tests/platform_specific/windows.rs"
      to: "src/main.rs"
      via: "Testing open_file_dialog, save_file_dialog, open_folder_dialog commands"
      pattern: "open_file_dialog.*filters.*Option"
    - from: "tests/platform_specific/windows.rs"
      to: "std::env::var(\"TEMP\")"
      via: "Windows temp directory access"
      pattern: "std::env::var.*TEMP"
---

<objective>
**Create Windows-specific test suite** covering file dialog operations, path handling, and temp directory access to increase coverage of Windows-specific code paths in main.rs.

**Purpose:** Windows file dialogs (lines 24-165) use tauri-plugin-dialog with platform-specific behavior. Windows temp directory (%TEMP%) and path separators (\\) require dedicated tests.

**Output:**
- 15-20 Windows-specific tests for file dialogs, path handling, temp operations
- Coverage increase of ~5-8 percentage points for Windows code paths
- Test file with cfg(target_os = "windows") for compile-time platform filtering
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-desktop-coverage-expansion/141-01-PLAN.md
@.planning/phases/140-desktop-coverage-baseline/140-02-SUMMARY.md
@.planning/phases/140-desktop-coverage-baseline/140-RESEARCH.md
@docs/DESKTOP_COVERAGE.md
@frontend-nextjs/src-tauri/src/main.rs
@frontend-nextjs/src-tauri/tests/platform_specific/mod.rs
@frontend-nextjs/src-tauri/tests/helpers/platform_helpers.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Windows-specific test file structure</name>
  <files>frontend-nextjs/src-tauri/tests/platform_specific/windows.rs</files>
  <action>
Create `windows.rs` test file with module-level documentation and cfg guard:

1. Add module documentation explaining:
   - Windows-specific tests for file dialogs, path handling, temp operations
   - Tests only compile and run on Windows (cfg(target_os = "windows"))
   - Focus on main.rs lines 24-165 (file dialogs) and Windows-specific paths

2. Import required modules:
   - `use std::fs;` for file operations
   - `use std::path::PathBuf;` for path handling
   - `use std::env;` for environment variables
   - `use crate::helpers::platform_helpers::*;` for helper utilities

3. Create helper function `create_temp_test_file()`:
   - Takes content: &str
   - Gets temp dir via `get_temp_dir()` or `std::env::var("TEMP")`
   - Creates unique filename with timestamp
   - Writes content to file
   - Returns PathBuf to file
   - Note: Cleanup handled in test (defer pattern with Drop if needed)

4. Create helper function `verify_windows_path_format()`:
   - Takes path: &str
   - Returns true if path contains backslashes
   - Returns true if path starts with drive letter (C:, D:, etc.)
   - Used to verify Windows path format in tests

5. Add module-level test `test_windows_platform_detection()`:
   - Calls `get_current_platform()` and asserts "windows"
   - Calls `is_platform("windows")` and asserts true
   - Calls `cfg_assert("windows")` to verify platform
   - Ensures tests only run on Windows (will panic on other platforms)

Do NOT add actual test implementations yet (Task 2).
  </action>
  <verify>
```bash
cd frontend-nextjs/src-tauri
# Verify file compiles (will skip on non-Windows)
rustc --test tests/platform_specific/windows.rs 2>&1 || true
# Check file exists
ls -la tests/platform_specific/windows.rs
```
  </verify>
  <done>
windows.rs file exists with module documentation, helper functions, and platform detection test
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Windows file dialog tests</name>
  <files>frontend-nextjs/src-tauri/tests/platform_specific/windows.rs</files>
  <action>
Add tests for file dialog operations from main.rs (lines 24-165):

1. `test_windows_temp_directory_format()`:
   - Get `std::env::var("TEMP")` or default to "C:\\Temp"
   - Assert path contains backslash: `assert!(temp_path.contains("\\"))`
   - Assert path exists: `assert!(PathBuf::from(&temp_path).exists())`
   - Verify parent directory exists

2. `test_windows_temp_directory_writable()`:
   - Create test file in temp dir using `create_temp_test_file()`
   - Write "test content" to file
   - Read back and verify content matches
   - Cleanup: `let _ = fs::remove_file(&test_file);`

3. `test_windows_path_separator()`:
   - Call `get_platform_separator()` and assert equals "\\\\"
   - Create PathBuf with backslashes: `C:\\Users\\test\\file.txt`
   - Verify `file_name()` returns "file.txt"
   - Verify `extension()` returns "txt"
   - Verify `parent()` returns "C:\\Users\\test"

4. `test_windows_path_buf_normalization()`:
   - Create path with mixed separators: `C:\\Users/test\\file.txt`
   - Verify PathBuf normalizes to backslashes
   - Assert `is_absolute()` returns true for Windows absolute paths

5. `test_windows_drive_letter_parsing()`:
   - Create paths: "C:\\", "D:\\Data", "E:\\Files\\test.txt"
   - Verify each starts with drive letter pattern: `[A-Z]:`
   - Use regex or string parsing to verify format

6. `test_windows_file_extensions()`:
   - Create test files: "test.txt", "document.pdf", "image.png", "archive.zip"
   - Verify `extension()` returns correct values
   - Verify `file_stem()` returns filename without extension

Each test should be annotated with `#[test]` and `#[cfg(target_os = "windows")]`.

Do NOT test actual Tauri commands (requires full app context, covered in integration tests).
  </action>
  <verify>
```bash
cd frontend-nextjs/src-tauri
cargo test --test platform_specific::windows::test_windows_temp_directory 2>&1 | head -20
```
  </verify>
  <done>
6 Windows file dialog tests added with cfg(target_os = "windows") guard, all testable without full Tauri app context
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Windows-specific system info tests</name>
  <files>frontend-nextjs/src-tauri/tests/platform_specific/windows.rs</files>
  <action>
Add tests for Windows system information and platform detection:

1. `test_windows_system_info_structure()`:
   - Mock `get_system_info()` command response structure
   - Verify response contains "platform": "windows"
   - Verify response contains "architecture" (x64 or arm64)
   - Verify response contains version string
   - Note: This tests the expected JSON structure, not actual command invocation

2. `test_windows_cfg_detection()`:
   - Test `cfg!(target_os = "windows")` returns true
   - Test `cfg!(target_os = "macos")` returns false
   - Test `cfg!(target_os = "linux")` returns false
   - Test `cfg!(target_arch)` returns correct value for current arch

3. `test_windows_any_platform_combinations()`:
   - Test `cfg!(any(target_os = "windows", target_os = "macos"))`
   - Test `cfg!(all(target_os = "windows", target_arch = "x86_64"))`
   - Test `cfg!(not(target_os = "linux"))`
   - Verify boolean logic matches expected platform

4. `test_windows_environment_variables()`:
   - Get `TEMP` environment variable
   - Get `APPDATA` environment variable (if exists)
   - Get `USERPROFILE` environment variable (if exists)
   - Assert at least one Windows-specific env var exists
   - Verify values contain backslashes

5. `test_windows_file_operations_roundtrip()`:
   - Create temp file with Windows-specific path
   - Write content with Windows line endings (CRLF)
   - Read back and verify content matches
   - Cleanup temp file

6. `test_windows_directory_listing()`:
   - Create temp directory with test files
   - List directory entries
   - Verify file names, extensions, sizes
   - Verify is_directory flag
   - Cleanup: remove directory and contents

Note: Tests 1-3 test the cfg macro and platform detection (no external dependencies). Tests 4-6 use Windows environment variables and paths.
  </action>
  <verify>
```bash
cd frontend-nextjs/src-tauri
cargo test --test platform_specific::windows 2>&1 | grep -E "test windows|running|passed"
```
  </verify>
  <done>
6 Windows system info tests added covering cfg detection, environment variables, and file operations roundtrip
  </done>
</task>

</tasks>

<verification>
**Overall Phase 141-02 Verification:**

1. Windows test file exists and compiles:
   ```bash
   cd frontend-nextjs/src-tauri
   ls -la tests/platform_specific/windows.rs
   rustc --crate-type lib tests/platform_specific/windows.rs --edition 2021
   ```

2. Module declaration added to mod.rs:
   ```bash
   grep -A 1 'target_os = "windows"' tests/platform_specific/mod.rs
   ```

3. Tests can be listed:
   ```bash
   cargo test --list 2>&1 | grep windows
   ```

4. Platform-specific tests run only on Windows:
   ```bash
   # On non-Windows, tests should be skipped (not compiled)
   cargo test platform_specific::windows 2>&1 | grep -i "skipped\|no tests"
   ```

**Acceptance Criteria:**
- windows.rs file exists with 15-20 tests
- All tests have #[cfg(target_os = "windows")] guard
- mod.rs includes windows module declaration with cfg gate
- Tests compile without errors (on any platform, thanks to cfg)
- Tests cover file dialogs, path handling, temp operations
</verification>

<success_criteria>
1. **Windows Test File Created:** tests/platform_specific/windows.rs with 15-20 tests
2. **Platform Guard Applied:** #[cfg(target_os = "windows")] on all tests and module declaration
3. **File Dialog Coverage:** Tests for temp directory, path separators, drive letters, extensions
4. **System Info Tests:** cfg detection, environment variables, file operations
5. **Compilation Success:** File compiles on all platforms (tests skipped on non-Windows)
</success_criteria>

<output>
After completion, create `.planning/phases/141-desktop-coverage-expansion/141-02-SUMMARY.md` with test count, coverage increase estimated, Windows-specific patterns documented, and handoff to Plan 03 (macOS tests).
</output>
