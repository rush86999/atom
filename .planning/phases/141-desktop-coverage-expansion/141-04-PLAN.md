---
phase: 141-desktop-coverage-expansion
plan: 04
type: execute
wave: 2
depends_on: ["141-01"]
files_modified:
  - frontend-nextjs/src-tauri/tests/platform_specific/linux.rs
  - frontend-nextjs/src-tauri/tests/platform_specific/mod.rs
  - .planning/phases/141-desktop-coverage-expansion/141-04-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "Linux-specific test file created with cfg(target_os = \"linux\")"
    - "XDG directory operations tested (XDG_DATA_HOME, XDG_CONFIG_HOME)"
    - "Path handling tested for Linux (forward slash separator)"
    - "Temp directory operations tested for Linux (/tmp or XDG_RUNTIME_DIR)"
    - "Tests compile successfully on Linux (or skipped gracefully on other platforms)"
  artifacts:
    - path: "frontend-nextjs/src-tauri/tests/platform_specific/linux.rs"
      provides: "Linux-specific test suite for XDG directories, window managers, and platform utilities"
      min_lines: 150
      contains: "test_linux_xdg_directories"
      contains: "test_linux_temp_directory"
      contains: "test_linux_path_separator"
    - path: "frontend-nextjs/src-tauri/tests/platform_specific/mod.rs"
      provides: "Linux module declaration with cfg gate"
      contains: "#[cfg(target_os = \"linux\")]"
      contains: "pub mod linux;"
  key_links:
    - from: "tests/platform_specific/linux.rs"
      to: "src/main.rs"
      via: "Testing XDG directory operations, temp directories, platform detection"
      pattern: "XDG_DATA_HOME|XDG_CONFIG_HOME|/tmp"
    - from: "tests/platform_specific/linux.rs"
      to: "std::env::var()"
      via: "Linux XDG environment variable access"
      pattern: "std::env::var.*XDG"
---

<objective>
**Create Linux-specific test suite** covering XDG directory operations, path handling, and temp directory access to increase coverage of Linux-specific code paths in main.rs.

**Purpose:** Linux uses XDG Base Directory Specification for config/data directories. Linux temp directories (/tmp or XDG_RUNTIME_DIR) and path separators (/) require dedicated tests.

**Output:**
- 15-20 Linux-specific tests for XDG directories, temp operations, path handling
- Coverage increase of ~5-8 percentage points for Linux code paths
- Test file with cfg(target_os = "linux") for compile-time platform filtering
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-desktop-coverage-expansion/141-01-PLAN.md
@.planning/phases/140-desktop-coverage-baseline/140-02-SUMMARY.md
@.planning/phases/140-desktop-coverage-baseline/140-RESEARCH.md
@docs/DESKTOP_COVERAGE.md
@frontend-nextjs/src-tauri/src/main.rs
@frontend-nextjs/src-tauri/tests/platform_specific/mod.rs
@frontend-nextjs/src-tauri/tests/helpers/platform_helpers.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Linux-specific test file structure</name>
  <files>frontend-nextjs/src-tauri/tests/platform_specific/linux.rs</files>
  <action>
Create `linux.rs` test file with module-level documentation and cfg guard:

1. Add module documentation explaining:
   - Linux-specific tests for XDG directories, temp operations, path handling
   - Tests only compile and run on Linux (cfg(target_os = "linux"))
   - Focus on XDG Base Directory Specification compliance

2. Import required modules:
   - `use std::fs;` for file operations
   - `use std::path::PathBuf;` for path handling
   - `use std::env;` for environment variables
   - `use crate::helpers::platform_helpers::*;` for helper utilities

3. Create helper function `get_xdg_data_home()`:
   - Checks `XDG_DATA_HOME` environment variable
   - Falls back to `$HOME/.local/share` if not set
   - Returns PathBuf to XDG data directory
   - Creates directory if it doesn't exist (for testing)

4. Create helper function `get_xdg_config_home()`:
   - Checks `XDG_CONFIG_HOME` environment variable
   - Falls back to `$HOME/.config` if not set
   - Returns PathBuf to XDG config directory

5. Create helper function `get_xdg_runtime_dir()`:
   - Checks `XDG_RUNTIME_DIR` environment variable
   - Falls back to `/tmp` if not set
   - Returns PathBuf to XDG runtime directory

6. Add module-level test `test_linux_platform_detection()`:
   - Calls `get_current_platform()` and asserts "linux"
   - Calls `is_platform("linux")` and asserts true
   - Calls `cfg_assert("linux")` to verify platform
   - Ensures tests only run on Linux (will panic on other platforms)

Do NOT add actual test implementations yet (Task 2).
  </action>
  <verify>
```bash
cd frontend-nextjs/src-tauri
# Verify file compiles (will skip on non-Linux)
rustc --test tests/platform_specific/linux.rs 2>&1 || true
# Check file exists
ls -la tests/platform_specific/linux.rs
```
  </verify>
  <done>
linux.rs file exists with module documentation, XDG helper functions, and platform detection test
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Linux XDG directory tests</name>
  <files>frontend-nextjs/src-tauri/tests/platform_specific/linux.rs</files>
  <action>
Add tests for Linux XDG directory operations:

1. `test_linux_xdg_data_home_directory()`:
   - Get XDG data home via `get_xdg_data_home()`
   - Assert path exists or can be created
   - Assert path contains ".local/share" or is from XDG_DATA_HOME env var
   - Verify parent directory exists

2. `test_linux_xdg_config_home_directory()`:
   - Get XDG config home via `get_xdg_config_home()`
   - Assert path exists or can be created
   - Assert path contains ".config" or is from XDG_CONFIG_HOME env var
   - Create test config file and verify write access

3. `test_linux_xdg_runtime_directory()`:
   - Get XDG runtime dir via `get_xdg_runtime_dir()`
   - Assert path exists or falls back to "/tmp"
   - Verify path is writable (create test file)
   - Cleanup test file

4. `test_linux_xdg_directory_fallbacks()`:
   - Temporarily unset XDG_DATA_HOME
   - Verify fallback to `$HOME/.local/share`
   - Temporarily unset XDG_CONFIG_HOME
   - Verify fallback to `$HOME/.config`
   - Restore original values

5. `test_linux_home_directory()`:
   - Get `HOME` environment variable
   - Assert path exists and starts with "/home/"
   - Verify home directory is readable

6. `test_linux_path_separator()`:
   - Call `get_platform_separator()` and assert equals "/"
   - Create PathBuf with forward slashes: "/home/user/file.txt"
   - Verify `file_name()` returns "file.txt"
   - Verify `extension()` returns "txt"
   - Verify no backslashes in path

Each test should be annotated with `#[test]` and `#[cfg(target_os = "linux")]`.
  </action>
  <verify>
```bash
cd frontend-nextjs/src-tauri
cargo test --test platform_specific::linux::test_linux_xdg 2>&1 | head -20
```
  </verify>
  <done>
6 Linux XDG directory tests added with cfg(target_os = "linux") guard
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Linux temp and system info tests</name>
  <files>frontend-nextjs/src-tauri/tests/platform_specific/linux.rs</files>
  <action>
Add tests for Linux temp directories and system information:

1. `test_linux_temp_directory_format()`:
   - Get temp dir via `std::env::temp_dir()`
   - Assert path starts with "/tmp/" or "/var/tmp/"
   - Assert path does NOT contain backslashes
   - Verify temp directory exists: `assert!(temp_dir.exists())`

2. `test_linux_temp_directory_writable()`:
   - Create test file in /tmp
   - Write "test content" to file
   - Read back and verify content matches
   - Cleanup: `let _ = fs::remove_file(&test_file);`

3. `test_linux_system_info_structure()`:
   - Mock `get_system_info()` command response structure
   - Verify response contains "platform": "linux"
   - Verify response contains "architecture" (x86_64 or other)
   - Verify response contains version string

4. `test_linux_cfg_detection()`:
   - Test `cfg!(target_os = "linux")` returns true
   - Test `cfg!(target_os = "windows")` returns false
   - Test `cfg!(target_os = "macos")` returns false
   - Test `cfg!(target_arch)` returns correct value

5. `test_linux_file_operations_roundtrip()`:
   - Create temp file with Linux-specific path
   - Write content with Unix line endings (LF only)
   - Read back and verify content matches
   - Verify permissions (if accessible)
   - Cleanup temp file

6. `test_linux_desktop_environment_detection()`:
   - Check for common desktop environment indicators:
     - `XDG_CURRENT_DESKTOP` env var (GNOME, KDE, Xfce, etc.)
     - `DESKTOP_SESSION` env var
   - Verify at least one env var is set (in desktop environment)
   - Log detected desktop environment for debugging

Note: Tests 1-2 test temp directories. Tests 3-4 test system info and cfg detection. Tests 5-6 test file operations and desktop environment.
  </action>
  <verify>
```bash
cd frontend-nextjs/src-tauri
cargo test --test platform_specific::linux 2>&1 | grep -E "test linux|running|passed"
```
  </verify>
  <done>
6 Linux temp and system info tests added covering temp directories, system info, cfg detection, and desktop environment detection
  </done>
</task>

</tasks>

<verification>
**Overall Phase 141-04 Verification:**

1. Linux test file exists and compiles:
   ```bash
   cd frontend-nextjs/src-tauri
   ls -la tests/platform_specific/linux.rs
   rustc --crate-type lib tests/platform_specific/linux.rs --edition 2021
   ```

2. Module declaration added to mod.rs:
   ```bash
   grep -A 1 'target_os = "linux"' tests/platform_specific/mod.rs
   ```

3. Tests can be listed:
   ```bash
   cargo test --list 2>&1 | grep linux
   ```

4. Platform-specific tests run only on Linux:
   ```bash
   # On non-Linux, tests should be skipped (not compiled)
   cargo test platform_specific::linux 2>&1 | grep -i "skipped\|no tests"
   ```

**Acceptance Criteria:**
- linux.rs file exists with 15-20 tests
- All tests have #[cfg(target_os = "linux")] guard
- mod.rs includes linux module declaration with cfg gate
- Tests compile without errors (on any platform, thanks to cfg)
- Tests cover XDG directories, temp operations, path handling, system info
</verification>

<success_criteria>
1. **Linux Test File Created:** tests/platform_specific/linux.rs with 15-20 tests
2. **Platform Guard Applied:** #[cfg(target_os = "linux")] on all tests and module declaration
3. **XDG Coverage:** Tests for XDG_DATA_HOME, XDG_CONFIG_HOME, XDG_RUNTIME_DIR
4. **Path Handling Tests:** Temp directory, path separators, home directory
5. **Compilation Success:** File compiles on all platforms (tests skipped on non-Linux)
</success_criteria>

<output>
After completion, create `.planning/phases/141-desktop-coverage-expansion/141-04-SUMMARY.md` with test count, coverage increase estimated, Linux-specific patterns documented, and handoff to Plan 05 (cross-platform IPC tests).
</output>
