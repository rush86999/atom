---
phase: 160-backend-80-percent-target
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/services/test_backend_gap_closure.py
  - backend/core/models.py
  - backend/tests/integration/services/conftest.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Model compatibility issues fixed (AgentEpisode field mismatches)"
    - "Database threading issues resolved (SQLite InterfaceError)"
    - "Async testing inconsistencies fixed (context resolver)"
    - "Missing imports added (TriggerInterceptor)"
    - "Blocked tests now pass and contribute to coverage"
  artifacts:
    - path: "backend/tests/integration/services/test_backend_gap_closure.py"
      provides: "Fixed backend services gap closure tests"
      min_lines: 1600
  key_links:
    - from: "test_backend_gap_closure.py"
      to: "core/models.py"
      via: "Fixed model definitions"
---

<objective>
Fix all Phase 159 blockers preventing tests from passing, enabling backend coverage to reach 80% target by resolving model compatibility, database threading, async testing, and import issues.

Purpose: Phase 159 created 45 tests but only 12 passed (27%) due to 4 critical blockers. This plan fixes those blockers, enabling the remaining 33 tests to pass and contribute their coverage gains toward the 80% target.

Output: All 45 tests passing (100% pass rate), backend coverage increase of +6-8% reaching ~80.6%.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-backend-80-percent-coverage/159-VERIFICATION.md
@.planning/phases/159-backend-80-percent-coverage/159-02-SUMMARY.md
@backend/tests/integration/services/test_backend_gap_closure.py
@backend/core/models.py
@backend/tests/integration/services/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix model compatibility issues (AgentEpisode)</name>
  <files>
    backend/core/models.py
    backend/tests/integration/services/test_backend_gap_closure.py
  </files>
  <action>
    Fix AgentEpisode model field mismatches identified in Phase 159:

    1. Analyze the errors:
       - Identify field mismatches in AgentEpisode model
       - Compare expected vs actual field definitions
       - Document all mismatches

    2. Fix AgentEpisode model in core/models.py:
       - Add missing fields
       - Fix field type mismatches
       - Ensure compatibility with test expectations
       - Add proper relationships

    3. Update test expectations if needed:
       - Align test assertions with corrected model
       - Fix any incorrect test assumptions
       - Ensure tests validate correct behavior

    4. Verify fixes:
       - Run affected tests
       - Confirm all AgentEpisode tests pass
       - Check model integrity

    Focus on making the model consistent and test-compatible.
  </action>
  <verify>
    pytest backend/tests/integration/services/test_backend_gap_closure.py -k "episode" -v
    All episode-related tests pass
    Model compatibility errors resolved
  </verify>
  <done>
    AgentEpisode model compatibility issues fixed, episode tests passing
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix database threading issues (SQLite InterfaceError)</name>
  <files>
    backend/tests/integration/services/conftest.py
    backend/tests/integration/services/test_backend_gap_closure.py
  </files>
  <action>
    Fix SQLite InterfaceError in concurrent tests:

    1. Analyze the threading errors:
       - Identify which tests cause InterfaceError
       - Understand SQLite threading limitations
       - Document concurrent test patterns

    2. Fix database session handling in conftest.py:
       - Add thread-safe session fixtures
       - Implement connection pooling for concurrency
       - Add database isolation between tests

    3. Fix affected tests:
       - Make tests thread-safe
       - Add proper session scoping
       - Fix concurrent access patterns
       - Add cleanup for concurrent resources

    4. Implement workarounds for SQLite limitations:
       - Use shared connections with proper locking
       - Avoid concurrent write operations
       - Implement sequential execution where needed

    5. Verify fixes:
       - Run previously failing concurrent tests
       - Confirm no InterfaceError
       - Check test isolation

    Focus on making tests work with SQLite's threading limitations.
  </action>
  <verify>
    pytest backend/tests/integration/services/test_backend_gap_closure.py -k "concurrent" -v
    Concurrent tests pass without InterfaceError
    Database threading issues resolved
  </verify>
  <done>
    Database threading issues fixed, concurrent tests passing
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix async testing inconsistencies (context resolver)</name>
  <files>
    backend/tests/integration/services/test_backend_gap_closure.py
  </files>
  <action>
    Fix async/await inconsistencies in context resolver tests:

    1. Analyze async testing errors:
       - Identify which async tests fail
       - Understand async/await mismatches
       - Document incorrect patterns

    2. Fix async test patterns:
       - Add missing async/await keywords
       - Fix async fixture usage
       - Implement proper async test structure
       - Add proper async cleanup

    3. Fix context resolver specific issues:
       - Resolve async context manager issues
       - Fix async method call patterns
       - Implement proper async test setup

    4. Add async test helpers if needed:
       - Create async test utilities
       - Add proper async fixtures
       - Implement async cleanup helpers

    5. Verify fixes:
       - Run async context resolver tests
       - Confirm all async tests pass
       - Check for proper async handling

    Focus on correct async/await usage throughout tests.
  </action>
  <verify>
    pytest backend/tests/integration/services/test_backend_gap_closure.py -k "context" -v
    All context resolver tests pass
    Async testing issues resolved
  </verify>
  <done>
    Async testing inconsistencies fixed, context resolver tests passing
  </done>
</task>

<task type="auto">
  <name>Task 4: Add missing imports (TriggerInterceptor)</name>
  <files>
    backend/tests/integration/services/test_backend_gap_closure.py
  </files>
  <action>
    Fix missing TriggerInterceptor imports:

    1. Analyze import errors:
       - Identify missing TriggerInterceptor imports
       - Understand import structure
       - Document all missing imports

    2. Add missing imports:
       - Import TriggerInterceptor service
       - Import related dependencies
       - Fix import paths and aliases
       - Add any missing mock imports

    3. Fix test imports:
       - Ensure all test files have correct imports
       - Add proper module imports
       - Fix relative imports if needed

    4. Verify imports:
       - Check for import errors
       - Confirm all modules accessible
       - Test import resolution

    5. Run affected tests:
       - Execute trigger interceptor tests
       - Confirm all tests pass
       - Verify coverage contribution

    Focus on complete and correct import statements.
  </action>
  <verify>
    pytest backend/tests/integration/services/test_backend_gap_closure.py -k "trigger" -v
    All trigger interceptor tests pass
    Import errors resolved
  </verify>
  <done>
    Missing imports added, trigger interceptor tests passing
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify all 45 tests pass and measure coverage</name>
  <files>
    backend/tests/coverage_reports/metrics/backend_phase_160.json
  </files>
  <action>
    Run all backend gap closure tests and measure coverage:

    1. Run all tests:
       ```bash
       cd backend
       pytest tests/integration/services/test_backend_gap_closure.py -v
       ```

    2. Verify test results:
       - All 45 tests passing
       - 100% pass rate achieved
       - No errors or blockers

    3. Measure coverage:
       ```bash
       pytest tests/integration/services/test_backend_gap_closure.py \
              --cov=core --cov=api --cov=tools \
              --cov-report=json:tests/coverage_reports/metrics/backend_phase_160.json \
              --cov-report=term-missing
       ```

    4. Calculate coverage improvement:
       - Compare with Phase 159 baseline (74.60%)
       - Document improvement percentage
       - Verify progress toward 80% target

    5. Document results:
       - Test pass rate: 100% (45/45)
       - Coverage improvement: +6-8% expected
       - Estimated final coverage: ~80.6%

    Create backend_phase_160.json with results.
  </action>
  <verify>
    backend/tests/coverage_reports/metrics/backend_phase_160.json exists
    All 45 tests passing (100% pass rate)
    Backend coverage >= 79% (approaching 80% target)
  </verify>
  <done>
    All blockers fixed, tests passing, coverage improved toward 80%
  </done>
</task>

</tasks>

<verification>
Run `cd backend && pytest tests/integration/services/test_backend_gap_closure.py --cov=core --cov=api --cov=tools --cov-report=term`
Verify all 45 tests pass
Verify backend coverage >= 79%
Verify coverage improved from Phase 159 baseline
</verification>

<success_criteria>
1. All 4 blockers fixed (model, threading, async, imports)
2. All 45 tests passing (100% pass rate)
3. Backend coverage >= 79% (approaching 80%)
4. Coverage improvement of +6-8% from baseline
</success_criteria>

<output>
After completion, create `.planning/phases/160-backend-80-percent-target/160-01-SUMMARY.md`
</output>
