---
phase: 084-core-services-unit-testing-training-graduation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/agent/test_agent_graduation_service.py
autonomous: true

must_haves:
  truths:
    - "Graduation service tests cover all readiness calculation paths (episodes, interventions, constitutional scores)"
    - "Criteria calculation tests cover all maturity levels (INTERN, SUPERVISED, AUTONOMOUS)"
    - "Promotion decision tests cover valid transitions, invalid maturity levels, non-existent agents"
    - "Constitutional compliance tests cover validation with segments, no segments, non-existent episodes"
    - "Supervision metrics tests cover session calculation, intervention rate, performance trends"
    - "Tests use AsyncMock for async methods and proper fixtures for database"
  artifacts:
    - path: backend/tests/unit/agent/test_agent_graduation_service.py
      provides: Comprehensive unit tests for AgentGraduationService
      min_lines: 1000
      exports: ["TestReadinessScoreCalculation", "TestGraduationCriteria", "TestPromotionDecisions", "TestConstitutionalCompliance", "TestSupervisionMetrics", "TestSandboxExecutor"]
  key_links:
    - from: backend/tests/unit/agent/test_agent_graduation_service.py
      to: backend/core/agent_graduation_service.py
      via: import AgentGraduationService
      pattern: "from core.agent_graduation_service import AgentGraduationService"
---

<objective>
Expand AgentGraduationService unit tests to achieve 90%+ coverage including readiness calculation, graduation criteria, promotion decisions, constitutional compliance, supervision metrics, and sandbox executor.

Purpose: AgentGraduationService validates agent readiness for promotion using episodic memory. The existing tests have a collection error and don't cover all code paths including sandbox executor, supervision metrics with full edge cases, skill usage metrics, and graduation exam execution. Additional tests needed for comprehensive coverage.

Output: Enhanced test file with 20+ new tests covering previously untested paths, reaching 90%+ coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/core/agent_graduation_service.py
@backend/tests/unit/agent/test_agent_graduation_service.py

# Phase 82-83 testing patterns to follow:
- Use pytest fixtures for mock database (db_session)
- Use AsyncMock for async methods
- Mock external dependencies (ConstitutionalValidator, get_lancedb_handler, get_sandbox_executor)
- Test both success and failure paths
- Test organization: New test classes added after related existing classes

# Existing test coverage (~18 tests with collection error):
- TestAgentGraduationService class with readiness score, graduation exam, promotion, transitions, intervention rate, audit trail
- TestSandboxExecutor class with exam scenarios
- Test supervision metrics, constitutional validation

# Key areas needing coverage:
- SandboxExecutor.execute_exam() edge cases
- validate_graduation_with_supervision() full paths
- calculate_skill_usage_metrics() and calculate_readiness_score_with_skills()
- _supervision_score() calculation details
- _calculate_performance_trend() edge cases
- run_graduation_exam() with edge cases
- get_graduation_audit_trail() edge cases
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix collection error and expand readiness score calculation tests</name>
  <files>backend/tests/unit/agent/test_agent_graduation_service.py</files>
  <action>
First fix the factory.LambdaFunction collection error, then expand readiness score tests:

1. Fix factory import error:
   - The error "AttributeError: module 'factory' has no attribute 'LambdaFunction'" needs investigation
   - Likely missing import in conftest.py or test file
   - May need to patch factory.LambdaFunction or use alternative fixture approach

2. TestReadinessScoreCalculation class (8 new tests):
   - Readiness score with exact minimum episodes (threshold boundary)
   - Readiness score with exact maximum intervention rate (threshold boundary)
   - Readiness score with exact minimum constitutional score (threshold boundary)
   - Readiness score recommendation generation for different score ranges (0-50, 50-75, 75-100)
   - Readiness score for unknown maturity level returns error dict
   - Readiness score for non-existent agent returns error dict
   - Readiness score gaps list all issues when multiple criteria fail
   - Readiness score ready=True only when all criteria pass

3. TestReadinessScoreEdgeCases class (6 new tests):
   - Readiness with zero episodes (intervention_rate = 1.0 per edge case)
   - Readiness with no constitutional scores (avg = 0.0)
   - Readiness with mixed episode statuses (only completed episodes counted)
   - Readiness score calculation uses weighted formula (40% episodes + 30% interventions + 30% constitutional)
   - Readiness target_maturity parameter overrides default criteria min_episodes
   - Readiness returns current_maturity in result dict

Location: Add after existing TestAgentGraduationService class (after line ~270)
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/agent/test_agent_graduation_service.py::TestReadinessScoreCalculation -v</verify>
  <done>Collection error fixed, readiness score paths fully tested</done>
</task>

<task type="auto">
  <name>Task 2: Add graduation criteria and promotion decision tests</name>
  <files>backend/tests/unit/agent/test_agent_graduation_service.py</files>
  <action>
Add comprehensive tests for graduation criteria and promotion decisions:

1. TestGraduationCriteria class (6 tests):
   - INTERN criteria: 10 min_episodes, 0.5 max_intervention_rate, 0.70 min_constitutional_score
   - SUPERVISED criteria: 25 min_episodes, 0.2 max_intervention_rate, 0.85 min_constitutional_score
   - AUTONOMOUS criteria: 50 min_episodes, 0.0 max_intervention_rate, 0.95 min_constitutional_score
   - Criteria accessed via AgentGraduationService.CRITERIA dict
   - Criteria structure validated (has min_episodes, max_intervention_rate, min_constitutional_score keys)
   - Unknown maturity level returns None from CRITERIA dict

2. TestPromotionDecisions class (8 tests):
   - promote_agent updates AgentStatus enum correctly
   - promote_agent sets configuration["promoted_at"] timestamp
   - promote_agent sets configuration["promoted_by"] user
   - promote_agent calls db.commit to persist changes
   - promote_agent updates agent.updated_at timestamp
   - promote_agent returns False for non-existent agent
   - promote_agent returns False for invalid maturity level (not in AgentStatus enum)
   - promote_agent logs success message with agent_id, new_maturity, validated_by

Mock dependencies:
- Mock db.query(AgentRegistry).filter().first() to return agent
- Mock db.commit for persistence

Location: Add after TestReadinessScoreEdgeCases class
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/agent/test_agent_graduation_service.py::TestGraduationCriteria -v</verify>
  <done>All graduation criteria tested, promotion decisions verified</done>
</task>

<task type="auto">
  <name>Task 3: Add sandbox executor and graduation exam tests</name>
  <files>backend/tests/unit/agent/test_agent_graduation_service.py</files>
  <action>
Add comprehensive tests for SandboxExecutor and graduation exam execution:

1. TestSandboxExecutorDetailed class (8 tests):
   - execute_exam returns success=False for non-existent agent
   - execute_exam returns score=0.0 when episode_count=0
   - execute_exam calculates episode_score (40% weight) from min_episodes threshold
   - execute_exam calculates intervention_score (30% weight) inverted (lower is better)
   - execute_exam calculates compliance_score (30% weight) from intervention_rate
   - execute_exam includes constitutional_violations list when interventions too high
   - execute_exam includes "insufficient_performance" violation when score < 0.7
   - execute_exam passed=True when score >= min_score AND compliance >= min_score

2. TestGraduationExamExecution class (6 tests):
   - execute_graduation_exam calls SandboxExecutor.execute_exam
   - execute_graduation_exam returns exam_completed=False on executor failure
   - execute_graduation_exam returns passed=False when exam success but score too low
   - execute_graduation_exam returns constitutional_compliance from executor
   - execute_graduation_exam returns constitutional_violations from executor
   - execute_graduation_exam returns score rounded to 2 decimal places

3. TestRunGraduationExam class (4 tests):
   - run_graduation_exam with empty edge_case_episodes returns passed=True, score=0
   - run_graduation_exam calls executor.execute_in_sandbox for each episode_id
   - run_graduation_exam includes replayed_actions in results
   - run_graduation_exam includes safety_violations in results

Mock dependencies:
- Mock SandboxExecutor with execute_exam method
- Mock get_sandbox_executor to return mock executor
- Mock db.query(Episode).filter().first() for episode lookup

Location: Add after TestPromotionDecisions class
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/agent/test_agent_graduation_service.py::TestSandboxExecutorDetailed -v</verify>
  <done>Sandbox executor fully tested, graduation exam paths covered</done>
</task>

<task type="auto">
  <name>Task 4: Add supervision metrics and performance trend tests</name>
  <files>backend/tests/unit/agent/test_agent_graduation_service.py</files>
  <action>
Add comprehensive tests for supervision metrics and performance trend calculation:

1. TestSupervisionMetricsDetailed class (8 tests):
   - calculate_supervision_metrics returns zero values when no sessions
   - calculate_supervision_metrics calculates total_supervision_hours from duration_seconds
   - calculate_supervision_metrics calculates intervention_rate per hour
   - calculate_supervision_metrics calculates average_supervisor_rating
   - calculate_supervision_metrics counts high_rating_sessions (4-5 stars)
   - calculate_supervision_metrics counts low_intervention_sessions (0-1 interventions)
   - calculate_supervision_metrics calculates successful_intervention_recovery_rate
   - calculate_supervision_metrics handles None supervisor_rating gracefully

2. TestPerformanceTrendCalculation class (6 tests):
   - _calculate_performance_trend returns "stable" with <10 sessions
   - _calculate_performance_trend returns "improving" when recent ratings higher
   - _calculate_performance_trend returns "declining" when recent ratings lower
   - _calculate_performance_trend combines rating_diff (60%) and intervention_diff (40%)
   - _calculate_performance_trend returns "stable" for score between -0.3 and 0.3
   - _calculate_performance_trend handles None started_at (treated as datetime.min)

3. TestSupervisionScoreCalculation class (5 tests):
   - _supervision_score calculates rating_score (40% of total)
   - _supervision_score calculates intervention_score (30% of total)
   - _supervision_score calculates high_quality_score (20% of total)
   - _supervision_score calculates trend_score (10% of total)
   - _supervision_score caps rating_score at 1.0 (4.0/5.0 target)

Mock dependencies:
- Mock db.query(SupervisionSession).filter() to return sessions
- Create SupervisionSession instances with all required fields

Location: Add after TestRunGraduationExam class
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/agent/test_agent_graduation_service.py::TestSupervisionMetricsDetailed -v</verify>
  <done>Supervision metrics fully tested, performance trends verified</done>
</task>

<task type="auto">
  <name>Task 5: Add validation with supervision, skill metrics, and audit trail tests</name>
  <files>backend/tests/unit/agent/test_agent_graduation_service.py</files>
  <action>
Add comprehensive tests for validation with supervision, skill metrics, and audit trail:

1. TestValidationWithSupervision class (8 tests):
   - validate_graduation_with_supervision combines episode and supervision metrics
   - validate_graduation_with_supervision adds supervision gaps to episode gaps
   - validate_graduation_with_supervision checks min_high_quality_sessions requirement
   - validate_graduation_with_supervision checks min_low_intervention_sessions requirement
   - validate_graduation_with_supervision checks average_supervisor_rating >= 3.5
   - validate_graduation_with_supervision checks intervention_rate threshold
   - validate_graduation_with_supervision calculates combined score (70% episode + 30% supervision)
   - validate_graduation_with_supervision ready=True only when all gaps empty and episode ready

2. TestSkillUsageMetrics class (6 tests):
   - calculate_skill_usage_metrics filters by agent_id and date range
   - calculate_skill_usage_metrics counts successful_executions vs total
   - calculate_skill_usage_metrics calculates success_rate
   - calculate_skill_usage_metrics counts unique_skills_used
   - calculate_skill_usage_metrics calculates skill_learning_velocity (episodes per day)
   - calculate_skill_usage_metrics handles days_back parameter correctly

3. TestReadinessWithSkills class (5 tests):
   - calculate_readiness_score_with_skills integrates skill metrics
   - calculate_readiness_score_with_skills applies skill_diversity_bonus (up to +5%)
   - calculate_readiness_score_with_skills caps final_score at 1.0
   - calculate_readiness_score_with_skills returns skill_metrics in result
   - calculate_readiness_score_with_skills returns skill_diversity_bonus in result

4. TestGraduationAuditTrail class (5 tests):
   - get_graduation_audit_trail groups episodes by maturity_at_time
   - get_graduation_audit_trail calculates episodes_by_maturity counts
   - get_graduation_audit_trail includes recent_episodes (max 10)
   - get_graduation_audit_trail handles agent with no episodes (empty lists)
   - get_graduation_audit_trail returns error dict for non-existent agent

Mock dependencies:
- Mock db.query for Episode, SupervisionSession, SkillExecution
- Mock get_lancedb_handler for LanceDB integration
- Mock ConstitutionalValidator for compliance checks

Location: Add after TestSupervisionScoreCalculation class
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/agent/test_agent_graduation_service.py::TestValidationWithSupervision -v</verify>
  <done>Supervision integration tested, skill metrics covered, audit trail verified</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. Test count increased by at least 20 new tests (from ~18 to 38+)
2. Collection error fixed (factory.LambdaFunction issue resolved)
3. Coverage for agent_graduation_service.py increased to 90%+
4. All new tests pass: pytest backend/tests/unit/agent/test_agent_graduation_service.py -v
5. No regressions in existing tests
6. Mock usage is appropriate (external services mocked, not real DB calls)

Coverage check:
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/agent/test_agent_graduation_service.py --cov=backend/core/agent_graduation_service --cov-report=term-missing
</verification>

<success_criteria>
1. 20+ new tests added to test_agent_graduation_service.py (total 38+ tests)
2. Coverage of agent_graduation_service.py reaches 90%+ (verify with --cov)
3. Collection error fixed (tests can be collected and run)
4. All readiness calculation paths tested (boundary conditions, unknown maturity, missing agent)
5. All graduation criteria verified (INTERN, SUPERVISED, AUTONOMOUS thresholds)
6. All promotion decision paths tested (valid, invalid maturity, non-existent agent)
7. Sandbox executor fully covered (edge cases, scoring, violations)
8. Supervision metrics and skill usage metrics fully tested
9. Tests use proper mocks (AsyncMock, db fixtures, external service patches)
</success_criteria>

<output>
After completion, create `.planning/phases/084-core-services-unit-testing-training-graduation/084-02-SUMMARY.md`
</output>
