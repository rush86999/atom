---
phase: 084-core-services-unit-testing-training-graduation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/governance/test_student_training_service.py
autonomous: true

must_haves:
  truths:
    - "Student training service tests cover all proposal generation paths (capability gaps, learning objectives, scenario templates)"
    - "Approval workflow tests cover modifications (duration override, hours per day limit), invalid status handling"
    - "Training completion tests cover confidence boost calculation, promotion decisions, blocked trigger resolution"
    - "Duration estimation tests cover AI-based factors (confidence score, capability gaps, similar agents, learning rate)"
    - "Error handling tests cover missing agents, invalid proposals, database failures"
    - "Tests use AsyncMock for async methods and proper fixtures for database"
  artifacts:
    - path: backend/tests/unit/governance/test_student_training_service.py
      provides: Comprehensive unit tests for StudentTrainingService
      min_lines: 1200
      exports: ["TestProposalGeneration", "TestApprovalWorkflow", "TestTrainingCompletion", "TestDurationEstimation", "TestErrorHandling"]
  key_links:
    - from: backend/tests/unit/governance/test_student_training_service.py
      to: backend/core/student_training_service.py
      via: import StudentTrainingService
      pattern: "from core.student_training_service import StudentTrainingService"
---

<objective>
Expand StudentTrainingService unit tests to achieve 90%+ coverage including proposal generation, approval workflow, training completion, duration estimation, and error handling.

Purpose: StudentTrainingService manages STUDENT agent training and maturity progression. The 20 existing tests cover basic functionality but miss edge cases, error paths, and full coverage of AI-based estimation logic. Additional tests are needed for comprehensive coverage of all code paths.

Output: Enhanced test file with 25+ new tests covering previously untested paths, reaching 90%+ coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/core/student_training_service.py
@backend/tests/unit/governance/test_student_training_service.py

# Phase 82-83 testing patterns to follow:
- Use pytest fixtures for mock database (db_session)
- Use AsyncMock for async methods (_identify_capability_gaps, _generate_learning_objectives)
- Mock external dependencies with patch
- Test both success and failure paths
- Test organization: New test classes added after related existing classes

# Existing test coverage (20 tests):
- TestTrainingDurationEstimation (4 tests)
- TestSessionCreation (3 tests)
- TestTrainingCompletion (3 tests)
- TestTrainingHistory (2 tests)
- TestConfidenceBoostCalculation (4 tests)
- TestCapabilityGapIdentification (2 tests)
- TestLearningRateCalculation (2 tests)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add proposal generation tests with all trigger types</name>
  <files>backend/tests/unit/governance/test_student_training_service.py</files>
  <action>
Add comprehensive tests for create_training_proposal method covering:

1. TestCreateTrainingProposal class (10 tests):
   - Proposal created for agent_message trigger type
   - Proposal created for workflow_trigger trigger type
   - Proposal created for form_submit trigger type
   - Proposal created for canvas_update trigger type
   - Capability gaps identified include category-specific (Finance, Sales, Operations, HR, Support)
   - Learning objectives generated include base objectives (trigger type understanding)
   - Learning objectives generated include capability-specific objectives (max 5 gaps)
   - Scenario template selected based on category from trigger_context
   - Scenario template defaults to "General Operations" for unknown category
   - Duration estimation called with correct parameters (target_maturity=INTERN)
   - Blocked trigger linked to proposal (proposal_id set on blocked_trigger)

2. TestProposalGenerationEdgeCases class (6 tests):
   - Proposal creation fails for non-existent agent (ValueError)
   - Proposal with no capability gaps handled gracefully
   - Proposal with empty trigger_context uses default scenario
   - Deduplication of capability gaps tested (duplicate gaps removed)
   - Proposal title includes scenario template name
   - Proposal description includes blocked task type and capability gaps

Mock dependencies:
- Mock db.query(AgentRegistry).filter().first() to return agent
- Mock AsyncMock for _identify_capability_gaps, _generate_learning_objectives
- Mock AsyncMock for estimate_training_duration returning TrainingDurationEstimate

Location: Add new test classes after TestLearningRateCalculation (after line ~830)
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/governance/test_student_training_service.py::TestCreateTrainingProposal -v</verify>
  <done>All proposal generation paths tested, trigger types covered, edge cases handled</done>
</task>

<task type="auto">
  <name>Task 2: Add approval workflow tests with modifications and error handling</name>
  <files>backend/tests/unit/governance/test_student_training_service.py</files>
  <action>
Add comprehensive tests for approve_training method covering:

1. TestApprovalWithModifications class (8 tests):
   - Duration override applied (user_override_duration_hours set)
   - Hours per day limit applied (hours_per_day_limit set)
   - Training end date calculated with custom hours_per_day_limit
   - Training end date calculated with default 8 hours/day when no limit
   - Proposal status updated to APPROVED
   - Approved by and approved at timestamps set
   - Training start date set to current datetime
   - Training session created with correct total_tasks (from learning_objectives length)

2. TestApprovalErrorHandling class (6 tests):
   - ValueError raised for non-existent proposal
   - ValueError raised for proposal not in PROPOSED status (APPROVED, EXECUTED, REJECTED)
   - Training session created with supervisor_id from user_id
   - Training session agent_name from proposal
   - Training session initial status is "scheduled"
   - Database commit/refresh called in correct order

Mock dependencies:
- Mock db.query(AgentProposal).filter().first() to return proposal
- Mock db.add for TrainingSession
- Mock db.commit and db.refresh

Location: Add after TestCreateTrainingProposal class
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/governance/test_student_training_service.py::TestApprovalWithModifications -v</verify>
  <done>All approval paths tested, modifications verified, error handling covered</done>
</task>

<task type="auto">
  <name>Task 3: Add training completion and promotion decision tests</name>
  <files>backend/tests/unit/governance/test_student_training_service.py</files>
  <action>
Add comprehensive tests for complete_training_session method covering:

1. TestTrainingCompletionPromotion class (8 tests):
   - Agent promoted to INTERN when confidence reaches 0.5 (exact threshold)
   - Agent NOT promoted when confidence below 0.5
   - Agent NOT promoted when already INTERN (status check)
   - Confidence boost capped at 1.0 (max value)
   - Session status updated to "completed"
   - Session completed_at timestamp set
   - Session duration_seconds calculated from started_at to completed_at
   - Session outcomes dict populated with performance_score, tasks_completed

2. TestBlockedTriggerResolution class (6 tests):
   - Blocked trigger resolved set to True
   - Blocked trigger resolved_at timestamp set
   - Blocked trigger resolution_outcome includes performance score
   - Blocked trigger resolution_outcome includes confidence boost
   - Blocked trigger resolution_outcome includes promoted status
   - Proposal status updated to EXECUTED

3. TestCompletionErrorHandling class (4 tests):
   - ValueError raised for non-existent session
   - ValueError raised for non-existent agent
   - Database operations transaction safe (all or nothing)
   - Return dict includes all expected fields (session_id, agent_id, performance_score, confidence_boost, old_confidence, new_confidence, promoted_to_intern, new_status, capabilities_developed)

Mock dependencies:
- Mock db.query(TrainingSession).filter().first() to return session
- Mock db.query(AgentRegistry).filter().first() to return agent
- Mock db.query(AgentProposal).filter().first() for proposal
- Mock db.query(BlockedTriggerContext).filter().first() for blocked trigger

Location: Add after TestApprovalErrorHandling class
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/governance/test_student_training_service.py::TestTrainingCompletionPromotion -v</verify>
  <done>All completion paths tested, promotion decisions verified, trigger resolution covered</done>
</task>

<task type="auto">
  <name>Task 4: Add duration estimation factor tests and learning scenarios</name>
  <files>backend/tests/unit/governance/test_student_training_service.py</files>
  <action>
Add comprehensive tests for estimate_training_duration method covering all estimation factors:

1. TestDurationEstimationFactors class (8 tests):
   - Confidence factor: Lower confidence (0.0) adds 20 hours to base
   - Confidence factor: Higher confidence (0.4) adds fewer hours
   - Capability gaps factor: Each gap adds ~4 hours
   - Historical factor: Similar agents' average duration used
   - Learning rate factor: Fast learner (>1.0) reduces duration
   - Learning rate factor: Slow learner (<1.0) increases duration
   - Min/max bounds: min_hours = 70% of estimate, max_hours = 150% of estimate
   - Confidence in estimate increases with more similar agents (0.5 + n*0.05)

2. TestDurationEstimationEdgeCases class (5 tests):
   - New agent with no similar agents uses base_hours
   - Agent not found raises ValueError
   - Invalid target_maturity handled gracefully
   - Reasoning string includes all 4 factors (confidence, gaps, historical, learning)
   - Similar agents list limited to 5 agents max

3. TestSimilarAgentsHistory class (4 tests):
   - Similar agents filtered by category and target_maturity
   - Similar agents filtered by confidence_score >= 0.5
   - Training duration summed from all completed sessions
   - Session count included in similar_agents data

Mock dependencies:
- Mock db.query(AgentRegistry).filter().first() for agent and similar agents
- Mock db.query(TrainingSession).filter() for similar agent sessions
- Mock AsyncMock for _calculate_learning_rate

Location: Add after TestCompletionErrorHandling class
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/governance/test_student_training_service.py::TestDurationEstimationFactors -v</verify>
  <done>All estimation factors tested, edge cases covered, similar agents logic verified</done>
</task>

<task type="auto">
  <name>Task 5: Add training history and learning objectives generation tests</name>
  <files>backend/tests/unit/governance/test_student_training_service.py</files>
  <action>
Add comprehensive tests for get_training_history and _generate_learning_objectives methods:

1. TestTrainingHistoryDetailed class (6 tests):
   - History returns sessions in descending created_at order
   - History includes proposal_title from linked proposal
   - History includes capability_gaps from linked proposal
   - History calculates training_duration_hours from duration_seconds
   - Limit parameter restricts number of returned sessions
   - History with no sessions returns empty list

2. TestLearningObjectivesGeneration class (6 tests):
   - Base objectives include trigger type execution flow
   - Base objectives include reliable task completion objective
   - Base objectives include decision-making patterns objective
   - Capability-specific objectives added (up to 5 gaps)
   - Category-specific objectives added (Finance, Sales)
   - Objectives deduplicated (no duplicates in final list)

3. TestScenarioTemplateSelection class (4 tests):
   - Finance category returns "Finance Fundamentals"
   - Sales category returns "Sales Operations"
   - Operations category returns "Process Automation"
   - Unknown category returns "General Operations"

Mock dependencies:
- Mock db.query(TrainingSession).filter().order_by().limit()
- Mock db.query(AgentProposal).filter().first() for linked proposals

Location: Add after TestDurationEstimationEdgeCases class
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/governance/test_student_training_service.py::TestTrainingHistoryDetailed -v</verify>
  <done>Training history covered, learning objectives tested, scenario selection verified</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. Test count increased by at least 25 new tests (from 20 to 45+)
2. Coverage for student_training_service.py increased to 90%+
3. All new tests pass: pytest backend/tests/unit/governance/test_student_training_service.py -v
4. No regressions in existing tests
5. Mock usage is appropriate (external services mocked, not real DB calls)

Coverage check:
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/governance/test_student_training_service.py --cov=backend/core/student_training_service --cov-report=term-missing
</verification>

<success_criteria>
1. 25+ new tests added to test_student_training_service.py (total 45+ tests)
2. Coverage of student_training_service.py reaches 90%+ (verify with --cov)
3. All proposal generation paths tested (4 trigger types, 5+ categories)
4. All approval workflow paths tested (modifications, invalid status, error cases)
5. All training completion paths tested (promotion decisions, trigger resolution)
6. All duration estimation factors tested (confidence, gaps, historical, learning rate)
7. Training history and learning objectives generation fully covered
8. Tests use proper mocks (AsyncMock for async methods, db fixtures for database)
</success_criteria>

<output>
After completion, create `.planning/phases/084-core-services-unit-testing-training-graduation/084-01-SUMMARY.md`
</output>
