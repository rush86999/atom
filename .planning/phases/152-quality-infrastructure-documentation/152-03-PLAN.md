---
phase: 152-quality-infrastructure-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/MOBILE_TESTING_GUIDE.md
autonomous: true

must_haves:
  truths:
    - "Mobile developers can find jest-expo/RNTL patterns in one guide"
    - "Platform-specific testing (iOS vs Android) is documented"
    - "Device mocking (camera, location, notifications) is covered"
    - "Coverage targets and measurement are explained"
    - "Cross-references to related docs exist"
  artifacts:
    - path: "docs/MOBILE_TESTING_GUIDE.md"
      provides: "Comprehensive mobile testing guide (jest-expo, RNTL, device mocks)"
      min_lines: 350
  key_links:
    - from: "docs/MOBILE_TESTING_GUIDE.md"
      to: "docs/FRONTEND_TESTING_GUIDE.md"
      via: "Reference link for shared React patterns"
      pattern: "FRONTEND_TESTING_GUIDE.md"
    - from: "docs/MOBILE_TESTING_GUIDE.md"
      to: "docs/PROPERTY_TESTING_PATTERNS.md"
      via: "Link to FastCheck property testing"
      pattern: "PROPERTY_TESTING_PATTERNS.md"
    - from: "docs/MOBILE_TESTING_GUIDE.md"
      to: "docs/TESTING_INDEX.md"
      via: "Link back to central index"
      pattern: "TESTING_INDEX.md"
---

<objective>
Create comprehensive mobile testing guide covering jest-expo, React Native Testing Library, platform-specific testing, and device mocking.

Purpose: Mobile testing uses React Native with platform-specific considerations (iOS vs Android) and device capabilities (camera, location, notifications). No dedicated guide exists for mobile developers. This guide follows the same structure as frontend/backend guides.

Output: A 350+ line guide covering test structure, framework patterns, platform-specific testing, device mocking, and coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-quality-infrastructure-documentation/152-RESEARCH.md
@mobile/jest.config.js
@.planning/phases/139-mobile-platform-specific/139-01-SUMMARY.md
@docs/FRONTEND_TESTING_GUIDE.md
@docs/PROPERTY_TESTING_PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MOBILE_TESTING_GUIDE.md with platform-specific patterns</name>
  <files>docs/MOBILE_TESTING_GUIDE.md</files>
  <action>
Create docs/MOBILE_TESTING_GUIDE.md with following structure:

1. **Header**: Title, "Platform: React Native (Expo)", "Frameworks: jest-expo, React Native Testing Library", "Target: 50%+ coverage", Last Updated

2. **Quick Start (5 min)** section:
   ```bash
   cd mobile
   npm test -- --watchAll=false
   # Expected: 398 tests pass, 100% pass rate
   ```

3. **Test Structure** section:
   ```
   mobile/src/__tests__/
   ├── unit/              # Isolated component/hook tests
   ├── integration/       # API integration (MSW)
   ├── platform-specific/ # iOS vs Android tests
   └── helpers/           # Test utilities (testUtils.ts)
   ```

4. **jest-expo Patterns** section with subsections:

   **Component Testing (React Native Testing Library)**:
   ```typescript
   import { render, screen } from '@testing-library/react-native';

   test('agent card displays agent name and status', () => {
     render(<AgentCard name="Test Agent" status="active" />);
     expect(screen.getByText('Test Agent')).toBeTruthy();
     expect(screen.getByText('active')).toBeTruthy();
   });
   ```

   **Platform-Specific Testing**:
   ```typescript
   import { renderWithPlatform } from '../helpers/testUtils';

   testEachPlatform('status bar uses correct insets', (platform) => {
     const { getByTestId } = renderWithPlatform(<StatusBar />, platform);
     const insets = platform === 'ios' ? getiOSInsets() : getAndroidInsets();
     expect(getByTestId('status-bar')).toHaveStyle({ paddingTop: insets.top });
   });
   ```

5. **Platform Mocking** section:

   **SafeAreaContext Mock**:
   ```typescript
   jest.mock('react-native-safe-area-context', () => ({
     SafeAreaProvider: ({ children }) => children,
     SafeAreaView: ({ children }) => children,
     useSafeAreaInsets: jest.fn(() => ({ top: 0, bottom: 0, left: 0, right: 0 })),
     useSafeAreaFrame: jest.fn(() => ({ width: 320, height: 640 })),
   }));
   ```

   **Platform.OS Switching**:
   ```typescript
   import { getiOSInsets, getAndroidInsets, mockPlatform, restorePlatform } from '../helpers/testUtils';

   beforeEach(() => {
     mockPlatform('ios');
   });

   afterEach(() => {
     restorePlatform();
   });
   ```

6. **Device Capability Mocking** section:

   **Camera Mock**:
   ```typescript
   jest.mock('expo-camera', () => ({
     Camera: { Constants: { Type: { back: 'back' } } },
     requestCameraPermissionsAsync: jest.fn(() => ({ status: 'granted' })),
   }));
   ```

   **Location Mock**:
   ```typescript
   jest.mock('expo-location', () => ({
     requestForegroundPermissionsAsync: jest.fn(() => ({ status: 'granted' })),
     getCurrentPositionAsync: jest.fn(() => ({ coords: { latitude: 37.7749, longitude: -122.4194 } })),
   }));
   ```

   **Notifications Mock**:
   ```typescript
   jest.mock('expo-notifications', () => ({
     requestBadgePermissionsAsync: jest.fn(() => ({ status: 'granted' })),
     setBadgeCountAsync: jest.fn(),
     scheduleNotificationAsync: jest.fn(),
   }));
   ```

7. **Async Testing** section:
   ```typescript
   import { waitFor } from '@testing-library/react-native';

   test('auth context hydrates from storage', async () => {
     const { result } = renderHook(() => useAuth());
     await waitFor(() => expect(result.current.isAuthenticated).toBe(true));
   });
   ```

8. **Coverage** section with table:
   | Module | Target | Current |
   |--------|--------|---------|
   | Navigation | 80% | 95% |
   | Services | 70% | 52% |
   | Screens | 60% | 42% |

   Commands:
   ```bash
   # Generate coverage report
   npm test -- --coverage --watchAll=false

   # View HTML report
   open coverage/lcov-report/index.html
   ```

9. **CI/CD** section:
   ```yaml
   # .github/workflows/mobile-tests.yml
   - name: Run mobile tests
     working-directory: ./mobile
     run: npm test -- --coverage --watchAll=false

   - name: Enforce 50% coverage threshold
     run: |
       COVERAGE=$(node -p "require('./coverage/coverage-summary.json').total.statements.pct")
       if (( $(echo "$COVERAGE < 50" | bc -l) )); then
         echo "Coverage $COVERAGE% below 50% threshold"
         exit 1
       fi
   ```

10. **Platform-Specific Considerations** section:
    - **iOS**: SafeArea handling, StatusBar, Dynamic Island (iPhone 14+)
    - **Android**: Navigation mode (gesture vs button), BackHandler, permissions
    - **Cross-platform**: Use Platform.OS conditional rendering, testBothPlatforms helper

11. **Troubleshooting** section with 3-4 issues:
    - TurboModule registry errors (use jest.mock for expo modules)
    - Async timing issues (use waitFor, flushPromises from testUtils)
    - Platform detection tests (use get_current_platform helper)

12. **Best Practices** section (5 items):
    1. Test behavior, not implementation (RNTL philosophy)
    2. Mock all expo modules (camera, location, notifications)
    3. Test platform-specific variations (iOS vs Android)
    4. Use property tests (FastCheck shared across frontend/mobile)
    5. Isolate tests (cleanup platform mocks in afterEach)

13. **Further Reading** section:
    - Frontend patterns: FRONTEND_TESTING_GUIDE.md (shared React patterns)
    - Property testing: PROPERTY_TESTING_PATTERNS.md (FastCheck)
    - Phase 139 completion: .planning/phases/139-mobile-platform-specific/139-07-SUMMARY.md

14. **See Also** section:
    - [Testing Documentation Index](TESTING_INDEX.md) - Central hub for all testing documentation
    - [Cross-Platform Coverage](CROSS_PLATFORM_COVERAGE.md) - Mobile threshold 50%

Target length: 350-450 lines. Use code blocks for all examples. Reference Phase 139 platform-specific infrastructure.
  </action>
  <verify>
1. File exists at docs/MOBILE_TESTING_GUIDE.md with >350 lines
2. Contains all major sections: Quick Start, Test Structure, jest-expo Patterns, Platform Mocking, Device Capability Mocking, Coverage, CI/CD, Platform-Specific Considerations
3. grep -c "```typescript" docs/MOBILE_TESTING_GUIDE.md returns >= 6
4. Further Reading section links to FRONTEND_TESTING_GUIDE.md, PROPERTY_TESTING_PATTERNS.md
5. Platform-specific considerations section covers iOS and Android differences
  </verify>
  <done>
Mobile developers have comprehensive reference for jest-expo/RNTL patterns with platform-specific considerations. Device mocking is documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update TESTING_INDEX.md with mobile guide link</name>
  <files>docs/TESTING_INDEX.md</files>
  <action>
Update docs/TESTING_INDEX.md to add MOBILE_TESTING_GUIDE.md link in "I Want to Test [Specific Platform]" section:

1. Find the Mobile (React Native) entry
2. Update from "MOBILE_TESTING_GUIDE.md (152-03, to be created)" to "[MOBILE_TESTING_GUIDE.md](MOBILE_TESTING_GUIDE.md) - jest-expo, React Native Testing Library, device mocks"
3. Update description to reflect actual guide content

Keep same relative positioning. Do not modify other sections.
  </action>
  <verify>
1. grep "MOBILE_TESTING_GUIDE.md" docs/TESTING_INDEX.md contains link (not "to be created")
2. grep "jest-expo" docs/TESTING_INDEX.md returns entry
3. grep "React Native Testing Library" docs/TESTING_INDEX.md returns entry
  </verify>
  <done>
Mobile guide is discoverable from central index.
  </done>
</task>

</tasks>

<verification>
Phase 152-03 success criteria:
1. MOBILE_TESTING_GUIDE.md exists with 350+ lines
2. All 3 areas covered (jest-expo, platform-specific testing, device mocking)
3. iOS vs Android differences documented
4. Coverage targets explained
5. Cross-references to related docs exist
</verification>

<success_criteria>
1. Mobile developers have comprehensive testing reference
2. Platform-specific testing patterns are documented
3. Device capability mocking (camera, location, notifications) is covered
4. Phase 139 platform infrastructure is referenced
</success_criteria>

<output>
After completion, create `.planning/phases/152-quality-infrastructure-documentation/152-03-SUMMARY.md`
</output>
