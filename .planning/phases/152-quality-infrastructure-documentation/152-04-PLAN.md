---
phase: 152-quality-infrastructure-documentation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DESKTOP_TESTING_GUIDE.md
autonomous: true

must_haves:
  truths:
    - "Desktop developers can find cargo test/proptest/tarpaulin patterns in one guide"
    - "Platform-specific testing (Windows/macOS/Linux) is documented"
    - "Tauri integration testing is covered"
    - "Coverage targets and measurement are explained"
    - "Cross-references to related docs exist"
  artifacts:
    - path: "docs/DESKTOP_TESTING_GUIDE.md"
      provides: "Comprehensive desktop testing guide (cargo test, proptest, tarpaulin, Tauri)"
      min_lines: 350
  key_links:
    - from: "docs/DESKTOP_TESTING_GUIDE.md"
      to: "docs/DESKTOP_COVERAGE.md"
      via: "Reference link for detailed coverage info"
      pattern: "DESKTOP_COVERAGE.md"
    - from: "docs/DESKTOP_TESTING_GUIDE.md"
      to: "docs/PROPERTY_TESTING_PATTERNS.md"
      via: "Link to proptest property testing"
      pattern: "PROPERTY_TESTING_PATTERNS.md"
    - from: "docs/DESKTOP_TESTING_GUIDE.md"
      to: "docs/TESTING_INDEX.md"
      via: "Link back to central index"
      pattern: "TESTING_INDEX.md"
---

<objective>
Create comprehensive desktop testing guide covering cargo test, proptest, tarpaulin, and Tauri integration testing.

Purpose: Desktop testing uses Rust with Tauri, requiring platform-specific tests (Windows/macOS/Linux) and cargo test infrastructure. No dedicated guide exists for desktop developers. This guide follows the same structure as other platform guides.

Output: A 350+ line guide covering test structure, framework patterns, platform-specific testing, Tauri integration, and coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-quality-infrastructure-documentation/152-RESEARCH.md
@frontend-nextjs/src-tauri/Cargo.toml
@.planning/phases/141-desktop-coverage-expansion/141-06-SUMMARY.md
@.planning/phases/142-desktop-rust-backend/142-07-SUMMARY.md
@docs/DESKTOP_COVERAGE.md
@docs/PROPERTY_TESTING_PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DESKTOP_TESTING_GUIDE.md with Rust/Tauri patterns</name>
  <files>docs/DESKTOP_TESTING_GUIDE.md</files>
  <action>
Create docs/DESKTOP_TESTING_GUIDE.md with following structure:

1. **Header**: Title, "Platform: Tauri (Rust)", "Frameworks: cargo test, proptest, tarpaulin", "Target: 40%+ coverage", Last Updated

2. **Quick Start (5 min)** section:
   ```bash
   cd frontend-nextjs/src-tauri
   cargo test
   # Expected: 83 tests compile and pass
   ```

3. **Test Structure** section:
   ```
   frontend-nextjs/src-tauri/tests/
   ├── main.rs             # Integration tests
   ├── platform_specific/
   │   ├── mod.rs         # Platform module exports
   │   ├── windows.rs     # Windows-specific tests (#[cfg(target_os = "windows")])
   │   ├── macos.rs       # macOS-specific tests (#[cfg(target_os = "macos")])
   │   └── linux.rs       # Linux-specific tests (#[cfg(target_os = "linux")])
   └── helpers/            # Test utilities
   ```

4. **cargo test Patterns** section with subsections:

   **Unit Tests**:
   ```rust
   #[cfg(test)]
   mod tests {
       use super::*;

       #[test]
       fn test_platform_detection() {
           let platform = get_current_platform();
           assert!(!platform.is_empty());
       }
   }
   ```

   **Async Tests (tokio)**:
   ```rust
   #[cfg(test)]
   mod tests {
       use super::*;
       use tokio::test;

       #[test]
       async fn test_async_device_capability() async {
           let result = get_device_capability().await;
           assert!(result.is_ok());
       }
   }
   ```

   **Platform-Specific Tests**:
   ```rust
   #[cfg(test)]
   #[cfg(target_os = "windows")]
   mod windows_tests {
       use super::*;

       #[test]
       fn test_windows_temp_directory() {
           let temp = get_temp_dir();
           assert!(temp.to_string_lossy().contains('\\'));
       }
   }
   ```

5. **Property Testing (proptest)** section:

   **Invariant Testing**:
   ```rust
   proptest! {
       #[test]
       fn test_path_normalization_doesnt_change_valid_paths(path in "[a-zA-Z0-9_/]+") {
           let normalized = normalize_path(&path);
           assert!(normalized.contains('/'));
           assert!(!normalized.contains('\\'));
       }
   }
   ```

   **Error Handling Properties**:
   ```rust
   proptest! {
       #[test]
       fn test_device_capability_fails_gracefully_on_invalid_input(input in "[^\u{0}-\u{7F}]*") {
           let result = validate_device_capability(&input);
           assert!(result.is_err() || input.is_empty());
       }
   }
   ```

6. **Tauri Integration Testing** section:

   **IPC Command Tests**:
   ```rust
   #[cfg(test)]
   #[cfg_attr(mobile, tauri::mobile_entry_point)]
   mod tests {
       use tauri::Manager;

       #[test]
       fn test_invoke_handler() {
           let app = tauri::test::mock_app();
           let result = app.emit_all("test-event", "test-payload");
           assert!(result.is_ok());
       }
   }
   ```

   **Window Operations**:
   ```rust
   #[test]
       fn test_window_creation() {
           let app = tauri::test::mock_app();
           let window = app.get_window("main").unwrap();
           assert_eq!(window.label(), "main");
       }
   }
   ```

7. **Platform-Specific Patterns** section:

   **Windows**:
   ```rust
   #[cfg(target_os = "windows")]
   #[test]
   fn test_windows_file_dialog() {
       let path = PathBuf::from("C:\\Users\\Test\\file.txt");
       assert!(path.is_absolute());
       assert!(path.starts_with("C:\\"));
   }
   ```

   **macOS**:
   ```rust
   #[cfg(target_os = "macos")]
   #[test]
   fn test_macos_app_support_dir() {
       let support_dir = dirs_next::config_dir();
       assert!(support_dir.is_some());
       assert!(support_dir.unwrap().ends_with("Application Support"));
   }
   ```

   **Linux**:
   ```rust
   #[cfg(target_os = "linux")]
   #[test]
   fn test_linux_xdg_config() {
       let config = std::env::var("XDG_CONFIG_HOME").ok();
       let home = std::env::var("HOME").ok();
       assert!(config.is_some() || home.is_some());
   }
   ```

8. **Coverage (tarpaulin)** section with table:
   | Module | Target | Current |
   |--------|--------|---------|
   | IPC Commands | 80% | 65% |
   | Device Capabilities | 50% | 15% |
   | System Tray | 60% | 0% |

   Commands:
   ```bash
   # Generate coverage report
   cargo tarpaulin --out Json --output-dir coverage

   # Generate HTML report
   cargo tarpaulin --out Html --output-dir coverage

   # View report
   open coverage/index.html
   ```

9. **CI/CD** section:
   ```yaml
   # .github/workflows/desktop-tests.yml
   - name: Run desktop tests
     working-directory: ./frontend-nextjs/src-tauri
     run: cargo test --verbose

   - name: Generate coverage
     working-directory: ./frontend-nextjs/src-tauri
     run: cargo tarpaulin --out Json --output-dir coverage -- --test-threads=1

   - name: Enforce 40% threshold
     run: |
       COVERAGE=$(node -p "JSON.parse(require('fs').readFileSync('coverage/coverage.json')).coverage")
       if (( $(echo "$COVERAGE < 40" | bc -l) )); then
         echo "Coverage $COVERAGE% below 40% threshold"
         exit 1
       fi
   ```

10. **Test Helpers** section:
    ```rust
    // helpers/mod.rs
    pub fn get_current_platform() -> String {
        std::env::const_detect::detect().into()
    }

    pub fn is_platform(target: &str) -> bool {
        std::env::consts::OS == target
    }

    pub fn get_temp_dir() -> PathBuf {
        std::env::temp_dir()
    }
    ```

11. **Troubleshooting** section with 3-4 issues:
    - tarpaulin linking errors on macOS (use CI/CD ubuntu-latest runner)
    - cfg! tests not running on other platforms (expected behavior)
    - Async test panics (use #[tokio::test] attribute)
    - GUI tests failing (GUI context not available, use unit tests instead)

12. **Best Practices** section (5 items):
    1. Use #[cfg(target_os)] for platform-specific tests (compile-time filtering)
    2. Property test invariants with proptest (not just example-based)
    3. Test error paths explicitly (Result types, error propagation)
    4. Use test-threads=1 for tarpaulin (single-threaded coverage)
    5. Mock external dependencies (file system, hardware)

13. **Further Reading** section:
    - Property testing: PROPERTY_TESTING_PATTERNS.md (proptest section)
    - Phase 141 completion: .planning/phases/141-desktop-coverage-expansion/141-06-SUMMARY.md
    - Phase 142 completion: .planning/phases/142-desktop-rust-backend/142-07-SUMMARY.md

14. **See Also** section:
    - [Testing Documentation Index](TESTING_INDEX.md) - Central hub for all testing documentation
    - [Desktop Coverage](DESKTOP_COVERAGE.md) - Detailed coverage analysis and gap tracking

Target length: 350-450 lines. Use code blocks for all examples. Reference Phase 141-142 infrastructure.
  </action>
  <verify>
1. File exists at docs/DESKTOP_TESTING_GUIDE.md with >350 lines
2. Contains all major sections: Quick Start, Test Structure, cargo test Patterns, proptest, Tauri Integration, Platform-Specific Patterns, Coverage, CI/CD
3. grep -c "```rust" docs/DESKTOP_TESTING_GUIDE.md returns >= 6
4. Further Reading section links to PROPERTY_TESTING_PATTERNS.md
5. Platform-specific patterns section covers Windows, macOS, Linux
  </verify>
  <done>
Desktop developers have comprehensive reference for cargo test/proptest/tarpaulin patterns with Tauri integration. Platform-specific testing is documented.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update TESTING_INDEX.md with desktop guide link</name>
  <files>docs/TESTING_INDEX.md</files>
  <action>
Update docs/TESTING_INDEX.md to add DESKTOP_TESTING_GUIDE.md link in "I Want to Test [Specific Platform]" section:

1. Find the Desktop (Tauri/Rust) entry
2. Update from "DESKTOP_TESTING_GUIDE.md (152-04, to be created)" to "[DESKTOP_TESTING_GUIDE.md](DESKTOP_TESTING_GUIDE.md) - cargo test, proptest, tarpaulin, Tauri integration"
3. Update description to reflect actual guide content

Keep same relative positioning. Do not modify other sections.
  </action>
  <verify>
1. grep "DESKTOP_TESTING_GUIDE.md" docs/TESTING_INDEX.md contains link (not "to be created")
2. grep "tarpaulin" docs/TESTING_INDEX.md returns entry
3. grep "proptest" docs/TESTING_INDEX.md returns entry
  </verify>
  <done>
Desktop guide is discoverable from central index.
  </done>
</task>

</tasks>

<verification>
Phase 152-04 success criteria:
1. DESKTOP_TESTING_GUIDE.md exists with 350+ lines
2. All 4 areas covered (cargo test, proptest, tarpaulin, Tauri integration)
3. Platform-specific testing (Windows/macOS/Linux) documented
4. Coverage targets explained
5. Cross-references to related docs exist
</verification>

<success_criteria>
1. Desktop developers have comprehensive testing reference
2. Tauri integration testing patterns are documented
3. Platform-specific test patterns (cfg! guards) are explained
4. Phase 141-142 infrastructure is referenced
</success_criteria>

<output>
After completion, create `.planning/phases/152-quality-infrastructure-documentation/152-04-SUMMARY.md`
</output>
