---
phase: 169-tools-integrations-coverage
plan: 02
type: execute
wave: 2
depends_on: ["169-01"]
files_modified:
  - backend/tests/unit/test_browser_tool.py
  - backend/tests/tools/test_browser_tool_complete.py
autonomous: true

must_haves:
  truths:
    - "BrowserSession class methods are tested (start, close)"
    - "BrowserSessionManager class methods are tested (create_session, get_session, close_session)"
    - "browser_create_session function is tested with governance enforcement"
    - "browser_navigate function is tested with Playwright mocks"
    - "browser_screenshot function is tested with base64 encoding"
  artifacts:
    - path: "backend/tests/unit/test_browser_tool.py"
      provides: "Browser tool unit tests"
      min_lines: 200
      exports: ["TestBrowserSession", "TestBrowserSessionManager", "TestBrowserFunctions"]
    - path: "backend/tests/tools/test_browser_tool_complete.py"
      provides: "Comprehensive browser tool tests"
      min_lines: 400
  key_links:
    - from: "backend/tests/unit/test_browser_tool.py"
      to: "backend/tools/browser_tool.py"
      via: "AsyncMock for Playwright objects"
      pattern: "mock_playwright|mock_browser|mock_page"
    - from: "backend/tests/unit/test_browser_tool.py"
      to: "core/service_factory.py"
      via: "Governance service mocking"
      pattern: "ServiceFactory\\.get_governance_service"
---

<objective>
Implement comprehensive tests for browser_tool.py functions with proper Playwright AsyncMock fixtures and governance integration.

Target: 75%+ line coverage for browser_tool.py (299 uncovered lines per gap analysis).

Browser tool has 9 public async functions requiring AsyncMock patterns:
- browser_create_session: Session creation with governance (INTERN+)
- browser_navigate: URL navigation with response handling
- browser_screenshot: Full page and viewport capture
- browser_fill_form: Multi-field form filling with submission
- browser_click: Element clicking with wait states
- browser_extract_text: Text extraction from page or selectors
- browser_execute_script: JavaScript execution in browser context
- browser_close_session: Session cleanup
- browser_get_page_info: Page metadata (title, URL, cookies)

Purpose: Provide comprehensive coverage with mocked Playwright objects (no real browsers).
Output: Working test file with 40+ tests covering all browser functions.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/169-tools-integrations-coverage/169-RESEARCH.md
@.planning/phases/169-tools-integrations-coverage/169-01-SUMMARY.md
@backend/tools/browser_tool.py
@backend/tests/tools/test_browser_tool_complete.py
@backend/tests/unit/test_browser_tool.py
</context>

<tasks>

<task type="auto">
  <name>Create AsyncMock fixtures for Playwright objects</name>
  <files>backend/tests/unit/test_browser_tool.py</files>
  <action>
    Create comprehensive AsyncMock fixtures for Playwright async API:

    1. mock_playwright fixture:
       - Patches `tools.browser_tool.async_playwright`
       - Returns MagicMock with __aenter__ for async context manager
       - Mocks chromium.launch() as AsyncMock

    2. mock_browser fixture:
       - MagicMock with spec=Browser
       - AsyncMock for new_context(), close()

    3. mock_browser_context fixture:
       - MagicMock with spec=BrowserContext
       - AsyncMock for new_page(), cookies(), close()

    4. mock_page fixture:
       - MagicMock with spec=Page
       - AsyncMock for goto(), title(), screenshot(), wait_for_selector(), fill(), click(), query_selector(), query_selector_all(), inner_text(), evaluate()

    5. mock_db fixture:
       - MagicMock with add(), commit(), refresh(), query(), rollback()

    6. mock_governance_service fixture:
       - MagicMock with can_perform_action() returning {"allowed": True, "reason": "..."}
       - AsyncMock for record_outcome()

    Reference existing patterns in backend/tests/tools/test_browser_tool_complete.py (lines 59-136).
  </action>
  <verify>cd backend && python -m pytest tests/unit/test_browser_tool.py::test_fixtures -v 2>/dev/null || python -c "from tests.unit.test_browser_tool import *; print('Fixtures load OK')"</verify>
  <done>All fixtures created and importable</done>
</task>

<task type="auto">
  <name>Test BrowserSession and BrowserSessionManager classes</name>
  <files>backend/tests/unit/test_browser_tool.py</files>
  <action>
    Create tests for BrowserSession and BrowserSessionManager classes:

    TestBrowserSession (5 tests):
    - test_browser_session_initialization: Verify all params stored
    - test_browser_session_start_chromium: Mock playwright, verify chromium launch
    - test_browser_session_start_firefox: Mock playwright, verify firefox launch
    - test_browser_session_close: Verify cleanup order (page, context, browser, playwright)
    - test_browser_session_properties: Verify headless, browser_type, user_id stored

    TestBrowserSessionManager (5 tests):
    - test_create_session: Verify session created and started
    - test_get_session: Verify session retrieval by ID
    - test_get_session_not_found: Verify None returned for invalid ID
    - test_close_session: Verify session removed from dict
    - test_cleanup_expired_sessions: Verify old sessions removed

    Use mock_playwright and AsyncMock for all async methods.
  </action>
  <verify>cd backend && python -m pytest tests/unit/test_browser_tool.py::TestBrowserSession tests/unit/test_browser_tool.py::TestBrowserSessionManager -v</verify>
  <done>10 tests passing for session classes</done>
</task>

<task type="auto">
  <name>Test browser_create_session with governance enforcement</name>
  <files>backend/tests/unit/test_browser_tool.py</files>
  <action>
    Create tests for browser_create_session function:

    TestBrowserCreateSession (6 tests):
    - test_create_session_success_no_agent: Create session without agent (no governance check)
    - test_create_session_success_intern_agent: Mock governance to allow INTERN
    - test_create_session_governance_blocked: Mock governance to deny STUDENT
    - test_create_session_headless_default: Verify BROWSER_HEADLESS env var used when headless=None
    - test_create_session_failure_exception: Test exception handling and governance.record_outcome
    - test_create_session_with_db_commit: Verify AgentExecution created and committed

    Use mock_governance_service to control allow/block decisions.
    Mock ServiceFactory.get_governance_service to return the mock.
    Patch FeatureFlags.should_enforce_governance to enable checks.
  </action>
  <verify>cd backend && python -m pytest tests/unit/test_browser_tool.py::TestBrowserCreateSession -v</verify>
  <done>6 tests passing for browser_create_session</done>
</task>

<task type="auto">
  <name>Test browser_navigate and browser_screenshot functions</name>
  <files>backend/tests/unit/test_browser_tool.py</files>
  <action>
    Create tests for browser navigation and screenshot functions:

    TestBrowserNavigate (5 tests):
    - test_navigate_success: Mock page.goto returning response with status=200
    - test_navigate_session_not_found: Verify error returned for invalid session_id
    - test_navigate_user_mismatch: Verify error when session.user_id != user_id
    - test_navigate_timeout_error: Mock goto to raise TimeoutError, verify error handling
    - test_navigate_with_wait_until: Test different wait_until values (load, domcontentloaded, networkidle)

    TestBrowserScreenshot (5 tests):
    - test_screenshot_base64: Mock page.screenshot returning bytes, verify base64 encoding
    - test_screenshot_to_file: Mock screenshot and verify file write with os.makedirs
    - test_screenshot_full_page: Test full_page=True parameter
    - test_screenshot_session_not_found: Verify error for invalid session
    - test_screenshot_encoding_error: Mock screenshot to raise exception, verify error handling

    Use mock_page with AsyncMock for goto(), title(), screenshot().
  </action>
  <verify>cd backend && python -m pytest tests/unit/test_browser_tool.py::TestBrowserNavigate tests/unit/test_browser_tool.py::TestBrowserScreenshot -v</verify>
  <done>10 tests passing for navigation and screenshot</done>
</task>

<task type="auto">
  <name>Test browser_fill_form and browser_click functions</name>
  <files>backend/tests/unit/test_browser_tool.py</files>
  <action>
    Create tests for form filling and clicking functions:

    TestBrowserFillForm (5 tests):
    - test_fill_form_success: Mock wait_for_selector, query_selector, evaluate, fill for multiple fields
    - test_fill_form_with_select_option: Test SELECT element handling with select_option
    - test_fill_form_with_submit: Mock submit button click, verify submitted=True
    - test_fill_form_field_failure: Mock one field to raise exception, verify others still fill
    - test_fill_form_empty_selectors: Test empty selectors dict returns fields_filled=0

    TestBrowserClick (5 tests):
    - test_click_success: Mock wait_for_selector and click
    - test_click_with_wait_for: Test wait_for parameter after click
    - test_click_session_not_found: Verify error for invalid session
    - test_click_timeout: Mock wait_for_selector to raise TimeoutError
    - test_click_element_not_found: Mock query_selector to return None

    Use mock_page with AsyncMock for all page interactions.
  </action>
  <verify>cd backend && python -m pytest tests/unit/test_browser_tool.py::TestBrowserFillForm tests/unit/test_browser_tool.py::TestBrowserClick -v</verify>
  <done>10 tests passing for form and click functions</done>
</task>

<task type="auto">
  <name>Test remaining browser functions (extract_text, execute_script, close_session, get_page_info)</name>
  <files>backend/tests/unit/test_browser_tool.py</files>
  <action>
    Create tests for remaining browser functions:

    TestBrowserExtractText (3 tests):
    - test_extract_text_full_page: Mock inner_text("body") to return page text
    - test_extract_text_with_selector: Mock query_selector_all and inner_text for specific elements
    - test_extract_text_session_not_found: Verify error for invalid session

    TestBrowserExecuteScript (3 tests):
    - test_execute_script_success: Mock page.evaluate to return result
    - test_execute_script_error: Mock evaluate to raise exception
    - test_execute_script_session_not_found: Verify error for invalid session

    TestBrowserCloseSession (2 tests):
    - test_close_session_success: Mock manager.close_session returning True
    - test_close_session_not_found: Verify error for invalid session

    TestBrowserGetPageInfo (2 tests):
    - test_get_page_info_success: Mock title(), url, context.cookies()
    - test_get_page_info_session_not_found: Verify error for invalid session

    Total: 10 tests for remaining functions
  </action>
  <verify>cd backend && python -m pytest tests/unit/test_browser_tool.py::TestBrowserExtractText tests/unit/test_browser_tool.py::TestBrowserExecuteScript tests/unit/test_browser_tool.py::TestBrowserCloseSession tests/unit/test_browser_tool.py::TestBrowserGetPageInfo -v</verify>
  <done>10 tests passing for remaining functions</done>
</task>

</tasks>

<verification>
After completion:
1. Run: `cd backend && python -m pytest tests/unit/test_browser_tool.py -v --cov=tools/browser_tool --cov-report=term-missing`
2. Verify 40+ tests total
3. Verify 75%+ coverage for browser_tool.py
4. All tests use AsyncMock for Playwright objects (no real browser launches)
</verification>

<success_criteria>
- 40+ tests covering all browser functions
- 75%+ line coverage for browser_tool.py
- All tests pass with mocked Playwright objects
- Governance enforcement tested (INTERN+ allowed, STUDENT blocked)
</success_criteria>

<output>
After completion, create `.planning/phases/169-tools-integrations-coverage/169-02-SUMMARY.md`
</output>
