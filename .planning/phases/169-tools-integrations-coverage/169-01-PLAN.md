---
phase: 169-tools-integrations-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tools/browser_tool.py
  - backend/tools/device_tool.py
  - backend/tests/unit/test_browser_tool.py
  - backend/tests/unit/test_device_tool.py
  - backend/tests/tools/test_browser_tool_complete.py
  - backend/tests/tools/test_device_tool_complete.py
autonomous: true

must_haves:
  truths:
    - "browser_tool.py imports without syntax errors"
    - "device_tool.py imports without encoding or syntax errors"
    - "Test files can import tool modules successfully"
    - "Existing test fixtures compile without errors"
  artifacts:
    - path: "backend/tools/browser_tool.py"
      provides: "Browser automation tool functions"
      contains: "from playwright.async_api import Browser, BrowserContext, Page"
    - path: "backend/tools/device_tool.py"
      provides: "Device capabilities tool functions"
      contains: "WEBSOCKET_AVAILABLE"
    - path: "backend/tests/unit/test_browser_tool.py"
      provides: "Browser tool unit tests with AsyncMock fixtures"
    - path: "backend/tests/unit/test_device_tool.py"
      provides: "Device tool unit tests with AsyncMock fixtures"
  key_links:
    - from: "backend/tests/unit/test_browser_tool.py"
      to: "backend/tools/browser_tool.py"
      via: "from tools.browser_tool import"
      pattern: "from tools\\.browser_tool import"
    - from: "backend/tests/unit/test_device_tool.py"
      to: "backend/tools/device_tool.py"
      via: "from tools.device_tool import"
      pattern: "from tools\\.device_tool import"
---

<objective>
Fix syntax errors and encoding issues in browser_tool.py and device_tool.py to unblock existing tests.

Both tool files have critical issues preventing import:
- browser_tool.py: Syntax error on line 53 (trailing comma in type annotation)
- device_tool.py: Non-ASCII character without encoding declaration

These issues prevent all 19 existing test files from running, despite having comprehensive test patterns already written.

Purpose: Unblock existing test infrastructure so tests can execute and coverage can be measured.
Output: Two tool files that import successfully, enabling test execution.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/169-tools-integrations-coverage/169-RESEARCH.md
@backend/tools/browser_tool.py
@backend/tools/device_tool.py
@backend/tests/tools/test_browser_tool_complete.py
@backend/tests/tools/test_device_tool_complete.py
</context>

<tasks>

<task type="auto">
  <name>Fix browser_tool.py syntax error</name>
  <files>backend/tools/browser_tool.py</files>
  <action>
    Fix the syntax error on line 53 of browser_tool.py:
    - Current: `session_id: str,` (invalid trailing comma in class method signature position)
    - The BrowserSession.__init__ method has correct syntax - line 53 appears to be a duplicate or malformed line
    - Remove the problematic line entirely (the correct __init__ starts at line 51)
    - Verify the fix by running: `python -c "from tools.browser_tool import browser_create_session; print('OK')"`
  </action>
  <verify>python -c "from backend.tools.browser_tool import browser_create_session; print('browser_tool imports OK')"</verify>
  <done>browser_tool.py imports without SyntaxError</done>
</task>

<task type="auto">
  <name>Fix device_tool.py encoding declaration</name>
  <files>backend/tools/device_tool.py</files>
  <action>
    Fix the encoding issue in device_tool.py:
    - Add `# -*- coding: utf-8 -*-` or `from __future__ import unicode_literals` at the top of the file
    - The file contains non-ASCII characters (likely in the docstring with checkmark emoji)
    - Place encoding declaration on line 1, before the docstring
    - Verify the fix by running: `python -c "from tools.device_tool import device_camera_snap; print('OK')"`
  </action>
  <verify>python -c "from backend.tools.device_tool import device_camera_snap; print('device_tool imports OK')"</verify>
  <done>device_tool.py imports without encoding error</done>
</task>

<task type="auto">
  <name>Verify test files can import and run basic checks</name>
  <files>backend/tests/unit/test_browser_tool.py, backend/tests/unit/test_device_tool.py</files>
  <action>
    Verify existing test files can now import the tool modules:
    1. Check if test files exist and are syntactically valid
    2. Try to import the test modules to verify they can load
    3. Run pytest with --collect-only to see if tests can be discovered
    4. If there are additional import errors, document them for next plans

    Run: `cd backend && python -m pytest tests/unit/test_browser_tool.py --collect-only -q`
    Run: `cd backend && python -m pytest tests/unit/test_device_tool.py --collect-only -q`
  </action>
  <verify>cd backend && python -m pytest tests/unit/test_browser_tool.py tests/unit/test_device_tool.py --collect-only -q</verify>
  <done>Tests can be collected (may fail but imports work)</done>
</task>

</tasks>

<verification>
After completion:
1. `python -c "from backend.tools.browser_tool import *"` completes without error
2. `python -c "from backend.tools.device_tool import *"` completes without error
3. `pytest --collect-only backend/tests/unit/test_browser_tool.py` shows tests collected
4. `pytest --collect-only backend/tests/unit/test_device_tool.py` shows tests collected
</verification>

<success_criteria>
- browser_tool.py imports successfully with no SyntaxError
- device_tool.py imports successfully with no encoding error
- Test discovery works for both test files (tests may fail but imports succeed)
</success_criteria>

<output>
After completion, create `.planning/phases/169-tools-integrations-coverage/169-01-SUMMARY.md`
</output>
