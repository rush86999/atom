---
phase: 169-tools-integrations-coverage
plan: 03
type: execute
wave: 2
depends_on: ["169-01"]
files_modified:
  - backend/tests/unit/test_device_tool.py
  - backend/tests/tools/test_device_tool_complete.py
autonomous: true

must_haves:
  truths:
    - "device_camera_snap function is tested with WebSocket mocking"
    - "device_screen_record_start function is tested with SUPERVISED+ governance"
    - "device_get_location function is tested with location data parsing"
    - "device_send_notification function is tested with notification delivery"
    - "device_execute_command function is tested with AUTONOMOUS-only enforcement"
  artifacts:
    - path: "backend/tests/unit/test_device_tool.py"
      provides: "Device tool unit tests"
      min_lines: 300
      exports: ["TestDeviceCameraSnap", "TestDeviceScreenRecord", "TestDeviceGetLocation", "TestDeviceSendNotification", "TestDeviceExecuteCommand"]
  key_links:
    - from: "backend/tests/unit/test_device_tool.py"
      to: "backend/tools/device_tool.py"
      via: "AsyncMock for WebSocket communication"
      pattern: "WEBSOCKET_AVAILABLE|send_device_command|is_device_online"
    - from: "backend/tests/unit/test_device_tool.py"
      to: "core/service_factory.py"
      via: "Governance service mocking"
      pattern: "ServiceFactory\\.get_governance_service"
---

<objective>
Implement comprehensive tests for device_tool.py functions with proper WebSocket AsyncMock fixtures and governance integration.

Target: 75%+ line coverage for device_tool.py (308 uncovered lines per gap analysis).

Device tool has 9 public async functions requiring AsyncMock patterns:
- device_camera_snap: Camera capture with governance (INTERN+)
- device_screen_record_start: Recording session creation with governance (SUPERVISED+)
- device_screen_record_stop: Recording session termination
- device_get_location: Location retrieval with governance (INTERN+)
- device_send_notification: Push notification delivery with governance (INTERN+)
- device_execute_command: Shell command execution with governance (AUTONOMOUS only)
- get_device_info: Device metadata retrieval
- list_devices: User device enumeration
- execute_device_command: Generic command router

Purpose: Provide comprehensive coverage with mocked WebSocket communication (no real devices).
Output: Working test file with 40+ tests covering all device functions.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/169-tools-integrations-coverage/169-RESEARCH.md
@.planning/phases/169-tools-integrations-coverage/169-01-SUMMARY.md
@backend/tools/device_tool.py
@backend/tests/tools/test_device_tool_complete.py
@backend/tests/unit/test_device_tool.py
</context>

<tasks>

<task type="auto">
  <name>Create AsyncMock fixtures for device WebSocket communication</name>
  <files>backend/tests/unit/test_device_tool.py</files>
  <action>
    Create comprehensive AsyncMock fixtures for WebSocket device communication:

    1. mock_db fixture:
       - MagicMock with add(), commit(), refresh(), query(), rollback()

    2. mock_device fixture:
       - MagicMock(spec=DeviceNode) with device_id, name, node_type, status, platform, capabilities
       - Set user_id for ownership tests

    3. Mock agent fixtures for maturity levels:
       - mock_student_agent: maturity_level=0 (blocked for most actions)
       - mock_intern_agent: maturity_level=1 (camera, location, notifications allowed)
       - mock_supervised_agent: maturity_level=2 (screen recording allowed)
       - mock_autonomous_agent: maturity_level=3 (command execution allowed)

    4. mock_governance_service fixture:
       - MagicMock with can_perform_action() returning configurable allow/block
       - AsyncMock for record_outcome()

    5. WebSocket communication patches:
       - Patch `tools.device_tool.WEBSOCKET_AVAILABLE` to True
       - Patch `tools.device_tool.is_device_online` to return True
       - Patch `tools.device_tool.send_device_command` as AsyncMock returning success

    Reference existing patterns in backend/tests/tools/test_device_tool_complete.py (lines 41-127).
  </action>
  <verify>cd backend && python -c "from tests.unit.test_device_tool import *; print('Fixtures load OK')"</verify>
  <done>All fixtures created and importable</done>
</task>

<task type="auto">
  <name>Test device_camera_snap with governance enforcement</name>
  <files>backend/tests/unit/test_device_tool.py</files>
  <action>
    Create tests for device_camera_snap function:

    TestDeviceCameraSnap (6 tests):
    - test_camera_snap_success: Patch WEBSOCKET_AVAILABLE=True, is_device_online=True, mock send_device_command to return base64 data
    - test_camera_snap_intern_allowed: Mock governance to allow INTERN agent
    - test_camera_snap_student_blocked: Mock governance to block STUDENT agent, verify governance_blocked=True
    - test_camera_snap_device_offline: Mock is_device_online=False, verify error about device not connected
    - test_camera_snap_websocket_unavailable: Patch WEBSOCKET_AVAILABLE=False, verify error message
    - test_camera_snap_audit_created: Verify _create_device_audit called with correct params

    Key patches needed:
    - `patch('tools.device_tool.WEBSOCKET_AVAILABLE', True)`
    - `patch('tools.device_tool.is_device_online', return_value=True)`
    - `patch('tools.device_tool.send_device_command', new_callable=AsyncMock)`
    - `patch('core.service_factory.ServiceFactory.get_governance_service')`
  </action>
  <verify>cd backend && python -m pytest tests/unit/test_device_tool.py::TestDeviceCameraSnap -v</verify>
  <done>6 tests passing for camera snap function</done>
</task>

<task type="auto">
  <name>Test device_screen_record_start and device_screen_record_stop</name>
  <files>backend/tests/unit/test_device_tool.py</files>
  <action>
    Create tests for screen recording functions:

    TestDeviceScreenRecordStart (6 tests):
    - test_screen_record_start_success_supervised: Mock governance to allow SUPERVISED agent
    - test_screen_record_start_intern_blocked: Mock governance to block INTERN agent (SUPERVISED+ required)
    - test_screen_record_start_duration_validation: Test max duration enforcement (DEVICE_SCREEN_RECORD_MAX_DURATION)
    - test_screen_record_start_device_not_found: Mock db.query to return None for device, verify error
    - test_screen_record_start_session_created: Verify DeviceSession created in database with status="active"
    - test_screen_record_start_websocket_command: Verify send_device_command called with correct params

    TestDeviceScreenRecordStop (4 tests):
    - test_screen_record_stop_success: Mock session exists, verify close and file_path returned
    - test_screen_record_stop_session_not_found: Verify error for invalid session_id
    - test_screen_record_stop_user_mismatch: Verify error when session.user_id != user_id
    - test_screen_record_stop_database_updated: Verify DeviceSession status set to "closed"

    Use AsyncMock for send_device_command and mock_db.query for DeviceNode/DeviceSession lookups.
  </action>
  <verify>cd backend && python -m pytest tests/unit/test_device_tool.py::TestDeviceScreenRecordStart tests/unit/test_device_tool.py::TestDeviceScreenRecordStop -v</verify>
  <done>10 tests passing for screen recording functions</done>
</task>

<task type="auto">
  <name>Test device_get_location and device_send_notification</name>
  <files>backend/tests/unit/test_device_tool.py</files>
  <action>
    Create tests for location and notification functions:

    TestDeviceGetLocation (5 tests):
    - test_get_location_success: Mock send_device_command to return lat/long/accuracy data
    - test_get_location_intern_allowed: Mock governance to allow INTERN agent
    - test_get_location_student_blocked: Mock governance to block STUDENT agent
    - test_get_location_device_offline: Mock is_device_online=False
    - test_get_location_audit_created: Verify audit entry created

    TestDeviceSendNotification (5 tests):
    - test_send_notification_success: Mock send_device_command to return success
    - test_send_notification_intern_allowed: Mock governance to allow INTERN agent
    - test_send_notification_student_blocked: Mock governance to block STUDENT agent
    - test_send_notification_with_sound_and_icon: Test optional parameters
    - test_send_notification_audit_created: Verify audit entry with notification details

    Same WebSocket patch pattern as camera tests.
  </action>
  <verify>cd backend && python -m pytest tests/unit/test_device_tool.py::TestDeviceGetLocation tests/unit/test_device_tool.py::TestDeviceSendNotification -v</verify>
  <done>10 tests passing for location and notification functions</done>
</task>

<task type="auto">
  <name>Test device_execute_command with AUTONOMOUS-only enforcement</name>
  <files>backend/tests/unit/test_device_tool.py</files>
  <action>
    Create tests for device_execute_command function:

    TestDeviceExecuteCommand (8 tests):
    - test_execute_command_success_autonomous: Mock governance to allow AUTONOMOUS agent for read command (ls)
    - test_execute_command_read_command_intern_allowed: Test ls command with INTERN agent (device_shell_read)
    - test_execute_command_supervised_monitor_allowed: Test ps command with SUPERVISED agent (device_shell_monitor)
    - test_execute_command_autonomous_only: Mock governance to block SUPERVISED agent for full command execution
    - test_execute_command_whitelist_enforcement: Test command not in DEVICE_COMMAND_WHITELIST is rejected
    - test_execute_command_timeout_validation: Test timeout > 300 seconds is rejected
    - test_execute_command_success_response: Mock send_device_command to return exit_code/stdout/stderr
    - test_execute_command_audit_created: Verify audit entry with command details

    Key governance actions to test:
    - device_shell_read (INTERN+): ls, pwd, cat, grep, head, tail, echo
    - device_shell_monitor (SUPERVISED+): find, ps, top, df, du
    - device_execute_command (AUTONOMOUS only): other commands

    Test whitelist: `DEVICE_COMMAND_WHITELIST` env var default values.
  </action>
  <verify>cd backend && python -m pytest tests/unit/test_device_tool.py::TestDeviceExecuteCommand -v</verify>
  <done>8 tests passing for command execution function</done>
</task>

<task type="auto">
  <name>Test helper functions (get_device_info, list_devices, execute_device_command)</name>
  <files>backend/tests/unit/test_device_tool.py</files>
  <action>
    Create tests for helper functions:

    TestGetDeviceInfo (3 tests):
    - test_get_device_info_success: Mock db.query to return DeviceNode, verify all fields mapped
    - test_get_device_info_not_found: Mock db.query to return None, verify None returned
    - test_get_device_info_fields_present: Verify device_id, name, platform, capabilities, last_seen in response

    TestListDevices (3 tests):
    - test_list_devices_all: Mock db.query to return multiple devices
    - test_list_devices_filtered_by_status: Test status filter (online, offline)
    - test_list_devices_empty: Mock db.query to return empty list

    TestExecuteDeviceCommand (4 tests):
    - test_execute_device_command_camera: Verify routing to device_camera_snap
    - test_execute_device_command_location: Verify routing to device_get_location
    - test_execute_device_command_notification: Verify routing to device_send_notification
    - test_execute_device_command_unknown_type: Verify error for unsupported command_type

    These tests don't require WebSocket patches (they test the routing logic).
  </action>
  <verify>cd backend && python -m pytest tests/unit/test_device_tool.py::TestGetDeviceInfo tests/unit/test_device_tool.py::TestListDevices tests/unit/test_device_tool.py::TestExecuteDeviceCommand -v</verify>
  <done>10 tests passing for helper functions</done>
</task>

<task type="auto">
  <name>Test DeviceSessionManager class</name>
  <files>backend/tests/unit/test_device_tool.py</files>
  <action>
    Create tests for DeviceSessionManager class:

    TestDeviceSessionManager (5 tests):
    - test_create_session: Verify session created with UUID and stored
    - test_get_session: Verify session retrieval by ID
    - test_get_session_not_found: Verify None for invalid session_id
    - test_close_session: Verify session status set to "closed" and removed
    - test_cleanup_expired_sessions: Create old session, verify cleanup removes it

    Test session dict structure:
    - session_id (UUID)
    - user_id
    - device_node_id
    - session_type
    - agent_id
    - configuration
    - status ("active", "closed")
    - created_at, last_used timestamps
  </action>
  <verify>cd backend && python -m pytest tests/unit/test_device_tool.py::TestDeviceSessionManager -v</verify>
  <done>5 tests passing for session manager</done>
</task>

</tasks>

<verification>
After completion:
1. Run: `cd backend && python -m pytest tests/unit/test_device_tool.py -v --cov=tools/device_tool --cov-report=term-missing`
2. Verify 45+ tests total
3. Verify 75%+ coverage for device_tool.py
4. All tests use AsyncMock for WebSocket communication (no real device connections)
</verification>

<success_criteria>
- 45+ tests covering all device functions
- 75%+ line coverage for device_tool.py
- All tests pass with mocked WebSocket communication
- Governance enforcement tested for all maturity levels (STUDENT blocked, INTERN/SUPERVISED/AUTONOMOUS per-action)
</success_criteria>

<output>
After completion, create `.planning/phases/169-tools-integrations-coverage/169-03-SUMMARY.md`
</output>
