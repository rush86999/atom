---
phase: 132-frontend-accessibility-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/package.json
  - frontend-nextjs/tests/setup.ts
  - frontend-nextjs/tests/accessibility-config.ts
autonomous: true

must_haves:
  truths:
    - jest-axe installed and configured in test environment
    - toHaveNoViolations matcher available globally in all tests
    - WCAG 2.1 AA compliance rules configured with appropriate exclusions
    - Accessibility configuration helper module exists for consistent testing
  artifacts:
    - path: frontend-nextjs/package.json
      provides: jest-axe dependency declaration
      contains: '"jest-axe":'
    - path: frontend-nextjs/tests/setup.ts
      provides: Global jest-axe matcher configuration
      exports: ['expect.extend(toHaveNoViolations)']
    - path: frontend-nextjs/tests/accessibility-config.ts
      provides: Configured axe instance for WCAG 2.1 AA testing
      min_lines: 15
  key_links:
    - from: frontend-nextjs/tests/setup.ts
      to: jest-axe
      via: "import { toHaveNoViolations } from 'jest-axe'"
      pattern: "toHaveNoViolations.*jest-axe"
---

<objective>
Configure jest-axe for WCAG 2.1 AA compliance testing in the Atom frontend.

Purpose: Establish the foundation for automated accessibility testing by installing jest-axe, configuring global Jest matchers, and creating a reusable accessibility configuration helper. This enables all subsequent accessibility test plans to use consistent WCAG 2.1 AA validation.

Output: jest-axe installed, global matcher configured, accessibility helper module created
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-frontend-accessibility-compliance/132-RESEARCH.md

# Phase 131 completed - custom hooks tested, test infrastructure established
@frontend-nextjs/tests/setup.ts
@frontend-nextjs/jest.config.js
@frontend-nextjs/package.json
</context>

<tasks>

<task type="auto">
  <name>Install jest-axe and TypeScript types</name>
  <files>frontend-nextjs/package.json</files>
  <action>
    Install jest-axe v10.0.0 and @types/jest-axe as dev dependencies:
    1. cd frontend-nextjs
    2. npm install --save-dev jest-axe @types/jest-axe
    3. Verify installation in package.json devDependencies

    Note: All other dependencies already installed (@testing-library/react@16.3.0, @testing-library/user-event@14.6.1, jest-environment-jsdom@30.0.5)
  </action>
  <verify>grep -E '"jest-axe".*"10\.0\.0"' frontend-nextjs/package.json</verify>
  <done>jest-axe and @types/jest-axe installed in devDependencies</done>
</task>

<task type="auto">
  <name>Configure global jest-axe matcher in test setup</name>
  <files>frontend-nextjs/tests/setup.ts</files>
  <action>
    Add jest-axe global matcher to tests/setup.ts:

    1. Add import after line 19 (after @testing-library/jest-dom import):
       import { toHaveNoViolations } from 'jest-axe';

    2. Add expect.extend call after the import:
       expect.extend(toHaveNoViolations);

    This makes toHaveNoViolations() available globally in all test files.

    Do NOT modify existing mocks or MSW server configuration.
  </action>
  <verify>grep -E "toHaveNoViolations.*jest-axe" frontend-nextjs/tests/setup.ts && grep -E "expect\.extend.*toHaveNoViolations" frontend-nextjs/tests/setup.ts</verify>
  <done>toHaveNoViolations matcher configured globally via expect.extend in setup.ts</done>
</task>

<task type="auto">
  <name>Create accessibility configuration helper module</name>
  <files>frontend-nextjs/tests/accessibility-config.ts</files>
  <action>
    Create frontend-nextjs/tests/accessibility-config.ts with:

    1. Import configureAxe from jest-axe
    2. Export configured axe instance with WCAG 2.1 AA settings:
       - Disable 'region' rule (isolated components don't have landmarks)
       - Set impactLevels to ['critical', 'serious'] (ignore moderate/minor)
       - Use default rules otherwise (full WCAG 2.1 AA compliance)

    Template:
    import { configureAxe } from 'jest-axe';

    const axe = configureAxe({
      rules: {
        'region': { enabled: false } // Disable for isolated component testing
      },
      impactLevels: ['critical', 'serious'] // Only critical/serious violations
    });

    export default axe;

    This provides consistent axe configuration for all accessibility tests.
  </action>
  <verify>test -f frontend-nextjs/tests/accessibility-config.ts && grep -E "configureAxe" frontend-nextjs/tests/accessibility-config.ts</verify>
  <done>Accessibility configuration helper module created with WCAG 2.1 AA settings</done>
</task>

<task type="auto">
  <name>Verify jest-axe configuration with smoke test</name>
  <files>frontend-nextjs/tests/accessibility.test.ts</files>
  <action>
    Create frontend-nextjs/tests/accessibility.test.ts smoke test:

    1. Import: { render } from '@testing-library/react', { axe } from 'jest-axe', { Button } from '@/components/ui/button'
    2. Test: "jest-axe matcher is configured" - renders Button, calls axe(container), expects toHaveNoViolations()
    3. Run: cd frontend-nextjs && npm test -- accessibility.test.ts

    This verifies the entire setup works end-to-end before component testing begins.

    Expected: Test passes with no violations (Button component has accessible name via text content)
  </action>
  <verify>cd frontend-nextjs && npm test -- accessibility.test.ts --silent 2>&1 | head -20</verify>
  <done>Smoke test passes, confirming jest-axe is properly configured and functional</done>
</task>

</tasks>

<verification>
Run the accessibility smoke test and verify:
1. jest-axe matcher is available globally (no import needed in test files)
2. toHaveNoViolations() detects WCAG violations
3. configureAxe options are applied (region rule disabled)
4. Test completes without errors

Command: cd frontend-nextjs && npm test -- accessibility.test.ts
</verification>

<success_criteria>
1. jest-axe v10.0.0 installed in devDependencies
2. toHaveNoViolations matcher configured globally in tests/setup.ts
3. accessibility-config.ts helper module created with WCAG 2.1 AA settings
4. Smoke test passes confirming configuration works
5. No existing tests broken by jest-axe addition
</success_criteria>

<output>
After completion, create `.planning/phases/132-frontend-accessibility-compliance/132-01-SUMMARY.md`
</output>
