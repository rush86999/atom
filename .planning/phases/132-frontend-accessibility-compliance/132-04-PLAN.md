---
phase: 132-frontend-accessibility-compliance
plan: 04
type: execute
wave: 3
depends_on: [132-02, 132-03]
files_modified:
  - frontend-nextjs/components/canvas/__tests__/AgentOperationTracker.a11y.test.tsx
  - frontend-nextjs/components/canvas/__tests__/InteractiveForm.a11y.test.tsx
  - frontend-nextjs/components/canvas/__tests__/ViewOrchestrator.a11y.test.tsx
  - frontend-nextjs/components/canvas/__tests__/BarChart.a11y.test.tsx
  - frontend-nextjs/components/canvas/__tests__/LineChart.a11y.test.tsx
  - frontend-nextjs/components/canvas/__tests__/PieChart.a11y.test.tsx
  - frontend-nextjs/components/canvas/AgentOperationTracker.tsx
  - frontend-nextjs/components/canvas/InteractiveForm.tsx
  - frontend-nextjs/components/canvas/ViewOrchestrator.tsx
  - frontend-nextjs/components/canvas/BarChart.tsx
  - frontend-nextjs/components/canvas/LineChart.tsx
  - frontend-nextjs/components/canvas/PieChart.tsx
autonomous: true

must_haves:
  truths:
    - Canvas components tested for WCAG violations
    - Data visualization accessibility validated (aria-label, aria-describedby)
    - Interactive form elements have proper labels and error states
    - Canvas state API accessibility verified (useCanvasState tested in Phase 131)
    - Screen reader compatibility for canvas content
  artifacts:
    - path: frontend-nextjs/components/canvas/__tests__/AgentOperationTracker.a11y.test.tsx
      provides: Agent operation tracker accessibility tests (aria-live, progress updates)
      min_lines: 35
    - path: frontend-nextjs/components/canvas/__tests__/InteractiveForm.a11y.test.tsx
      provides: Interactive form accessibility tests (labels, error states, aria-invalid)
      min_lines: 50
    - path: frontend-nextjs/components/canvas/__tests__/ViewOrchestrator.a11y.test.tsx
      provides: View orchestrator accessibility tests (aria-live regions, layout shifts)
      min_lines: 40
    - path: frontend-nextjs/components/canvas/__tests__/BarChart.a11y.test.tsx
      provides: Bar chart accessibility tests (aria-label, data table alternative)
      min_lines: 30
    - path: frontend-nextjs/components/canvas/__tests__/LineChart.a11y.test.tsx
      provides: Line chart accessibility tests (aria-label, data description)
      min_lines: 30
    - path: frontend-nextjs/components/canvas/__tests__/PieChart.a11y.test.tsx
      provides: Pie chart accessibility tests (aria-label, segment descriptions)
      min_lines: 30
  key_links:
    - from: AgentOperationTracker.a11y.test.tsx
      to: frontend-nextjs/components/canvas/AgentOperationTracker.tsx
      via: "import { AgentOperationTracker } from '@/components/canvas/AgentOperationTracker'"
      pattern: "AgentOperationTracker.*AgentOperationTracker\.tsx"
    - from: BarChart.a11y.test.tsx
      to: frontend-nextjs/components/canvas/BarChart.tsx
      via: "import { BarChart } from '@/components/canvas/BarChart'"
      pattern: "BarChart.*BarChart\.tsx"
---

<objective>
Test canvas components for WCAG 2.1 AA accessibility compliance.

Purpose: Validate that canvas presentation components (charts, forms, operation trackers) are accessible to screen reader users. Canvas components often present complex visual information that requires ARIA attributes, aria-live regions for dynamic updates, and text alternatives for data visualizations.

Output: 6 accessibility test files covering canvas components
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-frontend-accessibility-compliance/132-RESEARCH.md

# Plans 01-03 complete - jest-axe configured, core components tested
@.planning/phases/132-frontend-accessibility-compliance/132-01-PLAN.md
@.planning/phases/132-frontend-accessibility-compliance/132-02-PLAN.md
@.planning/phases/132-frontend-accessibility-compliance/132-03-PLAN.md

# Canvas components with Phase 131 test patterns
@frontend-nextjs/components/canvas/AgentOperationTracker.tsx
@frontend-nextjs/components/canvas/InteractiveForm.tsx
@frontend-nextjs/components/canvas/ViewOrchestrator.tsx
@frontend-nextjs/components/canvas/BarChart.tsx
@frontend-nextjs/components/canvas/LineChart.tsx
@frontend-nextjs/components/canvas/PieChart.tsx

# Canvas AI Accessibility (Phase 20)
@frontend-nextjs/hooks/useCanvasState.ts
</context>

<tasks>

<task type="auto">
  <name>Create AgentOperationTracker accessibility tests</name>
  <files>frontend-nextjs/components/canvas/__tests__/AgentOperationTracker.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/canvas/__tests__/AgentOperationTracker.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, userEvent from @testing-library/user-event, { axe } from 'jest-axe', { AgentOperationTracker } from @/components/canvas/AgentOperationTracker

    2. Test suite "AgentOperationTracker Accessibility" with tests:
       - "should have no accessibility violations" - render tracker with operations
       - "should have aria-live for progress updates" - verify aria-live="polite" or "assertive" for dynamic content
       - "should have accessible progress bar" - verify role="progressbar" with aria-valuenow
       - "should announce operation completion" - verify screen reader users get completion status
       - "should have aria-label for operation steps" - each step has descriptive label

    AgentOperationTracker provides real-time operation feedback - aria-live regions are critical for screen reader users to get updates.

    Reference: WAI-ARIA live regions for dynamic content updates.
  </action>
  <verify>cd frontend-nextjs && npm test -- AgentOperationTracker.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>AgentOperationTracker accessibility tests created with aria-live validation</done>
</task>

<task type="auto">
  <name>Create InteractiveForm accessibility tests</name>
  <files>frontend-nextjs/components/canvas/__tests__/InteractiveForm.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/canvas/__tests__/InteractiveForm.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, userEvent from @testing-library/user-event, { axe } from 'jest-axe', { InteractiveForm } from @/components/canvas/InteractiveForm

    2. Test suite "InteractiveForm Accessibility" with tests:
       - "should have no accessibility violations" - render form with multiple fields
       - "should have labels for all inputs" - verify each input has associated label
       - "should show errors with aria-invalid" - verify error states have aria-invalid="true"
       - "should have aria-describedby for error messages" - error messages linked to inputs
       - "should have accessible submit button" - button has descriptive text
       - "should be keyboard navigable" - Tab through all form fields in order

    InteractiveForms are critical for agent-human interaction - accessibility ensures all users can provide input.

    Reference: Form accessibility patterns from RESEARCH.md Example 2.
  </action>
  <verify>cd frontend-nextjs && npm test -- InteractiveForm.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>InteractiveForm accessibility tests created with label and error validation</done>
</task>

<task type="auto">
  <name>Create ViewOrchestrator accessibility tests</name>
  <files>frontend-nextjs/components/canvas/__tests__/ViewOrchestrator.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/canvas/__tests__/ViewOrchestrator.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, userEvent from @testing-library/user-event, { axe } from 'jest-axe', { ViewOrchestrator } from @/components/canvas/ViewOrchestrator

    2. Test suite "ViewOrchestrator Accessibility" with tests:
       - "should have no accessibility violations" - render orchestrator with multiple views
       - "should have aria-live for view changes" - verify aria-live announces view transitions
       - "should have landmark regions" - verify role="region" or landmark for view areas
       - "should handle layout shifts gracefully" - verify focus management during view changes
       - "should have aria-label for view controls" - view switch buttons have descriptive labels

    ViewOrchestrator manages multi-view canvas presentations - screen readers need context about view changes.

    Reference: Landmark regions and live regions for multi-view interfaces.
  </action>
  <verify>cd frontend-nextjs && npm test -- ViewOrchestrator.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>ViewOrchestrator accessibility tests created with landmark and live region validation</done>
</task>

<task type="auto">
  <name>Create BarChart accessibility tests</name>
  <files>frontend-nextjs/components/canvas/__tests__/BarChart.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/canvas/__tests__/BarChart.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, { axe } from 'jest-axe', { BarChart } from @/components/canvas/BarChart

    2. Test suite "BarChart Accessibility" with tests:
       - "should have no accessibility violations" - render chart with data
       - "should have aria-label describing chart" - verify aria-label on chart container
       - "should have aria-describedby for data summary" - chart description linked
       - "should have role='img'" - charts treated as images for screen readers
       - "should provide text alternative" - verify data is available as text (table or summary)

    Data visualizations require text alternatives - either data tables or comprehensive descriptions.

    Note: Recharts charts may need wrapper divs for aria attributes. Document any limitations.
  </action>
  <verify>cd frontend-nextjs && npm test -- BarChart.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>BarChart accessibility tests created with text alternative validation</done>
</task>

<task type="auto">
  <name>Create LineChart accessibility tests</name>
  <files>frontend-nextjs/components/canvas/__tests__/LineChart.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/canvas/__tests__/LineChart.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, { axe } from 'jest-axe', { LineChart } from @/components/canvas/LineChart

    2. Test suite "LineChart Accessibility" with tests:
       - "should have no accessibility violations" - render chart with time series data
       - "should have aria-label describing trend" - verify aria-label describes line chart
       - "should have aria-describedby for data summary" - trend description linked
       - "should have role='img'" - charts treated as images
       - "should provide text alternative" - verify data points accessible as text

    Line charts show trends over time - descriptions should convey the trend direction and key points.

    Reference: Chart accessibility guidelines from WebAIM.
  </action>
  <verify>cd frontend-nextjs && npm test -- LineChart.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>LineChart accessibility tests created with trend description validation</done>
</task>

<task type="auto">
  <name>Create PieChart accessibility tests</name>
  <files>frontend-nextjs/components/canvas/__tests__/PieChart.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/canvas/__tests__/PieChart.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, { axe } from 'jest-axe', { PieChart } from @/components/canvas/PieChart

    2. Test suite "PieChart Accessibility" with tests:
       - "should have no accessibility violations" - render chart with segmented data
       - "should have aria-label describing distribution" - verify aria-label describes pie chart
       - "should have aria-describedby for legend" - legend description linked
       - "should have role='img'" - charts treated as images
       - "should provide text alternative" - verify segments and percentages available as text

    Pie charts show part-to-whole relationships - descriptions should convey proportions and largest/smallest segments.

    Note: Color alone should not convey information - verify segments have labels or patterns.
  </action>
  <verify>cd frontend-nextjs && npm test -- PieChart.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>PieChart accessibility tests created with segment description validation</done>
</task>

</tasks>

<verification>
Run all canvas component accessibility tests and verify:
1. All tests pass (no WCAG violations)
2. Data visualizations have aria-label and text alternatives
3. aria-live regions present for dynamic content (AgentOperationTracker, ViewOrchestrator)
4. Forms have labels, error states with aria-invalid, and aria-describedby
5. Landmark regions used for multi-view layouts

Command: cd frontend-nextjs && npm test -- --testPathPattern="canvas.*\.a11y\.test\.tsx$" --silent
</verification>

<success_criteria>
1. 6 accessibility test files created for canvas components
2. All tests pass with 100% success rate
3. Data visualizations have text alternatives
4. aria-live regions verified for dynamic content
5. Form accessibility validated (labels, errors, descriptions)
</success_criteria>

<output>
After completion, create `.planning/phases/132-frontend-accessibility-compliance/132-04-SUMMARY.md`
</output>
