---
phase: 132-frontend-accessibility-compliance
plan: 05
type: execute
wave: 4
depends_on: [132-04]
files_modified:
  - frontend-nextjs/.github/workflows/frontend-accessibility.yml
  - frontend-nextjs/ACCESSIBILITY.md
  - frontend-nextjs/jest.config.js
  - .planning/ROADMAP.md
autonomous: true

must_haves:
  truths:
    - CI/CD workflow runs accessibility tests on every PR
    - Accessibility violations block merge with specific remediation guidance
    - Accessibility documentation covers testing patterns and known limitations
    - Jest configuration includes accessibility test patterns
    - ROADMAP.md updated with Phase 132 plans and completion status
  artifacts:
    - path: frontend-nextjs/.github/workflows/frontend-accessibility.yml
      provides: CI/CD workflow for accessibility testing
      min_lines: 50
    - path: frontend-nextjs/ACCESSIBILITY.md
      provides: Developer documentation for accessibility testing
      min_lines: 200
  key_links:
    - from: frontend-accessibility.yml
      to: frontend-nextjs/npm run test:ci
      via: "npm run test:ci -- --testPathPattern='\\.a11y\\.'"
      pattern: "test:ci.*a11y"
    - from: ACCESSIBILITY.md
      to: frontend-nextjs/tests/accessibility-config.ts
      via: "import axe from '@/tests/accessibility-config'"
      pattern: "accessibility-config"
---

<objective>
Integrate accessibility testing into CI/CD pipeline and create developer documentation.

Purpose: Ensure accessibility violations are caught in CI before merging to main branch. Create GitHub Actions workflow that runs accessibility tests on every PR, blocks merges on violations, and provides specific remediation guidance. Document accessibility testing patterns for developers to follow.

Output: CI/CD workflow, accessibility documentation, updated ROADMAP.md
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-frontend-accessibility-compliance/132-RESEARCH.md

# Plans 01-04 complete - jest-axe configured, all components tested
@.planning/phases/132-frontend-accessibility-compliance/132-01-PLAN.md
@.planning/phases/132-frontend-accessibility-compliance/132-02-PLAN.md
@.planning/phases/132-frontend-accessibility-compliance/132-03-PLAN.md
@.planning/phases/132-frontend-accessibility-compliance/132-04-PLAN.md

# Existing frontend CI/CD workflows
@.github/workflows/frontend-tests.yml
@.github/workflows/frontend-module-coverage.yml
</context>

<tasks>

<task type="auto">
  <name>Create accessibility testing CI/CD workflow</name>
  <files>frontend-nextjs/.github/workflows/frontend-accessibility.yml</files>
  <action>
    Create .github/workflows/frontend-accessibility.yml with:

    1. Trigger: push to main/develop, pull_request to main/develop, workflow_dispatch

    2. Job: accessibility (runs-on: ubuntu-latest, timeout-minutes: 10)
       - Checkout code (actions/checkout@v4)
       - Setup Node.js 20 with npm cache
       - Install dependencies (npm ci --legacy-peer-deps)
       - Clear Jest cache (npx jest --clearCache)
       - Run accessibility tests: npm run test:ci -- --testPathPattern="\.a11y\.test\.tsx$" --maxWorkers=2
       - Upload test results artifact on failure

    3. Optional: Comment PR with accessibility violations (use actions/github-script@v7)
       - Parse jest output for WCAG violations
       - Create formatted comment with:
         * Total violations found
         * Critical/serious violations by component
         * Specific remediation steps with WCAG links
         * Affected components list

    4. Fail the workflow if any accessibility violations found

    Reference: Pattern 6 from RESEARCH.md for CI/CD integration.
  </action>
  <verify>test -f .github/workflows/frontend-accessibility.yml && grep -E "a11y.*test" .github/workflows/frontend-accessibility.yml</verify>
  <done>Accessibility CI/CD workflow created with PR violation reporting</done>
</task>

<task type="auto">
  <name>Create accessibility testing documentation</name>
  <files>frontend-nextjs/ACCESSIBILITY.md</files>
  <action>
    Create frontend-nextjs/ACCESSIBILITY.md with:

    1. Overview: Accessibility testing with jest-axe for WCAG 2.1 AA compliance

    2. Setup:
       - jest-axe installed and configured
       - Global toHaveNoViolations() matcher
       - accessibility-config.ts helper module

    3. Testing Patterns (with code examples):
       - Basic accessibility test (axe() with toHaveNoViolations())
       - Keyboard navigation testing (userEvent.tab(), userEvent.keyboard())
       - ARIA attributes testing (aria-label, aria-expanded, aria-invalid)
       - React Portal testing (use baseElement instead of container)
       - Focus management testing (focus traps, focus restoration)
       - Dynamic content testing (aria-live regions)

    4. Common Pitfalls:
       - Color contrast not tested in JSDOM (use manual testing)
       - Isolated components need 'region' rule disabled
       - React Portals require baseElement for axe()
       - Icon-only buttons require aria-label

    5. CI/CD Integration:
       - Workflow runs on all PRs
       - Violations block merge
       - PR comments with remediation guidance

    6. Manual Testing Checklist:
       - Screen reader testing (NVDA, JAWS, VoiceOver)
       - Keyboard-only navigation
       - Color contrast verification (WebAIM Contrast Checker)
       - Focus visible indicators

    Reference: All patterns from RESEARCH.md with code examples.
  </action>
  <verify>test -f frontend-nextjs/ACCESSIBILITY.md && wc -l frontend-nextjs/ACCESSIBILITY.md | awk '{print $1}' | xargs -I {} test {} -gt 150</verify>
  <done>Accessibility documentation created with testing patterns and manual checklist</done>
</task>

<task type="auto">
  <name>Update Jest configuration for accessibility tests</name>
  <files>frontend-nextjs/jest.config.js</files>
  <action>
    Update frontend-nextjs/jest.config.js to include accessibility test pattern:

    1. In testMatch array, add pattern for accessibility tests:
       "<rootDir>/**/*\.a11y\.(test|spec)\.(ts|tsx)"

    This ensures Jest finds all .a11y.test.tsx files.

    Note: Current testMatch already uses wildcards, so *.test.tsx should already match *.a11y.test.tsx. Verify this is working or add explicit pattern if needed.

    Do NOT modify coverage thresholds or other settings.
  </action>
  <verify>grep -E "a11y|accessibility" frontend-nextjs/jest.config.js || grep -E "test.*test\.tsx" frontend-nextjs/jest.config.js</verify>
  <done>Jest configuration includes accessibility test pattern matching</done>
</task>

<task type="auto">
  <name>Update ROADMAP.md with Phase 132 completion</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Update .planning/ROADMAP.md for Phase 132:

    1. In Phase 132 section:
       - Change status from "Not started" to "Complete" with date
       - Update "**Plans:** 5/5 complete"
       - Mark all plans complete: [x] 132-01 through 132-05
       - Add completion notes with test count and pass rate

    2. In Progress table:
       - Update Phase 132 row: "5/5 | Complete | 2026-03-03"

    3. Add success metrics:
       - jest-axe configured for WCAG 2.1 AA
       - 17 accessibility test files created (6 core + 5 compound + 6 canvas)
       - CI/CD integration operational
       - Accessibility documentation complete

    4. Note any violations found and fixed during testing

    Follow ROADMAP.md formatting conventions (check Phase 131 entry as reference).
  </action>
  <verify>grep -E "Phase 132.*Complete" .planning/ROADMAP.md && grep -E "132-01.*x" .planning/ROADMAP.md</verify>
  <done>ROADMAP.md updated with Phase 132 completion status and metrics</done>
</task>

<task type="auto">
  <name>Run full accessibility test suite for verification</name>
  <files>frontend-nextjs/tests/</files>
  <action>
    Run complete accessibility test suite to verify all tests pass:

    1. cd frontend-nextjs
    2. npm run test:ci -- --testPathPattern="\.a11y\.test\.tsx$" --silent --verbose

    This runs all 17 accessibility test files and verifies:
    - No WCAG violations in tested components
    - All keyboard navigation tests pass
    - All ARIA attribute tests pass
    - All focus management tests pass

    Collect metrics:
    - Total tests run
    - Pass rate
    - Execution time
    - Any violations found (should be 0 after fixes)

    If any tests fail, investigate and fix before completing this task.

    Report metrics in SUMMARY.md.
  </action>
  <verify>cd frontend-nextjs && npm test -- --testPathPattern="\.a11y\.test\.tsx$" --silent 2>&1 | tail -20</verify>
  <done>Full accessibility test suite passes with 100% success rate, zero violations</done>
</task>

</tasks>

<verification>
Run the accessibility CI/CD workflow locally and verify:
1. All accessibility tests pass (npm run test:ci with a11y pattern)
2. CI/CD workflow file is valid YAML
3. Accessibility documentation is comprehensive (>150 lines)
4. ROADMAP.md updated with Phase 132 completion
5. Test metrics collected (test count, pass rate, execution time)

Command: cd frontend-nextjs && npm run test:ci -- --testPathPattern="\.a11y\.test\.tsx$"
</verification>

<success_criteria>
1. CI/CD workflow created and valid
2. Accessibility documentation complete with patterns and examples
3. Jest configuration includes accessibility test patterns
4. ROADMAP.md updated with Phase 132 completion
5. Full accessibility test suite passes with 100% success rate
6. Zero WCAG violations in production components
</success_criteria>

<output>
After completion, create `.planning/phases/132-frontend-accessibility-compliance/132-05-SUMMARY.md`
</output>
