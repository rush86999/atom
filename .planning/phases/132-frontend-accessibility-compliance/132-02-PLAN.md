---
phase: 132-frontend-accessibility-compliance
plan: 02
type: execute
wave: 2
depends_on: [132-01]
files_modified:
  - frontend-nextjs/components/__tests__/Button.a11y.test.tsx
  - frontend-nextjs/components/__tests__/Input.a11y.test.tsx
  - frontend-nextjs/components/__tests__/Select.a11y.test.tsx
  - frontend-nextjs/components/__tests__/Dialog.a11y.test.tsx
  - frontend-nextjs/components/__tests__/Checkbox.a11y.test.tsx
  - frontend-nextjs/components/__tests__/Switch.a11y.test.tsx
  - frontend-nextjs/components/ui/button.tsx
  - frontend-nextjs/components/ui/input.tsx
  - frontend-nextjs/components/ui/select.tsx
  - frontend-nextjs/components/ui/dialog.tsx
  - frontend-nextjs/components/ui/checkbox.tsx
  - frontend-nextjs/components/ui/switch.tsx
autonomous: true

must_haves:
  truths:
    - Core interactive components tested for WCAG violations
    - Keyboard navigation verified for all interactive elements
    - ARIA attributes validated for screen reader compatibility
    - Focus management tested (focus traps, focus restoration)
    - Icon-only buttons require aria-label
  artifacts:
    - path: frontend-nextjs/components/__tests__/Button.a11y.test.tsx
      provides: Button accessibility tests (keyboard, ARIA, violations)
      min_lines: 40
    - path: frontend-nextjs/components/__tests__/Dialog.a11y.test.tsx
      provides: Dialog accessibility tests (focus trap, Escape, aria-hidden)
      min_lines: 60
    - path: frontend-nextjs/components/__tests__/Input.a11y.test.tsx
      provides: Input accessibility tests (labels, aria-invalid, aria-describedby)
      min_lines: 35
    - path: frontend-nextjs/components/__tests__/Select.a11y.test.tsx
      provides: Select accessibility tests (aria-expanded, keyboard)
      min_lines: 40
    - path: frontend-nextjs/components/__tests__/Checkbox.a11y.test.tsx
      provides: Checkbox accessibility tests (labels, keyboard)
      min_lines: 30
    - path: frontend-nextjs/components/__tests__/Switch.a11y.test.tsx
      provides: Switch accessibility tests (aria-checked, keyboard)
      min_lines: 30
  key_links:
    - from: Button.a11y.test.tsx
      to: frontend-nextjs/components/ui/button.tsx
      via: "import { Button } from '@/components/ui/button'"
      pattern: "Button.*button\.tsx"
    - from: Dialog.a11y.test.tsx
      to: frontend-nextjs/components/ui/dialog.tsx
      via: "import { Dialog } from '@/components/ui/dialog'"
      pattern: "Dialog.*dialog\.tsx"
---

<objective>
Test core UI components for WCAG 2.1 AA accessibility compliance.

Purpose: Validate that the most frequently used interactive components (Button, Dialog, Input, Select, Checkbox, Switch) have no accessibility violations, proper keyboard navigation, and correct ARIA attributes. These components form the foundation of the Atom UI and are used across all features.

Output: 6 accessibility test files covering core interactive components with WCAG validation
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-frontend-accessibility-compliance/132-RESEARCH.md

# Plan 01 complete - jest-axe configured
@.planning/phases/132-frontend-accessibility-compliance/132-01-PLAN.md
@frontend-nextjs/tests/accessibility-config.ts
@frontend-nextjs/tests/setup.ts

# Existing test patterns
@frontend-nextjs/components/__tests__/Button.test.tsx
@frontend-nextjs/components/ui/button.tsx
@frontend-nextjs/components/ui/dialog.tsx
@frontend-nextjs/components/ui/input.tsx
</context>

<tasks>

<task type="auto">
  <name>Create Button accessibility tests</name>
  <files>frontend-nextjs/components/__tests__/Button.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/__tests__/Button.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, userEvent from @testing-library/user-event, { axe } from 'jest-axe', { Button } from @/components/ui/button

    2. Test suite "Button Accessibility" with tests:
       - "should have no accessibility violations" - render Button, await axe(container), expect toHaveNoViolations()
       - "should have accessible name for icon-only button" - render Button with aria-label, verify no violations
       - "should be keyboard accessible" - user.tab() to focus, user.keyboard('{Enter}') and user.keyboard(' ') to activate
       - "should not be keyboard accessible when disabled" - verify toBeDisabled()
       - "should have visible focus indicator" - Button already has focus-visible:ring-2 classes

    Use userEvent.setup() for keyboard tests. Use container for axe() calls.

    Reference: Pattern 4 from RESEARCH.md for keyboard testing.
  </action>
  <verify>cd frontend-nextjs && npm test -- Button.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>Button accessibility tests created with WCAG and keyboard validation</done>
</task>

<task type="auto">
  <name>Create Input accessibility tests</name>
  <files>frontend-nextjs/components/__tests__/Input.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/__tests__/Input.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, userEvent from @testing-library/user-event, { axe } from 'jest-axe', { Input } from @/components/ui/input

    2. Test suite "Input Accessibility" with tests:
       - "should have no accessibility violations" - render Input with placeholder
       - "should have accessible label" - render with label htmlFor linking to input id
       - "should have aria-label when visible label is absent" - aria-label required for standalone inputs
       - "should show error state with aria-invalid" - verify aria-invalid and aria-describedby attributes
       - "should be keyboard accessible" - user.tab() to focus, user.keyboard() to type

    Input components require either: visible label, aria-label, or aria-labelledby for WCAG compliance.

    Reference: Example 2 from RESEARCH.md.
  </action>
  <verify>cd frontend-nextjs && npm test -- Input.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>Input accessibility tests created with label and ARIA validation</done>
</task>

<task type="auto">
  <name>Create Dialog accessibility tests</name>
  <files>frontend-nextjs/components/__tests__/Dialog.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/__tests__/Dialog.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, userEvent from @testing-library/user-event, { axe } from 'jest-axe', { Dialog } from @/components/ui/dialog

    2. Test suite "Dialog Accessibility" with tests:
       - "should have no accessibility violations when open" - use baseElement for React Portal testing
       - "should trap focus within dialog" - verify Tab cycles through dialog elements only
       - "should close on Escape key" - user.keyboard('{Escape}') verifies onClose callback
       - "should have aria-hidden when closed" - queryByRole returns null when closed
       - "should return focus to trigger after close" - verify focus restoration after close

    IMPORTANT: Use baseElement instead of container for Dialog tests because Radix UI renders via React Portal to document.body.

    Reference: Pattern 5 and Example 4 from RESEARCH.md.
  </action>
  <verify>cd frontend-nextjs && npm test -- Dialog.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>Dialog accessibility tests created with focus trap and Escape key validation</done>
</task>

<task type="auto">
  <name>Create Select accessibility tests</name>
  <files>frontend-nextjs/components/__tests__/Select.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/__tests__/Select.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, userEvent from @testing-library/user-event, { axe } from 'jest-axe', { Select } from @/components/ui/select

    2. Test suite "Select Accessibility" with tests:
       - "should have no accessibility violations when closed" - render closed Select
       - "should have aria-expanded when closed" - expect aria-expanded="false"
       - "should have aria-expanded when open" - expect aria-expanded="true"
       - "should open with Enter key" - user.keyboard('{Enter}') to open dropdown
       - "should close with Escape key" - user.keyboard('{Escape}') to close

    Select components use role="combobox" and require aria-expanded attribute for screen readers.

    Reference: Example 3 from RESEARCH.md.
  </action>
  <verify>cd frontend-nextjs && npm test -- Select.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>Select accessibility tests created with aria-expanded and keyboard validation</done>
</task>

<task type="auto">
  <name>Create Checkbox accessibility tests</name>
  <files>frontend-nextjs/components/__tests__/Checkbox.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/__tests__/Checkbox.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, userEvent from @testing-library/user-event, { axe } from 'jest-axe', { Checkbox } from @/components/ui/checkbox

    2. Test suite "Checkbox Accessibility" with tests:
       - "should have no accessibility violations" - render Checkbox with label
       - "should have accessible label" - Checkbox must have associated text (implicit or explicit label)
       - "should be toggleable with Space key" - user.keyboard(' ') toggles checkbox
       - "should have aria-checked attribute" - verify aria-checked="true/false" state

    Radix UI Checkbox handles aria-checked automatically. Test verifies the implementation.

    Reference: Radix UI accessibility documentation for Checkbox component.
  </action>
  <verify>cd frontend-nextjs && npm test -- Checkbox.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>Checkbox accessibility tests created with label and keyboard validation</done>
</task>

<task type="auto">
  <name>Create Switch accessibility tests</name>
  <files>frontend-nextjs/components/__tests__/Switch.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/__tests__/Switch.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, userEvent from @testing-library/user-event, { axe } from 'jest-axe', { Switch } from @/components/ui/switch

    2. Test suite "Switch Accessibility" with tests:
       - "should have no accessibility violations" - render Switch with label
       - "should have accessible label" - Switch requires visible label for screen readers
       - "should be toggleable with Space key" - user.keyboard(' ') toggles switch
       - "should have aria-checked attribute" - verify aria-checked state changes

    Switch components use role="switch" and require aria-checked for accessibility state.

    Note: Switch is a specialized checkbox - same keyboard interaction (Space to toggle).
  </action>
  <verify>cd frontend-nextjs && npm test -- Switch.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>Switch accessibility tests created with aria-checked and keyboard validation</done>
</task>

</tasks>

<verification>
Run all core component accessibility tests and verify:
1. All tests pass (no WCAG violations in tested components)
2. Keyboard navigation tests work (Tab, Enter, Space, Escape)
3. ARIA attributes are present and correct
4. Focus management is validated (focus traps, focus restoration)
5. Test coverage includes: axe() validation, keyboard interaction, ARIA attributes

Command: cd frontend-nextjs && npm test -- --testPathPattern="\.a11y\.test\.tsx$" --silent
</verification>

<success_criteria>
1. 6 accessibility test files created (Button, Input, Dialog, Select, Checkbox, Switch)
2. All tests pass with 100% success rate
3. WCAG 2.1 AA violations detected and fixed (if any)
4. Keyboard navigation validated for all interactive components
5. ARIA attributes verified for screen reader compatibility
</success_criteria>

<output>
After completion, create `.planning/phases/132-frontend-accessibility-compliance/132-02-SUMMARY.md`
</output>
