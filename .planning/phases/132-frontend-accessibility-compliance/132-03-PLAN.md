---
phase: 132-frontend-accessibility-compliance
plan: 03
type: execute
wave: 2
depends_on: [132-01]
files_modified:
  - frontend-nextjs/components/__tests__/Tabs.a11y.test.tsx
  - frontend-nextjs/components/__tests__/Accordion.a11y.test.tsx
  - frontend-nextjs/components/__tests__/Tooltip.a11y.test.tsx
  - frontend-nextjs/components/__tests__/Popover.a11y.test.tsx
  - frontend-nextjs/components/__tests__/Dropdown.a11y.test.tsx
  - frontend-nextjs/components/__tests__/AlertDialog.a11y.test.tsx
  - frontend-nextjs/components/ui/tabs.tsx
  - frontend-nextjs/components/ui/accordion.tsx
  - frontend-nextjs/components/ui/tooltip.tsx
  - frontend-nextjs/components/ui/popover.tsx
  - frontend-nextjs/components/ui/dropdown-menu.tsx
autonomous: true

must_haves:
  truths:
    - Compound and interactive components tested for WCAG violations
    - Keyboard navigation verified (Arrow keys, Enter, Escape, Home/End)
    - ARIA attributes validated (aria-selected, aria-expanded, aria-controls)
    - Focus management tested for complex interactions
    - Screen reader compatibility verified
  artifacts:
    - path: frontend-nextjs/components/__tests__/Tabs.a11y.test.tsx
      provides: Tabs accessibility tests (aria-selected, Arrow navigation)
      min_lines: 40
    - path: frontend-nextjs/components/__tests__/Accordion.a11y.test.tsx
      provides: Accordion accessibility tests (aria-expanded, keyboard)
      min_lines: 35
    - path: frontend-nextjs/components/__tests__/Tooltip.a11y.test.tsx
      provides: Tooltip accessibility tests (aria-describedby, keyboard)
      min_lines: 30
    - path: frontend-nextjs/components/__tests__/Popover.a11y.test.tsx
      provides: Popover accessibility tests (aria-hidden, Escape, focus)
      min_lines: 35
    - path: frontend-nextjs/components/__tests__/Dropdown.a11y.test.tsx
      provides: Dropdown accessibility tests (aria-expanded, keyboard navigation)
      min_lines: 45
  key_links:
    - from: Tabs.a11y.test.tsx
      to: frontend-nextjs/components/ui/tabs.tsx
      via: "import { Tabs } from '@/components/ui/tabs'"
      pattern: "Tabs.*tabs\.tsx"
    - from: Accordion.a11y.test.tsx
      to: frontend-nextjs/components/ui/accordion.tsx
      via: "import { Accordion } from '@/components/ui/accordion'"
      pattern: "Accordion.*accordion\.tsx"
---

<objective>
Test compound and interactive UI components for WCAG 2.1 AA accessibility compliance.

Purpose: Validate that complex interactive components (Tabs, Accordion, Tooltip, Popover, Dropdown, AlertDialog) have proper ARIA attributes, keyboard navigation (including Arrow keys for lists), and focus management. These components involve compound widget patterns that require specific ARIA roles and state management.

Output: 5 accessibility test files covering compound interactive components
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-frontend-accessibility-compliance/132-RESEARCH.md

# Plan 01 complete - jest-axe configured
@.planning/phases/132-frontend-accessibility-compliance/132-01-PLAN.md
@frontend-nextjs/tests/accessibility-config.ts

# Radix UI components with built-in accessibility
@frontend-nextjs/components/ui/tabs.tsx
@frontend-nextjs/components/ui/accordion.tsx
@frontend-nextjs/components/ui/tooltip.tsx
@frontend-nextjs/components/ui/popover.tsx
@frontend-nextjs/components/ui/dropdown-menu.tsx
</context>

<tasks>

<task type="auto">
  <name>Create Tabs accessibility tests</name>
  <files>frontend-nextjs/components/__tests__/Tabs.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/__tests__/Tabs.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, userEvent from @testing-library/user-event, { axe } from 'jest-axe', { Tabs } from @/components/ui/tabs

    2. Test suite "Tabs Accessibility" with tests:
       - "should have no accessibility violations" - render Tabs with multiple TabPanels
       - "should have aria-selected on active tab" - verify aria-selected="true" on active tab
       - "should navigate with Arrow keys" - user.keyboard('{ArrowRight}') and '{ArrowLeft}' for tab switching
       - "should navigate with Home/End keys" - user.keyboard('{Home}') for first tab, '{End}' for last
       - "should have aria-controls linking to panel" - verify aria-controls attribute

    Tabs use role="tablist" for container, role="tab" for tabs, and aria-selected for state. Radix UI handles this automatically - tests verify implementation.

    Reference: WAI-ARIA Authoring Practices 1.2 for Tabs pattern.
  </action>
  <verify>cd frontend-nextjs && npm test -- Tabs.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>Tabs accessibility tests created with Arrow key and ARIA validation</done>
</task>

<task type="auto">
  <name>Create Accordion accessibility tests</name>
  <files>frontend-nextjs/components/__tests__/Accordion.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/__tests__/Accordion.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, userEvent from @testing-library/user-event, { axe } from 'jest-axe', { Accordion } from @/components/ui/accordion

    2. Test suite "Accordion Accessibility" with tests:
       - "should have no accessibility violations" - render Accordion with multiple items
       - "should have aria-expanded on trigger" - verify aria-expanded changes on toggle
       - "should toggle with Enter key" - user.keyboard('{Enter}') to expand/collapse
       - "should toggle with Space key" - user.keyboard(' ') to expand/collapse
       - "should have aria-controls linking to panel" - verify aria-controls attribute

    Accordion uses role="region" for panels and aria-expanded for open/closed state.

    Reference: WAI-ARIA Authoring Practices 1.2 for Accordion pattern (Disclosures).
  </action>
  <verify>cd frontend-nextjs && npm test -- Accordion.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>Accordion accessibility tests created with aria-expanded and keyboard validation</done>
</task>

<task type="auto">
  <name>Create Tooltip accessibility tests</name>
  <files>frontend-nextjs/components/__tests__/Tooltip.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/__tests__/Tooltip.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, userEvent from @testing-library/user-event, { axe } from 'jest-axe', { Tooltip } from @/components/ui/tooltip

    2. Test suite "Tooltip Accessibility" with tests:
       - "should have no accessibility violations" - render Tooltip with trigger
       - "should have aria-describedby on trigger" - verify aria-describedby links to tooltip content
       - "should show on hover" - mouse hover triggers tooltip (may need fireEvent for hover)
       - "should show on focus" - focus on trigger shows tooltip for keyboard users
       - "should hide on Escape key" - user.keyboard('{Escape}') closes tooltip

    Tooltips should be keyboard accessible (focus shows tooltip) and have aria-describedby linking trigger to content.

    Note: Radix UI Tooltip handles aria-describedby automatically.
  </action>
  <verify>cd frontend-nextjs && npm test -- Tooltip.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>Tooltip accessibility tests created with aria-describedby and keyboard validation</done>
</task>

<task type="auto">
  <name>Create Popover accessibility tests</name>
  <files>frontend-nextjs/components/__tests__/Popover.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/__tests__/Popover.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, userEvent from @testing-library/user-event, { axe } from 'jest-axe', { Popover } from @/components/ui/popover

    2. Test suite "Popover Accessibility" with tests:
       - "should have no accessibility violations when open" - use baseElement for Portal
       - "should have aria-hidden when closed" - verify content is hidden from screen readers when closed
       - "should close on Escape key" - user.keyboard('{Escape}') closes popover
       - "should trap focus when open" - Tab stays within popover boundaries
       - "should return focus to trigger after close" - verify focus restoration

    Popover renders via React Portal - use baseElement for axe() calls. Similar to Dialog tests.

    Reference: Dialog pattern from Plan 02 for Portal testing.
  </action>
  <verify>cd frontend-nextjs && npm test -- Popover.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>Popover accessibility tests created with focus trap and Escape key validation</done>
</task>

<task type="auto">
  <name>Create Dropdown accessibility tests</name>
  <files>frontend-nextjs/components/__tests__/Dropdown.a11y.test.tsx</files>
  <action>
    Create frontend-nextjs/components/__tests__/Dropdown.a11y.test.tsx with:

    1. Imports: render, screen from @testing-library/react, userEvent from @testing-library/user-event, { axe } from 'jest-axe', { DropdownMenu } from @/components/ui/dropdown-menu

    2. Test suite "Dropdown Accessibility" with tests:
       - "should have no accessibility violations when open" - render open dropdown
       - "should have aria-expanded attribute" - verify aria-expanded state changes
       - "should open with Enter/Space keys" - user.keyboard('{Enter}') and ' ' to open
       - "should navigate with Arrow keys" - user.keyboard('{ArrowDown}') for menu navigation
       - "should close on Escape key" - user.keyboard('{Escape}') closes menu
       - "should activate item with Enter" - user.keyboard('{Enter}') on menu item

    Dropdown uses role="menu" with aria-expanded for state. Arrow keys navigate menu items.

    Reference: WAI-ARIA Authoring Practices 1.2 for Menu pattern.
  </action>
  <verify>cd frontend-nextjs && npm test -- Dropdown.a11y.test.tsx --silent 2>&1 | head -30</verify>
  <done>Dropdown accessibility tests created with Arrow key navigation and ARIA validation</done>
</task>

</tasks>

<verification>
Run all compound component accessibility tests and verify:
1. All tests pass (no WCAG violations)
2. Arrow key navigation works (Tabs, Dropdown)
3. ARIA attributes are correct (aria-selected, aria-expanded, aria-controls, aria-describedby)
4. Focus management is validated (focus traps, focus restoration)
5. Escape key closes all open components (Popover, Dropdown, Tooltip)

Command: cd frontend-nextjs && npm test -- --testPathPattern="\.a11y\.test\.tsx$" --silent
</verification>

<success_criteria>
1. 5 accessibility test files created (Tabs, Accordion, Tooltip, Popover, Dropdown)
2. All tests pass with 100% success rate
3. Keyboard navigation validated including Arrow keys and Home/End
4. Compound widget ARIA patterns verified (tablist, menu, disclosure)
5. Focus management tested for all interactive components
</success_criteria>

<output>
After completion, create `.planning/phases/132-frontend-accessibility-compliance/132-03-SUMMARY.md`
</output>
