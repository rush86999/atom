---
phase: 225.1-agent-llmservice-migration
plan: 05
type: execute
wave: 1
depends_on: ["225.1-04"]
files_modified:
  - backend/tests/core/test_agent_execution_service_coverage.py
  - backend/tests/e2e/test_agent_execution_episodic_integration.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Failing agent execution service tests pass"
    - "E2E episodic integration tests pass"
    - "Test fixtures patch LLMService instead of BYOKHandler"
  artifacts:
    - path: "backend/tests/core/test_agent_execution_service_coverage.py"
      provides: "Comprehensive agent execution service tests"
      contains: "patch.*LLMService"
    - path: "backend/tests/e2e/test_agent_execution_episodic_integration.py"
      provides: "E2E episodic integration tests"
      contains: "patch.*LLMService"
  key_links:
    - from: "test_agent_execution_service_coverage.py"
      to: "core/agent_execution_service.py"
      via: "LLMService mock patches"
      pattern: "patch.*agent_execution_service\.LLMService"
---

# Phase 225.1 Plan 05: Fix Failing Agent Execution Tests

**Gap Closure:** Fix test infrastructure issues causing agent execution tests to fail

## Objective

Fix failing tests in the agent execution service test suite. The core migration to LLMService is complete (verified in 225.1-04), but test mocks still patch BYOKHandler instead of LLMService, causing AttributeError during test execution.

**Purpose:** Close Gap 1 from VERIFICATION.md - "Test Mock Updates Not Complete"

**Output:** Passing tests for agent execution service and E2E episodic integration

## Context

@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/225.1-agent-llmservice-migration/225.1-04-SUMMARY.md
@.planning/phases/225.1-agent-llmservice-migration/225.1-VERIFICATION.md

## Tasks

<task type="auto">
  <name>Task 1: Fix test_agent_execution_service_coverage.py mock patches</name>
  <files>backend/tests/core/test_agent_execution_service_coverage.py</files>
  <action>
    Update all BYOKHandler patches to LLMService in test_agent_execution_service_coverage.py:

    1. Change fixture mock_byok_handler (line 76-87) to mock_llm_service:
       - Update patch decorator: @patch('core.agent_execution_service.BYOKHandler') → @patch('core.agent_execution_service.LLMService')
       - Update fixture name: mock_byok_handler → mock_llm_service
       - Update yield variable: handler → llm_service

    2. Update all test function signatures that use mock_byok_handler:
       - Replace parameter: mock_byok_handler → mock_llm_service
       - Find all occurrences: grep -n "mock_byok_handler" in the file

    3. Update mock method return values if needed:
       - stream_completion should return async generator (use _async_stream_tokens helper)
       - analyze_query_complexity should return MagicMock with complexity_score and estimated_tokens
       - get_optimal_provider should return tuple: ("openai", "gpt-4")

    Migration pattern from 225.1-04 SUMMARY:
    - Import path: core.agent_execution_service.LLMService
    - LLMService returns same responses as BYOKHandler for these methods
  </action>
  <verify>
    grep -n "patch.*BYOKHandler" backend/tests/core/test_agent_execution_service_coverage.py | wc -l returns 0
    grep -n "patch.*LLMService" backend/tests/core/test_agent_execution_service_coverage.py | wc -l returns 5+
  </verify>
  <done>
    All BYOKHandler patches replaced with LLMService patches, test fixture renamed, all test functions updated
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix E2E episodic integration test mock patches</name>
  <files>backend/tests/e2e/test_agent_execution_episodic_integration.py</files>
  <action>
    Update BYOKHandler patches to LLMService in test_agent_execution_episodic_integration.py:

    1. Find all BYOKHandler patches:
       - grep -n "BYOKHandler" backend/tests/e2e/test_agent_execution_episodic_integration.py

    2. Update import statements if needed:
       - from core.llm.byok_handler import BYOKHandler → from core.llm_service import LLMService

    3. Update @patch decorators:
       - @patch('core.episode_segmentation_service.BYOKHandler') → @patch('core.episode_segmentation_service.LLMService')
       - @patch('core.llm.byok_handler.BYOKHandler.*') → @patch('core.llm_service.LLMService.*')

    4. Update fixture/method parameter names:
       - byok_handler → llm_service
       - mock_byok → mock_llm

    5. Verify mock return values match LLMService interface:
       - stream_completion returns async generator
       - generate_response → generate (if any)
       - generate_structured_response → generate_structured (if any)

    Note: EpisodeSegmentationService uses LLMService (migrated in 225.1-04), so tests should patch LLMService.
  </action>
  <verify>
    grep -n "BYOKHandler" backend/tests/e2e/test_agent_execution_episodic_integration.py | wc -l returns 0
    grep -n "LLMService" backend/tests/e2e/test_agent_execution_episodic_integration.py | wc -l returns 3+
  </verify>
  <done>
    All BYOKHandler references replaced with LLMService, patches target correct module paths
  </done>
</task>

<task type="auto">
  <name>Task 3: Run failing tests to verify fixes</name>
  <files>backend/tests/core/test_agent_execution_service_coverage.py, backend/tests/e2e/test_agent_execution_episodic_integration.py</files>
  <action>
    Run the previously failing tests to verify fixes:

    1. Run agent execution service coverage tests:
       ```bash
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/core/test_agent_execution_service_coverage.py -v
       ```

    2. Run E2E episodic integration tests:
       ```bash
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/e2e/test_agent_execution_episodic_integration.py -v
       ```

    3. If tests still fail, diagnose:
       - Check for remaining BYOKHandler patches: grep -rn "BYOKHandler" in the test file
       - Check for incorrect import paths (should be core.llm_service, not core.llm.llm_service)
       - Check for method name mismatches (generate_response → generate)

    Expected results:
    - test_agent_execution_service_coverage.py: All tests should pass (10 errors resolved)
    - test_agent_execution_episodic_integration.py: All tests should pass or fail for pre-existing reasons

    Note: Some E2E tests may have pre-existing failures (WebSocketManager test infrastructure issues) - these are documented in VERIFICATION.md and not related to LLM migration.
  </action>
  <verify>
    Test execution shows no AttributeError related to BYOKHandler
    Test execution shows pass/fail counts (0 errors from BYOKHandler patches)
  </verify>
  <done>
    Agent execution service tests pass without BYOKHandler-related errors
    E2E episodic integration tests run without BYOKHandler-related errors
  </done>
</task>

## Verification

1. **No BYOKHandler patches in updated test files**
   ```bash
   grep -rn "patch.*BYOKHandler" backend/tests/core/test_agent_execution_service_coverage.py backend/tests/e2e/test_agent_execution_episodic_integration.py
   ```
   Returns: empty (0 matches)

2. **LLMService patches present in updated test files**
   ```bash
   grep -rn "patch.*LLMService" backend/tests/core/test_agent_execution_service_coverage.py backend/tests/e2e/test_agent_execution_episodic_integration.py
   ```
   Returns: 5+ matches

3. **Tests run without BYOKHandler AttributeError**
   ```bash
   PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/core/test_agent_execution_service_coverage.py -v 2>&1 | grep -i "attributeError.*BYOKHandler"
   ```
   Returns: empty

## Success Criteria

- [ ] test_agent_execution_service_coverage.py has 0 BYOKHandler patches
- [ ] test_agent_execution_episodic_integration.py has 0 BYOKHandler patches
- [ ] Both test files use LLMService patches
- [ ] Agent execution service tests run without AttributeError
- [ ] E2E episodic integration tests run without BYOKHandler-related errors

## Output

After completion, create `.planning/phases/225.1-agent-llmservice-migration/225.1-05-SUMMARY.md`

Summary should include:
- Files modified (test_agent_execution_service_coverage.py, test_agent_execution_episodic_integration.py)
- Number of patches updated
- Test results before/after
- Any remaining issues identified
