---
phase: 225.1-agent-llmservice-migration
plan: 07
type: execute
wave: 1
depends_on: ["225.1-04"]
files_modified:
  - backend/tests/core/agents/test_atom_meta_agent_coverage.py
  - backend/tests/core/agents/test_atom_meta_agent_coverage_extend.py
  - backend/tests/core/agents/test_atom_meta_agent_react_loop.py
  - backend/tests/integration/services/conftest.py
  - backend/tests/api/test_atom_agent_endpoints.py
  - backend/tests/test_api_agent_endpoints.py
  - backend/tests/test_specialty_agents.py
  - backend/tests/test_atom_react.py
  - backend/tests/test_react_loop.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Meta agent tests use LLMService patches"
    - "API endpoint tests use LLMService patches"
    - "Integration test fixtures use LLMService instead of BYOKHandler"
    - "React loop tests use correct LLMService method names"
  artifacts:
    - path: "backend/tests/core/agents/test_atom_meta_agent_coverage.py"
      provides: "Meta agent coverage tests"
      contains: "patch.*LLMService"
    - path: "backend/tests/integration/services/conftest.py"
      provides: "Integration test fixtures"
      contains: "llm_service.*LLMService"
  key_links:
    - from: "test_atom_meta_agent_coverage.py"
      to: "core/atom_meta_agent.py"
      via: "LLMService mock patches"
      pattern: "patch.*atom_meta_agent\.LLMService"
---

# Phase 225.1 Plan 07: Update Meta Agent, API, and Integration Test Infrastructure

**Gap Closure:** Update remaining meta agent, API endpoint, and integration tests to use LLMService patches

## Objective

Update the remaining test infrastructure (meta agent tests, API endpoint tests, integration conftest fixtures) to patch LLMService instead of BYOKHandler. This completes the test infrastructure migration for all agent-related tests.

**Purpose:** Close Gap 1 from VERIFICATION.md - Complete test infrastructure updates for remaining test files

**Output:** All meta agent, API, and integration tests using LLMService patches

## Context

@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/225.1-agent-llmservice-migration/225.1-03-SUMMARY.md
@.planning/phases/225.1-agent-llmservice-migration/225.1-VERIFICATION.md

## Tasks

<task type="auto">
  <name>Task 1: Update meta agent test patches</name>
  <files>
    backend/tests/core/agents/test_atom_meta_agent_coverage.py
    backend/tests/core/agents/test_atom_meta_agent_coverage_extend.py
    backend/tests/core/agents/test_atom_meta_agent_react_loop.py
  </files>
  <action>
    Update BYOKHandler patches to LLMService in meta agent tests:

    1. For test_atom_meta_agent_coverage.py (lines 64, 79, 90, 203, 219):
       - Current: @patch('core.atom_meta_agent.BYOKHandler')
       - Change to: @patch('core.atom_meta_agent.LLMService')
       - Note: atom_meta_agent.py was verified in 225.1-03 as already using LLMService correctly
       - Tests just need to patch the correct import

    2. For test_atom_meta_agent_coverage_extend.py:
       - Find all BYOKHandler patches
       - Update to LLMService patches

    3. For test_atom_meta_agent_react_loop.py:
       - Find all BYOKHandler patches
       - Update to LLMService patches
       - Update method names: generate_response → generate, generate_structured_response → generate_structured

    4. Update variable names:
       - mock_byok → mock_llm
       - byok_handler → llm_service
       - mock_byok_handler → mock_llm_service

    Migration note: atom_meta_agent.py already uses LLMService (verified in 225.1-03)
  </action>
  <verify>
    grep -rn "patch.*BYOKHandler" backend/tests/core/agents/ 2>/dev/null | wc -l returns 0
    grep -rn "patch.*LLMService" backend/tests/core/agents/ 2>/dev/null | wc -l returns 5+
  </verify>
  <done>
    All meta agent tests use LLMService patches
  </done>
</task>

<task type="auto">
  <name>Task 2: Update API endpoint and specialty agent test patches</name>
  <files>
    backend/tests/api/test_atom_agent_endpoints.py
    backend/tests/test_api_agent_endpoints.py
    backend/tests/test_specialty_agents.py
    backend/tests/test_atom_react.py
    backend/tests/test_react_loop.py
  </files>
  <action>
    Update BYOKHandler patches to LLMService in API and agent tests:

    1. For test_atom_agent_endpoints.py:
       - Find all BYOKHandler patches
       - Update to LLMService patches
       - Note: Some endpoint tests may legitimately use BYOKHandler (atom_agent_endpoints.py itself still imports BYOKHandler)
       - Only update patches for agent classes that now use LLMService

    2. For test_api_agent_endpoints.py:
       - Find all BYOKHandler patches
       - Update to LLMService patches where applicable

    3. For test_specialty_agents.py:
       - Find all BYOKHandler patches
       - Update to LLMService patches
       - Check which specialty agents use LLMService vs. BYOKHandler

    4. For test_atom_react.py:
       - Find all BYOKHandler patches
       - Update to LLMService patches

    5. For test_react_loop.py:
       - Already updated in 225.1-03 (verify)
       - If BYOKHandler references remain, update them
       - Verify method names: generate(), generate_structured()

    Special case: API endpoint tests may legitimately patch BYOKHandler for atom_agent_endpoints.py itself
    (which still uses BYOKHandler for specific BYOK requirements). Only update agent class patches.
  </action>
  <verify>
    grep -rn "patch.*BYOKHandler" backend/tests/api/test_atom_agent_endpoints.py backend/tests/test_api_agent_endpoints.py backend/tests/test_specialty_agents.py backend/tests/test_atom_react.py backend/tests/test_react_loop.py 2>/dev/null | grep -v "atom_agent_endpoints" | wc -l returns 0 (excluding atom_agent_endpoints)
  </verify>
  <done>
    All applicable agent tests use LLMService patches
    API endpoint tests for atom_agent_endpoints.py may still patch BYOKHandler (legitimate)
  </done>
</task>

<task type="auto">
  <name>Task 3: Update integration conftest fixtures</name>
  <files>
    backend/tests/integration/services/conftest.py
    backend/tests/test_agent_governance_runtime.py
  </files>
  <action>
    Update integration test fixtures that use BYOKHandler:

    1. For conftest.py:
       - Find BYOKHandler patches in fixture definitions
       - Update to LLMService patches
       - Update fixture return values to match LLMService interface

    2. For test_agent_governance_runtime.py:
       - Lines 12, 45 patch BYOKHandler.__init__
       - Verify if these tests are for governance runtime (may legitimately use BYOKHandler)
       - If tests are for agent execution using LLMService, update patches
       - If tests are for direct BYOKHandler usage, keep BYOKHandler patches

    Governance tests may legitimately patch BYOKHandler for governance-specific LLM interactions.
    Only update patches that mock agent classes now using LLMService.

    Decision: Check if test imports agent classes (GenericAgent, etc.) that use LLMService
    - If yes → patch LLMService
    - If no (direct BYOKHandler usage) → keep BYOKHandler patch
  </action>
  <verify>
    Integration conftest fixtures provide LLMService mocks
    Governance runtime tests patches match the code under test
  </verify>
  <done>
    Integration fixtures use LLMService for migrated agent classes
    Legitimate BYOKHandler patches preserved for direct BYOK usage
  </done>
</task>

<task type="auto">
  <name>Task 4: Run updated tests and verify</name>
  <files>
    backend/tests/core/agents/
    backend/tests/api/
    backend/tests/integration/services/conftest.py
    backend/tests/test_react_loop.py
  </files>
  <action>
    Run the updated test suites to verify fixes:

    1. Run meta agent tests:
       ```bash
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/core/agents/test_atom_meta_agent_coverage.py -v
       ```

    2. Run react loop tests:
       ```bash
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_react_loop.py -v
       ```

    3. Run specialty agents tests:
       ```bash
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_specialty_agents.py -v
       ```

    4. Run all agent-related tests:
       ```bash
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/core/agents/ tests/test_atom_react.py tests/test_react_loop.py -v
       ```

    Expected results:
    - No AttributeError related to BYOKHandler for migrated agent classes
    - React loop tests should pass (updated in 225.1-03, verify still working)
    - Meta agent tests should pass (atom_meta_agent uses LLMService)

    Note: Some pre-existing test failures may exist (documented in VERIFICATION.md)
    Focus on eliminating BYOKHandler-related errors.
  </action>
  <verify>
    Test execution completes without BYOKHandler AttributeError for migrated classes
    Test results show pass/fail counts
  </verify>
  <done>
    Meta agent tests run without BYOKHandler errors
    React loop tests pass
    API endpoint tests run without migration-related errors
  </done>
</task>

## Verification

1. **No inappropriate BYOKHandler patches in agent tests**
   ```bash
   grep -rn "patch.*BYOKHandler" backend/tests/core/agents/ backend/tests/test_react_loop.py 2>/dev/null | wc -l
   ```
   Returns: 0

2. **LLMService patches present**
   ```bash
   grep -rn "patch.*LLMService" backend/tests/core/agents/ backend/tests/test_react_loop.py 2>/dev/null | wc -l
   ```
   Returns: 5+

3. **Tests run successfully**
   ```bash
   PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/core/agents/test_atom_meta_agent_coverage.py tests/test_react_loop.py -v 2>&1 | grep -E "passed|failed|error" | tail -5
   ```
   Returns: Test summary with no BYOKHandler AttributeError

## Success Criteria

- [ ] Meta agent tests use LLMService patches
- [ ] React loop tests use LLMService patches with correct method names
- [ ] Integration conftest provides LLMService fixtures
- [ ] Tests run without BYOKHandler-related AttributeError
- [ ] Legitimate BYOKHandler patches preserved (governance, atom_agent_endpoints)

## Output

After completion, create `.planning/phases/225.1-agent-llmservice-migration/225.1-07-SUMMARY.md`

Summary should include:
- Files modified (9 test files)
- Number of patches updated
- Test results
- Legitimate BYOKHandler patches identified and preserved
