---
phase: 225.1-agent-llmservice-migration
plan: 03
type: execute
wave: 2
depends_on: ["225.1-02"]
files_modified:
  - backend/core/generic_agent.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "generic_agent.py uses LLMService instead of BYOKHandler"
    - "All LLM interactions go through LLMService"
    - "Generic agent tests pass with LLMService integration"
    - "No regression in agent execution or capabilities"
  artifacts:
    - path: "backend/core/generic_agent.py"
      provides: "GenericAgent using LLMService"
      contains: "LLMService"
      min_lines: 550
  key_links:
    - from: "backend/core/generic_agent.py"
      to: "backend/core/llm_service.py"
      via: "self.llm service initialization"
      pattern: "LLMService"
    - from: "backend/core/generic_agent.py"
      to: "backend/core/llm/byok_handler.py"
      via: "No direct BYOKHandler import"
      pattern: "!from core.llm.byok_handler import BYOKHandler"
---

<objective>
Migrate generic_agent.py from BYOKHandler to LLMService wrapper.

Purpose: GenericAgent is the primary agent class. Migrating it to LLMService establishes the unified pattern and validates the migration approach before migrating other services.

Output: generic_agent.py using LLMService instead of direct BYOKHandler import.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

@.planning/phases/225.1-agent-llmservice-migration/225.1-02-SUMMARY.md
@.planning/phases/225-critical-migration-part-3/225-02-SUMMARY.md

@backend/core/generic_agent.py
@backend/core/llm_service.py
@backend/core/llm/byok_handler.py
</execution_context>

<context>
@backend/core/generic_agent.py
@backend/core/llm_service.py
@backend/core/llm/byok_handler.py
@backend/tests/test_generic_agent_coverage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GenericAgent Import Statement</name>
  <files>backend/core/generic_agent.py</files>
  <action>
    Replace the BYOKHandler import with LLMService import in generic_agent.py.

    Current import (line 12):
    ```python
    from core.llm.byok_handler import BYOKHandler
    ```

    Replace with:
    ```python
    from core.llm.llm_service import LLMService
    ```

    Also update the instructor imports section (lines 17-27) to remove the
    AsyncOpenAI import since it's no longer needed (LLMService handles that).

    New instructor section should be:
    ```python
    # Instructor library is used internally by LLMService for structured output
    # No need to import directly here
    instructor = None  # Reserved for future direct use if needed
    ```
  </action>
  <verify>grep -q "from core.llm.llm_service import LLMService" backend/core/generic_agent.py && ! grep -q "from core.llm.byok_handler import BYOKHandler" backend/core/generic_agent.py</verify>
  <done>Import statement changed from BYOKHandler to LLMService</done>
</task>

<task type="auto">
  <name>Task 2: Update GenericAgent LLM Initialization</name>
  <files>backend/core/generic_agent.py</files>
  <action>
    Update the GenericAgent.__init__() method to use LLMService instead of BYOKHandler.

    Current initialization (around line 72):
    ```python
    self.llm = BYOKHandler(workspace_id=workspace_id)
    ```

    Replace with:
    ```python
    self.llm = LLMService(workspace_id=workspace_id)
    ```

    Also update the LLM Integration Note comment (lines 60-71) to reflect the new pattern:

    OLD comment says:
    "Agent classes use BYOKHandler directly (internal layer)..."

    NEW comment should say:
    ```
    # LLM Integration Note:
    # Agent classes now use LLMService as the single source of truth for all LLM interactions.
    # LLMService wraps BYOKHandler and provides a unified interface with:
    # - Structured generation (via instructor integration)
    # - Cognitive tier routing (5-tier system)
    # - Cost tracking and telemetry
    # - BYOK key resolution
    #
    # Architecture layers:
    # - Layer 1 (Bottom): AsyncOpenAI/AsyncAnthropic clients (provider SDKs)
    # - Layer 2 (Middle): BYOKHandler (unified internal interface)
    # - Layer 3 (Top): LLMService (single source of truth for all code)
    # - All code: Uses Layer 3 (LLMService) for unified observability and management
    ```
  </action>
  <verify>grep -q "self.llm = LLMService" backend/core/generic_agent.py && grep -q "single source of truth" backend/core/generic_agent.py</verify>
  <done>GenericAgent initializes LLMService, architecture comment updated</done>
</task>

<task type="auto">
  <name>Task 3: Verify Method Calls Work with LLMService</name>
  <files>backend/core/generic_agent.py</files>
  <action>
    Verify that all existing method calls in generic_agent.py work with LLMService.

    The method calls should remain the same since LLMService exposes compatible methods:
    - Line 222: `self.llm.analyze_query_complexity(task_input)` - Now uses LLMService method
    - Lines 389-397: `self.llm.generate_structured_response()` - Uses LLMService.generate_structured()
    - Lines 406-412: `self.llm.generate_response()` - Uses LLMService.generate()

    Check if any method signatures differ between BYOKHandler and LLMService:
    - analyze_query_complexity: Same signature
    - generate_structured_response: Renamed to generate_structured in LLMService
    - generate_response: Renamed to generate in LLMService

    If method names differ, update the calls:

    1. For `generate_structured_response`, change to `generate_structured`:
       OLD: `await self.llm.generate_structured_response(...)`
       NEW: `await self.llm.generate_structured(...)`

    2. For `generate_response`, change to `generate`:
       OLD: `await self.llm.generate_response(...)`
       NEW: `await self.llm.generate(...)`

    Check parameters match between old and new methods. Update if needed.
  </action>
  <verify>grep -n "self.llm\." backend/core/generic_agent.py | grep -v "^#"  # Show all llm method calls</verify>
  <done>All LLM method calls use correct LLMService method names and parameters</done>
</task>

<task type="auto">
  <name>Task 4: Update Fallback Response Handling</name>
  <files>backend/core/generic_agent.py</files>
  <action>
    Review the fallback response handling in _react_step() method (lines 405-425).

    The current code calls `self.llm.generate_response()` as fallback.
    With LLMService, this is now `self.llm.generate()`.

    Ensure the fallback logic handles any differences in return type:
    - BYOKHandler.generate_response() returns: str
    - LLMService.generate() returns: str

    Return types should be compatible, but verify the error handling still works.

    Also check error handling:
    - Line 415: Checks for "not initialized" in response
    - Line 422: Creates fallback ReActStep with raw_response

    Ensure these work correctly with LLMService.generate() output.
  </action>
  <verify>python3 -c "from core.llm.llm_service import LLMService; import inspect; sig = inspect.signature(LLMService.generate); print('Returns str if not Dict-like:', 'str' in str(sig.return_annotation))"</verify>
  <done>Fallback response handling works with LLMService.generate()</done>
</task>

<task type="auto">
  <name>Task 5: Run Generic Agent Tests</name>
  <files>backend/tests/test_generic_agent_coverage.py</files>
  <action>
    Run the generic agent test suite to verify the migration works correctly.

    Execute tests:
    ```bash
    cd /Users/rushiparikh/projects/atom/backend
    python -m pytest tests/test_generic_agent_coverage.py -v
    ```

    Expected result: All tests pass (53 tests passed as per phase 225-02).

    If any tests fail:
    1. Check if failure is related to LLM migration (method name, signature)
    2. Check if failure is unrelated (existing test issue)
    3. Fix migration-related issues
    4. Document unrelated failures for separate resolution

    Common issues to check:
    - Method name mismatches (generate_structured vs generate_structured_response)
    - Parameter name differences (model_type vs model)
    - Return type differences (str vs Dict[str, Any])
  </action>
  <verify>cd /Users/rushiparikh/projects/atom/backend && python -m pytest tests/test_generic_agent_coverage.py -v --tb=short 2>&1 | tail -20</verify>
  <done>All generic agent tests pass with LLMService integration</done>
</task>

<task type="auto">
  <name>Task 6: Run React Loop Tests</name>
  <files>backend/tests/test_react_loop.py</files>
  <action>
    Run react loop tests which test GenericAgent execution.

    Execute tests:
    ```bash
    cd /Users/rushiparikh/projects/atom/backend
    python -m pytest tests/test_react_loop.py -v
    ```

    These tests verify the ReAct loop works correctly with LLMService.
  </action>
  <verify>cd /Users/rushiparikh/projects/atom/backend && python -m pytest tests/test_react_loop.py -v --tb=short 2>&1 | tail -10</verify>
  <done>React loop tests pass with LLMService integration</done>
</task>

</tasks>

<verification>
- generic_agent.py imports LLMService instead of BYOKHandler
- GenericAgent.__init__() creates LLMService instance
- All LLM method calls use correct LLMService method names
- Generic agent tests pass (53/53)
- React loop tests pass
- Architecture comments updated to reflect new pattern
</verification>

<success_criteria>
- GenericAgent uses LLMService as single source of truth
- No regression in agent capabilities
- All tests pass
- Code follows LLMService-first pattern
</success_criteria>

<output>
After completion, create `.planning/phases/225.1-agent-llmservice-migration/225.1-03-SUMMARY.md`
</output>
