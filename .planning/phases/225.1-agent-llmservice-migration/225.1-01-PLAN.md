---
phase: 225.1-agent-llmservice-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/225.1-agent-llmservice-migration/225.1-01-GAP_ANALYSIS.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All BYOKHandler methods used by agent classes are documented"
    - "Gap analysis identifies missing LLMService methods"
    - "Migration strategy is clearly defined"
  artifacts:
    - path: ".planning/phases/225.1-agent-llmservice-migration/225.1-01-GAP_ANALYSIS.md"
      provides: "Gap analysis document"
      min_lines: 50
  key_links:
    - from: "225.1-01-GAP_ANALYSIS.md"
      to: "backend/core/generic_agent.py"
      via: "Document analysis of BYOKHandler usage"
      pattern: "self.llm\\."
---

<objective>
Analyze the gap between BYOKHandler features and LLMService exposed methods.

Purpose: Understand what BYOKHandler methods agent classes use that LLMService doesn't expose, so we can add them to LLMService before migration.

Output: Gap analysis document listing all BYOKHandler methods used by agents and migration recommendations.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

@.planning/phases/225.1-agent-llmservice-migration/225.1-CONTEXT.md
@.planning/phases/225-critical-migration-part-3/225-02-SUMMARY.md
</execution_context>

<context>
@backend/core/generic_agent.py
@backend/core/agent_execution_service.py
@backend/core/episode_segmentation_service.py
@backend/core/llm/canvas_summary_service.py
@backend/core/llm/byok_handler.py
@backend/core/llm_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze GenericAgent BYOKHandler Usage</name>
  <files>backend/core/generic_agent.py</files>
  <action>
    Search for all `self.llm.` method calls in generic_agent.py to identify exactly which BYOKHandler methods the agent class uses:

    1. Search pattern: `self.llm\.` throughout the file
    2. For each match, document:
       - Method name (e.g., `analyze_query_complexity`, `generate_structured_response`, `generate_response`)
       - Line number where used
       - Context/purpose (why agent needs this method)
       - Parameters passed

    Expected findings based on code review:
    - Line 222: `self.llm.analyze_query_complexity(task_input)` - For TRACE Framework metrics
    - Lines 389-397: `self.llm.generate_structured_response()` - For ReAct step parsing with Pydantic validation
    - Lines 406-412: `self.llm.generate_response()` - Fallback for non-structured responses

    Document each usage with its purpose in the gap analysis document.
  </action>
  <verify>grep -n "self.llm\." backend/core/generic_agent.py shows all BYOKHandler method calls</verify>
  <done>All `self.llm.` method calls documented with line numbers and purpose</done>
</task>

<task type="auto">
  <name>Task 2: Analyze Other Services BYOKHandler Usage</name>
  <files>backend/core/agent_execution_service.py,backend/core/episode_segmentation_service.py,backend/core/llm/canvas_summary_service.py</files>
  <action>
    Search for BYOKHandler usage in other services that agents use:

    For each file:
    1. Find how BYOKHandler is instantiated (e.g., `BYOKHandler(workspace_id=...)`)
    2. Find all method calls on the handler instance
    3. Document the methods used and their purpose

    Known usages:
    - agent_execution_service.py (line 170): Creates `BYOKHandler(workspace_id=workspace_id, provider_id="auto")`
    - episode_segmentation_service.py (line 36): Imports BYOKHandler for embedding generation
    - canvas_summary_service.py (line 73-80): Accepts BYOKHandler as constructor parameter

    For each service, document:
    - How it creates/acquires the handler
    - Which methods it calls
    - Whether it uses advanced features (cognitive tiers, structured generation, etc.)
    - Migration complexity (simple/medium/complex)
  </action>
  <verify>grep -rn "BYOKHandler" backend/core/agent_execution_service.py backend/core/episode_segmentation_service.py backend/core/llm/canvas_summary_service.py shows all usage patterns</verify>
  <done>All service BYOKHandler usages documented with migration complexity ratings</done>
</task>

<task type="auto">
  <name>Task 3: Compare with LLMService Exposed Methods</name>
  <files>backend/core/llm_service.py</files>
  <action>
    Compare BYOKHandler methods used by agents against LLMService exposed methods:

    1. List all public methods in LLMService (scan llm_service.py)
    2. Identify which BYOKHandler methods are NOT exposed in LLMService

    LLMService currently exposes (from code review):
    - `generate()` - delegates to `generate_response()`
    - `generate_completion()` - OpenAI-style wrapper
    - `generate_with_tier()` - cognitive tier routing
    - `generate_structured()` - structured output with Pydantic
    - `stream_completion()` - streaming interface
    - `generate_embedding()` - embedding generation
    - `generate_embeddings_batch()` - batch embeddings
    - `get_optimal_provider()` - provider selection
    - `get_ranked_providers()` - provider ranking
    - `get_routing_info()` - routing without API call
    - `classify_tier()` - cognitive tier classification
    - `get_tier_description()` - tier information

    BYOKHandler methods used by agents (from Tasks 1-2):
    - `analyze_query_complexity()` - Check if exposed
    - `generate_structured_response()` - Check if `generate_structured()` is equivalent
    - `generate_response()` - Check if `generate()` is equivalent
    - Any others found in Tasks 1-2

    For each missing method, note:
    - Method name and signature
    - Whether it needs to be added or if existing LLMService method can be used
    - Migration complexity
  </action>
  <verify>grep -E "^\s*(async\s+)?def " backend/core/llm_service.py lists all public methods</verify>
  <done>All BYOKHandler methods cross-referenced with LLMService; gaps clearly identified</done>
</task>

<task type="auto">
  <name>Task 4: Create Gap Analysis Document</name>
  <files>.planning/phases/225.1-agent-llmservice-migration/225.1-01-GAP_ANALYSIS.md</files>
  <action>
    Create a comprehensive gap analysis document with:

    1. **Executive Summary** - Brief overview of findings
    2. **BYOKHandler Methods Used by Agents** - Complete list from Tasks 1-2
    3. **LLMService Exposed Methods** - Complete list from Task 3
    4. **Gap Analysis** - Methods missing from LLMService
    5. **Migration Strategy** - Recommended approach:
       - Methods to add to LLMService
       - Methods that can use existing LLMService alternatives
       - Services that need code changes vs. configuration changes
    6. **Risk Assessment** - Potential issues with migration
    7. **Migration Order** - Recommended sequence (simplest to most complex)

    Format as Markdown with sections, code examples, and tables for clarity.
  </action>
  <verify>test -f .planning/phases/225.1-agent-llmservice-migration/225.1-01-GAP_ANALYSIS.md && grep -q "Executive Summary" .planning/phases/225.1-agent-llmservice-migration/225.1-01-GAP_ANALYSIS.md</verify>
  <done>Gap analysis document exists with all required sections and migration recommendations</done>
</task>

</tasks>

<verification>
- Gap analysis document created at .planning/phases/225.1-agent-llmservice-migration/225.1-01-GAP_ANALYSIS.md
- All BYOKHandler method usages documented
- LLMService gaps identified
- Migration strategy clearly defined
</verification>

<success_criteria>
- Complete inventory of BYOKHandler methods used by agent classes
- Clear mapping of which methods need to be added to LLMService
- Recommended migration order from simplest to most complex
- Risk assessment for each migration step
</success_criteria>

<output>
After completion, create `.planning/phases/225.1-agent-llmservice-migration/225.1-01-SUMMARY.md`
</output>
