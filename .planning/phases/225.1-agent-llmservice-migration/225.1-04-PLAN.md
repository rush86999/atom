---
phase: 225.1-agent-llmservice-migration
plan: 04
type: execute
wave: 3
depends_on: ["225.1-03"]
files_modified:
  - backend/core/agent_execution_service.py
  - backend/core/episode_segmentation_service.py
  - backend/core/llm/canvas_summary_service.py
  - backend/apps/ai_employee/executor.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All agent services use LLMService instead of BYOKHandler"
    - "No direct BYOKHandler imports in agent-related services"
    - "All agent execution tests pass"
    - "No regression in agent execution, episode segmentation, or canvas summaries"
  artifacts:
    - path: "backend/core/agent_execution_service.py"
      provides: "AgentExecutionService using LLMService"
      contains: "LLMService"
    - path: "backend/core/episode_segmentation_service.py"
      provides: "EpisodeSegmentationService using LLMService"
      contains: "LLMService"
    - path: "backend/core/llm/canvas_summary_service.py"
      provides: "CanvasSummaryService accepting LLMService"
      contains: "LLMService"
  key_links:
    - from: "backend/core/agent_execution_service.py"
      to: "backend/core/llm_service.py"
      via: "LLMService import and usage"
      pattern: "LLMService"
    - from: "backend/core/episode_segmentation_service.py"
      to: "backend/core/llm_service.py"
      via: "LLMService import and usage"
      pattern: "LLMService"
    - from: "backend/core/llm/canvas_summary_service.py"
      to: "backend/core/llm_service.py"
      via: "LLMService type annotation"
      pattern: "LLMService"
---

<objective>
Migrate remaining agent services from BYOKHandler to LLMService.

Purpose: Complete the migration by updating all remaining services that use BYOKHandler directly. This establishes LLMService as the single source of truth across the entire agent system.

Output: All agent services using LLMService instead of direct BYOKHandler.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

@.planning/phases/225.1-agent-llmservice-migration/225.1-03-SUMMARY.md
@.planning/phases/225.1-agent-llmservice-migration/225.1-01-GAP_ANALYSIS.md

@backend/core/agent_execution_service.py
@backend/core/episode_segmentation_service.py
@backend/core/llm/canvas_summary_service.py
@backend/apps/ai_employee/executor.py
@backend/core/llm_service.py
</execution_context>

<context>
@backend/core/agent_execution_service.py
@backend/core/episode_segmentation_service.py
@backend/core/llm/canvas_summary_service.py
@backend/apps/ai_employee/executor.py
@backend/core/llm_service.py
@backend/tests/test_agent_execution_service.py
@backend/tests/test_canvas_summary_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate agent_execution_service.py</name>
  <files>backend/core/agent_execution_service.py</files>
  <action>
    Update agent_execution_service.py to use LLMService instead of BYOKHandler.

    1. Update import (line 27):
       OLD: `from core.llm.byok_handler import BYOKHandler, QueryComplexity`
       NEW: `from core.llm.llm_service import LLMService`

    2. Update BYOKHandler initialization (line 170):
       OLD: `byok_handler = BYOKHandler(workspace_id=workspace_id, provider_id="auto")`
       NEW: `llm_service = LLMService(workspace_id=workspace_id)`

    3. Check if BYOKHandler methods are called elsewhere in the file.
       Search for `byok_handler.` usage patterns and replace with `llm_service.`

    4. Note: agent_execution_service.py primarily uses the handler for streaming
       via stream_completion(). LLMService already exposes this method.

    5. Verify all method calls are compatible with LLMService interface.
  </action>
  <verify>grep -q "from core.llm.llm_service import LLMService" backend/core/agent_execution_service.py && ! grep -q "BYOKHandler" backend/core/agent_execution_service.py</verify>
  <done>agent_execution_service.py uses LLMService, no BYOKHandler imports</done>
</task>

<task type="auto">
  <name>Task 2: Migrate episode_segmentation_service.py</name>
  <files>backend/core/episode_segmentation_service.py</files>
  <action>
    Update episode_segmentation_service.py to use LLMService instead of BYOKHandler.

    1. Update import (line 36):
       OLD: `from core.llm.byok_handler import BYOKHandler`
       NEW: `from core.llm.llm_service import LLMService`

    2. Find where BYOKHandler is used in the service.
       The service likely uses BYOKHandler for embedding generation
       or semantic analysis.

    3. Replace BYOKHandler instantiation with LLMService.

    4. Update method calls if needed:
       - If using methods not exposed by LLMService, verify they exist
       - Check analyze_query_complexity usage if present

    5. Note: episode_segmentation_service may use BYOKHandler indirectly
       through CanvasSummaryService, so check dependency chain.
  </action>
  <verify>grep -q "from core.llm.llm_service import LLMService" backend/core/episode_segmentation_service.py && grep -v "BYOKHandler" backend/core/episode_segmentation_service.py | grep -q "LLMService"</verify>
  <done>episode_segmentation_service.py uses LLMService, no direct BYOKHandler usage</done>
</task>

<task type="auto">
  <name>Task 3: Migrate canvas_summary_service.py</name>
  <files>backend/core/llm/canvas_summary_service.py</files>
  <action>
    Update canvas_summary_service.py to accept LLMService instead of BYOKHandler.

    1. The service accepts a BYOKHandler instance via constructor (line 73-80).
       Change type annotation and documentation:

    2. Update __init__ parameter:
       OLD: `def __init__(self, byok_handler: Any):`
       NEW: `def __init__(self, llm_service: Any):`

    3. Update instance variable:
       OLD: `self.byok_handler = byok_handler`
       NEW: `self.llm_service = llm_service`

    4. Update all method calls that use `self.byok_handler`:
       OLD: `self.byok_handler.generate_response(...)`
       NEW: `self.llm_service.generate(...)`

       OLD: `self.byok_handler.generate_structured_response(...)`
       NEW: `self.llm_service.generate_structured(...)`

    5. Note: CanvasSummaryService has a lazy import pattern for BYOKHandler
       (line 25: `BYOKHandler = None`). Update this to `LLMService = None`
       and update the runtime import logic.

    6. Check for any method calls specific to BYOKHandler that may need
       LLMService equivalent.
  </action>
  <verify>grep -q "self.llm_service" backend/core/llm/canvas_summary_service.py && ! grep -q "self.byok_handler" backend/core/llm/canvas_summary_service.py</verify>
  <done>canvas_summary_service.py uses LLMService interface, no BYOKHandler references</done>
</task>

<task type="auto">
  <name>Task 4: Migrate ai_employee/executor.py</name>
  <files>backend/apps/ai_employee/executor.py</files>
  <action>
    Update ai_employee/executor.py to use LLMService instead of BYOKHandler.

    1. Update import (line 6):
       OLD: `from core.llm.byok_handler import BYOKHandler`
       NEW: `from core.llm.llm_service import LLMService`

    2. Find BYOKHandler usage in the file and replace with LLMService.

    3. Update method calls to use LLMService interface.

    4. Verify the migration works correctly.
  </action>
  <verify>grep -q "LLMService" backend/apps/ai_employee/executor.py && ! grep -q "BYOKHandler" backend/apps/ai_employee/executor.py</verify>
  <done>ai_employee/executor.py uses LLMService, no BYOKHandler imports</done>
</task>

<task type="auto">
  <name>Task 5: Search and Update Any Remaining BYOKHandler Imports</name>
  <files>backend/core/**/*.py</files>
  <action>
    Search for any remaining BYOKHandler imports in core services (excluding tests and docs).

    Run grep to find remaining imports:
    ```bash
    grep -rn "from core.llm.byok_handler import BYOKHandler" backend/core --include="*.py" | grep -v test | grep -v __pycache__
    ```

    For each file found:
    1. Determine if it's agent-related service
    2. If yes, update to use LLMService
    3. If no (e.g., API routes), leave as-is (may use BYOKHandler legitimately)

    Common exclusions:
    - Test files (should keep BYOKHandler for unit testing)
    - API routes that have specific BYOK requirements
    - Documentation files

    Focus on:
    - Agent services
    - Execution services
    - LLM-related services
  </action>
  <verify>grep -rn "from core.llm.byok_handler import BYOKHandler" backend/core --include="*.py" | grep -v test | grep -v __pycache__ | grep -v "agent_execution_service\|episode_segmentation\|canvas_summary" | wc -l</verify>
  <done>All agent-related services use LLMService; remaining BYOKHandler imports are legitimate (tests, API routes)</done>
</task>

<task type="auto">
  <name>Task 6: Run Agent Execution Tests</name>
  <files>backend/tests/test_agent_execution_service.py</files>
  <action>
    Run agent execution service tests to verify migration works.

    Execute tests:
    ```bash
    cd /Users/rushiparikh/projects/atom/backend
    python -m pytest tests/test_agent_execution_service.py -v
    ```

    Also run related integration tests:
    ```bash
    python -m pytest tests/integration/test_agent_execution_e2e.py -v
    ```

    Verify all tests pass with LLMService integration.
  </action>
  <verify>cd /Users/rushiparikh/projects/atom/backend && python -m pytest tests/test_agent_execution_service.py -v --tb=short 2>&1 | tail -10</verify>
  <done>Agent execution tests pass with LLMService integration</done>
</task>

<task type="auto">
  <name>Task 7: Run Canvas Summary Tests</name>
  <files>backend/tests/test_canvas_summary_service.py</files>
  <action>
    Run canvas summary service tests to verify migration works.

    Execute tests:
    ```bash
    cd /Users/rushiparikh/projects/atom/backend
    python -m pytest tests/test_canvas_summary_service.py -v
    ```

    Also run canvas summary integration tests:
    ```bash
    python -m pytest tests/test_canvas_summary_integration.py -v
    ```
  </action>
  <verify>cd /Users/rushiparikh/projects/atom/backend && python -m pytest tests/test_canvas_summary_service.py -v --tb=short 2>&1 | tail -10</verify>
  <done>Canvas summary tests pass with LLMService integration</done>
</task>

<task type="auto">
  <name>Task 8: Run End-to-End Agent Tests</name>
  <files>backend/tests/e2e/test_agent_execution_episodic_integration.py</files>
  <action>
    Run end-to-end agent execution tests to verify full workflow works.

    Execute tests:
    ```bash
    cd /Users/rushiparikh/projects/atom/backend
    python -m pytest tests/e2e/test_agent_execution_episodic_integration.py -v
    ```

    Also run specialty agent tests:
    ```bash
    python -m pytest tests/test_specialty_agents.py -v
    ```

    These tests verify the complete agent execution pipeline with episodic memory.
  </action>
  <verify>cd /Users/rushiparikh/projects/atom/backend && python -m pytest tests/e2e/test_agent_execution_episodic_integration.py -v --tb=short 2>&1 | tail -10</verify>
  <done>E2E agent tests pass with LLMService integration</done>
</task>

</tasks>

<verification>
- agent_execution_service.py uses LLMService
- episode_segmentation_service.py uses LLMService
- canvas_summary_service.py uses LLMService
- ai_employee/executor.py uses LLMService
- No remaining BYOKHandler imports in agent-related services
- Agent execution tests pass
- Canvas summary tests pass
- E2E agent tests pass
- No regression in agent capabilities
</verification>

<success_criteria>
- All agent services use LLMService as single source of truth
- No direct BYOKHandler usage in agent layer
- All tests pass
- No regression in functionality
- Unified LLM integration pattern established
</success_criteria>

<output>
After completion, create `.planning/phases/225.1-agent-llmservice-migration/225.1-04-SUMMARY.md`
</output>
