---
phase: 085-database-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/database/test_database_models.py
  - backend/core/models.py
autonomous: true

must_haves:
  truths:
    - "Database model tests cover all relationship types (one-to-one, one-to-many, many-to-many)"
    - "Foreign key constraints are enforced (user_id, agent_id, workspace_id references)"
    - "Cascade delete operations work correctly (agent -> executions, user -> sessions)"
    - "ORM queries return correct results with filters, joins, and aggregations"
    - "Enum constraints work (AgentStatus, UserRole, FeedbackStatus)"
    - "Unique constraints work (user.email, agent unique constraints)"
    - "NOT NULL constraints work on required fields"
  artifacts:
    - path: "backend/tests/database/test_database_models.py"
      provides: "Comprehensive database model tests"
      min_lines: 1500
      exports: ["TestRelationships", "TestConstraints", "TestCascades", "TestORMQueries"]
  key_links:
    - from: "backend/tests/database/test_database_models.py"
      to: "backend/core/models.py"
      via: "import models (AgentRegistry, AgentExecution, User, Workspace, etc.)"
      pattern: "from core.models import"
---

<objective>
Create comprehensive database model tests covering relationships, foreign keys, constraints, cascading operations, and ORM queries to achieve 90%+ coverage of database models.

Purpose: Database models are the foundation of Atom's data layer. Comprehensive testing ensures data integrity, relationships work correctly, constraints are enforced, and ORM queries return accurate results.

Output: New test file with 30+ tests covering all model relationships, constraints, and query patterns.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

Tests use:
- pytest fixtures for database sessions (db_session from conftest.py)
- Factory pattern for test data creation (factories in tests/factories/)
- Real database (SQLite for tests, migration to PostgreSQL in CI)
- SQLAlchemy ORM for queries
- pytest.mark.database for categorization
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/core/models.py
@backend/tests/conftest.py
@backend/tests/integration/test_database_integration.py

# Models to test:
- User, Workspace, Team (many-to-many relationships)
- AgentRegistry, AgentExecution (one-to-many cascade)
- AgentFeedback (relationships to agent, execution, user, episode)
- Episode, EpisodeSegment (one-to-many with cascade)
- CanvasAudit (tracking canvas presentations)
- OAuthToken, OAuthState (token encryption/decryption)
- HITLAction (human-in-the-loop approval flow)

# Existing test patterns:
- Use db_session fixture for real database operations
- Use factories for consistent test data creation
- Test rollback in fixtures, not in test cleanup
- Use pytest.raises() for exception testing
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create relationship tests for all model associations</name>
  <files>backend/tests/database/test_database_models.py</files>
  <action>
    Create test_database_models.py with comprehensive relationship tests:

    1. TestUserWorkspaceRelationship:
       - User can belong to multiple workspaces (many-to-many)
       - Workspace can have multiple users
       - user_workspaces association table works
       - Query workspaces from user relationship
       - Query users from workspace relationship

    2. TestTeamMembershipRelationship:
       - User can belong to multiple teams
       - Team can have multiple users
       - team_members association table works
       - Role field stored correctly (member, lead)

    3. TestAgentExecutionRelationship:
       - Agent has many executions (one-to-many)
       - Execution belongs to one agent
       - Cascade delete: deleting agent removes executions
       - Foreign key constraint enforced

    4. TestAgentFeedbackRelationships:
       - Feedback links to agent, execution, user, episode
       - All foreign keys are correct
       - Query feedback from agent.feedback_history
       - Query feedback from user.submitted_feedback

    5. TestEpisodeSegmentRelationship:
       - Episode has many segments
       - Segment belongs to one episode
       - Cascade delete works
       - Ordering of segments by timestamp

    6. TestOAuthTokenRelationship:
       - OAuthToken belongs to user
       - User has multiple oauth_tokens
       - Provider filtering works

    Use db_session fixture for database operations
    Use factories (AgentFactory, UserFactory) for test data
    Assert relationship queries return expected counts
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/database/test_database_models.py::TestRelationships -v</verify>
  <done>All relationship types tested (one-to-one, one-to-many, many-to-many), foreign keys verified</done>
</task>

<task type="auto">
  <name>Task 2: Create constraint tests (unique, not null, enum, check)</name>
  <files>backend/tests/database/test_database_models.py</files>
  <action>
    Add constraint tests to test_database_models.py:

    1. TestUniqueConstraints:
       - User.email must be unique (IntegrityError on duplicate)
       - OAuthState.state must be unique
       - Multiple users with same email rejected

    2. TestNotNullConstraints:
       - AgentRegistry.name cannot be NULL
       - User.email cannot be NULL
       - AgentRegistry.category cannot be NULL
       - Insert NULL and expect error

    3. TestEnumConstraints:
       - AgentStatus only allows student/intern/supervised/autonomous/paused/deprecated
       - UserRole only allows valid roles
       - FeedbackStatus only allows pending/accepted/rejected
       - HITLActionStatus only allows pending/approved/rejected/expired
       - Invalid enum values rejected

    4. TestForeignKeyConstraints:
       - AgentExecution.agent_id must reference valid agent
       - AgentFeedback.agent_id must reference valid agent
       - Episode.agent_id must reference valid agent
       - Invalid FK raises IntegrityError

    5. TestCheckConstraints (if defined in schema):
       - Confidence score between 0.0 and 1.0
       - Rating between 1 and 5 stars
       - Any other check constraints defined

    Use pytest.raises(IntegrityError) for constraint violations
    Rollback after each test to maintain isolation
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/database/test_database_models.py::TestConstraints -v</verify>
  <done>All constraints tested (unique, not null, enum, foreign key, check), violations raise IntegrityError</done>
</task>

<task type="auto">
  <name>Task 3: Create cascade delete and update tests</name>
  <files>backend/tests/database/test_database_models.py</files>
  <action>
    Add cascade operation tests to test_database_models.py:

    1. TestCascadeDeleteAgentToExecutions:
       - Create agent with 3 executions
       - Delete agent
       - Verify all 3 executions are deleted (cascade)
       - Query executions.filter_by(agent_id=agent.id) returns empty

    2. TestCascadeDeleteAgentToFeedback:
       - Create agent with 5 feedback entries
       - Delete agent
       - Verify feedback is cascade deleted

    3. TestCascadeDeleteUserToSessions:
       - Create user with 2 active sessions
       - Delete user
       - Verify sessions are deleted

    4. TestCascadeDeleteEpisodeToSegments:
       - Create episode with 10 segments
       - Delete episode
       - Verify all segments are deleted

    5. TestCascadeDeleteWorkspaceToTeams:
       - Create workspace with 2 teams
       - Delete workspace
       - Verify teams are deleted or FK nullified

    6. TestNoCascadeOnNullify:
       - Test relationships that set NULL instead of cascade
       - Verify foreign key is set to NULL on parent delete
       - Child record still exists after parent delete

    7. TestCascadeUpdate (if supported):
       - Update parent primary key
       - Verify child foreign keys updated

    Create test data, verify relationships exist, delete parent, verify cascade
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/database/test_database_models.py::TestCascades -v</verify>
  <done>All cascade operations tested, delete propagates correctly, orphaned records handled</done>
</task>

<task type="auto">
  <name>Task 4: Create ORM query tests (filters, joins, aggregations)</name>
  <files>backend/tests/database/test_database_models.py</files>
  <action>
    Add ORM query tests to test_database_models.py:

    1. TestFilterQueries:
       - Filter agents by status (STUDENT, INTERN, SUPERVISED, AUTONOMOUS)
       - Filter users by role
       - Filter executions by status (completed, failed, running)
       - Filter feedback by status (pending, accepted, rejected)
       - Chained filters (status AND category)

    2. TestJoinQueries:
       - Join agents with executions
       - Join users with workspaces
       - Join episodes with segments
       - Join feedback with agent and user
       - Eager loading with joinedload(), selectinload()

    3. TestAggregationQueries:
       - Count agents by status
       - Count executions per agent
       - Average confidence score by category
       - Max/min created_at timestamps
       - Sum of execution counts

    4. TestSortingAndPagination:
       - Order agents by confidence_score DESC
       - Order executions by created_at DESC
       - Order users by email ASC
       - Limit and offset for pagination
       - Pagination with limit=10, offset=20

    5. TestLikeAndILikeQueries:
       - Search agents by name pattern (LIKE)
       - Case-insensitive search (ILIKE if supported)
       - Start with, end with, contains patterns

    6. TestDateTimeQueries:
       - Filter by created_at date range
       - Filter by last_login > timedelta
       - Filter by completed_at between dates
       - Date truncation queries

    Use SQLAlchemy ORM methods (filter, join, group_by, order_by, limit, offset)
    Verify query results match expected data
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/database/test_database_models.py::TestORMQueries -v</verify>
  <done>All ORM query patterns tested (filter, join, aggregate, sort, paginate, like, datetime)</done>
</task>

<task type="auto">
  <name>Task 5: Create JSON field and special property tests</name>
  <files>backend/tests/database/test_database_models.py</files>
  <action>
    Add tests for JSON columns and special model properties:

    1. TestJSONFields:
       - AgentRegistry.configuration JSON field
       - AgentRegistry.schedule_config JSON field
       - User.preferences JSON field
       - Workspace.metadata_json JSON field
       - Store and retrieve nested JSON data
       - Query inside JSON (json_contains, json_extract if supported)

    2. TestTokenEncryption:
       - OAuthToken.access_token is encrypted on write
       - OAuthToken.access_token is decrypted on read
       - OAuthToken.refresh_token encryption/decryption
       - Backwards compatibility with plaintext tokens
       - _get_fernet_for_token() returns consistent key

    3. TestBooleanDefaults:
       - User.email_verified defaults to False
       - Workspace.is_startup defaults to False
       - OAuthState.used defaults to False
       - AgentRegistry.learning_phase_completed defaults to False

    4. TestDateTimeDefaults:
       - created_at defaults to current timestamp
       - updated_at auto-updates on record change
       - Server default vs Python default

    5. TestStringDefaults:
       - AgentRegistry.status defaults to 'student'
       - User.role defaults to 'member'
       - Workspace.status defaults to 'active'

    Test JSON field can store complex nested structures
    Test token properties work correctly (encrypt/decrypt)
    Test default values are set on new records
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/database/test_database_models.py::TestSpecialFields -v</verify>
  <done>JSON fields store/retrieve correctly, token encryption works, defaults applied</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. Test file created: backend/tests/database/test_database_models.py
2. Test count: 30+ tests covering all model relationships and constraints
3. Coverage for models.py reaches 90%+
4. All tests pass: pytest backend/tests/database/test_database_models.py -v
5. No regressions in existing database tests

Coverage check:
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/database/test_database_models.py --cov=backend/core/models --cov-report=term-missing
</verification>

<success_criteria>
1. 30+ new tests added for database models
2. Coverage of models.py reaches 90%+ (verify with --cov)
3. All relationship types tested (one-to-one, one-to-many, many-to-many)
4. All constraint types tested (unique, not null, enum, foreign key)
5. Cascade operations verified for all parent-child relationships
6. ORM query patterns tested (filter, join, aggregate, sort, paginate)
7. JSON fields and special properties tested (token encryption)
</success_criteria>

<output>
After completion, create `.planning/phases/085-database-integration-testing/085-01-SUMMARY.md`
</output>
