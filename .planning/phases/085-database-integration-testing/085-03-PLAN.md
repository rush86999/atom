---
phase: 085-database-integration-testing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/database/test_transactions.py
  - backend/core/database.py
autonomous: true

must_haves:
  truths:
    - "Transactions roll back on error, maintaining database consistency"
    - "Concurrent operations don't corrupt data (race conditions handled)"
    - "Isolation levels prevent dirty reads, non-repeatable reads, phantom reads"
    - "Deadlocks are detected and resolved"
    - "Nested transactions (savepoints) work correctly"
    - "Transaction context managers commit or rollback appropriately"
  artifacts:
    - path: "backend/tests/database/test_transactions.py"
      provides: "Comprehensive transaction tests for database operations"
      min_lines: 1000
      exports: ["TestTransactionRollback", "TestConcurrentOperations", "TestIsolationLevels", "TestDeadlockHandling"]
  key_links:
    - from: "backend/tests/database/test_transactions.py"
      to: "backend/core/database.py"
      via: "get_db_session context manager"
      pattern: "from core.database import get_db_session"
---

<objective>
Create comprehensive transaction tests covering rollback on error, concurrent operations, isolation levels, deadlock handling, and savepoint usage to ensure database consistency.

Purpose: Transactions are critical for data integrity. Testing ensures rollbacks work correctly, concurrent operations don't corrupt data, isolation levels are enforced, and deadlocks are handled gracefully.

Output: New test file with 15+ tests covering all transaction scenarios.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

Tests use:
- db_session fixture with automatic rollback
- Multiple concurrent sessions for race condition testing
- SQLAlchemy transaction.begin_nested() for savepoints
- Exception handling for rollback testing
- Threading/multiprocessing for concurrent operations
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/core/database.py
@backend/tests/conftest.py (db_session fixture)
@backend/tests/integration/test_database_integration.py (existing transaction tests)

# Existing transaction test patterns:
- TestTransactionRollback class in test_database_integration.py
- TestDatabaseConstraints for constraint enforcement
- TestCascadeOperations for delete cascades
- TestTransactionIsolation for isolation levels

# Transaction scenarios to test:
1. Explicit rollback on error
2. Implicit rollback on exception
3. Concurrent writes to same record
4. Concurrent reads with write (dirty read prevention)
5. Deadlock scenarios (circular dependencies)
6. Savepoint usage for nested transactions
7. Transaction isolation levels (READ COMMITTED, SERIALIZABLE)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create transaction rollback tests</name>
  <files>backend/tests/database/test_transactions.py</files>
  <action>
    Create test_transactions.py with rollback tests:

    1. TestExplicitRollback:
       - Begin transaction
       - Create agent
       - Call session.rollback()
       - Verify agent not in database
       - Verify query returns None

    2. TestImplicitRollbackOnError:
       - Begin transaction
       - Create agent
       - Create second agent with same unique constraint (should fail)
       - Catch IntegrityError
       - Verify first agent not committed (implicit rollback)
       - Verify database unchanged

    3. TestRollbackPartialChanges:
       - Begin transaction
       - Create agent
       - Create execution for that agent
       - Rollback
       - Verify neither agent nor execution in database
       - Verify no orphaned records

    4. TestCommitAfterMultipleOperations:
       - Begin transaction
       - Create agent
       - Update agent status
       - Create execution
       - Commit
       - Verify all changes persisted

    5. TestContextManagerRollback:
       - Use get_db_session context manager
       - Create records
       - Raise exception inside context
       - Verify automatic rollback
       - Verify no data leaked

    6. TestNestedTransactionRollback:
       - Begin outer transaction
       - Create agent
       - Begin nested transaction (savepoint)
       - Create execution
       - Rollback nested transaction
       - Verify execution rolled back but agent remains
       - Commit outer transaction

    Use session.rollback() and session.commit()
    Use pytest.raises for exception testing
    Verify database state after rollback
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/database/test_transactions.py::TestTransactionRollback -v</verify>
  <done>All rollback scenarios tested, data consistency maintained</done>
</task>

<task type="auto">
  <name>Task 2: Create concurrent operation tests</name>
  <files>backend/tests/database/test_transactions.py</files>
  <action>
    Add concurrent operation tests to test_transactions.py:

    1. TestConcurrentWriteSameRecord:
       - Create agent
       - Start two concurrent transactions
       - Both try to update agent status
       - Verify last commit wins (or first wins based on locking)
       - Verify no corruption

    2. TestConcurrentCreateDifferentRecords:
       - Start two concurrent transactions
       - Each creates different agent
       - Commit both
       - Verify both agents exist
       - Verify no ID collisions

    3. TestConcurrentReadWithWrite:
       - Transaction 1: Read agent, update, commit
       - Transaction 2: Read same agent concurrently
       - Verify Transaction 2 sees either old or new value (not intermediate)
       - Verify READ COMMITTED isolation prevents dirty reads

    4. TestConcurrentDelete:
       - Create agent with executions
       - Start two transactions
       - One deletes agent, one tries to update
       - Verify appropriate error or consistent result

    5. TestRaceConditionPrevention:
       - Create agent with confidence 0.5
       - Start two concurrent transactions
       - Both read confidence, increment, write
       - Verify final confidence is 0.7 (not 0.6 due to race)
       - Use SELECT FOR UPDATE or equivalent if needed

    6. TestOptimisticLocking:
       - Implement version column if available
       - Test concurrent updates with version check
       - Verify stale updates rejected

    Use threading for concurrent operations
    Use session.begin() for explicit transactions
    Use locks or SELECT FOR UPDATE for pessimistic locking
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/database/test_transactions.py::TestConcurrentOperations -v</verify>
  <done>Concurrent operations tested, race conditions handled, data integrity maintained</done>
</task>

<task type="auto">
  <name>Task 3: Create isolation level tests</name>
  <files>backend/tests/database/test_transactions.py</files>
  <action>
    Add isolation level tests to test_transactions.py:

    1. TestReadCommittedIsolation:
       - Set isolation level to READ COMMITTED
       - Transaction 1: Update agent (uncommitted)
       - Transaction 2: Read agent
       - Verify Transaction 2 sees old value (no dirty read)
       - Transaction 1 commits
       - Transaction 2 reads again
       - Verify now sees new value

    2. TestRepeatableReadIsolation:
       - Set isolation level to REPEATABLE READ
       - Transaction 1: Read agent
       - Transaction 2: Update and commit agent
       - Transaction 1: Read agent again
       - Verify sees same value both times (no non-repeatable read)

    3. TestSerializableIsolation:
       - Set isolation level to SERIALIZABLE
       - Transaction 1: Query agents by status
       - Transaction 2: Insert new agent with same status
       - Transaction 1: Query again
       - Verify same results both times (no phantom read)

    4. TestDirtyReadPrevention:
       - Default isolation (READ COMMITTED)
       - Transaction 1: Update agent, don't commit
       - Transaction 2: Read agent
       - Verify Transaction 2 doesn't see uncommitted change

    5. TestPhantomReadPrevention:
       - Transaction 1: Query all agents
       - Transaction 2: Insert and commit new agent
       - Transaction 1: Query again
       - Behavior depends on isolation level

    Use session.execution_options for isolation level
    Use multiple sessions for concurrent transactions
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/database/test_transactions.py::TestIsolationLevels -v</verify>
  <done>All isolation levels tested, dirty/non-repeatable/phantom reads prevented</done>
</task>

<task type="auto">
  <name>Task 4: Create deadlock and savepoint tests</name>
  <files>backend/tests/database/test_transactions.py</files>
  <action>
    Add deadlock and savepoint tests to test_transactions.py:

    1. TestDeadlockDetection:
       - Create two agents
       - Transaction 1: Lock agent 1, wait, try to lock agent 2
       - Transaction 2: Lock agent 2, wait, try to lock agent 1
       - Verify deadlock detected
       - Verify one transaction fails with deadlock error
       - Verify other transaction succeeds

    2. TestDeadlockRecovery:
       - Detect deadlock
       - Rollback failed transaction
       - Retry transaction
       - Verify retry succeeds

    3. TestSavepointCreation:
       - Begin transaction
       - Create agent (savepoint 1)
       - Create execution (savepoint 2)
       - Rollback to savepoint 1
       - Verify execution rolled back, agent remains
       - Commit transaction

    4. TestSavepointRelease:
       - Begin transaction
       - Create savepoint
       - Make changes
       - Release savepoint
       - Verify changes remain after commit

    5. TestNestedSavepoints:
       - Begin transaction
       - Create savepoint 1
       - Create savepoint 2
       - Rollback to savepoint 1
       - Verify both savepoint 1 and 2 changes rolled back

    6. TestTransactionTimeout:
       - Set transaction timeout
       - Create long-running transaction
       - Verify timeout enforced
       - Verify rollback on timeout

    Use session.begin_nested() for savepoints
    Use session.rollback(savepoint) for partial rollback
    Use threading for deadlock scenarios
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/database/test_transactions.py::TestDeadlockHandling -v</verify>
  <done>Deadlocks detected and resolved, savepoints work correctly</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. Test file created: backend/tests/database/test_transactions.py
2. Test count: 15+ tests covering all transaction scenarios
3. Coverage of database.py reaches 90%+
4. All tests pass: pytest backend/tests/database/test_transactions.py -v
5. No regressions in existing transaction tests

Coverage check:
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/database/test_transactions.py --cov=backend/core/database --cov-report=term-missing
</verification>

<success_criteria>
1. 15+ new tests added for database transactions
2. Rollback tested for error scenarios
3. Concurrent operations tested for race conditions
4. Isolation levels verified (READ COMMITTED, REPEATABLE READ, SERIALIZABLE)
5. Deadlock detection and handling tested
6. Savepoints tested for nested transactions
</success_criteria>

<output>
After completion, create `.planning/phases/085-database-integration-testing/085-03-SUMMARY.md`
</output>
