---
phase: 085-database-integration-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/database/test_migrations.py
  - backend/alembic/versions/*.py
autonomous: true

must_haves:
  truths:
    - "All Alembic migrations can be upgraded from base to head"
    - "All Alembic migrations can be downgraded from head to base"
    - "Data is preserved during migration (upgrade and downgrade)"
    - "Schema validation passes after each migration"
    - "Migration idempotency works (re-running same migration is safe)"
    - "Foreign key relationships maintained after migration"
  artifacts:
    - path: "backend/tests/database/test_migrations.py"
      provides: "Comprehensive migration tests for all Alembic migrations"
      min_lines: 1200
      exports: ["TestMigrationUpgrade", "TestMigrationDowngrade", "TestDataPreservation", "TestMigrationIdempotency"]
  key_links:
    - from: "backend/tests/database/test_migrations.py"
      to: "backend/alembic/versions/*.py"
      via: "alembic.command.upgrade/downgrade"
      pattern: "from alembic import command"
---

<objective>
Create comprehensive migration tests covering upgrade paths, downgrade paths, data preservation, schema validation, and idempotency for all Alembic migrations.

Purpose: Database migrations are critical for schema evolution. Testing ensures migrations work correctly, don't lose data, can be rolled back, and maintain data integrity through schema changes.

Output: New test file with tests covering all migrations in the alembic/versions directory.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

Tests use:
- alembic.command for running migrations (upgrade, downgrade, history, current)
- Fresh database fixture for migration testing
- Test data creation before migration
- Schema inspection with SQLAlchemy Inspector
- Data verification after migration
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/alembic/versions/*.py (60+ migration files)
@backend/alembic.ini
@backend/tests/e2e/fixtures/database_fixtures.py

# Key migrations to test (by feature):
- Initial migration: 981413555a0f_initial_migration_with_user_agent_field.py
- Episodic memory: 1770165004_add_episodic_memory_tables.py
- Student agent training: fa4f5aab967b_add_student_agent_training_and_maturity_.py
- Canvas recording: a0ab43a0b96f_add_canvas_recording_model.py
- Agent guidance: 60cad7faa40a_add_agent_guidance_and_view_.py
- Browser automation: f1a2b3c4d5e6_add_browser_automation_models.py
- Device capabilities: g1h2i3j4k5l6_add_device_capability_models.py
- Recent migrations: 20260220_add_cognitive_tier_preference.py, aa093d5ca52c_add_escalation_log_table.py

# Existing patterns from e2e fixtures:
- fresh_database fixture runs all migrations to head
- Use Inspector for schema validation
- Test upgrade then downgrade round-trip
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration upgrade and downgrade tests</name>
  <files>backend/tests/database/test_migrations.py</files>
  <action>
    Create test_migrations.py with upgrade/downgrade tests:

    1. TestMigrationUpgradeToHead:
       - Start from base (empty database)
       - Run alembic.command.upgrade(head)
       - Verify all migrations applied
       - Check alembic.command.current() returns head revision
       - Verify expected tables exist after upgrade

    2. TestMigrationDowngradeToBase:
       - Start from head
       - Run alembic.command.downgrade(base)
       - Verify all migrations reversed
       - Check database is empty (no tables)
       - Verify alembic.command.current() returns None

    3. TestMigrationUpgradeDowngradeRoundTrip:
       - Upgrade to head
       - Create test data in key tables
       - Downgrade to base
       - Upgrade to head again
       - Verify schema is consistent

    4. TestPartialUpgrade:
       - Upgrade to specific revision (not head)
       - Verify only migrations up to that revision applied
       - Continue upgrade to head
       - Verify remaining migrations applied

    5. TestSpecificMigration:
       - Test each critical migration individually
       - Upgrade by one revision
       - Verify schema changes for that revision
       - Continue through all revisions

    Use alembic.config.Config for configuration
    Use alembic.script.ScriptDirectory for migration listing
    Use SQLAlchemy Inspector for schema verification
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/database/test_migrations.py::TestMigrationPaths -v</verify>
  <done>All migrations upgrade and downgrade correctly, round-trip works</done>
</task>

<task type="auto">
  <name>Task 2: Create data preservation tests for migrations</name>
  <files>backend/tests/database/test_migrations.py</files>
  <action>
    Add data preservation tests to test_migrations.py:

    1. TestAgentDataPreservation:
       - Create agents with various statuses and confidence scores
       - Run migrations that modify agent_registry table
       - Verify all agents still exist
       - Verify status values preserved (including old enum values if changed)
       - Verify confidence scores unchanged
       - Verify configuration JSON preserved

    2. TestUserDataPreservation:
       - Create users with various roles and attributes
       - Run migrations that modify users table
       - Verify all users still exist
       - Verify email uniqueness maintained
       - Verify role transitions handled correctly
       - Verify preferences JSON preserved

    3. TestEpisodeDataPreservation:
       - Create episodes with segments
       - Run migrations that add episode fields
       - Verify episodes and segments intact
       - Verify canvas_context preserved if added
       - Verify segment ordering maintained

    4. TestTokenEncryptionMigration:
       - Create plaintext tokens
       - Run encryption migration
       - Verify tokens are encrypted
       - Verify tokens can still be decrypted
       - Verify backwards compatibility

    5. TestCascadeRelationshipPreservation:
       - Create agent with executions and feedback
       - Run migrations that modify relationships
       - Verify foreign keys still valid
       - Verify cascade behavior maintained

    Pattern: Create data -> Migrate -> Verify data intact
    Use assert for data verification
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/database/test_migrations.py::TestDataPreservation -v</verify>
  <done>Data preserved through all migrations, no data loss or corruption</done>
</task>

<task type="auto">
  <name>Task 3: Create schema validation tests for migrations</name>
  <files>backend/tests/database/test_migrations.py</files>
  <action>
    Add schema validation tests to test_migrations.py:

    1. TestTableCreation:
       - Verify each expected table exists after migration
       - Critical tables: agent_registry, agent_executions, users, workspaces, episodes, episode_segments, canvas_audit, oauth_tokens, hitl_actions

    2. TestColumnStructure:
       - Verify agent_registry has all expected columns
       - Verify users table has email, role, status columns
       - Verify episodes table has agent_id FK
       - Verify episode_segments has episode_id FK
       - Check column types match models (String, Integer, DateTime, JSON)

    3. TestForeignKeyConstraints:
       - Verify agent_executions.agent_id FK exists
       - Verify agent_feedback.agent_id FK exists
       - Verify episodes.agent_id FK exists
       - Verify oauth_tokens.user_id FK exists
       - Check ON DELETE behavior (cascade, restrict, set null)

    4. TestIndexCreation:
       - Verify indexes on foreign keys
       - Verify indexes on frequently queried fields (email, status, created_at)
       - Verify unique indexes (email, state)

    5. TestEnumConstraints:
       - Verify agent_registry.status check constraint exists
       - Verify users.role check constraint exists
       - Verify feedback status constraint exists

    Use SQLAlchemy Inspector.get_columns() for column verification
    Use Inspector.get_foreign_keys() for FK verification
    Use Inspector.get_indexes() for index verification
    Use Inspector.get_constraints() for constraint verification
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/database/test_migrations.py::TestSchemaValidation -v</verify>
  <done>Schema validated after migrations, all tables/columns/constraints correct</done>
</task>

<task type="auto">
  <name>Task 4: Create migration idempotency and edge case tests</name>
  <files>backend/tests/database/test_migrations.py</files>
  <action>
    Add idempotency and edge case tests to test_migrations.py:

    1. TestMigrationIdempotency:
       - Run upgrade to head
       - Run upgrade to head again (should be safe)
       - Verify no errors on re-run
       - Verify schema unchanged

    2. TestDowngradeIdempotency:
       - Run downgrade to base
       - Run downgrade to base again
       - Verify no errors
       - Verify database state consistent

    3. TestMigrationWithExistingData:
       - Create data before migration
       - Run migration
       - Verify data migrated correctly
       - Verify no data loss

    4. TestMigrationWithConflictingData:
       - Create data that conflicts with new constraints
       - Run migration (should handle gracefully)
       - Verify conflict resolution or clear error message

    5. TestPartialFailureRecovery:
       - Start migration
       - Simulate failure mid-migration
       - Re-run migration
       - Verify recovery works

    6. TestMergeMigrationHandling:
       - Test migrations that are merge points (e.g., merge_oauth_heads.py)
       - Verify merge migrations work correctly
       - Verify branch migrations apply before merge

    7. TestReversibleMigrations:
       - Verify all migrations are reversible
       - Test downgrade after each upgrade
       - Identify any non-reversible migrations

    Test idempotency by running same migration twice
    Test edge cases with conflicting data
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/database/test_migrations.py::TestMigrationEdgeCases -v</verify>
  <done>Migrations are idempotent, handle edge cases, recover from failures</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. Test file created: backend/tests/database/test_migrations.py
2. Test count: 15+ tests covering migration paths, data preservation, schema validation
3. All migrations tested (upgrade and downgrade)
4. Coverage of migration files reaches 90%+
5. All tests pass: pytest backend/tests/database/test_migrations.py -v
6. No regressions in existing tests

Coverage check:
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/database/test_migrations.py --cov=backend/alembic/versions --cov-report=term-missing
</verification>

<success_criteria>
1. 15+ new tests added for database migrations
2. All migrations tested for upgrade and downgrade paths
3. Data preservation verified for critical migrations
4. Schema validation passes after all migrations
5. Migration idempotency verified (safe to re-run)
6. Merge migrations tested and working
</success_criteria>

<output>
After completion, create `.planning/phases/085-database-integration-testing/085-02-SUMMARY.md`
</output>
