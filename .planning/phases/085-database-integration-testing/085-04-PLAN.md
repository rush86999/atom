---
phase: 085-database-integration-testing
plan: 04
type: execute
wave: 2
depends_on: ["085-01", "085-02", "085-03"]
files_modified:
  - backend/tests/integration/test_critical_paths.py
  - backend/core/agent_governance_service.py
  - backend/core/episode_segmentation_service.py
  - backend/core/episode_lifecycle_service.py
  - backend/tools/canvas_tool.py
  - backend/api/canvas_routes.py
  - backend/core/agent_graduation_service.py
autonomous: true

must_haves:
  truths:
    - "Agent execution flow tested end-to-end (governance check → streaming → LLM → logging)"
    - "Episode creation flow tested end-to-end (time gap → topic change → episode → storage)"
    - "Canvas presentation flow tested end-to-end (creation → rendering → submission → governance)"
    - "Graduation promotion flow tested end-to-end (criteria → compliance → promotion → update)"
    - "All maturity levels tested (STUDENT, INTERN, SUPERVISED, AUTONOMOUS)"
    - "Failure modes tested (provider failures, streaming interruption, governance bypass)"
  artifacts:
    - path: "backend/tests/integration/test_critical_paths.py"
      provides: "End-to-end integration tests for 4 critical business paths"
      min_lines: 2000
      exports: ["TestAgentExecutionFlow", "TestEpisodeCreationFlow", "TestCanvasPresentationFlow", "TestGraduationPromotionFlow"]
  key_links:
    - from: "backend/tests/integration/test_critical_paths.py"
      to: "backend/core/agent_governance_service.py"
      via: "check_permissions, stream_agent_response"
      pattern: "from core.agent_governance_service import"
    - from: "backend/tests/integration/test_critical_paths.py"
      to: "backend/core/episode_segmentation_service.py"
      via: "detect_time_gap, detect_topic_changes"
      pattern: "from core.episode_segmentation_service import"
    - from: "backend/tests/integration/test_critical_paths.py"
      to: "backend/tools/canvas_tool.py"
      via: "create_canvas, render_charts"
      pattern: "from tools.canvas_tool import"
---

<objective>
Create comprehensive end-to-end integration tests for the 4 critical business paths identified in Phase 81: agent execution, episode creation, canvas presentation, and graduation promotion.

Purpose: Integration tests compose individually tested components into complete workflows, ensuring the entire system works together. These tests validate the critical paths that power Atom's core functionality.

Output: New integration test file with 20+ tests covering all 4 critical business paths end-to-end.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

Tests use:
- db_session fixture for real database operations
- Mock services for external dependencies (LLM providers, WebSocket)
- pytest.mark.asyncio for async operation testing
- pytest.mark.integration for categorization
- Real service layer calls (not unit-level mocking)
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/tests/coverage_reports/CRITICAL_PATHS_ANALYSIS.md (Phase 81-03)
@backend/core/agent_governance_service.py
@backend/core/episode_segmentation_service.py
@backend/core/episode_lifecycle_service.py
@backend/tools/canvas_tool.py
@backend/core/agent_graduation_service.py

# 4 Critical Paths from Phase 81 Analysis:

1. Agent Execution Flow (9 steps):
   - Request → Governance check → Streaming response → LLM integration → Execution logging

2. Episode Creation Flow (3 steps):
   - Time gap detection → Topic change detection → Episode creation → Segment storage

3. Canvas Presentation Flow (2 steps):
   - Canvas creation → Chart rendering → Data submission → Governance enforcement

4. Graduation Promotion Flow (2 steps):
   - Graduation criteria → Constitutional check → Promotion execution → Maturity update

# Existing test patterns from Phase 82-84:
- Use AsyncMock for async methods
- Patch external services (LLM providers, WebSocket)
- Test both success and failure paths
- Verify database state changes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent execution flow integration tests</name>
  <files>backend/tests/integration/test_critical_paths.py</files>
  <action>
    Create test_critical_paths.py with agent execution flow tests:

    1. TestStudentAgentBlockedFromHighComplexity:
       - Create STUDENT agent (confidence < 0.5)
       - Try to perform HIGH complexity action (complexity >= 3)
       - Verify governance blocks the action
       - Verify check_permissions returns False
       - Verify AgentExecution NOT created in database

    2. TestAutonomousAgentSucceedsFullWorkflow:
       - Create AUTONOMOUS agent (confidence >= 0.9)
       - Execute full agent request
       - Verify governance check passes
       - Verify stream_agent_response called
       - Verify LLM response generated
       - Verify AgentExecution created with status="completed"
       - Verify execution logged in database

    3. TestStreamingInterruptionHandling:
       - Create agent, start streaming response
       - Simulate WebSocket disconnection mid-stream
       - Verify partial response handled gracefully
       - Verify execution logged with partial=True
       - Verify no database corruption

    4. TestLLMProviderFallback:
       - Configure primary LLM provider to fail
       - Configure secondary provider
       - Execute agent request
       - Verify fallback to secondary provider
       - Verify response generated
       - Verify provider logged in execution

    5. TestAuditTrailLoggingOnFailures:
       - Create agent, execute failing request
       - Verify AgentExecution created with status="failed"
       - Verify error_message populated
       - Verify execution logged even when LLM fails
       - Verify user_id, agent_id, timestamp all present

    6. TestInternAgentApprovalRequired:
       - Create INTERN agent (confidence 0.5-0.7)
       - Try action requiring approval
       - Verify proposal created, not executed
       - Verify governance requires approval

    Use real AgentGovernanceService (not mocked)
    Mock LLM provider responses only
    Verify database state changes
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/integration/test_critical_paths.py::TestAgentExecutionFlow -v</verify>
  <done>Agent execution flow tested end-to-end, governance verified, audit trail complete</done>
</task>

<task type="auto">
  <name>Task 2: Create episode creation flow integration tests</name>
  <files>backend/tests/integration/test_critical_paths.py</files>
  <action>
    Add episode creation flow tests to test_critical_paths.py:

    1. TestTimeGapDetectionBoundaries:
       - Create conversation with 5min gap
       - Verify detect_time_gap triggers episode break
       - Create conversation with 30min gap
       - Verify episode break triggered
       - Create conversation with 2hr gap
       - Verify episode break triggered
       - Create conversation with no gap
       - Verify single episode created

    2. TestTopicChangeSemanticDetection:
       - Create conversation about topic A (weather)
       - Switch to topic B (sports) without time gap
       - Verify detect_topic_changes identifies switch
       - Verify new episode created
       - Verify segment boundary at topic change

    3. TestEpisodeCreationEndToEnd:
       - Create multi-message conversation
       - Run full episode segmentation
       - Verify Episode record created in database
       - Verify EpisodeSegment records linked to episode
       - Verify agent_id populated
       - Verify timestamp ordering correct

    4. TestVectorStorageVerification:
       - Create episode with segments
       - Verify segments stored in LanceDB
       - Verify embeddings generated for each segment
       - Verify vector search can retrieve segments
       - Verify semantic search works

    5. TestSegmentationEdgeCases:
       - Empty conversation (no messages)
       - Single message episode
       - Rapid topic switches (3 topics in 10 messages)
       - Verify no crashes, reasonable segmentation

    6. TestEpisodeRetrievalAccuracy:
       - Create multiple episodes with different topics
       - Query for specific topic
       - Verify correct episodes retrieved
       - Verify semantic similarity scoring

    Use real EpisodeSegmentationService and EpisodeLifecycleService
    Mock LanceDB/vector operations if not available in test env
    Verify database state (Episode, EpisodeSegment tables)
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/integration/test_critical_paths.py::TestEpisodeCreationFlow -v</verify>
  <done>Episode creation flow tested, time gaps detected, topic changes handled, storage verified</done>
</task>

<task type="auto">
  <name>Task 3: Create canvas presentation flow integration tests</name>
  <files>backend/tests/integration/test_critical_paths.py</files>
  <action>
    Add canvas presentation flow tests to test_critical_paths.py:

    1. TestCanvasCreationDifferentChartTypes:
       - Create line chart canvas
       - Verify canvas created with correct type
       - Create bar chart canvas
       - Verify canvas created
       - Create pie chart canvas
       - Verify canvas created
       - Create markdown canvas
       - Verify canvas created

    2. TestChartRenderingAccuracy:
       - Create canvas with test data
       - Call render_charts
       - Verify data mapped correctly to chart elements
       - Verify labels, series, data points correct
       - Verify Recharts SVG structure valid

    3. TestFormDataValidationAndSubmission:
       - Create canvas with form fields
       - Submit form data
       - Verify validation rules enforced
       - Verify data stored in database
       - Verify CanvasAudit entry created

    4. TestGovernanceEnforcementOnCanvas:
       - Create STUDENT agent
       - Try to create form canvas
       - Verify governance blocks (forms require INTERN+)
       - Verify error message appropriate
       - Create AUTONOMOUS agent
       - Verify form creation allowed

    5. TestWebSocketCanvasUpdates:
       - Create canvas
       - Simulate WebSocket update
       - Verify canvas state updated
       - Verify subscribers notified
       - Verify state synchronization works

    6. TestCanvasStatePersistence:
       - Create canvas with initial state
       - Update canvas state
       - Verify state persisted to database
       - Retrieve canvas, verify state matches
       - Verify version increment if applicable

    Use real CanvasTool and canvas routes
    Mock WebSocket for real-time updates
    Verify CanvasAudit table entries
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/integration/test_critical_paths.py::TestCanvasPresentationFlow -v</verify>
  <done>Canvas presentation flow tested, all chart types render, governance enforced</done>
</task>

<task type="auto">
  <name>Task 4: Create graduation promotion flow integration tests</name>
  <files>backend/tests/integration/test_critical_paths.py</files>
  <action>
    Add graduation promotion flow tests to test_critical_paths.py:

    1. TestGraduationCriteriaCalculation:
       - Create agent with 10 episodes
       - Set intervention rate to 40%
       - Set constitutional score to 0.70
       - Calculate readiness
       - Verify STUDENT → INTERN criteria met
       - Verify readiness score correct (episodes + interventions + constitutional)

    2. TestConstitutionalComplianceValidation:
       - Create agent meeting episode/intervention criteria
       - Set constitutional score below threshold (0.65)
       - Verify validate_compliance returns False
       - Verify promotion blocked
       - Set constitutional score above threshold (0.75)
       - Verify compliance check passes

    3. TestEndToEndGraduationFlow:
       - Create qualified STUDENT agent
       - Execute full promotion process
       - Verify agent.status transitions to INTERN
       - Verify promotion timestamp recorded
       - Verify audit log entry created
       - Repeat for INTERN → SUPERVISED
       - Repeat for SUPERVISED → AUTONOMOUS

    4. TestReadinessScoreCalculation:
       - Test 40% episodes, 30% interventions, 30% constitutional split
       - Verify formula: readiness = 0.4*episode_score + 0.3*intervention_score + 0.3*constitutional_score
       - Verify boundary cases (exact threshold values)

    5. TestPromotionRejection:
       - Create agent with insufficient episodes (5)
       - Try to promote to INTERN
       - Verify promotion rejected
       - Verify agent status unchanged (STUDENT)
       - Verify rejection reason logged
       - Create agent with high intervention rate (50%)
       - Verify promotion rejected

    6. TestMaturityUpdatePersistence:
       - Promote agent
       - Verify database updated immediately
       - Query agent from new session
       - Verify status persisted
       - Verify confidence_score updated
       - Verify no stale state

    Use real AgentGraduationService
    Use real governance checks
    Verify database state changes
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/integration/test_critical_paths.py::TestGraduationPromotionFlow -v</verify>
  <done>Graduation flow tested, criteria calculated, compliance validated, promotion executed</done>
</task>

<task type="auto">
  <name>Task 5: Create cross-cutting integration tests</name>
  <files>backend/tests/integration/test_critical_paths.py</files>
  <action>
    Add cross-cutting tests covering shared concerns:

    1. TestGovernanceBypassPrevention:
       - Test governance check at all maturity levels
       - Verify STUDENT cannot perform restricted actions
       - Verify INTERN requires approval for complex actions
       - Verify AUTONOMOUS can perform all actions
       - Test emergency bypass (if enabled)

    2. TestDataIntegrityAcrossPaths:
       - Create data in agent execution flow
       - Access from episode flow (same agent)
       - Verify data consistency
       - Verify foreign keys maintained
       - Verify no orphaned records

    3. TestAuditTrailCompleteness:
       - Execute agent request
       - Create episode
       - Create canvas
       - Promote agent
       - Verify all actions logged in audit trail
       - Verify timestamps sequential
       - Verify user_id attribution correct

    4. TestErrorRecoveryAcrossPaths:
       - Trigger error in agent execution
       - Verify cleanup occurs
       - Continue to episode creation
       - Verify no corruption from previous error
       - Verify independent operation of paths

    5. TestConcurrencyAcrossPaths:
       - Execute agent request while creating episode
       - Verify no race conditions
       - Verify database locks handled
       - Verify both operations complete

    Test interactions between critical paths
    Verify shared state handled correctly
    Test error propagation across boundaries
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/integration/test_critical_paths.py::TestCrossCuttingConcerns -v</verify>
  <done>Cross-cutting concerns tested, governance enforced, data integrity maintained</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. Test file created: backend/tests/integration/test_critical_paths.py
2. Test count: 20+ tests covering all 4 critical paths
3. All critical paths tested end-to-end
4. Coverage for critical service files reaches 80%+
5. All tests pass: pytest backend/tests/integration/test_critical_paths.py -v
6. No regressions in existing tests

Coverage check:
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/integration/test_critical_paths.py --cov=backend/core/agent_governance_service --cov=backend/core/episode_segmentation_service --cov=backend/core/agent_graduation_service --cov=backend/tools/canvas_tool --cov-report=term-missing
</verification>

<success_criteria>
1. 20+ new integration tests added
2. Agent execution flow tested (governance → streaming → LLM → logging)
3. Episode creation flow tested (time gap → topic change → episode → storage)
4. Canvas presentation flow tested (creation → rendering → submission → governance)
5. Graduation flow tested (criteria → compliance → promotion → update)
6. All maturity levels tested (STUDENT, INTERN, SUPERVISED, AUTONOMOUS)
7. Failure modes tested (provider failures, streaming interruption, governance bypass)
</success_criteria>

<output>
After completion, create `.planning/phases/085-database-integration-testing/085-04-SUMMARY.md`
</output>
