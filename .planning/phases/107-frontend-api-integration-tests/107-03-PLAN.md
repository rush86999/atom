---
phase: 107-frontend-api-integration-tests
plan: 03
type: execute
wave: 2
depends_on: ["107-01", "107-04"]
files_modified:
  - frontend-nextjs/lib/__tests__/api/error-handling.test.ts
  - frontend-nextjs/lib/__tests__/api/timeout-handling.test.ts
  - frontend-nextjs/lib/__tests__/api/malformed-response.test.ts
autonomous: true

must_haves:
  truths:
    - "Network failure scenarios are tested (offline, DNS failure, connection refused)"
    - "Timeout scenarios are tested with proper retry logic and user feedback"
    - "Malformed response handling is tested with graceful error messages"
    - "Retry logic is tested for transient failures (503, 504)"
    - "Error boundaries prevent app crashes from API failures"
  artifacts:
    - path: "frontend-nextjs/lib/__tests__/api/error-handling.test.ts"
      provides: "Network failure and error handling tests"
      min_lines: 250
      exports: ["describe('API Error Handling')"]
    - path: "frontend-nextjs/lib/__tests__/api/timeout-handling.test.ts"
      provides: "Timeout and retry logic tests"
      min_lines: 200
      exports: ["describe('API Timeout Handling')"]
    - path: "frontend-nextjs/lib/__tests__/api/malformed-response.test.ts"
      provides: "Malformed response handling tests"
      min_lines: 150
      exports: ["describe('Malformed Response Handling')"]
  key_links:
    - from: "frontend-nextjs/lib/__tests__/api/error-handling.test.ts"
      to: "frontend-nextjs/lib/api.ts"
      via: "axios interceptor error handling"
      pattern: "apiClient\.interceptors\.response"
    - from: "frontend-nextjs/lib/__tests__/api/timeout-handling.test.ts"
      to: "frontend-nextjs/lib/api.ts"
      via: "axios timeout configuration"
      pattern: "API_TIMEOUT|MAX_RETRIES"
    - from: "frontend-nextjs/lib/__tests__/api/malformed-response.test.ts"
      to: "frontend-nextjs/lib/api.ts"
      via: "response parsing error handling"
      pattern: "try.*catch|JSON\.parse"
---

<objective>
Create comprehensive error handling tests for API integration covering network failures, timeout scenarios, and malformed responses.

Purpose: Ensure that frontend API clients handle all error scenarios gracefully with proper user feedback, retry logic, and app stability.

Output: 600+ lines of error handling tests covering network failures, timeouts, malformed responses, and retry logic.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend-nextjs/lib/api.ts
@frontend-nextjs/lib/api-client.ts
@frontend-nextjs/tests/setup.ts
@.planning/phases/107-frontend-api-integration-tests/107-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test network failure scenarios</name>
  <files>frontend-nextjs/lib/__tests__/api/error-handling.test.ts</files>
  <action>
Create network failure error handling tests:

1. Test offline/network unreachable:
   - Mock fetch/axios to throw Network Error
   - Verify error is caught and handled gracefully
   - Test that appropriate error message is shown to user
   - Verify no app crash or unhandled rejection

2. Test DNS failure:
   - Mock getaddrinfo ENOTFOUND error
   - Verify error message mentions DNS/connection issue
   - Test that user can retry manually

3. Test connection refused:
   - Mock ECONNREFUSED error
   - Verify error handling for backend not running
   - Test appropriate user feedback

4. Test SSL/TLS errors:
   - Mock certificate validation failures
   - Verify secure connection error handling
   - Test that detailed error is logged but safe message shown to user

5. Test request abortion:
   - Simulate user navigating away during request
   - Test that abort errors don't crash app
   - Verify cleanup of pending requests

6. Test chunked transfer encoding errors:
   - Mock incomplete chunked responses
   - Verify handling of partial data

7. Test error boundary integration:
   - Verify API errors don't trigger error boundary unnecessarily
   - Test that catastrophic errors (parsing, crash) do trigger boundary

Use MSW's networkError helpers for simulating network failures.
  </action>
  <verify>
npm test -- lib/__tests__/api/error-handling.test.ts --testNamePattern="network"
  </verify>
  <done>
15+ tests covering offline, DNS, connection refused, SSL, abortion, chunked errors, and error boundaries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test timeout scenarios and retry logic</name>
  <files>frontend-nextjs/lib/__tests__/api/timeout-handling.test.ts</files>
  <action>
Create timeout and retry logic tests:

1. Test request timeout:
   - Mock endpoint that delays longer than API_TIMEOUT (10s)
   - Use jest.useFakeTimers() to simulate timeout
   - Verify timeout error is thrown after configured duration
   - Test that timeout is configurable per-request

2. Test retry logic for transient failures:
   - Mock endpoint returning 503 Service Unavailable
   - Verify axios interceptor retries up to MAX_RETRIES (3)
   - Test exponential backoff between retries
   - Verify success after retries eventually succeeds

3. Test retry for 504 Gateway Timeout:
   - Mock endpoint returning 504 initially, then 200
   - Verify retry logic handles gateway timeouts

4. Test no retry for client errors (4xx):
   - Mock 400 Bad Request
   - Verify no retry attempt for 4xx errors
   - Test immediate failure for client errors

5. Test no retry for 401 Unauthorized:
   - Verify 401 triggers immediate failure
   - Test that token refresh might be attempted instead of retry

6. Test retry exhaustion:
   - Mock endpoint always returning 503
   - Verify failure after MAX_RETRIES attempts
   - Test that final error is propagated to caller

7. Test retry with request body:
   - Verify POST request body is preserved across retries
   - Test that large payloads don't cause issues during retry

8. Test concurrent request timeouts:
   - Multiple requests timing out simultaneously
   - Verify no race conditions or memory leaks

9. Test timeout cancellation:
   - Component unmount during pending request
   - Verify abort controller cancels request

Use jest.useFakeTimers() for timeout tests and advance timers appropriately.
  </action>
  <verify>
npm test -- lib/__tests__/api/timeout-handling.test.ts
  </verify>
  <done>
20+ tests covering timeouts, retries, 503/504 handling, 4xx behavior, and cancellation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Test malformed response handling</name>
  <files>frontend-nextjs/lib/__tests__/api/malformed-response.test.ts</files>
  <action>
Create malformed response handling tests:

1. Test invalid JSON response:
   - Mock endpoint returning invalid JSON
   - Verify JSON parsing error is caught
   - Test graceful degradation with error message

2. Test empty response body:
   - Mock 200 OK with empty body
   - Verify handling of empty responses
   - Test that default values are used appropriately

3. Test missing required fields:
   - Mock response missing required fields (success, data)
   - Verify validation catches missing fields
   - Test that error message indicates what's missing

4. Test wrong data types:
   - Mock response with wrong types (string instead of object)
   - Verify type validation or graceful fallback
   - Test that unexpected types don't crash app

5. Test oversized responses:
   - Mock extremely large JSON response
   - Verify memory handling
   - Test response size limits if configured

6. Test malformed headers:
   - Mock invalid Content-Type header
   - Verify handling of unspecified content types
   - Test text vs JSON handling

7. Test charset issues:
   - Mock response with invalid charset
   - Verify encoding fallback
   - Test UTF-8 vs other encodings

8. Test partial/truncated responses:
   - Mock response ending mid-JSON
   - Verify parsing error handling
   - Test that app doesn't hang waiting for more data

9. Test API version mismatch:
   - Mock response with unexpected API version structure
   - Verify backward compatibility handling
   - Test that deprecation warnings are logged

10. Test null/undefined values in response:
    - Mock response with null values in required fields
    - Verify null safety checks
    - Test default value application

Use MSW's ctx.json() for response mocking and ctx.set('headers') for header tests.
  </action>
  <verify>
npm test -- lib/__tests__/api/malformed-response.test.ts
  </verify>
  <done>
20+ tests covering JSON errors, empty responses, missing fields, type mismatches, size issues, headers, encoding, and more.
  </done>
</task>

</tasks>

<verification>
Run all error handling tests and verify:
- npm test -- lib/__tests__/api/error-handling.test.ts passes with 15+ tests
- npm test -- lib/__tests__/api/timeout-handling.test.ts passes with 20+ tests
- npm test -- lib/__tests__/api/malformed-response.test.ts passes with 20+ tests
- Coverage report shows error handling tests at 50%+ coverage
- No unhandled promise rejections in tests
- Error messages are user-friendly (not technical jargon)
</verification>

<success_criteria>
1. 55+ error handling tests created (network failures, timeouts, malformed responses)
2. All error scenarios have user-friendly error messages
3. Retry logic tested for transient failures
4. Tests achieve 50%+ coverage for error handling code
5. No app crashes from API errors in any scenario
</success_criteria>

<output>
After completion, create `.planning/phases/107-frontend-api-integration-tests/107-03-SUMMARY.md` with:
- Test count and pass rate
- Coverage metrics for error handling tests
- Error handling patterns documented
- Bugs or issues discovered (especially error message quality)
</output>
