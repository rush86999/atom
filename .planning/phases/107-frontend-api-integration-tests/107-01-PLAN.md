---
phase: 107-frontend-api-integration-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/lib/__tests__/api/agent-api.test.ts
  - frontend-nextjs/lib/__tests__/api/api-client.test.ts
autonomous: true

must_haves:
  truths:
    - "Agent chat streaming API calls are mocked and tested with request/response validation"
    - "Agent execution trigger API calls are tested with proper payload validation"
    - "Agent status polling API calls are tested with error scenarios"
    - "Tests verify WebSocket connection and message handling for agent streaming"
    - "API error responses (401, 403, 404, 500) are properly handled"
  artifacts:
    - path: "frontend-nextjs/lib/__tests__/api/agent-api.test.ts"
      provides: "Agent API integration tests"
      min_lines: 300
      exports: ["describe('Agent API Integration Tests')"]
    - path: "frontend-nextjs/lib/__tests__/api/api-client.test.ts"
      provides: "API client base tests"
      min_lines: 150
      exports: ["describe('API Client')"]
  key_links:
    - from: "frontend-nextjs/lib/__tests__/api/agent-api.test.ts"
      to: "backend/core/atom_agent_endpoints.py"
      via: "MSW mocked HTTP responses"
      pattern: "msw\.rest\.(get|post)"
    - from: "frontend-nextjs/lib/api.ts"
      to: "frontend-nextjs/lib/__tests__/api/agent-api.test.ts"
      via: "axios API client mocking"
      pattern: "apiClient\.(get|post)"
---

<objective>
Create comprehensive integration tests for Agent API endpoints including chat streaming, execution trigger, and status polling.

Purpose: Validate that frontend Agent API calls work correctly with mocked backend responses, ensuring proper request/response handling and error scenarios.

Output: 300+ lines of agent API integration tests covering chat, execution, and status endpoints.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend-nextjs/lib/api.ts
@frontend-nextjs/hooks/useWebSocket.ts
@backend/core/atom_agent_endpoints.py
@frontend-nextjs/tests/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up MSW (Mock Service Worker) for API mocking</name>
  <files>frontend-nextjs/lib/__tests__/api/agent-api.test.ts</files>
  <action>
Create agent-api.test.ts with MSW setup:

1. Install MSW if not already in package.json (msw@^2.0.0)
2. Import MSW handlers: { rest, setupWorker, setupServer }
3. Create a mock server setup for jsdom environment:
   ```typescript
   const server = setupServer(
     // Agent chat stream endpoint
     rest.post('/api/atom-agent/chat/stream', (req, res, ctx) => {
       return res(
         ctx.status(200),
         ctx.json({ success: true, response: 'Test response' })
       );
     }),
     // Agent execution endpoint
     rest.post('/api/atom-agent/execute-generated', (req, res, ctx) => {
       return res(
         ctx.status(200),
         ctx.json({ execution_id: 'exec-123', status: 'running' })
       );
     }),
     // Agent status endpoint
     rest.get('/api/atom-agent/agents/:agentId/status', (req, res, ctx) => {
       return res(
         ctx.status(200),
         ctx.json({ agent_id: req.params.agentId, status: 'idle' })
       );
     }),
   );
   ```

4. Add beforeEach/afterEach hooks to start/stop server
5. Test that server is properly configured
  </action>
  <verify>
npm test -- lib/__tests__/api/agent-api.test.ts
  </verify>
  <done>
MSW server is configured, starts before tests, stops after tests, and handles basic agent endpoint routes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test agent chat streaming API</name>
  <files>frontend-nextjs/lib/__tests__/api/agent-api.test.ts</files>
  <action>
Add chat streaming API tests:

1. Test successful chat request:
   - Mock POST /api/atom-agent/chat/stream returning 200
   - Call apiClient.post('/api/atom-agent/chat/stream', payload)
   - Verify response contains success: true and response data
   - Verify request payload includes message, user_id, session_id

2. Test chat with conversation history:
   - Mock endpoint accepting conversation_history array
   - Verify ChatMessage[] is serialized correctly
   - Test empty history vs populated history

3. Test chat streaming response (partial):
   - Mock streaming chunks for token-by-token response
   - Test that streaming updates are accumulated correctly

4. Test chat error scenarios:
   - 401 Unauthorized (missing/invalid token)
   - 400 Bad Request (invalid payload)
   - 500 Internal Server Error

5. Test agent governance in chat:
   - Verify agent_id is passed if provided
   - Test governance blocked responses (403 Forbidden)

Use waitFor for async assertions and proper cleanup.
  </action>
  <verify>
npm test -- lib/__tests__/api/agent-api.test.ts --testNamePattern="chat stream"
  </verify>
  <done>
15+ tests covering successful chat, conversation history, streaming, errors, and governance.
  </done>
</task>

<task type="auto">
  <name>Task 3: Test agent execution trigger and status polling APIs</name>
  <files>frontend-nextjs/lib/__tests__/api/agent-api.test.ts</files>
  <action>
Add execution and status API tests:

1. Test execution trigger:
   - Mock POST /api/atom-agent/execute-generated returning 200
   - Call with workflow_id and input_data
   - Verify response contains execution_id and status
   - Test different execution statuses: running, completed, failed

2. Test execution with governance:
   - Mock governance check responses (allowed/blocked)
   - Test that agent maturity level affects execution
   - Verify action complexity validation

3. Test status polling:
   - Mock GET /api/atom-agent/agents/:agentId/status
   - Test polling interval and cancellation
   - Verify status transitions: idle → running → completed

4. Test retrieve-hybrid endpoint:
   - Mock POST /api/atom-agent/agents/:agentId/retrieve-hybrid
   - Test query parameter handling
   - Verify retrieval with filters and pagination

5. Test error scenarios:
   - 404 for non-existent agent
   - 403 for governance blocked actions
   - 500 for backend failures

Use jest.useFakeTimers for polling interval tests.
  </action>
  <verify>
npm test -- lib/__tests__/api/agent-api.test.ts --testNamePattern="execution|status"
  </verify>
  <done>
15+ tests covering execution trigger, status polling, retrieval, and error handling.
  </done>
</task>

</tasks>

<verification>
Run all agent API integration tests and verify:
- npm test -- lib/__tests__/api/agent-api.test.ts passes with 30+ tests
- Coverage report shows agent-api.test.ts at 50%+ coverage for agent API calls
- All MSW handlers are properly configured and cleaned up
- No test isolation issues (tests don't affect each other)
</verification>

<success_criteria>
1. 30+ agent API integration tests created (chat, execution, status)
2. MSW handlers mock all agent endpoints (/api/atom-agent/*)
3. Error scenarios tested (401, 403, 404, 500)
4. Tests achieve 50%+ coverage for agent API integration code
</success_criteria>

<output>
After completion, create `.planning/phases/107-frontend-api-integration-tests/107-01-SUMMARY.md` with:
- Test count and pass rate
- Coverage metrics for agent API tests
- MSW handler documentation
- Bugs or issues discovered
</output>
