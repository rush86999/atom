---
phase: 107-frontend-api-integration-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/lib/__tests__/api/canvas-api.test.ts
  - frontend-nextjs/hooks/__tests__/useCanvasState.api.test.ts
autonomous: true

must_haves:
  truths:
    - "Canvas presentation API calls are mocked and tested with request/response validation"
    - "Canvas form submission API calls are tested with proper payload validation and governance"
    - "Canvas close/exit API calls are tested with state cleanup"
    - "Canvas state updates trigger appropriate API calls"
    - "Canvas API error responses are properly handled and displayed to users"
  artifacts:
    - path: "frontend-nextjs/lib/__tests__/api/canvas-api.test.ts"
      provides: "Canvas API integration tests"
      min_lines: 250
      exports: ["describe('Canvas API Integration Tests')"]
    - path: "frontend-nextjs/hooks/__tests__/useCanvasState.api.test.ts"
      provides: "Canvas state API integration tests"
      min_lines: 200
      exports: ["describe('useCanvasState API Integration')"]
  key_links:
    - from: "frontend-nextjs/lib/__tests__/api/canvas-api.test.ts"
      to: "backend/api/canvas_routes.py"
      via: "MSW mocked HTTP responses"
      pattern: "msw\.rest\.(get|post)"
    - from: "frontend-nextjs/hooks/useCanvasState.ts"
      to: "frontend-nextjs/hooks/__tests__/useCanvasState.api.test.ts"
      via: "renderHook testing"
      pattern: "renderHook.*useCanvasState"
---

<objective>
Create comprehensive integration tests for Canvas API endpoints including presentation, form submission, and close operations.

Purpose: Validate that frontend Canvas API calls work correctly with mocked backend responses, ensuring proper request/response handling, governance integration, and state cleanup.

Output: 450+ lines of canvas API integration tests covering presentation, form submission, and close operations.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend-nextjs/hooks/useCanvasState.ts
@backend/api/canvas_routes.py
@frontend-nextjs/tests/setup.ts
@.planning/phases/106-frontend-state-management/106-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up Canvas API MSW handlers and presentation tests</name>
  <files>frontend-nextjs/lib/__tests__/api/canvas-api.test.ts</files>
  <action>
Create canvas-api.test.ts with MSW handlers:

1. Import MSW and React Testing Library utilities
2. Create MSW handlers for canvas endpoints:
   ```typescript
   const server = setupServer(
     // Canvas submit endpoint
     rest.post('/api/canvas/submit', (req, res, ctx) => {
       return res(
         ctx.status(200),
         ctx.json({
           success: true,
           submission_id: 'sub-123',
           governance_check: { allowed: true }
         })
       );
     }),
     // Canvas status endpoint
     rest.get('/api/canvas/status', (req, res, ctx) => {
       return res(
         ctx.status(200),
         ctx.json({
           canvas_id: 'canvas-123',
           status: 'active',
           agent_execution_id: 'exec-456'
         })
       );
     }),
     // Canvas close endpoint
     rest.post('/api/canvas/:canvasId/close', (req, res, ctx) => {
       return res(
         ctx.status(200),
         ctx.json({ success: true, closed_at: '2024-02-28T10:00:00Z' })
       );
     }),
   );
   ```

3. Test canvas presentation (present operation):
   - Mock successful canvas present response
   - Verify request includes canvas_type and initial_data
   - Test different canvas types: generic, docs, email, sheets, orchestration, terminal, coding

4. Test canvas state retrieval:
   - Mock GET /api/canvas/status
   - Verify response contains canvas_id, status, agent_execution_id
   - Test different status values: active, closed, errored

5. Test accessibility API integration:
   - Verify canvas state is exposed via window.atom.canvas.getState()
   - Test that updates trigger accessibility tree changes
  </action>
  <verify>
npm test -- lib/__tests__/api/canvas-api.test.ts --testNamePattern="presentation|status"
  </verify>
  <done>
15+ tests covering canvas presentation, status retrieval, and accessibility API.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test canvas form submission API with governance</name>
  <files>frontend-nextjs/lib/__tests__/api/canvas-api.test.ts</files>
  <action>
Add form submission API tests:

1. Test successful form submission:
   - Mock POST /api/canvas/submit returning 200
   - Call with canvas_id and form_data
   - Verify response contains submission_id and governance_check
   - Test different form data types: text, select, checkbox, file

2. Test form submission with agent context:
   - Include agent_execution_id in request
   - Include agent_id for governance check
   - Verify governance_check reflects agent maturity level

3. Test governance blocked submissions:
   - Mock 403 Forbidden for STUDENT agent submitting form
   - Verify error response contains governance block reason
   - Test that form shows appropriate error message

4. Test form validation errors:
   - Mock 400 Bad Request for invalid form data
   - Test missing required fields
   - Test invalid data types (e.g., text in number field)

5. Test form submission retry:
   - Mock initial 500 error, then success
   - Verify retry logic works correctly
   - Test that retry doesn't duplicate submissions

6. Test form submission with file uploads:
   - Mock multipart form data handling
   - Test file size validation
   - Test file type validation

7. Test submission confirmation:
   - Verify success callback is triggered
   - Test that canvas state updates after submission
   - Test WebSocket notification of submission

Use waitFor for async assertions and proper cleanup.
  </action>
  <verify>
npm test -- lib/__tests__/api/canvas-api.test.ts --testNamePattern="submit|governance"
  </verify>
  <done>
20+ tests covering form submission, governance, validation, retries, and file uploads.
  </done>
</task>

<task type="auto">
  <name>Task 3: Test canvas close operation and useCanvasState API integration</name>
  <files>frontend-nextjs/hooks/__tests__/useCanvasState.api.test.ts</files>
  <action>
Create useCanvasState API integration tests:

1. Test canvas close API:
   - Mock POST /api/canvas/:canvasId/close returning 200
   - Call closeCanvas() from useCanvasState hook
   - Verify response contains success: true and closed_at timestamp
   - Test that canvas state is removed from state management

2. Test canvas close with unsaved changes:
   - Mock close endpoint that prompts for confirmation
   - Test that user is prompted before closing
   - Test cancel vs confirm behavior

3. Test canvas close error handling:
   - Mock 404 for non-existent canvas
   - Mock 500 for backend failure during close
   - Verify error is handled gracefully

4. Test useCanvasState API integration:
   - Use renderHook from @testing-library/react-hooks
   - Test that setState triggers appropriate API calls
   - Test that API responses update canvas state correctly
   - Test state cleanup on unmount

5. Test canvas lifecycle API calls:
   - Present → Update → Submit → Close flow
   - Verify each step calls correct API endpoint
   - Test that state transitions are atomic

6. Test concurrent canvas operations:
   - Test multiple canvases open simultaneously
   - Verify operations don't interfere with each other
   - Test canvas isolation (one canvas close doesn't affect others)

7. Test canvas WebSocket integration:
   - Mock WebSocket messages for canvas updates
   - Test that incoming updates modify canvas state
   - Test that canvas changes broadcast to other clients

Use act() for state updates and waitFor for async operations.
  </action>
  <verify>
npm test -- hooks/__tests__/useCanvasState.api.test.ts
  </verify>
  <done>
20+ tests covering canvas close, useCanvasState API integration, lifecycle, and WebSocket.
  </done>
</task>

</tasks>

<verification>
Run all canvas API integration tests and verify:
- npm test -- lib/__tests__/api/canvas-api.test.ts passes with 35+ tests
- npm test -- hooks/__tests__/useCanvasState.api.test.ts passes with 20+ tests
- Coverage report shows canvas API tests at 50%+ coverage
- MSW handlers properly mock all canvas endpoints
- No state leakage between tests
</verification>

<success_criteria>
1. 55+ canvas API integration tests created (presentation, submit, close, lifecycle)
2. MSW handlers mock all canvas endpoints (/api/canvas/*)
3. Governance integration tested for form submissions
4. Tests achieve 50%+ coverage for canvas API integration code
5. useCanvasState hook API integration tested
</success_criteria>

<output>
After completion, create `.planning/phases/107-frontend-api-integration-tests/107-02-SUMMARY.md` with:
- Test count and pass rate
- Coverage metrics for canvas API tests
- Governance integration test results
- Bugs or issues discovered
</output>
