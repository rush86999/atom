---
phase: 04-hybrid-retrieval
plan: 04
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - tests/unit/test_hybrid_retrieval.py
  - core/embedding_service.py
autonomous: true

must_haves:
  truths:
    - "Embedding consistency test passes (np.ndarray return type)"
    - "Unit tests pass 10/10 (100% pass rate)"
    - "Return type consistency between FastEmbed and standard embeddings"
  artifacts:
    - path: "core/embedding_service.py"
      provides: "Consistent np.ndarray return type from create_fastembed_embedding"
      contains: "def create_fastembed_embedding"
    - path: "tests/unit/test_hybrid_retrieval.py"
      provides: "Fixed unit tests with correct return type expectations"
  key_links:
    - from: "core/embedding_service.py"
      to: "tests/unit/test_hybrid_retrieval.py"
      via: "Return type contract (np.ndarray)"
      pattern: "np.ndarray"
---

<objective>
Fix embedding return type consistency and unit tests for hybrid retrieval.

Purpose: FastEmbed returns List[float] but tests expect np.ndarray with .shape attribute. This breaks consistency tests. Fix return type and update 5 failing unit tests.

Gap: 4-04 - "Embedding consistency" and "Unit tests pass (10/10)"

Output: Consistent np.ndarray return type, all 10 unit tests passing
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-hybrid-retrieval/04-hybrid-retrieval-VERIFICATION.md
@core/embedding_service.py
@tests/unit/test_hybrid_retrieval.py
</context>

<tasks>

<task type="auto">
  <name>Fix FastEmbed return type to np.ndarray</name>
  <files>core/embedding_service.py</files>
  <action>
Update create_fastembed_embedding to return np.ndarray instead of List[float]:

1. Find the function that returns List[float]
2. Change return type annotation to np.ndarray
3. Ensure the function converts FastEmbed output to numpy array
4. Add import: import numpy as np

Pattern to fix:
```python
# Before
def create_fastembed_embedding(text: str) -> List[float]:
    embedding = fastembed_model.embed(text)
    return list(embedding)  # Returns list

# After
def create_fastembed_embedding(text: str) -> np.ndarray:
    embedding = fastembed_model.embed(text)
    return np.array(embedding)  # Returns np.ndarray
```
  </action>
  <verify>grep -A 5 "def create_fastembed_embedding" core/embedding_service.py</verify>
  <done>Function returns np.ndarray with proper shape attribute</done>
</task>

<task type="auto">
  <name>Fix failing unit tests</name>
  <files>tests/unit/test_hybrid_retrieval.py</files>
  <action>
Fix the 5 failing unit tests:

1. test_create_fastembed_embedding_mocked - Expect np.ndarray with .shape
2. test_coarse_search_performance_mocked - Fix import issue
3. test_rerank_cross_encoder_mocked - Fix mocking issue
4. test_reranking_performance_mocked - Fix mocking issue
5. test_reranker_lazy_loading - Fix fixture issue

Approach:
- Run pytest tests/unit/test_hybrid_retrieval.py -v to see exact errors
- Fix import errors (missing modules, incorrect paths)
- Fix mock expectations to match actual return types
- Fix fixture dependencies (db_session vs db)

Target: 10/10 tests passing (100%)
  </action>
  <verify>pytest tests/unit/test_hybrid_retrieval.py -v --tb=short</verify>
  <done>10/10 tests passing, 0 failures</done>
</task>

</tasks>

<verification>
After completion:
1. Verify create_fastembed_embedding returns np.ndarray
2. Run all unit tests: pytest tests/unit/test_hybrid_retrieval.py -v
3. Verify 10/10 tests passing
4. Check VERIFICATION.md gap 4-04 status changed to complete
</verification>

<success_criteria>
- [ ] create_fastembed_embedding returns np.ndarray
- [ ] All 10 unit tests pass (100%)
- [ ] No AttributeError: 'list' object has no attribute 'shape'
- [ ] Verification gap 4-04 marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-hybrid-retrieval/gap-closure/04-04-SUMMARY.md`
</output>
