---
phase: 04-hybrid-retrieval
plan: 05
type: execute
wave: 2
depends_on: [04-04]
gap_closure: true
files_modified:
  - tests/property_tests/episodes/test_hybrid_retrieval_invariants.py
autonomous: true

must_haves:
  truths:
    - "Property tests run successfully without fixture errors"
    - "All property tests use db_session fixture (not db)"
    - "Monotonic improvement and top-K completeness tests pass"
  artifacts:
    - path: "tests/property_tests/episodes/test_hybrid_retrieval_invariants.py"
      provides: "Fixed property tests with correct fixture naming"
      contains: "def test_.*\(self, db_session\)"
  key_links:
    - from: "tests/property_tests/episodes/test_hybrid_retrieval_invariants.py"
      to: "backend/tests/conftest.py"
      via: "db_session fixture dependency"
      pattern: "db_session"
---

<objective>
Fix property test fixture naming and make tests runnable.

Purpose: Property tests use 'db' fixture but conftest provides 'db_session'. This causes ERROR at setup. Fix all fixture references to use db_session.

Gap: 4-05 - "Property tests run successfully"

Output: All property tests using db_session, tests run without setup errors
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-hybrid-retrieval/04-hybrid-retrieval-VERIFICATION.md
@tests/property_tests/episodes/test_hybrid_retrieval_invariants.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Fix property test fixture references</name>
  <files>tests/property_tests/episodes/test_hybrid_retrieval_invariants.py</files>
  <action>
Fix all property test functions to use 'db_session' instead of 'db':

1. Find all test functions with 'db' parameter:
   grep -n "def test_.*\(self, db\)" tests/property_tests/episodes/test_hybrid_retrieval_invariants.py

2. Replace 'db' with 'db_session' in function signatures:
   def test_foo(self, db): → def test_foo(self, db_session):

3. Update all references to 'db' within test bodies:
   db.query(...) → db_session.query(...)
   db.add(...) → db_session.add(...)
   db.commit() → db_session.commit()

4. Search for any other 'db' references that should be 'db_session'

Expected changes: ~6-8 test functions need updating
  </action>
  <verify>grep -c "def test_.*db_session" tests/property_tests/episodes/test_hybrid_retrieval_invariants.py</verify>
  <done>All test functions use db_session parameter</done>
</task>

<task type="auto">
  <name>Run property tests to verify fixes</name>
  <files>tests/property_tests/episodes/test_hybrid_retrieval_invariants.py</files>
  <action>
Run property tests to verify they run without setup errors:

1. Run: pytest tests/property_tests/episodes/test_hybrid_retrieval_invariants.py -v --tb=short
2. Check for ERROR at setup (should be 0)
3. Verify tests collect successfully
4. Check test results (some may still fail due to mocked data, but should run)

Target: 0 setup errors, tests execute successfully
  </action>
  <verify>pytest tests/property_tests/episodes/test_hybrid_retrieval_invariants.py -v --tb=short 2>&1 | grep -E "collected|ERROR|error"</verify>
  <done>Tests collect and run without setup errors</done>
</task>

</tasks>

<verification>
After completion:
1. Verify all test functions use db_session parameter
2. Run property tests and confirm 0 setup errors
3. Check tests execute (even if some fail due to mocks)
4. Update VERIFICATION.md gap 4-05 to complete
</verification>

<success_criteria>
- [ ] All property tests use db_session fixture
- [ ] 0 setup errors (ERROR at setup)
- [ ] Tests execute successfully
- [ ] Verification gap 4-05 marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/04-hybrid-retrieval/gap-closure/04-05-SUMMARY.md`
</output>
