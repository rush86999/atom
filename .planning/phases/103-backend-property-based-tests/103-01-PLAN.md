---
phase: 103-backend-property-based-tests
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/tests/property_tests/governance/test_governance_escalation_invariants.py
  - backend/tests/property_tests/governance/test_multi_agent_coordination_invariants.py
autonomous: true

must_haves:
  truths:
    - "Governance escalation triggers correctly when confidence drops below threshold"
    - "Multi-agent coordination maintains isolation between agents"
    - "Trigger interceptor routes STUDENT agents to training (not execution)"
    - "Supervision service correctly reports violations during agent execution"
    - "Graduation criteria validates all constitutional compliance checks"
  artifacts:
    - path: backend/tests/property_tests/governance/test_governance_escalation_invariants.py
      provides: Property tests for governance escalation invariants
      min_lines: 300
      exports: ["TestGovernanceEscalation", "TestConfidenceEscalation", "TestEscalationCooldowns"]
    - path: backend/tests/property_tests/governance/test_multi_agent_coordination_invariants.py
      provides: Property tests for multi-agent coordination invariants
      min_lines: 250
      exports: ["TestMultiAgentIsolation", "TestAgentResourceCompetition", "TestConcurrentExecution"]
  key_links:
    - from: backend/tests/property_tests/governance/test_governance_escalation_invariants.py
      to: backend/core/governance_cache.py
      via: Hypothesis strategies testing escalation triggers
      pattern: "@given(st.*) escalation properties"
    - from: backend/tests/property_tests/governance/test_multi_agent_coordination_invariants.py
      to: backend/core/agent_governance_service.py
      via: Hypothesis strategies testing multi-agent scenarios
      pattern: "@given(st.*) coordination properties"
---

<objective>
Create property-based tests for advanced governance invariants beyond Phase 101-04

Purpose: Phase 101-04 covered basic governance invariants (maturity ordering, permissions, cache). This plan adds advanced governance invariants for escalation logic, multi-agent coordination, trigger interception, supervision, and graduation. These are critical invariants that control agent lifecycle and safety.

Output: Two property test files with 550+ total lines, validating advanced governance invariants with Hypothesis strategies using strategic max_examples (200 critical, 100 standard, 50 IO-bound).
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/core/agent_governance_service.py
@backend/core/trigger_interceptor.py
@backend/core/supervision_service.py
@backend/core/student_training_service.py
@backend/core/agent_graduation_service.py
@backend/core/governance_cache.py
@backend/tests/property_tests/governance/test_governance_invariants_property.py

# From Phase 100 decisions
# Strategic max_examples: 200 for critical invariants, 100 for standard, 50 for IO-bound
# VALIDATED_BUG docstring pattern for bug-finding evidence
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create property tests for governance escalation invariants</name>
  <files>backend/tests/property_tests/governance/test_governance_escalation_invariants.py</files>
  <action>
Create new property test file backend/tests/property_tests/governance/test_governance_escalation_invariants.py:

**Test Structure (TDD RED-GREEN-REFACTOR):**

A. Confidence Escalation Invariants (max_examples=200 - CRITICAL):
   - test_confidence_drop_triggers_escalation
     Invariant: Confidence drop below threshold triggers tier escalation
     Strategy: st.lists of confidence scores with drops
     Given initial_confidence in [0.5, 0.9], if confidence drops by >0.2: escalate

   - test_escalation_only_upward
     Invariant: Escalation only moves to higher tiers (never down)
     Strategy: st.lists of tier transitions
     Valid escalations: Micro->Standard->Versatile->Heavy->Complex

   - test_escalation_has_cooldown
     Invariant: Same tier cannot be escalated twice within cooldown period
     Strategy: st.tuples(escalation_timestamp, current_timestamp)
     Cooldown: 5 minutes between escalations of same tier

   - test_escalation_preserves_context
     Invariant: Escalation includes original request context (prompt, tokens, complexity)
     Strategy: st.dictionaries with request metadata
     All context fields preserved across escalation

B. Trigger Interceptor Invariants (max_examples=200 - CRITICAL):
   - test_student_blocked_from_triggers
     Invariant: STUDENT agents BLOCKED from all automated triggers
     Strategy: st.tuples(agent_maturity, trigger_type)
     If maturity == STUDENT: trigger intercepted, routed to training

   - test_trigger_routing_deterministic
     Invariant: Same (agent, trigger) always routes to same destination
     Strategy: st.tuples(agent_id, trigger_type, action_type)
     100 calls with same inputs = same routing decision

   - test_blocked_trigger_has_proposal
     Invariant: Blocked triggers generate training proposal (not silent failure)
     Strategy: st.tuples(agent_id, trigger_action, trigger_context)
     Blocked triggers create BlockedTriggerContext with proposal_id

C. Supervision Service Invariants (max_examples=100 - STANDARD):
   - test_supervision_violation_reported
     Invariant: SUPERVISED agent violations generate supervision events
     Strategy: st.tuples(violation_type, severity, agent_action)
     All violations logged to SupervisionSession

   - test_intervention_requires_human_approval
     Invariant: SUPERVISED agent critical actions require human approval
     Strategy: st.tuples(action_complexity, agent_maturity)
     Complexity 4 + SUPERVISED = requires approval

   - test_supervision_session_isolated
     Invariant: Each SUPERVISED execution has isolated supervision session
     Strategy: st.lists of concurrent executions
     No session crossover between concurrent executions

**Documentation Pattern:**
```python
def test_confidence_drop_triggers_escalation(self, initial_confidence, confidence_drops):
    """
    PROPERTY: Confidence drop below threshold triggers tier escalation

    STRATEGY: st.lists of confidence scores with drops

    INVARIANT: if (initial_confidence - current_confidence) > 0.2: escalate

    ESCALATION_RULES:
    - Initial in [0.5, 0.7) (INTERN): drop to <0.5 -> escalate to MICRO
    - Initial in [0.7, 0.9) (SUPERVISED): drop to <0.7 -> escalate to STANDARD
    - Initial in [0.9, 1.0] (AUTONOMOUS): drop to <0.9 -> escalate to VERSATILE

    RADII: 200 examples explores confidence space around thresholds

    VALIDATED_BUG: None found (invariant holds)
    """
```

**Target:** 300+ lines, 12-15 properties
  </action>
  <verify>
Run tests: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/property_tests/governance/test_governance_escalation_invariants.py -v --hypothesis-seed=0
Verify all properties pass with 200/100 examples
  </verify>
  <done>
All 12-15 property tests pass, Hypothesis explores 200+ examples per property
  </done>
</task>

<task type="auto">
  <name>Task 2: Create property tests for multi-agent coordination invariants</name>
  <files>backend/tests/property_tests/governance/test_multi_agent_coordination_invariants.py</files>
  <action>
Create new property test file backend/tests/property_tests/governance/test_multi_agent_coordination_invariants.py:

**Test Structure (TDD RED-GREEN-REFACTOR):**

A. Multi-Agent Isolation Invariants (max_examples=200 - CRITICAL):
   - test_agent_execution_isolated
     Invariant: Agent executions are isolated (no shared state pollution)
     Strategy: st.lists of (agent_id, execution_context) tuples
     Agent A execution does not affect Agent B result

   - test_concurrent_agents_independent
     Invariant: Concurrent agent executions produce independent results
     Strategy: st.lists of agent_ids with concurrent execution markers
     Result of Agent A does not depend on Agent B running concurrently

   - test_agent_context_not_leaked
     Invariant: Agent context (workspace, user, session) not leaked between executions
     Strategy: st.tuples(workspace_id, user_id, session_id, agent_ids)
     Each agent has isolated context

B. Resource Competition Invariants (max_examples=100 - STANDARD):
   - test_governance_cache_threadsafe
     Invariant: GovernanceCache handles concurrent access safely
     Strategy: st.lists of agent_ids for concurrent cache access
     No race conditions, no corrupted cache entries

   - test_llm_quota_enforcement
     Invariant: LLM quota enforced per-agent (no shared quota bypass)
     Strategy: st.lists of (agent_id, token_count) requests
     Agent A quota exhaustion does not affect Agent B

   - test_workspace_resource_limits
     Invariant: Workspace resource limits enforced across all agents
     Strategy: st.integers for concurrent agent count, resource limits
     Total resource usage within workspace limits

C. Graduation Criteria Invariants (max_examples=100 - STANDARD):
   - test_graduation_thresholds_fixed
     Invariant: Graduation thresholds are constants (not variable)
     Strategy: st.sampled_from(maturity_levels)
     STUDENT->INTERN: 10 episodes, INTERN->SUPERVISED: 25, etc.

   - test_intervention_rate_decreasing
     Invariant: Successful graduation shows decreasing intervention rate
     Strategy: st.lists of intervention_counts for episodes
     For graduation: later episodes have fewer interventions

   - test_constitutional_score_required
     Invariant: Constitutional compliance score required for graduation
     Strategy: st.tuples(episode_count, intervention_rate, constitutional_score)
     If constitutional_score < threshold: graduation blocked

   - test_graduation_never_downgrades
     Invariant: Once graduated, agent never downgrades (maturity monotonic)
     Strategy: st.lists of graduation events
     Only transitions: STUDENT->INTERN->SUPERVISED->AUTONOMOUS

**Target:** 250+ lines, 10-12 properties
  </action>
  <verify>
Run tests: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/property_tests/governance/test_multi_agent_coordination_invariants.py -v --hypothesis-seed=0
Verify all properties pass with 200/100 examples
  </verify>
  <done>
All 10-12 property tests pass, Hypothesis explores 200+ examples per property
  </done>
</task>

</tasks>

<verification>
Run comprehensive property test suite:
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
  backend/tests/property_tests/governance/test_governance_escalation_invariants.py \
  backend/tests/property_tests/governance/test_multi_agent_coordination_invariants.py \
  -v --hypothesis-seed=0

Verify:
1. All property tests pass
2. Hypothesis explores target examples (200/100 based on criticality)
3. No flaky tests (add --hypothesis-seed=0 for reproducibility)
4. Invariants documented with VALIDATED_BUG pattern
5. Test execution completes in reasonable time (<3 minutes)
</verification>

<success_criteria>
1. Governance escalation invariants validated (confidence drops, escalation cooldowns, context preservation)
2. Multi-agent coordination invariants validated (isolation, resource competition, graduation)
3. Trigger interceptor invariants tested (STUDENT blocking, routing determinism)
4. Supervision service invariants tested (violation reporting, intervention approval)
5. Strategic max_examples applied (200 critical, 100 standard, 50 IO-bound)
6. All invariants documented with clear explanations in docstrings
7. Property tests integrate with existing pytest infrastructure
</success_criteria>

<output>
After completion, create `.planning/phases/103-backend-property-based-tests/103-01-SUMMARY.md`
</output>
