---
phase: 093-cost-tracking-budgets
plan: 04
type: execute
wave: 2
depends_on: ["093-01"]
files_modified:
  - backend/tests/unit/budget/test_budget_guardrails.py
  - backend/core/financial_ops_engine.py
  - backend/service_delivery/models.py
autonomous: true

must_haves:
  truths:
    - Warn threshold triggers at configured percentage (default 80%)
    - Pause threshold triggers at configured percentage (default 90%)
    - Block threshold triggers at configured percentage (default 100%)
    - Thresholds are configurable per budget
    - Enforcement actions match status (warn, pause, block)
  artifacts:
    - path: "backend/tests/unit/budget/test_budget_guardrails.py"
      provides: "Unit tests for budget guardrail threshold validation"
      min_lines: 250
    - path: "backend/core/financial_ops_engine.py"
      provides: "Enhanced BudgetGuardrails with configurable thresholds"
      contains: "class BudgetGuardrails|class BudgetLimit"
    - path: "backend/service_delivery/models.py"
      provides: "Configurable threshold columns on Project model"
      contains: "warn_threshold_pct|pause_threshold_pct"
  key_links:
    - from: "backend/tests/unit/budget/test_budget_guardrails.py"
      to: "backend/core/financial_ops_engine.py"
      via: "BudgetGuardrails and BudgetLimit imports"
      pattern: "from core.financial_ops_engine import"
    - from: "backend/tests/unit/budget/test_budget_guardrails.py"
      to: "backend/service_delivery/models.py"
      via: "Project threshold configuration"
      pattern: "Project.warn_threshold_pct"
    - from: "backend/core/financial_ops_engine.py"
      to: "backend/core/decimal_utils.py"
      via: "Decimal arithmetic for threshold calculations"
      pattern: "from core.decimal_utils import to_decimal"
---

<objective>
Budget guardrail validation testing verifies alert thresholds, enforcement actions, and configurable warnings/pauses/blocks to ensure budget spending is controlled at appropriate levels.

Purpose: Budget guardrails provide early warning before overspending occurs. Without proper threshold validation, alerts fire too late (after damage is done) or enforcement actions are incorrect (pause when should warn, block when should pause). Tests verify configurable thresholds (80% warn, 90% pause, 100% block), enforcement actions match status, and threshold behavior is consistent.

Output: Enhanced BudgetGuardrails with configurable thresholds, comprehensive threshold validation tests, and Project model with threshold configuration columns.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/templates/summary.md
</execution_context>

<context>
@.planning/phases/093-cost-tracking-budgets/093-01-SUMMARY.md
@.planning/phases/093-cost-tracking-budgets/093-RESEARCH.md
@backend/core/financial_ops_engine.py
@backend/core/decimal_utils.py
@backend/service_delivery/models.py
@backend/tests/fixtures/decimal_fixtures.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add configurable thresholds to Project model</name>
  <files>
    backend/service_delivery/models.py
  </files>
  <action>
  Add configurable threshold columns to Project model:

  Modify `backend/service_delivery/models.py` in Project class:
  1. Add `warn_threshold_pct` column:
     ```python
     warn_threshold_pct = Column(Integer, default=80)  # Warn at 80% utilization
     ```

  2. Add `pause_threshold_pct` column:
     ```python
     pause_threshold_pct = Column(Integer, default=90)  # Pause at 90% utilization
     ```

  3. Add `block_threshold_pct` column:
     ```python
     block_threshold_pct = Column(Integer, default=100)  # Block at 100% utilization
     ```

  4. Add check constraint if using PostgreSQL (document for SQLite):
     - Verify: warn_threshold_pct < pause_threshold_pct < block_threshold_pct
     - Application-level validation in BudgetGuardrails

  5. Add docstring explaining threshold behavior and defaults

  IMPORTANT: These are PER-PROJECT thresholds. Different projects can have different thresholds (e.g., critical project might warn at 50%).
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend grep -n "threshold_pct" backend/service_delivery/models.py`
  Expected: Shows 3 new threshold columns with default values
  </verify>
  <done>
  Project model has warn_threshold_pct, pause_threshold_pct, block_threshold_pct columns with defaults (80, 90, 100)
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance BudgetGuardrails with configurable thresholds</name>
  <files>
    backend/core/financial_ops_engine.py
  </files>
  <action>
  Enhance BudgetGuardrails class to support configurable thresholds:

  Modify `backend/core/financial_ops_engine.py`:
  1. Update BudgetLimit dataclass to include thresholds:
     ```python
     @dataclass
     class BudgetLimit:
         category: str
         monthly_limit: Decimal
         current_spend: Decimal = Decimal('0.00')
         deal_stage_required: Optional[str] = None
         milestone_required: Optional[str] = None
         warn_threshold_pct: int = 80  # NEW
         pause_threshold_pct: int = 90  # NEW
         block_threshold_pct: int = 100  # NEW
     ```

  2. Update BudgetGuardrails.check_spend() method to use configurable thresholds:
     - Use limit.warn_threshold_pct instead of hardcoded 80
     - Use limit.pause_threshold_pct instead of hardcoded 90
     - Use limit.block_threshold_pct instead of hardcoded 100
     - Calculate usage_pct = (current_spend / monthly_limit) * 100
     - Return appropriate SpendStatus based on which threshold crossed

  3. Add `get_threshold_status(self, limit: BudgetLimit) -> Dict[str, Any]` method:
     - Calculate current utilization percentage
     - Return: {"status": str, "usage_pct": Decimal, "next_threshold": str, "remaining_until_threshold": Decimal}

  4. Add `update_thresholds(self, category: str, warn: int = None, pause: int = None, block: int = None)` method:
     - Update threshold configuration for existing limit
     - Validate: warn < pause < block
     - Raise ValueError if invalid

  5. Add `reset_thresholds(self, category: str)` method:
     - Reset thresholds to defaults (80, 90, 100)

  6. Ensure all calculations use Decimal arithmetic (import to_decimal)

  IMPORTANT: Maintain backward compatibility - if thresholds not specified, use defaults.
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend python -c "from core.financial_ops_engine import BudgetLimit; b = BudgetLimit('test', 1000); print(b.warn_threshold_pct, b.pause_threshold_pct, b.block_threshold_pct)"`
  Expected: "80 90 100" (default thresholds)
  </verify>
  <done>
  BudgetLimit has configurable thresholds (warn, pause, block); BudgetGuardrails uses them in check_spend()
  </done>
</task>

<task type="auto">
  <name>Task 3: Create budget guardrail validation tests</name>
  <files>
    backend/tests/unit/budget/test_budget_guardrails.py
  </files>
  <action>
  Create comprehensive tests for budget guardrail validation:

  **backend/tests/unit/budget/test_budget_guardrails.py** (300 lines):
  - Import pytest, Decimal
  - Import BudgetGuardrails, BudgetLimit, SpendStatus from financial_ops_engine
  - Import money_strategy from decimal_fixtures

  Test class `TestDefaultThresholds`:
  1. `test_default_thresholds_80_90_100`: Verify defaults are 80%, 90%, 100%
  2. `test_default_warn_at_80_percent`: Spend crosses 80% → status=PENDING
  3. `test_default_pause_at_90_percent`: Spend crosses 90% → status=PAUSED
  4. `test_default_block_at_100_percent`: Spend crosses 100% → status=REJECTED

  Test class `TestConfigurableThresholds`:
  5. `test_custom_warn_threshold`: Set warn=70%, verify triggers at 70%
  6. `test_custom_pause_threshold`: Set pause=85%, verify triggers at 85%
  7. `test_custom_block_threshold`: Set block=95%, verify triggers at 95%
  8. `test_all_custom_thresholds`: Set 70/85/95, verify all trigger correctly
  9. `test_update_thresholds`: update_thresholds() modifies existing limit
  10. `test_reset_thresholds`: Reset restores defaults (80/90/100)

  Test class `TestThresholdValidation`:
  11. `test_thresholds_must_be_ordered`: warn < pause < block required
  12. `test_equal_thresholds_rejected`: warn=90, pause=90 raises error
  13. `test_inverted_thresholds_rejected`: warn=90, pause=80 raises error
  14. `test_negative_threshold_rejected`: Negative threshold raises error
  15. `test_threshold_over_100_rejected`: warn=110 raises error

  Test class `TestEnforcementActions`:
  16. `test_approved_status_below_warn`: Below warn threshold → status=APPROVED
  17. `test_pending_status_at_warn`: At warn threshold → status=PENDING
  18. `test_paused_status_at_pause`: At pause threshold → status=PAUSED
  19. `test_rejected_status_at_block`: At block threshold → status=REJECTED
  20. `test_over_budget_still_rejected`: Above 100% → status=REJECTED

  Test class `TestThresholdTransitions`:
  21. `test_transition_approved_to_pending`: Cross warn threshold
  22. `test_transition_pending_to_paused`: Cross pause threshold
  23. `test_transition_paused_to_rejected`: Cross block threshold
  24. `test_no_transition_below_warn`: Below all thresholds → approved
  25. `test_multiple_thresholds_crossed`: Large spend crossing all thresholds

  Test class `TestThresholdCalculations`:
  26. `test_utilization_percentage_calculation`: usage_pct = (burn / limit) * 100
  27. `test_remaining_until_threshold`: Calculate remaining until next threshold
  28. `test_threshold_boundary_exact`: Exact threshold value triggers correctly
  29. `test_threshold_boundary_one_cent`: One cent over threshold triggers
  30. `test_decimal_precision`: Threshold calculations use Decimal precision

  Test class `TestCategoryThresholds`:
  31. `test_different_categories_different_thresholds`: Each category can have different thresholds
  32. `test_category_independent_enforcement`: One category paused doesn't affect others
  33. `test_no_limit_means_no_enforcement`: Category without limit always approved

  Test class `TestThresholdStatusReporting`:
  34. `test_get_threshold_status`: Returns current status and next threshold
  35. `test_status_includes_remaining`: Report includes remaining until threshold
  36. `test_status_includes_usage_pct`: Report includes utilization percentage

  Use pytest fixtures for guardrail setup. Clean up test data in teardown.
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/budget/test_budget_guardrails.py -v`
  Expected: 36 tests pass, all threshold combinations validated
  </verify>
  <done>
  36 unit tests cover default thresholds, configurable thresholds, validation, enforcement actions, transitions, and calculations
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:
1. Project model has configurable threshold columns (warn, pause, block)
2. BudgetLimit dataclass includes threshold fields with defaults
3. BudgetGuardrails.check_spend() uses configurable thresholds
4. Threshold validation prevents invalid configurations (warn < pause < block)
5. Enforcement actions match status (approved, pending, paused, rejected)
</verification>

<success_criteria>
1. pytest tests/unit/budget/test_budget_guardrails.py passes (36 tests)
2. Default thresholds (80/90/100) work correctly
3. Custom thresholds are configurable per project
4. Threshold validation prevents invalid configurations
5. Enforcement actions (approve, warn, pause, block) match expected status
</success_criteria>

<output>
After completion, create `.planning/phases/093-cost-tracking-budgets/093-04-SUMMARY.md` with:
- Threshold configuration implementation
- Test coverage metrics (36 unit tests)
- Default and custom threshold validation
- Enforcement action verification
</output>
