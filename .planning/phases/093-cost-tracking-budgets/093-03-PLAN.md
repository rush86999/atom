---
phase: 093-cost-tracking-budgets
plan: 03
type: execute
wave: 2
depends_on: ["093-01", "093-02"]
files_modified:
  - backend/tests/property_tests/budget/test_cost_leak_invariants.py
  - backend/core/financial_ops_engine.py
autonomous: true

must_haves:
  truths:
    - All costs are categorized (property test validates no uncategorized)
    - Zombie subscriptions detected (unused for threshold days)
    - Redundant tools detected (multiple tools in same category)
    - Sum of detected savings matches expected (calculation invariant)
    - Cost leak detection is deterministic (same input = same output)
  artifacts:
    - path: "backend/tests/property_tests/budget/test_cost_leak_invariants.py"
      provides: "Hypothesis property tests for cost leak detection"
      min_lines: 250
    - path: "backend/core/financial_ops_engine.py"
      provides: "Enhanced CostLeakDetector with additional validation"
      contains: "class CostLeakDetector"
  key_links:
    - from: "backend/tests/property_tests/budget/test_cost_leak_invariants.py"
      to: "backend/core/financial_ops_engine.py"
      via: "CostLeakDetector and SaaSSubscription imports"
      pattern: "from core.financial_ops_engine import CostLeakDetector"
    - from: "backend/tests/property_tests/budget/test_cost_leak_invariants.py"
      to: "backend/tests/fixtures/decimal_fixtures.py"
      via: "Decimal strategies for cost calculations"
      pattern: "from tests.fixtures.decimal_fixtures import"
    - from: "backend/core/financial_ops_engine.py"
      to: "backend/core/decimal_utils.py"
      via: "Decimal arithmetic for cost calculations"
      pattern: "from core.decimal_utils import to_decimal"
---

<objective>
Property tests for cost leak detection validate invariants that prevent unexpected spend, uncategorized costs, and zombie subscriptions from draining budgets unnoticed.

Purpose: Cost leaks are silent budget killers. SaaS subscriptions go unused, redundant tools pile up, and costs slip through uncategorized. Property-based testing with Hypothesis generates hundreds of scenarios to validate cost leak detection invariants: all costs are categorized, unused subscriptions are detected, redundant tools are flagged, and savings calculations are accurate.

Output: Enhanced CostLeakDetector with validation methods, comprehensive Hypothesis property tests, and validated invariants for cost leak detection across 1000+ examples.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/templates/summary.md
</execution_context>

<context>
@.planning/phases/093-cost-tracking-budgets/093-01-SUMMARY.md
@.planning/phases/093-cost-tracking-budgets/093-02-SUMMARY.md
@.planning/phases/093-cost-tracking-budgets/093-RESEARCH.md
@backend/core/financial_ops_engine.py
@backend/core/decimal_utils.py
@backend/tests/fixtures/decimal_fixtures.py
@backend/tests/property_tests/financial/test_financial_invariants.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance CostLeakDetector with validation methods</name>
  <files>
    backend/core/financial_ops_engine.py
  </files>
  <action>
  Enhance existing CostLeakDetector with additional validation methods:

  Modify `backend/core/financial_ops_engine.py`:
  1. Add `validate_categorization(self) -> Dict[str, Any]` method to CostLeakDetector:
     - Check all subscriptions have valid category
     - Return: {"valid": bool, "uncategorized": List[str], "invalid": List[str]}

  2. Add `get_subscription_by_id(self, sub_id: str) -> SaaSSubscription` method:
     - Return subscription or None if not found
     - Used for property test validation

  3. Add `calculate_total_cost(self) -> Decimal` method:
     - Sum all subscription monthly costs
     - Use Decimal arithmetic with to_decimal()
     - Return total monthly cost

  4. Add `verify_savings_calculation(self) -> Dict[str, Any]` method:
     - Recalculate savings from detected unused subscriptions
     - Compare with get_savings_report() result
     - Return: {"match": bool, "expected": Decimal, "actual": Decimal, "diff": Decimal}

  5. Add `detect_anomalies(self) -> List[Dict[str, Any]]` method:
     - Detect unusual cost patterns (sudden spikes, zero user costs, etc.)
     - Return list of anomalies found

  6. Ensure all methods use Decimal arithmetic (import to_decimal)
  7. Add type hints for all methods

  IMPORTANT: Do not change existing detect_unused(), detect_redundant(), get_savings_report() methods - only add new validation methods.
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend python -c "from core.financial_ops_engine import CostLeakDetector; d = CostLeakDetector(); print(dir(d))" | grep validate`
  Expected: Shows validate_categorization, calculate_total_cost, verify_savings_calculation methods
  </verify>
  <done>
  CostLeakDetector enhanced with 5 validation methods; all existing functionality preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Create property tests for cost leak detection invariants</name>
  <files>
    backend/tests/property_tests/budget/test_cost_leak_invariants.py
  </files>
  <action>
  Create Hypothesis property tests for cost leak detection:

  **backend/tests/property_tests/budget/test_cost_leak_invariants.py** (300 lines):
  - Import pytest, hypothesis (given, strategies as st, settings, assume, example)
  - Import datetime, timedelta
  - Import Decimal
  - Import CostLeakDetector, SaaSSubscription from financial_ops_engine
  - Import to_decimal from decimal_utils
  - Import money_strategy, lists_of_decimals from decimal_fixtures

  Test class `TestCategorizationInvariants`:
  1. `@given(sub_count=st.integers(min_value=1, max_value=50), categories=st.lists(st.text(min_size=3, max_size=20), min_size=1, max_size=10, unique=True))`
     `test_all_subscriptions_categorized`: All subscriptions must have valid categories

  2. `@given(costs=lists_of_decimals('10', '1000', min_size=5, max_size=30))`
     `test_categorized_total_equals_uncategorized`: Sum of categorized = total (even with mix)

  3. `@given(sub_count=st.integers(min_value=1, max_value=20))`
     `test_no_empty_categories_in_valid_subscriptions`: Valid subscriptions have non-empty category strings

  Test class `TestUnusedSubscriptionInvariants`:
  4. `@given(sub_count=st.integers(min_value=1, max_value=30), threshold_days=st.integers(min_value=7, max_value=90))`
     `test_unused_detection_correct`: Unused subscriptions correctly identified (last_used < threshold)

  5. `@given(sub_count=st.integers(min_value=1, max_value=30))`
     `test_used_subscriptions_not_flagged`: Recently used subscriptions not in unused list

  6. `@given(monthly_costs=lists_of_decimals('10', '500', min_size=1, max_size=20), unused_days=st.integers(min_value=1, max_value=365))`
     `test_unused_savings_calculation`: Potential savings = sum of unused subscription costs

  7. `@given(threshold_days=st.integers(min_value=1, max_value=365))`
     `test_threshold_boundary`: Subscription exactly at threshold boundary detected correctly

  Test class `TestRedundantToolInvariants`:
  8. `@given(categories=st.lists(st.text(min_size=3, max_size=15), min_size=2, max_size=10, unique=True), tools_per_cat=st.integers(min_value=2, max_value=5))`
     `test_redundant_detection_correct`: Multiple tools in same category flagged as redundant

  9. `@given(categories=st.lists(st.text(min_size=3, max_size=15), min_size=1, max_size=5, unique=True))`
     `test_single_tool_not_redundant`: Single tool per category not flagged

  10. `@given(costs=lists_of_decimals('10', '200', min_size=2, max_size=10))`
     `test_redundant_cost_aggregation`: Total redundant cost = sum of all redundant tool costs

  Test class `TestSavingsCalculationInvariants`:
  11. `@given(unused_costs=lists_of_decimals('10', '1000', min_size=1, max_size=20))`
     `test_monthly_savings_invariant`: Monthly savings = sum of unused subscription costs

  12. `@given(unused_costs=lists_of_decimals('10', '1000', min_size=1, max_size=20))`
     `test_annual_savings_invariant`: Annual savings = monthly savings * 12

  13. `@given(mixed_costs=lists_of_decimals('10', '500', min_size=5, max_size=30))`
     `test_savings_sum_associative`: Order of adding costs doesn't affect total

  14. `@given(monthly_savings=money_strategy('100', '10000'))`
     `test_annual_projection_exact`: Annual projection has no rounding error

  Test class `TestDeterminismInvariants`:
  15. `@given(subscriptions=st.lists(st.builds(SaaSSubscription, ...), min_size=5, max_size=20))`
     `test_detection_is_deterministic`: Same input produces same output (run detection twice, compare results)

  16. `@given(sub_count=st.integers(min_value=1, max_value=20))`
     `test_report_consistency`: get_savings_report() returns consistent data across calls

  Test class `TestEdgeCases`:
  17. `test_empty_detector`: Empty detector (no subscriptions) returns empty lists and zero savings

  18. `test_all_subscriptions_unused`: All subscriptions unused → 100% flagged

  19. `test_no_subscriptions_unused`: All subscriptions used → empty unused list

  20. `@example(multiple_categories_same_tools=True)`
     `test_cross_category_redundancy`: Tools in different categories not flagged redundant

  Use @settings(max_examples=100) for thorough invariant validation. Use @example() for specific edge cases.
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/property_tests/budget/test_cost_leak_invariants.py -v`
  Expected: 20 property tests pass, 2000+ examples generated, no counterexamples
  </verify>
  <done>
  20 Hypothesis property tests validate cost leak detection invariants (categorization, unused detection, redundancy, savings calculation)
  </done>
</task>

<task type="auto">
  <name>Task 3: Add integration test for zombie subscription detection</name>
  <files>
    backend/tests/integration/test_zombie_subscription_detection.py
  </files>
  <action>
  Create integration test for zombie subscription (unused service) detection:

  **backend/tests/integration/test_zombie_subscription_detection.py** (150 lines):
  - Import pytest, datetime, timedelta
  - Import CostLeakDetector, SaaSSubscription
  - Import money_strategy from decimal_fixtures

  Test class `TestZombieSubscriptionDetection`:
  1. `test_zombie_subscription_30_days_unused`: Subscription unused for 30 days detected
  2. `test_zombie_subscription_90_days_unused`: Subscription unused for 90 days detected with higher priority
  3. `test_zombie_subscription_zero_active_users`: 0 active users flagged even if recently accessed
  4. `test_zombie_subscription_cost_weighting`: Higher cost zombie subscriptions prioritized in report
  5. `test_zombie_subscription_threshold_configurable`: Different thresholds (30, 60, 90 days) work correctly
  6. `test_active_subscription_not_flagged`: Subscription with active users not flagged as zombie
  7. `test_recently_used_subscription_not_flagged`: Subscription used yesterday not flagged
  8. `test_zombie_detection_with_mixed_usage`: Mix of zombie and active subscriptions detected correctly
  9. `test_zombie_savings_calculation`: Savings from canceling zombies calculated correctly
  10. `test_zombie_recommendation`: Zombie subscriptions have "cancel or review" recommendation

  Test class `TestZombieSubscriptionRecovery`:
  11. `test_zombie_becomes_active`: Subscription used again exits zombie list
  12. `test_zombie_recovery_tracked`: Recovery (reactivation) is trackable

  Use pytest fixtures for detector setup. Clean up test data in teardown.
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/integration/test_zombie_subscription_detection.py -v`
  Expected: 12 integration tests pass, zombie detection works correctly
  </verify>
  <done>
  12 integration tests validate zombie subscription detection, threshold behavior, and recovery tracking
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:
1. CostLeakDetector has validation methods for categorization and savings
2. Property tests validate all costs are categorized invariant
3. Property tests validate unused subscription detection across 2000+ examples
4. Property tests validate savings calculation invariants (monthly, annual)
5. Integration tests validate zombie subscription detection scenarios
</verification>

<success_criteria>
1. pytest tests/property_tests/budget/test_cost_leak_invariants.py passes (20 tests)
2. pytest tests/integration/test_zombie_subscription_detection.py passes (12 tests)
3. All cost categorization invariants hold (no uncategorized costs)
4. Savings calculations are exact (no rounding errors)
5. Detection is deterministic (same input = same output)
</success_criteria>

<output>
After completion, create `.planning/phases/093-cost-tracking-budgets/093-03-SUMMARY.md` with:
- CostLeakDetector enhancements
- Property test results (20 tests, 2000+ examples)
- Integration test results (12 zombie detection tests)
- Cost leak invariants validated
</output>
