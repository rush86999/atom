---
phase: 163-coverage-baseline-infrastructure
plan: "03"
type: auto
wave: 2
depends_on:
  - "163-01"
  - "163-02"
files_modified:
  - backend/docs/METHODOLOGY.md
  - backend/docs/COVERAGE_GUIDE.md
  - .planning/phases/163-coverage-baseline-infrastructure/163-VERIFICATION.md
autonomous: true

must_haves:
  truths:
    - "Team can read METHODOLOGY.md and understand the service-level estimation pitfall"
    - "Team can follow checklist in METHODOLOGY.md to measure actual line coverage"
    - "Documentation examples match actual script usage (generate_baseline_coverage_report.py, backend_coverage_gate.py)"
    - "Phase 163 verification report confirms all 3 requirements (COV-01, COV-02, COV-03) are satisfied"
  artifacts:
    - path: "backend/docs/METHODOLOGY.md"
      provides: "coverage methodology documentation explaining actual vs estimated coverage pitfall"
      min_lines: 150
    - path: "backend/docs/COVERAGE_GUIDE.md"
      provides: "updated coverage guide with actual line coverage methodology"
      contains: "CRITICAL: Use Actual Line Coverage, Not Estimates"
    - path: ".planning/phases/163-coverage-baseline-infrastructure/163-VERIFICATION.md"
      provides: "Phase 163 verification report with pass/fail for all success criteria"
      min_lines: 100
  key_links:
    - from: "backend/docs/METHODOLOGY.md"
      to: "backend/tests/scripts/generate_baseline_coverage_report.py"
      via: "documents how to use the baseline generation script"
    - from: "backend/docs/METHODOLOGY.md"
      to: "backend/tests/scripts/backend_coverage_gate.py"
      via: "documents how to use the quality gate script"
    - from: "backend/docs/COVERAGE_GUIDE.md"
      to: "backend/tests/coverage_reports/backend_163_baseline.json"
      via: "references baseline coverage.json format for parsing examples"
    - from: ".planning/phases/163-coverage-baseline-infrastructure/163-VERIFICATION.md"
      to: "backend/docs/METHODOLOGY.md"
      via: "verifies methodology documentation prevents recurrence of service-level estimation errors"
---

# Plan 163-03: Methodology Documentation and Phase Verification

## Objective
Document the actual coverage methodology (vs service-level estimates) and verify Phase 163 success criteria are met.

**Purpose:** Prevent recurrence of service-level estimation errors by documenting correct methodology and verifying phase completion.

**Output:** METHODOLOGY.md explaining the pitfall, updated COVERAGE_GUIDE.md, and verification report confirming Phase 163 success.

## Tasks

<task id="1" type="auto">
  <name>Create METHODOLOGY.md documenting actual vs estimated coverage</name>
  <files>
    - backend/docs/METHODOLOGY.md
  </files>
  <action>
    Create backend/docs/METHODOLOGY.md documenting coverage methodology:
    - Document the service-level estimation pitfall discovered in Phases 160-162:
      - Episode services estimated at 74.6% but actual was 8.50% (66.1 percentage point gap)
      - Root cause: Aggregating service-level boolean coverage instead of actual line execution
    - Explain the correct methodology:
      - Use `pytest --cov=backend --cov-branch --cov-report=json` for actual line coverage
      - Parse coverage.json for actual line execution data: `totals['line_covered'] / totals['num_statements']`
      - Verify coverage.json has `files` array with per-file breakdown (not just high-level totals)
    - Document how to read coverage.py JSON output:
      - `files` array: per-file coverage with `summary.covered` and `summary.num_statements`
      - `totals` object: overall line and branch coverage metrics
      - Branch coverage: `totals['branch_covered'] / totals['num_branches']`
    - Document the difference between service-level aggregation and actual line execution
    - Include examples from Atom's coverage journey:
      - Episode services: 74.6% estimated vs 8.50% actual
      - Backend overall: document the gap when using service-level vs line-level
    - Provide checklist for proper coverage measurement:
      - [ ] coverage.json exists and contains `files` array
      - [ ] Each file has `summary` with `covered` and `num_statements` keys
      - [ ] Coverage percentage = covered / num_statements (not aggregation of service booleans)
      - [ ] Branch coverage included if `--cov-branch` was used
    - Document progressive rollout strategy (70% → 75% → 80%)
    - Reference actual scripts: generate_baseline_coverage_report.py, backend_coverage_gate.py
  </action>
  <verify>
    Run: `grep -E "(service-level|actual line|coverage.json|checklist)" backend/docs/METHODOLOGY.md | head -10`
    Expected: Output shows pitfall documented, correct methodology explained, checklist present
  </verify>
  <done>
    METHODOLOGY.md created with clear explanation of service-level estimation pitfall; correct methodology documented with coverage.py execution data; checklist included
  </done>
</task>

<task id="2" type="auto">
  <name>Update COVERAGE_GUIDE.md with methodology clarification</name>
  <files>
    - backend/docs/COVERAGE_GUIDE.md
  </files>
  <action>
    Update backend/docs/COVERAGE_GUIDE.md with methodology corrections:
    - Add section: "CRITICAL: Use Actual Line Coverage, Not Estimates" at the top of the guide
    - Document how to generate coverage.json:
      - Command: `pytest --cov=backend --cov-branch --cov-report=json`
      - Output file: `coverage.json` with `files` array and `totals` object
    - Document how to parse coverage.json for actual line and branch metrics:
      ```python
      import json
      with open('coverage.json') as f:
          cov = json.load(f)
      line_cov = cov['totals']['line_covered'] / cov['totals']['num_statements']
      branch_cov = cov['totals']['branch_covered'] / cov['totals']['num_branches']
      ```
    - Update all examples to use coverage.py JSON output (remove service-level aggregation examples)
    - Add troubleshooting section: "Why Coverage Seems Higher Than It Is"
      - Explain service-level estimation pitfall
      - How to verify: check for `files` array in coverage.json
      - Correct command to run
    - Document integration with CI/CD and quality gates:
      - backend_coverage_gate.py usage
      - Emergency bypass with emergency_coverage_bypass.py
    - Document progressive threshold enforcement (70% → 75% → 80%)
  </action>
  <verify>
    Run: `grep -A 3 "CRITICAL: Use Actual" backend/docs/COVERAGE_GUIDE.md`
    Expected: Shows critical warning section at top of guide
  </verify>
  <done>
    COVERAGE_GUIDE.md updated with methodology clarification; all examples use coverage.py JSON output; service-level estimation examples removed; troubleshooting section added
  </done>
</task>

<task id="3" type="auto">
  <name>Verify documentation examples match actual script usage</name>
  <files>
    - backend/docs/METHODOLOGY.md
    - backend/docs/COVERAGE_GUIDE.md
    - backend/tests/scripts/generate_baseline_coverage_report.py
    - backend/tests/scripts/backend_coverage_gate.py
  </files>
  <action>
    Verify documentation examples match actual script usage:
    - Check METHODOLOGY.md script examples match generate_baseline_coverage_report.py usage:
      - Script path: `backend/tests/scripts/generate_baseline_coverage_report.py`
      - Command: `python backend/tests/scripts/generate_baseline_coverage_report.py`
    - Check METHODOLOGY.md quality gate examples match backend_coverage_gate.py usage:
      - Script path: `backend/tests/scripts/backend_coverage_gate.py`
      - Command: `python backend/tests/scripts/backend_coverage_gate.py`
    - Check COVERAGE_GUIDE.md coverage.json parsing examples match actual coverage.json structure:
      - Verify keys referenced in docs (`files`, `totals`, `line_covered`, `num_statements`) exist in actual coverage.json
      - Verify example code produces correct results when run against actual coverage.json
    - Fix any mismatches between documentation and actual script usage
  </action>
  <verify>
    Run: `python -c "exec(open('backend/docs/COVERAGE_GUIDE.md').read().split('```python')[1].split('```')[0])"` (if example code exists)
    Expected: Example code runs without errors against actual coverage.json
  </verify>
  <done>
    Documentation examples verified against actual script usage; script paths correct; command examples work; coverage.json parsing examples match actual structure
  </done>
</task>

<task id="4" type="auto">
  <name>Create Phase 163 verification report</name>
  <files>
    - .planning/phases/163-coverage-baseline-infrastructure/163-VERIFICATION.md
  </files>
  <action>
    Create .planning/phases/163-coverage-baseline-infrastructure/163-VERIFICATION.md:
    - Verify COV-01: Team can measure actual line coverage using coverage.py JSON output
      - Check coverage.json exists at backend/tests/coverage_reports/backend_163_baseline.json
      - Verify coverage.json contains `files` array with per-file coverage
      - Verify coverage.json contains actual line coverage (line_covered / num_statements)
      - Result: PASS/FAIL
    - Verify COV-02: Team can measure branch coverage with `--cov-branch` flag
      - Check pytest.ini has `--cov-branch` enabled
      - Verify coverage.json contains branch coverage metrics (branch_covered, num_branches)
      - Result: PASS/FAIL
    - Verify COV-03: Team can enforce progressive thresholds via quality gates with emergency bypass
      - Verify progressive_coverage_gate.py works (70% → 75% → 80% thresholds)
      - Verify emergency_coverage_bypass.py works with justification logging
      - Verify backend_coverage_gate.py is ready for CI/CD integration
      - Verify .github/workflows/ci.yml calls backend_coverage_gate.py
      - Result: PASS/FAIL
    - Verify baseline is accurate and prevents false confidence from service-level estimates
      - Review baseline report at backend_tests/coverage_reports/backend_163_baseline.md
      - Verify methodology documentation exists (METHODOLOGY.md)
      - Verify no service-level aggregation in baseline calculation
      - Result: PASS/FAIL
    - Document verification results with pass/fail for each criterion
    - Document baseline metrics: line coverage %, branch coverage %, total lines, covered lines
    - Create summary of Phase 163 outcomes
  </action>
  <verify>
    Run: `grep -E "(PASS|FAIL|COV-01|COV-02|COV-03)" .planning/phases/163-coverage-baseline-infrastructure/163-VERIFICATION.md`
    Expected: Shows pass/fail status for all 3 requirements
  </verify>
  <done>
    All 3 requirements verified; COV-01, COV-02, COV-03 status documented; baseline metrics recorded; verification report created with pass/fail results
  </done>
</task>

## Verification Criteria

1. **COV-01 satisfied**: Team can measure actual line coverage using coverage.py JSON output with per-file breakdown (not service-level estimates)
2. **COV-02 satisfied**: Team can measure branch coverage with `--cov-branch` flag enabled in pytest configuration
3. **COV-03 satisfied**: Team can enforce progressive coverage thresholds (70% → 75% → 80%) via quality gates with emergency bypass mechanism integrated in CI/CD
4. **Documentation accurate**: METHODOLOGY.md and COVERAGE_GUIDE.md examples match actual script usage
5. **Phase complete**: Phase 163 ready to hand off to Phase 164 (Gap Analysis & Prioritization)

## Success Criteria

- [ ] METHODOLOGY.md documents service-level estimation pitfall with Atom examples
- [ ] METHODOLOGY.md provides checklist for proper coverage measurement
- [ ] COVERAGE_GUIDE.md has critical warning section at top
- [ ] Documentation examples verified against actual scripts (generate_baseline_coverage_report.py, backend_coverage_gate.py)
- [ ] 163-VERIFICATION.md created with pass/fail for COV-01, COV-02, COV-03
- [ ] Baseline metrics documented (line coverage %, branch coverage %)

## Requirements Addressed

- **COV-01**: Team can measure actual line coverage using coverage.py JSON output (with per-file validation)
- **COV-02**: Team can measure branch coverage with `--cov-branch` flag enabled in pytest configuration
- **COV-03**: Team can enforce progressive coverage thresholds (70% → 75% → 80%) via quality gates with emergency bypass mechanism
