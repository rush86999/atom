---
phase: 116-student-training-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/agent/test_student_training_service.py
autonomous: true

must_haves:
  truths:
    - "All 6 failing tests in test_student_training_service.py now pass"
    - "Tests correctly instantiate models with required fields"
    - "Training proposal and session creation works end-to-end"
    - "Agent confidence boosts and promotion logic tested"
  artifacts:
    - path: "backend/tests/unit/agent/test_student_training_service.py"
      provides: "Passing tests for StudentTrainingService"
      min_lines: 350
  key_links:
    - from: "backend/tests/unit/agent/test_student_training_service.py"
      to: "backend/core/models.py"
      via: "AgentProposal and TrainingSession model instantiation"
      pattern: "(AgentProposal|TrainingSession)\\("
---

<objective>
Fix 6 failing tests in test_student_training_service.py to unblock coverage measurement.

**Purpose:** Research shows 6 out of 10 tests are failing in test_student_training_service.py. These failures prevent accurate coverage measurement. Fixing these tests will enable coverage assessment and likely improve coverage significantly.

**Output:** 10/10 tests passing, ready for coverage measurement in Plan 02
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-student-training-coverage/116-RESEARCH.md

# Proven patterns from Phase 112
@.planning/phases/112-agent-governance-coverage/112-01-SUMMARY.md

# Source files
@backend/core/models.py (AgentProposal, TrainingSession models)
@backend/tests/unit/agent/test_student_training_service.py (failing tests)
@backend/core/student_training_service.py
</context>

<tasks>

<task type="auto">
  <name>Run failing tests with verbose output to identify root cause</name>
  <files>backend/tests/unit/agent/test_student_training_service.py</files>
  <action>
    Run the student training service tests with verbose output to identify the exact failure reasons. Research indicates 6 failing tests, but root cause is unknown.

    Command:
    ```bash
    cd backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
      tests/unit/agent/test_student_training_service.py \
      -v \
      --tb=short
    ```

    Expected findings (based on Phase 112 patterns):
    - Model field mismatches (missing required fields)
    - Missing database commits before assertions
    - Incorrect fixture setup
    - Missing async decorators

    Document each failing test with:
    1. Test method name
    2. Error message
    3. Root cause (model mismatch, missing field, etc.)
  </action>
  <verify>All 6 test failures documented with error messages and root causes</verify>
  <done>Have clear list of fixes needed for each failing test</done>
</task>

<task type="auto">
  <name>Fix model instantiation issues in test_student_training_service.py</name>
  <files>backend/tests/unit/agent/test_student_training_service.py</files>
  <action>
    Fix identified model instantiation issues based on Task 1 findings.

    Common patterns from Phase 112:
    1. Missing required fields in AgentProposal or TrainingSession
    2. Using incorrect field names (e.g., workspace_id in ChatSession)
    3. Missing database commits before reading relationships

    For each failing test:
    1. Identify the model instantiation causing the error
    2. Add missing required fields based on model definition in models.py
    3. Ensure db_session.commit() is called before assertions
    4. Verify @pytest.mark.asyncio decorator is present

    Common required fields to check:
    - AgentProposal: id, agent_id, agent_name, proposal_type, status, learning_objectives, capability_gaps, estimated_duration_hours
    - TrainingSession: id, proposal_id, agent_id, agent_name, status, supervisor_id, started_at

    DO NOT modify:
    - Test class names
    - Test method names
    - Test assertions (unless clearly wrong)
    - Test logic/flow
  </action>
  <verify>
    cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/agent/test_student_training_service.py -v
  </verify>
  <done>
    All 10 tests pass (currently 4 passing, 6 failing)
    No TypeError or AttributeError messages
  </done>
</task>

<task type="auto">
  <name>Verify test fixes enable accurate coverage measurement</name>
  <files>backend/tests/unit/agent/test_student_training_service.py</files>
  <action>
    Run a quick coverage check on student_training_service.py to verify the fixes allow accurate measurement. This is NOT the final coverage report (that's Plan 02), just a sanity check.

    Command:
    ```bash
    cd backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
      tests/unit/agent/test_student_training_service.py \
      --cov=core.student_training_service \
      --cov-report=term-missing
    ```

    This should now run without errors and produce a coverage report. We're NOT targeting 60% yet - just verifying coverage measurement works.
  </action>
  <verify>Coverage report generates without errors</verify>
  <done>
    Coverage report shows baseline percentage for student_training_service.py
    Ready for Plan 02 combined coverage measurement
  </done>
</task>

</tasks>

<verification>
1. All 10 tests in test_student_training_service.py pass
2. No TypeError, AttributeError, or SQLAlchemy errors in test output
3. Coverage measurement runs successfully for student_training_service.py
4. Tests create valid AgentProposal and TrainingSession objects
</verification>

<success_criteria>
1. 10/10 tests passing in test_student_training_service.py
2. Coverage measurement works (no errors during --cov execution)
3. Test failures documented and root causes fixed
4. Models instantiated with correct required fields
</success_criteria>

<output>
After completion, create `.planning/phases/116-student-training-coverage/116-01-SUMMARY.md`
</output>
