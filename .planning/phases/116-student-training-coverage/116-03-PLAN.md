---
phase: 116-student-training-coverage
plan: 03
type: execute
wave: 3
depends_on: [116-02]
files_modified:
  - backend/tests/unit/agent/test_student_training_service.py
  - backend/tests/unit/governance/test_supervision_service.py
autonomous: true

must_haves:
  truths:
    - "All three services achieve 60%+ coverage"
    - "student_training_service.py coverage >= 60%"
    - "supervision_service.py coverage >= 60%"
    - "trigger_interceptor.py maintains 96% coverage"
    - "Combined test run passes with all tests green"
  artifacts:
    - path: "backend/tests/unit/agent/test_student_training_service.py"
      provides: "Complete test coverage for StudentTrainingService"
      min_lines: 400
    - path: "backend/tests/unit/governance/test_supervision_service.py"
      provides: "Complete test coverage for SupervisionService"
      min_lines: 600
  key_links:
    - from: "backend/tests/unit/agent/test_student_training_service.py"
      to: "backend/core/student_training_service.py"
      via: "Test methods covering previously-uncovered functions"
      pattern: "async def test_.*"
    - from: "backend/tests/unit/governance/test_supervision_service.py"
      to: "backend/core/supervision_service.py"
      via: "Test methods covering previously-uncovered functions"
      pattern: "async def test_.*"
---

<objective>
Add targeted tests to achieve 60%+ coverage for student_training_service.py and supervision_service.py.

**Purpose:** Based on coverage gaps identified in Plan 02, add focused tests to reach 60% coverage for the two services that need it. trigger_interceptor.py is already at 96% and requires no additional work.

**Output:** All three services at 60%+ coverage, Phase 116 success criteria met
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-student-training-coverage/116-RESEARCH.md
@.planning/phases/116-student-training-coverage/116-01-SUMMARY.md
@.planning/phases/116-student-training-coverage/116-02-SUMMARY.md

# Proven patterns from Phase 112
@.planning/phases/112-agent-governance-coverage/112-03-SUMMARY.md

# Source files
@backend/core/trigger_interceptor.py (96% coverage, no work needed)
@backend/core/student_training_service.py
@backend/core/supervision_service.py
@backend/tests/unit/agent/test_student_training_service.py
@backend/tests/unit/governance/test_supervision_service.py
@backend/tests/coverage_reports/phase_116_coverage_analysis.md
</context>

<tasks>

<task type="auto">
  <name>Add targeted tests for student_training_service.py gaps</name>
  <files>backend/tests/unit/agent/test_student_training_service.py</files>
  <action>
    Based on the coverage analysis from Plan 02, add tests to cover missing lines in student_training_service.py.

    Follow these patterns from existing tests:
    1. Use `@pytest.mark.asyncio` decorator for async tests
    2. Use `StudentAgentFactory` for agent creation
    3. Use `db_session` for real database operations
    4. Create models with all required fields

    Common gaps to address (examples from research):
    - Training session completion with various outcomes
    - Confidence boost calculation edge cases
    - Training history pagination and filtering
    - Capability gap identification for different trigger types
    - Learning objectives generation variations

    Add tests at the END of the file (after line 374) using this structure:

    ```python
    class TestStudentTrainingCoverageGaps:
        """Additional tests to reach 60% coverage"""

        @pytest.mark.asyncio
        async def test_[specific_uncovered_behavior](self, training_service, db_session):
            """
            Test [description of uncovered behavior].
            Covers lines [XX-YY] in student_training_service.py
            """
            # Arrange
            agent = StudentAgentFactory(_session=db_session)
            # ... setup for uncovered code path ...

            # Act
            result = await service.[uncovered_method](...)

            # Assert
            assert result["expected_field"] == expected_value
    ```

    Add tests for:
    1. Each uncovered function
    2. Edge cases in partially-covered functions
    3. Error handling paths
  </action>
  <verify>
    cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/agent/test_student_training_service.py -v
  </verify>
  <done>
    New tests pass
    Coverage for student_training_service.py increases toward 60%
  </done>
</task>

<task type="auto">
  <name>Add targeted tests for supervision_service.py gaps</name>
  <files>backend/tests/unit/governance/test_supervision_service.py</files>
  <action>
    Based on the coverage analysis from Plan 02, add tests to cover missing lines in supervision_service.py.

    Follow these patterns from existing tests:
    1. Use `@pytest.mark.asyncio` decorator for async tests
    2. Create AgentRegistry directly or via factory
    3. Use `db_session` for real database operations
    4. Test session lifecycle: create -> intervene -> complete

    Common gaps to address (examples from research):
    - Episode creation from supervision sessions (lines 383-409)
    - Real-time event streaming
    - Active session filtering by workspace
    - Supervision history with pagination
    - Different intervention scenarios

    Add tests at the END of the file (after line 561) using this structure:

    ```python
    class TestSupervisionCoverageGaps:
        """Additional tests to reach 60% coverage"""

        @pytest.mark.asyncio
        async def test_[specific_uncovered_behavior](self, db_session: Session):
            """
            Test [description of uncovered behavior].
            Covers lines [XX-YY] in supervision_service.py
            """
            # Arrange
            agent = AgentRegistry(...)
            db_session.add(agent)
            db_session.commit()

            service = SupervisionService(db_session)

            # Act - call uncovered code path
            result = await service.[uncovered_method](...)

            # Assert
            assert result.expected_field == expected_value
    ```

    Add tests for:
    1. Each uncovered function
    2. Edge cases in partially-covered functions
    3. Error handling paths
    4. Episode integration if uncovered
  </action>
  <verify>
    cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/governance/test_supervision_service.py -v
  </verify>
  <done>
    New tests pass
    Coverage for supervision_service.py increases toward 60%
  </done>
</task>

<task type="auto">
  <name>Run final coverage verification for all three services</name>
  <files>backend/tests/coverage_reports/metrics/phase_116_coverage_final.json</files>
  <action>
    Run the final combined coverage measurement to verify all three services achieve 60%+ coverage.

    Command:
    ```bash
    cd backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
      tests/unit/governance/test_trigger_interceptor.py \
      tests/unit/agent/test_student_training_service.py \
      tests/unit/governance/test_supervision_service.py \
      --cov=core.trigger_interceptor \
      --cov=core.student_training_service \
      --cov=core.supervision_service \
      --cov-report=term-missing \
      --cov-report=json:tests/coverage_reports/metrics/phase_116_coverage_final.json \
      --cov-report=html:tests/coverage_reports/html/phase_116_final \
      -v
    ```

    Verify success criteria:
    1. trigger_interceptor.py >= 96% (maintains existing coverage)
    2. student_training_service.py >= 60%
    3. supervision_service.py >= 60%
    4. All tests pass (no failures)
  </action>
  <verify>
    Coverage report shows all three services >= 60%
    All tests pass with no failures
  </verify>
  <done>
    Phase 116 success criteria met:
    - trigger_interceptor.py: 96%+ coverage
    - student_training_service.py: 60%+ coverage
    - supervision_service.py: 60%+ coverage
    - STUDENT agent blocking, training proposals, supervision tracking tested
  </done>
</task>

</tasks>

<verification>
1. All new tests added in Task 1 and Task 2 pass
2. Coverage report shows student_training_service.py >= 60%
3. Coverage report shows supervision_service.py >= 60%
4. Coverage report shows trigger_interceptor.py >= 96%
5. No test failures in combined run
6. STUDENT agent blocking tested (test_student_blocked_from_automated_triggers)
7. Training proposal generation tested (test_training_proposal_creation)
8. Supervision tracking tested (test_supervision_session_creates_session)
</verification>

<success_criteria>
1. All three services achieve 60%+ coverage
2. Combined test run passes with 0 failures
3. Coverage baseline and final reports documented
4. Phase 116 ready for verification
</success_criteria>

<output>
After completion, create `.planning/phases/116-student-training-coverage/116-03-SUMMARY.md`
</output>
