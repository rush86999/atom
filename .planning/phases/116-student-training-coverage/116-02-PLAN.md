---
phase: 116-student-training-coverage
plan: 02
type: execute
wave: 2
depends_on: [116-01]
files_modified:
  - backend/tests/coverage_reports/metrics/phase_116_coverage_baseline.json
autonomous: true

must_haves:
  truths:
    - "Coverage baseline measured for all three student training services"
    - "trigger_interceptor.py confirmed at 96% coverage (exceeds target)"
    - "student_training_service.py baseline coverage documented"
    - "supervision_service.py baseline coverage documented"
    - "Missing coverage lines identified for gap-filling in Plan 03"
  artifacts:
    - path: "backend/tests/coverage_reports/metrics/phase_116_coverage_baseline.json"
      provides: "Baseline coverage metrics for all three services"
      contains: "coverage_percentages, missing_lines, test_counts"
  key_links:
    - from: "backend/tests/coverage_reports/metrics/phase_116_coverage_baseline.json"
      to: ".planning/phases/116-student-training-coverage/116-03-PLAN.md"
      via: "Missing lines inform gap-filling test additions"
      pattern: "missing_lines.*array"
---

<objective>
Measure coverage baseline for all three student training services and identify gaps.

**Purpose:** Establish accurate coverage baseline after fixing failing tests in Plan 01. Research shows trigger_interceptor.py is at 96% (exceeds target), but student_training_service.py and supervision_service.py coverage is unknown. This plan measures all three services combined and identifies specific lines needing coverage.

**Output:** Coverage baseline report with missing lines for gap-filling in Plan 03
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-student-training-coverage/116-RESEARCH.md
@.planning/phases/116-student-training-coverage/116-01-SUMMARY.md

# Proven patterns from Phase 112
@.planning/phases/112-agent-governance-coverage/112-02-SUMMARY.md

# Source files
@backend/core/trigger_interceptor.py
@backend/core/student_training_service.py
@backend/core/supervision_service.py
@backend/tests/unit/governance/test_trigger_interceptor.py
@backend/tests/unit/agent/test_student_training_service.py
@backend/tests/unit/governance/test_supervision_service.py
</context>

<tasks>

<task type="auto">
  <name>Run combined coverage measurement for all three services</name>
  <files>backend/tests/coverage_reports/metrics/phase_116_coverage_baseline.json</files>
  <action>
    Run combined coverage measurement for all three student training services. Research indicates trigger_interceptor.py is at 96%, but we need accurate measurements for the other two services.

    Command:
    ```bash
    cd backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
      tests/unit/governance/test_trigger_interceptor.py \
      tests/unit/agent/test_student_training_service.py \
      tests/unit/governance/test_supervision_service.py \
      --cov=core.trigger_interceptor \
      --cov=core.student_training_service \
      --cov=core.supervision_service \
      --cov-report=term-missing \
      --cov-report=json:tests/coverage_reports/metrics/phase_116_coverage_baseline.json \
      --cov-report=html:tests/coverage_reports/html/phase_116_baseline
    ```

    This produces:
    1. Terminal output with coverage percentages and missing lines
    2. JSON report for programmatic analysis
    3. HTML report for detailed inspection

    Expected outcomes (from research):
    - trigger_interceptor.py: ~96% (already exceeds 60% target)
    - student_training_service.py: Unknown (was blocked by failing tests)
    - supervision_service.py: Unknown (14 test classes exist)
  </action>
  <verify>
    File tests/coverage_reports/metrics/phase_116_coverage_baseline.json exists and contains coverage data for all three services
  </verify>
  <done>
    Combined coverage report generated
    All three services show coverage percentages
    Missing lines identified for services below 60%
  </done>
</task>

<task type="auto">
  <name>Analyze coverage results and identify missing lines</name>
  <files>backend/tests/coverage_reports/metrics/phase_116_coverage_baseline.json</files>
  <action>
    Analyze the coverage JSON report to identify specific missing lines for each service below 60% coverage.

    Parse the JSON report and extract:
    1. Coverage percentage for each service
    2. Missing line numbers for each service
    3. Functions/methods with no coverage
    4. Branch coverage gaps

    Create a structured summary in a markdown file for reference in Plan 03:

    ```bash
    # Create coverage analysis document
    cat > backend/tests/coverage_reports/phase_116_coverage_analysis.md << 'EOF'
    # Phase 116 Coverage Analysis

    ## trigger_interceptor.py
    - Coverage: XX%
    - Status: EXCEEDS_TARGET / NEEDS_WORK
    - Missing lines: XX, YY, ZZ
    - Functions needing coverage: function_name

    ## student_training_service.py
    - Coverage: XX%
    - Status: EXCEEDS_TARGET / NEEDS_WORK
    - Missing lines: XX, YY, ZZ
    - Functions needing coverage: function_name

    ## supervision_service.py
    - Coverage: XX%
    - Status: EXCEEDS_TARGET / NEEDS_WORK
    - Missing lines: XX, YY, ZZ
    - Functions needing coverage: function_name

    ## Gap Analysis Summary
    - Services meeting 60% target: X/3
    - Services needing additional tests: Y/3
    - Estimated tests needed: N
    EOF
    ```

    Use jq or Python to parse JSON:
    ```bash
    jq '.files | to_entries[] | {file: .key, coverage: .value.summary.covered, total: .value.summary.num_statements}' \
      tests/coverage_reports/metrics/phase_116_coverage_baseline.json
    ```
  </action>
  <verify>
    File backend/tests/coverage_reports/phase_116_coverage_analysis.md exists with coverage breakdown
    Each service has coverage percentage and missing lines documented
  </verify>
  <done>
    Coverage analysis complete
    Gap-filling priorities identified for Plan 03
  </done>
</task>

<task type="auto">
  <name>Document gap-filling strategy for Plan 03</name>
  <files>backend/tests/coverage_reports/phase_116_coverage_analysis.md</files>
  <action>
    Based on the coverage analysis, document the specific test additions needed in Plan 03 to reach 60% coverage for each service.

    For each service below 60%, identify:
    1. High-impact missing lines (affect multiple code paths)
    2. Untested functions/methods
    3. Edge cases not covered

    Append to the analysis document:

    ```markdown
    ## Gap-Filling Strategy for Plan 03

    ### Priority 1: student_training_service.py
    Estimated coverage: XX%
    Target: 60%
    Gap: YY%
    Tests needed:
    - Test for uncovered_function (lines XX-YY)
    - Test for edge case in existing_function (line ZZ)

    ### Priority 2: supervision_service.py
    Estimated coverage: XX%
    Target: 60%
    Gap: YY%
    Tests needed:
    - Test for uncovered_function (lines XX-YY)

    ### Priority 3: trigger_interceptor.py
    Current: 96%
    Status: ALREADY_EXCEEDS_TARGET
    Action: No tests needed (optional polish for lines 314-317, 439)
    ```

    This strategy document becomes the input for Plan 03 test creation.
  </action>
  <verify>
    Gap-filling strategy documented in analysis file
    Priority order established based on coverage gap size
  </verify>
  <done>
    Clear roadmap for Plan 03 test additions
    Estimated number of tests to add
  </done>
</task>

</tasks>

<verification>
1. Combined coverage report generated (JSON + HTML)
2. All three services show accurate coverage percentages
3. Missing lines documented for each service below 60%
4. Gap-filling strategy prioritized by impact
</verification>

<success_criteria>
1. Coverage baseline documented in phase_116_coverage_baseline.json
2. Coverage analysis created with missing lines per service
3. trigger_interceptor.py confirmed at 96% (exceeds target)
4. Gap-filling strategy ready for Plan 03 execution
</success_criteria>

<output>
After completion, create `.planning/phases/116-student-training-coverage/116-02-SUMMARY.md`
</output>
