---
phase: 71-core-ai-services-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_byok_handler.py
  - backend/tests/integration/test_byok_handler_integration.py
  - backend/tests/property_tests/llm/test_byok_handler_invariants.py
  - backend/tests/unit/llm/test_cognitive_tier_system.py
  - backend/tests/unit/llm/test_cache_aware_router.py
  - backend/tests/property_tests/llm/test_byok_handler_provider_invariants.py
autonomous: true

must_haves:
  truths:
    - "LLM routing and BYOK handler tests cover 80%+ of provider selection and streaming logic"
    - "Query complexity classification is tested with edge cases"
    - "Provider selection logic for all tiers (budget, mid, premium) is covered"
    - "Streaming response handling is tested with mocked LLM clients"
    - "Cognitive tier classification is validated with property tests"
    - "Cache-aware router cost optimization is tested"
  artifacts:
    - path: "backend/tests/unit/test_byok_handler.py"
      provides: "Unit tests for BYOK handler"
      min_lines: 250
    - path: "backend/tests/integration/test_byok_handler_integration.py"
      provides: "Integration tests for BYOK with real clients mocked"
      min_lines: 150
    - path: "backend/tests/unit/llm/test_cognitive_tier_system.py"
      provides: "Tests for cognitive tier classification"
      min_lines: 150
    - path: "backend/tests/unit/llm/test_cache_aware_router.py"
      provides: "Tests for cache-aware cost optimization"
      min_lines: 120
  key_links:
    - from: "backend/tests/unit/test_byok_handler.py"
      to: "backend/core/llm/byok_handler.py"
      via: "direct import of BYOKHandler"
    - from: "backend/tests/property_tests/llm/test_byok_handler_invariants.py"
      to: "backend/core/llm/byok_handler.py"
      via: "Hypothesis invariants"
    - from: "backend/tests/unit/llm/test_cognitive_tier_system.py"
      to: "backend/core/llm/cognitive_tier_system.py"
      via: "CognitiveClassifier tests"
---

<objective>
Achieve 80%+ test coverage for LLM routing and BYOK handler

**Purpose:** The BYOK (Bring Your Own Key) handler manages multi-provider LLM routing, cost optimization, and streaming responses. This is critical for reliability and cost management. High coverage ensures provider selection works correctly and streaming is robust.

**Key Files:**
- `backend/core/llm/byok_handler.py` (~700+ lines) - Multi-provider routing, streaming, complexity analysis
- `backend/core/llm/cognitive_tier_system.py` (~300+ lines) - 5-tier cognitive classification
- `backend/core/llm/cache_aware_router.py` (~200+ lines) - Cache-based cost optimization

**Current State:** Some tests exist but need significant expansion for 80%+ coverage.

**Output:** Comprehensive test suite covering provider selection, streaming, complexity analysis, and cognitive classification
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/71-core-ai-services-coverage/71-RESEARCH.md
@backend/core/llm/byok_handler.py
@backend/core/llm/cognitive_tier_system.py
@backend/core/llm/cache_aware_router.py

# Existing test patterns
@backend/tests/unit/test_byok_handler.py
@backend/tests/property_tests/llm/test_byok_handler_invariants.py
@backend/tests/unit/test_cognitive_tier_service.py
</context>

<tasks>

<task type="auto">
  <name>Enhance BYOK handler tests to 80%+ coverage</name>
  <files>backend/tests/unit/test_byok_handler.py</files>
  <action>
Enhance existing tests at `backend/tests/unit/test_byok_handler.py`:

1. **Test Coverage for BYOKHandler:**
   Based on byok_handler.py structure, ensure coverage of:
   - `test_analyze_query_complexity_simple` - Simple queries (basic greeting)
   - `test_analyze_query_complexity_moderate` - Moderate queries (analysis, explanation)
   - `test_analyze_query_complexity_complex` - Complex queries (multi-step)
   - `test_analyze_query_complexity_advanced` - Advanced queries (code, math)
   - `test_get_optimal_provider_budget_tier` - Cost-conscious routing
   - `test_get_optimal_provider_premium_tier` - Quality-first routing
   - `test_get_optimal_provider_code_task` - Code-specific routing
   - `test_get_optimal_provider_with_unavailable_provider` - Fallback handling
   - `test_stream_response_success` - Normal streaming flow
   - `test_stream_response_with_tools` - Tool calling support
   - `test_stream_response_timeout` - Timeout handling
   - `test_stream_response_rate_limit` - Rate limit handling
   - `test_stream_response_invalid_api_key` - Error handling
   - `test_estimate_tokens_from_prompt` - Token estimation
   - `test_select_model_for_complexity` - Model selection logic
   - `test_provider_initialization` - Client setup for each provider

2. **Mocking Strategy for LLM Clients:**
   ```python
   from unittest.mock import AsyncMock, patch, MagicMock

   @pytest.fixture
   def mock_openai_client():
       with patch('core.llm.byok_handler.AsyncOpenAI') as mock:
           client = MagicMock()
           mock.return_value = client
           yield client

   @pytest.mark.asyncio
   async def test_stream_response_openai(mock_openai_client):
       """Test streaming with mocked OpenAI client"""
       # Configure mock to return async generator
       async def mock_stream():
           yield MagicMock(choices=[MagicMock(delta=MagicMock(content="Hello"))])
       mock_openai_client.chat.completions.create = AsyncMock(return_value=mock_stream())
   ```

3. **Parametrized Provider Tests:**
   ```python
   @pytest.mark.parametrize("provider,expected_available", [
       ("openai", True),
       ("anthropic", True),
       ("deepseek", True),
       ("gemini", True),
       ("invalid_provider", False),
   ])
   def test_provider_availability(byok_handler, provider, expected_available):
       """Test provider availability check"""
       result = provider in byok_handler.clients
       assert result == expected_available
   ```

4. **Coverage Verification:**
   ```bash
   pytest backend/tests/unit/test_byok_handler.py \
          --cov=backend/core/llm/byok_handler \
          --cov-branch --cov-report=term-missing
   ```
   - Target: 80%+ coverage
   - Add tests for uncovered lines

5. **DO NOT:**
   - Don't call real LLM APIs
   - Don't use real API keys in tests
   - Don't rely on external network calls

Reference Phase 62 research: Use AsyncMock for all LLM client responses.
  </action>
  <verify>pytest backend/tests/unit/test_byok_handler.py -v --cov=backend/core/llm/byok_handler --cov-branch --cov-report=term-missing</verify>
  <done>All tests pass, coverage >=80% for byok_handler.py</done>
</task>

<task type="auto">
  <name>Create tests for cognitive tier classification system</name>
  <files>backend/tests/unit/llm/test_cognitive_tier_system.py</files>
  <action>
Create comprehensive tests for `backend/core/llm/cognitive_tier_system.py`:

1. **File:** `backend/tests/unit/llm/test_cognitive_tier_system.py`

2. **Test Cases for CognitiveClassifier:**
   - `test_classify_micro_tier` - <100 tokens, simple queries
   - `test_classify_standard_tier` - 100-500 tokens, moderate
   - `test_classify_versatile_tier` - 500-2k tokens, multi-step
   - `test_classify_heavy_tier` - 2k-5k tokens, complex
   - `test_classify_complex_tier` - >5k tokens or code/math
   - `test_token_count_estimation` - Character-based estimation
   - `test_semantic_pattern_detection` - Code/math/technical keywords
   - `test_task_type_influence` - Chat vs code generation
   - `test_edge_case_empty_prompt` - Handle empty input
   - `test_edge_case_very_long_prompt` - Handle >10k tokens

3. **Parametrized Tier Tests:**
   ```python
   @pytest.mark.parametrize("prompt,expected_tier", [
       ("hi", CognitiveTier.MICRO),
       ("hello world", CognitiveTier.MICRO),
       ("analyze the following data", CognitiveTier.STANDARD),
       ("write a python function", CognitiveTier.COMPLEX),  # Code keyword
       ("solve this integral", CognitiveTier.COMPLEX),      # Math keyword
       ("explain quantum mechanics in detail", CognitiveTier.VERSATILE),
   ])
   def test_cognitive_classification(classifier, prompt, expected_tier):
       """Test classification for various prompt types"""
       tier = classifier.classify(prompt)
       assert tier == expected_tier
   ```

4. **Property-Based Tests:**
   Create or enhance `backend/tests/property_tests/llm/test_cognitive_tier_invariants.py`:
   ```python
   @settings(max_examples=50)
   @given(prompt=st.text(min_size=1, max_size=1000))
   def test_classification_always_returns_valid_tier(prompt):
       """INVARIANT: Classification always returns valid CognitiveTier"""
       classifier = CognitiveClassifier()
       tier = classifier.classify(prompt)
       assert tier in CognitiveTier
   ```

5. **Coverage Verification:**
   ```bash
   pytest backend/tests/unit/llm/test_cognitive_tier_system.py \
          backend/tests/property_tests/llm/test_cognitive_tier_invariants.py \
          --cov=backend/core/llm/cognitive_tier_system \
          --cov-branch --cov-report=term-missing
   ```
   - Target: 80%+ coverage
  </action>
  <verify>pytest backend/tests/unit/llm/test_cognitive_tier_system.py backend/tests/property_tests/llm/test_cognitive_tier_invariants.py -v --cov=backend/core/llm/cognitive_tier_system --cov-branch</verify>
  <done>All tests pass, coverage >=80% for cognitive_tier_system.py</done>
</task>

<task type="auto">
  <name>Create tests for cache-aware router</name>
  <files>backend/tests/unit/llm/test_cache_aware_router.py</files>
  <action>
Create comprehensive tests for `backend/core/llm/cache_aware_router.py`:

1. **File:** `backend/tests/unit/llm/test_cache_aware_router.py`

2. **Test Cases for Cache-Aware Routing:**
   - `test_select_provider_with_cache_hit` - Use cached provider
   - `test_select_provider_with_cache_miss` - Calculate optimal
   - `test_cache_key_generation` - Consistent key for same inputs
   - `test_cache_invalidation` - Clear stale entries
   - `test_cost_saving_calculation` - Verify 90% cost reduction target
   - `test_cache_warmup_for_common_queries` - Prefill popular prompts
   - `test_cache_with_varying_complexity` - Different tiers cache separately

3. **Cost Optimization Tests:**
   ```python
   def test_cache_aware_cost_savings():
       """Verify cache-aware routing reduces costs"""
       router = CacheAwareRouter()

       # First call - no cache
       provider1 = router.select_provider("test prompt", complexity=QueryComplexity.SIMPLE)

       # Second call - cache hit
       provider2 = router.select_provider("test prompt", complexity=QueryComplexity.SIMPLE)

       # Should use cached (cheaper) provider
       assert provider1 == provider2
       assert router.cache_hit_count > 0
   ```

4. **Coverage Verification:**
   ```bash
   pytest backend/tests/unit/llm/test_cache_aware_router.py \
          --cov=backend/core/llm/cache_aware_router \
          --cov-branch --cov-report=term-missing
   ```
   - Target: 80%+ coverage
  </action>
  <verify>pytest backend/tests/unit/llm/test_cache_aware_router.py -v --cov=backend/core/llm/cache_aware_router --cov-branch</verify>
  <done>All tests pass, coverage >=80% for cache_aware_router.py</done>
</task>

</tasks>

<verification>
1. Run full LLM routing test suite:
   ```bash
   pytest backend/tests/unit/test_byok_handler.py \
          backend/tests/integration/test_byok_handler_integration.py \
          backend/tests/property_tests/llm/test_byok_handler_invariants.py \
          backend/tests/property_tests/llm/test_byok_handler_provider_invariants.py \
          backend/tests/unit/llm/test_cognitive_tier_system.py \
          backend/tests/unit/llm/test_cache_aware_router.py \
          -v --cov=backend/core/llm/byok_handler \
          --cov=backend/core/llm/cognitive_tier_system \
          --cov=backend/core/llm/cache_aware_router \
          --cov-branch --cov-report=term-missing
   ```

2. Verify coverage >= 80% for all three modules

3. Run cognitive tier property tests

4. Verify no real LLM API calls are made (all mocked)

5. Check existing LLM tests still pass:
   ```bash
   pytest backend/tests/test_cognitive_tier_api.py -v
   pytest backend/tests/test_cognitive_tier_e2e.py -v
   ```
</verification>

<success_criteria>
1. BYOK handler tests >= 80% coverage
2. Cognitive tier system tests >= 80% coverage
3. Cache-aware router tests >= 80% coverage
4. All query complexity levels tested (simple, moderate, complex, advanced)
5. Provider selection for all tiers validated
6. Streaming response handling tested with mocked clients
7. Property-based tests validate classification invariants
8. No real LLM API calls in tests
9. All tests pass consistently
</success_criteria>

<output>
After completion, create `.planning/phases/71-core-ai-services-coverage/71-03-SUMMARY.md`
</output>
