---
phase: 71-core-ai-services-coverage
plan: 05
type: execute
wave: 2
depends_on: ["71-01", "71-02", "71-03"]
files_modified:
  - backend/tests/unit/episodes/test_episode_lifecycle_service.py
  - backend/tests/unit/episodes/test_episode_segmentation_service.py
  - backend/tests/unit/episodes/test_episode_retrieval_service.py
  - backend/tests/integration/episodes/test_episode_lifecycle_lancedb.py
  - backend/tests/property_tests/episodes/test_episode_segmentation_invariants.py
  - backend/tests/property_tests/episodes/test_episode_retrieval_invariants.py
  - backend/tests/property_tests/episodes/test_episode_lifecycle_invariants.py
autonomous: true

must_haves:
  truths:
    - "Episode and memory management services achieve 80%+ test coverage"
    - "Time gap detection (30-minute threshold) is tested"
    - "Topic change detection using semantic similarity is covered"
    - "Task completion detection is tested"
    - "Episode archival to LanceDB is validated"
    - "Retrieval modes (temporal, semantic, sequential, contextual) work"
    - "Episode graduation integration is tested"
  artifacts:
    - path: "backend/tests/unit/episodes/test_episode_lifecycle_service.py"
      provides: "Episode lifecycle management tests"
      min_lines: 200
    - path: "backend/tests/unit/episodes/test_episode_segmentation_service.py"
      provides: "Episode boundary detection tests"
      min_lines: 200
    - path: "backend/tests/unit/episodes/test_episode_retrieval_service.py"
      provides: "Episode retrieval and search tests"
      min_lines: 200
    - path: "backend/tests/integration/episodes/test_episode_lifecycle_lancedb.py"
      provides: "Integration tests with LanceDB"
      min_lines: 150
  key_links:
    - from: "backend/tests/unit/episodes/test_episode_lifecycle_service.py"
      to: "backend/core/episode_lifecycle_service.py"
      via: "EpisodeLifecycleService tests"
    - from: "backend/tests/unit/episodes/test_episode_segmentation_service.py"
      to: "backend/core/episode_segmentation_service.py"
      via: "EpisodeBoundaryDetector tests"
    - from: "backend/tests/property_tests/episodes/test_episode_segmentation_invariants.py"
      to: "backend/core/episode_segmentation_service.py"
      via: "Hypothesis invariants"
    - from: "backend/tests/integration/episodes/test_episode_lifecycle_lancedb.py"
      to: "backend/core/lancedb_handler.py"
      via: "LanceDB integration"
---

<objective>
Achieve 80%+ test coverage for episode and memory management services

**Purpose:** The episodic memory system is core to agent learning. It segments interactions into episodes, stores them in LanceDB for semantic search, and retrieves them for context. This enables agents to learn from past experiences.

**Key Files:**
- `backend/core/episode_lifecycle_service.py` (~200+ lines) - Episode creation, archival, completion
- `backend/core/episode_segmentation_service.py` (~300+ lines) - Time gap, topic change, task completion detection
- `backend/core/episode_retrieval_service.py` (~250+ lines) - Temporal, semantic, sequential, contextual retrieval

**Current State:** Test files exist but need enhancement for 80%+ coverage.

**Output:** Comprehensive test suite covering episode lifecycle, boundary detection, and all retrieval modes
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/71-core-ai-services-coverage/71-RESEARCH.md
@backend/core/episode_lifecycle_service.py
@backend/core/episode_segmentation_service.py
@backend/core/episode_retrieval_service.py

# Existing test patterns
@backend/tests/test_episode_segmentation.py
@backend/tests/unit/episodes/test_episode_lifecycle_service.py
@backend/tests/unit/episodes/test_episode_segmentation_service.py
@backend/tests/unit/episodes/test_episode_retrieval_service.py
@backend/tests/property_tests/episodes/test_episode_segmentation_invariants.py
</context>

<tasks>

<task type="auto">
  <name>Enhance episode lifecycle service tests to 80%+ coverage</name>
  <files>backend/tests/unit/episodes/test_episode_lifecycle_service.py</files>
  <action>
Enhance existing tests at `backend/tests/unit/episodes/test_episode_lifecycle_service.py`:

1. **Test Coverage for EpisodeLifecycleService:**
   Based on episode_lifecycle_service.py structure, ensure coverage of:
   - `test_create_episode` - Creates new episode with agent context
   - `test_create_episode_with_canvas_data` - Includes canvas presentation
   - `test_create_episode_with_feedback` - Includes user feedback
   - `test_complete_episode` - Marks episode as completed
   - `test_archive_episode_to_lancedb` - Stores in vector DB
   - `test_archive_episode_with_segments` - Handles segmented episodes
   - `test_get_episode_by_id` - Retrieves episode details
   - `test_list_agent_episodes` - Lists episodes for agent
   - `test_list_episodes_by_maturity` - Filters by maturity level
   - `test_get_episode_statistics` - Calculates episode metrics
   - `test_delete_episode` - Removes episode
   - `test_edge_case_empty_messages` - Handles empty conversation
   - `test_edge_case_single_message` - Single message episode

2. **Mocking Strategy:**
   - Mock LanceDB handler for vector operations
   - Mock database session for episode records
   - Mock BYOKHandler for LLM-based summarization

3. **Integration Tests:**
   Create or enhance `backend/tests/integration/episodes/test_episode_lifecycle_lancedb.py`:
   - `test_create_and_archive_episode` - Full flow with LanceDB
   - `test_retrieve_archived_episode` - Fetch from vector DB
   - `test_search_episodes_by_similarity` - Semantic search

4. **Coverage Verification:**
   ```bash
   pytest backend/tests/unit/episodes/test_episode_lifecycle_service.py \
          --cov=backend/core/episode_lifecycle_service \
          --cov-branch --cov-report=term-missing
   ```
   - Target: 80%+ coverage
   - Add tests for uncovered lines
  </action>
  <verify>pytest backend/tests/unit/episodes/test_episode_lifecycle_service.py backend/tests/integration/episodes/test_episode_lifecycle_lancedb.py -v --cov=backend/core/episode_lifecycle_service --cov-branch --cov-report=term-missing</verify>
  <done>All tests pass, coverage >=80% for episode_lifecycle_service.py</done>
</task>

<task type="auto">
  <name>Enhance episode segmentation service tests to 80%+ coverage</name>
  <files>backend/tests/unit/episodes/test_episode_segmentation_service.py</files>
  <action>
Enhance existing tests at `backend/tests/unit/episodes/test_episode_segmentation_service.py`:

1. **Test Coverage for EpisodeBoundaryDetector:**
   - `test_detect_time_gap_none` - No gaps in continuous conversation
   - `test_detect_time_gap_single` - Detects 30+ minute gap
   - `test_detect_time_gap_multiple` - Multiple gaps detected
   - `test_detect_time_gap_exactly_threshold` - Boundary case at 30 minutes
   - `test_detect_topic_changes_no_change` - Same topic throughout
   - `test_detect_topic_changes_with_change` - Topic shift detected
   - `test_detect_topic_changes_semantic_similarity` - Uses embeddings
   - `test_detect_task_completion` - Task completion signals
   - `test_segment_conversation_single_episode` - No boundaries
   - `test_segment_conversation_multiple_episodes` - Multiple boundaries

2. **Property-Based Tests:**
   Enhance `backend/tests/property_tests/episodes/test_episode_segmentation_invariants.py`:
   ```python
   from hypothesis import given, strategies as st, settings
   from datetime import datetime, timedelta

   @settings(max_examples=50)
   @given(
       message_count=st.integers(min_value=1, max_value=100),
       gap_minutes=st.integers(min_value=0, max_value=120)
   )
   def test_time_gap_detection_invariant(message_count, gap_minutes):
       """INVARIANT: Time gaps are correctly identified for any message sequence"""
       messages = create_test_messages(message_count, gap_minutes)
       detector = EpisodeBoundaryDetector(mock_lancedb)
       gaps = detector.detect_time_gap(messages)

       # Verify: Gap count should match expected
       expected_gaps = sum(1 for i in range(1, len(messages))
                          if (messages[i].created_at - messages[i-1].created_at).total_seconds() / 60 >= 30)
       assert len(gaps) == expected_gaps
   ```

3. **Cosine Similarity Tests:**
   - `test_cosine_similarity_identical` - Same vectors = 1.0
   - `test_cosine_similarity_orthogonal` - Orthogonal vectors = 0.0
   - `test_cosine_similarity_opposite` - Opposite vectors = -1.0

4. **Coverage Verification:**
   ```bash
   pytest backend/tests/unit/episodes/test_episode_segmentation_service.py \
          backend/tests/property_tests/episodes/test_episode_segmentation_invariants.py \
          --cov=backend/core/episode_segmentation_service \
          --cov-branch --cov-report=term-missing
   ```
   - Target: 80%+ coverage
  </action>
  <verify>pytest backend/tests/unit/episodes/test_episode_segmentation_service.py backend/tests/property_tests/episodes/test_episode_segmentation_invariants.py -v --cov=backend/core/episode_segmentation_service --cov-branch</verify>
  <done>All tests pass, coverage >=80% for episode_segmentation_service.py</done>
</task>

<task type="auto">
  <name>Enhance episode retrieval service tests to 80%+ coverage</name>
  <files>backend/tests/unit/episodes/test_episode_retrieval_service.py</files>
  <action>
Enhance existing tests at `backend/tests/unit/episodes/test_episode_retrieval_service.py`:

1. **Test Coverage for EpisodeRetrievalService:**
   Based on episode_retrieval_service.py structure, ensure coverage of:

   **Temporal Retrieval:**
   - `test_retrieve_by_time_range_recent` - Last N episodes
   - `test_retrieve_by_time_range_historical` - Specific date range
   - `test_retrieve_by_time_range_empty` - No episodes in range

   **Semantic Retrieval:**
   - `test_retrieve_by_similarity` - Semantic vector search
   - `test_retrieve_by_similarity_threshold` - Minimum similarity filter
   - `test_retrieve_by_similarity_top_k` - Limit results

   **Sequential Retrieval:**
   - `test_retrieve_sequential_after_episode` - Episodes after given ID
   - `test_retrieve_sequential_before_episode` - Episodes before given ID
   - `test_retrieve_sequential_between` - Episode range

   **Contextual Retrieval:**
   - `test_retrieve_by_context_agent_type` - Similar agents
   - `test_retrieve_by_context_task_type` - Similar tasks
   - `test_retrieve_by_context_combined` - Multiple context factors

   **Hybrid Retrieval:**
   - `test_retrieve_hybrid_temporal_semantic` - Combined time + similarity
   - `test_retrieve_hybrid_with_filters` - Multiple filters

2. **Mocking Strategy:**
   - Mock LanceDB vector search
   - Mock database queries for episode metadata

3. **Property-Based Tests:**
   Create or enhance `backend/tests/property_tests/episodes/test_episode_retrieval_invariants.py`:
   ```python
   @settings(max_examples=50)
   @given(
       query=st.text(min_size=1, max_size=200),
       top_k=st.integers(min_value=1, max_value=20)
   )
   def test_retrieval_returns_valid_episodes_invariant(query, top_k):
       """INVARIANT: Retrieved episodes have valid structure"""
       service = EpisodeRetrievalService(mock_db, mock_lancedb)
       episodes = service.retrieve_by_similarity(query, top_k=top_k)

       for episode in episodes:
           assert hasattr(episode, 'id')
           assert hasattr(episode, 'agent_id')
           assert hasattr(episode, 'created_at')
           assert len(episodes) <= top_k
   ```

4. **Coverage Verification:**
   ```bash
   pytest backend/tests/unit/episodes/test_episode_retrieval_service.py \
          backend/tests/property_tests/episodes/test_episode_retrieval_invariants.py \
          --cov=backend/core/episode_retrieval_service \
          --cov-branch --cov-report=term-missing
   ```
   - Target: 80%+ coverage
  </action>
  <verify>pytest backend/tests/unit/episodes/test_episode_retrieval_service.py backend/tests/property_tests/episodes/test_episode_retrieval_invariants.py -v --cov=backend/core/episode_retrieval_service --cov-branch</verify>
  <done>All tests pass, coverage >=80% for episode_retrieval_service.py</done>
</task>

</tasks>

<verification>
1. Run full episode service test suite:
   ```bash
   pytest backend/tests/unit/episodes/test_episode_lifecycle_service.py \
          backend/tests/unit/episodes/test_episode_segmentation_service.py \
          backend/tests/unit/episodes/test_episode_retrieval_service.py \
          backend/tests/integration/episodes/test_episode_lifecycle_lancedb.py \
          backend/tests/property_tests/episodes/test_episode_segmentation_invariants.py \
          backend/tests/property_tests/episodes/test_episode_retrieval_invariants.py \
          backend/tests/property_tests/episodes/test_episode_lifecycle_invariants.py \
          -v --cov=backend/core/episode_lifecycle_service \
          --cov=backend/core/episode_segmentation_service \
          --cov=backend/core/episode_retrieval_service \
          --cov-branch --cov-report=term-missing
   ```

2. Verify coverage >= 80% for all three episode services

3. Run integration tests with LanceDB (may require LanceDB availability)

4. Verify property-based tests validate invariants

5. Check existing episode tests still pass:
   ```bash
   pytest backend/tests/test_episode_segmentation.py -v
   pytest backend/tests/test_supervision_episode_creation.py -v
   ```
</verification>

<success_criteria>
1. Episode lifecycle tests >= 80% coverage
2. Episode segmentation tests >= 80% coverage
3. Episode retrieval tests >= 80% coverage
4. All boundary detection methods tested (time, topic, task)
5. All retrieval modes tested (temporal, semantic, sequential, contextual)
6. LanceDB integration tests pass
7. Property-based tests validate segmentation and retrieval invariants
8. Canvas presentation data handling tested
9. Feedback integration with episodes tested
10. All tests pass consistently
</success_criteria>

<output>
After completion, create `.planning/phases/71-core-ai-services-coverage/71-05-SUMMARY.md`
</output>
