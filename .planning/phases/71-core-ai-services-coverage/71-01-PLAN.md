---
phase: 71-core-ai-services-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/agent/test_agent_execution_service.py
  - backend/tests/integration/agent/test_agent_execution_orchestration.py
  - backend/tests/property_tests/agent/test_agent_execution_invariants.py
  - backend/tests/unit/agent/test_agent_context_resolver.py
autonomous: true

must_haves:
  truths:
    - "Agent execution service has comprehensive tests covering 80%+ of code paths"
    - "Agent context resolver fallback chain is fully tested"
    - "All error paths in execute_agent_chat are covered"
    - "WebSocket streaming logic is tested with mocked connections"
    - "LLM provider selection logic in execution flow is tested"
  artifacts:
    - path: "backend/tests/unit/agent/test_agent_execution_service.py"
      provides: "Unit tests for agent execution service"
      min_lines: 200
    - path: "backend/tests/integration/agent/test_agent_execution_orchestration.py"
      provides: "Integration tests for execution orchestration"
      min_lines: 150
    - path: "backend/tests/property_tests/agent/test_agent_execution_invariants.py"
      provides: "Property-based tests for execution invariants"
      min_lines: 100
    - path: "backend/tests/unit/agent/test_agent_context_resolver.py"
      provides: "Tests for agent context resolver fallback chain"
      min_lines: 120
  key_links:
    - from: "backend/tests/unit/agent/test_agent_execution_service.py"
      to: "backend/core/agent_execution_service.py"
      via: "direct import of execute_agent_chat"
    - from: "backend/tests/unit/agent/test_agent_context_resolver.py"
      to: "backend/core/agent_context_resolver.py"
      via: "direct import of AgentContextResolver"
---

<objective>
Achieve 80%+ test coverage for agent orchestration service

**Purpose:** The agent execution service and context resolver are central to all agent interactions. They handle agent resolution, governance checks, LLM routing, and WebSocket streaming. High coverage ensures reliability across all agent interactions.

**Key Files:**
- `backend/core/agent_execution_service.py` (~270 lines) - Main execute_agent_chat function
- `backend/core/agent_context_resolver.py` (~140 lines) - Fallback chain resolution

**Current State:** Existing tests exist but coverage is unknown. Phase 62 research showed overall coverage was only 17.12%.

**Output:** Comprehensive test suite with 80%+ coverage for agent orchestration paths
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/71-core-ai-services-coverage/71-RESEARCH.md
@backend/core/agent_execution_service.py
@backend/core/agent_context_resolver.py

# Existing test patterns
@backend/tests/unit/test_agent_governance_service.py
@backend/tests/integration/test_agent_execution_orchestration.py
@backend/tests/property_tests/agents/test_agent_execution_invariants.py
</context>

<tasks>

<task type="auto">
  <name>Create comprehensive unit tests for agent execution service</name>
  <files>backend/tests/unit/agent/test_agent_execution_service.py</files>
  <action>
Create comprehensive unit tests for `backend/core/agent_execution_service.py`:

1. **Test Structure:**
   - Use pytest with asyncio_mode=auto (already configured)
   - Create fixtures for mock_db, mock_byok_handler, mock_agent
   - Use AsyncMock for LLM streaming responses

2. **Test Cases to Cover:**
   - `test_execute_agent_chat_success` - Normal execution flow
   - `test_execute_agent_chat_with_governance_block` - Governance denial path
   - `test_execute_agent_chat_agent_not_found` - Fallback to system default
   - `test_execute_agent_chat_streaming_mode` - WebSocket broadcast
   - `test_execute_agent_chat_with_conversation_history` - History context
   - `test_execute_agent_chat_llm_failure` - BYOK handler error handling
   - `test_execute_agent_chat_emergency_bypass` - EMERGENCY_GOVERNANCE_BYPASS flag
   - `test_execute_agent_chat_db_commit_failure` - Audit trail failure doesn't block
   - `test_execute_agent_chat_websocket_failure` - Graceful handling of WebSocket errors

3. **Mocking Strategy:**
   - Mock AgentContextResolver.resolve_agent_for_request
   - Mock AgentGovernanceService.can_perform_action
   - Mock BYOKHandler for LLM responses (AsyncMock for streaming)
   - Mock ws_manager.broadcast for WebSocket tests

4. **Coverage Verification:**
   After writing tests, run: `pytest backend/tests/unit/agent/test_agent_execution_service.py --cov=backend/core/agent_execution_service --cov-branch --cov-report=term-missing`
   - Target: 80%+ coverage
   - Add tests for any missing lines shown in report

5. **DO NOT:**
   - Don't mock the service methods themselves - test real execution paths
   - Don't call real LLM APIs - always mock BYOKHandler
   - Don't rely on external services

Reference existing test patterns in backend/tests/unit/test_agent_governance_service.py for fixture structure.
  </action>
  <verify>pytest backend/tests/unit/agent/test_agent_execution_service.py -v --cov=backend/core/agent_execution_service --cov-branch --cov-report=term-missing</verify>
  <done>All tests pass, coverage >=80% for agent_execution_service.py</done>
</task>

<task type="auto">
  <name>Create comprehensive unit tests for agent context resolver</name>
  <files>backend/tests/unit/agent/test_agent_context_resolver.py</files>
  <action>
Create comprehensive unit tests for `backend/core/agent_context_resolver.py`:

1. **Test Structure:**
   - File: `backend/tests/unit/agent/test_agent_context_resolver.py`
   - Use pytest with mock_db fixture

2. **Test Cases for Fallback Chain:**
   - `test_resolve_agent_explicit_id_found` - Level 1: Explicit agent_id
   - `test_resolve_agent_explicit_id_not_found` - Falls through to session
   - `test_resolve_agent_session_found` - Level 2: Session agent
   - `test_resolve_agent_session_not_found` - Falls through to system default
   - `test_resolve_agent_system_default_exists` - Level 3: System default
   - `test_resolve_agent_system_default_created` - Creates Chat Assistant if missing
   - `test_resolve_agent_all_failures` - Returns None with resolution_failed
   - `test_resolution_context_contains_path` - Verify context tracking
   - `test_resolution_context_timestamps` - Verify timing

3. **Edge Cases:**
   - `test_get_agent_with_invalid_id` - Agent not found
   - `test_get_session_agent_with_invalid_session` - Session not found
   - `test_resolution_with_no_session_id` - Skip session level

4. **Coverage Verification:**
   Run: `pytest backend/tests/unit/agent/test_agent_context_resolver.py --cov=backend/core/agent_context_resolver --cov-branch --cov-report=term-missing`
   - Target: 80%+ coverage

5. **Note:** This file already exists at backend/tests/unit/governance/test_agent_context_resolver.py - either enhance it or create the new path following the unit/agent/ structure. Choose based on which location makes more sense organizationally.
  </action>
  <verify>pytest backend/tests/unit/agent/test_agent_context_resolver.py -v --cov=backend/core/agent_context_resolver --cov-branch --cov-report=term-missing</verify>
  <done>All tests pass, coverage >=80% for agent_context_resolver.py</done>
</task>

<task type="auto">
  <name>Create property-based tests for execution invariants</name>
  <files>backend/tests/property_tests/agent/test_agent_execution_invariants.py</files>
  <action>
Enhance/create property-based tests for agent execution invariants:

1. **File:** `backend/tests/property_tests/agent/test_agent_execution_invariants.py`
   - Note: This file may already exist - enhance existing tests

2. **Hypothesis Invariants to Test:**
   - `test_resolution_context_always_has_timestamp` - Timestamp present for any resolution
   - `test_resolution_path_never_empty` - Resolution path always recorded
   - `test_execution_id_always_uuid` - Execution ID is valid UUID format
   - `test_message_format_valid` - Messages array always has role/content structure

3. **Strategy:**
   ```python
   from hypothesis import given, strategies as st, settings
   from hypothesis import HealthCheck

   @settings(max_examples=50, suppress_health_check=[HealthCheck.function_scoped_fixture])
   @given(
       agent_id=st.text(min_size=1, max_size=50),
       user_id=st.text(min_size=1, max_size=50),
       message=st.text(min_size=1, max_size=500)
   )
   async def test_execution_id_format_invariant(agent_id, user_id, message):
       """INVARIANT: Execution ID is always valid UUID format"""
       # Test implementation
   ```

4. **Coverage Verification:**
   Run: `pytest backend/tests/property_tests/agent/test_agent_execution_invariants.py -v --cov=backend/core/agent_execution_service --cov-branch`

5. **max_examples=50 per Phase 62 research** - Higher values don't improve bug finding significantly
  </action>
  <verify>pytest backend/tests/property_tests/agent/test_agent_execution_invariants.py -v</verify>
  <done>All property tests pass, invariants validated</done>
</task>

</tasks>

<verification>
1. Run full test suite for agent execution:
   ```bash
   pytest backend/tests/unit/agent/test_agent_execution_service.py \
          backend/tests/unit/agent/test_agent_context_resolver.py \
          backend/tests/property_tests/agent/test_agent_execution_invariants.py \
          -v --cov=backend/core/agent_execution_service \
          --cov=backend/core/agent_context_resolver \
          --cov-branch --cov-report=term-missing
   ```

2. Verify coverage >= 80% for both modules

3. Check no tests are skipped or marked as expected failure

4. Run integration tests to ensure no regressions:
   ```bash
   pytest backend/tests/integration/agent/test_agent_execution_orchestration.py -v
   ```
</verification>

<success_criteria>
1. Unit test file exists with 200+ lines covering all execution paths
2. Context resolver tests cover 100% of fallback chain
3. Property-based tests validate critical invariants
4. Coverage >= 80% for agent_execution_service.py
5. Coverage >= 80% for agent_context_resolver.py
6. All tests pass consistently (3 consecutive runs)
7. No mocked service methods - real execution paths tested
8. LLM responses mocked (no real API calls)
</success_criteria>

<output>
After completion, create `.planning/phases/71-core-ai-services-coverage/71-01-SUMMARY.md`
</output>
