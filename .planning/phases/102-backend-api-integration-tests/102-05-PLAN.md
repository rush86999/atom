---
phase: 102-backend-api-integration-tests
plan: 05
type: execute
wave: 3
depends_on: [102-01, 102-02, 102-03, 102-04]
files_modified:
  - backend/tests/test_api_request_validation.py
autonomous: true

must_haves:
  truths:
    - "All API endpoints validate request schemas using Pydantic models"
    - "Missing required fields return 422 Unprocessable Entity"
    - "Invalid data types return appropriate validation errors"
    - "String length constraints enforced (min/max)"
    - "Format validation enforced (URLs, emails, UUIDs)"
    - "Validation errors include field path and error message"
  artifacts:
    - path: "backend/tests/test_api_request_validation.py"
      provides: "Comprehensive request validation tests for all API endpoints"
      min_lines: 200
      exports: ["TestAgentEndpointValidation", "TestCanvasEndpointValidation", "TestBrowserEndpointValidation", "TestDeviceEndpointValidation"]
  key_links:
    - from: "backend/tests/test_api_request_validation.py"
      to: "backend/core/atom_agent_endpoints.py"
      via: "ChatRequest model validation"
      pattern: "ChatRequest.*message.*user_id"
    - from: "backend/tests/test_api_request_validation.py"
      to: "backend/api/canvas_routes.py"
      via: "FormSubmission model validation"
      pattern: "FormSubmission.*canvas_id.*form_data"
    - from: "backend/tests/test_api_request_validation.py"
      to: "backend/api/browser_routes.py"
      via: "Browser request model validation"
      pattern: "CreateSessionRequest|NavigateRequest"
    - from: "backend/tests/test_api_request_validation.py"
      to: "backend/api/device_capabilities.py"
      via: "Device request model validation"
      pattern: "CameraSnapRequest|ExecuteCommandRequest"
---

<objective>
Create comprehensive request validation tests covering all API endpoints to ensure schema validation, type checking, and constraint enforcement work correctly.

Purpose: Request validation is the first line of defense against malformed input and potential injection attacks. Comprehensive validation tests ensure all endpoints properly reject invalid requests before processing.

Output: Integration test file with 35+ tests covering validation for all major API endpoints.
</object>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-down/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/tests/conftest.py
@backend/core/atom_agent_endpoints.py (ChatRequest, ChatMessage models)
@backend/api/canvas_routes.py (FormSubmission model)
@backend/api/browser_routes.py (CreateSessionRequest, NavigateRequest, etc.)
@backend/api/device_capabilities.py (CameraSnapRequest, ExecuteCommandRequest, etc.)

Pydantic validation patterns:
- Required fields: 422 if missing
- Type validation: 422 if wrong type
- String constraints: min_length, max_length
- Format validation: URL, email, UUID patterns
- Custom validators: via @field_validator
</context>

<tasks>

<task type="auto">
  <name>Implement agent endpoint validation tests</name>
  <files>backend/tests/test_api_request_validation.py</files>
  <action>
Create TestAgentEndpointValidation class for agent request validation:

1. POST /api/atom-agent/chat (ChatRequest validation):
   - Missing message field: 422 with field path
   - Missing user_id field: 422 with field path
   - Empty message rejected (min_length=1)
   - Message exceeding max_length (10000) rejected
   - Invalid session_id format (not UUID) rejected
   - conversation_history must be array of ChatMessage objects
   - Each ChatMessage requires role and content
   - Role must be in ["user", "assistant", "system"]

2. POST /api/atom-agent/sessions (create session):
   - Missing user_id in request body: 422
   - user_id must be string

3. POST /api/atom-agent/execute (execution request):
   - Missing agent_id: 422
   - Missing input_data: 422
   - input_data must be dict/object
   - Invalid workflow_id format rejected

4. GET /api/atom-agent/sessions/{id}/history (path validation):
   - Invalid session_id format handled
   - Negative limit parameter rejected
   - Limit exceeds maximum rejected

Use TestClient with minimal mocking (validation happens before business logic).
Assert on status_code=422 for validation errors.
Check response.json() for detail with field locations.
  </action>
  <verify>pytest tests/test_api_request_validation.py::TestAgentEndpointValidation -v</verify>
  <done>12+ tests pass covering agent endpoint validation</done>
</task>

<task type="auto">
  <name>Implement canvas endpoint validation tests</name>
  <files>backend/tests/test_api_request_validation.py</files>
  <action>
Create TestCanvasEndpointValidation class for canvas request validation:

1. POST /api/canvas/submit (FormSubmission validation):
   - Missing canvas_id: 422 with field path
   - Missing form_data: 422 with field path
   - canvas_id must be string (not number/null)
   - form_data must be dict/object (not array/string)
   - Empty form_data object rejected
   - agent_execution_id must be string or null
   - agent_id must be string or null
   - Form data keys must be strings
   - Nested form_data validated (no circular references)

2. Canvas ID format validation:
   - UUID format preferred but string accepted
   - Empty canvas_id rejected
   - canvas_id max_length enforced

3. Form data constraints:
   - Max 100 fields in form_data
   - Field names max 100 chars
   - Field values max 10000 chars (prevent DoS)

Use pytest.mark.parametrize for multiple invalid inputs.
Test each constraint independently.
  </action>
  <verify>pytest tests/test_api_request_validation.py::TestCanvasEndpointValidation -v</verify>
  <done>8+ tests pass covering canvas endpoint validation</done>
</task>

<task type="auto">
  <name>Implement browser endpoint validation tests</name>
  <files>backend/tests/test_api_request_validation.py</files>
  <action>
Create TestBrowserEndpointValidation class for browser request validation:

1. POST /api/browser/session/create (CreateSessionRequest):
   - browser_type must be in ["chromium", "firefox", "webkit"]
   - Invalid browser_type: 422 with allowed values
   - headless must be boolean or null
   - agent_id must be string or null

2. POST /api/browser/navigate (NavigateRequest):
   - Missing session_id: 422
   - Missing url: 422
   - Invalid URL format: 422 with URL validation error
   - wait_until must be in ["load", "domcontentloaded", "networkidle"]
   - URL max_length enforced (2048 chars)

3. POST /api/browser/screenshot (ScreenshotRequest):
   - Missing session_id: 422
   - full_page must be boolean
   - path must be string or null

4. POST /api/browser/fill-form (FillFormRequest):
   - Missing session_id: 422
   - Missing selectors: 422
   - selectors must be dict with string keys/values
   - submit must be boolean

5. POST /api/browser/click (ClickRequest):
   - Missing session_id: 422
   - Missing selector: 422
   - selector must be non-empty string

6. POST /api/browser/execute-script (ExecuteScriptRequest):
   - Missing session_id: 422
   - Missing script: 422
   - Script max_length enforced (100000 chars)
   - script must be string

7. POST /api/browser/session/close (CloseSessionRequest):
   - Missing session_id: 422
   - session_id format validation

Test each endpoint's Pydantic model constraints.
Use parameterized tests for similar validations.
  </action>
  <verify>pytest tests/test_api_request_validation.py::TestBrowserEndpointValidation -v</verify>
  <done>12+ tests pass covering browser endpoint validation</done>
</task>

<task type="auto">
  <name>Implement device endpoint validation tests</name>
  <files>backend/tests/test_api_request_validation.py</files>
  <action>
Create TestDeviceEndpointValidation class for device request validation:

1. POST /api/devices/camera/snap (CameraSnapRequest):
   - Missing device_node_id: 422
   - resolution format validation (e.g., "1920x1080")
   - Invalid resolution format rejected
   - save_path must be string or null
   - camera_id must be string or null

2. POST /api/devices/screen/record/start (ScreenRecordStartRequest):
   - Missing device_node_id: 422
   - duration_seconds must be positive integer
   - duration_seconds max value (3600 = 1 hour)
   - audio_enabled must be boolean
   - output_format must be in ["mp4", "webm", "gif"]

3. POST /api/devices/location (GetLocationRequest):
   - Missing device_node_id: 422
   - accuracy must be in ["high", "medium", "low"]

4. POST /api/devices/notification (SendNotificationRequest):
   - Missing device_node_id: 422
   - Missing title: 422
   - Missing body: 422
   - title max_length (100 chars)
   - body max_length (500 chars)
   - icon and sound must be strings or null

5. POST /api/devices/execute (ExecuteCommandRequest - critical):
   - Missing device_node_id: 422
   - Missing command: 422
   - command must be non-empty string
   - command max_length (10000 chars)
   - timeout_seconds must be positive (max 300)
   - working_dir must be string or null
   - environment must be dict or null

6. GET /api/devices query parameters:
   - status must be in ["online", "offline", "busy"] or null
   - Invalid status returns 422

7. GET /api/devices/{id} path parameters:
   - Invalid device_node_id format handled

Pay special attention to ExecuteCommandRequest validation (security critical).
Test boundary values (0, max+1, negative).
  </action>
  <verify>pytest tests/test_api_request_validation.py::TestDeviceEndpointValidation -v</verify>
  <done>15+ tests pass covering device endpoint validation</done>
</task>

</tasks>

<verification>
Overall plan verification:
1. Run validation tests: pytest tests/test_api_request_validation.py -v
2. Verify 422 status codes for all validation errors
3. Check error messages include field paths
4. Verify constraint enforcement (length, format, ranges)
5. Test coverage for all Pydantic models
</verification>

<success_criteria>
1. 35+ validation tests created
2. All major API endpoints covered
3. Required field validation tested
4. Type validation tested
5. Constraint validation tested (length, format, range)
6. Error responses include field path and message
7. Tests run in <30 seconds with 100% pass rate
8. No business logic triggered (pure validation)
</success_criteria>

<output>
After completion, create `.planning/phases/102-backend-api-integration-tests/102-05-SUMMARY.md` with:
- Tests created count
- Validation coverage by endpoint
- Any missing validation discovered
- Recommendations for Plan 06
</output>
