---
phase: 102-backend-api-integration-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_api_agent_endpoints.py
  - backend/tests/test_api_integration_fixtures.py
autonomous: true

must_haves:
  truths:
    - "Agent chat endpoint accepts POST requests with message and user_id"
    - "Session creation endpoint returns unique session_id for tracking"
    - "Session history endpoint retrieves conversation messages chronologically"
    - "Agent execution endpoint triggers agent with governance validation"
    - "Request validation rejects malformed messages (empty, too long)"
    - "Response structure follows BaseAPIRouter success/error format"
  artifacts:
    - path: "backend/tests/test_api_agent_endpoints.py"
      provides: "Agent endpoints integration tests (chat, sessions, execution)"
      min_lines: 200
      exports: ["TestAgentChatEndpoints", "TestAgentSessions", "TestAgentExecution"]
    - path: "backend/tests/test_api_integration_fixtures.py"
      provides: "Shared fixtures for API integration testing"
      min_lines: 100
      exports: ["api_test_client", "mock_agent_resolver", "create_test_session"]
  key_links:
    - from: "backend/tests/test_api_agent_endpoints.py"
      to: "backend/core/atom_agent_endpoints.py"
      via: "FastAPI TestClient dependency injection"
      pattern: "from fastapi.testclient import TestClient"
    - from: "backend/tests/test_api_agent_endpoints.py"
      to: "backend/core/chat_session_manager.py"
      via: "Session manager mocking for isolation"
      pattern: "patch.*chat_session_manager"
---

<objective>
Create comprehensive integration tests for agent chat and session endpoints covering request validation, response structure, and error handling.

Purpose: Agent endpoints are the primary interface for LLM interactions and require thorough testing to ensure reliable chat, session management, and agent execution flows.

Output: Integration test file with 40+ tests covering chat, sessions, execution, validation, and errors.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/tests/conftest.py
@backend/core/atom_agent_endpoints.py
@backend/tests/test_browser_automation.py

Reference existing patterns:
- TestClient from FastAPI for endpoint testing
- AsyncMock for async service mocking
- conftest.py fixtures (db_session, test_user, test_agent_*)
- test_browser_automation.py for API test patterns
</context>

<tasks>

<task type="auto">
  <name>Create API integration test fixtures</name>
  <files>backend/tests/test_api_integration_fixtures.py</files>
  <action>
Create shared fixtures file for API integration tests:

1. api_test_client: FastAPI TestClient with authenticated user context
   - Import main app from main.py
   - Create TestClient with dependency override for get_current_user
   - Return client with auth headers pre-configured

2. mock_agent_resolver: Mock AgentContextResolver for testing
   - Return AsyncMock that resolves to test agents by maturity level
   - Support resolve_agent_for_request(agent_id, user_id, action_type)
   - Include governance check mock results

3. create_test_session: Helper to create chat sessions
   - Accept db_session, user_id, optional title
   - Return session dict with session_id, created_at, metadata

4. mock_llm_streaming: AsyncMock for LLM streaming responses
   - Yield tokens one at a time for streaming endpoint tests
   - Support error injection for failure scenarios

Use pytest.fixture decorators with function scope.
Reference conftest.py patterns for fixture consistency.
  </action>
  <verify>pytest tests/test_api_integration_fixtures.py -v --collect-only</verify>
  <done>All fixtures collectable without import errors, api_test_client returns TestClient instance</done>
</task>

<task type="auto">
  <name>Implement agent chat endpoint tests</name>
  <files>backend/tests/test_api_agent_endpoints.py</files>
  <action>
Create TestAgentChatEndpoints class with comprehensive tests:

1. POST /api/atom-agent/chat (success path):
   - Valid message returns 200 with assistant response
   - Response contains session_id, message content, timestamp
   - Conversation history passed correctly when provided
   - Context metadata included in response

2. POST /api/atom-agent/chat (validation):
   - Empty message rejected with 400 error
   - Message exceeding 10000 chars rejected
   - Missing user_id returns 422 validation error
   - Invalid agent_id returns appropriate error

3. POST /api/atom-agent/chat (governance):
   - Student agent receives governance-appropriate responses
   - Intern agent triggers proposal workflow for complex actions
   - Autonomous agent executes without restrictions

4. POST /api/atom-agent/chat (streaming):
   - Streaming endpoint yields Server-Sent Events
   - Tokens arrive incrementally (not buffered)
   - Stream closes with completion event

5. POST /api/atom-agent/chat (errors):
   - LLM timeout returns 503 with retry_after header
   - LLM rate limit returns 429 with retry info
   - Database error returns 500 with error details

Use api_test_client fixture, mock services with patch decorators.
Assert on status_code, response.json() structure, and exact error messages.
  </action>
  <verify>pytest tests/test_api_agent_endpoints.py::TestAgentChatEndpoints -v</verify>
  <done>15+ tests pass covering success, validation, governance, streaming, and error paths</done>
</task>

<task type="auto">
  <name>Implement session management tests</name>
  <files>backend/tests/test_api_agent_endpoints.py</files>
  <action>
Create TestAgentSessions class with session lifecycle tests:

1. GET /api/atom-agent/sessions (list):
   - Returns 200 with sessions array
   - Limit parameter restricts returned count
   - Each session has id, title, date, preview fields
   - Empty result returns [] not null

2. POST /api/atom-agent/sessions (create):
   - Creates session with unique session_id
   - Session includes created_at timestamp
   - User association verified in database

3. GET /api/atom-agent/sessions/{id}/history (retrieve):
   - Returns 200 with messages array
   - Messages ordered chronologically
   - Each message has id, role, content, timestamp, metadata
   - 404 returned for non-existent session_id

4. Session persistence:
   - Messages persist across retrieval calls
   - Multiple users have isolated sessions
   - Session activity updates on chat

Use db_session fixture to verify database state.
Use create_test_session fixture for setup.
  </action>
  <verify>pytest tests/test_api_agent_endpoints.py::TestAgentSessions -v</verify>
  <done>12+ tests pass covering session CRUD, history retrieval, and isolation</done>
</task>

<task type="auto">
  <name>Implement agent execution endpoint tests</name>
  <files>backend/tests/test_api_agent_endpoints.py</files>
  <action>
Create TestAgentExecution class with execution trigger tests:

1. POST /api/atom-agent/execute (success):
   - Valid agent_id and input trigger execution
   - Returns 200 with execution_id and status="running"
   - AgentExecution record created in database
   - Execution linked to agent and workspace

2. POST /api/atom-agent/execute (governance):
   - Student agent blocked from destructive actions
   - Intern agent returns proposal_id for approval
   - Autonomous agent executes immediately
   - Governance check failures return 403

3. GET /api/atom-agent/execute/{id}/status (polling):
   - Returns current execution status
   - Status transitions: running -> completed
   - Includes output_summary when complete
   - 404 for non-existent execution_id

4. POST /api/atom-agent/execute/{id}/stop (cancel):
   - Stops running execution
   - Status changes to "cancelled"
   - Output contains cancellation reason

5. Execute endpoint (validation):
   - Missing agent_id returns 422
   - Invalid execution_id format rejected
   - Empty input_data rejected

Use test_agent_* fixtures from conftest.py.
Mock LLM responses to avoid external dependencies.
Verify database state for audit trail.
  </action>
  <verify>pytest tests/test_api_agent_endpoints.py::TestAgentExecution -v</verify>
  <done>13+ tests pass covering execution lifecycle, governance, status polling, and cancellation</done>
</task>

</tasks>

<verification>
Overall phase verification:
1. Run full test suite: pytest tests/test_api_agent_endpoints.py -v
2. Check coverage: pytest --cov=core/atom_agent_endpoints --cov-report=term-missing
3. Verify all tests pass (40+ total)
4. Confirm coverage >60% for atom_agent_endpoints.py
5. Check response structure consistency (BaseAPIRouter format)
</verification>

<success_criteria>
1. 40+ integration tests created for agent endpoints
2. All endpoints covered: chat, sessions, execute, status
3. Request validation tests for all required fields
4. Governance integration tests for all maturity levels
5. Error handling tests for timeout, rate limit, validation failures
6. Database state verification for audit trail
7. Tests run in <60 seconds with >98% pass rate
8. Coverage >60% for atom_agent_endpoints.py
</success_criteria>

<output>
After completion, create `.planning/phases/102-backend-api-integration-tests/102-01-SUMMARY.md` with:
- Tests created count
- Coverage achieved
- Any blocking issues found
- Recommendations for Plan 02
</output>
