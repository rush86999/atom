---
phase: 102-backend-api-integration-tests
plan: 04
type: execute
wave: 2
depends_on: [102-01, 102-02]
files_modified:
  - backend/tests/test_api_device_routes.py
autonomous: true

must_haves:
  truths:
    - "POST /api/devices/camera/snap requires INTERN+ maturity (governance enforced)"
    - "POST /api/devices/screen/record/start requires SUPERVISED+ maturity"
    - "POST /api/devices/execute requires AUTONOMOUS maturity (security critical)"
    - "All device actions create DeviceAudit records with action_type and success status"
    - "GET /api/devices lists user's devices with status filtering"
    - "Request validation rejects invalid device_node_id and malformed parameters"
  artifacts:
    - path: "backend/tests/test_api_device_routes.py"
      provides: "Device routes integration tests (camera, screen, location, notification, execute)"
      min_lines: 280
      exports: ["TestDeviceCamera", "TestDeviceScreenRecord", "TestDeviceLocation", "TestDeviceNotification", "TestDeviceExecute", "TestDeviceList"]
  key_links:
    - from: "backend/tests/test_api_device_routes.py"
      to: "backend/api/device_capabilities.py"
      via: "FastAPI TestClient dependency injection"
      pattern: "from fastapi.testclient import TestClient"
    - from: "backend/tests/test_api_device_routes.py"
      to: "backend/core/models.py"
      via: "DeviceAudit, DeviceNode model verification"
      pattern: "DeviceAudit.*action_type.*device_node_id"
    - from: "backend/tests/test_api_device_routes.py"
      to: "backend/tools/device_tool.py"
      via: "Device tool function mocking"
      pattern: "patch.*device_tool"
---

<objective>
Create integration tests for device capabilities routes covering camera, screen recording, location, notifications, and command execution with maturity-based governance validation.

Purpose: Device capabilities are security-sensitive operations with varying maturity requirements (INTERN+ for camera/location, SUPERVISED+ for screen recording, AUTONOMOUS only for command execution). Tests must verify governance enforcement at each maturity threshold.

Output: Integration test file with 40+ tests covering all device operations with governance validation.
</object>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-down/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/tests/conftest.py
@backend/api/device_capabilities.py
@backend/tools/device_tool.py
@backend/tests/test_device_tool.py (for device tool patterns)

Device maturity requirements:
- Camera snap: INTERN+ (complexity 2)
- Location: INTERN+ (complexity 2)
- Notifications: INTERN+ (complexity 2)
- Screen recording: SUPERVISED+ (complexity 3)
- Command execution: AUTONOMOUS only (complexity 4 - security critical)

All actions create DeviceAudit records.
Agent resolution via AgentContextResolver.
User ownership verification for device access.
</context>

<tasks>

<task type="auto">
  <name>Implement device camera and location tests</name>
  <files>backend/tests/test_api_device_routes.py</files>
  <action>
Create TestDeviceCamera and TestDeviceLocation classes:

1. POST /api/devices/camera/snap (success):
   - Captures photo with optional camera_id and resolution
   - Returns file_path, device_node_id, resolution
   - DeviceAudit created with action_type="camera_snap"
   - Success status and duration_ms recorded

2. POST /api/devices/camera/snap (governance):
   - STUDENT agent blocked (INTERN+ required)
   - INTERN+ agents can capture
   - Governance check failure returns permission_denied_error
   - Blocked actions create audit with success=False

3. POST /api/devices/location (success):
   - Gets device location with accuracy option
   - Returns latitude, longitude, accuracy
   - DeviceAudit created with action_type="get_location"
   - governance_check_passed reflects governance result

4. POST /api/devices/location (governance):
   - STUDENT agent blocked (INTERN+ required)
   - Location not retrieved without governance pass
   - Error response includes required maturity level

5. Request validation (both endpoints):
   - device_node_id required
   - Resolution format validated (e.g., "1920x1080")
   - Accuracy must be valid value (high/medium/low)
   - Returns 422 for malformed requests

Use test_agent_student, test_agent_intern fixtures.
Mock device_tool.device_camera_snap and device_get_location.
Verify DeviceAudit records in database.
  </action>
  <verify>pytest tests/test_api_device_routes.py::TestDeviceCamera -v && pytest tests/test_api_device_routes.py::TestDeviceLocation -v</verify>
  <done>12+ tests pass covering camera and location with governance</done>
</task>

<task type="auto">
  <name>Implement screen recording tests</name>
  <files>backend/tests/test_api_device_routes.py</files>
  <action>
Create TestDeviceScreenRecord class with recording tests:

1. POST /api/devices/screen/record/start (success):
   - Starts recording with duration and audio options
   - Returns session_id, device_node_id, output_format
   - DeviceAudit created with action_type="screen_record_start"
   - Database session not applicable (use DeviceAudit for tracking)

2. POST /api/devices/screen/record/start (governance):
   - STUDENT agent blocked (SUPERVISED+ required)
   - INTERN agent blocked (SUPERVISED+ required)
   - SUPERVISED and AUTONOMOUS agents can start recording
   - Governance failure returns maturity level in error

3. POST /api/devices/screen/record/stop (success):
   - Stops recording session
   - Returns file_path, duration_seconds
   - DeviceAudit created with action_type="screen_record_stop"
   - Duration calculated from start time

4. POST /api/devices/screen/record/stop (validation):
   - session_id required
   - Returns error for non-existent session
   - Handles already-stopped sessions gracefully

5. Recording lifecycle:
   - Start creates session_id
   - Stop uses same session_id
   - Audit trail shows start and stop events
   - File path consistent between start and stop

Use test_agent_supervised fixture for SUPERVISED tests.
Mock device_screen_record_start and device_screen_record_stop.
Verify session_id consistency.
  </action>
  <verify>pytest tests/test_api_device_routes.py::TestDeviceScreenRecord -v</verify>
  <done>10+ tests pass covering screen recording lifecycle</done>
</task>

<task type="auto">
  <name>Implement notification and command execution tests</name>
  <files>backend/tests/test_api_device_routes.py</files>
  <action>
Create TestDeviceNotification and TestDeviceExecute classes:

1. POST /api/devices/notification (success):
   - Sends notification with title and body
   - Optional icon and sound parameters
   - Returns success confirmation
   - DeviceAudit created with action_type="send_notification"

2. POST /api/devices/notification (governance):
   - STUDENT agent blocked (INTERN+ required)
   - INTERN+ agents can send notifications
   - Governance check verified before sending

3. POST /api/devices/execute (AUTONOMOUS only):
   - Executes shell command on device
   - AUTONOMOUS agents only (strictest governance)
   - Command whitelist enforced
   - Timeout applied (max 300s)
   - Returns exit_code, stdout, stderr

4. POST /api/devices/execute (governance - strict):
   - STUDENT blocked (AUTONOMOUS required)
   - INTERN blocked (AUTONOMOUS required)
   - SUPERVISED blocked (AUTONOMOUS required)
   - Only AUTONOMOUS status allows execution
   - Error message explicitly states AUTONOMOUS requirement

5. POST /api/devices/execute (security):
   - Command cannot be empty
   - Dangerous commands blocked by whitelist
   - Working directory restricted
   - Timeout enforced (max 300s)
   - No interactive shell access

6. POST /api/devices/execute (response):
   - Returns exit_code (0 for success)
   - stdout captured in response
   - stderr captured for errors
   - execution_time recorded

Use test_agent_autonomous for command execution tests.
Mock device_execute_command.
Verify governance_denied_error for non-autonomous agents.
  </action>
  <verify>pytest tests/test_api_device_routes.py::TestDeviceNotification -v && pytest tests/test_api_device_routes.py::TestDeviceExecute -v</verify>
  <done>12+ tests pass covering notifications and command execution</done>
</task>

<task type="auto">
  <name>Implement device listing and audit tests</name>
  <files>backend/tests/test_api_device_routes.py</files>
  <action>
Create TestDeviceList and TestDeviceAudit classes:

1. GET /api/devices (list):
   - Returns array of user's devices
   - Each device has id, device_id, name, status, capabilities
   - Optional status filter (online/offline/busy)
   - Empty array when no devices (not null)

2. GET /api/devices/{device_node_id} (info):
   - Returns device information
   - User ownership verified (403 if not owned)
   - 404 for non-existent device
   - Includes platform, capabilities, last_seen

3. GET /api/devices/{device_node_id}/audit:
   - Returns audit trail for device
   - Limited by limit parameter (default 100)
   - Ordered by created_at DESC
   - Each entry has action_type, success, result_summary, error_message
   - User ownership verified

4. GET /api/devices/sessions/active:
   - Returns active device sessions
   - Filtered by current user
   - Each session has session_id, session_type, status, device_node_id
   - Empty array when no active sessions

5. Device audit completeness:
   - All device actions create audit entries
   - governance_check_passed reflects actual check
   - agent_id populated when agent-initiated
   - duration_ms recorded
   - file_path included for camera/screen operations

6. User ownership verification:
   - Users can only access their own devices
   - 403 returned for unauthorized access
   - Ownership checked via DeviceNode.user_id

Create test_device_node fixture for device ownership tests.
Use db_session to verify DeviceNode and DeviceAudit records.
  </action>
  <verify>pytest tests/test_api_device_routes.py::TestDeviceList -v && pytest tests/test_api_device_routes.py::TestDeviceAudit -v</verify>
  <done>10+ tests pass covering device listing, info, audit, and ownership</done>
</task>

</tasks>

<verification>
Overall plan verification:
1. Run device tests: pytest tests/test_api_device_routes.py -v
2. Check coverage: pytest --cov=api/device_capabilities --cov-report=term-missing
3. Verify all tests pass (40+ total)
4. Confirm coverage >60% for device_capabilities.py
5. Verify governance enforcement at each maturity threshold
6. Check AUTONOMOUS-only enforcement for command execution
</verification>

<success_criteria>
1. 40+ integration tests created for device routes
2. All device operations tested (camera, screen, location, notification, execute)
3. Governance thresholds verified (INTERN+, SUPERVISED+, AUTONOMOUS only)
4. DeviceAudit record creation verified for all actions
5. User ownership verification tested
6. Request validation tests for all required fields
7. Security tests for command execution (whitelist, timeout)
8. Tests run in <60 seconds with >98% pass rate
9. Coverage >60% for device_capabilities.py
</success_criteria>

<output>
After completion, create `.planning/phases/102-backend-api-integration-tests/102-04-SUMMARY.md` with:
- Tests created count
- Coverage achieved
- Maturity threshold verification results
- Recommendations for Plan 05
</output>
