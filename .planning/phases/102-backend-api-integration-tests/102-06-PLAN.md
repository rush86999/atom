---
phase: 102-backend-api-integration-tests
plan: 06
type: execute
wave: 4
depends_on: [102-01, 102-02, 102-03, 102-04, 102-05]
files_modified:
  - backend/tests/test_api_database_transactions.py
  - backend/tests/test_api_coverage_summary.py
autonomous: true

must_haves:
  truths:
    - "Database transactions roll back on error, preventing partial state"
    - "Concurrent API requests handle race conditions correctly"
    - "Audit records created atomically with business operations"
    - "All API routes covered with 60%+ test coverage"
    - "Coverage report generated showing per-endpoint breakdown"
    - "Phase success criteria verified and documented"
  artifacts:
    - path: "backend/tests/test_api_database_transactions.py"
      provides: "Database transaction and rollback tests for API endpoints"
      min_lines: 180
      exports: ["TestTransactionRollback", "TestConcurrentRequests", "TestAuditAtomicity"]
    - path: "backend/tests/test_api_coverage_summary.py"
      provides: "Coverage summary and phase verification"
      min_lines: 100
      exports: ["generate_coverage_report", "verify_phase_success_criteria"]
  key_links:
    - from: "backend/tests/test_api_database_transactions.py"
      to: "backend/core/database.py"
      via: "Session and transaction management"
      pattern: "db_session.commit|db_session.rollback"
    - from: "backend/tests/test_api_database_transactions.py"
      to: "backend/api/canvas_routes.py"
      via: "Transaction rollback on submission failure"
      pattern: "try.*commit.*except.*rollback"
    - from: "backend/tests/test_api_coverage_summary.py"
      to: "backend/tests/coverage_reports/metrics/coverage.json"
      via: "Coverage data aggregation"
      pattern: "coverage.*totals.*percent_covered"
---

<objective>
Create database transaction tests to verify rollback behavior, concurrent request handling, and audit atomicity. Generate coverage summary and verify all phase success criteria.

Purpose: Database transactions are critical for data integrity. Tests must verify that API endpoints properly roll back on errors, handle concurrent requests safely, and create audit records atomically with business operations.

Output: Transaction tests with 20+ tests, coverage summary report, and phase verification document.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-down/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/tests/conftest.py (db_session fixture)
@backend/core/database.py (Session management)
@backend/api/canvas_routes.py (transaction patterns)
@backend/api/browser_routes.py (audit creation patterns)

Transaction patterns in codebase:
- db_session: Session = Depends(get_db) provides transaction scope
- Explicit db.commit() after successful operations
- Exception handling with rollback
- Audit records created within same transaction
</context>

<tasks>

<task type="auto">
  <name>Implement transaction rollback tests</name>
  <files>backend/tests/test_api_database_transactions.py</files>
  <action>
Create TestTransactionRollback class with rollback tests:

1. POST /api/canvas/submit (rollback on error):
   - Simulate database error during CanvasAudit creation
   - Verify AgentExecution not created (rolled back)
   - Verify no partial data in database
   - Verify appropriate error response to client

2. POST /api/browser/session/create (rollback on error):
   - Simulate BrowserSession creation failure
   - Verify no orphaned records
   - Verify audit not created when session fails
   - Verify clean state after failure

3. POST /api/devices/execute (rollback on security violation):
   - Simulate command execution blocked by governance
   - Verify DeviceAudit created with failure status
   - Verify no command execution state leaked
   - Verify transaction atomicity maintained

4. POST /api/atom-agent/execute (rollback on agent error):
   - Simulate agent execution failure
   - Verify AgentExecution marked as failed
   - Verify no partial state in other tables
   - Verify audit trail reflects failure

5. Multi-table operations (rollback cascades):
   - Operation touching multiple tables fails mid-way
   - Verify all related changes rolled back
   - Verify referential integrity maintained
   - Verify no orphaned records

Use patch side_effect to simulate database errors.
Use db_session to verify clean state.
Test with explicit db.flush() to trigger errors before commit.
  </action>
  <verify>pytest tests/test_api_database_transactions.py::TestTransactionRollback -v</verify>
  <done>8+ tests pass covering rollback scenarios</done>
</task>

<task type="auto">
  <name>Implement concurrent request tests</name>
  <files>backend/tests/test_api_database_transactions.py</files>
  <action>
Create TestConcurrentRequests class with concurrency tests:

1. Concurrent form submissions:
   - Submit same canvas from multiple "users" simultaneously
   - Verify all submissions create separate CanvasAudit records
   - Verify no lost updates (all form_data preserved)
   - Verify sequential IDs assigned correctly

2. Concurrent agent executions:
   - Trigger same agent from multiple requests
   - Verify unique AgentExecution records created
   - Verify no race condition in agent selection
   - Verify governance checks applied consistently

3. Concurrent session creation:
   - Create multiple browser sessions simultaneously
   - Verify unique session_ids assigned
   - Verify no session_id collisions
   - Verify all sessions persisted correctly

4. Concurrent audit creation:
   - Multiple actions creating audit records simultaneously
   - Verify all audits persisted
   - Verify audit IDs are unique
   - Verify no lost audit entries

Use asyncio.gather() for concurrent requests.
Use unique_resource_name fixture for parallel safety.
Verify database state after concurrent operations complete.
  </action>
  <verify>pytest tests/test_api_database_transactions.py::TestConcurrentRequests -v</verify>
  <done>6+ tests pass covering concurrent operations</done>
</task>

<task type="auto">
  <name>Implement audit atomicity tests</name>
  <files>backend/tests/test_api_database_transactions.py</files>
  <action>
Create TestAuditAtomicity class with audit consistency tests:

1. Canvas submission audit atomicity:
   - CanvasAudit created in same transaction as AgentExecution
   - Both succeed or both fail (no partial state)
   - governance_check_passed consistent across records
   - Timestamps consistent (created_at close)

2. Browser action audit atomicity:
   - BrowserAudit created with BrowserSession update
   - Audit creation failure doesn't prevent session update
   - Session update failure rolls back audit
   - Audit includes all expected fields

3. Device action audit atomicity:
   - DeviceAudit created with device action
   - Audit failure doesn't prevent action (best-effort)
   - Action failure prevents audit creation
   - Audit includes result_summary and error_message

4. Audit trail completeness:
   - Every action creates exactly one audit
   - No missing audit entries
   - No duplicate audit entries
   - Audit fields populated correctly (action_type, success, duration_ms)

5. Audit ownership:
   - All audits have user_id set
   - Agent audits have agent_id set
   - Session audits have session_id set
   - Cross-user isolation verified

Verify transaction boundaries with db_session queries.
Check audit counts match operation counts.
Verify audit record content consistency.
  </action>
  <verify>pytest tests/test_api_database_transactions.py::TestAuditAtomicity -v</verify>
  <done>6+ tests pass covering audit atomicity</done>
</task>

<task type="auto">
  <name>Generate coverage summary and verify phase</name>
  <files>backend/tests/test_api_coverage_summary.py</files>
  <action>
Create coverage summary and verification module:

1. generate_coverage_report() function:
   - Run pytest with --cov for all API routes
   - Aggregate coverage by endpoint file:
     * core/atom_agent_endpoints.py
     * api/canvas_routes.py
     * api/browser_routes.py
     * api/device_capabilities.py
   - Calculate average coverage across all API files
   - Generate per-file breakdown with line counts
   - Output formatted report to console

2. verify_phase_success_criteria() function:
   - Check all test files exist and are importable
   - Verify test counts per plan:
     * Plan 01: 40+ agent endpoint tests
     * Plan 02: 30+ canvas tests
     * Plan 03: 35+ browser tests
     * Plan 04: 40+ device tests
     * Plan 05: 35+ validation tests
     * Plan 06: 20+ transaction tests
   - Verify coverage >60% for each API file
   - Verify overall pass rate >98%
   - Document any gaps or issues found

3. Test execution summary:
   - Total tests created
   - Total assertions
   - Pass rate
   - Execution time
   - Coverage percentage

Use pytest --cov with JSON output for parsing.
Check for existence of test files before running.
Generate human-readable summary.
  </action>
  <verify>pytest tests/test_api_coverage_summary.py -v && python -c "from backend.tests.test_api_coverage_summary import generate_coverage_report, verify_phase_success_criteria; generate_coverage_report(); verify_phase_success_criteria()"</verify>
  <done>Coverage report generated, success criteria verified</done>
</task>

</tasks>

<verification>
Overall phase verification:
1. Run transaction tests: pytest tests/test_api_database_transactions.py -v
2. Run coverage summary: pytest tests/test_api_coverage_summary.py -v
3. Run full API test suite: pytest tests/test_api_*.py -v
4. Check coverage: pytest --cov=api --cov=core/atom_agent_endpoints --cov-report=term-missing
5. Verify all success criteria met
6. Document results in phase summary
</verification>

<success_criteria>
1. 20+ transaction tests created (rollback, concurrency, atomicity)
2. All rollback scenarios tested
3. Concurrent request handling verified
4. Audit atomicity verified
5. Coverage report generated showing per-file breakdown
6. All API files have 60%+ coverage
7. Overall test pass rate >98%
8. Phase success criteria documented
9. Tests run in <90 seconds total
</success_criteria>

<output>
After completion, create `.planning/phases/102-backend-api-integration-tests/102-06-SUMMARY.md` with:
- Total tests created (all plans)
- Coverage achieved per API file
- Transaction test results
- Success criteria verification status
- Recommendations for Phase 103
- Overall phase assessment (COMPLETE/PARTIAL)
</output>
