---
phase: 102-backend-api-integration-tests
plan: 03
type: execute
wave: 2
depends_on: [102-01, 102-02]
files_modified:
  - backend/tests/test_api_browser_routes.py
autonomous: true

must_haves:
  truths:
    - "POST /api/browser/session/create creates BrowserSession with governance check"
    - "POST /api/browser/navigate requires INTERN+ maturity for agent-initiated navigation"
    - "Browser actions (screenshot, click, fill-form) create BrowserAudit records"
    - "GET /api/browser/sessions lists user's browser sessions"
    - "Request validation rejects invalid session_id and malformed URLs"
    - "Governance blocking creates audit entry with governance_check_passed=False"
  artifacts:
    - path: "backend/tests/test_api_browser_routes.py"
      provides: "Browser routes integration tests (session, navigate, actions, audit)"
      min_lines: 250
      exports: ["TestBrowserSession", "TestBrowserNavigation", "TestBrowserActions", "TestBrowserAudit"]
  key_links:
    - from: "backend/tests/test_api_browser_routes.py"
      to: "backend/api/browser_routes.py"
      via: "FastAPI TestClient dependency injection"
      pattern: "from fastapi.testclient import TestClient"
    - from: "backend/tests/test_api_browser_routes.py"
      to: "backend/core/models.py"
      via: "BrowserAudit, BrowserSession model verification"
      pattern: "BrowserAudit.*action_type.*session_id"
    - from: "backend/tests/test_api_browser_routes.py"
      to: "backend/tools/browser_tool.py"
      via: "Browser tool function mocking"
      pattern: "patch.*browser_tool"
---

<objective>
Create integration tests for browser automation routes covering session creation, navigation, actions (screenshot, click, fill-form), and audit trail creation with governance validation.

Purpose: Browser automation is a high-risk capability requiring INTERN+ maturity for all actions. Tests must verify governance enforcement, audit trail completeness, and error handling.

Output: Integration test file with 35+ tests covering browser session lifecycle, navigation, actions, and audit.
</object>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-down/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/tests/conftest.py
@backend/api/browser_routes.py
@backend/tools/browser_tool.py
@backend/tests/test_browser_automation.py (for browser tool patterns)

Browser routes patterns:
- All actions require INTERN+ maturity via governance check
- BrowserAudit records created for all actions
- Database session records updated with current_url, page_title
- Request models: CreateSessionRequest, NavigateRequest, ScreenshotRequest, etc.
- Response uses BaseAPIRouter format
</context>

<tasks>

<task type="auto">
  <name>Implement browser session tests</name>
  <files>backend/tests/test_api_browser_routes.py</files>
  <action>
Create TestBrowserSession class with session lifecycle tests:

1. POST /api/browser/session/create (success - no agent):
   - Creates session without governance when no agent_id
   - Returns session_id, browser_type, headless status
   - BrowserSession record created in database
   - status="active", metadata_json contains creation info

2. POST /api/browser/session/create (success - with agent):
   - AUTONOMOUS, SUPERVISED, INTERN agents pass governance
   - STUDENT agent blocked (INTERN+ required)
   - Agent_id linked to BrowserSession record
   - Governance check verified before creation

3. POST /api/browser/session/create (validation):
   - Invalid browser_type rejected (must be chromium/firefox/webkit)
   - headless must be boolean or null
   - Returns appropriate validation error (422)

4. GET /api/browser/sessions (list):
   - Returns array of user's browser sessions
   - Each session has session_id, browser_type, status, current_url
   - Limited to 50 most recent sessions
   - Sessions ordered by created_at DESC

5. GET /api/browser/session/{id}/info (retrieve):
   - Returns session information including DB status
   - Includes db_session_id, created_at, status
   - 404 for non-existent session_id

Use test_agent_* fixtures for governance testing.
Mock browser_tool.browser_create_session.
Verify BrowserSession database records.
  </action>
  <verify>pytest tests/test_api_browser_routes.py::TestBrowserSession -v</verify>
  <done>12+ tests pass covering session creation, listing, and retrieval</done>
</task>

<task type="auto">
  <name>Implement browser navigation tests</name>
  <files>backend/tests/test_api_browser_routes.py</files>
<action>
Create TestBrowserNavigation class with navigation tests:

1. POST /api/browser/navigate (success - no agent):
   - Navigates to valid URL without governance when no agent_id
   - Returns success with url, title, load_time
   - BrowserAudit record created with action_type="navigate"
   - Database session updated with current_url and page_title

2. POST /api/browser/navigate (governance enforcement):
   - AUTONOMOUS, SUPERVISED, INTERN agents can navigate
   - STUDENT agent blocked with governance_denied_error
   - Blocked navigation creates audit with governance_check_passed=False
   - AgentExecution created for successful navigation

3. POST /api/browser/navigate (request validation):
   - session_id required (422 if missing)
   - url required (422 if missing)
   - Invalid URL format rejected
   - wait_until must be valid value (load/domcontentloaded/networkidle)

4. POST /api/browser/navigate (error handling):
   - Invalid session_id returns appropriate error
   - Navigation timeout handled gracefully
   - Page load errors returned with details

5. POST /api/browser/navigate (audit trail):
   - BrowserAudit includes action_target (URL)
   - action_params includes wait_until setting
   - duration_ms recorded
   - success reflects actual navigation result

Use patch for browser_navigate tool function.
Verify database state for BrowserSession and BrowserAudit.
Test with test_agent_student, test_agent_intern fixtures.
  </action>
  <verify>pytest tests/test_api_browser_routes.py::TestBrowserNavigation -v</verify>
  <done>10+ tests pass covering navigation paths, governance, and audit</done>
</task>

<task type="auto">
  <name>Implement browser action tests (screenshot, click, fill-form, etc.)</name>
  <files>backend/tests/test_api_browser_routes.py</files>
  <action>
Create TestBrowserActions class with action endpoint tests:

1. POST /api/browser/screenshot:
   - Captures screenshot with full_page option
   - Returns file_path or base64 data
   - Audit created with action_type="screenshot"
   - Duration_ms and file size recorded

2. POST /api/browser/click:
   - Clicks element at CSS selector
   - wait_for option supported
   - Audit includes action_target (selector)
   - Governance enforced (INTERN+)

3. POST /api/browser/fill-form:
   - Fills multiple form fields via selectors dict
   - submit option triggers form submission
   - Form submission requires SUPERVISED+ (higher governance)
   - Audit includes field count in action_params

4. POST /api/browser/extract-text:
   - Extracts text from page or specific element
   - Selector optional (full page if omitted)
   - Returns text length in result_summary
   - Audit includes extracted char count

5. POST /api/browser/execute-script:
   - Executes JavaScript in browser context
   - SUPERVISED+ maturity required
   - Script length logged (not full script for security)
   - Audit includes script length not content

6. POST /api/browser/session/close:
   - Closes active browser session
   - Database session status updated to "closed"
   - closed_at timestamp set
   - Audit created for closure

For each action:
- Test success path without agent
- Test governance enforcement with STUDENT agent
- Test success with INTERN+ agent
- Verify audit record created
- Check response structure

Mock all browser_tool.* functions.
Use AsyncMock for async tool functions.
  </action>
  <verify>pytest tests/test_api_browser_routes.py::TestBrowserActions -v</verify>
  <done>18+ tests pass covering all browser actions with governance</done>
</task>

<task type="auto">
  <name>Implement browser audit and error tests</name>
  <files>backend/tests/test_api_browser_routes.py</files>
  <action>
Create TestBrowserAudit class with audit trail tests:

1. GET /api/browser/audit (retrieve):
   - Returns audit entries for current user
   - Optional session_id filter works correctly
   - Limit parameter restricts count (default 100)
   - Ordered by created_at DESC (newest first)
   - Each entry has id, action_type, success, result_summary

2. Audit record completeness:
   - All actions create audit entries
   - governance_check_passed reflects actual governance result
   - agent_id populated when agent initiated
   - duration_ms recorded for all actions
   - result_data included on success

3. Error scenario auditing:
   - Failed actions have success=False
   - error_message populated on failure
   - Governance blocked actions audit with reason

4. Database error handling:
   - Session creation failure on DB error returns appropriate error
   - Navigation failure doesn't create orphaned audit records
   - Transaction rollback on database errors

5. Session cleanup on errors:
   - Failed session creation doesn't leave zombie records
   - Session close failure logged but audit created

Use db_session fixture to verify audit records.
Test error injection with side_effect.
  </action>
  <verify>pytest tests/test_api_browser_routes.py::TestBrowserAudit -v</verify>
  <done>8+ tests pass covering audit retrieval and error scenarios</done>
</task>

</tasks>

<verification>
Overall plan verification:
1. Run browser tests: pytest tests/test_api_browser_routes.py -v
2. Check coverage: pytest --cov=api/browser_routes --cov-report=term-missing
3. Verify all tests pass (35+ total)
4. Confirm coverage >60% for browser_routes.py
5. Verify governance enforcement at maturity boundaries
6. Check audit trail completeness
</verification>

<success_criteria>
1. 35+ integration tests created for browser routes
2. All browser actions tested (create, navigate, screenshot, click, fill-form, extract, execute-script, close)
3. Governance enforcement verified for all maturity levels
4. BrowserAudit record creation verified for all actions
5. Request validation tests for all required fields
6. Error handling tests for database and browser failures
7. Tests run in <60 seconds with >98% pass rate
8. Coverage >60% for browser_routes.py
</success_criteria>

<output>
After completion, create `.planning/phases/102-backend-api-integration-tests/102-03-SUMMARY.md` with:
- Tests created count
- Coverage achieved
- Governance blocking scenarios verified
- Recommendations for Plan 04
</output>
