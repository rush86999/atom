---
phase: 102-backend-api-integration-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_api_canvas_routes.py
autonomous: true

must_haves:
  truths:
    - "POST /api/canvas/submit accepts form_data and creates CanvasAudit record"
    - "Form submission governance check validates agent maturity level"
    - "GET /api/canvas/status returns active status and feature list"
    - "Request validation rejects empty canvas_id or malformed form_data"
    - "WebSocket broadcast triggered on successful form submission"
    - "Agent execution linked to originating submission"
  artifacts:
    - path: "backend/tests/test_api_canvas_routes.py"
      provides: "Canvas routes integration tests (submit, status, governance)"
      min_lines: 180
      exports: ["TestCanvasSubmit", "TestCanvasStatus", "CanvasRequestFixtures"]
  key_links:
    - from: "backend/tests/test_api_canvas_routes.py"
      to: "backend/api/canvas_routes.py"
      via: "FastAPI TestClient dependency injection"
      pattern: "from fastapi.testclient import TestClient"
    - from: "backend/tests/test_api_canvas_routes.py"
      to: "backend/core/models.py"
      via: "CanvasAudit model verification"
      pattern: "CanvasAudit.*canvas_id.*action"
    - from: "backend/tests/test_api_canvas_routes.py"
      to: "backend/core/websockets.py"
      via: "WebSocket manager mocking"
      pattern: "patch.*ws_manager"
---

<objective>
Create integration tests for canvas form submission and status endpoints covering governance validation, audit trail creation, and WebSocket notifications.

Purpose: Canvas form submissions require governance checks (SUPERVISED+ for submit_form), create audit records, and trigger WebSocket notifications—critical paths requiring comprehensive testing.

Output: Integration test file with 30+ tests covering form submission, status, governance, and errors.
</object>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-down/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-down/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/tests/conftest.py
@backend/api/canvas_routes.py
@backend/core/models.py (CanvasAudit, AgentRegistry, User)
@backend/tests/test_api_agent_endpoints.py (for shared fixture patterns)

Reference canvas routes structure:
- FormSubmission model: canvas_id, form_data, agent_execution_id, agent_id
- Governance enforcement via FeatureFlags.should_enforce_governance('form')
- CanvasAudit record creation with governance_check_passed
- WebSocket broadcast via ws_manager.broadcast()
</context>

<tasks>

<task type="auto">
  <name>Implement canvas form submission tests</name>
  <files>backend/tests/test_api_canvas_routes.py</files>
  <action>
Create TestCanvasSubmit class with form submission tests:

1. POST /api/canvas/submit (success - no agent):
   - Valid canvas_id and form_data return 200
   - Response contains submission_id (CanvasAudit.id)
   - CanvasAudit record created in database
   - governance_check is null when no agent_id
   - WebSocket broadcast triggered to user channel

2. POST /api/canvas/submit (success - with agent):
   - AUTONOMOUS agent bypasses governance
   - SUPERVISED agent passes governance check
   - AgentExecution record created for submission
   - submission_execution_id returned in response
   - governance_check.allowed = True in response

3. POST /api/canvas/submit (governance blocked):
   - STUDENT agent blocked (maturity < 0.5)
   - INTERN agent requires approval for submit_form (complexity 3)
   - Returns governance_denied_error with maturity level
   - CanvasAudit record created with governance_check_passed = False
   - No AgentExecution created when blocked

4. POST /api/canvas/submit (originating execution):
   - agent_execution_id links to original execution
   - Agent resolved from originating execution if agent_id not provided
   - Audit record includes originating_execution_id in metadata

5. POST /api/canvas/submit (validation):
   - Empty canvas_id rejected (400/422)
   - Empty form_data dict rejected
   - Missing user_id handled by dependency injection
   - Malformed form_data (non-dict) rejected

Use test_agent_autonomous, test_agent_student, test_agent_intern fixtures.
Mock ws_manager.broadcast with AsyncMock.
Verify CanvasAudit records in database.
  </action>
  <verify>pytest tests/test_api_canvas_routes.py::TestCanvasSubmit -v</verify>
  <done>15+ tests pass covering submission paths, governance, and validation</done>
</task>

<task type="auto">
  <name>Implement canvas status and WebSocket tests</name>
  <files>backend/tests/test_api_canvas_routes.py</files>
  <action>
Create TestCanvasStatus and CanvasWebSocketTests classes:

1. GET /api/canvas/status (success):
   - Returns 200 with status="active"
   - Response includes user_id from current_user
   - Features list includes: markdown, status_panel, form, charts
   - Response follows BaseAPIRouter success format

2. GET /api/canvas/status (authentication):
   - Requires authenticated user (via get_current_user)
   - 401 returned without auth token
   - User context properly injected

3. WebSocket broadcast verification:
   - ws_manager.broadcast called with user channel
   - Broadcast includes type: "canvas:form_submitted"
   - Broadcast data includes canvas_id, form_data, user_id
   - Broadcast includes agent_id and submission_id when agent present
   - Broadcast includes governance_check result

4. Form submission execution lifecycle:
   - Submission execution created with status="running"
   - Execution marked "completed" after successful submission
   - duration_seconds calculated on completion
   - governance.record_outcome called for agent

5. Status endpoint features:
   - All expected features present in list
   - Feature names are strings (not translated)
   - Consistent feature set across calls

Use AsyncMock for ws_manager.broadcast.
Patch governance.record_outcome for verification.
  </action>
  <verify>pytest tests/test_api_canvas_routes.py::TestCanvasStatus -v && pytest tests/test_api_canvas_routes.py::CanvasWebSocketTests -v</verify>
  <done>12+ tests pass covering status endpoint, WebSocket broadcast, and execution lifecycle</done>
</task>

<task type="auto">
  <name>Add canvas request fixtures and error tests</name>
  <files>backend/tests/test_api_canvas_routes.py</files>
  <action>
Create CanvasRequestFixtures and error handling tests:

1. canvas_submission_request fixture:
   - Returns valid FormSubmission dict
   - Accepts overrides for canvas_id, form_data, agent_id
   - Generates unique canvas_id using uuid

2. mock_canvas_governance fixture:
   - Mocks AgentGovernanceService.can_perform_action
   - Configurable return values (allowed/blocked)
   - Returns governance_check dict with reason

3. POST /api/canvas/submit (database errors):
   - Database connection failure returns 500
   - Constraint violation handled gracefully
   - Transaction rollback on error

4. POST /api/canvas/submit (WebSocket errors):
   - WebSocket broadcast failure doesn't block submission
   - Error logged but submission succeeds
   - Response includes warning about broadcast failure

5. Execution completion errors:
   - Failed completion marking doesn't affect submission
   - Error logged appropriately
   - Response still returns success with submission_id

Use patch for database session errors.
Use side_effect for simulating database failures.
Verify error responses include actionable messages.
  </action>
  <verify>pytest tests/test_api_canvas_routes.py -k "error or fixture" -v</verify>
  <done>6+ tests pass covering error paths, 2 fixtures created and usable</done>
</task>

</tasks>

<verification>
Overall plan verification:
1. Run canvas tests: pytest tests/test_api_canvas_routes.py -v
2. Check coverage: pytest --cov=api/canvas_routes --cov-report=term-missing
3. Verify all tests pass (30+ total)
4. Confirm coverage >60% for canvas_routes.py
5. Check CanvasAudit records created correctly
6. Verify governance checks enforced
</verification>

<success_criteria>
1. 30+ integration tests created for canvas routes
2. Form submission tested for all governance paths (AUTONOMOUS, SUPERVISED, STUDENT, INTERN)
3. CanvasAudit record creation verified
4. WebSocket broadcast verification complete
5. Request validation tests for all required fields
6. Error handling tests for database and WebSocket failures
7. Tests run in <45 seconds with >98% pass rate
8. Coverage >60% for canvas_routes.py
</success_criteria>

<output>
After completion, create `.planning/phases/102-backend-api-integration-tests/102-02-SUMMARY.md` with:
- Tests created count
- Coverage achieved
- Governance blocking scenarios verified
- Recommendations for Plan 03
</output>
