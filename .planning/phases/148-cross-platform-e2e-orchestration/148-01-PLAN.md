---
phase: 148-cross-platform-e2e-orchestration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/e2e-unified.yml
  - backend/tests/scripts/e2e_aggregator.py
autonomous: true

must_haves:
  truths:
    - "CI workflow runs all three platform E2E tests in parallel"
    - "E2E test results are downloaded from all platform jobs"
    - "Aggregation script processes results from all three platforms"
    - "Unified E2E report contains platform breakdown and aggregate metrics"
    - "E2E report is uploaded as CI artifact for historical tracking"
  artifacts:
    - path: ".github/workflows/e2e-unified.yml"
      provides: "GitHub Actions workflow orchestrating all E2E tests"
      contains: "jobs: e2e-web, e2e-mobile (API-level), e2e-desktop, aggregate"
    - path: "backend/tests/scripts/e2e_aggregator.py"
      provides: "Cross-platform E2E result aggregation script"
      exports: ["load_json", "extract_metrics", "calculate_aggregate_metrics", "generate_summary"]
  key_links:
    - from: ".github/workflows/e2e-unified.yml"
      to: "backend/tests/scripts/e2e_aggregator.py"
      via: "Python script invocation in aggregate job"
      pattern: "python3.*e2e_aggregator.py"
    - from: ".github/workflows/e2e-unified.yml"
      to: "backend/tests/e2e_api/"
      via: "Mobile API-level test execution"
      pattern: "pytest.*e2e_api"
---

<objective>
Enhance E2E orchestration CI/CD workflow and aggregation script to handle all three platforms (web/Playwright, mobile/API-level pytest, desktop/Tauri) with unified result reporting.

Purpose: Enable cross-platform E2E test execution with automated result aggregation following Phase 147 property testing pattern. The workflow orchestrates parallel platform execution, downloads artifacts, and generates unified reports for historical trending.

Mobile testing uses API-level pytest tests (not Detox) because Detox E2E is BLOCKED by expo-dev-client requirement per RESEARCH.md. API-level tests use standard pytest JSON format (same as Playwright), simplifying aggregation.

Output: Enhanced `.github/workflows/e2e-unified.yml` workflow with 4 jobs (web, mobile API tests, desktop, aggregate) and updated `e2e_aggregator.py` script that parses Playwright pytest format for web/mobile and Tauri cargo test format for desktop.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-cross-platform-e2e-orchestration/148-RESEARCH.md
@.planning/phases/147-cross-platform-property-testing/147-*-SUMMARY.md
@.github/workflows/e2e-unified.yml
@backend/tests/scripts/e2e_aggregator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance E2E Aggregation Script</name>
  <files>backend/tests/scripts/e2e_aggregator.py</files>
  <action>
    Enhance backend/tests/scripts/e2e_aggregator.py to handle Tauri cargo test JSON format:

    1. Add `extract_tauri_metrics()` function for cargo test --format json parsing:
       - Parse cargo JSON output (type: 'test', 'suite', etc.)
       - Calculate total/passed/failed/skipped from test results
       - Handle cargo's non-zero exit code (use || true in CI)
       - Return dict matching Playwright pytest format for consistency

    2. Update `extract_metrics()` to dispatch by format detection:
       - Existing: "stats" key → Playwright pytest format (used by both web E2E and mobile API tests)
       - NEW: "test_suites" or cargo structure → Tauri format
       - Fallback: Return error dict with "error" key

    Note: Mobile API tests use pytest (same as Playwright), so they share the "stats" key format. No separate Detox parsing needed (Detox E2E is BLOCKED per RESEARCH.md).

    3. Add `parse_cargo_json_line()` helper for line-by-line parsing:
       - cargo test --format json outputs one JSON object per line
       - Filter for "test" type entries
       - Extract test name and passed status
       - Aggregate into summary metrics

    4. Enhance error handling with detailed error messages:
       - Include platform name in error messages
       - Report file path when JSON parsing fails
       - Provide guidance for common errors (missing keys, invalid formats)

    Follow the existing code style (argparse, typing, pathlib) and maintain backward compatibility with existing Playwright pytest format. Note: Mobile API tests use pytest (same format as Playwright), so no separate Detox parser is needed.
  </action>
  <verify>
    python3 backend/tests/scripts/e2e_aggregator.py --help
    python3 -c "from backend.tests.scripts.e2e_aggregator import extract_tauri_metrics, parse_cargo_json_line; print('Import successful')"
  </verify>
  <done>
    Aggregation script handles all three platform test formats: Playwright pytest (stats key) for web/mobile, Tauri cargo (test_suites/cargo JSON) for desktop.
  </done>
</task>

<task type="auto">
  <name>Task 2: Complete E2E Workflow Aggregation Job</name>
  <files>.github/workflows/e2e-unified.yml</files>
  <action>
    Complete the aggregate job in .github/workflows/e2e-unified.yml:

    1. Verify artifact download steps use continue-on-error: true (for missing platform reports)
    2. Add error handling for aggregator script invocation:
       - Wrap aggregator call in try/catch or use continue-on-error: true
       - Verify Python 3 is available before running script
       - Check if results/ directory exists before aggregation
    3. Add conditional steps for report generation:
       - Only generate summary if at least one platform report exists
       - Skip upload if unified results JSON is missing
       - Add comment on commit only if e2e_summary.md was generated
    4. Enhance commit comment with platform breakdown:
       - Show per-platform pass/fail counts
       - Include total duration across all platforms
       - Highlight any platform failures with emoji indicators

    Do NOT modify the e2e-web, e2e-mobile, or e2e-desktop jobs - they are already complete from prior work. Focus only on the aggregate job orchestration and error handling.
  </action>
  <verify>
    yamllint .github/workflows/e2e-unified.yml 2>/dev/null || grep -A 20 "aggregate:" .github/workflows/e2e-unified.yml
  </verify>
  <done>
    Aggregate job downloads all platform artifacts, runs e2e_aggregator.py with proper error handling, and uploads unified results with commit comment.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add E2E Historical Trending Support</name>
  <files>backend/tests/scripts/e2e_aggregator.py</files>
  <action>
    Add historical trending to e2e_aggregator.py for E2E test results over time:

    1. Add `--trend-file` argument for JSON file storing historical results:
       - Default: backend/tests/coverage_reports/metrics/e2e_trend.json
       - Create directory if not exists
       - Load existing history or initialize empty list

    2. Append current run to history in `append_to_history()` function:
       - Entry: {timestamp, aggregate: {total_tests, total_passed, total_failed, pass_rate}, platforms: [{platform, total, passed, failed, duration}]}
       - Keep last 90 days of history (configurable via env E2E_TREND_DAYS)
       - Sort by timestamp descending (newest first)

    3. Add `calculate_trend_metrics()` for trend analysis:
       - Compare current run vs previous run (if exists)
       - Calculate pass rate change (delta percentage)
       - Calculate test count change (added/removed tests)
       - Identify platforms with declining pass rates

    4. Enhance generate_summary() to include trend section:
       - "## Trend Analysis" with delta indicators (↑↓→)
       - Show pass rate change vs previous run
       - Show test count change
       - Highlight platforms with >5% pass rate decline

    5. Update main() to call trend functions:
       - Load trend file, append current run, save updated history
       - Pass trend metrics to generate_summary()
       - Return error code 0 for success, 1 for test failures, 2 for trend decline

    Follow Phase 146 pattern (cross_platform_trend.json) for historical data structure. Use same 90-day retention period.
  </action>
  <verify>
    python3 backend/tests/scripts/e2e_aggregator.py --web tests/data/sample_web.json --trend-file /tmp/e2e_trend_test.json --output /tmp/e2e_output.json --summary /tmp/e2e_summary.md
    cat /tmp/e2e_trend_test.json | jq '.[] | .timestamp, .aggregate.pass_rate'
  </verify>
  <done>
    Aggregator appends results to e2e_trend.json, calculates trend metrics, includes trend analysis in summary markdown.
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify the enhanced E2E orchestration:

1. Check e2e_aggregator.py handles all platform formats:
   - Create sample JSON files for Playwright pytest (stats key - used by both web E2E and mobile API tests), Tauri (test_suites or cargo JSON structure)
   - Run aggregator with all inputs: `python3 backend/tests/scripts/e2e_aggregator.py --web sample_web.json --mobile sample_mobile.json --desktop sample_desktop.json --output output.json --summary summary.md`
   - Verify output.json contains platforms array with web/mobile/desktop entries
   - Verify summary.md contains platform breakdown section

2. Verify workflow file syntax:
   - Run `yamllint .github/workflows/e2e-unified.yml` (if installed) or `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/e2e-unified.yml'))"`
   - Check aggregate job depends on [e2e-web, e2e-mobile, e2e-desktop]
   - Verify all platform download steps use continue-on-error: true
   - Note: e2e-mobile job runs API-level pytest tests (not Detox E2E)

3. Verify trending functionality:
   - Run aggregator twice with --trend-file
   - Check e2e_trend.json contains two entries sorted by timestamp
   - Verify summary.md second run includes trend analysis with delta indicators
</verification>

<success_criteria>
E2E orchestration complete when:
- [ ] e2e_aggregator.py parses Playwright pytest (web/mobile) and Tauri cargo test (desktop) JSON formats
- [ ] e2e-unified.yml workflow orchestrates 4 jobs (web, mobile API tests, desktop, aggregate) with proper dependencies
- [ ] Aggregate job downloads artifacts, runs aggregator, uploads unified results
- [ ] E2E results appended to e2e_trend.json for historical tracking
- [ ] Summary markdown includes trend analysis with delta indicators
</success_criteria>

<output>
After completion, create `.planning/phases/148-cross-platform-e2e-orchestration/148-01-SUMMARY.md`
</output>
