---
phase: 148-cross-platform-e2e-orchestration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/e2e_ui/tests/test_agent_execution.py
  - backend/tests/e2e_ui/tests/test_canvas_presentation.py
  - frontend-nextjs/src-tauri/tests/agent_execution_integration_test.rs
  - frontend-nextjs/src-tauri/tests/canvas_integration_test.rs
autonomous: true

must_haves:
  truths:
    - "Playwright E2E tests validate agent spawn, chat, and streaming workflows"
    - "Playwright E2E tests validate canvas presentation, charts, and form submission"
    - "Tauri integration tests validate agent IPC commands and state management"
    - "Tauri integration tests validate canvas window creation and IPC events"
    - "All E2E tests are independent (cleanup after test, no shared state)"
  artifacts:
    - path: "backend/tests/e2e_ui/tests/test_agent_execution.py"
      provides: "Agent execution E2E tests with Playwright"
      contains: "test_agent_spawn, test_agent_chat, test_agent_streaming"
    - path: "backend/tests/e2e_ui/tests/test_canvas_presentation.py"
      provides: "Canvas presentation E2E tests with Playwright"
      contains: "test_canvas_present, test_canvas_charts, test_canvas_forms"
    - path: "frontend-nextjs/src-tauri/tests/agent_execution_integration_test.rs"
      provides: "Agent IPC integration tests for Tauri"
      contains: "#[tauri::test] functions for agent commands"
    - path: "frontend-nextjs/src-tauri/tests/canvas_integration_test.rs"
      provides: "Canvas IPC integration tests for Tauri"
      contains: "#[tauri::test] functions for canvas events"
  key_links:
    - from: "backend/tests/e2e_ui/tests/test_agent_execution.py"
      to: "http://localhost:3001"
      via: "Playwright page.goto() and page.click() interactions"
      pattern: "page.goto.*localhost"
    - from: "frontend-nextjs/src-tauri/tests/agent_execution_integration_test.rs"
      to: "main:app"
      via: "Tauri app.trigger() IPC command invocation"
      pattern: "app.trigger.*agent_"
---

<objective>
Implement critical workflow E2E tests for agent execution and canvas presentation across web (Playwright) and desktop (Tauri) platforms.

Purpose: Ensure end-to-end validation of core user workflows (agent interactions, canvas presentations) with tests that can run in CI/CD and catch regressions. Focus on orchestration-ready tests rather than comprehensive coverage - use unit/integration tests for edge cases.

Output: 2 Playwright E2E test files (agent execution, canvas presentation) and 2 Tauri integration test files (agent IPC, canvas events) covering critical workflows with proper cleanup and independence.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/148-cross-platform-e2e-orchestration/148-RESEARCH.md
@backend/tests/e2e_ui/tests/test_agent_chat.py
@backend/tests/e2e_ui/tests/test_canvas_charts.py
@frontend-nextjs/src-tauri/tests/commands_test.rs
@frontend-nextjs/src-tauri/tests/canvas_integration_test.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Agent Execution E2E Tests (Playwright)</name>
  <files>backend/tests/e2e_ui/tests/test_agent_execution.py</files>
  <action>
    Create backend/tests/e2e_ui/tests/test_agent_execution.py with Playwright E2E tests for agent workflows:

    1. Implement `test_agent_spawn_and_chat()` test:
       - Navigate to http://localhost:3001/agents
       - Click "Spawn Agent" button (use page.click() with auto-waiting)
       - Fill agent name input: page.fill("input[name='agentName']", "E2ETestAgent")
       - Submit form and wait for navigation: page.wait_for_url("**/agents/**")
       - Assert agent appears in agent list: expect(page.locator(".agent-list")).to_contain_text("E2ETestAgent")

    2. Implement `test_agent_streaming_response()` test:
       - Navigate to specific agent page (create agent via API for faster test)
       - Send message via chat input: page.fill("textarea[name='message']", "Hello E2E")
       - Click send button: page.click("button[type='submit']")
       - Wait for streaming response: page.wait_for_selector(".agent-message", timeout=5000)
       - Assert response contains text: expect(page.locator(".agent-message")).to_contain_text("hello", timeout=5000)

    3. Implement `test_agent_governance_maturity()` test:
       - Create agents with different maturity levels via API (STUDENT, INTERN, AUTONOMOUS)
       - Navigate to governance page: page.goto("http://localhost:3001/governance")
       - Verify maturity badges display correctly: expect(page.locator(".maturity-badge")).to_have_count(3)
       - Test governance block: Click "Execute Action" on STUDENT agent, expect error message

    4. Add test cleanup with `pytest.fixture` autouse:
       - Delete test-created agents via API after each test
       - Use unique agent names with UUID suffix for isolation
       - Wrap cleanup in try/finally to ensure execution

    Follow existing test patterns from test_agent_chat.py (use Page fixture, expect assertions, proper selectors). Use `pytest.mark.e2e` for CI/CD discovery.
  </action>
  <verify>
    cd backend && python -m pytest tests/e2e_ui/tests/test_agent_execution.py -v --tb=short
  </verify>
  <done>
    test_agent_execution.py with 3 E2E tests covering agent spawn, streaming chat, and governance maturity validation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Canvas Presentation E2E Tests (Playwright)</name>
  <files>backend/tests/e2e_ui/tests/test_canvas_presentation.py</files>
  <action>
    Create backend/tests/e2e_ui/tests/test_canvas_presentation.py with Playwright E2E tests for canvas workflows:

    1. Implement `test_canvas_chart_presentation()` test:
       - Navigate to canvas page: page.goto("http://localhost:3001/canvas")
       - Click "New Canvas" button: page.click("button:has-text('New Canvas')")
       - Select chart type: page.click("button[value='line']")
       - Submit and wait for canvas: page.wait_for_selector(".canvas-container", timeout=5000)
       - Assert chart renders: expect(page.locator("canvas")).to_have_attribute("data-chart-type", "line")

    2. Implement `test_canvas_form_submission()` test:
       - Create form canvas via API for faster test setup
       - Navigate to canvas page: page.goto(f"http://localhost:3001/canvas/{canvas_id}")
       - Fill form fields: page.fill("input[name='email']", "e2e@test.com"), page.fill("textarea[name='message']", "Test message")
       - Submit form: page.click("button[type='submit']")
       - Wait for success message: page.wait_for_selector(".success-message", timeout=5000)
       - Assert canvas state changed: expect(page.locator(".canvas-state")).to_contain_text("submitted")

    3. Implement `test_canvas_accessibility_tree()` test:
       - Navigate to canvas page with existing canvas
       - Get canvas state via API: page.evaluate("window.atom.canvas.getState()")
       - Assert state contains required fields: type, data, timestamp
       - Verify hidden accessibility tree: page.locator("div[role='log'][aria-live]").count() > 0

    4. Add test cleanup fixture:
       - Delete test-created canvases via API after each test
       - Use unique canvas IDs via API for test isolation
       - Handle canvas not found errors gracefully in cleanup

    Follow existing test patterns from test_canvas_charts.py. Use `pytest.mark.e2e` and group tests by workflow (chart presentation, form submission, accessibility).
  </action>
  <verify>
    cd backend && python -m pytest tests/e2e_ui/tests/test_canvas_presentation.py -v --tb=short
  </verify>
  <done>
    test_canvas_presentation.py with 3 E2E tests covering chart presentation, form submission, and accessibility tree validation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Agent IPC Integration Tests (Tauri)</name>
  <files>frontend-nextjs/src-tauri/tests/agent_execution_integration_test.rs</files>
  <action>
    Create frontend-nextjs/src-tauri/tests/agent_execution_integration_test.rs with Tauri integration tests for agent IPC:

    1. Create module with `#[tauri::test]` attribute tests (no GUI context required):
       - Use existing test structure from commands_test.rs
       - Import tauri::Manager and agent command handlers

    2. Implement `test_agent_spawn_ipc()` test:
       - Create app instance with minimal setup (no window needed)
       - Trigger agent spawn via IPC: app.emit("agent_spawn", AgentSpawnRequest { name: "TestAgent".to_string(), maturity: "INTERN".to_string() })
       - Verify agent created: Check internal agent registry or mock database
       - Assert response contains agent_id: response.get("agent_id").is_some()

    3. Implement `test_agent_chat_ipc()` test:
       - Create agent via setup (reuse spawn logic)
       - Trigger chat message via IPC: app.emit("agent_chat", AgentChatRequest { agent_id: test_id.clone(), message: "Hello".to_string() })
       - Verify chat initiated: Check agent execution record created
       - Assert streaming flag: response.get("streaming").is_some()

    4. Implement `test_agent_governance_check()` test:
       - Create STUDENT maturity agent via setup
       - Trigger restricted action: app.emit("agent_execute", AgentExecuteRequest { agent_id: student_id, action: "delete".to_string() })
       - Verify governance blocks: Assert response contains error "STUDENT agents cannot perform delete actions"
       - Test AUTONOMOUS agent passes same action

    Follow existing patterns from commands_test.rs and ipc_commands_test.rs. Use Result<(), String> return type for test functions. Mock database operations to avoid full backend dependency.
  </action>
  <verify>
    cd frontend-nextjs/src-tauri && cargo test --test agent_execution_integration_test -v
  </verify>
  <done>
    agent_execution_integration_test.rs with 3 Tauri integration tests covering agent spawn IPC, chat IPC, and governance checks.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Canvas IPC Integration Tests (Tauri)</name>
  <files>frontend-nextjs/src-tauri/tests/canvas_integration_test.rs</files>
  <action>
    Enhance frontend-nextjs/src-tauri/tests/canvas_integration_test.rs with additional Tauri integration tests for canvas IPC:

    1. Add `test_canvas_present_ipc()` test (if not exists):
       - Trigger canvas presentation via IPC: app.emit("canvas:present", CanvasPresentRequest { id: "test-canvas".to_string(), type_: CanvasType::Chart, data: serde_json::json!({"type": "line"}) })
       - Verify canvas window created: Check canvas state registry
       - Assert window title contains canvas ID
       - Test canvas cleanup on close

    2. Add `test_canvas_form_submission_ipc()` test:
       - Create form canvas via setup
       - Trigger form submission via IPC: app.emit("canvas:submit", CanvasSubmitRequest { canvas_id: test_id.clone(), data: serde_json::json!({"email": "test@example.com"}) })
       - Verify submission recorded: Check audit log or mock database
       - Assert canvas state transition: presenting → submitted

    3. Add `test_canvas_state_serialization()` test:
       - Create canvas with complex data (nested objects, arrays, special characters)
       - Serialize state via IPC: app.emit("canvas:get_state", CanvasGetStateRequest { canvas_id: test_id.clone() })
       - Verify roundtrip preservation: Deep equality check on deserialized state
       - Test special characters: Unicode, emoji, escape sequences

    Follow existing patterns from canvas_integration_test.rs. Use serde_json for JSON handling. Add #[tauri::test] attribute to all test functions.
  </action>
  <verify>
    cd frontend-nextjs/src-tauri && cargo test --test canvas_integration_test -v
  </verify>
  <done>
    canvas_integration_test.rs enhanced with 3 additional Tauri integration tests covering canvas presentation IPC, form submission IPC, and state serialization.
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify the E2E tests:

1. Run Playwright E2E tests locally:
   - Start backend: `cd backend && python -m uvicorn main:app --host 0.0.0.0 --port 8001`
   - Start frontend: `cd frontend-nextjs && npm run dev`
   - Run tests: `cd backend && pytest tests/e2e_ui/tests/test_agent_execution.py tests/e2e_ui/tests/test_canvas_presentation.py -v`
   - Verify all 6 tests pass (3 agent + 3 canvas)

2. Run Tauri integration tests:
   - `cd frontend-nextjs/src-tauri && cargo test --test agent_execution_integration_test --test canvas_integration_test -v`
   - Verify all tests pass (agent + canvas tests)

3. Verify test independence:
   - Run tests in parallel: `pytest tests/e2e_ui/tests/test_agent_execution.py tests/e2e_ui/tests/test_canvas_presentation.py -n 4`
   - Verify no test failures due to shared state
   - Check cleanup happens even on test failure

4. Verify E2E markers:
   - `grep -r "pytest.mark.e2e" backend/tests/e2e_ui/tests/`
   - Ensure new test files have proper markers for CI/CD discovery
</verification>

<success_criteria>
Critical workflow E2E tests complete when:
- [ ] 6 Playwright E2E tests (3 agent, 3 canvas) execute successfully
- [ ] 6 Tauri integration tests (3 agent, 3 canvas) execute successfully
- [ ] All tests are independent (parallel execution, proper cleanup)
- [ ] Tests are marked with @pytest.mark.e2e for CI/CD discovery
- [ ] Tests use unique identifiers (UUID suffixes) for isolation
</success_criteria>

<output>
After completion, create `.planning/phases/148-cross-platform-e2e-orchestration/148-02-SUMMARY.md`
</output>
