---
phase: 290-comprehensive-test-suite
plan: 02
type: execute
wave: 1
depends_on: ["290-01"]
files_modified:
  - backend/tests/test_auto_dev/test_fitness_service.py
  - backend/tests/test_auto_dev/test_evolution_engine.py
  - backend/tests/test_auto_dev/test_advisor_service.py
  - backend/tests/test_auto_dev/test_reflection_engine.py
  - backend/tests/test_auto_dev/test_alpha_evolver_engine.py
  - backend/tests/test_auto_dev/conftest.py
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "All 24 failing tests now pass (100% pass rate)"
    - "Coverage increases to >=80% for all auto_dev modules"
    - "Fitness scoring math verified and corrected"
    - "Pattern detection grouping logic working"
    - "Episode model queries return data correctly"
    - "Integration tests properly set up with database sessions"
  artifacts:
    - path: "backend/tests/test_auto_dev/test_fitness_service.py"
      provides: "Fixed fitness scoring tests with correct model field names"
      min_lines: 220
    - path: "backend/tests/test_auto_dev/test_evolution_engine.py"
      provides: "Fixed trigger detection and pruning tests"
      min_lines: 203
    - path: "backend/tests/test_auto_dev/test_advisor_service.py"
      provides: "Fixed variant comparison and LLM integration tests"
      min_lines: 176
    - path: "backend/tests/test_auto_dev/test_reflection_engine.py"
      provides: "Fixed pattern detection grouping logic tests"
      min_lines: 185
    - path: "backend/tests/test_auto_dev/test_alpha_evolver_engine.py"
      provides: "Fixed episode analysis tests"
      min_lines: 228
    - path: "backend/tests/test_auto_dev/conftest.py"
      provides: "Enhanced fixtures with proper database session management"
      min_lines: 489
  key_links:
    - from: "test_fitness_service.py"
      to: "core.auto_dev.models.WorkflowVariant"
      via: "workflow_definition field (not workflow_def)"
      pattern: "WorkflowVariant\\(.*workflow_definition="
    - from: "test_evolution_engine.py"
      to: "core.auto_dev.evolution_engine.EvolutionEngine"
      via: "_check_optimization_triggers and _prune_variants methods"
      pattern: "evolution_engine\\._check_optimization_triggers\\("
    - from: "test_advisor_service.py"
      to: "core.auto_dev.advisor_service.AdvisorService"
      via: "LLM service mock with proper context passing"
      pattern: "mock_auto_dev_llm\\.generate_completion"
    - from: "conftest.py"
      to: "core.models.AgentEpisode"
      via: "auto_dev_db_session fixture creates and commits episodes"
      pattern: "auto_dev_db_session\\.add\\(episode\\)"
---

<objective>
Fix all 24 failing auto_dev tests to achieve 100% pass rate and 80%+ coverage.

Purpose: Close verification gaps from Phase 290 Plan 01 by fixing test failures, database persistence issues, scoring math, and pattern detection logic.
Output: 152/152 tests passing, 80%+ coverage for auto_dev modules
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/290-comprehensive-test-suite/290-VERIFICATION.md
@.planning/phases/290-comprehensive-test-suite/290-01-SUMMARY.md
@backend/tests/test_auto_dev/conftest.py
@backend/core/auto_dev/models.py
@backend/core/auto_dev/fitness_service.py
@backend/core/auto_dev/evolution_engine.py
@backend/core/auto_dev/advisor_service.py
@backend/core/auto_dev/reflection_engine.py
@backend/core/auto_dev/alpha_evolver_engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix fitness service test model field names and scoring math</name>
  <files>backend/tests/test_auto_dev/test_fitness_service.py</files>
  <read_first>
    1. backend/tests/test_auto_dev/test_fitness_service.py (to see current test structure)
    2. backend/core/auto_dev/models.py (to verify WorkflowVariant field names)
    3. backend/core/auto_dev/fitness_service.py (to understand scoring implementation)
  </read_first>
  <action>
    Fix the fitness service tests by:

    1. **Replace all incorrect field references** (line 45):
       - Change `workflow_def={}` to `workflow_definition={}`
       - Change any other instances of `workflow_def` to `workflow_definition`
       - This fixes the TypeError: 'workflow_def' is an invalid keyword argument

    2. **Fix scoring math tests** (test_compilation_check, test_semantic_similarity):
       - Add assertions that verify the actual scoring formula: `score = (syntax_ok * 0.3) + (execution_ok * 0.4) + (semantic_ok * 0.3)`
       - Verify that syntax errors penalize score by 0.3 (30% weight)
       - Verify that execution failures penalize score by 0.4 (40% weight)
       - Add test for score normalization to [0.0, 1.0] range

    3. **Fix stable sorting in get_top_variants tests**:
       - Verify that variants with equal scores are sorted by created_at (oldest first)
       - Add test for deterministic ordering when scores are tied

    4. **Add coverage for missing edge cases**:
       - Test with missing fitness_signals (should handle gracefully)
       - Test with malformed proxy_signals (should not crash)
       - Test score boundary conditions (0.0, 0.5, 1.0)

    Expected changes:
    - Line 45: `workflow_def={}` → `workflow_definition={}`
    - Lines 56-63: Add specific score assertions with expected values
    - Lines 80-100: Add stable sorting verification
    - Add 3-4 new test methods for edge cases
  </action>
  <verify>
    <automated>cd /Users/rushiparikh/projects/atom && python3 -m pytest backend/tests/test_auto_dev/test_fitness_service.py -xvs 2>&1 | grep -E "(PASSED|FAILED|ERROR)" | head -20</automated>
  </verify>
  <done>
    - All 17 fitness service tests pass (0 failures)
    - WorkflowVariant model uses correct field name `workflow_definition`
    - Scoring math verified: 30% syntax, 40% execution, 30% semantic
    - Stable sorting confirmed for equal scores
    - Edge cases covered (missing signals, malformed data, boundary values)
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix evolution engine trigger detection and pruning tests</name>
  <files>backend/tests/test_auto_dev/test_evolution_engine.py</files>
  <read_first>
    1. backend/tests/test_auto_dev/test_evolution_engine.py (to see current test structure)
    2. backend/core/auto_dev/evolution_engine.py (to understand trigger detection logic)
    3. backend/core/auto_dev/models.py (to verify model field names)
  </read_first>
  <action>
    Fix the evolution engine tests by:

    1. **Fix trigger detection tests** (test_detects_high_latency, test_detects_high_token_usage):
       - Mock the `_should_optimize` method to return True (bypass capability gate)
       - Create SkillExecutionEvent with latency > LATENCY_THRESHOLD_SECONDS (5.0)
       - Create SkillExecutionEvent with token_usage > TOKEN_THRESHOLD (5000)
       - Verify `_check_optimization_triggers` returns correct reason string
       - Assert that `_trigger_alpha_evolver` is called with correct parameters

    2. **Fix variant pruning tests** (test_prunes_low_fitness_variants):
       - Create 5 WorkflowVariant instances with fitness scores: 0.2, 0.4, 0.6, 0.8, 0.9
       - Call pruning method with threshold 0.5
       - Verify that variants with score < 0.5 are marked as "pruned" (evaluation_status="pruned")
       - Verify that variants with score >= 0.5 remain "evaluated"
       - Test that pruning respects tenant_id isolation

    3. **Add coverage for missing optimization trigger logic**:
       - Test LATENCY_THRESHOLD_SECONDS trigger (lines 59-68 in evolution_engine.py)
       - Test TOKEN_THRESHOLD trigger (lines 59-68)
       - Test partial failure trigger (retry_count > 3)
       - Test low fitness score trigger (fitness < 0.5)

    4. **Fix model field names**:
       - Ensure all WorkflowVariant creations use `workflow_definition` (not `workflow_def`)
       - Ensure all ToolMutation creations use correct field names

    Expected changes:
    - Add `@patch` decorator for `_should_optimize` returning True
    - Add assertions for trigger reason strings (e.g., "high_latency", "high_token_usage")
    - Add pruning logic tests with status verification
    - Add 4-5 new test methods for trigger detection edge cases
  </action>
  <verify>
    <automated>cd /Users/rushiparikh/projects/atom && python3 -m pytest backend/tests/test_auto_dev/test_evolution_engine.py -xvs 2>&1 | grep -E "(PASSED|FAILED|ERROR)" | head -20</automated>
  </verify>
  <done>
    - All 17 evolution engine tests pass (0 failures)
    - Trigger detection tests verify latency, token, and fitness thresholds
    - Pruning tests confirm low-fitness variants are marked correctly
    - Coverage increases to >=80% for evolution_engine.py (currently 41%)
    - All model field references correct (workflow_definition, not workflow_def)
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix advisor service variant comparison and LLM integration tests</name>
  <files>backend/tests/test_auto_dev/test_advisor_service.py</files>
  <read_first>
    1. backend/tests/test_auto_dev/test_advisor_service.py (to see current test structure)
    2. backend/core/auto_dev/advisor_service.py (to understand LLM integration)
    3. backend/core/auto_dev/models.py (to verify model field names)
  </read_first>
  <action>
    Fix the advisor service tests by:

    1. **Fix variant comparison tests** (test_analyze_variant_performance):
       - Create 3 WorkflowVariant instances with different fitness scores and signals
       - Mock LLM service to return deterministic comparison analysis
       - Verify that `analyze_variant_performance` returns expected comparison data
       - Check that comparison includes: fitness_delta, recommendations, confidence

    2. **Fix LLM context passing** (test_generate_guidance):
       - Mock `mock_auto_dev_llm.generate_completion` to return structured guidance
       - Verify that LLM is called with proper context: tenant_id, agent_id, variant data
       - Assert that generated guidance contains expected sections: summary, recommendations, risk_level
       - Test that LLM errors are handled gracefully (returns error message, doesn't crash)

    3. **Fix model field names**:
       - Replace `workflow_def` with `workflow_definition` in all WorkflowVariant creations
       - Ensure all fitness_signals are properly structured JSON

    4. **Add coverage for missing advisor logic** (lines 111-150, 156-177):
       - Test variant ranking logic (sort by fitness_score descending)
       - Test performance trend detection (improving vs declining)
       - Test recommendation generation (promote, prune, keep_testing)
       - Test caching behavior (same query returns cached result)

    5. **Fix integration with FitnessService**:
       - Mock FitnessService.get_top_variants to return test data
       - Verify advisor correctly queries fitness service for comparisons
       - Test advisor handles empty variant lists gracefully

    Expected changes:
    - Fix WorkflowVariant field name on all creations
    - Add LLM mock assertions with expected parameters
    - Add variant comparison logic tests
    - Add 3-4 new test methods for coverage gaps
  </action>
  <verify>
    <automated>cd /Users/rushiparikh/projects/atom && python3 -m pytest backend/tests/test_auto_dev/test_advisor_service.py -xvs 2>&1 | grep -E "(PASSED|FAILED|ERROR)" | head -20</automated>
  </verify>
  <done>
    - All 13 advisor service tests pass (0 failures)
    - Variant comparison correctly analyzes fitness deltas and trends
    - LLM integration verified with proper context passing
    - Coverage increases to >=80% for advisor_service.py (currently 63%)
    - Caching behavior tested and working
  </done>
</task>

<task type="auto">
  <name>Task 4: Fix reflection engine pattern detection grouping tests</name>
  <files>backend/tests/test_auto_dev/test_reflection_engine.py</files>
  <read_first>
    1. backend/tests/test_auto_dev/test_reflection_engine.py (to see current test structure)
    2. backend/core/auto_dev/reflection_engine.py (to understand pattern detection logic)
    3. backend/core/auto_dev/event_hooks.py (to understand event structure)
  </read_first>
  <action>
    Fix the reflection engine tests by:

    1. **Fix pattern detection grouping** (test_detects_repeated_failure_patterns):
       - Create 3 TaskEvents with similar error traces (same error_type, similar stack traces)
       - Verify that `_find_similar_failures` groups them by error signature
       - Assert that pattern frequency reaches threshold (e.g., 3 occurrences)
       - Test that MementoEngine is triggered after threshold is met

    2. **Fix error signature extraction**:
       - Test that error_type is extracted correctly from error_trace (e.g., "ValueError", "TypeError")
       - Test that function_name is extracted from stack trace (e.g., "process_sales", line 42)
       - Test that similar errors with different line numbers are still grouped
       - Test that different error types are NOT grouped together

    3. **Fix pattern reset after promotion** (test_resets_patterns_after_promotion):
       - Create failure pattern with frequency 3 (above threshold)
       - Simulate skill promotion event
       - Verify that pattern frequency is reset to 0
       - Verify that pattern is removed from tracking

    4. **Add coverage for missing grouping logic**:
       - Test with mixed error types (should only group similar ones)
       - Test with malformed error traces (should handle gracefully)
       - Test threshold behavior (trigger at exactly 3, not at 2)
       - Test cross-agent pattern detection (different agent_ids, same error)

    5. **Fix capability gate mocking**:
       - Mock `_should_process_agent` lambda to return True
       - Ensure events are tracked for testing
       - Verify gate logic is bypassed in tests but present in production

    Expected changes:
    - Add error signature grouping tests with specific assertions
    - Add threshold verification (frequency >= 3 triggers promotion)
    - Add pattern reset tests after skill promotion
    - Add 3-4 new test methods for edge cases
  </action>
  <verify>
    <automated>cd /Users/rushiparikh/projects/atom && python3 -m pytest backend/tests/test_auto_dev/test_reflection_engine.py -xvs 2>&1 | grep -E "(PASSED|FAILED|ERROR)" | head -20</automated>
  </verify>
  <done>
    - All 14 reflection engine tests pass (0 failures)
    - Pattern detection correctly groups similar failures by error signature
    - Threshold triggering verified (3 occurrences → trigger MementoEngine)
    - Pattern reset after promotion working correctly
    - Coverage increases for reflection_engine.py (currently has gaps in grouping logic)
  </done>
</task>

<task type="auto">
  <name>Task 5: Fix alpha evolver episode analysis and integration test setup</name>
  <files>
    backend/tests/test_auto_dev/test_alpha_evolver_engine.py
    backend/tests/test_auto_dev/test_auto_dev_integration.py
    backend/tests/test_auto_dev/conftest.py
  </files>
  <read_first>
    1. backend/tests/test_auto_dev/test_alpha_evolver_engine.py (to see current test structure)
    2. backend/tests/test_auto_dev/test_auto_dev_integration.py (to see integration test issues)
    3. backend/core/auto_dev/alpha_evolver_engine.py (to understand episode analysis)
    4. backend/tests/test_auto_dev/conftest.py (to see database session setup)
  </read_first>
  <action>
    Fix the alpha evolver and integration tests by:

    1. **Fix episode analysis to use AgentEpisode model** (test_analyze_episode):
       - Update alpha_evolver_engine.py line 54 to import AgentEpisode (not Episode)
       - Change query from `Episode` to `AgentEpisode` in analyze_episode method
       - Update tests to create AgentEpisode instances with all required fields
       - Verify that episode queries return data from test database

    2. **Fix database session persistence in conftest.py**:
       - Ensure `auto_dev_db_session` fixture commits data before yielding
       - Add `session.flush()` after adding episodes to ensure they're persisted
       - Add `session.refresh(episode)` after commit to ensure ID is populated
       - Verify that AgentEpisode and EpisodeSegment tables are created in test database

    3. **Fix integration test setup** (test_auto_dev_integration.py):
       - Add proper database session management (use auto_dev_db_session fixture)
       - Ensure EventBus is properly initialized before each test
       - Reset event bus state between tests (clear subscribers)
       - Add proper cleanup for database state (rollback or truncate tables)

    4. **Add coverage for missing alpha evolver logic** (lines 62-78, 113-115):
       - Test analyze_episode with real episodes from database
       - Test optimization target identification (high_latency, high_token_usage)
       - Test mutation generation with LLM service
       - Test sandbox execution of mutations
       - Test variant spawning with parent_tool_id lineage

    5. **Fix cross-service integration**:
       - Test full lifecycle: Episode → ReflectionEngine → MementoEngine → SkillCandidate
       - Test full lifecycle: Episode → EvolutionEngine → AlphaEvolver → WorkflowVariant
       - Verify that events are published and consumed correctly
       - Test error handling when services are unavailable

    Expected changes:
    - alpha_evolver_engine.py line 54: `from core.models import Episode` → `from core.models import AgentEpisode`
    - alpha_evolver_engine.py line 57: `self.db.query(Episode)` → `self.db.query(AgentEpisode)`
    - conftest.py: Add `session.flush()` and `session.refresh()` after episode creation
    - Integration tests: Add proper session cleanup and event bus reset
    - Add 4-5 new test methods for episode analysis coverage
  </action>
  <verify>
    <automated>cd /Users/rushiparikh/projects/atom && python3 -m pytest backend/tests/test_auto_dev/test_alpha_evolver_engine.py backend/tests/test_auto_dev/test_auto_dev_integration.py -xvs 2>&1 | grep -E "(PASSED|FAILED|ERROR)" | head -30</automated>
  </verify>
  <done>
    - All 17 alpha evolver tests pass (0 failures)
    - All 15 integration tests pass (0 failures)
    - Episode analysis correctly uses AgentEpisode model from database
    - Database sessions properly persist and query test data
    - Coverage increases to >=80% for alpha_evolver_engine.py (currently 63%)
    - Integration tests verify end-to-end flows without state leakage
  </done>
</task>

<task type="auto">
  <name>Task 6: Add coverage tests for remaining edge cases (container_sandbox, capability_gate)</name>
  <files>
    backend/tests/test_auto_dev/test_container_sandbox.py
    backend/tests/test_auto_dev/test_capability_gate.py
  </files>
  <read_first>
    1. backend/tests/test_auto_dev/test_container_sandbox.py (to see current test coverage)
    2. backend/tests/test_auto_dev/test_capability_gate.py (to see current test coverage)
    3. backend/core/auto_dev/container_sandbox.py (to identify uncovered lines)
    4. backend/core/auto_dev/capability_gate.py (to identify uncovered lines)
  </read_first>
  <action>
    Add coverage tests for edge cases in container_sandbox and capability_gate:

    1. **Container sandbox edge cases** (lines 118-187):
       - Test Docker fallback when Docker daemon is unavailable
       - Test timeout enforcement (code execution exceeds timeout limit)
       - Test memory limit enforcement (code uses excessive memory)
       - Test security constraints (network disabled, read-only filesystem)
       - Test subprocess execution when Docker fails

    2. **Capability gate error handling** (lines 59-66, 121-125, 228-229):
       - Test error handling when graduation service is unavailable
       - Test error handling when agent maturity level is invalid
       - Test error handling when workspace settings are malformed
       - Test permission denied paths with AUTONOMOUS agents
       - Test capability checks for specific operations (mutation, promotion)

    3. **Add timeout enforcement test** (currently failing):
       - Mock sandbox execution that exceeds timeout
       - Verify that execution is terminated after timeout seconds
       - Verify that error message includes timeout information
       - Test that timeout doesn't crash the sandbox (graceful termination)

    4. **Add Docker fallback test**:
       - Mock Docker as unavailable (raises exception)
       - Verify that subprocess execution is used as fallback
       - Verify that both methods return compatible result structures
       - Test that fallback execution has same security constraints

    Expected changes:
    - Add 3-4 new test methods for container_sandbox edge cases
    - Add 2-3 new test methods for capability_gate error handling
    - Fix 1 existing timeout test that's currently failing
    - Ensure all tests use proper mocking (no real Docker/subprocess calls)
  </action>
  <verify>
    <automated>cd /Users/rushiparikh/projects/atom && python3 -m pytest backend/tests/test_auto_dev/test_container_sandbox.py backend/tests/test_auto_dev/test_capability_gate.py -xvs 2>&1 | grep -E "(PASSED|FAILED|ERROR)" | head -30</automated>
  </verify>
  <done>
    - All 13 container_sandbox tests pass (0 failures)
    - All 13 capability_gate tests pass (0 failures)
    - Coverage increases to >=80% for container_sandbox.py (currently 62%)
    - Coverage increases to >=80% for capability_gate.py (currently 73%)
    - Docker fallback logic tested and working
    - Timeout enforcement tested and working
    - Error handling paths fully covered
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production | Test code must not modify production database or call external services |
| sandbox → host | ContainerSandbox must enforce security constraints to prevent escape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-290-01 | Spoofing | test_auto_dev | accept | Test environment only, no production impact |
| T-290-02 | Tampering | ContainerSandbox | mitigate | Ensure subprocess execution validates code before running, use chroot if available |
| T-290-03 | Repudiation | test logs | accept | Test execution logs are sufficient for debugging |
| T-290-04 | Information Disclosure | test database | accept | In-memory SQLite with no external connectivity |
| T-290-05 | Denial of Service | sandbox timeout | mitigate | Enforce timeout and memory limits in subprocess execution |
| T-290-06 | Elevation of Privilege | capability gate | mitigate | Validate maturity_level and workspace_settings before granting permissions |

</threat_model>

<verification>
## Overall Verification Steps

1. **Run all auto_dev tests**:
   ```bash
   python3 -m pytest backend/tests/test_auto_dev/ -v
   ```
   Expected: 152 tests collected, 152 passed, 0 failed

2. **Check coverage**:
   ```bash
   python3 -m pytest backend/tests/test_auto_dev/ --cov=core.auto_dev --cov-report=term-missing
   ```
   Expected: >=80% coverage for all auto_dev modules

3. **Verify specific test files**:
   ```bash
   python3 -m pytest backend/tests/test_auto_dev/test_fitness_service.py -v
   python3 -m pytest backend/tests/test_auto_dev/test_evolution_engine.py -v
   python3 -m pytest backend/tests/test_auto_dev/test_advisor_service.py -v
   python3 -m pytest backend/tests/test_auto_dev/test_reflection_engine.py -v
   python3 -m pytest backend/tests/test_auto_dev/test_alpha_evolver_engine.py -v
   python3 -m pytest backend/tests/test_auto_dev/test_container_sandbox.py -v
   python3 -m pytest backend/tests/test_auto_dev/test_capability_gate.py -v
   python3 -m pytest backend/tests/test_auto_dev/test_auto_dev_integration.py -v
   ```
   Expected: All tests pass (0 failures)

4. **Verify database persistence**:
   ```bash
   python3 -m pytest backend/tests/test_auto_dev/test_memento_engine.py::TestMementoEngineEpisodeAnalysis -v
   ```
   Expected: All episode analysis tests pass (episodes found in database)

5. **Performance check**:
   ```bash
   time python3 -m pytest backend/tests/test_auto_dev/ -v
   ```
   Expected: All tests complete in <60 seconds
</verification>

<success_criteria>
1. All 152 auto_dev tests pass (100% pass rate, 0 failures)
2. Coverage >=80% for all auto_dev modules (evolution_engine, alpha_evolver_engine, advisor_service, container_sandbox, capability_gate)
3. Episode model database queries work correctly (no "not found" errors)
4. Fitness scoring math verified and corrected (30% syntax, 40% execution, 30% semantic)
5. Pattern detection grouping logic working (similar failures grouped correctly)
6. Integration tests properly set up with database sessions (no state leakage)
7. All tests complete in <60 seconds (performance target maintained)
</success_criteria>

<output>
After completion, create `.planning/phases/290-comprehensive-test-suite/290-02-SUMMARY.md`
</output>
