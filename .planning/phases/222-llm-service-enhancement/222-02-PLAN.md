---
phase: 222-llm-service-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/llm_service.py
  - backend/tests/test_llm_service.py
autonomous: true

must_haves:
  truths:
    - "LLMService.generate_structured() method accepts Pydantic model and returns instance"
    - "Structured output validates JSON schema against provided model"
    - "Structured output supports tenant-aware routing (BYOK vs Managed)"
    - "Structured output includes vision support for multimodal inputs"
  artifacts:
    - path: "backend/core/llm_service.py"
      provides: "Structured output interface with JSON schema validation"
      contains: "async def generate_structured"
      exports: ["generate_structured"]
    - path: "backend/tests/test_llm_service.py"
      provides: "Test coverage for structured output"
      exports: ["test_generate_structured_basic", "test_generate_structured_vision"]
  key_links:
    - from: "backend/core/llm_service.py"
      to: "backend/core/llm/byok_handler.py"
      via: "self.handler.generate_structured_response()"
      pattern: "self\.handler\.generate_structured_response"
---

<objective>
Add structured output interface to LLMService by exposing BYOKHandler's generate_structured_response method with JSON schema validation.

Purpose: Enable LLMService to generate structured JSON output that validates against Pydantic models, using instructor library with tenant-aware routing and vision support.

Output: LLMService.generate_structured() method returning validated Pydantic model instances, with comprehensive test coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/llm_service.py
@backend/core/llm/byok_handler.py (has generate_structured_response method at ~1043-1250)
@backend/core/llm/cognitive_tier_system.py
</context>

<tasks>

<task type="auto">
  <name>Add generate_structured method to LLMService</name>
  <files>backend/core/llm_service.py</files>
  <action>
Add async generate_structured method to LLMService class:

1. Add method signature accepting:
   - prompt: str
   - response_model: type[BaseModel] (Pydantic model class)
   - system_instruction: str = "You are a helpful assistant."
   - temperature: float = 0.2
   - model: str = "auto"
   - task_type: Optional[str] = None
   - agent_id: Optional[str] = None
   - image_payload: Optional[str] = None

2. Delegate to self.handler.generate_structured_response() with parameters:
   - Extract system_instruction from prompt/messages if not provided
   - Analyze complexity for model selection if model == "auto"
   - Get optimal provider with requires_structured=True
   - Pass through all parameters including image_payload

3. Return type: Optional[response_model] (instance of provided Pydantic model or None if failed)

4. Add docstring with:
   - Example usage with Pydantic model
   - Description of tenant-aware routing (BYOK vs Managed)
   - Vision support documentation

5. Handle instructor not available case gracefully (return None)

Reference: BYOKHandler.generate_structured_response() at lines ~1043-1250
  </action>
  <verify>
grep -n "async def generate_structured" backend/core/llm_service.py
python -c "from core.llm_service import LLMService; import inspect; print(inspect.signature(LLMService.generate_structured))"
  </verify>
  <done>
LLMService has generate_structured method that returns Pydantic model instances
  </done>
</task>

<task type="auto">
  <name>Add structured output tests for LLMService</name>
  <files>backend/tests/test_llm_service.py</files>
  <action>
Add comprehensive tests for LLMService.generate_structured():

1. Test cases:
   - test_generate_structured_basic: Verify returns Pydantic model instance
   - test_generate_structured_auto_model: Verify auto model selection
   - test_generate_structured_with_vision: Verify vision payload support
   - test_generate_structured_instructor_unavailable: Verify graceful fallback
   - test_generate_structured_no_clients: Verify handles no clients

2. Use pytest-asyncio for async test support

3. Mock BYOKHandler.generate_structured_response:
   - Create sample Pydantic model for testing (e.g., TestResponse(name=str, value=int))
   - Mock returns instance of test model
   - Mock returns None for error cases

4. Verify:
   - Response is instance of provided model
   - Schema validation passes/fails appropriately
   - None returned on failure

Pattern: Reference Phase 68 test patterns for mocking async handlers
  </action>
  <verify>
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_llm_service.py -v -k structured
  </verify>
  <done>
All structured output tests pass (5+ tests) with mocked BYOKHandler
  </done>
</task>

<task type="auto">
  <name>Add structured output integration test</name>
  <files>backend/tests/test_llm_service.py</files>
  <action>
Add integration test for structured output with real models:

1. Test case: test_generate_structured_real_model
   - Use simple Pydantic model (e.g., SummarizationResult(summary: str, sentiment: str))
   - Mock BYOKHandler to return valid instance
   - Verify all fields are present and correct type

2. Test case: test_generate_structured_complex_model
   - Use nested Pydantic model (e.g., with List[SubModel])
   - Verify nested structure handled correctly

3. Test case: test_generate_structured_with_custom_system
   - Pass custom system_instruction
   - Verify it's passed through to handler

4. Add type checking test:
   - Verify return type annotation is correct (Optional[response_model])

This ensures real-world usage patterns work correctly.
  </action>
  <verify>
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_llm_service.py -v -k "structured and real"
  </verify>
  <done>
Integration tests pass with real Pydantic models
  </done>
</task>

</tasks>

<verification>
Overall verification steps:

1. Code verification:
   - LLMService.generate_structured() method exists and is async
   - Method delegates to self.handler.generate_structured_response()
   - Proper type hints with Optional[response_model] return type

2. Test verification:
   - pytest tests/test_llm_service.py -v -k structured
   - All tests pass
   - Coverage >80% for new code

3. Integration verification:
   - Test with real Pydantic model (nested structure)
   - Verify schema validation works
</verification>

<success_criteria>
1. LLMService exposes generate_structured method with Pydantic model parameter
2. Method returns validated instance of provided model or None
3. Supports tenant-aware routing (BYOK vs Managed AI)
4. Supports vision inputs via image_payload parameter
5. Test coverage >80% for structured output functionality
</success_criteria>

<output>
After completion, create `.planning/phases/222-llm-service-enhancement/222-02-SUMMARY.md`
</output>
