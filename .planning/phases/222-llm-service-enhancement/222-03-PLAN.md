---
phase: 222-llm-service-enhancement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/llm_service.py
  - backend/tests/test_llm_service.py
autonomous: true

must_haves:
  truths:
    - "LLMService.generate_with_tier() method accepts cognitive tier parameter"
    - "Cognitive tier routing integrates with CognitiveTierService"
    - "Method supports automatic escalation on quality issues"
    - "Method returns response with tier metadata (tier, provider, model, cost_cents, escalated)"
  artifacts:
    - path: "backend/core/llm_service.py"
      provides: "Cognitive tier routing interface"
      contains: "async def generate_with_tier"
      exports: ["generate_with_tier"]
    - path: "backend/tests/test_llm_service.py"
      provides: "Test coverage for cognitive tier routing"
      exports: ["test_generate_with_tier_basic", "test_generate_with_tier_escalation"]
  key_links:
    - from: "backend/core/llm_service.py"
      to: "backend/core/llm/byok_handler.py"
      via: "self.handler.generate_with_cognitive_tier()"
      pattern: "self\.handler\.generate_with_cognitive_tier"
    - from: "backend/core/llm_service.py"
      to: "backend/core/llm/cognitive_tier_service.py"
      via: "self.handler.tier_service"
      pattern: "tier_service"
---

<objective>
Add cognitive tier routing to LLMService by exposing BYOKHandler's generate_with_cognitive_tier method with intelligent 5-tier routing.

Purpose: Enable LLMService to use the full cognitive tier pipeline (classification -> budget check -> model selection -> generation -> escalation) for cost-optimized, quality-aware LLM routing.

Output: LLMService.generate_with_tier() method returning response with tier metadata, with comprehensive test coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/llm_service.py
@backend/core/llm/byok_handler.py (has generate_with_cognitive_tier method at ~862-1041)
@backend/core/llm/cognitive_tier_system.py (CognitiveTier enum: MICRO/STANDARD/VERSATILE/HEAVY/COMPLEX)
@backend/core/llm/cognitive_tier_service.py (orchestration layer for tier management)
</context>

<tasks>

<task type="auto">
  <name>Add generate_with_tier method to LLMService</name>
  <files>backend/core/llm_service.py</files>
  <action>
Add async generate_with_tier method to LLMService class:

1. Add method signature accepting:
   - prompt: str
   - system_instruction: str = "You are a helpful assistant."
   - task_type: Optional[str] = None
   - user_tier_override: Optional[str] = None (bypasses classification)
   - agent_id: Optional[str] = None
   - image_payload: Optional[str] = None

2. Delegate to self.handler.generate_with_cognitive_tier() with all parameters

3. Return type: Dict[str, Any] with keys:
   - response: str (generated text)
   - tier: str (cognitive tier used)
   - provider: str (provider ID used)
   - model: str (model name used)
   - cost_cents: float (estimated cost)
   - escalated: bool (whether escalation occurred)
   - request_id: str (unique request ID)
   - error: str (error message if failed)

4. Add comprehensive docstring with:
   - Example usage showing tier override
   - Description of 5-tier system (MICRO/STANDARD/VERSATILE/HEAVY/COMPLEX)
   - Escalation behavior documentation
   - Return value structure

5. Add CognitiveTier import for type hints

Reference: BYOKHandler.generate_with_cognitive_tier() at lines ~862-1041
  </action>
  <verify>
grep -n "async def generate_with_tier" backend/core/llm_service.py
python -c "from core.llm_service import LLMService; import inspect; print(inspect.signature(LLMService.generate_with_tier))"
  </verify>
  <done>
LLMService has generate_with_tier method returning Dict with tier metadata
  </done>
</task>

<task type="auto">
  <name>Add cognitive tier routing tests for LLMService</name>
  <files>backend/tests/test_llm_service.py</files>
  <action>
Add comprehensive tests for LLMService.generate_with_tier():

1. Test cases:
   - test_generate_with_tier_basic: Verify returns dict with expected keys
   - test_generate_with_tier_auto_classification: Verify tier classification works
   - test_generate_with_tier_user_override: Verify user_tier_override bypasses classification
   - test_generate_with_tier_with_vision: Verify image_payload support
   - test_generate_with_tier_budget_exceeded: Verify budget check rejection
   - test_generate_with_tier_escalation: Verify escalation on quality issues

2. Use pytest-asyncio for async test support

3. Mock BYOKHandler.generate_with_cognitive_tier:
   - Return sample dict with all expected keys
   - Test different tier values (micro, standard, versatile, heavy, complex)
   - Test escalation scenario (escalated=True)

4. Verify:
   - All expected keys present in response
   - Tier values are valid CognitiveTier enum values
   - Cost_cents is numeric
   - Escalated flag is boolean

Pattern: Reference Phase 68 cognitive tier tests
  </action>
  <verify>
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_llm_service.py -v -k "with_tier"
  </verify>
  <done>
All cognitive tier routing tests pass (6+ tests) with mocked BYOKHandler
  </done>
</task>

<task type="auto">
  <name>Add cognitive tier helper methods to LLMService</name>
  <files>backend/core/llm_service.py</files>
  <action>
Add helper methods to LLMService for cognitive tier operations:

1. Add classify_tier() method:
   - Delegates to self.handler.classify_cognitive_tier()
   - Returns CognitiveTier enum value
   - Accepts prompt and optional task_type

2. Add get_tier_description() method:
   - Returns human-readable tier description
   - Maps tier to characteristics (cost, quality, use cases)

3. Update imports to include CognitiveTier from core.llm.cognitive_tier_system

4. Add docstrings explaining:
   - When to use each helper method
   - Relationship between classification and routing

These helpers enable users to understand tier decisions without making API calls.
  </action>
  <verify>
grep -n "def classify_tier\|def get_tier_description" backend/core/llm_service.py
  </verify>
  <done>
LLMService has helper methods for cognitive tier classification and descriptions
  </done>
</task>

<task type="auto">
  <name>Add helper method tests</name>
  <files>backend/tests/test_llm_service.py</files>
  <action>
Add tests for cognitive tier helper methods:

1. Test cases:
   - test_classify_tier_simple: Verify simple prompts classify as MICRO
   - test_classify_tier_code: Verify code prompts classify higher
   - test_classify_tier_with_task_type: Verify task_type affects classification
   - test_get_tier_description: Verify returns valid descriptions for all tiers
   - test_get_tier_description_invalid: Verify handles invalid tier gracefully

2. Mock BYOKHandler.classify_cognitive_tier for deterministic testing

3. Verify:
   - Classify returns valid CognitiveTier enum values
   - Tier descriptions contain expected keywords (cost, quality, use cases)

4. Test all 5 tiers: MICRO, STANDARD, VERSATILE, HEAVY, COMPLEX
  </action>
  <verify>
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_llm_service.py -v -k "classify_tier or tier_description"
  </verify>
  <done>
All helper method tests pass (5+ tests)
  </done>
</task>

</tasks>

<verification>
Overall verification steps:

1. Code verification:
   - LLMService.generate_with_tier() method exists and is async
   - Method delegates to self.handler.generate_with_cognitive_tier()
   - Helper methods (classify_tier, get_tier_description) present
   - CognitiveTier imported for type hints

2. Test verification:
   - pytest tests/test_llm_service.py -v -k "tier"
   - All tests pass
   - Coverage >80% for new code

3. Integration verification:
   - Test tier classification for various prompt types
   - Verify tier descriptions for all 5 tiers
</verification>

<success_criteria>
1. LLMService exposes generate_with_tier method with cognitive tier routing
2. Method returns Dict with all required keys (response, tier, provider, model, cost_cents, escalated)
3. Supports user_tier_override to bypass classification
4. Supports automatic escalation on quality issues
5. Helper methods (classify_tier, get_tier_description) work correctly
6. Test coverage >80% for tier routing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/222-llm-service-enhancement/222-03-SUMMARY.md`
</output>
