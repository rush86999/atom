---
phase: 222-llm-service-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/llm_service.py
  - backend/tests/test_llm_service.py
autonomous: true

must_haves:
  truths:
    - "LLMService.stream_completion() method yields tokens as AsyncGenerator"
    - "Stream completion supports provider fallback on failure"
    - "Stream completion integrates with governance tracking (AgentExecution records)"
    - "Existing LLMService.generate() and generate_completion() methods unchanged"
  artifacts:
    - path: "backend/core/llm_service.py"
      provides: "Unified LLM service with streaming interface"
      contains: "async def stream_completion"
      exports: ["stream_completion", "generate", "generate_completion"]
    - path: "backend/tests/test_llm_service.py"
      provides: "Test coverage for LLMService streaming"
      exports: ["test_stream_completion_basic", "test_stream_completion_fallback"]
  key_links:
    - from: "backend/core/llm_service.py"
      to: "backend/core/llm/byok_handler.py"
      via: "self.handler.stream_completion()"
      pattern: "self\.handler\.stream_completion"
---

<objective>
Add streaming response interface to LLMService by exposing BYOKHandler's stream_completion method.

Purpose: Enable streaming LLM responses through the unified LLMService interface, supporting real-time token-by-token output with automatic provider fallback and governance tracking.

Output: LLMService.stream_completion() method yielding tokens as AsyncGenerator, with comprehensive test coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/llm_service.py
@backend/core/llm/byok_handler.py (has stream_completion method with provider fallback)
@backend/core/llm/cognitive_tier_system.py
</context>

<tasks>

<task type="auto">
  <name>Add stream_completion method to LLMService</name>
  <files>backend/core/llm_service.py</files>
  <action>
Add async stream_completion method to LLMService class:

1. Add method signature accepting:
   - messages: List[Dict[str, str]] (OpenAI format)
   - model: str = "auto"
   - provider_id: str = "auto"
   - temperature: float = 0.7
   - max_tokens: int = 1000
   - agent_id: Optional[str] = None
   - db: Optional[Session] = None

2. Delegate to self.handler.stream_completion() with parameters:
   - Analyze complexity from messages if provider_id == "auto"
   - Get optimal provider if auto, use provided provider_id otherwise
   - Map model from "auto" to optimal model based on complexity
   - Pass through all other parameters

3. Return type: AsyncGenerator[str, None] yielding tokens

4. Add docstring with example usage

5. DO NOT modify existing generate() or generate_completion() methods (backward compatibility)

Reference: BYOKHandler.stream_completion() at lines ~1387-1525 in byok_handler.py
  </action>
  <verify>
grep -n "async def stream_completion" backend/core/llm_service.py
python -c "from core.llm_service import LLMService; import inspect; print(inspect.signature(LLMService.stream_completion))"
  </verify>
  <done>
LLMService has stream_completion method that yields tokens via AsyncGenerator
  </done>
</task>

<task type="auto">
  <name>Add streaming tests for LLMService</name>
  <files>backend/tests/test_llm_service.py</files>
  <action>
Add comprehensive tests for LLMService.stream_completion():

1. Create test file if not exists: backend/tests/test_llm_service.py

2. Test cases:
   - test_stream_completion_basic: Verify tokens are yielded
   - test_stream_completion_auto_provider: Verify auto provider selection works
   - test_stream_completion_with_agent_id: Verify governance tracking integration
   - test_stream_completion_empty_messages: Handle edge case gracefully

3. Use pytest-asyncio for async test support
   - Mark async tests with @pytest.mark.asyncio

4. Mock BYOKHandler.stream_completion to avoid real API calls in tests
   - Use pytest.mockAsyncMock for mocking
   - Yield sample tokens: ["hello", " world", "!"]

5. Verify method signature and return type

Pattern: Reference Phase 68 test patterns (backend/tests/test_cognitive_tier_classification.py)
  </action>
  <verify>
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_llm_service.py -v -k stream
  </verify>
  <done>
All streaming tests pass (4+ tests) with mocked BYOKHandler
  </done>
</task>

<task type="auto">
  <name>Add backward compatibility verification</name>
  <files>backend/tests/test_llm_service.py</files>
  <action>
Add backward compatibility tests to ensure existing methods work unchanged:

1. Test cases:
   - test_generate_method_unchanged: Verify existing generate() works
   - test_generate_completion_method_unchanged: Verify generate_completion() works
   - test_get_provider_method_unchanged: Verify get_provider() works
   - test_estimate_tokens_method_unchanged: Verify estimate_tokens() works

2. Verify method signatures match original:
   - Same parameter names and types
   - Same return types

3. Test that existing code using LLMService continues to work:
   - Create LLMService instance
   - Call generate() with prompt
   - Verify response format unchanged

This ensures LLM-05 requirement (backward compatibility) is met.
  </action>
  <verify>
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_llm_service.py -v -k backward
  </verify>
  <done>
All backward compatibility tests pass (4+ tests)
  </done>
</task>

</tasks>

<verification>
Overall verification steps:

1. Code verification:
   - LLMService.stream_completion() method exists and is async
   - Method delegates to self.handler.stream_completion()
   - Existing methods (generate, generate_completion) unchanged

2. Test verification:
   - pytest tests/test_llm_service.py -v --cov=core/llm_service
   - All tests pass
   - Coverage >80% for new code

3. Integration verification:
   - Import LLMService and call stream_completion
   - Verify AsyncGenerator return type
</verification>

<success_criteria>
1. LLMService exposes stream_completion method yielding AsyncGenerator[str, None]
2. Stream completion supports auto provider selection and explicit provider_id
3. Stream completion integrates with BYOKHandler's provider fallback mechanism
4. All backward compatibility tests pass (existing methods unchanged)
5. Test coverage >80% for LLMService
</success_criteria>

<output>
After completion, create `.planning/phases/222-llm-service-enhancement/222-01-SUMMARY.md`
</output>
