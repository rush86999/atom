---
phase: 222-llm-service-enhancement
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/llm_service.py
  - backend/tests/test_llm_service.py
autonomous: true

must_haves:
  truths:
    - "LLMService.get_optimal_provider() method returns (provider, model) tuple"
    - "Provider selection supports complexity-based routing"
    - "Provider selection supports cache-aware cost optimization"
    - "Provider selection supports BYOK vs Managed AI routing"
  artifacts:
    - path: "backend/core/llm_service.py"
      provides: "Provider selection utilities"
      contains: "def get_optimal_provider"
      exports: ["get_optimal_provider", "get_ranked_providers", "get_routing_info"]
    - path: "backend/tests/test_llm_service.py"
      provides: "Test coverage for provider selection"
      exports: ["test_get_optimal_provider", "test_get_ranked_providers"]
  key_links:
    - from: "backend/core/llm_service.py"
      to: "backend/core/llm/byok_handler.py"
      via: "self.handler.get_optimal_provider()"
      pattern: "self\.handler\.get_optimal_provider"
---

<objective>
Add provider selection utilities to LLMService by exposing BYOKHandler's provider selection methods with cache-aware cost optimization.

Purpose: Enable LLMService users to query optimal provider/model combinations without making API calls, supporting UI preview and cost estimation features.

Output: LLMService methods (get_optimal_provider, get_ranked_providers, get_routing_info) with comprehensive test coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/llm_service.py
@backend/core/llm/byok_handler.py (has get_optimal_provider, get_ranked_providers, get_routing_info at ~397-1290)
@backend/core/llm/cache_aware_router.py (cache-aware cost calculation)
@backend/core/llm/cognitive_tier_system.py
</context>

<tasks>

<task type="auto">
  <name>Add get_optimal_provider method to LLMService</name>
  <files>backend/core/llm_service.py</files>
  <action>
Add get_optimal_provider method to LLMService class:

1. Add method signature accepting:
   - complexity: str = "moderate" (or QueryComplexity enum)
   - task_type: Optional[str] = None
   - prefer_cost: bool = True
   - tenant_plan: str = "free"
   - is_managed_service: bool = True
   - requires_tools: bool = False
   - requires_structured: bool = False

2. Delegate to self.handler.get_optimal_provider() with parameters:
   - Map complexity string to QueryComplexity enum
   - Pass through all other parameters

3. Return type: Tuple[str, str] (provider_id, model_name)

4. Add docstring with:
   - Example usage
   - Description of BPC (Benchmark-Price-Capability) algorithm
   - Complexity level explanation

5. Add QueryComplexity import from core.llm.byok_handler

Reference: BYOKHandler.get_optimal_provider() at lines ~397-420
  </action>
  <verify>
grep -n "def get_optimal_provider" backend/core/llm_service.py
python -c "from core.llm_service import LLMService; import inspect; print(inspect.signature(LLMService.get_optimal_provider))"
  </verify>
  <done>
LLMService has get_optimal_provider method returning (provider, model) tuple
  </done>
</task>

<task type="auto">
  <name>Add get_ranked_providers method to LLMService</name>
  <files>backend/core/llm_service.py</files>
  <action>
Add get_ranked_providers method to LLMService class:

1. Add method signature accepting:
   - complexity: str = "moderate"
   - task_type: Optional[str] = None
   - prefer_cost: bool = True
   - tenant_plan: str = "free"
   - is_managed_service: bool = True
   - requires_tools: bool = False
   - requires_structured: bool = False
   - estimated_tokens: int = 1000
   - cognitive_tier: Optional[str] = None

2. Delegate to self.handler.get_ranked_providers() with parameters:
   - Map complexity string to QueryComplexity enum
   - Map cognitive_tier string to CognitiveTier enum if provided
   - Pass through all other parameters

3. Return type: List[Tuple[str, str]] (list of (provider_id, model_name) tuples)

4. Add docstring with:
   - Example usage
   - Description of cache-aware cost scoring
   - Cognitive tier filtering explanation

5. Add CognitiveTier import if not already present

Reference: BYOKHandler.get_ranked_providers() at lines ~422-616
  </action>
  <verify>
grep -n "def get_ranked_providers" backend/core/llm_service.py
python -c "from core.llm_service import LLMService; import inspect; print(inspect.signature(LLMService.get_ranked_providers))"
  </verify>
  <done>
LLMService has get_ranked_providers method returning list of (provider, model) tuples
  </done>
</task>

<task type="auto">
  <name>Add get_routing_info method to LLMService</name>
  <files>backend/core/llm_service.py</files>
  <action>
Add get_routing_info method to LLMService class:

1. Add method signature accepting:
   - prompt: str
   - task_type: Optional[str] = None

2. Delegate to self.handler.get_routing_info() with parameters

3. Return type: Dict[str, Any] with keys:
   - complexity: str (complexity level)
   - selected_provider: str (provider ID)
   - selected_model: str (model name)
   - available_providers: List[str] (list of providers)
   - cost_tier: str (budget/mid/premium)
   - estimated_cost_usd: Optional[float] (estimated cost)

4. Add docstring with:
   - Example usage for UI preview
   - Description of returned fields
   - Use case: show user which model will be used before generating

5. Handle error case when no providers available

Reference: BYOKHandler.get_routing_info() at lines ~1257-1290
  </action>
  <verify>
grep -n "def get_routing_info" backend/core/llm_service.py
python -c "from core.llm_service import LLMService; import inspect; print(inspect.signature(LLMService.get_routing_info))"
  </verify>
  <done>
LLMService has get_routing_info method returning routing decision metadata
  </done>
</task>

<task type="auto">
  <name>Add provider selection tests for LLMService</name>
  <files>backend/tests/test_llm_service.py</files>
  <action>
Add comprehensive tests for provider selection methods:

1. Test cases for get_optimal_provider:
   - test_get_optimal_provider_basic: Verify returns (provider, model) tuple
   - test_get_optimal_provider_all_complexities: Test all complexity levels
   - test_get_optimal_provider_with_tools: Verify requires_tools filtering
   - test_get_optimal_provider_prefer_quality: Verify quality preference

2. Test cases for get_ranked_providers:
   - test_get_ranked_providers_returns_list: Verify returns list of tuples
   - test_get_ranked_providers_with_cache_aware: Verify token count affects ranking
   - test_get_ranked_providers_with_cognitive_tier: Verify tier filtering
   - test_get_ranked_providers_empty: Verify handles no providers gracefully

3. Test cases for get_routing_info:
   - test_get_routing_info_basic: Verify returns all expected keys
   - test_get_routing_info_estimates_cost: Verify cost estimation included
   - test_get_routing_info_no_providers: Verify error handling

4. Mock BYOKHandler methods to avoid dependency on real providers

5. Verify:
   - Return types match expected (tuples, lists, dicts)
   - All complexity levels map correctly
   - Cognitive tier filtering works
  </action>
  <verify>
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_llm_service.py -v -k "provider or routing"
  </verify>
  <done>
All provider selection tests pass (10+ tests) with mocked BYOKHandler
  </done>
</task>

</tasks>

<verification>
Overall verification steps:

1. Code verification:
   - LLMService.get_optimal_provider() method exists
   - LLMService.get_ranked_providers() method exists
   - LLMService.get_routing_info() method exists
   - All methods delegate to corresponding BYOKHandler methods
   - Proper type hints and imports

2. Test verification:
   - pytest tests/test_llm_service.py -v -k "provider or routing"
   - All tests pass
   - Coverage >80% for new code

3. Integration verification:
   - Test all complexity levels (simple, moderate, complex, advanced)
   - Test cognitive tier filtering
   - Verify cost estimation works
</verification>

<success_criteria>
1. LLMService exposes get_optimal_provider method returning (provider, model) tuple
2. LLMService exposes get_ranked_providers method returning list of ranked options
3. LLMService exposes get_routing_info method returning routing decision metadata
4. All provider selection methods support complexity-based routing
5. All provider selection methods support cache-aware cost optimization
6. Test coverage >80% for provider selection functionality
</success_criteria>

<output>
After completion, create `.planning/phases/222-llm-service-enhancement/222-04-SUMMARY.md`
</output>
