---
phase: 195-coverage-push-22-25
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_analytics_routes_coverage.py
autonomous: true

must_haves:
  truths:
    - "Analytics dashboard routes API endpoints achieve 70%+ line coverage"
    - "FastAPI TestClient used for endpoint testing"
    - "Analytics summary, sentiment, and correlation endpoints tested"
    - "Pass rate maintains >80% quality standard"
  artifacts:
    - path: "backend/tests/api/test_analytics_routes_coverage.py"
      provides: "Coverage tests for analytics dashboard routes"
      min_lines: 450
      contains: "TestAnalyticsRoutes"
  key_links:
    - from: "backend/tests/api/test_analytics_routes_coverage.py"
      to: "backend/api/analytics_dashboard_routes.py"
      via: "from api.analytics_dashboard_routes import"
      pattern: "from api.analytics_dashboard_routes import"
---

<objective>
Create comprehensive test coverage for analytics dashboard API routes (currently partial coverage). Test analytics summary, sentiment analysis, cross-platform correlation, and predictive insights endpoints using FastAPI TestClient. The analytics_dashboard_routes.py file has ~500+ lines with multiple endpoints covering message analytics and cross-platform insights.

Purpose: Ensure analytics API routes are tested for dashboard data aggregation workflows
Output: Test file (450+ lines) with 70%+ coverage, >80% pass rate
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-coverage-push-18-22/194-07-PLAN.md
@.planning/phases/194-coverage-push-18-22/194-07-SUMMARY.md
@.planning/codebase/TESTING.md

# Analytics routes context
@backend/api/analytics_dashboard_routes.py

# Existing tests reference
@backend/tests/api/test_analytics_dashboard_endpoints.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create analytics routes API coverage tests</name>
  <files>backend/tests/api/test_analytics_routes_coverage.py</files>
  <action>
Create `test_analytics_routes_coverage.py` with FastAPI TestClient:

1. **Test file structure:**
   ```python
   """
   Coverage-driven tests for analytics_dashboard_routes.py (partial -> 70%+ target)

   API Endpoints Tested:
   - GET /api/analytics/summary - Comprehensive analytics summary
   - GET /api/analytics/sentiment - Sentiment analysis breakdown
   - GET /api/analytics/cross-platform - Cross-platform correlation
   - GET /api/analytics/predictive - Predictive insights

   Coverage Target Areas:
   - Lines 1-30: Route initialization and dependencies
   - Lines 30-85: Summary endpoint
   - Lines 85-150: Sentiment endpoint
   - Lines 150-250: Cross-platform endpoint
   - Lines 250-350: Predictive endpoint
   """
   from fastapi.testclient import TestClient
   from unittest.mock import Mock, patch, AsyncMock, MagicMock
   from datetime import datetime, timezone
   import pytest

   from api.analytics_dashboard_routes import router
   from core.message_analytics_engine import MessageAnalyticsEngine, get_message_analytics_engine
   from core.cross_platform_correlation import CrossPlatformCorrelationEngine, get_cross_platform_correlation_engine
   from core.predictive_insights import PredictiveInsightsEngine, get_predictive_insights_engine, UrgencyLevel
   ```

2. **Fixtures setup:**
   ```python
   @pytest.fixture
   def mock_analytics_engine():
       """Mock MessageAnalyticsEngine."""
       engine = Mock(spec=MessageAnalyticsEngine)
       return engine

   @pytest.fixture
   def mock_correlation_engine():
       """Mock CrossPlatformCorrelationEngine."""
       engine = Mock(spec=CrossPlatformCorrelationEngine)
       return engine

   @pytest.fixture
   def mock_predictive_engine():
       """Mock PredictiveInsightsEngine."""
       engine = Mock(spec=PredictiveInsightsEngine)
       return engine

   @pytest.fixture
   def client(mock_analytics_engine, mock_correlation_engine, mock_predictive_engine):
       """Test client with mocked engines."""
       def override_analytics():
           return mock_analytics_engine

       def override_correlation():
           return mock_correlation_engine

       def override_predictive():
           return mock_predictive_engine

       from core.main import app
       app.dependency_overrides[get_message_analytics_engine] = override_analytics
       app.dependency_overrides[get_cross_platform_correlation_engine] = override_correlation
       app.dependency_overrides[get_predictive_insights_engine] = override_predictive

       client = TestClient(app)
       yield client

       app.dependency_overrides.clear()
   ```

3. **Summary endpoint tests (10 tests):**
   ```python
   def test_get_analytics_summary_default(self, client, mock_analytics_engine):
       """Cover summary endpoint with default parameters (lines 26-83)."""
       response = client.get("/api/analytics/summary")

       assert response.status_code == 200
       result = response.json()
       assert result["time_window"] == "24h"
       assert "message_stats" in result
       assert "response_times" in result
       assert "activity_peaks" in result
       assert "cross_platform" in result

   def test_get_analytics_summary_custom_time_window(self, client, mock_analytics_engine):
       """Cover summary with custom time window."""
       response = client.get("/api/analytics/summary?time_window=7d")

       assert response.status_code == 200
       assert response.json()["time_window"] == "7d"

   @pytest.mark.parametrize("time_window", ["24h", "7d", "30d", "all"])
   def test_get_analytics_summary_time_windows(self, client, mock_analytics_engine, time_window):
       """Cover various time window options."""
       response = client.get(f"/api/analytics/summary?time_window={time_window}")

       assert response.status_code == 200
       assert response.json()["time_window"] == time_window

   def test_get_analytics_summary_with_platform_filter(self, client, mock_analytics_engine):
       """Cover summary with platform filter."""
       response = client.get("/api/analytics/summary?platform=slack")

       assert response.status_code == 200
       assert "platform_filter" in response.json()

   def test_get_analytics_summary_message_stats_structure(self, client, mock_analytics_engine):
       """Cover message stats structure in summary."""
       response = client.get("/api/analytics/summary")

       assert response.status_code == 200
       stats = response.json()["message_stats"]
       assert "total_messages" in stats
       assert "total_words" in stats
       assert "with_attachments" in stats
       assert "with_mentions" in stats
       assert "with_urls" in stats
       assert "sentiment_distribution" in stats

   def test_get_analytics_summary_response_times_structure(self, client, mock_analytics_engine):
       """Cover response times structure in summary."""
       response = client.get("/api/analytics/summary")

       assert response.status_code == 200
       times = response.json()["response_times"]
       assert "avg_response_seconds" in times
       assert "median_response_seconds" in times
       assert "p95_response_seconds" in times
       assert "total_responses_analyzed" in times

   def test_get_analytics_summary_cross_platform_structure(self, client, mock_analytics_engine):
       """Cover cross-platform structure in summary."""
       response = client.get("/api/analytics/summary")

       assert response.status_code == 200
       cp = response.json()["cross_platform"]
       assert "platforms" in cp
       assert "most_active_platform" in cp
       assert "total_messages" in cp
   ```

4. **Sentiment endpoint tests (8 tests):**
   ```python
   def test_get_sentiment_analysis_default(self, client):
       """Cover sentiment analysis with default parameters (lines 86-100+)."""
       response = client.get("/api/analytics/sentiment")

       assert response.status_code == 200
       result = response.json()
       assert "distribution" in result
       assert "trends" in result

   def test_get_sentiment_with_platform_filter(self, client):
       """Cover sentiment with platform filter."""
       response = client.get("/api/analytics/sentiment?platform=slack")

       assert response.status_code == 200

   def test_get_sentiment_with_time_window(self, client):
       """Cover sentiment with time window."""
       response = client.get("/api/analytics/sentiment?time_window=7d")

       assert response.status_code == 200
       assert response.json()["time_window"] == "7d"

   @pytest.mark.parametrize("sentiment", ["positive", "negative", "neutral"])
   def test_sentiment_distribution_categories(self, client, sentiment):
       """Cover sentiment distribution categories."""
       response = client.get("/api/analytics/sentiment")

       assert response.status_code == 200
       distribution = response.json().get("distribution", {})
       assert sentiment in distribution
   ```

5. **Cross-platform endpoint tests (8 tests):**
   ```python
   @patch('api.analytics_dashboard_routes.get_cross_platform_correlation_engine')
   def test_get_cross_platform_correlation(self, mock_get_engine, client, mock_correlation_engine):
       """Cover cross-platform correlation endpoint."""
       mock_get_engine.return_value = mock_correlation_engine
       mock_correlation_engine.get_correlated_messages.return_value = []

       response = client.get("/api/analytics/cross-platform")

       assert response.status_code == 200
       result = response.json()
       assert "correlations" in result

   def test_get_cross_platform_with_time_range(self, client):
       """Cover cross-platform with time range."""
       response = client.get("/api/analytics/cross-platform?start=2024-01-01&end=2024-01-31")

       assert response.status_code == 200

   def test_get_cross_platform_with_platforms(self, client):
       """Cover cross-platform with specific platforms."""
       response = client.get("/api/analytics/cross-platform?platforms=slack,discord")

       assert response.status_code == 200

   @pytest.mark.parametrize("threshold", [0.5, 0.7, 0.9])
   def test_get_cross_platform_with_threshold(self, client, threshold):
       """Cover cross-platform with correlation threshold."""
       response = client.get(f"/api/analytics/cross-platform?threshold={threshold}")

       assert response.status_code == 200
   ```

6. **Predictive insights endpoint tests (10 tests):**
   ```python
   @patch('api.analytics_dashboard_routes.get_predictive_insights_engine')
   def test_get_predictive_insights_default(self, mock_get_engine, client, mock_predictive_engine):
       """Cover predictive insights endpoint (lines 150-250+)."""
       mock_get_engine.return_value = mock_predictive_engine
       mock_predictive_engine.generate_insights.return_value = {
           "predictions": [],
           "urgency": UrgencyLevel.LOW.value
       }

       response = client.get("/api/analytics/predictive")

       assert response.status_code == 200
       result = response.json()
       assert "predictions" in result

   @pytest.mark.parametrize("urgency", ["low", "medium", "high", "critical"])
   def test_predictive_insights_urgency_levels(self, client, urgency):
       """Cover various urgency levels."""
       response = client.get(f"/api/analytics/predictive?urgency={urgency}")

       assert response.status_code == 200

   def test_predictive_insights_with_lookahead(self, client):
       """Cover predictive insights with lookahead parameter."""
       response = client.get("/api/analytics/predictive?lookahead_days=7")

       assert response.status_code == 200

   def test_predictive_insights_confidence_threshold(self, client):
       """Cover predictive insights with confidence threshold."""
       response = client.get("/api/analytics/predictive?min_confidence=0.8")

       assert response.status_code == 200
   ```

7. **Error handling tests (6 tests):**
   ```python
   @patch('api.analytics_dashboard_routes.get_message_analytics_engine')
   def test_summary_endpoint_exception_handling(self, mock_get_engine, client):
       """Cover exception handling in summary endpoint."""
       mock_get_engine.side_effect = Exception("Analytics engine failed")

       response = client.get("/api/analytics/summary")

       # Should return error response, not crash
       assert response.status_code in [500, 200]

   @patch('api.analytics_dashboard_routes.get_predictive_insights_engine')
   def test_predictive_endpoint_exception_handling(self, mock_get_engine, client):
       """Cover exception handling in predictive endpoint."""
       mock_get_engine.side_effect = Exception("Predictive engine failed")

       response = client.get("/api/analytics/predictive")

       assert response.status_code in [500, 200]
   ```

8. **Query parameter validation tests (4 tests):**
   ```python
   @pytest.mark.parametrize("invalid_time_window", ["invalid", "123", ""])
   def test_invalid_time_window_handling(self, client, invalid_time_window):
       """Cover handling of invalid time window parameters."""
       response = client.get(f"/api/analytics/summary?time_window={invalid_time_window}")

       # Should use default or return validation error
       assert response.status_code in [200, 422]

   def test_multiple_query_parameters(self, client):
       """Cover multiple query parameters combined."""
       response = client.get("/api/analytics/summary?time_window=7d&platform=slack")

       assert response.status_code == 200
       result = response.json()
       assert result["time_window"] == "7d"
   ```

**Total target: 45-55 tests (450-550 lines)** covering all analytics API endpoints
  </action>
  <verify>pytest backend/tests/api/test_analytics_routes_coverage.py -v --tb=short 2>&1 | grep -E "(test_.*PASSED|test_.*FAILED|test_.*ERROR)" | head -30</verify>
  <done>45-55 tests created for analytics routes API, targeting 70%+ coverage</done>
</task>

<task type="auto">
  <name>Task 2: Generate coverage report for analytics routes</name>
  <files>.planning/phases/195-coverage-push-22-25/195-03-coverage.json</files>
  <action>
Run coverage measurement for analytics routes:

1. **Generate coverage report:**
   ```bash
   cd /Users/rushiparikh/projects/atom/backend && \
   PYTHONPATH=/Users/rushiparikh/projects/atom/backend \
   pytest tests/api/test_analytics_routes_coverage.py \
     --cov=api/analytics_dashboard_routes \
     --cov-report=term-missing \
     --cov-report=json:/Users/rushiparikh/projects/atom/.planning/phases/195-coverage-push-22-25/195-03-coverage.json \
     -v \
     --tb=short
   ```

2. **Check coverage achieved:**
   ```bash
   cat .planning/phases/195-coverage-push-22-25/195-03-coverage.json | \
     python3 -c "
import json, sys
d = json.load(sys.stdin)
file_data = d['files'].get('api/analytics_dashboard_routes.py', {})
if file_data:
    summary = file_data.get('summary', {})
    print(f\"Coverage: {summary.get('percent_covered', 0):.1f}%\")
    print(f\"Statements: {summary.get('covered_lines', 0)}/{summary.get('num_statements', 0)}\")
else:
    print('File not found in coverage report')
"
   ```

3. **Verify pass rate:**
   - Target: >80% pass rate
   - All analytics endpoints tested (summary, sentiment, cross-platform, predictive)
  </action>
  <verify>cat .planning/phases/195-coverage-push-22-25/195-03-coverage.json | python3 -c "import json,sys; d=json.load(sys.stdin); f=d['files'].get('api/analytics_dashboard_routes.py',{}); print(f\"Coverage: {f.get('summary',{}).get('percent_covered',0):.1f}%\" if f else 'N/A')"</verify>
  <done>Coverage report showing 70%+ coverage, pass rate >80%</done>
</task>

</tasks>

<verification>
1. Test file created at backend/tests/api/test_analytics_routes_coverage.py (450+ lines)
2. FastAPI TestClient used for endpoint testing
3. Summary endpoint tested (various time windows, platform filters)
4. Sentiment endpoint tested (distribution, trends, filters)
5. Cross-platform endpoint tested (correlations, thresholds)
6. Predictive endpoint tested (urgency levels, lookahead, confidence)
7. Error handling tested
8. Query parameter validation tested
9. Pass rate >80%
10. Coverage 70%+ achieved
11. 45-55 tests created
</verification>

<success_criteria>
- Analytics routes coverage: 70%+ target
- Pass rate: >80%
- All endpoints tested (summary, sentiment, cross-platform, predictive)
- Error paths tested
- 45-55 tests created
</success_criteria>

<output>
After completion, create `.planning/phases/195-coverage-push-22-25/195-03-SUMMARY.md` with:
- Coverage achieved (percent and statements)
- Pass rate achieved
- Endpoints tested
- Key findings from analytics API testing
</output>
