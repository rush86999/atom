---
phase: 101-backend-core-services-unit-tests
plan: 04
type: tdd
wave: 2
depends_on: ["101-01", "101-02", "101-03"]
files_modified:
  - backend/tests/property_tests/governance/test_governance_invariants_property.py
  - backend/tests/property_tests/episodes/test_episode_invariants_property.py
  - backend/tests/property_tests/canvas/test_canvas_invariants_property.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - Property-based tests validate critical invariants for governance, episodes, and canvas
    - Hypothesis tests use strategic max_examples (200 for critical, 100 for standard, 50 for IO-bound)
    - All property tests pass with 100+ examples per invariant
    - Invariants documented in docstrings with VALIDATED_BUG pattern for bug findings
  artifacts:
    - path: backend/tests/property_tests/governance/test_governance_invariants_property.py
      provides: Property-based tests for governance invariants
      min_lines: 200
    - path: backend/tests/property_tests/episodes/test_episode_invariants_property.py
      provides: Property-based tests for episode invariants
      min_lines: 200
    - path: backend/tests/property_tests/canvas/test_canvas_invariants_property.py
      provides: Property-based tests for canvas invariants
      min_lines: 150
  key_links:
    - from: backend/tests/property_tests/governance/test_governance_invariants_property.py
      to: backend/core/agent_governance_service.py
      via: Hypothesis strategies and property testing
      pattern: @given(st.*) property decorators
    - from: backend/tests/property_tests/episodes/test_episode_invariants_property.py
      to: backend/core/episode_segmentation_service.py
      via: Hypothesis strategies and property testing
      pattern: @given(st.*) property decorators
    - from: backend/tests/property_tests/canvas/test_canvas_invariants_property.py
      to: backend/tools/canvas_tool.py
      via: Hypothesis strategies and property testing
      pattern: @given(st.*) property decorators
---

<objective>
Create property-based tests for critical invariants in governance, episodes, and canvas services

Purpose: Property-based testing validates system invariants across thousands of auto-generated inputs, finding edge cases that example-based tests miss. This plan focuses on state machine invariants (maturity levels), data transformations (segmentation boundaries), and business rules (permission checks).

Output: Three property test files with 550+ total lines, validating critical invariants with Hypothesis strategies
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/core/agent_governance_service.py
@backend/core/episode_segmentation_service.py
@backend/tools/canvas_tool.py
@backend/tests/property_tests/governance/test_governance_invariants.py

# From Phase 100 decisions
# Strategic max_examples: 200 for critical invariants, 100 for standard, 50 for IO-bound
# VALIDATED_BUG docstring pattern for bug-finding evidence
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create property-based tests for governance invariants</name>
  <files>backend/tests/property_tests/governance/test_governance_invariants_property.py</files>
  <action>
Create new property test file backend/tests/property_tests/governance/test_governance_invariants_property.py:

**Test Structure (TDD RED→GREEN→REFACTOR):**

A. Maturity Level Invariants (max_examples=200 - CRITICAL):
   - test_maturity_levels_total_ordering
     Invariant: STUDENT < INTERN < SUPERVISED < AUTONOMOUS
     Strategy: st.sampled_from(["STUDENT", "INTERN", "SUPERVISED", "AUTONOMOUS"])

   - test_action_complexity_permitted_by_maturity
     Invariant: For any action, permitted iff complexity <= maturity_level
     Strategy: st.tuples(maturity_level, action_complexity)

   - test_maturity_never_decreases
     Invariant: Once promoted, agent maturity never decreases
     Strategy: st.lists of maturity transitions

B. Permission Check Invariants (max_examples=100 - STANDARD):
   - test_permission_check_idempotent
     Invariant: Calling permission_check twice with same inputs returns same result
     Strategy: st.tuples(agent_id, action_type, context)

   - test_denied_permission_has_reason
     Invariant: If permission denied, reason field is non-empty string
     Strategy: st.tuples(agent_id, action_type)

C. Governance Cache Invariants (max_examples=200 - CRITICAL):
   - test_cache_lookup_under_1ms
     Invariant: Cached governance checks complete in <1ms (performance property)
     Strategy: st.lists of agent_ids for batch lookup

   - test_cache_consistency_with_database
     Invariant: Cached value matches database query for same key
     Strategy: st.sampled_from(cached_keys)

**Documentation Pattern:**
```python
def test_maturity_levels_total_ordering(levels):
    """
    PROPERTY: Maturity levels form total ordering (STUDENT < INTERN < SUPERVISED < AUTONOMOUS)

    STRATEGY: st.sampled_from(["STUDENT", "INTERN", "SUPERVISED", "AUTONOMOUS"])

    INVARIANT: For any two levels a, b: a < b OR b < a OR a == b

    RADII: 200 examples explores all 6 pairwise comparisons

    VALIDATED_BUG: None found (invariant holds)
    """
```

**Target:** 200+ lines, 8-10 properties
  </action>
  <verify>
Run tests: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/property_tests/governance/test_governance_invariants_property.py -v --hypothesis-seed=0
Verify all properties pass with 200/100 examples
  </verify>
  <done>
All 8-10 property tests pass, Hypothesis explores 200+ examples per property
  </done>
</task>

<task type="auto">
  <name>Task 2: Create property-based tests for episode invariants</name>
  <files>backend/tests/property_tests/episodes/test_episode_invariants_property.py</files>
  <action>
Create new property test file backend/tests/property_tests/episodes/test_episode_invariants_property.py:

**Test Structure (TDD RED→GREEN→REFACTOR):**

A. Segmentation Boundary Invariants (max_examples=200 - CRITICAL):
   - test_time_gap_exclusive_threshold
     Invariant: Gap of exactly threshold minutes does NOT trigger segmentation
     Strategy: st.datetimes with gap exactly at threshold

   - test_boundaries_increase_monotonically
     Invariant: Boundary indices always increase (no duplicates, no decreasing)
     Strategy: st.lists of message timestamps

   - test_segmentation_preserves_message_order
     Invariant: Messages within segment maintain original order
     Strategy: st.lists of (timestamp, content) tuples

B. Retrieval Mode Invariants (max_examples=100 - STANDARD):
   - test_temporal_retrieval_time_bound
     Invariant: Episodes retrieved within specified time range only
     Strategy: st.tuples(time_range, episodes)

   - test_semantic_retrieval_similarity_decreases
     Invariant: Results sorted by decreasing similarity score
     Strategy: st.lists of (episode, similarity_score)

   - test_sequential_retrieval_completeness
     Invariant: Sequential retrieval returns all segments of episode
     Strategy: st.lists of episode segments

C. Lifecycle State Invariants (max_examples=100 - STANDARD):
   - test_decay_score_non_negative
     Invariant: Decay score always in [0, 1]
     Strategy: st.integers for episode age in days

   - test_decay_score_monotonically_decreases
     Invariant: Decay score decreases (or stays same) as episode ages
     Strategy: st.lists of (age_1, age_2) where age_2 > age_1

   - test_archived_episodes_read_only
     Invariant: Archived episodes cannot be modified
     Strategy: st.sampled_from(episode_states)

**Target:** 200+ lines, 10-12 properties
  </action>
  <verify>
Run tests: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/property_tests/episodes/test_episode_invariants_property.py -v --hypothesis-seed=0
Verify all properties pass with 200/100 examples
  </verify>
  <done>
All 10-12 property tests pass, Hypothesis explores 200+ examples per property
  </done>
</task>

<task type="auto">
  <name>Task 3: Create property-based tests for canvas invariants</name>
  <files>backend/tests/property_tests/canvas/test_canvas_invariants_property.py</files>
  <action>
Create new property test file backend/tests/property_tests/canvas/test_canvas_invariants_property.py:

**Test Structure (TDD RED→GREEN→REFACTOR):**

A. Canvas Audit Invariants (max_examples=100 - STANDARD):
   - test_audit_created_for_every_present
     Invariant: Every canvas presentation creates exactly one audit entry
     Strategy: st.tuples(user_id, canvas_type, action)

   - test_audit_timestamp_after_creation
     Invariant: Audit created_at <= current_time (within clock skew tolerance)
     Strategy: st.datetimes for operation timestamps

   - test_audit_governance_reflection
     Invariant: governance_check_passed matches actual governance decision
     Strategy: st.tuples(agent_maturity, action_complexity)

B. Chart Data Invariants (max_examples=100 - STANDARD):
   - test_chart_data_non_empty
     Invariant: Chart presentation requires non-empty data list
     Strategy: st.lists of dictionaries

   - test_chart_keys_consistent
     Invariant: All data rows have same keys (table consistency)
     Strategy: st.lists of st.dictionaries(st.sampled_from(["x", "y", "label"]))

C. Component Type Invariants (max_examples=50 - IO_BOUND):
   - test_registered_component_type_valid
     Invariant: Registered component types have required fields
     Strategy: st.sampled_from(canvas_type_registry.keys())

   - test_canvas_id_unique
     Invariant: Concurrent presentations generate unique canvas IDs
     Strategy: st.lists of presentation calls

**Target:** 150+ lines, 6-8 properties
  </action>
  <verify>
Run tests: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/property_tests/canvas/test_canvas_invariants_property.py -v --hypothesis-seed=0
Verify all properties pass with 100/50 examples
  </verify>
  <done>
All 6-8 property tests pass, Hypothesis explores 100+ examples per property
  </done>
</task>

</tasks>

<verification>
Run comprehensive property test suite:
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/property_tests/governance/test_governance_invariants_property.py backend/tests/property_tests/episodes/test_episode_invariants_property.py backend/tests/property_tests/canvas/test_canvas_invariants_property.py -v --hypothesis-seed=0

Verify:
1. All property tests pass
2. Hypothesis explores target examples (200/100/50 based on criticality)
3. No flaky tests (add --hypothesis-seed=0 for reproducibility)
4. Invariants documented with VALIDATED_BUG pattern
5. Test execution completes in reasonable time (<2 minutes)
</verification>

<success_criteria>
1. Governance invariants validated (maturity ordering, permission checks, cache consistency)
2. Episode invariants validated (boundary detection, retrieval modes, lifecycle states)
3. Canvas invariants validated (audit creation, data consistency, component types)
4. Strategic max_examples applied (200 critical, 100 standard, 50 IO-bound)
5. All invariants documented with clear explanations in docstrings
6. Property tests integrate with existing pytest infrastructure
</success_criteria>

<output>
After completion, create `.planning/phases/101-backend-core-services-unit-tests/101-04-SUMMARY.md`
</output>
