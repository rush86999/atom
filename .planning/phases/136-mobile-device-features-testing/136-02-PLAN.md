---
phase: 136-mobile-device-features-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/__tests__/services/locationService.test.ts
  - mobile/src/__tests__/helpers/deviceMocks.ts
autonomous: true

must_haves:
  truths:
    - "Location service achieves 80%+ test coverage"
    - "Background location tracking tested with permission flow (Android)"
    - "Geofence enter/exit event notifications tested with callbacks"
    - "Location history CRUD operations tested (add, get, clear)"
    - "Settings deep link tested (Linking.openURL mocked)"
    - "Battery usage indicator tested based on tracking state and accuracy"
    - "Reverse geocoding tested with missing address components"
  artifacts:
    - path: mobile/src/__tests__/services/locationService.test.ts
      provides: "Location service test suite with 80%+ coverage"
      min_lines: 900
      exports: ["locationService tests"]
    - path: mobile/src/__tests__/helpers/deviceMocks.ts
      provides: "Location-specific mock factories"
      min_lines: 150
  key_links:
    - from: mobile/src/__tests__/services/locationService.test.ts
      to: mobile/src/services/locationService.ts
      via: "import { locationService } from '../../services/locationService'"
      pattern: "locationService\\.(startBackgroundTracking|onGeofenceEvent|getLocationHistory)"
---

<objective>
**Add comprehensive tests for location service features to achieve 80%+ coverage**

**Purpose:** Location service has existing tests (47 tests, ~75% coverage) but is missing coverage for background tracking (Android-specific), geofence event notifications, location history CRUD operations, settings deep link, battery usage indicator, and edge cases in geocoding. This plan adds ~18 new tests targeting 80%+ coverage.

**Output:** Enhanced locationService.test.ts with ~65 total tests (47 existing + 18 new), deviceMocks.ts extended with location-specific mock factories.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-mobile-device-features-testing/136-RESEARCH.md

# Phase 135 Foundation
@.planning/phases/135-mobile-coverage-foundation/135-07-GAP_CLOSURE_PLAN.md

# Service Implementation
@mobile/src/services/locationService.ts

# Existing Tests
@mobile/src/__tests__/services/locationService.test.ts
@mobile/src/__tests__/helpers/testUtils.ts

# Test Infrastructure
@mobile/jest.setup.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add background location tracking tests</name>
  <files>mobile/src/__tests__/services/locationService.test.ts</files>
  <action>
    Add tests for background location tracking (Android-specific feature):

    1. Test startBackgroundTracking requests background permission on Android:
       - Mock Platform.OS = 'android'
       - Mock Location.requestBackgroundPermissionsAsync returns 'granted'
       - Call startBackgroundTracking
       - Verify background permission requested, isBackgroundTrackingActive = true

    2. Test startBackgroundTracking fails when permission denied:
       - Mock Platform.OS = 'android'
       - Mock background permission returns 'denied'
       - Call startBackgroundTracking
       - Verify returns false, isBackgroundTrackingActive = false

    3. Test startBackgroundTracking works on iOS with foreground permission:
       - Mock Platform.OS = 'ios'
       - Mock foreground permission granted
       - Call startBackgroundTracking
       - Verify succeeds without requesting background (iOS uses 'Always' permission)

    4. Test stopTracking clears background tracking state:
       - Start background tracking
       - Call stopTracking
       - Verify isBackgroundTrackingActive = false, watchId cleared

    5. Test stopTracking clears updateTimer:
       - Start tracking (sets updateTimer via throttling)
       - Call stopTracking
       - Verify updateTimer = null

    Pattern from existing tracking tests:
    ```typescript
    describe('Background Tracking', () => {
      beforeEach(() => {
        mockPlatform('android');
      });

      test('should request background permission on Android', async () => {
        // ... test implementation
      });
    });
    ```
  </action>
  <verify>npm test -- locationService.test.ts --testNamePattern="Background Tracking"</verify>
  <done>5 new background tracking tests added, covering Android permission flow, iOS behavior, and state cleanup</done>
</task>

<task type="auto">
  <name>Task 2: Add geofence event notification tests</name>
  <files>mobile/src/__tests__/services/locationService.test.ts</files>
  <action>
    Add tests for geofence event notifications with callbacks:

    1. Test onGeofenceEvent subscribes to enter events:
       - Add geofence with notifyOnEntry = true
       - Subscribe callback with onGeofenceEvent
       - Trigger location update inside geofence
       - Verify callback invoked with 'enter' event

    2. Test onGeofenceEvent subscribes to exit events:
       - Add geofence with notifyOnExit = true
       - Subscribe callback
       - Trigger location update outside geofence
       - Verify callback invoked with 'exit' event

    3. Test onGeofenceEvent unsubscribe removes callback:
       - Subscribe to geofence events
       - Call unsubscribe function returned by onGeofenceEvent
       - Trigger location update
       - Verify callback NOT invoked

    4. Test multiple geofence listeners all notified:
       - Add 2 geofence regions
       - Subscribe 2 different callbacks
       - Trigger location inside both regions
       - Verify both callbacks invoked

    5. Test geofence notification includes correct data:
       - Verify notification object has: region, event ('enter'|'exit'), location, timestamp
       - Verify location has coords, timestamp

    Use waitForCondition from testUtils.ts for async event verification:
    ```typescript
    await waitForCondition(() => callbackInvoked === true, 5000);
    ```
  </action>
  <verify>npm test -- locationService.test.ts --testNamePattern="Geofence Events"</verify>
  <done>5 new geofence event tests added, covering enter/exit notifications, unsubscribe, multiple listeners, and notification data structure</done>
</task>

<task type="auto">
  <name>Task 3: Add location history and settings tests</name>
  <files>mobile/src/__tests__/services/locationService.test.ts</files>
  <action>
    Add tests for location history CRUD and settings deep link:

    1. Test addToHistory adds location entry:
       - Mock AsyncStorage.getItem returns empty history
       - Call getCurrentLocation (which adds to history)
       - Verify AsyncStorage.setItem called with JSON containing new entry

    2. Test getLocationHistory returns all entries:
       - Mock history with 10 entries
       - Call getLocationHistory
       - Verify returns 10 entries, most recent last

    3. Test getLocationHistory with limit parameter:
       - Mock history with 100 entries
       - Call getLocationHistory(10)
       - Verify returns only last 10 entries

    4. Test clearLocationHistory empties storage:
       - Mock history with entries
       - Call clearLocationHistory
       - Verify AsyncStorage.removeItem called with HISTORY_STORAGE_KEY
       - Verify getLocationHistory returns empty array

    5. Test openSettings calls Linking.openURL on iOS:
       - Mock Platform.OS = 'ios'
       - Mock Linking.openURL
       - Call openSettings
       - Verify openURL called with 'app-settings:'

    6. Test openSettings sends intent on Android:
       - Mock Platform.OS = 'android'
       - Mock Linking.sendIntent
       - Call openSettings
       - Verify sendIntent called with 'android.settings.LOCATION_SOURCE_SETTINGS'

    Mock AsyncStorage pattern from jest.setup.js:
    ```typescript
    const mockAsyncStorage = require('@react-native-async-storage/async-storage');
    mockAsyncStorage.getItem.mockResolvedValue(JSON.stringify(historyEntries));
    ```
  </action>
  <verify>npm test -- locationService.test.ts --testNamePattern="Location History|Settings"</verify>
  <done>6 new tests added, covering history CRUD operations and platform-specific settings deep link behavior</done>
</task>

<task type="auto">
  <name>Task 4: Add battery usage and geocoding edge case tests</name>
  <files>mobile/src/__tests__/services/locationService.test.ts</files>
  <action>
    Add tests for battery usage indicator and geocoding edge cases:

    1. Test getBatteryUsage returns 'low' when not tracking:
       - Verify isTracking = false
       - Call getBatteryUsage
       - Verify returns 'low'

    2. Test getBatteryUsage returns 'high' for background tracking:
       - Set isBackgroundTracking = true
       - Call getBatteryUsage
       - Verify returns 'high'

    3. Test getBatteryUsage returns 'medium' for high accuracy:
       - Set isTracking = true, accuracy = 'high'
       - Call getBatteryUsage
       - Verify returns 'medium'

    4. Test getBatteryUsage returns 'high' for best/navigation accuracy:
       - Set isTracking = true, accuracy = 'best'
       - Call getBatteryUsage
       - Verify returns 'high'

    5. Test reverseGeocode handles missing address components:
       - Mock Location.reverseGeocodeAsync returns empty array
       - Call reverseGeocode
       - Verify returns null

    6. Test reverseGeocode handles partial address:
       - Mock geocode with only city (no street, no postalCode)
       - Call reverseGeocode
       - Verify returns 'San Francisco, CA' (available parts only)

    7. Test geocode handles invalid address:
       - Mock Location.geocodeAsync returns empty array
       - Call geocode('invalid address')
       - Verify returns null

    Battery usage logic from locationService.ts:
    - Not tracking -> 'low'
    - Background tracking -> 'high'
    - Best/Navigation accuracy -> 'high'
    - High accuracy -> 'medium'
    - Balanced/Low -> 'low'
  </action>
  <verify>npm test -- locationService.test.ts --testNamePattern="Battery|Geocoding"</verify>
  <done>7 new tests added, covering battery usage indicator logic and geocoding edge cases (missing data, partial addresses, invalid inputs)</done>
</task>

</tasks>

<verification>
Overall verification after all tasks:
1. Run location service tests: npm test -- locationService.test.ts
2. Verify 65 total tests (47 existing + 18 new)
3. Generate coverage report: npm test -- --coverage --collectCoverageFrom='mobile/src/services/locationService.ts'
4. Verify locationService.ts coverage >= 80%
5. Verify all new tests use Phase 135 utilities (flushPromises, waitForCondition, mockPlatform)
</verification>

<success_criteria>
- Location service achieves 80%+ statement coverage
- 18 new tests added (background: 5, geofence events: 5, history/settings: 6, battery/geocoding: 7)
- deviceMocks.ts extended with location mock factories
- All tests use mockPlatform for iOS/Android testing
- Test pass rate >= 95%
</success_criteria>

<output>
After completion, create `.planning/phases/136-mobile-device-features-testing/136-02-SUMMARY.md` with:
- Test counts before/after (47 -> 65)
- Coverage percentage achieved
- List of new test features (background tracking, geofence events, history, settings, battery, geocoding)
- Location mock utilities created
</output>
