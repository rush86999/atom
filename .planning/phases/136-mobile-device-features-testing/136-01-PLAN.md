---
phase: 136-mobile-device-features-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/__tests__/services/cameraService.test.ts
  - mobile/src/__tests__/helpers/testUtils.ts
autonomous: true

must_haves:
  truths:
    - "Camera service achieves 80%+ test coverage"
    - "Barcode scanning tested with various barcode types (QR, Code128, EAN)"
    - "Multiple photo capture tested with delete/clear operations"
    - "Image manipulation tested (crop, rotate, flip) with ImageManipulator mock"
    - "Platform-specific camera availability tested (iOS, Android, web)"
    - "EXIF data preservation tested in photo capture"
  artifacts:
    - path: mobile/src/__tests__/services/cameraService.test.ts
      provides: "Camera service test suite with 80%+ coverage"
      min_lines: 800
      exports: ["cameraService tests"]
    - path: mobile/src/__tests__/helpers/deviceMocks.ts
      provides: "Device-specific mock factories for camera testing"
      min_lines: 100
  key_links:
    - from: mobile/src/__tests__/services/cameraService.test.ts
      to: mobile/src/services/cameraService.ts
      via: "import { cameraService } from '../../services/cameraService'"
      pattern: "cameraService\\.(takePicture|scanBarcode|captureMultiplePhotos)"
---

<objective>
**Add comprehensive tests for camera service features to achieve 80%+ coverage**

**Purpose:** Camera service has existing tests (53 tests, ~70% coverage) but is missing coverage for barcode scanning, multiple photo capture workflows, image manipulation operations, document edge detection, and platform-specific behaviors. This plan adds ~15 new tests targeting 80%+ coverage.

**Output:** Enhanced cameraService.test.ts with ~68 total tests (53 existing + 15 new), deviceMocks.ts helper module with camera-specific mock factories.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-mobile-device-features-testing/136-RESEARCH.md

# Phase 135 Foundation
@.planning/phases/135-mobile-coverage-foundation/135-07-GAP_CLOSURE_PLAN.md

# Service Implementation
@mobile/src/services/cameraService.ts

# Existing Tests
@mobile/src/__tests__/services/cameraService.test.ts
@mobile/src/__tests__/helpers/testUtils.ts

# Test Infrastructure
@mobile/jest.setup.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add barcode scanning tests</name>
  <files>mobile/src/__tests__/services/cameraService.test.ts</files>
  <action>
    Add tests for barcode scanning functionality to cameraService.test.ts:

    1. Test successful QR code scanning with valid BarcodeScanningResult:
       - Mock BarcodeScanningResult with barcodes array containing QR code
       - Verify scanBarcode returns correct type and data
       - Verify cornerPoints are correctly mapped (4 points = corners returned)

    2. Test barcode scanning with multiple barcodes (returns first):
       - Mock result with 3 barcodes (QR, Code128, EAN)
       - Verify only first barcode is returned

    3. Test barcode scanning with no barcodes:
       - Mock empty barcodes array
       - Verify scanBarcode returns null

    4. Test barcode scanning with missing corner points:
       - Mock result with 2 corner points (not 4)
       - Verify corners property is undefined

    Pattern from existing tests:
    ```typescript
    describe('Barcode Scanning', () => {
      test('should scan QR code successfully', () => {
        const mockResult = {
          barcodes: [{ type: 'qr', rawValue: 'https://example.com', cornerPoints: [...] }],
        };
        const result = await cameraService.scanBarcode(mockResult);
        expect(result).toEqual({ type: 'qr', data: 'https://example.com', corners: {...} });
      });
    });
    ```
  </action>
  <verify>npm test -- cameraService.test.ts --testNamePattern="Barcode Scanning"</verify>
  <done>5 new barcode scanning tests added, all passing, covering QR/Code128/EAN types, empty results, and corner point variations</done>
</task>

<task type="auto">
  <name>Task 2: Add multiple photo capture and management tests</name>
  <files>mobile/src/__tests__/services/cameraService.test.ts</files>
  <action>
    Add tests for multiple photo capture workflow:

    1. Test captureMultiplePhotos captures specified count:
       - Mock takePictureAsync for 3 consecutive calls
       - Call captureMultiplePhotos with count=3
       - Verify 3 photos returned and stored in service

    2. Test getCapturedPhotos returns photo array with index:
       - After capturing multiple photos
       - Verify getCapturedPhotos returns photos array + currentIndex

    3. Test deleteCapturedPhoto removes photo at index:
       - Capture 3 photos, delete index 1
       - Verify 2 photos remain, deleted photo removed
       - Verify currentIndex adjusted if needed

    4. Test clearCapturedPhotos empties array:
       - Capture multiple photos
       - Call clearCapturedPhotos
       - Verify photos array is empty, currentIndex reset to 0

    5. Test deleteCapturedPhoto handles invalid index:
       - Try delete index 10 when only 3 photos exist
       - Verify no errors, photos unchanged

    Use existing mockCameraRef pattern from Photo Capture tests.
  </action>
  <verify>npm test -- cameraService.test.ts --testNamePattern="Multiple Photos"</verify>
  <done>5 new multiple photo tests added, covering capture, retrieval, delete, clear, and invalid index handling</done>
</task>

<task type="auto">
  <name>Task 3: Add image manipulation tests</name>
  <files>mobile/src/__tests__/services/cameraService.test.ts</files>
  <action>
    Add tests for image manipulation operations:

    1. Test rotateImage by 90 degrees:
       - Mock ImageManipulator.manipulateAsync with rotate operation
       - Verify rotateImage calls manipulateAsync correctly
       - Verify returned URI is valid

    2. Test flipImage horizontal:
       - Mock ImageManipulator.manipulateAsync with flip horizontal
       - Verify flipImage uses FlipType.Horizontal
       - Verify returned URI

    3. Test flipImage vertical:
       - Mock ImageManipulator with flip vertical
       - Verify FlipType.Vertical used

    4. Test rotateImage handles multiple rotations (180 = 2x90):
       - Mock with 180 degrees
       - Verify manipulations array has 2 rotation entries

    5. Test cropToDocument calculates correct bounds:
       - Mock ImageManipulator with crop operation
       - Verify crop origin and dimensions from corner points
       - Call cropToDocument with mock DocumentCorners

    Mock setup pattern:
    ```typescript
    jest.mock('expo-image-manipulator', () => ({
      manipulateAsync: jest.fn().mockResolvedValue({
        uri: 'file:///mock/manipulated.jpg',
        width: 1920,
        height: 1080,
      }),
    }));
    ```
  </action>
  <verify>npm test -- cameraService.test.ts --testNamePattern="Image Manipulation"</verify>
  <done>5 new image manipulation tests added, covering rotate, flip (horizontal/vertical), crop operations, and edge cases</done>
</task>

<task type="auto">
  <name>Task 4: Add platform-specific and edge case tests</name>
  <files>mobile/src/__tests__/services/cameraService.test.ts</files>
  <action>
    Add tests for platform-specific behavior and edge cases:

    1. Test isAvailable returns false on web platform:
       - Mock Platform.OS = 'web'
       - Verify isAvailable returns false (bypasses CameraView.isAvailableAsync)

    2. Test getAvailableCameraTypes for Android:
       - Mock Platform.OS = 'android'
       - Verify returns ['back', 'front']

    3. Test getAvailableCameraTypes for iOS:
       - Mock Platform.OS = 'ios'
       - Verify returns ['back', 'front']

    4. Test saveToGallery on iOS calls savePhotoToLibraryAsync:
       - Mock Platform.OS = 'ios'
       - Call saveToGallery
       - Verify CameraView.savePhotoToLibraryAsync called

    5. Test saveToGallery on Android returns true (auto-save):
       - Mock Platform.OS = 'android'
       - Call saveToGallery
       - Verify returns true, no savePhotoToLibraryAsync call

    6. Test detectDocumentEdges returns null (not implemented):
       - Call detectDocumentEdges
       - Verify returns null with logged message

    7. Test EXIF data preserved when available:
       - Mock takePictureAsync with exif property
       - Verify captured media includes exif data

    8. Test camera mode switching:
       - Test setCameraMode, getCameraMode
       - Verify mode changes between 'picture', 'video', 'document', 'barcode'

    Use mockPlatform from testUtils.ts for platform switching.
  </action>
  <verify>npm test -- cameraService.test.ts --testNamePattern="Platform|Edge Case|Camera Mode"</verify>
  <done>8 new tests added, covering platform differences, web detection, gallery save behavior, EXIF preservation, and camera modes</done>
</task>

<task type="auto">
  <name>Task 5: Create device mock utilities for camera tests</name>
  <files>mobile/src/__tests__/helpers/deviceMocks.ts</files>
  <action>
    Create new file mobile/src/__tests__/helpers/deviceMocks.ts with camera-specific mock factories:

    1. createMockCameraRef() - Factory for mock CameraView refs:
       - Returns object with current: { takePictureAsync, recordAsync, stopRecording }
       - Accepts options for configuring mock behavior (shouldSucceed, mockUri, etc.)
       - Use in tests instead of manual mock ref creation

    2. createMockBarcodeResult(type, data, corners?) - Mock BarcodeScanningResult:
       - Returns realistic barcode mock with type, data, corner points
       - Supports QR, Code128, EAN, Code39 formats

    3. createMockPhoto(options?) - Mock CapturedMedia:
       - Returns photo object with uri, type, width, height, size, exif
       - Configurable dimensions and EXIF data

    4. createMockDocumentCorners() - Mock DocumentCorners:
       - Returns 4 corner points for document edge testing
       - Realistic coordinates within 0-1 range

    Export all utilities with JSDoc documentation.
    Import from testUtils.ts where needed (flushPromises, waitForCondition).

    This reduces boilerplate in test files and provides consistent mock objects.
  </action>
  <verify>npm test -- deviceMocks.test.ts (if created) or grep for createMockCameraRef usage</verify>
  <done>deviceMocks.ts created with 4 mock factory functions, documented and exported, used in cameraService.test.ts</done>
</task>

</tasks>

<verification>
Overall verification after all tasks:
1. Run camera service tests: npm test -- cameraService.test.ts
2. Verify 68 total tests (53 existing + 15 new)
3. Generate coverage report: npm test -- --coverage --collectCoverageFrom='mobile/src/services/cameraService.ts'
4. Verify cameraService.ts coverage >= 80%
5. Verify all new tests use Phase 135 utilities (flushPromises, waitForCondition, resetAllMocks)
</verification>

<success_criteria>
- Camera service achieves 80%+ statement coverage
- 15 new tests added (barcode: 5, multiple photos: 5, manipulation: 5, platform/edge: 8)
- deviceMocks.ts helper module created with 4 mock factory functions
- All tests use Phase 135 test utilities consistently
- Test pass rate >= 95%
</success_criteria>

<output>
After completion, create `.planning/phases/136-mobile-device-features-testing/136-01-SUMMARY.md` with:
- Test counts before/after (53 -> 68)
- Coverage percentage achieved
- List of new test features (barcode, multiple photos, manipulation, platform-specific)
- Device mock utilities created
</output>
