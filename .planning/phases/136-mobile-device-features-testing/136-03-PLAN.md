---
phase: 136-mobile-device-features-testing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/__tests__/services/notificationService.test.ts
  - mobile/src/__tests__/helpers/deviceMocks.ts
autonomous: true

must_haves:
  truths:
    - "Notification service achieves 80%+ test coverage"
    - "Push token registration tested with backend API call verification"
    - "Notification received listener tested with realistic notification payload"
    - "Notification response listener tested with user interaction data"
    - "Android notification channel configuration tested (importance, vibration, light color)"
    - "Token registration error handling tested (E_NOTIFICATIONS_TOKEN_NOT_REGISTERED)"
    - "Badge count operations tested (set, get, increment, decrement, clear)"
  artifacts:
    - path: mobile/src/__tests__/services/notificationService.test.ts
      provides: "Notification service test suite with 80%+ coverage"
      min_lines: 700
      exports: ["notificationService tests"]
    - path: mobile/src/__tests__/helpers/deviceMocks.ts
      provides: "Notification-specific mock factories"
      min_lines: 120
  key_links:
    - from: mobile/src/__tests__/services/notificationService.test.ts
      to: mobile/src/services/notificationService.ts
      via: "import { notificationService } from '../../services/notificationService'"
      pattern: "notificationService\\.(registerForPushNotifications|onNotification|setBadgeCount)"
---

<objective>
**Add comprehensive tests for notification service features to achieve 80%+ coverage**

**Purpose:** Notification service has existing tests (37 tests, ~70% coverage) but is missing coverage for push token registration with backend API communication, notification received/response listener execution, Android notification channel configuration, token registration error handling, badge count operations, and scheduled notification lifecycle. This plan adds ~20 new tests targeting 80%+ coverage.

**Output:** Enhanced notificationService.test.ts with ~57 total tests (37 existing + 20 new), deviceMocks.ts extended with notification-specific mock factories.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-mobile-device-features-testing/136-RESEARCH.md

# Phase 135 Foundation
@.planning/phases/135-mobile-coverage-foundation/135-07-GAP_CLOSURE_PLAN.md

# Service Implementation
@mobile/src/services/notificationService.ts

# Existing Tests
@mobile/src/__tests__/services/notificationService.test.ts
@mobile/src/__tests__/helpers/testUtils.ts

# Test Infrastructure
@mobile/jest.setup.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add push token registration tests with backend API</name>
  <files>mobile/src/__tests__/services/notificationService.test.ts</files>
  <action>
    Add tests for push token registration with backend API integration:

    1. Test registerForPushNotifications calls backend API:
       - Mock Device.isDevice = true, permissions granted
       - Mock Notifications.getExpoPushTokenAsync returns token
       - Mock storageService.getStringAsync returns auth token
       - Mock fetch for POST /api/mobile/notifications/register
       - Call registerForPushNotifications with userId, deviceId
       - Verify fetch called with correct URL, headers, body

    2. Test registerForPushNotifications handles backend API failure:
       - Mock fetch returns 500 error
       - Call registerForPushNotifications
       - Verify token still returned (backend failure non-blocking)
       - Verify console.error logged

    3. Test registerForPushNotifications without userId skips backend:
       - Call registerForPushNotifications() without userId
       - Verify fetch NOT called
       - Verify token still returned

    4. Test registerForPushNotifications returns existing token if already registered:
       - Set pushToken on service
       - Call registerForPushNotifications
       - Verify getExpoPushTokenAsync NOT called again
       - Verify returns cached token

    5. Test registerForPushNotifications requires physical device:
       - Mock Device.isDevice = false
       - Call registerForPushNotifications
       - Verify returns null, console.warn logged

    Fetch mock pattern:
    ```typescript
    global.fetch = jest.fn().mockResolvedValue({
      ok: true,
      json: async () => ({ success: true }),
    });
    ```
  </action>
  <verify>npm test -- notificationService.test.ts --testNamePattern="Push Token Registration"</verify>
  <done>5 new push token registration tests added, covering backend API call, failure handling, caching, and device check</done>
</task>

<task type="auto">
  <name>Task 2: Add notification listener tests</name>
  <files>mobile/src/__tests__/services/notificationService.test.ts</files>
  <action>
    Add tests for notification received and response listeners:

    1. Test onNotification subscriber receives notification:
       - Subscribe callback via onNotification
       - Trigger notification by calling notifyListeners (via initialize or manual)
       - Verify callback invoked with Notification object

    2. Test onNotification unsubscribe stops notifications:
       - Subscribe, get unsubscribe function
       - Call unsubscribe
       - Trigger notification
       - Verify callback NOT invoked

    3. Test onNotificationResponse subscriber receives response:
       - Subscribe via onNotificationResponse
       - Trigger response via notifyResponseListeners
       - Verify callback invoked with response data

    4. Test onNotificationResponse handles user action:
       - Mock response with actionIdentifier, notification, userText
       - Trigger response listener
       - Verify callback receives full response object

    5. Test multiple notification listeners all receive events:
       - Subscribe 3 different callbacks
       - Trigger notification
       - Verify all 3 callbacks invoked

    6. Test notification listener errors don't affect other listeners:
       - Subscribe 2 listeners, first throws error
       - Trigger notification
       - Verify second listener still invoked, error logged

    To trigger listeners in tests, you may need to access private notifyListeners/notifyResponseListeners methods via (service as any).notifyListeners(mockNotification).
  </action>
  <verify>npm test -- notificationService.test.ts --testNamePattern="Notification Listeners"</verify>
  <done>6 new notification listener tests added, covering subscribe/unsubscribe, response handling, multiple listeners, and error isolation</done>
</task>

<task type="auto">
  <name>Task 3: Add Android channel configuration and error tests</name>
  <files>mobile/src/__tests__/services/notificationService.test.ts</files>
  <action>
    Add tests for Android notification channel and error handling:

    1. Test configureAndroidChannel on Android:
       - Mock Platform.OS = 'android'
       - Call initialize (which calls configureAndroidChannel)
       - Verify Notifications.setNotificationChannelAsync called with:
         - Channel ID: 'atom-notifications'
         - Name: 'Atom Notifications'
         - Importance: HIGH
         - Vibration pattern: [0, 250, 250, 250]
         - Light color: '#FF231F7C'

    2. Test configureAndroidChannel skipped on iOS:
       - Mock Platform.OS = 'ios'
       - Call initialize
       - Verify setNotificationChannelAsync NOT called

    3. Test registerTokenWithBackend handles auth token missing:
       - Mock storageService.getStringAsync returns null
       - Call registerForPushNotifications with userId
       - Verify fetch still called (no auth header)
       - Verify error handled gracefully

    4. Test getExpoPushTokenAsync error handled:
       - Mock Notifications.getExpoPushTokenAsync throws error with code 'E_NOTIFICATIONS_TOKEN_NOT_REGISTERED'
       - Call registerForPushNotifications
       - Verify returns null
       - Verify specific error logged

    5. Test getExpoPushTokenAsync generic error handled:
       - Mock getExpoPushTokenAsync throws unknown error
       - Call registerForPushNotifications
       - Verify returns null
       - Verify console.error logged

    6. Test permission status returns 'denied' on check error:
       - Mock Notifications.getPermissionsAsync throws error
       - Call getPermissionStatus
       - Verify returns 'denied'

    Use mockPlatform from testUtils.ts for platform switching.
  </action>
  <verify>npm test -- notificationService.test.ts --testNamePattern="Android Channel|Error Handling"</verify>
  <done>6 new tests added, covering Android channel configuration, platform differences, and error handling scenarios</done>
</task>

<task type="auto">
  <name>Task 4: Add badge count and scheduled notification lifecycle tests</name>
  <files>mobile/src/__tests__/services/notificationService.test.ts</files>
  <action>
    Add tests for badge count operations and scheduled notification lifecycle:

    1. Test setBadgeCount calls Notifications.setBadgeCountAsync:
       - Call setBadgeCount(5)
       - Verify setBadgeCountAsync called with 5

    2. Test getBadgeCount returns current badge count:
       - Mock Notifications.getBadgeCountAsync returns 3
       - Call getBadgeCount
       - Verify returns 3

    3. Test increment badge count (manual tracking):
       - Note: Service doesn't have increment, but test setBadgeCount with calculated value
       - Get current badge (3), set to 4
       - Verify setBadgeCountAsync called with 4

    4. Test clear badge count (set to 0):
       - Call setBadgeCount(0)
       - Verify setBadgeCountAsync called with 0

    5. Test scheduleNotification returns identifier:
       - Mock Notifications.scheduleNotificationAsync returns 'notif-id-123'
       - Call scheduleNotification({ title: 'Test' }, 10)
       - Verify returns 'notif-id-123'
       - Verify trigger.seconds = 10

    6. Test scheduleNotification requires permission:
       - Mock permission status = 'denied'
       - Call scheduleNotification
       - Verify throws error 'Permission not granted'

    7. Test cancelScheduledNotification calls API:
       - Call cancelNotification('notif-id-123')
       - Verify Notifications.cancelScheduledNotificationAsync called with 'notif-id-123'

    8. Test cancelAllNotifications clears all:
       - Call cancelAllNotifications
       - Verify cancelAllScheduledNotificationsAsync called
       - Verify dismissAllNotificationsAsync called

    Use setupFakeTimers and advanceTimersByTime for scheduled notification timing tests if needed.
  </action>
  <verify>npm test -- notificationService.test.ts --testNamePattern="Badge|Scheduled Notification"</verify>
  <done>8 new tests added, covering badge count operations, scheduled notification lifecycle, permission requirements, and cancellation</done>
</task>

</tasks>

<verification>
Overall verification after all tasks:
1. Run notification service tests: npm test -- notificationService.test.ts
2. Verify 57 total tests (37 existing + 20 new)
3. Generate coverage report: npm test -- --coverage --collectCoverageFrom='mobile/src/services/notificationService.ts'
4. Verify notificationService.ts coverage >= 80%
5. Verify all new tests use Phase 135 utilities (flushPromises, waitForCondition, mockPlatform)
</verification>

<success_criteria>
- Notification service achieves 80%+ statement coverage
- 20 new tests added (push token: 5, listeners: 6, Android/errors: 6, badge/scheduled: 8)
- deviceMocks.ts extended with notification mock factories
- All tests use mockPlatform for iOS/Android testing
- Test pass rate >= 95%
</success_criteria>

<output>
After completion, create `.planning/phases/136-mobile-device-features-testing/136-03-SUMMARY.md` with:
- Test counts before/after (37 -> 57)
- Coverage percentage achieved
- List of new test features (push token, listeners, Android channels, errors, badge, scheduled)
- Notification mock utilities created
</output>
