---
phase: 136-mobile-device-features-testing
plan: 07
type: execute
wave: 3
depends_on: ["136-01", "136-02", "136-03", "136-04", "136-05", "136-06"]
files_modified:
  - .github/workflows/mobile-tests.yml
  - mobile/coverage-summary.md
  - .planning/phases/136-mobile-device-features-testing/136-FINAL.md
autonomous: true

must_haves:
  truths:
    - "All 4 device services achieve 80%+ test coverage"
    - "Coverage report generated and documented"
    - "CI/CD workflow enforces device service coverage thresholds"
    - "Phase 136 summary document created with test counts and metrics"
    - "Handoff to Phase 137 prepared"
  artifacts:
    - path: mobile/coverage-summary.md
      provides: "Device feature coverage report"
      min_lines: 150
      exports: ["coverage metrics"]
    - path: .github/workflows/mobile-tests.yml
      provides: "CI/CD workflow with device coverage thresholds"
      contains: "coverage thresholds for camera, location, notification, offline sync"
    - path: .planning/phases/136-mobile-device-features-testing/136-FINAL.md
      provides: "Phase 136 summary and handoff"
      min_lines: 300
  key_links:
    - from: .github/workflows/mobile-tests.yml
      to: mobile/src/__tests__/services/*.test.ts
      via: "npm run test:coverage -- mobile/src/services"
      pattern: "coverage --coverageThreshold='\\{.*cameraService.*80.*locationService.*80.*notificationService.*80.*offlineSyncService.*80.*\\}'"
---

<objective>
**Verify device service coverage targets and create phase summary with handoff to Phase 137**

**Purpose:** Final plan to verify all 4 device services (camera, location, notification, offline sync) achieve 80%+ coverage, generate coverage report documentation, enhance CI/CD workflow with device-specific coverage thresholds, and create comprehensive phase summary with metrics and handoff to Phase 137.

**Output:** Coverage report documentation, CI/CD workflow with device thresholds, phase summary with test counts/coverage metrics, and handoff to Phase 137.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-mobile-device-features-testing/136-RESEARCH.md

# Phase 135 Foundation
@.planning/phases/135-mobile-coverage-foundation/135-07-GAP_CLOSURE_PLAN.md

# Service Files and Tests
@mobile/src/services/cameraService.ts
@mobile/src/services/locationService.ts
@mobile/src/services/notificationService.ts
@mobile/src/services/offlineSyncService.ts
@mobile/src/__tests__/services/cameraService.test.ts
@mobile/src/__tests__/services/locationService.test.ts
@mobile/src/__tests__/services/notificationService.test.ts
@mobile/src/__tests__/services/offlineSyncService.test.ts

# CI/CD Infrastructure
@.github/workflows/mobile-tests.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run coverage measurement for all device services</name>
  <files>mobile/coverage-summary.md</files>
  <action>
    Generate coverage report for all 4 device services:

    1. Run coverage for mobile services:
       ```bash
       cd mobile
       npm test -- --coverage --collectCoverageFrom='src/services/cameraService.ts'
       npm test -- --coverage --collectCoverageFrom='src/services/locationService.ts'
       npm test -- --coverage --collectCoverageFrom='src/services/notificationService.ts'
       npm test -- --coverage --collectCoverageFrom='src/services/offlineSyncService.ts'
       ```

    2. Aggregate coverage data:
       - Extract statements, branches, functions, lines coverage for each service
       - Calculate average across all 4 services
       - Identify any services below 80% threshold

    3. Create coverage-summary.md with:
       - Service-by-service breakdown table
       - Before/after comparison (baseline vs current)
       - Test counts for each service
       - Gap analysis (what's still missing if below 80%)
       - Recommendations for any remaining gaps

    4. If any service below 80%, document:
       - Specific uncovered functions/lines
       - Priority for adding tests (HIGH/MEDIUM/LOW)
       - Estimated effort to reach 80%

    Coverage summary format:
    ```markdown
    # Mobile Device Features Coverage Report

    Generated: [timestamp]

    ## Service Breakdown

    | Service | Statements | Branches | Functions | Lines | Tests |
    |----------|------------|----------|-----------|-------|-------|
    | cameraService | 82% | 78% | 85% | 81% | 68 |
    | locationService | 84% | 80% | 88% | 83% | 65 |
    | ...
    ```
  </action>
  <verify>cat mobile/coverage-summary.md | grep "Statements"</verify>
  <done>Coverage report generated, all services at 80%+ or gap analysis documented, summary table with before/after comparison</done>
</task>

<task type="auto">
  <name>Task 2: Enhance CI/CD workflow with device coverage thresholds</name>
  <files>.github/workflows/mobile-tests.yml</files>
  <action>
    Update or create .github/workflows/mobile-tests.yml with device service coverage enforcement:

    1. Add coverage thresholds step:
       ```yaml
      - name: Check device service coverage
        run: |
          npm run test:coverage -- mobile/src/services
        # Check coverage.json for per-service thresholds
      ```

    2. Add coverage threshold configuration:
       - In package.json, add script: "test:device-coverage": "jest --coverage --coverageThreshold='{\"src/services/cameraService.ts\": {\"statements\": 80, \"branches\": 75, \"functions\": 80, \"lines\": 80}, ...}'"
       - Or use separate coverage check script

    3. Add PR comment bot for coverage:
       - Comment on PR with device service coverage changes
       - Fail PR if any service below 80%
       - Show trend (+/- percentage points)

    4. Configure separate coverage job (optional but recommended):
       - Device coverage job runs after all tests pass
       - Generates coverage artifact with 30-day retention
       - Fails build if threshold not met

    5. Document threshold rationale:
       - 80% statements ensures comprehensive testing
       - 75% branches allows for some platform-specific branches
       - Services with hardware integration may have lower branch coverage due to Expo mocks

    If mobile-tests.yml exists, enhance it. If not, create new workflow file.
  </action>
  <verify>grep -c "coverageThreshold\|coverage" .github/workflows/mobile-tests.yml</verify>
  <done>CI/CD workflow enhanced with device coverage thresholds, coverage check job configured, PR comment bot added, threshold rationale documented</done>
</task>

<task type="auto">
  <name>Task 3: Create Phase 136 summary document</name>
  <files>.planning/phases/136-mobile-device-features-testing/136-FINAL.md</files>
  <action>
    Create comprehensive phase summary document 136-FINAL.md:

    1. Phase Overview:
       - Goal: Device features tested (camera, location, notifications, offline sync)
       - Status: Complete (or Partial if issues)
       - Duration: [start date] to [end date]
       - Plans completed: 7/7

    2. Test Coverage Summary:
       - Total tests added: [count from all plans]
       - Coverage achieved by service:
         - cameraService: [coverage]% ([before]% -> [after]%)
         - locationService: [coverage]% ([before]% -> [after]%)
         - notificationService: [coverage]% ([before]% -> [after]%)
         - offlineSyncService: [coverage]% ([before]% -> [after]%)
       - Overall mobile coverage: [coverage]%

    3. Test Files Created/Enhanced:
       - cameraService.test.ts: [tests] tests
       - locationService.test.ts: [tests] tests
       - notificationService.test.ts: [tests] tests
       - offlineSyncService.test.ts: [tests] tests
       - deviceMocks.ts: [factories] factories
       - devicePermissions.integration.test.ts: [tests] tests
       - offlineSyncNetwork.integration.test.ts: [tests] tests

    4. Key Features Tested:
       - Camera: barcode scanning, multiple photos, image manipulation, platform-specific
       - Location: background tracking, geofence events, history CRUD, settings deep link, battery usage
       - Notifications: push token registration, listeners, Android channels, badge counts
       - Offline Sync: network switching, periodic sync, cleanup, quota, delta hash, quality metrics

    5. Infrastructure Improvements:
       - Device mock utilities created
       - Test utilities extended
       - CI/CD coverage thresholds configured

    6. Handoff to Phase 137 (Mobile Navigation Testing):
       - What's ready: Test infrastructure stable, device services covered
       - What's next: Navigation testing (React Navigation screens, deep links, route parameters)
       - Dependencies: Phase 137 depends on Phase 136 completion

    7. Lessons Learned:
       - [Any issues encountered and resolved]
       - [Patterns that worked well]
       - [Recommendations for future phases]

    8. Open Items (if any):
       - [Services still below 80% with remediation plan]
       - [Known flaky tests]
       - [Documentation needs]
  </action>
  <verify>grep -c "##" .planning/phases/136-mobile-device-features-testing/136-FINAL.md</verify>
  <done>136-FINAL.md created with comprehensive summary including coverage metrics, test counts, feature breakdown, infrastructure improvements, handoff to Phase 137, and lessons learned</done>
</task>

</tasks>

<verification>
Overall verification after all tasks:
1. Verify coverage-summary.md exists with complete breakdown: ls mobile/coverage-summary.md
2. Verify CI/CD workflow updated: grep "coverageThreshold" .github/workflows/mobile-tests.yml
3. Verify 136-FINAL.md exists with all sections
4. Run all device service tests: npm test -- mobile/src/__tests__/services/
5. Verify 80%+ target achieved for all services or gap analysis documented
</verification>

<success_criteria>
- Coverage report generated (coverage-summary.md)
- CI/CD workflow enforces 80%+ thresholds for device services
- 136-FINAL.md created with complete summary
- All 4 device services at 80%+ or gap analysis documented
- Handoff to Phase 137 prepared
</success_criteria>

<output>
After completion, create `.planning/phases/136-mobile-device-features-testing/136-07-SUMMARY.md` with:
- Final coverage percentages for all 4 services
- CI/CD workflow changes
- Phase 136 summary location
- Next steps for Phase 137
</output>
