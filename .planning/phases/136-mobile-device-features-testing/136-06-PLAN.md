---
phase: 136-mobile-device-features-testing
plan: 06
type: execute
wave: 2
depends_on: ["136-01", "136-02", "136-03", "136-04", "136-05"]
files_modified:
  - mobile/src/__tests__/integration/devicePermissions.integration.test.ts
  - mobile/src/__tests__/integration/offlineSyncNetwork.integration.test.ts
autonomous: true

must_haves:
  truths:
    - "Camera→location→notification permission request flow tested"
    - "Permission state persistence tested across service restarts"
    - "Multiple permission requests in sequence tested"
    - "Permission denial recovery tested with open settings flow"
    - "Online→offline→online transitions tested with sync trigger"
    - "Network type switching tested (WiFi→cellular→none)"
    - "Sync retry tested with exponential backoff"
    - "Sync cancellation tested during batch processing"
  artifacts:
    - path: mobile/src/__tests__/integration/devicePermissions.integration.test.ts
      provides: "Device permission integration tests"
      min_lines: 350
      exports: ["permission flow integration tests"]
    - path: mobile/src/__tests__/integration/offlineSyncNetwork.integration.test.ts
      provides: "Offline sync network integration tests"
      min_lines: 350
      exports: ["network switching integration tests"]
  key_links:
    - from: mobile/src/__tests__/integration/devicePermissions.integration.test.ts
      to: mobile/src/services/cameraService.ts
      via: "import { cameraService } from '../../services/cameraService'"
      pattern: "cameraService\\.requestPermissions\\(\\)"
    - from: mobile/src/__tests__/integration/offlineSyncNetwork.integration.test.ts
      to: mobile/src/services/offlineSyncService.ts
      via: "import { offlineSyncService } from '../../services/offlineSyncService'"
      pattern: "offlineSyncService\\.initialize\\(\\)"
---

<objective>
**Create integration tests for device permission flows and network switching scenarios**

**Purpose:** Unit tests cover individual service methods, but integration tests verify end-to-end scenarios like multi-service permission flows and network state transitions. These tests ensure camera, location, and notification services work together for permissions, and offline sync properly handles network switching, retry logic, and cancellation.

**Output:** Two integration test files (~700 total lines) with 12 integration tests covering permission flows and network switching.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-mobile-device-features-testing/136-RESEARCH.md

# Phase 135 Foundation
@.planning/phases/135-mobile-coverage-foundation/135-07-GAP_CLOSURE_PLAN.md

# Service Implementations
@mobile/src/services/cameraService.ts
@mobile/src/services/locationService.ts
@mobile/src/services/notificationService.ts
@mobile/src/services/offlineSyncService.ts

# Service Tests
@mobile/src/__tests__/services/cameraService.test.ts
@mobile/src/__tests__/services/locationService.test.ts
@mobile/src/__tests__/services/notificationService.test.ts
@mobile/src/__tests__/services/offlineSyncService.test.ts

# Test Infrastructure
@mobile/src/__tests__/helpers/testUtils.ts
@mobile/src/__tests__/helpers/deviceMocks.ts
@mobile/jest.setup.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create device permission integration tests</name>
  <files>mobile/src/__tests__/integration/devicePermissions.integration.test.ts</files>
  <action>
    Create file mobile/src/__tests__/integration/devicePermissions.integration.test.ts with permission flow integration tests:

    1. Test camera→location→notification permission request flow:
       - Import all 3 services
       - Reset all service states before test
       - Request camera permissions (mock granted)
       - Request location permissions (mock granted)
       - Request notification permissions (mock granted)
       - Verify all 3 services have granted status
       - Verify all 3 Expo permission APIs called

    2. Test permission state persists across service imports:
       - Import cameraService, request permissions
       - Re-import cameraService (jest.resetModules)
       - Verify permission status persisted (if using storage) or cleared (singleton)
       - This test documents actual behavior

    3. Test multiple permission requests in sequence:
       - Request camera permissions 3 times consecutively
       - Verify Expo API called 3 times (no caching in service layer)
       - Verify status consistent across requests

    4. Test permission denial recovery with open settings:
       - Mock camera permission denied
       - Request permissions, verify denied
       - Mock location.openSettings (if available) or Linking.openURL
       - After settings "opened", request permissions again (mock granted)
       - Verify second request succeeds

    5. Test one permission denied doesn't block others:
       - Mock camera: granted, location: denied, notification: granted
       - Request all 3 permissions in sequence
       - Verify camera and notification succeed, location fails independently

    6. Test permission request with canAskAgain = false:
       - Mock permission status: denied, canAskAgain: false
       - Request permissions
       - Verify returns denied without calling API again

    Import pattern for integration tests:
    ```typescript
    import { cameraService } from '../../services/cameraService';
    import { locationService } from '../../services/locationService';
    import { notificationService } from '../../services/notificationService';

    beforeEach(() => {
      jest.resetModules();
      jest.clearAllMocks();
    });
    ```
  </action>
  <verify>npm test -- devicePermissions.integration.test.ts</verify>
  <done>6 permission integration tests created, covering sequential permission flows, state persistence, denial recovery, independent permissions, and canAskAgain behavior</done>
</task>

<task type="auto">
  <name>Task 2: Create offline sync network integration tests</name>
  <files>mobile/src/__tests__/integration/offlineSyncNetwork.integration.test.ts</files>
  <action>
    Create file mobile/src/__tests__/integration/offlineSyncNetwork.integration.test.ts with network switching integration tests:

    1. Test online→offline→online transition with sync trigger:
       - Initialize offlineSyncService
       - Mock NetInfo.fetch: isConnected = true initially
       - Queue 3 actions (verify queued successfully)
       - Trigger network state change: isConnected = false (using simulateNetworkSwitch)
       - Queue 2 more actions (verify queued, not synced)
       - Trigger network state: isConnected = true
       - Verify sync triggered automatically
       - Verify all 5 actions synced

    2. Test network type switching (WiFi→cellular→none):
       - Initialize service
       - Mock NetInfo state: type: 'wifi', isConnected: true
       - Queue action, verify syncs
       - Switch to cellular (type: 'cellular', isConnected: true)
       - Queue action, verify syncs (still works on cellular)
       - Switch to none (isConnected: false)
       - Queue action, verify queued (not synced)
       - Verify service state tracks each network type correctly

    3. Test sync retry with exponential backoff:
       - Queue action with syncAttempts = 1
       - Mock API to fail (500 error)
       - Trigger sync
       - Verify retry delay applied (measure delay or check setTimeout call)
       - Verify syncAttempts incremented
       - Second retry should have longer delay (2x first delay)

    4. Test sync cancellation during batch processing:
       - Queue 25 actions (3 batches of 10)
       - Start sync
       - After first batch completes, call cancelSync()
       - Verify sync stops, remaining actions not synced
       - Verify sync state: cancelled = true, syncInProgress = false
       - Verify queue still contains pending actions

    5. Test concurrent sync requests prevented:
       - Queue actions
       - Call triggerSync() twice simultaneously (Promise.all)
       - Verify only one sync executes
       - Verify first call returns { success: true }, second returns { success: false, synced: 0 }

    6. Test network switching during active sync:
       - Start sync with 20 actions
       - After 5 actions synced, trigger network offline
       - Verify sync completes current batch
       - Verify remaining actions stay pending
       - Trigger network online
       - Verify remaining actions synced

    Use simulateNetworkSwitch from deviceMocks.ts:
    ```typescript
    import { simulateNetworkSwitch } from '../helpers/deviceMocks';
    import NetInfo from '@react-native-community/netinfo';

    simulateNetworkSwitch(NetInfo, false);
    ```
  </action>
  <verify>npm test -- offlineSyncNetwork.integration.test.ts</verify>
  <done>6 network integration tests created, covering network transitions, type switching, retry backoff, cancellation, concurrent sync prevention, and mid-sync network changes</done>
</task>

</tasks>

<verification>
Overall verification after all tasks:
1. Run integration tests: npm test -- "*.integration.test.ts"
2. Verify 12 total integration tests (6 permissions + 6 network)
3. Verify no test failures or timeouts
4. Verify all integration tests use Phase 135 utilities (flushPromises, waitForCondition, resetAllMocks, simulateNetworkSwitch)
5. Generate coverage report for integration test files
</verification>

<success_criteria>
- 2 integration test files created (~700 total lines)
- 12 integration tests created (6 permission + 6 network)
- All tests use deviceMocks.ts factories (simulateNetworkSwitch, etc.)
- All tests use testUtils.ts async helpers
- Test pass rate >= 95%
- Integration tests run in <30 seconds total
</success_criteria>

<output>
After completion, create `.planning/phases/136-mobile-device-features-testing/136-06-SUMMARY.md` with:
- Test file counts and locations
- Integration test counts (6 + 6 = 12)
- Coverage of permission flow scenarios
- Coverage of network switching scenarios
- Integration with deviceMocks.ts and testUtils.ts
</output>
