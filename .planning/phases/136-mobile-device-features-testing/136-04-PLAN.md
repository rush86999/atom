---
phase: 136-mobile-device-features-testing
plan: 04
type: execute
wave: 2
depends_on: ["136-05"]
files_modified:
  - mobile/src/__tests__/services/offlineSyncService.test.ts
  - mobile/src/__tests__/services/offlineSync.test.ts
  - mobile/src/__tests__/helpers/deviceMocks.ts
autonomous: true

must_haves:
  truths:
    - "Offline sync service achieves 80%+ test coverage"
    - "Network switching listener tested with connection restored callback"
    - "Periodic sync timer tested with fake timer advancement (5 minutes)"
    - "Cleanup task tested with LRU eviction verification"
    - "Storage quota enforcement tested at 95% threshold"
    - "Storage quota cleanup tested with priority preservation (critical actions not deleted)"
    - "Delta hash generation tested for identical payload detection"
    - "Quality metrics tested (total syncs, success rate, conflict rate, duration, bytes transferred)"
    - "Sync progress callback tested with batch completion updates"
  artifacts:
    - path: mobile/src/__tests__/services/offlineSyncService.test.ts
      provides: "Offline sync service test suite with 80%+ coverage"
      min_lines: 1100
      exports: ["offlineSyncService tests"]
    - path: mobile/src/__tests__/helpers/deviceMocks.ts
      provides: "Offline sync mock factories"
      min_lines: 180
  key_links:
    - from: mobile/src/__tests__/services/offlineSyncService.test.ts
      to: mobile/src/services/offlineSyncService.ts
      via: "import { offlineSyncService } from '../../services/offlineSyncService'"
      pattern: "offlineSyncService\\.(triggerSync|queueAction|subscribeToProgress)"
---

<objective>
**Add comprehensive tests for offline sync service features to achieve 80%+ coverage**

**Purpose:** Offline sync service has existing tests (32 tests, ~65% coverage) but is missing coverage for network switching event listener, periodic sync timer execution, cleanup task with LRU eviction, storage quota enforcement, delta hash generation, quality metrics tracking, and sync progress callbacks. This plan adds ~22 new tests targeting 80%+ coverage.

**Output:** Enhanced offlineSyncService.test.ts with ~54 total tests (32 existing + 22 new), deviceMocks.ts extended with offline sync mock factories.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-mobile-device-features-testing/136-RESEARCH.md

# Phase 135 Foundation
@.planning/phases/135-mobile-coverage-foundation/135-07-GAP_CLOSURE_PLAN.md

# Service Implementation
@mobile/src/services/offlineSyncService.ts

# Existing Tests
@mobile/src/__tests__/services/offlineSyncService.test.ts
@mobile/src/__tests__/services/offlineSyncService.test.ts
@mobile/src/__tests__/helpers/testUtils.ts

# Test Infrastructure
@mobile/jest.setup.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add network switching and periodic sync tests</name>
  <files>mobile/src/__tests__/services/offlineSyncService.test.ts</files>
  <action>
    Add tests for network switching event listener and periodic sync timer:

    1. Test network switching from offline to online triggers sync:
       - Initialize service (sets up NetInfo listener)
       - Mock NetInfo.fetch returns { isConnected: false }
       - Trigger offline state
       - Queue an action
       - Trigger NetInfo callback with { isConnected: true }
       - Verify sync triggered (check syncInProgress, API called)

    2. Test periodic sync timer executes every 5 minutes:
       - Use setupFakeTimers from testUtils.ts
       - Initialize service
       - Advance timers by 5 minutes (300,000ms)
       - Verify triggerSync called
       - Advance by another 5 minutes
       - Verify triggerSync called again

    3. Test periodic sync only runs when online:
       - Set isOnline = false
       - Advance timers by 5 minutes
       - Verify triggerSync NOT called

    4. Test periodic sync doesn't trigger if sync already in progress:
       - Start sync (set syncInProgress = true)
       - Advance timers by 5 minutes
       - Verify triggerSync exits early, returns { success: false, synced: 0 }

    5. Test network state listener notifies subscribers:
       - Subscribe to sync state changes
       - Trigger network state change
       - Verify subscriber callback invoked with updated state

    6. Test cleanup task runs hourly:
       - Initialize service (starts cleanup task)
       - Advance timers by 1 hour (3,600,000ms)
       - Verify cleanupOldEntries called
       - Verify checkStorageQuota called

    NetInfo mock pattern:
    ```typescript
    const NetInfo = require('@react-native-community/netinfo');
    const mockCallback = NetInfo.addEventListener.mock.calls[0][0];
    mockCallback({ isConnected: true });
    ```
  </action>
  <verify>npm test -- offlineSyncService.test.ts --testNamePattern="Network Switching|Periodic Sync"</verify>
  <done>6 new tests added, covering network switching triggers, periodic 5-minute timer, online-only execution, in-progress blocking, state notifications, and hourly cleanup</done>
</task>

<task type="auto">
  <name>Task 2: Add storage quota and cleanup tests</name>
  <files>mobile/src/__tests__/services/offlineSyncService.test.ts</files>
  <action>
    Add tests for storage quota enforcement and LRU cleanup:

    1. Test checkStorageQuota returns true when under threshold:
       - Mock storageQuota.usedBytes = 30MB (60% of 50MB)
       - Call checkStorageQuota(10MB)
       - Verify returns true (projected 40MB < 95%)

    2. Test checkStorageQuota returns false when exceeding 95%:
       - Mock storageQuota.usedBytes = 48MB (96% of 50MB)
       - Call checkStorageQuota(2MB)
       - Verify returns false (projected 50MB >= 95%)

    3. Test checkStorageQuota logs warning at 80% threshold:
       - Mock storageQuota.usedBytes = 40MB (80% exactly)
       - Call checkStorageQuota
       - Verify returns true, warning logged

    4. Test cleanupOldEntries removes oldest low-priority actions:
       - Create 5 actions with different priorities and timestamps
       - Call cleanupOldEntries(requiredBytes: large value)
       - Verify oldest low-priority (<7) actions removed
       - Verify high-priority (>=7) actions preserved
       - Verify storage quota updated

    5. Test cleanupOldEntries preserves critical actions:
       - Create mix of critical (priority 10) and low (priority 2) actions
       - Call cleanupOldEntries
       - Verify ALL critical actions remain
       - Verify only low-priority removed

    6. Test cleanupOldEntries stops when enough space freed:
       - Create 10 actions, need space for 3
       - Call cleanupOldEntries
       - Verify only oldest 3-4 removed, stops when quota met

    Storage quota constants:
    - STORAGE_QUOTA_BYTES = 50 * 1024 * 1024 (50MB)
    - WARNING_THRESHOLD = 0.8 (80%)
    - ENFORCEMENT_THRESHOLD = 0.95 (95%)
  </action>
  <verify>npm test -- offlineSyncService.test.ts --testNamePattern="Storage Quota|Cleanup"</verify>
  <done>6 new tests added, covering quota checking (under/warning/exceeding), LRU eviction, priority preservation, and space-based stopping</done>
</task>

<task type="auto">
  <name>Task 3: Add delta hash, quality metrics, and progress callback tests</name>
  <files>mobile/src/__tests__/services/offlineSyncService.test.ts</files>
  <action>
    Add tests for delta hash generation, quality metrics, and sync progress:

    1. Test generateDeltaHash produces consistent hash for same payload:
       - Queue action with payload { data: 'test' }
       - Verify deltaHash is set
       - Queue identical payload
       - Verify deltaHash matches first

    2. Test generateDeltaHash differs for different payloads:
       - Queue action with payload { data: 'test1' }
       - Queue action with payload { data: 'test2' }
       - Verify deltaHash values are different

    3. Test updateQualityMetrics aggregates correctly:
       - Mock existing quality metrics (totalSyncs: 10, successfulSyncs: 8)
       - Trigger sync with result (synced: 5, failed: 1, duration: 1000, bytesTransferred: 5000)
       - Verify qualityMetrics updated:
         - totalSyncs = 11
         - successfulSyncs = 9
         - avgSyncDuration = weighted average
         - avgBytesTransferred = weighted average

    4. Test getQualityMetrics returns current metrics:
       - Initialize service (loads quality metrics)
       - Call getQualityMetrics
       - Verify returns object with all 7 properties

    5. Test subscribeToProgress receives progress updates:
       - Subscribe via subscribeToProgress
       - Trigger sync with 25 actions (batch size 10)
       - Verify callback invoked with progress (0, 10, 20, ..., 100)
       - Verify operation strings describe current batch

    6. Test sync progress callback includes batch completion:
       - Queue 15 actions (batches: 0-9, 10-14)
       - Subscribe to progress
       - Trigger sync
       - Verify callback receives: 0%, 66%, 100%

    7. Test sync progress callback uses human-readable operation names:
       - Trigger sync
       - Verify operation strings like "Syncing batch 1", "Syncing batch 2"

    8. Test subscribeToProgress unsubscribe stops callbacks:
       - Subscribe, get unsubscribe function
       - Call unsubscribe
       - Trigger sync
       - Verify progress callback NOT invoked

    Quality metrics properties:
    - totalSyncs, successfulSyncs, failedSyncs, conflictRate, avgSyncDuration, avgBytesTransferred, lastQualityCheck
  </action>
  <verify>npm test -- offlineSyncService.test.ts --testNamePattern="Delta Hash|Quality Metrics|Progress Callback"</verify>
  <done>8 new tests added, covering delta hash consistency/difference, quality metrics aggregation, progress callbacks with batch updates, and unsubscribe</done>
</task>

<task type="auto">
  <name>Task 4: Add conflict resolution and sync cancellation tests</name>
  <files>mobile/src/__tests__/services/offlineSyncService.test.ts</files>
  <action>
    Add tests for conflict resolution strategies and sync cancellation:

    1. Test conflict resolution with server_wins skips update:
       - Queue action with conflictResolution = 'server_wins'
       - Mock API GET returns server data with newer timestamp
       - Trigger sync
       - Verify action marked completed without update

    2. Test conflict resolution with client_wins proceeds:
       - Queue action with conflictResolution = 'client_wins'
       - Mock API GET returns server data with newer timestamp
       - Trigger sync
       - Verify API PUT called with client data

    3. Test conflict resolution with merge combines data:
       - Queue action with conflictResolution = 'merge'
       - Mock API GET returns server data
       - Trigger sync
       - Verify merged data (tags union, metadata merge)
       - Verify API PUT called with merged result

    4. Test subscribeToConflicts receives conflict notifications:
       - Subscribe via subscribeToConflicts
       - Trigger sync that produces conflict
       - Verify callback invoked with conflicting OfflineAction

    5. Test cancelSync stops in-progress sync:
       - Start sync with 20 actions
       - While processing, call cancelSync
       - Verify sync stops, cancelled flag set
       - Verify remaining actions not synced

    6. Test cancelSync when no sync in progress does nothing:
       - Ensure syncInProgress = false
       - Call cancelSync
       - Verify no errors, no state changes

    7. Test exponential backoff applied for retries:
       - Queue action (syncAttempts = 1)
       - Trigger sync
       - Verify delay applied: BASE_RETRY_DELAY * 2^1 = 2000ms minimum
       - Verify action status changed to 'syncing' then back to 'pending' on failure

    8. Test max sync attempts enforced:
       - Queue action with syncAttempts = 5 (MAX_SYNC_ATTEMPTS)
       - Trigger sync
       - Verify syncAction returns { success: false }
       - Verify action NOT retried

    Conflict resolution types: 'last_write_wins', 'manual', 'server_wins', 'client_wins', 'merge'
  </action>
  <verify>npm test -- offlineSyncService.test.ts --testNamePattern="Conflict|Cancellation|Retry"</verify>
  <done>8 new tests added, covering 4 conflict resolution strategies, conflict subscription, sync cancellation, exponential backoff, and max attempts enforcement</done>
</task>

</tasks>

<verification>
Overall verification after all tasks:
1. Run offline sync service tests: npm test -- offlineSyncService.test.ts
2. Verify 54 total tests (32 existing + 22 new)
3. Generate coverage report: npm test -- --coverage --collectCoverageFrom='mobile/src/services/offlineSyncService.ts'
4. Verify offlineSyncService.ts coverage >= 80%
5. Verify all new tests use Phase 135 utilities (flushPromises, waitForCondition, setupFakeTimers, advanceTimersByTime)
</verification>

<success_criteria>
- Offline sync service achieves 80%+ statement coverage
- 22 new tests added (network/timer: 6, quota/cleanup: 6, delta/quality/progress: 8, conflict/cancel: 8)
- deviceMocks.ts extended with offline sync mock factories
- All tests use setupFakeTimers for timer-based operations
- Test pass rate >= 95%
</success_criteria>

<output>
After completion, create `.planning/phases/136-mobile-device-features-testing/136-04-SUMMARY.md` with:
- Test counts before/after (32 -> 54)
- Coverage percentage achieved
- List of new test features (network switching, periodic sync, quota, cleanup, delta hash, quality metrics, progress, conflicts, cancellation)
- Offline sync mock utilities created
</output>
