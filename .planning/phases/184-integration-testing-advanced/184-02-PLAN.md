---
phase: 184-integration-testing-advanced
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/services/test_lancedb_vector_operations.py
autonomous: true

must_haves:
  truths:
    - "Dual vector storage (1024-dim vector + 384-dim vector_fastembed) is tested with dimension validation"
    - "Vector search operations tested with similarity search, filters, and result ranking"
    - "Embedding generation tested for all providers (local, OpenAI, FastEmbed)"
    - "Add/remove document operations tested with batch processing"
    - "Error paths tested for vector dimension mismatches, search failures, and embedding failures"
  artifacts:
    - path: "backend/tests/integration/services/test_lancedb_vector_operations.py"
      provides: "LanceDB vector operations and dual storage tests"
      min_lines: 900
      exports: ["TestDualVectorStorage", "TestVectorSearch", "TestDocumentOperations", "TestEmbeddingGeneration", "TestVectorErrorPaths"]
  key_links:
    - from: "backend/tests/integration/services/test_lancedb_vector_operations.py"
      to: "backend/core/lancedb_handler.py"
      via: "Module-level lancedb and numpy mocking"
      pattern: "patch\\('core.lancedb_handler.np.array'\\)"
---

<objective>
Create comprehensive LanceDB vector operations tests to achieve 75%+ line coverage on vector-related code (lines 400-900 in lancedb_handler.py).

Purpose: LanceDB handler supports dual vector storage (1024-dim SentenceTransformers + 384-dim FastEmbed) which needs dedicated testing. Vector search, similarity queries, and document operations are core functionality with significant uncovered lines.

Output: New test file with 900+ lines covering dual vector storage, search operations, document CRUD, and embedding generation. Tests extend proven mocking patterns from Phase 170.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-integration-testing-advanced/184-RESEARCH.md
@backend/core/lancedb_handler.py
@backend/tests/integration/services/test_lancedb_integration_coverage.py
@backend/tests/integration/services/test_lancedb_initialization.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vector operations test infrastructure</name>
  <files>backend/tests/integration/services/test_lancedb_vector_operations.py</files>
  <action>
Create new test file backend/tests/integration/services/test_lancedb_vector_operations.py with specialized fixtures:

1. Module-level mocking for lancedb and numpy (before imports):
```python
import sys
from unittest.mock import Mock, MagicMock, patch, AsyncMock
import pytest

# Mock lancedb at module level
sys.modules['lancedb'] = MagicMock()

# Mock numpy for vector operations
import numpy as np
```

2. Fixtures for vector operations:
   - mock_table_with_dual_vectors: Mock table with both vector and vector_fastembed columns
   - sample_embeddings_1024: 1024-dim vector for SentenceTransformers
   - sample_embeddings_384: 384-dim vector for FastEmbed
   - sample_search_results: Pandas DataFrame with search results including _distance column
   - handler_with_dual_storage: Handler configured for dual vector storage

3. Vector dimension fixtures:
   - embed_1024: Returns [0.1] * 1024 numpy array
   - embed_384: Returns [0.1] * 384 numpy array
   - embed_mismatched: Returns wrong dimension vector for error testing
  </action>
  <verify>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend python -m pytest tests/integration/services/test_lancedb_vector_operations.py -v --co -q
Expected: File loads without import errors, fixtures discoverable
  </verify>
  <done>Test file created with dual vector mocking infrastructure and 6+ specialized fixtures</done>
</task>

<task type="auto">
  <name>Task 2: Implement dual vector storage tests</name>
  <files>backend/tests/integration/services/test_lancedb_vector_operations.py</files>
  <action>
Add TestDualVectorStorage class (10+ tests) covering:

1. Vector column configuration:
   - test_vector_columns_configured_on_init: vector_columns dict has 1024 and 384 dims
   - test_sentence_transformers_vector_dimension: "vector" column expects 1024 dims
   - test_fastembed_vector_dimension: "vector_fastembed" column expects 384 dims
   - test_add_embedding_uses_correct_dimension: add_document() uses correct vector for provider

2. Dual storage operations:
   - test_add_document_with_both_vectors: Document added with both vector columns populated
   - test_add_document_with_sentence_transformers_only: Only "vector" column when provider is "local"
   - test_add_document_with_fastembed_only: Only "vector_fastembed" column when provider is "fastembed"
   - test_add_document_with_openai_fallback: OpenAI embedding uses "vector" column (1024-dim compatible)

3. Vector dimension validation:
   - test_dimension_mismatch_rejected: 1024-dim vector rejected for FastEmbed column
   - test_dimension_mismatch_rejected_inverse: 384-dim vector rejected for SentenceTransformers column
   - test_zero_vector_accepted: Zero-length vector handled gracefully

4. Vector retrieval:
   - test_get_embedding_returns_sentence_transformers_vector: get_embedding() returns 1024-dim vector by default
   - test_get_embedding_returns_fastembed_vector: get_embedding() returns 384-dim vector when specified
   - test_add_embedding_stores_correct_column: add_embedding() uses correct vector column

Use @patch for table.add() to verify vector column assignments
  </action>
  <verify>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend python -m pytest tests/integration/services/test_lancedb_vector_operations.py::TestDualVectorStorage -v
Expected: 10+ tests passing, covering dual vector storage logic in lancedb_handler.py
  </verify>
  <done>10+ dual vector storage tests created, covering vector column configuration, dimension validation, and storage operations</done>
</task>

<task type="auto">
  <name>Task 3: Implement vector search tests</name>
  <files>backend/tests/integration/services/test_lancedb_vector_operations.py</files>
  <action>
Add TestVectorSearch class (12+ tests) covering:

1. Basic similarity search:
   - test_similarity_search_with_query_embedding: Query text embedded before search
   - test_similarity_search_returns_ranked_results: Results sorted by _distance ascending
   - test_similarity_search_with_limit: Limit parameter restricts result count
   - test_similarity_search_score_calculation: Scores calculated as 1.0 - _distance

2. Filtered search:
   - test_search_with_user_id_filter: user_id filter applied via .where()
   - test_search_with_workspace_id_filter: workspace_id filter applied via .where()
   - test_search_with_source_filter: source filter applied via .where()
   - test_search_with_combined_filters: Multiple filters chained together

3. Search with different vector columns:
   - test_search_with_fastembed_vector: Uses vector_fastembed for fast searches
   - test_search_with_sentence_transformers_vector: Uses vector for quality searches
   - test_search_fallback_to_primary_vector: Falls back to "vector" if specified column missing

4. Search result processing:
   - test_search_returns_empty_when_table_not_found: Non-existent table returns []
   - test_search_returns_empty_when_pandas_unavailable: PANDAS_AVAILABLE=False returns []
   - test_search_converts_dataframe_to_dict_list: Pandas DataFrame converted to list of dicts
   - test_search_includes_metadata_in_results: Metadata column included in results

Use @patch for embed_text, get_table, and table operations
  </action>
  <verify>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend python -m pytest tests/integration/services/test_lancedb_vector_operations.py::TestVectorSearch -v
Expected: 12+ tests passing, covering vector search logic with filters and result processing
  </verify>
  <done>12+ vector search tests created, covering similarity search, filters, vector columns, and result processing</done>
</task>

<task type="auto">
  <name>Task 4: Implement document operations tests</name>
  <files>backend/tests/integration/services/test_lancedb_vector_operations.py</files>
  <action>
Add TestDocumentOperations class (10+ tests) covering:

1. Single document operations:
   - test_add_document_success: Document added with embedding generated
   - test_add_document_generates_embedding: embed_text() called for text field
   - test_add_document_with_metadata: Metadata dict stored correctly
   - test_add_document_handles_missing_text: Missing text field handled gracefully

2. Batch document operations:
   - test_add_documents_batch_success: Multiple documents added in single batch
   - test_add_documents_batch_generates_embeddings_per_doc: embed_text() called for each document
   - test_add_documents_batch_skips_failed_embeddings: Documents with failed embeddings skipped
   - test_add_documents_batch_returns_success_count: Returns count of successfully added documents

3. Document retrieval:
   - test_get_document_by_id: Retrieves document with specified id
   - test_get_document_returns_none_for_not_found: Non-existent id returns None
   - test_get_documents_with_ids: Retrieves multiple documents by id list

4. Document deletion:
   - test_delete_document_by_id: Deletes document from table
   - test_delete_document_handles_not_found: Non-existent id handled gracefully
   - test_delete_documents_batch: Deletes multiple documents in batch

5. Table operations:
   - test_create_table_with_schema: Table created with correct schema for vector columns
   - test_drop_table_removes_table: Table dropped successfully
   - test_table_exists_check: Returns True/False for table existence

Use @patch for table operations and verify method calls
  </action>
  <verify>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend python -m pytest tests/integration/services/test_lancedb_vector_operations.py::TestDocumentOperations -v
Expected: 10+ tests passing, covering document CRUD operations
  </verify>
  <done>10+ document operation tests created, covering single/batch add, retrieval, deletion, and table operations</done>
</task>

<task type="auto">
  <name>Task 5: Implement embedding generation and error path tests</name>
  <files>backend/tests/integration/services/test_lancedb_vector_operations.py</files>
  <action>
Add TestEmbeddingGeneration and TestVectorErrorPaths classes (15+ tests total) covering:

TestEmbeddingGeneration (7 tests):
1. Local embeddings:
   - test_embed_text_with_sentence_transformers: Returns 1024-dim vector
   - test_embed_text_normalizes_vector: Vector normalized to unit length
   - test_embed_text_handles_empty_string: Empty string returns valid vector
   - test_embed_text_handles_unicode: Unicode text handled correctly

2. FastEmbed embeddings:
   - test_embed_text_with_fastembed_provider: Returns 384-dim vector
   - test_fastembed_uses_correct_model: BAAI/bge-small-en-v1.5 model used

3. OpenAI embeddings:
   - test_embed_text_with_openai_provider: Calls OpenAI API
   - test_openai_embedding_returns_correct_dimension: Returns vector compatible with 1024-dim column

TestVectorErrorPaths (8 tests):
1. Dimension errors:
   - test_dimension_mismatch_raises_error: Wrong dimension vector rejected
   - test_embedding_generation_failure_fallback: Embedding failure falls back to zero vector
   - test_search_with_none_embedding_returns_empty: None embedding from embed_text returns []

2. Table errors:
   - test_table_not_found_returns_empty: get_table() returns None handled in search
   - test_table_creation_failure_handled: Table creation error caught and logged

3. Search errors:
   - test_search_timeout_handled: Search timeout returns empty results
   - test_search_with_malformed_results: Malformed DataFrame handled gracefully

4. Batch errors:
   - test_batch_operation_continues_on_error: Failed documents don't stop batch processing
   - test_batch_returns_partial_success_count: Returns count of successful operations even with failures
  </action>
  <verify>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend python -m pytest tests/integration/services/test_lancedb_vector_operations.py::TestEmbeddingGeneration tests/integration/services/test_lancedb_vector_operations.py::TestVectorErrorPaths -v
Expected: 15+ tests passing, covering embedding generation and error handling
  </verify>
  <done>15+ embedding and error path tests created, covering all providers and vector operation error scenarios</done>
</task>

<task type="auto">
  <name>Task 6: Measure coverage and create summary</name>
  <files>backend/tests/integration/services/test_lancedb_vector_operations.py</files>
  <action>
1. Run coverage measurement for LanceDB vector operations:
```bash
cd /Users/rushiparikh/projects/atom/backend
pytest tests/integration/services/test_lancedb_vector_operations.py --cov=core/lancedb_handler --cov-report=term-missing --cov-report=json -o cov_report_fail_under=75
```

2. Parse coverage.json to extract line coverage for vector operations code (lines 400-900)

3. Create test class TestCoverageReporting with:
   - test_vector_operations_coverage_target_met: Assert 75%+ coverage on lines 400-900
   - test_dual_storage_coverage_comprehensive: Assert 80%+ of vector storage methods covered
   - test_search_coverage_comprehensive: Assert 80%+ of search methods covered

4. Document any uncovered lines with rationale:
   - External dependency edge cases (real LanceDB errors)
   - Rare threading edge cases in model loading
   - Pandas-specific error paths requiring real DataFrames

5. Create 184-02-SUMMARY.md with:
   - Tests created (45+ tests across 5 test classes)
   - Coverage achieved (target: 75%+ on vector operations code)
   - Dual vector storage coverage details
   - Search operations coverage details
   - Uncovered lines and rationale
  </action>
  <verify>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/integration/services/test_lancedb_vector_operations.py --cov=core/lancedb_handler --cov-report=term-missing | grep "core/lancedb_handler.py"
Expected: 75%+ coverage on vector operations code (lines 400-900), overall LanceDB coverage increased from 33%
  </verify>
  <done>Coverage measured and documented, 75%+ target achieved on vector operations code, SUMMARY.md created with dual storage details</done>
</task>

</tasks>

<verification>
Run complete test suite for LanceDB vector operations:
```bash
cd /Users/rushiparikh/projects/atom/backend
pytest tests/integration/services/test_lancedb_vector_operations.py -v --tb=short --cov=core/lancedb_handler --cov-report=term-missing
```

Verify:
1. All 45+ tests passing (100% pass rate)
2. 75%+ coverage on vector operations code (lines 400-900)
3. Dual vector storage tested (both 1024-dim and 384-dim)
4. Search operations tested with filters
5. Document CRUD operations tested
6. Error paths tested for dimension mismatches and failures
7. Integration with Plan 01 initialization tests
</verification>

<success_criteria>
1. Test file created: test_lancedb_vector_operations.py with 900+ lines
2. 45+ tests created across 5 test classes
3. 100% test pass rate
4. 75%+ line coverage on LanceDB vector operations code (lines 400-900)
5. Dual vector storage fully tested (both dimensions)
6. Search operations tested with filters and result processing
7. Document CRUD operations tested (single and batch)
8. Error paths tested for embedding failures and dimension mismatches
9. SUMMARY.md documenting coverage results
</success_criteria>

<output>
After completion, create `.planning/phases/184-integration-testing-advanced/184-02-SUMMARY.md` with:
- Tests created count and classes
- Coverage achieved (line count and percentage)
- Dual vector storage coverage details
- Search operations coverage details
- Uncovered lines and rationale
- Integration with Plan 01 and Phase 170 tests
</output>
