---
phase: 184-integration-testing-advanced
plan: 03
type: execute
wave: 2
depends_on: ["184-01", "184-02"]
files_modified:
  - backend/tests/integration/services/test_lancedb_advanced.py
autonomous: true

must_haves:
  truths:
    - "Knowledge graph operations (add_edge, query_edges, graph_traversal) are tested"
    - "Batch operations tested for large-scale document insertion and vector operations"
    - "S3/R2 storage configuration tested with endpoint, credentials, and region"
    - "Advanced embedding features tested (hybrid search, reranking, multi-provider)"
    - "Error paths tested for knowledge graph failures, S3 connection errors, and batch timeouts"
  artifacts:
    - path: "backend/tests/integration/services/test_lancedb_advanced.py"
      provides: "LanceDB advanced features tests (knowledge graph, batch, S3)"
      min_lines: 700
      exports: ["TestKnowledgeGraphOperations", "TestBatchOperations", "TestS3Storage", "TestAdvancedEmbedding", "TestAdvancedErrorPaths"]
  key_links:
    - from: "backend/tests/integration/services/test_lancedb_advanced.py"
      to: "backend/core/lancedb_handler.py"
      via: "Module-level lancedb, boto3, and s3fs mocking"
      pattern: "patch\\('boto3.client'\\)"
---

<objective>
Create comprehensive LanceDB advanced features tests to achieve 75%+ line coverage on advanced functionality (lines 900-1397 in lancedb_handler.py).

Purpose: LanceDB handler has advanced features beyond basic vector operations: knowledge graph edges, batch operations, S3/R2 storage, and advanced embedding capabilities. These features represent the remaining uncovered lines in the 1397-line file.

Output: New test file with 700+ lines covering knowledge graph operations, batch processing, S3 storage configuration, and advanced embedding features. Tests use advanced mocking patterns for boto3 and s3fs.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/184-integration-testing-advanced/184-RESEARCH.md
@backend/core/lancedb_handler.py
@backend/tests/integration/services/test_lancedb_integration_coverage.py
@backend/tests/integration/services/test_lancedb_initialization.py
@backend/tests/integration/services/test_lancedb_vector_operations.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create advanced features test infrastructure</name>
  <files>backend/tests/integration/services/test_lancedb_advanced.py</files>
  <action>
Create new test file backend/tests/integration/services/test_lancedb_advanced.py with specialized mocking:

1. Module-level mocking for lancedb, boto3, and s3fs (before imports):
```python
import sys
from unittest.mock import Mock, MagicMock, patch, AsyncMock
import pytest

# Mock lancedb at module level
sys.modules['lancedb'] = MagicMock()

# Mock boto3 for S3 operations
sys.modules['boto3'] = MagicMock()
sys.modules['botocore'] = MagicMock()

# Mock s3fs for S3 filesystem operations
sys.modules['s3fs'] = MagicMock()

# Import after mocking
from core.lancedb_handler import LanceDBHandler
```

2. Fixtures for advanced features:
   - mock_knowledge_graph_table: Mock table with from_id, to_id, type, description, vector columns
   - mock_s3_client: Mock boto3 S3 client for storage operations
   - mock_s3fs: Mock s3fs filesystem for S3 path operations
   - handler_with_s3_config: Handler initialized with S3/R2 configuration
   - large_batch_documents: Fixture with 100+ documents for batch testing

3. S3 configuration fixtures:
   - mock_r2_credentials: R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, AWS_ENDPOINT_URL
   - mock_aws_credentials: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_REGION
   - s3_storage_options: Complete storage_options dict for S3 connection
  </action>
  <verify>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend python -m pytest tests/integration/services/test_lancedb_advanced.py -v --co -q
Expected: File loads without import errors, fixtures discoverable
  </verify>
  <done>Test file created with advanced mocking infrastructure for boto3, s3fs, and knowledge graph operations</done>
</task>

<task type="auto">
  <name>Task 2: Implement knowledge graph operations tests</name>
  <files>backend/tests/integration/services/test_lancedb_advanced.py</files>
  <action>
Add TestKnowledgeGraphOperations class (10+ tests) covering:

1. Edge creation:
   - test_add_knowledge_edge_success: Edge added with from_id, to_id, rel_type, description
   - test_add_edge_generates_embedding: Description text embedded for vector search
   - test_add_edge_creates_table_if_not_exists: knowledge_graph table created on first edge
   - test_add_edge_with_metadata: Metadata dict stored with edge

2. Edge retrieval:
   - test_query_knowledge_graph_by_from_id: Returns edges where from_id matches
   - test_query_knowledge_graph_by_to_id: Returns edges where to_id matches
   - test_query_knowledge_graph_by_type: Returns edges with specified relationship type
   - test_query_knowledge_graph_combined_filters: Combines from_id, to_id, and type filters

3. Graph traversal:
   - test_get_neighbors_returns_connected_entities: Returns all entities connected to given ID
   - test_get_neighbors_filters_by_relationship_type: Optional type filtering for neighbors
   - test_get_neighbors_returns_empty_for_isolated_entity: Isolated entities return empty list

4. Edge updates and deletion:
   - test_update_edge_description: Updates edge description and regenerates embedding
   - test_delete_edge_removes_from_graph: Edge removed by from_id, to_id, type tuple
   - test_delete_edge_handles_non_existent: Non-existent edge handled gracefully

5. Knowledge graph search:
   - test_search_knowledge_graph_by_description: Semantic search over edge descriptions
   - test_search_knowledge_graph_returns_edges_with_scores: Results include relevance scores

Use @patch for table operations and verify knowledge_graph schema
  </action>
  <verify>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend python -m pytest tests/integration/services/test_lancedb_advanced.py::TestKnowledgeGraphOperations -v
Expected: 10+ tests passing, covering knowledge graph CRUD and search operations
  </verify>
  <done>10+ knowledge graph tests created, covering edge creation, retrieval, traversal, updates, and search</done>
</task>

<task type="auto">
  <name>Task 3: Implement batch operations tests</name>
  <files>backend/tests/integration/services/test_lancedb_advanced.py</files>
  <action>
Add TestBatchOperations class (8+ tests) covering:

1. Large batch insertion:
   - test_add_large_batch_success: 100+ documents added in single batch
   - test_batch_performance_under_5_seconds: Large batch completes in under 5 seconds
   - test_batch_returns_accurate_count: Returns count of successfully added documents
   - test_batch_handles_partial_failures: Continues on individual document failures

2. Batch embedding generation:
   - test_batch_generates_embeddings_concurrently: Embeddings generated efficiently
   - test_batch_handles_embedding_timeout: Individual embedding timeouts don't stop batch
   - test_batch_with_mixed_providers: Handles documents with different embedding providers

3. Batch vector operations:
   - test_batch_vector_search: Search over multiple vectors in batch
   - test_batch_similarity_scores: Returns similarity scores for all query-document pairs
   - test_batch_delete: Deletes multiple documents by id list

4. Batch table operations:
   - test_batch_create_tables: Creates multiple tables efficiently
   - test_batch_drop_tables: Drops multiple tables efficiently
   - test_batch_table_exists: Checks existence of multiple tables

5. Batch error handling:
   - test_batch_operation_rollback_on_error: Rollback on critical errors
   - test_batch_continues_on_non_critical_errors: Continues on non-critical individual failures

Use mock tables with add() accepting lists of documents
  </action>
  <verify>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend python -m pytest tests/integration/services/test_lancedb_advanced.py::TestBatchOperations -v
Expected: 8+ tests passing, covering batch operations for 100+ documents
  </verify>
  <done>8+ batch operation tests created, covering large-scale insertion, embedding generation, and error handling</done>
</task>

<task type="auto">
  <name>Task 4: Implement S3/R2 storage tests</name>
  <files>backend/tests/integration/services/test_lancedb_advanced.py</files>
  <action>
Add TestS3Storage class (8+ tests) covering:

1. S3 connection configuration:
   - test_s3_endpoint_from_env: AWS_ENDPOINT_URL used in storage_options
   - test_r2_keys_from_env: R2_ACCESS_KEY_ID and R2_SECRET_ACCESS_KEY used
   - test_aws_keys_fallback: Falls back to AWS_ACCESS_KEY_ID when R2 keys missing
   - test_region_configuration: AWS_REGION or "auto" used in storage_options

2. S3 connection lifecycle:
   - test_connect_to_s3_database: lancedb.connect() called with s3:// URI and storage_options
   - test_s3_connection_persists: Connection reused across operations
   - test_s3_connection_failure_handled: Connection failures return error status

3. S3 table operations:
   - test_create_table_on_s3: Tables created in S3-backed database
   - test_table_persistence_across_sessions: Tables persist between handler instances
   - test_s3_table_operations_timeout: S3 operations timeout handled gracefully

4. R2-specific configuration:
   - test_r2_endpoint_url_format: Cloudflare R2 endpoint format supported
   - test_r2_connection_with_custom_port: Custom port in endpoint URL handled

Use @patch for lancedb.connect and boto3.client
  </action>
  <verify>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend python -m pytest tests/integration/services/test_lancedb_advanced.py::TestS3Storage -v
Expected: 8+ tests passing, covering S3/R2 configuration and connection
  </verify>
  <done>8+ S3/R2 storage tests created, covering endpoint configuration, credentials, and table operations</done>
</task>

<task type="auto">
  <name>Task 5: Implement advanced embedding and error path tests</name>
  <files>backend/tests/integration/services/test_lancedb_advanced.py</files>
  <action>
Add TestAdvancedEmbedding and TestAdvancedErrorPaths classes (12+ tests total) covering:

TestAdvancedEmbedding (6 tests):
1. Hybrid search:
   - test_hybrid_search_combines_vector_and_keyword: Combines semantic and keyword search
   - test_hybrid_search_reranking: Results reranked by combined score
   - test_hybrid_search_with_rrf: Reciprocal Rank Fusion for result combination

2. Multi-provider embeddings:
   - test_multi_provider_embedding_fallback: Falls back through providers (OpenAI -> local -> mock)
   - test_embedding_cache: Embeddings cached for repeated texts
   - test_parallel_embedding_generation: Multiple embeddings generated concurrently

3. Advanced query features:
   - test_query_with_mmr: Maximal Marginal Relevance for diverse results
   - test_query_with_reranking: Results reranked by custom scoring
   - test_query_with_filters_and_reranking: Combined filtering and reranking

TestAdvancedErrorPaths (6 tests):
1. Knowledge graph errors:
   - test_knowledge_graph_query_failure: Query failures return empty results
   - test_edge_creation_with_invalid_ids: Invalid entity IDs handled gracefully
   - test_edge_creation_with_missing_fields: Missing fields handled with defaults

2. S3 storage errors:
   - test_s3_connection_timeout: Connection timeout handled gracefully
   - test_s3_authentication_failure: Invalid credentials return error status
   - test_s3_permission_denied: Permission errors logged and handled

3. Batch errors:
   - test_batch_too_large_returns_error: Batch size limits enforced
   - test_batch_memory_limit_exceeded: Memory limits handled gracefully

4. Edge cases:
   - test_empty_knowledge_graph_operations: Empty graph operations return empty results
   - test_s3_uri_parsing_errors: Malformed S3 URIs handled gracefully
  </action>
  <verify>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend python -m pytest tests/integration/services/test_lancedb_advanced.py::TestAdvancedEmbedding tests/integration/services/test_lancedb_advanced.py::TestAdvancedErrorPaths -v
Expected: 12+ tests passing, covering advanced embedding features and error handling
  </verify>
  <done>12+ advanced embedding and error path tests created, covering hybrid search, multi-provider, and error scenarios</done>
</task>

<task type="auto">
  <name>Task 6: Measure coverage and create summary</name>
  <files>backend/tests/integration/services/test_lancedb_advanced.py</files>
  <action>
1. Run coverage measurement for LanceDB advanced features:
```bash
cd /Users/rushiparikh/projects/atom/backend
pytest tests/integration/services/test_lancedb_advanced.py --cov=core/lancedb_handler --cov-report=term-missing --cov-report=json -o cov_report_fail_under=75
```

2. Parse coverage.json to extract line coverage for advanced features code (lines 900-1397)

3. Create test class TestCoverageReporting with:
   - test_advanced_features_coverage_target_met: Assert 75%+ coverage on lines 900-1397
   - test_knowledge_graph_coverage_comprehensive: Assert 80%+ of knowledge graph methods covered
   - test_s3_storage_coverage_comprehensive: Assert 80%+ of S3 configuration covered

4. Document any uncovered lines with rationale:
   - Real S3 connection edge cases (requires actual S3/R2 endpoint)
   - Threading edge cases in concurrent embedding generation
   - Rare pandas/s3fs integration error paths

5. Create 184-03-SUMMARY.md with:
   - Tests created (35+ tests across 5 test classes)
   - Coverage achieved (target: 75%+ on advanced features code)
   - Knowledge graph coverage details
   - S3/R2 storage coverage details
   - Batch operations coverage details
   - Overall LanceDB handler coverage aggregation (Plans 01+02+03)

6. Calculate aggregate coverage across all 3 LanceDB plans:
   - Combine coverage from test_lancedb_initialization.py
   - Combine coverage from test_lancedb_vector_operations.py
   - Combine coverage from test_lancedb_advanced.py
   - Report overall LanceDB handler coverage improvement (from 33% baseline)
  </action>
  <verify>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/integration/services/test_lancedb_advanced.py tests/integration/services/test_lancedb_initialization.py tests/integration/services/test_lancedb_vector_operations.py --cov=core/lancedb_handler --cov-report=term-missing | grep "TOTAL"
Expected: 75%+ overall coverage on LanceDB handler (up from 33% baseline)
  </verify>
  <done>Coverage measured and documented, 75%+ target achieved on advanced features, aggregate LanceDB coverage summary created</done>
</task>

</tasks>

<verification>
Run complete test suite for LanceDB advanced features:
```bash
cd /Users/rushiparikh/projects/atom/backend
pytest tests/integration/services/test_lancedb_advanced.py -v --tb=short --cov=core/lancedb_handler --cov-report=term-missing
```

Verify aggregate LanceDB coverage:
```bash
pytest tests/integration/services/test_lancedb_initialization.py tests/integration/services/test_lancedb_vector_operations.py tests/integration/services/test_lancedb_advanced.py --cov=core/lancedb_handler --cov-report=term-missing
```

Verify:
1. All 35+ tests passing (100% pass rate)
2. 75%+ coverage on advanced features code (lines 900-1397)
3. Knowledge graph operations tested
4. Batch operations tested with 100+ documents
5. S3/R2 configuration tested
6. Overall LanceDB coverage at 75%+ (up from 33%)
7. Integration with Plans 01 and 02
</verification>

<success_criteria>
1. Test file created: test_lancedb_advanced.py with 700+ lines
2. 35+ tests created across 5 test classes
3. 100% test pass rate
4. 75%+ line coverage on LanceDB advanced features code (lines 900-1397)
5. Knowledge graph operations fully tested
6. Batch operations tested for 100+ documents
7. S3/R2 configuration tested
8. Overall LanceDB handler coverage at 75%+ (significant improvement from 33% baseline)
9. SUMMARY.md documenting coverage aggregation
</success_criteria>

<output>
After completion, create `.planning/phases/184-integration-testing-advanced/184-03-SUMMARY.md` with:
- Tests created count and classes
- Coverage achieved (line count and percentage)
- Knowledge graph coverage details
- S3/R2 storage coverage details
- Overall LanceDB handler coverage aggregation (Plans 01+02+03)
- Coverage improvement from 33% baseline
- Integration with Phase 170 LanceDB tests
</output>
