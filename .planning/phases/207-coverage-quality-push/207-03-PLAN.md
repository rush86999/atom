---
phase: 207-coverage-quality-push
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/api/test_onboarding_routes.py
  - tests/unit/api/test_sales_routes.py
autonomous: true

must_haves:
  truths:
    - "Onboarding endpoints handle user registration and setup"
    - "Sales routes handle quote and invoice operations"
    - "Error cases (validation errors, duplicate entries) are tested"
    - "All tests collect successfully (0 collection errors)"
    - "60%+ branch coverage achieved"
  artifacts:
    - path: "tests/unit/api/test_onboarding_routes.py"
      provides: "Onboarding API route tests"
      min_lines: 200
    - path: "tests/unit/api/test_sales_routes.py"
      provides: "Sales API route tests"
      min_lines: 200
  key_links:
    - from: "tests/unit/api/test_onboarding_routes.py"
      to: "api/onboarding_routes.py"
      via: "Onboarding flow endpoint testing"
      pattern: "client.post.*onboard|response.json"
    - from: "tests/unit/api/test_sales_routes.py"
      to: "api/sales_routes.py"
      via: "Sales operations endpoint testing"
      pattern: "client.post.*quote|client.get.*invoice"
---

## Objective

Create comprehensive tests for medium-sized API route modules (`api/onboarding_routes.py` and `api/sales_routes.py`) to achieve 85% coverage with 60%+ branch coverage.

**Purpose:** API routes are HIGH testability (55-58 lines, simple request/response). Focus on user onboarding flow and sales operations (quotes, invoices).

**Output:** ~50 tests across 2 files, 85% line coverage, 60%+ branch coverage, 0 collection errors.

## Execution Context

@/Users/rushiparikh/projects/atom/backend/.planning/phases/207-coverage-quality-push/207-RESEARCH.md
@/Users/rushiparikh/projects/atom/backend/.planning/phases/207-coverage-quality-push/207-01-PLAN.md
@/Users/rushiparikh/projects/atom/backend/.planning/phases/207-coverage-quality-push/207-02-PLAN.md
@/Users/rushiparikh/projects/atom/backend/tests/TEST_STANDARDS.md

@api/onboarding_routes.py
@api/sales_routes.py
@core/models.py

## Context

@.planning/PROJECT.md

# Phase 207 Wave 1 Progress
- Plan 01: Reports & WebSocket routes (35 lines) ✅
- Plan 02: Analytics & Time Travel routes (81 lines) ✅
- Plan 03: Onboarding & Sales routes (113 lines total) - CURRENT

# Testing Patterns
- Onboarding tests: Multi-step flow, validation, duplicate prevention
- Sales tests: Quote generation, invoice creation, status updates
- Branch coverage: Test all validation branches, error handling paths

## Tasks

### Task 1: Test Onboarding Routes

**Files:** `tests/unit/api/test_onboarding_routes.py`

**Action:**
Create comprehensive tests for `api/onboarding_routes.py` (55 lines, est. 85% coverage):

```python
"""
Unit Tests for Onboarding Routes

Tests user onboarding endpoints:
- POST /api/onboarding/start - Initialize onboarding
- POST /api/onboarding/step/{step} - Complete onboarding step
- GET /api/onboarding/status/{user_id} - Get onboarding status
- Error cases: invalid step, duplicate onboarding, validation errors

Target Coverage: 85%
Target Branch Coverage: 60%+
"""

import pytest
from fastapi.testclient import TestClient
from sqlalchemy.orm import Session
from unittest.mock import Mock, patch
from datetime import datetime

from main import app
from core.models import User, OnboardingSession, OnboardingStep
from tests.factories.user_factory import UserFactory
from tests.factories.onboarding_factory import OnboardingSessionFactory


class TestStartOnboarding:
    """Tests for POST /api/onboarding/start endpoint."""

    def test_start_onboarding_success(self, db_session: Session, client: TestClient):
        """Test POST /api/onboarding/start creates new onboarding session."""
        # Arrange: Prepare onboarding data
        onboarding_data = {
            "user_id": "test-user-123",
            "email": "test@example.com",
            "company_name": "Test Company",
            "plan": "pro"
        }

        # Act: Start onboarding
        response = client.post("/api/onboarding/start", json=onboarding_data)

        # Assert: Verify session created
        assert response.status_code == 201
        data = response.json()
        assert data["user_id"] == "test-user-123"
        assert data["email"] == "test@example.com"
        assert data["status"] == "in_progress"
        assert "session_id" in data

    def test_start_onboarding_missing_required_field(self, db_session: Session, client: TestClient):
        """Test POST /api/onboarding/start returns 400 for missing email."""
        # Arrange: Missing required field
        onboarding_data = {
            "user_id": "test-user-123",
            "company_name": "Test Company"
            # email missing
        }

        # Act: Try to start onboarding
        response = client.post("/api/onboarding/start", json=onboarding_data)

        # Assert: Verify validation error
        assert response.status_code == 422  # FastAPI validation

    def test_start_onboarding_duplicate_user(self, db_session: Session, client: TestClient):
        """Test POST /api/onboarding/start returns 400 for duplicate user onboarding."""
        # Arrange: Create existing onboarding session
        existing_session = OnboardingSessionFactory(
            user_id="test-user-123",
            email="test@example.com",
            status="in_progress"
        )
        db_session.commit()

        # Act: Try to start duplicate onboarding
        onboarding_data = {
            "user_id": "test-user-123",
            "email": "test@example.com",
            "company_name": "Test Company"
        }
        response = client.post("/api/onboarding/start", json=onboarding_data)

        # Assert: Verify duplicate error
        assert response.status_code == 400
        assert "already onboarded" in response.json()["detail"].lower()

    def test_start_onboarding_invalid_email_format(self, db_session: Session, client: TestClient):
        """Test POST /api/onboarding/start returns 400 for invalid email format."""
        # Arrange: Invalid email format
        onboarding_data = {
            "user_id": "test-user-123",
            "email": "invalid-email",
            "company_name": "Test Company"
        }

        # Act: Try to start onboarding
        response = client.post("/api/onboarding/start", json=onboarding_data)

        # Assert: Verify validation error
        assert response.status_code == 422


class TestOnboardingSteps:
    """Tests for POST /api/onboarding/step/{step} endpoint."""

    def test_complete_onboarding_step_success(self, db_session: Session, client: TestClient):
        """Test POST /api/onboarding/step/{step} completes onboarding step."""
        # Arrange: Create onboarding session
        session = OnboardingSessionFactory(
            user_id="test-user-123",
            status="in_progress"
        )
        db_session.commit()

        # Act: Complete step 1 (profile_setup)
        step_data = {
            "session_id": str(session.id),
            "step": "profile_setup",
            "data": {
                "name": "Test User",
                "timezone": "UTC"
            }
        }
        response = client.post("/api/onboarding/step/profile_setup", json=step_data)

        # Assert: Verify step completed
        assert response.status_code == 200
        data = response.json()
        assert data["step"] == "profile_setup"
        assert data["status"] == "completed"

    def test_complete_onboarding_step_invalid_step_name(self, db_session: Session, client: TestClient):
        """Test POST /api/onboarding/step/{step} returns 400 for invalid step."""
        # Arrange: Create onboarding session
        session = OnboardingSessionFactory(user_id="test-user-123")
        db_session.commit()

        # Act: Try to complete invalid step
        step_data = {
            "session_id": str(session.id),
            "step": "invalid_step",
            "data": {}
        }
        response = client.post("/api/onboarding/step/invalid_step", json=step_data)

        # Assert: Verify error
        assert response.status_code == 400
        assert "invalid step" in response.json()["detail"].lower()

    def test_complete_onboarding_step_out_of_order(self, db_session: Session, client: TestClient):
        """Test POST /api/onboarding/step/{step} returns 400 for out-of-order steps."""
        # Arrange: Create onboarding session (no steps completed)
        session = OnboardingSessionFactory(
            user_id="test-user-123",
            current_step=1
        )
        db_session.commit()

        # Act: Try to complete step 3 without completing step 2
        step_data = {
            "session_id": str(session.id),
            "step": "team_setup",  # Step 3
            "data": {}
        }
        response = client.post("/api/onboarding/step/team_setup", json=step_data)

        # Assert: Verify out-of-order error
        assert response.status_code == 400
        assert "out of order" in response.json()["detail"].lower()

    def test_complete_onboarding_step_missing_session_id(self, db_session: Session, client: TestClient):
        """Test POST /api/onboarding/step/{step} returns 400 for missing session_id."""
        # Act: Complete step without session_id
        step_data = {
            "step": "profile_setup",
            "data": {}
        }
        response = client.post("/api/onboarding/step/profile_setup", json=step_data)

        # Assert: Verify validation error
        assert response.status_code == 422

    @patch('api.onboarding_tracks.process_step_completion')
    def test_complete_onboarding_step_trigger_next_step(self, mock_process, db_session: Session, client: TestClient):
        """Test completing step automatically triggers next step."""
        # Arrange: Create onboarding session
        session = OnboardingSessionFactory(
            user_id="test-user-123",
            current_step=1
        )
        db_session.commit()

        # Act: Complete step 1
        step_data = {
            "session_id": str(session.id),
            "step": "profile_setup",
            "data": {"name": "Test User"}
        }
        response = client.post("/api/onboarding/step/profile_setup", json=step_data)

        # Assert: Next step triggered
        assert response.status_code == 200
        mock_process.assert_called_once()


class TestGetOnboardingStatus:
    """Tests for GET /api/onboarding/status/{user_id} endpoint."""

    def test_get_onboarding_status_success(self, db_session: Session, client: TestClient):
        """Test GET /api/onboarding/status/{user_id} returns onboarding progress."""
        # Arrange: Create onboarding session with completed steps
        session = OnboardingSessionFactory(
            user_id="test-user-123",
            current_step=3,
            status="in_progress"
        )
        step1 = OnboardingStep(
            session_id=session.id,
            step_name="profile_setup",
            status="completed",
            completed_at=datetime.utcnow()
        )
        step2 = OnboardingStep(
            session_id=session.id,
            step_name="workspace_setup",
            status="completed",
            completed_at=datetime.utcnow()
        )
        db_session.add_all([step1, step2])
        db_session.commit()

        # Act: Get onboarding status
        response = client.get(f"/api/onboarding/status/test-user-123")

        # Assert: Verify status
        assert response.status_code == 200
        status = response.json()
        assert status["user_id"] == "test-user-123"
        assert status["current_step"] == 3
        assert status["total_steps"] == 5
        assert status["progress_percentage"] == 60.0  # 3/5 * 100
        assert len(status["completed_steps"]) == 2

    def test_get_onboarding_status_not_found(self, db_session: Session, client: TestClient):
        """Test GET /api/onboarding/status/{user_id} returns 404 for non-existent user."""
        # Act: Get status for non-existent user
        response = client.get("/api/onboarding/status/nonexistent-user")

        # Assert: Verify 404
        assert response.status_code == 404
        assert "not found" in response.json()["detail"].lower()

    def test_get_onboarding_status_completed(self, db_session: Session, client: TestClient):
        """Test GET /api/onboarding/status/{user_id} returns 100% for completed onboarding."""
        # Arrange: Create completed onboarding session
        session = OnboardingSessionFactory(
            user_id="test-user-123",
            current_step=5,
            status="completed"
        )
        db_session.commit()

        # Act: Get status
        response = client.get(f"/api/onboarding/status/test-user-123")

        # Assert: Verify 100% progress
        assert response.status_code == 200
        status = response.json()
        assert status["status"] == "completed"
        assert status["progress_percentage"] == 100.0


class TestOnboardingCompletion:
    """Tests for onboarding completion logic."""

    def test_complete_onboarding_finalizes_session(self, db_session: Session, client: TestClient):
        """Test completing final step sets session status to completed."""
        # Arrange: Create onboarding session at final step
        session = OnboardingSessionFactory(
            user_id="test-user-123",
            current_step=5,
            status="in_progress"
        )
        db_session.commit()

        # Act: Complete final step
        step_data = {
            "session_id": str(session.id),
            "step": "review_complete",
            "data": {"confirmed": True}
        }
        response = client.post("/api/onboarding/step/review_complete", json=step_data)

        # Assert: Session finalized
        assert response.status_code == 200
        data = response.json()
        assert data["status"] == "completed"
        assert data["completed_at"] is not None

    @patch('api.onboarding_tracks.send_welcome_email')
    def test_complete_onboarding_sends_welcome_email(self, mock_email, db_session: Session, client: TestClient):
        """Test completing onboarding sends welcome email."""
        # Arrange: Create onboarding session
        session = OnboardingSessionFactory(
            user_id="test-user-123",
            email="test@example.com",
            current_step=5
        )
        db_session.commit()

        # Act: Complete final step
        step_data = {
            "session_id": str(session.id),
            "step": "review_complete",
            "data": {"confirmed": True}
        }
        client.post("/api/onboarding/step/review_complete", json=step_data)

        # Assert: Welcome email sent
        mock_email.assert_called_once_with("test@example.com")

    def test_complete_onboarding_creates_default_workspace(self, db_session: Session, client: TestClient):
        """Test completing onboarding creates default workspace."""
        # Arrange: Create onboarding session
        session = OnboardingSessionFactory(
            user_id="test-user-123",
            current_step=5
        )
        db_session.commit()

        # Act: Complete final step
        step_data = {
            "session_id": str(session.id),
            "step": "review_complete",
            "data": {"confirmed": True}
        }
        response = client.post("/api/onboarding/step/review_complete", json=step_data)

        # Assert: Workspace created
        assert response.status_code == 200
        data = response.json()
        assert "workspace_id" in data


class TestOnboardingErrorHandling:
    """Tests for onboarding error handling."""

    @patch('api.onboarding_tracks.save_step_data')
    def test_database_error_on_step_completion(self, mock_save, db_session: Session, client: TestClient):
        """Test database errors during step completion are handled."""
        # Arrange: Mock database error
        mock_save.side_effect = Exception("Database connection failed")

        session = OnboardingSessionFactory(user_id="test-user-123")
        db_session.commit()

        # Act: Try to complete step
        step_data = {
            "session_id": str(session.id),
            "step": "profile_setup",
            "data": {}
        }
        response = client.post("/api/onboarding/step/profile_setup", json=step_data)

        # Assert: Error handled gracefully
        assert response.status_code == 500
        assert "database" in response.json()["detail"].lower()

    def test_invalid_step_data_schema(self, db_session: Session, client: TestClient):
        """Test invalid step data returns 400."""
        # Arrange: Create session
        session = OnboardingSessionFactory(user_id="test-user-123")
        db_session.commit()

        # Act: Send invalid data for step
        step_data = {
            "session_id": str(session.id),
            "step": "profile_setup",
            "data": {"invalid_field": "value"}  # Missing required fields
        }
        response = client.post("/api/onboarding/step/profile_setup", json=step_data)

        # Assert: Validation error
        assert response.status_code == 422
```

**Verify:**
```bash
# Verify collection
pytest tests/unit/api/test_onboarding_routes.py --collect-only -q

# Run tests with coverage
pytest tests/unit/api/test_onboarding_routes.py \
  --cov=api.onboarding_routes \
  --cov-branch \
  --cov-report=term-missing \
  -v

# Verify metrics
# - Line coverage: 85%+ (target)
# - Branch coverage: 60%+ (target)
# - Tests: ~25 tests
# - Collection errors: 0
```

**Done:**
- `tests/unit/api/test_onboarding_routes.py` created with ~25 tests
- 85%+ line coverage achieved
- 60%+ branch coverage achieved
- 0 collection errors
- All tests passing

### Task 2: Test Sales Routes

**Files:** `tests/unit/api/test_sales_routes.py`

**Action:**
Create comprehensive tests for `api/sales_routes.py` (58 lines, est. 85% coverage):

```python
"""
Unit Tests for Sales Routes

Tests sales operations endpoints:
- POST /api/sales/quotes - Create sales quote
- GET /api/sales/quotes/{id} - Get quote details
- POST /api/sales/quotes/{id}/accept - Accept quote (create invoice)
- GET /api/sales/invoices/{id} - Get invoice details
- Error cases: invalid quote ID, expired quote, payment failures

Target Coverage: 85%
Target Branch Coverage: 60%+
"""

import pytest
from fastapi.testclient import TestClient
from sqlalchemy.orm import Session
from unittest.mock import Mock, patch
from datetime import datetime, timedelta

from main import app
from core.models import Quote, Invoice, QuoteItem, Customer
from tests.factories.sales_factory import QuoteFactory, InvoiceFactory


class TestCreateQuote:
    """Tests for POST /api/sales/quotes endpoint."""

    def test_create_quote_success(self, db_session: Session, client: TestClient):
        """Test POST /api/sales/quotes creates new quote."""
        # Arrange: Prepare quote data
        quote_data = {
            "customer_id": "cust-123",
            "items": [
                {"product_id": "prod-1", "quantity": 2, "unit_price": 100.00},
                {"product_id": "prod-2", "quantity": 1, "unit_price": 50.00}
            ],
            "valid_until": (datetime.utcnow() + timedelta(days=30)).isoformat(),
            "notes": "Test quote"
        }

        # Act: Create quote
        response = client.post("/api/sales/quotes", json=quote_data)

        # Assert: Verify quote created
        assert response.status_code == 201
        data = response.json()
        assert data["customer_id"] == "cust-123"
        assert len(data["items"]) == 2
        assert data["total_amount"] == 250.00  # 2*100 + 1*50
        assert data["status"] == "draft"

    def test_create_quote_missing_customer_id(self, db_session: Session, client: TestClient):
        """Test POST /api/sales/quotes returns 400 for missing customer_id."""
        # Arrange: Missing required field
        quote_data = {
            "items": [],
            "valid_until": datetime.utcnow().isoformat()
        }

        # Act: Try to create quote
        response = client.post("/api/sales/quotes", json=quote_data)

        # Assert: Verify validation error
        assert response.status_code == 422

    def test_create_quote_empty_items(self, db_session: Session, client: TestClient):
        """Test POST /api/sales/quotes returns 400 for empty items list."""
        # Arrange: Empty items list
        quote_data = {
            "customer_id": "cust-123",
            "items": [],
            "valid_until": datetime.utcnow().isoformat()
        }

        # Act: Try to create quote
        response = client.post("/api/sales/quotes", json=quote_data)

        # Assert: Verify validation error
        assert response.status_code == 400
        assert "at least one item" in response.json()["detail"].lower()

    def test_create_quote_expired_validity_date(self, db_session: Session, client: TestClient):
        """Test POST /api/sales/quotes returns 400 for expired validity date."""
        # Arrange: Past validity date
        quote_data = {
            "customer_id": "cust-123",
            "items": [{"product_id": "prod-1", "quantity": 1, "unit_price": 100}],
            "valid_until": (datetime.utcnow() - timedelta(days=1)).isoformat()
        }

        # Act: Try to create quote
        response = client.post("/api/sales/quotes", json=quote_data)

        # Assert: Verify validation error
        assert response.status_code == 400
        assert "expired" in response.json()["detail"].lower()

    def test_create_quote_calculates_totals(self, db_session: Session, client: TestClient):
        """Test POST /api/sales/quotes correctly calculates totals."""
        # Arrange: Quote with multiple items
        quote_data = {
            "customer_id": "cust-123",
            "items": [
                {"product_id": "prod-1", "quantity": 3, "unit_price": 100.00},
                {"product_id": "prod-2", "quantity": 2, "unit_price": 50.00},
                {"product_id": "prod-3", "quantity": 1, "unit_price": 25.00}
            ],
            "tax_rate": 0.10,
            "valid_until": datetime.utcnow().isoformat()
        }

        # Act: Create quote
        response = client.post("/api/sales/quotes", json=quote_data)

        # Assert: Verify calculations
        assert response.status_code == 201
        data = response.json()
        assert data["subtotal"] == 425.00  # 3*100 + 2*50 + 1*25
        assert data["tax_amount"] == 42.50  # 425 * 0.10
        assert data["total_amount"] == 467.50  # 425 + 42.50


class TestGetQuote:
    """Tests for GET /api/sales/quotes/{id} endpoint."""

    def test_get_quote_success(self, db_session: Session, client: TestClient):
        """Test GET /api/sales/quotes/{id} returns quote details."""
        # Arrange: Create quote
        quote = QuoteFactory(
            customer_id="cust-123",
            status="draft",
            total_amount=250.00
        )
        db_session.commit()

        # Act: Get quote
        response = client.get(f"/api/sales/quotes/{quote.id}")

        # Assert: Verify quote returned
        assert response.status_code == 200
        data = response.json()
        assert data["id"] == quote.id
        assert data["customer_id"] == "cust-123"
        assert data["total_amount"] == 250.00

    def test_get_quote_not_found(self, db_session: Session, client: TestClient):
        """Test GET /api/sales/quotes/{id} returns 404 for non-existent quote."""
        # Act: Get non-existent quote
        response = client.get("/api/sales/quotes/nonexistent-id")

        # Assert: Verify 404
        assert response.status_code == 404
        assert "not found" in response.json()["detail"].lower()


class TestAcceptQuote:
    """Tests for POST /api/sales/quotes/{id}/accept endpoint."""

    def test_accept_quote_success(self, db_session: Session, client: TestClient):
        """Test POST /api/sales/quotes/{id}/accept creates invoice and updates quote status."""
        # Arrange: Create draft quote
        quote = QuoteFactory(
            customer_id="cust-123",
            status="draft",
            total_amount=250.00,
            valid_until=datetime.utcnow() + timedelta(days=30)
        )
        db_session.commit()

        # Act: Accept quote
        response = client.post(f"/api/sales/quotes/{quote.id}/accept")

        # Assert: Quote accepted and invoice created
        assert response.status_code == 200
        data = response.json()
        assert data["quote_id"] == str(quote.id)
        assert data["quote_status"] == "accepted"
        assert "invoice_id" in data

    def test_accept_quote_already_accepted(self, db_session: Session, client: TestClient):
        """Test POST /api/sales/quotes/{id}/accept returns 400 for already accepted quote."""
        # Arrange: Create already accepted quote
        quote = QuoteFactory(
            customer_id="cust-123",
            status="accepted",
            total_amount=250.00
        )
        db_session.commit()

        # Act: Try to accept already accepted quote
        response = client.post(f"/api/sales/quotes/{quote.id}/accept")

        # Assert: Verify error
        assert response.status_code == 400
        assert "already accepted" in response.json()["detail"].lower()

    def test_accept_quote_expired(self, db_session: Session, client: TestClient):
        """Test POST /api/sales/quotes/{id}/accept returns 400 for expired quote."""
        # Arrange: Create expired quote
        quote = QuoteFactory(
            customer_id="cust-123",
            status="draft",
            total_amount=250.00,
            valid_until=datetime.utcnow() - timedelta(days=1)
        )
        db_session.commit()

        # Act: Try to accept expired quote
        response = client.post(f"/api/sales/quotes/{quote.id}/accept")

        # Assert: Verify expired error
        assert response.status_code == 400
        assert "expired" in response.json()["detail"].lower()

    @patch('api.sales_routes.create_invoice_from_quote')
    def test_accept_quote_invoice_creation_failure(self, mock_create, db_session: Session, client: TestClient):
        """Test POST /api/sales/quotes/{id}/accept handles invoice creation failure."""
        # Arrange: Mock invoice creation failure
        mock_create.side_effect = Exception("Invoice service unavailable")

        quote = QuoteFactory(
            customer_id="cust-123",
            status="draft",
            total_amount=250.00
        )
        db_session.commit()

        # Act: Try to accept quote
        response = client.post(f"/api/sales/quotes/{quote.id}/accept")

        # Assert: Error handled gracefully
        assert response.status_code == 500
        assert "invoice" in response.json()["detail"].lower()


class TestGetInvoice:
    """Tests for GET /api/sales/invoices/{id} endpoint."""

    def test_get_invoice_success(self, db_session: Session, client: TestClient):
        """Test GET /api/sales/invoices/{id} returns invoice details."""
        # Arrange: Create invoice
        invoice = InvoiceFactory(
            customer_id="cust-123",
            total_amount=467.50,
            status="unpaid"
        )
        db_session.commit()

        # Act: Get invoice
        response = client.get(f"/api/sales/invoices/{invoice.id}")

        # Assert: Verify invoice returned
        assert response.status_code == 200
        data = response.json()
        assert data["id"] == invoice.id
        assert data["customer_id"] == "cust-123"
        assert data["total_amount"] == 467.50
        assert data["status"] == "unpaid"

    def test_get_invoice_not_found(self, db_session: Session, client: TestClient):
        """Test GET /api/sales/invoices/{id} returns 404 for non-existent invoice."""
        # Act: Get non-existent invoice
        response = client.get("/api/sales/invoices/nonexistent-id")

        # Assert: Verify 404
        assert response.status_code == 404
        assert "not found" in response.json()["detail"].lower()

    def test_get_invoice_includes_line_items(self, db_session: Session, client: TestClient):
        """Test GET /api/sales/invoices/{id} includes line items."""
        # Arrange: Create invoice with items
        invoice = InvoiceFactory(
            customer_id="cust-123",
            total_amount=250.00
        )
        item1 = QuoteItem(
            invoice_id=invoice.id,
            product_id="prod-1",
            quantity=2,
            unit_price=100.00
        )
        item2 = QuoteItem(
            invoice_id=invoice.id,
            product_id="prod-2",
            quantity=1,
            unit_price=50.00
        )
        db_session.add_all([item1, item2])
        db_session.commit()

        # Act: Get invoice
        response = client.get(f"/api/sales/invoices/{invoice.id}")

        # Assert: Verify line items included
        assert response.status_code == 200
        data = response.json()
        assert len(data["line_items"]) == 2
        assert data["line_items"][0]["product_id"] == "prod-1"


class TestInvoicePayment:
    """Tests for invoice payment operations."""

    def test_record_payment_success(self, db_session: Session, client: TestClient):
        """Test POST /api/sales/invoices/{id}/payment records payment."""
        # Arrange: Create unpaid invoice
        invoice = InvoiceFactory(
            customer_id="cust-123",
            total_amount=467.50,
            status="unpaid",
            amount_paid=0.00
        )
        db_session.commit()

        # Act: Record payment
        payment_data = {
            "amount": 467.50,
            "payment_method": "credit_card",
            "transaction_id": "txn-12345"
        }
        response = client.post(f"/api/sales/invoices/{invoice.id}/payment", json=payment_data)

        # Assert: Payment recorded
        assert response.status_code == 200
        data = response.json()
        assert data["status"] == "paid"
        assert data["amount_paid"] == 467.50
        assert data["paid_at"] is not None

    def test_record_partial_payment(self, db_session: Session, client: TestClient):
        """Test POST /api/sales/invoices/{id}/payment records partial payment."""
        # Arrange: Create unpaid invoice
        invoice = InvoiceFactory(
            customer_id="cust-123",
            total_amount=467.50,
            status="unpaid",
            amount_paid=0.00
        )
        db_session.commit()

        # Act: Record partial payment
        payment_data = {
            "amount": 200.00,
            "payment_method": "credit_card",
            "transaction_id": "txn-12345"
        }
        response = client.post(f"/api/sales/invoices/{invoice.id}/payment", json=payment_data)

        # Assert: Partial payment recorded
        assert response.status_code == 200
        data = response.json()
        assert data["status"] == "partial"
        assert data["amount_paid"] == 200.00
        assert data["balance_due"] == 267.50

    def test_record_payment_overpayment(self, db_session: Session, client: TestClient):
        """Test POST /api/sales/invoices/{id}/payment returns 400 for overpayment."""
        # Arrange: Create invoice
        invoice = InvoiceFactory(
            customer_id="cust-123",
            total_amount=467.50,
            status="unpaid"
        )
        db_session.commit()

        # Act: Try to record overpayment
        payment_data = {
            "amount": 500.00,  # More than total
            "payment_method": "credit_card",
            "transaction_id": "txn-12345"
        }
        response = client.post(f"/api/sales/invoices/{invoice.id}/payment", json=payment_data)

        # Assert: Verify error
        assert response.status_code == 400
        assert "overpayment" in response.json()["detail"].lower()

    def test_record_payment_already_paid(self, db_session: Session, client: TestClient):
        """Test POST /api/sales/invoices/{id}/payment returns 400 for already paid invoice."""
        # Arrange: Create paid invoice
        invoice = InvoiceFactory(
            customer_id="cust-123",
            total_amount=467.50,
            status="paid",
            amount_paid=467.50
        )
        db_session.commit()

        # Act: Try to record payment
        payment_data = {
            "amount": 100.00,
            "payment_method": "credit_card",
            "transaction_id": "txn-12345"
        }
        response = client.post(f"/api/sales/invoices/{invoice.id}/payment", json=payment_data)

        # Assert: Verify error
        assert response.status_code == 400
        assert "already paid" in response.json()["detail"].lower()


class TestSalesReports:
    """Tests for sales reporting endpoints."""

    def test_get_sales_summary(self, db_session: Session, client: TestClient):
        """Test GET /api/sales/reports/summary returns sales summary."""
        # Arrange: Create quotes and invoices
        quote1 = QuoteFactory(customer_id="cust-1", total_amount=100.00, status="draft")
        quote2 = QuoteFactory(customer_id="cust-2", total_amount=200.00, status="accepted")
        invoice1 = InvoiceFactory(customer_id="cust-1", total_amount=100.00, status="paid")
        db_session.commit()

        # Act: Get sales summary
        response = client.get("/api/sales/reports/summary")

        # Assert: Verify summary
        assert response.status_code == 200
        summary = response.json()
        assert summary["total_quotes"] == 2
        assert summary["accepted_quotes"] == 1
        assert summary["total_revenue"] == 100.00

    def test_get_sales_summary_with_date_filter(self, db_session: Session, client: TestClient):
        """Test GET /api/sales/reports/summary filters by date range."""
        # Arrange: Create quotes at different times
        old_quote = QuoteFactory(
            customer_id="cust-1",
            total_amount=100.00,
            created_at=datetime.utcnow() - timedelta(days=10)
        )
        recent_quote = QuoteFactory(
            customer_id="cust-2",
            total_amount=200.00,
            created_at=datetime.utcnow()
        )
        db_session.commit()

        # Act: Get summary with date filter
        start_date = (datetime.utcnow() - timedelta(days=1)).isoformat()
        response = client.get(f"/api/sales/reports/summary?start_date={start_date}")

        # Assert: Only recent quotes included
        assert response.status_code == 200
        summary = response.json()
        assert summary["total_quotes"] == 1
        assert summary["total_value"] == 200.00
```

**Verify:**
```bash
# Verify collection
pytest tests/unit/api/test_sales_routes.py --collect-only -q

# Run tests with coverage
pytest tests/unit/api/test_sales_routes.py \
  --cov=api.sales_routes \
  --cov-branch \
  --cov-report=term-missing \
  -v

# Verify metrics
# - Line coverage: 85%+ (target)
# - Branch coverage: 60%+ (target)
# - Tests: ~25 tests
# - Collection errors: 0
```

**Done:**
- `tests/unit/api/test_sales_routes.py` created with ~25 tests
- 85%+ line coverage achieved
- 60%+ branch coverage achieved
- 0 collection errors
- All tests passing

### Task 3: Verify Collection and Coverage

**Files:** None (verification task)

**Action:**
Verify all tests from Tasks 1-2 collect successfully and meet coverage targets:

```bash
# Step 1: Verify no collection errors
pytest tests/unit/api/test_onboarding_routes.py tests/unit/api/test_sales_routes.py \
  --collect-only -q

# Step 2: Run all tests with branch coverage
pytest tests/unit/api/test_onboarding_routes.py tests/unit/api/test_sales_routes.py \
  --cov=api.onboarding_routes \
  --cov=api.sales_routes \
  --cov-branch \
  --cov-report=term-missing \
  --cov-report=html \
  -v

# Step 3: Verify metrics
echo "=== Coverage Report ==="
coverage report | tail -1

# Step 4: Verify branch coverage specifically
coverage report --show-missing | grep "branch"

# Step 5: Check for collection errors
pytest tests/unit/api/test_onboarding_routes.py tests/unit/api/test_sales_routes.py \
  --collect-only 2>&1 | grep -i error || echo "No collection errors ✓"

# Step 6: Wave 1 summary
echo "=== Wave 1 Summary ==="
echo "Plans completed: 207-01, 207-02, 207-03"
echo "Total modules tested: 6 API routes"
echo "Estimated tests created: ~120"
```

**Verify:**
- [ ] 0 collection errors
- [ ] 85% line coverage for both files
- [ ] 60%+ branch coverage for both files
- [ ] ~50 total tests
- [ ] 95%+ pass rate

**Done:**
- All tests collect successfully
- Coverage targets met
- Branch coverage targets met
- Wave 1 complete (3 plans, 6 API routes)
- Ready for Wave 2 execution

## Verification

### Overall Plan Checks

1. **Test Coverage**
   - [ ] `api/onboarding_routes.py`: 85%+ coverage
   - [ ] `api/sales_routes.py`: 85%+ coverage
   - [ ] Both files: 60%+ branch coverage

2. **Test Quality**
   - [ ] Onboarding flow tested (start, steps, completion)
   - [ ] Sales operations tested (quotes, invoices, payments)
   - [ ] Error cases tested (validation, expired, duplicates)
   - [ ] Multi-step flows tested

3. **Collection Stability**
   - [ ] 0 collection errors
   - [ ] All imports resolve correctly
   - [ ] Fixtures loaded successfully

4. **Wave 1 Completion**
   - [ ] 3 plans completed (207-01, 207-02, 207-03)
   - [ ] 6 API route modules tested
   - [ ] ~120 total tests created
   - [ ] 85-95% coverage achieved

## Success Criteria

Plan is **SUCCESSFUL** when:
- `tests/unit/api/test_onboarding_routes.py` created with ~25 tests, 85%+ coverage
- `tests/unit/api/test_sales_routes.py` created with ~25 tests, 85%+ coverage
- Both files achieve 60%+ branch coverage
- 0 collection errors
- 95%+ tests passing
- Wave 1 complete (6 API routes, ~120 tests)

## Output

After completion, create `.planning/phases/207-coverage-quality-push/207-03-SUMMARY.md`

**Wave 1 Summary:** 6 API route modules tested (reports, websocket, analytics, time-travel, onboarding, sales) with ~120 tests achieving 85-95% coverage.

---

*Plan created: March 18, 2026*
*Estimated effort: 3-4 hours*
*Dependencies: None (Wave 1, Plan 03 - parallel with 207-01, 207-02)*
