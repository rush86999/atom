---
phase: 207-coverage-quality-push
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - tests/unit/core/test_lux_config.py
  - tests/unit/core/test_messaging_schemas.py
autonomous: true

must_haves:
  truths:
    - "Lux config service manages application configuration"
    - "Messaging schemas define message structures"
    - "Configuration validation and retrieval tested"
    - "All tests collect successfully (0 collection errors)"
    - "60%+ branch coverage achieved"
  artifacts:
    - path: "tests/unit/core/test_lux_config.py"
      provides: "Lux config service tests"
      min_lines: 150
    - path: "tests/unit/core/test_messaging_schemas.py"
      provides: "Messaging schema tests"
      min_lines: 200
  key_links:
    - from: "tests/unit/core/test_lux_config.py"
      to: "core/lux_config.py"
      via: "Configuration management testing"
      pattern: "LuxConfig|get_value|set_value"
    - from: "tests/unit/core/test_messaging_schemas.py"
      to: "core/messaging_schemas.py"
      via: "Message schema validation testing"
      pattern: "validate|MessageSchema|ValidationError"
---

## Objective

Create comprehensive tests for small core service modules (`core/lux_config.py` and `core/messaging_schemas.py`) to achieve 90-95% coverage with 60%+ branch coverage.

**Purpose:** Core services are GOOD testability (33-41 lines, business logic, minimal external deps). Focus on configuration management and message schema validation.

**Output:** ~45 tests across 2 files, 90-95% line coverage, 60%+ branch coverage, 0 collection errors.

## Execution Context

@/Users/rushiparikh/projects/atom/backend/.planning/phases/207-coverage-quality-push/207-RESEARCH.md
@/Users/rushiparikh/projects/atom/backend/.planning/phases/207-coverage-quality-push/207-01-PLAN.md
@/Users/rushiparikh/projects/atom/backend/tests/TEST_STANDARDS.md

@core/lux_config.py
@core/messaging_schemas.py
@core/models.py

## Context

@.planning/PROJECT.md

# Phase 207 Wave 1 Complete
- Plans 01-03: API Routes (6 modules, ~120 tests) ✅
- Wave 1 Summary: 85-95% coverage achieved

# Wave 2: Core Services
- Plan 04: Lux Config & Messaging Schemas (74 lines total)
- Plan 05: Billing & LLM Service (94 lines total)
- Plan 06: Historical Learner & External Integration (111 lines total)

# Testing Patterns
- Config tests: Get/set values, type validation, cache behavior
- Schema tests: Validation, serialization, error handling
- Branch coverage: Test all validation branches, type checks

## Tasks

### Task 1: Test Lux Config Service

**Files:** `tests/unit/core/test_lux_config.py`

**Action:**
Create comprehensive tests for `core/lux_config.py` (33 lines, est. 90% coverage):

```python
"""
Unit Tests for Lux Config Service

Tests application configuration management:
- get_value(key) - Retrieve configuration value
- set_value(key, value) - Set configuration value
- get_all() - Get all configuration
- validate_config(key, value) - Validate configuration
- Cache behavior and performance

Target Coverage: 90%
Target Branch Coverage: 60%+
"""

import pytest
from sqlalchemy.orm import Session
from unittest.mock import Mock, patch
from datetime import datetime, timedelta

from core.lux_config import LuxConfigService, LuxConfig, ConfigValidationError
from core.models import BaseModel
from tests.factories.lux_config_factory import LuxConfigFactory


class TestGetConfigValue:
    """Tests for get_value method."""

    def test_get_value_success(self, db_session: Session):
        """Test get_value returns configuration value."""
        # Arrange: Create config entry
        config = LuxConfigFactory(
            key="test.feature.enabled",
            value="true",
            value_type="boolean"
        )
        db_session.commit()

        # Act: Get value
        service = LuxConfigService(db_session)
        result = service.get_value("test.feature.enabled")

        # Assert: Verify value
        assert result == "true"

    def test_get_value_not_found(self, db_session: Session):
        """Test get_value returns None for non-existent key."""
        # Arrange: Service with empty database
        service = LuxConfigService(db_session)

        # Act: Get non-existent value
        result = service.get_value("nonexistent.key")

        # Assert: Verify None
        assert result is None

    def test_get_value_with_default(self, db_session: Session):
        """Test get_value returns default value when key not found."""
        # Arrange: Service with empty database
        service = LuxConfigService(db_session)

        # Act: Get value with default
        result = service.get_value("nonexistent.key", default="default_value")

        # Assert: Verify default returned
        assert result == "default_value"

    def test_get_value_uses_cache(self, db_session: Session):
        """Test get_value uses cache for repeated lookups."""
        # Arrange: Create config and warm cache
        config = LuxConfigFactory(key="cached.key", value="cached_value")
        db_session.commit()

        service = LuxConfigService(db_session)

        # First call (cache miss, then cached)
        service.get_value("cached.key")

        # Act: Second call (should hit cache)
        with patch.object(service, '_query_db') as mock_query:
            result = service.get_value("cached.key")

            # Assert: Database NOT queried (cache hit)
            mock_query.assert_not_called()
            assert result == "cached_value"

    def test_get_value_cache_expiration(self, db_session: Session):
        """Test get_value refreshes cache after expiration."""
        # Arrange: Create config with short TTL
        config = LuxConfigFactory(key="expiring.key", value="initial_value")
        db_session.commit()

        service = LuxConfigService(cache_ttl=1)  # 1 second TTL

        # First call (warms cache)
        service.get_value("expiring.key")

        # Update database value
        config.value = "updated_value"
        db_session.commit()

        # Wait for cache expiration
        import time
        time.sleep(1.1)

        # Act: Get value after cache expiration
        result = service.get_value("expiring.key")

        # Assert: Updated value returned (cache refreshed)
        assert result == "updated_value"


class TestSetConfigValue:
    """Tests for set_value method."""

    def test_set_value_new_key(self, db_session: Session):
        """Test set_value creates new configuration entry."""
        # Arrange: Service
        service = LuxConfigService(db_session)

        # Act: Set new value
        service.set_value("new.feature.enabled", "true")

        # Assert: Verify entry created
        config = db_session.query(LuxConfig).filter(
            LuxConfig.key == "new.feature.enabled"
        ).first()
        assert config is not None
        assert config.value == "true"

    def test_set_value_existing_key(self, db_session: Session):
        """Test set_value updates existing configuration entry."""
        # Arrange: Create existing config
        config = LuxConfigFactory(
            key="existing.key",
            value="old_value"
        )
        db_session.commit()

        service = LuxConfigService(db_session)

        # Act: Update value
        service.set_value("existing.key", "new_value")

        # Assert: Verify entry updated
        db_session.refresh(config)
        assert config.value == "new_value"
        assert config.updated_at > config.created_at

    def test_set_value_invalidates_cache(self, db_session: Session):
        """Test set_value invalidates cache for the key."""
        # Arrange: Create config and warm cache
        config = LuxConfigFactory(key="cached.key", value="old_value")
        db_session.commit()

        service = LuxConfigService(db_session)
        service.get_value("cached.key")  # Warm cache

        # Act: Update value
        service.set_value("cached.key", "new_value")

        # Immediately get value (should reflect update, not cached old value)
        result = service.get_value("cached.key")

        # Assert: New value returned (cache invalidated)
        assert result == "new_value"

    def test_set_value_with_type_validation(self, db_session: Session):
        """Test set_value validates value against specified type."""
        # Arrange: Service
        service = LuxConfigService(db_session)

        # Act: Set value with type
        service.set_value("max.retries", "5", value_type="integer")

        # Assert: Verify type stored
        config = db_session.query(LuxConfig).filter(
            LuxConfig.key == "max.retries"
        ).first()
        assert config.value_type == "integer"
        assert config.value == "5"


class TestGetAllConfig:
    """Tests for get_all method."""

    def test_get_all_returns_all_configurations(self, db_session: Session):
        """Test get_all returns all configuration entries."""
        # Arrange: Create multiple configs
        LuxConfigFactory(key="feature.a", value="true")
        LuxConfigFactory(key="feature.b", value="false")
        LuxConfigFactory(key="feature.c", value="100")
        db_session.commit()

        service = LuxConfigService(db_session)

        # Act: Get all config
        result = service.get_all()

        # Assert: Verify all returned
        assert len(result) == 3
        assert any(c["key"] == "feature.a" for c in result)
        assert any(c["key"] == "feature.b" for c in result)
        assert any(c["key"] == "feature.c" for c in result)

    def test_get_all_empty_database(self, db_session: Session):
        """Test get_all returns empty list for empty database."""
        # Arrange: Service with empty database
        service = LuxConfigService(db_session)

        # Act: Get all config
        result = service.get_all()

        # Assert: Verify empty list
        assert result == []

    def test_get_all_filters_by_prefix(self, db_session: Session):
        """Test get_all filters configurations by key prefix."""
        # Arrange: Create configs with different prefixes
        LuxConfigFactory(key="feature.a.enabled", value="true")
        LuxConfigFactory(key="feature.b.enabled", value="false")
        LuxConfigFactory(key="system.max_retries", value="5")
        db_session.commit()

        service = LuxConfigService(db_session)

        # Act: Get config with prefix
        result = service.get_all(prefix="feature.")

        # Assert: Only feature.* configs returned
        assert len(result) == 2
        assert all(c["key"].startswith("feature.") for c in result)


class TestConfigValidation:
    """Tests for configuration validation."""

    def test_validate_config_boolean_success(self, db_session: Session):
        """Test validate_config accepts valid boolean values."""
        service = LuxConfigService(db_session)

        # Act/Assert: Valid boolean values
        assert service.validate_config("test.key", "true", "boolean") is True
        assert service.validate_config("test.key", "false", "boolean") is True
        assert service.validate_config("test.key", True, "boolean") is True

    def test_validate_config_boolean_failure(self, db_session: Session):
        """Test validate_config rejects invalid boolean values."""
        service = LuxConfigService(db_session)

        # Act/Assert: Invalid boolean values
        with pytest.raises(ConfigValidationError):
            service.validate_config("test.key", "not_a_boolean", "boolean")

    def test_validate_config_integer_success(self, db_session: Session):
        """Test validate_config accepts valid integer values."""
        service = LuxConfigService(db_session)

        # Act/Assert: Valid integer values
        assert service.validate_config("test.key", "100", "integer") is True
        assert service.validate_config("test.key", 100, "integer") is True
        assert service.validate_config("test.key", "-50", "integer") is True

    def test_validate_config_integer_failure(self, db_session: Session):
        """Test validate_config rejects invalid integer values."""
        service = LuxConfigService(db_session)

        # Act/Assert: Invalid integer values
        with pytest.raises(ConfigValidationError):
            service.validate_config("test.key", "not_an_integer", "integer")
        with pytest.raises(ConfigValidationError):
            service.validate_config("test.key", "12.5", "integer")

    def test_validate_config_range_validation(self, db_session: Session):
        """Test validate_config validates value ranges."""
        service = LuxConfigService(db_session)

        # Act/Assert: Valid range
        assert service.validate_config("test.key", "50", "integer", min_value=0, max_value=100) is True

        # Act/Assert: Out of range
        with pytest.raises(ConfigValidationError):
            service.validate_config("test.key", "150", "integer", min_value=0, max_value=100)
        with pytest.raises(ConfigValidationError):
            service.validate_config("test.key", "-10", "integer", min_value=0, max_value=100)

    def test_validate_config_string_length(self, db_session: Session):
        """Test validate_config validates string length."""
        service = LuxConfigService(db_session)

        # Act/Assert: Valid length
        assert service.validate_config("test.key", "valid_string", "string", max_length=20) is True

        # Act/Assert: Too long
        with pytest.raises(ConfigValidationError):
            service.validate_config("test.key", "x" * 100, "string", max_length=20)


class TestConfigTypeConversion:
    """Tests for type conversion utilities."""

    def test_get_value_as_boolean(self, db_session: Session):
        """Test get_value_as_boolean converts string to boolean."""
        # Arrange: Create config
        LuxConfigFactory(key="bool.config", value="true")
        db_session.commit()

        service = LuxConfigService(db_session)

        # Act: Get as boolean
        result = service.get_value_as_boolean("bool.config")

        # Assert: Verify boolean type
        assert isinstance(result, bool)
        assert result is True

    def test_get_value_as_integer(self, db_session: Session):
        """Test get_value_as_integer converts string to integer."""
        # Arrange: Create config
        LuxConfigFactory(key="int.config", value="42")
        db_session.commit()

        service = LuxConfigService(db_session)

        # Act: Get as integer
        result = service.get_value_as_integer("int.config")

        # Assert: Verify integer type
        assert isinstance(result, int)
        assert result == 42

    def test_get_value_as_float(self, db_session: Session):
        """Test get_value_as_float converts string to float."""
        # Arrange: Create config
        LuxConfigFactory(key="float.config", value="3.14")
        db_session.commit()

        service = LuxConfigService(db_session)

        # Act: Get as float
        result = service.get_value_as_float("float.config")

        # Assert: Verify float type
        assert isinstance(result, float)
        assert result == 3.14


class TestConfigBulkOperations:
    """Tests for bulk configuration operations."""

    def test_set_multiple_configs(self, db_session: Session):
        """Test set_multiple creates multiple configurations."""
        service = LuxConfigService(db_session)

        # Act: Set multiple configs
        configs = {
            "feature.a": "true",
            "feature.b": "false",
            "feature.c": "100"
        }
        service.set_multiple(configs)

        # Assert: Verify all created
        for key, value in configs.items():
            config = db_session.query(LuxConfig).filter(
                LuxConfig.key == key
            ).first()
            assert config is not None
            assert config.value == value

    def test_delete_config(self, db_session: Session):
        """Test delete removes configuration entry."""
        # Arrange: Create config
        config = LuxConfigFactory(key="to.delete", value="value")
        db_session.commit()

        service = LuxConfigService(db_session)

        # Act: Delete config
        service.delete("to.delete")

        # Assert: Verify deleted
        config = db_session.query(LuxConfig).filter(
            LuxConfig.key == "to.delete"
        ).first()
        assert config is None

    def test_delete_nonexistent_config(self, db_session: Session):
        """Test delete handles non-existent config gracefully."""
        service = LuxConfigService(db_session)

        # Act: Delete non-existent config (should not raise)
        service.delete("nonexistent.key")

        # Assert: No error
        assert True


class TestConfigPerformance:
    """Tests for config service performance."""

    def test_cache_performance(self, db_session: Session):
        """Test cache provides sub-millisecond lookups."""
        import time

        # Arrange: Create config and warm cache
        config = LuxConfigFactory(key="perf.key", value="value")
        db_session.commit()

        service = LuxConfigService(db_session)
        service.get_value("perf.key")  # Warm cache

        # Act: Measure 1000 cached lookups
        start = time.perf_counter()
        for _ in range(1000):
            service.get_value("perf.key")
        elapsed = time.perf_counter() - start

        # Assert: Average < 1ms per lookup
        avg_time_ms = (elapsed / 1000) * 1000
        assert avg_time_ms < 1.0, f"Cache lookup too slow: {avg_time_ms:.2f}ms"
```

**Verify:**
```bash
# Verify collection
pytest tests/unit/core/test_lux_config.py --collect-only -q

# Run tests with coverage
pytest tests/unit/core/test_lux_config.py \
  --cov=core.lux_config \
  --cov-branch \
  --cov-report=term-missing \
  -v

# Verify metrics
# - Line coverage: 90%+ (target)
# - Branch coverage: 60%+ (target)
# - Tests: ~25 tests
# - Collection errors: 0
```

**Done:**
- `tests/unit/core/test_lux_config.py` created with ~25 tests
- 90%+ line coverage achieved
- 60%+ branch coverage achieved
- 0 collection errors
- All tests passing

### Task 2: Test Messaging Schemas

**Files:** `tests/unit/core/test_messaging_schemas.py`

**Action:**
Create comprehensive tests for `core/messaging_schemas.py` (41 lines, est. 95% coverage):

```python
"""
Unit Tests for Messaging Schemas

Tests message schema definitions and validation:
- Message types (text, image, audio, video, file)
- Schema validation
- Message serialization/deserialization
- Error handling for invalid messages

Target Coverage: 95%
Target Branch Coverage: 60%+
"""

import pytest
from datetime import datetime
from pydantic import ValidationError
from unittest.mock import Mock, patch
import json

from core.messaging_schemas import (
    MessageSchema,
    TextMessage,
    ImageMessage,
    AudioMessage,
    VideoMessage,
    FileMessage,
    MessageMetadata,
    MessagePriority,
    MessageType
)


class TestMessageSchema:
    """Tests for base MessageSchema."""

    def test_create_minimal_message(self):
        """Test creating message with minimal required fields."""
        # Arrange: Message data
        message_data = {
            "id": "msg-123",
            "type": "text",
            "sender_id": "user-123",
            "content": "Hello World",
            "timestamp": datetime.utcnow().isoformat()
        }

        # Act: Create message
        message = MessageSchema(**message_data)

        # Assert: Verify fields
        assert message.id == "msg-123"
        assert message.type == MessageType.TEXT
        assert message.sender_id == "user-123"
        assert message.content == "Hello World"

    def test_create_message_with_metadata(self):
        """Test creating message with metadata."""
        # Arrange: Message data with metadata
        message_data = {
            "id": "msg-123",
            "type": "text",
            "sender_id": "user-123",
            "content": "Hello",
            "timestamp": datetime.utcnow().isoformat(),
            "metadata": {
                "priority": "high",
                "thread_id": "thread-456",
                "reply_to": "msg-122"
            }
        }

        # Act: Create message
        message = MessageSchema(**message_data)

        # Assert: Verify metadata
        assert message.metadata.priority == MessagePriority.HIGH
        assert message.metadata.thread_id == "thread-456"
        assert message.metadata.reply_to == "msg-122"

    def test_message_serialization(self):
        """Test message serializes to JSON correctly."""
        # Arrange: Create message
        message = MessageSchema(
            id="msg-123",
            type=MessageType.TEXT,
            sender_id="user-123",
            content="Hello World",
            timestamp=datetime.utcnow()
        )

        # Act: Serialize to JSON
        json_data = message.model_dump_json()

        # Assert: Verify JSON structure
        data = json.loads(json_data)
        assert data["id"] == "msg-123"
        assert data["type"] == "text"
        assert data["sender_id"] == "user-123"
        assert data["content"] == "Hello World"

    def test_message_deserialization(self):
        """Test message deserializes from JSON correctly."""
        # Arrange: JSON data
        json_data = {
            "id": "msg-123",
            "type": "text",
            "sender_id": "user-123",
            "content": "Hello World",
            "timestamp": datetime.utcnow().isoformat()
        }

        # Act: Deserialize from JSON
        message = MessageSchema(**json_data)

        # Assert: Verify message
        assert message.id == "msg-123"
        assert isinstance(message, MessageSchema)


class TestTextMessage:
    """Tests for TextMessage schema."""

    def test_create_text_message(self):
        """Test creating text message."""
        # Arrange: Text message data
        message_data = {
            "id": "msg-123",
            "type": "text",
            "sender_id": "user-123",
            "content": "This is a text message",
            "timestamp": datetime.utcnow().isoformat()
        }

        # Act: Create text message
        message = TextMessage(**message_data)

        # Assert: Verify message
        assert message.content == "This is a text message"
        assert isinstance(message, TextMessage)

    def test_text_message_max_length(self):
        """Test text message enforces max length."""
        # Arrange: Text message exceeding max length
        long_content = "x" * 10001  # Max 10000 chars

        message_data = {
            "id": "msg-123",
            "type": "text",
            "sender_id": "user-123",
            "content": long_content,
            "timestamp": datetime.utcnow().isoformat()
        }

        # Act/Assert: Should raise validation error
        with pytest.raises(ValidationError):
            TextMessage(**message_data)


class TestImageMessage:
    """Tests for ImageMessage schema."""

    def test_create_image_message(self):
        """Test creating image message."""
        # Arrange: Image message data
        message_data = {
            "id": "msg-123",
            "type": "image",
            "sender_id": "user-123",
            "content": "base64_encoded_image_data",
            "timestamp": datetime.utcnow().isoformat(),
            "image_metadata": {
                "format": "png",
                "width": 1920,
                "height": 1080,
                "size_bytes": 102400
            }
        }

        # Act: Create image message
        message = ImageMessage(**message_data)

        # Assert: Verify message
        assert message.image_metadata.format == "png"
        assert message.image_metadata.width == 1920
        assert message.image_metadata.height == 1080

    def test_image_message_missing_metadata(self):
        """Test image message requires image_metadata."""
        # Arrange: Image message without metadata
        message_data = {
            "id": "msg-123",
            "type": "image",
            "sender_id": "user-123",
            "content": "base64_data",
            "timestamp": datetime.utcnow().isoformat()
        }

        # Act/Assert: Should raise validation error
        with pytest.raises(ValidationError):
            ImageMessage(**message_data)


class TestAudioMessage:
    """Tests for AudioMessage schema."""

    def test_create_audio_message(self):
        """Test creating audio message."""
        # Arrange: Audio message data
        message_data = {
            "id": "msg-123",
            "type": "audio",
            "sender_id": "user-123",
            "content": "base64_audio_data",
            "timestamp": datetime.utcnow().isoformat(),
            "audio_metadata": {
                "format": "mp3",
                "duration_seconds": 120,
                "size_bytes": 204800
            }
        }

        # Act: Create audio message
        message = AudioMessage(**message_data)

        # Assert: Verify message
        assert message.audio_metadata.format == "mp3"
        assert message.audio_metadata.duration_seconds == 120

    def test_audio_message_duration_validation(self):
        """Test audio message validates duration."""
        # Arrange: Audio message with negative duration
        message_data = {
            "id": "msg-123",
            "type": "audio",
            "sender_id": "user-123",
            "content": "base64_data",
            "timestamp": datetime.utcnow().isoformat(),
            "audio_metadata": {
                "format": "mp3",
                "duration_seconds": -10,  # Invalid
                "size_bytes": 1024
            }
        }

        # Act/Assert: Should raise validation error
        with pytest.raises(ValidationError):
            AudioMessage(**message_data)


class TestVideoMessage:
    """Tests for VideoMessage schema."""

    def test_create_video_message(self):
        """Test creating video message."""
        # Arrange: Video message data
        message_data = {
            "id": "msg-123",
            "type": "video",
            "sender_id": "user-123",
            "content": "base64_video_data",
            "timestamp": datetime.utcnow().isoformat(),
            "video_metadata": {
                "format": "mp4",
                "duration_seconds": 300,
                "resolution": "1920x1080",
                "size_bytes": 5120000
            }
        }

        # Act: Create video message
        message = VideoMessage(**message_data)

        # Assert: Verify message
        assert message.video_metadata.format == "mp4"
        assert message.video_metadata.resolution == "1920x1080"

    def test_video_message_resolution_format(self):
        """Test video message validates resolution format."""
        # Arrange: Video message with invalid resolution
        message_data = {
            "id": "msg-123",
            "type": "video",
            "sender_id": "user-123",
            "content": "base64_data",
            "timestamp": datetime.utcnow().isoformat(),
            "video_metadata": {
                "format": "mp4",
                "duration_seconds": 300,
                "resolution": "invalid_resolution",  # Should be WIDTHxHEIGHT
                "size_bytes": 1024
            }
        }

        # Act/Assert: Should raise validation error
        with pytest.raises(ValidationError):
            VideoMessage(**message_data)


class TestFileMessage:
    """Tests for FileMessage schema."""

    def test_create_file_message(self):
        """Test creating file message."""
        # Arrange: File message data
        message_data = {
            "id": "msg-123",
            "type": "file",
            "sender_id": "user-123",
            "content": "base64_file_data",
            "timestamp": datetime.utcnow().isoformat(),
            "file_metadata": {
                "filename": "document.pdf",
                "mime_type": "application/pdf",
                "size_bytes": 102400
            }
        }

        # Act: Create file message
        message = FileMessage(**message_data)

        # Assert: Verify message
        assert message.file_metadata.filename == "document.pdf"
        assert message.file_metadata.mime_type == "application/pdf"

    def test_file_message_disallowed_types(self):
        """Test file message blocks disallowed file types."""
        # Arrange: File message with disallowed type
        message_data = {
            "id": "msg-123",
            "type": "file",
            "sender_id": "user-123",
            "content": "base64_data",
            "timestamp": datetime.utcnow().isoformat(),
            "file_metadata": {
                "filename": "malicious.exe",
                "mime_type": "application/x-executable",
                "size_bytes": 1024
            }
        }

        # Act/Assert: Should raise validation error
        with pytest.raises(ValidationError):
            FileMessage(**message_data)


class TestMessageMetadata:
    """Tests for MessageMetadata schema."""

    def test_create_metadata_with_priority(self):
        """Test creating metadata with priority."""
        # Arrange: Metadata data
        metadata_data = {
            "priority": "urgent",
            "thread_id": "thread-123",
            "reply_to": "msg-122",
            "tags": ["important", "urgent"]
        }

        # Act: Create metadata
        metadata = MessageMetadata(**metadata_data)

        # Assert: Verify fields
        assert metadata.priority == MessagePriority.URGENT
        assert metadata.thread_id == "thread-123"
        assert "important" in metadata.tags

    def test_metadata_optional_fields(self):
        """Test metadata with optional fields omitted."""
        # Arrange: Minimal metadata
        metadata_data = {
            "priority": "normal"
        }

        # Act: Create metadata
        metadata = MessageMetadata(**metadata_data)

        # Assert: Verify default values
        assert metadata.priority == MessagePriority.NORMAL
        assert metadata.thread_id is None
        assert metadata.tags == []

    def test_metadata_tag_validation(self):
        """Test metadata validates tag format."""
        # Arrange: Metadata with invalid tag (too long)
        metadata_data = {
            "priority": "normal",
            "tags": ["valid", "x" * 100]  # Max 50 chars per tag
        }

        # Act/Assert: Should raise validation error
        with pytest.raises(ValidationError):
            MessageMetadata(**metadata_data)


class TestMessageTypeValidation:
    """Tests for message type validation."""

    def test_invalid_message_type(self):
        """Test invalid message type raises error."""
        # Arrange: Message with invalid type
        message_data = {
            "id": "msg-123",
            "type": "invalid_type",
            "sender_id": "user-123",
            "content": "test",
            "timestamp": datetime.utcnow().isoformat()
        }

        # Act/Assert: Should raise validation error
        with pytest.raises(ValidationError):
            MessageSchema(**message_data)

    def test_valid_message_types(self):
        """Test all valid message types are accepted."""
        valid_types = ["text", "image", "audio", "video", "file"]

        for msg_type in valid_types:
            message_data = {
                "id": f"msg-{msg_type}",
                "type": msg_type,
                "sender_id": "user-123",
                "content": "test",
                "timestamp": datetime.utcnow().isoformat()
            }

            # Act: Create message (should not raise)
            message = MessageSchema(**message_data)

            # Assert: Type matches
            assert message.type.value == msg_type


class TestMessageSerializationEdgeCases:
    """Tests for message serialization edge cases."""

    def test_message_with_unicode_content(self):
        """Test message with unicode characters serializes correctly."""
        # Arrange: Message with emoji and unicode
        message_data = {
            "id": "msg-123",
            "type": "text",
            "sender_id": "user-123",
            "content": "Hello 🌍 世界 Ñoño",
            "timestamp": datetime.utcnow().isoformat()
        }

        # Act: Create and serialize
        message = MessageSchema(**message_data)
        json_data = message.model_dump_json()

        # Assert: Unicode preserved
        assert "🌍" in json_data
        assert "世界" in json_data

    def test_message_with_null_values(self):
        """Test message handles null values correctly."""
        # Arrange: Message with optional null fields
        message_data = {
            "id": "msg-123",
            "type": "text",
            "sender_id": "user-123",
            "content": "test",
            "timestamp": datetime.utcnow().isoformat(),
            "metadata": None
        }

        # Act: Create message (should not raise)
        message = MessageSchema(**message_data)

        # Assert: Null handled
        assert message.metadata is None
```

**Verify:**
```bash
# Verify collection
pytest tests/unit/core/test_messaging_schemas.py --collect-only -q

# Run tests with coverage
pytest tests/unit/core/test_messaging_schemas.py \
  --cov=core.messaging_schemas \
  --cov-branch \
  --cov-report=term-missing \
  -v

# Verify metrics
# - Line coverage: 95%+ (target)
# - Branch coverage: 60%+ (target)
# - Tests: ~20 tests
# - Collection errors: 0
```

**Done:**
- `tests/unit/core/test_messaging_schemas.py` created with ~20 tests
- 95%+ line coverage achieved
- 60%+ branch coverage achieved
- 0 collection errors
- All tests passing

### Task 3: Verify Collection and Coverage

**Files:** None (verification task)

**Action:**
Verify all tests from Tasks 1-2 collect successfully and meet coverage targets:

```bash
# Step 1: Verify no collection errors
pytest tests/unit/core/test_lux_config.py tests/unit/core/test_messaging_schemas.py \
  --collect-only -q

# Step 2: Run all tests with branch coverage
pytest tests/unit/core/test_lux_config.py tests/unit/core/test_messaging_schemas.py \
  --cov=core.lux_config \
  --cov=core.messaging_schemas \
  --cov-branch \
  --cov-report=term-missing \
  --cov-report=html \
  -v

# Step 3: Verify metrics
echo "=== Coverage Report ==="
coverage report | tail -1

# Step 4: Verify branch coverage specifically
coverage report --show-missing | grep "branch"

# Step 5: Check for collection errors
pytest tests/unit/core/test_lux_config.py tests/unit/core/test_messaging_schemas.py \
  --collect-only 2>&1 | grep -i error || echo "No collection errors ✓"
```

**Verify:**
- [ ] 0 collection errors
- [ ] 90-95% line coverage for both files
- [ ] 60%+ branch coverage for both files
- [ ] ~45 total tests
- [ ] 95%+ pass rate

**Done:**
- All tests collect successfully
- Coverage targets met
- Branch coverage targets met
- Ready for Plan 05 execution

## Verification

### Overall Plan Checks

1. **Test Coverage**
   - [ ] `core/lux_config.py`: 90%+ coverage
   - [ ] `core/messaging_schemas.py`: 95%+ coverage
   - [ ] Both files: 60%+ branch coverage

2. **Test Quality**
   - [ ] Config operations tested (get, set, delete, cache)
   - [ ] Message types tested (text, image, audio, video, file)
   - [ ] Validation tested (type checking, range validation)
   - [ ] Serialization/deserialization tested

3. **Collection Stability**
   - [ ] 0 collection errors
   - [ ] All imports resolve correctly
   - [ ] Fixtures loaded successfully

4. **Test Count**
   - [ ] ~45 tests created
   - [ ] 95%+ pass rate

## Success Criteria

Plan is **SUCCESSFUL** when:
- `tests/unit/core/test_lux_config.py` created with ~25 tests, 90%+ coverage
- `tests/unit/core/test_messaging_schemas.py` created with ~20 tests, 95%+ coverage
- Both files achieve 60%+ branch coverage
- 0 collection errors
- 95%+ tests passing
- Ready for Plan 05

## Output

After completion, create `.planning/phases/207-coverage-quality-push/207-04-SUMMARY.md`

---

*Plan created: March 18, 2026*
*Estimated effort: 3-4 hours*
*Dependencies: None (Wave 2, Plan 04 - parallel with 207-05, 207-06)*
