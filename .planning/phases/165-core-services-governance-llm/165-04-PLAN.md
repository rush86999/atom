---
phase: 165-core-services-coverage-governance-llm
plan: 04
type: execute
wave: 3
depends_on: ["165-01", "165-02", "165-03"]
files_modified:
  - backend/tests/coverage_reports/metrics/backend_phase_165_governance_llm.json
  - backend/tests/scripts/measure_phase_165_coverage.py
autonomous: true

must_haves:
  truths:
    - "Agent governance service achieves 80%+ line coverage (measured with --cov-branch)"
    - "LLM service achieves 80%+ line coverage (measured with --cov-branch)"
    - "Coverage report saved to metrics directory for phase tracking"
    - "Branch coverage measured alongside line coverage"
  artifacts:
    - path: "backend/tests/coverage_reports/metrics/backend_phase_165_governance_llm.json"
      provides: "Phase 165 coverage report (governance + LLM services)"
      contains: "coverage.json format with percent_covered, percent_covered_branch"
    - path: "backend/tests/scripts/measure_phase_165_coverage.py"
      provides: "Coverage measurement script for Phase 165"
      min_lines: 50
  key_links:
    - from: "backend/tests/scripts/measure_phase_165_coverage.py"
      to: "backend/tests/coverage_reports/metrics/backend_phase_165_governance_llm.json"
      via: "pytest --cov-report=json"
      pattern: "pytest.*--cov-report=json"
---

<objective>
Measure and verify 80%+ line coverage for governance and LLM services. Generate coverage report with branch coverage metrics for phase tracking.

Purpose: Validate that Phase 165 achieved its coverage target. Use actual line coverage (not service-level estimates) per Phase 163 methodology.

Output: Coverage report JSON showing 80%+ line coverage for agent_governance_service.py and byok_handler.py.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/165-core-services-governance-llm/165-RESEARCH.md

@backend/tests/scripts/generate_baseline_coverage_report.py
@backend/tests/docs/COVERAGE_GUIDE.md
@backend/tests/coverage_reports/metrics/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 165 coverage measurement script</name>
  <files>backend/tests/scripts/measure_phase_165_coverage.py</files>
  <action>
    Create coverage measurement script for Phase 165 governance and LLM services.

    Script structure:
    ```python
    #!/usr/bin/env python3
    """
    Coverage measurement script for Phase 165: Core Services Coverage (Governance & LLM)

    Measures actual line coverage (not service-level estimates) for:
    - agent_governance_service.py (770 lines)
    - byok_handler.py (1556 lines)

    Target: 80%+ line coverage with branch coverage enabled
    """
    import subprocess
    import json
    import sys
    from pathlib import Path

    # Coverage targets
    TARGET_LINE_COVERAGE = 80.0

    # Services under test
    SERVICES = [
        "core.agent_governance_service",
        "core.llm.byok_handler",
        "core.llm.cognitive_tier_system",
    ]

    # Test files
    TEST_FILES = [
        "tests/integration/services/test_governance_coverage.py",
        "tests/unit/services/test_agent_governance_service.py",
        "tests/integration/services/test_llm_coverage_governance_llm.py",
        "tests/unit/llm/test_byok_handler.py",
        "tests/unit/llm/test_cognitive_tier_classifier.py",
        "tests/property_tests/governance/test_governance_invariants_extended.py",
        "tests/property_tests/governance/test_governance_cache_consistency.py",
        "tests/property_tests/llm/test_cognitive_tier_invariants.py",
    ]

    def run_coverage_measurement():
        \"\"\"Run pytest with coverage and generate report.\"\"\"
        cmd = [
            "pytest",
            *TEST_FILES,
            "--cov", "core.agent_governance_service",
            "--cov", "core.llm.byok_handler",
            "--cov", "core.llm.cognitive_tier_system",
            "--cov-branch",
            "--cov-report=term-missing",
            "--cov-report=html:tests/coverage_reports/html",
            "--cov-report=json:tests/coverage_reports/metrics/backend_phase_165_governance_llm.json",
            "-v"
        ]

        print(f"Running: {' '.join(cmd)}")
        result = subprocess.run(cmd, cwd=Path(__file__).parent.parent.parent)

        return result.returncode

    def check_coverage_thresholds():
        \"\"\"Check if coverage meets 80% target.\"\"\"
        report_path = Path(__file__).parent.parent / "coverage_reports/metrics/backend_phase_165_governance_llm.json"

        if not report_path.exists():
            print(f"ERROR: Coverage report not found at {report_path}")
            return False

        with open(report_path) as f:
            report = json.load(f)

        # Check overall coverage
        overall_pct = report["totals"]["percent_covered"]
        overall_branch_pct = report["totals"].get("percent_covered_branch", 0)

        print(f"\\n=== Phase 165 Coverage Summary ===")
        print(f"Line Coverage: {overall_pct:.1f}%")
        print(f"Branch Coverage: {overall_branch_pct:.1f}%")

        # Check individual files
        print(f"\\n=== Per-File Coverage ===")
        for filename, file_data in report["files"].items():
            if any(s in filename for s in ["agent_governance_service", "byok_handler", "cognitive_tier"]):
                line_pct = file_data["summary"]["percent_covered"]
                branch_pct = file_data["summary"].get("percent_covered_branch", 0)
                print(f"{filename}: {line_pct:.1f}% line, {branch_pct:.1f}% branch")

        # Check target
        if overall_pct >= TARGET_LINE_COVERAGE:
            print(f"\\n✓ PASSED: {overall_pct:.1f}% >= {TARGET_LINE_COVERAGE}% target")
            return True
        else:
            print(f"\\n✗ FAILED: {overall_pct:.1f}% < {TARGET_LINE_COVERAGE}% target")
            return False

    if __name__ == "__main__":
        returncode = run_coverage_measurement()
        if returncode == 0:
            success = check_coverage_thresholds()
            sys.exit(0 if success else 1)
        else:
            sys.exit(returncode)
    ```

    Make script executable and follow COVERAGE_GUIDE.md methodology (actual line coverage, not estimates).

    DO NOT:
    - Use service-level estimation (must use coverage.py JSON output)
    - Measure without --cov-branch flag (branch coverage required)
  </action>
  <verify>python backend/tests/scripts/measure_phase_165_coverage.py</verify>
  <done>Script runs pytest with coverage for all Phase 165 tests; generates JSON report with line and branch coverage; checks against 80% target</done>
</task>

<task type="auto">
  <name>Task 2: Run coverage measurement and verify 80% target</name>
  <files>backend/tests/coverage_reports/metrics/backend_phase_165_governance_llm.json</files>
  <action>
    Execute the coverage measurement script and verify 80%+ line coverage target is met.

    Steps:
    1. Run the measurement script:
       ```bash
       cd backend
       python tests/scripts/measure_phase_165_coverage.py
       ```

    2. Check the generated JSON report:
       ```bash
       cat tests/coverage_reports/metrics/backend_phase_165_governance_llm.json | python -m json.tool
       ```

    3. Verify per-file coverage:
       - agent_governance_service.py: 80%+ line coverage
       - byok_handler.py: 80%+ line coverage
       - cognitive_tier_system.py: 80%+ line coverage (if applicable)

    4. If coverage < 80%, identify gaps:
       ```bash
       pytest tests/integration/services/test_governance_coverage.py \
              tests/integration/services/test_llm_coverage_governance_llm.py \
              --cov=core.agent_governance_service \
              --cov=core.llm.byok_handler \
              --cov-branch \
              --cov-report=term-missing
       ```

    5. For any missing lines, add targeted tests in the appropriate test file.

    DO NOT:
    - Accept coverage without checking --cov-branch output
    - Consider service-level estimates as valid (must use actual line coverage)
  </action>
  <verify>
    1. Check report exists: ls -la backend/tests/coverage_reports/metrics/backend_phase_165_governance_llm.json
    2. Verify 80%+ target: cat backend/tests/coverage_reports/metrics/backend_phase_165_governance_llm.json | grep '"percent_covered"'
  </verify>
  <done>Coverage report shows 80%+ line coverage for agent_governance_service.py and byok_handler.py; branch coverage measured and reported</done>
</task>

<task type="auto">
  <name>Task 3: Create phase verification summary</name>
  <files>.planning/phases/165-core-services-governance-llm/165-VERIFICATION.md</files>
  <action>
    Create verification summary documenting Phase 165 completion against all success criteria.

    Verification template:
    ```markdown
    # Phase 165: Core Services Coverage (Governance & LLM) - Verification

    **Verified:** 2026-03-11
    **Status:** COMPLETE / PARTIAL / FAILED

    ## Requirements Verification

    ### CORE-01: Agent Governance Service Coverage
    - [x] 80%+ line coverage achieved
    - [x] Maturity routing tested (4x4 matrix)
    - [x] Permission checks tested
    - [x] Cache validation tested

    ### CORE-02: LLM Service Coverage
    - [x] 80%+ line coverage achieved
    - [x] Provider routing tested (4 complexity levels)
    - [x] Cognitive tier classification tested (5 tiers)
    - [x] Streaming tested with AsyncMock
    - [x] Cache routing tested

    ### CORE-04: Governance Invariants (Property-Based Tests)
    - [x] Confidence score bounds [0.0, 1.0] validated
    - [x] Cache consistency validated
    - [x] Maturity rules validated
    - [x] Permission check invariants validated

    ### CORE-05: Maturity Matrix (Parametrized Tests)
    - [x] 4 maturity levels x 4 action complexities = 16 combinations tested
    - [x] All agent behaviors covered

    ## Coverage Metrics

    | Service | Lines | Covered | % | Branch % |
    |---------|-------|---------|---|----------|
    | agent_governance_service.py | 770 | XXX | XX.X% | XX.X% |
    | byok_handler.py | 1556 | XXX | XX.X% | XX.X% |
    | cognitive_tier_system.py | XXX | XXX | XX.X% | XX.X% |
    | **TOTAL** | **2325+** | **XXX** | **XX.X%** | **XX.X%** |

    ## Test Files Created

    - tests/integration/services/test_governance_coverage.py (XXX tests)
    - tests/integration/services/test_llm_coverage_governance_llm.py (XXX tests)
    - tests/property_tests/governance/test_governance_invariants_extended.py (XXX tests)
    - tests/property_tests/llm/test_cognitive_tier_invariants.py (XXX tests)
    - tests/property_tests/governance/test_governance_cache_consistency.py (XXX tests)

    ## Known Gaps (if any)

    List any uncovered lines or methods if coverage < 80%.

    ## Next Steps

    Phase 166: Core Services Coverage (Episodic Memory)
    ```

    Fill in actual coverage numbers from the JSON report.
  </action>
  <verify>
    1. Check verification file exists: cat .planning/phases/165-core-services-governance-llm/165-VERIFICATION.md
    2. Verify all 4 requirements (CORE-01, CORE-02, CORE-04, CORE-05) are marked
  </verify>
  <done>Verification summary created with actual coverage metrics; all requirements checked; gaps (if any) documented</done>
</task>

</tasks>

<verification>
After all tasks complete, verify phase success:

```bash
# Check coverage report
cat backend/tests/coverage_reports/metrics/backend_phase_165_governance_llm.json | grep -E '"percent_covered"|"percent_covered_branch"'

# Verify all test files exist
ls -la backend/tests/integration/services/test_governance_coverage.py
ls -la backend/tests/integration/services/test_llm_coverage_governance_llm.py
ls -la backend/tests/property_tests/governance/test_governance_invariants_extended.py
ls -la backend/tests/property_tests/llm/test_cognitive_tier_invariants.py
ls -la backend/tests/property_tests/governance/test_governance_cache_consistency.py

# Check verification summary
cat .planning/phases/165-core-services-governance-llm/165-VERIFICATION.md
```

Expected:
- agent_governance_service.py >= 80% coverage
- byok_handler.py >= 80% coverage
- All test files created and passing
- Verification summary complete
</verification>

<success_criteria>
1. agent_governance_service.py achieves 80%+ line coverage (measured with --cov-branch)
2. byok_handler.py achieves 80%+ line coverage (measured with --cov-branch)
3. Coverage report JSON generated in metrics directory
4. Branch coverage measured and reported alongside line coverage
5. All Phase 165 test files created and passing
6. Verification summary documents all requirements met
</success_criteria>

<output>
After completion, create `.planning/phases/165-core-services-governance-llm/165-04-SUMMARY.md`
</output>
