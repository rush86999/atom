# Phase 225.2: Infrastructure LLMService Migration - Context

**Gathered:** March 22, 2026
**Status:** Ready for planning

## Phase Boundary

Complete the LLMService migration by refactoring the final 2 infrastructure files that still use BYOKHandler directly, ensuring unified LLM interface across the entire codebase.

**Goals:**
1. **Final Migration** — Migrate event_sourced_architecture.py and workflow_engine.py to LLMService
2. **Remove Legacy Imports** — Eliminate all remaining direct BYOKHandler usage in production code
3. **Test Compatibility** — Ensure all existing tests pass with LLMService integration
4. **Complete Standardization** — Achieve 100% LLMService coverage across all services

## Files in Scope

### 1. event_sourced_architecture.py
**Current Usage:**
```python
# Line 151
from core.llm.byok_handler import BYOKHandler
handler = BYOKHandler()
```
**Purpose:** LLM analysis for event perception and reasoning
**Test Coverage:** Check for existing tests

### 2. workflow_engine.py
**Current Usage:**
```python
# Lines 1580, 1632
from core.llm.byok_handler import BYOKHandler
llm_handler = BYOKHandler(self.db)
response = await llm_handler.chat_completion(...)
```
**Purpose:** Agent LLM execution within workflows
**Test Coverage:** Check for existing tests

## Implementation Decisions

### Locked Decisions (from Milestone v6.0)
- **Unified LLM Interface** — All LLM interactions must flow through LLMService
- **Migration Pattern** — Follow established pattern from Phases 223-225.1
- **Async/Await** — LLMService is fully async, add await where needed
- **Test Updates** — Update test mocks to patch LLMService instead of BYOKHandler

### Claude's Discretion
- **File Migration Order** — Determine which file to migrate first (check dependencies)
- **Method Mapping** — Determine which LLMService methods to use (generate vs. chat_completion)
- **Error Handling** — Ensure error handling patterns work with LLMService
- **Testing Strategy** — Decide whether to test individually or together

## Migration Checklist

For each file:
1. Replace `from core.llm.byok_handler import BYOKHandler` with `from core.llm_service import get_llm_service`
2. Update instantiation: `BYOKHandler()` → `get_llm_service()`
3. Update method calls:
   - `handler.chat_completion()` → `await service.generate()` or `await service.generate_structured()`
4. Add `await` for async method calls
5. Update response parsing if needed
6. Update test mocks: patch `LLMService` instead of `BYOKHandler`
7. Run tests to verify no regression
8. Verify embeddings, cost tracking, side effects work correctly

## Verification Criteria

- [ ] event_sourced_architecture.py uses LLMService instead of BYOKHandler
- [ ] workflow_engine.py uses LLMService instead of BYOKHandler
- [ ] No direct `from core.llm.byok_handler import BYOKHandler` in production code (except LLMService wrapper and API layer)
- [ ] All existing tests pass with LLMService integration
- [ ] No regression in event processing or workflow execution
- [ ] 100% LLMService coverage achieved

## Success Criteria

**What must be TRUE:**
1. Both infrastructure files use LLMService for all LLM interactions
2. All tests pass with LLMService integration
3. No regression in event processing or workflow execution
4. Codebase achieves 100% LLMService coverage (excluding LLMService wrapper itself and API boundary)

## Dependencies

- **Phase 225.1 (Agent LLMService Migration)** — Must be complete (✓ COMPLETE)
- **Phase 225 (Critical Migration Part 3)** — Must be complete (✓ COMPLETE)

## Related Requirements

- **STD-01**: Core services use unified LLM interface
- **STD-02**: Standardize async/await patterns across services
- **MIG-10**: Complete LLMService migration across all services

---

**Phase:** 225.2-infrastructure-migration
**Context Created:** 2026-03-22
**Next Step:** Plan and execute migration for both files.
