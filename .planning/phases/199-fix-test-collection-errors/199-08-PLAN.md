---
phase: 199-fix-test-collection-errors
plan: 08
type: execute
wave: 3
depends_on: [199-04, 199-05]
files_modified: [backend/tests/core/test_student_training_service_coverage.py]
autonomous: true

must_haves:
  truths:
    - "student_training_service.py coverage reaches 75%+"
    - "Training session management tested"
    - "Proposal workflow scenarios covered"
    - "Supervision integration tested"
  artifacts:
    - path: "backend/tests/core/test_student_training_service_coverage.py"
      provides: "Comprehensive student training service tests"
      contains: "15-20 tests covering sessions, proposals, supervision"
  key_links:
    - from: "test_student_training_service_coverage.py"
      to: "core/student_training_service.py"
      via: "pytest coverage"
      pattern: "test_.*training.*session|proposal.*workflow|supervision.*integration"
---

<objective>
Increase student_training_service.py coverage to 75%+ by testing training sessions and proposal workflows.

Purpose: Unblock training service coverage and enable internship progression scenarios.
Output: 15-20 new tests achieving 75%+ coverage on student_training_service.py.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-fix-test-collection-errors/199-RESEARCH.md
@.planning/phases/199-fix-test-collection-errors/199-05-TARGETS.md
@backend/core/student_training_service.py
@backend/tests/core/
@.planning/phases/198-coverage-push-85/198-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Analyze student_training_service.py Coverage Needs</name>
  <files>backend/core/student_training_service.py, backend/tests/coverage_reports/html/index.html</files>
  <action>
    1. Read HTML coverage report for student_training_service.py

    2. Check current coverage status:
       - Was coverage blocked by schema issues in Phase 198?
       - Which functions are currently uncovered?
       - Which training scenarios need testing?

    3. Key training service scenarios to target:
       - Training session CRUD operations
       - Proposal workflow (create, approve, reject, expire)
       - Training duration estimation
       - Supervision integration
       - Training completion and graduation trigger
       - Error paths (invalid sessions, missing agents)

    4. Check for schema issues:
       - Are TrainingSession model fields accessible?
       - Are AgentProposal model fields accessible?
       - Any Pydantic/SQLAlchemy compatibility issues?

    5. Create test plan based on actual uncovered lines
  </action>
  <verify>
    Run: open backend/tests/coverage_reports/html/index.html and navigate to student_training_service.py
    Expected: HTML report shows current coverage status
  </verify>
  <done>
    Current coverage status documented
    Schema issues identified and resolved
    Key training scenarios catalogued
    Test plan created for coverage gaps
  </done>
</task>

<task type="auto">
  <name>Create Student Training Service Tests</name>
  <files>backend/tests/core/test_student_training_service_coverage.py</files>
  <action>
    Create test file: backend/tests/core/test_student_training_service_coverage.py

    Include tests for training service functionality:

    1. Training Session Tests (4-5 tests):
       - test_create_training_session_for_student_agent
       - test_get_training_session_by_id
       - test_update_training_session_progress
       - test_complete_training_session_success
       - test_complete_training_session_failure

    2. Proposal Workflow Tests (4-5 tests):
       - test_create_proposal_for_intern_agent
       - test_approve_proposal_executes_action
       - test_reject_proposal_blocks_action
       - test_proposal_expires_after_timeout
       - test_proposal_with_invalid_action

    3. Training Duration Tests (2-3 tests):
       - test_estimate_training_duration_new_agent
       - test_estimate_training_duration_with_history
       - test_estimate_training_duration_override

    4. Supervision Integration Tests (3-4 tests):
       - test_training_references_supervision_session
       - test_supervision_failure_extends_training
       - test_training_complete_allows_supervision_promotion
       - test_training_incomplete_blocks_graduation

    5. Error Path Tests (2-3 tests):
       - test_training_with_nonexistent_agent
       - test_proposal_with_deleted_session
       - test_training_with_invalid_maturity_level

    Use existing test patterns from Phase 198
    Follow Pydantic v2 and SQLAlchemy 2.0 patterns from Plan 02
    Mock dependencies appropriately (supervision_service, governance_cache)
  </action>
  <verify>
    Run: python3 -m pytest backend/tests/core/test_student_training_service_coverage.py -v
    Expected: 15-20 tests collected, all passing
  </verify>
  <done>
    15-20 training service tests created
    Training session tests included (4-5)
    Proposal workflow tests included (4-5)
    Training duration tests included (2-3)
    Supervision integration tests included (3-4)
    Error path tests included (2-3)
  </done>
</task>

<task type="auto">
  <name>Verify student_training_service.py Coverage</name>
  <files>backend/tests/core/test_student_training_service_coverage.py</files>
  <action>
    1. Run tests with coverage for student_training_service:
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/core/test_student_training_service_coverage.py --cov=core.student_training_service --cov-branch --cov-report=term-missing -v

    2. Verify coverage percentage:
       - Target: 75%+ coverage
       - If <75%, add more tests for remaining uncovered lines

    3. Run all training-related tests together:
       pytest backend/tests/core/test_student_training*.py backend/tests/core/test_supervision*.py -v

    4. Verify no regressions in existing tests

    5. If coverage target not met, iterate with additional tests
  </action>
  <verify>
    Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/core/test_student_training_service_coverage.py --cov=core.student_training_service --cov-branch --cov-report=term-missing -q
    Expected: "75.XX% coverage" (75% or higher)
  </verify>
  <done>
    student_training_service.py coverage 75%+
    New tests passing without regressions
    All training tests passing together
    Schema issues resolved
    Coverage target achieved
  </done>
</task>

</tasks>

<verification>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/core/test_student_training_service_coverage.py --cov=core.student_training_service --cov-branch -q
Expected: "75.XX% coverage" (75% or higher)

Run: python3 -m pytest backend/tests/core/test_student_training*.py -q
Expected: All tests passing
</verification>

<success_criteria>
- student_training_service.py coverage increased to 75%+ (from blocked/baseline)
- 15-20 new tests covering training scenarios
- Training session CRUD operations tested
- Proposal workflow scenarios covered
- Training duration estimation tested
- Supervision integration validated
- Error paths and edge cases covered
</success_criteria>

<output>
After completion, create `.planning/phases/199-fix-test-collection-errors/199-08-SUMMARY.md`
</output>
