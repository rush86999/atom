---
phase: 199-fix-test-collection-errors
plan: 09
type: execute
wave: 4
depends_on: [199-06, 199-07, 199-08]
files_modified: [backend/tests/e2e/test_agent_execution_episodic_integration.py]
autonomous: true

must_haves:
  truths:
    - "Agent execution to episodic memory flow tested"
    - "Episode creation from agent actions validated"
    - "All maturity levels create episodes correctly"
    - "Canvas context linked to episodes"
    - "Feedback context linked to episodes"
  artifacts:
    - path: "backend/tests/e2e/test_agent_execution_episodic_integration.py"
      provides: "E2E tests for agent execution and episodic memory integration"
      contains: "5-8 tests covering execution → episode creation pipeline"
  key_links:
    - from: "test_agent_execution_episodic_integration.py"
      to: "core/atom_agent_endpoints.py, core/episode_segmentation_service.py"
      via: "E2E integration test"
      pattern: "test_.*agent.*execution.*episode|episodic.*memory.*integration"
---

<objective>
Create E2E integration tests for agent execution to episodic memory flow.

Purpose: Validate the complete pipeline from agent action execution through episode creation and retrieval.
Output: 5-8 E2E tests validating governance → execution → episodic memory integration.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-fix-test-collection-errors/199-RESEARCH.md
@backend/core/atom_agent_endpoints.py
@backend/core/episode_segmentation_service.py
@backend/tests/e2e/conftest_e2e.py
@.planning/phases/198-coverage-push-85/198-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create E2E Integration Test File</name>
  <files>backend/tests/e2e/test_agent_execution_episodic_integration.py</files>
  <action>
    Create test file: backend/tests/e2e/test_agent_execution_episodic_integration.py

    Import required fixtures from conftest_e2e.py:
    - test_app (FastAPI test client)
    - db_session (database session)
    - mock_llm_response (deterministic LLM mocking)
    - mock_embedding_vectors (deterministic embeddings)
    - test_agent_* fixtures for all maturity levels

    Set up E2E test structure using existing patterns from Phase 198 Plan 06
  </action>
  <verify>
    Run: python3 -m pytest backend/tests/e2e/test_agent_execution_episodic_integration.py --collect-only
    Expected: Test file collects successfully
  </verify>
  <done>
    E2E test file created
    Required fixtures imported
    Test structure established
    File collects without errors
  </done>
</task>

<task type="auto">
  <name>Create Agent Execution Episode Tests</name>
  <files>backend/tests/e2e/test_agent_execution_episodic_integration.py</files>
  <action>
    Add E2E tests for agent execution → episode creation:

    1. AUTONOMOUS Agent Episode Creation (2 tests):
       - test_autonomous_agent_execution_creates_episode
         - Execute AUTONOMOUS agent chat
         - Verify episode created with correct agent_id
         - Verify episode contains action segments
         - Verify episode has LLM-generated summary

       - test_autonomous_agent_multiple_actions_creates_segments
         - Execute multiple AUTONOMOUS agent actions
         - Verify episode has multiple segments
         - Verify segment timestamps are sequential

    2. SUPERVISED Agent Episode Creation (2 tests):
       - test_supervised_agent_execution_creates_monitored_episode
         - Execute SUPERVISED agent chat
         - Verify episode created with supervision metadata
         - Verify supervision session linked

       - test_supervised_agent_intervention_creates_episode_segment
         - Execute SUPERVISED agent with intervention
         - Verify episode segment records intervention
         - Verify intervention reason stored

    3. Canvas Context Integration (1 test):
       - test_agent_canvas_presentation_creates_canvas_episode
         - Execute agent with canvas presentation
         - Verify episode contains canvas_context
         - Verify canvas_type and content linked

    4. Feedback Context Integration (1 test):
       - test_agent_with_feedback_creates_feedback_episode
         - Execute agent action
         - Add feedback (thumbs up/down)
         - Verify episode contains feedback_context
         - Verify feedback score affects retrieval

    Use existing E2E test patterns from Phase 198 Plan 06
    Follow Pydantic v2 patterns from Plan 02
  </action>
  <verify>
    Run: python3 -m pytest backend/tests/e2e/test_agent_execution_episodic_integration.py -v
    Expected: 6 tests collected, all passing
  </verify>
  <done>
    6 E2E integration tests created
    AUTONOMOUS agent episode tests (2)
    SUPERVISED agent episode tests (2)
    Canvas context integration test (1)
    Feedback context integration test (1)
  </done>
</task>

<task type="auto">
  <name>Verify E2E Integration Test Execution</name>
  <files>backend/tests/e2e/test_agent_execution_episodic_integration.py</files>
  <action>
    1. Run E2E tests with full stack:
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/e2e/test_agent_execution_episodic_integration.py -v

    2. Verify each test validates:
       - Agent execution completes successfully
       - Episode created in database
       - Episode segments created correctly
       - Canvas/feedback context linked (where applicable)
       - No errors in integration flow

    3. Run with coverage to measure integration path coverage:
       pytest backend/tests/e2e/test_agent_execution_episodic_integration.py --cov=core.atom_agent_endpoints --cov=core.episode_segmentation_service --cov-report=term-missing -q

    4. Verify no regressions in existing E2E tests

    5. Check for flaky tests (run tests twice)
  </action>
  <verify>
    Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/e2e/test_agent_execution_episodic_integration.py -v
    Expected: All 6 tests passing, no errors
  </verify>
  <done>
    E2E tests passing consistently
    Agent execution → episode creation validated
    Canvas and feedback context integration verified
    Coverage measured for integration paths
    No regressions in existing E2E tests
    No flaky tests detected
  </done>
</task>

</tasks>

<verification>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/e2e/test_agent_execution_episodic_integration.py -v
Expected: 6 tests passing

Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/e2e/test_agent_execution_episodic_integration.py --cov=core.atom_agent_endpoints --cov=core.episode_segmentation_service --cov-branch -q
Expected: Coverage increased for integration paths
</verification>

<success_criteria>
- 5-8 E2E integration tests created
- Agent execution → episode creation flow validated
- All maturity levels tested (AUTONOMOUS, SUPERVISED)
- Canvas context integration verified
- Feedback context integration verified
- Tests pass consistently without flakiness
- Integration path coverage increased
</success_criteria>

<output>
After completion, create `.planning/phases/199-fix-test-collection-errors/199-09-SUMMARY.md`
</output>
