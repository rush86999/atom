---
phase: 199-fix-test-collection-errors
plan: 04
type: execute
wave: 2
depends_on: [199-01, 199-02, 199-03]
files_modified: [backend/tests/coverage_reports/baseline_coverage.json]
autonomous: true

must_haves:
  truths:
    - "Full test suite runs without collection errors"
    - "Coverage report generated successfully"
    - "Baseline coverage measured accurately"
    - "Coverage JSON available for analysis"
  artifacts:
    - path: "backend/tests/coverage_reports/baseline_coverage.json"
      provides: "Coverage report for Phase 199 baseline"
      contains: "coverage percentages by module"
    - path: "backend/tests/coverage_reports/html/index.html"
      provides: "HTML coverage report"
      contains: "coverage visualization"
  key_links:
    - from: "pytest --cov"
      to: "coverage.json"
      via: "pytest-cov plugin"
      pattern: "--cov-report=json"
---

<objective>
Measure baseline coverage accurately after fixing collection errors.

Purpose: Establish accurate coverage baseline for targeting 85% overall coverage.
Output: Coverage report (JSON + HTML) showing actual coverage with all tests collected.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-fix-test-collection-errors/199-RESEARCH.md
@backend/pytest.ini
@backend/tests/coverage_reports/
@.planning/phases/198-coverage-push-85/198-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Generate Coverage Report with All Tests</name>
  <files>backend/tests/</files>
  <action>
    1. From backend directory, run pytest with coverage:
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest --cov=core --cov=api --cov=tools --cov-branch --cov-report=json --cov-report=html --cov-report=term-missing -q

    2. Coverage report will be generated in:
       - backend/coverage.json (root of project, not backend/tests/)
       - backend/htmlcov/ or backend/tests/coverage_reports/html/

    3. Command should complete without collection errors (Wave 1 fixes)

    4. Capture overall coverage percentage from terminal output

    5. Note: Overall coverage should be higher than 74.6% baseline from Phase 198
       because 150+ tests created in Phase 198 can now be collected
  </action>
  <verify>
    Run: ls -la /Users/rushiparikh/projects/atom/backend/coverage.json 2>/dev/null || ls -la /Users/rushiparikh/projects/atom/backend/tests/coverage_reports/*.json 2>/dev/null
    Expected: coverage.json file exists
  </verify>
  <done>
    Coverage report generated (JSON + HTML)
    Overall coverage percentage captured
    0 collection errors during run
    Coverage file location documented
  </done>
</task>

<task type="auto">
  <name>Analyze Baseline Coverage Results</name>
  <files>backend/coverage.json or backend/tests/coverage_reports/*.json</files>
  <action>
    1. Read coverage.json to extract module-level coverage

    2. Key modules to analyze:
       - core/agent_governance_service.py (target: 62% → 85%)
       - core/trigger_interceptor.py (target: 74% → 85%)
       - core/episode_segmentation_service.py (target: 83.8% → 85%)
       - core/agent_graduation_service.py (target: 73.8% → 85%)
       - core/student_training_service.py (target: blocked → 75%)
       - core/supervision_service.py (already 78%)
       - core/governance_cache.py (already 90%+)

    3. For each module, document:
       - Current coverage percentage
       - Lines of code
       - Lines covered
       - Lines missing
       - Gap to 85% target

    4. Calculate impact score: (Lines × Coverage Gap) to prioritize targets

    5. Identify modules with highest impact for Wave 3 targeting
  </action>
  <verify>
    Run: python3 -c "import json; data = json.load(open('backend/coverage.json')); print(f\"Overall: {data['totals']['percent_covered']:.1f}%\")" 2>/dev/null || python3 -c "import json; data = json.load(open('backend/tests/coverage_reports/coverage.json')); print(f\"Overall: {data['totals']['percent_covered']:.1f}%\")"
    Expected: Overall coverage percentage displayed
  </verify>
  <done>
    Overall coverage documented
    Module-level coverage extracted
    Lines of code and missing coverage identified
    High-impact targets ranked for Wave 3
    Gap to 85% target calculated
  </done>
</task>

<task type="auto">
  <name>Document Coverage Baseline for Phase 199</name>
  <files>.planning/phases/199-fix-test-collection-errors/199-04-PLAN.md</files>
  <action>
    Create a coverage baseline summary:

    1. Overall coverage:
       - Phase 198: 74.6% (with collection errors)
       - Phase 199 baseline: [ACTUAL] % (with all tests collected)
       - Gap to 85%: [CALCULATED] %

    2. Module breakdown:
       | Module | Coverage | Lines | Gap | Priority |
       |--------|----------|-------|-----|----------|
       | agent_governance_service | XX% | XXX | XX% | HIGH |
       | trigger_interceptor | XX% | XXX | XX% | MED |
       | [etc.] | | | | |

    3. Collection errors:
       - Phase 198: 10 collection errors
       - Phase 199 baseline: 0 collection errors

    4. Tests collected:
       - Phase 198: ~5,700 (with errors)
       - Phase 199 baseline: ~5,800+ (clean collection)

    Save this summary for Wave 3 targeting decisions.
  </action>
  <verify>
    Run: cat .planning/phases/199-fix-test-collection-errors/199-04-SUMMARY.md | grep "Overall coverage"
    Expected: Overall coverage documented in summary
  </verify>
  <done>
    Coverage baseline summary created
    Overall vs Phase 198 comparison documented
    Module-level coverage catalogued
    Collection error fix verified (0 errors)
    High-impact targets identified for Wave 3
  </done>
</task>

</tasks>

<verification>
Run: python3 -m pytest --cov=core --cov=api --cov=tools --cov-branch --cov-report=json -q 2>&1 | tail -5
Expected: "XX.XX% coverage" with 0 collection errors

Run: ls -la backend/coverage.json 2>/dev/null || ls -la backend/tests/coverage_reports/*.json 2>/dev/null
Expected: Coverage JSON file exists
</verification>

<success_criteria>
- Full coverage report generated (JSON + HTML)
- Overall coverage measured accurately (no collection errors)
- Module-level coverage extracted for targeting
- 0 collection errors during coverage run
- High-impact modules identified for Wave 3
</success_criteria>

<output>
After completion, create `.planning/phases/199-fix-test-collection-errors/199-04-SUMMARY.md`
</output>
