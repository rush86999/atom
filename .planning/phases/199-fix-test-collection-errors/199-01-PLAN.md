---
phase: 199-fix-test-collection-errors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/pytest.ini]
autonomous: true

must_haves:
  truths:
    - "pytest --collect-only shows 0 collection errors"
    - "Archive tests excluded from collection"
    - "Non-backend tests excluded from collection"
    - "pytest deprecation warning resolved"
  artifacts:
    - path: "backend/pytest.ini"
      provides: "pytest configuration with ignore patterns"
      contains: "--ignore=archive/ --ignore=frontend-nextjs/ --ignore=scripts/"
  key_links:
    - from: "pytest --collect-only"
      to: "backend/pytest.ini"
      via: "pytest configuration parsing"
      pattern: "--ignore=archive/"
---

<objective>
Fix test collection errors by configuring pytest to exclude non-backend test files and resolve deprecation warnings.

Purpose: Enable 150+ existing tests from Phase 198 to be collected and measured for coverage.
Output: Updated pytest.ini with proper ignore patterns, 0 collection errors.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-fix-test-collection-errors/199-RESEARCH.md
@backend/pytest.ini
@backend/conftest.py
@.planning/phases/198-coverage-push-85/198-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Fix Collection Errors via pytest.ini Configuration</name>
  <files>backend/pytest.ini</files>
  <action>
    1. Read current backend/pytest.ini
    2. Add the following --ignore patterns to the addopts line:
       - --ignore=archive/
       - --ignore=frontend-nextjs/
       - --ignore=scripts/
    3. The addopts line should become:
       addopts = -q --strict-markers --tb=line --ignore=archive/ --ignore=frontend-nextjs/ --ignore=scripts/ --ignore=tests/integration/episodes/test_lancedb_integration.py --ignore=tests/integration/episodes/test_graduation_validation.py --ignore=tests/integration/episodes/test_episode_lifecycle_lancedb.py --ignore=tests/integration/governance/test_graduation_exams.py --ignore=tests/unit/test_agent_integration_gateway.py --deselect=tests/test_agent_governance_runtime.py::test_agent_governance_gating --maxfail=10
    4. These ignore patterns exclude:
       - archive/old_project_structure/ tests (6 ModuleNotFoundError errors)
       - frontend-nextjs/ project tests (1 ModuleNotFoundError error)
       - scripts/ tests (2 ModuleNotFoundError errors)
    5. The backend/ conftest.py deprecation warning is benign (pytest 9.0.2 allows non-top-level conftest in testpaths directory)
  </action>
  <verify>
    Run: python3 -m pytest --collect-only 2>&1 | grep "ERROR collecting"
    Expected: 0 ERROR collecting lines (archive and non-backend files excluded)
  </verify>
  <done>
    Archive tests excluded from pytest collection
    Non-backend tests excluded from pytest collection
    0 collection errors from ModuleNotFoundError
    pytest --collect-only completes without errors
  </done>
</task>

<task type="auto">
  <name>Verify Collection Error Fix</name>
  <files>backend/pytest.ini</files>
  <action>
    1. Run pytest --collect-only -q to verify collection
    2. Check output for "ERROR collecting" - should be 0
    3. Check output for "collected" to verify tests are found
    4. The backend/ conftest.py deprecation warning is acceptable (pytest allows this in testpaths)
    5. Count total tests collected - should be ~5,700+ tests
  </action>
  <verify>
    Run: python3 -m pytest --collect-only -q 2>&1 | tail -5
    Expected: "XX tests collected, 0 errors" (no ERROR collecting lines)
  </verify>
  <done>
    pytest --collect-only shows 0 errors
    5,700+ tests collected
    Only backend tests included in collection
    Archive and non-backend files excluded
  </done>
</task>

</tasks>

<verification>
Run: python3 -m pytest --collect-only 2>&1 | grep -c "ERROR collecting"
Expected: 0 (no collection errors)

Run: python3 -m pytest --collect-only -q 2>&1 | tail -2
Expected: "XX tests collected, 0 errors"
</verification>

<success_criteria>
- pytest.ini updated with --ignore patterns for archive/, frontend-nextjs/, scripts/
- pytest --collect-only shows 0 collection errors
- 5,700+ backend tests collected successfully
- Archive and non-backend test files excluded from collection
</success_criteria>

<output>
After completion, create `.planning/phases/199-fix-test-collection-errors/199-01-SUMMARY.md`
</output>
