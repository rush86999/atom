---
phase: 199-fix-test-collection-errors
plan: 03
type: execute
wave: 1
depends_on: [199-01]
files_modified: [backend/tests/unit/governance/test_governance_streaming.py]
autonomous: true

must_haves:
  truths:
    - "CanvasAudit schema assertions match current model"
    - "Removed fields (agent_execution_id, component_type) no longer referenced"
    - "Tests pass with current schema"
    - "0 CanvasAudit-related AttributeError failures"
  artifacts:
    - path: "backend/tests/unit/governance/test_governance_streaming.py"
      provides: "Governance streaming tests with correct CanvasAudit schema"
      excludes: "agent_execution_id, component_type"
  key_links:
    - from: "test assertions"
      to: "CanvasAudit model"
      via: "current schema only"
      pattern: "assert audit\.canvas_type"
---

<objective>
Fix CanvasAudit schema drift in test assertions by removing references to deleted fields.

Purpose: Fix 2 failing governance streaming tests that reference removed CanvasAudit fields.
Output: Updated test assertions matching current CanvasAudit schema.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-fix-test-collection-errors/199-RESEARCH.md
@backend/core/models.py (CanvasAudit model)
@backend/tests/unit/governance/test_governance_streaming.py
@.planning/phases/198-coverage-push-85/198-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Identify CanvasAudit Schema Drift Issues</name>
  <files>backend/core/models.py, backend/tests/</files>
  <action>
    1. Read current CanvasAudit model schema from backend/core/models.py
    2. Current CanvasAudit fields (from Phase 198):
       - id
       - agent_id
       - canvas_type
       - content
       - metadata
       - timestamp
    3. Removed fields (no longer exist):
       - agent_execution_id (REMOVED)
       - component_type (REMOVED)
    4. Search for tests referencing removed fields:
       grep -r "agent_execution_id" backend/tests/
       grep -r "component_type" backend/tests/
    5. Document all test files that need fixes
  </action>
  <verify>
    Run: grep -r "agent_execution_id\|component_type" backend/tests/ 2>&1 | wc -l
    Expected: Count of references to fix (should be small, 2-5)
  </verify>
  <done>
    Current CanvasAudit schema documented
    Removed fields identified (agent_execution_id, component_type)
    Test files with drift references catalogued
  </done>
</task>

<task type="auto">
  <name>Update CanvasAudit Test Assertions</name>
  <files>backend/tests/unit/governance/test_governance_streaming.py</files>
  <action>
    For each test assertion referencing removed CanvasAudit fields:

    1. Remove agent_execution_id assertions:
       OLD: assert audit.agent_execution_id is not None
       NEW: (delete this assertion - field no longer exists)

    2. Remove component_type assertions:
       OLD: assert audit.component_type == "chart"
       NEW: (delete this assertion - field no longer exists)

    3. Keep valid assertions for current schema:
       - assert audit.canvas_type == "line_chart"
       - assert audit.content == {"data": [1, 2, 3]}
       - assert audit.agent_id == agent.id

    4. For governance streaming tests with canvas presentation:
       - Verify canvas_type matches requested type
       - Verify content matches presented data
       - Verify agent_id is set correctly
       - Do NOT verify agent_execution_id or component_type

    Use Edit tool to remove invalid assertions from test files.
    Ensure test logic still validates canvas presentation behavior.
  </action>
  <verify>
    Run: grep -r "agent_execution_id\|component_type" backend/tests/ 2>&1 | grep -v "# old\|# TODO\|# removed"
    Expected: 0 results (all invalid assertions removed)
  </verify>
  <done>
    All agent_execution_id assertions removed
    All component_type assertions removed
    Valid CanvasAudit assertions preserved (canvas_type, content, agent_id)
    Test logic validates canvas presentation behavior
  </done>
</task>

<task type="auto">
  <name>Verify CanvasAudit Test Fixes</name>
  <files>backend/tests/unit/governance/test_governance_streaming.py</files>
  <action>
    1. Run affected tests to verify they pass:
       pytest backend/tests/unit/governance/test_governance_streaming.py -v
    2. Check for AttributeError on removed fields
    3. Verify tests still validate canvas presentation:
       - CanvasAudit records created
       - canvas_type matches request
       - content matches presented data
    4. Ensure no new failures introduced by schema fixes
  </action>
  <verify>
    Run: pytest backend/tests/unit/governance/test_governance_streaming.py -v 2>&1 | grep -E "PASSED|FAILED|ERROR"
    Expected: All tests PASSED, no AttributeError on CanvasAudit fields
  </verify>
  <done>
    Governance streaming tests pass without AttributeError
    CanvasAudit assertions use current schema only
    0 references to removed fields
    Test coverage of canvas presentation preserved
  </done>
</task>

</tasks>

<verification>
Run: grep -r "agent_execution_id\|component_type" backend/tests/ 2>&1 | wc -l
Expected: 0 (all invalid references removed)

Run: pytest backend/tests/unit/governance/test_governance_streaming.py -v 2>&1 | tail -5
Expected: "X passed in Y.YYs" (all tests passing)
</verification>

<success_criteria>
- CanvasAudit schema drift fixed in test assertions
- Removed fields (agent_execution_id, component_type) no longer referenced
- Tests pass with current schema
- 0 CanvasAudit-related AttributeError failures
</success_criteria>

<output>
After completion, create `.planning/phases/199-fix-test-collection-errors/199-03-SUMMARY.md`
</output>
