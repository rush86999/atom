---
phase: 199-fix-test-collection-errors
plan: 07
type: execute
wave: 3
depends_on: [199-04, 199-05]
files_modified: [backend/tests/core/test_trigger_interceptor_coverage.py]
autonomous: true

must_haves:
  truths:
    - "trigger_interceptor.py coverage reaches 85%+"
    - "Routing scenarios across maturity levels tested"
    - "Maturity transition edge cases covered"
    - "Trigger validation edge cases tested"
  artifacts:
    - path: "backend/tests/core/test_trigger_interceptor_coverage.py"
      provides: "Comprehensive trigger interceptor tests"
      contains: "8-12 tests covering routing, transitions, validation"
  key_links:
    - from: "test_trigger_interceptor_coverage.py"
      to: "core/trigger_interceptor.py"
      via: "pytest coverage"
      pattern: "test_.*routing|maturity.*transition|trigger.*validation"
---

<objective>
Increase trigger_interceptor.py coverage from 74% to 85%+ by targeting uncovered routing scenarios.

Purpose: Close coverage gap on high-impact interceptor module using data-driven targeting.
Output: 8-12 new tests achieving 85%+ coverage on trigger_interceptor.py.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-fix-test-collection-errors/199-RESEARCH.md
@.planning/phases/199-fix-test-collection-errors/199-05-TARGETS.md
@backend/core/trigger_interceptor.py
@backend/tests/core/
@.planning/phases/198-coverage-push-85/198-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Analyze Uncovered Lines in trigger_interceptor.py</name>
  <files>backend/core/trigger_interceptor.py, backend/tests/coverage_reports/html/index.html</files>
  <action>
    1. Read HTML coverage report for trigger_interceptor.py

    2. Identify uncovered lines by function:
       - Which routing scenarios are untested?
       - Which maturity transitions lack coverage?
       - Which trigger validation paths are missing?

    3. Common interceptor edge cases to target:
       - Routing decisions for all 4 maturity levels
       - Maturity level transitions (STUDENT→INTERN, INTERN→SUPERVISED, etc.)
       - Trigger priority handling
       - Blocked trigger contexts
       - Trigger validation edge cases
       - Error paths (invalid triggers, missing agents)

    4. Create test plan based on actual uncovered lines:
       - List specific functions needing coverage
       - Identify routing scenarios for each function
       - Prioritize by lines covered per test
  </action>
  <verify>
    Run: open backend/tests/coverage_reports/html/index.html and navigate to trigger_interceptor.py
    Expected: HTML report shows uncovered lines in red
  </verify>
  <done>
    Uncovered lines identified by function
    Routing scenarios catalogued
    Maturity transition cases identified
    Test plan prioritized by impact
  </done>
</task>

<task type="auto">
  <name>Create Trigger Interceptor Tests</name>
  <files>backend/tests/core/test_trigger_interceptor_coverage.py</files>
  <action>
    Create test file: backend/tests/core/test_trigger_interceptor_coverage.py

    Include tests for identified uncovered lines:

    1. Maturity Routing Tests (3-4 tests):
       - test_student_trigger_blocked_routed_to_training
       - test_intern_trigger_requires_approval
       - test_supervised_trigger_allows_with_monitoring
       - test_autonomous_trigger_full_execution

    2. Maturity Transition Tests (2-3 tests):
       - test_routing_during_student_to_intern_transition
       - test_routing_during_intern_to_supervised_promotion
       - test_routing_after_supervised_to_autonomous_graduation

    3. Trigger Priority Tests (2 tests):
       - test_high_priority_trigger_bypasses_queue
       - test_low_priority_trigger_deferred_during_load

    4. Validation Edge Cases (2-3 tests):
       - test_trigger_with_invalid_agent_id
       - test_trigger_with_missing_action_complexity
       - test_trigger_validation_with_malformed_payload

    Use existing test patterns from governance tests
    Follow Pydantic v2 patterns from Plan 02
    Mock governance cache and training service appropriately
  </action>
  <verify>
    Run: python3 -m pytest backend/tests/core/test_trigger_interceptor_coverage.py -v
    Expected: 8-12 tests collected, all passing
  </verify>
  <done>
    8-12 trigger interceptor tests created
    Maturity routing tests included (3-4)
    Maturity transition tests included (2-3)
    Trigger priority tests included (2)
    Validation edge cases included (2-3)
  </done>
</task>

<task type="auto">
  <name>Verify trigger_interceptor.py Coverage</name>
  <files>backend/tests/core/test_trigger_interceptor_coverage.py</files>
  <action>
    1. Run tests with coverage for trigger_interceptor:
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/core/test_trigger_interceptor_coverage.py --cov=core.trigger_interceptor --cov-branch --cov-report=term-missing -v

    2. Verify coverage percentage:
       - Target: 85%+ coverage
       - If <85%, add more tests for remaining uncovered lines

    3. Run all interceptor tests together (if existing):
       pytest backend/tests/core/test_trigger_interceptor*.py -v

    4. Verify no regressions in existing tests

    5. If coverage target not met, iterate with additional tests
  </action>
  <verify>
    Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/core/test_trigger_interceptor_coverage.py --cov=core.trigger_interceptor --cov-branch --cov-report=term-missing -q
    Expected: "85%+ coverage" for trigger_interceptor
  </verify>
  <done>
    trigger_interceptor.py coverage 85%+
    New tests passing without regressions
    All interceptor tests passing together
    Uncovered lines reduced to <15%
    Coverage target achieved
  </done>
</task>

</tasks>

<verification>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/core/test_trigger_interceptor_coverage.py --cov=core.trigger_interceptor --cov-branch -q
Expected: "85.XX% coverage" (85% or higher)

Run: python3 -m pytest backend/tests/core/test_trigger_interceptor*.py -q
Expected: All tests passing
</verification>

<success_criteria>
- trigger_interceptor.py coverage increased to 85%+ (from 74%)
- 8-12 new tests covering routing scenarios
- All maturity levels tested in routing paths
- Maturity transition edge cases covered
- Trigger priority handling tested
- Validation edge cases covered
</success_criteria>

<output>
After completion, create `.planning/phases/199-fix-test-collection-errors/199-07-SUMMARY.md`
</output>
