---
phase: 199-fix-test-collection-errors
plan: 02
type: execute
wave: 1
depends_on: [199-01]
files_modified: [backend/tests/api/test_api_routes_coverage.py, backend/tests/api/test_feedback_analytics.py, backend/tests/api/test_feedback_enhanced.py, backend/tests/core/test_agent_governance_service_coverage_extend.py, backend/tests/core/test_agent_graduation_service_coverage.py]
autonomous: true

must_haves:
  truths:
    - "Pydantic v2 patterns used in all test fixtures"
    - "SQLAlchemy 2.0 query patterns used in all tests"
    - "Tests collect without AttributeError"
    - "Deprecated parse_obj() replaced with model_validate()"
  artifacts:
    - path: "backend/tests/api/test_api_routes_coverage.py"
      provides: "API routes coverage tests with Pydantic v2"
      contains: "model_validate"
    - path: "backend/tests/core/test_agent_governance_service_coverage_extend.py"
      provides: "Governance service tests with SQLAlchemy 2.0"
      contains: "select("
  key_links:
    - from: "test fixtures"
      to: "Pydantic models"
      via: "model_validate() method"
      pattern: "\.model_validate\("
    - from: "test queries"
      to: "SQLAlchemy session"
      via: "select() statements"
      pattern: "session\.execute\(select\("
---

<objective>
Migrate test fixtures from Pydantic v1 to v2 patterns and SQLAlchemy 1.4 to 2.0 query syntax.

Purpose: Fix AttributeError and TypeError in test collection caused by deprecated patterns.
Output: Updated test files with v2/v2.0 patterns, tests collect successfully.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-fix-test-collection-errors/199-RESEARCH.md
@backend/tests/api/test_api_routes_coverage.py
@backend/tests/api/test_feedback_analytics.py
@backend/tests/api/test_feedback_enhanced.py
@backend/tests/core/test_agent_governance_service_coverage_extend.py
@backend/tests/core/test_agent_graduation_service_coverage.py
@.planning/phases/198-coverage-push-85/198-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Search for Deprecated Pydantic v1 Patterns</name>
  <files>backend/tests/</files>
  <action>
    1. Search for deprecated Pydantic v1 patterns in test files:
       grep -r "\.parse_obj(" backend/tests/
       grep -r "\.update({" backend/tests/
       grep -r "\.dict()" backend/tests/
       grep -r "\.json()" backend/tests/
    2. Document all files that need migration
    3. Focus on these known problematic files from Phase 198:
       - tests/api/test_api_routes_coverage.py
       - tests/api/test_feedback_analytics.py
       - tests/api/test_feedback_enhanced.py
       - tests/core/test_agent_governance_service_coverage_extend.py
       - tests/core/test_agent_graduation_service_coverage.py
  </action>
  <verify>
    Run: grep -r "\.parse_obj(" backend/tests/ 2>&1 | wc -l
    Expected: 0 (after migration)
  </verify>
  <done>
    All Pydantic v1 patterns identified
    Files needing migration documented
    grep search results saved for reference
  </done>
</task>

<task type="auto">
  <name>Replace Pydantic v1 with v2 Patterns</name>
  <files>backend/tests/api/test_api_routes_coverage.py, backend/tests/api/test_feedback_analytics.py, backend/tests/api/test_feedback_enhanced.py, backend/tests/core/test_agent_governance_service_coverage_extend.py, backend/tests/core/test_agent_graduation_service_coverage.py</files>
  <action>
    For each identified file, replace Pydantic v1 patterns with v2:

    1. parse_obj() → model_validate():
       OLD: agent_data = AgentSchema.parse_obj(db_agent.__dict__)
       NEW: agent_data = AgentSchema.model_validate(db_agent)

    2. .update() → .model_copy(update=...):
       OLD: agent_copy = agent_data.update({"status": "active"})
       NEW: agent_copy = agent_data.model_copy(update={"status": "active"})

    3. .dict() → .model_dump():
       OLD: agent_dict = agent_data.dict()
       NEW: agent_dict = agent_data.model_dump()

    4. .json() → .model_dump_json():
       OLD: agent_json = agent_data.json()
       NEW: agent_json = agent_data.model_dump_json()

    Use sed or Edit tool to replace these patterns across all test files.
    Verify each replacement maintains test logic.
  </action>
  <verify>
    Run: grep -r "\.parse_obj\|\.update({\|\.dict()\|\.json()" backend/tests/ 2>&1 | grep -v "# old\|# TODO\|# deprecated"
    Expected: 0 results (all v1 patterns replaced)
  </verify>
  <done>
    All parse_obj() replaced with model_validate()
    All .update() replaced with .model_copy(update=...)
    All .dict() replaced with .model_dump()
    All .json() replaced with .model_dump_json()
  </done>
</task>

<task type="auto">
  <name>Replace SQLAlchemy 1.4 with 2.0 Query Patterns</name>
  <files>backend/tests/core/test_agent_governance_service_coverage_extend.py, backend/tests/core/test_agent_graduation_service_coverage.py</files>
  <action>
    For test files that use SQLAlchemy, replace 1.4 query syntax with 2.0:

    1. session.query(Model) → session.execute(select(Model)):
       OLD: agents = session.query(Agent).filter(Agent.status == "active").all()
       NEW: from sqlalchemy import select
            stmt = select(Agent).where(Agent.status == "active")
            agents = session.execute(stmt).scalars().all()

    2. .filter() → .where():
       OLD: session.query(Model).filter(Model.field == value)
       NEW: select(Model).where(Model.field == value)

    3. .first() → .scalar_one_or_none():
       OLD: agent = session.query(Agent).filter(Agent.id == agent_id).first()
       NEW: stmt = select(Agent).where(Agent.id == agent_id)
            agent = session.execute(stmt).scalar_one_or_none()

    4. .all() → .scalars().all():
       OLD: agents = session.query(Agent).all()
       NEW: agents = session.execute(select(Agent)).scalars().all()

    Update imports to include: from sqlalchemy import select
    Use Edit tool to replace these patterns in affected test files.
  </action>
  <verify>
    Run: grep -r "session\.query(" backend/tests/ 2>&1 | grep -v "# old\|# TODO\|# legacy"
    Expected: 0 results (all 1.4 patterns replaced)
  </verify>
  <done>
    All session.query() replaced with session.execute(select())
    All .filter() replaced with .where()
    All .first() replaced with .scalar_one_or_none()
    All .all() replaced with .scalars().all()
    sqlalchemy.select imported where needed
  </done>
</task>

</tasks>

<verification>
Run: python3 -m pytest --collect-only backend/tests/api/test_api_routes_coverage.py backend/tests/api/test_feedback_analytics.py backend/tests/api/test_feedback_enhanced.py backend/tests/core/test_agent_governance_service_coverage_extend.py backend/tests/core/test_agent_graduation_service_coverage.py -q
Expected: "XX tests collected, 0 errors" (no collection errors for these files)

Run: grep -r "\.parse_obj\|session\.query(" backend/tests/ 2>&1 | wc -l
Expected: 0 (all deprecated patterns replaced)
</verification>

<success_criteria>
- Pydantic v1 patterns (parse_obj, update, dict, json) replaced with v2 equivalents
- SQLAlchemy 1.4 patterns (session.query, filter, first) replaced with 2.0 equivalents
- Test files collect without AttributeError or TypeError
- 0 deprecated patterns remain in test files
</success_criteria>

<output>
After completion, create `.planning/phases/199-fix-test-collection-errors/199-02-SUMMARY.md`
</output>
