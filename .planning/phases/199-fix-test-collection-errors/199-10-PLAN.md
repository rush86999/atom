---
phase: 199-fix-test-collection-errors
plan: 10
type: execute
wave: 4
depends_on: [199-06, 199-07, 199-08]
files_modified: [backend/tests/e2e/test_training_supervision_integration.py]
autonomous: true

must_haves:
  truths:
    - "Training to supervision workflow tested"
    - "Supervision session lifecycle validated"
    - "Graduation criteria integration verified"
    - "Intervention to training extension flow tested"
  artifacts:
    - path: "backend/tests/e2e/test_training_supervision_integration.py"
      provides: "E2E tests for training and supervision integration"
      contains: "4-6 tests covering training → supervision → graduation pipeline"
  key_links:
    - from: "test_training_supervision_integration.py"
      to: "core/student_training_service.py, core/supervision_service.py, core/agent_graduation_service.py"
      via: "E2E integration test"
      pattern: "test_.*training.*supervision|supervision.*graduation|training.*workflow"
---

<objective>
Create E2E integration tests for training → supervision → graduation workflow.

Purpose: Validate the complete agent development pipeline from training through supervision to graduation.
Output: 4-6 E2E tests validating training, supervision, and graduation integration.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-fix-test-collection-errors/199-RESEARCH.md
@backend/core/student_training_service.py
@backend/core/supervision_service.py
@backend/core/agent_graduation_service.py
@backend/tests/e2e/conftest_e2e.py
@.planning/phases/198-coverage-push-85/198-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create Training Supervision Integration Test File</name>
  <files>backend/tests/e2e/test_training_supervision_integration.py</files>
  <action>
    Create test file: backend/tests/e2e/test_training_supervision_integration.py

    Import required fixtures from conftest_e2e.py:
    - test_app (FastAPI test client)
    - db_session (database session)
    - mock_llm_response (deterministic LLM mocking)
    - test_agent_* fixtures for all maturity levels

    Set up E2E test structure for training → supervision workflow
  </action>
  <verify>
    Run: python3 -m pytest backend/tests/e2e/test_training_supervision_integration.py --collect-only
    Expected: Test file collects successfully
  </verify>
  <done>
    E2E integration test file created
    Required fixtures imported
    Test structure for training workflow established
    File collects without errors
  </done>
</task>

<task type="auto">
  <name>Create Training Supervision Workflow Tests</name>
  <files>backend/tests/e2e/test_training_supervision_integration.py</files>
  <action>
    Add E2E tests for training → supervision → graduation:

    1. Training Session Workflow (2 tests):
       - test_student_agent_creates_training_session
         - Create STUDENT agent
         - Trigger automated action (should be blocked)
         - Verify training session created
         - Verify training duration estimated

       - test_training_completion_allows_supervision_promotion
         - Create STUDENT agent
         - Complete training session
         - Verify agent promoted to INTERN maturity
         - Verify trigger routing changed

    2. Supervision Session Workflow (2 tests):
       - test_supervised_agent_creates_supervision_session
         - Create SUPERVISED agent
         - Execute action requiring monitoring
         - Verify supervision session created
         - Verify real-time monitoring active

       - test_supervision_intervention_extends_training
         - Create SUPERVISED agent
         - Execute action with intervention
         - Verify supervision failure recorded
         - Verify training extended due to intervention

    3. Graduation Integration (1-2 tests):
       - test_supervision_success_allows_graduation_exam
         - Create SUPERVISED agent with successful supervision
         - Verify graduation exam eligibility
         - Verify graduation criteria met

       - test_graduation_success_promotes_to_autonomous
         - Create SUPERVISED agent meeting all criteria
         - Execute graduation exam
         - Verify agent promoted to AUTONOMOUS
         - Verify trigger routing bypasses supervision

    Use existing E2E test patterns from Phase 198 Plans 04, 06
    Follow Pydantic v2 patterns from Plan 02
    Mock external dependencies appropriately
  </action>
  <verify>
    Run: python3 -m pytest backend/tests/e2e/test_training_supervision_integration.py -v
    Expected: 5-7 tests collected, all passing
  </verify>
  <done>
    5-7 E2E integration tests created
    Training session workflow tests (2)
    Supervision session workflow tests (2)
    Graduation integration tests (1-2)
    Training → supervision → graduation flow validated
  </done>
</task>

<task type="auto">
  <name>Verify Training Supervision Integration Tests</name>
  <files>backend/tests/e2e/test_training_supervision_integration.py</files>
  <action>
    1. Run E2E tests with full stack:
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/e2e/test_training_supervision_integration.py -v

    2. Verify each test validates:
       - Training session creation and completion
       - Supervision session lifecycle
       - Graduation criteria and promotion
       - Integration between services
       - No errors in workflow

    3. Run with coverage to measure integration path coverage:
       pytest backend/tests/e2e/test_training_supervision_integration.py --cov=core.student_training_service --cov=core.supervision_service --cov=core.agent_graduation_service --cov-report=term-missing -q

    4. Verify no regressions in existing E2E tests

    5. Check for flaky tests (run tests twice)
  </action>
  <verify>
    Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/e2e/test_training_supervision_integration.py -v
    Expected: All 5-7 tests passing, no errors
  </verify>
  <done>
    E2E tests passing consistently
    Training → supervision workflow validated
    Graduation integration verified
    Coverage measured for integration paths
    No regressions in existing E2E tests
    No flaky tests detected
  </done>
</task>

</tasks>

<verification>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/e2e/test_training_supervision_integration.py -v
Expected: 5-7 tests passing

Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/e2e/test_training_supervision_integration.py --cov=core.student_training_service --cov=core.supervision_service --cov=core.agent_graduation_service --cov-branch -q
Expected: Coverage increased for integration paths
</verification>

<success_criteria>
- 4-6 E2E integration tests created
- Training session lifecycle validated
- Supervision session lifecycle validated
- Graduation integration verified
- Training → supervision → graduation workflow tested
- Tests pass consistently without flakiness
- Integration path coverage increased
</success_criteria>

<output>
After completion, create `.planning/phases/199-fix-test-collection-errors/199-10-SUMMARY.md`
</output>
