---
phase: 199-fix-test-collection-errors
plan: 11
type: execute
wave: 5
depends_on: [199-01, 199-02, 199-03, 199-06, 199-07, 199-08, 199-09, 199-10]
files_modified: [backend/tests/coverage_reports/final_coverage.json]
autonomous: true

must_haves:
  truths:
    - "Overall backend coverage reaches 85%+"
    - "Full test suite runs without collection errors"
    - "Coverage report generated successfully"
    - "Phase 199 target achieved"
  artifacts:
    - path: "backend/tests/coverage_reports/final_coverage.json"
      provides: "Final coverage report for Phase 199"
      contains: "85%+ overall coverage"
    - path: "backend/tests/coverage_reports/html/index.html"
      provides: "HTML coverage report"
      contains: "final coverage visualization"
  key_links:
    - from: "pytest --cov"
      to: "final_coverage.json"
      via: "pytest-cov plugin"
      pattern: "--cov-report=json"
---

<objective>
Generate final coverage report and verify 85% overall coverage target achieved.

Purpose: Validate Phase 199 success and document coverage achievements.
Output: Final coverage report (JSON + HTML) showing 85%+ overall coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/199-fix-test-collection-errors/199-RESEARCH.md
@.planning/phases/199-fix-test-collection-errors/199-04-SUMMARY.md
@backend/pytest.ini
@.planning/phases/198-coverage-push-85/198-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Generate Final Coverage Report</name>
  <files>backend/tests/</files>
  <action>
    1. From backend directory, run pytest with full coverage:
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest --cov=core --cov=api --cov=tools --cov-branch --cov-report=json --cov-report=html --cov-report=term-missing -q

    2. Capture overall coverage percentage from terminal output

    3. Verify coverage report generated:
       - backend/coverage.json (or backend/tests/coverage_reports/coverage.json)
       - backend/htmlcov/index.html (or backend/tests/coverage_reports/html/index.html)

    4. Compare to Phase 198 baseline (74.6%)
    5. Verify 85% target achieved or calculate remaining gap

    Note: Overall coverage should be significantly higher than Phase 198 because:
    - Collection errors fixed (150+ tests now collected)
    - Pydantic v2/SQLAlchemy 2.0 migration complete
    - CanvasAudit schema drift fixed
    - New tests from Waves 3-4 included
  </action>
  <verify>
    Run: python3 -c "import json; data = json.load(open('backend/coverage.json')); print(f\"Overall: {data['totals']['percent_covered']:.1f}%\")" 2>/dev/null || python3 -c "import json; data = json.load(open('backend/tests/coverage_reports/coverage.json')); print(f\"Overall: {data['totals']['percent_covered']:.1f}%\")"
    Expected: "Overall: 85.X%" (85% or higher)
  </verify>
  <done>
    Final coverage report generated (JSON + HTML)
    Overall coverage percentage captured
    Comparison to Phase 198 documented
    85% target verified (or gap calculated)
  </done>
</task>

<task type="auto">
  <name>Verify Collection Errors Fixed</name>
  <files>backend/pytest.ini</files>
  <action>
    1. Run pytest collection to verify 0 errors:
       python3 -m pytest --collect-only -q

    2. Check output for collection errors:
       - Should show "XX tests collected, 0 errors"
       - No "ERROR collecting" lines
       - No ModuleNotFoundError for archive or non-backend tests

    3. Verify pytest.ini ignore patterns working:
       - archive/ tests excluded
       - frontend-nextjs/ tests excluded
       - scripts/ tests excluded

    4. Count total tests collected
    5. Compare to Phase 198 test count (should be higher with 150+ tests unblocked)
  </action>
  <verify>
    Run: python3 -m pytest --collect-only -q 2>&1 | tail -2
    Expected: "XX tests collected, 0 errors" (no errors)
  </verify>
  <done>
    0 collection errors verified
    All tests collected successfully
    Archive and non-backend tests excluded
    Test count increased from Phase 198
  </done>
</task>

<task type="auto">
  <name>Analyze Final Coverage by Module</name>
  <files>backend/coverage.json or backend/tests/coverage_reports/coverage.json</files>
  <action>
    1. Read coverage.json to extract module-level results

    2. Document coverage for key modules:
       | Module | Phase 198 | Phase 199 | Target | Status |
       |--------|-----------|-----------|--------|--------|
       | agent_governance_service | 62% | XX% | 85% | |
       | trigger_interceptor | 74% | XX% | 85% | |
       | episode_segmentation_service | 83.8% | XX% | 85% | |
       | agent_graduation_service | 73.8% | XX% | 85% | |
       | student_training_service | Blocked | XX% | 75% | |
       | supervision_service | 78% | XX% | 80% | |
       | governance_cache | 90%+ | XX% | 90% | |

    3. Calculate overall coverage improvement:
       - Phase 198: 74.6%
       - Phase 199: XX%
       - Improvement: +XX%

    4. Identify any remaining gaps below 85%

    5. Document test count:
       - Phase 198: ~5,700 tests (with collection errors)
       - Phase 199: ~5,900+ tests (clean collection)
  </action>
  <verify>
    Run: cat .planning/phases/199-fix-test-collection-errors/199-11-SUMMARY.md | grep -E "Phase 198|Phase 199|Improvement"
    Expected: Module-level comparison documented
  </verify>
  <done>
    Module-level coverage extracted
    Comparison to Phase 198 documented
    Overall improvement calculated
    Remaining gaps identified (if any)
    Test count increase documented
  </done>
</task>

</tasks>

<verification>
Run: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest --cov=core --cov=api --cov=tools --cov-branch --cov-report=term-missing -q 2>&1 | grep "coverage:"
Expected: "85.XX% coverage" (85% or higher)

Run: python3 -m pytest --collect-only -q 2>&1 | tail -2
Expected: "XX tests collected, 0 errors"
</verification>

<success_criteria>
- Overall backend coverage reaches 85%+ (from 74.6%)
- 0 collection errors (from 10+ in Phase 198)
- Coverage report generated (JSON + HTML)
- Module-level coverage improvements documented
- Test count increased (150+ tests unblocked)
- Phase 199 target achieved
</success_criteria>

<output>
After completion, create `.planning/phases/199-fix-test-collection-errors/199-11-SUMMARY.md`
</output>
