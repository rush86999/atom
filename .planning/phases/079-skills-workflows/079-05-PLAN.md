---
phase: 079-skills-workflows
plan: 05
type: execute
wave: 2
depends_on: ["079-02"]
files_modified:
  - backend/tests/e2e_ui/pages/page_objects.py
autonomous: true

must_haves:
  truths:
    - "User can uninstall skill and it's removed from installed list"
    - "Uninstall shows confirmation dialog before removal"
    - "Uninstall button shows loading state during uninstall"
    - "Skill configuration is removed when skill is uninstalled"
    - "Uninstalled skill can be reinstalled from marketplace"
    - "Uninstall blocks if skill has active executions"
    - "Execution history is preserved after uninstall"
  artifacts:
    - path: "backend/tests/e2e_ui/pages/page_objects.py"
      provides: "Extend SkillInstallationPage with uninstallation locators and methods"
      min_lines: 50  # Additional methods to existing class
      contains: "uninstall_skill|confirm_uninstall"
    - path: "backend/tests/e2e_ui/tests/test_skills_uninstallation.py"
      provides: "E2E tests for skill uninstallation workflow with cleanup verification"
      min_lines: 150
      contains: "test_skill_uninstall_from_installed_list"
  key_links:
    - from: "test_skill_uninstall_from_installed_list"
      to: "DELETE /api/skills/{skill_id} endpoint or uninstall handler"
      via: "DELETE request or uninstall action"
      pattern: "uninstall|delete.*skill"
    - from: "SkillInstallationPage.confirm_uninstall()"
      to: "confirmation dialog"
      via: "modal or alert"
      pattern: "confirm.*uninstall"
    - from: "test_uninstall_blocks_active_executions"
      to: "execution status check"
      via: "SkillExecution status query"
      pattern: "active.*execution.*block"
---

<objective>
Create Skill Uninstallation E2E tests with confirmation dialog, cleanup verification, and reinstallation validation.

Purpose: Enable testing of skill uninstallation workflow including confirmation, cleanup, blocking conditions, and reinstallation
Output: Extended SkillInstallationPage with uninstall methods and skill uninstallation tests
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/079-skills-workflows/079-02-PLAN.md
@backend/api/skill_routes.py
@backend/core/skill_registry_service.py
@backend/tests/e2e_ui/pages/page_objects.py

# Skill Uninstallation Context
- Uninstall endpoint: DELETE /api/skills/{skill_id} (may be 501 currently, implement for tests)
- Uninstall confirmation: Modal dialog with skill name, warning message
- Uninstall button states: "Uninstall" -> "Uninstalling..." -> Removed from list
- Blocking conditions: Active executions (running/pending), skill dependencies
- Cleanup actions: Remove config, mark as uninstalled, clean execution history (archive)
- Reinstall: Same flow as initial installation, should succeed after uninstall
- Execution history: Preserved but marked as uninstalled (soft delete)
</context>

<tasks>

<task type="auto">
  <name>Extend SkillInstallationPage with uninstallation methods</name>
  <files>backend/tests/e2e_ui/pages/page_objects.py</files>
  <action>
    Add uninstallation locators and methods to SkillInstallationPage class in page_objects.py:

    1. Additional Locators (CSS selectors):
       - uninstall_button - Uninstall button on installed skill card
       - uninstall_button_loading - Loading state of uninstall button
       - uninstall_confirmation_modal - Uninstall confirmation dialog
       - uninstall_skill_name - Skill name in confirmation dialog
       - uninstall_warning_message - Warning message about data loss
       - confirm_uninstall_button - Confirm uninstall button
       - cancel_uninstall_button - Cancel uninstall button
       - uninstall_success_message - Success message after uninstall
       - uninstall_error_message - Error message if uninstall fails
       - active_execution_warning - Warning about active executions blocking uninstall

    2. Additional Methods:
       - click_uninstall(skill_name: str) -> None - Click uninstall button for skill
       - is_uninstalling() -> bool - Check if uninstall in progress
       - is_confirmation_dialog_visible() -> bool - Check if confirmation modal shown
       - get_confirmation_skill_name() -> str - Get skill name from confirmation
       - get_confirmation_warning() -> str - Get warning message text
       - confirm_uninstall() -> None - Confirm uninstall in dialog
       - cancel_uninstall() -> None - Cancel uninstall in dialog
       - wait_for_uninstall_complete(timeout: int = 5000) -> None - Wait for uninstall to finish
       - is_skill_uninstalled(skill_name: str) -> bool - Check if skill removed from list
       - get_uninstall_error() -> str - Get uninstall error message
       - has_active_execution_warning() -> bool - Check if active execution warning shown

    Follow existing SkillInstallationPage pattern
    Extend the class created in plan 079-02
    Include docstrings with Google-style Args/Returns
    Handle both modal and inline confirmation flows
  </action>
  <verify>
    # Check file has uninstallation methods
    grep -c "uninstall_button\|confirm_uninstall" backend/tests/e2e_ui/pages/page_objects.py
  </verify>
  <done>
    SkillInstallationPage extended with 50+ lines including uninstall locators and methods
  </done>
</task>

<task type="auto">
  <name>Create skill uninstallation E2E tests</name>
  <files>backend/tests/e2e_ui/tests/test_skills_uninstallation.py</files>
  <action>
    Create test_skills_uninstallation.py with:

    1. Helper functions:
       - install_skill_for_uninstall_test(db_session, user) -> str
         - Install a test skill
         - Return skill_id
       - create_skill_with_active_execution(db_session, skill_id: str) -> str
         - Create skill with running execution
         - Return execution_id

    2. Test cases:
       - test_skill_uninstall_from_installed_list():
         - Install skill
         - Navigate to installed skills list
         - Click uninstall button
         - Verify confirmation dialog appears
         - Verify skill name in confirmation
         - Confirm uninstall
         - Wait for completion
         - Verify skill removed from list
         - Verify success message shown

       - test_uninstall_confirmation_dialog():
         - Click uninstall
         - Verify dialog title is correct
         - Verify warning message about data loss
         - Verify skill name displayed
         - Click cancel
         - Verify dialog closes
         - Verify skill still installed

       - test_uninstall_button_states():
         - Click uninstall
         - Verify initial "Uninstall" text
         - Confirm uninstall
         - Verify "Uninstalling..." loading state
         - Wait for completion
         - Verify button disappears (skill removed)

       - test_uninstall_removes_configuration():
         - Install skill
         - Configure skill with API keys and options
         - Uninstall skill
         - Reinstall same skill
         - Verify configuration is empty (not persisted)
         - Verify fresh config state

       - test_uninstalled_skill_can_reinstall():
         - Install skill
         - Uninstall skill
         - Navigate to marketplace
         - Find same skill
         - Install again
         - Verify installation succeeds
         - Verify skill in installed list

       - test_uninstall_blocks_active_executions():
         - Install skill
         - Create active execution (running/pending)
         - Attempt uninstall
         - Verify active execution warning shown
         - Verify uninstall blocked
         - Wait for execution to complete
         - Retry uninstall
         - Verify succeeds

       - test_uninstall_preserves_execution_history():
         - Install and execute skill multiple times
         - Get execution history count
         - Uninstall skill
         - Query database for execution records
         - Verify records still exist (soft delete)
         - Verify skill marked as uninstalled

       - test_uninstall_multiple_skills():
         - Install multiple skills
         - Uninstall skills one by one
         - Verify each removal independent
         - Verify only selected skill removed

       - test_uninstall_error_handling():
         - Mock uninstall failure (server error)
         - Click uninstall and confirm
         - Verify error message shown
         - Verify skill still installed
         - Verify retry possible

       - test_uninstall_from_marketplace():
         - Install skill
         - Navigate to marketplace
         - Find installed skill
         - Verify button shows "Installed" or "Uninstall"
         - Click uninstall option
         - Confirm and verify removal

       - test_uninstall_last_skill():
         - Install single skill
         - Uninstall it
         - Verify empty state shown
         - Verify helpful message to browse marketplace

       - test_uninstall_confirmation_message_accuracy():
         - Create skill with dependencies/usage
         - Attempt uninstall
         - Verify warning mentions specific risks
         - Verify message is clear and actionable

    Use authenticated_page fixture from auth_fixtures.py
    Use db_session for database verification
    Create test skills with unique IDs (UUID v4)
    Test both soft delete (preserve history) and hard delete scenarios
  </action>
  <verify>
    # Check test file exists with tests
    grep -c "def test_.*uninstall" backend/tests/e2e_ui/tests/test_skills_uninstallation.py
    grep -c "SkillInstallationPage" backend/tests/e2e_ui/tests/test_skills_uninstallation.py
  </verify>
  <done>
    test_skills_uninstallation.py exists with 12+ test cases covering SKILL-05 requirements
  </done>
</task>

</tasks>

<verification>
1. SkillInstallationPage extended with uninstall methods
2. All locators handle uninstall components correctly
3. Test file includes confirmation dialog tests
4. Tests verify cleanup (config removal)
5. Tests verify reinstallation works
6. Active execution blocking tests included
7. Execution history preservation verified
</verification>

<success_criteria>
- SkillInstallationPage extended with 50+ lines
- test_skills_uninstallation.py with 12+ test cases
- Tests cover: uninstall flow, confirmation, button states, config cleanup, reinstall, active blocking, history preservation, error handling, empty state
- Tests verify complete uninstallation lifecycle
</success_criteria>

<output>
After completion, create .planning/phases/079-skills-workflows/079-05-SUMMARY.md
</output>
