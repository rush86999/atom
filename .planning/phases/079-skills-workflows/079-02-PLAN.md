---
phase: 079-skills-workflows
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/e2e_ui/pages/page_objects.py
autonomous: true

must_haves:
  truths:
    - "User can install skill from marketplace and see it in installed skills list"
    - "Install button shows loading state during installation"
    - "Install button changes to 'Installed' after successful installation"
    - "Governance check blocks installation for restricted skills (STUDENT agent)"
    - "Security scan results are displayed before installation"
    - "Installation creates execution record in database"
    - "Installed skills list shows all installed skills with uninstall option"
  artifacts:
    - path: "backend/tests/e2e_ui/pages/page_objects.py"
      provides: "SkillInstallationPage Page Object with installation locators and methods"
      min_lines: 100
      contains: "class SkillInstallationPage"
    - path: "backend/tests/e2e_ui/tests/test_skills_installation.py"
      provides: "E2E tests for skill installation workflow with governance"
      min_lines: 150
      contains: "test_skill_install_from_marketplace"
  key_links:
    - from: "test_skill_install_from_marketplace"
      to: "/api/skills/install endpoint"
      via: "POST request"
      pattern: "skills.*install"
    - from: "SkillInstallationPage.install_skill()"
      to: "governance check"
      via: "agent maturity validation"
      pattern: "governance.*install"
    - from: "test_student_blocked_from_installation"
      to: "AgentGovernanceService"
      via: "maturity level check"
      pattern: "STUDENT.*blocked"
---

<objective>
Create Skill Installation Page Object and E2E tests for installing skills from marketplace with governance enforcement.

Purpose: Enable testing of skill installation workflow including button states, governance checks, security scan display, and database verification
Output: SkillInstallationPage Page Object and skill installation tests
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/078-canvas-presentations/078-01-SUMMARY.md
@backend/api/skill_routes.py
@backend/core/skill_marketplace_service.py
@backend/core/agent_governance_service.py
@backend/tests/e2e_ui/pages/page_objects.py

# Skill Installation Context
- Installation endpoint: POST /api/skills/install or install_skill() in SkillMarketplaceService
- Governance check: STUDENT agents blocked from Python skills (security risk)
- Security scan results displayed before install: {safe: bool, risk_level: str, findings: []}
- Install button states: "Install" (default) -> "Installing..." (loading) -> "Installed" (success)
- Installation creates SkillExecution record with status="installed"
- Installed skills accessible via separate list or filter on marketplace
- Agent maturity levels: STUDENT, INTERN, SUPERVISED, AUTONOMOUS
</context>

<tasks>

<task type="auto">
  <name>Create SkillInstallationPage Page Object class</name>
  <files>backend/tests/e2e_ui/pages/page_objects.py</files>
  <action>
    Add SkillInstallationPage class to page_objects.py with:

    1. Locators (CSS selectors):
       - install_button - Install button on skill card (or page)
       - install_button_loading - Loading state of install button
       - install_button_installed - Installed state button
       - installation_modal - Installation confirmation modal (if exists)
       - security_scan_banner - Security scan results display
       - security_scan_safe - Safe/unsafe indicator
       - security_scan_findings - List of security findings
       - confirm_install_button - Confirm install in modal
       - cancel_install_button - Cancel install in modal
       - installed_skills_list - List of installed skills
       - installed_skill_card - Individual installed skill card
       - uninstall_button - Uninstall button on installed skill
       - installation_success_message - Success toast/message
       - installation_error_message - Error message display

    2. Methods:
       - is_loaded() -> bool - Check if installation page/modal is visible
       - click_install_button() -> None - Click install button
       - is_installing() -> bool - Check if in loading state
       - is_installed() -> bool - Check if skill shows as installed
       - get_install_button_text() -> str - Get current button text
       - confirm_installation() -> None - Confirm install in modal
       - cancel_installation() -> None - Cancel install in modal
       - get_security_scan_result() -> dict - Get security scan info (safe, risk_level, findings)
       - is_security_scan_visible() -> bool - Check if security banner shown
       - wait_for_installation_complete(timeout: int = 10000) -> None - Wait for install to finish
       - get_installed_skills_count() -> int - Get count of installed skills
       - is_skill_installed(skill_name: str) -> bool - Check if skill in installed list
       - click_uninstall(skill_name: str) -> None - Click uninstall for specific skill
       - get_install_message() -> str - Get success/error message text

    Follow BasePage pattern from existing page_objects.py
    Handle both modal and inline installation flows
    Include docstrings with Google-style Args/Returns
    Support both marketplace install and standalone skill installation
  </action>
  <verify>
    # Check file has SkillInstallationPage class
    grep -c "class SkillInstallationPage" backend/tests/e2e_ui/pages/page_objects.py
    grep -c "install_button\|security_scan\|installed_skills" backend/tests/e2e_ui/pages/page_objects.py
  </verify>
  <done>
    SkillInstallationPage class exists with 100+ lines including all required locators and methods
  </done>
</task>

<task type="auto">
  <name>Create skill installation E2E tests</name>
  <files>backend/tests/e2e_ui/tests/test_skills_installation.py</files>
  <action>
    Create test_skills_installation.py with:

    1. Helper functions:
       - create_installable_skill(db_session, skill_data: dict) -> str
         - Create skill record with Active status
         - Return skill_id
       - setup_installation_page(browser, skill_id: str, user) -> Page
         - Navigate to skill installation page
         - Return initialized page

    2. Test cases:
       - test_skill_install_from_marketplace():
         - Navigate to marketplace
         - Find test skill card
         - Click install button
         - Wait for installation to complete
         - Verify button changes to "Installed"
         - Verify skill appears in installed list

       - test_install_button_states():
         - Verify initial "Install" button text
         - Click install
         - Verify "Installing..." loading state
         - Wait for completion
         - Verify "Installed" final state

       - test_installation_security_scan_display():
         - Create skill with security scan results
         - Navigate to installation
         - Verify security banner displays
         - Verify risk_level shown correctly
         - Verify findings list displayed

       - test_installation_creates_database_record():
         - Install skill via UI
         - Query database for SkillExecution record
         - Verify record exists with status="installed"
         - Verify skill_id and agent_id match

       - test_student_blocked_from_python_skill_installation():
         - Create STUDENT agent
         - Attempt to install Python skill
         - Verify governance error shown
         - Verify installation blocked
         - Verify error message mentions maturity restriction

       - test_intern_can_install_prompt_only_skill():
         - Create INTERN agent
         - Install prompt_only skill (low risk)
         - Verify installation succeeds
         - Verify no approval required for low-risk skills

       - test_supervised_can_install_any_active_skill():
         - Create SUPERVISED agent
         - Install Python code skill
         - Verify installation succeeds
         - Verify database record created

       - test_installation_error_handling():
         - Mock failed installation (network error, server error)
         - Click install button
         - Verify error message displayed
         - Verify button returns to "Install" state
         - Verify skill not added to installed list

       - test_install_same_skill_twice():
         - Install skill
         - Verify success
         - Try to install again
         - Verify already installed message or idempotent behavior

       - test_installed_skills_list_updates():
         - Get initial installed count
         - Install new skill
         - Verify count increased by 1
         - Verify new skill appears in list

    Use authenticated_page fixture from auth_fixtures.py
    Use db_session fixture for database verification
    Create test skills with unique IDs (UUID v4) to prevent collisions
    Mock governance checks as needed for testing different maturity levels
  </action>
  <verify>
    # Check test file exists with tests
    grep -c "def test_.*install" backend/tests/e2e_ui/tests/test_skills_installation.py
    grep -c "SkillInstallationPage" backend/tests/e2e_ui/tests/test_skills_installation.py
  </verify>
  <done>
    test_skills_installation.py exists with 10+ test cases covering SKILL-02 requirements
  </done>
</task>

</tasks>

<verification>
1. SkillInstallationPage class added to page_objects.py
2. All locators handle installation components correctly
3. Test file includes governance tests for different maturity levels
4. Tests verify database state after installation
5. Security scan display tests included
6. Error handling tests cover failure scenarios
</verification>

<success_criteria>
- SkillInstallationPage Page Object with 100+ lines
- test_skills_installation.py with 10+ test cases
- Tests cover: installation flow, button states, security scan, governance blocking, database verification, error handling
- Tests verify governance enforcement for STUDENT/INTERN/SUPERVISED agents
</success_criteria>

<output>
After completion, create .planning/phases/079-skills-workflows/079-02-SUMMARY.md
</output>
