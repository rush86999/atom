---
phase: 079-skills-workflows
plan: 03
type: execute
wave: 2
depends_on: ["079-01"]
files_modified:
  - backend/tests/e2e_ui/pages/page_objects.py
autonomous: true

must_haves:
  truths:
    - "User can configure skill settings (API keys, options, preferences)"
    - "Configuration page displays all configurable skill parameters"
    - "API key fields show masked/hidden values"
    - "Validation prevents saving invalid configuration"
    - "Save button shows loading state during save"
    - "Configuration persists after page refresh"
    - "Reset button restores default configuration"
  artifacts:
    - path: "backend/tests/e2e_ui/pages/page_objects.py"
      provides: "SkillConfigPage Page Object with configuration form locators and methods"
      min_lines: 120
      contains: "class SkillConfigPage"
    - path: "backend/tests/e2e_ui/tests/test_skills_configuration.py"
      provides: "E2E tests for skill configuration workflow"
      min_lines: 150
      contains: "test_skill_configuration_page_loads"
  key_links:
    - from: "SkillConfigPage.save_configuration()"
      to: "skill configuration storage"
      via: "POST/PUT to config endpoint or database"
      pattern: "config.*save"
    - from: "test_api_key_masking"
      to: "input type=password or masked display"
      via: "CSS class or attribute"
      pattern: "mask|password|hidden"
    - from: "test_reset_to_defaults"
      to: "default config values"
      via: "reset button or API call"
      pattern: "reset.*default"
---

<objective>
Create Skill Configuration Page Object and E2E tests for configuring skill settings with validation and persistence.

Purpose: Enable testing of skill configuration workflow including API keys, options, preferences, validation, and persistence
Output: SkillConfigPage Page Object and skill configuration tests
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/078-canvas-presentations/078-03-SUMMARY.md
@backend/api/skill_routes.py
@backend/core/skill_marketplace_service.py
@backend/tests/e2e_ui/pages/page_objects.py
@frontend-nextjs/pages/admin/skills/new.tsx

# Skill Configuration Context
- Skills may require configuration: API keys, endpoints, options, preferences
- Configuration stored per-skill, per-user (or per-workspace)
- API keys should be masked in display (password type input or asterisks)
- Configuration validation: required fields, format checks (URLs, email)
- Save operation persists to database or skill-specific storage
- Reset operation restores skill defaults from SKILL.md
- Configuration may include: boolean toggles, text inputs, selects, number inputs
</context>

<tasks>

<task type="auto">
  <name>Create SkillConfigPage Page Object class</name>
  <files>backend/tests/e2e_ui/pages/page_objects.py</files>
  <action>
    Add SkillConfigPage class to page_objects.py with:

    1. Locators (CSS selectors):
       - config_container - Main configuration container div
       - config_form - Configuration form element
       - api_key_section - API keys configuration section
       - api_key_input - API key text input (password type)
       - api_key_masked - Masked display value
       - api_key_show_button - Toggle visibility button
       - options_section - Options/settings section
       - boolean_toggle - Checkbox/toggle for boolean options
       - text_input - Generic text input field
       - number_input - Number input field
       - select_dropdown - Dropdown select field
       - text_area - Multi-line text area
       - save_button - Save configuration button
       - save_button_loading - Loading state of save button
       - reset_button - Reset to defaults button
       - cancel_button - Cancel/discard changes button
       - validation_error - Field validation error message
       - success_message - Configuration saved success message
       - config_field_label - Field label element
       - config_field_helper - Helper text/tooltip for field

    2. Methods:
       - is_loaded() -> bool - Check if config page is visible
       - get_field_count() -> int - Get number of config fields
       - set_api_key(key_name: str, value: str) -> None - Set API key value
       - get_api_key_value(key_name: str) -> str - Get API key value (may be masked)
       - show_api_key(key_name: str) -> None - Toggle API key visibility
       - set_boolean_option(option_name: str, value: bool) -> None - Set boolean toggle
       - get_boolean_option(option_name: str) -> bool - Get boolean value
       - set_text_field(field_name: str, value: str) -> None - Set text field value
       - get_text_field(field_name: str) -> str - Get text field value
       - set_number_field(field_name: str, value: float) -> None - Set number field value
       - get_number_field(field_name: str) -> float - Get number field value
       - select_option(field_name: str, option: str) -> None - Select dropdown option
       - get_selected_option(field_name: str) -> str - Get selected dropdown value
       - click_save() -> None - Click save button
       - click_reset() -> None - Click reset button
       - click_cancel() -> None - Click cancel button
       - is_saving() -> bool - Check if save button in loading state
       - get_validation_errors() -> dict - Get all validation errors {field: error_message}
       - has_field_error(field_name: str) -> bool - Check if field has validation error
       - wait_for_save_complete(timeout: int = 5000) -> None - Wait for save to complete
       - is_success_message_visible() -> bool - Check if success message shown
       - get_all_field_values() -> dict - Get all field values as dict

    Follow BasePage pattern from existing page_objects.py
    Handle dynamic field types (skill configs vary by skill)
    Support field identification by label, name, or data attribute
    Include docstrings with Google-style Args/Returns
  </action>
  <verify>
    # Check file has SkillConfigPage class
    grep -c "class SkillConfigPage" backend/tests/e2e_ui/pages/page_objects.py
    grep -c "api_key_input\|save_button\|reset_button" backend/tests/e2e_ui/pages/page_objects.py
  </verify>
  <done>
    SkillConfigPage class exists with 120+ lines including all required locators and methods
  </done>
</task>

<task type="auto">
  <name>Create skill configuration E2E tests</name>
  <files>backend/tests/e2e_ui/tests/test_skills_configuration.py</files>
  <action>
    Create test_skills_configuration.py with:

    1. Helper functions:
       - create_skill_with_config(db_session, config_schema: dict) -> str
         - Create skill that requires configuration
         - Define config schema (fields, types, validation rules)
         - Return skill_id
       - setup_config_page(browser, skill_id: str, user) -> Page
         - Navigate to skill config page
         - Return initialized page

    2. Test cases:
       - test_skill_configuration_page_loads():
         - Navigate to config page for skill with config
         - Verify SkillConfigPage.is_loaded() returns True
         - Verify all expected fields are displayed

       - test_api_key_masking():
         - Create skill with API key field
         - Enter API key value
         - Verify input type is "password" or value is masked
         - Click show/hide toggle
         - Verify value visibility toggles

       - test_boolean_option_toggle():
         - Set boolean option to True
         - Verify toggle state changes
         - Set boolean option to False
         - Verify toggle returns to off state

       - test_text_field_validation():
         - Create text field with required validation
         - Leave empty and save
         - Verify validation error shown
         - Enter valid value and save
         - Verify validation clears

       - test_number_field_constraints():
         - Create number field with min/max
         - Enter value below min
         - Verify validation error
         - Enter value above max
         - Verify validation error
         - Enter valid value
         - Verify no error

       - test_select_option():
         - Select different dropdown option
         - Verify selection persists
         - Save configuration
         - Verify saved value selected after reload

       - test_save_configuration():
         - Modify multiple config fields
         - Click save
         - Wait for save to complete
         - Verify success message shown
         - Reload page
         - Verify values persisted

       - test_save_loading_state():
         - Click save button
         - Verify "Saving..." or loading indicator
         - Wait for completion
         - Verify button returns to normal state

       - test_reset_to_defaults():
         - Modify config fields
         - Save changes
         - Click reset button
         - Verify confirmation dialog (if exists)
         - Confirm reset
         - Verify fields return to default values

       - test_cancel_discards_changes():
         - Modify config fields
         - Click cancel button
         - Verify changes not saved
         - Reload page
         - Verify previous values still present

       - test_multi_field_configuration():
         - Create skill with multiple field types
         - Set values for all fields
         - Save configuration
         - Verify all values persisted correctly

       - test_configuration_validation_errors():
         - Submit invalid configuration (multiple errors)
         - Verify all errors displayed
         - Fix one error
         - Verify other errors still shown
         - Fix all errors
         - Verify save succeeds

    Use authenticated_page fixture from auth_fixtures.py
    Use db_session for database verification of saved config
    Create test skills with unique config schemas (UUID v4)
    Test various field types: text, password, number, boolean, select, textarea
  </action>
  <verify>
    # Check test file exists with tests
    grep -c "def test_.*config" backend/tests/e2e_ui/tests/test_skills_configuration.py
    grep -c "SkillConfigPage" backend/tests/e2e_ui/tests/test_skills_configuration.py
  </verify>
  <done>
    test_skills_configuration.py exists with 12+ test cases covering SKILL-03 requirements
  </done>
</task>

</tasks>

<verification>
1. SkillConfigPage class added to page_objects.py
2. All locators handle configuration components correctly
3. Test file includes validation tests for all field types
4. Tests verify persistence across page reload
5. API key masking tests included
6. Reset and cancel workflows tested
</verification>

<success_criteria>
- SkillConfigPage Page Object with 120+ lines
- test_skills_configuration.py with 12+ test cases
- Tests cover: page load, API keys, boolean toggles, validation, number constraints, selects, save/load, reset, cancel, multi-field
- Tests verify persistence and validation thoroughly
</success_criteria>

<output>
After completion, create .planning/phases/079-skills-workflows/079-03-SUMMARY.md
</output>
