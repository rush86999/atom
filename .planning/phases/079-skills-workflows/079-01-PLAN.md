---
phase: 079-skills-workflows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/e2e_ui/pages/page_objects.py
autonomous: true

must_haves:
  truths:
    - "User can browse skill marketplace with search and category filters"
    - "Marketplace displays skill cards with name, description, rating, author"
    - "Search filters skills by query text (name, description, tags)"
    - "Category filters show available categories and filter results"
    - "Pagination works for large skill listings"
    - "Empty state displays when no skills match filters"
  artifacts:
    - path: "backend/tests/e2e_ui/pages/page_objects.py"
      provides: "SkillsMarketplacePage Page Object with marketplace locators and interaction methods"
      min_lines: 120
      contains: "class SkillsMarketplacePage"
    - path: "backend/tests/e2e_ui/tests/test_skills_marketplace.py"
      provides: "E2E tests for skill marketplace browsing, search, and filters"
      min_lines: 150
      contains: "test_marketplace_browse"
  key_links:
    - from: "SkillsMarketplacePage.search_skills()"
      to: "/api/skills/list endpoint"
      via: "search query parameter"
      pattern: "skills.*list.*search"
    - from: "test_marketplace_category_filter"
      to: "SkillMarketplaceService.search_skills()"
      via: "category filter"
      pattern: "category.*filter"
---

<objective>
Create Skills Marketplace Page Object and E2E tests for browsing skills with search and category filters.

Purpose: Enable testing of skill marketplace browsing workflow including search, category filtering, pagination, and skill card display
Output: SkillsMarketplacePage Page Object and marketplace browsing tests
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/078-canvas-presentations/078-01-SUMMARY.md
@backend/api/skill_routes.py
@backend/core/skill_marketplace_service.py
@backend/tests/e2e_ui/pages/page_objects.py

# Skill Marketplace Context
- Marketplace endpoint: GET /api/skills/list with query, category, skill_type, limit params
- SkillMarketplaceService.search_skills() provides search with PostgreSQL full-text search
- Marketplace displays skill cards with: name, description, category, tags, author, version, rating
- Categories: data_processing, automation, integration, productivity, utilities, developer_tools
- Pagination: page and page_size parameters (default page_size=20)
- Search filters: skill_id, description text, category, skill_type (prompt_only, python_code)
</context>

<tasks>

<task type="auto">
  <name>Create SkillsMarketplacePage Page Object class</name>
  <files>backend/tests/e2e_ui/pages/page_objects.py</files>
  <action>
    Add SkillsMarketplacePage class to page_objects.py with:

    1. Locators (CSS selectors):
       - marketplace_container - Main marketplace container div
       - search_input - Search text input field
       - search_button - Search submit button
       - category_filter - Category dropdown/selector
       - skill_type_filter - Skill type filter (prompt_only, python_code)
       - skill_cards - Individual skill card elements
       - skill_card_name - Skill name display on card
       - skill_card_description - Skill description text
       - skill_card_category - Category badge on card
       - skill_card_rating - Star rating display
       - skill_card_author - Author name display
       - skill_card_install_button - Install button on skill card
       - pagination_container - Pagination controls
       - next_page_button - Next page button
       - prev_page_button - Previous page button
       - page_indicator - Current page indicator
       - empty_state - Empty state message when no skills found
       - loading_spinner - Loading state indicator

    2. Methods:
       - is_loaded() -> bool - Check if marketplace is visible
       - search(query: str) -> None - Enter search query and submit
       - select_category(category: str) -> None - Select category filter
       - select_skill_type(skill_type: str) -> None - Select skill type filter
       - get_skill_count() -> int - Get number of skill cards displayed
       - get_skill_names() -> list[str] - Get list of displayed skill names
       - get_skill_card_info(index: int) -> dict - Get skill details at index (name, description, category, rating, author)
       - click_skill_install(index: int) -> None - Click install button on skill card
       - click_next_page() -> None - Navigate to next page
       - click_prev_page() -> None - Navigate to previous page
       - get_current_page() -> int - Get current page number
       - is_empty_state_visible() -> bool - Check if empty state is shown
       - wait_for_skills_load(timeout: int = 5000) -> None - Wait for skill cards to load

    Follow BasePage pattern from existing page_objects.py
    Use data-testid selectors where available (check frontend for test-id attributes)
    Include docstrings with Google-style Args/Returns
    Handle pagination state (disable prev on first page, disable next on last page)
  </action>
  <verify>
    # Check file has SkillsMarketplacePage class
    grep -c "class SkillsMarketplacePage" backend/tests/e2e_ui/pages/page_objects.py
    grep -c "search_input\|skill_cards\|category_filter" backend/tests/e2e_ui/pages/page_objects.py
  </verify>
  <done>
    SkillsMarketplacePage class exists with 120+ lines including all required locators and methods
  </done>
</task>

<task type="auto">
  <name>Create marketplace browsing E2E tests</name>
  <files>backend/tests/e2e_ui/tests/test_skills_marketplace.py</files>
  <action>
    Create test_skills_marketplace.py with:

    1. Helper functions:
       - create_test_skills_marketplace(db_session, count: int = 10) -> list[dict]
         - Create test skill records in SkillExecution table
         - Mix of categories, skill_types, and names
         - Returns list of created skill IDs
       - setup_marketplace_page(browser, authenticated_user) -> Page
         - Navigate to marketplace and return initialized page

    2. Test cases:
       - test_marketplace_loads():
         - Navigate to marketplace
         - Verify SkillsMarketplacePage.is_loaded() returns True
         - Verify skill cards are displayed (count > 0 or empty state)

       - test_marketplace_search_by_name():
         - Create skills with specific names
         - Search for partial name match
         - Verify results contain matching skills only
         - Verify non-matching skills are not shown

       - test_marketplace_search_by_description():
         - Create skills with specific descriptions
         - Search for description keyword
         - Verify results match description content

       - test_marketplace_category_filter():
         - Create skills across multiple categories
         - Select category from filter
         - Verify only skills in category are shown
         - Verify skill_count updates correctly

       - test_marketplace_skill_type_filter():
         - Create prompt_only and python_code skills
         - Filter by skill_type
         - Verify only matching type shown

       - test_marketplace_combined_filters():
         - Apply search + category + type filters
         - Verify results match all criteria

       - test_marketplace_pagination():
         - Create 25+ skills (page_size=20)
         - Verify first page shows 20 skills
         - Click next page
         - Verify second page loads
         - Verify page indicator updates

       - test_marketplace_empty_state():
         - Search for non-existent skill
         - Verify empty state message is visible
         - Verify helpful message shown

       - test_marketplace_skill_card_display():
         - Verify skill card shows: name, description, category badge, author
         - Verify rating stars display (if rated)
         - Verify install button is present

       - test_marketplace_sort_options():
         - Test sorting by relevance, name, created_at
         - Verify order changes correctly

    Use authenticated_page fixture from auth_fixtures.py
    Use API-first setup for test data (create skills via db_session)
    Include UUID v4 in skill names for uniqueness (e.g., "test-skill-{uuid}")
  </action>
  <verify>
    # Check test file exists with tests
    grep -c "def test_marketplace" backend/tests/e2e_ui/tests/test_skills_marketplace.py
    grep -c "SkillsMarketplacePage" backend/tests/e2e_ui/tests/test_skills_marketplace.py
  </verify>
  <done>
    test_skills_marketplace.py exists with 10+ test cases covering SKILL-01 requirements
  </done>
</task>

</tasks>

<verification>
1. SkillsMarketplacePage class added to page_objects.py
2. All locators handle marketplace components correctly
3. Test file includes helper functions for skill data setup
4. Tests use API-first setup via db_session for fast state initialization
5. All tests use unique IDs to prevent data collisions
6. Empty state handling verified
</verification>

<success_criteria>
- SkillsMarketplacePage Page Object with 120+ lines
- test_skills_marketplace.py with 10+ test cases
- Tests cover: browsing, search, category filter, type filter, pagination, empty state, sorting
- Tests follow Phase 78 patterns for consistency
</success_criteria>

<output>
After completion, create .planning/phases/079-skills-workflows/079-01-SUMMARY.md
</output>
