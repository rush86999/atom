---
phase: 254-frontend-coverage-baseline
plan: 02
type: execute
wave: 2
depends_on: [254-01]
files_modified:
  - frontend-nextjs/tests/agents/test-agent-card.test.tsx
  - frontend-nextjs/tests/agents/test-agent-manager.test.tsx
  - frontend-nextjs/tests/agents/test-agent-studio.test.tsx
  - frontend-nextjs/tests/agents/test-agent-terminal.test.tsx
autonomous: true
requirements:
  - COV-F-02
  - COV-F-05
user_setup: []

must_haves:
  truths:
    - "Agent components have test coverage (AgentCard, AgentManager, AgentStudio, AgentTerminal)"
    - "Coverage reaches 70% for tested agent components"
    - "Tests follow React Testing Library patterns (user-centric queries)"
    - "Agent component tests cover critical paths (render, interactions, state)"
  artifacts:
    - path: "frontend-nextjs/tests/agents/test-agent-card.test.tsx"
      provides: "AgentCard component tests"
      contains: "render tests, interaction tests, maturity badge display"
    - path: "frontend-nextjs/tests/agents/test-agent-manager.test.tsx"
      provides: "AgentManager component tests"
      contains: "agent list, creation, deletion, filtering tests"
    - path: "frontend-nextjs/tests/agents/test-agent-studio.test.tsx"
      provides: "AgentStudio component tests"
      contains: "configuration panel, role settings, skill selection tests"
    - path: "frontend-nextjs/tests/agents/test-agent-terminal.test.tsx"
      provides: "AgentTerminal component tests"
      contains: "terminal render, message display, command execution tests"
  key_links:
    - from: "tests/agents/test-agent-card.test.tsx"
      to: "components/Agents/AgentCard.tsx"
      via: "import { AgentCard } from '@/components/Agents/AgentCard'"
      pattern: "import.*AgentCard.*from.*@/components/Agents/AgentCard"
    - from: "tests/agents/test-agent-manager.test.tsx"
      to: "components/Agents/AgentManager.tsx"
      via: "import { AgentManager } from '@/components/Agents/AgentManager'"
      pattern: "import.*AgentManager.*from.*@/components/Agents/AgentManager"
    - from: "tests/agents/test-agent-studio.test.tsx"
      to: "components/Agents/AgentStudio.tsx"
      via: "import { AgentStudio } from '@/components/Agents/AgentStudio'"
      pattern: "import.*AgentStudio.*from.*@/components/Agents/AgentStudio"
    - from: "tests/agents/test-agent-terminal.test.tsx"
      to: "components/Agents/AgentTerminal.tsx"
      via: "import { AgentTerminal } from '@/components/Agents/AgentTerminal'"
      pattern: "import.*AgentTerminal.*from.*@/components/Agents/AgentTerminal"
---

<objective>
Create comprehensive tests for critical agent components to reach 70% coverage

Purpose: Cover agent-related components (AgentCard, AgentManager, AgentStudio, AgentTerminal) which are core to the application's agent management functionality
Output: 4 test files with 70%+ coverage for agent components
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/254-frontend-coverage-baseline/254-01-SUMMARY.md
@frontend-nextjs/jest.config.js
@frontend-nextjs/tests/integration/forms.test.tsx
@frontend-nextjs/components/canvas/__tests__/canvas-coverage-summary.md

# React Testing Library patterns (from existing tests)
- User-centric queries: getByRole, getByLabelText, getByText, queryBy
- Async testing: waitFor, act
- Mock patterns: jest.fn(), jest.mock()
- Test structure: describe blocks grouping related tests
- Accessibility: getByRole('button', { name: /submit/i })

# Agent component files to test (from coverage baseline)
- components/Agents/AgentCard.tsx (18 lines, 0% coverage)
- components/Agents/AgentManager.tsx (87 lines, 0% coverage)
- components/Agents/AgentStudio.tsx (144 lines, 0% coverage)
- components/Agents/AgentTerminal.tsx (20 lines, 0% coverage)

# Agent component features (based on file structure)
- AgentCard: Display agent info, maturity badge, actions
- AgentManager: Agent list, creation, deletion, filtering
- AgentStudio: Configuration panel, role settings, skill selection
- AgentTerminal: Terminal render, message display, command execution

# Test file location pattern
- Test files in tests/agents/ directory (parallel to components/Agents/)
- Use __tests__ subdirectory if preferred
- Import components with @/components/Agents/ComponentName
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AgentCard component tests</name>
  <files>
    frontend-nextjs/tests/agents/test-agent-card.test.tsx
    frontend-nextjs/components/Agents/AgentCard.tsx
  </files>
  <read_first>
    frontend-nextjs/components/Agents/AgentCard.tsx
    frontend-nextjs/tests/integration/forms.test.tsx
    frontend-nextjs/jest.setup.js
  </read_first>
  <action>
    Create test-agent-card.test.tsx with comprehensive AgentCard tests:

    File: frontend-nextjs/tests/agents/test-agent-card.test.tsx

    Test structure:
    ```typescript
    import { render, screen, fireEvent, waitFor } from '@testing-library/react';
    import userEvent from '@testing-library/user-event';
    import { AgentCard } from '@/components/Agents/AgentCard';

    describe('AgentCard Component', () => {
      // Mock props interface based on component usage
      const mockAgent = {
        id: 'agent-123',
        name: 'Test Agent',
        description: 'A test agent',
        maturity: 'AUTONOMOUS',
        status: 'active',
        capabilities: ['chat', 'workflow']
      };

      const mockHandlers = {
        onEdit: jest.fn(),
        onDelete: jest.fn(),
        onExecute: jest.fn()
      };

      afterEach(() => {
        jest.clearAllMocks();
      });

      // Render tests (4-5 tests)
      describe('Rendering', () => {
        it('should render agent name and description', () => {
          render(<AgentCard agent={mockAgent} {...mockHandlers} />);
          expect(screen.getByText('Test Agent')).toBeInTheDocument();
          expect(screen.getByText('A test agent')).toBeInTheDocument();
        });

        it('should display maturity badge with correct level', () => {
          render(<AgentCard agent={mockAgent} {...mockHandlers} />);
          expect(screen.getByText('AUTONOMOUS')).toBeInTheDocument();
        });

        it('should render action buttons', () => {
          render(<AgentCard agent={mockAgent} {...mockHandlers} />);
          expect(screen.getByRole('button', { name: /edit/i })).toBeInTheDocument();
          expect(screen.getByRole('button', { name: /execute/i })).toBeInTheDocument();
        });

        it('should display agent status indicator', () => {
          render(<AgentCard agent={mockAgent} {...mockHandlers} />);
          expect(screen.getByText(/active/i)).toBeInTheDocument();
        });

        it('should show capabilities as tags', () => {
          render(<AgentCard agent={mockAgent} {...mockHandlers} />);
          expect(screen.getByText('chat')).toBeInTheDocument();
          expect(screen.getByText('workflow')).toBeInTheDocument();
        });
      });

      // Interaction tests (4-5 tests)
      describe('User Interactions', () => {
        it('should call onEdit when edit button clicked', async () => {
          render(<AgentCard agent={mockAgent} {...mockHandlers} />);
          await userEvent.click(screen.getByRole('button', { name: /edit/i }));
          expect(mockHandlers.onEdit).toHaveBeenCalledWith('agent-123');
        });

        it('should call onExecute when execute button clicked', async () => {
          render(<AgentCard agent={mockAgent} {...mockHandlers} />);
          await userEvent.click(screen.getByRole('button', { name: /execute/i }));
          expect(mockHandlers.onExecute).toHaveBeenCalledWith('agent-123');
        });

        it('should show confirmation dialog before delete', async () => {
          render(<AgentCard agent={mockAgent} {...mockHandlers} />);
          await userEvent.click(screen.getByRole('button', { name: /delete/i }));
          await waitFor(() => {
            expect(screen.getByText(/are you sure/i)).toBeInTheDocument();
          });
        });

        it('should call onDelete after confirmation', async () => {
          render(<AgentCard agent={mockAgent} {...mockHandlers} />);
          await userEvent.click(screen.getByRole('button', { name: /delete/i }));
          await userEvent.click(screen.getByRole('button', { name: /confirm/i }));
          expect(mockHandlers.onDelete).toHaveBeenCalledWith('agent-123');
        });
      });

      // State tests (3-4 tests)
      describe('State Variations', () => {
        it('should display disabled state when agent is inactive', () => {
          const inactiveAgent = { ...mockAgent, status: 'inactive' };
          render(<AgentCard agent={inactiveAgent} {...mockHandlers} />);
          expect(screen.getByRole('button', { name: /execute/i })).toBeDisabled();
        });

        it('should show different maturity levels correctly', () => {
          const studentAgent = { ...mockAgent, maturity: 'STUDENT' };
          render(<AgentCard agent={studentAgent} {...mockHandlers} />);
          expect(screen.getByText('STUDENT')).toBeInTheDocument();
        });

        it('should handle missing description gracefully', () => {
          const agentNoDesc = { ...mockAgent, description: undefined };
          render(<AgentCard agent={agentNoDesc} {...mockHandlers} />);
          expect(screen.getByText('Test Agent')).toBeInTheDocument();
        });
      });

      // Edge cases (2-3 tests)
      describe('Edge Cases', () => {
        it('should handle empty capabilities array', () => {
          const agentNoCaps = { ...mockAgent, capabilities: [] };
          render(<AgentCard agent={agentNoCaps} {...mockHandlers} />);
          expect(screen.queryByText('chat')).not.toBeInTheDocument();
        });

        it('should handle missing handlers gracefully', () => {
          render(<AgentCard agent={mockAgent} />);
          expect(screen.getByText('Test Agent')).toBeInTheDocument();
        });
      });
    });
    ```

    Target: 20+ tests covering render, interactions, state variations, edge cases
  </action>
  <verify>
    <automated>cd frontend-nextjs && npm test -- tests/agents/test-agent-card.test.tsx --coverage --collectCoverageFrom=components/Agents/AgentCard.tsx --passWithNoTests</automated>
  </verify>
  <done>
    test-agent-card.test.tsx created with 20+ tests
    Tests cover render, interactions, state variations, edge cases
    Coverage reaches 70%+ for AgentCard component
  </done>
</task>

<task type="auto">
  <name>Task 2: Create AgentManager component tests</name>
  <files>
    frontend-nextjs/tests/agents/test-agent-manager.test.tsx
    frontend-nextjs/components/Agents/AgentManager.tsx
  </files>
  <read_first>
    frontend-nextjs/components/Agents/AgentManager.tsx
    frontend-nextjs/tests/agents/test-agent-card.test.tsx
    frontend-nextjs/jest.setup.js
  </read_first>
  <action>
    Create test-agent-manager.test.tsx with comprehensive AgentManager tests:

    File: frontend-nextjs/tests/agents/test-agent-manager.test.tsx

    Test structure:
    ```typescript
    import { render, screen, fireEvent, waitFor } from '@testing-library/react';
    import userEvent from '@testing-library/user-event';
    import { AgentManager } from '@/components/Agents/AgentManager';

    // Mock child components
    jest.mock('@/components/Agents/AgentCard', () => ({
      AgentCard: ({ agent, onEdit, onDelete, onExecute }) => (
        <div data-testid={`agent-${agent.id}`}>
          <span>{agent.name}</span>
          <button onClick={() => onEdit?.(agent.id)}>Edit</button>
          <button onClick={() => onDelete?.(agent.id)}>Delete</button>
          <button onClick={() => onExecute?.(agent.id)}>Execute</button>
        </div>
      )
    }));

    describe('AgentManager Component', () => {
      const mockAgents = [
        { id: 'agent-1', name: 'Agent One', maturity: 'AUTONOMOUS', status: 'active' },
        { id: 'agent-2', name: 'Agent Two', maturity: 'STUDENT', status: 'active' },
        { id: 'agent-3', name: 'Agent Three', maturity: 'INTERN', status: 'inactive' }
      ];

      const mockHandlers = {
        onCreateAgent: jest.fn(),
        onEditAgent: jest.fn(),
        onDeleteAgent: jest.fn(),
        onExecuteAgent: jest.fn()
      };

      afterEach(() => {
        jest.clearAllMocks();
      });

      // Render tests (4-5 tests)
      describe('Rendering', () => {
        it('should render all agents in list', () => {
          render(<AgentManager agents={mockAgents} {...mockHandlers} />);
          expect(screen.getByText('Agent One')).toBeInTheDocument();
          expect(screen.getByText('Agent Two')).toBeInTheDocument();
          expect(screen.getByText('Agent Three')).toBeInTheDocument();
        });

        it('should render create agent button', () => {
          render(<AgentManager agents={mockAgents} {...mockHandlers} />);
          expect(screen.getByRole('button', { name: /create agent/i })).toBeInTheDocument();
        });

        it('should render filter controls', () => {
          render(<AgentManager agents={mockAgents} {...mockHandlers} />);
          expect(screen.getByLabelText(/filter by maturity/i)).toBeInTheDocument();
          expect(screen.getByLabelText(/filter by status/i)).toBeInTheDocument();
        });

        it('should show agent count', () => {
          render(<AgentManager agents={mockAgents} {...mockHandlers} />);
          expect(screen.getByText(/3 agents/i)).toBeInTheDocument();
        });

        it('should render empty state when no agents', () => {
          render(<AgentManager agents={[]} {...mockHandlers} />);
          expect(screen.getByText(/no agents found/i)).toBeInTheDocument();
        });
      });

      // Filtering tests (4-5 tests)
      describe('Filtering', () => {
        it('should filter agents by maturity level', async () => {
          render(<AgentManager agents={mockAgents} {...mockHandlers} />);
          const maturityFilter = screen.getByLabelText(/filter by maturity/i);
          await userEvent.selectOptions(maturityFilter, 'AUTONOMOUS');
          expect(screen.getByText('Agent One')).toBeInTheDocument();
          expect(screen.queryByText('Agent Two')).not.toBeInTheDocument();
        });

        it('should filter agents by status', async () => {
          render(<AgentManager agents={mockAgents} {...mockHandlers} />);
          const statusFilter = screen.getByLabelText(/filter by status/i);
          await userEvent.selectOptions(statusFilter, 'active');
          expect(screen.getByText('Agent One')).toBeInTheDocument();
          expect(screen.queryByText('Agent Three')).not.toBeInTheDocument();
        });

        it('should reset filters when reset button clicked', async () => {
          render(<AgentManager agents={mockAgents} {...mockHandlers} />);
          const maturityFilter = screen.getByLabelText(/filter by maturity/i);
          await userEvent.selectOptions(maturityFilter, 'AUTONOMOUS');
          await userEvent.click(screen.getByRole('button', { name: /reset filters/i }));
          expect(screen.getByText('Agent Two')).toBeInTheDocument();
        });
      });

      // Creation tests (3-4 tests)
      describe('Agent Creation', () => {
        it('should open creation modal when create button clicked', async () => {
          render(<AgentManager agents={mockAgents} {...mockHandlers} />);
          await userEvent.click(screen.getByRole('button', { name: /create agent/i }));
          await waitFor(() => {
            expect(screen.getByRole('dialog')).toBeInTheDocument();
          });
        });

        it('should call onCreateAgent with new agent data', async () => {
          render(<AgentManager agents={mockAgents} {...mockHandlers} />);
          await userEvent.click(screen.getByRole('button', { name: /create agent/i }));
          await userEvent.type(screen.getByLabelText(/agent name/i), 'New Agent');
          await userEvent.click(screen.getByRole('button', { name: /create/i }));
          expect(mockHandlers.onCreateAgent).toHaveBeenCalledWith(
            expect.objectContaining({ name: 'New Agent' })
          );
        });
      });

      // Agent operations tests (3-4 tests)
      describe('Agent Operations', () => {
        it('should call onEditAgent when agent edit clicked', async () => {
          render(<AgentManager agents={mockAgents} {...mockHandlers} />);
          await userEvent.click(screen.getByText('Agent One').closest('[data-testid="agent-1"]').querySelector('button'));
          expect(mockHandlers.onEditAgent).toHaveBeenCalledWith('agent-1');
        });

        it('should call onDeleteAgent when agent delete clicked', async () => {
          render(<AgentManager agents={mockAgents} {...mockHandlers} />);
          const deleteButton = screen.getByText('Agent One').closest('[data-testid="agent-1"]').querySelectorAll('button')[1];
          await userEvent.click(deleteButton);
          expect(mockHandlers.onDeleteAgent).toHaveBeenCalledWith('agent-1');
        });

        it('should call onExecuteAgent when agent execute clicked', async () => {
          render(<AgentManager agents={mockAgents} {...mockHandlers} />);
          const executeButton = screen.getByText('Agent One').closest('[data-testid="agent-1"]').querySelectorAll('button')[2];
          await userEvent.click(executeButton);
          expect(mockHandlers.onExecuteAgent).toHaveBeenCalledWith('agent-1');
        });
      });

      // Search tests (2-3 tests)
      describe('Search', () => {
        it('should filter agents by search query', async () => {
          render(<AgentManager agents={mockAgents} {...mockHandlers} />);
          await userEvent.type(screen.getByLabelText(/search agents/i), 'Agent One');
          expect(screen.getByText('Agent One')).toBeInTheDocument();
          expect(screen.queryByText('Agent Two')).not.toBeInTheDocument();
        });

        it('should show no results for non-matching search', async () => {
          render(<AgentManager agents={mockAgents} {...mockHandlers} />);
          await userEvent.type(screen.getByLabelText(/search agents/i), 'NonExistent');
          expect(screen.getByText(/no agents found/i)).toBeInTheDocument();
        });
      });

      // Edge cases (2-3 tests)
      describe('Edge Cases', () => {
        it('should handle undefined agents gracefully', () => {
          render(<AgentManager agents={undefined} {...mockHandlers} />);
          expect(screen.getByText(/no agents found/i)).toBeInTheDocument();
        });

        it('should handle large agent lists efficiently', () => {
          const largeAgentList = Array.from({ length: 100 }, (_, i) => ({
            id: `agent-${i}`,
            name: `Agent ${i}`,
            maturity: 'AUTONOMOUS',
            status: 'active'
          }));
          render(<AgentManager agents={largeAgentList} {...mockHandlers} />);
          expect(screen.getByText(/100 agents/i)).toBeInTheDocument();
        });
      });
    });
    ```

    Target: 25+ tests covering render, filtering, creation, operations, search, edge cases
  </action>
  <verify>
    <automated>cd frontend-nextjs && npm test -- tests/agents/test-agent-manager.test.tsx --coverage --collectCoverageFrom=components/Agents/AgentManager.tsx --passWithNoTests</automated>
  </verify>
  <done>
    test-agent-manager.test.tsx created with 25+ tests
    Tests cover render, filtering, creation, operations, search, edge cases
    Coverage reaches 70%+ for AgentManager component
  </done>
</task>

<task type="auto">
  <name>Task 3: Create AgentStudio component tests</name>
  <files>
    frontend-nextjs/tests/agents/test-agent-studio.test.tsx
    frontend-nextjs/components/Agents/AgentStudio.tsx
  </files>
  <read_first>
    frontend-nextjs/components/Agents/AgentStudio.tsx
    frontend-nextjs/tests/agents/test-agent-manager.test.tsx
    frontend-nextjs/jest.setup.js
  </read_first>
  <action>
    Create test-agent-studio.test.tsx with comprehensive AgentStudio tests:

    File: frontend-nextjs/tests/agents/test-agent-studio.test.tsx

    Test structure:
    ```typescript
    import { render, screen, fireEvent, waitFor } from '@testing-library/react';
    import userEvent from '@testing-library/user-event';
    import { AgentStudio } from '@/components/Agents/AgentStudio';

    describe('AgentStudio Component', () => {
      const mockAgent = {
        id: 'agent-123',
        name: 'Test Agent',
        description: 'Test description',
        maturity: 'INTERN',
        role: 'assistant',
        skills: ['chat', 'workflow'],
        config: { temperature: 0.7, maxTokens: 1000 }
      };

      const mockHandlers = {
        onSave: jest.fn(),
        onCancel: jest.fn(),
        onSkillToggle: jest.fn(),
        onConfigChange: jest.fn()
      };

      afterEach(() => {
        jest.clearAllMocks();
      });

      // Render tests (5-6 tests)
      describe('Rendering', () => {
        it('should render agent name input', () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          expect(screen.getByLabelText(/agent name/i)).toBeInTheDocument();
        });

        it('should render description textarea', () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          expect(screen.getByLabelText(/description/i)).toBeInTheDocument();
        });

        it('should render maturity level selector', () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          expect(screen.getByLabelText(/maturity level/i)).toBeInTheDocument();
        });

        it('should render role selector', () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          expect(screen.getByLabelText(/role/i)).toBeInTheDocument();
        });

        it('should render skills selection panel', () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          expect(screen.getByText(/skills/i)).toBeInTheDocument();
          expect(screen.getByText('chat')).toBeInTheDocument();
        });

        it('should render configuration panel', () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          expect(screen.getByText(/configuration/i)).toBeInTheDocument();
        });
      });

      // Form input tests (5-6 tests)
      describe('Form Inputs', () => {
        it('should update agent name on input change', async () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          const nameInput = screen.getByLabelText(/agent name/i);
          await userEvent.clear(nameInput);
          await userEvent.type(nameInput, 'Updated Agent');
          expect(nameInput).toHaveValue('Updated Agent');
        });

        it('should update description on textarea change', async () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          const descInput = screen.getByLabelText(/description/i);
          await userEvent.clear(descInput);
          await userEvent.type(descInput, 'Updated description');
          expect(descInput).toHaveValue('Updated description');
        });

        it('should change maturity level selection', async () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          const maturitySelect = screen.getByLabelText(/maturity level/i);
          await userEvent.selectOptions(maturitySelect, 'SUPERVISED');
          expect(maturitySelect).toHaveValue('SUPERVISED');
        });

        it('should change role selection', async () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          const roleSelect = screen.getByLabelText(/role/i);
          await userEvent.selectOptions(roleSelect, 'analyst');
          expect(roleSelect).toHaveValue('analyst');
        });
      });

      // Skills tests (4-5 tests)
      describe('Skills Selection', () => {
        it('should display available skills as checkboxes', () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          expect(screen.getByRole('checkbox', { name: /chat/i })).toBeInTheDocument();
          expect(screen.getByRole('checkbox', { name: /workflow/i })).toBeInTheDocument();
        });

        it('should check skills that are already assigned', () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          expect(screen.getByRole('checkbox', { name: /chat/i })).toBeChecked();
        });

        it('should call onSkillToggle when skill checkbox clicked', async () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          const workflowCheckbox = screen.getByRole('checkbox', { name: /workflow/i });
          await userEvent.click(workflowCheckbox);
          expect(mockHandlers.onSkillToggle).toHaveBeenCalledWith('workflow', false);
        });

        it('should filter skills by category', async () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          await userEvent.click(screen.getByRole('tab', { name: /communication/i }));
          expect(screen.getByRole('checkbox', { name: /chat/i })).toBeInTheDocument();
        });
      });

      // Configuration tests (4-5 tests)
      describe('Configuration', () => {
        it('should render temperature slider', () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          expect(screen.getByLabelText(/temperature/i)).toBeInTheDocument();
        });

        it('should update temperature on slider change', async () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          const tempSlider = screen.getByLabelText(/temperature/i);
          await userEvent.type(tempSlider, '0.9');
          expect(mockHandlers.onConfigChange).toHaveBeenCalledWith(
            expect.objectContaining({ temperature: 0.9 })
          );
        });

        it('should render max tokens input', () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          expect(screen.getByLabelText(/max tokens/i)).toBeInTheDocument();
        });

        it('should validate temperature range (0-1)', async () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          const tempSlider = screen.getByLabelText(/temperature/i);
          await userEvent.type(tempSlider, '1.5');
          await waitFor(() => {
            expect(screen.getByText(/temperature must be between 0 and 1/i)).toBeInTheDocument();
          });
        });
      });

      // Save/Cancel tests (3-4 tests)
      describe('Actions', () => {
        it('should call onSave with agent data when save clicked', async () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          await userEvent.click(screen.getByRole('button', { name: /save/i }));
          expect(mockHandlers.onSave).toHaveBeenCalledWith(
            expect.objectContaining({
              id: 'agent-123',
              name: 'Test Agent'
            })
          );
        });

        it('should call onCancel when cancel clicked', async () => {
          render(<AgentStudio agent={mockAgent} {...mockHandlers} />);
          await userEvent.click(screen.getByRole('button', { name: /cancel/i }));
          expect(mockHandlers.onCancel).toHaveBeenCalled();
        });

        it('should show validation errors for required fields', async () => {
          render(<AgentStudio agent={{ ...mockAgent, name: '' }} {...mockHandlers} />);
          await userEvent.click(screen.getByRole('button', { name: /save/i }));
          expect(screen.getByText(/name is required/i)).toBeInTheDocument();
        });
      });

      // Edge cases (2-3 tests)
      describe('Edge Cases', () => {
        it('should handle null agent gracefully', () => {
          render(<AgentStudio agent={null} {...mockHandlers} />);
          expect(screen.getByLabelText(/agent name/i })).toHaveValue('');
        });

        it('should handle missing config object', () => {
          render(<AgentStudio agent={{ ...mockAgent, config: undefined }} {...mockHandlers} />);
          expect(screen.getByLabelText(/temperature/i })).toBeInTheDocument();
        });
      });
    });
    ```

    Target: 25+ tests covering render, form inputs, skills, configuration, actions, edge cases
  </action>
  <verify>
    <automated>cd frontend-nextjs && npm test -- tests/agents/test-agent-studio.test.tsx --coverage --collectCoverageFrom=components/Agents/AgentStudio.tsx --passWithNoTests</automated>
  </verify>
  <done>
    test-agent-studio.test.tsx created with 25+ tests
    Tests cover render, form inputs, skills, configuration, actions, edge cases
    Coverage reaches 70%+ for AgentStudio component
  </done>
</task>

<task type="auto">
  <name>Task 4: Create AgentTerminal component tests</name>
  <files>
    frontend-nextjs/tests/agents/test-agent-terminal.test.tsx
    frontend-nextjs/components/Agents/AgentTerminal.tsx
  </files>
  <read_first>
    frontend-nextjs/components/Agents/AgentTerminal.tsx
    frontend-nextjs/tests/agents/test-agent-studio.test.tsx
    frontend-nextjs/jest.setup.js
  </read_first>
  <action>
    Create test-agent-terminal.test.tsx with comprehensive AgentTerminal tests:

    File: frontend-nextjs/tests/agents/test-agent-terminal.test.tsx

    Test structure:
    ```typescript
    import { render, screen, fireEvent, waitFor } from '@testing-library/react';
    import userEvent from '@testing-library/user-event';
    import { AgentTerminal } from '@/components/Agents/AgentTerminal';

    describe('AgentTerminal Component', () => {
      const mockMessages = [
        { id: '1', role: 'user', content: 'Hello agent', timestamp: '2026-04-11T10:00:00Z' },
        { id: '2', role: 'assistant', content: 'Hello user', timestamp: '2026-04-11T10:00:01Z' }
      ];

      const mockHandlers = {
        onSendMessage: jest.fn(),
        onClear: jest.fn()
      };

      afterEach(() => {
        jest.clearAllMocks();
      });

      // Render tests (4-5 tests)
      describe('Rendering', () => {
        it('should render terminal container', () => {
          render(<AgentTerminal messages={mockMessages} {...mockHandlers} />);
          expect(screen.getByTestId('agent-terminal')).toBeInTheDocument();
        });

        it('should render message list', () => {
          render(<AgentTerminal messages={mockMessages} {...mockHandlers} />);
          expect(screen.getByText('Hello agent')).toBeInTheDocument();
          expect(screen.getByText('Hello user')).toBeInTheDocument();
        });

        it('should render message input field', () => {
          render(<AgentTerminal messages={mockMessages} {...mockHandlers} />);
          expect(screen.getByRole('textbox')).toBeInTheDocument();
        });

        it('should render send button', () => {
          render(<AgentTerminal messages={mockMessages} {...mockHandlers} />);
          expect(screen.getByRole('button', { name: /send/i })).toBeInTheDocument();
        });

        it('should render clear button', () => {
          render(<AgentTerminal messages={mockMessages} {...mockHandlers} />);
          expect(screen.getByRole('button', { name: /clear/i })).toBeInTheDocument();
        });
      });

      // Message display tests (4-5 tests)
      describe('Message Display', () => {
        it('should display user messages with correct styling', () => {
          render(<AgentTerminal messages={mockMessages} {...mockHandlers} />);
          const userMessage = screen.getByText('Hello agent').closest('.message-user');
          expect(userMessage).toBeInTheDocument();
        });

        it('should display assistant messages with correct styling', () => {
          render(<AgentTerminal messages={mockMessages} {...mockHandlers} />);
          const assistantMessage = screen.getByText('Hello user').closest('.message-assistant');
          expect(assistantMessage).toBeInTheDocument();
        });

        it('should show timestamps for messages', () => {
          render(<AgentTerminal messages={mockMessages} {...mockHandlers} />);
          expect(screen.getByText(/10:00:00/)).toBeInTheDocument();
        });

        it('should render empty state when no messages', () => {
          render(<AgentTerminal messages={[]} {...mockHandlers} />);
          expect(screen.getByText(/no messages/i)).toBeInTheDocument();
        });
      });

      // Message input tests (4-5 tests)
      describe('Message Input', () => {
        it('should update input value on typing', async () => {
          render(<AgentTerminal messages={mockMessages} {...mockHandlers} />);
          const input = screen.getByRole('textbox');
          await userEvent.type(input, 'New message');
          expect(input).toHaveValue('New message');
        });

        it('should call onSendMessage when send button clicked', async () => {
          render(<AgentTerminal messages={mockMessages} {...mockHandlers} />);
          const input = screen.getByRole('textbox');
          await userEvent.type(input, 'Test message');
          await userEvent.click(screen.getByRole('button', { name: /send/i }));
          expect(mockHandlers.onSendMessage).toHaveBeenCalledWith('Test message');
        });

        it('should send message on Enter key press', async () => {
          render(<AgentTerminal messages={mockMessages} {...mockHandlers} />);
          const input = screen.getByRole('textbox');
          await userEvent.type(input, 'Test message{Enter}');
          expect(mockHandlers.onSendMessage).toHaveBeenCalledWith('Test message');
        });

        it('should clear input after sending', async () => {
          render(<AgentTerminal messages={mockMessages} {...mockHandlers} />);
          const input = screen.getByRole('textbox');
          await userEvent.type(input, 'Test message');
          await userEvent.click(screen.getByRole('button', { name: /send/i }));
          await waitFor(() => {
            expect(input).toHaveValue('');
          });
        });

        it('should not send empty messages', async () => {
          render(<AgentTerminal messages={mockMessages} {...mockHandlers} />);
          await userEvent.click(screen.getByRole('button', { name: /send/i }));
          expect(mockHandlers.onSendMessage).not.toHaveBeenCalled();
        });
      });

      // Clear functionality tests (2-3 tests)
      describe('Clear Functionality', () => {
        it('should call onClear when clear button clicked', async () => {
          render(<AgentTerminal messages={mockMessages} {...mockHandlers} />);
          await userEvent.click(screen.getByRole('button', { name: /clear/i }));
          expect(mockHandlers.onClear).toHaveBeenCalled();
        });

        it('should show confirmation before clearing', async () => {
          render(<AgentTerminal messages={mockMessages} {...mockHandlers} />);
          await userEvent.click(screen.getByRole('button', { name: /clear/i }));
          await waitFor(() => {
            expect(screen.getByText(/clear all messages/i)).toBeInTheDocument();
          });
        });
      });

      // Auto-scroll tests (2-3 tests)
      describe('Auto-scroll', () => {
        it('should auto-scroll to bottom when new message arrives', async () => {
          const { container } = render(<AgentTerminal messages={[]} {...mockHandlers} />);
          const terminal = container.querySelector('[data-testid="agent-terminal"]');
          const initialScrollTop = terminal.scrollTop;
          // Simulate new message
          render(<AgentTerminal messages={[...mockMessages, { id: '3', role: 'assistant', content: 'New', timestamp: '2026-04-11T10:00:02Z' }]} {...mockHandlers} />);
          await waitFor(() => {
            expect(terminal.scrollTop).toBeGreaterThan(initialScrollTop);
          });
        });
      });

      // Edge cases (2-3 tests)
      describe('Edge Cases', () => {
        it('should handle null messages gracefully', () => {
          render(<AgentTerminal messages={null} {...mockHandlers} />);
          expect(screen.getByRole('textbox')).toBeInTheDocument();
        });

        it('should handle very long messages with scrolling', () => {
          const longMessage = 'A'.repeat(10000);
          render(<AgentTerminal messages={[{ id: '1', role: 'user', content: longMessage, timestamp: '2026-04-11T10:00:00Z' }]} {...mockHandlers} />);
          expect(screen.getByText(longMessage.substring(0, 100))).toBeInTheDocument();
        });
      });
    });
    ```

    Target: 20+ tests covering render, message display, input, clear, auto-scroll, edge cases
  </action>
  <verify>
    <automated>cd frontend-nextjs && npm test -- tests/agents/test-agent-terminal.test.tsx --coverage --collectCoverageFrom=components/Agents/AgentTerminal.tsx --passWithNoTests</automated>
  </verify>
  <done>
    test-agent-terminal.test.tsx created with 20+ tests
    Tests cover render, message display, input, clear, auto-scroll, edge cases
    Coverage reaches 70%+ for AgentTerminal component
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Test creation has no security impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | Test creation has no security impact |
</threat_model>

<verification>
1. All 4 test files created in tests/agents/ directory
2. Each test file has 20+ tests
3. Tests use React Testing Library patterns (getByRole, getByLabelText)
4. Tests cover render, interactions, state, edge cases
5. Coverage reaches 70%+ for each tested component
</verification>

<success_criteria>
1. AgentCard component has 70%+ coverage
2. AgentManager component has 70%+ coverage
3. AgentStudio component has 70%+ coverage
4. AgentTerminal component has 70%+ coverage
5. All tests follow React Testing Library patterns
</success_criteria>

<output>
After completion, create `.planning/phases/254-frontend-coverage-baseline/254-02-SUMMARY.md`
</output>
