---
phase: 105-frontend-component-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/components/canvas/__tests__/agent-request-prompt.test.tsx
  - frontend-nextjs/components/canvas/__tests__/operation-error-guide.test.tsx
autonomous: true

must_haves:
  truths:
    - "AgentRequestPrompt component renders with 50%+ coverage including loading, urgency, and expiration states"
    - "OperationErrorGuide component renders with 50%+ coverage including error types, resolutions, and technical details"
    - "Both components use user-centric queries (getByRole, getByLabelText) not implementation details"
    - "Accessibility trees are tested for role='log', aria-live, and JSON state serialization"
  artifacts:
    - path: frontend-nextjs/components/canvas/__tests__/agent-request-prompt.test.tsx
      provides: AgentRequestPrompt component tests (50+ tests)
      min_lines: 400
    - path: frontend-nextjs/components/canvas/__tests__/operation-error-guide.test.tsx
      provides: OperationErrorGuide component tests (50+ tests)
      min_lines: 400
  key_links:
    - from: agent-request-prompt.test.tsx
      to: AgentRequestPrompt.tsx
      via: import statement and render calls
      pattern: "import.*from.*AgentRequestPrompt|render.*AgentRequestPrompt"
    - from: operation-error-guide.test.tsx
      to: OperationErrorGuide.tsx
      via: import statement and render calls
      pattern: "import.*from.*OperationErrorGuide|render.*OperationErrorGuide"
---

<objective>
Create comprehensive React Testing Library tests for AgentRequestPrompt and OperationErrorGuide canvas guidance components.

This plan tests two of the five canvas guidance components that enable real-time agent operation visibility and error resolution. These components are critical for user-agent interaction quality.

The tests follow React Testing Library best practices: user-centric queries (getByRole, getByLabelText), testing behavior not implementation, and accessibility tree validation for AI agent consumption.

Output: 100+ tests across 2 files (50+ each) achieving 50%+ coverage for both components.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend-nextjs/components/canvas/AgentRequestPrompt.tsx
@frontend-nextjs/components/canvas/OperationErrorGuide.tsx
@frontend-nextjs/components/canvas/__tests__/agent-operation-tracker.test.tsx
@frontend-nextjs/components/canvas/__tests__/canvas-accessibility-tree.test-utils.tsx
@frontend-nextjs/jest.config.js
@frontend-nextjs/components/canvas/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Create AgentRequestPrompt component tests</name>
  <files>frontend-nextjs/components/canvas/__tests__/agent-request-prompt.test.tsx</files>
  <action>
Create agent-request-prompt.test.tsx with 50+ tests covering:

1. **Rendering Tests** (10 tests):
   - Renders null when no request data
   - Renders loading state with skeleton animation
   - Renders header with agent name and request type
   - Renders urgency badge (low, medium, high, blocking)
   - Renders expiration countdown timer
   - Handles expired state (disabled controls)
   - Renders explanation section
   - Renders context (operation, impact, alternatives)
   - Suggested option displays "Suggested" badge
   - Governance footer renders (audit log, signature, revocation)

2. **User Interaction Tests** (15 tests):
   - Clicking option selects it
   - Selected option shows checkmark
   - Clicking submit button sends response via WebSocket
   - Submit button calls REST API (/api/agent-guidance/request/:id/respond)
   - Submit button triggers onResponse callback
   - Disabled state when expired
   - Disabled state when responding
   - Keyboard navigation (Tab, Enter, Space)
   - Multiple options can be selected before submit
   - Re-selecting same option changes selection

3. **Accessibility Tree Tests** (10 tests):
   - Hidden accessibility div with role="log"
   - aria-live="polite" attribute
   - aria-label="Agent request prompt"
   - display:none style
   - data-canvas-state="agent_request_prompt"
   - data-request-id attribute
   - data-request-type attribute
   - data-urgency attribute
   - data-options-count attribute
   - JSON state serialization (full request + user_decision)

4. **Edge Cases Tests** (10 tests):
   - Empty options array
   - Missing suggested_option (no default selection)
   - No expiration (no timer displayed)
   - Empty context object
   - Missing alternatives in context
   - Very long option labels (truncation)
   - Very long explanation (scrolling)
   - WebSocket send failure handling
   - API failure handling (404, 500)
   - Multiple requests (requestId filtering)

5. **WebSocket Integration Tests** (5 tests):
   - Subscribes to 'agent:request' messages
   - Filters by requestId if specified
   - Pre-selects suggested option
   - Sets up expiration timer
   - Cleans up event listeners on unmount

**Test Patterns:**
- Use screen.getByRole('button', { name: /option label/i }) for option selection
- Use screen.getByText(/urgency/i) for urgency badge
- Use container.querySelector('[role="log"]') for accessibility tree
- Mock useWebSocket hook: { socket: { send: jest.fn() }, connected: true }
- Mock fetch for API calls

**File Location:** frontend-nextjs/components/canvas/__tests__/agent-request-prompt.test.tsx
  </action>
  <verify>
cd frontend-nextjs && npm test -- agent-request-prompt --coverage --watchAll=false --passWithNoTests
  </verify>
  <done>
50+ tests created, all passing, 50%+ coverage for AgentRequestPrompt component
  </done>
</task>

<task type="auto">
  <name>Create OperationErrorGuide component tests</name>
  <files>frontend-nextjs/components/canvas/__tests__/operation-error-guide.test.tsx</files>
  <action>
Create operation-error-guide.test.tsx with 50+ tests covering:

1. **Rendering Tests** (10 tests):
   - Renders null when no error data
   - Renders error header with icon based on type
   - Renders error message and code
   - Renders agent analysis section (what happened, why, impact)
   - Renders suggested resolutions list
   - Suggested resolution highlighted with blue border
   - Renders resolution title and description
   - Renders resolution steps as numbered list
   - Agent can fix badge displays
   - Technical details collapsible section

2. **User Interaction Tests** (12 tests):
   - Clicking resolution expands it
   - Clicking expanded resolution collapses it
   - Clicking "Let Agent Fix" button sends resolution
   - Clicking "Fix Manually" button sends resolution
   - Selected resolution shows ring-2 ring-blue-500
   - WebSocket message sent on selection
   - onResolutionSelect callback triggered
   - Multiple resolutions can be expanded independently
   - Technical details <details> toggle works
   - Keyboard navigation for resolution buttons

3. **Error Type Tests** (8 tests):
   - permission_denied shows lock icon
   - auth_expired shows key icon
   - network_error shows globe icon
   - rate_limit shows clock icon
   - invalid_input shows warning icon
   - resource_not_found shows search icon
   - Unknown type shows X icon
   - Error message renders correctly for all types

4. **Accessibility Tree Tests** (8 tests):
   - Hidden accessibility div with role="alert"
   - aria-live="assertive" attribute
   - aria-label="Operation error guide"
   - display:none style
   - data-canvas-state="operation_error_guide"
   - data-error-type attribute
   - data-error-code attribute
   - JSON state serialization (error + resolutions)

5. **Edge Cases Tests** (8 tests):
   - Empty resolutions array
   - Missing suggested_resolution (no default expansion)
   - Very long error message (wrapping)
   - Very long resolution steps (scrolling)
   - Missing agent_analysis fields
   - Steps array empty
   - WebSocket send failure handling
   - Multiple errors (operationId filtering)

6. **Resolution Selection Tests** (4 tests):
   - Selecting resolution marks it visually
   - Agent can fix button shows correct text
   - Manual fix button shows correct text
   - Selection persists across re-renders

**Test Patterns:**
- Use screen.getByRole('button', { name: /resolution title/i }) for resolution selection
- Use screen.getByText(/agent analysis/i) for analysis section
- Use container.querySelector('[role="alert"]') for accessibility tree
- Mock useWebSocket hook: { socket: { send: jest.fn() }, connected: true }

**File Location:** frontend-nextjs/components/canvas/__tests__/operation-error-guide.test.tsx
  </action>
  <verify>
cd frontend-nextjs && npm test -- operation-error-guide --coverage --watchAll=false --passWithNoTests
  </verify>
  <done>
50+ tests created, all passing, 50%+ coverage for OperationErrorGuide component
  </done>
</task>

</tasks>

<verification>
Run coverage report to verify 50%+ coverage for both AgentRequestPrompt and OperationErrorGuide:

```bash
cd frontend-nextjs
npm test -- coverage --watchAll=false --passWithNoTests
```

Check coverage/coverage-summary.json for:
- components/canvas/AgentRequestPrompt.tsx: 50%+
- components/canvas/OperationErrorGuide.tsx: 50%+

Verify all tests use user-centric queries (getByRole, getByLabelText) not implementation details (container.querySelector for elements not meant for users).
</verification>

<success_criteria>
1. 100+ tests created across 2 files (50+ each)
2. All tests passing (100% pass rate)
3. 50%+ coverage achieved for AgentRequestPrompt component
4. 50%+ coverage achieved for OperationErrorGuide component
5. All queries use user-centric selectors (getByRole, getByLabelText)
6. Accessibility tree tests pass for both components
</success_criteria>

<output>
After completion, create `.planning/phases/105-frontend-component-tests/105-01-SUMMARY.md` with:
- Test count (100+ tests)
- Coverage percentages for each component
- Any bugs or issues found
- Files created list
</output>
