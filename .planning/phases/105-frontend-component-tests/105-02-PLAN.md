---
phase: 105-frontend-component-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/components/canvas/__tests__/line-chart.test.tsx
  - frontend-nextjs/components/canvas/__tests__/bar-chart.test.tsx
  - frontend-nextjs/components/canvas/__tests__/pie-chart.test.tsx
autonomous: true

must_haves:
  truths:
    - "LineChart component renders with 50%+ coverage including data rendering, tooltips, and accessibility"
    - "BarChart component renders with 50%+ coverage including data rendering, axes, and accessibility"
    - "PieChart component renders with 50%+ coverage including data rendering, legend, and accessibility"
    - "All chart components register with canvas state API for AI agent access"
  artifacts:
    - path: frontend-nextjs/components/canvas/__tests__/line-chart.test.tsx
      provides: LineChart component tests (30+ tests)
      min_lines: 300
    - path: frontend-nextjs/components/canvas/__tests__/bar-chart.test.tsx
      provides: BarChart component tests (30+ tests)
      min_lines: 300
    - path: frontend-nextjs/components/canvas/__tests__/pie-chart.test.tsx
      provides: PieChart component tests (30+ tests)
      min_lines: 300
  key_links:
    - from: line-chart.test.tsx
      to: LineChart.tsx
      via: import statement and render calls
      pattern: "import.*from.*LineChart|render.*LineChartCanvas"
    - from: bar-chart.test.tsx
      to: BarChart.tsx
      via: import statement and render calls
      pattern: "import.*from.*BarChart|render.*BarChartCanvas"
    - from: pie-chart.test.tsx
      to: PieChart.tsx
      via: import statement and render calls
      pattern: "import.*from.*PieChart|render.*PieChartCanvas"
---

<objective>
Create comprehensive React Testing Library tests for LineChart, BarChart, and PieChart canvas visualization components.

This plan tests the three chart components that provide data visualization capabilities in the canvas system. These components use Recharts library and integrate with the canvas state API for AI agent accessibility.

The tests verify chart rendering, data accuracy, accessibility tree registration, and responsive container behavior.

Output: 90+ tests across 3 files (30+ each) achieving 50%+ coverage for all chart components.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend-nextjs/components/canvas/LineChart.tsx
@frontend-nextjs/components/canvas/BarChart.tsx
@frontend-nextjs/components/canvas/PieChart.tsx
@frontend-nextjs/components/canvas/__tests__/agent-operation-tracker.test.tsx
@frontend-nextjs/components/canvas/types/index.ts
@frontend-nextjs/jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Create LineChart component tests</name>
  <files>frontend-nextjs/components/canvas/__tests__/line-chart.test.tsx</files>
  <action>
Create line-chart.test.tsx with 30+ tests covering:

1. **Rendering Tests** (8 tests):
   - Renders without crashing
   - Renders title if provided
   - Renders ResponsiveContainer wrapper
   - Renders Recharts LineChart component
   - Renders XAxis with timestamp data
   - Renders YAxis with value data
   - Renders Tooltip
   - Renders Legend

2. **Data Rendering Tests** (8 tests):
   - Renders data points correctly
   - Handles empty data array
   - Handles single data point
   - Handles many data points (50+)
   - Handles null/undefined values
   - Handles negative values
   - Handles zero values
   - Custom color prop applied

3. **Canvas State API Tests** (8 tests):
   - Registers with window.atom.canvas on mount
   - Creates canvas_id with timestamp
   - getState returns canvas state for matching ID
   - getAllStates includes line chart state
   - State has correct chart_type: 'line'
   - State has correct component: 'line_chart'
   - State includes data_points array
   - Cleans up on unmount (original functions restored)

4. **Accessibility Tests** (6 tests):
   - Accessibility tree has role="log"
   - aria-live="polite" attribute
   - aria-label includes "line chart"
   - display:none style
   - data-canvas-state attribute
   - JSON state serializes correctly

**Test Patterns:**
- Use screen.getByText(/title/i) for title rendering
- Use container.querySelector('svg') for Recharts output
- Mock window.atom.canvas before test
- Use waitFor for useEffect registration

**File Location:** frontend-nextjs/components/canvas/__tests__/line-chart.test.tsx
  </action>
  <verify>
cd frontend-nextjs && npm test -- line-chart --coverage --watchAll=false --passWithNoTests
  </verify>
  <done>
30+ tests created, all passing, 50%+ coverage for LineChart component
  </done>
</task>

<task type="auto">
  <name>Create BarChart component tests</name>
  <files>frontend-nextjs/components/canvas/__tests__/bar-chart.test.tsx</files>
  <action>
Create bar-chart.test.tsx with 30+ tests covering:

1. **Rendering Tests** (8 tests):
   - Renders without crashing
   - Renders title if provided
   - Renders ResponsiveContainer wrapper
   - Renders Recharts BarChart component
   - Renders XAxis with name data
   - Renders YAxis with value data
   - Renders Tooltip
   - Renders Legend

2. **Data Rendering Tests** (8 tests):
   - Renders bars for each data point
   - Handles empty data array
   - Handles single data point
   - Handles many data points (50+)
   - Handles null/undefined values
   - Handles negative values
   - Handles zero values
   - Custom color prop applied to bars

3. **Canvas State API Tests** (8 tests):
   - Registers with window.atom.canvas on mount
   - Creates canvas_id with timestamp
   - getState returns canvas state for matching ID
   - getAllStates includes bar chart state
   - State has correct chart_type: 'bar'
   - State has correct component: 'bar_chart'
   - State includes data_points array
   - Cleans up on unmount (original functions restored)

4. **Accessibility Tests** (6 tests):
   - Accessibility tree has role="log"
   - aria-live="polite" attribute
   - aria-label includes "bar chart"
   - display:none style
   - data-canvas-state attribute
   - JSON state serializes correctly

**Test Patterns:**
- Use screen.getByText(/title/i) for title rendering
- Use container.querySelector('svg') for Recharts output
- Mock window.atom.canvas before test
- Use waitFor for useEffect registration

**File Location:** frontend-nextjs/components/canvas/__tests__/bar-chart.test.tsx
  </action>
  <verify>
cd frontend-nextjs && npm test -- bar-chart --coverage --watchAll=false --passWithNoTests
  </verify>
  <done>
30+ tests created, all passing, 50%+ coverage for BarChart component
  </done>
</task>

<task type="auto">
  <name>Create PieChart component tests</name>
  <files>frontend-nextjs/components/canvas/__tests__/pie-chart.test.tsx</files>
  <action>
Create pie-chart.test.tsx with 30+ tests covering:

1. **Rendering Tests** (8 tests):
   - Renders without crashing
   - Renders title if provided
   - Renders ResponsiveContainer wrapper
   - Renders Recharts PieChart component
   - Renders Pie with data
   - Renders Tooltip
   - Renders Legend
   - Renders Cell components with colors

2. **Data Rendering Tests** (8 tests):
   - Renders pie slices for each data point
   - Handles empty data array
   - Handles single data point
   - Handles many data points (10+)
   - Handles null/undefined values
   - Handles zero values (empty slice)
   - Color rotation works (6 colors cycle)
   - Labels display correctly (name: value)

3. **Canvas State API Tests** (8 tests):
   - Registers with window.atom.canvas on mount
   - Creates canvas_id with timestamp
   - getState returns canvas state for matching ID
   - getAllStates includes pie chart state
   - State has correct chart_type: 'pie'
   - State has correct component: 'pie_chart'
   - State includes data_points array
   - Cleans up on unmount (original functions restored)

4. **Accessibility Tests** (6 tests):
   - Accessibility tree has role="log"
   - aria-live="polite" attribute
   - aria-label includes "pie chart"
   - display:none style
   - data-canvas-state attribute
   - JSON state serializes correctly

**Test Patterns:**
- Use screen.getByText(/title/i) for title rendering
- Use container.querySelector('svg') for Recharts output
- Mock window.atom.canvas before test
- Use waitFor for useEffect registration

**File Location:** frontend-nextjs/components/canvas/__tests__/pie-chart.test.tsx
  </action>
  <verify>
cd frontend-nextjs && npm test -- pie-chart --coverage --watchAll=false --passWithNoTests
  </verify>
  <done>
30+ tests created, all passing, 50%+ coverage for PieChart component
  </done>
</task>

</tasks>

<verification>
Run coverage report to verify 50%+ coverage for all chart components:

```bash
cd frontend-nextjs
npm test -- coverage --watchAll=false --passWithNoTests
```

Check coverage/coverage-summary.json for:
- components/canvas/LineChart.tsx: 50%+
- components/canvas/BarChart.tsx: 50%+
- components/canvas/PieChart.tsx: 50%+

Verify canvas state API registration tests pass for all charts.
</verification>

<success_criteria>
1. 90+ tests created across 3 files (30+ each)
2. All tests passing (100% pass rate)
3. 50%+ coverage achieved for all three chart components
4. Canvas state API registration verified for all charts
5. Accessibility tree tests pass for all charts
</success_criteria>

<output>
After completion, create `.planning/phases/105-frontend-component-tests/105-02-SUMMARY.md` with:
- Test count (90+ tests)
- Coverage percentages for each component
- Any bugs or issues found
- Files created list
</output>
