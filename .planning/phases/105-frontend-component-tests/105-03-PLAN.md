---
phase: 105-frontend-component-tests
plan: 03
type: execute
wave: 2
depends_on: [105-01, 105-02]
files_modified:
  - frontend-nextjs/components/canvas/__tests__/interactive-form.test.tsx
  - frontend-nextjs/components/canvas/__tests__/view-orchestrator.test.tsx
autonomous: true

must_haves:
  truths:
    - "InteractiveForm component renders with 50%+ coverage including field types, validation, and submission"
    - "ViewOrchestrator component renders with 50%+ coverage including layouts, views, and guidance"
    - "Both components handle loading, empty, and error states correctly"
    - "User-centric queries used throughout (getByRole, getByLabelText)"
  artifacts:
    - path: frontend-nextjs/components/canvas/__tests__/interactive-form.test.tsx
      provides: InteractiveForm component tests (40+ tests)
      min_lines: 400
    - path: frontend-nextjs/components/canvas/__tests__/view-orchestrator.test.tsx
      provides: ViewOrchestrator component tests (40+ tests)
      min_lines: 500
  key_links:
    - from: interactive-form.test.tsx
      to: InteractiveForm.tsx
      via: import statement and render calls
      pattern: "import.*from.*InteractiveForm|render.*InteractiveForm"
    - from: view-orchestrator.test.tsx
      to: ViewOrchestrator.tsx
      via: import statement and render calls
      pattern: "import.*from.*ViewOrchestrator|render.*ViewOrchestrator"
---

<objective>
Create comprehensive React Testing Library tests for InteractiveForm and ViewOrchestrator canvas components.

This plan tests the form component (field validation, submission, error states) and the multi-view layout orchestrator (split views, tabs, canvas guidance). These are complex components with substantial user interaction.

The tests verify form validation rules, submission flows, error handling, view layouts, WebSocket integration, and accessibility.

Output: 80+ tests across 2 files (40+ each) achieving 50%+ coverage for both components.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend-nextjs/components/canvas/InteractiveForm.tsx
@frontend-nextjs/components/canvas/ViewOrchestrator.tsx
@frontend-nextjs/components/canvas/__tests__/agent-operation-tracker.test.tsx
@frontend-nextjs/components/canvas/types/index.ts
@frontend-nextjs/jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Create InteractiveForm component tests</name>
  <files>frontend-nextjs/components/canvas/__tests__/interactive-form.test.tsx</files>
  <action>
Create interactive-form.test.tsx with 40+ tests covering:

1. **Rendering Tests** (8 tests):
   - Renders form with all fields
   - Renders title if provided
   - Renders custom submit label
   - Renders required field indicators (*)
   - Renders field labels correctly
   - Renders placeholders for text inputs
   - Renders select options
   - Renders checkboxes

2. **Field Type Tests** (6 tests):
   - Text input field renders and accepts input
   - Email input field renders and accepts email
   - Number input field renders and accepts numbers
   - Select dropdown renders options
   - Checkbox renders and toggles
   - All field types can exist in same form

3. **Validation Tests** (10 tests):
   - Required field validation on submit
   - Email format validation (pattern)
   - Number min value validation
   - Number max value validation
   - Custom validation message displays
   - Multiple validation errors at once
   - Validation clears when input corrected
   - Submit button disabled during validation errors
   - Empty required field shows error
   - Invalid email format shows error

4. **Submission Tests** (8 tests):
   - Form submit calls onSubmit with data
   - Submit button shows "Submitting..." during submit
   - Submit button disabled during submit
   - Successful submit shows success message
   - Success message auto-hides after 3 seconds
   - Submit failure shows error message
   - Form resets after successful submit
   - Form data includes all field values

5. **Canvas State API Tests** (5 tests):
   - Registers with window.atom.canvas on mount
   - State includes form_schema (fields array)
   - State includes form_data (current values)
   - State includes validation_errors array
   - State updates on input change

6. **Edge Cases Tests** (6 tests):
   - Empty fields array
   - No required fields
   - All fields optional
   - Very long field labels
   - Special characters in validation
   - Multiple submits in rapid succession

**Test Patterns:**
- Use screen.getByLabelText(/field label/i) for field queries
- Use screen.getByRole('button', { name: /submit/i }) for submit button
- Use user.type() for input simulation
- Mock onSubmit promise (resolve/reject)

**File Location:** frontend-nextjs/components/canvas/__tests__/interactive-form.test.tsx
  </action>
  <verify>
cd frontend-nextjs && npm test -- interactive-form --coverage --watchAll=false --passWithNoTests
  </verify>
  <done>
40+ tests created, all passing, 50%+ coverage for InteractiveForm component
  </done>
</task>

<task type="auto">
  <name>Create ViewOrchestrator component tests</name>
  <files>frontend-nextjs/components/canvas/__tests__/view-orchestrator.test.tsx</files>
  <action>
Create view-orchestrator.test.tsx with 40+ tests covering:

1. **Rendering Tests** (8 tests):
   - Renders empty state when no views
   - Renders loading message in empty state
   - Renders active views when provided
   - Renders view header with title
   - Renders view status badge (active/background/closed)
   - Renders "Take Control" button
   - Renders canvas guidance panel when provided
   - Renders collapse button for guidance panel

2. **Layout Tests** (6 tests):
   - split_horizontal layout renders side-by-side
   - split_vertical layout renders stacked
   - grid layout renders 2x2 grid
   - tabs layout renders tab buttons
   - Active tab highlighted
   - Layout classes applied correctly

3. **Canvas Guidance Tests** (6 tests):
   - Renders agent guidance panel
   - Displays agent message
   - Displays "What you're seeing" section
   - Displays control buttons
   - Collapse button hides panel
   - Expand button shows panel again

4. **WebSocket Integration Tests** (8 tests):
   - Listens for 'view:switch' messages
   - Updates layout on switch message
   - Updates active_views on switch
   - Handles 'view:activated' messages
   - Handles 'view:closed' messages
   - Handles 'view:guidance_update' messages
   - Sends 'view:takeover' message on Take Control
   - Sends 'view:control_action' on control button

5. **User Interaction Tests** (6 tests):
   - Clicking Take Control sends WebSocket message
   - Take Control calls onViewTakeover callback
   - Clicking control button sends action
   - Tab switching works in tabs layout
   - Guidance panel collapse/expand toggles
   - View status badge colors correct

6. **Accessibility Tests** (6 tests):
   - Empty state has accessibility tree
   - Orchestrator state has accessibility tree
   - role="log" for state tree
   - aria-live="polite" attribute
   - JSON state includes layout
   - JSON state includes active_views array

**Test Patterns:**
- Use screen.getByText(/no active views/i) for empty state
- Use screen.getByRole('button', { name: /take control/i }) for button
- Mock useWebSocket hook with send mock
- Simulate WebSocket messages manually

**File Location:** frontend-nextjs/components/canvas/__tests__/view-orchestrator.test.tsx
  </action>
  <verify>
cd frontend-nextjs && npm test -- view-orchestrator --coverage --watchAll=false --passWithNoTests
  </verify>
  <done>
40+ tests created, all passing, 50%+ coverage for ViewOrchestrator component
  </done>
</task>

</tasks>

<verification>
Run coverage report to verify 50%+ coverage for both components:

```bash
cd frontend-nextjs
npm test -- coverage --watchAll=false --passWithNoTests
```

Check coverage/coverage-summary.json for:
- components/canvas/InteractiveForm.tsx: 50%+
- components/canvas/ViewOrchestrator.tsx: 50%+

Verify all validation states and user interactions are tested.
</verification>

<success_criteria>
1. 80+ tests created across 2 files (40+ each)
2. All tests passing (100% pass rate)
3. 50%+ coverage achieved for InteractiveForm
4. 50%+ coverage achieved for ViewOrchestrator
5. Form validation thoroughly tested
6. View layouts and WebSocket integration tested
</success_criteria>

<output>
After completion, create `.planning/phases/105-frontend-component-tests/105-03-SUMMARY.md` with:
- Test count (80+ tests)
- Coverage percentages for each component
- Any bugs or issues found
- Files created list
</output>
