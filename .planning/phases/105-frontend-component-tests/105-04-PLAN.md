---
phase: 105-frontend-component-tests
plan: 04
type: execute
wave: 2
depends_on: [105-01, 105-02, 105-03]
files_modified:
  - frontend-nextjs/components/canvas/__tests__/integration-connection-guide.test.tsx
  - frontend-nextjs/components/layout/__tests__/layout.test.tsx
autonomous: true

must_haves:
  truths:
    - "IntegrationConnectionGuide component renders with 50%+ coverage including OAuth stages, permissions, and guidance"
    - "Layout components render with 50%+ coverage including responsive behavior and breakpoints"
    - "Responsive design tested with viewport variations"
    - "User-centric queries used for all interactive elements"
  artifacts:
    - path: frontend-nextjs/components/canvas/__tests__/integration-connection-guide.test.tsx
      provides: IntegrationConnectionGuide component tests (40+ tests)
      min_lines: 400
    - path: frontend-nextjs/components/layout/__tests__/layout.test.tsx
      provides: Layout component tests (40+ tests)
      min_lines: 400
  key_links:
    - from: integration-connection-guide.test.tsx
      to: IntegrationConnectionGuide.tsx
      via: import statement and render calls
      pattern: "import.*from.*IntegrationConnectionGuide|render.*IntegrationConnectionGuide"
    - from: layout.test.tsx
      to: layout/Layout.tsx
      via: import statement and render calls
      pattern: "import.*from.*Layout|render.*Layout"
---

<objective>
Create comprehensive React Testing Library tests for IntegrationConnectionGuide and Layout components.

This plan tests the OAuth/integration flow guidance component (5-stage progress, permissions display, browser session preview) and the main layout component (responsive design, navigation, breakpoint behavior).

The tests verify multi-stage progress tracking, permission display with risk levels, responsive layout behavior, and accessibility.

Output: 80+ tests across 2 files (40+ each) achieving 50%+ coverage for both components.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend-nextjs/components/canvas/IntegrationConnectionGuide.tsx
@frontend-nextjs/components/layout/Layout.tsx
@frontend-nextjs/components/canvas/__tests__/agent-operation-tracker.test.tsx
@frontend-nextjs/components/canvas/types/index.ts
@frontend-nextjs/jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Create IntegrationConnectionGuide component tests</name>
  <files>frontend-nextjs/components/canvas/__tests__/integration-connection-guide.test.tsx</files>
  <action>
Create integration-connection-guide.test.tsx with 40+ tests covering:

1. **Rendering Tests** (8 tests):
   - Renders loading state (skeleton) initially
   - Renders integration name in header
   - Renders progress steps (5 stages)
   - Renders stage icons (rocket, lock, phone, check, party)
   - Renders agent guidance section
   - Renders permissions list when provided
   - Renders browser session preview
   - Renders connection status

2. **Progress Stage Tests** (10 tests):
   - Initiating stage shows first step active
   - Authorizing stage shows second step active
   - Callback stage shows third step active
   - Verifying stage shows fourth step active
   - Complete stage shows all steps complete
   - Completed steps show checkmark
   - Active step highlighted in blue
   - Pending steps grayed out
   - Progress line fills between steps
   - Stage transitions update UI

3. **Agent Guidance Tests** (5 tests):
   - Renders current step title
   - Renders explanation text
   - Renders "Why this is needed" section
   - Renders "What's next" section
   - Guidance updates on WebSocket message

4. **Permissions Tests** (6 tests):
   - Renders permission list
   - Shows permission scope
   - Shows risk level badge (low/medium/high)
   - High risk shows red badge
   - Medium risk shows yellow badge
   - Low risk shows green badge

5. **Permissions Expansion Tests** (4 tests):
   - Clicking permission expands details
   - Expanded permission shows why_needed text
   - Clicking again collapses
   - Multiple permissions can be expanded independently

6. **Browser Session Tests** (4 tests):
   - Renders browser session preview in authorizing stage
   - Shows "You can see" badge when user_can_see true
   - Shows "Agent controlled" badge when user_can_see false
   - Open Authorization Page button renders with URL

7. **Connection Status Tests** (4 tests):
   - Shows connection status text
   - Error status shows in red
   - Complete status shows in green
   - Retry button appears when retry_available true

8. **Completion Tests** (4 tests):
   - Renders completion message when stage complete
   - Shows integration name in message
   - Calls onComplete callback when complete
   - Completion message styled green

9. **Accessibility Tests** (4 tests):
   - Loading state has accessibility tree
   - Active state has accessibility tree
   - JSON state includes stage
   - JSON state includes permissions_count

10. **WebSocket Tests** (4 tests):
    - Subscribes to canvas:update messages
    - Filters by integrationId if specified
    - Updates stage on message
    - Cleans up event listeners

**Test Patterns:**
- Use screen.getByText(/stage name/i) for step indicators
- Use screen.getByRole('button', { name: /permission scope/i }) for permission expansion
- Mock useWebSocket hook
- Mock window.open for authorization URL

**File Location:** frontend-nextjs/components/canvas/__tests__/integration-connection-guide.test.tsx
  </action>
  <verify>
cd frontend-nextjs && npm test -- integration-connection-guide --coverage --watchAll=false --passWithNoTests
  </verify>
  <done>
40+ tests created, all passing, 50%+ coverage for IntegrationConnectionGuide component
  </done>
</task>

<task type="auto">
  <name>Create Layout component tests</name>
  <files>frontend-nextjs/components/layout/__tests__/layout.test.tsx</files>
  <action>
Create layout.test.tsx with 40+ tests covering:

1. **Rendering Tests** (8 tests):
   - Renders without crashing
   - Renders header/navigation
   - Renders main content area
   - Renders footer if present
   - Renders sidebar on desktop
   - Hides sidebar on mobile initially
   - Renders mobile menu toggle
   - Renders user menu if authenticated

2. **Responsive Tests** (8 tests):
   - Desktop viewport (1024px+) shows sidebar
   - Tablet viewport (768px-1023px) shows collapsible sidebar
   - Mobile viewport (<768px) hides sidebar
   - Mobile menu toggle opens sidebar
   - Mobile menu toggle closes sidebar
   - Overlay appears on mobile when sidebar open
   - Clicking overlay closes sidebar
   - Layout adapts to viewport resize

3. **Navigation Tests** (6 tests):
   - Navigation links render correctly
   - Active link highlighted
   - Clicking nav link navigates
   - Logo links to home
   - User menu renders dropdown
   - User menu has logout option

4. **Sidebar Tests** (5 tests):
   - Renders sidebar navigation items
   - Sidebar items have icons
   - Active sidebar item highlighted
   - Clicking sidebar item navigates
   - Sidebar collapse/expand works

5. **Header Tests** (5 tests):
   - Renders app title/logo
   - Renders search input if enabled
   - Renders notification bell
   - Renders user avatar
   - Sticky header works on scroll

6. **Accessibility Tests** (4 tests):
   - Navigation has role="navigation"
   - Main landmark has role="main"
   - Skip link works for keyboard navigation
   - ARIA labels on interactive elements

7. **Edge Cases Tests** (4 tests):
   - Handles missing user (not authenticated)
   - Handles no navigation items
   - Handles very long page titles
   - Handles rapid viewport resizing

**Test Patterns:**
- Use screen.getByRole('navigation') for nav
- Use screen.getByRole('button', { name: /menu/i }) for menu toggle
- Use window.innerWidth to test viewports
- Mock Next.js router
- Mock auth session

**File Location:** frontend-nextjs/components/layout/__tests__/layout.test.tsx
  </action>
  <verify>
cd frontend-nextjs && npm test -- layout --coverage --watchAll=false --passWithNoTests
  </verify>
  <done>
40+ tests created, all passing, 50%+ coverage for Layout component
  </done>
</task>

</tasks>

<verification>
Run coverage report to verify 50%+ coverage for both components:

```bash
cd frontend-nextjs
npm test -- coverage --watchAll=false --passWithNoTests
```

Check coverage/coverage-summary.json for:
- components/canvas/IntegrationConnectionGuide.tsx: 50%+
- components/layout/Layout.tsx: 50%+

Verify responsive design tests use viewport variations.
</verification>

<success_criteria>
1. 80+ tests created across 2 files (40+ each)
2. All tests passing (100% pass rate)
3. 50%+ coverage achieved for IntegrationConnectionGuide
4. 50%+ coverage achieved for Layout
5. OAuth flow stages all tested
6. Responsive behavior tested with viewport variations
</success_criteria>

<output>
After completion, create `.planning/phases/105-frontend-component-tests/105-04-SUMMARY.md` with:
- Test count (80+ tests)
- Coverage percentages for each component
- Any bugs or issues found
- Files created list
</output>
