---
phase: 086-property-based-testing-core-services
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/property_tests/governance/test_governance_cache_invariants.py
  - backend/core/governance_cache.py
autonomous: true

must_haves:
  truths:
    - "All governance cache property tests pass with 100% success rate"
    - "Cache invariants verified: idempotency, TTL expiration, LRU eviction, thread safety"
    - "Performance invariants verified: <10ms lookup, >90% hit rate achievable"
    - "Any bugs discovered by Hypothesis are documented and fixed"
    - "Test coverage report shows governance_cache.py has 90%+ coverage"
  artifacts:
    - path: "backend/tests/property_tests/governance/test_governance_cache_invariants.py"
      provides: "Comprehensive property tests for governance cache invariants"
      contains: "Tests for get/set, TTL, LRU, thread safety, performance, statistics"
    - path: "backend/core/governance_cache.py"
      provides: "Production governance cache implementation"
      min_lines: 200
  key_links:
    - from: "backend/tests/property_tests/governance/test_governance_cache_invariants.py"
      to: "backend/core/governance_cache.py"
      via: "Direct import and instantiation of GovernanceCache"
      pattern: "from core.governance_cache import GovernanceCache"
---

<objective>
Verify and expand property-based tests for GovernanceCache to ensure all critical invariants are tested and passing.

Purpose: The governance cache is performance-critical (<10ms P99 target) and security-sensitive (governs agent actions). Property-based tests with Hypothesis generate hundreds of random inputs to find edge cases that unit tests miss.

Output: Working property test suite with documented invariants, any bugs fixed, coverage report showing 90%+ coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
@.planning/phases/086-property-based-testing-core-services/086-RESEARCH.md

# Existing property tests to review
@backend/tests/property_tests/governance/test_governance_cache_invariants.py

# Implementation under test
@backend/core/governance_cache.py
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/086-property-based-testing-core-services/086-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run and verify governance cache property tests</name>
  <files>
    backend/tests/property_tests/governance/test_governance_cache_invariants.py
    backend/core/governance_cache.py
  </files>
  <action>
    Run existing property tests for governance cache:
    ```bash
    cd backend && ./venv/bin/pytest tests/property_tests/governance/test_governance_cache_invariants.py -v --hypothesis-show-statistics
    ```

    If tests fail:
    1. Analyze the minimal counterexample provided by Hypothesis
    2. Determine if it's a test bug (invalid assumption) or implementation bug
    3. For implementation bugs: fix in governance_cache.py, document with BUG_FIX comment
    4. For test bugs: fix the test strategy or assumption

    Key invariants to verify:
    - Cache miss returns None (test_cache_miss_returns_none)
    - Set followed by get returns cached value (test_cache_set_then_get)
    - Keys are unique per (agent_id, action_type) (test_cache_key_uniqueness)
    - TTL expiration works correctly (test_cache_expires_after_ttl)
    - LRU evicts oldest entries (test_lru_eviction_oldest_first)
    - Thread safety for concurrent access (test_concurrent_read_safety, test_concurrent_write_safety)
    - Performance <10ms for lookups (test_lookup_latency_performance)

    For each failing test, document:
    - Minimal counterexample from Hypothesis
    - Root cause analysis
    - Fix applied
  </action>
  <verify>
    All property tests pass: `pytest tests/property_tests/governance/test_governance_cache_invariants.py -v`
    Exit code 0, no failures in output
  </verify>
  <done>
    All governance cache property tests pass with Hypothesis statistics showing successful shrinking
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate coverage report for governance cache</name>
  <files>
    backend/tests/coverage_reports/metrics/coverage.json
    backend/core/governance_cache.py
  </files>
  <action>
    Generate coverage report specifically for governance_cache.py:
    ```bash
    cd backend && ./venv/bin/pytest tests/property_tests/governance/test_governance_cache_invariants.py \
      --cov=core.governance_cache \
      --cov-report=term-missing \
      --cov-report=html \
      --cov-report=json
    ```

    Check coverage percentage meets 90%+ target:
    ```bash
    python -c "import json; data = json.load(open('tests/coverage_reports/metrics/coverage.json')); print(data['files']['core/governance_cache.py']['summary']['percent_covered'])"
    ```

    If coverage <90%, identify missing lines and add tests for uncovered branches:
    - Check HTML report: htmlcov/core_governance_cache_py.html
    - Focus on error handling, edge cases, boundary conditions
    - Add new property tests using @given decorators for missing scenarios

    Document coverage in test file header:
    ```python
    """
    Coverage: XX% (as of YYYY-MM-DD)
    Missing lines: N (descriptions of any gaps)
    """
    ```
  </action>
  <verify>
    Coverage report shows >=90% for governance_cache.py
    Check: Coverage JSON file contains governance_cache.py with percent_covered >= 90
  </verify>
  <done>
    Governance cache has 90%+ coverage from property tests
    Coverage report saved to tests/coverage_reports/metrics/coverage.json
  </done>
</task>

<task type="auto">
  <name>Task 3: Document discovered bugs and invariants</name>
  <files>
    .planning/phases/086-property-based-testing-core-services/086-01-SUMMARY.md
    backend/tests/property_tests/governance/BUG_FINDINGS.md
  </files>
  <action>
    Create documentation of property test findings:

    1. Create BUG_FINDINGS.md with:
       - List of bugs discovered by Hypothesis (if any)
       - Minimal counterexample for each bug
       - Root cause analysis
       - Fix applied
       - Commit reference

    2. Document verified invariants:
       - Idempotency: get(key) returns same value within TTL
       - Exclusivity: Time gaps use > not >= for boundary
       - Consistency: Thread-safe concurrent access
       - Performance: O(1) lookup time maintained
       - Eviction: LRU evicts least recently used

    3. Create 086-01-SUMMARY.md with:
       - Test execution results
       - Coverage achieved
       - Bugs found and fixed
       - Invariants verified
       - Performance measurements (latency percentiles)

    Template for BUG_FINDINGS.md:
    ```markdown
    # Governance Cache Property Test Bug Findings

    ## Bugs Discovered

    ### BUG-001: [Title]
    - **Discovered by**: test_name
    - **Minimal counterexample**: [Hypothesis output]
    - **Root cause**: [Analysis]
    - **Fix**: [Description]
    - **Commit**: [Reference]

    ## Verified Invariants
    - [List of all verified invariants]
    ```
  </action>
  <verify>
    BUG_FINDINGS.md exists in backend/tests/property_tests/governance/
    086-01-SUMMARY.md exists in phase directory
    Both files contain complete documentation
  </verify>
  <done>
    Complete documentation of property test results and findings
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
1. Run full property test suite: `pytest tests/property_tests/governance/ -v --hypothesis-show-statistics`
2. Verify all tests pass (exit code 0)
3. Check coverage >= 90%: `pytest --cov=core.governance_cache --cov-report=json`
4. Verify documentation exists and is complete
</verification>

<success_criteria>
1. All governance cache property tests pass (100% success rate)
2. Coverage >= 90% for governance_cache.py
3. Hypothesis statistics show tests found interesting cases
4. Any bugs discovered are fixed and documented
5. Invariants are documented in BUG_FINDINGS.md
6. Performance requirements verified (<10ms P99 lookup)
</success_criteria>

<output>
After completion, create `.planning/phases/086-property-based-testing-core-services/086-01-SUMMARY.md` with:
- Test execution summary (pass/fail counts)
- Coverage percentage achieved
- Bugs discovered and fixed
- Invariants verified
- Performance measurements
- Next steps (if any gaps remain)
</output>
