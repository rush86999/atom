---
phase: 243-memory-and-performance-bug-discovery
plan: 05
type: execute
wave: 4
depends_on: ["243-01", "243-02", "243-03", "243-04"]
files_modified:
  - .github/workflows/memory-performance-weekly.yml
  - backend/tests/bug_discovery/core/__init__.py
  - backend/docs/MEMORY_PERFORMANCE_BUG_DISCOVERY.md
  - backend/tests/memory_leaks/README.md
  - backend/tests/performance_regression/README.md
autonomous: true

must_haves:
  truths:
    - "Weekly CI workflow runs memory leak, performance regression, and Lighthouse tests"
    - "Workflow runs on Sunday 3 AM UTC schedule (separate from bug-discovery-weekly)"
    - "Memory leak tests run sequentially (-n 1) to avoid interference"
    - "Performance regression tests use --benchmark-compare with --benchmark-compare-fail"
    - "Lighthouse tests include regression detection with baseline comparison"
    - "Weekly reports aggregate memory leaks, performance regressions, Lighthouse alerts"
    - "Documentation covers setup, usage, CI/CD, troubleshooting"
  artifacts:
    - path: ".github/workflows/memory-performance-weekly.yml"
      provides: "Weekly CI workflow for memory/performance bug discovery"
    - path: "backend/docs/MEMORY_PERFORMANCE_BUG_DISCOVERY.md"
      provides: "Comprehensive documentation (300+ lines)"
    - path: "backend/tests/memory_leaks/README.md"
      provides: "Memory leak tests documentation with examples"
    - path: "backend/tests/performance_regression/README.md"
      provides: "Performance regression tests documentation with examples"
    - path: "backend/tests/bug_discovery/core/__init__.py"
      provides: "run_memory_performance_discovery() convenience function"
  key_links:
    - from: ".github/workflows/memory-performance-weekly.yml"
      to: "backend/tests/bug_discovery/core/__init__.py"
      via: "run_memory_performance_discovery() invocation"
      pattern: "python.*run_memory_performance_discovery"
    - from: "backend/docs/MEMORY_PERFORMANCE_BUG_DISCOVERY.md"
      to: ".github/workflows/memory-performance-weekly.yml"
      via: "Workflow documentation reference"
      pattern: "memory-performance-weekly.yml"
    - from: "backend/tests/memory_leaks/README.md"
      to: "backend/tests/memory_leaks/conftest.py"
      via: "memray_session fixture documentation"
      pattern: "memray_session.*fixture"
---

<objective>
Create weekly CI pipeline and comprehensive documentation for memory/performance bug discovery.

**Purpose:** Orchestrate memory leak detection (memray), performance regression (pytest-benchmark), and Lighthouse CI tests in a unified weekly pipeline. Provide comprehensive documentation for setup, usage, CI/CD integration, and troubleshooting. Completes Phase 243 with production-ready infrastructure.

**Output:**
- memory-performance-weekly.yml GitHub Actions workflow (Sunday 3 AM UTC)
- run_memory_performance_discovery() orchestration function
- MEMORY_PERFORMANCE_BUG_DISCOVERY.md comprehensive guide
- README.md files for memory_leaks/ and performance_regression/ directories
- Weekly report aggregation and artifact upload
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/243-memory-and-performance-bug-discovery/243-RESEARCH.md
@.github/workflows/bug-discovery-weekly.yml
@.github/workflows/lighthouse-ci.yml
@backend/tests/bug_discovery/core/discovery_coordinator.py
@backend/docs/BUG_DISCOVERY_INFRASTRUCTURE.md
@backend/tests/memory_leaks/243-01-PLAN.md
@backend/tests/performance_regression/243-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Create weekly CI pipeline and orchestration function</name>
  <files>.github/workflows/memory-performance-weekly.yml, backend/tests/bug_discovery/core/__init__.py</files>
  <action>
    1. Create .github/workflows/memory-performance-weekly.yml:
       - Schedule: cron '0 3 * * 0' (Sunday 3 AM UTC)
       - Python 3.11+ requirement for memray
       - Jobs: memory-leaks, performance-regression, lighthouse-ci
       - Memory leak job: pytest tests/memory_leaks/ -v -m memory_leak -n 1 (sequential)
       - Performance regression job: pytest tests/performance_regression/ --benchmark-only --benchmark-compare=baseline --benchmark-compare-fail=mean:20%
       - Lighthouse job: Run full Lighthouse CI with regression check
       - Upload artifacts: memray flame graphs, benchmark JSON, Lighthouse reports
       - Bug filing integration with GITHUB_TOKEN/GITHUB_REPOSITORY

    2. Update backend/tests/bug_discovery/core/__init__.py:
       - Add run_memory_performance_discovery() function
       - Orchestrate: memory leaks -> performance regression -> Lighthouse
       - Aggregate results across all 3 discovery methods
       - Generate weekly HTML report
       - Return summary dict (bugs_found, filed_bugs, report_path)

    Pattern from bug-discovery-weekly.yml:
    ```yaml
    name: Memory & Performance Discovery Weekly

    on:
      schedule:
        - cron: '0 3 * * 0'  # Sunday 3 AM UTC
      workflow_dispatch:

    env:
      PYTHON_VERSION: '3.11'

    jobs:
      memory-leaks:
        runs-on: ubuntu-latest
        timeout-minutes: 60
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-python@v4
            with:
              python-version: ${{ env.PYTHON_VERSION }}
          - name: Install dependencies
            run: |
              pip install -e backend
              pip install -r backend/requirements-testing.txt
          - name: Run memory leak tests
            run: |
              pytest backend/tests/memory_leaks/ -v -m memory_leak -n 1
          - name: Upload flame graphs
            uses: actions/upload-artifact@v3
            with:
              name: memray-flame-graphs
              path: backend/tests/memory_leaks/artifacts/

      performance-regression:
        runs-on: ubuntu-latest
        timeout-minutes: 30
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-python@v4
          - name: Run performance regression tests
            run: |
              pytest backend/tests/performance_regression/ --benchmark-only --benchmark-compare=baseline --benchmark-compare-fail=mean:20%
    ```

    Orchestration function pattern:
    ```python
    def run_memory_performance_discovery(
        github_token: str,
        github_repository: str,
        upload_artifacts: bool = True
    ) -> dict:
        from subprocess import run, PIPE

        results = {
            "memory_leaks": {"bugs_found": 0, "bugs": []},
            "performance_regressions": {"bugs_found": 0, "bugs": []},
            "lighthouse": {"bugs_found": 0, "bugs": []}
        }

        # Run memory leak tests
        mem_result = run(["pytest", "tests/memory_leaks/", "-v", "-m", "memory_leak", "-n", "1"], capture_output=True)
        # Parse results and file bugs...

        # Run performance regression tests
        perf_result = run(["pytest", "tests/performance_regression/", "--benchmark-only", "--benchmark-compare-fail=mean:20%"], capture_output=True)
        # Parse results and file bugs...

        # Generate report
        return {
            "bugs_found": sum(r["bugs_found"] for r in results.values()),
            "report_path": "storage/reports/memory_performance_weekly.html"
        }
    ```
  </action>
  <verify>
    ls -la .github/workflows/memory-performance-weekly.yml
    grep -q "cron: '0 3 \* \* 0'" .github/workflows/memory-performance-weekly.yml
    grep -q "run_memory_performance_discovery" backend/tests/bug_discovery/core/__init__.py
  </verify>
  <done>
    Weekly workflow created, orchestration function exists in bug_discovery/core/__init__.py
  </done>
</task>

<task type="auto">
  <name>Create comprehensive documentation and README files</name>
  <files>backend/docs/MEMORY_PERFORMANCE_BUG_DISCOVERY.md, backend/tests/memory_leaks/README.md, backend/tests/performance_regression/README.md</files>
  <action>
    Create 3 documentation files:

    1. backend/docs/MEMORY_PERFORMANCE_BUG_DISCOVERY.md (300+ lines):
       - Overview: Memory leak detection + performance regression
       - Architecture: memray + pytest-benchmark + Lighthouse CI
       - Setup: memray installation, Python 3.11+ requirement
       - Usage: pytest markers, baseline management
       - CI/CD: Weekly workflow, PR integration
       - Bug filing: MemoryPerformanceFilingService integration
       - Troubleshooting: Common issues (memray not installed, baseline missing, regression false positives)
       - Examples: Writing memory leak tests, performance regression tests

    2. backend/tests/memory_leaks/README.md (150+ lines):
       - Purpose: Python-level memory leak detection with memray
       - Fixtures: memray_session usage
       - Test patterns: Amplification loops (100 iterations), threshold assertions
       - Examples: Agent execution, governance cache, LLM streaming
       - Troubleshooting: Graceful degradation if memray unavailable
       - Flame graph generation and analysis

    3. backend/tests/performance_regression/README.md (150+ lines):
       - Purpose: Performance regression detection with pytest-benchmark
       - Fixtures: check_regression, performance_baseline
       - Test patterns: @pytest.mark.benchmark, baseline comparison
       - Examples: API latency, database query, governance cache
       - Baseline management: Initial baseline, updates, thresholds
       - CI integration: --benchmark-compare-fail

    Follow documentation pattern from BUG_DISCOVERY_INFRASTRUCTURE.md:
    - Clear sections with ## headers
    - Code examples in ```python blocks
    - CLI commands in ```bash blocks
    - Tables for configuration options
    - Links to related documentation

    Include in all docs:
    - memray Python 3.11+ requirement
    - 20% regression threshold rationale
    - Weekly CI schedule (Sunday 3 AM UTC)
    - Bug filing integration via MemoryPerformanceFilingService
  </action>
  <verify>
    ls -la backend/docs/MEMORY_PERFORMANCE_BUG_DISCOVERY.md
    ls -la backend/tests/memory_leaks/README.md
    ls -la backend/tests/performance_regression/README.md
    grep -q "memray" backend/docs/MEMORY_PERFORMANCE_BUG_DISCOVERY.md
    grep -q "pytest-benchmark" backend/tests/performance_regression/README.md
  </verify>
  <done>
    Documentation files exist with comprehensive coverage of setup, usage, CI/CD, troubleshooting
  </done>
</task>

</tasks>

<verification>
1. Verify workflow syntax: yamllint .github/workflows/memory-performance-weekly.yml (if available)
2. Verify orchestration function: grep "def run_memory_performance_discovery" backend/tests/bug_discovery/core/__init__.py
3. Check documentation completeness: wc -l backend/docs/MEMORY_PERFORMANCE_BUG_DISCOVERY.md (expect 300+)
4. Verify README examples: grep "def test_" backend/tests/memory_leaks/README.md
5. Check CI schedule: grep "cron" .github/workflows/memory-performance-weekly.yml
</verification>

<success_criteria>
1. memory-performance-weekly.yml workflow created with Sunday 3 AM UTC schedule
2. run_memory_performance_discovery() function orchestrates all 3 discovery methods
3. MEMORY_PERFORMANCE_BUG_DISCOVERY.md comprehensive guide (300+ lines)
4. memory_leaks/README.md with memray usage examples (150+ lines)
5. performance_regression/README.md with pytest-benchmark examples (150+ lines)
6. Workflow uploads artifacts: flame graphs, benchmark JSON, Lighthouse reports
7. Documentation covers setup, usage, CI/CD, troubleshooting
8. Python 3.11+ requirement documented for memray
9. 20% regression threshold documented with rationale
10. Bug filing integration documented (MemoryPerformanceFilingService)
</success_criteria>

<output>
After completion, create `.planning/phases/243-memory-and-performance-bug-discovery/243-05-SUMMARY.md` with:
- Files created/modified
- Documentation word counts
- CI schedule and configuration
- Final phase verification summary
</output>
