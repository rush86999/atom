---
phase: 178-api-routes-coverage-admin-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_admin_skill_routes.py
autonomous: true

must_haves:
  truths:
    - "Admin skill routes achieve 75%+ line coverage measured by pytest --cov"
    - "POST /api/admin/skills endpoint tested with happy path (skill creation success)"
    - "Error paths tested: 403 unauthorized (non-super_admin), 409 policy violation, 422 validation error, 500 internal error"
    - "Security scanning paths tested (static scan pass, critical findings rejection, LLM scan optional)"
    - "Test file follows per-file FastAPI app pattern with isolated TestClient"
  artifacts:
    - path: "backend/tests/api/test_admin_skill_routes.py"
      provides: "Admin skill routes coverage tests"
      min_lines: 600
      exports: ["TestAdminSkillRoutesSuccess", "TestAdminSkillRoutesAuth", "TestAdminSkillRoutesSecurity", "TestAdminSkillRoutesError"]
  key_links:
    - from: "backend/tests/api/test_admin_skill_routes.py"
      to: "api/admin/skill_routes.py"
      via: "import router and create TestClient"
      pattern: "from api.admin.skill_routes import router"
---

<objective>
Create comprehensive test coverage for admin skill routes (`api/admin/skill_routes.py`) achieving 75%+ line coverage.

Purpose: Admin skill routes enable creation of new standardized skill packages with proactive security auditing. This is a critical administrative function requiring AUTONOMOUS maturity and super_admin role.

Output: Test file `test_admin_skill_routes.py` with 600-800 lines covering all endpoint paths, error conditions, and security scenarios.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/178-api-routes-coverage-admin-system/178-RESEARCH.md
@backend/api/admin/skill_routes.py
@backend/tests/api/test_admin_routes_part1.py
@backend/tests/api/test_analytics_routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test file with fixtures for admin skill routes</name>
  <files>backend/tests/api/test_admin_skill_routes.py</files>
  <action>
Create `backend/tests/api/test_admin_skill_routes.py` with the following structure:

1. **Module docstring**: Describe coverage scope (admin skill routes, 75%+ target, 1 endpoint)

2. **Imports**: Standard test imports from Phase 177 pattern
   ```python
   import pytest
   import uuid
   from unittest.mock import MagicMock, AsyncMock, patch
   from fastapi import FastAPI
   from fastapi.testclient import TestClient
   from sqlalchemy import create_engine
   from sqlalchemy.orm import sessionmaker, Session
   from sqlalchemy.pool import StaticPool
   from api.admin.skill_routes import router
   from core.models import Base, User
   ```

3. **Test database fixture** (`test_db`): In-memory SQLite with StaticPool, create User table only

4. **FastAPI app fixture** (`test_app`): Create FastAPI app, include skill_routes router, override get_db dependency

5. **TestClient fixture** (`client`): Return TestClient(test_app)

6. **Super admin user fixture** (`super_admin_user`): User with role="super_admin", status="active"

7. **Regular user fixture** (`regular_user`): User with role="member", status="active" (for auth testing)

8. **Authenticated admin client fixture** (`authenticated_admin_client`): Override get_current_user to return super_admin_user

9. **Unauthenticated client fixture** (`unauthenticated_client`): Override get_current_user to raise 401

10. **Mock security analyzer fixture** (`mock_static_analyzer`): MagicMock for StaticAnalyzer with configurable findings

11. **Mock skill builder fixture** (`mock_skill_builder`): AsyncMock for skill_builder_service with deterministic return values

Reference: `backend/tests/api/test_admin_routes_part1.py` lines 40-150 for fixture patterns.
  </action>
  <verify>
    File exists at backend/tests/api/test_admin_skill_routes.py with all fixtures defined
  </verify>
  <done>
    All 11 fixtures are present and follow Phase 177 patterns
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement happy path tests for skill creation</name>
  <files>backend/tests/api/test_admin_skill_routes.py</files>
  <action>
Add test class `TestAdminSkillRoutesSuccess` with the following tests:

1. **test_create_skill_success**: Test successful skill creation with valid request
   - Mock static_analyzer.scan_content to return empty findings
   - Mock skill_builder_service.create_skill_package to return success
   - POST /api/admin/skills with valid CreateSkillRequest
   - Assert 200 status, success=True, skill data in response

2. **test_create_skill_with_all_fields**: Test skill creation with all optional fields
   - Include capabilities list, multiple scripts
   - Verify all fields are passed to skill builder

3. **test_create_skill_tenant_id_from_admin**: Test that tenant_id is extracted from admin user
   - Create super_admin with specific tenant_id
   - Verify tenant_id is passed to skill builder

4. **test_create_skill_default_author**: Test that author defaults to admin email
   - Omit email from user, verify "Admin" as default

5. **test_create_skill_without_llm_scan**: Test skill creation when LLM scan is disabled
   - Set ATOM_SECURITY_ENABLE_LLM_SCAN=false
   - Verify only static scan runs

Reference: `backend/tests/api/test_analytics_routes.py` lines 71-95 for success test patterns.
  </action>
  <verify>
    Test class TestAdminSkillRoutesSuccess exists with 5 test methods
  </verify>
  <done>
    All happy path tests pass with mocked services
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement authentication and authorization tests</name>
  <files>backend/tests/api/test_admin_skill_routes.py</files>
  <action>
Add test class `TestAdminSkillRoutesAuth` with the following tests:

1. **test_create_skill_requires_super_admin**: Test that non-super_admin cannot create skills
   - Use regular_user fixture (role="member")
   - POST /api/admin/skills should return 403

2. **test_create_skill_unauthenticated**: Test that unauthenticated request fails
   - Use unauthenticated_client (no auth override)
   - POST /api/admin/skills should return 401

3. **test_create_skill_inactive_admin**: Test that inactive admin cannot create skills
   - Create super_admin with status="inactive"
   - POST /api/admin/skills should return 401 or 403

4. **test_get_super_admin_dependency**: Test get_super_admin dependency directly
   - Verify dependency raises 403 for non-super_admin role
   - Verify dependency returns user for super_admin role

Reference: `backend/tests/api/test_admin_routes_part1.py` lines 250-320 for auth test patterns.
  </action>
  <verify>
    Test class TestAdminSkillRoutesAuth exists with 4 test methods
  </verify>
  <done>
    All auth tests verify proper role enforcement
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement security scanning tests</name>
  <files>backend/tests/api/test_admin_skill_routes.py</files>
  <action>
Add test class `TestAdminSkillRoutesSecurity` with the following tests:

1. **test_security_scan_static_pass**: Test skill creation when static scan passes
   - Mock static_analyzer.scan_content to return []
   - Verify skill creation proceeds

2. **test_security_scan_critical_finding**: Test skill rejection for critical findings
   - Mock static_analyzer.scan_content to return findings with severity="HIGH"
   - POST /api/admin/skills should return 403 with findings details

3. **test_security_scan_multiple_findings**: Test with multiple security findings
   - Mock findings with mixed severities (LOW, MEDIUM, HIGH)
   - Verify only HIGH/CRITICAL findings block creation

4. **test_llm_scan_enabled**: Test LLM scan when enabled
   - Set ATOM_SECURITY_ENABLE_LLM_SCAN=true
   - Mock LLMAnalyzer to return findings
   - Verify LLM findings are combined with static findings

5. **test_llm_scan_failure_blocks**: Test that LLM scan failure doesn't block
   - Mock LLMAnalyzer.analyze to raise Exception
   - Verify skill creation continues (logs warning)

6. **test_security_scan_exception**: Test handling of security module exceptions
   - Mock StaticAnalyzer to raise Exception
   - Verify skill creation continues (graceful degradation)

Reference: `backend/api/admin/skill_routes.py` lines 38-74 for security scan logic.
  </action>
  <verify>
    Test class TestAdminSkillRoutesSecurity exists with 6 test methods
  </verify>
  <done>
    All security scan paths are tested (pass, fail, LLM, exceptions)
  </done>
</task>

<task type="auto">
  <name>Task 5: Implement error path tests</name>
  <files>backend/tests/api/test_admin_skill_routes.py</files>
  <action>
Add test class `TestAdminSkillRoutesError` with the following tests:

1. **test_create_skill_validation_error**: Test request validation failures
   - POST with missing required fields (name, description)
   - Assert 422 validation error

2. **test_create_skill_invalid_scripts**: Test invalid scripts format
   - POST with non-dict scripts field
   - Assert 422 validation error

3. **test_create_skill_builder_fails**: Test skill builder service failure
   - Mock skill_builder_service.create_skill_package to return success=False
   - Assert validation error is raised

4. **test_create_skill_unhandled_exception**: Test unhandled exception path
   - Mock skill_builder_service.create_skill_package to raise Exception
   - Assert 500 internal error

5. **test_create_skill_empty_name**: Test empty name validation
   - POST with name=""
   - Assert 422 validation error

6. **test_create_skill_invalid_capabilities**: Test invalid capabilities format
   - POST with capabilities as string instead of list
   - Assert 422 validation error

Reference: `backend/tests/api/test_analytics_routes.py` lines 200-250 for error path patterns.
  </action>
  <verify>
    Test class TestAdminSkillRoutesError exists with 6 test methods
  </verify>
  <done>
    All error paths return correct status codes (422, 500)
  </done>
</task>

<task type="auto">
  <name>Task 6: Run tests and verify coverage</name>
  <files>backend/tests/api/test_admin_skill_routes.py</files>
  <action>
Execute the following commands:

1. Run the new test file:
   ```bash
   cd /Users/rushiparikh/projects/atom/backend
   pytest tests/api/test_admin_skill_routes.py -v
   ```

2. Run with coverage on admin skill routes:
   ```bash
   pytest tests/api/test_admin_skill_routes.py --cov=api/admin/skill_routes --cov-report=term-missing --cov-report=json
   ```

3. Verify coverage is 75%+:
   ```bash
   # Check coverage.json for api/admin/skill_routes.py percent_covered
   ```

4. If coverage < 75%, identify missing lines and add tests for uncovered paths:
   - Check coverage report for missing lines
   - Add tests for any uncovered conditional branches
   - Common gaps: exception handlers, edge cases in loops, optional parameters

5. Run all existing tests to ensure no regressions:
   ```bash
   pytest tests/api/test_admin_routes_part1.py -v
   pytest tests/api/test_health_routes.py -v
   ```

Reference: Phase 177-01-SUMMARY.md for coverage measurement approach.
  </action>
  <verify>
    Coverage report shows 75%+ for api/admin/skill_routes.py
  </verify>
  <done>
    api/admin/skill_routes.py achieves 75%+ line coverage, all tests pass
  </done>
</task>

</tasks>

<verification>
Overall phase verification:

1. Test file exists at backend/tests/api/test_admin_skill_routes.py
2. All test fixtures follow per-file FastAPI app pattern
3. POST /api/admin/skills endpoint tested with:
   - Happy paths (success, all fields, tenant_id, default author, no LLM scan)
   - Auth paths (requires super_admin, unauthenticated, inactive admin)
   - Security paths (static pass, critical findings, multiple findings, LLM enabled, LLM failure, scan exception)
   - Error paths (validation, invalid scripts, builder fails, unhandled exception)
4. Coverage measured via pytest --cov shows 75%+ for api/admin/skill_routes.py
5. All tests pass when run individually and with existing test suite
</verification>

<success_criteria>
1. api/admin/skill_routes.py achieves 75%+ line coverage
2. Test file has 600-800 lines with 21+ tests
3. All endpoint paths (1 endpoint) tested with happy and error cases
4. External services properly mocked (StaticAnalyzer, skill_builder_service)
5. Governance enforcement tested (super_admin required)
</success_criteria>

<output>
After completion, create `.planning/phases/178-api-routes-coverage-admin-system/178-01-SUMMARY.md` with:
- Coverage achieved (line count and percentage)
- Number of tests written
- Any challenges encountered and solutions
- Missing coverage areas (if any) for future improvement
</output>
