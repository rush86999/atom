---
phase: 178-api-routes-coverage-admin-system
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_admin_sync_routes_coverage.py
autonomous: true

must_haves:
  truths:
    - "Sync admin routes achieve 75%+ line coverage measured by pytest --cov"
    - "POST /api/admin/sync/trigger tested (manual sync trigger, governance enforced)"
    - "GET /api/admin/sync/status tested (sync state, age calculation, no state)"
    - "GET /api/admin/sync/config tested (configuration values)"
    - "Rating sync endpoints tested (trigger, status, failed uploads, retry)"
    - "WebSocket endpoints tested (status, reconnect, enable, disable)"
    - "Conflict resolution endpoints tested (list, get detail, resolve, bulk resolve)"
    - "Error paths tested (404 not found, governance failures, validation errors)"
  artifacts:
    - path: "backend/tests/api/test_admin_sync_routes_coverage.py"
      provides: "Sync admin routes coverage tests"
      min_lines: 400
      exports: ["TestSyncTrigger", "TestSyncStatus", "TestSyncConfig", "TestRatingSync", "TestWebSocketManagement", "TestConflictResolution", "TestGovernanceEnforcement"]
  key_links:
    - from: "backend/tests/api/test_admin_sync_routes_coverage.py"
      to: "api/sync_admin_routes.py"
      via: "import router and create TestClient"
      pattern: "from api.sync_admin_routes import router"
---

<objective>
Create comprehensive test coverage for sync admin routes (`api/sync_admin_routes.py`) achieving 75%+ line coverage.

Purpose: Sync admin routes manage Atom SaaS marketplace synchronization including manual triggers, status monitoring, rating sync, WebSocket management, and conflict resolution. These routes have AUTONOMOUS maturity requirements and use placeholder implementations.

Output: Test file `test_admin_sync_routes_coverage.py` with 400-600 lines covering all sync admin endpoints with governance enforcement and error handling.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/178-api-routes-coverage-admin-system/178-RESEARCH.md
@backend/api/sync_admin_routes.py
@backend/tests/api/test_admin_routes_part2.py
@backend/tests/api/test_admin_routes_part1.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test file with fixtures for sync admin routes</name>
  <files>backend/tests/api/test_admin_sync_routes_coverage.py</files>
  <action>
Create `backend/tests/api/test_admin_sync_routes_coverage.py` with the following structure:

1. **Module docstring**: Describe coverage scope (sync admin routes, 75%+ target, 14 endpoints)

2. **Imports**: Standard test imports plus sync specific:
   ```python
   import pytest
   import uuid
   from datetime import datetime, timezone
   from typing import List
   from unittest.mock import MagicMock, AsyncMock, patch
   from fastapi import FastAPI
   from fastapi.testclient import TestClient
   from sqlalchemy import create_engine
   from sqlalchemy.orm import sessionmaker, Session
   from sqlalchemy.pool import StaticPool
   from api.sync_admin_routes import router
   from core.models import Base, User, SyncState
   ```

3. **Test database fixture** (`test_db`): In-memory SQLite with StaticPool, create User and SyncState tables

4. **FastAPI app fixture** (`test_app`): Create FastAPI app, include sync_admin_routes router

5. **TestClient fixture** (`client`): Return TestClient(test_app)

6. **Admin user fixture** (`admin_user`): User with role="super_admin"

7. **Regular user fixture** (`regular_user`): User with role="member" (for governance testing)

8. **Authenticated client fixture** (`authenticated_client`): Override get_current_user

9. **Mock governance cache fixture** (`mock_governance_cache`): AsyncMock for maturity checks

Reference: `backend/tests/api/test_admin_routes_part2.py` lines 40-125 for fixture patterns.
  </action>
  <verify>
    File exists at backend/tests/api/test_admin_sync_routes_coverage.py with all fixtures defined
  </verify>
  <done>
    All 9 fixtures are present and follow Phase 177 patterns
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement sync trigger and status tests</name>
  <files>backend/tests/api/test_admin_sync_routes_coverage.py</files>
  <action>
Add test class `TestSyncTrigger` with the following tests:

1. **test_trigger_manual_sync_success**: Test successful manual sync trigger
   - POST /api/admin/sync/trigger with authenticated client
   - Assert 202 status, sync_id returned, status="queued"

2. **test_trigger_manual_sync_generates_sync_id**: Test sync ID format
   - POST /api/admin/sync/trigger
   - Assert sync_id matches format "manual_YYYYMMDD_HHMMSS"

3. **test_trigger_manual_sync_governance_enforced**: Test AUTONOMOUS requirement
   - Mock governance to return non-AUTONOMOUS maturity
   - POST /api/admin/sync/trigger should fail governance check

Add test class `TestSyncStatus` with the following tests:

1. **test_get_sync_status_with_state**: Test getting status when SyncState exists
   - Create SyncState in test_db with last_sync
   - GET /api/admin/sync/status
   - Assert 200 status, fields match SyncState

2. **test_get_sync_status_no_state**: Test getting status when no SyncState
   - Mock db.query(SyncState).first() to return None
   - GET /api/admin/sync/status
   - Assert 200 status with default values (idle, 0 skills cached)

3. **test_get_sync_status_age_calculation**: Test sync_age_minutes calculation
   - Create SyncState with last_sync = 30 minutes ago
   - GET /api/admin/sync/status
   - Assert sync_age_minutes ≈ 30

Add test class `TestSyncConfig` with the following tests:

1. **test_get_sync_config**: Test getting sync configuration
   - GET /api/admin/sync/config
   - Assert 200 status, verify all config fields present

Reference: `backend/api/sync_admin_routes.py` lines 153-239 for sync trigger/status/config logic.
  </action>
  <verify>
    Test classes TestSyncTrigger, TestSyncStatus, TestSyncConfig exist with all tests
  </verify>
  <done>
    All sync trigger and status tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement rating sync tests</name>
  <files>backend/tests/api/test_admin_sync_routes_coverage.py</files>
  <action>
Add test class `TestRatingSync` with the following tests:

1. **test_trigger_rating_sync_success**: Test triggering rating sync
   - POST /api/admin/sync/ratings
   - Assert 202 status, sync_id returned with "rating_" prefix

2. **test_trigger_rating_sync_governance_enforced**: Test HIGH complexity governance
   - Mock governance to return non-AUTONOMOUS maturity
   - POST /api/admin/sync/ratings should fail governance check

3. **test_get_rating_sync_status**: Test getting rating sync status
   - GET /api/admin/sync/ratings/status
   - Assert 200 status, verify placeholder response fields

4. **test_list_failed_rating_uploads**: Test listing failed uploads
   - GET /api/admin/sync/ratings/failed-uploads
   - Assert 200 status, verify empty list (placeholder)

5. **test_list_failed_uploads_with_pagination**: Test pagination parameters
   - GET /api/admin/sync/ratings/failed-uploads?page=2&page_size=50
   - Assert 200 status

6. **test_retry_failed_upload**: Test retrying failed upload
   - POST /api/admin/sync/ratings/failed-uploads/{upload_id}/retry
   - Assert 200 status, success=True

7. **test_retry_upload_governance_enforced**: Test MODERATE complexity governance
   - Mock governance to return non-AUTONOMOUS maturity
   - POST retry should fail governance check

Reference: `backend/api/sync_admin_routes.py` lines 246-339 for rating sync logic.
  </action>
  <verify>
    Test class TestRatingSync exists with 7 test methods
  </verify>
  <done>
    All rating sync tests pass with proper governance checks
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement WebSocket management tests</name>
  <files>backend/tests/api/test_admin_sync_routes_coverage.py</files>
  <action>
Add test class `TestWebSocketManagement` with the following tests:

1. **test_get_websocket_status**: Test getting WebSocket status
   - GET /api/admin/sync/websocket/status
   - Assert 200 status, verify placeholder response (connected=False)

2. **test_force_websocket_reconnect**: Test forcing WebSocket reconnection
   - POST /api/admin/sync/websocket/reconnect
   - Assert 200 status, reconnect_triggered=True

3. **test_websocket_reconnect_governance_enforced**: Test MODERATE complexity governance
   - Mock governance to return non-AUTONOMOUS maturity
   - POST reconnect should fail governance check

4. **test_disable_websocket**: Test disabling WebSocket
   - POST /api/admin/sync/websocket/disable
   - Assert 200 status, enabled=False

5. **test_disable_websocket_governance_enforced**: Test HIGH complexity governance
   - Mock governance to return non-AUTONOMOUS maturity
   - POST disable should fail governance check

6. **test_enable_websocket**: Test enabling WebSocket
   - POST /api/admin/sync/websocket/enable
   - Assert 200 status, enabled=True

7. **test_enable_websocket_governance_enforced**: Test MODERATE complexity governance
   - Mock governance to return non-AUTONOMOUS maturity
   - POST enable should fail governance check

Reference: `backend/api/sync_admin_routes.py` lines 346-446 for WebSocket logic.
  </action>
  <verify>
    Test class TestWebSocketManagement exists with 7 test methods
  </verify>
  <done>
    All WebSocket management tests pass with governance enforcement
  </done>
</task>

<task type="auto">
  <name>Task 5: Implement conflict resolution tests</name>
  <files>backend/tests/api/test_admin_sync_routes_coverage.py</files>
  <action>
Add test class `TestConflictResolution` with the following tests:

1. **test_list_conflicts**: Test listing conflicts
   - GET /api/admin/sync/conflicts
   - Assert 200 status, verify empty list (placeholder)

2. **test_list_conflicts_with_filters**: Test listing with status filter
   - GET /api/admin/sync/conflicts?status=unresolved
   - Assert 200 status

3. **test_list_conflicts_with_pagination**: Test pagination parameters
   - GET /api/admin/sync/conflicts?page=1&page_size=20
   - Assert 200 status

4. **test_get_conflict_detail_not_found**: Test getting non-existent conflict
   - GET /api/admin/sync/conflicts/{conflict_id}
   - Assert 404 status (placeholder always raises not_found)

5. **test_resolve_conflict**: Test resolving conflict
   - POST /api/admin/sync/conflicts/{conflict_id}/resolve?strategy=local_wins
   - Assert 200 status, conflict_id in response

6. **test_resolve_conflict_governance_enforced**: Test HIGH complexity governance
   - Mock governance to return non-AUTONOMOUS maturity
   - POST resolve should fail governance check

7. **test_bulk_resolve_conflicts**: Test bulk resolving conflicts
   - POST /api/admin/sync/conflicts/bulk-resolve?strategy=merge
   - With conflict_ids=["id1", "id2", "id3"]
   - Assert 200 status, resolved_count=3, failed_count=0

8. **test_bulk_resolve_governance_enforced**: Test CRITICAL complexity governance
   - Mock governance to return non-AUTONOMOUS maturity
   - POST bulk-resolve should fail governance check

9. **test_bulk_resolve_with_failures**: Test bulk resolve with some failures
   - Mock partial success scenario
   - Assert failed_count > 0, failed_ids populated

Reference: `backend/api/sync_admin_routes.py` lines 453-544 for conflict resolution logic.
  </action>
  <verify>
    Test class TestConflictResolution exists with 9 test methods
  </verify>
  <done>
    All conflict resolution tests pass with governance enforcement
  </done>
</task>

<task type="auto">
  <name>Task 6: Implement governance enforcement tests</name>
  <files>backend/tests/api/test_admin_sync_routes_coverage.py</files>
  <action>
Add test class `TestGovernanceEnforcement` with the following tests:

1. **test_all_crITICAL_endpoints_require_autonomous**: Test CRITICAL complexity endpoints
   - Mock GovernanceCache to return "STUDENT" maturity
   - Attempt POST /api/admin/sync/trigger (CRITICAL)
   - Attempt POST /api/admin/sync/conflicts/bulk-resolve (CRITICAL)
   - Assert both return 403 with governance error

2. **test_HIGH_endpoints_require_autonomous**: Test HIGH complexity endpoints
   - Mock GovernanceCache to return "INTERN" maturity
   - Attempt POST /api/admin/sync/ratings (HIGH)
   - Attempt POST /api/admin/sync/websocket/disable (HIGH)
   - Attempt POST /api/admin/sync/conflicts/{id}/resolve (HIGH)
   - Assert all return 403

3. **test_MODERATE_endpoints_require_autonomous**: Test MODERATE complexity endpoints
   - Mock GovernanceCache to return "SUPERVISED" maturity
   - Attempt POST /api/admin/sync/ratings/failed-uploads/{id}/retry (MODERATE)
   - Attempt POST /api/admin/sync/websocket/reconnect (MODERATE)
   - Attempt POST /api/admin/sync/websocket/enable (MODERATE)
   - Assert all return 403

4. **test_AUTONOMOUS_passes_all_checks**: Test AUTONOMOUS maturity passes
   - Mock GovernanceCache to return "AUTONOMOUS" maturity
   - POST /api/admin/sync/trigger should succeed (202)
   - POST /api/admin/sync/ratings should succeed (202)
   - POST /api/admin/sync/conflicts/bulk-resolve should succeed (200)

5. **test_governance_cache_integration**: Test GovernanceCache integration
   - Verify GovernanceCache is instantiated in routes
   - Verify maturity level is checked correctly

Reference: `backend/api/sync_admin_routes.py` for @require_governance decorators on lines 154-157, 247-250, 314-317, 366-369, 394-397, 422-425, 486-489, 516-519.
  </action>
  <verify>
    Test class TestGovernanceEnforcement exists with 5 test methods
  </verify>
  <done>
    All governance enforcement tests verify proper maturity checks
  </done>
</task>

<task type="auto">
  <name>Task 7: Run tests and verify coverage</name>
  <files>backend/tests/api/test_admin_sync_routes_coverage.py</files>
  <action>
Execute the following commands:

1. Run the new test file:
   ```bash
   cd /Users/rushiparikh/projects/atom/backend
   pytest tests/api/test_admin_sync_routes_coverage.py -v
   ```

2. Run with coverage on sync admin routes:
   ```bash
   pytest tests/api/test_admin_sync_routes_coverage.py --cov=api/sync_admin_routes --cov-report=term-missing --cov-report=json
   ```

3. Verify coverage is 75%+:
   ```bash
   # Check coverage.json for api/sync_admin_routes.py percent_covered
   ```

4. If coverage < 75%, identify missing lines and add tests:
   - Common gaps: exception handlers, placeholder logic branches
   - Add tests for uncovered conditional branches

5. Run existing admin routes tests to ensure compatibility:
   ```bash
   pytest tests/api/test_admin_routes_part1.py -v
   pytest tests/api/test_admin_routes_part2.py -v
   ```

6. Check if any admin_routes.py endpoints need additional coverage:
   ```bash
   pytest tests/api/test_admin_routes_part1.py tests/api/test_admin_routes_part2.py --cov=api/admin_routes --cov-report=term-missing
   ```
   - If admin_routes.py coverage < 75%, add tests to new file to extend coverage

Reference: Phase 177-01-SUMMARY.md for coverage measurement approach.
  </action>
  <verify>
    Coverage report shows 75%+ for api/sync_admin_routes.py
  </verify>
  <done>
    api/sync_admin_routes.py achieves 75%+ line coverage, all tests pass
  </done>
</task>

</tasks>

<verification>
Overall phase verification:

1. Test file exists at backend/tests/api/test_admin_sync_routes_coverage.py
2. All test fixtures follow per-file FastAPI app pattern
3. All 14 sync admin endpoints tested:
   - POST /api/admin/sync/trigger (manual sync, governance)
   - GET /api/admin/sync/status (sync state, age, no state)
   - GET /api/admin/sync/config (configuration)
   - POST /api/admin/sync/ratings (rating sync trigger, governance)
   - GET /api/admin/sync/ratings/status (rating sync status)
   - GET /api/admin/sync/ratings/failed-uploads (list, pagination)
   - POST /api/admin/sync/ratings/failed-uploads/{id}/retry (retry, governance)
   - GET /api/admin/sync/websocket/status (status)
   - POST /api/admin/sync/websocket/reconnect (reconnect, governance)
   - POST /api/admin/sync/websocket/disable (disable, governance)
   - POST /api/admin/sync/websocket/enable (enable, governance)
   - GET /api/admin/sync/conflicts (list, filters, pagination)
   - GET /api/admin/sync/conflicts/{id} (detail, 404)
   - POST /api/admin/sync/conflicts/{id}/resolve (resolve, governance)
   - POST /api/admin/sync/conflicts/bulk-resolve (bulk resolve, governance)
4. Coverage measured via pytest --cov shows 75%+ for api/sync_admin_routes.py
5. All tests pass when run individually and with existing test suite
6. Governance enforcement tested for all complexity levels (CRITICAL, HIGH, MODERATE)
</verification>

<success_criteria>
1. api/sync_admin_routes.py achieves 75%+ line coverage
2. Test file has 400-600 lines with 30+ tests
3. All 14 endpoints tested with happy and error cases
4. Governance enforcement tested (AUTONOMOUS required for all operations)
5. Placeholder implementations tested with mock responses
</success_criteria>

<output>
After completion, create `.planning/phases/178-api-routes-coverage-admin-system/178-04-SUMMARY.md` with:
- Coverage achieved (line count and percentage)
- Number of tests written
- Any challenges encountered and solutions
- Missing coverage areas (if any) for future improvement
- Total phase coverage summary (all 4 route groups combined)
</output>
