---
phase: 178-api-routes-coverage-admin-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_admin_business_facts_routes.py
autonomous: true

must_haves:
  truths:
    - "Admin business facts routes achieve 75%+ line coverage measured by pytest --cov"
    - "GET /api/admin/governance/facts endpoint tested (list with filters, deleted filtered out)"
    - "GET /api/admin/governance/facts/{fact_id} tested (success, 404 not found)"
    - "POST /api/admin/governance/facts tested (create fact, success/error)"
    - "PUT /api/admin/governance/facts/{fact_id} tested (update all fields, partial update, 404)"
    - "DELETE /api/admin/governance/facts/{fact_id} tested (soft delete, 404)"
    - "POST /api/admin/governance/facts/upload tested (file upload, extraction, invalid file type)"
    - "POST /api/admin/governance/facts/{fact_id}/verify-citation tested (S3 check, local check, mixed results)"
  artifacts:
    - path: "backend/tests/api/test_admin_business_facts_routes.py"
      provides: "Admin business facts routes coverage tests"
      min_lines: 700
      exports: ["TestBusinessFactsList", "TestBusinessFactsGet", "TestBusinessFactsCreate", "TestBusinessFactsUpdate", "TestBusinessFactsDelete", "TestBusinessFactsUpload", "TestBusinessFactsVerify"]
  key_links:
    - from: "backend/tests/api/test_admin_business_facts_routes.py"
      to: "api/admin/business_facts_routes.py"
      via: "import router and create TestClient"
      pattern: "from api.admin.business_facts_routes import router"
---

<objective>
Create comprehensive test coverage for admin business facts routes (`api/admin/business_facts_routes.py`) achieving 75%+ line coverage.

Purpose: Business facts routes manage verified business knowledge with JIT citation verification, document upload for fact extraction, and CRUD operations. All endpoints require ADMIN role.

Output: Test file `test_admin_business_facts_routes.py` with 700-900 lines covering all 7 endpoints with happy paths, error conditions, and external service mocking.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/178-api-routes-coverage-admin-system/178-RESEARCH.md
@backend/api/admin/business_facts_routes.py
@backend/tests/api/test_admin_routes_part1.py
@backend/tests/api/test_analytics_routes.py
@backend/tests/api/test_business_facts_routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test file with fixtures for business facts routes</name>
  <files>backend/tests/api/test_admin_business_facts_routes.py</files>
  <action>
Create `backend/tests/api/test_admin_business_facts_routes.py` with the following structure:

1. **Module docstring**: Describe coverage scope (business facts routes, 75%+ target, 7 endpoints)

2. **Imports**: Standard test imports plus business facts specific:
   ```python
   import pytest
   import uuid
   import io
   from datetime import datetime
   from typing import List
   from unittest.mock import MagicMock, AsyncMock, patch
   from fastapi import FastAPI
   from fastapi.testclient import TestClient
   from sqlalchemy import create_engine
   from sqlalchemy.orm import sessionmaker, Session
   from sqlalchemy.pool import StaticPool
   from api.admin.business_facts_routes import router
   from core.models import Base, User
   from core.agent_world_model import BusinessFact
   ```

3. **Test database fixture** (`test_db`): In-memory SQLite with StaticPool

4. **FastAPI app fixture** (`test_app`): Include business_facts_routes router

5. **TestClient fixture** (`client`): Return TestClient(test_app)

6. **Admin user fixture** (`admin_user`): User with role="admin"

7. **Non-admin user fixture** (`regular_user`): User with role="member" (for role testing)

8. **Authenticated admin client fixture** (`authenticated_admin_client`): Override get_current_user and require_role

9. **Mock WorldModelService fixture** (`mock_world_model_service`): AsyncMock with deterministic return values for list_all_facts, get_fact_by_id, record_business_fact, delete_fact, update_fact_verification, bulk_record_facts

10. **Mock storage service fixture** (`mock_storage_service`): MagicMock for R2/S3 operations (upload_file, check_exists)

11. **Mock policy fact extractor fixture** (`mock_policy_extractor`): AsyncMock for fact extraction

12. **Mock upload file fixture** (`mock_pdf_upload`): Function returning BytesIO for file upload testing

Reference: `backend/tests/api/test_admin_routes_part1.py` lines 40-150 for fixture patterns and Phase 178-RESEARCH.md lines 109-144 for AsyncMock patterns.
  </action>
  <verify>
    File exists at backend/tests/api/test_admin_business_facts_routes.py with all fixtures defined
  </verify>
  <done>
    All 12 fixtures are present and follow Phase 177 patterns
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement list and get endpoint tests</name>
  <files>backend/tests/api/test_admin_business_facts_routes.py</files>
  <action>
Add test class `TestBusinessFactsList` with the following tests:

1. **test_list_facts_success**: Test listing all facts with no filters
   - Mock WorldModelService.list_all_facts to return list of BusinessFact
   - GET /api/admin/governance/facts
   - Assert 200 status, verify deleted facts filtered out

2. **test_list_facts_with_status_filter**: Test listing with status filter
   - Mock list_all_facts with status="verified" parameter
   - GET /api/admin/governance/facts?status=verified
   - Assert filtered results

3. **test_list_facts_with_domain_filter**: Test listing with domain filter
   - Mock list_all_facts with domain="accounting" parameter
   - GET /api/admin/governance/facts?domain=accounting
   - Assert filtered results

4. **test_list_facts_with_limit**: Test listing with custom limit
   - Mock list_all_facts with limit=50 parameter
   - GET /api/admin/governance/facts?limit=50
   - Assert limit respected

5. **test_list_facts_excludes_deleted**: Test that deleted facts are not returned
   - Mock list_all_facts to include fact with verification_status="deleted"
   - Assert deleted fact not in response

6. **test_list_facts_empty**: Test listing when no facts exist
   - Mock list_all_facts to return empty list
   - Assert 200 with empty array

Add test class `TestBusinessFactsGet` with the following tests:

1. **test_get_fact_success**: Test getting fact by ID
   - Mock WorldModelService.get_fact_by_id to return BusinessFact
   - GET /api/admin/governance/facts/{fact_id}
   - Assert 200 status, verify all fields populated

2. **test_get_fact_not_found**: Test getting non-existent fact
   - Mock get_fact_by_id to return None
   - GET /api/admin/governance/facts/{fact_id}
   - Assert 404 status

3. **test_get_fact_with_metadata**: Test fact with metadata domain extraction
   - Mock fact with metadata={"domain": "custom"}
   - Assert domain="custom" in response

Reference: `backend/api/admin/business_facts_routes.py` lines 66-122 for list/get logic.
  </action>
  <verify>
    Test classes TestBusinessFactsList and TestBusinessFactsGet exist with all tests
  </verify>
  <done>
    All list and get tests pass with mocked WorldModelService
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement create, update, and delete endpoint tests</name>
  <files>backend/tests/api/test_admin_business_facts_routes.py</files>
  <action>
Add test class `TestBusinessFactsCreate` with the following tests:

1. **test_create_fact_success**: Test creating fact with valid data
   - Mock WorldModelService.record_business_fact to return True
   - POST /api/admin/governance/facts with FactCreateRequest
   - Assert 201 status, verify fact returned

2. **test_create_fact_with_citations**: Test creating fact with citations
   - POST with citations=["doc1.pdf:page1"]
   - Assert citations in response

3. **test_create_fact_with_domain**: Test creating fact with custom domain
   - POST with domain="accounting"
   - Assert domain in response

4. **test_create_fact_failure**: Test when record_business_fact fails
   - Mock record_business_fact to return False
   - POST /api/admin/governance/facts
   - Assert 500 error

Add test class `TestBusinessFactsUpdate` with the following tests:

1. **test_update_fact_all_fields**: Test updating all fact fields
   - Mock get_fact_by_id to return existing fact
   - PUT /api/admin/governance/facts/{fact_id} with all fields
   - Assert 200 status, verify updated values

2. **test_update_fact_partial**: Test partial update (only fact text)
   - PUT with only fact field
   - Assert other fields preserved

3. **test_update_fact_verification_status**: Test updating verification status
   - PUT with verification_status="outdated"
   - Verify update_fact_verification called

4. **test_update_fact_not_found**: Test updating non-existent fact
   - Mock get_fact_by_id to return None
   - PUT /api/admin/governance/facts/{fact_id}
   - Assert 404 status

Add test class `TestBusinessFactsDelete` with the following tests:

1. **test_delete_fact_success**: Test soft deleting a fact
   - Mock WorldModelService.delete_fact to return True
   - DELETE /api/admin/governance/facts/{fact_id}
   - Assert 200 status, verify status="deleted"

2. **test_delete_fact_not_found**: Test deleting non-existent fact
   - Mock delete_fact to return False
   - DELETE /api/admin/governance/facts/{fact_id}
   - Assert 404 status

Reference: `backend/api/admin/business_facts_routes.py` lines 125-228 for create/update/delete logic.
  </action>
  <verify>
    Test classes TestBusinessFactsCreate, TestBusinessFactsUpdate, TestBusinessFactsDelete exist with all tests
  </verify>
  <done>
    All CRUD tests pass with mocked WorldModelService
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement file upload endpoint tests</name>
  <files>backend/tests/api/test_admin_business_facts_routes.py</files>
  <action>
Add test class `TestBusinessFactsUpload` with the following tests:

1. **test_upload_and_extract_success**: Test successful document upload and extraction
   - Mock storage_service.upload_file to return "s3://bucket/key"
   - Mock policy_extractor.extract_facts_from_document to return ExtractionResult with facts
   - Mock WorldModelService.bulk_record_facts to return count
   - POST /api/admin/governance/facts/upload with PDF file
   - Assert 200 status, verify facts_extracted count

2. **test_upload_with_custom_domain**: Test upload with domain parameter
   - POST with domain="accounting"
   - Verify facts have domain="accounting"

3. **test_upload_invalid_file_type**: Test upload with unsupported file type
   - POST /api/admin/governance/facts/upload with .exe file
   - Assert 422 validation error with "Unsupported file type"

4. **test_upload_extracts_multiple_facts**: Test extraction of multiple facts
   - Mock extract_facts_from_document to return multiple ExtractedFact
   - Verify all facts stored

5. **test_upload_citation_format**: Test that S3 URI is used as citation
   - Verify citations contain "s3://" format

6. **test_upload_temp_file_cleanup**: Test that temp files are cleaned up
   - Mock os.unlink to verify cleanup
   - Test finally block execution

7. **test_upload_extraction_fails**: Test handling of extraction failure
   - Mock extract_facts_from_document to raise Exception
   - Assert 500 error with details

Reference: `backend/api/admin/business_facts_routes.py` lines 231-333 for upload logic and Phase 178-RESEARCH.md lines 349-402 for file upload patterns.
  </action>
  <verify>
    Test class TestBusinessFactsUpload exists with 7 test methods
  </verify>
  <done>
    All upload tests pass with mocked storage and extractor
  </done>
</task>

<task type="auto">
  <name>Task 5: Implement citation verification tests</name>
  <files>backend/tests/api/test_admin_business_facts_routes.py</files>
  <action>
Add test class `TestBusinessFactsVerify` with the following tests:

1. **test_verify_citation_s3_exists**: Test verifying S3 citation that exists
   - Mock get_fact_by_id to return fact with s3:// citation
   - Mock storage_service.check_exists to return True
   - POST /api/admin/governance/facts/{fact_id}/verify-citation
   - Assert 200 status, new_status="verified"

2. **test_verify_citation_s3_missing**: Test verifying S3 citation that doesn't exist
   - Mock storage_service.check_exists to return False
   - POST /api/admin/governance/facts/{fact_id}/verify-citation
   - Assert 200 status, new_status="outdated"

3. **test_verify_citation_local_exists**: Test verifying local file citation that exists
   - Mock fact with local citation (not s3://)
   - Mock os.path.exists to return True
   - POST /api/admin/governance/facts/{fact_id}/verify-citation
   - Assert citation source="Local"

4. **test_verify_citation_mixed_sources**: Test fact with mixed citation sources
   - Mock fact with both S3 and local citations
   - Mock check_exists for S3, os.path.exists for local
   - Verify results contain both sources

5. **test_verify_citation_all_valid**: Test verification when all citations valid
   - Mock all checks to return True
   - Assert new_status="verified"

6. **test_verify_citation_cross_bucket**: Test citation from different bucket
   - Mock citation with different bucket name
   - Verify proper parsing logic

7. **test_verify_citation_fact_not_found**: Test verifying non-existent fact
   - Mock get_fact_by_id to return None
   - POST /api/admin/governance/facts/{fact_id}/verify-citation
   - Assert 404 status

Reference: `backend/api/admin/business_facts_routes.py` lines 336-407 for citation verification logic.
  </action>
  <verify>
    Test class TestBusinessFactsVerify exists with 7 test methods
  </verify>
  <done>
    All citation verification tests pass with mocked storage
  </done>
</task>

<task type="auto">
  <name>Task 6: Implement authentication and role tests</name>
  <files>backend/tests/api/test_admin_business_facts_routes.py</files>
  <action>
Add test class `TestBusinessFactsAuth` with the following tests:

1. **test_list_facts_requires_admin_role**: Test that non-admin cannot list facts
   - Use regular_user fixture (role="member")
   - Override require_role to fail
   - GET /api/admin/governance/facts should return 403

2. **test_create_fact_requires_admin_role**: Test that non-admin cannot create facts
   - Use regular_user
   - POST /api/admin/governance/facts should return 403

3. **test_upload_requires_admin_role**: Test that non-admin cannot upload
   - Use regular_user
   - POST /api/admin/governance/facts/upload should return 403

4. **test_delete_fact_requires_admin_role**: Test that non-admin cannot delete
   - Use regular_user
   - DELETE /api/admin/governance/facts/{fact_id} should return 403

Reference: `backend/api/admin/business_facts_routes.py` lines 72, 102, 129, 169, 216, 236, 340 for require_role dependency.
  </action>
  <verify>
    Test class TestBusinessFactsAuth exists with 4 test methods
  </verify>
  <done>
    All role enforcement tests verify 403 for non-admin users
  </done>
</task>

<task type="auto">
  <name>Task 7: Run tests and verify coverage</name>
  <files>backend/tests/api/test_admin_business_facts_routes.py</files>
  <action>
Execute the following commands:

1. Run the new test file:
   ```bash
   cd /Users/rushiparikh/projects/atom/backend
   pytest tests/api/test_admin_business_facts_routes.py -v
   ```

2. Run with coverage on business facts routes:
   ```bash
   pytest tests/api/test_admin_business_facts_routes.py --cov=api/admin/business_facts_routes --cov-report=term-missing --cov-report=json
   ```

3. Verify coverage is 75%+:
   ```bash
   # Check coverage.json for api/admin/business_facts_routes.py percent_covered
   ```

4. If coverage < 75%, identify missing lines and add tests:
   - Common gaps: exception handlers in upload, citation parsing edge cases
   - Add tests for uncovered conditional branches

5. Run existing business facts tests to ensure compatibility:
   ```bash
   pytest tests/test_business_facts_routes.py -v
   ```

Reference: Phase 177-01-SUMMARY.md for coverage measurement approach.
  </action>
  <verify>
    Coverage report shows 75%+ for api/admin/business_facts_routes.py
  </verify>
  <done>
    api/admin/business_facts_routes.py achieves 75%+ line coverage, all tests pass
  </done>
</task>

</tasks>

<verification>
Overall phase verification:

1. Test file exists at backend/tests/api/test_admin_business_facts_routes.py
2. All test fixtures follow per-file FastAPI app pattern
3. All 7 endpoints tested:
   - GET /api/admin/governance/facts (list with filters, deleted filter)
   - GET /api/admin/governance/facts/{fact_id} (success, 404)
   - POST /api/admin/governance/facts (create, success/error)
   - PUT /api/admin/governance/facts/{fact_id} (all fields, partial, 404)
   - DELETE /api/admin/governance/facts/{fact_id} (soft delete, 404)
   - POST /api/admin/governance/facts/upload (upload, extraction, invalid type)
   - POST /api/admin/governance/facts/{fact_id}/verify-citation (S3, local, mixed, 404)
4. Coverage measured via pytest --cov shows 75%+ for api/admin/business_facts_routes.py
5. All tests pass when run individually and with existing test suite
</verification>

<success_criteria>
1. api/admin/business_facts_routes.py achieves 75%+ line coverage
2. Test file has 700-900 lines with 30+ tests
3. All 7 endpoints tested with happy and error cases
4. External services properly mocked (WorldModelService, storage service, policy extractor)
5. Role enforcement tested (ADMIN required)
</success_criteria>

<output>
After completion, create `.planning/phases/178-api-routes-coverage-admin-system/178-02-SUMMARY.md` with:
- Coverage achieved (line count and percentage)
- Number of tests written
- Any challenges encountered and solutions
- Missing coverage areas (if any) for future improvement
</output>
