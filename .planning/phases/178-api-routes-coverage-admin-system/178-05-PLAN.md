---
phase: 178-api-routes-coverage-admin-system
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_admin_routes_coverage.py
autonomous: true

must_haves:
  truths:
    - "Admin routes achieve 75%+ line coverage measured by pytest --cov"
    - "Admin user CRUD tested (list, get, create, update, delete, update last login)"
    - "Admin role CRUD tested (list, get, create, update, delete with user assignment check)"
    - "WebSocket management tested (status, reconnect, enable, disable)"
    - "Rating sync tested (trigger, failed uploads list, retry failed upload)"
    - "Conflict resolution tested (list, get detail, resolve, bulk resolve)"
    - "Authentication tested (super_admin requirement for all endpoints)"
    - "Error paths tested (404 not found, 409 conflict, 422 validation, 500 internal error)"
  artifacts:
    - path: "backend/tests/api/test_admin_routes_coverage.py"
      provides: "Admin routes coverage tests"
      min_lines: 800
      exports: ["TestAdminUserList", "TestAdminUserGet", "TestAdminUserCreate", "TestAdminUserUpdate", "TestAdminUserDelete", "TestAdminUserLastLogin", "TestAdminRoleList", "TestAdminRoleGet", "TestAdminRoleCreate", "TestAdminRoleUpdate", "TestAdminRoleDelete", "TestWebSocketStatus", "TestWebSocketReconnect", "TestWebSocketToggle", "TestRatingSync", "TestFailedRatingUploads", "TestRetryRatingUpload", "TestConflictList", "TestConflictGet", "TestConflictResolve", "TestBulkConflictResolve"]
  key_links:
    - from: "backend/tests/api/test_admin_routes_coverage.py"
      to: "api/admin_routes.py"
      via: "import router and create TestClient"
      pattern: "from api.admin_routes import router"
---

<objective>
Create comprehensive test coverage for admin routes (`api/admin_routes.py`) achieving 75%+ line coverage.

Purpose: Admin routes manage administrative users and roles with RBAC, WebSocket connection management, rating synchronization with Atom SaaS, and conflict resolution for sync operations. All endpoints require super_admin role and critical operations use AUTONOMOUS maturity governance.

Output: Test file `test_admin_routes_coverage.py` with 800-1000 lines covering all 22 endpoints with happy paths, error conditions, governance enforcement, and proper mocking of external services.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/178-api-routes-coverage-admin-system/178-RESEARCH.md
@backend/api/admin_routes.py
@backend/tests/api/test_admin_routes_part1.py
@backend/tests/api/test_admin_routes_part2.py
@backend/core/models.py (AdminUser, AdminRole, WebSocketState, FailedRatingUpload, ConflictLog)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test file with fixtures for admin routes</name>
  <files>backend/tests/api/test_admin_routes_coverage.py</files>
  <action>
Create `backend/tests/api/test_admin_routes_coverage.py` with the following structure:

1. **Module docstring**: Describe coverage scope (admin routes, 75%+ target, 22 endpoints across 5 categories)

2. **Imports**: Standard test imports plus admin specific:
   ```python
   import pytest
   import uuid
   import io
   from datetime import datetime, timezone
   from typing import List
   from unittest.mock import MagicMock, AsyncMock, patch
   from fastapi import FastAPI
   from fastapi.testclient import TestClient
   from sqlalchemy import create_engine
   from sqlalchemy.orm import sessionmaker, Session
   from sqlalchemy.pool import StaticPool
   from api.admin_routes import router
   from core.models import Base, User, AdminUser, AdminRole, WebSocketState, FailedRatingUpload, SkillRating, ConflictLog
   ```

3. **Test database fixture** (`test_db`): In-memory SQLite with StaticPool, create all admin tables

4. **FastAPI app fixture** (`test_app`): Create FastAPI app, include admin_routes router, override get_db

5. **TestClient fixture** (`client`): Return TestClient(test_app)

6. **Super admin user fixture** (`super_admin_user`): User with role="super_admin", status="active"

7. **Regular user fixture** (`regular_user`): User with role="member" (for auth testing)

8. **Authenticated admin client fixture** (`authenticated_admin_client`): Override get_current_user and require_super_admin

9. **Unauthenticated client fixture** (`unauthenticated_client`): No auth override

10. **Admin role fixture** (`test_admin_role`): AdminRole with permissions

11. **Admin user fixture** (`test_admin_user`): AdminUser linked to test_admin_role

12. **Mock RatingSyncService fixture** (`mock_rating_sync_service`): AsyncMock for sync operations

13. **Mock AtomSaaSClient fixture** (`mock_saas_client`): AsyncMock for SaaS API calls

14. **Mock ConflictResolutionService fixture** (`mock_conflict_resolver`): AsyncMock for conflict operations

Reference: `backend/tests/api/test_admin_routes_part1.py` lines 40-150 and `test_admin_routes_part2.py` lines 40-125 for fixture patterns.
  </action>
  <verify>
    File exists at backend/tests/api/test_admin_routes_coverage.py with all fixtures defined
  </verify>
  <done>
    All 14 fixtures are present and follow Phase 177/178 patterns
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement admin user list and get endpoint tests</name>
  <files>backend/tests/api/test_admin_routes_coverage.py</files>
  <action>
Add test class `TestAdminUserList` with the following tests:

1. **test_list_admin_users_success**: Test listing all admin users
   - Create multiple AdminUser with different AdminRole in test_db
   - GET /api/admin/users with authenticated admin client
   - Assert 200 status, verify all users returned with role_name and permissions

2. **test_list_admin_users_empty**: Test listing when no users exist
   - Query with empty database
   - Assert 200 status with empty list

3. **test_list_admin_users_includes_permissions**: Test that permissions are included
   - Create AdminRole with specific permissions
   - Create AdminUser linked to role
   - GET /api/admin/users
   - Assert response includes permissions dict

Add test class `TestAdminUserGet` with the following tests:

1. **test_get_admin_user_success**: Test getting specific admin user
   - Create AdminUser in test_db
   - GET /api/admin/users/{admin_id}
   - Assert 200 status, verify all fields (id, email, name, role_id, status, last_login, created_at)

2. **test_get_admin_user_not_found**: Test getting non-existent admin user
   - GET /api/admin/users/{nonexistent_id}
   - Assert 404 status

3. **test_get_admin_user_includes_created_at**: Test created_at field is returned
   - Create AdminUser with known created_at
   - GET /api/admin/users/{admin_id}
   - Assert created_at in response

Reference: `backend/api/admin_routes.py` lines 127-154 for list/get logic.
  </action>
  <verify>
    Test classes TestAdminUserList and TestAdminUserGet exist with all tests
  </verify>
  <done>
    All list and get tests pass with proper data population
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement admin user create, update, and delete tests</name>
  <files>backend/tests/api/test_admin_routes_coverage.py</files>
  <action>
Add test class `TestAdminUserCreate` with the following tests:

1. **test_create_admin_user_success**: Test successful admin user creation
   - Create AdminRole in test_db
   - POST /api/admin/users with CreateAdminUserRequest (email, name, password, role_id)
   - Assert 201 status, verify user returned with all fields

2. **test_create_admin_user_role_not_found**: Test creation with non-existent role
   - POST /api/admin/users with invalid role_id
   - Assert 404 status with field="role_id" details

3. **test_create_admin_user_duplicate_email**: Test creation with duplicate email
   - Create first AdminUser with email
   - POST /api/admin/users with same email
   - Assert 409 status with "already exists" message

4. **test_create_admin_user_password_hashed**: Test that password is hashed
   - POST /api/admin/users
   - Verify password_hash is set, not plain password

5. **test_create_admin_user_default_status**: Test that status defaults to "active"
   - POST /api/admin/users
   - Assert status="active" in response

Add test class `TestAdminUserUpdate` with the following tests:

1. **test_update_admin_user_name**: Test updating only name
   - Create AdminUser in test_db
   - PATCH /api/admin/users/{admin_id} with name only
   - Assert 200 status, verify name updated, other fields unchanged

2. **test_update_admin_user_role**: Test updating role
   - Create second AdminRole in test_db
   - PATCH /api/admin/users/{admin_id} with role_id
   - Assert 200 status, verify role_id updated

3. **test_update_admin_user_role_not_found**: Test updating with non-existent role
   - PATCH /api/admin/users/{admin_id} with invalid role_id
   - Assert 404 status

4. **test_update_admin_user_status**: Test updating status
   - PATCH /api/admin/users/{admin_id} with status="inactive"
   - Assert 200 status, verify status updated

5. **test_update_admin_user_not_found**: Test updating non-existent user
   - PATCH /api/admin/users/{nonexistent_id}
   - Assert 404 status

Add test class `TestAdminUserDelete` with the following tests:

1. **test_delete_admin_user_success**: Test successful deletion
   - Create AdminUser in test_db
   - DELETE /api/admin/users/{admin_id}
   - Assert 200 status with success message

2. **test_delete_admin_user_not_found**: Test deleting non-existent user
   - DELETE /api/admin/users/{nonexistent_id}
   - Assert 404 status

Reference: `backend/api/admin_routes.py` lines 183-298 for create/update/delete logic.
  </action>
  <verify>
    Test classes TestAdminUserCreate, TestAdminUserUpdate, TestAdminUserDelete exist with all tests
  </verify>
  <done>
    All CRUD tests pass with proper error handling
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement admin user last login and admin role tests</name>
  <files>backend/tests/api/test_admin_routes_coverage.py</files>
  <action>
Add test class `TestAdminUserLastLogin` with the following tests:

1. **test_update_last_login_success**: Test updating last login timestamp
   - Create AdminUser in test_db
   - PATCH /api/admin/users/{admin_id}/last-login
   - Assert 200 status with success message

2. **test_update_last_login_not_found**: Test updating non-existent user
   - PATCH /api/admin/users/{nonexistent_id}/last-login
   - Assert 404 status

3. **test_update_last_login_sets_timestamp**: Test that timestamp is set correctly
   - Create AdminUser with old last_login
   - PATCH /api/admin/users/{admin_id}/last-login
   - Verify last_login updated to recent datetime

Add test class `TestAdminRoleList` with the following tests:

1. **test_list_admin_roles_success**: Test listing all admin roles
   - Create multiple AdminRole in test_db
   - GET /api/admin/roles
   - Assert 200 status, verify all roles with permissions

2. **test_list_admin_roles_empty**: Test listing when no roles exist
   - Assert 200 status with empty list

Add test class `TestAdminRoleGet` with the following tests:

1. **test_get_admin_role_success**: Test getting specific role
   - Create AdminRole in test_db
   - GET /api/admin/roles/{role_id}
   - Assert 200 status, verify all fields

2. **test_get_admin_role_not_found**: Test getting non-existent role
   - GET /api/admin/roles/{nonexistent_id}
   - Assert 404 status

Add test class `TestAdminRoleCreate` with the following tests:

1. **test_create_admin_role_success**: Test successful role creation
   - POST /api/admin/roles with CreateAdminRoleRequest (name, permissions, description)
   - Assert 201 status, verify role returned

2. **test_create_admin_role_duplicate_name**: Test creation with duplicate name
   - Create first AdminRole with name
   - POST /api/admin/roles with same name
   - Assert 409 status with "already exists" message

Reference: `backend/api/admin_routes.py` lines 336-454 for last login and role logic.
  </action>
  <verify>
    Test classes TestAdminUserLastLogin, TestAdminRoleList, TestAdminRoleGet, TestAdminRoleCreate exist with all tests
  </verify>
  <done>
    All last login and role tests pass
  </done>
</task>

<task type="auto">
  <name>Task 5: Implement admin role update and delete tests</name>
  <files>backend/tests/api/test_admin_routes_coverage.py</files>
  <action>
Add test class `TestAdminRoleUpdate` with the following tests:

1. **test_update_admin_role_name**: Test updating role name
   - Create AdminRole in test_db
   - PATCH /api/admin/roles/{role_id} with new name
   - Assert 200 status, verify name updated

2. **test_update_admin_role_name_conflict**: Test updating name to existing name
   - Create two AdminRole with different names
   - PATCH first role with second role's name
   - Assert 409 status with "already exists" message

3. **test_update_admin_role_permissions**: Test updating permissions
   - PATCH /api/admin/roles/{role_id} with new permissions dict
   - Assert 200 status, verify permissions updated

4. **test_update_admin_role_description**: Test updating description
   - PATCH /api/admin/roles/{role_id} with description
   - Assert 200 status, verify description updated

5. **test_update_admin_role_not_found**: Test updating non-existent role
   - PATCH /api/admin/roles/{nonexistent_id}
   - Assert 404 status

Add test class `TestAdminRoleDelete` with the following tests:

1. **test_delete_admin_role_success**: Test successful role deletion
   - Create AdminRole in test_db (no users assigned)
   - DELETE /api/admin/roles/{role_id}
   - Assert 200 status with success message

2. **test_delete_admin_role_with_users_fails**: Test deletion when role assigned to users
   - Create AdminRole and AdminUser with that role_id
   - DELETE /api/admin/roles/{role_id}
   - Assert 409 status with "still assigned" message

3. **test_delete_admin_role_not_found**: Test deleting non-existent role
   - DELETE /api/admin/roles/{nonexistent_id}
   - Assert 404 status

Reference: `backend/api/admin_routes.py` lines 457-544 for role update/delete logic.
  </action>
  <verify>
    Test classes TestAdminRoleUpdate and TestAdminRoleDelete exist with all tests
  </verify>
  <done>
    All role update/delete tests pass with proper constraint handling
  </done>
</task>

<task type="auto">
  <name>Task 6: Implement WebSocket management tests</name>
  <files>backend/tests/api/test_admin_routes_coverage.py</files>
  <action>
Add test class `TestWebSocketStatus` with the following tests:

1. **test_get_websocket_status_connected**: Test WebSocket status when connected
   - Create WebSocketState with connected=True, ws_url, timestamps in test_db
   - GET /api/admin/websocket/status
   - Assert 200 status, verify all fields populated

2. **test_get_websocket_status_no_state**: Test WebSocket status when no state exists
   - Query without WebSocketState in database
   - Assert 200 status with default values (connected=False, reconnect_attempts=0)

3. **test_get_websocket_status_disconnected**: Test WebSocket status when disconnected
   - Create WebSocketState with connected=False, disconnect_reason
   - GET /api/admin/websocket/status
   - Assert 200 status, verify connected=False and reason included

Add test class `TestWebSocketReconnect` with the following tests:

1. **test_trigger_websocket_reconnect**: Test forcing WebSocket reconnection
   - Create WebSocketState in test_db
   - POST /api/admin/websocket/reconnect
   - Assert 200 status, reconnect_triggered=True

2. **test_trigger_websocket_reconnect_resets_counters**: Test that reconnect resets counters
   - Create WebSocketState with reconnect_attempts=5, consecutive_failures=3
   - POST /api/admin/websocket/reconnect
   - Verify reconnect_attempts=0, consecutive_failures=0, fallback_to_polling=False

Add test class `TestWebSocketToggle` with the following tests:

1. **test_disable_websocket**: Test disabling WebSocket
   - Create WebSocketState in test_db
   - POST /api/admin/websocket/disable
   - Assert 200 status, enabled=False, fallback_to_polling=True

2. **test_enable_websocket**: Test enabling WebSocket
   - Create WebSocketState with fallback_to_polling=True
   - POST /api/admin/websocket/enable
   - Assert 200 status, enabled=True, fallback_to_polling=False

3. **test_disable_websocket_sets_disconnect_reason**: Test disable sets disconnect reason
   - POST /api/admin/websocket/disable
   - Verify disconnect_reason="disabled_by_admin"

Reference: `backend/api/admin_routes.py` lines 577-757 for WebSocket logic.
  </action>
  <verify>
    Test classes TestWebSocketStatus, TestWebSocketReconnect, TestWebSocketToggle exist with all tests
  </verify>
  <done>
    All WebSocket management tests pass
  </done>
</task>

<task type="auto">
  <name>Task 7: Implement rating sync tests</name>
  <files>backend/tests/api/test_admin_routes_coverage.py</files>
  <action>
Add test class `TestRatingSync` with the following tests:

1. **test_trigger_rating_sync_success**: Test triggering rating sync
   - Mock RatingSyncService.sync_ratings to return {"success": True, "uploaded": 10, "failed": 0, "skipped": 0}
   - POST /api/admin/sync/ratings with upload_all=False
   - Assert 200 status, verify counts in response

2. **test_trigger_rating_sync_upload_all**: Test sync with upload_all flag
   - Mock RatingSyncService.sync_ratings
   - POST /api/admin/sync/ratings with upload_all=True
   - Verify upload_all passed to service

3. **test_trigger_rating_sync_in_progress**: Test sync when already in progress
   - Mock RatingSyncService._sync_in_progress=True
   - POST /api/admin/sync/ratings
   - Assert 503 status with "already in progress" message

4. **test_trigger_rating_sync_governance_enforced**: Test HIGH complexity governance
   - Mock non-AUTONOMOUS maturity
   - POST /api/admin/sync/ratings should fail governance check

Add test class `TestFailedRatingUploads` with the following tests:

1. **test_get_failed_rating_uploads**: Test listing failed uploads
   - Create FailedRatingUpload records in test_db
   - GET /api/admin/ratings/failed-uploads
   - Assert 200 status, verify failed uploads returned

2. **test_get_failed_rating_uploads_empty**: Test listing when no failed uploads
   - GET /api/admin/ratings/failed-uploads
   - Assert 200 status with empty list

3. **test_get_failed_uploads_limited**: Test that results are limited
   - Create 150 FailedRatingUpload records
   - GET /api/admin/ratings/failed-uploads
   - Assert max 100 records returned (route's limit)

Add test class `TestRetryRatingUpload` with the following tests:

1. **test_retry_failed_upload_success**: Test retrying failed upload successfully
   - Create FailedRatingUpload and SkillRating in test_db
   - Mock RatingSyncService.upload_rating to return {"success": True, "rating_id": "remote-123"}
   - POST /api/admin/ratings/failed-uploads/{failed_id}/retry
   - Assert 200 status, success=True

2. **test_retry_failed_upload_rating_deleted**: Test retry when rating was deleted
   - Create FailedRatingUpload but no SkillRating
   - POST /api/admin/ratings/failed-uploads/{failed_id}/retry
   - Assert 200 status, failed record removed

3. **test_retry_failed_upload_fails_again**: Test retry that fails again
   - Create FailedRatingUpload and SkillRating
   - Mock RatingSyncService.upload_rating to return {"success": False, "error": "API error"}
   - POST /api/admin/ratings/failed-uploads/{failed_id}/retry
   - Assert 200 status, success=False, retry_count incremented

4. **test_retry_failed_upload_not_found**: Test retrying non-existent failed upload
   - POST /api/admin/ratings/failed-uploads/{nonexistent_id}/retry
   - Assert 404 status

5. **test_retry_upload_governance_enforced**: Test HIGH complexity governance
   - Mock non-AUTONOMOUS maturity
   - POST retry should fail governance check

Reference: `backend/api/admin_routes.py` lines 799-1018 for rating sync logic.
  </action>
  <verify>
    Test classes TestRatingSync, TestFailedRatingUploads, TestRetryRatingUpload exist with all tests
  </verify>
  <done>
    All rating sync tests pass with proper service mocking
  </done>
</task>

<task type="auto">
  <name>Task 8: Implement conflict resolution tests</name>
  <files>backend/tests/api/test_admin_routes_coverage.py</files>
  <action>
Add test class `TestConflictList` with the following tests:

1. **test_list_conflicts**: Test listing unresolved conflicts
   - Create ConflictLog records in test_db
   - Mock ConflictResolutionService.get_unresolved_conflicts to return conflicts
   - GET /api/admin/conflicts
   - Assert 200 status, verify conflicts returned

2. **test_list_conflicts_with_filters**: Test listing with severity and type filters
   - Mock get_unresolved_conflicts with severity="HIGH", conflict_type="skill_data"
   - GET /api/admin/conflicts?severity=HIGH&conflict_type=skill_data
   - Assert filters passed to service

3. **test_list_conflicts_with_pagination**: Test pagination parameters
   - GET /api/admin/conflicts?page=2&page_size=25
   - Assert page and page_size passed to service

Add test class `TestConflictGet` with the following tests:

1. **test_get_conflict_success**: Test getting conflict by ID
   - Create ConflictLog in test_db
   - Mock ConflictResolutionService.get_conflict_by_id to return conflict
   - GET /api/admin/conflicts/{conflict_id}
   - Assert 200 status, verify all fields

2. **test_get_conflict_not_found**: Test getting non-existent conflict
   - Mock get_conflict_by_id to return None
   - GET /api/admin/conflicts/{conflict_id}
   - Assert 404 status

Add test class `TestConflictResolve` with the following tests:

1. **test_resolve_conflict_remote_wins**: Test resolving with remote_wins strategy
   - Create ConflictLog in test_db
   - Mock ConflictResolutionService.resolve_conflict to return resolved_data
   - POST /api/admin/conflicts/{conflict_id}/resolve with strategy=remote_wins
   - Assert 200 status, success=True

2. **test_resolve_conflict_local_wins**: Test resolving with local_wins strategy
   - POST /api/admin/conflicts/{conflict_id}/resolve with strategy=local_wins
   - Assert 200 status

3. **test_resolve_conflict_merge**: Test resolving with merge strategy
   - POST /api/admin/conflicts/{conflict_id}/resolve with strategy=merge
   - Assert 200 status

4. **test_resolve_conflict_invalid_strategy**: Test with invalid strategy
   - POST /api/admin/conflicts/{conflict_id}/resolve with strategy=invalid
   - Assert 422 validation error with valid_strategies in details

5. **test_resolve_conflict_not_found**: Test resolving non-existent conflict
   - Mock get_conflict_by_id to return None
   - POST /api/admin/conflicts/{conflict_id}/resolve
   - Assert 404 status

Add test class `TestBulkConflictResolve` with the following tests:

1. **test_bulk_resolve_conflicts_success**: Test bulk resolving conflicts
   - Create multiple ConflictLog in test_db
   - Mock resolve_conflict to return resolved data for each
   - POST /api/admin/conflicts/bulk-resolve with conflict_ids=[...] and strategy=remote_wins
   - Assert 200 status, resolved_count=3, failed_count=0

2. **test_bulk_resolve_with_failures**: Test bulk resolve with some failures
   - Mock resolve_conflict to return None for some conflicts
   - POST /api/admin/conflicts/bulk-resolve
   - Assert failed_count > 0, errors populated

3. **test_bulk_resolve_invalid_strategy**: Test bulk resolve with invalid strategy
   - POST /api/admin/conflicts/bulk-resolve with strategy=invalid
   - Assert 422 validation error

4. **test_bulk_resolve_empty_conflict_ids**: Test with empty conflict_ids list
   - POST /api/admin/conflicts/bulk-resolve with conflict_ids=[]
   - Assert 422 validation error

5. **test_bulk_resolve_too_many_conflicts**: Test exceeding max conflict_ids
   - POST /api/admin/conflicts/bulk-resolve with 101 conflict_ids
   - Assert 422 validation error

Reference: `backend/api/admin_routes.py` lines 1079-1354 for conflict resolution logic.
  </action>
  <verify>
    Test classes TestConflictList, TestConflictGet, TestConflictResolve, TestBulkConflictResolve exist with all tests
  </verify>
  <done>
    All conflict resolution tests pass with proper service mocking
  </done>
</task>

<task type="auto">
  <name>Task 9: Implement authentication and governance tests</name>
  <files>backend/tests/api/test_admin_routes_coverage.py</files>
  <action>
Add test class `TestAdminRoutesAuth` with the following tests:

1. **test_list_users_requires_super_admin**: Test that non-super_admin cannot list users
   - Use regular_user fixture (role="member")
   - GET /api/admin/users should return 403

2. **test_create_user_requires_super_admin**: Test that non-super_admin cannot create users
   - Use regular_user
   - POST /api/admin/users should return 403

3. **test_create_role_requires_super_admin**: Test that non-super_admin cannot create roles
   - Use regular_user
   - POST /api/admin/roles should return 403

4. **test_unauthenticated_request_fails**: Test unauthenticated request
   - Use unauthenticated_client (no auth override)
   - GET /api/admin/users should return 401 or 403

5. **test_inactive_admin_blocked**: Test that inactive admin is blocked
   - Create super_admin with status="inactive"
   - Attempt operation should fail auth

Add test class `TestGovernanceEnforcement` with the following tests:

1. **test_create_user_governance_CRITICAL**: Test CRITICAL complexity governance
   - Mock non-AUTONOMOUS maturity
   - POST /api/admin/users should fail with 403

2. **test_delete_user_governance_CRITICAL**: Test delete user governance
   - Mock non-AUTONOMOUS maturity
   - DELETE /api/admin/users/{id} should fail with 403

3. **test_create_role_governance_CRITICAL**: Test create role governance
   - Mock non-AUTONOMOUS maturity
   - POST /api/admin/roles should fail with 403

4. **test_delete_role_governance_CRITICAL**: Test delete role governance
   - Mock non-AUTONOMOUS maturity
   - DELETE /api/admin/roles/{id} should fail with 403

5. **test_rating_sync_governance_HIGH**: Test HIGH complexity governance
   - Mock non-AUTONOMOUS maturity
   - POST /api/admin/sync/ratings should fail with 403

6. **test_websocket_disable_governance_HIGH**: Test WebSocket disable governance
   - Mock non-AUTONOMOUS maturity
   - POST /api/admin/websocket/disable should fail with 403

7. **test_resolve_conflict_governance_HIGH**: Test conflict resolve governance
   - Mock non-AUTONOMOUS maturity
   - POST /api/admin/conflicts/{id}/resolve should fail with 403

8. **test_AUTONOMOUS_passes_all_checks**: Test AUTONOMOUS maturity passes all
   - Mock AUTONOMOUS maturity
   - All operations should succeed (200/201)

Reference: `backend/api/admin_routes.py` lines 29-41 for require_super_admin, and @require_governance decorators on lines 184-188, 301-305, 408-412, 506-510, 632-636, 676-680, 719-723, 800-804, 926-930, 1080-1084, 1150-1154, 1198-1202, 1285-1289.
  </action>
  <verify>
    Test classes TestAdminRoutesAuth and TestGovernanceEnforcement exist with all tests
  </verify>
  <done>
    All authentication and governance tests verify proper enforcement
  </done>
</task>

<task type="auto">
  <name>Task 10: Run tests and verify coverage</name>
  <files>backend/tests/api/test_admin_routes_coverage.py</files>
  <action>
Execute the following commands:

1. Run the new test file:
   ```bash
   cd /Users/rushiparikh/projects/atom/backend
   pytest tests/api/test_admin_routes_coverage.py -v
   ```

2. Run with coverage on admin routes:
   ```bash
   pytest tests/api/test_admin_routes_coverage.py --cov=api/admin_routes --cov-report=term-missing --cov-report=json
   ```

3. Verify coverage is 75%+:
   ```bash
   # Check coverage.json for api/admin_routes.py percent_covered
   ```

4. If coverage < 75%, identify missing lines and add tests:
   - Check coverage report for missing lines
   - Common gaps: exception handlers, edge cases in sync operations, optional parameter handling
   - Add tests for uncovered conditional branches

5. Run existing admin routes tests to ensure compatibility:
   ```bash
   pytest tests/api/test_admin_routes_part1.py -v
   pytest tests/api/test_admin_routes_part2.py -v
   ```

6. Verify all endpoint categories are covered:
   - Admin user CRUD (6 endpoints)
   - Admin role CRUD (5 endpoints)
   - WebSocket management (4 endpoints)
   - Rating sync (3 endpoints)
   - Conflict resolution (4 endpoints)

Reference: Phase 177-01-SUMMARY.md for coverage measurement approach.
  </action>
  <verify>
    Coverage report shows 75%+ for api/admin_routes.py
  </verify>
  <done>
    api/admin_routes.py achieves 75%+ line coverage, all tests pass
  </done>
</task>

</tasks>

<verification>
Overall phase verification:

1. Test file exists at backend/tests/api/test_admin_routes_coverage.py
2. All test fixtures follow per-file FastAPI app pattern
3. All 22 endpoints tested across 5 categories:
   - Admin user CRUD (6): list, get, create, update, delete, update last login
   - Admin role CRUD (5): list, get, create, update, delete
   - WebSocket management (4): status, reconnect, disable, enable
   - Rating sync (3): trigger sync, failed uploads, retry
   - Conflict resolution (4): list, get detail, resolve, bulk resolve
4. Coverage measured via pytest --cov shows 75%+ for api/admin_routes.py
5. All tests pass when run individually and with existing test suite
6. Authentication tested (super_admin requirement)
7. Governance enforcement tested (AUTONOMOUS required for CRITICAL/HIGH operations)
</verification>

<success_criteria>
1. api/admin_routes.py achieves 75%+ line coverage
2. Test file has 800-1000 lines with 45+ tests
3. All 22 endpoints tested with happy and error cases
4. External services properly mocked (RatingSyncService, AtomSaaSClient, ConflictResolutionService)
5. Governance enforcement tested (AUTONOMOUS required for critical operations)
6. Database constraints tested (duplicate email, role name conflict, role with users)
</success_criteria>

<output>
After completion, create `.planning/phases/178-api-routes-coverage-admin-system/178-05-SUMMARY.md` with:
- Coverage achieved (line count and percentage)
- Number of tests written
- Any challenges encountered and solutions
- Missing coverage areas (if any) for future improvement
- Total phase 178 coverage summary (all 5 route groups combined: skill_routes, business_facts_routes, system_health_routes, sync_admin_routes, admin_routes)
</output>
