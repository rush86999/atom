---
phase: 178-api-routes-coverage-admin-system
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_admin_system_health_routes.py
autonomous: true

must_haves:
  truths:
    - "System health routes achieve 75%+ line coverage measured by pytest --cov"
    - "GET /api/admin/health tested (all services operational, degraded states, status determination)"
    - "Database health check tested (operational, degraded, timeout)"
    - "Redis health check tested (operational, degraded, disabled, no client)"
    - "Vector store health check tested (operational, degraded, maintenance mode, import failure)"
    - "Public health routes tested (liveness, readiness with DB/disk checks, metrics endpoint)"
  artifacts:
    - path: "backend/tests/api/test_admin_system_health_routes.py"
      provides: "System health routes coverage tests"
      min_lines: 500
      exports: ["TestAdminSystemHealth", "TestDatabaseHealth", "TestRedisHealth", "TestVectorHealth", "TestPublicHealthLiveness", "TestPublicHealthReadiness", "TestPublicHealthMetrics"]
  key_links:
    - from: "backend/tests/api/test_admin_system_health_routes.py"
      to: "api/admin/system_health_routes.py"
      via: "import router and create TestClient"
      pattern: "from api.admin.system_health_routes import router"
    - from: "backend/tests/api/test_admin_system_health_routes.py"
      to: "api/health_routes.py"
      via: "import router and create TestClient"
      pattern: "from api.health_routes import router as health_router"
---

<objective>
Create comprehensive test coverage for system health routes (`api/admin/system_health_routes.py` and `api/health_routes.py`) achieving 75%+ line coverage.

Purpose: System health routes provide monitoring for admin dashboard (database, Redis, LanceDB) and public orchestration probes (liveness, readiness, metrics). Admin routes require super_admin role; public routes require no authentication.

Output: Test file `test_admin_system_health_routes.py` with 500-700 lines covering all health check endpoints with various service states.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/178-api-routes-coverage-admin-system/178-RESEARCH.md
@backend/api/admin/system_health_routes.py
@backend/api/health_routes.py
@backend/tests/api/test_health_routes.py
@backend/tests/api/test_admin_routes_part1.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test file with fixtures for system health routes</name>
  <files>backend/tests/api/test_admin_system_health_routes.py</files>
  <action>
Create `backend/tests/api/test_admin_system_health_routes.py` with the following structure:

1. **Module docstring**: Describe coverage scope (system health routes, 75%+ target, admin + public health endpoints)

2. **Imports**: Standard test imports plus health specific:
   ```python
   import pytest
   import time
   from unittest.mock import MagicMock, AsyncMock, patch
   from fastapi import FastAPI
   from fastapi.testclient import TestClient
   from sqlalchemy import create_engine
   from sqlalchemy.orm import sessionmaker, Session
   from sqlalchemy.pool import StaticPool
   from sqlalchemy import text
   from api.admin.system_health_routes import router as admin_health_router
   from api.health_routes import router as health_router
   from core.models import Base, User
   ```

3. **Test database fixture** (`test_db`): In-memory SQLite with StaticPool

4. **Admin health app fixture** (`admin_health_app`): FastAPI app with admin_health_router

5. **Public health app fixture** (`public_health_app`): FastAPI app with health_router

6. **Admin client fixture** (`admin_health_client`): TestClient for admin health routes

7. **Public client fixture** (`public_health_client`): TestClient for public health routes

8. **Super admin user fixture** (`super_admin_user`): User with role="super_admin"

9. **Authenticated admin client fixture** (`authenticated_admin_client`): Override get_super_admin dependency

10. **Mock database fixture** (`mock_db`): MagicMock for database health checks

11. **Mock Redis fixture** (`mock_redis`): MagicMock for Redis health checks

12. **Mock LanceDB handler fixture** (`mock_lancedb`): MagicMock for LanceDB health checks

13. **Mock psutil fixture** (`mock_psutil`): MagicMock for disk space checks

Reference: `backend/tests/api/test_health_routes.py` lines 26-35 for app structure and Phase 178-RESEARCH.md lines 404-460 for health check patterns.
  </action>
  <verify>
    File exists at backend/tests/api/test_admin_system_health_routes.py with all fixtures defined
  </verify>
  <done>
    All 13 fixtures are present and follow Phase 177 patterns
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement admin system health endpoint tests</name>
  <files>backend/tests/api/test_admin_system_health_routes.py</files>
  <action>
Add test class `TestAdminSystemHealth` with the following tests:

1. **test_admin_system_health_all_operational**: Test all services operational
   - Mock db.execute to return quickly (<2s)
   - Mock cache.redis_client.ping to return True
   - Mock LanceDBHandler.test_connection to return {"connected": True}
   - GET /api/admin/health with authenticated admin client
   - Assert 200 status, status="healthy", all services "operational"

2. **test_admin_system_health_degraded_database**: Test degraded database state
   - Mock db.execute to take >2s (simulate slow query)
   - GET /api/admin/health
   - Assert 200 status, status="degraded", database="degraded"

3. **test_admin_system_health_degraded_redis**: Test degraded Redis state
   - Mock cache.redis_client.ping to return False
   - GET /api/admin/health
   - Assert 200 status, status="degraded", redis="degraded"

4. **test_admin_system_health_degraded_vector**: Test degraded vector store
   - Mock LanceDBHandler.test_connection to return {"connected": False}
   - GET /api/admin/health
   - Assert 200 status, status="degraded", vector_store="degraded"

5. **test_admin_system_health_redis_disabled**: Test when Redis is not enabled
   - Mock cache.redis_client to None
   - Mock cache.config.redis.enabled to False
   - GET /api/admin/health
   - Assert redis="unknown" (not degraded since disabled)

6. **test_admin_system_health_no_redis_client**: Test when Redis enabled but no client
   - Mock cache.redis_client to None
   - Mock cache.config.redis.enabled to True
   - GET /api/admin/health
   - Assert redis="degraded"

7. **test_admin_system_health_vector_maintenance**: Test vector store in maintenance
   - Mock HAS_LANCEDB=False (import failed)
   - GET /api/admin/health
   - Assert vector_store="maintenance"

8. **test_admin_system_health_version_included**: Test version field in response
   - Mock all services operational
   - GET /api/admin/health
   - Assert response includes version="2.1.0"

9. **test_admin_system_health_requires_super_admin**: Test authentication requirement
   - GET /api/admin/health without auth
   - Assert 401 or 403 status

Reference: `backend/api/admin/system_health_routes.py` lines 28-107 for admin health logic.
  </action>
  <verify>
    Test class TestAdminSystemHealth exists with 9 test methods
  </verify>
  <done>
    All admin system health tests pass with mocked services
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement database health check tests</name>
  <files>backend/tests/api/test_admin_system_health_routes.py</files>
  <action>
Add test class `TestDatabaseHealth` with the following tests:

1. **test_database_check_operational_fast**: Test fast database response
   - Mock db.execute with time.time() to return <2s
   - Assert db_status="operational"

2. **test_database_check_degraded_slow**: Test slow database response
   - Mock db.execute with time.time() to return >2s
   - Assert db_status="degraded"

3. **test_database_check_exception**: Test database exception handling
   - Mock db.execute to raise Exception
   - Assert db_status="degraded" (exception caught)

4. **test_database_check_timeout**: Test database timeout scenario
   - Mock db.execute to hang (simulate timeout)
   - Assert db_status="degraded"

Reference: `backend/api/admin/system_health_routes.py` lines 41-50 for database health logic.
  </action>
  <verify>
    Test class TestDatabaseHealth exists with 4 test methods
  </verify>
  <done>
    All database health tests cover all code paths
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement Redis and vector store health tests</name>
  <files>backend/tests/api/test_admin_system_health_routes.py</files>
  <action>
Add test class `TestRedisHealth` with the following tests:

1. **test_redis_check_operational**: Test Redis with ping success
   - Mock cache.redis_client.ping to return True
   - Assert redis_status="operational"

2. **test_redis_check_degraded**: Test Redis with ping failure
   - Mock cache.redis_client.ping to return False
   - Assert redis_status="degraded"

3. **test_redis_check_exception**: Test Redis exception handling
   - Mock cache.redis_client.ping to raise Exception
   - Assert redis_status="degraded"

4. **test_redis_check_no_client_enabled**: Test no client when enabled
   - Mock cache.redis_client to None
   - Mock cache.config.redis.enabled to True
   - Assert redis_status="degraded"

5. **test_redis_check_disabled**: Test Redis disabled
   - Mock cache.redis_client to None
   - Mock cache.config.redis.enabled to False
   - Assert redis_status="unknown"

Add test class `TestVectorHealth` with the following tests:

1. **test_vector_check_operational**: Test LanceDB connected
   - Mock LanceDBHandler.test_connection to return {"connected": True}
   - Assert vector_status="operational"

2. **test_vector_check_degraded**: Test LanceDB not connected
   - Mock LanceDBHandler.test_connection to return {"connected": False}
   - Assert vector_status="degraded"

3. **test_vector_check_exception**: Test LanceDB exception
   - Mock LanceDBHandler to raise Exception
   - Assert vector_status="degraded"

4. **test_vector_check_not_available**: Test LanceDB import failed
   - Set HAS_LANCEDB=False in test module
   - Assert vector_status="maintenance"

Reference: `backend/api/admin/system_health_routes.py` lines 52-87 for Redis and vector health logic.
  </action>
  <verify>
    Test classes TestRedisHealth and TestVectorHealth exist with all tests
  </verify>
  <done>
    All Redis and vector store health tests cover all code paths
  </done>
</task>

<task type="auto">
  <name>Task 5: Implement public health endpoint tests (liveness, readiness, metrics)</name>
  <files>backend/tests/api/test_admin_system_health_routes.py</files>
  <action>
Add test class `TestPublicHealthLiveness` with the following tests:

1. **test_liveness_returns_200**: Test liveness probe returns 200
   - GET /health/live with public client
   - Assert 200 status

2. **test_liveness_returns_alive_status**: Test liveness status
   - GET /health/live
   - Assert response["status"] == "alive"

3. **test_liveness_includes_timestamp**: Test timestamp field
   - GET /health/live
   - Assert "timestamp" in response

Add test class `TestPublicHealthReadiness` with the following tests:

1. **test_readiness_200_when_healthy**: Test readiness when all healthy
   - Mock _check_database to return healthy=True
   - Mock _check_disk_space to return healthy=True
   - GET /health/ready
   - Assert 200 status

2. **test_readiness_503_when_db_unhealthy**: Test readiness when DB down
   - Mock _check_database to return healthy=False
   - GET /health/ready
   - Assert 503 status

3. **test_readiness_503_when_disk_unhealthy**: Test readiness when disk low
   - Mock _check_disk_space to return healthy=False
   - GET /health/ready
   - Assert 503 status

4. **test_readiness_includes_checks**: Test checks field
   - GET /health/ready (when healthy)
   - Assert "checks" in response with database and disk

5. **test_readiness_no_auth_required**: Test readiness works without auth
   - GET /health/ready without headers
   - Assert 200 or 503 (not 401)

Add test class `TestPublicHealthMetrics` with the following tests:

1. **test_metrics_returns_prometheus_format**: Test metrics endpoint
   - GET /health/metrics
   - Assert content-type includes "text/plain"

2. **test_metrics_no_auth_required**: Test metrics works without auth
   - GET /health/metrics without headers
   - Assert 200 status

3. **test_metrics_generates_latest**: Test generate_latest called
   - Mock prometheus_client.generate_latest
   - GET /health/metrics
   - Verify mock called

Reference: `backend/api/health_routes.py` lines 38-82 (liveness), 84-185 (readiness), 409-447 (metrics) and existing `backend/tests/api/test_health_routes.py` for patterns.
  </action>
  <verify>
    Test classes TestPublicHealthLiveness, TestPublicHealthReadiness, TestPublicHealthMetrics exist with all tests
  </verify>
  <done>
    All public health tests pass
  </done>
</task>

<task type="auto">
  <name>Task 6: Implement disk space and database connectivity tests</name>
  <files>backend/tests/api/test_admin_system_health_routes.py</files>
  <action>
Add test class `TestDiskSpaceHealth` with the following tests:

1. **test_disk_space_adequate**: Test adequate disk space
   - Mock psutil.disk_usage to return >1GB free
   - Mock _check_disk_space
   - Assert healthy=True, free_gb >= 1.0

2. **test_disk_space_low**: Test low disk space
   - Mock psutil.disk_usage to return <1GB free
   - Mock _check_disk_space
   - Assert healthy=False, message includes "Low disk space"

3. **test_disk_space_exception**: Test disk check exception
   - Mock psutil.disk_usage to raise Exception
   - Mock _check_disk_space
   - Assert healthy=False, message includes "Disk check error"

Add test class `TestDatabaseConnectivity` with the following tests:

1. **test_db_connectivity_healthy**: Test database connectivity healthy
   - Mock db.execute(text("SELECT 1")) to succeed
   - GET /health/db
   - Assert 200 status, database["connected"]=True

2. **test_db_connectivity_unhealthy**: Test database connectivity unhealthy
   - Mock db.execute to raise Exception
   - GET /health/db
   - Assert 503 status, database["connected"]=False

3. **test_db_connectivity_includes_pool_status**: Test connection pool status
   - Mock successful query
   - GET /health/db
   - Assert response includes pool_status fields

4. **test_db_connectivity_slow_query_warning**: Test slow query warning
   - Mock db.execute with query_time >100ms
   - GET /health/db
   - Assert response includes warning field

Reference: `backend/api/health_routes.py` lines 374-406 (disk space) and 304-371 (database connectivity).
  </action>
  <verify>
    Test classes TestDiskSpaceHealth and TestDatabaseConnectivity exist with all tests
  </verify>
  <done>
    All disk space and database connectivity tests pass
  </done>
</task>

<task type="auto">
  <name>Task 7: Run tests and verify coverage</name>
  <files>backend/tests/api/test_admin_system_health_routes.py</files>
  <action>
Execute the following commands:

1. Run the new test file:
   ```bash
   cd /Users/rushiparikh/projects/atom/backend
   pytest tests/api/test_admin_system_health_routes.py -v
   ```

2. Run with coverage on both health route files:
   ```bash
   pytest tests/api/test_admin_system_health_routes.py --cov=api/admin/system_health_routes --cov=api/health_routes --cov-report=term-missing --cov-report=json
   ```

3. Verify coverage is 75%+:
   ```bash
   # Check coverage.json for both route files percent_covered
   ```

4. If coverage < 75%, identify missing lines and add tests:
   - Common gaps: exception handlers, edge cases in service state combinations
   - Add tests for uncovered conditional branches

5. Run existing health tests to ensure compatibility:
   ```bash
   pytest tests/api/test_health_routes.py -v
   ```

6. Specifically verify LanceDB import path is covered:
   - Mock HAS_LANCEDB flag for coverage
   - Test both import success and failure paths

Reference: Phase 177-01-SUMMARY.md for coverage measurement approach and Phase 178-RESEARCH.md lines 523-526 for LanceDB import handling.
  </action>
  <verify>
    Coverage report shows 75%+ for both api/admin/system_health_routes.py and api/health_routes.py
  </verify>
  <done>
    Both health route files achieve 75%+ line coverage, all tests pass
  </done>
</task>

</tasks>

<verification>
Overall phase verification:

1. Test file exists at backend/tests/api/test_admin_system_health_routes.py
2. All test fixtures follow per-file FastAPI app pattern with two routers
3. Admin system health endpoint tested:
   - All services operational
   - Individual degraded states (DB, Redis, vector)
   - Status determination logic
   - Authentication requirement
4. Public health endpoints tested:
   - Liveness probe (200, status, timestamp)
   - Readiness probe (healthy, DB unhealthy, disk unhealthy, checks field)
   - Metrics endpoint (Prometheus format, no auth)
5. Coverage measured via pytest --cov shows 75%+ for both files
6. All tests pass when run individually and with existing test suite
</verification>

<success_criteria>
1. api/admin/system_health_routes.py achieves 75%+ line coverage
2. api/health_routes.py achieves 75%+ line coverage (extending existing coverage)
3. Test file has 500-700 lines with 30+ tests
4. All health check combinations tested (service states, auth, edge cases)
5. External services properly mocked (Redis, LanceDB, psutil)
</success_criteria>

<output>
After completion, create `.planning/phases/178-api-routes-coverage-admin-system/178-03-SUMMARY.md` with:
- Coverage achieved for both files (line count and percentage)
- Number of tests written
- Any challenges encountered and solutions
- Missing coverage areas (if any) for future improvement
</output>
