---
phase: 62-test-coverage-80pct
plan: 05
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - core/episode_segmentation_service.py
  - core/lancedb_handler.py
  - tests/unit/test_episode_segmentation.py
  - tests/unit/test_lancedb_handler.py
autonomous: true

must_haves:
  truths:
    - "Episode segmentation has comprehensive unit test coverage (>80%)"
    - "LanceDB handler has comprehensive unit test coverage (>80%)"
    - "Episode creation and retrieval tested"
    - "Vector storage and semantic search tested"
  artifacts:
    - path: "tests/unit/test_episode_segmentation.py"
      provides: "Comprehensive episode segmentation tests"
      min_lines: 700
    - path: "tests/unit/test_lancedb_handler.py"
      provides: "Comprehensive LanceDB handler tests"
      min_lines: 650
  key_links:
    - from: "tests/unit/test_episode_segmentation.py"
      to: "core/episode_segmentation_service.py"
      via: "Unit test coverage"
      pattern: "def test_.*episode"
    - from: "tests/unit/test_lancedb_handler.py"
      to: "core/lancedb_handler.py"
      via: "Unit test coverage"
      pattern: "def test_.*lancedb"
---

<objective>
Add comprehensive test coverage for episode_segmentation_service.py (580 lines, 8.3% coverage → 80%+) and lancedb_handler.py (619 lines, 16.2% coverage → 80%+).

Purpose: Episodic memory is the foundation of agent learning. Untested segmentation could corrupt memory, and untested LanceDB operations could cause data loss or incorrect retrieval.

Output: 700+ lines for episode segmentation tests, 650+ lines for LanceDB tests, achieving 80%+ coverage for both files
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/docs/COVERAGE_ANALYSIS.md
@core/episode_segmentation_service.py
@core/lancedb_handler.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Analyze episodic memory components and coverage gaps</name>
  <files>core/episode_segmentation_service.py, core/lancedb_handler.py</files>
  <action>
Analyze episodic memory files to identify untested code:

1. Read episode_segmentation_service.py and identify:
   - Segmentation triggers (time gaps, topic changes, task completion)
   - Episode creation methods
   - Segment lifecycle management
   - Metadata extraction
   - Canvas and feedback integration
   - Retrieval modes (temporal, semantic, sequential, contextual)

2. Read lancedb_handler.py and identify:
   - Connection and initialization
   - Vector table creation
   - Insert operations
   - Semantic search queries
   - Deletion and cleanup
   - Batch operations

3. Create test matrix for both files:
   - Method name | Current coverage | Test priority | Dependencies
   - Focus on methods with <50% coverage

4. Identify test scenarios needed:
   - Episode creation from agent execution
   - Time-based segmentation (gaps > threshold)
   - Topic-based segmentation (embedding distance)
   - Task completion segmentation
   - LanceDB vector storage and retrieval
   - Semantic search accuracy
   - Hybrid PostgreSQL + LanceDB queries
   - Episode lifecycle (decay, consolidation, archival)
  </action>
  <verify>grep -c "def test_.*episode\|def test_.*lancedb" tests/unit/test_episode_segmentation.py tests/unit/test_lancedb_handler.py 2>/dev/null || echo "0"</verify>
  <done>Test matrix created, 50-60 test scenarios identified</done>
</task>

<task type="auto">
  <name>Write comprehensive unit tests for episode segmentation</name>
  <files>tests/unit/test_episode_segmentation.py</files>
  <action>
Create tests/unit/test_episode_segmentation.py with:

**Test Classes:**
1. TestEpisodeSegmentation (8-10 tests)
   - Create episode from agent execution
   - Create episode with canvas context
   - Create episode with feedback context
   - Time-based segmentation (gap > threshold)
   - Topic-based segmentation (embedding drift)
   - Task completion segmentation
   - Episode metadata extraction
   - Episode with empty content

2. TestEpisodeRetrieval (8-10 tests)
   - Temporal retrieval (time range)
   - Semantic retrieval (vector similarity)
   - Sequential retrieval (full episode)
   - Contextual retrieval (hybrid)
   - Retrieval with canvas type filter
   - Retrieval with feedback weighting
   - Retrieval limit and offset
   - Empty result handling

3. TestEpisodeLifecycle (6-8 tests)
   - Episode decay (old episodes)
   - Episode consolidation (merge similar)
   - Episode archival (PostgreSQL → LanceDB)
   - Episode deletion
   - Episode access logging
   - Lifecycle transition triggers

4. TestCanvasIntegration (5-6 tests)
   - Episode with chart canvas
   - Episode with form canvas
   - Episode with sheet canvas
   - Canvas action filtering
   - Canvas context enrichment

5. TestFeedbackIntegration (5-6 tests)
   - Episode with positive feedback
   - Episode with negative feedback
   - Feedback score aggregation
   - Feedback-weighted retrieval
   - Feedback context enrichment

**Target:** 700+ lines, 32-40 tests, 80%+ coverage
  </action>
  <verify>wc -l tests/unit/test_episode_segmentation.py && pytest tests/unit/test_episode_segmentation.py -v --tb=short | tail -20</verify>
  <done>700+ lines, 32-40 tests, 80%+ coverage achieved</done>
</task>

<task type="auto">
  <name>Write comprehensive unit tests for LanceDB handler</name>
  <files>tests/unit/test_lancedb_handler.py</files>
  <action>
Create tests/unit/test_lancedb_handler.py with:

**Test Classes:**
1. TestLanceDBInitialization (4-5 tests)
   - Initialize LanceDB connection
   - Create vector table
   - Initialize with existing table
   - Invalid URI handling
   - Table schema validation

2. TestVectorOperations (8-10 tests)
   - Insert single vector
   - Insert batch vectors
   - Vector with metadata
   - Upsert operation
   - Delete by ID
   - Delete batch
   - Delete by filter
   - Vector update

3. TestSemanticSearch (8-10 tests)
   - Basic vector search
   - Search with limit
   - Search with filter
   - Search with distance threshold
   - Empty search results
   - Search performance (large dataset)
   - Search with pre-filter
   - Hybrid search (vector + metadata)

4. TestTableManagement (5-6 tests)
   - List all tables
   - Get table schema
   - Drop table
   - Table size check
   - Table compaction
   - Backup and restore

5. TestErrorHandling (5-6 tests)
   - Invalid vector dimension
   - Connection failure
   - Insert failure handling
   - Search failure handling
   - Concurrent access handling
   - Graceful degradation

**Target:** 650+ lines, 30-35 tests, 80%+ coverage
  </action>
  <verify>wc -l tests/unit/test_lancedb_handler.py && pytest tests/unit/test_lancedb_handler.py -v --tb=short | tail -20</verify>
  <done>650+ lines, 30-35 tests, 80%+ coverage achieved</done>
</task>

<task type="auto">
  <name>Run tests and verify coverage target</name>
  <files>tests/unit/test_episode_segmentation.py, tests/unit/test_lancedb_handler.py</files>
  <action>
Verify tests pass and coverage target met:

1. Run pytest with coverage:
   ```bash
   pytest tests/unit/test_episode_segmentation.py tests/unit/test_lancedb_handler.py \
          --cov=core/episode_segmentation_service --cov=core/lancedb_handler \
          --cov-report=term-missing --cov-report=json -v
   ```

2. Verify:
   - All tests pass (0 failures)
   - Coverage for episode_segmentation_service.py ≥80%
   - Coverage for lancedb_handler.py ≥80%
   - No untested critical paths

3. If coverage <80%, add tests for missing lines
   - Use coverage report to identify uncovered lines
   - Add tests for uncovered branches
   - Re-run until target met
  </action>
  <verify>grep -E "episode_segmentation_service|lancedb_handler" coverage.json | jq -c '.files[].coverage'</verify>
  <done>All tests pass, 80%+ coverage for both files</done>
</task>

</tasks>

<verification>
After completion:
1. Verify test files exist with 700+ and 650+ lines respectively
2. Run pytest and confirm all tests pass
3. Check coverage report shows 80%+ for both files
4. Verify no critical memory operations untested
</verification>

<success_criteria>
- [ ] tests/unit/test_episode_segmentation.py created (700+ lines)
- [ ] tests/unit/test_lancedb_handler.py created (650+ lines)
- [ ] 62-75 tests covering all episodic memory functionality
- [ ] All tests passing (0 failures)
- [ ] Coverage ≥80% for both files
- [ ] Test execution time <45 seconds total
</success_criteria>

<output>
After completion, create `.planning/phases/62-test-coverage-80pct/62-05-SUMMARY.md`
</output>
