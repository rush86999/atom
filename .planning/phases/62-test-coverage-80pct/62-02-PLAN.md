---
phase: 62-test-coverage-80pct
plan: 02
type: execute
wave: 1
depends_on: [62-01]
files_modified:
  - core/workflow_engine.py
  - tests/unit/test_workflow_engine.py
autonomous: true

must_haves:
  truths:
    - "Workflow engine has comprehensive unit test coverage (>80%)"
    - "All workflow execution paths tested (start, stop, pause, resume, error handling)"
    - "DAG validation tested (cycle detection, topological sort)"
    - "Test coverage for workflow state machine transitions"
  artifacts:
    - path: "tests/unit/test_workflow_engine.py"
      provides: "Comprehensive workflow engine tests"
      min_lines: 800
  key_links:
    - from: "tests/unit/test_workflow_engine.py"
      to: "core/workflow_engine.py"
      via: "Unit test coverage"
      pattern: "def test_.*workflow"
---

<objective>
Add comprehensive test coverage for workflow_engine.py (1,163 lines, 4.8% coverage → 80%+).

Purpose: Workflow engine is the highest priority file (Impact Score: 1,107). It executes all agent workflows and untested code could cause production failures.

Output: 800+ lines of unit tests achieving 80%+ coverage for workflow_engine.py
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiphanh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/docs/COVERAGE_ANALYSIS.md
@core/workflow_engine.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Analyze workflow_engine.py structure and coverage gaps</name>
  <files>core/workflow_engine.py</files>
  <action>
Analyze workflow_engine.py to identify untested code:

1. Read the file and identify:
   - All public methods and classes
   - Current test coverage (using coverage report)
   - Critical execution paths (workflow start, stop, pause, resume)
   - Error handling paths
   - State machine logic
   - DAG validation logic

2. Create test matrix:
   - Method name | Current coverage | Test priority | Dependencies
   - Focus on methods with <50% coverage

3. Identify test scenarios needed:
   - Normal workflow execution (happy path)
   - Error conditions (invalid DAG, circular dependencies)
   - State transitions (paused → running, running → stopped)
   - Edge cases (empty DAG, single node, large DAGs)
  </action>
  <verify>grep -c "def test_.*workflow" tests/unit/test_workflow_engine.py 2>/dev/null || echo "0"</verify>
  <done>Test matrix created, 40-50 test scenarios identified</done>
</task>

<task type="auto">
  <name>Write comprehensive unit tests for workflow engine</name>
  <files>tests/unit/test_workflow_engine.py</files>
  <action>
Create tests/unit/test_workflow_engine.py with:

**Test Classes:**
1. TestWorkflowEngineInitialization (3-5 tests)
   - Engine initialization with valid config
   - Engine initialization with invalid config
   - Engine initialization with existing workflows

2. TestWorkflowExecution (8-10 tests)
   - Start workflow successfully
   - Start workflow with invalid DAG (circular dependency)
   - Start workflow with missing dependencies
   - Pause running workflow
   - Resume paused workflow
   - Stop workflow
   - Stop already stopped workflow
   - Complete workflow execution

3. TestDAGValidation (6-8 tests)
   - Valid DAG with topological sort
   - DAG with circular dependency (detect cycle)
   - DAG with self-loop
   - DAG with missing node reference
   - Empty DAG
   - Single node DAG
   - Large DAG (100+ nodes)

4. TestWorkflowStateTransitions (6-8 tests)
   - Running → Paused transition
   - Paused → Running transition
   - Running → Stopped transition
   - Paused → Stopped transition
   - Stopped → Running transition (if supported)
   - Invalid state transitions

5. TestErrorHandling (6-8 tests)
   - Workflow execution with node failure
   - Workflow execution with timeout
   - Workflow execution with dependency failure
   - Retry logic for failed nodes
   - Error propagation to workflow status
   - Graceful degradation

6. TestWorkflowPersistence (4-6 tests)
   - Save workflow state to database
   - Load workflow state from database
   - Update workflow state during execution
   - Delete workflow state

**Target:** 800+ lines, 40-50 tests, 80%+ coverage
  </action>
  <verify>wc -l tests/unit/test_workflow_engine.py && pytest tests/unit/test_workflow_engine.py -v --tb=short | tail -20</verify>
  <done>800+ lines, 40-50 tests, 80%+ coverage achieved</done>
</task>

<task type="auto">
  <name>Run tests and verify coverage target</name>
  <files>tests/unit/test_workflow_engine.py</files>
  <action>
Verify tests pass and coverage target met:

1. Run pytest with coverage:
   ```bash
   pytest tests/unit/test_workflow_engine.py --cov=core/workflow_engine \
          --cov-report=term-missing --cov-report=json -v
   ```

2. Verify:
   - All tests pass (0 failures)
   - Coverage for workflow_engine.py ≥80%
   - No untested critical paths

3. If coverage <80%, add tests for missing lines
   - Use coverage report to identify uncovered lines
   - Add tests for uncovered branches
   - Re-run until target met
  </action>
  <verify>grep "workflow_engine.py" coverage.json | jq -c '.files[].coverage' | head -1</verify>
  <done>All tests pass, 80%+ coverage achieved</done>
</task>

</tasks>

<verification>
After completion:
1. Verify test file exists with 800+ lines
2. Run pytest and confirm all tests pass
3. Check coverage report shows 80%+ for workflow_engine.py
4. Verify no critical execution paths untested
</verification>

<success_criteria>
- [ ] tests/unit/test_workflow_engine.py created (800+ lines)
- [ ] 40-50 tests covering all workflow engine functionality
- [ ] All tests passing (0 failures)
- [ ] Coverage ≥80% for workflow_engine.py
- [ ] Test execution time <30 seconds total
</success_criteria>

<output>
After completion, create `.planning/phases/62-test-coverage-80pct/62-02-SUMMARY.md`
</output>
