---
phase: 62-test-coverage-80pct
plan: 03
type: execute
wave: 1
depends_on: [62-02]
files_modified:
  - core/atom_agent_endpoints.py
  - tests/api/test_atom_agent_endpoints.py
autonomous: true

must_haves:
  truths:
    - "Agent endpoints have comprehensive test coverage (>80%)"
    - "All agent execution API paths tested"
    - "LLM streaming integration tested"
    - "Error handling and validation tested"
  artifacts:
    - path: "tests/api/test_atom_agent_endpoints.py"
      provides: "Comprehensive agent endpoint tests"
      min_lines: 700
  key_links:
    - from: "tests/api/test_atom_agent_endpoints.py"
      to: "core/atom_agent_endpoints.py"
      via: "API test coverage"
      pattern: "def test_.*endpoint"
---

<objective>
Add comprehensive test coverage for atom_agent_endpoints.py (774 lines, 9.1% coverage → 80%+).

Purpose: Agent endpoints are the main API for agent execution. Untested endpoints could cause API failures in high-traffic scenarios.

Output: 700+ lines of API tests achieving 80%+ coverage for atom_agent_endpoints.py
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/docs/COVERAGE_ANALYSIS.md
@core/atom_agent_endpoints.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Analyze agent endpoints structure and coverage gaps</name>
  <files>core/atom_agent_endpoints.py</files>
  <action>
Analyze atom_agent_endpoints.py to identify untested code:

1. Read the file and identify:
   - All API endpoint routes (POST, GET, PUT, DELETE)
   - Current test coverage (using coverage report)
   - Critical paths (agent execution, streaming, error handling)
   - Middleware and dependencies (governance, LLM, persistence)

2. Create test matrix:
   - Endpoint | Method | Current coverage | Test priority | Auth required
   - Focus on endpoints with <50% coverage

3. Identify test scenarios needed:
   - Agent execution (happy path with streaming)
   - Agent execution with various maturity levels
   - Governance enforcement (blocking STUDENT agents)
   - Error conditions (invalid input, agent not found, LLM failures)
   - Stream interruption handling
   - Rate limiting
   - Request validation
  </action>
  <verify>grep -c "def test_.*endpoint" tests/api/test_atom_agent_endpoints.py 2>/dev/null || echo "0"</verify>
  <done>Test matrix created, 35-40 test scenarios identified</done>
</task>

<task type="auto">
  <name>Write comprehensive API tests for agent endpoints</name>
  <files>tests/api/test_atom_agent_endpoints.py</files>
  <action>
Create tests/api/test_atom_agent_endpoints.py with:

**Test Classes:**
1. TestAgentExecutionEndpoint (8-10 tests)
   - Execute agent successfully (200, streaming response)
   - Execute agent with invalid payload (422)
   - Execute agent that doesn't exist (404)
   - Execute agent with STUDENT maturity blocked (403)
   - Execute agent with INTERN maturity requires approval
   - Execute AUTONOMOUS agent with full permissions
   - Stream interruption handling
   - Agent execution timeout

2. TestAgentStatusEndpoint (5-7 tests)
   - Get agent status (200, running)
   - Get status for non-existent agent (404)
   - Get status for completed agent (200, stopped)
   - Status includes execution details and results

3. TestAgentListEndpoint (4-5 tests)
   - List all agents (200)
   - List agents filtered by status
   - List agents filtered by maturity
   - Pagination support

4. TestAgentStopEndpoint (5-6 tests)
   - Stop running agent (200)
   - Stop already stopped agent (400)
   - Stop non-existent agent (404)
   - Force stop option

5. TestAgentPauseResumeEndpoint (4-5 tests)
   - Pause running agent (200)
   - Resume paused agent (200)
   - Pause already paused agent (400)
   - Resume running agent (400)

6. TestStreamValidation (5-6 tests)
   - Streaming response format validation
   - Chunk delimiter correctness
   - Stream completion signal
   - Truncated stream handling

7. TestGovernanceIntegration (6-8 tests)
   - STUDENT agent blocked from complex actions
   - INTERN agent requires proposal for actions
   - SUPERVISED agent allows execution with monitoring
   - AUTONOMOUS agent has full access
   - Governance cache integration

**Target:** 700+ lines, 35-40 tests, 80%+ coverage
  </action>
  <verify>wc -l tests/api/test_atom_agent_endpoints.py && pytest tests/api/test_atom_agent_endpoints.py -v --tb=short | tail -20</verify>
  <done>700+ lines, 35-40 tests, 80%+ coverage achieved</done>
</task>

<task type="auto">
  <name>Run tests and verify coverage target</name>
  <files>tests/api/test_atom_agent_endpoints.py</files>
  <action>
Verify tests pass and coverage target met:

1. Run pytest with coverage:
   ```bash
   pytest tests/api/test_atom_agent_endpoints.py --cov=core/atom_agent_endpoints \
          --cov-report=term-missing --cov-report=json -v
   ```

2. Verify:
   - All tests pass (0 failures)
   - Coverage for atom_agent_endpoints.py ≥80%
   - No untested critical API paths

3. If coverage <80%, add tests for missing lines
   - Use coverage report to identify uncovered lines
   - Add tests for uncovered branches
   - Re-run until target met
  </action>
  <verify>grep "atom_agent_endpoints.py" coverage.json | jq -c '.files[].coverage' | head -1</verify>
  <done>All tests pass, 80%+ coverage achieved</done>
</task>

</tasks>

<verification>
After completion:
1. Verify test file exists with 700+ lines
2. Run pytest and confirm all tests pass
3. Check coverage report shows 80%+ for atom_agent_endpoints.py
4. Verify no critical API paths untested
</verification>

<success_criteria>
- [ ] tests/api/test_atom_agent_endpoints.py created (700+ lines)
- [ ] 35-40 tests covering all agent endpoint functionality
- [ ] All tests passing (0 failures)
- [ ] Coverage ≥80% for atom_agent_endpoints.py
- [ ] Test execution time <30 seconds total
</success_criteria>

<output>
After completion, create `.planning/phases/62-test-coverage-80pct/62-03-SUMMARY.md`
</output>
