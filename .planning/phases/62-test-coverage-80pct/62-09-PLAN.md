---
phase: 62-test-coverage-80pct
plan: 09
type: execute
wave: 3
depends_on: [62-01]
files_modified:
  - api/workspace_routes.py
  - api/auth_routes.py
  - api/token_routes.py
  - api/marketing_routes.py
  - api/operational_routes.py
  - api/user_activity_routes.py
  - tests/api/test_routes_batch.py
autonomous: true

must_haves:
  truths:
    - "API routes have comprehensive test coverage (>75%)"
    - "Workspace routes tested"
    - "Authentication routes tested"
    - "Token management routes tested"
    - "Additional API routes tested (marketing, operations, user activity)"
  artifacts:
    - path: "tests/api/test_routes_batch.py"
      provides: "Comprehensive API routes tests (batch)"
      min_lines: 1000
  key_links:
    - from: "tests/api/test_routes_batch.py"
      to: "api/"
      via: "API test coverage"
      pattern: "def test_.*route"
---

<objective>
Add comprehensive test coverage for API routes in Wave 3B.

This batch plan covers 6 API route files with varying coverage levels. API module already has 38.2% coverage (best of all modules), so these tests will build on existing foundation.

Files covered:
- workspace_routes.py (workspace management)
- auth_routes.py (authentication and authorization)
- token_routes.py (token management and refresh)
- marketing_routes.py (marketing and analytics)
- operational_routes.py (operational endpoints)
- user_activity_routes.py (user activity tracking)

Purpose: API routes are the public interface of Atom. Comprehensive testing ensures reliability and prevents regressions in production API endpoints.

Output: 1,000+ lines of API tests achieving 75%+ coverage across all 6 files
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/docs/COVERAGE_ANALYSIS.md
@api/workspace_routes.py
@api/auth_routes.py
@api/token_routes.py
@api/marketing_routes.py
@api/operational_routes.py
@api/user_activity_routes.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Analyze API routes structure and coverage gaps</name>
  <files>api/workspace_routes.py, api/auth_routes.py, api/token_routes.py, api/marketing_routes.py, api/operational_routes.py, api/user_activity_routes.py</files>
  <action>
Analyze all 6 API route files to identify untested code:

1. Read each file and identify:
   - workspace_routes.py: CRUD operations, member management, settings
   - auth_routes.py: Login, logout, registration, password reset
   - token_routes.py: Token generation, refresh, revocation
   - marketing_routes.py: Analytics, campaigns, referrals
   - operational_routes.py: Health checks, metrics, status
   - user_activity_routes.py: Activity logging, tracking, analytics

2. Create combined test matrix:
   - Route File | Endpoint | Current coverage | Test priority
   - Focus on endpoints with <50% coverage

3. Identify test scenarios needed (total 45-55 tests):
   - Workspace Routes: Create workspace, update workspace, delete workspace, add/remove members, workspace settings
   - Auth Routes: Login success/failure, logout, registration, password reset, token refresh
   - Token Routes: Generate token, refresh token, revoke token, token validation, list tokens
   - Marketing Routes: Analytics queries, campaign management, referral tracking
   - Operational Routes: Health checks, metrics endpoints, status reports
   - User Activity: Log activity, retrieve history, activity analytics
  </action>
  <verify>grep -c "def test_.*route" tests/api/test_routes_batch.py 2>/dev/null || echo "0"</verify>
  <done>Test matrix created, 45-55 test scenarios identified</done>
</task>

<task type="auto">
  <name>Write comprehensive API tests for routes batch</name>
  <files>tests/api/test_routes_batch.py</files>
  <action>
Create tests/api/test_routes_batch.py with:

**Test Classes:**
1. TestWorkspaceRoutes (8-10 tests)
   - Create workspace (201)
   - Get workspace by ID (200)
   - Update workspace (200)
   - Delete workspace (200)
   - List all workspaces (200)
   - Add member to workspace (200)
   - Remove member from workspace (200)
   - Update workspace settings (200)
   - Invalid workspace ID (404)
   - Unauthorized access (401)

2. TestAuthRoutes (8-10 tests)
   - User login success (200)
   - User login with invalid credentials (401)
   - User logout (200)
   - User registration (201)
   - Registration with duplicate email (400)
   - Password reset request (200)
   - Password reset confirmation (200)
   - Token refresh (200)
   - Refresh with expired token (401)
   - Refresh with invalid token (401)

3. TestTokenRoutes (7-8 tests)
   - Generate access token (200)
   - Generate token with scopes (200)
   - Refresh access token (200)
   - Revoke token (200)
   - List active tokens (200)
   - Validate token (200)
   - Validate expired token (401)
   - Validate revoked token (401)

4. TestMarketingRoutes (6-7 tests)
   - Get marketing analytics (200)
   - Create campaign (201)
   - Update campaign (200)
   - List campaigns (200)
   - Track referral (200)
   - Get referral stats (200)
   - Invalid campaign ID (404)

5. TestOperationalRoutes (5-6 tests)
   - Health check (200)
   - Readiness check (200)
   - Metrics endpoint (200)
   - Status report (200)
   - System information (200)
   - Maintenance mode toggle (200)

6. TestUserActivityRoutes (7-8 tests)
   - Log user activity (200)
   - Get activity history (200)
   - Get activity by type (200)
   - Get activity analytics (200)
   - Export activity data (200)
   - Filter by date range (200)
   - Filter by user (200)
   - Invalid user ID (404)

**Target:** 1,000+ lines, 45-55 tests, 75%+ coverage across all 6 files

**Note:** Use TestClient from FastAPI for endpoint testing
  </action>
  <verify>wc -l tests/api/test_routes_batch.py && pytest tests/api/test_routes_batch.py -v --tb=short | tail -20</verify>
  <done>1,000+ lines, 45-55 tests, 75%+ coverage achieved</done>
</task>

<task type="auto">
  <name>Run tests and verify coverage target</name>
  <files>tests/api/test_routes_batch.py</files>
  <action>
Verify tests pass and coverage target met:

1. Run pytest with coverage:
   ```bash
   pytest tests/api/test_routes_batch.py \
          --cov=api/workspace_routes \
          --cov=api/auth_routes \
          --cov=api/token_routes \
          --cov=api/marketing_routes \
          --cov=api/operational_routes \
          --cov=api/user_activity_routes \
          --cov-report=term-missing --cov-report=json -v
   ```

2. Verify:
   - All tests pass (0 failures)
   - Average coverage ≥75% across all 6 files
   - No untested critical endpoints

3. If coverage <75%, add tests for missing lines
   - Use coverage report to identify uncovered lines
   - Add tests for uncovered branches
   - Re-run until target met
  </action>
  <verify>grep -E "workspace_routes|auth_routes|token_routes|marketing_routes|operational_routes|user_activity_routes" coverage.json | jq -c '.files[].coverage'</verify>
  <done>All tests pass, 75%+ average coverage achieved</done>
</task>

</tasks>

<verification>
After completion:
1. Verify test file exists with 1,000+ lines
2. Run pytest and confirm all tests pass
3. Check coverage report shows 75%+ average across all 6 files
4. Verify no critical API endpoints untested
</verification>

<success_criteria>
- [ ] tests/api/test_routes_batch.py created (1,000+ lines)
- [ ] 45-55 tests covering all 6 API route files
- [ ] All tests passing (0 failures)
- [ ] Average coverage ≥75% across all 6 files
- [ ] Test execution time <45 seconds total
</success_criteria>

<output>
After completion, create `.planning/phases/62-test-coverage-80pct/62-09-SUMMARY.md`
</output>
