---
phase: 62-test-coverage-80pct
plan: 62.1
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_atom_governance.py
  - backend/tests/test_atom_react.py
  - backend/tests/unit/test_atom_meta_agent.py
  - backend/tests/unit/test_core_services_batch.py
  - backend/tests/core/test_agent_evolution_loop.py
  - backend/tests/core/test_group_reflection_service.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All 6 governance and core tests collect without import errors"
    - "Tests can execute (pytest discovers them successfully)"
    - "Tests with module import issues resolve cleanly"
  artifacts:
    - path: "backend/tests/test_atom_governance.py"
      provides: "Governance gating tests"
      contains: "from core.atom_meta_agent import AtomMetaAgent"
    - path: "backend/tests/test_atom_react.py"
      provides: "React integration tests"
      min_lines: 1
    - path: "backend/tests/unit/test_atom_meta_agent.py"
      provides: "Meta agent unit tests"
      min_lines: 1
  key_links:
    - from: "backend/tests/test_atom_governance.py"
      to: "core.atom_meta_agent"
      via: "from core.atom_meta_agent import AtomMetaAgent"
      pattern: "from core.atom_meta_agent"
---

<objective>
Fix import and module errors in 6 test files to enable pytest collection.

**Purpose:** The 24 collection errors block test execution. This plan fixes the first 6 errors related to governance and core services.

**Missing from verification gap:**
- Import errors: `from core.atom_meta_agent import AtomMetaAgent` fails (module may not exist or path differs)
- Missing test implementations or stubs causing collection failures
- Module path mismatches between test imports and actual code structure

**Output:** 6 test files collectable by pytest without errors.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-test-coverage-80pct/62-VERIFICATION.md
@.planning/phases/62-test-coverage-80pct/62-RESEARCH.md

# Collection Error Details (from pytest --collect-only):
ERROR tests/test_atom_governance.py
ERROR tests/test_atom_react.py
ERROR tests/unit/test_atom_meta_agent.py
ERROR tests/unit/test_core_services_batch.py
ERROR tests/core/test_agent_evolution_loop.py
ERROR tests/core/test_group_reflection_service.py

Common errors:
- Missing module imports (AtomMetaAgent, atom_enriched_service)
- Wrong fixture names ('benchmark' not found, 'last' not found, 'e2e' mark not found)
</context>

<tasks>

<task type="auto">
  <name>Fix AtomMetaAgent import errors</name>
  <files>backend/tests/test_atom_governance.py, backend/tests/unit/test_atom_meta_agent.py</files>
  <action>
    For each test file importing AtomMetaAgent:
    1. Check if core/atom_meta_agent.py exists
    2. If exists: Fix import path (may need core.services.atom_meta_agent or similar)
    3. If doesn't exist: Create stub or mock for testing purposes
    4. Verify import statement matches actual module structure

    Pattern to find/fix:
    - FROM: from core.atom_meta_agent import AtomMetaAgent
    - TO: Correct path based on actual codebase structure

    Run after fix: pytest tests/test_atom_governance.py --collect-only
  </action>
  <verify>pytest tests/test_atom_governance.py tests/unit/test_atom_meta_agent.py --collect-only shows no ERROR</verify>
  <done>Both test files collect without import errors</done>
</task>

<task type="auto">
  <name>Fix atom_enriched_service import in core_services_batch tests</name>
  <files>backend/tests/unit/test_core_services_batch.py</files>
  <action>
    1. Find the correct module path for atom_enriched_service
    2. Update import statement to match actual codebase
    3. If service doesn't exist: Create minimal stub or skip tests gracefully

    Likely fixes:
    - Find actual module: grep -r "class.*EnrichedService" core/
    - Update import based on findings
    - OR use pytest.skip for unimplemented features

    Run after fix: pytest tests/unit/test_core_services_batch.py --collect-only
  </action>
  <verify>pytest tests/unit/test_core_services_batch.py --collect-only shows tests collected (may skip, but no ERROR)</verify>
  <done>Core services batch test file collects without errors</done>
</task>

<task type="auto">
  <name>Fix agent evolution and reflection service test imports</name>
  <files>backend/tests/core/test_agent_evolution_loop.py, backend/tests/core/test_group_reflection_service.py, backend/tests/test_atom_react.py</files>
  <action>
    For each file:
    1. test_agent_evolution_loop.py: Check for AgentEvolutionLoop import
    2. test_group_reflection_service.py: Check for GroupReflectionService import
    3. test_atom_react.py: Check for react-related imports

    Fix strategy:
    - If module exists but wrong path: Fix import
    - If module doesn't exist: Add pytest.skip with descriptive message
    - If integration test requiring external service: Mock or skip

    Pattern:
    ```python
    try:
        from core.actual_module import ActualClass
    except ImportError:
        pytest.skip("Module not implemented yet")
    ```

    Run after each fix: pytest [file] --collect-only
  </action>
  <verify>pytest tests/core/test_agent_evolution_loop.py tests/core/test_group_reflection_service.py tests/test_atom_react.py --collect-only shows no ERROR</verify>
  <done>All 3 test files collect without import errors (tests may skip, but collection succeeds)</done>
</task>

</tasks>

<verification>
After completion, run:
```bash
cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_atom_governance.py tests/test_atom_react.py tests/unit/test_atom_meta_agent.py tests/unit/test_core_services_batch.py tests/core/test_agent_evolution_loop.py tests/core/test_group_reflection_service.py --collect-only
```

Expected: 0 ERROR status for these 6 files (tests may be skipped or collected, but no collection errors)
</verification>

<success_criteria>
- pytest --collect-only shows 0 ERROR for the 6 fixed test files
- Import errors resolved (modules found or gracefully skipped)
- Tests can be executed (may fail at runtime, but collection succeeds)
</success_criteria>

<output>
After completion, create `.planning/phases/62-test-coverage-80pct/62.1-SUMMARY.md` documenting:
- Number of collection errors fixed
- Modules that needed import path fixes
- Modules that were stubbed/skipped
- Remaining blockers (if any)
</output>
