---
phase: 62-test-coverage-80pct
plan: 06
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - integrations/slack_enhanced_service.py
  - tests/integration/test_slack_enhanced_service.py
autonomous: true

must_haves:
  truths:
    - "Slack enhanced service has comprehensive integration test coverage (>80%)"
    - "Slack API interactions tested (messages, channels, events)"
    - "Webhook handling tested"
    - "Error handling and retry logic tested"
  artifacts:
    - path: "tests/integration/test_slack_enhanced_service.py"
      provides: "Comprehensive Slack enhanced service tests"
      min_lines: 550
  key_links:
    - from: "tests/integration/test_slack_enhanced_service.py"
      to: "integrations/slack_enhanced_service.py"
      via: "Integration test coverage"
      pattern: "def test_.*slack"
---

<objective>
Add comprehensive test coverage for slack_enhanced_service.py (666 lines, 22.3% coverage → 80%+).

Purpose: Slack integration is high-usage (frequent agent interactions). Untested code could cause message failures, webhook issues, or data loss in Slack workflows.

Output: 550+ lines of integration tests achieving 80%+ coverage for slack_enhanced_service.py
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/docs/COVERAGE_ANALYSIS.md
@integrations/slack_enhanced_service.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Analyze Slack enhanced service structure and coverage gaps</name>
  <files>integrations/slack_enhanced_service.py</files>
  <action>
Analyze slack_enhanced_service.py to identify untested code:

1. Read the file and identify:
   - All Slack API methods (chat, channels, users, files, reactions)
   - Webhook event handlers
   - Real-time message (RTM) handling
   - Slash command handlers
   - Authentication and token management
   - Rate limiting handling
   - Error handling and retry logic
   - Message formatting and parsing

2. Create test matrix:
   - Method name | Current coverage | Test priority | External dependency
   - Focus on methods with <50% coverage

3. Identify test scenarios needed:
   - Send message to channel
   - Send direct message
   - Create and join channels
   - Handle webhook events (message posted, reaction added)
   - Handle slash commands
   - File upload and download
   - User information retrieval
   - Rate limit retry logic
   - Connection failures
   - Invalid token handling
  </action>
  <verify>grep -c "def test_.*slack" tests/integration/test_slack_enhanced_service.py 2>/dev/null || echo "0"</verify>
  <done>Test matrix created, 25-30 test scenarios identified</done>
</task>

<task type="auto">
  <name>Write comprehensive integration tests for Slack service</name>
  <files>tests/integration/test_slack_enhanced_service.py</files>
  <action>
Create tests/integration/test_slack_enhanced_service.py with:

**Test Classes:**
1. TestSlackAuthentication (3-4 tests)
   - Valid token authentication
   - Invalid token handling
   - Token refresh
   - Permission validation

2. TestSlackMessaging (6-8 tests)
   - Send message to channel
   - Send direct message
   - Send message with attachments
   - Send message with blocks
   - Send threaded message
   - Edit message
   - Delete message
   - Message formatting

3. TestSlackChannels (5-6 tests)
   - List channels
   - Create channel
   - Join channel
   - Leave channel
   - Archive channel
   - Channel information

4. TestSlackWebhooks (5-6 tests)
   - Handle message posted event
   - Handle reaction added event
   - Handle app mention event
   - Handle team join event
   - Webhook signature verification
   - Invalid webhook payload

5. TestSlackSlashCommands (4-5 tests)
   - Handle slash command
   - Parse command arguments
   - Command response format
   - Unknown command handling
   - Unauthorized command

6. TestSlackFiles (4-5 tests)
   - Upload file to channel
   - Upload file with snippet
   - Download file
   - List files
   - Delete file

7. TestSlackUsers (3-4 tests)
   - List users
   - Get user info
   - Search users by email
   - User presence

8. TestSlackErrorHandling (5-6 tests)
   - Rate limit error (429) with retry
   - Invalid channel error
   - Network timeout
   - API server error
   - Malformed response
   - Retry backoff logic

**Target:** 550+ lines, 25-30 tests, 80%+ coverage

**Note:** Use mocking for external Slack API calls to avoid dependency on Slack service availability
  </action>
  <verify>wc -l tests/integration/test_slack_enhanced_service.py && pytest tests/integration/test_slack_enhanced_service.py -v --tb=short | tail -20</verify>
  <done>550+ lines, 25-30 tests, 80%+ coverage achieved</done>
</task>

<task type="auto">
  <name>Run tests and verify coverage target</name>
  <files>tests/integration/test_slack_enhanced_service.py</files>
  <action>
Verify tests pass and coverage target met:

1. Run pytest with coverage:
   ```bash
   pytest tests/integration/test_slack_enhanced_service.py \
          --cov=integrations/slack_enhanced_service \
          --cov-report=term-missing --cov-report=json -v
   ```

2. Verify:
   - All tests pass (0 failures)
   - Coverage for slack_enhanced_service.py ≥80%
   - No untested critical API paths

3. If coverage <80%, add tests for missing lines
   - Use coverage report to identify uncovered lines
   - Add tests for uncovered branches
   - Re-run until target met
  </action>
  <verify>grep "slack_enhanced_service.py" coverage.json | jq -c '.files[].coverage' | head -1</verify>
  <done>All tests pass, 80%+ coverage achieved</done>
</task>

</tasks>

<verification>
After completion:
1. Verify test file exists with 550+ lines
2. Run pytest and confirm all tests pass
3. Check coverage report shows 80%+ for slack_enhanced_service.py
4. Verify no critical Slack API paths untested
</verification>

<success_criteria>
- [ ] tests/integration/test_slack_enhanced_service.py created (550+ lines)
- [ ] 25-30 tests covering all Slack service functionality
- [ ] All tests passing (0 failures)
- [ ] Coverage ≥80% for slack_enhanced_service.py
- [ ] Test execution time <30 seconds total
</success_criteria>

<output>
After completion, create `.planning/phases/62-test-coverage-80pct/62-06-SUMMARY.md`
</output>
