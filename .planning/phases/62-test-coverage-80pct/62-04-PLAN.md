---
phase: 62-test-coverage-80pct
plan: 04
type: execute
wave: 1
depends_on: [62-01]
files_modified:
  - core/llm/byok_handler.py
  - tests/unit/test_byok_handler.py
autonomous: true

must_haves:
  truths:
    - "BYOK handler has comprehensive unit test coverage (>80%)"
    - "Multi-provider LLM routing tested (OpenAI, Anthropic, DeepSeek, Gemini)"
    - "Provider fallback and error handling tested"
    - "Token counting and cost calculation tested"
  artifacts:
    - path: "tests/unit/test_byok_handler.py"
      provides: "Comprehensive BYOK handler tests"
      min_lines: 600
  key_links:
    - from: "tests/unit/test_byok_handler.py"
      to: "core/llm/byok_handler.py"
      via: "Unit test coverage"
      pattern: "def test_.*byok.*handler"
---

<objective>
Add comprehensive test coverage for byok_handler.py (549 lines, 8.5% coverage → 80%+).

Purpose: BYOK handler manages multi-provider LLM routing (OpenAI, Anthropic, DeepSeek, Gemini). Untested error handling could result in incorrect AI responses or provider billing issues.

Output: 600+ lines of unit tests achieving 80%+ coverage for byok_handler.py
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/docs/COVERAGE_ANALYSIS.md
@core/llm/byok_handler.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Analyze BYOK handler structure and coverage gaps</name>
  <files>core/llm/byok_handler.py</files>
  <action>
Analyze byok_handler.py to identify untested code:

1. Read the file and identify:
   - All provider handlers (OpenAI, Anthropic, DeepSeek, Gemini)
   - Token counting methods for each provider
   - Provider selection logic
   - Fallback mechanisms
   - Cost calculation methods
   - Error handling paths
   - Streaming response handling

2. Create test matrix:
   - Method name | Provider | Current coverage | Test priority
   - Focus on methods with <50% coverage

3. Identify test scenarios needed:
   - Provider initialization (all 4 providers)
   - Successful LLM calls for each provider
   - Provider fallback on failure
   - Token counting accuracy (OpenAI tiktoken, Anthropic, DeepSeek, Gemini)
   - Cost calculation per provider
   - Streaming response handling
   - Error conditions (invalid API key, rate limit, timeout)
   - Provider selection based on model name
   - Concurrent request handling
  </action>
  <verify>grep -c "def test_.*byok" tests/unit/test_byok_handler.py 2>/dev/null || echo "0"</verify>
  <done>Test matrix created, 30-35 test scenarios identified</done>
</task>

<task type="auto">
  <name>Write comprehensive unit tests for BYOK handler</name>
  <files>tests/unit/test_byok_handler.py</files>
  <action>
Create tests/unit/test_byok_handler.py with:

**Test Classes:**
1. TestProviderInitialization (4-6 tests)
   - Initialize OpenAI provider
   - Initialize Anthropic provider
   - Initialize DeepSeek provider
   - Initialize Gemini provider
   - Invalid provider configuration
   - Missing API key handling

2. TestProviderSelection (5-6 tests)
   - Select OpenAI for gpt-* models
   - Select Anthropic for claude-* models
   - Select DeepSeek for deepseek-* models
   - Select Gemini for gemini-* models
   - Unknown model default provider
   - Custom provider override

3. TestTokenCounting (8-10 tests)
   - OpenAI token counting (tiktoken)
   - Anthropic token counting
   - DeepSeek token counting
   - Gemini token counting
   - Empty string token count
   - Long text token count (>1000 tokens)
   - Special characters and unicode
   - Token count accuracy verification

4. TestCostCalculation (6-8 tests)
   - OpenAI cost calculation (input + output)
   - Anthropic cost calculation
   - DeepSeek cost calculation
   - Gemini cost calculation
   - Cost calculation with cached tokens
   - Zero token cost edge case
   - Large batch cost calculation

5. TestLLMExecution (6-8 tests)
   - Successful OpenAI completion
   - Successful Anthropic completion
   - Successful DeepSeek completion
   - Successful Gemini completion
   - Streaming response handling
   - Non-streaming response handling
   - Response parsing and validation

6. TestProviderFallback (4-5 tests)
   - Fallback to secondary provider on failure
   - Fallback chain exhaustion
   - Fallback with different model
   - Fallback logging and monitoring
   - Disabled fallback behavior

7. TestErrorHandling (6-8 tests)
   - Invalid API key error
   - Rate limit error (429)
   - Timeout error
   - Network connection error
   - Invalid request error (400)
   - Provider unavailable error
   - Graceful degradation on all providers down

**Target:** 600+ lines, 30-35 tests, 80%+ coverage
  </action>
  <verify>wc -l tests/unit/test_byok_handler.py && pytest tests/unit/test_byok_handler.py -v --tb=short | tail -20</verify>
  <done>600+ lines, 30-35 tests, 80%+ coverage achieved</done>
</task>

<task type="auto">
  <name>Run tests and verify coverage target</name>
  <files>tests/unit/test_byok_handler.py</files>
  <action>
Verify tests pass and coverage target met:

1. Run pytest with coverage:
   ```bash
   pytest tests/unit/test_byok_handler.py --cov=core/llm/byok_handler \
          --cov-report=term-missing --cov-report=json -v
   ```

2. Verify:
   - All tests pass (0 failures)
   - Coverage for byok_handler.py ≥80%
   - No untested critical paths

3. If coverage <80%, add tests for missing lines
   - Use coverage report to identify uncovered lines
   - Add tests for uncovered branches
   - Re-run until target met
  </action>
  <verify>grep "byok_handler.py" coverage.json | jq -c '.files[].coverage' | head -1</verify>
  <done>All tests pass, 80%+ coverage achieved</done>
</task>

</tasks>

<verification>
After completion:
1. Verify test file exists with 600+ lines
2. Run pytest and confirm all tests pass
3. Check coverage report shows 80%+ for byok_handler.py
4. Verify no critical provider handling paths untested
</verification>

<success_criteria>
- [ ] tests/unit/test_byok_handler.py created (600+ lines)
- [ ] 30-35 tests covering all BYOK handler functionality
- [ ] All tests passing (0 failures)
- [ ] Coverage ≥80% for byok_handler.py
- [ ] Test execution time <30 seconds total
</success_criteria>

<output>
After completion, create `.planning/phases/62-test-coverage-80pct/62-04-SUMMARY.md`
</output>
