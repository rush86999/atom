---
phase: 62-test-coverage-80pct
plan: 07
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - integrations/mcp_service.py
  - tests/integration/test_mcp_service.py
autonomous: true

must_haves:
  truths:
    - "MCP service has comprehensive integration test coverage (>80%)"
    - "MCP server connection and lifecycle tested"
    - "Tool discovery and execution tested"
    - "Error handling and timeout tested"
  artifacts:
    - path: "tests/integration/test_mcp_service.py"
      provides: "Comprehensive MCP service tests"
      min_lines: 700
  key_links:
    - from: "tests/integration/test_mcp_service.py"
      to: "integrations/mcp_service.py"
      via: "Integration test coverage"
      pattern: "def test_.*mcp"
---

<objective>
Add comprehensive test coverage for mcp_service.py (1,113 lines, 2.0% coverage → 80%+).

Purpose: MCP (Model Context Protocol) service manages external tool integrations. At 2.0% coverage, this is the second-highest priority file. Untested code could cause tool failures, connection leaks, or incorrect tool execution.

Output: 700+ lines of integration tests achieving 80%+ coverage for mcp_service.py
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/docs/COVERAGE_ANALYSIS.md
@integrations/mcp_service.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Analyze MCP service structure and coverage gaps</name>
  <files>integrations/mcp_service.py</files>
  <action>
Analyze mcp_service.py to identify untested code:

1. Read the file and identify:
   - MCP server connection management
   - Server lifecycle (start, stop, restart)
   - Tool discovery and listing
   - Tool execution with arguments
   - Response parsing and validation
   - Error handling (timeouts, connection failures, invalid responses)
   - Authentication and authorization
   - Resource management (handles, streams)
   - Server health checks
   - Concurrent request handling

2. Create test matrix:
   - Method name | Current coverage | Test priority | External dependency
   - Focus on methods with <10% coverage (entire file)

3. Identify test scenarios needed:
   - Server startup and shutdown
   - Tool discovery and listing
   - Successful tool execution
   - Tool execution with arguments
   - Tool execution timeout
   - Connection failure handling
   - Invalid tool handling
   - Response parsing
   - Health check status
   - Multiple server management
   - Concurrent tool execution
  </action>
  <verify>grep -c "def test_.*mcp" tests/integration/test_mcp_service.py 2>/dev/null || echo "0"</verify>
  <done>Test matrix created, 35-40 test scenarios identified</done>
</task>

<task type="auto">
  <name>Write comprehensive integration tests for MCP service</name>
  <files>tests/integration/test_mcp_service.py</files>
  <action>
Create tests/integration/test_mcp_service.py with:

**Test Classes:**
1. TestMCPServerLifecycle (6-8 tests)
   - Start MCP server successfully
   - Stop MCP server
   - Restart MCP server
   - Start server with invalid config
   - Start server with connection timeout
   - Server health check (healthy)
   - Server health check (unhealthy)
   - Multiple server instances

2. TestMCPToolDiscovery (5-6 tests)
   - List available tools
   - Get tool details
   - Tool schema validation
   - Tool discovery after server restart
   - Discovery timeout handling
   - Empty tool list handling

3. TestMCPToolExecution (8-10 tests)
   - Execute tool successfully
   - Execute tool with arguments
   - Execute tool with complex arguments (nested objects)
   - Execute tool with binary data
   - Execute non-existent tool (404)
   - Execute tool with invalid arguments
   - Tool execution timeout
   - Tool execution response parsing
   - Tool execution error handling
   - Tool execution with streaming response

4. TestMCPConnectionManagement (5-6 tests)
   - Connection pool management
   - Connection leak prevention
   - Connection retry logic
   - Connection timeout handling
   - Connection authentication
   - Connection with invalid credentials

5. TestMCPResponseHandling (5-6 tests)
   - Parse successful response
   - Parse error response
   - Parse streaming response chunks
   - Parse binary response
   - Parse malformed response
   - Response validation

6. TestMCPErrorHandling (6-8 tests)
   - Connection refused error
   - Authentication failure
   - Timeout error
   - Invalid tool error
   - Invalid arguments error
   - Server internal error
   - Network partition error
   - Graceful degradation

7. TestMCPConcurrency (4-5 tests)
   - Concurrent tool execution
   - Concurrent tool discovery
   - Concurrent server management
   - Thread safety
   - Race condition handling

**Target:** 700+ lines, 35-40 tests, 80%+ coverage

**Note:** Use mocking for external MCP server connections or use test MCP servers
  </action>
  <verify>wc -l tests/integration/test_mcp_service.py && pytest tests/integration/test_mcp_service.py -v --tb=short | tail -20</verify>
  <done>700+ lines, 35-40 tests, 80%+ coverage achieved</done>
</task>

<task type="auto">
  <name>Run tests and verify coverage target</name>
  <files>tests/integration/test_mcp_service.py</files>
  <action>
Verify tests pass and coverage target met:

1. Run pytest with coverage:
   ```bash
   pytest tests/integration/test_mcp_service.py \
          --cov=integrations/mcp_service \
          --cov-report=term-missing --cov-report=json -v
   ```

2. Verify:
   - All tests pass (0 failures)
   - Coverage for mcp_service.py ≥80%
   - No untested critical paths

3. If coverage <80%, add tests for missing lines
   - Use coverage report to identify uncovered lines
   - Add tests for uncovered branches
   - Re-run until target met
  </action>
  <verify>grep "mcp_service.py" coverage.json | jq -c '.files[].coverage' | head -1</verify>
  <done>All tests pass, 80%+ coverage achieved</done>
</task>

</tasks>

<verification>
After completion:
1. Verify test file exists with 700+ lines
2. Run pytest and confirm all tests pass
3. Check coverage report shows 80%+ for mcp_service.py
4. Verify no critical MCP service paths untested
</verification>

<success_criteria>
- [ ] tests/integration/test_mcp_service.py created (700+ lines)
- [ ] 35-40 tests covering all MCP service functionality
- [ ] All tests passing (0 failures)
- [ ] Coverage ≥80% for mcp_service.py
- [ ] Test execution time <45 seconds total
</success_criteria>

<output>
After completion, create `.planning/phases/62-test-coverage-80pct/62-07-SUMMARY.md`
</output>
