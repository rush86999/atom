---
phase: 62-test-coverage-80pct
plan: 10
type: execute
wave: 3
depends_on: [62-01]
files_modified:
  - Multiple remaining low-coverage core services
  - tests/unit/test_core_services_batch.py
  - tests/integration/test_integrations_batch.py
autonomous: true

must_haves:
  truths:
    - "Remaining core services have test coverage (>60%)"
    - "Remaining integrations have test coverage (>60%)"
    - "Overall coverage target approaching 50%"
    - "No critical files remaining below 30% coverage"
  artifacts:
    - path: "tests/unit/test_core_services_batch.py"
      provides: "Core services tests (batch)"
      min_lines: 800
    - path: "tests/integration/test_integrations_batch.py"
      provides: "Integration tests (batch)"
      min_lines: 800
  key_links:
    - from: "tests/unit/test_core_services_batch.py"
      to: "core/"
      via: "Unit test coverage"
      pattern: "def test_.*service"
    - from: "tests/integration/test_integrations_batch.py"
      to: "integrations/"
      via: "Integration test coverage"
      pattern: "def test_.*integration"
---

<objective>
Add test coverage for remaining low-coverage files in Wave 3C to achieve overall 50% coverage target.

This batch plan covers the remaining core services and integrations that weren't addressed in previous waves. The goal is to bring overall coverage from 17.12% baseline to approximately 50% by addressing the long tail of low-coverage files.

Target categories:
- Core services: 10-15 remaining files with <50% coverage
- Integration services: 20-25 remaining files with <40% coverage
- Focus on files >300 lines with <20% coverage

Purpose: Complete the coverage improvement journey by addressing the long tail of untested code. This plan builds on Waves 1 and 2 to achieve the 50% overall coverage milestone.

Output: 1,600+ lines of tests (800 unit + 800 integration) achieving 60%+ coverage for targeted files
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@backend/docs/COVERAGE_ANALYSIS.md
@core/
@integrations/
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Identify remaining low-coverage files for batch testing</name>
  <files>backend/docs/COVERAGE_ANALYSIS.md</files>
  <action>
Generate updated coverage report and identify remaining gaps:

1. Run pytest with coverage on all existing tests:
   ```bash
   pytest tests/ --cov=core --cov=api --cov=integrations --cov=tools \
          --cov-report=term --cov-report=json -q
   ```

2. Identify remaining files with:
   - <30% coverage AND >200 lines (priority 1)
   - <50% coverage AND >400 lines (priority 2)
   - Any file with 0% coverage and >100 lines (priority 3)

3. Create target list:
   - Group by category (core vs integration)
   - Prioritize by impact score (lines × (1 - coverage))
   - Select top 30-40 files for this batch

4. Estimate coverage gain:
   - Current overall: [from coverage report]
   - Target overall: 50%
   - Expected gain: +32.88 percentage points
   - Files needed: 30-40 files at 60%+ average coverage
  </action>
  <verify>grep -c "def test_.*service\|def test_.*integration" tests/unit/test_core_services_batch.py tests/integration/test_integrations_batch.py 2>/dev/null || echo "0"</verify>
  <done>30-40 target files identified, 40-50 test scenarios planned</done>
</task>

<task type="auto">
  <name>Write comprehensive unit tests for core services batch</name>
  <files>tests/unit/test_core_services_batch.py</files>
  <action>
Create tests/unit/test_core_services_batch.py with:

**Test Structure:**
This file will contain tests for multiple core services grouped by functionality. Based on coverage analysis, prioritize:

1. **Agent Management Services** (6-8 tests)
   - Agent lifecycle operations
   - Agent configuration
   - Agent state management
   - Agent deployment

2. **Storage Services** (6-8 tests)
   - File storage operations
   - Blob storage
   - Storage cleanup
   - Storage metrics

3. **Cache Services** (5-6 tests)
   - Cache set/get/delete
   - Cache expiration
   - Cache invalidation
   - Distributed caching

4. **Notification Services** (5-6 tests)
   - Send notifications
   - Notification templates
   - Notification batching
   - Notification history

5. **Analytics Services** (6-8 tests)
   - Event tracking
   - Metrics aggregation
   - Analytics queries
   - Report generation

6. **Configuration Services** (4-5 tests)
   - Config loading
   - Config validation
   - Config updates
   - Feature flags

**Target:** 800+ lines, 32-42 tests, 60%+ coverage

**Note:** Each service will have 3-6 focused tests covering critical paths
  </action>
  <verify>wc -l tests/unit/test_core_services_batch.py && pytest tests/unit/test_core_services_batch.py -v --tb=short | tail -20</verify>
  <done>800+ lines, 32-42 tests, 60%+ coverage achieved</done>
</task>

<task type="auto">
  <name>Write comprehensive integration tests for integrations batch</name>
  <files>tests/integration/test_integrations_batch.py</files>
  <action>
Create tests/integration/test_integrations_batch.py with:

**Test Structure:**
This file will contain tests for multiple integration services. Based on coverage analysis, prioritize:

1. **Education Integration Services** (5-6 tests)
   - Education customization operations
   - Course management
   - Student tracking
   - LMS integration

2. **Finance Integration Services** (5-6 tests)
   - Finance customization operations
   - Transaction processing
   - Account management
   - Financial reporting

3. **Google Services Integration** (4-5 tests)
   - Google Chat operations
   - Google Drive integration
   - Google Calendar integration
   - Authentication

4. **Zoom Integration** (4-5 tests)
   - Meeting management
   - Webhook handling
   - User operations
   - Recording management

5. **Enterprise Integration** (4-5 tests)
   - Enterprise operations
   - SSO integration
   - User provisioning
   - Policy management

6. **Communication Integration** (5-6 tests)
   - Message routing
   - Channel management
   - Webhook handling
   - Media handling

**Target:** 800+ lines, 27-38 tests, 60%+ coverage

**Note:** Use mocking for external API calls to ensure test reliability
  </action>
  <verify>wc -l tests/integration/test_integrations_batch.py && pytest tests/integration/test_integrations_batch.py -v --tb=short | tail -20</verify>
  <done>800+ lines, 27-38 tests, 60%+ coverage achieved</done>
</task>

<task type="auto">
  <name>Run tests and verify overall coverage target</name>
  <files>tests/unit/test_core_services_batch.py, tests/integration/test_integrations_batch.py</files>
  <action>
Verify tests pass and overall coverage target met:

1. Run pytest with coverage on entire codebase:
   ```bash
   pytest tests/ --cov=core --cov=api --cov=integrations --cov=tools \
          --cov-report=term --cov-report=json --cov-report=html -v
   ```

2. Verify:
   - All tests pass (0 failures)
   - Overall coverage ≥50% (primary goal)
   - Targeted files in batch ≥60% coverage
   - No critical files remaining below 30% coverage

3. Generate updated coverage analysis:
   - Compare to baseline (17.12%)
   - Calculate coverage gain
   - Identify remaining gaps for future work

4. Update COVERAGE_ANALYSIS.md with:
   - New overall coverage percentage
   - Files that achieved target
   - Remaining files below 30%
   - Recommendations for next phase
  </action>
  <verify>python3 -c "import json; c=json.load(open('coverage.json')); files=c['files']; total_lines=sum(f['summary']['num_statements'] for f in files); total_covered=sum(f['summary']['covered_lines'] for f in files); print(f'{total_covered}/{total_lines} = {100*total_covered/total_lines:.1f}%')"</verify>
  <done>All tests pass, overall coverage ≥50% achieved</done>
</task>

</tasks>

<verification>
After completion:
1. Verify test files exist with 800+ lines each
2. Run pytest and confirm all tests pass
3. Check overall coverage report shows ≥50%
4. Verify no critical files remaining below 30% coverage
5. Update COVERAGE_ANALYSIS.md with new metrics
</verification>

<success_criteria>
- [ ] tests/unit/test_core_services_batch.py created (800+ lines)
- [ ] tests/integration/test_integrations_batch.py created (800+ lines)
- [ ] 60-80 tests covering remaining core and integration services
- [ ] All tests passing (0 failures)
- [ ] Overall coverage ≥50% (major milestone)
- [ ] Test execution time <90 seconds total
</success_criteria>

<output>
After completion, create `.planning/phases/62-test-coverage-80pct/62-10-SUMMARY.md`
</output>
