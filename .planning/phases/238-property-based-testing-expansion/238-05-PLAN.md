---
phase: 238-property-based-testing-expansion
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/property_tests/state_machines/__init__.py
  - backend/tests/property_tests/state_machines/conftest.py
  - backend/tests/property_tests/state_machines/test_graduation_state_machine.py
  - backend/tests/property_tests/security/__init__.py
  - backend/tests/property_tests/security/conftest.py
  - backend/tests/property_tests/security/test_sql_injection.py
  - backend/tests/property_tests/security/test_xss_prevention.py
  - backend/tests/property_tests/security/test_csrf_protection.py
  - backend/tests/property_tests/INVARIANTS.md
autonomous: true

must_haves:
  truths:
    - "Agent graduation is monotonic (maturity never decreases: STUDENT → INTERN → SUPERVISED → AUTONOMOUS)"
    - "Training sessions follow valid transitions (PENDING → IN_PROGRESS → COMPLETED, no invalid state jumps)"
    - "SQL injection attempts are sanitized (no SQL executed from malicious input)"
    - "XSS payloads are escaped (output does not contain unescaped <script> tags)"
    - "CSRF tokens are validated on state-changing requests (POST/DELETE without token rejected)"
  artifacts:
    - path: "backend/tests/property_tests/state_machines/test_graduation_state_machine.py"
      provides: "Agent graduation state machine invariant tests (monotonic maturity progression)"
      min_lines: 180
      contains: "@pytest.mark.property", "RuleBasedStateMachine", "graduation", "monotonic"
    - path: "backend/tests/property_tests/security/test_sql_injection.py"
      provides: "SQL injection invariant tests (malicious SQL is sanitized)"
      min_lines: 130
      contains: "@pytest.mark.property", "@given", "sql_injection", "sanitized"
    - path: "backend/tests/property_tests/security/test_xss_prevention.py"
      provides: "XSS prevention invariant tests (XSS payloads are escaped)"
      min_lines: 120
      contains: "@pytest.mark.property", "@given", "xss", "escaped", "<script"
    - path: "backend/tests/property_tests/security/test_csrf_protection.py"
      provides: "CSRF protection invariant tests (CSRF tokens validated on state changes)"
      min_lines: 120
      contains: "@pytest.mark.property", "@given", "csrf", "token", "validated"
    - path: "backend/tests/property_tests/INVARIANTS.md"
      provides: "Updated invariant catalog with all 50+ new invariants from Phase 238"
      min_lines: 2100
      contains: "238-01", "238-02", "238-03", "238-04", "238-05"
  key_links:
    - from: "backend/tests/property_tests/state_machines/test_graduation_state_machine.py"
      to: "backend/core/agent_graduation_service.py"
      via: "AgentGraduationService.check_graduation_eligibility()"
      pattern: "from core.agent_graduation_service import"
    - from: "backend/tests/property_tests/security/test_sql_injection.py"
      to: "backend/core/models.py"
      via: "SQLAlchemy ORM queries (should sanitize input)"
      pattern: "from sqlalchemy import"
    - from: "backend/tests/property_tests/security/test_xss_prevention.py"
      to: "backend/api/canvas_routes.py"
      via: "Canvas rendering (should escape user input)"
      pattern: "from fastapi import"
    - from: "backend/tests/property_tests/security/test_csrf_protection.py"
      to: "backend/core/models.py"
      via: "CSRF token validation in state-changing endpoints"
      pattern: "csrf"
---

<objective>
Create state machine property tests using RuleBasedStateMachine and security property tests for SQL injection, XSS, and CSRF prevention. Update INVARIANTS.md with all new invariants.

**Purpose:** PROP-03 requires state machine invariants (agent graduation monotonicity). PROP-04 requires security invariants (SQL injection, XSS, CSRF). These tests catch critical bugs: graduation regression (AUTONOMOUS → STUDENT), SQL injection vulnerabilities, XSS exploits, CSRF bypasses.

**Output:** 6 new test files + updated INVARIANTS.md with 12-15 property tests covering graduation state machine, training session transitions, and security invariants.

**Why this matters:** State machine testing with RuleBasedStateMachine automatically generates random transition sequences to find invariant violations. Security property tests use Hypothesis strategies to generate infinite variations of attack vectors (SQL injection, XSS payloads).
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/238-property-based-testing-expansion/238-RESEARCH.md
@backend/tests/property_tests/INVARIANTS.md
@backend/tests/property_tests/conftest.py
@backend/tests/bug_discovery/TEMPLATES/PROPERTY_TEMPLATE.md
@backend/core/agent_graduation_service.py
@backend/core/student_training_service.py
@backend/core/models.py
@backend/core/api_governance.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create state machines and security test infrastructure</name>
  <files>
    backend/tests/property_tests/state_machines/__init__.py
    backend/tests/property_tests/state_machines/conftest.py
    backend/tests/property_tests/security/__init__.py
    backend/tests/property_tests/security/conftest.py
  </files>
  <action>
    Create state_machines and security property test directories with fixtures:

    1. Create `backend/tests/property_tests/state_machines/__init__.py` (empty file)

    2. Create `backend/tests/property_tests/state_machines/conftest.py` with:
       - Import db_session from parent conftest
       - Fixture: `test_training_sessions(db_session)` - creates TrainingSession with valid states
       - Fixture: `test_agents_for_graduation(db_session)` - creates agents at all maturity levels
       - Import from hypothesis.stateful for RuleBasedStateMachine
       - Import Hypothesis strategies (st.integers, st.floats, st.text)

    3. Create `backend/tests/property_tests/security/__init__.py` (empty file)

    4. Create `backend/tests/property_tests/security/conftest.py` with:
       - Import db_session from parent conftest
       - Import authenticated_client from e2e_ui fixtures
       - Fixture: `malicious_sql_inputs()` - returns st.one_of() for SQL injection patterns
       - Fixture: `xss_payloads()` - returns st.one_of() for XSS attack vectors
       - Fixture: `csrf_test_client()` - creates client without CSRF token for testing
       - Import Hypothesis strategies (st.text, st.lists, st.dictionaries)

    Follow pattern from `backend/tests/property_tests/governance/test_governance_invariants_property.py`:
    - HYPOTHESIS_SETTINGS_CRITICAL: max_examples=200 (graduation monotonicity)
    - HYPOTHESIS_SETTINGS_STANDARD: max_examples=100 (security invariants)
    - HYPOTHESIS_SETTINGS_IO: max_examples=50 (DB operations for SQL injection)

    DO NOT duplicate db_session - import from parent:
    `from tests.property_tests.conftest import db_session, DEFAULT_PROFILE, CI_PROFILE`
  </action>
  <verify>
    ls -la backend/tests/property_tests/state_machines/ shows __init__.py and conftest.py
    ls -la backend/tests/property_tests/security/ shows __init__.py and conftest.py
    grep -l "from tests.property_tests.conftest import" backend/tests/property_tests/state_machines/conftest.py backend/tests/property_tests/security/conftest.py
    grep -l "RuleBasedStateMachine\|hypothesis.stateful" backend/tests/property_tests/state_machines/conftest.py
  </verify>
  <done>
    state_machines and security directories exist with fixtures imported from parent (no duplicate db_session)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create agent graduation state machine property tests</name>
  <files>
    backend/tests/property_tests/state_machines/test_graduation_state_machine.py
  </files>
  <action>
    Create `backend/tests/property_tests/state_machines/test_graduation_state_machine.py` with 2-3 RuleBasedStateMachine tests:

    Test 1: `test_agent_graduation_monotonic_state_machine`
    - Use `hypothesis.stateful.RuleBasedStateMachine`
    - States: STUDENT (0), INTERN (1), SUPERVISED (2), AUTONOMOUS (3)
    - @initialize(): Create STUDENT agent (confidence < 0.5)
    - @rule(confidence_boost=floats(min_value=0.0, max_value=0.3)):
      - Boost agent confidence
      - Update maturity based on new confidence (<0.5 STUDENT, 0.5-0.7 INTERN, 0.7-0.9 SUPERVISED, >0.9 AUTONOMOUS)
    - @invariant() maturity_never_decreases():
      - Assert: maturity_history is monotonically non-decreasing
      - For all i > 0: maturity_order[history[i]] >= maturity_order[history[i-1]]
    - Settings: HYPOTHESIS_SETTINGS_CRITICAL (max_examples=200 for thorough transition exploration)

    Test 2: `test_graduation_requirements_satisfied_before_promotion`
    - Invariant: Promotion only occurs if requirements met (episode count, intervention rate, constitutional score)
    - Strategy: st.tuples(
        st.integers(0, 100),  # episode_count
        st.floats(0.0, 1.0),  # intervention_rate
        st.floats(0.5, 1.0)   # constitutional_score
      )
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Assert: Promotion occurs iff all requirements met

    Test 3 (optional): `test_training_session_state_transitions`
    - Use RuleBasedStateMachine for training sessions
    - States: PENDING, IN_PROGRESS, COMPLETED, CANCELLED
    - Rules: start(), complete(), cancel()
    - @invariant(): No invalid transitions (e.g., PENDING → COMPLETED without IN_PROGRESS)
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)

    DOCUMENT INVARIANTS FIRST (PROP-05 requirement):
    Each test docstring must have:
    - "PROPERTY:" - what invariant is being tested
    - "STRATEGY:" - what Hypothesis strategies generate inputs
    - "INVARIANT:" - formal statement of what must be true
    - "RADII:" - why N examples are sufficient

    Follow pattern from RESEARCH.md lines 191-256 for RuleBasedStateMachine structure.
  </action>
  <verify>
    pytest backend/tests/property_tests/state_machines/test_graduation_state_machine.py -v -k "test_agent_graduation_monotonic" passes
    grep -c "@pytest.mark.property" backend/tests/property_tests/state_machines/test_graduation_state_machine.py >= 2
    grep -c "RuleBasedStateMachine" backend/tests/property_tests/state_machines/test_graduation_state_machine.py >= 1
    grep -c "@invariant()" backend/tests/property_tests/state_machines/test_graduation_state_machine.py >= 1
  </verify>
  <done>
    2-3 state machine property tests using RuleBasedStateMachine (PROP-03 compliant)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create security property tests (SQL injection, XSS, CSRF)</name>
  <files>
    backend/tests/property_tests/security/test_sql_injection.py
    backend/tests/property_tests/security/test_xss_prevention.py
    backend/tests/property_tests/security/test_csrf_protection.py
  </files>
  <action>
    Create three test files with 8-10 property tests total:

    **test_sql_injection.py** (3 tests):
    Test 1: `test_sql_injection_sanitized_in_queries`
    - Invariant: SQL injection attempts are sanitized (no SQL executed from malicious input)
    - Strategy: st.one_of(
        st.just("' OR '1'='1"),
        st.just("'; DROP TABLE users; --"),
        st.just("1' UNION SELECT * FROM users --"),
        st.text().map(lambda x: f"'{x}' OR '1'='1")  # Generic pattern
      )
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Act: Query agent by name with malicious input
    - Assert: Query returns 0 results or single exact match (never all records)

    Test 2: `test_sql_injection_in_agent_creation`
    - Invariant: Agent creation with malicious SQL does not execute SQL
    - Strategy: st.dictionaries(
        st.just("name"),
        st.one_of(st.text(), st.just("'; DROP TABLE agents; --"))
      )
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Assert: Agent created with name as literal string (no SQL executed)

    Test 3: `test_sql_injection_in_filter_clauses`
    - Invariant: Filter clauses sanitize SQL metacharacters
    - Strategy: st.lists(st.text().filter(lambda x: "'" in x or ";" in x), min_size=1, max_size=10)
    - Settings: HYPOTHESIS_SETTINGS_IO (max_examples=50, DB operations)

    **test_xss_prevention.py** (3 tests):
    Test 1: `test_xss_payloads_escaped_in_response`
    - Invariant: XSS payloads are escaped (output does not contain unescaped <script> tags)
    - Strategy: st.one_of(
        st.just("<script>alert('XSS')</script>"),
        st.just("<img src=x onerror=alert('XSS')>"),
        st.just("javascript:alert('XSS')"),
        st.text().map(lambda x: f"<script>{x}</script>")
      )
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Act: Create agent with XSS payload in name, retrieve via API
    - Assert: Response contains escaped entities (&lt;script&gt;, not <script>)

    Test 2: `test_xss_in_canvas_content`
    - Invariant: Canvas content sanitizes HTML tags
    - Strategy: st.dictionaries(
        st.sampled_from(["title", "content"]),
        st.just("<script>alert('XSS')</script>")
      )
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Assert: Canvas content is escaped or sanitized (no <script> in output)

    Test 3: `test_xss_in_user_generated_content`
    - Invariant: All user-generated content fields escape HTML
    - Strategy: st.tuples(st.text(), st.sampled_from(["name", "description", "content"]))
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Assert: HTML special chars are escaped (<, >, &, ", ')

    **test_csrf_protection.py** (2-3 tests):
    Test 1: `test_csrf_token_required_on_state_changing_requests`
    - Invariant: POST/DELETE requests without CSRF token are rejected
    - Strategy: st.sampled_from(["POST", "DELETE", "PUT"]) for http_methods
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Act: Make state-changing request without CSRF token
    - Assert: Response is 403 Forbidden (not 200 OK)

    Test 2: `test_csrf_token_validated_on_mutating_operations`
    - Invariant: Invalid CSRF tokens are rejected (only valid tokens accepted)
    - Strategy: st.one_of(
        st.just(""),
        st.just("invalid"),
        st.just("null"),
        st.text(min_size=32, max_size=32).filter(lambda x: x != "valid_token_format")
      )
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Act: Make request with invalid CSRF token
    - Assert: Response is 403 Forbidden

    Test 3 (optional): `test_safe_methods_exempt_from_csrf`
    - Invariant: GET, HEAD, OPTIONS requests don't require CSRF token
    - Strategy: st.sampled_from(["GET", "HEAD", "OPTIONS"])
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Act: Make safe request without CSRF token
    - Assert: Response is not 403 Forbidden (safe methods work without token)

    DOCUMENT INVARIANTS FIRST in each test docstring (PROP-05 requirement).
  </action>
  <verify>
    pytest backend/tests/property_tests/security/test_sql_injection.py -v passes
    pytest backend/tests/property_tests/security/test_xss_prevention.py -v passes
    pytest backend/tests/property_tests/security/test_csrf_protection.py -v passes
    grep -c "@pytest.mark.property" backend/tests/property_tests/security/test_*.py | awk '{sum+=$1} END {print sum}'  # Should be 8+
    grep -c "sql_injection\|xss\|csrf" backend/tests/property_tests/security/test_*.py | awk '{sum+=$1} END {print sum}'  # Should be 8+
  </verify>
  <done>
    8-10 security property tests covering SQL injection, XSS, CSRF invariants (PROP-04 compliant)
  </done>
</task>

<task type="auto">
  <name>Task 4: Update INVARIANTS.md with all 50+ new invariants</name>
  <files>
    backend/tests/property_tests/INVARIANTS.md
  </files>
  <action>
    Update `backend/tests/property_tests/INVARIANTS.md` with all new invariants from Phase 238:

    1. Read existing INVARIANTS.md (2021 lines, 66 documented invariants)
    2. Add new section: "## Phase 238: Property-Based Testing Expansion Invariants"
    3. For each plan (238-01 through 238-05), add invariant entries:

    **Format for each invariant:**
    ```markdown
    ### INV-XXX: [Invariant Name]

    **Category:** [Agent Execution|LLM Routing|Episodic Memory|Governance|API Contracts|State Machines|Security]

    **Formal Definition:** [Mathematical statement of invariant]

    **Test Location:** `backend/tests/property_tests/[category]/test_[name].py`

    **Hypothesis Strategy:** [Strategy used to generate inputs]

    **Criticality:** [CRITICAL|STANDARD|IO-BOUND]

    **Validation:** [Pass/Fail, bug discovered if any]

    **Example:**
    ```python
    # Invariant: For any agent_id, executing twice produces same result
    @given(st.uuids())
    @settings(max_examples=200)
    def test_execution_idempotent(agent_id):
        result_1 = execute_agent(agent_id, params={})
        result_2 = execute_agent(agent_id, params={})
        assert result_1.execution_id == result_2.execution_id
    ```
    ```

    4. Add invariants for:
       - 238-01: Agent execution (3-4 invariants: idempotence, termination, determinism)
       - 238-02: LLM routing (4-5 invariants: routing consistency, tier mapping, cache-aware)
       - 238-03: Episodic memory (4-5 invariants: segmentation, retrieval, lifecycle)
       - 238-04: API contracts + governance (5-6 invariants: malformed JSON, authorization, cache)
       - 238-05: State machines + security (6-7 invariants: graduation, SQL injection, XSS, CSRF)

    5. Update total count at top of INVARIANTS.md (66 → 116+ invariants)

    INVARIANTS.md is the authoritative catalog of all invariants - new tests MUST add entries here (RESEARCH.md line 685-689).
  </action>
  <verify>
    grep -c "### INV-" backend/tests/property_tests/INVARIANTS.md | awk '{print $1 >= 116 ? "PASS" : "FAIL"}'  # Should be 116+
    grep "Phase 238" backend/tests/property_tests/INVARIANTS.md
    grep "238-01\|238-02\|238-03\|238-04\|238-05" backend/tests/property_tests/INVARIANTS.md
    grep -c "Agent Execution\|LLM Routing\|Episodic Memory\|API Contracts\|State Machines\|Security" backend/tests/property_tests/INVARIANTS.md | awk '{sum+=$1} END {print sum >= 6 ? "PASS" : "FAIL"}'  # All 6 categories present
  </verify>
  <done>
    INVARIANTS.md updated with 50+ new invariants from Phase 238 (total 116+ invariants)
  </done>
</task>

</tasks>

<verification>
## Overall Phase Checks

After all tasks complete, verify:

```bash
# 1. All new tests run
pytest backend/tests/property_tests/state_machines/ -v
pytest backend/tests/property_tests/security/ -v

# 2. Property tests have correct marker
pytest backend/tests/property_tests/state_machines/ -v -m property
pytest backend/tests/property_tests/security/ -v -m property

# 3. Invariant documentation present (PROP-05)
grep -r "INVARIANT:" backend/tests/property_tests/state_machines/ backend/tests/property_tests/security/ | wc -l  # Should be 10+

# 4. State machine testing present (PROP-03)
grep -c "RuleBasedStateMachine" backend/tests/property_tests/state_machines/test_graduation_state_machine.py
grep -c "@invariant()" backend/tests/property_tests/state_machines/test_graduation_state_machine.py

# 5. Security invariants cover all required areas (PROP-04)
grep -c "sql_injection\|xss\|csrf" backend/tests/property_tests/security/test_*.py | awk '{sum+=$1} END {print sum}'  # Should be 8+

# 6. INVARIANTS.md updated
grep -c "### INV-" backend/tests/property_tests/INVARIANTS.md  # Should be 116+
```

Expected results:
- 12-15 property tests across 6 files
- All tests have invariant documentation (PROPERTY, STRATEGY, INVARIANT, RADII)
- State machine tests use RuleBasedStateMachine with @invariant() checks
- Security tests cover SQL injection, XSS, CSRF (PROP-04 requirement met)
- INVARIANTS.md updated with 50+ new invariants (66 → 116+)
</verification>

<success_criteria>
1. 12-15 property tests created covering state machines and security invariants
2. All tests follow invariant-first pattern (PROP-05): invariant documented BEFORE test code
3. Tests use correct max_examples: 200 (CRITICAL), 100 (STANDARD), 50 (IO-bound)
4. State machine tests use RuleBasedStateMachine (PROP-03 requirement met)
5. Security tests cover SQL injection, XSS, CSRF (PROP-04 requirement met)
6. All tests pass with `pytest backend/tests/property_tests/state_machines/ -v` and `pytest backend/tests/property_tests/security/ -v`
7. INVARIANTS.md updated with 50+ new invariants (66 → 116+ total invariants)
</success_criteria>

<output>
After completion, create `.planning/phases/238-property-based-testing-expansion/238-05-SUMMARY.md` with:
- Tests created: List all test files and test counts (total should be 50+ tests across 5 plans)
- Invariants validated: Summary of all invariants covered (50+ new invariants)
- Bugs found: Any invariant violations discovered (graduation regression, SQL injection, XSS, CSRF bypasses)
- Total invariants: 116+ invariants documented in INVARIANTS.md
- Phase 238 complete: PROP-01 through PROP-05 requirements satisfied
</output>
