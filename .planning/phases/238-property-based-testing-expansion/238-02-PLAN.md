---
phase: 238-property-based-testing-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/property_tests/llm_routing/__init__.py
  - backend/tests/property_tests/llm_routing/conftest.py
  - backend/tests/property_tests/llm_routing/test_routing_consistency.py
  - backend/tests/property_tests/llm_routing/test_cognitive_tier_mapping.py
  - backend/tests/property_tests/llm_routing/test_cache_aware_routing.py
autonomous: true

must_haves:
  truths:
    - "Same prompt always routes to same LLM provider (routing is deterministic)"
    - "Token count maps to correct cognitive tier (Micro < 1K, Standard < 8K, Versatile < 32K, Heavy < 128K, Complex > 128K)"
    - "Cached prompts bypass classification (use cached tier, don't reclassify)"
    - "Cache-agnostic prompts are classified using token count + semantic complexity"
    - "Tier escalation preserves correctness (higher tier = same or better quality)"
  artifacts:
    - path: "backend/tests/property_tests/llm_routing/test_routing_consistency.py"
      provides: "Routing consistency invariant tests (same prompt → same provider)"
      min_lines: 120
      contains: "@pytest.mark.property", "@given", "routing", "consistent"
    - path: "backend/tests/property_tests/llm_routing/test_cognitive_tier_mapping.py"
      provides: "Cognitive tier mapping invariant tests (token count → correct tier)"
      min_lines: 150
      contains: "@pytest.mark.property", "@given", "cognitive_tier", "token_count"
    - path: "backend/tests/property_tests/llm_routing/test_cache_aware_routing.py"
      provides: "Cache-aware routing invariant tests (cached prompts skip classification)"
      min_lines: 120
      contains: "@pytest.mark.property", "@given", "cache_aware", "cached"
  key_links:
    - from: "backend/tests/property_tests/llm_routing/test_routing_consistency.py"
      to: "backend/core/llm/cognitive_tier_system.py"
      via: "CognitiveClassifier.classify() and CacheAwareRouter.route()"
      pattern: "from core.llm.cognitive_tier_system import"
    - from: "backend/tests/property_tests/llm_routing/conftest.py"
      to: "backend/tests/property_tests/conftest.py"
      via: "Import db_session, test_agent fixtures"
      pattern: "from tests.property_tests.conftest import"
---

<objective>
Create LLM routing property tests validating cognitive tier consistency, provider routing determinism, and cache-aware behavior.

**Purpose:** PROP-01 requires 50+ new property tests for critical paths. LLM routing is critical - every agent interaction uses LLM responses. Wrong tier = wasted cost or poor quality. Inconsistent routing = non-deterministic agent behavior.

**Output:** 3 new test files with 10-12 property tests covering routing consistency, cognitive tier mapping, and cache-aware routing invariants.

**Why this matters:** Property tests find edge cases like "token count exactly 8000 maps to wrong tier" or "cached prompts still get reclassified." Example-based tests miss these boundary conditions.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/238-property-based-testing-expansion/238-RESEARCH.md
@backend/tests/property_tests/INVARIANTS.md
@backend/tests/property_tests/conftest.py
@backend/tests/bug_discovery/TEMPLATES/PROPERTY_TEMPLATE.md
@backend/core/llm/cognitive_tier_system.py
@backend/core/llm/cache_aware_router.py
@backend/core/llm/escalation_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LLM routing test infrastructure</name>
  <files>
    backend/tests/property_tests/llm_routing/__init__.py
    backend/tests/property_tests/llm_routing/conftest.py
  </files>
  <action>
    Create llm_routing property test directory structure with fixtures:

    1. Create `backend/tests/property_tests/llm_routing/__init__.py` (empty file)

    2. Create `backend/tests/property_tests/llm_routing/conftest.py` with:
       - Import db_session from parent conftest
       - Fixture: `mock_llm_providers()` - mocks OpenAI, Anthropic, DeepSeek, Gemini providers
       - Fixture: `test_cognitive_classifier()` - creates CognitiveClassifier instance
       - Fixture: `test_cache_aware_router()` - creates CacheAwareRouter with mock cache
       - Fixture: `sample_prompts()` - returns st.text() strategy for prompt generation
       - Import Hypothesis strategies (st.text, st.integers, st.tuples, st.dictionaries)

    Follow pattern from `backend/tests/property_tests/governance/test_governance_invariants_property.py` lines 43-59:
    - HYPOTHESIS_SETTINGS_CRITICAL: max_examples=200 (tier mapping boundary conditions)
    - HYPOTHESIS_SETTINGS_STANDARD: max_examples=100 (routing consistency)
    - HYPOTHESIS_SETTINGS_IO: max_examples=50 (cache operations)

    DO NOT duplicate db_session - import from parent:
    `from tests.property_tests.conftest import db_session, DEFAULT_PROFILE, CI_PROFILE`
  </action>
  <verify>
    ls -la backend/tests/property_tests/llm_routing/ shows __init__.py and conftest.py
    grep -l "from tests.property_tests.conftest import" backend/tests/property_tests/llm_routing/conftest.py
    grep -l "mock_llm_providers\|test_cognitive_classifier" backend/tests/property_tests/llm_routing/conftest.py
  </verify>
  <done>
    llm_routing directory exists with fixtures imported from parent (no duplicate db_session)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create routing consistency property tests</name>
  <files>
    backend/tests/property_tests/llm_routing/test_routing_consistency.py
  </files>
  <action>
    Create `backend/tests/property_tests/llm_routing/test_routing_consistency.py` with 3-4 property tests:

    Test 1: `test_same_prompt_routes_to_same_provider`
    - Invariant: For any prompt, routing is deterministic (same prompt → same provider)
    - Strategy: st.text(min_size=1, max_size=10000) for prompts (unicode, various lengths)
    - Settings: HYPOTHESIS_SETTINGS_CRITICAL (max_examples=200)
    - Act: Route same prompt 3 times
    - Assert: All 3 routes return same provider

    Test 2: `test_routing_invariant_under_token_count_variance`
    - Invariant: Token count variations within same tier map to same provider
    - Strategy: st.integers(1000, 7000) for token_count (all in Standard tier range)
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Assert: All token counts in [1000, 7000] map to Standard tier

    Test 3: `test_routing_preserves_complexity_classification`
    - Invariant: Same semantic complexity → same tier regardless of exact token count
    - Strategy: st.dictionaries(st.text(), st.integers()) for complex prompts
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)

    Test 4 (optional): `test_provider_fallback_consistency`
    - Invariant: Provider failures trigger consistent fallback chain
    - Strategy: st.sampled_from(["openai", "anthropic", "deepseek"]) for failed_providers
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)

    DOCUMENT INVARIANTS FIRST (PROP-05 requirement):
    Each test docstring must have:
    - "PROPERTY:" - what invariant is being tested
    - "STRATEGY:" - what Hypothesis strategies generate inputs
    - "INVARIANT:" - formal statement of what must be true
    - "RADII:" - why N examples are sufficient
  </action>
  <verify>
    pytest backend/tests/property_tests/llm_routing/test_routing_consistency.py -v -k "test_same_prompt_routes_to_same_provider" passes
    grep -c "@pytest.mark.property" backend/tests/property_tests/llm_routing/test_routing_consistency.py >= 3
    grep -c "INVARIANT:" backend/tests/property_tests/llm_routing/test_routing_consistency.py >= 3
  </verify>
  <done>
    3-4 routing consistency property tests with invariant documentation (PROP-05 compliant)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create cognitive tier mapping and cache-aware routing tests</name>
  <files>
    backend/tests/property_tests/llm_routing/test_cognitive_tier_mapping.py
    backend/tests/property_tests/llm_routing/test_cache_aware_routing.py
  </files>
  <action>
    Create two test files with 7-8 property tests total:

    **test_cognitive_tier_mapping.py** (4 tests):
    Test 1: `test_tier_boundary_conditions`
    - Invariant: Token count at tier boundaries maps to correct tier
    - Strategy: st.one_of(st.just(1), st.just(1000), st.just(8000), st.just(32000), st.just(128000)) for boundary values
    - Use @example() decorator for exact boundary values
    - Settings: HYPOTHESIS_SETTINGS_CRITICAL (max_examples=200)
    - Assert:
      - 1-999 tokens → Micro tier
      - 1000-7999 tokens → Standard tier
      - 8000-31999 tokens → Versatile tier
      - 32000-127999 tokens → Heavy tier
      - 128000+ tokens → Complex tier

    Test 2: `test_tier_mapping_monotonic`
    - Invariant: Higher token count never maps to lower tier
    - Strategy: st.tuples(st.integers(1, 200000), st.integers(1, 200000)) for (tokens_a, tokens_b)
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Assert: If tokens_a < tokens_b, then tier(tokens_a) <= tier(tokens_b)

    Test 3: `test_semantic_complexity_increases_tier`
    - Invariant: High semantic complexity bumps tier (even with low token count)
    - Strategy: st.tuples(st.integers(1000, 5000), st.floats(0.0, 1.0)) for (token_count, complexity_score)
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Assert: complexity > 0.7 may increase tier by 1 level

    Test 4: `test_task_type_influences_tier`
    - Invariant: Certain task types force minimum tier (e.g., code_generation → Standard+)
    - Strategy: st.sampled_from(["chat", "code_generation", "analysis", "translation"]) for task_types
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)

    **test_cache_aware_routing.py** (3-4 tests):
    Test 1: `test_cached_prompts_skip_classification`
    - Invariant: Cached prompts use cached tier, don't reclassify
    - Strategy: st.text(min_size=1, max_size=5000) for prompts
    - Settings: HYPOTHESIS_SETTINGS_IO (max_examples=50, involves cache)
    - Act: Route prompt, cache result, route again
    - Assert: Second route uses cached tier (classification not called)

    Test 2: `test_cache_invalidation_propagates`
    - Invariant: Cache invalidation triggers reclassification on next route
    - Strategy: st.text(min_size=1, max_size=5000) for prompts
    - Settings: HYPOTHESIS_SETTINGS_IO (max_examples=50)
    - Act: Route prompt, invalidate cache, route again
    - Assert: Second route reclassifies (fresh tier assigned)

    Test 3: `test_cache_key_consistency`
    - Invariant: Same prompt hash always produces same cache key
    - Strategy: st.text(min_size=1, max_size=5000) for prompts
    - Settings: HYPOTHESIS_SETTINGS_CRITICAL (max_examples=200)
    - Assert: hash(prompt) is invariant (same input → same hash)

    Test 4 (optional): `test_cache_size_bounds`
    - Invariant: Cache size respects configured limits (evicts oldest when full)
    - Strategy: st.lists(st.text(), min_size=0, max_size=1000) for prompt lists
    - Settings: HYPOTHESIS_SETTINGS_IO (max_examples=50)

    DOCUMENT INVARIANTS FIRST in each test docstring (PROP-05 requirement).
  </action>
  <verify>
    pytest backend/tests/property_tests/llm_routing/test_cognitive_tier_mapping.py -v passes
    pytest backend/tests/property_tests/llm_routing/test_cache_aware_routing.py -v passes
    grep -c "@pytest.mark.property" backend/tests/property_tests/llm_routing/test_*.py >= 7
    grep -c "@example(" backend/tests/property_tests/llm_routing/test_cognitive_tier_mapping.py >= 1
  </verify>
  <done>
    7-8 tier mapping + cache-aware routing property tests with boundary condition testing
  </done>
</task>

</tasks>

<verification>
## Overall Phase Checks

After all tasks complete, verify:

```bash
# 1. All new tests run
pytest backend/tests/property_tests/llm_routing/ -v

# 2. Property tests have correct marker
pytest backend/tests/property_tests/llm_routing/ -v -m property

# 3. Invariant documentation present (PROP-05)
grep -r "INVARIANT:" backend/tests/property_tests/llm_routing/ | wc -l  # Should be 10+

# 4. No duplicate db_session fixture
! grep -q "def db_session" backend/tests/property_tests/llm_routing/conftest.py

# 5. Tests use imported fixtures
grep -q "from tests.property_tests.conftest import" backend/tests/property_tests/llm_routing/conftest.py

# 6. Boundary values tested with @example()
grep -c "@example(" backend/tests/property_tests/llm_routing/test_cognitive_tier_mapping.py
```

Expected results:
- 10-12 property tests across 3 files
- All tests have invariant documentation (PROPERTY, STRATEGY, INVARIANT, RADII)
- No duplicate fixtures (reuse from parent conftest.py)
- Boundary conditions tested with @example() decorator
</verification>

<success_criteria>
1. 10-12 property tests created covering LLM routing consistency, cognitive tier mapping, cache-aware behavior
2. All tests follow invariant-first pattern (PROP-05): invariant documented BEFORE test code
3. Tests use correct max_examples: 200 (CRITICAL for boundaries), 100 (STANDARD), 50 (IO-bound for cache)
4. Tests import fixtures from parent conftest.py (no duplicate db_session)
5. All tests pass with `pytest backend/tests/property_tests/llm_routing/ -v`
6. INVARIANTS.md updated with new invariants (add 4-5 entries for LLM routing)
</success_criteria>

<output>
After completion, create `.planning/phases/238-property-based-testing-expansion/238-02-SUMMARY.md` with:
- Tests created: List all test files and test counts
- Invariants validated: Summary of what each test covers
- Bugs found: Any invariant violations discovered (e.g., boundary condition bugs)
- Next steps: Link to 238-03 (episodic memory tests)
</output>
