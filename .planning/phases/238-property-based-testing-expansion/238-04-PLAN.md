---
phase: 238-property-based-testing-expansion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/property_tests/api_contracts/__init__.py
  - backend/tests/property_tests/api_contracts/conftest.py
  - backend/tests/property_tests/api_contracts/test_malformed_json.py
  - backend/tests/property_tests/api_contracts/test_oversized_payloads.py
  - backend/tests/property_tests/governance/test_authorization_invariants.py
  - backend/tests/property_tests/governance/test_governance_cache_invariants.py
autonomous: true

must_haves:
  truths:
    - "Malformed JSON returns 400/422 (not 500 Internal Server Error)"
    - "Oversized payloads return 413 Payload Too Large (not OOM or crash)"
    - "API responses conform to OpenAPI schema (valid types, required fields present)"
    - "Authorization checks are monotonic (higher maturity ≥ lower maturity permissions)"
    - "Cache invalidation propagates correctly (stale entries are removed)"
  artifacts:
    - path: "backend/tests/property_tests/api_contracts/test_malformed_json.py"
      provides: "Malformed JSON invariant tests (API handles invalid JSON gracefully)"
      min_lines: 120
      contains: "@pytest.mark.property", "@given", "malformed", "json"
    - path: "backend/tests/property_tests/api_contracts/test_oversized_payloads.py"
      provides: "Oversized payload invariant tests (large payloads rejected with 413)"
      min_lines: 100
      contains: "@pytest.mark.property", "@given", "oversized", "payload"
    - path: "backend/tests/property_tests/governance/test_authorization_invariants.py"
      provides: "Authorization monotonicity invariant tests (higher maturity ≥ lower permissions)"
      min_lines: 130
      contains: "@pytest.mark.property", "@given", "authorization", "monotonic"
    - path: "backend/tests/property_tests/governance/test_governance_cache_invariants.py"
      provides: "Cache invalidation invariant tests (stale entries removed correctly)"
      min_lines: 120
      contains: "@pytest.mark.property", "@given", "cache", "invalidation"
  key_links:
    - from: "backend/tests/property_tests/api_contracts/test_malformed_json.py"
      to: "backend/core/atom_agent_endpoints.py"
      via: "FastAPI endpoints with @app.post decorators"
      pattern: "from fastapi import.*status"
    - from: "backend/tests/property_tests/governance/test_authorization_invariants.py"
      to: "backend/core/agent_governance_service.py"
      via: "AgentGovernanceService.check_permission()"
      pattern: "from core.agent_governance_service import"
    - from: "backend/tests/property_tests/api_contracts/conftest.py"
      to: "backend/tests/e2e_ui/fixtures/auth_fixtures.py"
      via: "Import authenticated_client for API testing"
      pattern: "from tests.e2e_ui.fixtures.auth_fixtures import"
    - from: "backend/tests/property_tests/governance/test_governance_cache_invariants.py"
      to: "backend/core/governance_cache.py"
      via: "GovernanceCache.invalidate()"
      pattern: "from core.governance_cache import"
---

<objective>
Create API contract fuzzing tests and expand governance invariants with authorization monotonicity and cache invalidation.

**Purpose:** PROP-02 requires API contract invariants (malformed JSON, oversized payloads, response validation). PROP-01 requires governance invariants expansion. These tests prevent crashes from malformed input and ensure permissions are enforced correctly.

**Output:** 4 new test files with 10-12 property tests covering malformed JSON handling, oversized payload rejection, authorization monotonicity, and cache invalidation propagation.

**Why this matters:** API fuzzing finds crashes that example-based tests miss. A single malformed JSON payload causing 500 error = potential DoS vulnerability. Authorization monotonicity violations = permission escalation bugs.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/238-property-based-testing-expansion/238-RESEARCH.md
@backend/tests/property_tests/INVARIANTS.md
@backend/tests/property_tests/conftest.py
@backend/tests/bug_discovery/TEMPLATES/PROPERTY_TEMPLATE.md
@backend/tests/e2e_ui/fixtures/auth_fixtures.py
@backend/core/atom_agent_endpoints.py
@backend/core/agent_governance_service.py
@backend/core/governance_cache.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API contracts test infrastructure</name>
  <files>
    backend/tests/property_tests/api_contracts/__init__.py
    backend/tests/property_tests/api_contracts/conftest.py
  </files>
  <action>
    Create api_contracts property test directory structure with fixtures:

    1. Create `backend/tests/property_tests/api_contracts/__init__.py` (empty file)

    2. Create `backend/tests/property_tests/api_contracts/conftest.py` with:
       - Import authenticated_client from e2e_ui fixtures (10-100x faster than UI login)
       - Fixture: `api_client(authenticated_client)` - wraps FastAPI TestClient
       - Fixture: `malformed_json_strategy()` - returns st.one_of() for malformed JSON generation
       - Fixture: `oversized_payload_strategy()` - returns st.integers() for payload size
       - Import Hypothesis strategies (st.text, st.integers, st.lists, st.dictionaries, st.none)

    Follow fixture reuse pattern from `backend/tests/bug_discovery/FIXTURE_REUSE_GUIDE.md` lines 117-174:
    - Import `authenticated_client` from `tests.e2e_ui.fixtures.auth_fixtures`
    - DO NOT create new auth fixtures - reuse existing ones

    Follow pattern from `backend/tests/property_tests/governance/test_governance_invariants_property.py`:
    - HYPOTHESIS_SETTINGS_CRITICAL: max_examples=200 (response validation)
    - HYPOTHESIS_SETTINGS_STANDARD: max_examples=100 (malformed JSON)
    - HYPOTHESIS_SETTINGS_IO: max_examples=50 (oversized payloads, IO-bound)
  </action>
  <verify>
    ls -la backend/tests/property_tests/api_contracts/ shows __init__.py and conftest.py
    grep -l "from tests.e2e_ui.fixtures.auth_fixtures import" backend/tests/property_tests/api_contracts/conftest.py
    grep -l "authenticated_client" backend/tests/property_tests/api_contracts/conftest.py
  </verify>
  <done>
    api_contracts directory exists with fixtures imported from e2e_ui (no duplicate auth fixtures)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create malformed JSON property tests</name>
  <files>
    backend/tests/property_tests/api_contracts/test_malformed_json.py
  </files>
  <action>
    Create `backend/tests/property_tests/api_contracts/test_malformed_json.py` with 3-4 property tests:

    Test 1: `test_api_rejects_malformed_json_gracefully`
    - Invariant: Malformed JSON returns 400/422 (not 500 Internal Server Error)
    - Strategy: st.one_of(
        st.text(min_size=0, max_size=10000),  # Random text (not valid JSON)
        st.dictionaries(st.text(), st.none()),  # Dict with None values (invalid JSON)
        st.just('{"invalid": json}'),  # Specifically malformed JSON
        st.just('null'), st.just(''), st.just('[]'), st.just('{}')
      )
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100, API calls are IO-bound)
    - Act: POST to /api/v1/agents/execute with malformed JSON
    - Assert: response.status_code in [400, 422, 413] (client error, not server error)
    - Assert: response.status_code != 500 (never crash with 500)

    Test 2: `test_api_handles_invalid_utf8`
    - Invariant: Invalid UTF-8 sequences return 400 (not crash)
    - Strategy: st.binary() with invalid UTF-8 sequences
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Assert: response.status_code in [400, 422]

    Test 3: `test_api_handles_null_bytes_and_injection`
    - Invariant: Null bytes and injection attempts are sanitized
    - Strategy: st.text() with null bytes (\x00) and SQL injection patterns
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Assert: No 500 errors, input is sanitized

    Test 4 (optional): `test_api_response_conforms_to_openapi_schema`
    - Invariant: Valid requests return responses conforming to OpenAPI schema
    - Strategy: st.dictionaries(st.text(), st.text()) for valid JSON payloads
    - Settings: HYPOTHESIS_SETTINGS_CRITICAL (max_examples=200)
    - Assert: Response has valid content-type, required fields present

    DOCUMENT INVARIANTS FIRST (PROP-05 requirement):
    Each test docstring must have:
    - "PROPERTY:" - what invariant is being tested
    - "STRATEGY:" - what Hypothesis strategies generate inputs
    - "INVARIANT:" - formal statement of what must be true
    - "RADII:" - why N examples are sufficient
  </action>
  <verify>
    pytest backend/tests/property_tests/api_contracts/test_malformed_json.py -v -k "test_api_rejects_malformed_json_gracefully" passes
    grep -c "@pytest.mark.property" backend/tests/property_tests/api_contracts/test_malformed_json.py >= 3
    grep -c "INVARIANT:" backend/tests/property_tests/api_contracts/test_malformed_json.py >= 3
    grep "status_code != 500\|status_code in \[400" backend/tests/property_tests/api_contracts/test_malformed_json.py
  </verify>
  <done>
    3-4 malformed JSON property tests with invariant documentation (PROP-05 compliant)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create oversized payload and governance expansion tests</name>
  <files>
    backend/tests/property_tests/api_contracts/test_oversized_payloads.py
    backend/tests/property_tests/governance/test_authorization_invariants.py
    backend/tests/property_tests/governance/test_governance_cache_invariants.py
  </files>
  <action>
    Create three test files with 7-8 property tests total:

    **test_oversized_payloads.py** (2-3 tests):
    Test 1: `test_api_rejects_oversized_payloads`
    - Invariant: Oversized payloads return 413 Payload Too Large (not OOM or crash)
    - Strategy: st.integers(min_value=1_000_000, max_value=100_000_000) for payload_size
    - Settings: HYPOTHESIS_SETTINGS_IO (max_examples=50, slow network operations)
    - Act: POST to /api/v1/agents/execute with {"data": "x" * payload_size}
    - Assert: response.status_code in [400, 413] (not 500)

    Test 2: `test_api_handles_empty_payloads`
    - Invariant: Empty payloads are handled gracefully (not crash)
    - Strategy: st.one_of(st.just(''), st.just('{}'), st.just('[]'))
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Assert: response.status_code in [400, 422] (not 500)

    Test 3 (optional): `test_api_handles_deeply_nested_json`
    - Invariant: Deeply nested JSON structures don't cause stack overflow
    - Strategy: st.recursive(st.none(), lambda s: st.dictionaries(st.text(), s), max_leaves=50)
    - Settings: HYPOTHESIS_SETTINGS_IO (max_examples=50)

    **test_authorization_invariants.py** (3-4 tests):
    Test 1: `test_authorization_monotonic_with_maturity`
    - Invariant: Higher maturity levels have ≥ permissions of lower levels
    - Strategy: st.tuples(
        sampled_from(["STUDENT", "INTERN", "SUPERVISED", "AUTONOMOUS"]),
        sampled_from(["STUDENT", "INTERN", "SUPERVISED", "AUTONOMOUS"]),
        sampled_from([1, 2, 3, 4])  # action complexity
      )
    - Settings: HYPOTHESIS_SETTINGS_CRITICAL (max_examples=200, explores all 4x4x4=64 combinations)
    - Assert: If maturity_a > maturity_b and complexity permitted by a, then permitted by b

    Test 2: `test_permission_check_idempotent`
    - Invariant: Permission checks return same result for same inputs
    - Strategy: st.tuples(st.uuids(), st.text()) for (agent_id, action)
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Act: Check permission twice
    - Assert: result_1 == result_2

    Test 3: `test_authorization_denied_for_insufficient_maturity`
    - Invariant: Actions exceeding maturity capability are denied
    - Strategy: st.tuples(
        sampled_from(["STUDENT", "INTERN"]),
        sampled_from([3, 4])  # High complexity actions
      )
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)
    - Assert: Permission denied for insufficient maturity

    Test 4 (optional): `test_cross_tenant_authorization_isolation`
    - Invariant: Agents cannot access resources from other tenants
    - Strategy: st.tuples(st.uuids(), st.uuids()) for (agent_id, other_tenant_id)
    - Settings: HYPOTHESIS_SETTINGS_STANDARD (max_examples=100)

    **test_governance_cache_invariants.py** (2-3 tests):
    Test 1: `test_cache_invalidation_propagates`
    - Invariant: Cache invalidation removes stale entries
    - Strategy: st.lists(st.uuids(), min_size=10, max_size=100) for agent_ids
    - Settings: HYPOTHESIS_SETTINGS_IO (max_examples=50, DB operations)
    - Act: Warm cache, update agent, invalidate cache
    - Assert: Cache returns fresh data (not stale)

    Test 2: `test_cache_consistency_with_database`
    - Invariant: Cached values match database values
    - Strategy: st.integers(min_value=10, max_value=100) for agent_count
    - Settings: HYPOTHESIS_SETTINGS_IO (max_examples=50)
    - Act: Cache agents, update DB, compare
    - Assert: Cached value == DB value (after invalidation)

    Test 3 (optional): `test_cache_hit_rate_above_threshold`
    - Invariant: Cache hit rate > 90% for repeated lookups
    - Strategy: st.lists(st.uuids(), min_size=50, max_size=200) for repeated lookups
    - Settings: HYPOTHESIS_SETTINGS_IO (max_examples=50)
    - Assert: cache_hits / total_lookups > 0.90

    DOCUMENT INVARIANTS FIRST in each test docstring (PROP-05 requirement).
  </action>
  <verify>
    pytest backend/tests/property_tests/api_contracts/test_oversized_payloads.py -v passes
    pytest backend/tests/property_tests/governance/test_authorization_invariants.py -v passes
    pytest backend/tests/property_tests/governance/test_governance_cache_invariants.py -v passes
    grep -c "@pytest.mark.property" backend/tests/property_tests/api_contracts/test_oversized_payloads.py backend/tests/property_tests/governance/test_authorization_invariants.py backend/tests/property_tests/governance/test_governance_cache_invariants.py | awk '{sum+=$1} END {print sum}'  # Should be 7+
  </verify>
  <done>
    7-8 oversized payload + authorization + cache invalidation property tests
  </done>
</task>

</tasks>

<verification>
## Overall Phase Checks

After all tasks complete, verify:

```bash
# 1. All new tests run
pytest backend/tests/property_tests/api_contracts/ -v
pytest backend/tests/property_tests/governance/test_authorization_invariants.py -v
pytest backend/tests/property_tests/governance/test_governance_cache_invariants.py -v

# 2. Property tests have correct marker
pytest backend/tests/property_tests/api_contracts/ -v -m property
pytest backend/tests/property_tests/governance/test_authorization_invariants.py -v -m property

# 3. Invariant documentation present (PROP-05)
grep -r "INVARIANT:" backend/tests/property_tests/api_contracts/ backend/tests/property_tests/governance/test_authorization_invariants.py backend/tests/property_tests/governance/test_governance_cache_invariants.py | wc -l  # Should be 10+

# 4. Reuse existing auth fixtures
grep -q "from tests.e2e_ui.fixtures.auth_fixtures import" backend/tests/property_tests/api_contracts/conftest.py

# 5. Malformed JSON tests check for 500 errors
grep -q "status_code != 500\|!= 500" backend/tests/property_tests/api_contracts/test_malformed_json.py
```

Expected results:
- 10-12 property tests across 4 files
- All tests have invariant documentation (PROPERTY, STRATEGY, INVARIANT, RADII)
- Auth fixtures reused from e2e_ui (no duplicate fixtures)
- Malformed JSON tests verify 500 errors never occur
</verification>

<success_criteria>
1. 10-12 property tests created covering malformed JSON, oversized payloads, authorization monotonicity, cache invalidation
2. All tests follow invariant-first pattern (PROP-05): invariant documented BEFORE test code
3. Tests use correct max_examples: 200 (CRITICAL), 100 (STANDARD), 50 (IO-bound)
4. Tests reuse existing auth fixtures from e2e_ui (FIXTURE_REUSE_GUIDE.md compliance)
5. Malformed JSON tests verify no 500 errors (client errors only)
6. All tests pass with `pytest backend/tests/property_tests/api_contracts/ -v` and governance tests
7. INVARIANTS.md updated with new invariants (add 5-6 entries for API contracts and governance)
</success_criteria>

<output>
After completion, create `.planning/phases/238-property-based-testing-expansion/238-04-SUMMARY.md` with:
- Tests created: List all test files and test counts
- Invariants validated: Summary of what each test covers
- Bugs found: Any invariant violations discovered (e.g., 500 errors on malformed JSON, cache staleness)
- Next steps: Link to 238-05 (state machine and security tests)
</output>
