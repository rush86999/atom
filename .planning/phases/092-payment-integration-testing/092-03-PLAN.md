---
phase: 092-payment-integration-testing
plan: 03
type: execute
wave: 2
depends_on: ["092-01"]
files_modified:
  - backend/tests/payment_integration/test_idempotency.py
  - backend/tests/property_tests/payment/test_payment_idempotency_invariants.py
  - backend/integrations/stripe_service.py
autonomous: true

must_haves:
  truths:
    - Idempotency keys prevent duplicate charges (same key returns cached response)
    - Idempotency key generation uses UUID v4 or business-derived keys (no collisions)
    - Idempotency keys are unique per operation (charge, refund, subscription)
    - Idempotency replay returns cached response with Idempotent-Replayed header
    - Property tests validate no key collisions across 1000+ generated keys
  artifacts:
    - path: "backend/tests/payment_integration/test_idempotency.py"
      provides: "Idempotency integration tests for Stripe operations"
      min_lines: 200
    - path: "backend/tests/property_tests/payment/__init__.py"
      provides: "Payment property tests package marker"
    - path: "backend/tests/property_tests/payment/test_payment_idempotency_invariants.py"
      provides: "Hypothesis property tests for idempotency invariants"
      min_lines: 250
    - path: "backend/integrations/stripe_service.py"
      provides: "Enhanced with idempotency key generation"
      contains: "generate_idempotency_key"
  key_links:
    - from: "backend/tests/payment_integration/test_idempotency.py"
      to: "backend/integrations/stripe_service.py"
      via: "idempotency key generation function"
      pattern: "from integrations.stripe_service import generate_idempotency_key"
    - from: "backend/tests/property_tests/payment/test_payment_idempotency_invariants.py"
      to: "backend/tests/fixtures/payment_fixtures.py"
      via: "payment fixtures for test data"
      pattern: "from tests.fixtures.payment_fixtures import StripeChargeFactory"
    - from: "backend/tests/property_tests/payment/test_payment_idempotency_invariants.py"
      to: "backend/tests/fixtures/decimal_fixtures.py"
      via: "Decimal strategies for amounts"
      pattern: "from tests.fixtures.decimal_fixtures import money_strategy"
---

<objective>
Idempotency validation preventing duplicate charges and lost payments through UUID-based key generation, property-based invariant testing, and integration tests confirming replay behavior.

Purpose: Idempotency is CRITICAL for payment systems. Network retries MUST NOT cause duplicate charges, and lost requests MUST be recoverable. Stripe idempotency keys are valid for 24 hours and prevent double-charging when clients retry failed requests.

Output: Idempotency key generator, integration tests for replay behavior, and Hypothesis property tests validating key uniqueness across 1000+ operations.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/092-payment-integration-testing/092-01-SUMMARY.md
@.planning/phases/092-payment-integration-testing/092-RESEARCH.md
@backend/core/decimal_utils.py
@backend/tests/fixtures/payment_fixtures.py
@backend/tests/fixtures/decimal_fixtures.py
@backend/tests/property_tests/financial/test_financial_invariants.py
@backend/integrations/stripe_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add idempotency key generation to stripe_service.py</name>
  <files>
  backend/integrations/stripe_service.py
  </files>
  <action>
  Add idempotency key generation to StripeService:

  Modify `backend/integrations/stripe_service.py`:
  1. Add imports: `import uuid`, `from typing import Optional`, `from datetime import datetime`

  2. Add `generate_idempotency_key()` static method:
     ```python
     @staticmethod
     def generate_idempotency_key(operation_type: str, *identifier_parts) -> str:
         """
         Generate unique idempotency key for Stripe operations.

         Format: {operation_type}_{uuid8}_{identifiers}_timestamp
         Example: charge_a1b2c3d4_cust123_order456_1700000000

         Args:
             operation_type: Type of operation (charge, refund, subscription)
             *identifier_parts: Business identifiers (customer_id, order_id, etc.)

         Returns:
             Unique idempotency key (string, <255 chars per Stripe limits)
         """
         # UUID v4 for base uniqueness (8 hex chars = 32 bits entropy)
         uuid_suffix = uuid.uuid4().hex[:8]

         # Business identifiers for cross-request uniqueness
         identifier = "_".join(str(p) for p in identifier_parts if p) if identifier_parts else "generic"

         # Timestamp for temporal uniqueness (prevents replay across sessions)
         timestamp = int(datetime.now().timestamp())

         # Combine: operation + uuid + identifiers + timestamp
         key = f"{operation_type}_{uuid_suffix}_{identifier}_{timestamp}"

         # Stripe limits idempotency keys to 255 characters
         if len(key) > 255:
             # Truncate identifier if too long
             key = f"{operation_type}_{uuid_suffix}_{timestamp}"

         return key
     ```

  3. Update `create_payment()` method to accept optional `idempotency_key` parameter:
     - Add parameter: `idempotency_key: Optional[str] = None`
     - If not provided, generate using: `self.generate_idempotency_key("charge", customer, description)`
     - Add to request data: `data["idempotency_key"] = idempotency_key`

  4. Add similar idempotency support to `create_subscription()`, `create_customer()`, etc.

  IMPORTANT: Never generate idempotency keys automatically for GET requests (idempotent by definition). Only POST requests creating resources need keys.
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend python -c "from integrations.stripe_service import StripeService; k = StripeService.generate_idempotency_key('charge', 'cust123', 'order456'); print(k, len(k))"
  Expected: Prints key like "charge_a1b2c3d4_cust123_order456_1700000000", length < 255
  </verify>
  <done>
  stripe_service.py has generate_idempotency_key() method using UUID v4 + business identifiers, create_payment() accepts idempotency_key parameter
  </done>
</task>

<task type="auto">
  <name>Task 2: Create idempotency integration tests</name>
  <files>
  backend/tests/payment_integration/test_idempotency.py
  backend/tests/property_tests/payment/__init__.py
  </files>
  <action>
  Create integration tests for idempotency behavior:

  **backend/tests/payment_integration/test_idempotency.py** (250 lines):
  - Import pytest, uuid, stripe
  - Import stripe_mock_server fixture, StripeChargeFactory from payment_fixtures
  - Import generate_idempotency_key from stripe_service

  Test class `TestIdempotencyKeyGeneration`:
  1. `test_uuid_based_keys_are_unique`: Generate 100 keys, verify all unique using set()
  2. `test_business_derived_keys_include_identifiers`: Verify customer_id, order_id in key
  3. `test_keys_under_255_chars`: Generate keys with long identifiers, verify length < 255
  4. `test_keys_different_for_different_operations`: Same identifiers + different operation = different keys

  Test class `TestIdempotencyReplayBehavior`:
  5. `test_same_key_returns_same_charge`: Create charge with idempotency key, replay same key, verify same charge ID
  6. `test_different_key_creates_new_charge`: Create 2 charges with different keys, verify 2 different charge IDs
  7. `test_replayed_request_has_header`: Replay charge, verify response has Idempotent-Replayed: true header
  8. `test_replay_with_different_params_fails`: Create charge, replay with different amount, verify Stripe returns original (ignores new params)

  Test class `TestIdempotencyExpiry`:
  9. `test_keys_valid_for_24_hours`: Document that Stripe idempotency keys expire after 24 hours (test uses comment to note this)
  10. `test_key_reuse_after_expiry`: Document behavior after 24-hour window (Stripe treats as new request)

  Test class `TestIdempotencyInProductionFlow`:
  11. `test_payment_retry_with_same_key`: Simulate network failure + retry, verify only 1 charge created
  12. `test_concurrent_requests_same_key`: Send 2 concurrent requests with same key, verify only 1 charge (stripe-mock should handle this)
  13. `test_refund_idempotency`: Create refund with idempotency key, verify replay behavior

  Use stripe-mock server from Plan 01 for all tests. Use StripeChargeFactory for test data.
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/payment_integration/test_idempotency.py -v`
  Expected: All 13 tests pass, idempotency replay behavior verified
  </verify>
  <done>
  13 integration tests cover idempotency key generation, replay behavior, expiry, and production flow scenarios
  </done>
</task>

<task type="auto">
  <name>Task 3: Create Hypothesis property tests for idempotency invariants</name>
  <files>
  backend/tests/property_tests/payment/test_payment_idempotency_invariants.py
  </files>
  <action>
  Create Hypothesis property tests for idempotency invariants:

  **backend/tests/property_tests/payment/test_payment_idempotency_invariants.py** (300 lines):
  - Import pytest, hypothesis (given, strategies as st, settings)
  - Import uuid
  - Import generate_idempotency_key from stripe_service
  - Import StripeChargeFactory from payment_fixtures
  - Import money_strategy from decimal_fixtures

  Test class `TestIdempotencyKeyUniqueness`:
  1. `@given(st.text(min_size=5, max_size=20), st.integers(min_value=1, max_value=1000))`
     `test_uuid_prevents_collisions`: Generate idempotency keys for same operation + different order_ids, verify no collisions (set size = list size)

  2. `@given(st.lists(st.integers(min_value=1, max_value=10000), min_size=10, max_size=100))`
     `test_business_derived_keys_unique`: Generate keys for list of order IDs, verify all unique

  3. `@given(st.text(min_size=1, max_value=50), st.integers(min_value=1, max_value=1000))`
     `test_long_identifiers_truncated_safely`: Generate keys with long identifiers, verify length < 255 and still unique

  Test class `TestIdempotencyInPaymentOperations`:
  4. `@given(amount=money_strategy())`
     `test_charge_idempotency_for_all_amounts`: Create charge with idempotency key for various amounts, verify replay returns same charge

  5. `@given(amount=money_strategy(), customer_id=st.text(min_size=5, max_size=20))`
     `test_charge_different_keys_different_charges`: Create charges with different keys, verify different charge IDs

  6. `@given(st.integers(min_value=1, max_value=50))`
     `test_batch_charges_unique`: Create N charges with unique keys, verify N unique charge IDs

  Test class `TestIdempotencyFailureModes`:
  7. `@given(st.text(min_size=5, max_size=20))`
     `test_network_retry_preserves_idempotency`: Simulate retry after network error, verify same charge ID

  8. `@given(st.integers(min_value=1, max_value=10))`
     `test_concurrent_requests_no_double_charge`: Simulate concurrent requests with same key, verify only 1 charge

  Test class `TestIdempotencyAcrossOperations`:
  9. `@given(operation_type=st.sampled_from(['charge', 'refund', 'subscription']))`
     `test_different_operation_types_different_keys`: Verify same identifiers + different operation = different keys

  10. `@given(st.lists(st.integers(min_value=1, max_value=1000), min_size=2, max_size=10))`
      `test_batch_operations_all_unique`: Generate keys for multiple operations, verify all unique

  Test class `TestIdempotencyEdgeCases`:
  11. `test_empty_identifiers_generates_valid_key`: Generate key with no identifiers, verify valid
  12. `test_unicode_identifiers_handled`: Generate key with unicode characters, verify valid ASCII
  13. `test_timestamp_ensures_temporal_uniqueness`: Generate keys rapidly, verify timestamp prevents collision

  Use @settings(max_examples=100) for property tests to find edge cases. Use assume() to filter invalid inputs.
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/property_tests/payment/test_payment_idempotency_invariants.py -v`
  Expected: All 13 property tests pass, no collisions found across 1000+ examples
  </verify>
  <done>
  13 Hypothesis property tests validate idempotency invariants (uniqueness, replay behavior, edge cases) across 1000+ auto-generated examples
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:
1. Idempotency keys use UUID v4 + business identifiers (no collisions across 1000+ keys)
2. Same idempotency key returns cached response (Idempotent-Replayed header present)
3. Different idempotency keys create different charges (no cross-contamination)
4. Property tests use Hypothesis @given decorator with appropriate strategies
5. All monetary values use Decimal from decimal_utils (no float types)
</verification>

<success_criteria>
1. pytest tests/payment_integration/test_idempotency.py passes (13+ integration tests)
2. pytest tests/property_tests/payment/test_payment_idempotency_invariants.py passes (13+ property tests)
3. Idempotency key length < 255 characters for all test cases
4. No key collisions across 1000+ generated keys (set size = list size)
5. Stripe-mock server confirms idempotency replay behavior (same charge ID)
</success_criteria>

<output>
After completion, create `.planning/phases/092-payment-integration-testing/092-03-SUMMARY.md` with:
- Idempotency key generation algorithm (UUID v4 + business + timestamp)
- Test results (26 total tests: 13 integration + 13 property)
- Collision analysis (0 collisions across N examples)
- Stripe idempotency behavior documentation
</output>
