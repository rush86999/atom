---
phase: 092-payment-integration-testing
plan: 05
type: execute
wave: 4
depends_on: ["092-01", "092-02", "092-03", "092-04"]
files_modified:
  - backend/tests/payment_integration/test_payment_flows.py
  - backend/tests/payment_integration/__init__.py
  - backend/tests/payment_integration/conftest.py
autonomous: true

must_haves:
  truths:
    - Complete charge flow works (create charge → webhook → accounting ledger entry)
    - Refund flow works correctly (original charge → refund → ledger credit)
    - Subscription flow works (create → invoice payment → ledger entries)
    - Invoice payment flow works (create invoice → pay → reconcile)
    - All payment flows use Decimal precision (no float types)
  artifacts:
    - path: "backend/tests/payment_integration/test_payment_flows.py"
      provides: "End-to-end payment flow integration tests"
      min_lines: 400
    - path: "backend/tests/payment_integration/conftest.py"
      provides: "Enhanced with payment flow fixtures"
      exports: ["payment_flow_test_client", "setup_customer", "setup_subscription"]
  key_links:
    - from: "backend/tests/payment_integration/test_payment_flows.py"
      to: "backend/integrations/stripe_service.py"
      via: "payment service calls"
      pattern: "stripe_service.create_payment|stripe_service.create_subscription"
    - from: "backend/tests/payment_integration/test_payment_flows.py"
      to: "backend/integrations/stripe_routes.py"
      via: "webhook endpoint"
      pattern: "client.post.*webhooks"
    - from: "backend/tests/payment_integration/test_payment_flows.py"
      to: "backend/core/decimal_utils.py"
      via: "Decimal precision for all amounts"
      pattern: "from core.decimal_utils import to_decimal"
---

<objective>
End-to-end integration tests validating complete payment flows (charges, refunds, subscriptions, invoices) with webhook processing and accounting ledger integration.

Purpose: Individual component tests (mock server, idempotency, race conditions) verify parts of the system work. Flow integration tests verify the COMPLETE payment journey from API call through webhook processing to accounting ledger entry, ensuring all components work together correctly.

Output: 20+ end-to-end flow tests covering charges, refunds, subscriptions, invoices, and edge cases with full accounting integration.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/092-payment-integration-testing/092-01-SUMMARY.md
@.planning/phases/092-payment-integration-testing/092-02-SUMMARY.md
@.planning/phases/092-payment-integration-testing/092-03-SUMMARY.md
@.planning/phases/092-payment-integration-testing/092-04-SUMMARY.md
@.planning/phases/092-payment-integration-testing/092-RESEARCH.md
@backend/core/decimal_utils.py
@backend/tests/fixtures/payment_fixtures.py
@backend/tests/payment_integration/conftest.py
@backend/integrations/stripe_service.py
@backend/integrations/stripe_routes.py
@backend/accounting/ingestion.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance conftest.py with payment flow fixtures</name>
  <files>
  backend/tests/payment_integration/conftest.py
  </files>
  <action>
  Enhance existing conftest.py with payment flow specific fixtures:

  Modify `backend/tests/payment_integration/conftest.py` (add 100 lines to existing 150):
  1. Add `@pytest.fixture setup_customer(db_session)`:
     - Creates test customer in database
     - Returns customer_id and customer object
     - Use factory_boy pattern or direct ORM insert

  2. Add `@pytest.fixture setup_payment_method(db_session, setup_customer)`:
     - Creates test payment method (card token)
     - Returns payment_method_id
     - Uses Stripe test card token "tok_visa"

  3. Add `@pytest.fixture setup_subscription(db_session, setup_customer)`:
     - Creates test subscription with price
     - Returns subscription_id and price_id
     - Uses StripeSubscriptionFactory

  4. Add `@pytest.fixture payment_flow_test_client(db_session)`:
     - Returns FastAPI TestClient configured for payment flows
     - Pre-configured with test authentication headers
     - Pre-seeded with test customer and payment method

  5. Add `@pytest.fixture accounting_ledger(db_session)`:
     - Returns accessor for accounting ledger entries
     - Function to query ledger by transaction_id
     - Function to verify debit/credit balance

  6. Add `@pytest.fixture webhook_payload_builder()`:
     - Returns builder function for webhook payloads
     - Supports all event types (payment_intent, invoice, subscription)
     - Includes signature generation

  IMPORTANT: All fixtures use Decimal from decimal_utils for monetary values. All fixtures clean up after themselves (yield pattern with rollback).
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/payment_integration/test_payment_flows.py::test_charge_flow --fixtures`
  Expected: Shows all fixtures available, setup_customer creates customer
  </verify>
  <done>
  conftest.py has fixtures for customer setup, payment methods, subscriptions, accounting ledger access, and webhook payload building
  </done>
</task>

<task type="auto">
  <name>Task 2: Create charge flow integration tests</name>
  <files>
  backend/tests/payment_integration/test_payment_flows.py
  </files>
  <action>
  Create charge flow integration tests (part 1 of 2):

  **backend/tests/payment_integration/test_payment_flows.py** (450 lines total, this task creates first 250 lines):
  - Import pytest, asyncio, freezegun
  - Import stripe, stripe_service
  - Import fixtures from conftest and payment_fixtures
  - Import to_decimal from decimal_utils
  - Import TransactionIngestor from accounting.ingestion

  Test class `TestChargeFlow`:
  1. `test_complete_charge_flow`: Create charge via API → receive payment_intent.succeeded webhook → verify ledger entry created
  2. `test_charge_with_customer`: Create charge with customer_id → verify customer charged → verify ledger
  3. `test_charge_with_metadata`: Create charge with metadata (order_id) → verify metadata in webhook → verify ledger
  4. `test_charge_failed_flow`: Create charge that fails → receive payment_intent.failed webhook → verify ledger has NO entry
  5. `test_charge_partial_refund`: Create charge → refund partial amount → verify ledger has credit entry
  6. `test_charge_full_refund`: Create charge → refund full amount → verify ledger shows net zero

  Test class `TestChargeEdgeCases`:
  7. `test_zero_amount_charge`: Attempt $0 charge → verify rejected or handled
  8. `test_large_amount_charge`: Create $9999.99 charge → verify precision maintained
  9. `test_fractional_cent_rounding`: Create $10.005 charge → verify rounds to $10.01 correctly
  10. `test_charge_with_decimal_amount`: Create charge using Decimal amount → verify no precision loss

  Test class `TestChargeIdempotencyInFlows`:
  11. `test_charge_retry_with_idempotency`: Create charge → fail network → retry with same key → verify single charge
  12. `test_charge_different_keys_different_charges`: Create 2 charges with different keys → verify 2 ledger entries

  Test class `TestChargeWebhookIntegration`:
  13. `test_webhook_after_charge`: Create charge → simulate webhook delivery → verify ledger update
  14. `test_webhook_before_charge_arrives_first`: Webhook arrives before charge created → verify deferred processing
  15. `test_duplicate_webhook_ignored`: Deliver same webhook twice → verify single ledger entry

  Test class `TestChargeAccountingIntegration`:
  16. `test_charge_creates_debit_entry`: Verify charge creates debit in asset account
  17. `test_charge_creates_credit_entry`: Verify charge creates credit in revenue account
  18. `test_double_entry_validation`: Verify debits == credits after charge

  Use freezegun for time-dependent tests. Use to_decimal for all monetary values. Use accounting_ledger fixture to verify ledger entries.
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/payment_integration/test_payment_flows.py::TestChargeFlow -v`
  Expected: All 6 charge flow tests pass, ledger entries created correctly
  </verify>
  <done>
  18 tests cover charge flows, edge cases, idempotency, webhook integration, and accounting integration
  </done>
</task>

<task type="auto">
  <name>Task 3: Create subscription and invoice flow integration tests</name>
  <files>
  backend/tests/payment_integration/test_payment_flows.py
  </files>
  <action>
  Complete test_payment_flows.py with subscription and invoice flow tests (remaining 200 lines):

  Add to **backend/tests/payment_integration/test_payment_flows.py**:

  Test class `TestSubscriptionFlow`:
  19. `test_complete_subscription_flow`: Create subscription → invoice created → payment succeeded → verify ledger entries
  20. `test_subscription_with_trial`: Create subscription with trial → verify no charge during trial
  21. `test_subscription_cancelled`: Create subscription → cancel → verify prorated refund → verify ledger
  22. `test_subscription_updated`: Create subscription → update quantity → verify new invoice amount
  23. `test_subscription_payment_failed`: Create subscription → payment fails → verify retry logic → verify dunning

  Test class `TestInvoiceFlow`:
  24. `test_complete_invoice_flow`: Create invoice → pay invoice → verify payment succeeded webhook → verify ledger
  25. `test_invoice_with_multiple_line_items`: Create invoice with 3 line items → verify total correct → verify ledger
  26. `test_invoice_paid_with_webhook`: Pay invoice via webhook → verify ledger update
  27. `test_invoice_marked_uncollectible`: Mark invoice uncollectible → verify write-off → verify ledger

  Test class `TestSubscriptionAccountingIntegration`:
  28. `test_subscription_creates_recurring_entries`: Verify multiple invoices create multiple ledger entries
  29. `test_subscription_revenue_recognition`: Verify revenue recognized correctly over time
  30. `test_subscription_cancelation_proration`: Verify prorated refund calculated correctly

  Test class `TestEndToEndScenarios`:
  31. `test_new_customer_subscription_flow`: New customer → subscription → first invoice → payment → verify all ledger entries
  32. `test_existing_customer_upgrade`: Existing customer → upgrade plan → verify prorated charge → verify ledger
  33. `test_customer_downgrade`: Downgrade plan → verify credit → verify ledger

  Test class `TestFlowErrorRecovery`:
  34. `test_webhook_delivery_failure_payment_succeeds`: Payment succeeds, webhook fails → verify manual recovery
  35. `test_payment_fails_retries_succeeds`: Payment fails, retries succeed → verify eventual success
  36. `test_partial_payment_scenarios`: Invoice partially paid → verify remaining balance tracked

  Use StripeSubscriptionFactory and StripeInvoiceFactory for test data. Verify accounting ledger entries using accounting_ledger fixture.
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/payment_integration/test_payment_flows.py -v`
  Expected: All 36 tests pass, complete payment flows validated end-to-end
  </verify>
  <done>
  36 total integration tests cover charges, subscriptions, invoices, accounting integration, and error recovery scenarios
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:
1. Complete charge flow works (API → webhook → ledger)
2. Subscription flow works (create → invoice → payment → ledger)
3. Refund flow works correctly (original charge → refund → credit)
4. Invoice payment flow works (create → pay → reconcile)
5. All monetary values use Decimal (no float types in any test)
</verification>

<success_criteria>
1. pytest tests/payment_integration/test_payment_flows.py passes (36+ integration tests)
2. All tests use Decimal from decimal_utils for monetary values
3. Accounting ledger entries created correctly for all flows
4. Double-entry bookkeeping validated (debits == credits)
5. Webhook processing verified for all event types
</success_criteria>

<output>
After completion, create `.planning/phases/092-payment-integration-testing/092-05-SUMMARY.md` with:
- Complete payment flow documentation (charge, refund, subscription, invoice)
- Test results (36 tests passing)
- Accounting integration verification
- Edge case coverage (zero amounts, large amounts, fractional cents)
- Recommendations for production monitoring
</output>
