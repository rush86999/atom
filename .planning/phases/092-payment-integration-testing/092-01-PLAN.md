---
phase: 092-payment-integration-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/payment_integration/__init__.py
  - backend/tests/payment_integration/conftest.py
  - backend/tests/payment_integration/test_stripe_mock_server.py
  - backend/tests/mocks/stripe_mock_server.py
  - backend/tests/fixtures/payment_fixtures.py
autonomous: true

must_haves:
  truths:
    - stripe-mock Docker container starts on ports 12111-12112 for all payment tests
    - Test fixtures create Stripe charge/invoice/subscription objects with valid fields
    - Mock server responds deterministically to Stripe API calls (no rate limits, no network)
    - Tests validate mock responses match real Stripe test mode behavior
    - Payment fixtures use Decimal from Phase 91 for all monetary values
  artifacts:
    - path: "backend/tests/payment_integration/conftest.py"
      provides: "pytest fixtures for stripe-mock and payment objects"
      exports: ["stripe_mock_server", "db_session", "payment_client"]
    - path: "backend/tests/mocks/stripe_mock_server.py"
      provides: "stripe-mock Docker container wrapper"
      exports: ["start_stripe_mock", "stop_stripe_mock", "stripe_mock_url"]
    - path: "backend/tests/fixtures/payment_fixtures.py"
      provides: "Factory Boy factories for Stripe objects"
      exports: ["StripeChargeFactory", "StripeInvoiceFactory", "StripeSubscriptionFactory", "StripeWebhookEventFactory"]
    - path: "backend/tests/payment_integration/test_stripe_mock_server.py"
      provides: "stripe-mock server validation tests"
      min_lines: 100
  key_links:
    - from: "backend/tests/payment_integration/test_stripe_mock_server.py"
      to: "backend/tests/mocks/stripe_mock_server.py"
      via: "pytest fixture import"
      pattern: "from tests.mocks.stripe_mock_server import start_stripe_mock"
    - from: "backend/tests/fixtures/payment_fixtures.py"
      to: "backend/core/decimal_utils.py"
      via: "Decimal precision for money"
      pattern: "from core.decimal_utils import to_decimal"
---

<objective>
Payment provider mock server infrastructure that matches real Stripe/PayPal behavior using stripe-mock Docker container, factory_boy for test data generation, and pytest fixtures for deterministic testing.

Purpose: Enable reliable payment integration tests without network calls, rate limits, or real API dependencies. Mock server must match real Stripe behavior to prevent "tests pass but production fails" scenarios.

Output: Mock server wrapper, payment fixtures, and validation tests confirming stripe-mock behaves like real Stripe test mode.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/091-core-accounting-logic/091-05-SUMMARY.md
@.planning/phases/092-payment-integration-testing/092-RESEARCH.md
@backend/core/decimal_utils.py
@backend/tests/fixtures/decimal_fixtures.py
@backend/integrations/stripe_service.py
@backend/integrations/stripe_routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create stripe-mock Docker wrapper and pytest fixtures</name>
  <files>
  backend/tests/mocks/stripe_mock_server.py
  backend/tests/payment_integration/__init__.py
  backend/tests/payment_integration/conftest.py
  </files>
  <action>
  Create stripe-mock Docker container wrapper and pytest fixtures:

  1. **backend/tests/mocks/stripe_mock_server.py** (200 lines):
     - Import docker module (already in requirements.txt)
     - `start_stripe_mock()` function: spawns stripe/stripe-mock:latest container, ports 12111-12112, returns container object
     - `stop_stripe_mock(container)` function: stops and removes container
     - `stripe_mock_url()` property: returns "http://localhost:12111"
     - `is_stripe_mock_running()` function: checks if port 12111 is open using requests
     - Add logging for container start/stop events
     - Handle Docker errors gracefully (raise StripeMockError with helpful message)

  2. **backend/tests/payment_integration/__init__.py**: Empty marker file

  3. **backend/tests/payment_integration/conftest.py** (150 lines):
     - `@pytest.fixture(scope="session") stripe_mock_container()`: starts stripe-mock once per test session, yields container, auto-cleanup
     - `@pytest.fixture(autouse=True) mock_stripe_api()`: patches stripe.api_base to point to mock server using unittest.mock.patch
     - `@pytest.fixture db_session()`: in-memory SQLite database with test schema (reuse pattern from Phase 91)
     - `@pytest.fixture payment_client()`: FastAPI TestClient for stripe_routes
     - `@pytest.fixture stripe_access_token()`: fake test token "sk_test_mock_token"

  Pattern: Use context managers for Docker container lifecycle, ensure cleanup even if tests fail.
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/payment_integration/test_stripe_mock_server.py::test_mock_server_starts -v`
  Expected: Container starts, port 12111 responds, test passes
  </verify>
  <done>
  stripe-mock container starts on ports 12111-12112, pytest fixtures provide mock server URL and database session to all tests
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Factory Boy factories for payment test data</name>
  <files>
  backend/tests/fixtures/payment_fixtures.py
  </files>
  <action>
  Create Factory Boy factories for Stripe payment objects with Decimal precision:

  **backend/tests/fixtures/payment_fixtures.py** (300 lines):
  - Import factory from factory_boy (already in requirements.txt)
  - Import to_decimal from core.decimal_utils (Phase 91)

  Define factories:
  1. `StripeChargeFactory`:
     - model: dict (Stripe API returns dict)
     - id = factory.Sequence(lambda n: f"ch_test_{n}")
     - amount = 1000 (default: $10.00 in cents)
     - currency = "usd"
     - status = "succeeded"
     - object = "charge"
     - customer = factory.SubFactory("StripeCustomerFactory")
     - created = factory.LazyFunction(lambda: int(datetime.now().timestamp()))

  2. `StripeInvoiceFactory`:
     - id = factory.Sequence(lambda n: f"in_test_{n}")
     - amount_due = 1000
     - currency = "usd"
     - status = "paid"
     - total = 1000
     - subscription = factory.SubFactory("StripeSubscriptionFactory")

  3. `StripeSubscriptionFactory`:
     - id = factory.Sequence(lambda n: f"sub_test_{n}")
     - status = "active"
     - current_period_end = factory.LazyFunction(lambda: int((datetime.now() + timedelta(days=30)).timestamp()))
     - items = factory.List([factory.SubFactory("StripeSubscriptionItemFactory")])

  4. `StripeWebhookEventFactory`:
     - id = factory.Sequence(lambda n: f"evt_test_{n}")
     - type = "payment_intent.succeeded"
     - data = factory.LazyAttribute(lambda obj: {"object": StripeChargeFactory()})
     - created = factory.LazyFunction(lambda: int(datetime.now().timestamp()))

  All monetary values MUST use to_decimal() from Phase 91's decimal_utils.py for internal representation, convert to cents (int) for Stripe API compatibility.

  Export: StripeChargeFactory, StripeInvoiceFactory, StripeSubscriptionFactory, StripeWebhookEventFactory
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend python -c "from tests.fixtures.payment_fixtures import StripeChargeFactory; c = StripeChargeFactory(); print(c['id'], c['amount'])"`
  Expected: Prints "ch_test_0 1000"
  </verify>
  <done>
  Factory Boy factories create valid Stripe objects (charges, invoices, subscriptions, webhooks) with Decimal precision and realistic field values
  </done>
</task>

<task type="auto">
  <name>Task 3: Create stripe-mock validation tests</name>
  <files>
  backend/tests/payment_integration/test_stripe_mock_server.py
  </files>
  <action>
  Create validation tests for stripe-mock server behavior:

  **backend/tests/payment_integration/test_stripe_mock_server.py** (200 lines):
  - Import pytest, stripe, requests
  - Import fixtures from conftest and payment_fixtures

  Test class `TestStripeMockServer`:
  1. `test_mock_server_starts`: Verifies container starts, port 12111 responds to GET / with 200
  2. `test_mock_create_charge`: Creates charge via stripe.Charge.create(), verifies status="succeeded", amount matches
  3. `test_mock_list_charges`: Lists charges, verifies pagination works (limit parameter)
  4. `test_mock_create_customer`: Creates customer, verifies customer.id prefix "cus_test_"
  5. `test_mock_create_subscription`: Creates subscription with items, verifies status="active"
  6. `test_mock_error_handling`: Triggers Stripe error (e.g., invalid card), verifies error response structure

  Test class `TestMockVsRealBehavior`:
  7. `test_charge_response_structure`: Compares mock response keys to real Stripe charge keys (id, amount, currency, status, object)
  8. `test_idempotency_replay_header`: Creates charge with idempotency key, replays, verifies response same (mock should support this)

  Use @pytest.mark.django_db if needed, but since we're using stripe-mock (no DB needed), just @pytest.mark.asyncio for async tests.

  IMPORTANT: All monetary values in tests must use Decimal from decimal_utils, converted to integer cents for Stripe API (Stripe uses cents integers).
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/payment_integration/test_stripe_mock_server.py -v`
  Expected: All 8 tests pass, stripe-mock container starts/stops cleanly
  </verify>
  <done>
  stripe-mock server behaves like real Stripe API (charges, customers, subscriptions), response structure validated, idempotency replay works
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:
1. stripe-mock container lifecycle managed correctly (start, test, stop)
2. Factory Boy factories generate valid Stripe objects with 100+ test examples per factory
3. Mock server responds to all Stripe API operations used in stripe_service.py
4. Tests pass deterministically (no network calls, no rate limits)
5. Decimal precision used for all monetary values (to_decimal from Phase 91)
</verification>

<success_criteria>
1. pytest tests/payment_integration/test_stripe_mock_server.py passes (8+ tests)
2. stripe-mock container starts in <5 seconds, tests run in <30 seconds
3. Factory Boy factories create 100+ valid Stripe objects without errors
4. Mock server response structure matches real Stripe API (id, amount, currency, status fields)
5. All payment fixtures use Decimal from decimal_utils.py (no float types)
</success_criteria>

<output>
After completion, create `.planning/phases/092-payment-integration-testing/092-01-SUMMARY.md` with:
- Mock server wrapper implementation details
- Factory Boy factory definitions
- Test results (8 tests passing)
- Docker compose command for local development (optional)
</output>
