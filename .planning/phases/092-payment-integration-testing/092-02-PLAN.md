---
phase: 092-payment-integration-testing
plan: 02
type: execute
wave: 2
depends_on: ["092-01"]
files_modified:
  - backend/tests/payment_integration/test_webhook_processing.py
  - backend/tests/mocks/webhook_simulator.py
  - backend/integrations/stripe_routes.py
autonomous: true

must_haves:
  truths:
    - Webhook signature verification prevents forged webhooks (timing-safe comparison)
    - Duplicate webhook events are rejected (event ID deduplication)
    - Out-of-order webhook delivery is handled correctly (deferred processing)
    - Webhook retries work with exponential backoff (1s, 2s, 4s delays)
    - Webhook timeout scenarios are handled gracefully (503 response, retry later)
  artifacts:
    - path: "backend/tests/payment_integration/test_webhook_processing.py"
      provides: "Webhook processing tests for all scenarios"
      min_lines: 250
    - path: "backend/tests/mocks/webhook_simulator.py"
      provides: "Webhook retry and out-of-order simulator"
      exports: ["WebhookSimulator", "simulate_retry", "simulate_out_of_order"]
    - path: "backend/integrations/stripe_routes.py"
      provides: "Enhanced webhook handler with signature verification"
      contains: "stripe.Webhook.construct_event"
  key_links:
    - from: "backend/tests/payment_integration/test_webhook_processing.py"
      to: "backend/tests/mocks/webhook_simulator.py"
      via: "simulator import for retry testing"
      pattern: "from tests.mocks.webhook_simulator import simulate_retry"
    - from: "backend/tests/payment_integration/test_webhook_processing.py"
      to: "backend/tests/fixtures/payment_fixtures.py"
      via: "StripeWebhookEventFactory for test data"
      pattern: "from tests.fixtures.payment_fixtures import StripeWebhookEventFactory"
    - from: "backend/integrations/stripe_routes.py"
      to: "backend/integrations/stripe_service.py"
      via: "payment processing calls"
      pattern: "stripe_service.create_payment|stripe_service.get_payment"
---

<objective>
Comprehensive webhook testing covering success, failure, retries, timeouts, and out-of-order delivery scenarios with signature verification and deduplication.

Purpose: Webhooks are the primary way Atom learns about payment events. Stripe explicitly warns webhooks are NOT guaranteed to arrive in order and may be delivered multiple times. Tests must verify our webhook handler handles these edge cases correctly.

Output: Webhook simulator, enhanced webhook handler with signature verification, and 15+ tests covering all webhook scenarios.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/092-payment-integration-testing/092-01-SUMMARY.md
@.planning/phases/092-payment-integration-testing/092-RESEARCH.md
@backend/tests/fixtures/payment_fixtures.py
@backend/integrations/stripe_routes.py
@backend/tests/payment_integration/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create webhook simulator for retry and out-of-order testing</name>
  <files>
  backend/tests/mocks/webhook_simulator.py
  </files>
  <action>
  Create webhook simulator for testing retry, out-of-order, and burst scenarios:

  **backend/tests/mocks/webhook_simulator.py** (250 lines):
  - Import asyncio, time, random
  - Import freezegun for time control (already in requirements.txt)

  Classes:
  1. `WebhookSimulator`:
     - `__init__(self, webhook_endpoint_url)`: Store endpoint URL
     - `send_event(self, event_data, signature=None)`: POST webhook to endpoint
     - `send_batch(self, events, delay_ms=0)`: Send multiple webhooks with optional delay
     - `send_out_of_order(self, events)`: Shuffle events randomly before sending
     - `send_with_retry(self, event, max_retries=3)`: Implement exponential backoff (1s, 2s, 4s)
     - `send_duplicate(self, event, count=2)`: Send same event multiple times

  2. `WebhookEventBuilder`:
     - `create_payment_succeeded(self, payment_id, amount)`: Build payment_intent.succeeded event
     - `create_payment_failed(self, payment_id, error_code)`: Build payment_intent.payment_failed event
     - `create_subscription_created(self, subscription_id)`: Build customer.subscription.created
     - `create_invoice_paid(self, invoice_id, amount)`: Build invoice.payment_succeeded
     - `with_signature(self, payload, secret)`: Generate Stripe webhook signature (HMAC-SHA256, timestamp t=..., v1=...)

  Functions:
  - `generate_stripe_signature(payload, secret)`: Generate realistic Stripe signature header
    - Format: `t={timestamp},v1={hmac_sha256}`
    - Use hmac.new() with SHA256, secret key
    - Include timestamp for replay attack prevention

  - `simulate_out_of_order_delivery(events)`: Shuffle events using random.shuffle()
  - `simulate_burst_delivery(events, events_per_second)`: Deliver in bursts (e.g., 100 events in 1 second)
  - `simulate_retry_scenario(failure_count)`: Simulate first N attempts failing (503), then success

  Export: WebhookSimulator, WebhookEventBuilder, generate_stripe_signature, simulate_out_of_order_delivery, simulate_burst_delivery
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend python -c "from tests.mocks.webhook_simulator import WebhookSimulator; print(WebhookSimulator.__doc__)"`
  Expected: Class documentation prints, imports work
  </verify>
  <done>
  WebhookSimulator can send webhooks with retry, out-of-order, and duplicate scenarios; signature generator produces valid Stripe-format headers
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance webhook handler with signature verification and deduplication</name>
  <files>
  backend/integrations/stripe_routes.py
  </files>
  <action>
  Enhance the existing `/webhooks` endpoint in stripe_routes.py with signature verification and deduplication:

  Modify `backend/integrations/stripe_routes.py`:
  1. Add webhook event deduplication cache:
     - Add `PROCESSED_WEBHOOK_EVENTS = {}` (in-memory cache, event_id -> timestamp)
     - Add `CLEANUP_INTERVAL = 3600` (1 hour)
     - Add function `cleanup_processed_events()`: Remove events older than 24 hours

  2. Enhance `handle_stripe_webhook()` function:
     - Add signature verification using `stripe.Webhook.construct_event()`
     - Add event deduplication check: if event["id"] in PROCESSED_WEBHOOK_EVENTS, return 200 with "duplicate" status
     - Add event to PROCESSED_WEBHOOK_EVENTS after successful processing
     - Handle all event types from research (payment_intent.*, invoice.*, customer.subscription.*)

  3. Add webhook helper functions:
     - `verify_webhook_signature(payload, signature, secret)`: Use stripe.Webhook.construct_event(), raise HTTPException 400 if invalid
     - `is_duplicate_event(event_id)`: Check PROCESSED_WEBHOOK_EVENTS cache
     - `mark_event_processed(event_id)`: Add to cache with timestamp

  4. Update error handling:
     - Return 503 on temporary failures (triggers Stripe retry)
     - Return 400 on permanent failures (invalid signature, malformed payload)
     - Log all webhook events with event_id, type, status

  IMPORTANT: Use timing-safe string comparison (stripe.Webhook.construct_event does this automatically). Never use == for signature comparison.
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend grep -n "construct_event" backend/integrations/stripe_routes.py`
  Expected: Shows stripe.Webhook.construct_event() call in webhook handler
  </verify>
  <done>
  Webhook handler verifies signatures using Stripe SDK, rejects duplicate events, and returns appropriate HTTP codes for retry scenarios
  </done>
</task>

<task type="auto">
  <name>Task 3: Create comprehensive webhook processing tests</name>
  <files>
  backend/tests/payment_integration/test_webhook_processing.py
  </files>
  <action>
  Create comprehensive webhook processing tests covering all scenarios:

  **backend/tests/payment_integration/test_webhook_processing.py** (350 lines):
  - Import pytest, asyncio, freezegun
  - Import WebhookSimulator, generate_stripe_signature from webhook_simulator
  - Import StripeWebhookEventFactory from payment_fixtures
  - Import stripe_routes handlers

  Test class `TestWebhookSignatureVerification`:
  1. `test_valid_signature_accepted`: Send webhook with valid signature, verify 200 response
  2. `test_invalid_signature_rejected`: Send webhook with invalid signature, verify 400 response
  3. `test_missing_signature_rejected`: Send webhook without signature, verify 400 response
  4. `test_expired_timestamp_rejected`: Send webhook with old timestamp (>15 minutes), verify 400

  Test class `TestWebhookDeduplication`:
  5. `test_duplicate_webhook_rejected`: Send same webhook twice, verify second returns "duplicate" status
  6. `test_different_webhooks_accepted`: Send two different webhooks, verify both processed
  7. `test_duplicate_after_cleanup`: Send duplicate after 24-hour cache cleanup, verify accepted

  Test class `TestWebhookOutOfOrderDelivery`:
  8. `test_invoice_paid_before_payment_succeeded`: Send invoice.payment_succeeded before payment_intent.succeeded, verify deferred processing
  9. `test_out_of_order_subscription_events`: Send subscription.updated before subscription.created, verify handler logic
  10. `test_event_order_does_not_affect_ledger`: Process webhooks in random order, verify accounting ledger consistent

  Test class `TestWebhookRetries`:
  11. `test_webhook_retry_exponential_backoff`: Use freezegun to simulate 503 responses, verify retry delays (1s, 2s, 4s)
  12. `test_webhook_retry_eventual_success`: Fail twice, succeed on third attempt, verify event processed once
  13. `test_webhook_max_retries_exceeded`: Fail 4 times (exceeds Stripe's retry limit), verify manual intervention needed

  Test class `TestWebhookTimeouts`:
  14. `test_webhook_timeout_returns_503`: Simulate timeout (event processing takes >30s), verify 503 response
  15. `test_burst_webhook_delivery`: Send 100 webhooks in 1 second, verify all processed without errors

  Test class `TestWebhookEventTypes`:
  16. `test_payment_succeeded_creates_ledger_entry`: Verify accounting ledger entry created
  17. `test_payment_failed_creates_alert`: Verify failure alert logged
  18. `test_subscription_created_updates_customer`: Verify customer status updated

  Use pytest.mark.asyncio for async tests. Use freezegun.freeze_time() for time-dependent tests.
  </action>
  <verify>
  Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/payment_integration/test_webhook_processing.py -v`
  Expected: All 18 tests pass, webhook signature verification works, deduplication prevents double-processing
  </verify>
  <done>
  18 tests cover all webhook scenarios (signature, deduplication, out-of-order, retry, timeout, event types), all tests pass deterministically
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:
1. Webhook signature verification prevents forged webhooks (timing-safe comparison)
2. Duplicate webhooks are rejected via event ID cache
3. Out-of-order webhooks are handled correctly (deferred processing, idempotency)
4. Retries work with exponential backoff (freezegun-verified timing)
5. All event types (payment_intent, invoice, subscription) processed correctly
</verification>

<success_criteria>
1. pytest tests/payment_integration/test_webhook_processing.py passes (18+ tests)
2. Signature verification uses stripe.Webhook.construct_event() (timing-safe)
3. Duplicate webhooks return "duplicate" status (no double-posting)
4. Out-of-order delivery doesn't corrupt accounting ledger
5. Webhook retry tests use freezegun for deterministic timing (no actual delays)
</success_criteria>

<output>
After completion, create `.planning/phases/092-payment-integration-testing/092-02-SUMMARY.md` with:
- Webhook simulator implementation details
- Signature verification approach (Stripe SDK)
- Deduplication cache design
- Test results (18 tests passing)
</output>
