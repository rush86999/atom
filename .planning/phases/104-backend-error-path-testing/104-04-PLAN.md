---
phase: 104-backend-error-path-testing
plan: 04
type: execute
wave: 2
depends_on: [104-01, 104-02, 104-03]
files_modified:
  - backend/tests/error_paths/test_edge_case_error_paths.py
  - backend/tests/error_paths/BUG_FINDINGS.md
autonomous: true

must_haves:
  truths:
    - "Empty collections (lists, dicts, sets) are handled without crashes"
    - "Null/None inputs are handled gracefully across all services"
    - "String edge cases (empty, unicode, special characters) are processed correctly"
    - "Numeric edge cases (zero, negative, infinity, NaN) are handled"
    - "Date/time edge cases (leap years, timezones, DST transitions) are handled"
    - "Concurrent access edge cases (race conditions, deadlocks) are prevented"
    - "All edge case error paths are tested with VALIDATED_BUG documentation"
  artifacts:
    - path: backend/tests/error_paths/test_edge_case_error_paths.py
      provides: Edge case error path tests
      min_lines: 400
      exports: [TestEmptyInputs, TestNullInputs, TestStringEdgeCases, TestNumericEdgeCases, TestDatetimeEdgeCases, TestConcurrencyEdgeCases]
    - path: backend/tests/error_paths/BUG_FINDINGS.md
      provides: Bug findings documentation
      contains: "VALIDATED_BUG"
  key_links:
    - from: test_edge_case_error_paths.py
      to: core/ (various services)
      via: "import core services"
      pattern: "from core."
---

<objective>
Create comprehensive edge case tests for critical services covering empty inputs, null handling, string/numeric/datetime edge cases, and concurrent access.

**Purpose:** Edge cases are where production bugs hide. Comprehensive edge case testing prevents crashes from unexpected inputs.

**Output:** 400+ line test file with 30+ tests covering common edge case scenarios.

**Why This Matters:**
- Empty collections can cause iteration errors
- None/null inputs can cause AttributeError or TypeError
- Unicode and special characters can cause encoding errors
- Infinity/NaN can propagate through calculations
- Date/time edge cases can cause incorrect business logic
- Concurrent access can cause race conditions
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/TESTING.md

# Reference existing edge case patterns
@backend/tests/error_paths/BUG_FINDINGS.md
@backend/tests/error_paths/test_governance_cache_error_paths.py

# Services under test (various core services)
@backend/core/agent_governance_service.py
@backend/core/governance_cache.py
@backend/core/episode_segmentation_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_edge_case_error_paths.py file structure</name>
  <files>backend/tests/error_paths/test_edge_case_error_paths.py</files>
  <action>
Create `backend/tests/error_paths/test_edge_case_error_paths.py` with the following structure:

```python
"""
Edge case error path tests.

Tests cover:
- Empty inputs (empty lists, dicts, strings)
- Null/None handling across services
- String edge cases (unicode, special characters, encoding)
- Numeric edge cases (zero, negative, infinity, NaN)
- Datetime edge cases (leap years, timezones, DST)
- Concurrency edge cases (race conditions, deadlocks)

VALIDATED_BUG: Document all bugs found with VALIDATED_BUG docstring pattern.
"""

import math
import time
import pytest
import asyncio
from datetime import datetime, timedelta, timezone
from decimal import Decimal
from unittest.mock import Mock, MagicMock, AsyncMock, patch
from concurrent.futures import ThreadPoolExecutor

from core.governance_cache import GovernanceCache
from core.agent_governance_service import AgentGovernanceService


class TestEmptyInputs:
    """Test empty input handling."""

    # Tests will be added in Task 2


class TestNullInputs:
    """Test None/null input handling."""

    # Tests will be added in Task 3


class TestStringEdgeCases:
    """Test string edge case handling."""

    # Tests will be added in Task 4


class TestNumericEdgeCases:
    """Test numeric edge case handling."""

    # Tests will be added in Task 5


class TestDatetimeEdgeCases:
    """Test datetime edge case handling."""

    # Tests will be added in Task 6


class TestConcurrencyEdgeCases:
    """Test concurrent access handling."""

    # Tests will be added in Task 7
```

Include necessary fixtures for test services and edge case data.
  </action>
  <verify>File exists with 6 test class stubs and docstring describing VALIDATED_BUG pattern</verify>
  <done>test_edge_case_error_paths.py created with class structure and fixtures</done>
</task>

<task type="auto">
  <name>Task 2: Implement empty input tests</name>
  <files>backend/tests/error_paths/test_edge_case_error_paths.py</files>
  <action>
Implement TestEmptyInputs class with the following test methods:

1. `test_empty_list_in_governance_check` - Tests governance check with empty permissions list
2. `test_empty_dict_in_cache_entry` - Tests cache entry with empty dict data
3. `test_empty_string_in_agent_id` - Tests empty string agent ID handling
4. `test_empty_string_in_user_id` - Tests empty string user ID handling
5. `test_empty_messages_in_episode_creation` - Tests episode creation with empty messages list

Use VALIDATED_BUG docstring pattern for any bugs found:
```python
def test_empty_list_in_governance_check(self):
    """
    VALIDATED_BUG or NO_BUG

    Test that governance check handles empty permissions list.

    Expected: Returns denied (no permissions = no access)
    Actual: [Test result]
    Severity: [CRITICAL/HIGH/MEDIUM/LOW]
    Impact: [What happens if this bug exists in production]
    Fix: [How to fix]
    """
    cache = GovernanceCache(max_size=100, ttl_seconds=60)
    result = cache.set("agent-1", "stream_chat", {"allowed": True, "permissions": []})
    # Test behavior
```
  </action>
  <verify>pytest backend/tests/error_paths/test_edge_case_error_paths.py::TestEmptyInputs -v passes 5 tests</verify>
  <done>5 empty input tests implemented with VALIDATED_BUG patterns</done>
</task>

<task type="auto">
  <name>Task 3: Implement null input tests</name>
  <files>backend/tests/error_paths/test_edge_case_error_paths.py</files>
  <action>
Implement TestNullInputs class with the following test methods:

1. `test_none_agent_id_in_governance_check` - Tests None agent ID handling
2. `test_none_action_type_in_cache_lookup` - Tests None action type in cache
3. `test_none_data_in_cache_set` - Tests None data in cache entry
4. `test_none_confidence_score_in_agent` - Tests None confidence score handling
5. `test_none_in_maturity_level_check` - Tests None maturity level handling

Use VALIDATED_BUG docstring pattern for any bugs found.
  </action>
  <verify>pytest backend/tests/error_paths/test_edge_case_error_paths.py::TestNullInputs -v passes 5 tests</verify>
  <done>5 null input tests implemented with VALIDATED_BUG patterns</done>
</task>

<task type="auto">
  <name>Task 4: Implement string edge case tests</name>
  <files>backend/tests/error_paths/test_edge_case_error_paths.py</files>
  <action>
Implement TestStringEdgeCases class with the following test methods:

1. `test_unicode_in_agent_id` - Tests unicode characters in agent ID
2. `test_special_characters_in_user_input` - Tests special characters (<, >, &, ", ') in user input
3. `test_emoji_in_agent_name` - Tests emoji characters in agent name
4. `test_very_long_string_in_agent_id` - Tests extremely long string (10k+ chars)
5. `test_null_byte_in_string` - Tests null byte (\x00) in string
6. `test_mixed_encoding_string` - Tests mixed encoding string handling

Use VALIDATED_BUG docstring pattern for any bugs found.
  </action>
  <verify>pytest backend/tests/error_paths/test_edge_case_error_paths.py::TestStringEdgeCases -v passes 6 tests</verify>
  <done>6 string edge case tests implemented with VALIDATED_BUG patterns</done>
</task>

<task type="auto">
  <name>Task 5: Implement numeric edge case tests</name>
  <files>backend/tests/error_paths/test_edge_case_error_paths.py</files>
  <action>
Implement TestNumericEdgeCases class with the following test methods:

1. `test_zero_confidence_score` - Tests confidence score = 0.0 boundary
2. `test_negative_confidence_score` - Tests negative confidence score handling
3. `test_confidence_score_greater_than_one` - Tests confidence score > 1.0
4. `test_infinity_in_numeric_calculation` - Tests math.inf handling
5. `test_nan_in_numeric_calculation` - Tests math.nan handling
6. `test_very_large_numeric_value` - Tests very large integer (2**63)

Use VALIDATED_BUG docstring pattern for any bugs found.
  </action>
  <verify>pytest backend/tests/error_paths/test_edge_case_error_paths.py::TestNumericEdgeCases -v passes 6 tests</verify>
  <done>6 numeric edge case tests implemented with VALIDATED_BUG patterns</done>
</task>

<task type="auto">
  <name>Task 6: Implement datetime edge case tests</name>
  <files>backend/tests/error_paths/test_edge_case_error_paths.py</files>
  <action>
Implement TestDatetimeEdgeCases class with the following test methods:

1. `test_leap_year_date_handling` - Tests Feb 29 date handling
2. `test_dst_transition_handling` - Tests daylight saving time transition
3. `test_timezone_aware_datetime` - Tests timezone-aware datetime handling
4. `test_far_future_date` - Tests date far in future (year 3000)
5. `test_far_past_date` - Tests date far in past (year 1900)
6. `test_negative_timedelta` - Tests negative time delta handling

Use VALIDATED_BUG docstring pattern for any bugs found.
  </action>
  <verify>pytest backend/tests/error_paths/test_edge_case_error_paths.py::TestDatetimeEdgeCases -v passes 6 tests</verify>
  <done>6 datetime edge case tests implemented with VALIDATED_BUG patterns</done>
</task>

<task type="auto">
  <name>Task 7: Implement concurrency edge case tests</name>
  <files>backend/tests/error_paths/test_edge_case_error_paths.py</files>
  <action>
Implement TestConcurrencyEdgeCases class with the following test methods:

1. `test_concurrent_cache_writes_same_key` - Tests concurrent writes to same cache key
2. `test_concurrent_cache_reads_during_write` - Tests reads during write operation
3. `test_concurrent_governance_checks` - Tests concurrent governance checks
4. `test_race_condition_in_cache_eviction` - Tests race during cache eviction
5. `test_deadlock_prevention` - Tests that deadlock doesn't occur with circular dependencies

Use asyncio and ThreadPoolExecutor for concurrent test execution.

Use VALIDATED_BUG docstring pattern for any bugs found.
  </action>
  <verify>pytest backend/tests/error_paths/test_edge_case_error_paths.py::TestConcurrencyEdgeCases -v passes 5 tests</verify>
  <done>5 concurrency edge case tests implemented with VALIDATED_BUG patterns</done>
</task>

</tasks>

<verification>
After completion, verify:

1. Run all edge case error path tests:
```bash
pytest backend/tests/error_paths/test_edge_case_error_paths.py -v
```

2. Verify test count: 33 tests (5 + 5 + 6 + 6 + 6 + 5)

3. Run with coverage:
```bash
pytest backend/tests/error_paths/test_edge_case_error_paths.py --cov=core.governance_cache --cov=core.agent_governance_service --cov-report=term-missing
```

4. Check that BUG_FINDINGS.md is updated with any VALIDATED_BUG findings

5. Verify all tests pass (100% pass rate expected)
</verification>

<success_criteria>
1. test_edge_case_error_paths.py exists with 400+ lines
2. 33+ edge case tests implemented
3. All tests pass (100% pass rate)
4. VALIDATED_BUG docstrings used for any bugs found
5. BUG_FINDINGS.md updated with edge case bug findings
6. Coverage of edge case error paths >60%
</success_criteria>

<output>
After completion, create `.planning/phases/104-backend-error-path-testing/104-04-SUMMARY.md` with:
- Tests created (count, lines)
- Pass rate
- Bugs found (VALIDATED_BUG count)
- Coverage improvement
- File locations
</output>
