---
phase: 216-fix-business-facts-test-failures
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/api/test_admin_business_facts_routes.py
autonomous: true

must_haves:
  truths:
    - All upload tests pass with mocked WorldModelService
    - All citation verification tests pass with mocked services
    - Mocks patch correct import locations (api.admin.business_facts_routes)
    - S3/R2 storage operations properly mocked
    - PDF extraction service properly mocked
  artifacts:
    - path: tests/api/test_admin_business_facts_routes.py
      provides: Fixed mock patching for WorldModelService in upload and verify tests
      contains: test_upload_and_extract_success, test_upload_extracts_multiple_facts, test_upload_extraction_fails, test_verify_citation_*
  key_links:
    - from: tests/api/test_admin_business_facts_routes.py
      to: api/admin/business_facts_routes.py
      via: WorldModelService import location
      pattern: 'from core.agent_world_model import WorldModelService'
    - from: test mocks
      to: api/admin/business_facts_routes.py
      via: patch target 'api.admin.business_facts_routes.WorldModelService'
      pattern: "patch\('api\.admin\.business_facts_routes\.WorldModelService'"
---

<objective>
Fix mock patching for WorldModelService in upload and citation verification tests

**Purpose:**
Tests are patching the wrong import location for WorldModelService. The API routes import WorldModelService from `core.agent_world_model`, so mocks must patch `api.admin.business_facts_routes.WorldModelService` (where it's imported and used), not the original module.

**Impact:**
- 8 tests fail due to mock not intercepting service instantiation
- Upload tests call real WorldModelService which may fail without database
- Citation verification tests return 404 because fact lookup uses real service

**Output:**
- Fixed mock patch locations in 8 tests
- Reusable pattern for service mocking in API route tests
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/216-fix-business-facts-test-failures/216-RESEARCH.md
@.planning/phases/216-fix-business-facts-test-failures/216-CONTEXT.md
@.planning/STATE.md

# Mock Pattern Reference
Similar fix in Phase 215 for ABTestingService:
Changed patch from 'core.ab_testing_service.ABTestingService' to 'api.ab_testing.ABTestingService'
</context>

<tasks>

<task type="auto">
  <name>Fix WorldModelService patch location in upload tests</name>
  <files>tests/api/test_admin_business_facts_routes.py</files>
  <action>
    Fix all upload test methods that patch WorldModelService. The upload tests (lines 802-1008) need correct patch location.

    **Current incorrect patches (find and replace):**
    ```python
    patch('core.agent_world_model.WorldModelService', ...)
    ```

    **Change to (for all upload tests):**
    ```python
    patch('api.admin.business_facts_routes.WorldModelService', ...)
    ```

    **Specific test methods to update:**
    1. test_upload_and_extract_success (line 813)
    2. test_upload_with_custom_domain (line 853)
    3. test_upload_extracts_multiple_facts (line 913)
    4. test_upload_citation_format (line 939)
    5. test_upload_temp_file_cleanup (line 970)
    6. test_upload_extraction_fails (line 999 - no WorldModelService patch, check)

    **Note:** test_upload_invalid_file_type and test_upload_extraction_fails may not need WorldModelService patch.

    **Verification:**
    Run: `pytest tests/api/test_admin_business_facts_routes.py::TestBusinessFactsUpload -v`
    Expected: All upload tests pass
  </action>
  <verify>pytest tests/api/test_admin_business_facts_routes.py::TestBusinessFactsUpload -v passes all tests</verify>
  <done>All upload tests patch WorldModelService at correct import location</done>
</task>

<task type="auto">
  <name>Fix WorldModelService patch location in citation verification tests</name>
  <files>tests/api/test_admin_business_facts_routes.py</files>
  <action>
    Fix all citation verification test methods that patch WorldModelService. The verify tests (lines 1014-1205) need correct patch location.

    **Current incorrect patches (find and replace):**
    ```python
    patch('core.agent_world_model.WorldModelService', ...)
    ```

    **Change to (for all verify tests except test_verify_citation_fact_not_found):**
    ```python
    patch('api.admin.business_facts_routes.WorldModelService', ...)
    ```

    **Specific test methods to update:**
    1. test_verify_citation_s3_exists (line 1029)
    2. test_verify_citation_s3_missing (line 1061)
    3. test_verify_citation_local_exists (line 1087)
    4. test_verify_citation_mixed_sources (line 1117)
    5. test_verify_citation_all_valid (line 1149)
    6. test_verify_citation_cross_bucket (line 1177)
    7. test_verify_citation_fact_not_found (line 1199) - already uses correct location 'api.admin.business_facts_routes.WorldModelService'

    **Why:**
    - The API route imports WorldModelService at module level
    - Must patch where it's imported (api.admin.business_facts_routes.WorldModelService)
    - test_verify_citation_fact_not_found already uses correct pattern

    **Verification:**
    Run: `pytest tests/api/test_admin_business_facts_routes.py::TestBusinessFactsVerify -v`
    Expected: All verification tests pass
  </action>
  <verify>pytest tests/api/test_admin_business_facts_routes.py::TestBusinessFactsVerify -v passes all tests</verify>
  <done>All citation verification tests patch WorldModelService at correct import location</done>
</task>

<task type="auto">
  <name>Verify all remaining tests pass after mock fixes</name>
  <files>tests/api/test_admin_business_facts_routes.py</files>
  <action>
    Run the full business facts test suite to verify all 10 previously failing tests now pass.

    Command:
    ```bash
    pytest tests/api/test_admin_business_facts_routes.py -v
    ```

    Expected: All 37 tests pass (21 already passing + 10 now fixed)

    **Previously failing tests that should now pass:**
    1. test_get_fact_not_found (Wave 1 fix)
    2. test_upload_invalid_file_type (Wave 1 fix)
    3. test_upload_and_extract_success (Wave 2 fix)
    4. test_upload_extracts_multiple_facts (Wave 2 fix)
    5. test_upload_extraction_fails (Wave 2 fix)
    6. test_verify_citation_s3_exists (Wave 2 fix)
    7. test_verify_citation_s3_missing (Wave 2 fix)
    8. test_verify_citation_local_exists (Wave 2 fix)
    9. test_verify_citation_mixed_sources (Wave 2 fix)
    10. test_verify_citation_all_valid (Wave 2 fix)

    If any test still fails, analyze the error:
    - If response structure: Apply Wave 1 pattern
    - If mock not working: Check patch location or missing mock configuration
  </action>
  <verify>All 37 tests in test_admin_business_facts_routes.py pass</verify>
  <done>Wave 2 complete: All 10 previously failing tests now pass</done>
</task>

</tasks>

<verification>
- [ ] All upload tests pass with correctly patched WorldModelService
- [ ] All citation verification tests pass with correctly patched WorldModelService
- [ ] Mock patch locations match where services are imported in routes
- [ ] Full test suite passes (37/37 tests)
</verification>

<success_criteria>
- [ ] 10/10 previously failing tests now pass
- [ ] 100% test pass rate for business facts routes
- [ ] No production code changes
- [ ] Mock patching pattern documented for future API tests
</success_criteria>

<output>
After completion, create `.planning/phases/216-fix-business-facts-test-failures/216-02-SUMMARY.md` with:
- All 10 tests now passing
- Mock patching pattern used (api.admin.business_facts_routes.WorldModelService)
- Comparison table of before/after patch locations
- Note: Wave 3 (S3/R2 and PDF extraction mocks) not needed
</output>
