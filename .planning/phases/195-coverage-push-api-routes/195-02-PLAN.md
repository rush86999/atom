---
phase: 195-coverage-push-api-routes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_agent_control_routes_coverage.py
autonomous: true

must_haves:
  truths:
    - "Agent control routes have comprehensive test coverage (75%+)"
    - "DaemonManager is mocked for all endpoints"
    - "All agent lifecycle endpoints tested (start, stop, restart, status, execute)"
    - "Error cases are covered (400 when already running, 500 on failure)"
    - "Request validation is tested (port ranges, timeout values)"
  artifacts:
    - path: "backend/tests/api/test_agent_control_routes_coverage.py"
      provides: "Agent control routes test coverage"
      min_lines: 500
      exports: ["TestAgentStart", "TestAgentStop", "TestAgentRestart", "TestAgentStatus", "TestAgentExecute"]
  key_links:
    - from: "backend/tests/api/test_agent_control_routes_coverage.py"
      to: "backend/api/agent_control_routes.py"
      via: "TestClient with DaemonManager mocking"
      pattern: "client\\.(get|post).*'/api/agent"
---

<objective>
Agent Control Routes Coverage Enhancement

**What:** Create comprehensive test coverage for agent_control_routes.py using FastAPI TestClient and DaemonManager mocking.

**Purpose:** Agent control routes enable programmatic Atom OS daemon management. These are critical for external agent integration but currently lack comprehensive coverage.

**Output:**
- New test file with 500+ lines covering all agent control endpoints
- 75%+ line coverage for agent_control_routes.py
- DaemonManager mocking for all lifecycle operations
- Request validation testing (port ranges, timeout values)
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/194-coverage-push-18-22/194-07-SUMMARY.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/api/agent_control_routes.py
@backend/cli/daemon.py
@backend/tests/api/test_agent_control_routes.py
</context>

<tasks>

<task type="auto">
  <name>Create Agent Control routes coverage tests</name>
  <files>backend/tests/api/test_agent_control_routes_coverage.py</files>
  <action>
    Create test_agent_control_routes_coverage.py with comprehensive coverage:

    **Fixtures:**
    - `client()` - TestClient for endpoint testing
    - `mock_daemon_manager()` - Patch for cli.daemon.DaemonManager

    **Test Classes:**

    1. TestAgentStart (5 tests):
       - Successful start returns StartAgentResponse with pid
       - Start when already running returns 400
       - Start with custom port (port field validation)
       - Start with host parameter
       - Start with workers parameter (1-16 range)

    2. TestAgentStop (4 tests):
       - Successful stop returns StopAgentResponse
       - Stop when not running returns 400
       - Stop calls DaemonManager.stop_daemon()
       - Stop handles exceptions gracefully

    3. TestAgentRestart (4 tests):
       - Successful restart returns RestartAgentResponse
       - Restart when not running starts fresh (was_running=False)
       - Restart when running stops first (was_running=True)
       - Restart with 2-second delay between stop and start

    4. TestAgentStatus (3 tests):
       - Successful status returns AgentStatusResponse
       - Status includes running, pid, uptime_seconds, memory_mb, cpu_percent
       - Status handles DaemonManager.get_status() errors

    5. TestAgentExecute (2 tests):
       - Execute returns placeholder response
       - Execute validates command field presence
       - Execute validates timeout range (1-300)

    6. TestAgentControlValidation (4 tests):
       - Port validation (ge=1, le=65535)
       - Workers validation (ge=1, le=16)
       - Timeout validation (ge=1, le=300)
       - Required field validation (command for execute)

    7. TestAgentControlErrorPaths (3 tests):
       - RuntimeError from start_daemon returns 500
       - IOError from start_daemon returns 500
       - Generic exception returns 500

    **Mocking Strategy:**
    - Use `patch('cli.daemon.DaemonManager')` for all daemon operations
    - Mock class methods: is_running(), get_pid(), start_daemon(), stop_daemon(), get_status()
    - Use MagicMock for return values

    **Verify Pattern from 194-07:**
    - TestClient for endpoint testing
    - Model validation (StartAgentRequest, StopAgentResponse, etc.)
    - Error response testing (400, 500)
  </action>
  <verify>
    cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/api/test_agent_control_routes_coverage.py -v --cov=api/agent_control_routes --cov-report=term-missing
  </verify>
  <done>
    - 25+ tests created
    - 75%+ coverage for agent_control_routes.py
    - DaemonManager fully mocked
    - All request validation tested
  </done>
</task>

</tasks>

<verification>
1. Test file created with 500+ lines
2. 25+ tests covering all agent control endpoints
3. 75%+ line coverage achieved
4. DaemonManager mocking works correctly
5. Request validation tested
6. All tests pass (100% pass rate)
</verification>

<success_criteria>
- **Coverage:** 75%+ for agent_control_routes.py (355 lines)
- **Tests:** 25+ tests across 7 test classes
- **Pass Rate:** 100%
- **Endpoints:** All 5 endpoints tested (POST /start, POST /stop, POST /restart, GET /status, POST /execute)
</success_criteria>

<output>
After completion, create `.planning/phases/195-coverage-push-api-routes/195-02-SUMMARY.md`
</output>
