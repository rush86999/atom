---
phase: 195-coverage-push-api-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_auth_2fa_routes_coverage.py
autonomous: true

must_haves:
  truths:
    - "Auth 2FA routes have comprehensive test coverage (80%+)"
    - "All 2FA endpoints (status, setup, enable, disable) are tested"
    - "TOTP verification is mocked properly with pyotp"
    - "Audit events are verified for enable/disable operations"
    - "Error cases are covered (409, 422, 401)"
  artifacts:
    - path: "backend/tests/api/test_auth_2fa_routes_coverage.py"
      provides: "Auth 2FA routes test coverage"
      min_lines: 400
      exports: ["TestAuth2FAStatus", "TestAuth2FASetup", "TestAuth2FAEnable", "TestAuth2FADisable"]
  key_links:
    - from: "backend/tests/api/test_auth_2fa_routes_coverage.py"
      to: "backend/api/auth_2fa_routes.py"
      via: "TestClient with dependency override"
      pattern: "client\\.(get|post).*'/api/auth/2fa"
---

<objective>
Auth 2FA Routes Coverage Enhancement

**What:** Create comprehensive test coverage for auth_2fa_routes.py using FastAPI TestClient and proper mocking.

**Purpose:** Auth 2FA is critical security infrastructure. Current test_auth_2fa_routes.py has 401 lines but lacks coverage for edge cases, audit verification, and realistic TOTP mocking.

**Output:**
- Enhanced test file with 400+ lines covering all 2FA endpoints
- 80%+ line coverage for auth_2fa_routes.py
- Proper pyotp mocking for TOTP verification
- Audit event verification for enable/disable operations
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/194-coverage-push-18-22/194-07-SUMMARY.md
@.planning/phases/194-coverage-push-18-22/194-08-SUMMARY.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/api/auth_2fa_routes.py
@backend/tests/api/test_auth_2fa_routes.py
</context>

<tasks>

<task type="auto">
  <name>Create Auth 2FA routes coverage tests</name>
  <files>backend/tests/api/test_auth_2fa_routes_coverage.py</files>
  <action>
    Create test_auth_2fa_routes_coverage.py with comprehensive coverage:

    **Fixtures:**
    - `test_db()` - In-memory SQLite with StaticPool
    - `test_app()` - FastAPI app with get_db override
    - `client()` - TestClient for endpoint testing
    - `mock_user()` - User fixture with 2FA fields
    - `mock_totp()` - pyotp.TOTP mock

    **Test Classes:**

    1. TestAuth2FAStatus (3 tests):
       - Status when 2FA disabled
       - Status when 2FA enabled
       - Status requires authentication (401)

    2. TestAuth2FASetup (4 tests):
       - Successful setup generates secret and otpauth_url
       - Setup when already enabled returns 409
       - Setup stores secret in database
       - Setup issuer name is "Atom AI (Upstream)"

    3. TestAuth2FAEnable (5 tests):
       - Enable with valid code updates two_factor_enabled
       - Enable when already enabled returns 409
       - Enable without setup (no secret) returns validation error
       - Enable with invalid code returns validation error
       - Enable creates backup codes

    4. TestAuth2FADisable (4 tests):
       - Disable with valid code clears 2FA fields
       - Disable when not enabled returns validation error
       - Disable with invalid code returns validation error
       - Disable clears secret and backup_codes

    5. TestAuth2FAAuditIntegration (2 tests):
       - Enable logs audit event with UPDATE type
       - Disable logs audit event with UPDATE type

    6. TestAuth2FAErrorHandling (3 tests):
       - Invalid code format validation
       - Missing code field validation
       - Unauthorized access returns 401

    **Mocking Strategy:**
    - Use `patch('api.auth_2fa_routes.pyotp')` for TOTP
    - Use `patch('api.auth_2fa_routes.audit_service')` for audit
    - Mock User with two_factor_enabled, two_factor_secret, two_factor_backup_codes

    **Verify Pattern from 194-07:**
    - TestClient with dependency override
    - AsyncMock for async operations
    - In-memory SQLite for database operations
  </action>
  <verify>
    cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/api/test_auth_2fa_routes_coverage.py -v --cov=api/auth_2fa_routes --cov-report=term-missing
  </verify>
  <done>
    - 21+ tests created
    - 80%+ coverage for auth_2fa_routes.py
    - All TOTP operations properly mocked
    - Audit events verified
  </done>
</task>

</tasks>

<verification>
1. Test file created with 400+ lines
2. 21+ tests covering all 2FA endpoints
3. 80%+ line coverage achieved
4. pyotp mocking works correctly
5. Audit service calls verified
6. All tests pass (100% pass rate)
</verification>

<success_criteria>
- **Coverage:** 80%+ for auth_2fa_routes.py (124 lines)
- **Tests:** 21+ tests across 6 test classes
- **Pass Rate:** 100%
- **Endpoints:** All 4 endpoints tested (GET /status, POST /setup, POST /enable, POST /disable)
</success_criteria>

<output>
After completion, create `.planning/phases/195-coverage-push-api-routes/195-01-SUMMARY.md`
</output>
