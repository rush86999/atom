---
phase: 195-coverage-push-api-routes
plan: 04
type: execute
wave: 2
depends_on: [195-01]
files_modified:
  - backend/tests/api/test_admin_skill_routes_coverage.py
autonomous: true

must_haves:
  truths:
    - "Admin skill routes have comprehensive test coverage (70%+)"
    - "Super admin permission check is tested"
    - "Security scanning (static + LLM) is mocked"
    - "Skill creation flow is tested end-to-end"
    - "Security policy violations are handled correctly"
  artifacts:
    - path: "backend/tests/api/test_admin_skill_routes_coverage.py"
      provides: "Admin skill routes test coverage"
      min_lines: 400
      exports: ["TestAdminSkillCreate", "TestAdminSkillSecurity", "TestAdminSkillValidation"]
  key_links:
    - from: "backend/tests/api/test_admin_skill_routes_coverage.py"
      to: "backend/api/admin/skill_routes.py"
      via: "TestClient with super admin mocking"
      pattern: "client\\.post.*'/api/admin/skills"
---

<objective>
Admin Skill Routes Coverage Enhancement

**What:** Create comprehensive test coverage for admin/skill_routes.py using FastAPI TestClient and security scanning mocking.

**Purpose:** Admin skill routes enable skill package creation with security scanning. These are critical for skill marketplace governance but lack comprehensive test coverage.

**Output:**
- New test file with 400+ lines covering admin skill endpoints
- 70%+ line coverage for admin/skill_routes.py
- Security scanning mocking (StaticAnalyzer, LLMAnalyzer)
- Super admin permission testing
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/194-coverage-push-18-22/194-07-SUMMARY.md
@.planning/phases/195-coverage-push-api-routes/195-01-SUMMARY.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/api/admin/skill_routes.py
@backend/core/skill_builder_service.py
@backend/tests/api/test_admin_skill_routes.py
</context>

<tasks>

<task type="auto">
  <name>Create Admin Skill routes coverage tests</name>
  <files>backend/tests/api/test_admin_skill_routes_coverage.py</files>
  <action>
    Create test_admin_skill_routes_coverage.py with comprehensive coverage:

    **Fixtures:**
    - `test_db()` - In-memory SQLite with StaticPool
    - `test_app()` - FastAPI app with skill routes and get_db override
    - `client()` - TestClient for endpoint testing
    - `admin_user()` - Super admin user fixture
    - `mock_skill_builder()` - Patch for skill_builder_service
    - `mock_static_analyzer()` - Patch for StaticAnalyzer
    - `mock_llm_analyzer()` - Patch for LLMAnalyzer

    **Test Classes:**

    1. TestAdminSkillCreate (4 tests):
       - Successful skill creation returns success response
       - Skill creation uses admin's tenant_id
       - Skill creation calls skill_builder_service.create_skill_package
       - Skill creation returns metadata in response

    2. TestAdminSkillSecurity (5 tests):
       - Static scan is performed on skill content
       - LLM scan is performed when ATOM_SECURITY_ENABLE_LLM_SCAN=true
       - Critical findings block skill creation (403)
       - Non-critical findings allow skill creation
       - Security scan failure logs warning but allows creation

    3. TestAdminSkillValidation (4 tests):
       - Name field is required
       - Description field is required
       - Instructions field is required
       - Scripts dict is required

    4. TestAdminSkillPermissions (3 tests):
       - Super admin can create skills
       - Non-super admin is rejected (403)
       - Unauthenticated user is rejected (401)

    5. TestAdminSkillErrorHandling (3 tests):
       - Skill builder failure returns validation error
       - Invalid security findings format handled
       - Database commit failure handled gracefully

    **Mocking Strategy:**
    - Use `patch('core.skill_builder_service.skill_builder_service')`
    - Use `patch('atom_security.analyzers.static.StaticAnalyzer')`
    - Use `patch('atom_security.analyzers.llm.LLMAnalyzer')`
    - Mock security findings with severity levels (HIGH, CRITICAL, MEDIUM, LOW)

    **Verify Pattern from 194-07 and 195-01:**
    - TestClient with dependency override
    - Super admin permission testing
    - Security scanning mocked with realistic findings
    - Error response testing (401, 403, 422)
  </action>
  <verify>
    cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/api/test_admin_skill_routes_coverage.py -v --cov=api/admin/skill_routes --cov-report=term-missing
  </verify>
  <done>
    - 19+ tests created
    - 70%+ coverage for admin/skill_routes.py
    - Security scanning properly mocked
    - Permission checks tested
  </done>
</task>

</tasks>

<verification>
1. Test file created with 400+ lines
2. 19+ tests covering admin skill endpoints
3. 70%+ line coverage achieved
4. Security scanning mocked correctly
5. Permission checks tested
6. All tests pass (100% pass rate)
</verification>

<success_criteria>
- **Coverage:** 70%+ for admin/skill_routes.py (99 lines)
- **Tests:** 19+ tests across 5 test classes
- **Pass Rate:** 100%
- **Endpoints:** POST /api/admin/skills tested
</success_criteria>

<output>
After completion, create `.planning/phases/195-coverage-push-api-routes/195-04-SUMMARY.md`
</output>
