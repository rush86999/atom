---
phase: 61-atom-saas-marketplace-sync
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_conflict_resolution_service.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All test functions use db_session fixture parameter correctly"
    - "All references to undefined 'db' variable replaced with 'db_session'"
    - "Tests run without fixture-related errors"
    - "Fixture functions (local_rating, etc.) use db_session correctly"
  artifacts:
    - path: "backend/tests/test_conflict_resolution_service.py"
      provides: "Fixed conflict resolution tests"
      contains: "def test_*\\(self, db_session"
  key_links:
    - from: "test functions"
      to: "db_session fixture"
      via: "function parameter"
      pattern: "db_session"
---

<objective>
Close Gap 4 from verification: Fix test fixture references in conflict resolution tests.

Purpose: Tests use 'db' fixture but conftest provides 'db_session'. Fixture functions reference undefined 'db' variable. Fix all references to use 'db_session' consistently.

Output: Fixed test file with all references using db_session correctly
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-atom-saas-marketplace-sync/61-04-SUMMARY.md
@.planning/phases/61-atom-saas-marketplace-sync/61-VERIFICATION.md
@backend/tests/test_conflict_resolution_service.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Fix test fixture references in conflict resolution tests</name>
  <files>backend/tests/test_conflict_resolution_service.py</files>
  <action>
Fix the test fixture references in backend/tests/test_conflict_resolution_service.py:

**Issue**: The fixture functions (local_rating, etc.) use 'db' variable but the parameter is 'db_session'. Test functions also reference 'db' instead of 'db_session'.

**Fixes needed**:

1. **Fix fixture functions** (lines 54-65):
   - Change: `def local_rating(db_session):` (already correct)
   - Change: `db.add(rating)` -> `db_session.add(rating)`
   - Change: `db.commit()` -> `db_session.commit()`

2. **Fix all test function bodies** that reference `db`:
   - Find all: `ConflictResolutionService(db)` -> `ConflictResolutionService(db_session)`
   - Find all: `RatingSyncService(db)` -> `RatingSyncService(db_session)`
   - Find all: `db.query(ConflictLog)` -> `db_session.query(ConflictLog)`

3. **Verify no remaining bare `db` references**:
   - The only `db` references should be in `db_session` parameter names
   - No standalone `db.add`, `db.commit`, `db.query`, etc.

**Pattern to apply**:
```bash
# Run these replacements:
sed -i '' 's/\bConflictResolutionService(db\b)/ConflictResolutionService(db_session)/g' backend/tests/test_conflict_resolution_service.py
sed -i '' 's/\bRatingSyncService(db\b)/RatingSyncService(db_session)/g' backend/tests/test_conflict_resolution_service.py
sed -i '' 's/\bdb\.query(/db_session.query(/g' backend/tests/test_conflict_resolution_service.py
sed -i '' 's/\bdb\.add(/db_session.add(/g' backend/tests/test_conflict_resolution_service.py
sed -i '' 's/\bdb\.commit()/db_session.commit()/g' backend/tests/test_conflict_resolution_service.py
```

**After fixing, verify**:
- No test function has bare `db` references
- All test functions use `db_session` parameter
- All fixture functions use `db_session` variable
  </action>
  <verify>pytest backend/tests/test_conflict_resolution_service.py -v --tb=short 2>&1 | head -50</verify>
  <done>All tests pass, no fixture-related errors, grep shows no bare 'db' references</done>
</task>

</tasks>

<verification>
After completion:
1. Run pytest to verify all tests pass
2. Grep for bare 'db' references (should find none except in parameter names)
3. Confirm gap 4 from VERIFICATION.md is closed
</verification>

<success_criteria>
- [ ] All test functions use db_session parameter correctly
- [ ] All fixture bodies use db_session variable
- [ ] No bare 'db' references in test code
- [ ] All 36 tests pass
- [ ] Gap 4 from VERIFICATION.md marked as closed
</success_criteria>

<output>
After completion, create `.planning/phases/61-atom-saas-marketplace-sync/61-08-SUMMARY.md`
</output>
