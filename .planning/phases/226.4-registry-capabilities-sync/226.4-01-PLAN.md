---
phase: 226.4-registry-capabilities-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/models.py
  - backend/alembic/versions/226403220000_add_capabilities_and_exclusion.py
  - backend/core/provider_auto_discovery.py
  - backend/core/benchmarks.py
autonomous: true

must_haves:
  truths:
    - "ModelCatalog has capabilities column storing JSON array of capability tags"
    - "ModelCatalog has exclude_from_general_routing boolean column defaulting to False"
    - "ProviderAutoDiscovery auto-detects capabilities from model metadata and sets flags"
    - "LUX model auto-detected as computer_use capability with exclude_from_general_routing=True"
    - "benchmarks.py has MODEL_CAPABILITY_SCORES dictionary with capability-specific quality scores"
    - "get_capability_score(model_id, capability) function returns capability-specific scores"
    - "Database migration successfully applies new columns to existing model_catalog table"
  artifacts:
    - path: "backend/core/models.py"
      provides: "ModelCatalog with capabilities and exclude_from_general_routing columns"
      contains: "capabilities = Column(JSON)"
      contains: "exclude_from_general_routing = Column(Boolean, default=False)"
    - path: "backend/alembic/versions/226403220000_add_capabilities_and_exclusion.py"
      provides: "Alembic migration for new columns"
      exports: ["upgrade", "downgrade"]
    - path: "backend/core/provider_auto_discovery.py"
      provides: "Capability auto-detection from model metadata"
      contains: "_detect_capabilities"
      contains: "computer_use"
    - path: "backend/core/benchmarks.py"
      provides: "Capability-specific quality scores"
      contains: "MODEL_CAPABILITY_SCORES"
      contains: "get_capability_score"
  key_links:
    - from: "backend/core/provider_auto_discovery.py"
      to: "backend/core/models.py"
      via: "ModelCatalog capabilities column population"
      pattern: "capabilities.*=.*_detect_capabilities"
    - from: "backend/core/benchmarks.py"
      to: "backend/core/llm/byok_handler.py"
      via: "get_capability_score function for BPC routing"
      pattern: "get_capability_score\(model_id, capability\)"
---

<objective>
Add capability-based routing infrastructure to ModelCatalog with auto-detection of specialized model capabilities (chat, vision, computer_use, browser_use) and exclusion flags to prevent specialized models from appearing in general routing.

Purpose: Enable LUX (computer_use specialized) and other specialized models to be properly categorized and excluded from general text routing while being available for their specific capabilities.

Output: Updated ModelCatalog schema, migration, auto-detection logic, and capability-specific quality scores.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Related Phase Summaries
@.planning/phases/226.1-provider-registry-foundation/226.1-01-SUMMARY.md
@.planning/phases/226.2-lux-integration-routing/226.2-01-SUMMARY.md

# Existing Implementation
@backend/core/models.py (lines 8135-8186 - ProviderRegistry, ModelCatalog)
@backend/core/provider_auto_discovery.py (lines 88-138 - extraction logic)
@backend/core/benchmarks.py (MODEL_QUALITY_SCORES dictionary)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add capabilities and exclude_from_general_routing to ModelCatalog</name>
  <files>backend/core/models.py</files>
  <action>
    Modify the ModelCatalog class in backend/core/models.py (around line 8162) to add two new columns:

    1. **capabilities**: JSON column storing array of capability tags
       - Default: ["chat"] for general-purpose models
       - Examples: ["vision"], ["computer_use", "browser_use"], ["chat", "tools"]
       - Capabilities: chat, vision, tools, computer_use, browser_use, embedding, audio

    2. **exclude_from_general_routing**: Boolean column
       - Default: False
       - When True: model excluded from general text routing (chat, summarization)
       - Derived: Automatically True if capabilities does NOT include "chat"

    Add these columns after line 8175 (after mode column), before source column.
    Use SQLAlchemy JSON type for capabilities, Boolean for exclude flag.

    DO NOT modify existing columns to maintain backward compatibility.
  </action>
  <verify>
    grep -n "capabilities = Column(JSON)" backend/core/models.py
    grep -n "exclude_from_general_routing = Column(Boolean" backend/core/models.py
  </verify>
  <done>
    ModelCatalog class has capabilities JSON column and exclude_from_general_routing Boolean column with proper defaults.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Alembic migration for new columns</name>
  <files>backend/alembic/versions/226403220000_add_capabilities_and_exclusion.py</files>
  <action>
    Create a new Alembic migration file:
    - Filename: backend/alembic/versions/226403220000_add_capabilities_and_exclusion.py
    - Revision: 226403220000
    - Down revision: 226103220000 (from Phase 226.1)

    Migration steps:
    1. Add capabilities JSON column to model_catalog table with default '["chat"]'
    2. Add exclude_from_general_routing Boolean column to model_catalog table with default False
    3. Create index on exclude_from_general_routing for filtering performance

    Pattern to follow: Use existing Phase 226.1 migration structure (226103220000_add_provider_registry.py)

    Include upgrade() and downgrade() methods.
  </action>
  <verify>
    alembic upgrade head
    sqlite3 backend/atom_dev.db "PRAGMA table_info(model_catalog);" | grep -E "capabilities|exclude_from_general_routing"
  </verify>
  <done>
    Migration applies successfully, new columns exist in model_catalog table with proper defaults.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add capability detection to ProviderAutoDiscovery</name>
  <files>backend/core/provider_auto_discovery.py</files>
  <action>
    Modify _extract_model_from_pricing method in ProviderAutoDiscovery to auto-detect capabilities:

    Add new method `_detect_capabilities(model_name: str, pricing: Dict) -> List[str]` that:
    1. Starts with default ["chat"] capability
    2. Adds "vision" if mode="vision" or pricing.get("supports_vision")=True
    3. Adds "tools" if pricing.get("supports_function_calling")=True
    4. Adds "computer_use" if model_name.startswith("lux") (LUX specialization per Decision 2)
    5. Adds "browser_use" if model_name.startswith("lux") (LUX supports both)
    6. Removes "chat" if model is vision-only (mode="vision" and no chat support)

    Special handling for LUX (Decision 2):
    - LUX capabilities: ["computer_use", "browser_use"] (NO "chat")
    - exclude_from_general_routing: True (derived from lack of chat capability)

    Update _extract_model_from_pricing to populate:
    - "capabilities": _detect_capabilities(model_name, pricing)
    - "exclude_from_general_routing": "chat" not in capabilities

    Reference existing extraction pattern at lines 112-138.
  </action>
  <verify>
    python -c "from core.provider_auto_discovery import ProviderAutoDiscovery; pad = ProviderAutoDiscovery(); print(pad._detect_capabilities('lux-1.0', {'mode': 'chat', 'supports_function_calling': True}))"
  </verify>
  <done>
    _detect_capabilities returns ["computer_use", "browser_use"] for LUX models, ["chat", "vision", "tools"] for GPT-4o, etc.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add MODEL_CAPABILITY_SCORES to benchmarks.py</name>
  <files>backend/core/benchmarks.py</files>
  <action>
    Add capability-specific quality scores to backend/core/benchmarks.py:

    1. Create MODEL_CAPABILITY_SCORES dictionary after MODEL_QUALITY_SCORES:
       ```python
       MODEL_CAPABILITY_SCORES = {
           "computer_use": {
               "lux-1.0": 95,  # Specialized for computer use
               "claude-3.5-sonnet": 85,  # Good but not specialized
               "gpt-4o": 80,
           },
           "vision": {
               "gpt-4o": 95,
               "claude-3.5-sonnet": 90,
               "gemini-2.0-flash": 88,
               "lux-1.0": 85,  # Has vision but not specialized for it
           },
           "tools": {
               "claude-3.5-sonnet": 93,
               "gpt-4o": 91,
               "gemini-2.0-flash": 85,
           },
       }
       ```

    2. Add get_capability_score(model_id: str, capability: str) -> int:
       - Returns capability-specific score if model_id exists in MODEL_CAPABILITY_SCORES[capability]
       - Falls back to MODEL_QUALITY_SCORES[model_id] if capability-specific score missing
       - Returns 70 as floor for unknown models (same as get_quality_score)

    Pattern reference: Existing get_quality_score function at lines 48-73.
  </action>
  <verify>
    python -c "from core.benchmarks import get_capability_score; print(get_capability_score('lux-1.0', 'computer_use')); print(get_capability_score('lux-1.0', 'vision'))"
  </verify>
  <done>
    get_capability_score('lux-1.0', 'computer_use') returns 95; get_capability_score('lux-1.0', 'vision') returns 85 (fallback to MODEL_QUALITY_SCORES).
  </done>
</task>

</tasks>

<verification>
1. Run migration: alembic upgrade head
2. Test capability detection: LUX models get ["computer_use", "browser_use"] without "chat"
3. Verify capability scores: get_capability_score returns correct values for all capabilities
4. Test backward compatibility: Existing models without explicit capabilities default to ["chat"]
</verification>

<success_criteria>
- ModelCatalog schema extended with capabilities (JSON) and exclude_from_general_routing (Boolean)
- Migration applies cleanly without data loss
- LUX models auto-detected as computer_use specialized, excluded from general routing
- MODEL_CAPABILITY_SCORES provides capability-specific quality scores
- get_capability_score function with proper fallback implemented
- All existing tests pass (no regression in Phase 226.1/226.2 functionality)
</success_criteria>

<output>
After completion, create `.planning/phases/226.4-registry-capabilities-sync/226.4-01-SUMMARY.md`
</output>
