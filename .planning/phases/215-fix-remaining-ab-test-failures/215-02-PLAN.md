---
phase: 215-fix-remaining-ab-test-failures
plan: 02
type: execute
wave: 2
depends_on: ["215-01"]
files_modified:
  - backend/tests/api/test_ab_testing_routes.py
autonomous: true

must_haves:
  truths:
    - "Both TestStartTest fixtures mock start_test() method properly"
    - "Tests return expected response data without database access"
    - "All 10 previously failing A/B tests now pass"
  artifacts:
    - path: "backend/tests/api/test_ab_testing_routes.py"
      provides: "TestStartTest fixtures with start_test mocking"
      contains: "mock_service.start_test.return_value"
  key_links:
    - from: "backend/tests/api/test_ab_testing_routes.py"
      to: "core.ab_testing_service.ABTestingService.start_test"
      via: "MagicMock return_value"
      pattern: "mock_service\\.start_test\\.return_value"
---

<objective>
Fix TestStartTest fixtures (2 tests) by adding start_test method mocks to return proper test data without database access.

Purpose: Tests currently fail because the mock service doesn't return data for start_test() calls, causing "Test not found" errors. By properly mocking start_test(), tests receive expected response data.

Output: 2 TestStartTest fixtures with proper start_test mocks, completing the fix for all 10 failing A/B tests.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/215-fix-remaining-ab-test-failures/215-RESEARCH.md
@.planning/phases/215-fix-remaining-ab-test-failures/215-CONTEXT.md
@.planning/phases/215-fix-remaining-ab-test-failures/215-01-SUMMARY.md
@backend/core/ab_testing_service.py
@backend/tests/api/test_ab_testing_routes.py
</context>

<tasks>

<task type="auto">
  <name>Fix 2 TestStartTest fixtures with start_test mocks</name>
  <files>backend/tests/api/test_ab_testing_routes.py</files>
  <action>
    For the 2 failing TestStartTest methods, add start_test mock BEFORE making the API request.

    The 2 tests to fix (both already have partial mocking but need it moved before client.post):

    1. test_start_test_success (line 275):
       - The mock_service.start_test.return_value is already present (line 281-286)
       - BUT it's INSIDE the with block, which is correct
       - The issue is likely timing - verify the mock is properly set
       - Keep the existing mock but ensure it's complete

    2. test_start_test_includes_timestamp (line 295):
       - Similar to above, mock exists at line 301-307
       - Verify the mock structure matches what the endpoint expects

    NOTE: Research shows these tests ALREADY have start_test mocks. Investigate why they still fail:
    - Check if the mock is actually being used
    - Verify the return value structure matches ABTestingService.start_test() return format
    - Ensure the test isn't falling through to a different error path

    The expected return format from start_test():
    ```python
    {
        "test_id": "test-123",
        "name": "Test A",
        "status": "running",
        "started_at": "2026-03-12T10:00:00Z"  # ISO format string
    }
    ```

    If tests already have correct mocks, the issue may be with the API endpoint not using the mocked service correctly. Check the import path in the API routes.
  </action>
  <verify>
    Run specific tests: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/api/test_ab_testing_routes.py::TestStartTest::test_start_test_success backend/tests/api/test_ab_testing_routes.py::TestStartTest::test_start_test_includes_timestamp -v

    Expected: Both tests pass with 200 status
  </verify>
  <done>
    Both TestStartTest fixtures have proper start_test mocks. Tests pass without database access.
  </done>
</task>

<task type="auto">
  <name>Run full A/B test suite verification</name>
  <files>backend/tests/api/test_ab_testing_routes.py</files>
  <action>
    Run the complete A/B testing test suite to verify all 10 previously failing tests now pass.

    Command: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/api/test_ab_testing_routes.py -v

    Verify:
    1. All 8 TestCreateTest tests pass
    2. All 5 TestStartTest tests pass (including the 2 fixed)
    3. No database-related errors in output
    4. Overall 100% pass rate for the 10 previously failing tests
  </action>
  <verify>
    Full test run output showing:
    - All TestCreateTest tests: PASSED
    - All TestStartTest tests: PASSED
    - No "no such table: agent_registry" errors
    - No "Test not found" errors
  </verify>
  <done>
    All 10 previously failing A/B tests now pass. 100% pass rate achieved.
  </done>
</task>

</tasks>

<verification>
After execution, verify:
1. All 10 previously failing tests pass
2. Complete test suite runs without database errors
3. Test execution time remains fast (<5 seconds)
4. Production code unchanged
</verification>

<success_criteria>
- [ ] test_start_test_success passes
- [ ] test_start_test_includes_timestamp passes
- [ ] All 8 TestCreateTest tests still passing (regression check)
- [ ] Full A/B test suite: 100% pass rate
- [ ] No database queries during test execution
- [ ] Production code unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/215-fix-remaining-ab-test-failures/215-02-SUMMARY.md` and overall phase summary `215-SUMMARY.md`
</output>
