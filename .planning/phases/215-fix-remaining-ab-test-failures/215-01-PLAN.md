---
phase: 215-fix-remaining-ab-test-failures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_ab_testing_routes.py
autonomous: true

must_haves:
  truths:
    - "All 8 TestCreateTest fixtures mock database query for agent lookup"
    - "Tests create mock agent with required AgentRegistry attributes",
    - "No database queries occur during test execution"
    - "Tests pass with mocked agent lookup"
  artifacts:
    - path: "backend/tests/api/test_ab_testing_routes.py"
      provides: "TestCreateTest fixtures with proper agent lookup mocking"
      contains: "mock_service.db.query"
  key_links:
    - from: "backend/tests/api/test_ab_testing_routes.py"
      to: "core.ab_testing_service.ABTestingService"
      via: "patch decorator"
      pattern: "patch\\('core\\.ab_testing_service\\.ABTestingService'\\)"
---

<objective>
Fix TestCreateTest fixtures (8 tests) by adding agent lookup mocks to prevent database queries during test execution.

Purpose: Tests currently fail because ABTestingService.create_test() queries the database to verify agent exists, but the test database doesn't have the agent_registry table. By mocking the database query, we isolate tests from database dependencies.

Output: 8 TestCreateTest fixtures with proper agent lookup mocks, all tests passing without database access.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/215-fix-remaining-ab-test-failures/215-RESEARCH.md
@.planning/phases/215-fix-remaining-ab-test-failures/215-CONTEXT.md
@backend/core/ab_testing_service.py
@backend/tests/api/test_ab_testing_routes.py
</context>

<tasks>

<task type="auto">
  <name>Fix 8 TestCreateTest fixtures with agent lookup mocks</name>
  <files>backend/tests/api/test_ab_testing_routes.py</files>
  <action>
    For each of the 8 TestCreateTest test methods, add agent lookup mocking BEFORE the existing create_test.return_value mock.

    The pattern to add (after MockService.return_value = mock_service):
    ```python
    # Mock agent lookup to prevent database query
    mock_agent = MagicMock()
    mock_agent.id = "agent-1"  # or "agent-2" for test_create_test_with_all_fields
    mock_agent.name = "Test Agent"
    mock_agent.status = "active"
    mock_agent.confidence_score = 0.8
    mock_service.db.query.return_value.filter.return_value.first.return_value = mock_agent
    ```

    The 8 tests to fix:
    1. test_create_test_success (line 32) - use agent-1
    2. test_create_test_with_all_fields (line 65) - use agent-2
    3. test_create_test_default_values (line 100) - use agent-1
    4. test_create_test_prompt_type (line 130) - use agent-1
    5. test_create_test_agent_config_type (line 161) - use agent-1
    6. test_create_test_validation_error (line 192) - use agent-1 (or None for invalid case)
    7. test_create_test_traffic_percentage_validation (line 214) - use agent-1
    8. test_create_test_confidence_validation (line 237) - use agent-1

    NOTE: For test_create_test_validation_error, the agent_id is empty string "", so return None for the mock agent.

    DO NOT modify production code (ab_testing_service.py or models.py).
  </action>
  <verify>
    Run specific tests: PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/api/test_ab_testing_routes.py::TestCreateTest -v

    Expected: All 8 tests pass (or fail on validation logic only, not database errors)
  </verify>
  <done>
    All 8 TestCreateTest fixtures have agent lookup mocks added. Tests no longer fail with "no such table: agent_registry" errors.
  </done>
</task>

</tasks>

<verification>
After execution, verify:
1. All 8 TestCreateTest tests pass without database errors
2. No "no such table: agent_registry" errors in test output
3. Test execution remains fast (no real DB queries)
</verification>

<success_criteria>
- [ ] All 8 TestCreateTest tests pass
- [ ] No database schema errors in test output
- [ ] Tests are properly isolated (no real DB queries)
- [ ] Production code unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/215-fix-remaining-ab-test-failures/215-01-SUMMARY.md`
</output>
