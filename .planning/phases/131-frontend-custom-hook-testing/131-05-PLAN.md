---
phase: 131-frontend-custom-hook-testing
plan: 05
type: execute
wave: 4
depends_on: [131-02]
files_modified:
  - frontend-nextjs/hooks/__tests__/useCommunicationSearch.test.ts
  - frontend-nextjs/hooks/__tests__/useMemorySearch.test.ts
  - frontend-nextjs/hooks/__tests__/useSecurityScanner.test.ts
  - frontend-nextjs/hooks/__tests__/useCliHandler.test.ts
autonomous: true

must_haves:
  truths:
    - "Search hooks test query handling and result state"
    - "Security scanner tests both desktop and web modes"
    - "Tauri bridge mocking works correctly"
    - "CLI handler tests checkCli on mount"
    - "Search options (tag, appId, limit) are tested"
  artifacts:
    - path: frontend-nextjs/hooks/__tests__/useCommunicationSearch.test.ts
      provides: "Communication search tests"
      min_lines: 80
    - path: frontend-nextjs/hooks/__tests__/useMemorySearch.test.ts
      provides: "Memory search tests"
      min_lines: 90
    - path: frontend-nextjs/hooks/__tests__/useSecurityScanner.test.ts
      provides: "Security scanner tests"
      min_lines: 120
    - path: frontend-nextjs/hooks/__tests__/useCliHandler.test.ts
      provides: "CLI handler tests"
      min_lines: 60
  key_links:
    - from: frontend-nextjs/hooks/__tests__/useCommunicationSearch.test.ts
      to: frontend-nextjs/hooks/useCommunicationSearch.ts
      via: "fetch mock with query encoding"
      pattern: "encodeURIComponent.*searchMessages"
    - from: frontend-nextjs/hooks/__tests__/useSecurityScanner.test.ts
      to: frontend-nextjs/hooks/useSecurityScanner.ts
      via: "Tauri invoke and fetch mocks"
      pattern: "__TAURI__.*core\\.invoke.*execute_command"
---

<objective>
Create test files for search and security hooks (useCommunicationSearch, useMemorySearch, useSecurityScanner, useCliHandler) covering search functionality, Tauri bridge mocking, and CLI integration.

Purpose: Test hooks that handle search queries, security scanning with desktop/web mode detection, and Tauri CLI integration, requiring complex mocking strategies.

Output: Four new test files with comprehensive search and security testing.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-frontend-custom-hook-testing/131-RESEARCH.md
@.planning/phases/131-frontend-custom-hook-testing/131-02-SUMMARY.md

@frontend-nextjs/hooks/useCommunicationSearch.ts
@frontend-nextjs/hooks/useMemorySearch.ts
@frontend-nextjs/hooks/useSecurityScanner.ts
@frontend-nextjs/hooks/useCliHandler.ts
@frontend-nextjs/hooks/__tests__/useChatMemory.test.ts
@frontend-nextjs/jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Create useCommunicationSearch.test.ts with search query testing</name>
  <files>frontend-nextjs/hooks/__tests__/useCommunicationSearch.test.ts</files>
  <action>
    Create test file for useCommunicationSearch hook at frontend-nextjs/hooks/__tests__/useCommunicationSearch.test.ts

    Test coverage:
    1. Search functionality tests:
       - searchMessages calls API with encoded query
       - Sets results state from response
       - Sets isSearching during request
       - Resets isSearching after completion

    2. Empty query handling:
       - Returns early when query is empty/whitespace
       - Clears results for empty query
       - Does not call API for empty query

    3. Search result structure:
       - SearchResult interface fields
       - Handles data.success and data.results
       - Clears results on API failure

    4. Error handling:
       - Handles fetch errors gracefully
       - Shows toast error on failure
       - Sets isSearching to false on error

    5. API URL construction:
       - Correct endpoint: /api/atom/communication/memory/search
       - Query parameter encoding
       - Limit parameter (20)

    Mock fetch globally
    Mock toast from 'sonner'
    Test query encoding with special characters
  </action>
  <verify>
    npm test -- useCommunicationSearch.test.ts
  </verify>
  <done>
    - All 10+ tests pass
    - Query encoding verified
    - Empty query handling tested
    - Error states covered
    - Coverage >85% for useCommunicationSearch.ts
  </done>
</task>

<task type="auto">
  <name>Create useMemorySearch.test.ts with search options testing</name>
  <files>frontend-nextjs/hooks/__tests__/useMemorySearch.test.ts</files>
  <action>
    Create test file for useMemorySearch hook at frontend-nextjs/hooks/__tests__/useMemorySearch.test.ts

    Test coverage:
    1. Search functionality tests:
       - searchMemory calls API with query
       - Includes tag parameter when provided
       - Includes appId parameter when provided
       - Includes limit parameter (default 20)
       - Sets results state from response

    2. Options handling:
       - tag option appended to URL
       - appId option appended to URL
       - Custom limit value used
       - Default limit is 20

    3. Empty query handling:
       - Clears results for empty query
       - Does not call API for empty query

    4. Clear search tests:
       - clearSearch() clears results array
       - Does not call API

    5. Result structure:
       - MemorySearchResult interface fields
       - Handles optional fields (subject, tags, metadata)

    6. Error handling:
       - Handles fetch errors
       - Shows toast error on failure
       - Sets isSearching to false on error

    Mock fetch globally
    Test URL construction with multiple options
    Mock toast from 'sonner'
  </action>
  <verify>
    npm test -- useMemorySearch.test.ts
  </verify>
  <done>
    - All 12+ tests pass
    - All search options tested
    - URL construction verified
    - clearSearch function tested
    - Coverage >85% for useMemorySearch.ts
  </done>
</task>

<task type="auto">
  <name>Create useSecurityScanner.test.ts with Tauri and fetch mocks</name>
  <files>frontend-nextjs/hooks/__tests__/useSecurityScanner.test.ts</files>
  <action>
    Create test file for useSecurityScanner hook at frontend-nextjs/hooks/__tests__/useSecurityScanner.test.ts

    Test coverage:
    1. Desktop mode (Tauri) tests:
       - Detects __TAURI__ in window
       - Attempts local CLI scanner first
       - Calls invoke for write_file_content
       - Calls invoke for execute_command with atom_security
       - Parses JSON output from scan
       - Maps findings to Finding interface

    2. Web mode/fallback tests:
       - Falls back to API when Tauri unavailable
       - Calls /api/protection/scan endpoint
       - Correctly formats POST body
       - Maps API response to results

    3. Scan function tests:
       - scanSkill sets isScanning to true
       - Accepts skillName, instructionBody, fileContents
       - Sets results state on completion
       - Sets isScanning to false in finally

    4. Tauri bridge mocking:
       - Mock window.__TAURI__.core.invoke
       - Mock window.__TAURI__.invoke (legacy)
       - Test both invoke patterns
       - Handle import from @tauri-apps/api/core

    5. Result structure:
       - isSafe boolean
       - findings array with Finding interface
       - Finding: category, severity, description, analyzer

    6. Error handling:
       - Handles Tauri invoke errors
       - Falls back to web API on Tauri failure
       - Shows toast error on failure
       - Sets isScanning to false on error

    7. Edge cases:
       - Missing fileContents defaults to instructionBody
       - Handles missing main.py in fileContents
       - JSON parse error handling

    Mock window.__TAURI__ with both core.invoke and legacy invoke
    Mock fetch for web mode
    Mock toast from 'sonner'
  </action>
  <verify>
    npm test -- useSecurityScanner.test.ts
  </verify>
  <done>
    - All 15+ tests pass
    - Desktop mode tested
    - Web mode tested
    - Tauri invoke mocking works
    - Fallback logic verified
    - Coverage >85% for useSecurityScanner.ts
  </done>
</task>

<task type="auto">
  <name>Create useCliHandler.test.ts with CLI integration testing</name>
  <files>frontend-nextjs/hooks/__tests__/useCliHandler.test.ts</files>
  <action>
    Create test file for useCliHandler hook at frontend-nextjs/hooks/__tests__/useCliHandler.test.ts

    Test coverage:
    1. CLI detection tests:
       - Checks window.__TAURI__ on mount
       - Returns early when not in Tauri
       - Does not call getMatches when __TAURI__ missing

    2. Scan command handling:
       - Imports @tauri-apps/plugin-cli getMatches
       - Checks for 'scan' subcommand
       - Extracts path from arguments
       - Calls invoke for execute_command

    3. Toast notifications:
       - Shows loading toast during scan
       - Shows success toast on completion
       - Shows error toast on failure

    4. Command execution:
       - Calls invoke('execute_command') with python3
       - Passes atom_security module arguments
       - Uses correct format (text)

    5. Error handling:
       - Handles missing getMatches gracefully
       - Handles invoke errors
       - Logs errors to console
       - Shows error toast

    6. Session dependency:
       - Runs effect when session changes
       - Uses session from useSession

    Mock window.__TAURI__ with invoke
    Mock @tauri-apps/plugin-cli getMatches
    Mock useSession from 'next-auth/react'
    Mock toast from 'sonner'
    Test effect cleanup
  </action>
  <verify>
    npm test -- useCliHandler.test.ts
  </verify>
  <done>
    - All 10+ tests pass
    - Tauri CLI detection tested
    - Scan command handling verified
    - Toast notifications tested
    - Coverage >85% for useCliHandler.ts
  </done>
</task>

</tasks>

<verification>
1. Run npm test -- hooks/__tests__/useCommunicationSearch.test.ts --coverage
2. Run npm test -- hooks/__tests__/useMemorySearch.test.ts --coverage
3. Run npm test -- hooks/__tests__/useSecurityScanner.test.ts --coverage
4. Run npm test -- hooks/__tests__/useCliHandler.test.ts --coverage
5. Verify all tests pass independently
6. Check coverage report shows >85% for each hook
7. Verify Tauri mocks work correctly
</verification>

<success_criteria>
- Four new test files created
- Search query handling fully tested
- Tauri bridge mocking works for both invoke patterns
- Desktop/web mode detection tested for security scanner
- CLI handler tested with getMatches mocking
- Coverage threshold of 85% met for all four hooks
- All mocks properly cleared in beforeEach
</success_criteria>

<output>
After completion, create `.planning/phases/131-frontend-custom-hook-testing/131-05-SUMMARY.md`
</output>
