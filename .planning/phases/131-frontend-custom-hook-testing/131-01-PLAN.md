---
phase: 131-frontend-custom-hook-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/hooks/__tests__/use-toast.test.ts
  - frontend-nextjs/hooks/__tests__/useUndoRedo.test.ts
  - frontend-nextjs/hooks/__tests__/useVoiceIO.test.ts
autonomous: true

must_haves:
  truths:
    - "Simple state hooks have isolated test files covering all state transitions"
    - "Timer-based cleanup is tested to prevent memory leaks"
    - "Wrapper hooks test delegation to underlying hooks"
    - "All hooks with setTimeout/setInterval test cleanup functions"
  artifacts:
    - path: frontend-nextjs/hooks/__tests__/use-toast.test.ts
      provides: "Toast notification hook tests"
      min_lines: 80
    - path: frontend-nextjs/hooks/__tests__/useUndoRedo.test.ts
      provides: "Undo/redo state machine tests"
      min_lines: 120
    - path: frontend-nextjs/hooks/__tests__/useVoiceIO.test.ts
      provides: "Voice I/O wrapper tests"
      min_lines: 40
  key_links:
    - from: frontend-nextjs/hooks/__tests__/use-toast.test.ts
      to: frontend-nextjs/hooks/use-toast.ts
      via: "renderHook with jest.useFakeTimers()"
      pattern: "setTimeout.*dismiss"
    - from: frontend-nextjs/hooks/__tests__/useUndoRedo.test.ts
      to: frontend-nextjs/hooks/useUndoRedo.ts
      via: "renderHook state transition testing"
      pattern: "undo.*redo.*takeSnapshot"
---

<objective>
Create test files for simple state management hooks (use-toast, useUndoRedo, useVoiceIO) covering all state transitions, timer cleanup, and delegation patterns.

Purpose: Establish baseline testing patterns for simple hooks before progressing to complex async and browser API hooks. These hooks are ideal starting points as they have minimal dependencies and clear state machine patterns.

Output: Three new test files with comprehensive coverage of mount/update/unmount lifecycles.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-frontend-custom-hook-testing/131-RESEARCH.md

@frontend-nextjs/hooks/use-toast.ts
@frontend-nextjs/hooks/useUndoRedo.ts
@frontend-nextjs/hooks/useVoiceIO.ts
@frontend-nextjs/hooks/__tests__/useCanvasState.api.test.ts
@frontend-nextjs/hooks/__tests__/useChatMemory.test.ts
@frontend-nextjs/hooks/__tests__/useWebSocket.test.ts
@frontend-nextjs/jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Create use-toast.test.ts with timer cleanup testing</name>
  <files>frontend-nextjs/hooks/__tests__/use-toast.test.ts</files>
  <action>
    Create test file for use-toast hook at frontend-nextjs/hooks/__tests__/use-toast.test.ts

    Test coverage:
    1. Initialization tests:
       - Initial empty toasts array
       - toast function exists
       - dismiss function exists

    2. Toast creation tests:
       - Creates toast with correct structure (id, title, description, variant, duration)
       - Adds toast to toasts array
       - Generates unique IDs (test multiple toasts have different IDs)
       - Uses default variant 'default'
       - Uses default duration 3000ms

    3. Toast dismissal tests:
       - Removes toast by ID from array
       - Only removes specified toast
       - Handles non-existent ID gracefully

    4. Timer cleanup tests (CRITICAL - memory leak prevention):
       - Use jest.useFakeTimers() for setTimeout testing
       - Toast auto-dismisses after duration
       - Cleanup function called on unmount
       - Multiple timers work independently
       - Zero duration means no auto-dismiss

    5. Edge cases:
       - Empty title/description handling
       - Destructive variant
       - Multiple concurrent toasts

    Pattern from existing useCanvasState.api.test.ts:
    - Use renderHook from @testing-library/react
    - Use act() for state updates
    - Use jest.useFakeTimers() for timer testing
    - Use waitFor for async expectations
  </action>
  <verify>
    npm test -- use-toast.test.ts
  </verify>
  <done>
    - All 10+ tests pass
    - Timer cleanup verified with jest.useFakeTimers()
    - Unmount cleanup tested
    - Coverage >85% for use-toast.ts
  </done>
</task>

<task type="auto">
  <name>Create useUndoRedo.test.ts with history stack testing</name>
  <files>frontend-nextjs/hooks/__tests__/useUndoRedo.test.ts</files>
  <action>
    Create test file for useUndoRedo hook at frontend-nextjs/hooks/__tests__/useUndoRedo.test.ts

    Test coverage:
    1. Initialization tests:
       - Initial state matches initialState prop
       - Empty past and future arrays
       - canUndo and canRedo are false

    2. Take snapshot tests:
       - Saves current state to past
       - Updates present state
       - Clears future array
       - Limits history to 50 entries
       - Correctly handles FlowState with nodes and edges

    3. Undo tests:
       - Moves present to future
       - Restores previous state from past
       - canUndo becomes false when past empty
       - canRedo becomes true after undo
       - No-op when canUndo is false

    4. Redo tests:
       - Moves present back to past
       - Restores future state to present
       - canRedo becomes false when future empty
       - canUndo becomes true after redo
       - No-op when canRedo is false

    5. Reset history tests:
       - Clears past and future
       - Resets present to initialState
       - canUndo and canRedo become false

    6. Edge cases:
       - Undo/redo with single state
       - Multiple snapshots in sequence
       - History limit enforcement (50 states max)

    Import types from reactflow for Node, Edge types
  </action>
  <verify>
    npm test -- useUndoRedo.test.ts
  </verify>
  <done>
    - All 15+ tests pass
    - All state transitions covered
    - History limit (50) tested
    - Edge cases covered
    - Coverage >85% for useUndoRedo.ts
  </done>
</task>

<task type="auto">
  <name>Create useVoiceIO.test.ts with delegation testing</name>
  <files>frontend-nextjs/hooks/__tests__/useVoiceIO.test.ts</files>
  <action>
    Create test file for useVoiceIO hook at frontend-nextjs/hooks/__tests__/useVoiceIO.test.ts

    Test coverage:
    1. Delegation tests (useVoiceIO is a thin wrapper):
       - Returns all useSpeechRecognition properties
       - isSupported aliases browserSupportsSpeechRecognition
       - wakeWordActive aliases wakeWordEnabled
       - toggleWakeWord delegates to setWakeWordMode

    2. Passthrough behavior:
       - All methods pass through correctly
       - Options parameter is passed through
       - Return value structure matches expected UseVoiceIOReturn

    Note: useVoiceIO simply re-exports useSpeechRecognition with renamed properties.
    Tests should verify the mapping is correct, not re-test useSpeechRecognition
    (which will have its own comprehensive test file in Plan 03).
  </action>
  <verify>
    npm test -- useVoiceIO.test.ts
  </verify>
  <done>
    - All delegation tests pass
    - Property aliases verified
    - Coverage >85% for useVoiceIO.ts (thin wrapper)
  </done>
</task>

</tasks>

<verification>
1. Run npm test -- hooks/__tests__/use-toast.test.ts --coverage
2. Run npm test -- hooks/__tests__/useUndoRedo.test.ts --coverage
3. Run npm test -- hooks/__tests__/useVoiceIO.test.ts --coverage
4. Verify all tests pass independently
5. Check coverage report shows >85% for each hook
6. Verify timer cleanup tested with jest.useFakeTimers()
</verification>

<success_criteria>
- Three new test files created
- All tests pass independently
- Timer cleanup verified for use-toast
- History stack behavior fully tested for useUndoRedo
- Delegation pattern verified for useVoiceIO
- Coverage threshold of 85% met for all three hooks
- No test interdependencies (each test file can run in isolation)
</success_criteria>

<output>
After completion, create `.planning/phases/131-frontend-custom-hook-testing/131-01-SUMMARY.md`
</output>
