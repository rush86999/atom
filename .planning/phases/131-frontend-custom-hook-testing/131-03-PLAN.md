---
phase: 131-frontend-custom-hook-testing
plan: 03
type: execute
wave: 2
depends_on: [131-01]
files_modified:
  - frontend-nextjs/hooks/__tests__/useSpeechRecognition.test.ts
  - frontend-nextjs/hooks/__tests__/useTextToSpeech.test.ts
  - frontend-nextjs/hooks/__tests__/useVoiceAgent.test.ts
autonomous: true

must_haves:
  truths:
    - "Browser API hooks mock native SpeechRecognition/SpeechSynthesis APIs"
    - "Event handlers (onresult, onerror, onend) are tested"
    - "Wake word mode logic is tested"
    - "Audio element cleanup prevents memory leaks"
    - "Browser support detection works correctly"
  artifacts:
    - path: frontend-nextjs/hooks/__tests__/useSpeechRecognition.test.ts
      provides: "Speech recognition hook tests"
      min_lines: 150
    - path: frontend-nextjs/hooks/__tests__/useTextToSpeech.test.ts
      provides: "Text-to-speech hook tests"
      min_lines: 120
    - path: frontend-nextjs/hooks/__tests__/useVoiceAgent.test.ts
      provides: "Voice agent audio player tests"
      min_lines: 80
  key_links:
    - from: frontend-nextjs/hooks/__tests__/useSpeechRecognition.test.ts
      to: frontend-nextjs/hooks/useSpeechRecognition.ts
      via: "window.SpeechRecognition mock"
      pattern: "window\\.SpeechRecognition.*webkitSpeechRecognition"
    - from: frontend-nextjs/hooks/__tests__/useTextToSpeech.test.ts
      to: frontend-nextjs/hooks/useTextToSpeech.ts
      via: "window.speechSynthesis mock"
      pattern: "speechSynthesis.*speak.*cancel"
    - from: frontend-nextjs/hooks/__tests__/useVoiceAgent.test.ts
      to: frontend-nextjs/hooks/useVoiceAgent.ts
      via: "Audio element mock"
      pattern: "new Audio\\(\\).*play\\(\\).*addEventListener"
---

<objective>
Create test files for browser API hooks (useSpeechRecognition, useTextToSpeech, useVoiceAgent) covering SpeechRecognition API, SpeechSynthesis API, and Audio element with proper cleanup.

Purpose: Test hooks that interact with browser-native APIs, requiring mocking of global window objects, event handler testing, and cleanup verification for memory leak prevention.

Output: Three new test files with comprehensive browser API testing.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-frontend-custom-hook-testing/131-RESEARCH.md
@.planning/phases/131-frontend-custom-hook-testing/131-01-SUMMARY.md

@frontend-nextjs/hooks/useSpeechRecognition.ts
@frontend-nextjs/hooks/useTextToSpeech.ts
@frontend-nextjs/hooks/useVoiceAgent.ts
@frontend-nextjs/hooks/__tests__/useCanvasState.api.test.ts
@frontend-nextjs/jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Create useSpeechRecognition.test.ts with SpeechRecognition API mock</name>
  <files>frontend-nextjs/hooks/__tests__/useSpeechRecognition.test.ts</files>
  <action>
    Create test file for useSpeechRecognition hook at frontend-nextjs/hooks/__tests__/useSpeechRecognition.test.ts

    Test coverage:
    1. Browser support detection:
       - Detects SpeechRecognition availability
       - Detects webkitSpeechRecognition as fallback
       - Sets browserSupportsSpeechRecognition correctly
       - Handles no browser support

    2. Initialization tests:
       - Creates SpeechRecognition instance
       - Sets continuous=true
       - Sets interimResults=true
       - Sets lang='en-US'

    3. Start/stop listening tests:
       - startListening() calls recognition.start()
       - Sets isListening to true
       - stopListening() calls recognition.stop()
       - Sets isListening to false
       - Handles starting when already listening
       - Handles stopping when not listening

    4. Transcript tests:
       - onresult updates transcript state
       - Handles both final and interim results
       - Accumulates transcript segments
       - resetTranscript clears transcript

    5. Wake word mode tests:
       - Wake word mode is off by default
       - setWakeWordMode enables wake word
       - Filters transcript for "atom" keyword when enabled
       - Auto-restarts on onend when wake word enabled
       - Handles "no-speech" error when wake word enabled

    6. Event handlers:
       - onerror sets isListening to false
       - onend handles auto-restart logic
       - Event handlers properly cleaned up

    7. Cleanup tests:
       - useEffect cleanup stops recognition
       - Removes event listeners on unmount

    Mock window.SpeechRecognition and window.webkitSpeechRecognition
    Create mock recognition instance with onresult, onerror, onend
    Trigger callbacks manually in tests
  </action>
  <verify>
    npm test -- useSpeechRecognition.test.ts
  </verify>
  <done>
    - All 18+ tests pass
    - All event handlers tested
    - Wake word logic verified
    - Cleanup on unmount tested
    - Coverage >85% for useSpeechRecognition.ts
  </done>
</task>

<task type="auto">
  <name>Create useTextToSpeech.test.ts with SpeechSynthesis API mock</name>
  <files>frontend-nextjs/hooks/__tests__/useTextToSpeech.test.ts</files>
  <action>
    Create test file for useTextToSpeech hook at frontend-nextjs/hooks/__tests__/useTextToSpeech.test.ts

    Test coverage:
    1. Browser support detection:
       - Detects speechSynthesis availability
       - Sets isSupported correctly
       - Handles no browser support

    2. Voice loading tests:
       - Loads voices from getVoices()
       - Handles async voice loading (onvoiceschanged)
       - Selects default English voice
       - Falls back to first available voice

    3. Speak tests:
       - speak() creates SpeechSynthesisUtterance
       - Sets utterance voice when selected
       - Sets rate=1.0, pitch=1.0, volume=1.0
       - Calls speechSynthesis.speak()
       - Cancels previous speech before speaking

    4. State management tests:
       - onstart sets isSpeaking=true, isPaused=false
       - onend sets isSpeaking=false, isPaused=false
       - onerror sets isSpeaking=false
       - pause() sets isPaused=true
       - resume() sets isPaused=false

    5. Stop tests:
       - stop() cancels speech
       - Resets isSpeaking to false
       - Resets isPaused to false

    6. Voice selection:
       - setVoice updates selectedVoice
       - Subsequent speech uses selected voice

    Mock window.speechSynthesis with getVoices, speak, cancel, pause, resume
    Mock onvoiceschanged event
    Create mock utterance with onstart, onend, onerror callbacks
  </action>
  <verify>
    npm test -- useTextToSpeech.test.ts
  </verify>
  <done>
    - All 15+ tests pass
    - All speech synthesis methods tested
    - Voice selection verified
    - State transitions covered
    - Coverage >85% for useTextToSpeech.ts
  </done>
</task>

<task type="auto">
  <name>Create useVoiceAgent.test.ts with Audio element mock</name>
  <files>frontend-nextjs/hooks/__tests__/useVoiceAgent.test.ts</files>
  <action>
    Create test file for useVoiceAgent hook at frontend-nextjs/hooks/__tests__/useVoiceAgent.test.ts

    Test coverage:
    1. Initialization tests:
       - Creates Audio element on mount
       - Adds event listeners (ended, error)
       - Initial isPlaying is false

    2. Play audio tests:
       - playAudio() handles base64 data
       - Converts base64 to Blob for better performance
       - Creates data URI for audio
       - Sets audio src correctly
       - Calls audio.play()
       - Sets isPlaying to true

    3. Audio loading tests:
       - Handles data:audio/* URIs directly
       - Creates Blob from base64 string
       - Uses URL.createObjectURL for blob
       - Sets correct MIME type (audio/mpeg)

    4. Stop audio tests:
       - stopAudio() pauses playback
       - Resets currentTime to 0
       - Sets isPlaying to false

    5. Event handler tests:
       - onended sets isPlaying to false
       - onerror sets isPlaying to false
       - Error logged to console

    6. Cleanup tests (CRITICAL):
       - useEffect cleanup removes event listeners
       - Pauses audio on unmount
       - Sets audioRef to null

    7. Edge cases:
       - Handles empty audioData
       - Handles blob creation failure
       - Falls back to data URI on error

    Mock global.Audio with mock constructor
    Mock URL.createObjectURL and URL.revokeObjectURL
    Track event listener additions/removals
  </action>
  <verify>
    npm test -- useVoiceAgent.test.ts
  </verify>
  <done>
    - All 12+ tests pass
    - Audio element lifecycle tested
    - Event handlers verified
    - Cleanup on unmount tested
    - Blob creation and fallback paths covered
    - Coverage >85% for useVoiceAgent.ts
  </done>
</task>

</tasks>

<verification>
1. Run npm test -- hooks/__tests__/useSpeechRecognition.test.ts --coverage
2. Run npm test -- hooks/__tests__/useTextToSpeech.test.ts --coverage
3. Run npm test -- hooks/__tests__/useVoiceAgent.test.ts --coverage
4. Verify all tests pass independently
5. Check coverage report shows >85% for each hook
6. Verify cleanup functions tested for all browser API hooks
</verification>

<success_criteria>
- Three new test files created
- All browser API mocks properly implemented
- Event handlers tested by direct callback invocation
- Wake word logic fully tested for useSpeechRecognition
- Audio element cleanup verified for useVoiceAgent
- Coverage threshold of 85% met for all three hooks
- Memory leak prevention verified via cleanup tests
</success_criteria>

<output>
After completion, create `.planning/phases/131-frontend-custom-hook-testing/131-03-SUMMARY.md`
</output>
