---
phase: 131-frontend-custom-hook-testing
plan: 06
type: execute
wave: 5
depends_on: [131-03, 131-02]
files_modified:
  - frontend-nextjs/hooks/__tests__/useUserActivity.test.ts
  - frontend-nextjs/hooks/__tests__/useWhatsAppWebSocket.test.ts
  - frontend-nextjs/hooks/__tests__/useWhatsAppWebSocketEnhanced.test.ts
  - frontend-nextjs/hooks/test-helpers.ts
autonomous: true

must_haves:
  truths:
    - "Complex hooks with timers test full lifecycle and cleanup"
    - "WebSocket hooks test connection, reconnection, and message handling"
    - "Event listeners are properly removed on unmount"
    - "All intervals cleared to prevent memory leaks"
    - "Test helpers created for reusable mocking patterns"
  artifacts:
    - path: frontend-nextjs/hooks/__tests__/useUserActivity.test.ts
      provides: "User activity tracking tests"
      min_lines: 150
    - path: frontend-nextjs/hooks/__tests__/useWhatsAppWebSocket.test.ts
      provides: "WhatsApp WebSocket tests"
      min_lines: 150
    - path: frontend-nextjs/hooks/__tests__/useWhatsAppWebSocketEnhanced.test.ts
      provides: "Enhanced WhatsApp WebSocket tests"
      min_lines: 180
    - path: frontend-nextjs/hooks/test-helpers.ts
      provides: "Reusable test utilities for hooks"
      min_lines: 100
  key_links:
    - from: frontend-nextjs/hooks/__tests__/useUserActivity.test.ts
      to: frontend-nextjs/hooks/useUserActivity.ts
      via: "setInterval mock and event listener spies"
      pattern: "setInterval.*30000.*addEventListener.*recordActivity"
    - from: frontend-nextjs/hooks/__tests__/useWhatsAppWebSocket.test.ts
      to: frontend-nextjs/hooks/useWhatsAppWebSocket.ts
      via: "WebSocket class mock"
      pattern: "WebSocket.*onopen.*onmessage.*onclose"
---

<objective>
Create test files for the most complex hooks (useUserActivity, useWhatsAppWebSocket, useWhatsAppWebSocketEnhanced) covering timer-based activity tracking, WebSocket connection lifecycle, reconnection logic, and create reusable test helpers.

Purpose: Test the most complex hooks with multiple side effects (timers, event listeners, WebSocket), requiring comprehensive cleanup testing and reusable test helper utilities.

Output: Three new test files for complex hooks plus a test helpers utility file.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-frontend-custom-hook-testing/131-RESEARCH.md
@.planning/phases/131-frontend-custom-hook-testing/131-03-SUMMARY.md
@.planning/phases/131-frontend-custom-hook-testing/131-02-SUMMARY.md

@frontend-nextjs/hooks/useUserActivity.ts
@frontend-nextjs/hooks/useWhatsAppWebSocket.ts
@frontend-nextjs/hooks/useWhatsAppWebSocketEnhanced.ts
@frontend-nextjs/hooks/__tests__/useWebSocket.test.ts
@frontend-nextjs/jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Create test-helpers.ts with reusable mocking utilities</name>
  <files>frontend-nextjs/hooks/test-helpers.ts</files>
  <action>
    Create test helpers utility file at frontend-nextjs/hooks/test-helpers.ts

    Export utilities for hook testing:

    1. Mock WebSocket factory:
       ```typescript
       export function createMockWebSocket(options?: {
         readyState?: number;
         delayOpen?: boolean;
       })
       ```
       - Returns mock WebSocket instance
       - Spies on send, close, addEventListener, removeEventListener
       - Provides helper methods to trigger events (simulateOpen, simulateMessage, simulateClose, simulateError)
       - Clears mocks in cleanup

    2. Mock SpeechRecognition factory:
       ```typescript
       export function createMockSpeechRecognition()
       ```
       - Returns mock recognition instance
       - Spies on start, stop
       - Provides methods to trigger callbacks (triggerResult, triggerError, triggerEnd)
       - Configures continuous, interimResults, lang

    3. Mock SpeechSynthesis factory:
       ```typescript
       export function createMockSpeechSynthesis()
       ```
       - Returns mock speechSynthesis object
       - Spies on speak, cancel, pause, resume
       - Provides getVoices mock
       - Provides onvoiceschanged trigger

    4. Timer utilities:
       ```typescript
       export function setupFakeTimers()
       export function cleanupFakeTimers()
       ```
       - Wrappers for jest.useFakeTimers()/jest.useRealTimers()
       - Consistent timer management across tests

    5. Fetch mock helpers:
       ```typescript
       export function mockFetchResponse(data: any, ok: boolean = true)
       export function mockFetchError(error: Error)
       ```
       - Standardized fetch mocking
       - JSON serialization handling

    Pattern from existing useWebSocket.test.ts with extracted helpers
    Use JSDoc comments for usage examples
  </action>
  <verify>
    npm test -- test-helpers.test.ts (if separate test file created)
  </verify>
  <done>
    - Test helpers file created
    - All helper functions exported
    - JSDoc comments provided
    - Helpers used in subsequent test files
  </done>
</task>

<task type="auto">
  <name>Create useUserActivity.test.ts with timer and event listener cleanup</name>
  <files>frontend-nextjs/hooks/__tests__/useUserActivity.test.ts</files>
  <action>
    Create test file for useUserActivity hook at frontend-nextjs/hooks/__tests__/useUserActivity.test.ts

    Test coverage:
    1. Session token generation tests:
       - Generates session token on mount
       - Token format: web_timestamp_randomstring
       - Uses Date.now() and Math.random()

    2. Activity tracking tests:
       - Adds event listeners for mousedown, keydown, scroll, touchstart
       - Listeners use passive: true option
       - All listeners call recordActivity

    3. Heartbeat tests:
       - Sends heartbeat immediately on mount
       - Sends heartbeat every interval (default 30000ms)
       - Calls correct API endpoint
       - Includes session_token, session_type, user_agent in body

    4. Heartbeat response handling:
       - Updates state from API response
       - Calls onStateChange callback if provided
       - Clears error on success

    5. Manual override tests:
       - setManualOverride calls API with state
       - Includes expires_at if provided
       - Updates state on success
       - clearManualOverride calls DELETE endpoint

    6. Cleanup tests (CRITICAL - memory leak prevention):
       - Clears activity timeout on unmount
       - Removes all event listeners
       - Clears heartbeat interval
       - Each listener removed individually

    7. Error handling:
       - Sets error state on fetch failure
       - Logs error to console
       - Continues operation after error

    8. Enabled flag tests:
       - Does not track when enabled=false
       - Does not send heartbeat when disabled
       - Does not set up interval when disabled

    Use jest.useFakeTimers() for interval testing
    Spy on window.addEventListener and window.removeEventListener
    Mock fetch for API calls
    Test cleanup by calling unmount() and verifying spies
  </action>
  <verify>
    npm test -- useUserActivity.test.ts
  </verify>
  <done>
    - All 20+ tests pass
    - All timers tested and cleaned up
    - All event listeners tested with cleanup
    - Heartbeat API calls verified
    - Manual override tested
    - Memory leak prevention verified
    - Coverage >85% for useUserActivity.ts
  </done>
</task>

<task type="auto">
  <name>Create useWhatsAppWebSocket.test.ts with WebSocket lifecycle</name>
  <files>frontend-nextjs/hooks/__tests__/useWhatsAppWebSocket.test.ts</files>
  <action>
    Create test file for useWhatsAppWebSocket hook at frontend-nextjs/hooks/__tests__/useWhatsAppWebSocket.test.ts

    Test coverage:
    1. Connection lifecycle tests:
       - Creates WebSocket on mount if autoConnect=true
       - Uses correct URL with ws:// protocol
       - Sets isConnecting during connection
       - Sets isConnected on open
       - Connection state transitions correctly

    2. Message handling tests:
       - Receives messages via onmessage
       - Parses JSON from event.data
       - Updates lastMessage state
       - Handles specific message types (ping, pong, connection_status, new_message, etc.)

    3. Send message tests:
       - sendMessage() sends JSON string
       - Only sends when connected
       - Returns false when not connected
       - Returns true on success

    4. Subscription tests:
       - subscribeToEvents() sends subscribe message
       - unsubscribeFromEvents() sends unsubscribe message
       - Correct JSON format for subscriptions

    5. Reconnection tests:
       - Attempts reconnection on close (if not manual)
       - Respects reconnectAttempts limit
       - Uses reconnectDelay between attempts
       - Increments reconnectCount

    6. Ping/pong tests:
       - Sends ping every pingInterval
       - Handles pong responses
       - Clears ping interval on disconnect

    7. Cleanup tests (CRITICAL):
       - Closes WebSocket on unmount
       - Clears all timeouts (reconnect, ping)
       - Does not leak intervals

    8. Error handling:
       - Handles onerror events
       - Sets error state
       - Clears timeouts on error

    Use test helpers (createMockWebSocket) from test-helpers.ts
    Trigger WebSocket callbacks manually in tests
    Test reconnection with jest.useFakeTimers()
  </action>
  <verify>
    npm test -- useWhatsAppWebSocket.test.ts
  </verify>
  <done>
    - All 20+ tests pass
    - Connection lifecycle fully tested
    - Reconnection logic verified
    - Message handling tested
    - Cleanup on unmount verified
    - Coverage >85% for useWhatsAppWebSocket.ts
  </done>
</task>

<task type="auto">
  <name>Create useWhatsAppWebSocketEnhanced.test.ts with toast integration</name>
  <files>frontend-nextjs/hooks/__tests__/useWhatsAppWebSocketEnhanced.test.ts</files>
  <action>
    Create test file for useWhatsAppWebSocketEnhanced hook at frontend-nextjs/hooks/__tests__/useWhatsAppWebSocketEnhanced.test.ts

    Test coverage:
    1. Enhanced features tests:
       - All base WebSocket functionality from useWhatsAppWebSocket
       - Additional debug logging with debugMode
       - Toast notifications for connection events
       - testConnection() method
       - sendTestNotification() method

    2. Toast integration tests:
       - Shows success toast on connection
       - Shows error toast on connection failure
       - Shows error toast on WebSocket error message
       - Shows warning toast when connection lost

    3. Debug mode tests:
       - Logs when debugMode=true
       - No logs when debugMode=false (default)
       - Logs include helpful context

    4. Additional methods:
       - testConnection() sends test notification if connected
       - testConnection() calls connect() if not connected
       - sendTestNotification() sends test_notification message

    5. Message handling:
       - Handles subscription_confirmed message
       - Handles test_notification_response
       - Logs unknown message types when debugMode enabled

    6. Cleanup tests:
       - All cleanup from base hook
       - Additional cleanup for enhanced features
       - No toast calls after unmount

    7. Error handling:
       - Enhanced error logging
       - Toast notifications for all error types
       - Graceful degradation

    Mock toast from @/components/ui/use-toast or similar
    Use test helpers for WebSocket mocking
    Test toast calls with jest.fn() spies
    Verify debug logging with console.log spy
  </action>
  <verify>
    npm test -- useWhatsAppWebSocketEnhanced.test.ts
  </verify>
  <done>
    - All 20+ tests pass
    - Base functionality verified
    - Toast integration tested
    - Debug mode tested
    - Additional methods tested
    - Cleanup verified
    - Coverage >85% for useWhatsAppWebSocketEnhanced.ts
  </done>
</task>

</tasks>

<verification>
1. Run npm test -- hooks/__tests__/useUserActivity.test.ts --coverage
2. Run npm test -- hooks/__tests__/useWhatsAppWebSocket.test.ts --coverage
3. Run npm test -- hooks/__tests__/useWhatsAppWebSocketEnhanced.test.ts --coverage
4. Verify all tests pass independently
5. Check coverage report shows >85% for each hook
6. Verify cleanup functions thoroughly tested
7. Confirm test helpers are used and work correctly
</verification>

<success_criteria>
- Test helpers utility created and used
- Three complex hook test files created
- All timer cleanup tested (setInterval, setTimeout)
- All event listener cleanup tested
- WebSocket lifecycle fully covered
- Reconnection logic tested
- Memory leak prevention verified
- Coverage threshold of 85% met for all three hooks
</success_criteria>

<output>
After completion, create `.planning/phases/131-frontend-custom-hook-testing/131-06-SUMMARY.md`
</output>
