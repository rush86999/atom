---
phase: 131-frontend-custom-hook-testing
plan: 04
type: execute
wave: 3
depends_on: [131-02]
files_modified:
  - frontend-nextjs/hooks/__tests__/useLiveSupport.test.ts
  - frontend-nextjs/hooks/__tests__/useLiveFinance.test.ts
  - frontend-nextjs/hooks/__tests__/useLiveProjects.test.ts
  - frontend-nextjs/hooks/__tests__/useLiveSales.test.ts
  - frontend-nextjs/hooks/__tests__/useLiveCommunication.test.ts
autonomous: true

must_haves:
  truths:
    - "Live data hooks test polling with setInterval cleanup"
    - "Mock data and real API paths are both tested"
    - "Data transformation from API to UI model is verified"
    - "Provider availability tracking is tested"
    - "Refresh functions work correctly"
  artifacts:
    - path: frontend-nextjs/hooks/__tests__/useLiveSupport.test.ts
      provides: "Live support tickets tests"
      min_lines: 60
    - path: frontend-nextjs/hooks/__tests__/useLiveFinance.test.ts
      provides: "Live finance data tests"
      min_lines: 80
    - path: frontend-nextjs/hooks/__tests__/useLiveProjects.test.ts
      provides: "Live projects data tests"
      min_lines: 80
    - path: frontend-nextjs/hooks/__tests__/useLiveSales.test.ts
      provides: "Live sales pipeline tests"
      min_lines: 80
    - path: frontend-nextjs/hooks/__tests__/useLiveCommunication.test.ts
      provides: "Live communication inbox tests"
      min_lines: 80
  key_links:
    - from: frontend-nextjs/hooks/__tests__/useLiveFinance.test.ts
      to: frontend-nextjs/hooks/useLiveFinance.ts
      via: "fetch mock and setInterval spy"
      pattern: "fetchLiveFinance.*setInterval.*60000"
    - from: frontend-nextjs/hooks/__tests__/useLiveCommunication.test.ts
      to: frontend-nextjs/hooks/useLiveCommunication.ts
      via: "data transformation testing"
      pattern: "map.*RawUnifiedMessage.*uiMessages"
---

<objective>
Create test files for live data hooks (useLiveSupport, useLiveFinance, useLiveProjects, useLiveSales, useLiveCommunication) covering polling behavior, data transformation, and provider tracking.

Purpose: Test hooks that poll APIs every 30-60 seconds, requiring setInterval cleanup testing, data transformation verification, and provider availability tracking.

Output: Five new test files with comprehensive polling and data transformation testing.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-frontend-custom-hook-testing/131-RESEARCH.md
@.planning/phases/131-frontend-custom-hook-testing/131-02-SUMMARY.md

@frontend-nextjs/hooks/useLiveSupport.ts
@frontend-nextjs/hooks/useLiveFinance.ts
@frontend-nextjs/hooks/useLiveProjects.ts
@frontend-nextjs/hooks/useLiveSales.ts
@frontend-nextjs/hooks/useLiveCommunication.ts
@frontend-nextjs/hooks/__tests__/useLiveContacts.test.ts
@frontend-nextjs/jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Create useLiveSupport.test.ts with mock data testing</name>
  <files>frontend-nextjs/hooks/__tests__/useLiveSupport.test.ts</files>
  <action>
    Create test file for useLiveSupport hook at frontend-nextjs/hooks/__tests__/useLiveSupport.test.ts

    Test coverage:
    1. Data fetching tests:
       - Fetches support tickets on mount
       - Sets tickets state from mock data
       - Sets isLoading to false after fetch
       - Handles artificial delay (setTimeout)

    2. Mock data structure:
       - Correct ticket types (Ticket interface)
       - Platform types: zendesk, freshdesk, intercom
       - Priority levels: High, Medium, Low
       - Status types: Open, Pending, Closed

    3. Refresh function:
       - Re-fetches tickets when called
       - Updates tickets state

    4. Loading states:
       - isLoading starts as true
       - Becomes false after fetch

    5. Error handling:
       - Handles fetch errors gracefully
       - Sets isLoading to false on error

    Note: This hook uses mock data with artificial delay, not real fetch
    Use jest.useFakeTimers() for setTimeout testing
  </action>
  <verify>
    npm test -- useLiveSupport.test.ts
  </verify>
  <done>
    - All 8+ tests pass
    - Mock data structure verified
    - Loading states tested
    - Refresh function tested
    - Coverage >85% for useLiveSupport.ts
  </done>
</task>

<task type="auto">
  <name>Create useLiveFinance.test.ts with polling and data mapping</name>
  <files>frontend-nextjs/hooks/__tests__/useLiveFinance.test.ts</files>
  <action>
    Create test file for useLiveFinance hook at frontend-nextjs/hooks/__tests__/useLiveFinance.test.ts

    Test coverage:
    1. Data fetching tests:
       - Fetches finance data on mount
       - Sets transactions state
       - Sets stats state correctly
       - Sets activeProviders state
       - Parses LiveFinanceResponse

    2. Data structure tests:
       - UnifiedTransaction interface fields
       - Platform types: stripe, xero, quickbooks, zoho, dynamics
       - FinanceStats structure (total_revenue, pending_revenue, etc.)

    3. Polling behavior:
       - Sets up 60-second interval
       - Cleans up interval on unmount
       - Clears interval in cleanup function

    4. Refresh function:
       - Re-fetches finance data when called
       - Updates all states

    5. Loading states:
       - isLoading starts as true
       - Becomes false after fetch

    Mock fetch for /api/atom/finance/live/overview
    Use jest.spyOn for setInterval testing
  </action>
  <verify>
    npm test -- useLiveFinance.test.ts
  </verify>
  <done>
    - All 10+ tests pass
    - API response parsing verified
    - Polling cleanup tested
    - Data types validated
    - Coverage >85% for useLiveFinance.ts
  </done>
</task>

<task type="auto">
  <name>Create useLiveProjects.test.ts with project board testing</name>
  <files>frontend-nextjs/hooks/__tests__/useLiveProjects.test.ts</files>
  <action>
    Create test file for useLiveProjects hook at frontend-nextjs/hooks/__tests__/useLiveProjects.test.ts

    Test coverage:
    1. Data fetching tests:
       - Fetches project board on mount
       - Sets tasks state
       - Sets stats state
       - Sets activeProviders state
       - Parses LiveProjectsResponse

    2. Data structure tests:
       - UnifiedTask interface fields
       - Platform types: asana, jira, trello, clickup, zoho, planner
       - ProjectStats structure

    3. Polling behavior:
       - Sets up 60-second interval
       - Cleans up interval on unmount

    4. Refresh function:
       - Re-fetches project data
       - Updates all states

    5. Loading states:
       - Initial isLoading is true
       - Becomes false after fetch

    Mock fetch for /api/atom/projects/live/board
    Test polling cleanup
  </action>
  <verify>
    npm test -- useLiveProjects.test.ts
  </verify>
  <done>
    - All 10+ tests pass
    - Task data mapping verified
    - Stats structure tested
    - Polling cleanup verified
    - Coverage >85% for useLiveProjects.ts
  </done>
</task>

<task type="auto">
  <name>Create useLiveSales.test.ts with pipeline testing</name>
  <files>frontend-nextjs/hooks/__tests__/useLiveSales.test.ts</files>
  <action>
    Create test file for useLiveSales hook at frontend-nextjs/hooks/__tests__/useLiveSales.test.ts

    Test coverage:
    1. Data fetching tests:
       - Fetches sales pipeline on mount
       - Sets deals state
       - Sets stats state
       - Sets activeProviders state
       - Parses LivePipelineResponse

    2. Data structure tests:
       - UnifiedDeal interface fields
       - Platform types: salesforce, hubspot, zoho, dynamics
       - SalesStats structure (total_pipeline_value, win_rate, etc.)

    3. Polling behavior:
       - Sets up 60-second interval
       - Cleans up interval on unmount

    4. Refresh function:
       - Re-fetches pipeline data
       - Updates all states

    5. Loading states:
       - Initial isLoading is true
       - Becomes false after fetch

    Mock fetch for /api/atom/sales/live/pipeline
    Test polling with jest.spyOn(global, 'setInterval')
  </action>
  <verify>
    npm test -- useLiveSales.test.ts
  </verify>
  <done>
    - All 10+ tests pass
    - Deal data mapping verified
    - Sales stats tested
    - Polling cleanup verified
    - Coverage >85% for useLiveSales.ts
  </done>
</task>

<task type="auto">
  <name>Create useLiveCommunication.test.ts with data transformation</name>
  <files>frontend-nextjs/hooks/__tests__/useLiveCommunication.test.ts</files>
  <action>
    Create test file for useLiveCommunication hook at frontend-nextjs/hooks/__tests__/useLiveCommunication.test.ts

    Test coverage:
    1. Data fetching tests:
       - Fetches live inbox on mount
       - Sets messages state
       - Sets activeProviders state
       - Parses LiveInboxResponse

    2. Data transformation tests (IMPORTANT):
       - Maps RawUnifiedMessage to Message UI model
       - Maps 'gmail' provider to 'email' platform
       - Maps 'sender' to 'from'
       - Adds default subject for chat platforms
       - Converts timestamp string to Date
       - Maps 'unread' status to unread boolean
       - Triggers content to preview (substring 0-100)

    3. Provider types:
       - slack, gmail, discord, teams, zoho, outlook

    4. Polling behavior:
       - Sets up 30-second interval (faster than others)
       - Cleans up interval on unmount

    5. Refresh function:
       - Re-fetches inbox data
       - Updates messages state

    6. Loading states:
       - Initial isLoading is true
       - Becomes false after fetch

    Mock fetch for /api/atom/communication/live/inbox
    Verify data transformation with detailed field mapping tests
  </action>
  <verify>
    npm test -- useLiveCommunication.test.ts
  </verify>
  <done>
    - All 12+ tests pass
    - Data transformation fully verified
    - Field mapping validated
    - Polling cleanup tested
    - Coverage >85% for useLiveCommunication.ts
  </done>
</task>

</tasks>

<verification>
1. Run npm test -- hooks/__tests__/useLiveSupport.test.ts --coverage
2. Run npm test -- hooks/__tests__/useLiveFinance.test.ts --coverage
3. Run npm test -- hooks/__tests__/useLiveProjects.test.ts --coverage
4. Run npm test -- hooks/__tests__/useLiveSales.test.ts --coverage
5. Run npm test -- hooks/__tests__/useLiveCommunication.test.ts --coverage
6. Verify all tests pass independently
7. Check coverage report shows >85% for each hook
8. Verify polling cleanup tested for all hooks except useLiveSupport
</verification>

<success_criteria>
- Five new test files created
- All polling hooks test interval cleanup
- Data transformation verified for useLiveCommunication
- Provider tracking tested
- Refresh functions work correctly
- Coverage threshold of 85% met for all five hooks
- Mock data testing works for useLiveSupport
</success_criteria>

<output>
After completion, create `.planning/phases/131-frontend-custom-hook-testing/131-04-SUMMARY.md`
</output>
