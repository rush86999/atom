---
phase: 131-frontend-custom-hook-testing
plan: 02
type: execute
wave: 2
depends_on: [131-01]
files_modified:
  - frontend-nextjs/hooks/__tests__/useCognitiveTier.test.ts
  - frontend-nextjs/hooks/__tests__/useFileUpload.test.ts
  - frontend-nextjs/hooks/__tests__/useLiveContacts.test.ts
  - frontend-nextjs/hooks/__tests__/useLiveKnowledge.test.ts
autonomous: true

must_haves:
  truths:
    - "Async hooks test fetch calls with mocked responses"
    - "Loading states are tested through all transitions"
    - "Error handling covers network failures and API errors"
    - "Polling hooks test interval setup and cleanup"
    - "Progress tracking tested for file upload"
  artifacts:
    - path: frontend-nextjs/hooks/__tests__/useCognitiveTier.test.ts
      provides: "Cognitive tier preference tests"
      min_lines: 120
    - path: frontend-nextjs/hooks/__tests__/useFileUpload.test.ts
      provides: "File upload with progress tests"
      min_lines: 80
    - path: frontend-nextjs/hooks/__tests__/useLiveContacts.test.ts
      provides: "Live contacts polling tests"
      min_lines: 60
    - path: frontend-nextjs/hooks/__tests__/useLiveKnowledge.test.ts
      provides: "Live knowledge data tests"
      min_lines: 80
  key_links:
    - from: frontend-nextjs/hooks/__tests__/useCognitiveTier.test.ts
      to: frontend-nextjs/hooks/useCognitiveTier.ts
      via: "jest.fn() mock for fetch"
      pattern: "fetch.*cognitive-tier"
    - from: frontend-nextjs/hooks/__tests__/useFileUpload.test.ts
      to: frontend-nextjs/hooks/useFileUpload.ts
      via: "apiClient mock with onUploadProgress"
      pattern: "uploadFile.*progress.*setProgress"
    - from: frontend-nextjs/hooks/__tests__/useLiveContacts.test.ts
      to: frontend-nextjs/hooks/useLiveContacts.ts
      via: "setInterval mock for polling"
      pattern: "setInterval.*60000.*fetchContacts"
---

<objective>
Create test files for async data fetching hooks (useCognitiveTier, useFileUpload, useLiveContacts, useLiveKnowledge) covering API calls, loading states, error handling, and polling behavior.

Purpose: Test hooks that make async fetch calls, requiring mock fetch/apiClient, waitFor for async assertions, and proper cleanup testing for polling intervals.

Output: Four new test files with comprehensive async operation testing.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-frontend-custom-hook-testing/131-RESEARCH.md
@.planning/phases/131-frontend-custom-hook-testing/131-01-SUMMARY.md

@frontend-nextjs/hooks/useCognitiveTier.ts
@frontend-nextjs/hooks/useFileUpload.ts
@frontend-nextjs/hooks/useLiveContacts.ts
@frontend-nextjs/hooks/useLiveKnowledge.ts
@frontend-nextjs/hooks/__tests__/useChatMemory.test.ts
@frontend-nextjs/jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Create useCognitiveTier.test.ts with API mocking</name>
  <files>frontend-nextjs/hooks/__tests__/useCognitiveTier.test.ts</files>
  <action>
    Create test file for useCognitiveTier hook at frontend-nextjs/hooks/__tests__/useCognitiveTier.test.ts

    Test coverage:
    1. Fetch preferences tests:
       - Fetches preferences on mount
       - Sets preferences state on successful fetch
       - Sets loading to false after fetch
       - Handles API errors gracefully

    2. Save preferences tests:
       - Sends POST request with correct payload
       - Updates preferences state on success
       - Returns true on success, false on failure
       - Sets saving state correctly

    3. Estimate cost tests:
       - Sends request with prompt and optional tokens
       - Returns cost estimates array
       - Handles missing/invalid parameters
       - Returns empty array on error

    4. Compare tiers tests:
       - Fetches tier comparisons
       - Returns tier comparison array
       - Handles API errors

    5. State management:
       - Initial loading state is true
       - Loading becomes false after fetch
       - Saving state transitions correctly

    Mock fetch globally with jest.fn()
    Use waitFor for async state updates
    Pattern from useChatMemory.test.ts
  </action>
  <verify>
    npm test -- useCognitiveTier.test.ts
  </verify>
  <done>
    - All 12+ tests pass
    - All API methods tested
    - Loading/saving states verified
    - Error handling covered
    - Coverage >85% for useCognitiveTier.ts
  </done>
</task>

<task type="auto">
  <name>Create useFileUpload.test.ts with progress tracking</name>
  <files>frontend-nextjs/hooks/__tests__/useFileUpload.test.ts</files>
  <action>
    Create test file for useFileUpload hook at frontend-nextjs/hooks/__tests__/useFileUpload.test.ts

    Test coverage:
    1. Upload state tests:
       - Initial isUploading is false
       - Initial progress is 0
       - isUploading becomes true during upload
       - Progress updates during upload
       - isUploading becomes false after completion

    2. Success path:
       - Returns response data on success
       - Resets isUploading to false
       - Progress reaches 100%

    3. Error handling:
       - Throws error on upload failure
       - Sets isUploading to false in finally block
       - Console.error called with error details

    4. FormData:
       - Creates FormData with file
       - Sets correct Content-Type header

    Mock apiClient.post with jest.fn()
    Test the onUploadProgress callback simulation
  </action>
  <verify>
    npm test -- useFileUpload.test.ts
  </verify>
  <done>
    - All 8+ tests pass
    - Progress tracking verified
    - Error states tested
    - FormData creation verified
    - Coverage >85% for useFileUpload.ts
  </done>
</task>

<task type="auto">
  <name>Create useLiveContacts.test.ts with polling cleanup</name>
  <files>frontend-nextjs/hooks/__tests__/useLiveContacts.test.ts</files>
  <action>
    Create test file for useLiveContacts hook at frontend-nextjs/hooks/__tests__/useLiveContacts.test.ts

    Test coverage:
    1. Data fetching tests:
       - Fetches contacts on mount
       - Sets contacts state from API response
       - Sets loading to false after fetch
       - Parses data.ok and data.contacts correctly

    2. Polling behavior:
       - Sets up 60-second interval
       - Cleans up interval on unmount
       - Clears interval in cleanup function

    3. Error handling:
       - Handles fetch errors gracefully
       - Sets loading to false on error
       - Console.error called with error

    4. Initial state:
       - Loading starts as true
       - Contacts starts empty

    Mock fetch with jest.fn()
    Use jest.useFakeTimers() or jest.spyOn(global, 'setInterval') for interval testing
    Test unmount cleanup
  </action>
  <verify>
    npm test -- useLiveContacts.test.ts
  </verify>
  <done>
    - All 8+ tests pass
    - Polling interval tested
    - Cleanup on unmount verified
    - Error handling covered
    - Coverage >85% for useLiveContacts.ts
  </done>
</task>

<task type="auto">
  <name>Create useLiveKnowledge.test.ts with axios mocking</name>
  <files>frontend-nextjs/hooks/__tests__/useLiveKnowledge.test.ts</files>
  <action>
    Create test file for useLiveKnowledge hook at frontend-nextjs/hooks/__tests__/useLiveKnowledge.test.ts

    Test coverage:
    1. Fetch knowledge tests:
       - Fetches knowledge items on mount
       - Maps API entities to KnowledgeItem format
       - Sets items state correctly
       - Handles loading states

    2. Fetch insights tests:
       - Fetches smart insights
       - Sets insights state
       - Sets insightsLoading correctly

    3. Refresh function:
       - Calls both fetchKnowledge and fetchInsights
       - Uses Promise.all for parallel fetching
       - Updates both states on refresh

    4. Error handling:
       - Handles axios errors
       - Shows toast error on failure
       - Sets loading states to false in finally

    5. Data mapping:
       - Correctly maps entity fields
       - Handles missing fields gracefully

    Mock axios.get with jest.fn()
    Mock toast from 'sonner' for error testing
  </action>
  <verify>
    npm test -- useLiveKnowledge.test.ts
  </verify>
  <done>
    - All 10+ tests pass
    - Axios calls mocked
    - Data mapping verified
    - Refresh function tested
    - Error handling with toast covered
    - Coverage >85% for useLiveKnowledge.ts
  </done>
</task>

</tasks>

<verification>
1. Run npm test -- hooks/__tests__/useCognitiveTier.test.ts --coverage
2. Run npm test -- hooks/__tests__/useFileUpload.test.ts --coverage
3. Run npm test -- hooks/__tests__/useLiveContacts.test.ts --coverage
4. Run npm test -- hooks/__tests__/useLiveKnowledge.test.ts --coverage
5. Verify all tests pass independently
6. Check coverage report shows >85% for each hook
7. Verify interval cleanup tested with jest.spyOn or unmount
</verification>

<success_criteria>
- Four new test files created
- All async operations tested with waitFor
- Loading states verified through transitions
- Error handling covers network and API errors
- Polling cleanup tested for useLiveContacts
- Coverage threshold of 85% met for all four hooks
- All mocks properly cleared in beforeEach
</success_criteria>

<output>
After completion, create `.planning/phases/131-frontend-custom-hook-testing/131-02-SUMMARY.md`
</output>
