---
phase: 098-property-testing-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/098-property-testing-expansion/098-01-SUMMARY.md
  - backend/tests/property_tests/INVARIANTS.md
autonomous: true

must_haves:
  truths:
    - "Existing property test inventory cataloged with file locations and test counts"
    - "Critical invariant gaps identified across all 4 platforms"
    - "Priority invariants documented for expansion in later plans"
    - "Backend INVARIANTS.md updated with frontend/mobile/desktop invariants"
  artifacts:
    - path: ".planning/phases/098-property-testing-expansion/.property-test-inventory.json"
      provides: "Complete inventory of all property tests across platforms"
      contains: "file_paths, test_counts, invariant_types, coverage_gaps"
    - path: "backend/tests/property_tests/INVARIANTS.md"
      provides: "Master invariant catalog for all platforms"
      contains: "frontend_invariants, mobile_invariants, desktop_invariants, untested_invariants"
  key_links:
    - from: ".property-test-inventory.json"
      to: "098-02-PLAN.md, 098-03-PLAN.md, 098-04-PLAN.md"
      via: "gap analysis drives test creation priorities"
      pattern: "untested_invariants array"
---

<objective>
**Survey all existing property tests across platforms and identify gaps in critical invariant coverage**

**Purpose:** Phase 098 research revealed 229+ property tests already exist (far exceeding 30+ target). This plan creates a comprehensive inventory of existing tests and identifies untested critical invariants for targeted expansion in later plans. Quality over quantity approach ensures we test business-critical invariants, not inflate test counts.

**Output:** Property test inventory JSON file, updated INVARIANTS.md with cross-platform catalog, gap analysis report
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/098-property-testing-expansion/098-RESEARCH.md

# Existing Property Test Patterns
@backend/tests/property_tests/INVARIANTS.md
@frontend-nextjs/tests/property/reducer-invariants.test.ts
@mobile/src/__tests__/property/queueInvariants.test.ts
@frontend-nextjs/tests/property/tauriCommandInvariants.test.ts
@frontend-nextjs/src-tauri/tests/file_operations_proptest.rs
</context>

<tasks>

<task type="auto">
  <name>Survey existing property tests across all platforms</name>
  <files>
    .planning/phases/098-property-testing-expansion/.property-test-inventory.json
    backend/tests/property_tests/INVARIANTS.md
  </files>
  <action>
    Create comprehensive inventory of all property tests:

    1. **Backend (Hypothesis):** Scan `backend/tests/property_tests/` recursively
       - Count test files with `@given` decorator
       - Catalog domains: governance, episodes, state_management, api_contracts, database, auth, etc.
       - Extract invariant names from test functions (e.g., `test_state_update_immutability`)
       - Count tests per file

    2. **Frontend (FastCheck):** Scan `frontend-nextjs/tests/property/`
       - Count `fc.assert()` calls
       - Catalog domains: state_management, reducer_invariants, tauri_command_invariants
       - Extract invariant names from test descriptions
       - Note: 48 properties already exist (from reducer-invariants, state-management, tauriCommandInvariants)

    3. **Mobile (FastCheck):** Scan `mobile/src/__tests__/property/`
       - Count `fc.assert()` calls
       - Catalog domains: queue_invariants
       - Note: 13 properties already exist (basic queue invariants)

    4. **Desktop (proptest + FastCheck):** Scan `frontend-nextjs/src-tauri/tests/`
       - Count `proptest!` macro calls and `fc.assert()` calls
       - Catalog Rust properties in `file_operations_proptest.rs` (15 properties)
       - Catalog JS properties in `tauriCommandInvariants.test.ts` (21 properties)
       - Catalog sample tests in `property/mod.rs` (3 properties)

    5. **Create inventory JSON:**
       ```json
       {
         "generated_at": "2026-02-26T...",
         "total_property_tests": 229,
         "platforms": {
           "backend": { "test_files": 129, "estimated_properties": 100, "domains": [...] },
           "frontend": { "test_files": 3, "properties": 48, "domains": [...] },
           "mobile": { "test_files": 1, "properties": 13, "domains": [...] },
           "desktop": { "rust_properties": 18, "js_properties": 21, "total": 39 }
         },
         "coverage_by_domain": {
           "state_management": { "backend": 15, "frontend": 27, "mobile": 0, "desktop": 0 },
           "queue_invariants": { "backend": 0, "frontend": 0, "mobile": 13, "desktop": 0 },
           "file_operations": { "backend": 10, "frontend": 0, "mobile": 0, "desktop": 15 },
           ...
         },
         "gaps": {
           "frontend": ["state_machine_transitions", "api_contract_roundtrip"],
           "mobile": ["device_state_invariants", "advanced_sync_logic"],
           "desktop": ["ipc_serialization", "window_state_management"]
         }
       }
       ```

    Use `grep` commands for efficient scanning:
    - Backend: `grep -r "@given" backend/tests/property_tests/ | wc -l`
    - Frontend: `grep -h "fc.assert" frontend-nextjs/tests/property/*.test.ts | wc -l`
    - Mobile: `grep -h "fc.assert" mobile/src/__tests__/property/*.test.ts | wc -l`
    - Desktop Rust: `grep -c "fn prop_" frontend-nextjs/src-tauri/tests/file_operations_proptest.rs`
  </action>
  <verify>
    - `.property-test-inventory.json` exists and is valid JSON
    - `jq '.total_property_tests > 200' .property-test-inventory.json` returns true
    - `jq '.platforms | keys | length == 4' .property-test-inventory.json` returns true
    - `jq '.gaps | length > 0' .property-test-inventory.json` returns true
  </verify>
  <done>
    Property test inventory created with 200+ properties cataloged across 4 platforms, gaps identified for targeted expansion
  </done>
</task>

<task type="auto">
  <name>Identify critical invariant gaps</name>
  <files>
    .planning/phases/098-property-testing-expansion/.property-test-inventory.json
    backend/tests/property_tests/INVARIANTS.md
  </files>
  <action>
    Identify untested critical invariants based on business impact:

    1. **State Machine Invariants (HIGH PRIORITY):**
       - Check if state transitions are tested (canvas state, sync status, auth flow)
       - Gap: Frontend state machine tests missing (Phase 098-02 will add)

    2. **API Contract Round-Trip (MEDIUM PRIORITY):**
       - Check if API serialization/deserialization is tested
       - Gap: Frontend API contract round-trip tests missing

    3. **Advanced Sync Logic (HIGH PRIORITY for Mobile):**
       - Check beyond basic queue ordering (conflict resolution, retry backoff, batch optimization)
       - Gap: Mobile has 13 basic queue properties, needs advanced sync invariants (Phase 098-03 will add)

    4. **IPC Message Serialization (MEDIUM PRIORITY for Desktop):**
       - Check if Tauri IPC message integrity is tested
       - Gap: Desktop Tauri command invariants tested, but IPC serialization missing (Phase 098-04 will add)

    5. **Device State Invariants (LOW-MEDIUM PRIORITY for Mobile):**
       - Check if device state changes are tested
       - Gap: Mobile device state transitions not covered by property tests

    Update `.property-test-inventory.json` with `priority_gaps`:
    ```json
    "priority_gaps": [
      { "platform": "frontend", "domain": "state_machines", "priority": "HIGH", "plan": "098-02" },
      { "platform": "mobile", "domain": "sync_logic", "priority": "HIGH", "plan": "098-03" },
      { "platform": "desktop", "domain": "ipc_serialization", "priority": "MEDIUM", "plan": "098-04" },
      { "platform": "frontend", "domain": "api_roundtrip", "priority": "MEDIUM", "plan": "098-02" }
    ]
    ```

    Use existing codebase analysis:
    - Frontend: Scan `frontend-nextjs/` for state machines (useState with enum, reducer patterns)
    - Mobile: Scan `mobile/src/services/` for sync logic (offlineSyncService.ts)
    - Desktop: Scan `frontend-nextjs/src-tauri/src/` for IPC handlers
  </action>
  <verify>
    - `jq '.priority_gaps | length >= 3' .property-test-inventory.json` returns true
    - Each gap has platform, domain, priority, and plan assigned
    - HIGH priority gaps exist for at least 2 platforms
  </verify>
  <done>
    Critical invariant gaps identified with priorities and assigned to specific plans for implementation
  </done>
</task>

<task type="auto">
  <name>Update INVARIANTS.md with cross-platform catalog</name>
  <files>
    backend/tests/property_tests/INVARIANTS.md
  </files>
  <action>
    Extend `backend/tests/property_tests/INVARIANTS.md` to include all platforms:

    Add new sections after existing backend invariants:

    ```markdown
    ---

    ## Frontend Property Tests (FastCheck)

    ### State Management Invariants
    **Location**: `frontend-nextjs/tests/property/state-management.test.ts`
    **Framework**: FastCheck 4.5.3
    **Test Count**: 15 properties
    **Coverage**: Immutability, idempotency, rollback, composition, null handling

    ### Reducer Invariants
    **Location**: `frontend-nextjs/tests/property/reducer-invariants.test.ts`
    **Test Count**: 13 properties
    **Coverage**: Reducer purity, action handling, field isolation, composition

    ### Tauri Command Invariants
    **Location**: `frontend-nextjs/tests/property/tauriCommandInvariants.test.ts`
    **Test Count**: 21 properties
    **Coverage**: File path validation, command whitelist, session state, notifications

    **Total Frontend Properties**: 48

    ---

    ## Mobile Property Tests (FastCheck)

    ### Queue Invariants
    **Location**: `mobile/src/__tests__/property/queueInvariants.test.ts`
    **Test Count**: 13 properties
    **Coverage**: Queue ordering, size limits, priority mapping, retry logic

    **Total Mobile Properties**: 13

    **Gap**: Advanced sync invariants (conflict resolution, retry backoff) - Phase 098-03

    ---

    ## Desktop Property Tests (proptest + FastCheck)

    ### File Operations Invariants (Rust)
    **Location**: `frontend-nextjs/src-tauri/tests/file_operations_proptest.rs`
    **Framework**: proptest 1.0
    **Test Count**: 15 properties
    **Coverage**: Path traversal, file write/read round-trip, directory creation, cross-platform paths

    ### Sample Invariants (Rust)
    **Location**: `frontend-nextjs/src-tauri/tests/property/mod.rs`
    **Test Count**: 3 properties
    **Coverage**: String reversal, vector sorting, option identity

    ### Tauri Command Invariants (JavaScript)
    **Location**: `frontend-nextjs/tests/property/tauriCommandInvariants.test.ts`
    **Framework**: FastCheck 4.5.3
    **Test Count**: 21 properties
    **Coverage**: Path validation, parameter validation, whitelist enforcement, session consistency

    **Total Desktop Properties**: 39

    **Gap**: IPC message serialization, window state management - Phase 098-04

    ---

    ## Cross-Platform Invariant Summary

    | Platform | Test Files | Properties | Framework | Status |
    |----------|-----------|------------|-----------|--------|
    | Backend (Python) | 129 | ~100 | Hypothesis | ✅ Extensive |
    | Frontend (TypeScript) | 3 | 48 | FastCheck | ✅ Good |
    | Mobile (TypeScript) | 1 | 13 | FastCheck | ⚠️ Basic only |
    | Desktop (Rust + TS) | 2 | 39 | proptest + FastCheck | ✅ Good |
    | **TOTAL** | **135** | **~200** | - | **Exceeds target** |

    **Phase 098 Focus**: Quality over quantity - add VALIDATED_BUG documentation, identify untested critical invariants
    ```

    Use `cat >> backend/tests/property_tests/INVARIANTS.md` to append (create new file if truncated).

    Preserve existing backend invariants (562 lines as of research date).
  </action>
  <verify>
    - `grep -c "## Frontend Property Tests" backend/tests/property_tests/INVARIANTS.md` returns 1
    - `grep -c "## Mobile Property Tests" backend/tests/property_tests/INVARIANTS.md` returns 1
    - `grep -c "## Desktop Property Tests" backend/tests/property_tests/INVARIANTS.md` returns 1
    - `grep -c "Cross-Platform Invariant Summary" backend/tests/property_tests/INVARIANTS.md` returns 1
    - Backend invariants section preserved (existing 562 lines still present)
  </verify>
  <done>
    INVARIANTS.md extended with cross-platform catalog documenting ~200 property tests across all platforms
  </done>
</task>

</tasks>

<verification>
## Plan Verification

1. **Property test inventory created:**
   - `.property-test-inventory.json` exists with valid JSON
   - Total property tests > 200 (confirms research findings)
   - All 4 platforms cataloged with test counts

2. **Critical gaps identified:**
   - At least 3 priority gaps documented
   - Each gap has platform, domain, priority, and assigned plan
   - HIGH priority gaps for mobile and frontend

3. **INVARIANTS.md updated:**
   - Frontend, mobile, desktop sections added
   - Cross-platform summary table included
   - Existing backend invariants preserved

4. **Quality over quantity approach:**
   - Focus on untested critical invariants, not inflating counts
   - Gap analysis drives targeted expansion in later plans
</verification>

<success_criteria>
- Inventory JSON documents 200+ existing property tests (confirms we exceeded 30+ target)
- At least 3 critical invariant gaps identified with HIGH/MEDIUM priorities
- INVARIANTS.md includes cross-platform catalog with all platforms
- Each gap assigned to specific implementation plan (098-02, 098-03, 098-04)
</success_criteria>

<output>
After completion, create `.planning/phases/098-property-testing-expansion/098-01-SUMMARY.md` with:
- Property test counts per platform
- Critical gaps identified
- Updated INVARIANTS.md sections
- Recommendations for Plans 02-04
</output>
