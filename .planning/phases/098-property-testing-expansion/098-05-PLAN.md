---
phase: 098-property-testing-expansion
plan: 05
type: execute
wave: 3
depends_on: ["098-01", "098-02", "098-03", "098-04"]
files_modified:
  - backend/tests/property_tests/INVARIANTS.md
  - docs/PROPERTY_TESTING_PATTERNS.md
  - .planning/phases/098-property-testing-expansion/098-05-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "Unified invariant catalog documents all 250+ properties across platforms"
    - "Property testing patterns guide created with examples from all platforms"
    - "Best practices documented for adding new property tests"
    - "Quality metrics show 250+ properties exceeding 30+ target by 8x"
  artifacts:
    - path: "backend/tests/property_tests/INVARIANTS.md"
      provides: "Master invariant catalog for all platforms"
      contains: "frontend_invariants, mobile_invariants, desktop_invariants, cross_platform_summary"
    - path: "docs/PROPERTY_TESTING_PATTERNS.md"
      provides: "Property testing patterns guide with examples"
      min_lines: 500
      contains: "framework_guides, pattern_catalog, best_practices, anti_patterns, quality_gates"
  key_links:
    - from: "docs/PROPERTY_TESTING_PATTERNS.md"
      to: "backend/tests/property_tests/, frontend-nextjs/tests/property/, mobile/src/__tests__/property/"
      via: "pattern examples link to actual test files"
      pattern: "See: tests/property/"
    - from: "INVARIANTS.md"
      to: "docs/PROPERTY_TESTING_PATTERNS.md"
      via: "invariant catalog references pattern guide"
      pattern: "For implementation patterns, see: docs/PROPERTY_TESTING_PATTERNS.md"
---

<objective>
**Create unified invariant catalog and property testing patterns documentation**

**Purpose:** Phase 098 created 50+ new property tests across 4 platforms. This plan consolidates all invariants into a master catalog and creates a comprehensive patterns guide for developers. Documentation ensures knowledge sharing and consistent property test quality going forward.

**Output:** Updated INVARIANTS.md with 250+ property catalog, PROPERTY_TESTING_PATTERNS.md guide with examples, best practices for adding new tests
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/098-property-testing-expansion/098-RESEARCH.md

# All Phase 098 Plans (for SUMMARY content)
@.planning/phases/098-property-testing-expansion/098-01-PLAN.md
@.planning/phases/098-property-testing-expansion/098-02-PLAN.md
@.planning/phases/098-property-testing-expansion/098-03-PLAN.md
@.planning/phases/098-property-testing-expansion/098-04-PLAN.md

# Existing Invariant Catalog (to extend)
@backend/tests/property_tests/INVARIANTS.md

# Documentation to Reference
@docs/CANVAS_IMPLEMENTATION_COMPLETE.md
@docs/AGENT_GUIDANCE_IMPLEMENTATION.md
</context>

<tasks>

<task type="auto">
  <name>Update INVARIANTS.md with complete cross-platform catalog</name>
  <files>
    backend/tests/property_tests/INVARIANTS.md
  </files>
  <action>
    Update `backend/tests/property_tests/INVARIANTS.md` to include all Phase 098 additions:

    1. **Add Phase 098 New Invariants Section:**
       ```markdown
    ---

    ## Phase 098: Property Testing Expansion (February 2026)

    ### New Invariants Added
    **Total New Properties:** 50+ across 4 platforms

    #### Frontend (Plan 098-02)
    - State machine transitions: 10-15 properties
      - Canvas state machine (draft -> presenting -> presented -> closed)
      - Sync status state machine (pending -> syncing -> completed/failed)
      - Auth flow state machine (guest -> authenticating -> authenticated)
    - API contract round-trip: 5-7 properties
      - Request serialization round-trip
      - Response deserialization integrity
      - Date/DateTime field preservation
    - **Total Frontend After Phase 098:** 63+ properties (48 existing + 15 new)

    #### Mobile (Plan 098-03)
    - Advanced sync logic: 10-12 properties
      - Timestamp-based conflict resolution
      - Exponential retry backoff
      - Batch size limits and ordering
      - Network-aware sync behavior
    - Device state: 5-7 properties
      - Permission state transitions
      - Biometric auth state machine
      - Connectivity state transitions
    - **Total Mobile After Phase 098:** 28+ properties (13 existing + 15 new)

    #### Desktop (Plan 098-04)
    - IPC serialization (Rust): 8-10 properties
      - Command serialization round-trip
      - Error handling invariants
      - Type safety for enums
    - Window state (Rust): 5-7 properties
      - Window size constraints
      - Fullscreen toggle consistency
      - Minimize/maximize transitions
    - **Total Desktop After Phase 098:** 52+ properties (39 existing + 13 new)

    ---

    ## Updated Cross-Platform Invariant Summary

    | Platform | Test Files | Properties (Phase 098) | Framework | Status |
    |----------|-----------|-------------------------|-----------|--------|
    | Backend (Python) | 129 | ~100 | Hypothesis | ✅ Extensive |
    | Frontend (TypeScript) | 5 | 63+ | FastCheck | ✅ Excellent |
    | Mobile (TypeScript) | 3 | 28+ | FastCheck | ✅ Good |
    | Desktop (Rust + TS) | 4 | 52+ | proptest + FastCheck | ✅ Good |
    | **TOTAL** | **141** | **~250** | - | **8x target exceeded** |

    **Phase 098 Achievement:** 30+ target exceeded by 8x with focus on quality over quantity
    ```

    2. **Add Critical Invariant Inventory:**
       ```markdown
    ---

    ## Critical Business Invariants Catalog

    ### High Priority (Security/Data Loss Prevention)
    - State immutability (all platforms)
    - Round-trip serialization integrity (all platforms)
    - Path traversal prevention (backend, desktop)
    - Command whitelist enforcement (desktop, mobile)
    - Transaction atomicity (backend)

    ### Medium Priority (Business Logic)
    - State machine transitions (frontend, mobile)
    - Conflict resolution (mobile sync)
    - API contract validation (frontend, mobile)
    - Window state consistency (desktop)

    ### Low Priority (Performance/Optimization)
    - Queue ordering (mobile)
    - Batch optimization (mobile sync)
    - Cache consistency (backend)

    ---

    ## Property Testing Best Practices

    ### VALIDATED_BUG Documentation
    All property tests should include VALIDATED_BUG docstrings:
    - Document bugs found during testing
    - If no bugs found, state "None - invariant validated during implementation"
    - Include root cause analysis for bugs
    - Reference commit hashes where bugs were fixed

    ### numRuns Guidelines
    - **Fast in-memory tests:** 100 runs (frontend state machines)
    - **IO-bound tests:** 50 runs (file operations, API calls)
    - **Expensive tests:** 10-20 runs (database queries, network calls)

    ### Seed Values for Reproducibility
    - Always set deterministic seeds (e.g., seed: 23001)
    - Document seed in test description
    - Use sequential seeds across related tests
    ```

    3. **Maintain existing backend invariants** (preserve all existing content)

    Use bash command to append (or edit file to insert sections):
    ```bash
    # Count total properties documented
    grep -c "## " backend/tests/property_tests/INVARIANTS.md
    ```
  </action>
  <verify>
    - `grep -c "Phase 098" backend/tests/property_tests/INVARIANTS.md` >= 1
    - `grep -c "Total Frontend After Phase 098" backend/tests/property_tests/INVARIANTS.md` >= 1
    - `grep -c "Total Mobile After Phase 098" backend/tests/property_tests/INVARIANTS.md` >= 1
    - `grep -c "Total Desktop After Phase 098" backend/tests/property_tests/INVARIANTS.md` >= 1
    - Cross-platform summary table shows ~250 total properties
  </verify>
  <done>
    INVARIANTS.md updated with Phase 098 additions, cross-platform catalog showing ~250 properties
  </done>
</task>

<task type="auto">
  <name>Create property testing patterns guide</name>
  <files>
    docs/PROPERTY_TESTING_PATTERNS.md
  </files>
  <action>
    Create comprehensive property testing patterns guide:

    **File:** `docs/PROPERTY_TESTING_PATTERNS.md`

    **Structure:**
    ```markdown
    # Property Testing Patterns Guide

    **Last Updated:** 2026-02-26
    **Applies to:** Atom platform v4.0 (backend, frontend, mobile, desktop)

    ---

    ## Overview

    Property-based testing validates invariants across hundreds of randomly generated inputs, finding edge cases that example-based testing misses. Atom uses 3 frameworks across 4 platforms with ~250 total property tests.

    **Quality over Quantity:** Focus on critical business invariants, not maximizing test counts.

    ---

    ## Framework Quick Reference

    | Platform | Framework | Config | Generator Example |
    |----------|-----------|--------|-------------------|
    | Backend Python | Hypothesis 6.151.5 | `@settings(max_examples=100)` | `st.integers(min=0, max=100)` |
    | Frontend TS | FastCheck 4.5.3 | `{ numRuns: 100 }` | `fc.integer({ min: 0, max: 100 })` |
    | Mobile TS | FastCheck 4.5.3 | `{ numRuns: 100 }` | `fc.integer({ min: 0, max: 100 })` |
    | Desktop Rust | proptest 1.0+ | `proptest! { #\[test] }` | `prop::num::i32::ANY` |

    ---

    ## Pattern Catalog

    ### Pattern 1: Invariant-First Testing

    **What:** Define the invariant (property that must always hold), then write tests.

    **When to use:** ALL property tests - start here always.

    **Backend Example (Hypothesis):**
    \```python
    @given(st.integers(), st.text())
    @settings(max_examples=100)
    def test_state_update_immutability(initial_count, initial_name):
        """
        INVARIANT: State update should not mutate input state
        VALIDATED_BUG: Shallow copy caused reference sharing
        Root cause: Using state.copy() instead of copy.deepcopy()
        """
        state = {"count": initial_count, "name": initial_name}
        state_copy = copy.deepcopy(state)
        update_state(state, {"count": initial_count + 1})
        assert state == state_copy  # Original unchanged
    \```

    **Frontend Example (FastCheck):**
    \```typescript
    fc.assert(
      fc.property(fc.integer(), (initialCount) => {
        const state = { count: initialCount };
        const stateCopy = JSON.stringify(state);
        counterReducer(state, { type: 'INCREMENT' });
        expect(JSON.stringify(state)).toBe(stateCopy);
      }),
      { numRuns: 100, seed: 23001 }
    );
    \```

    **See also:** `backend/tests/property_tests/state_management/test_state_management_invariants.py`

    ---

    ### Pattern 2: State Machine Testing

    **What:** Model stateful systems with explicit state transitions and validate invariants.

    **When to use:** Testing sync status, auth flows, workflow states, canvas lifecycle.

    **Mobile Example (FastCheck):**
    \```typescript
    const validTransitions: Record<string, string[]> = {
      pending: ['syncing', 'failed'],
      syncing: ['completed', 'failed'],
      completed: [],
      failed: ['syncing'],
    };

    fc.assert(
      fc.property(
        fc.constantFrom(...['pending', 'syncing', 'completed', 'failed']),
        fc.constantFrom(...['pending', 'syncing', 'completed', 'failed']),
        (fromStatus, toStatus) => {
          if (fromStatus === toStatus) return true;
          const allowed = validTransitions[fromStatus] || [];
          if (allowed.length === 0) {
            expect(toStatus).toBe(fromStatus);
          } else {
            expect(allowed).toContain(toStatus);
          }
        }
      ),
      { numRuns: 100 }
    );
    \```

    **See also:** `mobile/src/__tests__/property/advanced-sync-invariants.test.ts`

    ---

    ### Pattern 3: Round-Trip Invariants

    **What:** Verify serialize → deserialize returns original data.

    **When to use:** Testing JSON encoding, file I/O, API contracts, IPC messages.

    **Desktop Example (Rust proptest):**
    \```rust
    proptest! {
        #[test]
        fn prop_ipc_roundtrip(cmd in any::<ICommand>()) {
            /// INVARIANT: IPC serialization round-trip preserves data
            let serialized = serde_json::to_string(&cmd).unwrap();
            let deserialized: ICommand = serde_json::from_str(&serialized).unwrap();
            prop_assert_eq!(cmd, deserialized);
        }
    }
    \```

    **See also:** `frontend-nextjs/src-tauri/tests/ipc_serialization_proptest.rs`

    ---

    ### Pattern 4: Generator Composition

    **What:** Build complex test data generators from simple primitives.

    **When to use:** Testing complex data structures, business logic with nested objects.

    **Frontend Example (FastCheck):**
    \```typescript
    const actionGenerator = fc.record({
      id: fc.uuid(),
      type: fc.constantFrom('agent_message', 'workflow_trigger'),
      priority: fc.integer({ min: 1, max: 10 }),
      created_at: fc.integer({ min: 1000000000, max: 9999999999 }),
      payload: fc.object(),
    });

    fc.assert(fc.property(actionGenerator, (action) => {
      expect(action.priority).toBeGreaterThanOrEqual(1);
      expect(action.priority).toBeLessThanOrEqual(10);
    }));
    \```

    **See also:** `mobile/src/__tests__/property/queueInvariants.test.ts`

    ---

    ## Best Practices

    ### 1. VALIDATED_BUG Documentation
    Every property test must document edge cases found or validated:

    \```typescript
    /**
     * INVARIANT: [Description of invariant]
     *
     * VALIDATED_BUG: [Description of bug found or "None"]
     * Root cause: [Why bug occurred]
     * Fixed in: [Commit hash or "N/A"]
     * Scenario: [Example that triggered bug]
     */
    \```

    ### 2. Deterministic Seeds
    Always set seeds for reproducibility:
    - FastCheck: `{ seed: 23001 }`
    - Hypothesis: `@settings(seed=12345)`
    - proptest: Use `proptest` seed via environment variable

    ### 3. numRuns Tuning
    - Fast (in-memory): 100 runs
    - Medium (file I/O): 50 runs
    - Slow (network): 10-20 runs

    ### 4. Test Isolation
    - Property tests should not depend on shared state
    - Mock external dependencies (databases, APIs)
    - Use pure functions when possible

    ---

    ## Anti-Patterns to Avoid

    ### Weak Properties
    **Bad:** `expect(x).toBe(x)` - always passes
    **Good:** Test business logic that could fail

    ### Over-Constrained Generators
    **Bad:** Filtering out too many edge cases
    **Good:** Test the validation logic itself

    ### Ignoring Reproducibility
    **Bad:** Not setting seed, flaky tests
    **Good:** Always set seed, deterministic results

    ---

    ## Adding New Property Tests

    ### Step 1: Identify Invariant
    - What property MUST always hold true?
    - What are the failure modes?
    - Is this business-critical?

    ### Step 2: Choose Framework
    - Backend Python: Hypothesis
    - Frontend/Mobile TS: FastCheck
    - Desktop Rust: proptest

    ### Step 3: Write Test
    - Follow pattern catalog above
    - Include VALIDATED_BUG docstring
    - Set appropriate numRuns and seed

    ### Step 4: Verify
    - Test must pass (100%)
    - Add to INVARIANTS.md catalog
    - Run in CI to catch regressions

    ---

    ## Platform-Specific Guidelines

    ### Backend (Python/Hypothesis)
    - Use `@given()` decorator with strategies
    - Use `@settings(max_examples=N)` for iteration count
    - Import from `hypothesis` strategies: `st.integers()`, `st.text()`, `st.builds()`

    ### Frontend (TypeScript/FastCheck)
    - Use `fc.assert(fc.property(...))` syntax
    - Wrap in `it()` blocks for Jest compatibility
    - Import from `fast-check`: `fc.integer()`, `fc.string()`, `fc.record()`

    ### Mobile (TypeScript/FastCheck)
    - Same as frontend (FastCheck)
    - Test with jest-expo runner
    - Mock Expo modules in `jest.setup.js`

    ### Desktop (Rust/proptest)
    - Use `proptest! { #\[test] }` macro
    - Use `prop::prelude::*` strategies
    - Run with `cargo test`

    ---

    ## Quality Gates

    - All property tests must pass (100% pass rate)
    - New invariants added to INVARIANTS.md
    - VALIDATED_BUG docstrings required
    - numRuns and seed documented

    ---

    ## Further Reading

    - Hypothesis Docs: https://hypothesis.readthedocs.io/
    - FastCheck Docs: https://fast-check.dev/
    - proptest Docs: https://altsysrq.github.io/proptest-book/
    - Backend INVARIANTS.md: `backend/tests/property_tests/INVARIANTS.md`
    ```

    Ensure file is at least 500 lines with comprehensive examples.
  </action>
  <verify>
    - `docs/PROPERTY_TESTING_PATTERNS.md` exists
    - `wc -l docs/PROPERTY_TESTING_PATTERNS.md` >= 500
    - `grep -c "Pattern [0-9]:" docs/PROPERTY_TESTING_PATTERNS.md` >= 4
    - `grep -c "## Platform-Specific" docs/PROPERTY_TESTING_PATTERNS.md` >= 1
    - File includes framework quick reference table
  </verify>
  <done>
    Property testing patterns guide created with 500+ lines covering all platforms and patterns
  </done>
</task>

</tasks>

<verification>
## Plan Verification

1. **INVARIANTS.md updated:**
   - Phase 098 additions documented
   - Cross-platform summary shows ~250 total properties
   - Critical business invariant inventory included
   - Best practices section added

2. **PROPERTY_TESTING_PATTERNS.md created:**
   - 500+ lines comprehensive guide
   - Framework quick reference table
   - Pattern catalog with 4+ patterns
   - Platform-specific guidelines for all 4 platforms
   - Anti-patterns documented

3. **Quality metrics:**
   - ~250 properties documented (exceeds 30+ target by 8x)
   - All platforms covered
   - Best practices documented for future tests

4. **Documentation quality:**
   - Examples link to actual test files
   - VALIDATED_BUG pattern explained
   - numRuns and seed guidelines provided
</verification>

<success_criteria>
- INVARIANTS.md updated with Phase 098 additions
- PROPERTY_TESTING_PATTERNS.md created (500+ lines)
- ~250 total properties documented across platforms
- Best practices guide available for developers
- Critical invariant inventory created
</success_criteria>

<output>
After completion, create `.planning/phases/098-property-testing-expansion/098-05-SUMMARY.md` with:
- Documentation created (INVARIANTS.md updates, new guide)
- Property test totals across platforms
- Best practices documented
- Quality metrics summary
</output>
