---
phase: 090-quality-gates-cicd
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/scripts/check_pass_rate.py
  - backend/tests/coverage_reports/metrics/test_health.json
  - backend/pytest.ini
  - .github/workflows/test-coverage.yml
autonomous: true

must_haves:
  truths:
    - "Test suite maintains 98%+ pass rate across all tests"
    - "Flaky tests are automatically detected and tracked across runs"
    - "Failures are categorized by type (assertion, error, timeout, skip)"
    - "CI gate blocks PR if pass rate drops below 98%"
    - "Test health metrics stored in test_health.json for trend analysis"
  artifacts:
    - path: "backend/tests/scripts/check_pass_rate.py"
      provides: "Pass rate validation script"
      min_lines: 80
    - path: "backend/tests/coverage_reports/metrics/test_health.json"
      provides: "Test health metrics storage"
      contains: "pass_rate_history"
    - path: "backend/tests/scripts/detect_flaky_tests.py"
      provides: "Flaky test detection script"
      min_lines: 60
  key_links:
    - from: ".github/workflows/test-coverage.yml"
      to: "backend/tests/scripts/check_pass_rate.py"
      via: "Python script execution in CI"
      pattern: "check_pass_rate.py"
    - from: "backend/tests/scripts/check_pass_rate.py"
      to: "backend/tests/coverage_reports/metrics/test_health.json"
      via: "JSON read/write for persistence"
      pattern: "test_health.json"
---

<objective>
Implement test pass rate validation with flaky test detection to ensure test suite health and prevent unreliable tests from blocking development.

Purpose: A 98%+ pass rate ensures the test suite remains reliable and actionable. Flaky test detection identifies tests that fail intermittently, allowing targeted fixes before they waste developer time. Pass rate tracking provides visibility into test suite health over time.

Output: Pass rate validation script, flaky test detection, test_health.json for metrics storage, CI gate enforcement, pytest configuration for test reliability
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/TESTING.md
@backend/pytest.ini
@backend/tests/conftest.py
@.github/workflows/ci.yml
@.github/workflows/test-coverage.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pass rate validation script</name>
  <files>backend/tests/scripts/check_pass_rate.py</files>
  <action>
    Create backend/tests/scripts/check_pass_rate.py that:

    1. Parses pytest JSON output (--json-report flag) for test results
    2. Calculates pass rate: passed / (passed + failed) * 100
    3. Enforces 98% minimum threshold (configurable via --minimum flag)
    4. Categorizes failures: assertion errors, exceptions, timeouts, skips
    5. Outputs summary with pass/fail counts, percentage, failure breakdown
    6. Returns exit code 0 if >=98%, exit code 1 if below threshold

    Script structure:
    - parse_pytest_json(json_file) -> dict with passed, failed, error, skipped counts
    - calculate_pass_rate(results) -> float percentage
    - categorize_failures(results) -> dict mapping failure type to count
    - print_summary(pass_rate, results, categories) -> formatted output
    - main() with argparse for --json-file, --minimum, --verbose options

    Use json for parsing, argparse for CLI, sys.exit for return codes.
  </action>
  <verify>
    python backend/tests/scripts/check_pass_rate.py --help
  </verify>
  <done>
    Pass rate script validates 98% threshold, categorizes failure types, provides clear output. Can parse pytest JSON output or run pytest internally.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create flaky test detection script</name>
  <files>backend/tests/scripts/detect_flaky_tests.py</files>
  <action>
    Create backend/tests/scripts/detect_flaky_tests.py that:

    1. Runs tests 3 times with different random seeds (--random-order-seed)
    2. Records which tests fail in each run
    3. Identifies flaky tests: pass sometimes, fail sometimes
    4. Outputs list of flaky tests with failure frequency
    5. Updates test_health.json with flaky test entries

    Script structure:
    - run_tests_with_seed(seed) -> subprocess call to pytest
    - parse_test_results(output) -> set of failed test names
    - compare_results(results_list) -> dict mapping test to failure count
    - identify_flaky(failure_counts) -> tests with 1-2 failures (not 0 or 3)
    - update_health_json(flaky_tests) -> append to flaky_tests array
    - main() with --runs, --seed-count, --update-json options

    Use subprocess for pytest, json for health file, argparse for options.
  </action>
  <verify>
    python backend/tests/scripts/detect_flaky_tests.py --help
  </verify>
  <done>
    Flaky test detection runs tests multiple times, identifies inconsistent failures, updates health JSON. Helps maintain test suite reliability.
  </done>
</task>

<task type="auto">
  <name>Task 3: Initialize test_health.json for metrics tracking</name>
  <files>backend/tests/coverage_reports/metrics/test_health.json</files>
  <action>
    Create backend/tests/coverage_reports/metrics/test_health.json with structure:
    {
      "pass_rate_history": [
        {
          "date": "2026-02-25",
          "phase": "090",
          "plan": "02",
          "total_tests": 342,
          "passed": 342,
          "failed": 0,
          "skipped": 0,
          "pass_rate": 100.0,
          "duration_seconds": 120
        }
      ],
      "flaky_tests": [],
      "failure_categories": {
        "assertion": 0,
        "error": 0,
        "timeout": 0,
        "skip": 0
      },
      "baseline": {
        "date": "2026-02-25",
        "pass_rate": 100.0,
        "total_tests": 342
      },
      "metadata": {
        "format_version": 1,
        "minimum_pass_rate": 98.0,
        "last_updated": "2026-02-25T00:00:00Z"
      }
    }

    Use current test count from ROADMAP (342 tests from Phase 03). Create directory if doesn't exist.
  </action>
  <verify>
    cat backend/tests/coverage_reports/metrics/test_health.json | jq .pass_rate_history
  </verify>
  <done>
    test_health.json initialized with current test suite baseline. Future runs will track pass rate trends and flaky test detection results.
  </done>
</task>

<task type="auto">
  <name>Task 4: Configure pytest for test reliability</name>
  <files>backend/pytest.ini backend/pyproject.toml</files>
  <action>
    Update test configuration for reliability:

    1. pytest.ini: Add to addopts:
       - --random-order for test independence validation
       - --reruns 2 with --reruns-delay 1 for flaky test retries
       - --json-report --json-report-file=pytest_report.json for CI parsing
       - --maxfail=10 to stop after 10 failures (save CI time)

    2. pyproject.toml: Add to [project.optional-dependencies]:
       - Add pytest-json-report, pytest-random-order, pytest-rerunfailures
       - Create test-quality flavor with these dependencies

    3. Document test markers for flaky tests in pytest.ini markers section:
       - flaky: Tests that may be flaky and need retry (temporary workaround only)

    Do NOT modify existing markers or test paths.
  </action>
  <verify>
    grep "random-order" backend/pytest.ini
    grep "pytest-json-report" backend/pyproject.toml
  </verify>
  <done>
    pytest configured for reliability testing. Random order catches state dependencies. Reruns handle transient failures. JSON output enables CI parsing.
  </done>
</task>

</tasks>

<verification>
# Plan Verification Steps

1. **Pass rate script functional**
   ```bash
   cd backend && python tests/scripts/check_pass_rate.py --help
   python tests/scripts/check_pass_rate.py --minimum 98
   ```

2. **Flaky test detection functional**
   ```bash
   cd backend && python tests/scripts/detect_flaky_tests.py --runs 2 --help
   ```

3. **test_health.json valid and initialized**
   ```bash
   cat backend/tests/coverage_reports/metrics/test_health.json | jq .pass_rate_history[0]
   cat backend/tests/coverage_reports/metrics/test_health.json | jq .flaky_tests
   ```

4. **pytest reliability options configured**
   ```bash
   grep "random-order" backend/pytest.ini
   grep "reruns" backend/pytest.ini
   grep "json-report" backend/pytest.ini
   ```

5. **End-to-end: Run tests with pass rate check**
   ```bash
   cd backend
   pytest tests/ --json-report --json-report-file=pytest_report.json -q
   python tests/scripts/check_pass_rate.py --json-file pytest_report.json
   ```

6. **CI workflow includes pass rate gate**
   ```bash
   grep "check_pass_rate" .github/workflows/test-coverage.yml
   grep "98%" .github/workflows/ci.yml
   ```
</verification>

<success_criteria>
- Pass rate validation enforces 98% minimum threshold
- Flaky tests automatically detected and tracked
- Test health metrics stored in test_health.json
- pytest configured with random order and reruns for reliability
- CI pipeline includes pass rate gate blocking low-quality PRs
- Clear documentation for test quality standards
</success_criteria>

<output>
After completion, create `.planning/phases/090-quality-gates-cicd/090-02-SUMMARY.md` with:
- Files created/modified (4 files)
- Pass rate threshold (98% minimum)
- Flaky test detection behavior (3 runs with random seeds)
- test_health.json baseline (100% pass rate, 342 tests)
- pytest reliability options (random-order, reruns, json-report)
- Verification results (all 6 checks passed)
</output>
