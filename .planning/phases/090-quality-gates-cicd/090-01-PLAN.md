---
phase: 090-quality-gates-cicd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pytest.ini
  - backend/pyproject.toml
  - backend/tests/coverage_reports/metrics/trending.json
  - .github/workflows/test-coverage.yml
autonomous: true

must_haves:
  truths:
    - "Pre-commit hook enforces 80% minimum coverage on new code changes"
    - "Coverage threshold configured in pytest-cov with fail-under=80"
    - "Coverage diff check prevents coverage regression in PRs (5% minimum)"
    - "trending.json stores historical coverage data for trend analysis"
    - "Coverage enforcement is documented and discoverable by developers"
  artifacts:
    - path: "backend/.git/hooks/pre-commit"
      provides: "Pre-commit hook enforcing coverage gate"
      min_lines: 20
    - path: "backend/tests/scripts/enforce_coverage.py"
      provides: "Coverage enforcement script"
      min_lines: 50
    - path: "backend/tests/coverage_reports/metrics/trending.json"
      provides: "Historical coverage data storage"
      contains: "coverage_history"
  key_links:
    - from: "backend/.git/hooks/pre-commit"
      to: "backend/tests/scripts/enforce_coverage.py"
      via: "Python subprocess execution"
      pattern: "python.*enforce_coverage"
    - from: ".github/workflows/test-coverage.yml"
      to: "backend/tests/coverage_reports/metrics/trending.json"
      via: "Coverage data persistence"
      pattern: "trending.json"
---

<objective>
Establish pre-commit coverage enforcement gates to prevent coverage regression and ensure new code meets minimum coverage standards.

Purpose: Coverage enforcement gates prevent technical debt accumulation by requiring 80% coverage on new code and preventing coverage regressions in PRs. This ensures the test suite grows alongside the codebase and critical paths remain protected.

Output: Pre-commit hook enforcing 80% coverage, pytest-cov configuration with fail-under threshold, trending.json for historical data, updated CI workflow with coverage checks
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/TESTING.md
@backend/pytest.ini
@backend/pyproject.toml
@backend/tests/conftest.py
@.github/workflows/test-coverage.yml
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure pytest-cov with coverage enforcement threshold</name>
  <files>backend/pytest.ini</files>
  <action>
    Update pytest.ini [coverage:run] and [coverage:report] sections:

    1. Set fail_under = 80 in [coverage:report] section
    2. Add precision = 2 for consistent reporting
    3. Configure skip_covered = false for full visibility
    4. Add fail_under branch coverage (set to 70)
    5. Add partial_branches = true for better branch coverage tracking

    Do NOT modify existing exclude_lines or omit patterns.

    Reference existing configuration in pytest.ini lines 91-122.
  </action>
  <verify>
    grep -A5 "\[coverage:report\]" backend/pytest.ini | grep "fail_under = 80"
  </verify>
  <done>
    Coverage threshold enforced at 80% line coverage, 70% branch coverage. Running pytest with --cov flag will fail if coverage drops below threshold.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create coverage enforcement script for pre-commit hook</name>
  <files>backend/tests/scripts/enforce_coverage.py</files>
  <action>
    Create Python script backend/tests/scripts/enforce_coverage.py that:

    1. Runs pytest with coverage on changed files only (using git diff)
    2. Parses coverage.json output for new/modified files
    3. Enforces 80% minimum on new code, 5% improvement on modified files
    4. Outputs clear error messages showing which files fail and by how much
    5. Returns exit code 1 on failure, 0 on success

    Script structure:
    - parse_args() for options (files-only, minimum, verbose)
    - get_changed_files() using git diff HEAD or --staged
    - run_coverage() subprocess to pytest with --cov-fail-under=0 (calculate but don't fail)
    - check_coverage_thresholds() compares actual vs minimum
    - main() orchestrates the flow

    Use argparse for CLI, subprocess for pytest invocation, json for coverage.json parsing.
  </action>
  <verify>
    python backend/tests/scripts/enforce_coverage.py --help
  </verify>
  <done>
    Coverage enforcement script can be run standalone, supports --files-only for PR validation, --minimum flag for custom thresholds, and --verbose for detailed output.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create pre-commit hook for coverage enforcement</name>
  <files>backend/.git/hooks/pre-commit backend/tests/scripts/install_hooks.sh</files>
  <action>
    Create two files:

    1. backend/.git/hooks/pre-commit (executable shell script):
       - Runs enforce_coverage.py on staged Python files (*.py)
       - Only runs if Python files were changed (git diff --name-only --cached | grep .py)
       - Outputs coverage summary in CI format (pass/fail)
       - Exits with code 1 to block commit if coverage fails
       - Include helpful message: "Run 'pytest tests/ --cov' to see full coverage report"

    2. backend/tests/scripts/install_hooks.sh (setup script):
       - Copies pre-commit hook to .git/hooks/pre-commit (makes it executable)
       - Creates symlink or copy from repo .githooks/pre-commit if exists
       - Provides instructions for manual setup if automated install fails
       - Checks if .git/hooks directory exists

    Use bash shebang, set -e for error handling, chmod +x for executable permission.
  </action>
  <verify>
    test -x backend/.git/hooks/pre-commit || backend/tests/scripts/install_hooks.sh && test -x backend/.git/hooks/pre-commit
  </verify>
  <done>
    Pre-commit hook installed and executable. Committing Python code without 80% coverage is blocked. Install script enables easy setup for all developers.
  </done>
</task>

<task type="auto">
  <name>Task 4: Initialize trending.json for historical coverage tracking</name>
  <files>backend/tests/coverage_reports/metrics/trending.json</files>
  <action>
    Create backend/tests/coverage_reports/metrics/trending.json with structure:
    {
      "coverage_history": [
        {
          "date": "2026-02-25",
          "phase": "090",
          "plan": "01",
          "coverage_percent": 74.55,
          "files_covered": 156,
          "files_total": 205,
          "branches_covered": 52,
          "branches_total": 74
        }
      ],
      "baseline": {
        "date": "2026-02-25",
        "coverage_percent": 74.55
      },
      "metadata": {
        "format_version": 1,
        "last_updated": "2026-02-25T00:00:00Z"
      }
    }

    Use current coverage.json values for baseline. Create directory if doesn't exist.
  </action>
  <verify>
    cat backend/tests/coverage_reports/metrics/trending.json | jq .coverage_history
  </verify>
  <done>
    trending.json initialized with current coverage baseline. Future coverage runs will append entries for trend analysis and regression detection.
  </done>
</task>

</tasks>

<verification>
# Plan Verification Steps

1. **Pre-commit hook installed and executable**
   ```bash
   test -x backend/.git/hooks/pre-commit && echo "Hook exists" || backend/tests/scripts/install_hooks.sh
   ```

2. **Coverage enforcement script functional**
   ```bash
   cd backend && python tests/scripts/enforce_coverage.py --help
   python tests/scripts/enforce_coverage.py --minimum 80 --files-only
   ```

3. **pytest-cov threshold configured**
   ```bash
   grep "fail_under = 80" backend/pytest.ini
   ```

4. **trending.json initialized with valid JSON**
   ```bash
   cat backend/tests/coverage_reports/metrics/trending.json | jq .coverage_history[0]
   ```

5. **End-to-end: Attempt commit with low coverage file**
   - Create test file with <80% coverage
   - Stage with git add
   - Run pre-commit hook directly: .git/hooks/pre-commit
   - Verify hook blocks commit (exit code 1)

6. **CI workflow integration verified**
   ```bash
   grep "cov-fail-under=80" .github/workflows/test-coverage.yml
   grep "trending.json" .github/workflows/ci.yml
   ```
</verification>

<success_criteria>
- Pre-commit hook enforces 80% minimum coverage on new code
- Modified code requires 5% coverage improvement or must maintain existing level
- pytest-cov fails entire test suite if coverage drops below 80%
- trending.json stores baseline coverage for trend comparison
- Clear error messages guide developers to fix coverage issues
- Documentation exists for hook installation and usage
</success_criteria>

<output>
After completion, create `.planning/phases/090-quality-gates-cicd/090-01-SUMMARY.md` with:
- Files created/modified (4 files)
- Coverage threshold configured (80% line, 70% branch)
- Pre-commit hook behavior (blocks low coverage commits)
- trending.json baseline (74.55% current coverage)
- Verification results (all 6 checks passed)
</output>
