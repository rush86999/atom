---
phase: 292-coverage-baselines-prioritization
plan: 02
type: execute
wave: 2
depends_on:
  - 292-01
files_modified:
  - backend/tests/scripts/coverage_to_prioritize.py
  - backend/tests/scripts/prioritize_frontend_components.py
  - backend/tests/coverage_reports/metrics/prioritized_files_v11.0.json
  - backend/tests/coverage_reports/HIGH_IMPACT_PRIORITIZATION_v11.0.md
  - frontend-nextjs/coverage/prioritized_frontend_components_v11.0.json
  - frontend-nextjs/coverage/HIGH_IMPACT_FRONTEND_COMPONENTS_v11.0.md
autonomous: true
requirements:
  - COV-B-05
  - COV-F-05

must_haves:
  truths:
    - "Backend high-impact file list exists with 3 tiers (Tier 1: <10% coverage >200 lines, Tier 2: 10-30% >200 lines, Tier 3: 30-50% >200 lines)"
    - "Files are sorted by priority_score descending within each tier"
    - "Frontend high-impact component list exists prioritized by business criticality (Canvas > Chat > Agent Dashboard > Integrations > Other)"
    - "Both lists delivered in JSON (parseable by json.load()) and Markdown formats"
    - "Frontend and backend lists target complementary (non-overlapping) files"
  artifacts:
    - path: "backend/tests/coverage_reports/metrics/prioritized_files_v11.0.json"
      provides: "Backend prioritized file list with tier, priority_score, coverage_pct per file"
      contains: '"tier_1"'
    - path: "backend/tests/coverage_reports/HIGH_IMPACT_PRIORITIZATION_v11.0.md"
      provides: "Human-readable backend prioritization report"
      contains: "Tier 1"
    - path: "frontend-nextjs/coverage/prioritized_frontend_components_v11.0.json"
      provides: "Frontend prioritized component list with criticality tier"
      contains: '"criticality"'
    - path: "frontend-nextjs/coverage/HIGH_IMPACT_FRONTEND_COMPONENTS_v11.0.md"
      provides: "Human-readable frontend prioritization report"
      contains: "Canvas"
  key_links:
    - from: "coverage_to_prioritize.py"
      to: "phase_292_backend_baseline.json"
      via: "reads and converts coverage data"
      pattern: "phase_292_backend_baseline"
    - from: "prioritize_high_impact_files.py"
      to: "prioritized_files_v11.0.json"
      via: "calculate priority_score = (uncovered * impact) / (coverage + 1)"
      pattern: "priority_score"
    - from: "prioritized_files_v11.0.json"
      to: "Phases 293-295"
      via: "consumed by coverage expansion executor scripts"
      pattern: "json.load"
---

<objective>
Generate prioritized high-impact file lists for backend and frontend to guide coverage expansion in Phases 293-295.

Purpose: Create actionable, tiered prioritization lists so coverage expansion work targets the highest-impact files first. Backend uses the proven priority_score formula with business impact weighting. Frontend uses business criticality (Canvas, Chat, Agent Dashboard, Integrations) as the primary sort key.

Output:
1. Backend wrapper script: `coverage_to_prioritize.py` (converts raw coverage.json to prioritize_high_impact_files.py input format)
2. Frontend prioritization script: `prioritize_frontend_components.py` (generates frontend lists by business criticality)
3. Backend JSON list: `prioritized_files_v11.0.json` (machine-readable with 3 tiers)
4. Backend Markdown report: `HIGH_IMPACT_PRIORITIZATION_v11.0.md` (human-readable)
5. Frontend JSON list: `prioritized_frontend_components_v11.0.json`
6. Frontend Markdown report: `HIGH_IMPACT_FRONTEND_COMPONENTS_v11.0.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/292-coverage-baselines-prioritization/292-CONTEXT.md
@backend/tests/scripts/prioritize_high_impact_files.py
@backend/tests/scripts/business_impact_scorer.py
@backend/tests/coverage_reports/metrics/business_impact_scores.json
@backend/tests/coverage_reports/metrics/phase_292_backend_baseline.json
@frontend-nextjs/coverage/coverage-final.json
@backend/tests/coverage_reports/HIGH_IMPACT_FILES.md
</context>

<interfaces>

### Phase 292 Backend Baseline (fresh output from Plan 01)

Located at `backend/tests/coverage_reports/metrics/phase_292_backend_baseline.json`.

```json
{
  "meta": { "format": 3, "timestamp": "2026-04-24T...", "branch_coverage": true },
  "files": {
    "core/agent_governance_service.py": {
      "executed_lines": [1, 2, 3, ...],
      "missing_lines": [100, ...],
      "summary": { "percent_covered": 85.3, "num_statements": 200, "covered_lines": 170, "missing_lines": 30 }
    },
    ...
  },
  "totals": { "percent_covered": 18.25, ... }
}
```

### Input format expected by prioritize_high_impact_files.py

The `--baseline` file must have a `files_below_threshold` array. Each entry needs:
```json
{
  "file": "core/agent_governance_service.py",
  "coverage_pct": 8.5,
  "total_lines": 300,
  "covered_lines": 25,
  "uncovered_lines": 275
}
```

### Business impact scores (existing, authoritative per D-06)

From `backend/tests/coverage_reports/metrics/business_impact_scores.json`:
```json
{
  "all_files": [
    { "file": "core/agent_governance_service.py", "tier": "Critical", "score": 10 },
    ...
  ]
}
```

Tier scores: Critical=10, High=7, Medium=5, Low=3

### Frontend coverage-final.json structure

```json
{
  "total": { "lines": { "total": 10000, "covered": 1461, "pct": 14.61 } },
  "components/canvas/Canvas.tsx": {
    "lines": { "total": 150, "covered": 30, "pct": 20.0 }
  },
  ...
}
```

### Priority score formula (from prioritize_high_impact_files.py)

```
priority_score = (uncovered_lines * impact_score) / (coverage_pct + 1)
```

### Business criticality mapping for frontend (per D-07)

```
Critical: components/canvas/**, components/chat/**, components/agent-dashboard/**
High:     components/integrations/**
Medium:   components/ui/**, pages/**, lib/**
Low:      hooks/**, shared/**
```

</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create backend prioritization wrapper and generate tiered list</name>
  <files>
    backend/tests/scripts/coverage_to_prioritize.py
    backend/tests/coverage_reports/metrics/prioritized_files_v11.0.json
    backend/tests/coverage_reports/HIGH_IMPACT_PRIORITIZATION_v11.0.md
  </files>
  <read_first>
    backend/tests/scripts/prioritize_high_impact_files.py
    backend/tests/scripts/business_impact_scorer.py
    backend/tests/coverage_reports/metrics/business_impact_scores.json
    backend/tests/coverage_reports/metrics/phase_292_backend_baseline.json
    backend/tests/coverage_reports/HIGH_IMPACT_FILES.md
  </read_first>
  <action>
    Per D-05 (reuse existing scripts) and D-04 (3 tiers), create a wrapper script `coverage_to_prioritize.py` that converts raw pytest coverage.json into the `files_below_threshold` format expected by `prioritize_high_impact_files.py`, then runs the prioritization, post-processes into 3 tiers, and writes both JSON and Markdown output.

    **Step 1: Create the wrapper script.**

    Write `backend/tests/scripts/coverage_to_prioritize.py` with the following behavior:

    1. **CLI arguments:**
       `--coverage-file` (required, path to coverage.json)
       `--impact-file` (default `tests/coverage_reports/metrics/business_impact_scores.json`)
       `--min-lines` (default 200, per D-06)
       `--output-dir` (default `tests/coverage_reports/metrics/`)
       `--markdown-dir` (default `tests/coverage_reports/`)

    2. **Conversion logic:**
       Read the raw coverage.json. For each file in `files`:
       - Extract `filepath`, `summary.percent_covered`, `summary.num_statements`, `summary.covered_lines`
       - Calculate `uncovered_lines = num_statements - covered_lines`
       - Filter: ONLY include files with `num_statements >= min_lines` (default 200)
       - Build the `files_below_threshold` array with fields: `file`, `coverage_pct`, `total_lines`, `covered_lines`, `uncovered_lines`

    3. **Interim file:** Write temp baseline JSON to `{output_dir}/prioritize_input.json` with `{ "files_below_threshold": [...] }`

    4. **Invoke prioritize_high_impact_files.py:**
       `python tests/scripts/prioritize_high_impact_files.py --baseline {temp_file} --impact {impact_file} --output {output_dir}/prioritized_raw.json --report {temp_md} --min-coverage 50`

       Using `--min-coverage 50` captures all files below 50% so the 3 tiers are all included.

    5. **Post-process into 3 tiers:**
       Read raw prioritized output. Add `tier_label` to each ranked file:
       - `"Tier 1 (must-fix)"` when `coverage_pct < 10`
       - `"Tier 2 (should-fix)"` when `10 <= coverage_pct < 30`
       - `"Tier 3 (nice-to-fix)"` when `30 <= coverage_pct < 50`

       Group by `tier_label`.

    6. **Write final JSON** to `{output_dir}/prioritized_files_v11.0.json`:
       ```json
       {
         "generated_at": "<timestamp>",
         "baseline_source": "phase_292_backend_baseline.json",
         "formula": "priority_score = (uncovered_lines * impact_score) / (coverage_pct + 1)",
         "tier_definitions": {
           "Tier 1 (must-fix)": "< 10% coverage, > 200 lines (highest impact)",
           "Tier 2 (should-fix)": "10-30% coverage, > 200 lines",
           "Tier 3 (nice-to-fix)": "30-50% coverage, > 200 lines"
         },
         "summary": {
           "total_files_prioritized": <total>,
           "tier_1_count": <count>,
           "tier_2_count": <count>,
           "tier_3_count": <count>,
           "total_uncovered_lines": <sum>,
           "top_3_files": ["file1", "file2", "file3"]
         },
         "tier_1": [ files sorted by priority_score desc ],
         "tier_2": [ files sorted by priority_score desc ],
         "tier_3": [ files sorted by priority_score desc ]
       }
       ```

    7. **Write Markdown report** to `{markdown_dir}/HIGH_IMPACT_PRIORITIZATION_v11.0.md`:
       - Title and metadata
       - Executive Summary: total files, uncovered lines, tier counts
       - Tier Definitions table
       - Tier 1 section: Rank | File | Coverage% | Uncovered | Impact | Priority Score | Business Tier
       - Tier 2 section: same table format
       - Tier 3 section: top 20 only ("Showing top 20 of N")
       - Formula explanation
       - Quick wins: 0% coverage AND Critical/High business impact

    **Step 2: Run the wrapper.**

    ```
    cd /Users/rushiparikh/projects/atom/backend
    python tests/scripts/coverage_to_prioritize.py \
      --coverage-file tests/coverage_reports/metrics/phase_292_backend_baseline.json \
      --output-dir tests/coverage_reports/metrics/ \
      --markdown-dir tests/coverage_reports/
    ```

    **Step 3: Validate output.**

    Run the Python validation inline:
    ```python
    import json
    with open("tests/coverage_reports/metrics/prioritized_files_v11.0.json") as f:
        data = json.load(f)
    assert "tier_1" in data and "tier_2" in data and "tier_3" in data
    for tier_key in ["tier_1", "tier_2", "tier_3"]:
        files = data[tier_key]
        for i in range(len(files) - 1):
            assert files[i]["priority_score"] >= files[i+1]["priority_score"], \
                f"{tier_key} not sorted desc: {files[i]['file']}"
    print(f"OK: Tier1={len(data['tier_1'])} Tier2={len(data['tier_2'])} Tier3={len(data['tier_3'])}")
    print(f"Top 3: {data['summary']['top_3_files']}")
    ```
  </action>
  <acceptance_criteria>
    - `coverage_to_prioritize.py` exists and is executable
    - Running it produces `prioritized_files_v11.0.json` with tier_1, tier_2, tier_3 keys
    - Each tier array sorted by priority_score descending
    - `HIGH_IMPACT_PRIORITIZATION_v11.0.md` exists with Tier 1/2/3 sections
    - All output JSON parseable by json.load() (D-10)
    - Tier 1 files have coverage_pct < 10 (D-04)
    - Tier 2 files have 10 <= coverage_pct < 30 (D-04)
    - Tier 3 files have 30 <= coverage_pct < 50 (D-04)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/rushiparikh/projects/atom/backend && python -c "import json; d=json.load(open('tests/coverage_reports/metrics/prioritized_files_v11.0.json')); assert 'tier_1' in d; assert 'tier_2' in d; assert 'tier_3' in d; print(f'T1={len(d[\"tier_1\"])} T2={len(d[\"tier_2\"])} T3={len(d[\"tier_3\"])}')" && test -f tests/coverage_reports/HIGH_IMPACT_PRIORITIZATION_v11.0.md && grep -q "Tier 1" tests/coverage_reports/HIGH_IMPACT_PRIORITIZATION_v11.0.md</automated>
  </verify>
  <done>Backend tiered high-impact file list generated in JSON and Markdown, structural validation passed.</done>
</task>

<task type="auto">
  <name>Task 2: Create frontend high-impact component list by business criticality</name>
  <files>
    backend/tests/scripts/prioritize_frontend_components.py
    frontend-nextjs/coverage/prioritized_frontend_components_v11.0.json
    frontend-nextjs/coverage/HIGH_IMPACT_FRONTEND_COMPONENTS_v11.0.md
  </files>
  <read_first>
    frontend-nextjs/jest.config.js
    frontend-nextjs/coverage/coverage-final.json
  </read_first>
  <action>
    Per D-07, frontend components are prioritized by BUSINESS CRITICALITY first -- Canvas, Chat, Agent Dashboard, Integrations -- with coverage percentage as a secondary factor.

    Create a Python script `backend/tests/scripts/prioritize_frontend_components.py` that reads the frontend coverage JSON and produces prioritized component lists.

    **Step 1: Create the frontend prioritization script.**

    Write `backend/tests/scripts/prioritize_frontend_components.py` with:

    1. **CLI arguments:**
       `--coverage-file` (required, path to frontend coverage-final.json)
       `--output-dir` (default, e.g., the frontend-nextjs/coverage/ directory)
       `--markdown-dir` (default, same as output-dir)

    2. **Business criticality mapping** (per D-07):
       ```python
       CRITICALITY_TIERS = {
           "Critical": {
               "patterns": ["components/canvas/", "components/chat/", "components/agent-dashboard/"],
               "description": "Core user-facing features (Canvas presentations, Chat interface, Agent Dashboard)",
               "score": 10
           },
           "High": {
               "patterns": ["components/integrations/"],
               "description": "Integration components (WhatsApp, Slack, etc.)",
               "score": 7
           },
           "Medium": {
               "patterns": ["components/ui/", "pages/", "lib/"],
               "description": "UI components, page layouts, utility libraries",
               "score": 5
           },
           "Low": {
               "patterns": ["hooks/", "shared/"],
               "description": "Custom hooks, shared utilities",
               "score": 3
           }
       }
       ```

    3. **Scoring logic:**
       For each file in the coverage JSON:
       - Determine `criticality_tier` by matching filepath patterns (check Critical first, then High, then Medium, then Low)
       - Compute `uncovered_lines = lines.total - lines.covered`
       - Compute `priority_score = (uncovered_lines * criticality_score) / (lines.pct + 1)` (same formula as backend for consistency, per D-05 spirit)
       - Filter: include ALL files (no LOC minimum, since frontend components are smaller than backend services)

    4. **Sorting** (per D-07):
       Primary sort: criticality_score descending (Critical first)
       Secondary sort: priority_score descending (within same criticality)
       Tertiary sort: lines.total descending (more code = more impact)

    5. **Write JSON** to `{output_dir}/prioritized_frontend_components_v11.0.json`:
       ```json
       {
         "generated_at": "<timestamp>",
         "baseline_source": "coverage-final.json",
         "criticality_definitions": {
           "Critical": "Core features: Canvas, Chat, Agent Dashboard",
           "High": "Integration components",
           "Medium": "UI, pages, lib",
           "Low": "Hooks, shared"
         },
         "summary": {
           "total_components": <total>,
           "critical_count": <count>,
           "high_count": <count>,
           "medium_count": <count>,
           "low_count": <count>,
           "overall_line_coverage": <from total.lines.pct>,
           "top_3_components": ["file1", "file2", "file3"]
         },
         "by_criticality": {
           "Critical": [ files sorted by priority_score desc ],
           "High": [ files sorted by priority_score desc ],
           "Medium": [ files sorted by priority_score desc ],
           "Low": [ files sorted by priority_score desc ]
         }
       }
       ```
       Each file entry: `file`, `coverage_pct`, `total_lines`, `covered_lines`, `uncovered_lines`, `criticality_tier`, `criticality_score`, `priority_score`

    6. **Write Markdown report** to `{markdown_dir}/HIGH_IMPACT_FRONTEND_COMPONENTS_v11.0.md`:
       - Title: "# Frontend High-Impact Component Prioritization v11.0"
       - Metadata: Phase 292, generated timestamp
       - Executive Summary: total components, overall line coverage, tier counts
       - Criticality Definitions table
       - Critical section with table: Rank | Component | Coverage% | Lines | Uncovered | Priority Score
       - High section with same table format
       - Medium section (top 30 only) with note "Showing top 30 of N"
       - Low section (top 10 only) with note "Showing top 10 of N"
       - Priority Score Formula explanation (same formula as backend)
       - Quick wins section: components with 0% coverage in Critical/High tiers

    **Step 2: Run the script.**

    ```
    cd /Users/rushiparikh/projects/atom/backend
    python tests/scripts/prioritize_frontend_components.py \
      --coverage-file /Users/rushiparikh/projects/atom/frontend-nextjs/coverage/coverage-final.json \
      --output-dir /Users/rushiparikh/projects/atom/frontend-nextjs/coverage/
    ```

    **Step 3: Validate output.**

    ```python
    import json
    with open("/Users/rushiparikh/projects/atom/frontend-nextjs/coverage/prioritized_frontend_components_v11.0.json") as f:
        data = json.load(f)
    assert "by_criticality" in data, "Missing by_criticality"
    for tier in ["Critical", "High", "Medium", "Low"]:
        assert tier in data["by_criticality"], f"Missing tier {tier}"
        files = data["by_criticality"][tier]
        for i in range(len(files) - 1):
            assert files[i]["priority_score"] >= files[i+1]["priority_score"], \
                f"{tier} not sorted: {files[i]['file']}"
    print(f"OK: Critical={len(data['by_criticality']['Critical'])} High={len(data['by_criticality']['High'])}")
    print(f"Medium={len(data['by_criticality']['Medium'])} Low={len(data['by_criticality']['Low'])}")
    print(f"Top 3: {data['summary']['top_3_components']}")
    ```
  </action>
  <acceptance_criteria>
    - `backend/tests/scripts/prioritize_frontend_components.py` exists and is executable
    - Running it produces `prioritized_frontend_components_v11.0.json` in frontend-nextjs/coverage/
    - JSON has `by_criticality` with Critical, High, Medium, Low keys
    - Each tier array sorted by priority_score descending (D-07)
    - Critical tier lists Canvas, Chat, Agent Dashboard components first (D-07)
    - `HIGH_IMPACT_FRONTEND_COMPONENTS_v11.0.md` exists in frontend-nextjs/coverage/
    - Markdown contains "Critical", "High", "Medium", "Low" sections
    - All JSON output parseable by json.load() (D-10)
  </acceptance_criteria>
  <verify>
    <automated>test -f /Users/rushiparikh/projects/atom/frontend-nextjs/coverage/prioritized_frontend_components_v11.0.json && python -c "
import json
d = json.load(open('/Users/rushiparikh/projects/atom/frontend-nextjs/coverage/prioritized_frontend_components_v11.0.json'))
assert 'by_criticality' in d
assert 'Critical' in d['by_criticality']
assert 'High' in d['by_criticality']
print(f'OK: C={len(d[\"by_criticality\"][\"Critical\"])} H={len(d[\"by_criticality\"][\"High\"])} M={len(d[\"by_criticality\"][\"Medium\"])} L={len(d[\"by_criticality\"][\"Low\"])}')
" && test -f /Users/rushiparikh/projects/atom/frontend-nextjs/coverage/HIGH_IMPACT_FRONTEND_COMPONENTS_v11.0.md && grep -q "Critical" /Users/rushiparikh/projects/atom/frontend-nextjs/coverage/HIGH_IMPACT_FRONTEND_COMPONENTS_v11.0.md</automated>
  </verify>
  <done>Frontend high-impact component list generated by business criticality, JSON+Markdown delivered.</done>
</task>

<task type="auto">
  <name>Task 3: Cross-check outputs and finalize deliverables</name>
  <files>
    backend/tests/coverage_reports/metrics/prioritized_files_v11.0.json
    frontend-nextjs/coverage/prioritized_frontend_components_v11.0.json
  </files>
  <read_first>
    backend/tests/coverage_reports/metrics/prioritized_files_v11.0.json
    frontend-nextjs/coverage/prioritized_frontend_components_v11.0.json
  </read_first>
  <action>
    Per D-10 (cross-check that lists target complementary files), run a validation that confirms:

    1. **No overlapping files**: Backend list contains Python files in `core/`, `api/`, `tools/`. Frontend list contains TypeScript/TSX files. They should have zero overlap. Verify this.

    2. **All output is parseable**: Run `json.load()` on all four output files.

    3. **Print final summary** to stdout:

    ```
    ============================================================
    PHASE 292 PRIORITIZATION COMPLETE
    ============================================================

    BACKEND HIGH-IMPACT FILES:
      Tier 1 (must-fix, <10% coverage):       XX files, XX uncovered lines
      Tier 2 (should-fix, 10-30% coverage):    XX files, XX uncovered lines
      Tier 3 (nice-to-fix, 30-50% coverage):   XX files, XX uncovered lines
      Total:                                    XX files, XX uncovered lines
      Top 3: 1. file1 (priority: XXX)
             2. file2 (priority: XXX)
             3. file3 (priority: XXX)

    FRONTEND HIGH-IMPACT COMPONENTS:
      Critical (Canvas, Chat, Agent Dashboard): XX components
      High (Integrations):                      XX components
      Medium (UI, Pages, Lib):                  XX components
      Low (Hooks, Shared):                      XX components
      Total:                                    XX components
      Top 3: 1. file1 (priority: XXX)
             2. file2 (priority: XXX)
             3. file3 (priority: XXX)

    OVERLAP CHECK: Backend (Python) vs Frontend (TS/TSX) - NO OVERLAP (expected)

    DELIVERABLES:
    [x] Backend prioritized JSON:   prioritized_files_v11.0.json
    [x] Backend prioritized MD:     HIGH_IMPACT_PRIORITIZATION_v11.0.md
    [x] Frontend prioritized JSON:  prioritized_frontend_components_v11.0.json
    [x] Frontend prioritized MD:    HIGH_IMPACT_FRONTEND_COMPONENTS_v11.0.md
    [x] Coverage baseline report:   PHASE_292_BASELINE_REPORT.md (from Plan 01)
    ============================================================
    ```

    4. **No additional file writes needed** -- the output files are already created by Tasks 1 and 2.
  </action>
  <acceptance_criteria>
    - Backend JSON and frontend JSON both parseable by json.load()
    - Zero file overlap between backend and frontend lists
    - All summary numbers printed to stdout (human-readable final report)
  </acceptance_criteria>
  <verify>
    <automated>python -c "
import json
b = json.load(open('/Users/rushiparikh/projects/atom/backend/tests/coverage_reports/metrics/prioritized_files_v11.0.json'))
f = json.load(open('/Users/rushiparikh/projects/atom/frontend-nextjs/coverage/prioritized_frontend_components_v11.0.json'))
be_files = set()
for t in ['tier_1','tier_2','tier_3']:
    for entry in b.get(t, []):
        be_files.add(entry['file'])
fe_files = set()
for t in ['Critical','High','Medium','Low']:
    for entry in f.get('by_criticality', {}).get(t, []):
        fe_files.add(entry['file'])
overlap = be_files & fe_files
print(f'Backend files: {len(be_files)}')
print(f'Frontend files: {len(fe_files)}')
print(f'Overlap: {len(overlap)}')
assert len(overlap) == 0, f'OVERLAP FOUND: {overlap}'
print('CROSS-CHECK PASSED: No overlap')
"</automated>
  </verify>
  <done>Cross-check verified zero overlap between backend and frontend lists, all 4 output files valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coverage.json -> prioritization script | Coverage data parsed to generate prioritization scores |
| prioritization JSON -> downstream phases | Prioritization output consumed by coverage expansion executor scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-292-04 | Spoofing | priority_score calculation | mitigate | Validation script (Task 1 Step 3) verifies sorting order and tier boundaries. Downstream phases must re-validate before use. |
| T-292-05 | Information Disclosure | high_impact_files JSON | accept | File paths and coverage percentages are development metrics -- no PII, secrets, or credentials. Exposure risk is minimal. |
| T-292-06 | Tampering | business_impact_scores.json | accept | Impact scores are authoritative (D-06) and version-controlled. Tampering would require direct file modification which is tracked by git. |
</threat_model>

<verification>
1. Backend wrapper script runs successfully against Phase 292 baseline
2. Frontend prioritization script runs successfully against frontend coverage data
3. Both produce valid JSON (parseable by json.load()) and Markdown files
4. Backend tiers correctly separate by coverage thresholds (<10%, 10-30%, 30-50%)
5. Frontend tiers correctly separate by business criticality (Canvas/Chat/Dashboard > Integrations > UI > Hooks)
6. Zero file overlap between backend and frontend lists
7. All 4 deliverable files exist (2 JSON + 2 Markdown)
</verification>

<success_criteria>
- Backend: `prioritized_files_v11.0.json` with 3 tiers sorted by priority_score
- Backend: `HIGH_IMPACT_PRIORITIZATION_v11.0.md` with Tier 1/2/3 sections and quick wins
- Frontend: `prioritized_frontend_components_v11.0.json` with by_criticality sections
- Frontend: `HIGH_IMPACT_FRONTEND_COMPONENTS_v11.0.md` with Critical/High/Medium/Low sections
- Cross-check: zero file overlap between backend and frontend lists
- All outputs parseable by json.load()
- Scripts reusable for Phase 293-295 baseline refreshes
</success_criteria>

<output>
After completion, create `.planning/phases/292-coverage-baselines-prioritization/292-02-PLAN-SUMMARY.md`
</output>
