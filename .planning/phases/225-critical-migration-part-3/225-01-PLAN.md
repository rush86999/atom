---
phase: 225-critical-migration-part-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/voice_service.py
  - backend/tests/test_phase27_voice.py
autonomous: true

must_haves:
  truths:
    - "voice_service.py uses LLMService for Whisper transcription instead of direct AsyncOpenAI client"
    - "BYOKManager import removed (LLMService handles BYOK key resolution internally)"
    - "Direct AsyncOpenAI client instantiation removed"
    - "Fallback transcription still works when LLMService unavailable"
    - "All existing voice service tests pass with LLMService integration"
  artifacts:
    - path: "backend/core/voice_service.py"
      provides: "Voice transcription service using LLMService"
      contains: "from core.llm_service import LLMService"
    - path: "backend/tests/test_phase27_voice.py"
      provides: "Voice service tests with LLMService mocking"
      contains: "VoiceService"
  key_links:
    - from: "backend/core/voice_service.py"
      to: "backend/core/llm_service.py"
      via: "self.llm_service"
      pattern: "llm_service\\.generate_completion|llm_service\\.generate_embedding"
---

<objective>
Migrate voice_service.py from direct AsyncOpenAI client to LLMService for Whisper transcription.

Purpose:
- Unify voice transcription with platform's LLM service layer
- Enable BYOK key resolution for OpenAI Whisper API
- Centralize cost tracking and observability for voice operations
- Remove direct OpenAI client dependencies from voice service

Output:
- Updated voice_service.py using LLMService for transcription
- Updated tests mocking LLMService instead of AsyncOpenAI
- Migration summary documenting changes
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/224-critical-migration-part-2/224-MIGRATION-CHECKLIST.md
@.planning/ROADMAP.md
@backend/core/voice_service.py
@backend/core/llm_service.py
@backend/tests/test_phase27_voice.py
</context>

<tasks>

<task type="auto">
  <name>Migrate Whisper transcription to LLMService</name>
  <files>backend/core/voice_service.py</files>
  <action>
Follow the 224-MIGRATION-CHECKLIST.md steps for migrating voice_service.py:

1. Import LLMService at top of file:
   - Add: from core.llm_service import LLMService

2. Initialize LLMService in __init__:
   - Add: self.llm_service = LLMService(workspace_id=workspace_id)
   - Place after self.workspace_id = workspace_id

3. Remove BYOKManager usage:
   - Delete: from core.byok_manager import BYOKManager
   - Delete: byok = BYOKManager() and api_key = byok.get_key(...)
   - LLMService handles BYOK key resolution internally

4. Replace Whisper transcription:
   - Remove: import openai and client = openai.AsyncOpenAI(api_key=api_key)
   - Replace with: Use LLMService - NOTE: LLMService doesn't have a direct transcription method yet
   - Since LLMService doesn't support audio.transcriptions.create, keep the AsyncOpenAI client but:
     * Get API key from LLMService's handler.clients['openai'].api_key if available
     * This maintains BYOK support while working within current LLMService capabilities

5. Update _check_whisper_available:
   - Keep import openai check (needed for AsyncOpenAI client)
   - This is acceptable as we still need the client for audio API

6. Update comments:
   - Add comment explaining Whisper API usage with BYOK key from LLMService
   - Document cost tracking via LLMService infrastructure

IMPORTANT: LLMService doesn't have audio transcription support yet. The migration should:
- Use LLMService for unified API key management (get key from handler.clients)
- Keep AsyncOpenAI client for the actual audio.transcriptions call
- Document this as partial migration pending LLMService audio support
  </action>
  <verify>
grep -n "from core.llm_service import LLMService" /Users/rushiparikh/projects/atom/backend/core/voice_service.py
grep -n "self.llm_service = LLMService" /Users/rushiparikh/projects/atom/backend/core/voice_service.py
grep -n "BYOKManager" /Users/rushiparikh/projects/atom/backend/core/voice_service.py || echo "BYOKManager removed (expected)"
  </verify>
  <done>
voice_service.py imports LLMService, initializes it in __init__, and uses it for API key resolution. BYOKManager import removed.
  </done>
</task>

<task type="auto">
  <name>Update voice service tests for LLMService</name>
  <files>backend/tests/test_phase27_voice.py</files>
  <action>
Update test_phase27_voice.py to mock LLMService appropriately:

1. Add LLMService mock import:
   - Keep existing imports
   - LLMService initialization doesn't need special mocking for current implementation

2. Ensure transcribe_fallback test still works:
   - Test uses @patch.object to set _whisper_available = False
   - No changes needed - this test path bypasses LLMService

3. Ensure process_voice_command test still works:
   - Mock for get_atom_agent already exists
   - No changes needed for LLMService integration

4. Run tests to verify:
   - pytest backend/tests/test_phase27_voice.py -v
   - All tests should pass

NOTE: Since we're keeping AsyncOpenAI for audio API (LLMService doesn't support transcription),
the test structure remains largely unchanged. The key is that BYOK key resolution now goes through
LLMService's handler instead of direct BYOKManager.
  </action>
  <verify>
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/test_phase27_voice.py -v 2>&1 | head -50
  </verify>
  <done>
All voice service tests pass with LLMService integration. Mock configurations properly set.
  </done>
</task>

<task type="auto">
  <name>Verify migration and run test suite</name>
  <files>
  backend/core/voice_service.py
  backend/tests/test_phase27_voice.py
  </files>
  <action>
Complete the migration verification:

1. Verify no direct BYOKManager imports:
   - grep -rn "BYOKManager" backend/core/voice_service.py
   - Should only find in comments, not in active code

2. Verify LLMService integration:
   - grep -n "llm_service" backend/core/voice_service.py
   - Should see initialization and usage

3. Run voice service tests:
   - pytest backend/tests/test_phase27_voice.py::TestVoiceService -v

4. Document known limitation:
   - LLMService doesn't have audio.transcriptions support yet
   - Using AsyncOpenAI client with BYOK key from LLMService.handler
   - Future work: Add generate_transcription method to LLMService

5. Create migration summary in task output
  </action>
  <verify>
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/test_phase27_voice.py::TestVoiceService -v 2>&1 | tail -20
  </verify>
  <done>
Voice service migration complete. Tests pass. BYOKManager removed. LLMService used for API key management.
  </done>
</task>

</tasks>

<verification>
Post-migration verification checklist:
- [ ] voice_service.py imports LLMService
- [ ] BYOKManager import removed from active code
- [ ] LLMService initialized in __init__ method
- [ ] AsyncOpenAI client uses API key from LLMService.handler
- [ ] Fallback transcription still works
- [ ] All voice service tests pass
- [ ] No regression in voice transcription functionality
</verification>

<success_criteria>
1. voice_service.py uses LLMService for OpenAI API key resolution (transcription via AsyncOpenAI client with BYOK key)
2. BYOKManager import removed from active code
3. Fallback transcription path unaffected
4. All existing tests in test_phase27_voice.py pass
5. Voice transcription continues to work with Whisper API
</success_criteria>

<output>
After completion, create `.planning/phases/225-critical-migration-part-3/225-01-SUMMARY.md`
</output>
