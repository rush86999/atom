---
phase: 228-api-routes-tools-standardization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/competitor_analysis_routes.py
  - backend/api/learning_plan_routes.py
autonomous: true

must_haves:
  truths:
    - "API routes use LLMService instead of BYOKHandler"
    - "Method calls use generate_structured not generate_structured_response"
    - "Database session passed to LLMService for usage tracking"
    - "No BYOKHandler imports remain in migrated files"
  artifacts:
    - path: "backend/api/competitor_analysis_routes.py"
      provides: "Competitor analysis API with LLMService integration"
      exports: ["analyze_with_llm", "analyze_competitors"]
    - path: "backend/api/learning_plan_routes.py"
      provides: "Learning plan API with LLMService integration"
      exports: ["generate_learning_modules", "create_learning_plan"]
  key_links:
    - from: "backend/api/competitor_analysis_routes.py"
      to: "core.llm_service.LLMService"
      via: "from core.llm_service import LLMService"
      pattern: "from core.llm_service import LLMService"
    - from: "backend/api/learning_plan_routes.py"
      to: "core.llm_service.LLMService"
      via: "from core.llm_service import LLMService"
      pattern: "from core.llm_service import LLMService"
---

<objective>
Migrate competitor_analysis_routes.py and learning_plan_routes.py from BYOKHandler to LLMService.

Purpose: Complete STD-04 (BYOKHandler Standardization for API routes) by replacing module-level BYOKHandler singletons with function-level LLMService instances.

Output: Two API route files using LLMService.generate_structured() for structured LLM output.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/228-api-routes-tools-standardization/228-RESEARCH.md
@backend/core/llm_service.py

# Reference patterns from Phase 225.1
@.planning/phases/225.1-agent-llmservice-migration/225.1-03-SUMMARY.md

# Reference pattern from Phase 227
@.planning/phases/227-agent-system-standardization/227-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Migrate competitor_analysis_routes.py to LLMService</name>
  <files>backend/api/competitor_analysis_routes.py</files>
  <action>
    1. Replace import: Change `from core.llm.byok_handler import BYOKHandler` to `from core.llm_service import LLMService`

    2. Remove module-level singleton: Delete line 27 `byok_handler = BYOKHandler()`

    3. Update analyze_with_llm function signature (line 122):
       - Add `db: Session` parameter after `focus_areas: List[str]`
       - Function becomes: `async def analyze_with_llm(competitor_data: dict, focus_areas: List[str], db: Session) -> CompetitorInsight:`

    4. Replace BYOKHandler instantiation in analyze_with_llm:
       - Delete any module-level byok_handler reference
       - Add: `llm = LLMService(workspace_id="default", db=db)` at start of try block (line 154)

    5. Update method call (line 156):
       - Change `await byok_handler.generate_structured_response(...)` to `await llm.generate_structured(...)`

    6. Update route handler to pass db session:
       - In analyze_competitors endpoint (line 506), change `await analyze_with_llm(competitor_data, payload.focus_areas)`
       - To: `await analyze_with_llm(competitor_data, payload.focus_areas, db)`

    CRITICAL: Do NOT change fallback logic in _generate_fallback_insights function - it's template-based, not LLM-based
  </action>
  <verify>
    grep -n "BYOKHandler" backend/api/competitor_analysis_routes.py  # Should return nothing
    grep -n "from core.llm_service import LLMService" backend/api/competitor_analysis_routes.py  # Should find import
    grep -n "generate_structured" backend/api/competitor_analysis_routes.py  # Should find new method name
  </verify>
  <done>
    competitor_analysis_routes.py imports LLMService from core.llm_service (not core.llm.byok_handler)
    analyze_with_llm function accepts db: Session parameter
    LLMService instantiated with workspace_id="default" and db=db
    Method call uses generate_structured (not generate_structured_response)
    No BYOKHandler references remain in the file
  </done>
</task>

<task type="auto">
  <name>Migrate learning_plan_routes.py to LLMService</name>
  <files>backend/api/learning_plan_routes.py</files>
  <action>
    1. Replace import: Change `from core.llm.byok_handler import BYOKHandler` (line 18) to `from core.llm_service import LLMService`

    2. Remove module-level singleton: Delete line 27 `byok_handler = BYOKHandler()`

    3. Update generate_learning_modules function signature (line 75):
       - Add `db: Session` parameter after `learning_goals: List[str] = []`
       - Function becomes: `async def generate_learning_modules(topic: str, current_level: str, duration_weeks: int, preferred_formats: List[str], learning_goals: List[str] = [], db: Session) -> List[LearningModule]:`

    4. Replace BYOKHandler instantiation in generate_learning_modules:
       - Add: `llm = LLMService(workspace_id="default", db=db)` at start of try block (line 127)

    5. Update method call (line 128):
       - Change `await byok_handler.generate_structured_response(...)` to `await llm.generate_structured(...)`

    6. Update route handler to pass db session:
       - In create_learning_plan endpoint (line 462), change `await generate_learning_modules(...)`
       - To: `await generate_learning_modules(..., db=db)` (add db as last parameter)

    CRITICAL: Do NOT change _generate_template_modules function - it's template-based fallback, not LLM-based
  </action>
  <verify>
    grep -n "BYOKHandler" backend/api/learning_plan_routes.py  # Should return nothing
    grep -n "from core.llm_service import LLMService" backend/api/learning_plan_routes.py  # Should find import
    grep -n "generate_structured" backend/api/learning_plan_routes.py  # Should find new method name
  </verify>
  <done>
    learning_plan_routes.py imports LLMService from core.llm_service (not core.llm.byok_handler)
    generate_learning_modules function accepts db: Session parameter
    LLMService instantiated with workspace_id="default" and db=db
    Method call uses generate_structured (not generate_structured_response)
    No BYOKHandler references remain in the file
  </done>
</task>

<task type="auto">
  <name>Verify migration and run API tests</name>
  <files>backend/api/competitor_analysis_routes.py, backend/api/learning_plan_routes.py</files>
  <action>
    1. Verify no BYOKHandler imports in either file:
       - `grep -r "BYOKHandler" backend/api/competitor_analysis_routes.py backend/api/learning_plan_routes.py`
       - Should return empty

    2. Verify LLMService imports present:
       - `grep "from core.llm_service import LLMService" backend/api/competitor_analysis_routes.py backend/api/learning_plan_routes.py`
       - Should find in both files

    3. Verify method name changes:
       - `grep "generate_structured_response" backend/api/competitor_analysis_routes.py backend/api/learning_plan_routes.py`
       - Should return empty (old method name not used)
       - `grep "generate_structured" backend/api/competitor_analysis_routes.py backend/api/learning_plan_routes.py`
       - Should find in both files

    4. Check if tests exist for these routes:
       - `ls backend/tests/api/test_competitor* 2>/dev/null || echo "No competitor analysis tests"`
       - `ls backend/tests/api/test_learning* 2>/dev/null || echo "No learning plan tests"`
       - If tests exist, they should pass (no BYOKHandler mocks to update based on research)

    5. Verify import path is correct (not core.llm.llm_service):
       - `grep "from core.llm.llm_service" backend/api/competitor_analysis_routes.py backend/api/learning_plan_routes.py`
       - Should return empty (wrong import path from Phase 225.1 Plan 02 bug)
  </action>
  <verify>
    # Final verification commands
    bash -c '! grep -r "BYOKHandler" backend/api/competitor_analysis_routes.py backend/api/learning_plan_routes.py'
    bash -c 'grep -c "from core.llm_service import LLMService" backend/api/competitor_analysis_routes.py backend/api/learning_plan_routes.py | grep -q "^2$"'
    bash -c '! grep -r "generate_structured_response" backend/api/competitor_analysis_routes.py backend/api/learning_plan_routes.py'
  </verify>
  <done>
    No BYOKHandler imports in either migrated file
    LLMService imported from correct path (core.llm_service) in both files
    Method name changed to generate_structured (no generate_structured_response)
    Tests pass if they exist
  </done>
</task>

</tasks>

<verification>
## Migration Verification

1. Code Review:
   - No BYOKHandler imports in competitor_analysis_routes.py or learning_plan_routes.py
   - LLMService imported from core.llm_service (not core.llm.llm_service)
   - Functions accept db: Session parameter
   - Method calls use generate_structured() not generate_structured_response()
   - Module-level singletons removed

2. Functional Testing:
   - Competitor analysis API creates CompetitorInsight objects via LLM
   - Learning plan API generates LearningModule objects via LLM
   - Fallback logic still works when LLM fails
   - Database usage tracking enabled via db session

3. Test Verification:
   - Existing tests pass (if any)
   - No BYOKHandler mock patches needed
</verification>

<success_criteria>
1. Both API route files use LLMService instead of BYOKHandler
2. Method calls use generate_structured() (new method name)
3. Database session passed for usage tracking
4. No BYOKHandler imports remain in migrated files
5. Import path is correct: from core.llm_service import LLMService
6. Fallback logic unchanged (template-based, not LLM-based)
</success_criteria>

<output>
After completion, create `.planning/phases/228-api-routes-tools-standardization/228-01-SUMMARY.md` with:
- Files modified
- Migration changes made
- Verification results
- Test results
- Commits created
</output>
