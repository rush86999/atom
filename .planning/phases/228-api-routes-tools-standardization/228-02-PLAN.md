---
phase: 228-api-routes-tools-standardization
plan: 02
type: execute
wave: 2
depends_on: ["228-01"]
files_modified:
  - backend/api/mobile_agent_routes.py
  - backend/tests/api/test_mobile_agent_routes.py
autonomous: true

must_haves:
  truths:
    - "mobile_agent_routes.py uses LLMService instead of BYOKHandler"
    - "WebSocket streaming uses LLMService.stream_completion"
    - "Query complexity analysis uses LLMService.analyze_query_complexity"
    - "Provider selection uses LLMService.get_optimal_provider"
    - "Tests updated to patch LLMService instead of BYOKHandler"
  artifacts:
    - path: "backend/api/mobile_agent_routes.py"
      provides: "Mobile agent API with LLMService integration"
      exports: ["mobile_agent_chat", "list_mobile_agents"]
    - path: "backend/tests/api/test_mobile_agent_routes.py"
      provides: "Test suite updated for LLMService"
      exports: ["test_byok_handler_integration"]
  key_links:
    - from: "backend/api/mobile_agent_routes.py"
      to: "core.llm_service.LLMService"
      via: "from core.llm_service import LLMService"
      pattern: "from core.llm_service import LLMService"
    - from: "backend/tests/api/test_mobile_agent_routes.py"
      to: "core.llm_service.LLMService"
      via: "@patch decorator update"
      pattern: "@patch('api.mobile_agent_routes.LLMService')"
---

<objective>
Migrate mobile_agent_routes.py from BYOKHandler to LLMService, including WebSocket streaming and update test mocks.

Purpose: Complete STD-04 (BYOKHandler Standardization for API routes) by migrating the mobile agent chat endpoint which uses streaming LLM responses.

Output: Mobile agent routes file using LLMService.stream_completion() with updated test mocks.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/228-api-routes-tools-standardization/228-RESEARCH.md
@backend/core/llm_service.py

# Reference pattern from Phase 227 (similar WebSocket streaming migration)
@.planning/phases/227-agent-system-standardization/227-01-SUMMARY.md

# Test file structure
@backend/tests/api/test_mobile_agent_routes.py
</context>

<tasks>

<task type="auto">
  <name>Migrate mobile_agent_routes.py to LLMService</name>
  <files>backend/api/mobile_agent_routes.py</files>
  <action>
    1. Replace import (line 26):
       - Change: `from core.llm.byok_handler import BYOKHandler`
       - To: `from core.llm_service import LLMService`

    2. Replace BYOKHandler instantiation in mobile_agent_chat function (line 322):
       - Change: `byok_handler = BYOKHandler(workspace_id="default", provider_id="auto")`
       - To: `llm_service = LLMService(workspace_id="default")`
       - Note: Remove provider_id parameter (not used by LLMService constructor)

    3. Update analyze_query_complexity call (line 325):
       - Change: `complexity = byok_handler.analyze_query_complexity(request.message, task_type="chat")`
       - To: `complexity = llm_service.analyze_query_complexity(request.message, task_type="chat")`
       - Note: Method name unchanged, only instance variable changed

    4. Update get_optimal_provider call (lines 326-333):
       - Change: `provider_id, model = byok_handler.get_optimal_provider(...)`
       - To: `provider_id, model = llm_service.get_optimal_provider(...)`
       - Note: Method name unchanged, only instance variable changed

    5. Update stream_completion call (lines 356-363):
       - Change: `async for token in byok_handler.stream_completion(...)`
       - To: `async for token in llm_service.stream_completion(...)`
       - Note: Method name unchanged, only instance variable changed

    CRITICAL: The logger.info line (line 335) uses provider_id and model variables - no change needed, they're populated from get_optimal_provider

    CRITICAL: Follow the Phase 227 pattern - streaming migration was tested and verified in atom_agent_endpoints.py
  </action>
  <verify>
    grep -n "BYOKHandler" backend/api/mobile_agent_routes.py  # Should return nothing
    grep -n "from core.llm_service import LLMService" backend/api/mobile_agent_routes.py  # Should find import
    grep -n "llm_service\." backend/api/mobile_agent_routes.py  # Should find new instance variable
  </verify>
  <done>
    mobile_agent_routes.py imports LLMService from core.llm_service
    LLMService instantiated with workspace_id="default" (no provider_id parameter)
    All LLM method calls use llm_service instance variable
    No BYOKHandler references remain in the file
  </done>
</task>

<task type="auto">
  <name>Update test file to patch LLMService</name>
  <files>backend/tests/api/test_mobile_agent_routes.py</files>
  <action>
    1. Find test_byok_handler_integration function (around line 349-354):
       - Current function name: `test_byok_handler_integration`
       - Current code checks for BYOKHandler import

    2. Rename function and update implementation:
       - Change function name from `test_byok_handler_integration` to `test_llm_service_integration`
       - Update import check:
         - Change: `from api.mobile_agent_routes import BYOKHandler`
         - To: `from api.mobile_agent_routes import LLMService`
       - Update assertion: `assert LLMService is not None`

    3. Verify no other tests mock BYOKHandler:
       - Search for `@patch.*BYOKHandler` patterns
       - None should exist (tests are structural, not functional with LLM mocking)

    4. Add a comment noting the migration:
       - Add docstring comment: "# Phase 228: Migrated from BYOKHandler to LLMService"
       - Above the test function

    CRITICAL: Unlike Phase 225.1 Plans 05-07, this test file doesn't have extensive @patch decorators to update
    The test file uses structural testing (checking functions exist) rather than functional testing with mocks
  </action>
  <verify>
    grep -n "test_byok_handler_integration" backend/tests/api/test_mobile_agent_routes.py  # Should return nothing
    grep -n "test_llm_service_integration" backend/tests/api/test_mobile_agent_routes.py  # Should find renamed test
    grep -n "BYOKHandler" backend/tests/api/test_mobile_agent_routes.py  # Should only find in old comment if at all
  </verify>
  <done>
    test_byok_handler_integration renamed to test_llm_service_integration
    Test checks for LLMService import instead of BYOKHandler
    No @patch decorators reference BYOKHandler
    Test file documents Phase 228 migration
  </done>
</task>

<task type="auto">
  <name>Verify migration and run tests</name>
  <files>backend/api/mobile_agent_routes.py, backend/tests/api/test_mobile_agent_routes.py</files>
  <action>
    1. Verify no BYOKHandler imports in mobile_agent_routes.py:
       - `grep -n "BYOKHandler" backend/api/mobile_agent_routes.py`
       - Should return empty

    2. Verify LLMService imports and usage:
       - `grep -n "from core.llm_service import LLMService" backend/api/mobile_agent_routes.py`
       - Should find import
       - `grep -n "llm_service\." backend/api/mobile_agent_routes.py`
       - Should find 3 usages: analyze_query_complexity, get_optimal_provider, stream_completion

    3. Verify test file updated:
       - `grep -n "test_llm_service_integration" backend/tests/api/test_mobile_agent_routes.py`
       - Should find renamed test
       - `grep -c "LLMService" backend/tests/api/test_mobile_agent_routes.py | grep -q "^1$"`
       - Should find exactly 1 LLMService reference (in renamed test)

    4. Run mobile agent tests:
       - `pytest backend/tests/api/test_mobile_agent_routes.py -v 2>&1 | head -50`
       - Tests should pass (they're structural, not functional)

    5. Verify import path correctness:
       - `grep "from core.llm.llm_service" backend/api/mobile_agent_routes.py`
       - Should return empty (wrong path bug from Phase 225.1 Plan 02)
  </action>
  <verify>
    # Final verification commands
    bash -c '! grep -n "BYOKHandler" backend/api/mobile_agent_routes.py'
    bash -c 'grep -c "llm_service\." backend/api/mobile_agent_routes.py | grep -q "^3$"'
    bash -c 'grep -n "test_llm_service_integration" backend/tests/api/test_mobile_agent_routes.py'
  </verify>
  <done>
    No BYOKHandler imports in mobile_agent_routes.py
    LLMService used for all 3 LLM operations (analyze_query_complexity, get_optimal_provider, stream_completion)
    Test file updated with renamed test_llm_service_integration function
    Import path is correct (core.llm_service not core.llm.llm_service)
  </done>
</task>

</tasks>

<verification>
## Migration Verification

1. Code Review:
   - No BYOKHandler imports in mobile_agent_routes.py
   - LLMService imported from core.llm_service
   - LLMService instantiated without provider_id parameter
   - All 3 LLM operations use llm_service instance
   - Test function renamed and updated

2. Functional Testing:
   - Mobile agent chat endpoint performs query complexity analysis
   - Optimal provider selection works correctly
   - WebSocket streaming yields tokens correctly
   - Error handling preserved

3. Test Verification:
   - test_llm_service_integration passes
   - No BYOKHandler references in test file
   - Tests validate LLMService integration

4. Integration Verification:
   - Follows Phase 227 pattern (atom_agent_endpoints.py streaming)
   - Consistent with Phase 225.1 agent service migrations
</verification>

<success_criteria>
1. mobile_agent_routes.py uses LLMService instead of BYOKHandler
2. All 3 LLM operations migrated (analyze_query_complexity, get_optimal_provider, stream_completion)
3. Test file updated (test renamed, checks for LLMService)
4. No BYOKHandler imports remain in migrated files
5. Import path is correct: from core.llm_service import LLMService
</success_criteria>

<output>
After completion, create `.planning/phases/228-api-routes-tools-standardization/228-02-SUMMARY.md` with:
- Files modified
- Migration changes made
- Verification results
- Test results
- Commits created
- Phase completion summary
</output>
