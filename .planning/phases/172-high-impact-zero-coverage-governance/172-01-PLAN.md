---
phase: 172-high-impact-zero-coverage-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_agent_governance_routes.py
  - backend/tests/integration/test_agent_governance_routes.py
autonomous: true

must_haves:
  truths:
    - "Agent governance routes achieve 75%+ line coverage"
    - "All 13 endpoints in agent_governance_routes.py have test coverage"
    - "Authentication and authorization paths are tested"
    - "Mock data endpoints return expected governance structures"
    - "Error paths (404, 500) are tested for governance endpoints"
  artifacts:
    - path: "backend/tests/api/test_agent_governance_routes.py"
      provides: "TestClient-based API route tests for agent governance"
      min_lines: 400
      exports: ["TestAgentGovernanceRoutes", "test_governance_rules", "test_list_agents", "test_get_agent_maturity"]
    - path: "backend/tests/integration/test_agent_governance_routes.py"
      provides: "Integration tests for governance with database"
      min_lines: 200
      exports: ["TestGovernanceIntegration", "test_approval_workflow"]
  key_links:
    - from: "backend/tests/api/test_agent_governance_routes.py"
      to: "backend/api/agent_governance_routes.py"
      via: "TestClient router import"
      pattern: "from api.agent_governance_routes import router"
    - from: "backend/tests/api/test_agent_governance_routes.py"
      to: "core/intervention_service.py"
      via: "patch mocking"
      pattern: "patch.*intervention_service"
---

<objective>
Achieve 75%+ line coverage on agent_governance_routes.py (209 lines, 13 endpoints) with TestClient-based tests covering all governance endpoints, authentication flows, and error paths.

Purpose: Agent governance routes are critical for controlling agent behavior based on maturity levels. Zero coverage exists on these 13 endpoints that manage agent permissions, workflow approvals, and capability enforcement.

Output: Comprehensive test file with 60+ tests covering all governance endpoints.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 167 TestClient patterns
@backend/tests/api/test_agent_guidance_routes.py
@backend/tests/api/conftest.py

# Source file to test
@backend/api/agent_governance_routes.py
</context>

<tasks>

<task type="auto">
  <name>Create TestClient fixtures and test file structure for agent governance routes</name>
  <files>backend/tests/api/test_agent_governance_routes.py</files>
  <action>
    Create test_agent_governance_routes.py with:
    1. Import router from api.agent_governance_routes
    2. Create TestClient fixture for the router
    3. Create mock_user fixture with proper User model
    4. Create mock_admin_user fixture with super_admin role for approval tests
    5. Patch intervention_service for approve/reject endpoints
    6. Use existing conftest.py patterns from test_agent_guidance_routes.py

    Test file structure:
    - Fixtures: client, mock_user, mock_admin_user, mock_intervention_service
    - Test classes by endpoint category: Governance Rules, Agent Listing, Maturity Checks, Workflow Deployment, Approvals, Feedback, Capabilities, Action Enforcement

    Pattern from test_agent_guidance_routes.py:
    ```python
    @pytest.fixture
    def client():
        return TestClient(router)

    @pytest.fixture
    def mock_user(db: Session):
        user_id = str(uuid.uuid4())
        user = User(id=user_id, email=f"test-{user_id}@example.com", ...)
        db.add(user); db.commit(); return user
    ```
  </action>
  <verify>
    grep -c "def test_" backend/tests/api/test_agent_governance_routes.py
    File created with at least 10 test method stubs
  </verify>
  <done>Test file structure created with fixtures and test class organization</done>
</task>

<task type="auto">
  <name>Implement governance rules and agent listing endpoint tests</name>
  <files>backend/tests/api/test_agent_governance_routes.py</files>
  <action>
    Add tests for GET /api/agent-governance/rules and GET /api/agent-governance/agents:

    1. test_get_governance_rules_success:
       - GET /api/agent-governance/rules
       - Assert 200 status
       - Verify response contains maturity_levels (student, intern, supervised, autonomous)
       - Verify action_complexity mapping (1-4)
       - Verify promotion_requirements

    2. test_list_agents_all_categories:
       - GET /api/agent-governance/agents
       - Assert 200 status
       - Verify all MOCK_AGENTS returned (8 agents)
       - Check each agent has: agent_id, name, category, maturity_level, confidence_score, can_deploy_directly, requires_approval

    3. test_list_agents_filter_by_category:
       - GET /api/agent-governance/agents?category=sales
       - Assert 200 status
       - Verify only sales agents returned
       - Test categories: sales, marketing, support, engineering, hr, finance, data, productivity

    4. test_list_agents_maturity_calculation:
       - Verify confidence score to maturity mapping: >=0.9=autonomous, >=0.7=supervised, >=0.5=intern, <0.5=student
       - Test boundary conditions: 0.89 (supervised), 0.69 (intern), 0.49 (student)

    5. test_list_agents_can_deploy_logic:
       - Verify autonomous agents can_deploy_directly=True
       - Verify supervised agents with confidence>=0.8 can_deploy_directly=True
       - Verify intern/student agents can_deploy_directly=False

    Use MOCK_AGENTS data from agent_governance_routes.py for assertions.
  </action>
  <verify>
    pytest backend/tests/api/test_agent_governance_routes.py -k "test_get_governance_rules or test_list_agents" -v
    All 5 tests pass
  </verify>
  <done>Governance rules and agent listing endpoints tested with 5 passing tests</done>
</task>

<task type="auto">
  <name>Implement agent maturity, deployment check, and approval submission tests</name>
  <files>backend/tests/api/test_agent_governance_routes.py</files>
  <action>
    Add tests for GET /api/agent-governance/agents/{agent_id}, POST /check-deployment, POST /submit-for-approval:

    1. test_get_agent_maturity_success:
       - GET /api/agent-governance/agents/sales-agent
       - Assert 200 status
       - Verify agent details: id, name, category, maturity_level, confidence_score
       - Test all 8 mock agents

    2. test_get_agent_maturity_not_found:
       - GET /api/agent-governance/agents/unknown-agent
       - Assert 404 status
       - Verify error response

    3. test_check_deployment_autonomous_approved:
       - POST /api/agent-governance/check-deployment with finance-agent (0.92 confidence)
       - Assert 200 status
       - Verify can_deploy=True, requires_approval=False, status="approved"

    4. test_check_deployment_supervised_needs_approval:
       - POST /api/agent-governance/check-deployment with marketing-agent (0.72 confidence)
       - Assert 200 status
       - Verify can_deploy=False, requires_approval=True, status="pending"
       - Verify approver_role_required set

    5. test_check_deployment_agent_not_found:
       - POST /api/agent-governance/check-deployment with unknown agent
       - Assert 404 status

    6. test_submit_for_approval_success:
       - POST /api/agent-governance/submit-for-approval with engineering-agent (0.45 confidence)
       - Assert 200 status
       - Verify approval_id generated (format: apr_{timestamp}_{agent_id})
       - Verify status="pending"
       - Verify estimated_review_time in response

    7. test_submit_for_approval_response_includes_workflow:
       - Verify workflow_name, agent_id, agent_name, maturity_level in response
       - Verify message about approval notification
  </action>
  <verify>
    pytest backend/tests/api/test_agent_governance_routes.py -k "test_get_agent_maturity or test_check_deployment or test_submit_for_approval" -v
    All 7 tests pass
  </verify>
  <done>Agent maturity, deployment check, and approval endpoints tested with 7 passing tests</done>
</task>

<task type="auto">
  <name>Implement approval workflow (approve/reject), feedback, and pending approvals tests</name>
  <files>backend/tests/api/test_agent_governance_routes.py</files>
  <action>
    Add tests for POST /approve/{approval_id}, POST /reject/{approval_id}, POST /feedback, GET /pending-approvals:

    1. test_approve_workflow_success:
       - Create mock user with TEAM_LEAD role
       - POST /api/agent-governance/approve/{approval_id} with approver_id
       - Patch intervention_service.approve_intervention to return {"success": True}
       - Assert 200 status
       - Verify status="approved", approved_by set, approved_at timestamp

    2. test_approve_workflow_unauthorized_role:
       - Create mock user with MEMBER role
       - POST /api/agent-governance/approve/{approval_id}
       - Assert 403 status
       - Verify permission_denied_error raised

    3. test_approve_workflow_approval_failed:
       - Patch intervention_service.approve_intervention to return {"success": False, "message": "Already approved"}
       - POST /api/agent-governance/approve/{approval_id}
       - Assert 400 status
       - Verify error_code="APPROVAL_FAILED"

    4. test_reject_workflow_success:
       - POST /api/agent-governance/reject/{approval_id} with approver_id and reason
       - Patch intervention_service.reject_intervention to return {"success": True}
       - Assert 200 status
       - Verify status="rejected", rejected_by set, reason in response

    5. test_submit_agent_feedback_success:
       - POST /api/agent-governance/feedback with valid feedback
       - Use existing mock agent (sales-agent)
       - Assert 200 status
       - Verify message about feedback review

    6. test_submit_agent_feedback_agent_not_found:
       - POST /api/agent-governance/feedback with unknown agent
       - Assert 404 status

    7. test_list_pending_approvals_success:
       - GET /api/agent-governance/pending-approvals
       - Patch intervention_service.get_pending_interventions to return mock list
       - Assert 200 status
       - Verify pending_approvals array and count

    8. test_list_pending_approvals_filtered_by_approver:
       - GET /api/agent-governance/pending-approvals?approver_id={user_id}
       - Verify filter applied to intervention_service call
  </action>
  <verify>
    pytest backend/tests/api/test_agent_governance_routes.py -k "test_approve or test_reject or test_feedback or test_pending" -v
    All 8 tests pass
  </verify>
  <done>Approval workflow, feedback, and pending approvals endpoints tested with 8 passing tests</done>
</task>

<task type="auto">
  <name>Implement capabilities, action enforcement, and workflow generation endpoint tests</name>
  <files>backend/tests/api/test_agent_governance_routes.py</files>
  <action>
    Add tests for GET /agents/{agent_id}/capabilities, POST /enforce-action, POST /generate-workflow:

    1. test_get_agent_capabilities_autonomous:
       - GET /api/agent-governance/agents/finance-agent/capabilities
       - Assert 200 status
       - Verify max_complexity=4 for autonomous
       - Verify all 23 actions in allowed_actions (search, read, list, ..., delete, execute, deploy, payment, approve)
       - Verify restricted_actions is empty

    2. test_get_agent_capabilities_student:
       - GET /api/agent-governance/agents/engineering-agent/capabilities (0.45 confidence = student)
       - Assert 200 status
       - Verify max_complexity=1
       - Verify only low-complexity actions in allowed_actions (search, read, list, get, fetch, summarize)
       - Verify high-complexity actions in restricted_actions (delete, execute, deploy, etc.)

    3. test_get_agent_capabilities_supervised:
       - GET /api/agent-governance/agents/marketing-agent/capabilities (0.72 confidence = supervised)
       - Assert 200 status
       - Verify max_complexity=3
       - Verify allowed includes complexity 1-3 actions
       - Verify restricted includes complexity 4 actions

    4. test_enforce_action_approved:
       - POST /api/agent-governance/enforce-action with finance-agent (autonomous) and action_type="delete"
       - Assert 200 status
       - Verify proceed=True, status="APPROVED"
       - Verify action_required=None

    5. test_enforce_action_blocked_student:
       - POST /api/agent-governance/enforce-action with hr-agent (0.38 confidence = student) and action_type="delete"
       - Assert 200 status
       - Verify proceed=False, status="BLOCKED"
       - Verify reason includes required maturity level
       - Verify action_required="HUMAN_APPROVAL"

    6. test_enforce_action_pending_approval_supervised:
       - POST /api/agent-governance/enforce-action with marketing-agent (0.72 confidence = supervised) and action_type="create"
       - Assert 200 status
       - Verify proceed=True, status="PENDING_APPROVAL"
       - Verify action_required="WAIT_FOR_APPROVAL"

    7. test_enforce_action_unknown_agent:
       - POST /api/agent-governance/enforce-action with unknown agent
       - Assert 200 status (endpoint returns response, doesn't raise)
       - Verify proceed=False, status="BLOCKED"

    8. test_generate_workflow_success:
       - POST /api/agent-governance/generate-workflow with agent_id and description
       - Assert 200 status
       - Verify workflow object with: name, agent_id, trigger, steps
       - Verify can_deploy_directly based on agent maturity

    9. test_generate_workflow_agent_not_found:
       - POST /api/agent-governance/generate-workflow with unknown agent
       - Assert 404 status

    10. test_action_complexity_mapping:
       - Test all action types map to correct complexity (1-4)
       - Verify action_complexity dict in response matches expected mappings
  </action>
  <verify>
    pytest backend/tests/api/test_agent_governance_routes.py -k "test_get_agent_capabilities or test_enforce_action or test_generate_workflow" -v
    All 10 tests pass
  </verify>
  <done>Capabilities, action enforcement, and workflow generation endpoints tested with 10 passing tests</done>
</task>

<task type="auto">
  <name>Measure coverage and verify 75%+ target achieved</name>
  <files>backend/tests/api/test_agent_governance_routes.py</files>
  <action>
    1. Run pytest with coverage on agent_governance_routes.py:
       ```bash
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/api/test_agent_governance_routes.py backend/tests/integration/test_agent_governance_routes.py \
         --cov=backend/api/agent_governance_routes --cov-report=term-missing --cov-report=json -v
       ```

    2. Verify coverage report shows 75%+ line coverage:
       - Check terminal output for coverage percentage
       - Check backend/coverage.json for agent_governance_routes.py coverage

    3. If below 75%, add tests for missing lines:
       - Identify uncovered lines from --cov-report=term-missing
       - Add tests for specific uncovered code paths
       - Common missing paths: error handlers, edge cases, validation branches

    4. Create coverage summary:
       - Document final coverage percentage
       - List all endpoints tested (13 total)
       - Note any excluded lines (if any)

    5. Update test file docstring with coverage achievement
  </action>
  <verify>
    Coverage report shows 75%+ for backend/api/agent_governance_routes.py
    grep -A5 "agent_governance_routes" coverage.json | grep "percent_covered"
  </verify>
  <done>Agent governance routes achieve 75%+ line coverage with comprehensive endpoint testing</done>
</task>

</tasks>

<verification>
1. All tests pass: pytest backend/tests/api/test_agent_governance_routes.py -v (30+ tests)
2. Coverage 75%+ on agent_governance_routes.py: pytest --cov backend/api/agent_governance_routes
3. All 13 endpoints have test coverage: grep "def test_" | grep -E "(rules|agents|check_deployment|submit|approve|reject|feedback|capabilities|enforce|generate)"
4. Authentication paths tested: Tests with mock_user of different roles
5. Error paths tested: 404 for unknown agents, 403 for unauthorized, 400 for failed approvals
</verification>

<success_criteria>
1. Agent governance routes achieve 75%+ line coverage
2. All 13 endpoints tested with happy path and error cases
3. Test file follows Phase 167 TestClient patterns
4. 30+ passing tests created
5. No SQLAlchemy conflicts (tests use proper isolation)
</success_criteria>

<output>
After completion, create `.planning/phases/172-high-impact-zero-coverage-governance/172-01-SUMMARY.md`
</output>
