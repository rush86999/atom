---
phase: 172-high-impact-zero-coverage-governance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_agent_guidance_routes.py
  - backend/tests/unit/test_agent_guidance_routes.py
autonomous: true

must_haves:
  truths:
    - "Agent guidance routes achieve 75%+ line coverage"
    - "All 14 endpoints in agent_guidance_routes.py have test coverage"
    - "Operation tracking (start, update, complete, get) is fully tested"
    - "View orchestration (switch, layout) is fully tested"
    - "Error guidance and agent request endpoints are fully tested"
  artifacts:
    - path: "backend/tests/api/test_agent_guidance_routes.py"
      provides: "Complete TestClient-based API route tests for agent guidance"
      min_lines: 800
      exports: ["test_start_operation", "test_update_operation", "test_switch_view", "test_present_error", "test_create_permission_request"]
    - path: "backend/tests/unit/test_agent_guidance_routes.py"
      provides: "Unit tests for agent guidance route helpers and validation"
      min_lines: 200
  key_links:
    - from: "backend/tests/api/test_agent_guidance_routes.py"
      to: "backend/api/agent_guidance_routes.py"
      via: "TestClient router import"
      pattern: "from api.agent_guidance_routes import router"
    - from: "backend/tests/api/test_agent_guidance_routes.py"
      to: "tools/agent_guidance_canvas_tool.py"
      via: "patch mocking"
      pattern: "patch.*get_agent_guidance_system"
    - from: "backend/tests/api/test_agent_guidance_routes.py"
      to: "core/view_coordinator.py"
      via: "patch mocking"
      pattern: "patch.*get_view_coordinator"
---

<objective>
Achieve 75%+ line coverage on agent_guidance_routes.py (171 lines, 14 endpoints) by completing the existing stub tests in test_agent_guidance_routes.py. The file has 82 stub tests that need full implementation following TestClient patterns.

Purpose: Agent guidance routes manage real-time agent operation tracking, view orchestration, error guidance, and agent requests. Existing stub tests need implementation to achieve coverage target.

Output: Fully implemented test file with 80+ passing tests covering all agent guidance endpoints.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing stub tests to implement
@backend/tests/api/test_agent_guidance_routes.py

# Phase 167 TestClient patterns
@backend/tests/api/conftest.py

# Source file to test
@backend/api/agent_guidance_routes.py
</context>

<tasks>

<task type="auto">
  <name>Complete operation tracking endpoint tests (start, update, complete, get)</name>
  <files>backend/tests/api/test_agent_guidance_routes.py</files>
  <action>
    The existing stub tests for operation tracking need full implementation. Update these tests to properly assert response structure and mock service calls:

    1. test_start_operation_success (line ~105):
       - Keep existing structure but add proper assertions
       - Verify response.status_code == 200 (not accepting 500)
       - Verify response.json()["data"]["operation_id"] exists
       - Assert mock_system_instance.start_operation called with correct params

    2. test_start_operation_minimal_data (line ~135):
       - Test with required fields only (agent_id, operation_type, context)
       - Verify optional fields (total_steps, metadata) handled correctly

    3. test_update_operation_step (line ~164):
       - Verify update_step called with step and progress
       - Assert response 200 with success message

    4. test_update_operation_context (line ~193):
       - Verify update_context called with what/why/next_steps
       - Test context-only update (no step/progress)

    5. test_update_operation_add_log (line ~223):
       - Verify update_step called with add_log parameter
       - Test log entry structure

    6. test_update_operation_combined (line ~256):
       - Test simultaneous step + context update
       - Verify both update_step and update_context called

    7. test_complete_operation_success (line ~292):
       - Verify complete_operation called with status="completed"
       - Assert final_message included in call

    8. test_complete_operation_failed (line ~320):
       - Test status="failed" with error message
       - Verify failure handling

    9. test_complete_operation_default_status (line ~348):
       - Test empty request body uses default status="completed"
       - Verify default parameter behavior

    10. test_get_operation_success (line ~378):
        - Direct DB query for AgentOperationTracker (no mock needed)
        - Verify operation object serialized correctly
        - Check all fields: operation_id, agent_id, status, progress, logs, etc.

    11. test_get_operation_not_found (line ~396):
        - Verify 404 response for fake operation_id
        - Check error response structure

    Pattern for proper assertions:
    ```python
    assert response.status_code == 200
    data = response.json()
    assert "operation_id" in data["data"]
    mock_system_instance.start_operation.assert_called_once_with(
        user_id=mock_user.id,
        agent_id="test_agent",
        operation_type="data_processing",
        context={"task": "process_data"},
        total_steps=10,
        metadata={"source": "test"}
    )
    ```
  </action>
  <verify>
    pytest backend/tests/api/test_agent_guidance_routes.py -k "test_start_operation or test_update_operation or test_complete_operation or test_get_operation" -v
    All 11 tests pass with proper assertions (not accepting 500 status)
  </verify>
  <done>Operation tracking endpoints fully tested with 11 passing tests and proper assertions</done>
</task>

<task type="auto">
  <name>Complete view orchestration endpoint tests (switch, layout)</name>
  <files>backend/tests/api/test_agent_guidance_routes.py</files>
  <action>
    The existing stub tests for view orchestration need proper assertion implementation:

    1. test_switch_view_browser (line ~416):
       - Add assertion for response.status_code == 200
       - Verify switch_to_browser_view called with URL, guidance, session_id
       - Assert success message returned

    2. test_switch_view_browser_missing_url (line ~443):
       - Verify validation error (400 or 422)
       - Check error message mentions URL required

    3. test_switch_view_terminal (line ~464):
       - Verify switch_to_terminal_view called with command
       - Assert response 200

    4. test_switch_view_terminal_missing_command (line ~491):
       - Verify validation error for missing command
       - Check error response structure

    5. test_switch_view_unknown_type (line ~512):
       - Verify validation error for unknown view_type
       - Check error mentions invalid view type

    6. test_set_layout_canvas (line ~537):
       - Verify set_layout called with layout="canvas"
       - Assert response 200

    7. test_set_layout_split_horizontal (line ~561):
       - Test layout="split_horizontal"
       - Verify mock call

    8. test_set_layout_split_vertical (line ~584):
       - Test layout="split_vertical"
       - Verify mock call

    9. test_set_layout_tabs (line ~607):
       - Test layout="tabs"
       - Verify mock call

    10. test_set_layout_grid (line ~630):
        - Test layout="grid"
        - Verify mock call

    For validation error tests, ensure proper error checking:
    ```python
    assert response.status_code in [400, 422]
    data = response.json()
    assert "error" in data or "detail" in data
    ```
  </action>
  <verify>
    pytest backend/tests/api/test_agent_guidance_routes.py -k "test_switch_view or test_set_layout" -v
    All 10 tests pass with proper assertions
  </verify>
  <done>View orchestration endpoints fully tested with 10 passing tests</done>
</task>

<task type="auto">
  <name>Complete error guidance endpoint tests (present, track-resolution)</name>
  <files>backend/tests/api/test_agent_guidance_routes.py</files>
  <action>
    Update error guidance tests with proper assertions and mock verification:

    1. test_present_error_success (line ~657):
       - Assert response.status_code == 200
       - Verify present_error called with operation_id, error dict, agent_id
       - Check error structure passed to mock

    2. test_present_error_without_agent_id (line ~687):
       - Test with optional agent_id omitted
       - Verify present_error called with agent_id=None
       - Assert response 200

    3. test_present_error_with_complex_error (NEW):
       - Add test for complex error object with stack trace
       - Test error with type, message, code, stack_trace fields

    4. test_present_error_operation_not_found (NEW):
       - Test error handling when operation doesn't exist
       - Verify appropriate error response

    5. test_track_resolution_success (line ~719):
       - Verify track_resolution called with all parameters
       - Assert success=True recorded

    6. test_track_resolution_failed (line ~747):
       - Test with success=False
       - Verify failure tracking

    7. test_track_resolution_minimal_data (line ~775):
       - Test with only required fields (error_type, resolution_attempted, success)
       - Verify optional fields (error_code, user_feedback) default correctly

    8. test_track_resolution_with_user_feedback (NEW):
       - Test with detailed user_feedback
       - Verify feedback recorded in call

    9. test_track_resolution_not_agent_suggested (NEW):
       - Test with agent_suggested=False
       - Verify human-initiated resolution tracking

    For all error guidance tests, verify the ErrorGuidanceEngine mock:
    ```python
    mock_engine_instance.present_error.assert_called_once_with(
        user_id=mock_user.id,
        operation_id=mock_operation.operation_id,
        error=request.error,
        agent_id="test_agent"
    )
    ```
  </action>
  <verify>
    pytest backend/tests/api/test_agent_guidance_routes.py -k "test_present_error or test_track_resolution" -v
    All 9 tests pass (6 existing + 3 new)
  </verify>
  <done>Error guidance endpoints fully tested with 9 passing tests</done>
</task>

<task type="auto">
  <name>Complete agent request endpoint tests (permission, decision, respond, get)</name>
  <files>backend/tests/api/test_agent_guidance_routes.py</files>
  <action>
    Implement proper assertions for agent request tests:

    1. test_create_permission_request_success (line ~804):
       - Assert response.status_code == 200
       - Verify create_permission_request returns request_id
       - Check all parameters passed: user_id, agent_id, title, permission, context, urgency

    2. test_create_permission_request_with_expiration (line ~831):
       - Test with expires_in parameter
       - Verify expiration time handling

    3. test_create_permission_request_all_urgency_levels (NEW):
       - Test urgency="low", "medium", "high"
       - Verify all urgency levels handled

    4. test_create_decision_request_success (line ~863):
       - Verify create_decision_request called correctly
       - Check options list passed

    5. test_create_decision_request_with_suggestion (line ~891):
       - Test with suggested_option parameter
       - Verify suggestion included in call

    6. test_create_decision_request_with_expiration (NEW):
       - Test decision request with expires_in

    7. test_respond_to_request_success (line ~924):
       - Verify handle_response called with response data
       - Test approval response

    8. test_respond_to_request_deny (line ~955):
       - Test denial response
       - Verify negative response handling

    9. test_respond_to_request_with_custom_response (NEW):
       - Test complex response object with multiple fields

    10. test_get_request_success (line ~990):
        - Direct DB query for AgentRequestLog
        - Verify request serialization with all fields

    11. test_get_request_not_found (line ~1008):
        - Verify 404 for unknown request_id

    12. test_get_request_with_response (NEW):
        - Test getting request that already has user_response
        - Verify responded_at timestamp included
  </action>
  <verify>
    pytest backend/tests/api/test_agent_guidance_routes.py -k "test_create_permission or test_create_decision or test_respond_to_request or test_get_request" -v
    All 12 tests pass (9 existing + 3 new)
  </verify>
  <done>Agent request endpoints fully tested with 12 passing tests</done>
</task>

<task type="auto">
  <name>Measure coverage and verify 75%+ target achieved</name>
  <files>backend/tests/api/test_agent_guidance_routes.py</files>
  <action>
    1. Run pytest with coverage on agent_guidance_routes.py:
       ```bash
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/api/test_agent_guidance_routes.py backend/tests/unit/test_agent_guidance_routes.py \
         --cov=backend/api/agent_guidance_routes --cov-report=term-missing --cov-report=json -v
       ```

    2. Verify coverage report shows 75%+:
       - Check terminal output for percentage
       - Review backend/coverage.json for agent_guidance_routes.py

    3. Analyze uncovered lines:
       - Identify specific lines not covered
       - Add targeted tests for missing paths

    4. Common missing paths to check:
       - Exception handlers in try/except blocks
       - Edge cases in validation logic
       - Optional parameter handling
       - DB query result not found paths

    5. Add tests for any remaining gaps:
       - If below 75%, create tests for uncovered branches
       - Focus on high-impact paths (user-facing features)

    6. Document final coverage:
       - Update test file docstring with coverage percentage
       - Note any excluded lines
  </action>
  <verify>
    Coverage report shows 75%+ for backend/api/agent_guidance_routes.py
    pytest backend/tests/api/test_agent_guidance_routes.py --cov backend/api/agent_guidance_routes --cov-report=term | grep "agent_guidance_routes"
  </verify>
  <done>Agent guidance routes achieve 75%+ line coverage with 42+ passing tests</done>
</task>

</tasks>

<verification>
1. All stub tests implemented with proper assertions (not just accepting 200/500)
2. Coverage 75%+ on agent_guidance_routes.py
3. All 14 endpoints have test coverage
4. Mock service calls verified with assert_called_once_with
5. Error paths tested (404, validation errors)
</verification>

<success_criteria>
1. Agent guidance routes achieve 75%+ line coverage
2. All 82 stub tests fully implemented with proper assertions
3. New tests added for edge cases (3-5 new tests)
4. 45+ total passing tests
5. Test file follows Phase 167 TestClient patterns
</success_criteria>

<output>
After completion, create `.planning/phases/172-high-impact-zero-coverage-governance/172-02-SUMMARY.md`
</output>
