---
phase: 172-high-impact-zero-coverage-governance
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_admin_routes.py
autonomous: true

must_haves:
  truths:
    - "Admin WebSocket management endpoints achieve 75%+ coverage"
    - "Rating sync endpoints achieve 75%+ coverage"
    - "Conflict management endpoints achieve 75%+ coverage"
    - "WebSocketState model queries are tested"
    - "Governance enforcement (AUTONOMOUS for HIGH complexity) is tested"
  artifacts:
    - path: "backend/tests/api/test_admin_routes.py"
      provides: "TestClient-based API tests for WebSocket, rating sync, and conflict management"
      min_lines: 400 (additional lines)
      exports: ["test_get_websocket_status", "test_trigger_websocket_reconnect", "test_disable_websocket", "test_enable_websocket", "test_trigger_rating_sync", "test_list_conflicts", "test_resolve_conflict"]
  key_links:
    - from: "backend/tests/api/test_admin_routes.py"
      to: "backend/api/admin_routes.py"
      via: "TestClient router import (same file as Part 1)"
      pattern: "from api.admin_routes import router"
    - from: "backend/tests/api/test_admin_routes.py"
      to: "core/models.py"
      via: "WebSocketState, FailedRatingUpload, ConflictLog queries"
      pattern: "db.query(WebSocketState|FailedRatingUpload|ConflictLog)"
    - from: "backend/tests/api/test_admin_routes.py"
      to: "core/rating_sync_service.py"
      via: "patch mocking"
      pattern: "patch.*RatingSyncService"
---

<objective>
Achieve 75%+ line coverage on admin_routes.py Part 2 - WebSocket Management, Rating Sync, and Conflict Management (lines 546-1355). Covers WebSocket status/control, rating sync with Atom SaaS, and conflict resolution for skill sync.

Purpose: Admin routes Part 2 manages WebSocket connections, rating synchronization with Atom SaaS, and conflict resolution for skill data sync. These are operational endpoints requiring testing of service integration and governance enforcement.

Output: Extended test file with 30+ additional tests covering WebSocket, rating sync, and conflict endpoints.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Part 1 tests (extend this file)
@backend/tests/api/test_admin_routes.py

# Phase 167 TestClient patterns
@backend/tests/api/conftest.py

# Source file to test (Part 2)
@backend/api/admin_routes.py
</context>

<tasks>

<task type="auto">
  <name>Implement WebSocket status and management endpoint tests</name>
  <files>backend/tests/api/test_admin_routes.py</files>
  <action>
    Add tests for WebSocket endpoints: GET /api/admin/websocket/status, POST /api/admin/websocket/reconnect, POST /api/admin/websocket/disable, POST /api/admin/websocket/enable:

    1. test_get_websocket_status_connected:
       - Create WebSocketState in DB with connected=True
       - Set last_connected_at, last_message_at, reconnect_attempts, etc.
       - GET /api/admin/websocket/status
       - Assert 200 status
       - Verify response: connected=True, ws_url, timestamps, reconnect_attempts, consecutive_failures

    2. test_get_websocket_status_disconnected:
       - Create WebSocketState with connected=False
       - GET /api/admin/websocket/status
       - Assert 200 status
       - Verify connected=False

    3. test_get_websocket_status_no_state:
       - No WebSocketState in DB
       - GET /api/admin/websocket/status
       - Assert 200 status
       - Verify default values returned (connected=False, reconnect_attempts=0, etc.)

    4. test_get_websocket_status_unauthorized:
       - Patch get_current_user to return non-autonomous user
       - GET /api/admin/websocket/status
       - Assert 403 status
       - Verify error mentions AUTONOMOUS required

    5. test_trigger_websocket_reconnect_success:
       - Create WebSocketState
       - POST /api/admin/websocket/reconnect
       - Patch get_current_user for auth
       - Assert 200 status
       - Verify reconnect_triggered=True
       - Verify DB updated: reconnect_attempts=0, consecutive_failures=0, fallback_to_polling=False

    6. test_trigger_websocket_reconnect_creates_state:
       - No WebSocketState in DB
       - POST /api/admin/websocket/reconnect
       - Verify new WebSocketState created with id=1

    7. test_trigger_websocket_reconnect_governance:
       - Verify @require_governance with HIGH complexity
       - Patch governance to fail for non-autonomous
       - Assert governance enforced

    8. test_disable_websocket_success:
       - Create WebSocketState with connected=True
       - POST /api/admin/websocket/disable
       - Assert 200 status
       - Verify websocket_enabled=False
       - Verify DB: fallback_to_polling=True, connected=False, disconnect_reason="disabled_by_admin"

    9. test_disable_websocket_creates_state:
       - No WebSocketState in DB
       - POST /api/admin/websocket/disable
       - Verify new state created

    10. test_disable_websocket_governance:
        - Verify HIGH complexity governance

    11. test_enable_websocket_success:
        - Create WebSocketState with fallback_to_polling=True
        - POST /api/admin/websocket/enable
        - Assert 200 status
        - Verify websocket_enabled=True
        - Verify DB: fallback_to_polling=False, next_ws_attempt_at=None, reconnect_attempts=0

    12. test_enable_websocket_creates_state:
        - No WebSocketState in DB
        - POST /api/admin/websocket/enable
        - Verify new state created

    13. test_enable_websocket_governance:
        - Verify HIGH complexity governance

    For WebSocket tests, verify the governance check:
    ```python
    from core.agent_governance_service import GovernanceCache
    governance_cache = GovernanceCache()
    # Verify maturity check happens
    ```

    Note: If WebSocketState model doesn't exist in models.py, create it in test setup:
    ```python
    # Create a mock table for testing
    from sqlalchemy import Column, String, Boolean, Integer, DateTime
    class WebSocketState(Base):
        __tablename__ = "websocket_state"
        id = Column(Integer, primary_key=True)
        connected = Column(Boolean, default=False)
        # ... other fields
    ```
  </action>
  <verify>
    pytest backend/tests/api/test_admin_routes.py -k "websocket" -v
    All 13 tests pass
  </verify>
  <done>WebSocket management endpoints tested with 13 passing tests</done>
</task>

<task type="auto">
  <name>Implement rating sync endpoint tests</name>
  <files>backend/tests/api/test_admin_routes.py</files>
  <action>
    Add tests for rating sync endpoints: POST /api/admin/sync/ratings, GET /api/admin/ratings/failed-uploads, POST /api/admin/ratings/failed-uploads/{failed_id}/retry:

    1. test_trigger_rating_sync_success:
       - Create mock SkillRating records with synced_at=None (pending)
       - Patch RatingSyncService to return {"success": True, "uploaded": 5, "failed": 0, "skipped": 0}
       - POST /api/admin/sync/ratings with upload_all=False
       - Assert 200 status
       - Verify response: success=True, uploaded=5, pending_count
       - Verify service called with upload_all=False

    2. test_trigger_rating_sync_upload_all:
       - POST with upload_all=True
       - Verify service called with upload_all=True

    3. test_trigger_rating_sync_in_progress:
       - Patch RatingSyncService._sync_in_progress = True
       - POST /api/admin/sync/ratings
       - Assert 503 status
       - Verify error_code="RATING_SYNC_IN_PROGRESS"

    4. test_trigger_rating_sync_with_failures:
       - Patch service to return {"success": True, "uploaded": 3, "failed": 2, "skipped": 1}
       - Verify failure counts in response

    5. test_trigger_rating_sync_governance:
       - Verify @require_governance with HIGH complexity
       - Test AUTONOMOUS maturity enforcement

    6. test_get_failed_rating_uploads_success:
       - Create FailedRatingUpload records in DB
       - GET /api/admin/ratings/failed-uploads
       - Assert 200 status
       - Verify list returned with: id, rating_id, error_message, failed_at, retry_count, last_retry_at
       - Verify limited to 100 records

    7. test_get_failed_rating_uploads_empty:
       - No failed uploads in DB
       - GET /api/admin/ratings/failed-uploads
       - Assert 200 status with empty list

    8. test_get_failed_rating_uploads_unauthorized:
       - Patch get_current_user for non-autonomous
       - GET /api/admin/ratings/failed-uploads
       - Assert 403 status

    9. test_retry_failed_rating_upload_success:
       - Create FailedRatingUpload and associated SkillRating
       - Patch RatingSyncService.upload_rating to return {"success": True, "rating_id": "remote_123"}
       - POST /api/admin/ratings/failed-uploads/{failed_id}/retry
       - Assert 200 status
       - Verify retry_triggered=True
       - Verify FailedRatingUpload deleted from DB
       - Verify SkillRating synced (mark_as_synced called)

    10. test_retry_failed_rating_upload_rating_deleted:
        - Create FailedRatingUpload but no SkillRating
        - POST retry
        - Assert 200 status
        - Verify message about rating no longer existing
        - Verify failed record removed

    11. test_retry_failed_rating_upload_failed_again:
        - Patch upload_rating to return {"success": False, "error": "Network error"}
        - POST retry
        - Assert 200 status with success=False
        - Verify retry_count incremented
        - Verify last_retry_at updated

    12. test_retry_failed_rating_upload_not_found:
        - POST retry with non-existent failed_id
        - Assert 404 status

    For rating sync tests, verify service integration:
    ```python
    from core.rating_sync_service import RatingSyncService
    from core.atom_saas_client import AtomSaaSClient
    # Verify client and service instantiation
    ```
  </action>
  <verify>
    pytest backend/tests/api/test_admin_routes.py -k "rating" -v
    All 12 tests pass
  </verify>
  <done>Rating sync endpoints tested with 12 passing tests</done>
</task>

<task type="auto">
  <name>Implement conflict management endpoint tests</name>
  <files>backend/tests/api/test_admin_routes.py</files>
  <action>
    Add tests for conflict endpoints: GET /api/admin/conflicts, GET /api/admin/conflicts/{conflict_id}, POST /api/admin/conflicts/{conflict_id}/resolve, POST /api/admin/conflicts/bulk-resolve:

    1. test_list_conflicts_success:
       - Create ConflictLog records with different severity, conflict_type
       - Patch ConflictResolutionService.get_unresolved_conflicts to return mock list
       - GET /api/admin/conflicts
       - Assert 200 status
       - Verify response: conflicts array, total_count, page, page_size
       - Verify pagination applied

    2. test_list_conflicts_filtered_by_severity:
       - GET /api/admin/conflicts?severity=high
       - Verify filter passed to service

    3. test_list_conflicts_filtered_by_type:
       - GET /api/admin/conflicts?conflict_type=version_mismatch
       - Verify filter passed to service

    4. test_list_conflicts_paginated:
       - Create 100 conflicts
       - GET /api/admin/conflicts?page=2&page_size=20
       - Verify correct slice returned

    5. test_list_conflicts_empty:
       - No conflicts in DB
       - GET /api/admin/conflicts
       - Assert 200 status with empty conflicts array

    6. test_list_conflicts_governance:
       - Verify @require_governance with HIGH complexity

    7. test_get_conflict_success:
       - Create ConflictLog
       - Patch ConflictResolutionService.get_conflict_by_id to return conflict
       - GET /api/admin/conflicts/{conflict_id}
       - Assert 200 status
       - Verify all fields: id, skill_id, conflict_type, severity, local_data, remote_data, resolution_strategy, resolved_at, etc.

    8. test_get_conflict_not_found:
       - Patch get_conflict_by_id to return None
       - GET /api/admin/conflicts/{conflict_id}
       - Assert 404 status

    9. test_get_conflict_governance:
       - Verify HIGH complexity governance

    10. test_resolve_conflict_remote_wins:
        - Create ConflictLog
        - Patch ConflictResolutionService.resolve_conflict to return resolved_data
        - POST /api/admin/conflicts/{conflict_id}/resolve with {"strategy": "remote_wins", "resolved_by": "admin"}
        - Assert 200 status
        - Verify success=True, resolved_data returned
        - Verify service called with strategy and resolved_by

    11. test_resolve_conflict_local_wins:
        - Test with strategy="local_wins"

    12. test_resolve_conflict_merge:
        - Test with strategy="merge"

    13. test_resolve_conflict_invalid_strategy:
        - POST with strategy="invalid"
        - Assert 422 validation error
        - Verify error lists valid strategies

    14. test_resolve_conflict_updates_cache:
        - Patch SkillCache query
        - Resolve conflict successfully
        - Verify cache updated with resolved_data
        - Verify expires_at set correctly

    15. test_resolve_conflict_not_found:
        - Patch resolve_conflict to return None
        - POST resolve
        - Assert 200 with success=False (not 404, service returns None gracefully)

    16. test_resolve_conflict_governance:
        - Verify HIGH complexity governance

    17. test_bulk_resolve_conflicts_success:
        - Create multiple ConflictLog records
        - Patch resolve_conflict for each to return resolved_data
        - POST /api/admin/conflicts/bulk-resolve with conflict_ids=[1,2,3], strategy="remote_wins"
        - Assert 200 status
        - Verify resolved_count=3, failed_count=0

    18. test_bulk_resolve_conflicts_partial_failure:
        - Patch some resolves to succeed, some to fail
        - POST bulk-resolve
        - Verify resolved_count and failed_count both > 0
        - Verify errors array populated

    19. test_bulk_resolve_conflicts_invalid_strategy:
        - POST with invalid strategy
        - Assert 422 validation error

    20. test_bulk_resolve_conflicts_too_many_ids:
        - POST with 101 conflict_ids (max is 100)
        - Assert 422 validation error

    21. test_bulk_resolve_conflicts_governance:
        - Verify HIGH complexity governance

    For conflict resolution tests, verify the strategy validation:
    ```python
    valid_strategies = ["remote_wins", "local_wins", "merge"]
    if request.strategy not in valid_strategies:
        raise router.validation_error(...)
    ```
  </action>
  <verify>
    pytest backend/tests/api/test_admin_routes.py -k "conflict" -v
    All 21 tests pass
  </verify>
  <done>Conflict management endpoints tested with 21 passing tests</done>
</task>

<task type="auto">
  <name>Measure total admin routes coverage and verify 75%+ target</name>
  <files>backend/tests/api/test_admin_routes.py</files>
  <action>
    1. Run pytest with coverage on complete admin_routes.py:
       ```bash
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/api/test_admin_routes.py \
         --cov=backend/api/admin_routes --cov-report=term-missing --cov-report=json -v
       ```

    2. Verify coverage for entire file:
       - Part 1 (lines 1-545): User/Role management
       - Part 2 (lines 546-1355): WebSocket, Rating Sync, Conflicts
       - Overall: 75%+ coverage

    3. Analyze any remaining uncovered lines:
       - Check exception handlers
       - Verify error paths
       - Look for edge cases

    4. Add targeted tests for gaps:
       - If WebSocket governance not fully covered, add more auth tests
       - If rating sync error handling not covered, add failure tests
       - If conflict cache update not covered, add cache test

    5. Verify all endpoint categories covered:
       - User Management: 18 tests (from Part 1)
       - Role Management: 16 tests (from Part 1)
       - WebSocket: 13 tests (Part 2)
       - Rating Sync: 12 tests (Part 2)
       - Conflicts: 21 tests (Part 2)
       - Total: 80+ tests

    6. Document final coverage:
       - Overall percentage
       - Coverage by endpoint category
       - Any remaining gaps

    Success criteria: 75%+ coverage on entire admin_routes.py file
  </action>
  <verify>
    Coverage report shows 75%+ for backend/api/admin_routes.py
    pytest backend/tests/api/test_admin_routes.py --cov backend/api/admin_routes --cov-report=term | tail -5
  </verify>
  <done>Admin routes complete (Parts 1 & 2) achieve 75%+ coverage with 80+ passing tests</done>
</task>

</tasks>

<verification>
1. WebSocket management endpoints tested: status, reconnect, disable, enable (13 tests)
2. Rating sync endpoints tested: sync, failed uploads, retry (12 tests)
3. Conflict management endpoints tested: list, get, resolve, bulk-resolve (21 tests)
4. Governance enforcement tested: HIGH complexity requires AUTONOMOUS maturity
5. Error paths tested: 403 unauthorized, 404 not found, 422 validation, 503 service unavailable
6. Service integration mocked: RatingSyncService, ConflictResolutionService, AtomSaaSClient
</verification>

<success_criteria>
1. Admin routes Part 2 (lines 546-1355) achieves 75%+ line coverage
2. Overall admin_routes.py achieves 75%+ coverage (Parts 1 & 2 combined)
3. 46 new tests added in Part 2 (13 + 12 + 21)
4. Total 80+ tests in admin_routes test file
5. Test file follows Phase 167 TestClient patterns
</success_criteria>

<output>
After completion, create `.planning/phases/172-high-impact-zero-coverage-governance/172-04-SUMMARY.md`
</output>
