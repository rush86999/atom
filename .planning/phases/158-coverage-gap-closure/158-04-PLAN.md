---
phase: 158-coverage-gap-closure
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/tests/integration/services/test_llm_service_http_coverage.py
  - backend/tests/integration/services/conftest.py
  - backend/tests/coverage_reports/metrics/llm_service_coverage.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "LLM service coverage increases from 36.5% toward 80% target"
    - "HTTP-level mocking exercises actual BYOK methods (generate_response, _call_*)"
    - "All provider paths (OpenAI, Anthropic, DeepSeek, Gemini) are tested"
    - "Streaming responses are tested with HTTP-level mocks"
    - "Rate limiting and error handling are tested at HTTP level"
  artifacts:
    - path: "backend/tests/integration/services/test_llm_service_http_coverage.py"
      provides: "HTTP-level LLM service tests for 80% coverage"
      min_lines: 400
      exports: ["TestLLMHTTPLevelCoverage", "TestStreamingHTTPLevel", "TestRateLimitingHTTPLevel", "TestErrorHandlingHTTPLevel"]
    - path: "backend/tests/coverage_reports/metrics/llm_service_coverage.json"
      provides: "LLM service coverage metrics"
      contains: "coverage_pct"
  key_links:
    - from: "test_llm_service_http_coverage.py"
      to: "core/llm/byok_handler.py"
      via: "HTTP-level mocking for BYOK methods"
      pattern: "generate_response|_call_openai|_call_anthropic"
    - from: "test_llm_service_http_coverage.py"
      to: "core/llm/cache_aware_router.py"
      via: "Cache-aware routing tests"
      pattern: "get_cached_response|set_cached_response"
    - from: "test_llm_service_http_coverage.py"
      to: "core/llm/escalation_manager.py"
      via: "Escalation manager tests"
      pattern: "check_quality_threshold|escalate_request"
---

<objective>
Expand LLM service coverage from 36.5% to 80% using HTTP-level mocking to exercise actual BYOK methods (generate_response, _call_*) instead of client-level mocking that skips critical code paths.

Purpose: Current LLM service tests use client-level mocking which validates logic but doesn't achieve coverage targets. HTTP-level mocking exercises the actual BYOK methods that stream responses, handle errors, and interact with providers.

Output: 40+ new tests covering all provider paths, streaming, rate limiting, and error handling at HTTP level.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/tmp/phase158-gaps-verification.md
@.planning/phases/156-core-services-coverage-high-impact/156-02-SUMMARY.md
@.planning/phases/156-core-services-coverage-high-impact/156-05-SUMMARY.md
@backend/core/llm/byok_handler.py
@backend/core/llm/cache_aware_router.py
@backend/core/llm/escalation_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up HTTP-level mocking infrastructure for LLM tests</name>
  <files>
    backend/tests/integration/services/test_llm_service_http_coverage.py
    backend/tests/integration/services/conftest.py
  </files>
  <action>
    Create HTTP-level mocking infrastructure to exercise actual BYOK methods:

    1. Create backend/tests/integration/services/test_llm_service_http_coverage.py

    2. Add HTTP mock fixtures to conftest.py:
       - mock_openai_http: HTTP mock for OpenAI API responses
       - mock_anthropic_http: HTTP mock for Anthropic API responses
       - mock_deepseek_http: HTTP mock for DeepSeek API responses
       - mock_gemini_http: HTTP mock for Gemini API responses
       - mock_http_error: HTTP mock for error responses (429, 500, etc.)

    3. Use pytest's monkeypatch or requests_mock to intercept HTTP calls:
       - Patch at the HTTP client level (requests/httpx)
       - Return realistic response bodies (streaming chunks, error payloads)
       - Simulate network errors, timeouts, rate limits

    4. Pattern for HTTP-level mocking:
       ```python
       @pytest.fixture
       def mock_openai_stream(monkeypatch):
           def mock_stream_post(*args, **kwargs):
               # Return mock response with streaming chunks
               return MockStreamResponse(["chunk1", "chunk2", "chunk3"])

           monkeypatch.setattr("requests.post", mock_stream_post)
       ```

    DO NOT mock at the client level (e.g., OpenAI() class). Mock at HTTP level to exercise generate_response() and _call_* methods.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_llm_service_http_coverage.py::test_http_mock_setup --collect-only`
    Verify HTTP mock fixtures are registered and discoverable
  </verify>
  <done>
    HTTP-level mocking infrastructure in place, all provider HTTP mocks created
  </done>
</task>

<task type="auto">
  <name>Task 2: Test all provider HTTP paths (OpenAI, Anthropic, DeepSeek, Gemini)</name>
  <files>
    backend/tests/integration/services/test_llm_service_http_coverage.py
  </files>
  <action>
    Create tests for all provider HTTP paths using HTTP-level mocking:

    Class: TestLLMHTTPLevelCoverage

    1. test_openai_http_request (parametrize for models):
       - Mock OpenAI HTTP response (gpt-4, gpt-3.5-turbo)
       - Call generate_response() with OpenAI provider
       - Verify HTTP request includes correct headers, body
       - Verify response parsing handles streaming chunks

    2. test_anthropic_http_request (parametrize for models):
       - Mock Anthropic HTTP response (claude-3-opus, claude-3-sonnet)
       - Call generate_response() with Anthropic provider
       - Verify Anthropic-specific headers (x-api-key, anthropic-version)
       - Verify response parsing

    3. test_deepseek_http_request:
       - Mock DeepSeek HTTP response
       - Verify DeepSeek-specific request format
       - Verify response parsing

    4. test_gemini_http_request:
       - Mock Gemini HTTP response
       - Verify Gemini-specific request format
       - Verify response parsing

    5. test_provider_fallback:
       - Mock primary provider failure
       - Verify fallback to secondary provider
       - Verify error logging

    Use @pytest.mark.parametrize for model variations.
    Test both success and error HTTP responses (200, 400, 401, 429, 500).
    Verify request headers include API keys, content-type, etc.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_llm_service_http_coverage.py::TestLLMHTTPLevelCoverage -v`
    At least 10 provider path tests created and passing
  </verify>
  <done>
    All provider HTTP paths tested with realistic HTTP mocking
  </done>
</task>

<task type="auto">
  <name>Task 3: Test streaming responses at HTTP level</name>
  <files>
    backend/tests/integration/services/test_llm_service_http_coverage.py
  </files>
  <action>
    Create tests for streaming responses using HTTP-level mocking:

    Class: TestStreamingHTTPLevel

    1. test_openai_streaming_chunks:
       - Mock OpenAI streaming response with multiple chunks
       - Verify chunks are processed correctly
       - Verify chunk accumulation and final response

    2. test_anthropic_streaming_chunks:
       - Mock Anthropic streaming response
       - Verify SSE (Server-Sent Events) parsing
       - Verify start/end event handling

    3. test_streaming_timeout:
       - Mock slow streaming response
       - Verify timeout handling
       - Verify partial response handling

    4. test_streaming_error_mid_response:
       - Mock streaming error after some chunks
       - Verify error handling
       - Verify cleanup of partial response

    5. test_streaming_empty_chunks:
       - Mock streaming response with empty chunks
       - Verify handling of empty/whitespace chunks

    6. test_streaming_large_response:
       - Mock large streaming response (100+ chunks)
       - Verify performance and memory handling

    Pattern for mocking streaming:
    ```python
    class MockStreamResponse:
        def __iter__(self):
            for chunk in ["chunk1", "chunk2", "chunk3"]:
                yield f"data: {chunk}\n\n"
    ```

    Test chunk accumulation, SSE parsing, timeout handling, and error recovery.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_llm_service_http_coverage.py::TestStreamingHTTPLevel -v`
    At least 6 streaming tests created and passing
  </verify>
  <done>
    Streaming responses tested comprehensively at HTTP level
  </done>
</task>

<task type="auto">
  <name>Task 4: Test rate limiting and error handling at HTTP level</name>
  <files>
    backend/tests/integration/services/test_llm_service_http_coverage.py
  </files>
  <action>
    Create tests for rate limiting and error handling using HTTP-level mocking:

    Class: TestRateLimitingHTTPLevel
    Class: TestErrorHandlingHTTPLevel

    Rate limiting tests:
    1. test_429_rate_limit_retry:
       - Mock 429 response with Retry-After header
       - Verify retry logic respects Retry-After
       - Verify eventual success after retry

    2. test_rate_limit_backoff:
       - Mock multiple 429 responses
       - Verify exponential backoff
       - Verify max retry limit

    3. test_concurrent_request_limiting:
       - Mock rate limit for concurrent requests
       - Verify request queuing/throttling

    Error handling tests:
    1. test_401_unauthorized_recovery:
       - Mock 401 response
       - Verify error handling
       - Verify API key refresh attempt

    2. test_500_internal_server_error:
       - Mock 500 response
       - Verify retry logic
       - Verify fallback provider

    3. test_network_timeout:
       - Mock network timeout
       - Verify timeout handling
       - Verify graceful degradation

    4. test_malformed_response:
       - Mock malformed JSON response
       - Verify error parsing
       - Verify error logging

    5. test_empty_response:
       - Mock empty response body
       - Verify handling

    Test error recovery, retry logic, fallback behavior, and logging.
    Verify coverage of all error paths in BYOK handler.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_llm_service_http_coverage.py::TestRateLimitingHTTPLevel -v`
    Run `pytest backend/tests/integration/services/test_llm_service_http_coverage.py::TestErrorHandlingHTTPLevel -v`
    At least 8 rate limiting and error handling tests created and passing
  </verify>
  <done>
    Rate limiting and error handling tested comprehensively at HTTP level
  </done>
</task>

<task type="auto">
  <name>Task 5: Measure and verify LLM service coverage reaches 80%</name>
  <files>
    backend/tests/coverage_reports/metrics/llm_service_coverage.json
  </files>
  <action>
    Run coverage measurement for LLM service and verify 80% target:

    1. Run pytest with coverage:
       ```bash
       cd backend
       pytest tests/integration/services/test_llm_service_http_coverage.py \
              --cov=core/llm/byok_handler \
              --cov=core/llm/cache_aware_router \
              --cov=core/llm/escalation_manager \
              --cov-report=json:tests/coverage_reports/metrics/llm_service_coverage.json \
              --cov-report=term
       ```

    2. Parse coverage report:
       - Extract coverage percentage for byok_handler.py
       - Identify uncovered lines
       - Categorize uncovered lines (dead code, error paths, edge cases)

    3. If coverage < 80%:
       - Identify top 5 uncovered functions/lines
       - Create targeted tests for uncovered paths
       - Re-run coverage measurement

    4. Document coverage results:
       - Final coverage percentage
       - Remaining gaps (if any)
       - Test count (passing/total)

    5. Update cross_platform_summary.json with new backend coverage

    Use pytest-cov for coverage measurement.
    Focus on BYOK methods: generate_response(), _call_openai(), _call_anthropic(), etc.
  </action>
  <verify>
    backend/tests/coverage_reports/metrics/llm_service_coverage.json exists
    Coverage percentage >= 80% for byok_handler.py
    All tests passing
  </verify>
  <done>
    LLM service coverage reaches 80% target, documented in coverage report
  </done>
</task>

</tasks>

<verification>
Run `cd backend && pytest tests/integration/services/test_llm_service_http_coverage.py --cov=core/llm/byok_handler --cov=core/llm/cache_aware_router --cov=core/llm/escalation_manager`
Verify coverage report shows >= 80% for all LLM service files
Verify all tests pass (no failures)
Verify backend/tests/coverage_reports/cross_platform_summary.json updated
</verification>

<success_criteria>
1. At least 30 new tests created (10 provider paths + 6 streaming + 8 rate limiting/errors + 6 verification)
2. LLM service coverage >= 80% (up from 36.5%)
3. HTTP-level mocking exercises actual BYOK methods (not just client-level mocks)
4. All provider paths tested (OpenAI, Anthropic, DeepSeek, Gemini)
5. Streaming, rate limiting, and error handling comprehensively tested
</success_criteria>

<output>
After completion, create `.planning/phases/158-coverage-gap-closure/158-04-SUMMARY.md`
</output>
