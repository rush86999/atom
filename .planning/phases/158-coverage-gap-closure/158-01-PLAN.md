---
phase: 158-coverage-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - menubar/src-tauri/src/main.rs
  - menubar/src-tauri/Cargo.toml
  - backend/tests/coverage_reports/desktop_coverage.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Desktop Tauri application compiles without errors (cargo check passes)"
    - "All 19 compilation errors in main.rs are resolved"
    - "Tarpaulin can run and generate baseline coverage report"
    - "Desktop coverage baseline measured (actual coverage, not 0%)"
    - "23 accessibility tests from Phase 157 can execute"
  artifacts:
    - path: "menubar/src-tauri/src/main.rs"
      provides: "Tauri main entry point with compilation errors fixed"
      min_lines: 100
    - path: "backend/tests/coverage_reports/desktop_coverage.json"
      provides: "Desktop coverage baseline measurement"
      contains: "coverage_pct"
  key_links:
    - from: "menubar/src-tauri/src/main.rs"
      to: "cargo tarpaulin"
      via: "Rust compilation must succeed before coverage can be measured"
      pattern: "cargo check|cargo build"
    - from: "menubar/src-tauri/tests/"
      to: "tarpaulin coverage"
      via: "Tests require successful compilation to run"
      pattern: "cargo tarpaulin --out Json"
---

<objective>
Fix 19 pre-existing compilation errors in menubar/src-tauri/src/main.rs to unblock desktop coverage measurement and enable 23 accessibility tests from Phase 157 to execute.

Purpose: Desktop coverage is currently 0% because compilation errors block Tarpaulin from measuring coverage. This plan resolves the root cause blocking all desktop testing progress.

Output: Compiling Tauri application, baseline coverage measurement, and 23 unblocked accessibility tests.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/tmp/phase158-gaps-verification.md

# Desktop compilation context
@menubar/src-tauri/src/main.rs
@menubar/src-tauri/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and categorize compilation errors</name>
  <files>
    menubar/src-tauri/src/main.rs
    menubar/src-tauri/Cargo.toml
  </files>
  <action>
    Run `cargo check --manifest-path=menubar/src-tauri/Cargo.toml` to capture all compilation errors.

    Categorize errors by type:
    1. Missing imports (use statements)
    2. Type mismatches
    3. Missing dependencies in Cargo.toml
    4. Deprecated API usage
    5. Syntax errors

    Document each error with:
    - Error message
    - Line number
    - Category
    - Proposed fix

    Save diagnostic output to a temporary file for reference during fixes.
  </action>
  <verify>
    Run `cargo check 2>&1 | tee /tmp/desktop_compile_errors.txt`
    Count errors: `grep -c "^error" /tmp/desktop_compile_errors.txt`
    Expected: ~19 errors documented
  </verify>
  <done>
    All 19 compilation errors documented with categories and proposed fixes
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix compilation errors by category</name>
  <files>
    menubar/src-tauri/src/main.rs
    menubar/src-tauri/Cargo.toml
  </files>
  <action>
    Fix errors systematically by category (highest priority first):

    Priority order:
    1. Missing dependencies - Add to Cargo.toml if legitimate dependencies are missing
    2. Missing imports - Add appropriate use statements
    3. Type mismatches - Fix type annotations or conversions
    4. Deprecated APIs - Update to current Tauri API versions
    5. Syntax errors - Fix syntax issues

    For each error:
    1. Locate the problematic code
    2. Apply the minimal fix that resolves the error
    3. Add a comment if the fix changes behavior significantly
    4. Verify the fix compiles

    Common Tauri 2.x patterns to check:
    - `tauri::Manager` for window management
    - `tauri::AppHandle` for app context
    - `tauri::Window` for window operations
    - Use `#[tauri::command]` for commands invoked from frontend

    DO NOT refactor code - only fix what's necessary to compile. DO NOT add new features.
  </action>
  <verify>
    After each category of fixes, run `cargo check --manifest-path=menubar/src-tauri/Cargo.toml`
    Expected: Error count decreases with each fix round
    Final: `cargo check` exits with 0 errors
  </verify>
  <done>
    `cargo check` passes with 0 errors, `cargo build` succeeds
  </done>
</task>

<task type="auto">
  <name>Task 3: Generate baseline desktop coverage with Tarpaulin</name>
  <files>
    backend/tests/coverage_reports/desktop_coverage.json
  </files>
  <action>
    Now that compilation succeeds, run Tarpaulin to measure baseline desktop coverage:

    1. Run coverage:
       ```bash
       cd menubar/src-tauri
       cargo tarpaulin --out Json --output-file ../../backend/tests/coverage_reports/desktop_coverage.json --verbose
       ```

    2. If Tarpaulin has issues on macOS (known linking problems), use ubuntu-latest runner pattern:
       - Note the limitation in the summary
       - Record that CI/CD should use ubuntu-latest for desktop coverage

    3. Parse the coverage report to extract:
       - Overall coverage percentage
       - Files covered/total
       - Uncovered file list

    4. Update backend/tests/coverage_reports/cross_platform_summary.json with new desktop coverage

    5. Run the 23 accessibility tests from Phase 157:
       ```bash
       cd menubar/src-tauri
       cargo test accessibility --verbose
       ```

    Record results in the summary.
  </action>
  <verify>
    1. backend/tests/coverage_reports/desktop_coverage.json exists with valid JSON
    2. cargo test accessibility runs (may have failures but should execute)
    3. cross_platform_summary.json updated with desktop coverage > 0%
  </verify>
  <done>
    Desktop compilation errors fixed, baseline coverage measured, accessibility tests unblocked
  </done>
</task>

</tasks>

<verification>
Run `cargo check --manifest-path=menubar/src-tauri/Cargo.toml` - must pass with 0 errors
Run `cargo build --manifest-path=menubar/src-tauri/Cargo.toml` - must succeed
Verify backend/tests/coverage_reports/desktop_coverage.json exists and contains coverage data
Verify cargo test accessibility executes (tests may fail but compilation must succeed)
</verification>

<success_criteria>
1. Zero compilation errors when running `cargo check`
2. Tarpaulin generates valid coverage report (desktop_coverage.json)
3. Desktop coverage measured as > 0% (actual baseline established)
4. Accessibility tests from Phase 157 can execute (unblocked)
5. cross_platform_summary.json updated with desktop coverage data
</success_criteria>

<output>
After completion, create `.planning/phases/158-coverage-gap-closure/158-01-SUMMARY.md`
</output>
