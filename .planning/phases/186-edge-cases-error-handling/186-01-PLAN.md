---
phase: 186-edge-cases-error-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/error_paths/test_agent_lifecycle_error_paths.py
  - backend/tests/error_paths/test_workflow_error_paths.py
  - backend/tests/boundary_conditions/test_api_boundary_conditions.py
autonomous: true

must_haves:
  truths:
    - "Error handling paths for agent lifecycle services (graduation, promotion, evolution) achieve 75%+ line coverage"
    - "Error handling paths for workflow services (optimizer, advanced workflows) achieve 75%+ line coverage"
    - "Boundary conditions for API routes (pagination, limits, validation) achieve 75%+ line coverage"
    - "All error scenarios use VALIDATED_BUG pattern with severity classification"
  artifacts:
    - path: "backend/tests/error_paths/test_agent_lifecycle_error_paths.py"
      provides: "Agent lifecycle error path tests (graduation, promotion, evolution)"
      min_lines: 800
      exports: ["TestAgentGraduationErrorPaths", "TestAgentPromotionErrorPaths", "TestAgentEvolutionErrorPaths"]
    - path: "backend/tests/error_paths/test_workflow_error_paths.py"
      provides: "Workflow service error path tests"
      min_lines: 700
      exports: ["TestWorkflowOptimizerErrorPaths", "TestAdvancedWorkflowErrorPaths", "TestWorkflowValidationErrorPaths"]
    - path: "backend/tests/boundary_conditions/test_api_boundary_conditions.py"
      provides: "API boundary condition tests (pagination, limits, validation)"
      min_lines: 600
      exports: ["TestPaginationBoundaries", "TestRateLimitBoundaries", "TestValidationBoundaries"]
  key_links:
    - from: "backend/tests/error_paths/test_agent_lifecycle_error_paths.py"
      to: "backend/core/agent_graduation_service.py"
      via: "Direct import and mock testing"
      pattern: "from core.agent_graduation_service import AgentGraduationService"
    - from: "backend/tests/error_paths/test_workflow_error_paths.py"
      to: "backend/core/ai_workflow_optimizer.py"
      via: "Direct import and mock testing"
      pattern: "from core.ai_workflow_optimizer import AIWorkflowOptimizer"
    - from: "backend/tests/boundary_conditions/test_api_boundary_conditions.py"
      to: "backend/api"
      via: "FastAPI TestClient"
      pattern: "from fastapi.testclient import TestClient"

---

<objective>
Create comprehensive error path and boundary condition tests for agent lifecycle services, workflow services, and API routes to achieve 75%+ line coverage on error handling paths.

Purpose: Core services for agent graduation, promotion, workflow optimization, and API validation lack comprehensive error path coverage. These are critical governance and automation features that must handle edge cases gracefully (invalid inputs, missing data, constraint violations, boundary conditions).

Output: Three new test files with 2100+ lines covering error paths and boundary conditions with VALIDATED_BUG pattern documentation.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/tests/error_paths/ERROR_PATH_DOCUMENTATION.md
@backend/core/agent_graduation_service.py
@backend/core/agent_promotion_service.py
@backend/core/agent_evolution_loop.py
@backend/core/ai_workflow_optimizer.py
@backend/core/advanced_workflow_system.py

# Existing Error Path Coverage
- error_paths/: 322 tests across 10 files (auth, database, finance, governance, LLM, security, websocket, etc.)
- boundary_conditions/: 77 tests (episode boundaries, governance boundaries, LLM boundaries, maturity boundaries)
- critical_error_paths/: 217 tests (circuit breaker, timeouts, graceful degradation, rate limiting)

# Phase 104 Error Path Patterns
- VALIDATED_BUG docstring pattern with Expected/Actual/Severity/Impact/Fix/Validated
- pytest.raises() for exception validation
- Descriptive test names: test_{function}_with_{error_condition}
- Fixtures for common setup (mock_db, valid_password_hash, etc.)

# Missing Coverage Areas
- Agent lifecycle services: graduation, promotion, evolution (0 error path tests)
- Workflow services: optimizer, advanced workflows (0 error path tests)
- API boundary conditions: pagination, rate limits, validation (partial coverage)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent lifecycle error path tests</name>
  <files>backend/tests/error_paths/test_agent_lifecycle_error_paths.py</files>
  <action>
    Create new test file backend/tests/error_paths/test_agent_lifecycle_error_paths.py with comprehensive error path coverage for agent lifecycle services.

    **File structure:**
    ```python
    """
    Agent Lifecycle Error Path Tests

    Tests error handling and edge cases for:
    - AgentGraduationService (episode-based promotion readiness)
    - AgentPromotionService (maturity level transitions)
    - AgentEvolutionLoop (continuous improvement cycles)

    Uses VALIDATED_BUG pattern for documenting discovered issues.
    """
    import pytest
    from datetime import datetime, timedelta, timezone
    from unittest.mock import Mock, MagicMock, AsyncMock, patch
    from sqlalchemy.orm import Session

    from core.agent_graduation_service import AgentGraduationService, SandboxExecutor
    from core.agent_promotion_service import AgentPromotionService
    from core.agent_evolution_loop import AgentEvolutionLoop
    from core.models import AgentRegistry, AgentStatus, Episode, EpisodeSegment

    # Fixtures
    @pytest.fixture
    def mock_db():
        """Mock database session."""
        return Mock(spec=Session)

    @pytest.fixture
    def sample_agent():
        """Sample agent for testing."""
        agent = Mock(spec=AgentRegistry)
        agent.id = "test-agent-001"
        agent.status = AgentStatus.INTERN
        agent.maturity_level = "INTERN"
        return agent
    ```

    **Test classes to implement:**

    1. **TestAgentGraduationErrorPaths** (25+ tests):
       - None/empty agent_id handling
       - Agent not found scenarios
       - Zero episode count handling
       - Invalid maturity level strings
       - Missing episode segments
       - Negative intervention counts
       - Division by zero in rate calculations
       - Malformed episode data
       - Concurrent graduation attempts
       - Database query failures
       - LanceDB unavailability
       - Constitutional score boundary conditions (0.0, 1.0, >1.0, <0.0)
       - Empty constitutional violations list
       - Invalid target maturity (non-existent levels)
       - Graduation criteria missing for level
       - Exam execution failures
       - Sandbox timeout scenarios

    2. **TestAgentPromotionErrorPaths** (20+ tests):
       - Promotion without graduation exam
       - Invalid status transitions (INTERN → AUTONOMOUS skipping SUPERVISED)
       - Demotion scenarios
       - Promotion during active execution
       - Missing audit trail entries
       - Concurrent promotion attempts
       - Rollback on promotion failure
       - History preservation
       - Permission denied for non-admin users
       - Agent already at target maturity
       - Status string to enum conversion errors
       - Database constraint violations

    3. **TestAgentEvolutionErrorPaths** (15+ tests):
       - Evolution loop interruption
       - Learning rate boundary conditions
       - Stagnation detection (no improvement)
       - Negative fitness scores
       - Missing evolution parameters
       - Evolution cycle timeout
       - Infinite loop prevention
       - Resource exhaustion during evolution
       - Conflicting evolution strategies
       - Evolution data corruption

    **Use VALIDATED_BUG pattern for any discovered bugs:**
    ```python
    def test_graduation_with_zero_episode_count(self):
        """
        VALIDATED_BUG: Graduation crashes with zero episodes

        Expected:
            - Should return passed=False with "insufficient_episode_count" violation
            - Graceful handling without crash

        Actual:
            - [Document actual behavior if buggy]

        Severity: HIGH
        Impact:
            - Agents with no episodes cause crash instead of graceful rejection
            - New agents cannot be checked for graduation readiness

        Fix:
            - Add early return when episode_count == 0 with appropriate error message

        Validated: [Test result]
        """
        # Test implementation
    ```
  </action>
  <verify>
    Run tests to verify they execute correctly:
    ```bash
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/error_paths/test_agent_lifecycle_error_paths.py -v --tb=short --override-ini="addopts="
    ```
    Expected: 60+ tests created, all pass or fail with documented VALIDATED_BUG issues
  </verify>
  <done>
    - test_agent_lifecycle_error_paths.py created with 800+ lines
    - 60+ tests across 3 test classes (Graduation, Promotion, Evolution)
    - All tests use VALIDATED_BUG pattern for discovered issues
    - Coverage measured on agent lifecycle services (target: 75%+)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create workflow error path tests</name>
  <files>backend/tests/error_paths/test_workflow_error_paths.py</files>
  <action>
    Create new test file backend/tests/error_paths/test_workflow_error_paths.py with comprehensive error path coverage for workflow services.

    **Test classes to implement:**

    1. **TestWorkflowOptimizerErrorPaths** (25+ tests):
       - None/empty workflow definitions
       - Circular workflow dependencies (A → B → A)
       - Missing required workflow parameters
       - Invalid optimization strategies
       - Optimization timeout scenarios
       - Resource constraint violations
       - Negative cost/efficiency scores
       - Conflicting optimization goals
       - Workflow step failures during optimization
       - Empty workflow node list
       - Disconnected workflow graphs
       - Maximum depth exceeded
       - Optimization with missing LLM provider
       - Concurrent optimization attempts
       - Optimization state corruption

    2. **TestAdvancedWorkflowErrorPaths** (20+ tests):
       - Workflow execution with missing steps
       - Step timeout handling
       - Step failure rollback scenarios
       - Invalid workflow state transitions
       - Concurrent workflow execution
       - Workflow cancellation during execution
       - Missing input data for steps
       - Output schema validation failures
       - Workflow version conflicts
       - Orphaned workflow execution records
       - Workflow execution history overflow
       - Persistent execution state corruption
       - Workflow recovery after crash

    3. **TestWorkflowValidationErrorPaths** (15+ tests):
       - Invalid workflow JSON schemas
       - Malformed workflow definitions
       - Missing required fields in workflow JSON
       - Type validation failures (string vs int)
       - Enum validation failures (invalid maturity levels)
       - Range validation failures (negative counts, >max values)
       - Reference validation failures (referenced non-existent steps)
       - Circular reference detection
       - Duplicate step IDs
       - Empty step names
       - Unicode/special characters in step names
       - Excessively long workflow names (>255 chars)

    **Import structure:**
    ```python
    from core.ai_workflow_optimizer import AIWorkflowOptimizer, OptimizationStrategy
    from core.advanced_workflow_system import AdvancedWorkflowSystem, WorkflowExecution
    from core.workflow_analytics_engine import WorkflowAnalyticsEngine
    ```

    **Example test pattern:**
    ```python
    def test_workflow_optimizer_with_circular_dependencies(self):
        """
        VALIDATED_BUG: Circular workflow dependencies not detected

        Expected:
            - Should detect A → B → A circular dependency
            - Should raise ValueError with descriptive error

        Actual:
            - [Document actual behavior]

        Severity: HIGH
        Impact:
            - Infinite loops during workflow optimization
            - System hangs when processing circular workflows

        Fix:
            - Implement topological sort with cycle detection
            - Raise ValueError before processing

        Validated: [Test result]
        """
        optimizer = AIWorkflowOptimizer(mock_db)
        workflow = {
            "steps": [
                {"id": "A", "depends_on": ["B"]},
                {"id": "B", "depends_on": ["A"]}
            ]
        }
        # Test implementation
    ```
  </action>
  <verify>
    Run tests to verify they execute correctly:
    ```bash
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/error_paths/test_workflow_error_paths.py -v --tb=short --override-ini="addopts="
    ```
    Expected: 60+ tests created, all pass or fail with documented VALIDATED_BUG issues
  </verify>
  <done>
    - test_workflow_error_paths.py created with 700+ lines
    - 60+ tests across 3 test classes (Optimizer, Advanced Workflows, Validation)
    - All tests use VALIDATED_BUG pattern for discovered issues
    - Coverage measured on workflow services (target: 75%+)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create API boundary condition tests</name>
  <files>backend/tests/boundary_conditions/test_api_boundary_conditions.py</files>
  <action>
    Create new test file backend/tests/boundary_conditions/test_api_boundary_conditions.py with comprehensive boundary condition tests for API routes.

    **Test classes to implement:**

    1. **TestPaginationBoundaries** (20+ tests):
       - Zero page number (should default to 1 or raise error)
       - Negative page number
       - Zero page size
       - Negative page size
       - Excessive page size (>1000, >10000)
       - Page beyond available data
       - Pagination with empty result set
       - Pagination offset overflow
       - Pagination with None values
       - Cursor-based pagination edge cases
       - Pagination with deleted/moved items
       - Concurrent pagination requests

    2. **TestRateLimitBoundaries** (15+ tests):
       - Rate limit at exact threshold (boundary hit)
       - Rate limit one below threshold
       - Rate limit one above threshold
       - Rate limit window rollover
       - Concurrent requests at limit boundary
       - Rate limit with multiple clients
       - Rate limit reset timing
       - Rate limit with burst traffic
       - Rate limit exemption (admin users)
       - Rate limit headers (X-RateLimit-Remaining, etc.)

    3. **TestValidationBoundaries** (20+ tests):
       - String length boundaries (empty, 1 char, max, max+1, 10000 chars)
       - Numeric boundaries (0, -1, max int, min int, float inf, NaN)
       - Date/time boundaries (epoch, far future, far past, DST transitions)
       - Enum validation (valid, invalid, case variations)
       - UUID validation (valid, invalid format, None, empty)
       - Email validation (valid formats, invalid formats, boundary cases)
       - Array/list boundaries (empty, single item, max items, max+1)
       - Object nesting depth limits
       - Special characters and unicode handling
       - SQL injection patterns in input
       - XSS patterns in input
       - Path traversal patterns in input

    **Import structure:**
    ```python
    from fastapi.testclient import TestClient
    from unittest.mock import patch, Mock

    # Import the main FastAPI app
    from main import app

    @pytest.fixture
    def client():
        """Test client for API requests."""
        return TestClient(app)
    ```

    **Example test pattern:**
    ```python
    def test_pagination_with_negative_page_number(self, client):
        """
        VALIDATED_BUG: API accepts negative page numbers

        Expected:
            - Should return 400 Bad Request
            - Error message should indicate invalid page number

        Actual:
            - [Document actual behavior]

        Severity: MEDIUM
        Impact:
            - Confusing pagination results
            - Potential array index errors in backend

        Fix:
            - Add validation: page >= 1
            - Return 400 with error message for invalid values

        Validated: [Test result]
        """
        response = client.get("/api/v1/agents?page=-1")
        # Test implementation
    ```
  </action>
  <verify>
    Run tests to verify they execute correctly:
    ```bash
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/boundary_conditions/test_api_boundary_conditions.py -v --tb=short --override-ini="addopts="
    ```
    Expected: 55+ tests created, all pass or fail with documented VALIDATED_BUG issues
  </verify>
  <done>
    - test_api_boundary_conditions.py created with 600+ lines
    - 55+ tests across 3 test classes (Pagination, Rate Limits, Validation)
    - All tests use VALIDATED_BUG pattern for discovered issues
    - Coverage measured on API route validation (target: 75%+)
  </done>
</task>

<task type="auto">
  <name>Task 4: Measure coverage and create summary</name>
  <files>backend/tests/error_paths/test_agent_lifecycle_error_paths.py backend/tests/error_paths/test_workflow_error_paths.py backend/tests/boundary_conditions/test_api_boundary_conditions.py</files>
  <action>
    Measure coverage for all services tested in this plan and create comprehensive summary.

    **Coverage measurement:**
    ```bash
    cd /Users/rushiparikh/projects/atom/backend

    # Agent lifecycle coverage
    pytest tests/error_paths/test_agent_lifecycle_error_paths.py --cov=core.agent_graduation_service --cov=core.agent_promotion_service --cov=core.agent_evolution_loop --cov-report=term-missing --cov-report=json > coverage_agent_lifecycle.txt 2>&1

    # Workflow services coverage
    pytest tests/error_paths/test_workflow_error_paths.py --cov=core.ai_workflow_optimizer --cov=core.advanced_workflow_system --cov-report=term-missing --cov-report=json > coverage_workflows.txt 2>&1

    # Overall error path coverage
    pytest tests/error_paths/test_agent_lifecycle_error_paths.py tests/error_paths/test_workflow_error_paths.py tests/boundary_conditions/test_api_boundary_conditions.py --cov-report=term-missing --cov-report=json > coverage_phase186.txt 2>&1
    ```

    **Create coverage report with:**
    1. Line coverage percentage for each service
    2. Total tests created (target: 175+ tests)
    3. VALIDATED_BUG count by severity
    4. Uncovered lines with rationale
    5. Comparison to 75% target

    **Create 186-01-SUMMARY.md with:**
    - Tests created count by service (agent lifecycle, workflows, API)
    - Coverage achieved vs target (75%+)
    - VALIDATED_BUG findings summary
    - Key error patterns discovered
    - Uncovered lines and rationale
    - Integration with existing error_paths tests
  </action>
  <verify>
    Check coverage results meet 75%+ target:
    ```bash
    # Parse coverage results
    python -c "import json; data = json.load(open('coverage.json')); print(f\"Overall coverage: {data['totals']['percent_covered']:.2f}%\")"
    ```
    Expected: 75%+ coverage on all tested services, 175+ total tests
  </verify>
  <done>
    - Coverage measured for all 6 services (3 agent lifecycle, 2 workflow, API routes)
    - 75%+ target achieved or gap documented
    - 186-01-SUMMARY.md created with coverage results and VALIDATED_BUG findings
    - All tests integrated into existing test suite structure
  </done>
</task>

</tasks>

<verification>
Overall phase verification:

1. **Test file existence:**
   ```bash
   ls -la backend/tests/error_paths/test_agent_lifecycle_error_paths.py
   ls -la backend/tests/error_paths/test_workflow_error_paths.py
   ls -la backend/tests/boundary_conditions/test_api_boundary_conditions.py
   ```
   Expected: All 3 test files exist

2. **Test execution:**
   ```bash
   PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/error_paths/test_agent_lifecycle_error_paths.py backend/tests/error_paths/test_workflow_error_paths.py backend/tests/boundary_conditions/test_api_boundary_conditions.py -v --override-ini="addopts=" -q
   ```
   Expected: 175+ tests execute (pass or fail with documented bugs)

3. **Coverage measurement:**
   ```bash
   PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/error_paths/test_agent_lifecycle_error_paths.py backend/tests/error_paths/test_workflow_error_paths.py backend/tests/boundary_conditions/test_api_boundary_conditions.py --cov=core.agent_graduation_service --cov=core.agent_promotion_service --cov=core.agent_evolution_loop --cov=core.ai_workflow_optimizer --cov=core.advanced_workflow_system --cov-report=term-missing --override-ini="addopts="
   ```
   Expected: 75%+ line coverage on all 5 core services

4. **VALIDATED_BUG documentation:**
   ```bash
   grep -c "VALIDATED_BUG" backend/tests/error_paths/test_agent_lifecycle_error_paths.py
   grep -c "VALIDATED_BUG" backend/tests/error_paths/test_workflow_error_paths.py
   grep -c "VALIDATED_BUG" backend/tests/boundary_conditions/test_api_boundary_conditions.py
   ```
   Expected: VALIDATED_BUG pattern used for discovered issues

5. **Code quality:**
   - All tests follow Phase 104 error path patterns (VALIDATED_BUG docstrings, pytest.raises(), descriptive names)
   - Fixtures used for common setup (mock_db, sample_agent, client)
   - Test isolation (each test independent, no shared state)
</verification>

<success_criteria>
Phase 186 Plan 01 is complete when:
1. All 3 test files created (agent_lifecycle, workflow, api_boundary_conditions)
2. 175+ tests created across 9 test classes
3. 75%+ line coverage achieved on agent lifecycle services (graduation, promotion, evolution)
4. 75%+ line coverage achieved on workflow services (optimizer, advanced workflows)
5. 75%+ line coverage achieved on API boundary condition handling
6. All discovered bugs documented with VALIDATED_BUG pattern
7. Coverage report and summary created (186-01-SUMMARY.md)
8. All tests integrated into existing test suite structure
</success_criteria>

<output>
After completion, create `.planning/phases/186-edge-cases-error-handling/186-01-SUMMARY.md` with:
- Tests created count by service
- Coverage achieved vs 75% target
- VALIDATED_BUG findings by severity
- Key error patterns discovered
- Integration with existing error_paths tests
</output>
