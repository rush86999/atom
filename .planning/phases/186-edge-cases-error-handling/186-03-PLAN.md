---
phase: 186-edge-cases-error-handling
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/error_paths/test_skill_execution_error_paths.py
  - backend/tests/boundary_conditions/test_integration_boundaries.py
autonomous: true

must_haves:
  truths:
    - "Error handling paths for skill execution services achieve 75%+ line coverage"
    - "Boundary conditions for external integrations achieve 75%+ line coverage"
    - "All error scenarios use VALIDATED_BUG pattern with severity classification"
  artifacts:
    - path: "backend/tests/error_paths/test_skill_execution_error_paths.py"
      provides: "Skill execution error path tests (adapter, composition, marketplace)"
      min_lines: 700
      exports: ["TestSkillAdapterErrorPaths", "TestSkillCompositionErrorPaths", "TestSkillMarketplaceErrorPaths"]
    - path: "backend/tests/boundary_conditions/test_integration_boundaries.py"
      provides: "Integration boundary condition tests (OAuth, webhooks, external APIs)"
      min_lines: 600
      exports: ["TestOAuthBoundaries", "TestWebhookBoundaries", "TestExternalAPIBoundaries"]
  key_links:
    - from: "backend/tests/error_paths/test_skill_execution_error_paths.py"
      to: "backend/core/skill_adapter.py"
      via: "Direct import and mock testing"
      pattern: "from core.skill_adapter import SkillAdapter"
    - from: "backend/tests/boundary_conditions/test_integration_boundaries.py"
      to: "backend/api"
      via: "FastAPI TestClient"
      pattern: "client.post('/api/v1/integrations/')"

---

<objective>
Create comprehensive error path tests for skill execution services and boundary condition tests for external integrations to achieve 75%+ line coverage.

Purpose: Skill execution involves complex dynamic loading, composition, and marketplace interactions that require robust error handling. External integrations (OAuth, webhooks, APIs) have critical boundary conditions around timeouts, state transitions, and malformed data.

Output: Two new test files with 1300+ lines covering error paths and boundary conditions with VALIDATED_BUG pattern.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/tests/error_paths/ERROR_PATH_DOCUMENTATION.md
@backend/core/skill_adapter.py
@backend/core/skill_composition_engine.py
@backend/core/skill_marketplace_service.py

# Related Phase Coverage
- Phase 183: Skill Execution (coverage planned but error paths not tested)
- Integration testing from Phase 170, 184
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skill execution error path tests</name>
  <files>backend/tests/error_paths/test_skill_execution_error_paths.py</files>
  <action>
    Create test file with error path coverage for skill execution services.

    **Test classes:**

    1. **TestSkillAdapterErrorPaths** (20+ tests):
       - Load skill with None/empty skill_id
       - Load non-existent skill
       - Load skill with malformed SKILL.md
       - Load skill with import errors
       - Load skill with missing dependencies
       - Execute skill with invalid parameters
       - Execute skill with timeout
       - Execute skill with unhandled exceptions
       - Skill reload during execution
       - Concurrent skill execution
       - Skill with malicious code (sandbox escape)

    2. **TestSkillCompositionErrorPaths** (15+ tests):
       - Compose with None workflow definition
       - Compose with circular dependencies
       - Compose with missing skills
       - Compose with disconnected graph
       - Execute composed workflow with step failure
       - Rollback on workflow failure
       - Concurrent workflow execution
       - Workflow state corruption
       - Maximum depth exceeded
       - Invalid skill output types

    3. **TestSkillMarketplaceErrorPaths** (15+ tests):
       - Search with None/empty query
       - Search with SQL injection patterns
       - Install from marketplace with invalid URL
       - Install with corrupted skill package
       - Install with security scan failure
       - Version conflict resolution
       - License validation failures
       - Marketplace API timeout
       - Malformed marketplace response
       - Concurrent marketplace requests

    **Example test:**
    ```python
    def test_skill_adapter_with_malformed_skill_md(self):
        """
        VALIDATED_BUG: Malformed SKILL.md crashes adapter

        Expected:
            - Should return validation error
            - Should indicate parsing failure

        Actual:
            - [Document actual behavior]

        Severity: MEDIUM
        Impact:
            - Attacker can crash skill loading with malicious SKILL.md
            - No graceful degradation for parse errors

        Fix:
            - Add lenient SKILL.md parsing with error recovery
            - Return validation error with specific failure reason

        Validated: [Test result]
        """
        adapter = SkillAdapter(mock_db)
        # Test implementation
    ```
  </action>
  <verify>
    Run tests:
    ```bash
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/error_paths/test_skill_execution_error_paths.py -v --tb=short --override-ini="addopts="
    ```
    Expected: 50+ tests created
  </verify>
  <done>
    - test_skill_execution_error_paths.py created with 700+ lines
    - 50+ tests across 3 test classes
    - 75%+ coverage on skill services
  </done>
</task>

<task type="auto">
  <name>Task 2: Create integration boundary condition tests</name>
  <files>backend/tests/boundary_conditions/test_integration_boundaries.py</files>
  <action>
    Create test file with boundary condition tests for external integrations.

    **Test classes:**

    1. **TestOAuthBoundaries** (15+ tests):
       - OAuth with expired tokens
       - OAuth with revoked tokens
       - OAuth with invalid state parameter
       - OAuth with CSRF token mismatch
       - OAuth timeout during handshake
       - OAuth with malformed callback URL
       - OAuth with missing required scopes
       - Concurrent OAuth requests
       - Token refresh boundary conditions

    2. **TestWebhookBoundaries** (15+ tests):
       - Webhook with None/empty payload
       - Webhook with malformed JSON
       - Webhook with missing signature
       - Webhook with invalid signature
       - Webhook with replayed timestamp
       - Webhook timeout handling
       - Concurrent webhook delivery
       - Webhook with oversized payload
       - Webhook with special characters

    3. **TestExternalAPIBoundaries** (15+ tests):
       - API call with timeout at exact boundary
       - API call with rate limit at boundary
       - API call with negative retry count
       - API call with excessive retry count
       - API call with None callback URL
       - API call with malformed response
       - API call with 5xx error handling
       - API call with 4xx error handling
       - API call with chunked transfer encoding

    **Example test:**
    ```python
    def test_webhook_with_invalid_signature(self, client):
        """
        VALIDATED_BUG: Invalid webhook signature accepted

        Expected:
            - Should return 401 Unauthorized
            - Should log security event

        Actual:
            - [Document actual behavior]

        Severity: HIGH
        Impact:
            - Security vulnerability allowing forged webhook requests
            - Potential data injection attacks

        Fix:
            - Verify HMAC signature for all webhook requests
            - Return 401 for invalid signatures

        Validated: [Test result]
        """
        response = client.post("/api/v1/webhooks/github",
            json={"action": "opened"},
            headers={"X-Hub-Signature-256": "invalid"}
        )
        # Test implementation
    ```
  </action>
  <verify>
    Run tests:
    ```bash
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/boundary_conditions/test_integration_boundaries.py -v --tb=short --override-ini="addopts="
    ```
    Expected: 45+ tests created
  </verify>
  <done>
    - test_integration_boundaries.py created with 600+ lines
    - 45+ tests across 3 test classes
    - 75%+ coverage on integration code
  </done>
</task>

<task type="auto">
  <name>Task 3: Measure coverage and create summary</name>
  <files>backend/tests/error_paths/test_skill_execution_error_paths.py backend/tests/boundary_conditions/test_integration_boundaries.py</files>
  <action>
    Measure coverage and create 186-03-SUMMARY.md.
  </action>
  <verify>
    Check 75%+ coverage achieved
  </verify>
  <done>
    - Coverage measured and documented
    - 186-03-SUMMARY.md created
  </done>
</task>

</tasks>

<verification>
1. Test file existence
2. Test execution (95+ tests)
3. Coverage measurement (75%+ target)
4. VALIDATED_BUG documentation
</verification>

<success_criteria>
1. Both test files created
2. 95+ tests created
3. 75%+ line coverage
4. VALIDATED_BUG pattern used
5. 186-03-SUMMARY.md created
</success_criteria>

<output>
After completion, create `.planning/phases/186-edge-cases-error-handling/186-03-SUMMARY.md`
</output>
