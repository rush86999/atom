---
phase: 75-test-infrastructure-fixtures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/e2e_ui/conftest.py
  - backend/tests/e2e_ui/pyproject.toml
  - backend/tests/e2e_ui/playwright.config.ts
autonomous: true

must_haves:
  truths:
    - "Developer can run pytest tests/e2e_ui/ and Playwright browser launches correctly"
    - "Playwright configuration includes base URL, browsers (Chromium, Firefox, WebKit), timeout settings, and retries"
    - "Pytest configuration for E2E UI tests is separate from unit tests"
  artifacts:
    - path: "backend/tests/e2e_ui/conftest.py"
      provides: "Root E2E UI test configuration with fixtures"
      min_lines: 100
    - path: "backend/tests/e2e_ui/pyproject.toml"
      provides: "Pytest configuration for E2E UI tests"
      contains: "[tool.pytest.ini_options]"
    - path: "backend/tests/e2e_ui/playwright.config.ts"
      provides: "Playwright browser configuration"
      contains: "baseURL"
  key_links:
    - from: "backend/tests/e2e_ui/conftest.py"
      to: "frontend-nextjs"
      via: "base_url environment variable"
      pattern: "base_url|BASE_URL"
---

<objective>
Create E2E UI testing directory structure with Playwright and pytest configuration files.

Purpose: Establish the foundational configuration for Playwright-based UI testing with proper test discovery, browser settings, and pytest integration.

Output: E2E UI test directory with conftest.py, pyproject.toml, and playwright.config.ts
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/TESTING.md
@.planning/codebase/STACK.md
</execution_context>

<context>
# Existing E2E Test Infrastructure
@backend/tests/e2e/conftest.py - Has extensive E2E fixture patterns for backend testing
@backend/pytest.ini - Has pytest configuration for backend tests

# Playwright Installation
@backend/requirements.txt - playwright==1.40.0 already installed
@backend/requirements.txt - pytest-playwright==0.3.3 already installed
@backend/requirements.txt - pytest-base-url==2.0.0 already installed

# Frontend Target
@frontend-nextjs/ - Next.js frontend application
@frontend-nextjs/package.json - Contains dev/build scripts
</context>

<tasks>

<task type="auto">
  <name>Create E2E UI test directory and configuration files</name>
  <files>backend/tests/e2e_ui/conftest.py, backend/tests/e2e_ui/pyproject.toml, backend/tests/e2e_ui/playwright.config.ts, backend/tests/e2e_ui/__init__.py, backend/tests/e2e_ui/README.md</files>
  <action>
Create new E2E UI test directory structure:

1. Create directory: `backend/tests/e2e_ui/`

2. Create `backend/tests/e2e_ui/__init__.py`:
```python
"""E2E UI tests for Atom frontend using Playwright."""
```

3. Create `backend/tests/e2e_ui/conftest.py` with:
   - Import pytest and pytest_playwright
   - Define `base_url` fixture that reads from environment or defaults to http://localhost:3000
   - Define `browser_context_args` fixture for consistent browser context
   - Define `page` fixture that extends base page with custom helpers
   - Define `screenshot_on_failure` fixture for debugging
   - Configure pytest markers for UI tests (@pytest.mark.ui, @pytest.mark.auth, @pytest.mark.canvas, etc.)
   - Set up `worker_id` fixture for parallel execution (reads PYTEST_XDIST_WORKER_ID)
   - Define `unique_test_id` fixture combining worker_id + uuid4 for collision-free data
   - Set test timeouts (default 30s, slow tests 60s)
   - Configure retries for flaky tests (2 retries)

4. Create `backend/tests/e2e_ui/pyproject.toml` with:
   - [tool.pytest.ini_options] section
   - testpaths = ["tests/e2e_ui"]
   - python_files = "test_*.py"
   - markers section: ui, auth, slow, canvas, agent, skill
   - addopts with --base-url=http://localhost:3000, --browser-channel=chrome, --output=tests/e2e_ui/test-results
   - async_mode = "auto"

5. Create `backend/tests/e2e_ui/playwright.config.ts`:
   - Use TypeScript (create JS config if TS not available in test env)
   - Set baseURL from process.env.BASE_URL or http://localhost:3000
   - Configure browsers: chromium, firefox, webkit (for local testing)
   - Set timeout: 30000 (30s default)
   - Set retries: 2 for CI, 0 for local
   - Configure screenshot: "only-on-failure"
   - Configure video: "retain-on-failure" with dir: test-results/videos
   - Set use.viewport: { width: 1280, height: 720 }
   - Set trace: "on-first-retry" with dir: test-results/trace

6. Create `backend/tests/e2e_ui/README.md` with:
   - Purpose: E2E UI testing for Atom frontend
   - Prerequisites: Docker Compose, frontend running
   - Quick start commands
   - Directory structure explanation
   - Fixture documentation
   - Troubleshooting section

IMPORTANT: Do NOT modify existing backend/tests/conftest.py or backend/pytest.ini - create separate configuration for E2E UI tests to avoid conflicts.
  </action>
  <verify>
Run: `ls -la backend/tests/e2e_ui/`
Expected: conftest.py, pyproject.toml, playwright.config.ts, __init__.py, README.md all exist

Run: `grep -q "base_url" backend/tests/e2e_ui/conftest.py && echo "base_url fixture found"`
Expected: "base_url fixture found"

Run: `grep -q "testpaths.*e2e_ui" backend/tests/e2e_ui/pyproject.toml && echo "pyproject.toml configured"`
Expected: "pyproject.toml configured"
  </verify>
  <done>
E2E UI test directory created with conftest.py providing base_url fixture, pyproject.toml with pytest configuration, and playwright.config.ts with browser settings. README.md documents usage.
</done>
</task>

</tasks>

<verification>
# Phase Verification

1. Directory exists: `backend/tests/e2e_ui/`
2. Configuration files present: conftest.py, pyproject.toml, playwright.config.ts
3. base_url fixture defaults to http://localhost:3000
4. Playwright browsers configured (Chromium, Firefox, WebKit)
5. Timeouts configured (30s default, 60s slow)
6. Retries configured (2 retries)
7. README.md explains usage

Run: `PYTHONPATH=backend pytest backend/tests/e2e_ui/ --collect-only`
Expected: Tests collected (0 initially, but no errors about missing fixtures)

Run: `grep -E "(baseURL|timeout|retries)" backend/tests/e2e_ui/playwright.config.ts`
Expected: All three settings present
</verification>

<success_criteria>
1. Developer can run `pytest tests/e2e_ui/` and pytest loads configuration without errors
2. Playwright configuration includes base URL (http://localhost:3000), browsers (Chromium, Firefox, WebKit), timeout (30s), and retries (2)
3. conftest.py provides base_url, page, browser_context_args fixtures for use in tests
4. README.md documents the E2E UI test infrastructure
5. E2E UI test configuration is separate from backend unit tests (no conflicts)
</success_criteria>

<output>
After completion, create `.planning/phases/75-test-infrastructure-fixtures/75-01-SUMMARY.md` with:
- Configuration files created
- Fixture documentation
- Any deviations from plan
- Next steps (Plan 02: Test Fixtures and Page Objects)
</output>
