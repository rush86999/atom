---
phase: 75-test-infrastructure-fixtures
plan: 02
type: execute
wave: 1
depends_on: [75-01]
files_modified:
  - backend/tests/e2e_ui/fixtures/auth_fixtures.py
  - backend/tests/e2e_ui/fixtures/page_objects.py
  - backend/tests/e2e_ui/fixtures/__init__.py
autonomous: true

must_haves:
  truths:
    - "Tests can use authenticated_browser fixture to get pre-authenticated browser context"
    - "Page Object classes provide clean abstractions for UI interactions (LoginPage, HomePage, ChatPage)"
    - "Authentication fixtures use API-first setup (bypassing UI for faster test initialization)"
  artifacts:
    - path: "backend/tests/e2e_ui/fixtures/auth_fixtures.py"
      provides: "Authentication fixtures for E2E UI tests"
      min_lines: 80
      exports: ["authenticated_browser", "test_user_token", "create_test_user_via_api"]
    - path: "backend/tests/e2e_ui/fixtures/page_objects.py"
      provides: "Page Object Model classes for UI abstractions"
      min_lines: 150
      exports: ["PageObject", "LoginPage", "HomePage", "ChatPage"]
  key_links:
    - from: "backend/tests/e2e_ui/fixtures/auth_fixtures.py"
      to: "backend/core/models.py"
      via: "User model creation"
      pattern: "User\("
    - from: "backend/tests/e2e_ui/fixtures/page_objects.py"
      to: "frontend-nextjs"
      via: "CSS selectors and data-testid attributes"
      pattern: "page\.locator|data-testid"
---

<objective>
Create authentication fixtures and Page Object classes for E2E UI tests.

Purpose: Provide reusable fixtures for authenticated browser contexts and Page Object abstractions for cleaner, more maintainable UI tests. API-first authentication bypasses slow UI login flows.

Output: Fixtures module with auth_fixtures.py and page_objects.py
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/TESTING.md
@.planning/phases/75-test-infrastructure-fixtures/75-01-PLAN.md
</execution_context>

<context>
# Existing Test Fixtures Pattern
@backend/tests/e2e/conftest.py - Has test_user_token fixture pattern
@backend/tests/e2e/conftest.py - Has auth_headers fixture pattern
@backend/tests/fixtures/ - Existing fixture modules for reference

# Authentication System
@backend/core/models.py - User model with JWT tokens
@backend/api/ - Authentication endpoints (login, token refresh)

# Frontend Structure
@frontend-nextjs/app/ - Next.js app router structure
@frontend-nextjs/components/ - UI components
</context>

<tasks>

<task type="auto">
  <name>Create authentication fixtures with API-first setup</name>
  <files>backend/tests/e2e_ui/fixtures/auth_fixtures.py, backend/tests/e2e_ui/fixtures/__init__.py</files>
  <action>
Create `backend/tests/e2e_ui/fixtures/` directory and add authentication fixtures:

1. Create `backend/tests/e2e_ui/fixtures/__init__.py`:
```python
"""E2E UI test fixtures."""
from .auth_fixtures import *  # noqa: F401, F403
from .page_objects import *  # noqa: F401, F403
```

2. Create `backend/tests/e2e_ui/fixtures/auth_fixtures.py` with:

Fixture: `test_user(db_session)` - Function-scoped
- Creates User model instance with unique email (using unique_test_id)
- Sets password with bcrypt (use passlib.hash.bcrypt)
- Returns user object

Fixture: `test_user_token(test_user, db_session)` - Function-scoped
- Creates JWT token for test_user
- Uses backend token generation (python-jose)
- Sets 24h expiry
- Returns encoded JWT string

Fixture: `create_test_user_via_api(async_client, unique_test_id)` - Function-scoped
- API-first user creation (bypasses UI)
- POST to /api/auth/register with test data
- Returns {user_id, email, token}
- Handles "user exists" errors gracefully

Fixture: `authenticated_browser(browser, test_user_token, base_url)` - Function-scoped
- Creates browser context with authentication stored
- Adds cookie: name="token", value=test_user_token, path="/", domain=localhost
- Sets localStorage: "authToken" -> test_user_token
- Returns context with authentication already set up
- Uses unique_test_id to avoid cookie conflicts

Fixture: `auth_headers(test_user_token)` - Function-scoped
- Returns {"Authorization": f"Bearer {test_user_token}"}
- For API requests in tests

Fixture: `unique_test_id(worker_id)` - Function-scoped (autouse for collision prevention)
- Combines worker_id from PYTEST_XDIST_WORKER_ID with uuid4
- Returns string like "gw0_abc12345"
- Used in email, username generation

IMPORTANT: Use bcrypt for password hashing. Use python-jose for JWT. API-first setup means calling FastAPI endpoints directly, not going through login form.
  </action>
  <verify>
Run: `grep -q "def authenticated_browser" backend/tests/e2e_ui/fixtures/auth_fixtures.py && echo "authenticated_browser fixture found"`
Expected: "authenticated_browser fixture found"

Run: `grep -q "def test_user_token" backend/tests/e2e_ui/fixtures/auth_fixtures.py && echo "test_user_token fixture found"`
Expected: "test_user_token fixture found"

Run: `grep -q "unique_test_id" backend/tests/e2e_ui/fixtures/auth_fixtures.py && echo "unique_test_id fixture found"`
Expected: "unique_test_id fixture found"
  </verify>
  <done>
Authentication fixtures module created with test_user, test_user_token, create_test_user_via_api, authenticated_browser, auth_headers, and unique_test_id fixtures. Fixtures use API-first approach for fast setup and UUID-based data for parallel execution safety.
</done>
</task>

<task type="auto">
  <name>Create Page Object Model classes for UI abstractions</name>
  <files>backend/tests/e2e_ui/fixtures/page_objects.py</files>
  <action>
Create `backend/tests/e2e_ui/fixtures/page_objects.py` with Page Object classes:

Base Class: `PageObject`
- Constructor: __init__(self, page: Page, base_url: str)
- Methods: goto(path), wait_for_selector(selector), wait_for_url(pattern), is_visible(selector), click(selector), fill(selector, value), get_text(selector), get_attribute(selector, attr)
- All locator methods use data-testid attributes (preferred over CSS/XPath)
- Raises clear error messages when elements not found

LoginPage(PageObject):
- Properties: email_input, password_input, submit_button, error_message
- Methods: login(email, password) -> bool, expect_error(message) -> None, is_loaded() -> bool
- Locators: data-testid="login-email", data-testid="login-password", data-testid="login-submit", data-testid="login-error"

HomePage(PageObject):
- Properties: navigation_bar, user_menu, logout_button, chat_link, skills_link
- Methods: navigate_to_chat() -> ChatPage, navigate_to_skills() -> SkillsPage, logout() -> LoginPage, is_authenticated() -> bool, get_user_name() -> str
- Locators: data-testid="nav-home", data-testid="nav-chat", data-testid="nav-skills", data-testid="user-menu", data-testid="logout-button"

ChatPage(PageObject):
- Properties: chat_input, send_button, message_list, agent_selector, canvas_area
- Methods: send_message(message) -> None, wait_for_response() -> dict, get_last_message() -> str, select_agent(agent_name) -> None, is_canvas_present() -> bool
- Locators: data-testid="chat-input", data-testid="send-button", data-testid="message-list", data-testid="agent-selector", data-testid="canvas-area"

CanvasPage(PageObject):
- Properties: canvas_container, component_list, form_submit_button, close_button
- Methods: get_canvas_type() -> str, get_component_count() -> int, submit_form(data) -> None, close() -> HomePage
- Locators: data-testid="canvas-container", data-testid="component-list", data-testid="form-submit", data-testid="canvas-close"

SkillsPage(PageObject):
- Properties: skills_list, install_button, search_input, category_filter
- Methods: search_skills(query) -> None, install_skill(skill_name) -> None, is_skill_installed(skill_name) -> bool
- Locators: data-testid="skills-list", data-testid="install-button", data-testid="skill-search"

IMPORTANT: All Page Objects use data-testid selectors. Add fallback logic if data-testid not found. Each page returns other Page Objects from navigation methods (e.g., home.navigate_to_chat() returns ChatPage).
  </action>
  <verify>
Run: `grep -q "class PageObject" backend/tests/e2e_ui/fixtures/page_objects.py && echo "PageObject base class found"`
Expected: "PageObject base class found"

Run: `grep -q "class LoginPage" backend/tests/e2e_ui/fixtures/page_objects.py && echo "LoginPage found"`
Expected: "LoginPage found"

Run: `grep -q "class ChatPage" backend/tests/e2e_ui/fixtures/page_objects.py && echo "ChatPage found"`
Expected: "ChatPage found"

Run: `grep -q "data-testid" backend/tests/e2e_ui/fixtures/page_objects.py | wc -l`
Expected: 10+ (at least 10 data-testid references)
  </verify>
  <done>
Page Object Model classes created with PageObject base class and page-specific classes (LoginPage, HomePage, ChatPage, CanvasPage, SkillsPage). All use data-testid selectors for resilience against UI changes.
</done>
</task>

</tasks>

<verification>
# Phase Verification

1. Directory `backend/tests/e2e_ui/fixtures/` exists
2. `auth_fixtures.py` has all required fixtures (authenticated_browser, test_user_token, unique_test_id)
3. `page_objects.py` has PageObject base and at least 4 page classes
4. All fixtures use data-testid selectors
5. API-first authentication implemented
6. Page objects return other page objects from navigation methods

Run: `python -c "from backend.tests.e2e_ui.fixtures import auth_fixtures, page_objects; print('Import successful')"`
Expected: "Import successful" (no import errors)
</verification>

<success_criteria>
1. Tests can use `authenticated_browser` fixture to get browser with auth already set up
2. Tests can use `LoginPage`, `HomePage`, `ChatPage` classes for clean UI interactions
3. Authentication uses API-first approach (not slow UI login)
4. unique_test_id prevents data collisions in parallel execution
5. Page objects use data-testid selectors (stable against UI changes)
</success_criteria>

<output>
After completion, create `.planning/phases/75-test-infrastructure-fixtures/75-02-SUMMARY.md` with:
- Fixtures created with documentation
- Page Object classes diagram
- Example test showing fixture usage
- Next steps (Plan 03: Test Data Factories)
</output>
