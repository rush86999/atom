---
phase: 75-test-infrastructure-fixtures
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/e2e_ui/fixtures/test_data_factory.py
autonomous: true

must_haves:
  truths:
    - "Tests can use factories to generate unique test data (users, agents, skills, projects)"
    - "Each factory uses UUID v4 to prevent parallel execution collisions"
    - "Factories provide realistic data for testing (not 'test1', 'test2' strings)"
  artifacts:
    - path: "backend/tests/e2e_ui/fixtures/test_data_factory.py"
      provides: "Test data factories for E2E UI tests"
      min_lines: 200
      exports: ["user_factory", "agent_factory", "skill_factory", "project_factory"]
  key_links:
    - from: "backend/tests/e2e_ui/fixtures/test_data_factory.py"
      to: "backend/core/models.py"
      via: "Model instantiation"
      pattern: "AgentRegistry|User|Project"
---

<objective>
Create test data factories for generating unique, realistic test data in E2E UI tests.

Purpose: Provide factory functions for creating test data with UUID-based uniqueness to prevent parallel execution collisions. Factories generate realistic data (not placeholder values) for better test simulation.

Output: test_data_factory.py with factory functions for all major entities
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/TESTING.md
@.planning/phases/75-test-infrastructure-fixtures/75-02-PLAN.md

@backend/tests/e2e/fixtures/test_data_factory.py - Reference existing factory patterns
</context>

<context>
# Existing Factory Pattern
@backend/tests/e2e/fixtures/test_data_factory.py - Has crm_contact_factory, task_factory patterns
@backend/tests/factories/ - Existing factory modules for reference
@backend/core/models.py - User, AgentRegistry, Project models

# Required Data Types
- Users: email, username, display_name, password
- Agents: name, category, maturity_level, capabilities
- Skills: name, description, category, permissions
- Projects: name, description, owner
- Episodes: title, summary, content, tags
- Canvas: type, components, layout
</context>

<tasks>

<task type="auto">
  <name>Create test data factories for E2E UI tests</name>
  <files>backend/tests/e2e_ui/fixtures/test_data_factory.py</files>
  <action>
Create `backend/tests/e2e_ui/fixtures/test_data_factory.py` with factory functions:

Factory: `user_factory(unique_test_id, **kwargs)` - Function
- Default values:
  - email: f"test.user.{unique_test_id}@example.com"
  - username: f"testuser_{unique_test_id}"
  - display_name: f"Test User {unique_test_id[:8]}"
  - password: "SecureTestPassword123!" (consistent for tests)
- kwargs can override any field
- Returns dict with all user fields

Factory: `agent_factory(unique_test_id, **kwargs)` - Function
- Default values:
  - name: f"Test Agent {unique_test_id[:8]}"
  - category: "testing"
  - maturity_level: "INTERN" (STUDENT, INTERN, SUPERVISED, AUTONOMOUS)
  - description: f"Test agent created by {unique_test_id}"
  - capabilities: ["markdown", "charts"]
  - confidence_score: 0.6 (varies by maturity)
- Returns dict with all agent fields

Factory: `skill_factory(unique_test_id, **kwargs)` - Function
- Default values:
  - name: f"test-skill-{unique_test_id[:8]}"
  - display_name: f"Test Skill {unique_test_id[:8]}"
  - description: f"A test skill for {unique_test_id}"
  - category: "productivity"
  - permissions: ["read:basic", "write:basic"]
  - version: "1.0.0"
- Returns dict with all skill fields

Factory: `project_factory(unique_test_id, **kwargs)` - Function
- Default values:
  - name: f"Test Project {unique_test_id[:8]}"
  - description: f"Test project description {unique_test_id}"
  - owner_id: f"user_{unique_test_id[:8]}"
- Returns dict with all project fields

Factory: `episode_factory(unique_test_id, **kwargs)` - Function
- Default values:
  - title: f"Test Episode {unique_test_id[:8]}"
  - summary: f"Test episode summary {unique_test_id}"
  - episode_type: "testing"
  - tags: ["e2e", "test", unique_test_id[:8]]
- Returns dict with all episode fields

Factory: `canvas_factory(unique_test_id, **kwargs)` - Function
- Default values:
  - canvas_id: f"canvas_{unique_test_id}"
  - type: "form"
  - title: f"Test Canvas {unique_test_id[:8]}"
  - components: [
      {"type": "text", "content": "Test content"},
      {"type": "button", "label": "Submit", "action": "submit"}
    ]
- Returns dict with all canvas fields

Factory: `chat_message_factory(unique_test_id, **kwargs)` - Function
- Default values:
  - content: f"Test message {unique_test_id}"
  - role: "user"
  - agent_id: f"agent_{unique_test_id[:8]}"
- Returns dict with all message fields

Helper: `random_email(unique_test_id)` - Function
- Returns unique email based on unique_test_id

Helper: `random_username(unique_test_id)` - Function
- Returns unique username based on unique_test_id

Helper: `random_password()` - Function
- Returns consistent test password "SecureTestPassword123!"

Helper: `random_project_name(unique_test_id)` - Function
- Returns unique project name

IMPORTANT: All factories must use unique_test_id (from worker_id + uuid4) to prevent collisions. Default values should be realistic, not placeholders like "test1" or "foo". Use Faker library if available for more realistic data (names, companies, etc.), otherwise use consistent patterns.
  </action>
  <verify>
Run: `grep -q "def user_factory" backend/tests/e2e_ui/fixtures/test_data_factory.py && echo "user_factory found"`
Expected: "user_factory found"

Run: `grep -q "def agent_factory" backend/tests/e2e_ui/fixtures/test_data_factory.py && echo "agent_factory found"`
Expected: "agent_factory found"

Run: `grep -q "unique_test_id" backend/tests/e2e_ui/fixtures/test_data_factory.py | wc -l`
Expected: 7+ (at least 7 uses of unique_test_id)

Run: `python -c "from backend.tests.e2e_ui.fixtures.test_data_factory import user_factory; result = user_factory('test_123'); print(result['email'])"`
Expected: Email contains "test_123" (e.g., "test.user.test_123@example.com")
  </verify>
  <done>
Test data factories module created with user_factory, agent_factory, skill_factory, project_factory, episode_factory, canvas_factory, and chat_message_factory. All use unique_test_id for parallel execution safety and generate realistic default values.
</done>
</task>

</tasks>

<verification>
# Phase Verification

1. `test_data_factory.py` exists in fixtures directory
2. All 7 factories defined (user, agent, skill, project, episode, canvas, chat_message)
3. All factories accept unique_test_id parameter
4. All factories accept **kwargs for overrides
5. Generated data includes unique_test_id in relevant fields
6. Import test passes without errors

Run: `python -c "
from backend.tests.e2e_ui.fixtures.test_data_factory import *
user = user_factory('abc123')
agent = agent_factory('abc123')
skill = skill_factory('abc123')
print('User:', user['email'])
print('Agent:', agent['name'])
print('Skill:', skill['name'])
print('All factories work!')
"`
Expected: No errors, output shows unique_test_id in data
</verification>

<success_criteria>
1. Tests can use user_factory('gw0_123') to get unique user data
2. Generated emails are unique (contain unique_test_id)
3. Factories provide realistic data, not placeholder strings
4. All factories support **kwargs overrides
5. Factories work in parallel execution (no collisions)
</success_criteria>

<output>
After completion, create `.planning/phases/75-test-infrastructure-fixtures/75-03-SUMMARY.md` with:
- Factories documentation with examples
- Factory usage patterns
- Sample test showing factory usage
- Next steps (Plan 04: Database Isolation)
</output>
