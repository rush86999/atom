---
phase: 75-test-infrastructure-fixtures
plan: 05
type: execute
wave: 2
depends_on: [75-04]
files_modified:
  - backend/tests/e2e_ui/fixtures/api_setup.py
autonomous: true

must_haves:
  truths:
    - "Tests can use API setup utilities to initialize state without slow UI interactions"
    - "API setup bypasses UI login, data entry, and navigation for 10-100x faster initialization"
    - "Setup utilities work with authenticated browser contexts"
  artifacts:
    - path: "backend/tests/e2e_ui/fixtures/api_setup.py"
      provides: "API-first setup utilities for fast test state initialization"
      min_lines: 180
      exports: ["api_create_user", "api_create_agent", "api_create_skill", "api_create_project", "api_login"]
  key_links:
    - from: "backend/tests/e2e_ui/fixtures/api_setup.py"
      to: "backend/api/"
      via: "FastAPI endpoint calls"
      pattern: "client\.post|client\.get"
    - from: "backend/tests/e2e_ui/fixtures/api_setup.py"
      to: "backend/tests/e2e_ui/fixtures/auth_fixtures.py"
      via: "test_user_token fixture usage"
      pattern: "test_user_token"
---

<objective>
Create API-first setup utilities for fast test state initialization.

Purpose: Bypass slow UI interactions (login, data entry) by using direct API calls for test setup. API setup is 10-100x faster than UI-based setup and more reliable.

Output: API setup utilities module with helper functions for common operations
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/TESTING.md
@.planning/phases/75-test-infrastructure-fixtures/75-04-PLAN.md

@backend/api/ - FastAPI routes to reference
@backend/tests/e2e/conftest.py - AsyncClient pattern reference
</context>

<context>
# API Endpoints Available
@backend/api/auth_routes.py - Authentication endpoints (register, login, token)
@backend/api/ - Agent, skill, project CRUD endpoints
@backend/core/models.py - Model definitions for request bodies

# HTTP Client Patterns
@backend/tests/e2e/conftest.py - Uses httpx.AsyncClient for API calls
@backend/requirements.txt - httpx>=0.24.0 for async HTTP

# Setup Utilities Needed
- User creation and authentication
- Agent creation
- Skill creation
- Project creation
- Canvas creation
- Episode creation
</context>

<tasks>

<task type="auto">
  <name>Create API-first setup utilities for test initialization</name>
  <files>backend/tests/e2e_ui/fixtures/api_setup.py</files>
  <action>
Create `backend/tests/e2e_ui/fixtures/api_setup.py` with API helper functions:

Function: `api_create_user(async_client, user_data)` - Async
- POST to /api/auth/register with user_data
- Handles 409 Conflict (user exists) by logging and returning existing user
- Returns: {user_id, email, token, status_code}
- Raises: AssertionError if 4xx/5xx (except 409)

Function: `api_login(async_client, email, password)` - Async
- POST to /api/auth/login with {email, password}
- Returns: {token, refresh_token, user_id, email}
- Stores token in async_client.headers for subsequent calls

Function: `api_create_agent(async_client, agent_data)` - Async
- POST to /api/agents with agent_data
- Requires authentication (token in headers)
- Returns: {agent_id, name, status, created_at}
- Auto-generates agent_id if not provided

Function: `api_create_skill(async_client, skill_data)` - Async
- POST to /api/skills with skill_data
- Requires authentication
- Returns: {skill_id, name, version, status}
- Auto-generates skill_id if not provided

Function: `api_create_project(async_client, project_data)` - Async
- POST to /api/projects with project_data
- Requires authentication
- Returns: {project_id, name, created_at}
- Links project to authenticated user

Function: `api_create_canvas(async_client, canvas_data)` - Async
- POST to /api/canvas with canvas_data
- Requires authentication
- Returns: {canvas_id, type, created_at}

Function: `api_create_episode(async_client, episode_data)` - Async
- POST to /api/episodes with episode_data
- Requires authentication
- Returns: {episode_id, title, created_at}

Function: `api_setup_authenticated_state(async_client, base_url)` - Async fixture-like function
- Creates user via api_create_user
- Logs in via api_login
- Creates default agent
- Creates default project
- Returns: {user, token, agent, project}

Fixture: `api_client(base_url)` - Function-scoped
- Creates httpx.AsyncClient with base_url
- Sets timeout: 30.0
- Yields client
- Closes client on teardown

Fixture: `authenticated_api_client(api_client, test_user)` - Function-scoped
- Wraps api_client with authentication
- Calls api_login with test_user credentials
- Adds Authorization header to api_client
- Yields authenticated client

Helper: `handle_api_error(response, context="")` - Function
- Extracts error message from response
- Raises AssertionError with details
- Used by all api_* functions

Helper: `assert_success(response, context="")` - Function
- Asserts 2xx status code
- Raises with details on failure
- Returns response.json()

IMPORTANT: All functions are async (use await). Handle 409 gracefully (resource already exists). Set timeouts to prevent hanging. Use httpx.AsyncClient, not requests (sync). Return full response data for test assertions.
  </action>
  <verify>
Run: `grep -q "async def api_create_user" backend/tests/e2e_ui/fixtures/api_setup.py && echo "api_create_user found"`
Expected: "api_create_user found"

Run: `grep -q "async def api_login" backend/tests/e2e_ui/fixtures/api_setup.py && echo "api_login found"`
Expected: "api_login found"

Run: `grep -q "httpx\.AsyncClient" backend/tests/e2e_ui/fixtures/api_setup.py && echo "AsyncClient pattern found"`
Expected: "AsyncClient pattern found"

Run: `grep -c "async def api_" backend/tests/e2e_ui/fixtures/api_setup.py`
Expected: 6+ (at least 6 api_create functions)
  </verify>
  <done>
API setup utilities created with async functions for user, agent, skill, project, canvas, and episode creation. api_client and authenticated_api_client fixtures provide HTTP client with authentication. All use httpx.AsyncClient for async operations.
</done>
</task>

</tasks>

<verification>
# Phase Verification

1. `api_setup.py` exists in fixtures directory
2. All 6 api_create functions defined (user, agent, skill, project, canvas, episode)
3. api_login function exists
4. api_client fixture exists
5. All functions are async (use await)
6. Error handling in place (handle_api_error, assert_success)

Run: `python -c "
import asyncio
from backend.tests.e2e_ui.fixtures.api_setup import assert_success, handle_api_error
class MockResponse:
    status_code = 200
    def json(self): return {'ok': True}
response = MockResponse()
result = assert_success(response, 'test context')
print('Result:', result)
"`
Expected: No errors, result is {'ok': True}
</verification>

<success_criteria>
1. Tests can call await api_create_user(client, user_data) to create users
2. API setup is 10-100x faster than UI-based setup
3. Authenticated client auto-includes Authorization header
4. Error handling provides clear failure messages
5. Functions handle edge cases (409 Conflict, timeouts)
</success_criteria>

<output>
After completion, create `.planning/phases/75-test-infrastructure-fixtures/75-05-SUMMARY.md` with:
- API setup functions documentation
- Usage examples with before/after timing comparison
- Error handling patterns
- Next steps (Plan 06: Docker Compose Environment)
</output>
