---
phase: 227-agent-system-standardization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/atom_agent_endpoints.py
  - backend/tests/e2e/test_scenario_02_streaming.py
autonomous: true

must_haves:
  truths:
    - "atom_agent_endpoints.py uses LLMService instead of BYOKHandler for WebSocket streaming"
    - "WebSocket streaming endpoint passes db session to LLMService for usage tracking"
    - "Provider selection uses LLMService methods (analyze_query_complexity, get_optimal_provider)"
    - "stream_completion method called on LLMService, not BYOKHandler"
    - "All test mocks updated to patch LLMService instead of BYOKHandler"
  artifacts:
    - path: "backend/core/atom_agent_endpoints.py"
      provides: "WebSocket streaming agent chat endpoint"
      contains: "from core.llm_service import LLMService"
      min_lines: 50
    - path: "backend/tests/e2e/test_scenario_02_streaming.py"
      provides: "WebSocket streaming tests"
      exports: ["test_streaming_agent_chat"]
  key_links:
    - from: "backend/core/atom_agent_endpoints.py"
      to: "backend/core/llm_service.py"
      via: "LLMService import and initialization"
      pattern: "llm_service = LLMService\\(workspace_id=ws_id\\)"
---

<objective>
Migrate atom_agent_endpoints.py WebSocket streaming endpoint from BYOKHandler to LLMService.

Purpose: Complete STD-03 (BYOKHandler Standardization for agent systems) by updating the remaining agent system file that still uses BYOKHandler directly.

Output: Migrated WebSocket streaming endpoint using LLMService with usage tracking, updated test mocks.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/225.1-agent-llmservice-migration/225.1-01-SUMMARY.md
@.planning/phases/225.1-agent-llmservice-migration/225.1-03-SUMMARY.md
@.planning/phases/225.1-agent-llmservice-migration/225.1-VERIFICATION.md
@.planning/phases/227-agent-system-standardization/227-RESEARCH.md
@backend/core/atom_agent_endpoints.py
@backend/core/llm_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace BYOKHandler import and instantiation with LLMService</name>
  <files>backend/core/atom_agent_endpoints.py</files>
  <action>
In the stream_chat_agent function (around line 1666):

1. Replace import:
   FROM: `from core.llm.byok_handler import BYOKHandler, QueryComplexity`
   TO:   `from core.llm_service import LLMService`

2. Replace instantiation (around line 1723):
   FROM: `byok_handler = BYOKHandler(workspace_id=ws_id, provider_id="auto")`
   TO:   `llm_service = LLMService(workspace_id=ws_id, db=get_db_session()())`

   Note: Pass db session for usage tracking. Create db session before LLMService init if not already available.

3. Replace all `byok_handler.` references with `llm_service.`:
   - Line 1769: `complexity = byok_handler.analyze_query_complexity(...)`
     → `complexity = llm_service.analyze_query_complexity(...)`
   - Line 1770: `provider_id, model = byok_handler.get_optimal_provider(...)`
     → `provider_id, model = llm_service.get_optimal_provider(...)`
   - Line 1815: `async for token in byok_handler.stream_completion(...)`
     → `async for token in llm_service.stream_completion(...)`

4. Remove the separate QueryComplexity import (not needed, LLMService handles it)

5. Update architecture comment if present to reflect LLMService usage
  </action>
  <verify>
grep -n "from core.llm.byok_handler import BYOKHandler" backend/core/atom_agent_endpoints.py returns nothing
grep -n "from core.llm_service import LLMService" backend/core/atom_agent_endpoints.py returns 1+ matches
grep -n "byok_handler" backend/core/atom_agent_endpoints.py returns nothing (or only in comments)
  </verify>
  <done>
atom_agent_endpoints.py imports and instantiates LLMService instead of BYOKHandler. All method calls updated to use llm_service instance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update streaming tests to patch LLMService instead of BYOKHandler</name>
  <files>backend/tests/e2e/test_scenario_02_streaming.py</files>
  <action>
Update test_scenario_02_streaming.py to patch LLMService:

1. Find all @patch decorators for BYOKHandler:
   - `@patch('core.atom_agent_endpoints.BYOKHandler')`

2. Replace with LLMService patches:
   - `@patch('core.atom_agent_endpoints.LLMService')`

3. Update mock return values if needed:
   - Method names unchanged for stream_completion, so no changes needed there
   - analyze_query_complexity and get_optimal_provider are on LLMService now

4. Verify parameter names in mock assertions match LLMService usage:
   - `llm_service` instead of `byok_handler` in parameter names

Reference: Phase 225.1 Plans 05-07 for test mock migration patterns.
  </action>
  <verify>
grep -n "@patch.*BYOKHandler" backend/tests/e2e/test_scenario_02_streaming.py returns nothing
grep -n "@patch.*LLMService" backend/tests/e2e/test_scenario_02_streaming.py returns 1+ matches
  </verify>
  <done>
All BYOKHandler test patches replaced with LLMService patches in streaming tests.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run WebSocket streaming tests to verify migration</name>
  <files>backend/tests/e2e/test_scenario_02_streaming.py</files>
  <action>
Run the streaming tests to verify the migration works:

1. Run streaming tests:
   `pytest backend/tests/e2e/test_scenario_02_streaming.py -v`

2. Verify no BYOKHandler AttributeError in test output

3. Verify WebSocket streaming flow works end-to-end:
   - LLMService initialization
   - Provider selection via analyze_query_complexity and get_optimal_provider
   - Token streaming via stream_completion

4. If tests fail due to missing db session, add it to LLMService init in atom_agent_endpoints.py
  </action>
  <verify>
pytest backend/tests/e2e/test_scenario_02_streaming.py passes with 0 BYOKHandler-related errors
Check for "AttributeError: 'LLMService' object has no attribute" in output - should be none
  </verify>
  <done>
WebSocket streaming tests pass with LLMService integration. No BYOKHandler AttributeError.
  </done>
</task>

</tasks>

<verification>
## Overall Verification

1. **Code Inspection**: grep confirms no BYOKHandler imports in atom_agent_endpoints.py (except comments)

2. **Import Verification**: LLMService import present, properly instantiated with workspace_id and db session

3. **Method Call Verification**: All LLM method calls use llm_service instance with correct method names:
   - analyze_query_complexity (not generate_response)
   - get_optimal_provider (not get_optimal_provider)
   - stream_completion (same name)

4. **Test Verification**: WebSocket streaming tests pass with LLMService mocks

5. **Usage Tracking**: db session passed to LLMService for usage tracking in database

## Success Criteria

- atom_agent_endpoints.py uses LLMService for all LLM interactions
- No BYOKHandler imports in agent system files (atom_agent_endpoints.py)
- WebSocket streaming tests pass
- No regression in agent chat functionality
</verification>

<success_criteria>
Phase 227 Plan 01 succeeds when:

1. atom_agent_endpoints.py imports LLMService instead of BYOKHandler
2. LLMService instantiated with workspace_id and db session
3. All method calls (analyze_query_complexity, get_optimal_provider, stream_completion) use llm_service
4. Test mocks updated to patch LLMService instead of BYOKHandler
5. WebSocket streaming tests pass (test_scenario_02_streaming.py)
6. No BYOKHandler AttributeError in test output
7. No regression in agent chat WebSocket streaming functionality
</success_criteria>

<output>
After completion, create `.planning/phases/227-agent-system-standardization/227-01-SUMMARY.md` with:
- Files modified (atom_agent_endpoints.py, test_scenario_02_streaming.py)
- Number of BYOKHandler references replaced
- Test results (pass/fail counts)
- Any issues encountered and resolutions
</output>
