---
phase: 250-all-test-fixes
plan: 02
type: execute
wave: 2
depends_on: [250-01]
files_modified: []
autonomous: true
requirements: [FIX-03, FIX-04]

must_haves:
  truths:
    - All medium-priority test failures are fixed
    - All low-priority test failures are fixed
    - 100% test pass rate achieved (zero failures)
    - Test suite runs end-to-end without manual intervention
    - Test results are reproducible across multiple runs
  artifacts:
    - path: backend/TEST_FAILURE_REPORT.md
      provides: Updated test failure report (all failures fixed or documented)
      min_lines: 50
  key_links:
    - from: pytest execution
      to: test output
      via: pytest exit code 0
      pattern: "passed.*warnings in"
---

<objective>
Fix all remaining test failures and achieve 100% pass rate

Purpose: After infrastructure is unblocked (Plan 01), fix all remaining medium and low priority test failures to achieve 100% test pass rate as required by FIX-03 and FIX-04.

Output: 100% test pass rate with all tests passing
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@backend/TEST_FAILURE_REPORT.md
@backend/TESTING.md
@.planning/phases/250-all-test-fixes/250-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover and document remaining test failures</name>
  <files>backend/TEST_FAILURE_REPORT.md</files>
  <read_first>backend/TEST_FAILURE_REPORT.md, .planning/phases/250-all-test-fixes/250-01-SUMMARY.md</read_first>
  <action>
    After Plan 01 infrastructure fix, discover all remaining test failures:

    1. Run the full API test suite:
       ```bash
       cd /Users/rushiparikh/projects/atom/backend
       PYTHONPATH=. pytest tests/api/ --ignore=tests/e2e_ui -v --tb=short --maxfail=20
       ```

    2. Run a broader test sample (core services):
       ```bash
       PYTHONPATH=. pytest tests/core/ --ignore=tests/e2e_ui -v --tb=short --maxfail=20
       ```

    3. Document all failures found:
       - Test file and function name
       - Error message and stack trace
       - Category (medium/low priority per TEST_FAILURE_REPORT.md)
       - Estimated fix complexity

    4. Update backend/TEST_FAILURE_REPORT.md:
       - Mark Phase 249 fixes as complete (DTO-001 to DTO-004, CANVAS-001 to CANVAS-003)
       - Add new failure entries for any remaining failures
       - Update priority matrix

    Do NOT fix any tests yet - only discover and document.
  </action>
  <verify>
    <automated>cd /Users/rushiparikh/projects/atom/backend && PYTHONPATH=. pytest tests/api/ tests/core/ --ignore=tests/e2e_ui --collect-only -q 2>&1 | grep -E "test session starts|collected" | head -2</automated>
  </verify>
  <done>
    - All remaining test failures documented
    - TEST_FAILURE_REPORT.md updated with current status
    - Clear list of fixes needed for 100% pass rate
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix medium-priority test failures</name>
  <files>backend/tests/api/*.py, backend/tests/core/*.py, backend/core/**/*.py, backend/api/**/*.py</files>
  <read_first>backend/TEST_FAILURE_REPORT.md, .planning/phases/250-all-test-fixes/250-01-SUMMARY.md</read_first>
  <action>
    Fix all medium-priority test failures documented in Task 1:

    1. For each medium-priority failure (P2 per TEST_FAILURE_REPORT.md):
       a. Read the test file to understand expected behavior
       b. Read the source file(s) being tested
       c. Identify the root cause (test bug vs implementation bug vs configuration issue)
       d. Apply TDD approach:
          - Run test to confirm failure (RED phase already done in Task 1)
          - Fix the issue (source code or test code)
          - Run test to confirm pass (GREEN phase)
          - Run related tests to check for regressions

    2. Common medium-priority issues to address:
       - Missing or incorrect test fixtures
       - API endpoint path changes
       - Response format mismatches
       - Mock/stub configuration issues
       - Database state issues

    3. Track progress with git commits:
       - Commit message pattern: "fix(tests): fix <test_name> - <brief description>"
       - One commit per fix or related group of fixes

    Do NOT skip tests or mark them as flaky - fix the root cause.
  </action>
  <verify>
    <automated>cd /Users/rushiparikh/projects/atom/backend && PYTHONPATH=. pytest tests/api/ tests/core/ --ignore=tests/e2e_ui -v --tb=line 2>&1 | grep -E "passed|failed|ERROR" | tail -3</automated>
  </verify>
  <done>
    - All medium-priority test failures fixed
    - Tests pass with no regressions
    - Git commits document each fix
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix low-priority test failures</name>
  <files>backend/tests/**/*.py</files>
  <read_first>backend/TEST_FAILURE_REPORT.md, .planning/phases/250-all-test-fixes/250-01-SUMMARY.md</read_first>
  <action>
    Fix all low-priority test failures documented in Task 1:

    1. For each low-priority failure (P3 per TEST_FAILURE_REPORT.md):
       a. Read the test file to understand expected behavior
       b. Read the source file(s) being tested
       c. Identify the root cause
       d. Apply TDD approach (fix, verify, check regressions)

    2. Common low-priority issues to address:
       - Deprecated test patterns
       - Orphaned test files (feature removed but tests remain)
       - Minor assertion issues
       - Edge case handling

    3. For orphaned tests (feature no longer exists):
       - Remove the test file with appropriate commit message
       - Document removal in TEST_FAILURE_REPORT.md

    4. Track progress with git commits:
       - Commit message pattern: "fix(tests): fix <test_name> - <brief description>"
       - For removals: "refactor(tests): remove obsolete test <test_name>"

    Do NOT mark tests as skip or xfail to hide failures.
  </action>
  <verify>
    <automated>cd /Users/rushiparikh/projects/atom/backend && PYTHONPATH=. pytest tests/ --ignore=tests/e2e_ui --ignore=tests/integration --ignore=tests/property_tests --ignore=tests/scenarios --ignore=tests/security --ignore=tests/unit --ignore=tests/contract -v --tb=line 2>&1 | grep -E "passed|failed" | tail -3</automated>
  </verify>
  <done>
    - All low-priority test failures fixed or removed
    - Tests pass with no regressions
    - Git commits document each fix or removal
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify 100% test pass rate and reproducibility</name>
  <files>backend/TEST_FAILURE_REPORT.md, .planning/phases/250-all-test-fixes/250-02-SUMMARY.md</files>
  <read_first>backend/TEST_FAILURE_REPORT.md, backend/TESTING.md, .planning/phases/250-all-test-fixes/250-01-SUMMARY.md</read_first>
  <action>
    Verify 100% test pass rate and reproducibility per FIX-04 requirements:

    1. Run full test suite multiple times to verify reproducibility:
       ```bash
       # Run 1
       PYTHONPATH=. pytest tests/api/ tests/core/ --ignore=tests/e2e_ui -v --tb=short > run1.log 2>&1

       # Run 2
       PYTHONPATH=. pytest tests/api/ tests/core/ --ignore=tests/e2e_ui -v --tb=short > run2.log 2>&1

       # Run 3
       PYTHONPATH=. pytest tests/api/ tests/core/ --ignore=tests/e2e_ui -v --tb=short > run3.log 2>&1

       # Compare results
       grep -E "passed|failed" run1.log run2.log run3.log
       ```

    2. Verify 100% pass rate:
       - All runs should show "0 failed"
       - No intermittent failures (flaky tests)
       - Consistent test count across runs

    3. Update backend/TEST_FAILURE_REPORT.md:
       - Mark all failures as RESOLVED
       - Add summary section with final pass rate
       - Document any tests that were removed (with justification)

    4. Create final summary:
       - Total tests executed
       - Pass rate percentage
       - Number of fixes applied
       - Verification that FIX-03 and FIX-04 requirements are met

    If any tests fail, return to Task 2 or 3 to fix them.
  </action>
  <verify>
    <automated>cd /Users/rushiparikh/projects/atom/backend && for i in 1 2 3; do PYTHONPATH=. pytest tests/api/ tests/core/ --ignore=tests/e2e_ui -q --tb=no 2>&1 | grep -E "passed|failed"; done</automated>
  </verify>
  <done>
    - 100% test pass rate achieved (0 failures)
    - Results reproducible across 3 consecutive runs
    - TEST_FAILURE_REPORT.md updated with final status
    - FIX-03 and FIX-04 requirements verified as complete
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production code | Tests may indirectly modify code under test during fixes |
| test execution → system state | Tests may create/modify database records, files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-250-04 | T (Tampering) | test fixes | mitigate | Review all code changes, ensure fixes are minimal and correct |
| T-250-05 | I (Information Disclosure) | test output | accept | Test output may contain system paths, acceptable for development |
| T-250-06 | D (Denial of Service) | test execution | accept | Tests run in isolated environment, no production impact |
</threat_model>

<verification>
1. All tests pass (100% pass rate)
2. Results reproducible across multiple runs
3. No flaky tests detected
4. TEST_FAILURE_REPORT.md shows all failures resolved
</verification>

<success_criteria>
1. 100% test pass rate achieved (0 failures, 0 errors)
2. Test suite runs end-to-end without manual intervention
3. Results reproducible across 3 consecutive runs
4. FIX-03 (all medium/low failures fixed) verified complete
5. FIX-04 (100% pass rate) verified complete
</success_criteria>

<output>
After completion, create `.planning/phases/250-all-test-fixes/250-02-SUMMARY.md`
</output>
