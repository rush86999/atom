---
phase: 250-all-test-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/conftest.py, backend/conftest.py]
autonomous: true
requirements: [FIX-03, FIX-04]

must_haves:
  truths:
    - Pytest can discover and run tests from backend directory without errors
    - E2E UI fixtures are correctly imported when available
    - Test suite runs without ImportError for pytest_plugins
    - 100% of collected tests can execute (0% blocked by infrastructure issues)
  artifacts:
    - path: tests/conftest.py
      provides: Root conftest with conditional pytest_plugins
      min_lines: 30
    - path: backend/conftest.py
      provides: Backend-specific conftest without pytest_plugins
      min_lines: 20
  key_links:
    - from: tests/conftest.py
      to: backend/tests/e2e_ui/fixtures/*.py
      via: conditional import with try/except
      pattern: "pytest_plugins = \\[|try:|except ImportError:"
---

<objective>
Fix test infrastructure blockers preventing test execution

Purpose: The root conftest.py references e2e_ui fixtures that don't exist at the specified path, causing ImportError that blocks all test execution. This must be fixed before any test failures can be addressed.

Output: Working test infrastructure that allows pytest to discover and run tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@backend/TEST_FAILURE_REPORT.md
@backend/TESTING.md
@backend/conftest.py
@backend/pytest.ini
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix root conftest pytest_plugins ImportError</name>
  <files>tests/conftest.py</files>
  <read_first>tests/conftest.py, backend/conftest.py</read_first>
  <behavior>
    - Test 1: pytest can import root conftest without ModuleNotFoundError
    - Test 2: pytest can discover tests from backend directory
    - Test 3: E2E tests work when e2e_ui fixtures are available
    - Test 4: Non-E2E tests work without e2e_ui fixtures
  </behavior>
  <action>
    Modify /Users/rushiparikh/projects/atom/tests/conftest.py to handle missing e2e_ui fixtures gracefully:

    1. Change pytest_plugins from hardcoded list to conditional import:
       - Try to import each e2e_ui fixture module
       - Only add to pytest_plugins if import succeeds
       - Use try/except ImportError pattern

    2. The fix should:
       - Allow tests to run when e2e_ui fixtures are not available
       - Load e2e_ui fixtures when they ARE available (for E2E tests)
       - Not break when pytest is run from different directories

    3. Implementation pattern:
       ```python
       pytest_plugins = []
       for plugin in [
           "tests.e2e_ui.fixtures.auth_fixtures",
           "tests.e2e_ui.fixtures.database_fixtures",
           "tests.e2e_ui.fixtures.api_fixtures",
           "tests.e2e_ui.fixtures.test_data_factory",
       ]:
           try:
               __import__(plugin)
               pytest_plugins.append(plugin)
           except (ImportError, ModuleNotFoundError):
               pass  # Plugin not available, skip it
       ```

    Do NOT change backend/conftest.py - it already has the correct comments about pytest_plugins being moved to root.
  </action>
  <verify>
    <automated>cd /Users/rushiparikh/projects/atom/backend && PYTHONPATH=. pytest tests/api/test_ab_testing_routes.py::TestCreateTest::test_create_test_success -v --tb=short</automated>
  </verify>
  <done>
    - pytest runs without ImportError for pytest_plugins
    - Tests can be discovered and executed from backend directory
    - E2E fixtures load when available
    - Non-E2E tests work without E2E fixtures
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify test suite runs without infrastructure errors</name>
  <files>backend/pytest.ini, backend/conftest.py</files>
  <read_first>backend/pytest.ini, backend/conftest.py, tests/conftest.py</read_first>
  <action>
    Run a broader test suite to verify infrastructure fixes work:

    1. From /Users/rushiparikh/projects/atom/backend, run:
       ```bash
       PYTHONPATH=. pytest tests/api/ --ignore=tests/e2e_ui -v --tb=short --collect-only
       ```

    2. Verify collection completes without ImportError

    3. Run a subset of tests to verify execution:
       ```bash
       PYTHONPATH=. pytest tests/api/test_ab_testing_routes.py -v --tb=short
       ```

    4. Check for any remaining infrastructure issues:
       - Import errors in conftest
       - Path resolution issues
       - Plugin loading failures

    5. Document any remaining issues (these become tasks for Plan 02)

    Do NOT modify test code - only verify and document issues.
  </action>
  <verify>
    <automated>cd /Users/rushiparikh/projects/atom/backend && PYTHONPATH=. pytest tests/api/ --ignore=tests/e2e_ui --collect-only -q 2>&1 | grep -E "(collected|error|ERROR)" | head -5</automated>
  </verify>
  <done>
    - Test collection completes without ImportError
    - Tests execute successfully
    - Remaining test failures (if any) are documented for Plan 02
    - Infrastructure blocker is resolved
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conftest → test modules | conftest code executes before any tests, could affect all tests |
| pytest_plugins → pytest core | Plugin imports can crash pytest before test discovery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-250-01 | T (Tampering) | tests/conftest.py | mitigate | Use try/except for plugin imports, validate plugin list |
| T-250-02 | T (Tampering) | backend/conftest.py | mitigate | Keep pytest_plugins disabled in backend conftest (already done) |
| T-250-03 | I (Information Disclosure) | pytest output | accept | Test output may contain paths, acceptable for development |
</threat_model>

<verification>
1. Test collection succeeds without ImportError
2. Tests can be executed from backend directory
3. pytest_plugins load conditionally based on availability
4. No regression in existing passing tests
</verification>

<success_criteria>
1. pytest can discover and run tests without ImportError
2. E2E fixtures load when available, gracefully skipped when not
3. Test suite execution is unblocked for Phase 250-02
4. At least 50 tests can run successfully (prove infrastructure works)
</success_criteria>

<output>
After completion, create `.planning/phases/250-all-test-fixes/250-01-SUMMARY.md`
</output>
