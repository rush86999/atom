---
phase: 236-cross-platform-and-stress-testing
plan: 08
type: execute
wave: 4
depends_on: ["236-01", "236-02", "236-03", "236-04", "236-05", "236-06", "236-07"]
files_modified:
  - backend/tests/bug_discovery/bug_filing_service.py
  - backend/tests/bug_discovery/test_automated_bug_filing.py
  - backend/tests/bug_discovery/fixtures/bug_filing_fixtures.py
  - .github/workflows/automated-bug-filing.yml
  - backend/docs/BUG_DISCOVERY.md
autonomous: true
user_setup:
  - service: github
    why: "Automated bug filing via GitHub Issues API"
    env_vars:
      - name: GITHUB_TOKEN
        source: "GitHub Settings -> Developer settings -> Personal access tokens -> Tokens (classic)"
      - name: GITHUB_REPOSITORY
        source: "Environment variable: owner/repo (e.g., rushiparikh/atom)"
    dashboard_config:
      - task: "Create GitHub Personal Access Token"
        location: "GitHub Settings -> Developer settings -> Personal access tokens -> Tokens (classic) -> Generate new token (classic)"
      - task: "Grant repo scope"
        location: "Token scopes: repo (full control of private repositories)"

must_haves:
  truths:
    - "Automated bug filing service creates GitHub Issues for test failures"
    - "Bug metadata collection includes screenshots, logs, traces, and test context"
    - "Bug filing service integrates with all test types (load, network, memory, mobile, desktop, visual, a11y)"
    - "GitHub Issues are created with labels, milestones, and reproducible test cases"
    - "Bug filing is idempotent (no duplicate issues for same failure)"
    - "CI/CD pipeline triggers bug filing on test failures"
  artifacts:
    - path: "backend/tests/bug_discovery/bug_filing_service.py"
      provides: "Bug filing service with GitHub Issues API integration"
      contains: "class BugFilingService"
      min_lines: 150
    - path: "backend/tests/bug_discovery/test_automated_bug_filing.py"
      provides: "Automated bug filing tests"
      contains: "test_bug_filing_load_failure|test_bug_filing_network_failure"
      min_lines: 100
    - path: "backend/tests/bug_discovery/fixtures/bug_filing_fixtures.py"
      provides: "Bug filing fixtures and mock GitHub API"
      contains: "@pytest.fixture"
      min_lines: 80
    - path: ".github/workflows/automated-bug-filing.yml"
      provides: "GitHub Actions workflow for automated bug filing"
      contains: "uses: actions/checkout@v3"
      min_lines: 80
    - path: "backend/docs/BUG_DISCOVERY.md"
      provides: "Automated bug discovery documentation"
      contains: "# Automated Bug Discovery"
      min_lines: 80
  key_links:
    - from: "backend/tests/bug_discovery/bug_filing_service.py"
      to: "https://api.github.com/repos/owner/repo/issues"
      via: "GitHub REST API for creating issues"
      pattern: "requests.post.*api.github.com.*issues"
    - from: "backend/tests/bug_discovery/bug_filing_service.py"
      to: "backend/tests/load/"
      via: "Bug filing triggered by load test failures"
      pattern: "load_test_failure|threshold_exceeded"
    - from: "backend/tests/bug_discovery/bug_filing_service.py"
      to: "backend/tests/e2e_ui/tests/"
      via: "Bug filing triggered by E2E test failures"
      pattern: "e2e_test_failure|screenshot_on_failure"
    - from: ".github/workflows/automated-bug-filing.yml"
      to: "backend/tests/bug_discovery/bug_filing_service.py"
      via: "CI/CD workflow calls bug filing service"
      pattern: "python.*bug_filing_service|file_bug"
---

<objective>
Create automated bug discovery and filing service that creates GitHub Issues for test failures with complete metadata (screenshots, logs, traces, reproducible test cases).

Purpose: Automatically discover and file bugs from test failures with actionable information, reducing manual triage and accelerating bug fixes.
Output: Bug filing service, automated bug filing tests, CI/CD workflow, and documentation.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/236-cross-platform-and-stress-testing/236-RESEARCH.md
@.planning/phases/236-cross-platform-and-stress-testing/236-01-SUMMARY.md
@.planning/phases/236-cross-platform-and-stress-testing/236-02-SUMMARY.md
@.planning/phases/236-cross-platform-and-stress-testing/236-03-SUMMARY.md
@.planning/phases/236-cross-platform-and-stress-testing/236-04-SUMMARY.md
@.planning/phases/236-cross-platform-and-stress-testing/236-05-SUMMARY.md
@.planning/phases/236-cross-platform-and-stress-testing/236-06-SUMMARY.md
@.planning/phases/236-cross-platform-and-stress-testing/236-07-SUMMARY.md
@backend/tests/e2e_ui/conftest.py
@backend/tests/load/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bug filing service</name>
  <files>backend/tests/bug_discovery/bug_filing_service.py</files>
  <action>
    Create `backend/tests/bug_discovery/bug_filing_service.py`:

    1. **BugFilingService class**:
       - __init__(self, github_token, github_repository)
       - file_bug(self, test_name, error_message, metadata)
       - _check_duplicate_bug(self, title)  # Idempotency
       - _create_github_issue(self, title, body, labels)
       - _generate_bug_title(self, test_name, error_type)
       - _generate_bug_body(self, metadata)
       - _attach_screenshot(self, issue_number, screenshot_path)
       - _attach_logs(self, issue_number, log_path)

    2. **Bug metadata collection**:
       - Test name and file path
       - Error message and stack trace
       - Screenshot (if visual/E2E test)
       - Test logs (stdout/stderr)
       - Performance metrics (if load/performance test)
       - Platform info (OS, browser, Python version)
       - Test run context (CI/CD, local, scheduled)

    3. **Issue template**:
       ```markdown
       ## Bug Description
       {error_message}

       ## Test Context
       - Test: {test_name}
       - File: {test_file}
       - Platform: {os_info}
       - Python: {python_version}

       ## Steps to Reproduce
       1. Run test: `pytest {test_file}::{test_name}`
       2. Observe failure

       ## Expected Behavior
       {expected_behavior}

       ## Actual Behavior
       {actual_behavior}

       ## Screenshots
       {screenshot_url}

       ## Logs
       ```
       {log_content}
       ```

       ## Metadata
       - Test run: {ci_run_url}
       - Commit: {commit_sha}
       - Branch: {branch_name}
       ```

    4. **Labels**:
       - bug (automatic)
       - automated (automatic)
       - test-type: load, e2e, mobile, desktop, visual, a11y, memory
       - severity: critical, high, medium, low (based on test type)
       - platform: web, mobile, desktop

    Use GitHub REST API: POST /repos/{owner}/{repo}/issues
    Use requests library for HTTP calls (or httpx)
    Use environment variables: GITHUB_TOKEN, GITHUB_REPOSITORY
    Implement idempotency: check for existing issues with same title before creating new one.
  </action>
  <verify>grep -E "(class BugFilingService|file_bug|_check_duplicate_bug|_create_github_issue)" backend/tests/bug_discovery/bug_filing_service.py</verify>
  <done>BugFilingService class exists with all required methods</done>
</task>

<task type="auto">
  <name>Task 2: Create bug filing fixtures</name>
  <files>backend/tests/bug_discovery/fixtures/bug_filing_fixtures.py</files>
  <action>
    Create `backend/tests/bug_discovery/fixtures/bug_filing_fixtures.py`:

    1. **mock_github_api fixture**:
       - Mock GitHub API calls (requests.post, requests.get)
       - Return mock responses for issue creation
       - Track created issues for verification in tests
       - Reset mock after each test

    2. **bug_filing_service fixture**:
       - Create BugFilingService instance with test config
       - Use mock_github_api fixture
       - Return service instance for testing

    3. **test_metadata fixture**:
       - Create sample metadata dict (simulating test failure)
       - Include: test_name, error_message, screenshot_path, log_path, platform_info
       - Return metadata for bug filing tests

    4. **screenshot_on_failure fixture**:
       - Pytest hook to capture screenshot on test failure
       - Use pytest_exception_interact hook
       - Save screenshot to: backend/tests/artifacts/screenshots/{test_name}.png
       - Return screenshot path

    5. **log_on_failure fixture**:
       - Pytest hook to capture logs on test failure
       - Use pytest_runtest_makereport hook
       - Save logs to: backend/tests/artifacts/logs/{test_name}.log
       - Return log path

    Use pytest hooks for automatic screenshot/log capture on failure.
    Use unittest.mock.patch for GitHub API mocking.
    Create artifacts directory if it doesn't exist: backend/tests/artifacts/
  </action>
  <verify>grep -E "(mock_github_api|bug_filing_service|test_metadata|screenshot_on_failure)" backend/tests/bug_discovery/fixtures/bug_filing_fixtures.py</verify>
  <done>All 5 bug filing fixtures exist with GitHub API mocking</done>
</task>

<task type="auto">
  <name>Task 3: Create automated bug filing tests</name>
  <files>backend/tests/bug_discovery/test_automated_bug_filing.py</files>
  <action>
    Create `backend/tests/bug_discovery/test_automated_bug_filing.py`:

    1. **test_bug_filing_load_failure(bug_filing_service, test_metadata)**:
       - Simulate load test failure (threshold exceeded)
       - Call service.file_bug(test_name="test_api_load_baseline", error_message="p(95) > 500ms", metadata={...})
       - Verify GitHub issue created with correct title: "[Bug] Load test failed: test_api_load_baseline"
       - Verify issue body contains error message and metadata
       - Verify issue labels include: bug, automated, test-type:load, severity:high

    2. **test_bug_filing_network_failure(bug_filing_service, test_metadata)**:
       - Simulate network test failure (offline mode test failed)
       - Call service.file_bug(test_name="test_offline_mode", error_message="Network error not handled", metadata={...})
       - Verify GitHub issue created with test-type:network label
       - Verify issue body includes network context (slow 3G, offline mode)

    3. **test_bug_filing_memory_leak(bug_filing_service, test_metadata)**:
       - Simulate memory leak test failure
       - Call service.file_bug(test_name="test_memory_leak_agent_execution", error_message="Memory leak detected: 10MB increase", metadata={...})
       - Verify GitHub issue created with test-type:memory label
       - Verify issue body includes heap snapshot comparison

    4. **test_bug_filing_mobile_api(bug_filing_service, test_metadata)**:
       - Simulate mobile API test failure
       - Call service.file_bug(test_name="test_mobile_agent_execute", error_message="API returned 500", metadata={...})
       - Verify GitHub issue created with test-type:mobile, platform:mobile labels

    5. **test_bug_filing_desktop(bug_filing_service, test_metadata)**:
       - Simulate desktop test failure
       - Call service.file_bug(test_name="test_window_minimize", error_message="Window minimize failed", metadata={...})
       - Verify GitHub issue created with test-type:desktop, platform:desktop labels

    6. **test_bug_filing_visual_regression(bug_filing_service, test_metadata)**:
       - Simulate visual regression failure
       - Call service.file_bug(test_name="test_visual_dashboard", error_message="Visual diff detected", metadata={...})
       - Verify GitHub issue created with test-type:visual label
       - Verify issue body includes Percy diff URL

    7. **test_bug_filing_accessibility(bug_filing_service, test_metadata)**:
       - Simulate accessibility test failure
       - Call service.file_bug(test_name="test_wcag_compliance_dashboard", error_message="WCAG violation: color-contrast", metadata={...})
       - Verify GitHub issue created with test-type:a11y label
       - Verify issue body includes axe-core violation details

    8. **test_bug_filing_idempotency(bug_filing_service, test_metadata)**:
       - Call service.file_bug() twice with same test_name and error_message
       - Verify only 1 GitHub issue created (idempotency)
       - Verify _check_duplicate_bug() prevents duplicate filing

    Use bug_filing_service fixture (with mocked GitHub API).
    Use test_metadata fixture for sample metadata.
    Verify issue creation via mock_github_api fixture.
    Test all 7 test types: load, network, memory, mobile, desktop, visual, a11y.
  </action>
  <verify>pytest backend/tests/bug_discovery/test_automated_bug_filing.py -v</verify>
  <done>All 8 automated bug filing tests pass</done>
</task>

<task type="auto">
  <name>Task 4: Create CI/CD workflow for automated bug filing</name>
  <files>.github/workflows/automated-bug-filing.yml</files>
  <action>
    Create `.github/workflows/automated-bug-filing.yml`:

    1. **Workflow trigger**:
       - on: [workflow_run, workflow_dispatch] (triggered after test workflows)
       - workflows: ["Load Testing", "E2E Testing", "Mobile Testing", "Desktop Testing", "Visual Regression", "Accessibility Testing"]
       - types: [completed]

    2. **Job configuration**:
       ```yaml
       jobs:
         file-bugs:
           if: ${{ github.event.workflow_run.conclusion == 'failure' }}
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v3
             - name: Set up Python
               uses: actions/setup-python@v4
               with:
                 python-version: '3.11'
             - name: Install dependencies
               run: |
                 pip install requests pytest
             - name: Download test artifacts
               uses: actions/download-artifact@v3
               with:
                 path: backend/tests/artifacts/
             - name: File bugs for test failures
               env:
                 GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                 GITHUB_REPOSITORY: ${{ github.repository }}
               run: |
                 python backend/tests/bug_discovery/file_bugs_from_artifacts.py
             - name: Comment on PR with bug links
               uses: actions/github-script@v6
               with:
                 script: |
                   // Comment bug links on PR
       ```

    3. **Bug filing script**:
       - Create backend/tests/bug_discovery/file_bugs_from_artifacts.py
       - Scan artifacts/ directory for test failure reports
       - Parse failure reports (JSON files with test_name, error_message, metadata)
       - Call BugFilingService.file_bug() for each failure
       - Log created issues to stdout

    Use workflow_run trigger (runs after test workflows complete).
    Use GITHUB_TOKEN (automatic, no need to create PAT).
    Download artifacts from failed test runs (screenshots, logs).
    File bugs only on workflow failure (if: github.event.workflow_run.conclusion == 'failure').
  </action>
  <verify>cat .github/workflows/automated-bug-filing.yml | grep -E "(on:|workflow_run|file-bugs|GITHUB_TOKEN)"</verify>
  <done>Automated bug filing workflow exists and triggers on test failures</done>
</task>

<task type="auto">
  <name>Task 5: Create bug discovery documentation</name>
  <files>backend/docs/BUG_DISCOVERY.md</files>
  <action>
    Create `backend/docs/BUG_DISCOVERY.md` with:

    1. **Overview**: Purpose of automated bug discovery (file bugs from test failures automatically)
    2. **How it works**:
       - Test failures trigger bug filing service
       - Bug metadata collection (screenshots, logs, traces)
       - GitHub Issues created with labels and templates
       - Idempotency (no duplicate issues)

    3. **Supported test types**:
       - Load testing (k6 threshold failures)
       - Network simulation (offline mode, slow 3G failures)
       - Memory leak detection (heap snapshot failures)
       - Mobile API (authentication, agent, workflow failures)
       - Desktop (window management, native feature failures)
       - Visual regression (Percy diff failures)
       - Accessibility (WCAG violation failures)

    4. **Bug metadata**:
       - Test name and file path
       - Error message and stack trace
       - Screenshots (for visual/E2E tests)
       - Test logs (stdout/stderr)
       - Performance metrics
       - Platform info (OS, browser, Python version)
       - CI/CD context (commit, branch, run URL)

    5. **GitHub issue template**: Show example issue structure
    6. **Labels**:
       - bug, automated
       - test-type: load, network, memory, mobile, desktop, visual, a11y
       - severity: critical, high, medium, low
       - platform: web, mobile, desktop

    7. **CI/CD integration**:
       - Automated bug filing workflow (.github/workflows/automated-bug-filing.yml)
       - Triggers on test workflow failures
       - Comments bug links on PR

    8. **Manual bug filing**:
       - Run bug filing service locally: `python backend/tests/bug_discovery/bug_filing_service.py`
       - File bug from test failure report: `python file_bugs_from_artifacts.py`

    9. **Troubleshooting**:
       - GitHub token not found (check GITHUB_TOKEN env var)
       - Duplicate issues (check _check_duplicate_bug logic)
       - Missing screenshots/logs (check artifact download)

    Include example bug filing command:
    ```bash
    export GITHUB_TOKEN=ghp_xxx
    export GITHUB_REPOSITORY=owner/repo
    python backend/tests/bug_discovery/file_bugs_from_artifacts.py
    ```
  </action>
  <verify>cat backend/docs/BUG_DISCOVERY.md | grep -E "(Overview|How it works|Supported test types|Bug metadata)"</verify>
  <done>BUG_DISCOVERY.md contains complete automated bug discovery documentation</done>
</task>

</tasks>

<verification>
Run automated bug filing tests:
```bash
export GITHUB_TOKEN=ghp_test_token
export GITHUB_REPOSITORY=test/repo
pytest backend/tests/bug_discovery/test_automated_bug_filing.py -v
```

Verify:
- All 8 tests pass (load, network, memory, mobile, desktop, visual, a11y, idempotency)
- Bug filing service creates GitHub Issues with correct structure
- Idempotency works (no duplicate issues)
- Bug metadata includes screenshots, logs, and context
</verification>

<success_criteria>
Phase 236-08 is complete when:
- bug_filing_service.py contains BugFilingService class with all methods (file_bug, _check_duplicate_bug, _create_github_issue, _attach_screenshot, etc.)
- bug_filing_fixtures.py contains 5 fixtures (mock_github_api, bug_filing_service, test_metadata, screenshot_on_failure, log_on_failure)
- test_automated_bug_filing.py has 8 tests passing (load, network, memory, mobile, desktop, visual, a11y, idempotency)
- automated-bug-filing.yml workflow triggers on test failures
- BUG_DISCOVERY.md documents bug discovery process, supported test types, and troubleshooting
- Bug filing service integrates with all 7 test types (236-01 through 236-07)
- GitHub Issues are created with labels, templates, and metadata
- Idempotency prevents duplicate issues
</success_criteria>

<output>
After completion, create `.planning/phases/236-cross-platform-and-stress-testing/236-08-SUMMARY.md` with:
- Bug filing service implementation complete
- Automated bug filing test results
- CI/CD workflow integration verified
- Example GitHub Issues created
- Idempotency verification results
</output>
