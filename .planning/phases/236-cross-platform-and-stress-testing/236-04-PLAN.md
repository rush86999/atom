---
phase: 236-cross-platform-and-stress-testing
plan: 04
type: execute
wave: 2
depends_on: ["234-01", "235-06"]
files_modified:
  - backend/tests/mobile_api/test_mobile_auth.py
  - backend/tests/mobile_api/test_mobile_agent_execution.py
  - backend/tests/mobile_api/test_mobile_workflow_execution.py
  - backend/tests/mobile_api/test_mobile_device_features.py
  - backend/tests/mobile_api/fixtures/mobile_fixtures.py
  - backend/tests/mobile_api/README.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Mobile API authentication works via direct API calls (not web UI)"
    - "Mobile API agent execution works with governance checks"
    - "Mobile API workflow execution works with skill composition"
    - "Mobile API device features work (camera, location, notifications)"
    - "Mobile tests use pytest fixtures for authentication (no UI overhead)"
    - "Mobile API responses match web API responses (consistency)"
  artifacts:
    - path: "backend/tests/mobile_api/fixtures/mobile_fixtures.py"
      provides: "Mobile API authentication and setup fixtures"
      contains: "@pytest.fixture"
      min_lines: 80
    - path: "backend/tests/mobile_api/test_mobile_auth.py"
      provides: "Mobile API authentication tests (login, token refresh, logout)"
      contains: "test_mobile_login|test_mobile_token_refresh"
      min_lines: 100
    - path: "backend/tests/mobile_api/test_mobile_agent_execution.py"
      provides: "Mobile API agent execution tests"
      contains: "test_mobile_agent_execute|test_mobile_agent_stream"
      min_lines: 120
    - path: "backend/tests/mobile_api/test_mobile_workflow_execution.py"
      provides: "Mobile API workflow execution tests"
      contains: "test_mobile_workflow_create|test_mobile_workflow_execute"
      min_lines: 120
    - path: "backend/tests/mobile_api/test_mobile_device_features.py"
      provides: "Mobile API device features tests (camera, location, notifications)"
      contains: "test_mobile_camera_capture|test_mobile_location_get"
      min_lines: 100
    - path: "backend/tests/mobile_api/README.md"
      provides: "Mobile API testing documentation"
      contains: "# Mobile API Testing"
      min_lines: 60
  key_links:
    - from: "backend/tests/mobile_api/fixtures/mobile_fixtures.py"
      to: "backend/tests/e2e_ui/fixtures/auth_fixtures.py"
      via: "Shared authentication patterns (API-first, not UI-based)"
      pattern: "api_token|auth_token"
    - from: "backend/tests/mobile_api/test_mobile_auth.py"
      to: "backend/core/auth_endpoints.py"
      via: "Direct API calls to /api/auth/login"
      pattern: "client.post.*api/auth/login"
    - from: "backend/tests/mobile_api/test_mobile_agent_execution.py"
      to: "backend/core/atom_agent_endpoints.py"
      via: "Direct API calls to /api/v1/agents/execute"
      pattern: "client.post.*api/v1/agents/execute"
    - from: "backend/tests/mobile_api/test_mobile_workflow_execution.py"
      to: "backend/api/workflow_routes.py"
      via: "Direct API calls to /api/v1/workflows"
      pattern: "client.post.*api/v1/workflows"
    - from: "backend/tests/mobile_api/test_mobile_device_features.py"
      to: "backend/api/device_capabilities.py"
      via: "Direct API calls to /api/v1/device/*"
      pattern: "client.post.*api/v1/device"
---

<objective>
Create mobile API-level tests for authentication, agent execution, workflow execution, and device features to verify mobile app functionality without UI overhead.

Purpose: Ensure mobile apps can authenticate, execute agents, run workflows, and access device features via REST API with consistent responses to web API.
Output: Mobile fixtures, 4 mobile API test files, and README documentation.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/236-cross-platform-and-stress-testing/236-RESEARCH.md
@.planning/phases/234-authentication-and-agent-e2e/234-01-SUMMARY.md
@.planning/phases/235-canvas-and-workflow-e2e/235-06-SUMMARY.md
@backend/tests/e2e_ui/fixtures/auth_fixtures.py
@backend/core/auth_endpoints.py
@backend/core/atom_agent_endpoints.py
@backend/api/workflow_routes.py
@backend/api/device_capabilities.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mobile API authentication fixtures</name>
  <files>backend/tests/mobile_api/fixtures/mobile_fixtures.py</files>
  <action>
    Create `backend/tests/mobile_api/fixtures/mobile_fixtures.py` with API-first auth:

    1. **mobile_api_client fixture**:
       - Create TestClient from FastAPI app
       - Use from core.main import app (or main:app)
       - Configure client with base_url and headers
       - Return client instance for API calls

    2. **mobile_auth_token fixture**:
       - Use mobile_api_client to POST /api/auth/login
       - Use test user credentials (email, password)
       - Extract access_token from response JSON
       - Verify response status is 200
       - Return token string

    3. **mobile_auth_headers fixture**:
       - Use mobile_auth_token fixture
       - Return dict: {"Authorization": f"Bearer {token}"}

    4. **mobile_test_user fixture**:
       - Create test user in database via API or direct DB insert
       - Return user object with email, password, user_id
       - Cleanup after test (delete user)

    DO NOT use Playwright or browser automation (API-only tests).
    DO NOT use web UI authentication fixtures (too slow for mobile API tests).
    Use TestClient for in-memory API testing (no server startup needed).
  </action>
  <verify>grep -E "(mobile_api_client|mobile_auth_token|mobile_auth_headers)" backend/tests/mobile_api/fixtures/mobile_fixtures.py</verify>
  <done>All 4 mobile fixtures exist with API-first authentication</done>
</task>

<task type="auto">
  <name>Task 2: Create mobile API authentication tests</name>
  <files>backend/tests/mobile_api/test_mobile_auth.py</files>
  <action>
    Create `backend/tests/mobile_api/test_mobile_auth.py`:

    1. **test_mobile_login_success(mobile_api_client, mobile_test_user)**:
       - POST /api/auth/login with valid credentials
       - Verify status code is 200
       - Verify response contains access_token (string, non-empty)
       - Verify response contains token_type == "bearer"
       - Verify response contains expires_in (integer, >0)

    2. **test_mobile_login_invalid_credentials(mobile_api_client)**:
       - POST /api/auth/login with invalid password
       - Verify status code is 401
       - Verify response contains error message
       - Verify access_token NOT in response

    3. **test_mobile_token_refresh(mobile_api_client, mobile_auth_token)**:
       - POST /api/auth/refresh with refresh token
       - Verify status code is 200
       - Verify new access_token returned
       - Verify new token differs from old token

    4. **test_mobile_token_validation(mobile_api_client, mobile_auth_headers)**:
       - GET /api/auth/me with Authorization header
       - Verify status code is 200
       - Verify response contains user data (email, user_id, role)
       - Verify email matches authenticated user

    5. **test_mobile_logout(mobile_api_client, mobile_auth_headers)**:
       - POST /api/auth/logout with Authorization header
       - Verify status code is 200
       - Verify token is invalidated (subsequent request returns 401)

    Use mobile_api_client for direct API calls (no browser).
    Use mobile_auth_headers fixture for authenticated requests.
    Verify response structure matches web API responses (consistency).
  </action>
  <verify>pytest backend/tests/mobile_api/test_mobile_auth.py -v</verify>
  <done>All 5 mobile authentication tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Create mobile API agent execution tests</name>
  <files>backend/tests/mobile_api/test_mobile_agent_execution.py</files>
  <action>
    Create `backend/tests/mobile_api/test_mobile_agent_execution.py`:

    1. **test_mobile_agent_execute_success(mobile_api_client, mobile_auth_headers)**:
       - GET /api/v1/agents (list agents)
       - Select first agent from response
       - POST /api/v1/agents/{agent_id}/execute with payload: {"query": "test query"}
       - Verify status code is 200
       - Verify response contains execution_id
       - Verify response contains agent response

    2. **test_mobile_agent_execute_with_params(mobile_api_client, mobile_auth_headers)**:
       - Execute agent with parameters
       - POST /api/v1/agents/{agent_id}/execute with: {"query": "test", "params": {"key": "value"}}
       - Verify parameters are passed correctly
       - Verify response uses parameters in execution

    3. **test_mobile_agent_stream_execution(mobile_api_client, mobile_auth_headers)**:
       - Execute agent with streaming enabled
       - POST /api/v1/agents/{agent_id}/execute with: {"stream": true}
       - Verify response is streaming (chunked transfer encoding)
       - Parse streaming response and verify chunks contain data

    4. **test_mobile_agent_execution_governance(mobile_api_client, mobile_auth_headers)**:
       - Execute agent that requires governance check
       - Verify governance enforcement (e.g., STUDENT agent blocked from certain actions)
       - Verify governance error message if action not allowed

    5. **test_mobile_agent_execution_history(mobile_api_client, mobile_auth_headers)**:
       - GET /api/v1/agents/executions (list executions)
       - Verify response contains previous executions
       - Verify pagination works (limit, offset)

    Use mobile_auth_headers for authenticated requests.
    Use existing agent IDs from database or create test agents.
    Verify agent execution matches web API behavior (same response structure).
  </action>
  <verify>pytest backend/tests/mobile_api/test_mobile_agent_execution.py -v</verify>
  <done>All 5 mobile agent execution tests pass</done>
</task>

<task type="auto">
  <name>Task 4: Create mobile API workflow execution tests</name>
  <files>backend/tests/mobile_api/test_mobile_workflow_execution.py</files>
  <action>
    Create `backend/tests/mobile_api/test_mobile_workflow_execution.py`:

    1. **test_mobile_workflow_create(mobile_api_client, mobile_auth_headers)**:
       - POST /api/v1/workflows with: {"name": "test workflow", "description": "mobile test"}
       - Verify status code is 201
       - Verify response contains workflow_id
       - Verify response contains workflow details (name, description, created_at)

    2. **test_mobile_workflow_add_skill(mobile_api_client, mobile_auth_headers)**:
       - Create workflow and get workflow_id
       - POST /api/v1/workflows/{workflow_id}/skills with: {"skill_id": "test_skill", "order": 1}
       - Verify status code is 200
       - GET /api/v1/workflows/{workflow_id} and verify skill added

    3. **test_mobile_workflow_execute(mobile_api_client, mobile_auth_headers)**:
       - Create workflow with skills
       - POST /api/v1/workflows/{workflow_id}/execute with: {"input": "test input"}
       - Verify status code is 200
       - Verify response contains execution_id
       - Verify workflow executes skills in correct order

    4. **test_mobile_workflow_dag_validation(mobile_api_client, mobile_auth_headers)**:
       - Create workflow with cyclic dependency (skill A -> B -> A)
       - POST /api/v1/workflows/{workflow_id}/validate
       - Verify validation fails (cycle detected)
       - Verify error message mentions "cycle" or "DAG"

    5. **test_mobile_workflow_execution_history(mobile_api_client, mobile_auth_headers)**:
       - GET /api/v1/workflows/executions (list workflow executions)
       - Verify response contains execution history
       - Verify status, start_time, end_time fields

    Use workflow API endpoints (created in Phase 235-06).
    Verify DAG validation logic works correctly.
    Compare workflow execution results with web API (consistency).
  </action>
  <verify>pytest backend/tests/mobile_api/test_mobile_workflow_execution.py -v</verify>
  <done>All 5 mobile workflow execution tests pass</done>
</task>

<task type="auto">
  <name>Task 5: Create mobile API device features tests</name>
  <files>backend/tests/mobile_api/test_mobile_device_features.py</files>
  <action>
    Create `backend/tests/mobile_api/test_mobile_device_features.py`:

    1. **test_mobile_camera_capture(mobile_api_client, mobile_auth_headers)**:
       - POST /api/v1/device/capture with: {"type": "image", "quality": "high"}
       - Verify status code is 200 (if camera available) or 501 (not available)
       - If available, verify response contains image_data or image_url

    2. **test_mobile_location_get(mobile_api_client, mobile_auth_headers)**:
       - GET /api/v1/device/location
       - Verify status code is 200 (if location available) or 501 (not available)
       - If available, verify response contains latitude, longitude, accuracy

    3. **test_mobile_notifications_send(mobile_api_client, mobile_auth_headers)**:
       - POST /api/v1/device/notifications with: {"title": "test", "body": "test notification"}
       - Verify status code is 200 (if notifications available) or 501 (not available)
       - If available, verify notification was sent

    4. **test_mobile_device_capabilities(mobile_api_client, mobile_auth_headers)**:
       - GET /api/v1/device/capabilities
       - Verify status code is 200
       - Verify response lists available features (camera, location, notifications)
       - Verify each feature has boolean flag (available/not available)

    5. **test_mobile_device_permissions(mobile_api_client, mobile_auth_headers)**:
       - GET /api/v1/device/permissions
       - Verify status code is 200
       - Verify response shows permission status (granted/denied)
       - Test permission request workflow (if applicable)

    Use pytest.skip for device features not available in test environment (CI/CD).
    Use mocking for device features (camera, location) if hardware not available.
    Verify governance enforcement (INTERN+ required for device features).
  </action>
  <verify>pytest backend/tests/mobile_api/test_mobile_device_features.py -v</verify>
  <done>All 5 mobile device features tests pass (or skipped with reason)</done>
</task>

<task type="auto">
  <name>Task 6: Create mobile API testing documentation</name>
  <files>backend/tests/mobile_api/README.md</files>
  <action>
    Create `backend/tests/mobile_api/README.md` with:

    1. **Overview**: Purpose of mobile API testing (API-level, no UI)
    2. **Prerequisites**: FastAPI TestClient, test database
    3. **Running tests**:
       - Run all: `pytest backend/tests/mobile_api/ -v`
       - Run specific: `pytest backend/tests/mobile_api/test_mobile_auth.py -v`
       - Run with coverage: `pytest backend/tests/mobile_api/ --cov=backend/api --cov-report=html`

    4. **Test categories**:
       - Authentication (login, token refresh, logout)
       - Agent execution (sync, streaming, governance)
       - Workflow execution (create, validate, execute)
       - Device features (camera, location, notifications)

    5. **Fixtures**: mobile_api_client, mobile_auth_token, mobile_auth_headers
    6. **Consistency with web API**: Response structure, error codes, status codes
    7. **Troubleshooting**: Common issues (auth failures, missing permissions)

    Include example API call:
    ```bash
    curl -X POST http://localhost:8000/api/auth/login \
      -H "Content-Type: application/json" \
      -d '{"email": "test@example.com", "password": "password"}'
    ```
  </action>
  <verify>cat backend/tests/mobile_api/README.md | grep -E "(Overview|Prerequisites|Test categories|Consistency)"</verify>
  <done>README.md contains complete mobile API testing documentation</done>
</task>

</tasks>

<verification>
Run all mobile API tests:
```bash
pytest backend/tests/mobile_api/ -v
```

Verify:
- All authentication tests pass (login, refresh, logout)
- All agent execution tests pass (execute, stream, governance)
- All workflow execution tests pass (create, validate, execute)
- Device features tests pass or skip appropriately
- API responses match web API responses (consistency)
</verification>

<success_criteria>
Phase 236-04 is complete when:
- mobile_fixtures.py contains 4 fixtures (api_client, auth_token, auth_headers, test_user)
- test_mobile_auth.py has 5 tests passing (login, invalid, refresh, validation, logout)
- test_mobile_agent_execution.py has 5 tests passing (execute, params, stream, governance, history)
- test_mobile_workflow_execution.py has 5 tests passing (create, add_skill, execute, dag, history)
- test_mobile_device_features.py has 5 tests passing (camera, location, notifications, capabilities, permissions)
- README.md documents mobile API testing approach and fixtures
- All tests use API-first approach (TestClient, no Playwright)
- API responses consistent with web API (same structure, status codes)
</success_criteria>

<output>
After completion, create `.planning/phases/236-cross-platform-and-stress-testing/236-04-SUMMARY.md` with:
- Mobile API test results (pass/fail counts)
- Any inconsistencies discovered between mobile and web API
- Device feature availability in test environment
- Recommendations for mobile API improvements
</output>
