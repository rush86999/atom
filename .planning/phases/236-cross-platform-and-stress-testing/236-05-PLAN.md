---
phase: 236-cross-platform-and-stress-testing
plan: 05
type: execute
wave: 3
depends_on: ["235-07"]
files_modified:
  - desktop/tests/test_window_management.py
  - desktop/tests/test_native_features.py
  - desktop/tests/test_cross_platform.py
  - desktop/tests/fixtures/desktop_fixtures.py
  - desktop/tests/README.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Desktop Tauri window management tests work (minimize, maximize, close, resize)"
    - "Desktop native features tests work (file system access, system tray)"
    - "Desktop cross-platform tests work on Windows, macOS, and Linux"
    - "Desktop tests use Tauri-specific APIs (tauri::Manager, tauri::AppHandle)"
    - "Desktop tests verify platform-specific behavior (paths, shortcuts)"
  artifacts:
    - path: "desktop/tests/fixtures/desktop_fixtures.py"
      provides: "Desktop Tauri app fixtures for testing"
      contains: "@pytest.fixture"
      min_lines: 80
    - path: "desktop/tests/test_window_management.py"
      provides: "Window management tests (minimize, maximize, close, resize)"
      contains: "test_window_minimize|test_window_maximize|test_window_resize"
      min_lines: 120
    - path: "desktop/tests/test_native_features.py"
      provides: "Native features tests (file system, system tray, dialogs)"
      contains: "test_file_system_access|test_system_tray"
      min_lines: 120
    - path: "desktop/tests/test_cross_platform.py"
      provides: "Cross-platform tests (Windows, macOS, Linux behavior)"
      contains: "test_platform_specific_paths|test_platform_shortcuts"
      min_lines: 100
    - path: "desktop/tests/README.md"
      provides: "Desktop testing documentation"
      contains: "# Desktop Testing"
      min_lines: 60
  key_links:
    - from: "desktop/tests/fixtures/desktop_fixtures.py"
      to: "desktop/src-tauri/"
      via: "Tauri app initialization and window management"
      pattern: "tauri::Builder|AppHandle"
    - from: "desktop/tests/test_window_management.py"
      to: "desktop/src-tauri/src/window.rs"
      via: "Window management functions"
      pattern: "window.minimize|window.maximize|window.close"
    - from: "desktop/tests/test_native_features.py"
      to: "desktop/src-tauri/src/native.rs"
      via: "Native feature implementations"
      pattern: "fs::|dialog::|tray::"
    - from: "desktop/tests/test_cross_platform.py"
      to: "desktop/src-tauri/src/platform.rs"
      via: "Platform-specific implementations"
      pattern: "cfg!(windows)|cfg!(macos)|cfg!(linux)"
---

<objective>
Create desktop Tauri tests for window management, native features (file system, system tray), and cross-platform behavior to verify desktop app functionality on Windows, macOS, and Linux.

Purpose: Ensure desktop app works correctly across platforms with proper window management, native feature integration, and platform-specific behavior.
Output: Desktop fixtures, 3 desktop test files, and README documentation.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/236-cross-platform-and-stress-testing/236-RESEARCH.md
@.planning/phases/235-canvas-and-workflow-e2e/235-07-SUMMARY.md
@desktop/src-tauri/tauri.conf.json
@desktop/src-tauri/src/main.rs
@desktop/src-tauri/src/window.rs
@desktop/src-tauri/src/native.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create desktop Tauri app fixtures</name>
  <files>desktop/tests/fixtures/desktop_fixtures.py</files>
  <action>
    Create `desktop/tests/fixtures/desktop_fixtures.py` with Tauri app setup:

    1. **tauri_app fixture**:
       - Build Tauri app in test mode (cargo build --tests)
       - Use subprocess to spawn tauri dev process
       - Wait for app to start (check for window or listen on port)
       - Return app process and window handle
       - Cleanup: terminate app process after test

    2. **tauri_window fixture**:
       - Use tauri_app fixture
       - Get main window handle via Tauri API
       - Return window object for manipulation (resize, minimize, etc.)

    3. **tauri_app_handle fixture**:
       - Use tauri_app fixture
       - Get AppHandle via Tauri API
       - Return handle for native feature access (fs, dialogs, tray)

    4. **platform_info fixture**:
       - Detect current platform (Windows, macOS, Linux)
       - Use platform module: platform.system()
       - Return dict: {"platform": "darwin"|"linux"|"windows", "is_test": bool}

    Use pytest fixtures with proper setup/teardown (yield for cleanup).
    Use subprocess.Popen for spawning Tauri app with timeout.
    Add pytest.skip for platforms where Tauri not configured.
  </action>
  <verify>grep -E "(tauri_app|tauri_window|tauri_app_handle|platform_info)" desktop/tests/fixtures/desktop_fixtures.py</verify>
  <done>All 4 desktop fixtures exist with Tauri app initialization</done>
</task>

<task type="auto">
  <name>Task 2: Create window management tests</name>
  <files>desktop/tests/test_window_management.py</files>
  <action>
    Create `desktop/tests/test_window_management.py`:

    1. **test_window_minimize(tauri_window)**:
       - Get initial window state (visible, not minimized)
       - Call window.minimize() via Tauri API
       - Verify window is minimized (state check)
       - Call window.unminimize()
       - Verify window is restored

    2. **test_window_maximize(tauri_window)**:
       - Get initial window size
       - Call window.maximize() via Tauri API
       - Verify window is maximized (size = screen size)
       - Call window.unmaximize()
       - Verify window restored to original size

    3. **test_window_close(tauri_app)**:
       - Get main window handle
       - Call window.close() via Tauri API
       - Verify window is closed (not visible, not in window list)
       - Verify app is still running (background process)

    4. **test_window_resize(tauri_window)**:
       - Get initial window size
       - Call window.resize() with new size (width: 1200, height: 800)
       - Verify window size changed to target size
       - Restore original size

    5. **test_window_title(tauri_window)**:
       - Get initial window title
       - Call window.set_title("Test Title")
       - Verify window title changed to "Test Title"
       - Restore original title

    6. **test_window_always_on_top(tauri_window)**:
       - Call window.set_always_on_top(true)
       - Verify window is always on top
       - Call window.set_always_on_top(false)
       - Verify normal z-order restored

    Use Tauri window API for all window operations.
    Verify state changes via Tauri API getters.
    Test on at least one platform (macOS, Linux, or Windows).
  </action>
  <verify>pytest desktop/tests/test_window_management.py -v</verify>
  <done>All 6 window management tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Create native features tests</name>
  <files>desktop/tests/test_native_features.py</files>
  <action>
    Create `desktop/tests/test_native_features.py`:

    1. **test_file_system_access(tauri_app_handle)**:
       - Use Tauri fs API to read file (test file in desktop/tests/data/)
       - Verify file content matches expected content
       - Use Tauri fs API to write file (temp file)
       - Verify file written successfully
       - Cleanup: delete test file

    2. **test_file_system_dialog(tauri_app_handle)**:
       - Use Tauri dialog API to open file dialog
       - Verify dialog opens (may require manual interaction or mocking)
       - Use Tauri dialog API to save file dialog
       - Verify dialog opens and returns path (mocked)

    3. **test_system_tray_icon(tauri_app)**:
       - Verify system tray icon exists
       - Click tray icon (if possible via automation)
       - Verify tray menu appears (quit, show, hide options)
       - Test menu item clicks

    4. **test_system_tray_menu_actions(tauri_app)**:
       - Click "Show" menu item in tray
       - Verify window becomes visible
       - Click "Hide" menu item in tray
       - Verify window hides
       - Click "Quit" menu item in tray
       - Verify app terminates

    5. **test_native_notifications(tauri_app_handle)**:
       - Use Tauri notification API to send notification
       - Verify notification sent (OS notification appears)
       - Verify notification contains title and body

    Use Tauri APIs: tauri::api::fs, tauri::api::dialog, tauri::api::notification.
    Mock dialogs that require user interaction (file open/save).
    Use pytest.mark.skipif for features not available on current platform.
  </action>
  <verify>pytest desktop/tests/test_native_features.py -v</verify>
  <done>All 5 native features tests pass (or skipped with reason)</done>
</task>

<task type="auto">
  <name>Task 4: Create cross-platform tests</name>
  <files>desktop/tests/test_cross_platform.py</files>
  <action>
    Create `desktop/tests/test_cross_platform.py`:

    1. **test_platform_specific_paths(platform_info)**:
       - On Windows: verify app data in %APPDATA% or %LOCALAPPDATA%
       - On macOS: verify app data in ~/Library/Application Support/
       - On Linux: verify app data in ~/.config/ or ~/.local/share/
       - Use platform_info fixture to detect platform
       - Verify path construction is correct

    2. **test_platform_shortcuts(platform_info, tauri_window)**:
       - On Windows: test Win+Alt+Q shortcut (quit)
       - On macOS: test Cmd+Q shortcut (quit), Cmd+W (close window)
       - On Linux: test Ctrl+Q shortcut (quit)
       - Simulate keyboard shortcuts via Tauri or system API
       - Verify shortcuts work correctly

    3. **test_platform_menu_bar(platform_info)**:
       - On macOS: verify app menu exists (Apple menu, app menu)
       - On Windows: verify menu bar exists (if implemented)
       - On Linux: verify menu bar exists (if implemented)
       - Verify menu items are correct (File, Edit, View, Help)

    4. **test_platform_window_decorations(platform_info, tauri_window)**:
       - On Windows: verify standard window decorations (close, minimize, maximize buttons)
       - On macOS: verify standard macOS decorations (red, yellow, green buttons)
       - On Linux: verify window decorations match theme (GNOME/KDE)
       - Test window decorations work correctly

    5. **test_platform_startup_behavior(platform_info)**:
       - On macOS: verify app starts with single window (no hidden windows)
       - On Windows: verify app starts with main window
       - On Linux: verify app starts with main window
       - Verify no startup errors or crashes

    Use platform-specific conditionals: cfg!(windows), cfg!(macos), cfg!(target_os = "linux").
    Add pytest.mark.skipif for platforms not under test (e.g., skip macOS tests on Linux).
    Document platform-specific behavior in test docstrings.
  </action>
  <verify>pytest desktop/tests/test_cross_platform.py -v</verify>
  <done>All 5 cross-platform tests pass (or skipped with reason)</done>
</task>

<task type="auto">
  <name>Task 5: Create desktop testing documentation</name>
  <files>desktop/tests/README.md</files>
  <action>
    Create `desktop/tests/README.md` with:

    1. **Overview**: Purpose of desktop testing (Tauri app, cross-platform)
    2. **Prerequisites**: Rust, Cargo, Tauri CLI, platform-specific dependencies
    3. **Running tests**:
       - Run all: `pytest desktop/tests/ -v`
       - Run specific: `pytest desktop/tests/test_window_management.py -v`
       - Run on platform: `pytest desktop/tests/ -v --platform=macos`

    4. **Test categories**:
       - Window management (minimize, maximize, resize, close)
       - Native features (file system, dialogs, system tray, notifications)
       - Cross-platform (paths, shortcuts, menu bar, decorations)

    5. **Fixtures**: tauri_app, tauri_window, tauri_app_handle, platform_info
    6. **Platform-specific behavior**: Document differences between Windows, macOS, Linux
    7. **Troubleshooting**: Common issues (build failures, missing dependencies)

    Include example Tauri build command:
    ```bash
    cd desktop/src-tauri
    cargo build
    cargo test
    ```

    Include platform-specific setup instructions:
    - macOS: No additional dependencies
    - Linux: install libwebkit2gtk, libgtk-3
    - Windows: Install WebView2 runtime
  </action>
  <verify>cat desktop/tests/README.md | grep -E "(Overview|Prerequisites|Test categories|Platform-specific)"</verify>
  <done>README.md contains complete desktop testing documentation</done>
</task>

</tasks>

<verification>
Run all desktop tests:
```bash
pytest desktop/tests/ -v
```

Verify:
- All window management tests pass (minimize, maximize, resize, close, title, always on top)
- All native features tests pass (fs access, dialogs, system tray, notifications)
- All cross-platform tests pass (or skipped appropriately)
- Tests use Tauri-specific APIs (not web APIs)
- Platform-specific behavior is correct
</verification>

<success_criteria>
Phase 236-05 is complete when:
- desktop_fixtures.py contains 4 fixtures (tauri_app, tauri_window, tauri_app_handle, platform_info)
- test_window_management.py has 6 tests passing (minimize, maximize, close, resize, title, always on top)
- test_native_features.py has 5 tests passing (fs access, fs dialog, tray icon, tray actions, notifications)
- test_cross_platform.py has 5 tests passing (paths, shortcuts, menu bar, decorations, startup)
- README.md documents desktop testing approach, fixtures, and platform-specific behavior
- All tests use Tauri APIs (tauri::Manager, tauri::AppHandle, tauri::api::fs, etc.)
- Platform-specific tests use cfg! macros or pytest.mark.skipif appropriately
</success_criteria>

<output>
After completion, create `.planning/phases/236-cross-platform-and-stress-testing/236-05-SUMMARY.md` with:
- Desktop test results (pass/fail counts per platform)
- Platform-specific issues discovered
- Cross-platform consistency verification
- Recommendations for desktop app improvements
</output>
