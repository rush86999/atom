---
phase: 236-cross-platform-and-stress-testing
plan: 06
type: execute
wave: 3
depends_on: []
files_modified:
  - frontend-nextjs/tests/visual/test_visual_regression_login.py
  - frontend-nextjs/tests/visual/test_visual_regression_dashboard.py
  - frontend-nextjs/tests/visual/test_visual_regression_agents.py
  - frontend-nextjs/tests/visual/test_visual_regression_canvas.py
  - frontend-nextjs/tests/visual/test_visual_regression_workflows.py
  - frontend-nextjs/tests/visual/fixtures/percy_fixtures.py
  - frontend-nextjs/tests/visual/README.md
  - .percy.yml
autonomous: true
user_setup:
  - service: percy
    why: "Visual regression testing with Percy"
    env_vars:
      - name: PERCY_TOKEN
        source: "Percy Dashboard -> Project Settings -> Token"
    dashboard_config:
      - task: "Create Percy project"
        location: "https://percy.io/create-project"
      - task: "Get PERCY_TOKEN"
        location: "Percy Dashboard -> Project Settings"

must_haves:
  truths:
    - "Percy is installed and configured for visual regression testing"
    - "Percy visual tests cover 20+ critical pages (login, dashboard, agents, canvas, workflows)"
    - "Visual diffs are detected and reported in Percy dashboard"
    - "Percy integration works with CI/CD pipeline (GitHub Actions)"
    - "Screenshots are captured for all major UI states and components"
    - "Visual tests run on every PR and block merging on visual regressions"
  artifacts:
    - path: "frontend-nextjs/tests/visual/fixtures/percy_fixtures.py"
      provides: "Percy fixture and helper functions"
      contains: "@pytest.fixture"
      min_lines: 60
    - path: "frontend-nextjs/tests/visual/test_visual_regression_login.py"
      provides: "Login page visual regression tests"
      contains: "test_visual_login_page|test_visual_login_error"
      min_lines: 80
    - path: "frontend-nextjs/tests/visual/test_visual_regression_dashboard.py"
      provides: "Dashboard page visual regression tests"
      contains: "test_visual_dashboard|test_visual_dashboard_empty"
      min_lines: 100
    - path: "frontend-nextjs/tests/visual/test_visual_regression_agents.py"
      provides: "Agents page visual regression tests"
      contains: "test_visual_agents_list|test_visual_agent_detail"
      min_lines: 100
    - path: "frontend-nextjs/tests/visual/test_visual_regression_canvas.py"
      provides: "Canvas pages visual regression tests"
      contains: "test_visual_canvas_chart|test_visual_canvas_sheet|test_visual_canvas_form"
      min_lines: 120
    - path: "frontend-nextjs/tests/visual/test_visual_regression_workflows.py"
      provides: "Workflows page visual regression tests"
      contains: "test_visual_workflows_list|test_visual_workflow_builder"
      min_lines: 100
    - path: "frontend-nextjs/tests/visual/README.md"
      provides: "Visual regression testing documentation"
      contains: "# Visual Regression Testing"
      min_lines: 60
    - path: ".percy.yml"
      provides: "Percy configuration file"
      contains: "version:"
      min_lines: 30
  key_links:
    - from: "frontend-nextjs/tests/visual/fixtures/percy_fixtures.py"
      to: "frontend-nextjs/playwright.config.ts"
      via: "Playwright configuration for Percy integration"
      pattern: "@percy/playwright|percySnapshot"
    - from: "frontend-nextjs/tests/visual/test_visual_regression_*.py"
      to: ".percy.yml"
      via: "Percy configuration for snapshot widths and ignore regions"
      pattern: "widths:|ignore:"
    - from: "frontend-nextjs/tests/visual/"
      to: ".github/workflows/"
      via: "Percy CI/CD integration (GitHub Actions)"
      pattern: "percy-cli|percy exec"
---

<objective>
Install and configure Percy for visual regression testing, create visual tests for 20+ critical pages across login, dashboard, agents, canvas, and workflows, and integrate with CI/CD pipeline.

Purpose: Catch visual regressions (CSS/layout changes, broken components, cross-browser issues) before they reach production by comparing screenshots against baseline.
Output: Percy fixtures, 5 visual test files, Percy configuration, README documentation, and CI/CD integration.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/236-cross-platform-and-stress-testing/236-RESEARCH.md
@.planning/phases/233-test-infrastructure-foundation/233-05-SUMMARY.md
@frontend-nextjs/playwright.config.ts
@frontend-nextjs/tests/e2e_ui/conftest.py
@frontend-nextjs/app/login/page.tsx
@frontend-nextjs/app/dashboard/page.tsx
@frontend-nextjs/app/agents/page.tsx
@frontend-nextjs/app/canvas/page.tsx
@frontend-nextjs/app/workflows/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Percy and create configuration</name>
  <files>.percy.yml</files>
  <action>
    Create `.percy.yml` with Percy configuration:

    1. **Install Percy CLI** (via Bash):
       ```bash
       npm install --save-dev @percy/cli @percy/playwright
       ```
       Or use global install: `npm install -g @percy/cli`

    2. **Create .percy.yml** with:
       ```yaml
       version: 2
       snapshot:
         widths: [375, 768, 1280]  # Mobile, tablet, desktop
         min-height: 1024
         percy-css: |
           /* Hide dynamic content */
           .timestamp { display: none !important; }
           .session-id { display: none !important; }
         ignore:
           - /* Ignore dynamic timestamps */
           - '.timestamp'
           - '.session-id'
           - '.loading-spinner'
       discovery:
         allowed-hostnames:
           - localhost
           - localhost:3000
       agent:
           asset-discovery:
             network-idle-timeout: 100
       ```

    3. **Configure Playwright for Percy**:
       - Install @percy/playwright
       - Add Percy to playwright.config.ts plugins (if applicable)

    DO NOT use @percy/puppeteer (use Playwright integration).
    DO NOT skip snapshot widths configuration (need responsive testing).
  </action>
  <verify>cat .percy.yml | grep -E "(version:|widths:|min-height:|percy-css:)"</verify>
  <done>Percy is installed, .percy.yml configured with widths and ignore rules</done>
</task>

<task type="auto">
  <name>Task 2: Create Percy fixtures</name>
  <files>frontend-nextjs/tests/visual/fixtures/percy_fixtures.py</files>
  <action>
    Create `frontend-nextjs/tests/visual/fixtures/percy_fixtures.py`:

    1. **percy_page fixture**:
       - Create Playwright page with Percy enabled
       - Use playwright.sync_playwright() context
       - Initialize Percy with percySnapshot helper
       - Return page object

    2. **percy_snapshot helper function**:
       - Accept page, name (string), widths (list)
       - Call percySnapshot(page, name, widths)
       - Add optional parameters: ignore_regions, custom_css

    3. **authenticated_percy_page fixture**:
       - Use percy_page fixture
       - Login via API (not UI) for faster setup
       - Navigate to dashboard
       - Return authenticated page

    4. **percy_test_data fixture**:
       - Create test data for visual tests
       - Return dict with test agents, test canvas, test workflows
       - Cleanup after test (delete test data)

    Use Playwright Python API (sync API preferred for tests).
    Use @percy/playwright npm package for percySnapshot function.
    Use pytest fixtures with proper setup/teardown.
  </action>
  <verify>grep -E "(percy_page|percy_snapshot|authenticated_percy_page)" frontend-nextjs/tests/visual/fixtures/percy_fixtures.py</verify>
  <done>All 4 Percy fixtures exist with percySnapshot helper</done>
</task>

<task type="auto">
  <name>Task 3: Create login page visual regression tests</name>
  <files>frontend-nextjs/tests/visual/test_visual_regression_login.py</files>
  <action>
    Create `frontend-nextjs/tests/visual/test_visual_regression_login.py`:

    1. **test_visual_login_page(percy_page)**:
       - Navigate to /login
       - Wait for page load
       - Take Percy snapshot: "Login Page"
       - Verify login form is visible (email, password inputs, submit button)
       - Verify logo and branding elements present

    2. **test_visual_login_error(percy_page)**:
       - Navigate to /login
       - Fill email with invalid format
       - Trigger validation error
       - Take Percy snapshot: "Login Page - Validation Error"
       - Verify error message is visible and styled correctly

    3. **test_visual_login_loading(percy_page)**:
       - Navigate to /login
       - Fill credentials and submit
       - Intercept response to delay loading state
       - Take Percy snapshot: "Login Page - Loading"
       - Verify loading spinner or indicator is present

    4. **test_visual_login_mobile(percy_page)**:
       - Set viewport to mobile size (375x667)
       - Navigate to /login
       - Take Percy snapshot: "Login Page - Mobile"
       - Verify mobile layout (stacked inputs, full-width button)

    Use percy_page fixture for Playwright page with Percy.
    Use percy_snapshot(page, name) helper function.
    Test multiple states (default, error, loading, mobile).
  </action>
  <verify>pytest frontend-nextjs/tests/visual/test_visual_regression_login.py -v</verify>
  <done>All 4 login visual tests pass and snapshots uploaded to Percy</done>
</task>

<task type="auto">
  <name>Task 4: Create dashboard visual regression tests</name>
  <files>frontend-nextjs/tests/visual/test_visual_regression_dashboard.py</files>
  <action>
    Create `frontend-nextjs/tests/visual/test_visual_regression_dashboard.py`:

    1. **test_visual_dashboard(authenticated_percy_page)**:
       - Navigate to /dashboard
       - Wait for page load
       - Take Percy snapshot: "Dashboard"
       - Verify dashboard layout (sidebar, main content, header)

    2. **test_visual_dashboard_empty(authenticated_percy_page)**:
       - Create fresh user with no data
       - Navigate to /dashboard
       - Take Percy snapshot: "Dashboard - Empty State"
       - Verify empty state message and CTA

    3. **test_visual_dashboard_with_data(authenticated_percy_page)**:
       - Populate dashboard with test data (agents, canvas, workflows)
       - Navigate to /dashboard
       - Take Percy snapshot: "Dashboard - With Data"
       - Verify data cards and stats visible

    4. **test_visual_dashboard_sidebar(authenticated_percy_page)**:
       - Navigate to /dashboard
       - Expand/collapse sidebar
       - Take Percy snapshot: "Dashboard - Sidebar Expanded"
       - Take Percy snapshot: "Dashboard - Sidebar Collapsed"
       - Verify sidebar transitions work correctly

    5. **test_visual_dashboard_mobile(authenticated_percy_page)**:
       - Set viewport to mobile size
       - Navigate to /dashboard
       - Take Percy snapshot: "Dashboard - Mobile"
       - Verify mobile layout (hamburger menu, bottom nav if applicable)

    Use authenticated_percy_page fixture for authenticated session.
    Create test data using percy_test_data fixture.
    Test multiple viewport sizes (mobile, tablet, desktop).
  </action>
  <verify>pytest frontend-nextjs/tests/visual/test_visual_regression_dashboard.py -v</verify>
  <done>All 5 dashboard visual tests pass and snapshots uploaded to Percy</done>
</task>

<task type="auto">
  <name>Task 5: Create agents and canvas visual regression tests</name>
  <files>frontend-nextjs/tests/visual/test_visual_regression_agents.py, frontend-nextjs/tests/visual/test_visual_regression_canvas.py</files>
  <action>
    Create agents and canvas visual tests:

    **test_visual_regression_agents.py**:

    1. **test_visual_agents_list(authenticated_percy_page)**:
       - Navigate to /agents
       - Take snapshot: "Agents List"
       - Verify agent cards, filters, search

    2. **test_visual_agent_detail(authenticated_percy_page)**:
       - Navigate to /agents/{agent_id}
       - Take snapshot: "Agent Detail"
       - Verify agent info, execute button, history

    3. **test_visual_agent_execution(authenticated_percy_page)**:
       - Execute agent
       - Wait for response
       - Take snapshot: "Agent Execution Result"
       - Verify response display, formatting

    **test_visual_regression_canvas.py**:

    1. **test_visual_canvas_chart(authenticated_percy_page)**:
       - Present chart canvas
       - Take snapshot: "Canvas - Chart"
       - Verify chart rendering, axes, labels

    2. **test_visual_canvas_sheet(authenticated_percy_page)**:
       - Present sheet canvas
       - Take snapshot: "Canvas - Sheet"
       - Verify data grid, pagination, sorting

    3. **test_visual_canvas_form(authenticated_percy_page)**:
       - Present form canvas
       - Take snapshot: "Canvas - Form"
       - Verify form fields, validation, submit button

    4. **test_visual_canvas_docs(authenticated_percy_page)**:
       - Present docs canvas
       - Take snapshot: "Canvas - Docs"
       - Verify markdown rendering, formatting

    5. **test_visual_canvas_email(authenticated_percy_page)**:
       - Present email canvas
       - Take snapshot: "Canvas - Email"
       - Verify email fields (to, subject, body)

    6. **test_visual_canvas_terminal(authenticated_percy_page)**:
       - Present terminal canvas
       - Take snapshot: "Canvas - Terminal"
       - Verify terminal output, scrollable area

    7. **test_visual_canvas_coding(authenticated_percy_page)**:
       - Present coding canvas
       - Take snapshot: "Canvas - Coding"
       - Verify syntax highlighting, line numbers

    Use authenticated_percy_page for all tests.
    Test all 7 canvas types for comprehensive coverage.
    Ignore dynamic content (timestamps, session IDs) in Percy config.
  </action>
  <verify>
    pytest frontend-nextjs/tests/visual/test_visual_regression_agents.py -v
    pytest frontend-nextjs/tests/visual/test_visual_regression_canvas.py -v
  </verify>
  <done>All 10 visual tests pass (3 agents + 7 canvas) and snapshots uploaded</done>
</task>

<task type="auto">
  <name>Task 6: Create workflows visual regression tests</name>
  <files>frontend-nextjs/tests/visual/test_visual_regression_workflows.py</files>
  <action>
    Create `frontend-nextjs/tests/visual/test_visual_regression_workflows.py`:

    1. **test_visual_workflows_list(authenticated_percy_page)**:
       - Navigate to /workflows
       - Take snapshot: "Workflows List"
       - Verify workflow cards, create button, filters

    2. **test_visual_workflow_builder_empty(authenticated_percy_page)**:
       - Navigate to /workflows/new
       - Take snapshot: "Workflow Builder - Empty"
       - Verify empty canvas, skill palette, drag-drop hints

    3. **test_visual_workflow_builder_with_skills(authenticated_percy_page)**:
       - Create workflow with 2-3 skills
       - Take snapshot: "Workflow Builder - With Skills"
       - Verify skill nodes, connections, DAG visualization

    4. **test_visual_workflow_execution(authenticated_percy_page)**:
       - Execute workflow
       - Take snapshot: "Workflow Execution - Running"
       - Verify progress indicators, skill status

    5. **test_visual_workflow_results(authenticated_percy_page)**:
       - Wait for workflow completion
       - Take snapshot: "Workflow Execution - Results"
       - Verify results display, success/failure indicators

    Use authenticated_percy_page fixture.
    Test workflow builder with various skill compositions.
    Test workflow execution states (idle, running, completed, failed).
  </action>
  <verify>pytest frontend-nextjs/tests/visual/test_visual_regression_workflows.py -v</verify>
  <done>All 5 workflow visual tests pass and snapshots uploaded</done>
</task>

<task type="auto">
  <name>Task 7: Create visual regression testing documentation</name>
  <files>frontend-nextjs/tests/visual/README.md</files>
  <action>
    Create `frontend-nextjs/tests/visual/README.md` with:

    1. **Overview**: Purpose of visual regression testing (Percy, catch UI regressions)
    2. **Prerequisites**: Percy account, PERCY_TOKEN environment variable, Percy CLI installed
    3. **Percy setup**:
       - Install: `npm install --save-dev @percy/cli @percy/playwright`
       - Configure: .percy.yml (widths, ignore rules)
       - Set token: `export PERCY_TOKEN=your_token_here`

    4. **Running tests**:
       - Run locally: `pytest frontend-nextjs/tests/visual/ -v`
       - Run with Percy: `percy exec -- pytest frontend-nextjs/tests/visual/ -v`
       - Run specific: `pytest frontend-nextjs/tests/visual/test_visual_regression_login.py -v`

    5. **Test coverage**: List all pages tested (login, dashboard, agents, canvas, workflows)
    6. **CI/CD integration**: GitHub Actions workflow example
    7. **Reviewing diffs**: Percy dashboard, approving/rejecting changes
    8. **Troubleshooting**: Common issues (token missing, snapshot failures, ignored regions)

    Include Percy dashboard link: https://percy.io
    Include snapshot count: 20+ critical pages tested
  </action>
  <verify>cat frontend-nextjs/tests/visual/README.md | grep -E "(Overview|Prerequisites|Percy setup|Running tests)"</verify>
  <done>README.md contains complete visual regression testing documentation</done>
</task>

</tasks>

<verification>
Run all visual regression tests with Percy:
```bash
export PERCY_TOKEN=your_token_here
percy exec -- pytest frontend-nextjs/tests/visual/ -v
```

Verify:
- All tests pass and snapshots uploaded to Percy
- Percy dashboard shows all snapshots
- Visual diffs are detected (make a small CSS change and re-run)
- Tests run on multiple widths (mobile, tablet, desktop)
</verification>

<success_criteria>
Phase 236-06 is complete when:
- Percy CLI is installed (@percy/cli, @percy/playwright)
- .percy.yml configured with widths (375, 768, 1280) and ignore rules
- percy_fixtures.py contains 4 fixtures (percy_page, percy_snapshot, authenticated_page, test_data)
- test_visual_regression_login.py has 4 tests passing (page, error, loading, mobile)
- test_visual_regression_dashboard.py has 5 tests passing (default, empty, with_data, sidebar, mobile)
- test_visual_regression_agents.py has 3 tests passing (list, detail, execution)
- test_visual_regression_canvas.py has 7 tests passing (all canvas types)
- test_visual_regression_workflows.py has 5 tests passing (list, builder_empty, builder_skills, execution, results)
- README.md documents Percy setup, running tests, and reviewing diffs
- Total 24+ visual snapshots uploaded to Percy
</success_criteria>

<output>
After completion, create `.planning/phases/236-cross-platform-and-stress-testing/236-06-SUMMARY.md` with:
- Percy setup complete with token configuration
- Visual test results (24+ snapshots)
- Any visual regressions detected
- Percy dashboard link for reviewing snapshots
</output>
