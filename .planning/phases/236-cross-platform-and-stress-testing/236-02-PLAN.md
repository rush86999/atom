---
phase: 236-cross-platform-and-stress-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/e2e_ui/tests/test_network_slow_3g.py
  - backend/tests/e2e_ui/tests/test_network_offline.py
  - backend/tests/e2e_ui/tests/test_network_database_drop.py
  - backend/tests/e2e_ui/tests/test_network_api_timeout.py
  - backend/tests/e2e_ui/fixtures/network_fixtures.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Slow 3G connection simulation works (Playwright context.route() with throttling)"
    - "Offline mode testing handles network disconnection gracefully"
    - "Database connection drop tests verify error recovery and reconnection logic"
    - "API timeout tests verify proper timeout handling and error messages"
    - "Network tests use Playwright's network interception capabilities"
    - "Error handling is verified across authentication, agent execution, and canvas operations"
  artifacts:
    - path: "backend/tests/e2e_ui/fixtures/network_fixtures.py"
      provides: "Network simulation fixtures (slow 3G, offline, timeout)"
      contains: "@pytest.fixture"
      min_lines: 80
    - path: "backend/tests/e2e_ui/tests/test_network_slow_3g.py"
      provides: "Slow 3G connection simulation tests"
      contains: "test_slow_3g_login|test_slow_3g_agent_execution"
      min_lines: 100
    - path: "backend/tests/e2e_ui/tests/test_network_offline.py"
      provides: "Offline mode testing"
      contains: "test_offline_mode|test_network_reconnect"
      min_lines: 100
    - path: "backend/tests/e2e_ui/tests/test_network_database_drop.py"
      provides: "Database connection drop and recovery tests"
      contains: "test_database_connection_drop"
      min_lines: 120
    - path: "backend/tests/e2e_ui/tests/test_network_api_timeout.py"
      provides: "API timeout handling tests"
      contains: "test_api_timeout_handling"
      min_lines: 100
  key_links:
    - from: "backend/tests/e2e_ui/fixtures/network_fixtures.py"
      to: "backend/tests/e2e_ui/conftest.py"
      via: "Network fixtures registered in conftest"
      pattern: "pytest_plugins.*network_fixtures"
    - from: "backend/tests/e2e_ui/tests/test_network_*.py"
      to: "playwright"
      via: "Playwright context.route() for network interception"
      pattern: "context.route|page.route"
    - from: "backend/tests/e2e_ui/tests/test_network_database_drop.py"
      to: "backend/core/database.py"
      via: "Database connection pool and error handling"
      pattern: "engine.connect|SessionLocal"
---

<objective>
Create comprehensive network simulation and failure injection tests using Playwright's network interception to verify error handling under slow 3G connections, offline mode, database connection drops, and API timeouts.

Purpose: Ensure the platform gracefully handles network failures and degraded conditions with proper error messages and recovery mechanisms.
Output: Network fixtures, 4 network failure test files covering slow 3G, offline, database drops, and API timeouts.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/236-cross-platform-and-stress-testing/236-RESEARCH.md
@.planning/phases/233-test-infrastructure-foundation/233-05-SUMMARY.md
@backend/tests/e2e_ui/conftest.py
@backend/tests/e2e_ui/fixtures/auth_fixtures.py
@backend/core/database.py
@backend/core/auth_endpoints.py
@backend/core/atom_agent_endpoints.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create network simulation fixtures</name>
  <files>backend/tests/e2e_ui/fixtures/network_fixtures.py</files>
  <action>
    Create `backend/tests/e2e_ui/fixtures/network_fixtures.py` with network simulation capabilities:

    1. **slow_3g_context fixture**:
       - Playwright context with slow 3G throttling
       - Download: 500 Kbps, Upload: 500 Kbps, Latency: 400ms
       - Use `browser.new_context()` with offline=False and throttling profile
       - Return context with page ready for testing

    2. **offline_mode_context fixture**:
       - Start with normal context
       - Yield context and control function
       - Control function: `go_offline()` sets context.offline=True
       - Control function: `come_online()` sets context.offline=False

    3. **timeout_api_context fixture**:
       - Context with modified routing to add delays to specific endpoints
       - Use `context.route()` to intercept API calls and add 30s delay
       - Target endpoints: /api/v1/agents/execute, /api/v1/canvas/present

    4. **database_drop_simulation fixture**:
       - Yield page and control function
       - Control function: `simulate_db_drop()` stops PostgreSQL/SQLite temporarily
       - Control function: `restore_db()` restarts database
       - Use subprocess to control database service

    DO NOT use environment variables for network simulation (use Playwright context API instead).
    Use pytest context managers for proper cleanup.
  </action>
  <verify>grep -E "(slow_3g_context|offline_mode_context|timeout_api_context)" backend/tests/e2e_ui/fixtures/network_fixtures.py</verify>
  <done>All 4 network fixtures exist with proper setup/teardown logic</done>
</task>

<task type="auto">
  <name>Task 2: Create slow 3G connection simulation tests</name>
  <files>backend/tests/e2e_ui/tests/test_network_slow_3g.py</files>
  <action>
    Create `backend/tests/e2e_ui/tests/test_network_slow_3g.py`:

    1. **test_slow_3g_login_success(slow_3g_context, test_user)**:
       - Use slow_3g_context (400ms latency, 500 Kbps)
       - Navigate to login page
       - Fill credentials and submit
       - Verify login succeeds (may take 5-10 seconds)
       - Verify error message NOT shown
       - Assert: page.wait_for_url("**/dashboard", timeout=15000)

    2. **test_slow_3g_agent_execution(slow_3g_context, authenticated_page_api)**:
       - Use authenticated page with slow 3G
       - Navigate to agents page
       - Select agent and execute
       - Verify agent execution completes (may take longer)
       - Verify response received within 30s

    3. **test_slow_3g_canvas_rendering(slow_3g_context, authenticated_page_api)**:
       - Use authenticated page with slow 3G
       - Present chart canvas (use existing canvas presentation helper)
       - Verify canvas renders within 20s timeout
       - Verify chart content loads correctly

    4. **test_slow_3g_error_handling(slow_3g_context)**:
       - Use slow 3G and navigate to non-existent page
       - Verify 404 error page appears
       - Verify error message is clear and actionable

    Use `slow_3g_context` fixture from network_fixtures.py.
    Use longer timeouts (wait_for_timeout, page.wait_for_url with timeout=30000).
  </action>
  <verify>pytest backend/tests/e2e_ui/tests/test_network_slow_3g.py -v --timeout=60</verify>
  <done>All 4 slow 3G tests pass with extended timeouts</done>
</task>

<task type="auto">
  <name>Task 3: Create offline mode testing</name>
  <files>backend/tests/e2e_ui/tests/test_network_offline.py</files>
  <action>
    Create `backend/tests/e2e_ui/tests/test_network_offline.py`:

    1. **test_offline_mode_during_login(offline_mode_context)**:
       - Navigate to login page
       - Call go_offline() control function
       - Try to fill credentials and submit
       - Verify error message appears (network error, connection failed)
       - Verify NOT redirected to dashboard
       - Call come_online() and verify login works

    2. **test_offline_mode_during_agent_execution(offline_mode_context, authenticated_page_api)**:
       - Start on dashboard (authenticated)
       - Navigate to agents page
       - Call go_offline() control function
       - Try to execute agent
       - Verify error message (network error, request failed)
       - Call come_online() and retry execution
       - Verify agent executes successfully after reconnection

    3. **test_network_reconnect_after_offline(offline_mode_context)**:
       - Login successfully
       - Go offline
       - Navigate to different page (verify cached content or error)
       - Come back online
       - Verify network requests work again
       - Verify session still valid (token not cleared)

    4. **test_offline_mode_indicator(offline_mode_context)**:
       - Go offline while authenticated
       - Verify UI shows offline indicator (if implemented) or graceful degradation
       - Verify no console errors or crashes
       - Come online and verify normal operation resumes

    Use `offline_mode_context` fixture which provides go_offline() and come_online() control functions.
    Verify error messages are user-friendly (not technical stack traces).
  </action>
  <verify>pytest backend/tests/e2e_ui/tests/test_network_offline.py -v</verify>
  <done>All 4 offline mode tests pass with proper error messages</done>
</task>

<task type="auto">
  <name>Task 4: Create database connection drop tests</name>
  <files>backend/tests/e2e_ui/tests/test_network_database_drop.py</files>
  <action>
    Create `backend/tests/e2e_ui/tests/test_network_database_drop.py`:

    1. **test_database_connection_drop_during_login(database_drop_simulation, test_user)**:
       - Start login flow
       - Call simulate_db_drop() control function (stops database)
       - Try to submit login form
       - Verify error message (database connection error, service unavailable)
       - Verify 503 or 500 status code
       - Call restore_db() control function
       - Retry login and verify success

    2. **test_database_connection_drop_during_agent_execution(database_drop_simulation, authenticated_page_api)**:
       - Start authenticated session
       - Begin agent execution (POST /api/v1/agents/execute)
       - Simulate database drop during execution
       - Verify error handling (graceful failure, no crash)
       - Restore database and verify system recovers

    3. **test_database_connection_pool_exhaustion(database_drop_simulation)**:
       - Simulate multiple concurrent requests
       - Drop database connections
       - Verify connection pool handles exhaustion gracefully
       - Verify no connection leaks (check connection count before/after)
       - Restore database and verify new connections work

    4. **test_database_reconnection_logic(database_drop_simulation)**:
       - Verify automatic reconnection attempts (if implemented)
       - Test reconnection with exponential backoff
       - Verify max retry limits
       - Test successful reconnection after database restored

    Use `database_drop_simulation` fixture which provides simulate_db_drop() and restore_db() control functions.
    For SQLite: simulate by temporarily locking file or renaming database file.
    For PostgreSQL: use `pg_ctl stop` and `pg_ctl start` via subprocess.
    Add pytest.skip for databases without connection pool support.
  </action>
  <verify>pytest backend/tests/e2e_ui/tests/test_network_database_drop.py -v</verify>
  <done>All 4 database drop tests pass with proper error recovery</done>
</task>

<task type="auto">
  <name>Task 5: Create API timeout handling tests</name>
  <files>backend/tests/e2e_ui/tests/test_network_api_timeout.py</files>
  <action>
    Create `backend/tests/e2e_ui/tests/test_network_api_timeout.py`:

    1. **test_api_timeout_during_agent_execution(timeout_api_context, authenticated_page_api)**:
       - Use timeout_api_context which adds 30s delay to /api/v1/agents/execute
       - Execute agent via UI
       - Verify timeout error message appears after 30s
       - Verify UI shows "Request timeout" or "Agent execution timed out"
       - Verify no hanging requests (client-side timeout)

    2. **test_api_timeout_during_canvas_presentation(timeout_api_context, authenticated_page_api)**:
       - Use timeout context for canvas API
       - Try to present canvas
       - Verify timeout handling
       - Verify error message is clear
       - Verify UI recovers (no frozen state)

    3. **test_api_timeout_retry_logic(timeout_api_context, authenticated_page_api)**:
       - Test automatic retry mechanism (if implemented)
       - Verify retry limit (e.g., 3 retries)
       - Verify exponential backoff between retries
       - Verify final timeout error after retries exhausted

    4. **test_concurrent_timeout_handling(timeout_api_context, authenticated_page_api)**:
       - Start multiple requests simultaneously
       - Simulate timeout for all requests
       - Verify all requests timeout gracefully
       - Verify no resource leaks or hanging connections
       - Verify UI remains responsive

    Use `timeout_api_context` fixture from network_fixtures.py.
    Use Playwright's `context.route()` to intercept and delay API calls:
    ```javascript
    await context.route('**/api/v1/agents/execute', route => {
      setTimeout(() => route.continue(), 30000);
    });
    ```
    Verify client-side timeout is less than server timeout (avoid indefinite waiting).
  </action>
  <verify>pytest backend/tests/e2e_ui/tests/test_network_api_timeout.py -v</verify>
  <done>All 4 API timeout tests pass with proper timeout handling</done>
</task>

</tasks>

<verification>
Run all network simulation tests:
```bash
pytest backend/tests/e2e_ui/tests/test_network_slow_3g.py -v --timeout=60
pytest backend/tests/e2e_ui/tests/test_network_offline.py -v
pytest backend/tests/e2e_ui/tests/test_network_database_drop.py -v
pytest backend/tests/e2e_ui/tests/test_network_api_timeout.py -v
```

Verify:
- All tests pass with extended timeouts where needed
- Error messages are user-friendly and actionable
- No hanging tests or infinite waits
- Proper cleanup after each test (database restored, network reconnected)
</verification>

<success_criteria>
Phase 236-02 is complete when:
- network_fixtures.py contains 4 fixtures (slow_3g, offline, timeout, db_drop)
- test_network_slow_3g.py has 4 tests passing (login, agent, canvas, error handling)
- test_network_offline.py has 4 tests passing (login, agent, reconnect, indicator)
- test_network_database_drop.py has 4 tests passing (login, agent, pool, reconnection)
- test_network_api_timeout.py has 4 tests passing (agent, canvas, retry, concurrent)
- All tests use Playwright's network interception (context.route(), context.offline)
- Error messages are user-friendly and not technical stack traces
- Tests include proper cleanup and teardown
</success_criteria>

<output>
After completion, create `.planning/phases/236-cross-platform-and-stress-testing/236-02-SUMMARY.md` with:
- Network simulation test results
- Error handling weaknesses discovered
- Recommendations for improving network resilience
- Timeout values configured
</output>
