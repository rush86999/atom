---
phase: 236-cross-platform-and-stress-testing
plan: 03
type: execute
wave: 2
depends_on: ["236-01"]
files_modified:
  - backend/tests/e2e_ui/tests/test_memory_leak_detection.py
  - backend/tests/e2e_ui/fixtures/memory_fixtures.py
  - backend/tests/e2e_ui/tests/test_performance_regression.py
  - .github/workflows/lighthouse-ci.yml
  - backend/docs/LIGHTHOUSE_SETUP.md
autonomous: true
user_setup:
  - service: lighthouse-ci
    why: "Performance regression testing with Lighthouse CI"
    env_vars:
      - name: LHCI_GITHUB_APP_TOKEN
        source: "GitHub Settings -> Developer settings -> GitHub Apps -> Lighthouse CI"
    dashboard_config:
      - task: "Install Lighthouse CI GitHub App"
        location: "https://github.com/apps/lighthouse-ci"

must_haves:
  truths:
    - "Memory leak detection uses CDP heap snapshots (before/after comparison)"
    - "Memory leaks are detected in agent execution and canvas presentation flows"
    - "Lighthouse CI integration runs performance tests on critical pages"
    - "Performance regression detection catches significant slowdowns (>20% degradation)"
    - "Performance budgets are configured for critical pages"
    - "CI/CD pipeline runs Lighthouse tests and blocks regressions"
  artifacts:
    - path: "backend/tests/e2e_ui/fixtures/memory_fixtures.py"
      provides: "CDP heap snapshot fixtures and memory comparison helpers"
      contains: "@pytest.fixture"
      min_lines: 100
    - path: "backend/tests/e2e_ui/tests/test_memory_leak_detection.py"
      provides: "Memory leak detection tests for agent and canvas flows"
      contains: "test_memory_leak_agent_execution|test_memory_leak_canvas_cycles"
      min_lines: 120
    - path: "backend/tests/e2e_ui/tests/test_performance_regression.py"
      provides: "Performance regression tests with Lighthouse"
      contains: "test_lighthouse_performance_scores"
      min_lines: 100
    - path: ".github/workflows/lighthouse-ci.yml"
      provides: "Lighthouse CI GitHub Actions workflow"
      contains: "uses: treosh/lighthouse-ci-action"
      min_lines: 80
    - path: "backend/docs/LIGHTHOUSE_SETUP.md"
      provides: "Lighthouse CI setup and configuration documentation"
      contains: "# Lighthouse CI Setup"
      min_lines: 80
  key_links:
    - from: "backend/tests/e2e_ui/fixtures/memory_fixtures.py"
      to: "playwright"
      via: "CDP (Chrome DevTools Protocol) for heap snapshots"
      pattern: "page.context.cdp|cdp_session.send"
    - from: "backend/tests/e2e_ui/tests/test_performance_regression.py"
      to: ".github/workflows/lighthouse-ci.yml"
      via: "Lighthouse CI workflow triggered on PR"
      pattern: "lhci autorun|lighthouse-ci-action"
    - from: "backend/tests/e2e_ui/tests/test_memory_leak_detection.py"
      to: "backend/tests/e2e_ui/fixtures/memory_fixtures.py"
      via: "Heap snapshot comparison helpers"
      pattern: "compare_heap_snapshots|get_heap_snapshot"
---

<objective>
Create memory leak detection tests using CDP heap snapshots and integrate Lighthouse CI for performance regression testing to catch significant performance degradations.

Purpose: Detect memory leaks in critical user flows (agent execution, canvas presentation) and establish performance regression guards in CI/CD pipeline.
Output: Memory fixtures, memory leak tests, Lighthouse CI workflow, and setup documentation.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/236-cross-platform-and-stress-testing/236-RESEARCH.md
@.planning/phases/236-cross-platform-and-stress-testing/236-01-SUMMARY.md
@.planning/phases/233-test-infrastructure-foundation/233-05-SUMMARY.md
@backend/tests/e2e_ui/conftest.py
@backend/tests/e2e_ui/fixtures/auth_fixtures.py
@backend/core/atom_agent_endpoints.py
@backend/tools/canvas_tool.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CDP heap snapshot fixtures</name>
  <files>backend/tests/e2e_ui/fixtures/memory_fixtures.py</files>
  <action>
    Create `backend/tests/e2e_ui/fixtures/memory_fixtures.py` with CDP memory inspection:

    1. **cdp_session fixture**:
       - Get CDP session from Playwright page context
       - Use `page.context.cdp` or `page.context.new_cdp_session(page)`
       - Enable CDP domains: Memory, HeapProfiler
       - Return session object for heap operations

    2. **heap_snapshot fixture**:
       - Take initial heap snapshot via CDP
       - Use `HeapProfiler.takeHeapSnapshot()` or `Memory.getHeapSnapshot()`
       - Parse snapshot data (JSON format)
       - Return snapshot object with node counts, sizes, types

    3. **compare_heap_snapshots helper function**:
       - Accept before_snapshot and after_snapshot
       - Compare node counts by type (e.g., 'string', 'object', 'closure')
       - Calculate size difference (before - after)
       - Return dict: {"detached_nodes": N, "size_increase_bytes": N, "leak_detected": bool}
       - Leak threshold: >1000 detached nodes or >10MB size increase

    4. **memory_stats helper function**:
       - Get current memory usage via CDP
       - Return JSHeapUsedSize, JSHeapTotalSize, JSHeapSizeLimit
       - Calculate percentage used: used / limit * 100

    Use Playwright's CDP support: `page.context.cdp` (Chromium-only).
    Add pytest.skip for non-Chromium browsers (Firefox, Safari don't support CDP).
  </action>
  <verify>grep -E "(cdp_session|heap_snapshot|compare_heap_snapshots)" backend/tests/e2e_ui/fixtures/memory_fixtures.py</verify>
  <done>All 4 memory fixtures exist with CDP integration</done>
</task>

<task type="auto">
  <name>Task 2: Create memory leak detection tests</name>
  <files>backend/tests/e2e_ui/tests/test_memory_leak_detection.py</files>
  <action>
    Create `backend/tests/e2e_ui/tests/test_memory_leak_detection.py`:

    1. **test_memory_leak_agent_execution(cdp_session, authenticated_page_api)**:
       - Take initial heap snapshot
       - Execute agent 10 times (same agent, repeated execution)
       - Take final heap snapshot
       - Use compare_heap_snapshots() to detect leaks
       - Assert: leak_detected is False
       - Assert: size_increase_bytes < 10MB (acceptable growth)

    2. **test_memory_leak_canvas_cycles(cdp_session, authenticated_page_api)**:
       - Take initial heap snapshot
       - Present and close canvas 20 times (rapid cycles)
       - Use different canvas types (chart, sheet, form, docs)
       - Take final heap snapshot
       - Compare snapshots
       - Assert: detached_nodes < 500 (some DOM nodes expected)
       - Assert: size_increase_bytes < 5MB

    3. **test_memory_leak_session_persistence(cdp_session, authenticated_page_api)**:
       - Take initial heap snapshot
       - Navigate between pages: dashboard -> agents -> canvas -> workflows (10 cycles)
       - Take final heap snapshot
       - Verify no significant memory growth
       - Assert: size_increase_bytes < 8MB

    4. **test_memory_leak_event_listeners(cdp_session)**:
       - Take initial heap snapshot
       - Add and remove event listeners repeatedly
       - Verify no listener leaks
       - Check for detached DOM nodes with event handlers
       - Assert: detached_nodes < 200

    Use `cdp_session` and heap snapshot comparison fixtures from memory_fixtures.py.
    Use repeated operations to amplify small leaks (detectable).
    Add pytest.mark.skipif(not chromium) for CDP-specific tests.
  </action>
  <verify>pytest backend/tests/e2e_ui/tests/test_memory_leak_detection.py -v --browser chromium</verify>
  <done>All 4 memory leak tests pass with acceptable memory growth</done>
</task>

<task type="auto">
  <name>Task 3: Create performance regression tests with Lighthouse</name>
  <files>backend/tests/e2e_ui/tests/test_performance_regression.py</files>
  <action>
    Create `backend/tests/e2e_ui/tests/test_performance_regression.py`:

    1. **test_lighthouse_performance_scores(page, test_user)**:
       - Login and navigate to dashboard
       - Run Lighthouse via subprocess: `lighthouse http://localhost:3000/dashboard --output=json --output-path=/tmp/lighthouse-report.json`
       - Parse Lighthouse report JSON
       - Verify performance score > 90
       - Verify accessibility score > 90
       - Verify best-practices score > 90
       - Verify SEO score > 80 (lower priority)

    2. **test_lighthouse_performance_budgets(page)**:
       - Define performance budgets:
         - Time to First Byte (TTFB) < 600ms
         - First Contentful Paint (FCP) < 1.5s
         - Largest Contentful Paint (LCP) < 2.5s
         - Total Blocking Time (TBT) < 300ms
         - Cumulative Layout Shift (CLS) < 0.1
       - Run Lighthouse on critical pages (login, dashboard, agents)
       - Assert all metrics meet budget thresholds

    3. **test_performance_regression_detection(page)**:
       - Run Lighthouse and extract metrics
       - Compare against baseline (stored in tests/data/lighthouse-baseline.json)
       - If regression > 20% degradation, fail test
       - If improvement > 10%, update baseline
       - Alert on significant metric changes

    Use Lighthouse CLI (npm install -g lighthouse).
    Store baseline metrics in JSON file for regression detection.
    Use pytest markers: @pytest.mark.lighthouse for these tests.
  </action>
  <verify>pytest backend/tests/e2e_ui/tests/test_performance_regression.py -v -m lighthouse</verify>
  <done>All Lighthouse tests pass with performance scores >90</done>
</task>

<task type="auto">
  <name>Task 4: Create Lighthouse CI GitHub Actions workflow</name>
  <files>.github/workflows/lighthouse-ci.yml</files>
  <action>
    Create `.github/workflows/lighthouse-ci.yml`:

    1. **Workflow trigger**:
       - on: pull_request (target: main)
       - on: push (branches: [main])

    2. **Job configuration**:
       ```yaml
       jobs:
         lighthouse-ci:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v3
             - name: Use Node.js
               uses: actions/setup-node@v3
               with:
                 node-version: '18'
             - name: Install dependencies
               run: npm install -g lighthouse
             - name: Start server
               run: |
                 cd backend
                 python -m uvicorn main:app &
                 sleep 10
             - name: Run Lighthouse CI
               uses: treosh/lighthouse-ci-action@v9
               with:
                 urls: |
                   http://localhost:3000/
                   http://localhost:3000/dashboard
                   http://localhost:3000/agents
                 uploadArtifacts: true
                 temporaryPublicStorage: true
       ```

    3. **Performance budgets**:
       - Add budget.json file in .lighthouserc.json configuration
       - Configure thresholds for TTFB, FCP, LCP, TBT, CLS

    4. **Comment on PR**:
       - Use lhci action to comment scores on PR
       - Fail PR if performance budget exceeded

    Use treosh/lighthouse-ci-action (proven, well-maintained).
    Run on Ubuntu latest with Node.js 18+.
  </action>
  <verify>cat .github/workflows/lighthouse-ci.yml | grep -E "(treosh/lighthouse-ci-action|urls:|uploadArtifacts)"</verify>
  <done>Lighthouse CI workflow exists with URL configuration and artifact upload</done>
</task>

<task type="auto">
  <name>Task 5: Create Lighthouse setup documentation</name>
  <files>backend/docs/LIGHTHOUSE_SETUP.md</files>
  <action>
    Create `backend/docs/LIGHTHOUSE_SETUP.md` with:

    1. **Overview**: Purpose of Lighthouse CI, performance regression detection
    2. **Prerequisites**: Node.js 18+, Lighthouse CLI installation
    3. **Local development**:
       - Install Lighthouse: `npm install -g lighthouse`
       - Run locally: `lighthouse http://localhost:3000/dashboard --view`
       - Run specific tests: `pytest tests/e2e_ui/tests/test_performance_regression.py -m lighthouse`

    4. **Performance budgets**:
       - List all budgets with thresholds (TTFB <600ms, FCP <1.5s, etc.)
       - Explain how to adjust budgets in .lighthouserc.json

    5. **CI/CD integration**:
       - GitHub Actions workflow explanation
       - PR comment behavior
       - Failing PR on regression

    6. **Troubleshooting**:
       - Common Lighthouse failures (timeout, port binding)
       - False positives and how to handle them
       - Updating baseline metrics

    7. **Interpreting results**:
       - Performance scores: 0-100 scale
       - Metrics: FCP, LCP, TBT, CLS, TTFB
       - Regression thresholds (20% degradation)

    Include example Lighthouse report screenshot location.
    Reference: https://github.com/GoogleChrome/lighthouse
  </action>
  <verify>cat backend/docs/LIGHTHOUSE_SETUP.md | grep -E "(Overview|Prerequisites|Performance budgets|CI/CD)"</verify>
  <done>LIGHTHOUSE_SETUP.md contains complete setup and troubleshooting guide</done>
</task>

</tasks>

<verification>
Run all memory and performance tests:
```bash
pytest backend/tests/e2e_ui/tests/test_memory_leak_detection.py -v --browser chromium
pytest backend/tests/e2e_ui/tests/test_performance_regression.py -v -m lighthouse
```

Verify:
- All memory leak tests pass (no leaks detected)
- All Lighthouse tests pass with scores >90
- Performance budgets are met
</verification>

<success_criteria>
Phase 236-03 is complete when:
- memory_fixtures.py contains 4 fixtures (cdp_session, heap_snapshot, compare, stats)
- test_memory_leak_detection.py has 4 tests passing (agent, canvas, session, listeners)
- test_performance_regression.py has 3 tests passing (scores, budgets, regression)
- lighthouse-ci.yml workflow runs on PR and uploads artifacts
- LIGHTHOUSE_SETUP.md documents setup, budgets, and troubleshooting
- All tests use CDP for heap snapshots (Chromium-only)
- Performance budgets configured (TTFB <600ms, FCP <1.5s, LCP <2.5s, TBT <300ms, CLS <0.1)
</success_criteria>

<output>
After completion, create `.planning/phases/236-cross-platform-and-stress-testing/236-03-SUMMARY.md` with:
- Memory leak detection results (baseline metrics)
- Lighthouse performance scores for critical pages
- Performance budgets configured
- Any memory leaks or performance issues discovered
- Recommendations for optimization
</output>
