---
phase: 125-financial-property-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/property_tests/financial/test_decimal_precision_invariants.py
  - backend/tests/property_tests/financial/test_double_entry_invariants.py
autonomous: true

must_haves:
  truths:
    - "All decimal precision property tests use max_examples=200 for critical invariants"
    - "All double-entry property tests use max_examples=200 for accounting fundamentals"
    - "Tests run successfully without timeouts after max_examples increase"
  artifacts:
    - path: backend/tests/property_tests/financial/test_decimal_precision_invariants.py
      contains: "@settings(max_examples=200)"
      min_occurrences: 26  # All 26 tests should use max_examples=200
    - path: backend/tests/property_tests/financial/test_double_entry_invariants.py
      contains: "@settings(max_examples=200)"
      min_occurrences: 14  # All 14 tests should use max_examples=200
  key_links:
    - from: test_decimal_precision_invariants.py
      to: hypothesis@settings
      via: max_examples parameter
      pattern: "@settings\\(max_examples=200\\)"
---

<objective>
Upgrade decimal precision and double-entry property tests to max_examples=200 for critical financial invariants.

Purpose: PROP-03 requires max_examples=200 for all critical financial invariants. Decimal precision tests (26 tests) and double-entry tests (14 tests) validate fundamental accounting properties that must be thoroughly tested.

Output: Updated test files with all tests using max_examples=200.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/125-financial-property-tests/125-RESEARCH.md

# Existing test patterns
@backend/tests/property_tests/financial/test_decimal_precision_invariants.py
@backend/tests/property_tests/financial/test_double_entry_invariants.py
@backend/tests/fixtures/decimal_fixtures.py
</context>

<tasks>

<task type="auto">
  <name>Upgrade all decimal precision tests to max_examples=200</name>
  <files>backend/tests/property_tests/financial/test_decimal_precision_invariants.py</files>
  <action>
    Update all @settings(max_examples=N) decorators in test_decimal_precision_invariants.py to max_examples=200.

    Current state (from grep):
    - Lines using max_examples=100: 18 tests
    - Lines using max_examples=50: 5 tests
    - Lines using max_examples=200: 3 tests

    Required action:
    1. Replace all @settings(max_examples=100) with @settings(max_examples=200)
    2. Replace all @settings(max_examples=50) with @settings(max_examples=200)
    3. Leave existing @settings(max_examples=200) unchanged

    Classes to update:
    - TestPrecisionPreservationInvariants (4 tests, currently max_examples=100)
    - TestConservationOfValueInvariants (4 tests, currently max_examples=100)
    - TestCurrencyRoundingInvariants (5 tests, mix of 50/100/200)
    - TestArithmeticOperationInvariants (4 tests, currently max_examples=100)
    - TestIdempotencyInvariants (4 tests, currently max_examples=100)
    - TestExactComparisonInvariants (3 tests, currently max_examples=100)
    - TestTaxAndPercentageInvariants (2 tests, mix of 50/200)

    CRITICAL: This is a financial invariant - precision bugs cause accounting discrepancies.
    All 26 tests must use max_examples=200 per PROP-03 requirement.

    DO NOT modify test logic, only the @settings decorator.
  </action>
  <verify>
    Run: cd /Users/rushiparikh/projects/atom/backend && pytest tests/property_tests/financial/test_decimal_precision_invariants.py -v --tb=short

    Expected: All 26 tests pass
    Verify: grep -c "@settings(max_examples=200)" backend/tests/property_tests/financial/test_decimal_precision_invariants.py returns 26
  </verify>
  <done>
    All 26 decimal precision tests use max_examples=200 and pass.
  </done>
</task>

<task type="auto">
  <name>Upgrade all double-entry tests to max_examples=200</name>
  <files>backend/tests/property_tests/financial/test_double_entry_invariants.py</files>
  <action>
    Update all @settings(max_examples=N) decorators in test_double_entry_invariants.py to max_examples=200.

    Current state (from grep):
    - Lines using max_examples=100: 7 tests
    - Lines using max_examples=200: 7 tests

    Required action:
    1. Replace all @settings(max_examples=100) with @settings(max_examples=200)
    2. Leave existing @settings(max_examples=200) unchanged

    Classes to update:
    - TestDoubleEntryValidationInvariants (7 tests, currently mix of 100/200)
    - TestAccountingEquationInvariants (4 tests, currently max_examples=100)
    - TestTransactionIntegrityInvariants (3 tests, currently max_examples=100)

    CRITICAL: Double-entry accounting is FUNDAMENTAL to GAAP/IFRS compliance.
    All 14 tests must use max_examples=200 per PROP-03 requirement.

    DO NOT modify test logic, only the @settings decorator.
  </action>
  <verify>
    Run: cd /Users/rushiparikh/projects/atom/backend && pytest tests/property_tests/financial/test_double_entry_invariants.py -v --tb=short

    Expected: All 14 tests pass
    Verify: grep -c "@settings(max_examples=200)" backend/tests/property_tests/financial/test_double_entry_invariants.py returns 14
  </verify>
  <done>
    All 14 double-entry tests use max_examples=200 and pass.
  </done>
</task>

<task type="auto">
  <name>Verify upgraded tests pass and document max_examples coverage</name>
  <files>
    - backend/tests/property_tests/financial/test_decimal_precision_invariants.py
    - backend/tests/property_tests/financial/test_double_entry_invariants.py
  </files>
  <action>
    Run combined verification of all upgraded financial property tests.

    Commands:
    1. Run both test files together: pytest tests/property_tests/financial/test_decimal_precision_invariants.py tests/property_tests/financial/test_double_entry_invariants.py -v --tb=short

    2. Count total max_examples=200 usage:
       grep -r "@settings(max_examples=200)" backend/tests/property_tests/financial/test_decimal_precision_invariants.py backend/tests/property_tests/financial/test_double_entry_invariants.py | wc -l

    Expected results:
    - 26 decimal precision tests pass
    - 14 double-entry tests pass
    - 40 total @settings(max_examples=200) decorators

    Create coverage summary documenting:
    - Test file name
    - Number of tests
    - All using max_examples=200
    - Test execution time (should complete < 60 seconds)
  </action>
  <verify>
    Run: cd /Users/rushiparikh/projects/atom/backend && pytest tests/property_tests/financial/test_decimal_precision_invariants.py tests/property_tests/financial/test_double_entry_invariants.py -v --tb=short

    Expected: All 40 tests pass (26 + 14)
  </verify>
  <done>
    All 40 decimal precision and double-entry tests use max_examples=200 and pass.
  </done>
</task>

</tasks>

<verification>
Run complete financial property test suite to verify max_examples=200 upgrades:

```bash
cd /Users/rushiparikh/projects/atom/backend
pytest tests/property_tests/financial/test_decimal_precision_invariants.py \
      tests/property_tests/financial/test_double_entry_invariants.py \
      -v --tb=short
```

Expected: 40 tests pass, all using max_examples=200

Verify coverage:
```bash
grep -c "@settings(max_examples=200)" tests/property_tests/financial/test_decimal_precision_invariants.py  # Should be 26
grep -c "@settings(max_examples=200)" tests/property_tests/financial/test_double_entry_invariants.py  # Should be 14
```
</verification>

<success_criteria>
1. All 26 decimal precision tests use max_examples=200 (verified by grep count)
2. All 14 double-entry tests use max_examples=200 (verified by grep count)
3. All 40 tests pass (pytest -v shows 40 passed)
4. No test timeouts (all tests complete in < 60 seconds)
5. Decimal precision invariants validated with 200 examples per test
6. Double-entry accounting invariants validated with 200 examples per test
</success_criteria>

<output>
After completion, create `.planning/phases/125-financial-property-tests/125-01-SUMMARY.md` with:
- Number of tests upgraded to max_examples=200
- Test execution results (pass/fail counts)
- Any issues encountered during upgrade
- Execution time measurements
</output>
