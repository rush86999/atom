---
phase: 125-financial-property-tests
plan: 02
type: execute
wave: 2
depends_on: ["125-01"]
files_modified:
  - backend/tests/property_tests/finance/test_audit_immutability_invariants.py
  - backend/tests/property_tests/financial/test_financial_invariants.py
autonomous: true

must_haves:
  truths:
    - "All audit immutability property tests use max_examples=200 for critical fraud-prevention invariants"
    - "All financial invariants property tests use max_examples=200 for critical business logic"
    - "Tests run successfully without timeouts after max_examples increase"
  artifacts:
    - path: backend/tests/property_tests/finance/test_audit_immutability_invariants.py
      contains: "@settings(max_examples=200)"
      min_occurrences: 9  # All 9 tests should use max_examples=200
    - path: backend/tests/property_tests/financial/test_financial_invariants.py
      contains: "@settings(max_examples=200)"
      min_occurrences: 21  # All 21 tests should use max_examples=200
  key_links:
    - from: test_audit_immutability_invariants.py
      to: core/models.py FinancialAudit
      via: hash chain integrity
      pattern: "entry_hash|prev_hash"
    - from: test_financial_invariants.py
      to: financial business logic
      via: validation invariants
      pattern: "invariant|property"
---

<objective>
Upgrade audit immutability and financial invariants property tests to max_examples=200 for critical financial invariants.

Purpose: PROP-03 requires max_examples=200 for all critical financial invariants. Audit immutability tests (9 tests) prevent fraud by ensuring audit trails cannot be tampered with. Financial invariants tests (21 tests) validate business logic correctness.

Output: Updated test files with all tests using max_examples=200.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/125-financial-property-tests/125-RESEARCH.md
@.planning/phases/125-financial-property-tests/125-01-SUMMARY.md

# Existing test patterns
@backend/tests/property_tests/finance/test_audit_immutability_invariants.py
@backend/tests/property_tests/financial/test_financial_invariants.py
@backend/tests/fixtures/financial_audit_fixtures.py

# Already upgraded in Plan 01
@backend/tests/property_tests/financial/test_decimal_precision_invariants.py
@backend/tests/property_tests/financial/test_double_entry_invariants.py
</context>

<tasks>

<task type="auto">
  <name>Upgrade all audit immutability tests to max_examples=200</name>
  <files>backend/tests/property_tests/finance/test_audit_immutability_invariants.py</files>
  <action>
    Update all @settings(max_examples=N) decorators in test_audit_immutability_invariants.py to max_examples=200.

    Current state (from grep):
    - Lines using max_examples=100: 2 tests
    - Lines using max_examples=50: 7 tests

    Required action:
    1. Replace all @settings(max_examples=100) with @settings(max_examples=200)
    2. Replace all @settings(max_examples=50) with @settings(max_examples=200)

    Tests to update (line numbers from grep):
    - test_audits_cannot_be_modified (line 48, currently 50)
    - test_audits_cannot_be_deleted (line 98, currently 50)
    - test_hash_chain_integrity (line 147, currently 100)
    - test_prev_hash_linking (line 182, currently 50)
    - test_first_entry_empty_prev_hash (line 224, currently 100)
    - test_tampering_detection_across_chain (line 267, currently 50)
    - test_sequence_number_monotonic (line 315, currently 50)
    - test_cascade_deletion_prevented (line 352, currently 50)
    - test_bulk_operations_protected (line 398, currently 50)

    CRITICAL: Audit immutability is FUNDAMENTAL to fraud prevention.
    All 9 tests must use max_examples=200 per PROP-03 requirement.

    DO NOT modify test logic, only the @settings decorator.
  </action>
  <verify>
    Run: cd /Users/rushiparikh/projects/atom/backend && pytest tests/property_tests/finance/test_audit_immutability_invariants.py -v --tb=short

    Expected: All 9 tests pass
    Verify: grep -c "@settings(max_examples=200)" backend/tests/property_tests/finance/test_audit_immutability_invariants.py returns 9
  </verify>
  <done>
    All 9 audit immutability tests use max_examples=200 and pass.
  </done>
</task>

<task type="auto">
  <name>Upgrade all financial invariants tests to max_examples=200</name>
  <files>backend/tests/property_tests/financial/test_financial_invariants.py</files>
  <action>
    Update all @settings(max_examples=50) decorators in test_financial_invariants.py to max_examples=200.

    Current state (from grep):
    - Lines using max_examples=50: 21 tests

    Required action:
    1. Replace all @settings(max_examples=50) with @settings(max_examples=200)

    Tests to update (21 total):
    - All tests in test_financial_invariants.py currently use max_examples=50

    CRITICAL: Financial invariants validate business logic correctness.
    All 21 tests must use max_examples=200 per PROP-03 requirement.

    DO NOT modify test logic, only the @settings decorator.

    Note: Review test file to understand test classes and purposes before making changes.
  </action>
  <verify>
    Run: cd /Users/rushiparikh/projects/atom/backend && pytest tests/property_tests/financial/test_financial_invariants.py -v --tb=short

    Expected: All 21 tests pass
    Verify: grep -c "@settings(max_examples=200)" backend/tests/property_tests/financial/test_financial_invariants.py returns 21
  </verify>
  <done>
    All 21 financial invariants tests use max_examples=200 and pass.
  </done>
</task>

<task type="auto">
  <name>Verify upgraded tests pass and document max_examples coverage</name>
  <files>
    - backend/tests/property_tests/finance/test_audit_immutability_invariants.py
    - backend/tests/property_tests/financial/test_financial_invariants.py
  </files>
  <action>
    Run combined verification of all upgraded financial property tests from Plan 02.

    Commands:
    1. Run both test files together: pytest tests/property_tests/finance/test_audit_immutability_invariants.py tests/property_tests/financial/test_financial_invariants.py -v --tb=short

    2. Count total max_examples=200 usage:
       grep -c "@settings(max_examples=200)" backend/tests/property_tests/finance/test_audit_immutability_invariants.py
       grep -c "@settings(max_examples=200)" backend/tests/property_tests/financial/test_financial_invariants.py

    Expected results:
    - 9 audit immutability tests pass
    - 21 financial invariants tests pass
    - 30 total @settings(max_examples=200) decorators

    Create coverage summary documenting:
    - Test file name
    - Number of tests
    - All using max_examples=200
    - Test execution time (should complete < 120 seconds for audit tests due to database)
  </action>
  <verify>
    Run: cd /Users/rushiparikh/projects/atom/backend && pytest tests/property_tests/finance/test_audit_immutability_invariants.py tests/property_tests/financial/test_financial_invariants.py -v --tb=short

    Expected: All 30 tests pass (9 + 21)
  </verify>
  <done>
    All 30 audit immutability and financial invariants tests use max_examples=200 and pass.
  </done>
</task>

</tasks>

<verification>
Run complete Plan 02 financial property test suite to verify max_examples=200 upgrades:

```bash
cd /Users/rushiparikh/projects/atom/backend
pytest tests/property_tests/finance/test_audit_immutability_invariants.py \
      tests/property_tests/financial/test_financial_invariants.py \
      -v --tb=short
```

Expected: 30 tests pass, all using max_examples=200

Verify coverage:
```bash
grep -c "@settings(max_examples=200)" tests/property_tests/finance/test_audit_immutability_invariants.py  # Should be 9
grep -c "@settings(max_examples=200)" tests/property_tests/financial/test_financial_invariants.py  # Should be 21
```
</verification>

<success_criteria>
1. All 9 audit immutability tests use max_examples=200 (verified by grep count)
2. All 21 financial invariants tests use max_examples=200 (verified by grep count)
3. All 30 tests pass (pytest -v shows 30 passed)
4. No test timeouts (all tests complete in < 120 seconds)
5. Audit immutability invariants validated with 200 examples per test (fraud prevention)
6. Financial invariants validated with 200 examples per test (business logic)
</success_criteria>

<output>
After completion, create `.planning/phases/125-financial-property-tests/125-02-SUMMARY.md` with:
- Number of tests upgraded to max_examples=200
- Test execution results (pass/fail counts)
- Any issues encountered during upgrade
- Execution time measurements
</output>
