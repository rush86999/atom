---
phase: 161-model-fixes-and-database
plan: 02
type: execute
wave: 2
depends_on: ["161-01"]
files_modified:
  - backend/core/episode_segmentation_service.py
  - backend/core/episode_retrieval_service.py
  - backend/core/episode_lifecycle_service.py
  - backend/tests/integration/services/test_backend_gap_closure.py
  - backend/tests/coverage_reports/metrics/backend_phase_161_plan2.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Episode service methods implemented and tested"
    - "Service implementation gaps closed"
    - "Coverage increases by +8-12% from Phase 161 baseline"
    - "Backend coverage progresses toward 80% target"
  artifacts:
    - path: "backend/core/episode_segmentation_service.py"
      provides: "Complete segmentation service implementation"
    - path: "backend/core/episode_retrieval_service.py"
      provides: "Complete retrieval service implementation"
    - path: "backend/core/episode_lifecycle_service.py"
      provides: "Complete lifecycle service implementation"
    - path: "backend/tests/coverage_reports/metrics/backend_phase_161_plan2.json"
      provides: "Coverage report showing +8-12% improvement"
      contains: "coverage_pct >= 47"
  key_links:
    - from: "episode_*_service.py"
      to: "test_backend_gap_closure.py"
      via: "Service implementations enable tests"
---

<objective>
Implement missing episode service methods (segmentation, retrieval, lifecycle) to close service implementation gaps, enabling coverage increase of +8-12% from Phase 161 baseline toward 80% target.

Purpose: Phase 160 identified incomplete episode service implementations blocking tests. This plan implements missing methods in segmentation, retrieval, and lifecycle services, enabling comprehensive test coverage and measurable progress toward 80%.

Output: Complete episode service implementations, tests passing, coverage increase of +8-12%.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-backend-80-percent-target/160-VERIFICATION.md
@backend/core/episode_segmentation_service.py
@backend/core/episode_retrieval_service.py
@backend/core/episode_lifecycle_service.py
@backend/core/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement missing episode segmentation methods</name>
  <files>
    backend/core/episode_segmentation_service.py
  </files>
  <action>
    Implement missing segmentation service methods:

    1. Analyze current implementation:
       - Review existing segmentation methods
       - Identify missing/incomplete methods
       - Document what needs implementation

    2. Implement missing segmentation logic:
       - detect_time_gap(): Analyze timestamps, detect gaps > threshold
       - detect_topic_change(): Use embeddings to detect semantic shifts
       - detect_task_completion(): Identify task completion markers
       - segment_episode(): Main segmentation orchestration

    3. Add proper error handling:
       - Handle edge cases (empty conversations, single messages)
       - Validate inputs
       - Log segmentation decisions

    4. Add database integration:
       - Save segments to EpisodeSegment model
       - Update Episode records with segment counts
       - Query conversation history

    5. Add testing support:
       - Make methods testable
       - Add logging for debugging
       - Document interfaces

    Implement complete segmentation functionality.
  </action>
  <verify>
    pytest backend/tests/integration/services/test_backend_gap_closure.py -k "segment" -v
    Segmentation tests pass
    Methods implemented and tested
  </verify>
  <done>
    Episode segmentation service methods implemented
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement missing episode retrieval methods</name>
  <files>
    backend/core/episode_retrieval_service.py
  </files>
  <action>
    Implement missing retrieval service methods:

    1. Analyze current implementation:
       - Review existing retrieval methods
       - Identify missing/incomplete methods
       - Document retrieval gaps

    2. Implement missing retrieval logic:
       - retrieve_temporal(): Query by time range with pagination
       - retrieve_semantic(): Vector similarity search with embeddings
       - retrieve_contextual(): Filter by agent/canvas context
       - retrieve_sequential(): Full episodes with all segments

    3. Add vector database integration:
       - Integrate with LanceDB for semantic search
       - Generate embeddings for queries
       - Handle similarity thresholds

    4. Add database integration:
       - Query Episode and EpisodeSegment models
       - Join with agent/canvas context
       - Apply filters and sorting

    5. Add performance optimization:
       - Implement pagination for large result sets
       - Add caching for frequent queries
       - Optimize database queries

    Implement complete retrieval functionality.
  </action>
  <verify>
    pytest backend/tests/integration/services/test_backend_gap_closure.py -k "retriev" -v
    Retrieval tests pass
    Methods implemented and tested
  </verify>
  <done>
    Episode retrieval service methods implemented
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement missing episode lifecycle methods</name>
  <files>
    backend/core/episode_lifecycle_service.py
  </files>
  <action>
    Implement missing lifecycle service methods:

    1. Analyze current implementation:
       - Review existing lifecycle methods
       - Identify missing/incomplete methods
       - Document lifecycle gaps

    2. Implement missing lifecycle logic:
       - decay_episode(): Reduce old episode weights
       - consolidate_episode(): Merge related episodes
       - archive_episode(): Move to cold storage (LanceDB)
       - cleanup_expired_episodes(): Remove very old episodes

    3. Add cold storage integration:
       - Archive episodes to LanceDB
       - Implement retrieval from archive
       - Manage hot/cold storage split

    4. Add scheduling:
       - Background task scheduling
       - Automated lifecycle operations
       - Failure handling and retry logic

    5. Add database integration:
       - Update Episode records with lifecycle state
       - Track lifecycle operations
       - Audit trail for lifecycle changes

    Implement complete lifecycle management.
  </action>
  <verify>
    pytest backend/tests/integration/services/test_backend_gap_closure.py -k "lifecycle" -v
    Lifecycle tests pass
    Methods implemented and tested
  </verify>
  <done>
    Episode lifecycle service methods implemented
  </done>
</task>

<task type="auto">
  <name>Task 4: Create tests for new service methods and measure coverage</name>
  <files>
    backend/tests/integration/services/test_backend_gap_closure.py
    backend/tests/coverage_reports/metrics/backend_phase_161_plan2.json
  </files>
  <action>
    Create comprehensive tests for newly implemented methods:

    1. Update test file with new test cases:
       - Test segmentation logic with various scenarios
       - Test retrieval modes with mock data
       - Test lifecycle operations
       - Test error handling

    2. Add integration tests:
       - End-to-end episode creation and segmentation
       - Retrieval across different modes
       - Lifecycle state transitions

    3. Run all tests:
       ```bash
       pytest tests/integration/services/test_backend_gap_closure.py \
              --cov=core/episode_segmentation_service \
              --cov=core/episode_retrieval_service \
              --cov=core/episode_lifecycle_service \
              --cov-report=json:tests/coverage_reports/metrics/backend_phase_161_plan2.json \
              --cov-report=term-missing
       ```

    4. Calculate coverage improvement:
       - Extract coverage percentage
       - Compare with Phase 161 baseline (~39%)
       - Document improvement
       - Verify target range (47-51% expected)

    Create comprehensive test suite for episode services.
  </action>
  <verify>
    backend/tests/coverage_reports/metrics/backend_phase_161_plan2.json exists
    Coverage >= 47% (+8% from Phase 161 baseline)
    All new service methods tested
  </verify>
  <done>
    Service implementations tested, coverage improved by +8-12%
  </done>
</task>

</tasks>

<verification>
Run `cd backend && pytest tests/integration/services/test_backend_gap_closure.py --cov=core --cov=api --cov=tools --cov-report=term`
Verify coverage >= 47%
Verify all episode service tests pass
Verify service implementation gaps closed
</verification>

<success_criteria>
1. Episode segmentation methods implemented and tested
2. Episode retrieval methods implemented and tested
3. Episode lifecycle methods implemented and tested
4. Coverage >= 47% (+8% from baseline)
5. All tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/161-model-fixes-and-database/161-02-SUMMARY.md`
</output>
