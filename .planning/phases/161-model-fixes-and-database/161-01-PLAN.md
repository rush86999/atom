---
phase: 161-model-fixes-and-database
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/models.py
  - backend/core/agent_governance_service.py
  - backend/tests/integration/services/test_backend_gap_closure.py
  - backend/tests/coverage_reports/metrics/backend_phase_161.json
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Model compatibility issues fixed (AgentEpisode field mismatches resolved)"
    - "Missing database tables created (blocked_triggers, supervised_execution_queue)"
    - "Blocked tests now pass and contribute to coverage"
    - "Coverage increases by +15-20% from Phase 160 baseline (24%)"
  artifacts:
    - path: "backend/core/models.py"
      provides: "Fixed model with all relationships"
    - path: "backend/tests/integration/services/test_backend_gap_closure.py"
      provides: "Fixed tests now passing"
      min_lines: 1700
    - path: "backend/tests/coverage_reports/metrics/backend_phase_161.json"
      provides: "Coverage report showing +15-20% improvement"
      contains: "coverage_pct >= 39"
  key_links:
    - from: "models.py"
      to: "test_backend_gap_closure.py"
      via: "Model fixes enable tests to pass"
---

<objective>
Fix model compatibility issues and add missing database tables to enable blocked tests to pass, increasing backend coverage by +15-20% from 24% baseline toward 80% target.

Purpose: Phase 160 identified 10 tests blocked by AgentEpisode model field mismatches and missing database tables (blocked_triggers, supervised_execution_queue). This plan fixes these critical issues, enabling tests to pass and contribute their coverage gains.

Output: Model compatibility resolved, missing tables added, 10+ tests now passing, coverage increase of +15-20%.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-backend-80-percent-target/160-VERIFICATION.md
@backend/core/models.py
@backend/tests/integration/services/test_backend_gap_closure.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze and fix AgentEpisode model field mismatches</name>
  <files>
    backend/core/models.py
    backend/tests/integration/services/test_backend_gap_closure.py
  </files>
  <action>
    Fix AgentEpisode model compatibility issues:

    1. Analyze test failures:
       - Run episode tests and capture errors
       - Identify field mismatch messages
       - Document all incompatible fields

    2. Fix AgentEpisode model in models.py:
       - Add missing fields referenced by tests
       - Fix field type mismatches (string vs int, etc.)
       - Ensure proper relationships
       - Add missing foreign keys

    3. Common fixes needed:
       - agent_id: Optional[str] vs Integer
       - episode_type: String field with enum values
       - status: String field with proper states
       - timestamps: DateTime fields
       - relationships: Proper back-populates

    4. Update test expectations:
       - Fix test assertions to match corrected model
       - Remove incorrect assumptions
       - Validate proper behavior

    5. Create migration if needed:
       - Generate Alembic migration for model changes
       - Update database schema
       - Test migration

    Fix model to be test-compatible and complete.
  </action>
  <verify>
    pytest backend/tests/integration/services/test_backend_gap_closure.py -k "episode" -v
    Episode-related tests pass
    Model compatibility errors resolved
  </verify>
  <done>
    AgentEpisode model fixed, episode tests passing
  </done>
</task>

<task type="auto">
  <name>Task 2: Add missing database tables</name>
  <files>
    backend/core/models.py
  </files>
  <action>
    Create missing database tables identified in Phase 160:

    1. Create BlockedTrigger model:
       - id: Primary key
       - trigger_id: String, unique
       - agent_id: String (foreign key to AgentRegistry)
       - reason: Text (why blocked)
       - timestamp: DateTime
       - resolved: Boolean default False
       - Table name: blocked_triggers

    2. Create SupervisedExecutionQueue model:
       - id: Primary key
       - agent_id: String (foreign key to AgentRegistry)
       - execution_id: String (foreign key to AgentExecution)
       - supervision_type: String (real-time, post-execution)
       - status: String (pending, active, completed, cancelled)
       - supervisor_id: String (optional)
       - timestamp: DateTime
       - metadata: JSON
       - Table name: supervised_execution_queue

    3. Add relationships:
       - BlockedTrigger → AgentRegistry
       - SupervisedExecutionQueue → AgentRegistry
       - SupervisedExecutionQueue → AgentExecution

    4. Create indexes:
       - idx_blocked_triggers_agent_id
       - idx_supervised_queue_agent_id
       - idx_supervised_queue_status

    5. Generate migration:
       - Use Alembic to create migration
       - Test migration on development database
       - Verify table creation

    Add tables to models.py and create migration.
  </action>
  <verify>
    alembic revision --autogenerate -m "add blocked triggers and supervised execution queue"
    Migration file created
    Tables defined in models.py
  </verify>
  <done>
    Missing tables added to models.py, migration created
  </done>
</task>

<task type="auto">
  <name>Task 3: Update tests to use fixed models and new tables</name>
  <files>
    backend/tests/integration/services/test_backend_gap_closure.py
    backend/tests/integration/services/conftest.py
  </files>
  <action>
    Update tests to work with fixed models:

    1. Update episode tests:
       - Fix test data to match corrected AgentEpisode model
       - Update assertions for correct field types
       - Add proper test fixtures for new fields

    2. Add tests for BlockedTrigger:
       - test_create_blocked_trigger
       - test_resolve_blocked_trigger
       - test_list_blocked_triggers
       - test_blocked_trigger_relationships

    3. Add tests for SupervisedExecutionQueue:
       - test_enqueue_supervised_execution
       - test_process_supervised_execution
       - test_supervision_status_tracking
       - test_queue_cleanup

    4. Update conftest.py fixtures:
       - Add fixtures for BlockedTrigger
       - Add fixtures for SupervisedExecutionQueue
       - Ensure proper cleanup

    5. Run all tests:
       - Verify new tests pass
       - Verify previously blocked tests now pass
       - Check for remaining issues

    Update tests to utilize fixed models and new tables.
  </action>
  <verify>
    pytest backend/tests/integration/services/test_backend_gap_closure.py -v
    New tests for BlockedTrigger and SupervisedExecutionQueue pass
    Previously blocked episode tests now pass
  </verify>
  <done>
    Tests updated, new tables tested, previously blocked tests passing
  </done>
</task>

<task type="auto">
  <name>Task 4: Measure coverage improvement</name>
  <files>
    backend/tests/coverage_reports/metrics/backend_phase_161.json
  </files>
  <action>
    Measure coverage after model and database fixes:

    1. Run database migration:
       ```bash
       cd backend
       alembic upgrade head
       ```

    2. Run coverage measurement:
       ```bash
       pytest tests/integration/services/test_backend_gap_closure.py \
              tests/integration/services/test_llm_service_gap_closure.py \
              tests/integration/services/test_llm_service_http_coverage.py \
              --cov=core --cov=api --cov=tools \
              --cov-report=json:tests/coverage_reports/metrics/backend_phase_161.json \
              --cov-report=term-missing
       ```

    3. Calculate improvement:
       - Extract coverage percentage
       - Compare with Phase 160 baseline (24%)
       - Document improvement percentage
       - Verify target range (39-44% expected)

    4. Document results:
       - Final coverage percentage
       - Improvement from baseline
       - Tests now passing
       - Remaining gaps

    Create backend_phase_161.json with coverage data.
  </action>
  <verify>
    backend/tests/coverage_reports/metrics/backend_phase_161.json exists
    Coverage >= 39% (15%+ improvement from 24% baseline)
    Previously blocked tests now passing
  </verify>
  <done>
    Coverage improved by +15-20%, model and database issues resolved
  </done>
</task>

</tasks>

<verification>
Run `cd backend && pytest tests/integration/services/test_backend_gap_closure.py --cov=core --cov=api --cov=tools --cov-report=term`
Verify coverage >= 39%
Verify all episode, BlockedTrigger, and SupervisedExecutionQueue tests pass
Verify improvement from Phase 160 baseline
</verification>

<success_criteria>
1. AgentEpisode model compatibility fixed
2. BlockedTrigger table added and tested
3. SupervisedExecutionQueue table added and tested
4. 10+ previously blocked tests now passing
5. Coverage >= 39% (+15% from 24% baseline)
</success_criteria>

<output>
After completion, create `.planning/phases/161-model-fixes-and-database/161-01-SUMMARY.md`
</output>
