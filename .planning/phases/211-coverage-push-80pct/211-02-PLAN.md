---
phase: 211-coverage-push-80pct
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/tests/test_webhook_handlers.py
  - backend/tests/test_unified_message_processor.py
  - backend/tests/test_jwt_verifier.py
autonomous: true

must_haves:
  truths:
    - "webhook_handlers.py achieves 75%+ line coverage (tests for all webhook endpoints, error handling)"
    - "unified_message_processor.py achieves 75%+ line coverage (tests for message processing, routing)"
    - "jwt_verifier.py achieves 75%+ line coverage (tests for JWT token validation, signature verification, claims parsing)"
    - "All tests pass with pytest"
    - "No regression in existing coverage"
  artifacts:
    - path: "backend/tests/test_webhook_handlers.py"
      provides: "Unit tests for webhook handler functions"
      min_lines: 300
      exports: ["TestWebhookHandlers", "TestWebhookSignatureValidation"]
    - path: "backend/tests/test_unified_message_processor.py"
      provides: "Unit tests for UnifiedMessageProcessor class"
      min_lines: 250
      exports: ["TestUnifiedMessageProcessor", "TestMessageRouting"]
    - path: "backend/tests/test_jwt_verifier.py"
      provides: "Unit tests for JWTVerifier class"
      min_lines: 200
      exports: ["TestJWTVerifier", "TestTokenValidation", "TestClaimsParsing"]
  key_links:
    - from: "backend/tests/test_webhook_handlers.py"
      to: "backend/core/webhook_handlers.py"
      via: "Direct imports and testing"
      pattern: "from core.webhook_handlers import"
    - from: "backend/tests/test_unified_message_processor.py"
      to: "backend/core/unified_message_processor.py"
      via: "Direct imports and testing"
      pattern: "from core.unified_message_processor import"
    - from: "backend/tests/test_jwt_verifier.py"
      to: "backend/core/jwt_verifier.py"
      via: "Direct imports and testing"
      pattern: "from core.jwt_verifier import"
---

<objective>
Achieve 75%+ coverage on message handling services (webhook_handlers, unified_message_processor, jwt_verifier) with comprehensive unit tests.

Purpose: These services handle external integration points and message routing. Testing them provides coverage for critical communication paths and ensures webhook/token security.

Output: Three test files with 750+ total lines, achieving 75%+ coverage on each target file.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/209-load-stress-testing/209-07-PLAN.md
@backend/tests/test_governance_cache.py
@backend/core/webhook_handlers.py
@backend/core/unified_message_processor.py
@backend/core/jwt_verifier.py

# Test Pattern Reference
From Phase 209: Use pytest fixtures for setup, AsyncMock for async methods, parametrize for multiple cases.

# Target Files
1. webhook_handlers.py: ~248 lines, 0% coverage
   - Webhook signature validation
   - Webhook event handlers (GitHub, Slack, generic)
   - Error handling and retry logic

2. unified_message_processor.py: ~267 lines, 0% coverage
   - UnifiedMessageProcessor class
   - Message routing logic
   - Format conversion (JSON, XML, plain text)

3. jwt_verifier.py: 447 lines, 0% coverage
   - JWTVerifier class
   - JWT token verification/validation
   - Signature verification and claims parsing
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create comprehensive tests for webhook_handlers</name>
  <files>backend/tests/test_webhook_handlers.py</files>
  <action>
Create backend/tests/test_webhook_handlers.py with comprehensive tests for webhook_handlers.py:

1. Import required modules: pytest, json, hmac, hashlib
2. Import from core.webhook_handlers (verify actual imports after reading the file)

3. Create class `TestWebhookSignatureValidation`:
   - test_valid_signature: Verifies valid HMAC signature passes
   - test_invalid_signature: Verifies invalid signature fails
   - test_missing_signature: Verifies missing signature header fails
   - test_signature_with_different_secret: Verifies wrong secret fails
   - test_timestamp_validation: Verifies timestamp freshness check
   - test_timestamp_too_old: Verifies old timestamps are rejected

4. Create class `TestGitHubWebhookHandler`:
   - test_ping_event: Handles GitHub ping event
   - test_push_event: Handles GitHub push event
   - test_pull_request_event: Handles PR events
   - test_invalid_payload: Rejects malformed GitHub payload
   - test_missing_repository_id: Handles missing repo ID

5. Create class `TestSlackWebhookHandler`:
   - test_url_verification: Handles Slack URL verification challenge
   - test_message_event: Handles Slack message events
   - test_app_mention_event: Handles app mention events
   - test_invalid_payload: Rejects malformed Slack payload

6. Create class `TestGenericWebhookHandler`:
   - test_post_event: Handles generic POST webhook
   - test_get_event: Handles generic GET webhook
   - test_json_payload: Processes JSON payloads
   - test_form_data_payload: Processes form-encoded data
   - test_query_params: Processes query parameters

7. Create class `TestWebhookErrorHandling`:
   - test_rate_limiting: Verifies rate limit response
   - test_retry_logic: Verifies retry-after header
   - test_internal_error: Verifies 500 response handling
   - test_timeout: Verifies timeout handling

8. Create fixtures:
   - `webhook_secret`: Returns test webhook secret
   - `github_payload`: Returns sample GitHub webhook payload
   - `slack_payload`: Returns sample Slack webhook payload

9. Use pytest.mark.parametrize for multiple event types
  </action>
  <verify>
pytest backend/tests/test_webhook_handlers.py -v
# All tests should pass
pytest backend/tests/test_webhook_handlers.py --cov=core/webhook_handlers --cov-report=term-missing
# Coverage should be 75%+
  </verify>
  <done>
All webhook_handlers tests passing, 75%+ coverage achieved
  </done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive tests for unified_message_processor</name>
  <files>backend/tests/test_unified_message_processor.py</files>
  <action>
Create backend/tests/test_unified_message_processor.py with comprehensive tests for unified_message_processor.py:

1. Import required modules: pytest, json, asyncio
2. Import from core.unified_message_processor (verify actual imports)

3. Create class `TestUnifiedMessageProcessor`:
   - test_initialization: Verifies processor initialization
   - test_process_json_message: Successfully processes JSON message
   - test_process_xml_message: Successfully processes XML message
   - test_process_plain_text_message: Successfully processes text message
   - test_invalid_format: Rejects unsupported format
   - test_malformed_json: Handles malformed JSON gracefully
   - test_malformed_xml: Handles malformed XML gracefully
   - test_empty_message: Handles empty message

4. Create class `TestMessageRouting`:
   - test_route_to_agent: Routes message to specific agent
   - test_route_to_workflow: Routes message to workflow
   - test_route_to_default: Uses default route when no match
   - test_route_by_priority: Respects message priority
   - test_route_by_type: Routes based on message type
   - test_unknown_route_type: Handles unknown route type

5. Create class `TestMessageTransformation`:
   - test_transform_to_agent_format: Transforms for agent consumption
   - test_transform_to_workflow_format: Transforms for workflow
   - test_transform_with_custom_mapping: Uses custom field mapping
   - test_transform_preserves_metadata: Preserves original metadata
   - test_transform_adds_timestamp: Adds processing timestamp

6. Create class `TestMessageBatchProcessing`:
   - test_batch_process_multiple: Processes batch of messages
   - test_batch_with_failure: Continues on individual failure
   - test_batch_order_preserved: Preserves message order
   - test_batch_empty: Handles empty batch

7. Create class `TestMessageValidation`:
   - test_validate_required_fields: Checks required fields present
   - test_validate_field_types: Validates field types
   - test_validate_with_schema: Uses JSON schema validation
   - test_validation_fails_invalid_data: Rejects invalid data

8. Create fixtures:
   - `message_processor`: Returns UnifiedMessageProcessor instance
   - `sample_json_message`: Returns sample JSON message
   - `sample_xml_message`: Returns sample XML message

9. Use parametrize for multiple message types and routes
  </action>
  <verify>
pytest backend/tests/test_unified_message_processor.py -v
# All tests should pass
pytest backend/tests/test_unified_message_processor.py --cov=core/unified_message_processor --cov-report=term-missing
# Coverage should be 75%+
  </verify>
  <done>
All unified_message_processor tests passing, 75%+ coverage achieved
  </done>
</task>

<task type="auto">
  <name>Task 3: Create comprehensive tests for jwt_verifier</name>
  <files>backend/tests/test_jwt_verifier.py</files>
  <action>
Create backend/tests/test_jwt_verifier.py with comprehensive tests for jwt_verifier.py:

1. Import required modules: pytest, jwt, datetime
2. Import from core.jwt_verifier (verify actual imports)

3. Create class `TestJWTVerifier`:
   - test_initialization: Verifies JWTVerifier initialization with secret
   - test_initialization_with_algorithm: Sets custom algorithm
   - test_verify_valid_token: Valid JWT token passes verification
   - test_verify_invalid_signature: Invalid signature is rejected
   - test_verify_expired_token: Expired token is rejected
   - test_verify_malformed_token: Malformed token is rejected
   - test_verify_missing_claims: Token without required claims is rejected
   - test_verify_returns_payload: Returns decoded payload
   - test_verify_algorithm_mismatch: Wrong algorithm is rejected
   - test_decode_without_verification: Decodes token without verification
   - test_decode_returns_claims: Returns all claims
   - test_decode_handles_malformed: Handles malformed tokens gracefully

4. Create fixtures:
   - `jwt_verifier`: Returns JWTVerifier instance with test secret
   - `valid_token`: Returns valid JWT token with standard claims
   - `expired_token`: Returns expired JWT token
   - `invalid_token`: Returns token with invalid signature
   - `test_payload`: Returns dict of test payload

5. Test edge cases:
   - Empty token
   - None token
   - Tokens with various algorithms (HS256, RS256)
   - Tokens with custom claims
   - Tokens with expiration
   - Tokens with issued_at and not_before claims

6. Use pytest.mark.parametrize for multiple token types
  </action>
  <verify>
pytest backend/tests/test_jwt_verifier.py -v
# All tests should pass
pytest backend/tests/test_jwt_verifier.py --cov=core/jwt_verifier --cov-report=term-missing
# Coverage should be 75%+
  </verify>
  <done>
All jwt_verifier tests passing, 75%+ coverage achieved
  </done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Run all tests: pytest backend/tests/test_webhook_handlers.py backend/tests/test_unified_message_processor.py backend/tests/test_jwt_verifier.py -v
2. Verify coverage: pytest --cov=core/webhook_handlers --cov=core/unified_message_processor --cov=core/jwt_verifier --cov-report=term-missing
3. All three files should show 75%+ coverage
4. Verify no regression: pytest tests/ --cov-report=term (overall coverage should not decrease)
</verification>

<success_criteria>
1. All tests pass (100% pass rate)
2. webhook_handlers.py coverage >= 75%
3. unified_message_processor.py coverage >= 75%
4. jwt_verifier.py coverage >= 75%
5. No regression in existing coverage
</success_criteria>

<output>
After completion, create `.planning/phases/211-coverage-push-80pct/211-02-SUMMARY.md`
</output>
