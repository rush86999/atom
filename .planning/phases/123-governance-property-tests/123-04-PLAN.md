---
phase: 123-governance-property-tests
plan: 04
type: execute
wave: 2
depends_on: [123-01, 123-02, 123-03]
files_modified:
  - backend/tests/coverage_reports/metrics/phase_123_property_test_summary.md
  - .planning/REQUIREMENTS.md
autonomous: true

must_haves:
  truths:
    - "All 40+ governance property tests pass (10 async + 15 cache + 18 edge cases)"
    - "Maturity routing invariants validated (STUDENT blocked, AUTONOMOUS allowed)"
    - "Permission check consistency validated (same inputs = same outputs)"
    - "Governance cache invariants validated (correctness, consistency, <1ms P99)"
    - "PROP-01 requirement marked complete in REQUIREMENTS.md"
  artifacts:
    - path: "backend/tests/coverage_reports/metrics/phase_123_property_test_summary.md"
      provides: "Property test summary for Phase 123"
      contains: "test counts, coverage gaps addressed, invariants validated"
  key_links:
    - from: "backend/tests/coverage_reports/metrics/phase_123_property_test_summary.md"
      to: ".planning/REQUIREMENTS.md"
      via: "PROP-01 requirement completion"
      pattern: "PROP-01.*complete"
---

<objective>
Verify Phase 123 success criteria and generate property test summary.

**Purpose:** Run all governance property tests to verify invariants hold. Generate summary document and mark PROP-01 requirement complete. This is the verification wave that depends on all three previous plans.

**Output:** Phase 123 completion verification, property test summary document, PROP-01 requirement marked complete
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-governance-property-tests/123-RESEARCH.md

# Previous work
@.planning/phases/123-governance-property-tests/123-01-SUMMARY.md
@.planning/phases/123-governance-property-tests/123-02-SUMMARY.md
@.planning/phases/123-governance-property-tests/123-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Run combined property test suite for all governance invariants</name>
  <files>backend/tests/coverage_reports/metrics/phase_123_property_test_summary.md</files>
  <action>
    Run all Phase 123 property tests together to verify invariants.

    Command:
    ```bash
    cd backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
      tests/property_tests/governance/test_async_governance_invariants.py \
      tests/property_tests/governance/test_cache_correctness_invariants.py \
      tests/property_tests/governance/test_governance_edge_cases.py \
      tests/property_tests/governance/test_governance_invariants_property.py \
      -v \
      --hypothesis-show-statistics
    ```

    Expected results:
    - test_async_governance_invariants.py: 10+ tests
    - test_cache_correctness_invariants.py: 15+ tests
    - test_governance_edge_cases.py: 18+ tests
    - test_governance_invariants_property.py: 25+ existing tests
    - Total: 68+ property tests passing

    Look for:
    - "Hypothesis calls:" line showing number of examples generated
    - All tests PASS status
    - No "Falsified" or "Shrunk" output (indicates found bugs)
    - P99 latency < 1ms for performance tests

    Capture test counts by category:
    - Async governance: XX tests
    - Cache correctness: XX tests
    - Cache consistency: XX tests
    - Cache performance: XX tests
    - Edge cases: XX tests
    - Combinatorial invariants: XX tests
  </action>
  <verify>
    All 68+ property tests pass
    No Hypothesis falsification messages
    Test output shows "Hypothesis calls:" with thousands of examples generated
  </verify>
  <done>
    All property tests passing
    Test counts captured by category
    No invariants violated
  </done>
</task>

<task type="auto">
  <name>Generate property test summary document</name>
  <files>backend/tests/coverage_reports/metrics/phase_123_property_test_summary.md</files>
  <action>
    Create property test summary document for Phase 123.

    File location: backend/tests/coverage_reports/metrics/phase_123_property_test_summary.md

    Content format:
    ```markdown
    # Phase 123 Property Test Summary

    **Generated:** 2026-03-02
    **Phase:** 123 (Governance Property Tests)
    **Status:** COMPLETE

    ## Overview

    Property-based tests validate governance system invariants using Hypothesis to explore thousands of auto-generated test cases. Unlike example-based tests, property tests find edge cases that developers miss.

    ## Test Results

    | Category | Tests | Examples/Test | Status |
    |----------|-------|---------------|--------|
    | Async Governance | XX | 200 | PASS |
    | Cache Correctness | XX | 100 | PASS |
    | Cache Consistency | XX | 100 | PASS |
    | Cache Performance | XX | 200 | PASS |
    | Edge Cases | XX | 100 | PASS |
    | Combinatorial Invariants | XX | 200 | PASS |
    | **TOTAL** | **68+** | **~12,000+** | **PASS** |

    ## Invariants Validated

    ### Maturity Routing Invariants
    - [x] STUDENT agents blocked from complexity 2+ actions
    - [x] AUTONOMOUS agents allowed all complexity levels
    - [x] Maturity levels form total ordering (STUDENT < INTERN < SUPERVISED < AUTONOMOUS)
    - [x] Maturity never decreases (monotonic progression)

    ### Permission Check Invariants
    - [x] Permission checks are idempotent (same inputs -> same outputs)
    - [x] Action complexity matches maturity capability matrix
    - [x] Unknown actions default to complexity 2 (moderate risk)
    - [x] Action matching is case-insensitive

    ### Cache Correctness Invariants
    - [x] Cached values match database queries
    - [x] Cache invalidation removes stale entries after status changes
    - [x] Cache returns consistent results for repeated lookups
    - [x] Cache entries expire correctly after TTL

    ### Cache Performance Invariants
    - [x] P99 cache lookup latency < 1ms
    - [x] P99 cache set latency < 1ms
    - [x] P99 cache invalidate latency < 1ms
    - [x] Cache hit rate > 95% after warming

    ### Async Governance Invariants
    - [x] resolve_agent_for_request() always returns (agent, context) tuple
    - [x] Resolution path always non-empty
    - [x] Invalid agent IDs return None agent but valid context
    - [x] Explicit agent_id takes priority over session agent

    ### Edge Case Invariants
    - [x] Empty action types default to moderate risk
    - [x] None values handled gracefully
    - [x] Special characters in action types don't cause crashes
    - [x] Confidence scores at exact boundaries work correctly

    ## Coverage Gaps Addressed

    From Phase 123 research, three coverage gaps were identified and addressed:

    1. **Async governance methods** (test_async_governance_invariants.py)
       - Gap: AgentContextResolver.resolve_agent_for_request() not covered
       - Solution: 10+ async property tests with @pytest.mark.asyncio pattern
       - Status: CLOSED

    2. **Cache correctness** (test_cache_correctness_invariants.py)
       - Gap: Cache-database consistency not systematically validated
       - Solution: 15+ property tests for correctness, consistency, performance
       - Status: CLOSED

    3. **Edge case combinations** (test_governance_edge_cases.py)
       - Gap: Rare combinations (STUDENT with cached permission trying complexity 4) not tested
       - Solution: 18+ property tests exploring 640+ maturity×action×complexity combos
       - Status: CLOSED

    ## Files Created

    - backend/tests/property_tests/governance/test_async_governance_invariants.py (300+ lines)
    - backend/tests/property_tests/governance/test_cache_correctness_invariants.py (400+ lines)
    - backend/tests/property_tests/governance/test_governance_edge_cases.py (350+ lines)

    ## Requirements Met

    - [x] **PROP-01**: Governance invariants tested — Maturity routing, permission checks, cache consistency validated with Hypothesis (Phase 123, 2026-03-02)

    ## Hypothesis Settings

    All property tests use strategic max_examples values per complexity:

    - CRITICAL (200 examples): Maturity ordering, cache performance, async correctness, combinatorial invariants
    - STANDARD (100 examples): Permission checks, determinism, cache consistency, edge cases
    - IO_BOUND (50 examples): Database-heavy operations

    This balances thoroughness with execution time (total test run ~2-3 minutes).

    ## Next Steps

    Phase 124: Episode Property Tests
    - Validate segmentation invariants
    - Validate retrieval ranking invariants
    - Validate lifecycle transitions

    ---

    *Property testing finds edge cases developers miss. Example-based tests verify known scenarios; property tests verify all scenarios.*
    ```

    Use actual test counts from pytest run output.
  </action>
  <verify>
    File exists at backend/tests/coverage_reports/metrics/phase_123_property_test_summary.md
    Contains test counts by category
    Shows all invariants validated
    Lists coverage gaps addressed
  </verify>
  <done>
    Property test summary document created
    All 68+ tests documented
    Invariants validated listed
  </done>
</task>

<task type="auto">
  <name>Verify critical invariants with targeted Hypothesis runs</name>
  <files>backend/tests/coverage_reports/metrics/phase_123_property_test_summary.md</files>
  <action>
    Run targeted Hypothesis verification for critical invariants.

    Run 1: Maturity routing (most critical invariant)
    ```bash
    cd backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
      tests/property_tests/governance/test_governance_invariants_property.py::TestMaturityLevelInvariants \
      tests/property_tests/governance/test_governance_edge_cases.py::TestCombinatorialInvariants \
      -v \
      --hypothesis-show-statistics
    ```

    Run 2: Cache performance (SLA requirement)
    ```bash
    cd backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
      tests/property_tests/governance/test_cache_correctness_invariants.py::TestCachePerformanceInvariants \
      -v \
      --hypothesis-show-statistics
    ```

    Run 3: Async correctness (gap closure)
    ```bash
    cd backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
      tests/property_tests/governance/test_async_governance_invariants.py \
      -v \
      --hypothesis-show-statistics
    ```

    For each run, verify:
    - No falsified examples (Hypothesis didn't find violations)
    - Examples generated equals max_examples × tests
    - Performance tests show P99 < 1ms

    Update summary document with any additional findings.
  </action>
  <verify>
    Maturity routing tests: 200 examples per test, no violations
    Cache performance tests: P99 < 1ms verified
    Async governance tests: 200 examples per test, no violations
  </verify>
  <done>
    All critical invariants verified with Hypothesis
    No violations found across thousands of test cases
    Performance SLA (<1ms P99) validated
  </done>
</task>

<task type="auto">
  <name>Update REQUIREMENTS.md to mark PROP-01 complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Update REQUIREMENTS.md to mark PROP-01 as complete.

    Find the PROP-01 entry (around line 39) and update:
    ```markdown
    ### Property-Based Testing

    - [x] **PROP-01**: Governance invariants tested — Maturity routing, permission checks, cache consistency validated with Hypothesis (Phase 123, 2026-03-02)
      - 68+ property tests passing
      - 12,000+ Hypothesis-generated examples
      - 3 coverage gaps closed (async, cache correctness, edge cases)
      - Invariants: maturity routing, permission consistency, cache <1ms P99
    ```

    Also update the Traceability table at bottom:
    ```markdown
    | Requirement | Phase | Status |
    |-------------|-------|--------|
    | ...
    | PROP-01 | Phase 123 | Complete |
    | PROP-02 | Phase 124 | Pending |
    | ...
    ```

    Evidence:
    - Property test summary: tests/coverage_reports/metrics/phase_123_property_test_summary.md
    - All 68+ tests passing
    - Invariants validated (maturity routing, permission checks, cache consistency)
  </action>
  <verify>grep -A5 "PROP-01" .planning/REQUIREMENTS.md shows [x] checked</verify>
  <done>PROP-01 marked complete with test counts and invariants validated</done>
</task>

</tasks>

<verification>
1. All 68+ property tests pass in combined run
2. Hypothesis statistics show 12,000+ examples generated
3. No falsified examples (no invariants violated)
4. Property test summary document created
5. Critical invariants verified (maturity routing, cache performance, async correctness)
6. PROP-01 requirement marked complete in REQUIREMENTS.md
7. Coverage gaps from research documented as CLOSED
</verification>

<success_criteria>
1. All Phase 123 property tests passing (68+ tests)
2. Summary document shows 12,000+ Hypothesis examples generated
3. Maturity routing invariants validated
4. Permission check consistency validated
5. Cache invariants validated (correctness, consistency, <1ms P99)
6. PROP-01 requirement marked complete
7. Three coverage gaps from research addressed and closed
</success_criteria>

<output>
After completion, create `.planning/phases/123-governance-property-tests/123-04-SUMMARY.md`
</output>
