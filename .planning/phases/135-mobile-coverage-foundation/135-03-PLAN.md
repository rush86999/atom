---
phase: 135-mobile-coverage-foundation
plan: 03
type: execute
wave: 2
depends_on: [135-02]
files_modified:
  - mobile/src/__tests__/contexts/WebSocketContext.test.tsx
  - mobile/src/__tests__/contexts/DeviceContext.test.tsx
  - mobile/src/__tests__/contexts/AuthContext.test.tsx
autonomous: true
estimated_duration: 12 minutes

must_haves:
  truths:
    - "WebSocketContext tests cover connection, reconnection, streaming"
    - "DeviceContext tests cover camera, location, notifications, biometric"
    - "AuthContext tests cover login, biometric, logout, token refresh"
    - "Context coverage reaches 80%+"
  artifacts:
    - path: "mobile/src/__tests__/contexts/WebSocketContext.test.tsx"
      provides: "WebSocketContext tests"
      min_lines: 400
      exports: ["describe('WebSocketContext')"]
    - path: "mobile/src/__tests__/contexts/DeviceContext.test.tsx"
      provides: "DeviceContext tests"
      min_lines: 300
    - path: "mobile/src/__tests__/contexts/AuthContext.test.tsx"
      provides: "AuthContext tests"
      min_lines: 300
  key_links:
    - from: "mobile/src/__tests__/contexts/WebSocketContext.test.tsx"
      to: "mobile/src/contexts/WebSocketContext.tsx"
      via: "import { WebSocketProvider, useWebSocket }"
      pattern: "from.*contexts/WebSocketContext"
    - from: "mobile/src/__tests__/contexts/DeviceContext.test.tsx"
      to: "mobile/src/contexts/DeviceContext.tsx"
      via: "import { DeviceProvider, useDevice }"
      pattern: "from.*contexts/DeviceContext"
    - from: "mobile/src/__tests__/contexts/AuthContext.test.tsx"
      to: "mobile/src/contexts/AuthContext.tsx"
      via: "import { AuthProvider, useAuth }"
      pattern: "from.*contexts/AuthContext"
---

# Plan 135-03: Test Context Providers

## Objective

Add comprehensive tests for all 3 context providers (WebSocketContext, DeviceContext, AuthContext).

**Purpose:** Contexts are critical state management infrastructure. All components depend on these. High ROI for coverage effort.

**Output:** 70+ context tests, 80%+ context coverage

## Context

@.planning/phases/135-mobile-coverage-foundation/135-RESEARCH.md
@mobile/src/contexts/WebSocketContext.tsx
@mobile/src/contexts/DeviceContext.tsx
@mobile/src/contexts/AuthContext.tsx

From research:
- WebSocketContext: 644 lines, real-time agent communication
- DeviceContext: Device capabilities and permissions
- AuthContext: Authentication and biometric auth
- Existing AuthContext tests need enhancement

## Tasks

<task type="auto">
  <name>Task 1: Test WebSocketContext</name>
  <files>mobile/src/__tests__/contexts/WebSocketContext.test.tsx</files>
  <action>
Create comprehensive WebSocketContext tests (30+ tests):

```typescript
import { renderHook, waitFor, act } from '@testing-library/react-native';
import { WebSocketProvider, useWebSocket, useAgentChat } from '../../contexts/WebSocketContext';
import { io, Socket } from 'socket.io-client';

jest.mock('socket.io-client');
jest.mock('@react-native-async-storage/async-storage');
jest.mock('../../contexts/AuthContext');

const mockSocket = {
  connected: false,
  on: jest.fn(),
  emit: jest.fn(),
  disconnect: jest.fn(),
  connect: jest.fn(),
  once: jest.fn(),
};

(io as jest.MockedFunction<typeof io>).mockReturnValue(mockSocket as any);

describe('WebSocketContext', () => {
  const wrapper = ({ children }: { children: React.ReactNode }) => (
    <WebSocketProvider>{children}</WebSocketProvider>
  );

  beforeEach(() => {
    jest.clearAllMocks();
    mockSocket.connected = false;
  });

  // Connection tests (6 tests)
  describe('Connection', () => {
    it('should connect when authenticated', async () => {
      // Test: socket connection triggered
    });

    it('should handle connection success', async () => {
      // Test: connected state, heartbeat started
    });

    it('should handle connection error', async () => {
      // Test: error state set, reconnect attempted
    });

    it('should track connecting state', async () => {
      // Test: isConnecting true during connection
    });

    it('should track connection quality', async () => {
      // Test: excellent/good/poor based on latency
    });

    it('should cleanup on unmount', async () => {
      // Test: disconnect, intervals cleared
    });
  });

  // Reconnection tests (3 tests)
  describe('Reconnection', () => {
    it('should reconnect on disconnect', async () => {});
    it('should stop reconnecting after max attempts', async () => {});
    it('should rejoin rooms after reconnection', async () => {});
  });

  // Streaming tests (4 tests)
  describe('Streaming', () => {
    it('should send streaming message', async () => {});
    it('should handle streaming chunks', async () => {});
    it('should handle streaming complete', async () => {});
    it('should handle streaming error', async () => {});
  });

  // Room management (3 tests)
  describe('Rooms', () => {
    it('should join room and persist to storage', async () => {});
    it('should leave room and remove from storage', async () => {});
    it('should rejoin rooms after reconnection', async () => {});
  });

  // Heartbeat tests (2 tests)
  describe('Heartbeat', () => {
    it('should send heartbeat every 30 seconds', async () => {});
    it('should update connection quality based on latency', async () => {});
  });

  // Agent chat hook tests (2 tests)
  describe('useAgentChat', () => {
    it('should manage chat messages', async () => {});
    it('should send message via socket', async () => {});
  });
});
```

Focus on: connection lifecycle, reconnection logic, streaming callbacks, room persistence
  </action>
  <verify>npm test -- contexts --testPathPattern=WebSocket passes</verify>
  <done>30+ WebSocketContext tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Enhance DeviceContext tests</name>
  <files>mobile/src/__tests__/contexts/DeviceContext.test.tsx</files>
  <action>
Add missing DeviceContext tests (20+ tests):

```typescript
import { renderHook, act } from '@testing-library/react-native';
import { DeviceProvider, useDevice } from '../../contexts/DeviceContext';

jest.mock('expo-camera');
jest.mock('expo-location');
jest.mock('expo-notifications');
jest.mock('expo-local-authentication');

describe('DeviceContext', () => {
  // Camera tests (4 tests)
  describe('Camera', () => {
    it('should check camera availability', async () => {});
    it('should request camera permissions', async () => {});
    it('should handle camera permission denial', async () => {});
    it('should get camera capabilities', async () => {});
  });

  // Location tests (4 tests)
  describe('Location', () => {
    it('should request location permissions', async () => {});
    it('should get current location', async () => {});
    it('should start location tracking', async () => {});
    it('should stop location tracking', async () => {});
  });

  // Notification tests (3 tests)
  describe('Notifications', () => {
    it('should request notification permissions', async () => {});
    it('should send local notification', async () => {});
    it('should schedule notification', async () => {});
  });

  // Biometric tests (4 tests)
  describe('Biometric', () => {
    it('should check biometric availability', async () => {});
    it('should authenticate with biometrics', async () => {});
    it('should handle biometric unavailability', async () => {});
    it('should get biometric type', async () => {});
  });

  // Platform tests (2 tests)
  describe('Platform', () => {
    it('should detect platform correctly', () => {});
    it('should provide device info', () => {});
  });
});
```

Enhance existing file with missing capabilities
  </action>
  <verify>npm test -- contexts --testPathPattern=DeviceContext passes</verify>
  <done>20+ DeviceContext tests passing</done>
</task>

<task type="auto">
  <name>Task 3: Complete AuthContext tests</name>
  <files>mobile/src/__tests__/contexts/AuthContext.test.tsx</files>
  <action>
Add missing AuthContext tests (20+ tests):

```typescript
import { renderHook, act, waitFor } from '@testing-library/react-native';
import { AuthProvider, useAuth } from '../../contexts/AuthContext';

jest.mock('@react-native-async-storage/async-storage');
jest.mock('expo-local-authentication');
jest.mock('../../services/api');

describe('AuthContext', () => {
  // Login flow (4 tests)
  describe('Login', () => {
    it('should login successfully with email/password', async () => {});
    it('should handle login failure', async () => {});
    it('should store token on login', async () => {});
    it('should navigate to home on login', async () => {});
  });

  // Biometric auth (3 tests)
  describe('Biometric', () => {
    it('should authenticate with biometrics', async () => {});
    it('should handle biometric unavailability', async () => {});
    it('should handle biometric enrollment check', async () => {});
  });

  // Logout (2 tests)
  describe('Logout', () => {
    it('should logout and clear tokens', async () => {});
    it('should navigate to login on logout', async () => {});
  });

  // Token management (2 tests)
  describe('Token', () => {
    it('should refresh expired token', async () => {});
    it('should handle token refresh failure', async () => {});
  });

  // User state (3 tests)
  describe('User', () => {
    it('should provide user data', () => {});
    it('should update user profile', async () => {});
    it('should clear user on logout', async () => {});
  });

  // Registration (3 tests)
  describe('Registration', () => {
    it('should register new user', async () => {});
    it('should handle registration failure', async () => {});
    it('should login after registration', async () => {});
  });

  // Password reset (3 tests)
  describe('Password', () => {
    it('should request password reset', async () => {});
    it('should handle reset failure', async () => {});
    it('should reset password with token', async () => {});
  });
});
```

Complete existing test file with missing scenarios
  </action>
  <verify>npm test -- contexts --testPathPattern=AuthContext passes</verify>
  <done>20+ AuthContext tests passing</done>
</task>

## Verification

```bash
cd mobile && npm test -- contexts --coverage
```

Expected:
- All context tests passing (70+ tests)
- Coverage for contexts: 80%+
- Test file sizes: 300-500 lines each

## Success Criteria

1. WebSocketContext test file created with 30+ tests
2. DeviceContext tests enhanced with 20+ tests
3. AuthContext tests completed with 20+ tests
4. Context coverage reaches 80%+
5. All context use cases tested

## Output

After completion, create `.planning/phases/135-mobile-coverage-foundation/135-03-SUMMARY.md`
