---
phase: 135-mobile-coverage-foundation
plan: 04B
type: execute
wave: 2
depends_on: [135-02]
files_modified:
  - mobile/src/__tests__/services/offlineSyncService.test.ts
  - mobile/src/__tests__/services/canvasSyncService.test.ts
autonomous: true
estimated_duration: 12 minutes

must_haves:
  truths:
    - "offlineSyncService tests cover queue management, sync execution, network handling, conflict resolution"
    - "offlineSyncService queue tests verify priority sorting, storage quota, cleanup"
    - "offlineSyncService conflict tests detect server timestamp conflicts and apply resolutions"
    - "canvasSyncService tests verify list sync, single sync, form sync, state tracking"
    - "Sync service coverage reaches 75%+ for offline functionality"
  artifacts:
    - path: "mobile/src/__tests__/services/offlineSyncService.test.ts"
      provides: "Offline sync service tests"
      min_lines: 800
    - path: "mobile/src/__tests__/services/canvasSyncService.test.ts"
      provides: "Canvas sync service tests"
      min_lines: 300
  key_links:
    - from: "mobile/src/__tests__/services/offlineSyncService.test.ts"
      to: "mobile/src/services/offlineSyncService.ts"
      via: "import { offlineSyncService }"
      pattern: "from.*services/offlineSyncService"
---

# Plan 135-04B: Test Sync Services

## Objective

Add comprehensive tests for sync services (offlineSyncService, canvasSyncService).

**Purpose:** These services handle critical offline functionality and canvas synchronization. offlineSyncService is large (1,146 lines) with complex queue/conflict logic.

**Output:** 50+ sync service tests, 75%+ coverage for sync services

## Context

@.planning/phases/135-mobile-coverage-foundation/135-RESEARCH.md
@mobile/src/services/offlineSyncService.ts

From research:
- offlineSyncService.ts: 1,146 lines - offline queue and sync
- canvasSyncService.ts: ~300 lines - canvas synchronization

**Note:** Split from original 135-04 to manage scope. Plan 135-04A covers agent integration services (agentDeviceBridge, workflowSyncService).

## Tasks

<task type="auto">
  <name>Task 1: Enhance offlineSyncService tests (30+ tests)</name>
  <files>mobile/src/__tests__/services/offlineSyncService.test.ts</files>
  <action>
Add comprehensive sync tests to existing file:

```typescript
import { offlineSyncService, OfflineActionType, SyncPriority } from '../../services/offlineSyncService';

jest.mock('@react-native-community/netinfo');
jest.mock('../../services/storageService');
jest.mock('../../services/api');

describe('offlineSyncService', () => {
  beforeEach(async () => {
    await offlineSyncService.initialize();
  });

  // Queue management (6 tests)
  describe('Queue', () => {
    it('should queue action with priority', async () => {});

    it('should sort queue by priority', async () => {});

    it('should check storage quota before queueing', async () => {});

    it('should cleanup old entries when quota exceeded', async () => {});

    it('should get pending count', async () => {});

    it('should clear queue', async () => {});
  });

  // Sync execution (8 tests)
  describe('Sync Execution', () => {
    it('should sync actions when online', async () => {});

    it('should sync critical actions immediately', async () => {});

    it('should handle sync failure with retry', async () => {});

    it('should stop retrying after max attempts', async () => {});

    it('should process in batches', async () => {});

    it('should cancel ongoing sync', async () => {});

    it('should not sync when offline', async () => {});

    it('should remove completed actions', async () => {});
  });

  // Network handling (3 tests)
  describe('Network', () => {
    it('should trigger sync when connection restored', async () => {});

    it('should subscribe to network changes', async () => {});

    it('should start periodic sync', async () => {});
  });

  // Conflict resolution (5 tests)
  describe('Conflicts', () => {
    it('should detect server timestamp conflict', async () => {});

    it('should apply last_write_wins resolution', async () => {});

    it('should apply server_wins resolution', async () => {});

    it('should apply client_wins resolution', async () => {});

    it('should subscribe to conflict notifications', async () => {});
  });

  // Entity-specific sync (4 tests)
  describe('Entity Sync', () => {
    it('should sync agent entity', async () => {});

    it('should sync workflow entity', async () => {});

    it('should sync canvas entity', async () => {});

    it('should sync episode entity', async () => {});
  });

  // State management (4 tests)
  describe('State', () => {
    it('should update sync state correctly', async () => {});

    it('should subscribe to state changes', async () => {});

    it('should subscribe to progress updates', async () => {});

    it('should get sync state', async () => {});
  });

  // Quality metrics (2 tests)
  describe('Quality', () => {
    it('should track quality metrics', async () => {});

    it('should get storage quota', async () => {});
  });
});
```

Enhance existing test file with comprehensive scenarios
  </action>
  <verify>npm test -- services --testPathPattern=offlineSync passes</verify>
  <done>30+ offlineSyncService tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Test canvasSyncService (15+ tests)</name>
  <files>mobile/src/__tests__/services/canvasSyncService.test.ts</files>
  <action>
Create canvas sync tests:

```typescript
import { canvasSyncService } from '../../services/canvasSyncService';

jest.mock('../../services/api');
jest.mock('../../services/storageService');

describe('canvasSyncService', () => {
  // List sync (3 tests)
  describe('List Sync', () => {
    it('should sync canvas list', async () => {});
    it('should handle empty list', async () => {});
    it('should filter by type', async () => {});
  });

  // Single sync (4 tests)
  describe('Single Sync', () => {
    it('should sync single canvas', async () => {});
    it('should sync canvas updates', async () => {});
    it('should handle canvas not found', async () => {});
    it('should resolve canvas conflicts', async () => {});
  });

  // Form sync (4 tests)
  describe('Form Sync', () => {
    it('should sync form submissions', async () => {});
    it('should queue form submits offline', async () => {});
    it('should handle form validation errors', async () => {});
    it('should track form submit status', async () => {});
  });

  // State (4 tests)
  describe('State', () => {
    it('should track sync progress', async () => {});
    it('should get last sync time', async () => {});
    it('should force refresh', async () => {});
    it('should handle partial sync', async () => {});
  });
});
```

Create new test file with canvas-specific scenarios
  </action>
  <verify>npm test -- services --testPathPattern=canvasSync passes</verify>
  <done>15+ canvasSyncService tests passing</done>
</task>

## Verification

```bash
cd mobile && npm test -- services --testPathPattern="offlineSync|canvasSync" --coverage
```

Expected:
- All sync service tests passing (45+ tests)
- Coverage for sync services: 75%+
- Queue, conflict resolution, and entity sync fully tested

## Success Criteria

1. offlineSyncService test enhanced with 30+ tests
2. canvasSyncService test created with 15+ tests
3. Queue management tested (priority, sorting, quota, cleanup)
4. Conflict resolution tested (server_wins, client_wins, last_write_wins)
5. Sync service coverage reaches 75%+

## Output

After completion, create `.planning/phases/135-mobile-coverage-foundation/135-04B-SUMMARY.md`
