---
phase: 135-mobile-coverage-foundation
plan: 05
type: execute
wave: 2
depends_on: [135-02]
files_modified:
  - mobile/src/__tests__/screens/chat/ChatTabScreen.test.tsx
  - mobile/src/__tests__/screens/chat/ConversationListScreen.test.tsx
  - mobile/src/__tests__/screens/agent/AgentListScreen.test.tsx
  - mobile/src/__tests__/screens/agent/AgentChatScreen.test.tsx
  - mobile/src/__tests__/components/chat/StreamingText.test.tsx
  - mobile/src/__tests__/components/chat/MessageList.test.tsx
  - mobile/src/__tests__/components/chat/TypingIndicator.test.tsx
  - mobile/src/__tests__/components/chat/MessageInput.test.tsx
  - mobile/src/__tests__/navigation/AppNavigator.test.tsx
  - mobile/src/__tests__/navigation/AuthNavigator.test.tsx
autonomous: true
estimated_duration: 15 minutes

must_haves:
  truths:
    - "Chat screens render and handle user interactions"
    - "Agent screens display and navigate correctly"
    - "Chat components handle streaming, messages, input"
    - "Navigation routes between screens correctly"
    - "Screen coverage reaches 70%+"
  artifacts:
    - path: "mobile/src/__tests__/screens/chat/ChatTabScreen.test.tsx"
      provides: "Chat tab screen tests"
      min_lines: 300
    - path: "mobile/src/__tests__/components/chat/StreamingText.test.tsx"
      provides: "Streaming text component tests"
      min_lines: 200
    - path: "mobile/src/__tests__/navigation/AppNavigator.test.tsx"
      provides: "App navigator tests"
      min_lines: 250
  key_links:
    - from: "mobile/src/__tests__/screens/chat/ChatTabScreen.test.tsx"
      to: "mobile/src/screens/chat/ChatTabScreen.tsx"
      via: "import { ChatTabScreen }"
      pattern: "from.*screens/chat/ChatTabScreen"
    - from: "mobile/src/__tests__/navigation/AppNavigator.test.tsx"
      to: "mobile/src/navigation/AppNavigator.tsx"
      via: "import AppNavigator"
      pattern: "from.*navigation/AppNavigator"
---

# Plan 135-05: Test Screens and Navigation

## Objective

Add tests for critical screens and navigation structure.

**Purpose:** Screens are high-visibility UX. Navigation is app infrastructure. Combined ~2,000 lines of code.

**Output:** 75+ screen/component/navigation tests, 70%+ coverage

## Context

@.planning/phases/135-mobile-coverage-foundation/135-RESEARCH.md
@mobile/src/screens/chat/ChatTabScreen.tsx
@mobile/src/navigation/AppNavigator.tsx

From research:
- 21 screen files, 8 untested
- Navigation files: 100% untested (4 files)
- Chat screens are critical UX (ChatTabScreen, ConversationListScreen, AgentChatScreen)

## Tasks

<task type="auto">
  <name>Task 1: Test chat screens (30+ tests)</name>
  <files>mobile/src/__tests__/screens/chat/*.test.tsx</files>
  <action>
Create comprehensive chat screen tests:

**ChatTabScreen.test.tsx** (8 tests):
```typescript
import { render, screen, fireEvent, waitFor } from '@testing-library/react-native';
import { ChatTabScreen } from '../../screens/chat/ChatTabScreen';

jest.mock('../../contexts/WebSocketContext');
jest.mock('../../contexts/AuthContext');

describe('ChatTabScreen', () => {
  it('should render chat interface', () => {});
  it('should display message list', () => {});
  it('should handle message input', async () => {});
  it('should show typing indicator', () => {});
  it('should handle streaming messages', async () => {});
  it('should scroll to latest message', async () => {});
  it('should handle empty message state', () => {});
  it('should handle error state', () => {});
});
```

**ConversationListScreen.test.tsx** (8 tests):
```typescript
describe('ConversationListScreen', () => {
  it('should render conversation list', () => {});
  it('should filter conversations by agent', () => {});
  it('should handle conversation selection', () => {});
  it('should show unread badges', () => {});
  it('should handle pull to refresh', () => {});
  it('should navigate to chat on press', () => {});
  it('should show empty state', () => {});
  it('should handle loading state', () => {});
});
```

**AgentChatScreen.test.tsx** - enhance existing (8 tests):
```typescript
describe('AgentChatScreen', () => {
  it('should render agent info header', () => {});
  it('should display agent messages', () => {});
  it('should handle canvas presentation', () => {});
  it('should handle agent suggestions', () => {});
  it('should show agent maturity level', () => {});
  it('should handle attachment button', () => {});
  it('should handle voice input button', () => {});
  it('should navigate back on press', () => {});
});
```

Focus: message rendering, streaming, navigation, state handling
  </action>
  <verify>npm test -- screens --testPathPattern=chat passes</verify>
  <done>24+ chat screen tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Test agent screens (15+ tests)</name>
  <files>mobile/src/__tests__/screens/agent/*.test.tsx</files>
  <action>
Create agent screen tests:

**AgentListScreen.test.tsx** - new (8 tests):
```typescript
import { render, screen, fireEvent } from '@testing-library/react-native';
import { AgentListScreen } from '../../screens/agent/AgentListScreen';

jest.mock('../../contexts/AuthContext');
jest.mock('@react-navigation/native');

describe('AgentListScreen', () => {
  it('should render agent list', () => {});
  it('should filter agents by maturity', () => {});
  it('should search agents by name', () => {});
  it('should show agent status indicators', () => {});
  it('should handle agent selection', () => {});
  it('should navigate to agent chat', () => {});
  it('should show create agent FAB', () => {});
  it('should show empty state', () => {});
});
```

**AgentDetailScreen.test.tsx** - new (7 tests):
```typescript
describe('AgentDetailScreen', () => {
  it('should render agent details', () => {});
  it('should show agent description', () => {});
  it('should display agent maturity level', () => {});
  it('should handle start chat button', () => {});
  it('should handle edit agent', () => {});
  it('should handle delete agent', () => {});
  it('should show loading state', () => {});
});
```

Focus: list rendering, filtering, navigation, actions
  </action>
  <verify>npm test -- screens --testPathPattern=agent passes</verify>
  <done>15+ agent screen tests passing</done>
</task>

<task type="auto">
  <name>Task 3: Test chat components (15+ tests)</name>
  <files>mobile/src/__tests__/components/chat/*.test.tsx</files>
  <action>
Create chat component tests:

**StreamingText.test.tsx** - new (5 tests):
```typescript
import { render } from '@testing-library/react-native';
import { StreamingText } from '../../components/chat/StreamingText';

describe('StreamingText', () => {
  it('should render text content', () => {});
  it('should handle streaming chunks', () => {});
  it('should show cursor while streaming', () => {});
  it('should apply markdown formatting', () => {});
  it('should handle empty text', () => {});
});
```

**MessageList.test.tsx** - new (4 tests):
```typescript
describe('MessageList', () => {
  it('should render list of messages', () => {});
  it('should differentiate user/agent messages', () => {});
  it('should show timestamps', () => {});
  it('should invert scroll for new messages', () => {});
});
```

**TypingIndicator.test.tsx** - new (3 tests):
```typescript
describe('TypingIndicator', () => {
  it('should show animated dots', () => {});
  it('should display typing agent names', () => {});
  it('should hide when not typing', () => {});
});
```

**MessageInput.test.tsx** - new (3 tests):
```typescript
describe('MessageInput', () => {
  it('should handle text input', () => {});
  it('should send on button press', () => {});
  it('should clear after send', () => {});
});
```

Focus: rendering, user interaction, edge cases
  </action>
  <verify>npm test -- components --testPathPattern=chat passes</verify>
  <done>15+ chat component tests passing</done>
</task>

<task type="auto">
  <name>Task 4: Test navigation (15+ tests)</name>
  <files>mobile/src/__tests__/navigation/*.test.tsx</files>
  <action>
Create navigation tests:

**AppNavigator.test.tsx** - new (8 tests):
```typescript
import { render } from '@testing-library/react-native';
import { NavigationContainer } from '@react-navigation/native';
import AppNavigator from '../../navigation/AppNavigator';

jest.mock('../../screens/auth/LoginScreen');
jest.mock('../../screens/chat/ChatTabScreen');
jest.mock('../../contexts/AuthContext', () => ({
  useAuth: () => ({ isAuthenticated: true, user: { id: '1' } }),
}));

describe('AppNavigator', () => {
  it('should render auth stack when not authenticated', () => {});
  it('should render app stack when authenticated', () => {});
  it('should handle deep linking', () => {});
  it('should handle navigation state changes', () => {});
  it('should handle tab navigation', () => {});
  it('should handle nested navigation', () => {});
  it('should handle navigation params', () => {});
  it('should handle back button', () => {});
});
```

**AuthNavigator.test.tsx** - new (4 tests):
```typescript
describe('AuthNavigator', () => {
  it('should show login screen initially', () => {});
  it('should navigate to register', () => {});
  it('should navigate to forgot password', () => {});
  it('should handle biometric auth screen', () => {});
});
```

**MainTabsNavigator.test.tsx** - new (3 tests):
```typescript
describe('MainTabsNavigator', () => {
  it('should render all tabs', () => {});
  it('should switch tabs on press', () => {});
  it('should show active tab indicator', () => {});
});
```

Focus: routing, deep links, state, navigation types
  </action>
  <verify>npm test -- navigation passes</verify>
  <done>15+ navigation tests passing</done>
</task>

## Verification

```bash
cd mobile && npm test -- screens --coverage
cd mobile && npm test -- components/chat --coverage
cd mobile && npm test -- navigation --coverage
```

Expected:
- All screen/component tests passing (75+ tests)
- Coverage for screens: 70%+
- Coverage for navigation: 70%+

## Success Criteria

1. Chat screen tests created with 24+ tests
2. Agent screen tests created with 15+ tests
3. Chat component tests created with 15+ tests
4. Navigation tests created with 15+ tests
5. Screen/navigation coverage reaches 70%+

## Output

After completion, create `.planning/phases/135-mobile-coverage-foundation/135-05-SUMMARY.md`
