---
phase: 177-api-routes-coverage-analytics-reporting
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_ab_testing_routes.py
  - backend/tests/api/conftest.py
autonomous: true

must_haves:
  truths:
    - "A/B testing routes achieve 75%+ line coverage"
    - "All A/B testing endpoints tested (create, start, complete, assign, record, results, list)"
    - "ABTestingService mocked with deterministic test data"
    - "Request validation tested (Pydantic model validation)"
    - "Error paths tested (not found, service errors, validation errors)"
  artifacts:
    - path: "backend/tests/api/test_ab_testing_routes.py"
      provides: "A/B testing routes test coverage"
      min_lines: 600
    - path: "backend/tests/api/conftest.py"
      provides: "A/B testing fixtures"
      exports: ["mock_ab_testing_service", "ab_testing_client"]
  key_links:
    - from: "test_ab_testing_routes.py"
      to: "ab_testing.py"
      via: "TestClient with per-file FastAPI app"
      pattern: "TestClient.*ab.*router"
    - from: "mock_ab_testing_service"
      to: "ABTestingService"
      via: "AsyncMock pattern"
      pattern: "AsyncMock.*ABTestingService"
---

<objective>
Achieve 75%+ line coverage on A/B testing routes (ab_testing.py) through comprehensive TestClient-based API testing.

Purpose: A/B testing routes enable creating and managing experiments for agent configuration, prompts, and strategies. These endpoints are critical for data-driven agent optimization.

Output: 600+ lines of test code covering all A/B testing endpoints with 75%+ line coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/api/ab_testing.py
@.planning/phases/172-high-impact-zero-coverage-governance/172-01-SUMMARY.md
@.planning/phases/167-api-routes-coverage/167-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create A/B testing fixtures</name>
  <files>backend/tests/api/conftest.py</files>
  <action>
    Add A/B testing fixtures to backend/tests/api/conftest.py:

    1. mock_ab_testing_service: AsyncMock for ABTestingService
       - create_test() returns dict with test_id, status="draft", created_at
       - create_test() returns {"error": "..."} for validation failures
       - start_test() returns dict with status="running", started_at
       - start_test() returns {"error": "..."} for already started/completed tests
       - complete_test() returns dict with results, winner, p_value
       - complete_test() returns {"error": "..."} for tests with insufficient data
       - assign_variant() returns dict with variant ("A" or "B"), config, existing_assignment
       - assign_variant() returns {"error": "..."} for non-existent tests
       - record_metric() returns dict with metric_id, recorded_at
       - record_metric() returns {"error": "..."} for invalid data
       - get_test_results() returns dict with variant_a, variant_b, winner
       - get_test_results() returns {"error": "..."} for non-existent tests
       - list_tests() returns list of test dicts
       - list_tests() filters by agent_id, status when provided

    2. sample_test_request: Factory for CreateTestRequest
       - name: "Test A"
       - test_type: "prompt"
       - agent_id: "test-agent"
       - variant_a_config: {"temperature": 0.7}
       - variant_b_config: {"temperature": 0.9}
       - primary_metric: "satisfaction_rate"
       - traffic_percentage: 0.5
       - min_sample_size: 100
       - confidence_level: 0.95

    3. ab_testing_client: TestClient with A/B testing router
       - Uses per-file FastAPI app pattern
       - Includes ab_testing.py router with /api/ab-tests prefix

    4. mock_db_session: Mock Session for database dependency
       - Used for get_db dependency injection

    Follow Phase 172-01 fixture pattern.
  </action>
  <verify>grep -E "(mock_ab_testing_service|sample_test_request|ab_testing_client)" backend/tests/api/conftest.py</verify>
  <done>4 new fixtures added to conftest.py for A/B testing</done>
</task>

<task type="auto">
  <name>Create A/B testing routes tests</name>
  <files>backend/tests/api/test_ab_testing_routes.py</files>
  <action>
    Create test_ab_testing_routes.py with 600+ lines covering:

    1. TestCreateTest (8 tests):
       - test_create_test_success - Creates test with valid data
       - test_create_test_with_all_fields - Includes description, secondary_metrics
       - test_create_test_default_values - Uses defaults for optional fields
       - test_create_test_prompt_type - Handles test_type="prompt"
       - test_create_test_agent_config_type - Handles test_type="agent_config"
       - test_create_test_validation_error - Returns 400 for invalid data
       - test_create_test_traffic_percentage_validation - Enforces ge=0.0, le=1.0
       - test_create_test_confidence_validation - Enforces ge=0.0, le=1.0

    2. TestStartTest (5 tests):
       - test_start_test_success - Changes status to running
       - test_start_test_includes_timestamp - Returns started_at
       - test_start_test_already_running - Returns error for already running test
       - test_start_test_not_found - Returns 404 for non-existent test
       - test_start_test_error_handling - Handles service exceptions

    3. TestCompleteTest (6 tests):
       - test_complete_test_success - Completes test and calculates results
       - test_complete_test_includes_results - Returns variant_a_metrics, variant_b_metrics
       - test_complete_test_includes_winner - Returns winner (A, B, or inconclusive)
       - test_complete_test_includes_p_value - Returns statistical significance
       - test_complete_test_insufficient_data - Returns error for insufficient sample size
       - test_complete_test_not_found - Returns 404 for non-existent test

    4. TestAssignVariant (7 tests):
       - test_assign_variant_success - Assigns user to variant
       - test_assign_variant_consistent - Same user gets same variant
       - test_assign_variant_returns_config - Includes variant configuration
       - test_assign_variant_existing_assignment - Indicates existing_assignment=True
       - test_assign_variant_with_session - Handles optional session_id
       - test_assign_variant_not_found - Returns 400 for non-existent test
       - test_assign_variant_error_handling - Handles service exceptions

    5. TestRecordMetric (6 tests):
       - test_record_metric_with_success - Records boolean success metric
       - test_record_metric_with_value - Records numerical metric value
       - test_record_metric_with_metadata - Includes metadata in recording
       - test_record_metric_minimal - Records with only required user_id
       - test_record_metric_validation - Returns error for invalid data
       - test_record_metric_not_found - Returns 400 for non-existent test

    6. TestGetTestResults (5 tests):
       - test_get_test_results_success - Returns test results
       - test_get_test_results_includes_variants - Returns variant_a and variant_b data
       - test_get_test_results_winner - Includes winner when completed
       - test_get_test_results_not_found - Returns 404 for non-existent test
       - test_get_test_results_error_handling - Handles service exceptions

    7. TestListTests (6 tests):
       - test_list_tests_success - Returns list of tests
       - test_list_tests_filtered_by_agent - Filters by agent_id
       - test_list_tests_filtered_by_status - Filters by status (draft, running, completed)
       - test_list_tests_with_limit - Respects limit parameter
       - test_list_tests_empty - Returns empty list when no tests
       - test_list_tests_limit_validation - Enforces ge=1, le=100

    8. TestRequestModels (4 tests):
       - test_create_test_request_required_fields - Validates required fields
       - test_create_test_request_optional_fields - Handles optional fields correctly
       - test_assign_variant_request_required_fields - Validates user_id required
       - test_record_metric_request_at_least_one - Requires success or metric_value

    9. TestErrorResponses (4 tests):
       - test_error_response_format - Consistent error format
       - test_error_response_code_includes_ab_test_error - Uses AB_TEST_ERROR code
       - test_not_found_response_format - 404 response format
       - test_validation_error_response - 400 for validation errors

    10. TestTestTypes (4 tests):
       - test_create_test_agent_config_type - Handles agent_config tests
       - test_create_test_prompt_type - Handles prompt tests
       - test_create_test_strategy_type - Handles strategy tests
       - test_create_test_tool_type - Handles tool tests

    Use TestClient pattern from Phase 172-01.
    Use AsyncMock for all service methods.
    Test Pydantic model validation (required fields, constraints).
    Test error paths: 400 for validation, 404 for not found, 500 for service errors.
  </action>
  <verify>pytest backend/tests/api/test_ab_testing_routes.py -v --collect-only | grep "test_" | wc -l</verify>
  <done>55+ tests created covering all A/B testing endpoints</done>
</task>

<task type="auto">
  <name>Measure A/B testing routes coverage</name>
  <files>backend/tests/api/test_ab_testing_routes.py</files>
  <action>
    Run coverage measurement on A/B testing routes:

    1. Execute pytest with coverage:
       pytest backend/tests/api/test_ab_testing_routes.py \
         --cov=backend/api/ab_testing \
         --cov-report=term-missing \
         --cov-report=json \
         -v

    2. Parse coverage JSON output to extract line coverage percentage

    3. If coverage < 75%:
       - Identify uncovered lines (missing from coverage report)
       - Add targeted tests for uncovered code paths
       - Common gaps: error handling, validation, edge cases

    4. Target coverage improvements:
       - Service error handling in all endpoint try/catch blocks
       - Error response formatting (router.error_response, router.not_found_error)
       - Pydantic validation paths for all request models
       - Query parameter validation (limit, agent_id, status filters)

    5. Re-run coverage after improvements

    Use Phase 172-01 pattern: 74.6% coverage achieved with 43 tests.
  </action>
  <verify>pytest backend/tests/api/test_ab_testing_routes.py --cov=backend/api/ab_testing --cov-report=term-missing -v | grep -E "TOTAL.*[0-9]+%"</verify>
  <done>75%+ line coverage achieved on ab_testing.py</done>
</task>

</tasks>

<verification>
1. All tests pass: pytest backend/tests/api/test_ab_testing_routes.py -v
2. Coverage >= 75% on ab_testing.py
3. All endpoints tested: create, start, complete, assign, record, results, list
4. Fixtures added to conftest.py: mock_ab_testing_service, sample_test_request
5. Request validation tested for all Pydantic models
</verification>

<success_criteria>
1. 75%+ line coverage on ab_testing.py
2. 55+ passing tests covering all A/B testing endpoints
3. 600+ lines of test code
4. All test types covered (agent_config, prompt, strategy, tool)
5. Request/response validation tested
</success_criteria>

<output>
After completion, create `.planning/phases/177-api-routes-coverage-analytics-reporting/177-04-SUMMARY.md`
</output>
