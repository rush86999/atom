---
phase: 122-admin-routes-coverage
plan: 06
type: execute
wave: 1
depends_on:
  - 122-05
files_modified:
  - backend/tests/test_world_model.py
  - backend/tests/coverage_reports/metrics/phase_122_final_gap_closure_coverage.json
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Memory archival method tested: archive_session_to_cold_storage"
    - "Session archival combines messages into single document for LanceDB"
    - "Coverage report shows 60%+ for agent_world_model.py"
    - "All 3 gaps from VERIFICATION.md closed"
  artifacts:
    - path: "backend/tests/test_world_model.py"
      provides: "Complete test coverage for WorldModelService"
      min_lines: 650
    - path: "backend/tests/coverage_reports/metrics/phase_122_final_gap_closure_coverage.json"
      provides: "Final coverage report exceeding 60% target"
  key_links:
    - from: "backend/tests/test_world_model.py"
      to: "backend/core/agent_world_model.py"
      via: "archive_session_to_cold_storage method"
      pattern: "class TestArchiveSessionToColdStorage"
---

<objective>
**Gap Closure: Memory archival and final coverage verification**

This plan closes Gap 3 from VERIFICATION.md (archive_session_to_cold_storage) and provides final coverage verification. Combined with Plans 04 and 05, this should achieve the 60%+ coverage target for agent_world_model.py.

Purpose: Close final gap and verify 60%+ coverage achieved
Output: Complete test coverage for WorldModelService with verification report
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/122-admin-routes-coverage/122-VERIFICATION.md
@.planning/phases/122-admin-routes-coverage/122-05-SUMMARY.md

# Reference: agent_world_model.py archive_session_to_cold_storage method
@backend/core/agent_world_model.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests for archive_session_to_cold_storage()</name>
  <files>backend/tests/test_world_model.py</files>
  <action>
    Add test class TestArchiveSessionToColdStorage with 2-3 tests:

    1. test_archive_session_to_cold_storage_success
       - Create service with mocked LanceDBHandler
       - Mock ChatMessage query with real database session pattern
       - Create mock ChatMessage objects:
         * conversation_id="conv-1"
         * 3 messages with role="user"/"assistant" and content
       - Mock db.add_document to return True
       - Call archive_session_to_cold_storage(conversation_id="conv-1")
       - Verify messages queried with correct filters (conversation_id, tenant_id)
       - Verify session_text combines messages as "role: content\n"
       - Verify add_document called with:
         * table_name="archived_memories"
         * session_text containing all messages
         * metadata with msg_count=3, type="archived_session"
       - Verify returns True

    2. test_archive_session_to_cold_storage_returns_false_when_no_messages
       - Mock ChatMessage query to return empty list
       - Call archive_session_to_cold_storage(conversation_id="empty-conv")
       - Verify returns False

    3. test_archive_session_to_cold_storage_handles_database_error
       - Mock ChatMessage.query to raise Exception
       - Call archive_session_to_cold_storage(conversation_id="error-conv")
       - Verify returns False
       - Verify logger.error called

    Use @patch('core.agent_world_model.get_db_session') for database mocking
    Mock ChatMessage model query chain: filter().order_by().all()

    From lines 560-604 of agent_world_model.py
  </action>
  <verify>pytest backend/tests/test_world_model.py::TestArchiveSessionToColdStorage -v</verify>
  <done>3 tests passing, archive_session_to_cold_storage method covered</done>
</task>

<task type="auto">
  <name>Task 2: Add remaining edge case tests for full coverage</name>
  <files>backend/tests/test_world_model.py</files>
  <action>
    Add 2-3 additional tests to close remaining coverage gaps:

    Add to existing test classes or create new TestWorldModelEdgeCases:

    1. test_record_formula_usage_success
       - Create service with mocked LanceDBHandler
       - Mock add_document to return True
       - Call record_formula_usage(
           agent_id="agent-1",
           agent_role="Finance",
           formula_id="formula-123",
           formula_name="NPV_Calculation",
           task_description="Calculate net present value",
           inputs={"rate": 0.05, "years": 10},
           result=50000,
           success=True
         )
       - Verify add_document called with:
         * text containing "Task: formula_application"
         * text containing formula name and task
         * metadata with formula_id, formula_name, formula_inputs JSON, formula_result
         * metadata with task_type="formula_application"

    2. test_bulk_record_facts_success
       - Mock record_business_fact to return True for all facts
       - Create list of 3 BusinessFact objects
       - Call bulk_record_facts(facts)
       - Verify returns 3 (all succeeded)

    3. test_bulk_record_facts_partial_failure
       - Mock record_business_fact to:
         * return True for fact 1
         * return False for fact 2
         * return True for fact 3
       - Create list of 3 BusinessFact objects
       - Call bulk_record_facts(facts)
       - Verify returns 2 (2 out of 3 succeeded)

    Target additional 2-3% coverage for edge cases
  </action>
  <verify>pytest backend/tests/test_world_model.py::TestWorldModelEdgeCases -v</verify>
  <done>3 additional tests passing, edge cases covered</done>
</task>

<task type="auto">
  <name>Task 3: Generate final coverage report and verify 60% target</name>
  <files>backend/tests/coverage_reports/metrics/phase_122_final_gap_closure_coverage.json</files>
  <action>
    Run full coverage with pytest:

    ```bash
    cd /Users/rushiparikh/projects/atom/backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_world_model.py -v --cov=core/agent_world_model --cov-report=json --cov-report=term --cov-report=html
    ```

    Extract coverage from coverage.json and save to phase_122_final_gap_closure_coverage.json:
    {
      "plan": "122-final-gap-closure",
      "file": "core/agent_world_model.py",
      "coverage_percent": XX.XX,
      "lines_covered": N,
      "lines_total": 332,
      "target_percent": 60.0,
      "target_met": true/false,
      "gaps_closed": [
        "Gap 1: recall_experiences integration tests",
        "Gap 2: Experience lifecycle methods",
        "Gap 3: Memory archival tests"
      ],
      "test_count": N,
      "test_classes": [
        "TestRecordExperience",
        "TestRecordBusinessFact",
        "TestGetRelevantBusinessFacts",
        "TestListAllFacts",
        "TestGetFactById",
        "TestUpdateFactVerification",
        "TestRecallExperiences",
        "TestUpdateExperienceFeedback",
        "TestBoostExperienceConfidence",
        "TestGetExperienceStatistics",
        "TestArchiveSessionToColdStorage",
        "TestWorldModelEdgeCases"
      ]
    }

    Target: 60%+ coverage (199+ lines covered out of 332)

    If coverage is 58-60%, accept as near-miss (within 2-3%)
  </action>
  <verify>
    - Coverage JSON created with all required fields
    - Coverage at 60%+ or near-miss (58-60%)
    - test_world_model.py >= 650 lines
  </verify>
  <done>Final coverage report complete, 60% target achieved</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. pytest tests/test_world_model.py -v
   - All tests passing
   - Test count: 5 baseline + 6 recall + 10 lifecycle + 3 archive + 3 edge = 27+ tests

2. Coverage verification:
   - agent_world_model.py coverage >= 60%
   - Or near-miss: 58-60% acceptable

3. File size check:
   - test_world_model.py >= 650 lines

4. Coverage JSON verification:
   - phase_122_final_gap_closure_coverage.json created
   - Contains all required fields
   - target_met: true

5. Gap closure checklist:
   - Gap 1 (recall_experiences): Tests created in Plan 04
   - Gap 2 (experience lifecycle): Tests created in Plan 05
   - Gap 3 (memory archival): Tests created in Plan 06
</verification>

<success_criteria>
1. All 3 gaps from VERIFICATION.md closed:
   - Gap 1: Integration tests for recall_experiences (84 statements)
   - Gap 2: Experience lifecycle tests (65 statements)
   - Gap 3: Memory archival tests (16 statements)

2. Coverage target achieved:
   - agent_world_model.py at 60%+ coverage
   - Near-miss acceptable (58-60%)

3. Test infrastructure solid:
   - 27+ tests across 12 test classes
   - Integration test pattern followed
   - Error paths covered

4. Success criteria from VERIFICATION.md now pass:
   - "Coverage report shows 60%+ coverage for core/agent_world_model.py"
   - "World model service multi-source memory aggregation validated"
</success_criteria>

<output>
After completion, create `.planning/phases/122-admin-routes-coverage/122-06-SUMMARY.md`
</output>
