---
phase: 122-admin-routes-coverage
plan: 02
type: execute
wave: 2
depends_on: [122-01]
files_modified:
  - backend/tests/coverage_reports/metrics/phase_122_business_facts_routes_analysis.md
  - backend/tests/coverage_reports/metrics/phase_122_world_model_analysis.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Coverage gap analysis documents created for both target files"
    - "Zero-coverage functions and methods identified"
    - "Partial coverage areas (<60%) categorized by priority"
    - "Test count estimated to reach 60% coverage target"
  artifacts:
    - path: "backend/tests/coverage_reports/metrics/phase_122_business_facts_routes_analysis.md"
      provides: "Line-by-line coverage gap analysis for business_facts_routes.py"
    - path: "backend/tests/coverage_reports/metrics/phase_122_world_model_analysis.md"
      provides: "Line-by-line coverage gap analysis for agent_world_model.py"
  key_links:
    - from: "phase_122_business_facts_routes_analysis.md"
      to: "backend/api/admin/business_facts_routes.py"
      via: "Coverage JSON missing_lines mapping"
    - from: "phase_122_world_model_analysis.md"
      to: "backend/core/agent_world_model.py"
      via: "Coverage JSON missing_lines mapping"
---

<objective>
Analyze coverage gaps to identify specific missing lines and zero-coverage functions.

Parse baseline coverage JSONs from Plan 01 to extract exact missing lines, categorize
gaps by priority (HIGH/MEDIUM/LOW), and estimate test count needed to reach 60% target.

Purpose: Create detailed gap analysis for targeted test addition in Plan 03
Output: Two analysis documents with prioritized test list
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/121-health-monitoring-coverage/121-02-PLAN.md

# Reference patterns from Phase 121 gap analysis
@backend/tests/coverage_reports/metrics/phase_121_health_analysis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse coverage JSONs and extract missing lines</name>
  <files>
  - backend/tests/coverage_reports/metrics/phase_122_business_facts_routes_analysis.md
  - backend/tests/coverage_reports/metrics/phase_122_world_model_analysis.md
  </files>
  <action>
Parse baseline coverage JSONs to extract exact missing lines:

1. **Read coverage JSONs**:
   ```python
   import json

   # Read business facts routes coverage
   with open('backend/tests/coverage_reports/metrics/phase_122_business_facts_routes_baseline.json') as f:
       bf_coverage = json.load(f)

   # Read world model coverage
   with open('backend/tests/coverage_reports/metrics/phase_122_world_model_baseline.json') as f:
       wm_coverage = json.load(f)
   ```

2. **Extract missing_lines for business_facts_routes.py**:
   ```python
   bf_files = bf_coverage.get('files', {})
   bf_target = None
   for filepath, data in bf_files.items():
       if 'business_facts_routes.py' in filepath:
           bf_target = data
           break

   bf_missing = bf_target.get('summary', {}).get('missing_lines', 0) if bf_target else 0
   bf_covered = bf_target.get('summary', {}).get('num_statements', 0) - bf_missing if bf_target else 0
   bf_percent = bf_target.get('summary', {}).get('percent_covered', 0) if bf_target else 0
   ```

3. **Extract missing_lines for agent_world_model.py**:
   ```python
   wm_files = wm_coverage.get('files', {})
   wm_target = None
   for filepath, data in wm_files.items():
       if 'agent_world_model.py' in filepath:
           wm_target = data
           break

   wm_missing = wm_target.get('summary', {}).get('missing_lines', 0) if wm_target else 0
   wm_covered = wm_target.get('summary', {}).get('num_statements', 0) - wm_missing if wm_target else 0
   wm_percent = wm_target.get('summary', {}).get('percent_covered', 0) if wm_target else 0
   ```

4. **Create analysis documents** in Tasks 2 and 3
  </action>
  <verify>
  Run: `python -c "import json; print(json.load(open('backend/tests/coverage_reports/metrics/phase_122_business_facts_routes_baseline.json')).get('files', {}))"`
  Expected: Valid JSON with coverage data for business_facts_routes.py
  </verify>
  <done>
  Coverage JSONs parsed successfully with missing_lines counts extracted
  </done>
</task>

<task type="auto">
  <name>Task 2: Create business_facts_routes.py gap analysis document</name>
  <files>backend/tests/coverage_reports/metrics/phase_122_business_facts_routes_analysis.md</files>
  <action>
Create detailed gap analysis for api/admin/business_facts_routes.py:

**Document Structure**:

```markdown
# Phase 122: Business Facts Routes Coverage Gap Analysis

**Target File**: `backend/api/admin/business_facts_routes.py`
**Baseline Coverage**: XX.X% (Plan 01)
**Target Coverage**: 60%+
**Lines to Add**: ~XX lines

## Coverage Summary

| Metric | Value |
|--------|-------|
| Total Lines | 407 |
| Covered Lines | XX |
| Missing Lines | XX |
| Coverage % | XX.X% |
| Target % | 60% |
| Gap | XX.X% |

## Zero-Coverage Routes

The following routes have 0% coverage from Plan 01 tests:

- [ ] `PUT /api/admin/governance/facts/{fact_id}` — Update existing fact (lines 163-208)
- [ ] `DELETE /api/admin/governance/facts/{fact_id}` — Soft delete fact (lines 211-227)
- [ ] `POST /api/admin/governance/facts/upload` — Upload and extract facts from document (lines 230-332)
- [ ] `POST /api/admin/governance/facts/{fact_id}/verify-citation` — Re-verify citations (lines 335-406)

## Partial Coverage Routes

The following routes have partial coverage:

- [x] `GET /api/admin/governance/facts` — List facts (covered: lines 65-94)
- [x] `GET /api/admin/governance/facts/{fact_id}` — Get single fact (covered: lines 97-121)
- [x] `POST /api/admin/governance/facts` — Create fact (covered: lines 124-160)

## HIGH Priority Gaps (Error Paths)

1. **Update Fact — Not Found** (lines 176-178)
   - Error: `router.not_found_error("Business fact", fact_id)`
   - Test: Update non-existent fact ID
   - Estimated impact: +2 lines

2. **Delete Fact — Not Found** (lines 224-225)
   - Error: `router.not_found_error("Business fact", fact_id)`
   - Test: Delete non-existent fact ID
   - Estimated impact: +2 lines

3. **Upload — Unsupported File Type** (lines 242-249)
   - Error: `router.validation_error()` for invalid extensions
   - Test: Upload .exe, .sh, or other invalid file type
   - Estimated impact: +8 lines

4. **Upload — Extraction Failure** (lines 322-324)
   - Error: `router.internal_error()` on extraction exception
   - Test: Mock extraction to raise exception
   - Estimated impact: +3 lines

5. **Verify Citation — Not Found** (lines 347-349)
   - Error: `router.not_found_error("Business fact", fact_id)`
   - Test: Verify citation for non-existent fact
   - Estimated impact: +3 lines

## MEDIUM Priority Gaps (Core Features)

1. **Update Fact — Full Flow** (lines 163-208)
   - Features: Verification status update, re-record fact
   - Tests needed:
     - Update verification status only
     - Update fact text/citations (re-record flow)
     - Update domain metadata
   - Estimated impact: +30 lines

2. **Delete Fact — Soft Delete** (lines 211-227)
   - Features: Soft delete via wm.delete_fact()
   - Tests needed:
     - Successful soft delete
     - Verify deleted facts filtered from list
   - Estimated impact: +15 lines

3. **Upload and Extract** (lines 230-332)
   - Features: File upload, R2 storage, fact extraction
   - Tests needed:
     - Successful PDF upload and extraction
     - Successful DOCX upload and extraction
     - R2 upload verification (mock storage service)
     - Bulk fact storage (wm.bulk_record_facts)
   - Estimated impact: +80 lines

## LOW Priority Gaps (Edge Cases)

1. **List Facts — Filtering** (lines 67-70, 82-94)
   - Features: status/domain filters, limit param
   - Tests needed:
     - Filter by verification status
     - Filter by domain
     - Apply limit
   - Estimated impact: +10 lines

2. **Verify Citation — S3 Check** (lines 360-377)
   - Features: S3 citation validation
   - Tests needed:
     - Valid S3 citation (exists=True)
     - Invalid S3 citation (exists=False)
     - Cross-bucket citation handling
   - Estimated impact: +20 lines

3. **Verify Citation — Local Check** (lines 379-386)
   - Features: Local file fallback
   - Tests needed:
     - Local file exists
     - Local file not found
   - Estimated impact: +8 lines

## Test Estimates

| Priority | Tests Needed | Line Impact | Cumulative % |
|----------|--------------|-------------|--------------|
| HIGH | 5 tests | +15 lines | ~25% |
| MEDIUM | 8 tests | +125 lines | ~55% |
| LOW | 6 tests | +38 lines | ~65% |

**Total Estimated Tests**: 19 tests (3 existing from Plan 01 + 19 new = 22 total)
**Projected Coverage**: ~65% (exceeds 60% target)

## Recommended Test Order for Plan 03

1. Update fact not found (HIGH)
2. Delete fact not found (HIGH)
3. Upload unsupported file type (HIGH)
4. Upload extraction failure (HIGH)
5. Verify citation not found (HIGH)
6. Update fact verification status (MEDIUM)
7. Update fact with re-record (MEDIUM)
8. Delete fact success (MEDIUM)
9. Upload PDF success (MEDIUM)
10. Verify citation S3 valid (MEDIUM)
11. List facts with filters (LOW)
12. Verify citation local fallback (LOW)

**Remaining tests** for full coverage can be added in future phases.
```
  </action>
  <verify>
  File exists at: backend/tests/coverage_reports/metrics/phase_122_business_facts_routes_analysis.md
  Contains: Coverage summary, zero-coverage routes, partial coverage routes, priority gaps, test estimates
  </verify>
  <done>
  Gap analysis document created with prioritized test list for business_facts_routes.py
  </done>
</task>

<task type="auto">
  <name>Task 3: Create agent_world_model.py gap analysis document</name>
  <files>backend/tests/coverage_reports/metrics/phase_122_world_model_analysis.md</files>
  <action>
Create detailed gap analysis for core/agent_world_model.py:

**Document Structure**:

```markdown
# Phase 122: Agent World Model Coverage Gap Analysis

**Target File**: `backend/core/agent_world_model.py`
**Baseline Coverage**: XX.X% (Plan 01)
**Target Coverage**: 60%+
**Lines to Add**: ~XX lines

## Coverage Summary

| Metric | Value |
|--------|-------|
| Total Lines | 815 |
| Covered Lines | XX |
| Missing Lines | XX |
| Coverage % | XX.X% |
| Target % | 60% |
| Gap | XX.X% |

## Zero-Coverage Methods

The following WorldModelService methods have 0% coverage from Plan 01 tests:

- [ ] `record_formula_usage()` — Record formula application as experience (lines 120-180)
- [ ] `update_experience_feedback()` — Update experience with human feedback (lines 182-239)
- [ ] `boost_experience_confidence()` — Boost confidence on successful reuse (lines 241-294)
- [ ] `get_experience_statistics()` — Get learning health statistics (lines 296-350)
- [ ] `update_fact_verification()` — Update fact verification status (lines 385-414)
- [ ] `archive_session_to_cold_storage()` — Archive chat session to LanceDB (lines 445-489)
- [ ] `recall_experiences()` — Multi-source memory aggregation (lines 491-720)
- [ ] `_extract_canvas_insights()` — Extract patterns from canvas context (lines 722-814)

## Partial Coverage Methods

The following methods have partial coverage:

- [x] `record_experience()` — Basic record flow covered (lines 79-118)
- [x] `record_business_fact()` — Basic record covered (lines 352-383)
- [x] `get_relevant_business_facts()` — Search covered (lines 416-442)

## HIGH Priority Gaps (Core Multi-Source Memory)

1. **recall_experiences() — Main Orchestration** (lines 491-720)
   - Features: Multi-source aggregation (experiences, knowledge, formulas, conversations, episodes)
   - Tests needed:
     - Experience retrieval with role filtering
     - Knowledge graph context retrieval
     - Formula search and hot fallback
     - Conversation retrieval
     - Episode retrieval with canvas/feedback enrichment
     - Canvas insights extraction
   - Estimated impact: +150 lines

2. **archive_session_to_cold_storage()** (lines 445-489)
   - Features: Postgres to LanceDB archival
   - Tests needed:
     - Successful session archival
     - Empty session handling
     - Database error handling
   - Estimated impact: +30 lines

3. **get_experience_statistics()** (lines 296-350)
   - Features: Learning health metrics
   - Tests needed:
     - Overall statistics (no filters)
     - Agent-specific statistics
     - Role-specific statistics
   - Estimated impact: +40 lines

## MEDIUM Priority Gaps (Experience Lifecycle)

1. **update_experience_feedback()** (lines 182-239)
   - Features: Human feedback integration
   - Tests needed:
     - Successful feedback update
     - Experience not found
     - Confidence recalculation (60% old + 40% new)
   - Estimated impact: +35 lines

2. **boost_experience_confidence()** (lines 241-294)
   - Features: Confidence boost on successful reuse
   - Tests needed:
     - Successful confidence boost
     - Cap at 1.0 (max confidence)
     - Boost count tracking
     - Experience not found
   - Estimated impact: +30 lines

3. **update_fact_verification()** (lines 385-414)
   - Features: Fact verification status update
   - Tests needed:
     - Successful status update
     - Fact not found
     - Status text replacement
   - Estimated impact: +20 lines

## LOW Priority Gaps (Specialized Features)

1. **record_formula_usage()** (lines 120-180)
   - Features: Formula application tracking
   - Tests needed:
     - Successful formula usage record
     - Metadata construction (formula_id, formula_name, inputs, result)
   - Estimated impact: +25 lines

2. **_extract_canvas_insights()** (lines 722-814)
   - Features: Canvas pattern extraction
   - Tests needed:
     - Canvas type counting
     - User action tracking
     - High-engagement canvas detection
     - Interaction pattern analysis
   - Estimated impact: +60 lines

## Test Estimates

| Priority | Tests Needed | Line Impact | Cumulative % |
|----------|--------------|-------------|--------------|
| HIGH | 12 tests | +220 lines | ~50% |
| MEDIUM | 10 tests | +85 lines | ~60% |
| LOW | 5 tests | +85 lines | ~70% |

**Total Estimated Tests**: 27 tests (3 existing from Plan 01 + 27 new = 30 total)
**Projected Coverage**: ~65% (exceeds 60% target)

## Recommended Test Order for Plan 03

1. recall_experiences basic orchestration (HIGH)
2. recall_experiences role filtering (HIGH)
3. recall_experiences formula search (HIGH)
4. archive_session_to_cold_storage success (HIGH)
5. get_experience_statistics overall (HIGH)
6. update_experience_feedback success (MEDIUM)
7. boost_experience_confidence success (MEDIUM)
8. update_fact_verification success (MEDIUM)
9. record_formula_usage success (LOW)
10. _extract_canvas_insights patterns (LOW)

**Note**: recall_experiences() is complex (230 lines). Split into multiple tests covering each data source.
```
  </action>
  <verify>
  File exists at: backend/tests/coverage_reports/metrics/phase_122_world_model_analysis.md
  Contains: Coverage summary, zero-coverage methods, partial coverage methods, priority gaps, test estimates
  </verify>
  <done>
  Gap analysis document created with prioritized test list for agent_world_model.py
  </done>
</task>

</tasks>

<verification>
Overall Phase 122-02 completion checks:
1. Both coverage JSONs parsed successfully
2. Business facts routes gap analysis created with HIGH/MEDIUM/LOW priorities
3. World model gap analysis created with HIGH/MEDIUM/LOW priorities
4. Test count estimated to reach 60% target
</verification>

<success_criteria>
- phase_122_business_facts_routes_analysis.md exists with prioritized gap list
- phase_122_world_model_analysis.md exists with prioritized gap list
- Test estimates show 60%+ coverage achievable with reasonable test count
- Zero-coverage functions/methods clearly identified
</success_criteria>

<output>
After completion, create `.planning/phases/122-admin-routes-coverage/122-02-SUMMARY.md` with:
1. Total gaps identified by priority (HIGH/MEDIUM/LOW)
2. Estimated tests needed to reach 60% target
3. Projected final coverage percentage
4. Prioritized test list for Plan 03 execution
</output>
