---
phase: 122-admin-routes-coverage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_world_model.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Integration tests cover recall_experiences() method (lines 622-827, 84 statements)"
    - "All 5 memory sources tested: facts, experiences, formulas, episodes, conversations"
    - "Multi-source aggregation logic validated with realistic test data"
  artifacts:
    - path: "backend/tests/test_world_model.py"
      provides: "Integration tests for multi-source memory aggregation"
      min_lines: 450
  key_links:
    - from: "backend/tests/test_world_model.py"
      to: "backend/core/agent_world_model.py"
      via: "recall_experiences method"
      pattern: "async def test_recall_experiences"
---

<objective>
**Gap Closure: Integration tests for recall_experiences() multi-source memory aggregation**

This plan closes Gap 1 from VERIFICATION.md: the critical `recall_experiences()` method (lines 622-827, 84 statements) has 0% coverage. This is the core JIT fact provision method that aggregates data from 5 sources: facts, experiences, formulas, episodes, and conversations.

Purpose: Achieve18% coverage increase by testing the 230-line multi-source aggregation method
Output: Integration tests for recall_experiences() with all 5 memory sources
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/122-admin-routes-coverage/122-VERIFICATION.md
@.planning/phases/122-admin-routes-coverage/122-03-SUMMARY.md

# Integration Test Pattern from Phase 121-04
Use real db_session fixture instead of mocking get_db()
Seed test database with realistic data
Integration tests achieve actual code path coverage
Accept near-miss if within 2-3% of target

# Reference: agent_world_model.py recall_experiences() method
@backend/core/agent_world_model.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add integration tests for recall_experiences() basic flow</name>
  <files>backend/tests/test_world_model.py</files>
  <action>
    Add test class TestRecallExperiences with 3-4 integration tests:

    1. test_recall_experiences_returns_empty_dict_when_no_data
       - Mock agent with category "Finance"
       - Mock all 5 search sources to return empty results
       - Verify returns dict with 7 keys (experiences, knowledge, knowledge_graph, formulas, conversations, business_facts, episodes)
       - All lists should be empty

    2. test_recall_experiences_aggregates_experiences_with_role_scoping
       - Create test agent with category "Finance"
       - Mock LanceDB search to return 3 experiences:
         * 1 with agent_role="Finance" (should be included)
         * 1 with agent_role matching agent.id (creator match)
         * 1 with agent_role="Engineering" (should be excluded)
       - Verify only role-matched and creator-matched experiences returned
       - Verify sorted by confidence_score descending

    3. test_recall_experiences_filters_low_confidence_failures
       - Mock agent with category "Sales"
       - Mock LanceDB search to return experiences:
         * outcome="failed", confidence=0.9 (should be included - high confidence)
         * outcome="failed", confidence=0.5 (should be excluded - low confidence)
         * outcome="Success", confidence=0.7 (should be included)
       - Verify low-confidence failures filtered out

    Import AgentRegistry from core.models for agent mock
    Use AsyncMock for LanceDBHandler.search method
  </action>
  <verify>pytest backend/tests/test_world_model.py::TestRecallExperiences -v</verify>
  <done>3 tests passing, recall_experiences() entry point covered</done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests for multi-source aggregation</name>
  <files>backend/tests/test_world_model.py</files>
  <action>
    Add 3-4 more tests to TestRecallExperiences class:

    4. test_recall_experiences_aggregates_all_5_memory_sources
       - Mock agent with category="General"
       - Mock LanceDB search for agent_experience table (returns 1 experience)
       - Mock LanceDB search for documents table (returns 1 knowledge result)
       - Mock LanceDB search for business_facts table (returns 1 fact)
       - Mock PostgreSQL ChatMessage query with get_db_session (returns 1 conversation)
       - Patch EpisodeRetrievalService.retrieve_contextual (returns 1 episode)
       - Verify all 5 sources appear in return dict
       - Verify structure of each source type

    5. test_recall_experiences_enriches_episodes_with_canvas_context
       - Mock agent
       - Patch EpisodeRetrievalService.retrieve_contextual to return episode with canvas_ids
       - Mock _fetch_canvas_context to return canvas data
       - Verify episodes in result have canvas_context key
       - Verify canvas_context structure

    6. test_recall_experiences_handles_missing_optional_dependencies
       - Mock agent
       - Mock graphrag_engine.get_context_for_ai to raise Exception
       - Mock get_formula_manager to raise Exception
       - Mock EpisodeRetrievalService to raise Exception
       - Verify method continues and returns partial results
       - Verify knowledge_graph is empty string on error
       - Verify formulas is empty list on error
       - Verify episodes is empty list on error

    Use patch decorator for dependency mocking:
    @patch('core.agent_world_model.graphrag_engine')
    @patch('core.agent_world_model.get_formula_manager')
    @patch('core.agent_world_model.EpisodeRetrievalService')
  </action>
  <verify>pytest backend/tests/test_world_model.py::TestRecallExperiences -v</verify>
  <done>6 total tests in TestRecallExperiences, all memory sources covered</done>
</task>

<task type="auto">
  <name>Task 3: Generate coverage measurement for recall_experiences()</name>
  <files>backend/tests/coverage_reports/metrics/phase_122_plan04_coverage.json</files>
  <action>
    Run coverage with pytest:

    ```bash
    cd /Users/rushiparikh/projects/atom/backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_world_model.py::TestRecallExperiences --cov=core/agent_world_model --cov-report=json --cov-report=term -v
    ```

    Extract agent_world_model.py coverage from coverage.json
    Save to phase_122_plan04_coverage.json with structure:
    {
      "plan": "122-04",
      "file": "core/agent_world_model.py",
      "coverage_percent": XX.XX,
      "lines_covered": N,
      "lines_total": 332,
      "focus": "recall_experiences integration tests"
    }

    Target: 28.92% + 18% = 47%+ coverage (recall_experiences has 84 statements)
  </action>
  <verify>
    - Coverage JSON file created
    - Coverage shows increase for agent_world_model.py
    - New test file line count >= 450 lines
  </verify>
  <done>Coverage measurement complete, recall_experiences() method tested</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. pytest tests/test_world_model.py::TestRecallExperiences -v
   - All 6 tests passing

2. Check test_world_model.py line count
   - Should be >= 450 lines (313 baseline + ~137 new)

3. Verify coverage JSON created
   - File exists at backend/tests/coverage_reports/metrics/phase_122_plan04_coverage.json
   - Shows increased coverage percentage for agent_world_model.py
</verification>

<success_criteria>
1. TestRecallExperiences class created with 6 tests covering:
   - Empty result handling
   - Role-based scoping
   - Confidence filtering
   - All 5 memory sources
   - Episode enrichment with canvas/feedback
   - Graceful error handling for missing dependencies

2. Coverage increase of 15-18% for agent_world_model.py
   - Baseline: 28.92%
   - Target: 44-47% after this plan

3. Integration test pattern followed from Phase 121-04
   - Real dependency patching, not just mocks
   - Error path coverage
   - Realistic test data
</success_criteria>

<output>
After completion, create `.planning/phases/122-admin-routes-coverage/122-04-SUMMARY.md`
</output>
