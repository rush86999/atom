---
phase: 122-admin-routes-coverage
plan: 05
type: execute
wave: 1
depends_on:
  - 122-04
files_modified:
  - backend/tests/test_world_model.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Experience lifecycle methods tested: update_experience_feedback, boost_experience_confidence, get_experience_statistics"
    - "Feedback update blends confidence scores correctly (60% old + 40% feedback)"
    - "Confidence boost caps at 1.0 and tracks boost count"
    - "Statistics aggregation filters by agent_id and agent_role"
  artifacts:
    - path: "backend/tests/test_world_model.py"
      provides: "Experience lifecycle tests"
      min_lines: 550
  key_links:
    - from: "backend/tests/test_world_model.py"
      to: "backend/core/agent_world_model.py"
      via: "update_experience_feedback, boost_experience_confidence, get_experience_statistics"
      pattern: "class TestExperienceLifecycle"
---

<objective>
**Gap Closure: Experience lifecycle methods (update_experience_feedback, boost_experience_confidence, get_experience_statistics)**

This plan closes Gap 2 from VERIFICATION.md: three critical agent learning methods have 0% coverage. These methods are essential for the graduation framework and agent learning system.

Purpose: Achieve 11% coverage increase by testing experience lifecycle methods (65 statements total)
Output: Tests for feedback updates, confidence boosting, and statistics aggregation
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/122-admin-routes-coverage/122-VERIFICATION.md
@.planning/phases/122-admin-routes-coverage/122-04-SUMMARY.md

# Reference: agent_world_model.py experience lifecycle methods
@backend/core/agent_world_model.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tests for update_experience_feedback()</name>
  <files>backend/tests/test_world_model.py</files>
  <action>
    Add test class TestUpdateExperienceFeedback with 2-3 tests:

    1. test_update_experience_feedback_blends_confidence_scores
       - Create service with mocked LanceDBHandler
       - Mock search to return experience with:
         * id="exp-1"
         * confidence_score=0.5
         * metadata with agent_id, task_type, outcome, etc.
       - Call update_experience_feedback(exp_id="exp-1", feedback_score=0.8, feedback_notes="Great work")
       - Verify add_document called with new_confidence = 0.5 * 0.6 + (0.8 + 1.0) / 2.0 * 0.4 = 0.3 + 0.36 = 0.66
       - Verify metadata includes feedback_score=0.8, feedback_notes, feedback_at
       - Verify text includes feedback notes
       - Return True

    2. test_update_experience_feedback_returns_false_when_not_found
       - Mock search to return results without matching experience_id
       - Call update_experience_feedback(exp_id="nonexistent", feedback_score=0.5)
       - Verify returns False
       - Verify logger.warning called

    3. test_update_experience_feedback_handles_negative_feedback
       - Mock search to return experience with confidence_score=0.7
       - Call update_experience_feedback with feedback_score=-1.0 (worst feedback)
       - Verify new confidence decreased: 0.7 * 0.6 + (0.0) * 0.4 = 0.42
       - Verify negative feedback properly recorded

    Confidence blend formula from lines 210-213:
    new_confidence = old_confidence * 0.6 + (feedback_score + 1.0) / 2.0 * 0.4
  </action>
  <verify>pytest backend/tests/test_world_model.py::TestUpdateExperienceFeedback -v</verify>
  <done>3 tests passing, update_experience_feedback method covered</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for boost_experience_confidence()</name>
  <files>backend/tests/test_world_model.py</files>
  <action>
    Add test class TestBoostExperienceConfidence with 2-3 tests:

    1. test_boost_experience_confidence_increases_score
       - Create service with mocked LanceDBHandler
       - Mock search to return experience with:
         * id="exp-2"
         * confidence_score=0.6
         * boost_count=0
       - Call boost_experience_confidence(exp_id="exp-2", boost_amount=0.1)
       - Verify add_document called with new_confidence = min(1.0, 0.6 + 0.1) = 0.7
       - Verify boost_count incremented to 1
       - Verify last_boosted_at timestamp added
       - Verify returns True

    2. test_boost_experience_confidence_caps_at_one
       - Mock search to return experience with confidence_score=0.95
       - Call boost_experience_confidence(exp_id="exp-3", boost_amount=0.2)
       - Verify new_confidence = 1.0 (capped)
       - Verify boost_count still incremented

    3. test_boost_experience_confidence_returns_false_when_not_found
       - Mock search to return results without matching experience_id
       - Call boost_experience_confidence(exp_id="nonexistent")
       - Verify returns False
       - Verify logger.warning called

    From lines 241-294 of agent_world_model.py
  </action>
  <verify>pytest backend/tests/test_world_model.py::TestBoostExperienceConfidence -v</verify>
  <done>3 tests passing, boost_experience_confidence method covered</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for get_experience_statistics()</name>
  <files>backend/tests/test_world_model.py</files>
  <action>
    Add test class TestGetExperienceStatistics with 2-3 tests:

    1. test_get_experience_statistics_aggregates_all_experiences
       - Create service with mocked LanceDBHandler
       - Mock search to return 5 experiences:
         * 3 with outcome="Success"
         * 2 with outcome="Failure"
         * Varying confidence scores: 0.8, 0.7, 0.9, 0.5, 0.6 (avg = 0.7)
         * 2 with feedback_score set
       - Call get_experience_statistics()
       - Verify returns:
         * total_experiences=5
         * successes=3
         * failures=2
         * success_rate=0.6 (3/5)
         * avg_confidence~=0.7
         * feedback_coverage=0.4 (2/5)
         * agent_id=None
         * agent_role=None

    2. test_get_experience_statistics_filters_by_agent_id
       - Mock search to return 10 experiences with different agent_ids
       - Call get_experience_statistics(agent_id="agent-123")
       - Verify only experiences with agent_id="agent-123" counted
       - Verify agent_id in returned dict matches filter

    3. test_get_experience_statistics_filters_by_agent_role
       - Mock search to return experiences with agent_role:
         * 3 "Finance"
         * 2 "Engineering"
         * 1 "finance" (different case - should still match)
       - Call get_experience_statistics(agent_role="finance")
       - Verify case-insensitive matching (both "Finance" and "finance" included)
       - Verify statistics only include matching role

    4. test_get_experience_statistics_handles_search_error
       - Mock search to raise Exception
       - Call get_experience_statistics()
       - Verify returns dict with "error" key
       - Verify logger.error called

    From lines 296-350 of agent_world_model.py
  </action>
  <verify>pytest backend/tests/test_world_model.py::TestGetExperienceStatistics -v</verify>
  <done>4 tests passing, get_experience_statistics method covered</done>
</task>

<task type="auto">
  <name>Task 4: Generate coverage measurement for experience lifecycle</name>
  <files>backend/tests/coverage_reports/metrics/phase_122_plan05_coverage.json</files>
  <action>
    Run coverage with pytest:

    ```bash
    cd /Users/rushiparikh/projects/atom/backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_world_model.py -v --cov=core/agent_world_model --cov-report=json --cov-report=term
    ```

    Extract agent_world_model.py coverage from coverage.json
    Save to phase_122_plan05_coverage.json with structure:
    {
      "plan": "122-05",
      "file": "core/agent_world_model.py",
      "coverage_percent": XX.XX,
      "lines_covered": N,
      "lines_total": 332,
      "focus": "experience lifecycle tests"
    }

    Target: 47% + 11% = 58%+ coverage (65 statements in 3 methods)
  </action>
  <verify>
    - Coverage JSON file created
    - Coverage shows 55-60% for agent_world_model.py
    - Test file line count >= 550 lines
  </verify>
  <done>Coverage measurement complete, near 60% target</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. pytest tests/test_world_model.py::TestUpdateExperienceFeedback -v
   - 3 tests passing

2. pytest tests/test_world_model.py::TestBoostExperienceConfidence -v
   - 3 tests passing

3. pytest tests/test_world_model.py::TestGetExperienceStatistics -v
   - 4 tests passing

4. Full test run:
   pytest tests/test_world_model.py -v
   - All new tests passing

5. Check coverage
   - agent_world_model.py at 55-60% coverage
</verification>

<success_criteria>
1. 10 new tests added for experience lifecycle:
   - 3 for update_experience_feedback
   - 3 for boost_experience_confidence
   - 4 for get_experience_statistics

2. Coverage at 55-60% for agent_world_model.py
   - Baseline: 28.92%
   - After Plan 04: ~47%
   - After Plan 05: ~58%

3. All experience lifecycle methods have test coverage:
   - Feedback updates with confidence blending
   - Confidence boosting with cap at 1.0
   - Statistics with filtering by agent/role
</success_criteria>

<output>
After completion, create `.planning/phases/122-admin-routes-coverage/122-05-SUMMARY.md`
</output>
