---
phase: 077-agent-chat-streaming
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/e2e_ui/pages/page_objects.py
autonomous: true

must_haves:
  truths:
    - "User can access chat interface page and see chat input"
    - "Chat input field accepts user text messages"
    - "Send button submits chat messages"
    - "Chat history displays previous messages"
  artifacts:
    - path: "backend/tests/e2e_ui/pages/page_objects.py"
      provides: "ChatPage Page Object with chat input, send button, message history locators"
      min_lines: 100
      contains: "class ChatPage"
  key_links:
    - from: "ChatPage.is_loaded()"
      to: "chat interface container"
      via: "data-testid selector"
      pattern: "chat.*container|chat.*input"
---

<objective>
Create ChatPage Page Object for agent chat interface testing.

Purpose: Provide UI abstraction for chat interaction tests (send messages, view history, wait for responses)
Output: ChatPage class with locators for chat input, send button, message list, streaming indicators
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/075-test-infrastructure-fixtures/75-07-SUMMARY.md
@.planning/phases/076-authentication-user-management/076-05-SUMMARY.md
@backend/core/atom_agent_endpoints.py
@backend/tests/e2e_ui/pages/page_objects.py

# Agent Chat Context
- Chat endpoint: POST /api/atom-agent/chat/stream
- WebSocket streaming via core.websockets.ConnectionManager
- Chat message types: user, assistant
- Message metadata: timestamp, agent_id, agent_name, streaming status
</context>

<tasks>

<task type="auto">
  <name>Create ChatPage Page Object class</name>
  <files>backend/tests/e2e_ui/pages/page_objects.py</files>
  <action>
    Add ChatPage class to page_objects.py with:

    1. Locators (data-testid selectors):
       - chat_container - Main chat interface container
       - chat_input - Message input field
       - send_button - Message send button
       - message_list - Chat history container
       - user_message - User message bubbles
       - assistant_message - Agent response bubbles
       - streaming_indicator - Loading/streaming state indicator
       - agent_selector - Dropdown to select agent maturity level

    2. Methods:
       - is_loaded() -> bool - Check if chat interface is visible
       - navigate() -> None - Navigate to chat page
       - send_message(text: str) -> None - Type and send message
       - get_last_message() -> str - Get most recent message text
       - get_message_count() -> int - Count messages in history
       - wait_for_response(timeout: int = 5000) -> None - Wait for assistant response
       - is_streaming() -> bool - Check if streaming in progress
       - select_agent(agent_name: str) -> None - Select agent from dropdown

    Follow BasePage pattern from existing page_objects.py
    Use data-testid selectors for resilience
    Include docstrings with Google-style Args/Returns
  </action>
  <verify>
    # Check file has ChatPage class with required methods
    grep -c "class ChatPage" backend/tests/e2e_ui/pages/page_objects.py
    grep -c "chat_input\|send_button\|message_list" backend/tests/e2e_ui/pages/page_objects.py
  </verify>
  <done>
    ChatPage class exists with 100+ lines including all required locators and methods
  </done>
</task>

</tasks>

<verification>
1. ChatPage class added to page_objects.py
2. All locators use data-testid selectors
3. All methods defined with type hints
4. Page follows BasePage pattern
</verification>

<success_criteria>
- ChatPage class with 100+ lines
- 8+ locators for chat interface elements
- 8+ methods for chat interaction
- Tests can import and instantiate ChatPage
</success_criteria>

<output>
After completion, create .planning/phases/077-agent-chat-streaming/077-01-SUMMARY.md
</output>
