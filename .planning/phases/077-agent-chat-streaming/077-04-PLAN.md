---
phase: 077-agent-chat-streaming
plan: 04
type: execute
wave: 1
depends_on: [01]
files_modified:
  - backend/tests/e2e_ui/tests/test_agent_streaming.py
autonomous: true

must_haves:
  truths:
    - "Agent response appears token-by-token during streaming"
    - "Streaming completion event marks final response"
    - "Full response displays correctly after streaming"
    - "Streaming indicator shows during generation"
  artifacts:
    - path: "backend/tests/e2e_ui/tests/test_agent_streaming.py"
      provides: "Streaming token-by-token display tests (4 test functions)"
      min_lines: 250
      contains: "test_token_streaming|test_streaming_complete"
  key_links:
    - from: "test_token_streaming"
      to: "streaming:update WebSocket event"
      via: "ConnectionManager.broadcast()"
      pattern: "STREAMING_UPDATE|streaming:update"
    - from: "test_token_streaming"
      to: "ChatPage.wait_for_response()"
      via: "Polling for response element"
      pattern: "wait_for_response|assistant_message"
---

<objective>
Create E2E tests for token-by-token streaming responses (AGENT-02).

Purpose: Validate agent responses stream token-by-token via WebSocket and display correctly
Output: 4 test functions covering streaming display, token accumulation, completion, streaming indicator
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/075-test-infrastructure-fixtures/75-07-SUMMARY.md
@.planning/phases/076-authentication-user-management/076-01-SUMMARY.md
@.planning/phases/077-agent-chat-streaming/077-01-SUMMARY.md
@backend/core/atom_agent_endpoints.py (chat_stream_agent function)
@backend/core/websockets.py
@backend/tests/e2e_ui/pages/page_objects.py

# Streaming Context
- Endpoint: POST /api/atom-agent/chat/stream
- WebSocket events: streaming:start, streaming:update, streaming:complete, streaming:error
- Message format: {type, id, delta, complete, metadata}
- Delta contains individual tokens
- Complete=true signals end of stream
- Streaming shows loading indicator during generation
</context>

<tasks>

<task type="auto">
  <name>Create streaming response tests</name>
  <files>backend/tests/e2e_ui/tests/test_agent_streaming.py</files>
  <action>
    Create test_agent_streaming.py with 4 test functions:

    1. test_token_streaming_displays_progressively(browser, authenticated_page)
       - Navigate to chat page
       - Send chat message
       - Capture WebSocket messages
       - Verify streaming:start event received first
       - Verify multiple streaming:update events (deltas)
       - Verify response text grows incrementally
       - Verify streaming:complete event received

    2. test_full_response_shows_after_streaming(browser, authenticated_page)
       - Send message and wait for complete event
       - Verify final response contains all accumulated tokens
       - Verify response is not truncated
       - Verify assistant message styling applied

    3. test_streaming_indicator_visible_during_generation(browser, authenticated_page)
       - Send message
       - Verify streaming indicator appears immediately
       - Verify indicator disappears after complete event
       - Verify indicator has correct aria-live for accessibility

    4. test_streaming_error_handling(browser, authenticated_page)
       - Mock LLM failure scenario
       - Send message
       - Verify streaming:error event received
       - Verify error message displayed to user
       - Verify chat interface recovers from error

    Use ChatPage.is_streaming() method
    Use Playwright's wait_for_selector() for async response
    Use UUID v4 for unique messages (avoid caching)
    Set appropriate timeouts (up to 10s for LLM response)
    Include docstrings and type hints
  </action>
  <verify>
    # Check test file exists
    grep -c "def test_" backend/tests/e2e_ui/tests/test_agent_streaming.py
    # Verify streaming tests
    grep -i "streaming" backend/tests/e2e_ui/tests/test_agent_streaming.py
  </verify>
  <done>
    4 test functions covering streaming display
  </done>
</task>

</tasks>

<verification>
1. test_agent_streaming.py file created
2. 4 test functions implemented
3. Tests validate WebSocket streaming events
4. Covers AGENT-02 requirement
</verification>

<success_criteria>
- 4 test functions (250+ lines)
- Token-by-token streaming validated
- Completion event tested
- Streaming indicator tested
- Error handling tested
</success_criteria>

<output>
After completion, create .planning/phases/077-agent-chat-streaming/077-04-SUMMARY.md
</output>
