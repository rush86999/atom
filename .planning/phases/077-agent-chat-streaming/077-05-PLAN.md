---
phase: 077-agent-chat-streaming
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - backend/tests/e2e_ui/tests/test_agent_governance.py
  - backend/tests/e2e_ui/fixtures/api_fixtures.py
autonomous: true

must_haves:
  truths:
    - "STUDENT agent blocked from restricted actions (error shown)"
    - "INTERN agent shows approval dialog for actions"
    - "Approval dialog allows approve/reject actions"
    - "Agent governance checks use correct maturity level"
  artifacts:
    - path: "backend/tests/e2e_ui/tests/test_agent_governance.py"
      provides: "Agent governance enforcement tests (5 test functions)"
      min_lines: 300
      contains: "test_student_blocked|test_intern_approval"
    - path: "backend/tests/e2e_ui/fixtures/api_fixtures.py"
      provides: "Agent creation fixtures for maturity levels"
      min_lines: 50
      contains: "setup_test_agent|STUDENT|INTERN"
  key_links:
    - from: "test_student_blocked"
      to: "AgentGovernanceService.can_perform_action()"
      via: "Governance check before action"
      pattern: "can_perform_action|governance_check"
    - from: "test_intern_approval"
      to: "ProposalService.create_action_proposal()"
      via: "Proposal workflow for INTERN agents"
      pattern: "create_action_proposal|approval"
---

<objective>
Create E2E tests for agent governance enforcement (AGENT-04, AGENT-05).

Purpose: Validate STUDENT agents are blocked and INTERN agents require approval before actions
Output: 5 test functions covering STUDENT blocking, INTERN approval, SUPERVISED auto-execution, approval dialog
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/075-test-infrastructure-fixtures/75-07-SUMMARY.md
@.planning/phases/076-authentication-user-management/076-01-SUMMARY.md
@.planning/phases/077-agent-chat-streaming/077-01-SUMMARY.md
@backend/core/agent_governance_service.py
@backend/core/proposal_service.py
@backend/core/models.py (AgentStatus enum)
@backend/tests/e2e_ui/pages/page_objects.py

# Governance Context
- Maturity levels: STUDENT (0.5), INTERN (0.7), SUPERVISED (0.9), AUTONOMOUS (>0.9)
- Action complexity: 1 (LOW), 2 (MODERATE), 3 (HIGH), 4 (CRITICAL)
- STUDENT blocked from complexity 2+ actions
- INTERN requires approval for complexity 3+ actions
- SUPERVISED auto-executes with monitoring
- Proposal workflow: create -> submit_for_approval -> approve/reject -> execute
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add agent creation fixtures</name>
  <files>backend/tests/e2e_ui/fixtures/api_fixtures.py</files>
  <action>
    Add to api_fixtures.py:

    1. @pytest.fixture function scope: test_agent_data()
       Returns dict with agent_id, name, status (STUDENT/INTERN/SUPERVISED/AUTONOMOUS)

    2. @pytest.fixture function scope: setup_test_agent(authenticated_api_client, test_agent_data)
       Creates agent via API (POST /api/agent-governance/register)
       Returns dict with agent data

    3. Helper function: create_test_agent(client, name, status)
       Creates agent with specified maturity level
       Returns agent_id and confirmation

    Follow existing fixture pattern from api_fixtures.py
    Use AgentStatus enum from models.py
    Include UUID v4 for unique agent names
  </action>
  <verify>
    grep -c "setup_test_agent\|test_agent_data" backend/tests/e2e_ui/fixtures/api_fixtures.py
  </verify>
  <done>
    Agent creation fixtures added to api_fixtures.py
  </done>
</task>

<task type="auto">
  <name>Task 2: Create governance enforcement tests</name>
  <files>backend/tests/e2e_ui/tests/test_agent_governance.py</files>
  <action>
    Create test_agent_governance.py with 5 test functions:

    1. test_student_agent_blocked_from_restricted_actions(browser, db_session, setup_test_agent)
       - Create STUDENT agent via API
       - Select STUDENT agent in chat interface
       - Send message requiring action (e.g., "delete project")
       - Verify governance error message displayed
       - Verify action not executed
       - Verify error message contains "not permitted" or similar

    2. test_intern_agent_shows_approval_dialog(browser, db_session, setup_test_agent)
       - Create INTERN agent via API
       - Select INTERN agent
       - Send message requiring action
       - Verify approval dialog appears
       - Verify dialog shows proposed action
       - Verify approve/reject buttons present

    3. test_intern_approval_execute_on_approve(browser, db_session, setup_test_agent)
       - Create INTERN agent
       - Trigger action proposal
       - Click approve button
       - Verify action executes
       - Verify success message shown

    4. test_intern_reject_blocks_action(browser, db_session, setup_test_agent)
       - Create INTERN agent
       - Trigger action proposal
       - Click reject button
       - Verify action does NOT execute
       - Verify rejection message shown

    5. test_supervised_agent_auto_executes(browser, db_session, setup_test_agent)
       - Create SUPERVISED agent
       - Send message requiring action
       - Verify no approval dialog (auto-executes)
       - Verify action completes
       - Verify execution logged

    Use setup_test_agent fixture from Task 1
    Use ChatPage Page Object for UI interaction
    Use API-first setup for agent creation
    Use UUID v4 for unique agent names
    Include docstrings and type hints
  </action>
  <verify>
    grep -c "def test_" backend/tests/e2e_ui/tests/test_agent_governance.py
    grep "STUDENT\|INTERN\|SUPERVISED" backend/tests/e2e_ui/tests/test_agent_governance.py
  </verify>
  <done>
    5 test functions covering governance enforcement
  </done>
</task>

</tasks>

<verification>
1. api_fixtures.py has agent creation fixtures
2. test_agent_governance.py file created
3. 5 test functions implemented
4. Tests cover AGENT-04 and AGENT-05 requirements
5. Uses API-first agent creation
</verification>

<success_criteria>
- 2 agent creation fixtures added
- 5 test functions (300+ lines)
- STUDENT blocking tested
- INTERN approval workflow tested
- SUPERVISED auto-execution tested
</success_criteria>

<output>
After completion, create .planning/phases/077-agent-chat-streaming/077-05-SUMMARY.md
</output>
