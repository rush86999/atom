---
phase: 077-agent-chat-streaming
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - backend/tests/e2e_ui/tests/test_agent_chat.py
autonomous: true

must_haves:
  truths:
    - "User can type message in chat input and click send"
    - "Sent message appears in chat history"
    - "Message persists after sending"
    - "Multiple messages can be sent in sequence"
  artifacts:
    - path: "backend/tests/e2e_ui/tests/test_agent_chat.py"
      provides: "Chat message sending tests (5 test functions)"
      min_lines: 200
      contains: "test_send_chat_message|test_message_in_history"
  key_links:
    - from: "test_send_chat_message"
      to: "ChatPage.send_message()"
      via: "Page Object method call"
      pattern: "chat_page.send_message"
    - from: "test_send_chat_message"
      to: "/api/atom-agent/chat"
      via: "HTTP POST request"
      pattern: "chat/stream|atom-agent"
---

<objective>
Create E2E tests for sending chat messages to agent (AGENT-01).

Purpose: Validate user can send messages through chat interface and see them in history
Output: 5 test functions covering message sending, history display, empty input, multiple messages, message persistence
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/075-test-infrastructure-fixtures/75-07-SUMMARY.md
@.planning/phases/076-authentication-user-management/076-01-SUMMARY.md
@.planning/phases/077-agent-chat-streaming/077-01-SUMMARY.md
@backend/core/atom_agent_endpoints.py
@backend/tests/e2e_ui/pages/page_objects.py

# Agent Chat API Context
- Chat endpoint: POST /api/atom-agent/chat
- Streaming endpoint: POST /api/atom-agent/chat/stream
- Request model: ChatRequest (message, user_id, session_id, agent_id)
- Response includes: success, message_id, session_id, agent_id
</context>

<tasks>

<task type="auto">
  <name>Create chat message sending tests</name>
  <files>backend/tests/e2e_ui/tests/test_agent_chat.py</files>
  <action>
    Create test_agent_chat.py with 5 test functions:

    1. test_send_chat_message(browser, db_session, authenticated_page)
       - Create test user via API
       - Navigate to chat page using ChatPage
       - Type message "Hello agent" in chat input
       - Click send button
       - Verify message appears in chat history
       - Verify message has user role styling

    2. test_message_appears_in_history(browser, db_session)
       - Create test user
       - Send multiple messages (3 messages)
       - Verify all messages appear in history
       - Verify message count matches

    3. test_empty_message_not_sent(browser, db_session)
       - Navigate to chat page
       - Click send without typing
       - Verify send button disabled or no message sent
       - Verify message count unchanged

    4. test_long_message_truncates_or_scrolls(browser, db_session)
       - Send long message (500+ chars)
       - Verify message displays properly
       - Verify input field clears after send

    5. test_message_persistence_after_refresh(browser, db_session)
       - Send message
       - Refresh page
       - Verify message still in history
       - Verify session_id persists

    Use authenticated_page fixture from Phase 76
    Use ChatPage Page Object from 077-01
    Use UUID v4 for unique message content
    Include docstrings and type hints
  </action>
  <verify>
    # Check test file exists with required tests
    grep -c "def test_" backend/tests/e2e_ui/tests/test_agent_chat.py
    # Verify uses ChatPage
    grep "ChatPage" backend/tests/e2e_ui/tests/test_agent_chat.py
  </verify>
  <done>
    5 test functions created covering all message sending scenarios
  </done>
</task>

</tasks>

<verification>
1. test_agent_chat.py file created
2. 5 test functions implemented
3. Uses ChatPage Page Object
4. Uses authenticated_page fixture
5. Tests use UUID v4 for unique data
</verification>

<success_criteria>
- 5 test functions (200+ lines)
- All tests use ChatPage methods
- Covers AGENT-01 requirement fully
</success_criteria>

<output>
After completion, create .planning/phases/077-agent-chat-streaming/077-02-SUMMARY.md
</output>
