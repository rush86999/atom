---
phase: 077-agent-chat-streaming
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - backend/tests/e2e_ui/tests/test_agent_websocket.py
autonomous: true

must_haves:
  truths:
    - "WebSocket connection established when chat page loads"
    - "WebSocket receives streaming update events"
    - "WebSocket connection closes when page unloads"
    - "WebSocket reconnection works on connection drop"
  artifacts:
    - path: "backend/tests/e2e_ui/tests/test_agent_websocket.py"
      provides: "WebSocket connection lifecycle tests (4 test functions)"
      min_lines: 250
      contains: "test_websocket_connect|test_streaming_update"
  key_links:
    - from: "test_websocket_connection"
      to: "ws://localhost:8001/ws"
      via: "WebSocket protocol"
      pattern: "websocket|ws://"
    - from: "test_websocket_connection"
      to: "ConnectionManager.connect()"
      via: "core.websockets module"
      pattern: "websockets\.py|ConnectionManager"
---

<objective>
Create E2E tests for WebSocket connection lifecycle (AGENT-03).

Purpose: Validate WebSocket connection for agent chat streaming works correctly
Output: 4 test functions covering connection, message receipt, disconnection, reconnection
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/075-test-infrastructure-fixtures/75-07-SUMMARY.md
@.planning/phases/076-authentication-user-management/076-01-SUMMARY.md
@.planning/phases/077-agent-chat-streaming/077-01-SUMMARY.md
@backend/core/websockets.py
@backend/api/websocket_routes.py
@backend/tests/e2e_ui/pages/page_objects.py

# WebSocket Context
- WebSocket endpoint: ws://localhost:8001/ws/{workspace_id}
- Connection manager: core.websockets.ConnectionManager
- Message types: streaming:update, streaming:complete, streaming:error
- User channel format: user:{user_id}
- Authentication via JWT token in WebSocket URL or headers
</context>

<tasks>

<task type="auto">
  <name>Create WebSocket connection lifecycle tests</name>
  <files>backend/tests/e2e_ui/tests/test_agent_websocket.py</files>
  <action>
    Create test_agent_websocket.py with 4 test functions:

    1. test_websocket_connection_established(browser, authenticated_page)
       - Navigate to chat page
       - Intercept WebSocket connections
       - Verify WebSocket URL is correct
       - Verify connection succeeds (status 101)
       - Verify authentication token sent

    2. test_websocket_receives_streaming_events(browser, authenticated_page)
       - Connect to chat page
       - Listen for WebSocket messages
       - Send chat message
       - Verify streaming:start event received
       - Verify streaming:update events received (token streaming)
       - Verify streaming:complete event received

    3. test_websocket_disconnects_on_navigation(browser, authenticated_page)
       - Establish WebSocket connection
       - Navigate away from chat page
       - Verify WebSocket closes
       - Verify no new messages received

    4. test_websocket_reconnects_after_disconnect(browser, authenticated_page)
       - Establish connection
       - Simulate connection drop
       - Verify reconnection attempt
       - Verify connection restored

    Use Playwright's web_socket_listener for WebSocket interception
    Use authenticated_page fixture for authentication
    Include timeout handling for async WebSocket events
    Use UUID v4 for unique message content
    Add docstrings with Args/Returns
  </action>
  <verify>
    # Check test file exists
    grep -c "def test_" backend/tests/e2e_ui/tests/test_agent_websocket.py
    # Verify WebSocket testing
    grep -i "websocket" backend/tests/e2e_ui/tests/test_agent_websocket.py
  </verify>
  <done>
    4 test functions covering WebSocket connection lifecycle
  </done>
</task>

</tasks>

<verification>
1. test_agent_websocket.py file created
2. 4 test functions implemented
3. Uses Playwright WebSocket interception
4. Tests cover AGENT-03 requirement
</verification>

<success_criteria>
- 4 test functions (250+ lines)
- WebSocket connection validated
- Streaming events tested
- Reconnection tested
</success_criteria>

<output>
After completion, create .planning/phases/077-agent-chat-streaming/077-03-SUMMARY.md
</output>
