---
phase: 155-quick-wins-leaf-components-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/utils/test_formatters.py
  - backend/tests/unit/utils/test_validators.py
  - backend/core/agent_utils.py
  - backend/core/email_utils.py
  - backend/core/decimal_utils.py
  - menubar/src-tauri/src/helpers.rs
  - menubar/src-tauri/src/platform_specific.rs
  - menubar/src-tauri/tests/helpers_test.rs
  - menubar/src-tauri/tests/platform_specific_test.rs
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Backend utility functions achieve 80%+ coverage with pure function tests"
    - "Desktop helper functions achieve 80%+ coverage with Rust unit tests"
    - "Platform-specific utilities (macOS, Windows, Linux) achieve 80%+ coverage"
    - "Formatter functions (date, currency, phone, string) tested with various inputs"
    - "Validator functions (email, URL, phone, UUID) tested with valid and invalid inputs"
    - "Desktop helpers (file operations, path handling, OS detection) tested with edge cases"
    - "Platform-specific code (macOS notifications, Windows registry, Linux paths) tested per platform"
    - "Utility functions use pytest.mark.parametrize for efficient edge case testing"
    - "Coverage report shows 80%+ for all utility files across backend and desktop"
  artifacts:
    - path: "backend/tests/unit/utils/test_formatters.py"
      provides: "Formatter function tests (date, currency, phone, string)"
      min_lines: 150
      contains: "test_format_date", "test_format_currency", "test_format_phone"
    - path: "backend/tests/unit/utils/test_validators.py"
      provides: "Validator function tests (email, URL, phone, UUID)"
      min_lines: 150
      contains: "test_validate_email", "test_validate_url", "test_validate_phone"
    - path: "menubar/src-tauri/tests/helpers_test.rs"
      provides: "Desktop helper function tests"
      min_lines: 150
      contains: "mod tests", "fn test_file_operations", "fn test_path_handling"
    - path: "menubar/src-tauri/tests/platform_specific_test.rs"
      provides: "Platform-specific utility tests"
      min_lines: 150
      contains: "mod tests", "fn test_macos_helpers", "fn test_windows_helpers"
    - path: "backend/tests/coverage_reports/html/index.html"
      provides: "Coverage report showing 80%+ utility coverage"
      contains: "agent_utils.py", "email_utils.py", "decimal_utils.py"
  key_links:
    - from: "backend/tests/unit/utils/test_formatters.py"
      to: "backend/core/agent_utils.py"
      via: "from core.agent_utils import format_agent_id"
      pattern: "from core.*utils import"
    - from: "backend/tests/unit/utils/test_validators.py"
      to: "backend/core/email_utils.py"
      via: "from core.email_utils import validate_email"
      pattern: "from core.*utils import"
    - from: "menubar/src-tauri/tests/helpers_test.rs"
      to: "menubar/src-tauri/src/helpers.rs"
      via: "use crate::helpers::*"
      pattern: "use crate::helpers"
    - from: "menubar/src-tauri/tests/platform_specific_test.rs"
      to: "menubar/src-tauri/src/platform_specific.rs"
      via: "use crate::platform_specific::*"
      pattern: "use crate::platform_specific"
    - from: "pytest --cov=core --cov-report=html"
      to: "backend/tests/coverage_reports/html/index.html"
      via: "Coverage report generation"
      pattern: "pytest.*cov.*utils"
---

<objective>
Cross-Platform Utility Testing - Achieve 80%+ coverage on backend pure functions and desktop helper/platform-specific utilities

Purpose: Utility functions are pure functions with simple inputs/outputs, making them ideal quick wins. Backend has formatters and validators that need comprehensive testing. Desktop has helper functions (file operations, path handling) and platform-specific code (macOS, Windows, Linux differences) that need coverage. These are low-complexity but high-impact code paths.

Output: 4 new test files (150+ lines each) covering backend formatters/validators and desktop helpers/platform-specific utilities with 80%+ coverage
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/155-quick-wins-leaf-components-infrastructure/155-RESEARCH.md
@backend/tests/README.md
@backend/core/agent_utils.py
@backend/core/email_utils.py
@backend/core/decimal_utils.py
@menubar/src-tauri/src/helpers.rs
@menubar/src-tauri/src/platform_specific.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test Backend Formatter Functions (date, currency, phone, string)</name>
  <files>backend/tests/unit/utils/test_formatters.py</files>
  <action>
Create comprehensive tests for formatter utility functions in backend/core/:

1. **Agent Utility formatters** (backend/core/agent_utils.py):
   - test_format_agent_id: Valid agent ID formatting
   - test_parse_agent_id: Agent ID parsing with validation
   - test_format_maturity_level: Maturity level display names (STUDENT -> "Student Agent")

2. **Date/Time formatters**:
   - test_format_date_iso: ISO date formatting (2026-03-08 -> "March 8, 2026")
   - test_format_datetime: DateTime formatting with time
   - test_format_timestamp: Unix timestamp to readable date
   - test_format_relative_time: Relative time ("2 hours ago")

3. **Number/Currency formatters**:
   - test_format_currency_usd: USD currency formatting ($1,000.00)
   - test_format_currency_eur: EUR currency formatting (€1,000.00)
   - test_format_number: Number formatting with commas (1,000,000)
   - test_format_percentage: Percentage formatting (50.5%)
   - test_format_decimal: Decimal precision handling

4. **String formatters**:
   - test_format_phone: Phone number formatting ((123) 456-7890)
   - test_format_name: Name capitalization (john -> John)
   - test_truncate_text: Text truncation with ellipsis (...)
   - test_sanitize_string: String sanitization (remove HTML, special chars)

5. **Edge cases** (use pytest.mark.parametrize):
   - Empty strings, null values
   - Boundary values (min/max numbers, empty arrays)
   - Special characters (Unicode, emojis)
   - Invalid inputs (negative numbers, malformed dates)

Pattern example from research:
```python
@pytest.mark.parametrize("input_date,expected_output", [
    ("2026-03-08", "March 8, 2026"),
    ("2026-01-01", "January 1, 2026"),
    ("2026-12-31", "December 31, 2026"),
])
def test_format_date(self, input_date, expected_output):
    assert format_date(input_date) == expected_output
```

Test pure function behavior - no mocking needed, just input -> output validation.
</action>
  <verify>
cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/utils/test_formatters.py -v --cov=core/agent_utils --cov=core/decimal_utils --cov-report=term-missing
</verify>
  <done>
Formatter functions have 80%+ coverage with comprehensive parametrized tests covering edge cases
</done>
</task>

<task type="auto">
  <name>Task 2: Test Backend Validator Functions (email, URL, phone, UUID)</name>
  <files>backend/tests/unit/utils/test_validators.py</files>
  <action>
Create comprehensive tests for validator utility functions in backend/core/:

1. **Email validators** (backend/core/email_utils.py):
   - test_validate_email_valid: Accepts valid email formats (user@example.com)
   - test_validate_email_invalid_format: Rejects invalid emails (no @, invalid domain)
   - test_validate_email_edge_cases: Edge cases (subdomains, + addressing, Unicode)
   - test_validate_email_empty: Rejects empty strings and None

2. **URL validators**:
   - test_validate_url_valid: Accepts valid URLs (https://example.com)
   - test_validate_url_invalid_scheme: Rejects invalid schemes (ftp://, no scheme)
   - test_validate_url_query_params: Validates URLs with query parameters
   - test_validate_url_fragment: Validates URLs with fragments (#section)

3. **Phone validators**:
   - test_validate_phone_valid: Accepts valid phone numbers (1234567890)
   - test_validate_phone_invalid_length: Rejects invalid lengths
   - test_validate_phone_international: Validates international formats (+1...)
   - test_validate_phone_formatting: Validates formatted numbers ((123) 456-7890)

4. **UUID validators**:
   - test_validate_uuid_valid: Accepts valid UUID v4 format
   - test_validate_uuid_invalid_format: Rejects malformed UUIDs
   - test_validate_uuid_version: Validates UUID version (v4 vs v1)

5. **General validators**:
   - test_validate_boolean: Boolean string validation ("true"/"false")
   - test_validate_integer: Integer validation with ranges
   - test_validate_float: Float validation with precision
   - test_validate_json: JSON string validation

6. **Edge cases** (use pytest.mark.parametrize):
   - Empty strings, null values
   - Boundary values (min/max lengths)
   - Special characters (Unicode, emojis)
   - Malformed inputs (typos, truncated)

Pattern example:
```python
@pytest.mark.parametrize("invalid_email", [
    "plainaddress",
    "@missingusername.com",
    "username@.com",
    "username@com",
    "",
    None,
])
def test_validate_email_invalid(self, invalid_email):
    with pytest.raises(ValidationError):
        validate_email(invalid_email)
```

Test validation logic - both valid and invalid inputs, with clear error messages.
</action>
  <verify>
cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/utils/test_validators.py -v --cov=core/email_utils --cov-report=term-missing
</verify>
  <done>
Validator functions have 80%+ coverage with comprehensive valid/invalid input tests
</done>
</task>

<task type="auto">
  <name>Task 3: Test Desktop Helper Functions (file operations, path handling, OS detection)</name>
  <files>menubar/src-tauri/tests/helpers_test.rs</files>
  <action>
Create comprehensive tests for desktop helper functions in menubar/src-tauri/src/helpers.rs:

1. **File operation helpers**:
   - test_read_file_content: Reads file content correctly
   - test_write_file_content: Writes content to file
   - test_file_exists: Checks file existence
   - test_delete_file: Deletes file safely
   - test_create_directory: Creates directory with parents
   - test_get_file_extension: Extracts file extension correctly

2. **Path handling helpers**:
   - test_join_paths: Joins path segments correctly
   - test_normalize_path: Normalizes path separators (Windows \ vs Unix /)
   - test_get_parent_directory: Extracts parent directory
   - test_get_file_name: Extracts file name from path
   - test_is_absolute_path: Detects absolute vs relative paths

3. **OS detection helpers**:
   - test_is_macos: Detects macOS platform
   - test_is_windows: Detects Windows platform
   - test_is_linux: Detects Linux platform
   - test_get_home_directory: Returns correct home directory per platform

4. **String helpers**:
   - test_truncate_string: Truncates with ellipsis
   - test_sanitize_filename: Removes invalid filename characters
   - test_generate_unique_id: Generates unique identifiers

5. **Edge cases**:
   - Empty paths, null inputs
   - Paths with special characters
   - Very long paths (>260 chars on Windows)
   - Unicode paths, emoji in filenames

Pattern example:
```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_join_paths() {
        let result = join_paths(&["home", "user", "documents"]);
        assert!(result.contains("documents"));
    }

    #[test]
    fn test_get_file_extension() {
        assert_eq!(get_file_extension("test.txt"), Some("txt".to_string()));
        assert_eq!(get_file_extension("test"), None);
        assert_eq!(get_file_extension(".hidden"), None);
    }

    #[test]
    fn test_is_macos() {
        let result = is_macos();
        // Result depends on test platform
        assert!(result || !result); // Just verify it returns a boolean
    }
}
```

Use Rust's built-in test framework. For file operations, use temp directories (tempfile crate) to avoid side effects.
</action>
  <verify>
cd menubar/src-tauri && cargo test helpers_test -- --nocapture
</verify>
  <done>
Desktop helper functions have 80%+ coverage with file operations and path handling tests passing
</done>
</task>

<task type="auto">
  <name>Task 4: Test Desktop Platform-Specific Utilities (macOS, Windows, Linux)</name>
  <files>menubar/src-tauri/tests/platform_specific_test.rs</files>
  <action>
Create comprehensive tests for platform-specific utilities in menubar/src-tauri/src/platform_specific.rs:

1. **macOS-specific utilities**:
   - test_get_macos_app_support_path: Returns ~/Library/Application Support
   - test_send_macos_notification: Sends macOS notification (mocked)
   - test_get_macos_version: Parses macOS version string
   - test_is_macos_sandboxed: Detects sandbox environment

2. **Windows-specific utilities**:
   - test_get_windows_app_data_path: Returns %APPDATA% path
   - test_get_windows_temp_path: Returns temp directory
   - test_send_windows_notification: Sends Windows toast notification (mocked)
   - test_get_windows_version: Parses Windows version

3. **Linux-specific utilities**:
   - test_get_linux_config_path: Returns ~/.config path
   - test_get_linux_data_path: Returns ~/.local/share path
   - test_send_linux_notification: Sends libnotify notification (mocked)
   - test_detect_desktop_environment: Detects GNOME/KDE/other

4. **Cross-platform compatibility**:
   - test_get_cache_path: Returns correct path per platform
   - test_get_log_path: Returns correct log path per platform
   - test_open_url: Opens URL in default browser per platform
   - test_show_in_folder: Shows file in file manager per platform

5. **Conditional compilation tests**:
   - Use #[cfg(target_os = "macos")] for macOS-only tests
   - Use #[cfg(target_os = "windows")] for Windows-only tests
   - Use #[cfg(target_os = "linux")] for Linux-only tests

Pattern example:
```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[cfg(target_os = "macos")]
    #[test]
    fn test_get_macos_app_support_path() {
        let path = get_macos_app_support_path("Atom");
        assert!(path.contains("Library"));
        assert!(path.contains("Application Support"));
        assert!(path.contains("Atom"));
    }

    #[cfg(target_os = "windows")]
    #[test]
    fn test_get_windows_app_data_path() {
        let path = get_windows_app_data_path("Atom");
        assert!(path.contains("AppData"));
        assert!(path.contains("Roaming"));
    }

    #[cfg(target_os = "linux")]
    #[test]
    fn test_get_linux_config_path() {
        let path = get_linux_config_path("atom");
        assert!(path.contains(".config"));
        assert!(path.contains("atom"));
    }

    #[test]
    fn test_get_cache_path() {
        let path = get_cache_path("Atom");
        assert!(!path.is_empty());
        // Path depends on platform
    }
}
```

Use conditional compilation to run platform-specific tests only on that platform. Mock OS-level operations (notifications, file manager) to avoid side effects.
</action>
  <verify>
cd menubar/src-tauri && cargo test platform_specific_test -- --nocapture
</verify>
  <done>
Platform-specific utilities have 80%+ coverage with macOS, Windows, and Linux tests passing
</done>
</task>

</tasks>

<verification>
Run all utility tests together and verify coverage:
```bash
# Backend
cd backend
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/utils/ -v --cov=core/agent_utils --cov=core/email_utils --cov=core/decimal_utils --cov-report=html --cov-report=term-missing

# Desktop
cd menubar/src-tauri
cargo test helpers_test platform_specific_test --all
```

Check coverage reports:
```bash
open backend/tests/coverage_reports/html/index.html
```

Verify 80%+ coverage for all utility files:
- Backend: agent_utils.py, email_utils.py, decimal_utils.py: 80%+
- Desktop: helpers.rs, platform_specific.rs: 80%+
</verification>

<success_criteria>
1. All 4 test files created (test_formatters.py, test_validators.py, helpers_test.rs, platform_specific_test.rs)
2. 600+ total test lines covering utility functions across backend and desktop
3. 80%+ coverage achieved on all utility files (backend and desktop)
4. All tests pass (pytest for backend, cargo test for desktop)
5. pytest.mark.parametrize used for efficient edge case testing
6. Conditional compilation used for platform-specific desktop tests
7. Coverage report confirms 80%+ threshold met
</success_criteria>

<output>
After completion, create `.planning/phases/155-quick-wins-leaf-components-infrastructure/155-02-SUMMARY.md` with:
- Number of utility test files created per platform
- Coverage percentages for each utility file
- Total test count and parametrize usage count
- Examples of edge cases covered
- Platform-specific test distribution (macOS, Windows, Linux)
</output>
