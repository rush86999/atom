---
phase: 74-quality-gates-property-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
must_haves:
  truths:
    - "All tests must pass before merge is allowed"
    - "Test failures block CI pipeline even if coverage passes"
    - "pytest exit code determines job success/failure"
    - "No '|| true' or other bypasses on test execution"
  artifacts:
    - path: ".github/workflows/ci.yml"
      contains: "pytest --maxfail=10"
      exports: ["exit code check"]
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pytest test execution"
      via: "pytest exit code determines job status"
      pattern: "pytest.*--maxfail"
---

<objective>
Enforce GATES-02: All tests must pass before merge is allowed.

Purpose: Ensure CI pipeline blocks merges when tests fail, preventing broken code from reaching production.
Output: CI workflow configuration that fails fast on test failures.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-quality-gates-property-testing/074-RESEARCH.md
@.planning/ROADMAP.md

@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Verify test execution blocks on failures</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    1. In backend-test-full job, locate pytest command (line ~169)
    2. Verify --maxfail=10 is set (fast-fail behavior)
    3. Ensure NO "|| true", "|| continue", or other bypass patterns
    4. Ensure job continues only on success (implicit via exit code)

    Current expected pattern:
    ```yaml
    pytest tests/ -v -n auto \
      --json-report --json-report-file=pytest_report.json \
      --cov=core \
      ... \
      --maxfail=10
    ```
    # No || after this command - exit code controls job success

    5. Add explicit comment for GATES-02:
    ```yaml
    # GATES-02: All tests must pass - exit code blocks merge on failure
    ```

    If bypass exists (|| true/continue), remove it.

    Do NOT modify: parallel execution flag (-n auto), coverage flags, maxfail value
  </action>
  <verify>grep -A5 "pytest tests/ -v -n auto" .github/workflows/ci.yml | grep -v "|| true"</verify>
  <done>Test command fails the job when any test fails (no bypass present)</done>
</task>

<task type="auto">
  <name>Verify quality gates depend on test success</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    1. Locate test-quality-gates job definition
    2. Verify it has `needs: backend-test-full` dependency
    3. This ensures quality gates ONLY run if tests pass

    Expected (line ~236):
    ```yaml
    test-quality-gates:
      runs-on: ubuntu-latest
      needs: backend-test-full
    ```

    4. If `needs:` is missing, add it to enforce test-before-quality-gates ordering
    5. Add comment: "# GATES-02: Quality gates run only after tests pass"

    Do NOT modify: quality gate test commands, job names
  </action>
  <verify>grep -B2 "test-quality-gates:" .github/workflows/ci.yml | grep "needs:"</verify>
  <done>Quality gates job requires successful test completion before running</done>
</task>

<task type="auto">
  <name>Add test summary to job output</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    1. In backend-test-full job, after pytest runs, verify test count is displayed
    2. Add step to parse and display test results in summary:
       ```yaml
      - name: Test Results Summary
        if: always()
        run: |
          echo "## Test Results" >> $GITHUB_STEP_SUMMARY
          if [ -f pytest_report.json ]; then
            python tests/scripts/parse_pytest_output.py pytest_report.json | tee -a $GITHUB_STEP_SUMMARY
          fi
       ```
    3. This satisfies GATES-03: Coverage/test reports viewable in CI output

    Do NOT modify: pytest command, json-report generation
  </action>
  <verify>grep -n "Test Results Summary" .github/workflows/ci.yml</verify>
  <done>Test results appear in GitHub Actions summary with pass/fail counts</done>
</task>

</tasks>

<verification>
1. Run: grep -n "|| true" .github/workflows/ci.yml | grep pytest - Should return empty (no bypasses)
2. Run: grep -B5 "test-quality-gates:" .github/workflows/ci.yml | grep "needs:" - Shows dependency on backend-test-full
3. Run: grep -n "maxfail" .github/workflows/ci.yml - Confirms fast-fail behavior
</verification>

<success_criteria>
- pytest exit code determines job success (no bypass patterns)
- Quality gates only run after tests pass (needs: dependency)
- Test results visible in GitHub Actions summary
- --maxfail ensures fast-fail on test failures
</success_criteria>

<output>
After completion, create `.planning/phases/74-quality-gates-property-testing/74-02-SUMMARY.md`
</output>
