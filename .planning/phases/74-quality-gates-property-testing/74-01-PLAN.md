---
phase: 74-quality-gates-property-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/test-coverage.yml
autonomous: true
must_haves:
  truths:
    - "CI pipeline blocks deployment when coverage drops below 80%"
    - "Coverage gate failure is clearly visible in CI/CD output"
    - "Coverage report is uploaded as artifact for inspection"
    - "Both main CI and test-coverage workflows enforce 80% threshold"
  artifacts:
    - path: ".github/workflows/ci.yml"
      contains: "--cov-fail-under=80"
    - path: ".github/workflows/test-coverage.yml"
      contains: "--cov-fail-under=80"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "pytest coverage command"
      via: "--cov-fail-under=80 flag"
      pattern: "--cov-fail-under=80"
---

<objective>
Implement CI/CD coverage gates that enforce 80% minimum coverage threshold and block deployment on regression.

Purpose: Production deployment safety - ensure code quality never degrades below 80% coverage threshold.
Output: Updated CI workflows with blocking 80% coverage gates.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-quality-gates-property-testing/074-RESEARCH.md
@.planning/ROADMAP.md

@.github/workflows/ci.yml
@.github/workflows/test-coverage.yml
</context>

<tasks>

<task type="auto">
  <name>Update main CI workflow with 80% coverage gate</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    1. Locate the "TQ-05: Coverage Quality" step in test-quality-gates job
    2. Change --cov-fail-under from 50 to 80
    3. Update step name from "TQ-05 Enforce coverage threshold (50% minimum)" to "GATES-01 Enforce 80% coverage threshold"
    4. Remove the "|| echo..." fallback that makes the gate non-blocking
    5. Update quality gate summary to reflect 80% threshold

    Current code (lines ~418-421):
    ```yaml
    --cov-fail-under=50 \
    || echo "Coverage below 50% threshold (informational for now)"
    ```

    Change to:
    ```yaml
    --cov-fail-under=80
    ```
    (This makes it BLOCKING - pipeline fails if coverage < 80%)

    6. Also update the backend-test-full job coverage command (line ~176) to include --cov-fail-under=80
    7. Add explicit comment "# GATES-01, GATES-05: Blocks deployment if coverage < 80%"

    Do NOT modify: test job structure, matrix strategies, artifact uploads
  </action>
  <verify>grep -n "cov-fail-under=80" .github/workflows/ci.yml</verify>
  <done>ci.yml contains --cov-fail-under=80 with no fallback echo that would bypass the gate</done>
</task>

<task type="auto">
  <name>Update test-coverage workflow with 80% gate</name>
  <files>.github/workflows/test-coverage.yml</files>
  <action>
    1. Locate the "Run tests with coverage" step (line ~30)
    2. Change --cov-fail-under from 25 to 80
    3. Update MINIMUM_GREEN from 80 to 85 (for PR comment gradient)
    4. Add branch coverage flag: --cov-branch

    Current code (line ~38):
    ```yaml
    --cov-fail-under=25
    ```

    Change to:
    ```yaml
    --cov-branch \
    --cov-fail-under=80
    ```

    5. Add comment "# GATES-01: Deployment blocked if coverage < 80%"

    Do NOT modify: artifact upload, PR comment action, diff-cover check
  </action>
  <verify>grep -n "cov-fail-under=80" .github/workflows/test-coverage.yml</verify>
  <done>test-coverage.yml enforces 80% threshold with branch coverage enabled</done>
</task>

<task type="auto">
  <name>Add coverage report artifact upload to CI</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    1. In backend-test-full job, after coverage generation, verify upload-artifact step exists
    2. Ensure coverage.json is uploaded with retention-days: 30
    3. Add a new step after "Coverage summary" to display coverage in job summary:
       ```yaml
      - name: Add coverage to job summary
        run: |
          echo "## Coverage Report" >> $GITHUB_STEP_SUMMARY
          if [ -f tests/coverage_reports/metrics/coverage.json ]; then
            python -c "import json; d=json.load(open('tests/coverage_reports/metrics/coverage.json')); p=d['totals']['percent_covered']; print(f'{p:.1f}%')" | tee -a $GITHUB_STEP_SUMMARY
          fi
       ```
    4. This ensures GATES-03: Coverage report is viewable in CI/CD output

    Do NOT modify: existing upload-artifact step, coverage generation command
  </action>
  <verify>grep -n "GITHUB_STEP_SUMMARY" .github/workflows/ci.yml | grep coverage</verify>
  <done>Coverage percentage appears in GitHub Actions job summary page</done>
</task>

</tasks>

<verification>
1. Run: grep -n "cov-fail-under" .github/workflows/*.yml - Both workflows show 80
2. Run: grep -n "GATES-01" .github/workflows/ci.yml - Comment references requirement
3. Run: git diff --no-ext-diff .github/workflows/ci.yml | grep -E "^[\+\-].*cov-fail-under" - Shows 50->80 change
4. Verify no "|| echo" bypass exists after --cov-fail-under=80
</verification>

<success_criteria>
- CI pipeline exits with error code 1 if coverage drops below 80%
- Coverage percentage visible in GitHub Actions job summary
- Both ci.yml and test-coverage.yml enforce 80% threshold
- Branch coverage (--cov-branch) enabled for stricter measurement
</success_criteria>

<output>
After completion, create `.planning/phases/74-quality-gates-property-testing/74-01-SUMMARY.md`
</output>
