---
phase: 096-mobile-integration
plan: 04
type: execute
wave: 2
depends_on:
  - 096-02
files_modified:
  - mobile/src/__tests__/integration/devicePermissions.integration.test.ts
  - mobile/src/__tests__/integration/offlineSyncNetwork.integration.test.ts
autonomous: true

must_haves:
  truths:
    - "Device permissions integration tests cover camera, location, notifications permission flows"
    - "Offline sync integration tests cover network state transitions and sync on reconnect"
    - "Tests use mocked Expo modules and NetInfo for network state simulation"
    - "Tests verify proper permission request handling and sync triggers"
  artifacts:
    - path: mobile/src/__tests__/integration/devicePermissions.integration.test.ts
      provides: Device permission integration tests
      min_lines: 200
    - path: mobile/src/__tests__/integration/offlineSyncNetwork.integration.test.ts
      provides: Offline sync network integration tests
      min_lines: 180
  key_links:
    - from: devicePermissions.integration.test.ts
      to: jest.setup.js
      via: Expo module mocks (camera, location, notifications)
      pattern: expo-camera|expo-location|expo-notifications
    - from: offlineSyncNetwork.integration.test.ts
      to: jest.setup.js
      via: NetInfo mock
      pattern: @react-native-community/netinfo
---

<objective>
Create integration tests for device permissions and offline sync with network transitions.

**Purpose**: Test permission flows for device features (camera, location, notifications) and offline sync behavior when network state changes. These are cross-cutting concerns that require integrating multiple services and platform APIs.

**Output**: Two integration test files covering device permission workflows and offline sync network transitions.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
</execution_context>

<context>
@mobile/jest.setup.js
@mobile/src/services/offlineSyncService.ts
@mobile/src/__tests__/services/offlineSync.test.ts
@mobile/src/__tests__/contexts/DeviceContext.test.tsx
@.planning/phases/096-mobile-integration/096-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create device permissions integration tests</name>
  <files>mobile/src/__tests__/integration/devicePermissions.integration.test.ts</files>
  <action>
    Create mobile/src/__tests__/integration/devicePermissions.integration.test.ts:

    **Import services:**
    ```typescript
    import * as Camera from 'expo-camera';
    import * as Location from 'expo-location';
    import * as Notifications from 'expo-notifications';
    import * as LocalAuthentication from 'expo-local-authentication';
    import { devicePermissionsService } from '../../services/devicePermissionsService';
    ```

    **Test suites:**

    1. **Permission Request Workflows** (4-6 tests):
       - Should request camera permission and handle grant
       - Should request camera permission and handle denial
       - Should request location permission (foreground) and handle grant
       - Should request location permission (background) and handle grant
       - Should request notification permissions and handle grant
       - Should request biometric enrollment check

    2. **Permission State Transitions** (3-5 tests):
       - Should track permission state from notAsked to granted
       - Should track permission state from notAsked to denied
       - Should handle canAskAgain flag correctly
       - Should persist permission state across app restarts

    3. **Multi-Permission Flows** (3-5 tests):
       - Should request multiple permissions in sequence
       - Should handle partial grant (some granted, some denied)
       - Should show appropriate UI for each permission type
       - Should batch permission requests where possible

    4. **Platform-Specific Behavior** (4-6 tests):
       - Should handle iOS permission dialog behavior
       - Should handle Android permission rationale
       - Should handle iOS app-level permissions (via Settings)
       - Should handle Android runtime permissions
       - Should detect platform correctly (Platform.OS)

    5. **Permission Caching** (2-3 tests):
       - Should cache granted permissions in AsyncStorage
       - Should load cached permissions on app startup
       - Should invalidate cache when permissions change

    Use jest.spyOn to mock Expo permission methods with different return values (granted, denied, notAsked).
  </action>
  <verify>cd mobile && npm test -- devicePermissions.integration.test.ts</verify>
  <done>devicePermissions.integration.test.ts with 15-25 integration tests</done>
</task>

<task type="auto">
  <name>Create offline sync network integration tests</name>
  <files>mobile/src/__tests__/integration/offlineSyncNetwork.integration.test.ts</files>
  <action>
    Create mobile/src/__tests__/integration/offlineSyncNetwork.integration.test.ts:

    **Import services:**
    ```typescript
    import NetInfo from '@react-native-community/netinfo';
    import { offlineSyncService } from '../../services/offlineSyncService';
    import { apiService } from '../../services/api';
    ```

    **Test suites:**

    1. **Network State Transitions** (4-6 tests):
       - Should detect network state change from online to offline
       - Should detect network state change from offline to online
       - Should queue actions when offline
       - Should trigger sync when connection restored
       - Should handle network type changes (wifi, cellular, none)

    2. **Sync on Reconnect** (3-5 tests):
       - Should sync queued actions when reconnecting
       - Should sync in priority order on reconnect
       - Should handle sync failure during reconnect
       - Should retry failed sync after reconnect
       - Should notify user of sync status

    3. **Batch Sync Behavior** (3-5 tests):
       - Should sync in batches (max 100 actions per batch)
       - Should handle partial batch failures
       - Should continue syncing remaining batches after failure
       - Should respect batch size limits

    4. **Network Edge Cases** (3-5 tests):
       - Should handle slow network connections
       - Should handle intermittent connectivity (flapping)
       - Should handle network timeout during sync
       - Should handle connection with limited internet reachability

    5. **Sync Listener Integration** (2-4 tests):
       - Should register network state listener on initialization
       - Should unregister network state listener on cleanup
       - Should trigger sync only once per reconnect event
       - Should debounce rapid network state changes

    Mock NetInfo.fetch to return different network states. Mock fetch for API calls.
  </action>
  <verify>cd mobile && npm test -- offlineSyncNetwork.integration.test.ts</verify>
  <done>offlineSyncNetwork.integration.test.ts with 15-25 integration tests</done>
</task>

</tasks>

<verification>
After completion:
1. Run `cd mobile && npm test -- devicePermissions.integration.test.ts` - all tests pass
2. Run `cd mobile && npm test -- offlineSyncNetwork.integration.test.ts` - all tests pass
3. Check integration test coverage: `cd mobile && npm run test:coverage -- integration/`
4. Verify no native module errors
5. Run all tests to ensure no regressions
</verification>

<success_criteria>
1. devicePermissions.integration.test.ts exists with 15-25 tests
2. offlineSyncNetwork.integration.test.ts exists with 15-25 tests
3. All integration tests pass
4. Coverage >75% for permission and sync flows
5. Tests properly mock Expo modules and NetInfo
</success_criteria>

<output>
After completion, create `.planning/phases/096-mobile-integration/096-04-SUMMARY.md` with:
- Integration test structure and test counts
- Permission state transition patterns
- Network state simulation patterns
- Coverage metrics
</output>
