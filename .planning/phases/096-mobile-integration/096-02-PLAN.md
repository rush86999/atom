---
phase: 096-mobile-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/__tests__/services/biometricService.test.ts
  - mobile/src/__tests__/services/notificationService.test.ts
autonomous: true

must_haves:
  truths:
    - "Biometric authentication tests cover permission states, success/failure, lockout scenarios"
    - "Notification service tests cover scheduling, permissions, badge management"
    - "Tests use existing Expo module mocks from jest.setup.js"
    - "Tests pass with 80%+ coverage for biometric and notification services"
  artifacts:
    - path: mobile/src/__tests__/services/biometricService.test.ts
      provides: Biometric authentication integration tests
      min_lines: 150
    - path: mobile/src/__tests__/services/notificationService.test.ts
      provides: Notification service integration tests
      min_lines: 200
  key_links:
    - from: biometricService.test.ts
      to: jest.setup.js
      via: expo-local-authentication mock
      pattern: expo-local-authentication
    - from: notificationService.test.ts
      to: jest.setup.js
      via: expo-notifications mock
      pattern: expo-notifications
---

<objective>
Create integration tests for biometric authentication and notification services.

**Purpose**: Test device features that require platform permissions (biometric auth, notifications) using existing Expo module mocks. These are critical security and user-facing features that need comprehensive test coverage.

**Output**: Two integration test files covering biometric authentication flows and notification management with proper permission testing.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
</execution_context>

<context>
@mobile/jest.setup.js
@mobile/src/services/biometricService.ts
@mobile/src/services/notificationService.ts
@mobile/src/__tests__/services/offlineSync.test.ts
@.planning/phases/096-mobile-integration/096-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create biometric authentication service tests</name>
  <files>mobile/src/__tests__/services/biometricService.test.ts</files>
  <action>
    Create mobile/src/__tests__/services/biometricService.test.ts with comprehensive tests:

    **Import and setup:**
    ```typescript
    import * as LocalAuthentication from 'expo-local-authentication';
    import * as SecureStore from 'expo-secure-store';
    import { biometricService } from '../../services/biometricService';
    ```

    **Test suites:**

    1. **Hardware Availability Tests** (3-5 tests):
       - Should check if biometric hardware is available
       - Should return false when hardware not available
       - Should handle hardware check errors

    2. **Enrollment Status Tests** (3-5 tests):
       - Should check if user has enrolled biometrics
       - Should return false when no biometrics enrolled
       - Should handle enrollment check errors

    3. **Authentication Flow Tests** (5-7 tests):
       - Should authenticate successfully with valid biometrics
       - Should return success=true on successful authentication
       - Should handle authentication failure
       - Should return appropriate error codes (not_enrolled, authentication_failed, etc.)
       - Should support cancel fallback

    4. **Permission States Tests** (4-6 tests):
       - Should handle granted permission state
       - Should handle denied permission state
       - Should handle notAsked permission state
       - Should request permissions when not granted

    5. **Lockout Scenarios** (3-5 tests):
       - Should lock out after max failed attempts (5 attempts)
       - Should prevent authentication while locked out
       - Should reset lockout after timeout
       - Should track failed attempt count

    6. **Credential Storage Tests** (3-5 tests):
       - Should store auth token securely after successful auth
       - Should retrieve stored auth token
       - Should clear stored credentials on logout

    Use jest.spyOn to mock LocalAuthentication methods per test. Follow patterns from offlineSync.test.ts for test structure.
  </action>
  <verify>cd mobile && npm test -- biometricService.test.ts</verify>
  <done>biometricService.test.ts with 20-30 tests covering authentication flows</done>
</task>

<task type="auto">
  <name>Create notification service tests</name>
  <files>mobile/src/__tests__/services/notificationService.test.ts</files>
  <action>
    Create mobile/src/__tests__/services/notificationService.test.ts with comprehensive tests:

    **Import and setup:**
    ```typescript
    import * as Notifications from 'expo-notifications';
    import { notificationService } from '../../services/notificationService';
    ```

    **Test suites:**

    1. **Permission Tests** (4-6 tests):
       - Should request notification permissions on initialization
       - Should handle granted permission status
       - Should handle denied permission status
       - Should check current permission status
       - Should handle iOS-specific permissions (alert, badge, sound)
       - Should handle Android permission settings

    2. **Local Notification Tests** (5-7 tests):
       - Should schedule local notification with valid content
       - Should accept notification content (title, body, data)
       - Should return notification ID on successful scheduling
       - Should handle scheduling errors
       - Should cancel scheduled notification by ID
       - Should cancel all scheduled notifications

    3. **Badge Management Tests** (3-5 tests):
       - Should get current badge count
       - Should set badge count
       - Should increment badge count
       - Should decrement badge count
       - Should clear badge count (set to 0)

    4. **Push Token Tests** (3-5 tests):
       - Should get Expo push token
       - Should handle token generation errors
       - Should cache push token locally
       - Should refresh token when expired

    5. **Notification Handler Tests** (3-5 tests):
       - Should set notification handler
       - Should handle received notifications
       - Should handle notification responses (user tap)
       - Should register notification received listener
       - Should register notification response listener

    6. **Foreground Notification Tests** (3-4 tests):
       - Should present notification while app is foreground
       - Should handle foreground notification presentation
       - Should dismiss programmatically presented notification

    Use jest.spyOn to mock Notifications methods. Each test should verify correct mock calls and return values.
  </action>
  <verify>cd mobile && npm test -- notificationService.test.ts</verify>
  <done>notificationService.test.ts with 25-35 tests covering notification features</done>
</task>

</tasks>

<verification>
After completion:
1. Run `cd mobile && npm test -- biometricService.test.ts` - all tests pass
2. Run `cd mobile && npm test -- notificationService.test.ts` - all tests pass
3. Check coverage: `cd mobile && npm run test:coverage -- services/biometricService.test.ts` - >80% coverage
4. Verify Expo mocks are used (no native module errors)
5. Run all mobile tests to ensure no regressions: `cd mobile && npm test`
</verification>

<success_criteria>
1. biometricService.test.ts exists with 20-30 tests
2. notificationService.test.ts exists with 25-35 tests
3. All tests pass with npm test
4. Coverage >80% for both services
5. Tests use existing Expo mocks from jest.setup.js
6. No native module errors in test output
</success_criteria>

<output>
After completion, create `.planning/phases/096-mobile-integration/096-02-SUMMARY.md` with:
- Test file structure and test counts
- Coverage metrics for both services
- Permission state testing patterns used
- Any discovered edge cases or bugs
</output>
