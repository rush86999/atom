---
phase: 096-mobile-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/scripts/aggregate_coverage.py
autonomous: true

must_haves:
  truths:
    - "Coverage aggregator script parses jest-expo JSON coverage from mobile"
    - "Aggregator produces unified coverage report with per-platform breakdown (backend, frontend, mobile)"
    - "Unified report includes overall coverage percentage across all platforms"
    - "Script handles missing mobile coverage file gracefully (exit with warning, not error)"
  artifacts:
    - path: backend/tests/scripts/aggregate_coverage.py
      provides: Unified coverage aggregation for backend, frontend, and mobile
      min_lines: 200
      exports: [load_pytest_coverage, load_jest_coverage, load_jest_expo_coverage, aggregate_coverage, generate_report]
  key_links:
    - from: aggregate_coverage.py
      to: mobile/coverage/coverage-final.json
      via: load_jest_expo_coverage function
      pattern: coverage-final\.json
---

<objective>
Extend unified coverage aggregation script to include mobile jest-expo coverage.

**Purpose**: Combine pytest JSON coverage (backend), Jest JSON coverage (frontend), and jest-expo JSON coverage (mobile) into a single unified report with per-platform breakdown. This enables tracking overall coverage across all platforms and supports the 80% overall quality gate.

**Output**: Extended Python script that parses all three coverage formats, aggregates metrics, and produces unified JSON/text reports.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
</execution_context>

<context>
@backend/tests/scripts/aggregate_coverage.py
@mobile/jest.config.js
@.planning/ROADMAP.md
@.planning/phases/095-backend-frontend-integration/095-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add jest-expo coverage loader to aggregation script</name>
  <files>backend/tests/scripts/aggregate_coverage.py</files>
  <action>
    Extend backend/tests/scripts/aggregate_coverage.py with mobile coverage support:

    1. **Add load_jest_expo_coverage(path: Path) -> Dict[str, Any]** function:
       - Load jest-expo coverage-final.json from mobile/coverage/coverage-final.json
       - Parse Jest format (same as frontend): iterate entries, sum lines covered/total
       - Return dict with platform='mobile', coverage_pct, covered, total, branches_covered, branches_total, branch_coverage_pct
       - Handle FileNotFoundError by returning platform='mobile', coverage_pct=0.0, error='file not found'

    2. **Update aggregate_coverage()** function signature:
       - Add parameter: jest_expo_path: Path = Path('mobile/coverage/coverage-final.json')
       - Call load_jest_expo_coverage and include in platforms dict
       - Compute overall coverage: (covered_backend + covered_frontend + covered_mobile) / (total_backend + total_frontend + total_mobile)
       - Return dict with platforms: {python: {...}, javascript: {...}, mobile: {...}}

    3. **Update main()** function:
       - Add --mobile-coverage argument default=mobile/coverage/coverage-final.json
       - Pass mobile coverage path to aggregate_coverage()

    4. **Update generate_report()** function:
       - Add mobile row to text format table
       - Add mobile to markdown format table
       - Ensure all three platforms appear in reports

    5. **Update argparse** in main():
       - Add --mobile-coverage CLI argument
       - Pass to aggregate_coverage call

    Use existing patterns from load_jest_coverage (frontend) for consistency.
  </action>
  <verify>cd backend && python tests/scripts/aggregate_coverage.py --help | grep mobile</verify>
  <done>aggregate_coverage.py supports mobile coverage with load_jest_expo_coverage function</done>
</task>

<task type="auto">
  <name>Test mobile coverage aggregation</name>
  <files>backend/tests/scripts/coverage_reports/unified/coverage.json</files>
  <action>
    1. Ensure mobile/coverage/coverage-final.json exists (run mobile tests if needed: cd mobile && npm run test:coverage)
    2. Run aggregation script: python backend/tests/scripts/aggregate_coverage.py --format text
    3. Verify mobile platform appears in output
    4. Run with JSON format: python backend/tests/scripts/aggregate_coverage.py --format json
    5. Verify unified JSON structure: cat backend/tests/scripts/coverage_reports/unified/coverage.json | jq .platforms.mobile
    6. Test graceful handling with missing mobile coverage: temporarily rename mobile coverage file, run script, verify warning not error

    Overall coverage should now include mobile in weighted average calculation.
  </action>
  <verify>cat backend/tests/scripts/coverage_reports/unified/coverage.json | jq '.platforms | keys'</verify>
  <done>Mobile coverage included in unified report, graceful handling of missing file</done>
</task>

</tasks>

<verification>
After completion:
1. Run `python backend/tests/scripts/aggregate_coverage.py --format text` - should show 3 platforms (python, javascript, mobile)
2. Verify mobile coverage in JSON: `cat backend/tests/scripts/coverage_reports/unified/coverage.json | jq .platforms.mobile`
3. Test missing file handling: rename mobile coverage, run script, verify warning not error
4. Verify overall coverage calculation includes all three platforms
</verification>

<success_criteria>
1. load_jest_expo_coverage function exists and parses mobile coverage
2. Mobile platform appears in all report formats (text, json, markdown)
3. Overall coverage percentage includes mobile in weighted average
4. Script handles missing mobile coverage gracefully (warning, not error)
5. CLI accepts --mobile-coverage argument
</success_criteria>

<output>
After completion, create `.planning/phases/096-mobile-integration/096-01-SUMMARY.md` with:
- load_jest_expo_coverage implementation
- Sample unified output with mobile included
- Test execution results
- Updated overall coverage formula (3 platforms)
</output>
