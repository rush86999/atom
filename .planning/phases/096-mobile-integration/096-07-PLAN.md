---
phase: 096-mobile-integration
plan: 07
type: execute
wave: 4
depends_on:
  - 096-01
  - 096-02
  - 096-03
  - 096-04
  - 096-05
  - 096-06
files_modified:
  - .planning/phases/096-mobile-integration/096-VERIFICATION.md
autonomous: true

must_haves:
  truths:
    - "All 6 success criteria from Phase 096 validated"
    - "MOBL-01 to MOBL-04 requirements verified complete"
    - "Coverage metrics collected and documented"
    - "Test infrastructure ready for Phase 097 (Desktop)"
  artifacts:
    - path: .planning/phases/096-mobile-integration/096-VERIFICATION.md
      provides: Phase verification report
      min_lines: 200
    - path: .planning/phases/096-mobile-integration/096-FINAL-VERIFICATION.md
      provides: Final phase summary
      min_lines: 150
  key_links:
    - from: 096-VERIFICATION.md
      to: 096-01-SUMMARY.md through 096-06-SUMMARY.md
      via: All plan summaries referenced
      pattern: SUMMARY
---

<objective>
Verify Phase 096 completion against all success criteria and requirements.

**Purpose**: Validate that all Phase 096 plans are complete, mobile integration tests are working, coverage meets targets, and infrastructure is ready for Phase 097 (Desktop Testing).

**Output**: Comprehensive verification report documenting all success criteria, test metrics, and any gaps or issues discovered.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
</execution_context>

<context>
@.planning/phases/096-mobile-integration/096-RESEARCH.md
@.planning/phases/095-backend-frontend-integration/095-VERIFICATION.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Verify coverage aggregation includes mobile</name>
  <files>backend/tests/scripts/aggregate_coverage.py</files>
  <action>
    Verify unified coverage aggregation works with mobile:

    1. Check load_jest_expo_coverage function exists: grep -n "load_jest_expo_coverage" backend/tests/scripts/aggregate_coverage.py
    2. Run aggregation script: python backend/tests/scripts/aggregate_coverage.py --format text
    3. Verify output shows 3 platforms (python, javascript, mobile)
    4. Verify mobile coverage appears in JSON: cat backend/tests/scripts/coverage_reports/unified/coverage.json | jq .platforms.mobile
    5. Test missing file handling: temporarily move mobile coverage, run script, verify graceful degradation

    Document coverage percentages for each platform in verification report.
  </action>
  <verify>python backend/tests/scripts/aggregate_coverage.py --format text | grep -E "(Platform|Overall|Mobile)"</verify>
  <done>Mobile coverage successfully aggregated with backend and frontend</done>
</task>

<task type="auto">
  <name>Verify mobile test coverage and pass rate</name>
  <files>mobile/coverage/coverage-final.json</files>
  <action>
    Collect mobile test metrics:

    1. Run mobile tests with coverage: cd mobile && npm run test:coverage
    2. Check test pass rate: grep -E "Tests:.*passed" mobile/coverage/lcov-report/index.html or use jest output
    3. Check coverage percentage: cat mobile/coverage/coverage-summary.json | jq .total.lines.pct
    4. Count total tests: cd mobile && npm test 2>&1 | grep -E "tests?|suites?" | tail -5
    5. List all new test files created: find mobile/src/__tests__ -name "*.test.ts" -newer mobile/package.json

    Document:
    - Total test count
    - Pass rate (target: 98%+)
    - Coverage percentage (target: 80%+)
    - New test files created
  </action>
  <verify>cd mobile && npm test 2>&1 | tail -20</verify>
  <done>Mobile test metrics collected and documented</done>
</task>

<task type="auto">
  <name>Verify CI workflow configuration</name>
  <files>.github/workflows/mobile-tests.yml</files>
  <action>
    Verify mobile CI workflow is configured correctly:

    1. Check workflow file exists: ls -la .github/workflows/mobile-tests.yml
    2. Verify workflow triggers: grep -E "(push|pull_request)" .github/workflows/mobile-tests.yml
    3. Verify test command: grep -E "npm.*test" .github/workflows/mobile-tests.yml
    4. Verify coverage upload: grep -A5 "upload-artifact" .github/workflows/mobile-tests.yml | grep coverage
    5. Verify unified workflow includes mobile: grep -E "mobile|needs.*test" .github/workflows/unified-tests.yml

    Document:
    - Workflow trigger configuration
    - Test execution time estimates
    - Artifact upload configuration
    - Integration with unified workflow
  </action>
  <verify>yamllint .github/workflows/mobile-tests.yml 2>&1 || cat .github/workflows/mobile-tests.yml | python -c "import yaml; yaml.safe_load_all(open(0))"</verify>
  <done>CI workflow verified and documented</done>
</task>

<task type="auto">
  <name>Verify all requirements satisfied</name>
  <files>.planning/phases/096-mobile-integration/096-VERIFICATION.md</files>
  <action>
    Create verification report for Phase 096:

    For each requirement (MOBL-01 to MOBL-04):
    1. **MOBL-01: Device feature mocking**
       - Verify biometricService.test.ts exists and tests Expo module mocks
       - Verify notificationService.test.ts exists and tests Expo module mocks
       - Verify camera, location, notification permissions tested
       - Document test count and coverage

    2. **MOBL-02: Offline data sync**
       - Verify offlineSyncNetwork.integration.test.ts exists
       - Verify sync on reconnect tested
       - Verify conflict resolution tested
       - Verify queue persistence tested

    3. **MOBL-03: Platform permissions & auth**
       - Verify devicePermissions.integration.test.ts exists
       - Verify iOS/Android permission flows tested
       - Verify biometric auth tested
       - Verify credential storage tested

    4. **MOBL-04: Cross-platform consistency** (partial)
       - Verify apiContracts.test.ts exists
       - Verify featureParity.test.ts exists
       - Verify shared API contracts validated
       - Document any feature gaps found

    For each success criterion from ROADMAP.md:
    1. Mobile integration tests cover device features - verified
    2. Offline sync tests verify queue, sync, conflicts - verified
    3. Platform permissions & auth tests complete - verified
    4. FastCheck property tests exist (5-10 properties) - verified
    5. Mobile tests workflow configured - verified
    6. Cross-platform consistency tests exist - verified

    Create VERIFICATION.md with:
    - Requirements satisfaction table
    - Success criteria validation
    - Test metrics (count, pass rate, coverage)
    - Discovered issues or gaps
    - Recommendations for Phase 097
  </action>
  <verify>cat .planning/phases/096-mobile-integration/096-VERIFICATION.md | wc -l</verify>
  <done>Verification report created with all criteria validated</done>
</task>

<task type="auto">
  <name>Create final phase summary</name>
  <files>.planning/phases/096-mobile-integration/096-FINAL-VERIFICATION.md</files>
  <action>
    Create final summary document with:

    1. **Phase Overview**:
       - 7 plans executed across 4 waves
       - Total duration estimate vs actual
       - Requirements completed (MOBL-01 to MOBL-04)

    2. **Deliverables**:
       - Test files created (with counts)
       - Coverage aggregation extended for mobile
       - CI workflow configured
       - Property tests implemented

    3. **Metrics**:
       - Total tests added
       - Test pass rate
       - Coverage percentage
       - Property test count

    4. **Infrastructure Ready**:
       - jest-expo configuration validated
       - Expo module mocks verified
       - CI/CD integration complete
       - Coverage aggregation operational

    5. **Lessons Learned**:
       - What worked well (existing jest.setup.js, test patterns)
       - What could be improved
       - Recommendations for Phase 097

    6. **Next Steps**:
       - Phase 097: Desktop Testing
       - Any outstanding work deferred to Phase 098
  </action>
  <verify>cat .planning/phases/096-mobile-integration/096-FINAL-VERIFICATION.md | grep -E "(Tests|Coverage|Requirements)"</verify>
  <done>Final phase summary created</done>
</task>

</tasks>

<verification>
After completion:
1. Read 096-VERIFICATION.md and verify all 6 success criteria addressed
2. Verify MOBL-01 to MOBL-04 requirements marked complete
3. Check coverage metrics documented
4. Verify all 7 plan summaries referenced
5. Confirm Phase 097 readiness documented
</verification>

<success_criteria>
1. 096-VERIFICATION.md exists with all criteria validated
2. 096-FINAL-VERIFICATION.md exists with phase summary
3. MOBL-01 to MOBL-04 marked complete
4. Test metrics documented (count, pass rate, coverage)
5. Phase 097 readiness confirmed
</success_criteria>

<output>
After completion, `.planning/phases/096-mobile-integration/096-FINAL-VERIFICATION.md` contains:
- Complete phase overview
- All deliverables listed
- Test metrics and coverage
- Infrastructure status
- Lessons learned
- Next steps for Phase 097
</output>
