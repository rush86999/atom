---
phase: 096-mobile-integration
plan: 06
type: execute
wave: 3
depends_on:
  - 096-02
files_modified:
  - mobile/src/__tests__/cross-platform/apiContracts.test.ts
  - mobile/src/__tests__/cross-platform/featureParity.test.ts
autonomous: true

must_haves:
  truths:
    - "API contract tests validate shared types between mobile and backend"
    - "Feature parity tests verify mobile supports all web features"
    - "Tests catch breaking changes in shared API contracts"
    - "Canvas type parity verified between web and mobile"
  artifacts:
    - path: mobile/src/__tests__/cross-platform/apiContracts.test.ts
      provides: API contract validation tests
      min_lines: 150
    - path: mobile/src/__tests__/cross-platform/featureParity.test.ts
      provides: Feature parity tests between web and mobile
      min_lines: 120
  key_links:
    - from: apiContracts.test.ts
      to: backend/api/
      via: Shared API contracts
      pattern: agent.*canvas.*workflow
    - from: apiContracts.test.ts
      to: frontend-nextjs/src/types/
      via: Shared TypeScript types
      pattern: AgentMessage|CanvasState
---

<objective>
Create cross-platform consistency tests for API contracts and feature parity.

**Purpose**: Verify that mobile and web implementations have identical behavior for shared features (agent chat, canvas presentations, workflows). Tests validate API contracts and ensure mobile supports all web features.

**Output**: Two cross-platform test files covering API contract validation and feature parity.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
</execution_context>

<context>
@mobile/src/services/agentService.ts
@mobile/src/services/canvasService.ts
@mobile/src/services/workflowService.ts
@mobile/src/types/agent.ts
@mobile/src/types/canvas.ts
@frontend-nextjs/src/types/
@backend/api/
@.planning/phases/096-mobile-integration/096-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create API contract validation tests</name>
  <files>mobile/src/__tests__/cross-platform/apiContracts.test.ts</files>
  <action>
    Create mobile/src/__tests__/cross-platform/apiContracts.test.ts:

    **Import services and types:**
    ```typescript
    import { apiService } from '../../services/api';
    import { agentService } from '../../services/agentService';
    import { canvasService } from '../../services/canvasService';
    import { workflowService } from '../../services/workflowService';
    // Import shared types if available, or define expected shapes
    ```

    **Test suites:**

    1. **Agent Message API Contract** (3-5 tests):
       - Should send agent messages matching backend schema
       - Should receive agent responses with expected fields (message_id, status, timestamp)
       - Should handle streaming response format
       - Should validate message types (text, canvas, workflow)
       - Should include required metadata (agent_id, session_id, platform)

    2. **Canvas State API Contract** (3-5 tests):
       - Should serialize canvas state matching backend schema
       - Should support all canvas types (generic, docs, email, sheets, orchestration, terminal, coding)
       - Should include required canvas fields (id, type, data, metadata)
       - Should handle canvas update requests
       - Should validate canvas presentation responses

    3. **Workflow API Contract** (3-5 tests):
       - Should trigger workflow matching backend schema
       - Should receive workflow execution status
       - Should validate workflow input/output formats
       - Should handle workflow list requests
       - Should include required workflow metadata

    4. **Request/Response Validation** (3-4 tests):
       - Should handle authentication headers consistently
       - Should include platform identifier in requests
       - Should parse error responses consistently
       - Should handle rate limit responses

    5. **Data Structure Consistency** (2-3 tests):
       - Agent message shape matches web implementation
       - Canvas state shape matches web implementation
       - Workflow trigger shape matches web implementation

    Use mock API responses that match backend schema. Validate structure using TypeScript type checks or runtime validators.
  </action>
  <verify>cd mobile && npm test -- apiContracts.test.ts</verify>
  <done>apiContracts.test.ts with 15-20 contract validation tests</done>
</task>

<task type="auto">
  <name>Create feature parity tests</name>
  <files>mobile/src/__tests__/cross-platform/featureParity.test.ts</files>
  <action>
    Create mobile/src/__tests__/cross-platform/featureParity.test.ts:

    **Import services:**
    ```typescript
    import { agentService } from '../../services/agentService';
    import { canvasService } from '../../services/canvasService';
    import { workflowService } from '../../services/workflowService';
    import { notificationService } from '../../services/notificationService';
    ```

    **Test suites:**

    1. **Agent Chat Feature Parity** (3-5 tests):
       - Should support all web agent chat features (streaming, history, feedback, canvas)
       - Should support same agent message types as web
       - Should support same agent configuration options
       - Should match web agent execution capabilities

    2. **Canvas Feature Parity** (3-5 tests):
       - Should support all 7 web canvas types
       - Should support same canvas interaction modes
       - Should support same canvas update mechanisms
       - Should match web canvas presentation capabilities

    3. **Workflow Feature Parity** (2-4 tests):
       - Should support web workflow trigger features
       - Should support same workflow execution modes
       - Should match web workflow monitoring capabilities

    4. **Notification Feature Parity** (2-3 tests):
       - Should support same notification types as web
       - Should support same notification scheduling
       - Should match web badge management

    5. **Device-Specific Features** (2-3 tests):
       - Should expose mobile-specific features (camera, location, biometric)
       - Should document mobile-only capabilities
       - Should fallback gracefully for web-only features

    Define expected feature sets as arrays, check mobile services support all expected features.
  </action>
  <verify>cd mobile && npm test -- featureParity.test.ts</verify>
  <done>featureParity.test.ts with 12-20 feature parity tests</done>
</task>

</tasks>

<verification>
After completion:
1. Run `cd mobile && npm test -- apiContracts.test.ts` - all tests pass
2. Run `cd mobile && npm test -- featureParity.test.ts` - all tests pass
3. Verify API contracts match actual backend schema (check backend API routes)
4. Verify feature sets match web implementation (check frontend services)
5. Run all tests to ensure no regressions
</verification>

<success_criteria>
1. apiContracts.test.ts exists with 15-20 tests
2. featureParity.test.ts exists with 12-20 tests
3. All contract tests validate against known backend schema
4. All feature parity tests verify mobile supports web features
5. Tests document any intentional feature differences
</success_criteria>

<output>
After completion, create `.planning/phases/096-mobile-integration/096-06-SUMMARY.md` with:
- API contract validation patterns
- Feature parity checklist (mobile vs web)
- Any discovered contract mismatches
- Recommendations for maintaining parity
</output>
