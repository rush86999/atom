---
phase: 096-mobile-integration
plan: 05
type: execute
wave: 3
depends_on:
  - 096-02
  - 096-04
files_modified:
  - mobile/src/__tests__/property/queueInvariants.test.ts
  - mobile/package.json
autonomous: true

must_haves:
  truths:
    - "FastCheck property tests validate queue ordering invariant (priority-based)"
    - "FastCheck property tests validate sync idempotency invariant"
    - "FastCheck property tests validate queue size limit invariant"
    - "FastCheck package added to mobile devDependencies"
  artifacts:
    - path: mobile/src/__tests__/property/queueInvariants.test.ts
      provides: Property-based tests for offline queue invariants
      min_lines: 150
    - path: mobile/package.json
      provides: FastCheck dependency for property testing
      contains: fast-check
  key_links:
    - from: queueInvariants.test.ts
      to: fast-check
      via: fc.property, fc.assert
      pattern: fc\.(property|assert)
    - from: queueInvariants.test.ts
      to: backend/tests/property_tests/governance/test_governance_maturity_invariants.py
      via: Hypothesis patterns mirrored in FastCheck
      pattern: invariant.*property
---

<objective>
Create FastCheck property tests for mobile offline queue invariants.

**Purpose**: Use property-based testing to validate critical invariants of the offline queue system (ordering, idempotency, size limits). This catches edge cases that example-based tests miss, following patterns proven in backend Hypothesis tests.

**Output**: FastCheck property test file for queue invariants, with fast-check added to mobile devDependencies.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
</execution_context>

<context>
@backend/tests/property_tests/governance/test_governance_maturity_invariants.py
@mobile/src/__tests__/services/offlineSync.test.ts
@mobile/src/services/offlineSyncService.ts
@mobile/package.json
@.planning/phases/096-mobile-integration/096-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Add FastCheck dependency to mobile</name>
  <files>mobile/package.json</files>
  <action>
    Add fast-check to mobile devDependencies:

    1. Run: cd mobile && npm install --save-dev fast-check
    2. Verify fast-check appears in package.json devDependencies
    3. Check version: fast-check@^4.5.3 (same as frontend)

    FastCheck is the JavaScript/TypeScript equivalent of Python's Hypothesis for property-based testing.
  </action>
  <verify>grep fast-check mobile/package.json</verify>
  <done>fast-check added to mobile devDependencies</done>
</task>

<task type="auto">
  <name>Create queue invariants property tests</name>
  <files>mobile/src/__tests__/property/queueInvariants.test.ts</files>
  <action>
    Create mobile/src/__tests__/property/queueInvariants.test.ts with FastCheck properties:

    **Import and setup:**
    ```typescript
    import fc from 'fast-check';
    import { offlineSyncService, OfflineAction } from '../../services/offlineSyncService';
    ```

    **Property tests:**

    1. **Queue Ordering Invariant** (2-3 properties):
       - Property: Higher priority actions should appear before lower priority
       - Property: For equal priority, earlier created actions should appear first
       - Use: fc.array of action records with fc.integer for priority (1-10), fc.integer for created_at
       - Settings: { numRuns: 100 }

    2. **Sync Idempotency Invariant** (1-2 properties):
       - Property: Syncing the same action twice should produce same result
       - Use: fc.record with id (fc.uuid()), type (fc.constantFrom), payload (fc.object())
       - Settings: { numRuns: 50 } (IO-bound, fewer runs)

    3. **Queue Size Limit Invariant** (1 property):
       - Property: Queue size should never exceed max (1000)
       - Use: fc.integer for count (1001-2000)
       - Settings: { numRuns: 10 } (expensive, fewer runs)

    4. **Priority Sum Invariant** (1-2 properties):
       - Property: Sum of priorities in sorted queue should equal sum in unsorted queue
       - Use: fc.array of actions with priority weights
       - Settings: { numRuns: 100 }

    5. **Retry Count Invariant** (1-2 properties):
       - Property: Retry count should never exceed max_retries (5)
       - Use: fc.integer for initial retries (0-4), mock sync failures
       - Settings: { numRuns: 50 }

    Follow patterns from backend Hypothesis tests (test_governance_maturity_invariants.py) but adapted to TypeScript/FastCheck syntax.

    Each property should have:
    - fc.assert(fc.property(..., assertion))
    - Clear docstring explaining the invariant
    - Generator strategies appropriate for the data type
    - numRuns setting (100 for fast, 50 for IO-bound)
  </action>
  <verify>cd mobile && npm test -- queueInvariants.test.ts</verify>
  <done>queueInvariants.test.ts with 5-10 FastCheck properties</done>
</task>

</tasks>

<verification>
After completion:
1. Run `cd mobile && npm test -- queueInvariants.test.ts` - all properties pass
2. Verify FastCheck is in package.json devDependencies
3. Check properties run with expected numRuns
4. Verify property tests find at least one edge case (if shrinking works)
5. Run all tests to ensure no regressions
</verification>

<success_criteria>
1. fast-check dependency installed in mobile
2. queueInvariants.test.ts exists with 5-10 properties
3. All property tests pass with fc.assert
4. Each property has clear invariant docstring
5. numRuns configured appropriately per property
6. Tests mirror backend Hypothesis patterns
</success_criteria>

<output>
After completion, create `.planning/phases/096-mobile-integration/096-05-SUMMARY.md` with:
- FastCheck installation details
- Property test list with invariants documented
- Generator strategies used
- numRuns settings and rationale
- Any counterexamples found during development
</output>
