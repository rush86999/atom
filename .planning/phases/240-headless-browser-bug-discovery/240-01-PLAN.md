---
phase: 240-headless-browser-bug-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/browser_discovery/test_console_errors.py
  - backend/tests/browser_discovery/test_accessibility.py
autonomous: true

must_haves:
  truths:
    - "Console error detection captures JavaScript errors and unhandled exceptions"
    - "Accessibility violation detection uses axe-core for WCAG 2.1 AA compliance"
    - "Tests use existing console_monitor and accessibility_checker fixtures from conftest.py"
    - "All console errors are captured with timestamp, URL, and location metadata"
    - "WCAG 2.1 AA violations include impact level, description, and remediation guidance"
  artifacts:
    - path: "backend/tests/browser_discovery/test_console_errors.py"
      provides: "Console error detection tests (BROWSER-02)"
      min_lines: 100
      contains: "console_monitor, assert_no_console_errors"
    - path: "backend/tests/browser_discovery/test_accessibility.py"
      provides: "Accessibility violation detection tests (BROWSER-03)"
      min_lines: 100
      contains: "accessibility_checker, assert_accessibility, wcag2a, wcag2aa, wcag21a, wcag21aa"
  key_links:
    - from: "test_console_errors.py"
      to: "conftest.py"
      via: "import console_monitor, assert_no_console_errors fixtures"
      pattern: "from.*conftest.*import.*console_monitor"
    - from: "test_accessibility.py"
      to: "conftest.py"
      via: "import accessibility_checker, assert_accessibility fixtures"
      pattern: "from.*conftest.*import.*accessibility_checker"
---

<objective>
Create console error detection and accessibility violation detection tests for browser bug discovery.

Purpose: JavaScript console errors and accessibility violations are common UI bugs that degrade user experience. Detecting them automatically through headless browser testing enables early bug discovery before production deployment.

Output: Two test files with 15+ tests covering console error detection (BROWSER-02) and accessibility violations (BROWSER-03) using existing fixtures.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/240-headless-browser-bug-discovery/240-RESEARCH.md
@backend/tests/browser_discovery/conftest.py
@backend/tests/e2e_ui/fixtures/auth_fixtures.py
</context>

<tasks>

<task type="auto">
  <name>Create console error detection tests</name>
  <files>backend/tests/browser_discovery/test_console_errors.py</files>
  <action>
Create test_console_errors.py with console error detection tests:

1. Import fixtures from existing conftest.py:
   ```python
   from tests.browser_discovery.conftest import (
       authenticated_page, console_monitor, assert_no_console_errors
   )
   ```

2. Create test class TestConsoleErrors with methods:
   - test_no_console_errors_on_dashboard() - Navigate to /dashboard, assert no console errors
   - test_no_console_errors_on_agents_list() - Navigate to /agents, assert no console errors
   - test_no_console_errors_on_agent_creation() - Navigate to /agents/new, assert no console errors
   - test_no_console_errors_on_canvas_list() - Navigate to /canvas, assert no console errors
   - test_no_console_errors_on_workflows_list() - Navigate to /workflows, assert no console errors
   - test_console_error_captures_metadata() - Verify console errors include timestamp, URL, location
   - test_console_warnings_logged_but_not_failed() - Warnings should be captured but not fail tests

3. Each test should:
   - Use authenticated_page fixture (API-first auth from conftest.py)
   - Navigate to page using page.goto(f"{base_url}/path")
   - Wait for load state: page.wait_for_load_state("domcontentloaded")
   - Use assert_no_console_errors() fixture to verify no errors
   - Use pytest.mark.browser_discovery marker

4. File location: backend/tests/browser_discovery/test_console_errors.py
   - Target: ~100-120 lines
   - Tests: ~7-8 test methods

DO NOT:
- Recreate console_monitor fixture (import from conftest.py)
- Create new authentication logic (use authenticated_page from conftest.py)
- Use UI login for authentication (API-first is 10-100x faster)
  </action>
  <verify>
Run pytest to verify tests are discoverable and fixtures load:
```bash
cd backend && pytest tests/browser_discovery/test_console_errors.py -v --collect-only
```
Expected: 7-8 tests collected with browser_discovery marker
  </verify>
  <done>
Test file test_console_errors.py exists with 7+ tests using console_monitor and assert_no_console_errors fixtures from conftest.py. All tests follow pattern: navigate → wait → assert_no_console_errors().
  </done>
</task>

<task type="auto">
  <name>Create accessibility violation detection tests</name>
  <files>backend/tests/browser_discovery/test_accessibility.py</files>
  <action>
Create test_accessibility.py with WCAG 2.1 AA compliance tests:

1. Import fixtures from existing conftest.py:
   ```python
   from tests.browser_discovery.conftest import (
       authenticated_page, accessibility_checker, assert_accessibility
   )
   ```

2. Create test class TestAccessibility with methods:
   - test_dashboard_wcag_aa_compliance() - Navigate to /dashboard, assert no WCAG violations
   - test_agents_list_wcag_aa_compliance() - Navigate to /agents, assert no violations
   - test_agent_creation_form_wcag_aa() - Navigate to /agents/new, check form accessibility
   - test_canvas_list_wcag_aa_compliance() - Navigate to /canvas, assert no violations
   - test_workflows_list_wcag_aa_compliance() - Navigate to /workflows, assert no violations
   - test_accessibility_violation_metadata() - Verify violations include id, impact, description, help_url
   - test_accessibility_graceful_degradation() - Skip test if axe-core fails to load (network issues)

3. Each test should:
   - Use authenticated_page fixture (API-first auth)
   - Navigate to page using page.goto(f"{base_url}/path")
   - Use assert_accessibility() fixture to verify WCAG 2.1 AA compliance
   - Use pytest.mark.accessibility marker
   - Handle graceful degradation (skip if axe-core unavailable)

4. File location: backend/tests/browser_discovery/test_accessibility.py
   - Target: ~100-120 lines
   - Tests: ~7-8 test methods

DO NOT:
- Recreate accessibility_checker fixture (import from conftest.py)
- Create custom WCAG rule checks (use axe-core from fixture)
- Fail tests on moderate/minor violations (only critical/serious should fail)
  </action>
  <verify>
Run pytest to verify tests are discoverable:
```bash
cd backend && pytest tests/browser_discovery/test_accessibility.py -v --collect-only
```
Expected: 7-8 tests collected with accessibility marker
  </verify>
  <done>
Test file test_accessibility.py exists with 7+ tests using accessibility_checker and assert_accessibility fixtures. All tests check WCAG 2.1 AA compliance (wcag2a, wcag2aa, wcag21a, wcag21aa tags).
  </done>
</task>

</tasks>

<verification>
Run tests to verify they execute correctly with fixtures:
```bash
cd backend && pytest tests/browser_discovery/test_console_errors.py tests/browser_discovery/test_accessibility.py -v -m "browser_discovery or accessibility" --tb=short
```

Expected results:
- 14-16 total tests collected (7-8 per file)
- Fixtures load successfully from conftest.py
- authenticated_page bypasses UI login (JWT in localStorage)
- console_monitor captures JavaScript errors
- accessibility_checker injects axe-core 4.8.2
- Tests fail if actual bugs exist (this is expected - discovering bugs is the goal)
</verification>

<success_criteria>
1. test_console_errors.py created with 7+ tests using console_monitor fixture
2. test_accessibility.py created with 7+ tests using accessibility_checker fixture
3. Both files import fixtures from existing conftest.py (no duplication)
4. All tests use authenticated_page for API-first authentication (10-100x faster than UI login)
5. pytest --collect-only shows 14-16 tests with proper markers
6. Console error tests capture errors with timestamp, URL, location metadata
7. Accessibility tests verify WCAG 2.1 AA compliance with axe-core
</success_criteria>

<output>
After completion, create `.planning/phases/240-headless-browser-bug-discovery/240-01-SUMMARY.md`
</output>
