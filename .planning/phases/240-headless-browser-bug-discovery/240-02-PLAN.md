---
phase: 240-headless-browser-bug-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/browser_discovery/test_broken_links.py
  - backend/tests/browser_discovery/test_form_filling.py
autonomous: true

must_haves:
  truths:
    - "Broken link detection identifies 404 responses and redirect loops"
    - "Form filling tests edge cases (null bytes, XSS payloads, SQL injection)"
    - "Tests use existing broken_link_checker fixture from conftest.py"
    - "Form edge cases include null bytes, XSS payloads, SQL injection, unicode, massive strings"
    - "Link checking skips localhost in test environment"
  artifacts:
    - path: "backend/tests/browser_discovery/test_broken_links.py"
      provides: "Broken link detection tests (BROWSER-04)"
      min_lines: 80
      contains: "broken_link_checker, 404, redirect"
    - path: "backend/tests/browser_discovery/test_form_filling.py"
      provides: "Form filling edge case tests (BROWSER-06)"
      min_lines: 150
      contains: "null bytes, XSS payloads, SQL injection, unicode, massive strings"
  key_links:
    - from: "test_broken_links.py"
      to: "conftest.py"
      via: "import broken_link_checker fixture"
      pattern: "from.*conftest.*import.*broken_link_checker"
    - from: "test_form_filling.py"
      to: "conftest.py"
      via: "import authenticated_page fixture for form testing"
      pattern: "from.*conftest.*import.*authenticated_page"
---

<objective>
Create broken link detection and form filling edge case tests for browser bug discovery.

Purpose: Broken links and form input validation bugs are common security and UX issues. Automated detection of 404 responses, redirect loops, and edge case form handling (null bytes, XSS, SQL injection) prevents production bugs.

Output: Two test files with 12+ tests covering broken link detection (BROWSER-04) and form edge cases (BROWSER-06).
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/240-headless-browser-bug-discovery/240-RESEARCH.md
@backend/tests/browser_discovery/conftest.py
@backend/tests/e2e_ui/fixtures/auth_fixtures.py
</context>

<tasks>

<task type="auto">
  <name>Create broken link detection tests</name>
  <files>backend/tests/browser_discovery/test_broken_links.py</files>
  <action>
Create test_broken_links.py with broken link detection tests:

1. Import fixtures from existing conftest.py:
   ```python
   from tests.browser_discovery.conftest import (
       authenticated_page, broken_link_checker
   )
   ```

2. Create test class TestBrokenLinks with methods:
   - test_no_broken_links_on_dashboard() - Check all links on /dashboard
   - test_no_broken_links_on_agents_list() - Check all links on /agents
   - test_no_broken_links_on_canvas_list() - Check all links on /canvas
   - test_no_broken_links_on_workflows_list() - Check all links on /workflows
   - test_broken_link_includes_metadata() - Verify broken links include URL, text, status_code
   - test_link_checker_skips_localhost() - Verify localhost links are skipped in test env

3. Each test should:
   - Use authenticated_page fixture (API-first auth)
   - Navigate to page using page.goto(f"{base_url}/path")
   - Call broken_link_checker() to get list of broken links
   - Assert len(broken_links) == 0 with descriptive message
   - Use pytest.mark.broken_links marker

4. File location: backend/tests/browser_discovery/test_broken_links.py
   - Target: ~80-100 lines
   - Tests: ~6 test methods

DO NOT:
- Recreate broken_link_checker fixture (import from conftest.py)
- Check localhost/127.0.0.1 links (skip in test environment per fixture)
- Set timeout >5s on link checks (fixture handles this)
  </action>
  <verify>
Run pytest to verify tests are discoverable:
```bash
cd backend && pytest tests/browser_discovery/test_broken_links.py -v --collect-only
```
Expected: 6 tests collected with broken_links marker
  </verify>
  <done>
Test file test_broken_links.py exists with 6+ tests using broken_link_checker fixture from conftest.py. All tests check for 404 responses and skip localhost links.
  </done>
</task>

<task type="auto">
  <name>Create form filling edge case tests</name>
  <files>backend/tests/browser_discovery/test_form_filling.py</files>
  <action>
Create test_form_filling.py with form edge case security tests:

1. Import fixtures from existing conftest.py:
   ```python
   from tests.browser_discovery.conftest import authenticated_page, console_monitor
   ```

2. Create test class TestFormFilling with methods:
   - test_agent_form_handles_null_bytes() - Fill agent name with \x00, verify no crash
   - test_agent_form_sanitizes_xss_payloads() - Fill with <script>alert("XSS")</script>, verify sanitized
   - test_agent_form_sanitizes_xss_img_onerror() - Fill with <img src=x onerror=alert("XSS")>
   - test_agent_form_sanitizes_xss_double_quote() - Fill with "><script>alert(String.fromCharCode(88,83,83))</script>
   - test_agent_form_resists_sql_injection() - Fill with ' OR '1'='1, verify rejected/sanitized
   - test_agent_form_handles_unicode() - Fill with emoji (🎨), Chinese (你好), Arabic (مرحبا)
   - test_agent_form_handles_massive_input() - Fill with 10000 character string, verify handled gracefully
   - test_agent_form_handles_special_characters() - Fill with \n, \r, \t, \x1b

3. Each test should:
   - Use authenticated_page fixture (API-first auth)
   - Navigate to form page (e.g., /agents/new)
   - Fill form fields with edge case input
   - Submit form with page.click("button[type='submit']")
   - Verify no crash (body still visible) or appropriate validation error
   - Check console for errors using console_monitor
   - Use pytest.mark.browser_discovery marker

4. Edge case payloads from Phase 237 security patterns:
   - Null bytes: "agent\x00name\x00with\x00nulls"
   - XSS: '<script>alert("XSS")</script>', '<img src=x onerror=alert("XSS")>'
   - SQLi: "' OR '1'='1", "'; DROP TABLE agents; --"
   - Unicode: "🎨 Test Agent 你好 مرحبا"
   - Massive: "A" * 10000

5. File location: backend/tests/browser_discovery/test_form_filling.py
   - Target: ~150-200 lines
   - Tests: ~8 test methods

DO NOT:
- Create new form fixtures (use authenticated_page from conftest.py)
- Test only one edge case per test (each test should focus on one category)
- Skip console error checking (edge cases often trigger JavaScript errors)
  </action>
  <verify>
Run pytest to verify tests are discoverable:
```bash
cd backend && pytest tests/browser_discovery/test_form_filling.py -v --collect-only
```
Expected: 8 tests collected with browser_discovery marker
  </verify>
  <done>
Test file test_form_filling.py exists with 8+ tests covering null bytes, XSS payloads, SQL injection, unicode, massive strings, special characters. All tests verify no crash and check console for errors.
  </done>
</task>

</tasks>

<verification>
Run tests to verify they execute correctly:
```bash
cd backend && pytest tests/browser_discovery/test_broken_links.py tests/browser_discovery/test_form_filling.py -v -m "browser_discovery or broken_links" --tb=short
```

Expected results:
- 14 total tests collected (6 + 8)
- broken_link_checker finds all <a> elements and checks HTTP status codes
- Form tests submit edge case inputs and verify graceful handling
- Tests may fail if actual bugs exist (this is expected - bug discovery)
</verification>

<success_criteria>
1. test_broken_links.py created with 6+ tests using broken_link_checker fixture
2. test_form_filling.py created with 8+ tests covering all edge case categories
3. Both files import fixtures from existing conftest.py (no duplication)
4. Broken link tests check for 404s, 500s, and skip localhost
5. Form tests cover null bytes, XSS (4+ payloads), SQL injection, unicode, massive strings, special characters
6. All tests use authenticated_page for API-first authentication
7. pytest --collect-only shows 14 tests with proper markers
</success_criteria>

<output>
After completion, create `.planning/phases/240-headless-browser-bug-discovery/240-02-SUMMARY.md`
</output>
