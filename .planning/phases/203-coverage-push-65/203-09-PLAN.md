---
phase: 203-coverage-push-65
plan: 09
type: execute
wave: 3
depends_on: [04, 05, 06, 07, 08]
files_modified:
  - backend/tests/core/test_agent_social_layer_coverage.py
  - backend/tests/core/test_skill_registry_service_coverage.py
  - backend/tests/core/test_config_coverage.py
  - backend/core/agent_social_layer.py
  - backend/core/skill_registry_service.py
  - backend/core/config.py
autonomous: true

must_haves:
  truths:
    - agent_social_layer.py has 60%+ coverage
    - skill_registry_service.py has 70%+ coverage
    - config.py has 70%+ coverage
    - All three files are core infrastructure with achievable targets
  artifacts:
    - path: "backend/tests/core/test_agent_social_layer_coverage.py"
      provides: "Agent social layer tests"
      min_lines: 500
      exports: ["TestSocialInteractions", "TestAgentCollaboration"]
    - path: "backend/tests/core/test_skill_registry_service_coverage.py"
      provides: "Skill registry service tests"
      min_lines: 500
      exports: ["TestSkillRegistration", "TestSkillDiscovery"]
    - path: "backend/tests/core/test_config_coverage.py"
      provides: "Configuration management tests"
      min_lines: 400
      exports: ["TestConfigLoading", "TestEnvironmentVariables"]
  key_links:
    - from: "backend/tests/core/test_agent_social_layer_coverage.py"
      to: "backend/core/agent_social_layer.py"
      via: "from core.agent_social_layer import"
      pattern: "AgentSocialLayer"
    - from: "backend/tests/core/test_skill_registry_service_coverage.py"
      to: "backend/core/skill_registry_service.py"
      via: "from core.skill_registry_service import"
      pattern: "SkillRegistryService"
    - from: "backend/tests/core/test_config_coverage.py"
      to: "backend/core/config.py"
      via: "from core.config import"
      pattern: "Config"
---

<objective>
Achieve 60%+ coverage on agent_social_layer.py (379 statements), 70%+ on skill_registry_service.py (370 statements), and 70%+ on config.py (336 statements).

Purpose: These are MEDIUM complexity core infrastructure files. Targets are achievable because these files have clear interfaces (config loading, skill registration, social interactions) with limited complex orchestration.

Output: Three test files with 25-30 tests each covering the primary functionality of each module.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/202-coverage-push-60/202-PHASE-SUMMARY.md
@.planning/phases/203-coverage-push-65/203-01-PLAN.md
@.planning/phases/203-coverage-push-65/203-02-PLAN.md
@.planning/phases/203-coverage-push-65/203-03-PLAN.md
@.planning/phases/203-coverage-push-65/203-04-PLAN.md
@.planning/phases/203-coverage-push-65/203-05-PLAN.md
@.planning/phases/203-coverage-push-65/203-06-PLAN.md
@.planning/phases/203-coverage-push-65/203-07-PLAN.md
@.planning/phases/203-coverage-push-65/203-08-PLAN.md
@.planning/ROADMAP.md
@backend/core/agent_social_layer.py
@backend/core/skill_registry_service.py
@backend/core/config.py
@backend/tests/core/test_agent_social_layer_coverage.py
@backend/tests/core/test_skill_registry_service_coverage.py
@backend/tests/core/test_config_coverage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent social layer test file</name>
  <files>backend/tests/core/test_agent_social_layer_coverage.py</files>
  <action>
Create backend/tests/core/test_agent_social_layer_coverage.py:

```python
"""
Coverage tests for agent_social_layer.py.

Target: 60%+ coverage (379 statements, ~227 lines to cover)
Focus: Social interactions, agent collaboration, communication protocols
"""
import pytest
from unittest.mock import Mock, patch, AsyncMock
from datetime import datetime, timezone
from core.agent_social_layer import (
    AgentSocialLayer,
    SocialInteraction,
    CollaborationRequest,
    InteractionType,
    CollaborationStatus
)


class TestSocialLayerInitialization:
    """Test social layer initialization."""

    def test_layer_initialization(self):
        """Test social layer initializes correctly."""
        layer = AgentSocialLayer()
        assert layer is not None
        assert layer.interactions == []

    def test_layer_with_agent_id(self):
        """Test layer initialization with agent ID."""
        layer = AgentSocialLayer(agent_id="test-agent")
        assert layer.agent_id == "test-agent"

    def test_layer_with_db_session(self, db_session):
        """Test layer with database session."""
        layer = AgentSocialLayer(db_session=db_session)
        assert layer.db_session == db_session


class TestSocialInteractions:
    """Test agent social interaction management."""

    def test_create_interaction(self):
        """Test creating a social interaction."""
        layer = AgentSocialLayer()

        interaction = layer.create_interaction(
            from_agent="agent-1",
            to_agent="agent-2",
            interaction_type=InteractionType.REQUEST,
            content="Help needed with task"
        )

        assert interaction.id is not None
        assert interaction.from_agent == "agent-1"
        assert interaction.to_agent == "agent-2"

    def test_list_interactions_for_agent(self):
        """Test listing interactions for an agent."""
        layer = AgentSocialLayer()

        layer.create_interaction("agent-1", "agent-2", InteractionType.MESSAGE, "Hello")
        layer.create_interaction("agent-2", "agent-1", InteractionType.MESSAGE, "Hi back")

        interactions = layer.get_interactions("agent-1")
        assert len(interactions) >= 2

    def test_get_interaction_by_id(self):
        """Test getting specific interaction."""
        layer = AgentSocialLayer()

        interaction = layer.create_interaction("agent-1", "agent-2", InteractionType.MESSAGE, "Test")
        retrieved = layer.get_interaction(interaction.id)

        assert retrieved.id == interaction.id
        assert retrieved.content == "Test"

    def test_filter_interactions_by_type(self):
        """Test filtering interactions by type."""
        layer = AgentSocialLayer()

        layer.create_interaction("agent-1", "agent-2", InteractionType.MESSAGE, "Msg")
        layer.create_interaction("agent-1", "agent-2", InteractionType.REQUEST, "Request")

        messages = layer.get_interactions("agent-1", interaction_type=InteractionType.MESSAGE)
        assert len(messages) >= 1

    def test_get_interactions_in_time_range(self):
        """Test getting interactions within time range."""
        layer = AgentSocialLayer()

        now = datetime.now(timezone.utc)
        layer.create_interaction("agent-1", "agent-2", InteractionType.MESSAGE, "Test")

        start = now - timedelta(hours=1)
        end = now + timedelta(hours=1)

        interactions = layer.get_interactions_in_range("agent-1", start, end)
        assert len(interactions) >= 1


class TestAgentCollaboration:
    """Test agent collaboration workflows."""

    def test_create_collaboration_request(self):
        """Test creating collaboration request."""
        layer = AgentSocialLayer()

        request = layer.create_collaboration_request(
            from_agent="agent-1",
            to_agent="agent-2",
            task_id="task-123",
            description="Need help with data processing"
        )

        assert request.id is not None
        assert request.status == CollaborationStatus.PENDING
        assert request.task_id == "task-123"

    def test_accept_collaboration_request(self):
        """Test accepting collaboration request."""
        layer = AgentSocialLayer()

        request = layer.create_collaboration_request("agent-1", "agent-2", "task-1", "Help")
        accepted = layer.accept_request(request.id, "agent-2")

        assert accepted.status == CollaborationStatus.ACCEPTED
        assert accepted.accepted_at is not None

    def test_decline_collaboration_request(self):
        """Test declining collaboration request."""
        layer = AgentSocialLayer()

        request = layer.create_collaboration_request("agent-1", "agent-2", "task-1", "Help")
        declined = layer.decline_request(request.id, "agent-2", reason="Busy")

        assert declined.status == CollaborationStatus.DECLINED
        assert declined.declined_reason == "Busy"

    def test_list_pending_requests(self):
        """Test listing pending collaboration requests."""
        layer = AgentSocialLayer()

        layer.create_collaboration_request("agent-1", "agent-2", "task-1", "Help")
        layer.create_collaboration_request("agent-3", "agent-2", "task-2", "Assist")

        pending = layer.get_pending_requests("agent-2")
        assert len(pending) >= 2

    def test_complete_collaboration(self):
        """Test marking collaboration as complete."""
        layer = AgentSocialLayer()

        request = layer.create_collaboration_request("agent-1", "agent-2", "task-1", "Help")
        layer.accept_request(request.id, "agent-2")

        completed = layer.complete_collaboration(request.id, result="Task completed successfully")
        assert completed.status == CollaborationStatus.COMPLETED
        assert completed.result == "Task completed successfully"


class TestSocialProtocols:
    """Test social communication protocols."""

    def test_send_message(self):
        """Test sending message between agents."""
        layer = AgentSocialLayer()

        message = layer.send_message(
            from_agent="agent-1",
            to_agent="agent-2",
            content="Message content"
        )

        assert message.interaction_type == InteractionType.MESSAGE
        assert message.content == "Message content"

    def test_broadcast_message(self):
        """Test broadcasting message to multiple agents."""
        layer = AgentSocialLayer()

        messages = layer.broadcast_message(
            from_agent="agent-1",
            to_agents=["agent-2", "agent-3", "agent-4"],
            content="Team update"
        )

        assert len(messages) == 3

    def test_create_group_conversation(self):
        """Test creating group conversation."""
        layer = AgentSocialLayer()

        conversation = layer.create_conversation(
            participants=["agent-1", "agent-2", "agent-3"],
            topic="Project planning"
        )

        assert conversation.id is not None
        assert len(conversation.participants) == 3

    def test_add_message_to_conversation(self):
        """Test adding message to conversation."""
        layer = AgentSocialLayer()

        conversation = layer.create_conversation(["agent-1", "agent-2"], "Topic")
        message = layer.add_conversation_message(
            conversation_id=conversation.id,
            from_agent="agent-1",
            content="Message in conversation"
        )

        assert message.conversation_id == conversation.id

    def test_get_conversation_history(self):
        """Test getting conversation history."""
        layer = AgentSocialLayer()

        conversation = layer.create_conversation(["agent-1", "agent-2"], "Topic")
        layer.add_conversation_message(conversation.id, "agent-1", "Msg 1")
        layer.add_conversation_message(conversation.id, "agent-2", "Msg 2")

        history = layer.get_conversation_history(conversation.id)
        assert len(history) == 2


class TestSocialDiscovery:
    """Test agent discovery and social graph."""

    def test_discover_agents(self):
        """Test discovering available agents."""
        layer = AgentSocialLayer()

        agents = layer.discover_agents()
        assert isinstance(agents, list)

    def test_find_agents_by_capability(self):
        """Test finding agents with specific capability."""
        layer = AgentSocialLayer()

        agents = layer.find_agents_by_capability("data_processing")
        assert isinstance(agents, list)

    def test_get_agent_social_graph(self):
        """Test getting social connections for agent."""
        layer = AgentSocialLayer()

        layer.create_interaction("agent-1", "agent-2", InteractionType.MESSAGE, "Msg")
        layer.create_interaction("agent-1", "agent-3", InteractionType.MESSAGE, "Msg")

        graph = layer.get_social_graph("agent-1")
        assert len(graph["connections"]) >= 2

    def test_get_agent_recommendations(self):
        """Test getting agent collaboration recommendations."""
        layer = AgentSocialLayer()

        recommendations = layer.get_agent_recommendations("agent-1")
        assert isinstance(recommendations, list)


class TestErrorHandling:
    """Test error handling in social layer."""

    def test_handle_invalid_agent_id(self):
        """Test handling of invalid agent ID."""
        layer = AgentSocialLayer()

        with pytest.raises(ValueError):
            layer.get_interactions("nonexistent-agent")

    def test_handle_duplicate_request(self):
        """Test handling of duplicate collaboration request."""
        layer = AgentSocialLayer()

        layer.create_collaboration_request("agent-1", "agent-2", "task-1", "Help")

        with pytest.raises(ValueError):
            layer.create_collaboration_request("agent-1", "agent-2", "task-1", "Help")

    def test_handle_accept_by_wrong_agent(self):
        """Test handling of request accepted by wrong agent."""
        layer = AgentSocialLayer()

        request = layer.create_collaboration_request("agent-1", "agent-2", "task-1", "Help")

        with pytest.raises(ValueError):
            layer.accept_request(request.id, "agent-3")  # Not the recipient

    def test_handle_empty_message(self):
        """Test handling of empty message content."""
        layer = AgentSocialLayer()

        with pytest.raises(ValueError):
            layer.send_message("agent-1", "agent-2", "")
```
</action>
  <verify>
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend python3 -m pytest tests/core/test_agent_social_layer_coverage.py --collect-only -q | head -10
  </verify>
  <done>
    Test file created with 6 test classes
    ~25 tests covering social interactions, collaboration, protocols
    Error handling tests included
  </done>
</task>

<task type="auto">
  <name>Task 2: Create skill registry service test file</name>
  <files>backend/tests/core/test_skill_registry_service_coverage.py</files>
  <action>
Create backend/tests/core/test_skill_registry_service_coverage.py:

```python
"""
Coverage tests for skill_registry_service.py.

Target: 70%+ coverage (370 statements, ~259 lines to cover)
Focus: Skill registration, discovery, validation, lifecycle
"""
import pytest
from unittest.mock import Mock, patch
from datetime import datetime, timezone
from core.skill_registry_service import (
    SkillRegistryService,
    SkillDefinition,
    SkillStatus,
    SkillMetadata
)


class TestSkillRegistration:
    """Test skill registration functionality."""

    def test_register_skill(self):
        """Test registering a new skill."""
        service = SkillRegistryService()

        skill = service.register_skill(
            skill_id="test-skill",
            name="Test Skill",
            description="A test skill",
            version="1.0.0",
            entry_point="skills.test_skill:main"
        )

        assert skill.skill_id == "test-skill"
        assert skill.status == SkillStatus.REGISTERED

    def test_register_skill_with_metadata(self):
        """Test registering skill with metadata."""
        service = SkillRegistryService()

        skill = service.register_skill(
            skill_id="test-skill",
            name="Test Skill",
            description="Test",
            version="1.0.0",
            entry_point="skills.test:main",
            metadata={
                "author": "Test Author",
                "tags": ["test", "example"],
                "capabilities": ["data_processing"]
            }
        )

        assert skill.metadata["author"] == "Test Author"
        assert "test" in skill.metadata["tags"]

    def test_register_skill_duplicate(self):
        """Test handling duplicate skill registration."""
        service = SkillRegistryService()

        service.register_skill("test-skill", "Test", "Test", "1.0", "test:main")

        with pytest.raises(ValueError):
            service.register_skill("test-skill", "Test 2", "Test", "1.0", "test:main")

    def test_update_skill_definition(self):
        """Test updating skill definition."""
        service = SkillRegistryService()

        skill = service.register_skill("test-skill", "Test", "Test", "1.0", "test:main")
        updated = service.update_skill(
            skill_id="test-skill",
            description="Updated description",
            version="1.1.0"
        )

        assert updated.description == "Updated description"
        assert updated.version == "1.1.0"

    def test_unregister_skill(self):
        """Test unregistering a skill."""
        service = SkillRegistryService()

        skill = service.register_skill("test-skill", "Test", "Test", "1.0", "test:main")
        unregistered = service.unregister_skill("test-skill")

        assert unregistered.status == SkillStatus.UNREGISTERED

    def test_validate_skill_definition(self):
        """Test skill definition validation."""
        service = SkillRegistryService()

        # Valid definition
        valid = service.validate_skill({
            "skill_id": "test-skill",
            "name": "Test",
            "version": "1.0.0",
            "entry_point": "test:main"
        })
        assert valid["is_valid"] is True

        # Invalid definition
        invalid = service.validate_skill({
            "skill_id": "",  # Invalid empty ID
            "name": "Test"
        })
        assert invalid["is_valid"] is False


class TestSkillDiscovery:
    """Test skill discovery and querying."""

    def test_list_all_skills(self):
        """Test listing all registered skills."""
        service = SkillRegistryService()

        service.register_skill("skill-1", "Skill 1", "Test", "1.0", "test:main")
        service.register_skill("skill-2", "Skill 2", "Test", "1.0", "test:main")

        skills = service.list_skills()
        assert len(skills) >= 2

    def test_find_skill_by_id(self):
        """Test finding skill by ID."""
        service = SkillRegistryService()

        service.register_skill("test-skill", "Test", "Test", "1.0", "test:main")
        skill = service.get_skill("test-skill")

        assert skill is not None
        assert skill.skill_id == "test-skill"

    def test_find_skills_by_tag(self):
        """Test finding skills by tag."""
        service = SkillRegistryService()

        service.register_skill("skill-1", "Skill 1", "Test", "1.0", "test:main",
                               metadata={"tags": ["data", "processing"]})
        service.register_skill("skill-2", "Skill 2", "Test", "1.0", "test:main",
                               metadata={"tags": ["ui", "display"]})

        skills = service.find_skills_by_tag("data")
        assert len(skills) >= 1

    def test_find_skills_by_capability(self):
        """Test finding skills by capability."""
        service = SkillRegistryService()

        service.register_skill("skill-1", "Skill 1", "Test", "1.0", "test:main",
                               metadata={"capabilities": ["data_processing"]})

        skills = service.find_skills_by_capability("data_processing")
        assert len(skills) >= 1

    def test_search_skills_by_name(self):
        """Test searching skills by name."""
        service = SkillRegistryService()

        service.register_skill("data-processor", "Data Processor", "Test", "1.0", "test:main")
        service.register_skill("ui-renderer", "UI Renderer", "Test", "1.0", "test:main")

        results = service.search_skills("data")
        assert len(results) >= 1

    def test_get_skill_dependencies(self):
        """Test getting skill dependencies."""
        service = SkillRegistryService()

        service.register_skill("skill-1", "Skill 1", "Test", "1.0", "test:main",
                               dependencies=["common-lib"])

        deps = service.get_dependencies("skill-1")
        assert "common-lib" in deps


class TestSkillLifecycle:
    """Test skill lifecycle management."""

    def test_activate_skill(self):
        """Test activating a registered skill."""
        service = SkillRegistryService()

        skill = service.register_skill("test-skill", "Test", "Test", "1.0", "test:main")
        activated = service.activate_skill("test-skill")

        assert activated.status == SkillStatus.ACTIVE
        assert activated.activated_at is not None

    def test_deactivate_skill(self):
        """Test deactivating a skill."""
        service = SkillRegistryService()

        skill = service.register_skill("test-skill", "Test", "Test", "1.0", "test:main")
        service.activate_skill("test-skill")
        deactivated = service.deactivate_skill("test-skill")

        assert deactivated.status == SkillStatus.INACTIVE

    def test_get_active_skills(self):
        """Test getting all active skills."""
        service = SkillRegistryService()

        service.register_skill("skill-1", "Skill 1", "Test", "1.0", "test:main")
        service.register_skill("skill-2", "Skill 2", "Test", "1.0", "test:main")

        service.activate_skill("skill-1")
        service.activate_skill("skill-2")

        active = service.get_active_skills()
        assert len(active) >= 2

    def test_check_skill_compatibility(self):
        """Test checking skill compatibility with version."""
        service = SkillRegistryService()

        service.register_skill("test-skill", "Test", "Test", "2.0.0", "test:main")

        compatible = service.check_compatibility("test-skill", ">=1.0.0")
        assert compatible is True

        incompatible = service.check_compatibility("test-skill", ">=3.0.0")
        assert incompatible is False

    def test_get_skill_usage_stats(self):
        """Test getting skill usage statistics."""
        service = SkillRegistryService()

        service.register_skill("test-skill", "Test", "Test", "1.0", "test:main")
        service.record_usage("test-skill", "agent-1")
        service.record_usage("test-skill", "agent-2")

        stats = service.get_usage_stats("test-skill")
        assert stats["total_uses"] >= 2


class TestSkillValidation:
    """Test skill validation and security."""

    def test_validate_skill_entry_point(self):
        """Test skill entry point validation."""
        service = SkillRegistryService()

        # Valid entry point
        valid = service.validate_entry_point("skills.test:main")
        assert valid is True

        # Invalid entry point
        invalid = service.validate_entry_point("invalid-format")
        assert invalid is False

    def test_scan_skill_for_issues(self):
        """Test scanning skill for security issues."""
        service = SkillRegistryService()

        issues = service.scan_skill("test-skill", """
import os
def dangerous():
    os.system("rm -rf /")
""")

        assert len(issues) >= 1

    def test_validate_skill_permissions(self):
        """Test validating skill permissions."""
        service = SkillRegistryService()

        service.register_skill("test-skill", "Test", "Test", "1.0", "test:main",
                               required_permissions=["read_data"])

        perms = service.get_required_permissions("test-skill")
        assert "read_data" in perms

    def test_check_skill_safety(self):
        """Test checking if skill is safe to execute."""
        service = SkillRegistryService()

        safe_skill = service.register_skill("safe-skill", "Safe", "Test", "1.0", "test:main")

        safety = service.check_safety("safe-skill", agent_id="test-agent")
        assert safety["is_safe"] in [True, False]


class TestSkillStorage:
    """Test skill persistence and retrieval."""

    def test_save_skill_to_db(self, db_session):
        """Test saving skill to database."""
        service = SkillRegistryService(db_session=db_session)

        skill = service.register_skill("test-skill", "Test", "Test", "1.0", "test:main")
        saved = service.save_to_db(skill)

        assert saved is True

    def test_load_skill_from_db(self, db_session):
        """Test loading skill from database."""
        service = SkillRegistryService(db_session=db_session)

        service.register_skill("test-skill", "Test", "Test", "1.0", "test:main")
        service.save_to_db(service.get_skill("test-skill"))

        loaded = service.load_from_db("test-skill")
        assert loaded is not None
        assert loaded.skill_id == "test-skill"

    def test_export_skill_definition(self):
        """Test exporting skill definition."""
        service = SkillRegistryService()

        service.register_skill("test-skill", "Test", "Test", "1.0", "test:main",
                               metadata={"author": "Test"})

        exported = service.export_skill("test-skill", format="json")
        assert "skill_id" in exported
        assert "metadata" in exported

    def test_import_skill_definition(self):
        """Test importing skill definition."""
        service = SkillRegistryService()

        definition = {
            "skill_id": "imported-skill",
            "name": "Imported Skill",
            "description": "Imported",
            "version": "1.0.0",
            "entry_point": "test:main"
        }

        imported = service.import_skill(definition)
        assert imported.skill_id == "imported-skill"
```
</action>
  <verify>
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend python3 -m pytest tests/core/test_skill_registry_service_coverage.py --collect-only -q | head -10
  </verify>
  <done>
    Test file created with 6 test classes
    ~30 tests covering registration, discovery, lifecycle, validation
    Database persistence tests included
  </done>
</task>

<task type="auto">
  <name>Task 3: Create config test file</name>
  <files>backend/tests/core/test_config_coverage.py</files>
  <action>
Create backend/tests/core/test_config_coverage.py:

```python
"""
Coverage tests for config.py.

Target: 70%+ coverage (336 statements, ~235 lines to cover)
Focus: Config loading, environment variables, validation, defaults
"""
import pytest
from unittest.mock import patch, Mock
from pathlib import Path
import os
from core.config import Config, ConfigSource, ConfigError


class TestConfigLoading:
    """Test configuration loading from various sources."""

    def test_load_config_from_file(self):
        """Test loading configuration from file."""
        config = Config()

        with patch.object(config, '_read_config_file') as mock_read:
            mock_read.return_value = {
                "api_key": "test-key",
                "timeout": 30,
                "debug": True
            }

            loaded = config.load_from_file("config.yaml")
            assert loaded["api_key"] == "test-key"

    def test_load_config_from_env(self):
        """Test loading configuration from environment variables."""
        config = Config()

        with patch.dict(os.environ, {
            "ATOM_API_KEY": "env-key",
            "ATOM_TIMEOUT": "60",
            "ATOM_DEBUG": "true"
        }):
            loaded = config.load_from_env()
            assert loaded["api_key"] == "env-key"
            assert loaded["timeout"] == 60
            assert loaded["debug"] is True

    def test_load_config_with_defaults(self):
        """Test loading configuration with defaults."""
        config = Config()

        defaults = config.get_defaults()
        assert "api_key" in defaults or "timeout" in defaults

    def test_merge_config_sources(self):
        """Test merging configuration from multiple sources."""
        config = Config()

        with patch.object(config, '_read_config_file', return_value={"timeout": 30}):
            with patch.dict(os.environ, {"ATOM_API_KEY": "env-key"}):
                merged = config.merge_sources()
                assert merged["timeout"] == 30
                assert merged["api_key"] == "env-key"

    def test_load_config_priority(self):
        """Test config priority: env > file > defaults."""
        config = Config()

        defaults = {"timeout": 10, "api_key": "default-key"}
        file_config = {"timeout": 30}
        env_config = {"api_key": "env-key"}

        merged = config.merge_with_priority(
            defaults=defaults,
            file_config=file_config,
            env_config=env_config
        )

        assert merged["timeout"] == 30  # From file
        assert merged["api_key"] == "env-key"  # From env (higher priority)


class TestConfigValidation:
    """Test configuration validation."""

    def test_validate_valid_config(self):
        """Test validation of valid configuration."""
        config = Config()

        valid_config = {
            "api_key": "sk-test-key",
            "timeout": 30,
            "model": "gpt-4",
            "temperature": 0.7
        }

        result = config.validate(valid_config)
        assert result["is_valid"] is True

    def test_validate_invalid_config(self):
        """Test validation of invalid configuration."""
        config = Config()

        invalid_config = {
            "api_key": "",  # Invalid empty key
            "timeout": -1,  # Invalid negative timeout
            "temperature": 2.5  # Invalid > 1.0
        }

        result = config.validate(invalid_config)
        assert result["is_valid"] is False
        assert len(result["errors"]) > 0

    def test_validate_required_fields(self):
        """Test validation of required fields."""
        config = Config()

        # Missing required field
        incomplete_config = {"model": "gpt-4"}  # Missing api_key

        result = config.validate(incomplete_config)
        assert result["is_valid"] is False

    def test_validate_data_types(self):
        """Test validation of data types."""
        config = Config()

        # Type mismatch
        wrong_types = {
            "api_key": "sk-key",
            "timeout": "30",  # Should be int
            "debug": "true"  # Should be bool
        }

        result = config.validate(wrong_types, strict_types=True)
        assert result["is_valid"] is False


class TestEnvironmentVariables:
    """Test environment variable handling."""

    def test_get_env_var(self):
        """Test getting environment variable."""
        config = Config()

        with patch.dict(os.environ, {"ATOM_TEST_VAR": "test-value"}):
            value = config.get_env("TEST_VAR")
            assert value == "test-value"

    def test_get_env_var_with_default(self):
        """Test getting env var with default value."""
        config = Config()

        value = config.get_env("NONEXISTENT_VAR", default="default-value")
        assert value == "default-value"

    def test_get_env_var_typed(self):
        """Test getting typed environment variable."""
        config = Config()

        with patch.dict(os.environ, {
            "ATOM_TIMEOUT": "60",
            "ATOM_DEBUG": "true"
        }):
            timeout = config.get_env_int("TIMEOUT")
            debug = config.get_env_bool("DEBUG")

            assert timeout == 60
            assert debug is True

    def test_get_env_var_list(self):
        """Test getting list environment variable."""
        config = Config()

        with patch.dict(os.environ, {"ATOM_ENABLED_FEATURES": "feature1,feature2,feature3"}):
            features = config.get_env_list("ENABLED_FEATURES")
            assert len(features) == 3

    def test_get_required_env_var(self):
        """Test getting required environment variable."""
        config = Config()

        with patch.dict(os.environ, {"ATOM_REQUIRED_VAR": "value"}):
            value = config.get_required_env("REQUIRED_VAR")
            assert value == "value"

    def test_get_required_env_var_missing(self):
        """Test error when required env var is missing."""
        config = Config()

        with pytest.raises(ConfigError):
            config.get_required_env("MISSING_REQUIRED_VAR")


class TestConfigAccess:
    """Test configuration value access patterns."""

    def test_get_config_value(self):
        """Test getting configuration value."""
        config = Config()
        config.set("api_key", "test-key")

        assert config.get("api_key") == "test-key"

    def test_get_config_value_with_default(self):
        """Test getting config value with default."""
        config = Config()

        value = config.get("nonexistent_key", default="default")
        assert value == "default"

    def test_set_config_value(self):
        """Test setting configuration value."""
        config = Config()
        config.set("timeout", 60)

        assert config.get("timeout") == 60

    def test_get_nested_config_value(self):
        """Test getting nested configuration value."""
        config = Config()
        config.set("database.host", "localhost")
        config.set("database.port", 5432)

        assert config.get("database.host") == "localhost"
        assert config.get("database.port") == 5432

    def test_get_all_config(self):
        """Test getting all configuration."""
        config = Config()
        config.set("key1", "value1")
        config.set("key2", "value2")

        all_config = config.get_all()
        assert "key1" in all_config
        assert "key2" in all_config


class TestConfigReload:
    """Test configuration reload functionality."""

    def test_reload_config_from_file(self):
        """Test reloading configuration from file."""
        config = Config()
        config.load_from_file("config.yaml")

        # Modify file content
        with patch.object(config, '_read_config_file', return_value={"timeout": 60}):
            reloaded = config.reload()
            assert reloaded["timeout"] == 60

    def test_reload_preserves_overrides(self):
        """Test that programmatic overrides persist after reload."""
        config = Config()
        config.set("override_key", "override_value")

        with patch.object(config, '_read_config_file', return_value={"timeout": 30}):
            reloaded = config.reload()
            assert reloaded["override_key"] == "override_value"

    def test_watch_config_changes(self):
        """Test watching for config file changes."""
        config = Config()

        # Mock file watcher
        with patch.object(config, '_watch_file') as mock_watch:
            mock_watch.return_value = True
            watching = config.watch_file("config.yaml")
            assert watching is True


class TestConfigErrors:
    """Test configuration error handling."""

    def test_handle_missing_config_file(self):
        """Test handling of missing config file."""
        config = Config()

        with pytest.raises(ConfigError):
            config.load_from_file("nonexistent.yaml")

    def test_handle_malformed_config_file(self):
        """Test handling of malformed config file."""
        config = Config()

        with patch.object(config, '_read_config_file', side_effect=Exception("Parse error")):
            with pytest.raises(ConfigError):
                config.load_from_file("config.yaml")

    def test_handle_invalid_env_value(self):
        """Test handling of invalid environment variable value."""
        config = Config()

        with patch.dict(os.environ, {"ATOM_TIMEOUT": "not-a-number"}):
            with pytest.raises(ConfigError):
                config.get_env_int("TIMEOUT")

    def test_handle_validation_error(self):
        """Test handling validation errors."""
        config = Config()

        invalid_config = {"timeout": -100}  # Invalid negative timeout

        result = config.validate(invalid_config)
        assert result["is_valid"] is False
        assert "timeout" in str(result["errors"]).lower()
```
</action>
  <verify>
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend python3 -m pytest tests/core/test_config_coverage.py --collect-only -q | head -10
  </verify>
  <done>
    Test file created with 7 test classes
    ~30 tests covering loading, validation, env vars, access, errors
    Error handling tests included
  </done>
</task>

<task type="auto">
  <name>Task 4: Run tests and measure coverage</name>
  <files>backend/tests/core/test_agent_social_layer_coverage.py, backend/tests/core/test_skill_registry_service_coverage.py, backend/tests/core/test_config_coverage.py</files>
  <action>
Run all three test files with coverage measurement:

```bash
cd backend

# Run all tests with coverage
PYTHONPATH=. python3 -m pytest \
    tests/core/test_agent_social_layer_coverage.py \
    tests/core/test_skill_registry_service_coverage.py \
    tests/core/test_config_coverage.py \
    --cov=core/agent_social_layer \
    --cov=core/skill_registry_service \
    --cov=core/config \
    --cov-report=term-missing \
    --cov-report=coverage_wave3_plan9.json \
    -v

# Check coverage results
python3 << 'EOF'
import json

print("Wave 3 Plan 9 Coverage Results:")
print("=" * 60)

files_to_check = {
    "agent_social_layer.py": "60",
    "skill_registry_service.py": "70",
    "config.py": "70"
}

with open('coverage_wave3_plan9.json') as f:
    data = json.load(f)

for fname, target in files_to_check.items():
    for path, info in data['files'].items():
        if fname in path:
            summary = info['summary']
            pct = summary['percent_covered']
            print(f"{fname}: {pct:.2f}% (target: {target}%+)")
            if pct >= int(target):
                print("  SUCCESS: Target met")
            else:
                print(f"  Gap: {int(target) - pct:.1f} pp")
            break
EOF
```

Expected: agent_social_layer.py 60%+, skill_registry_service.py 70%+, config.py 70%+.
</action>
  <verify>
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend python3 -m pytest tests/core/test_agent_social_layer_coverage.py tests/core/test_skill_registry_service_coverage.py tests/core/test_config_coverage.py --cov=core/agent_social_layer --cov=core/skill_registry_service --cov=core/config --cov-report=json -q && python3 -c "import json; d=json.load(open('coverage.json')); files = [k for k in d['files'].keys() if 'agent_social_layer' in k or 'skill_registry_service' in k or ('config.py' in k and 'core' in k)]; [print(f'{f}: {d[\"files\"][f][\"summary\"][\"percent_covered\"]:.2f}%') for f in files]"
  </verify>
  <done>
    All three test files execute without errors
    agent_social_layer.py: 60%+ coverage
    skill_registry_service.py: 70%+ coverage
    config.py: 70%+ coverage
    Pass rate: 80%+ on achievable tests
    Total tests: ~85 across three files
  </done>
</task>

</tasks>

<verification>
Overall plan verification:

1. Test files created:
   - test_agent_social_layer_coverage.py
   - test_skill_registry_service_coverage.py
   - test_config_coverage.py
2. Test classes: 19 total (6 + 6 + 7)
3. Total tests: 85+ tests
4. Coverage achieved:
   - agent_social_layer.py: 60%+
   - skill_registry_service.py: 70%+
   - config.py: 70%+
5. Pass rate: 80%+ on achievable tests
6. All three files contribute to overall 65% target

Run verification:
```bash
cd backend
PYTHONPATH=. python3 -m pytest tests/core/test_agent_social_layer_coverage.py tests/core/test_skill_registry_service_coverage.py tests/core/test_config_coverage.py --collect-only -q | grep "test_" | wc -l
```
</verification>

<success_criteria>
1. Three test files created (500, 500, 400+ lines)
2. 85+ tests total across all files
3. agent_social_layer.py: 60%+ coverage
4. skill_registry_service.py: 70%+ coverage
5. config.py: 70%+ coverage
6. 80%+ pass rate on achievable tests
7. Coverage measured and documented
8. Combined contribution to overall target
</success_criteria>

<output>
After completion, create `.planning/phases/203-coverage-push-65/203-09-SUMMARY.md` with:
- Tests created (count, lines per file)
- Coverage achieved for each file
- Pass rate
- Files covered
- Combined contribution to overall target
</output>
