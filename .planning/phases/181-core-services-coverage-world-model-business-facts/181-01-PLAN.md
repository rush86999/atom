---
phase: 181-core-services-coverage-world-model-business-facts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_world_model.py
autonomous: true

must_haves:
  truths:
    - World Model Service core methods have 75%+ line coverage
    - Record experience tests cover success and error paths
    - Record business fact tests cover success and error paths
    - Update experience feedback tests cover confidence blending
    - Boost experience confidence tests cover capping at 1.0
    - Experience statistics tests cover aggregation and filtering
  artifacts:
    - path: backend/tests/test_world_model.py
      provides: World Model Service unit tests (core methods)
      min_lines: 1200
      exports: ["TestRecordExperience", "TestRecordBusinessFact", "TestUpdateExperienceFeedback", "TestBoostExperienceConfidence", "TestGetExperienceStatistics"]
  key_links:
    - from: backend/tests/test_world_model.py
      to: backend/core/agent_world_model.py
      via: pytest.mark.asyncio with AsyncMock/MagicMock patches
      pattern: "patch\\('core\\.agent_world_model\\.get_lancedb_handler'\\)"
---

<objective>
Expand World Model Service core methods test coverage to 75%+.

Purpose: Core World Model methods (record_experience, record_business_fact, update_experience_feedback, boost_experience_confidence, get_experience_statistics) need comprehensive error path and edge case testing. Current coverage is ~45%. Target is 75%+ line coverage.

Output: Expanded test_world_model.py with 50+ new tests covering error paths, edge cases, and all code branches.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/181-core-services-coverage-world-model-business-facts/181-RESEARCH.md
@backend/core/agent_world_model.py
@backend/tests/test_world_model.py (existing 1,511 lines)
@backend/tests/api/test_apar_routes_coverage.py (Phase 180 test pattern reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Record Experience Error Path Tests</name>
  <files>backend/tests/test_world_model.py</files>
  <action>
    Add TestRecordExperienceErrors test class with 8 tests:

    1. test_record_experience_lancedb_connection_failure - Mock db.add_document raises exception, verify returns False and logs error
    2. test_record_experience_with_none_metadata - Pass experience with None metadata values, verify graceful handling
    3. test_record_experience_with_empty_task_type - Pass empty string task_type, verify it's stored correctly
    4. test_record_experience_with_special_characters_in_text - Use emojis and unicode in input_summary, verify storage
    5. test_record_experience_with_thumbs_up_down_feedback - Test thumbs_up_down=True and False cases
    6. test_record_experience_with_star_rating - Test rating values 1-5
    7. test_record_experience_with_agent_execution_id - Test agent_execution_id linkage
    8. test_record_experience_with_feedback_type - Test all feedback_type values (correction, rating, approval, comment)

    Pattern from Phase 180: Use MagicMock for LanceDBHandler, patch at import location:
    with patch('core.agent_world_model.get_lancedb_handler') as mock_get_handler:
        mock_handler = MagicMock()
        mock_handler.add_document = Mock(side_effect=Exception("Connection failed"))
        mock_get_handler.return_value = mock_handler
        service = WorldModelService(workspace_id="test")
        service.db = mock_handler
  </action>
  <verify>pytest backend/tests/test_world_model.py::TestRecordExperienceErrors -v</verify>
  <done>All 8 error path tests passing, record_experience coverage at 75%+</done>
</task>

<task type="auto">
  <name>Task 2: Add Record Business Fact Error Path Tests</name>
  <files>backend/tests/test_world_model.py</files>
  <action>
    Add TestRecordBusinessFactErrors test class with 6 tests:

    1. test_record_business_fact_with_empty_citations - Pass empty list citations, verify storage succeeds
    2. test_record_business_fact_with_empty_fact_text - Pass empty string fact, verify storage succeeds
    3. test_record_business_fact_with_malformed_metadata - Pass metadata with non-serializable values, verify JSON handling
    4. test_record_business_fact_lancedb_add_failure - Mock db.add_document raises exception, verify returns False
    5. test_record_business_fact_with_all_verification_statuses - Test unverified, verified, outdated, deleted statuses
    6. test_record_business_fact_with_domain_metadata - Test domain field is stored in metadata

    Pattern from Phase 180: Use MagicMock for synchronous db.add_document method
  </action>
  <verify>pytest backend/tests/test_world_model.py::TestRecordBusinessFactErrors -v</verify>
  <done>All 6 error path tests passing, record_business_fact coverage at 75%+</done>
</task>

<task type="auto">
  <name>Task 3: Add Feedback and Confidence Tests</name>
  <files>backend/tests/test_world_model.py</files>
  <action>
    Add two test classes with 12 tests total:

    TestUpdateExperienceFeedbackErrors (6 tests):
    1. test_update_feedback_experience_not_found - Mock db.search returns empty, verify returns False
    2. test_update_feedback_lancedb_search_failure - Mock db.search raises exception, verify returns False
    3. test_update_feedback_with_extreme_negative_score - Test -1.0 score, verify confidence formula
    4. test_update_feedback_with_extreme_positive_score - Test 1.0 score, verify confidence formula
    5. test_update_feedback_confidence_formula_validation - Verify 60% old + 40% new blend formula
    6. test_update_feedback_with_feedback_notes - Test feedback_notes are added to enhanced_text

    TestBoostExperienceConfidenceErrors (6 tests):
    1. test_boost_confidence_experience_not_found - Mock db.search returns empty, verify returns False
    2. test_boost_confidence_with_zero_boost_amount - Test boost_amount=0.1 default, verify works
    3. test_boost_confidence_with_negative_boost_amount - Test negative boost, verify decreases confidence
    4. test_boost_confidence_at_max_already - Test existing confidence=1.0, verify capped at 1.0
    5. test_boost_confidence_boost_count_increment - Verify boost_count increments on each boost
    6. test_boost_confidence_last_boosted_at_set - Verify last_boosted_at timestamp is set

    Pattern from Phase 174: Use AsyncMock for async methods, MagicMock for sync methods
  </action>
  <verify>pytest backend/tests/test_world_model.py::TestUpdateExperienceFeedbackErrors backend/tests/test_world_model.py::TestBoostExperienceConfidenceErrors -v</verify>
  <done>All 12 tests passing, update_experience_feedback and boost_experience_confidence coverage at 75%+</done>
</task>

<task type="auto">
  <name>Task 4: Add Experience Statistics and Verification Tests</name>
  <files>backend/tests/test_world_model.py</files>
  <action>
    Add two test classes with 12 tests total:

    TestGetExperienceStatisticsErrors (6 tests):
    1. test_get_statistics_lancedb_search_failure - Mock db.search raises exception, verify returns error dict
    2. test_get_statistics_with_empty_results - Mock db.search returns empty list, verify all zeros
    3. test_get_statistics_with_malformed_metadata - Test results with missing metadata keys, verify default values
    4. test_get_statistics_case_insensitive_role_filtering - Test agent_role="Finance" matches "finance"
    5. test_get_statistics_agent_id_filtering - Test agent_id filter returns only matching experiences
    6. test_get_statistics_aggregation_calculations - Verify success_rate, avg_confidence, feedback_coverage formulas

    TestFactVerificationErrors (6 tests):
    1. test_update_fact_verification_not_found - Mock db.search returns empty, verify returns False
    2. test_update_fact_verification_lancedb_failure - Mock db.search raises exception, verify returns False
    3. test_update_fact_verification_with_deleted_status - Test status="deleted" update
    4. test_update_fact_verification_text_replacement - Verify text replacement from old status to new status
    5. test_get_fact_by_id_with_high_limit - Test limit=1000 for fact lookup
    6. test_list_all_facts_with_filters - Test status and domain filter combinations

    Pattern from Phase 180: Mock db.search to return structured dicts matching LanceDB response format
  </action>
  <verify>pytest backend/tests/test_world_model.py::TestGetExperienceStatisticsErrors backend/tests/test_world_model.py::TestFactVerificationErrors -v</verify>
  <done>All 12 tests passing, get_experience_statistics and fact verification coverage at 75%+</done>
</task>

</tasks>

<verification>
1. Run pytest backend/tests/test_world_model.py -v - verify 50+ new tests passing
2. Run pytest backend/tests/test_world_model.py --cov=core.agent_world_model --cov-report=term-missing - verify 75%+ line coverage
3. Run pytest backend/tests/test_world_model.py -k "TestRecordExperience or TestRecordBusinessFact or TestUpdate or TestBoost or TestGetStatistics or TestFactVerification" - verify specific test classes run
</verification>

<success_criteria>
- World Model Service (agent_world_model.py) achieves 75%+ line coverage
- 50+ new tests added covering all core method error paths
- All tests passing with 100% pass rate
- Test execution time <2 minutes
</success_criteria>

<output>
After completion, create `.planning/phases/181-core-services-coverage-world-model-business-facts/181-01-SUMMARY.md` with:
- Tests added count and test class names
- Coverage achieved per method (record_experience, record_business_fact, update_experience_feedback, boost_experience_confidence, get_experience_statistics)
- Any deviations from plan and reasons
</output>
