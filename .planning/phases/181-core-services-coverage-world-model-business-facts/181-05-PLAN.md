---
phase: 181-core-services-coverage-world-model-business-facts
plan: 05
type: execute
wave: 2
depends_on: ["181-01", "181-02", "181-03", "181-04"]
files_modified:
  - backend/tests/test_policy_fact_extractor.py
  - backend/tests/test_storage_service.py
autonomous: true

must_haves:
  truths:
    - Policy Fact Extractor has 60%+ line coverage
    - Storage Service has 80%+ line coverage
    - Extractor stub behavior is tested
    - S3 upload and check_exists are tested
  artifacts:
    - path: backend/tests/test_policy_fact_extractor.py
      provides: Policy Fact Extractor unit tests (NEW file)
      min_lines: 300
      exports: ["TestPolicyFactExtractorInit", "TestExtractFactsFromDocument", "TestGetPolicyFactExtractor"]
    - path: backend/tests/test_storage_service.py
      provides: Storage Service unit tests (NEW file)
      min_lines: 400
      exports: ["TestStorageServiceInit", "TestUploadFile", "TestCheckExists", "TestGetStorageService"]
  key_links:
    - from: backend/tests/test_policy_fact_extractor.py
      to: backend/core/policy_fact_extractor.py
      via: MagicMock for stub testing
      pattern: "patch\\('core\\.policy_fact_extractor"
    - from: backend/tests/test_storage_service.py
      to: backend/core/storage.py
      via: MagicMock for boto3 S3 client
      pattern: "patch\\('core\\.storage\\.boto3'"
---

<objective>
Create Policy Fact Extractor and Storage Service test coverage.

Purpose: Policy Fact Extractor (policy_fact_extractor.py) is a stub implementation (88 lines). Storage Service (storage.py) handles S3/R2 uploads and existence checks (69 lines). Current coverage is 0%. Target is 60%+ for extractor and 80%+ for storage.

Output: Two NEW test files with 25+ tests covering stub behavior validation and S3 operations.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/181-core-services-coverage-world-model-business-facts/181-RESEARCH.md
@backend/core/policy_fact_extractor.py (88 lines, stub)
@backend/core/storage.py (69 lines)
@backend/tests/test_world_model.py (existing test patterns reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Policy Fact Extractor Tests</name>
  <files>backend/tests/test_policy_fact_extractor.py</files>
  <action>
    Create NEW test file with two test classes (10 tests total):

    TestPolicyFactExtractorInit (3 tests):
    1. test_init_default_workspace - Verify PolicyFactExtractor() uses "default" workspace_id
    2. test_init_custom_workspace - Verify PolicyFactExtractor(workspace_id="test") uses custom value
    3. test_extractor_registry_returns_same_instance - Call get_policy_fact_extractor twice, verify same instance returned

    TestExtractFactsFromDocument (7 tests):
    1. test_extract_facts_returns_empty_list - Mock extractor returns empty, verify facts=[]
    2. test_extract_facts_returns_zero_extraction_time - Mock extractor returns extraction_time=0.0
    3. test_extract_facts_with_nonexistent_file - Pass path to non-existent file, verify doesn't crash (stub behavior)
    4. test_extract_facts_with_pdf_file - Pass .pdf file path, verify method called without error
    5. test_extract_facts_with_txt_file - Pass .txt file path, verify method called without error
    6. test_extract_facts_logs_warning - Verify logger.warning called for stub implementation
    7. test_extract_facts_returns_extraction_result_object - Verify returns ExtractionResult with facts and extraction_time

    Pattern from Phase 175: Use MagicMock for stub implementations, focus on interface validation
  </action>
  <verify>pytest backend/tests/test_policy_fact_extractor.py -v</verify>
  <done>All 10 tests passing, Policy Fact Extractor interface validated</done>
</task>

<task type="auto">
  <name>Task 2: Create Storage Service Tests</name>
  <files>backend/tests/test_storage_service.py</files>
  <action>
    Create NEW test file with three test classes (15 tests total):

    TestStorageServiceInit (3 tests):
    1. test_init_loads_s3_client - Mock boto3.client, verify S3 client created
    2. test_init_with_r2_credentials - Set env vars for R2, verify endpoint_url configured
    3. test_init_with_aws_credentials - Set env vars for AWS, verify uses default endpoint

    TestUploadFile (6 tests):
    1. test_upload_file_success - Mock upload_fileobj succeeds, verify returns s3:// URI
    2. test_upload_file_with_content_type - Pass content_type param, verify passed to S3
    3. test_upload_file_returns_s3_uri - Verify return format is s3://bucket/key
    4. test_upload_file_bucket_from_env - Set S3_BUCKET env var, verify used in URI
    5. test_upload_file_failure_raises_exception - Mock upload_fileobj raises exception, verify propagates
    6. test_upload_file_with_bytesio - Pass io.BytesIO, verify upload_fileobj called

    TestCheckExists (6 tests):
    1. test_check_exists_true - Mock head_object succeeds, verify returns True
    2. test_check_exists_false - Mock head_object raises 404, verify returns False
    3. test_check_exists_error_returns_false - Mock head_object raises other error, verify returns False
    4. test_check_exists_bucket_from_env - Set S3_BUCKET env var, verify used in call
    5. test_check_exists_with_key - Pass key param, verify head_object called with correct Key
    6. test_check_exists_client_error - Mock S3 client raises ClientError, verify handled

    Pattern from Phase 180: Use patch('core.storage.boto3.client') for S3 mocking
  </action>
  <verify>pytest backend/tests/test_storage_service.py -v</verify>
  <done>All 15 tests passing, Storage Service S3 operations covered</done>
</task>

</tasks>

<verification>
1. Run pytest backend/tests/test_policy_fact_extractor.py -v - verify 10 tests passing
2. Run pytest backend/tests/test_policy_fact_extractor.py --cov=core.policy_fact_extractor --cov-report=term-missing - verify 60%+ line coverage
3. Run pytest backend/tests/test_storage_service.py -v - verify 15 tests passing
4. Run pytest backend/tests/test_storage_service.py --cov=core.storage --cov-report=term-missing - verify 80%+ line coverage
</verification>

<success_criteria>
- Policy Fact Extractor achieves 60%+ line coverage (stub interface validated)
- Storage Service achieves 80%+ line coverage
- 25+ tests created across both files
- All tests passing with 100% pass rate
</success_criteria>

<output>
After completion, create `.planning/phases/181-core-services-coverage-world-model-business-facts/181-05-SUMMARY.md` with:
- Tests created count and test class names
- Coverage achieved for both services
- Any deviations from plan and reasons
</output>
