---
phase: 179-api-routes-coverage-ai-workflows
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_auto_install_routes_coverage.py
autonomous: true

must_haves:
  truths:
    - "Auto install routes (3 endpoints: install, batch, status) have 75%+ line coverage"
    - "POST /auto-install/install installs dependencies for skill with scan_for_vulnerabilities option"
    - "POST /auto-install/batch handles multiple skill installations in single request"
    - "GET /auto-install/status/{skill_id} checks if skill packages are installed (image exists)"
    - "Error paths tested: validation errors (422), 400 on install failure, package_type constraints"
    - "External AutoInstallerService properly mocked with AsyncMock for install operations"
    - "Database dependency (get_db) properly overridden for Session dependency"
  artifacts:
    - path: "backend/tests/api/test_auto_install_routes_coverage.py"
      provides: "Auto install routes test coverage (100 lines target, 3 endpoints)"
      min_lines: 350
      exports: ["TestAutoInstallSuccess", "TestAutoInstallBatch", "TestAutoInstallStatus", "TestAutoInstallErrorPaths"]
  key_links:
    - from: "test_auto_install_routes_coverage.py"
      to: "api/auto_install_routes.py"
      via: "TestClient with per-file FastAPI app"
      pattern: "TestClient.*router"
    - from: "test_auto_install_routes_coverage.py"
      to: "core.auto_installer_service.AutoInstallerService"
      via: "AsyncMock patch for install operations"
      pattern: "patch.*AutoInstallerService"
    - from: "test_auto_install_routes_coverage.py"
      to: "core.database.get_db"
      via: "Dependency override for database session"
      pattern: "dependency_overrides.*get_db"
---

<objective>
**Auto Install Routes Test Coverage**

Create comprehensive test coverage for auto install routes (auto_install_routes.py, 100 lines, 3 endpoints). Target 75%+ line coverage with TestClient-based integration tests.

**Purpose:** Ensure auto install endpoints (single install, batch install, status check) are thoroughly tested with proper mocking of AutoInstallerService and database dependencies.

**Output:** test_auto_install_routes_coverage.py (350+ lines, 12-15 tests) covering success paths, error paths, and edge cases.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/179-api-routes-coverage-ai-workflows/179-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Pattern References
@backend/tests/api/conftest.py (TestClient fixtures, mock patterns)
@backend/tests/api/test_admin_system_health_routes.py (database override pattern, Phase 178)

# Target Routes
@backend/api/auto_install_routes.py (100 lines, 3 endpoints)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Auto Install Routes Test File with Fixtures</name>
  <files>backend/tests/api/test_auto_install_routes_coverage.py</files>
  <action>
    Create test_auto_install_routes_coverage.py with the following structure:

    **Imports:** pytest, unittest.mock (AsyncMock, MagicMock, Mock, patch), fastapi.testclient.TestClient, typing

    **Fixtures (6-8 fixtures):**
    1. `mock_auto_installer()` - AsyncMock for AutoInstallerService with:
       - install_dependencies = AsyncMock(return_value={"success": True, "image_tag": "skill-123:python"})
       - batch_install = AsyncMock(return_value={"success": True, "installed": ["skill-1", "skill-2"]})
       - _get_image_tag = Mock(return_value="skill-123:python")
       - _image_exists = Mock(return_value=False)

    2. `mock_db_for_auto_install()` - Mock Session for get_db dependency

    3. `auto_install_client(mock_auto_installer, mock_db_for_auto_install)` - TestClient with:
       ```python
       from fastapi import FastAPI
       from api.auto_install_routes import router
       from core.database import get_db
       app = FastAPI()
       app.include_router(router)
       def override_get_db():
           yield mock_db_for_auto_install
       app.dependency_overrides[get_db] = override_get_db
       with patch('api.auto_install_routes.AutoInstallerService', return_value=mock_auto_installer):
           yield TestClient(app)
       app.dependency_overrides.clear()
       ```

    4. `sample_install_request()` - Factory for valid InstallRequest with skill_id/packages/package_type/agent_id/scan_for_vulnerabilities
    5. `sample_batch_install_request()` - Factory for valid BatchInstallRequest with installations list
    6. `install_success_response()` - Expected successful install response structure
    7. `batch_install_response()` - Expected batch install response structure

    **File header:** Module docstring describing coverage (100 lines, 3 endpoints, 75%+ target)
  </action>
  <verify>
    File created at backend/tests/api/test_auto_install_routes_coverage.py with 6+ fixtures
    auto_install_client fixture uses dependency override pattern for get_db
    mock_auto_installer has AsyncMock for async install methods
  </verify>
  <done>
    All fixtures created, test file structure ready for test classes
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Single Install Tests</name>
  <files>backend/tests/api/test_auto_install_routes_coverage.py</files>
  <action>
    Create `TestAutoInstallSuccess` class with 5-6 tests for single install endpoint:

    **Success Tests (3-4 tests):**
    1. `test_install_dependencies_python()` - POST /auto-install/install with python packages returns success with image_tag
    2. `test_install_dependencies_npm()` - Install with npm package_type works correctly
    3. `test_install_with_vulnerability_scan()` - scan_for_vulnerabilities=True includes security scan in result
    4. `test_install_multiple_packages()` - Installing multiple packages in single request succeeds

    **Validation Tests (2 tests):**
    5. `test_install_missing_skill_id()` - Missing skill_id returns 422 validation error
    6. `test_install_empty_packages()` - Empty packages list returns 422 (min_items=1 constraint)

    Use mock_auto_installer.install_dependencies.return_value to set success/failure scenarios.
  </action>
  <verify>
    TestAutoInstallSuccess class exists with 5-6 tests
    Tests verify response structure (success, image_tag, installed packages)
    Validation tests verify 422 status for missing required fields
  </verify>
  <done>
    Single install endpoint thoroughly tested
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Batch Install and Status Check Tests</name>
  <files>backend/tests/api/test_auto_install_routes_coverage.py</files>
  <action>
    Create `TestAutoInstallBatch` class with 4-5 tests for batch install:

    **Batch Install Tests (4-5 tests):**
    1. `test_batch_install_success()` - POST /auto-install/batch installs multiple skills successfully
    2. `test_batch_install_two_skills()` - Batch install with 2 different skills
    3. `test_batch_install_mixed_package_types()` - Batch with both python and npm packages
    4. `test_batch_install_empty()` - Empty installations list returns 422 (min_items=1 constraint)
    5. `test_batch_install_partial_failure()` - Batch with some successes and some failures

    Create `TestAutoInstallStatus` class with 3-4 tests for status endpoint:

    **Status Tests (3-4 tests):**
    1. `test_get_status_installed()` - GET /auto-install/status/{skill_id} returns installed=True when image exists
    2. `test_get_status_not_installed()` - Returns installed=False when image doesn't exist
    3. `test_get_status_npm_package()` - Status check with npm package_type
    4. `test_get_status_python_default()` - Status check defaults to python package_type

    Use mock_auto_installer._image_exists.return_value to control status check results.
  </action>
  <verify>
    TestAutoInstallBatch class exists with 4-5 tests
    TestAutoInstallStatus class exists with 3-4 tests
    Status tests verify installed boolean and image_tag in response
  </verify>
  <done>
    Batch install and status check endpoints tested
  </done>
</task>

<task type="auto">
  <name>Task 4: Add Error Path Tests</name>
  <files>backend/tests/api/test_auto_install_routes_coverage.py</files>
  <action>
    Create `TestAutoInstallErrorPaths` class with 4-5 tests:

    **Install Failure Tests (2 tests):**
    1. `test_install_failure_response()` - Install failure (success=False) returns 400 HTTPException with error details
    2. `test_install_service_error()` - AutoInstallerService exception results in error response

    **Validation Tests (2-3 tests):**
    3. `test_install_invalid_package_type()` - Invalid package_type (not python/npm) returns 422
    4. `test_batch_install_missing_agent_id()` - Batch install missing agent_id returns 422
    5. `test_status_skill_id_validation()` - Status check with malformed skill_id returns 422

    Use mock_auto_installer.install_dependencies.return_value = {"success": False, "error": "..."} to simulate failures.
    Use mock_auto_installer.install_dependencies.side_effect = Exception() for service errors.
  </action>
  <verify>
    TestAutoInstallErrorPaths class exists with 4-5 tests
    Tests cover 400 status for install failures
    Tests cover 422 status for validation errors
  </verify>
  <done>
    Error paths tested, API-03 requirements met
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify Coverage and Test Execution</name>
  <files>backend/tests/api/test_auto_install_routes_coverage.py</files>
  <action>
    **Verify Test Execution:**
    1. Run tests: `pytest backend/tests/api/test_auto_install_routes_coverage.py -v`
    2. Verify 12-15 tests pass (all test classes)
    3. Check no import errors or dependency conflicts

    **Verify Coverage:**
    1. Run coverage: `pytest backend/tests/api/test_auto_install_routes_coverage.py --cov=api/auto_install_routes --cov-report=term-missing`
    2. Verify 75%+ line coverage achieved
    3. Document any uncovered lines in SUMMARY

    **Update SUMMARY:**
    Create 179-03-SUMMARY.md with:
    - Test count and pass rate (target: 12-15 tests)
    - Coverage percentage (target: 75%+)
    - Test classes: Success, Batch, Status, ErrorPaths
    - Uncovered lines (if any)
    - Deviations from plan (if any)
  </action>
  <verify>
    pytest runs successfully with 12-15 passing tests
    Coverage report shows 75%+ for api/auto_install_routes.py
    179-03-SUMMARY.md created with coverage metrics
  </verify>
  <done>
    75%+ coverage achieved, all tests passing, SUMMARY documents coverage gaps
  </done>
</task>

</tasks>

<verification>
**Phase Verification:**
- [ ] Test file created with 350+ lines of test code
- [ ] 12-15 tests covering all 3 auto install endpoints
- [ ] Success paths tested: single install, batch install, status check
- [ ] Error paths tested: 400 install failures, 422 validation errors
- [ ] AutoInstallerService properly mocked with AsyncMock
- [ ] Database dependency (get_db) properly overridden
- [ ] Coverage measurement shows 75%+ for auto_install_routes.py
- [ ] SUMMARY.md documents results and any gaps
</verification>

<success_criteria>
**Phase 179 Plan 03 Complete When:**
1. test_auto_install_routes_coverage.py exists with 12-15 tests
2. All tests pass with pytest -v execution
3. Coverage report shows 75%+ line coverage for auto_install_routes.py (100 lines)
4. AutoInstallerService properly mocked with AsyncMock for async operations
5. Database dependency (get_db) properly overridden
6. API-03 requirement met: error paths (400, 422) tested
7. 179-03-SUMMARY.md documents coverage results
</success_criteria>

<output>
After completion, create `.planning/phases/179-api-routes-coverage-ai-workflows/179-03-SUMMARY.md`
</output>
