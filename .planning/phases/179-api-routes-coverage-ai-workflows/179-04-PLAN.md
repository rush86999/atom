---
phase: 179-api-routes-coverage-ai-workflows
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_workflow_analytics_routes_coverage.py
  - backend/tests/api/test_workflow_template_routes_coverage.py
autonomous: true

must_haves:
  truths:
    - "Workflow analytics routes (3 endpoints: analytics summary, recent executions, workflow stats) have 75%+ line coverage"
    - "GET /api/workflows/analytics returns execution summary with days parameter"
    - "GET /api/workflows/analytics/recent returns recent executions with limit parameter"
    - "GET /api/workflows/analytics/{workflow_id} returns stats for specific workflow"
    - "Workflow template routes (8 endpoints) enhanced with additional coverage to reach 75%+"
    - "Error paths tested for analytics: invalid parameters, missing workflows"
    - "Workflow metrics service properly mocked with MagicMock"
    - "Template manager properly mocked with MagicMock for all template operations"
  artifacts:
    - path: "backend/tests/api/test_workflow_analytics_routes_coverage.py"
      provides: "Workflow analytics routes test coverage (30 lines target, 3 endpoints)"
      min_lines: 250
      exports: ["TestWorkflowAnalyticsSummary", "TestWorkflowRecentExecutions", "TestWorkflowStats", "TestWorkflowAnalyticsErrorPaths"]
    - path: "backend/tests/api/test_workflow_template_routes_coverage.py"
      provides: "Workflow template routes enhanced coverage (360 lines target, 8 endpoints)"
      min_lines: 500
      exports: ["TestTemplateCreationErrorPaths", "TemplateExecutionErrorPaths", "TestTemplateImport"]
  key_links:
    - from: "test_workflow_analytics_routes_coverage.py"
      to: "api/workflow_analytics_routes.py"
      via: "TestClient with per-file FastAPI app"
      pattern: "TestClient.*router"
    - from: "test_workflow_template_routes_coverage.py"
      to: "api/workflow_template_routes.py"
      via: "Existing test file enhancement"
      pattern: "TestClient.*router"
    - from: "test_workflow_analytics_routes_coverage.py"
      to: "core.workflow_metrics.metrics"
      via: "MagicMock patch for workflow metrics"
      pattern: "patch.*metrics"
---

<objective>
**Workflow Analytics and Template Routes Enhanced Test Coverage**

Create comprehensive test coverage for workflow analytics routes and enhance existing workflow template routes tests. Target 75%+ line coverage for both route files combined.

**Purpose:** Ensure workflow analytics endpoints (3 routes) are fully tested and workflow template routes (8 routes) reach 75%+ coverage with additional test cases for governance and execution paths.

**Output:**
- test_workflow_analytics_routes_coverage.py (250+ lines, 8-10 tests) - NEW file
- test_workflow_template_routes_coverage.py (500+ lines, add 15-20 tests) - ENHANCED existing file
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/179-api-routes-coverage-ai-workflows/179-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Pattern References
@backend/tests/api/conftest.py (TestClient fixtures, mock patterns)
@backend/tests/api/test_workflow_template_routes.py (existing template tests - 617 lines, 40+ tests)
@backend/tests/api/test_multi_integration_workflow_routes.py (workflow test patterns)

# Target Routes
@backend/api/workflow_analytics_routes.py (30 lines, 3 endpoints)
@backend/api/workflow_template_routes.py (360 lines, 8 endpoints)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Workflow Analytics Routes Test File with Fixtures</name>
  <files>backend/tests/api/test_workflow_analytics_routes_coverage.py</files>
  <action>
    Create test_workflow_analytics_routes_coverage.py with the following structure:

    **Imports:** pytest, unittest.mock (AsyncMock, MagicMock, Mock, patch), fastapi.testclient.TestClient

    **Fixtures (5-6 fixtures):**
    1. `mock_workflow_metrics()` - MagicMock for core.workflow_metrics.metrics with:
       - get_summary = Mock(return_value={"total_executions": 100, "success_rate": 0.95})
       - get_recent_executions = Mock(return_value=[])
       - get_workflow_stats = Mock(return_value={"workflow_id": "wf-123", "executions": 50})

    2. `workflow_analytics_client(mock_workflow_metrics)` - TestClient with:
       ```python
       from fastapi import FastAPI
       from api.workflow_analytics_routes import router
       app = FastAPI()
       app.include_router(router)
       with patch('api.workflow_analytics_routes.metrics', mock_workflow_metrics):
           yield TestClient(app)
       ```

    3. `sample_analytics_summary()` - Expected analytics summary data structure
    4. `sample_recent_executions()` - Expected recent executions list
    5. `sample_workflow_stats()` - Expected workflow stats data
    6. `sample_workflow_id()` - Factory for valid workflow_id parameter

    **File header:** Module docstring describing coverage (30 lines, 3 endpoints, 75%+ target)
  </action>
  <verify>
    File created at backend/tests/api/test_workflow_analytics_routes_coverage.py with 5+ fixtures
    workflow_analytics_client fixture uses per-file FastAPI app pattern
    mock_workflow_metrics has all required methods mocked
  </verify>
  <done>
    All fixtures created, test file structure ready for test classes
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Workflow Analytics Success and Error Path Tests</name>
  <files>backend/tests/api/test_workflow_analytics_routes_coverage.py</files>
  <action>
    Create `TestWorkflowAnalyticsSummary` class with 2-3 tests:

    **Analytics Summary Tests (2-3 tests):**
    1. `test_get_analytics_summary_default()` - GET /api/workflows/analytics returns summary with default days=7
    2. `test_get_analytics_summary_custom_days()` - Summary with days=30 parameter
    3. `test_get_analytics_summary_structure()` - Verifies response has total_executions, success_rate, etc.

    Create `TestWorkflowRecentExecutions` class with 2-3 tests:

    **Recent Executions Tests (2-3 tests):**
    1. `test_get_recent_executions_default()` - GET /api/workflows/analytics/recent returns recent executions with default limit=20
    2. `test_get_recent_executions_custom_limit()` - Recent executions with custom limit parameter
    3. `test_get_recent_executions_empty()` - Returns empty list when no recent executions

    Create `TestWorkflowStats` class with 2-3 tests:

    **Workflow Stats Tests (2-3 tests):**
    1. `test_get_workflow_stats_success()` - GET /api/workflows/analytics/{workflow_id} returns stats for specific workflow
    2. `test_get_workflow_stats_not_found()` - Returns empty/404 for non-existent workflow
    3. `test_get_workflow_stats_structure()` - Verifies stats structure has workflow_id, executions, etc.

    Create `TestWorkflowAnalyticsErrorPaths` class with 2-3 tests:

    **Error Tests (2-3 tests):**
    1. `test_analytics_service_error()` - WorkflowMetrics service exception returns error response
    2. `test_analytics_invalid_days()` - Invalid days parameter (negative, zero) handled gracefully
    3. `test_analytics_invalid_limit()` - Invalid limit parameter handled gracefully
  </action>
  <verify>
    TestWorkflowAnalyticsSummary class exists with 2-3 tests
    TestWorkflowRecentExecutions class exists with 2-3 tests
    TestWorkflowStats class exists with 2-3 tests
    TestWorkflowAnalyticsErrorPaths class exists with 2-3 tests
    All tests use mock_workflow_metrics fixture
  </verify>
  <done>
    All 8-10 analytics tests created with success and error paths
  </done>
</task>

<task type="auto">
  <name>Task 3: Enhance Existing Workflow Template Routes Tests</name>
  <files>backend/tests/api/test_workflow_template_routes.py</files>
  <action>
    Read existing test_workflow_template_routes.py and add new test classes to reach 75%+ coverage:

    **Add `TestTemplateCreationErrorPaths` class with 4-5 tests:**
    1. `test_create_template_duplicate_name()` - Creating template with duplicate name handled correctly
    2. `test_create_template_invalid_complexity()` - Invalid complexity value returns 422
    3. `test_create_template_invalid_category()` - Invalid category returns 422
    4. `test_create_template_governance_blocked()` - Template creation blocked by governance (STUDENT maturity)
    5. `test_create_template_service_error()` - Template manager exception returns 500

    **Add `TestTemplateExecutionErrorPaths` class with 4-5 tests:**
    1. `test_execute_template_not_found()` - Executing non-existent template returns 404
    2. `test_execute_template_governance_blocked()` - Execution blocked by governance (low maturity)
    3. `test_execute_orchestrator_error()` - Orchestrator exception during execute_template
    4. `test_execute_template_invalid_parameters()` - Invalid parameters in execution request
    5. `test_execute_template_instantiation_failure()` - Workflow instantiation failure handled

    **Add `TestTemplateImport` class with 3-4 tests:**
    1. `test_import_template_success()` - POST /api/workflow-templates/{id}/import creates workflow from template
    2. `test_import_template_not_found()` - Import non-existent template returns 404
    3. `test_import_template_governance()` - Import requires governance check (LOW complexity)
    4. `test_import_template_customizations()` - Import with customizations parameter

    **Add `TestTemplateSearchErrorPaths` class with 2-3 tests:**
    1. `test_search_empty_query()` - Search with empty query returns all templates
    2. `test_search_no_results()` - Search with non-matching query returns empty list
    3. `test_search_service_error()` - Search service exception handled gracefully

    Append to existing file, maintain existing test class structure and fixtures.
  </action>
  <verify>
    test_workflow_template_routes.py enhanced with 4 new test classes
    New classes: CreationErrorPaths, ExecutionErrorPaths, Import, SearchErrorPaths
    Tests cover governance blocking, service errors, validation errors
  </verify>
  <done>
    Template routes enhanced with 15-20 new tests
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify Coverage and Test Execution</name>
  <files>
    backend/tests/api/test_workflow_analytics_routes_coverage.py
    backend/tests/api/test_workflow_template_routes.py
  </files>
  <action>
    **Verify Workflow Analytics Tests:**
    1. Run: `pytest backend/tests/api/test_workflow_analytics_routes_coverage.py -v`
    2. Verify 8-10 tests pass
    3. Check coverage: `pytest backend/tests/api/test_workflow_analytics_routes_coverage.py --cov=api/workflow_analytics_routes --cov-report=term-missing`
    4. Verify 75%+ line coverage for workflow_analytics_routes.py

    **Verify Enhanced Template Tests:**
    1. Run: `pytest backend/tests/api/test_workflow_template_routes.py -v`
    2. Verify 55-60 tests pass (existing 40 + new 15-20)
    3. Check coverage: `pytest backend/tests/api/test_workflow_template_routes.py --cov=api/workflow_template_routes --cov-report=term-missing`
    4. Verify 75%+ line coverage for workflow_template_routes.py

    **Update SUMMARY:**
    Create 179-04-SUMMARY.md with:
    - Test count and pass rate for both files
    - Coverage percentage for both route files (target: 75%+ each)
    - Test classes added to template routes
    - Uncovered lines (if any)
    - Deviations from plan (if any)
  </action>
  <verify>
    Analytics tests: 8-10 passing, 75%+ coverage for workflow_analytics_routes.py
    Template tests: 55-60 passing, 75%+ coverage for workflow_template_routes.py
    179-04-SUMMARY.md created with combined coverage metrics
  </verify>
  <done>
    75%+ coverage achieved for both route files, all tests passing
  </done>
</task>

</tasks>

<verification>
**Phase Verification:**
- [ ] test_workflow_analytics_routes_coverage.py created with 250+ lines
- [ ] test_workflow_template_routes.py enhanced with 15-20 new tests
- [ ] 8-10 analytics tests covering all 3 analytics endpoints
- [ ] Template routes enhanced with error paths and governance tests
- [ ] Workflow metrics service properly mocked with MagicMock
- [ ] Coverage measurement shows 75%+ for both route files
- [ ] SUMMARY.md documents combined results
</verification>

<success_criteria>
**Phase 179 Plan 04 Complete When:**
1. test_workflow_analytics_routes_coverage.py exists with 8-10 tests
2. test_workflow_template_routes.py enhanced to 55-60 tests total
3. Coverage report shows 75%+ for workflow_analytics_routes.py (30 lines)
4. Coverage report shows 75%+ for workflow_template_routes.py (360 lines)
5. Workflow metrics properly mocked
6. Template governance paths tested
7. 179-04-SUMMARY.md documents coverage results
</success_criteria>

<output>
After completion, create `.planning/phases/179-api-routes-coverage-ai-workflows/179-04-SUMMARY.md`
</output>
