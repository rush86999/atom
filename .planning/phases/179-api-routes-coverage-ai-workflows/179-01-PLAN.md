---
phase: 179-api-routes-coverage-ai-workflows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_ai_workflows_routes_coverage.py
autonomous: true

must_haves:
  truths:
    - "AI workflows routes (3 endpoints: nlu/parse, providers, complete) have 75%+ line coverage"
    - "POST /api/ai-workflows/nlu/parse returns intent, entities, tasks, confidence with proper error handling"
    - "GET /api/ai-workflows/providers returns available AI providers with enabled status"
    - "POST /api/ai-workflows/complete generates text completion with provider and tokens used"
    - "Error paths tested: invalid provider, empty text, service failures (400, 422, 500)"
    - "External services (enhanced_ai_workflow_endpoints.ai_service) properly mocked with AsyncMock"
  artifacts:
    - path: "backend/tests/api/test_ai_workflows_routes_coverage.py"
      provides: "AI workflows routes test coverage (182 lines target, 3 endpoints)"
      min_lines: 400
      exports: ["TestAIWorkflowsSuccess", "TestAIWorkflowsErrorPaths", "TestAIWorkflowsProviders"]
  key_links:
    - from: "test_ai_workflows_routes_coverage.py"
      to: "api/ai_workflows_routes.py"
      via: "TestClient with per-file FastAPI app"
      pattern: "TestClient.*router"
    - from: "test_ai_workflows_routes_coverage.py"
      to: "enhanced_ai_workflow_endpoints.ai_service"
      via: "AsyncMock patch for external AI service"
      pattern: "patch.*ai_service"
---

<objective>
**AI Workflows Routes Test Coverage**

Create comprehensive test coverage for AI workflows routes (ai_workflows_routes.py, 182 lines, 3 endpoints). Target 75%+ line coverage with TestClient-based integration tests.

**Purpose:** Ensure AI workflow endpoints (NLU parsing, provider listing, text completion) are thoroughly tested with proper mocking of external AI services.

**Output:** test_ai_workflows_routes_coverage.py (400+ lines, 15-20 tests) covering success paths, error paths, and edge cases.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/179-api-routes-coverage-ai-workflows/179-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Pattern References
@backend/tests/api/conftest.py (TestClient fixtures, mock patterns)
@backend/tests/api/test_admin_system_health_routes.py (error path patterns, Phase 178)
@backend/tests/api/test_workflow_template_routes.py (existing workflow tests)

# Target Routes
@backend/api/ai_workflows_routes.py (182 lines, 3 endpoints)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AI Workflows Routes Test File with Fixtures</name>
  <files>backend/tests/api/test_ai_workflows_routes_coverage.py</files>
  <action>
    Create test_ai_workflows_routes_coverage.py with the following structure:

    **Imports:** pytest, unittest.mock (AsyncMock, MagicMock, patch), fastapi.testclient.TestClient

    **Fixtures (6-8 fixtures):**
    1. `mock_ai_service()` - MagicMock for enhanced_ai_workflow_endpoints.ai_service with:
       - process_with_nlu = AsyncMock returning intent/entities/confidence
       - analyze_text = AsyncMock returning completion text
       - openai_api_key, anthropic_api_key, deepseek_api_key, google_api_key attributes

    2. `ai_workflows_client(mock_ai_service)` - TestClient with per-file FastAPI app:
       ```python
       from fastapi import FastAPI
       from api.ai_workflows_routes import router
       app = FastAPI()
       app.include_router(router)
       with patch('api.ai_workflows_routes.ai_service', mock_ai_service):
           yield TestClient(app)
       ```

    3. `sample_nlu_request()` - Factory for valid NLUParseRequest data
    4. `sample_completion_request()` - Factory for valid CompletionRequest data
    5. `nlu_parse_response_data()` - Expected response structure
    6. `completion_response_data()` - Expected completion response structure

    **File header:** Module docstring describing coverage (182 lines, 3 endpoints, 75%+ target)
  </action>
  <verify>
    File created at backend/tests/api/test_ai_workflows_routes_coverage.py with 6+ fixtures
    Fixture imports include AsyncMock, MagicMock, patch, TestClient
    ai_workflows_client fixture uses per-file FastAPI app pattern
  </verify>
  <done>
    All fixtures created, test file structure ready for test classes
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Success Path Tests for AI Workflows Endpoints</name>
  <files>backend/tests/api/test_ai_workflows_routes_coverage.py</files>
  <action>
    Create `TestAIWorkflowsSuccess` class with 6-8 success path tests:

    **NLU Parse Tests (3-4 tests):**
    1. `test_parse_nlu_success()` - Valid text with deepseek provider returns intent/entities/tasks/confidence
    2. `test_parse_nlu_with_openai()` - NLU parse with openai provider
    3. `test_parse_nlu_intent_only()` - Parse with intent_only=True
    4. `test_parse_nlu_fallback()` - Tests fallback when real AI service fails (mock side_effect=Exception)

    **Providers Tests (2 tests):**
    5. `test_get_providers_with_keys()` - Returns providers list with enabled=True when keys present
    6. `test_get_providers_no_keys()` - Returns all providers with enabled=False when no keys

    **Completion Tests (1-2 tests):**
    7. `test_complete_text_success()` - Valid prompt returns completion with provider_used/tokens_used
    8. `test_complete_text_with_custom_params()` - Completion with custom temperature/max_tokens

    Use mock_ai_service fixture to set return values. Assert response.status_code == 200 and verify response structure.
  </action>
  <verify>
    TestAIWorkflowsSuccess class exists with 6-8 test methods
    All tests use mock_ai_service fixture
    Tests verify response structure (intent, entities, confidence, provider_used, tokens_used)
  </verify>
  <done>
    All success path tests pass, 200 status codes verified, response structures validated
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Error Path Tests for AI Workflows Endpoints</name>
  <files>backend/tests/api/test_ai_workflows_routes_coverage.py</files>
  <action>
    Create `TestAIWorkflowsErrorPaths` class with 6-8 error path tests:

    **Validation Errors (422 status):**
    1. `test_parse_nlu_empty_text()` - Empty text should return 422 or process with fallback
    2. `test_complete_text_empty_prompt()` - Empty prompt returns 422 validation error
    3. `test_complete_text_invalid_max_tokens()` - Negative/invalid max_tokens returns 422

    **Service Errors (500/fallback handling):**
    4. `test_parse_nlu_service_error()` - AI service exception triggers fallback path
    5. `test_complete_text_service_error()` - Completion failure returns error response
    6. `test_get_providers_service_error()` - Provider list failure returns default providers

    **Edge Cases:**
    7. `test_parse_nlu_long_text()` - Very long text (>1000 chars) handled correctly
    8. `test_complete_text_with_special_chars()` - Text with special characters/unicode

    Use mock_ai_service.side_effect = Exception() to simulate service failures. Verify fallback behavior or error responses.
  </action>
  <verify>
    TestAIWorkflowsErrorPaths class exists with 6-8 test methods
    Tests cover 422 validation errors, 500 service errors, and edge cases
    Tests verify error handling or fallback behavior
  </verify>
  <done>
    All error path tests cover API-03 requirements (422, 500, constraint violations)
  </done>
</task>

<task type="auto">
  <name>Task 4: Verify Coverage and Test Execution</name>
  <files>backend/tests/api/test_ai_workflows_routes_coverage.py</files>
  <action>
    **Verify Test Execution:**
    1. Run tests: `pytest backend/tests/api/test_ai_workflows_routes_coverage.py -v`
    2. Verify 12-16 tests pass (success + error paths)
    3. Check no import errors or SQLAlchemy conflicts

    **Verify Coverage:**
    1. Run coverage: `pytest backend/tests/api/test_ai_workflows_routes_coverage.py --cov=api/ai_workflows_routes --cov-report=term-missing`
    2. Verify 75%+ line coverage achieved
    3. Document any uncovered lines in SUMMARY (e.g., unreachable fallback paths)

    **Update SUMMARY:**
    Create 179-01-SUMMARY.md with:
    - Test count and pass rate
    - Coverage percentage
    - Uncovered lines (if any)
    - Deviations from plan (if any)
  </action>
  <verify>
    pytest runs successfully with 12-16 passing tests
    Coverage report shows 75%+ for api/ai_workflows_routes.py
    179-01-SUMMARY.md created with coverage metrics
  </verify>
  <done>
    75%+ coverage achieved, all tests passing, SUMMARY documents coverage gaps
  </done>
</task>

</tasks>

<verification>
**Phase Verification:**
- [ ] Test file created with 400+ lines of test code
- [ ] 12-16 tests covering all 3 AI workflows endpoints
- [ ] Success paths tested: NLU parse, providers list, text completion
- [ ] Error paths tested: 422 validation, 500 service errors, edge cases
- [ ] External AI service mocked with AsyncMock pattern
- [ ] Coverage measurement shows 75%+ for ai_workflows_routes.py
- [ ] SUMMARY.md documents results and any gaps
</verification>

<success_criteria>
**Phase 179 Plan 01 Complete When:**
1. test_ai_workflows_routes_coverage.py exists with 12-16 tests
2. All tests pass with pytest -v execution
3. Coverage report shows 75%+ line coverage for ai_workflows_routes.py (182 lines)
4. External AI service properly mocked with AsyncMock
5. API-03 requirement met: error paths (422, 500) tested
6. 179-01-SUMMARY.md documents coverage results
</success_criteria>

<output>
After completion, create `.planning/phases/179-api-routes-coverage-ai-workflows/179-01-SUMMARY.md`
</output>
