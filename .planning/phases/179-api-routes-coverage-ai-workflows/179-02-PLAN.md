---
phase: 179-api-routes-coverage-ai-workflows
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_ai_accounting_routes_coverage.py
autonomous: true

must_haves:
  truths:
    - "AI accounting routes (13 endpoints: transactions, categorize, posting, forecast, export) have 75%+ line coverage"
    - "POST /ai-accounting/transactions ingests single transaction with categorization and confidence"
    - "POST /ai-accounting/bank-feed bulk ingests multiple transactions with auto_posted/review_required counts"
    - "GET /ai-accounting/review-queue returns pending transactions with suggested categories"
    - "PUT /ai-accounting/transactions/{id} and DELETE endpoints handle not-found with 404"
    - "GET /ai-accounting/dashboard/summary queries IntegrationMetric from database (mocked)"
    - "Error paths tested: validation errors, 404 not found, 500 service failures"
    - "External AI accounting service (ai_accounting) properly mocked with MagicMock"
  artifacts:
    - path: "backend/tests/api/test_ai_accounting_routes_coverage.py"
      provides: "AI accounting routes test coverage (352 lines target, 13 endpoints)"
      min_lines: 700
      exports: ["TestAccountingTransactionIngestion", "TestAccountingCategorization", "TestAccountingPosting", "TestAccountingExports", "TestAccountingErrorPaths"]
  key_links:
    - from: "test_ai_accounting_routes_coverage.py"
      to: "api/ai_accounting_routes.py"
      via: "TestClient with per-file FastAPI app"
      pattern: "TestClient.*router"
    - from: "test_ai_accounting_routes_coverage.py"
      to: "core.ai_accounting_engine.ai_accounting"
      via: "MagicMock patch for external accounting engine"
      pattern: "patch.*ai_accounting"
    - from: "test_ai_accounting_routes_coverage.py"
      to: "core.database.get_db"
      via: "Dependency override for database session"
      pattern: "dependency_overrides.*get_db"
---

<objective>
**AI Accounting Routes Test Coverage**

Create comprehensive test coverage for AI accounting routes (ai_accounting_routes.py, 352 lines, 13 endpoints). Target 75%+ line coverage with TestClient-based integration tests and database mocking.

**Purpose:** Ensure AI accounting endpoints (transaction ingestion, categorization, posting, forecasting, exports, dashboard) are thoroughly tested with proper mocking of external services and database dependencies.

**Output:** test_ai_accounting_routes_coverage.py (700+ lines, 25-30 tests) covering all CRUD operations, posting, forecasting, exports, and error paths.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/179-api-routes-coverage-ai-workflows/179-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Pattern References
@backend/tests/api/conftest.py (TestClient fixtures, mock patterns)
@backend/tests/api/test_admin_system_health_routes.py (database override pattern, Phase 178)
@backend/tests/api/test_admin_business_facts_routes.py (IntegrationMetric query patterns, Phase 178)

# Target Routes
@backend/api/ai_accounting_routes.py (352 lines, 13 endpoints)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AI Accounting Routes Test File with Fixtures</name>
  <files>backend/tests/api/test_ai_accounting_routes_coverage.py</files>
  <action>
    Create test_ai_accounting_routes_coverage.py with the following structure:

    **Imports:** pytest, unittest.mock (AsyncMock, MagicMock, Mock, patch), fastapi.testclient.TestClient, datetime, typing

    **Fixtures (8-10 fixtures):**
    1. `mock_ai_accounting()` - MagicMock for core.ai_accounting_engine.ai_accounting with:
       - ingest_transaction = Mock(return_value=Mock transaction with status/category/confidence)
       - ingest_bank_feed = Mock(return_value=list of Transaction mocks)
       - learn_categorization = Mock()
       - get_pending_review = Mock(return_value=[])
       - get_all_transactions = Mock(return_value=[])
       - update_transaction = Mock(return_value=True)
       - delete_transaction = Mock(return_value=True)
       - post_transaction = Mock(return_value=True)
       - auto_post_high_confidence = Mock(return_value=0)
       - _chart_of_accounts attribute (dict)
       - get_audit_log = Mock(return_value=[])
       - export_general_ledger_csv = Mock(return_value="csv,content")
       - export_trial_balance_json = Mock(return_value={})
       - get_13_week_forecast = Mock(return_value={"projection": []})
       - run_scenario = Mock(return_value={})

    2. `mock_db_for_accounting()` - Mock Session for get_db dependency with:
       - query = Mock returning Mock query chain
       - IntegrationMetric mock for dashboard summary

    3. `ai_accounting_client(mock_ai_accounting, mock_db_for_accounting)` - TestClient with:
       ```python
       from fastapi import FastAPI
       from api.ai_accounting_routes import router
       from core.database import get_db
       app = FastAPI()
       app.include_router(router)
       def override_get_db():
           yield mock_db_for_accounting
       app.dependency_overrides[get_db] = override_get_db
       with patch('core.ai_accounting_engine.ai_accounting', mock_ai_accounting):
           yield TestClient(app)
       app.dependency_overrides.clear()
       ```

    4. `sample_transaction_request()` - Factory for valid TransactionRequest
    5. `sample_bank_feed_request()` - Factory for valid BankFeedRequest
    6. `sample_categorize_request()` - Factory for valid CategorizeRequest
    7. `mock_transaction()` - Mock Transaction with all required attributes
    8. `mock_integration_metrics()` - Mock IntegrationMetric query results for dashboard

    **File header:** Module docstring describing coverage (352 lines, 13 endpoints, 75%+ target)
  </action>
  <verify>
    File created at backend/tests/api/test_ai_accounting_routes_coverage.py with 8+ fixtures
    ai_accounting_client fixture uses dependency override pattern for get_db
    mock_ai_accounting has all required methods mocked
  </verify>
  <done>
    All fixtures created, test file structure ready for test classes
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Transaction Ingestion and Categorization Tests</name>
  <files>backend/tests/api/test_ai_accounting_routes_coverage.py</files>
  <action>
    Create `TestAccountingTransactionIngestion` class with 5-6 tests:

    **Single Transaction Tests (3 tests):**
    1. `test_ingest_transaction_success()` - POST /ai-accounting/transactions with valid data returns success_response with id/status/category/confidence/requires_review
    2. `test_ingest_transaction_with_merchant()` - Transaction with merchant field processed correctly
    3. `test_ingest_transaction_all_sources()` - Test with different source values (bank, manual, import)

    **Bank Feed Tests (2-3 tests):**
    4. `test_ingest_bank_feed_success()` - POST /ai-accounting/bank-feed returns ingested/auto_categorized/review_required counts
    5. `test_ingest_bank_feed_empty()` - Empty bank feed returns zero counts
    6. `test_ingest_bank_feed_multiple()` - Multiple transactions with mixed confidence levels

    Create `TestAccountingCategorization` class with 4-5 tests:

    **Categorization Tests (4-5 tests):**
    1. `test_categorize_transaction_success()` - POST /ai-accounting/categorize teaches system with transaction_id/category_id
    2. `test_get_review_queue_empty()` - GET /ai-accounting/review-queue returns empty list when no pending
    3. `test_get_review_queue_with_items()` - Returns pending transactions with suggested_category/confidence/reasoning
    4. `test_get_all_transactions()` - GET /ai-accounting/all-transactions returns all transactions with status
    5. `test_categorize_with_user_learning()` - Categorization with different user_id parameters
  </action>
  <verify>
    TestAccountingTransactionIngestion class exists with 5-6 tests
    TestAccountingCategorization class exists with 4-5 tests
    Tests verify response structure (success_response format with data/message)
  </verify>
  <done>
    Transaction ingestion and categorization paths tested
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Transaction Update, Delete, and Posting Tests</name>
  <files>backend/tests/api/test_ai_accounting_routes_coverage.py</files>
  <action>
    Create `TestAccountingTransactionManagement` class with 5-6 tests:

    **Update Tests (2-3 tests):**
    1. `test_update_transaction_success()` - PUT /ai-accounting/transactions/{id} with valid updates returns success
    2. `test_update_transaction_not_found()` - Update non-existent transaction raises 404 via not_found_error
    3. `test_update_transaction_with_multiple_fields()` - Update description, amount, merchant simultaneously

    **Delete Tests (2 tests):**
    4. `test_delete_transaction_success()` - DELETE /ai-accounting/transactions/{id} returns success for existing transaction
    5. `test_delete_transaction_not_found()` - Delete non-existent transaction raises 404

    Create `TestAccountingPosting` class with 4-5 tests:

    **Posting Tests (4-5 tests):**
    1. `test_post_transaction_success()` - POST /ai-accounting/post/{id} returns success for postable transaction
    2. `test_post_transaction_requires_review()` - Posting low-confidence transaction raises 400 validation_error
    3. `test_post_transaction_not_found()` - Posting non-existent transaction raises 404
    4. `test_auto_post_high_confidence()` - POST /ai-accounting/auto-post returns posted_count
    5. `test_auto_post_empty()` - Auto-post with no high-confidence transactions returns 0

    Use mock_ai_accounting.return_value patterns to simulate success/failure scenarios.
  </action>
  <verify>
    TestAccountingTransactionManagement class exists with 5-6 tests
    TestAccountingPosting class exists with 4-5 tests
    Tests verify 404 responses for not-found scenarios
    Tests verify 400 validation errors for unpostable transactions
  </verify>
  <done>
    Transaction CRUD and posting operations tested
  </done>
</task>

<task type="auto">
  <name>Task 4: Add Chart of Accounts, Audit, Export, and Forecast Tests</name>
  <files>backend/tests/api/test_ai_accounting_routes_coverage.py</files>
  <action>
    Create `TestAccountingChartAndAudit` class with 3-4 tests:

    **Chart of Accounts (2 tests):**
    1. `test_get_chart_of_accounts()` - GET /ai-accounting/chart-of-accounts returns accounts list with id/name/type/keywords
    2. `test_get_chart_of_accounts_empty()` - Returns empty accounts list when none defined

    **Audit Log (1-2 tests):**
    3. `test_get_audit_log_all()` - GET /ai-accounting/audit-log returns all audit entries
    4. `test_get_audit_log_filtered()` - Audit log with transaction_id filter returns filtered results

    Create `TestAccountingExports` class with 3-4 tests:

    **Export Tests (3-4 tests):**
    1. `test_export_gl_csv()` - GET /ai-accounting/export/gl returns CSV with correct media_type and Content-Disposition header
    2. `test_export_trial_balance_json()` - GET /ai-accounting/export/trial-balance returns JSON data structure
    3. `test_export_gl_empty()` - Export with no transactions returns valid empty CSV
    4. `test_export_trial_balance_structure()` - Trial balance has expected data structure

    Create `TestAccountingForecasting` class with 3-4 tests:

    **Forecast Tests (3-4 tests):**
    1. `test_get_forecast()` - GET /ai-accounting/forecast returns projection data
    2. `test_run_scenario()` - POST /ai-accounting/scenario analyzes what-if scenario
    3. `test_forecast_with_workspace()` - Forecast with different workspace_id parameter
    4. `test_scenario_analysis()` - Scenario returns modified projection based on description
  </action>
  <verify>
    TestAccountingChartAndAudit class exists with 3-4 tests
    TestAccountingExports class exists with 3-4 tests
    TestAccountingForecasting class exists with 3-4 tests
    Export tests verify Content-Type and Content-Disposition headers
  </verify>
  <done>
    Chart, audit, export, and forecasting endpoints tested
  </done>
</task>

<task type="auto">
  <name>Task 5: Add Dashboard Summary and Error Path Tests</name>
  <files>backend/tests/api/test_ai_accounting_routes_coverage.py</files>
  <action>
    Create `TestAccountingDashboard` class with 3-4 tests:

    **Dashboard Tests (3-4 tests):**
    1. `test_get_dashboard_summary_success()` - GET /ai-accounting/dashboard/summary queries IntegrationMetric for total_revenue/pending_revenue
    2. `test_get_dashboard_summary_empty()` - Returns zero values when no metrics exist
    3. `test_get_dashboard_summary_multiple_metrics()` - Aggregates multiple IntegrationMetric records
    4. `test_get_dashboard_summary_database_error()` - Handles database query errors gracefully (500)

    Use mock_db_for_accounting.query.return_value.filter.return_value.all to return IntegrationMetric mocks.

    Create `TestAccountingErrorPaths` class with 4-5 tests:

    **Validation Errors (2-3 tests):**
    1. `test_ingest_transaction_missing_fields()` - Missing required fields returns 422 validation error
    2. `test_categorize_missing_ids()` - Categorize with missing transaction_id or category_id returns 422
    3. `test_invalid_date_format()` - Invalid ISO date format returns 422

    **Service Errors (2 tests):**
    4. `test_ai_accounting_service_error()` - AI accounting service exception returns 500 with error details
    5. `test_dashboard_unexpected_error()` - Unexpected database error returns 500 internal_error response

    Use mock_ai_accounting.method.side_effect = Exception() to simulate failures.
  </action>
  <verify>
    TestAccountingDashboard class exists with 3-4 tests
    TestAccountingErrorPaths class exists with 4-5 tests
    Dashboard tests verify IntegrationMetric query patterns
    Error path tests cover 422 and 500 status codes
  </verify>
  <done>
    Dashboard and error paths tested, API-03 requirements met
  </done>
</task>

<task type="auto">
  <name>Task 6: Verify Coverage and Test Execution</name>
  <files>backend/tests/api/test_ai_accounting_routes_coverage.py</files>
  <action>
    **Verify Test Execution:**
    1. Run tests: `pytest backend/tests/api/test_ai_accounting_routes_coverage.py -v`
    2. Verify 25-30 tests pass (all test classes)
    3. Check no import errors or SQLAlchemy conflicts

    **Verify Coverage:**
    1. Run coverage: `pytest backend/tests/api/test_ai_accounting_routes_coverage.py --cov=api/ai_accounting_routes --cov-report=term-missing`
    2. Verify 75%+ line coverage achieved
    3. Document any uncovered lines in SUMMARY (e.g., rare exception paths)

    **Update SUMMARY:**
    Create 179-02-SUMMARY.md with:
    - Test count and pass rate (target: 25-30 tests)
    - Coverage percentage (target: 75%+)
    - Test classes: TransactionIngestion, Categorization, TransactionManagement, Posting, ChartAndAudit, Exports, Forecasting, Dashboard, ErrorPaths
    - Uncovered lines (if any)
    - Deviations from plan (if any)
  </action>
  <verify>
    pytest runs successfully with 25-30 passing tests
    Coverage report shows 75%+ for api/ai_accounting_routes.py
    179-02-SUMMARY.md created with coverage metrics
  </verify>
  <done>
    75%+ coverage achieved, all tests passing, SUMMARY documents coverage gaps
  </done>
</task>

</tasks>

<verification>
**Phase Verification:**
- [ ] Test file created with 700+ lines of test code
- [ ] 25-30 tests covering all 13 AI accounting endpoints
- [ ] Success paths tested: ingestion, categorization, CRUD, posting, chart, audit, exports, forecasting, dashboard
- [ ] Error paths tested: 422 validation, 404 not found, 500 service errors
- [ ] Database dependency properly overridden with dependency_overrides pattern
- [ ] Coverage measurement shows 75%+ for ai_accounting_routes.py
- [ ] SUMMARY.md documents results and any gaps
</verification>

<success_criteria>
**Phase 179 Plan 02 Complete When:**
1. test_ai_accounting_routes_coverage.py exists with 25-30 tests
2. All tests pass with pytest -v execution
3. Coverage report shows 75%+ line coverage for ai_accounting_routes.py (352 lines)
4. External AI accounting service properly mocked with MagicMock
5. Database dependency (get_db) properly overridden
6. API-03 requirement met: error paths (422, 404, 500) tested
7. 179-02-SUMMARY.md documents coverage results
</success_criteria>

<output>
After completion, create `.planning/phases/179-api-routes-coverage-ai-workflows/179-02-SUMMARY.md`
</output>
