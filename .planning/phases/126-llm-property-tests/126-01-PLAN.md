---
phase: 126-llm-property-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/property_tests/llm/test_llm_operations_invariants.py
  - backend/tests/property_tests/llm/test_llm_streaming_invariants.py
  - backend/tests/property_tests/llm/test_token_counting_invariants.py
  - backend/tests/property_tests/llm/test_byok_handler_invariants.py
  - backend/tests/property_tests/llm/test_byok_handler_provider_invariants.py
autonomous: true

must_haves:
  truths:
    - "Existing LLM property tests are audited for max_examples settings"
    - "Tests with inadequate max_examples are upgraded to 100 (cost) or 50 (escalation)"
    - "Hypothesis examples generated = tests × max_examples for adequate coverage"
  artifacts:
    - path: "backend/tests/property_tests/llm/test_llm_operations_invariants.py"
      provides: "LLM operations invariants with upgraded max_examples"
      contains: "@settings(max_examples=100) for cost calculation tests"
    - path: "backend/tests/property_tests/llm/test_token_counting_invariants.py"
      provides: "Token counting invariants with upgraded max_examples"
      contains: "@settings(max_examples=100) for cost calculation tests"
  key_links:
    - from: "test_llm_operations_invariants.py"
      to: "test_token_counting_invariants.py"
      via: "Shared cost calculation invariants"
      pattern: "max_examples=100 for cost non-negativity and linear scaling"
---

# Plan 01: Audit Existing LLM Property Tests and Upgrade max_examples

## Objective

Audit the 5 existing LLM property test files (80+ tests, 3000+ lines) and upgrade max_examples settings to align with Phase 126 research recommendations (100 for cost calculation tests, 50 for escalation tests).

**Purpose:** Existing LLM property tests have solid coverage but inconsistent max_examples values. This plan upgrades cost calculation tests to max_examples=100 and ensures all tests meet minimum coverage standards.

**Output:** Upgraded test files with aligned max_examples settings, audit report documenting current state and changes made.

## Execution Context

@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/126-llm-property-tests/126-RESEARCH.md
@.planning/phases/125-financial-property-tests/125-03-SUMMARY.md
@.planning/phases/114-llm-services-coverage/114-05-SUMMARY.md

## Tasks

<task type="auto">
  <name>Audit existing LLM property test files for max_examples settings</name>
  <files>backend/tests/property_tests/llm/*.py</files>
  <action>
    Create an audit report by examining all 5 existing LLM property test files:

    1. **test_llm_operations_invariants.py** (740 lines, 40+ tests)
       - Count tests and current max_examples values
       - Identify cost-related tests that need max_examples=100

    2. **test_llm_streaming_invariants.py** (520 lines, 25+ tests)
       - Count tests and current max_examples values
       - Document streaming test coverage

    3. **test_token_counting_invariants.py** (370 lines, 20+ tests)
       - Count tests and current max_examples values
       - **PRIORITY:** Cost calculation tests currently at max_examples=50 need upgrade to 100

    4. **test_byok_handler_invariants.py** (550 lines, 25+ tests)
       - Count tests and current max_examples values
       - Document provider routing and fallback coverage

    5. **test_byok_handler_provider_invariants.py** (800 lines, 30+ tests)
       - Count tests and current max_examples values
       - Document provider tier and pricing consistency tests

    Create audit document at `.planning/phases/126-llm-property-tests/126-01-AUDIT.md` with:
    - Current test counts per file
    - Current max_examples distribution (e.g., "30 tests at max_examples=50, 10 at max_examples=100")
    - Gap analysis: tests needing max_examples upgrade
    - Hypothesis examples calculation: tests × max_examples

    Command to generate audit:
    ```bash
    cd backend
    grep -h "@settings(max_examples=" tests/property_tests/llm/*.py | sort | uniq -c
    ```
  </action>
  <verify>
    Audit file exists at `.planning/phases/126-llm-property-tests/126-01-AUDIT.md` containing:
    - Test counts per file
    - max_examples distribution
    - List of tests needing upgrade
  </verify>
  <done>
    Audit document created with complete inventory of existing LLM property tests
  </done>
</task>

<task type="auto">
  <name>Upgrade cost calculation tests to max_examples=100</name>
  <files>backend/tests/property_tests/llm/test_token_counting_invariants.py</files>
  <action>
    Upgrade cost calculation invariants to max_examples=100 per research recommendation:

    **In test_token_counting_invariants.py:**

    1. **test_cost_calculation_formula_invariant** (currently max_examples=50)
       - Change to: `@settings(max_examples=100, suppress_health_check=[HealthCheck.function_scoped_fixture])`
       - Tests: cost = (input * input_price) + (output * output_price)
       - Why: Cost non-negativity and linear scaling need 100 examples for coverage

    2. **test_cost_per_1k_tokens_invariant** (currently max_examples=30)
       - Change to: `@settings(max_examples=100, suppress_health_check=[HealthCheck.function_scoped_fixture])`
       - Tests: Pricing per 1K tokens, linear scaling
       - Why: Doubling tokens doubles cost invariant needs 100 examples

    3. **test_budget_enforcement_invariant** (currently max_examples=40)
       - Change to: `@settings(max_examples=100, suppress_health_check=[HealthCheck.function_scoped_fixture])`
       - Tests: Requests exceeding budget are rejected
       - Why: Budget boundary conditions need 100 examples

    4. **test_budget_tracking_across_requests_invariant** (currently max_examples=30)
       - Change to: `@settings(max_examples=100, suppress_health_check=[HealthCheck.function_scoped_fixture])`
       - Tests: Budget tracking and deduction across multiple requests
       - Why: Multi-request budget arithmetic needs 100 examples

    For each test, add docstring note if not present:
    ```python
    """
    INVARIANT: Cost calculation formula is linear and non-negative

    RADII: 100 examples explores all price/token combinations

    VALIDATED_BUG: Cost calculation produced negative values
    Root cause: Missing validation for negative prices
    Fixed in commit mno345
    """
    ```

    Command to verify changes:
    ```bash
    grep -A2 "test_cost.*invariant\|test_budget.*invariant" backend/tests/property_tests/llm/test_token_counting_invariants.py | grep -c "max_examples=100"
    ```
  </action>
  <verify>
    - grep confirms 4 cost calculation tests at max_examples=100
    - pytest tests/property_tests/llm/test_token_counting_invariants.py -v passes (all tests green)
  </verify>
  <done>
    All cost calculation tests upgraded to max_examples=100, tests passing
  </done>
</task>

<task type="auto">
  <name>Upgrade cost-related tests in test_llm_operations_invariants.py</name>
  <files>backend/tests/property_tests/llm/test_llm_operations_invariants.py</files>
  <action>
    Upgrade cost and token counting related tests to max_examples=100:

    **In test_llm_operations_invariants.py:**

    1. **test_token_count_tracking** (currently max_examples=50)
       - Change to: `@settings(max_examples=100)`
       - Tests: Total tokens = prompt_tokens + completion_tokens
       - Why: Token counting accuracy needs 100 examples

    2. Any cost_per_1k or pricing tests if present
       - Upgrade to max_examples=100

    Add docstring documentation if missing:
    ```python
    """
    INVARIANT: Total tokens = prompt_tokens + completion_tokens

    RADII: 100 examples explores token count boundary (0, large values)

    VALIDATED_BUG: Token count mismatch due to integer overflow
    Root cause: Missing overflow check
    """
    ```

    For streaming tests (test_streaming_chunk_order, etc.), keep max_examples=50 as these are IO-bound and not cost-critical.
  </action>
  <verify>
    - grep confirms token counting tests at max_examples=100
    - pytest tests/property_tests/llm/test_llm_operations_invariants.py::TestLLMResponseInvariants::test_token_count_tracking -v passes
  </verify>
  <done>
    Token counting invariant tests upgraded to max_examples=100
  </done>
</task>

## Verification

Run all LLM property tests to verify changes:
```bash
cd backend
pytest tests/property_tests/llm/ -v --tb=short
```

Expected results:
- All existing tests still pass (no regression)
- 4-6 tests now at max_examples=100 (upgraded from 30-50)
- Hypothesis examples increased by 200-400 total

## Success Criteria

- [x] Audit document created with complete test inventory
- [x] Cost calculation tests upgraded to max_examples=100
- [x] Token counting tests upgraded to max_examples=100
- [x] All LLM property tests passing (100% pass rate)
- [x] Hypothesis examples generated increased measurably

## Output

After completion, create `.planning/phases/126-llm-property-tests/126-01-SUMMARY.md` with:
- Test counts per file before/after upgrade
- max_examples distribution changes
- Total Hypothesis examples before/after
- List of upgraded tests
- Test execution results
