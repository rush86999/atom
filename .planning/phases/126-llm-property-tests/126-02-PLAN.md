---
phase: 126-llm-property-tests
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/tests/property_tests/llm/test_tier_escalation_invariants.py
  - backend/core/llm/escalation_manager.py
autonomous: true

must_haves:
  truths:
    - "Quality threshold <80 triggers escalation to next tier"
    - "Cooldown (5 minutes) prevents rapid tier cycling"
    - "COMPLEX tier cannot escalate further (max tier)"
    - "Max escalation limit (2) prevents runaway costs"
  artifacts:
    - path: "backend/tests/property_tests/llm/test_tier_escalation_invariants.py"
      provides: "Tier escalation property-based tests"
      contains: "Property tests for EscalationManager with max_examples=50"
    - path: "backend/core/llm/escalation_manager.py"
      provides: "EscalationManager production code"
      contains: "should_escalate, _is_on_cooldown, get_escalation_count methods"
  key_links:
    - from: "test_tier_escalation_invariants.py"
      to: "backend/core/llm/escalation_manager.py"
      via: "Property-based tests validate escalation invariants"
      pattern: "should_escalate returns (bool, EscalationReason, CognitiveTier)"
---

# Plan 02: Add Tier Escalation Property Tests (TDD)

## Objective

Create property-based tests for EscalationManager tier escalation logic using TDD methodology. The EscalationManager has comprehensive unit tests (test_escalation_manager.py with 24 tests) but lacks property-based tests for systematic invariant validation.

**Purpose:** Add Hypothesis property tests to validate tier escalation invariants across all combinations of tiers, quality scores, cooldown states, and escalation reasons.

**Output:** New test file test_tier_escalation_invariants.py with 10-15 property tests using max_examples=50.

## Execution Context

@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/126-llm-property-tests/126-RESEARCH.md
@backend/core/llm/escalation_manager.py (458 lines, EscalationManager implementation)
@backend/tests/test_escalation_manager.py (580 lines, 24 existing unit tests)

## Feature

<feature>
  <name>Tier Escalation Property Tests</name>
  <files>
    backend/tests/property_tests/llm/test_tier_escalation_invariants.py
    backend/core/llm/escalation_manager.py
  </files>
  <behavior>
    Property-based tests for EscalationManager invariants:

    1. **Quality Threshold Invariant:**
       - Given: current_tier in [MICRO, STANDARD, VERSATILE, HEAVY], response_quality in [0-100]
       - When: response_quality < 80
       - Then: should_escalate=True, reason=QUALITY_THRESHOLD, target_tier=next in order

    2. **Cooldown Invariant:**
       - Given: tier, minutes_since_escalation in [0-10]
       - When: minutes_since_escalation < 5
       - Then: is_on_cooldown=True, escalation blocked

    3. **Max Tier Invariant:**
       - Given: current_tier=COMPLEX
       - When: Any escalation trigger (quality <80, rate_limited, error)
       - Then: should_escalate=False (COMPLEX cannot escalate)

    4. **Rate Limit Priority Invariant:**
       - Given: Any tier, rate_limited=True
       - When: Checking should_escalate
       - Then: Escalates immediately (bypasses cooldown if not applicable)

    5. **Max Escalation Limit Invariant:**
       - Given: request_id, escalation_count=2 (MAX_ESCALATION_LIMIT)
       - When: Any escalation trigger
       - Then: should_escalate=False (max limit reached)

    6. **Confidence Threshold Invariant:**
       - Given: Any tier, confidence in [0-1]
       - When: confidence < 0.7
       - Then: should_escalate=True, reason=LOW_CONFIDENCE

    7. **Tier Order Invariant:**
       - Given: Any tier except COMPLEX
       - When: Escalation triggered
       - Then: target_tier = next in [MICRO, STANDARD, VERSATILE, HEAVY, COMPLEX]

    8. **Cooldown Expiry Invariant:**
       - Given: Tier on cooldown, minutes_since_escalation >= 5
       - When: Checking is_on_cooldown
       - Then: Returns False (cooldown expired)

    Cases: Generated via @given with st.sampled_from, st.integers, st.floats
  </behavior>
  <implementation>
    Create test file following existing patterns from test_escalation_manager.py:

    RED Phase:
    1. Create test_tier_escalation_invariants.py with failing tests
    2. Each test uses @given with strategies for inputs
    3. Tests assert on EscalationManager.should_escalate() return values
    4. Run pytest - expect tests to pass (EscalationManager already correct)

    GREEN Phase:
    1. Tests should already pass (EscalationManager implementation is correct)
    2. If any test fails, fix bug in EscalationManager

    REFACTOR Phase:
    1. Extract common Hypothesis settings to HYPOTHESIS_SETTINGS_ESCALATION
    2. Add helper methods for tier order verification
    3. Document VALIDATED_BUG sections if any bugs found

    Hypothesis settings:
    ```python
    HYPOTHESIS_SETTINGS_ESCALATION = {
        "suppress_health_check": [HealthCheck.function_scoped_fixture, HealthCheck.too_slow],
        "max_examples": 50  # Escalation tests: 4 tiers × 7 quality levels × 11 cooldown values = 308 combos
    }
    ```
  </implementation>
</feature>

## Tasks

<task type="auto">
  <name>RED: Create tier escalation property test file with failing expectations</name>
  <files>backend/tests/property_tests/llm/test_tier_escalation_invariants.py</files>
  <action>
    Create new test file at `backend/tests/property_tests/llm/test_tier_escalation_invariants.py`:

    ```python
    """
    Property-Based Tests for Tier Escalation Invariants

    Tests CRITICAL tier escalation invariants:
    - Quality threshold breaches (<80) trigger escalation
    - Cooldown period (5 min) prevents rapid cycling
    - COMPLEX tier cannot escalate further
    - Rate limit errors trigger immediate escalation
    - Max escalation limit (2) prevents runaway costs

    These tests protect against escalation bugs, rapid tier cycling, and cost overruns.
    """

    import pytest
    from hypothesis import given, settings, example, HealthCheck
    from hypothesis.strategies import sampled_from, integers, floats, uuids
    from datetime import datetime, timedelta
    from typing import Optional

    from core.llm.escalation_manager import (
        EscalationManager,
        EscalationReason,
        ESCALATION_COOLDOWN,
        MAX_ESCALATION_LIMIT,
        TIER_ORDER
    )
    from core.llm.cognitive_tier_system import CognitiveTier

    # Hypothesis settings for escalation tests
    HYPOTHESIS_SETTINGS_ESCALATION = {
        "suppress_health_check": [HealthCheck.function_scoped_fixture, HealthCheck.too_slow],
        "max_examples": 50
    }


    class TestQualityThresholdInvariants:
        """Property-based tests for quality threshold escalation triggers."""

        @given(
            current_tier=sampled_from([
                CognitiveTier.MICRO,
                CognitiveTier.STANDARD,
                CognitiveTier.VERSATILE,
                CognitiveTier.HEAVY
                # Note: COMPLEX tier tested separately
            ]),
            response_quality=sampled_from([None, 60, 70, 75, 79, 80, 85, 90, 95])
        )
        @settings(**HYPOTHESIS_SETTINGS_ESCALATION)
        def test_quality_threshold_breach_triggers_escalation(
            self, db_session, current_tier: CognitiveTier,
            response_quality: Optional[float]
        ):
            """
            PROPERTY: Quality score <80 triggers escalation to next tier

            STRATEGY: st.tuples(current_tier, response_quality)

            INVARIANT: quality <80 → escalate, quality >=80 → no escalate

            TIER_ORDER: [MICRO, STANDARD, VERSATILE, HEAVY, COMPLEX]

            RADII: 50 examples explores all tier×quality combinations (4×9=36)
            """
            manager = EscalationManager(db_session)

            should_escalate, reason, target_tier = manager.should_escalate(
                current_tier=current_tier,
                response_quality=response_quality,
                error=None,
                rate_limited=False
            )

            if response_quality is not None and response_quality < 80:
                # Quality threshold breach MUST trigger escalation
                assert should_escalate == True, \
                    f"Quality {response_quality} <80 should trigger escalation from {current_tier.value}"
                assert reason == EscalationReason.QUALITY_THRESHOLD, \
                    f"Reason must be QUALITY_THRESHOLD, got {reason}"

                # Verify target tier is next in order
                current_index = TIER_ORDER.index(current_tier)
                expected_target = TIER_ORDER[current_index + 1]
                assert target_tier == expected_target, \
                    f"Target tier should be {expected_target.value}, got {target_tier.value if target_tier else None}"

            elif response_quality is not None and response_quality >= 80:
                # Quality acceptable MUST NOT escalate
                assert should_escalate == False, \
                    f"Quality {response_quality} >=80 should NOT trigger escalation from {current_tier.value}"
                assert target_tier is None, "Target tier must be None when not escalating"


    class TestCooldownInvariants:
        """Property-based tests for escalation cooldown enforcement."""

        @given(
            tier=sampled_from([
                CognitiveTier.MICRO,
                CognitiveTier.STANDARD,
                CognitiveTier.VERSATILE,
                CognitiveTier.HEAVY,
                CognitiveTier.COMPLEX
            ]),
            minutes_since_escalation=integers(min_value=0, max_value=10)
        )
        @settings(**HYPOTHESIS_SETTINGS_ESCALATION)
        def test_cooldown_prevents_rapid_escalation(
            self, db_session, tier: CognitiveTier, minutes_since_escalation: int
        ):
            """
            PROPERTY: Tier on cooldown for 5 minutes after escalation

            STRATEGY: st.tuples(tier, minutes_since_escalation)

            INVARIANT: is_on_cooldown returns True for <5 min, False for >=5 min

            RADII: 50 examples explores cooldown boundary (5 tiers × 11 time values = 55)
            """
            manager = EscalationManager(db_session)

            # Simulate escalation at specific time in past
            escalation_time = datetime.utcnow() - timedelta(minutes=minutes_since_escalation)
            manager.escalation_log[tier.value] = escalation_time

            # Check cooldown status
            is_on_cooldown = manager._is_on_cooldown(tier)

            # Verify cooldown logic
            if minutes_since_escalation < 5:
                # Should still be on cooldown
                assert is_on_cooldown == True, \
                    f"Tier {tier.value} should be on cooldown at {minutes_since_escalation} minutes"
            else:
                # Cooldown expired
                assert is_on_cooldown == False, \
                    f"Tier {tier.value} should NOT be on cooldown at {minutes_since_escalation} minutes"


    class TestMaxTierInvariants:
        """Property-based tests for COMPLEX tier (max tier) behavior."""

        @given(
            response_quality=sampled_from([None, 50, 60, 70, 75, 80]),
            rate_limited=sampled_from([True, False]),
            has_error=sampled_from([True, False])
        )
        @settings(**HYPOTHESIS_SETTINGS_ESCALATION)
        def test_complex_tier_cannot_escalate(
            self, db_session, response_quality: Optional[float],
            rate_limited: bool, has_error: bool
        ):
            """
            PROPERTY: COMPLEX tier cannot escalate further (no higher tier exists)

            STRATEGY: st.tuples(response_quality, rate_limited, has_error)

            INVARIANT: COMPLEX tier always returns (False, None, None)

            RADII: 50 examples explores all trigger combinations for COMPLEX tier
            """
            manager = EscalationManager(db_session)

            error = "Some error" if has_error else None

            should_escalate, reason, target_tier = manager.should_escalate(
                current_tier=CognitiveTier.COMPLEX,
                response_quality=response_quality,
                error=error,
                rate_limited=rate_limited
            )

            # COMPLEX tier MUST NOT escalate
            assert should_escalate == False, \
                "COMPLEX tier cannot escalate further regardless of triggers"
            assert target_tier is None, "Target tier must be None for COMPLEX tier"


    class TestRateLimitInvariants:
        """Property-based tests for rate limit escalation behavior."""

        @given(
            current_tier=sampled_from([
                CognitiveTier.MICRO,
                CognitiveTier.STANDARD,
                CognitiveTier.VERSATILE,
                CognitiveTier.HEAVY
            ]),
            on_cooldown=sampled_from([True, False])
        )
        @settings(**HYPOTHESIS_SETTINGS_ESCALATION)
        def test_rate_limit_triggers_immediate_escalation(
            self, db_session, current_tier: CognitiveTier, on_cooldown: bool
        ):
            """
            PROPERTY: Rate limit errors trigger immediate escalation (priority over cooldown)

            STRATEGY: st.tuples(current_tier, on_cooldown)

            INVARIANT: rate_limited=True → escalate (unless on_cooldown or COMPLEX tier)

            RADII: 50 examples explores tier × cooldown combinations
            """
            manager = EscalationManager(db_session)

            # Set cooldown if specified
            if on_cooldown:
                manager.escalation_log[current_tier.value] = datetime.utcnow()

            should_escalate, reason, target_tier = manager.should_escalate(
                current_tier=current_tier,
                response_quality=None,
                error=None,
                rate_limited=True
            )

            # Rate limit triggers escalation if not on cooldown
            if on_cooldown:
                # Cooldown blocks even rate limit escalation
                assert should_escalate == False, \
                    "Cooldown should block escalation even for rate limits"
            else:
                assert should_escalate == True, \
                    "Rate limit should trigger escalation when not on cooldown"
                assert reason == EscalationReason.RATE_LIMITED, \
                    f"Reason must be RATE_LIMITED, got {reason}"


    class TestMaxEscalationLimitInvariants:
        """Property-based tests for max escalation limit per request."""

        @given(
            escalation_count=integers(min_value=0, max_value=5),
            response_quality=sampled_from([50, 60, 70, 75])
        )
        @settings(**HYPOTHESIS_SETTINGS_ESCALATION)
        def test_max_escalation_limit_enforced(
            self, db_session, escalation_count: int, response_quality: float
        ):
            """
            PROPERTY: Max escalation limit (2) prevents runaway costs

            STRATEGY: st.tuples(escalation_count, response_quality)

            INVARIANT: escalation_count >= 2 → no escalation

            RADII: 50 examples explores limit boundary (0-5 counts × 4 quality levels)
            """
            manager = EscalationManager(db_session)
            request_id = "test-request-123"

            # Set escalation count
            manager.request_escalations[request_id] = escalation_count

            should_escalate, reason, target_tier = manager.should_escalate(
                current_tier=CognitiveTier.STANDARD,
                response_quality=response_quality,
                error=None,
                rate_limited=False,
                request_id=request_id
            )

            if escalation_count >= MAX_ESCALATION_LIMIT:
                # At max limit, must not escalate
                assert should_escalate == False, \
                    f"Escalation count {escalation_count} >= {MAX_ESCALATION_LIMIT} should block escalation"
            else:
                # Below limit, should escalate on quality breach
                assert should_escalate == True, \
                    f"Escalation count {escalation_count} < {MAX_ESCALATION_LIMIT} should allow escalation"


    class TestConfidenceThresholdInvariants:
        """Property-based tests for confidence-based escalation."""

        @given(
            confidence=floats(min_value=0.0, max_value=1.0, allow_nan=False, allow_infinity=False)
        )
        @settings(**HYPOTHESIS_SETTINGS_ESCALATION)
        def test_confidence_threshold_breach_triggers_escalation(
            self, db_session, confidence: float
        ):
            """
            PROPERTY: Confidence < 0.7 triggers escalation

            STRATEGY: st.floats for confidence in [0, 1]

            INVARIANT: confidence < 0.7 → escalate, confidence >= 0.7 → no escalate

            RADII: 50 examples explores confidence boundary around 0.7 threshold
            """
            manager = EscalationManager(db_session)

            should_escalate, reason, target_tier = manager.should_escalate(
                current_tier=CognitiveTier.STANDARD,
                response_quality=None,
                error=None,
                rate_limited=False,
                confidence=confidence
            )

            if confidence < 0.7:
                # Below threshold MUST escalate
                assert should_escalate == True, \
                    f"Confidence {confidence:.2f} < 0.7 should trigger escalation"
                assert reason == EscalationReason.LOW_CONFIDENCE, \
                    f"Reason must be LOW_CONFIDENCE, got {reason}"
            else:
                # At or above threshold MUST NOT escalate
                assert should_escalate == False, \
                    f"Confidence {confidence:.2f} >= 0.7 should NOT trigger escalation"


    class TestTierOrderInvariants:
        """Property-based tests for tier progression order."""

        @given(
            current_tier=sampled_from([
                CognitiveTier.MICRO,
                CognitiveTier.STANDARD,
                CognitiveTier.VERSATILE,
                CognitiveTier.HEAVY
            ])
        )
        @settings(**HYPOTHESIS_SETTINGS_ESCALATION)
        def test_escalation_progresses_through_tier_order(
            self, db_session, current_tier: CognitiveTier
        ):
            """
            PROPERTY: Escalation follows tier order: MICRO → STANDARD → VERSATILE → HEAVY → COMPLEX

            STRATEGY: st.sampled_from for all escalatable tiers

            INVARIANT: target_tier is always next in TIER_ORDER

            RADII: 50 examples (4 tiers × repeated testing = deterministic)
            """
            manager = EscalationManager(db_session)

            should_escalate, reason, target_tier = manager.should_escalate(
                current_tier=current_tier,
                response_quality=70,  # Below threshold to trigger escalation
                error=None,
                rate_limited=False
            )

            if should_escalate:
                # Verify target is next in order
                current_index = TIER_ORDER.index(current_tier)
                expected_target = TIER_ORDER[current_index + 1]
                assert target_tier == expected_target, \
                    f"Expected {expected_target.value}, got {target_tier.value}"
    ```

    Run tests in RED phase:
    ```bash
    cd backend
    pytest tests/property_tests/llm/test_tier_escalation_invariants.py -v
    ```

    Expected: Tests should pass (EscalationManager implementation is already correct)
  </action>
  <verify>
    - Test file created at backend/tests/property_tests/llm/test_tier_escalation_invariants.py
    - pytest tests/property_tests/llm/test_tier_escalation_invariants.py -v runs (green if EscalationManager correct)
  </verify>
  <done>
    Tier escalation property tests created and passing (GREEN phase complete)
  </done>
</task>

<task type="auto">
  <name>GREEN: Verify tests pass and document any bugs found</name>
  <files>backend/tests/property_tests/llm/test_tier_escalation_invariants.py</files>
  <action>
    Run the property tests and verify results:

    ```bash
    cd backend
    pytest tests/property_tests/llm/test_tier_escalation_invariants.py -v --tb=short
    ```

    If any tests fail:
    1. Analyze failure to identify bug in EscalationManager
    2. Fix bug in escalation_manager.py
    3. Add VALIDATED_BUG docstring section documenting the fix
    4. Re-run tests until all pass

    Example VALIDATED_BUG section (if bug found):
    ```python
    """
    VALIDATED_BUG: Escalation triggered at quality<70 instead of <80
    Root cause: Incorrect threshold in ESCALATION_THRESHOLDS config
    Fixed in commit abc123
    """
    ```

    Count total tests and Hypothesis examples:
    - 7 test classes × ~2 tests each = ~14 tests
    - 14 tests × 50 max_examples = ~700 Hypothesis examples
  </action>
  <verify>
    - pytest shows all tests passing (100% pass rate)
    - Test count = 10-15 tests
    - Hypothesis examples generated = 500-750
  </verify>
  <done>
    All tier escalation property tests passing, invariants validated
  </done>
</task>

<task type="auto">
  <name>REFACTOR: Extract common patterns and add documentation</name>
  <files>backend/tests/property_tests/llm/test_tier_escalation_invariants.py</files>
  <action>
    Refactor test file for clarity and maintainability:

    1. **Extract common helper method** for tier order verification:
       ```python
       def _verify_next_tier(self, current_tier: CognitiveTier, target_tier: Optional[CognitiveTier]):
           """Verify target_tier is next in TIER_ORDER after current_tier."""
           if target_tier is None:
               return False
           current_index = TIER_ORDER.index(current_tier)
           expected_target = TIER_ORDER[current_index + 1]
           return target_tier == expected_target
       ```

    2. **Add module docstring** with test counts and invariant summary

    3. **Add __all__** exports for test classes

    4. **Ensure all tests have @settings decorator** with HYPOTHESIS_SETTINGS_ESCALATION

    5. **Add example decorator** for edge cases that previously caused bugs (if any found)

    Verify refactored code:
    ```bash
    cd backend
    pytest tests/property_tests/llm/test_tier_escalation_invariants.py -v
    ```
  </action>
  <verify>
    - Tests still passing after refactoring
    - Code has helper methods extracted
    - Module docstring complete with invariant summary
  </verify>
  <done>
    Test file refactored for maintainability, all tests passing
  </done>
</task>

## Verification

Run complete tier escalation test suite:
```bash
cd backend
pytest tests/property_tests/llm/test_tier_escalation_invariants.py -v --tb=short
```

Expected results:
- 10-15 property tests created
- All tests passing (100% pass rate)
- 500-750 Hypothesis examples generated (50 × test count)
- Test file follows existing patterns from test_escalation_manager.py

## Success Criteria

- [x] test_tier_escalation_invariants.py created with 10-15 property tests
- [x] All 8 escalation invariants covered (quality, cooldown, max tier, rate limit, max limit, confidence, tier order, cooldown expiry)
- [x] Tests use max_examples=50 per research recommendation
- [x] All tests passing (GREEN phase)
- [x] Refactored with helper methods and documentation

## Output

After completion, create `.planning/phases/126-llm-property-tests/126-02-SUMMARY.md` with:
- Test count and class breakdown
- Hypothesis examples generated
- Invariants validated list
- Any bugs found and fixed
- Test execution time and results
