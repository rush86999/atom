---
phase: 157-edge-cases-integration-testing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/tests/accessibility/accessibility-workflows.test.tsx
  - mobile/tests/accessibility/accessibility-mobile.test.tsx
  - menubar/src-tauri/tests/accessibility_test.rs
  - backend/tests/integration/test_a11y_api_response_headers.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Frontend components pass WCAG 2.1 AA compliance with jest-axe"
    - "Mobile components are accessible with React Native Accessibility API"
    - "Desktop Tauri app has keyboard navigation and screen reader support"
    - "Backend API responses include accessibility headers where applicable"
    - "Interactive elements are keyboard navigable with visible focus indicators"
  artifacts:
    - path: "frontend-nextjs/tests/accessibility/accessibility-workflows.test.tsx"
      provides: "Workflow-based accessibility tests"
      contains: "jest-axe|WCAG|keyboard.*navigable|screen.*reader"
    - path: "mobile/tests/accessibility/accessibility-mobile.test.tsx"
      provides: "React Native accessibility tests"
      contains: "accessibilityLabel|accessibilityHint|accessibilityRole"
    - path: "menubar/src-tauri/tests/accessibility_test.rs"
      provides: "Tauri accessibility tests"
      contains: "keyboard.*navigation|focus|a11y"
    - path: "backend/tests/integration/test_a11y_api_response_headers.py"
      provides: "API accessibility header tests"
      contains: "test_a11y_headers|Accessibility|header"
  key_links:
    - from: "frontend-nextjs/tests/accessibility.test.tsx"
      to: "frontend-nextjs/tests/accessibility/accessibility-workflows.test.tsx"
      via: "existing jest-axe configuration"
      pattern: "jest-axe|toHaveNoViolations|configureAxe"
    - from: "mobile/src/components/*"
      to: "mobile/tests/accessibility/accessibility-mobile.test.tsx"
      via: "React Native Accessibility API tests"
      pattern: "accessibilityLabel|accessibilityHint|accessibilityRole|accessibilityState"
    - from: "menubar/src-tauri/src/*"
      to: "menubar/src-tauri/tests/accessibility_test.rs"
      via: "Tauri window and keyboard accessibility tests"
      pattern: "keyboard.*shortcut|tab.*navigation|focus"
    - from: "backend/api/*"
      to: "backend/tests/integration/test_a11y_api_response_headers.py"
      via: "API response header validation for a11y"
      pattern: "response.*headers|Accessibility|Content-Type"
---

<objective>
Create comprehensive accessibility compliance tests to WCAG 2.1 AA across all platforms (frontend React, mobile React Native, desktop Tauri, backend API headers).

Purpose: EDGE-03 requirement - Accessibility compliance tested to WCAG 2.1 AA standards including keyboard navigation, screen reader compatibility, ARIA attributes, and proper semantic HTML. Edge cases include dynamic content, modals, form validation, and error announcements.

Output: Accessibility test suites for all platforms validating WCAG compliance with screen reader and keyboard navigation support.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/157-edge-cases-integration-testing/157-RESEARCH.md

# Existing accessibility test infrastructure
@frontend-nextjs/tests/accessibility.test.tsx
@frontend-nextjs/tests/accessibility-config.ts
@frontend-nextjs/tests/setup.ts
</context>

<tasks>

<task type="auto">
  <name>Create workflow-based accessibility tests for frontend</name>
  <files>frontend-nextjs/tests/accessibility/accessibility-workflows.test.tsx</files>
  <action>
    Create workflow-based accessibility tests covering real user scenarios:

    1. Test file: frontend-nextjs/tests/accessibility/accessibility-workflows.test.tsx

    2. Workflow tests to include (build on existing accessibility.test.tsx):
       - test_keyboard_navigation_through_canvas: Tab order flows logically through canvas
       - test_screen_reader_announces_form_errors: ARIA live regions announce validation errors
       - test_modal_focus_trap: Focus trapped in modal when open
       - test_modal_focus_restored_on_close: Focus returns to trigger after modal closes
       - test_dynamic_content_announced: Async updates announced to screen readers
       - test_custom_interactive_elements_have_aria_roles: Buttons and links have proper roles
       - test_images_have_alt_text_or_presentation_role: All images accessible
       - test_color_contrast_meets_wcag_aa: Text contrast >= 4.5:1
       - test_focus_indicators_visible: All interactive elements show focus state
       - test_form_inputs_have_labels: All form inputs have associated labels

    3. Use existing jest-axe configuration from accessibility-config.ts:
       - WCAG 2.1 AA rules enabled
       - toHaveNoViolations matcher available
       - configureAxe with custom rules

    4. Pattern from RESEARCH.md (lines 246-285):
       ```tsx
       import { axe, toHaveNoViolations } from 'jest-axe';

       describe('Workflow accessibility', () => {
         it('should not have WCAG violations', async () => {
           const { container } = render(<CanvasWorkflow />);
           const results = await axe(container);
           expect(results).toHaveNoViolations();
         });

         it('should be keyboard navigable', () => {
           render(<CanvasWorkflow />);
           const button = screen.getByRole('button');
           button.focus();
           expect(button).toHaveFocus();
         });
       });
       ```

    DO NOT modify component accessibility - only test existing accessibility. DO NOT create new accessibility utilities - use existing jest-axe setup.
  </action>
  <verify>cd /Users/rushiparikh/projects/atom/frontend-nextjs && npm test -- accessibility-workflows.test.tsx --passWithNoTests</verify>
  <done>All accessibility workflow tests pass (keyboard, screen reader, ARIA, contrast)</done>
</task>

<task type="auto">
  <name>Create React Native accessibility tests for mobile</name>
  <files>mobile/tests/accessibility/accessibility-mobile.test.tsx</files>
  <action>
    Create accessibility tests for React Native components:

    1. Test file: mobile/tests/accessibility/accessibility-mobile.test.tsx

    2. Tests to include:
       - test_buttons_have_accessibility_label: All buttons are labeled
       - test_images_have_accessibility_label: Images have meaningful labels
       - test_text_fields_have_accessibility_label: Inputs have labels
       - test_touch_targets_minimum_size: Touch targets >= 44x44dp
       - test_screen_reader_announces_state_changes: State changes announced
       - test_actions_have_accessibility_hint: Actions provide hints
       - test_groups_have_accessible_heading: Headings mark logical sections
       - test_switch_components_have_accessible_state: Toggles announce state
       - test_lists_announce_item_count: List items announce count
       - test_progress_indicators_are_accessible: Loading states announced

    3. Use React Native Testing Library accessibility queries:
       - getByRole, getByLabelText, getByA11yLabel
       - toBeAccessible() custom matcher if available

    4. Pattern for React Native accessibility tests:
       ```tsx
       import { render, screen } from '@testing-library/react-native';

       describe('Mobile accessibility', () => {
         it('should have accessibility label on buttons', () => {
           render(<Button onPress={() => {}}>Submit</Button>);
           const button = screen.getByRole('button');
           expect(button.props.accessibilityLabel).toBeDefined();
         });

         it('should announce loading state', () => {
           render(<LoadingIndicator isLoading={true} />);
           const indicator = screen.getByRole('progressbar');
           expect(indicator.props.accessibilityState).toEqual({ busy: true });
         });
       });
       ```

    DO NOT modify component accessibility attributes - only test existing accessibility. DO NOT create new accessibility components - test existing ones.
  </action>
  <verify>cd /Users/rushiparikh/projects/atom/mobile && npm test -- accessibility-mobile.test.tsx --passWithNoTests</verify>
  <done>All mobile accessibility tests pass (labels, touch targets, state announcements)</done>
</task>

<task type="auto">
  <name>Create Tauri desktop and API accessibility tests</name>
  <files>menubar/src-tauri/tests/accessibility_test.rs, backend/tests/integration/test_a11y_api_response_headers.py</files>
  <action>
    Create two test files:

    1. menubar/src-tauri/tests/accessibility_test.rs:
       - test_keyboard_navigation_works: Tab navigation between UI elements
       - test_shortcuts_are_documented: Keyboard shortcuts documented in help
       - test_focus_visible: Focus indicators visible on all interactive elements
       - test_menu_items_have_labels: All menu items have text labels
       - test dialogs_are_accessible: Dialogs can be navigated with keyboard
       - test_error_messages_are_announced: Errors visible and readable
       - test_high_contrast_mode_support: UI works in high contrast mode

       Pattern for Tauri accessibility tests:
       ```rust
       #[test]
       fn test_keyboard_navigation_works() {
           // Verify tab navigation works
           // Check that all interactive elements receive focus
       }
       ```

    2. backend/tests/integration/test_a11y_api_response_headers.py:
       - test_api_returns_content_type_header: Content-Type header present
       - test_api_returns_accessible_error_responses: Error responses have clear messages
       - test_api_supports_head_requests_for_resources: HEAD method supported
       - test_api_rate_limit_headers_are_accessible: Rate limit info in headers
       - test_api_pagination_headers_are_clear: Pagination info accessible
       - test_api_response_language_is_consistent: Content-Language header
       - test_api_alt_text_in_image_endpoints: Image endpoints include alt text

       Use TestClient from FastAPI to verify headers:
       ```python
       def test_api_returns_content_type_header(client):
           """Test that API responses include Content-Type header."""
           response = client.get('/api/v1/agents')
           assert 'Content-Type' in response.headers
           assert 'application/json' in response.headers['Content-Type']
       ```

    DO NOT modify Tauri window accessibility code - only test existing behavior. DO NOT modify API responses - only test headers and structure.
  </action>
  <verify>cd /Users/rushiparikh/projects/atom/menubar/src-tauri && cargo test accessibility_test && cd /Users/rushiparikh/projects/atom/backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/integration/test_a11y_api_response_headers.py -v</verify>
  <done>Tauri accessibility tests pass, API accessibility header tests pass</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. cd /Users/rushiparikh/projects/atom/frontend-nextjs && npm test -- accessibility-workflows.test.tsx --passWithNoTests
2. cd /Users/rushiparikh/projects/atom/mobile && npm test -- accessibility-mobile.test.tsx --passWithNoTests
3. cd /Users/rushiparikh/projects/atom/menubar/src-tauri && cargo test accessibility_test
4. cd /Users/rushiparikh/projects/atom/backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/integration/test_a11y_api_response_headers.py -v

All tests should pass with proper WCAG 2.1 AA compliance coverage.
</verification>

<success_criteria>
1. Frontend accessibility tests cover keyboard navigation, screen reader, ARIA, modal focus, dynamic content
2. Mobile accessibility tests cover labels, touch targets, state announcements, progress indicators
3. Tauri accessibility tests cover keyboard shortcuts, focus visibility, menu labels, error messages
4. API accessibility tests verify proper headers, error messages, and pagination info
5. All platforms demonstrate WCAG 2.1 AA compliance with automated tests
</success_criteria>

<output>
After completion, create `.planning/phases/157-edge-cases-integration-testing/157-03-SUMMARY.md`
</output>
