---
phase: 72-api-data-layer-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_rest_routes_coverage.py
  - backend/tests/api/conftest.py
autonomous: true

must_haves:
  truths:
    - "All critical REST API routes have tests covering 80%+ of request handling paths"
    - "API request validation and error cases are tested with edge cases (missing fields, invalid types)"
    - "API response structures and status codes are validated in tests"
    - "Tests cover both success and failure paths for CRUD operations"
    - "API tests use proper authentication fixtures (user, admin, dev-token)"
  artifacts:
    - path: "backend/tests/api/test_rest_routes_coverage.py"
      provides: "Comprehensive REST API route tests (agent, canvas, workflow, project routes)"
      min_lines: 400
      exports: ["TestAgentRoutes", "TestCanvasRoutes", "TestWorkflowRoutes", "TestProjectRoutes"]
    - path: "backend/tests/api/conftest.py"
      provides: "Shared API test fixtures (client with auth, request factories)"
      min_lines: 100
  key_links:
    - from: "test_rest_routes_coverage.py"
      to: "api/agent_routes.py"
      via: "FastAPI TestClient"
      pattern: "client\\.(get|post|put|delete)\\(.*/api/"
    - from: "test_rest_routes_coverage.py"
      to: "api/canvas_routes.py"
      via: "FastAPI TestClient"
      pattern: "canvas"
    - from: "test_rest_routes_coverage.py"
      to: "api/workflow_*_routes.py"
      via: "FastAPI TestClient"
      pattern: "workflow"
---

<objective>
Achieve 80%+ test coverage for REST API routes (excluding auth and WebSocket).

**Purpose:**
The API layer is the primary interface for frontend and external services. Currently, many API route files have insufficient test coverage. This plan ensures critical REST endpoints (agents, canvas, workflows, projects) are thoroughly tested for request handling, response validation, and error cases.

**Output:**
- Comprehensive test file `test_rest_routes_coverage.py` with 80%+ coverage on target routes
- Enhanced API fixtures in `conftest.py` for authenticated requests
- Tests covering CRUD operations, validation errors, and edge cases

**Coverage Targets:**
- Agent routes (api/agent_routes.py): 80%+
- Canvas routes (api/canvas_routes.py): 80%+
- Workflow routes (api/workflow_*.py): 80%+
- Project routes (api/project_routes.py): 80%+
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-api-data-layer-coverage/72-RESEARCH.md
@.planning/ROADMAP.md

# Existing patterns to follow
@backend/tests/api/test_api_routes_coverage.py
@backend/tests/api/test_agent_routes.py
@backend/tests/api/test_canvas_routes.py
@backend/tests/api/conftest.py
@backend/tests/conftest.py

# API routes to test
@backend/api/agent_routes.py
@backend/api/canvas_routes.py
@backend/api/workflow_template_routes.py
@backend/api/workflow_collaboration.py
@backend/api/project_routes.py
</context>

<tasks>

<task type="auto">
  <name>Create comprehensive REST API route tests</name>
  <files>backend/tests/api/test_rest_routes_coverage.py</files>
  <action>
Create a new test file `backend/tests/api/test_rest_routes_coverage.py` with comprehensive test coverage for REST API routes. Follow the existing test patterns from `test_api_routes_coverage.py` and `test_agent_routes.py`.

**Test Structure:**

1. **TestAgentRoutes class** - Test agent CRUD operations:
   - `test_list_agents_success` - GET /api/agents returns list with pagination
   - `test_list_agents_empty` - GET /api/agents with no agents returns empty list
   - `test_get_agent_by_id_success` - GET /api/agents/{id} returns agent details
   - `test_get_agent_by_id_not_found` - GET /api/agents/{id} with invalid ID returns 404
   - `test_create_agent_success` - POST /api/agents with valid data creates agent
   - `test_create_agent_validation_error` - POST /api/agents with missing required field returns 422
   - `test_create_agent_invalid_type` - POST /api/agents with invalid confidence_score type returns 422
   - `test_update_agent_success` - PUT /api/agents/{id} updates agent
   - `test_update_agent_not_found` - PUT /api/agents/{id} with invalid ID returns 404
   - `test_delete_agent_success` - DELETE /api/agents/{id} deletes agent
   - `test_delete_agent_not_found` - DELETE /api/agents/{id} with invalid ID returns 404

2. **TestCanvasRoutes class** - Test canvas presentation operations:
   - `test_create_canvas_success` - POST /api/canvas with valid components
   - `test_create_canvas_empty_components` - POST /api/canvas with empty components returns 422
   - `test_get_canvas_success` - GET /api/canvas/{id} returns canvas data
   - `test_get_canvas_not_found` - GET /api/canvas/{id} returns 404
   - `test_update_canvas_success` - PUT /api/canvas/{id} updates canvas
   - `test_submit_canvas_form_success` - POST /api/canvas/{id}/submit with valid form data
   - `test_submit_canvas_form_validation` - POST /api/canvas/{id}/submit with invalid data returns 422
   - `test_canvas_audit_logging` - Verify canvas actions are audited

3. **TestWorkflowRoutes class** - Test workflow template and execution routes:
   - `test_list_workflow_templates_success` - GET /api/workflows/templates
   - `test_create_workflow_template_success` - POST /api/workflows/templates
   - `test_create_workflow_template_validation` - POST with invalid workflow structure returns 422
   - `test_execute_workflow_success` - POST /api/workflows/{id}/execute
   - `test_execute_workflow_not_found` - POST /api/workflows/{id}/execute with invalid ID returns 404
   - `test_get_workflow_execution_status` - GET /api/workflows/executions/{id}
   - `test_workflow_collaboration_permissions` - Verify governance checks on workflow sharing

4. **TestProjectRoutes class** - Test project management routes:
   - `test_list_projects_success` - GET /api/projects
   - `test_create_project_success` - POST /api/projects with valid data
   - `test_create_project_validation` - POST with missing name returns 422
   - `test_get_project_by_id_success` - GET /api/projects/{id}
   - `test_update_project_success` - PUT /api/projects/{id}
   - `test_delete_project_success` - DELETE /api/projects/{id}

**Fixtures to use:**
- `client` - TestClient with authentication override (from conftest.py)
- `db_session` - Database session with transaction rollback
- `AgentFactory`, `UserFactory`, `CanvasAuditFactory` - For test data

**Authentication Pattern:**
```python
@pytest.fixture
def override_auth(self):
    from core.auth import get_current_user
    from core.models import User
    mock_user = User(
        id="test-user-id",
        email="test@example.com",
        role="super_admin",
        status=UserStatus.ACTIVE.value,
        email_verified=True
    )
    app.dependency_overrides[get_current_user] = lambda: mock_user
    yield
    app.dependency_overrides = {}
```

**Validation Tests to Include:**
- Missing required fields (422 errors)
- Invalid types (string instead of int, etc.)
- Empty strings where not allowed
- Negative values for positive-only fields
- Malformed JSON payloads
- SQL injection attempts (verify sanitization)
- XSS payloads in string fields
- UUID format validation for ID parameters

**DO NOT:**
- Create actual network connections (use TestClient)
- Skip authentication tests (use auth fixtures)
- Test only happy path (include error cases)
- Hardcode agent/user IDs (use factories with UUIDs)
- Use `client_no_auth` without also testing authenticated paths
</action>
  <verify>
Run: `PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/api/test_rest_routes_coverage.py -v --cov=api/agent_routes --cov=api/canvas_routes --cov=api/workflow_template_routes --cov=api/project_routes --cov-report=term-missing`

Expected: All tests pass, coverage shows 80%+ for targeted routes
  </verify>
  <done>
- All test classes pass with 100% success rate
- Coverage report shows 80%+ for agent_routes.py, canvas_routes.py, workflow routes, project_routes.py
- Tests include both success and failure paths
- Edge cases (validation errors, invalid inputs) are covered
- Authentication is properly mocked in all tests
  </done>
</task>

<task type="auto">
  <name>Enhance API test fixtures for authenticated requests</name>
  <files>backend/tests/api/conftest.py</files>
  <action>
Enhance `backend/tests/api/conftest.py` with additional fixtures for API testing.

**Fixtures to Add:**

1. **`authenticated_client`** - TestClient with pre-configured auth override:
   - Creates mock super_admin user
   - Overrides get_current_user dependency
   - Provides ready-to-use client for authenticated requests

2. **`api_request_factory`** - Factory for creating API requests:
   - `create_agent_request(name, category, status)` - Returns valid agent creation payload
   - `create_canvas_request(components, layout)` - Returns valid canvas payload
   - `create_workflow_request(name, steps)` - Returns valid workflow template payload
   - `create_project_request(name, description)` - Returns valid project payload

3. **`validation_error_test_data`** - Fixture providing invalid request payloads:
   - Missing required fields
   - Invalid data types
   - Empty strings
   - Negative values
   - Malformed data structures

Pattern to follow from existing `test_api_routes_coverage.py`:
```python
@pytest.fixture
def override_auth(self):
    from core.auth import get_current_user
    from core.models import User
    mock_user = User(
        id="test-user-id",
        email="test@example.com",
        role="super_admin",
        status=UserStatus.ACTIVE.value,
        email_verified=True
    )
    app.dependency_overrides[get_current_user] = lambda: mock_user
    yield
    app.dependency_overrides = {}
```

**DO NOT:**
- Create fixtures that duplicate existing conftest.py functionality
- Use hardcoded credentials (use factory-generated data)
- Create fixtures that depend on external services
  </done>
</task>

<task type="auto">
  <name>Run full API test suite and verify coverage</name>
  <files>backend/tests/api/test_rest_routes_coverage.py</files>
  <action>
Run the complete API test suite with coverage reporting to verify 80%+ coverage target is met.

**Steps:**
1. Run all new tests: `pytest backend/tests/api/test_rest_routes_coverage.py -v`
2. Run with coverage: `pytest backend/tests/api/test_rest_routes_coverage.py --cov=api/agent_routes --cov=api/canvas_routes --cov=api/workflow_template_routes --cov=api/workflow_collaboration --cov=api/project_routes --cov-report=html --cov-report=json`
3. Verify coverage report shows 80%+ for all target files
4. If any file is below 80%, add tests for uncovered lines (typically error handling, edge cases)
5. Generate final coverage report in `backend/tests/coverage_reports/`

**Uncovered Line Patterns to Address:**
- Exception handling blocks (try/except)
- Early returns in conditional branches
- Default values in optional parameters
- Validation errors in Pydantic models
- Edge cases in relationship queries

**Target Coverage Breakdown:**
- api/agent_routes.py: 80%+ (currently ~40-60%)
- api/canvas_routes.py: 80%+ (currently ~50-70%)
- api/workflow_template_routes.py: 80%+ (currently ~30-50%)
- api/project_routes.py: 80%+ (currently ~40-60%)
  </done>
</task>

</tasks>

<verification>
After completion, verify:

1. **Test Execution:** All tests in `test_rest_routes_coverage.py` pass without errors
2. **Coverage Metric:** `pytest --cov` shows 80%+ coverage for target API files
3. **Test Diversity:** Tests include success, failure, validation, and edge cases
4. **Fixture Quality:** New fixtures in conftest.py are reusable and well-documented
5. **No Regressions:** Existing API tests still pass

**Verification Command:**
```bash
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
  backend/tests/api/test_rest_routes_coverage.py \
  -v --cov=api/agent_routes --cov=api/canvas_routes \
  --cov=api/workflow_template_routes --cov=api/project_routes \
  --cov-report=term-missing --cov-report=json
```

**Expected Output:**
- All tests PASSED
- Coverage >= 80% for each target file
- No uncovered critical paths (authentication, validation, error handling)
</verification>

<success_criteria>
1. `test_rest_routes_coverage.py` exists with 50+ test methods covering 4 route categories
2. Coverage report shows 80%+ for agent_routes.py, canvas_routes.py, workflow routes, project_routes.py
3. All tests pass consistently (no flaky tests)
4. Tests use proper authentication fixtures (not unauthenticated only)
5. Edge cases and validation errors are covered (not just happy path)
6. Coverage JSON report generated in `backend/tests/coverage_reports/metrics/coverage.json`
</success_criteria>

<output>
After completion, create `.planning/phases/72-api-data-layer-coverage/72-01-SUMMARY.md` with:
- Test count and coverage metrics achieved
- List of API files now at 80%+ coverage
- Any uncovered lines requiring investigation
- Recommendations for 72-02 (auth/WebSocket)
</output>
