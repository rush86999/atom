# Plan 264-02: Fill Remaining Coverage Gaps

**Phase:** 264 - Coverage Expansion Wave 7 (FINAL)
**Plan:** 02 - Fill Remaining Coverage Gaps
**Type:** Auto (Autonomous Execution)
**Duration:** 45-60 minutes
**Dependencies:** Phase 264-01 (imports fixed, tests execute)

---

## Objective

Create gap-filling tests for low-coverage files, critical paths, and error handling. Target +5-10 percentage points coverage increase toward 80% goal.

---

## Context

After fixing import issues (Plan 264-01), tests execute but coverage gaps remain. This plan creates targeted tests for:

1. **Low-coverage files:** Files with <30% coverage
2. **Critical paths:** Business logic not yet tested
3. **Error handling:** Exception paths not covered
4. **Edge cases:** Boundary conditions not tested

**Target:** Add ~50 high-impact tests for maximum coverage gain.

---

## Tasks

### Task 1: Measure Current Coverage
**Type:** auto
**Command:**
```bash
cd backend
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/ --cov=core --cov=api --cov=tools --cov-report=json --cov-report=term-missing -q
```

**Steps:**
1. Run coverage measurement on all tests
2. Parse coverage.json for per-file coverage
3. Identify files with <30% coverage
4. Identify files with high line count but low coverage (high impact)
5. Create priority list: (lines uncovered) / (total lines) * (file importance)

**Expected Outcome:**
- Coverage report generated (coverage.json, term output)
- List of low-coverage files prioritized by impact
- Current overall coverage percentage measured

---

### Task 2: Analyze Coverage Gaps
**Type:** auto
**Target:** Top 10-15 high-impact files

**Analysis Steps:**
1. Read coverage.json for each prioritized file
2. Identify uncovered lines:
   - Missing branch coverage
   - Missing exception handling
   - Missing edge cases
   - Missing initialization/cleanup
3. Categorize gaps:
   - **Critical paths:** Business logic, data validation
   - **Error paths:** Exception handling, error recovery
   - **Edge cases:** Boundary conditions, null/empty handling
   - **Configuration:** Conditional logic, feature flags

**Expected Outcome:**
- Gap analysis document listing:
  - File name
  - Current coverage percentage
  - Uncovered line ranges
  - Gap category (critical/error/edge/config)
  - Test suggestions

---

### Task 3: Create Gap-Filling Tests for Core Services
**Type:** auto
**Target:** core/ services with coverage gaps

**Priority Files (examples):**
- `core/llm/llm_service.py` - LLM routing logic
- `core/agent_governance_service.py` - Governance checks
- `core/governance_cache.py` - Cache operations
- `core/episode_retrieval_service.py` - Episode retrieval modes
- `core/agent_graduation_service.py` - Graduation criteria

**Test Patterns:**
```python
# Example: LLM service gap-filling test
def test_llm_service_provider_fallback():
    """Test fallback to secondary provider on failure."""
    # Mock primary provider failure
    # Verify fallback to secondary
    # Assert response returned

def test_llm_service_token_limit_handling():
    """Test token limit exceeded handling."""
    # Mock token limit response
    # Verify truncation or error handling
    # Assert appropriate behavior

# Example: Governance cache gap-filling test
def test_governance_cache_invalidation():
    """Test cache invalidation on agent update."""
    # Warm cache with agent data
    # Update agent
    # Verify cache invalidated
    # Assert fresh data returned
```

**Tests to Create:** ~20 tests across 5 core service files

**Expected Outcome:**
- 20 new tests covering critical paths in core services
- Focus on error handling and edge cases
- All tests pass with proper mocking

---

### Task 4: Create Gap-Filling Tests for API Routes
**Type:** auto
**Target:** api/ routes with coverage gaps

**Priority Files (examples):**
- `api/atom_agent_endpoints.py` - Agent chat/streaming endpoints
- `api/canvas_routes.py` - Canvas presentation endpoints
- `api/admin_routes.py` - Admin operations
- `api/agent_control_routes.py` - Agent lifecycle management

**Test Patterns:**
```python
# Example: API endpoint gap-filling test
def test_agent_streaming_timeout_handling():
    """Test timeout handling in streaming endpoint."""
    # Mock slow LLM response
    # Verify timeout exception caught
    # Assert error response sent

def test_canvas_submission_validation_error():
    """Test validation error handling in canvas submit."""
    # Submit invalid canvas data
    # Verify 400 error returned
    # Assert validation error message

# Example: Admin route gap-filling test
def test_admin_bulk_operation_partial_failure():
    """Test partial failure handling in bulk operations."""
    # Mock some operations succeed, some fail
    # Verify partial success response
    # Assert error details for failed items
```

**Tests to Create:** ~15 tests across 4 API route files

**Expected Outcome:**
- 15 new tests covering error paths in API routes
- Focus on validation, error handling, partial failures
- All tests pass with API client mocking

---

### Task 5: Create Gap-Filling Tests for Tools
**Type:** auto
**Target:** tools/ with coverage gaps

**Priority Files (examples):**
- `tools/browser_tool.py` - Browser automation error handling
- `tools/device_tool.py` - Device capability checks
- `tools/canvas_tool.py` - Canvas rendering edge cases
- `tools/atom_cli_skill_wrapper.py` - CLI subprocess errors

**Test Patterns:**
```python
# Example: Browser tool gap-filling test
def test_browser_tool_navigation_timeout():
    """Test timeout handling in page navigation."""
    # Mock Playwright timeout
    # Verify timeout exception caught
    # Assert error returned, session cleaned up

def test_device_tool_camera_permission_denied():
    """Test camera permission denied handling."""
    # Mock permission denied error
    # Verify graceful error handling
    # Assert user-friendly error message

# Example: Canvas tool gap-filling test
def test_canvas_tool_large_dataset_rendering():
    """Test handling of large canvas datasets."""
    # Mock large dataset (>1000 items)
    # Verify pagination or sampling applied
    # Assert response size limited
```

**Tests to Create:** ~10 tests across 4 tool files

**Expected Outcome:**
- 10 new tests covering error paths in tools
- Focus on timeout, permissions, large datasets
- All tests pass with proper mocking

---

### Task 6: Create Property-Based Tests for Critical Invariants
**Type:** auto
**Target:** Critical system invariants

**Priority Invariants:**
- Agent governance: maturity checks never bypassed
- Episode storage: segments always linked to valid episode
- Cache consistency: cache updates always invalidate stale data
- LLM routing: provider selection always respects tier

**Test Patterns:**
```python
# Example: Property-based test for governance
@given(st.builds(AgentRegistry, maturity=st.sampled_from(['STUDENT', 'INTERN', 'SUPERVISED', 'AUTONOMOUS'])))
def test_governance_maturity_enforcement(agent):
    """Test that maturity checks are never bypassed."""
    # For each action complexity level
    # Verify maturity requirement enforced
    # Assert no bypass possible

# Example: Property-based test for cache
@given(st.integers(min_value=1, max_value=1000))
def test_cache_invalidation_consistency(agent_id):
    """Test that cache updates always invalidate stale data."""
    # Write to cache
    # Update agent
    # Verify cache returns fresh data
    # Assert stale data never returned
```

**Tests to Create:** ~5 property-based tests

**Expected Outcome:**
- 5 property-based tests covering critical invariants
- Tests use Hypothesis for comprehensive coverage
- All tests pass, invariants verified

---

## Success Criteria

- [ ] Current coverage measured (baseline)
- [ ] Coverage gap analysis completed
- [ ] ~50 new tests created (20 core + 15 API + 10 tools + 5 property)
- [ ] All new tests pass (100% pass rate)
- [ ] Coverage increased by +5-10 percentage points
- [ ] Coverage report shows improvement
- [ ] No regressions in existing tests

---

## Verification

**After test creation:**
```bash
# Run new tests
cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_new_file.py -v

# Measure coverage
cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/ --cov=core --cov=api --cov=tools --cov-report=term-missing -q | grep -E "TOTAL|core/|api/|tools/"

# Compare with baseline
diff coverage_before.json coverage_after.json
```

**Coverage comparison:**
```bash
# Parse coverage JSON
python3 << 'EOF'
import json

before = json.load(open('coverage_before.json'))
after = json.load(open('coverage_after.json'))

# Calculate overall coverage before/after
# Identify files with >5% improvement
# Print summary
EOF
```

---

## Output

- Coverage baseline report (coverage_before.json)
- Coverage gap analysis document
- New test files:
  - `tests/core/test_core_services_coverage_gaps.py` (20 tests)
  - `tests/api/test_api_routes_coverage_gaps.py` (15 tests)
  - `tests/tools/test_tools_coverage_gaps.py` (10 tests)
  - `tests/property_tests/test_critical_invariants.py` (5 tests)
- Coverage after report (coverage_after.json)
- Coverage improvement summary

---

## Notes

**Test Quality:**
- Focus on high-impact gaps (large files with low coverage)
- Test error paths and edge cases (not just happy paths)
- Use proper mocking to isolate units under test
- Property-based tests for critical invariants

**Coverage Strategy:**
- Target files with >100 lines but <30% coverage
- Prioritize business logic over configuration
- Error paths often have 0% coverage (easy wins)
- Edge cases in conditionals and loops

**Next Steps:**
After Plan 264-02, coverage gaps filled. Plan 264-03 will execute full suite and generate final report.
