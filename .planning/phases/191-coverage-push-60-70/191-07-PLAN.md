---
phase: 191-coverage-push-60-70
plan: 07
type: execute
wave: 2
depends_on: ["191-01", "191-02", "191-03", "191-04", "191-05"]
files_modified:
  - backend/tests/core/episodes/test_episode_retrieval_service_coverage.py
autonomous: true

must_haves:
  truths:
    - "EpisodeRetrievalService achieves 70%+ line coverage"
    - "All 4 retrieval modes tested (temporal, semantic, sequential, contextual)"
    - "Vector search mocked properly"
    - "Access logging covered"
  artifacts:
    - path: "backend/tests/core/episodes/test_episode_retrieval_service_coverage.py"
      provides: "EpisodeRetrievalService coverage tests"
      min_lines: 900
      exports: ["TestEpisodeRetrievalServiceCoverage"]
  key_links:
    - from: "backend/tests/core/episodes/test_episode_retrieval_service_coverage.py"
      to: "core/episode_retrieval_service.py"
      via: "direct import"
      pattern: "from core.episode_retrieval_service import"
---

<objective>
Achieve 70%+ line coverage on episode_retrieval_service.py (320 statements, currently 0%)

Purpose: EpisodeRetrievalService provides 4 retrieval modes (temporal, semantic, sequential, contextual) with LanceDB vector search, access logging, and PostgreSQL integration. This service requires AsyncMock patterns for async retrieval methods.

Output: Coverage-driven test file with 70%+ line coverage, all 4 retrieval modes tested, vector search mocking, access logging coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
@.planning/phases/191-coverage-push-60-70/191-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/episode_retrieval_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test file and retrieval mode tests</name>
  <files>backend/tests/core/episodes/test_episode_retrieval_service_coverage.py</files>
  <action>
Create test file with coverage-driven structure:

```python
"""
Coverage-driven tests for EpisodeRetrievalService (currently 0% -> target 70%+)

Target file: core/episode_retrieval_service.py (320 statements)

Focus areas from coverage gap analysis:
- Service initialization (lines 1-50)
- Temporal retrieval (lines 50-100)
- Semantic retrieval with vector search (lines 100-180)
- Sequential retrieval (lines 180-240)
- Contextual hybrid retrieval (lines 240-320)
"""

import pytest
from unittest.mock import Mock, AsyncMock, patch, MagicMock
from datetime import datetime, timedelta, timezone
from core.episode_retrieval_service import EpisodeRetrievalService, RetrievalMode

class TestEpisodeRetrievalServiceCoverage:
    """Coverage-driven tests for episode_retrieval_service.py"""

    def test_service_initialization(self, db_session):
        """Cover lines 1-50: Service initialization"""
        service = EpisodeRetrievalService(db_session)
        assert service.db is db_session
        assert service.lancedb_client is not None

    @pytest.mark.asyncio
    async def test_temporal_retrieval(self, mocker, db_session):
        """Cover temporal retrieval (lines 50-100)"""
        # Mock PostgreSQL query
        service = EpisodeRetrievalService(db_session)

        # Create test episodes
        from core.models import Episode
        ep1 = Episode(id="ep1", task_description="Task 1", created_at=datetime.now(timezone.utc) - timedelta(hours=2))
        ep2 = Episode(id="ep2", task_description="Task 2", created_at=datetime.now(timezone.utc))
        db_session.add_all([ep1, ep2])
        db_session.commit()

        results = await service.retrieve(RetrievalMode.TEMPORAL, limit=10)

        assert len(results) >= 1
```

Test temporal retrieval mode with time-based ordering.
  </action>
  <verify>Test file created with temporal retrieval tests</verify>
  <done>Temporal retrieval mode tested with time-based queries</done>
</task>

<task type="auto">
  <name>Task 2: Implement semantic and sequential retrieval tests</name>
  <files>backend/tests/core/episodes/test_episode_retrieval_service_coverage.py</files>
  <action>
Add tests for semantic and sequential retrieval modes:

```python
@pytest.mark.asyncio
async def test_semantic_retrieval(self, mocker, db_session):
    """Cover semantic retrieval with vector search (lines 100-180)"""
    # Mock LanceDB vector search
    mock_lancedb = mocker.patch("core.episode_retrieval_service.lancedb")
    mock_client = Mock()
    mock_table = Mock()

    # Mock vector search results
    mock_table.search.return_value.limit.return_value.to_pandas.return_value = [
        {"episode_id": "ep1", "score": 0.95},
        {"episode_id": "ep2", "score": 0.87},
    ]
    mock_client.open_table.return_value = mock_table
    mock_lancedb.connect.return_value = mock_client

    service = EpisodeRetrievalService(db_session)
    results = await service.retrieve(RetrievalMode.SEMANTIC, query="machine learning", limit=10)

    assert len(results) >= 1
    mock_table.search.assert_called_once()

@pytest.mark.asyncio
async def test_sequential_retrieval(self, db_session):
    """Cover sequential retrieval (lines 180-240)"""
    from core.models import Episode

    # Create sequential episodes
    service = EpisodeRetrievalService(db_session)
    ep1 = Episode(id="ep1", task_description="Task 1", sequence_order=1)
    ep2 = Episode(id="ep2", task_description="Task 2", sequence_order=2)
    db_session.add_all([ep1, ep2])
    db_session.commit()

    results = await service.retrieve(RetrievalMode.SEQUENTIAL, episode_id="ep1")

    assert len(results) >= 1
    assert results[0].sequence_order >= 1
```

Test semantic vector search and sequential ordering.
  </action>
  <verify>Semantic and sequential retrieval modes tested with LanceDB mocks</verify>
  <done>Vector search mocked properly, sequential ordering verified</done>
</task>

<task type="auto">
  <name>Task 3: Implement contextual retrieval and access logging tests</name>
  <files>backend/tests/core/episodes/test_episode_retrieval_service_coverage.py</files>
  <action>
Add tests for contextual hybrid retrieval and access logging:

```python
@pytest.mark.asyncio
async def test_contextual_hybrid_retrieval(self, mocker, db_session):
    """Cover contextual hybrid retrieval (lines 240-290)"""
    # Mock both PostgreSQL and LanceDB
    mock_lancedb = mocker.patch("core.episode_retrieval_service.lancedb")
    mock_client = Mock()
    mock_table = Mock()
    mock_table.search.return_value.limit.return_value.to_pandas.return_value = [
        {"episode_id": "ep1", "score": 0.92},
    ]
    mock_client.open_table.return_value = mock_table
    mock_lancedb.connect.return_value = mock_client

    service = EpisodeRetrievalService(db_session)
    results = await service.retrieve(RetrievalMode.CONTEXTUAL, query="agent workflow", limit=10)

    assert len(results) >= 1

@pytest.mark.asyncio
async def test_access_logging(self, db_session):
    """Cover access logging (lines 290-320)"""
    from core.models import EpisodeAccessLog

    service = EpisodeRetrievalService(db_session)
    await service.retrieve(RetrievalMode.TEMPORAL, limit=10)

    # Verify access log created
    logs = db_session.query(EpisodeAccessLog).all()
    assert len(logs) >= 1
    assert logs[0].retrieval_mode == "TEMPORAL"
```

Test hybrid retrieval combining PostgreSQL + LanceDB, and access logging.
  </action>
  <verify>Contextual hybrid retrieval tested, access logging verified</verify>
  <done>All 4 retrieval modes covered, access logging confirmed</done>
</task>

</tasks>

<verification>
After completion, verify:
1. Run: PYTHONPATH=backend pytest tests/core/episodes/test_episode_retrieval_service_coverage.py -v --cov=core/episode_retrieval_service --cov-branch --cov-report=term-missing
2. Check coverage achieves 70%+ line coverage
3. Verify all 4 retrieval modes tested
4. Confirm LanceDB vector search mocked properly
</verification>

<success_criteria>
1. 70%+ line coverage on episode_retrieval_service.py (224+ statements covered)
2. All 4 retrieval modes (temporal, semantic, sequential, contextual) tested
3. LanceDB vector search properly mocked
4. Access logging covered
</success_criteria>

<output>
After completion, create `.planning/phases/191-coverage-push-60-70/191-07-SUMMARY.md` with:
- Actual coverage achieved
- Retrieval modes covered
- Any VALIDATED_BUG findings
</output>
