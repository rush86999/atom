---
phase: 191-coverage-push-60-70
plan: 06
type: execute
wave: 2
depends_on: ["191-01", "191-02", "191-03", "191-04", "191-05"]
files_modified:
  - backend/tests/core/episodes/test_episode_segmentation_service_coverage.py
autonomous: true

must_haves:
  truths:
    - "EpisodeSegmentationService achieves 70%+ line coverage"
    - "Time-based segmentation tested"
    - "Canvas context segmentation covered"
    - "LanceDB integration properly mocked"
  artifacts:
    - path: "backend/tests/core/episodes/test_episode_segmentation_service_coverage.py"
      provides: "EpisodeSegmentationService coverage tests"
      min_lines: 1000
      exports: ["TestEpisodeSegmentationServiceCoverage"]
  key_links:
    - from: "backend/tests/core/episodes/test_episode_segmentation_service_coverage.py"
      to: "core/episode_segmentation_service.py"
      via: "direct import"
      pattern: "from core.episode_segmentation_service import"
---

<objective>
Achieve 70%+ line coverage on episode_segmentation_service.py (591 statements, currently 0%)

Purpose: EpisodeSegmentationService handles automatic episode segmentation using time gaps, topic changes, task completion detection, and canvas context. This is a complex async service with LanceDB integration requiring AsyncMock patterns.

Output: Coverage-driven test file with 70%+ line coverage, time-based segmentation tests, canvas context coverage, LanceDB mocking.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
@.planning/phases/191-coverage-push-60-70/191-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/episode_segmentation_service.py
@backend/tests/core/test_agent_evolution_loop_coverage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test file and initialization tests</name>
  <files>backend/tests/core/episodes/test_episode_segmentation_service_coverage.py</files>
  <action>
Create test file with coverage-driven structure:

```python
"""
Coverage-driven tests for EpisodeSegmentationService (currently 0% -> target 70%+)

Target file: core/episode_segmentation_service.py (591 statements)

Focus areas from coverage gap analysis:
- Service initialization (lines 1-80)
- Time-based segmentation (lines 80-200)
- Topic change detection (lines 200-300)
- Canvas context segmentation (lines 300-450)
- Task completion detection (lines 450-591)
"""

import pytest
from unittest.mock import Mock, AsyncMock, patch, MagicMock
from datetime import datetime, timedelta, timezone
from core.episode_segmentation_service import EpisodeSegmentationService

class TestEpisodeSegmentationServiceCoverage:
    """Coverage-driven tests for episode_segmentation_service.py"""

    def test_service_initialization(self, db_session):
        """Cover lines 1-80: Service initialization with dependencies"""
        service = EpisodeSegmentationService(db_session)
        assert service.db is db_session
        assert service.lancedb_client is not None
```

Test service initialization with database and LanceDB dependencies.
  </action>
  <verify>Test file created with initialization tests</verify>
  <done>Service initialization tested with mock dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Implement time-based segmentation tests</name>
  <files>backend/tests/core/episodes/test_episode_segmentation_service_coverage.py</files>
  <action>
Add tests for time-based segmentation with LanceDB mocking:

```python
def test_segment_by_time_gap(self, mocker, db_session):
    """Cover time-based segmentation (lines 80-200)"""
    # Mock LanceDB client
    mock_lancedb = mocker.patch("core.episode_segmentation_service.lancedb")
    mock_client = Mock()
    mock_table = Mock()
    mock_client.open_table.return_value = mock_table

    # Mock search results with time gaps
    import pandas as pd
    mock_table.search.return_value.to_pandas.return_value = pd.DataFrame({
        "episode_id": ["ep1", "ep2"],
        "timestamp": [
            datetime(2026, 1, 1, 10, 0, 0, tzinfo=timezone.utc),
            datetime(2026, 1, 1, 11, 0, 0, tzinfo=timezone.utc),  # 1 hour gap
        ],
        "task_description": ["Task 1", "Task 2"]
    })
    mock_lancedb.connect.return_value = mock_client

    service = EpisodeSegmentationService(db_session)
    segments = service.segment_by_time_gap(episode_id="test-ep", gap_minutes=30)

    assert len(segments) >= 1  # Should detect gap

@pytest.mark.parametrize("gap_minutes,expected_segments", [
    (5, 2),     # 5 min gap -> 2 segments
    (30, 2),    # 30 min gap -> 2 segments
    (120, 1),   # 120 min gap -> 1 segment (no gap detected)
])
def test_time_gap_thresholds(self, gap_minutes, expected_segments, mocker, db_session):
    """Cover various gap thresholds"""
    # Test with different gap thresholds
    pass
```

Use freezegun for consistent time-based testing.
  </action>
  <verify>Time-based segmentation tested with multiple gap thresholds</verify>
  <done>Time gap detection logic fully covered</done>
</task>

<task type="auto">
  <name>Task 3: Implement canvas context and task completion tests</name>
  <files>backend/tests/core/episodes/test_episode_segmentation_service_coverage.py</files>
  <action>
Add tests for canvas context and task completion segmentation:

```python
@pytest.mark.asyncio
async def test_segment_by_canvas_context(self, mocker, db_session):
    """Cover canvas context segmentation (lines 300-450)"""
    # Mock canvas tool operations
    mock_canvas = mocker.patch("core.episode_segmentation_service.CanvasTool")
    mock_canvas.get_active_canvases.return_value = [
        {"canvas_id": "canvas-1", "type": "chart"},
        {"canvas_id": "canvas-2", "type": "form"},
    ]

    service = EpisodeSegmentationService(db_session)
    segments = await service.segment_by_canvas_context(episode_id="test-ep")

    assert len(segments) >= 1
    mock_canvas.get_active_canvases.assert_called_once()

@pytest.mark.asyncio
async def test_segment_by_task_completion(self, mocker, db_session):
    """Cover task completion detection (lines 450-591)"""
    # Mock task completion detection
    mock_task_tracker = mocker.patch("core.episode_segmentation_service.TaskTracker")
    mock_task_tracker.get_completed_tasks.return_value = [
        {"task_id": "task-1", "completed_at": datetime.now(timezone.utc)},
    ]

    service = EpisodeSegmentationService(db_session)
    segments = await service.segment_by_task_completion(episode_id="test-ep")

    assert len(segments) >= 1
```

Test canvas context and task completion segmentation paths.
  </action>
  <verify>Canvas context and task completion segmentation tested</verify>
  <done>All segmentation methods covered with appropriate mocks</done>
</task>

</tasks>

<verification>
After completion, verify:
1. Run: PYTHONPATH=backend pytest tests/core/episodes/test_episode_segmentation_service_coverage.py -v --cov=core/episode_segmentation_service --cov-branch --cov-report=term-missing
2. Check coverage achieves 70%+ line coverage
3. Verify LanceDB properly mocked (no external dependencies)
4. Confirm async tests use pytest.mark.asyncio
</verification>

<success_criteria>
1. 70%+ line coverage on episode_segmentation_service.py (414+ statements covered)
2. Time-based segmentation tested with multiple gap thresholds
3. Canvas context segmentation covered
4. Task completion detection tested
5. LanceDB integration properly mocked
</success_criteria>

<output>
After completion, create `.planning/phases/191-coverage-push-60-70/191-06-SUMMARY.md` with:
- Actual coverage achieved
- Number of tests created
- Segmentation methods covered
- Any VALIDATED_BUG findings
</output>
