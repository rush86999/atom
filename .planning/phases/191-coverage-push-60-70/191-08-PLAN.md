---
phase: 191-coverage-push-60-70
plan: 08
type: execute
wave: 2
depends_on: ["191-01", "191-02", "191-03", "191-04", "191-05"]
files_modified:
  - backend/tests/core/episodes/test_episode_lifecycle_service_coverage.py
autonomous: true

must_haves:
  truths:
    - "EpisodeLifecycleService achieves 70%+ line coverage"
    - "Decay operations tested"
    - "Consolidation methods covered"
    - "Archival process verified"
  artifacts:
    - path: "backend/tests/core/episodes/test_episode_lifecycle_service_coverage.py"
      provides: "EpisodeLifecycleService coverage tests"
      min_lines: 800
      exports: ["TestEpisodeLifecycleServiceCoverage"]
  key_links:
    - from: "backend/tests/core/episodes/test_episode_lifecycle_service_coverage.py"
      to: "core/episode_lifecycle_service.py"
      via: "direct import"
      pattern: "from core.episode_lifecycle_service import"
---

<objective>
Achieve 70%+ line coverage on episode_lifecycle_service.py (351 statements, currently 0%)

Purpose: EpisodeLifecycleService manages episode lifecycle operations: decay (reducing access frequency over time), consolidation (merging related episodes), and archival (moving old episodes to cold storage). This is an async service with database and storage operations.

Output: Coverage-driven test file with 70%+ line coverage, decay operations tested, consolidation covered, archival verified.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
@.planning/phases/191-coverage-push-60-70/191-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/episode_lifecycle_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test file and decay operation tests</name>
  <files>backend/tests/core/episodes/test_episode_lifecycle_service_coverage.py</files>
  <action>
Create test file with coverage-driven structure:

```python
"""
Coverage-driven tests for EpisodeLifecycleService (currently 0% -> target 70%+)

Target file: core/episode_lifecycle_service.py (351 statements)

Focus areas from coverage gap analysis:
- Service initialization (lines 1-50)
- Decay operations (lines 50-120)
- Consolidation methods (lines 120-240)
- Archival process (lines 240-351)
"""

import pytest
from unittest.mock import Mock, AsyncMock, patch
from datetime import datetime, timedelta, timezone
from core.episode_lifecycle_service import EpisodeLifecycleService
from core.models import Episode

class TestEpisodeLifecycleServiceCoverage:
    """Coverage-driven tests for episode_lifecycle_service.py"""

    def test_service_initialization(self, db_session):
        """Cover lines 1-50: Service initialization"""
        service = EpisodeLifecycleService(db_session)
        assert service.db is db_session

    @pytest.mark.asyncio
    async def test_decay_episode_access_frequency(self, db_session):
        """Cover decay operation (lines 50-120)"""
        # Create test episode with high access frequency
        ep = Episode(
            id="ep1",
            task_description="Test task",
            access_frequency=100,
            last_accessed_at=datetime.now(timezone.utc)
        )
        db_session.add(ep)
        db_session.commit()

        service = EpisodeLifecycleService(db_session)
        await service.apply_decay("ep1", decay_factor=0.5)

        db_session.refresh(ep)
        assert ep.access_frequency < 100  # Should be decayed
```

Test decay operation that reduces access frequency over time.
  </action>
  <verify>Test file created with decay operation tests</verify>
  <done>Decay operation tested with various decay factors</done>
</task>

<task type="auto">
  <name>Task 2: Implement consolidation tests</name>
  <files>backend/tests/core/episodes/test_episode_lifecycle_service_coverage.py</files>
  <action>
Add tests for episode consolidation:

```python
@pytest.mark.asyncio
async def test_consolidate_related_episodes(self, db_session):
    """Cover consolidation (lines 120-240)"""
    # Create related episodes
    from core.models import Episode, EpisodeSegment

    service = EpisodeLifecycleService(db_session)

    ep1 = Episode(id="ep1", task_description="Task 1 part 1")
    ep2 = Episode(id="ep2", task_description="Task 1 part 2")
    db_session.add_all([ep1, ep2])
    db_session.commit()

    # Create segments
    seg1 = EpisodeSegment(episode_id="ep1", content="Segment 1")
    seg2 = EpisodeSegment(episode_id="ep2", content="Segment 2")
    db_session.add_all([seg1, seg2])
    db_session.commit()

    # Consolidate
    consolidated = await service.consolidate_episodes(["ep1", "ep2"])

    assert consolidated is not None
    assert consolidated.id != "ep1" and consolidated.id != "ep2"

@pytest.mark.asyncio
async def test_consolidation_with_segment_merging(self, db_session):
    """Cover segment merging during consolidation"""
    # Test that segments are properly merged during consolidation
    pass
```

Test episode consolidation merging related episodes and their segments.
  </action>
  <verify>Consolidation tested with multiple episodes and segment merging</verify>
  <done>Episode consolidation logic fully covered</done>
</task>

<task type="auto">
  <name>Task 3: Implement archival and cold storage tests</name>
  <files>backend/tests/core/episodes/test_episode_lifecycle_service_coverage.py</files>
  <action>
Add tests for archival to cold storage:

```python
@pytest.mark.asyncio
async def test_archive_old_episodes(self, mocker, db_session):
    """Cover archival process (lines 240-310)"""
    # Create old episodes (> 90 days)
    from core.models import Episode

    old_date = datetime.now(timezone.utc) - timedelta(days=100)
    ep1 = Episode(id="old-ep1", task_description="Old task 1", created_at=old_date)
    ep2 = Episode(id="old-ep2", task_description="Old task 2", created_at=old_date)
    db_session.add_all([ep1, ep2])
    db_session.commit()

    # Mock cold storage (LanceDB/S3)
    mock_storage = mocker.patch("core.episode_lifecycle_service.ColdStorage")
    mock_storage.archive_episodes.return_value = True

    service = EpisodeLifecycleService(db_session)
    archived = await service.archive_old_episodes(days_threshold=90)

    assert len(archived) == 2
    mock_storage.archive_episodes.assert_called_once()

@pytest.mark.asyncio
async def test_archive_with_cleanup(self, db_session):
    """Cover cleanup after archival (lines 310-351)"""
    # Test that archived episodes are removed from hot storage
    pass
```

Test archival process moving old episodes to cold storage.
  </action>
  <verify>Archival tested with old episodes, cold storage mocked properly</verify>
  <done>Archival process covered, cleanup after archival verified</done>
</task>

</tasks>

<verification>
After completion, verify:
1. Run: PYTHONPATH=backend pytest tests/core/episodes/test_episode_lifecycle_service_coverage.py -v --cov=core/episode_lifecycle_service --cov-branch --cov-report=term-missing
2. Check coverage achieves 70%+ line coverage
3. Verify decay, consolidation, archival all tested
4. Confirm cold storage properly mocked
</verification>

<success_criteria>
1. 70%+ line coverage on episode_lifecycle_service.py (246+ statements covered)
2. Decay operations tested with various factors
3. Episode consolidation covered with segment merging
4. Archival process verified with cold storage mocking
</success_criteria>

<output>
After completion, create `.planning/phases/191-coverage-push-60-70/191-08-SUMMARY.md` with:
- Actual coverage achieved
- Number of tests created
- Lifecycle operations covered
- Any VALIDATED_BUG findings
</output>
