---
phase: 191-coverage-push-60-70
plan: 13
type: execute
wave: 3
depends_on: ["191-06", "191-07", "191-08", "191-09", "191-10"]
files_modified:
  - backend/tests/api/test_atom_agent_endpoints_coverage_extend.py
autonomous: true

must_haves:
  truths:
    - "AtomAgentEndpoints coverage extended to 65%+"
    - "Chat endpoint tested with TestClient"
    - "Intent routing covered"
    - "Streaming responses mocked"
  artifacts:
    - path: "backend/tests/api/test_atom_agent_endpoints_coverage_extend.py"
      provides: "Extended AtomAgentEndpoints coverage tests"
      min_lines: 1100
      exports: ["TestAtomAgentEndpointsExtended"]
  key_links:
    - from: "backend/tests/api/test_atom_agent_endpoints_coverage_extend.py"
      to: "core/atom_agent_endpoints.py"
      via: "FastAPI TestClient"
      pattern: "from fastapi.testclient import TestClient"
---

<objective>
Extend AtomAgentEndpoints coverage from 0% to 65%+ (787 statements)

Purpose: AtomAgentEndpoints contains FastAPI routes for agent chat, streaming responses, intent routing, and session management. This is a large file with async endpoints requiring TestClient and AsyncMock patterns for streaming LLM responses.

Output: Extended coverage test file reaching 65%+ line coverage, chat endpoint tested, intent routing covered, streaming mocked.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
@.planning/phases/191-coverage-push-60-70/191-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/atom_agent_endpoints.py
@backend/tests/test_atom_agent_endpoints_coverage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test file and chat endpoint tests</name>
  <files>backend/tests/api/test_atom_agent_endpoints_coverage_extend.py</files>
  <action>
Create test file with coverage-driven structure:

```python
"""
Extended coverage tests for AtomAgentEndpoints (currently 0% -> target 65%+)

Target file: core/atom_agent_endpoints.py (787 statements)

Focus areas from coverage gap analysis:
- Endpoint initialization (lines 1-80)
- Chat endpoint (lines 80-250)
- Intent routing (lines 250-450)
- Streaming responses (lines 450-600)
- Session management (lines 600-787)
"""

import pytest
from unittest.mock import AsyncMock, Mock, patch
from fastapi.testclient import TestClient
from core.atom_agent_endpoints import app

class TestAtomAgentEndpointsExtended:
    """Extended coverage tests for atom_agent_endpoints.py"""

    def test_chat_endpoint_success(self, mocker):
        """Cover chat endpoint success path (lines 80-200)"""
        # Mock LLM handler
        mock_llm = AsyncMock()
        mock_llm.complete.return_value = "Hello! How can I help you?"
        mocker.patch("core.atom_agent_endpoints.get_llm_handler", return_value=mock_llm)

        client = TestClient(app)
        response = client.post(
            "/api/v1/agent/test-agent/chat",
            json={"message": "Hello"}
        )

        assert response.status_code == 200
        assert "Hello" in response.json()["response"]

    def test_chat_endpoint_validation(self):
        """Cover chat endpoint validation (lines 200-250)"""
        client = TestClient(app)

        # Missing message
        response = client.post(
            "/api/v1/agent/test-agent/chat",
            json={}
        )

        assert response.status_code == 422  # Validation error
```

Test chat endpoint with valid and invalid inputs.
  </action>
  <verify>Test file created with chat endpoint tests</verify>
  <done>Chat endpoint tested with success and validation paths</done>
</task>

<task type="auto">
  <name>Task 2: Implement intent routing tests</name>
  <files>backend/tests/api/test_atom_agent_endpoints_coverage_extend.py</files>
  <action>
Add tests for intent routing:

```python
@pytest.mark.parametrize("user_message,expected_intent", [
    ("create a workflow", "workflow_create"),
    ("show my tasks", "task_list"),
    ("schedule meeting", "calendar_schedule"),
    ("send email", "email_send"),
    ("what's the weather", "unknown"),  # Default fallback
])
def test_intent_routing(self, user_message, expected_intent, mocker):
    """Cover intent routing (lines 250-450)"""
    client = TestClient(app)

    response = client.post(
        "/api/v1/agent/test-agent/chat",
        json={"message": user_message}
    )

    assert response.status_code == 200
    # Verify intent was detected (check response metadata)

def test_intent_routing_with_context(self, mocker):
    """Cover intent routing with conversation context"""
    # Test that context influences intent detection
    pass
```

Test intent classification with various user inputs.
  </action>
  <verify>Intent routing tested with parametrized cases</verify>
  <done>Intent classification covered with various message types</done>
</task>

<task type="auto">
  <name>Task 3: Implement streaming and session tests</name>
  <files>backend/tests/api/test_atom_agent_endpoints_coverage_extend.py</files>
  <action>
Add tests for streaming responses and session management:

```python
@pytest.mark.asyncio
async def test_streaming_response(self, mocker):
    """Cover streaming response (lines 450-600)"""
    # Mock streaming LLM response
    mock_llm = AsyncMock()
    mock_llm.stream.return_value = iter([
        {"token": "Hello", "finish_reason": None},
        {"token": " world", "finish_reason": None},
        {"token": "!", "finish_reason": "stop"}
    ])
    mocker.patch("core.atom_agent_endpoints.get_llm_handler", return_value=mock_llm)

    client = TestClient(app)
    response = client.post(
        "/api/v1/agent/test-agent/chat/stream",
        json={"message": "Hi"}
    )

    assert response.status_code == 200
    # Verify streaming response format

def test_session_creation(self, mocker):
    """Cover session creation (lines 600-700)"""
    client = TestClient(app)

    response = client.post(
        "/api/v1/agent/test-agent/session",
        json={"user_id": "user-123"}
    )

    assert response.status_code == 200
    assert "session_id" in response.json()

def test_session_retrieval(self, mocker):
    """Cover session retrieval (lines 700-787)"""
    # First create a session, then retrieve it
    pass
```

Test streaming responses with AsyncMock and session management.
  </action>
  <verify>Streaming tested with token-by-token mocking, session operations covered</verify>
  <done>Streaming paths covered, session creation/retrieval verified</done>
</task>

</tasks>

<verification>
After completion, verify:
1. Run: PYTHONPATH=backend pytest tests/api/test_atom_agent_endpoints_coverage_extend.py -v --cov=core/atom_agent_endpoints --cov-branch --cov-report=term-missing
2. Check coverage achieves 65%+ line coverage
3. Verify chat endpoint tested
4. Confirm intent routing covered
5. Validate streaming responses mocked
</verification>

<success_criteria>
1. 65%+ line coverage on atom_agent_endpoints.py (512+ statements covered)
2. Chat endpoint tested with TestClient
3. Intent routing covered with parametrized tests
4. Streaming responses tested with AsyncMock
5. Session management verified
</success_criteria>

<output>
After completion, create `.planning/phases/191-coverage-push-60-70/191-13-SUMMARY.md` with:
- Actual coverage achieved
- Number of tests created
- Endpoints covered
- Any VALIDATED_BUG findings
</output>
