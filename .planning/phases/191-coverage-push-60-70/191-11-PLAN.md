---
phase: 191-coverage-push-60-70
plan: 11
type: execute
wave: 3
depends_on: ["191-06", "191-07", "191-08", "191-09", "191-10"]
files_modified:
  - backend/tests/core/agents/test_atom_meta_agent_coverage.py
autonomous: true

must_haves:
  truths:
    - "AtomMetaAgent achieves 60%+ line coverage (realistic for complex async)"
    - "Core initialization tested"
    - "Simple methods covered"
    - "Complex ReAct loop acknowledged as acceptable to skip"
  artifacts:
    - path: "backend/tests/core/agents/test_atom_meta_agent_coverage.py"
      provides: "AtomMetaAgent coverage tests"
      min_lines: 900
      exports: ["TestAtomMetaAgentCoverage"]
  key_links:
    - from: "backend/tests/core/agents/test_atom_meta_agent_coverage.py"
      to: "core/atom_meta_agent.py"
      via: "direct import"
      pattern: "from core.atom_meta_agent import"
---

<objective>
Achieve 60%+ line coverage on atom_meta_agent.py (422 statements, currently 0%)

Purpose: AtomMetaAgent is the meta-agent system with complex ReAct loops for tool use and reasoning. Phase 189 found this file to have 0% coverage due to async complexity. This plan targets 60% (not 75-80%) by focusing on testable methods and accepting that the complex ReAct execute() loop is acceptable to skip.

Output: Coverage-driven test file with 60%+ line coverage, initialization tested, simple methods covered, ReAct loop acknowledged.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
@.planning/phases/191-coverage-push-60-70/191-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/atom_meta_agent.py
@backend/tests/core/test_atom_meta_agent_coverage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test file and initialization tests</name>
  <files>backend/tests/core/agents/test_atom_meta_agent_coverage.py</files>
  <action>
Create test file with coverage-driven structure:

```python
"""
Coverage-driven tests for AtomMetaAgent (currently 0% -> target 60%+)

Target file: core/atom_meta_agent.py (422 statements)

Focus areas from coverage gap analysis:
- Agent initialization (lines 1-80)
- Configuration methods (lines 80-150)
- Tool registration (lines 150-220)
- Simple query methods (lines 220-300)
- ReAct execute() loop (lines 300-422) - ACKNOWLEDGED as too complex for full coverage

Note: The execute() method with complex ReAct loop is acceptable to partially skip
due to extensive async complexity and external dependencies. Focus on testable paths.
"""

import pytest
from unittest.mock import Mock, AsyncMock, patch
from core.atom_meta_agent import AtomMetaAgent

class TestAtomMetaAgentCoverage:
    """Coverage-driven tests for atom_meta_agent.py"""

    def test_agent_initialization(self, db_session):
        """Cover lines 1-80: Agent initialization"""
        config = {
            "name": "TestMetaAgent",
            "model": "gpt-4",
            "max_iterations": 10
        }

        agent = AtomMetaAgent(config, db_session)
        assert agent.config["name"] == "TestMetaAgent"
        assert agent.db is db_session
        assert agent.tools is not None
```

Test agent initialization with various configurations.
  </action>
  <verify>Test file created with initialization tests</verify>
  <done>Agent initialization tested with config variations</done>
</task>

<task type="auto">
  <name>Task 2: Implement configuration and tool registration tests</name>
  <files>backend/tests/core/agents/test_atom_meta_agent_coverage.py</files>
  <action>
Add tests for configuration methods and tool registration:

```python
def test_configuration_methods(self, db_session):
    """Cover configuration methods (lines 80-150)"""
    agent = AtomMetaAgent({}, db_session)

    # Test model configuration
    agent.set_model("claude-3-opus")
    assert agent.get_model() == "claude-3-opus"

    # Test temperature setting
    agent.set_temperature(0.7)
    assert agent.get_temperature() == 0.7

    # Test max iterations
    agent.set_max_iterations(20)
    assert agent.get_max_iterations() == 20

def test_tool_registration(self, db_session):
    """Cover tool registration (lines 150-220)"""
    agent = AtomMetaAgent({}, db_session)

    # Mock tool
    mock_tool = Mock()
    mock_tool.name = "test_tool"
    mock_tool.description = "A test tool"

    agent.register_tool(mock_tool)

    assert "test_tool" in agent.list_tools()
    assert agent.get_tool("test_tool") == mock_tool

def test_tool_removal(self, db_session):
    """Cover tool removal"""
    agent = AtomMetaAgent({}, db_session)

    mock_tool = Mock()
    mock_tool.name = "removable_tool"
    agent.register_tool(mock_tool)

    agent.remove_tool("removable_tool")
    assert "removable_tool" not in agent.list_tools()
```

Test configuration methods and tool registration/removal.
  </action>
  <verify>Configuration and tool registration tested</verify>
  <done>Config methods covered, tool management verified</done>
</task>

<task type="auto">
  <name>Task 3: Implement simple query and reasoning tests</name>
  <files>backend/tests/core/agents/test_atom_meta_agent_coverage.py</files>
  <action>
Add tests for simple query methods (avoiding complex execute() loop):

```python
@pytest.mark.asyncio
async def test_simple_query(self, mocker, db_session):
    """Cover simple query method (lines 220-300)"""
    # Mock LLM response for simple query
    mock_llm = AsyncMock()
    mock_llm.generate.return_value = "The answer is 42"

    agent = AtomMetaAgent({}, db_session)
    agent.llm_client = mock_llm

    response = await agent.query("What is the meaning of life?")

    assert "42" in response
    mock_llm.generate.assert_called_once()

def test_reasoning_trace_building(self, db_session):
    """Cover reasoning trace construction"""
    agent = AtomMetaAgent({}, db_session)

    # Add reasoning steps
    agent.add_reasoning_step("Observation", "User asked a question")
    agent.add_reasoning_step("Thought", "I need to answer")

    trace = agent.get_reasoning_trace()

    assert len(trace) == 2
    assert trace[0]["step_type"] == "Observation"
    assert trace[1]["step_type"] == "Thought"

def test_memory_operations(self, db_session):
    """Cover memory store/retrieve operations"""
    agent = AtomMetaAgent({}, db_session)

    agent.store_memory("key1", "value1")
    agent.store_memory("key2", {"complex": "value"})

    assert agent.retrieve_memory("key1") == "value1"
    assert agent.retrieve_memory("key2") == {"complex": "value"}
    assert agent.retrieve_memory("nonexistent") is None
```

Test simple query methods and reasoning/memory operations.
  </action>
  <verify>Simple query methods tested, reasoning trace and memory covered</verify>
  <done>Testable paths covered, complex ReAct loop acknowledged</done>
</task>

</tasks>

<verification>
After completion, verify:
1. Run: PYTHONPATH=backend pytest tests/core/agents/test_atom_meta_agent_coverage.py -v --cov=core/atom_meta_agent --cov-branch --cov-report=term-missing
2. Check coverage achieves 60%+ line coverage (253+ statements)
3. Verify testable methods covered
4. Document remaining uncovered lines (complex ReAct loop)
</verification>

<success_criteria>
1. 60%+ line coverage on atom_meta_agent.py (253+ statements covered)
2. Agent initialization tested
3. Configuration methods covered
4. Tool registration verified
5. Simple query operations tested
6. Complex ReAct loop acknowledged as acceptable to skip
</success_criteria>

<output>
After completion, create `.planning/phases/191-coverage-push-60-70/191-11-SUMMARY.md` with:
- Actual coverage achieved (target: 60%+)
- Number of tests created
- Methods covered vs acknowledged as skipped
- Any VALIDATED_BUG findings
</output>
