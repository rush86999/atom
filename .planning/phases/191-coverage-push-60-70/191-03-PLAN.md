---
phase: 191-coverage-push-60-70
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/core/governance/test_agent_context_resolver_coverage.py
autonomous: true

must_haves:
  truths:
    - "AgentContextResolver achieves 75%+ line coverage"
    - "Agent resolution by ID tested"
    - "Tenant-based context resolution tested"
    - "Error handling for missing agents verified"
  artifacts:
    - path: "backend/tests/core/governance/test_agent_context_resolver_coverage.py"
      provides: "AgentContextResolver coverage tests"
      min_lines: 400
      exports: ["TestAgentContextResolverCoverage"]
  key_links:
    - from: "backend/tests/core/governance/test_agent_context_resolver_coverage.py"
      to: "core/agent_context_resolver.py"
      via: "direct import"
      pattern: "from core.agent_context_resolver import"
---

<objective>
Achieve 75%+ line coverage on agent_context_resolver.py (145 statements, currently 0%)

Purpose: AgentContextResolver resolves agent contexts by ID and tenant, handling caching, validation, and error cases. This is a lightweight but critical service for agent lookup operations.

Output: Coverage-driven test file with 75%+ line coverage, agent resolution tests, tenant context testing, error path coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
@.planning/phases/191-coverage-push-60-70/191-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/agent_context_resolver.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test file and agent resolution tests</name>
  <files>backend/tests/core/governance/test_agent_context_resolver_coverage.py</files>
  <action>
Create test file with coverage-driven structure:

```python
"""
Coverage-driven tests for AgentContextResolver (currently 0% -> target 75%+)

Target file: core/agent_context_resolver.py (145 statements)

Focus areas from coverage gap analysis:
- Resolver initialization (lines 1-30)
- resolve_agent_by_id (lines 30-80)
- resolve_by_tenant (lines 80-110)
- Error handling (lines 110-145)
"""

import pytest
from core.agent_context_resolver import AgentContextResolver
from core.models import AgentRegistry

class TestAgentContextResolverCoverage:
    """Coverage-driven tests for agent_context_resolver.py"""

    def test_resolver_initialization(self, db_session):
        """Cover lines 1-30: Resolver initialization"""
        resolver = AgentContextResolver(db_session)
        assert resolver.db is db_session

    def test_resolve_agent_by_id_success(self, db_session):
        """Cover successful agent resolution (lines 30-60)"""
        # Create test agent
        agent = AgentRegistry(
            id="test-agent",
            tenant_id="tenant-1",
            name="Test Agent",
            maturity_level="INTERN",
            confidence_score=0.6,
            enabled=True
        )
        db_session.add(agent)
        db_session.commit()

        resolver = AgentContextResolver(db_session)
        result = resolver.resolve_agent_by_id("test-agent")

        assert result.id == "test-agent"
        assert result.tenant_id == "tenant-1"
```

Test successful agent resolution by ID.
  </action>
  <verify>Test file created with agent resolution tests</verify>
  <done>Agent resolution by ID fully tested</done>
</task>

<task type="auto">
  <name>Task 2: Implement tenant-based resolution and caching tests</name>
  <files>backend/tests/core/governance/test_agent_context_resolver_coverage.py</files>
  <action>
Add tests for tenant-based resolution and caching:

```python
def test_resolve_by_tenant(self, db_session):
    """Cover tenant-based resolution (lines 80-110)"""
    # Create multiple agents in same tenant
    agent1 = AgentRegistry(id="agent-1", tenant_id="tenant-1", maturity_level="INTERN", enabled=True)
    agent2 = AgentRegistry(id="agent-2", tenant_id="tenant-1", maturity_level="SUPERVISED", enabled=True)
    db_session.add_all([agent1, agent2])
    db_session.commit()

    resolver = AgentContextResolver(db_session)
    results = resolver.resolve_by_tenant("tenant-1")

    assert len(results) == 2
    assert any(a.id == "agent-1" for a in results)
    assert any(a.id == "agent-2" for a in results)

def test_resolve_cache_hit(self, db_session):
    """Cover caching behavior (lines 60-80)"""
    # Test that repeated calls use cache
    pass

def test_resolve_by_tenant_empty_result(self, db_session):
    """Cover empty tenant case"""
    resolver = AgentContextResolver(db_session)
    results = resolver.resolve_by_tenant("nonexistent-tenant")
    assert results == []
```

Test tenant filtering and cache behavior.
  </action>
  <verify>Tenant-based resolution tested, caching behavior verified</verify>
  <done>Tenant context resolution covered, cache hit/miss tested</done>
</task>

<task type="auto">
  <name>Task 3: Implement error handling tests</name>
  <files>backend/tests/core/governance/test_agent_context_resolver_coverage.py</files>
  <action>
Add tests for error paths and edge cases:

```python
@pytest.mark.parametrize("agent_id,error_expected", [
    (None, ValueError),           # None agent_id
    ("", ValueError),              # Empty string
    ("nonexistent", None),         # Non-existent agent (returns None)
])
def test_resolve_error_handling(self, agent_id, error_expected, db_session):
    """Cover error handling (lines 110-145)"""
    resolver = AgentContextResolver(db_session)

    if error_expected:
        with pytest.raises(error_expected):
            resolver.resolve_agent_by_id(agent_id)
    else:
        result = resolver.resolve_agent_by_id(agent_id)
        assert result is None

def test_resolve_disabled_agent(self, db_session):
    """Cover disabled agent case"""
    agent = AgentRegistry(
        id="disabled-agent",
        tenant_id="tenant-1",
        maturity_level="INTERN",
        enabled=False  # Disabled
    )
    db_session.add(agent)
    db_session.commit()

    resolver = AgentContextResolver(db_session)
    result = resolver.resolve_agent_by_id("disabled-agent")

    # Verify behavior for disabled agents
```

Test all error conditions and edge cases.
  </action>
  <verify>Error handling tests added for None, empty, nonexistent, disabled agents</verify>
  <done>Error paths fully covered, edge cases tested</done>
</task>

</tasks>

<verification>
After completion, verify:
1. Run: PYTHONPATH=backend pytest tests/core/governance/test_agent_context_resolver_coverage.py -v --cov=core/agent_context_resolver --cov-branch --cov-report=term-missing
2. Check coverage achieves 75%+ line coverage
3. Verify all error paths tested
4. Confirm tenant filtering works correctly
</verification>

<success_criteria>
1. 75%+ line coverage on agent_context_resolver.py (109+ statements covered)
2. Agent resolution by ID and tenant tested
3. Error handling for missing/disabled agents covered
4. Cache behavior verified
</success_criteria>

<output>
After completion, create `.planning/phases/191-coverage-push-60-70/191-03-SUMMARY.md` with:
- Actual coverage achieved
- Number of tests created
- Any VALIDATED_BUG findings
</output>
