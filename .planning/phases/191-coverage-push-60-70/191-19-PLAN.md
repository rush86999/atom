---
phase: 191-coverage-push-60-70
plan: 19
type: execute
wave: 4
depends_on: ["191-11", "191-12", "191-13", "191-14", "191-15"]
files_modified:
  - backend/tests/core/skills/test_skill_composition_engine_coverage_extend.py
autonomous: true

must_haves:
  truths:
    - "SkillCompositionEngine coverage extended from 76% to 80%+"
    - "DAG validation tested"
    - "Workflow execution covered"
    - "Circular dependency detection verified"
  artifacts:
    - path: "backend/tests/core/skills/test_skill_composition_engine_coverage_extend.py"
      provides: "Extended SkillCompositionEngine coverage tests"
      min_lines: 400
      exports: ["TestSkillCompositionEngineExtended"]
  key_links:
    - from: "backend/tests/core/skills/test_skill_composition_engine_coverage_extend.py"
      to: "core/skill_composition_engine.py"
      via: "direct import"
      pattern: "from core.skill_composition_engine import"
---

<objective>
Extend SkillCompositionEngine coverage from 76% to 80%+ (132 statements)

Purpose: SkillCompositionEngine creates and executes DAG-based skill compositions with dependency resolution, parallel execution, and error handling. Phase 183 achieved 76% coverage. This plan fills remaining gaps to reach 80%+.

Output: Extended coverage test file reaching 80%+ line coverage, DAG validation tested, workflow execution covered.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
@.planning/phases/191-coverage-push-60-70/191-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/skill_composition_engine.py
@backend/tests/core/skills/test_skill_composition_engine_coverage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create extended test file with DAG validation tests</name>
  <files>backend/tests/core/skills/test_skill_composition_engine_coverage_extend.py</files>
  <action>
Create extended test file to fill remaining coverage gaps:

```python
"""
Extended coverage tests for SkillCompositionEngine (currently 76% -> target 80%+)

Target file: core/skill_composition_engine.py (132 statements)

This file extends existing coverage from test_skill_composition_engine_coverage.py
by targeting remaining uncovered lines.

Focus areas (building on Phase 183 76% baseline):
- Enhanced DAG validation (lines 1-40)
- Circular dependency detection (lines 40-80)
- Parallel execution edge cases (lines 80-110)
- Error recovery (lines 110-132)
"""

import pytest
from core.skill_composition_engine import SkillCompositionEngine, CompositionError

class TestSkillCompositionEngineExtended:
    """Extended coverage tests for skill_composition_engine.py"""

    def test_dag_validation_with_complex_graph(self, db_session):
        """Cover enhanced DAG validation (lines 1-40)"""
        engine = SkillCompositionEngine(db_session)

        # Complex DAG with multiple levels
        workflow = {
            "nodes": [
                {"id": "A", "skill": "skill1"},
                {"id": "B", "skill": "skill2", "depends_on": ["A"]},
                {"id": "C", "skill": "skill3", "depends_on": ["A"]},
                {"id": "D", "skill": "skill4", "depends_on": ["B", "C"]},
            ]
        }

        is_valid, errors = engine.validate_workflow(workflow)

        assert is_valid is True
        assert len(errors) == 0

    def test_dag_validation_with_missing_dependencies(self, db_session):
        """Cover validation with missing dependencies"""
        engine = SkillCompositionEngine(db_session)

        workflow = {
            "nodes": [
                {"id": "A", "skill": "skill1"},
                {"id": "B", "skill": "skill2", "depends_on": ["C"]},  # C doesn't exist
            ]
        }

        is_valid, errors = engine.validate_workflow(workflow)

        assert is_valid is False
        assert any("missing" in str(e).lower() for e in errors)
```

Test DAG validation with complex and invalid graphs.
  </action>
  <verify>Extended test file created with DAG validation tests</verify>
  <done>DAG validation tested with complex graph structures</done>
</task>

<task type="auto">
  <name>Task 2: Implement circular dependency tests</name>
  <files>backend/tests/core/skills/test_skill_composition_engine_coverage_extend.py</files>
  <action>
Add tests for circular dependency detection:

```python
def test_circular_dependency_detection(self, db_session):
    """Cover circular dependency detection (lines 40-80)"""
    engine = SkillCompositionEngine(db_session)

    # Create circular dependency: A -> B -> C -> A
    workflow = {
        "nodes": [
            {"id": "A", "skill": "skill1", "depends_on": ["C"]},
            {"id": "B", "skill": "skill2", "depends_on": ["A"]},
            {"id": "C", "skill": "skill3", "depends_on": ["B"]},
        ]
    }

    is_valid, errors = engine.validate_workflow(workflow)

    assert is_valid is False
    assert any("circular" in str(e).lower() for e in errors)

def test_self_dependency_detection(self, db_session):
    """Cover self-dependency detection"""
    engine = SkillCompositionEngine(db_session)

    workflow = {
        "nodes": [
            {"id": "A", "skill": "skill1", "depends_on": ["A"]},  # Self-dependency
        ]
    }

    is_valid, errors = engine.validate_workflow(workflow)

    assert is_valid is False
    assert any("self" in str(e).lower() for e in errors)
```

Test circular and self-dependency detection.
  </action>
  <verify>Circular dependency detection tested with various cycle types</verify>
  <done>Dependency cycle detection verified</done>
</task>

<task type="auto">
  <name>Task 3: Implement parallel execution and error recovery tests</name>
  <files>backend/tests/core/skills/test_skill_composition_engine_coverage_extend.py</files>
  <action>
Add tests for parallel execution edge cases and error recovery:

```python
@pytest.mark.asyncio
async def test_parallel_execution_with_failures(self, mocker, db_session):
    """Cover parallel execution with partial failures (lines 80-110)"""
    engine = SkillCompositionEngine(db_session)

    # Mock skill execution - one fails, one succeeds
    async def mock_execute(node_id):
        if node_id == "A":
            return {"success": True, "output": "A result"}
        else:
            raise Exception(f"{node_id} failed")

    engine._execute_node = mock_execute

    workflow = {
        "nodes": [
            {"id": "A", "skill": "skill1"},
            {"id": "B", "skill": "skill2"},  # Will run in parallel
        ]
    }

    result = await engine.execute_workflow(workflow)

    # Should complete despite partial failure
    assert result["total_nodes"] == 2
    assert result["successful_nodes"] == 1

@pytest.mark.asyncio
async def test_error_recovery_with_retry(self, db_session):
    """Cover error recovery (lines 110-132)"""
    engine = SkillCompositionEngine(db_session)

    # Configure retry on workflow
    workflow = {
        "nodes": [
            {"id": "A", "skill": "flaky-skill"},
        ],
        "error_handling": {
            "retry_policy": "exponential_backoff",
            "max_retries": 3
        }
    }

    # Mock execution that fails then succeeds
    attempt = [0]
    async def mock_flaky_execute(node_id):
        attempt[0] += 1
        if attempt[0] < 3:
            raise Exception("Temporary failure")
        return {"success": True, "output": "Success"}

    engine._execute_node = mock_flaky_execute

    result = await engine.execute_workflow(workflow)

    assert result["successful_nodes"] == 1
    assert attempt[0] == 3  # Failed twice, succeeded on third try
```

Test parallel execution with failures and error recovery.
  </action>
  <verify>Parallel execution tested with failures, error recovery covered</verify>
  <done>Edge cases verified, retry logic tested</done>
</task>

</tasks>

<verification>
After completion, verify:
1. Run: PYTHONPATH=backend pytest tests/core/skills/test_skill_composition_engine_coverage_extend.py -v --cov=core/skill_composition_engine --cov-branch --cov-report=term-missing
2. Check coverage achieves 80%+ line coverage
3. Verify circular dependency detection tested
4. Confirm parallel execution edge cases covered
</verification>

<success_criteria>
1. 80%+ line coverage on skill_composition_engine.py (106+ statements covered)
2. DAG validation tested with complex graphs
3. Circular dependency detection verified
4. Parallel execution edge cases covered
5. Error recovery with retry logic tested
</success_criteria>

<output>
After completion, create `.planning/phases/191-coverage-push-60-70/191-19-SUMMARY.md` with:
- Actual coverage achieved (target: 80%+)
- Number of new tests added
- DAG features covered
- Any VALIDATED_BUG findings
</output>
