---
phase: 191-coverage-push-60-70
plan: 10
type: execute
wave: 2
depends_on: ["191-01", "191-02", "191-03", "191-04", "191-05"]
files_modified:
  - backend/tests/core/workflow/test_workflow_analytics_engine_coverage.py
autonomous: true

must_haves:
  truths:
    - "WorkflowAnalyticsEngine achieves 65%+ line coverage"
    - "Analytics computation methods tested"
    - "Metrics aggregation covered"
    - "Performance tracking verified"
  artifacts:
    - path: "backend/tests/core/workflow/test_workflow_analytics_engine_coverage.py"
      provides: "WorkflowAnalyticsEngine coverage tests"
      min_lines: 900
      exports: ["TestWorkflowAnalyticsEngineCoverage"]
  key_links:
    - from: "backend/tests/core/workflow/test_workflow_analytics_engine_coverage.py"
      to: "core/workflow_analytics_engine.py"
      via: "direct import"
      pattern: "from core.workflow_analytics_engine import"
---

<objective>
Achieve 65%+ line coverage on workflow_analytics_engine.py (561 statements, currently 0%)

Purpose: WorkflowAnalyticsEngine computes analytics for workflow executions including success rates, timing metrics, error analysis, and performance tracking. This service requires database queries and aggregation logic.

Output: Coverage-driven test file with 65%+ line coverage, analytics computation tested, metrics aggregation covered.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
@.planning/phases/191-coverage-push-60-70/191-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/workflow_analytics_engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test file and analytics computation tests</name>
  <files>backend/tests/core/workflow/test_workflow_analytics_engine_coverage.py</files>
  <action>
Create test file with coverage-driven structure:

```python
"""
Coverage-driven tests for WorkflowAnalyticsEngine (currently 0% -> target 65%+)

Target file: core/workflow_analytics_engine.py (561 statements)

Focus areas from coverage gap analysis:
- Engine initialization (lines 1-80)
- Success rate computation (lines 80-180)
- Timing metrics aggregation (lines 180-300)
- Error analysis (lines 300-450)
- Performance tracking (lines 450-561)
"""

import pytest
from datetime import datetime, timedelta, timezone
from core.workflow_analytics_engine import WorkflowAnalyticsEngine
from core.models import WorkflowExecution, WorkflowExecutionLog

class TestWorkflowAnalyticsEngineCoverage:
    """Coverage-driven tests for workflow_analytics_engine.py"""

    def test_engine_initialization(self, db_session):
        """Cover lines 1-80: Engine initialization"""
        engine = WorkflowAnalyticsEngine(db_session)
        assert engine.db is db_session

    def test_compute_success_rate(self, db_session):
        """Cover success rate computation (lines 80-180)"""
        # Create workflow executions with various statuses
        engine = WorkflowAnalyticsEngine(db_session)

        # Successful execution
        exec1 = WorkflowExecution(
            id="exec-1",
            workflow_id="wf-1",
            status="COMPLETED",
            started_at=datetime.now(timezone.utc) - timedelta(hours=1),
            completed_at=datetime.now(timezone.utc)
        )

        # Failed execution
        exec2 = WorkflowExecution(
            id="exec-2",
            workflow_id="wf-1",
            status="FAILED",
            started_at=datetime.now(timezone.utc) - timedelta(minutes=30),
            completed_at=datetime.now(timezone.utc)
        )

        db_session.add_all([exec1, exec2])
        db_session.commit()

        analytics = engine.compute_success_rate("wf-1")
        assert analytics["total"] == 2
        assert analytics["successful"] == 1
        assert analytics["failed"] == 1
        assert analytics["success_rate"] == 0.5
```

Test success rate computation with various execution statuses.
  </action>
  <verify>Test file created with success rate computation tests</verify>
  <done>Success rate calculation tested with completed/failed executions</done>
</task>

<task type="auto">
  <name>Task 2: Implement timing metrics tests</name>
  <files>backend/tests/core/workflow/test_workflow_analytics_engine_coverage.py</files>
  <action>
Add tests for timing metrics aggregation:

```python
def test_compute_timing_metrics(self, db_session):
    """Cover timing metrics aggregation (lines 180-300)"""
    engine = WorkflowAnalyticsEngine(db_session)

    # Create executions with varying durations
    exec1 = WorkflowExecution(
        id="exec-1",
        workflow_id="wf-1",
        status="COMPLETED",
        started_at=datetime(2026, 1, 1, 10, 0, 0, tzinfo=timezone.utc),
        completed_at=datetime(2026, 1, 1, 10, 5, 0, tzinfo=timezone.utc)  # 5 min
    )

    exec2 = WorkflowExecution(
        id="exec-2",
        workflow_id="wf-1",
        status="COMPLETED",
        started_at=datetime(2026, 1, 1, 11, 0, 0, tzinfo=timezone.utc),
        completed_at=datetime(2026, 1, 1, 11, 2, 30, tzinfo=timezone.utc)  # 2.5 min
    )

    db_session.add_all([exec1, exec2])
    db_session.commit()

    metrics = engine.compute_timing_metrics("wf-1")

    assert metrics["avg_duration_seconds"] > 0
    assert metrics["min_duration_seconds"] > 0
    assert metrics["max_duration_seconds"] > 0
    assert metrics["total_executions"] == 2

def test_step_timing_breakdown(self, db_session):
    """Cover step-level timing breakdown"""
    # Test timing analysis for individual steps
    pass
```

Test timing aggregation with various execution durations.
  </action>
  <verify>Timing metrics tested with multiple executions and step breakdown</verify>
  <done>Timing aggregation logic fully covered</done>
</task>

<task type="auto">
  <name>Task 3: Implement error analysis and performance tracking tests</name>
  <files>backend/tests/core/workflow/test_workflow_analytics_engine_coverage.py</files>
  <action>
Add tests for error analysis and performance tracking:

```python
def test_compute_error_analysis(self, db_session):
    """Cover error analysis (lines 300-450)"""
    engine = WorkflowAnalyticsEngine(db_session)

    # Create failed executions with errors
    exec1 = WorkflowExecution(
        id="exec-1",
        workflow_id="wf-1",
        status="FAILED",
        error_type="TimeoutError",
        error_message="Step timed out after 30s"
    )

    exec2 = WorkflowExecution(
        id="exec-2",
        workflow_id="wf-1",
        status="FAILED",
        error_type="ValueError",
        error_message="Invalid input parameter"
    )

    db_session.add_all([exec1, exec2])
    db_session.commit()

    error_analysis = engine.compute_error_analysis("wf-1")

    assert error_analysis["total_errors"] == 2
    assert "TimeoutError" in error_analysis["error_types"]
    assert "ValueError" in error_analysis["error_types"]

def test_performance_tracking(self, db_session):
    """Cover performance tracking (lines 450-561)"""
    engine = WorkflowAnalyticsEngine(db_session)

    # Create execution logs for performance tracking
    log1 = WorkflowExecutionLog(
        execution_id="exec-1",
        step_id="step-1",
        started_at=datetime.now(timezone.utc) - timedelta(seconds=10),
        completed_at=datetime.now(timezone.utc),
        status="COMPLETED"
    )

    db_session.add(log1)
    db_session.commit()

    performance = engine.get_performance_metrics("wf-1")
    assert performance["throughput_per_hour"] >= 0
```

Test error categorization and performance tracking.
  </action>
  <verify>Error analysis and performance tracking tested</verify>
  <done>Error categorization covered, performance metrics verified</done>
</task>

</tasks>

<verification>
After completion, verify:
1. Run: PYTHONPATH=backend pytest tests/core/workflow/test_workflow_analytics_engine_coverage.py -v --cov=core/workflow_analytics_engine --cov-branch --cov-report=term-missing
2. Check coverage achieves 65%+ line coverage
3. Verify analytics computation tested
4. Confirm timing aggregation covered
</verification>

<success_criteria>
1. 65%+ line coverage on workflow_analytics_engine.py (365+ statements covered)
2. Success rate computation tested
3. Timing metrics aggregation covered
4. Error analysis verified
5. Performance tracking tested
</success_criteria>

<output>
After completion, create `.planning/phases/191-coverage-push-60-70/191-10-SUMMARY.md` with:
- Actual coverage achieved
- Number of tests created
- Analytics methods covered
- Any VALIDATED_BUG findings
</output>
