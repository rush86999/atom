---
phase: 191-coverage-push-60-70
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/core/governance/test_agent_governance_service_coverage.py
autonomous: true

must_haves:
  truths:
    - "AgentGovernanceService achieves 75%+ line coverage"
    - "All maturity matrix combinations tested (4 levels x 4 complexities)"
    - "Permission check error paths tested (invalid inputs, missing agents)"
    - "Coverage verified with pytest --cov=branch"
  artifacts:
    - path: "backend/tests/core/governance/test_agent_governance_service_coverage.py"
      provides: "AgentGovernanceService coverage tests"
      min_lines: 800
      exports: ["TestAgentGovernanceServiceCoverage"]
  key_links:
    - from: "backend/tests/core/governance/test_agent_governance_service_coverage.py"
      to: "core/agent_governance_service.py"
      via: "direct import"
      pattern: "from core.agent_governance_service import"
---

<objective>
Achieve 75%+ line coverage on agent_governance_service.py (286 statements, currently 0%)

Purpose: AgentGovernanceService is the core governance component responsible for maturity-based routing, permission checks, and cache validation. This service controls which agents can perform which actions based on maturity level (STUDENT/INTERN/SUPERVISED/AUTONOMOUS) and action complexity (1-4).

Output: Coverage-driven test file with 75%+ line coverage, parametrized maturity matrix tests, error path testing, edge case coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
@.planning/phases/191-coverage-push-60-70/191-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/tests/core/test_agent_evolution_loop_coverage.py
@backend/core/agent_governance_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test file structure and initialization tests</name>
  <files>backend/tests/core/governance/test_agent_governance_service_coverage.py</files>
  <action>
Create test file with coverage-driven docstring pattern:

```python
"""
Coverage-driven tests for AgentGovernanceService (currently 0% -> target 75%+)

Target file: core/agent_governance_service.py (286 statements)

Focus areas from coverage gap analysis:
- __init__ and service initialization (lines 1-50)
- check_agent_permission maturity matrix (lines 50-150)
- Permission check error handling (lines 150-200)
- Cache invalidation methods (lines 200-250)
- Audit trail methods (lines 250-286)
"""

import pytest
from unittest.mock import Mock, AsyncMock, patch
from core.agent_governance_service import AgentGovernanceService
from core.models import AgentRegistry

class TestAgentGovernanceServiceCoverage:
    """Coverage-driven tests for agent_governance_service.py"""

    def test_service_initialization(self, db_session):
        """Cover lines 1-50: Service initialization with dependencies"""
        # Test initialization logic
```

Use db_session fixture from conftest.py for database access.
  </action>
  <verify>File created at backend/tests/core/governance/test_agent_governance_service_coverage.py with TestAgentGovernanceServiceCoverage class</verify>
  <done>Test file structure exists, imports working, db_session fixture available</done>
</task>

<task type="auto">
  <name>Task 2: Implement maturity matrix parametrized tests</name>
  <files>backend/tests/core/governance/test_agent_governance_service_coverage.py</files>
  <action>
Add parametrized test for maturity matrix (4 levels x 4 complexities = 16 combinations):

```python
@pytest.mark.parametrize("maturity,action,complexity,expected_allowed", [
    # STUDENT maturity (0.0-0.5)
    ("STUDENT", "present_canvas", 1, True),   # Action complexity 1: STUDENT+
    ("STUDENT", "stream_llm", 2, False),      # Action complexity 2: INTERN+
    ("STUDENT", "submit_form", 3, False),     # Action complexity 3: SUPERVISED+
    ("STUDENT", "delete_agent", 4, False),    # Action complexity 4: AUTONOMOUS only

    # INTERN maturity (0.5-0.7)
    ("INTERN", "present_canvas", 1, True),
    ("INTERN", "stream_llm", 2, True),
    ("INTERN", "submit_form", 3, False),
    ("INTERN", "delete_agent", 4, False),

    # SUPERVISED maturity (0.7-0.9)
    ("SUPERVISED", "present_canvas", 1, True),
    ("SUPERVISED", "stream_llm", 2, True),
    ("SUPERVISED", "submit_form", 3, True),
    ("SUPERVISED", "delete_agent", 4, False),

    # AUTONOMOUS maturity (>0.9)
    ("AUTONOMOUS", "present_canvas", 1, True),
    ("AUTONOMOUS", "stream_llm", 2, True),
    ("AUTONOMOUS", "submit_form", 3, True),
    ("AUTONOMOUS", "delete_agent", 4, True),
])
def test_maturity_matrix_enforcement(self, maturity, action, complexity, expected_allowed, db_session):
    """Cover maturity matrix enforcement (lines 50-150)"""
    service = AgentGovernanceService(db_session)
    agent = AgentRegistry(
        id="test-agent",
        maturity_level=maturity,
        confidence_score=0.8
    )
    result = service.check_agent_permission(agent, action, complexity)
    assert result.allowed == expected_allowed
```

This follows the proven pattern from Phase 189 cognitive tier system tests.
  </action>
  <verify>16 parametrized test cases added, all maturity/action/complexity combinations covered</verify>
  <done>Maturity matrix fully covered with parametrized tests, all combinations tested</done>
</task>

<task type="auto">
  <name>Task 3: Implement error path and edge case tests</name>
  <files>backend/tests/core/governance/test_agent_governance_service_coverage.py</files>
  <action>
Add edge case tests for invalid inputs (boundary conditions):

```python
@pytest.mark.parametrize("invalid_input,error_type", [
    (None, ValueError),           # None agent_id
    ("", ValueError),              # Empty string agent_id
    ("   ", ValueError),           # Whitespace agent_id
    ("x"*500, ValueError),         # Oversized agent_id
    (-1, ValueError),              # Negative confidence_score
    (1.5, ValueError),             # Confidence > 1.0
    ("INVALID", ValueError),       # Invalid maturity_level
    (5, ValueError),               # Invalid complexity (< 1 or > 4)
])
def test_permission_check_invalid_inputs(self, invalid_input, error_type, db_session):
    """Cover invalid input handling (lines 150-200)"""
    service = AgentGovernanceService(db_session)

    with pytest.raises(error_type):
        # Test various invalid input scenarios
        pass
```

Also test cache invalidation and audit trail methods.
  </action>
  <verify>Edge case tests added for None inputs, empty strings, boundary values, invalid enums</verify>
  <done>Error paths covered, boundary conditions tested, cache and audit methods exercised</done>
</task>

</tasks>

<verification>
After completion, verify:
1. Run: PYTHONPATH=backend pytest tests/core/governance/test_agent_governance_service_coverage.py -v --cov=core/agent_governance_service --cov-branch --cov-report=term-missing
2. Check coverage achieves 75%+ line coverage
3. Verify all 16 maturity matrix combinations tested
4. Confirm edge cases and error paths covered
</verification>

<success_criteria>
1. 75%+ line coverage on agent_governance_service.py (215+ statements covered)
2. 16 parametrized maturity matrix tests passing
3. Edge case tests for boundary conditions passing
4. Coverage report shows specific line coverage (not estimated)
</success_criteria>

<output>
After completion, create `.planning/phases/191-coverage-push-60-70/191-01-SUMMARY.md` with:
- Actual coverage achieved (line count and percentage)
- Number of tests created and passing
- Any VALIDATED_BUG findings
- Deviations from plan (if any)
</output>
