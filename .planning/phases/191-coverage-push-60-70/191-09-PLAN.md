---
phase: 191-coverage-push-60-70
plan: 09
type: execute
wave: 2
depends_on: ["191-01", "191-02", "191-03", "191-04", "191-05"]
files_modified:
  - backend/tests/core/workflow/test_workflow_engine_coverage_extend.py
autonomous: true

must_haves:
  truths:
    - "WorkflowEngine coverage extended from 5% to 60%+"
    - "Core workflow execution methods covered"
    - "Step execution logic tested"
    - "Complex async paths handled with appropriate mocks"
  artifacts:
    - path: "backend/tests/core/workflow/test_workflow_engine_coverage_extend.py"
      provides: "Extended WorkflowEngine coverage tests"
      min_lines: 1200
      exports: ["TestWorkflowEngineExtended"]
  key_links:
    - from: "backend/tests/core/workflow/test_workflow_engine_coverage_extend.py"
      to: "core/workflow_engine.py"
      via: "direct import"
      pattern: "from core.workflow_engine import"
---

<objective>
Extend WorkflowEngine coverage from 5% to 60%+ (1,163 statements, currently 5%)

Purpose: WorkflowEngine is the largest file with 1,163 statements and complex async methods. Phase 189 achieved 5% coverage. This plan extends to 60%+ by focusing on testable code paths and using extensive mocking for complex async operations.

Output: Extended coverage test file reaching 60%+ line coverage, core workflow execution tested, step execution covered, async handling with mocks.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
@.planning/phases/191-coverage-push-60-70/191-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/workflow_engine.py
@backend/tests/test_workflow_engine_coverage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend existing test file with initialization tests</name>
  <files>backend/tests/core/workflow/test_workflow_engine_coverage_extend.py</files>
  <action>
Create extended test file to build on existing coverage:

```python
"""
Extended coverage tests for WorkflowEngine (currently 5% -> target 60%+)

Target file: core/workflow_engine.py (1,163 statements)

This file extends existing coverage from test_workflow_engine_coverage.py
by targeting additional uncovered lines.

Focus areas (building on Phase 189 5% baseline):
- Enhanced initialization (lines 1-100)
- Workflow validation methods (lines 100-250)
- Step executor configuration (lines 250-400)
- Error handling paths (lines 400-600)
- State management (lines 600-800)

Note: Complex async methods (_execute_workflow_graph at 261 statements) are
deemed acceptable to skip due to extensive mocking requirements. Focus on
testable synchronous methods and simpler async paths.
"""

import pytest
from unittest.mock import Mock, AsyncMock, patch, MagicMock
from core.workflow_engine import WorkflowEngine, WorkflowState

class TestWorkflowEngineExtended:
    """Extended coverage tests for workflow_engine.py"""

    def test_engine_initialization_with_config(self, db_session):
        """Cover lines 1-100: Enhanced initialization with config"""
        config = {
            "max_steps": 1000,
            "timeout_seconds": 300,
            "retry_attempts": 3
        }
        engine = WorkflowEngine(db_session, config=config)
        assert engine.config["max_steps"] == 1000
        assert engine.config["timeout_seconds"] == 300

    def test_workflow_validation_success(self, db_session):
        """Cover workflow validation (lines 100-150)"""
        engine = WorkflowEngine(db_session)

        workflow_def = {
            "id": "wf-1",
            "steps": [
                {"id": "step-1", "action": "task", "target": "service.method"}
            ]
        }

        is_valid, errors = engine.validate_workflow(workflow_def)
        assert is_valid is True
        assert len(errors) == 0
```

Build on existing tests to cover initialization and validation.
  </action>
  <verify>Extended test file created with initialization and validation tests</verify>
  <done>Engine initialization tested with various config options</done>
</task>

<task type="auto">
  <name>Task 2: Implement step executor tests</name>
  <files>backend/tests/core/workflow/test_workflow_engine_coverage_extend.py</files>
  <action>
Add tests for step executor configuration and execution:

```python
@pytest.mark.asyncio
async def test_step_executor_configuration(self, db_session):
    """Cover step executor configuration (lines 250-400)"""
    engine = WorkflowEngine(db_session)

    step_def = {
        "id": "step-1",
        "action": "http_request",
        "config": {
            "url": "https://api.example.com",
            "method": "POST",
            "timeout": 30
        }
    }

    executor = engine._create_step_executor(step_def)
    assert executor is not None
    assert executor.config["url"] == "https://api.example.com"

@pytest.mark.asyncio
async def test_step_execution_success(self, mocker, db_session):
    """Cover successful step execution (lines 400-500)"""
    # Mock external service call
    mock_http = mocker.patch("core.workflow_engine.httpx.AsyncClient")
    mock_response = Mock()
    mock_response.status_code = 200
    mock_response.json.return_value = {"result": "success"}
    mock_http.return_value.__aenter__.return_value.post.return_value = mock_response

    engine = WorkflowEngine(db_session)
    result = await engine._execute_step({
        "id": "step-1",
        "action": "http_request",
        "config": {"url": "https://api.example.com"}
    })

    assert result["status"] == "success"
```

Test step executor creation and simple execution paths.
  </action>
  <verify>Step executor configuration and execution tested</verify>
  <done>Step execution logic covered with mocked external calls</done>
</task>

<task type="auto">
  <name>Task 3: Implement error handling and state management tests</name>
  <files>backend/tests/core/workflow/test_workflow_engine_coverage_extend.py</files>
  <action>
Add tests for error handling and state management:

```python
@pytest.mark.asyncio
async def test_step_execution_error_handling(self, mocker, db_session):
    """Cover error handling (lines 400-600)"""
    # Mock failed HTTP call
    mock_http = mocker.patch("core.workflow_engine.httpx.AsyncClient")
    mock_http.return_value.__aenter__.return_value.post.side_effect = Exception("Network error")

    engine = WorkflowEngine(db_session)
    result = await engine._execute_step({
        "id": "step-1",
        "action": "http_request",
        "config": {"url": "https://api.example.com"}
    })

    assert result["status"] == "failed"
    assert "error" in result

def test_state_management(self, db_session):
    """Cover state management (lines 600-800)"""
    engine = WorkflowEngine(db_session)

    # Test state transitions
    assert engine.state == WorkflowState.IDLE

    engine._transition_to(WorkflowState.RUNNING)
    assert engine.state == WorkflowState.RUNNING

    engine._transition_to(WorkflowState.COMPLETED)
    assert engine.state == WorkflowState.COMPLETED

def test_state_history_tracking(self, db_session):
    """Cover state history tracking"""
    engine = WorkflowEngine(db_session)

    engine._transition_to(WorkflowState.RUNNING)
    engine._transition_to(WorkflowState.COMPLETED)

    history = engine.get_state_history()
    assert len(history) >= 2
    assert WorkflowState.RUNNING in [h["state"] for h in history]
    assert WorkflowState.COMPLETED in [h["state"] for h in history]
```

Test error handling paths and state management.
  </action>
  <verify>Error handling and state management tested</verify>
  <done>Error paths covered, state transitions verified</done>
</task>

</tasks>

<verification>
After completion, verify:
1. Run: PYTHONPATH=backend pytest tests/core/workflow/test_workflow_engine_coverage_extend.py -v --cov=core/workflow_engine --cov-branch --cov-report=term-missing
2. Check coverage achieves 60%+ line coverage (698+ statements)
3. Verify step execution paths tested
4. Confirm error handling covered
</verification>

<success_criteria>
1. 60%+ line coverage on workflow_engine.py (698+ statements covered)
2. Core workflow execution methods tested
3. Step executor configuration covered
4. Error handling paths tested
5. State management verified
</success_criteria>

<output>
After completion, create `.planning/phases/191-coverage-push-60-70/191-09-SUMMARY.md` with:
- Actual coverage achieved (target: 60%+)
- Number of new tests added
- Methods covered
- Any VALIDATED_BUG findings
</output>
