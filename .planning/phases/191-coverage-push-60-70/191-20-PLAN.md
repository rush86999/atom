---
phase: 191-coverage-push-60-70
plan: 20
type: execute
wave: 4
depends_on: ["191-11", "191-12", "191-13", "191-14", "191-15"]
files_modified:
  - backend/tests/core/skills/test_skill_marketplace_service_coverage_extend.py
autonomous: true

must_haves:
  truths:
    - "SkillMarketplaceService coverage extended from 56% to 75%+"
    - "Skill search tested"
    - "Rating aggregation covered"
    - "Category filtering verified"
  artifacts:
    - path: "backend/tests/core/skills/test_skill_marketplace_service_coverage_extend.py"
      provides: "Extended SkillMarketplaceService coverage tests"
      min_lines: 500
      exports: ["TestSkillMarketplaceServiceExtended"]
  key_links:
    - from: "backend/tests/core/skills/test_skill_marketplace_service_coverage_extend.py"
      to: "core/skill_marketplace_service.py"
      via: "direct import"
      pattern: "from core.skill_marketplace_service import"
---

<objective>
Extend SkillMarketplaceService coverage from 56% to 75%+ (102 statements)

Purpose: SkillMarketplaceService provides skill search, rating aggregation, and category filtering for the community skills marketplace. Phase 183 achieved 56% coverage. This plan extends to 75%+ by filling gaps in search, ratings, and filtering.

Output: Extended coverage test file reaching 75%+ line coverage, skill search tested, rating aggregation covered.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
@.planning/phases/191-coverage-push-60-70/191-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/skill_marketplace_service.py
@backend/tests/core/skills/test_skill_marketplace_service_coverage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create extended test file with search tests</name>
  <files>backend/tests/core/skills/test_skill_marketplace_service_coverage_extend.py</files>
  <action>
Create extended test file to fill remaining coverage gaps:

```python
"""
Extended coverage tests for SkillMarketplaceService (currently 56% -> target 75%+)

Target file: core/skill_marketplace_service.py (102 statements)

This file extends existing coverage from test_skill_marketplace_service_coverage.py
by targeting remaining uncovered lines.

Focus areas (building on Phase 183 56% baseline):
- Enhanced search (lines 1-30)
- Category filtering (lines 30-60)
- Rating aggregation (lines 60-90)
- Trending skills (lines 90-102)
"""

import pytest
from core.skill_marketplace_service import SkillMarketplaceService

class TestSkillMarketplaceServiceExtended:
    """Extended coverage tests for skill_marketplace_service.py"""

    def test_search_with_fuzzy_match(self, db_session):
        """Cover enhanced search with fuzzy matching (lines 1-30)"""
        service = SkillMarketplaceService(db_session)

        # Create test skills
        # (Use factory or direct model creation)

        results = service.search_skills("excel", fuzzy=True)

        # Should find skills with similar names (e.g., "xls", "spreadsheet")
        assert len(results) >= 0

    def test_search_with_filters(self, db_session):
        """Cover search with multiple filters"""
        service = SkillMarketplaceService(db_session)

        results = service.search_skills(
            query="automation",
            category="productivity",
            min_rating=4.0,
            max_results=10
        )

        assert len(results) <= 10
        for skill in results:
            assert skill.category == "productivity"
            if skill.rating:
                assert skill.rating >= 4.0
```

Test skill search with fuzzy matching and filters.
  </action>
  <verify>Extended test file created with search enhancement tests</verify>
  <done>Skill search tested with fuzzy matching and filters</done>
</task>

<task type="auto">
  <name>Task 2: Implement category filtering tests</name>
  <files>backend/tests/core/skills/test_skill_marketplace_service_coverage_extend.py</files>
  <action>
Add tests for category filtering:

```python
def test_category_filtering(self, db_session):
    """Cover category filtering (lines 30-60)"""
    service = SkillMarketplaceService(db_session)

    # Get all categories
    categories = service.get_all_categories()

    assert "productivity" in categories
    assert "automation" in categories

    # Filter by specific category
    productivity_skills = service.get_skills_by_category("productivity")

    for skill in productivity_skills:
        assert skill.category == "productivity"

def test_subcategory_filtering(self, db_session):
    """Cover subcategory filtering"""
    service = SkillMarketplaceService(db_session)

    # Test hierarchical categories (e.g., productivity/task-management)
    subcategory_skills = service.get_skills_by_category(
        "productivity",
        subcategory="task-management"
    )

    for skill in subcategory_skills:
        assert skill.category == "productivity"
        assert skill.subcategory == "task-management"
```

Test category and subcategory filtering.
  </action>
  <verify>Category filtering tested with hierarchical categories</verify>
  <done>Category operations fully covered</done>
</task>

<task type="auto">
  <name>Task 3: Implement rating and trending tests</name>
  <files>backend/tests/core/skills/test_skill_marketplace_service_coverage_extend.py</files>
  <action>
Add tests for rating aggregation and trending skills:

```python
def test_rating_aggregation(self, db_session):
    """Cover rating aggregation (lines 60-90)"""
    service = SkillMarketplaceService(db_session)

    # Get aggregated ratings for a skill
    ratings = service.get_aggregated_ratings("test-skill")

    assert "average" in ratings
    assert "count" in ratings
    assert "distribution" in ratings

    # Verify 5-star distribution
    if ratings["count"] > 0:
        assert 1 <= ratings["average"] <= 5

def test_weighted_rating_calculation(self, db_session):
    """Cover weighted rating (more recent ratings weight more)"""
    service = SkillMarketplaceService(db_session)

    weighted = service.get_weighted_rating("test-skill")

    # Weighted rating should be between 1 and 5
    if weighted is not None:
        assert 1 <= weighted <= 5

def test_trending_skills(self, db_session):
    """Cover trending skills calculation (lines 90-102)"""
    service = SkillMarketplaceService(db_session)

    trending = service.get_trending_skills(
        time_period_days=7,
        limit=10
    )

    assert len(trending) <= 10
    # Verify trending order (most downloaded first)
    if len(trending) > 1:
        assert trending[0].trend_score >= trending[1].trend_score
```

Test rating aggregation and trending skills calculation.
  </action>
  <verify>Rating aggregation tested, trending calculation verified</verify>
  <done>Rating system fully covered, trending algorithm tested</done>
</task>

</tasks>

<verification>
After completion, verify:
1. Run: PYTHONPATH=backend pytest tests/core/skills/test_skill_marketplace_service_coverage_extend.py -v --cov=core/skill_marketplace_service --cov-branch --cov-report=term-missing
2. Check coverage achieves 75%+ line coverage
3. Verify search functionality tested
4. Confirm rating aggregation covered
</verification>

<success_criteria>
1. 75%+ line coverage on skill_marketplace_service.py (77+ statements covered)
2. Enhanced search with fuzzy matching tested
3. Category filtering covered with subcategories
4. Rating aggregation verified
5. Trending skills calculation tested
</success_criteria>

<output>
After completion, create `.planning/phases/191-coverage-push-60-70/191-20-SUMMARY.md` with:
- Actual coverage achieved (target: 75%+)
- Number of new tests added
- Marketplace features covered
- Any VALIDATED_BUG findings
</output>
