---
phase: 088-bug-discovery-error-paths-boundaries
plan: 04
type: execute
wave: 2
depends_on: ["088-01"]
files_modified:
  - backend/core/episode_segmentation_service.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Race conditions in episode segmentation are prevented or detected"
    - "EpisodeSegmentationService no longer accesses session.workspace_id (ChatSession doesn't have this field)"
    - "All 8 failing tests (3 error_paths, 5 concurrent_operations) now pass"
  artifacts:
    - path: "backend/core/episode_segmentation_service.py"
      provides: "Fixed episode segmentation service without workspace_id bug"
      min_lines: 1500
  key_links:
    - from: "backend/core/episode_segmentation_service.py"
      to: "backend/core/models.py (ChatSession)"
      via: "Service uses hardcoded 'default' workspace_id instead of accessing session field"
      pattern: "workspace_id.*=.*\"default\""
---

<objective>
Fix EpisodeSegmentationService workspace_id bug that blocks 8 tests from passing.

**Purpose:** The service at line 249 accesses `session.workspace_id` but ChatSession model doesn't have this field (see models.py line 1046-1061). This causes AttributeError in 3 error_paths tests and 5 concurrent_operations tests.

**Gap Reference:** VERIFICATION.md Gap 1 - "EpisodeSegmentationService workspace_id Bug (Production Code)"

**Output:** Fixed service code that doesn't access non-existent session.workspace_id field, enabling all 8 blocked tests to pass.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/088-bug-discovery-error-paths-boundaries/088-VERIFICATION.md
@.planning/phases/088-bug-discovery-error-paths-boundaries/088-01-SUMMARY.md
@.planning/phases/088-bug-discovery-error-paths-boundaries/088-03-SUMMARY.md
@backend/core/episode_segmentation_service.py
@backend/core/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix workspace_id bug in EpisodeSegmentationService</name>
  <files>backend/core/episode_segmentation_service.py</files>
  <action>
    Fix the workspace_id bug at line 249 in create_episode_from_session() method.

    Current code (BUGGY):
    ```python
    workspace_id=session.workspace_id or "default",  # Line 249 - AttributeError!
    ```

    The ChatSession model (core/models.py:1046-1061) does NOT have a workspace_id field.
    EpisodeSegmentationService already has a comment at line 249 acknowledging this:
    "# Single-tenant: always use default (ChatSession doesn't have workspace_id field)"

    The fix is to use the hardcoded "default" value directly without accessing session:
    ```python
    workspace_id="default",  # Single-tenant: always use default workspace
    ```

    This is consistent with the existing comment and single-tenant architecture.

    DO NOT:
    - Add workspace_id column to ChatSession (architectural decision outside this gap closure scope)
    - Change other parts of the episode creation logic
    - Modify the Episode model schema

    Simply remove the `session.workspace_id or` part from line 249.
  </action>
  <verify>
    grep -n "workspace_id=" backend/core/episode_segmentation_service.py shows line 249 uses hardcoded "default"
  </verify>
  <done>
    Line 249 no longer accesses session.workspace_id, uses hardcoded "default" value
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify the fix resolves the 8 blocked tests</name>
  <files>backend/tests/error_paths/test_episode_segmentation_error_paths.py</files>
  <action>
    Run the 3 error_paths tests and 5 concurrent_operations tests that were failing due to workspace_id bug.

    Tests that should now pass:
    - test_concurrent_episode_creation (test_episode_concurrency.py)
    - test_concurrent_segmentation_operations (test_episode_concurrency.py)
    - test_concurrent_lancedb_archival (test_episode_concurrency.py)
    - test_concurrent_canvas_context_extraction (test_episode_concurrency.py)
    - test_async_resource_cleanup_on_error (test_episode_concurrency.py)
    - Any episode error path tests that use ChatSession fixtures

    Run commands:
    ```bash
    pytest backend/tests/error_paths/test_episode_segmentation_error_paths.py -v -k "episode" --tb=short
    pytest backend/tests/concurrent_operations/test_episode_concurrency.py -v --tb=short
    ```

    Verify all 8 previously failing tests now pass.
  </action>
  <verify>
    pytest backend/tests/error_paths/test_episode_segmentation_error_paths.py -v shows 0 failed tests
    pytest backend/tests/concurrent_operations/test_episode_concurrency.py -v shows 0 failed tests
  </verify>
  <done>
    All 8 previously failing tests now pass (0 AttributeError: 'ChatSession' object has no attribute 'workspace_id')
  </done>
</task>

<task type="auto">
  <name>Task 3: Update BUG_FINDINGS.md with fix commit</name>
  <files>backend/tests/error_paths/BUG_FINDINGS.md</files>
  <action>
    Add a new bug entry to BUG_FINDINGS.md documenting the workspace_id bug that was just fixed:

    ```markdown
    ### Bug #9: EpisodeSegmentationService workspace_id AttributeError

    **File:** `backend/core/episode_segmentation_service.py`
    **Line:** 249
    **Found By:** `test_concurrent_episode_creation` and 7 other tests in Phase 088
    **Status:** FIXED
    **Fix Commit:** [ADD_COMMIT_HASH_HERE]
    **Severity:** HIGH
    **Impact:** Blocks 8 tests from passing, AttributeError in production if session creation logic changes

    **Description:**
    Service accesses `session.workspace_id` but ChatSession model doesn't have this field.
    Line 249: `workspace_id=session.workspace_id or "default"` causes AttributeError.

    **Fix:**
    Changed to hardcoded "default" value consistent with single-tenant architecture:
    `workspace_id="default"  # Single-tenant: always use default workspace`

    **Validated:** ✅ Fixed and all 8 blocked tests now pass
    ```

    Note: The commit hash should be added after the fix is committed.
  </action>
  <verify>
    grep -q "Bug #9: EpisodeSegmentationService workspace_id" backend/tests/error_paths/BUG_FINDINGS.md
  </verify>
  <done>
    BUG_FINDINGS.md updated with Bug #9 documenting the workspace_id fix
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:
1. pytest backend/tests/error_paths/test_episode_segmentation_error_paths.py -v passes (0 failures)
2. pytest backend/tests/concurrent_operations/test_episode_concurrency.py -v passes (0 failures)
3. No AttributeError: 'ChatSession' object has no attribute 'workspace_id' in test output
4. BUG_FINDINGS.md documents Bug #9 with fix status
</verification>

<success_criteria>
1. EpisodeSegmentationService line 249 uses hardcoded "default" workspace_id
2. All 3 episode error path tests that failed now pass
3. All 5 episode concurrent tests that failed now pass
4. BUG_FINDINGS.md updated with Bug #9 documentation
5. Git commit created with fix message
</success_criteria>

<output>
After completion, create `.planning/phases/088-bug-discovery-error-paths-boundaries/088-04-SUMMARY.md` with:
- Bug fix details (workspace_id hardcoded instead of accessing session field)
- Test results (8 previously failing tests now pass)
- Commit hash for the fix
</output>
