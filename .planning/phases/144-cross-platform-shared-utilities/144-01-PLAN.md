---
phase: 144-cross-platform-shared-utilities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/shared/test-utils/index.ts
  - frontend-nextjs/shared/test-utils/types.ts
  - frontend-nextjs/tsconfig.json
  - frontend-nextjs/jest.config.js
  - .planning/phases/144-cross-platform-shared-utilities/144-01-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "Shared test utilities directory exists at frontend-nextjs/shared/test-utils/"
    - "Barrel export (index.ts) exports all utility modules"
    - "Shared types defined for common test patterns"
    - "TypeScript path mapping configured for @atom/test-utils"
    - "Jest moduleNameMapper configured for @atom/test-utils"
  artifacts:
    - path: "frontend-nextjs/shared/test-utils/index.ts"
      provides: "Main export barrel for shared test utilities"
      min_lines: 30
      contains: "export"
    - path: "frontend-nextjs/shared/test-utils/types.ts"
      provides: "Shared TypeScript types for testing"
      min_lines: 50
      contains: "MockWebSocket"
      contains: "MockAgent"
      contains: "MockWorkflow"
    - path: "frontend-nextjs/tsconfig.json"
      provides: "TypeScript configuration with path mapping"
      contains: "@atom/test-utils"
    - path: "frontend-nextjs/jest.config.js"
      provides: "Jest configuration with module resolution"
      contains: "moduleNameMapper"
      contains: "@atom/test-utils"
---

<objective>
**Create shared test utilities infrastructure** establishing the directory structure, barrel exports, and TypeScript configuration for cross-platform test utilities used across frontend, mobile, and desktop platforms.

**Purpose:** Platform-specific test infrastructure in Phases 134, 139, and 143 created duplicate utilities (async helpers, mocks, assertions) across frontend-nextjs, mobile, and desktop. This plan creates the foundation for sharing these utilities, reducing code duplication and ensuring consistency.

**Output:**
- Shared utilities directory at `frontend-nextjs/shared/test-utils/`
- Main barrel export (`index.ts`) for all utility modules
- Shared TypeScript types (`types.ts`) for common test patterns
- TypeScript path mapping (`@atom/test-utils`) in frontend tsconfig.json
- Jest module name mapper configuration for runtime resolution
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-cross-platform-shared-utilities/144-RESEARCH.md
@mobile/src/__tests__/helpers/testUtils.ts
@frontend-nextjs/jest.setup.js
@frontend-nextjs/tsconfig.json
@frontend-nextjs/jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared test utilities directory structure and barrel export</name>
  <files>frontend-nextjs/shared/test-utils/index.ts</files>
  <action>
Create the shared test utilities directory structure and main barrel export:

1. Create directory:
   ```bash
   mkdir -p frontend-nextjs/shared/test-utils
   ```

2. Create `frontend-nextjs/shared/test-utils/index.ts` with barrel exports:
   - Add module documentation explaining cross-platform usage
   - Export types from `./types` (to be created in Task 2)
   - Add placeholder exports for upcoming modules:
     - `./async-utils` (Plan 02)
     - `./mock-factories` (Plan 03)
     - `./assertions` (Plan 03)
     - `./cleanup` (Plan 04)
     - `./platform-guards` (Plan 04)
     - `./test-data` (future)

3. Structure:
   ```typescript
   /**
    * Shared Test Utilities for Atom
    *
    * Platform-agnostic test helpers shared across:
    * - Frontend (Next.js with @testing-library/react)
    * - Mobile (React Native with @testing-library/react-native)
    * - Desktop (Tauri with cargo test - JSON fixtures only)
    *
    * Import via: @atom/test-utils
    *
    * @example
    * import { waitForAsync, createMockWebSocket } from '@atom/test-utils';
    */

   // Re-export all utility modules
   export * from './types';
   export * from './async-utils';
   export * from './mock-factories';
   export * from './assertions';
   export * from './cleanup';
   export * from './platform-guards';
   ```

Note: This will have compilation errors until subsequent plans create the modules.
  </action>
  <verify>
```bash
ls -la frontend-nextjs/shared/test-utils/
cat frontend-nextjs/shared/test-utils/index.ts
```
  </verify>
  <done>
Shared directory exists with index.ts barrel export containing all module re-exports
  </done>
</task>

<task type="auto">
  <name>Task 2: Create shared TypeScript types for test utilities</name>
  <files>frontend-nextjs/shared/test-utils/types.ts</files>
  <action>
Create `frontend-nextjs/shared/test-utils/types.ts` with shared TypeScript types:

1. Add module documentation for type definitions

2. Define mock interfaces based on existing patterns from testUtils.ts:
   ```typescript
   /**
    * Mock WebSocket interface for testing WebSocket-dependent components
    * Works across web (native WebSocket), mobile (ReconnectingWebSocket), and desktop
    */
   export interface MockWebSocket {
     url: string;
     connected: boolean;
     onopen: ((event: MessageEvent) => void) | null;
     onmessage: ((event: MessageEvent) => void) | null;
     onerror: ((event: Event) => void) | null;
     onclose: ((event: CloseEvent) => void) | null;
     send: jest.Mock;
     close: jest.Mock;
     addEventListener: jest.Mock;
     removeEventListener: jest.Mock;
   }

   /**
    * Mock agent data structure for agent governance tests
    */
   export interface MockAgent {
     id: string;
     name: string;
     maturity: 'STUDENT' | 'INTERN' | 'SUPERVISED' | 'AUTONOMOUS';
     confidence?: number;
   }

   /**
    * Mock workflow data structure for workflow tests
    */
   export interface MockWorkflow {
     id: string;
     name: string;
     steps: number;
     status?: 'pending' | 'running' | 'completed' | 'failed';
   }

   /**
    * Mock user data structure for authentication tests
    */
   export interface MockUser {
     id: string;
     name: string;
     email: string;
   }

   /**
    * Platform detection result type
    */
   export type PlatformType = 'web' | 'ios' | 'android' | 'desktop' | 'unknown';

   /**
    * Test data fixture configuration
    */
   export interface TestDataFixture {
     agents: MockAgent[];
     workflows: MockWorkflow[];
     user: MockUser;
   }
   ```

3. Export all types for use across platforms
  </action>
  <verify>
```bash
cat frontend-nextjs/shared/test-utils/types.ts
grep -c "export interface" frontend-nextjs/shared/test-utils/types.ts
```
  </verify>
  <done>
types.ts exists with at least 5 exported interfaces and comprehensive JSDoc documentation
  </done>
</task>

<task type="auto">
  <name>Task 3: Configure TypeScript path mapping for @atom/test-utils</name>
  <files>frontend-nextjs/tsconfig.json</files>
  <action>
Update `frontend-nextjs/tsconfig.json` to add path mapping for shared utilities:

1. Add to existing `paths` object in `compilerOptions`:
   ```json
   "@atom/test-utils": ["./shared/test-utils"],
   "@atom/test-utils/*": ["./shared/test-utils/*"]
   ```

2. Place after existing `@shared/*` paths (around line 53-54) for logical grouping

3. Verify the baseUrl is "." (already configured)

The mapping allows TypeScript to resolve:
- `import { waitForAsync } from '@atom/test-utils'` → `./shared/test-utils/index.ts`
- `import { MockWebSocket } from '@atom/test-utils/types'` → `./shared/test-utils/types.ts`
  </action>
  <verify>
```bash
grep "@atom/test-utils" frontend-nextjs/tsconfig.json
cat frontend-nextjs/tsconfig.json | jq .compilerOptions.paths
```
  </verify>
  <done>
tsconfig.json contains @atom/test-utils path mapping in compilerOptions.paths
  </done>
</task>

<task type="auto">
  <name>Task 4: Configure Jest moduleNameMapper for @atom/test-utils</name>
  <files>frontend-nextjs/jest.config.js</files>
  <action>
Update `frontend-nextjs/jest.config.js` to add moduleNameMapper entry:

1. Add to existing `moduleNameMapper` object:
   ```javascript
   "^@atom/test-utils(.*)$": "<rootDir>/shared/test-utils$1"
   ```

2. Place after existing `@hooks/(.*)$` entry (around line 103) for logical grouping

3. This ensures Jest runtime resolution matches TypeScript compile-time resolution

Note: The regex pattern `(.*)$` captures subpaths like `/types` for proper resolution
  </action>
  <verify>
```bash
grep "@atom/test-utils" frontend-nextjs/jest.config.js
```
  </verify>
  <done>
jest.config.js contains @atom/test-utils moduleNameMapper entry with regex pattern
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Verify TypeScript compiles: `cd frontend-nextjs && npx tsc --noEmit`
2. Verify directory structure: `ls -la frontend-nextjs/shared/test-utils/`
3. Verify exports are accessible (should fail until Plans 02-04 create modules)
4. Check tsconfig.json path mapping is valid JSON
</verification>

<success_criteria>
1. Shared utilities directory exists at frontend-nextjs/shared/test-utils/
2. index.ts barrel export re-exports all utility modules
3. types.ts defines at least 5 shared interfaces
4. TypeScript path mapping configured for @atom/test-utils
5. Jest moduleNameMapper configured for @atom/test-utils
6. No JSON syntax errors in configuration files
</success_criteria>

<output>
After completion, create `.planning/phases/144-cross-platform-shared-utilities/144-01-SUMMARY.md`
</output>
