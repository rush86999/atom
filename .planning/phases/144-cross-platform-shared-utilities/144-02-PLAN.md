---
phase: 144-cross-platform-shared-utilities
plan: 02
type: execute
wave: 2
depends_on: [144-01]
files_modified:
  - frontend-nextjs/shared/test-utils/async-utils.ts
  - frontend-nextjs/shared/test-utils/index.ts
  - .planning/phases/144-cross-platform-shared-utilities/144-02-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "Platform-agnostic async utilities created (waitForAsync, flushPromises, waitForCondition)"
    - "Utilities work with both @testing-library/react and @testing-library/react-native"
    - "Async utilities support Jest fake timers"
    - "All functions have JSDoc documentation with examples"
    - "Module exports match mobile testUtils.ts patterns"
  artifacts:
    - path: "frontend-nextjs/shared/test-utils/async-utils.ts"
      provides: "Platform-agnostic async test utilities"
      min_lines: 150
      contains: "waitForAsync"
      contains: "flushPromises"
      contains: "waitForCondition"
      contains: "wait"
      contains: "advanceTimersByTime"
      contains: "advanceTimersByTimeSync"
  key_links:
    - from: "frontend-nextjs/shared/test-utils/async-utils.ts"
      to: "mobile/src/__tests__/helpers/testUtils.ts"
      via: "pattern extraction from existing mobile utilities"
      pattern: "waitForAsync|flushPromises|waitForCondition"
---

<objective>
**Implement platform-agnostic async utilities** for cross-platform testing, extracting patterns from existing mobile testUtils.ts while ensuring compatibility with web (jsdom) and React Native test environments.

**Purpose:** Async utilities (waitFor, flushPromises, waitForCondition) are duplicated across mobile (622 lines in testUtils.ts) and need to be shared. This plan creates platform-agnostic versions that work with both @testing-library/react and @testing-library/react-native waitFor functions.

**Output:**
- async-utils.ts module with 6+ async utility functions
- JSDoc documentation with usage examples for each function
- Platform-agnostic implementation (no DOM-specific or RN-specific APIs)
- Integration with Jest fake timers
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-cross-platform-shared-utilities/144-RESEARCH.md
@.planning/phases/144-cross-platform-shared-utilities/144-01-SUMMARY.md
@mobile/src/__tests__/helpers/testUtils.ts
@frontend-nextjs/shared/test-utils/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create async-utils.ts module with platform-agnostic utilities</name>
  <files>frontend-nextjs/shared/test-utils/async-utils.ts</files>
  <action>
Create `frontend-nextjs/shared/test-utils/async-utils.ts` with platform-agnostic async utilities:

1. Add module documentation explaining cross-platform compatibility

2. Extract and adapt utilities from mobile testUtils.ts (lines 203-315):

   **waitForAsync** (lines 207-221):
   - Wrap waitFor with default 3000ms timeout
   - Use runtime import to avoid platform-specific dependencies
   - Document compatibility with both testing libraries

   **flushPromises** (lines 227-237):
   - Flush pending promises with setImmediate + jest.runAllTimers()
   - Works with Jest fake timers across all platforms
   - Return Promise<void> for await consistency

   **waitForCondition** (lines 244-260):
   - Poll condition function with timeout
   - Throw Error if condition not met within timeout
   - Use flushPromises internally for fake timer support

   **wait** (lines 279-286):
   - Simple setTimeout wrapper
   - Accept milliseconds parameter
   - Return Promise<void>

   **advanceTimersByTime** (lines 293-301):
   - Async version: jest.advanceTimersByTime() + flushPromises()
   - Use for tests that need promise resolution after timer advance

   **advanceTimersByTimeSync** (lines 308-314):
   - Synchronous version: only jest.advanceTimersByTime()
   - Use when no promise resolution needed
   - Document use case (heartbeat tests with 30s advance)

3. Add JSDoc comments with examples for each function

4. Ensure all functions export with `export const` for tree-shaking

5. Add platform-agnostic notice in module header

Do NOT include:
- Platform-specific APIs (Platform.OS, window.document)
- DOM-specific methods (querySelector, etc.)
- React Native-specific methods (Alert, etc.)
  </action>
  <verify>
```bash
cat frontend-nextjs/shared/test-utils/async-utils.ts
grep -c "export const" frontend-nextjs/shared/test-utils/async-utils.ts
wc -l frontend-nextjs/shared/test-utils/async-utils.ts
```
  </verify>
  <done>
async-utils.ts exists with 6 exported functions, 150+ lines, JSDoc documentation
  </done>
</task>

<task type="auto">
  <name>Task 2: Update index.ts barrel export to include async-utils</name>
  <files>frontend-nextjs/shared/test-utils/index.ts</files>
  <action>
Update `frontend-nextjs/shared/test-utils/index.ts` to properly export async-utils:

1. Add specific exports from async-utils for documentation purposes:
   ```typescript
   export {
     waitForAsync,
     flushPromises,
     waitForCondition,
     wait,
     advanceTimersByTime,
     advanceTimersByTimeSync,
   } from './async-utils';
   ```

2. Keep the wildcard export `export * from './async-utils';` for all exports

3. Add documentation comment grouping async utilities together

4. Ensure the module compiles (async-utils.ts must exist first)
  </action>
  <verify>
```bash
grep "async-utils" frontend-nextjs/shared/test-utils/index.ts
grep "waitForAsync" frontend-nextjs/shared/test-utils/index.ts
```
  </verify>
  <done>
index.ts exports async-utils functions both via wildcard and named exports
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Verify TypeScript compiles: `cd frontend-nextjs && npx tsc --noEmit`
2. Check all exported functions exist: `grep "export const" frontend-nextjs/shared/test-utils/async-utils.ts`
3. Verify JSDoc comments exist for each function
4. Ensure no platform-specific APIs are used (grep for Platform.OS, window.document, etc.)
</verification>

<success_criteria>
1. async-utils.ts exists with 6 async utility functions
2. All functions have JSDoc documentation with examples
3. Module uses platform-agnostic APIs only
4. index.ts properly exports all async utilities
5. File is 150+ lines with comprehensive documentation
6. No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/144-cross-platform-shared-utilities/144-02-SUMMARY.md`
</output>
