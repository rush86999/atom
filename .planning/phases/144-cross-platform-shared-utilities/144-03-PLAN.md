---
phase: 144-cross-platform-shared-utilities
plan: 03
type: execute
wave: 2
depends_on: [144-01]
files_modified:
  - frontend-nextjs/shared/test-utils/mock-factories.ts
  - frontend-nextjs/shared/test-utils/assertions.ts
  - frontend-nextjs/shared/test-utils/index.ts
  - .planning/phases/144-cross-platform-shared-utilities/144-03-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "Mock factory functions created (createMockWebSocket, createMockFn, createMockAsyncFn)"
    - "Assertion helpers created (assertThrows, assertRejects, assertRendersWithoutThrow)"
    - "Mock factories return jest.MockedFunction for TypeScript compatibility"
    - "All functions have JSDoc documentation with examples"
    - "Factories use MockWebSocket type from types.ts"
  artifacts:
    - path: "frontend-nextjs/shared/test-utils/mock-factories.ts"
      provides: "Mock object factories for testing"
      min_lines: 100
      contains: "createMockWebSocket"
      contains: "createMockFn"
      contains: "createMockAsyncFn"
    - path: "frontend-nextjs/shared/test-utils/assertions.ts"
      provides: "Custom assertion helpers"
      min_lines: 80
      contains: "assertThrows"
      contains: "assertRejects"
      contains: "assertRendersWithoutThrow"
  key_links:
    - from: "frontend-nextjs/shared/test-utils/mock-factories.ts"
      to: "mobile/src/__tests__/helpers/testUtils.ts"
      via: "pattern extraction from existing mobile utilities (lines 468-523)"
      pattern: "createMockWebSocket|createMockFn|createMockAsyncFn"
    - from: "frontend-nextjs/shared/test-utils/assertions.ts"
      to: "mobile/src/__tests__/helpers/testUtils.ts"
      via: "pattern extraction from existing mobile utilities (lines 526-572)"
      pattern: "assertThrows|assertRejects|assertRendersWithoutThrow"
---

<objective>
**Implement mock factories and assertion helpers** for cross-platform testing, extracting patterns from existing mobile testUtils.ts while maintaining TypeScript type safety and Jest compatibility.

**Purpose:** Mock factories (createMockWebSocket, createMockFn, createMockAsyncFn) and assertion helpers (assertThrows, assertRejects, assertRendersWithoutThrow) are duplicated across platforms. This plan creates shared, type-safe versions that work across web, mobile, and desktop.

**Output:**
- mock-factories.ts module with 3 mock factory functions
- assertions.ts module with 3 assertion helpers
- JSDoc documentation with usage examples
- TypeScript return types using jest.MockedFunction
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-cross-platform-shared-utilities/144-RESEARCH.md
@.planning/phases/144-cross-platform-shared-utilities/144-01-SUMMARY.md
@mobile/src/__tests__/helpers/testUtils.ts
@frontend-nextjs/shared/test-utils/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mock-factories.ts module with factory functions</name>
  <files>frontend-nextjs/shared/test-utils/mock-factories.ts</files>
  <action>
Create `frontend-nextjs/shared/test-utils/mock-factories.ts` with mock factory functions:

1. Add module documentation explaining mock factory patterns

2. Extract and adapt from mobile testUtils.ts (lines 468-523):

   **createMockWebSocket** (lines 473-491):
   - Accept `connected` boolean parameter (default: true)
   - Return MockWebSocket interface (from types.ts)
   - Include all event handler properties (onopen, onmessage, onerror, onclose)
   - Create jest.fn() for all methods (send, close, addEventListener, removeEventListener)
   - Set default url to 'ws://localhost:8000'
   - Add JSDoc with usage example

   **createMockFn** (lines 498-506):
   - Generic function accepting implementation: `<T extends (...args: unknown[]) => unknown>`
   - Return type: `jest.MockedFunction<T>`
   - Create `jest.fn(implementation)` and cast to MockedFunction
   - Document usage for synchronous functions

   **createMockAsyncFn** (lines 509-523):
   - Generic function accepting async implementation: `<T extends (...args: unknown[]) => Promise<unknown>>`
   - Return type: `jest.MockedFunction<T>`
   - Create `jest.fn(implementation)` and cast to MockedFunction
   - Document usage for async functions with await example

3. Import MockWebSocket type from ./types:
   ```typescript
   import type { MockWebSocket } from './types';
   ```

4. Add JSDoc comments with examples for each function

5. Ensure all functions export with `export const`

Do NOT include:
- Platform-specific WebSocket implementations
- Real network calls
- Non-Jest mocking libraries
  </action>
  <verify>
```bash
cat frontend-nextjs/shared/test-utils/mock-factories.ts
grep -c "export const" frontend-nextjs/shared/test-utils/mock-factories.ts
grep "MockWebSocket" frontend-nextjs/shared/test-utils/mock-factories.ts
```
  </verify>
  <done>
mock-factories.ts exists with 3 exported factory functions, imports MockWebSocket type
  </done>
</task>

<task type="auto">
  <name>Task 2: Create assertions.ts module with custom assertion helpers</name>
  <files>frontend-nextjs/shared/test-utils/assertions.ts</files>
  <action>
Create `frontend-nextjs/shared/test-utils/assertions.ts` with custom assertion helpers:

1. Add module documentation explaining assertion helper patterns

2. Extract and adapt from mobile testUtils.ts (lines 529-572):

   **assertThrows** (lines 534-544):
   - Accept `fn: () => void` and optional `errorMessage?: string`
   - Return type: `void`
   - Use `expect(fn).toThrow(errorMessage)` for assertion
   - Document sync error testing with example

   **assertRejects** (lines 550-561):
   - Accept `fn: () => Promise<unknown>` and optional `errorMessage?: string`
   - Return type: `Promise<void>`
   - Use `await expect(fn).rejects.toThrow(errorMessage)` for assertion
   - Document async error testing with await example

   **assertRendersWithoutThrow** (lines 564-572):
   - Accept `component: React.ReactElement`
   - Return type: `void`
   - Use `expect(() => render(component)).not.toThrow()` pattern
   - Import React and render from testing libraries
   - Note: This will need platform-specific render import (see below)

3. For assertRendersWithoutThrow, use conditional imports or documentation:
   - Add comment that render must be imported from platform-specific library
   - Or use generic wrapper approach documented in RESEARCH.md

4. Add JSDoc comments with examples for each function

5. Ensure all functions export with `export const`

Note: assertRendersWithoutThrow may need a platform-specific wrapper since render comes from different libraries
  </action>
  <verify>
```bash
cat frontend-nextjs/shared/test-utils/assertions.ts
grep -c "export const" frontend-nextjs/shared/test-utils/assertions.ts
grep "assertThrows\|assertRejects\|assertRendersWithoutThrow" frontend-nextjs/shared/test-utils/assertions.ts
```
  </verify>
  <done>
assertions.ts exists with 3 exported assertion functions and JSDoc documentation
  </done>
</task>

<task type="auto">
  <name>Task 3: Update index.ts barrel export to include mock-factories and assertions</name>
  <files>frontend-nextjs/shared/test-utils/index.ts</files>
  <action>
Update `frontend-nextjs/shared/test-utils/index.ts` to properly export new modules:

1. Add specific exports from mock-factories:
   ```typescript
   export {
     createMockWebSocket,
     createMockFn,
     createMockAsyncFn,
   } from './mock-factories';
   ```

2. Add specific exports from assertions:
   ```typescript
   export {
     assertThrows,
     assertRejects,
     assertRendersWithoutThrow,
   } from './assertions';
   ```

3. Keep the wildcard exports for both modules

4. Add documentation comments grouping mock factories and assertions together

5. Ensure module compiles with no TypeScript errors
  </action>
  <verify>
```bash
grep "mock-factories\|assertions" frontend-nextjs/shared/test-utils/index.ts
grep "createMockWebSocket\|assertThrows" frontend-nextjs/shared/test-utils/index.ts
```
  </verify>
  <done>
index.ts exports mock-factories and assertions functions via wildcard and named exports
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Verify TypeScript compiles: `cd frontend-nextjs && npx tsc --noEmit`
2. Check all exported functions exist: `grep "export const" frontend-nextjs/shared/test-utils/mock-factories.ts assertions.ts`
3. Verify type imports work: `grep "import.*from.*types" frontend-nextjs/shared/test-utils/mock-factories.ts`
4. Ensure JSDoc comments exist for each function
</verification>

<success_criteria>
1. mock-factories.ts exists with 3 factory functions
2. assertions.ts exists with 3 assertion helpers
3. All functions have JSDoc documentation
4. MockWebSocket type imported from types.ts
5. jest.MockedFunction used for return types
6. index.ts properly exports all functions
7. No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/144-cross-platform-shared-utilities/144-03-SUMMARY.md`
</output>
