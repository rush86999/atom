---
phase: 144-cross-platform-shared-utilities
plan: 04
type: execute
wave: 3
depends_on: [144-01]
files_modified:
  - frontend-nextjs/shared/test-utils/platform-guards.ts
  - frontend-nextjs/shared/test-utils/cleanup.ts
  - frontend-nextjs/shared/test-utils/index.ts
  - .planning/phases/144-cross-platform-shared-utilities/144-04-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "Platform guard functions created (isWeb, isReactNative, isTauri, isIOS, isAndroid)"
    - "Cleanup utilities created (resetAllMocks, setupFakeTimers, cleanupTest)"
    - "Platform guards use runtime detection (typeof checks)"
    - "Cleanup functions reset Expo module mocks when available"
    - "All functions have JSDoc documentation with examples"
  artifacts:
    - path: "frontend-nextjs/shared/test-utils/platform-guards.ts"
      provides: "Platform detection utilities for conditional testing"
      min_lines: 120
      contains: "isWeb"
      contains: "isReactNative"
      contains: "isTauri"
      contains: "isIOS"
      contains: "isAndroid"
      contains: "skipIfNotWeb"
    - path: "frontend-nextjs/shared/test-utils/cleanup.ts"
      provides: "Test cleanup and mock reset utilities"
      min_lines: 100
      contains: "resetAllMocks"
      contains: "setupFakeTimers"
      contains: "cleanupTest"
  key_links:
    - from: "frontend-nextjs/shared/test-utils/platform-guards.ts"
      to: "mobile/src/__tests__/helpers/testUtils.ts"
      via: "pattern extraction from existing mobile utilities (lines 86-99, 394-461)"
      pattern: "isIOS|isAndroid|testEachPlatform|skipOnPlatform"
    - from: "frontend-nextjs/shared/test-utils/cleanup.ts"
      to: "mobile/src/__tests__/helpers/testUtils.ts"
      via: "pattern extraction from existing mobile utilities (lines 324-388)"
      pattern: "resetAllMocks|setupFakeTimers|cleanupTest"
---

<objective>
**Implement platform guards and cleanup utilities** for cross-platform testing, providing runtime platform detection and comprehensive test cleanup functions that work across web, mobile, and desktop environments.

**Purpose:** Platform guards (isWeb, isReactNative, isTauri) enable conditional test logic, while cleanup utilities (resetAllMocks, setupFakeTimers, cleanupTest) ensure proper test isolation. These patterns are duplicated across platforms and need a shared implementation.

**Output:**
- platform-guards.ts module with 6+ platform detection functions
- cleanup.ts module with 3+ cleanup/reset functions
- JSDoc documentation with usage examples
- Runtime platform detection using typeof checks
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-cross-platform-shared-utilities/144-RESEARCH.md
@.planning/phases/144-cross-platform-shared-utilities/144-01-SUMMARY.md
@mobile/src/__tests__/helpers/testUtils.ts
@frontend-nextjs/shared/test-utils/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create platform-guards.ts module with runtime platform detection</name>
  <files>frontend-nextjs/shared/test-utils/platform-guards.ts</files>
  <action>
Create `frontend-nextjs/shared/test-utils/platform-guards.ts` with runtime platform detection:

1. Add module documentation explaining platform guard patterns

2. Extract and adapt from mobile testUtils.ts and RESEARCH.md examples:

   **isWeb** (RESEARCH.md lines 166-170):
   - Check `typeof window !== 'undefined' && typeof window.document !== 'undefined'`
   - Return boolean
   - Document web environment detection

   **isReactNative** (RESEARCH.md lines 175-178):
   - Check `typeof navigator !== 'undefined' && (navigator as any).product === 'ReactNative'`
   - Return boolean
   - Document RN environment detection

   **isTauri** (RESEARCH.md lines 183-186):
   - Check `typeof window !== 'undefined' && (window as any).__TAURI__ !== undefined`
   - Return boolean
   - Document Tauri desktop detection

   **isIOS** (testUtils.ts lines 89-91, adapted):
   - Check Platform.OS === 'ios' if available, otherwise false
   - Add typeof check for Platform to avoid errors in web/desktop
   - Return boolean

   **isAndroid** (testUtils.ts lines 97-99, adapted):
   - Check Platform.OS === 'android' if available, otherwise false
   - Add typeof check for Platform to avoid errors in web/desktop
   - Return boolean

   **skipIfNotWeb** (RESEARCH.md lines 195-197):
   - Return test.skip if not isWeb(), otherwise return test
   - Enable conditional test skipping based on platform
   - Document usage pattern

3. Add testEachPlatform helper (testUtils.ts lines 405-418):
   - Accept callback function with platform parameter
   - Loop through ['ios', 'android'] platforms
   - Call callback with cleanup after each iteration
   - Document cross-platform testing pattern

4. Add JSDoc comments with examples for each function

5. Ensure all functions export with `export const`

Note: Use defensive typeof checks for Platform since it's RN-only
  </action>
  <verify>
```bash
cat frontend-nextjs/shared/test-utils/platform-guards.ts
grep -c "export const" frontend-nextjs/shared/test-utils/platform-guards.ts
grep "isWeb\|isReactNative\|isTauri" frontend-nextjs/shared/test-utils/platform-guards.ts
```
  </verify>
  <done>
platform-guards.ts exists with 6+ exported functions, runtime platform detection using typeof
  </done>
</task>

<task type="auto">
  <name>Task 2: Create cleanup.ts module with test isolation utilities</name>
  <files>frontend-nextjs/shared/test-utils/cleanup.ts</files>
  <action>
Create `frontend-nextjs/shared/test-utils/cleanup.ts` with test cleanup utilities:

1. Add module documentation explaining cleanup patterns

2. Extract and adapt from mobile testUtils.ts (lines 324-388):

   **resetAllMocks** (lines 329-344):
   - Call jest.clearAllMocks()
   - Call jest.clearAllTimers()
   - Call jest.useRealTimers()
   - Check for and call global.__resetMmkvMock() if defined
   - Check for and call global.__resetAsyncStorageMock() if defined
   - Check for and call global.__resetSecureStoreMock() if defined
   - Document use in beforeEach for consistent test isolation

   **setupFakeTimers** (lines 355-359):
   - Call jest.useFakeTimers({ doNotFake: ['requestAnimationFrame', 'performance'] })
   - Document use in beforeEach for setTimeout/setInterval tests
   - Explain doNotFake option for performance-critical tests

   **cleanupTest** (lines 370-374):
   - Restore platform state (call restorePlatform if available)
   - Restore device state (call restoreDevice if available)
   - Call jest.clearAllMocks()
   - Document use in afterEach blocks

   **cleanupTestWithReset** (lines 385-388):
   - Call cleanupTest() first
   - Call jest.resetModules()
   - Document use for tests sharing modules

3. For restorePlatform/restoreDevice, add defensive checks:
   - Check if functions exist before calling (they're from platform-guards)
   - Or document that they need to be imported separately

4. Add JSDoc comments with beforeEach/afterEach examples

5. Ensure all functions export with `export const`

Note: Expo module mocks are mobile-specific but safe to check globally
  </action>
  <verify>
```bash
cat frontend-nextjs/shared/test-utils/cleanup.ts
grep -c "export const" frontend-nextjs/shared/test-utils/cleanup.ts
grep "resetAllMocks\|setupFakeTimers\|cleanupTest" frontend-nextjs/shared/test-utils/cleanup.ts
```
  </verify>
  <done>
cleanup.ts exists with 4+ exported cleanup functions, Expo mock checks with defensive typeof
  </done>
</task>

<task type="auto">
  <name>Task 3: Update index.ts barrel export to include platform-guards and cleanup</name>
  <files>frontend-nextjs/shared/test-utils/index.ts</files>
  <action>
Update `frontend-nextjs/shared/test-utils/index.ts` to properly export new modules:

1. Add specific exports from platform-guards:
   ```typescript
   export {
     isWeb,
     isReactNative,
     isTauri,
     isIOS,
     isAndroid,
     skipIfNotWeb,
     testEachPlatform,
   } from './platform-guards';
   ```

2. Add specific exports from cleanup:
   ```typescript
   export {
     resetAllMocks,
     setupFakeTimers,
     cleanupTest,
     cleanupTestWithReset,
   } from './cleanup';
   ```

3. Keep the wildcard exports for both modules

4. Add documentation comments grouping platform guards and cleanup together

5. Ensure module compiles with no TypeScript errors
  </action>
  <verify>
```bash
grep "platform-guards\|cleanup" frontend-nextjs/shared/test-utils/index.ts
grep "isWeb\|resetAllMocks" frontend-nextjs/shared/test-utils/index.ts
```
  </verify>
  <done>
index.ts exports platform-guards and cleanup functions via wildcard and named exports
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Verify TypeScript compiles: `cd frontend-nextjs && npx tsc --noEmit`
2. Check all exported functions exist: `grep "export const" frontend-nextjs/shared/test-utils/platform-guards.ts cleanup.ts`
3. Verify defensive checks for platform-specific APIs
4. Ensure JSDoc comments exist for each function
</verification>

<success_criteria>
1. platform-guards.ts exists with 6+ guard functions
2. cleanup.ts exists with 4+ cleanup functions
3. All functions have JSDoc documentation
4. Runtime detection uses typeof checks
5. Expo mock cleanup uses defensive global checks
6. index.ts properly exports all functions
7. No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/144-cross-platform-shared-utilities/144-04-SUMMARY.md`
</output>
