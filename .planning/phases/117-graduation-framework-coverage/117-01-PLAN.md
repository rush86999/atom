---
phase: 117-graduation-framework-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_agent_graduation.py
autonomous: true

must_haves:
  truths:
    - "Flaky test_promote_agent_success now passes consistently"
    - "Tests correctly use agent.configuration instead of agent.metadata_json"
    - "Agent promotion workflow tested end-to-end with real DB session"
    - "Coverage measurement works without test failures blocking"
  artifacts:
    - path: "backend/tests/test_agent_graduation.py"
      provides: "Passing tests for AgentGraduationService"
      min_lines: 380
  key_links:
    - from: "backend/tests/test_agent_graduation.py"
      to: "backend/core/agent_graduation_service.py"
      via: "Promotion test matches promote_agent implementation"
      pattern: "agent.configuration\\[\"promoted_by\"\\]"
---

<objective>
Fix 1 flaky test in test_agent_graduation.py and verify coverage baseline measurement.

**Purpose:** Research shows test_promote_agent_success fails intermittently due to checking wrong field (metadata_json instead of configuration). The service uses agent.configuration for promotion metadata. Fixing this test enables accurate coverage measurement.

**Current Coverage:** 46% (130/240 lines missing)
**Target:** 60%+ after Plan 03
**Output:** 10/10 tests passing, ready for coverage gap analysis in Plan 02
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Proven patterns from Phase 116
@.planning/phases/116-student-training-coverage/116-01-PLAN.md

# Proven patterns from Phase 112
@.planning/phases/112-agent-governance-coverage/112-01-SUMMARY.md

# Source files
@backend/core/agent_graduation_service.py (lines 449-453 use agent.configuration)
@backend/tests/test_agent_graduation.py (line 251 checks wrong field)
</context>

<tasks>

<task type="auto">
  <name>Verify flaky test root cause and fix field mismatch</name>
  <files>backend/tests/test_agent_graduation.py</files>
  <action>
    The test_promote_agent_success test fails because it checks agent.metadata_json.get("promoted_by") but the service uses agent.configuration["promoted_by"].

    Review the failing test (around line 225-251) and fix:
    1. Line 251: Change `agent.metadata_json.get("promoted_by")` to `agent.configuration.get("promoted_by")`
    2. Verify the mock agent initialization in the test uses `configuration` dict
    3. The test at line 250 currently checks `metadata_json is not None` - update to check `configuration`

    Looking at agent_graduation_service.py lines 449-453:
    ```python
    if not agent.configuration:
        agent.configuration = {}
    agent.configuration["promoted_at"] = datetime.now().isoformat()
    agent.configuration["promoted_by"] = validated_by
    ```

    The test must match this implementation.

    DO NOT modify:
    - Test class name (TestAgentPromotion)
    - Test method name (test_promote_agent_success)
    - The core test logic (create agent, call promote_agent, verify success)
  </action>
  <verify>
    cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/test_agent_graduation.py::TestAgentPromotion::test_promote_agent_success -v
  </verify>
  <done>
    test_promote_agent_success passes consistently
    No AssertionError about None == 'admin_user'
  </done>
</task>

<task type="auto">
  <name>Verify all 10 tests pass and measure baseline coverage</name>
  <files>backend/tests/test_agent_graduation.py</files>
  <action>
    Run the full test suite for agent_graduation_service.py to verify all tests pass and establish baseline coverage.

    Command:
    ```bash
    cd backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
      tests/test_agent_graduation.py \
      -v \
      --cov=core.agent_graduation_service \
      --cov-report=term-missing \
      --cov-report=json:tests/coverage_reports/metrics/phase_117_coverage_baseline.json
    ```

    This confirms:
    1. All 10 tests pass (currently 1 flaky, 9 passing)
    2. Baseline coverage is measured (currently ~46%)
    3. Coverage gaps are documented for Plan 02 analysis

    Expected baseline:
    - Current: 46% coverage (130/240 lines missing)
    - Missing line ranges: 35, 62-136, 206, 235, 289, 326-327, 381, 399-406, 485, 554-608, 628-671, 702-760, 790-809, 837-871, 904-922, 954-969
  </action>
  <verify>
    File tests/coverage_reports/metrics/phase_117_coverage_baseline.json exists
    All 10 tests show PASSED in output
    Coverage report shows accurate percentage
  </verify>
  <done>
    All 10 tests pass consistently
    Baseline coverage documented
    Ready for gap analysis in Plan 02
  </done>
</task>

</tasks>

<verification>
1. test_promote_agent_success passes consistently (no more flaky failures)
2. All 10 tests in test_agent_graduation.py pass
3. Coverage baseline JSON generated at phase_117_coverage_baseline.json
4. Test assertions use agent.configuration instead of agent.metadata_json
</verification>

<success_criteria>
1. 10/10 tests passing in test_agent_graduation.py
2. Coverage measurement works without test failures blocking
3. Baseline coverage documented (currently 46%, 130 missing lines)
4. Field mismatch fixed (configuration vs metadata_json)
</success_criteria>

<output>
After completion, create `.planning/phases/117-graduation-framework-coverage/117-01-SUMMARY.md`
</output>
