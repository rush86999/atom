---
phase: 156-core-services-coverage-high-impact
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/services/test_canvas_coverage.py
  - backend/tests/integration/services/test_websocket_coverage.py
  - backend/tools/canvas_tool.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "WebSocket broadcast mocking uses AsyncMock instead of Mock"
    - "Agent maturity detection uses correct field (status vs confidence_score)"
    - "Canvas form validation tests pass"
    - "Canvas state management tests pass"
    - "Canvas/WebSocket coverage increased to 60%+"
  artifacts:
    - path: "backend/tests/integration/services/test_canvas_coverage.py"
      provides: "Fixed canvas tests with AsyncMock and correct agent maturity"
      min_tests: 17
    - path: "backend/tests/integration/services/test_websocket_coverage.py"
      provides: "Fixed WebSocket tests with AsyncMock"
      min_tests: 14
  key_links:
    - from: "backend/tests/integration/services/test_canvas_coverage.py"
      to: "backend/tools/canvas_tool.py"
      via: "canvas operations with correct agent maturity detection"
      pattern: "present_chart|present_form|update_canvas"
    - from: "backend/tests/integration/services/test_websocket_coverage.py"
      to: "core/websockets.py"
      via: "WebSocket broadcast with AsyncMock"
      pattern: "broadcast"
---

# Phase 156: Canvas/WebSocket Gap Closure - Plan 10

<objective>
Fix WebSocket broadcast mocking (AsyncMock vs Mock), agent maturity detection bugs, and form validation issues to increase canvas coverage from 29% to 60%+

Purpose: Plan 156-04 created canvas/WebSocket tests but 71% of WebSocket tests and 53% of canvas tests are failing due to async mocking issues and agent resolution bugs.

Output: Working canvas/WebSocket tests with 60%+ coverage (up from 29%)
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/156-core-services-coverage-high-impact/156-VERIFICATION.md
@.planning/phases/156-core-services-coverage-high-impact/156-04-PLAN.md
@.planning/phases/156-core-services-coverage-high-impact/156-04-SUMMARY.md
@backend/tests/integration/services/test_canvas_coverage.py
@backend/tests/integration/services/test_websocket_coverage.py
@backend/tools/canvas_tool.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix WebSocket broadcast mocking to use AsyncMock</name>
  <files>backend/tests/integration/services/test_canvas_coverage.py</files>
  <action>
    Fix WebSocket broadcast mocking in both test files:

    1. **In test_canvas_coverage.py**:
       - mock_ws_manager fixture already uses AsyncMock for broadcast (line 105)
       - But tests may need to await broadcast calls
       - Add await for all manager.broadcast() calls
       - Update mock assertions to use assert_awaited if needed

    2. **In test_websocket_coverage.py**:
       - mock_ws_manager fixture uses AsyncMock (line 89)
       - Ensure all tests properly await async broadcast calls
       - Update TestWebSocketBroadcast.test_broadcast_to_canvas (1/5 passing)
       - Update TestWebSocketErrorHandling tests (0/3 passing)
       - Update TestWebSocketDataIntegrity tests (0/3 passing)

    Key pattern from plan 156-07 summary:
    - "Tests use Mock for async broadcast (should use AsyncMock)"
    - Change Mock() to AsyncMock() for async methods
    - Use await when calling async methods in tests

    Focus on these test classes:
    - TestCanvasStateManagement (1/3 passing - WebSocket issues)
    - TestGovernanceIntegration (3/5 passing - WebSocket issues)
    - TestWebSocketBroadcast (1/5 passing)
    - TestWebSocketErrorHandling (0/3 passing)
    - TestWebSocketDataIntegrity (0/3 passing)
  </action>
  <verify>
    pytest backend/tests/integration/services/test_canvas_coverage.py::TestCanvasStateManagement -v
    pytest backend/tests/integration/services/test_websocket_coverage.py -v
  </verify>
  <done>
    WebSocket tests 10/14 passing (71%, up from 29%), AsyncMock used correctly, broadcasts awaited
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix agent maturity detection in canvas tests</name>
  <files>backend/tests/integration/services/test_canvas_coverage.py</files>
  <action>
    Fix agent maturity detection issue identified in VERIFICATION.md:

    Problem: "Tests expect confidence_score to determine maturity, actual uses status field"

    Solution:
    1. When creating mock agents for tests, set BOTH status AND confidence_score:
       - STUDENT: status="student", confidence_score=0.3
       - INTERN: status="intern", confidence_score=0.6
       - SUPERVISED: status="supervised", confidence_score=0.8
       - AUTONOMOUS: status="autonomous", confidence_score=0.95

    2. Update test fixture mock_agent creation:
       - In mock_db_context fixture, add confidence_score when creating agents
       - Ensure status field matches expected maturity level

    3. Update TestFormPresentation tests (0/5 passing):
       - Create agents with correct status/confidence mapping
       - Fix agent resolution in present_form tests

    4. Update TestGovernanceIntegration tests (3/5 passing):
       - Ensure maturity checks use both status and confidence_score

    Pattern from plan 156-07 summary:
    "Added confidence_scores mapping: STUDENT: 0.3, INTERN: 0.6, SUPERVISED: 0.8, AUTONOMOUS: 0.95"
  </action>
  <verify>
    pytest backend/tests/integration/services/test_canvas_coverage.py::TestFormPresentation -v
    pytest backend/tests/integration/services/test_canvas_coverage.py::TestGovernanceIntegration -v
  </verify>
  <done>
    TestFormPresentation 4/5 passing (80%, up from 0%), TestGovernanceIntegration 5/5 passing (100%, up from 60%)
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix canvas form validation and state management tests</name>
  <files>backend/tests/integration/services/test_canvas_coverage.py</files>
  <action>
    Fix remaining canvas test failures:

    1. **Form validation NoneType errors (TestFormPresentation)**:
       - Tests failing with NoneType errors on form submission
       - Add proper form data setup with required fields
       - Mock form validation to return valid data
       - Ensure canvas_tool.py receives valid form structure

    2. **Canvas state management (TestCanvasStateManagement)**:
       - Issue: "Tests expect in-memory state, actual uses database"
       - Update tests to work with database-backed state
       - Use db_session fixture instead of in-memory mocks
       - Test state updates via database queries

    Specific test methods to fix:
    - test_present_form_with_validation (NoneType error)
    - test_present_form_with_governance_block (agent maturity)
    - test_update_canvas_state (database-backed state)
    - test_close_canvas_state (database-backed state)
    - test_canvas_state_persistence (database-backed state)

    Pattern from VERIFICATION.md:
    "Canvas state management tests (database-backed vs in-memory)"
    "Form validation tests (5 tests failing with NoneType errors)"
  </action>
  <verify>
    pytest backend/tests/integration/services/test_canvas_coverage.py -v
  </verify>
  <done>
    Canvas tests 14/17 passing (82%, up from 47%), no NoneType errors, state tests use database correctly
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run canvas test suite:
   ```bash
   pytest backend/tests/integration/services/test_canvas_coverage.py -v
   ```
   Expected: 14/17 tests passing (82%, up from 47%)

2. Run WebSocket test suite:
   ```bash
   pytest backend/tests/integration/services/test_websocket_coverage.py -v
   ```
   Expected: 10/14 tests passing (71%, up from 29%)

3. Run combined coverage report:
   ```bash
   cd backend
   pytest tests/integration/services/test_canvas_coverage.py tests/integration/services/test_websocket_coverage.py --cov=tools.canvas_tool --cov-report=term-missing
   ```
   Expected: 60%+ coverage for canvas_tool.py (up from 29%)
</verification>

<success_criteria>
- WebSocket broadcast uses AsyncMock correctly
- All async calls are properly awaited
- Agent maturity uses both status and confidence_score fields
- Canvas form validation tests pass (4/5 or better)
- Canvas state management tests pass (3/3)
- Overall canvas test pass rate 82% (14/17)
- Overall WebSocket test pass rate 71% (10/14)
- Combined canvas coverage 60%+ (up from 29%)
</success_criteria>

<output>
After completion, create `.planning/phases/156-core-services-coverage-high-impact/156-10-SUMMARY.md`
</output>
