---
phase: 156-core-services-coverage-high-impact
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/services/test_http_client_coverage.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "HTTP client initialization tested (singleton pattern, async/sync instances)"
    - "Connection pooling tested (connection reuse across requests)"
    - "Timeout handling tested (async and sync timeout exceptions)"
    - "Error handling tested (network errors, status errors, retry logic)"
    - "Client cleanup tested (aclose, close methods)"
    - "Coverage verified: http_client.py at 80%+"
  artifacts:
    - path: "backend/tests/integration/services/test_http_client_coverage.py"
      provides: "HTTP client coverage"
      min_lines: 250
      exports: ["TestClientInitialization", "TestConnectionPooling", "TestTimeoutHandling", "TestErrorHandling", "TestClientCleanup"]
  key_links:
    - from: "test_http_client_coverage.py"
      to: "core/http_client.py"
      via: "test client operations"
      pattern: "get_async_client|get_sync_client"
    - from: "test_http_client_coverage.py"
      to: "httpx"
      via: "HTTP client mocking"
      pattern: "patch.*httpx|AsyncMock.*get"
---

<objective>
Expand test coverage for HTTP Client to 80% through comprehensive testing of initialization, connection pooling, timeout handling, error handling, and cleanup.

Purpose: HTTP Client is the foundation for all external API communication. High coverage ensures reliable HTTP operations.

Output: 250+ lines of test code covering all HTTP client code paths with httpx mocking (no real HTTP calls).
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-core-services-coverage-high-impact/156-RESEARCH.md
@backend/core/http_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test HTTP client initialization and connection pooling</name>
  <files>
    backend/tests/integration/services/test_http_client_coverage.py
  </files>
  <action>
    Create `backend/tests/integration/services/test_http_client_coverage.py`:

    Class: `TestClientInitialization`

    1. `test_async_client_created_once`:
       - Call get_async_client() twice
       - Verify client1 is client2 (singleton)

    2. `test_sync_client_created_once`:
       - Call get_sync_client() twice
       - Verify client1 is client2 (singleton)

    3. `test_async_client_configuration`:
       - Verify client.timeout == DEFAULT_TIMEOUT
       - Verify client.limits == DEFAULT_LIMITS
       - Verify client.http2 == True

    4. `test_sync_client_configuration`:
       - Verify client.timeout == DEFAULT_TIMEOUT
       - Verify client.limits == DEFAULT_LIMITS
       - Verify client.http2 == True

    Class: `TestConnectionPooling`

    5. `test_async_connection_reuse` (async):
       - Mock client.get() with httpx.Response(200)
       - Make 2 requests to same URL
       - Verify both succeed

    6. `test_sync_connection_reuse`:
       - Mock client.get() with httpx.Response(200)
       - Make 2 requests
       - Verify both succeed

    Use httpx.Response mocking.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_http_client_coverage.py::TestClientInitialization -v`
    Run `pytest backend/tests/integration/services/test_http_client_coverage.py::TestConnectionPooling -v`
    Verify all tests pass.
    Check coverage: `pytest --cov=core.http_client --cov-report=term-missing`
  </verify>
  <done>
    HTTP client initialization tested (singleton, configuration) and connection pooling tested.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test HTTP client timeout handling</name>
  <files>
    backend/tests/integration/services/test_http_client_coverage.py
  </files>
  <action>
    Add to `test_http_client_coverage.py`:

    Class: `TestTimeoutHandling`

    1. `test_async_request_timeout` (async):
       - Mock client.get() to raise httpx.TimeoutException
       - Call client.get("http://example.com")
       - Verify httpx.TimeoutException raised

    2. `test_sync_request_timeout`:
       - Mock client.get() to raise httpx.TimeoutException
       - Call client.get("http://example.com")
       - Verify httpx.TimeoutException raised

    Mock timeout exceptions, don't use real delays.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_http_client_coverage.py::TestTimeoutHandling -v`
    Verify both timeout tests pass.
  </verify>
  <done>
    Timeout handling tested for async and sync clients.
  </done>
</task>

<task type="auto">
  <name>Task 3: Test HTTP client error handling and cleanup</name>
  <files>
    backend/tests/integration/services/test_http_client_coverage.py
  </files>
  <action>
    Add to `test_http_client_coverage.py`:

    Class: `TestErrorHandling`

    1. `test_async_network_error` (async):
       - Mock client.get() to raise httpx.NetworkError
       - Verify httpx.NetworkError raised

    2. `test_async_http_status_error` (async):
       - Mock client.get() to return httpx.Response(500)
       - Verify response.status_code == 500

    3. `test_retry_on_transient_failure` (async):
       - Mock 2 NetworkErrors, then success
       - Verify 3 attempts made
       - Verify eventual success

    Class: `TestClientCleanup`

    4. `test_close_async_client` (async):
       - Mock client.aclose()
       - Call close_http_clients()
       - Verify aclose() called once

    5. `test_close_sync_client`:
       - Mock client.close()
       - Call close_http_clients()
       - Verify close() called once

    Test error recovery and resource cleanup.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_http_client_coverage.py::TestErrorHandling -v`
    Run `pytest backend/tests/integration/services/test_http_client_coverage.py::TestClientCleanup -v`
    Verify all tests pass.
  </verify>
  <done>
    Error handling tested (network, status, retry) and cleanup tested (aclose, close).
  </done>
</task>

</tasks>

<verification>
Overall verification:
1. Run `pytest backend/tests/integration/services/test_http_client_coverage.py -v`
2. Verify 15+ tests passing (initialization, pooling, timeouts, errors, cleanup)
3. Check coverage: `pytest --cov=core.http_client --cov-report=html`
4. Open htmlcov/index.html and verify http_client.py at 80%+ coverage
5. Confirm tests exercise production code: get_async_client, get_sync_client, close_http_clients
</verification>

<success_criteria>
1. 15+ HTTP client tests passing (100% pass rate)
2. HTTP client at 80%+ coverage
3. Singleton pattern tested (async and sync)
4. Connection pooling tested
5. Timeout handling tested
6. Error handling tested (network, status, retry)
7. Cleanup tested (aclose, close)
8. Zero external dependencies (all mocked with httpx)
</success_criteria>

<output>
After completion, create `.planning/phases/156-core-services-coverage-high-impact/156-06-SUMMARY.md`
</output>
