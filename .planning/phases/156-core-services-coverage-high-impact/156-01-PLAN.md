---
phase: 156-core-services-coverage-high-impact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/services/test_governance_coverage.py
  - backend/tests/integration/services/conftest.py
  - backend/tests/integration/services/pytest.ini
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Agent governance coverage expanded to 80% (maturity routing, permission checking, lifecycle management, cache validation)"
    - "All maturity levels tested (STUDENT, INTERN, SUPERVISED, AUTONOMOUS) with action complexity matrix"
    - "Governance cache behavior validated (hit/miss, TTL expiration, invalidation)"
    - "Agent lifecycle operations tested (register, update, suspend, terminate)"
    - "Feedback adjudication workflow tested with AI adjudication mocking"
    - "HITL action lifecycle tested (create, approve, reject)"
  artifacts:
    - path: "backend/tests/integration/services/test_governance_coverage.py"
      provides: "80%+ coverage for AgentGovernanceService"
      min_lines: 500
      exports: ["TestAgentMaturityRouting", "TestAgentLifecycleManagement", "TestFeedbackAdjudication", "TestHITLActionManagement", "TestGovernanceCacheValidation"]
    - path: "backend/tests/integration/services/conftest.py"
      provides: "Shared service fixtures (db_session, governance_service, governance_cache)"
      min_lines: 50
    - path: "backend/tests/integration/services/pytest.ini"
      provides: "Isolated test configuration for service tests"
      min_lines: 10
  key_links:
    - from: "test_governance_coverage.py"
      to: "core/agent_governance_service.py"
      via: "import AgentGovernanceService"
      pattern: "from core.agent_governance_service import AgentGovernanceService"
    - from: "test_governance_coverage.py"
      to: "core/governance_cache.py"
      via: "cache fixture and invalidation testing"
      pattern: "governance_cache.invalidate_agent|governance_cache._hits|governance_cache._misses"
    - from: "test_governance_coverage.py"
      to: "core/models.py"
      via: "AgentRegistry, AgentFeedback, HITLAction models"
      pattern: "from core.models import AgentRegistry|AgentFeedback|HITLAction"
---

<objective>
Expand test coverage for Agent Governance Service to 80% through comprehensive testing of maturity routing, permission checking, lifecycle management, and cache validation.

Purpose: Agent Governance Service is a critical security component that controls which agents can perform which actions based on maturity levels. High coverage ensures agent attribution and governance are working correctly.

Output: 500+ lines of test code covering all governance code paths with mocked external dependencies (LLM providers, WebSocket).
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-core-services-coverage-high-impact/156-RESEARCH.md
@backend/core/agent_governance_service.py
@backend/core/governance_cache.py
@backend/core/models.py
@backend/tests/test_governance_services_integration.py
@backend/docs/AGENT_GOVERNANCE_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared service fixtures and isolated test configuration</name>
  <files>
    backend/tests/integration/services/conftest.py
    backend/tests/integration/services/pytest.ini
  </files>
  <action>
    Create shared fixtures for service testing:

    1. Create `backend/tests/integration/services/conftest.py`:
       - `db_session` fixture: Fresh database session with rollback
       - `governance_service` fixture: AgentGovernanceService instance
       - `governance_cache` fixture: GovernanceCache instance with short TTL for testing
       - `test_agent` fixture: Factory for creating test agents
       - `test_user` fixture: Factory for creating test users
       - Use minimal imports to avoid SQLAlchemy circular dependencies

    2. Create `backend/tests/integration/services/pytest.ini`:
       - Add `testpaths = tests/integration/services`
       - Add `pythonpath = backend`
       - Disable asyncio mode (not needed for sync service tests)
       - Add `--strict-markers` for test markers
       - Add `--tb=short` for shorter tracebacks
       - Isolate test discovery from other conftest.py files

    Use pytest yield pattern for cleanup (rollback transactions).
    Avoid importing from tests/e2e_ui/fixtures/ (causes SQLAlchemy issues).
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/conftest.py --collect-only` to verify fixtures are discoverable.
    Check that no SQLAlchemy import errors occur.
  </verify>
  <done>
    Shared fixtures created for all service tests, isolated test configuration prevents conftest pollution.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test agent maturity routing and permission matrix</name>
  <files>
    backend/tests/integration/services/test_governance_coverage.py
  </files>
  <action>
    Create `backend/tests/integration/services/test_governance_coverage.py` with maturity routing tests:

    Class: `TestAgentMaturityRouting`

    1. `test_maturity_action_matrix` (parametrize):
       - Test all maturity levels (STUDENT, INTERN, SUPERVISED, AUTONOMOUS)
       - Test all action complexities (1-4)
       - Expected results:
         - STUDENT: complexity 1 only
         - INTERN: complexity 1-2
         - SUPERVISED: complexity 1-3
         - AUTONOMOUS: complexity 1-4
       - Use `@pytest.mark.parametrize` with 16 test cases
       - Verify `can_perform_action()` returns correct allowed/disallowed

    2. `test_maturity_routing_with_cache`:
       - Create agent with INTERN status
       - Call `can_perform_action()` twice with cache
       - Verify cache hit on second call
       - Assert `governance_cache._hits >= 1`

    3. `test_confidence_score_routing`:
       - Test agents with confidence scores:
         - <0.5: Should route to STUDENT
         - 0.5-0.7: Should route to INTERN
         - 0.7-0.9: Should route to SUPERVISED
         - >0.9: Should route to AUTONOMOUS
       - Verify status assignment based on confidence

    Import from research pattern: Use parametrize for efficiency, test business logic not framework.
    Mock external dependencies (LLM, WebSocket) with unittest.mock.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_governance_coverage.py::TestAgentMaturityRouting -v`
    Verify all tests pass (16 parametrized + 2 additional = 18 tests).
    Check coverage report: `pytest --cov=core.agent_governance_service --cov-report=term-missing`
  </verify>
  <done>
    Agent maturity routing tested with full permission matrix, cache integration validated, confidence score routing verified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Test agent lifecycle management</name>
  <files>
    backend/tests/integration/services/test_governance_coverage.py
  </files>
  <action>
    Add lifecycle management tests to `test_governance_coverage.py`:

    Class: `TestAgentLifecycleManagement`

    1. `test_register_new_agent`:
       - Call `register_or_update_agent()` with new agent data
       - Verify agent.id is assigned
       - Verify agent.name matches input
       - Verify default status is STUDENT
       - Verify created_at timestamp

    2. `test_update_existing_agent`:
       - Create agent via register
       - Update agent with same module_path but different name/description
       - Verify agent.id unchanged (same agent)
       - Verify agent.name updated
       - Verify agent.description updated

    3. `test_suspend_agent`:
       - Create agent with AUTONOMOUS status
       - Call `suspend_agent(agent_id, reason)`
       - Verify agent.status == "SUSPENDED"
       - Verify suspension_reason logged

    4. `test_terminate_agent`:
       - Create agent with SUPERVISED status
       - Call `terminate_agent(agent_id, reason)`
       - Verify agent.status == "TERMINATED"
       - Verify terminated_at timestamp set

    5. `test_reactivate_suspended_agent`:
       - Create agent, suspend it
       - Call `reactivate_agent(agent_id)`
       - Verify agent.status restored to previous status

    Use db_session fixture for transaction rollback.
    Test all lifecycle state transitions.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_governance_coverage.py::TestAgentLifecycleManagement -v`
    Verify all 5 lifecycle tests pass.
    Check coverage covers register, update, suspend, terminate, reactivate methods.
  </verify>
  <done>
    Agent lifecycle management tested (creation, updates, suspension, termination, reactivation) with state transitions validated.
  </done>
</task>

<task type="auto">
  <name>Task 4: Test feedback adjudication workflow</name>
  <files>
    backend/tests/integration/services/test_governance_coverage.py
  </files>
  <action>
    Add feedback adjudication tests to `test_governance_coverage.py`:

    Class: `TestFeedbackAdjudication`

    1. `test_submit_feedback_triggers_adjudication`:
       - Create agent (INTERN status)
       - Create user with reputation score
       - Call `submit_feedback()` with original_output and user_correction
       - Mock `_adjudicate_feedback()` with AsyncMock
       - Verify feedback status is PENDING
       - Verify adjudication async task triggered

    2. `test_adjudicate_feedback_with_valid_correction` (async):
       - Create feedback with PENDING status
       - Mock LLM response: `{"valid": true, "confidence": 0.95}`
       - Call `_adjudicate_feedback(feedback)`
       - Verify feedback status updated to APPROVED
       - Verify adjudication_timestamp set

    3. `test_adjudicate_feedback_with_invalid_correction` (async):
       - Create feedback with PENDING status
       - Mock LLM response: `{"valid": false, "reason": "Correction is factually incorrect"}`
       - Call `_adjudicate_feedback(feedback)`
       - Verify feedback status updated to REJECTED
       - Verify rejection_reason logged

    4. `test_adjudication_with_high_reputation_user` (async):
       - Create feedback from user with reputation >0.9
       - Mock LLM response: `{"valid": true, "confidence": 0.98}`
       - Verify adjudication favors high-reputation users
       - Verify auto-approval threshold

    Use pytest-asyncio for async tests.
    Mock LLM provider with AsyncMock (no real LLM calls).
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_governance_coverage.py::TestFeedbackAdjudication -v`
    Verify all 4 async tests pass.
    Check coverage covers submit_feedback and _adjudicate_feedback methods.
  </verify>
  <done>
    Feedback adjudication workflow tested (submission, valid/invalid adjudication, reputation-based approval) with mocked LLM.
  </done>
</task>

<task type="auto">
  <name>Task 5: Test HITL action lifecycle and governance cache validation</name>
  <files>
    backend/tests/integration/services/test_governance_coverage.py
  </files>
  <action>
    Add HITL and cache tests to `test_governance_coverage.py`:

    Class: `TestHITLActionManagement`

    1. `test_create_hitl_action`:
       - Call `create_hitl_action()` for INTERN agent attempting complexity 3 action
       - Verify HITLAction created with PENDING status
       - Verify action_type and action_params stored

    2. `test_approve_hitl_action`:
       - Create HITL action with PENDING status
       - Call `handle_hitl_decision(action_id, "approve", reviewer_id)`
       - Verify action.status == APPROVED
       - Verify approved_by and approved_at set

    3. `test_reject_hitl_action`:
       - Create HITL action with PENDING status
       - Call `handle_hitl_decision(action_id, "reject", reviewer_id, reason)`
       - Verify action.status == REJECTED
       - Verify rejection_reason stored

    Class: `TestGovernanceCacheValidation`

    4. `test_cache_hit_reduces_db_lookup`:
       - Create agent and warm cache
       - Call `can_perform_action()` twice
       - Verify cache hit on second call
       - Assert `_hits` increased

    5. `test_cache_invalidation_on_agent_status_change`:
       - Create agent and warm cache
       - Change agent.status in database
       - Call `invalidate_agent(agent_id)`
       - Call `can_perform_action()` - should be cache miss
       - Assert `_misses` increased

    6. `test_cache_ttl_expiration`:
       - Create cache with TTL=1 second
       - Warm cache with agent lookup
       - Sleep 2 seconds
       - Call `can_perform_action()` - should be cache miss (expired)
       - Assert `_misses` increased

    Use time.sleep for TTL testing (acceptable for integration tests).
    Use governance_cache fixture with short TTL.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_governance_coverage.py::TestHITLActionManagement -v`
    Run `pytest backend/tests/integration/services/test_governance_coverage.py::TestGovernanceCacheValidation -v`
    Verify all 6 tests pass (3 HITL + 3 cache).
    Check coverage covers HITL actions and cache invalidation logic.
  </verify>
  <done>
    HITL action lifecycle tested (create, approve, reject) and governance cache validated (hit/miss, invalidation, TTL expiration).
  </done>
</task>

</tasks>

<verification>
Overall phase verification steps:
1. Run full governance test suite: `pytest backend/tests/integration/services/test_governance_coverage.py -v`
2. Verify 30+ tests passing (maturity matrix, lifecycle, feedback, HITL, cache)
3. Check coverage report: `pytest --cov=core.agent_governance_service --cov-report=html`
4. Open htmlcov/index.html and verify agent_governance_service.py at 80%+ coverage
5. Verify all code paths covered:
   - Maturity routing for all 4 levels
   - Permission checking for all 4 complexities
   - Agent lifecycle (register, update, suspend, terminate, reactivate)
   - Feedback submission and adjudication
   - HITL action management
   - Cache behavior (hit, miss, invalidation, TTL)
</verification>

<success_criteria>
1. 30+ governance tests passing (100% pass rate)
2. Agent governance service coverage at 80%+ (verified via coverage report)
3. All maturity levels tested with full permission matrix
4. Agent lifecycle state transitions validated
5. Feedback adjudication workflow tested with mocked LLM
6. HITL action lifecycle tested (create, approve, reject)
7. Governance cache behavior validated (hit/miss, invalidation, TTL)
8. Zero external dependencies (all LLM/WebSocket/DB mocked)
</success_criteria>

<output>
After completion, create `.planning/phases/156-core-services-coverage-high-impact/156-01-SUMMARY.md`
</output>
