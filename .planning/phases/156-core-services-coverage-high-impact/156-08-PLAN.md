---
phase: 156-core-services-coverage-high-impact
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/agent_governance_service.py
  - backend/tests/integration/services/test_governance_coverage.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "AgentGovernanceService.suspend_agent() method implemented"
    - "AgentGovernanceService.terminate_agent() method implemented"
    - "User.name setter issues resolved in tests"
    - "AgentFeedback.agent_id NOT NULL constraint handled"
    - "Agent governance coverage increased to 70%+"
  artifacts:
    - path: "backend/core/agent_governance_service.py"
      provides: "suspend_agent() and terminate_agent() methods"
      contains: "def suspend_agent", "def terminate_agent"
    - path: "backend/tests/integration/services/test_governance_coverage.py"
      provides: "Working governance tests with 70%+ coverage"
      min_tests: 36
  key_links:
    - from: "backend/tests/integration/services/test_governance_coverage.py"
      to: "backend/core/agent_governance_service.py"
      via: "test methods call suspend_agent and terminate_agent"
      pattern: "suspend_agent|terminate_agent"
---

# Phase 156: Agent Governance Gap Closure - Plan 08

<objective>
Implement missing AgentGovernanceService lifecycle methods (suspend_agent, terminate_agent) and fix test issues to increase governance coverage from 44% to 70%+

Purpose: Plan 156-01 created governance tests but 9 tests are failing due to missing service methods and model constraint issues. This plan implements the missing methods and fixes test design issues.

Output: Working governance tests with 70%+ coverage (up from 44%)
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/156-core-services-coverage-high-impact/156-VERIFICATION.md
@.planning/phases/156-core-services-coverage-high-impact/156-01-PLAN.md
@.planning/phases/156-core-services-coverage-high-impact/156-01-SUMMARY.md
@backend/core/agent_governance_service.py
@backend/tests/integration/services/test_governance_coverage.py
@backend/core/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AgentGovernanceService.suspend_agent() method</name>
  <files>backend/core/agent_governance_service.py</files>
  <action>
    Add the suspend_agent() method to AgentGovernanceService class:

    1. Method signature: def suspend_agent(self, agent_id: str) -> bool
    2. Implementation should:
       - Query AgentRegistry by agent_id
       - If not found, return False
       - Set agent.status to AgentStatus.SUSPENDED.value (or "suspended")
       - Set agent.suspended_at to current datetime
       - Commit to database
       - Invalidate governance cache for this agent
       - Return True on success
    3. Add logging for audit trail
    4. Handle database errors gracefully

    Reference existing methods in the class for patterns (register_or_update_agent for query/commit patterns)
  </action>
  <verify>
    pytest backend/tests/integration/services/test_governance_coverage.py::TestAgentLifecycleManagement -v -k suspend
  </verify>
  <done>
    suspend_agent() method exists, agent status changes to "suspended", cache invalidated, returns True on success
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement AgentGovernanceService.terminate_agent() method</name>
  <files>backend/core/agent_governance_service.py</files>
  <action>
    Add the terminate_agent() method to AgentGovernanceService class:

    1. Method signature: def terminate_agent(self, agent_id: str) -> bool
    2. Implementation should:
       - Query AgentRegistry by agent_id
       - If not found, return False
       - Set agent.status to AgentStatus.TERMINATED.value (or "terminated")
       - Set agent.terminated_at to current datetime
       - Commit to database
       - Invalidate governance cache for this agent
       - Return True on success
    3. Add logging for audit trail
    4. Handle database errors gracefully

    Reference suspend_agent() for consistent patterns
  </action>
  <verify>
    pytest backend/tests/integration/services/test_governance_coverage.py::TestAgentLifecycleManagement -v -k terminate
  </verify>
  <done>
    terminate_agent() method exists, agent status changes to "terminated", cache invalidated, returns True on success
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix User.name setter and AgentFeedback.agent_id issues in tests</name>
  <files>backend/tests/integration/services/test_governance_coverage.py</files>
  <action>
    Fix two test design issues identified in VERIFICATION.md:

    1. **User.name setter issues (2 HITL tests)**:
       - User model has first_name/last_name, not name
       - Tests use property setter pattern that doesn't exist
       - Fix: Update TestHITLActionManagement tests to use first_name directly
       - Do NOT try to set user.name = value (no setter exists)

    2. **AgentFeedback.agent_id NOT NULL constraint (3 feedback tests)**:
       - AgentFeedback requires agent_id but tests create without it
       - Fix: Create AgentRegistry first, then use its ID for AgentFeedback
       - Update TestFeedbackAdjudication tests

    Specific test classes to update:
    - TestHITLActionManagement (2 tests failing - User.name setter)
    - TestFeedbackAdjudication (3 tests failing - agent_id constraint)

    Pattern from plan 156-07 summary: Use correct User columns (first_name/last_name), create agents before feedback
  </action>
  <verify>
    pytest backend/tests/integration/services/test_governance_coverage.py -v
  </verify>
  <done>
    All governance tests pass (34/36 or better), no User.name setter errors, no agent_id constraint errors
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run full governance test suite:
   ```bash
   pytest backend/tests/integration/services/test_governance_coverage.py -v
   ```
   Expected: 34/36 tests passing (94% pass rate, up from 75%)

2. Run coverage report:
   ```bash
   cd backend
   pytest tests/integration/services/test_governance_coverage.py --cov=core.agent_governance_service --cov-report=term-missing
   ```
   Expected: 70%+ coverage (up from 44%)

3. Verify methods exist:
   ```bash
   python3 -c "from core.agent_governance_service import AgentGovernanceService; assert hasattr(AgentGovernanceService, 'suspend_agent'); assert hasattr(AgentGovernanceService, 'terminate_agent'); print('OK')"
   ```
</verification>

<success_criteria>
- AgentGovernanceService has suspend_agent() and terminate_agent() methods
- All governance tests execute without AttributeError (methods exist)
- Test pass rate improved from 75% to 94% (34/36 tests)
- Coverage improved from 44% to 70%+
- No User.name setter errors
- No agent_id NOT NULL constraint errors
</success_criteria>

<output>
After completion, create `.planning/phases/156-core-services-coverage-high-impact/156-08-SUMMARY.md`
</output>
