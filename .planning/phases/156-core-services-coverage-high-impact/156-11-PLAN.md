---
phase: 156-core-services-coverage-high-impact
plan: 11
type: execute
wave: 2
depends_on: [156-08, 156-10]
files_modified:
  - backend/core/models.py
  - backend/tests/integration/services/test_episode_services_coverage.py
  - backend/tests/conftest.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User.custom_role_id column nullable or tests provide default"
    - "Episodic memory tests execute without schema errors"
    - "Episode segmentation, retrieval, lifecycle tests pass"
    - "Episodic memory coverage increased to 70%+"
  artifacts:
    - path: "backend/core/models.py"
      provides: "Fixed User.custom_role_id column definition"
      contains: "custom_role_id.*nullable"
    - path: "backend/tests/integration/services/test_episode_services_coverage.py"
      provides: "Working episodic memory tests with 70%+ coverage"
      min_tests: 22
  key_links:
    - from: "backend/tests/integration/services/test_episode_services_coverage.py"
      to: "backend/core/models.py"
      via: "User model with nullable custom_role_id"
      pattern: "User.*custom_role_id"
    - from: "backend/tests/integration/services/test_episode_services_coverage.py"
      to: "backend/core/episode_segmentation_service.py"
      via: "episode segmentation and retrieval tests"
      pattern: "EpisodeSegmentationService|EpisodeRetrievalService"
---

# Phase 156: Episodic Memory Gap Closure - Plan 11

<objective>
Fix User.custom_role_id schema constraint to unblock episodic memory tests and increase coverage from 16% to 70%+

Purpose: Plan 156-03 created episodic memory tests (1,029 lines, 22 tests) but 16/22 tests are blocked by User.custom_role_id NOT NULL constraint errors.

Output: Working episodic memory tests with 70%+ coverage (up from 16%)
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/156-core-services-coverage-high-impact/156-VERIFICATION.md
@.planning/phases/156-core-services-coverage-high-impact/156-03-PLAN.md
@.planning/phases/156-core-services-coverage-high-impact/156-03-SUMMARY.md
@backend/tests/integration/services/test_episode_services_coverage.py
@backend/core/models.py
@backend/core/episode_segmentation_service.py
@backend/core/episode_retrieval_service.py
@backend/core/episode_lifecycle_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix User.custom_role_id schema constraint</name>
  <files>backend/core/models.py</files>
  <action>
    Fix the User.custom_role_id column to allow NULL values:

    1. **Locate the User model** in backend/core/models.py:
       - Find line ~387: custom_role_id = Column(String, ForeignKey("custom_roles.id"))

    2. **Add nullable=True** to the column definition:
       - Change: custom_role_id = Column(String, ForeignKey("custom_roles.id"))
       - To: custom_role_id = Column(String, ForeignKey("custom_roles.id"), nullable=True)

    3. **Verify the change**:
       - Check that nullable=True is added
       - No other changes to User model needed

    4. **Create migration** (optional but recommended):
       - Run: alembic revision -m "make_user_custom_role_id_nullable"
       - Add: op.alter_column('users', 'custom_role_id', existing_type=String, nullable=True)
       - Run: alembic upgrade head

    Rationale from VERIFICATION.md:
    "User.custom_role_id column has NOT NULL constraint but tests insert None"
    "Fix User.custom_role_id column definition (nullable or provide default)"

    Note: Column already has nullable=True in some parts of the codebase (line 387 shows nullable=True exists)
    - Verify if this is a migration issue or test fixture issue
    - If column is already nullable in model, fix may be in test fixtures instead
  </action>
  <verify>
    python3 -c "from core.models import User; print(User.custom_role_id.property.columns[0].nullable)"
    # Should print: True
  </verify>
  <done>
    User.custom_role_id column allows NULL, tests can insert None without constraint errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Update episodic memory test fixtures for User model</name>
  <files>backend/tests/integration/services/test_episode_services_coverage.py</files>
  <action>
    Fix test fixtures to properly handle User.custom_role_id:

    1. **Check if column is already nullable**:
       - If Task 1 shows nullable=True, the issue is in test fixtures
       - If Task 2 shows nullable=False, Task 1 migration is needed

    2. **Fix User creation in tests**:
       - Find all User() constructor calls in test_episode_services_coverage.py
       - If custom_role_id is provided, ensure it's a valid string or None
       - If custom_role_id is NOT provided, that's fine (nullable allows it)

    3. **Update conftest fixtures** if needed:
       - Check backend/tests/conftest.py for User fixtures
       - Ensure User fixtures don't hardcode invalid custom_role_id values
       - Add custom_role_id=None explicitly if needed

    4. **Handle test database state**:
       - Some tests may have stale database with old schema
       - Add pytest fixture to drop/recreate users table if needed
       - Use db_session fixture for clean database state

    Specific test classes affected (from VERIFICATION.md):
    - TestEpisodeSegmentation (time gap tests)
    - TestEpisodeRetrieval (retrieval mode tests)
    - TestEpisodeLifecycle (lifecycle management tests)

    Pattern from VERIFICATION.md:
    "16 tests error due to custom_role_id NOT NULL constraint failure"
    "User model has custom_role_id column but test fixture trying to insert None"
  </action>
  <verify>
    pytest backend/tests/integration/services/test_episode_services_coverage.py -v
  </verify>
  <done>
    All episodic memory tests execute without constraint errors, 20/22 tests passing (91%)
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix remaining episodic memory test failures</name>
  <files>backend/tests/integration/services/test_episode_services_coverage.py</files>
  <action>
    Fix any remaining test failures after schema constraint is resolved:

    1. **Run tests and identify failures**:
       - pytest backend/tests/integration/services/test_episode_services_coverage.py -v
       - Capture specific test failures

    2. **Common issues to fix**:
       - **LanceDB mock issues**: Ensure LanceDB client is properly mocked
       - **Embedding service issues**: Mock embedding generation for semantic tests
       - **Database session issues**: Use correct db_session fixture
       - **Timezone issues**: Use timezone-aware datetime objects
       - **Import errors**: Verify all episode services can be imported

    3. **Fix segmentation tests**:
       - test_detect_time_gaps: Verify time gap calculation logic
       - test_detect_topic_changes: Mock embedding similarity correctly
       - test_segment_creation: Ensure segments are created properly

    4. **Fix retrieval tests**:
       - test_temporal_retrieval: Mock database time range queries
       - test_semantic_retrieval: Mock vector similarity search
       - test_sequential_retrieval: Verify episode reconstruction
       - test_contextual_retrieval: Test hybrid retrieval

    5. **Fix lifecycle tests**:
       - test_episode_decay: Mock decay calculation
       - test_episode_consolidation: Test consolidation logic
       - test_episode_archival: Verify archival to cold storage

    Pattern from plan 156-03 summary:
    "Tests mock LanceDB to focus on segmentation/retrieval algorithms"
    Use Mock and AsyncMock for external dependencies (LanceDB, embeddings)
  </action>
  <verify>
    pytest backend/tests/integration/services/test_episode_services_coverage.py -v
  </verify>
  <done>
    20/22 tests passing (91%), only 2 minor edge cases failing, episodic memory coverage 70%+
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Verify schema change:
   ```bash
   python3 -c "from core.models import User; print('nullable:', User.custom_role_id.property.columns[0].nullable)"
   ```
   Expected: nullable: True

2. Run episodic memory test suite:
   ```bash
   pytest backend/tests/integration/services/test_episode_services_coverage.py -v
   ```
   Expected: 20/22 tests passing (91%, up from 23%)

3. Run coverage report:
   ```bash
   cd backend
   pytest tests/integration/services/test_episode_services_coverage.py --cov=core.episode_segmentation_service --cov=core.episode_retrieval_service --cov=core.episode_lifecycle_service --cov-report=term-missing
   ```
   Expected: 70%+ combined coverage for episode services (up from 16%)

4. Verify no constraint errors:
   ```bash
   pytest backend/tests/integration/services/test_episode_services_coverage.py -v 2>&1 | grep -i "constraint"
   ```
   Expected: No constraint errors in output
</verification>

<success_criteria>
- User.custom_role_id column is nullable (nullable=True)
- Tests can create User objects with custom_role_id=None
- 16 previously blocked tests now execute
- Test pass rate 91% (20/22 passing)
- Episodic memory coverage 70%+ (up from 16%)
- No NOT NULL constraint errors
- Episode segmentation, retrieval, lifecycle services tested
</success_criteria>

<output>
After completion, create `.planning/phases/156-core-services-coverage-high-impact/156-11-SUMMARY.md`
</output>
