---
phase: 156-core-services-coverage-high-impact
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/services/test_episode_services_coverage.py
  - backend/tests/integration/services/conftest.py
  - backend/tests/integration/services/pytest.ini
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Episodic memory coverage expanded to 80% (segmentation, retrieval, lifecycle, canvas/feedback integration)"
    - "Episode segmentation tested for time gaps (>30min) and topic changes (<0.75 similarity)"
    - "Retrieval modes tested (temporal, semantic, sequential, contextual)"
    - "Episode lifecycle tested (decay scoring, consolidation, archival)"
    - "Canvas integration tested with canvas-aware episodes"
    - "Feedback integration tested with feedback-weighted retrieval"
  artifacts:
    - path: "backend/tests/integration/services/test_episode_services_coverage.py"
      provides: "80%+ coverage for episode services"
      min_lines: 700
      exports: ["TestEpisodeSegmentation", "TestEpisodeRetrieval", "TestEpisodeLifecycle", "TestCanvasIntegration", "TestFeedbackIntegration"]
    - path: "backend/tests/integration/services/conftest.py"
      provides: "Shared fixtures (db_session, segmentation_service, retrieval_service, lifecycle_service)"
      min_lines: 70
    - path: "backend/tests/integration/services/pytest.ini"
      provides: "Isolated test configuration"
      min_lines: 10
  key_links:
    - from: "test_episode_services_coverage.py"
      to: "core/episode_segmentation_service.py"
      via: "import EpisodeSegmentationService"
      pattern: "from core.episode_segmentation_service import EpisodeSegmentationService"
    - from: "test_episode_services_coverage.py"
      to: "core/episode_retrieval_service.py"
      via: "import EpisodeRetrievalService"
      pattern: "from core.episode_retrieval_service import EpisodeRetrievalService"
    - from: "test_episode_services_coverage.py"
      to: "core/episode_lifecycle_service.py"
      via: "import EpisodeLifecycleService"
      pattern: "from core.episode_lifecycle_service import EpisodeLifecycleService"
    - from: "test_episode_services_coverage.py"
      to: "LanceDB"
      via: "mocked vector database operations"
      pattern: "patch.*lancedb|Mock.*embed_text|Mock.*search"
---

<objective>
Expand test coverage for Episodic Memory Services to 80% through comprehensive testing of segmentation algorithms, retrieval modes, lifecycle management, and canvas/feedback integration.

Purpose: Episodic Memory is the core learning system that enables agents to remember past experiences, retrieve relevant context, and improve over time. High coverage ensures reliable memory formation and retrieval.

Output: 700+ lines of test code covering all episode service code paths with mocked LanceDB (no real vector DB operations).
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-core-services-coverage-high-impact/156-RESEARCH.md
@backend/core/episode_segmentation_service.py
@backend/core/episode_retrieval_service.py
@backend/core/episode_lifecycle_service.py
@backend/tests/test_episode_services_comprehensive.py
@backend/docs/EPISODIC_MEMORY_IMPLEMENTATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update shared fixtures for episode service testing</name>
  <files>
    backend/tests/integration/services/conftest.py
  </files>
  <action>
    Update `backend/tests/integration/services/conftest.py` with episode fixtures:

    1. Add `segmentation_service` fixture:
       - Create EpisodeSegmentationService instance with db_session
       - Mock LanceDB handler with Mock()
       - Configure mock_embed_text to return test vectors

    2. Add `retrieval_service` fixture:
       - Create EpisodeRetrievalService instance with db_session
       - Mock LanceDB handler with Mock()
       - Configure mock_search to return test episodes

    3. Add `lifecycle_service` fixture:
       - Create EpisodeLifecycleService instance with db_session
       - Mock archival service with Mock()

    4. Add `test_episode` fixture:
       - Create test episode with user_id, started_at, summary
       - Add test segments (3-5 segments)
       - Return episode instance

    5. Add `test_messages` fixture:
       - Create test ChatMessage instances with timestamps
       - Include time gaps (>30min) for segmentation testing
       - Include topic changes for segmentation testing

    Update existing conftest.py (don't recreate).
    Mock LanceDB to avoid real vector DB operations.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/conftest.py::segmentation_service --collect-only`
    Verify all episode fixtures are discoverable.
  </verify>
  <done>
    Episode service fixtures added to shared conftest, ready for all episodic memory tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test episode segmentation algorithms</name>
  <files>
    backend/tests/integration/services/test_episode_services_coverage.py
  </files>
  <action>
    Create `backend/tests/integration/services/test_episode_services_coverage.py` with segmentation tests:

    Class: `TestEpisodeSegmentation`

    1. `test_detect_time_gaps`:
       - Create messages with timestamps:
         - Message 1: base_time
         - Message 2: base_time + 10min
         - Message 3: base_time + 45min (gap >30min)
         - Message 4: base_time + 50min
       - Mock LanceDB embed_text to return test vectors
       - Call detect_time_gaps(messages)
       - Verify 1 boundary detected at index 2
       - Verify gap duration calculated

    2. `test_detect_topic_changes`:
       - Create messages:
         - Message 1-2: "Python programming..."
         - Message 3-4: "Cooking recipes..." (topic change)
       - Mock LanceDB with similarity scores:
         - Python messages: [0.9, 0.1, 0.0]
         - Cooking messages: [0.1, 0.9, 0.0]
       - Call detect_topic_changes(messages)
       - Verify 1 topic change detected at index 2
       - Verify similarity threshold applied (<0.75)

    3. `test_create_episodes_from_boundaries`:
       - Create session with 3 messages
       - Create boundaries = [2] (split after index 1)
       - Call create_episodes(session_id, messages, boundaries)
       - Verify 2 episodes created
       - Verify episode 1 has 2 segments, episode 2 has 1 segment
       - Verify episode timestamps correct

    4. `test_segmentation_with_task_completion`:
       - Create messages showing task progression
       - Add "task completed" marker message
       - Call detect_task_completion(messages)
       - Verify boundary detected at task completion

    Import from research: Use time gaps (>30min) and topic changes (<0.75 similarity) for segmentation.
    Mock LanceDB embed_text to return similarity scores.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_episode_services_coverage.py::TestEpisodeSegmentation -v`
    Verify all segmentation tests pass.
    Check coverage: `pytest --cov=core.episode_segmentation_service --cov-report=term-missing`
  </verify>
  <done>
    Episode segmentation tested for time gaps, topic changes, boundary creation, and task completion detection.
  </done>
</task>

<task type="auto">
  <name>Task 3: Test episode retrieval modes</name>
  <files>
    backend/tests/integration/services/test_episode_services_coverage.py
  </files>
  <action>
    Add retrieval tests to `test_episode_services_coverage.py`:

    Class: `TestEpisodeRetrieval`

    1. `test_temporal_retrieval`:
       - Create 5 episodes with timestamps:
         - Episode 0: today
         - Episode 1: 1 day ago
         - Episode 2: 2 days ago
         - Episode 3: 5 days ago
         - Episode 4: 10 days ago
       - Call retrieve_temporal(user_id, days=3)
       - Verify returns episodes 0-2 (last 3 days)
       - Verify episode 3-4 excluded (>3 days)

    2. `test_semantic_retrieval`:
       - Create episode 1: "Python programming" with embedding [0.9, 0.1, 0.0]
       - Create episode 2: "Cooking recipes" with embedding [0.1, 0.9, 0.0]
       - Mock LanceDB search to return episode 1
       - Call retrieve_semantic(user_id, query="How do I write Python code?")
       - Verify episode 1 returned (Python topic)
       - Verify episode 2 not returned (different topic)

    3. `test_sequential_retrieval`:
       - Create episode with 3 segments
       - Call retrieve_sequential(episode_id)
       - Verify episode returned with all segments
       - Verify segments in correct order (order field)

    4. `test_contextual_retrieval`:
       - Create 5 episodes with timestamps and embeddings
       - Call retrieve_contextual(user_id, query="Recent episodes", days=7, limit=3)
       - Verify returns <=3 episodes
       - Verify episodes from last 7 days
       - Verify semantic relevance applied

    5. `test_retrieval_with_empty_results`:
       - Call retrieve_temporal for non-existent user
       - Verify empty list returned (no error)
       - Verify graceful handling

    Use Mock for LanceDB search operations.
    Test all 4 retrieval modes (temporal, semantic, sequential, contextual).
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_episode_services_coverage.py::TestEpisodeRetrieval -v`
    Verify all retrieval tests pass.
    Check coverage covers all retrieval modes.
  </verify>
  <done>
    Episode retrieval tested for all 4 modes (temporal, semantic, sequential, contextual) with empty result handling.
  </done>
</task>

<task type="auto">
  <name>Task 4: Test episode lifecycle management</name>
  <files>
    backend/tests/integration/services/test_episode_services_coverage.py
  </files>
  <action>
    Add lifecycle tests to `test_episode_services_coverage.py`:

    Class: `TestEpisodeLifecycle`

    1. `test_episode_decay`:
       - Create episode with:
         - started_at = 60 days ago
         - access_count = 5
       - Call calculate_decay_score(episode)
       - Verify decay_score > 0.5 (high decay due to age and low access)
       - Verify decay formula applied correctly

    2. `test_episode_consolidation`:
       - Create episode 1: "Part 1 of discussion" with embedding [0.9, 0.1, 0.0]
       - Create episode 2: "Part 2 of discussion" with embedding [0.9, 0.1, 0.0]
       - Call consolidate_episodes([episode1, episode2])
       - Verify consolidated episode created
       - Verify "consolidated" in episode.id
       - Verify original episodes marked as consolidated

    3. `test_episode_archival`:
       - Create episode with:
         - started_at = 365 days ago
         - access_count = 0
         - decay_score = 0.95
       - Mock archive_to_lancedb()
       - Call archive_episode(episode)
       - Verify archive_to_lancedb called once
       - Verify episode.archived = True
       - Verify archived_at timestamp set

    4. `test_episode_consolidation_with_feedback_aggregation`:
       - Create episodes with feedback scores
       - Consolidate episodes
       - Verify feedback scores aggregated
       - Verify average score calculated

    5. `test_lifecycle_cron_job`:
       - Create episodes with varying decay scores
       - Call lifecycle cron job
       - Verify high decay episodes archived
       - Verify related episodes consolidated
       - Verify cron job execution logged

    Use freezegun to control time for decay testing.
    Mock archival service for consolidation tests.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_episode_services_coverage.py::TestEpisodeLifecycle -v`
    Verify all lifecycle tests pass.
    Check coverage covers decay, consolidation, and archival methods.
  </verify>
  <done>
    Episode lifecycle tested (decay scoring, consolidation, archival) with cron job execution and feedback aggregation.
  </done>
</task>

<task type="auto">
  <name>Task 5: Test canvas and feedback integration</name>
  <files>
    backend/tests/integration/services/test_episode_services_coverage.py
  </files>
  <action>
    Add integration tests to `test_episode_services_coverage.py`:

    Class: `TestCanvasIntegration`

    1. `test_track_canvas_presentations`:
       - Create ChatSession
       - Create CanvasAudit (action="present", canvas_type="chart")
       - Create Episode linked to session
       - Call link_canvas_to_episode(episode_id, canvas_id)
       - Verify canvas linked in episode.canvas_context
       - Verify canvas metadata stored (type, action, timestamp)

    2. `test_retrieve_canvas_context`:
       - Create episode with 2 canvas presentations
       - Call get_canvas_context(episode_id)
       - Verify returns 2 canvas contexts
       - Verify canvas types (chart, form)
       - Verify canvas actions (present, submit)

    3. `test_episode_with_canvas_updates`:
       - Create episode with canvas presentation
       - Create CanvasAudit with action="update"
       - Verify update tracked in episode
       - Verify canvas state changes recorded

    Class: `TestFeedbackIntegration`

    4. `test_aggregate_feedback_scores`:
       - Create episode
       - Create 3 feedback records with ratings 0.8, 0.9, 0.7
       - Call get_feedback_score(episode_id)
       - Verify score = 0.8 (average)
       - Verify aggregation formula applied

    5. `test_feedback_weighted_retrieval`:
       - Create episode_high with feedback_score=0.9
       - Create episode_low with feedback_score=0.3
       - Call retrieve_with_feedback_weighting(user_id, boost_threshold=0.5)
       - Verify episode_high ranked first (boosted)
       - Verify episode_low ranked last (penalized)

    6. `test_feedback_linked_to_episode`:
       - Create AgentFeedback with episode_id
       - Verify feedback.episode_id populated
       - Verify feedback included in episode retrieval
       - Verify metadata-only linkage (no duplicate data)

    Test canvas-aware episodes and feedback-weighted retrieval.
    Use metadata-only linkage (no data duplication).
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_episode_services_coverage.py::TestCanvasIntegration -v`
    Run `pytest backend/tests/integration/services/test_episode_services_coverage.py::TestFeedbackIntegration -v`
    Verify all integration tests pass.
    Check coverage covers canvas and feedback integration methods.
  </verify>
  <done>
    Canvas integration tested (presentation tracking, context retrieval) and feedback integration tested (score aggregation, weighted retrieval, episode linkage).
  </done>
</task>

</tasks>

<verification>
Overall phase verification steps:
1. Run full episode test suite: `pytest backend/tests/integration/services/test_episode_services_coverage.py -v`
2. Verify 35+ tests passing (segmentation, retrieval, lifecycle, canvas, feedback)
3. Check coverage report: `pytest --cov=core.episode_segmentation_service --cov=core.episode_retrieval_service --cov=core.episode_lifecycle_service --cov-report=html`
4. Open htmlcov/index.html and verify all episode service files at 80%+ coverage
5. Verify all code paths covered:
   - Time gap detection (>30min threshold)
   - Topic change detection (<0.75 similarity)
   - Episode creation from boundaries
   - All 4 retrieval modes (temporal, semantic, sequential, contextual)
   - Decay scoring and consolidation
   - Archival to cold storage
   - Canvas-aware episode tracking
   - Feedback-weighted retrieval
</verification>

<success_criteria>
1. 35+ episodic memory tests passing (100% pass rate)
2. Episode services coverage at 80%+ (verified via coverage report)
3. Segmentation algorithms tested (time gaps, topic changes, boundaries)
4. All 4 retrieval modes tested (temporal, semantic, sequential, contextual)
5. Lifecycle management tested (decay, consolidation, archival)
6. Canvas integration tested (presentation tracking, context retrieval)
7. Feedback integration tested (score aggregation, weighted retrieval)
8. Zero external dependencies (LanceDB mocked with Mock)
</success_criteria>

<output>
After completion, create `.planning/phases/156-core-services-coverage-high-impact/156-03-SUMMARY.md`
</output>
