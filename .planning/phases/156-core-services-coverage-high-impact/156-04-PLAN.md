---
phase: 156-core-services-coverage-high-impact
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/services/test_canvas_coverage.py
  - backend/tests/integration/services/test_websocket_coverage.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Canvas state management tested (create, update, close operations)"
    - "Chart rendering tested for all types (line, bar, pie)"
    - "Form validation tested with field-level rules (email, number, required)"
    - "WebSocket broadcast tested with mocked manager"
    - "Governance integration tested (STUDENT blocked from forms, AUTONOMOUS full access)"
    - "Coverage verified: canvas_tool.py at 80%+"
  artifacts:
    - path: "backend/tests/integration/services/test_canvas_coverage.py"
      provides: "Canvas presentation and state management coverage"
      min_lines: 300
      exports: ["TestChartPresentation", "TestFormPresentation", "TestCanvasStateManagement", "TestGovernanceIntegration"]
    - path: "backend/tests/integration/services/test_websocket_coverage.py"
      provides: "WebSocket broadcast coverage for canvas"
      min_lines: 100
      exports: ["TestWebSocketBroadcast", "TestWebSocketErrorHandling"]
  key_links:
    - from: "test_canvas_coverage.py"
      to: "tools/canvas_tool.py"
      via: "test canvas operations"
      pattern: "present_chart|present_form|update_canvas"
    - from: "test_websocket_coverage.py"
      to: "core/websocket_manager.py"
      via: "mock WebSocket broadcast"
      pattern: "ws_manager.broadcast"
---

<objective>
Expand test coverage for Canvas Presentation to 80% through comprehensive testing of chart rendering, form validation, state management, and WebSocket delivery.

Purpose: Canvas Presentation is the primary user interface for agent responses. High coverage ensures reliable visualization and user interaction.

Output: 400+ lines of test code covering all canvas code paths with mocked WebSocket.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-core-services-coverage-high-impact/156-RESEARCH.md
@backend/tools/canvas_tool.py
@backend/tests/test_canvas_*.py
@backend/docs/CANVAS_IMPLEMENTATION_COMPLETE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test canvas chart presentation and rendering</name>
  <files>
    backend/tests/integration/services/test_canvas_coverage.py
  </files>
  <action>
    Create `backend/tests/integration/services/test_canvas_coverage.py`:

    Class: `TestChartPresentation`

    1. `test_present_line_chart` (async):
       - Chart data: [{"x": "Jan", "y": 100}, {"x": "Feb", "y": 200}, {"x": "Mar", "y": 150}]
       - Call present_chart(user_id, chart_type="line", data=chart_data, title="Monthly Sales")
       - Verify canvas_id returned
       - Verify CanvasAudit created with action="present"
       - Verify WebSocket broadcast called

    2. `test_present_bar_chart` (async):
       - Bar chart data with categories
       - Verify chart_type="bar" in CanvasAudit

    3. `test_present_pie_chart` (async):
       - Pie chart data with slices
       - Verify chart_type="pie" in CanvasAudit

    4. `test_chart_with_custom_options` (async):
       - Test custom colors, axes, legend
       - Verify options passed to canvas state

    Mock WebSocket broadcast with AsyncMock.
    Use pytest-asyncio for async tests.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_canvas_coverage.py::TestChartPresentation -v`
    Verify all chart tests pass.
    Check coverage: `pytest --cov=tools.canvas_tool --cov-report=term-missing`
  </verify>
  <done>
    Chart presentation tested for all types (line, bar, pie) with custom options.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test canvas form presentation and validation</name>
  <files>
    backend/tests/integration/services/test_canvas_coverage.py
  </files>
  <action>
    Add to `test_canvas_coverage.py`:

    Class: `TestFormPresentation`

    1. `test_present_form` (async):
       - Form schema with email and name fields
       - Verify canvas_id returned
       - Verify canvas_type="form" in CanvasAudit

    2. `test_form_validation` (async):
       - Valid submission: {"email": "test@example.com", "age": 25}
       - Verify result["valid"] == True

    3. `test_form_validation_invalid_email` (async):
       - Invalid: {"email": "not-an-email"}
       - Verify "email" in result["errors"]

    4. `test_form_validation_age_below_minimum` (async):
       - Invalid: {"age": 15}
       - Verify "age" in result["errors"]

    Test all field types (email, text, number).
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_canvas_coverage.py::TestFormPresentation -v`
    Verify all form tests pass.
  </verify>
  <done>
    Form presentation tested with field types and validation rules.
  </done>
</task>

<task type="auto">
  <name>Task 3: Test canvas state management and governance integration</name>
  <files>
    backend/tests/integration/services/test_canvas_coverage.py
  </files>
  <action>
    Add to `test_canvas_coverage.py`:

    Class: `TestCanvasStateManagement`

    1. `test_update_canvas_state` (async):
       - Create canvas with present_chart()
       - Call update_canvas(canvas_id, user_id, updates={"title": "Updated Title"})
       - Verify updated == True
       - Verify CanvasAudit.action == "update"

    2. `test_close_canvas` (async):
       - Create canvas with present_chart()
       - Call close_canvas(canvas_id, user_id)
       - Verify closed == True
       - Verify action == "close"

    3. `test_canvas_state_serialization` (async):
       - Create canvas with complex state
       - Verify state serialized to JSON
       - Verify no circular references

    Class: `TestGovernanceIntegration`

    4. `test_student_agent_can_present_charts` (async):
       - Create STUDENT agent
       - Call present_chart(agent_id=student_agent.id)
       - Verify allowed (complexity 1)

    5. `test_student_agent_blocked_from_forms` (async):
       - Create STUDENT agent
       - Call present_form(agent_id=student_agent.id)
       - Verify PermissionError raised (complexity 3)

    6. `test_autonomous_agent_full_access` (async):
       - Create AUTONOMOUS agent
       - Verify all operations allowed

    Use parametrize for maturity matrix.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_canvas_coverage.py::TestCanvasStateManagement -v`
    Run `pytest backend/tests/integration/services/test_canvas_coverage.py::TestGovernanceIntegration -v`
    Verify all tests pass.
  </verify>
  <done>
    Canvas state management and governance integration tested.
  </done>
</task>

</tasks>

<verification>
Overall verification:
1. Run `pytest backend/tests/integration/services/test_canvas_coverage.py -v`
2. Verify 20+ tests passing (charts, forms, state, governance)
3. Check coverage: `pytest --cov=tools.canvas_tool --cov-report=html`
4. Open htmlcov/index.html and verify canvas_tool.py at 80%+ coverage
5. Confirm tests exercise production code: present_chart, present_form, update_canvas, close_canvas, validate_form_submission
6. Verify WebSocket broadcast called in all presentation tests
</verification>

<success_criteria>
1. 20+ canvas tests passing (100% pass rate)
2. Canvas presentation at 80%+ coverage
3. Chart rendering tested (line, bar, pie)
4. Form validation tested (email, number, required)
5. State management tested (create, update, close)
6. Governance integration tested (maturity-based permissions)
7. WebSocket broadcast verified in all presentation tests
8. Zero external dependencies (WebSocket mocked)
</success_criteria>

<output>
After completion, create `.planning/phases/156-core-services-coverage-high-impact/156-04-SUMMARY.md`
</output>
