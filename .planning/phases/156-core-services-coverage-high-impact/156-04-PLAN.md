---
phase: 156-core-services-coverage-high-impact
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/services/test_canvas_presentation_coverage.py
  - backend/tests/integration/services/test_http_client_coverage.py
  - backend/tests/integration/services/conftest.py
  - backend/tests/integration/services/pytest.ini
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Canvas presentation coverage expanded to 80% (state management, chart rendering, form validation, interactive components)"
    - "HTTP client coverage expanded to 80% (connection pooling, retry logic, timeouts, error handling)"
    - "Canvas state management tested (create, update, close)"
    - "Chart rendering tested for all types (line, bar, pie)"
    - "Form validation tested with field-level rules"
    - "HTTP client tested with connection pooling, timeout handling, retry logic"
    - "WebSocket integration tested with mocked broadcast"
  artifacts:
    - path: "backend/tests/integration/services/test_canvas_presentation_coverage.py"
      provides: "80%+ coverage for canvas presentation"
      min_lines: 400
      exports: ["TestChartPresentation", "TestFormPresentation", "TestCanvasStateManagement", "TestGovernanceIntegration"]
    - path: "backend/tests/integration/services/test_http_client_coverage.py"
      provides: "80%+ coverage for HTTP client"
      min_lines: 250
      exports: ["TestClientInitialization", "TestConnectionPooling", "TestTimeoutHandling", "TestErrorHandling", "TestClientCleanup"]
    - path: "backend/tests/integration/services/conftest.py"
      provides: "Shared fixtures (db_session, mock_websocket_manager)"
      min_lines: 80
    - path: "backend/tests/integration/services/pytest.ini"
      provides: "Isolated test configuration"
      min_lines: 10
  key_links:
    - from: "test_canvas_presentation_coverage.py"
      to: "tools/canvas_tool.py"
      via: "import present_chart, present_form, update_canvas, close_canvas"
      pattern: "from tools.canvas_tool import present_chart|present_form"
    - from: "test_canvas_presentation_coverage.py"
      to: "WebSocket manager"
      via: "mocked broadcast operations"
      pattern: "AsyncMock.*ws_manager|patch.*ws_manager"
    - from: "test_http_client_coverage.py"
      to: "core/http_client.py"
      via: "import get_async_client, get_sync_client"
      pattern: "from core.http_client import get_async_client"
    - from: "test_http_client_coverage.py"
      to: "httpx"
      via: "HTTP client mocking with Transport ASGI"
      pattern: "httpx.*Transport|patch.*httpx"
---

<objective>
Expand test coverage for Canvas Presentation and HTTP Client to 80% through comprehensive testing of canvas state management, chart rendering, form validation, and HTTP client behavior (connection pooling, retry logic, timeouts).

Purpose: Canvas Presentation is the primary user interface for agent responses, and HTTP Client is the foundation for all external API communication. High coverage ensures reliable visualization and communication.

Output: 650+ lines of test code covering all canvas and HTTP client code paths with mocked WebSocket (no real WebSocket server) and httpx Transport mocking (no real HTTP calls).
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-core-services-coverage-high-impact/156-RESEARCH.md
@backend/tools/canvas_tool.py
@backend/core/http_client.py
@backend/tests/test_canvas_*.py
@backend/docs/CANVAS_IMPLEMENTATION_COMPLETE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update shared fixtures for canvas and HTTP client testing</name>
  <files>
    backend/tests/integration/services/conftest.py
  </files>
  <action>
    Update `backend/tests/integration/services/conftest.py` with canvas and HTTP fixtures:

    1. Add `mock_websocket_manager` fixture:
       - Mock WebSocket manager with AsyncMock
       - Configure broadcast method to return None
       - Use patch('tools.canvas_tool.ws_manager')

    2. Add `test_canvas` fixture:
       - Create test CanvasAudit record
       - Set canvas_type, action, user_id
       - Return canvas instance

    3. Add `async_http_client` fixture:
       - Create httpx.AsyncClient instance
       - Configure timeout and limits
       - Return client for testing

    4. Add `sync_http_client` fixture:
       - Create httpx.Client instance
       - Configure timeout and limits
       - Return client for testing

    Update existing conftest.py (don't recreate).
    Mock WebSocket to avoid real WebSocket server.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/conftest.py::mock_websocket_manager --collect-only`
    Verify canvas and HTTP fixtures are discoverable.
  </verify>
  <done>
    Canvas and HTTP client fixtures added to shared conftest, ready for all canvas and HTTP tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test canvas chart presentation and rendering</name>
  <files>
    backend/tests/integration/services/test_canvas_presentation_coverage.py
  </files>
  <action>
    Create `backend/tests/integration/services/test_canvas_presentation_coverage.py` with chart tests:

    Class: `TestChartPresentation`

    1. `test_present_line_chart` (async):
       - Chart data: [{"x": "Jan", "y": 100}, {"x": "Feb", "y": 200}, {"x": "Mar", "y": 150}]
       - Call present_chart(user_id, chart_type="line", data=chart_data, title="Monthly Sales")
       - Verify canvas_id returned
       - Verify mock_websocket_manager.broadcast called once
       - Verify CanvasAudit record created with action="present"

    2. `test_present_bar_chart` (async):
       - Chart data: [{"category": "A", "value": 100}, {"category": "B", "value": 200}]
       - Call present_chart(user_id, chart_type="bar", data=chart_data, title="Category Comparison")
       - Verify canvas_id returned
       - Verify chart_type="bar" in CanvasAudit

    3. `test_present_pie_chart` (async):
       - Chart data: [{"slice": "A", "value": 30}, {"slice": "B", "value": 70}]
       - Call present_chart(user_id, chart_type="pie", data=chart_data, title="Distribution")
       - Verify canvas_id returned
       - Verify chart_type="pie" in CanvasAudit

    4. `test_chart_with_custom_options` (async):
       - Call present_chart with custom options (colors, axes, legend)
       - Verify options passed to canvas state
       - Verify chart rendered with custom styling

    Use pytest-asyncio for async tests.
    Mock WebSocket broadcast with AsyncMock.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_canvas_presentation_coverage.py::TestChartPresentation -v`
    Verify all chart presentation tests pass.
    Check coverage: `pytest --cov=tools.canvas_tool --cov-report=term-missing`
  </verify>
  <done>
    Chart presentation tested for all types (line, bar, pie) with custom options and WebSocket delivery.
  </done>
</task>

<task type="auto">
  <name>Task 3: Test canvas form presentation and validation</name>
  <files>
    backend/tests/integration/services/test_canvas_presentation_coverage.py
  </files>
  <action>
    Add form tests to `test_canvas_presentation_coverage.py`:

    Class: `TestFormPresentation`

    1. `test_present_form` (async):
       - Form schema: {"fields": [{"name": "email", "type": "email", "required": True}, {"name": "name", "type": "text", "required": True}]}
       - Call present_form(user_id, form_schema=form_schema, title="User Information")
       - Verify canvas_id returned
       - Verify CanvasAudit record created with canvas_type="form"

    2. `test_form_validation` (async):
       - Form schema: {"fields": [{"name": "email", "type": "email", "required": True}, {"name": "age", "type": "number", "min": 18}]}
       - Valid submission: {"email": "test@example.com", "age": 25}
       - Call validate_form_submission(form_schema, valid_data)
       - Verify result["valid"] == True
       - Verify result["errors"] empty

    3. `test_form_validation_invalid_email` (async):
       - Invalid submission: {"email": "not-an-email", "age": 25}
       - Call validate_form_submission(form_schema, invalid_data)
       - Verify result["valid"] == False
       - Verify "email" in result["errors"]

    4. `test_form_validation_age_below_minimum` (async):
       - Invalid submission: {"email": "test@example.com", "age": 15}
       - Call validate_form_submission(form_schema, invalid_data)
       - Verify result["valid"] == False
       - Verify "age" in result["errors"]
       - Verify error message mentions minimum age

    5. `test_form_submission_with_file_upload` (async):
       - Form schema with file upload field
       - Mock file upload handler
       - Verify file validation applied
       - Verify submission stored

    Test all field types (email, text, number, file).
    Test validation rules (required, min, max, pattern).
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_canvas_presentation_coverage.py::TestFormPresentation -v`
    Verify all form presentation and validation tests pass.
    Check coverage covers form presentation and validation methods.
  </verify>
  <done>
    Form presentation tested with field types and validation rules (required, min, max, pattern, file upload).
  </done>
</task>

<task type="auto">
  <name>Task 4: Test canvas state management and governance integration</name>
  <files>
    backend/tests/integration/services/test_canvas_presentation_coverage.py
  </files>
  <action>
    Add state management and governance tests to `test_canvas_presentation_coverage.py`:

    Class: `TestCanvasStateManagement`

    1. `test_update_canvas_state` (async):
       - Create canvas with present_chart()
       - Call update_canvas(canvas_id, user_id, updates={"title": "Updated Title"})
       - Verify updated == True
       - Verify mock_websocket_manager.broadcast called
       - Verify CanvasAudit record updated with action="update"

    2. `test_close_canvas` (async):
       - Create canvas with present_chart()
       - Call close_canvas(canvas_id, user_id)
       - Verify closed == True
       - Verify CanvasAudit.action == "close"
       - Verify canvas marked as inactive

    3. `test_canvas_state_serialization` (async):
       - Create canvas with complex state (nested objects, arrays)
       - Retrieve canvas state
       - Verify state serialized to JSON correctly
       - Verify no circular references

    Class: `TestGovernanceIntegration`

    4. `test_student_agent_can_present_charts` (async):
       - Create agent with STUDENT status
       - Call present_chart(agent_id=student_agent.id)
       - Verify canvas_id returned (STUDENT allowed for complexity 1)
       - Verify governance check passed

    5. `test_student_agent_blocked_from_forms` (async):
       - Create agent with STUDENT status
       - Call present_form(agent_id=student_agent.id) - should raise PermissionError
       - Verify PermissionError raised (STUDENT blocked from complexity 3)
       - Verify governance check failed

    6. `test_autonomous_agent_full_access` (async):
       - Create agent with AUTONOMOUS status
       - Call present_chart(), present_form(), update_canvas(), close_canvas()
       - Verify all operations allowed (AUTONOMOUS full access)
       - Verify no permission errors

    Test all maturity levels against canvas operations.
    Use parametrize for maturity matrix testing.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_canvas_presentation_coverage.py::TestCanvasStateManagement -v`
    Run `pytest backend/tests/integration/services/test_canvas_presentation_coverage.py::TestGovernanceIntegration -v`
    Verify all state management and governance tests pass.
    Check coverage covers state management and governance integration.
  </verify>
  <done>
    Canvas state management tested (create, update, close) and governance integration tested (STUDENT blocked, AUTONOMOUS full access).
  </done>
</task>

<task type="auto">
  <name>Task 5: Test HTTP client initialization and connection pooling</name>
  <files>
    backend/tests/integration/services/test_http_client_coverage.py
  </files>
  <action>
    Create `backend/tests/integration/services/test_http_client_coverage.py` with HTTP client tests:

    Class: `TestClientInitialization`

    1. `test_async_client_created_once`:
       - Call get_async_client() twice
       - Verify client1 is client2 (singleton pattern)
       - Verify same instance returned

    2. `test_sync_client_created_once`:
       - Call get_sync_client() twice
       - Verify client1 is client2 (singleton pattern)
       - Verify same instance returned

    3. `test_async_client_configuration`:
       - Call get_async_client()
       - Verify client.timeout == DEFAULT_TIMEOUT
       - Verify client.limits == DEFAULT_LIMITS
       - Verify client.http2 == True

    4. `test_sync_client_configuration`:
       - Call get_sync_client()
       - Verify client.timeout == DEFAULT_TIMEOUT
       - Verify client.limits == DEFAULT_LIMITS
       - Verify client.http2 == True

    Class: `TestConnectionPooling`

    5. `test_async_connection_reuse` (async):
       - Mock client.get() with httpx.Response(200)
       - Make 2 requests to same URL
       - Verify both calls succeed
       - Verify connection reused (no new connection created)

    6. `test_sync_connection_reuse`:
       - Mock client.get() with httpx.Response(200)
       - Make 2 requests to same URL
       - Verify both calls succeed
       - Verify connection reused

    Test singleton pattern and connection reuse.
    Use httpx.Response mocking.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_http_client_coverage.py::TestClientInitialization -v`
    Run `pytest backend/tests/integration/services/test_http_client_coverage.py::TestConnectionPooling -v`
    Verify all HTTP client initialization and pooling tests pass.
    Check coverage: `pytest --cov=core.http_client --cov-report=term-missing`
  </verify>
  <done>
    HTTP client initialization tested (singleton pattern, configuration) and connection pooling tested (connection reuse).
  </done>
</task>

<task type="auto">
  <name>Task 6: Test HTTP client timeout handling, error handling, and cleanup</name>
  <files>
    backend/tests/integration/services/test_http_client_coverage.py
  </files>
  <action>
    Add timeout, error, and cleanup tests to `test_http_client_coverage.py`:

    Class: `TestTimeoutHandling`

    1. `test_async_request_timeout` (async):
       - Mock client.get() to raise httpx.TimeoutException("Request timed out")
       - Call client.get("http://example.com")
       - Verify httpx.TimeoutException raised
       - Verify timeout handling applied

    2. `test_sync_request_timeout`:
       - Mock client.get() to raise httpx.TimeoutException("Request timed out")
       - Call client.get("http://example.com")
       - Verify httpx.TimeoutException raised

    Class: `TestErrorHandling`

    3. `test_async_network_error` (async):
       - Mock client.get() to raise httpx.NetworkError("Connection failed")
       - Call client.get("http://example.com")
       - Verify httpx.NetworkError raised

    4. `test_async_http_status_error` (async):
       - Mock client.get() to return httpx.Response(500, text="Internal Server Error")
       - Call client.get("http://example.com")
       - Verify response.status_code == 500
       - Verify error not raised (status code returned)

    5. `test_retry_on_transient_failure` (async):
       - Mock client.get() to raise httpx.NetworkError twice, then succeed
       - Call client.get("http://example.com") with retry logic
       - Verify 3 attempts made
       - Verify eventual success

    Class: `TestClientCleanup`

    6. `test_close_async_client` (async):
       - Call get_async_client()
       - Mock client.aclose()
       - Call close_http_clients()
       - Verify aclose() called once

    7. `test_close_sync_client`:
       - Call get_sync_client()
       - Mock client.close()
       - Call close_http_clients()
       - Verify close() called once

    Test timeout, network errors, status errors, retry logic.
    Test resource cleanup (aclose, close).
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_http_client_coverage.py::TestTimeoutHandling -v`
    Run `pytest backend/tests/integration/services/test_http_client_coverage.py::TestErrorHandling -v`
    Run `pytest backend/tests/integration/services/test_http_client_coverage.py::TestClientCleanup -v`
    Verify all timeout, error, and cleanup tests pass.
    Check coverage covers timeout, error handling, retry, and cleanup methods.
  </verify>
  <done>
    HTTP client timeout handling tested, error handling tested (network errors, status errors, retry logic), and cleanup tested (aclose, close).
  </done>
</task>

</tasks>

<verification>
Overall phase verification steps:
1. Run full canvas test suite: `pytest backend/tests/integration/services/test_canvas_presentation_coverage.py -v`
2. Run full HTTP client test suite: `pytest backend/tests/integration/services/test_http_client_coverage.py -v`
3. Verify 30+ canvas tests passing (charts, forms, state, governance)
4. Verify 15+ HTTP client tests passing (initialization, pooling, timeouts, errors, cleanup)
5. Check coverage reports:
   - `pytest --cov=tools.canvas_tool --cov-report=html`
   - `pytest --cov=core.http_client --cov-report=html`
6. Open htmlcov/index.html and verify canvas_tool.py and http_client.py at 80%+ coverage
7. Verify all code paths covered:
   - Canvas: Chart rendering (line, bar, pie), form validation, state management, governance checks
   - HTTP Client: Singleton pattern, connection pooling, timeout handling, error handling, retry logic, cleanup
</verification>

<success_criteria>
1. 45+ total tests passing (30 canvas + 15 HTTP client, 100% pass rate)
2. Canvas presentation coverage at 80%+ (verified via coverage report)
3. HTTP client coverage at 80%+ (verified via coverage report)
4. Canvas chart rendering tested (line, bar, pie)
5. Canvas form validation tested (email, number, file upload)
6. Canvas state management tested (create, update, close)
7. Canvas governance integration tested (maturity-based permissions)
8. HTTP client connection pooling tested
9. HTTP client timeout handling tested
10. HTTP client error handling tested (network errors, status errors, retry)
11. HTTP client cleanup tested (aclose, close)
12. Zero external dependencies (WebSocket mocked, HTTP mocked with httpx)
</success_criteria>

<output>
After completion, create `.planning/phases/156-core-services-coverage-high-impact/156-04-SUMMARY.md`
</output>
