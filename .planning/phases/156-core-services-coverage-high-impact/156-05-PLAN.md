---
phase: 156-core-services-coverage-high-impact
plan: 05
type: execute
wave: 2
depends_on: ["156-02"]
files_modified:
  - backend/tests/integration/services/test_llm_coverage_part2.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Rate limiting tested with per-user limits and quota enforcement"
    - "Streaming responses tested with chunked delivery and token tracking"
    - "Context window management tested with truncation logic"
    - "Cache-aware routing tested with hit/miss scenarios"
    - "Model selection tested for all providers with fallback logic"
    - "Coverage verified: byok_handler.py at 80%+ (combined with plan 02)"
  artifacts:
    - path: "backend/tests/integration/services/test_llm_coverage_part2.py"
      provides: "BYOK streaming, rate limiting, cache coverage"
      min_lines: 350
      exports: ["TestRateLimiting", "TestStreamingResponses", "TestContextWindowManagement", "TestCacheAwareRouting", "TestModelSelection"]
  key_links:
    - from: "test_llm_coverage_part2.py"
      to: "core/llm/byok_handler.py"
      via: "test streaming, rate limiting, cache"
      pattern: "stream_response|check_rate_limit|get_cache_key"
---

<objective>
Expand test coverage for LLM Service Part 2: Rate limiting, streaming responses, context window management, cache-aware routing, and model selection.

Purpose: Complete BYOKHandler coverage by testing streaming behavior, rate limits, context windows, and caching logic.

Output: 350+ lines of test code covering remaining BYOK code paths with mocked dependencies.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-core-services-coverage-high-impact/156-RESEARCH.md
@backend/core/llm/byok_handler.py
@backend/core/llm/cognitive_tier_system.py
@backend/tests/test_byok_handler_integration.py
@backend/docs/COGNITIVE_TIER_SYSTEM.md
@.planning/phases/156-core-services-coverage-high-impact/156-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test rate limiting and quota enforcement</name>
  <files>
    backend/tests/integration/services/test_llm_coverage_part2.py
  </files>
  <action>
    Create `backend/tests/integration/services/test_llm_coverage_part2.py`:

    Class: `TestRateLimiting`

    1. `test_rate_limiting_by_user`:
       - Mock user with 10 requests/minute limit
       - Verify 10 requests allowed, 11th blocked

    2. `test_quota_enforcement`:
       - Mock workspace with 500K token quota
       - Mock usage at 1M tokens
       - Verify quota exceeded

    3. `test_rate_limit_reset_after_window`:
       - Mock last_request_time = 2 hours ago
       - Verify rate limit reset

    Use fakeredis for rate limiter (no real Redis).
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_llm_coverage_part2.py::TestRateLimiting -v`
    Verify all rate limiting tests pass.
  </verify>
  <done>
    Rate limiting tested with quota enforcement and window reset.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test streaming responses and context window management</name>
  <files>
    backend/tests/integration/services/test_llm_coverage_part2.py
  </files>
  <action>
    Add to `test_llm_coverage_part2.py`:

    Class: `TestStreamingResponses`

    1. `test_streaming_response_chunks` (async):
       - Mock stream with ["Hello", " world", "!", None]
       - Verify chunks received in order

    2. `test_streaming_with_token_tracking` (async):
       - Mock 3 chunks
       - Verify token count tracked

    3. `test_streaming_error_handling` (async):
       - Mock stream raises exception
       - Verify partial response returned

    Class: `TestContextWindowManagement`

    4. `test_get_context_window` (parametrize):
       - Test gpt-4 (8192), claude-3-opus (200000), deepseek-chat (16384)

    5. `test_truncate_to_context_window`:
       - Create 100K token prompt
       - Verify truncated to 8192 tokens

    6. `test_context_window_with_system_message`:
       - Test system + user message within window

    Use AsyncMock for streaming, pytest-asyncio for async tests.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_llm_coverage_part2.py::TestStreamingResponses -v`
    Run `pytest backend/tests/integration/services/test_llm_coverage_part2.py::TestContextWindowManagement -v`
    Verify all tests pass.
  </verify>
  <done>
    Streaming and context window management tested.
  </done>
</task>

<task type="auto">
  <name>Task 3: Test cache-aware routing and model selection</name>
  <files>
    backend/tests/integration/services/test_llm_coverage_part2.py
  </files>
  <action>
    Add to `test_llm_coverage_part2.py`:

    Class: `TestCacheAwareRouting`

    1. `test_cache_hit_returns_early`:
       - First call: provider called
       - Second call: provider NOT called (cache hit)

    2. `test_cache_key_generation`:
       - Same prompts → same key
       - Different prompts → different keys
       - Key includes model and temperature

    3. `test_cache_invalidation_on_parameter_change`:
       - Same prompt, different temperature → cache miss

    Class: `TestModelSelection`

    4. `test_select_model_for_complexity` (parametrize):
       - Test 6 provider × complexity combinations
       - openai+SIMPLE→o4-mini, openai+ADVANCED→o3
       - anthropic+MODERATE→claude-3-haiku, anthropic+ADVANCED→claude-4-opus
       - deepseek+SIMPLE→deepseek-chat, deepseek+ADVANCED→deepseek-v3.2-speciale

    5. `test_fallback_on_model_unavailable`:
       - Mock availability = False
       - Verify fallback model returned

    6. `test_model_selection_with_cache_preference`:
       - Verify caching capability influences selection

    Mock cache with in-memory dict.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_llm_coverage_part2.py::TestCacheAwareRouting -v`
    Run `pytest backend/tests/integration/services/test_llm_coverage_part2.py::TestModelSelection -v`
    Verify all tests pass.
  </verify>
  <done>
    Cache-aware routing and model selection tested.
  </done>
</task>

</tasks>

<verification>
Overall verification:
1. Run `pytest backend/tests/integration/services/test_llm_coverage_part2.py -v`
2. Verify 20+ tests passing (rate limiting, streaming, context window, cache, model selection)
3. Check combined coverage: `pytest --cov=core.llm.byok_handler --cov-report=html`
4. Verify byok_handler.py at 80%+ coverage (plans 02 + 05 combined)
5. Confirm tests exercise production code: check_rate_limit, check_quota, stream_response, truncate_to_window, select_model
</verification>

<success_criteria>
1. 20+ tests passing (100% pass rate)
2. BYOK handler at 80%+ coverage (plans 02 + 05 combined)
3. Rate limiting covered: check_rate_limit, check_quota
4. Streaming covered: stream_response, token tracking
5. Context window covered: get_context_window, truncate_to_window
6. Cache routing covered: cache hit/miss, key generation
7. Model selection covered with fallback logic
8. Zero external dependencies (all mocked)
</success_criteria>

<output>
After completion, create `.planning/phases/156-core-services-coverage-high-impact/156-05-SUMMARY.md`
</output>
