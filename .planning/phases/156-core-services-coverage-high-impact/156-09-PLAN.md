---
phase: 156-core-services-coverage-high-impact
plan: 09
type: execute
wave: 2
depends_on: [156-08, 156-10]
files_modified:
  - backend/tests/integration/services/test_llm_coverage_part1.py
  - backend/tests/integration/services/test_llm_coverage_part2.py
  - backend/core/llm/byok_handler.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Provider-specific code paths covered (OpenAI, Anthropic, DeepSeek, Gemini)"
    - "Error handling and edge cases covered (timeouts, rate limits, failures)"
    - "Streaming interruption and recovery tested"
    - "Cache invalidation and TTL expiration tested"
    - "LLM service coverage increased to 70%+"
  artifacts:
    - path: "backend/tests/integration/services/test_llm_coverage_part1.py"
      provides: "Expanded LLM coverage with provider-specific tests"
      min_tests: 70
    - path: "backend/tests/integration/services/test_llm_coverage_part2.py"
      provides: "Expanded LLM coverage with edge case tests"
      min_tests: 65
  key_links:
    - from: "backend/tests/integration/services/test_llm_coverage_*.py"
      to: "backend/core/llm/byok_handler.py"
      via: "provider-specific error paths and edge cases"
      pattern: "_call_openai|_call_anthropic|_call_deepseek|_call_gemini|stream_response|truncate_to_context"
---

# Phase 156: LLM Service Gap Closure - Plan 09

<objective>
Add tests for provider-specific code paths, error handling, streaming interruption, and cache invalidation to increase LLM service coverage from 37% to 70%+

Purpose: Plans 156-02 and 156-05 created LLM tests with 100% pass rate but only 37% coverage. Missing provider-specific logic, edge cases, and error paths.

Output: Comprehensive LLM tests with 70%+ coverage (up from 37%)
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/156-core-services-coverage-high-impact/156-VERIFICATION.md
@.planning/phases/156-core-services-coverage-high-impact/156-02-PLAN.md
@.planning/phases/156-core-services-coverage-high-impact/156-02-SUMMARY.md
@.planning/phases/156-core-services-coverage-high-impact/156-05-PLAN.md
@.planning/phases/156-core-services-coverage-high-impact/156-05-SUMMARY.md
@backend/tests/integration/services/test_llm_coverage_part1.py
@backend/tests/integration/services/test_llm_coverage_part2.py
@backend/core/llm/byok_handler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add provider-specific code path tests</name>
  <files>backend/tests/integration/services/test_llm_coverage_part1.py</files>
  <action>
    Add comprehensive tests for provider-specific code paths in BYOKHandler:

    1. **Create TestProviderSpecificPaths class** with tests:
       - test_openai_provider_routing: Verify OpenAI provider selected, correct endpoint called
       - test_anthropic_provider_routing: Verify Anthropic provider selected, correct headers
       - test_deepseek_provider_routing: Verify DeepSeek provider selected
       - test_gemini_provider_routing: Verify Gemini provider selected
       - test_moonshot_provider_routing: Verify Moonshot provider selected
       - test_provider_fallback_on_failure: Test fallback to secondary provider

    2. **Test provider-specific error handling**:
       - test_openai_rate_limit_error: Mock 429 response, verify retry logic
       - test_anthropic_timeout_error: Mock timeout, verify fallback
       - test_deepseek_api_error: Mock 500 error, verify error handling
       - test_gemini_invalid_request: Mock 400 error, verify validation

    3. **Test provider-specific request formatting**:
       - test_openai_request_format: Verify OpenAI message format
       - test_anthropic_request_format: Verify Anthropic message format
       - test_deepseek_request_format: Verify DeepSeek message format
       - test_gemini_request_format: Verify Gemini message format

    4. **Use parametrize for efficiency**:
       - @pytest.mark.parametrize for all 6 providers
       - Test routing, error handling, and formatting per provider

    5. **Mock provider clients directly**:
       - Patch core.llm.byok_handler.AsyncOpenAI for OpenAI
       - Patch core.llm.byok_handler.OpenAI for Anthropic
       - Mock responses to cover success and error paths

    Pattern from VERIFICATION.md:
    "Missing: Provider-specific code paths (OpenAI, Anthropic, DeepSeek, Gemini)"
    "Add tests for all provider-specific code paths"

    Add ~15 new tests to test_llm_coverage_part1.py
  </action>
  <verify>
    pytest backend/tests/integration/services/test_llm_coverage_part1.py::TestProviderSpecificPaths -v
  </verify>
  <done>
    15 new tests for provider-specific paths, all providers covered, error paths tested
  </done>
</task>

<task type="auto">
  <name>Task 2: Add error handling and edge case tests</name>
  <files>backend/tests/integration/services/test_llm_coverage_part2.py</files>
  <action>
    Add comprehensive tests for error handling and edge cases:

    1. **Create TestErrorHandling class** with tests:
       - test_timeout_during_streaming: Mock timeout during stream, verify recovery
       - test_rate_limit_exceeded: Mock rate limit, verify backoff/retry
       - test_invalid_api_key: Mock 401 error, verify proper error message
       - test_network_error: Mock connection error, verify retry
       - test_malformed_response: Mock invalid JSON, verify error handling
       - test_empty_response: Mock empty response, verify graceful handling
       - test_partial_response: Mock incomplete stream, verify recovery

    2. **Test edge cases**:
       - test_very_long_prompt: Test with 100K+ tokens, verify truncation
       - test_very_short_prompt: Test with 1 token, verify handling
       - test_special_characters: Test with unicode, emojis, control characters
       - test_concurrent_requests: Test multiple simultaneous requests
       - test_quota_exceeded_mid_stream: Mock quota error during streaming

    3. **Test cache edge cases**:
       - test_cache_collision: Different queries with same hash
       - test_cache_expiration_during_request: TTL expires mid-request
       - test_cache_with_special_characters: Test cache key with unicode

    4. **Test context window edge cases**:
       - test_exact_context_window_match: Prompt exactly at limit
       - test_context_window_with_system_message: System + user at limit
       - test_multiturn_context_window: Accumulated context exceeds limit

    5. **Use MockAsyncIterator for streaming tests**:
       - Reuse MockAsyncIterator pattern from plan 156-05
       - Simulate partial chunks, errors, timeouts during stream

    Pattern from VERIFICATION.md:
    "Missing: Edge case coverage (timeouts, rate limits, failures)"
    "Add tests for streaming interruption and recovery"

    Add ~17 new tests to test_llm_coverage_part2.py
  </action>
  <verify>
    pytest backend/tests/integration/services/test_llm_coverage_part2.py::TestErrorHandling -v
  </verify>
  <done>
    17 new tests for error handling and edge cases, timeouts, rate limits, failures covered
  </done>
</task>

<task type="auto">
  <name>Task 3: Add cache invalidation and streaming recovery tests</name>
  <files>backend/tests/integration/services/test_llm_coverage_part2.py</files>
  <action>
    Add tests for cache invalidation and streaming recovery:

    1. **Create TestCacheInvalidation class** with tests:
       - test_cache_key_generation: Verify consistent keys for same query
       - test_cache_hit_exact_match: Exact query cache hit
       - test_cache_miss_similar_query: Similar query cache miss
       - test_cache_invalidation_after_update: Invalidate when provider changes
       - test_cache_ttl_expiration: Verify TTL enforcement
       - test_cache_size_limit: Verify LRU eviction when full
       - test_cache_with_different_providers: Same query, different providers

    2. **Test cache-aware routing**:
       - test_cache_hit_returns_cached_response: Verify cached response returned
       - test_cache_miss_calls_provider: Verify provider called on miss
       - test_cache_warming_on_startup: Verify cache populated on startup
       - test_cache_statistics_tracking: Verify hit/miss metrics tracked

    3. **Create TestStreamingRecovery class** with tests:
       - test_stream_interruption_and_resume: Mock interruption, verify resume
       - test_stream_timeout_recovery: Mock timeout, verify retry from last chunk
       - test_stream_partial_chunk_recovery: Mock incomplete chunk, verify recovery
       - test_stream_error_mid_response: Mock error mid-stream, verify handling
       - test_stream_connection_dropped: Mock connection drop, verify reconnect

    4. **Test streaming edge cases**:
       - test_stream_with_very_long_response: 100K+ tokens streamed
       - test_stream_with_special_characters: Unicode, emojis in stream
       - test_stream_with_function_calls: Verify function calling in stream
       - test_stream_with_tool_use: Verify tool use in stream

    5. **Mock provider clients for streaming**:
       - Use AsyncMock for stream() methods
       - Simulate chunked responses with MockAsyncIterator
       - Simulate errors at different points in stream

    Pattern from VERIFICATION.md:
    "Missing: Cache invalidation and TTL expiration"
    "Missing: Streaming interruption and recovery"

    Add ~14 new tests to test_llm_coverage_part2.py
  </action>
  <verify>
    pytest backend/tests/integration/services/test_llm_coverage_part2.py::TestCacheInvalidation -v
    pytest backend/tests/integration/services/test_llm_coverage_part2.py::TestStreamingRecovery -v
  </verify>
  <done>
    14 new tests for cache invalidation and streaming recovery, TTL tested, recovery logic verified
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Run expanded LLM test suites:
   ```bash
   pytest backend/tests/integration/services/test_llm_coverage_part1.py -v
   pytest backend/tests/integration/services/test_llm_coverage_part2.py -v
   ```
   Expected: 135+ tests passing (up from 104)

2. Run combined coverage report:
   ```bash
   cd backend
   pytest tests/integration/services/test_llm_coverage_part1.py tests/integration/services/test_llm_coverage_part2.py --cov=core.llm.byok_handler --cov-report=term-missing
   ```
   Expected: 70%+ coverage for byok_handler.py (up from 37%)

3. Verify specific paths covered:
   ```bash
   # Check that provider-specific methods are covered
   grep -E "_call_(openai|anthropic|deepseek|gemini|moonshot)" backend/core/llm/byok_handler.py
   # Verify tests exist for each provider
   pytest backend/tests/integration/services/test_llm_coverage_part1.py -k "provider" -v
   ```
</verification>

<success_criteria>
- 46 new tests added (15 + 17 + 14)
- Total LLM tests: 150 (up from 104)
- Provider-specific paths covered for all 6 providers
- Error handling and edge cases covered
- Cache invalidation and TTL tested
- Streaming interruption and recovery tested
- LLM service coverage 70%+ (up from 37%)
- All new tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/156-core-services-coverage-high-impact/156-09-SUMMARY.md`
</output>
