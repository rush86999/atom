---
phase: 156-core-services-coverage-high-impact
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/services/test_byok_handler_coverage.py
  - backend/tests/integration/services/conftest.py
  - backend/tests/integration/services/pytest.ini
autonomous: true
user_setup: []

must_haves:
  truths:
    - "LLM service coverage expanded to 80% (BYOK handler, token counting, rate limiting, streaming)"
    - "Provider routing tested for all complexity levels (SIMPLE, MODERATE, COMPLEX, ADVANCED)"
    - "Token counting and cost estimation validated across providers (OpenAI, Anthropic, DeepSeek)"
    - "Rate limiting tested with quota enforcement and window reset"
    - "Streaming response handling tested with chunked responses"
    - "Context window management tested with truncation logic"
    - "Cache-aware routing tested with cache hit/miss scenarios"
  artifacts:
    - path: "backend/tests/integration/services/test_byok_handler_coverage.py"
      provides: "80%+ coverage for BYOKHandler"
      min_lines: 600
      exports: ["TestProviderRouting", "TestTokenCounting", "TestRateLimiting", "TestStreamingResponses", "TestContextWindowManagement", "TestCacheAwareRouting", "TestModelSelection"]
    - path: "backend/tests/integration/services/conftest.py"
      provides: "Shared fixtures (db_session, byok_handler)"
      min_lines: 60
    - path: "backend/tests/integration/services/pytest.ini"
      provides: "Isolated test configuration"
      min_lines: 10
  key_links:
    - from: "test_byok_handler_coverage.py"
      to: "core/llm/byok_handler.py"
      via: "import BYOKHandler"
      pattern: "from core.llm.byok_handler import BYOKHandler"
    - from: "test_byok_handler_coverage.py"
      to: "core/llm/cognitive_tier_system.py"
      via: "cognitive tier routing tests"
      pattern: "CognitiveTier|CognitiveClassifier"
    - from: "test_byok_handler_coverage.py"
      to: "OpenAI/Anthropic/DeepSeek APIs"
      via: "mocked provider responses"
      pattern: "AsyncMock.*generate|patch.*openai|patch.*anthropic"
---

<objective>
Expand test coverage for LLM Service (BYOK Handler) to 80% through comprehensive testing of provider routing, token counting, rate limiting, streaming, and cache-aware optimization.

Purpose: BYOKHandler is the core LLM routing layer that manages multi-provider requests, cost optimization, and rate limiting. High coverage ensures LLM routing is reliable, cost-effective, and performant.

Output: 600+ lines of test code covering all LLM service code paths with mocked provider APIs (no real LLM calls).
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-core-services-coverage-high-impact/156-RESEARCH.md
@backend/core/llm/byok_handler.py
@backend/core/llm/cognitive_tier_system.py
@backend/tests/test_byok_handler_integration.py
@backend/docs/COGNITIVE_TIER_SYSTEM.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update shared fixtures for BYOK handler testing</name>
  <files>
    backend/tests/integration/services/conftest.py
  </files>
  <action>
    Update `backend/tests/integration/services/conftest.py` with BYOK fixtures:

    1. Add `byok_handler` fixture:
       - Create BYOKHandler instance with test workspace_id
       - Mock environment variables for API keys
       - Return handler instance

    2. Add `mock_llm_providers` fixture:
       - Mock OpenAI client with AsyncMock
       - Mock Anthropic client with AsyncMock
       - Mock DeepSeek client with AsyncMock
       - Configure mock responses for generate() and stream() methods

    3. Add `mock_rate_limiter` fixture:
       - Mock Redis rate limiter with fakeredis
       - Configure rate limit windows and quotas

    Update existing conftest.py (don't recreate).
    Use unittest.mock.patch for provider mocking.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/conftest.py::byok_handler --collect-only`
    Verify fixture is discoverable and returns BYOKHandler instance.
  </verify>
  <done>
    BYOK handler fixtures added to shared conftest, ready for all LLM service tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test provider routing and complexity classification</name>
  <files>
    backend/tests/integration/services/test_byok_handler_coverage.py
  </files>
  <action>
    Create `backend/tests/integration/services/test_byok_handler_coverage.py` with routing tests:

    Class: `TestProviderRouting`

    1. `test_query_complexity_classification` (parametrize):
       - Test prompts: "What is 2+2?" (SIMPLE), "Summarize this" (MODERATE), "Analyze economic impact" (COMPLEX), "Write REST API" (ADVANCED)
       - Verify analyze_query_complexity() returns correct QueryComplexity enum
       - Use @pytest.mark.parametrize with 4 test cases

    2. `test_provider_selection_for_complexity`:
       - SIMPLE query → budget tier provider
       - MODERATE query → standard tier provider
       - COMPLEX query → premium tier provider
       - ADVANCED query → ultra tier provider
       - Verify get_routing_info() returns correct provider tier

    3. `test_provider_selection_for_task_type`:
       - Code task → code-specialized provider
       - Chat task → chat-optimized provider
       - Analysis task → high-context provider
       - Verify task_type influences provider selection

    4. `test_cognitive_tier_routing`:
       - MICRO tier → cheapest models (MiniMax, deepseek-chat)
       - STANDARD tier → balanced models (gpt-4o-mini, claude-3-haiku)
       - COMPLEX tier → premium models (gpt-4o, claude-4-opus)
       - Verify cognitive_tier parameter overrides complexity-based routing

    Use parametrize for efficiency.
    Test business logic, not framework code.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_byok_handler_coverage.py::TestProviderRouting -v`
    Verify all routing tests pass.
    Check coverage: `pytest --cov=core.llm.byok_handler --cov-report=term-missing`
  </verify>
  <done>
    Provider routing tested for all complexity levels, task types, and cognitive tiers with correct tier assignment.
  </done>
</task>

<task type="auto">
  <name>Task 3: Test token counting and cost estimation</name>
  <files>
    backend/tests/integration/services/test_byok_handler_coverage.py
  </files>
  <action>
    Add token counting tests to `test_byok_handler_coverage.py`:

    Class: `TestTokenCounting`

    1. `test_count_tokens` (parametrize):
       - Test inputs: "Hello world" (1-5 tokens), "The quick brown fox..." (10-15 tokens), "Write Python function..." (15-25 tokens)
       - Verify count_tokens() returns expected range
       - Use @pytest.mark.parametrize with 3 test cases

    2. `test_estimate_cost_by_provider`:
       - Prompt: "This is a test prompt with multiple tokens"
       - Max tokens: 100
       - Providers: openai, anthropic, deepseek
       - Verify estimate_cost() returns costs for all providers
       - Verify DeepSeek < OpenAI (cost comparison)

    3. `test_estimate_cost_with_cache_hit`:
       - Mock cache hit (prompt already cached)
       - Verify cost is $0.00 for cached prompt
       - Verify cache hit reduces token count

    4. `test_token_counting_for_images`:
       - Test image token counting (if supported)
       - Verify image dimensions affect token count
       - Verify image resolution scaling

    Import from research: Use token counting libraries (tiktoken for OpenAI, anthropic for Anthropic).
    Mock cache responses for cost estimation tests.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_byok_handler_coverage.py::TestTokenCounting -v`
    Verify all token counting tests pass.
    Check coverage covers count_tokens and estimate_cost methods.
  </verify>
  <done>
    Token counting and cost estimation tested across providers with cache hit validation.
  </done>
</task>

<task type="auto">
  <name>Task 4: Test rate limiting and quota enforcement</name>
  <files>
    backend/tests/integration/services/test_byok_handler_coverage.py
  </files>
  <action>
    Add rate limiting tests to `test_byok_handler_coverage.py`:

    Class: `TestRateLimiting`

    1. `test_rate_limiting_by_user`:
       - Create user_id with rate limit 10 requests/minute
       - Make 10 requests - all should be allowed
       - Mock request count = 1000 (exceeds limit)
       - Call check_rate_limit() - should return allowed=False
       - Verify retry_after present in response

    2. `test_quota_enforcement`:
       - Create workspace with monthly quota 500K tokens
       - Mock monthly usage = 1M tokens (exceeds quota)
       - Call check_quota() - should return exceeded=True
       - Verify quota reset date calculated

    3. `test_rate_limit_reset_after_window`:
       - Mock last_request_time = 2 hours ago
       - Call check_rate_limit() - should be allowed (window expired)
       - Verify rate limit reset

    4. `test_quota_enforcement_with_tier_multiplier`:
       - Test different quota tiers (free, pro, enterprise)
       - Verify tier multiplier applied correctly
       - Test quota upgrades mid-month

    Use fakeredis for rate limiter (no real Redis).
    Mock time with freezegun for deterministic testing.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_byok_handler_coverage.py::TestRateLimiting -v`
    Verify all rate limiting tests pass.
    Check coverage covers check_rate_limit and check_quota methods.
  </verify>
  <done>
    Rate limiting tested with quota enforcement, window reset, and tier multipliers.
  </done>
</task>

<task type="auto">
  <name>Task 5: Test streaming responses and context window management</name>
  <files>
    backend/tests/integration/services/test_byok_handler_coverage.py
  </files>
  <action>
    Add streaming and context window tests to `test_byok_handler_coverage.py`:

    Class: `TestStreamingResponses`

    1. `test_streaming_response_chunks` (async):
       - Mock provider stream with chunks: ["Hello", " world", "!", None]
       - Iterate over stream_response()
       - Verify chunks received in correct order
       - Verify stream ends with None

    2. `test_streaming_with_token_tracking` (async):
       - Mock stream with 3 chunks
       - Track tokens during streaming
       - Verify token count incremented per chunk
       - Verify final token count

    3. `test_streaming_error_handling` (async):
       - Mock stream raises exception mid-stream
       - Verify exception caught and handled
       - Verify partial response returned

    Class: `TestContextWindowManagement`

    4. `test_get_context_window` (parametrize):
       - Models: gpt-4 (8192), claude-3-opus (200000), deepseek-chat (16384)
       - Verify get_context_window() returns correct size
       - Use @pytest.mark.parametrize with 3 test cases

    5. `test_truncate_to_context_window`:
       - Create prompt with 100K tokens (very long)
       - Call truncate_to_window(model, max_tokens=8192)
       - Verify truncated prompt fits within window
       - Verify count_tokens(truncated) <= max_tokens

    6. `test_context_window_with_system_message`:
       - Add system message (1000 tokens)
       - Add user message (7000 tokens)
       - Verify total < 8192 (gpt-4 window)
       - Verify system message prioritized

    Use AsyncMock for streaming tests.
    Use pytest-asyncio for async test execution.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_byok_handler_coverage.py::TestStreamingResponses -v`
    Run `pytest backend/tests/integration/services/test_byok_handler_coverage.py::TestContextWindowManagement -v`
    Verify all streaming and context window tests pass.
    Check coverage covers stream_response and context window methods.
  </verify>
  <done>
    Streaming response handling tested with chunked delivery, token tracking, and error handling. Context window management tested with truncation logic.
  </done>
</task>

<task type="auto">
  <name>Task 6: Test cache-aware routing and model selection</name>
  <files>
    backend/tests/integration/services/test_byok_handler_coverage.py
  </files>
  <action>
    Add cache and model selection tests to `test_byok_handler_coverage.py`:

    Class: `TestCacheAwareRouting`

    1. `test_cache_hit_returns_early`:
       - First call: mock provider response, verify provider called
       - Second call with same prompt: verify provider NOT called (cache hit)
       - Verify cache hit reduces cost and latency

    2. `test_cache_key_generation`:
       - Prompt 1 and Prompt 2 (same): verify same cache key
       - Prompt 3 (different): verify different cache key
       - Verify cache key includes model and temperature

    3. `test_cache_invalidation_on_parameter_change`:
       - Generate with temperature=0.7
       - Generate with temperature=0.9 (same prompt)
       - Verify cache miss (different parameters)
       - Verify new cache entry created

    Class: `TestModelSelection`

    4. `test_select_model_for_complexity` (parametrize):
       - openai + SIMPLE → o4-mini
       - openai + ADVANCED → o3
       - anthropic + MODERATE → claude-3-haiku
       - anthropic + ADVANCED → claude-4-opus
       - deepseek + SIMPLE → deepseek-chat
       - deepseek + ADVANCED → deepseek-v3.2-speciale
       - Use @pytest.mark.parametrize with 6 test cases

    5. `test_fallback_on_model_unavailable`:
       - Mock model availability check = False
       - Call select_model()
       - Verify fallback model returned
       - Verify fallback logged

    6. `test_model_selection_with_cache_preference`:
       - Enable cache preference mode
       - Select model with caching support (claude-4-opus)
       - Verify caching capability influences selection

    Mock cache with in-memory dict for tests.
    Use parametrize for model selection matrix.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_byok_handler_coverage.py::TestCacheAwareRouting -v`
    Run `pytest backend/tests/integration/services/test_byok_handler_coverage.py::TestModelSelection -v`
    Verify all cache and model selection tests pass.
    Check coverage covers cache-aware routing and model selection methods.
  </verify>
  <done>
    Cache-aware routing tested with hit/miss scenarios and cache key generation. Model selection tested for all providers and complexities with fallback logic.
  </done>
</task>

</tasks>

<verification>
Overall phase verification steps:
1. Run full BYOK test suite: `pytest backend/tests/integration/services/test_byok_handler_coverage.py -v`
2. Verify 40+ tests passing (routing, token counting, rate limiting, streaming, context window, cache, model selection)
3. Check coverage report: `pytest --cov=core.llm.byok_handler --cov-report=html`
4. Open htmlcov/index.html and verify byok_handler.py at 80%+ coverage
5. Verify all code paths covered:
   - Provider routing for all complexity levels and task types
   - Token counting and cost estimation
   - Rate limiting and quota enforcement
   - Streaming response handling
   - Context window management and truncation
   - Cache-aware routing with hit/miss scenarios
   - Model selection with fallback logic
</verification>

<success_criteria>
1. 40+ LLM service tests passing (100% pass rate)
2. BYOK handler coverage at 80%+ (verified via coverage report)
3. Provider routing tested for all complexity levels
4. Token counting and cost estimation validated
5. Rate limiting tested with quota enforcement
6. Streaming responses tested with chunked delivery
7. Context window management tested with truncation
8. Cache-aware routing tested with cache hit/miss
9. Model selection tested with fallback logic
10. Zero external dependencies (all provider APIs mocked)
</success_criteria>

<output>
After completion, create `.planning/phases/156-core-services-coverage-high-impact/156-02-SUMMARY.md`
</output>
