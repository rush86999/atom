---
phase: 156-core-services-coverage-high-impact
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/services/test_llm_coverage_part1.py
  - backend/tests/integration/services/conftest.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Provider routing tested for all 4 complexity levels (SIMPLE, MODERATE, COMPLEX, ADVANCED)"
    - "Cognitive tier routing tested for all 5 tiers (MICRO, STANDARD, VERSATILE, HEAVY, COMPLEX)"
    - "Token counting validated across providers (tiktoken for OpenAI, anthropic for Anthropic)"
    - "Cost estimation tested with provider comparison (DeepSeek < OpenAI)"
    - "Coverage verified: byok_handler.py specific methods at 80%+"
  artifacts:
    - path: "backend/tests/integration/services/test_llm_coverage_part1.py"
      provides: "BYOK routing and token counting coverage"
      min_lines: 300
      exports: ["TestProviderRouting", "TestCognitiveTierRouting", "TestTokenCounting"]
    - path: "backend/tests/integration/services/conftest.py"
      provides: "byok_handler fixture"
      min_lines: 60
  key_links:
    - from: "test_llm_coverage_part1.py"
      to: "core/llm/byok_handler.py"
      via: "test routing logic"
      pattern: "analyze_query_complexity|get_routing_info"
    - from: "test_llm_coverage_part1.py"
      to: "core/llm/cognitive_tier_system.py"
      via: "cognitive tier tests"
      pattern: "CognitiveTier|CognitiveClassifier"
---

<objective>
Expand test coverage for LLM Service Part 1: Provider routing, cognitive tier routing, and token counting.

Purpose: BYOKHandler routes requests to appropriate LLM providers based on query complexity and cognitive tier. High coverage ensures routing is reliable and cost-effective.

Output: 300+ lines of test code covering routing logic with mocked provider APIs.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-core-services-coverage-high-impact/156-RESEARCH.md
@backend/core/llm/byok_handler.py
@backend/core/llm/cognitive_tier_system.py
@backend/tests/test_byok_handler_integration.py
@backend/docs/COGNITIVE_TIER_SYSTEM.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BYOK fixtures to conftest.py (append-only)</name>
  <files>
    backend/tests/integration/services/conftest.py
  </files>
  <action>
    APPEND to `backend/tests/integration/services/conftest.py` with BYOK fixtures (don't modify existing):

    1. Add `byok_handler` fixture:
       - Create BYOKHandler instance with test workspace_id
       - Mock environment variables for API keys
       - Return handler instance

    Use APPEND-ONLY pattern - no modifications to existing fixtures.
    All plans append their fixtures to avoid merge conflicts.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/conftest.py::byok_handler --collect-only`
    Verify fixture is discoverable.
  </verify>
  <done>
    BYOK handler fixture added to conftest.py without modifying existing fixtures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Test provider routing for complexity levels</name>
  <files>
    backend/tests/integration/services/test_llm_coverage_part1.py
  </files>
  <action>
    Create `backend/tests/integration/services/test_llm_coverage_part1.py`:

    Class: `TestProviderRouting`

    1. `test_query_complexity_classification` (parametrize):
       - Test 4 prompts spanning SIMPLE to ADVANCED complexity
       - Verify analyze_query_complexity() returns correct QueryComplexity enum
       - Use @pytest.mark.parametrize

    2. `test_provider_selection_for_complexity`:
       - Verify SIMPLE→budget, MODERATE→standard, COMPLEX→premium, ADVANCED→ultra
       - Test get_routing_info() returns correct tier

    3. `test_provider_selection_for_task_type`:
       - Code→code provider, chat→chat provider, analysis→high-context provider

    Class: `TestCognitiveTierRouting`

    4. `test_cognitive_tier_routing` (parametrize):
       - Test all 5 cognitive tiers (MICRO, STANDARD, VERSATILE, HEAVY, COMPLEX)
       - Verify tier parameter overrides complexity-based routing

    Use parametrize for efficiency. Test business logic only.
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_llm_coverage_part1.py::TestProviderRouting -v`
    Run `pytest backend/tests/integration/services/test_llm_coverage_part1.py::TestCognitiveTierRouting -v`
    Verify all tests pass.
  </verify>
  <done>
    Provider routing tested for all 4 complexity levels and 5 cognitive tiers.
  </done>
</task>

<task type="auto">
  <name>Task 3: Test token counting and cost estimation</name>
  <files>
    backend/tests/integration/services/test_llm_coverage_part1.py
  </files>
  <action>
    Add to `test_llm_coverage_part1.py`:

    Class: `TestTokenCounting`

    1. `test_count_tokens` (parametrize):
       - Test short (1-5 tokens), medium (10-15 tokens), long (15-25 tokens) inputs
       - Verify count_tokens() returns expected range

    2. `test_estimate_cost_by_provider`:
       - Compare OpenAI, Anthropic, DeepSeek costs for same prompt
       - Verify DeepSeek < OpenAI

    3. `test_estimate_cost_with_cache_hit`:
       - Mock cache hit
       - Verify cost is $0.00 for cached prompt

    Use token counting libraries (tiktoken, anthropic).
  </action>
  <verify>
    Run `pytest backend/tests/integration/services/test_llm_coverage_part1.py::TestTokenCounting -v`
    Verify all token counting tests pass.
  </verify>
  <done>
    Token counting and cost estimation tested across providers.
  </done>
</task>

</tasks>

<verification>
Overall verification:
1. Run `pytest backend/tests/integration/services/test_llm_coverage_part1.py -v`
2. Verify 15+ tests passing (routing, cognitive tier, token counting)
3. Check coverage: `pytest --cov=core.llm.byok_handler --cov-report=term-missing --cov-report=html`
4. Verify coverage report shows byok_handler.py at 40%+ (this is part 1 of 2)
5. Confirm tests exercise production code: analyze_query_complexity, get_routing_info, count_tokens, estimate_cost
</verification>

<success_criteria>
1. 15+ tests passing (100% pass rate)
2. Routing methods covered: analyze_query_complexity, get_routing_info
3. Token counting covered: count_tokens, estimate_cost
4. All 4 complexity levels tested
5. All 5 cognitive tiers tested
6. Cost comparison validated (DeepSeek < OpenAI)
7. Zero external dependencies (all mocked)
</success_criteria>

<output>
After completion, create `.planning/phases/156-core-services-coverage-high-impact/156-02-SUMMARY.md`
</output>
