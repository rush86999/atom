---
phase: 187-property-based-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/property_tests/governance/test_governance_invariants_extended.py
  - backend/tests/property_tests/governance/test_trigger_interceptor_invariants.py
  - backend/tests/property_tests/governance/test_rate_limit_invariants.py
  - backend/tests/property_tests/governance/test_audit_trail_invariants.py
  - backend/tests/property_tests/governance/test_concurrent_maturity_invariants.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Governance rate limit enforcement respects token/minute and request/minute bounds"
    - "Audit trail completeness ensures all governed actions are logged"
    - "Concurrent maturity transitions don't cause race conditions"
    - "Trigger interceptor blocks STUDENT agents from automated triggers"
    - "Confidence score bounds [0.0, 1.0] maintained across all operations"
    - "Maturity-action complexity matrix enforced consistently"
  artifacts:
    - path: "backend/tests/property_tests/governance/test_rate_limit_invariants.py"
      provides: "Rate limit enforcement property tests"
      min_lines: 600
      contains: "test_rate_limit_token_invariant", "test_rate_limit_request_invariant"
    - path: "backend/tests/property_tests/governance/test_audit_trail_invariants.py"
      provides: "Audit trail completeness property tests"
      min_lines: 500
      contains: "test_audit_action_logged_invariant", "test_audit_ordering_invariant"
    - path: "backend/tests/property_tests/governance/test_concurrent_maturity_invariants.py"
      provides: "Concurrent maturity transition property tests"
      min_lines: 400
      contains: "test_maturity_transition_race_invariant", "test_maturity_rollback_invariant"
    - path: "backend/tests/property_tests/governance/test_trigger_interceptor_invariants.py"
      provides: "Trigger interceptor routing property tests"
      min_lines: 500
      contains: "test_student_blocked_auto_trigger_invariant", "test_maturity_routing_invariant"
  key_links:
    - from: "backend/tests/property_tests/governance/test_trigger_interceptor_invariants.py"
      to: "backend/core/trigger_interceptor.py"
      via: "import TriggerInterceptor, MaturityLevel"
      pattern: "from core.trigger_interceptor import"
    - from: "backend/tests/property_tests/governance/test_rate_limit_invariants.py"
      to: "backend/core/governance_cache.py"
      via: "rate limit checks via cache"
      pattern: "governance_cache.*rate"
    - from: "backend/tests/property_tests/governance/test_audit_trail_invariants.py"
      to: "backend/core/models.py"
      via: "AgentOperationTracker model"
      pattern: "AgentOperationTracker"
---

<objective>
Extend governance property-based tests to achieve 80%+ coverage on governance invariants.

**Purpose:** Property-based testing for governance ensures that critical safety invariants (confidence bounds, maturity routing, rate limits, audit trails) hold across all possible inputs, not just hand-picked examples. This prevents governance bypasses and unauthorized agent actions.

**Output:** ~100 new property-based tests across 5 test files, 80%+ invariant coverage on governance services.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/187-property-based-testing/187-RESEARCH.md
@backend/tests/property_tests/README.md
@backend/tests/property_tests/governance/test_governance_invariants_extended.py
@backend/tests/property_tests/governance/GOVERNANCE_INVARIANTS.md
@backend/core/trigger_interceptor.py
@backend/core/governance_cache.py
@backend/core/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rate limit enforcement invariants</name>
  <files>backend/tests/property_tests/governance/test_rate_limit_invariants.py</files>
  <action>
    Create `test_rate_limit_invariants.py` with property-based tests for rate limit enforcement.

    **Test classes to create:**

    1. **TestRateLimitTokenInvariants** (300 lines)
       - `test_rate_limit_token_bounds_invariant`: For any token count (1-10000), rate limit never exceeds configured max
       - `test_rate_limit_token_reset_invariant`: After time window expires, token count resets correctly
       - `test_rate_limit_token_increment_invariant`: Each request increments token count by 1
       - `test_rate_limit_token_undershoot_invariant`: Token count never goes below 0

    2. **TestRateLimitRequestInvariants** (300 lines)
       - `test_rate_limit_request_bounds_invariant`: For any request rate (1-1000/min), rate limit enforced
       - `test_rate_limit_request_sliding_window_invariant`: Sliding window correctly tracks requests
       - `test_rate_limit_request_burst_invariant`: Burst requests don't exceed sustained rate

    **Use Hypothesis strategies:**
    - `st.integers(min_value=1, max_value=10000)` for token counts
    - `st.integers(min_value=1, max_value=1000)` for request rates
    - `st.timedeltas(min_value=timedelta(seconds=1), max_value=timedelta(hours=1))` for time windows

    **Settings:** `@settings(max_examples=200, suppress_health_check=[HealthCheck.function_scoped_fixture])`

    **Follow existing pattern from:** `test_governance_invariants_extended.py`

    **DO NOT:** Create actual rate limit implementation (tests only)
  </action>
  <verify>
    Run: `pytest backend/tests/property_tests/governance/test_rate_limit_invariants.py -v --hypothesis-max-examples=50`
    Expected: All tests pass with at least 50 examples each
  </verify>
  <done>
    Rate limit enforcement invariants cover:
    - Token count bounds [0, max_tokens]
    - Request rate bounds [0, max_requests/min]
    - Time window reset behavior
    - Sliding window correctness
  </done>
</task>

<task type="auto">
  <name>Task 2: Create audit trail completeness invariants</name>
  <files>backend/tests/property_tests/governance/test_audit_trail_invariants.py</files>
  <action>
    Create `test_audit_trail_invariants.py` with property-based tests for audit trail completeness.

    **Test classes to create:**

    1. **TestAuditTrailLoggingInvariants** (250 lines)
       - `test_audit_action_logged_invariant`: For any governed action, audit entry created
       - `test_audit_required_fields_invariant`: All audit entries have required fields (agent_id, action, timestamp)
       - `test_audit_timestamp_monotonic_invariant`: Audit timestamps are monotonically increasing
       - `test_audit_action_categorization_invariant`: Every action maps to valid category

    2. **TestAuditTrailRetrievalInvariants** (250 lines)
       - `test_audit_retrieval_ordering_invariant`: Retrieved audit logs are time-ordered
       - `test_audit_retrieval_filtering_invariant`: Filtered results match filter criteria
       - `test_audit_pagination_invariant`: Paginated results have no duplicates or gaps

    **Use Hypothesis strategies:**
    - `st.sampled_from()` for action types (create, read, update, delete, execute)
    - `st.text(min_size=1, max_size=50)` for agent names
    - `st.integers(min_value=1, max_value=100)` for pagination sizes

    **Settings:** `@settings(max_examples=100, suppress_health_check=[HealthCheck.function_scoped_fixture])`

    **Follow existing pattern from:** `test_governance_cache_invariants.py`

    **DO NOT:** Modify AgentOperationTracker model (tests only)
  </action>
  <verify>
    Run: `pytest backend/tests/property_tests/governance/test_audit_trail_invariants.py -v --hypothesis-max-examples=50`
    Expected: All tests pass with audit trail completeness verified
  </verify>
  <done>
    Audit trail completeness invariants cover:
    - All governed actions logged
    - Required fields present
    - Timestamp ordering maintained
    - Retrieval ordering and filtering correct
  </done>
</task>

<task type="auto">
  <name>Task 3: Create concurrent maturity transition invariants</name>
  <files>backend/tests/property_tests/governance/test_concurrent_maturity_invariants.py</files>
  <action>
    Create `test_concurrent_maturity_invariants.py` with property-based tests for concurrent maturity transitions.

    **Test classes to create:**

    1. **TestConcurrentMaturityInvariants** (200 lines)
       - `test_maturity_transition_race_invariant`: Concurrent maturity updates don't corrupt state
       - `test_maturity_rollback_invariant`: Failed transition rolls back to original maturity
       - `test_maturity_no_regression_invariant`: Maturity never decreases (STUDENT < INTERN < SUPERVISED < AUTONOMOUS)

    2. **TestMaturityStateConsistency** (200 lines)
       - `test_maturity_cache_consistency_invariant`: Cache and DB agree on maturity after concurrent updates
       - `test_maturity_permission_consistency_invariant`: Permissions update atomically with maturity

    **Use Hypothesis strategies:**
    - `st.sampled_from([AgentStatus.STUDENT, AgentStatus.INTERN, AgentStatus.SUPERVISED, AgentStatus.AUTONOMOUS])` for maturity levels
    - `st.integers(min_value=1, max_value=10)` for number of concurrent transitions

    **Settings:** `@settings(max_examples=100, suppress_health_check=[HealthCheck.function_scoped_fixture])`

    **Use threading for concurrent operations:**
    ```python
    import threading
    def transition_maturity(agent_id, new_maturity):
        # Simulate concurrent maturity transition
        ...

    threads = [threading.Thread(target=transition_maturity, args=(agent_id, maturity)) for maturity in maturities]
    for t in threads: t.start()
    for t in threads: t.join()
    ```

    **Follow existing pattern from:** `test_governance_maturity_invariants.py`

    **DO NOT:** Modify maturity transition logic (tests only)
  </action>
  <verify>
    Run: `pytest backend/tests/property_tests/governance/test_concurrent_maturity_invariants.py -v --hypothesis-max-examples=50`
    Expected: All tests pass, no race conditions detected
  </verify>
  <done>
    Concurrent maturity invariants cover:
    - No state corruption from concurrent updates
    - Rollback on failed transitions
    - No maturity regression
    - Cache consistency after concurrent updates
  </done>
</task>

<task type="auto">
  <name>Task 4: Create trigger interceptor routing invariants</name>
  <files>backend/tests/property_tests/governance/test_trigger_interceptor_invariants.py</files>
  <action>
    Create `test_trigger_interceptor_invariants.py` with property-based tests for trigger interceptor routing.

    **Test classes to create:**

    1. **TestTriggerInterceptorRoutingInvariants** (250 lines)
       - `test_student_blocked_auto_trigger_invariant`: STUDENT agents always blocked from automated triggers
       - `test_maturity_routing_invariant`: Routing decision matches maturity level (STUDENT->training, INTERN->proposal, SUPERVISED->supervision, AUTONOMOUS->execute)
       - `test_trigger_confidence_threshold_invariant`: Triggers below confidence threshold blocked

    2. **TestTriggerInterceptorStateInvariants** (250 lines)
       - `test_blocked_trigger_context_invariant`: Blocked triggers create BlockedTriggerContext entries
       - `test_proposal_creation_invariant`: INTERN triggers create AgentProposal entries
       - `test_supervision_session_invariant`: SUPERVISED triggers create SupervisionSession entries

    **Use Hypothesis strategies:**
    - `st.sampled_from([MaturityLevel.STUDENT, MaturityLevel.INTERN, MaturityLevel.SUPERVISED, MaturityLevel.AUTONOMOUS])`
    - `st.floats(min_value=0.0, max_value=1.0, allow_nan=False, allow_infinity=False)` for confidence scores
    - `st.sampled_from([1, 2, 3, 4])` for action complexity

    **Settings:** `@settings(max_examples=200, suppress_health_check=[HealthCheck.function_scoped_fixture])`

    **Follow existing pattern from:** `test_governance_invariants_extended.py`

    **Import and test:**
    ```python
    from core.trigger_interceptor import TriggerInterceptor, MaturityLevel
    from core.models import BlockedTriggerContext, AgentProposal, SupervisionSession
    ```

    **DO NOT:** Modify TriggerInterceptor implementation (tests only)
  </action>
  <verify>
    Run: `pytest backend/tests/property_tests/governance/test_trigger_interceptor_invariants.py -v --hypothesis-max-examples=50`
    Expected: All routing invariants hold, STUDENT always blocked
  </verify>
  <done>
    Trigger interceptor invariants cover:
    - STUDENT agents blocked from automated triggers
    - Routing matches maturity level matrix
    - Confidence threshold enforcement
    - Correct context/proposal/session creation
  </done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. **Test Execution:**
   ```bash
   pytest backend/tests/property_tests/governance/ -v -k "rate_limit or audit_trail or concurrent_maturity or trigger_interceptor" --hypothesis-max-examples=50
   ```
   Expected: All new tests pass

2. **Coverage Measurement:**
   ```bash
   pytest backend/tests/property_tests/governance/ --cov=core.agent_governance_service --cov=core.trigger_interceptor --cov=core.governance_cache --cov-report=term-missing
   ```
   Expected: 80%+ coverage on governance services

3. **Test Count:**
   ```bash
   grep -r "def test_" backend/tests/property_tests/governance/test_rate_limit_invariants.py backend/tests/property_tests/governance/test_audit_trail_invariants.py backend/tests/property_tests/governance/test_concurrent_maturity_invariants.py backend/tests/property_tests/governance/test_trigger_interceptor_invariants.py | wc -l
   ```
   Expected: ~100 new tests

4. **Invariant Documentation:**
   - Each test has docstring explaining the invariant
   - Critical invariants use `max_examples=200`
   - All tests use `@given` with Hypothesis strategies
</verification>

<success_criteria>
1. **Coverage Achievement:** Governance invariants achieve 80%+ property test coverage
2. **Test Quality:** All tests use `@given` with appropriate Hypothesis strategies
3. **Test Execution:** All tests pass (or document actual bugs with VALIDATED_BUG)
4. **Documentation:** Each invariant documented with clear docstring
</success_criteria>

<output>
After completion, create `.planning/phases/187-property-based-testing/187-01-SUMMARY.md` with:
- Tests created (count by file)
- Coverage metrics (governance services)
- Any invariants that failed (bugs found)
- Lines of test code added
- Duration
</output>
