---
phase: 187-property-based-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/property_tests/llm/test_token_counting_invariants.py
  - backend/tests/property_tests/llm/test_cost_calculation_invariants.py
  - backend/tests/property_tests/llm/test_cache_consistency_invariants.py
  - backend/tests/property_tests/llm/test_provider_fallback_invariants.py
  - backend/tests/property_tests/llm/test_multilingual_token_invariants.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Token counting accuracy maintained across multilingual text, emoji, and code"
    - "Cost calculation invariants hold for OpenAI, Anthropic, DeepSeek providers"
    - "Cache key consistency ensures same prompt generates same cache key"
    - "Provider fallback maintains data integrity and doesn't lose context"
    - "Tier classification respects token thresholds across all input types"
    - "Streaming invariants maintain message completeness"
  artifacts:
    - path: "backend/tests/property_tests/llm/test_token_counting_invariants.py"
      provides: "Token counting accuracy property tests"
      min_lines: 700
      contains: "test_token_count_multilingual_invariant", "test_token_count_emoji_invariant", "test_token_count_code_invariant"
    - path: "backend/tests/property_tests/llm/test_cost_calculation_invariants.py"
      provides: "Cost calculation property tests"
      min_lines: 600
      contains: "test_cost_calculation_openai_invariant", "test_cost_calculation_anthropic_invariant", "test_cost_aggregation_invariant"
    - path: "backend/tests/property_tests/llm/test_cache_consistency_invariants.py"
      provides: "Cache key consistency property tests"
      min_lines: 400
      contains: "test_cache_key_deterministic_invariant", "test_cache_lookup_consistency_invariant"
    - path: "backend/tests/property_tests/llm/test_provider_fallback_invariants.py"
      provides: "Provider fallback property tests"
      min_lines: 500
      contains: "test_fallback_state_preservation_invariant", "test_fallback_no_duplication_invariant"
    - path: "backend/tests/property_tests/llm/test_multilingual_token_invariants.py"
      provides: "Multilingual token counting property tests"
      min_lines: 400
      contains: "test_chinese_token_count_invariant", "test_arabic_token_count_invariant", "test_rtl_token_count_invariant"
  key_links:
    - from: "backend/tests/property_tests/llm/test_token_counting_invariants.py"
      to: "backend/core/llm/byok_handler.py"
      via: "import BYOKHandler"
      pattern: "from core.llm.byok_handler import"
    - from: "backend/tests/property_tests/llm/test_cost_calculation_invariants.py"
      to: "backend/core/llm/cognitive_tier_system.py"
      via: "cost calculation constants"
      pattern: "TIER_THRESHOLDS|COST_PER_TOKEN"
    - from: "backend/tests/property_tests/llm/test_cache_consistency_invariants.py"
      to: "backend/core/llm/cache_aware_router.py"
      via: "cache key generation"
      pattern: "cache_aware_router|generate_cache_key"
---

<objective>
Extend LLM property-based tests to achieve 80%+ coverage on LLM service invariants.

**Purpose:** Property-based testing for LLM services ensures that critical invariants (token counting, cost calculation, caching, provider fallback) hold across all possible inputs. This prevents billing errors, cache inconsistencies, and data loss during provider failures.

**Output:** ~100 new property-based tests across 5 test files, 80%+ invariant coverage on LLM services.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/187-property-based-testing/187-RESEARCH.md
@backend/tests/property_tests/llm/test_cognitive_tier_invariants.py
@backend/tests/property_tests/llm/STREAMING_INVARIANTS.md
@backend/core/llm/byok_handler.py
@backend/core/llm/cognitive_tier_system.py
@backend/core/llm/cache_aware_router.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create token counting accuracy invariants</name>
  <files>backend/tests/property_tests/llm/test_token_counting_invariants.py</files>
  <action>
    Create `test_token_counting_invariants.py` with property-based tests for token counting accuracy.

    **Test classes to create:**

    1. **TestTokenCountingBasicInvariants** (200 lines)
       - `test_token_count_positive_invariant`: For any non-empty text, token count >= 1
       - `test_token_count_empty_invariant`: Empty string has 0 tokens
       - `test_token_count_whitespace_invariant`: Whitespace-only text has predictable token count
       - `test_token_count_additivity_invariant`: Token count of concatenation >= sum of parts

    2. **TestTokenCountingEmojiInvariants** (200 lines)
       - `test_emoji_token_count_invariant`: Emoji have predictable token counts (typically 1-2 tokens)
       - `test_mixed_text_emoji_invariant`: Mixed text and emoji token count is sum of parts
       - `test_emoji_sequence_invariant`: Consecutive emoji don't exceed N tokens total

    3. **TestTokenCountingCodeInvariants** (300 lines)
       - `test_code_token_count_invariant`: Code tokens counted correctly (indentation, syntax)
       - `test_code_comment_invariant`: Comments included in token count
       - `test_multilingual_code_invariant`: Non-ASCII identifiers counted correctly

    **Use Hypothesis strategies:**
    - `st.text(min_size=0, max_size=10000)` for general text
    - `st.from_regex(EMOJI_PATTERN)` for emoji (define pattern for common emoji)
    - `st.text(alphabet=' \t\n{}[]();:,.<>+=-/')` for code-like text
    - `st.lists(st.sampled_from(['print', 'def', 'class', 'if', 'else', 'return']), min_size=0, max_size=100)` for code keywords

    **Settings:** `@settings(max_examples=200, suppress_health_check=[HealthCheck.function_scoped_fixture])`

    **Follow existing pattern from:** `test_token_counting_invariants.py` (extend existing)

    **DO NOT:** Modify actual token counting logic (tests only)
  </action>
  <verify>
    Run: `pytest backend/tests/property_tests/llm/test_token_counting_invariants.py -v --hypothesis-max-examples=50`
    Expected: All invariants hold for all text types
  </verify>
  <done>
    Token counting invariants cover:
    - Basic text tokenization (positive, empty, whitespace)
    - Emoji tokenization (predictable counts)
    - Code tokenization (syntax-aware)
    - Additivity properties
  </done>
</task>

<task type="auto">
  <name>Task 2: Create cost calculation invariants</name>
  <files>backend/tests/property_tests/llm/test_cost_calculation_invariants.py</files>
  <action>
    Create `test_cost_calculation_invariants.py` with property-based tests for cost calculation.

    **Test classes to create:**

    1. **TestCostCalculationOpenAI** (200 lines)
       - `test_openai_cost_positive_invariant`: For any token count, cost >= 0
       - `test_openai_cost_linear_invariant`: Cost scales linearly with token count
       - `test_openai_input_output_cost_invariant`: Input and output tokens priced separately
       - `test_openai_model_pricing_invariant`: Each model has correct pricing tier

    2. **TestCostCalculationAnthropic** (200 lines)
       - `test_anthropic_cost_positive_invariant`: For any token count, cost >= 0
       - `test_anthropic_cache_discount_invariant`: Cached tokens have discounted price
       - `test_anthropic_prompt_completion_invariant`: Prompt and completion priced separately

    3. **TestCostCalculationDeepSeek** (200 lines)
       - `test_deepseek_cost_positive_invariant`: For any token count, cost >= 0
       - `test_deepseek_pricing_invariant`: DeepSeek pricing structure applied correctly

    4. **TestCostAggregationInvariants** (200 lines)
       - `test_cost_aggregation_invariant`: Total cost = sum of individual request costs
       - `test_cost_no_overflow_invariant`: Aggregated costs don't overflow (use decimal)
       - `test_cost_currency_invariant`: All costs in USD with 6 decimal precision

    **Use Hypothesis strategies:**
    - `st.integers(min_value=1, max_value=100000)` for token counts
    - `st.sampled_from(['gpt-4', 'gpt-4-turbo', 'gpt-3.5-turbo', 'claude-3-opus', 'claude-3-sonnet', 'deepseek-chat'])` for models
    - `st.tuples(st.integers(min_value=0, max_value=100000), st.integers(min_value=0, max_value=100000))` for (input, output) token pairs

    **Settings:** `@settings(max_examples=200, suppress_health_check=[HealthCheck.function_scoped_fixture])`

    **Import and test:**
    ```python
    from core.llm.byok_handler import BYOKHandler
    from decimal import Decimal
    ```

    **DO NOT:** Modify actual cost calculation logic (tests only)
  </action>
  <verify>
    Run: `pytest backend/tests/property_tests/llm/test_cost_calculation_invariants.py -v --hypothesis-max-examples=50`
    Expected: All cost calculations accurate, no negative costs, correct linear scaling
  </verify>
  <done>
    Cost calculation invariants cover:
    - OpenAI pricing (positive, linear, input/output separate)
    - Anthropic pricing (cache discount, prompt/completion)
    - DeepSeek pricing structure
    - Cost aggregation (sum, no overflow, currency precision)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create cache consistency invariants</name>
  <files>backend/tests/property_tests/llm/test_cache_consistency_invariants.py</files>
  <action>
    Create `test_cache_consistency_invariants.py` with property-based tests for cache consistency.

    **Test classes to create:**

    1. **TestCacheKeyInvariants** (200 lines)
       - `test_cache_key_deterministic_invariant`: Same prompt always generates same cache key
       - `test_cache_key_collision_resistance_invariant`: Different prompts likely generate different keys
       - `test_cache_key_model_aware_invariant`: Cache key includes model identifier
       - `test_cache_key_parameter_aware_invariant`: Cache key includes temperature/max_tokens

    2. **TestCacheLookupInvariants** (200 lines)
       - `test_cache_lookup_consistency_invariant`: Cached value matches original request
       - `test_cache_miss_consistency_invariant`: Cache miss returns None/empty consistently
       - `test_cache_ttl_invariant`: Expired cache entries not returned

    **Use Hypothesis strategies:**
    - `st.text(min_size=1, max_size=5000)` for prompts
    - `st.sampled_from(['gpt-4', 'gpt-3.5-turbo', 'claude-3-opus'])` for models
    - `st.floats(min_value=0.0, max_value=2.0, allow_nan=False, allow_infinity=False)` for temperature
    - `st.integers(min_value=1, max_value=4096)` for max_tokens

    **Settings:** `@settings(max_examples=100, suppress_health_check=[HealthCheck.function_scoped_fixture])`

    **Import and test:**
    ```python
    from core.llm.cache_aware_router import CacheAwareRouter, generate_cache_key
    ```

    **Follow existing pattern from:** `test_governance_cache_consistency.py`

    **DO NOT:** Modify actual caching logic (tests only)
  </action>
  <verify>
    Run: `pytest backend/tests/property_tests/llm/test_cache_consistency_invariants.py -v --hypothesis-max-examples=50`
    Expected: All cache consistency invariants hold
  </verify>
  <done>
    Cache consistency invariants cover:
    - Cache key determinism (same input → same key)
    - Cache key collision resistance
    - Cache lookup consistency (hit/miss behavior)
    - TTL expiration behavior
  </done>
</task>

<task type="auto">
  <name>Task 4: Create provider fallback invariants</name>
  <files>backend/tests/property_tests/llm/test_provider_fallback_invariants.py</files>
  <action>
    Create `test_provider_fallback_invariants.py` with property-based tests for provider fallback.

    **Test classes to create:**

    1. **TestProviderFallbackStateInvariants** (250 lines)
       - `test_fallback_state_preservation_invariant`: Context preserved during provider fallback
       - `test_fallback_no_duplication_invariant`: No duplicate tokens/messages after fallback
       - `test_fallback_continuity_invariant`: Response continues seamlessly from failure point

    2. **TestProviderFallbackOrderingInvariants** (250 lines)
       - `test_fallback_priority_invariant`: Providers tried in configured priority order
       - `test_fallback_exhaustion_invariant`: All providers exhausted before failure

    **Use Hypothesis strategies:**
    - `st.lists(st.sampled_from(['openai', 'anthropic', 'deepseek']), min_size=1, max_size=5, unique=True)` for provider lists
    - `st.text(min_size=1, max_size=1000)` for prompts
    - `st.integers(min_value=1, max_value=100)` for failure point (token to fail at)

    **Settings:** `@settings(max_examples=100, suppress_health_check=[HealthCheck.function_scoped_fixture])`

    **Import and test:**
    ```python
    from core.llm.byok_handler import BYOKHandler
    from unittest.mock import Mock, patch
    ```

    **Mock provider failures:**
    ```python
    def mock_provider_fail_at(prompt, fail_at_token):
        call_count = 0
        def failing_stream():
            nonlocal call_count
            call_count += 1
            if call_count >= fail_at_token:
                raise Exception("Provider failed")
            yield "token"
        return failing_stream()
    ```

    **DO NOT:** Modify actual fallback logic (tests only)
  </action>
  <verify>
    Run: `pytest backend/tests/property_tests/llm/test_provider_fallback_invariants.py -v --hypothesis-max-examples=50`
    Expected: All fallback invariants hold, no data loss
  </verify>
  <done>
    Provider fallback invariants cover:
    - State preservation during fallback
    - No duplication after fallback
    - Response continuity from failure point
    - Priority order respected
    - All providers exhausted before final failure
  </done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. **Test Execution:**
   ```bash
   pytest backend/tests/property_tests/llm/ -v -k "token_counting or cost_calculation or cache_consistency or provider_fallback" --hypothesis-max-examples=50
   ```
   Expected: All new tests pass

2. **Coverage Measurement:**
   ```bash
   pytest backend/tests/property_tests/llm/ --cov=core.llm.byok_handler --cov=core.llm.cognitive_tier_system --cov=core.llm.cache_aware_router --cov-report=term-missing
   ```
   Expected: 80%+ coverage on LLM services

3. **Test Count:**
   ```bash
   grep -r "def test_" backend/tests/property_tests/llm/test_token_counting_invariants.py backend/tests/property_tests/llm/test_cost_calculation_invariants.py backend/tests/property_tests/llm/test_cache_consistency_invariants.py backend/tests/property_tests/llm/test_provider_fallback_invariants.py | wc -l
   ```
   Expected: ~100 new tests

4. **Invariant Documentation:**
   - Each test has docstring explaining the invariant
   - Cost calculations use Decimal for precision
   - All tests use `@given` with Hypothesis strategies
</verification>

<success_criteria>
1. **Coverage Achievement:** LLM invariants achieve 80%+ property test coverage
2. **Test Quality:** All tests use `@given` with appropriate Hypothesis strategies
3. **Test Execution:** All tests pass (or document actual bugs with VALIDATED_BUG)
4. **Documentation:** Each invariant documented with clear docstring
</success_criteria>

<output>
After completion, create `.planning/phases/187-property-based-testing/187-02-SUMMARY.md` with:
- Tests created (count by file)
- Coverage metrics (LLM services)
- Any invariants that failed (bugs found)
- Lines of test code added
- Duration
</output>
