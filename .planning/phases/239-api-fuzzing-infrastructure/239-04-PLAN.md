---
phase: 239-api-fuzzing-infrastructure
plan: 04
type: execute
wave: 2
depends_on: [239-01]
files_modified:
  - backend/tests/fuzzing/test_workflow_api_fuzzing.py
  - backend/tests/fuzzing/test_skill_installation_fuzzing.py
  - backend/tests/fuzzing/test_trigger_execution_fuzzing.py
autonomous: true

must_haves:
  truths:
    - "Workflow endpoint fuzzing discovers crashes in workflow trigger parsing"
    - "Skill installation fuzzing discovers crashes in skill import/parsing code"
    - "Trigger execution fuzzing discovers crashes in trigger validation"
    - "Fuzzing uses TestClient with database override (isolated test database)"
    - "Authenticated user fixture provides JWT tokens for protected endpoints"
    - "Skill content fuzzing includes malicious payloads (code injection, typosquatting)"
  artifacts:
    - path: "backend/tests/fuzzing/test_workflow_api_fuzzing.py"
      provides: "Workflow endpoint fuzzing harness (create, update, trigger)"
      min_lines: 150
      exports: ["test_workflow_create_fuzzing", "test_workflow_update_fuzzing", "test_workflow_trigger_fuzzing"]
    - path: "backend/tests/fuzzing/test_skill_installation_fuzzing.py"
      provides: "Skill installation fuzzing harness (import, execute, promote)"
      min_lines: 150
      exports: ["test_skill_import_fuzzing", "test_skill_execute_fuzzing", "test_skill_promote_fuzzing"]
    - path: "backend/tests/fuzzing/test_trigger_execution_fuzzing.py"
      provides: "Trigger execution fuzzing harness (validate, execute, schedule)"
      min_lines: 120
      exports: ["test_trigger_validate_fuzzing", "test_trigger_execute_fuzzing", "test_trigger_schedule_fuzzing"]
  key_links:
    - from: "backend/tests/fuzzing/test_workflow_api_fuzzing.py"
      to: "backend/api/workflow_debugging.py"
      via: "FastAPI TestClient fuzzing"
      pattern: "TestClient.*workflow"
    - from: "backend/tests/fuzzing/test_skill_installation_fuzzing.py"
      to: "backend/api/skill_routes.py"
      via: "FastAPI TestClient fuzzing"
      pattern: "TestClient.*skill"
    - from: "backend/tests/fuzzing/test_trigger_execution_fuzzing.py"
      to: "backend/api/ai_workflows_routes.py"
      via: "FastAPI TestClient fuzzing"
      pattern: "TestClient.*trigger"
    - from: "backend/tests/fuzzing/test_skill_installation_fuzzing.py"
      to: "backend/core/skill_registry_service.py"
      via: "Security validation fuzzing"
      pattern: "SkillRegistryService"
---

<objective>
Create Atheris fuzzing harnesses for workflow and skill endpoints (FUZZ-05 requirement): workflow create/update/trigger, skill import/execute/promote, trigger validation/execution/scheduling. Use FastAPI TestClient with database override. Focus on security edge cases for skill installation (code injection, typosquatting, malicious SKILL.md files).

Purpose: Discover crashes in workflow and skill endpoint parsing/validation code. Target: POST /api/workflows (create), PUT /api/workflows/{id} (update), POST /api/workflows/{id}/trigger, POST /api/skills/import, POST /api/skills/execute, POST /api/skills/promote, POST /api/triggers/validate, POST /api/triggers/execute, POST /api/triggers/schedule.

Output: 3 fuzzing test files with 9+ fuzz targets, using TestClient pattern, db_session fixture, 10000 iterations per endpoint, crash artifact collection with security-focused edge cases.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/239-api-fuzzing-infrastructure/239-RESEARCH.md
@.planning/phases/239-api-fuzzing-infrastructure/239-01-PLAN.md
@backend/tests/fuzzing/conftest.py
@backend/tests/e2e_ui/fixtures/auth_fixtures.py
@backend/tests/e2e_ui/fixtures/database_fixtures.py
@backend/api/workflow_debugging.py
@backend/api/skill_routes.py
@backend/api/ai_workflows_routes.py
@backend/core/skill_registry_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create workflow endpoint fuzzing harness (create, update, trigger)</name>
  <files>backend/tests/fuzzing/test_workflow_api_fuzzing.py</files>
  <action>
Create test_workflow_api_fuzzing.py with FastAPI TestClient fuzzing:

1. **test_workflow_create_fuzzing(db_session: Session, authenticated_user)**:
   - Markers: @pytest.mark.fuzzing, @pytest.mark.slow, @pytest.mark.timeout(300)
   - Override database: app.dependency_overrides[get_db] = lambda: db_session
   - Create TestClient(app)
   - Get user, token from authenticated_user
   - Define fuzz_one_input(data):
     - Use FuzzedDataProvider
     - Fuzz workflow_name (0-100 chars, None, empty, SQL injection)
     - Fuzz workflow_description (0-1000 chars, None, empty)
     - Fuzz workflow_definition dict (0-20 keys, nested structures, cyclical refs)
     - Fuzz triggers list (0-10 items, random trigger configs)
     - Call POST /api/workflows
     - Assert status in [200, 400, 401, 422]

2. **test_workflow_update_fuzzing(db_session: Session, authenticated_user)**:
   - Similar structure for PUT /api/workflows/{id}
   - Fuzz workflow_id (50 chars, None, empty)
   - Fuzz update_data dict (partial updates, null values, huge strings)
   - Assert status in [200, 400, 404, 422]

3. **test_workflow_trigger_fuzzing(db_session: Session, authenticated_user)**:
   - Similar structure for POST /api/workflows/{id}/trigger
   - Fuzz workflow_id variations
   - Fuzz input_data dict (0-20 keys, nested values, huge payloads)
   - Assert status in [200, 400, 404, 422]

4. **test_workflow_schedule_fuzzing(db_session: Session, authenticated_user)**:
   - Fuzz workflow scheduling endpoint
   - Fuzz schedule_config dict (cron expressions, intervals, invalid formats)
   - Test edge cases: invalid cron, past dates, huge intervals
   - Assert status in [200, 400, 404, 422]

Focus on workflow_definition edge cases: None, empty dict, nested cyclical references, huge YAML/JSON strings.
Test cron expression validation: invalid syntax, out-of-range values, special characters.
Use TestClient pattern for all workflow endpoints.

Add test for workflow DAG validation (cyclical dependencies, missing nodes).
  </action>
  <verify>
# Verify workflow fuzzing tests
pytest backend/tests/fuzzing/test_workflow_api_fuzzing.py -v -m fuzzing --collect-only

# Verify workflow definition edge cases
python -c "
import sys
sys.path.insert(0, 'backend')
from advanced_workflow_orchestrator import AdvancedWorkflowOrchestrator
# Test workflow definition edge cases
test_definitions = [
    None,
    {},
    {'nodes': []},
    {'nodes': [{'id': 'a', 'depends_on': 'a'}]},  # cyclical
    {'yaml': 'a' * 10000},  # huge
]
print('Workflow definition edge cases:', len(test_definitions))
"
  </verify>
  <done>
Workflow endpoint fuzzing created with:
- test_workflow_create_fuzzing() for POST /api/workflows
- test_workflow_update_fuzzing() for PUT /api/workflows/{id}
- test_workflow_trigger_fuzzing() for POST /api/workflows/{id}/trigger
- test_workflow_schedule_fuzzing() for scheduling endpoint
- Workflow definition edge cases: None, empty, cyclical, huge strings
- Cron expression validation: invalid syntax, out-of-range, special chars
- TestClient pattern with database override
- Authenticated user fixture for JWT tokens
- 10000 iterations per test
- Status code assertions: [200, 400, 401, 404, 422]
  </done>
</task>

<task type="auto">
  <name>Task 2: Create skill installation fuzzing harness (import, execute, promote)</name>
  <files>backend/tests/fuzzing/test_skill_installation_fuzzing.py</files>
  <action>
Create test_skill_installation_fuzzing.py with security-focused fuzzing:

1. **test_skill_import_fuzzing(db_session: Session, authenticated_user)**:
   - Markers: @pytest.mark.fuzzing, @pytest.mark.slow, @pytest.mark.timeout(300)
   - Override database, create TestClient
   - Get user, token from authenticated_user
   - Define fuzz_one_input(data):
     - Use FuzzedDataProvider
     - Fuzz source (github_url, file_upload, raw_content, invalid values)
     - Fuzz content (0-10000 chars, SKILL.md format, None, empty)
     - Fuzz metadata dict (0-10 keys, SQL injection, XSS)
     - Call POST /api/skills/import
     - Assert status in [200, 400, 401, 422]

2. **test_skill_execute_fuzzing(db_session: Session, authenticated_user)**:
   - Similar structure for POST /api/skills/execute
   - Fuzz skill_id (50 chars, None, empty)
   - Fuzz inputs dict (0-20 keys, code injection, huge values)
   - Fuzz agent_id (50 chars, None, empty)
   - Assert status in [200, 400, 404, 422]

3. **test_skill_promote_fuzzing(db_session: Session, authenticated_user)**:
   - Similar structure for POST /api/skills/promote
   - Fuzz skill_id variations
   - Test promotion from Untrusted -> Active status
   - Assert status in [200, 400, 404, 409]

4. **test_skill_content_security_fuzz(db_session: Session, authenticated_user)**:
   - Security-focused fuzzing for SKILL.md content
   - Test malicious payloads:
     - Code injection: "__import__('os').system('rm -rf /')"
     - Typosquatting: "requests" vs "requets", "numpy" vs "numpyy"
     - Path traversal: "../../../etc/passwd"
     - Null bytes: "skill\x00name"
   - Test skill metadata injection (author, tags with malicious content)
   - Assert no crashes (validation errors OK)

5. **test_skill_yaml_parsing_fuzz(db_session: Session, authenticated_user)**:
   - Fuzz YAML frontmatter parsing
   - Test malformed YAML, huge YAML, cyclical references
   - Test invalid YAML syntax, missing required fields
   - Assert no crashes (parsing errors OK)

Focus on security edge cases for skill installation (code injection, typosquatting from Phase 237).
Test SKILL.md parsing with malicious frontmatter (YAML injection, huge documents).
Use TestClient pattern for all skill endpoints.

Add test for skill dependency injection (requirements.txt with malicious packages).
  </action>
  <verify>
# Verify skill fuzzing tests
pytest backend/tests/fuzzing/test_skill_installation_fuzzing.py -v -m fuzzing --collect-only

# Verify security edge cases
grep -h "code injection\|typosquatting\|path traversal" backend/tests/fuzzing/test_skill_installation_fuzzing.py

# Verify YAML parsing tests
grep -c "YAML\|yaml" backend/tests/fuzzing/test_skill_installation_fuzzing.py
  </verify>
  <done>
Skill installation fuzzing created with:
- test_skill_import_fuzzing() for POST /api/skills/import
- test_skill_execute_fuzzing() for POST /api/skills/execute
- test_skill_promote_fuzzing() for POST /api/skills/promote
- test_skill_content_security_fuzz() for malicious payloads
- test_skill_yaml_parsing_fuzz() for YAML frontmatter parsing
- Security edge cases: code injection, typosquatting, path traversal, null bytes
- TestClient pattern with database override
- Authenticated user fixture for JWT tokens
- 10000 iterations per test
- Status code assertions: [200, 400, 401, 404, 422]
  </done>
</task>

<task type="auto">
  <name>Task 3: Create trigger execution fuzzing harness (validate, execute, schedule)</name>
  <files>backend/tests/fuzzing/test_trigger_execution_fuzzing.py</files>
  <action>
Create test_trigger_execution_fuzzing.py for trigger validation/execution fuzzing:

1. **test_trigger_validate_fuzz(db_session: Session, authenticated_user)**:
   - Markers: @pytest.mark.fuzzing, @pytest.mark.slow, @pytest.mark.timeout(300)
   - Override database, create TestClient
   - Get user, token from authenticated_user
   - Define fuzz_one_input(data):
     - Use FuzzedDataProvider
     - Fuzz trigger_type (webhook, schedule, event, invalid values)
     - Fuzz trigger_config dict (0-10 keys, random values)
     - Fuzz trigger_conditions list (0-5 items, invalid conditions)
     - Call POST /api/triggers/validate
     - Assert status in [200, 400, 422]

2. **test_trigger_execute_fuzz(db_session: Session, authenticated_user)**:
   - Similar structure for POST /api/triggers/execute
   - Fuzz trigger_id (50 chars, None, empty)
   - Fuzz execution_context dict (0-10 keys, nested values)
   - Fuzz trigger_payload (0-1000 chars, None, empty, JSON)
   - Assert status in [200, 400, 404, 422]

3. **test_trigger_schedule_fuzz(db_session: Session, authenticated_user)**:
   - Similar structure for POST /api/triggers/schedule
   - Fuzz trigger_id variations
   - Fuzz schedule_config dict (cron, interval, invalid formats)
   - Test past dates, future dates, huge intervals
   - Assert status in [200, 400, 404, 422]

4. **test_trigger_webhook_fuzz(db_session: Session, authenticated_user)**:
   - Fuzz webhook trigger handling
   - Fuzz webhook URL (invalid URLs, huge URLs, javascript: URLs)
   - Fuzz webhook headers dict (0-10 keys, SQL injection)
   - Fuzz webhook_payload (0-5000 chars, None, empty)
   - Assert no crashes (validation errors OK)

5. **test_trigger_event_fuzz(db_session: Session, authenticated_user)**:
   - Fuzz event trigger handling
   - Fuzz event_type (0-100 chars, None, empty)
   - Fuzz event_source (0-100 chars, None, empty)
   - Fuzz event_data dict (0-20 keys, nested structures)
   - Assert no crashes (validation errors OK)

Focus on trigger_config edge cases: None, empty dict, cyclical references, huge values.
Test webhook URL validation: protocol checking, length limits, forbidden protocols.
Use TestClient pattern for all trigger endpoints.

Add test for trigger condition evaluation (SQL injection in conditions).
  </action>
  <verify>
# Verify trigger fuzzing tests
pytest backend/tests/fuzzing/test_trigger_execution_fuzzing.py -v -m fuzzing --collect-only

# Verify webhook URL validation
python -c "
import sys
sys.path.insert(0, 'backend')
from urllib.parse import urlparse
# Test webhook URL edge cases
test_urls = [
    None,
    '',
    'javascript:alert(1)',
    'file:///etc/passwd',
    'http://' + 'a' * 2000 + '.com',
]
for url in test_urls:
    try:
        result = urlparse(url)
        print(f'{url[:50] if url else \"None\"}: scheme={result.scheme}')
    except Exception as e:
        print(f'{url[:50] if url else \"None\"}: {type(e).__name__}')
"
  </verify>
  <done>
Trigger execution fuzzing created with:
- test_trigger_validate_fuzz() for POST /api/triggers/validate
- test_trigger_execute_fuzz() for POST /api/triggers/execute
- test_trigger_schedule_fuzz() for POST /api/triggers/schedule
- test_trigger_webhook_fuzz() for webhook trigger handling
- test_trigger_event_fuzz() for event trigger handling
- Trigger config edge cases: None, empty, cyclical, huge values
- Webhook URL validation: protocol checking, length limits, forbidden protocols
- TestClient pattern with database override
- Authenticated user fixture for JWT tokens
- 10000 iterations per test
- Status code assertions: [200, 400, 404, 422]
  </done>
</task>

</tasks>

<verification>
# Overall verification

## 1. Test discovery
pytest backend/tests/fuzzing/test_workflow_api_fuzzing.py --collect-only -q
pytest backend/tests/fuzzing/test_skill_installation_fuzzing.py --collect-only -q
pytest backend/tests/fuzzing/test_trigger_execution_fuzzing.py --collect-only -q

## 2. Marker verification
pytest backend/tests/fuzzing/ -v -m fuzzing --collect-only | grep -E "(workflow|skill|trigger)"

## 3. Security edge case verification
grep -h "code injection\|typosquatting\|SQL injection\|XSS" backend/tests/fuzzing/test_skill_installation_fuzzing.py

## 4. YAML parsing verification
grep -c "YAML\|yaml" backend/tests/fuzzing/test_skill_installation_fuzzing.py

## 5. Webhook URL verification
grep -h "javascript:\|file://\|http://" backend/tests/fuzzing/test_trigger_execution_fuzzing.py

## 6. Quick fuzzing run (100 iterations)
FUZZ_ITERATIONS=100 pytest backend/tests/fuzzing/test_workflow_api_fuzzing.py::test_workflow_create_fuzzing -v -s --tb=short
</verification>

<success_criteria>
FUZZ-05 (Workflow/skill endpoint fuzzing) is complete when:
1. test_workflow_api_fuzzing.py exists with 4+ fuzz targets (create, update, trigger, schedule)
2. test_skill_installation_fuzzing.py exists with 5+ fuzz targets (import, execute, promote, security, YAML)
3. test_trigger_execution_fuzzing.py exists with 5+ fuzz targets (validate, execute, schedule, webhook, event)
4. Security edge cases covered: code injection, typosquatting, path traversal, null bytes
5. Webhook URL validation tested: protocol checking, length limits, forbidden protocols
6. YAML parsing tested: malformed YAML, huge YAML, cyclical references
7. All tests have @pytest.mark.fuzzing, @pytest.mark.timeout(300)
8. Quick verification run completes without hanging
</success_criteria>

<output>
After completion, create `.planning/phases/239-api-fuzzing-infrastructure/239-04-SUMMARY.md` with:
- Workflow endpoint fuzzing coverage (create, update, trigger, schedule)
- Skill installation fuzzing coverage (import, execute, promote, security, YAML)
- Trigger execution fuzzing coverage (validate, execute, schedule, webhook, event)
- Security edge cases tested (code injection, typosquatting, path traversal, null bytes)
- Webhook URL validation (protocol, length, forbidden protocols)
- YAML parsing tests (malformed, huge, cyclical)
- Next steps: Integrate fuzzing campaigns with weekly CI pipeline (Plan 05)
</output>
