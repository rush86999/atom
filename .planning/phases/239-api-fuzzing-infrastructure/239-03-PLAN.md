---
phase: 239-api-fuzzing-infrastructure
plan: 03
type: execute
wave: 2
depends_on: [239-01]
files_modified:
  - backend/tests/fuzzing/test_agent_api_fuzzing.py
  - backend/tests/fuzzing/test_canvas_presentation_fuzzing.py
  - backend/tests/fuzzing/test_agent_streaming_fuzzing.py
autonomous: true

must_haves:
  truths:
    - "Agent execution fuzzing discovers crashes in agent run parsing/validation code"
    - "Canvas presentation fuzzing discovers crashes in canvas state parsing"
    - "Fuzzing uses TestClient with database override (isolated test database)"
    - "Streaming endpoint fuzzing uses httpx client for realistic WebSocket testing"
    - "Authenticated user fixture provides JWT tokens for protected endpoints"
    - "Crashes saved to campaign-specific crash directories with stack traces"
  artifacts:
    - path: "backend/tests/fuzzing/test_agent_api_fuzzing.py"
      provides: "Agent execution fuzzing harness (run, status, delete)"
      min_lines: 150
      exports: ["test_agent_run_fuzzing", "test_agent_status_fuzzing", "test_agent_delete_fuzzing"]
    - path: "backend/tests/fuzzing/test_canvas_presentation_fuzzing.py"
      provides: "Canvas presentation fuzzing harness (present, update, close)"
      min_lines: 150
      exports: ["test_canvas_present_fuzzing", "test_canvas_update_fuzzing", "test_canvas_close_fuzzing"]
    - path: "backend/tests/fuzzing/test_agent_streaming_fuzzing.py"
      provides: "Agent chat streaming fuzzing harness (WebSocket, SSE)"
      min_lines: 120
      exports: ["test_agent_chat_streaming_fuzz", "test_agent_websocket_fuzz"]
  key_links:
    - from: "backend/tests/fuzzing/test_agent_api_fuzzing.py"
      to: "backend/api/agent_routes.py"
      via: "FastAPI TestClient fuzzing"
      pattern: "TestClient.*agent"
    - from: "backend/tests/fuzzing/test_canvas_presentation_fuzzing.py"
      to: "backend/api/canvas_routes.py"
      via: "FastAPI TestClient fuzzing"
      pattern: "TestClient.*canvas"
    - from: "backend/tests/fuzzing/test_agent_streaming_fuzzing.py"
      to: "backend/api/websocket_routes.py"
      via: "httpx client for WebSocket fuzzing"
      pattern: "httpx.*websocket"
    - from: "backend/tests/fuzzing/test_agent_api_fuzzing.py"
      to: "backend/tests/fuzzing/campaigns/fuzzing_orchestrator.py"
      via: "Campaign integration for crash filing"
      pattern: "FuzzingOrchestrator"
---

<objective>
Create Atheris fuzzing harnesses for agent execution endpoints (FUZZ-04 requirement): agent run, status, delete, canvas presentation (present, update, close), and agent chat streaming. Use FastAPI TestClient for standard endpoints and httpx client for WebSocket streaming endpoints.

Purpose: Discover crashes in agent execution and canvas presentation parsing/validation code. Target: POST /api/agents/{id}/run, GET /api/agents/{id}/status, DELETE /api/agents/{id}, POST /api/canvas/present, POST /api/agents/{id}/chat (WebSocket streaming).

Output: 3 fuzzing test files with 8+ fuzz targets, using TestClient/httpx patterns, db_session fixture, 10000 iterations per endpoint, crash artifact collection.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/239-api-fuzzing-infrastructure/239-RESEARCH.md
@.planning/phases/239-api-fuzzing-infrastructure/239-01-PLAN.md
@backend/tests/fuzzing/conftest.py
@backend/tests/e2e_ui/fixtures/auth_fixtures.py
@backend/tests/e2e_ui/fixtures/database_fixtures.py
@backend/api/agent_routes.py
@backend/api/canvas_routes.py
@backend/api/websocket_routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent execution fuzzing harness (run, status, delete)</name>
  <files>backend/tests/fuzzing/test_agent_api_fuzzing.py</files>
  <action>
Create test_agent_api_fuzzing.py with FastAPI TestClient fuzzing:

1. **Imports**:
   - pytest, atheris, sys
   - FastAPI TestClient
   - Session from sqlalchemy.orm
   - main:app, core.database:get_db
   - tests.e2e_ui.fixtures for db_session, authenticated_user

2. **test_agent_run_fuzzing(db_session: Session, authenticated_user)**:
   - Markers: @pytest.mark.fuzzing, @pytest.mark.slow, @pytest.mark.timeout(300)
   - Override database: app.dependency_overrides[get_db] = lambda: db_session
   - Create TestClient(app)
   - Get user, token from authenticated_user
   - Define fuzz_one_input(data):
     - Use FuzzedDataProvider
     - Fuzz agent_id (50 chars, UUID format, None, empty)
     - Fuzz parameters dict (0-10 keys, random values)
     - Call client.post(f"/api/agents/{agent_id}/run", json={parameters}, headers={Authorization})
     - Assert status in [200, 400, 401, 404, 422]
     - Catch ValueError, JSONDecodeError, re-raise Exception

3. **test_agent_status_fuzzing(db_session: Session, authenticated_user)**:
   - Similar structure for GET /api/agents/{id}/status
   - Fuzz agent_id variations
   - Assert status in [200, 400, 404]

4. **test_agent_delete_fuzzing(db_session: Session, authenticated_user)**:
   - Similar structure for DELETE /api/agents/{id}
   - Fuzz agent_id variations
   - Assert status in [200, 400, 404, 409]

5. **test_agent_list_fuzzing(db_session: Session, authenticated_user)**:
   - Fuzz query parameters: category (string), limit (int), offset (int)
   - Call GET /api/agents with fuzzed query params
   - Assert status in [200, 400]

Focus on agent_id edge cases: None, empty string, SQL injection, XSS, huge length (1000+ chars).
Use TestClient pattern for all agent CRUD endpoints.
Reuse authenticated_user fixture for JWT tokens.

Add test for agent parameter injection (code injection via parameters dict).
  </action>
  <verify>
# Verify agent fuzzing tests
pytest backend/tests/fuzzing/test_agent_api_fuzzing.py -v -m fuzzing --collect-only

# Verify agent ID edge cases
python -c "
import sys
sys.path.insert(0, 'backend')
from core.agent_governance_service import AgentGovernanceService
from sqlalchemy.orm import Session
# Test edge cases
test_ids = [
    None,
    '',
    'not-a-uuid',
    \"'; DROP TABLE agents; --\",
    'a' * 1000,
]
print('Agent ID edge cases:', test_ids)
"
  </verify>
  <done>
Agent execution fuzzing created with:
- test_agent_run_fuzzing() for POST /api/agents/{id}/run
- test_agent_status_fuzzing() for GET /api/agents/{id}/status
- test_agent_delete_fuzzing() for DELETE /api/agents/{id}
- test_agent_list_fuzzing() for GET /api/agents with query params
- Agent ID edge cases: None, empty, SQL injection, XSS, huge length
- TestClient pattern with database override
- Authenticated user fixture for JWT tokens
- 10000 iterations per test
- Status code assertions: [200, 400, 401, 404, 422]
  </done>
</task>

<task type="auto">
  <name>Task 2: Create canvas presentation fuzzing harness</name>
  <files>backend/tests/fuzzing/test_canvas_presentation_fuzzing.py</files>
  <action>
Create test_canvas_presentation_fuzzing.py for canvas state fuzzing:

1. **test_canvas_present_fuzzing(db_session: Session, authenticated_user)**:
   - Markers: @pytest.mark.fuzzing, @pytest.mark.slow, @pytest.mark.timeout(300)
   - Override database, create TestClient
   - Define fuzz_one_input(data):
     - Use FuzzedDataProvider
     - Fuzz canvas_type (chart, markdown, form, sheet, terminal, email, coding)
     - Fuzz canvas_data dict (0-20 keys, nested structures, huge values)
     - Fuzz agent_id (50 chars)
     - Call POST /api/canvas/present
     - Assert status in [200, 400, 401, 422]

2. **test_canvas_update_fuzzing(db_session: Session, authenticated_user)**:
   - Similar structure for POST /api/canvas/{id}/update
   - Fuzz canvas_id (50 chars)
   - Fuzz update_data dict (partial updates, full replacements, null values)
   - Assert status in [200, 400, 404, 422]

3. **test_canvas_close_fuzzing(db_session: Session, authenticated_user)**:
   - Similar structure for POST /api/canvas/{id}/close
   - Fuzz canvas_id variations
   - Assert status in [200, 400, 404]

4. **test_canvas_execute_fuzzing(db_session: Session, authenticated_user)**:
   - Fuzz canvas action execution (button clicks, form submissions)
   - Fuzz action_id (50 chars)
   - Fuzz action_parameters dict (0-10 keys)
   - Call POST /api/canvas/{id}/execute
   - Assert status in [200, 400, 404, 422]

Focus on canvas_data edge cases: None, empty dict, nested cyclical references, huge strings (10MB+).
Test canvas_type validation: invalid types, None, empty string, SQL injection.
Use TestClient pattern for all canvas endpoints.

Add test for canvas form submission with malicious payloads (SQL injection, XSS, null bytes).
  </action>
  <verify>
# Verify canvas fuzzing tests
pytest backend/tests/fuzzing/test_canvas_presentation_fuzzing.py -v -m fuzzing --collect-only

# Verify canvas type validation
python -c "
import sys
sys.path.insert(0, 'backend')
from tools.canvas_tool import CanvasTool
# Test canvas type validation
test_types = [
    None,
    '',
    'invalid_type',
    \"'; DROP TABLE canvas; --\",
    'a' * 100,
]
print('Canvas type edge cases:', test_types)
"
  </verify>
  <done>
Canvas presentation fuzzing created with:
- test_canvas_present_fuzzing() for POST /api/canvas/present
- test_canvas_update_fuzzing() for POST /api/canvas/{id}/update
- test_canvas_close_fuzzing() for POST /api/canvas/{id}/close
- test_canvas_execute_fuzzing() for POST /api/canvas/{id}/execute
- Canvas type validation: None, empty, invalid, SQL injection
- Canvas data edge cases: None, empty, nested cyclical, huge strings
- TestClient pattern with database override
- Authenticated user fixture for JWT tokens
- 10000 iterations per test
- Status code assertions: [200, 400, 401, 404, 422]
  </done>
</task>

<task type="auto">
  <name>Task 3: Create agent streaming fuzzing harness (WebSocket/SSE)</name>
  <files>backend/tests/fuzzing/test_agent_streaming_fuzzing.py</files>
  <action>
Create test_agent_streaming_fuzzing.py for streaming endpoint fuzzing:

1. **test_agent_chat_streaming_fuzz(authenticated_user)**:
   - Markers: @pytest.mark.fuzzing, @pytest.mark.slow, @pytest.mark.timeout(300)
   - Get user, token from authenticated_user
   - Define fuzz_one_input(data):
     - Use FuzzedDataProvider
     - Fuzz agent_id (50 chars, None, empty)
     - Fuzz message string (0-10000 chars, None, empty)
     - Fuzz parameters dict (0-10 keys, nested values)
     - Use httpx client for realistic HTTP testing:
       - with httpx.Client() as client:
         - response = client.post(f"{base_url}/api/agents/{agent_id}/chat", json={message, parameters}, headers={Authorization}, timeout=5)
     - Assert status in [200, 400, 401, 404, 422]
     - Catch httpx.ConnectError, TimeoutException (expected), re-raise Exception

2. **test_agent_websocket_fuzz(authenticated_user)**:
   - Fuzz WebSocket connection for agent chat
   - Fuzz agent_id, initial message
   - Use httpx WebSocket client (or websockets library)
   - Test malformed WebSocket frames, invalid message formats
   - Assert connection closes gracefully or returns error

3. **test_streaming_sse_fuzz(authenticated_user)**:
   - Fuzz Server-Sent Events (SSE) endpoint
   - Fuzz agent_id, query parameters
   - Test SSE with invalid Accept headers, malformed requests
   - Assert 200/404/400 responses (no crashes)

4. **test_streaming_timeout_fuzz(authenticated_user)**:
   - Fuzz streaming timeout handling
   - Create agents with infinite loops (simulated)
   - Test client timeout, server timeout
   - Assert graceful timeout handling (no hung connections)

Use httpx client (NOT requests) for async/streaming support.
Set short timeouts (5-10s) to prevent hangs.
Focus on streaming edge cases: huge messages, chunked encoding, connection drops.

Add test for concurrent streaming requests (multiple agents streaming simultaneously).
  </action>
  <verify>
# Verify streaming fuzzing tests
pytest backend/tests/fuzzing/test_agent_streaming_fuzzing.py -v -m fuzzing --collect-only

# Verify httpx client usage
grep -c "httpx" backend/tests/fuzzing/test_agent_streaming_fuzzing.py

# Verify timeout configuration
grep -h "timeout\|TimeoutException" backend/tests/fuzzing/test_agent_streaming_fuzzing.py
  </verify>
  <done>
Agent streaming fuzzing created with:
- test_agent_chat_streaming_fuzz() for POST /api/agents/{id}/chat
- test_agent_websocket_fuzz() for WebSocket connections
- test_streaming_sse_fuzz() for Server-Sent Events
- test_streaming_timeout_fuzz() for timeout handling
- httpx client for async/streaming support
- Short timeouts (5-10s) to prevent hangs
- Streaming edge cases: huge messages, chunked encoding, connection drops
- Concurrent streaming test for race conditions
- 10000 iterations per test
- Status code assertions: [200, 400, 401, 404, 422]
  </done>
</task>

</tasks>

<verification>
# Overall verification

## 1. Test discovery
pytest backend/tests/fuzzing/test_agent_api_fuzzing.py --collect-only -q
pytest backend/tests/fuzzing/test_canvas_presentation_fuzzing.py --collect-only -q
pytest backend/tests/fuzzing/test_agent_streaming_fuzzing.py --collect-only -q

## 2. Marker verification
pytest backend/tests/fuzzing/ -v -m fuzzing --collect-only | grep -E "(agent|canvas|streaming)"

## 3. httpx client verification (for streaming)
grep -c "httpx" backend/tests/fuzzing/test_agent_streaming_fuzzing.py
# Expected: > 0

## 4. TestClient pattern verification (for non-streaming)
grep -c "TestClient" backend/tests/fuzzing/test_agent_api_fuzzing.py
grep -c "TestClient" backend/tests/fuzzing/test_canvas_presentation_fuzzing.py
# Expected: > 0

## 5. Timeout configuration verification
grep -h "timeout=" backend/tests/fuzzing/test_agent_streaming_fuzzing.py

## 6. Edge case verification
grep -h "SQL injection\|XSS\|null byte" backend/tests/fuzzing/test_agent_*.py

## 7. Quick fuzzing run (100 iterations)
FUZZ_ITERATIONS=100 pytest backend/tests/fuzzing/test_agent_api_fuzzing.py::test_agent_run_fuzzing -v -s --tb=short
</verification>

<success_criteria>
FUZZ-04 (Agent execution fuzzing) is complete when:
1. test_agent_api_fuzzing.py exists with 4+ fuzz targets (run, status, delete, list)
2. test_canvas_presentation_fuzzing.py exists with 4+ fuzz targets (present, update, close, execute)
3. test_agent_streaming_fuzzing.py exists with 4+ fuzz targets (chat, websocket, SSE, timeout)
4. Agent/canvas tests use TestClient pattern
5. Streaming tests use httpx client with timeout configuration
6. All tests have @pytest.mark.fuzzing, @pytest.mark.timeout(300)
7. Edge cases covered: SQL injection, XSS, null bytes, huge inputs
8. Quick verification run completes without hanging
</success_criteria>

<output>
After completion, create `.planning/phases/239-api-fuzzing-infrastructure/239-03-SUMMARY.md` with:
- Agent execution fuzzing coverage (run, status, delete, list)
- Canvas presentation fuzzing coverage (present, update, close, execute)
- Streaming endpoint fuzzing coverage (chat, WebSocket, SSE, timeout)
- httpx vs TestClient pattern usage
- Streaming timeout configuration (5-10s)
- Edge cases tested (SQL injection, XSS, null bytes, huge inputs)
- Next steps: Create workflow/skill endpoint fuzzing (Plan 04)
</output>
