---
phase: 194-coverage-push-18-22
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/core/integration/test_lancedb_handler_coverage_extend.py
autonomous: true

must_haves:
  truths:
    - "LanceDBHandler tests use simplified mock setup (pytest-mock's mocker.fixture)"
    - "Complex mock hierarchies replaced with MagicMock for cleaner configuration"
    - "Test pass rate improves from 27.4% (Phase 193) to >80%"
    - "Coverage extends from 55% to 75%+ target (709 statements)"
  artifacts:
    - path: "backend/tests/core/integration/test_lancedb_handler_coverage_extend.py"
      provides: "Extended LanceDBHandler tests with simplified mocks"
      min_lines: 800
      contains: "TestLanceDBHandlerCoverageExtend"
  key_links:
    - from: "backend/tests/core/integration/test_lancedb_handler_coverage_extend.py"
      to: "backend/core/integration/lancedb_handler.py"
      via: "from core.integration.lancedb_handler import"
      pattern: "from core.integration.lancedb_handler import LanceDBHandler"
---

<objective>
Extend LanceDBHandler coverage from 55% to 75%+ by fixing mock complexity issues that caused 27.4% pass rate in Phase 193. Replace complex mock hierarchies with pytest-mock's simplified mocker.fixture and MagicMock patterns.

Purpose: Improve test reliability and coverage for LanceDB integration layer while avoiding complex mock setup
Output: Extended test file (800+ lines) with simplified mocks, 75%+ coverage, >80% pass rate
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-coverage-push-18-22/194-RESEARCH.md
@.planning/phases/193-coverage-push-15-18/193-08-SUMMARY.md

# LanceDBHandler Phase 193 blocker
@.planning/phases/193-coverage-push-15-18/193-08-SUMMARY.md

# Testing patterns
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend LanceDBHandler tests with simplified mock patterns</name>
  <files>backend/tests/core/integration/test_lancedb_handler_coverage_extend.py</files>
  <action>
Extend existing test file with simplified mock patterns:

1. **Test file structure:**
   ```python
   """
   Coverage-driven tests for lancedb_handler.py (55% -> 75%+ target)

   FIX for Phase 193 mock complexity issues:
   - Use pytest-mock's mocker.fixture instead of complex mock setup
   - Use MagicMock for flexible mocking
   - Use mock_open for file operations
   - Simplify mock configuration to reduce test fragility

   Coverage Target Areas:
   - Lines 100-200: Vector search with query embedding
   - Lines 200-300: Batch insert operations
   - Lines 300-400: Table management (create, delete)
   - Lines 400-500: Knowledge graph operations
   - Lines 500-600: Error handling and edge cases
   """
   class TestLanceDBHandlerCoverageExtend:
   ```

2. **Vector search with simplified mocks (15 tests):**
   ```python
   def test_vector_search_with_simplified_mocks(self, mocker):
       """Cover vector search (lines 100-200) with simplified mock setup."""
       # Simplified: Use MagicMock instead of complex mock hierarchy
       mock_client = mocker.MagicMock()
       mock_table = mocker.MagicMock()
       mock_client.open_table.return_value = mock_table
       mock_table.search.return_value = mock_table
       mock_table.to_pandas.return_value = []

       # Patch lancedb.connect at module level
       mocker.patch(
           "core.integration.lancedb_handler.lancedb.connect",
           return_value=mock_client
       )

       handler = LanceDBHandler(uri="memory://")
       results = handler.vector_search(query="test", limit=10)

       # Verify LanceDB operations without complex assertions
       mock_table.search.assert_called_once()

   @pytest.mark.parametrize("limit,expected_calls", [
       (10, 1),
       (50, 1),
       (100, 1),
   ])
   def test_vector_search_with_various_limits(self, mocker, limit, expected_calls):
       """Cover vector search with different limit values."""
       mock_client = mocker.MagicMock()
       mock_table = mocker.MagicMock()
       mock_client.open_table.return_value = mock_table
       mock_table.search.return_value = mock_table
       mock_table.limit.return_value = mock_table
       mock_table.to_pandas.return_value = []

       mocker.patch(
           "core.integration.lancedb_handler.lancedb.connect",
           return_value=mock_client
       )

       handler = LanceDBHandler(uri="memory://")
       handler.vector_search(query="test", limit=limit)

       mock_table.limit.assert_called_with(limit)

   def test_vector_search_with_filter(self, mocker):
       """Cover vector search with metadata filtering."""
       mock_client = mocker.MagicMock()
       mock_table = mocker.MagicMock()
       mock_client.open_table.return_value = mock_table
       mock_table.search.return_value = mock_table
       mock_table.where.return_value = mock_table
       mock_table.to_pandas.return_value = []

       mocker.patch(
           "core.integration.lancedb_handler.lancedb.connect",
           return_value=mock_client
       )

       handler = LanceDBHandler(uri="memory://")
       handler.vector_search(
           query="test",
           filter="agent_id = 'test-agent'",
           limit=10
       )

       mock_table.where.assert_called_once()

   def test_vector_search_empty_results(self, mocker):
       """Cover empty result handling (lines 120-140)."""
       mock_client = mocker.MagicMock()
       mock_table = mocker.MagicMock()
       mock_client.open_table.return_value = mock_table
       mock_table.search.return_value = mock_table
       mock_table.to_pandas.return_value = []  # Empty results

       mocker.patch(
           "core.integration.lancedb_handler.lancedb.connect",
           return_value=mock_client
       )

       handler = LanceDBHandler(uri="memory://")
       results = handler.vector_search(query="test", limit=10)

       assert results == []
   ```

3. **Batch insert with file operations (12 tests):**
   ```python
   def test_batch_insert_with_file_operations(self, mocker):
       """Cover batch insert (lines 200-300) with file operation mocks."""
       # Mock file operations using mock_open
       mocker.patch("builtins.open", mocker.mock_open(read_data="data"))
       mocker.patch("os.path.exists", return_value=True)
       mocker.patch("os.makedirs")

       # Mock LanceDB operations
       mock_client = mocker.MagicMock()
       mock_table = mocker.MagicMock()
       mock_client.open_table.return_value = mock_table
       mock_table.add.return_value = None

       mocker.patch(
           "core.integration.lancedb_handler.lancedb.connect",
           return_value=mock_client
       )

       handler = LanceDBHandler(uri="file://test.db")
       handler.batch_insert(data=[{"id": "1", "vector": [0.1, 0.2]}])

       # Verify table was created and data was added
       mock_client.create_table.assert_called_once()
       mock_table.add.assert_called_once()

   def test_batch_insert_with_chunks(self, mocker):
       """Cover chunked insertion for large datasets."""
       mocker.patch("builtins.open", mocker.mock_open(read_data="data"))
       mocker.patch("os.path.exists", return_value=True)
       mocker.patch("os.makedirs")

       mock_client = mocker.MagicMock()
       mock_table = mocker.MagicMock()
       mock_client.open_table.return_value = mock_table
       mock_table.add.return_value = None

       mocker.patch(
           "core.integration.lancedb_handler.lancedb.connect",
           return_value=mock_client
       )

       handler = LanceDBHandler(uri="file://test.db")

       # Insert data larger than default chunk size
       large_data = [{"id": str(i), "vector": [0.1, 0.2]} for i in range(1500)]
       handler.batch_insert(data=large_data, chunk_size=1000)

       # Should be called twice (1500 / 1000 = 2 chunks)
       assert mock_table.add.call_count == 2

   @pytest.mark.parametrize("batch_size,expected_chunks", [
       (100, 1),
       (500, 1),
       (1000, 2),
       (1500, 2),
   ])
   def test_batch_insert_chunking(self, mocker, batch_size, expected_chunks):
       """Cover chunking logic with various batch sizes."""
       mocker.patch("builtins.open", mocker.mock_open(read_data="data"))
       mocker.patch("os.path.exists", return_value=True)

       mock_client = mocker.MagicMock()
       mock_table = mocker.MagicMock()
       mock_client.open_table.return_value = mock_table

       mocker.patch(
           "core.integration.lancedb_handler.lancedb.connect",
           return_value=mock_client
       )

       handler = LanceDBHandler(uri="file://test.db")
       handler.batch_insert(
           data=[{"id": str(i), "vector": [0.1]} for i in range(batch_size)],
           chunk_size=1000
       )

       assert mock_table.add.call_count == expected_chunks
   ```

4. **Table management (10 tests):**
   ```python
   def test_create_table(self, mocker):
       """Cover table creation (lines 300-350)."""
       mock_client = mocker.MagicMock()
       mock_table = mocker.MagicMock()
       mock_client.create_table.return_value = mock_table

       mocker.patch(
           "core.integration.lancedb_handler.lancedb.connect",
           return_value=mock_client
       )

       handler = LanceDBHandler(uri="memory://")
       handler.create_table(
           name="test_table",
           schema=[{"name": "id", "type": "string"}]
       )

       mock_client.create_table.assert_called_once()

   def test_delete_table(self, mocker):
       """Cover table deletion (lines 350-400)."""
       mock_client = mocker.MagicMock()
       mock_table = mocker.MagicMock()
       mock_client.open_table.return_value = mock_table
       mock_client.drop.return_value = None

       mocker.patch(
           "core.integration.lancedb_handler.lancedb.connect",
           return_value=mock_client
       )

       handler = LanceDBHandler(uri="memory://")
       handler.delete_table(name="test_table")

       mock_table.drop.assert_called_once()

   def test_table_exists(self, mocker):
       """Cover table existence check."""
       mock_client = mocker.MagicMock()
       mock_client.open_table.side_effect = Exception("Table not found")

       mocker.patch(
           "core.integration.lancedb_handler.lancedb.connect",
           return_value=mock_client
       )

       handler = LanceDBHandler(uri="memory://")
       exists = handler.table_exists(name="test_table")

       assert exists is False
   ```

5. **Knowledge graph operations (8 tests):**
   ```python
   def test_knowledge_graph_traversal(self, mocker):
       """Cover knowledge graph operations (lines 400-500)."""
       mock_client = mocker.MagicMock()
       mock_table = mocker.MagicMock()
       mock_client.open_table.return_value = mock_table
       mock_table.search.return_value = mock_table
       mock_table.where.return_value = mock_table
       mock_table.to_pandas.return_value = []

       mocker.patch(
           "core.integration.lancedb_handler.lancedb.connect",
           return_value=mock_client
       )

       handler = LanceDBHandler(uri="memory://")
       results = handler.knowledge_graph_traversal(
           entity_id="test-entity",
           max_depth=2
       )

       # Verify search was called for traversal
       assert mock_table.search.call_count > 0

   def test_related_entities_search(self, mocker):
       """Cover related entity search."""
       mock_client = mocker.MagicMock()
       mock_table = mocker.MagicMock()
       mock_client.open_table.return_value = mock_table
       mock_table.search.return_value = mock_table
       mock_table.to_pandas.return_value = [
           {"id": "entity-1", "relation": "related_to"},
           {"id": "entity-2", "relation": "depends_on"}
       ]

       mocker.patch(
           "core.integration.lancedb_handler.lancedb.connect",
           return_value=mock_client
       )

       handler = LanceDBHandler(uri="memory://")
       results = handler.find_related_entities(
           entity_id="test-entity",
           relation_type="related_to"
       )

       # Should filter by relation type
       mock_table.search.assert_called_once()
   ```

6. **Error handling (10 tests):**
   ```python
   def test_connection_error_handling(self, mocker):
       """Cover connection error handling."""
       mocker.patch(
           "core.integration.lancedb_handler.lancedb.connect",
           side_effect=Exception("Connection failed")
       )

       handler = LanceDBHandler(uri="invalid://uri")

       with pytest.raises(ConnectionError):
           handler.vector_search(query="test", limit=10)

   def test_invalid_query_handling(self, mocker):
       """Cover invalid query error handling."""
       mock_client = mocker.MagicMock()
       mock_table = mocker.MagicMock()
       mock_client.open_table.return_value = mock_table
       mock_table.search.side_effect = ValueError("Invalid query")

       mocker.patch(
           "core.integration.lancedb_handler.lancedb.connect",
           return_value=mock_client
       )

       handler = LanceDBHandler(uri="memory://")

       with pytest.raises(ValueError):
           handler.vector_search(query="", limit=10)

   def test_timeout_handling(self, mocker):
       """Cover timeout handling."""
       mock_client = mocker.MagicMock()
       mock_table = mocker.MagicMock()
       mock_client.open_table.return_value = mock_table
       mock_table.search.side_effect = TimeoutError("Query timeout")

       mocker.patch(
           "core.integration.lancedb_handler.lancedb.connect",
           return_value=mock_client
       )

       handler = LanceDBHandler(uri="memory://")

       with pytest.raises(TimeoutError):
           handler.vector_search(query="test", limit=10)
   ```

7. **Initialization and configuration (5 tests):**
   ```python
   @pytest.mark.parametrize("uri,expected_type", [
       ("memory://", "memory"),
       ("file:///tmp/test.db", "file"),
       ("s3://bucket/path", "s3"),
   ])
   def test_handler_initialization(self, uri, expected_type):
       """Cover handler initialization for different URI types."""
       handler = LanceDBHandler(uri=uri)

       assert handler.uri == uri
       assert handler.connection_type == expected_type

   def test_handler_with_custom_config(self):
       """Cover handler initialization with custom configuration."""
       handler = LanceDBHandler(
           uri="memory://",
           embedding_dimension=384,
           index_type="IVF_FLAT"
       )

       assert handler.embedding_dimension == 384
       assert handler.index_type == "IVF_FLAT"
   ```

**Total target: 60-70 tests (800-900 lines)** covering vector search, batch insert, table management, knowledge graph, error handling
  </action>
  <verify>pytest backend/tests/core/integration/test_lancedb_handler_coverage_extend.py -v --tb=short 2>&1 | grep -E "(test_.*PASSED|test_.*FAILED)" | head -30</verify>
  <done>60-70 tests created with simplified mocker.fixture patterns, targeting 75%+ coverage</done>
</task>

<task type="auto">
  <name>Task 2: Generate coverage report and verify pass rate improvement</name>
  <files>.planning/phases/194-coverage-push-18-22/194-02-coverage.json</files>
  <action>
Run coverage measurement and verify pass rate improvement:

1. **Generate coverage report:**
   ```bash
   cd /Users/rushiparikh/projects/atom/backend && \
   PYTHONPATH=/Users/rushiparikh/projects/atom/backend \
   pytest tests/core/integration/test_lancedb_handler_coverage_extend.py \
     --cov=core/integration/lancedb_handler \
     --cov-report=term-missing \
     --cov-report=json:/Users/rushiparikh/projects/atom/.planning/phases/194-coverage-push-18-22/194-02-coverage.json \
     -v \
     --tb=short
   ```

2. **Verify pass rate improvement:**
   - Phase 193 baseline: 27.4% pass rate (23/84 tests passing)
   - Phase 194 target: >80% pass rate
   - Key improvement: simplified mocks reduce test fragility

3. **Check coverage achieved:**
   ```bash
   cat .planning/phases/194-coverage-push-18-22/194-02-coverage.json | \
     python3 -c "
import json, sys
d = json.load(sys.stdin)
file_data = d['files'].get('core/integration/lancedb_handler.py', {})
summary = file_data.get('summary', {})
print(f\"Coverage: {summary.get('percent_covered', 0):.1f}%\")
print(f\"Statements: {summary.get('covered_lines', 0)}/{summary.get('num_statements', 0)}\")
"
   ```

4. **Document mock simplification:**
   - Record reduction in mock setup complexity
   - Note any remaining LanceDB-specific challenges
  </action>
  <verify>cat .planning/phases/194-coverage-push-18-22/194-02-coverage.json | python3 -c "import json,sys; d=json.load(sys.stdin); print(f\"Coverage: {d['files'].get('core/integration/lancedb_handler.py',{}).get('summary',{}).get('percent_covered',0):.1f}%\")"</verify>
  <done>Coverage report showing 75%+ coverage, pass rate >80% (vs 27.4% in Phase 193)</done>
</task>

</tasks>

<verification>
1. Extended test file created at backend/tests/core/integration/test_lancedb_handler_coverage_extend.py (800+ lines)
2. Tests use pytest-mock's mocker.fixture for simplified mock setup
3. MagicMock used for flexible mock configuration
4. mock_open used for file operations
5. Pass rate >80% (vs 27.4% in Phase 193)
6. Coverage 75%+ achieved (from 55% baseline)
7. 60-70 tests created
</verification>

<success_criteria>
- LanceDBHandler coverage: 55% -> 75%+ (+20 percentage points)
- Pass rate: 27.4% -> >80% (3x improvement)
- Mock complexity: Simplified using mocker.fixture and MagicMock
- Tests created: 60-70 tests
- Coverage report JSON generated
</success_criteria>

<output>
After completion, create `.planning/phases/194-coverage-push-18-22/194-02-SUMMARY.md` with:
- Coverage achieved (percent and statements)
- Pass rate improvement (27.4% -> actual)
- Mock simplification patterns used
- Key findings from LanceDB testing
</output>
