---
phase: 194-coverage-push-18-22
plan: 06
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/tests/core/agents/test_atom_meta_agent_coverage_extend.py
autonomous: true

must_haves:
  truths:
    - "AtomMetaAgent tests use realistic 70% target for complex async orchestration"
    - "Tests focus on testable helper methods, not complex ReAct loop"
    - "Coverage extends from 62% to 70%+ target (422 statements)"
    - "Pass rate maintains >80% quality standard"
  artifacts:
    - path: "backend/tests/core/agents/test_atom_meta_agent_coverage_extend.py"
      provides: "Extended AtomMetaAgent tests with realistic coverage target"
      min_lines: 700
      contains: "TestAtomMetaAgentCoverageExtend"
  key_links:
    - from: "backend/tests/core/agents/test_atom_meta_agent_coverage_extend.py"
      to: "backend/core/agents/atom_meta_agent.py"
      via: "from core.agents.atom_meta_agent import"
      pattern: "from core.agents.atom_meta_agent import AtomMetaAgent"
---

<objective>
Extend AtomMetaAgent coverage from 62% to 70%+ using realistic targets for complex async ReAct loop orchestration per Phase 194 research. Focus on testable helper methods (tool selection, memory integration, state management) rather than the complex execute() method.

Purpose: Achieve realistic coverage on complex meta-agent while accepting limitations
Output: Extended test file (700+ lines) with 70%+ coverage, >80% pass rate
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/194-coverage-push-18-22/194-RESEARCH.md
@.planning/phases/193-coverage-push-15-18/193-07-SUMMARY.md

# AtomMetaAgent Phase 193 blocker
@.planning/phases/193-coverage-push-15-18/193-07-SUMMARY.md

# Testing patterns
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend AtomMetaAgent tests focusing on testable methods</name>
  <files>backend/tests/core/agents/test_atom_meta_agent_coverage_extend.py</files>
  <action>
Extend existing test file, focusing on testable methods:

1. **Test file structure:**
   ```python
   """
   Coverage-driven tests for atom_meta_agent.py (62% -> 70%+ target)

   REALISTIC TARGET per Phase 194 research:
   - Accept 70% for complex async ReAct loop (not 85% from Phase 193)
   - Focus on testable helper methods
   - Skip complex execute() async orchestration (40 statements)
   - Document remaining async methods as requiring integration testing

   Coverage Target Areas (Testable):
   - Lines 1-80: Agent initialization and configuration
   - Lines 80-150: Tool selection and routing
   - Lines 150-220: Memory integration (episodes, world model)
   - Lines 220-280: State management (context, observations)
   - Lines 280-340: Reflection and self-correction
   - Lines 340-422: Error handling and edge cases
   - PARTIAL: execute() method (test setup/teardown, not full ReAct loop)
   """
   class TestAtomMetaAgentCoverageExtend:
   ```

2. **Agent initialization tests (8 tests):**
   ```python
   def test_agent_initialization_with_defaults(self, db_session):
       """Cover agent initialization (lines 1-80) with default config."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(
           agent_id="test-meta-agent",
           db_session=db_session
       )

       assert agent.agent_id == "test-meta-agent"
       assert agent.db_session == db_session
       assert agent.tools is not None
       assert agent.memory is not None

   def test_agent_initialization_with_custom_config(self, db_session):
       """Cover initialization with custom configuration."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       config = {
           "max_iterations": 10,
           "reflection_threshold": 0.7,
           "temperature": 0.5
       }

       agent = AtomMetaAgent(
           agent_id="test-meta-agent",
           db_session=db_session,
           config=config
       )

       assert agent.config["max_iterations"] == 10
       assert agent.config["reflection_threshold"] == 0.7

   @pytest.mark.parametrize("max_iterations,expected", [
       (1, 1),
       (5, 5),
       (10, 10),
   ])
   def test_max_iterations_config(self, db_session, max_iterations, expected):
       """Cover max iterations configuration."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(
           agent_id="test",
           db_session=db_session,
           config={"max_iterations": max_iterations}
       )

       assert agent.config["max_iterations"] == expected

   def test_agent_with_memory_integration(self, db_session):
       """Cover memory integration setup."""
       from core.agents.atom_meta_agent import AtomMetaAgent
       from unittest.mock import MagicMock

       mock_memory = MagicMock()
       agent = AtomMetaAgent(
           agent_id="test",
           db_session=db_session,
           memory=mock_memory
       )

       assert agent.memory == mock_memory
   ```

3. **Tool selection tests (12 tests):**
   ```python
   def test_select_tool_for_query(self, db_session):
       """Cover tool selection (lines 80-150) for simple queries."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       # Query that should select a specific tool
       selected_tool = agent.select_tool("What's the weather like?")

       assert selected_tool is not None
       # Would select weather-related tool if available

   @pytest.mark.parametrize("query,expected_tool_type", [
       ("search for information", "search"),
       ("calculate 2 + 2", "calculator"),
       ("create a chart", "canvas"),
       ("browse the web", "browser"),
   ])
   def test_tool_selection_for_various_queries(self, db_session, query, expected_tool_type):
       """Cover tool selection for various query types."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       selected = agent.select_tool(query)

       # Verify tool type matches query intent
       if selected:
           assert expected_tool_type in selected.tool_type.lower() or selected.tool_type.lower() in expected_tool_type

   def test_select_tool_with_no_match(self, db_session):
       """Cover tool selection when no tool matches."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       # Ambiguous or nonsense query
       selected = agent.select_tool("xyz123")

       # Should return general purpose tool or None
       assert selected is None or selected.tool_type == "general"

   def test_get_available_tools(self, db_session):
       """Cover listing available tools."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       tools = agent.get_available_tools()

       assert isinstance(tools, list)
       assert len(tools) > 0

   def test_register_custom_tool(self, db_session):
       """Cover registering a custom tool."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       custom_tool = {
           "name": "custom_tool",
           "type": "custom",
           "description": "A custom tool",
           "function": lambda x: x
       }

       agent.register_tool(custom_tool)

       assert "custom_tool" in [t["name"] for t in agent.get_available_tools()]

   def test_tool_routing_with_maturity_check(self, db_session):
       """Cover tool routing with governance maturity check."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(
           agent_id="test",
           db_session=db_session,
           config={"maturity_level": "INTERN"}
       )

       # INTERN agents should be able to use basic tools
       can_use = agent.can_use_tool("basic_tool")

       # Result depends on governance configuration
       assert isinstance(can_use, bool)
   ```

4. **Memory integration tests (10 tests):**
   ```python
   def test_retrieve_from_episodic_memory(self, db_session):
       """Cover episodic memory retrieval (lines 150-220)."""
       from core.agents.atom_meta_agent import AtomMetaAgent
       from unittest.mock import MagicMock, patch

       mock_memory = MagicMock()
       mock_memory.retrieve_temporal.return_value = [
           {"episode_id": "ep-1", "content": "Previous context"}
       ]

       agent = AtomMetaAgent(
           agent_id="test",
           db_session=db_session,
           memory=mock_memory
       )

       episodes = agent.retrieve_relevant_episodes(agent_id="test", query="context")

       assert len(episodes) > 0
       mock_memory.retrieve_temporal.assert_called_once()

   def test_store_to_episodic_memory(self, db_session):
       """Cover storing to episodic memory."""
       from core.agents.atom_meta_agent import AtomMetaAgent
       from unittest.mock import MagicMock

       mock_memory = MagicMock()
       agent = AtomMetaAgent(
           agent_id="test",
           db_session=db_session,
           memory=mock_memory
       )

       agent.store_episode(
           agent_id="test",
           content="Test episode",
           metadata={"type": "test"}
       )

       mock_memory.create_episode.assert_called_once()

   def test_query_world_model(self, db_session):
       """Cover world model queries."""
       from core.agents.atom_meta_agent import AtomMetaAgent
       from unittest.mock import MagicMock

       mock_world_model = MagicMock()
       mock_world_model.recall.return_value = {
           "facts": ["fact1", "fact2"]
       }

       agent = AtomMetaAgent(
           agent_id="test",
           db_session=db_session,
           world_model=mock_world_model
       )

       facts = agent.query_world_model(query="test query")

       assert "facts" in facts
       mock_world_model.recall.assert_called_once()

   @pytest.mark.parametrize("memory_type,method", [
       ("episodic", "retrieve_relevant_episodes"),
       ("semantic", "semantic_search"),
       ("world_model", "query_world_model"),
   ])
   def test_access_different_memory_types(self, db_session, memory_type, method):
       """Cover accessing different memory types."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       # Verify memory access methods exist
       assert hasattr(agent, method)

   def test_memory_integration_with_context(self, db_session):
       """Cover memory integration with agent context."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       context = agent.build_context_from_memory(
           agent_id="test",
           query="test",
           include_episodes=True,
           include_world_model=True
       )

       # Context should include memory information
       assert isinstance(context, dict)
       assert "episodes" in context or "world_model" in context
   ```

5. **State management tests (12 tests):**
   ```python
   def test_update_agent_context(self, db_session):
       """Cover context update (lines 220-280)."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       agent.update_context(key="task", value="Complete the report")

       context = agent.get_context()

       assert context["task"] == "Complete the report"

   def test_add_observation(self, db_session):
       """Cover adding observations."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       agent.add_observation("User requested a summary")

       observations = agent.get_observations()

       assert len(observations) > 0
       assert "User requested a summary" in observations

   def test_get_observations_with_limit(self, db_session):
       """Cover retrieving observations with limit."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       for i in range(10):
           agent.add_observation(f"Observation {i}")

       # Get last 5 observations
       recent = agent.get_observations(limit=5)

       assert len(recent) == 5

   def test_clear_observations(self, db_session):
       """Cover clearing observations."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       agent.add_observation("Test")
       agent.clear_observations()

       observations = agent.get_observations()

       assert len(observations) == 0

   @pytest.mark.parametrize("state,expected", [
       ("idle", True),
       ("thinking", True),
       ("executing", True),
       ("invalid", False),
   ])
   def test_validate_agent_state(self, db_session, state, expected):
       """Cover validation of agent states."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       if expected:
           agent.set_state(state)
           assert agent.get_state() == state
       else:
           with pytest.raises(ValueError):
               agent.set_state(state)

   def test_state_transitions(self, db_session):
       """Cover valid state transitions."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       # Valid transition: idle -> thinking
       agent.set_state("idle")
       agent.set_state("thinking")

       assert agent.get_state() == "thinking"

   def test_get_execution_history(self, db_session):
       """Cover retrieving execution history."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       # Add some history entries
       agent.add_history_entry("action", "tool_call", {"tool": "search"})

       history = agent.get_execution_history()

       assert len(history) > 0
       assert history[0]["type"] == "action"
   ```

6. **Reflection tests (8 tests):**
   ```python
   def test_reflection_on_result(self, db_session):
       """Cover reflection logic (lines 280-340)."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(
           agent_id="test",
           db_session=db_session,
           config={"reflection_threshold": 0.7}
       )

       # Simulate a result that needs reflection
       reflection = agent.reflect_on_result(
           result="Partial answer obtained",
          confidence=0.5
       )

       # Should trigger reflection when confidence below threshold
       assert reflection["should_refine"] is True

   def test_reflection_with_high_confidence(self, db_session):
       """Cover reflection when confidence is high."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(
           agent_id="test",
           db_session=db_session,
           config={"reflection_threshold": 0.7}
       )

       reflection = agent.reflect_on_result(
           result="Complete answer",
           confidence=0.9
       )

       # Should not refine when confidence above threshold
       assert reflection["should_refine"] is False

   @pytest.mark.parametrize("confidence,threshold,should_refine", [
       (0.5, 0.7, True),
       (0.8, 0.7, False),
       (0.7, 0.7, False),  # Equal to threshold
   ])
   def test_reflection_threshold_logic(self, db_session, confidence, threshold, should_refine):
       """Cover reflection threshold logic."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(
           agent_id="test",
           db_session=db_session,
           config={"reflection_threshold": threshold}
       )

       reflection = agent.reflect_on_result(
           result="Test",
           confidence=confidence
       )

       assert reflection["should_refine"] == should_refine

   def test_self_correction_plan(self, db_session):
       """Cover self-correction planning."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       correction = agent.plan_self_correction(
           error="Tool returned unexpected result",
           context="Trying to calculate sum"
       )

       assert "action" in correction
       assert correction["action"] in ["retry", "alternate_tool", "request_clarification"]

   def test_learn_from_execution(self, db_session):
       """Cover learning from execution for future improvement."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       agent.learn_from_execution(
           tool="search",
           outcome="success",
           feedback="Relevant results found"
       )

       # Learning should be stored
       assert agent.has_learned_about("search")
   ```

7. **Error handling tests (6 tests):**
   ```python
   def test_handle_tool_failure(self, db_session):
       """Cover tool failure handling (lines 340-422)."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       error = agent.handle_tool_error(
           tool="search",
           error="Connection timeout"
       )

       assert error["handled"] is True
       assert "retry" in error or "fallback" in error

   def test_handle_memory_failure(self, db_session):
       """Cover memory retrieval failure handling."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       result = agent.handle_memory_failure(
           memory_type="episodic",
           error="Database connection failed"
       )

       # Should provide fallback or error recovery
       assert "fallback" in result or "retry" in result

   def test_graceful_degradation(self, db_session):
       """Cover graceful degradation when services unavailable."""
       from core.agents.atom_meta_agent import AtomMetaAgent

       agent = AtomMetaAgent(agent_id="test", db_session=db_session)

       # Simulate multiple services failing
       agent.register_service_status("memory", "unavailable")
       agent.register_service_status("world_model", "unavailable")

       # Agent should still function with limited capabilities
       can_operate = agent.can_operate()

       assert can_operate is True  # Degrades gracefully
   ```

**Total target: 56-66 tests (700-800 lines)** covering initialization, tool selection, memory integration, state management, reflection, error handling
**Note:** Accept 70% coverage as realistic target per Phase 194 research. Complex execute() method requires integration testing.
  </action>
  <verify>pytest backend/tests/core/agents/test_atom_meta_agent_coverage_extend.py -v --tb=short 2>&1 | grep -E "(test_.*PASSED|test_.*FAILED)" | head -30</verify>
  <done>56-66 tests created focusing on testable methods, targeting 70%+ coverage</done>
</task>

<task type="auto">
  <name>Task 2: Generate coverage report and document async method limitations</name>
  <files>.planning/phases/194-coverage-push-18-22/194-06-coverage.json</files>
  <action>
Run coverage measurement and document async method limitations:

1. **Generate coverage report:**
   ```bash
   cd /Users/rushiparikh/projects/atom/backend && \
   PYTHONPATH=/Users/rushiparikh/projects/atom/backend \
   pytest tests/core/agents/test_atom_meta_agent_coverage_extend.py \
     --cov=core/agents/atom_meta_agent \
     --cov-report=term-missing \
     --cov-report=json:/Users/rushiparikh/projects/atom/.planning/phases/194-coverage-push-18-22/194-06-coverage.json \
     -v \
     --tb=short
   ```

2. **Check coverage achieved:**
   ```bash
   cat .planning/phases/194-coverage-push-18-22/194-06-coverage.json | \
     python3 -c "
import json, sys
d = json.load(sys.stdin)
file_data = d['files'].get('core/agents/atom_meta_agent.py', {})
summary = file_data.get('summary', {})
print(f\"Coverage: {summary.get('percent_covered', 0):.1f}%\")
print(f\"Statements: {summary.get('covered_lines', 0)}/{summary.get('num_statements', 0)}\")
"
   ```

3. **Document async method limitations:**
   - Identify execute() method and complex ReAct loop
   - Note: Requires integration testing with real async execution
   - Document as Phase 195+ work
   - Record realistic 70% target acceptance per Phase 194 research

4. **Verify pass rate:**
   - Target: >80% pass rate
   - Compare to Phase 193: 94.1% pass rate (but only extended from 62%)
  </action>
  <verify>cat .planning/phases/194-coverage-push-18-22/194-06-coverage.json | python3 -c "import json,sys; d=json.load(sys.stdin); print(f\"Coverage: {d['files'].get('core/agents/atom_meta_agent.py',{}).get('summary',{}).get('percent_covered',0):.1f}%\")"</verify>
  <done>Coverage report showing 70%+ coverage, pass rate >80%, async method limitations documented</done>
</task>

</tasks>

<verification>
1. Extended test file created at backend/tests/core/agents/test_atom_meta_agent_coverage_extend.py (700+ lines)
2. Tests focus on testable methods (initialization, tool selection, memory, state, reflection)
3. Complex async execute() method partially tested
4. Pass rate >80%
5. Coverage 70%+ achieved (from 62% baseline)
6. Async method limitations documented
7. 56-66 tests created
8. Realistic target acceptance per research
</verification>

<success_criteria>
- AtomMetaAgent coverage: 62% -> 70%+ (+8 percentage points)
- Pass rate: >80%
- Realistic 70% target for complex async orchestration
- Integration test needs documented
- Tests created: 56-66 tests
</success_criteria>

<output>
After completion, create `.planning/phases/194-coverage-push-18-22/194-06-SUMMARY.md` with:
- Coverage achieved (percent and statements)
- Pass rate achieved
- Async method limitations documented
- Realistic target justification
</output>
