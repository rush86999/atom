---
phase: 147-cross-platform-property-testing
plan: 02
type: execute
wave: 2
depends_on: [147-01]
files_modified:
  - frontend-nextjs/tests/property/shared-invariants.test.ts
  - mobile/src/__tests__/property/shared-invariants.test.ts
  - mobile/src/shared/property-tests (symlink)
  - frontend-nextjs/src-tauri/tests/shared_property_tests/README.md
  - frontend-nextjs/src-tauri/tests/state_machine_proptest.rs
  - frontend-nextjs/src-tauri/tests/serialization_proptest.rs
  - frontend-nextjs/jest.config.js
  - mobile/jest.config.js
autonomous: true

must_haves:
  truths:
    - "Frontend test file imports and asserts shared property tests"
    - "Mobile SYMLINK points to frontend shared property tests directory"
    - "Mobile test file imports and asserts shared property tests via SYMLINK"
    - "Desktop has Rust proptest equivalents documenting correspondence to TypeScript properties"
    - "All platforms can run property tests independently"
  artifacts:
    - path: frontend-nextjs/tests/property/shared-invariants.test.ts
      provides: Frontend test runner for shared property tests
      imports: [canvasStateMachineProperty, maturityMonotonicProgression, jsonRoundtripPreservesData]
    - path: mobile/src/shared/property-tests
      provides: SYMLINK to frontend shared property tests
      symlink_target: ../../frontend-nextjs/shared/property-tests
    - path: mobile/src/__tests__/property/shared-invariants.test.ts
      provides: Mobile test runner for shared property tests
      imports: [@atom/property-tests or ../../frontend-nextjs/shared/property-tests]
    - path: frontend-nextjs/src-tauri/tests/shared_property_tests/README.md
      provides: Documentation for Rust-TypeScript correspondence
      contains: Mapping between Rust proptests and TypeScript FastCheck properties
    - path: frontend-nextjs/src-tauri/tests/state_machine_proptest.rs
      provides: Rust equivalent of canvas/agent state machine tests
      documents_correspondence: canvas-invariants.ts, agent-maturity-invariants.ts
    - path: frontend-nextjs/src-tauri/tests/serialization_proptest.rs
      provides: Rust equivalent of serialization tests
      documents_correspondence: serialization-invariants.ts
  key_links:
    - from: mobile/src/shared/property-tests
      to: frontend-nextjs/shared/property-tests
      via: SYMLINK
      pattern: ln -s ../../frontend-nextjs/shared/property-tests
    - from: mobile/src/__tests__/property/shared-invariants.test.ts
      to: frontend-nextjs/shared/property-tests
      via: import path (SYMLINK or relative)
      pattern: import.*from.*property-tests
    - from: frontend-nextjs/src-tauri/tests/state_machine_proptest.rs
      to: frontend-nextjs/shared/property-tests
      via: documentation comments
      pattern: Corresponds to: frontend-nextjs/shared/property-tests/
---

<objective>
Distribute shared property tests via SYMLINK strategy and create platform-specific test runners.

**Purpose:** Enable frontend, mobile, and desktop platforms to use the same property test invariants via SYMLINK (TypeScript) and documentation (Rust), ensuring consistent invariant validation across all platforms.

**Output:**
- Frontend test file that imports and asserts shared property tests
- Mobile SYMLINK to frontend shared property tests directory
- Mobile test file that imports shared tests via SYMLINK
- Rust proptest files with correspondence documentation to TypeScript properties
- README documenting Rust-TypeScript pattern mapping
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/147-cross-platform-property-testing/147-RESEARCH.md
@.planning/phases/147-cross-platform-property-testing/147-01-PLAN.md
@.planning/phases/144-cross-platform-shared-util*/144-*-SUMMARY.md
@frontend-nextjs/tests/property/agent-state-machine-invariants.test.ts
@mobile/src/__tests__/property/queueInvariants.test.ts
@frontend-nextjs/src-tauri/tests/ipc_serialization_proptest.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create frontend test file for shared property tests</name>
  <files>frontend-nextjs/tests/property/shared-invariants.test.ts</files>
  <action>
Create frontend-nextjs/tests/property/shared-invariants.test.ts with:

1. Import all shared properties from @atom/property-tests
2. Import fc from 'fast-check' for assertions
3. Import describe/it from '@jest/globals'

4. Test suite: "Shared Cross-Platform Property Invariants (Frontend)"

5. Test: "canvas state machine transitions are valid"
   - Asserts canvasStateMachineProperty
   - Uses PROPERTY_TEST_CONFIG from config

6. Test: "canvas cannot transition from presenting to idle directly"
   - Asserts canvasNoDirectPresentingToIdle

7. Test: "canvas error state can recover to idle"
   - Asserts canvasErrorRecoveryToIdle

8. Test: "canvas terminal states lead to idle"
   - Asserts canvasTerminalStatesLeadToIdle

9. Test: "agent maturity progression is monotonic"
   - Asserts maturityMonotonicProgression

10. Test: "AUTONOMOUS is terminal maturity level"
    - Asserts autonomousIsTerminal

11. Test: "STUDENT cannot skip directly to AUTONOMOUS"
    - Asserts studentCannotSkipToAutonomous

12. Test: "agent maturity transitions are always forward"
    - Asserts maturityTransitionsAreForward

13. Test: "JSON roundtrip preserves data"
    - Asserts jsonRoundtripPreservesData

14. Test: "agent data roundtrip preserves all fields"
    - Asserts agentDataRoundtrip

15. Test: "canvas data roundtrip preserves state"
    - Asserts canvasDataRoundtrip

16. Test: "array order is preserved in JSON roundtrip"
    - Asserts arrayOrderPreserved

Pattern from existing frontend property test:
```typescript
import fc from 'fast-check';
import { describe, it, expect } from '@jest/globals';
import {
  canvasStateMachineProperty,
  canvasNoDirectPresentingToIdle,
  // ... all other imports
} from '@atom/property-tests';
import { PROPERTY_TEST_CONFIG } from '@atom/property-tests';

describe('Shared Cross-Platform Property Invariants (Frontend)', () => {
  it('canvas state machine transitions are valid', () => {
    fc.assert(canvasStateMachineProperty, PROPERTY_TEST_CONFIG);
  });

  it('canvas cannot transition from presenting to idle directly', () => {
    fc.assert(canvasNoDirectPresentingToIdle, PROPERTY_TEST_CONFIG);
  });

  // ... continue for all 12 properties
});
```

Note: Uses @atom/property-tests import path from tsconfig.json paths configuration.
  </action>
  <verify>
Test file created at frontend-nextjs/tests/property/shared-invariants.test.ts
All 12 properties imported and asserted
Test suite uses describe/it pattern
  </verify>
  <done>Frontend test file created with 12 property test assertions</done>
</task>

<task type="auto">
  <name>Task 2: Create SYMLINK from mobile to frontend shared property tests</name>
  <files>mobile/src/shared/property-tests (symlink)</files>
  <action>
Create SYMLINK: mobile/src/shared/property-tests → ../../frontend-nextjs/shared/property-tests

From mobile directory:
```bash
cd /Users/rushiparikh/projects/atom/mobile/src
ln -s ../../frontend-nextjs/shared/property-tests property-tests
```

Verify SYMLINK created:
```bash
ls -la mobile/src/shared/property-tests
# Should show: property-tests -> ../../frontend-nextjs/shared/property-tests
```

Pattern from Phase 144 mobile shared SYMLINK:
- mobile/src/shared → ../frontend-nextjs/shared
- New SYMLINK extends existing pattern
- Relative path works across platforms

Note: The existing mobile/src/shared SYMLINK was created in Phase 144.
This new SYMLINK extends the pattern to include property-tests subdirectory.
  </action>
  <verify>
SYMLINK exists at mobile/src/shared/property-tests
SYMLINK target points to frontend-nextjs/shared/property-tests
ls -la shows relative path correctly
  </verify>
  <done>Mobile SYMLINK created pointing to frontend shared property tests</done>
</task>

<task type="auto">
  <name>Task 3: Create mobile test file for shared property tests</name>
  <files>mobile/src/__tests__/property/shared-invariants.test.ts</files>
  <action>
Create mobile/src/__tests__/property/shared-invariants.test.ts with:

1. Import all shared properties from relative path via SYMLINK
2. Import fc from 'fast-check' for assertions
3. Import test from '@jest/globals' (React Native pattern)

4. Test suite: "Shared Cross-Platform Property Invariants (Mobile)"

5. Same 12 test cases as frontend (Task 1), but using mobile-specific imports

Pattern from existing mobile property test (queueInvariants.test.ts):
```typescript
import fc from 'fast-check';
import test from '@jest/globals';
import {
  canvasStateMachineProperty,
  // ... all other imports
} from '../../shared/property-tests'; // Via SYMLINK

test('canvas state machine transitions are valid (Mobile)', () => {
  fc.assert(canvasStateMachineProperty);
});

// ... continue for all 12 properties
```

OR use describe/it pattern (if jest/globals supports it):
```typescript
import fc from 'fast-check';
import { describe, it, expect } from '@jest/globals';

describe('Shared Cross-Platform Property Invariants (Mobile)', () => {
  it('canvas state machine transitions are valid', () => {
    fc.assert(canvasStateMachineProperty);
  });
  // ... continue
});
```

Use test() if describe() not available in jest-expo.
  </action>
  <verify>
Test file created at mobile/src/__tests__/property/shared-invariants.test.ts
Imports resolve via SYMLINK (../../shared/property-tests)
All 12 properties imported and asserted
  </verify>
  <done>Mobile test file created with 12 property test assertions via SYMLINK</done>
</task>

<task type="auto">
  <name>Task 4: Create Rust state machine proptests with correspondence documentation</name>
  <files>frontend-nextjs/src-tauri/tests/state_machine_proptest.rs</files>
  <action>
Create frontend-nextjs/src-tauri/tests/state_machine_proptest.rs with:

1. Module documentation explaining correspondence to TypeScript properties:
   - "Canvas state machine invariants (mirrors TypeScript canvas-invariants.ts)"
   - "Agent maturity state machine invariants (mirrors TypeScript agent-maturity-invariants.ts)"
   - "Corresponds to: frontend-nextjs/shared/property-tests/"

2. CanvasState enum: Idle, Presenting, Submitted, Closed, Error

3. AgentMaturityLevel enum: Student, Intern, Supervised, Autonomous

4. Proptest: prop_canvas_state_machine_transitions
   - Tests valid state transitions
   - Corresponds to: canvasStateMachineProperty in canvas-invariants.ts
   - Uses valid_transitions match table

5. Proptest: prop_canvas_no_presenting_to_idle
   - Tests that Presenting cannot transition to Idle
   - Corresponds to: canvasNoDirectPresentingToIdle

6. Proptest: prop_canvas_error_to_idle
   - Tests that Error can transition to Idle
   - Corresponds to: canvasErrorRecoveryToIdle

7. Proptest: prop_maturity_monotonic_progression
   - Tests that maturity only increases
   - Corresponds to: maturityMonotonicProgression

8. Proptest: prop_autonomous_is_terminal
   - Tests that Autonomous has no higher levels
   - Corresponds to: autonomousIsTerminal

Pattern from existing desktop proptest (ipc_serialization_proptest.rs):
```rust
/// Canvas state machine invariants
///
/// Pattern: idle -> presenting -> (submitted | closed) -> idle
/// Any state -> error (on failure)
///
/// Corresponds to: frontend-nextjs/shared/property-tests/canvas-invariants.ts
use proptest::prelude::*;

#[derive(Debug, Clone, Copy, PartialEq)]
enum CanvasState {
    Idle,
    Presenting,
    Submitted,
    Closed,
    Error,
}

proptest! {
    #[test]
    fn prop_canvas_state_machine_transitions(
        from_state in prop::sample::select(vec![...]),
        to_state in prop::sample::select(vec![...]),
    ) {
        // Corresponds to: canvasStateMachineProperty
        let valid = match from_state {
            CanvasState::Idle => vec![CanvasState::Presenting, CanvasState::Error],
            // ... other transitions
        };
        prop_assert!(valid.contains(&to_state) || !valid.contains(&to_state));
    }
}
```

Note: Rust tests use proptest! macro, not direct function calls.
Correspondence documented in comments, not via imports (Rust cannot import .ts files).
  </action>
  <verify>
Rust proptest file created with 5 state machine tests
Correspondence comments link to TypeScript properties
Uses proptest! macro pattern
  </verify>
  <done>Rust state machine proptests created with correspondence documentation</done>
</task>

<task type="auto">
  <name>Task 5: Create Rust serialization proptests with correspondence documentation</name>
  <files>frontend-nextjs/src-tauri/tests/serialization_proptest.rs</files>
  <action>
Create frontend-nextjs/src-tauri/tests/serialization_proptest.rs with:

1. Module documentation explaining correspondence to TypeScript properties:
   - "Serialization roundtrip invariants (mirrors TypeScript serialization-invariants.ts)"
   - "Corresponds to: frontend-nextjs/shared/property-tests/serialization-invariants.ts"

2. Proptest: prop_json_roundtrip_preserves_data
   - Tests that serde_json (de)serialization preserves data
   - Uses any::&lt;Value&gt;() for arbitrary JSON
   - Corresponds to: jsonRoundtripPreservesData

3. Proptest: prop_agent_data_roundtrip
   - Tests that Agent data structure survives JSON roundtrip
   - Uses Agent struct with specific fields
   - Corresponds to: agentDataRoundtrip

4. Proptest: prop_array_order_preserved
   - Tests that Vec order is preserved
   - Uses vec for arbitrary arrays
   - Corresponds to: arrayOrderPreserved

5. Helper: deep_equals function (if needed for nested comparisons)

Pattern from existing desktop proptest (ipc_serialization_proptest.rs):
```rust
/// JSON roundtrip invariants
///
/// Corresponds to: frontend-nextjs/shared/property-tests/serialization-invariants.ts
use proptest::prelude::*;
use serde_json::Value;

proptest! {
    #[test]
    fn prop_json_roundtrip_preserves_data(input in any::<Value>()) {
        // Corresponds to: jsonRoundtripPreservesData
        let serialized = serde_json::to_string(&input).unwrap();
        let deserialized: Value = serde_json::from_str(&serialized).unwrap();
        prop_assert_eq!(input, deserialized);
    }
}

#[derive(Serialize, Deserialize, Debug, PartialEq)]
struct Agent {
    id: String,
    name: String,
    maturity_level: String,
    is_active: bool,
}

proptest! {
    #[test]
    fn prop_agent_data_roundtrip(
        id in "[a-z0-9]{32}",
        name in "[a-zA-Z]{5,50}",
        maturity_level in prop::sample::select(vec!["STUDENT", "INTERN", "SUPERVISED", "AUTONOMOUS"]),
        is_active in prop::bool::ANY,
    ) {
        // Corresponds to: agentDataRoundtrip
        let agent = Agent { id, name, maturity_level, is_active };
        let serialized = serde_json::to_string(&agent).unwrap();
        let deserialized: Agent = serde_json::from_str(&serialized).unwrap();
        prop_assert_eq!(agent, deserialized);
    }
}
```

Note: Rust uses Strategy-based generation (different from FastCheck arbitraries).
Correspondence documented in comments, not via code imports.
  </action>
  <verify>
Rust proptest file created with 3 serialization tests
Correspondence comments link to TypeScript properties
Uses serde_json for JSON roundtrip
  </verify>
  <done>Rust serialization proptests created with correspondence documentation</done>
</task>

<task type="auto">
  <name>Task 6: Create correspondence documentation README for Rust-TypeScript mapping</name>
  <files>frontend-nextjs/src-tauri/tests/shared_property_tests/README.md</files>
  <action>
Create directory: frontend-nextjs/src-tauri/tests/shared_property_tests/

Create frontend-nextjs/src-tauri/tests/shared_property_tests/README.md with:

1. Section: "Rust-TypeScript Property Test Correspondence"
   - Explains that Rust cannot import TypeScript tests directly
   - Both platforms verify same invariants using native PBT frameworks

2. Table: "Property Mapping"
   | TypeScript Property | Rust Proptest | Module | Description |
   |---------------------|--------------|--------|-------------|
   | canvasStateMachineProperty | prop_canvas_state_machine_transitions | state_machine_proptest.rs | Canvas state transitions are valid |
   | canvasNoDirectPresentingToIdle | prop_canvas_no_presenting_to_idle | state_machine_proptest.rs | Presenting cannot go to Idle |
   | maturityMonotonicProgression | prop_maturity_monotonic_progression | state_machine_proptest.rs | Maturity only increases |
   | autonomousIsTerminal | prop_autonomous_is_terminal | state_machine_proptest.rs | AUTONOMOUS is final level |
   | jsonRoundtripPreservesData | prop_json_roundtrip_preserves_data | serialization_proptest.rs | JSON roundtrip preserves data |
   | agentDataRoundtrip | prop_agent_data_roundtrip | serialization_proptest.rs | Agent data survives serialization |

3. Section: "Framework Differences"
   - FastCheck (TypeScript): Uses fc.property(), fc.assert()
   - proptest (Rust): Uses proptest! macro, prop_assert! macro
   - Generation: FastCheck uses arbitraries (fc.string()), proptest uses strategies (any::&lt;T&gt;())

4. Section: "Why Not SYMLINK for Rust?"
   - Rust cannot import TypeScript files (different compilation pipelines)
   - Different type systems (Rust vs TypeScript)
   - Documented correspondence provides same invariant validation

5. Section: "Running Property Tests"
   - Frontend: npm test -- shared-invariants
   - Mobile: npm test -- shared-invariants
   - Desktop: cargo test state_machine_proptest serialization_proptest

6. Section: "Adding New Properties"
   - Create TypeScript property in shared/property-tests/
   - Create corresponding Rust proptest in tests/
   - Update this README with mapping

Pattern from Phase 144 shared/test-utils/README.md:
- Comprehensive documentation explaining cross-platform usage
- Tables for quick reference
- Code examples for each platform
  </action>
  <verify>
README created with property mapping table
Framework differences documented
Running instructions for all platforms included
  </verify>
  <done>Correspondence documentation README created</done>
</task>

<task type="auto">
  <name>Task 7: Configure Jest for mobile property test discovery</name>
  <files>mobile/jest.config.js</files>
  <action>
Modify mobile/jest.config.js:
- Add testMatch pattern for shared property tests: '**/shared/property-tests/**/*.test.ts'
- Place after existing testMatch patterns
- Pattern ensures Jest discovers property tests imported via SYMLINK

Note: SYMLINK at mobile/src/shared/property-tests → frontend-nextjs/shared/property-tests
Jest discovers tests via testMatch pattern, not via SYMLINK directly.

Also add moduleNameMapper if needed for @atom/property-tests imports:
```javascript
moduleNameMapper: {
  '@atom/property-tests(.*)$': '<rootDir>/src/shared/property-tests$1',
  // ... other mappings
}
```

Pattern from Phase 144 (mobile Jest configuration):
- moduleNameMapper for @atom/test-utils
- testMatch patterns for test discovery

Verify configuration by running:
```bash
cd mobile
npm test -- --listTests | grep shared-invariants
```
Should show: mobile/src/__tests__/property/shared-invariants.test.ts
  </action>
  <verify>
Jest testMatch includes shared/property-tests/**/*.test.ts
moduleNameMapper includes @atom/property-tests if used
npm test -- --listTests shows shared-invariants.test.ts
  </verify>
  <done>Mobile Jest configured for shared property test discovery</done>
</task>

</tasks>

<verification>
After plan completion:
1. Frontend: Run npm test -- shared-invariants
   - Should execute 12 property tests, all passing
2. Mobile: Run npm test -- shared-invariants
   - Should execute 12 property tests via SYMLINK, all passing
3. Desktop: Run cargo test state_machine_proptest serialization_proptest
   - Should execute 8 Rust proptests, all passing
4. Verify SYMLINK: ls -la mobile/src/shared/property-tests
   - Should show: property-tests -> ../../frontend-nextjs/shared/property-tests
5. Verify correspondence: README.md has complete property mapping table
6. Verify imports: All test files can import shared properties without errors
</verification>

<success_criteria>
1. Frontend test file imports and asserts all 12 shared property tests
2. Mobile SYMLINK points to frontend shared property tests directory
3. Mobile test file imports shared tests via SYMLINK and asserts all 12 properties
4. Rust proptest files (2) created with correspondence documentation
5. README.md documents complete Rust-TypeScript property mapping
6. All platforms can run property tests independently and pass
7. Jest configuration updated for mobile test discovery
</success_criteria>

<output>
After completion, create `.planning/phases/147-cross-platform-property-testing/147-02-SUMMARY.md` with:
- Files created (6 files: frontend test, mobile test, 2 Rust proptests, README, SYMLINK)
- Files modified (1 file: mobile jest.config.js)
- Property tests distributed via SYMLINK
- Rust correspondence documentation
- Commits made (7 commits, one per task)
- Deviations (if any)
- Next steps for Plan 03 (cross-platform result aggregation)
</output>
