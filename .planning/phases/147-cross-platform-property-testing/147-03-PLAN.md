---
phase: 147-cross-platform-property-testing
plan: 03
type: execute
wave: 3
depends_on: [147-02]
files_modified:
  - backend/tests/scripts/aggregate_property_tests.py
  - backend/tests/test_aggregate_property_tests.py
  - backend/tests/coverage_reports/metrics/property_test_results.json
  - .github/workflows/cross-platform-property-tests.yml
autonomous: true

must_haves:
  truths:
    - "Aggregation script combines FastCheck (frontend/mobile) and proptest (desktop) results"
    - "Script outputs unified JSON with platform breakdown and pass rate"
    - "Unit tests verify parsing logic for all three platforms"
    - "CI/CD workflow runs property tests in parallel and aggregates results"
    - "PR comments include property test results with platform breakdown"
  artifacts:
    - path: backend/tests/scripts/aggregate_property_tests.py
      provides: Cross-platform property test result aggregation
      exports: [parse_jest_xml, parse_proptest_json, aggregate_results, main]
    - path: backend/tests/test_aggregate_property_tests.py
      provides: Unit tests for aggregation script
      contains: [30+ tests covering parsing, aggregation, CLI, E2E]
    - path: .github/workflows/cross-platform-property-tests.yml
      provides: CI/CD workflow for cross-platform property test execution
      jobs: [frontend-property-tests, mobile-property-tests, desktop-property-tests, aggregate-results]
    - path: backend/tests/coverage_reports/metrics/property_test_results.json
      provides: Aggregated property test results storage
      contains: [platforms, pass_rate, total, passed, failed, timestamp]
  key_links:
    - from: .github/workflows/cross-platform-property-tests.yml
      to: backend/tests/scripts/aggregate_property_tests.py
      via: workflow job execution
      pattern: aggregate_property_tests.py.*--frontend.*--mobile.*--desktop
    - from: backend/tests/scripts/aggregate_property_tests.py
      to: backend/tests/coverage_reports/metrics/property_test_results.json
      via: JSON output
      pattern: property_test_results.json
---

<objective>
Create cross-platform property test result aggregation and CI/CD integration.

**Purpose:** Aggregate FastCheck (frontend/mobile) and proptest (desktop) results into unified cross-platform report with platform breakdown, enabling visibility into property test health across all platforms.

**Output:**
- Aggregation script that combines Jest JUnit XML (frontend/mobile) and proptest JSON (desktop)
- Unit tests for aggregation script (30+ tests)
- CI/CD workflow that runs property tests in parallel and aggregates results
- PR comment integration showing property test results with platform breakdown
- Historical tracking of property test results over time
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/147-cross-platform-property-testing/147-RESEARCH.md
@.planning/phases/147-cross-platform-property-testing/147-02-PLAN.md
@.planning/phases/146-cross-platform-weigh*/146-*-SUMMARY.md
@backend/tests/scripts/cross_platform_coverage_gate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create property test result aggregation script</name>
  <files>backend/tests/scripts/aggregate_property_tests.py</files>
  <action>
Create backend/tests/scripts/aggregate_property_tests.py with:

1. Shebang: #!/usr/bin/env python3
2. Imports: json, xml.etree.ElementTree as ET, pathlib.Path, argparse, typing

3. Function: parse_jest_xml(junit_xml: Path) -> Dict[str, int]
   - Parses Jest JUnit XML output (frontend, mobile)
   - Extracts total, passed, failed from testcase elements
   - Handles missing files (returns 0, 0, 0 with warning)

4. Function: parse_proptest_json(proptest_json: Path) -> Dict[str, int]
   - Parses proptest JSON output (desktop)
   - Note: proptest doesn't natively output JSON, need custom format
   - If JSON not available, parse stdout with regex for "test result: ok"/"test result: FAILED"
   - Handles missing files (returns 0, 0, 0 with warning)

5. Function: aggregate_results(frontend: Dict, mobile: Dict, desktop: Dict) -> Dict
   - Sums total, passed, failed across platforms
   - Calculates pass_rate percentage
   - Returns structure:
     ```python
     {
       "total": int,
       "passed": int,
       "failed": int,
       "pass_rate": float,
       "platforms": {
         "frontend": {"total": int, "passed": int, "failed": int},
         "mobile": {"total": int, "passed": int, "failed": int},
         "desktop": {"total": int, "passed": int, "failed": int}
       },
       "timestamp": str
     }
     ```

6. Function: generate_pr_comment(results: Dict) -> str
   - Generates markdown table for PR comments
   - Shows platform breakdown with pass rates
   - Includes trend indicators (↑↓→) if historical data available
   - Example:
     ```
     ## Property Test Results

     | Platform | Passed | Total | Pass Rate |
     |----------|--------|-------|-----------|
     | Frontend | 12 | 12 | 100% |
     | Mobile | 12 | 12 | 100% |
     | Desktop | 8 | 8 | 100% |
     | **Overall** | **32** | **32** | **100%** |

     ✅ All property tests passed
     ```

7. Function: main() with CLI
   - Arguments: --frontend, --mobile, --desktop, --output, --format (text/json/markdown)
   - Loads results from each platform
   - Aggregates results
   - Outputs to stdout (text/markdown) or file (json)
   - Exits 0 if all tests pass, 1 if any failures

Pattern from Phase 146 cross_platform_coverage_gate.py:
- Missing files handled gracefully (0% with warning)
- Three output formats (text, JSON, markdown)
- CLI with argparse
- Type hints for all functions

Note: proptest JSON output format needs to be defined in CI/CD workflow (Task 4).
  </action>
  <verify>
Script created with all functions defined
Type hints present for all functions
CLI arguments configured (--frontend, --mobile, --desktop, --output, --format)
  </verify>
  <done>Property test aggregation script created</done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for aggregation script</name>
  <files>backend/tests/test_aggregate_property_tests.py</files>
  <action>
Create backend/tests/test_aggregate_property_tests.py with:

1. Imports: pytest, tempfile, Path, json
2. Import aggregate_property_tests module

3. Test fixtures:
   - mock_jest_xml() - Creates temporary Jest JUnit XML file
   - mock_proptest_json() - Creates temporary proptest JSON file
   - mock_aggregated_results() - Expected aggregation result

4. Jest XML parsing tests (5 tests):
   - test_parse_jest_xml_valid - Verify extraction of total/passed/failed
   - test_parse_jest_xml_missing - Verify 0,0,0 returned with warning
   - test_parse_jest_xml_invalid_xml - Verify error handling
   - test_parse_jest_xml_no_failures - Verify all tests passed
   - test_parse_jest_xml_all_failures - Verify all tests failed

5. Proptest parsing tests (4 tests):
   - test_parse_proptest_json_valid - Verify extraction of total/passed/failed
   - test_parse_proptest_json_missing - Verify 0,0,0 returned with warning
   - test_parse_proptest_json_invalid_json - Verify error handling
   - test_parse_proptest_json_stdout_fallback - Verify regex parsing of stdout

6. Aggregation tests (5 tests):
   - test_aggregate_results_all_pass - Verify 100% pass rate
   - test_aggregate_results_frontend_fails - Verify pass rate calculation
   - test_aggregate_results_missing_platform - Verify exclusion from total
   - test_aggregate_results_timestamp_included - Verify timestamp present
   - test_aggregate_results_pass_rate_calculation - Verify accurate percentage

7. CLI integration tests (4 tests):
   - test_cli_help_text - Verify --help output
   - test_cli_json_output - Verify JSON file generation
   - test_cli_markdown_output - Verify markdown table generation
   - test_cli_exit_code_on_failure - Verify exit 1 on failures

8. PR comment generation tests (3 tests):
   - test_generate_pr_comment_all_pass - Verify success message
   - test_generate_pr_comment_with_failures - Verify failure details
   - test_generate_pr_comment_markdown_table - Verify table format

9. End-to-end test (1 test):
   - test_full_pipeline - Load all formats, aggregate, output JSON

Pattern from Phase 146 test_cross_platform_coverage_gate.py:
- Comprehensive test coverage (20+ tests)
- Mock fixtures for test data
- Error handling tests for edge cases
- CLI integration tests

Target: 30+ unit tests total.
  </action>
  <verify>
30+ tests created
pytest discovers all tests
All tests pass
Coverage >80% for aggregation script
  </verify>
  <done>Unit tests created with 30+ tests covering all functionality</done>
</task>

<task type="auto">
  <name>Task 3: Add property test result output to Jest configurations</name>
  <files>frontend-nextjs/jest.config.js, mobile/jest.config.js</files>
  <action>
Modify frontend-nextjs/jest.config.js:
- Add reporters configuration for JUnit XML output
- Configure output directory: coverage/jest-frontend-property-results.xml
- Only apply to property tests (testResultsProcessor)

Pattern:
```javascript
module.exports = {
  // ... existing config
  reporters: [
    'default',
    ['jest-junit', {
      outputDirectory: './coverage',
      outputName: 'jest-frontend-property-results.xml',
      classNameTemplate: '{classname}',
      titleTemplate: '{title}',
      ancestorSeparator: ' › ',
      usePathForSuiteName: true,
    }],
  ],
  // Or add testResultsProcessor for conditional output
  testResultsProcessor: undefined, // Add only for property tests
};
```

Modify mobile/jest.config.js:
- Same configuration as frontend
- Output: coverage/jest-mobile-property-results.xml
- Same reporter configuration

Note: Jest-junit reporter may need to be installed if not already present.
Check package.json for jest-junit dependency.
Add if missing: npm install --save-dev jest-junit

Alternative: Use --ci flag with JUnit reporter
```bash
npm test -- --ci --outputFile=coverage/jest-frontend-property-results.json --json
```

For property tests specifically:
```bash
npm test -- shared-invariants --ci --outputFile=coverage/jest-frontend-property-results.json --json
```

Select approach based on existing Jest configuration.
  </action>
  <verify>
Jest configured to output property test results
Output files defined: jest-frontend-property-results.xml/json, jest-mobile-property-results.xml/json
jest-junit installed if needed
  </verify>
  <done>Jest configurations updated for property test result output</done>
</task>

<task type="auto">
  <name>Task 4: Create proptest result formatter for JSON output</name>
  <files>frontend-nextjs/src-tauri/tests/proptest_formatter.py</files>
  <action>
Create frontend-nextjs/src-tauri/tests/proptest_formatter.py with:

1. Shebang: #!/usr/bin/env python3
2. Purpose: Parse cargo test output and generate JSON for proptest results

3. Function: parse_proptest_output(cargo_output: str) -> Dict
   - Parses stdout from cargo test
   - Looks for "test prop_.* ... ok" and "test prop_.* ... FAILED" patterns
   - Extracts test name and result
   - Returns {"total": int, "passed": int, "failed": int, "tests": [...]}

4. Function: main()
   - Reads cargo test output from stdin or file
   - Calls parse_proptest_output()
   - Outputs JSON to stdout or file

Pattern from existing test output parsing:
- Regex: r"test (prop_\w+) \.\.\. (ok|FAILED)"
- Count passed (ok) and failed (FAILED)
- Output: {"total": 8, "passed": 8, "failed": 0}

Usage in CI/CD (Task 6):
```bash
cargo test property_tests 2>&1 | tee proptest-output.txt
python proptest_formatter.py < proptest-output.txt > proptest-results.json
```

Note: This script runs in desktop/tests/ directory for access to test output.
  </action>
  <verify>
Python script created with proptest parsing logic
Regex patterns for cargo test output
JSON output format matches aggregation script expectations
  </verify>
  <done>Proptest result formatter created for JSON output</done>
</task>

<task type="auto">
  <name>Task 5: Create aggregated results storage directory and initial state</name>
  <files>backend/tests/coverage_reports/metrics/property_test_results.json</files>
  <action>
Create directory: backend/tests/coverage_reports/metrics/ (if not exists)

Create backend/tests/coverage_reports/metrics/property_test_results.json with:
```json
{
  "total": 0,
  "passed": 0,
  "failed": 0,
  "pass_rate": 0.0,
  "platforms": {
    "frontend": {
      "total": 0,
      "passed": 0,
      "failed": 0
    },
    "mobile": {
      "total": 0,
      "passed": 0,
      "failed": 0
    },
    "desktop": {
      "total": 0,
      "passed": 0,
      "failed": 0
    }
  },
  "timestamp": null,
  "history": []
}
```

This file is:
- Updated by CI/CD workflow after each run
- Read by aggregation script for trend analysis
- Used for PR comment trend indicators (↑↓→)

Pattern from Phase 146 cross_platform_summary.json:
- Storage for aggregated cross-platform metrics
- History array for trend tracking
- Updated by CI/CD, read by aggregation scripts

Gitignore: Add to .gitignore if historical data should not be committed
Or: Commit initial state, ignore subsequent updates via git update-index --assume-unchanged

For now, commit the initial state (placeholder format).
  </action>
  <verify>
Directory exists
property_test_results.json created with placeholder structure
Format matches aggregation script output
  </verify>
  <done>Aggregated results storage initialized</done>
</task>

<task type="auto">
  <name>Task 6: Create CI/CD workflow for cross-platform property tests</name>
  <files>.github/workflows/cross-platform-property-tests.yml</files>
  <action>
Create .github/workflows/cross-platform-property-tests.yml with:

1. Workflow name: Cross-Platform Property Tests
2. Triggers: push, pull_request (paths: frontend-nextjs/shared/property-tests/**, mobile/src/__tests__/property/**, frontend-nextjs/src-tauri/tests/*proptest*.rs)
3. Jobs (4 jobs, 3 parallel + 1 sequential):

Job 1: frontend-property-tests
- Runs on: ubuntu-latest
- Steps:
  - Checkout repo
  - Setup Node.js
  - Install frontend dependencies (cd frontend-nextjs, npm ci)
  - Run property tests: npm test -- shared-invariants --ci --json --outputFile=coverage/jest-frontend-property-results.json
  - Upload artifact: jest-frontend-property-results.json

Job 2: mobile-property-tests
- Runs on: ubuntu-latest
- Steps:
  - Checkout repo
  - Setup Node.js
  - Install mobile dependencies (cd mobile, npm ci)
  - Run property tests: npm test -- shared-invariants --ci --json --outputFile=coverage/jest-mobile-property-results.json
  - Upload artifact: jest-mobile-property-results.json

Job 3: desktop-property-tests
- Runs on: ubuntu-latest
- Steps:
  - Checkout repo
  - Setup Rust toolchain
  - Run property tests: cd frontend-nextjs/src-tauri, cargo test property_tests -- --nocapture 2>&1 | tee proptest-output.txt
  - Format results: python tests/proptest_formatter.py < proptest-output.txt > proptest-results.json
  - Upload artifact: proptest-results.json

Job 4: aggregate-results (depends on: frontend-property-tests, mobile-property-tests, desktop-property-tests)
- Runs on: ubuntu-latest
- Steps:
  - Checkout repo
  - Setup Python
  - Download artifacts from previous jobs
  - Run aggregation: python backend/tests/scripts/aggregate_property_tests.py --frontend jest-frontend-property-results.json --mobile jest-mobile-property-results.json --desktop proptest-results.json --output property_test_results.json
  - Upload artifact: property_test_results.json
  - PR comment: Use actions/github-script to post comment with results
  - Trend tracking: Update backend/tests/coverage_reports/metrics/property_test_results.json

4. PR Comment step:
```yaml
- name: Comment PR with results
  uses: actions/github-script@v7
  if: github.event_name == 'pull_request'
  with:
    script: |
      const fs = require('fs');
      const results = JSON.parse(fs.readFileSync('property_test_results.json', 'utf8'));
      const comment = `## Property Test Results\n\n| Platform | Passed | Total | Pass Rate |\n|----------|--------|-------|-----------|\n| Frontend | ${results.platforms.frontend.passed} | ${results.platforms.frontend.total} | ${((results.platforms.frontend.passed / results.platforms.frontend.total) * 100).toFixed(1)}% |\n| Mobile | ${results.platforms.mobile.passed} | ${results.platforms.mobile.total} | ${((results.platforms.mobile.passed / results.platforms.mobile.total) * 100).toFixed(1)}% |\n| Desktop | ${results.platforms.desktop.passed} | ${results.platforms.desktop.total} | ${((results.platforms.desktop.passed / results.platforms.desktop.total) * 100).toFixed(1)}% |\n| **Overall** | **${results.passed}** | **${results.total}** | **${results.pass_rate.toFixed(1)}%** |\n\n${results.failed > 0 ? '❌ Some property tests failed' : '✅ All property tests passed'}`;
      github.rest.issues.createComment({
        issue_number: context.issue.number,
        owner: context.repo.owner,
        repo: context.repo.repo,
        body: comment
      });
```

Pattern from Phase 146 cross-platform-coverage.yml:
- 4 parallel platform jobs + 1 aggregation job
- Artifact upload/download for result passing
- PR comment integration
- Trend tracking with historical data
  </action>
  <verify>
Workflow file created with 4 jobs
Jobs 1-3 run in parallel, Job 4 depends on 1-3
Artifact upload/download configured
PR comment step included
  </verify>
  <done>CI/CD workflow created for cross-platform property tests</done>
</task>

</tasks>

<verification>
After plan completion:
1. Run aggregation script tests: pytest backend/tests/test_aggregate_property_tests.py
   - Should pass all 30+ tests
2. Test aggregation script manually: python backend/tests/scripts/aggregate_property_tests.py --help
   - Should show usage and arguments
3. Verify Jest output: npm test -- shared-invariants --ci --json
   - Should create property test results file
4. Verify proptest formatter: python frontend-nextjs/src-tauri/tests/proptest_formatter.py --help
   - Should show usage
5. Verify CI/CD workflow: github.com/.../actions/workflows/cross-platform-property-tests.yml
   - Should show workflow file
6. Verify storage directory: ls backend/tests/coverage_reports/metrics/property_test_results.json
   - Should exist with placeholder structure
</verification>

<success_criteria>
1. Aggregation script combines FastCheck and proptest results with platform breakdown
2. Unit tests (30+) verify parsing, aggregation, CLI, and PR comment generation
3. Jest configured for property test result output (frontend + mobile)
4. Proptest formatter creates JSON from cargo test output
5. CI/CD workflow runs property tests in parallel and aggregates results
6. PR comments include property test results with platform breakdown table
7. Historical tracking initialized for trend analysis
</success_criteria>

<output>
After completion, create `.planning/phases/147-cross-platform-property-testing/147-03-SUMMARY.md` with:
- Files created (5 files: aggregation script, tests, proptest formatter, CI workflow, storage)
- Files modified (2 files: frontend jest.config.js, mobile jest.config.js)
- Unit tests created (30+ tests)
- CI/CD workflow structure (4 jobs, parallel + aggregation)
- Commits made (6 commits, one per task)
- Deviations (if any)
- Next steps for Plan 04 (documentation and final verification)
</output>
