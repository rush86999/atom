---
phase: 162-episode-service-comprehensive-testing
plan: 04
type: execute
wave: 2
depends_on: ["162-02"]
files_modified:
  - backend/tests/unit/episodes/test_episode_retrieval_advanced.py
  - backend/tests/unit/episodes/conftest.py
autonomous: true

must_haves:
  truths:
    - "retrieve_sequential returns full episodes with segments"
    - "retrieve_canvas_aware filters by canvas type and detail level"
    - "retrieve_by_business_data filters by canvas critical_data_points"
    - "retrieve_with_supervision_context enriches with supervision metadata"
    - "Canvas context detail filtering (summary/standard/full) is tested"
    - "Feedback-weighted contextual retrieval is tested"
    - "Test coverage increases by ~2-3 percentage points on retrieval service"
  artifacts:
    - path: backend/tests/unit/episodes/test_episode_retrieval_advanced.py
      provides: "Advanced retrieval mode tests"
      min_lines: 700
  key_links:
    - from: "backend/tests/unit/episodes/test_episode_retrieval_advanced.py"
      to: "backend/core/episode_retrieval_service.py"
      via: "from core.episode_retrieval_service import EpisodeRetrievalService"
      pattern: "from core.episode_retrieval_service import"
---

<objective>
**Advanced Retrieval Mode Testing**

Test the advanced retrieval modes that were untested in Phase 161: sequential (full episodes with segments), canvas-aware (with detail filtering), business data filtering, and supervision context enrichment.

**Purpose:** Phase 161 tested retrieve_temporal and retrieve_semantic (32.5% coverage), but advanced retrieval modes (retrieve_sequential, retrieve_canvas_aware, retrieve_by_business_data, retrieve_with_supervision_context) are completely untested.

**Output:** ~25 tests for advanced retrieval modes, targeting ~2-3% overall backend coverage increase.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-episode-service-comprehensive-testing/162-RESEARCH.md
@.planning/phases/161-model-fixes-and-database/161-VERIFICATION.md
@backend/core/episode_retrieval_service.py
@backend/tests/unit/episodes/test_episode_retrieval_coverage.py
@backend/tests/unit/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Add retrieval test fixtures to conftest.py</name>
  <files>backend/tests/unit/episodes/conftest.py</files>
  <action>
    Add these fixtures to backend/tests/unit/episodes/conftest.py:

    1. **retrieval_service(db_session, mock_lancedb)**:
       - Patch get_lancedb_handler to return mock_lancedb
       - Patch AgentGovernanceService to return governance check with allowed=True
       - Return EpisodeRetrievalService(db_session)

    2. **retrieval_service_mocked(db_session, mock_lancedb)**:
       - Same as retrieval_service but assigns service.lancedb = mock_lancedb
       - Mock governance to always allow

    3. **episode_test_data(db_session)**: Creates test episode with segments
       - Episode with id, agent_id, status="completed"
       - 3 EpisodeSegment records linked to episode
       - 2 CanvasAudit records with episode_id set
       - 1 AgentFeedback record with episode_id set

    4. **episode_with_canvas_context(db_session)**:
       - Episode with canvas_action_count=2
       - CanvasAudit with canvas_type="sheets"
       - EpisodeSegment with canvas_context dict

    5. **episode_with_supervision(db_session)**:
       - Episode with supervisor_id, supervisor_rating=4, intervention_count=1

    Use db_session.commit() after creating records.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/conftest.py --collect-only
    Expected: New retrieval fixtures collected without errors
  </verify>
  <done>
    All retrieval fixtures defined and importable
  </done>
</task>

<task type="auto">
  <name>Test sequential retrieval with segments</name>
  <files>backend/tests/unit/episodes/test_episode_retrieval_advanced.py</files>
  <action>
    Create TestSequentialRetrieval class with these tests:

    1. **test_retrieve_sequential_full_episode_with_segments** (async):
       - Create episode with 3 segments
       - Call await retrieval_service.retrieve_sequential(episode_id, agent_id)
       - Verify result contains:
         * "episode" dict with all fields
         * "segments" list with 3 items
         * Segments ordered by sequence_order

    2. **test_retrieve_sequential_with_canvas_context** (async):
       - Create episode with canvas_ids
       - Create CanvasAudit records
       - Call retrieve_sequential(include_canvas=True)
       - Verify "canvas_context" in result
       - Verify canvas data populated

    3. **test_retrieve_sequential_with_feedback_context** (async):
       - Create episode with feedback_ids
       - Create AgentFeedback records
       - Call retrieve_sequential(include_feedback=True)
       - Verify "feedback_context" in result
       - Verify feedback data populated

    4. **test_retrieve_sequential_exclude_canvas_feedback** (async):
       - Create episode with canvas_ids and feedback_ids
       - Call retrieve_sequential(include_canvas=False, include_feedback=False)
       - Verify canvas_context and feedback_context NOT in result

    5. **test_retrieve_sequential_episode_not_found** (async):
       - Call with non-existent episode_id
       - Verify returns {"error": "Episode not found"}

    6. **test_serialize_segment**:
       - Create EpisodeSegment with canvas_context dict
       - Call _serialize_segment(segment)
       - Verify all fields including canvas_context in output

    Use @pytest.mark.asyncio for async tests.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/test_episode_retrieval_advanced.py::TestSequentialRetrieval -v
    Expected: 6 tests pass
  </verify>
  <done>
    All sequential retrieval tests pass
  </done>
</task>

<task type="auto">
  <name>Test canvas-aware retrieval with detail filtering</name>
  <files>backend/tests/unit/episodes/test_episode_retrieval_advanced.py</files>
  <action>
    Create TestCanvasAwareRetrieval class with these tests:

    1. **test_retrieve_canvas_aware_with_summary_detail** (async):
       - Mock lancedb.search to return episode with canvas_type="sheets"
       - Create EpisodeSegment with canvas_context dict
       - Call await retrieve_canvas_aware(agent_id, "sales data", canvas_context_detail="summary")
       - Verify segments have filtered canvas_context with only presentation_summary

    2. **test_retrieve_canvas_aware_with_standard_detail** (async):
       - Same setup, use canvas_context_detail="standard"
       - Verify canvas_context contains presentation_summary + critical_data_points
       - Verify visual_elements NOT included

    3. **test_retrieve_canvas_aware_with_full_detail** (async):
       - Same setup, use canvas_context_detail="full"
       - Verify canvas_context contains all fields including visual_elements

    4. **test_retrieve_canvas_aware_with_canvas_type_filter** (async):
       - Mock lancedb.search with canvas_type filter
       - Call retrieve_canvas_aware(agent_id, query, canvas_type="sheets")
       - Verify only sheets-type episodes returned

    5. **test_retrieve_canvas_aware_governance_blocked** (async):
       - Mock governance to return allowed=False
       - Call retrieve_canvas_aware()
       - Verify returns error with governance_check

    6. **test_filter_canvas_context_detail**:
       - Test _filter_canvas_context_detail with "summary", "standard", "full"
       - Test with unknown detail level (should default to summary)

    7. **test_retrieve_canvas_aware_lancedb_error** (async):
       - Mock lancedb.search to raise Exception
       - Call retrieve_canvas_aware()
       - Verify returns error dict

    Use @pytest.mark.asyncio for async tests.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/test_episode_retrieval_advanced.py::TestCanvasAwareRetrieval -v
    Expected: 7 tests pass
  </verify>
  <done>
    All canvas-aware retrieval tests pass, detail filtering verified
  </done>
</task>

<task type="auto">
  <name>Test business data and canvas type retrieval</name>
  <files>backend/tests/unit/episodes/test_episode_retrieval_advanced.py</files>
  <action>
    Create TestBusinessDataRetrieval class with these tests:

    1. **test_retrieve_by_business_data_with_filters** (async):
       - Create EpisodeSegment with canvas_context containing critical_data_points
       - Create canvas_context = {"critical_data_points": {"approval_status": "approved", "revenue": 1000000}}
       - Call await retrieve_by_business_data(agent_id, {"approval_status": "approved"})
       - Verify episodes with matching business data returned

    2. **test_retrieve_by_business_data_with_operator_filters** (async):
       - Create segment with revenue in critical_data_points
       - Call retrieve_by_business_data(agent_id, {"revenue": {"$gt": 500000}})
       - Verify SQL filter applied correctly

    3. **test_retrieve_by_business_data_empty_results** (async):
       - Mock query to return empty list
       - Call retrieve_by_business_data()
       - Verify returns {"episodes": [], "count": 0}

    4. **test_retrieve_by_canvas_type** (async):
       - Create CanvasAudit with canvas_type="sheets", action="submit"
       - Create Episode linked via canvas_id
       - Call await retrieve_by_canvas_type(agent_id, "sheets", "submit")
       - Verify episodes with sheets canvas returned

    5. **test_retrieve_by_canvas_type_governance_check** (async):
       - Mock governance to return allowed=False
       - Call retrieve_by_canvas_type()
       - Verify returns error

    6. **test_retrieve_by_canvas_type_time_range** (async):
       - Create episodes from 7 days ago and 40 days ago
       - Call retrieve_by_canvas_type(agent_id, "sheets", time_range="30d")
       - Verify only 7-day episode returned

    Use @pytest.mark.asyncio for async tests.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/test_episode_retrieval_advanced.py::TestBusinessDataRetrieval -v
    Expected: 6 tests pass
  </verify>
  <done>
    All business data and canvas type retrieval tests pass
  </done>
</task>

<task type="auto">
  <name>Test supervision context retrieval</name>
  <files>backend/tests/unit/episodes/test_episode_retrieval_advanced.py</files>
  <action>
    Create TestSupervisionContextRetrieval class with these tests:

    1. **test_retrieve_with_supervision_context_high_rated** (async):
       - Create episodes with supervisor_rating >= 4
       - Call await retrieve_with_supervision_context(agent_id, supervision_outcome_filter="high_rated")
       - Verify only high-rated episodes returned
       - Verify supervision_context in each episode

    2. **test_retrieve_with_supervision_context_low_intervention** (async):
       - Create episodes with intervention_count 0-2
       - Call with supervision_outcome_filter="low_intervention"
       - Verify only low-intervention episodes returned

    3. **test_retrieve_with_supervision_context_min_rating** (async):
       - Create episodes with ratings 3, 4, 5
       - Call with min_rating=4
       - Verify only 4+ rated episodes returned

    4. **test_retrieve_with_supervision_context_max_interventions** (async):
       - Create episodes with intervention counts 0, 2, 5
       - Call with max_interventions=2
       - Verify only <=2 intervention episodes returned

    5. **test_create_supervision_context**:
       - Create episode with supervision fields
       - Call _create_supervision_context(episode)
       - Verify returns dict with has_supervision, supervisor_rating, intervention_count, etc.

    6. **test_assess_outcome_quality**:
       - Test with rating=5, intervention=0 → "excellent"
       - Test with rating=4, intervention=2 → "good"
       - Test with rating=3 → "fair"
       - Test with rating=2 → "poor"

    7. **test_filter_improvement_trend**:
       - Create 10 episodes with improving ratings over time
       - Call _filter_improvement_trend(episodes)
       - Verify episodes with positive trend returned

    Use @pytest.mark.asyncio for async tests.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/test_episode_retrieval_advanced.py::TestSupervisionContextRetrieval -v
    Expected: 7 tests pass
  </verify>
  <done>
    All supervision context retrieval tests pass
  </done>
</task>

<task type="auto">
  <name>Test contextual retrieval with feedback weighting</name>
  <files>backend/tests/unit/episodes/test_episode_retrieval_advanced.py</files>
  <action>
    Create TestContextualRetrieval class with these tests:

    1. **test_retrieve_contextual_canvas_boost** (async):
       - Create episodes: one with canvas_action_count=2, one without
       - Mock retrieve_temporal and retrieve_semantic to return episodes
       - Call await retrieve_contextual(agent_id, "task")
       - Verify episode with canvas gets +0.1 boost

    2. **test_retrieve_contextual_positive_feedback_boost** (async):
       - Create episode with aggregate_feedback_score=0.5 (positive)
       - Call retrieve_contextual()
       - Verify positive feedback episode gets +0.2 boost

    3. **test_retrieve_contextual_negative_feedback_penalty** (async):
       - Create episode with aggregate_feedback_score=-0.5 (negative)
       - Call retrieve_contextual()
       - Verify negative feedback episode gets -0.3 penalty

    4. **test_retrieve_contextual_hybrid_scoring** (async):
       - Mock temporal (weight 0.3) and semantic (weight 0.7) results
       - Create episode appearing in both
       - Verify combined score = 0.3 + 0.7 = 1.0

    5. **test_retrieve_contextual_require_canvas** (async):
       - Call retrieve_contextual(require_canvas=True)
       - Verify episodes without canvas filtered out

    6. **test_retrieve_contextual_require_feedback** (async):
       - Call retrieve_contextual(require_feedback=True)
       - Verify episodes without feedback filtered out

    7. **test_fetch_canvas_context**:
       - Create CanvasAudit records
       - Call await _fetch_canvas_context(canvas_ids)
       - Verify returns list of canvas dicts

    8. **test_fetch_feedback_context**:
       - Create AgentFeedback records
       - Call await _fetch_feedback_context(feedback_ids)
       - Verify returns list of feedback dicts

    Use @pytest.mark.asyncio for async tests.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/test_episode_retrieval_advanced.py::TestContextualRetrieval -v
    Expected: 8 tests pass
  </verify>
  <done>
    All contextual retrieval tests pass, feedback weighting verified
  </done>
</task>

<task type="auto">
  <name>Generate coverage report and verify</name>
  <files>backend/tests/coverage_reports/backend_phase_162_plan4.json</files>
  <action>
    Generate coverage report for retrieval service:

    1. Run pytest with coverage:
       ```bash
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/episodes/test_episode_retrieval_advanced.py -v --cov=core.episode_retrieval_service --cov-report=json --cov-report=term
       ```

    2. Save coverage JSON to backend/tests/coverage_reports/backend_phase_162_plan4.json

    3. Extract key metrics:
       - EpisodeRetrievalService overall coverage percentage
       - Lines covered vs total
       - Coverage for each retrieval mode

    4. Verify targeted methods are covered:
       - retrieve_sequential: target 70%+
       - retrieve_canvas_aware: target 60%+
       - retrieve_by_business_data: target 60%+
       - retrieve_with_supervision_context: target 60%+
       - retrieve_contextual: target 60%+ (already partially tested)

    If any method below target, add 1-2 tests to increase coverage.
  </action>
  <verify>
    Run: cat backend/tests/coverage_reports/backend_phase_162_plan4.json | jq '.files["core/episode_retrieval_service.py"].summary'
    Expected: Coverage 65%+ on retrieval service (up from 32.5%)
  </verify>
  <done>
    Retrieval service coverage increased to 65%+, all advanced retrieval modes tested
  </done>
</task>

</tasks>

<verification>
1. Run all advanced retrieval tests: pytest backend/tests/unit/episodes/test_episode_retrieval_advanced.py -v
2. Verify 25+ tests pass
3. Verify coverage report shows 65%+ on EpisodeRetrievalService
4. Verify all retrieval modes (sequential, canvas_aware, business_data, supervision_context, contextual) tested
5. Verify canvas context detail filtering tested
</verification>

<success_criteria>
- EpisodeRetrievalService coverage increases from 32.5% to 65%+
- All 4 advanced retrieval modes have tests
- Canvas context detail filtering (summary/standard/full) verified
- Feedback-weighted retrieval tested
- 25+ new tests created
- Overall backend coverage increases by ~2-3 percentage points
</success_criteria>

<output>
After completion, create `.planning/phases/162-episode-service-comprehensive-testing/162-04-SUMMARY.md` with:
- Tests created count
- Coverage achieved for retrieval service
- All retrieval modes verified
- Coverage report location
</output>
