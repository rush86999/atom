---
phase: 162-episode-service-comprehensive-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/episodes/test_episode_lifecycle_coverage.py
  - backend/tests/unit/episodes/conftest.py
autonomous: true

must_haves:
  truths:
    - "All async lifecycle methods are tested with @pytest.mark.asyncio"
    - "decay_old_episodes applies decay to episodes exceeding threshold"
    - "consolidate_similar_episodes merges semantically similar episodes"
    - "update_importance_scores adjusts episode importance based on feedback"
    - "batch_update_access_counts increments access counts for multiple episodes"
    - "archive_to_cold_storage marks episodes as archived with timestamp"
    - "Test coverage increases by ~2-3 percentage points on lifecycle service"
  artifacts:
    - path: backend/tests/unit/episodes/test_episode_lifecycle_coverage.py
      provides: "Async lifecycle service tests"
      min_lines: 500
    - path: backend/tests/unit/episodes/conftest.py
      provides: "Episode test fixtures"
      exports: ["lifecycle_service", "lifecycle_service_mocked", "episode_test_agent"]
  key_links:
    - from: "backend/tests/unit/episodes/test_episode_lifecycle_coverage.py"
      to: "backend/core/episode_lifecycle_service.py"
      via: "from core.episode_lifecycle_service import EpisodeLifecycleService"
      pattern: "from core.episode_lifecycle_service import"
---

<objective>
**Async Lifecycle Service Testing**

Test async episode lifecycle methods that were missed in Phase 161 (which tested sync wrappers instead of async implementations). This plan focuses on the core async operations: decay, consolidation, importance updates, access counting, and archival.

**Purpose:** Phase 161 tested sync wrapper methods (apply_decay, consolidate_episodes) but not the actual async methods (decay_old_episodes, consolidate_similar_episodes), leaving 67.8% of lifecycle service untested.

**Output:** ~30 tests for async lifecycle methods, targeting ~2-3% overall backend coverage increase.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-episode-service-comprehensive-testing/162-RESEARCH.md
@.planning/phases/161-model-fixes-and-database/161-VERIFICATION.md
@backend/core/episode_lifecycle_service.py
@backend/tests/unit/episodes/test_episode_lifecycle_coverage.py
@backend/tests/unit/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Create episode test fixtures in conftest.py</name>
  <files>backend/tests/unit/episodes/conftest.py</files>
  <action>
    Create a new conftest.py file in backend/tests/unit/episodes/ with the following fixtures:

    1. **db_session()**: In-memory SQLite database session using tempfile.mkstemp() for isolation
       - Create all tables from Base.metadata.sorted_tables
       - Handle NoReferencedTableError for missing FK references
       - Cleanup: close session, dispose engine, unlink temp file

    2. **mock_lancedb()**: MagicMock with these methods:
       - embed_text: returns [0.1, 0.2, 0.3]
       - search: returns []
       - add_document: MagicMock()
       - create_table: MagicMock()
       - db.table_names: returns []

    3. **episode_test_agent()**: AgentRegistry instance with:
       - id: str(uuid.uuid4())
       - name: "TestAgent"
       - status: AgentStatus.AUTONOMOUS
       - tenant_id: "default"

    4. **lifecycle_service(db_session, mock_lancedb)**:
       - Patch get_lancedb_handler to return mock_lancedb
       - Return EpisodeLifecycleService(db_session)

    5. **lifecycle_service_mocked(db_session, mock_lancedb)**:
       - Same as lifecycle_service but assigns service.lancedb = mock_lancedb
       - Use for tests that need to mock search return values

    Follow the pattern from backend/tests/unit/conftest.py lines 25-82 for database fixture.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/conftest.py --collect-only
    Expected: Fixtures are collected without errors
  </verify>
  <done>
    All fixtures (db_session, mock_lancedb, episode_test_agent, lifecycle_service, lifecycle_service_mocked) are defined and importable
  </done>
</task>

<task type="auto">
  <name>Test async episode decay methods</name>
  <files>backend/tests/unit/episodes/test_episode_lifecycle_coverage.py</files>
  <action>
    Create TestAsyncDecay class with these tests:

    1. **test_decay_old_episodes_applies_decay_to_old_episodes** (async):
       - Create 3 episodes: 30 days old, 100 days old, 200 days old
       - Call await lifecycle_service.decay_old_episodes(days_threshold=90)
       - Verify: 30-day episode NOT affected, 100-day decayed, 200-day decayed AND archived
       - Use datetime.now(timezone.utc) - timedelta(days=X) for timestamps

    2. **test_decay_old_episodes_excludes_archived** (async):
       - Create archived episode (status="archived")
       - Call await decay_old_episodes()
       - Verify archived episode NOT in affected count

    3. **test_decay_old_episodes_empty_database** (async):
       - Call with no episodes
       - Verify returns {"affected": 0, "archived": 0}

    4. **test_decay_old_episodes_threshold_edge_case** (async):
       - Create episode exactly 90 days old
       - Verify it IS decayed (threshold is <, not <=)

    5. **test_update_lifecycle_single_episode**:
       - Create episode with started_at 60 days ago
       - Call lifecycle_service.update_lifecycle(episode)
       - Verify decay_score = 60/90 = 0.67

    6. **test_update_lifecycle_timezone_aware**:
       - Create episode with timezone-aware started_at
       - Verify no TypeError comparing datetimes

    Use @pytest.mark.asyncio decorator for async tests. Mock LanceDB since decay doesn't use it.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/test_episode_lifecycle_coverage.py::TestAsyncDecay -v
    Expected: 6 tests pass, coverage of decay_old_episodes and update_lifecycle methods
  </verify>
  <done>
    All decay tests pass with proper async/await patterns and datetime handling
  </done>
</task>

<task type="auto">
  <name>Test async episode consolidation methods</name>
  <files>backend/tests/unit/episodes/test_episode_lifecycle_coverage.py</files>
  <action>
    Create TestAsyncConsolidation class with these tests:

    1. **test_consolidate_similar_episodes_semantic_match** (async):
       - Create 5 episodes with same agent_id, status="completed", consolidated_into=None
       - Mock lancedb.search to return 2 similar episodes with _distance < 0.15 (similarity > 0.85)
       - Call await consolidate_similar_episodes(agent_id, similarity_threshold=0.85)
       - Verify 2 episodes have consolidated_into set to parent episode ID

    2. **test_consolidate_similar_episodes_below_threshold** (async):
       - Create 3 episodes
       - Mock lancedb.search to return episodes with _distance > 0.2 (similarity < 0.8)
       - Verify no consolidation occurs

    3. **test_consolidate_similar_episodes_skip_already_consolidated** (async):
       - Create episode with consolidated_into already set
       - Verify it's skipped

    4. **test_consolidate_similar_episodes_empty_results** (async):
       - Mock lancedb.search to return []
       - Verify returns {"consolidated": 0, "parent_episodes": 0}

    5. **test_consolidate_similar_episodes_lancedb_error_handling** (async):
       - Mock lancedb.search to raise Exception
       - Verify rollback occurs and returns zeros

    6. **test_consolidate_episodes_sync_wrapper**:
       - Test the synchronous consolidate_episodes() wrapper
       - Verify it calls async method via asyncio.run or threading

    Note: Episode.consolidated_into field may be missing from schema. If tests fail due to missing field, mark as xfail or skip with TODO comment.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/test_episode_lifecycle_coverage.py::TestAsyncConsolidation -v
    Expected: 6 tests pass (or xfail if consolidated_into field missing)
  </verify>
  <done>
    All consolidation tests pass, semantic similarity logic verified
  </done>
</task>

<task type="auto">
  <name>Test async importance and access count methods</name>
  <files>backend/tests/unit/episodes/test_episode_lifecycle_coverage.py</files>
  <action>
    Create TestAsyncImportanceAndAccess class with these tests:

    1. **test_update_importance_scores_positive_feedback** (async):
       - Create episode with importance_score = 0.5
       - Call await update_importance_scores(episode_id, user_feedback=0.8)
       - Verify new_importance = 0.5 * 0.8 + (0.8 + 1.0) / 2.0 * 0.2
       - Verify result clamped to [0.0, 1.0]

    2. **test_update_importance_scores_negative_feedback** (async):
       - Create episode with importance_score = 0.5
       - Call with user_feedback = -0.5
       - Verify score decreases appropriately

    3. **test_update_importance_scores_episode_not_found** (async):
       - Call with non-existent episode_id
       - Verify returns False

    4. **test_batch_update_access_counts_multiple** (async):
       - Create 3 episodes with initial access_count = 0
       - Call await batch_update_access_counts([id1, id2, id3])
       - Verify all 3 have access_count = 1
       - Verify returns {"updated": 3}

    5. **test_batch_update_access_counts_partial** (async):
       - Create 1 episode, pass [valid_id, "invalid_id"]
       - Verify returns {"updated": 1}

    6. **test_batch_update_access_counts_empty** (async):
       - Call with empty list
       - Verify returns {"updated": 0}

    7. **test_archive_to_cold_storage_success** (async):
       - Create episode with status="completed"
       - Call await archive_to_cold_storage(episode_id)
       - Verify status="archived" and archived_at is set

    8. **test_archive_to_cold_storage_not_found** (async):
       - Call with non-existent episode_id
       - Verify returns False

    Use @pytest.mark.asyncio for async tests.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/test_episode_lifecycle_coverage.py::TestAsyncImportanceAndAccess -v
    Expected: 8 tests pass
  </verify>
  <done>
    All importance and access count tests pass
  </done>
</task>

<task type="auto">
  <name>Generate coverage report and verify</name>
  <files>backend/tests/coverage_reports/backend_phase_162_plan1.json</files>
  <action>
    Generate coverage report for lifecycle service:

    1. Run pytest with coverage:
       ```bash
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/episodes/test_episode_lifecycle_coverage.py -v --cov=core.episode_lifecycle_service --cov-report=json --cov-report=term
       ```

    2. Save coverage JSON to backend/tests/coverage_reports/backend_phase_162_plan1.json

    3. Extract key metrics:
       - EpisodeLifecycleService overall coverage percentage
       - Lines covered vs total
       - Coverage gap remaining

    4. Verify targeted async methods are covered:
       - decay_old_episodes: target 80%+
       - consolidate_similar_episodes: target 70%+
       - update_importance_scores: target 80%+
       - batch_update_access_counts: target 80%+
       - archive_to_cold_storage: target 80%+

    If any method below target, add 1-2 tests to increase coverage.
  </action>
  <verify>
    Run: cat backend/tests/coverage_reports/backend_phase_162_plan1.json | jq '.files["core/episode_lifecycle_service.py"].summary'
    Expected: Coverage 65%+ on lifecycle service (up from 32.2%)
  </verify>
  <done>
    Lifecycle service coverage increased to 65%+, all async methods tested
  </done>
</task>

</tasks>

<verification>
1. Run all lifecycle tests: pytest backend/tests/unit/episodes/test_episode_lifecycle_coverage.py -v
2. Verify 20+ tests pass
3. Verify coverage report shows 65%+ on EpisodeLifecycleService
4. Verify @pytest.mark.asyncio used for all async method tests
5. Verify datetime.now(timezone.utc) used for all timestamp comparisons
</verification>

<success_criteria>
- EpisodeLifecycleService coverage increases from 32.2% to 65%+
- All async lifecycle methods (decay_old_episodes, consolidate_similar_episodes, update_importance_scores, batch_update_access_counts, archive_to_cold_storage) have tests
- 20+ new tests created
- Tests use proper async/await patterns with @pytest.mark.asyncio
- Overall backend coverage increases by ~2-3 percentage points
</success_criteria>

<output>
After completion, create `.planning/phases/162-episode-service-comprehensive-testing/162-01-SUMMARY.md` with:
- Tests created count
- Coverage achieved for lifecycle service
- Any issues encountered (e.g., consolidated_into field missing)
- Coverage report location
</output>
