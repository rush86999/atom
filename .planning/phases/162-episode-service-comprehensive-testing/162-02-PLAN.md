---
phase: 162-episode-service-comprehensive-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/episodes/test_episode_segmentation_comprehensive.py
  - backend/tests/unit/episodes/conftest.py
autonomous: true

must_haves:
  truths:
    - "create_episode_from_session is tested with full flow including segmentation"
    - "Episode creation links CanvasAudit and AgentFeedback via episode_id"
    - "Boundary detection integration (time gaps + topic changes) is tested"
    - "Segment creation with sequence ordering is verified"
    - "LanceDB archival with metadata is tested"
    - "Canvas context extraction and LLM summary generation is tested"
    - "Test coverage increases by ~2-3 percentage points on segmentation service"
  artifacts:
    - path: backend/tests/unit/episodes/test_episode_segmentation_comprehensive.py
      provides: "Full episode creation flow tests"
      min_lines: 600
  key_links:
    - from: "backend/tests/unit/episodes/test_episode_segmentation_comprehensive.py"
      to: "backend/core/episode_segmentation_service.py"
      via: "from core.episode_segmentation_service import EpisodeSegmentationService"
      pattern: "from core.episode_segmentation_service import"
---

<objective>
**Full Episode Creation Flow Testing**

Test the complete episode creation pipeline from create_episode_from_session through segmentation, canvas/feedback linkage, and LanceDB archival. Phase 161 tested boundary detection in isolation but not the full async creation flow.

**Purpose:** The create_episode_from_session method is the primary entry point for episodic memory but was untested in Phase 161. This plan tests the complete flow including canvas/feedback linkage.

**Output:** ~20 tests for episode creation flow, targeting ~2-3% overall backend coverage increase.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-episode-service-comprehensive-testing/162-RESEARCH.md
@.planning/phases/161-model-fixes-and-database/161-VERIFICATION.md
@backend/core/episode_segmentation_service.py
@backend/tests/unit/episodes/test_episode_segmentation_coverage.py
@backend/tests/unit/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Add episode creation fixtures to conftest.py</name>
  <files>backend/tests/unit/episodes/conftest.py</files>
  <action>
    Add these fixtures to backend/tests/unit/episodes/conftest.py (create if doesn't exist):

    1. **episode_test_session(db_session)**: ChatSession instance
       - id: str(uuid.uuid4())
       - user_id: "test_user"
       - created_at: datetime.now(timezone.utc) - timedelta(hours=1)

    2. **episode_test_user(db_session)**: User instance
       - id: "test_user"
       - email: "test@example.com"

    3. **episode_test_messages(episode_test_session)**: List of ChatMessage
       - 5 messages with varying timestamps (no gaps > 30 min)
       - Mix of user and assistant roles
       - All linked to episode_test_session.id

    4. **segmentation_service(db_session, mock_lancedb, mock_byok_handler)**:
       - Patch get_lancedb_handler and CanvasSummaryService
       - Return EpisodeSegmentationService(db_session, mock_byok_handler)
       - Assign service.lancedb = mock_lancedb

    5. **segmentation_service_mocked(...)**: Same but with mocked CanvasSummaryService
       - Mock canvas_summary_service.generate_summary to return "Test summary"

    Use timezone-aware datetimes: datetime.now(timezone.utc).
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/conftest.py --collect-only
    Expected: New fixtures collected without errors
  </verify>
  <done>
    All episode creation fixtures defined and importable
  </done>
</task>

<task type="auto">
  <name>Test create_episode_from_session full flow</name>
  <files>backend/tests/unit/episodes/test_episode_segmentation_comprehensive.py</files>
  <action>
    Create TestEpisodeCreationFlow class with these tests:

    1. **test_create_episode_from_session_full_flow** (async):
       - Create session, user, 5 messages, 2 executions in db_session
       - Mock lancedb.embed_text, search, add_document
       - Mock canvas_summary_service.generate_summary
       - Call await segmentation_service.create_episode_from_session(session_id, agent_id, force_create=True)
       - Verify episode created with:
         * status = "completed"
         * Correct started_at, ended_at timestamps
         * topics extracted from messages
         * entities extracted
         * maturity_at_time set

    2. **test_create_episode_from_session_with_boundaries** (async):
       - Create messages with 35-minute gap (exceeds 30-min threshold)
       - Verify segments created at boundary positions
       - Verify segment sequence_order correct

    3. **test_create_episode_from_session_too_small** (async):
       - Create session with only 1 message
       - Call without force_create=True
       - Verify returns None
       - Verify with force_create=True, episode is created

    4. **test_create_episode_from_session_session_not_found** (async):
       - Call with non-existent session_id
       - Verify returns None and error logged

    5. **test_create_episode_from_session_title_generation**:
       - Test _generate_title with first user message
       - Test truncation at 50 chars
       - Test fallback when no messages

    Use @pytest.mark.asyncio for async tests. Commit records to db_session before calling service method.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/test_episode_segmentation_comprehensive.py::TestEpisodeCreationFlow -v
    Expected: 5 tests pass
  </verify>
  <done>
    All episode creation flow tests pass
  </done>
</task>

<task type="auto">
  <name>Test canvas and feedback linkage in episodes</name>
  <files>backend/tests/unit/episodes/test_episode_segmentation_comprehensive.py</files>
  <action>
    Create TestCanvasFeedbackLinkage class with these tests:

    1. **test_create_episode_with_canvas_context** (async):
       - Create CanvasAudit record with session_id
       - Create messages and session
       - Call create_episode_from_session()
       - Verify episode.canvas_ids contains canvas ID
       - Verify episode.canvas_action_count = 1
       - Verify CanvasAudit.episode_id is set (back-linkage)

    2. **test_create_episode_with_feedback_context** (async):
       - Create AgentFeedback with agent_execution_id
       - Create AgentExecution linked to feedback
       - Call create_episode_from_session()
       - Verify episode.feedback_ids contains feedback ID
       - Verify episode.aggregate_feedback_score calculated
       - Verify AgentFeedback.episode_id is set

    3. **test_fetch_canvas_context**:
       - Create 3 CanvasAudit records for session
       - Call segmentation_service._fetch_canvas_context(session_id)
       - Verify returns 3 audits ordered by created_at
       - Verify handles empty results

    4. **test_extract_canvas_context**:
       - Create CanvasAudit with audit_metadata containing:
         * component: "SalesChart"
         * revenue: 1000000
         * approval_status: "approved"
       - Call _extract_canvas_context([canvas])
       - Verify canvas_type, presentation_summary, critical_data_points

    5. **test_extract_canvas_context_detail_filtering**:
       - Create full canvas context dict
       - Call _filter_canvas_context_detail with "summary"
       - Verify only presentation_summary returned
       - Test "standard" and "full" detail levels

    6. **test_calculate_feedback_score**:
       - Create feedbacks: thumbs_up (+1.0), rating 4 (+0.5), thumbs_down (-1.0)
       - Call _calculate_feedback_score(feedbacks)
       - Verify aggregate = (1.0 + 0.5 - 1.0) / 3 = 0.167
       - Verify returns None for empty list

    Use db_session.commit() after adding records before service calls.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/test_episode_segmentation_comprehensive.py::TestCanvasFeedbackLinkage -v
    Expected: 6 tests pass
  </verify>
  <done>
    All canvas/feedback linkage tests pass, back-linkage verified
  </done>
</task>

<task type="auto">
  <name>Test episode segment creation and LanceDB archival</name>
  <files>backend/tests/unit/episodes/test_episode_segmentation_comprehensive.py</files>
  <action>
    Create TestSegmentCreationAndArchival class with these tests:

    1. **test_create_segments_with_boundaries** (async):
       - Create episode with 5 messages
       - Set boundaries = [2, 4] (create segments at positions 2 and 4)
       - Call await segmentation_service._create_segments(episode, messages, [], boundaries)
       - Verify 3 segments created (0-1, 2-3, 4)
       - Verify sequence_order: 0, 1, 2
       - Verify segment_type = "conversation"

    2. **test_create_segments_with_executions**:
       - Create episode and 2 AgentExecution records
       - Call _create_segments()
       - Verify execution segments created with correct segment_type

    3. **test_create_segments_with_canvas_context** (async):
       - Create canvas_context dict with presentation_summary
       - Call _create_segments() with canvas_context
       - Verify segments have canvas_context in metadata

    4. **test_archive_to_lancedb** (async):
       - Create completed episode
       - Mock lancedb.db.table_names = [] (create new table)
       - Call await segmentation_service._archive_to_lancedb(episode)
       - Verify lancedb.create_table called
       - Verify lancedb.add_document called with:
         * table_name = "episodes"
         * text contains episode title and summary
         * metadata contains episode_id, agent_id, topics, etc.

    5. **test_archive_to_lancedb_unavailable** (async):
       - Mock lancedb.db = None
       - Call _archive_to_lancedb()
       - Verify returns without error (logs warning)

    6. **test_format_messages_and_summarize**:
       - Test _format_messages() with list of messages
       - Test _summarize_messages() truncation logic

    Use @pytest.mark.asyncio for async tests.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/test_episode_segmentation_comprehensive.py::TestSegmentCreationAndArchival -v
    Expected: 6 tests pass
  </verify>
  <done>
    All segment creation and archival tests pass
  </done>
</task>

<task type="auto">
  <name>Test episode helper methods (topics, entities, importance)</name>
  <files>backend/tests/unit/episodes/test_episode_segmentation_comprehensive.py</files>
  <action>
    Create TestEpisodeHelperMethods class with these tests:

    1. **test_extract_topics_from_messages**:
       - Create messages about "data analysis" and "machine learning"
       - Call _extract_topics(messages, [])
       - Verify returns words > 4 chars
       - Verify limited to 5 topics

    2. **test_extract_topics_empty**:
       - Call with empty message list
       - Verify returns []

    3. **test_extract_entities_from_messages**:
       - Create message with email, phone, URL
       - Call _extract_entities(messages, [])
       - Verify email, phone, URL extracted via regex

    4. **test_extract_entities_from_executions**:
       - Create execution with task_description containing capitalized words
       - Call _extract_entities([], executions)
       - Verify capitalized words extracted as entities

    5. **test_calculate_importance**:
       - Test with 15 messages (high importance)
       - Test with 5 messages + 1 execution (medium importance)
       - Test with 2 messages (low importance)
       - Verify score clamped to [0.0, 1.0]

    6. **test_calculate_duration**:
       - Create messages spanning 300 seconds
       - Call _calculate_duration(messages, [])
       - Verify returns 300

    7. **test_get_agent_maturity**:
       - Create AgentRegistry with status = AgentStatus.SUPERVISED
       - Call _get_agent_maturity(agent_id)
       - Verify returns "supervised" (value)

    8. **test_count_interventions**:
       - Create execution with metadata_json = {"human_intervention": true}
       - Call _count_interventions([execution])
       - Verify returns 1

    These are unit tests for helper methods - no async needed.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/test_episode_segmentation_comprehensive.py::TestEpisodeHelperMethods -v
    Expected: 8 tests pass
  </verify>
  <done>
    All helper method tests pass
  </done>
</task>

<task type="auto">
  <name>Generate coverage report and verify</name>
  <files>backend/tests/coverage_reports/backend_phase_162_plan2.json</files>
  <action>
    Generate coverage report for segmentation service:

    1. Run pytest with coverage:
       ```bash
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/episodes/test_episode_segmentation_comprehensive.py -v --cov=core.episode_segmentation_service --cov-report=json --cov-report=term
       ```

    2. Save coverage JSON to backend/tests/coverage_reports/backend_phase_162_plan2.json

    3. Extract key metrics:
       - EpisodeSegmentationService overall coverage percentage
       - Lines covered vs total
       - create_episode_from_session coverage

    4. Verify targeted methods are covered:
       - create_episode_from_session: target 60%+
       - _create_segments: target 70%+
       - _archive_to_lancedb: target 60%+
       - _fetch_canvas_context: target 70%+
       - _extract_canvas_context: target 70%+

    If any method below target, add 1-2 tests to increase coverage.
  </action>
  <verify>
    Run: cat backend/tests/coverage_reports/backend_phase_162_plan2.json | jq '.files["core/episode_segmentation_service.py"].summary'
    Expected: Coverage 45%+ on segmentation service (up from 17.1%)
  </verify>
  <done>
    Segmentation service coverage increased to 45%+, create_episode_from_session tested
  </done>
</task>

</tasks>

<verification>
1. Run all segmentation comprehensive tests: pytest backend/tests/unit/episodes/test_episode_segmentation_comprehensive.py -v
2. Verify 20+ tests pass
3. Verify coverage report shows 45%+ on EpisodeSegmentationService (up from 17.1%)
4. Verify canvas/feedback linkage tested (back-linkage verified)
5. Verify @pytest.mark.asyncio used for async methods
</verification>

<success_criteria>
- EpisodeSegmentationService coverage increases from 17.1% to 45%+
- create_episode_from_session method has full flow tests
- Canvas and feedback linkage is tested
- 20+ new tests created
- Overall backend coverage increases by ~2-3 percentage points
</success_criteria>

<output>
After completion, create `.planning/phases/162-episode-service-comprehensive-testing/162-02-SUMMARY.md` with:
- Tests created count
- Coverage achieved for segmentation service
- Canvas/feedback linkage verification status
- Coverage report location
</output>
