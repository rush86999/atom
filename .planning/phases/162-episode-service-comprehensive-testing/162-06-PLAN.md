---
phase: 162-episode-service-comprehensive-testing
plan: 06
type: execute
wave: 2
depends_on: [162-05]
files_modified:
  - backend/tests/unit/episodes/test_episode_lifecycle_coverage.py
  - backend/tests/unit/episodes/conftest.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All 20 lifecycle service tests pass (no xfailed)"
    - "EpisodeLifecycleService coverage increases to 65%+"
    - "Consolidation tests execute without AttributeError"
    - "Test fixtures use updated schema (consolidated_into column)"
  artifacts:
    - path: "backend/tests/unit/episodes/test_episode_lifecycle_coverage.py"
      provides: "Lifecycle service tests with new schema support"
      min_lines: 1500
    - path: "backend/tests/coverage_reports/backend_phase_162_plan6.json"
      provides: "Coverage report after unblocking tests"
      contains: "EpisodeLifecycleService"
  key_links:
    - from: "backend/tests/unit/episodes/test_episode_lifecycle_coverage.py"
      to: "backend/core/episode_lifecycle_service.py"
      via: "Direct method calls on lifecycle service"
      pattern: "lifecycle_service\\.(consolidate_similar_episodes|decay_old_episodes)"
    - from: "backend/tests/unit/episodes/test_episode_lifecycle_coverage.py"
      to: "backend/core/models.py"
      via: "Episode object creation with consolidated_into"
      pattern: "consolidated_into=.*None"
---

<objective>
Unblock and re-run lifecycle service tests after schema migration

Purpose: Plan 05 added the consolidated_into column to AgentEpisode. This plan removes the xfail markers from blocked tests, updates test fixtures to use the new schema, and verifies that lifecycle service coverage reaches 65%+.

Gap source: 162-VERIFICATION.md - EpisodeLifecycleService achieved 50% coverage (missing 15pp from 65% target). 5 tests xfailed due to missing Episode.consolidated_into schema column.

Output: Updated test file with all tests passing, coverage report showing 65%+
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/162-episode-service-comprehensive-testing/162-VERIFICATION.md
@.planning/phases/162-episode-service-comprehensive-testing/162-01-SUMMARY.md
@.planning/phases/162-episode-service-comprehensive-testing/162-05-PLAN.md
@backend/tests/unit/episodes/test_episode_lifecycle_coverage.py
@backend/tests/unit/episodes/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Remove xfail markers from consolidation tests</name>
  <files>backend/tests/unit/episodes/test_episode_lifecycle_coverage.py</files>
  <action>
    Update test_episode_lifecycle_coverage.py to remove xfail markers from tests blocked by missing consolidated_into column:

    1. Find all tests marked with @pytest.mark.xfail that have reason containing "consolidated_into"
    2. Remove the @pytest.mark.xfail decorator from these tests
    3. Tests to unblock (based on 162-01-SUMMARY.md):
       - test_consolidate_similar_episodes_basic
       - test_consolidate_similar_episodes_similarity_threshold
       - test_consolidate_similar_episodes_already_consolidated
       - test_consolidate_similar_episodes_max_iterations
       - test_consolidate_similar_episodes_consolidation_limit
    4. Verify each test creates Episode objects with consolidated_into=None (the default)
    5. The tests should now run and verify consolidation behavior
  </action>
  <verify>
    grep -n "xfail.*consolidated_into" /Users/rushiparikh/projects/atom/backend/tests/unit/episodes/test_episode_lifecycle_coverage.py returns nothing
    grep -c "def test_consolidate" /Users/rushiparikh/projects/atom/backend/tests/unit/episodes/test_episode_lifecycle_coverage.py returns 5
  </verify>
  <done>All consolidation tests have xfail markers removed, tests will execute</done>
</task>

<task type="auto">
  <name>Update test fixtures for new schema columns</name>
  <files>backend/tests/unit/episodes/conftest.py</files>
  <action>
    Update conftest.py fixtures to use new schema columns:

    1. Find the episode_test_data fixture (around line 521)
    2. Ensure Episode creation includes default values for new columns:
       - consolidated_into=None (default)
       - supervisor_rating=None (default)
       - intervention_types=None (default)
       - supervision_feedback=None (default)
    3. Find the episode_with_supervision fixture (around line 678)
    4. Update it to use the new schema columns directly:
       ```python
       # Supervision fields (now in schema)
       supervisor_id="supervisor_123",
       supervisor_rating=4,
       intervention_types=["human_correction"],
       supervision_feedback="Good performance"
       ```
    5. Remove any kwargs or workarounds for missing columns
    6. Ensure all Episode instantiations in fixtures are compatible with new schema
  </action>
  <verify>
    grep -n "supervisor_rating\|intervention_types\|supervision_feedback" /Users/rushiparikh/projects/atom/backend/tests/unit/episodes/conftest.py returns valid Episode instantiations
    pytest --collect-only backend/tests/unit/episodes/test_episode_lifecycle_coverage.py shows no fixture errors
  </verify>
  <done>All test fixtures use updated schema columns, no fixture errors</done>
</task>

<task type="auto">
  <name>Re-run lifecycle service tests with new schema</name>
  <files>backend/tests/coverage_reports/backend_phase_162_plan6.json</files>
  <action>
    Run the lifecycle service tests with coverage after schema migration:

    1. Ensure database is at latest migration: `cd backend && alembic upgrade head`
    2. Run tests with coverage:
       ```bash
       cd backend
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
         tests/unit/episodes/test_episode_lifecycle_coverage.py \
         -v \
         --cov=core.episode_lifecycle_service \
         --cov-report=term \
         --cov-report=json:tests/coverage_reports/backend_phase_162_plan6.json
       ```
    3. Record test results: count passing, failing, skipped
    4. Check coverage percentage for EpisodeLifecycleService
    5. Target: 65%+ coverage (up from 50%)
    6. Save coverage report to tests/coverage_reports/backend_phase_162_plan6.json
  </action>
  <verify>
    ls -la /Users/rushiparikh/projects/atom/backend/tests/coverage_reports/backend_phase_162_plan6.json
    python -c "import json; d=json.load(open('/Users/rushiparikh/projects/atom/backend/tests/coverage_reports/backend_phase_162_plan6.json')); print(f\"Coverage: {d['files'][0]['summary']['percent_covered']:.1f}%\")"
  </verify>
  <done>Tests pass (20/20), EpisodeLifecycleService coverage at 65%+</done>
</task>

<task type="auto">
  <name>Add additional tests to reach 65% coverage if needed</name>
  <files>backend/tests/unit/episodes/test_episode_lifecycle_coverage.py</files>
  <action>
    If coverage is below 65% after unblocking tests, add additional test cases:

    1. Check coverage report for missing lines in episode_lifecycle_service.py
    2. Identify uncovered branches and edge cases
    3. Common missing coverage areas:
       - Error handling in decay_old_episodes
       - Edge cases in consolidate_similar_episodes (empty results, single episode)
       - Importance score calculation edge cases
       - Access count updates
       - Archive date calculations
    4. Add focused tests for uncovered code paths
    5. Each new test should:
       - Be specific to one uncovered branch
       - Use minimal fixture setup
       - Have clear assertions
    6. Re-run coverage measurement
    7. Continue adding tests until 65%+ achieved

    Example test patterns:
    ```python
    def test_decay_old_episodes_empty_db():
        """Test decay with no episodes in database"""
        result = await lifecycle_service.decay_old_episodes(days=90)
        assert result["decayed_count"] == 0

    def test_importance_score_edge_cases():
        """Test importance score with minimum/maximum values"""
        episode = Episode(importance_score=0.0, ...)
        # Test minimum importance handling
    ```
  </action>
  <verify>
    python -c "import json; d=json.load(open('/Users/rushiparikh/projects/atom/backend/tests/coverage_reports/backend_phase_162_plan6.json')); print(f\"Coverage: {d['files'][0]['summary']['percent_covered']:.1f}%\")" | grep -E "6[5-9]\.[0-9]|7[0-9]\.[0-9]|8[0-9]\.[0-9]|9[0-9]\.[0-9]|100\.0"
  </verify>
  <done>EpisodeLifecycleService coverage at 65%+ (minimum threshold met)</done>
</task>

</tasks>

<verification>
After completing all tasks:

1. Run full lifecycle test suite: `pytest backend/tests/unit/episodes/test_episode_lifecycle_coverage.py -v`
2. Verify 20/20 tests passing (no xfailed, no failed)
3. Check coverage report shows 65%+ for EpisodeLifecycleService
4. Verify consolidation tests create episodes with consolidated_into properly set
</verification>

<success_criteria>
- All 20 lifecycle tests passing (no xfailed)
- EpisodeLifecycleService coverage >= 65%
- Coverage report generated: backend_phase_162_plan6.json
- Consolidation feature tests execute without AttributeError
</success_criteria>

<output>
After completion, create `.planning/phases/162-episode-service-comprehensive-testing/162-06-SUMMARY.md`
</output>
