---
phase: 162-episode-service-comprehensive-testing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/episodes/test_episode_supervision_skill.py
  - backend/tests/unit/episodes/conftest.py
autonomous: true

must_haves:
  truths:
    - "create_supervision_episode creates episodes from SupervisionSession"
    - "Supervision episode includes intervention types and ratings"
    - "create_skill_episode creates episodes from skill execution"
    - "Skill episodes track success/failure and execution time"
    - "Supervision segments capture agent actions, interventions, outcomes"
    - "Test coverage increases by ~1-2 percentage points"
  artifacts:
    - path: backend/tests/unit/episodes/test_episode_supervision_skill.py
      provides: "Supervision and skill episode tests"
      min_lines: 400
  key_links:
    - from: "backend/tests/unit/episodes/test_episode_supervision_skill.py"
      to: "backend/core/episode_segmentation_service.py"
      via: "from core.episode_segmentation_service import EpisodeSegmentationService"
      pattern: "create_supervision_episode|create_skill_episode"
---

<objective>
**Supervision and Skill Episode Testing**

Test the specialized episode creation methods for supervision sessions and skill executions. These are critical for agent graduation tracking and skill performance monitoring.

**Purpose:** Supervision episodes track agent learning with human guidance, while skill episodes track community skill execution. Both were untested in Phase 161.

**Output:** ~15 tests for supervision and skill episodes, targeting ~1-2% overall backend coverage increase.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-episode-service-comprehensive-testing/162-RESEARCH.md
@.planning/phases/161-model-fixes-and-database/161-VERIFICATION.md
@backend/core/episode_segmentation_service.py (lines 1088-1548)
@backend/tests/unit/episodes/test_episode_segmentation_coverage.py
@backend/tests/unit/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Add supervision and skill fixtures to conftest.py</name>
  <files>backend/tests/unit/episodes/conftest.py</files>
  <action>
    Add these fixtures to backend/tests/unit/episodes/conftest.py:

    1. **episode_test_supervision_session(db_session)**: SupervisionSession instance
       - id: str(uuid.uuid4())
       - agent_id: "test_agent"
       - supervisor_id: "test_supervisor"
       - status: "completed"
       - supervisor_rating: 4
       - intervention_count: 2
       - interventions: [{"type": "human_correction", "timestamp": "2026-03-10T10:00:00Z"}]
       - started_at: datetime.now(timezone.utc) - timedelta(hours=1)
       - completed_at: datetime.now(timezone.utc)

    2. **episode_test_skill_execution()**: Dict representing skill execution
       - skill_name: "data_analyzer"
       - inputs: {"dataset": "sales_2024.csv", "format": "json"}
       - result: {"total": 1000000, "records": 500}
       - error: None
       - execution_time: 1.5

    3. **episode_test_failed_skill_execution()**: Failed skill execution
       - skill_name: "email_sender"
       - error: Exception("SMTP connection failed")
       - execution_time: 0.5

    Use conftest.py from backend/tests/unit/episodes/ if it already exists.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/conftest.py --collect-only
    Expected: New fixtures collected without errors
  </verify>
  <done>
    All supervision and skill fixtures defined
  </done>
</task>

<task type="auto">
  <name>Test supervision episode creation</name>
  <files>backend/tests/unit/episodes/test_episode_supervision_skill.py</files>
  <action>
    Create TestSupervisionEpisodeCreation class with these tests:

    1. **test_create_supervision_episode_from_session** (async):
       - Create SupervisionSession with interventions
       - Create AgentExecution linked to session
       - Mock lancedb.search, add_document, create_table
       - Call await segmentation_service.create_supervision_episode(session, execution, db)
       - Verify episode created with:
         * title contains "Supervision Session"
         * supervisor_rating from session
         * intervention_count from session
         * intervention_types extracted from interventions list
         * maturity_at_time = "supervised"

    2. **test_create_supervision_episode_with_multiple_interventions** (async):
       - Create session with 3 different intervention types
       - Verify all intervention types extracted
       - Verify intervention_count = 3

    3. **test_create_supervision_episode_segments** (async):
       - Call create_supervision_episode()
       - Verify 3 segments created:
         * "execution" segment for agent actions
         * "intervention" segment for supervisor interventions
         * "reflection" segment for outcome

    4. **test_create_supervision_episode_archives_to_lancedb** (async):
       - Mock lancedb methods
       - Call create_supervision_episode()
       - Verify _archive_supervision_episode_to_lancedb called
       - Verify metadata includes supervision-specific fields

    5. **test_format_supervision_outcome**:
       - Create SupervisionSession with rating=5, feedback="Great work"
       - Call _format_supervision_outcome(session)
       - Verify output contains rating, feedback, duration

    6. **test_extract_supervision_topics**:
       - Create session with agent_name="DataAnalyzer"
       - Create execution with task_description="analyze Q4 sales"
       - Call _extract_supervision_topics(session, execution)
       - Verify topics include agent and task keywords

    Use @pytest.mark.asyncio for async tests.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/test_episode_supervision_skill.py::TestSupervisionEpisodeCreation -v
    Expected: 6 tests pass
  </verify>
  <done>
    All supervision episode creation tests pass
  </done>
</task>

<task type="auto">
  <name>Test supervision episode helper methods</name>
  <files>backend/tests/unit/episodes/test_episode_supervision_skill.py</files>
  <action>
    Create TestSupervisionHelperMethods class with these tests:

    1. **test_format_agent_actions**:
       - Create AgentExecution with task_description, status, input_summary, output_summary
       - Call _format_agent_actions(interventions, execution)
       - Verify output contains task, status, input, output

    2. **test_format_interventions**:
       - Create interventions list with type, timestamp, guidance
       - Call _format_interventions(interventions)
       - Verify formatted output with numbered list

    3. **test_extract_supervision_entities**:
       - Create SupervisionSession
       - Call _extract_supervision_entities(session, execution)
       - Verify returns session_id, agent_id, supervisor_id

    4. **test_calculate_supervision_importance_high_rating**:
       - Create session with supervisor_rating=5, intervention_count=0
       - Call _calculate_supervision_importance(session)
       - Verify score >= 0.7 (high importance)

    5. **test_calculate_supervision_importance_low_rating**:
       - Create session with supervisor_rating=2, intervention_count=5
       - Call _calculate_supervision_importance(session)
       - Verify score <= 0.4 (low importance)

    6. **test_calculate_supervision_importance_clamped**:
       - Create session with extreme values
       - Verify score clamped to [0.0, 1.0]

    These are unit tests for helper methods - no async needed.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/test_episode_supervision_skill.py::TestSupervisionHelperMethods -v
    Expected: 6 tests pass
  </verify>
  <done>
    All supervision helper method tests pass
  </done>
</task>

<task type="auto">
  <name>Test skill episode creation</name>
  <files>backend/tests/unit/episodes/test_episode_supervision_skill.py</files>
  <action>
    Create TestSkillEpisodeCreation class with these tests:

    1. **test_create_skill_episode_success** (async):
       - Call await segmentation_service.create_skill_episode(
           agent_id="agent1",
           skill_name="data_analyzer",
           inputs={"dataset": "sales.csv"},
           result={"total": 1000000},
           error=None,
           execution_time=1.5
         )
       - Verify EpisodeSegment created with:
         * segment_type = "skill_execution"
         * source_type = "skill_execution"
         * content_summary contains "Success"
         * metadata contains skill_name, execution_time, etc.

    2. **test_create_skill_episode_failure** (async):
       - Call with error=Exception("Connection failed")
       - Verify content_summary contains "Failed"
       - Verify metadata contains error_type and error_message

    3. **test_create_skill_episode_metadata_extraction**:
       - Call extract_skill_metadata(context_data)
       - Verify returns skill_name, skill_source, execution_successful
       - Verify input_hash is SHA256 hash of input_summary

    4. **test_summarize_skill_inputs**:
       - Call _summarize_skill_inputs({"long_key": "x" * 200})
       - Verify long values truncated to 100 chars
       - Verify returns dict as string

    5. **test_format_skill_content_success**:
       - Call _format_skill_content("skill_name", result={"data": 123}, error=None)
       - Verify output contains "Status: Success" and result

    6. **test_format_skill_content_failure**:
       - Call _format_skill_content("skill_name", result=None, error=Exception("Failed"))
       - Verify output contains "Status: Failed" and error type

    Use @pytest.mark.asyncio for async tests.
  </action>
  <verify>
    Run: pytest backend/tests/unit/episodes/test_episode_supervision_skill.py::TestSkillEpisodeCreation -v
    Expected: 6 tests pass
  </verify>
  <done>
    All skill episode creation tests pass
  </done>
</task>

<task type="auto">
  <name>Generate coverage report and verify</name>
  <files>backend/tests/coverage_reports/backend_phase_162_plan3.json</files>
  <action>
    Generate coverage report for supervision and skill methods:

    1. Run pytest with coverage:
       ```bash
       PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/episodes/test_episode_supervision_skill.py -v --cov=core.episode_segmentation_service --cov-report=json --cov-report=term
       ```

    2. Save coverage JSON to backend/tests/coverage_reports/backend_phase_162_plan3.json

    3. Extract key metrics:
       - Coverage for create_supervision_episode method
       - Coverage for create_skill_episode method
       - Coverage for helper methods

    4. Verify targeted methods are covered:
       - create_supervision_episode: target 70%+
       - create_skill_episode: target 80%+ (smaller method)
       - _format_supervision_outcome: target 80%+
       - extract_skill_metadata: target 80%+

    If any method below target, add 1-2 tests to increase coverage.
  </action>
  <verify>
    Run: cat backend/tests/coverage_reports/backend_phase_162_plan3.json | jq '.files["core/episode_segmentation_service.py"].summary'
    Expected: Additional 5-8% coverage on segmentation service from supervision/skill methods
  </verify>
  <done>
    Supervision and skill episode methods tested
  </done>
</task>

</tasks>

<verification>
1. Run all supervision/skill tests: pytest backend/tests/unit/episodes/test_episode_supervision_skill.py -v
2. Verify 15+ tests pass
3. Verify supervision episode segments created correctly
4. Verify skill episode metadata captured
5. Verify coverage increase on segmentation service
</verification>

<success_criteria>
- create_supervision_episode has 70%+ coverage
- create_skill_episode has 80%+ coverage
- 15+ new tests created
- Supervision segments (agent actions, interventions, outcome) tested
- Skill episode metadata (success/failure, execution time) tested
- Overall backend coverage increases by ~1-2 percentage points
</success_criteria>

<output>
After completion, create `.planning/phases/162-episode-service-comprehensive-testing/162-03-SUMMARY.md` with:
- Tests created count
- Coverage achieved for supervision/skill methods
- Any issues encountered
- Coverage report location
</output>
