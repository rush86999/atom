---
phase: 130-frontend-module-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/coverage/baseline-130.json
  - frontend-nextjs/scripts/coverage-audit.js
  - .planning/phases/130-frontend-module-coverage/130-01-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "Coverage measurement is accurate and reproducible"
    - "Discrepancy between ROADMAP (89.84%) and actual (4.87%) is documented"
    - "Baseline report includes per-module breakdown by directory"
    - "Test infrastructure is verified working (Jest, RTL, MSW)"
  artifacts:
    - path: "frontend-nextjs/coverage/baseline-130.json"
      provides: "Baseline coverage metrics before adding tests"
      min_lines: 50
    - path: "frontend-nextjs/scripts/coverage-audit.js"
      provides: "Coverage analysis script for per-module reporting"
      min_lines: 100
    - path: ".planning/phases/130-frontend-module-coverage/130-01-SUMMARY.md"
      provides: "Summary of audit findings and baseline status"
      min_lines: 100
  key_links:
    - from: "scripts/coverage-audit.js"
      to: "coverage/coverage-summary.json"
      via: "fs.readFileSync parsing"
      pattern: "JSON\\.parse\\(fs\\.readFileSync"
    - from: "scripts/coverage-audit.js"
      to: "ROADMAP.md"
      via: "Documentation of discrepancy"
      pattern: "89\\.84.*4\\.87"
---

# Plan 01: Coverage Audit & Baseline Verification

<objective>
Establish accurate frontend coverage baseline and verify the discrepancy between claimed coverage (89.84% in ROADMAP.md) and actual measured coverage (4.87%).

**Purpose:** Before adding tests, we must verify our measurement is accurate and understand why ROADMAP claims 89.84% when actual coverage is 4.87%.

**Output:** Verified baseline report, coverage audit script, documented discrepancy root cause
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/130-frontend-module-coverage/130-RESEARCH.md
@frontend-nextjs/jest.config.js
@frontend-nextjs/package.json
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Jest configuration and re-run coverage</name>
  <files>frontend-nextjs/jest.config.js</files>
  <action>
Verify Jest configuration is correct:
1. Check collectCoverageFrom includes all source directories (components/, pages/, lib/, hooks/)
2. Confirm coverageReporters includes "json-summary" for per-module data
3. Verify testMatch patterns are finding existing tests
4. Run `npm run test:coverage` to generate fresh coverage data
5. Capture full output including test pass/fail summary

Do NOT modify jest.config.js unless there's a clear bug preventing accurate coverage collection.
  </action>
  <verify>
cd /Users/rushiparikh/projects/atom/frontend-nextjs && npm run test:coverage 2>&1 | tee /tmp/coverage-output.txt
grep -E "(Test Suites:|Tests:|Statements.*%|Branches.*%|Functions.*%|Lines.*%)" /tmp/coverage-output.txt
  </verify>
  <done>
Coverage runs successfully and coverage/coverage-summary.json is generated with 660+ file entries
  </done>
</task>

<task type="auto">
  <name>Task 2: Create coverage audit script</name>
  <files>frontend-nextjs/scripts/coverage-audit.js</files>
  <action>
Create `frontend-nextjs/scripts/coverage-audit.js` that:
1. Reads coverage/coverage-summary.json
2. Generates detailed per-module breakdown report
3. Groups modules by directory (components/, lib/, hooks/, pages/)
4. Identifies modules at each coverage tier: 0%, 1-25%, 26-50%, 51-79%, 80-100%
5. Lists top 20 and bottom 20 modules by coverage
6. Calculates directory-level totals
7. Outputs report to stdout and writes coverage/baseline-130.json

Script should be Node.js (no TypeScript compilation needed), handle missing files gracefully, and format output as markdown tables.

Reference the analyze_coverage.js created during research as a starting point, but enhance with:
- Coverage tier grouping
- Business value categorization (integration vs core vs ui)
- Estimated testing effort for each module
  </action>
  <verify>
cd /Users/rushiparikh/projects/atom/frontend-nextjs && node scripts/coverage-audit.js | head -100
test -f coverage/baseline-130.json && cat coverage/baseline-130.json | jq '.total'
  </verify>
  <done>
Script runs without errors and generates baseline-130.json with total coverage metrics
  </done>
</task>

<task type="auto">
  <name>Task 3: Document coverage discrepancy root cause</name>
  <files>.planning/phases/130-frontend-module-coverage/130-01-SUMMARY.md</files>
  <action>
Create summary document documenting:
1. **Actual baseline metrics**: Lines %, Statements %, Functions %, Branches %
2. **Directory breakdown**: components/, lib/, hooks/, pages/ with coverage percentages
3. **Discrepancy analysis**: Explain why ROADMAP shows 89.84% vs actual 4.87%
   - Investigate: Was 89.84% measured on tested files only?
   - Check: Were directories excluded from previous measurements?
   - Note: Current measurement includes ALL source files per jest.config.js
4. **Zero-coverage modules**: Count and list top 20 by line count
5. **Test infrastructure status**: Jest version, RTL version, test count, pass rate
6. **Recommendations**: Is 80% target realistic? Should focus be on critical paths?

Format as markdown with sections, bullet points, and code blocks for data.
  </action>
  <verify>
cat .planning/phases/130-frontend-module-coverage/130-01-SUMMARY.md | grep -E "(Actual baseline|Directory breakdown|Discrepancy|Zero-coverage modules|Test infrastructure|Recommendations)" | wc -l
test -f .planning/phases/130-frontend-module-coverage/130-01-SUMMARY.md && grep -q "4.87%" .planning/phases/130-frontend-module-coverage/130-01-SUMMARY.md
  </verify>
  <done>
Summary document exists with all 6 sections, documents 4.87% actual vs 89.84% claimed
  </done>
</task>

</tasks>

<verification>
Overall phase verification:
1. Coverage re-run completes successfully
2. Baseline JSON saved to coverage/baseline-130.json
3. Audit script generates detailed per-module breakdown
4. Discrepancy root cause documented in SUMMARY.md
5. Summary includes recommendation on 80% target feasibility
</verification>

<success_criteria>
1. Baseline coverage established: 4.87% lines (±0.5% variance acceptable)
2. Per-module breakdown available in baseline-130.json
3. Discrepancy between ROADMAP and actual documented with root cause hypothesis
4. Audit script can be reused for future coverage measurement
5. Clear understanding of which modules need tests for 80% target
</success_criteria>

<output>
After completion, create `.planning/phases/130-frontend-module-coverage/130-01-SUMMARY.md` with:
- Baseline coverage metrics
- Directory-level breakdown
- Discrepancy root cause analysis
- Test infrastructure status
- Recommendations for Plans 02-06
