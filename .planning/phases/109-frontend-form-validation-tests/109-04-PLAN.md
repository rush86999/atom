---
phase: 109-frontend-form-validation-tests
plan: 04
type: execute
wave: 2
depends_on: [109-01, 109-02]
files_modified:
  - frontend-nextjs/tests/property/__tests__/form-validation-invariants.test.ts
  - frontend-nextjs/tests/property/__tests__/validation-property-tests.test.ts
autonomous: true

must_haves:
  truths:
    - "Required validation invariant: empty values always fail, non-empty always pass"
    - "Range validation invariant: min/max boundaries are correctly enforced"
    - "Pattern validation invariant: regex patterns match correctly across random inputs"
    - "Property tests use 50-100 examples for validation invariants"
  artifacts:
    - path: frontend-nextjs/tests/property/__tests__/form-validation-invariants.test.ts
      provides: Property tests for form validation invariants
      min_lines: 600
      exports: []
    - path: frontend-nextjs/tests/property/__tests__/validation-property-tests.test.ts
      provides: Property tests for validation utilities
      min_lines: 400
      exports: []
  key_links:
    - from: frontend-nextjs/tests/property/__tests__/form-validation-invariants.test.ts
      to: frontend-nextjs/components/canvas/InteractiveForm.tsx
      via: FastCheck property testing
      pattern: import.*fc.*from.*fast-check
    - from: frontend-nextjs/tests/property/__tests__/validation-property-tests.test.ts
      to: frontend-nextjs/lib/validation.ts
      via: FastCheck property testing
      pattern: fc\.assert.*fc\.property
---

<objective>
**What:** Create FastCheck property-based tests for form validation invariants

**Purpose:** Validate validation rules hold true across randomly generated inputs (FRNT-05 Criterion 1 + Phase 108 patterns)

**Output:** Two test files with 1000+ lines of property tests using FastCheck
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/109-frontend-form-validation-tests/109-RESEARCH.md

# FastCheck patterns from Phase 108
@frontend-nextjs/tests/property/__tests__/chat-state-machine.test.ts
@frontend-nextjs/tests/property/__tests__/state-transition-validation.test.ts
@frontend-nextjs/components/canvas/InteractiveForm.tsx
@frontend-nextjs/lib/validation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create form validation property tests</name>
  <files>frontend-nextjs/tests/property/__tests__/form-validation-invariants.test.ts</files>
  <action>
Create `frontend-nextjs/tests/property/__tests__/form-validation-invariants.test.ts` with FastCheck property tests:

**Test Structure:**
```typescript
import fc from 'fast-check';
import { render, screen, waitFor } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import '@testing-library/jest-dom';
import { InteractiveForm } from '@/components/canvas/InteractiveForm';

// Mock window.atom.canvas
const mockCanvasAPI = {
  getState: jest.fn(),
  getAllStates: jest.fn(),
  subscribe: jest.fn(),
  subscribeAll: jest.fn()
};

beforeEach(() => {
  (window as any).atom = { canvas: mockCanvasAPI };
  jest.clearAllMocks();
});

afterEach(() => {
  delete (window as any).atom;
});
```

**Property Tests (600+ lines, 100 runs each):**

1. **Required Field Invariants** (4 tests, 150 lines)
   ```typescript
   test('required validation rejects empty strings', () => {
     fc.assert(
       fc.property(
         fc.string(),
         (value) => {
           // Empty strings should always fail required validation
           if (!value || value.trim().length === 0) {
             const mockSubmit = jest.fn().mockResolvedValue({});
             const fields = [{
               name: 'test',
               label: 'Test',
               type: 'text' as const,
               required: true
             }];
             render(<InteractiveForm fields={fields} onSubmit={mockSubmit} />);
             // Submit empty and verify rejection
             // ... test implementation
             return true;
           }
           return true;
         }
       ),
       { numRuns: 100 }
     );
   });
   ```

2. **Numeric Range Invariants** (4 tests, 150 lines)
   - Numbers below min always fail
   - Numbers above max always fail
   - Numbers within range always pass
   - Boundary values (min, max) always pass

3. **String Length Invariants** (4 tests, 150 lines)
   - Strings shorter than min always fail
   - Strings longer than max always fail
   - Strings within range always pass
   - Boundary lengths (min, max) always pass

4. **Pattern Validation Invariants** (4 tests, 150 lines)
   - Valid email patterns contain @ and domain
   - Alphanumeric patterns reject special chars
   - Numeric patterns reject letters
   - Custom patterns match correctly

5. **Multi-Field Validation Invariants** (3 tests, 100 lines)
   - All required fields must be non-empty
   - Validation is independent across fields
   - Submit blocked if any field invalid

**FastCheck Patterns:**
```typescript
// Use appropriate arbitraries
fc.string()         // Random strings
fc.nat()            // Natural numbers
fc.float()          // Floating point
fc.constantFrom()   // Fixed values
fc.tuple()          // Combined arbitraries
fc.record()         // Object with specified shape

// Configure runs
{ numRuns: 50 }     // For IO-bound (form tests)
{ seed: 24001 }     // Reproducible tests
```
  </action>
  <verify>
```bash
cd frontend-nextjs && npm test -- form-validation-invariants.test.ts --no-coverage
```
All tests should pass (19+ property tests with 100 runs each)
  </verify>
  <done>Form validation property tests created with 19+ invariants</done>
</task>

<task type="auto">
  <name>Task 2: Create validation utility property tests</name>
  <files>frontend-nextjs/tests/property/__tests__/validation-property-tests.test.ts</files>
  <action>
Create `frontend-nextjs/tests/property/__tests__/validation-property-tests.test.ts` with validation utility property tests:

**Test Structure:**
```typescript
import fc from 'fast-check';
import {
  validateEmail,
  validateRequired,
  validateLength,
  validateUrl,
  validatePhone,
  validatePasswordStrength,
  validateRange,
  validatePattern
} from '@/lib/validation';
```

**Property Tests (400+ lines, 50-100 runs each):**

1. **validateRequired Invariants** (3 tests, 80 lines)
   ```typescript
   test('required validation rejects only falsy empty values', () => {
     fc.assert(
       fc.property(
         fc.anything(),
         (value) => {
           // Empty array, empty string, null, undefined should fail
           const isEmpty = value === '' ||
                          value === null ||
                          value === undefined ||
                          (Array.isArray(value) && value.length === 0) ||
                          (typeof value === 'string' && value.trim().length === 0);

           const result = validateRequired(value as any);
           if (isEmpty) {
             return result === false;
           }
           return result === true;
         }
       ),
       { numRuns: 100 }
     );
   });
   ```

2. **validateEmail Invariants** (3 tests, 80 lines)
   - Valid emails always contain exactly one @
   - Valid emails always have non-empty domain
   - Valid emails never start/end with @

3. **validateLength Invariants** (3 tests, 80 lines)
   - String length <= max always passes max constraint
   - String length >= min always passes min constraint
   - Length validations are transitive (a <= b <= c)

4. **validateUrl Invariants** (3 tests, 80 lines)
   - Valid URLs always have :// separator
   - Valid URLs always have non-empty protocol
   - Valid URLs always have non-empty host

5. **validateRange Invariants** (3 tests, 80 lines)
   - value >= min passes min check
   - value <= max passes max check
   - NaN always fails range validation

6. **validatePattern Invariants** (2 tests, 60 lines)
   - Matching strings always pass
   - Non-matching strings always fail
   - Pattern behavior is consistent

**Property Test Pattern from Phase 108:**
```typescript
test('invariant description', () => {
  fc.assert(
    fc.property(
      fc.string(),           // Input generator
      (input) => {           // Predicate
        // Test implementation
        return invariant;     // Boolean result
      }
    ),
    { numRuns: 50, seed: 24100 }  // Configuration
  );
});
```
  </action>
  <verify>
```bash
cd frontend-nextjs && npm test -- validation-property-tests.test.ts --no-coverage
```
All tests should pass (17+ property tests)
  </verify>
  <done>Validation utility property tests created with 17+ invariants</done>
</task>

</tasks>

<verification>
Run both test files:
```bash
cd frontend-nextjs && npm test -- --testPathPattern="form-validation-invariants|validation-property-tests" --no-coverage
```

Expected results:
- 36+ property tests passing
- 1000+ lines of property test code
- All invariants validated across random inputs
- numRuns configured appropriately (50-100)
</verification>

<success_criteria>
1. Form validation property test file created: form-validation-invariants.test.ts (600+ lines, 19+ tests)
2. Validation utility property test file created: validation-property-tests.test.ts (400+ lines, 17+ tests)
3. All property tests use FastCheck fc.assert/fc.property pattern
4. numRuns: 50-100 for all tests (Phase 108 pattern)
5. Invariants cover required, range, length, pattern validations
</success_criteria>

<output>
After completion, create `.planning/phases/109-frontend-form-validation-tests/109-04-SUMMARY.md` with:
- Property test coverage
- Invariants validated
- Any invariant violations discovered
- FastCheck configuration summary
</output>
