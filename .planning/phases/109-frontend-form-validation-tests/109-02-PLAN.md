---
phase: 109-frontend-form-validation-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/components/canvas/__tests__/form-format-validation.test.tsx
  - frontend-nextjs/lib/__tests__/validation-patterns.test.ts
autonomous: true

must_haves:
  truths:
    - "Email format validation accepts valid emails and rejects invalid formats"
    - "Phone number validation accepts multiple international formats"
    - "URL validation accepts http, https, ftp protocols and rejects malformed URLs"
    - "Custom regex patterns validate correctly with proper error messages"
  artifacts:
    - path: frontend-nextjs/components/canvas/__tests__/form-format-validation.test.tsx
      provides: Format validation tests for InteractiveForm
      min_lines: 500
      exports: []
    - path: frontend-nextjs/lib/__tests__/validation-patterns.test.ts
      provides: Pattern validation tests for validation utilities
      min_lines: 350
      exports: []
  key_links:
    - from: frontend-nextjs/components/canvas/__tests__/form-format-validation.test.tsx
      to: frontend-nextjs/components/canvas/InteractiveForm.tsx
      via: import InteractiveForm with pattern validation
      pattern: validation:.*pattern
    - from: frontend-nextjs/lib/__tests__/validation-patterns.test.ts
      to: frontend-nextjs/lib/validation.ts
      via: import pattern validation functions
      pattern: validatePattern|validateEmail|validatePhone|validateUrl
---

<objective>
**What:** Create format validation tests for email, phone, URL, and custom patterns

**Purpose:** Ensure all format validations work correctly with valid and invalid inputs (FRNT-05 Criterion 1)

**Output:** Two test files with 850+ lines covering email, phone, URL, and custom pattern validation
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/109-frontend-form-validation-tests/109-RESEARCH.md

# Reference patterns
@frontend-nextjs/components/canvas/__tests__/interactive-form.test.tsx
@frontend-nextjs/lib/__tests__/validation.test.ts
@frontend-nextjs/components/canvas/InteractiveForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create InteractiveForm format validation tests</name>
  <files>frontend-nextjs/components/canvas/__tests__/form-format-validation.test.tsx</files>
  <action>
Create `frontend-nextjs/components/canvas/__tests__/form-format-validation.test.tsx` with format validation tests:

**Test Structure:**
```typescript
import { render, screen, waitFor } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import '@testing-library/jest-dom';
import { InteractiveForm } from '../InteractiveForm';

// Mock window.atom.canvas
const mockCanvasAPI = {
  getState: jest.fn(),
  getAllStates: jest.fn(),
  subscribe: jest.fn(),
  subscribeAll: jest.fn()
};

beforeEach(() => {
  (window as any).atom = { canvas: mockCanvasAPI };
  jest.clearAllMocks();
});

afterEach(() => {
  delete (window as any).atom;
});
```

**Test Groups (500+ lines):**

1. **Email Format Validation** (12 tests, 150 lines)
   - Accepts standard format: `user@example.com`
   - Accepts with subdomain: `user@mail.example.com`
   - Accepts with plus: `user+tag@example.com`
   - Accepts with numbers: `user123@example.com`
   - Accepts with dots: `first.last@example.com`
   - Accepts international TLD: `user@example.co.uk`
   - Rejects missing @: `userexample.com`
   - Rejects missing domain: `user@`
   - Rejects missing user: `@example.com`
   - Rejects double @: `user@@example.com`
   - Rejects trailing dot: `user@example.`
   - Rejects leading dot: `.user@example.com`

2. **Phone Format Validation** (10 tests, 120 lines)
   - Accepts 10-digit: `1234567890`
   - Accepts formatted: `(123) 456-7890`
   - Accepts dashes: `123-456-7890`
   - Accepts spaces: `123 456 7890`
   - Accepts international: `+44 20 1234 5678`
   - Accepts with extension: `123-456-7890 x123`
   - Rejects too short: `123`
   - Rejects letters only: `abcdefghij`
   - Rejects mixed alphanumeric: `123abc4567`
   - Rejects empty after validation

3. **URL Format Validation** (10 tests, 120 lines)
   - Accepts http: `http://example.com`
   - Accepts https: `https://example.com`
   - Accepts with path: `https://example.com/path`
   - Accepts with query: `https://example.com?query=value`
   - Accepts with port: `https://example.com:8080`
   - Accepts ftp: `ftp://example.com`
   - Rejects no protocol: `example.com`
   - Rejects invalid protocol: `mailto://example.com` (depending on validation)
   - Rejects empty protocol: `://example.com`
   - Rejects malformed: `https://`

4. **Custom Pattern Validation** (8 tests, 100 lines)
   - Custom regex pattern: `^\d{3}-\d{3}-\d{4}$` for phone
   - Custom pattern: `^[A-Z]{2}\d{4}$` for product codes
   - Custom error messages display correctly
   - Pattern flags (case sensitivity)
   - Pattern with character classes: `^[a-zA-Z0-9]+$`
   - Pattern with anchors: `^...$` vs `...`
   - Pattern with quantifiers: `{3,5}`, `*`, `+`, `?`
   - Complex pattern: password requirements

**Key Pattern:** Use validation.pattern in field config, test both valid and invalid cases
  </action>
  <verify>
```bash
cd frontend-nextjs && npm test -- form-format-validation.test.tsx --no-coverage
```
All tests should pass (40+ tests expected)
  </verify>
  <done>Format validation test file created with 40+ tests</done>
</task>

<task type="auto">
  <name>Task 2: Create validation pattern utility tests</name>
  <files>frontend-nextjs/lib/__tests__/validation-patterns.test.ts</files>
  <action>
Create `frontend-nextjs/lib/__tests__/validation-patterns.test.ts` with comprehensive pattern validation tests:

**Test Structure:**
```typescript
import {
  validateEmail,
  validateUrl,
  validatePhone,
  validatePattern
} from '../validation';
```

**Test Groups (350+ lines):**

1. **validateEmail Comprehensive Tests** (18 tests, 140 lines)
   - RFC 5322 valid formats
   - Edge cases: quoted strings, comments
   - IDN (Internationalized Domain Names)
   - IP address literals: `user@[127.0.0.1]`
   - Long local parts
   - Multiple @ signs rejection
   - Trailing dots rejection
   - Leading dots rejection
   - Consecutive dots rejection
   - Top-level domain requirements
   - Plus addressing
   - Subdomain depth
   - Numeric local parts
   - Underscore in local part
   - Hyphen in domain

2. **validateUrl Comprehensive Tests** (12 tests, 100 lines)
   - Protocol validation: http, https, ftp, sftp
   - Port numbers: valid range 1-65535
   - Query strings with multiple params
   - Fragments (#section)
   - Auth sections: user:pass@host
   - IPv4 host: `http://127.0.0.1`
   - IPv6 host: `http://[::1]`
   - Punycode domains
   - Relative paths rejection
   - Protocol-relative URLs: `//example.com`
   - File: protocol handling
   - Data URI handling

3. **validatePhone Comprehensive Tests** (12 tests, 100 lines)
   - NANP format (North America): 10 digits
   - E.164 format: `+1234567890`
   - Various separators: dashes, dots, spaces, parentheses
   - Extensions: x123, ext. 123, #123
   - Country codes: +1, +44, +86, etc.
   - Minimum digit requirements
   - Maximum digit limits
   - Letters rejection
   - Partial number rejection
   - International format variations

4. **validatePattern Comprehensive Tests** (10 tests, 90 lines)
   - Simple alphanumeric: `^[a-zA-Z0-9]+$`
   - Username pattern: `^[a-zA-Z0-9_]{3,20}$`
   - Product code: `^[A-Z]{2}-\d{4}$`
   - Postal code variants
   - Date format: `^\d{4}-\d{2}-\d{2}$`
   - Credit card format: `^\d{4}[-\s]?\d{4}[-\s]?\d{4}[-\s]?\d{4}$`
   - Hex color: `^#[0-9A-Fa-f]{6}$`
   - Social Security: `^\d{3}-\d{2}-\d{4}$`
   - Time format: `^\d{2}:\d{2}:\d{2}$`
   - Complex password pattern

**Pattern:** Test both acceptance and rejection for each format, use realistic examples from production
  </action>
  <verify>
```bash
cd frontend-nextjs && npm test -- validation-patterns.test.ts --no-coverage
```
All tests should pass (50+ tests expected)
  </verify>
  <done>Validation pattern tests created with 50+ tests</done>
</task>

</tasks>

<verification>
Run both test files:
```bash
cd frontend-nextjs && npm test -- --testPathPattern="form-format-validation|validation-patterns" --no-coverage
```

Expected results:
- 90+ tests passing
- 850+ lines of test code
- All format validations tested (email, phone, URL, custom patterns)
</verification>

<success_criteria>
1. Format validation test file created: form-format-validation.test.tsx (500+ lines, 40+ tests)
2. Validation pattern test file created: validation-patterns.test.ts (350+ lines, 50+ tests)
3. Email validation covers valid/invalid formats (20+ cases)
4. Phone validation covers international formats (10+ cases)
5. URL validation covers protocols and edge cases (15+ cases)
6. Custom pattern validation with regex patterns (10+ cases)
</success_criteria>

<output>
After completion, create `.planning/phases/109-frontend-form-validation-tests/109-02-SUMMARY.md` with:
- Format coverage metrics
- Pattern test results
- Any format validation bugs discovered
</output>
