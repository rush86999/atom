---
phase: 201-coverage-push-85
plan: 06
type: execute
wave: 2
depends_on: [201-01]
files_modified:
  - backend/tests/cli/
  - backend/cli/
autonomous: true

must_haves:
  truths:
    - "CLI command functions are covered"
    - "Argument parsing tested"
    - "Error handling validated"
    - "Coverage increases from 16% baseline"
  artifacts:
    - path: "backend/tests/cli/test_cli_coverage.py"
      provides: "CLI comprehensive test coverage"
      contains: "test_cli_commands, test_argument_parsing, test_error_handling"
  key_links:
    - from: "backend/tests/cli/test_cli_coverage.py"
      to: "backend/cli/main.py"
      via: "Import and test CLI entry points"
      pattern: "from cli.main import"
---

<objective>
Achieve 60%+ coverage for CLI module (from 16% baseline) by testing command-line interface functions.

CLI module provides command-line access to Atom functionality. Current coverage is 16% (136/718 lines covered).

Purpose: Cover CLI commands, argument parsing, and error handling
Output: Test suite for CLI module with 60%+ coverage
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-fix-collection-errors/200-05-SUMMARY.md
@.planning/phases/201-coverage-push-85/201-01-SUMMARY.md

# CLI context from coverage analysis
- Current coverage: 16% (136/718 lines covered)
- Missing lines: 582
- Key files: main.py (entry point), daemon.py (daemon management), commands/
- 6 files total in cli/ directory
- Click/Typer CLI framework likely used
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze CLI structure and create test infrastructure</name>
  <files>backend/tests/cli/test_cli_coverage.py</files>
  <action>
First, analyze the CLI structure:

```bash
ls -la backend/cli/
head -50 backend/cli/main.py
```

Then create comprehensive test file for CLI coverage:

```python
"""Test coverage for CLI module - Target 60%+ coverage."""

import pytest
from click.testing import CliRunner
from unittest.mock import patch, MagicMock
from pathlib import Path

# Import CLI entry points
try:
    from cli.main import cli
    from cli.daemon import DaemonCLI
except ImportError:
    pytest.skip("CLI modules not available", allow_module_level=True)


@pytest.fixture
def runner():
    """Create Click CLI test runner."""
    return CliRunner()


@pytest.fixture
def mock_daemon():
    """Mock daemon process."""
    daemon = MagicMock()
    daemon.is_running.return_value = False
    daemon.pid = None
    return daemon


class TestCLIEntryPoint:
    """Test main CLI entry point."""

    def test_cli_help(self, runner):
        """Test CLI help command."""
        result = runner.invoke(cli, ['--help'])
        assert result.exit_code == 0
        assert 'Usage:' in result.output or 'Commands:' in result.output

    def test_cli_version(self, runner):
        """Test CLI version flag."""
        result = runner.invoke(cli, ['--version'])
        assert result.exit_code == 0
        # Version output should be present

    def test_cli_invalid_command(self, runner):
        """Test CLI with invalid command."""
        result = runner.invoke(cli, ['invalid-command-that-does-not-exist'])
        assert result.exit_code != 0


class TestDaemonCommands:
    """Test daemon management commands."""

    def test_daemon_start(self, runner, mock_daemon):
        """Test starting the daemon."""
        with patch('cli.daemon.DaemonController') as mock_controller:
            mock_controller.return_value = mock_daemon

            result = runner.invoke(cli, ['daemon', 'start'])
            assert result.exit_code == 0

    def test_daemon_stop(self, runner, mock_daemon):
        """Test stopping the daemon."""
        mock_daemon.is_running.return_value = True
        mock_daemon.pid = 12345

        with patch('cli.daemon.DaemonController') as mock_controller:
            mock_controller.return_value = mock_daemon

            result = runner.invoke(cli, ['daemon', 'stop'])
            assert result.exit_code == 0

    def test_daemon_status_running(self, runner, mock_daemon):
        """Test daemon status when running."""
        mock_daemon.is_running.return_value = True
        mock_daemon.uptime.return_value = "2 hours"

        with patch('cli.daemon.DaemonController') as mock_controller:
            mock_controller.return_value = mock_daemon

            result = runner.invoke(cli, ['daemon', 'status'])
            assert result.exit_code == 0
            assert 'running' in result.output.lower()

    def test_daemon_status_stopped(self, runner, mock_daemon):
        """Test daemon status when stopped."""
        mock_daemon.is_running.return_value = False

        with patch('cli.daemon.DaemonController') as mock_controller:
            mock_controller.return_value = mock_daemon

            result = runner.invoke(cli, ['daemon', 'status'])
            assert result.exit_code == 0
            assert 'stopped' in result.output.lower() or 'not running' in result.output.lower()

    def test_daemon_restart(self, runner, mock_daemon):
        """Test restarting the daemon."""
        mock_daemon.is_running.return_value = True

        with patch('cli.daemon.DaemonController') as mock_controller:
            mock_controller.return_value = mock_daemon

            result = runner.invoke(cli, ['daemon', 'restart'])
            assert result.exit_code == 0


class TestAgentCommands:
    """Test agent management commands."""

    def test_agent_list(self, runner):
        """Test listing agents."""
        with patch('cli.main.AgentRegistry') as mock_registry:
            mock_agents = [
                MagicMock(id="agent-1", name="Agent 1", status="active"),
                MagicMock(id="agent-2", name="Agent 2", status="active"),
            ]
            mock_registry.query.all.return_value = mock_agents

            result = runner.invoke(cli, ['agent', 'list'])
            assert result.exit_code == 0

    def test_agent_info(self, runner):
        """Test getting agent info."""
        with patch('cli.main.AgentRegistry') as mock_registry:
            mock_agent = MagicMock(
                id="agent-1",
                name="Test Agent",
                status="active",
                confidence_score=0.85
            )
            mock_registry.query.filter_by.first.return_value = mock_agent

            result = runner.invoke(cli, ['agent', 'info', 'agent-1'])
            assert result.exit_code == 0

    def test_agent_create(self, runner):
        """Test creating a new agent."""
        with patch('cli.main.AgentRegistry') as mock_registry:
            result = runner.invoke(cli, [
                'agent', 'create',
                '--name', 'Test Agent',
                '--category', 'Test',
                '--module', 'test.module'
            ])
            # Should succeed or have clear error message

    def test_agent_delete(self, runner):
        """Test deleting an agent."""
        with patch('cli.main.AgentRegistry') as mock_registry:
            mock_agent = MagicMock(id="agent-1")
            mock_registry.query.filter_by.first.return_value = mock_agent

            result = runner.invoke(cli, ['agent', 'delete', 'agent-1'])
            # Should prompt for confirmation or succeed with --force


class TestExecutionCommands:
    """Test agent execution commands."""

    def test_execute_prompt(self, runner):
        """Test executing a prompt with an agent."""
        with patch('cli.main.AtomAgent') as mock_agent_cls:
            mock_agent = MagicMock()
            mock_agent.execute.return_value = "Test response"
            mock_agent_cls.return_value = mock_agent

            result = runner.invoke(cli, [
                'execute', 'agent-1',
                '--prompt', 'Hello, world!'
            ])
            # Should execute or provide clear error

    def test_execute_with_file(self, runner, tmp_path):
        """Test executing prompt from file."""
        prompt_file = tmp_path / "prompt.txt"
        prompt_file.write_text("Test prompt from file")

        with patch('cli.main.AtomAgent') as mock_agent_cls:
            mock_agent = MagicMock()
            mock_agent.execute.return_value = "Response"
            mock_agent_cls.return_value = mock_agent

            result = runner.invoke(cli, [
                'execute', 'agent-1',
                '--file', str(prompt_file)
            ])
            # Should read file and execute


class TestConfigCommands:
    """Test configuration commands."""

    def test_config_get(self, runner):
        """Test getting configuration value."""
        with patch('cli.main.config') as mock_config:
            mock_config.get.return_value = "test-value"

            result = runner.invoke(cli, ['config', 'get', 'test.key'])
            assert result.exit_code == 0

    def test_config_set(self, runner):
        """Test setting configuration value."""
        with patch('cli.main.config') as mock_config:
            result = runner.invoke(cli, ['config', 'set', 'test.key', 'new-value'])
            # Should succeed or show validation error

    def test_config_list(self, runner):
        """Test listing all configuration."""
        with patch('cli.main.config') as mock_config:
            mock_config.items.return_value = {
                'key1': 'value1',
                'key2': 'value2',
            }

            result = runner.invoke(cli, ['config', 'list'])
            assert result.exit_code == 0


class TestErrorHandling:
    """Test CLI error handling."""

    def test_missing_required_argument(self, runner):
        """Test error when required argument is missing."""
        result = runner.invoke(cli, ['agent', 'create'])
        assert result.exit_code != 0

    def test_invalid_option_value(self, runner):
        """Test error with invalid option value."""
        result = runner.invoke(cli, ['agent', 'create', '--confidence', 'invalid'])
        assert result.exit_code != 0

    def test_database_connection_error(self, runner):
        """Test handling of database connection errors."""
        with patch('cli.main.Session') as mock_session:
            mock_session.side_effect = Exception("Database connection failed")

            result = runner.invoke(cli, ['agent', 'list'])
            # Should show user-friendly error message
```

Focus on testing CLI commands with Click test runner.
  </action>
  <verify>test_cli_coverage.py exists with 20+ tests</verify>
  <done>CLI commands tested with comprehensive coverage</done>
</task>

</tasks>

<verification>
1. test_cli_coverage.py created with 20+ tests
2. All tests pass (95%+ pass rate)
3. Coverage for CLI module increases from 16% to 60%+
4. All major CLI commands tested
5. Argument parsing validated
6. Error handling paths included
</verification>

<success_criteria>
- 20+ tests created for CLI module
- Coverage: 60%+ (430+ lines covered, up from 136)
- Pass rate: 95%+
- All command groups tested (daemon, agent, execute, config)
- Error handling covered
- Help and version commands tested
</success_criteria>

<output>
After completion, create `.planning/phases/201-coverage-push-85/201-06-SUMMARY.md` with:
1. Coverage achieved for CLI module (60%+ target)
2. Test count and pass rate
3. Commands covered
4. Any deviations
</output>
