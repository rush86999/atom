---
phase: 201-coverage-push-85
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/core/test_agent_governance_service_coverage.py
  - backend/tests/conftest.py
  - backend/pytest.ini
autonomous: true

must_haves:
  truths:
    - "Existing tests pass with 95%+ pass rate"
    - "Test collection succeeds with zero errors"
    - "Coverage can be measured accurately"
    - "Test infrastructure is stable across runs"
  artifacts:
    - path: "backend/tests/conftest.py"
      provides: "Shared pytest fixtures and configuration"
      contains: "fixture db_session, fixture client"
    - path: "backend/pytest.ini"
      provides: "Pytest configuration with zero collection errors"
      contains: "44 ignore patterns"
    - path: "backend/tests/core/test_agent_governance_service_coverage.py"
      provides: "Example of coverage-driven test patterns"
      contains: "test_* functions with coverage-focused naming"
  key_links:
    - from: "backend/tests/conftest.py"
      to: "backend/core/models.py"
      via: "Session fixture for database access"
      pattern: "@pytest.fixture.*def db_session"
    - from: "backend/tests"
      to: "backend/pytest.ini"
      via: "Test discovery configuration"
      pattern: "testpaths.*=.*tests"
---

<objective>
Establish test infrastructure quality by verifying existing test suite stability and identifying any failing tests that need fixes before coverage expansion.

This plan ensures the foundation is solid before adding new tests. Failing tests indicate unstable infrastructure that would block accurate coverage measurement.

Purpose: Verify test suite health and establish baseline for coverage expansion
Output: Test infrastructure assessment report with failing test categorization
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/200-fix-collection-errors/200-PHASE-SUMMARY.md
@.planning/phases/200-fix-collection-errors/200-05-SUMMARY.md
@.planning/phases/201-coverage-push-85/201-RESEARCH.md

# Key context from Phase 200
- 14,440 tests collecting with zero errors
- Coverage baseline: 20.11% (18,453/74,018 lines)
- pytest.ini configured with 44 ignore patterns
- Tests must be run from backend/ directory for correct results
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify test collection stability</name>
  <files>backend/pytest.ini</files>
  <action>
Run pytest collection 3 times from backend/ directory to verify stability:
```bash
cd /Users/rushiparikh/projects/atom/backend
python3 -m pytest --collect-only -q 2>&1 | tail -1
```

Expected: ~14,440 tests collected with zero errors across all 3 runs.

If collection count varies by more than 5 tests or errors occur:
- Check pytest.ini ignore patterns are still valid
- Verify no new files were added with import errors
- Document any deviations in plan summary

DO NOT modify ignore patterns - Phase 200 established the baseline.
  </action>
  <verify>grep "collected" test_collection_output.txt shows 14,440 ± 5 across 3 runs</verify>
  <done>Test collection stable at 14,440 tests, zero errors</done>
</task>

<task type="auto">
  <name>Task 2: Run existing test suite and assess pass rate</name>
  <files>backend/tests/</files>
  <action>
Run full test suite with timeout to identify failing tests:
```bash
cd /Users/rushiparikh/projects/atom/backend
python3 -m pytest --maxfail=50 --tb=line -q 2>&1 | tee test_run_output.txt
```

Categorize failures:
1. Import errors (Pydantic v2, SQLAlchemy 2.0)
2. Fixture errors (missing or broken fixtures)
3. Assertion failures (tests failing due to code changes)
4. Timeout errors (tests hanging or too slow)
5. Skip/xfail counts

Document in test_infrastructure_assessment.md:
- Total tests run
- Pass/fail/skip counts
- Top 5 failure categories with examples
- Estimated fix time for each category

Stop test run after 50 failures to save time - we need categories, not full list.
  </action>
  <verify>test_infrastructure_assessment.md exists with failure categorization</verify>
  <done>Test suite assessment complete with categorized failures</done>
</task>

<task type="auto">
  <name>Task 3: Measure current coverage baseline</name>
  <files>backend/coverage.json</files>
  <action>
Generate fresh coverage report to verify Phase 200 baseline:
```bash
cd /Users/rushiparikh/projects/atom/backend
python3 -m pytest --cov=backend --cov-branch --cov-report=json --cov-report=term-missing -q
```

Extract and document:
1. Overall coverage percentage
2. Module-level breakdown (tools, cli, core, api)
3. Top 10 uncovered files by missing line count
4. Files with 0% coverage (>100 lines)

Compare to Phase 200 baseline (20.11%) and document any deviations.
  </action>
  <verify>coverage.json exists with overall_percent_covered field</verify>
  <done>Coverage baseline verified at 20.11% ± 1%</done>
</task>

</tasks>

<verification>
1. Test collection stable across 3 consecutive runs (14,440 ± 5 tests)
2. Zero collection errors confirmed
3. Test infrastructure assessment document created with failure categorization
4. Coverage baseline measured and matches Phase 200 (20.11% ± 1%)
5. pytest.ini unchanged from Phase 200 baseline
</verification>

<success_criteria>
- Test collection count documented: 14,440 tests
- Collection errors: 0
- Pass rate documented (baseline for Wave 1 improvements)
- Coverage baseline: 20.11% verified
- Test infrastructure assessment report created
- Next wave (Wave 2) requirements documented based on assessment
</success_criteria>

<output>
After completion, create `.planning/phases/201-coverage-push-85/201-01-SUMMARY.md` with:
1. Test collection stability results (3 runs)
2. Test suite pass rate and failure categorization
3. Coverage baseline verification
4. Test infrastructure quality assessment
5. Recommendations for Wave 2 based on findings
</output>
