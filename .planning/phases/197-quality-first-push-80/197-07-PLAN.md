---
phase: 197-quality-first-push-80
plan: "07"
type: "execute"
wave: 7
depends_on: ["197-06"]
files_modified: ["tests/test_*.py"]
autonomous: true
must_haves:
  truths:
    - "Edge cases and error paths tested across all modules"
    - "Utility modules: 5-10% coverage improvement each"
    - "API modules: 10-15% coverage improvement each"
    - "Core service modules: 10-20% coverage improvement each"
    - "Integration modules: 10-15% coverage improvement each"
  artifacts:
    - path: "tests/test_edge_cases.py"
      provides: "Comprehensive edge case and error path test suite"
      min_lines: 500
      exports: ["test_boundary_conditions", "test_error_scenarios", "test_invalid_inputs"]
  key_links:
    - from: "tests/test_edge_cases.py"
      to: "overall coverage"
      via: "Coverage measurement"
      pattern: "pytest --cov=backend --cov-report=term-missing"
    - from: "tests/test_edge_cases.py"
      to: "78-79% coverage target"
      via: "Overall coverage verification"
      pattern: "--cov-fail-under=78"
---
<objective>
Add edge case and error path tests across the codebase to push towards 80% overall coverage.

Purpose: Target utility, API, core service, and integration modules with error scenarios and boundary conditions.
Output: Test suite with comprehensive edge case coverage achieving 78-79% overall coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/197-quality-first-push-80/197-06-SUMMARY.md
@/Users/rushiparikh/projects/atom/backend/core/
@/Users/rushiparikh/projects/atom/backend/api/
@/Users/rushiparikh/projects/atom/backend/tools/
</context>

<tasks>

<task type="auto">
  <name>Identify remaining coverage gaps</name>
  <files>tests/</files>
  <action>
    1. Run comprehensive coverage: `python -m pytest tests/ --cov=backend --cov-report=term-missing --cov-report=html > overall-cov.txt`
    2. Parse coverage report:
       ```bash
       grep "^backend" overall-cov.txt | sort -t% -k2 -n | head -20
       # Show 20 least-covered modules
       ```
    3. Categorize by type:
       - Utility modules (helpers, config, utils)
       - API modules (routes, endpoints)
       - Core service modules (business logic)
       - Integration modules (external APIs)
    4. Create: PLANS/197-07-coverage-gaps.md with prioritized list
    5. Target: Identify 50+ modules needing edge case coverage
  </action>
  <verify>
    ```bash
    cat PLANS/197-07-coverage-gaps.md
    grep "TOTAL" overall-cov.txt
    # Should show current overall coverage (aiming for 75% → 78-79%)
    ```
  </verify>
  <done>
    - overall-cov.txt shows baseline 75% coverage
    - PLANS/197-07-coverage-gaps.md lists 50+ modules by type
    - Prioritized by impact and complexity
  </done>
</task>

<task type="auto">
  <name>Target utility modules</name>
  <files>tests/test_edge_cases.py, backend/core/utils/</files>
  <action>
    Add coverage to helper/utility modules (5-10% each):
    1. Test common utilities:
       - String helpers (formatting, validation, sanitization)
       - Date/time utilities (parsing, formatting, timezone handling)
       - File helpers (path operations, file validation)
    2. Test configuration modules:
       - Environment variable loading
       - Config validation (missing required vars, invalid values)
       - Default values
    3. Test error scenarios:
       - Invalid inputs (null, empty, wrong type)
       - Boundary conditions (max length, min value)
       - Edge cases (unicode, special characters)
    4. Run: `python -m pytest tests/test_edge_cases.py -k "utility or helper" -v`
  </action>
  <verify>
    ```bash
    python -m pytest tests/test_edge_cases.py -k "util or helper" -v
    python -m pytest tests/test_edge_cases.py --cov=backend/core/utils --cov-report=term-missing
    # Should show 5-10% improvement
    ```
  </verify>
  <done>
    - Common utilities tested (string, date, file)
    - Configuration modules covered
    - Error scenarios verified
    - 5-10% coverage improvement per utility module
  </done>
</task>

<task type="auto">
  <name>Target API modules</name>
  <files>tests/test_edge_cases.py, backend/api/</files>
  <action>
    Add coverage to API route modules (10-15% each):
    1. Test all API endpoints:
       - GET endpoints (query params, pagination, filtering)
       - POST endpoints (request validation, body parsing)
       - PUT/PATCH endpoints (partial updates, validation)
       - DELETE endpoints (cascade deletes, soft deletes)
    2. Test authentication/authorization:
       - Missing auth tokens
       - Invalid credentials
       - Insufficient permissions
    3. Test error scenarios:
       - Invalid request formats
       - Missing required fields
       - Malformed JSON
       - Rate limiting
    4. Run: `python -m pytest tests/test_edge_cases.py -k "api or endpoint" -v`
  </action>
  <verify>
    ```bash
    python -m pytest tests/test_edge_cases.py -k "api or route" -v
    python -m pytest tests/test_edge_cases.py --cov=backend/api --cov-report=term-missing
    # Should show 10-15% improvement
    ```
  </verify>
  <done>
    - All API endpoints tested
    - Request/response cycles verified
    - Authentication/authorization covered
    - 10-15% coverage improvement per API module
  </done>
</task>

<task type="auto">
  <name>Target core service modules</name>
  <files>tests/test_edge_cases.py, backend/core/</files>
  <action>
    Add coverage to core service modules (10-20% each):
    1. Test business logic:
       - Service layer functions
       - Business rule validation
       - Data transformation
    2. Test service interactions:
       - Database operations (CRUD)
       - Cache interactions (get, set, invalidate)
       - External service calls (mocked)
    3. Test error handling:
       - Database errors (connection, constraint violation)
       - Cache failures (miss, timeout)
       - External service errors (timeout, 5xx)
    4. Run: `python -m pytest tests/test_edge_cases.py -k "service or core" -v`
  </action>
  <verify>
    ```bash
    python -m pytest tests/test_edge_cases.py -k "service or business" -v
    python -m pytest tests/test_edge_cases.py --cov=backend/core --cov-report=term-missing
    # Should show 10-20% improvement
    ```
  </verify>
  <done>
    - Business logic tested
    - Service interactions verified
    - Error handling covered
    - 10-20% coverage improvement per service module
  </done>
</task>

<task type="auto">
  <name>Target integration modules</name>
  <files>tests/test_edge_cases.py, backend/integrations/</files>
  <action>
    Add coverage to integration and third-party modules (10-15% each):
    1. Test external integrations:
       - LLM provider adapters (OpenAI, Anthropic, etc.)
       - Storage adapters (S3, R2)
       - Notification services (email, webhooks)
    2. Test adapter patterns:
       - Connection handling (connect, disconnect, retry)
       - Request/response mapping
       - Error translation (external → internal)
    3. Test error scenarios:
       - Connection failures
       - Timeout handling
       - Rate limiting
       - Invalid credentials
    4. Run: `python -m pytest tests/test_edge_cases.py -k "integration or adapter" -v`
  </action>
  <verify>
    ```bash
    python -m pytest tests/test_edge_cases.py -k "integration or external" -v
    python -m pytest tests/test_edge_cases.py --cov=backend/integrations --cov-report=term-missing
    # Should show 10-15% improvement
    ```
  </verify>
  <done>
    - External integrations tested
    - Adapter patterns covered
    - Connection handling verified
    - 10-15% coverage improvement per integration module
  </done>
</task>

<task type="auto">
  <name>Add edge case tests</name>
  <files>tests/test_edge_cases.py</files>
  <action>
    Test error scenarios and edge cases across modules:
    1. Test boundary conditions:
       - Empty inputs, null values
       - Max/min values (integers, strings, arrays)
       - Zero/negative values
    2. Test invalid inputs:
       - Wrong data types (string instead of int)
       - Malformed data (invalid JSON, broken UTF-8)
       - Injection attempts (SQL, XSS, path traversal)
    3. Test error propagation:
       - Error bubbles up correctly
       - Error messages are user-friendly
       - Stack traces not exposed in production
    4. Test concurrency issues:
       - Race conditions (simulated)
       - Deadlock scenarios
       - Resource exhaustion
    5. Run: `python -m pytest tests/test_edge_cases.py -k "edge or error" -v`
  </action>
  <verify>
    ```bash
    python -m pytest tests/test_edge_cases.py -k "edge or boundary" -v
    python -m pytest tests/test_edge_cases.py -k "error or invalid" -v
    # All edge case tests passing
    ```
  </verify>
  <done>
    - Error handling tested across all modules
    - Boundary conditions covered
    - Invalid input scenarios handled
    - Concurrency issues addressed
  </done>
</task>

<task type="auto">
  <name>Verify overall coverage</name>
  <files>tests/</files>
  <action>
    1. Run comprehensive coverage:
       ```bash
       python -m pytest tests/ --cov=backend --cov-report=term-missing --cov-report=html > final-cov.txt
       ```
    2. Check overall coverage:
       ```bash
       grep "TOTAL" final-cov.txt
       # Target: 78-79%
       ```
    3. Verify coverage threshold:
       ```bash
       python -m pytest tests/ --cov=backend --cov-fail-under=78
       # Should pass (exit code 0)
       ```
    4. Review high-impact modules:
       ```bash
       grep -E "(agent|workflow|ingestion|governance)" final-cov.txt
       # All >70% coverage
       ```
    5. Document: PLANS/197-07-results.md
  </action>
  <verify>
    ```bash
    python -m pytest tests/ --cov=backend --cov-fail-under=78
    # Exit code 0 = success
    ```
  </verify>
  <done>
    - Overall coverage reaches 78-79%
    - All high-impact modules covered
    - Edge cases tested comprehensively
    - Error handling verified across modules
    - Results documented for Plan 08
  </done>
</task>

</tasks>

<verification>
```bash
# Overall coverage report
python -m pytest tests/ --cov=backend --cov-report=term-missing --cov-report=html

# Coverage summary
python -m pytest tests/ --cov=backend --cov-report=summary

# Threshold check
python -m pytest tests/ --cov=backend --cov-fail-under=78
```
</verification>

<success_criteria>
- [ ] Overall coverage: 75% → 78-79%
- [ ] Utility modules: 5-10% improvement each
- [ ] API modules: 10-15% improvement each
- [ ] Core service modules: 10-20% improvement each
- [ ] Integration modules: 10-15% improvement each
- [ ] Edge cases tested across all modules
- [ ] Error handling comprehensive
- [ ] Results documented for Plan 08
</success_criteria>

<output>
After completion, create `.planning/phases/197-quality-first-push-80/197-07-SUMMARY.md`
</output>
