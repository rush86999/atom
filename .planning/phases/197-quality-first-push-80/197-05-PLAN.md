---
phase: 197-quality-first-push-80
plan: "05"
type: "execute"
wave: 5
depends_on: ["197-04"]
files_modified: ["tests/test_auto_document_ingestion.py", "backend/tools/auto_document_ingestion.py"]
autonomous: true
must_haves:
  truths:
    - "auto_document_ingestion coverage improved from 0% to 60%"
    - "Document upload and validation tested"
    - "File processing (PDF, text) covered"
    - "Content analysis and summarization verified"
  artifacts:
    - path: "tests/test_auto_document_ingestion.py"
      provides: "Comprehensive test suite for document ingestion"
      min_lines: 350
      exports: ["test_upload_endpoint", "test_pdf_parsing", "test_content_analysis"]
  key_links:
    - from: "tests/test_auto_document_ingestion.py"
      to: "backend/tools/auto_document_ingestion.py"
      via: "Coverage measurement"
      pattern: "pytest --cov.*auto_document_ingestion"
    - from: "tests/test_auto_document_ingestion.py"
      to: "60% coverage target"
      via: "Coverage threshold verification"
      pattern: "--cov-fail-under=60"
---
<objective>
Add test coverage to auto_document_ingestion module, targeting 0% → 60% coverage.

Purpose: Cover document upload, file processing, content analysis, and storage to establish baseline coverage.
Output: Test suite with 60% coverage of auto_document_ingestion functionality.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/197-quality-first-push-80/197-04-SUMMARY.md
@/Users/rushiparikh/projects/atom/backend/tools/auto_document_ingestion.py
</context>

<tasks>

<task type="auto">
  <name>Analyze ingestion system gaps</name>
  <files>backend/tools/auto_document_ingestion.py, tests/test_auto_document_ingestion.py</files>
  <action>
    1. Run baseline coverage: `python -m pytest tests/test_auto_document_ingestion.py --cov=backend/tools/auto_document_ingestion --cov-report=term-missing > baseline-cov.txt`
    2. List uncovered functions (upload, parsing, analysis, storage)
    3. Create: PLANS/197-05-ingestion-gaps.md
    4. Prioritize by importance (upload > parsing > storage > batch)
  </action>
  <verify>
    ```bash
    cat PLANS/197-05-ingestion-gaps.md
    grep "TOTAL" baseline-cov.txt
    ```
  </verify>
  <done>
    - baseline-cov.txt shows 0% coverage
    - PLANS/197-05-ingestion-gaps.md lists uncovered functions
  </done>
</task>

<task type="auto">
  <name>Add document upload tests</name>
  <files>tests/test_auto_document_ingestion.py</files>
  <action>
    Test document upload and validation:
    1. Test file upload endpoint: POST /api/v1/documents/upload
    2. Test document validation (size <10MB, type PDF/TXT, content not empty)
    3. Test invalid files (too large, wrong type, empty content)
    4. Test duplicate file handling
    5. Run: `python -m pytest tests/test_auto_document_ingestion.py -k upload -v`
  </action>
  <verify>
    ```bash
    python -m pytest tests/test_auto_document_ingestion.py::test_upload_success -v
    python -m pytest tests/test_auto_document_ingestion.py::test_upload_invalid_size -v
    ```
  </verify>
  <done>
    - File upload endpoint tested
    - Document validation verified (size, type, content)
    - Error handling for invalid documents
    - 15-25% coverage achieved
  </done>
</task>

<task type="auto">
  <name>Add file processing tests</name>
  <files>tests/test_auto_document_ingestion.py</files>
  <action>
    Test document parsing and extraction:
    1. Test PDF parsing (text extraction, metadata)
    2. Test text file processing
    3. Test metadata extraction (author, creation date, page count)
    4. Test corrupted file handling
    5. Run: `python -m pytest tests/test_auto_document_ingestion.py -k processing -v`
  </action>
  <verify>
    ```bash
    python -m pytest tests/test_auto_document_ingestion.py -k "pdf or parsing" -v
    ```
  </verify>
  <done>
    - PDF parsing tests implemented
    - Text extraction verified
    - Metadata extraction working
    - 25-40% coverage achieved
  </done>
</task>

<task type="auto">
  <name>Add content analysis tests</name>
  <files>tests/test_auto_document_ingestion.py</files>
  <action>
    Test content analysis and summarization:
    1. Test content summarization (LLM-based)
    2. Test key extraction (entities, topics)
    3. Test content categorization
    4. Test analysis failure handling
    5. Run: `python -m pytest tests/test_auto_document_ingestion.py -k analysis -v`
  </action>
  <verify>
    ```bash
    python -m pytest tests/test_auto_document_ingestion.py -k "summarize or analyze" -v
    ```
  </verify>
  <done>
    - Content summarization tested
    - Key extraction verified
    - Categorization working
    - 40-50% coverage achieved
  </done>
</task>

<task type="auto">
  <name>Add storage integration tests</name>
  <files>tests/test_auto_document_ingestion.py</files>
  <action>
    Test document storage and retrieval:
    1. Test document storage operations (save, update, delete)
    2. Test retrieval functionality (search by metadata, content)
    3. Test indexing and search
    4. Test storage failure handling
    5. Run: `python -m pytest tests/test_auto_document_ingestion.py -k storage -v`
  </action>
  <verify>
    ```bash
    python -m pytest tests/test_auto_document_ingestion.py -k "storage or retrieve" -v
    ```
  </verify>
  <done>
    - Storage operations tested
    - Retrieval verified
    - Search functionality working
    - 50-55% coverage achieved
  </done>
</task>

<task type="auto">
  <name>Add batch processing tests</name>
  <files>tests/test_auto_document_ingestion.py</files>
  <action>
    Test multiple document processing:
    1. Test batch upload (multiple files)
    2. Test concurrent processing limits
    3. Test batch progress tracking
    4. Test batch error handling (partial failures)
    5. Run: `python -m pytest tests/test_auto_document_ingestion.py -k batch -v`
  </action>
  <verify>
    ```bash
    python -m pytest tests/test_auto_document_ingestion.py -k "batch or concurrent" -v
    ```
  </verify>
  <done>
    - Batch upload tested
    - Concurrent processing verified
    - Error handling covered
    - 55-60% coverage achieved
  </done>
</task>

<task type="auto">
  <name>Verify coverage achievement</name>
  <files>tests/test_auto_document_ingestion.py, backend/tools/auto_document_ingestion.py</files>
  <action>
    1. Run: `python -m pytest tests/test_auto_document_ingestion.py --cov=backend/tools/auto_document_ingestion --cov-report=term-missing --cov-report=html > coverage-report.txt`
    2. Check: `grep "TOTAL" coverage-report.txt` (target: 60%+)
    3. Verify: `python -m pytest tests/test_auto_document_ingestion.py --cov=backend/tools/auto_document_ingestion --cov-fail-under=60`
    4. Document: PLANS/197-05-results.md
  </action>
  <verify>
    ```bash
    python -m pytest tests/test_auto_document_ingestion.py --cov=backend/tools/auto_document_ingestion --cov-fail-under=60
    ```
  </verify>
  <done>
    - Coverage reaches 60% target
    - All ingestion scenarios tested
    - No regression in document processing
    - Results documented for Plan 06
  </done>
</task>

</tasks>

<verification>
```bash
python -m pytest tests/test_auto_document_ingestion.py --cov=backend/tools/auto_document_ingestion --cov-report=term-missing
python -m pytest tests/test_auto_document_ingestion.py -v --tb=short
python -m pytest tests/test_auto_document_ingestion.py --cov=backend/tools/auto_document_ingestion --cov-fail-under=60
```
</verification>

<success_criteria>
- [ ] auto_document_ingestion coverage: 0% → 60%
- [ ] Document upload and validation tested
- [ ] File processing (PDF, text) covered
- [ ] Content analysis verified
- [ ] Storage integration covered
- [ ] Batch processing tested
- [ ] Results documented for Plan 06
</success_criteria>

<output>
After completion, create `.planning/phases/197-quality-first-push-80/197-05-SUMMARY.md`
</output>
