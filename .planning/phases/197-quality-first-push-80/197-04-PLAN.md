---
phase: 197-quality-first-push-80
plan: "04"
type: "execute"
wave: 4
depends_on: ["197-03"]
files_modified: ["tests/test_atom_agent_endpoints.py", "backend/core/atom_agent_endpoints.py"]
autonomous: true
must_haves:
  truths:
    - "atom_agent_endpoints coverage improved from 0% to 60%"
    - "All main endpoints tested (chat, streaming, agent execution)"
    - "Governance integration verified"
    - "Streaming functionality covered"
  artifacts:
    - path: "tests/test_atom_agent_endpoints.py"
      provides: "Comprehensive test suite for agent endpoints"
      min_lines: 400
      exports: ["test_chat_endpoint", "test_streaming_endpoint", "test_governance_checks"]
  key_links:
    - from: "tests/test_atom_agent_endpoints.py"
      to: "backend/core/atom_agent_endpoints.py"
      via: "Coverage measurement"
      pattern: "pytest --cov.*atom_agent_endpoints"
    - from: "tests/test_atom_agent_endpoints.py"
      to: "60% coverage target"
      via: "Coverage threshold verification"
      pattern: "--cov-fail-under=60"
---
<objective>
Add comprehensive test coverage to atom_agent_endpoints module, targeting 0% → 60% coverage.

Purpose: Cover main chat/streaming endpoints, governance integration, and error handling to establish baseline coverage.
Output: Test suite with 60% coverage of atom_agent_endpoints functionality.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/197-quality-first-push-80/197-03-SUMMARY.md
@/Users/rushiparikh/projects/atom/backend/core/atom_agent_endpoints.py
</context>

<tasks>

<task type="auto">
  <name>Analyze current coverage gaps</name>
  <files>backend/core/atom_agent_endpoints.py, tests/test_atom_agent_endpoints.py</files>
  <action>
    Identify uncovered endpoints and functionality in atom_agent_endpoints:
    1. Run baseline coverage: `python -m pytest tests/test_atom_agent_endpoints.py --cov=backend/core/atom_agent_endpoints --cov-report=term-missing > baseline-cov.txt`
    2. Parse coverage report:
       ```bash
       grep -A 20 "atom_agent_endpoints" baseline-cov.txt | grep "^"
       ```
    3. List uncovered functions and lines
    4. Identify gaps:
       - POST /api/v1/chat endpoint
       - WebSocket streaming endpoint
       - Agent resolution logic
       - Governance checks
       - Error handling paths
    5. Create: PLANS/197-04-coverage-gaps.md

    Target: List all uncovered code paths in atom_agent_endpoints
  </action>
  <verify>
    ```bash
    # Verify analysis
    cat PLANS/197-04-coverage-gaps.md
    grep "TOTAL" baseline-cov.txt
    # Should show current coverage percentage
    ```
  </verify>
  <done>
    - baseline-cov.txt shows current 0% coverage
    - PLANS/197-04-coverage-gaps.md lists all uncovered functions
    - Prioritized test list by importance
  </done>
</task>

<task type="auto">
  <name>Add basic endpoint tests</name>
  <files>tests/test_atom_agent_endpoints.py</files>
  <action>
    Create tests for main chat and streaming endpoints:
    1. Test POST /api/v1/chat:
       ```python
       def test_chat_endpoint_success(client, mock_llm):
           response = client.post("/api/v1/chat", json={
               "agent_id": "test-agent",
               "message": "Hello"
           })
           assert response.status_code == 200
           assert "response" in response.json()
       ```
    2. Test streaming endpoint:
       ```python
       def test_streaming_endpoint(client, mock_llm):
           response = client.post("/api/v1/chat/stream", json={
               "agent_id": "test-agent",
               "message": "Stream test"
           })
           assert response.status_code == 200
           # Verify streaming response format
       ```
    3. Test request validation:
       ```python
       def test_chat_missing_fields(client):
           response = client.post("/api/v1/chat", json={})
           assert response.status_code == 422
       ```
    4. Test agent resolution:
       ```python
       def test_chat_agent_not_found(client):
           response = client.post("/api/v1/chat", json={
               "agent_id": "nonexistent",
               "message": "Test"
           })
           assert response.status_code == 404
       ```
    5. Run tests: `python -m pytest tests/test_atom_agent_endpoints.py -v`

    Reference: backend/core/atom_agent_endpoints.py for endpoint signatures
  </action>
  <verify>
    ```bash
    # Verify endpoint tests
    python -m pytest tests/test_atom_agent_endpoints.py::test_chat_endpoint_success -v
    python -m pytest tests/test_atom_agent_endpoints.py::test_streaming_endpoint -v
    # All new tests passing
    ```
  </verify>
  <done>
    - POST /api/v1/chat endpoint tested
    - Streaming endpoint tests implemented
    - Request validation tests added
    - Agent resolution failures tested
    - 20-30% coverage achieved
  </done>
</task>

<task type="auto">
  <name>Add governance integration tests</name>
  <files>tests/test_atom_agent_endpoints.py</files>
  <action>
    Test agent governance integration with endpoint execution:
    1. Test governance checks for maturity levels:
       ```python
       def test_governance_student_blocked(client, mock_llm):
           response = client.post("/api/v1/chat", json={
               "agent_id": "student-agent",
               "message": "Execute action"
           })
           assert response.status_code == 403  # STUDENT blocked
       ```

    2. Test governance cache lookup:
       ```python
       def test_governance_cache_hit(client, mock_governance_cache):
           mock_governance_cache.get_agent_maturity.return_value = "AUTONOMOUS"
           response = client.post("/api/v1/chat", json={
               "agent_id": "autonomous-agent",
               "message": "Execute"
           })
           assert response.status_code == 200
       ```

    3. Test permission-based routing:
       ```python
       def test_governance_permission_denied(client, mock_llm):
           response = client.post("/api/v1/chat", json={
               "agent_id": "intern-agent",
               "message": "Delete data"
           })
           assert response.status_code == 403  # Permission denied
       ```

    4. Test governance bypass (emergency):
       ```python
       def test_governance_emergency_bypass(client, mock_llm):
           with patch.dict(os.environ, {"EMERGENCY_GOVERNANCE_BYPASS": "true"}):
               response = client.post("/api/v1/chat", json={
                   "agent_id": "any-agent",
                   "message": "Any action"
               })
               assert response.status_code == 200
       ```

    5. Run tests: `python -m pytest tests/test_atom_agent_endpoints.py -k governance -v`

    Reference: backend/core/agent_governance_service.py for governance patterns
  </action>
  <verify>
    ```bash
    # Verify governance tests
    python -m pytest tests/test_atom_agent_endpoints.py -k "governance" -v
    # Should pass all governance tests
    ```
  </verify>
  <done>
    - Governance checks tested for all maturity levels
    - Authorization tests for different agent types
    - Permission-based routing tested
    - Emergency bypass scenario covered
    - 30-45% coverage achieved
  </done>
</task>

<task type="auto">
  <name>Add error handling tests</name>
  <files>tests/test_atom_agent_endpoints.py</files>
  <action>
    Test error scenarios and edge cases:
    1. Test invalid request handling:
       ```python
       def test_chat_invalid_json(client):
           response = client.post("/api/v1/chat", data="invalid json")
           assert response.status_code == 422
       ```

    2. Test agent resolution failures:
       ```python
       def test_chat_agent_resolution_error(client, mock_agent_resolver):
           mock_agent_resolver.side_effect = Exception("Resolution failed")
           response = client.post("/api/v1/chat", json={
               "agent_id": "bad-agent",
               "message": "Test"
           })
           assert response.status_code == 500
           assert "error" in response.json()
       ```

    3. Test streaming error scenarios:
       ```python
       def test_streaming_interruption(client, mock_llm):
           mock_llm.stream_generate.side_effect = RuntimeError("Stream interrupted")
           response = client.post("/api/v1/chat/stream", json={
               "agent_id": "test-agent",
               "message": "Stream test"
           })
           # Verify graceful error handling
       ```

    4. Test LLM provider failures:
       ```python
       def test_chat_llm_timeout(client, mock_llm):
           mock_llm.generate.side_effect = TimeoutError("LLM timeout")
           response = client.post("/api/v1/chat", json={
               "agent_id": "test-agent",
               "message": "Test"
           })
           assert response.status_code == 503
       ```

    5. Run tests: `python -m pytest tests/test_atom_agent_endpoints.py -k "error" -v`

    Reference: backend/core/atom_agent_endpoints.py for error handling paths
  </action>
  <verify>
    ```bash
    # Verify error tests
    python -m pytest tests/test_atom_agent_endpoints.py -k "error" -v
    # Should pass all error handling tests
    ```
  </verify>
  <done>
    - Invalid request handling tested
    - Agent resolution failures covered
    - Streaming error scenarios tested
    - LLM provider failure handling verified
    - 45-55% coverage achieved
  </done>
</task>

<task type="auto">
  <name>Add streaming integration tests</name>
  <files>tests/test_atom_agent_endpoints.py</files>
  <action>
    Test real-time streaming functionality:
    1. Test WebSocket connection establishment:
       ```python
       def test_streaming_websocket_connect(client, mock_websocket):
           with client.websocket_connect("/api/v1/chat/stream") as websocket:
               assert websocket is not None
       ```

    2. Test token-by-token streaming:
       ```python
       def test_streaming_tokens(client, mock_llm):
           mock_llm.stream_generate.return_value = iter(["token1", "token2", "token3"])
           response = client.post("/api/v1/chat/stream", json={
               "agent_id": "test-agent",
               "message": "Stream test"
           })
           # Verify streaming response format
           assert response.status_code == 200
       ```

    3. Test stream interruption handling:
       ```python
       def test_streaming_client_disconnect(client, mock_llm):
           # Test graceful handling when client disconnects mid-stream
           pass
       ```

    4. Test stream timeout handling:
       ```python
       def test_streaming_timeout(client, mock_llm):
           mock_llm.stream_generate.side_effect = TimeoutError("Stream timeout")
           response = client.post("/api/v1/chat/stream", json={
               "agent_id": "test-agent",
               "message": "Stream test"
           })
           # Verify timeout handling
       ```

    5. Run tests: `python -m pytest tests/test_atom_agent_endpoints.py -k "stream" -v`

    Reference: backend/core/atom_agent_endpoints.py for streaming logic
  </action>
  <verify>
    ```bash
    # Verify streaming tests
    python -m pytest tests/test_atom_agent_endpoints.py -k "stream" -v
    # Should pass all streaming tests
    ```
  </verify>
  <done>
    - WebSocket connection tests passing
    - Token-by-token streaming verified
    - Stream interruption handling tested
    - Timeout scenarios covered
    - 55-60% coverage achieved
  </done>
</task>

<task type="auto">
  <name>Verify coverage improvement</name>
  <files>tests/test_atom_agent_endpoints.py, backend/core/atom_agent_endpoints.py</files>
  <action>
    Measure and verify 60% coverage target:
    1. Run full coverage report:
       ```bash
       python -m pytest tests/test_atom_agent_endpoints.py --cov=backend/core/atom_agent_endpoints --cov-report=term-missing --cov-report=html > coverage-report.txt
       ```
    2. Check coverage percentage:
       ```bash
       grep "TOTAL" coverage-report.txt
       # Target: 60%+
       ```
    3. Verify coverage threshold:
       ```bash
       python -m pytest tests/test_atom_agent_endpoints.py --cov=backend/core/atom_agent_endpoints --cov-fail-under=60
       # Should pass (exit code 0)
       ```
    4. Review uncovered lines:
       ```bash
       grep -A 20 "atom_agent_endpoints" coverage-report.txt | grep "^"
       # Identify remaining gaps for future work
       ```
    5. Document results: PLANS/197-04-results.md

    If coverage <60%, add targeted tests for remaining gaps
  </action>
  <verify>
    ```bash
    # Verify coverage threshold
    python -m pytest tests/test_atom_agent_endpoints.py --cov=backend/core/atom_agent_endpoints --cov-fail-under=60
    # Exit code 0 = success
    ```
  </verify>
  <done>
    - Coverage reaches 60% target
    - All new tests passing
    - No regression in existing functionality
    - Results documented for Plan 05
    - atom_agent_endpoints baseline established
  </done>
</task>

</tasks>

<verification>
```bash
# Coverage measurement
python -m pytest tests/test_atom_agent_endpoints.py --cov=backend/core/atom_agent_endpoints --cov-report=term-missing

# Run specific test file
python -m pytest tests/test_atom_agent_endpoints.py -v

# Coverage threshold check
python -m pytest tests/test_atom_agent_endpoints.py --cov=backend/core/atom_agent_endpoints --cov-fail-under=60
```
</verification>

<success_criteria>
- [ ] atom_agent_endpoints coverage: 0% → 60%
- [ ] All main endpoints tested
- [ ] Governance integration verified
- [ ] Streaming functionality covered
- [ ] Error handling tested
- [ ] No regressions introduced
- [ ] Results documented for Plan 05
</success_criteria>

<output>
After completion, create `.planning/phases/197-quality-first-push-80/197-04-SUMMARY.md`
</output>
