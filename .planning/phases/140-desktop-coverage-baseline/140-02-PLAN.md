---
phase: 140-desktop-coverage-baseline
plan: 02
type: execute
wave: 2
depends_on: ["140-01"]
files_modified:
  - frontend-nextjs/src-tauri/tests/platform_specific/mod.rs
  - frontend-nextjs/src-tauri/tests/platform_specific/conditional_compilation.rs
  - frontend-nextjs/src-tauri/tests/helpers/mod.rs
  - frontend-nextjs/src-tauri/tests/helpers/platform_helpers.rs
autonomous: true

must_haves:
  truths:
    - "Platform-specific tests organized by Windows/macOS/Linux directories"
    - "cfg! macro helper utilities available for platform detection tests"
    - "Conditional compilation tests verify platform-specific code paths"
    - "Platform helper utilities mirror mobile testing patterns from Phase 139"
    - "Tests compile on all platforms (platform-specific tests use #[cfg])"
  artifacts:
    - path: frontend-nextjs/src-tauri/tests/platform_specific/mod.rs
      provides: "Platform-specific test module organization"
      exports: ["windows", "macos", "linux", "cross_platform"]
    - path: frontend-nextjs/src-tauri/tests/helpers/platform_helpers.rs
      provides: "Platform detection and switching utilities (mirrors Phase 139 testUtils.ts)"
      exports: ["get_current_platform", "is_platform", "cfg_assert"]
    - path: frontend-nextjs/src-tauri/tests/platform_specific/conditional_compilation.rs
      provides: "Tests for cfg! macro and conditional compilation patterns"
      exports: ["test_cfg_macro", "test_platform_detection_logic"]
  key_links:
    - from: frontend-nextjs/src-tauri/tests/platform_specific/mod.rs
      to: frontend-nextjs/src-tauri/tests/cross_platform_validation_test.rs
      via: "mod cross_platform;"
      pattern: "pub use cross_platform::"
    - from: frontend-nextjs/src-tauri/tests/helpers/platform_helpers.rs
      to: mobile/src/__tests__/helpers/testUtils.ts (Phase 139)
      via: "Pattern mirroring (mockPlatform/restorePlatform)"
      pattern: "get_current_platform|is_platform"
---

<objective>
Organize platform-specific desktop tests and create platform helper utilities mirroring Phase 139 mobile patterns.

**Purpose:** Establish test infrastructure for Windows/macOS/Linux platform-specific testing with proper conditional compilation and helper utilities.

**Output:** Platform-specific test directory structure with helper utilities and conditional compilation tests.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-desktop-coverage-baseline/140-RESEARCH.md
@.planning/phases/139-mobile-platform-specific-testing/139-05-SUMMARY.md
@frontend-nextjs/src-tauri/tests/cross_platform_validation_test.rs
@frontend-nextjs/src-tauri/tests/device_capabilities_test.rs
@frontend-nextjs/src-tauri/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Create platform-specific test module structure</name>
  <files>frontend-nextjs/src-tauri/tests/platform_specific/mod.rs</files>
  <action>
    Create frontend-nextjs/src-tauri/tests/platform_specific/mod.rs with:
    - Module-level documentation explaining platform-specific organization
    - Platform-specific modules using #[cfg(target_os = "...")] attributes:
      - #[cfg(target_os = "windows")] pub mod windows;
      - #[cfg(target_os = "macos")] pub mod macos;
      - #[cfg(target_os = "linux")] pub mod linux;
      - pub mod cross_platform; // runs on all platforms
      - pub mod conditional_compilation; // cfg! macro testing
    - Re-exports for common types: pub use cross_platform::*;
    - Module declarations must NOT use cfg! for module visibility (use #[cfg] on module declarations themselves)
    - Pattern reference: Phase 139 mobile/tests/platform-specific/infrastructure.test.tsx

    Note: Do NOT create windows.rs, macos.rs, linux.rs files yet (those are for Phase 141). This plan only establishes the module structure.
  </action>
  <verify>
    grep -q "#\[cfg(target_os = \"windows\")\]" frontend-nextjs/src-tauri/tests/platform_specific/mod.rs
    grep -q "#\[cfg(target_os = \"macos\")\]" frontend-nextjs/src-tauri/tests/platform_specific/mod.rs
    grep -q "#\[cfg(target_os = \"linux\")\]" frontend-nextjs/src-tauri/tests/platform_specific/mod.rs
    grep -q "pub mod cross_platform" frontend-nextjs/src-tauri/tests/platform_specific/mod.rs
    grep -q "pub mod conditional_compilation" frontend-nextjs/src-tauri/tests/platform_specific/mod.rs
  </verify>
  <done>
    platform_specific/mod.rs exists with cfg-gated platform modules and cross-platform modules
  </done>
</task>

<task type="auto">
  <name>Create conditional compilation tests</name>
  <files>frontend-nextjs/src-tauri/tests/platform_specific/conditional_compilation.rs</files>
  <action>
    Create frontend-nextjs/src-tauri/tests/platform_specific/conditional_compilation.rs with:
    - Tests for cfg! macro runtime detection (from 140-RESEARCH.md lines 136-169):
      - test_cfg_macro_platform_detection() - verifies cfg!(target_os) returns expected values
      - test_cfg_macro_architecture_detection() - verifies cfg!(target_arch) for x86_64/aarch64
      - test_cfg_macro_endian_detection() - verifies cfg!(target_endian)
    - Tests for conditional compilation patterns:
      - test_cfg_any_combinations() - tests #[cfg(any(target_os = "windows", target_os = "macos"))]
      - test_cfg_all_combinations() - tests #[cfg(all(target_os = "linux", target_arch = "x86_64"))]
      - test_cfg_not_operator() - tests #[cfg(not(target_os = "windows"))]
    - Tests mirroring cross_platform_validation_test.rs lines 16-56:
      - test_platform_detection_logic() - platform string extraction using cfg!
      - test_path_separator_consistency() - PathBuf handles both separators
    - Use assert_eq!, assert!, matches! macros for assertions
    - All tests use #[test] attribute (no #[ignore] in this phase)
  </action>
  <verify>
    grep -q "fn test_cfg_macro_platform_detection" frontend-nextjs/src-tauri/tests/platform_specific/conditional_compilation.rs
    grep -q "fn test_cfg_macro_architecture_detection" frontend-nextjs/src-tauri/tests/platform_specific/conditional_compilation.rs
    grep -q "fn test_platform_detection_logic" frontend-nextjs/src-tauri/tests/platform_specific/conditional_compilation.rs
    grep -q "cfg!(target_os" frontend-nextjs/src-tauri/tests/platform_specific/conditional_compilation.rs
    cargo test --manifest-path=frontend-nextjs/src-tauri/Cargo.toml conditional_compilation 2>&1 | head -30
  </verify>
  <done>
    conditional_compilation.rs exists with cfg! macro and conditional compilation tests
  </done>
</task>

<task type="auto">
  <name>Create platform helper utilities</name>
  <files>frontend-nextjs/src-tauri/tests/helpers/platform_helpers.rs</files>
  <action>
    Create frontend-nextjs/src-tauri/tests/helpers/mod.rs (if not exists) and platform_helpers.rs with:
    - Module declaration: pub mod platform_helpers; in helpers/mod.rs
    - In platform_helpers.rs, create helper functions mirroring Phase 139 testUtils.ts patterns:
      - get_current_platform() -> &'static str - returns "windows", "macos", "linux", or "unknown" using cfg!
      - is_platform(expected: &str) -> bool - compares current platform to expected string
      - cfg_assert(platform: &str) - assertion macro-like function for platform detection
      - get_temp_dir() -> PathBuf - returns platform-specific temp directory
      - get_platform_separator() -> &'static str - returns "\\" on Windows, "/" on Unix
    - Documentation comments explaining each helper's purpose
    - #[cfg(test)] mod tests with helper validation:
      - test_get_current_platform_returns_valid_value()
      - test_is_platform_matches_current_platform()
      - test_get_platform_separator_is_consistent()
    - Pattern reference: mobile/src/__tests__/helpers/testUtils.ts (mockPlatform/restorePlatform, getiOSInsets, getAndroidInsets)

    Note: Desktop doesn't need "mock" platform switching (unlike mobile with Platform.OS mocking). Desktop helpers are for runtime detection and assertion convenience.
  </action>
  <verify>
    grep -q "pub fn get_current_platform" frontend-nextjs/src-tauri/tests/helpers/platform_helpers.rs
    grep -q "pub fn is_platform" frontend-nextjs/src-tauri/tests/helpers/platform_helpers.rs
    grep -q "pub fn cfg_assert" frontend-nextjs/src-tauri/tests/helpers/platform_helpers.rs
    grep -q "pub fn get_temp_dir" frontend-nextjs/src-tauri/tests/helpers/platform_helpers.rs
    test -f frontend-nextjs/src-tauri/tests/helpers/mod.rs
    cargo test --manifest-path=frontend-nextjs/src-tauri/Cargo.toml helpers 2>&1 | head -20
  </verify>
  <done>
    helpers/platform_helpers.rs exists with platform detection utilities and tests
  </done>
</task>

</tasks>

<verification>
1. Run cargo test --manifest-path=frontend-nextjs/src-tauri/Cargo.toml platform_specific
2. Verify platform-specific module compiles on current platform (other platforms' modules are excluded by #[cfg])
3. Verify conditional_compilation tests pass (cfg! macro tests)
4. Verify platform_helpers tests pass (all helper functions work correctly)
5. Run cargo test --all and verify no module resolution errors
</verification>

<success_criteria>
1. Platform-specific module structure created with Windows/macOS/Linux/cross-platform organization
2. Conditional compilation tests verify cfg! macro and #[cfg] attribute patterns
3. Platform helper utilities available for all test modules
4. Tests compile on all platforms (platform-specific modules use #[cfg] attributes)
5. Pattern mirrors Phase 139 mobile infrastructure (helpers, platform isolation)
</success_criteria>

<output>
After completion, create `.planning/phases/140-desktop-coverage-baseline/140-02-SUMMARY.md` with:
- Platform-specific module structure created
- Helper utilities established (mirroring Phase 139 patterns)
- Conditional compilation tests passing
- Test file organization documented
- Next steps for Plan 03 (documentation and CI/CD)
</output>
