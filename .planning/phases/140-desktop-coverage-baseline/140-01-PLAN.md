---
phase: 140-desktop-coverage-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/src-tauri/coverage.sh
  - frontend-nextjs/src-tauri/tarpaulin.toml
  - frontend-nextjs/src-tauri/tests/coverage/mod.rs
autonomous: true

must_haves:
  truths:
    - "cargo-tarpaulin generates HTML coverage reports for Tauri app"
    - "Baseline coverage percentage is measured and documented"
    - "Coverage report identifies uncovered lines in main.rs"
    - "Coverage script works on x86_64 architecture"
    - "Test files are excluded from coverage calculations"
  artifacts:
    - path: frontend-nextjs/src-tauri/tarpaulin.toml
      provides: "Tarpaulin configuration with HTML output and test exclusions"
      contains: "[exclude-files]", "[report]", "out = [\"Html\"]"
    - path: frontend-nextjs/src-tauri/tests/coverage/mod.rs
      provides: "Coverage module for report aggregation and baseline tracking"
      exports: ["generate_baseline", "parse_coverage_report"]
    - path: frontend-nextjs/src-tauri/coverage/baseline.json
      provides: "Baseline coverage data with timestamp and percentage"
      contains: "baseline_coverage", "timestamp", "measured_at"
  key_links:
    - from: frontend-nextjs/src-tauri/coverage.sh
      to: cargo-tarpaulin CLI
      via: "./coverage.sh --html"
      pattern: "cargo tarpaulin.*--out Html"
---

<objective>
Establish Tauri/Rust coverage baseline measurement infrastructure using cargo-tarpaulin with HTML reporting, test file exclusions, and baseline tracking.

**Purpose:** Enable accurate coverage measurement for desktop (Tauri/Rust) code as foundation for Phase 141 expansion to 80% target.

**Output:** Functional coverage infrastructure with baseline report showing current coverage percentage and identified gaps.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-desktop-coverage-baseline/140-RESEARCH.md
@frontend-nextjs/src-tauri/coverage.sh
@frontend-nextjs/src-tauri/Cargo.toml
@frontend-nextjs/src-tauri/src/main.rs
@.github/workflows/desktop-tests.yml
</context>

<tasks>

<task type="auto">
  <name>Create tarpaulin.toml configuration file</name>
  <files>frontend-nextjs/src-tauri/tarpaulin.toml</files>
  <action>
    Create tarpaulin.toml configuration file at frontend-nextjs/src-tauri/tarpaulin.toml with:
    - [exclude-files] section with patterns = ["tests/*", "*/tests/*"]
    - [report] section with out = ["Html", "Json"] and output-dir = "coverage-report"
    - [features] section with coverage_threshold = 80 (for future Phase 142 enforcement, informational only in Phase 140)
    - [engine] section with default = "ptrace" for compatibility across platforms

    Do NOT set fail-under in this phase (Phase 140 is baseline measurement, not enforcement)

    Reference: https://github.com/simonvandel/tarpaulin for configuration syntax
  </action>
  <verify>
    grep -q "\[exclude-files\]" frontend-nextjs/src-tauri/tarpaulin.toml
    grep -q "out = \[\"Html\"" frontend-nextjs/src-tauri/tarpaulin.toml
    grep -q "patterns = \[\"tests/\*\"" frontend-nextjs/src-tauri/tarpaulin.toml
  </verify>
  <done>
    tarpaulin.toml exists with HTML/JSON output configuration and test file exclusions
  </done>
</task>

<task type="auto">
  <name>Update coverage.sh script for HTML output and architecture detection</name>
  <files>frontend-nextjs/src-tauri/coverage.sh</files>
  <action>
    Update frontend-nextjs/src-tauri/coverage.sh to:
    - Change default OUTPUT_FORMAT from "Json" to "Html" (line 31)
    - Change default OUTPUT_DIR from "coverage" to "coverage-report" (line 32)
    - Update tarpaulin command to read from tarpaulin.toml by default (remove redundant --out and --output-dir flags, use --config tarpaulin.toml)
    - Add --fail-under 0 flag to baseline command (Phase 140 measures, doesn't enforce)
    - Preserve ARM architecture detection (lines 22-28) - this is working correctly
    - Add --verbose flag for detailed output during baseline generation
    - Update help text to mention tarpaulin.toml configuration
  </action>
  <verify>
    grep -q "Html" frontend-nextjs/src-tauri/coverage.sh | grep -q "OUTPUT_FORMAT"
    grep -q "coverage-report" frontend-nextjs/src-tauri/coverage.sh | grep -q "OUTPUT_DIR"
    grep -q "tarpaulin.toml" frontend-nextjs/src-tauri/coverage.sh
  </verify>
  <done>
    coverage.sh generates HTML reports in coverage-report directory with ARM detection preserved
  </done>
</task>

<task type="auto">
  <name>Create coverage module for baseline tracking</name>
  <files>frontend-nextjs/src-tauri/tests/coverage/mod.rs</files>
  <action>
    Create frontend-nextjs/src-tauri/tests/coverage/mod.rs with:
    - Module-level documentation explaining baseline tracking purpose
    - parse_coverage_report() function to read coverage-report/index.html (or coverage.json) and extract percentage
    - generate_baseline() function that writes baseline.json with fields: baseline_coverage (f64), measured_at (ISO8601), total_lines (usize), covered_lines (usize), platform (String), arch (String)
    - get_baseline_path() helper returning PathBuf to coverage/baseline.json
    - load_baseline() function reading baseline.json and returning CoverageBaseline struct
    - #[cfg(test)] mod tests with basic parsing tests

    Use serde_json for JSON serialization. Handle missing report gracefully (return 0.0 baseline).
  </action>
  <verify>
    grep -q "pub fn parse_coverage_report" frontend-nextjs/src-tauri/tests/coverage/mod.rs
    grep -q "pub fn generate_baseline" frontend-nextjs/src-tauri/tests/coverage/mod.rs
    grep -q "CoverageBaseline" frontend-nextjs/src-tauri/tests/coverage/mod.rs
    cargo test --manifest-path=frontend-nextjs/src-tauri/Cargo.toml --test coverage 2>&1 | head -20
  </verify>
  <done>
    coverage/mod.rs exists with baseline parsing and generation functions
  </done>
</task>

</tasks>

<verification>
1. Run ./coverage.sh --html and verify coverage-report/index.html is generated
2. Verify coverage-report directory contains index.html with line-by-line coverage data
3. Verify tests/ directory is excluded from coverage calculations
4. Run cargo test on tests/coverage/mod.rs to verify module compiles
5. Check baseline.json is created with valid coverage percentage
</verification>

<success_criteria>
1. Tarpaulin configured with HTML output in tarpaulin.toml
2. Coverage script generates HTML reports in coverage-report directory
3. Baseline coverage percentage measured and documented in baseline.json
4. Coverage gaps identified (which lines in main.rs are uncovered)
5. Test files excluded from coverage calculations (verified in report)
</success_criteria>

<output>
After completion, create `.planning/phases/140-desktop-coverage-baseline/140-01-SUMMARY.md` with:
- Baseline coverage percentage measured
- Coverage gap summary (X lines covered / Y total lines in main.rs)
- Files created (tarpaulin.toml, tests/coverage/mod.rs, baseline.json)
- Next steps for Plan 02 (platform-specific test organization)
</output>
