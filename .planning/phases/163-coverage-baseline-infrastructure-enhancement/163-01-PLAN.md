---
phase: 163-coverage-baseline-infrastructure-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pytest.ini
  - backend/tests/scripts/generate_baseline_coverage_report.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "pytest runs with --cov-branch flag enabled for all coverage measurements"
    - "coverage.json output includes both percent_covered and percent_covered_branch fields"
    - "Baseline measurement includes entire backend codebase (core/, api/, tools/ directories)"
    - "Coverage reports distinguish between line coverage and branch coverage clearly"
  artifacts:
    - path: "backend/pytest.ini"
      provides: "pytest configuration with branch coverage enabled"
      contains: "branch = true"
    - path: "backend/tests/coverage_reports/metrics/backend_163_baseline.json"
      provides: "Comprehensive baseline coverage report with line and branch metrics"
      exports: ["percent_covered", "percent_covered_branch"]
    - path: "backend/tests/scripts/generate_baseline_coverage_report.py"
      provides: "Standardized baseline generation script"
      min_lines: 50
  key_links:
    - from: "backend/pytest.ini"
      to: "coverage.py branch coverage measurement"
      via: "[coverage:run] branch = true configuration"
      pattern: "branch\\s*=\\s*true"
    - from: "backend/tests/scripts/generate_baseline_coverage_report.py"
      to: "pytest --cov-branch"
      via: "subprocess call to pytest with branch coverage flag"
      pattern: "--cov-branch"
---

<objective>
Establish accurate actual line coverage baseline with branch coverage enabled. This plan ensures that coverage measurements use the --cov-branch flag consistently across all testing, and generates a comprehensive baseline that measures the entire backend codebase (not service-level estimates).

Purpose: Fix the methodology gap from Phase 160-162 where service-level estimates (74.6%) masked true actual line coverage (8.50%). By ensuring --cov-branch is consistently used and measuring the full backend codebase, we prevent false confidence and establish an accurate baseline for the 80% target journey.

Output: Updated pytest.ini with explicit --cov-branch flag, standardized baseline generation script, and backend_163_baseline.json with accurate line + branch coverage metrics for entire backend.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/pytest.ini
@backend/tests/scripts/generate_baseline_coverage_report.py
@backend/tests/docs/COVERAGE_GUIDE.md

# Key Context from Phase 160-162 Investigation
The root cause of the coverage gap was:
1. Service-level coverage estimates (74.6%) were used instead of actual line coverage (8.50%)
2. This created a 66 percentage point false confidence gap
3. Phase 161 measured actual backend coverage at 8.50% (5751/72622 lines)
4. pytest.ini already has `branch = true` in [coverage:run] section
5. Need to ensure --cov-branch flag is used in actual pytest invocations
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and enhance pytest configuration for branch coverage</name>
  <files>backend/pytest.ini</files>
  <action>
    1. Read backend/pytest.ini to verify current coverage configuration
    2. Ensure [coverage:run] section has `branch = true` (already present per research)
    3. Add explicit documentation comment explaining --cov-branch usage
    4. Verify addopts includes any necessary coverage flags
    5. Add reference to --cov-branch in the comments explaining why branch coverage matters

    DO NOT change the `branch = true` setting if already present - just add documentation.

    The key addition is a comment block in the [coverage:run] section that explains:
    - Branch coverage measures decision paths (if/else, try/except)
    - Line coverage alone can be misleading (100% lines, 50% branches)
    - Use `pytest --cov-branch` to enable branch coverage in CLI
  </action>
  <verify>
    grep -A 5 "\\[coverage:run\\]" /Users/rushiparikh/projects/atom/backend/pytest.ini | grep "branch = true"
    grep -i "branch.*coverage" /Users/rushiparikh/projects/atom/backend/pytest.ini | grep -v "^#"
  </verify>
  <done>
    pytest.ini has `branch = true` configuration with explanatory comments about branch coverage importance
  </done>
</task>

<task type="auto">
  <name>Task 2: Create standardized baseline generation script with branch coverage</name>
  <files>backend/tests/scripts/generate_baseline_coverage_report.py</files>
  <action>
    1. Read existing backend/tests/scripts/generate_baseline_coverage_report.py if it exists
    2. Create or update the script to:
       - Run pytest with --cov=core --cov=api --cov=tools --cov-branch flags
       - Generate coverage.json output with both line and branch metrics
       - Save to backend/tests/coverage_reports/metrics/backend_163_baseline.json
       - Include metadata: timestamp, command used, scope (full backend)
       - Print summary showing line coverage AND branch coverage separately
    3. Script should accept optional --output argument for custom output path
    4. Script should use subprocess to run pytest and capture exit code
    5. Include error handling for pytest failures

    The script MUST include:
    ```python
    cmd = [
        "pytest",
        "tests/",
        "--cov", "core",
        "--cov", "api",
        "--cov", "tools",
        "--cov-branch",  # CRITICAL: enables branch coverage
        "--cov-report", "json",
        "--cov-report", "term-missing",
        "-q"
    ]
    ```

    Output JSON MUST contain both:
    - percent_covered (line coverage)
    - percent_covered_branch (branch coverage)
  </action>
  <verify>
    python /Users/rushiparikh/projects/atom/backend/tests/scripts/generate_baseline_coverage_report.py
    jq -r '.meta.branch_coverage' /Users/rushiparikh/projects/atom/backend/tests/coverage_reports/metrics/backend_163_baseline.json
  </verify>
  <done>
    Script generates backend_163_baseline.json with line AND branch coverage metrics, metadata shows branch_coverage: true
  </done>
</task>

<task type="auto">
  <name>Task 3: Generate and verify comprehensive baseline with branch coverage</name>
  <files>backend/tests/coverage_reports/metrics/backend_163_baseline.json</files>
  <action>
    1. Run the baseline generation script from Task 2:
       ```bash
       cd /Users/rushiparikh/projects/atom/backend
       python tests/scripts/generate_baseline_coverage_report.py
       ```
    2. Verify the generated JSON contains:
       - totals.percent_covered (line coverage percentage)
       - totals.percent_covered_branch (branch coverage percentage)
       - meta.branch_coverage = true
       - meta.timestamp
       - files object with comprehensive backend file coverage
    3. Print a summary showing:
       - Total lines covered vs total statements
       - Total branches covered vs total branches
       - Overall line coverage percentage
       - Overall branch coverage percentage
       - Gap to 80% target for both metrics
    4. Save summary as backend_163_baseline_summary.txt alongside the JSON
    5. Compare with Phase 161 baseline (8.50%) to confirm methodology consistency

    Expected format:
    ```
    Backend Coverage Baseline - Phase 163
    ======================================
    Generated: [timestamp]
    Scope: core/, api/, tools/ (full backend)

    Line Coverage:
      Covered: X / Y lines
      Percentage: Z% (gap to 80%: [80-Z]%)

    Branch Coverage:
      Covered: A / B branches
      Percentage: C% (gap to 70% target: [70-C]%)

    Methodology: pytest --cov-branch (actual execution data, not estimates)
    ```
  </action>
  <verify>
    jq -e '.meta.branch_coverage == true' /Users/rushiparikh/projects/atom/backend/tests/coverage_reports/metrics/backend_163_baseline.json
    jq -e '.totals.percent_covered' /Users/rushiparikh/projects/atom/backend/tests/coverage_reports/metrics/backend_163_baseline.json
    jq -e '.totals.percent_covered_branch' /Users/rushiparikh/projects/atom/backend/tests/coverage_reports/metrics/backend_163_baseline.json
    ls /Users/rushiparikh/projects/atom/backend/tests/coverage_reports/metrics/backend_163_baseline_summary.txt
  </verify>
  <done>
    backend_163_baseline.json exists with line AND branch coverage, summary.txt shows gap to 80% target, methodology documented as actual execution (not estimates)
  </done>
</task>

</tasks>

<verification>
Overall Phase Verification:
1. Run `pytest tests/ --cov=core --cov=api --cov=tools --cov-branch --cov-report=term-missing -q` and verify branch coverage is shown in output
2. Check that coverage.json has both percent_covered and percent_covered_branch fields
3. Confirm backend_163_baseline.json measures entire backend (check files count > 500)
4. Verify summary clearly distinguishes line vs branch coverage
</verification>

<success_criteria>
1. pytest configuration explicitly documents --cov-branch usage
2. Baseline script generates coverage with both line AND branch metrics
3. backend_163_baseline.json shows comprehensive backend coverage (not single-file estimates)
4. Summary clearly shows gap to 80% line coverage and 70% branch coverage targets
5. Team can run `python tests/scripts/generate_baseline_coverage_report.py` to regenerate baseline anytime
</success_criteria>

<output>
After completion, create `.planning/phases/163-coverage-baseline-infrastructure-enhancement/163-01-SUMMARY.md` with:
- Actual line coverage percentage from baseline
- Branch coverage percentage from baseline
- Gap to 80% target (both line and branch)
- File count measured (confirming full backend scope)
- Methodology confirmation (actual execution vs estimates)
</output>
