---
phase: 214-fix-test-failures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_ab_testing_routes.py
autonomous: true

must_haves:
  truths:
    - "All 10 failing A/B tests now pass with 200 responses"
    - "Test routes match actual API routes without double prefix"
    - "No new code added to production API (test-only fix)"
  artifacts:
    - path: "backend/tests/api/test_ab_testing_routes.py"
      provides: "Fixed test file with correct router prefix"
      min_lines: 1300
      contains: "app.include_router(router)"
  key_links:
    - from: "tests/api/test_ab_testing_routes.py"
      to: "api/ab_testing.py"
      via: "Direct router import without duplicate prefix"
      pattern: "app.include_router\\(router\\)"
---

<objective>
Fix double prefix bug causing 10 A/B testing test failures.

Purpose: Tests were adding `/api/ab-tests` prefix when router already has it, causing routes like `/api/ab-tests/api/ab-tests/create` (404 errors).

Output: Single-line change in test file removing duplicate prefix parameter.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/214-fix-test-failures/214-RESEARCH.md
@.planning/phases/214-fix-test-failures/214-CONTEXT.md
@backend/api/ab_testing.py
@backend/tests/api/test_ab_testing_routes.py
</context>

<tasks>

<task type="auto">
  <name>Fix double prefix in A/B testing test file</name>
  <files>backend/tests/api/test_ab_testing_routes.py</files>
  <action>
    The router at api/ab_testing.py:30 already has `prefix="/api/ab-tests"`:
    ```python
    router = BaseAPIRouter(prefix="/api/ab-tests", tags=["A/B Testing"])
    ```

    The test file incorrectly adds the same prefix again in each fixture's app.include_router() call (lines 28, 271, 366, 509, 674, 813, 936, 1077, 1146, 1220).

    Change ALL instances from:
    ```python
    app.include_router(router, prefix="/api/ab-tests")
    ```

    To:
    ```python
    app.include_router(router)  # Router already has prefix
    ```

    This is a find-and-replace operation across the entire file. The router already defines the prefix, so adding it again creates double-nested routes.

    DO NOT modify api/ab_testing.py — the router definition is correct.
  </action>
  <verify>Run: pytest backend/tests/api/test_ab_testing_routes.py -v

    All 10 previously failing tests should now pass:
    - test_create_test_success
    - test_create_test_with_all_fields
    - test_create_test_default_values
    - test_create_test_prompt_type
    - test_create_test_agent_config_type
    - test_create_test_validation_error
    - test_create_test_traffic_percentage_validation
    - test_create_test_confidence_validation
    - test_start_test_success
    - test_start_test_includes_timestamp</verify>
  <done>Tests pass with 200 status codes. No 404 errors on POST /api/ab-tests/create or /api/ab-tests/{test_id}/start.</done>
</task>

</tasks>

<verification>
1. Run full test suite: pytest backend/tests/api/test_ab_testing_routes.py -v
2. Verify all 10 previously failing tests now pass
3. Confirm no 404 errors in test output
4. Run broader test suite to ensure no regressions: pytest backend/tests/api/ -k "ab" -v
</verification>

<success_criteria>
- [ ] All 10 A/B testing tests pass (0 failures)
- [ ] No 404 errors in test output
- [ ] Only test file modified (no production code changes)
- [ ] Full test suite passes with no new failures
</success_criteria>

<output>
After completion, create `.planning/phases/214-fix-test-failures/214-01-SUMMARY.md`
</output>
