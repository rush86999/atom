---
phase: 238-multi-agent-governance-hitl-hardening
plan: 01
type: execute
wave: 1
depends_on: [237]
autonomous: true

must_haves:
  truths:
    - "Fleet budget aggregation enforces aggregate spend limits across entire delegation chain"
    - "Recursion guardrails prevent infinite agent-recruiting loops with 5-level depth limit"
    - "HITL context propagation includes chain_id and blackboard snapshot for user awareness"
    - "Multi-agent governance checks aggregate spend before allowing sub-tasks"
    - "All governance and budget hardening synced to upstream repository"
  artifacts:
    - path: "backend-saas/core/models.py"
      provides: "Enhanced data models with chain-aware tracking"
      contains: "HITLAction.chain_id ForeignKey"
      contains: "HITLAction.context_snapshot JSONB"
      contains: "DelegationChain.total_spend_usd"
      contains: "DelegationChain.max_depth"
---

<objective>
Implement chain-aware guardrails for multi-agent "Fleet Admiralty" operations with aggregate budget limits and enriched HITL context propagation.

Purpose: Harden the Fleet Admiralty model by ensuring multi-agent operations are gated by aggregate budget limits and users have full context (including Shared Blackboard) during human-in-the-loop interventions, with upstream parity maintained.

Output: Fleet-aware governance, budget aggregation, recursion guardrails, and HITL context propagation fully implemented and tested.
</objective>

<tasks>

<task type="manual">
  <name>Task 1: Enhance data models for chain-aware tracking</name>
  <files>
    - backend-saas/core/models.py
  </files>
  <action>
    Add chain-level context tracking to HITL and delegation models:

    1. **Modify HITLAction model**:
       - Add `chain_id` column: ForeignKey to `DelegationChain` (nullable=True for backward compatibility)
       - Add `context_snapshot` column: JSONB field for storing blackboard state at HITL trigger time
       - Update `__repr__` to include chain information
       - Create Alembic migration for new columns

    2. **Modify DelegationChain model**:
       - Add `total_spend_usd` column: Float field with default=0.0 for aggregate fleet spend tracking
       - Add `max_depth` column: Integer field with default=5 for recursion limit policy
       - Add `budget_limit_usd` column: Float field (nullable) for fleet-specific budget cap
       - Update `__repr__` to include budget tracking info
       - Create Alembic migration for new columns

    3. **Database migration**:
       ```bash
       cd backend-saas
       alembic revision -m "Add chain-aware tracking to HITLAction and DelegationChain"
       # Add columns in upgrade()
       alembic upgrade head
       ```

  </action>
  <verify>
    Grep check: `grep -n "chain_id.*ForeignKey\|context_snapshot.*JSONB" backend-saas/core/models.py` (should return results)
    Grep check: `grep -n "total_spend_usd\|max_depth\|budget_limit_usd" backend-saas/core/models.py` (should return results in DelegationChain)
    Migration exists: `ls backend-saas/alembic/versions/*chain_aware*.py` (should exist)
  </verify>
  <done>
    Data models enhanced with chain_id tracking, context snapshots, and aggregate budget fields
  </done>
</task>

</tasks>

<verification>
1. Fleet budget aggregation enforces aggregate spend limits across entire delegation chain
2. Recursion guardrails prevent infinite agent-recruiting loops with 5-level depth limit
3. HITL context propagation includes chain_id and blackboard snapshot for user awareness
4. Multi-agent governance checks aggregate spend before allowing sub-tasks
5. All governance and budget hardening synced to upstream repository without SaaS patterns
</verification>

<success_criteria>
- Fleet budget aggregation blocks actions when aggregate chain spend exceeds fleet cap
- Recursion depth limit enforced (5 levels max) with clear error messaging
- HITL interventions include chain_id, agent role, delegation depth, and blackboard snapshot
- Meta-Agent can observe and retry rejected fleet actions
- All changes ported to upstream with tenant_id, billing, and quota patterns removed
</success_criteria>

<output>
After completion, create `.planning/phases/238-multi-agent-governance-hitl-hardening/238-01-SUMMARY.md`
</output>
