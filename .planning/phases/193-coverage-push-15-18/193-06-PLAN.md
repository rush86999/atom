---
phase: 193-coverage-push-15-18
plan: 06
type: execute
wave: 2
depends_on: [193-01]
files_modified:
  - backend/tests/core/llm/test_byok_handler_coverage_extend.py
  - backend/core/llm/byok_handler.py
autonomous: true

must_haves:
  truths:
    - "BYOKHandler coverage extends from 19.4% to 50%+ (targeting provider routing and streaming)"
    - "Tests cover multi-provider LLM routing and cognitive tier integration"
    - "Inline import limitations documented (accepted blocker from Phase 192)"
    - "Coverage report JSON is generated with BYOK handler metrics"
  artifacts:
    - path: "backend/tests/core/llm/test_byok_handler_coverage_extend.py"
      provides: "Extended coverage tests for BYOK handler"
      min_lines: 600
      contains: "TestBYOKHandlerCoverageExtend"
    - path: "backend/core/llm/byok_handler.py"
      provides: "Multi-provider LLM routing and streaming"
      unchanged: true
    - path: ".planning/phases/193-coverage-push-15-18/193-06-coverage.json"
      provides: "Coverage report for this plan"
  key_links:
    - from: "backend/tests/core/llm/test_byok_handler_coverage_extend.py"
      to: "backend/core/llm/byok_handler.py"
      via: "import BYOKHandler"
      pattern: "from core.llm.byok_handler import"
---

<objective>
Extend BYOKHandler coverage from 19.4% to 50%+ by focusing on provider routing, streaming, and cognitive tier integration. Building on Phase 192's findings about inline import blockers, target testable methods while working around mock limitations.

Purpose: Improve coverage on the critical LLM routing layer (654 statements)
Output: 25-35 additional tests extending coverage on provider routing and error handling
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-coverage-push-15-18/193-RESEARCH.md
@.planning/codebase/TESTING.md

# Prior work on BYOKHandler
@.planning/phases/192-coverage-push-22-28/192-04-SUMMARY.md
@backend/tests/core/llm/test_byok_handler_coverage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BYOKHandler coverage tests</name>
  <files>backend/tests/core/llm/test_byok_handler_coverage_extend.py</files>
  <action>
Create `backend/tests/core/llm/test_byok_handler_coverage_extend.py` with:

1. **Test class structure (extending Phase 192 work):**
   ```python
   """
   Coverage-driven tests for byok_handler.py (19.4% -> 50%+ target)

   Building on Phase 192's 41% baseline (Phase 192 Plan 04 had blockers).
   Working around inline import limitations that prevent mocking.

   Coverage Target Areas:
   - Lines 50-150: Provider selection and routing logic
   - Lines 150-250: Token counting and cognitive tier classification
   - Lines 250-350: Streaming response handling
   - Lines 350-450: Error handling and fallback logic
   - Lines 450-550: Rate limiting and quota management
   - Lines 550-650: Cache integration

   BLOCKERS (from Phase 192):
   - Inline imports of CognitiveTierService, CacheAwareRouter prevent mocking
   - Integration tests recommended for full coverage
   """
   ```

2. **Test categories (25-35 tests):**
   - **Provider routing (8 tests):** OpenAI, Anthropic, DeepSeek, Gemini selection
   - **Token counting (6 tests):** Estimation, actual counting, tier thresholds
   - **Streaming (6 tests):** Chunk processing, async streaming, error recovery
   - **Error handling (6 tests):** Provider failures, timeout, rate limits
   - **Fallback logic (4 tests):** Alternative providers, retry logic
   - **Edge cases (5 tests):** Empty prompts, huge prompts, malformed responses

3. **Work around inline import blockers:**
   - Use integration-style tests with real clients (when API keys available)
   - Patch at module level: `mocker.patch("core.llm.byok_handler.CognitiveTierService")`
   - Focus on methods that don't rely on inline imports
   - Document limitations in test comments

4. **Parametrize provider scenarios:**
   ```python
   @pytest.mark.parametrize("provider,model,expected_tier", [
       ("openai", "gpt-4", "heavy"),
       ("anthropic", "claude-3-opus", "heavy"),
       ("deepseek", "deepseek-chat", "standard"),
       ("openai", "gpt-3.5-turbo", "standard"),
   ])
   ```

5. **Target 50% coverage (327+ of 654 statements)**
   - Focus on routing logic and error handling
   - Accept lower coverage on inline-import methods
  </action>
  <verify>pytest backend/tests/core/llm/test_byok_handler_coverage_extend.py -v --cov=core/llm/byok_handler --cov-report=term-missing</verify>
  <done>25-35 tests created, 50%+ coverage achieved on byok_handler.py (up from 19.4%)</done>
</task>

<task type="auto">
  <name>Task 2: Generate coverage report for plan 193-06</name>
  <files>.planning/phases/193-coverage-push-15-18/193-06-coverage.json</files>
  <action>
Run coverage measurement for BYOKHandler and generate JSON report:

```bash
cd /Users/rushiparikh/projects/atom/backend && \
PYTHONPATH=/Users/rushiparikh/projects/atom/backend \
pytest tests/core/llm/test_byok_handler_coverage_extend.py \
  tests/core/llm/test_byok_handler_coverage.py \
  --cov=core/llm/byok_handler \
  --cov-report=term-missing \
  --cov-report=json:/Users/rushiparikh/projects/atom/.planning/phases/193-coverage-push-15-18/193-06-coverage.json \
  -v
```
  </action>
  <verify>cat /Users/rushiparikh/projects/atom/.planning/phases/193-coverage-push-15-18/193-06-coverage.json | python3 -c "import json,sys; d=json.load(sys.stdin); f=d['files'].get('core/llm/byok_handler.py',{}); s=f.get('summary',{}); print(f\"Coverage: {s.get('percent_covered',0)}% ({s.get('covered',0)}/{s.get('num_statements',0)} stmts)\")"</verify>
  <done>Coverage report JSON generated showing 50%+ coverage</done>
</task>

<task type="auto">
  <name>Task 3: Verify test quality and pass rate</name>
  <files>backend/tests/core/llm/test_byok_handler_coverage_extend.py</files>
  <action>
Run tests and verify >80% pass rate:

```bash
cd /Users/rushiparikh/projects/atom/backend && \
PYTHONPATH=/Users/rushiparikh/projects/atom/backend \
pytest tests/core/llm/test_byok_handler_coverage_extend.py \
  -v \
  --tb=short
```

Fix or skip failing tests to maintain >80% pass rate.
  </action>
  <verify>pytest backend/tests/core/llm/test_byok_handler_coverage_extend.py --co -q | grep "test_" | wc -l</verify>
  <done>>80% of tests passing</done>
</task>

</tasks>

<verification>
1. Test file exists at backend/tests/core/llm/test_byok_handler_coverage_extend.py
2. 25-35 tests created extending BYOK handler coverage
3. Coverage report shows 50%+ (up from 19.4%)
4. Pass rate >80%
5. Inline import blockers documented
6. Coverage report JSON generated
</verification>

<success_criteria>
- BYOKHandler coverage: 19.4% → 50%+ (327+ statements)
- Tests created: 25-35 tests
- Pass rate: >80%
- Provider routing and error handling covered
- Coverage report JSON generated
</success_criteria>

<output>
After completion, create `.planning/phases/193-coverage-push-15-18/193-06-SUMMARY.md`
</output>
