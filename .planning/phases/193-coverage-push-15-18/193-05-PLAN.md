---
phase: 193-coverage-push-15-18
plan: 05
type: execute
wave: 2
depends_on: [193-01]
files_modified:
  - backend/tests/core/workflow/test_workflow_engine_coverage_extend.py
  - backend/core/workflow_engine.py
autonomous: true

must_haves:
  truths:
    - "WorkflowEngine coverage extends from 11.6% to 40%+ (targeting high-impact synchronous methods)"
    - "Tests cover workflow execution, validation, and error handling paths"
    - "Complex async orchestration methods remain partially covered (accepted from Phase 192)"
    - "Coverage report JSON is generated with workflow engine metrics"
  artifacts:
    - path: "backend/tests/core/workflow/test_workflow_engine_coverage_extend.py"
      provides: "Extended coverage tests for workflow engine"
      min_lines: 700
      contains: "TestWorkflowEngineCoverageExtend"
    - path: "backend/core/workflow_engine.py"
      provides: "Workflow orchestration engine"
      unchanged: true
    - path: ".planning/phases/193-coverage-push-15-18/193-05-coverage.json"
      provides: "Coverage report for this plan"
  key_links:
    - from: "backend/tests/core/workflow/test_workflow_engine_coverage_extend.py"
      to: "backend/core/workflow_engine.py"
      via: "import WorkflowEngine"
      pattern: "from core.workflow_engine import"
---

<objective>
Extend WorkflowEngine coverage from 11.6% to 40%+ by focusing on high-impact synchronous methods. Building on Phase 192's 13% baseline, target the remaining testable methods while accepting partial coverage on complex async orchestration (learned from Phase 192 blocker analysis).

Purpose: Improve coverage on the largest workflow file (1,164 statements) with realistic targets
Output: 30-40 additional tests extending coverage on execution, validation, and error handling
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-coverage-push-15-18/193-RESEARCH.md
@.planning/codebase/TESTING.md

# Prior work on WorkflowEngine
@.planning/phases/192-coverage-push-22-28/192-01-SUMMARY.md
@backend/tests/core/workflow/test_workflow_engine_coverage_fix.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend WorkflowEngine coverage tests</name>
  <files>backend/tests/core/workflow/test_workflow_engine_coverage_extend.py</files>
  <action>
Create `backend/tests/core/workflow/test_workflow_engine_coverage_extend.py` with:

1. **Test class structure (extending Phase 192 work):**
   ```python
   """
   Coverage-driven tests for workflow_engine.py (13% -> 40%+ target)

   Building on Phase 192's 13% baseline (148/1,164 statements).
   Focus on testable synchronous methods, accept partial coverage on async orchestration.

   Coverage Target Areas:
   - Lines 120-200: Workflow validation and schema checking
   - Lines 200-300: Step execution with output mapping
   - Lines 300-400: Error handling and rollback logic
   - Lines 400-500: Workflow status transitions
   - Lines 500-600: Semaphore and concurrency management
   - Lines 700-900: Additional synchronous helper methods

   SKIPPED (async orchestration, requires integration testing):
   - Lines 50-120: _execute_workflow_graph (261 statements, 0% coverage)
   """
   ```

2. **Test categories (30-40 tests):**
   - **Extended validation (6 tests):** Schema validation, cycle detection, missing dependencies
   - **Step execution (8 tests):** Output mapping, parameter passing, step chaining
   - **Error handling (8 tests):** Step failures, rollback, error propagation
   - **Status transitions (6 tests):** Created→Running→Completed/Failed/Cancelled
   - **Concurrency (4 tests):** Semaphore limits, parallel execution, deadlock prevention
   - **Edge cases (6 tests):** Empty workflows, single-step workflows, circular refs

3. **Parametrize workflow scenarios:**
   ```python
   @pytest.mark.parametrize("status,action,expected_next_status", [
       ("created", "execute", "running"),
       ("running", "complete", "completed"),
       ("running", "fail", "failed"),
       ("running", "cancel", "cancelled"),
   ])
   ```

4. **Use pytest-mock for external dependencies:**
   - Mock LLM execution: `mocker.patch("core.workflow_engine.execute_llm_step")`
   - Mock API calls: `mocker.patch("core.workflow_engine.execute_api_step")`
   - Use real DB session (conftest.db_session) for persistence

5. **Target 40% coverage (465+ of 1,164 statements)**
   - Focus on synchronous testable methods
   - Accept 0% on `_execute_workflow_graph` (261 statements of async orchestration)
  </action>
  <verify>pytest backend/tests/core/workflow/test_workflow_engine_coverage_extend.py -v --cov=core/workflow_engine --cov-report=term-missing</verify>
  <done>30-40 tests created, 40%+ coverage achieved on workflow_engine.py (up from 11.6%)</done>
</task>

<task type="auto">
  <name>Task 2: Generate coverage report for plan 193-05</name>
  <files>.planning/phases/193-coverage-push-15-18/193-05-coverage.json</files>
  <action>
Run coverage measurement for WorkflowEngine and generate JSON report:

```bash
cd /Users/rushiparikh/projects/atom/backend && \
PYTHONPATH=/Users/rushiparikh/projects/atom/backend \
pytest tests/core/workflow/test_workflow_engine_coverage_extend.py \
  tests/core/workflow/test_workflow_engine_coverage_fix.py \
  --cov=core/workflow_engine \
  --cov-report=term-missing \
  --cov-report=json:/Users/rushiparikh/projects/atom/.planning/phases/193-coverage-push-15-18/193-05-coverage.json \
  -v
```

Note: Include both Phase 192 and Phase 193 tests for aggregate coverage.
  </action>
  <verify>cat /Users/rushiparikh/projects/atom/.planning/phases/193-coverage-push-15-18/193-05-coverage.json | python3 -c "import json,sys; d=json.load(sys.stdin); f=d['files'].get('core/workflow_engine.py',{}); s=f.get('summary',{}); print(f\"Coverage: {s.get('percent_covered',0)}% ({s.get('covered',0)}/{s.get('num_statements',0)} stmts)\")"</verify>
  <done>Coverage report JSON generated showing 40%+ coverage</done>
</task>

<task type="auto">
  <name>Task 3: Verify test quality and pass rate</name>
  <files>backend/tests/core/workflow/test_workflow_engine_coverage_extend.py</files>
  <action>
Run tests and verify >80% pass rate:

```bash
cd /Users/rushiparikh/projects/atom/backend && \
PYTHONPATH=/Users/rushiparikh/projects/atom/backend \
pytest tests/core/workflow/test_workflow_engine_coverage_extend.py \
  -v \
  --tb=short
```

Fix or skip failing tests to maintain >80% pass rate.
  </action>
  <verify>pytest backend/tests/core/workflow/test_workflow_engine_coverage_extend.py --co -q | grep "test_" | wc -l</verify>
  <done>>80% of tests passing</done>
</task>

</tasks>

<verification>
1. Test file exists at backend/tests/core/workflow/test_workflow_engine_coverage_extend.py
2. 30-40 tests created extending workflow engine coverage
3. Coverage report shows 40%+ (up from 11.6%)
4. Pass rate >80%
5. Coverage report JSON generated
</verification>

<success_criteria>
- WorkflowEngine coverage: 11.6% → 40%+ (465+ statements)
- Tests created: 30-40 tests
- Pass rate: >80%
- High-impact synchronous methods covered
- Coverage report JSON generated
</success_criteria>

<output>
After completion, create `.planning/phases/193-coverage-push-15-18/193-05-SUMMARY.md`
</output>
