---
phase: 193-coverage-push-15-18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/core/episodes/test_episode_retrieval_service_coverage.py
  - backend/core/episode_retrieval_service.py
autonomous: true

must_haves:
  truths:
    - "EpisodeRetrievalService has test coverage for all four retrieval modes (temporal, semantic, sequential, contextual)"
    - "EpisodeRetrievalService test coverage increases from 0% to 60%+ (320 statements → ~190+ covered)"
    - "Tests mock external dependencies (LanceDB, PostgreSQL) appropriately for unit testing"
    - "Coverage report JSON is generated and includes episode_retrieval_service.py metrics"
  artifacts:
    - path: "backend/tests/core/episodes/test_episode_retrieval_service_coverage.py"
      provides: "Coverage tests for episode retrieval service"
      min_lines: 600
      contains: "TestEpisodeRetrievalServiceCoverage"
    - path: "backend/core/episode_retrieval_service.py"
      provides: "Episode retrieval service with four modes"
      unchanged: true
    - path: ".planning/phases/193-coverage-push-15-18/193-01-coverage.json"
      provides: "Coverage report for this plan"
  key_links:
    - from: "backend/tests/core/episodes/test_episode_retrieval_service_coverage.py"
      to: "backend/core/episode_retrieval_service.py"
      via: "import EpisodeRetrievalService"
      pattern: "from core.episode_retrieval_service import"
---

<objective>
Create comprehensive test coverage for EpisodeRetrievalService (currently 0%, 320 statements) focusing on the four retrieval modes: temporal, semantic, sequential, and contextual. This Priority 1 file is central to the episodic memory system and has zero coverage.

Purpose: Enable testing of episode retrieval which is critical for agent learning and memory access
Output: 40-50 tests covering retrieval modes, error handling, and edge cases
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/193-coverage-push-15-18/193-RESEARCH.md
@.planning/codebase/TESTING.md

# Proven patterns from Phase 192
@.planning/phases/192-coverage-push-22-28/192-01-SUMMARY.md
@.planning/phases/192-coverage-push-22-28/192-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EpisodeRetrievalService coverage test file</name>
  <files>backend/tests/core/episodes/test_episode_retrieval_service_coverage.py</files>
  <action>
Create `backend/tests/core/episodes/test_episode_retrieval_service_coverage.py` with:

1. **Test class structure:**
   ```python
   """
   Coverage-driven tests for episode_retrieval_service.py (0% -> 60%+ target)

   Coverage Target Areas:
   - Lines 1-50: Service initialization and configuration
   - Lines 50-150: Temporal retrieval mode (time-based episode search)
   - Lines 150-250: Semantic retrieval mode (vector search with LanceDB)
   - Lines 250-350: Sequential retrieval mode (full episode with segments)
   - Lines 350-450: Contextual retrieval mode (hybrid approach)
   - Lines 450-550: Error handling and edge cases
   """
   ```

2. **Test categories (40-50 tests):**
   - **Initialization (5 tests):** Default config, custom LanceDB client, custom DB session
   - **Temporal retrieval (10 tests):** Date range queries, limit/offset, sort orders, empty results
   - **Semantic retrieval (10 tests):** Vector search, similarity thresholds, embedding fallback
   - **Sequential retrieval (8 tests):** Full episode loading, segment ordering, pagination
   - **Contextual retrieval (8 tests):** Hybrid temporal+semantic, context weighting
   - **Error handling (5 tests):** Invalid agent ID, database errors, LanceDB failures
   - **Edge cases (4 tests):** Empty database, malformed queries, concurrent access

3. **Use pytest-mock for external dependencies:**
   - Mock LanceDB client: `mocker.patch("core.episode_retrieval_service.lancedb")`
   - Mock PostgreSQL queries: Use real DB session (conftest.db_session) for integration testing
   - Mock embedding service: `mocker.patch("core.episode_retrieval_service.generate_embedding")`

4. **Parametrize test cases for retrieval modes:**
   ```python
   @pytest.mark.parametrize("mode,query,expected_count", [
       ("temporal", {"date_range": "7d"}, 10),
       ("semantic", {"query": "meeting notes", "threshold": 0.7}, 5),
       ("sequential", {"episode_id": "ep-123"}, 1),
       ("contextual", {"query": "budget", "context": "finance"}, 3),
   ])
   ```

5. **Target 60% coverage (190+ of 320 statements)**
   - Focus on testable synchronous methods
   - Accept lower coverage on complex async orchestration (learned from Phase 192)
  </action>
  <verify>pytest backend/tests/core/episodes/test_episode_retrieval_service_coverage.py -v --cov=core/episode_retrieval_service --cov-report=term-missing</verify>
  <done>40-50 tests created, 60%+ coverage achieved on episode_retrieval_service.py</done>
</task>

<task type="auto">
  <name>Task 2: Generate coverage report for plan 193-01</name>
  <files>.planning/phases/193-coverage-push-15-18/193-01-coverage.json</files>
  <action>
Run coverage measurement for EpisodeRetrievalService and generate JSON report:

```bash
cd /Users/rushiparikh/projects/atom/backend && \
PYTHONPATH=/Users/rushiparikh/projects/atom/backend \
pytest tests/core/episodes/test_episode_retrieval_service_coverage.py \
  --cov=core/episode_retrieval_service \
  --cov-report=term-missing \
  --cov-report=json:/Users/rushiparikh/projects/atom/.planning/phases/193-coverage-push-15-18/193-01-coverage.json \
  -v
```

Report must include:
- percent_covered
- num_statements (covered/total)
- missing_lines
- summary by section
  </action>
  <verify>cat /Users/rushiparikh/projects/atom/.planning/phases/193-coverage-push-15-18/193-01-coverage.json | python3 -c "import json,sys; d=json.load(sys.stdin); print(f\"Coverage: {d['files'].get('core/episode_retrieval_service.py',{}).get('summary',{}).get('percent_covered',0)}%\")"</verify>
  <done>Coverage report JSON generated at .planning/phases/193-coverage-push-15-18/193-01-coverage.json showing 60%+ coverage</done>
</task>

<task type="auto">
  <name>Task 3: Verify test quality and pass rate</name>
  <files>backend/tests/core/episodes/test_episode_retrieval_service_coverage.py</files>
  <action>
Run tests and verify >80% pass rate (quality focus from Phase 192):

```bash
cd /Users/rushiparikh/projects/atom/backend && \
PYTHONPATH=/Users/rushiparikh/projects/atom/backend \
pytest tests/core/episodes/test_episode_retrieval_service_coverage.py \
  -v \
  --tb=short \
  | tee /tmp/193-01-test-results.txt
```

Check pass rate:
```bash
grep -E "passed|failed" /tmp/193-01-test-results.txt
```

If pass rate < 80%:
- Identify failing tests
- Categorize failures: flaky (timing), blocked (missing deps), invalid (wrong assertions)
- Fix or skip problematic tests with `@pytest.mark.skip(reason="...")`
  </action>
  <verify>pytest backend/tests/core/episodes/test_episode_retrieval_service_coverage.py --co -q | grep "test_" | wc -l</verify>
  <done>>80% of tests passing, failing tests categorized and documented</done>
</task>

</tasks>

<verification>
1. Test file exists at backend/tests/core/episodes/test_episode_retrieval_service_coverage.py
2. 40-50 tests created covering all four retrieval modes
3. Coverage report shows 60%+ coverage (190+ of 320 statements)
4. Pass rate >80% (quality improvement from Phase 192's 68.5%)
5. Coverage report JSON generated at phase directory
</verification>

<success_criteria>
- EpisodeRetrievalService coverage: 0% → 60%+ (190+ statements)
- Tests created: 40-50 tests
- Pass rate: >80% (vs 68.5% in Phase 192)
- All four retrieval modes tested
- Coverage report JSON generated
</success_criteria>

<output>
After completion, create `.planning/phases/193-coverage-push-15-18/193-01-SUMMARY.md` with:
- Coverage achieved (percent and statements)
- Test count and pass rate
- Key findings from episode retrieval testing
- Blockers identified (if any)
</output>
