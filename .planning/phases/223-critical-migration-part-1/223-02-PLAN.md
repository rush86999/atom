---
phase: 223-critical-migration-part-1
plan: 02
type: execute
wave: 2
depends_on: [223-01]
files_modified:
  - backend/core/embedding_service.py
  - backend/tests/core/services/test_embedding_service.py
  - backend/tests/core/agents/test_embedding_service_coverage.py
autonomous: true

must_haves:
  truths:
    - "embedding_service.py uses LLMService instead of direct AsyncOpenAI import"
    - "_generate_openai_embedding method delegates to llm_service.generate_embedding"
    - "_generate_openai_embeddings_batch method delegates to llm_service.generate_embeddings_batch"
    - "Existing embedding tests still pass with LLMService integration"
    - "No regression in embedding generation (same output dimensions and values)"
  artifacts:
    - path: "backend/core/embedding_service.py"
      provides: "Embedding generation via LLMService"
      contains: "from core.llm_service import LLMService"
      removes: "from openai import AsyncOpenAI"
  key_links:
    - from: "backend/core/embedding_service.py"
      to: "backend/core/llm_service.py"
      via: "LLMService instantiation and generate_embedding call"
      pattern: "self\\.llm_service\\.generate_embedding"
    - from: "backend/tests/core/services/test_embedding_service.py"
      to: "backend/core/embedding_service.py"
      via: "Test execution verifies migration compatibility"
      pattern: "pytest.*test_embedding"
---

<objective>
Migrate embedding_service.py from direct AsyncOpenAI usage to LLMService.

Purpose: Eliminate direct AsyncOpenAI client usage in embedding_service.py, consolidating all LLM interactions through the unified LLMService interface for consistency, observability, and BYOK support.

Output: embedding_service.py using LLMService for all OpenAI embedding operations, with all existing tests passing.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Previous phase 223 work (MUST READ)
@.planning/phases/223-critical-migration-part-1/223-01-SUMMARY.md

# Phase 222 patterns
@.planning/phases/222-llm-service-enhancement/222-04-SUMMARY.md

# File being migrated
@backend/core/embedding_service.py
  - Lines 647, 669: AsyncOpenAI import locations
  - Lines 644-664: _generate_openai_embedding method
  - Lines 666-691: _generate_openai_embeddings_batch method
  - Constructor: __init__ method around lines 63-100

# Existing tests (must keep passing)
@backend/tests/core/services/test_embedding_service.py
@backend/tests/core/agents/test_embedding_service_coverage.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LLMService to EmbeddingService initialization</name>
  <files>backend/core/embedding_service.py</files>
  <action>
Modify EmbeddingService.__init__ to create LLMService instance:

1. Add import at top of file (after existing imports):
   ```python
   from core.llm_service import LLMService
   ```

2. In __init__ method (around line 63-100), add:
   ```python
   # Initialize LLMService for OpenAI embeddings (unified interface)
   self.llm_service = LLMService(workspace_id="default")
   ```

3. DO NOT remove:
   - FastEmbed client initialization (keep local embedding working)
   - Cohere client initialization (keep alternative provider working)
   - Existing config handling

4. Add comment explaining LLMService is for OpenAI embeddings only (FastEmbed/Cohere use their own clients)
  </action>
  <verify>grep -n "self.llm_service = LLMService" backend/core/embedding_service.py</verify>
  <done>LLMService initialized in __init__, available for OpenAI embedding methods</done>
</task>

<task type="auto">
  <name>Task 2: Migrate _generate_openai_embedding to use LLMService</name>
  <files>backend/core/embedding_service.py</files>
  <action>
Replace AsyncOpenAI client usage with LLMService delegation:

1. Remove lines 647-651 (AsyncOpenAI client creation):
   ```python
   # DELETE these lines:
   from openai import AsyncOpenAI
   client = AsyncOpenAI(api_key=...)
   ```

2. Replace client.embeddings.create call (lines 653-656) with:
   ```python
   embedding = await self.llm_service.generate_embedding(
       text=text,
       model=self.model
   )
   return embedding
   ```

3. Keep error handling structure but simplify:
   - Remove ImportError exception (no longer importing openai directly)
   - Keep general Exception handler with logging

4. Update docstring to reflect LLMService usage

The method should now be ~10 lines instead of ~20 lines.
  </action>
  <verify>
grep -n "self.llm_service.generate_embedding" backend/core/embedding_service.py
! grep -n "from openai import AsyncOpenAI" backend/core/embedding_service.py
  </verify>
  <done>Method uses LLMService, no direct AsyncOpenAI import, returns embedding vector</done>
</task>

<task type="auto">
  <name>Task 3: Migrate _generate_openai_embeddings_batch to use LLMService</name>
  <files>backend/core/embedding_service.py</files>
  <action>
Replace AsyncOpenAI client usage with LLMService delegation:

1. Remove lines 669-673 (AsyncOpenAI client creation):
   ```python
   # DELETE these lines:
   from openai import AsyncOpenAI
   client = AsyncOpenAI(api_key=...)
   ```

2. Replace entire batch loop (lines 675-687) with:
   ```python
   embeddings = await self.llm_service.generate_embeddings_batch(
       texts=texts,
       model=self.model
   )
   return embeddings
   ```

3. Remove batch_size variable and loop (LLMService handles batching internally)

4. Keep error handling with logging

5. Update docstring to reflect LLMService handles batching

The method should now be ~8 lines instead of ~25 lines.
  </action>
  <verify>
grep -n "self.llm_service.generate_embeddings_batch" backend/core/embedding_service.py
! grep -n "batch_size = 2048" backend/core/embedding_service.py
  </verify>
  <done>Method delegates to LLMService for batching, no manual batch handling</done>
</task>

<task type="auto">
  <name>Task 4: Verify existing tests still pass</name>
  <files>backend/tests/core/services/test_embedding_service.py, backend/tests/core/agents/test_embedding_service_coverage.py</files>
  <action>
Run existing embedding service tests to verify migration didn't break functionality:

1. Run all embedding service tests:
   ```bash
   python3 -m pytest backend/tests/core/services/test_embedding_service.py -v
   python3 -m pytest backend/tests/core/agents/test_embedding_service_coverage.py -v
   ```

2. Check for:
   - All tests passing (no new failures)
   - No regression in embedding dimensions (1536 for text-embedding-3-small)
   - Batch operations still work correctly
   - Error handling still works

3. If any tests fail, investigate:
   - Mock expectations may need updating (if mocking AsyncOpenAI directly)
   - Test setup may need LLMService mocking instead

4. DO NOT modify test logic unless tests are failing due to brittle mocks

5. Verify test output shows expected pass rate
  </action>
  <verify>
python3 -m pytest backend/tests/core/services/test_embedding_service.py backend/tests/core/agents/test_embedding_service_coverage.py -v --tb=short
  </verify>
  <done>All existing embedding service tests pass with LLMService integration</done>
</task>

</tasks>

<verification>
1. Code verification: No "from openai import AsyncOpenAI" in embedding_service.py
2. Code verification: Both _generate_openai_embedding and _generate_openai_embeddings_batch use self.llm_service
3. Test verification: All existing embedding tests pass
4. Integration verification: Generate embedding returns correct dimensions
5. Pattern verification: Follows Phase 222 migration patterns (delegation to LLMService)
</verification>

<success_criteria>
1. embedding_service.py has zero direct AsyncOpenAI imports
2. _generate_openai_embedding uses self.llm_service.generate_embedding
3. _generate_openai_embeddings_batch uses self.llm_service.generate_embeddings_batch
4. All existing tests pass (test_embedding_service.py, test_embedding_service_coverage.py)
5. Embedding generation produces same results (dimensions, values) as before migration
</success_criteria>

<output>
After completion, create `.planning/phases/223-critical-migration-part-1/223-02-SUMMARY.md` with:
- Migration changes made (LLMService added, AsyncOpenAI removed)
- Test results (all passing)
- Lines of code reduction (simpler methods)
- Next step: Ready for 223-03 (migrate graphrag_engine.py)
</output>
