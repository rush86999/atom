---
phase: 223-critical-migration-part-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/llm_service.py
  - backend/tests/test_llm_service.py
autonomous: true

must_haves:
  truths:
    - "LLMService can generate embeddings via generate_embedding method"
    - "LLMService can generate batch embeddings via generate_embeddings_batch method"
    - "Embedding methods support OpenAI text-embedding-3-small and text-embedding-3-large models"
    - "Embedding methods delegate to AsyncOpenAI for actual API calls"
    - "Existing embedding_service.py tests still pass after migration"
  artifacts:
    - path: "backend/core/llm_service.py"
      provides: "generate_embedding and generate_embeddings_batch methods"
      min_lines: 80
    - path: "backend/tests/test_llm_service.py"
      provides: "Tests for embedding generation"
      exports: ["test_generate_embedding", "test_generate_embeddings_batch"]
  key_links:
    - from: "backend/core/embedding_service.py"
      to: "backend/core/llm_service.py"
      via: "LLMService.generate_embedding call"
      pattern: "llm_service\\.generate_embedding"
---

<objective>
Add embedding generation support to LLMService for migration consistency.

Purpose: embedding_service.py currently uses AsyncOpenAI() directly, which violates the unified LLMService pattern. LLMService needs embedding generation capabilities so all LLM interactions flow through one interface.

Output: LLMService with generate_embedding() and generate_embeddings_batch() methods, tested and ready for embedding_service.py migration.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 222 established patterns
@.planning/phases/222-llm-service-enhancement/222-01-SUMMARY.md
@.planning/phases/222-llm-service-enhancement/222-04-SUMMARY.md

# Files being extended
@backend/core/llm_service.py (existing methods, BYOKHandler delegation pattern)
@backend/tests/test_llm_service.py (test structure, fixture patterns)

# Target migration file (for reference)
@backend/core/embedding_service.py (lines 644-691: current AsyncOpenAI usage)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generate_embedding method to LLMService</name>
  <files>backend/core/llm_service.py</files>
  <action>
Add async generate_embedding() method to LLMService class:

1. Import AsyncOpenAI lazily (inside method, not top-level):
   ```python
   from openai import AsyncOpenAI
   ```

2. Method signature:
   ```python
   async def generate_embedding(
       self,
       text: str,
       model: str = "text-embedding-3-small"
   ) -> List[float]:
   ```

3. Implementation:
   - Get API key from BYOKHandler or environment
   - Create AsyncOpenAI client with API key
   - Call client.embeddings.create(model=model, input=text)
   - Return response.data[0].embedding as List[float]

4. Error handling:
   - ImportError: raise Exception with pip install instructions
   - API errors: log and re-raise with context

5. Docstring with example usage matching LLMService patterns

Place after generate_structured() method, before get_provider().
  </action>
  <verify>grep -n "async def generate_embedding" backend/core/llm_service.py</verify>
  <done>Method exists with correct signature, returns List[float], handles errors properly</done>
</task>

<task type="auto">
  <name>Task 2: Add generate_embeddings_batch method to LLMService</name>
  <files>backend/core/llm_service.py</files>
  <action>
Add async generate_embeddings_batch() method to LLMService class:

1. Method signature:
   ```python
   async def generate_embeddings_batch(
       self,
       texts: List[str],
       model: str = "text-embedding-3-small"
   ) -> List[List[float]]:
   ```

2. Implementation:
   - Get API key from BYOKHandler or environment
   - Create AsyncOpenAI client with API key
   - Process in batches of 2048 (OpenAI limit)
   - For each batch: call client.embeddings.create(model=model, input=batch)
   - Extract embeddings from response.data
   - Return flattened list of embeddings

3. Error handling:
   - Log error but include details about which batch failed
   - Re-raise with batch index for debugging

4. Docstring with batch size explanation

Place immediately after generate_embedding() method.
  </action>
  <verify>grep -n "async def generate_embeddings_batch" backend/core/llm_service.py</verify>
  <done>Method exists, handles batching correctly, processes up to 2048 texts per request</done>
</task>

<task type="auto">
  <name>Task 3: Add embedding generation tests</name>
  <files>backend/tests/test_llm_service.py</files>
  <action>
Add TestLLMServiceEmbedding class with tests:

1. Test fixtures (inherit from existing classes):
   - mock_openai_client: AsyncMock for AsyncOpenAI
   - mock_embedding_response: Mock with response.data[0].embedding structure

2. Test cases:
   - test_generate_embedding_basic: Verify vector returned, correct dimensions
   - test_generate_embedding_with_custom_model: text-embedding-3-large model
   - test_generate_embedding_api_key_from_byok: Uses BYOKHandler for API key
   - test_generate_embedding_batch_basic: Multiple texts processed
   - test_generate_embedding_batch_large: Respects 2048 batch limit
   - test_generate_embedding_error_handling: API error caught and logged

3. Mock AsyncOpenAI to avoid real API calls:
   - Mock client.embeddings.create to return predefined embedding
   - Return 1536-dimension vector for text-embedding-3-small
   - Return 3072-dimension vector for text-embedding-3-large

Add after TestLLMServiceBackwardCompatibility class.
  </action>
  <verify>
grep -n "class TestLLMServiceEmbedding" backend/tests/test_llm_service.py
python3 -m pytest backend/tests/test_llm_service.py::TestLLMServiceEmbedding -v
  </verify>
  <done>6 tests passing, all embedding methods tested with mocked AsyncOpenAI client</done>
</task>

</tasks>

<verification>
1. Code verification: LLMService has generate_embedding and generate_embeddings_batch methods
2. Type verification: Methods return List[float] and List[List[float]] respectively
3. Test verification: All 6 embedding tests pass
4. Integration verification: Can import LLMService and call generate_embedding without errors
5. Pattern verification: Follows Phase 222 patterns (lazy imports, BYOKHandler delegation, error handling)
</verification>

<success_criteria>
1. LLMService.generate_embedding(text) returns 1536-dimension vector for text-embedding-3-small
2. LLMService.generate_embeddings_batch(texts) processes multiple texts in one call
3. Batch size of 2048 is respected (no API errors for large batches)
4. All 6 new tests pass with mocked AsyncOpenAI client
5. Methods follow existing LLMService patterns (docstrings, error handling, BYOK integration)
</success_criteria>

<output>
After completion, create `.planning/phases/223-critical-migration-part-1/223-01-SUMMARY.md` with:
- Methods added (generate_embedding, generate_embeddings_batch)
- Tests created (6 embedding tests)
- Verification results (all passing)
- Next step: Ready for 223-02 (migrate embedding_service.py to use LLMService)
</output>
