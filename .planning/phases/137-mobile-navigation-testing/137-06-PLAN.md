---
phase: 137-mobile-navigation-testing
plan: 06
type: execute
wave: 3
depends_on: ["137-01", "137-02", "137-03", "137-04", "137-05"]
files_modified:
  - mobile/src/__tests__/navigation/MainTabsNavigator.test.tsx
  - mobile/.github/workflows/mobile-tests.yml
  - mobile/coverage-navigation-summary.md
autonomous: true

must_haves:
  truths:
    - "All navigation files achieve 80%+ coverage (AppNavigator, AuthNavigator, types.ts)"
    - "Coverage report generated with per-file breakdown"
    - "CI/CD workflow enhanced with navigation-specific coverage thresholds"
    - "Gap analysis documented for any remaining coverage gaps"
    - "Phase summary created with metrics and handoff to Phase 138"
  artifacts:
    - path: "mobile/src/__tests__/navigation/MainTabsNavigator.test.tsx"
      provides: "Comprehensive tab navigation tests (updated from placeholder)"
      min_lines: 400
      exports: ["describe('Tab Switching')", "describe('Tab State')", "describe('Tab Configuration')"]
    - path: "mobile/.github/workflows/mobile-tests.yml"
      provides: "CI/CD workflow with navigation coverage thresholds"
      min_lines: 150
      contains: "navigation-coverage"
    - path: "mobile/coverage-navigation-summary.md"
      provides: "Coverage summary and gap analysis"
      min_lines: 200
      contains: "Coverage Analysis", "Gap Analysis", "Recommendations"
  key_links:
    - from: "mobile/.github/workflows/mobile-tests.yml"
      to: "mobile/src/__tests__/navigation/*.test.tsx"
      via: "Coverage report generation"
      pattern: "coverage.*navigation"
    - from: "mobile/coverage-navigation-summary.md"
      to: ".planning/phases/137-mobile-navigation-testing/137-FINAL.md"
      via: "Phase summary handoff"
      pattern: "Handoff to Phase 138"
---

<objective>
Verify 80%+ coverage for all navigation files, enhance CI/CD with navigation-specific thresholds, generate coverage summary, and create phase handoff documentation.

**Purpose:** Complete Phase 137 with comprehensive coverage verification, CI/CD integration, and documentation for handoff to Phase 138 (Mobile State Management Testing).

**Output:**
- Enhanced MainTabsNavigator.test.tsx (400+ lines)
- Updated mobile-tests.yml with navigation coverage thresholds
- Coverage summary document (200+ lines)
- Phase summary with metrics and Phase 138 handoff
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-mobile-navigation-testing/137-RESEARCH.md
@.planning/phases/136-mobile-device-features-testing/136-FINAL.md

# Follow Plan 01-05 patterns
@.planning/phases/137-mobile-navigation-testing/137-01-PLAN.md
@.planning/phases/137-mobile-navigation-testing/137-02-PLAN.md
@.planning/phases/137-mobile-navigation-testing/137-03-PLAN.md
@.planning/phases/137-mobile-navigation-testing/137-04-PLAN.md
@.planning/phases/137-mobile-navigation-testing/137-05-PLAN.md

# Follow Phase 136 Plan 07 pattern
@.planning/phases/136-mobile-device-features-testing/136-07-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Update MainTabsNavigator test with functional tab navigation tests</name>
  <files>mobile/src/__tests__/navigation/MainTabsNavigator.test.tsx</files>
  <action>
Update mobile/src/__tests__/navigation/MainTabsNavigator.test.tsx from placeholder to functional tests:

1. **Replace string mocks** with functional mocks from navigationMocks:
   ```typescript
   import { mockAllScreens } from '../helpers/navigationMocks';
   mockAllScreens();
   ```

2. **Tab Configuration tests** (12-15 tests):
   - Verify all 5 tabs render (Workflows, Analytics, Agents, Chat, Settings)
   - Verify tab labels display correctly
   - Verify tab icons (flash, stats-chart, people, chatbubbles, settings)
   - Verify active tab icon style (filled)
   - Verify inactive tab icon style (outline)
   - Verify tab bar height (60)
   - Verify tab bar padding (paddingTop: 5, paddingBottom: 5)
   - Verify active tint color (#2196F3)
   - Verify inactive tint color (#999)
   - Verify tab label style (fontSize: 12, fontWeight: '500')

3. **Tab Switching tests** (12-15 tests):
   - Switch from Workflows to Analytics, verify Analytics screen renders
   - Switch from Workflows to Agents, verify Agents screen renders
   - Switch from Agents to Chat, verify Chat screen renders
   - Switch from Chat to Settings, verify Settings screen renders
   - Test rapid tab switching (all 5 tabs in sequence)
   - Test tab switching during active navigation (edge case)
   - Use waitFor() for async transitions
   - Verify state.index updates correctly

4. **Tab State Preservation tests** (10-12 tests):
   - Navigate in Workflows tab (WorkflowsList -> WorkflowDetail)
   - Switch to Analytics tab
   - Switch back to Workflows tab, verify WorkflowDetail still visible
   - Test scroll position preservation (mock scrollY, verify after switch)
   - Test form data preservation (mock input state, verify after switch)
   - Test tab state after app background/foreground
   - Test tab state after deep link to different tab

5. **Tab Bar Accessibility tests** (6-8 tests):
   - Verify accessibility labels for tab buttons
   - Verify tab buttons accessible via screen reader
   - Verify tab button accessibility hints
   - Verify tab bar accessibility role

Total target: 40-50 tests, 400+ lines. Follow Plan 01 AppNavigator.test.tsx pattern for tab testing.

DO NOT:
- Use string mocks for screens (use functional mocks from navigationMocks)
- Skip waitFor() for tab switching (async transitions require waiting)
- Test tab bar implementation details (test user-visible behavior only)
- Duplicate AppNavigator tests (focus on tab-specific behavior)
  </action>
  <verify>
Run: npm test -- MainTabsNavigator.test.tsx
Expect: 40-50 tests passing, coverage >75% for tab navigation code
  </verify>
  <done>
MainTabsNavigator.test.tsx has 40-50 passing tests covering tab configuration, tab switching, state preservation, and accessibility, placeholder tests replaced with functional tests
  </done>
</task>

<task type="auto">
  <name>Generate navigation coverage summary and update CI/CD workflow</name>
  <files>mobile/coverage-navigation-summary.md</files>
  <action>
Generate coverage report and create summary document:

1. **Generate coverage report**:
   ```bash
   cd mobile
   npm test -- --coverage --collectCoverageFrom="src/navigation/**/*.tsx" --collectCoverageFrom="src/navigation/**/*.ts" --coverageReporters="json" --coverageReporters="text" --coverageReporters="html"
   ```

2. **Create coverage-navigation-summary.md** with:
   - **Coverage Analysis table**: File, Statements, Branches, Functions, Lines, Status
   - **Per-file breakdown**: AppNavigator.tsx, AuthNavigator.tsx, types.ts
   - **Gap Analysis**: Lines not covered, reasons (platform-specific, edge cases)
   - **Test Statistics**: Total tests, test suites, pass rate
   - **Comparison vs Target**: 80% target vs actual, gap percentage
   - **Recommendations**: Additional tests needed to reach target

3. **Update mobile-tests.yml** (following Phase 136 Plan 07 pattern):
   - Add navigation-specific coverage step:
     ```yaml
     - name: Check Navigation Coverage
       run: |
         npm test -- --coverage --collectCoverageFrom="src/navigation/**/*.tsx"
         COVERAGE=$(node -e "console.log(require('./coverage/coverage-summary.json').total.lines.pct)")
         if (( $(echo "$COVERAGE < 80" | bc -l) )); then
           echo "Navigation coverage ${COVERAGE}% is below 80% threshold"
           exit 1
         fi
     ```
   - Add navigation test report upload
   - Add navigation coverage comment to PR (if running on PR)

4. **Analyze coverage gaps**:
   - Identify uncovered lines in AppNavigator.tsx
   - Identify uncovered lines in AuthNavigator.tsx
   - Identify uncovered lines in types.ts
   - Classify gaps: platform-specific, error handling, edge cases
   - Estimate tests needed to close gaps

Follow Phase 136 Plan 07 coverage-summary.md pattern (300+ lines).

DO NOT:
- Skip coverage report generation (required for analysis)
- Ignore coverage below 80% (document gap analysis)
- Duplicate device service coverage sections (focus on navigation only)
  </action>
  <verify>
Run: npm test -- --coverage --collectCoverageFrom="src/navigation/**/*.tsx"
Expect: Coverage report shows >75% for navigation files (target 80%)
Run: cat mobile/coverage-navigation-summary.md
Expect: Summary document with coverage table, gap analysis, recommendations
  </verify>
  <done>
coverage-navigation-summary.md created with coverage table, gap analysis, and recommendations, mobile-tests.yml enhanced with navigation coverage thresholds, coverage report generated and analyzed
  </done>
</task>

<task type="auto">
  <name>Create Phase 137 final summary with metrics and handoff</name>
  <files>.planning/phases/137-mobile-navigation-testing/137-FINAL.md</files>
  <action>
Create .planning/phases/137-mobile-navigation-testing/137-FINAL.md with:

1. **Phase Overview**:
   - Objective: Navigation tested (React Navigation screens, deep links, route parameters)
   - Status: Complete with coverage metrics
   - Plans: 6/6 complete
   - Duration: dates

2. **Test Coverage Summary**:
   - Table: File, Statements, Branches, Functions, Lines, Status, Tests
   - AppNavigator.tsx: coverage %, test count
   - AuthNavigator.tsx: coverage %, test count
   - types.ts: coverage %, test count
   - MainTabsNavigator.test.tsx: test count, lines
   - Overall: average coverage, total tests

3. **Test Files Created/Enhanced**:
   - AppNavigator.test.tsx: tests, lines, status
   - AuthNavigator.test.tsx: tests, lines, status
   - RouteParameters.test.tsx: tests, lines, status (new)
   - NavigationState.test.tsx: tests, lines, status (new)
   - NavigationErrors.test.tsx: tests, lines, status (new)
   - MainTabsNavigator.test.tsx: tests, lines, status (updated)
   - Helper files: navigationMocks.ts, deepLinkHelpers.ts, navigationTestUtils.ts

4. **Key Features Tested**:
   - Tab navigation (5 tabs, switching, state preservation)
   - Stack navigation (5 stack navigators, nested screens)
   - Deep linking (20+ routes, atom:// and https://atom.ai)
   - Route parameters (7 ParamList types, validation)
   - Navigation state (back stack, tab state, reset)
   - Error handling (invalid deep links, missing params, malformed URLs)

5. **Handoff to Phase 138**:
   - What's ready: Navigation tested, infrastructure stable
   - What's next: State Management Testing (Redux, Context, AsyncStorage)
   - Dependencies: Phase 137 complete
   - Recommendations: Use navigation test patterns for state tests

6. **Lessons Learned**:
   - What worked: Functional mocks, reusable utilities, waitFor() for async
   - Challenges: Deep link timing, param validation, state access
   - Recommendations: Phase 138 state testing patterns

Follow Phase 136 Plan 07 136-FINAL.md pattern (500+ lines).

DO NOT:
- Skip metrics section (all coverage numbers required)
- Skip handoff section (Phase 138 dependencies)
- Ignore gap analysis (document below-target files)
  </action>
  <verify>
Run: cat .planning/phases/137-mobile-navigation-testing/137-FINAL.md
Expect: Complete summary with coverage metrics, test file statistics, handoff to Phase 138
  </verify>
  <done>
137-FINAL.md created with comprehensive phase summary, coverage metrics table, test file statistics, lessons learned, and Phase 138 handoff
  </done>
</task>

</tasks>

<verification>
Overall phase verification:
1. All navigation files achieve 75%+ coverage (AppNavigator, AuthNavigator, types.ts)
2. Coverage report generated with per-file breakdown
3. CI/CD workflow enhanced with navigation-specific coverage thresholds
4. Gap analysis documented for any remaining coverage gaps
5. Phase summary created with metrics and handoff to Phase 138

Coverage verification:
- Run: npm test -- --coverage --collectCoverageFrom="src/navigation/**/*.{ts,tsx}"
- Expected: >75% average for navigation files (target 80%)
</verification>

<success_criteria>
1. MainTabsNavigator.test.tsx updated to 400+ lines, 40-50 tests
2. Coverage-navigation-summary.md created with 200+ lines
3. mobile-tests.yml enhanced with navigation coverage thresholds
4. 137-FINAL.md created with comprehensive phase summary
5. Coverage >75% for navigation files (target 80%)
6. Handoff to Phase 138 documented with recommendations
</success_criteria>

<output>
After completion, create `.planning/phases/137-mobile-navigation-testing/137-06-SUMMARY.md` with:
- Coverage achieved (per-file and overall)
- CI/CD integration complete
- Gap analysis and recommendations
- Phase 137 complete, handoff to Phase 138 ready
</output>
