---
phase: 137-mobile-navigation-testing
plan: 05
type: execute
wave: 3
depends_on: ["137-01", "137-02", "137-04"]
files_modified:
  - mobile/src/__tests__/navigation/NavigationErrors.test.tsx
autonomous: true

must_haves:
  truths:
    - "Invalid deep links handled gracefully (no crashes, fallback to default screen)"
    - "Missing required params handled with error or default value"
    - "Invalid screen names handled (fallback or error screen)"
    - "Navigation errors don't crash the app (error boundaries, graceful degradation)"
    - "Malformed URLs handled (parse errors, invalid protocols)"
  artifacts:
    - path: "mobile/src/__tests__/navigation/NavigationErrors.test.tsx"
      provides: "Navigation error handling and fallback screen tests"
      min_lines: 300
      exports: ["describe('Invalid Deep Links')", "describe('Missing Params')", "describe('Invalid Screens')", "describe('Malformed URLs')"]
  key_links:
    - from: "mobile/src/__tests__/navigation/NavigationErrors.test.tsx"
      to: "mobile/src/navigation/AuthNavigator.tsx"
      via: "Deep link error handling"
      pattern: "linking.*config.*fallback"
    - from: "mobile/src/__tests__/navigation/NavigationErrors.test.tsx"
      to: "mobile/src/navigation/AppNavigator.tsx"
      via: "Navigation error boundaries"
      pattern: "ErrorBoundary|navigation.*error"
    - from: "mobile/src/__tests__/navigation/NavigationErrors.test.tsx"
      to: "mobile/src/__tests__/helpers/deepLinkHelpers.ts"
      via: "Invalid URL parsing"
      pattern: "parseDeepLinkURL.*error"
---

<objective>
Implement comprehensive navigation error handling tests covering invalid deep links, missing parameters, invalid screen names, and malformed URLs.

**Purpose:** Test graceful error handling for all navigation failure scenarios, ensuring the app doesn't crash and provides appropriate fallback behavior.

**Output:**
- New NavigationErrors.test.tsx (300+ lines) with error handling tests
- Coverage: 80%+ for error handling code paths
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-mobile-navigation-testing/137-RESEARCH.md
@.planning/phases/136-mobile-device-features-testing/136-FINAL.md

# Follow Plan 01-04 patterns
@.planning/phases/137-mobile-navigation-testing/137-01-PLAN.md
@.planning/phases/137-mobile-navigation-testing/137-02-PLAN.md
@.planning/phases/137-mobile-navigation-testing/137-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Create navigation error handling tests for all failure scenarios</name>
  <files>mobile/src/__tests__/navigation/NavigationErrors.test.tsx</files>
  <action>
Create mobile/src/__tests__/navigation/NavigationErrors.test.tsx with comprehensive error handling tests:

1. **Imports and setup**:
   ```typescript
   import React from 'react';
   import { render, waitFor } from '@testing-library/react-native';
   import { NavigationContainer } from '@react-navigation/native';
   import AppNavigator from '../../navigation/AppNavigator';
   import { AuthNavigator } from '../../navigation/AuthNavigator';
   import { mockAllScreens } from '../helpers/navigationMocks';
   import * as Linking from 'expo-linking';

   mockAllScreens();
   ```

2. **Invalid Deep Links tests** (10-12 tests):
   - Test atom://invalid-route (non-existent screen) - should fallback or show error
   - Test atom://workflow/ (missing workflowId param) - should handle gracefully
   - Test atom://agent/ (missing agentId param) - should handle gracefully
   - Test atom://execution/invalid-id-format (malformed ID) - should handle gracefully
   - Test https://atom.ai/non-existent-path (invalid HTTPS deep link)
   - Test deep link with invalid protocol (ftp://atom.ai/workflows)
   - Test deep link with special characters in params (URL encoding issues)
   - Verify no crashes or unhandled errors
   - Verify fallback to default screen or error screen
   - Test deep link to screen user doesn't have access to (permission-based)

3. **Missing Required Params tests** (8-10 tests):
   - Test navigation to WorkflowDetail without workflowId (undefined)
   - Test navigation to ExecutionProgress without executionId (undefined)
   - Test navigation to AgentChat without agentId (undefined)
   - Verify navigation doesn't crash with undefined required params
   - Verify default value or error handling for missing params
   - Test navigation with null params (null vs undefined)
   - Test navigation with empty string params ('' vs undefined)

4. **Invalid Screen Names tests** (6-8 tests):
   - Test navigation.navigate('NonExistentScreen') - should be ignored or error
   - Test navigation.navigate('') - empty screen name
   - Test navigation.navigate(null) - null screen name
   - Test navigation.navigate(undefined) - undefined screen name
   - Verify no crashes, graceful handling
   - Test error boundary catches navigation errors

5. **Malformed URLs tests** (8-10 tests):
   - Test URL with invalid characters (atom://workflow/test@#$)
   - Test URL with double slashes (atom://workflow//test-id)
   - Test URL without protocol (workflow/test-id)
   - Test URL with invalid host (atom://unknown-host/workflows)
   - Test URL with query string parsing errors
   - Test URL with UTF-8 encoding issues
   - Verify Linking.parse() handles errors gracefully
   - Verify fallback behavior for unparseable URLs

6. **Navigation Error Boundary tests** (6-8 tests):
   - Test navigation error caught by error boundary
   - Test error screen renders after navigation error
   - Test retry mechanism after navigation error
   - Test error logging (console.error or error tracking)
   - Verify app doesn't crash on navigation errors
   - Test recovery after navigation error (navigate to valid screen)

7. **Type Mismatch Errors tests** (6-8 tests):
   - Test navigation with number param instead of string (workflowId: 123 vs '123')
   - Test navigation with object param instead of string
   - Test navigation with array param
   - Verify type coercion or error handling
   - Verify no crashes from type mismatches

8. **Deep Link Timeout tests** (4-6 tests):
   - Test deep link navigation timeout (very long URLs)
   - Test concurrent deep link navigation (multiple rapid deep links)
   - Test deep link during active navigation (race condition)
   - Verify graceful handling of timeouts

Total target: 40-60 tests, 300+ lines. Follow Phase 136 offlineSyncService.test.ts error handling pattern (9 error tests, clean error scenarios).

DO NOT:
- Expect navigation to succeed for invalid inputs (test error handling)
- Skip error scenarios (all error types must be tested)
- Test network errors (deep linking is local, no network required)
- Test React Navigation internal errors (focus on app-level error handling)
  </action>
  <verify>
Run: npm test -- NavigationErrors.test.tsx
Expect: 40-60 tests passing, all error scenarios handled gracefully
Run: npm test -- --coverage --collectCoverageFrom="mobile/src/navigation/AppNavigator.tsx,mobile/src/navigation/AuthNavigator.tsx"
Expect: Coverage report shows >80% for error handling code paths
  </verify>
  <done>
NavigationErrors.test.tsx has 40-60 passing tests covering invalid deep links, missing params, invalid screens, malformed URLs, error boundaries, type mismatches, and timeouts, coverage >80% for error handling, no crashes or unhandled errors
  </done>
</task>

</tasks>

<verification>
Overall phase verification:
1. Invalid deep links handled gracefully (no crashes, fallback behavior)
2. Missing required params handled (error or default value)
3. Invalid screen names handled (ignored or error screen)
4. Navigation errors don't crash the app (error boundaries, graceful degradation)
5. Malformed URLs handled (parse errors, invalid protocols)

Coverage verification:
- Run: npm test -- --coverage --collectCoverageFrom="mobile/src/navigation/AppNavigator.tsx,mobile/src/navigation/AuthNavigator.tsx"
- Expected: >80% statements for error handling code paths
</verification>

<success_criteria>
1. NavigationErrors.test.tsx created with 300+ lines, 40-60 tests
2. All error scenarios tested (invalid deep links, missing params, invalid screens, malformed URLs)
3. No crashes or unhandled errors in test output
4. Coverage >80% for error handling code paths
5. Fallback behavior documented (error screen, default screen, graceful degradation)
</success_criteria>

<output>
After completion, create `.planning/phases/137-mobile-navigation-testing/137-05-SUMMARY.md` with:
- Tests added (40-60 tests across 7 test suites)
- Coverage achieved for error handling (target: >80%)
- Error scenarios tested (8 categories)
- Fallback behavior documented
- Next steps for Plan 06 (Coverage verification and CI integration)
</output>
