---
phase: 137-mobile-navigation-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/src/__tests__/navigation/AppNavigator.test.tsx
  - mobile/src/__tests__/helpers/navigationMocks.ts
autonomous: true

must_haves:
  truths:
    - "All 5 tab screens render with functional screen components (not string mocks)"
    - "Tab navigation switching works between Workflows, Analytics, Agents, Chat, Settings"
    - "Stack navigation works for WorkflowDetail, AgentChat, ExecutionProgress screens"
    - "Tab icons render correctly (filled when active, outline when inactive)"
    - "Tab bar styling matches configuration (height 60, active tint #2196F3)"
  artifacts:
    - path: "mobile/src/__tests__/navigation/AppNavigator.test.tsx"
      provides: "Tab and stack navigation tests with functional screen mocks"
      min_lines: 500
      exports: ["describe('Tab Navigation')", "describe('Stack Navigation')", "describe('Tab Switching')"]
    - path: "mobile/src/__tests__/helpers/navigationMocks.ts"
      provides: "Functional screen mock components with testIDs"
      min_lines: 150
      exports: ["createMockScreen", "mockAllScreens"]
  key_links:
    - from: "mobile/src/__tests__/navigation/AppNavigator.test.tsx"
      to: "mobile/src/navigation/AppNavigator.tsx"
      via: "NavigationContainer wrapper with functional screen mocks"
      pattern: "NavigationContainer.*AppNavigator"
    - from: "mobile/src/__tests__/navigation/AppNavigator.test.tsx"
      to: "mobile/src/__tests__/helpers/navigationMocks.ts"
      via: "Functional screen mock imports"
      pattern: "from.*navigationMocks"
---

<objective>
Implement comprehensive React Navigation screen testing for AppNavigator with functional screen mocks and real navigation interactions.

**Purpose:** Replace placeholder string mocks (0% coverage) with functional component mocks and actual navigation tests, targeting 80% coverage for AppNavigator.tsx (294 lines).

**Output:**
- Enhanced AppNavigator.test.tsx (500+ lines) with tab navigation, stack navigation, and tab switching tests
- New navigationMocks.ts helper (150+ lines) with reusable functional screen mock factories
- Coverage: 80%+ for AppNavigator.tsx (up from ~0%)
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-mobile-navigation-testing/137-RESEARCH.md
@.planning/phases/136-mobile-device-features-testing/136-FINAL.md

# Phase 136 patterns to follow
@mobile/src/__tests__/helpers/deviceMocks.ts
@mobile/src/__tests__/helpers/testUtils.ts
@mobile/src/__tests__/services/cameraService.test.ts
</context>

<tasks>

<task type="auto">
  <name>Create navigation mock helpers with functional screen components</name>
  <files>mobile/src/__tests__/helpers/navigationMocks.ts</files>
  <action>
Create mobile/src/__tests__/helpers/navigationMocks.ts with functional screen mock factories:

1. **createMockScreen(screenName, testId)** factory function:
   ```typescript
   export const createMockScreen = (screenName: string, testId: string) => {
     const React = require('react');
     const { View } = require('react-native');

     return function MockScreen({ route, navigation }: any) {
       return React.createElement(View, {
         testID: testId || `screen-${screenName.toLowerCase()}`,
         screenName,
         routeParams: route?.params
       });
     };
   };
   ```

2. **mockAllScreens()** function to mock all screen imports with functional mocks:
   ```typescript
   export const mockAllScreens = () => {
     jest.mock('../../screens/workflows/WorkflowsListScreen', () => createMockScreen('WorkflowsList', 'workflows-list-screen'));
     jest.mock('../../screens/workflows/WorkflowDetailScreen', () => createMockScreen('WorkflowDetail', 'workflow-detail-screen'));
     // ... all other screens
   };
   ```

3. **Tab screen testIDs** matching AppNavigator tab names:
   - workflows-list-screen (WorkflowsTab)
   - analytics-dashboard-screen (AnalyticsTab)
   - agent-list-screen (AgentsTab)
   - chat-tab-screen (ChatTab)
   - settings-screen (SettingsTab)

4. **Stack screen testIDs** for nested navigation:
   - workflow-detail-screen (WorkflowDetail)
   - workflow-trigger-screen (WorkflowTrigger)
   - execution-progress-screen (ExecutionProgress)
   - workflow-logs-screen (WorkflowLogs)
   - agent-chat-screen (AgentChat)

Follow Phase 136 deviceMocks.ts pattern (789 lines) - export reusable factories, use consistent naming, add JSDoc comments.

DO NOT:
- Use string mocks (jest.mock('../Screen', () => 'Screen'))
- Create inline mocks in test files (centralize in navigationMocks.ts)
- Add routing logic to mocks (keep them simple functional components)
  </action>
  <verify>
Run: npm test -- AppNavigator.test.tsx --testNamePattern="should render.*screen"
Expect: Tests pass, all screen testIDs found in rendered output
  </verify>
  <done>
All screens mocked as functional components with unique testIDs, navigationMocks.ts exports reusable mock factories, testIDs match AppNavigator screen names for consistency
  </done>
</task>

<task type="auto">
  <name>Replace placeholder AppNavigator tests with functional navigation tests</name>
  <files>mobile/src/__tests__/navigation/AppNavigator.test.tsx</files>
  <action>
Replace mobile/src/__tests__/navigation/AppNavigator.test.tsx with comprehensive tests:

1. **Remove string mocks**, import from navigationMocks:
   ```typescript
   import { mockAllScreens } from '../helpers/navigationMocks';
   mockAllScreens();
   ```

2. **Tab Navigation tests** (15-20 tests):
   - Render all 5 tabs with unique testIDs
   - Verify tab labels (Workflows, Analytics, Agents, Chat, Settings)
   - Verify tab icons (Ionicons with correct names: flash, stats-chart, people, chatbubbles, settings)
   - Verify active tab styling (filled icons, active tint #2196F3)
   - Verify inactive tab styling (outline icons, inactive tint #999)
   - Verify tab bar configuration (height 60, padding 5)
   - Test tab switching with fireEvent.press on tab buttons
   - Use waitFor() for async navigation transitions

3. **Stack Navigation tests** (10-15 tests):
   - Render WorkflowStack with 5 screens (WorkflowsList, WorkflowDetail, WorkflowTrigger, ExecutionProgress, WorkflowLogs)
   - Render AnalyticsStack with AnalyticsDashboard
   - Render AgentStack with AgentList + AgentChat
   - Render ChatStack with ChatTab + AgentChat
   - Verify header styling (backgroundColor #2196F3, headerTintColor #fff)
   - Test modal presentation for WorkflowTrigger (presentation: 'modal')
   - Test headerShown: false for WorkflowsList, ChatTab, AnalyticsDashboard, AgentList

4. **Tab Switching tests** (8-10 tests):
   - Switch from Workflows to Analytics tab
   - Switch from Workflows to Agents tab
   - Switch from Agents to Chat tab
   - Switch from Chat to Settings tab
   - Verify screen changes using getByTestId for each tab's screen testID
   - Use waitFor() for async transitions (React Navigation has 200-300ms animations)

5. **Navigation State tests** (5-8 tests):
   - Verify initial route is WorkflowsTab (index 0)
   - Use useNavigationState hook to get current state
   - Test state object structure (routeNames array, routes array, index)
   - Test state preservation after tab switch (routes remain in history)

Total target: 40-50 tests, 500+ lines. Follow Phase 136 cameraService.test.ts pattern (8 test suites, 68 tests, 1,100+ lines).

DO NOT:
- Use string mocks for screens (use functional mocks from navigationMocks)
- Test internal navigation state directly (test via useNavigationState or getByTestId)
- Skip waitFor() for navigation transitions (causes flaky tests)
- Repeat the same assertion for all tabs (use data-driven tests or parameterized describes)
  </action>
  <verify>
Run: npm test -- AppNavigator.test.tsx
Expect: 40-50 tests passing, coverage >75% for AppNavigator.tsx
Run: npm test -- --coverage --collectCoverageFrom="mobile/src/navigation/AppNavigator.tsx"
Expect: Coverage report shows >75% statements, >75% lines
  </verify>
  <done>
AppNavigator.test.tsx has 40-50 passing tests covering tab navigation, stack navigation, tab switching, and navigation state, coverage >75% for AppNavigator.tsx, all tests use functional screen mocks with testIDs
  </done>
</task>

</tasks>

<verification>
Overall phase verification:
1. All 5 tab screens render with functional screen components (not string mocks)
2. Tab navigation switching works between all 5 tabs (Workflows, Analytics, Agents, Chat, Settings)
3. Stack navigation works for all nested screens (WorkflowDetail, AgentChat, ExecutionProgress, WorkflowLogs)
4. Tab icons render correctly (filled when active, outline when inactive)
5. Tab bar styling matches AppNavigator configuration

Coverage verification:
- Run: npm test -- --coverage --collectCoverageFrom="mobile/src/navigation/AppNavigator.tsx"
- Expected: >75% statements, >75% lines (Plan 02 will close remaining gap)
</verification>

<success_criteria>
1. AppNavigator.test.tsx expanded to 500+ lines with 40-50 tests
2. navigationMocks.ts helper created with 150+ lines of reusable mock factories
3. All screen mocks use functional components with testIDs (no string mocks)
4. Coverage >75% for AppNavigator.tsx (up from ~0%)
5. All tests pass with stable execution (no flaky waitFor issues)
</success_criteria>

<output>
After completion, create `.planning/phases/137-mobile-navigation-testing/137-01-SUMMARY.md` with:
- Tests added (40-50 tests across 4-5 test suites)
- Coverage achieved for AppNavigator.tsx (target: >75%)
- navigationMocks.ts helper created (150+ lines)
- Next steps for Plan 02 (AuthNavigator deep linking tests)
</output>
