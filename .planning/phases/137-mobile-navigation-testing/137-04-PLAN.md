---
phase: 137-mobile-navigation-testing
plan: 04
type: execute
wave: 2
depends_on: ["137-01", "137-02"]
files_modified:
  - mobile/src/__tests__/navigation/NavigationState.test.tsx
autonomous: true

must_haves:
  truths:
    - "Back stack maintained after navigating from WorkflowsList -> WorkflowDetail -> WorkflowTrigger"
    - "Tab state preserved when switching between tabs (scroll position, form data)"
    - "Navigation state structure verified (routes array, index, key, type)"
    - "Tab switching updates navigation state index correctly"
    - "Navigation reset works (back to root of stack, tab reset on logout)"
  artifacts:
    - path: "mobile/src/__tests__/navigation/NavigationState.test.tsx"
      provides: "Navigation state management tests (back stack, tab state, reset)"
      min_lines: 350
      exports: ["describe('Back Stack')", "describe('Tab State')", "describe('Navigation Reset')", "describe('State Structure')"]
  key_links:
    - from: "mobile/src/__tests__/navigation/NavigationState.test.tsx"
      to: "mobile/src/navigation/AppNavigator.tsx"
      via: "NavigationContainer state tracking"
      pattern: "useNavigationState|navigation.getState"
    - from: "mobile/src/__tests__/navigation/NavigationState.test.tsx"
      to: "mobile/src/navigation/AuthNavigator.tsx"
      via: "Auth flow state transitions"
      pattern: "navigation.reset|initialRouteName"
---

<objective>
Implement comprehensive navigation state management tests covering back stack, tab state preservation, navigation reset, and state structure validation.

**Purpose:** Test navigation state behavior to ensure proper back navigation, tab switching, and state reset functionality.

**Output:**
- New NavigationState.test.tsx (350+ lines) with navigation state tests
- Coverage: 80%+ for navigation state management logic
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-mobile-navigation-testing/137-RESEARCH.md
@.planning/phases/136-mobile-device-features-testing/136-FINAL.md

# Follow Plan 01-02 patterns
@.planning/phases/137-mobile-navigation-testing/137-01-PLAN.md
@.planning/phases/137-mobile-navigation-testing/137-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Create navigation state management tests for back stack and tab navigation</name>
  <files>mobile/src/__tests__/navigation/NavigationState.test.tsx</files>
  <action>
Create mobile/src/__tests__/navigation/NavigationState.test.tsx with comprehensive navigation state tests:

1. **Imports and setup**:
   ```typescript
   import React from 'react';
   import { render, fireEvent, waitFor } from '@testing-library/react-native';
   import { NavigationContainer, useNavigationState } from '@react-navigation/native';
   import AppNavigator from '../../navigation/AppNavigator';
   import { AuthNavigator } from '../../navigation/AuthNavigator';
   import { mockAllScreens } from '../helpers/navigationMocks';

   mockAllScreens();
   ```

2. **Back Stack tests** (12-15 tests):
   - Navigate from WorkflowsList -> WorkflowDetail, verify back stack has 2 routes
   - Navigate from WorkflowDetail -> WorkflowTrigger, verify back stack has 3 routes
   - Navigate from WorkflowTrigger -> ExecutionProgress, verify back stack has 4 routes
   - Test navigation.goBack() returns to previous screen
   - Test back stack after navigating AgentList -> AgentChat
   - Test back stack after multiple tab switches + stack navigation
   - Verify stack is independent per tab (WorkflowStack vs AgentStack)
   - Test back stack after deep link navigation to nested screen
   - Use useNavigationState hook to verify state.routes array length
   - Test state.index points to current route in routes array

3. **Tab State Preservation tests** (10-12 tests):
   - Navigate in Workflows tab (WorkflowsList -> WorkflowDetail)
   - Switch to Analytics tab
   - Switch back to Workflows tab, verify WorkflowDetail still visible
   - Test tab state preservation across all 5 tabs
   - Test tab state after deep link to different tab
   - Verify state.history preserved during tab switches
   - Test scroll position preservation (mock scroll position, verify after tab switch)
   - Test form data preservation (mock form state, verify after tab switch)

4. **Navigation State Structure tests** (8-10 tests):
   - Verify state object has required properties (key, index, routeNames, routes, history, stale, type)
   - Verify state.routes is array of route objects (key, name, params)
   - Verify state.index is number pointing to current route
   - Verify state.routeNames contains all registered route names
   - Verify state.type matches navigator type ('tab', 'stack')
   - Test state structure for TabNavigator (type: 'tab')
   - Test state structure for StackNavigator (type: 'stack')

5. **Navigation Reset tests** (8-10 tests):
   - Test navigation.reset() clears back stack
   - Test navigation.reset() returns to root of stack
   - Test tab reset after logout (all tabs reset to initial screen)
   - Test reset with NavigationContainer initialState prop
   - Test reset after deep link navigation
   - Verify state.routes.length === 1 after reset
   - Verify state.index === 0 after reset

6. **Tab Switching State tests** (8-10 tests):
   - Test state.index changes when switching tabs (0 -> 1 -> 2)
   - Test state.routes[0].name matches current tab screen name
   - Test state.routeNames stays constant during tab switches
   - Test state.stale === false after tab switch
   - Test tab switch updates state without affecting other tabs' state

7. **Deep Link Navigation State tests** (6-8 tests):
   - Test deep link to WorkflowDetail updates state with workflowId param
   - Test deep link to AgentChat updates state with agentId param
   - Test state.params populated from deep link URL
   - Test deep link navigation from tab context vs direct link
   - Test state after invalid deep link (fallback to default screen)

Total target: 50-70 tests, 350+ lines. Follow Phase 136 notificationService.test.ts pattern (11 test suites, 89 tests, 1,500+ lines) for state-related testing.

DO NOT:
- Test internal React Navigation state management (test via useNavigationState hook)
- Mock navigation state directly (use NavigationContainer to get real state)
- Skip waitFor() for state updates (navigation transitions are async)
- Test state persistence across app restarts (covered in Phase 138 AsyncStorage tests)
  </action>
  <verify>
Run: npm test -- NavigationState.test.tsx
Expect: 50-70 tests passing, all state assertions pass
Run: npm test -- --coverage --collectCoverageFrom="mobile/src/navigation/AppNavigator.tsx"
Expect: Coverage report shows >80% for state management paths
  </verify>
  <done>
NavigationState.test.tsx has 50-70 passing tests covering back stack, tab state, state structure, reset, and tab switching, coverage >80% for navigation state management, all tests use useNavigationState hook for real state access
  </done>
</task>

</tasks>

<verification>
Overall phase verification:
1. Back stack maintained correctly after stack navigation (2-4 routes)
2. Tab state preserved when switching tabs (scroll position, form data, nested navigation)
3. Navigation state structure validated (key, index, routeNames, routes, type)
4. Tab switching updates state.index correctly
5. Navigation reset works (clears back stack, resets to root)

Coverage verification:
- Run: npm test -- --coverage --collectCoverageFrom="mobile/src/navigation/AppNavigator.tsx,mobile/src/navigation/AuthNavigator.tsx"
- Expected: >80% statements for state management code paths
</verification>

<success_criteria>
1. NavigationState.test.tsx created with 350+ lines, 50-70 tests
2. Back stack tests verify 2-4 route navigation chains
3. Tab state tests verify preservation across all 5 tabs
4. Navigation reset tests verify stack clearing and root return
5. Coverage >80% for navigation state management code paths
6. All tests pass with stable execution (no flaky state access issues)
</success_criteria>

<output>
After completion, create `.planning/phases/137-mobile-navigation-testing/137-04-SUMMARY.md` with:
- Tests added (50-70 tests across 6 test suites)
- Coverage achieved for navigation state management (target: >80%)
- Back stack behavior verified (multi-level navigation)
- Tab state preservation verified (all 5 tabs)
- Next steps for Plan 05 (Navigation error handling tests)
</output>
