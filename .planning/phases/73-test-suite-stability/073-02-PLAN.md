---
phase: 073-test-suite-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_host_shell_service.py
  - backend/tests/test_minimal_app.py
  - backend/tests/test_social_graduation_integration.py
  - backend/tests/unit/test_workflow_parameter_validator.py
  - backend/tests/conftest.py
autonomous: true

must_haves:
  truths:
    - "All hardcoded resource IDs replaced with unique_resource_name"
    - "Tests can run in parallel without ID collisions"
    - "No 'test-agent', 'test_user' hardcoded strings remain"
    - "Random-order test run executed to identify hidden test dependencies"
  artifacts:
    - path: "backend/tests/conftest.py"
      provides: "unique_resource_name fixture for parallel execution"
      contains: "def unique_resource_name"
    - path: "backend/tests/docs/TEST_ISOLATION_PATTERNS.md"
      provides: "Documentation for isolation patterns"
      contains: "unique_resource_name"
  key_links:
    - from: "backend/tests/unit/test_host_shell_service.py"
      to: "conftest.py unique_resource_name"
      via: "fixture parameter"
      pattern: "unique_resource_name"
    - from: "pytest --random-order command"
      to: "flaky test detection"
      via: "pytest-random-order plugin"
      pattern: "--random-order"
---

<objective>
Replace hardcoded test resource IDs with unique_resource_name fixture to enable parallel execution without collisions.

This plan addresses one of the most common causes of flaky tests: hardcoded resource identifiers that cause IntegrityError and duplicate key violations when tests run in parallel with pytest-xdist.

Purpose: Enable parallel test execution by eliminating resource ID collisions
Output: Tests using dynamic unique IDs, ready for parallel execution
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/073-test-suite-stability/073-RESEARCH.md
@backend/tests/conftest.py
@backend/tests/docs/TEST_ISOLATION_PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded IDs in test_host_shell_service.py</name>
  <files>backend/tests/unit/test_host_shell_service.py</files>
  <action>
    1. Read the file and identify all hardcoded "test-agent" strings
    2. Add `unique_resource_name` fixture parameter to each test function that uses "test-agent"
    3. Replace all `agent_id="test-agent"` with `agent_id=unique_resource_name`
    4. Also check for any other hardcoded IDs like "test_user", "test-workflow"
    5. Run the tests to verify they still pass: `pytest tests/unit/test_host_shell_service.py -v`

    Pattern: Replace string literals with fixture parameter
    Before: `agent_id="test-agent"`
    After: `agent_id=unique_resource_name`
  </action>
  <verify>pytest tests/unit/test_host_shell_service.py -v 2>&1 | tail -10</verify>
  <done>
    - All hardcoded "test-agent" strings replaced
    - Tests still pass (or fail for non-ID-related reasons)
    - unique_resource_name fixture added to all affected test signatures
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded IDs in test_minimal_app.py</name>
  <files>backend/tests/test_minimal_app.py</files>
  <action>
    1. Read the file and find hardcoded "test_user" strings
    2. Add `unique_resource_name` fixture parameter to affected tests
    3. Replace `user_id="test_user"` with `user_id=unique_resource_name`
    4. Replace `search_documents(..., "test_user", ...)` with `search_documents(..., unique_resource_name, ...)`
    5. Run tests to verify: `pytest tests/test_minimal_app.py -v -k "test_"`

    Note: This is an integration test file, may have async markers
  </action>
  <verify>pytest tests/test_minimal_app.py -v -k "test_" 2>&1 | tail -10</verify>
  <done>
    - All hardcoded "test_user" strings replaced with unique_resource_name
    - Tests run without collision errors
    - No duplicate key violations
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace hardcoded IDs in graduation and workflow tests</name>
  <files>backend/tests/test_social_graduation_integration.py, backend/tests/unit/test_workflow_parameter_validator.py</files>
  <action>
    1. In test_social_graduation_integration.py:
       - Replace "test-agent-intern-grad", "test-agent-supervised-grad", "test-agent-student-grad", "test-agent-autonomous"
       - Use unique_resource_name for each (call it multiple times if needed, or append suffixes)
    2. In test_workflow_parameter_validator.py:
       - Replace the hardcoded "test_user" in the test
       - Add unique_resource_name fixture
    3. Run tests to verify: `pytest tests/test_social_graduation_integration.py tests/unit/test_workflow_parameter_validator.py -v`

    For graduation tests with multiple agents, use suffixes:
    - intern_id = f"{unique_resource_name}_intern"
    - supervised_id = f"{unique_resource_name}_supervised"
  </action>
  <verify>pytest tests/test_social_graduation_integration.py tests/unit/test_workflow_parameter_validator.py -v 2>&1 | tail -15</verify>
  <done>
    - Graduation tests use dynamic agent IDs with suffixes
    - Workflow validator test uses unique user ID
    - No ID collision errors in test output
  </done>
</task>

<task type="auto">
  <name>Task 4: Run proactive random-order test for flaky detection</name>
  <files>backend/tests</files>
  <action>
    1. Verify pytest-random-order is installed: `pip show pytest-random-order`
    2. Run a subset of tests with random order to detect hidden dependencies:
       `pytest tests/unit/test_host_shell_service.py tests/test_minimal_app.py --random-order -v`
    3. Look for tests that fail in random order but pass in normal order
    4. Document any flaky tests discovered - these indicate hidden dependencies
    5. This is a PROACTIVE check before the full random-order run in plan 05

    Hidden dependencies often appear as:
    - Tests that assume database state from previous tests
    - Tests that rely on global variables modified by other tests
    - Tests with improper fixture scope (session vs function)
  </action>
  <verify>pytest tests/unit/test_host_shell_service.py tests/test_minimal_app.py --random-order -v 2>&1 | grep -E "passed|failed|error"</verify>
  <done>
    - Random-order test run completed
    - Any order-dependent failures documented
    - Hidden dependencies identified (if any)
    - Results recorded for flaky test tracking
  </done>
</task>

<task type="auto">
  <name>Task 5: Scan for remaining hardcoded test data patterns</name>
  <files>backend/tests</files>
  <action>
    1. Scan for remaining patterns: `grep -r '"test-agent"\|'"'test-agent'" backend/tests --include="*.py"`
    2. Scan for other common patterns: `grep -r 'id="test_\|id='"'test_" backend/tests --include="*.py" | head -20`
    3. Document findings in SUMMARY.md - we won't fix all in this plan, just identify
    4. Focus on the most critical patterns that cause parallel execution failures
    5. The research indicated 754 test files - we're prioritizing high-impact fixes

    Target: Identify top 10 files with hardcoded IDs for potential follow-up work
  </action>
  <verify>grep -r '"test-agent"\|'"'test-agent'" backend/tests --include="*.py" 2>&1 | wc -l</verify>
  <done>
    - Scan complete with documented findings
    - Critical files in this plan fixed
    - Remaining issues documented for follow-up
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
1. Tests in modified files use unique_resource_name fixture
2. No hardcoded "test-agent" in test_host_shell_service.py
3. No hardcoded "test_user" in test_minimal_app.py (for user IDs)
4. Tests can be collected and run
</verification>

<success_criteria>
1. All modified tests use dynamic resource IDs
2. Tests still pass (failures not related to ID collisions)
3. Documented remaining hardcoded ID patterns for follow-up
4. Ready to run parallel execution tests in plan 73-05
</success_criteria>

<output>
After completion, create `.planning/phases/073-test-suite-stability/073-02-SUMMARY.md`
</output>
