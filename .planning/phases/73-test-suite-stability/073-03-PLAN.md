---
phase: 073-test-suite-stability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pytest.ini
  - backend/requirements-testing.txt
autonomous: true

must_haves:
  truths:
    - "pytest-xdist installed and configured for parallel execution"
    - "pytest-random-order installed for flaky test detection"
    - "pytest-rerunfailures configured for automatic retry"
    - "pytest-timeout configured to prevent hung tests"
  artifacts:
    - path: "backend/pytest.ini"
      provides: "Pytest configuration with parallel execution settings"
      contains: "addopts = .*-n auto"
    - path: "backend/requirements-testing.txt"
      provides: "Testing dependencies"
      contains: "pytest-xdist"
  key_links:
    - from: "pytest.ini"
      to: "pytest-xdist plugin"
      via: "-n auto flag"
      pattern: "-n auto"
---

<objective>
Configure pytest for parallel execution with xdist and enable flaky test detection tools.

This plan sets up the infrastructure for parallel test execution and flaky test detection. The research shows pytest-xdist can achieve 50-80% execution time reduction, and pytest-random-order helps identify hidden test dependencies.

Purpose: Enable parallel test execution and flaky test detection
Output: Configured pytest with xdist, random-order, and timeout support
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/073-test-suite-stability/073-RESEARCH.md
@backend/pytest.ini
@backend/requirements-testing.txt
@backend/tests/docs/TEST_ISOLATION_PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and install pytest-xdist and related packages</name>
  <files>backend/requirements-testing.txt</files>
  <action>
    1. Check if pytest-xdist is installed: `pip show pytest-xdist`
    2. Check if pytest-random-order is installed: `pip show pytest-random-order`
    3. Check if pytest-rerunfailures is installed: `pip show pytest-rerunfailures`
    4. Check if pytest-timeout is installed: `pip show pytest-timeout`
    5. If any are missing, install them: `pip install pytest-xdist pytest-random-order pytest-rerunfailures pytest-timeout`
    6. Verify pytest-xdist version is >=3.6.0 for optimal pytest-cov integration

    The research indicates all these should already be in requirements-testing.txt
  </action>
  <verify>pip show pytest-xdist pytest-random-order pytest-rerunfailures pytest-timeout 2>&1 | grep -E "Name:|Version:"</verify>
  <done>
    - pytest-xdist installed (version >=3.6.0)
    - pytest-random-order installed
    - pytest-rerunfailures installed
    - pytest-timeout installed
  </done>
</task>

<task type="auto">
  <name>Task 2: Update pytest.ini for parallel execution configuration</name>
  <files>backend/pytest.ini</files>
  <action>
    1. Read current pytest.ini and note the existing addopts line (line 56)
    2. Add xdist configuration: `-n auto --dist=loadscope`
       - `-n auto`: Auto-detect CPU cores
       - `--dist=loadscope`: Group tests by module (better for shared fixtures)
    3. Add timeout configuration: `--timeout=300` (5 minutes per test max)
    4. DO NOT add random-order to addopts (it's for detection only, not default)
    5. DO NOT add reruns to addopts (retries mask flaky tests)

    Updated addopts should include (keeping existing options):
    `-q --strict-markers --tb=line ... -n auto --dist=loadscope --timeout=300`

    Preserve all existing ignores, coverage options, and markers.
  </action>
  <verify>grep "addopts" backend/pytest.ini | grep -E "n auto|loadscope|timeout"</verify>
  <done>
    - pytest.ini updated with `-n auto --dist=loadscope --timeout=300`
    - All existing options preserved
    - File is valid INI format
  </done>
</task>

<task type="auto">
  <name>Task 3: Create parallel execution test script</name>
  <files>backend/scripts/test_parallel.sh</files>
  <action>
    1. Create directory if needed: `mkdir -p backend/scripts`
    2. Create test_parallel.sh script with:
       - Sequential test run command
       - Parallel test run command with time comparison
       - Random order test command for flaky detection
    3. Script should be executable: `chmod +x backend/scripts/test_parallel.sh`
    4. Include commands for:
       - `pytest -n auto --dist=loadscope -v` (parallel)
       - `pytest --random-order -v` (random order for flaky detection)
       - `pytest --reruns 3 -v` (rerun for flaky detection)
    5. Add comments explaining each command's purpose

    Script content:
    ```bash
    #!/bin/bash
    # Test suite stability verification script

    echo "=== Sequential Baseline ==="
    time pytest -v

    echo "=== Parallel Execution ==="
    time pytest -n auto --dist=loadscope -v

    echo "=== Random Order (Flaky Detection) ==="
    time pytest --random-order -v
    ```
  </action>
  <verify>cat backend/scripts/test_parallel.sh | head -20</verify>
  <done>
    - Script created at backend/scripts/test_parallel.sh
    - Script is executable (chmod +x)
    - Contains sequential, parallel, and random-order test commands
  </done>
</task>

<task type="auto">
  <name>Task 4: Run baseline parallel execution test</name>
  <files>backend/pytest.ini</files>
  <action>
    1. Run a small subset of tests in parallel to verify xdist works
    2. Command: `pytest tests/unit/test_host_shell_service.py -n auto --dist=loadscope -v`
    3. Verify worker IDs appear in output (gw0, gw1, etc.)
    4. Check that tests complete (may pass or fail, but must not crash)
    5. Document the time difference vs sequential

    We're using a subset to quickly verify configuration before full runs in later plans.
  </action>
  <verify>pytest tests/unit/test_host_shell_service.py -n auto --dist=loadscope -v 2>&1 | grep -E "gw[0-9]|passed|failed"</verify>
  <done>
    - Parallel execution runs without errors
    - Worker IDs (gw0, gw1, etc.) appear in output
    - Tests complete (even if some fail)
    - Time is recorded for comparison
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
1. pytest.ini contains `-n auto --dist=loadscope --timeout=300`
2. All required packages installed (pytest-xdist, pytest-random-order, pytest-rerunfailures, pytest-timeout)
3. test_parallel.sh script exists and is executable
4. Parallel test run completes successfully
</verification>

<success_criteria>
1. Parallel execution configured and working
2. Test script created for stability verification
3. Baseline timing documented
4. Ready to run full parallel test suite in plan 73-05
</success_criteria>

<output>
After completion, create `.planning/phases/073-test-suite-stability/073-03-SUMMARY.md`
</output>
