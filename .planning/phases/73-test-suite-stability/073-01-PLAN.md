---
phase: 073-test-suite-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/conftest.py
  - backend/tests/fixtures/__init__.py
  - backend/tests/unit/test_episode_segmentation_service.py
autonomous: true

must_haves:
  truths:
    - "Test collection completes without ModuleNotFoundError"
    - "All fixtures can be imported successfully"
    - "Tests run and collect with pytest --collect-only"
  artifacts:
    - path: "backend/tests/conftest.py"
      provides: "Root test fixtures and configuration"
      contains: "import tests.fixtures.mock_services"
    - path: "backend/tests/fixtures/__init__.py"
      provides: "Fixtures module initialization"
      contains: "MockLLMProvider, MockEmbeddingService"
  key_links:
    - from: "backend/tests/conftest.py"
      to: "tests.fixtures.mock_services"
      via: "from tests.fixtures.mock_services import"
      pattern: "from tests.fixtures.mock_services import"
---

<objective>
Fix critical ModuleNotFoundError that blocks pytest collection and stabilize fixture imports.

This plan addresses the blocking issue identified in RESEARCH.md: "ModuleNotFoundError: No module named 'tests.fixtures'" which prevents any test execution. The fix involves ensuring proper module structure and export paths for all fixtures.

Purpose: Unblock all testing work by resolving collection errors
Output: Working pytest collection, all fixtures importable
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/073-test-suite-stability/073-RESEARCH.md
@backend/tests/conftest.py
@backend/tests/fixtures/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ModuleNotFoundError in fixture imports</name>
  <files>backend/tests/conftest.py, backend/tests/fixtures/__init__.py</files>
  <action>
    1. Run `pytest --collect-only -q` to verify current ModuleNotFoundError
    2. Update `backend/tests/fixtures/__init__.py` to export all fixture classes explicitly:
       - Add: from .mock_services import MockLLMProvider, MockEmbeddingService, MockStorageService, MockCacheService, MockWebSocket
    3. Verify the imports in conftest.py (line 45-48) match the exports
    4. Run `pytest --collect-only -q` again to confirm collection succeeds
    5. The unique_resource_name fixture is already implemented correctly (lines 234-246)

    Do NOT change the import structure or move files - only add exports to __init__.py
  </action>
  <verify>pytest --collect-only -q 2>&1 | head -20</verify>
  <done>
    - pytest collection completes without ModuleNotFoundError
    - All mock service classes are importable from tests.fixtures.mock_services
    - Collection shows at least 500+ tests collected (not an error)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify fixture import isolation</name>
  <files>backend/tests/conftest.py, backend/tests/fixtures/__init__.py</files>
  <action>
    1. Run a quick subset of tests to verify fixtures work: `pytest tests/unit/test_episode_segmentation_service.py -v`
    2. Verify the db_session, unique_resource_name, and mock_llm_response fixtures all load
    3. Check that no import errors appear in the test output
    4. If tests fail for other reasons (not imports), that's acceptable - we're only fixing imports
    5. Document any remaining import issues in the plan summary

    The research shows fixtures are well-implemented. We're ensuring they're all accessible.
  </action>
  <verify>pytest tests/unit/test_episode_segmentation_service.py -v 2>&1 | head -30</verify>
  <done>
    - Tests run without import errors
    - Fixtures are accessible from tests.fixtures
    - Any test failures are NOT due to ModuleNotFoundError
  </done>
</task>

<task type="auto">
  <name>Task 3: Run baseline collection and document test count</name>
  <files>backend/tests/conftest.py</files>
  <action>
    1. Run full test collection: `pytest --collect-only -q` from backend directory
    2. Count collected tests with: `pytest --collect-only -q | grep -E "test session starts|collected"`
    3. Save collection output to a temporary file for reference
    4. Document the baseline test count in the SUMMARY.md

    This establishes our baseline before optimization work in subsequent plans.
  </action>
  <verify>pytest --collect-only -q 2>&1 | tail -5</verify>
  <done>
    - Full test collection completes without errors
    - Test count is documented (expecting 700-900+ tests)
    - No ModuleNotFoundError appears in output
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
1. `pytest --collect-only` completes without ModuleNotFoundError
2. Fixture imports work: `python -c "from tests.fixtures.mock_services import MockLLMProvider"`
3. At least 500 tests can be collected
4. No import errors in conftest.py
</verification>

<success_criteria>
1. Pytest collection completes successfully (0 errors)
2. All mock service classes are importable
3. Baseline test count is documented
4. Ready to proceed with flaky test identification in plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/073-test-suite-stability/073-01-SUMMARY.md`
</output>
