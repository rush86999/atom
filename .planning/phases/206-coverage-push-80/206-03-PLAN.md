---
phase: 206-coverage-push-80
plan: 03
type: execute
wave: 2
depends_on: ["206-01"]
files_modified:
  - backend/tests/core/governance/test_agent_context_resolver_coverage.py
  - backend/tests/core/llm/test_byok_handler_coverage.py
autonomous: true

must_haves:
  truths:
    - "Agent context resolver tests cover agent resolution, context extraction, and permission-based filtering"
    - "BYOK handler tests cover provider selection, streaming responses, cost optimization, and cognitive tier routing"
    - "Both files now appear in coverage report (expanded to 6+ files total)"
    - "Test collection shows zero errors for new tests"
    - "Coverage increases toward 80% target (2-3pp cumulative gain expected)"
  artifacts:
    - path: "backend/tests/core/governance/test_agent_context_resolver_coverage.py"
      provides: "AgentContextResolver test coverage"
      exports: ["test_resolve_agent_by_id", "test_extract_context", "test_filter_context_by_permissions"]
    - path: "backend/tests/core/llm/test_byok_handler_coverage.py"
      provides: "BYOKHandler comprehensive test coverage"
      exports: ["test_provider_selection_openai", "test_streaming_response", "test_cost_optimization", "test_cognitive_tier_routing"]
  key_links:
    - from: "backend/tests/core/governance/test_agent_context_resolver_coverage.py"
      to: "core.agent_context_resolver.AgentContextResolver"
      via: "direct import and testing"
      pattern: "from core.agent_context_resolver import AgentContextResolver"
    - from: "backend/tests/core/llm/test_byok_handler_coverage.py"
      to: "core.llm.byok_handler.BYOKHandler"
      via: "direct import and testing"
      pattern: "from core.llm.byok_handler import BYOKHandler"
---

<objective>
Create comprehensive test coverage for agent context resolver and BYOK (Bring Your Own Key) handler. These are critical infrastructure files for agent operations and LLM integration, representing ~1900 statements of core functionality.

Purpose: Expand coverage base by adding context resolution and LLM handler tests. The BYOK handler is particularly important as it manages multi-provider LLM routing, cost optimization, and cognitive tier system.

Output: Two comprehensive test files with 50-60 tests covering agent resolution, context extraction, provider selection, streaming, and cognitive tier routing.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
@.planning/phases/206-coverage-push-80/206-02-SUMMARY.md
</execution_context>

<context>
@.planning/phases/206-coverage-push-80/206-RESEARCH.md
@.planning/phases/204-coverage-push-75-80/204-05-SUMMARY.md
@core/agent_context_resolver.py
@core/llm/byok_handler.py

# AsyncMock Pattern from Phase 205
# Use AsyncMock for async methods, patch at usage location

# BYOK Research Finding
# "Multi-provider support (OpenAI, Anthropic, DeepSeek, Gemini)"
# "Cognitive tier system for cost optimization"
# "Token-by-token streaming via WebSocket"
</context>

<tasks>

<task type="auto">
  <name>Create AgentContextResolver comprehensive test coverage</name>
  <files>backend/tests/core/governance/test_agent_context_resolver_coverage.py</files>
  <action>
    Create test_agent_context_resolver_coverage.py with comprehensive coverage:

    ```python
    """AgentContextResolver comprehensive coverage tests."""
    import pytest
    from unittest.mock import Mock, patch, AsyncMock
    from sqlalchemy.orm import Session

    from core.agent_context_resolver import AgentContextResolver
    from core.models import AgentRegistry, MaturityLevel


    @pytest.fixture
    def mock_db():
        """Mock database session."""
        return Mock(spec=Session)


    @pytest.fixture
    def resolver(mock_db):
        """AgentContextResolver fixture."""
        return AgentContextResolver(mock_db)


    class TestAgentResolution:
        """Test agent resolution by ID, name, and type."""

        def test_resolve_agent_by_id_found(self, resolver, mock_db):
            """Resolve agent by ID when agent exists."""
            mock_agent = Mock(spec=AgentRegistry)
            mock_agent.agent_id = "agent-123"
            mock_agent.name = "Test Agent"
            mock_agent.maturity_level = MaturityLevel.INTERN

            mock_db.query.return_value.filter.return_value.first.return_value = mock_agent

            result = resolver.resolve_agent("agent-123")

            assert result is not None
            assert result.agent_id == "agent-123"
            assert result.maturity_level == MaturityLevel.INTERN

        def test_resolve_agent_by_id_not_found(self, resolver, mock_db):
            """Return None when agent ID not found."""
            mock_db.query.return_value.filter.return_value.first.return_value = None

            result = resolver.resolve_agent("nonexistent")

            assert result is None

        def test_resolve_agent_by_name(self, resolver, mock_db):
            """Resolve agent by name."""
            mock_agent = Mock(spec=AgentRegistry)
            mock_agent.agent_id = "agent-456"
            mock_agent.name = "Analytics Agent"

            mock_db.query.return_value.filter.return_value.filter.return_value.first.return_value = mock_agent

            result = resolver.resolve_agent_by_name("Analytics Agent")

            assert result is not None
            assert result.agent_id == "agent-456"

        def test_resolve_agent_by_type(self, resolver, mock_db):
            """Resolve agents by type."""
            mock_agents = [
                Mock(spec=AgentRegistry, agent_id="agent-1", agent_type="analytics"),
                Mock(spec=AgentRegistry, agent_id="agent-2", agent_type="analytics")
            ]

            mock_db.query.return_value.filter.return_value.all.return_value = mock_agents

            results = resolver.resolve_agents_by_type("analytics")

            assert len(results) == 2
            assert all(a.agent_type == "analytics" for a in results)


    class TestContextExtraction:
        """Test context extraction from agent configuration."""

        def test_extract_base_context(self, resolver):
            """Extract base context from agent configuration."""
            agent_config = {
                "agent_id": "agent-123",
                "name": "Test Agent",
                "capabilities": ["read", "write"],
                "permissions": ["canvas:present", "data:read"]
            }

            context = resolver.extract_context(agent_config)

            assert context["agent_id"] == "agent-123"
            assert "capabilities" in context
            assert "permissions" in context

        def test_extract_workflow_context(self, resolver):
            """Extract workflow-specific context."""
            agent_config = {
                "agent_id": "workflow-agent",
                "workflow_id": "wf-123",
                "workflow_template": "data_processing",
                "step_id": "step-456"
            }

            context = resolver.extract_workflow_context(agent_config)

            assert context["workflow_id"] == "wf-123"
            assert context["step_id"] == "step-456"

        def test_extract_user_context(self, resolver):
            """Extract user-specific context for agent."""
            user_context = resolver.extract_user_context(
                user_id="user-123",
                workspace_id="ws-456"
            )

            assert user_context["user_id"] == "user-123"
            assert user_context["workspace_id"] == "ws-456"
            assert "permissions" in user_context


    class TestPermissionFiltering:
        """Test permission-based context filtering."""

        def test_filter_context_by_maturity_student(self, resolver):
            """Filter context for STUDENT maturity (read-only)."""
            full_context = {
                "capabilities": ["read", "write", "delete", "execute"],
                "permissions": ["canvas:present", "data:write", "data:delete"]
            }

            filtered = resolver.filter_context_by_maturity(
                full_context,
                MaturityLevel.STUDENT
            )

            # STUDENT only gets read and present
            assert "read" in filtered["capabilities"]
            assert "write" not in filtered["capabilities"]
            assert "delete" not in filtered["capabilities"]
            assert "canvas:present" in filtered["permissions"]
            assert "data:write" not in filtered["permissions"]

        def test_filter_context_by_maturity_autonomous(self, resolver):
            """AUTONOMOUS agents get full context."""
            full_context = {
                "capabilities": ["read", "write", "delete", "execute"],
                "permissions": ["canvas:present", "data:write", "data:delete"]
            }

            filtered = resolver.filter_context_by_maturity(
                full_context,
                MaturityLevel.AUTONOMOUS
            )

            # AUTONOMOUS gets everything
            assert filtered == full_context

        def test_filter_context_removes_sensitive_data(self, resolver):
            """Remove sensitive data like API keys from context."""
            context_with_secrets = {
                "agent_id": "agent-123",
                "api_key": "sk-12345",
                "webhook_url": "https://example.com/hook",
                "capabilities": ["read"]
            }

            filtered = resolver.filter_sensitive_data(context_with_secrets)

            assert "api_key" not in filtered
            assert filtered.get("api_key") == "***REDACTED***"
            assert "capabilities" in filtered  # Non-sensitive data preserved


    class TestContextValidation:
        """Test context validation."""

        def test_validate_context_missing_required_fields(self, resolver):
            """Reject context missing required fields."""
            invalid_context = {
                "agent_id": "agent-123"
                # Missing: capabilities, permissions
            }

            is_valid, errors = resolver.validate_context(invalid_context)

            assert is_valid is False
            assert len(errors) > 0

        def test_validate_context_valid(self, resolver):
            """Accept valid context."""
            valid_context = {
                "agent_id": "agent-123",
                "capabilities": ["read"],
                "permissions": ["data:read"],
                "maturity_level": MaturityLevel.INTERN
            }

            is_valid, errors = resolver.validate_context(valid_context)

            assert is_valid is True
            assert len(errors) == 0


    class TestContextCaching:
        """Test context caching for performance."""

        def test_cache_agent_context(self, resolver):
            """Cache agent context for repeated access."""
            agent = Mock(spec=AgentRegistry)
            agent.agent_id = "agent-cache"
            agent.name = "Cache Test"

            # First call - cache miss
            context1 = resolver.get_cached_context("agent-cache")
            assert context1 is None

            # Set cache
            resolver.set_cached_context("agent-cache", {"name": "Cached"})

            # Second call - cache hit
            context2 = resolver.get_cached_context("agent-cache")
            assert context2 == {"name": "Cached"}

        def test_invalidate_context_cache(self, resolver):
            """Invalidate cached context on agent update."""
            resolver.set_cached_context("agent-123", {"name": "Old"})

            resolver.invalidate_cache("agent-123")

            cached = resolver.get_cached_context("agent-123")
            assert cached is None
    ```

    Create 25-30 tests covering:
    - Agent resolution (by ID, name, type)
    - Context extraction (base, workflow, user)
    - Permission filtering (by maturity level)
    - Sensitive data filtering
    - Context validation
    - Context caching
  </action>
  <verify>
    Run: python3 -m pytest backend/tests/core/governance/test_agent_context_resolver_coverage.py -v
    Expected: 25-30 tests pass, 0 collection errors
  </verify>
  <done>
    AgentContextResolver coverage complete: 25-30 tests covering resolution, extraction, filtering, validation, caching
  </done>
</task>

<task type="auto">
  <name>Create BYOKHandler comprehensive test coverage</name>
  <files>backend/tests/core/llm/test_byok_handler_coverage.py</files>
  <action>
    Create test_byok_handler_coverage.py with comprehensive coverage:

    ```python
    """BYOKHandler comprehensive coverage tests."""
    import pytest
    from unittest.mock import Mock, patch, AsyncMock, MagicMock
    from core.llm.byok_handler import BYOKHandler, LLMProvider, CognitiveTier
    from core.models import MaturityLevel


    @pytest.fixture
    def mock_config():
        """Mock LLM configuration."""
        return {
            "providers": {
                "openai": {"api_key": "sk-test", "enabled": True},
                "anthropic": {"api_key": "sk-ant-test", "enabled": True},
                "deepseek": {"api_key": "sk-ds-test", "enabled": True}
            },
            "default_provider": "openai"
        }


    @pytest.fixture
    def byok_handler(mock_config):
        """BYOKHandler fixture."""
        return BYOKHandler(mock_config)


    class TestProviderSelection:
        """Test LLM provider selection logic."""

        def test_select_provider_default(self, byok_handler):
            """Select default provider when no preference specified."""
            provider = byok_handler.select_provider()

            assert provider == LLMProvider.OPENAI

        def test_select_provider_by_request(self, byok_handler):
            """Select provider specified in request."""
            provider = byok_handler.select_provider(preferred_provider="anthropic")

            assert provider == LLMProvider.ANTHROPIC

        def test_select_provider_fallback_on_disabled(self, byok_handler):
            """Fallback to default when requested provider is disabled."""
            byok_handler.config["providers"]["anthropic"]["enabled"] = False

            provider = byok_handler.select_provider(preferred_provider="anthropic")

            assert provider == LLMProvider.OPENAI  # Fallback to default

        def test_select_provider_by_maturity(self, byok_handler):
            """Select provider based on agent maturity level."""
            # STUDENT gets faster/cheaper provider
            provider = byok_handler.select_provider_by_maturity(MaturityLevel.STUDENT)

            assert provider in [LLMProvider.OPENAI, LLMProvider.DEEPSEEK]

        def test_select_provider_by_cost_tier(self, byok_handler):
            """Select provider based on cognitive cost tier."""
            provider = byok_handler.select_provider_by_tier(CognitiveTier.MICRO)

            assert provider == LLMProvider.DEEPSEEK  # Cheapest

        def test_select_provider_for_complex_task(self, byok_handler):
            """Select premium provider for complex tasks."""
            provider = byok_handler.select_provider_by_tier(CognitiveTier.COMPLEX)

            assert provider in [LLMProvider.ANTHROPIC, LLMProvider.OPENAI]


    class TestStreamingResponse:
        """Test streaming LLM responses."""

        @pytest.mark.asyncio
        async def test_streaming_response_openai(self, byok_handler):
            """Stream response from OpenAI provider."""
            with patch('core.llm.byok_handler.openai.AsyncOpenAI') as mock_openai:
                mock_response = AsyncMock()
                mock_response.__aiter__ = AsyncMock(return_value=iter([
                    Mock(choices=[Mock(delta=Mock(content="Hello"))]),
                    Mock(choices=[Mock(delta=Mock(content=" world"))]),
                    Mock(choices=[Mock(delta=Mock(content=""))])  # End
                ]))

                mock_client = AsyncMock()
                mock_client.chat.completions.create.return_value = mock_response
                mock_openai.return_value = mock_client

                chunks = []
                async for chunk in byok_handler.stream_response(
                    messages=[{"role": "user", "content": "Hi"}],
                    provider=LLMProvider.OPENAI
                ):
                    chunks.append(chunk)

                assert "Hello" in "".join(chunks)

        @pytest.mark.asyncio
        async def test_streaming_response_anthropic(self, byok_handler):
            """Stream response from Anthropic provider."""
            with patch('core.llm.byok_handler.anthropic.Anthropic') as mock_anthropic:
                mock_response = AsyncMock()
                mock_response.__aiter__ = AsyncMock(return_value=iter([
                    Mock(delta=Mock(text="Hello")),
                    Mock(delta=Mock(text=" world")),
                    Mock(delta=Mock(text=""))
                ]))

                mock_client = AsyncMock()
                mock_client.messages.stream.return_value = mock_response
                mock_anthropic.return_value = mock_client

                chunks = []
                async for chunk in byok_handler.stream_response(
                    messages=[{"role": "user", "content": "Hi"}],
                    provider=LLMProvider.ANTHROPIC
                ):
                    chunks.append(chunk)

                assert "Hello" in "".join(chunks)


    class TestCostOptimization:
        """Test cost optimization and caching."""

        def test_estimate_token_count(self, byok_handler):
            """Estimate token count for prompt."""
            prompt = "Hello, world!"
            count = byok_handler.estimate_tokens(prompt, provider=LLMProvider.OPENAI)

            assert count > 0
            assert isinstance(count, int)

        def test_calculate_cost_openai(self, byok_handler):
            """Calculate cost for OpenAI provider."""
            cost = byok_handler.calculate_cost(
                input_tokens=1000,
                output_tokens=500,
                provider=LLMProvider.OPENAI
            )

            assert cost > 0
            # OpenAI GPT-4: ~$0.03/1k input, $0.06/1k output
            assert 0.01 < cost < 0.10

        def test_calculate_cost_deepseek_cheaper(self, byok_handler):
            """DeepSeek is cheaper than OpenAI."""
            openai_cost = byok_handler.calculate_cost(
                input_tokens=1000,
                output_tokens=500,
                provider=LLMProvider.OPENAI
            )
            deepseek_cost = byok_handler.calculate_cost(
                input_tokens=1000,
                output_tokens=500,
                provider=LLMProvider.DEEPSEEK
            )

            assert deepseek_cost < openai_cost

        def test_cache_hit_reduces_cost(self, byok_handler):
            """Cache hit reduces cost by 90%+."""
            uncached_cost = byok_handler.calculate_cost(
                input_tokens=1000,
                output_tokens=500,
                provider=LLMProvider.OPENAI
            )

            cached_cost = byok_handler.calculate_cost(
                input_tokens=1000,
                output_tokens=500,
                provider=LLMProvider.OPENAI,
                cache_hit=True
            )

            assert cached_cost < uncached_cost * 0.1  # 90% reduction


    class TestCognitiveTierRouting:
        """Test cognitive tier-based routing."""

        def test_classify_cognitive_tier_micro(self, byok_handler):
            """Classify simple prompts as MICRO tier."""
            tier = byok_handler.classify_tier("Hello")

            assert tier == CognitiveTier.MICRO

        def test_classify_cognitive_tier_standard(self, byok_handler):
            """Classify medium prompts as STANDARD tier."""
            prompt = "Summarize the following article: " + "word " * 50
            tier = byok_handler.classify_tier(prompt)

            assert tier == CognitiveTier.STANDARD

        def test_classify_cognitive_tier_heavy(self, byok_handler):
            """Classify long, complex prompts as HEAVY tier."""
            prompt = "Analyze the following data: " + "word " * 500
            tier = byok_handler.classify_tier(prompt)

            assert tier == CognitiveTier.HEAVY

        def test_route_by_tier_cost_optimized(self, byok_handler):
            """Route to cheapest provider for tier."""
            provider = byok_handler.route_by_tier(CognitiveTier.MICRO)

            assert provider == LLMProvider.DEEPSEEK  # Cheapest

        def test_route_by_tier_quality_optimized(self, byok_handler):
            """Route to best provider for complex tasks."""
            provider = byok_handler.route_by_tier(
                CognitiveTier.COMPLEX,
                optimize="quality"
            )

            assert provider in [LLMProvider.ANTHROPIC, LLMProvider.OPENAI]


    class TestTokenManagement:
        """Test token counting and limits."""

        def test_count_tokens_openai(self, byok_handler):
            """Count tokens using OpenAI tokenizer."""
            text = "Hello, world! This is a test."
            count = byok_handler.count_tokens(text, provider=LLMProvider.OPENAI)

            assert count > 0

        def test_truncate_to_fit_limit(self, byok_handler):
            """Truncate text to fit within token limit."""
            long_text = "word " * 1000
            truncated = byok_handler.truncate_to_limit(
                long_text,
                limit=100,
                provider=LLMProvider.OPENAI
            )

            assert len(truncated) < len(long_text)
            assert byok_handler.count_tokens(truncated) <= 100

        def test_estimate_response_tokens(self, byok_handler):
            """Estimate response token count."""
            input_tokens = 1000
            estimated_output = byok_handler.estimate_response_tokens(input_tokens)

            # Response typically 0.5-2x input
            assert input_tokens * 0.5 <= estimated_output <= input_tokens * 2


    class TestErrorHandling:
        """Test error handling for LLM calls."""

        @pytest.mark.asyncio
        async def test_handle_api_key_error(self, byok_handler):
            """Handle invalid API key gracefully."""
            with patch('core.llm.byok_handler.openai.AsyncOpenAI') as mock_openai:
                mock_openai.side_effect = Exception("Invalid API key")

                with pytest.raises(Exception) as exc_info:
                    await byok_handler.stream_response(
                        messages=[{"role": "user", "content": "Hi"}],
                        provider=LLMProvider.OPENAI
                    )

                assert "API key" in str(exc_info.value) or "authentication" in str(exc_info.value).lower()

        @pytest.mark.asyncio
        async def test_handle_rate_limit_error(self, byok_handler):
            """Handle rate limit with retry logic."""
        with patch('core.llm.byok_handler.openai.AsyncOpenAI') as mock_openai:
            mock_client = AsyncMock()
            mock_client.chat.completions.create.side_effect = [
                Exception("Rate limit"),
                MagicMock()  # Success on retry
            ]
            mock_openai.return_value = mock_client

            # Should retry and succeed
            result = await byok_handler.stream_response_with_retry(
                messages=[{"role": "user", "content": "Hi"}],
                provider=LLMProvider.OPENAI,
                max_retries=2
            )

            assert result is not None
    ```

    Create 25-30 tests covering:
    - Provider selection (default, requested, fallback, by maturity)
    - Streaming responses (OpenAI, Anthropic, DeepSeek)
    - Cost optimization (estimation, calculation, caching)
    - Cognitive tier routing (classification, routing)
    - Token management (counting, truncation)
    - Error handling (API keys, rate limits)
  </action>
  <verify>
    Run: python3 -m pytest backend/tests/core/llm/test_byok_handler_coverage.py -v
    Expected: 25-30 tests pass, 0 collection errors
  </verify>
  <done>
    BYOKHandler coverage complete: 25-30 tests covering provider selection, streaming, cost optimization, cognitive tier routing
  </done>
</task>

<task type="auto">
  <name>Verify Wave 2 tests expand coverage to 6+ files</name>
  <files>backend/tests/core/governance/test_agent_context_resolver_coverage.py,backend/tests/core/llm/test_byok_handler_coverage.py</files>
  <action>
    Run full coverage measurement to verify Wave 2 expansion:

    ```bash
    cd /Users/rushiparikh/projects/atom/backend

    # Run all tests with coverage
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/ --cov=. --cov-branch --cov-report=json --cov-report=term 2>&1 | grep -E "Coverage:" | tail -1

    # Check file count in coverage
    python3 -c "
    import json
    c=json.load(open('coverage.json'))
    print(f'Files in coverage: {len(c[\"files\"])}')
    print('\\nFiles:')
    for f in sorted(c['files'].keys()):
        data = c['files'][f]['summary']
        print(f'  {f}: {data[\"percent_covered\"]:.1f}%')
    "
    ```

    Verify:
    - File count increased to 6+ files
    - New files: agent_governance_service, agent_governance_cache, agent_context_resolver, byok_handler
    - Overall coverage >= 76% (2-3pp cumulative gain from baseline)
    - Zero collection errors
  </action>
  <verify>
    Coverage report shows:
    - Files in coverage: >= 6
    - Overall coverage: >= 76.0% (2-3pp gain from 74.69% baseline)
    - Collection errors: 0
    - New files visible: agent_context_resolver, byok_handler
  </verify>
  <done>
    Wave 2 coverage verified: 6+ files in coverage, 76%+ overall, 4 new governance/LLM files tested
  </done>
</task>

</tasks>

<verification>
- test_agent_context_resolver_coverage.py created with 25-30 tests
- test_byok_handler_coverage.py created with 25-30 tests
- Both files import successfully (no collection errors)
- Coverage report shows 6+ files (added agent_context_resolver, byok_handler)
- Overall coverage >= 76% (2-3pp cumulative gain)
</verification>

<success_criteria>
- 50-60 tests pass (25-30 each file)
- Zero collection errors
- 6+ files in coverage report
- Overall coverage >= 76%
- All 4 Wave 2 files visible in coverage
</success_criteria>

<output>
After completion, create `.planning/phases/206-coverage-push-80/206-03-SUMMARY.md` with:
- Tests created count (50-60)
- Cumulative coverage gain (2-3pp)
- Total files in coverage (6+)
- List of all files now under coverage
- Next steps for Wave 3 (workflow and memory files)
</output>
