---
phase: 089-bug-discovery-failure-modes-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/failure_modes/conftest.py
  - backend/tests/failure_modes/test_network_timeouts.py
  - backend/tests/failure_modes/test_provider_failures.py
  - backend/tests/failure_modes/test_database_connection_loss.py
  - backend/tests/failure_modes/test_resource_exhaustion.py
  - backend/tests/failure_modes/BUG_FINDINGS.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "All network timeout scenarios are tested (LLM providers, database, WebSocket)"
    - "Provider failure cascades are tested (all providers fail, rate limiting, fallback)"
    - "Database connection exhaustion is tested (pool timeout, recovery, deadlocks)"
    - "Resource exhaustion scenarios are tested (OOM, disk full, file descriptors)"
    - "System degrades gracefully under all failure modes (no crashes)"
    - "All discovered bugs documented in BUG_FINDINGS.md with severity and fix recommendations"
  artifacts:
    - path: "backend/tests/failure_modes/conftest.py"
      provides: "Failure injection fixtures (mock_timeout, mock_provider_error, mock_connection_error)"
      min_lines: 400
    - path: "backend/tests/failure_modes/test_network_timeouts.py"
      provides: "Network timeout tests for LLM, DB, WebSocket"
      min_lines: 500
    - path: "backend/tests/failure_modes/test_provider_failures.py"
      provides: "Provider failure cascade and fallback tests"
      min_lines: 500
    - path: "backend/tests/failure_modes/test_database_connection_loss.py"
      provides: "Connection pool, deadlock, and recovery tests"
      min_lines: 600
    - path: "backend/tests/failure_modes/test_resource_exhaustion.py"
      provides: "OOM, disk full, file descriptor limit tests"
      min_lines: 500
    - path: "backend/tests/failure_modes/BUG_FINDINGS.md"
      provides: "Documented bugs with severity, impact, and fix recommendations"
      min_lines: 300
  key_links:
    - from: "test_network_timeouts.py"
      to: "core/llm/byok_handler.py"
      via: "AsyncMock(side_effect=asyncio.TimeoutError) for client.chat.completions.create"
      pattern: "asyncio.TimeoutError"
    - from: "test_provider_failures.py"
      to: "core/llm/byok_handler.py"
      via: "Mock all providers to fail sequentially, test fallback logic"
      pattern: "handler.clients\[provider_id\]"
    - from: "test_database_connection_loss.py"
      to: "core/database.py"
      via: "Mock engine.connect to raise OperationalError (pool exhausted, timeout, deadlock)"
      pattern: "sqlalchemy.exc.OperationalError"
    - from: "test_resource_exhaustion.py"
      to: "core/governance_cache.py"
      via: "Test cache with max_size=10**15 to trigger MemoryError or ValueError"
      pattern: "GovernanceCache\(max_size="
---

<objective>
Create comprehensive failure mode test suite to discover how the Atom platform behaves when external dependencies fail. Tests simulate network timeouts, LLM provider failures, database connection loss, and resource exhaustion to verify graceful degradation and prevent production outages.

Purpose: Discover cascading failure bugs, verify fallback logic works, ensure system remains partially functional when components fail

Output: 20+ tests across 4 failure mode categories, documented bugs in BUG_FINDINGS.md
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/089-bug-discovery-failure-modes-security/089-RESEARCH.md
@.planning/phases/088-bug-discovery-error-paths-boundaries/088-01-SUMMARY.md
@backend/tests/error_paths/conftest.py
@backend/core/llm/byok_handler.py
@backend/core/agent_governance_service.py
@backend/tools/canvas_tool.py
@backend/core/database.py
@backend/core/governance_cache.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create failure mode test infrastructure</name>
  <files>backend/tests/failure_modes/conftest.py</files>
  <action>
Create conftest.py with failure injection fixtures:

1. **Network timeout fixtures:**
   - `mock_llm_timeout()` - Mock LLM client to raise asyncio.TimeoutError
   - `mock_db_timeout()` - Mock database connection to raise OperationalError with timeout
   - `mock_websocket_timeout()` - Mock WebSocket to raise ConnectionClosed

2. **Provider failure fixtures:**
   - `mock_all_providers_fail()` - Mock all LLM providers to raise Exception
   - `mock_primary_provider_fail()` - Mock primary provider to fail, secondary succeeds
   - `mock_provider_rate_limit()` - Mock provider to raise rate limit error

3. **Database connection fixtures:**
   - `mock_pool_exhausted()` - Mock connection pool to raise OperationalError (pool exhausted)
   - `mock_connection_closed()` - Mock connection to close mid-transaction
   - `mock_deadlock()` - Mock commit to raise OperationalError (deadlock detected)

4. **Resource exhaustion fixtures:**
   - `mock_memory_error()` - Mock MemoryError via side_effect
   - `mock_disk_full()` - Mock disk full OperationalError
   - `assert_graceful_degradation()` - Verify system continues operating after failure

5. **Helper functions:**
   - `assert_timeout_handled()` - Verify timeout exception raised and handled
   - `assert_fallback_triggered()` - Verify fallback provider attempted
   - `assert_recovery_succeeded()` - Verify system recovered after failure

Import from Phase 088 error_paths/conftest.py for consistency. Use AsyncMock for async failures, MagicMock for sync.
  </action>
  <verify>
Run: `cd backend && PYTHONPATH=. pytest tests/failure_modes/ --collect-only`
Expected: No collection errors, fixtures available
  </verify>
  <done>
conftest.py created with 20+ failure injection fixtures, helper functions for timeout/fallback/recovery verification
  </done>
</task>

<task type="auto">
  <name>Task 2: Network timeout tests</name>
  <files>backend/tests/failure_modes/test_network_timeouts.py</files>
  <action>
Create test_network_timeouts.py with timeout simulation tests:

1. **LLM provider timeout tests:**
   - `test_llm_provider_timeout_during_generate()` - Mock client.chat.completions.create with asyncio.TimeoutError, verify error message
   - `test_llm_provider_timeout_during_stream()` - Mock stream to timeout after yielding tokens, verify partial response handled
   - `test_all_llm_providers_timeout()` - Mock all providers to timeout, verify graceful degradation
   - `test_websocket_connection_dropped()` - Mock WebSocket ConnectionClosed during stream, verify cleanup

2. **Database timeout tests:**
   - `test_database_connection_timeout()` - Mock engine.connect with OperationalError("connection timeout"), verify exception raised
   - `test_query_execution_timeout()` - Mock session.execute with asyncio.TimeoutError, verify handled
   - `test_transaction_timeout()` - Mock commit to timeout, verify rollback executed

3. **WebSocket timeout tests:**
   - `test_websocket_timeout_during_broadcast()` - Mock ConnectionManager.broadcast to timeout, verify error logged
   - `test_websocket_timeout_during_personal_message()` - Mock send_personal_message to timeout, verify user notified

4. **Recovery tests:**
   - `test_retry_after_timeout()` - Mock timeout then success, verify retry logic works
   - `test_timeout_does_not_crash_system()` - Verify system remains functional after timeout

Use pytest.mark.asyncio for async tests. Use mock_llm_timeout(), mock_db_timeout() fixtures. Pattern from 089-RESEARCH.md lines 91-188.
  </action>
  <verify>
Run: `cd backend && PYTHONPATH=. pytest tests/failure_modes/test_network_timeouts.py -v`
Expected: 5+ timeout tests passing, verifying timeouts are handled gracefully
  </verify>
  <done>
5+ network timeout tests created, timeouts don't crash system, retries work where expected
  </done>
</task>

<task type="auto">
  <name>Task 3: Provider failure cascade tests</name>
  <files>backend/tests/failure_modes/test_provider_failures.py</files>
  <action>
Create test_provider_failures.py with provider failure cascade tests:

1. **All providers fail tests:**
   - `test_all_providers_fail()` - Mock all providers (openai, anthropic, deepseek, gemini) to raise Exception, verify error message mentions all providers
   - `test_all_providers_timeout()` - Mock all providers to timeout, verify graceful degradation
   - `test_all_providers_rate_limited()` - Mock all providers to raise rate limit errors, verify clear error message

2. **Primary provider failure tests:**
   - `test_primary_provider_fails_fallback_to_secondary()` - Mock OpenAI fails, Anthropic succeeds, verify Anthropic response returned
   - `test_primary_provider_rate_limits_secondary_succeeds()` - Mock OpenAI rate limit, Anthropic succeeds, verify fallback worked
   - `test_primary_provider_timeout_secondary_succeeds()` - Mock OpenAI timeout, verify secondary provider attempted

3. **Provider cascade tests:**
   - `test_providers_fail_sequentially()` - Mock providers to fail one by one, verify all attempted before giving up
   - `test_provider_unavailable()` - Mock provider to raise ConnectionError, verify next provider tried
   - `test_provider_api_key_invalid()` - Mock provider to raise AuthenticationError, verify fallback to next provider

4. **Fallback verification:**
   - `test_fallback_provider_called()` - Verify fallback provider API called when primary fails
   - `test_no_providers_configured()` - Test with empty providers dict, verify clear error message

Use BYOKHandler.clients and BYOKHandler.async_clients dicts to mock providers. Pattern from 089-RESEARCH.md lines 197-271.
  </action>
  <verify>
Run: `cd backend && PYTHONPATH=. pytest tests/failure_modes/test_provider_failures.py -v`
Expected: 6+ provider failure tests passing, fallback logic verified
  </verify>
  <done>
6+ provider failure tests created, fallback logic works, all providers tried before error
  </done>
</task>

<task type="auto">
  <name>Task 4: Database connection exhaustion tests</name>
  <files>backend/tests/failure_modes/test_database_connection_loss.py</files>
  <action>
Create test_database_connection_loss.py with connection failure tests:

1. **Connection pool exhaustion tests:**
   - `test_connection_pool_exhausted()` - Mock engine.connect to raise OperationalError("pool exhausted"), verify timeout error raised
   - `test_connection_timeout()` - Mock connection to raise OperationalError("connection timeout"), verify exception handled
   - `test_too_many_connections()` - Mock SQLAlchemy to raise "too many connections" error, verify system handles gracefully

2. **Connection closed mid-transaction tests:**
   - `test_connection_closed_during_query()` - Mock session.execute to raise DBAPIError("connection closed"), verify rollback called
   - `test_connection_closed_during_commit()` - Mock session.commit to raise DBAPIError, verify rollback executed
   - `test_connection_leak()` - Simulate many connections not closed, verify pool recovers

3. **Deadlock tests:**
   - `test_database_deadlock()` - Mock session.commit to raise OperationalError("deadlock detected"), verify retry attempted or error raised
   - `test_deadlock_retry_succeeds()` - Mock deadlock then success, verify retry logic works
   - `test_concurrent_update_deadlock()` - Simulate concurrent updates to same row (document SQLite limitation)

4. **Recovery tests:**
   - `test_connection_recovers_after_timeout()` - Mock timeout then success, verify new connections work
   - `test_pool_recovers_after_exhaustion()` - Verify pool returns to healthy state after exhaustion
   - `test_deadlock_does_not_hang_system()` - Verify deadlock doesn't cause infinite hang

Use mock_pool_exhausted(), mock_connection_closed(), mock_deadlock() fixtures. Document SQLite vs PostgreSQL differences. Pattern from 089-RESEARCH.md lines 278-358.
  </action>
  <verify>
Run: `cd backend && PYTHONPATH=. pytest tests/failure_modes/test_database_connection_loss.py -v`
Expected: 5+ connection failure tests passing, recovery verified
  </verify>
  <done>
5+ database connection failure tests created, connection recovery works, deadlocks don't hang
  </done>
</task>

<task type="auto">
  <name>Task 5: Resource exhaustion tests</name>
  <files>backend/tests/failure_modes/test_resource_exhaustion.py</files>
  <action>
Create test_resource_exhaustion.py with resource exhaustion tests:

1. **Out of memory tests:**
   - `test_out_of_memory_error()` - Try to create GovernanceCache with max_size=10**15, verify MemoryError or ValueError raised
   - `test_large_allocation_fails_gracefully()` - Mock large operation to raise MemoryError, verify system doesn't crash
   - `test_memory_error_does_not_corrupt_data()` - Verify cache state consistent after MemoryError

2. **Disk full tests:**
   - `test_disk_full_on_database_write()` - Mock database write to raise OperationalError("disk full"), verify error caught
   - `test_disk_full_on_log_write()` - Mock logger to raise OSError("No space left on device"), verify logged error
   - `test_disk_full_does_not_crash_system()` - Verify system continues in read-only mode

3. **File descriptor limit tests:**
   - `test_file_descriptor_limit()` - Try to open many sockets (simulate ulimit reached), verify OSError caught
   - `test_too_many_open_files()` - Mock file operations to raise "Too many open files", verify system recovers
   - `test_file_descriptors_cleaned_up()` - Verify connections/files closed after use

4. **Graceful degradation tests:**
   - `test_cache_degrades_gracefully_under_memory_pressure()` - Verify cache evicts entries when full
   - `test_database_degrades_to_read_only_on_disk_full()` - Verify read operations work when writes fail
   - `test_system_remains_functional_with_partial_failures()` - Verify 50% functionality remains when components fail

Use mock_memory_error(), mock_disk_full() fixtures. Pattern from 089-RESEARCH.md lines 367-433. Note: Some tests simulate failure rather than actually exhausting resources (for CI speed).
  </action>
  <verify>
Run: `cd backend && PYTHONPATH=. pytest tests/failure_modes/test_resource_exhaustion.py -v`
Expected: 4+ resource exhaustion tests passing, graceful degradation verified
  </verify>
  <done>
4+ resource exhaustion tests created, system degrades gracefully, no crashes
  </done>
</task>

<task type="auto">
  <name>Task 6: Document bugs and verify graceful degradation</name>
  <files>backend/tests/failure_modes/BUG_FINDINGS.md</files>
  <action>
Create BUG_FINDINGS.md documenting discovered bugs:

1. **Bug documentation format:**
   ```markdown
   ## Bug N: [Title]
   **Severity:** [Critical/High/Medium/Low]
   **Location:** `file:function`
   **Failure Mode:** [What external failure triggers this bug]
   **Root Cause:** [Why the bug occurs]
   **Impact:** [What breaks in production]
   **Test Case:** `test_function_name()` demonstrates the bug
   **Fix Recommendation:** [How to fix]
   **Expected Behavior:** [What should happen instead]
   ```

2. **Review all test files for bugs:**
   - Check if timeouts cause crashes (should degrade gracefully)
   - Check if provider failures don't trigger fallback
   - Check if connection errors don't recover
   - Check if resource exhaustion causes data corruption

3. **Categorize bugs by severity:**
   - **Critical:** System crash, data loss, security breach
   - **High:** Feature unavailable, cascading failures
   - **Medium:** Degraded performance, partial unavailability
   - **Low:** Minor issues, edge cases

4. **Verify graceful degradation:**
   - Run all tests: `cd backend && PYTHONPATH=. pytest tests/failure_modes/ -v`
   - Check that no test causes unhandled exception
   - Verify error messages are helpful
   - Verify system recovers after failure

Reference Phase 088 BUG_FINDINGS.md format. Use assert_graceful_degradation() helper to verify system continues operating.
  </action>
  <verify>
Run: `cd backend && PYTHONPATH=. pytest tests/failure_modes/ -v --tb=short`
Expected: All tests pass, BUG_FINDINGS.md created with documented bugs (if any)
  </verify>
  <done>
BUG_FINDINGS.md created with all discovered bugs documented, graceful degradation verified across all failure modes
  </done>
</task>

</tasks>

<verification>
Post-execution verification checklist:

1. **Test file creation:**
   ```bash
   ls -la backend/tests/failure_modes/
   # Expected: conftest.py, test_network_timeouts.py, test_provider_failures.py,
   #          test_database_connection_loss.py, test_resource_exhaustion.py,
   #          BUG_FINDINGS.md
   ```

2. **Test execution:**
   ```bash
   cd backend && PYTHONPATH=. pytest tests/failure_modes/ -v
   # Expected: 20+ tests passing, no collection errors
   ```

3. **Coverage verification:**
   ```bash
   cd backend && PYTHONPATH=. pytest tests/failure_modes/ --cov=core/llm/byok_handler --cov=core/database --cov=core/governance_cache --cov-report=term-missing
   # Expected: Failure mode code paths covered (timeout, fallback, recovery)
   ```

4. **Bug documentation:**
   ```bash
   cat backend/tests/failure_modes/BUG_FINDINGS.md
   # Expected: Bugs documented with severity, impact, fix recommendations
   ```

5. **Graceful degradation verification:**
   - System remains functional after timeout (not crashed)
   - Provider fallback attempts secondary provider
   - Database connections recover after pool exhaustion
   - Resource errors don't cause data corruption
</verification>

<success_criteria>
1. **20+ failure mode tests created** covering network timeouts, provider failures, database connection loss, and resource exhaustion
2. **All tests use proper mocks** (AsyncMock for async failures, MagicMock for sync, no actual resource exhaustion)
3. **Failure modes documented** in BUG_FINDINGS.md with severity breakdown (Critical/High/Medium/Low)
4. **Graceful degradation verified** - system remains partially functional under all failure scenarios
5. **Test execution time < 30 seconds** for fast feedback (mocks ensure speed)
6. **No unhandled exceptions** - all failures caught and handled gracefully
7. **Recovery verified** - system recovers after transient failures (timeout, connection closed, deadlock)
</success_criteria>

<output>
After completion, create `.planning/phases/089-bug-discovery-failure-modes-security/089-01-SUMMARY.md`

Summary should include:
- Test count breakdown by category (timeouts, providers, database, resources)
- Bugs discovered with severity breakdown
- Graceful degradation verification results
- Coverage improvements for failure handling code
- Recommendations for production resilience improvements
</output>
