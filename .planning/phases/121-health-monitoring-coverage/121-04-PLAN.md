---
phase: 121-health-monitoring-coverage
plan: 04
type: execute
wave: 4
depends_on: [121-01, 121-02, 121-03]
files_modified: [backend/tests/test_health_routes.py]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Coverage report shows 60%+ coverage for api/health_routes.py"
  artifacts:
    - path: "backend/tests/test_health_routes.py"
      provides: "Integration tests with real database connectivity"
      min_lines: 700  # Current 568 + ~150 new lines
      contains: "class TestDatabaseConnectivityIntegration"
  key_links:
    - from: "backend/tests/test_health_routes.py"
      to: "backend/api/health_routes.py"
      via: "Real database integration tests using db_session fixture"
      pattern: "def test_.*_integration.*db_session"
---

# Plan 04: Integration Tests with Real Database

**Gap Closure Plan** - Addresses coverage gap identified in 121-VERIFICATION.md

## Objective

Create integration tests with real database connectivity to increase api/health_routes.py coverage from 42.11% to 60%+. The existing tests use heavy mocking that skips actual code paths. This plan adds 15-20 integration tests using the existing `db_session` fixture (SQLite) to execute actual database queries and health check logic.

**Purpose:** Close the 17.89 percentage point coverage gap for api/health_routes.py

**Output:** 15-20 new integration tests with real database connectivity

## Execution Context

@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

## Context

@.planning/phases/121-health-monitoring-coverage/121-VERIFICATION.md
@.planning/phases/121-health-monitoring-coverage/121-03-SUMMARY.md
@backend/tests/conftest.py
@backend/api/health_routes.py
@backend/core/database.py

## Gap Analysis (from VERIFICATION.md)

**Gap 1:** api/health_routes.py coverage at 42.11% (17.89 pp below 60% target)

**Root Cause:** 27 tests created but heavy mocking skips actual code paths

**Missing Tests:**
1. Integration tests with real database - Current tests mock get_db() which skips actual database query execution
2. Sync subsystem integration - Tests check response structure but don't execute sync logic
3. Readiness probe error paths - Only 86.5% coverage, missing error handling paths
4. Pool status checking - Connection pool metrics not validated
5. Performance warning path - Slow query warning not tested

**Estimated Tests Needed:** 15-20 additional integration tests

## Tasks

<task type="auto">
  <name>Task 1: Create integration test class with real database</name>
  <files>backend/tests/test_health_routes.py</files>
  <action>
    Create TestDatabaseConnectivityIntegration class with integration tests using real db_session fixture (SQLite). Add 5-6 tests:

    1. test_database_connectivity_with_real_db: Uses db_session fixture to test /health/db endpoint with actual database queries. Validates:
       - Status is "healthy" with real database
       - query_time_ms is populated
       - pool_status contains all required fields (size, checked_in, checked_out, overflow, max_overflow)

    2. test_database_connectivity_pool_status_fields: Validates connection pool status structure with real engine.pool calls. Tests all fields:
       - pool.size() returns valid integer
       - pool.checkedin() returns valid integer
       - pool.checkedout() returns valid integer
       - pool.overflow() returns valid integer
       - pool.max_overflow returns valid integer

    3. test_readiness_probe_with_real_database: Tests readiness_probe with real database connectivity. Validates:
       - Returns 200 when database is accessible
       - checks.database.healthy is True
       - checks.database.latency_ms is populated
       - checks.database.message contains "Database accessible"

    4. test_check_database_internal_with_real_session: Tests _check_database() with real database session. Validates:
       - Executes actual SELECT 1 query
       - Returns healthy=True when query succeeds
       - Returns valid latency_ms measurement
       - Returns correct message

    5. test_execute_db_query_internal_with_real_session: Tests _execute_db_query() with real database. Validates:
       - Executes SELECT 1 query successfully
       - Returns True when fetchone() succeeds
       - Handles real Session object correctly

    Use @pytest.mark.asyncio for async tests and db_session fixture for real database.
  </action>
  <verify>
    Run: pytest backend/tests/test_health_routes.py::TestDatabaseConnectivityIntegration -v
    Expected: All 5-6 tests pass with real database execution
  </verify>
  <done>
    Integration test class created with 5-6 tests using real db_session fixture
    All tests execute actual database queries (no mocking of get_db)
    Pool status fields validated with real engine.pool calls
  </done>
</task>

<task type="auto">
  <name>Task 2: Add readiness probe error path integration tests</name>
  <files>backend/tests/test_health_routes.py</files>
  <action>
    Create TestReadinessProbeErrorPaths class with error path integration tests. Add 4-5 tests:

    1. test_readiness_probe_database_timeout: Simulates database timeout using monkeypatch to make SQL query slow. Validates:
       - Returns 503 when database times out
       - checks.database.healthy is False
       - checks.database.message contains "timeout"
       - checks.database.latency_ms reflects timeout duration

    2. test_readiness_probe_disk_space_critical: Uses monkeypatch to simulate critical disk space (<1GB). Validates:
       - Returns 503 when disk space insufficient
       - checks.disk.healthy is False
       - checks.disk.free_gb < 1.0
       - checks.disk.message contains "Low disk space"

    3. test_readiness_probe_both_failures: Tests both database and disk failures simultaneously. Validates:
       - Returns 503 with multiple failed checks
       - Both database and disk show healthy=False
       - Response includes all failure details

    4. test_readiness_probe_partial_failure: Tests readiness when one check passes, one fails. Validates:
       - Returns 503 (any failure = not ready)
       - Passing check shows healthy=True
       - Failing check shows healthy=False

    Use monkeypatch fixture to simulate error conditions without mocking the entire health check functions.
  </action>
  <verify>
    Run: pytest backend/tests/test_health_routes.py::TestReadinessProbeErrorPaths -v
    Expected: All 4-5 tests pass with error path validation
  </verify>
  <done>
    Error path integration tests created with 4-5 tests
    Timeout, disk space, and multi-failure scenarios validated
    Error response structures verified
  </done>
</task>

<task type="auto">
  <name>Task 3: Add performance and edge case integration tests</name>
  <files>backend/tests/test_health_routes.py</files>
  <action>
    Create TestHealthRoutesPerformance class with performance and edge case tests. Add 4-5 tests:

    1. test_database_slow_query_warning: Tests slow query (>100ms) triggers warning. Use monkeypatch to make time.time() return values that simulate >100ms query time. Validates:
       - response.json()["database"]["warning"] is present
       - warning message contains "Slow query"
       - Status still returns 200 (warning, not error)

    2. test_liveness_probe_performance: Validates liveness probe <10ms target. Run 100 iterations and measure average latency. Validates:
       - Average response time < 10ms
       - P95 response time < 20ms
       - All requests return 200

    3. test_readiness_probe_performance: Validates readiness probe <100ms target with real database. Run 50 iterations. Validates:
       - Average response time < 100ms (includes real DB query)
       - P95 response time < 200ms
       - All requests return 200 or 503 (no crashes)

    4. test_concurrent_health_checks: Tests concurrent health check requests don't cause issues. Use asyncio.gather() or threading. Validates:
       - 10 concurrent requests all succeed
       - No database connection errors
       - Consistent responses across concurrent calls

    5. test_database_session_cleanup: Validates database sessions are properly closed after health checks. Check engine.pool.checkedin() equals initial value after test. Validates:
       - No connection leaks
       - pool.checkedin() returns to expected value
       - pool.checkedout() returns to 0 or initial value
  </action>
  <verify>
    Run: pytest backend/tests/test_health_routes.py::TestHealthRoutesPerformance -v
    Expected: All 4-5 tests pass with performance metrics validated
  </verify>
  <done>
    Performance integration tests created with 4-5 tests
    Liveness <10ms, readiness <100ms targets validated
    Slow query warning path covered
    Connection leak prevention verified
  </done>
</task>

<task type="auto">
  <name>Task 4: Run final coverage measurement and verify 60%+ target</name>
  <files>backend/tests/coverage_reports/metrics/phase_121_coverage_final_gap_closure.json</files>
  <action>
    Generate final coverage report to verify 60%+ target achieved for api/health_routes.py:

    1. Set environment variable to use SQLite for tests:
       export ATOM_MOCK_DATABASE=false
       export DATABASE_URL="sqlite:///tmp/test_coverage.db"

    2. Run pytest with coverage on health_routes only:
       cd backend
       pytest tests/test_health_routes.py \
         --cov=api/health_routes \
         --cov-report=term-missing \
         --cov-report=json:tests/coverage_reports/metrics/phase_121_coverage_final_gap_closure.json \
         -v

    3. Parse coverage JSON output and extract:
       - api/health_routes.py percent_covered
       - lines_covered / total_lines
       - Missing lines count

    4. Create summary JSON file with:
       {
         "file": "api/health_routes.py",
         "baseline_coverage": 42.11,
         "final_coverage": <measured_value>,
         "improvement": <final - baseline>,
         "target": 60.0,
         "target_achieved": <final >= 60.0>,
         "tests_added": <count>,
         "timestamp": "<ISO timestamp>"
       }

    5. If final_coverage < 60%, note remaining gap and identify specific uncovered lines.
  </action>
  <verify>
    Run: python -c "import json; data = json.load(open('backend/tests/coverage_reports/metrics/phase_121_coverage_final_gap_closure.json')); print(f\"Coverage: {data['final_coverage']}%\"); assert data['final_coverage'] >= 60.0, f\"Target not met: {data['final_coverage']}% < 60%\""
    Expected: api/health_routes.py coverage >= 60%
  </verify>
  <done>
    Final coverage report generated at phase_121_coverage_final_gap_closure.json
    api/health_routes.py coverage >= 60% (target achieved)
    Coverage improvement documented in summary JSON
    If target not met, remaining gaps identified for next iteration
  </done>
</task>

## Verification

After all tasks complete, verify:

1. **Test Count:** 15-20 new integration tests added across 3 test classes
2. **Test Pass Rate:** 100% (all new tests pass)
3. **Coverage:** api/health_routes.py >= 60% (measured with pytest --cov)
4. **Real Database:** Integration tests use db_session fixture (no get_db() mocking)
5. **Code Paths:** Error paths, pool status, performance warnings all covered

Run verification:
```bash
cd backend
pytest tests/test_health_routes.py -v --cov=api/health_routes --cov-report=term-missing
```

Expected output:
- test_database_connectivity_with_real_db PASSED
- test_readiness_probe_database_timeout PASSED
- test_database_slow_query_warning PASSED
- (all 15-20 new tests pass)
- api/health_routes.py coverage >= 60%

## Success Criteria

1. **Coverage Target:** api/health_routes.py coverage >= 60% (up from 42.11%)
2. **Integration Tests:** 15-20 new tests with real database connectivity
3. **Zero Mocking of get_db():** Integration tests use actual db_session fixture
4. **Error Paths Covered:** Timeout, disk space, slow queries all tested
5. **Performance Validated:** <10ms liveness, <100ms readiness targets verified

## Output

After completion, create `.planning/phases/121-health-monitoring-coverage/121-04-SUMMARY.md` with:

- Final coverage percentage for api/health_routes.py
- Test count breakdown (integration tests added)
- Coverage improvement (percentage points gained)
- Remaining uncovered lines (if any)
- Performance metrics summary
