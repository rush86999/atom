---
phase: 208-integration-performance-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/workflows/conftest.py
  - backend/tests/integration/workflows/test_workflow_engine_e2e.py
  - backend/tests/integration/workflows/test_episode_segmentation_e2e.py
  - backend/tests/integration/workflows/test_multi_agent_workflows.py
  - backend/tests/integration/workflows/__init__.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - Integration tests execute workflow_engine with realistic database and mocked external services
    - Integration tests execute episode_segmentation_service with realistic state transitions
    - Multi-agent workflow scenarios test agent-to-agent communication paths
    - All integration tests use real database (SQLite in-memory) with transaction rollback
    - External services (LLM providers, WebSocket) are mocked to prevent flakiness
  artifacts:
    - path: backend/tests/integration/workflows/conftest.py
      provides: Shared fixtures for workflow integration tests
      contains: db_session, mock_llm_provider, mock_websocket_manager
    - path: backend/tests/integration/workflows/test_workflow_engine_e2e.py
      provides: End-to-end workflow execution tests
      min_lines: 200
    - path: backend/tests/integration/workflows/test_episode_segmentation_e2e.py
      provides: Episode segmentation integration tests
      min_lines: 150
    - path: backend/tests/integration/workflows/test_multi_agent_workflows.py
      provides: Multi-agent orchestration tests
      min_lines: 100
  key_links:
    - from: test_workflow_engine_e2e.py
      to: core.workflow_engine.WorkflowEngine
      via: direct import and async execution
      pattern: from core.workflow_engine import WorkflowEngine
    - from: test_episode_segmentation_e2e.py
      to: core.episode_segmentation_service.EpisodeSegmentationService
      via: direct import and database session
      pattern: from core.episode_segmentation_service import EpisodeSegmentationService
    - from: conftest.py
      to: tests.factories.workflow_factory
      via: factory_boy fixtures
      pattern: from tests.factories.workflow_factory import
---

<objective>
Create integration tests for complex orchestration modules (workflow_engine, episode_segmentation) that are poorly suited to unit testing. These tests use realistic database sessions with transaction rollback, mock only external services (LLM providers, WebSocket), and validate multi-service integration paths.

Purpose: Complex orchestration modules (>1000 lines) achieved only 10-15% unit coverage in Phase 206 because unit tests have diminishing returns with async orchestration, external dependencies, and complex state management. Integration tests are the appropriate tool for validating these modules.

Output: 4 integration test files with shared fixtures, 50-60 integration tests covering workflow execution, episode segmentation, and multi-agent scenarios.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/208-integration-performance-testing/208-RESEARCH.md
@.planning/phases/207-coverage-quality-push/207-PHASE-SUMMARY.md
@backend/tests/integration/test_workflow_engine_integration.py
@backend/tests/integration/conftest.py
@backend/core/workflow_engine.py
@backend/core/episode_segmentation_service.py
@backend/tests/factories/workflow_factory.py
</context>

<tasks>

<task type="auto">
  <name>Create workflow integration test fixtures (conftest.py)</name>
  <files>backend/tests/integration/workflows/conftest.py</files>
  <action>
    Create shared fixtures for workflow integration tests:

    1. db_session fixture: SQLite in-memory database with transaction rollback
       - Use pytest.fixture with scope="function"
       - Create all tables before test, rollback after test
       - Return Session for database operations

    2. mock_llm_provider fixture: AsyncMock for LLM provider calls
       - Mock call_llm() to return {"result": "success"}
       - Mock stream_llm() to return async generator
       - Support multiple providers (OpenAI, Anthropic, DeepSeek)

    3. mock_websocket_manager fixture: MagicMock for WebSocket notifications
       - Mock notify_workflow_status() as AsyncMock
       - Mock broadcast_message() as AsyncMock
       - Track all calls for verification

    4. sample_workflow fixture: Valid workflow definition for tests
       - Simple 2-step workflow (step1 -> step2)
       - Complex branching workflow (if/else conditions)
       - Error handling workflow (try/catch steps)

    5. sample_episode_context fixture: Episode context for segmentation tests
       - ChatSession with messages
       - Canvas presentation events
       - User feedback data

    Reference: backend/tests/integration/conftest.py for existing patterns
    DO NOT mock database - use real SQLite in-memory for persistence testing
  </action>
  <verify>
    pytest tests/integration/workflows/conftest.py --collect-only shows fixtures available
  </verify>
  <done>
    Shared fixtures created with real database (SQLite in-memory) and mocked external services (LLM, WebSocket)
  </done>
</task>

<task type="auto">
  <name>Create workflow engine E2E integration tests</name>
  <files>backend/tests/integration/workflows/test_workflow_engine_e2e.py</files>
  <action>
    Create end-to-end integration tests for WorkflowEngine (2260 lines, 10% unit coverage):

    Test class: TestWorkflowEngineE2E

    1. test_simple_workflow_execution (async)
       - Create simple workflow (2 steps, linear execution)
       - Execute with mocked LLM provider
       - Verify WorkflowExecution created in database
       - Verify WorkflowStepExecution records for each step
       - Verify final status is COMPLETED

    2. test_workflow_with_conditional_branching (async)
       - Create workflow with if/else condition
       - Set condition to true, verify correct branch executed
       - Set condition to false, verify alternate branch executed
       - Verify database state matches expected execution path

    3. test_workflow_parallel_execution (async)
       - Create workflow with 3 parallel steps
       - Execute with max_concurrent_steps=2
       - Verify steps executed in correct order (topological sort)
       - Verify parallel steps completed independently

    4. test_workflow_error_recovery (async)
       - Create workflow with failing step
       - Verify error caught and logged
       - Verify WorkflowExecution status is FAILED
       - Verify retry logic works (if implemented)

    5. test_workflow_pause_resume (async)
       - Create workflow with manual approval step
       - Execute until pause point
       - Verify status is PAUSED
       - Resume execution, verify completes successfully

    6. test_workflow_database_persistence (async)
       - Execute workflow that updates database
       - Verify changes persisted across execution
       - Verify rollback on failure (if transaction support exists)

    Use pytest.mark.asyncio for async tests
    Use real db_session fixture (not mocked)
    Mock only external services (LLM provider, WebSocket manager)
  </action>
  <verify>
    pytest tests/integration/workflows/test_workflow_engine_e2e.py -v
    All 6 tests pass, execution time <30 seconds
  </verify>
  <done>
    6 E2E integration tests for workflow_engine covering execution, branching, parallel execution, errors, pause/resume, and persistence
  </done>
</task>

<task type="auto">
  <name>Create episode segmentation E2E integration tests</name>
  <files>backend/tests/integration/workflows/test_episode_segmentation_e2e.py</files>
  <action>
    Create end-to-end integration tests for EpisodeSegmentationService (1536 lines, 15% unit coverage):

    Test class: TestEpisodeSegmentationE2E

    1. test_episode_creation_from_chat_session (async)
       - Create ChatSession with 10 messages
       - Create sample_episode_context with session data
       - Call create_episode() with realistic context
       - Verify Episode record created in database
       - Verify EpisodeSegment records created for each message
       - Verify LLM canvas summary generated

    2. test_episode_segmentation_time_gap (async)
       - Create messages with 2-hour gap between message 5 and 6
       - Call should_create_new_episode()
       - Verify returns True (time gap threshold exceeded)
       - Verify creates 2 episodes when segmenting

    3. test_episode_segmentation_topic_change (async)
       - Create messages discussing topic A, then switch to topic B
       - Call should_create_new_episode() with topic detection
       - Verify returns True (topic change detected)
       - Verify segments grouped by topic

    4. test_episode_with_canvas_presentation (async)
       - Create CanvasAudit record (chart presentation)
       - Create episode context with canvas reference
       - Call create_episode() with canvas data
       - Verify Episode.canvas_reference populated
       - Verify canvas metadata stored in episode

    5. test_episode_with_user_feedback (async)
       - Create AgentFeedback record (thumbs up/down)
       - Create episode context with feedback data
       - Call create_episode() with feedback
       - Verify Episode.feedback_reference populated
       - Verify feedback score affects episode retrieval weight

    Use pytest.mark.asyncio for async tests
    Use real db_session fixture for database operations
    Mock LLM provider for summary generation (return fixed summary)
  </action>
  <verify>
    pytest tests/integration/workflows/test_episode_segmentation_e2e.py -v
    All 5 tests pass, execution time <30 seconds
  </verify>
  <done>
    5 E2E integration tests for episode_segmentation covering creation, time gaps, topic changes, canvas presentations, and user feedback
  </done>
</task>

<task type="auto">
  <name>Create multi-agent workflow integration tests</name>
  <files>backend/tests/integration/workflows/test_multi_agent_workflows.py</files>
  <action>
    Create integration tests for multi-agent orchestration scenarios:

    Test class: TestMultiAgentWorkflows

    1. test_agent_handoff_workflow (async)
       - Create workflow with 2 agent steps (agent1 -> agent2)
       - Agent1: Data collection agent (INTERN maturity)
       - Agent2: Analysis agent (SUPERVISED maturity)
       - Execute workflow with mocked governance checks
       - Verify agent1 output passed to agent2
       - Verify governance checks called for each agent

    2. test_parallel_agent_execution (async)
       - Create workflow with 3 parallel agent steps
       - All agents access different data sources
       - Execute with max_concurrent_steps=3
       - Verify all agents executed concurrently
       - Verify no race conditions in database

    3. test_agent_error_propagation (async)
       - Create workflow: agent1 -> agent2 -> agent3
       - Mock agent2 to raise exception
       - Verify error propagates to agent3
       - Verify workflow status is FAILED
       - Verify error logged in WorkflowExecution

    4. test_agent_governance_enforcement (async)
       - Create workflow with AUTONOMOUS-required action
       - Execute with STUDENT maturity agent
       - Verify governance blocks execution
       - Verify trigger_interceptor called
       - Verify workflow status is BLOCKED

    Use pytest.mark.asyncio for async tests
    Mock governance_cache for permission checks
    Mock trigger_interceptor for maturity enforcement
    Use real workflow engine for orchestration logic
  </action>
  <verify>
    pytest tests/integration/workflows/test_multi_agent_workflows.py -v
    All 4 tests pass, execution time <30 seconds
  </verify>
  <done>
    4 multi-agent workflow integration tests covering handoffs, parallel execution, error propagation, and governance enforcement
  </done>
</task>

</tasks>

<verification>
Run integration test suite:
```bash
cd backend
pytest tests/integration/workflows/ -v --tb=short
```

Expected results:
- 15-19 integration tests created
- All tests pass (100% pass rate)
- Execution time <30 seconds for full suite
- No collection errors
- Database properly cleaned between tests (transaction rollback)

Verify coverage on target modules:
```bash
pytest tests/integration/workflows/ --cov=core.workflow_engine --cov=core.episode_segmentation_service --cov-report=term-missing
```

Expected coverage improvements:
- workflow_engine.py: 10% -> 25%+ (integration tests add new coverage)
- episode_segmentation_service.py: 15% -> 30%+ (integration tests add new coverage)
</verification>

<success_criteria>
- [ ] 4 integration test files created with shared fixtures
- [ ] 15-19 integration tests covering complex orchestration
- [ ] Real database (SQLite in-memory) used for persistence testing
- [ ] External services (LLM, WebSocket) mocked to prevent flakiness
- [ ] All tests pass with 100% pass rate
- [ ] Integration test suite executes in <30 seconds
- [ ] workflow_engine coverage increases from 10% to 25%+
- [ ] episode_segmentation_service coverage increases from 15% to 30%+
</success_criteria>

<output>
After completion, create `.planning/phases/208-integration-performance-testing/208-01-SUMMARY.md` with:
- Test count breakdown by file
- Coverage improvements for target modules
- Execution time metrics
- Any issues encountered (database schema conflicts, mock configuration, etc.)
</output>
