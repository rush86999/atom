---
phase: 208-integration-performance-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/contracts/test_agent_api_contracts.py
  - backend/tests/integration/contracts/test_workflow_api_contracts.py
  - backend/tests/integration/contracts/test_canvas_api_contracts.py
  - backend/tests/integration/contracts/test_governance_api_contracts.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - Schemathesis contract tests validate OpenAPI spec for agent endpoints
    - Schemathesis contract tests validate OpenAPI spec for workflow endpoints
    - Schemathesis contract tests validate OpenAPI spec for canvas endpoints
    - Schemathesis contract tests validate OpenAPI spec for governance endpoints
    - All contract tests use Hypothesis settings (max_examples=20, deadline=None)
    - Contract tests run in separate pytest invocation (not blocking main suite)
  artifacts:
    - path: backend/tests/integration/contracts/test_agent_api_contracts.py
      provides: Agent API contract validation
      min_lines: 80
    - path: backend/tests/integration/contracts/test_workflow_api_contracts.py
      provides: Workflow API contract validation
      min_lines: 80
    - path: backend/tests/integration/contracts/test_canvas_api_contracts.py
      provides: Canvas API contract validation
      min_lines: 60
    - path: backend/tests/integration/contracts/test_governance_api_contracts.py
      provides: Governance API contract validation
      min_lines: 80
  key_links:
    - from: test_agent_api_contracts.py
      to: main.app (FastAPI application)
      via: schemathesis.from_wsgi
      pattern: schema = schemathesis.from_wsgi\("/openapi.json", app\)
    - from: test_workflow_api_contracts.py
      to: main.app
      via: schemathesis.from_wsgi
      pattern: schema = schemathesis.from_wsgi\("/openapi.json", app\)
    - from: test_canvas_api_contracts.py
      to: main.app
      via: schemathesis.from_wsgi
      pattern: schema = schemathesis.from_wsgi\("/openapi.json", app\)
    - from: test_governance_api_contracts.py
      to: main.app
      via: schemathesis.from_wsgi
      pattern: schema = schemathesis.from_wsgi\("/openapi.json", app\)
---

<objective>
Create API contract tests using Schemathesis for critical endpoints (agent, workflow, canvas, governance). These tests use property-based testing to validate that the actual API implementation matches the OpenAPI specification, automatically generating edge cases that humans wouldn't think of.

Purpose: API contract testing detects breaking changes before deployment, validates request/response schemas match the spec, and tests edge cases through Hypothesis property-based generation. This complements unit tests by focusing on API contracts rather than implementation logic.

Output: 4 contract test files with Schemathesis validation for 15+ endpoints across 4 API domains.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/208-integration-performance-testing/208-RESEARCH.md
@backend/tests/contract/conftest.py
@backend/tests/contract/test_agent_api_contract.py
@backend/docs/API_CONTRACT_TESTING.md
@backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Create agent API contract tests</name>
  <files>backend/tests/integration/contracts/test_agent_api_contracts.py</files>
  <action>
    Create Schemathesis contract tests for Agent API endpoints:

    Import schema from FastAPI app:
    ```python
    import pytest
    import schemathesis
    from hypothesis import settings, given
    from main import app

    schema = schemathesis.from_wsgi("/openapi.json", app)
    ```

    Test class: TestAgentAPIContracts

    1. test_get_agent_by_id
       - Endpoint: GET /api/v1/agents/{agent_id}
       - Settings: max_examples=20, deadline=None
       - Validates: Response schema matches OpenAPI spec
       - Expected status codes: 200, 404, 403

    2. test_list_agents
       - Endpoint: GET /api/v1/agents
       - Settings: max_examples=15
       - Validates: Pagination, query parameters
       - Expected status codes: 200, 400

    3. test_create_agent
       - Endpoint: POST /api/v1/agents
       - Settings: max_examples=15
       - Validates: Request body schema, required fields
       - Expected status codes: 200, 201, 400, 422

    4. test_update_agent
       - Endpoint: PUT /api/v1/agents/{agent_id}
       - Settings: max_examples=10
       - Validates: Partial updates, field validation
       - Expected status codes: 200, 404, 422

    5. test_delete_agent
       - Endpoint: DELETE /api/v1/agents/{agent_id}
       - Settings: max_examples=10
       - Validates: Cascade deletion behavior
       - Expected status codes: 200, 404, 403

    6. test_agent_chat
       - Endpoint: POST /api/v1/agents/{agent_id}/chat
       - Settings: max_examples=10
       - Validates: Chat request/response schema
       - Expected status codes: 200, 404, 500

    Use schema.parametrize() for each endpoint
    Use @settings decorator for Hypothesis configuration
    Use case.call_and_validate() for automatic schema validation
    Add pytest.mark.contract marker for separate test run
  </action>
  <verify>
    pytest tests/integration/contracts/test_agent_api_contracts.py -v -m contract
    All 6 contract tests pass
  </verify>
  <done>
    6 agent API contract tests validating CRUD operations and chat endpoint
  </done>
</task>

<task type="auto">
  <name>Create workflow API contract tests</name>
  <files>backend/tests/integration/contracts/test_workflow_api_contracts.py</files>
  <action>
    Create Schemathesis contract tests for Workflow API endpoints:

    Test class: TestWorkflowAPIContracts

    1. test_get_workflow_templates
       - Endpoint: GET /api/v1/workflows/templates
       - Settings: max_examples=15
       - Validates: Template list schema
       - Expected status codes: 200, 400

    2. test_get_workflow_by_id
       - Endpoint: GET /api/v1/workflows/{workflow_id}
       - Settings: max_examples=20
       - Validates: Workflow definition schema
       - Expected status codes: 200, 404

    3. test_create_workflow
       - Endpoint: POST /api/v1/workflows
       - Settings: max_examples=15
       - Validates: Workflow definition schema (nodes, connections)
       - Expected status codes: 200, 201, 400, 422

    4. test_execute_workflow
       - Endpoint: POST /api/v1/workflows/{workflow_id}/execute
       - Settings: max_examples=10
       - Validates: Execution request/response
       - Expected status codes: 200, 404, 500

    5. test_get_workflow_execution
       - Endpoint: GET /api/v1/workflows/executions/{execution_id}
       - Settings: max_examples=15
       - Validates: Execution status schema
       - Expected status codes: 200, 404

    6. test_cancel_workflow_execution
       - Endpoint: POST /api/v1/workflows/executions/{execution_id}/cancel
       - Settings: max_examples=10
       - Validates: Cancellation response
       - Expected status codes: 200, 404, 409

    Use schema.parametrize() for each endpoint
    Add pytest.mark.contract marker
  </action>
  <verify>
    pytest tests/integration/contracts/test_workflow_api_contracts.py -v -m contract
    All 6 contract tests pass
  </verify>
  <done>
    6 workflow API contract tests validating template, CRUD, execution, and cancellation endpoints
  </done>
</task>

<task type="auto">
  <name>Create canvas API contract tests</name>
  <files>backend/tests/integration/contracts/test_canvas_api_contracts.py</files>
  <action>
    Create Schemathesis contract tests for Canvas API endpoints:

    Test class: TestCanvasAPIContracts

    1. test_create_canvas
       - Endpoint: POST /api/v1/canvas
       - Settings: max_examples=15
       - Validates: Canvas creation request schema
       - Expected status codes: 200, 201, 400, 422

    2. test_get_canvas_by_id
       - Endpoint: GET /api/v1/canvas/{canvas_id}
       - Settings: max_examples=20
       - Validates: Canvas response schema (7 canvas types)
       - Expected status codes: 200, 404

    3. test_update_canvas
       - Endpoint: PUT /api/v1/canvas/{canvas_id}
       - Settings: max_examples=15
       - Validates: Canvas update schema
       - Expected status codes: 200, 404, 422

    4. test_delete_canvas
       - Endpoint: DELETE /api/v1/canvas/{canvas_id}
       - Settings: max_examples=10
       - Validates: Deletion response
       - Expected status codes: 200, 404

    Use schema.parametrize() for each endpoint
    Add pytest.mark.contract marker
    Reference backend/docs/API_CONTRACT_TESTING.md for patterns
  </action>
  <verify>
    pytest tests/integration/contracts/test_canvas_api_contracts.py -v -m contract
    All 4 contract tests pass
  </verify>
  <done>
    4 canvas API contract tests validating CRUD operations for all 7 canvas types
  </done>
</task>

<task type="auto">
  <name>Create governance API contract tests</name>
  <files>backend/tests/integration/contracts/test_governance_api_contracts.py</files>
  <action>
    Create Schemathesis contract tests for Governance API endpoints:

    Test class: TestGovernanceAPIContracts

    1. test_get_governance_status
       - Endpoint: GET /api/v1/governance/status
       - Settings: max_examples=10
       - Validates: Governance status schema
       - Expected status codes: 200

    2. test_check_agent_permission
       - Endpoint: POST /api/v1/governance/check-permission
       - Settings: max_examples=15
       - Validates: Permission check request/response
       - Expected status codes: 200, 400, 422

    3. test_get_agent_maturity
       - Endpoint: GET /api/v1/governance/agents/{agent_id}/maturity
       - Settings: max_examples=20
       - Validates: Maturity level schema (STUDENT/INTERN/SUPERVISED/AUTONOMOUS)
       - Expected status codes: 200, 404

    4. test_set_agent_maturity
       - Endpoint: PUT /api/v1/governance/agents/{agent_id}/maturity
       - Settings: max_examples=10
       - Validates: Maturity update request/response
       - Expected status codes: 200, 404, 403

    5. test_get_governance_audit_log
       - Endpoint: GET /api/v1/governance/audit
       - Settings: max_examples=15
       - Validates: Audit log pagination and filtering
       - Expected status codes: 200, 400

    6. test_create_training_proposal
       - Endpoint: POST /api/v1/governance/training/proposals
       - Settings: max_examples=10
       - Validates: Training proposal schema
       - Expected status codes: 200, 201, 400, 422

    Use schema.parametrize() for each endpoint
    Add pytest.mark.contract marker
  </action>
  <verify>
    pytest tests/integration/contracts/test_governance_api_contracts.py -v -m contract
    All 6 contract tests pass
  </verify>
  <done>
    6 governance API contract tests validating status, permissions, maturity, audit log, and training proposals
  </done>
</task>

</tasks>

<verification>
Run contract test suite:
```bash
cd backend
pytest tests/integration/contracts/ -v -m contract --tb=short
```

Expected results:
- 22 contract tests created (6 agent + 6 workflow + 4 canvas + 6 governance)
- All tests pass (100% pass rate)
- Execution time <2 minutes (Schemathesis generates multiple examples per test)
- No OpenAPI schema validation errors

Verify OpenAPI spec generation:
```bash
cd backend
python -c "from main import app; import json; print(json.dumps(app.openapi(), indent=2))" > /tmp/openapi.json
```

Expected results:
- OpenAPI spec generated successfully
- All 15+ endpoints documented
- Request/response schemas defined for all endpoints
</verification>

<success_criteria>
- [ ] 4 contract test files created
- [ ] 22 contract tests covering 15+ endpoints
- [ ] All tests use Schemathesis with Hypothesis settings
- [ ] Contract tests marked with pytest.mark.contract
- [ ] 100% pass rate for all contract tests
- [ ] Execution time <2 minutes for full suite
- [ ] OpenAPI spec validates successfully
</success_criteria>

<output>
After completion, create `.planning/phases/208-integration-performance-testing/208-02-SUMMARY.md` with:
- Contract test count breakdown by API domain
- OpenAPI spec validation results
- Any schema mismatches detected
- Execution time metrics
</output>
