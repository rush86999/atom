---
phase: 208-integration-performance-testing
plan: 03
type: execute
wave: 2
depends_on: [208-01]
files_modified:
  - backend/tests/integration/performance/conftest.py
  - backend/tests/integration/performance/test_workflow_performance.py
  - backend/tests/integration/performance/test_episode_performance.py
  - backend/tests/integration/performance/test_governance_performance.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - Performance benchmarks measure workflow engine critical paths with pytest-benchmark
    - Performance benchmarks measure episode segmentation critical paths
    - Performance benchmarks measure governance cache performance
    - All benchmarks use pytest.mark.benchmark for historical tracking
    - Benchmark data saved for regression detection (.benchmark_cache directory)
  artifacts:
    - path: backend/tests/integration/performance/conftest.py
      provides: Shared fixtures for performance benchmarks
      contains: benchmark fixture, warmup_iterations, min_rounds
    - path: backend/tests/integration/performance/test_workflow_performance.py
      provides: Workflow engine performance benchmarks
      min_lines: 150
    - path: backend/tests/integration/performance/test_episode_performance.py
      provides: Episode segmentation performance benchmarks
      min_lines: 100
    - path: backend/tests/integration/performance/test_governance_performance.py
      provides: Governance cache performance benchmarks
      min_lines: 80
  key_links:
    - from: test_workflow_performance.py
      to: core.workflow_engine.WorkflowEngine
      via: direct import and benchmark fixture
      pattern: @pytest.mark.benchmark\(group="workflow-[^"]+"\)
    - from: test_episode_performance.py
      to: core.episode_segmentation_service.EpisodeSegmentationService
      via: direct import and benchmark fixture
      pattern: @pytest.mark.benchmark\(group="episode-[^"]+"\)
    - from: test_governance_performance.py
      to: core.governance_cache.GovernanceCache
      via: direct import and benchmark fixture
      pattern: @pytest.mark.benchmark\(group="governance-[^"]+"\)
---

<objective>
Create performance benchmarks for critical paths using pytest-benchmark. These tests measure execution time for workflow validation, episode segmentation, and governance operations, establishing baseline performance and enabling regression detection through historical tracking.

Purpose: Performance testing validates that critical operations meet performance targets (<100ms P50, <500ms P99) and detects performance regressions before they reach production. pytest-benchmark provides historical tracking and statistical analysis without manual timing code.

Output: 3 performance benchmark files with 15-20 benchmarks covering workflow, episode, and governance operations.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/208-integration-performance-testing/208-RESEARCH.md
@backend/tests/test_performance_benchmarks.py
@backend/core/workflow_engine.py
@backend/core/episode_segmentation_service.py
@backend/core/governance_cache.py
@backend/core/monitoring.py
</context>

<tasks>

<task type="auto">
  <name>Create performance benchmark fixtures (conftest.py)</name>
  <files>backend/tests/integration/performance/conftest.py</files>
  <action>
    Create shared fixtures for performance benchmarks:

    1. benchmark fixture configuration:
       - Set warmup_iterations=2 (JIT compilation warmup)
       - Set min_rounds=5 (minimum benchmark iterations)
       - Configure timer (time.perf_counter for high resolution)
       - Enable histogram tracking (percentiles)

    2. skip_benchmark fixture:
       - Skip benchmarks if pytest-benchmark not installed
       - Check for BENCHMARK_AVAILABLE environment variable
       - Provide clear skip message

    3. small_workflow fixture:
       - Pre-created 2-step workflow for benchmarks
       - Reusable across multiple benchmark tests
       - Avoids setup time in benchmark measurement

    4. medium_workflow fixture:
       - Pre-created 5-step workflow with branching
       - Tests realistic workflow complexity
       - Includes conditional logic

    5. sample_episode_context fixture:
       - Pre-created episode context for benchmarks
       - Includes 10 messages, canvas reference, feedback
       - Reusable across episode benchmarks

    Reference: backend/tests/test_performance_benchmarks.py for existing patterns
    Reference: pytest-benchmark documentation for configuration options
  </action>
  <verify>
    pytest tests/integration/performance/conftest.py --collect-only shows fixtures available
  </verify>
  <done>
    Shared fixtures created with benchmark configuration (warmup, min_rounds) and reusable test data
  </done>
</task>

<task type="auto">
  <name>Create workflow engine performance benchmarks</name>
  <files>backend/tests/integration/performance/test_workflow_performance.py</files>
  <action>
    Create performance benchmarks for WorkflowEngine critical paths:

    Test class: TestWorkflowPerformance

    1. test_workflow_schema_validation (group: workflow-validation)
       - Benchmark: _validate_workflow_schema() method
       - Target: <50ms P50 (schema validation should be fast)
       - Input: 5-step workflow with various node types
       - Verify: Returns True (valid schema)

    2. test_topological_sort_5_steps (group: workflow-sort)
       - Benchmark: _topological_sort() method
       - Target: <20ms P50 (graph traversal is fast)
       - Input: 5-step linear workflow
       - Verify: Correct execution order

    3. test_topological_sort_20_steps (group: workflow-sort)
       - Benchmark: _topological_sort() with complex graph
       - Target: <100ms P50 (larger graph acceptable)
       - Input: 20-step workflow with branching
       - Verify: Correct execution order

    4. test_parameter_resolution (group: workflow-params)
       - Benchmark: _resolve_parameters() method
       - Target: <30ms P50 (parameter interpolation)
       - Input: Workflow with 5 parameter references
       - Verify: All parameters resolved correctly

    5. test_condition_evaluation (group: workflow-conditions)
       - Benchmark: _evaluate_condition() method
       - Target: <10ms P50 (condition checks should be instant)
       - Input: Various condition types (equals, contains, exists)
       - Verify: Correct boolean result

    6. test_workflow_state_snapshot (group: workflow-state)
       - Benchmark: State manager serialization
       - Target: <50ms P50 (state serialization)
       - Input: 10-step workflow execution state
       - Verify: JSON-serializable output

    7. test_workflow_dag_validation (group: workflow-dag)
       - Benchmark: Cycle detection in workflow graph
       - Target: <100ms P50 (graph algorithms)
       - Input: 20-step workflow with potential cycles
       - Verify: Detects cycles correctly

    Use @pytest.mark.benchmark(group="...") for grouping
    Use benchmark() fixture for timing
    DO NOT add hard-coded time assertions (rely on historical tracking)
    Add target comments for documentation (<Xms P50)
  </action>
  <verify>
    pytest tests/integration/performance/test_workflow_performance.py -v --benchmark-only
    All 7 benchmarks execute successfully
    Benchmark data saved to .benchmark_cache/
  </verify>
  <done>
    7 workflow performance benchmarks covering validation, sorting, parameters, conditions, state, and DAG validation
  </done>
</task>

<task type="auto">
  <name>Create episode segmentation performance benchmarks</name>
  <files>backend/tests/integration/performance/test_episode_performance.py</files>
  <action>
    Create performance benchmarks for EpisodeSegmentationService:

    Test class: TestEpisodePerformance

    1. test_should_create_new_episode_time_gap (group: episode-detection)
       - Benchmark: should_create_new_episode() time gap check
       - Target: <10ms P50 (time comparison is fast)
       - Input: 2 messages with 2-hour gap
       - Verify: Returns True (gap detected)

    2. test_should_create_new_episode_topic_change (group: episode-detection)
       - Benchmark: should_create_new_episode() topic change detection
       - Target: <50ms P50 (requires LLM embedding comparison)
       - Input: 20 messages with topic shift
       - Verify: Returns True (topic change detected)
       - Mock LLM embedding call (return fixed vector)

    3. test_create_episode_10_messages (group: episode-creation)
       - Benchmark: create_episode() with 10 messages
       - Target: <200ms P50 (includes LLM summary generation)
       - Input: Sample episode context with 10 messages
       - Verify: Episode record created
       - Mock LLM summary generation (return fixed summary)

    4. test_create_episode_50_messages (group: episode-creation)
       - Benchmark: create_episode() with 50 messages (large episode)
       - Target: <500ms P50 (scales linearly)
       - Input: Sample episode context with 50 messages
       - Verify: Episode record created
       - Mock LLM summary generation

    5. test_segment_episode_by_time (group: episode-segmentation)
       - Benchmark: Segment episode by time gaps
       - Target: <100ms P50 (batch processing)
       - Input: 50 messages with various time gaps
       - Verify: Returns 3-5 episode segments

    Use @pytest.mark.benchmark(group="...") for grouping
    Mock LLM calls for summary generation (we're testing orchestration, not LLM)
    Mock embedding calls for topic detection (fixed vector comparison)
  </action>
  <verify>
    pytest tests/integration/performance/test_episode_performance.py -v --benchmark-only
    All 5 benchmarks execute successfully
  </verify>
  <done>
    5 episode performance benchmarks covering detection, creation, and segmentation
  </done>
</task>

<task type="auto">
  <name>Create governance cache performance benchmarks</name>
  <files>backend/tests/integration/performance/test_governance_performance.py</files>
  <action>
    Create performance benchmarks for GovernanceCache:

    Test class: TestGovernancePerformance

    1. test_cache_get_hit (group: governance-cache)
       - Benchmark: Cache get operation (cache hit)
       - Target: <1ms P50 (cache hits must be instant)
       - Input: Cached agent permission check
       - Verify: Returns cached value

    2. test_cache_get_miss (group: governance-cache)
       - Benchmark: Cache get operation (cache miss)
       - Target: <5ms P50 (cache misses faster than DB)
       - Input: Non-cached agent permission check
       - Verify: Returns None (cache miss)

    3. test_cache_set (group: governance-cache)
       - Benchmark: Cache set operation
       - Target: <1ms P50 (cache writes must be instant)
       - Input: Store agent permission result
       - Verify: Value retrievable after set

    4. test_cache_bulk_invalidate (group: governance-cache)
       - Benchmark: Bulk cache invalidation
       - Target: <10ms P50 (clear all cache entries)
       - Input: Cache with 100 entries
       - Verify: All entries cleared

    5. test_governance_check_cached (group: governance-check)
       - Benchmark: Full governance check (cached)
       - Target: <1ms P50 (governance decision from cache)
       - Input: Agent maturity + action complexity check
       - Verify: Returns allowed/denied decision

    Use @pytest.mark.benchmark(group="...") for grouping
    Pre-populate cache for hit benchmarks
    Clear cache before each benchmark (isolation)
  </action>
  <verify>
    pytest tests/integration/performance/test_governance_performance.py -v --benchmark-only
    All 5 benchmarks execute successfully
  </verify>
  <done>
    5 governance cache performance benchmarks covering get, set, invalidation, and governance checks
  </done>
</task>

</tasks>

<verification>
Run performance benchmark suite:
```bash
cd backend
pytest tests/integration/performance/ -v --benchmark-only --benchmark-autosave
```

Expected results:
- 17 performance benchmarks created (7 workflow + 5 episode + 5 governance)
- All benchmarks execute successfully
- Benchmark data saved to .benchmark_cache/
- Execution time <5 minutes for full suite

Generate benchmark report:
```bash
cd backend
pytest tests/integration/performance/ --benchmark-only --benchmark-histogram
```

Expected results:
- Histogram generated for each benchmark group
- P50, P95, P99 percentiles visible
- Historical comparison available (after second run)

Verify target metrics:
```bash
cd backend
pytest tests/integration/performance/ --benchmark-only --benchmark-sort=name
```

Expected results:
- workflow_validation <50ms P50
- governance_cache_get <1ms P50
- episode_detection <10ms P50
</verification>

<success_criteria>
- [ ] 3 performance benchmark files created
- [ ] 17 performance benchmarks covering workflow, episode, and governance
- [ ] All benchmarks use pytest.mark.benchmark for grouping
- [ ] Benchmark data saved to .benchmark_cache/ for historical tracking
- [ ] Target metrics documented (<Xms P50)
- [ ] No hard-coded time assertions (historical tracking only)
- [ ] Execution time <5 minutes for full suite
</success_criteria>

<output>
After completion, create `.planning/phases/208-integration-performance-testing/208-03-SUMMARY.md` with:
- Benchmark count breakdown by module
- Baseline performance metrics (P50, P95, P99)
- Any benchmarks exceeding targets
- Benchmark report generation instructions
</output>
