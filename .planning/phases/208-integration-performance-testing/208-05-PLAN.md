---
phase: 208-integration-performance-testing
plan: 05
type: execute
wave: 3
depends_on: [208-03]
files_modified:
  - backend/tests/integration/performance/test_api_latency_benchmarks.py
  - backend/tests/integration/performance/test_database_query_performance.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - API latency benchmarks measure critical endpoint response times
    - Database query benchmarks measure query performance with realistic data volumes
    - All benchmarks use pytest-benchmark for historical tracking
    - Benchmark targets documented (<100ms P50, <500ms P99)
  artifacts:
    - path: backend/tests/integration/performance/test_api_latency_benchmarks.py
      provides: API endpoint latency benchmarks
      min_lines: 150
    - path: backend/tests/integration/performance/test_database_query_performance.py
      provides: Database query performance benchmarks
      min_lines: 120
  key_links:
    - from: test_api_latency_benchmarks.py
      to: FastAPI TestClient
      via: httpx.AsyncClient
      pattern: from httpx import AsyncClient
    - from: test_database_query_performance.py
      to: SQLAlchemy Session
      via: database query execution
      pattern: db_session.execute\(select\(\.model\)\)
---

<objective>
Create additional performance benchmarks for API latency and database queries. These benchmarks measure response times for critical API endpoints and query performance for common database operations, establishing performance baselines and enabling regression detection.

Purpose: API latency and database query performance are critical for user experience. These benchmarks validate that endpoints meet performance targets and database queries are optimized, preventing performance regressions before they impact users.

Output: 2 performance benchmark files with 12-15 benchmarks covering API latency and database queries.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/208-integration-performance-testing/208-RESEARCH.md
@backend/tests/integration/performance/test_workflow_performance.py
@backend/tests/integration/performance/conftest.py
@backend/core/atom_agent_endpoints.py
@backend/api/agent_governance_routes.py
@backend/core/models.py
</context>

<tasks>

<task type="auto">
  <name>Create API latency benchmarks</name>
  <files>backend/tests/integration/performance/test_api_latency_benchmarks.py</files>
  <action>
    Create API latency benchmarks for critical endpoints:

    Test class: TestAPILatency

    1. test_health_check_latency (group: api-latency)
       - Endpoint: GET /health/live
       - Target: <10ms P50 (liveness probe must be instant)
       - Verify: 200 response

    2. test_agent_list_latency (group: api-latency)
       - Endpoint: GET /api/v1/agents
       - Target: <100ms P50 (agent listing)
       - Setup: Create 10 agents in database
       - Verify: 200 response with agent list

    3. test_agent_get_latency (group: api-latency)
       - Endpoint: GET /api/v1/agents/{agent_id}
       - Target: <50ms P50 (single agent fetch)
       - Setup: Create agent in database
       - Verify: 200 response with agent details

    4. test_governance_check_latency (group: api-latency)
       - Endpoint: POST /api/v1/governance/check-permission
       - Target: <20ms P50 (cached governance checks)
       - Setup: Pre-populate governance cache
       - Verify: 200 response with permission result

    5. test_workflow_execute_latency (group: api-latency)
       - Endpoint: POST /api/v1/workflows/{workflow_id}/execute
       - Target: <200ms P50 (workflow initiation)
       - Setup: Create simple 2-step workflow
       - Mock: Actual execution (return execution_id)
       - Verify: 200 response with execution_id

    6. test_canvas_create_latency (group: api-latency)
       - Endpoint: POST /api/v1/canvas
       - Target: <100ms P50 (canvas creation)
       - Input: Simple markdown canvas
       - Verify: 201 response with canvas_id

    7. test_episode_list_latency (group: api-latency)
       - Endpoint: GET /api/v1/episodes
       - Target: <100ms P50 (episode listing)
       - Setup: Create 10 episodes in database
       - Verify: 200 response with episode list

    8. test_analytics_dashboard_latency (group: api-latency)
       - Endpoint: GET /api/v1/analytics/dashboard
       - Target: <500ms P50 (analytics aggregation)
       - Setup: Create sample analytics data
       - Verify: 200 response with dashboard data

    Use httpx.AsyncClient for async API calls
    Use FastAPI TestClient for synchronous calls
    Use benchmark() fixture for timing
    Add target comments (<Xms P50) for documentation
  </action>
  <verify>
    pytest tests/integration/performance/test_api_latency_benchmarks.py -v --benchmark-only
    All 8 benchmarks execute successfully
  </verify>
  <done>
    8 API latency benchmarks covering health, agents, governance, workflows, canvas, episodes, and analytics
  </done>
</task>

<task type="auto">
  <name>Create database query performance benchmarks</name>
  <files>backend/tests/integration/performance/test_database_query_performance.py</files>
  <action>
    Create database query performance benchmarks:

    Test class: TestDatabaseQueryPerformance

    1. test_agent_select_by_id (group: db-query)
       - Query: AgentRegistry.get(agent_id)
       - Target: <10ms P50 (primary key lookup)
       - Setup: Create 100 agents in database
       - Verify: Agent retrieved correctly

    2. test_agent_list_pagination (group: db-query)
       - Query: AgentRegistry.list(limit=20, offset=0)
       - Target: <20ms P50 (paginated query)
       - Setup: Create 100 agents in database
       - Verify: 20 agents returned

    3. test_episode_select_with_relations (group: db-query)
       - Query: Episode.get_with_segments()
       - Target: <50ms P50 (join with segments)
       - Setup: Create 10 episodes with 50 segments each
       - Verify: Episode with all segments loaded

    4. test_canvas_audit_insert (group: db-query)
       - Query: CanvasAudit.insert()
       - Target: <20ms P50 (audit log insert)
       - Setup: Benchmark batch of 10 inserts
       - Verify: All records created

    5. test_workflow_execution_insert (group: db-query)
       - Query: WorkflowExecution.insert()
       - Target: <30ms P50 (execution record insert)
       - Setup: Benchmark with step executions
       - Verify: Execution and steps created

    6. test_governance_cache_query (group: db-query)
       - Query: AgentRegistry maturity check
       - Target: <5ms P50 (maturity lookup)
       - Setup: Query with index on maturity column
       - Verify: Correct maturity returned

    7. test_episode_search_full_text (group: db-query)
       - Query: Episode.search(query text)
       - Target: <100ms P50 (full-text search)
       - Setup: Create 100 episodes with searchable content
       - Verify: Relevant episodes returned

    Use real db_session (not mocked)
    Use realistic data volumes (100+ records)
    Verify indexes are used (EXPLAIN QUERY PLAN)
  </action>
  <verify>
    pytest tests/integration/performance/test_database_query_performance.py -v --benchmark-only
    All 7 benchmarks execute successfully
  </verify>
  <done>
    7 database query performance benchmarks covering selects, inserts, joins, and full-text search
  </done>
</task>

</tasks>

<verification>
Run API latency benchmark suite:
```bash
cd backend
pytest tests/integration/performance/test_api_latency_benchmarks.py -v --benchmark-only --benchmark-autosave
```

Expected results:
- 8 API latency benchmarks created
- All benchmarks execute successfully
- Benchmark data saved for historical tracking

Run database query benchmark suite:
```bash
cd backend
pytest tests/integration/performance/test_database_query_performance.py -v --benchmark-only
```

Expected results:
- 7 database query benchmarks created
- All benchmarks execute successfully
- Query execution times within targets

Generate combined benchmark report:
```bash
cd backend
pytest tests/integration/performance/ --benchmark-only --benchmark-histogram --benchmark-sort=fullname
```

Expected results:
- Histogram showing P50, P95, P99 for all benchmarks
- Benchmarks grouped by type (api-latency, db-query)
- Historical comparison available
</verification>

<success_criteria>
- [ ] 2 performance benchmark files created
- [ ] 15 performance benchmarks (8 API + 7 database)
- [ ] All benchmarks use pytest.mark.benchmark for grouping
- [ ] Target metrics documented (<Xms P50)
- [ ] Benchmark data saved for historical tracking
- [ ] Execution time <10 minutes for full suite
</success_criteria>

<output>
After completion, create `.planning/phases/208-integration-performance-testing/208-05-SUMMARY.md` with:
- Benchmark count breakdown by type
- Baseline performance metrics (P50, P95, P99)
- Any benchmarks exceeding targets
- Database query analysis (EXPLAIN results)
</output>
