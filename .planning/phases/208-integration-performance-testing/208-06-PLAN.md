---
phase: 208-integration-performance-testing
plan: 06
type: execute
wave: 4
depends_on: [208-01, 208-02, 208-03, 208-04, 208-05]
files_modified:
  - backend/tests/integration/scripts/run_integration_tests.sh
  - backend/tests/integration/scripts/run_contract_tests.sh
  - backend/tests/integration/scripts/run_performance_tests.sh
  - backend/tests/integration/scripts/run_quality_tests.sh
  - backend/tests/integration/scripts/generate_test_report.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - Shell scripts exist for running integration, contract, performance, and quality test suites
    - Python script generates comprehensive test report combining all test types
    - All scripts are executable (chmod +x)
    - Scripts use pytest markers for selective test execution
  artifacts:
    - path: backend/tests/integration/scripts/run_integration_tests.sh
      provides: Integration test runner
      min_lines: 30
    - path: backend/tests/integration/scripts/run_contract_tests.sh
      provides: Contract test runner
      min_lines: 25
    - path: backend/tests/integration/scripts/run_performance_tests.sh
      provides: Performance benchmark runner
      min_lines: 30
    - path: backend/tests/integration/scripts/run_quality_tests.sh
      provides: Quality test runner
      min_lines: 25
    - path: backend/tests/integration/scripts/generate_test_report.py
      provides: Test report generator
      min_lines: 150
  key_links:
    - from: run_integration_tests.sh
      to: pytest tests/integration/workflows/
      via: shell command execution
      pattern: pytest.*--integration
    - from: run_contract_tests.sh
      to: pytest tests/integration/contracts/
      via: shell command execution
      pattern: pytest.*--contract
    - from: generate_test_report.py
      to: pytest json output
      via: pytest --json-report
      pattern: --json-report
---

<objective>
Create test runner scripts and report generator for Phase 208 test infrastructure. These scripts provide convenient commands for running integration, contract, performance, and quality test suites, plus a Python script that generates comprehensive test reports combining all test types.

Purpose: Test automation scripts make it easy to run specific test suites and generate reports. This improves developer experience and enables CI/CD integration for the new test infrastructure.

Output: 4 shell scripts for test execution and 1 Python script for report generation.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/208-integration-performance-testing/208-RESEARCH.md
@backend/pytest.ini
@backend/scripts/
</context>

<tasks>

<task type="auto">
  <name>Create integration test runner script</name>
  <files>backend/tests/integration/scripts/run_integration_tests.sh</files>
  <action>
    Create shell script for running integration tests:

    File: run_integration_tests.sh
    Shebang: #!/bin/bash

    Script functionality:
    1. Change to backend directory (cd "$(dirname "$0")/../../../")
    2. Parse command line arguments:
       - --coverage: Enable coverage reporting
       - --verbose: Verbose output (-vv)
       - --filter: Run specific test file
    3. Set default pytest markers: -m integration (or no marker for all integration tests)
    4. Run pytest with options:
       - --tb=short (short traceback)
       - --maxfail=5 (stop after 5 failures)
       - -v (verbose output)
       - Optional: --cov for coverage
    5. Capture exit code and print summary
    6. Return exit code (0 for success, 1 for failure)

    Example usage:
    ```bash
    ./run_integration_tests.sh
    ./run_integration_tests.sh --coverage
    ./run_integration_tests.sh --filter test_workflow_engine_e2e
    ```

    Make executable: chmod +x run_integration_tests.sh
  </action>
  <verify>
    backend/tests/integration/scripts/run_integration_tests.sh --help shows usage
    chmod +x verified (ls -l shows executable permissions)
  </verify>
  <done>
    Shell script created with integration test runner, coverage support, and filter option
  </done>
</task>

<task type="auto">
  <name>Create contract test runner script</name>
  <files>backend/tests/integration/scripts/run_contract_tests.sh</files>
  <action>
    Create shell script for running contract tests:

    File: run_contract_tests.sh
    Shebang: #!/bin/bash

    Script functionality:
    1. Change to backend directory
    2. Parse command line arguments:
       - --verbose: Verbose output
       - --endpoint: Run specific endpoint tests
    3. Set pytest marker: -m contract
    4. Run pytest with options:
       - --tb=short
       - -v
       - tests/integration/contracts/
    5. Capture Schemathesis output
    6. Print summary with contract validation results
    7. Return exit code

    Example usage:
    ```bash
    ./run_contract_tests.sh
    ./run_contract_tests.sh --endpoint agent
    ```

    Make executable: chmod +x run_contract_tests.sh
  </action>
  <verify>
    backend/tests/integration/scripts/run_contract_tests.sh --help shows usage
    chmod +x verified
  </verify>
  <done>
    Shell script created with contract test runner and endpoint filter option
  </done>
</task>

<task type="auto">
  <name>Create performance benchmark runner script</name>
  <files>backend/tests/integration/scripts/run_performance_tests.sh</files>
  <action>
    Create shell script for running performance benchmarks:

    File: run_performance_tests.sh
    Shebang: #!/bin/bash

    Script functionality:
    1. Change to backend directory
    2. Parse command line arguments:
       - --histogram: Generate histogram output
       - --save: Save benchmark data
       --compare: Compare to previous run
    3. Run pytest with options:
       - --benchmark-only (only benchmark tests)
       - -v (verbose output)
       - tests/integration/performance/
    4. Add benchmark options:
       - --benchmark-autosave (if --save)
       - --benchmark-histogram (if --histogram)
       - --benchmark-compare (if --compare)
    5. Print summary with P50, P95, P99 metrics
    6. Return exit code

    Example usage:
    ```bash
    ./run_performance_tests.sh
    ./run_performance_tests.sh --histogram --save
    ```

    Make executable: chmod +x run_performance_tests.sh
  </action>
  <verify>
    backend/tests/integration/scripts/run_performance_tests.sh --help shows usage
    chmod +x verified
  </verify>
  <done>
    Shell script created with performance benchmark runner and histogram/save options
  </done>
</task>

<task type="auto">
  <name>Create quality test runner script</name>
  <files>backend/tests/integration/scripts/run_quality_tests.sh</files>
  <action>
    Create shell script for running quality tests:

    File: run_quality_tests.sh
    Shebang: #!/bin/bash

    Script functionality:
    1. Change to backend directory
    2. Parse command line arguments:
       - --repeat: Number of repeats for flakiness detection
       - --random-seed: Specific random seed
    3. Run pytest with options:
       - --randomly-seed (if specified)
       - --count (if repeat specified)
       - -v
       - tests/integration/quality/
    4. Print summary with flakiness rate
    5. Return exit code

    Example usage:
    ```bash
    ./run_quality_tests.sh
    ./run_quality_tests.sh --repeat=5 --random-seed=1234
    ```

    Make executable: chmod +x run_quality_tests.sh
  </action>
  <verify>
    backend/tests/integration/scripts/run_quality_tests.sh --help shows usage
    chmod +x verified
  </verify>
  <done>
    Shell script created with quality test runner and repeat/random-seed options
  </done>
</task>

<task type="auto">
  <name>Create test report generator script</name>
  <files>backend/tests/integration/scripts/generate_test_report.py</files>
  <action>
    Create Python script for generating comprehensive test reports:

    File: generate_test_report.py
    Shebang: #!/usr/bin/env python3

    Script functionality:
    1. Parse command line arguments:
       - --output: Output file (default: test-report.json)
       - --format: Output format (json, html, markdown)
       - --include: Test types to include (integration, contract, performance, quality)
    2. Run each test suite with --json-report flag:
       - integration: pytest tests/integration/workflows/ --json-report
       - contract: pytest tests/integration/contracts/ --json-report
       - performance: pytest tests/integration/performance/ --json-report
       - quality: pytest tests/integration/quality/ --json-report
    3. Parse JSON reports:
       - Extract test counts (total, passed, failed, skipped)
       - Extract execution time
       - Extract coverage data (if available)
       - Extract benchmark data (P50, P95, P99)
    4. Generate combined report:
       - Summary section: Overall pass rate, total tests
       - Integration section: Test results, coverage
       - Contract section: Schema validation results
       - Performance section: Benchmark metrics
       - Quality section: Flakiness rate, collection stability
    5. Write output in specified format (json, html, markdown)

    Example usage:
    ```bash
    ./generate_test_report.py --format markdown --output TEST_REPORT.md
    ./generate_test_report.py --include integration,performance
    ```

    Make executable: chmod +x generate_test_report.py
  </action>
  <verify>
    python3 backend/tests/integration/scripts/generate_test_report.py --help shows usage
    chmod +x verified
  </verify>
  <done>
    Python script created with test report generator supporting multiple output formats
  </done>
</task>

</tasks>

<verification>
Verify all scripts are executable:
```bash
ls -la backend/tests/integration/scripts/*.sh backend/tests/integration/scripts/*.py
```

Expected results:
- All scripts have executable permissions (-rwxr-xr-x)

Test each script:
```bash
cd backend/tests/integration/scripts/
./run_integration_tests.sh --help
./run_contract_tests.sh --help
./run_performance_tests.sh --help
./run_quality_tests.sh --help
./generate_test_report.py --help
```

Expected results:
- All scripts show usage/help information
- No syntax errors

Generate test report:
```bash
cd backend
python3 tests/integration/scripts/generate_test_report.py --format markdown --output TEST_REPORT.md
```

Expected results:
- TEST_REPORT.md created with summary sections
- Report includes integration, contract, performance, quality sections
</verification>

<success_criteria>
- [ ] 5 script files created (4 shell + 1 Python)
- [ ] All scripts have executable permissions
- [ ] All scripts show help/usage information
- [ ] Integration test script supports coverage and filter options
- [ ] Contract test script supports endpoint filter
- [ ] Performance test script supports histogram and save options
- [ ] Quality test script supports repeat and random-seed options
- [ ] Report generator supports json, html, markdown formats
- [ ] Test report generates successfully
</success_criteria>

<output>
After completion, create `.planning/phases/208-integration-performance-testing/208-06-SUMMARY.md` with:
- Script count breakdown
- Usage examples for each script
- Test report sample output
- Any issues with script execution
</output>
