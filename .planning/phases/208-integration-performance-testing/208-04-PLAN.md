---
phase: 208-integration-performance-testing
plan: 04
type: execute
wave: 2
depends_on: [208-01]
files_modified:
  - backend/tests/integration/quality/test_flakiness_detection.py
  - backend/tests/integration/quality/test_test_isolation.py
  - backend/tests/integration/quality/test_collection_stability.py
  - backend/tests/integration/quality/conftest.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - Flakiness detection tests identify tests that fail intermittently
    - Test isolation tests verify no shared state between tests
    - Collection stability tests verify tests collect consistently
    - All quality tests use pytest-randomly for order randomization
    - Quality metrics tracked for test suite health
  artifacts:
    - path: backend/tests/integration/quality/test_flakiness_detection.py
      provides: Flakiness detection tests
      min_lines: 100
    - path: backend/tests/integration/quality/test_test_isolation.py
      provides: Test isolation verification
      min_lines: 80
    - path: backend/tests/integration/quality/test_collection_stability.py
      provides: Collection stability verification
      min_lines: 60
    - path: backend/tests/integration/quality/conftest.py
      provides: Quality test fixtures
      contains: repeat_fixture, random_seed_fixture
  key_links:
    - from: test_flakiness_detection.py
      to: pytest tests
      via: pytest_repeat plugin
      pattern: @pytest.mark.repeat\(5\)
    - from: test_test_isolation.py
      to: database sessions and caches
      via: fixture verification
      pattern: def test_no_shared_state\(db_session\):
    - from: test_collection_stability.py
      to: pytest collection
      via: subprocess execution
      pattern: subprocess.run\(\[.*pytest.*--collect-only\]\)
---

<objective>
Create test quality infrastructure to detect flaky tests, verify test isolation, and ensure collection stability. These tests use pytest-randomly, pytest-rerunfailures, and custom verification scripts to maintain test suite health and prevent regressions in test infrastructure.

Purpose: Test quality metrics are critical for maintaining a reliable test suite. Flaky tests erode developer trust, shared state causes intermittent failures, and collection errors block CI pipelines. This infrastructure ensures test suite health is measurable and maintainable.

Output: 3 quality test files with 10-15 tests verifying flakiness detection, test isolation, and collection stability.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/208-integration-performance-testing/208-RESEARCH.md
@backend/pytest.ini
@backend/tests/integration/conftest.py
@backend/requirements-testing.txt
</context>

<tasks>

<task type="auto">
  <name>Create quality test fixtures (conftest.py)</name>
  <files>backend/tests/integration/quality/conftest.py</files>
  <action>
    Create shared fixtures for test quality verification:

    1. random_seed fixture:
       - Generates consistent random seed for reproducible test runs
       - Use pytest.mark.randomly fixture
       - Log seed for debugging flaky tests

    2. repeat_count fixture:
       - Configurable repeat count for flakiness detection
       - Default: 5 iterations
       - Override via pytest mark: @pytest.mark.repeat(10)

    3. test_database fixture:
       - Isolated database for testing test isolation
       - Verify no data leaks between tests
       - Clean schema before each quality test

    4. clean_cache fixture:
       - Clear all caches before quality tests
       - governance_cache, lru_cache, etc.
       - Verify cache isolation

    5. subprocess_runner fixture:
       - Helper for running pytest in subprocess
       - Used for collection stability tests
       - Capture stdout/stderr for analysis

    Reference: backend/tests/integration/conftest.py for existing patterns
    Use pytest-rerunfailures configuration from pytest.ini
  </action>
  <verify>
    pytest tests/integration/quality/conftest.py --collect-only shows fixtures available
  </verify>
  <done>
    Shared fixtures created with random seed, repeat count, isolated database, cache clearing, and subprocess runner
  </done>
</task>

<task type="auto">
  <name>Create flakiness detection tests</name>
  <files>backend/tests/integration/quality/test_flakiness_detection.py</files>
  <action>
    Create tests to detect flaky test behavior:

    Test class: TestFlakinessDetection

    1. test_deterministic_test_repeat (repeat=5)
       - Create deterministic test (always passes)
       - Run 5 times with pytest-repeat
       - Verify: 5/5 passes (100% consistency)

    2. test_randomness_aware_test (repeat=5)
       - Create test using random data
       - Use random_seed fixture for reproducibility
       - Verify: 5/5 passes (randomness controlled)

    3. test_async_test_determinism (repeat=5, asyncio)
       - Create async test with potential race conditions
       - Use proper async fixtures
       - Verify: 5/5 passes (no race conditions)

    4. test_database_rollback_determinism (repeat=5)
       - Create test modifying database
       - Verify database clean after each run
       - Verify: 5/5 passes (no data leakage)

    5. test_mock_configuration_determinism (repeat=5)
       - Create test with mocked dependencies
       - Verify mocks reset between runs
       - Verify: 5/5 passes (no mock state leakage)

    6. measure_flakiness_rate
       - Run sample of integration tests 5 times each
       - Calculate flakiness rate (failures / total runs)
       - Fail if flakiness rate >5% (acceptable threshold)
       - Tests: workflow_engine_e2e, episode_segmentation_e2e

    Use @pytest.mark.repeat(5) for repeated execution
    Use pytest-randomly for order randomization
    Log any inconsistent behavior
  </action>
  <verify>
    pytest tests/integration/quality/test_flakiness_detection.py -v --count=5
    All 6 tests pass, flakiness rate <5%
  </verify>
  <done>
    6 flakiness detection tests with repeat execution and flakiness rate measurement
  </done>
</task>

<task type="auto">
  <name>Create test isolation verification tests</name>
  <files>backend/tests/integration/quality/test_test_isolation.py</files>
  <action>
    Create tests to verify test isolation:

    Test class: TestIsolation

    1. test_database_isolation
       - Create test that inserts data into database
       - Create second test that verifies no data exists
       - Run in sequence with pytest-randomly
       - Verify: No data leakage between tests

    2. test_cache_isolation
       - Create test that populates governance_cache
       - Create second test that verifies cache is empty
       - Verify: Cache cleared between tests

    3. test_mock_isolation
       - Create test with configured AsyncMock
       - Create second test verifying mock not configured
       - Verify: Mock state not shared

    4. test_fixture_isolation
       - Create test using session-scoped fixture
       - Create test using function-scoped fixture
       - Verify: Function fixtures isolated, session fixtures shared

    5. test_global_state_isolation
       - Create test that sets global variable
       - Create second test verifying global is None
       - Verify: Global state not leaked

    6. verify_asyncio_cleanup
       - Create async test with pending tasks
       - Verify all tasks cleaned up after test
       - Verify: No asyncio warnings

    Use pytest-randomly for random order execution
    Use subprocess to run tests in separate process
    Check for shared state violations
  </action>
  <verify>
    pytest tests/integration/quality/test_test_isolation.py -v --randomly-seed=1234
    All 6 tests pass, no shared state detected
  </verify>
  <done>
    6 test isolation verification tests covering database, cache, mocks, fixtures, global state, and asyncio
  </done>
</task>

<task type="auto">
  <name>Create collection stability verification tests</name>
  <files>backend/tests/integration/quality/test_collection_stability.py</files>
  <action>
    Create tests to verify collection stability:

    Test class: TestCollectionStability

    1. test_collection_no_errors
       - Run pytest --collect-only on integration tests
       - Verify: Zero collection errors
       - Check stderr for import errors

    2. test_collection_consistency (repeat=3)
       - Run pytest --collect-only 3 times
       - Compare test counts between runs
       - Verify: Same test count each time (no variance)

    3. test_collection_order_independence
       - Run pytest --collect-only with --randomly-seed
       - Run again with different seed
       - Verify: Same tests collected regardless of order

    4. test_import_order_independence
       - Run tests with different import orders
       - Use PYTHONPATH variations
       - Verify: All tests collect regardless

    5. measure_collection_time
       - Time pytest --collect-only execution
       - Verify: Collection completes in <10 seconds
       - Log collection time for trend tracking

    Use subprocess.run() for isolation
    Parse pytest output for test counts
    Compare results between runs
  </action>
  <verify>
    pytest tests/integration/quality/test_collection_stability.py -v
    All 5 tests pass, collection stable and consistent
  </verify>
  <done>
    5 collection stability verification tests covering errors, consistency, order independence, and performance
  </done>
</task>

</tasks>

<verification>
Run quality test suite:
```bash
cd backend
pytest tests/integration/quality/ -v --randomly-seed=1234
```

Expected results:
- 16 quality tests created (6 flakiness + 6 isolation + 4 collection)
- All tests pass (100% pass rate)
- Execution time <5 minutes

Run flakiness detection on integration tests:
```bash
cd backend
pytest tests/integration/workflows/ --count=5 --randomly
```

Expected results:
- All integration tests pass consistently (5/5 runs)
- Flakiness rate <5%

Run collection stability check:
```bash
cd backend
for i in {1..3}; do pytest tests/integration/ --collect-only | grep "test session starts"; done
```

Expected results:
- Same test count each run (variance = 0)
- Zero collection errors
</verification>

<success_criteria>
- [ ] 3 quality test files created
- [ ] 16 quality tests covering flakiness, isolation, and collection
- [ ] All quality tests pass with 100% consistency
- [ ] Flakiness rate <5% on integration test sample
- [ ] Collection stable across 3 runs (variance = 0)
- [ ] Test isolation verified (no shared state)
- [ ] Execution time <5 minutes for full suite
</success_criteria>

<output>
After completion, create `.planning/phases/208-integration-performance-testing/208-04-SUMMARY.md` with:
- Quality test count breakdown
- Flakiness rate measurements
- Collection stability metrics
- Any isolation issues detected
- Recommendations for test quality improvements
</output>
