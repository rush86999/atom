---
phase: 129-backend-critical-error-paths
plan: 04
type: execute
wave: 2
depends_on: ["129-02"]
files_modified:
  - backend/tests/critical_error_paths/test_external_service_timeouts.py
autonomous: true

must_haves:
  truths:
    - External service timeouts trigger fallback to next provider
    - HTTP timeout errors are caught and handled gracefully
    - Circuit breaker engages after repeated timeout failures
    - Multiple provider fallback works correctly
  artifacts:
    - path: backend/tests/critical_error_paths/test_external_service_timeouts.py
      provides: External service timeout test suite with respx mocking
      min_lines: 250
  key_links:
    - from: backend/tests/critical_error_paths/test_external_service_timeouts.py
      to: backend/core/llm/byok_handler.py
      via: BYOKHandler class
      pattern: "BYOKHandler|generate_response"
    - from: backend/tests/critical_error_paths/test_external_service_timeouts.py
      to: backend/core/auto_healing.py
      via: CircuitBreaker for external services
      pattern: "CircuitBreaker|auto_healing_engine"
---

<objective>
Create comprehensive test suite for external service timeout scenarios with circuit breaker verification.

**Purpose**: Validate that external service (LLM provider) timeouts are handled gracefully with circuit breaker engagement, fallback to secondary providers, and proper error propagation.

**Output**: Test suite using respx for HTTP mocking to simulate timeouts, verify circuit breaker integration, and test multi-provider fallback scenarios.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/129-backend-critical-error-paths/129-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/llm/byok_handler.py
@backend/core/auto_healing.py
@backend/tests/failure_modes/test_provider_failures.py
@backend/tests/failure_modes/test_network_timeouts.py
@backend/tests/failure_modes/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Implement external service timeout tests with respx</name>
  <files>backend/tests/critical_error_paths/test_external_service_timeouts.py</files>
  <action>
    Create backend/tests/critical_error_paths/test_external_service_timeouts.py with HTTP timeout tests.

    PREREQUISITE: Install respx if not available (check if in requirements.txt).

    Test classes and methods:

    **TestHTTPTimeouts:**
    - `test_llm_provider_timeout_handling` - httpx.TimeoutException via respx
    - `test_read_timeout_handling` - Read operation timeout
    - `test_connect_timeout_handling` - Connection timeout
    - `test_request_timeout_with_partial_response` - Timeout mid-response

    **TestCircuitBreakerIntegration:**
    - `test_circuit_breaker_opens_on_timeouts` - Multiple timeouts open circuit
    - `test_circuit_breaker_prevents_calls_after_timeout` - No calls after OPEN
    - `test_circuit_breaker_half_open_timeout_retry` - Retry after timeout period
    - `test_per_service_circuit_breakers` - Different breakers per provider

    **TestProviderFallback:**
    - `test_primary_timeout_secondary_succeeds` - Fallback on timeout
    - `test_all_providers_timeout` - All providers timeout
    - `test_timeout_then_retry_on_same_provider` - Retry same provider after delay
    - `test_cascading_timeout_failures` - Each provider times out sequentially

    **TestTimeoutErrorPropagation:**
    - `test_timeout_converts_to_atom_exception` - Timeout -> AtomException
    - `test_timeout_error_response_format` - Correct API error format
    - `test_timeout_logged_correctly` - Logging verification

    **TestEdgeCases:**
    - `test_very_short_timeout` - 1ms timeout handling
    - `test_very_long_timeout` - 5+ minute timeout
    - `test_timeout_during_streaming` - Timeout during response streaming
    - `test_concurrent_timeout_requests` - Multiple requests timeout together

    Use respx for HTTP mocking (per RESEARCH.md recommendation):
    ```python
    @respx.mock
    def test_llm_provider_timeout_handling(self):
        respx.get("https://api.openai.com/v1/chat/completions").mock(
            side_effect=httpx.TimeoutException("Request timed out")
        )
        handler = BYOKHandler(workspace_id="test")
        with pytest.raises((httpx.TimeoutException, Exception)):
            await handler.generate("test prompt")
    ```

    For async tests: use @pytest.mark.asyncio and async def test methods.

    Use existing patterns from test_provider_failures.py but add respx for more realistic HTTP mocking.

    CRITICAL: Mock at the httpx/HTTP client level, not the BYOKHandler method, to test actual timeout handling.
  </action>
  <verify>
    Run: pytest backend/tests/critical_error_paths/test_external_service_timeouts.py -v
    Expected: All 20+ tests pass, HTTP timeouts correctly mocked
  </verify>
  <done>
    20+ tests covering HTTP timeouts, circuit breaker, and fallback
  </done>
</task>

</tasks>

<verification>
After completion, verify:
1. HTTP timeouts are properly mocked using respx
2. Circuit breaker integrates with timeout handling
3. Fallback logic works correctly
4. Async timeout scenarios are tested
5. No real HTTP requests are made during tests
</verification>

<success_criteria>
1. 20+ tests covering all timeout scenarios
2. 100% test pass rate
3. Circuit breaker state transitions verified
4. Fallback logic validated
5. Test suite runs in under 30 seconds (mocked timeouts, no real delays)
</success_criteria>

<output>
After completion, create `.planning/phases/129-backend-critical-error-paths/129-04-SUMMARY.md` with:
- Timeout handling validation results
- Circuit breaker integration status
- Any issues with fallback logic
</output>
