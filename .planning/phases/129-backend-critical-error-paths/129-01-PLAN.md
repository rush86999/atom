---
phase: 129-backend-critical-error-paths
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/critical_error_paths/conftest.py
  - backend/tests/critical_error_paths/test_database_connection_failures.py
autonomous: true

must_haves:
  truths:
    - Database connection failures trigger retry logic
    - Connection pool exhaustion is handled gracefully
    - Deadlock errors result in retry attempts
    - Database errors propagate through service layer correctly
  artifacts:
    - path: backend/tests/critical_error_paths/test_database_connection_failures.py
      provides: Database connection failure test suite
      min_lines: 200
    - path: backend/tests/critical_error_paths/conftest.py
      provides: Shared fixtures for critical error testing
      min_lines: 100
  key_links:
    - from: backend/tests/critical_error_paths/test_database_connection_failures.py
      to: backend/core/database.py
      via: get_db_session context manager
      pattern: "get_db_session|create_engine"
    - from: backend/tests/critical_error_paths/test_database_connection_failures.py
      to: backend/core/exceptions.py
      via: DatabaseConnectionError exception
      pattern: "DatabaseConnectionError|OperationalError"
---

<objective>
Create comprehensive test suite for database connection failure scenarios with retry logic validation.

**Purpose**: Ensure the application handles database connection failures gracefully with proper retry logic, connection pool management, and error propagation through the service layer.

**Output**: Test suite validating connection refused, pool exhaustion, deadlock handling, and timeout scenarios with existing SQLAlchemy error injection fixtures.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/129-backend-critical-error-paths/129-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/database.py
@backend/core/exceptions.py
@backend/core/auto_healing.py
@backend/tests/error_paths/conftest.py
@backend/tests/failure_modes/conftest.py
@backend/tests/error_paths/test_database_error_paths.py
</context>

<tasks>

<task type="auto">
  <name>Create critical error paths conftest with database failure fixtures</name>
  <files>backend/tests/critical_error_paths/conftest.py</files>
  <action>
    Create backend/tests/critical_error_paths/conftest.py with shared fixtures for critical error testing.

    Fixtures to implement:
    1. `db_session_with_retry` - Database session that tracks retry attempts
    2. `mock_connection_failure` - Patch engine.connect to raise OperationalError
    3. `mock_pool_exhaustion` - Simulate connection pool exhaustion
    4. `mock_deadlock_scenario` - Mock deadlock OperationalError
    5. `track_retry_attempts` - Fixture to count retry calls

    Use existing patterns from backend/tests/error_paths/conftest.py and backend/tests/failure_modes/conftest.py.

    Key imports:
    - pytest, sqlalchemy.exc (OperationalError, IntegrityError, DBAPIError)
    - unittest.mock (patch, MagicMock)
    - core.database.get_db_session
    - core.exceptions.DatabaseConnectionError

    DO NOT rebuild existing fixtures - extend and specialize them for critical path testing.
  </action>
  <verify>
    Run: pytest backend/tests/critical_error_paths/conftest.py --collect-only
    Expected: Fixtures registered (no collection errors)
  </verify>
  <done>
    Fixtures exist and import successfully from other test files
  </done>
</task>

<task type="auto">
  <name>Implement database connection failure tests</name>
  <files>backend/tests/critical_error_paths/test_database_connection_failures.py</files>
  <action>
    Create backend/tests/critical_error_paths/test_database_connection_failures.py with comprehensive connection failure tests.

    Test classes and methods:

    **TestConnectionRefused:**
    - `test_connection_refused_triggers_retry` - Verify retry logic on connection refused
    - `test_connection_refused_eventually_succeeds` - Mock fail-then-succeed pattern
    - `test_connection_refused_max_retries_exceeded` - Verify max retry limit

    **TestConnectionPool:**
    - `test_pool_exhaustion_handling` - Test pool exhausted OperationalError
    - `test_pool_recovery_after_exhaustion` - Verify pool can recover
    - `test_concurrent_connection_limit` - Test multiple simultaneous connections

    **TestDeadlock:**
    - `test_deadlock_triggers_retry` - Deadlock OperationalError triggers retry
    - `test_deadlock_retry_with_backoff` - Verify exponential backoff on deadlock
    - `test_deadlock_max_retries` - Deadlock stops after max attempts

    **TestConnectionTimeout:**
    - `test_connection_timeout_handling` - Timeout OperationalError handling
    - `test_timeout_recovery` - Recovery after timeout

    **TestErrorPropagation:**
    - `test_database_error_to_atom_exception` - SQLAlchemy to AtomException conversion
    - `test_connection_error_propagates_to_api` - Service -> API error propagation

    Use fixtures from conftest.py and existing error_paths/test_database_error_paths.py patterns.

    For retry verification: use `@retry_with_backoff` decorator from core.auto_healing if service uses it, or mock retry logic.

    CRITICAL: Patch where code USES the database, not where it's DEFINED (e.g., 'core.services.get_db_session', not 'core.database.get_db_session').
  </action>
  <verify>
    Run: pytest backend/tests/critical_error_paths/test_database_connection_failures.py -v
    Expected: All tests pass, no connection errors to test database itself
  </verify>
  <done>
    15+ tests covering connection refused, pool exhaustion, deadlock, timeout, and error propagation
  </done>
</task>

</tasks>

<verification>
After completion, verify:
1. All database connection failure tests pass
2. Retry logic is exercised (mock verify call counts)
3. Error propagation path is validated (exception -> service -> API)
4. No actual database connections are made during tests (all mocked)
5. Tests complete in under 30 seconds (no time.sleep without mock)
</verification>

<success_criteria>
1. 15+ tests covering 5 critical database failure scenarios
2. 100% test pass rate with mocked database failures
3. Retry logic validated with call count verification
4. Error propagation verified from SQLAlchemy to AtomException to HTTP response
5. Test suite runs in under 30 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/129-backend-critical-error-paths/129-01-SUMMARY.md` with:
- Tests created and their coverage
- Any gaps discovered in existing retry logic
- Measurement of actual retry behavior vs expected
</output>
