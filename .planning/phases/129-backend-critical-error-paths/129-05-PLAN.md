---
phase: 129-backend-critical-error-paths
plan: 05
type: execute
wave: 3
depends_on: ["129-01", "129-02", "129-03", "129-04"]
files_modified:
  - backend/tests/critical_error_paths/test_error_propagation_e2e.py
  - backend/tests/critical_error_paths/test_graceful_degradation.py
autonomous: true

must_haves:
  truths:
    - Errors propagate correctly from service layer to API to client
    - Graceful degradation maintains partial functionality during failures
    - Error responses follow standardized format (error_code, message, details)
    - Critical paths have fallback behavior when dependencies fail
  artifacts:
    - path: backend/tests/critical_error_paths/test_error_propagation_e2e.py
      provides: End-to-end error propagation test suite
      min_lines: 200
    - path: backend/tests/critical_error_paths/test_graceful_degradation.py
      provides: Graceful degradation test suite
      min_lines: 200
  key_links:
    - from: backend/tests/critical_error_paths/test_error_propagation_e2e.py
      to: backend/core/error_handlers.py
      via: global_exception_handler and atom_exception_handler
      pattern: "global_exception_handler|atom_exception_handler|api_error"
    - from: backend/tests/critical_error_paths/test_error_propagation_e2e.py
      to: backend/core/exceptions.py
      via: AtomException hierarchy
      pattern: "AtomException|DatabaseConnectionError|LLMRateLimitError"
    - from: backend/tests/critical_error_paths/test_graceful_degradation.py
      to: backend/api/
      via: FastAPI endpoints
      pattern: "router|endpoint|@app\."
---

<objective>
Create comprehensive end-to-end tests for error propagation and graceful degradation verification.

**Purpose**: Validate that errors propagate correctly through the entire stack (service -> API -> client) with standardized response format, and that the system gracefully degrades when critical dependencies fail.

**Output**: Two test suites validating error propagation paths and graceful degradation scenarios for all critical system paths.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/phases/129-backend-critical-error-paths/129-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/error_handlers.py
@backend/core/exceptions.py
@backend/main.py
@backend/tests/error_paths/conftest.py
@backend/tests/failure_modes/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Implement end-to-end error propagation tests</name>
  <files>backend/tests/critical_error_paths/test_error_propagation_e2e.py</files>
  <action>
    Create backend/tests/critical_error_paths/test_error_propagation_e2e.py with E2E error propagation tests.

    Test classes and methods:

    **TestServiceToAPIErrorPropagation:**
    - `test_database_error_to_500_response` - DatabaseConnectionError -> 500
    - `test_validation_error_to_400_response` - ValidationError -> 400
    - `test_not_found_error_to_404_response` - NotFoundError -> 404
    - `test_permission_denied_to_403_response` - ForbiddenError -> 403
    - `test_rate_limit_to_429_response` - LLMRateLimitError -> 429

    **TestErrorResponseFormat:**
    - `test_standard_error_response_fields` - Verify all fields present
    - `test_error_code_present` - error_code field in response
    - `test_message_field_present` - message field in response
    - `test_timestamp_field_present` - timestamp field in response
    - `test_details_field_optional` - details field when available

    **TestAtomExceptionPropagation:**
    - `test_atom_exception_caught_by_handler` - AtomException -> atom_exception_handler
    - `test_atom_exception_severity_mapping` - Severity to HTTP status code
    - `test_atom_exception_details_in_response` - Details passed through
    - `test_atom_exception_request_id_tracing` - Request ID in error response

    **TestGlobalExceptionHandling:**
    - `test_uncaught_exception_to_500` - Generic Exception -> 500
    - `test_tracebag_hidden_in_production` - No traceback in production
    - `test_traceback_visible_in_development` - Traceback in development mode
    - `test_request_id_logged_in_error` - Request ID in logs

    **TestErrorResponseConsistency:**
    - `test_all_errors_have_success_false` - success: false in all errors
    - `test_error_codes_match_enum` - error_code valid ErrorCode enum value
    - `test_content_type_json` - Content-Type: application/json
    - `test_cors_headers_on_error` - CORS headers present on error responses

    Use FastAPI TestClient for end-to-end testing:
    ```python
    from fastapi.testclient import TestClient
    from main import app

    def test_database_error_to_500_response(client):
        with patch('core.database.get_db_session',
                  side_effect=DatabaseConnectionError("Connection failed")):
            response = client.get("/api/v1/agents")
        assert response.status_code == 500
        assert "error_code" in response.json()
        assert response.json()["error_code"] == "DB_6002"
    ```

    Use existing error_handlers.py global_exception_handler and atom_exception_handler.

    Test both AtomException paths (if available) and legacy error paths.
  </action>
  <verify>
    Run: pytest backend/tests/critical_error_paths/test_error_propagation_e2e.py -v
    Expected: All 20+ tests pass, error format validated
  </verify>
  <done>
    20+ tests covering error propagation and response format
  </done>
</task>

<task type="auto">
  <name>Implement graceful degradation tests</name>
  <files>backend/tests/critical_error_paths/test_graceful_degradation.py</files>
  <action>
    Create backend/tests/critical_error_paths/test_graceful_degradation.py with graceful degradation tests.

    Test classes and methods:

    **TestLLMServiceDegradation:**
    - `test_llm_fallback_returns_cached_response` - Fallback to cached response
    - `test_llm_fallback_returns_error_message` - Clear error when all fail
    - `test_llm_partial_stream_timeout_handling` - Handle timeout mid-stream
    - `test_llm_rate_limit_graceful_degradation` - 429 doesn't crash app

    **TestDatabaseDegradation:**
    - `test_cache_works_during_db_failure` - Cache operational when DB down
    - `test_read_only_mode_on_write_failure` - Read succeeds, writes fail gracefully
    - `test_connection_pool_recovery` - Pool recovers after outage
    - `test_session_cleanup_after_error` - Session usable after error

    **TestGovernanceDegradation:**
    - `test_governance_cache_miss_fallback` - Fallback on cache miss
    - `test_governance_default_to_safe_denies` - Deny when governance unavailable
    - `test_governance_logging_continues_during_error` - Errors logged, app continues

    **TestCanvasDegradation:**
    - `test_canvas_render_failure_fallback` - Fallback component on render fail
    - `test_canvas_state_preserved_on_error` - State saved even if render fails
    - `test_canvas_partial_render` - Partial render when some components fail

    **TestAPIEndpointDegradation:**
    - `test_health_check_always_responds` - Health check works during outages
    - `test_metrics_endpoint_resilient` - Metrics work during partial outage
    - `test_non_critical_endpoints_fail_gracefully` - Clear error, no crash

    **TestMultiServiceFailure:**
    - `test_db_and_llm_simultaneous_failure` - Multiple dependencies down
    - `test_cascade_failure_prevention` - Circuit breaker prevents cascade
    - `test_system_recovers_after_outage` - Full recovery after dependencies return

    Use existing fixtures: assert_graceful_degradation from conftest.py.

    Test pattern:
    ```python
    def test_cache_works_during_db_failure(self, db_session):
        from core.governance_cache import GovernanceCache
        cache = GovernanceCache(max_size=100, ttl_seconds=60)
        # Pre-populate cache
        cache.set("test-agent", "stream_chat", {"allowed": True})
        # Simulate DB failure
        with patch('core.database.get_db_session',
                  side_effect=OperationalError("Connection failed")):
            # Cache should still work
            result = cache.get("test-agent", "stream_chat")
            assert result is not None
            assert result["allowed"] == True
    ```

    Focus on behavior testing, not implementation: verify system continues operating (possibly in degraded mode) rather than crashing.
  </action>
  <verify>
    Run: pytest backend/tests/critical_error_paths/test_graceful_degradation.py -v
    Expected: All 20+ tests pass, degraded behavior verified
  </verify>
  <done>
    20+ tests covering graceful degradation across all services
  </done>
</task>

</tasks>

<verification>
After completion, verify:
1. Error propagation path tested from service to API to client
2. All error response formats follow standard (error_code, message, details, timestamp)
3. Graceful degradation verified for all critical paths
4. System continues operating (possibly degraded) during failures
5. Health checks and metrics remain available during outages
</verification>

<success_criteria>
1. 40+ tests covering error propagation (20+) and graceful degradation (20+)
2. 100% test pass rate
3. All critical error paths validated end-to-end
4. Error response format standardized
5. Graceful degradation verified for all services
</success_criteria>

<output>
After completion, create `.planning/phases/129-backend-critical-error-paths/129-05-SUMMARY.md` with:
- Error propagation validation results
- Graceful degradation coverage matrix
- Any gaps in error handling discovered
- Recommendations for improving error resilience
</output>
