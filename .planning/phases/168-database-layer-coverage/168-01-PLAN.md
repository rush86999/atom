---
phase: 168-database-layer-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/database/test_core_models.py
  - backend/tests/factories/core_factory.py
autonomous: true

must_haves:
  truths:
    - "Core models (AgentRegistry, User, Workspace, Team, Tenant) can be created via factories"
    - "Core models have CRUD operations tested with 80%+ line coverage"
    - "Core model relationships (one-to-many, many-to-many) work bidirectionally"
    - "Core model constraints (unique, not null) are validated"
    - "Core model JSON fields serialize/deserialize correctly"
  artifacts:
    - path: "backend/tests/database/test_core_models.py"
      provides: "Core model CRUD, relationship, and constraint tests"
      min_lines: 500
    - path: "backend/tests/factories/core_factory.py"
      provides: "Factory Boy factories for core models"
      exports: ["WorkspaceFactory", "TeamFactory", "TenantFactory", "UserAccountFactory"]
  key_links:
    - from: "backend/tests/database/test_core_models.py"
      to: "backend/core/models.py"
      via: "direct model imports and factory usage"
      pattern: "from core.models import .*"
    - from: "backend/tests/factories/core_factory.py"
      to: "backend/tests/factories/base.py"
      via: "BaseFactory inheritance"
      pattern: "class.*Factory\\(BaseFactory\\)"
---

<objective>
Extend existing core model test coverage to 80%+ by adding comprehensive CRUD, relationship, and constraint tests for high-value core models (AgentRegistry, User, Workspace, Team, Tenant, UserAccount, OAuthToken, ChatSession, ChatMessage).

Purpose: Core models are foundational to the entire application - they handle agents, users, workspaces, authentication, and real-time communication. Achieving 80%+ coverage on these models ensures data integrity, prevents regressions, and validates the ORM patterns used throughout the codebase.

Output: Comprehensive test file `test_core_models.py` with 500+ lines covering CRUD operations, relationships, constraints, and JSON field handling for all core models.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/168-database-layer-coverage/168-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/tests/database/test_database_models.py
@backend/tests/conftest.py
@backend/tests/factories/agent_factory.py
@backend/tests/factories/user_factory.py
@backend/core/models.py
</context>

<tasks>

<task type="auto">
  <name>Create core_factory.py with missing core model factories</name>
  <files>backend/tests/factories/core_factory.py</files>
  <action>
Create `backend/tests/factories/core_factory.py` with factories for core models that don't have factories yet:

1. **WorkspaceFactory** - for Workspace model
   - Required: name, status (default: "active")
   - Optional: description, plan_tier, is_startup, metadata_json

2. **TeamFactory** - for Team model
   - Required: name, workspace_id
   - Optional: description
   - Use WorkspaceFactory for workspace relationship

3. **TenantFactory** - for Tenant model
   - Required: name, subdomain
   - Optional: plan_type, edition, memory_limit_mb

4. **UserAccountFactory** - for UserAccount model (IM platform linking)
   - Required: user_id, tenant_id, platform
   - Optional: platform_user_id, chat_id, username

5. **OAuthTokenFactory** - for OAuthToken model
   - Required: user_id, provider, token
   - Optional: refresh_token, expires_at

6. **ChatSessionFactory** - for ChatSession model
   - Required: user_id (optional for anonymous)
   - Optional: agent_id, title, status

7. **ChatMessageFactory** - for ChatMessage model
   - Required: session_id, role, content
   - Optional: metadata_json

Follow the existing factory pattern from `agent_factory.py`:
- Inherit from BaseFactory
- Use factory.Faker for dynamic data
- Use factory.LazyFunction for defaults
- Set `_session` parameter handling in Meta class

Reference existing factories in `backend/tests/factories/agent_factory.py` and `user_factory.py` for patterns.
  </action>
  <verify>
```bash
cd /Users/rushiparikh/projects/atom/backend && python -c "
from tests.factories.core_factory import (
    WorkspaceFactory, TeamFactory, TenantFactory,
    UserAccountFactory, OAuthTokenFactory, ChatSessionFactory, ChatMessageFactory
)
print('All factories imported successfully')
"
```
  </verify>
  <done>
All 7 factories can be imported without errors, each follows BaseFactory pattern with proper field defaults and Faker data generation.
  </done>
</task>

<task type="auto">
  <name>Create test_core_models.py with comprehensive coverage</name>
  <files>backend/tests/database/test_core_models.py</files>
  <action>
Create `backend/tests/database/test_core_models.py` with comprehensive test coverage for core models. Organize into test classes by model group:

**Class: TestWorkspaceModels**
- `test_workspace_create_with_defaults` - Create workspace with minimal fields
- `test_workspace_create_with_all_fields` - Create with all optional fields
- `test_workspace_tenant_relationship` - Test workspace belongs to tenant
- `test_workspace_teams_relationship` - Test workspace has many teams
- `test_workspace_products_relationship` - Test workspace has products
- `test_workspace_is_startup_property` - Test boolean property
- `test_workspace_metadata_json_field` - Test JSON serialization

**Class: TestTeamModels**
- `test_team_create_with_defaults` - Create team with workspace
- `test_team_members_relationship` - Test many-to-many with users
- `test_team_workspace_relationship` - Test team belongs to workspace
- `test_team_messages_relationship` - Test team has messages

**Class: TestTenantModels**
- `test_tenant_create_with_defaults` - Create tenant with required fields
- `test_tenant_edition_properties` - Test is_personal, is_enterprise properties
- `test_tenant_edition_display_name` - Test display name property
- `test_tenant_can_upgrade_method` - Test upgrade eligibility logic
- `test_tenant_push_tokens_cascade` - Test cascade delete to PushToken
- `test_tenant_settings_cascade` - Test cascade delete to TenantSetting

**Class: TestUserAccountModels**
- `test_user_account_create` - Create IM platform account link
- `test_user_account_unique_constraint` - Test platform+platform_user_id uniqueness
- `test_user_account_user_relationship` - Test belongs to user
- `test_user_account_tenant_relationship` - Test belongs to tenant
- `test_user_account_is_valid_property` - Test LinkToken is_valid property

**Class: TestOAuthTokenModels**
- `test_oauth_token_create` - Create OAuth token with provider
- `test_oauth_token_user_relationship` - Test token belongs to user
- `test_oauth_token_expires_at_field` - Test datetime handling
- `test_oauth_token_encrypted_token` - Test token is stored (encryption is handled at service layer)

**Class: TestChatModels**
- `test_chat_session_create` - Create chat session
- `test_chat_session_anonymous` - Test session without user
- `test_chat_session_messages_relationship` - Test session has messages
- `test_chat_message_create` - Create message with role/content
- `test_chat_message_session_relationship` - Test message belongs to session
- `test_chat_message_metadata_json` - Test JSON field handling

Use `db_session` fixture from conftest.py. Use the new factories from Task 1. Follow test patterns from `test_database_models.py` for structure.

Target: 500+ lines, 30+ tests, 80%+ model coverage.
  </action>
  <verify>
```bash
cd /Users/rushiparikh/projects/atom/backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/database/test_core_models.py -v --cov=core.models --cov-report=term-missing | grep -A5 "test_core_models"
```
  </verify>
  <done>
All 30+ tests pass, core models show 80%+ line coverage in coverage report. No test isolation failures (run twice to verify).
  </done>
</task>

<task type="auto">
  <name>Add constraint and validation tests for core models</name>
  <files>backend/tests/database/test_core_models.py</files>
  <action>
Extend `test_core_models.py` with constraint validation tests:

**Class: TestCoreModelConstraints**
- `test_workspace_name_not_null` - Verify workspace.name cannot be NULL
- `test_tenant_subdomain_unique` - Verify subdomain uniqueness (IntegrityError on duplicate)
- `test_user_email_unique` - Verify email uniqueness (already exists, keep or extend)
- `test_user_account_platform_user_id_unique` - Verify composite unique constraint
- `test_tenant_setting_workspace_key_unique` - Verify TenantSetting composite unique

**Class: TestCoreModelCascadeBehaviors**
- `test_tenant_delete_cascades_to_workspaces` - Verify CASCADE delete
- `test_tenant_delete_cascades_to_users` - Verify CASCADE delete
- `test_workspace_delete_cascades_to_teams` - Verify teams deleted (may need manual delete)
- `test_user_delete_cascades_to_user_accounts` - Verify CASCADE delete
- `test_workspace_delete_nullifies_tenant_id` - Verify NULLIFY behavior

**Class: TestCoreModelJSONFields**
- `test_workspace_metadata_json_serialization` - Create with dict, retrieve as dict
- `test_workspace_metadata_json_null_handling` - Test NULL values
- `test_chat_message_metadata_json_complex` - Test nested JSON structures
- `test_user_preferences_json_field` - Test user.preferences JSON field

**Class: TestCoreModelTimestamps**
- `test_workspace_created_at_auto_generated` - Verify created_at is set
- `test_workspace_updated_at_on_update` - Verify updated_at changes on save
- `test_user_last_login_nullable` - Test nullable datetime field

Use pytest.raises(IntegrityError) for constraint violations. Use db_session.refresh() after updates to verify timestamp changes.

Target: Additional 200+ lines, 15+ tests.
  </action>
  <verify>
```bash
cd /Users/rushiparikh/projects/atom/backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/database/test_core_models.py::TestCoreModelConstraints -v && pytest tests/database/test_core_models.py::TestCoreModelCascadeBehaviors -v && pytest tests/database/test_core_models.py::TestCoreModelJSONFields -v
```
  </verify>
  <done>
All constraint tests properly raise IntegrityError where expected. Cascade behaviors match model configurations. JSON fields serialize/deserialize correctly. Timestamps auto-generate and update.
  </done>
</task>

</tasks>

<verification>
Run all core model tests and verify coverage:

```bash
cd /Users/rushiparikh/projects/atom/backend
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/database/test_core_models.py -v --cov=core.models --cov-report=term-missing --cov-report=json
```

Verify:
1. All tests pass (50+ tests total)
2. Core models show 80%+ line coverage
3. No test isolation failures (run suite twice)
4. Coverage JSON shows reduced uncovered lines in core/models.py
</verification>

<success_criteria>
1. 50+ tests covering Workspace, Team, Tenant, UserAccount, OAuthToken, ChatSession, ChatMessage models
2. 80%+ line coverage for core models (measured by coverage.py)
3. All relationships tested bidirectionally (parent -> children, child -> parent)
4. All unique constraints tested with IntegrityError
5. All cascade behaviors verified (delete, nullify)
6. JSON fields tested for serialization/deserialization
7. Timestamp auto-generation and update behavior verified
8. Test isolation confirmed (tests pass when run in any order)
</success_criteria>

<output>
After completion, create `.planning/phases/168-database-layer-coverage/168-01-SUMMARY.md` with:
- Tests created (count, file paths)
- Coverage achieved (line coverage % for core/models.py)
- Any issues found (model configuration problems, missing relationships)
- Next steps (what core models still need coverage)
</output>
