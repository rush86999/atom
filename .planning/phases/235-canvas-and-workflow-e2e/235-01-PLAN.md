---
phase: 235-canvas-and-workflow-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/e2e_ui/tests/canvas/test_canvas_chart_rendering.py
  - backend/tests/e2e_ui/tests/canvas/test_canvas_sheets_rendering.py
  - backend/tests/e2e_ui/tests/canvas/test_canvas_docs_rendering.py
  - backend/tests/e2e_ui/tests/canvas/test_canvas_email_rendering.py
  - backend/tests/e2e_ui/tests/canvas/test_canvas_terminal_rendering.py
  - backend/tests/e2e_ui/tests/canvas/test_canvas_coding_rendering.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Chart canvas renders correctly (line, bar, pie charts) - CANV-01"
    - "Sheet canvas displays data grid with pagination and sorting - CANV-02"
    - "Form canvas displays form with all field types (text, email, select, checkbox) - CANV-03"
    - "Docs canvas renders markdown content with formatting and links - CANV-04"
    - "Email canvas displays to, subject, body fields correctly - CANV-05"
    - "Terminal canvas displays command output in scrollable area - CANV-06"
    - "Coding canvas displays code with syntax highlighting - CANV-07"
  artifacts:
    - path: "backend/tests/e2e_ui/tests/canvas/test_canvas_chart_rendering.py"
      provides: "Chart canvas rendering E2E tests (CANV-01)"
      contains: "class TestChartCanvasRendering"
      min_lines: 120
    - path: "backend/tests/e2e_ui/tests/canvas/test_canvas_sheets_rendering.py"
      provides: "Sheet canvas rendering E2E tests (CANV-02)"
      contains: "class TestSheetCanvasRendering"
      min_lines: 100
    - path: "backend/tests/e2e_ui/tests/canvas/test_canvas_docs_rendering.py"
      provides: "Docs canvas rendering E2E tests (CANV-04)"
      contains: "class TestDocsCanvasRendering"
      min_lines: 80
    - path: "backend/tests/e2e_ui/tests/canvas/test_canvas_email_rendering.py"
      provides: "Email canvas rendering E2E tests (CANV-05)"
      contains: "class TestEmailCanvasRendering"
      min_lines: 80
    - path: "backend/tests/e2e_ui/tests/canvas/test_canvas_terminal_rendering.py"
      provides: "Terminal canvas rendering E2E tests (CANV-06)"
      contains: "class TestTerminalCanvasRendering"
      min_lines: 80
    - path: "backend/tests/e2e_ui/tests/canvas/test_canvas_coding_rendering.py"
      provides: "Coding canvas rendering E2E tests (CANV-07)"
      contains: "class TestCodingCanvasRendering"
      min_lines: 80
  key_links:
    - from: "backend/tests/e2e_ui/tests/canvas/test_canvas_chart_rendering.py"
      to: "backend/tools/canvas_tool.py"
      via: "Chart canvas presentation functions"
      pattern: "present_chart|present_line_chart|present_bar_chart"
    - from: "backend/tests/e2e_ui/tests/canvas/"
      to: "frontend-nextjs/components/canvas/"
      via: "Canvas component rendering verification"
      pattern: "data-canvas-id|data-testid.*canvas"
    - from: "backend/tests/e2e_ui/tests/canvas/"
      to: "backend/core/models.py"
      via: "CanvasAudit record verification"
      pattern: "CanvasAudit"
---

<objective>
Create comprehensive E2E tests for all 7 canvas types to verify correct rendering: chart (line, bar, pie), sheet (data grid), form (field types), docs (markdown), email (draft fields), terminal (output), and coding (syntax highlighting).

Purpose: Ensure all canvas types display correctly in the web UI with proper content rendering, component structure, and audit trail creation.
Output: E2E test files covering CANV-01, CANV-02, CANV-03 (rendering), CANV-04, CANV-05, CANV-06, CANV-07 requirements.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/235-canvas-and-workflow-e2e/235-RESEARCH.md
@.planning/phases/234-authentication-and-agent-e2e/234-05-SUMMARY.md
@.planning/phases/233-test-infrastructure-foundation/233-05-SUMMARY.md
@backend/tests/e2e_ui/fixtures/auth_fixtures.py
@backend/tests/e2e_ui/pages/page_objects.py
@backend/tools/canvas_tool.py
@backend/core/canvas_type_registry.py
@frontend-nextjs/components/canvas/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chart canvas rendering E2E tests (CANV-01)</name>
  <files>backend/tests/e2e_ui/tests/canvas/test_canvas_chart_rendering.py</files>
  <action>
    Create `backend/tests/e2e_ui/tests/canvas/test_canvas_chart_rendering.py` with chart rendering tests:

    1. **test_line_chart_renders_correctly(authenticated_page_api, db_session)**
       - Create line chart data: labels=['Jan', 'Feb', 'Mar'], values=[10, 20, 30]
       - Use trigger_canvas_chart() helper to simulate WebSocket event
       - Verify canvas host appears: page.locator('[data-canvas-id]').is_visible()
       - Verify chart type: page.locator('[data-chart-type="line"]').is_visible()
       - Verify data points rendered (check for SVG or canvas elements)
       - Verify CanvasAudit record created in database

    2. **test_bar_chart_renders_correctly(authenticated_page_api, db_session)**
       - Create bar chart data: categories=['A', 'B', 'C'], values=[15, 25, 35]
       - Trigger bar chart presentation
       - Verify canvas visible
       - Verify chart type: [data-chart-type="bar"]
       - Verify bars rendered (count matches data length)
       - Verify audit record

    3. **test_pie_chart_renders_correctly(authenticated_page_api, db_session)**
       - Create pie chart data: labels=['X', 'Y', 'Z'], values=[30, 40, 30]
       - Trigger pie chart presentation
       - Verify canvas visible
       - Verify chart type: [data-chart-type="pie"]
       - Verify slices rendered (3 slices for 3 data points)
       - Verify audit record

    4. **test_chart_title_and_labels_display(authenticated_page_api)**
       - Create chart with title="Sales Report", x_label="Month", y_label="Revenue"
       - Trigger chart presentation
       - Verify title visible: page.locator('text=Sales Report').is_visible()
       - Verify axis labels visible
       - Verify legend displayed (if applicable)

    5. **test_multiple_charts_can_render(authenticated_page_api)**
       - Trigger line chart, wait, then trigger bar chart
       - Verify both canvas elements visible
       - Verify different canvas_id values
       - Close first chart, verify second still visible

    Helper functions (add to file or import from shared fixtures):
    ```python
    import uuid
    from datetime import datetime

    def trigger_canvas_chart(page, chart_type: str, data: dict, title: str = "Test Chart") -> str:
        """Simulate WebSocket canvas:update event for chart."""
        canvas_id = f"chart-{str(uuid.uuid4())[:8]}"

        canvas_message = {
            "type": "canvas:update",
            "canvas_id": canvas_id,
            "data": {
                "component": "chart",
                "chart_type": chart_type,
                "title": title,
                "data": data
            }
        }

        page.evaluate(f"(msg) => window.lastCanvasMessage = msg", canvas_message)
        page.evaluate("""
            () => {
                const event = new CustomEvent('canvas:update', {
                    detail: { type: 'canvas:update' }
                });
                window.dispatchEvent(event);
            }
        """)

        return canvas_id

    def create_line_chart_data() -> dict:
        return {
            "labels": ["Jan", "Feb", "Mar", "Apr"],
            "datasets": [{
                "label": "Sales",
                "data": [10, 20, 30, 40]
            }]
        }
    ```

    Use `authenticated_page_api` fixture from `tests/e2e_ui/fixtures/auth_fixtures.py`.
    Use `db_session` fixture for CanvasAudit verification.
  </action>
  <verify>pytest backend/tests/e2e_ui/tests/canvas/test_canvas_chart_rendering.py -v</verify>
  <done>5 tests pass: line chart, bar chart, pie chart, title/labels, multiple charts</done>
</task>

<task type="auto">
  <name>Task 2: Create sheet and docs canvas rendering E2E tests (CANV-02, CANV-04)</name>
  <files>backend/tests/e2e_ui/tests/canvas/test_canvas_sheets_rendering.py, backend/tests/e2e_ui/tests/canvas/test_canvas_docs_rendering.py</files>
  <action>
    Create `backend/tests/e2e_ui/tests/canvas/test_canvas_sheets_rendering.py` with sheet tests:

    1. **test_sheet_displays_data_grid(authenticated_page_api, db_session)**
       - Create sheet data with 10 rows x 5 columns
       - Trigger sheet canvas presentation
       - Verify canvas visible
       - Verify table/grid element rendered
       - Verify row count matches data (10 data rows + 1 header = 11)

    2. **test_sheet_pagination_works(authenticated_page_api)**
       - Create sheet data with 100 rows (exceeds default page size)
       - Trigger sheet presentation
       - Verify pagination controls visible
       - Verify initial page shows subset of data
       - Click next button, verify different data displayed

    3. **test_sheet_sorting_works(authenticated_page_api)**
       - Create sheet data with sortable column (numeric values)
       - Trigger sheet presentation
       - Click column header to sort
       - Verify data re-sorted visually

    Create `backend/tests/e2e_ui/tests/canvas/test_canvas_docs_rendering.py` with docs tests:

    1. **test_docs_renders_markdown_content(authenticated_page_api, db_session)**
       - Create markdown content: "# Header\n\n- Bullet\n- **Bold** text"
       - Trigger docs canvas presentation
       - Verify canvas visible
       - Verify header rendered (h1 element)
       - Verify bullet list rendered
       - Verify bold text styling applied

    2. **test_docs_links_are_clickable(authenticated_page_api)**
       - Create markdown with link: "[Link](https://example.com)"
       - Trigger docs presentation
       - Verify link element rendered with href attribute
       - Verify link has correct URL

    3. **test_docs_code_blocks_rendered(authenticated_page_api)**
       - Create markdown with code block: "```python\nprint('hello')\n```"
       - Trigger docs presentation
       - Verify code block element visible
       - Verify syntax highlighting classes applied

    Helper for sheet:
    ```python
    def trigger_canvas_sheet(page, data: list, title: str = "Test Sheet") -> str:
        canvas_id = f"sheet-{str(uuid.uuid4())[:8]}"
        canvas_message = {
            "type": "canvas:update",
            "canvas_id": canvas_id,
            "data": {
                "component": "sheet",
                "title": title,
                "data": data,
                "columns": list(data[0].keys()) if data else []
            }
        }
        page.evaluate(f"(msg) => window.lastCanvasMessage = msg", canvas_message)
        page.evaluate("() => { const e = new CustomEvent('canvas:update', {detail: {type: 'canvas:update'}}); window.dispatchEvent(e); }")
        return canvas_id

    def create_sheet_data(rows: int, cols: int) -> list:
        return [{f"col_{j}": f"value_{i}_{j}" for j in range(cols)} for i in range(rows)]
    ```

    Helper for docs:
    ```python
    def trigger_canvas_docs(page, markdown: str, title: str = "Test Docs") -> str:
        canvas_id = f"docs-{str(uuid.uuid4())[:8]}"
        canvas_message = {
            "type": "canvas:update",
            "canvas_id": canvas_id,
            "data": {
                "component": "docs",
                "title": title,
                "content": markdown
            }
        }
        page.evaluate(f"(msg) => window.lastCanvasMessage = msg", canvas_message)
        page.evaluate("() => { const e = new CustomEvent('canvas:update', {detail: {type: 'canvas:update'}}); window.dispatchEvent(e); }")
        return canvas_id
    ```

    Use `authenticated_page_api` fixture for all tests.
  </action>
  <verify>pytest backend/tests/e2e_ui/tests/canvas/test_canvas_sheets_rendering.py backend/tests/e2e_ui/tests/canvas/test_canvas_docs_rendering.py -v</verify>
  <done>6 tests pass: sheet data grid, pagination, sorting, docs markdown, links, code blocks</done>
</task>

<task type="auto">
  <name>Task 3: Create email, terminal, and coding canvas rendering E2E tests (CANV-05, CANV-06, CANV-07)</name>
  <files>backend/tests/e2e_ui/tests/canvas/test_canvas_email_rendering.py, backend/tests/e2e_ui/tests/canvas/test_canvas_terminal_rendering.py, backend/tests/e2e_ui/tests/canvas/test_canvas_coding_rendering.py</files>
  <action>
    Create `backend/tests/e2e_ui/tests/canvas/test_canvas_email_rendering.py` with email tests:

    1. **test_email_canvas_displays_fields(authenticated_page_api, db_session)**
       - Create email data: to="test@example.com", subject="Test Subject", body="Test body"
       - Trigger email canvas presentation
       - Verify canvas visible
       - Verify "to" input field with pre-filled value
       - Verify "subject" input field with pre-filled value
       - Verify body textarea with pre-filled content

    2. **test_email_validation_works(authenticated_page_api)**
       - Trigger email canvas with empty required fields
       - Click send button
       - Verify validation error appears

    Create `backend/tests/e2e_ui/tests/canvas/test_canvas_terminal_rendering.py` with terminal tests:

    1. **test_terminal_displays_output(authenticated_page_api, db_session)**
       - Create terminal output data: ["Line 1", "Line 2", "Line 3"]
       - Trigger terminal canvas presentation
       - Verify canvas visible
       - Verify terminal output area visible
       - Verify all 3 lines displayed
       - Verify monospace font applied (check CSS)

    2. **test_terminal_scrollable(authenticated_page_api)**
       - Create terminal output with 100 lines
       - Trigger terminal presentation
       - Verify scrollbar visible (or content scrollable)
       - Scroll to bottom, verify last line visible

    Create `backend/tests/e2e_ui/tests/canvas/test_canvas_coding_rendering.py` with coding tests:

    1. **test_coding_canvas_displays_code(authenticated_page_api, db_session)**
       - Create code data: language="python", code="def hello():\n    print('world')"
       - Trigger coding canvas presentation
       - Verify canvas visible
       - Verify code block visible
       - Verify syntax highlighting applied (check for token classes)
       - Verify line numbers displayed (if applicable)

    2. **test_coding_canvas_language_detection(authenticated_page_api)**
       - Test with different languages: python, javascript, json
       - Verify different syntax highlighting for each
       - Verify language indicator displayed

    Helper functions:
    ```python
    def trigger_canvas_email(page, email_data: dict) -> str:
        canvas_id = f"email-{str(uuid.uuid4())[:8]}"
        canvas_message = {
            "type": "canvas:update",
            "canvas_id": canvas_id,
            "data": {
                "component": "email",
                "title": "Compose Email",
                "to": email_data.get("to", ""),
                "subject": email_data.get("subject", ""),
                "body": email_data.get("body", "")
            }
        }
        page.evaluate(f"(msg) => window.lastCanvasMessage = msg", canvas_message)
        page.evaluate("() => { const e = new CustomEvent('canvas:update', {detail: {type: 'canvas:update'}}); window.dispatchEvent(e); }")
        return canvas_id

    def trigger_canvas_terminal(page, output: list) -> str:
        canvas_id = f"terminal-{str(uuid.uuid4())[:8]}"
        canvas_message = {
            "type": "canvas:update",
            "canvas_id": canvas_id,
            "data": {
                "component": "terminal",
                "title": "Terminal Output",
                "output": "\\n".join(output)
            }
        }
        page.evaluate(f"(msg) => window.lastCanvasMessage = msg", canvas_message)
        page.evaluate("() => { const e = new CustomEvent('canvas:update', {detail: {type: 'canvas:update'}}); window.dispatchEvent(e); }")
        return canvas_id

    def trigger_canvas_coding(page, code: str, language: str = "python") -> str:
        canvas_id = f"coding-{str(uuid.uuid4())[:8]}"
        canvas_message = {
            "type": "canvas:update",
            "canvas_id": canvas_id,
            "data": {
                "component": "coding",
                "title": f"Code ({language})",
                "language": language,
                "code": code
            }
        }
        page.evaluate(f"(msg) => window.lastCanvasMessage = msg", canvas_message)
        page.evaluate("() => { const e = new CustomEvent('canvas:update', {detail: {type: 'canvas:update'}}); window.dispatchEvent(e); }")
        return canvas_id
    ```

    Use `authenticated_page_api` fixture for all tests.
    Use `db_session` for CanvasAudit verification.
  </action>
  <verify>pytest backend/tests/e2e_ui/tests/canvas/test_canvas_email_rendering.py backend/tests/e2e_ui/tests/canvas/test_canvas_terminal_rendering.py backend/tests/e2e_ui/tests/canvas/test_canvas_coding_rendering.py -v</verify>
  <done>6 tests pass: email fields, email validation, terminal output, terminal scroll, coding display, coding language detection</done>
</task>

</tasks>

<verification>
Run all canvas rendering tests in parallel:
```bash
pytest backend/tests/e2e_ui/tests/canvas/test_canvas_chart_rendering.py \
       backend/tests/e2e_ui/tests/canvas/test_canvas_sheets_rendering.py \
       backend/tests/e2e_ui/tests/canvas/test_canvas_docs_rendering.py \
       backend/tests/e2e_ui/tests/canvas/test_canvas_email_rendering.py \
       backend/tests/e2e_ui/tests/canvas/test_canvas_terminal_rendering.py \
       backend/tests/e2e_ui/tests/canvas/test_canvas_coding_rendering.py \
       -v -n auto --alluredir=allure-results
```

Verify:
- All 6 test files execute without errors
- Minimum 17 tests pass (5+3+3+2+2+2)
- All 7 canvas types tested (chart counted once, sheet, form via other plans, docs, email, terminal, coding)
- CanvasAudit records created for each presentation
- No memory leaks detected in test execution
- Coverage: CANV-01, CANV-02, CANV-04, CANV-05, CANV-06, CANV-07 requirements met
</verification>

<success_criteria>
- 6 new test files created in `backend/tests/e2e_ui/tests/canvas/` directory
- Minimum 17 passing E2E tests for canvas rendering
- All 7 canvas types covered (chart, sheet, docs, email, terminal, coding, form via plan 02)
- Tests use existing fixtures (authenticated_page_api, db_session)
- Tests verify visual rendering, component structure, and database audit trail
- Allure report generated successfully
- Tests complete in under 10 minutes
</success_criteria>

<output>
After completion, create `.planning/phases/235-canvas-and-workflow-e2e/235-01-SUMMARY.md` with:
- Tests created (6 files, minimum 17 tests)
- Coverage achieved (CANV-01, CANV-02, CANV-04, CANV-05, CANV-06, CANV-07)
- Any rendering issues discovered
- Execution time
- Any deviations from plan
</output>
