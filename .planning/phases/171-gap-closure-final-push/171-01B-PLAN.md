---
phase: 171-gap-closure-final-push
plan: 01B
type: execute
wave: 1
depends_on: [171-01A]
files_modified:
  - backend/tests/integration/services/conftest_episode.py
  - backend/tests/integration/services/test_episode_services_coverage.py
  - backend/tests/integration/services/test_governance_coverage.py
  - backend/tests/integration/services/test_llm_coverage_governance_llm.py
autonomous: true

must_haves:
  truths:
    - "Episode service tests can run without import errors"
    - "Governance and LLM tests can run in combined test suite"
    - "All tests pass after conflict resolution"
    - "Combined test suite (150+ tests) executes without SQLAlchemy errors"
  artifacts:
    - path: "backend/tests/integration/services/conftest_episode.py"
      provides: "Episode test fixtures compatible with unified models"
    - path: "backend/tests/integration/services/test_episode_services_coverage.py"
      provides: "Episode tests with corrected imports"
    - path: "backend/tests/integration/services/test_governance_coverage.py"
      provides: "Governance tests with corrected imports"
  key_links:
    - from: "backend/tests/integration/services/test_episode_services_coverage.py"
      to: "backend/core/models.py"
      via: "episode model imports without conflict"
      pattern: "from core\\.models import.*Episode"
    - from: "backend/tests/integration/services/test_governance_coverage.py"
      to: "backend/accounting/models.py"
      via: "direct import for accounting models"
      pattern: "from accounting\\.models import"
---

<objective>
Fix test imports and verify combined test suite execution after SQLAlchemy conflict resolution.

With duplicate models removed from core/models.py (171-01A), this plan updates test files to use correct import paths and verifies that all governance, LLM, and episode tests can run together without "Table already defined" errors.

Purpose: Unblock existing test infrastructure so comprehensive test suites can run together, enabling accurate coverage measurement across the entire backend.

Output: All test imports corrected, combined test suite (150+ tests) passing without SQLAlchemy conflicts.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/171-gap-closure-final-push/171-RESEARCH.md
@.planning/phases/171-gap-closure-final-push/171-01A-PLAN.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/tests/integration/services/conftest_episode.py
@backend/tests/integration/services/test_episode_services_coverage.py
@backend/tests/integration/services/test_governance_coverage.py
@backend/tests/integration/services/test_llm_coverage_governance_llm.py
</context>

<tasks>

<task type="auto">
  <name>Fix episode service test imports and fixtures</name>
  <files>backend/tests/integration/services/conftest_episode.py, backend/tests/integration/services/test_episode_services_coverage.py</files>
  <action>
    Update episode test files to use unified model imports:

    1. In conftest_episode.py:
       - Update imports to use authoritative model locations
       - Change `from core.models import Transaction` to `from accounting.models import Transaction`
       - Ensure Episode, EpisodeSegment, EpisodeAccessLog import from core.models
       - Fix any factory imports

    2. In test_episode_services_coverage.py:
       - Update model imports consistently
       - Fix AgentExecution, AgentEpisode, CanvasAudit imports
       - Ensure relationship imports use correct modules

    3. Run test collection to verify imports work:
       `cd backend && pytest tests/integration/services/test_episode_services_coverage.py --collect-only -q`
  </action>
  <verify>
    `cd backend && pytest tests/integration/services/test_episode_services_coverage.py --collect-only -q`
  </verify>
  <done>
    Episode tests collect without SQLAlchemy metadata errors. All imports resolve correctly.
  </done>
</task>

<task type="auto">
  <name>Fix governance and LLM test imports for combined execution</name>
  <files>backend/tests/integration/services/test_governance_coverage.py, backend/tests/integration/services/test_llm_coverage_governance_llm.py</files>
  <action>
    Update governance and LLM test imports for unified execution:

    1. In test_governance_coverage.py:
       - Verify AgentRegistry, AgentExecution imports from core.models
       - Check for any duplicate model references
       - Ensure no accounting model imports conflict

    2. In test_llm_coverage_governance_llm.py:
       - Update combined test imports
       - Ensure governance + LLM models can coexist
       - Fix any relationship imports

    3. Test combined execution:
       `cd backend && pytest tests/integration/services/test_governance_coverage.py tests/integration/services/test_llm_coverage_governance_llm.py --collect-only -q`

    4. Verify no "Table already defined" errors in collection
  </action>
  <verify>
    `cd backend && pytest tests/integration/services/test_governance_coverage.py tests/integration/services/test_llm_coverage_governance_llm.py -v --tb=short`
  </verify>
  <done>
    Governance and LLM tests run together without SQLAlchemy conflicts. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Run combined test suite to verify conflict resolution</name>
  <files>backend/tests/integration/services/</files>
  <action>
    Run the combined test suite across governance, LLM, and episode services:

    1. Run all three test suites together:
       ```bash
       cd backend && pytest \
         tests/integration/services/test_governance_coverage.py \
         tests/integration/services/test_llm_coverage_governance_llm.py \
         tests/integration/services/test_episode_services_coverage.py \
         -v --tb=short
       ```

    2. Verify:
       - No "Table already defined" errors
       - No "NoForeignKeysError" issues
       - All tests that passed individually still pass
       - No new failures introduced

    3. Count total tests passing (expected: 150+ tests across all suites)

    4. Document any remaining import issues for next plan
  </action>
  <verify>
    Combined test suite runs without SQLAlchemy metadata errors. All tests pass.
  </verify>
  <done>
    Combined governance + LLM + episode test suite (150+ tests) passes without SQLAlchemy conflicts. Ready for coverage measurement.
  </done>
</task>

</tasks>

<verification>
After completion, run the combined test suite and verify:

```bash
cd /Users/rushiparikh/projects/atom/backend
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
  tests/integration/services/test_governance_coverage.py \
  tests/integration/services/test_llm_coverage_governance_llm.py \
  tests/integration/services/test_episode_services_coverage.py \
  -v --tb=short --maxfail=5
```

Expected results:
1. No SQLAlchemy "Table already defined" errors
2. All 150+ tests pass
3. No import errors in any test file
4. Models import consistently from authoritative modules
</verification>

<success_criteria>
1. Episode tests collect and pass without SQLAlchemy errors
2. Governance and LLM tests run in combined suite
3. All 150+ tests pass after conflict resolution
4. No remaining SQLAlchemy metadata conflicts
5. Combined execution verified for coverage measurement readiness
</success_criteria>

<output>
After completion, create `.planning/phases/171-gap-closure-final-push/171-01B-SUMMARY.md` with:
- Test imports fixed across all test suites
- Combined test suite verified (150+ tests passing)
- SQLAlchemy conflicts fully resolved
- Ready for 171-02 (actual coverage measurement)
</output>
