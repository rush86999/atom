---
phase: 112-agent-governance-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/governance/test_agent_context_resolver.py
autonomous: true

must_haves:
  truths:
    - "All 7 failing tests in test_agent_context_resolver.py now pass"
    - "ChatSession objects created without workspace_id parameter"
    - "agent_context_resolver.py coverage increases from 60.68% to ~75%"
    - "Tests can create sessions and associate agents successfully"
  artifacts:
    - path: "backend/tests/unit/governance/test_agent_context_resolver.py"
      provides: "Tests for AgentContextResolver with correct ChatSession model usage"
      min_lines: 560
  key_links:
    - from: "backend/tests/unit/governance/test_agent_context_resolver.py"
      to: "backend/core/models.py"
      via: "ChatSession model instantiation"
      pattern: "ChatSession\\("
---

<objective>
Fix ChatSession model mismatch in test_agent_context_resolver.py to unblock 7 failing tests.

**Purpose:** Tests incorrectly pass `workspace_id` parameter that doesn't exist in ChatSession model definition. Removing this parameter will fix 7 failing tests and immediately improve agent_context_resolver.py coverage from 60.68% to ~75%.

**Output:** 27 tests passing (currently 20 passing, 7 failing), coverage boost for agent_context_resolver.py
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-agent-governance-coverage/112-RESEARCH.md

# Source files
@backend/core/models.py (ChatSession model - lines 1046-1060)
@backend/tests/unit/governance/test_agent_context_resolver.py (failing tests)
@backend/core/agent_context_resolver.py
</context>

<tasks>

<task type="auto">
  <name>Fix ChatSession instantiation in test_agent_context_resolver.py</name>
  <files>backend/tests/unit/governance/test_agent_context_resolver.py</files>
  <action>
    Remove `workspace_id="default"` parameter from all ChatSession instantiations in test_agent_context_resolver.py.

    The ChatSession model (lines 1046-1060 in models.py) does NOT have a workspace_id field:
    - Fields: id, user_id, title, metadata_json, created_at, updated_at, message_count
    - NO workspace_id field exists

    Find all occurrences of:
    ```python
    session = ChatSession(
        id="test_session_id",
        user_id="test_user",
        workspace_id="default",  # ← REMOVE THIS LINE
        metadata_json={"agent_id": agent.id}
    )
    ```

    Replace with:
    ```python
    session = ChatSession(
        id="test_session_id",
        user_id="test_user",
        metadata_json={"agent_id": agent.id}
    )
    ```

    Expected occurrences: 7 instances (lines 77, 115, 155, 225, 376, 410, 432 based on research)

    Do NOT modify:
    - test class names
    - test method names
    - test logic/assertions
    - Any other ChatSession fields (id, user_id, metadata_json are correct)
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/governance/test_agent_context_resolver.py -v</verify>
  <done>All 27 tests pass (currently 20 passing, 7 failing due to workspace_id TypeError)</done>
</task>

<task type="auto">
  <name>Verify agent_context_resolver coverage improved to ~75%</name>
  <files>backend/tests/unit/governance/test_agent_context_resolver.py</files>
  <action>
    Run coverage on agent_context_resolver.py to verify the fix improved coverage from 60.68% to approximately 75%.

    The 7 fixed tests cover these previously-uncovered lines:
    - Lines 78-84: Session agent retrieval with metadata access
    - Lines 104-106: Error handling in _get_agent
    - Lines 124-135: Session metadata extraction and agent lookup
    - Lines 176-178: System default creation error handling
    - Lines 200-218: set_session_agent method (full coverage)

    Command:
    ```bash
    cd backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
      tests/unit/governance/test_agent_context_resolver.py \
      --cov=core.agent_context_resolver \
      --cov-report=term-missing \
      --cov-report=html
    ```

    Expected outcome: Coverage ~75% (up from 60.68%)
  </action>
  <verify>
    Coverage report shows agent_context_resolver.py coverage ≥ 70%
    Look for: "core/agent_context_resolver_resolver.py" line in coverage output
  </verify>
  <done>
    Coverage report shows ≥70% coverage for agent_context_resolver.py
    Uncovered lines reduced from 22 to ~10 or fewer
  </done>
</task>

</tasks>

<verification>
1. All 27 tests in test_agent_context_resolver.py pass
2. Coverage report shows agent_context_resolver.py ≥70%
3. No TypeError messages about 'workspace_id' in test output
4. Session management tests (set_session_agent, _get_session_agent) work correctly
</verification>

<success_criteria>
1. 27/27 tests passing in test_agent_context_resolver.py
2. agent_context_resolver.py coverage ≥70% (up from 60.68%)
3. All session-based resolution tests work without workspace_id parameter
4. Tests demonstrate: session creation, agent association, session-based resolution
</success_criteria>

<output>
After completion, create `.planning/phases/112-agent-governance-coverage/112-01-SUMMARY.md`
</output>
