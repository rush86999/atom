---
phase: 112-agent-governance-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/governance/test_agent_context_resolver.py
autonomous: true

must_haves:
  truths:
    - "agent_context_resolver.py coverage ≥60% (target achieved)"
    - "All previously uncovered error handling paths now tested"
    - "Exception handling in _get_agent tested"
    - "Exception handling in _get_session_agent tested"
  artifacts:
    - path: "backend/tests/unit/governance/test_agent_context_resolver.py"
      provides: "Comprehensive tests for AgentContextResolver including error paths"
      min_lines: 600
  key_links:
    - from: "backend/tests/unit/governance/test_agent_context_resolver.py"
      to: "backend/core/agent_context_resolver.py"
      via: "Error handling test cases"
      pattern: "test.*exception|test.*error"
---

<objective>
Add targeted tests for remaining uncovered lines in agent_context_resolver.py to achieve 60%+ coverage.

**Purpose:** Plan 01 fixes will get coverage to ~75%, but we need to ensure any remaining uncovered lines (especially error handling paths) have dedicated tests. This plan adds tests for edge cases and error scenarios.

**Output:** agent_context_resolver.py coverage ≥60%, all error paths tested
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-agent-governance-coverage/112-RESEARCH.md

# Source files
@backend/core/agent_context_resolver.py
@backend/tests/unit/governance/test_agent_context_resolver.py
</context>

<tasks>

<task type="auto">
  <name>Add error handling tests for _get_agent exception path</name>
  <files>backend/tests/unit/governance/test_agent_context_resolver.py</files>
  <action>
    Add test for the exception handling path in _get_agent (lines 104-106).

    Add a new test class or method to TestErrorHandling section:

    ```python
    @pytest.mark.asyncio
    async def test_get_agent_handles_database_exception(self, context_resolver, db_session):
        \"\"\"Test _get_agent handles database exceptions gracefully.\"\"\"
        # Mock the query to raise an exception
        with patch.object(context_resolver.db, 'query') as mock_query:
            mock_query.side_effect = Exception("Database connection lost")

            # Should return None, not crash
            agent = context_resolver._get_agent("some_agent_id")
            assert agent is None
    ```

    This tests lines 104-106 in agent_context_resolver.py:
    ```python
    except Exception as e:
        logger.error(f"Error fetching agent {agent_id}: {e}")
        return None
    ```
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/governance/test_agent_context_resolver.py::TestErrorHandling::test_get_agent_handles_database_exception -v</verify>
  <done>Test passes, exception path in _get_agent is covered</done>
</task>

<task type="auto">
  <name>Add error handling tests for _get_session_agent exception path</name>
  <files>backend/tests/unit/governance/test_agent_context_resolver.py</files>
  <action>
    Add test for the exception handling path in _get_session_agent (lines 133-135).

    Add to TestErrorHandling section:

    ```python
    @pytest.mark.asyncio
    async def test_get_session_agent_handles_database_exception(self, context_resolver):
        \"\"\"Test _get_session_agent handles database exceptions gracefully.\"\"\"
        # Mock the query to raise an exception
        with patch.object(context_resolver.db, 'query') as mock_query:
            mock_query.side_effect = Exception("Database connection lost")

            # Should return None, not crash
            agent = context_resolver._get_session_agent("some_session_id")
            assert agent is None
    ```

    This tests lines 133-135 in agent_context_resolver.py:
    ```python
    except Exception as e:
        logger.error(f"Error getting session agent: {e}")
        return None
    ```
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/governance/test_agent_context_resolver.py::TestErrorHandling::test_get_session_agent_handles_database_exception -v</verify>
  <done>Test passes, exception path in _get_session_agent is covered</done>
</task>

<task type="auto">
  <name>Add error handling test for set_session_agent exception path</name>
  <files>backend/tests/unit/governance/test_agent_context_resolver.py</files>
  <action>
    Add test for the exception handling path in set_session_agent (lines 216-218).

    Add to TestSessionManagement or TestErrorHandling section:

    ```python
    @pytest.mark.asyncio
    async def test_set_session_agent_handles_exception_gracefully(self, context_resolver, db_session):
        \"\"\"Test set_session_agent handles exceptions gracefully.\"\"\"
        agent = AgentFactory(_session=db_session)
        session = ChatSession(
            id="test_session_id",
            user_id="test_user",
            metadata_json={}
        )
        db_session.add(session)
        db_session.commit()

        # Mock db.commit to raise exception
        with patch.object(context_resolver.db, 'commit') as mock_commit:
            mock_commit.side_effect = Exception("Transaction failed")

            # Should return False, not crash
            success = context_resolver.set_session_agent(
                session_id="test_session_id",
                agent_id=agent.id
            )
            assert success is False
    ```

    This tests lines 216-218 in agent_context_resolver.py:
    ```python
    except Exception as e:
        logger.error(f"Error setting session agent: {e}")
        return False
    ```
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/governance/test_agent_context_resolver.py::TestSessionManagement::test_set_session_agent_handles_exception_gracefully -v</verify>
  <done>Test passes, exception path in set_session_agent is covered</done>
</task>

<task type="auto">
  <name>Run coverage verification for agent_context_resolver.py</name>
  <files>backend/tests/unit/governance/test_agent_context_resolver.py</files>
  <action>
    Run full coverage report on agent_context_resolver.py to verify ≥60% coverage achieved.

    Command:
    ```bash
    cd backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
      tests/unit/governance/test_agent_context_resolver.py \
      --cov=core.agent_context_resolver \
      --cov-report=term-missing \
      --cov-report=html
    ```

    Capture coverage percentage and verify it's ≥60%.
  </action>
  <verify>Coverage output shows agent_context_resolver.py ≥60%</verify>
  <done>agent_context_resolver.py coverage ≥60%, success criteria met</done>
</task>

</tasks>

<verification>
1. New error handling tests pass
2. Coverage report shows agent_context_resolver.py ≥60%
3. All exception paths (lines 104-106, 133-135, 216-218) are covered
4. No tests fail
</verification>

<success_criteria>
1. agent_context_resolver.py coverage ≥60%
2. Exception handling paths in _get_agent, _get_session_agent, set_session_agent tested
3. All new tests pass
4. Total test count increased by 2-3 tests
</success_criteria>

<output>
After completion, create `.planning/phases/112-agent-governance-coverage/112-02-SUMMARY.md`
</output>
