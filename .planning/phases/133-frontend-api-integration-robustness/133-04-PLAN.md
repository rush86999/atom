---
phase: 133-frontend-api-integration-robustness
plan: 04
type: execute
wave: 2
depends_on: [133-01, 133-02, 133-03]
files_modified:
  - frontend-nextjs/tests/integration/api-robustness.test.tsx
  - frontend-nextjs/tests/mocks/scenarios/error-recovery.ts
autonomous: true

must_haves:
  truths:
    - "Full error recovery flows tested end-to-end"
    - "Loading → error → retry → success flow validated"
    - "Automatic retry with exponential backoff tested"
    - "Network error recovery validated"
    - "Timeout with retry flow works correctly"
  artifacts:
    - path: frontend-nextjs/tests/integration/api-robustness.test.tsx
      provides: "End-to-end API robustness integration tests"
      min_lines: 250
    - path: frontend-nextjs/tests/mocks/scenarios/error-recovery.ts
      provides: "MSW handlers for error recovery scenarios"
      exports: ["errorRecoveryHandlers", "createRecoveryScenario"]
  key_links:
    - from: frontend-nextjs/tests/integration/api-robustness.test.tsx
      to: frontend-nextjs/tests/mocks/scenarios/error-recovery.ts
      via: "import { errorRecoveryHandlers } from '@/tests/mocks/scenarios/error-recovery'"
      pattern: "errorRecoveryHandlers"
    - from: frontend-nextjs/tests/integration/api-robustness.test.tsx
      to: frontend-nextjs/lib/api.ts
      via: "import { apiClient } from '@/lib/api'"
      pattern: "apiClient\\."
---

<objective>
Create integration tests that validate complete API failure recovery flows from loading through error to retry and success.

Purpose: Unit tests cover retry logic and loading states in isolation. Integration tests validate the complete user flow: initial request fails → error displayed → user clicks retry → request succeeds. This ensures the entire error recovery path works.

Output: Comprehensive integration tests for error recovery flows, MSW recovery scenario handlers
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-frontend-api-integration-robustness/133-RESEARCH.md

@frontend-nextjs/tests/integration/form-submission-msw.test.tsx
@frontend-nextjs/tests/mocks/scenarios/retry-scenarios.ts
@frontend-nextjs/lib/api.ts
@frontend-nextjs/lib/error-mapping.ts
</context>

<tasks>

<task type="auto">
  <name>Create error recovery MSW handlers</name>
  <files>frontend-nextjs/tests/mocks/scenarios/error-recovery.ts</files>
  <action>
Create frontend-nextjs/tests/mocks/scenarios/error-recovery.ts with:

1. createRecoveryScenario(endpoint, options) factory:
   - Options: { failAttempts, successAfter, errorType, delayBetweenAttempts }
   - Returns handler that fails N times then succeeds
   - Tracks attempt count in closure
   - Supports different error types (503, 504, network error)

2. errorRecoveryHandlers array with common scenarios:
   - Flaky endpoint: Fails 2x with 503, succeeds on 3rd
   - Timeout recovery: 1st attempt times out, 2nd succeeds
   - Network recovery: 1st attempt has network error, 2nd succeeds
   - Partial outage: 503 → 503 → 200 (recovery pattern)
   - Degraded service: Slow response → error → success

3. createRetryTracker() utility:
   - Returns { getAttempts, resetAttempts, trackAttempt }
   - Use in tests to verify retry count
   - Exports timestamps for delay validation

4. Export scenario handlers organized by recovery type:
   - transientErrorHandlers: Temporary failures that recover
   - timeoutRecoveryHandlers: Timeout then success
   - networkRecoveryHandlers: Network failure then recovery
   - allRecoveryHandlers: Combined array

Each handler should:
- Simulate realistic backend behavior (not just immediate success)
- Include proper error payloads (error_code, message, retry_after)
- Support ctx.delay() for timeout simulation
- Include timestamp for delay measurement

Reference 133-RESEARCH.md Example 1 (createSlowEndpointHandler pattern)
  </action>
  <verify>
npm test -- tests/mocks/__tests__/error-recovery.test.ts
  </verify>
  <done>
error-recovery.ts exports recovery handlers, createRecoveryScenario factory works, retry tracking utilities available
  </done>
</task>

<task type="auto">
  <name>Create API robustness integration tests</name>
  <files>frontend-nextjs/tests/integration/api-robustness.test.tsx</files>
  <action>
Create comprehensive API robustness integration tests (250+ lines):

1. Test complete flow: loading → error → retry → success
   - Mock endpoint that fails first attempt, succeeds second
   - Trigger API call (component or direct apiClient)
   - Assert loading state visible
   - Wait for error state
   - Assert user-friendly error message
   - Trigger retry (button or automatic)
   - Assert loading state during retry
   - Wait for success state
   - Assert data displayed, error cleared
   - Verify 2 attempts made

2. Test automatic retry with exponential backoff:
   - Mock endpoint: 503 → 503 → 200
   - Trigger API call
   - Capture attempt timestamps
   - Verify 3 attempts total (initial + 2 retries)
   - Verify exponential delay (delay2 > delay1)
   - Verify success on 3rd attempt

3. Test network error recovery:
   - Mock network error on first attempt
   - Mock success on retry
   - Simulate network recovery (server.resetHandlers)
   - Trigger manual retry
   - Verify success after retry

4. Test timeout with retry:
   - Mock endpoint with 15s delay (exceeds 10s timeout)
   - First attempt: timeout
   - Second attempt: fast response (100ms)
   - Verify timeout error, then success

5. Test retry exhaustion:
   - Mock endpoint that always returns 503
   - Trigger API call
   - Wait for MAX_RETRIES exhausted
   - Verify final error displayed
   - Verify "Retry" button still available

6. Test request body preservation:
   - Mock POST endpoint: fail → success
   - Send request with body
   - Verify body preserved in retry
   - Assert correct data received on success

7. Test concurrent request errors:
   - Trigger multiple simultaneous requests
   - Mock mixed success/failure responses
   - Verify each request handled independently
   - Verify no state leakage between requests

IMPORTANT integration test patterns:
- Use waitFor() for async state transitions
- Use findBy* for timeout-based assertions
- Test full user flows, not just API client
- Include component rendering where applicable
- Use server.use() for per-test handler overrides
- Reset handlers in afterEach

Reference 133-RESEARCH.md Pattern 6 (Integration Tests for API Failure Recovery)
  </action>
  <verify>
npm test -- tests/integration/api-robustness.test.tsx --no-coverage
  </verify>
  <done>
All integration tests pass, complete flows validated, exponential backoff verified, recovery scenarios tested
  </done>
</task>

<task type="auto">
  <name>Create component-level error recovery tests</name>
  <files>frontend-nextjs/components/__tests__/AgentChat.robustness.test.tsx</files>
  <action>
Create component-level error recovery tests for key components:

1. AgentChat component error recovery:
   - Test chat message send failure (503)
   - Verify error message displayed
   - Test retry button functionality
   - Verify message sent on retry

2. CanvasForm component error recovery:
   - Test form submission failure (403 governance)
   - Verify governance error message
   - Test retry after permission granted
   - Verify form submits successfully

3. AgentList component error recovery:
   - Test agent list fetch failure
   - Verify loading → error transition
   - Test refresh/retry functionality
   - Verify agents displayed after retry

4. DeviceControl component error recovery:
   - Test device command failure
   - Verify device error handling
   - Test retry functionality

For each component test:
- Render component with MSW handlers
- Simulate user interactions (click, type, submit)
- Verify UI state transitions (loading → error → success)
- Test retry mechanisms (auto and manual)
- Verify user-friendly error messages
- Use findBy* for timeout handling
- Cleanup with server.resetHandlers()

Use pattern from existing integration tests (form-submission-msw.test.tsx)
  </action>
  <verify>
npm test -- components/__tests__/*.robustness.test.tsx
  </verify>
  <done>
Component error recovery tests created, 4 components tested, user flows validated
  </done>
</task>

</tasks>

<verification>
1. npm test -- tests/integration/api-robustness.test.tsx passes all tests
2. Complete loading → error → retry → success flow validated
3. Exponential backoff verified in integration tests
4. Network error recovery tested
5. Timeout recovery tested
6. Request body preservation tested
7. Retry exhaustion scenario tested
8. Component-level error recovery tests pass
</verification>

<success_criteria>
1. error-recovery.ts created with recovery scenario handlers
2. api-robustness.test.tsx with 7+ integration test scenarios
3. All tests use waitFor/findBy* patterns correctly
4. Complete user flows validated (not just API calls)
5. Component-level error recovery tests for key components
6. 100% test pass rate
</success_criteria>

<output>
After completion, create `.planning/phases/133-frontend-api-integration-robustness/133-04-SUMMARY.md`
</output>
