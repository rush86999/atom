---
phase: 133-frontend-api-integration-robustness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/tests/mocks/scenarios/loading-states.ts
  - frontend-nextjs/lib/__tests__/api/loading-states.test.ts
autonomous: true

must_haves:
  truths:
    - "Loading indicators are visible during async operations"
    - "Skeleton loaders display while fetching data"
    - "Submit buttons disable during form submission"
    - "Loading states tested with waitFor and findBy* queries"
    - "Slow endpoints (2s delay) trigger loading UI correctly"
  artifacts:
    - path: frontend-nextjs/tests/mocks/scenarios/loading-states.ts
      provides: "MSW handlers for loading state testing"
      exports: ["slowHandlers", "loadingStateHandlers"]
    - path: frontend-nextjs/lib/__tests__/api/loading-states.test.ts
      provides: "Loading state validation tests"
      min_lines: 150
  key_links:
    - from: frontend-nextjs/lib/__tests__/api/loading-states.test.ts
      to: frontend-nextjs/tests/mocks/scenarios/loading-states.ts
      via: "import { slowHandlers } from '@/tests/mocks/scenarios/loading-states'"
      pattern: "slowHandlers|ctx\\.delay"
---

<objective>
Create loading state testing infrastructure with MSW handlers for simulating slow endpoints and comprehensive tests for loading indicators.

Purpose: Loading states are critical UX but often untested. MSW's ctx.delay() enables realistic loading state testing without artificial setTimeouts. Tests ensure users see feedback during async operations.

Output: MSW loading state handlers, comprehensive loading state tests using waitFor/findBy*
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-frontend-api-integration-robustness/133-RESEARCH.md

@frontend-nextjs/tests/mocks/handlers.ts
@frontend-nextjs/lib/api.ts
@frontend-nextjs/lib/__tests__/api/timeout-handling.test.ts
</context>

<tasks>

<task type="auto">
  <name>Create loading state MSW handlers</name>
  <files>frontend-nextjs/tests/mocks/scenarios/loading-states.ts</files>
  <action>
Create frontend-nextjs/tests/mocks/scenarios/loading-states.ts with:

1. slowHandlers array with delayed responses:
   - GET /api/atom-agent/agents with 2s delay
   - GET /api/atom-agent/agents/:agentId/status with 1.5s delay
   - POST /api/canvas/submit with 2.5s delay
   - GET /api/canvas/status with 1s delay
   - POST /api/atom-agent/chat/stream with 3s delay

2. createSlowEndpoint(method, path, delayMs) factory:
   - Returns MSW handler with ctx.delay(delayMs)
   - Supports GET, POST, PUT, DELETE
   - Returns realistic success response after delay

3. createProgressiveLoadingEndpoint(method, path, delays) factory:
   - Returns handler that simulates progressive loading
   - Accepts array of delays for subsequent requests
   - Useful for testing skeleton → data transitions

4. Export handlers organized by API:
   - agentSlowHandlers: Agent API endpoints
   - canvasSlowHandlers: Canvas API endpoints
   - deviceSlowHandlers: Device API endpoints
   - allSlowHandlers: Combined array

Each handler should:
- Use ctx.delay() for realistic delay (not setTimeout)
- Return proper response shape matching production API
- Include timestamp field for latency validation

Use pattern from 133-RESEARCH.md Example 1 (createSlowEndpointHandler)
  </action>
  <verify>
npm test -- tests/mocks/__tests__/loading-states.test.ts
  </verify>
  <done>
loading-states.ts exports slow handlers for all major endpoints, createSlowEndpoint factory works, delays are configurable
  </done>
</task>

<task type="auto">
  <name>Create loading state tests with waitFor patterns</name>
  <files>frontend-nextjs/lib/__tests__/api/loading-states.test.ts</files>
  <action>
Create comprehensive loading state tests (150+ lines):

1. Test loading spinner visibility:
   - Mock slow endpoint with 2s delay
   - Call API method
   - Assert loading state is active (if component has loading prop)
   - Use waitFor for state transition

2. Test skeleton loader display:
   - Mock endpoint with 1s delay
   - Render component that shows skeleton
   - Assert skeleton elements present (getByTestId('skeleton-item'))
   - Wait for data load
   - Assert skeleton removed, data displayed

3. Test submit button disabled state:
   - Mock POST endpoint with 1.5s delay
   - Render form component
   - Trigger submit
   - Assert button disabled during submission
   - Assert loading text visible
   - Wait for completion
   - Assert button re-enabled

4. Test multiple concurrent loading states:
   - Mock multiple slow endpoints
   - Trigger parallel requests
   - Verify all loading states active
   - Verify all complete independently

5. Test loading → error transition:
   - Mock endpoint with delay then error
   - Assert loading state first
   - Wait for error state
   - Assert loading cleared, error shown

6. Test loading → success transition:
   - Mock endpoint with delay then success
   - Assert loading state first
   - Wait for success state
   - Assert loading cleared, data shown

IMPORTANT patterns:
- Use waitFor() instead of getBy* for async state
- Use findBy* queries for timeout-based assertions
- DO NOT use jest.useFakeTimers() for loading tests (can miss transitions)
- Use server.use() to override default handlers per test

Pattern from 133-RESEARCH.md Example 2 (skeleton loader testing)
  </action>
  <verify>
npm test -- lib/__tests__/api/loading-states.test.ts --no-coverage
  </verify>
  <done>
All loading state tests pass, waitFor used for async assertions, no fakeTimers, loading→error→success transitions validated
  </done>
</task>

<task type="auto">
  <name>Create loading state test helpers</name>
  <files>frontend-nextjs/tests/test-helpers/loading-state.ts</files>
  <action>
Create reusable loading state test helpers:

1. createLoadingStateTest(component, endpoint, delay):
   - Returns configured test with slow endpoint
   - Wraps render with loading assertions
   - Returns { waitForLoading, waitForData, waitForError }

2. assertLoadingState(renderResult):
   - Checks for common loading indicators
   - Supports: spinner, skeleton, progress bar
   - Returns true if any loading state detected

3. waitForLoadingComplete(renderResult, timeout):
   - Waits for all loading states to clear
   - Uses waitFor with custom timeout
   - Throws descriptive error if timeout exceeded

4. mockSlowEndpoint(server, method, path, delay):
   - Helper to apply slow handler
   - Returns cleanup function
   - Use in beforeEach/afterEach for setup

5. createProgressiveLoadingMock(endpoint, delays):
   - Creates endpoint that returns progressively faster responses
   - Useful for testing optimistic updates
   - Returns handler with attempt tracking

Export all helpers from tests/test-helpers/loading-state.ts
Import in other test files to reduce duplication

Add JSDoc comments with usage examples for each helper
  </action>
  <verify>
npm test -- tests/test-helpers/__tests__/loading-state.test.ts
  </verify>
  <done>
loading-state.ts exports 5 helper functions, all helpers tested, usage examples in JSDoc comments
  </done>
</task>

</tasks>

<verification>
1. npm test -- lib/__tests__/api/loading-states.test.ts passes all tests
2. Tests use waitFor() instead of getBy* for async assertions
3. No jest.useFakeTimers() in loading tests
4. Skeleton loaders validated for data fetching
5. Submit button disabled state tested
6. Loading → error → success transitions validated
7. Test helpers reduce code duplication
</verification>

<success_criteria>
1. loading-states.ts created with slow handlers for all major endpoints
2. Loading state tests use waitFor/findBy* patterns correctly
3. No fakeTimers used in loading tests
4. Test helpers created and exported
5. All loading transitions (loading→error, loading→success) tested
</success_criteria>

<output>
After completion, create `.planning/phases/133-frontend-api-integration-robustness/133-03-SUMMARY.md`
</output>
