---
phase: 133-frontend-api-integration-robustness
plan: 05
type: execute
wave: 3
depends_on: [133-01, 133-02, 133-03, 133-04]
files_modified:
  - frontend-nextjs/tests/mocks/handlers.ts
  - frontend-nextjs/docs/API_ROBUSTNESS.md
autonomous: true

must_haves:
  truths:
    - "All API endpoints have comprehensive error scenario handlers"
    - "MSW handlers cover 401, 403, 404, 500, 503, 504, 429, network errors, timeouts"
    - "Documentation explains error recovery patterns"
    - "Test utilities are reusable across the codebase"
    - "CI/CD integration validates API robustness"
  artifacts:
    - path: frontend-nextjs/tests/mocks/handlers.ts
      provides: "Comprehensive MSW handlers with error scenarios"
    - path: frontend-nextjs/docs/API_ROBUSTNESS.md
      provides: "API robustness testing documentation"
      min_lines: 300
    - path: .github/workflows/frontend-api-robustness.yml
      provides: "CI/CD workflow for API robustness tests"
  key_links:
    - from: .github/workflows/frontend-api-robustness.yml
      to: frontend-nextjs/lib/__tests__/api/retry-logic.test.ts
      via: "npm test -- lib/__tests__/api/retry-logic.test.ts"
      pattern: "retry-logic\\.test"
    - from: .github/workflows/frontend-api-robustness.yml
      to: frontend-nextjs/tests/integration/api-robustness.test.tsx
      via: "npm test -- tests/integration/api-robustness.test.tsx"
      pattern: "api-robustness\\.test"
---

<objective>
Finalize Phase 133 with comprehensive documentation, MSW handler expansion, and CI/CD integration for API robustness testing.

Purpose: Ensure API robustness testing is sustainable and maintainable. Documentation helps developers write robust API code, CI/CD integration prevents regression, and expanded MSW handlers provide comprehensive error coverage.

Output: Complete MSW error scenario handlers, API robustness documentation, CI/CD workflow
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-frontend-api-integration-robustness/133-RESEARCH.md

@frontend-nextjs/tests/mocks/handlers.ts
@frontend-nextjs/tests/mocks/errors.ts
@frontend-nextjs/lib/api.ts
@.planning/phases/132-frontend-accessibility-compliance/132-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Expand MSW handlers with comprehensive error scenarios</name>
  <files>frontend-nextjs/tests/mocks/handlers.ts</files>
  <action>
Enhance tests/mocks/handlers.ts to include error scenario variants for all major endpoints:

For each existing handler, create error variants:
1. Agent API error handlers:
   - GET /api/atom-agent/agents → 500, 503, 429 variants
   - POST /api/atom-agent/chat/stream → timeout, 503 variants
   - GET /api/atom-agent/agents/:agentId/status → 404, 500 variants

2. Canvas API error handlers:
   - POST /api/canvas/submit → 403 (governance), 500, 503 variants
   - GET /api/canvas/status → 404, 504 variants
   - POST /api/canvas/:canvasId/execute → 403, 500 variants

3. Device API error handlers:
   - POST /api/devices/camera/snap → 503, timeout variants
   - POST /api/devices/screen/record/start → 403 (maturity), 500 variants
   - POST /api/devices/location → 503, network error variants

4. Integration API error handlers:
   - OAuth endpoints: access_denied, timeout variants
   - API endpoints: rate limit (429), service unavailable (503)

Pattern for error handler variants:
- Export as {endpoint}ErrorHandlers array
- Include proper error payloads (error_code, message, retry_after)
- Add ctx.delay() for timeout scenarios
- Use existing errorHandlers from errors.ts as reference

Add comment documentation at top of handlers.ts:
- List all available handler categories
- Explain how to override handlers in tests
- Show examples of common error testing patterns

DO NOT duplicate existing errorHandlers from errors.ts - reference them instead
  </action>
  <verify>
npm test -- tests/mocks/__tests__/handlers.test.ts --passWithNoTests
  </verify>
  <done>
handlers.ts includes error variants for all major endpoints, documentation comments added, no duplication of errors.ts
  </done>
</task>

<task type="auto">
  <name>Create API robustness documentation</name>
  <files>frontend-nextjs/docs/API_ROBUSTNESS.md</files>
  <action>
Create comprehensive API robustness documentation (300+ lines) with:

1. Overview section:
   - Purpose of API robustness testing
   - Stack: MSW + @lifeomic/attempt + Testing Library
   - Goals: retry logic, user-friendly errors, loading states

2. MSW Handler Usage:
   - How to use default handlers
   - How to override handlers for specific tests
   - Available error scenario handlers
   - Examples: network errors, server errors, timeouts

3. Error Message Mapping:
   - Using error-mapping.ts utilities
   - getUserFriendlyErrorMessage() examples
   - getErrorAction() examples
   - Custom error messages for specific scenarios

4. Retry Logic Configuration:
   - @lifeomic/attempt options explanation
   - Exponential backoff with jitter
   - Retryable vs non-retryable errors
   - Per-endpoint retry configuration

5. Loading State Testing:
   - Using waitFor and findBy* queries
   - Skeleton loader testing
   - Submit button disabled state testing
   - Slow endpoint simulation with ctx.delay()

6. Integration Testing Patterns:
   - Complete error recovery flows
   - Component-level error handling
   - Mocking network recovery
   - Testing retry exhaustion

7. Common Pitfalls:
   - Using fakeTimers for loading tests (anti-pattern)
   - Testing technical error messages (anti-pattern)
   - Retrying 4xx errors (anti-pattern)
   - Missing jitter in exponential backoff

8. CI/CD Integration:
   - Running API robustness tests in CI
   - Coverage requirements
   - Test execution time expectations

9. Troubleshooting:
   - MSW handler not responding
   - Retry not triggering
   - Loading state not visible
   - Test flakiness with async operations

Include code examples throughout
  </action>
  <verify>
ls -la frontend-nextjs/docs/API_ROBUSTNESS.md && wc -l frontend-nextjs/docs/API_ROBUSTNESS.md
  </verify>
  <done>
API_ROBUSTNESS.md created with 300+ lines, covers all sections, includes code examples
  </done>
</task>

<task type="auto">
  <name>Create CI/CD workflow for API robustness tests</name>
  <files>.github/workflows/frontend-api-robustness.yml</files>
  <action>
Create .github/workflows/frontend-api-robustness.yml with:

1. Workflow triggers:
   - push: frontend-nextjs/lib/api.ts, frontend-nextjs/lib/__tests__/api/
   - pull_request: frontend-nextjs/lib/api.ts, frontend-nextjs/lib/__tests__/api/
   - workflow_dispatch: manual trigger

2. Jobs:
   - retry-logic-tests: npm test -- lib/__tests__/api/retry-logic.test.ts
   - error-message-tests: npm test -- lib/__tests__/api/user-friendly-errors.test.ts
   - loading-state-tests: npm test -- lib/__tests__/api/loading-states.test.ts
   - integration-tests: npm test -- tests/integration/api-robustness.test.tsx
   - component-robustness: npm test -- components/__tests__/*.robustness.test.tsx

3. Configuration:
   - runs-on: ubuntu-latest
   - node-version: 18
   - timeout-minutes: 15
   - Cache node_modules for faster runs

4. Test execution:
   - npm ci
   - npm run test:ci for coverage
   - Separate job for integration tests (longer timeout)

5. Coverage requirements:
   - Enforce minimum coverage for api.ts (80%)
   - Fail build if coverage below threshold
   - Upload coverage artifacts

6. Reporting:
   - PR comments with test results summary
   - Fail-fast on critical test failures
   - Retry flaky tests (max 2 attempts)

Reference existing workflow: .github/workflows/frontend-a11y-tests.yml (from Phase 132)
  </action>
  <verify>
yamllint .github/workflows/frontend-api-robustness.yml || echo "yamllint not available, checking syntax" && cat .github/workflows/frontend-api-robustness.yml
  </verify>
  <done>
frontend-api-robustness.yml created, valid YAML syntax, includes 5 test jobs, coverage requirements configured
  </done>
</task>

<task type="auto">
  <name>Create phase verification document</name>
  <files>.planning/phases/133-frontend-api-integration-robustness/133-VERIFICATION.md</files>
  <action>
Create 133-VERIFICATION.md with:

1. Success Criteria Checklist (from ROADMAP.md):
   - [ ] MSW mocks all API endpoints with realistic error responses
   - [ ] Loading states tested for all async operations
   - [ ] Error states tested with user-friendly error messages
   - [ ] Retry logic tested with exponential backoff validation
   - [ ] Integration tests cover API failure recovery flows

2. Implementation Summary:
   - Plans executed: 133-01 through 133-05
   - Files created: error-mapping.ts, retry-logic.test.ts, loading-states.ts, etc.
   - Files modified: api.ts, handlers.ts
   - Test coverage improvements

3. Test Results:
   - Total tests created
   - Pass rate
   - Execution time
   - Coverage metrics

4. Verification by Truth:
   - For each truth, provide evidence it's satisfied
   - Link to test file or code location
   - Example output or screenshot if applicable

5. Known Limitations:
   - Any scenarios not covered
   - Performance considerations
   - Future enhancements

6. Handoff to Phase 134:
   - Dependent phases identified
   - Artifacts created for reuse
   - Recommendations for next phase
  </action>
  <verify>
ls -la .planning/phases/133-frontend-api-integration-robustness/133-VERIFICATION.md
  </verify>
  <done>
133-VERIFICATION.md created with all success criteria assessed, implementation summary complete
  </done>
</task>

</tasks>

<verification>
1. npm test -- lib/__tests__/api/ tests/integration/api-robustness.test.tsx passes (100% pass rate)
2. All success criteria from ROADMAP.md satisfied
3. MSW handlers include error scenarios for all major endpoints
4. API_ROBUSTNESS.md documentation complete (300+ lines)
5. CI/CD workflow created and tested
6. 133-VERIFICATION.md created with all criteria assessed
</verification>

<success_criteria>
1. All 5 plans (133-01 through 133-05) complete
2. MSW handlers expanded with error scenarios
3. API_ROBUSTNESS.md documentation created
4. CI/CD workflow for API robustness tests
5. Phase verification document complete
6. ROADMAP.md updated with Phase 133 completion
</success_criteria>

<output>
After completion, create:
- `.planning/phases/133-frontend-api-integration-robustness/133-05-SUMMARY.md`
- Update ROADMAP.md to mark Phase 133 complete
</output>
