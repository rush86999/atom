---
phase: 133-frontend-api-integration-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/lib/api.ts
autonomous: true

must_haves:
  truths:
    - "API retry logic uses exponential backoff with jitter"
    - "Retries only happen for transient errors (5xx, network), not 4xx client errors"
    - "Retry delay increases exponentially (1s, 2s, 4s) with randomness"
    - "@lifeomic/attempt library handles retry orchestration"
  artifacts:
    - path: frontend-nextjs/lib/api.ts
      provides: "API client with exponential backoff retry"
      exports: ["apiClient", "systemAPI", "agentAPI"]
    - path: frontend-nextjs/lib/__tests__/api/retry-logic.test.ts
      provides: "Retry logic validation tests"
      min_lines: 150
  key_links:
    - from: frontend-nextjs/lib/api.ts
      to: "@lifeomic/attempt"
      via: "import attempt from '@lifeomic/attempt'"
      pattern: "attempt\\("
---

<objective>
Enhance API retry logic with exponential backoff using @lifeomic/attempt (already installed) to prevent retry storms and improve transient failure recovery.

Purpose: Current retry logic uses simple linear backoff (1s, 1s, 1s) which can cause retry storms when multiple clients fail simultaneously. Exponential backoff with jitter randomizes retry delays, preventing synchronized retries from overwhelming the backend.

Output: Enhanced api.ts with @lifeomic/attempt integration, comprehensive retry logic tests
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-frontend-api-integration-robustness/133-RESEARCH.md

@frontend-nextjs/lib/api.ts
@frontend-nextjs/package.json
@frontend-nextjs/lib/__tests__/api/timeout-handling.test.ts
</context>

<tasks>

<task type="auto">
  <name>Enhance api.ts retry logic with @lifeomic/attempt exponential backoff</name>
  <files>frontend-nextjs/lib/api.ts</files>
  <action>
Replace the existing simple retry logic (lines 41-59) with @lifeomic/attempt integration:

1. Import attempt from '@lifeomic/attempt' (already in package.json v3.0.3)
2. Configure retry options:
   - maxAttempts: 3
   - delay: 1000 (initial 1 second)
   - factor: 2 (exponential backoff: 1s, 2s, 4s)
   - jitter: true (add randomness to prevent retry storms)
   - minDelay: 500, maxDelay: 10000
   - timeout: 10000
3. Add handleError predicate to distinguish retryable vs non-retryable errors:
   - DO NOT retry: 4xx errors (except 408 Request Timeout)
   - DO retry: 5xx errors (500, 503, 504)
   - DO retry: Network errors (ECONNABORTED, ETIMEDOUT, ECONNRESET, ENOTFOUND)
4. Preserve original request config (error.config) across retries
5. Add beforeAttempt logging for debugging

DO NOT change:
- MAX_RETRIES = 3
- API_TIMEOUT = 10000
- Existing API endpoint definitions
- Request interceptor (auth token handling)
  </action>
  <verify>
npm test -- lib/__tests__/api/retry-logic.test.ts --passWithNoTests
  </verify>
  <done>
Response interceptor uses @lifeomic/attempt with exponential backoff, jitter enabled, handleError predicate skips 4xx retries except 408, network errors retry correctly
  </done>
</task>

<task type="auto">
  <name>Create retry logic validation tests with MSW</name>
  <files>frontend-nextjs/lib/__tests__/api/retry-logic.test.ts</files>
  <action>
Create comprehensive retry logic tests (150+ lines):

1. Test exponential backoff timing:
   - Mock endpoint that fails 2x then succeeds
   - Capture attempt timestamps
   - Verify delay increases (delay2 > delay1, within jitter tolerance)

2. Test retry exhaustion:
   - Endpoint always returns 503
   - Verify MAX_RETRIES=3 attempts made
   - Verify error thrown after exhaustion

3. Test no retry on 4xx client errors:
   - Test 401 Unauthorized (1 attempt only)
   - Test 403 Forbidden (1 attempt only)
   - Test 404 Not Found (1 attempt only)

4. Test retry on transient errors:
   - Test 500 Internal Server Error (retries)
   - Test 503 Service Unavailable (retries)
   - Test 504 Gateway Timeout (retries)
   - Test network errors (retries)

5. Test request body preservation:
   - POST endpoint with request body
   - Fail first attempt, succeed second
   - Verify body preserved in retry

6. Test jitter prevents synchronized retries:
   - Multiple concurrent requests
   - Verify delays vary (not identical)

Use MSW rest handlers for mocking, server.use() for test-specific overrides
  </action>
  <verify>
npm test -- lib/__tests__/api/retry-logic.test.ts
  </verify>
  <done>
All retry tests pass: exponential backoff verified, 4xx no-retry confirmed, 5xx retry working, body preserved, jitter adding variance
  </done>
</task>

<task type="auto">
  <name>Create MSW retry scenario handlers</name>
  <files>frontend-nextjs/tests/mocks/scenarios/retry-scenarios.ts</files>
  <action>
Create dedicated retry scenario handlers for reuse across tests:

1. Create directory: tests/mocks/scenarios/
2. Create retry-scenarios.ts with:
   - createFlakyEndpoint(endpoint, failureCount): Fails N times then succeeds
   - createAlwaysFailingEndpoint(endpoint, statusCode): Always returns error
   - createSlowEndpoint(endpoint, delayMs): Delayed response for timeout testing
   - export const retryHandlers = [...] with common retry test scenarios

Each handler should:
- Track attempt count in closure
- Support dynamic success/failure based on attempt number
- Return realistic backend responses with proper error payloads
- Include ctx.delay() for timeout simulation

Export handlers to be used by:
- lib/__tests__/api/retry-logic.test.ts
- tests/integration/api-robustness.test.tsx (Plan 04)
  </action>
  <verify>
npm test -- tests/mocks/__tests__/retry-scenarios.test.ts
  </verify>
  <done>
retry-scenarios.ts exists with factory functions, exports handlers for common retry test scenarios, tests validate handler behavior
  </done>
</task>

</tasks>

<verification>
1. npm test -- lib/__tests__/api/retry-logic.test.ts passes all tests
2. Retry delays show exponential increase (1s -> 2s -> 4s) with jitter variance
3. 4xx errors trigger single attempt (no retry)
4. 5xx errors trigger 3 attempts (initial + 2 retries)
5. Request body preserved across retries
6. Jitter prevents identical delay timestamps in concurrent requests
</verification>

<success_criteria>
1. api.ts uses @lifeomic/attempt for retry orchestration
2. Exponential backoff configured (factor: 2, jitter: true)
3. handleError predicate correctly distinguishes retryable/non-retryable errors
4. All retry logic tests pass (100% pass rate)
5. MSW retry scenario handlers reusable across test suites
</success_criteria>

<output>
After completion, create `.planning/phases/133-frontend-api-integration-robustness/133-01-SUMMARY.md`
</output>
