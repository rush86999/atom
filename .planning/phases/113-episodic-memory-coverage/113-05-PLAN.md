---
phase: 113-episodic-memory-coverage
plan: 05
type: execute
wave: 1
depends_on: ["113-04"]
files_modified:
  - backend/tests/unit/episodes/test_episode_segmentation_coverage.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All 75+ tests pass without complex mock chain conflicts"
    - "Episode segmentation service coverage is at or above 60%"
    - "Helper methods tested directly (not through async create_episode_from_session)"
    - "Supervision episode methods tested for coverage"
    - "Canvas LLM summary integration tested"
  artifacts:
    - path: "backend/tests/unit/episodes/test_episode_segmentation_coverage.py"
      provides: "Unit tests for episode_segmentation_service.py"
      min_lines: 2400
      contains: "75+ passing tests"
    - path: "backend/tests/coverage_reports/metrics/phase_113_coverage_final.json"
      provides: "Final coverage metrics for phase 113"
      contains: "episode_segmentation_service coverage >= 60%"
  key_links:
    - from: "backend/tests/unit/episodes/test_episode_segmentation_coverage.py"
      to: "backend/core/episode_segmentation_service.py"
      via: "Direct helper method testing (not complex integration mocks)"
      pattern: "_calculate_duration|_generate_title|_extract_topics|_extract_entities"
---

## Objective

Fix the 6 remaining failing tests and add 15-20 new targeted tests to bring `episode_segmentation_service.py` coverage from 30.19% to 60%+.

**Purpose:** Complete the phase 113 coverage target for all three episodic memory services.

**Context from Plan 04:**
- 6 failing tests are integration tests calling `create_episode_from_session()` with complex mock chains
- Root cause: `db.query().filter().filter()` mock chain conflicts between different query types
- Recommended approach: Refactor to helper method testing (test `_calculate_duration()`, `_generate_title()`, etc. directly)

**Output:** 75+ passing tests, 60%+ coverage for episode_segmentation_service.py

---

## Execution Context

@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md

---

## Context

@.planning/phases/113-episodic-memory-coverage/113-04-SUMMARY.md
@.planning/phases/113-episodic-memory-coverage/113-VERIFICATION.md
@/Users/rushiparikh/projects/atom/backend/core/episode_segmentation_service.py
@/Users/rushiparikh/projects/atom/backend/tests/unit/episodes/test_episode_segmentation_coverage.py

---

## Tasks

<task type="auto">
  <name>Refactor 6 failing tests to helper method tests</name>
  <files>backend/tests/unit/episodes/test_episode_segmentation_coverage.py</files>
  <action>
    Replace or restructure the 6 failing integration tests to test helper methods directly instead of through `create_episode_from_session()`. The failing tests are:

    1. `test_create_episode_from_session_calculates_duration` → Test `_calculate_duration()` directly
    2. `test_create_episode_from_session_minimum_size_enforced` → Test minimum size logic directly
    3. `test_create_episode_from_session_includes_canvas_context` → Test `_extract_canvas_context()` directly
    4. `test_create_episode_from_session_includes_feedback_context` → Test `_calculate_feedback_score()` directly
    5. `test_create_episode_from_session_uses_agent_maturity` → Test `_get_agent_maturity()` directly
    6. `test_create_episode_from_session_with_llm_canvas_summary` → Test `_extract_canvas_context_llm()` directly

    **Pattern for refactoring:**
    - Remove complex `db_session.query()` mock chain setup
    - Create simple test data (messages, executions directly)
    - Call the helper method directly with test data
    - Assert on the return value

    Example refactoring for duration:
    ```python
    def test_calculate_duration_from_messages(self, segmentation_service):
        now = datetime.now()
        messages = [
            ChatMessage(id="m1", created_at=now - timedelta(minutes=30)),
            ChatMessage(id="m2", created_at=now)
        ]
        duration = segmentation_service._calculate_duration(messages, [])
        assert duration == 1800  # 30 minutes in seconds
    ```

    DO NOT create new integration tests with complex mock chains.
  </action>
  <verify>pytest backend/tests/unit/episodes/test_episode_segmentation_coverage.py::TestEpisodeCreation -v --tb=short</verify>
  <done>All 6 tests pass with simplified helper method testing</done>
</task>

<task type="auto">
  <name>Add 15-20 new tests for uncovered code paths</name>
  <files>backend/tests/unit/episodes/test_episode_segmentation_coverage.py</files>
  <action>
    Add 15-20 new tests targeting uncovered lines in episode_segmentation_service.py. Focus on these uncovered sections:

    **1. Canvas Context Integration (lines 128-147, 647-736):**
    - `test_filter_canvas_context_detail_summary()` - Test summary detail level
    - `test_filter_canvas_context_detail_standard()` - Test standard detail level
    - `test_filter_canvas_context_detail_full()` - Test full detail level
    - `test_filter_canvas_context_detail_unknown_level()` - Test fallback for unknown level

    **2. Supervision Episodes (lines 1047-1390):**
    - `test_format_agent_actions_with_execution()` - Test execution formatting
    - `test_format_agent_actions_without_execution()` - Test null execution handling
    - `test_format_interventions_empty_list()` - Test empty interventions
    - `test_extract_supervision_topics_from_agent_name()` - Test topic extraction from agent name
    - `test_extract_supervision_topics_with_interventions()` - Test intervention topics
    - `test_archive_supervision_episode_to_lancedb()` - Test archival

    **3. Skill Episodes (lines 1396-1502):**
    - `test_create_skill_episode_success()` - Test successful skill episode
    - `test_create_skill_episode_with_error()` - Test skill episode with error
    - `test_summarize_skill_inputs_truncation()` - Test input summary truncation
    - `test_format_skill_content_with_error()` - Test error formatting

    **4. Entity Extraction (lines 362-419):**
    - `test_extract_entities_from_execution_metadata()` - Test metadata entity extraction
    - `test_extract_entities_phone_numbers()` - Test phone regex extraction
    - `test_extract_entities_urls()` - Test URL extraction

    **5. Topic Extraction (lines 346-360):**
    - `test_extract_topics_with_none_content()` - Test None content handling
    - `test_extract_topics_limit_to_five()` - Test topic limiting

    Use direct method calls with test data - NO complex mock chains.
  </action>
  <verify>pytest backend/tests/unit/episodes/test_episode_segmentation_coverage.py -v --cov=backend/core/episode_segmentation_service --cov-report=term-missing</verify>
  <done>Coverage for episode_segmentation_service.py >= 60%</done>
</task>

<task type="auto">
  <name>Verify all tests pass and generate coverage report</name>
  <files>backend/tests/coverage_reports/metrics/phase_113_coverage_final.json</files>
  <action>
    Run the full test suite and generate final coverage report:

    1. Run all segmentation tests: `pytest backend/tests/unit/episodes/test_episode_segmentation_coverage.py -v`
    2. Generate coverage: `pytest backend/tests/unit/episodes/test_episode_segmentation_coverage.py --cov=backend/core/episode_segmentation_service --cov-report=json --cov-report=term`
    3. Copy coverage report to `backend/tests/coverage_reports/metrics/phase_113_coverage_final.json`
    4. Verify pass rate is 100% (75+ tests)
    5. Verify coverage >= 60%

    If coverage < 60%, add more targeted tests for remaining uncovered lines.
  </action>
  <verify>pytest backend/tests/unit/episodes/test_episode_segmentation_coverage.py --cov=backend/core/episode_segmentation_service --cov-report=term-missing | grep -E "(TOTAL|episode_segmentation_service)"</verify>
  <done>
    - All 75+ tests pass
    - episode_segmentation_service.py coverage >= 60%
    - Coverage report saved to phase_113_coverage_final.json
  </done>
</task>

---

## Verification

1. All tests pass: `pytest backend/tests/unit/episodes/test_episode_segmentation_coverage.py -v` returns 0 exit code
2. Coverage >= 60%: Check coverage report shows episode_segmentation_service >= 60%
3. No mock chain conflicts: Tests use direct helper method calls, not complex integration flows
4. All three episodic memory services now have 60%+ coverage:
   - episode_lifecycle_service.py: 91.47% ✅
   - episode_retrieval_service.py: 66.45% ✅
   - episode_segmentation_service.py: >= 60% ✅

---

## Success Criteria

1. **Test Pass Rate:** 100% (75+ tests passing, 0 failing)
2. **Coverage:** episode_segmentation_service.py >= 60% (up from 30.19%)
3. **Phase Goal:** All three episodic memory services achieve 60%+ coverage
4. **Code Quality:** Tests use direct helper method testing (not complex mock chains)

---

## Output

After completion, create `.planning/phases/113-episodic-memory-coverage/113-05-SUMMARY.md` with:
- Test results (passing count, failing count)
- Coverage metrics (before/after)
- List of new tests added
- Coverage gap closure achieved
