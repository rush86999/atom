---
phase: 113-episodic-memory-coverage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/episodes/test_episode_segmentation_coverage.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All 47 tests pass (37 existing + 10 fixed)"
    - "Coverage report shows episode_segmentation_service.py coverage at or above current 30.19%"
    - "No model field mismatches between tests and actual model definitions"
    - "AgentStatus enum properly mocked for maturity-related tests"
  artifacts:
    - path: "backend/tests/unit/episodes/test_episode_segmentation_coverage.py"
      provides: "Fixed tests for episode segmentation service"
      min_lines: 2000
  key_links:
    - from: "backend/tests/unit/episodes/test_episode_segmentation_coverage.py"
      to: "backend/core/models.py"
      via: "Model field alignment"
      pattern: "input_summary|canvas_action_count|intervention_type"
---

<objective>
Fix 10 failing tests in test_episode_segmentation_coverage.py by correcting model field mismatches and mock configurations.

**Purpose:** Tests are failing due to model field changes from Plans 01/02:
1. `task_description` -> `input_summary` in AgentExecution
2. Missing `canvas_action_count` field in Episode fixtures (causes NoneType comparison errors)
3. `intervention_type` field doesn't exist in SupervisionSession model
4. AgentStatus enum mocking issues (using .value on MagicMock)

This is a test infrastructure fix, not new functionality. Use proven patterns from Phase 112.

**Output:** 10 fixed tests + coverage measurement to determine if additional tests needed for 60% target
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-episodic-memory-coverage/113-VERIFICATION.md
@.planning/phases/113-episodic-memory-coverage/113-02-PLAN.md

# Source files
@backend/core/episode_segmentation_service.py (1503 lines)
@backend/core/models.py (AgentExecution, Episode, SupervisionSession, AgentStatus)
@backend/tests/unit/episodes/test_episode_segmentation_coverage.py (2006 lines, 47 tests)

# Model field reference
- AgentExecution: uses `input_summary` (NOT `task_description`)
- Episode: has `canvas_action_count` field (default=0, required for comparisons)
- SupervisionSession: NO `intervention_type` field (has `interventions` JSON array)
- AgentStatus: enum with STUDENT/INTERN/SUPERVISED/AUTONOMOUS values

# Proven patterns from Phase 112
@backend/tests/unit/governance/test_agent_governance_coverage.py (proper enum mocking)
@backend/tests/unit/episodes/test_episode_lifecycle_coverage.py (Episode fixtures)
</context>

<tasks>

<task type="auto">
  <name>Fix AgentExecution and Episode model field mismatches</name>
  <files>backend/tests/unit/episodes/test_episode_segmentation_coverage.py</files>
  <action>
    Fix 7 failing tests with model field mismatches:

    Tests to fix (lines 661-713, 715-780, 782-855, 927-978, 980-1036):

    1. test_create_episode_from_session_calculates_duration (line 628)
       - Issue: Missing canvas_action_count=0 in Episode comparisons
       - Fix: Episode object created in test needs canvas_action_count=0

    2. test_create_episode_from_session_minimum_size_enforced (line 661)
       - Issue: Missing canvas_action_count causes NoneType comparison
       - Fix: Add canvas_action_count=0 to Episode creation in mock_add

    3. test_create_episode_from_session_includes_canvas_context (line 715)
       - Issue: Missing canvas_action_count causes NoneType comparison
       - Fix: Add canvas_action_count=0 to Episode creation

    4. test_create_episode_from_session_includes_feedback_context (line 782)
       - Issue: AgentExecution uses task_description (should be input_summary)
       - Fix: Change task_description="Help task" to input_summary="Help task"
       - Also fix AgentFeedback fields (execution_id -> agent_execution_id)

    5. test_create_episode_from_session_with_llm_canvas_summary (line 980)
       - Issue: Missing canvas_action_count in assertions
       - Fix: Ensure canvas_action_count is set when episode created

    6. test_create_episode_from_session_uses_agent_maturity (line 876)
       - Issue: AgentStatus enum mock returns MagicMock instead of string
       - Fix: Mock AgentRegistry.status to return enum value directly, not MagicMock

    7. test_create_episode_from_session_captures_maturity (line 618)
       - Issue: Same AgentStatus enum mock issue
       - Fix: Same fix as #6

    Pattern for canvas_action_count fix:
    In mock_add function, add: obj.canvas_action_count = getattr(obj, 'canvas_action_count', 0)

    Pattern for AgentStatus fix:
    mock_agent = AgentRegistry(id=agent_id, status=AgentStatus.AUTONOMOUS.value)
    Or use: mock_agent.status = "AUTONOMOUS"

    Pattern for AgentExecution fix:
    Replace: task_description="..."
    With: input_summary="..."
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/episodes/test_episode_segmentation_coverage.py::TestEpisodeCreation::test_create_episode_from_session_calculates_duration tests/unit/episodes/test_episode_segmentation_coverage.py::TestEpisodeCreation::test_create_episode_from_session_minimum_size_enforced tests/unit/episodes/test_episode_segmentation_coverage.py::TestEpisodeCreation::test_create_episode_from_session_includes_canvas_context tests/unit/episodes/test_episode_segmentation_coverage.py::TestEpisodeCreation::test_create_episode_from_session_includes_feedback_context tests/unit/episodes/test_episode_segmentation_coverage.py::TestEpisodeCreation::test_create_episode_from_session_with_llm_canvas_summary tests/unit/episodes/test_episode_segmentation_coverage.py::TestEpisodeCreation::test_create_episode_from_session_uses_agent_maturity -v</verify>
  <done>All 7 session episode tests pass with corrected model fields</done>
</task>

<task type="auto">
  <name>Fix SupervisionSession model field mismatches</name>
  <files>backend/tests/unit/episodes/test_episode_segmentation_coverage.py</files>
  <action>
    Fix 3 failing supervision episode tests:

    Tests to fix (lines 1276-1301, 1303-1326, 1327-1334):

    1. test_create_supervision_episode_from_supervision_session (line 1276)
       - Issue: SupervisionSession doesn't have intervention_type field
       - Fix: Remove intervention_type="human_correction" from SupervisionSession fixture
       - Use interventions=[{"type": "human_correction", ...}] instead

    2. test_create_supervision_episode_includes_intervention_details (line 1303)
       - Issue: Same intervention_type field issue
       - Fix: Replace intervention_type with interventions JSON array
       - Example: interventions=[{"action": "pause", "reason": "Safety concern"}]

    3. test_create_supervision_episode_graduation_tracking (line 1327)
       - Issue: Same intervention_type field issue
       - Fix: Remove intervention_type from SupervisionSession fixture

    SupervisionSession model has:
    - interventions (JSON, default=list) - array of intervention records
    - intervention_count (Integer, default=0)
    - NO intervention_type field

    Correct pattern:
    supervision = SupervisionSession(
        id=session_id,
        agent_id=agent_id,
        status="active",
        interventions=[{"type": "pause", "timestamp": datetime.now().isoformat()}],
        intervention_count=1,
        created_at=datetime.now()
    )
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/episodes/test_episode_segmentation_coverage.py::TestSupervisionEpisodeCreation -v</verify>
  <done>All 6 supervision episode tests pass with corrected model fields</done>
</task>

<task type="auto">
  <name>Verify all 47 tests pass and measure coverage</name>
  <files>backend/tests/unit/episodes/test_episode_segmentation_coverage.py</files>
  <action>
    Run full test suite to verify all 47 tests pass:

    1. Run: pytest tests/unit/episodes/test_episode_segmentation_coverage.py -v
    2. Verify: 47 passed, 0 failed
    3. Generate coverage report: pytest --cov=core.episode_segmentation_service --cov-report=term-missing tests/unit/episodes/test_episode_segmentation_coverage.py
    4. Record current coverage percentage after fixes
    5. Calculate gap to 60% target
    6. Document findings for potential Plan 05 (if more tests needed)

    If coverage still below 60% after fixes:
    - Identify uncovered lines (missing from coverage report)
    - Calculate how many more tests needed
    - Note this in SUMMARY.md for next plan decision

    If coverage at or above 60% after fixes:
    - Phase 113 goal achieved for segmentation service
    - All 3 services now meet 60%+ target
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/episodes/test_episode_segmentation_coverage.py -v --cov=core.episode_segmentation_service --cov-report=term-missing</verify>
  <done>All 47 tests pass, coverage measured, gap to 60% calculated</done>
</task>

</tasks>

<verification>
1. All 47 tests pass (37 previously passing + 10 fixed)
2. Model field mismatches resolved:
   - AgentExecution uses input_summary (not task_description)
   - Episode fixtures include canvas_action_count=0
   - SupervisionSession uses interventions JSON array (not intervention_type field)
   - AgentStatus enum mocked properly with string values
3. Coverage report shows episode_segmentation_service.py coverage (current vs target)
4. No NoneType comparison errors
5. Documented gap to 60% if additional tests needed
</verification>

<success_criteria>
1. 10 failing tests now pass
2. All 47 tests in file pass (100% pass rate)
3. Model fields align with backend/core/models.py definitions
4. Coverage measured and gap to 60% documented
5. Test infrastructure issues resolved using Phase 112 patterns
</success_criteria>

<output>
After completion, create `.planning/phases/113-episodic-memory-coverage/113-04-SUMMARY.md`

Summary must include:
- Tests fixed (which tests and what changed)
- Current coverage percentage
- Gap to 60% target
- Recommendation for Plan 05 (more tests needed or goal achieved)
</output>
