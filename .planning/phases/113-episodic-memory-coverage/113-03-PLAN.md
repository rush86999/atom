---
phase: 113-episodic-memory-coverage
plan: 03
type: execute
wave: 2
depends_on: ["113-01", "113-02"]
files_modified:
  - backend/tests/unit/episodes/test_episode_lifecycle_coverage.py
autonomous: true

must_haves:
  truths:
    - "Coverage report shows 65%+ coverage for episode_lifecycle_service.py (up from 59.69%)"
    - "Tests cover batch update access counts method"
    - "Tests cover edge cases in consolidation and archival"
    - "All three episode services achieve 60%+ coverage target"
  artifacts:
    - path: "backend/tests/unit/episodes/test_episode_lifecycle_coverage.py"
      provides: "Comprehensive tests for episode lifecycle service"
      min_lines: 550
  key_links:
    - from: "backend/tests/unit/episodes/test_episode_lifecycle_coverage.py"
      to: "backend/core/episode_lifecycle_service.py"
      via: "Direct method testing with mocked database"
      pattern: "batch_update_access_counts|decay_old_episodes|consolidate_similar_episodes"
---

<objective>
Complete episode lifecycle service coverage to reach 65%+ and verify all three services achieve 60%+ target.

**Purpose:** The episode lifecycle service is very close to 60% coverage (59.69%) and needs only a few more tests to cross the threshold. This plan adds the missing coverage for batch_update_access_counts, edge cases in consolidation, and final verification that all three services achieve the 60% target.

**Output:** 6 new tests for lifecycle service, combined verification run confirming all three services ≥60%
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-episodic-memory-coverage/113-RESEARCH.md

# Source files
@backend/core/episode_lifecycle_service.py (251 lines, 6 functions, 59.69% coverage)
@backend/tests/unit/episodes/test_episode_lifecycle_coverage.py (existing 15 tests)
@backend/tests/unit/episodes/conftest.py (shared fixtures)
@backend/tests/factories/episode_factory.py (test data factories)

# Proven patterns from Phase 112
@backend/tests/unit/governance/test_agent_governance_coverage.py (comprehensive test structure)
@backend/tests/unit/governance/test_agent_context_resolver.py (async test patterns)
</context>

<tasks>

<task type="auto">
  <name>Add tests for batch update access counts and edge cases</name>
  <files>backend/tests/unit/episodes/test_episode_lifecycle_coverage.py</files>
  <action>
    Add 6 new tests to complete lifecycle service coverage:

    Add to TestLifecycleService (new class or extend existing):
    1. test_batch_update_access_counts_multiple_episodes
       - Mock 3 episodes with different access counts
       - Mock query chain to return episodes by ID
       - Verify each episode's access_count incremented
       - Verify updated count matches number of IDs

    2. test_batch_update_access_counts_empty_list
       - Mock empty episode_ids list
       - Verify returns {"updated": 0}
       - Verify no queries made

    3. test_batch_update_access_counts_nonexistent_episodes
       - Mock query to return None for some IDs
       - Verify handles missing episodes gracefully
       - Verify only existing episodes updated

    4. test_batch_update_access_counts_duplicate_ids
       - Mock episode_ids with duplicates
       - Verify each episode updated once
       - Verify updated count accurate

    5. test_consolidate_similar_episodes_json_metadata_parsing
       - Mock LanceDB search with string metadata (not dict)
       - Verify JSON parsing handled correctly
       - Verify episode_id extracted from parsed metadata

    6. test_archive_to_cold_storage_nonexistent_episode
       - Mock query to return None for episode_id
       - Verify returns False (not exception)
       - Verify no commit attempted

    Pattern from Phase 112: Use uuid.uuid4() for unique IDs, mock query chains with filter().first() pattern for single record lookups.
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/episodes/test_episode_lifecycle_coverage.py -v</verify>
  <done>All 6 new tests pass, lifecycle service coverage ≥65%</done>
</task>

<task type="auto">
  <name>Run combined coverage verification for all three services</name>
  <files>backend/tests/unit/episodes/test_episode_segmentation_coverage.py,backend/tests/unit/episodes/test_episode_retrieval_coverage.py,backend/tests/unit/episodes/test_episode_lifecycle_coverage.py</files>
  <action>
    Run comprehensive coverage verification for all three episode services:

    Command:
    ```bash
    cd backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
      tests/unit/episodes/test_episode_segmentation_coverage.py \
      tests/unit/episodes/test_episode_retrieval_coverage.py \
      tests/unit/episodes/test_episode_lifecycle_coverage.py \
      --cov=core.episode_segmentation_service \
      --cov=core.episode_retrieval_service \
      --cov=core.episode_lifecycle_service \
      --cov-report=term-missing \
      --cov-report=html:tests/coverage_reports/html/phase_113
    ```

    Expected outcomes:
    - episode_segmentation_service.py: ≥60% coverage (Plan 01 brought it from 23.47% to ~50%+)
    - episode_retrieval_service.py: ≥60% coverage (Plan 02 brought it from 33.98% to ~60%+)
    - episode_lifecycle_service.py: ≥65% coverage (from 59.69% with new tests)

    If any service is below 60%, analyze uncovered lines and add targeted tests in this same task to reach threshold.

    Coverage report location: backend/tests/coverage_reports/html/phase_113/index.html
  </action>
  <verify>
    Coverage report shows all three services ≥60%:
    - episode_segmentation_service.py ≥60%
    - episode_retrieval_service.py ≥60%
    - episode_lifecycle_service.py ≥60%
  </verify>
  <done>
    All three episode services achieve 60%+ coverage target
    Combined coverage report generated
    Phase 113 success criteria met
  </done>
</task>

</tasks>

<verification>
1. All 6 new tests pass for lifecycle service
2. episode_lifecycle_service.py coverage ≥65% (exceeds 60% target)
3. Combined coverage run confirms all three services ≥60%
4. Coverage report HTML generated for phase 113
5. Phase 113 CORE-02 requirement success criteria met
</verification>

<success_criteria>
1. 6 new tests added to test_episode_lifecycle_coverage.py
2. episode_lifecycle_service.py coverage ≥65% (exceeds 60% target)
3. All three episode services achieve 60%+ coverage:
   - episode_segmentation_service.py ≥60%
   - episode_retrieval_service.py ≥60%
   - episode_lifecycle_service.py ≥60%
4. Combined coverage report generated
5. No test failures when running full episode test suite
</success_criteria>

<output>
After completion, create `.planning/phases/113-episodic-memory-coverage/113-03-SUMMARY.md` and `.planning/phases/113-episodic-memory-coverage/113-VERIFICATION.md`
</output>
