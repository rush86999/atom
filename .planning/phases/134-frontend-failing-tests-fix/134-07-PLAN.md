---
phase: 134-frontend-failing-tests-fix
plan: 07
type: execute
wave: 4
depends_on: [134-01, 134-02, 134-03, 134-04, 134-05, 134-06]
files_modified:
  - frontend-nextjs/tests/accessibility.test.tsx
  - frontend-nextjs/tests/test-helpers/__tests__/loading-state.test.tsx
  - frontend-nextjs/tests/mocks/__tests__/msw-setup.test.ts
  - frontend-nextjs/tests/mocks/__tests__/retry-scenarios.test.ts
  - frontend-nextjs/tests/mocks/__tests__/error-recovery.test.ts
  - frontend-nextjs/tests/integration/__tests__/*.test.tsx
autonomous: true

must_haves:
  truths:
    - "All frontend tests pass (100% pass rate achieved)"
    - "No flaky tests detected when running test suite multiple times"
    - "Test execution time is acceptable (<30 seconds for full suite)"
    - "Coverage report generates without errors"
  artifacts:
    - path: frontend-nextjs/tests/
      provides: Complete test suite with 100% pass rate
      contains: "**/*.test.{ts,tsx}"
  key_links:
    - from: jest.config.js
      to: all test files
      via: "testMatch pattern"
      pattern: "**/__tests__/**/*.test.[jt]s"
---

<objective>
Verify 100% test pass rate and fix remaining edge cases

Purpose: Run full test suite to verify all fixes, identify any remaining edge case failures, and ensure flaky test detection is working

Output: Frontend test suite achieving 100% pass rate
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-frontend-failing-tests-fix/134-RESEARCH.md
@frontend-nextjs/jest.config.js

# Research key findings:
# - Common pitfall #7: Flaky test prevention
# - Need to verify tests pass consistently across multiple runs
# - Phase goal: 40% pass rate -> 100% pass rate
</context>

<tasks>

<task type="auto">
  <name>Run full test suite and fix edge cases</name>
  <files>frontend-nextjs/tests/accessibility.test.tsx, frontend-nextjs/tests/test-helpers/__tests__/loading-state.test.tsx, frontend-nextjs/tests/mocks/__tests__/*.test.ts, frontend-nextjs/tests/integration/__tests__/*.test.tsx</files>
  <action>
    Run the complete test suite and fix any remaining edge case failures:

    1. **Run full test suite**:
       ```bash
       cd /Users/rushiparikh/projects/atom/frontend-nextjs
       npm test -- --passWithNoTests --coverage 2>&1 | tee test-results.txt
       ```

    2. **Analyze remaining failures** from test output:
       - Count passing vs failing tests
       - Categorize failures by error type
       - Identify if any are truly flaky (intermittent)

    3. **Fix edge case failures**:
       For each remaining failing test:
       - Add missing waitFor for timing issues
       - Fix mock data mismatches
       - Correct assertion expectations
       - Add missing test setup/teardown

    4. **Run test suite 3 times** to detect flakiness:
       ```bash
       for i in 1 2 3; do npm test -- 2>&1 | grep -E "(PASS|FAIL)" | tail -1; done
       ```
       If results differ between runs, tests are flaky and need:
       - Longer waitFor timeouts
       - Better mock isolation
       - Deterministic test data (no Math.random())

    5. **Common edge case fixes**:
       - **Missing `await` keyword**: Add before async assertions
       - **Race conditions**: Use `findBy*` instead of `getBy*`
       - **Mock not resetting**: Add cleanup in afterEach
       - **Selector ambiguity**: Use more specific test IDs or data attributes

    6. **Document any intentional skips**:
       ```typescript
       test.skip('expensive operation - requires manual verification', () => {
         // This test requires real browser/different environment
       });
       ```
  </action>
  <verify>cd /Users/rushiparikh/projects/atom/frontend-nextjs && npm test -- 2>&1 | grep -E "Tests:.*passed|PASS|FAIL"</verify>
  <done>Full test suite passes with 100% pass rate</done>
</task>

</tasks>

<verification>
After fixing edge cases, verify:
1. `npm test` shows 0 failures
2. Running tests 3 times produces consistent results (no flaky tests)
3. Coverage report generates successfully
4. No tests have `test.skip` unless intentionally documented
</verification>

<success_criteria>
1. All frontend tests pass (100% pass rate)
2. No flaky tests detected across 3 consecutive runs
3. Test suite completes in <30 seconds
4. Coverage report shows no collection errors
5. From 21/35 failing tests to 0/35 failing tests
</success_criteria>

<output>
After completion, create `.planning/phases/134-frontend-failing-tests-fix/134-07-SUMMARY.md`
</output>
