---
phase: 134-frontend-failing-tests-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/tests/mocks/handlers.ts
autonomous: true

must_haves:
  truths:
    - "MSW handlers.ts file parses without syntax errors"
    - "Duplicate comment closing character removed from line 76"
    - "MSW server imports successfully in setup.ts"
    - "No 'Jest encountered an unexpected token' errors in test output"
  artifacts:
    - path: frontend-nextjs/tests/mocks/handlers.ts
      provides: MSW API endpoint handlers for all mock scenarios
      contains: "import { rest, RestHandler } from 'msw'"
      min_lines: 1900
  key_links:
    - from: frontend-nextjs/tests/setup.ts
      to: frontend-nextjs/tests/mocks/handlers.ts
      via: "require('./mocks/server').server"
      pattern: "require.*mocks/server"
---

<objective>
Fix MSW handlers syntax error blocking 21+ frontend tests

Purpose: Remove duplicate `*/` comment closing character on line 76 of handlers.ts that prevents Jest from parsing the file, causing MSW server initialization to fail and cascading into 21+ test failures

Output: Fixed handlers.ts file with valid syntax, enabling MSW server to load properly
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-frontend-failing-tests-fix/134-RESEARCH.md
@frontend-nextjs/tests/mocks/handlers.ts

# Research key findings:
# - Line 76 has duplicate `*/` causing syntax error
# - This blocks MSW server initialization completely
# - 21+ tests fail due to undefined server references
</context>

<tasks>

<task type="auto">
  <name>Fix duplicate comment closing in handlers.ts</name>
  <files>frontend-nextjs/tests/mocks/handlers.ts</files>
  <action>
    Remove the duplicate `*/` on line 76 of frontend-nextjs/tests/mocks/handlers.ts

    Current state (lines 73-77):
    ```typescript
     * Note: This file contains endpoint-specific handlers. For generic error handlers
     * (network errors, malformed responses, etc.), see tests/mocks/errors.ts
     */
     */
    //     ^^^ DUPLICATE closing comment
    ```

    Change to (remove line 76):
    ```typescript
     * Note: This file contains endpoint-specific handlers. For generic error handlers
     * (network errors, malformed responses, etc.), see tests/mocks/errors.ts
     */
    ```

    Do NOT modify any other content in the file - the rest of the handlers are correctly implemented from Phase 133.
  </action>
  <verify>cd /Users/rushiparikh/projects/atom/frontend-nextjs && npx tsc --noEmit tests/mocks/handlers.ts 2>&1 | grep -v "node_modules" || echo "TypeScript compilation successful"</verify>
  <done>handlers.ts parses without syntax errors, MSW server imports successfully</done>
</task>

</tasks>

<verification>
After fixing the syntax error, verify:
1. `npx tsc --noEmit tests/mocks/handlers.ts` completes without syntax errors
2. `node -e "require('./tests/mocks/server')"` executes without throwing
3. Test output no longer shows "Jest encountered an unexpected token" errors
4. MSW warning in setup.ts should no longer appear
</verification>

<success_criteria>
1. handlers.ts file has no syntax errors (TypeScript compilation passes)
2. Line 76 contains only one `*/` (the comment block closer)
3. MSW server loads successfully when required
4. Related tests that were blocked by syntax error now show different failures (not import errors)
</success_criteria>

<output>
After completion, create `.planning/phases/134-frontend-failing-tests-fix/134-01-SUMMARY.md`
</output>
