---
phase: 173-high-impact-zero-coverage-llm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_cognitive_tier_routes.py
files_created:
  - backend/tests/api/test_cognitive_tier_routes.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Cognitive tier routes API endpoints return correct HTTP status codes"
    - "GET /preferences/{workspace_id} returns defaults when no preference exists"
    - "POST /preferences/{workspace_id} creates or updates tier preferences"
    - "PUT /preferences/{workspace_id}/budget updates only budget fields"
    - "GET /estimate-cost returns cost estimates for all cognitive tiers"
    - "GET /compare-tiers returns tier comparison data"
    - "DELETE /preferences/{workspace_id} removes custom preferences"
    - "Invalid tier values return 400 Bad Request"
    - "Negative budget values return 400 Bad Request"
  artifacts:
    - path: "backend/tests/api/test_cognitive_tier_routes.py"
      provides: "TestClient-based API tests for cognitive tier routes"
      min_lines: 450
      exports: ["TestGetPreferences", "TestCreateOrUpdatePreferences", "TestUpdateBudget", "TestEstimateCost", "TestCompareTiers", "TestDeletePreferences"]
  key_links:
    - from: "backend/tests/api/test_cognitive_tier_routes.py"
      to: "backend/api/cognitive_tier_routes.py"
      via: "from api.cognitive_tier_routes import router"
      pattern: "from fastapi.testclient import TestClient"
---

<objective>
Create comprehensive TestClient-based API tests for cognitive tier routes to achieve 75%+ line coverage on api/cognitive_tier_routes.py (601 lines).

This file contains 6 endpoints for managing cognitive tier preferences, cost estimation, and tier comparison. The tests will follow Phase 172 patterns for TestClient-based API route testing with proper fixture setup, database mocking, and comprehensive coverage of success and error paths.

Purpose: Ensure all cognitive tier management endpoints are thoroughly tested with proper request validation, response serialization, and error handling.
Output: TestClient-based test file with 6+ test classes covering all endpoints (30+ tests total)
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/173-high-impact-zero-coverage-llm/173-RESEARCH.md

@backend/api/cognitive_tier_routes.py
@backend/tests/api/test_agent_governance_routes.py (Phase 172 pattern reference)
@backend/core/llm/cognitive_tier_system.py (for tier enum values)
</context>

<tasks>

<task type="auto">
  <name>Create TestClient fixtures and test class for GET /preferences/{workspace_id}</name>
  <files>backend/tests/api/test_cognitive_tier_routes.py</files>
  <action>
Create the test file with the following structure:

1. Import required modules:
   - pytest, uuid, unittest.mock (patch, MagicMock)
   - TestClient from fastapi.testclient
   - Session from sqlalchemy.orm
   - router from api.cognitive_tier_routes
   - CognitiveTierPreference from core.models

2. Create fixtures (following Phase 172 pattern):
   - client(): TestClient fixture that creates FastAPI app and includes router
   - db_session(): Database session fixture with transaction rollback
   - mock_preference(): Fixture that creates a test CognitiveTierPreference record

3. Create TestGetPreferences class with tests:
   - test_get_preferences_default_when_none_exists: Verify default values are returned (200 status, default_tier="standard", enable_cache_aware_routing=True)
   - test_get_preferences_returns_existing_preference: Verify existing preference is returned with all fields
   - test_get_preferences_with_full_preference_data: Test with all preference fields populated

4. Target coverage: Lines 132-188 in cognitive_tier_routes.py (get_preferences function)
  </action>
  <verify>Run pytest backend/tests/api/test_cognitive_tier_routes.py::TestGetPreferences -v</verify>
  <done>All GET /preferences tests pass (3 tests), get_preferences function achieves 80%+ coverage</done>
</task>

<task type="auto">
  <name>Create test class for POST /preferences/{workspace_id} (create/update preferences)</name>
  <files>backend/tests/api/test_cognitive_tier_routes.py</files>
  <action>
Create TestCreateOrUpdatePreferences class with tests:

1. Success cases:
   - test_create_new_preference_success: POST with valid data creates new preference (201 status or 200)
   - test_update_existing_preference_success: POST to existing workspace_id updates preference
   - test_create_preference_with_all_fields: Test all optional fields (min_tier, max_tier, budgets, providers, flags)

2. Validation error cases (400 Bad Request):
   - test_invalid_default_tier_returns_400: POST with invalid tier value (e.g., "invalid_tier")
   - test_invalid_min_tier_returns_400: POST with invalid min_tier value
   - test_invalid_max_tier_returns_400: POST with invalid max_tier value
   - test_negative_monthly_budget_returns_400: POST with monthly_budget_cents=-1
   - test_negative_max_cost_per_request_returns_400: POST with max_cost_per_request_cents=-1

3. Edge cases:
   - test_empty_preferred_providers_list: Verify empty list is handled correctly
   - test_null_optional_fields: Verify None values for optional fields

Target coverage: Lines 196-303 in cognitive_tier_routes.py (create_or_update_preferences function)
  </action>
  <verify>Run pytest backend/tests/api/test_cognitive_tier_routes.py::TestCreateOrUpdatePreferences -v</verify>
  <done>All POST /preferences tests pass (10+ tests), create_or_update_preferences function achieves 80%+ coverage</done>
</task>

<task type="auto">
  <name>Create test class for PUT /preferences/{workspace_id}/budget and DELETE /preferences/{workspace_id}</name>
  <files>backend/tests/api/test_cognitive_tier_routes.py</files>
  <action>
Create TestUpdateBudget and TestDeletePreferences classes:

TestUpdateBudget:
1. Success cases:
   - test_update_monthly_budget_success: Update monthly_budget_cents field only
   - test_update_max_cost_per_request_success: Update max_cost_per_request_cents field only
   - test_update_both_budget_fields_success: Update both budget fields
   - test_create_default_preference_when_none_exists: PUT on non-existent preference creates default

2. Validation error cases:
   - test_negative_monthly_budget_returns_400
   - test_negative_max_cost_per_request_returns_400

TestDeletePreferences:
1. Success cases:
   - test_delete_existing_preference_success: DELETE removes preference, returns success message
   - test_delete_nonexistent_preference_success: DELETE on non-existent preference returns success (idempotent)

2. Response verification:
   - test_delete_response_structure: Verify response has success=True, message, workspace_id fields

Target coverage: Lines 312-384 (update_budget) and Lines 565-594 (delete_preferences)
  </action>
  <verify>Run pytest backend/tests/api/test_cognitive_tier_routes.py::TestUpdateBudget backend/tests/api/test_cognitive_tier_routes.py::TestDeletePreferences -v</verify>
  <done>All PUT /budget and DELETE /preferences tests pass (8+ tests), both functions achieve 80%+ coverage</done>
</task>

<task type="auto">
  <name>Create test class for GET /estimate-cost endpoint</name>
  <files>backend/tests/api/test_cognitive_tier_routes.py</files>
  <action>
Create TestEstimateCost class with tests:

1. Success cases:
   - test_estimate_cost_with_prompt_only: Verify token estimation from prompt (1 token ≈ 4 chars)
   - test_estimate_cost_with_prompt_and_tokens: Verify estimated_tokens parameter overrides prompt estimation
   - test_estimate_cost_with_all_tiers: Verify all 5 cognitive tiers are in estimates
   - test_estimate_cost_default_when_no_params: Verify defaults to 100 tokens when no params provided
   - test_estimate_cost_with_specific_tier_filter: Verify tier parameter filters estimates

2. Response structure verification:
   - test_estimate_cost_response_has_estimates_array: Verify estimates key exists
   - test_estimate_cost_response_has_recommended_tier: Verify recommended_tier key exists
   - test_estimate_cost_includes_models_in_tier: Verify each estimate has models_in_tier list
   - test_estimate_cost_cache_aware_available_field: Verify cache_aware_available boolean

3. Edge cases:
   - test_estimate_cost_empty_prompt: Verify empty prompt is handled
   - test_estimate_cost_very_long_prompt: Verify long prompts are handled

4. Mock the pricing fetcher to return consistent pricing data:
   - Patch get_pricing_fetcher to return mock with get_model_price method
   - Return model pricing with input_cost_per_token and output_cost_per_token

Target coverage: Lines 393-481 in cognitive_tier_routes.py (estimate_cost function)
  </action>
  <verify>Run pytest backend/tests/api/test_cognitive_tier_routes.py::TestEstimateCost -v</verify>
  <done>All GET /estimate-cost tests pass (10+ tests), estimate_cost function achieves 80%+ coverage</done>
</task>

<task type="auto">
  <name>Create test class for GET /compare-tiers endpoint with complete verification</name>
  <files>backend/tests/api/test_cognitive_tier_routes.py</files>
  <action>
Create TestCompareTiers class with tests:

1. Success cases:
   - test_compare_tiers_returns_all_five_tiers: Verify all 5 CognitiveTier values are present
   - test_compare_tiers_response_structure: Verify total_tiers=5, tiers is a list
   - test_compare_tiers_each_tier_has_required_fields: tier, description, quality_range, cost_range_usd, example_models, cache_aware_support

2. Tier-specific verification:
   - test_compare_tiers_micro_tier_data: Verify MICRO tier has quality_range="0-80"
   - test_compare_tiers_standard_tier_data: Verify STANDARD tier has quality_range="80-86"
   - test_compare_tiers_versatile_tier_data: Verify VERSATILE tier has quality_range="86-90"
   - test_compare_tiers_heavy_tier_data: Verify HEAVY tier has quality_range="90-94"
   - test_compare_tiers_complex_tier_data: Verify COMPLEX tier has quality_range="94-100"

3. Data validation:
   - test_compare_tiers_example_models_limit: Verify example_models has max 3 models
   - test_compare_tiers_cost_range_format: Verify cost_range_usd format "$X.XXXXXX - $Y.YYYYYY"
   - test_compare_tiers_description_not_empty: Verify all tier descriptions are non-empty

4. Mock pricing fetcher for consistent cost calculations:
   - Patch get_pricing_fetcher to return mock with get_model_price method
   - Return pricing data with input_cost_per_token and output_cost_per_token

Target coverage: Lines 490-557 in cognitive_tier_routes.py (compare_tiers function)
  </action>
  <verify>Run pytest backend/tests/api/test_cognitive_tier_routes.py::TestCompareTiers -v</verify>
  <done>All GET /compare-tiers tests pass (10+ tests), compare_tiers function achieves 80%+ coverage</done>
</task>

<task type="auto">
  <name>Run complete test suite and verify 75%+ coverage on cognitive_tier_routes.py</name>
  <files>backend/tests/api/test_cognitive_tier_routes.py</files>
  <action>
1. Run all tests in the file to verify no conflicts:
   pytest backend/tests/api/test_cognitive_tier_routes.py -v

2. Run coverage on cognitive_tier_routes.py:
   pytest backend/tests/api/test_cognitive_tier_routes.py --cov=backend/api/cognitive_tier_routes --cov-report=term-missing

3. Verify coverage meets 75%+ target:
   - Current coverage should be >75%
   - If not, identify missing lines and add targeted tests

4. Verify no test dependencies or conflicts:
   - Each test class should be independent
   - Fixtures should provide clean state (transaction rollback)

5. Add any missing edge case tests for uncovered lines:
   - Error paths not yet covered
   - Edge cases in response serialization
   - Database query edge cases

Target: 75%+ line coverage on cognitive_tier_routes.py (601 lines)
  </action>
  <verify>
    pytest backend/tests/api/test_cognitive_tier_routes.py --cov=backend/api/cognitive_tier_routes --cov-report=term-missing --cov-report=json
  </verify>
  <done>All tests pass, cognitive_tier_routes.py achieves 75%+ line coverage (450+ lines covered), 30+ tests total</done>
</task>

</tasks>

<verification>
1. Run full test suite: pytest backend/tests/api/test_cognitive_tier_routes.py -v
2. Verify coverage: pytest backend/tests/api/test_cognitive_tier_routes.py --cov=backend/api/cognitive_tier_routes --cov-report=term-missing
3. Confirm 75%+ coverage achieved
4. Verify all 6 endpoints are covered with success and error paths
</verification>

<success_criteria>
1. api/cognitive_tier_routes.py achieves 75%+ line coverage
2. All 6 endpoints have comprehensive tests (GET/POST/PUT/DELETE preferences, estimate-cost, compare-tiers)
3. Validation errors (400 Bad Request) are tested for invalid inputs
4. Test file follows Phase 172 TestClient patterns
5. 30+ tests covering success paths, error paths, and edge cases
</success_criteria>

<output>
After completion, create `.planning/phases/173-high-impact-zero-coverage-llm/173-01-SUMMARY.md` with:
- Test coverage metrics (line coverage %, lines covered)
- Number of tests created by endpoint
- Any uncovered lines requiring follow-up
</output>
