---
phase: 173-high-impact-zero-coverage-llm
plan: 05
type: execute
wave: 2
depends_on: ["173-01", "173-02"]
files_modified:
  - backend/tests/api/test_atom_agent_endpoints.py
files_created:
  - backend/tests/api/test_atom_agent_endpoints.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Chat endpoint returns assistant responses for valid requests"
    - "Session management creates and updates chat sessions"
    - "Streaming endpoint yields tokens correctly"
    - "Intent classification routes to appropriate handlers"
    - "Error responses return proper HTTP status codes"
    - "Chat history is saved correctly"
  artifacts:
    - path: "backend/tests/api/test_atom_agent_endpoints.py"
      provides: "TestClient-based API tests for atom agent endpoints"
      min_lines: 500
      exports: ["TestChatEndpoint", "TestStreamingEndpoint", "TestSessionManagement", "TestIntentRouting", "TestChatHistory"]
  key_links:
    - from: "backend/tests/api/test_atom_agent_endpoints.py"
      to: "backend/core/atom_agent_endpoints.py"
      via: "from core.atom_agent_endpoints import router"
      pattern: "from fastapi.testclient import TestClient"
---

<objective>
Create comprehensive TestClient-based API tests for atom agent endpoints to achieve 75%+ line coverage on core/atom_agent_endpoints.py (2,043 lines, ~8% coverage).

This file contains the primary chat and streaming endpoints for the Atom AI agent, including:
- POST /chat - Main chat endpoint
- GET /sessions - List chat sessions
- POST /sessions - Create new session
- POST /stream - Streaming chat responses
- Intent routing and workflow triggers

Purpose: Ensure all agent endpoints are thoroughly tested with proper request handling, response serialization, error handling, and chat history persistence.
Output: TestClient-based test file with 5+ test classes covering all major endpoints (40+ tests total)
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/173-high-impact-zero-coverage-llm/173-RESEARCH.md

@backend/core/atom_agent_endpoints.py (chat, sessions, streaming endpoints)
@backend/tests/api/test_agent_governance_routes.py (Phase 172 TestClient pattern reference)
@backend/core/chat_session_manager.py (session management)
@backend/core/chat_context_manager.py (context management)
@backend/core/lancedb_handler.py (chat history)
</context>

<tasks>

<task type="auto">
  <name>Create TestClient fixtures and test class for POST /chat endpoint</name>
  <files>backend/tests/api/test_atom_agent_endpoints.py</files>
  <action>
Create the test file with the following structure:

1. Import required modules:
   - pytest, uuid, unittest.mock (patch, MagicMock, AsyncMock)
   - TestClient from fastapi.testclient
   - router from core.atom_agent_endpoints
   - ChatRequest, ChatMessage from core.atom_agent_endpoints
   - Database models for sessions

2. Create fixtures:
   - client(): TestClient fixture with atom agent router
   - db_session(): Database session with transaction rollback
   - mock_user(): Create test user
   - mock_ai_service(): Mock RealAIWorkflowService for LLM responses
   - mock_session_manager(): Mock chat session manager
   - mock_history_manager(): Mock chat history manager

3. Create TestChatEndpoint class:
   - test_chat_success_returns_response: Valid request returns 200 with assistant message
   - test_chat_with_new_session_creates_session: No session_id → creates new session
   - test_chat_with_existing_session_reuses_session: Existing session_id → reuses
   - test_chat_with_conversation_history_includes_history: Previous messages included
   - test_chat_with_agent_id_parameter: Agent ID passed to governance
   - test_chat_with_workspace_id_parameter: Workspace ID included
   - test_chat_saves_history: Both user and assistant messages saved
   - test_chat_with_current_page_context: Current page included in context
   - test_chat_intent_classification_routes_correctly: Intent detected and routed
   - test_chat_error_returns_500: AI service error → 500 response

4. Mock AI service response:
   ```python
   mock_ai_service.return_value.generate_workflow_from_text.return_value = {
       "response": "Test assistant response",
       "intent": "chat",
       "entities": {}
   }
   ```

Target coverage: Lines 200-400 in atom_agent_endpoints.py (chat endpoint)
  </action>
  <verify>pytest backend/tests/api/test_atom_agent_endpoints.py::TestChatEndpoint -v</verify>
  <done>All chat endpoint tests pass (10+ tests), chat function achieves 75%+ coverage</done>
</task>

<task type="auto">
  <name>Create test class for session management endpoints</name>
  <files>backend/tests/api/test_atom_agent_endpoints.py</files>
  <action>
Create TestSessionManagement class:

1. GET /sessions tests:
   - test_list_sessions_returns_user_sessions: Returns list of sessions for user_id
   - test_list_sessions_with_limit_parameter: Respects limit parameter
   - test_list_sessions_empty_when_no_sessions: Returns empty list for new user
   - test_list_sessions_includes_preview: Each session has preview text
   - test_list_sessions_error_handling: Database error returns 500

2. POST /sessions tests:
   - test_create_session_returns_session_id: Creates new session with unique ID
   - test_create_session_with_title: Custom title saved to metadata
   - test_create_session_with_user_id: User ID associated with session
   - test_create_session_default_title: No title → generates default

3. Session data structure verification:
   - test_session_response_has_required_fields: id, title, date, preview
   - test_session_title_from_metadata: Uses metadata.title if available
   - test_session_preview_from_last_message: Uses last message as preview

4. Mock session manager:
   ```python
   mock_session_manager.create_session.return_value = "test_session_123"
   mock_session_manager.list_user_sessions.return_value = [
       {"session_id": "sess_1", "last_active": "2026-03-12T10:00:00Z", "metadata": {"title": "Test Chat"}}
   ]
   ```

Target coverage: Lines 150-200 in atom_agent_endpoints.py (session endpoints)
  </action>
  <verify>pytest backend/tests/api/test_atom_agent_endpoints.py::TestSessionManagement -v</verify>
  <done>All session management tests pass (9+ tests), session endpoints achieve 75%+ coverage</done>
</task>

<task type="auto">
  <name>Create test class for streaming endpoint with async mocking</name>
  <files>backend/tests/api/test_atom_agent_endpoints.py</files>
  <action>
Create TestStreamingEndpoint class:

1. Note: The streaming endpoint uses Server-Sent Events (SSE)
   - Use TestClient with streaming support
   - Mock async LLM streaming response

2. Test cases:
   - test_stream_returns_sse_format: Response has text/event-stream content type
   - test_stream_yields_tokens: Multiple tokens yielded over time
   - test_stream_with_new_session: Streaming creates session if needed
   - test_stream_includes_final_message: Stream ends with complete response
   - test_stream_error_handling: Error during stream → graceful handling
   - test_stream_with_conversation_history: History included in stream request

3. Mock streaming response pattern:
   ```python
   async def mock_stream():
       yield "data: Hello\n\n"
       yield "data: world\n\n"
       yield "data: [DONE]\n\n"

   with patch('core.atom_agent_endpoints.generate_workflow_stream', side_effect=mock_stream):
       response = client.post("/api/atom-agent/stream", json=request)
   ```

4. Verify SSE format:
   - Content-Type: text/event-stream
   - Each chunk prefixed with "data: "
   - Final chunk is "[DONE]"

Target coverage: Streaming endpoint in atom_agent_endpoints.py (approximately lines 400-600)
  </action>
  <verify>pytest backend/tests/api/test_atom_agent_endpoints.py::TestStreamingEndpoint -v</verify>
  <done>All streaming tests pass (6+ tests), streaming endpoint achieves 75%+ coverage</done>
</task>

<task type="auto">
  <name>Create test class for intent routing and workflow triggers</name>
  <files>backend/tests/api/test_atom_agent_endpoints.py</files>
  <action>
Create TestIntentRouting class:

1. Test intent detection:
   - test_intent_chat_detected: "hello" → chat intent
   - test_intent_task_detected: "create a task" → task creation workflow
   - test_intent_workflow_detected: "run workflow" → workflow execution
   - test_intent_calendar_detected: "schedule meeting" → calendar integration
   - test_intent_email_detected: "send email" → Gmail integration
   - test_intent_finance_detected: "create invoice" → finance workflow
   - test_intent_default_for_unknown: Unknown intent → default chat

2. Test workflow execution:
   - test_workflow_creation_intent_creates_workflow: Intent triggers workflow save
   - test_task_creation_intent_creates_task: Intent triggers task creation
   - test_calendar_intent_creates_event: Intent triggers calendar event creation

3. Mock intent classification:
   ```python
   mock_ai_service.return_value.detect_intent.return_value = {
       "intent": "create_task",
       "confidence": 0.9,
       "entities": {"task_name": "Test task"}
   }
   ```

4. Test response structure:
   - test_response_includes_intent: Response includes detected intent
   - test_response_includes_workflow_id: Workflow creation returns ID
   - test_response_includes_task_id: Task creation returns task ID

Target coverage: Intent routing logic in chat endpoint (lines 250-350)
  </action>
  <verify>pytest backend/tests/api/test_atom_agent_endpoints.py::TestIntentRouting -v</verify>
  <done>All intent routing tests pass (10+ tests), intent detection verified</done>
</task>

<task type="auto">
  <name>Create test class for chat history persistence</name>
  <files>backend/tests/api/test_atom_agent_endpoints.py</files>
  <action>
Create TestChatHistory class:

1. Test history saving:
   - test_user_message_saved_to_history: User message saved with role="user"
   - test_assistant_message_saved_to_history: Assistant response saved with role="assistant"
   - test_history_includes_intent_metadata: Intent stored in message metadata
   - test_history_includes_entities: Extracted entities stored in metadata
   - test_history_includes_workflow_id: Workflow ID saved when created
   - test_history_includes_task_id: Task ID saved when created

2. Test history retrieval:
   - test_conversation_history_loaded_for_session: Previous messages loaded
   - test_conversation_history_order_preserved: Messages in chronological order
   - test_conversation_history_with_session_id: Only messages for session loaded

3. Mock history manager:
   ```python
   mock_history_manager.save_message.return_value = "msg_id_123"
   mock_history_manager.get_messages.return_value = [
       {"role": "user", "content": "Previous message"},
       {"role": "assistant", "content": "Previous response"}
   ]
   ```

4. Test session activity tracking:
   - test_session_activity_updated_on_message: Session last_active timestamp updated
   - test_session_manager_update_called: verify update_session_activity called

Target coverage: save_chat_interaction function and chat history persistence (lines 92-146, 250-400)
  </action>
  <verify>pytest backend/tests/api/test_atom_agent_endpoints.py::TestChatHistory -v</verify>
  <done>All chat history tests pass (8+ tests), history persistence verified</done>
</task>

<task type="auto">
  <name>Run complete test suite and verify 75%+ coverage on atom_agent_endpoints.py</name>
  <files>backend/tests/api/test_atom_agent_endpoints.py</files>
  <action>
1. Run all tests in the file:
   pytest backend/tests/api/test_atom_agent_endpoints.py -v

2. Run coverage on atom_agent_endpoints.py:
   pytest backend/tests/api/test_atom_agent_endpoints.py --cov=backend/core/atom_agent_endpoints --cov-report=term-missing

3. Verify coverage meets 75%+ target:
   - Current coverage should be >75% (from ~8% baseline)
   - If not, identify missing lines and add targeted tests

4. Focus areas for additional coverage:
   - Error paths (database failures, AI service failures)
   - Edge cases (empty messages, very long messages)
   - Additional endpoints if any exist
   - Context passing with current_page parameter

5. Add missing tests for uncovered lines:
   - Test error response structure for various failures
   - Test with None/missing optional parameters
   - Test response serialization edge cases
   - Test integration with episode creation (trigger_episode_creation)

6. Verify no test dependencies:
   - Each test class should be independent
   - Fixtures provide clean state (transaction rollback)
   - No shared state between tests

Target: 75%+ line coverage on atom_agent_endpoints.py (2,043 lines → 1532+ lines covered)
  </action>
  <verify>
    pytest backend/tests/api/test_atom_agent_endpoints.py --cov=backend/core/atom_agent_endpoints --cov-report=term-missing --cov-report=json
  </verify>
  <done>All tests pass, atom_agent_endpoints.py achieves 75%+ line coverage (1532+ lines covered), 40+ tests total</done>
</task>

</tasks>

<verification>
1. Run full test suite: pytest backend/tests/api/test_atom_agent_endpoints.py -v
2. Verify coverage: pytest backend/tests/api/test_atom_agent_endpoints.py --cov=backend/core/atom_agent_endpoints --cov-report=term-missing
3. Confirm 75%+ coverage achieved
4. Verify all major endpoints covered (chat, sessions, streaming, intent routing)
</verification>

<success_criteria>
1. core/atom_agent_endpoints.py achieves 75%+ line coverage
2. Chat endpoint (POST /chat) has comprehensive tests
3. Session management endpoints (GET/POST /sessions) fully tested
4. Streaming endpoint (POST /stream) tested with SSE format verification
5. Intent routing tested for all major intents
6. Chat history persistence verified
7. 40+ tests total covering success and error paths
</success_criteria>

<output>
After completion, create `.planning/phases/173-high-impact-zero-coverage-llm/173-05-SUMMARY.md` with:
- Coverage metrics before and after (8% → 75%+)
- Number of tests by endpoint category
- Uncovered lines requiring follow-up (if any)
- List of mocked services and patterns used
</output>
