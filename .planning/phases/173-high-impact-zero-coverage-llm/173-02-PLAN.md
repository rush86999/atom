---
phase: 173-high-impact-zero-coverage-llm
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/llm/test_byok_handler_coverage.py
  - backend/tests/integration/test_byok_handler_streaming.py
files_created:
  - backend/tests/integration/test_byok_handler_streaming.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "stream_completion() method handles async streaming responses correctly"
    - "generate_with_cognitive_tier() uses tier classification and escalation"
    - "generate_structured_response() returns JSON-structured outputs"
    - "_get_coordinated_vision_description() handles multimodal inputs"
    - "Async LLM client calls are properly mocked using AsyncMock"
    - "Streaming errors (timeouts, rate limits) are handled gracefully"
    - "Vision model selection works for image inputs"
  artifacts:
    - path: "backend/tests/integration/test_byok_handler_streaming.py"
      provides: "Async streaming tests for BYOK handler"
      min_lines: 300
      exports: ["TestStreamCompletion", "TestVisionHandling", "TestStructuredResponse"]
    - path: "backend/tests/unit/llm/test_byok_handler_coverage.py"
      provides: "Expanded BYOK handler coverage tests"
      min_lines: 500
      exports: ["TestGenerateWithCognitiveTier", "TestStructuredResponseGeneration", "TestVisionDescription"]
  key_links:
    - from: "backend/tests/integration/test_byok_handler_streaming.py"
      to: "backend/core/llm/byok_handler.py"
      via: "from core.llm.byok_handler import BYOKHandler"
      pattern: "async def stream_completion"
    - from: "backend/tests/unit/llm/test_byok_handler_coverage.py"
      to: "backend/core/llm/byok_handler.py"
      via: "from core.llm.byok_handler import BYOKHandler"
      pattern: "async def generate_with_cognitive_tier|async def generate_structured_response"
---

<objective>
Expand BYOK handler coverage to 75%+ by adding tests for missing methods: stream_completion, generate_with_cognitive_tier, generate_structured_response, and _get_coordinated_vision_description.

The byok_handler.py file (1,556 lines) currently has ~15% coverage from Phase 165. Key methods lacking coverage include async streaming, cognitive tier orchestration, structured outputs, and vision model handling.

Purpose: Ensure critical BYOK handler methods for streaming LLM responses, tier-based routing, and multimodal inputs are thoroughly tested.
Output: Expanded test coverage with 20+ new tests across existing and new test files
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/173-high-impact-zero-coverage-llm/173-RESEARCH.md

@backend/core/llm/byok_handler.py (methods: stream_completion, generate_with_cognitive_tier, generate_structured_response, _get_coordinated_vision_description)
@backend/tests/unit/llm/test_byok_handler_coverage.py (existing 23K lines)
@backend/tests/integration/services/test_llm_http_integration_coverage.py (Phase 170 pattern for HTTP mocking)
@backend/core/llm/cognitive_tier_system.py (CognitiveTier enum and CognitiveClassifier)
</context>

<tasks>

<task type="auto">
  <name>Create streaming tests with httpx.AsyncMock for stream_completion method</name>
  <files>backend/tests/integration/test_byok_handler_streaming.py</files>
  <action>
Create a new test file for BYOK handler streaming functionality:

1. File structure:
   - Import pytest, pytest-asyncio, unittest.mock (AsyncMock, patch, MagicMock)
   - Import BYOKHandler, QueryComplexity from core.llm.byok_handler
   - Import CognitiveTier from core.llm.cognitive_tier_system

2. Create fixtures:
   - mock_async_openai_client: AsyncMock for AsyncOpenAI client
   - mock_stream_response: Mock async generator for streaming chunks
   - handler_with_mocked_client: BYOKHandler fixture with mocked async clients

3. Create TestStreamCompletion class with tests:
   - test_stream_completion_yields_tokens: Verify async generator yields string tokens
   - test_stream_completion_with_openai_provider: Mock OpenAI streaming response
   - test_stream_completion_with_deepseek_provider: Mock DeepSeek streaming response
   - test_stream_completion_empty_stream: Handle empty stream gracefully
   - test_stream_completion_with_system_instruction: Include system prompt in messages
   - test_stream_completion_with_temperature: Pass temperature parameter

4. Mock streaming pattern (Phase 170):
   ```python
   async def mock_stream():
       yield {"choices": [{"delta": {"content": "Hello"}}]}
       yield {"choices": [{"delta": {"content": " world"}}]}]
       yield {"choices": [{"finish_reason": "stop"}]}
   ```

Target coverage: stream_completion method (approximately lines 700-800 in byok_handler.py)
  </action>
  <verify>pytest backend/tests/integration/test_byok_handler_streaming.py::TestStreamCompletion -v</verify>
  <done>All streaming tests pass (6+ tests), stream_completion achieves 75%+ coverage</done>
</task>

<task type="auto">
  <name>Create tests for generate_with_cognitive_tier orchestration</name>
  <files>backend/tests/unit/llm/test_byok_handler_coverage.py</files>
  <action>
Expand the existing test_byok_handler_coverage.py file with TestGenerateWithCognitiveTier class:

1. Test class structure:
   - Mock tier_service (CognitiveTierService) for tier selection
   - Mock escalation logic for quality-based escalation
   - Mock generate_response for actual LLM calls

2. Test cases:
   - test_generate_with_cognitive_tier_classifies_prompt: Verify CognitiveClassifier.classify is called
   - test_generate_with_cognitive_tier_selects_model: Verify tier service selects optimal model
   - test_generate_with_cognitive_tier_checks_budget: Verify budget constraint check before generation
   - test_generate_with_cognitive_tier_escalates_on_low_quality: Verify escalation on quality threshold breach
   - test_generate_with_cognitive_tier_respects_max_escalation_limit: Verify max 2 escalations enforced
   - test_generate_with_cognitive_tier_returns_response_with_metadata: Verify response includes tier, provider, model fields
   - test_generate_with_cognitive_tier_with_user_override: Verify user_tier_override bypasses classification

3. Mock pattern:
   ```python
   with patch('core.llm.byok_handler.CognitiveTierService') as mock_service:
       mock_service.return_value.select_tier.return_value = CognitiveTier.STANDARD
       mock_service.return_value.check_budget_constraint.return_value = True
       mock_service.return_value.handle_escalation.return_value = (False, None, None)
   ```

Target coverage: generate_with_cognitive_tier method (approximately lines 900-1100)
  </action>
  <verify>pytest backend/tests/unit/llm/test_byok_handler_coverage.py::TestGenerateWithCognitiveTier -v</verify>
  <done>All cognitive tier tests pass (7+ tests), generate_with_cognitive_tier achieves 75%+ coverage</done>
</task>

<task type="auto">
  <name>Create tests for generate_structured_response with JSON schema validation</name>
  <files>backend/tests/unit/llm/test_byok_handler_coverage.py</files>
  <action>
Add TestStructuredResponseGeneration class to test_byok_handler_coverage.py:

1. Test cases:
   - test_generate_structured_response_with_json_schema: Verify response follows JSON schema
   - test_generate_structured_response_with_response_format: Verify response_format="json_mode" parameter
   - test_generate_structured_response_parse_error_handling: Handle invalid JSON from LLM gracefully
   - test_generate_structured_response_with_complex_schema: Test nested object schema validation
   - test_generate_structured_response_retry_on_parse_failure: Verify retry logic for JSON parsing

2. Mock LLM response with structured output:
   ```python
   mock_response = {
       "choices": [{
           "message": {
               "content": '{"name": "Test", "value": 42}'
           }
       }]
   }
   ```

3. Schema validation:
   - Use pydantic BaseModel for schema definition
   - Verify returned dict matches schema structure
   - Test required fields, optional fields, nested fields

Target coverage: generate_structured_response method (approximately lines 1100-1250)
  </action>
  <verify>pytest backend/tests/unit/llm/test_byok_handler_coverage.py::TestStructuredResponseGeneration -v</verify>
  <done>All structured response tests pass (5+ tests), generate_structured_response achieves 75%+ coverage</done>
</task>

<task type="auto">
  <name>Create tests for _get_coordinated_vision_description multimodal handling</name>
  <files>backend/tests/integration/test_byok_handler_streaming.py</files>
  <action>
Add TestVisionHandling class to test_byok_handler_streaming.py:

1. Test cases:
   - test_coordinated_vision_with_base64_image: Verify base64 image payload is processed
   - test_coordinated_vision_with_image_url: Verify URL image payload is processed
   - test_coordinated_vision_selects_vision_capable_model: Verify model selection excludes REASONING_MODELS_WITHOUT_VISION
   - test_coordinated_vision_fallback_to_separate_models: Verify fallback when primary model lacks vision
   - test_coordinated_vision_mixed_text_image: Verify both text and image are included in request
   - test_coordinated_vision_with_vision_only_model: Verify VISION_ONLY_MODELS are used appropriately

2. Mock patterns for vision models:
   - Mock vision model: "gpt-4o" or "claude-3-5-sonnet" (has vision)
   - Mock reasoning-only model: "deepseek-v3.2" (no vision)
   - Mock vision-only model: "janus-pro-7b"

3. Test payload formats:
   - Base64: "data:image/png;base64,iVBORw0KG..."
   - URL: "https://example.com/image.png"

4. Verify message structure:
   - Content should be list with text and image_url objects
   - Verify proper OpenAI vision API format

Target coverage: _get_coordinated_vision_description method and related vision handling
  </action>
  <verify>pytest backend/tests/integration/test_byok_handler_streaming.py::TestVisionHandling -v</verify>
  <done>All vision handling tests pass (6+ tests), _get_coordinated_vision_description achieves 75%+ coverage</done>
</task>

<task type="auto">
  <name>Run coverage verification and add edge case tests for uncovered lines</name>
  <files>backend/tests/unit/llm/test_byok_handler_coverage.py</files>
  <action>
1. Run combined coverage on byok_handler.py:
   pytest backend/tests/unit/llm/test_byok_handler_coverage.py backend/tests/integration/test_byok_handler_streaming.py --cov=backend/core/llm/byok_handler --cov-report=term-missing

2. Identify uncovered lines and add targeted tests:
   - Error paths in streaming (connection errors, incomplete streams)
   - Escalation logic edge cases (already at max tier, cooldown active)
   - Vision model selection edge cases (no vision models available)
   - Structured response edge cases (empty schema, malformed JSON)

3. Add any missing test classes:
   - TestProviderFallbackOrder: Verify _get_provider_fallback_order returns correct priority
   - TestContextWindowMethods: Verify get_context_window and truncate_to_context utilities
   - Trial restriction tests: Verify _is_trial_restricted logic

4. Verify coverage target:
   - Target: 75%+ line coverage (current ~15%, need +60%)
   - Focus: Critical paths for streaming, tier orchestration, vision, structured outputs

5. Run final coverage check and document results
  </action>
  <verify>
    pytest backend/tests/unit/llm/test_byok_handler_coverage.py backend/tests/integration/test_byok_handler_streaming.py --cov=backend/core/llm/byok_handler --cov-report=term-missing --cov-report=json
  </verify>
  <done>byok_handler.py achieves 75%+ line coverage (1167+ lines covered), 25+ new tests created</done>
</task>

</tasks>

<verification>
1. Run all BYOK handler tests: pytest backend/tests/unit/llm/test_byok_handler_coverage.py backend/tests/integration/test_byok_handler_streaming.py -v
2. Verify coverage target: pytest backend/tests/unit/llm/test_byok_handler_coverage.py backend/tests/integration/test_byok_handler_streaming.py --cov=backend/core/llm/byok_handler --cov-report=term-missing
3. Confirm 75%+ coverage achieved
4. Verify all critical methods (stream_completion, generate_with_cognitive_tier, generate_structured_response, _get_coordinated_vision_description) have 75%+ coverage
</verification>

<success_criteria>
1. core/llm/byok_handler.py achieves 75%+ line coverage
2. stream_completion method has comprehensive async streaming tests
3. generate_with_cognitive_tier method tests tier orchestration and escalation
4. generate_structured_response method tests JSON schema validation
5. _get_coordinated_vision_description method tests multimodal handling
6. 25+ new tests covering previously uncovered methods
</success_criteria>

<output>
After completion, create `.planning/phases/173-high-impact-zero-coverage-llm/173-02-SUMMARY.md` with:
- Coverage metrics before and after (15% → 75%+)
- Number of tests added by method
- Uncovered lines requiring follow-up (if any)
- List of mocked providers and patterns used
</output>
