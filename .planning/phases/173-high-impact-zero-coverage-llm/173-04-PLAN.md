---
phase: 173-high-impact-zero-coverage-llm
plan: 04
type: execute
wave: 2
depends_on: ["173-01", "173-02", "173-03"]
files_modified:
  - backend/tests/integration/test_llm_integration.py
  - backend/tests/integration/test_cognitive_tier_service_integration.py
files_created:
  - backend/tests/integration/test_llm_integration.py
  - backend/tests/integration/test_cognitive_tier_service_integration.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "End-to-end LLM workflows complete successfully with mocked providers"
    - "Provider fallback mechanism works on failure"
    - "Budget enforcement prevents overspending"
    - "Cache-aware routing reduces effective cost"
    - "Escalation triggers on quality threshold breach"
    - "Cognitive tier service orchestration works end-to-end"
  artifacts:
    - path: "backend/tests/integration/test_llm_integration.py"
      provides: "End-to-end LLM workflow integration tests"
      min_lines: 400
      exports: ["TestProviderFallback", "TestBudgetEnforcement", "TestCacheAwareRouting", "TestEscalationWorkflow"]
    - path: "backend/tests/integration/test_cognitive_tier_service_integration.py"
      provides: "Cognitive tier service orchestration tests"
      min_lines: 350
      exports: ["TestTierSelection", "TestModelSelection", "TestCostCalculation", "TestWorkspacePreferences"]
  key_links:
    - from: "backend/tests/integration/test_llm_integration.py"
      to: "backend/core/llm/byok_handler.py"
      via: "from core.llm.byok_handler import BYOKHandler"
      pattern: "async def generate_response"
    - from: "backend/tests/integration/test_cognitive_tier_service_integration.py"
      to: "backend/core/llm/cognitive_tier_service.py"
      via: "from core.llm.cognitive_tier_service import CognitiveTierService"
      pattern: "def select_tier|def get_optimal_model"
---

<objective>
Create comprehensive end-to-end integration tests for LLM workflows including provider fallback, budget enforcement, cache-aware routing, escalation, and cognitive tier service orchestration.

Integration tests verify that components work together correctly by testing complete workflows from request to response with mocked external LLM providers.

Purpose: Validate that LLM service orchestration, tier selection, budget checking, provider fallback, and escalation work correctly in integrated scenarios.
Output: Two integration test files with 20+ tests covering critical end-to-end workflows
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/173-high-impact-zero-coverage-llm/173-RESEARCH.md

@backend/core/llm/byok_handler.py (BYOKHandler with provider routing)
@backend/core/llm/cognitive_tier_service.py (CognitiveTierService orchestration)
@backend/core/llm/escalation_manager.py (EscalationManager)
@backend/core/llm/cache_aware_router.py (CacheAwareRouter)
@backend/tests/integration/services/test_llm_http_integration_coverage.py (Phase 170 HTTP mocking patterns)
</context>

<tasks>

<task type="auto">
  <name>Create LLM integration test file with provider fallback tests</name>
  <files>backend/tests/integration/test_llm_integration.py</files>
  <action>
Create a new integration test file for end-to-end LLM workflows:

1. File setup:
   - Import pytest, pytest-asyncio, unittest.mock (AsyncMock, patch, MagicMock)
   - Import BYOKHandler, QueryComplexity from core.llm.byok_handler
   - Import CognitiveTier from core.llm.cognitive_tier_system
   - Import httpx.MockTransport for HTTP mocking (Phase 170 pattern)

2. Create TestProviderFallback class:
   - test_provider_fallback_on_rate_limit: Primary provider rate limits → fallback to secondary
   - test_provider_fallback_on_connection_error: Primary provider fails → fallback works
   - test_provider_fallback_exhaustion_returns_error: All providers fail → appropriate error
   - test_fallback_preserves_context: Context maintained across provider switches
   - test_fallback_priority_order: Verify fallback order (deepseek → openai → moonshot → minimax)

3. Mock provider responses:
   ```python
   async def mock_rate_limit_error():
       raise RateLimitError("Rate limit exceeded")

   async def mock_fallback_provider():
       return {"choices": [{"message": {"content": "Fallback response"}}]}
   ```

4. Test with different provider combinations:
   - OpenAI → Anthropic fallback
   - DeepSeek → OpenAI fallback
   - Multi-hop fallback (provider1 → provider2 → provider3)

Target coverage: BYOKHandler provider fallback logic (_get_provider_fallback_order, generate_response error handling)
  </action>
  <verify>pytest backend/tests/integration/test_llm_integration.py::TestProviderFallback -v</verify>
  <done>All provider fallback tests pass (5+ tests), fallback logic verified</done>
</task>

<task type="auto">
  <name>Create budget enforcement and cache-aware routing tests</name>
  <files>backend/tests/integration/test_llm_integration.py</files>
  <action>
Add TestBudgetEnforcement and TestCacheAwareRouting classes:

TestBudgetEnforcement:
1. Test cases:
   - test_request_within_budget_succeeds: Cost below monthly budget → proceed
   - test_request_exceeds_monthly_budget_blocked: Cost exceeds budget → blocked
   - test_request_exceeds_per_request_limit_blocked: Per-request limit enforced
   - test_budget_check_before_llm_call: Budget checked before provider call
   - test_budget_warning_logged: Warning logged when approaching budget

2. Mock budget preference:
   ```python
   preference = CognitiveTierPreference(
       monthly_budget_cents=1000,  # $10.00
       max_cost_per_request_cents=10  # $0.10
   )
   ```

TestCacheAwareRouting:
1. Test cases:
   - test_cache_hit_reduces_effective_cost: Cached prompt has lower effective cost
   - test_cache_miss_uses_full_cost: Uncached prompt uses full cost
   - test_cache_hit_probability_prediction: Router predicts cache probability
   - test_cache_aware_model_selection: Models with caching preferred
   - test_cache_outcome_recording: Cache outcomes recorded for future predictions

2. Mock cache router:
   ```python
   with patch('core.llm.byok_handler.CacheAwareRouter') as mock_router:
       mock_router.return_value.predict_cache_hit_probability.return_value = 0.8
       mock_router.return_value.calculate_effective_cost.return_value = 0.0001
   ```

Target coverage: Budget checking logic in cognitive_tier_service.py, cache-aware routing in cache_aware_router.py
  </action>
  <verify>pytest backend/tests/integration/test_llm_integration.py::TestBudgetEnforcement backend/tests/integration/test_llm_integration.py::TestCacheAwareRouting -v</verify>
  <done>All budget and cache tests pass (10+ tests), enforcement verified</done>
</task>

<task type="auto">
  <name>Create escalation workflow integration tests</name>
  <files>backend/tests/integration/test_llm_integration.py</files>
  <action>
Add TestEscalationWorkflow class:

1. Test cases:
   - test_escalation_on_low_quality_response: Quality < 80 → escalate to next tier
   - test_escalation_on_rate_limit: Rate limited → immediate escalation
   - test_escalation_stops_at_max_tier: COMPLEX tier → no further escalation
   - test_escalation_respects_cooldown: Recent escalation → blocked
   - test_escalation_limit_enforced: Max 2 escalations per request
   - test_escalation_with_requery_uses_new_tier: Escalated request uses higher tier

2. Mock escalation scenario:
   ```python
   # First call with low quality
   with patch('core.llm.cognitive_tier_service.get_quality_score', return_value=70):
       result1 = await handler.generate_with_cognitive_tier(prompt)
       # Should trigger escalation

   # Second call should use higher tier
   result2 = await handler.generate_with_cognitive_tier(prompt, escalated=True)
   ```

3. Test escalation manager integration:
   - Verify EscalationManager.should_escalate called correctly
   - Verify target tier calculation
   - Verify cooldown timestamp set
   - Verify escalation logged to database

Target coverage: Escalation workflow integration (byok_handler.py + cognitive_tier_service.py + escalation_manager.py)
  </action>
  <verify>pytest backend/tests/integration/test_llm_integration.py::TestEscalationWorkflow -v</verify>
  <done>All escalation workflow tests pass (6+ tests), escalation integration verified</done>
</task>

<task type="auto">
  <name>Create cognitive tier service orchestration integration tests</name>
  <files>backend/tests/integration/test_cognitive_tier_service_integration.py</files>
  <action>
Create a new integration test file for cognitive tier service:

1. File setup:
   - Import pytest, unittest.mock
   - Import CognitiveTierService, CognitiveTier from core.llm.cognitive_tier_service
   - Import CognitiveClassifier, EscalationManager
   - Mock database session for preferences

2. Create TestTierSelection class:
   - test_select_tier_with_user_override: user_tier_override bypasses classification
   - test_select_tier_with_workspace_preference: Default tier from preference used
   - test_select_tier_applies_min_constraint: Classified tier below min → raised to min
   - test_select_tier_applies_max_constraint: Classified tier above max → lowered to max
   - test_select_tier_auto_classification_fallback: No preference → auto classification

3. Create TestModelSelection class:
   - test_get_optimal_model_returns_provider_and_model: Returns tuple (provider, model)
   - test_get_optimal_model_filters_by_preferred_providers: Preferred providers respected
   - test_get_optimal_model_requires_tools_filtering: Models without tools filtered
   - test_get_optimal_model_uses_cache_aware_cost: Lowest effective cost selected
   - test_get_optimal_model_no_models_for_tier: Returns (None, None) when no models match

4. Create TestCostCalculation class:
   - test_calculate_request_cost_estimates_tokens: Token count from prompt length
   - test_calculate_request_cost_includes_cache_discount: Cache discount applied
   - test_calculate_request_cost_returns_cents: Cost returned in cents
   - test_calculate_request_cost_with_model_parameter: Specific model pricing used

Target coverage: CognitiveTierService orchestration methods (select_tier, get_optimal_model, calculate_request_cost)
  </action>
  <verify>pytest backend/tests/integration/test_cognitive_tier_service_integration.py -v</verify>
  <done>All cognitive tier service tests pass (12+ tests), orchestration verified</done>
</task>

<task type="auto">
  <name>Create workspace preferences and escalation integration tests</name>
  <files>backend/tests/integration/test_cognitive_tier_service_integration.py</files>
  <action>
Add TestWorkspacePreferences and TestEscalationIntegration classes:

TestWorkspacePreferences:
1. Test cases:
   - test_get_workspace_preference_returns_none_when_not_set: No DB preference → None
   - test_get_workspace_preference_returns_saved_preference: Returns saved preference
   - test_workspace_preference_isolation: Different workspaces have independent preferences
   - test_invalid_preference_values_handled_gracefully: Invalid tier → fallback to default

2. Test with database mock:
   ```python
   mock_pref = CognitiveTierPreference(
       workspace_id="test_workspace",
       default_tier="versatile",
       min_tier="standard",
       max_tier="complex",
       monthly_budget_cents=5000
   )
   ```

TestEscalationIntegration:
1. Test cases:
   - test_handle_escalation_with_auto_escalation_enabled: Escalation proceeds when enabled
   - test_handle_escalation_disabled_by_preference: enable_auto_escalation=False → no escalation
   - test_escalation_delegates_to_escalation_manager: EscalationManager methods called
   - test_escalation_returns_reason_and_target_tier: Response includes escalation details

2. Test full escalation workflow:
   ```python
   # First attempt with low quality
   tier = service.select_tier("complex query")
   provider, model = service.get_optimal_model(tier, 1000)
   # Simulate low quality response
   should_escalate, reason, target = service.handle_escalation(tier, response_quality=70)
   assert should_escalate is True
   assert target == CognitiveTier.HEAVY
   ```

Target coverage: Remaining cognitive_tier_service.py methods (get_workspace_preference, handle_escalation, _model_to_provider)
  </action>
  <verify>pytest backend/tests/integration/test_cognitive_tier_service_integration.py::TestWorkspacePreferences backend/tests/integration/test_cognitive_tier_service_integration.py::TestEscalationIntegration -v</verify>
  <done>All preference and escalation tests pass (8+ tests), integration verified</done>
</task>

<task type="auto">
  <name>Run full integration test suite and verify workflow coverage</name>
  <files>backend/tests/integration/test_llm_integration.py</files>
  <action>
1. Run all integration tests:
   pytest backend/tests/integration/test_llm_integration.py backend/tests/integration/test_cognitive_tier_service_integration.py -v

2. Run combined coverage on target files:
   pytest backend/tests/integration/test_llm_integration.py backend/tests/integration/test_cognitive_tier_service_integration.py --cov=backend/core/llm/byok_handler --cov=backend/core/llm/cognitive_tier_service --cov=backend/core/llm/escalation_manager --cov=backend/core/llm/cache_aware_router --cov-report=term-missing

3. Verify integration scenarios:
   - Provider fallback works across multiple providers
   - Budget enforcement blocks overspending
   - Cache-aware routing reduces effective cost
   - Escalation triggers and stops appropriately
   - Workspace preferences are isolated
   - Tier selection considers all constraints

4. Add any missing integration tests:
   - Test workspace preference isolation across concurrent requests
   - Test cache outcome recording for future predictions
   - Test escalation with database persistence
   - Test model selection with all tier constraints

5. Document integration test results:
   - Number of workflows tested
   - Coverage contributions to each file
   - Any gaps identified

Target: Integration tests provide additional coverage for orchestration paths not covered by unit tests
  </action>
  <verify>
    pytest backend/tests/integration/test_llm_integration.py backend/tests/integration/test_cognitive_tier_service_integration.py --cov=backend/core/llm/byok_handler --cov=backend/core/llm/cognitive_tier_service --cov=backend/core/llm/escalation_manager --cov=backend/core/llm/cache_aware_router --cov-report=term-missing --cov-report=json
  </verify>
  <done>All integration tests pass (20+ tests), orchestration workflows verified, additional coverage achieved</done>
</task>

</tasks>

<verification>
1. Run all integration tests: pytest backend/tests/integration/test_llm_integration.py backend/tests/integration/test_cognitive_tier_service_integration.py -v
2. Verify coverage contribution: Run with --cov flag to see additional coverage from integration tests
3. Confirm all critical workflows are tested
4. Verify no test isolation issues (clean database state between tests)
</verification>

<success_criteria>
1. Provider fallback mechanism tested with multiple providers
2. Budget enforcement verified with monthly and per-request limits
3. Cache-aware routing cost reduction verified
4. Escalation workflow tested with quality thresholds and limits
5. Cognitive tier service orchestration tested end-to-end
6. Workspace preferences tested with isolation
7. 20+ integration tests total
</success_criteria>

<output>
After completion, create `.planning/phases/173-high-impact-zero-coverage-llm/173-04-SUMMARY.md` with:
- Integration test coverage metrics
- Number of workflows tested
- Coverage contribution to each file
- List of verified integration scenarios
</output>
