---
phase: 167-api-routes-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/contract/conftest.py
  - backend/tests/contract/test_openapi_validation.py
  - backend/tests/contract/test_agent_api_contract.py
  - backend/tests/contract/test_canvas_api_contract.py
  - backend/tests/contract/test_browser_api_contract.py
  - backend/requirements-testing.txt
  - backend/tests/contract/CONTRACT_TEST_RESULTS.md
autonomous: true

must_haves:
  truths:
    - "API contracts validated using Schemathesis for OpenAPI spec compliance"
    - "Contract tests verify request/response schemas match OpenAPI documentation"
    - "Schemathesis tests run via pytest with Hypothesis property-based testing"
    - "Contract test failures generate actionable reports with schema violations"
  artifacts:
    - path: "backend/tests/contract/conftest.py"
      provides: "Schemathesis fixtures and schema loading"
      min_lines: 50
    - path: "backend/tests/contract/test_openapi_validation.py"
      provides: "OpenAPI schema validation tests"
      min_lines: 80
    - path: "backend/tests/contract/test_agent_api_contract.py"
      provides: "Agent endpoint contract tests"
      min_lines: 80
    - path: "backend/tests/contract/test_canvas_api_contract.py"
      provides: "Canvas endpoint contract tests"
      min_lines: 80
    - path: "backend/tests/contract/test_browser_api_contract.py"
      provides: "Browser endpoint contract tests"
      min_lines: 60
    - path: "backend/tests/contract/CONTRACT_TEST_RESULTS.md"
      provides: "Contract test execution report"
      min_lines: 40
  key_links:
    - from: "backend/tests/contract/conftest.py"
      to: "backend/main_api_app.py"
      via: "FastAPI app.openapi() schema extraction"
      pattern: "schemathesis\\.openapi\\.(from_dict|from_wsgi)"
    - from: "backend/tests/contract/test_openapi_validation.py"
      to: "backend/main_api_app.py"
      via: "OpenAPI schema validation"
      pattern: "app\\.openapi\\(\\)|schema\\.validate"
    - from: "backend/requirements-testing.txt"
      to: "backend/tests/contract/conftest.py"
      via: "schemathesis dependency"
      pattern: "schemathesis"
---

<objective>
Schemathesis-based contract testing for OpenAPI spec validation across agent, canvas, and browser endpoints.

Purpose: Ensure API contracts match OpenAPI specification using property-based testing with Schemathesis.
Output: Contract test suite validating request/response schemas for all major API endpoints.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/tests/contract/conftest.py
@backend/tests/contract/test_core_api.py
@backend/main_api_app.py
@backend/requirements-testing.txt
</context>

<tasks>

<task type="auto">
  <name>Update Schemathesis conftest and verify dependencies</name>
  <files>backend/tests/contract/conftest.py</files>
  <action>
Update backend/tests/contract/conftest.py for comprehensive contract testing:

1. Import and verify Schemathesis dependencies:
   ```python
   import pytest
   import schemathesis
   from hypothesis import settings, HealthCheck
   from fastapi.testclient import TestClient
   from main_api_app import app
   ```

2. Create comprehensive schema fixture:
   - Load OpenAPI schema from app.openapi()
   - Handle schema validation errors gracefully
   - Support both test and production schemas

3. Create authenticated_client_for_contract fixture:
   - TestClient with pre-configured auth headers
   - Support multiple user roles for permission testing

4. Configure Hypothesis settings for property-based testing:
   ```python
   hypothesis_settings = settings(
       max_examples=50,
       deadline=1000,
       derandomize=True,
       suppress_health_check=list(HealthCheck)
   )
   ```

5. Add custom Schemathesis hooks for:
   - Auth header injection for protected endpoints
   - Database state reset between test cases
   - Response validation overrides for non-standard responses

6. Create endpoint_filter fixture to exclude problematic endpoints:
   - Exclude endpoints requiring external services (e.g., actual LLM calls)
   - Exclude websockets (Schemathesis doesn't handle WS)
   - Document why each endpoint is excluded

File size target: 50+ lines with comprehensive fixtures.
  </action>
  <verify>
Run: python -m pytest backend/tests/contract/conftest.py --collect-only
Check: No import errors for schemathesis, fixtures available
  </verify>
  <done>
Schemathesis conftest provides schema loading, auth, and Hypothesis configuration for contract tests.
  </done>
</task>

<task type="auto">
  <name>Add Schemathesis to testing requirements</name>
  <files>backend/requirements-testing.txt</files>
  <action>
Update backend/requirements-testing.txt to add Schemathesis:

1. Add Schemathesis dependency:
   ```
   # API Contract Testing
   schemathesis>=3.30.0,<4.0.0  # OpenAPI contract testing with Hypothesis
   ```

2. Ensure related dependencies are present:
   - hypothesis>=6.92.0 (already in requirements.txt, verify version)
   - httpx>=0.24.0 (for async transport if needed)

3. Add comment about Schemathesis purpose and usage

Do NOT remove any existing dependencies. Only add Schemathesis if not already present.
  </action>
  <verify>
Run: grep -i schemathesis backend/requirements-testing.txt
Check: schemathesis>=3.30.0,<4.0.0 line exists
  </verify>
  <done>
Schemathesis added to testing requirements for contract testing.
  </done>
</task>

<task type="auto">
  <name>Create OpenAPI schema validation tests</name>
  <files>backend/tests/contract/test_openapi_validation.py</files>
  <action>
Create backend/tests/contract/test_openapi_validation.py for schema validation:

1. Test class TestOpenAPISchemaStructure:
   - test_openapi_schema_version: Schema has openapi: 3.x.x
   - test_schema_has_info_section: Contains title, version, description
   - test_schema_has_paths_section: All routes documented
   - test_schema_has_components_section: Models defined

2. Test class TestEndpointDocumentation:
   - test_all_endpoints_have_tags: Each path has at least one tag
   - test_all_endpoints_have_summary: Operations have descriptions
   - test_responses_documented: All operations have response schemas
   - test_request_bodies_documented: POST/PUT have request schemas

3. Test class TestSchemaConsistency:
   - test_response_schemas_valid: All response refs resolve
   - test_request_schemas_valid: All request refs resolve
   - test_schema_reuse_consistent: Same components used consistently
   - test_security_schemes_defined: Auth methods documented

4. Test class TestSchemaCoverage:
   - test_documented_routes_match_actual: No undocumented routes
   - test_actual_routes_match_documented: No documented-but-missing routes
   - test_deprecated_routes_marked: Deprecated status in schema

5. Use pytest and direct schema inspection:
   ```python
   def test_openapi_schema_version(schema):
       assert schema.openapi.startswith("3.")
   ```

File size target: 80+ lines with at least 10 test methods.
  </action>
  <verify>
Run: python -m pytest backend/tests/contract/test_openapi_validation.py -v
Check: All schema validation tests pass
  </verify>
  <done>
OpenAPI validation tests verify schema structure, documentation completeness, and consistency.
  </done>
</task>

<task type="auto">
  <name>Create agent API contract tests</name>
  <files>backend/tests/contract/test_agent_api_contract.py</files>
  <action>
Create backend/tests/contract/test_agent_api_contract.py for agent endpoints:

1. Test class TestAgentListContract:
   - test_get_agents_contracts: GET /api/agents/ validates response
   - test_get_agents_pagination: Pagination parameters work
   - test_get_agents_filtering: Filter parameters validated

2. Test class TestAgentDetailContract:
   - test_get_agent_by_id: GET /api/agents/{id} schema valid
   - test_get_agent_not_found: 404 response schema valid
   - test_get_agent_invalid_id: 422 for malformed ID

3. Test class TestAgentSpawnContract:
   - test_spawn_agent_contracts: POST /api/agents/spawn validates
   - test_spawn_request_schema: Request body schema enforced
   - test_spawn_success_response: 201 response schema valid
   - test_spawn_validation_errors: 422 response schema valid

4. Test class TestAgentExecuteContract:
   - test_execute_agent_contracts: POST /api/agents/execute validates
   - test_execute_request_schema: Complex request with config
   - test_execute_streaming_response: Stream response handled

5. Use Schemathesis schema.and_() for combining validations:
   ```python
   @schema.parametrize("/api/agents/", method="GET")
   def test_get_agents_contract(case):
       response = case.call()
       case.validate_response(response)
   ```

6. Add auth header injection for protected endpoints

File size target: 80+ lines with at least 8 contract test methods.
  </action>
  <verify>
Run: python -m pytest backend/tests/contract/test_agent_api_contract.py -v
Check: All agent contract tests pass or failures are documented
  </verify>
  <done>
Agent API contract tests validate request/response schemas for all agent endpoints.
  </done>
</task>

<task type="auto">
  <name>Create canvas API contract tests</name>
  <files>backend/tests/contract/test_canvas_api_contract.py</files>
  <action>
Create backend/tests/contract/test_canvas_api_contract.py for canvas endpoints:

1. Test class TestCanvasSubmissionContract:
   - test_submit_canvas_contracts: POST /api/canvas/submit validates
   - test_submit_request_schema: Form submission schema enforced
   - test_submit_success_response: 200 response includes audit_id
   - test_submit_validation_errors: 400/422 schemas valid

2. Test class TestCanvasQueryContract:
   - test_get_canvas_contracts: GET /api/canvas/{id} validates
   - test_list_canvases_contracts: GET /api/canvas/ validates
   - test_canvas_not_found: 404 response schema valid

3. Test class TestCanvasTypeContracts:
   - test_chart_canvas_schema: Chart canvas response schema
   - test_form_canvas_schema: Form canvas with fields schema
   - test_markdown_canvas_schema: Markdown content schema
   - test_sheet_canvas_schema: Spreadsheet data schema

4. Test class TestCanvasWebSocketContract:
   - Note: Skip websocket endpoints (Schemathesis limitation)
   - Document WS endpoints in test docstring for manual testing

5. Use schema.parametrize() for each endpoint pattern:
   ```python
   @schema.parametrize()
   def test_canvas_endpoints(case):
       response = case.call_asgi()
       case.validate_response(response)
   ```

6. Add custom checks for canvas-specific validations:
   - canvas_id format validation
   - form_data structure validation

File size target: 80+ lines with at least 8 contract test methods.
  </action>
  <verify>
Run: python -m pytest backend/tests/contract/test_canvas_api_contract.py -v
Check: All canvas contract tests pass or failures are documented
  </verify>
  <done>
Canvas API contract tests validate request/response schemas for all canvas endpoints.
  </done>
</task>

<task type="auto">
  <name>Create browser API contract tests</name>
  <files>backend/tests/contract/test_browser_api_contract.py</files>
  <action>
Create backend/tests/contract/test_browser_api_contract.py for browser endpoints:

1. Test class TestBrowserSessionContract:
   - test_create_session_contracts: POST /api/browser/session validates
   - test_list_sessions_contracts: GET /api/browser/sessions validates
   - test_close_session_contracts: DELETE /api/browser/session/{id} validates

2. Test class TestBrowserNavigationContract:
   - test_navigate_contracts: POST /api/browser/navigate validates
   - test_navigate_request_schema: URL parameter validation
   - test_navigate_errors: 400/422 schemas for invalid URLs

3. Test class TestBrowserInteractionContract:
   - test_click_contracts: POST /api/browser/click validates
   - test_fill_contracts: POST /api/browser/fill validates
   - test_screenshot_contracts: POST /api/browser/screenshot validates
   - test_screenshot_response: Base64 image in response schema

4. Test class TestBrowserGovernanceContract:
   - test_governance_headers: X-Agent-Maturity header processed
   - test_permission_denied: 403 response schema valid
   - test_unauthorized: 401 response schema valid

5. Mock Playwright browser for contract tests:
   - Use monkeypatch to mock browser launch
   - Return valid mock responses for all browser operations
   - Ensure mocks return schema-compliant data

6. Add custom Hypothesis strategies for browser-specific inputs:
   - URL strategy for valid web URLs
   - Selector strategy for CSS selectors

File size target: 60+ lines with at least 6 contract test methods.
  </action>
  <verify>
Run: python -m pytest backend/tests/contract/test_browser_api_contract.py -v
Check: All browser contract tests pass or failures are documented
  </verify>
  <done>
Browser API contract tests validate request/response schemas for all browser endpoints.
  </done>
</task>

<task type="auto">
  <name>Generate contract test results report</name>
  <files>backend/tests/contract/CONTRACT_TEST_RESULTS.md</files>
  <action>
Create backend/tests/contract/CONTRACT_TEST_RESULTS.md documenting contract test execution:

1. Run full contract test suite and capture results:
   ```bash
   pytest backend/tests/contract/ -v --tb=short > contract_test_output.txt 2>&1
   ```

2. Document results in markdown format:
   - Test execution summary (total, passed, failed, skipped)
   - Coverage by endpoint category (agent, canvas, browser)
   - Schema validation issues found
   - Contract violations detected
   - Excluded endpoints with justification

3. For each failure, document:
   - Endpoint path and method
   - Expected vs actual schema
   - Root cause (implementation vs documentation)
   - Recommended fix

4. Create action items for:
   - Schema updates needed (if docs are wrong)
   - Implementation fixes (if code is wrong)
   - Test exclusions (if external dependencies)

5. Include command to re-run contract tests:
   ```bash
   pytest backend/tests/contract/ -v
   ```

File size target: 40+ lines with comprehensive test results documentation.
  </action>
  <verify>
Run: ls -la backend/tests/contract/CONTRACT_TEST_RESULTS.md
Check: File exists with test results summary
  </verify>
  <done>
Contract test results report documents all schema validations, failures, and action items.
  </done>
</task>

</tasks>

<verification>
1. Install Schemathesis: pip install -r backend/requirements-testing.txt
2. Run contract tests: python -m pytest backend/tests/contract/ -v
3. Verify schema validation: python -c "from main_api_app import app; print('Schema valid' if app.openapi().get('openapi', '').startswith('3') else 'Schema invalid')"
4. Check results report: cat backend/tests/contract/CONTRACT_TEST_RESULTS.md
5. Run specific contract test categories: pytest backend/tests/contract/test_agent_api_contract.py -v
</verification>

<success_criteria>
1. Schemathesis added to requirements and installed successfully
2. All contract test files created (4+ test files)
3. OpenAPI schema validation tests pass
4. Contract tests execute for agent, canvas, browser endpoints
5. Results report documents any schema violations
6. Excluded endpoints documented with justification
</success_criteria>

<output>
After completion, create `.planning/phases/167-api-routes-coverage/167-02-SUMMARY.md` with:
- Contract test execution summary
- Schema validation results
- Contract violations found and fixed
- Endpoints excluded with justification
- Recommendations for API documentation improvements
</output>
