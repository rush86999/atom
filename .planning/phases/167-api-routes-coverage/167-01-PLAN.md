---
phase: 167-api-routes-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_health_routes.py
  - backend/tests/api/test_canvas_routes.py
  - backend/tests/api/test_browser_routes.py
  - backend/tests/api/test_device_routes.py
  - backend/tests/api/test_auth_routes.py
  - backend/tests/api/conftest.py
  - backend/tests/conftest.py
autonomous: true

must_haves:
  truths:
    - "FastAPI endpoints (health, canvas, browser, device, auth) achieve 75%+ line coverage using TestClient"
    - "All happy path tests use proper fixture isolation (db_session, test_client)"
    - "Test coverage report shows API module coverage increased from 0.0% baseline"
    - "Tests cover request validation, response serialization, and business logic"
  artifacts:
    - path: "backend/tests/api/test_health_routes.py"
      provides: "Health endpoint TestClient tests"
      min_lines: 80
    - path: "backend/tests/api/test_canvas_routes.py"
      provides: "Canvas routes TestClient tests"
      min_lines: 120
    - path: "backend/tests/api/test_browser_routes.py"
      provides: "Browser automation routes tests"
      min_lines: 100
    - path: "backend/tests/api/test_device_routes.py"
      provides: "Device capability routes tests"
      min_lines: 80
    - path: "backend/tests/api/test_auth_routes.py"
      provides: "Authentication routes tests"
      min_lines: 100
    - path: "backend/tests/api/conftest.py"
      provides: "API test fixtures and TestClient wrapper"
      min_lines: 60
  key_links:
    - from: "backend/tests/api/test_health_routes.py"
      to: "backend/api/health_routes.py"
      via: "TestClient from tests.api.conftest.api_test_client"
      pattern: "from.*conftest.*import.*api_test_client|client\\.get\\(\"/health"
    - from: "backend/tests/api/conftest.py"
      to: "backend/main_api_app.py"
      via: "FastAPI app import for TestClient"
      pattern: "from main_api_app import app"
---

<objective>
TestClient-based coverage for core FastAPI endpoints (health, canvas, browser, device, auth).

Purpose: Achieve 75%+ line coverage on critical API routes using FastAPI TestClient with proper fixture isolation.
Output: Five new test files with comprehensive happy path and business logic coverage.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/tests/coverage_reports/GAP_ANALYSIS_164.md
@backend/api/health_routes.py
@backend/api/canvas_routes.py
@backend/api/browser_routes.py
@backend/api/device_capabilities.py
@backend/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Create API test fixtures and TestClient wrapper</name>
  <files>backend/tests/api/conftest.py</files>
  <action>
Create enhanced API test fixtures file at backend/tests/api/conftest.py:

1. Import FastAPI app from main_api_app.py:
   ```python
   from fastapi.testclient import TestClient
   from main_api_app import app
   ```

2. Create api_test_client fixture with proper isolation:
   - Use TestClient(app) wrapper
   - Set raise_server_exceptions=False for graceful error handling
   - Support headers injection for auth testing
   - Include database session injection for tests needing DB setup

3. Create authenticated_client fixture for protected endpoints:
   - Return TestClient with pre-configured Authorization header
   - Support different user roles (admin, regular user)

4. Add mock_service fixture for external dependencies:
   - Mock LLM service for agent chat endpoints
   - Mock Playwright for browser automation endpoints
   - Mock storage service for file operations

5. Add route_coverage helper to track which endpoints have been tested

Do NOT duplicate fixtures already in backend/tests/conftest.py - only API-specific ones.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/conftest.py::test_api_client_fixture --collect-only
Check: File exists and contains api_test_client, authenticated_client fixtures
  </verify>
  <done>
API conftest.py provides reusable TestClient fixtures for all API tests with auth and external service mocking support.
  </done>
</task>

<task type="auto">
  <name>Test health check routes with TestClient</name>
  <files>backend/tests/api/test_health_routes.py</files>
  <action>
Create backend/tests/api/test_health_routes.py with comprehensive coverage:

1. Test class TestLivenessProbe:
   - test_liveness_returns_200: GET /health/live returns status=alive
   - test_liveness_includes_timestamp: Response has ISO format timestamp
   - test_liveness_no_auth_required: Works without Authorization header

2. Test class TestReadinessProbe:
   - test_readiness_returns_200_when_healthy: All dependencies available
   - test_readiness_returns_503_when_db_down: Mock database failure
   - test_readiness_checks_disk_space: Verify MIN_DISK_GB validation
   - test_readiness_includes_dependency_status: Returns DB and disk status

3. Test class TestMetricsEndpoint:
   - test_metrics_returns_prometheus_format: CONTENT_TYPE_LATEST header
   - test_metrics_includes_http_metrics: Request counts, latency
   - test_metrics_includes_agent_metrics: Agent execution counters
   - test_metrics_accessible_without_auth: Prometheus scraping

4. Use pytest.mark.parametrize for edge cases:
   - Disk space at threshold (exactly MIN_DISK_GB)
   - Database timeout scenarios
   - Concurrent metric scrapes

5. Mock database dependencies using monkeypatch for DB failure tests

File size target: 80+ lines with at least 8 test methods covering all health endpoints.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_health_routes.py -v --cov=backend/api/health_routes --cov-report=term-missing
Check: Coverage >75% for health_routes.py, all tests pass
  </verify>
  <done>
Health routes test file with 75%+ coverage covering liveness, readiness, metrics endpoints including error paths.
  </done>
</task>

<task type="auto">
  <name>Test canvas routes with TestClient</name>
  <files>backend/tests/api/test_canvas_routes.py</files>
  <action>
Create backend/tests/api/test_canvas_routes.py with comprehensive coverage:

1. Test class TestCanvasFormSubmission:
   - test_submit_form_with_valid_data: POST /api/canvas/submit success
   - test_submit_form_links_to_agent_execution: agent_execution_id tracked
   - test_submit_form_requires_auth: 401 without Authorization header
   - test_submit_form_validates_canvas_id: 400 for missing/invalid canvas_id
   - test_submit_creates_canvas_audit_record: Audit trail created

2. Test class TestCanvasQueryEndpoints:
   - test_get_canvas_by_id: Retrieve existing canvas
   - test_get_canvas_list: List all canvases with pagination
   - test_get_canvas_not_found: 404 for non-existent canvas

3. Test class TestCanvasTypes:
   - test_chart_canvas_presentation: Chart canvas type rendering
   - test_form_canvas_presentation: Form canvas with input fields
   - test_markdown_canvas_presentation: Markdown content rendering
   - test_sheet_canvas_presentation: Spreadsheet data display

4. Test class TestCanvasGovernance:
   - test_form_submission_governance_check: Validates agent permissions
   - test_supervised_agent_can_submit: SUPERVISED+ maturity allows forms
   - test_student_agent_blocked: STUDENT maturity blocked from forms

5. Use fixtures for test data:
   - db_session for database operations
   - test_agent_autonomous for permission tests
   - test_agent_student for governance blocking tests
   - authenticated_client for protected endpoints

6. Mock WebSocket manager for broadcast tests

File size target: 120+ lines with at least 12 test methods covering canvas operations.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_canvas_routes.py -v --cov=backend/api/canvas_routes --cov-report=term-missing
Check: Coverage >75% for canvas_routes.py, all tests pass
  </verify>
  <done>
Canvas routes test file with 75%+ coverage covering form submission, queries, types, and governance.
  </done>
</task>

<task type="auto">
  <name>Test browser automation routes with TestClient</name>
  <files>backend/tests/api/test_browser_routes.py</files>
  <action>
Create backend/tests/api/test_browser_routes.py with comprehensive coverage:

1. Test class TestBrowserSessionManagement:
   - test_create_browser_session: POST /api/browser/session creates session
   - test_create_session_requires_intern_maturity: INTERN+ required
   - test_list_active_sessions: GET /api/browser/sessions returns list
   - test_close_browser_session: DELETE /api/browser/session/{id}
   - test_close_nonexistent_session: 404 for invalid session ID

2. Test class TestBrowserNavigation:
   - test_navigate_to_url: POST /api/browser/navigate with valid URL
   - test_navigate_requires_active_session: 400 without session_id
   - test_navigate_validates_url_format: 400 for invalid URLs
   - test_get_current_url: GET /api/browser/url returns current page

3. Test class TestBrowserInteractions:
   - test_click_element: POST /api/browser/click with selector
   - test_fill_form: POST /api/browser/fill with form data
   - test_screenshot: POST /api/browser/screenshot returns base64 image
   - test_execute_script: POST /api/browser/execute for JavaScript

4. Test class TestBrowserGovernance:
   - test_browser_requires_intern_minimum: STUDENT blocked
   - test_autonomous_full_access: AUTONOMOUS can use all features
   - test_supervised_with_monitoring: SUPERVISED logged

5. Mock Playwright dependencies:
   - Mock playwright browser launch
   - Mock page navigation and interaction methods
   - Mock screenshot return value (base64 string)

6. Use existing test_agent_* fixtures for maturity testing

File size target: 100+ lines with at least 10 test methods covering browser automation.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_browser_routes.py -v --cov=backend/api/browser_routes --cov-report=term-missing
Check: Coverage >75% for browser_routes.py, all tests pass
  </verify>
  <done>
Browser routes test file with 75%+ coverage covering sessions, navigation, interactions, and governance.
  </done>
</task>

<task type="auto">
  <name>Test device capability routes with TestClient</name>
  <files>backend/tests/api/test_device_routes.py</files>
  <action>
Create backend/tests/api/test_device_routes.py with comprehensive coverage:

1. Test class TestCameraCapability:
   - test_request_camera_access: POST /api/device/camera/request
   - test_camera_requires_intern_maturity: INTERN+ required
   - test_student_blocked_from_camera: STUDENT gets 403
   - test_camera_permission_denied: Mock permission denied scenario

2. Test class TestScreenRecording:
   - test_start_screen_recording: POST /api/device/screen/start
   - test_screen_recording_requires_supervised: SUPERVISED+ required
   - test_intern_blocked_from_recording: INTERN gets 403
   - test_stop_screen_recording: POST /api/device/screen/stop

3. Test class TestLocationAccess:
   - test_get_device_location: GET /api/device/location
   - test_location_requires_intern_maturity: INTERN+ required
   - test_location_returns_coordinates: Response has lat/lon

4. Test class TestNotifications:
   - test_send_notification: POST /api/device/notify
   - test_notification_requires_intern: INTERN+ minimum
   - test_notification_payload_validation: Validates message content

5. Test class TestDeviceWebSocket:
   - test_websocket_connection_upgrade: WebSocket endpoint accessible
   - test_websocket_requires_auth: 401 without valid token

6. Mock device APIs:
   - Mock camera permission check
   - Mock screen recording API
   - Mock geolocation service
   - Mock notification system

File size target: 80+ lines with at least 8 test methods covering device capabilities.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_device_routes.py -v --cov=backend/api/device_capabilities --cov-report=term-missing
Check: Coverage >75% for device_capabilities.py, all tests pass
  </verify>
  <done>
Device routes test file with 75%+ coverage covering camera, screen, location, notifications with governance.
  </done>
</task>

<task type="auto">
  <name>Test authentication routes with TestClient</name>
  <files>backend/tests/api/test_auth_routes.py</files>
  <action>
Create backend/tests/api/test_auth_routes.py with comprehensive coverage:

1. Test class TestUserAuthentication:
   - test_login_with_valid_credentials: POST /api/auth/login success
   - test_login_invalid_credentials: 401 for wrong password
   - test_login_missing_fields: 422 for missing email/password
   - test_login_returns_token: Response includes access_token

2. Test class TestUserRegistration:
   - test_register_new_user: POST /api/auth/register success
   - test_register_duplicate_email: 400 for existing email
   - test_register_validates_email_format: 422 for invalid email
   - test_register_password_requirements: 422 for weak passwords

3. Test class TestTokenRefresh:
   - test_refresh_valid_token: POST /api/auth/refresh success
   - test_refresh_expired_token: 401 for expired tokens
   - test_refresh_invalid_token: 401 for malformed tokens

4. Test class TestPasswordReset:
   - test_request_password_reset: POST /api/auth/reset/request
   - test_reset_with_valid_token: POST /api/auth/reset/confirm
   - test_reset_invalid_token: 400 for invalid reset token

5. Test class TestLogout:
   - test_logout_revokes_token: POST /api/auth/logout success
   - test_logout_without_auth: 401 without Authorization header

6. Use db_session fixture for user creation/cleanup
   - Create test users with hashed_password
   - Clean up users after each test

7. Mock email service for password reset emails

File size target: 100+ lines with at least 10 test methods covering auth flows.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_auth_routes.py -v --cov=backend/api/auth_routes --cov-report=term-missing
Check: Coverage >75% for auth_routes.py (if exists), all tests pass
  </verify>
  <done>
Auth routes test file with 75%+ coverage covering login, registration, token refresh, password reset, logout.
  </done>
</task>

</tasks>

<verification>
1. Run all new API tests: python -m pytest backend/tests/api/test_*.py -v
2. Generate coverage report: python -m pytest backend/tests/api/ --cov=backend/api --cov-report=term-missing --cov-report=json
3. Verify API module coverage increased from 0.0% baseline (check backend/coverage.json for api module percent_covered > 10%)
4. Confirm no test isolation failures (run with pytest-xdist: pytest -n auto backend/tests/api/)
5. Check coverage for each target file: health_routes.py >75%, canvas_routes.py >75%, browser_routes.py >75%, device_capabilities.py >75%
</verification>

<success_criteria>
1. Five new test files created (health, canvas, browser, device, auth routes)
2. Each test file has 75%+ coverage of its target route file
3. All tests use proper fixtures (db_session, test_client, test_agent_*)
4. API module overall coverage increased from 0.0% baseline
5. All tests pass with no isolation issues
</success_criteria>

<output>
After completion, create `.planning/phases/167-api-routes-coverage/167-01-SUMMARY.md` with:
- Coverage achieved for each tested route file
- Total new test count and lines of test code
- API module coverage percentage increase
- Any uncovered edge cases identified for follow-up
</output>
