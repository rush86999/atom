---
phase: 167-api-routes-coverage
plan: 04
type: execute
wave: 2
depends_on: ["167-01"]
files_modified:
  - backend/tests/api/test_request_validation.py
  - backend/tests/api/test_response_serialization.py
  - backend/tests/api/test_dto_validation.py
  - backend/tests/api/conftest_validation.py
  - backend/tests/api/COVERAGE_REPORT.md
autonomous: true

must_haves:
  truths:
    - "Request validation and response serialization are tested for all major endpoints"
    - "Pydantic models validate incoming requests and serialize responses correctly"
    - "DTO (Data Transfer Object) schemas match OpenAPI documentation"
    - "Edge cases in data types (nulls, empty strings, large numbers) handled correctly"
    - "DateTime and enum serialization validated"
  artifacts:
    - path: "backend/tests/api/test_request_validation.py"
      provides: "Request validation tests"
      min_lines: 120
    - path: "backend/tests/api/test_response_serialization.py"
      provides: "Response serialization tests"
      min_lines: 120
    - path: "backend/tests/api/test_dto_validation.py"
      provides: "DTO schema validation tests"
      min_lines: 100
    - path: "backend/tests/api/conftest_validation.py"
      provides: "Validation-specific fixtures"
      min_lines: 60
    - path: "backend/tests/api/COVERAGE_REPORT.md"
      provides: "Coverage summary report"
      min_lines: 50
  key_links:
    - from: "backend/tests/api/test_request_validation.py"
      to: "backend/core/models.py"
      via: "Pydantic model validation"
      pattern: "BaseModel|Field\\(|validator"
    - from: "backend/tests/api/test_response_serialization.py"
      to: "backend/api/health_routes.py"
      via: "Response schema validation"
      pattern: "response_model=|from_attributes"
    - from: "backend/tests/api/test_dto_validation.py"
      to: "backend/core/dto/"
      via: "DTO schema testing"
      pattern: "class.*Request.*BaseModel|class.*Response.*BaseModel"
---

<objective>
Request validation and response serialization testing with DTO schema validation and edge case handling.

Purpose: Ensure all API endpoints properly validate requests and serialize responses using Pydantic models.
Output: Comprehensive validation and serialization tests covering all major data types and edge cases.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/core/models.py
@backend/api/health_routes.py
@backend/api/canvas_routes.py
@backend/tests/conftest.py
@.planning/phases/167-api-routes-coverage/167-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Create validation-specific test fixtures</name>
  <files>backend/tests/api/conftest_validation.py</files>
  <action>
Create backend/tests/api/conftest_validation.py for validation testing fixtures:

1. Import validation utilities:
   ```python
   import pytest
   from pydantic import ValidationError, BaseModel
   from typing import Dict, Any, List, Optional
   from datetime import datetime
   ```

2. Create invalid_data_generator fixture:
   - Generate various invalid inputs (null, wrong types, empty strings)
   - Support edge cases (very long strings, special characters)
   - Generate boundary values (min/max numbers)

3. Create valid_request_factory fixture:
   - Generate valid request payloads for each endpoint
   - Support customization via parameters
   - Include all required and optional fields

4. Create response_validator fixture:
   - Validate response JSON against expected schema
   - Check for unexpected fields
   - Verify data type correctness

5. Create edge_case_values fixture:
   - Boundary numeric values (int max/min, float precision)
   - String edge cases (empty, max length, unicode)
   - DateTime edge cases (epoch, far future)
   - Null/None handling

6. Add validation_error_matcher helper:
   - Extract specific validation errors from Pydantic exceptions
   - Format error messages for test assertions

File size target: 60+ lines with reusable validation fixtures.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/conftest_validation.py --collect-only
Check: All validation fixtures available without errors
  </verify>
  <done>
Validation conftest provides reusable fixtures for request/response validation testing.
  </done>
</task>

<task type="auto">
  <name>Create request validation tests</name>
  <files>backend/tests/api/test_request_validation.py</files>
  <action>
Create backend/tests/api/test_request_validation.py for request validation:

1. Test class TestAgentRequestValidation:
   - test_spawn_agent_missing_required_field: 422 without name
   - test_spawn_agent_invalid_maturity: Invalid maturity enum
   - test_spawn_agent_confidence_out_of_range: >1.0 or <0.0
   - test_spawn_agent_extra_fields_rejected: Unknown fields
   - test_spawn_agent_string_for_numeric_field: Type error

2. Test class TestCanvasRequestValidation:
   - test_submit_canvas_missing_canvas_id: Required field
   - test_submit_canvas_invalid_form_data_type: Not a dict
   - test_submit_canvas_empty_form_data: Edge case
   - test_submit_canvas_invalid_execution_id_format: UUID validation
   - test_submit_canvas_form_data_too_large: Size limit

3. Test class TestBrowserRequestValidation:
   - test_navigate_invalid_url_format: Not a URL
   - test_navigate_missing_url_field: Required field
   - test_click_invalid_selector_format: CSS syntax error
   - test_fill_missing_selector_field: Required field
   - test_execute_script_injection_attempt: Script restrictions

4. Test class TestAuthRequestValidation:
   - test_login_missing_email_field: Required field
   - test_login_invalid_email_format: Email validation
   - test_login_short_password: Min length enforcement
   - test_register_password_mismatch: Confirm password
   - test_register_weak_password: Password complexity

5. Test class TestDataTypeValidation:
   - test_string_fields_reject_numbers: Type enforcement
   - test_numeric_fields_reject_strings: Type enforcement
   - test_datetime_fields_require_iso_format: DateTime parsing
   - test_boolean_fields_accept_truthy_falsy: Boolean parsing
   - test_enum_fields_accept_valid_values: Enum validation

6. Use pytest.mark.parametrize for multiple invalid inputs:
   ```python
   @pytest.mark.parametrize("invalid_maturity", ["INVALID", None, 123, ""])
   def test_spawn_agent_invalid_maturity(client, invalid_maturity):
       response = client.post("/api/agents/spawn", json={"maturity": invalid_maturity})
       assert response.status_code == 422
   ```

File size target: 120+ lines with at least 15 validation test methods.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_request_validation.py -v
Check: All validation tests pass, proper 422 responses
  </verify>
  <done>
Request validation tests cover all major endpoints with type, format, and constraint validation.
  </done>
</task>

<task type="auto">
  <name>Create response serialization tests</name>
  <files>backend/tests/api/test_response_serialization.py</files>
  <action>
Create backend/tests/api/test_response_serialization.py for response validation:

1. Test class TestHealthResponseSerialization:
   - test_liveness_response_schema: Matches expected structure
   - test_liveness_datetime_format: ISO 8601 format
   - test_readiness_response_includes_dependencies: DB and disk status
   - test_metrics_response_content_type: Prometheus format

2. Test class TestAgentResponseSerialization:
   - test_agent_list_response_schema: List structure
   - test_agent_detail_response_fields: All expected fields
   - test_agent_datetime_fields_serialized: ISO format
   - test_agent_enum_fields_serialized: String representation
   - test_agent_nullable_fields_handle_null: None becomes null

3. Test class TestCanvasResponseSerialization:
   - test_canvas_response_schema_validation: Matches OpenAPI
   - test_canvas_audit_response_includes_timestamps: DateTime fields
   - test_canvas_form_response_serializes_nested_data: Complex structures
   - test_canvas_list_response_pagination: Links and counts

4. Test class TestErrorResponseSerialization:
   - test_401_response_schema: Consistent error format
   - test_403_response_includes_reason: Governance explanation
   - test_422_response_includes_field_errors: Validation details
   - test_500_response_includes_correlation_id: Debugging info
   - test_error_responses_dont_leak_internals: No stack traces

5. Test class TestDataTypeSerialization:
   - test_datetime_fields_serialize_to_iso: Consistent format
   - test_enum_fields_serialize_to_strings: Enum values
   - test_decimal_fields_serialize_correctly: Precision preserved
   - test_uuid_fields_serialize_to_strings: UUID format
   - test_nullable_fields_serialize_to_null: Not omitted

6. Test class TestResponseHeaders:
   - test_content_type_header_set: application/json
   - test_cache_headers_on_cacheable_endpoints: ETag, Last-Modified
   - test_cors_headers_set: Access-Control-Allow-Origin

7. Use response_validator fixture from conftest_validation.py

File size target: 120+ lines with at least 15 serialization test methods.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_response_serialization.py -v
Check: All serialization tests pass, responses match schemas
  </verify>
  <done>
Response serialization tests cover all major endpoints with schema and header validation.
  </done>
</task>

<task type="auto">
  <name>Create DTO validation tests</name>
  <files>backend/tests/api/test_dto_validation.py</files>
  <action>
Create backend/tests/api/test_dto_validation.py for DTO schema validation:

1. Test class TestAgentDTOValidation:
   - test_agent_request_dto_required_fields: name, category, module_path
   - test_agent_request_dto_optional_fields: description, config
   - test_agent_response_dto_all_fields: id, created_at, status
   - test_agent_dto_enum_validation: maturity, status enums

2. Test class TestCanvasDTOValidation:
   - test_canvas_submission_dto_required_fields: canvas_id, form_data
   - test_canvas_submission_dto_optional_fields: agent_id, execution_id
   - test_canvas_response_dto_field_types: Correct types
   - test_canvas_dto_nested_validation: form_data structure

3. Test class TestBrowserDTOValidation:
   - test_browser_session_dto_fields: session_id, url
   - test_browser_navigation_dto_url_validation: URL format
   - test_browser_action_dto_selector_validation: CSS selector
   - test_browser_dto_timeout_validation: Positive integer

4. Test class TestAuthDTOValidation:
   - test_login_request_dto_fields: email, password
   - test_login_request_dto_email_validation: Email format
   - test_register_request_dto_password_validation: Complexity rules
   - test_auth_response_dto_token_fields: access_token, refresh_token

5. Test class TestDTOEdgeCases:
   - test_dto_handles_null_optional_fields: None values
   - test_dto_rejects_null_required_fields: 422 on null
   - test_dto_handles_empty_strings: Edge case handling
   - test_dto_handles_large_collections: Array limits
   - test_dto_handles_unicode_characters: Internationalization

6. Test class TestDTOOpenAPIAlignment:
   - test_dto_fields_match_openapi_schema: Compare with app.openapi()
   - test_dto_required_fields_match_documentation: Consistency check
   - test_dto_types_match_openapi_types: Type alignment
   - test_dto_enum_values_match_documentation: Enum consistency

7. Use Pydantic directly for DTO testing:
   ```python
   def test_agent_request_dto_required_fields():
       with pytest.raises(ValidationError):
           AgentRequestDTO(name="test")  # Missing required fields
   ```

File size target: 100+ lines with at least 12 DTO validation test methods.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_dto_validation.py -v
Check: All DTO tests pass, Pydantic validations work correctly
  </verify>
  <done>
DTO validation tests cover request/response schemas with OpenAPI alignment verification.
  </done>
</task>

<task type="auto">
  <name>Generate API routes coverage report</name>
  <files>backend/tests/api/COVERAGE_REPORT.md</files>
  <action>
Create backend/tests/api/COVERAGE_REPORT.md documenting Phase 167 coverage:

1. Run complete API test suite and capture coverage:
   ```bash
   pytest backend/tests/api/ -v --cov=backend/api --cov-report=term-missing --cov-report=html --cov-report=json > api_coverage_output.txt 2>&1
   ```

2. Parse coverage results and document in markdown:
   - Overall API module coverage percentage
   - Coverage breakdown by file (health_routes, canvas_routes, etc.)
   - Lines covered vs total lines
   - Uncovered critical paths identified

3. Document test counts by category:
   - Happy path tests: count
   - Error path tests: count
   - Validation tests: count
   - Serialization tests: count
   - Contract tests: count

4. List uncovered endpoints and paths:
   - Endpoints with <50% coverage
   - Endpoints with no tests
   - Critical paths not covered
   - Recommendations for follow-up

5. Document DTO validation status:
   - DTOs with full validation
   - DTOs missing validation
   - OpenAPI alignment issues found

6. Include coverage trends:
   - Baseline: 0.0% coverage
   - Phase 167 target: 75% coverage
   - Actual achieved: [TBD]
   - Gap remaining to target

7. Add command to regenerate report:
   ```bash
   pytest backend/tests/api/ --cov=backend/api --cov-report=html
   open htmlcov/index.html
   ```

File size target: 50+ lines with comprehensive coverage documentation.
  </action>
  <verify>
Run: ls -la backend/tests/api/COVERAGE_REPORT.md
Check: File exists with coverage summary and recommendations
  </verify>
  <done>
Coverage report documents API test coverage, gaps, and recommendations for follow-up phases.
  </done>
</task>

</tasks>

<verification>
1. Run all validation tests: python -m pytest backend/tests/api/test_*validation*.py -v
2. Run coverage for API module: pytest backend/tests/api/ --cov=backend/api --cov-report=term-missing
3. Verify 75%+ coverage target achieved: Check API module percent_covered
4. Check DTO alignment: python -m pytest backend/tests/api/test_dto_validation.py -v
5. Review coverage report: cat backend/tests/api/COVERAGE_REPORT.md
</verification>

<success_criteria>
1. Validation test files created (request, response, DTO)
2. Request validation covers all major endpoints with type/format checking
3. Response serialization verified against schemas
4. DTO validation includes OpenAPI alignment checks
5. API module coverage reaches 75%+ target
6. Coverage report documents gaps and recommendations
</success_criteria>

<output>
After completion, create `.planning/phases/167-api-routes-coverage/167-04-SUMMARY.md` with:
- Request validation test results
- Response serialization verification results
- DTO validation coverage
- Overall API coverage percentage achieved
- Remaining gaps identified
- Recommendations for Phase 168 (if needed)
</output>
