---
phase: 198-coverage-push-85
plan: 06
type: execute
wave: 3
depends_on: [198-02, 198-03]
files_modified:
  - backend/tests/integration/test_agent_execution_e2e.py
  - backend/core/atom_agent_endpoints.py
  - backend/core/llm/byok_handler.py
autonomous: true

must_haves:
  truths:
    - "Agent execution E2E workflow tested (governance → execution → episodic memory)"
    - "All maturity levels tested in E2E context (STUDENT, INTERN, SUPERVISED, AUTONOMOUS)"
    - "Episode creation verified after agent execution"
    - "Agent execution tracking verified (status, latency, error handling)"
  artifacts:
    - path: "backend/tests/integration/test_agent_execution_e2e.py"
      provides: "E2E agent execution workflow tests"
      min_lines: 300
      exports: ["TestAgentExecutionE2E", "TestMaturityLevelExecution"]
  key_links:
    - from: "tests/integration/test_agent_execution_e2e.py"
      to: "core.atom_agent_endpoints"
      via: "full execution workflow"
      pattern: "test_execution_creates_episode|test_execution_with_maturity_"
    - from: "tests/integration/test_agent_execution_e2e.py"
      to: "core.episode_segmentation_service"
      via: "episode creation verification"
      pattern: "assert episode_count == 1"
---

<objective>
Create end-to-end integration tests for agent execution workflow (governance → LLM → episodic memory). Test all 4 maturity levels in execution context, verify episode creation and execution tracking.

Purpose: Integration tests catch bugs that unit tests miss. E2E testing of critical workflow contributes ~1-2% to overall 85% coverage target with high confidence.

Output: 15-20 E2E tests, agent execution workflow validated
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-coverage-push-85/198-RESEARCH.md

# Integration test patterns
@backend/tests/integration/
@backend/core/atom_agent_endpoints.py
@backend/core/llm/byok_handler.py
@backend/core/episode_segmentation_service.py

# Phase 197 patterns
@.planning/phases/197-quality-first-push-80/197-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up E2E test infrastructure</name>
  <files>
    backend/tests/integration/conftest.py
    backend/tests/integration/test_agent_execution_e2e.py
  </files>
  <action>
    1. Create E2E test fixtures in conftest.py:
       - e2e_db_session: Database session with full schema
       - e2e_client: TestClient with dependency overrides
       - mock_llm_streaming: AsyncMock for LLM responses
       - mock_websocket: AsyncMock for WebSocket notifications

    2. Configure pytest-asyncio for E2E tests:
       Add to conftest.py: asyncio_mode = "auto"

    3. Create test file structure:
       @pytest.mark.integration decorator on E2E tests
       Separate from unit tests for selective execution

    Pattern from Phase 197 integration tests:
    Use AsyncMock for LLM streaming responses
    Mock WebSocket manager for notifications
    Full database schema (not mocked) for true E2E
  </action>
  <verify>
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/integration/test_agent_execution_e2e.py --collect-only
    Expected: Test file collects without errors
  </verify>
  <done>
    E2E test infrastructure ready, fixtures created
  </done>
</task>

<task type="auto">
  <name>Task 2: Add AUTONOMOUS agent execution E2E tests</name>
  <files>
    backend/tests/integration/test_agent_execution_e2e.py
  </files>
  <action>
    Add TestAgentExecutionE2E class with 4-5 AUTONOMOUS tests:

    1. Basic execution tests (2 tests):
       - test_autonomous_agent_execution_creates_episode
       - test_autonomous_agent_execution_with_streaming_response

    2. Execution tracking tests (2 tests):
       - test_execution_status_tracking (pending → running → completed)
       - test_execution_latency_measurement

    3. Edge case tests (1 test):
       - test_autonomous_agent_execution_with_llm_error

    Pattern from RESEARCH.md Appendix B:
    agent = AgentFactory(maturity_level="AUTONOMOUS")
    response = client.post(f"/agents/{agent.id}/execute", json={"message": "Test"})
    assert response.status_code == 200
    episodes = db_session.query(Episode).filter_by(agent_id=agent.id).all()
    assert len(episodes) == 1
  </action>
  <verify>
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/integration/test_agent_execution_e2e.py::TestAgentExecutionE2E -v -k "autonomous"
    Expected: 4-5 tests passing
  </verify>
  <done>
    4-5 AUTONOMOUS E2E tests passing
  </done>
</task>

<task type="auto">
  <name>Task 3: Add SUPERVISED and INTERN agent E2E tests</name>
  <files>
    backend/tests/integration/test_agent_execution_e2e.py
  </files>
  <action>
    Add 4-5 tests for SUPERVISED and INTERN maturity levels:

    1. SUPERVISED execution tests (2 tests):
       - test_supervised_agent_execution_with_monitoring
       - test_supervised_agent_execution_with_intervention

    2. INTERN execution tests (2 tests):
       - test_intern_agent_execution_with_proposal
       - test_intern_agent_execution_approval_flow

    3. Edge case tests (1 test):
       - test_intern_agent_proposal_rejection

    Pattern from Phase 197:
    Mock supervision monitoring for SUPERVISED tests
    Verify proposal creation for INTERN agents
    Assert execution blocks without approval
  </action>
  <verify>
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/integration/test_agent_execution_e2e.py::TestAgentExecutionE2E -v -k "supervised or intern"
    Expected: 4-5 tests passing
  </verify>
  <done>
    4-5 SUPERVISED/INTERN E2E tests passing
  </done>
</task>

<task type="auto">
  <name>Task 4: Add STUDENT agent and error path E2E tests</name>
  <files>
    backend/tests/integration/test_agent_execution_e2e.py
  </files>
  <action>
    Add 3-4 tests for STUDENT agents and error paths:

    1. STUDENT execution tests (2 tests):
       - test_student_agent_blocked_from_execution
       - test_student_agent_read_only_operations

    2. Error path tests (2 tests):
       - test_execution_with_nonexistent_agent
       - test_execution_with_invalid_message_format

    Pattern from trigger_interceptor behavior:
    STUDENT agents should be blocked from automated execution
    Verify 403 Forbidden response for STUDENT execution attempts
  </action>
  <verify>
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/integration/test_agent_execution_e2e.py::TestAgentExecutionE2E -v -k "student or error"
    Expected: 3-4 tests passing
  </verify>
  <done>
    3-4 STUDENT/error path E2E tests passing
  </done>
</task>

<task type="auto">
  <name>Task 5: Add episodic memory integration verification</name>
  <files>
    backend/tests/integration/test_agent_execution_e2e.py
  </files>
  <action>
    Add TestEpisodicMemoryIntegration class with 4-5 tests:

    1. Episode creation tests (2 tests):
       - test_episode_creation_after_execution
       - test_episode_segments_creation

    2. Episode metadata tests (2 tests):
       - test_episode_with_canvas_context
       - test_episode_with_feedback_context

    3. Edge case tests (1 test):
       - test_episode_creation_with_execution_failure

    Pattern from RESEARCH.md:
    Execute agent, then query Episode and EpisodeSegment models
    Assert episode_count == 1 and segment_count > 0
    Verify episode.metadata contains execution_context
  </action>
  <verify>
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/integration/test_agent_execution_e2e.py::TestEpisodicMemoryIntegration -v
    Expected: 4-5 tests passing
  </verify>
  <done>
    4-5 episodic memory integration tests passing
  </done>
</task>

<task type="auto">
  <name>Task 6: Verify E2E test coverage and execution</name>
  <files>
    backend/tests/integration/test_agent_execution_e2e.py
  </files>
  <action>
    1. Run full E2E test suite:
       pytest backend/tests/integration/test_agent_execution_e2e.py -v -m integration

    2. Verify test count: 15-20 tests total
    3. Check integration coverage contribution:
       pytest backend/tests/integration/test_agent_execution_e2e.py --cov=core.atom_agent_endpoints --cov=core.episode_segmentation_service --cov-report=term-missing

    4. Generate E2E test summary:
       - Tests passing: 15-20
       - Maturity levels tested: 4/4
       - Integration paths validated: governance → execution → episodic memory

    5. Document any flaky tests or timing issues
  </action>
  <verify>
    E2E test suite passes: 15-20 tests
    Integration coverage: +1-2% contribution to overall coverage
    No timing dependencies or flaky tests
  </verify>
  <done>
    E2E test suite complete, 15-20 tests passing, integration validated
  </done>
</task>

</tasks>

<verification>
1. Run E2E tests: pytest backend/tests/integration/test_agent_execution_e2e.py -v -m integration
   Verify: 15-20 tests passing
2. Check maturity levels: 4/4 levels tested (STUDENT, INTERN, SUPERVISED, AUTONOMOUS)
3. Verify episodic memory integration: Episode and EpisodeSegment records created
4. Check integration coverage: pytest backend/tests/integration/ --cov=core --cov-report=term
   Verify: +1-2% coverage contribution
</verification>

<success_criteria>
- E2E tests created: 15-20 tests
- Maturity levels tested: 4/4 (STUDENT, INTERN, SUPERVISED, AUTONOMOUS)
- Integration paths validated: governance → execution → episodic memory
- Episode creation verified: 100% of successful executions
- Execution tracking verified: status, latency, errors
- Pass rate: >95%
- Integration coverage contribution: +1-2%
</success_criteria>

<output>
After completion, create `.planning/phases/198-coverage-push-85/198-06-SUMMARY.md`
</output>
