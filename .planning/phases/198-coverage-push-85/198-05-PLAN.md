---
phase: 198-coverage-push-85
plan: 05
type: execute
wave: 2
depends_on: [198-01]
files_modified:
  - backend/tests/unit/governance/test_governance_cache_coverage.py
  - backend/tests/api/test_health_routes_coverage.py
  - backend/core/governance_cache.py
  -backend/core/monitoring.py
autonomous: true

must_haves:
  truths:
    - "Governance cache coverage increases to 90%+ (from 80%)"
    - "Monitoring coverage increases to 75%+ (from current baseline)"
    - "Cache hit/miss scenarios tested"
    - "Health check endpoints tested (liveness, readiness, metrics)"
  artifacts:
    - path: "backend/tests/unit/governance/test_governance_cache_coverage.py"
      provides: "Governance cache edge case tests"
      min_lines: 150
      exports: ["TestGovernanceCacheEdgeCases", "TestCachePerformance"]
    - path: "backend/tests/api/test_health_routes_coverage.py"
      provides: "Health check endpoint tests"
      min_lines: 100
      exports: ["TestHealthRoutes", "TestMetricsEndpoints"]
  key_links:
    - from: "tests/unit/governance/test_governance_cache_coverage.py"
      to: "core.governance_cache"
      via: "cache performance tests"
      pattern: "test_cache_hit_|test_cache_miss_"
    - from: "tests/api/test_health_routes_coverage.py"
      to: "api.health_routes"
      via: "health endpoint tests"
      pattern: "test_health_live|test_health_ready"
---

<objective>
Increase cache and monitoring coverage to 75-90% through performance testing and health check validation. Target governance_cache.py (80% → 90%) and monitoring.py (baseline → 75%).

Purpose: Cache and monitoring are foundational for performance and observability. High-impact coverage improvement (10-15%) contributes ~0.5-1% to overall 85% target.

Output: 15-20 new tests, cache/monitoring coverage at 75-90%+
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/198-coverage-push-85/198-RESEARCH.md

# Phase 197 patterns
@.planning/phases/197-quality-first-push-80/197-08-SUMMARY.md

# Existing test files
@backend/tests/unit/governance/test_governance_cache_coverage.py

# Source files
@backend/core/governance_cache.py
@backend/core/monitoring.py
@backend/api/health_routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze cache/monitoring coverage gaps</name>
  <files>
    backend/core/governance_cache.py
    backend/core/monitoring.py
  </files>
  <action>
    1. Run coverage report on cache and monitoring:
       pytest backend/tests/unit/governance/test_governance_cache_coverage.py --cov=core.governance_cache --cov=core.monitoring --cov-report=term-missing

    2. Identify uncovered lines and code paths:
       - Cache: hit/miss scenarios, invalidation, cache warming, TTL handling
       - Monitoring: metrics collection, health checks, performance logging

    3. Categorize gaps: performance scenarios, edge cases, error paths
    4. Create test plan for 10-15% coverage improvement
  </action>
  <verify>
    Coverage report shows baseline: cache 80%, monitoring TBD
    Gap analysis identifies 15-20 uncovered lines
  </verify>
  <done>
    Coverage gap analysis complete, test plan created
  </done>
</task>

<task type="auto">
  <name>Task 2: Add governance cache edge case tests</name>
  <files>
    backend/tests/unit/governance/test_governance_cache_coverage.py
  </files>
  <action>
    Add TestGovernanceCacheEdgeCases class with 8-10 tests:

    1. Cache hit/miss tests (3 tests):
       - test_cache_hit_with_valid_key
       - test_cache_miss_with_invalid_key
       - test_cache_hit_rate_measurement

    2. Cache invalidation tests (2 tests):
       - test_cache_invalidation_on_agent_update
       - test_cache_invalidation_on_bulk_updates

    3. Cache warming tests (2 tests):
       - test_cache_warming_on_startup
       - test_cache_warming_with_missed_keys

    4. Edge case tests (2-3 tests):
       - test_cache_with_null_key
       - test_cache_with_expired_ttl
       - test_cache_overflow_protection

    Pattern from Phase 197 governance tests:
    Mock Redis backend
    Assert cache_hit_rate > 0.90 (90% target from Phase 197)
    Measure lookup latency < 0.001 (1ms target)
  </action>
  <verify>
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/governance/test_governance_cache_coverage.py::TestGovernanceCacheEdgeCases -v
    Expected: 8-10 new tests passing
  </verify>
  <done>
    8-10 cache edge case tests passing, coverage 85%+
  </done>
</task>

<task type="auto">
  <name>Task 3: Add cache performance tests</name>
  <files>
    backend/tests/unit/governance/test_governance_cache_coverage.py
  </files>
  <action>
    Add TestCachePerformance class with 5-6 tests:

    1. Performance benchmark tests (3 tests):
       - test_cache_lookup_latency_sub_1ms (measure avg latency)
       - test_cache_throughput_1000_ops_per_second
       - test_concurrent_cache_access

    2. Memory tests (2 tests):
       - test_cache_memory_usage_limits
       - test_cache_eviction_policy

    3. Edge case tests (1 test):
       - test_cache_with_redis_connection_failure

    Pattern from Phase 197:
    Use time.perf_counter() for latency measurement
    Run 1000 operations for throughput testing
    Assert latency < 0.001 (1ms)
  </action>
  <verify>
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/governance/test_governance_cache_coverage.py::TestCachePerformance -v
    Expected: 5-6 new tests passing
  </verify>
  <done>
    5-6 cache performance tests passing, cache coverage 90%+
  </done>
</task>

<task type="auto">
  <name>Task 4: Add health check and monitoring tests</name>
  <files>
    backend/tests/api/test_health_routes_coverage.py
  </files>
  <action>
    Add TestHealthRoutes and TestMetricsEndpoints classes with 10-12 tests:

    1. Health check tests (5 tests):
       - test_health_live_endpoint (liveness probe)
       - test_health_ready_endpoint (readiness with DB check)
       - test_health_ready_with_database_unavailable
       - test_health_ready_with_disk_space_low
       - test_health_metrics_endpoint

    2. Metrics collection tests (3 tests):
       - test_prometheus_metrics_collection
       - test_http_request_metrics
       - test_agent_execution_metrics

    3. Logging tests (2 tests):
       - test_structured_logging_format
       - test_context_binding_in_logs

    4. Edge case tests (1-2 tests):
       - test_health_with_service_dependencies_down
       - test_metrics_with_invalid_format

    Pattern from Phase 197:
    Use TestClient for FastAPI endpoint testing
    Assert response status codes (200 for healthy, 503 for unhealthy)
    Verify metrics in Prometheus format
  </action>
  <verify>
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/api/test_health_routes_coverage.py -v
    Expected: 10-12 new tests passing
  </verify>
  <done>
    10-12 health/monitoring tests passing, monitoring coverage 75%+
  </done>
</task>

<task type="auto">
  <name>Task 5: Verify cache/monitoring coverage targets</name>
  <files>
    backend/tests/unit/governance/test_governance_cache_coverage.py
    backend/tests/api/test_health_routes_coverage.py
  </files>
  <action>
    1. Run coverage report on cache and monitoring:
       pytest backend/tests/unit/governance/test_governance_cache_coverage.py backend/tests/api/test_health_routes_coverage.py --cov=core.governance_cache --cov=core.monitoring --cov-report=term-missing

    2. Verify targets:
       - governance_cache.py: 90%+ (from 80%)
       - monitoring.py: 75%+ (from baseline)

    3. Document remaining gaps for future phases
    4. Generate coverage summary

    If targets not met:
    - Identify specific uncovered lines
    - Add targeted tests for remaining gaps
    - Re-run coverage measurement
  </action>
  <verify>
    Coverage report shows: cache 90%+, monitoring 75%+
    Test count: 15-20 tests total
  </verify>
  <done>
    Cache/monitoring at 75-90% coverage, 15-20 new tests created
  </done>
</task>

</tasks>

<verification>
1. Run cache/monitoring coverage tests: pytest backend/tests/unit/governance/test_governance_cache_coverage.py backend/tests/api/test_health_routes_coverage.py -v
   Verify: 15-20 tests passing
2. Check coverage percentages: pytest backend/tests/unit/governance/test_governance_cache_coverage.py backend/tests/api/test_health_routes_coverage.py --cov=core.governance_cache --cov=core.monitoring --cov-report=term
   Verify: cache 90%+, monitoring 75%+
3. Verify cache performance: latency < 1ms, throughput > 1000 ops/s
4. Verify health endpoints: /health/live, /health/ready, /health/metrics all tested
</verification>

<success_criteria>
- governance_cache.py coverage: 90%+ (from 80%)
- monitoring.py coverage: 75%+ (from baseline)
- New tests created: 15-20 tests
- Cache performance: latency < 1ms, throughput > 1000 ops/s
- Health endpoints: 3/3 endpoints tested
- Pass rate: >95%
</success_criteria>

<output>
After completion, create `.planning/phases/198-coverage-push-85/198-05-SUMMARY.md`
</output>
