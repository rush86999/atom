---
context: phase
phase: 306-tdd-bug-discovery-coverage
task: 8
total_tasks: 11
status: in_progress
last_updated: 2026-08-02T01:57:51Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each constraint below was discovered through failure. Acknowledge each before proceeding.

- [ ] CONSTRAINT: `NotificationService.send_notification` has a 3-arg signature `(user_id, notification_type, data)` — never pass 7 kwargs (previous call raised TypeError, swallowed → notifications never delivered).
- [ ] CONSTRAINT: `budget_enforcement_service._send_enforcement_notification` admin query must NOT join `User.custom_role` / `CustomRole.permissions` (relationships commented out at models.py:454 → AttributeError). Use `User.role.in_(['admin','owner','billing_admin'])` + tenant fallback.
- [ ] CONSTRAINT: Gateway route tests must clear module-global `_rate_limit_state` (in `core.llm.gateway.auth`) between runs or rate-limit tests flake.
- [ ] CONSTRAINT: Patching the healer in tests requires `core.llm.routing.request_healer.get_request_healer` (imported inside `chat_completion`), NOT `core.llm.byok_handler.get_request_healer`.

**Do not proceed until all boxes are checked.**

<current_state>
Session implemented the core deliverable of "Atom: Close LLM-Gateway Gaps vs. Manifest" (a plan under phase 306): Phase A (OpenAI/Anthropic-compatible gateway), B1 (budget-notification fix), Phase C (6 providers). 22 tests passing; main_api_app imports clean. Pausing at task 8/11 with B2-B4 and Phase D remaining. Context was ~75% exhausted at pause — resume by re-running the 22-test set, then continue B2-B4.
</current_state>

<completed_work>

- Task 1: A1 models `GatewayApiKey` + `GatewayRequestLog` + guarded alembic migration `20260801_add_gateway_models.py` - Done
- Task 2: A2 `BYOKHandler.chat_completion` (full-history, fallback+healer, cost/usage/health/feedback; `GatewayBlockedError`/`AllProvidersFailedError`) - Done
- Task 3: A3 `core/llm/gateway/` package (`wire_formats.py`, `auth.py`, `gateway_service.py`, `__init__.py` w/ `map_gateway_error`) - Done
- Task 4: A4 `api/openai_gateway_routes.py` — `/v1/chat/completions`, `/v1/messages`, `/v1/models` + SSE adapters - Done
- Task 5: A5 `api/gateway_key_routes.py` — create/list/revoke/rotate (plaintext once) - Done
- Task 6: A6 wiring in `main_api_app.py` (guarded) + env vars (CLAUDE.md) + `docs/architecture/LLM_GATEWAY.md` - Done
- Task 7: A7 tests (22 passing in `test_round70_gateway.py`, `test_round70_budget_notification_fix.py`, `unit/core/test_byok_chat_completion.py`) - Done
- Task 9: C 6 providers (`xai`,`cerebras`,`fireworks`,`huggingface`,`nvidia_nim`,`zai`) in `providers_config` + BYOK defaults/env keys - Done
- Task 11: Cross-cutting docs + CI hygiene + verification (`ALL IMPORTS OK`) - Done
</completed_work>

<remaining_work>

- Task 8 (in_progress): B2 `personal_budget_service.send_budget_alert` also call `NotificationService.send_notification(user_id,"budget_alert",{...})`; B3 gateway threshold alerts (50/80/90/100%, fire-once per workspace-day, `ATOM_GATEWAY_BUDGET_ALERTS`); B4 `GatewayRequestLog` write hooks (stream+non-stream) + pii redaction + drop auth headers + truncate 64KB + retention sweep in `periodic_tasks` + `api/gateway_log_routes.py` (`GET /api/v1/gateway/logs` + `{id}`, owner-scoped).
- Task 10 (not_started): Phase D subscription-credential reuse — `LLMCredentialService.get_credential` priority OAuth→subscription→BYOK→ENV, `credential_type` column on `LLMOAuthCredential` + migration, `api/llm_oauth_routes.py` connect flow, security deferrals doc.
</remaining_work>

<decisions_made>

- Consolidated Phase-A tests into one `test_round70_gateway.py` (shared `make_db`/`make_client` helpers) instead of separate auth/routes/anthropic files.
- Fixed the deeper admin-join bug (non-existent relationships) — switched to `User.role.in_([...])`.
- `map_gateway_error` is a standalone module function (usable without a GatewayService).
- Rate limiting is in-memory per-key sliding window; tests must clear `_rate_limit_state`.
- Wire-format tests live in `TestWireFormats` class inside `test_round70_gateway.py` (no separate `test_gateway_wire_formats.py`).
</decisions_made>

<blockers>
- AWS Bedrock (C2 stretch): needs AWS creds + boto3 — deferred, documented as known limitation.
- Consumer-session cookie/token capture (Phase D): out of scope — only OAuth-granted flows ship (security deferral documented in LLM_GATEWAY.md).
</blockers>

## Required Reading (in order)
1. `docs/architecture/LLM_GATEWAY.md` — the design doc for the work just completed
2. `backend/core/llm/gateway/__init__.py` — `map_gateway_error` central error table
3. `backend/core/llm/gateway/auth.py` — identity resolution + `_rate_limit_state`
4. `.planning/METHODOLOGY.md` (if it exists) — project analytical lenses

## Critical Anti-Patterns (do NOT repeat these)
- 7-kwarg call to 3-arg `send_notification` → structural fix: always pass `(user_id, notification_type, data)` dict.
- Joining relationships that don't exist on the model → verify relationships exist before writing queries against them.
- Patching a symbol at the wrong import location (function-local imports) → patch the actual import source module.

## Infrastructure State
- No servers running; work was file edits + pytest only.
- `main_api_app` imports clean (verified).
- Many pre-existing uncommitted modified files (backend/api/*, backend/core/*) predate this session — do NOT assume they are part of the gateway work.

## Pre-Execution Critique Required
- Not applicable (execution already in progress).

<context>
Implemented Phase A/B1/C of the LLM-Gateway plan under phase 306. All core gateway files created, wired, and tested. Remaining work is the alerting/log-viewer (B2-B4) and subscription reuse (D). The `RuntimeWarning` at byok_handler.py:748 about un-awaited coroutine is pre-existing and benign (`run_until_complete` awaits when a loop is available).
</context>

<next_action>
Start with: re-run `PYTHONPATH=backend backend/venv/bin/python -m pytest backend/tests/unit/core/test_byok_chat_completion.py backend/tests/test_round70_budget_notification_fix.py backend/tests/test_round70_gateway.py -q` (expect 22 passed), then implement Task 8 (B2→B3→B4) starting with the B2 `personal_budget_service.send_budget_alert` NotificationService call.
</next_action>
