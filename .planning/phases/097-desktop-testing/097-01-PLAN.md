---
phase: 097-desktop-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/scripts/aggregate_coverage.py
autonomous: true

must_haves:
  truths:
    - "Coverage aggregator script parses Rust tarpaulin JSON coverage from desktop"
    - "Aggregator produces unified coverage report with per-platform breakdown (backend, frontend, mobile, desktop)"
    - "Unified report includes overall coverage percentage across all 4 platforms"
    - "Script handles missing desktop coverage file gracefully (exit with warning, not error)"
  artifacts:
    - path: backend/tests/scripts/aggregate_coverage.py
      provides: Unified coverage aggregation for all 4 platforms
      min_lines: 250
      exports: [load_pytest_coverage, load_jest_coverage, load_jest_expo_coverage, load_tarpaulin_coverage, aggregate_coverage, generate_report]
  key_links:
    - from: aggregate_coverage.py
      to: frontend-nextjs/src-tauri/coverage/coverage.json
      via: load_tarpaulin_coverage function
      pattern: coverage\.json.*tarpaulin
    - from: aggregate_coverage.py
      to: frontend-nextjs/src-tauri/coverage.sh
      via: tarpaulin JSON format parsing
      pattern: cargo tarpaulin
---

<objective>
Extend unified coverage aggregation script to include Rust tarpaulin coverage from desktop.

**Purpose**: Combine pytest JSON coverage (backend), Jest JSON coverage (frontend), jest-expo JSON coverage (mobile), and cargo-tarpaulin JSON coverage (desktop) into a single unified report with per-platform breakdown. This enables tracking overall coverage across all 4 platforms and supports the 80% overall quality gate.

**Output**: Extended Python script that parses all four coverage formats, aggregates metrics, and produces unified JSON/text reports.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
</execution_context>

<context>
@backend/tests/scripts/aggregate_coverage.py
@frontend-nextjs/src-tauri/coverage.sh
@.planning/ROADMAP.md
@.planning/phases/095-backend-frontend-integration/095-02-SUMMARY.md
@.planning/phases/096-mobile-integration/096-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add tarpaulin coverage loader to aggregation script</name>
  <files>backend/tests/scripts/aggregate_coverage.py</files>
  <action>
    Extend backend/tests/scripts/aggregate_coverage.py with desktop coverage support:

    1. **Add load_tarpaulin_coverage(path: Path) -> Dict[str, Any]** function:
       - Load tarpaulin coverage.json from frontend-nextjs/src-tauri/coverage/coverage.json
       - Parse tarpaulin JSON format: { "files": { "path/to/file.rs": { "stats": { "covered": 50, "coverable": 100, "percent": 50.0 } } } }
       - Return dict with platform='rust', coverage_pct, covered, total, branches_covered (0), branches_total (0), branch_coverage_pct (0.0)
       - Handle FileNotFoundError by returning platform='rust', coverage_pct=0.0, error='file not found'
       - Note: tarpaulin doesn't provide branch coverage, set branches to 0

    2. **Update aggregate_coverage()** function signature:
       - Add parameter: tarpaulin_path: Optional[Path] = None
       - Call load_tarpaulin_coverage and include in platforms dict if available
       - Compute overall coverage: (covered_backend + covered_frontend + covered_mobile + covered_rust) / (total_backend + total_frontend + total_mobile + total_rust)
       - Return dict with platforms: {python: {...}, javascript: {...}, mobile: {...}, rust: {...}}

    3. **Update main()** function:
       - Add --desktop-coverage argument default=frontend-nextjs/src-tauri/coverage/coverage.json
       - Pass desktop coverage path to aggregate_coverage()

    4. **Update generate_report()** function:
       - Add rust/desktop row to text format table
       - Add rust/desktop to markdown format table
       - Ensure all four platforms appear in reports

    Use existing patterns from load_jest_coverage and load_jest_expo_coverage for consistency.
  </action>
  <verify>cd backend && python tests/scripts/aggregate_coverage.py --help | grep desktop</verify>
  <done>aggregate_coverage.py supports desktop coverage with load_tarpaulin_coverage function</done>
</task>

<task type="auto">
  <name>Test desktop coverage aggregation</name>
  <files>backend/tests/scripts/coverage_reports/unified/coverage.json</files>
  <action>
    1. Check if desktop coverage exists: ls frontend-nextjs/src-tauri/coverage/coverage.json
    2. If missing, run: cd frontend-nextjs/src-tauri && ./coverage.sh (if x86_64)
    3. Run aggregation script: python backend/tests/scripts/aggregate_coverage.py --format text
    4. Verify rust/desktop platform appears in output
    5. Run with JSON format: python backend/tests/scripts/aggregate_coverage.py --format json
    6. Verify unified JSON structure: cat backend/tests/scripts/coverage_reports/unified/coverage.json | jq .platforms.rust
    7. Test graceful handling with missing desktop coverage: temporarily rename desktop coverage file, run script, verify warning not error

    Overall coverage should now include desktop in weighted average calculation.
  </action>
  <verify>cat backend/tests/scripts/coverage_reports/unified/coverage.json | jq '.platforms | keys'</verify>
  <done>Desktop coverage included in unified report, graceful handling of missing file</done>
</task>

</tasks>

<verification>
After completion:
1. Run `python backend/tests/scripts/aggregate_coverage.py --format text` - should show 4 platforms (python, javascript, mobile, rust)
2. Verify desktop coverage in JSON: `cat backend/tests/scripts/coverage_reports/unified/coverage.json | jq .platforms.rust`
3. Test missing file handling: rename desktop coverage, run script, verify warning not error
4. Verify overall coverage calculation includes all four platforms
</verification>

<success_criteria>
1. load_tarpaulin_coverage function exists and parses desktop coverage
2. Rust/desktop platform appears in all report formats (text, json, markdown)
3. Overall coverage percentage includes desktop in weighted average
4. Script handles missing desktop coverage gracefully (warning, not error)
5. CLI accepts --desktop-coverage argument
</success_criteria>

<output>
After completion, create `.planning/phases/097-desktop-testing/097-01-SUMMARY.md` with:
- load_tarpaulin_coverage implementation
- Sample unified output with desktop included
- Test execution results
- Updated overall coverage formula (4 platforms)
</output>
