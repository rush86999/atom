---
phase: 097-desktop-testing
plan: 03
type: execute
wave: 2
depends_on:
  - 097-02
files_modified:
  - frontend-nextjs/src-tauri/tests/property/file_operations_proptest.rs
  - frontend-nextjs/src-tauri/tests/property/mod.rs
autonomous: true

must_haves:
  truths:
    - "Proptest property tests validate path traversal prevention invariant"
    - "Proptest property tests validate file permissions preservation invariant"
    - "Proptest property tests validate nested directory creation invariant"
    - "Proptest property tests validate cross-platform path consistency invariant"
  artifacts:
    - path: frontend-nextjs/src-tauri/tests/property/file_operations_proptest.rs
      provides: Property-based tests for file operation invariants
      min_lines: 200
  key_links:
    - from: file_operations_proptest.rs
      to: proptest crate
      via: proptest!, prop_assert!, prop_assert_eq!
      pattern: proptest!|prop_assert
    - from: file_operations_proptest.rs
      to: backend/tests/property_tests/governance/test_governance_maturity_invariants.py
      via: Hypothesis patterns mirrored in proptest
      pattern: invariant.*property
---

<objective>
Create Rust property tests for desktop file operation invariants using proptest.

**Purpose**: Use property-based testing to validate critical invariants of file operations (path traversal prevention, permission enforcement, atomic operations, cross-platform consistency). This catches edge cases that example-based tests miss, following patterns proven in backend Hypothesis tests.

**Output**: Rust proptest file for file operation invariants with comprehensive coverage of desktop-specific file handling.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
</execution_context>

<context>
@frontend-nextjs/src-tauri/tests/commands_test.rs
@frontend-nextjs/src-tauri/tests/property/mod.rs
@backend/tests/property_tests/governance/test_governance_maturity_invariants.py
@.planning/phases/097-desktop-testing/097-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create file operations property tests</name>
  <files>frontend-nextjs/src-tauri/tests/property/file_operations_proptest.rs</files>
  <action>
    Create frontend-nextjs/src-tauri/tests/property/file_operations_proptest.rs with proptest properties:

    **Module structure:**
    ```rust
    //! Property-based tests for file operations
    //!
    //! Tests file path validation, permission handling, and cross-platform consistency
    //! using proptest to generate edge cases.

    use std::fs;
    use std::path::{Path, PathBuf};
    use proptest::prelude::*;
    ```

    **Property tests:**

    1. **Path Traversal Prevention Invariant** (1-2 properties):
       - Property: Paths with ".." segments should be normalized to safe paths
       - Property: Normalized paths should not escape root directory
       - Use: regex strategy for path traversal patterns
       - Settings: default cases (256)

    2. **File Permissions Preservation Invariant** (1-2 properties):
       - Property: Content written then read should match exactly
       - Use: fc.string() for arbitrary content, fc.ascii_string(1, 32) for filename
       - Use std::env::temp_dir() for test file location
       - Settings: 100 cases (fast I/O)

    3. **Nested Directory Creation Invariant** (1-2 properties):
       - Property: Creating nested directories should produce valid path structure
       - Use: fc.array of directory names
       - Verify: fs::create_dir_all creates all ancestors
       - Settings: 50 cases (moderate I/O)

    4. **Cross-Platform Path Consistency Invariant** (2-3 properties):
       - Property: PathBuf operations work consistently on all platforms
       - Property: File names extract correctly across platforms
       - Use: path segments with platform-agnostic characters
       - Settings: 100 cases

    5. **Path Normalization Invariant** (1-2 properties):
       - Property: Normalizing same path multiple times yields stable result
       - Use: path strings with redundant separators
       - Settings: 100 cases

    6. **File Existence Invariant** (1 property):
       - Property: write() followed by exists() returns true
       - Use: arbitrary content and temp file paths
       - Settings: 50 cases (I/O bound)

    Each property should have:
    - proptest! { #[test] fn property_name(input in strategy) { ... } }
    - prop_assert! or prop_assert_eq! for invariant checking
    - Clear docstring explaining the invariant
    - Cleanup in finally blocks or use scope-based patterns

    Follow patterns from backend Hypothesis tests but adapted to Rust/proptest syntax.
  </action>
  <verify>cd frontend-nextjs/src-tauri && cargo test --test file_operations_proptest</verify>
  <done>file_operations_proptest.rs with 8-12 properties</done>
</task>

<task type="auto">
  <name>Update property module to include file operations</name>
  <files>frontend-nextjs/src-tauri/tests/property/mod.rs</files>
  <action>
    Update frontend-nextjs/src-tauri/tests/property/mod.rs:

    1. Add module declaration: mod file_operations_proptest;
    2. Update module docstring to reference file operations tests
    3. Verify cargo test discovers the new module

    This ensures property tests are included in cargo test runs.
  </action>
  <verify>cd frontend-nextjs/src-tauri && cargo test --test property | grep file_operations</verify>
  <done>File operations property tests included in module</done>
</task>

</tasks>

<verification>
After completion:
1. Run `cd frontend-nextjs/src-tauri && cargo test --test file_operations_proptest` - all properties pass
2. Verify properties run with expected case counts
3. Check properties find at least one edge case (if shrinking works)
4. Run all tests to ensure no regressions
</verification>

<success_criteria>
1. file_operations_proptest.rs exists with 8-12 properties
2. All property tests pass with prop_assert!
3. Each property has clear invariant docstring
4. Properties use appropriate strategies (regex, string, array)
5. Test files cleaned up after execution (temp files)
6. Tests mirror backend Hypothesis patterns
</success_criteria>

<output>
After completion, create `.planning/phases/097-desktop-testing/097-03-SUMMARY.md` with:
- Property test list with invariants documented
- Generator strategies used
- Case count settings and rationale
- Any counterexamples found during development
- Cross-platform validation notes
</output>
