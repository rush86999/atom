---
phase: 097-desktop-testing
plan: 05
type: execute
wave: 2
depends_on:
  - 097-01
files_modified:
  - frontend-nextjs/src-tauri/tests/integration/file_dialog_integration.rs
  - frontend-nextjs/src-tauri/tests/integration/menu_bar_integration.rs
  - frontend-nextjs/src-tauri/tests/integration/notification_integration.rs
  - frontend-nextjs/src-tauri/tests/integration/cross_platform_validation.rs
  - frontend-nextjs/src-tauri/tests/integration/mod.rs
autonomous: true

must_haves:
  truths:
    - "Tauri integration tests cover native file dialog workflows"
    - "Tauri integration tests cover menu bar system tray interactions"
    - "Tauri integration tests cover notification delivery"
    - "Cross-platform validation tests verify macOS/Windows/Linux consistency"
    - "Tests use temp directories and proper cleanup"
  artifacts:
    - path: frontend-nextjs/src-tauri/tests/integration/file_dialog_integration.rs
      provides: Integration tests for file dialog workflows
      min_lines: 100
    - path: frontend-nextjs/src-tauri/tests/integration/menu_bar_integration.rs
      provides: Integration tests for menu bar interactions
      min_lines: 80
    - path: frontend-nextjs/src-tauri/tests/integration/notification_integration.rs
      provides: Integration tests for notification system
      min_lines: 80
    - path: frontend-nextjs/src-tauri/tests/integration/cross_platform_validation.rs
      provides: Cross-platform consistency tests
      min_lines: 100
  key_links:
    - from: tests/integration/
      to: tests/commands_test.rs
      via: Existing test patterns
      pattern: std::env::temp_dir|fs::write|fs::remove
    - from: tests/integration/cross_platform_validation.rs
      to: cfg!(target_os)
      via: Platform-specific conditional compilation
      pattern: cfg!\(target_os
---

<objective>
Create Tauri integration tests for native APIs, menu bar, notifications, and cross-platform validation.

**Purpose**: Test Tauri integration with native platform APIs (file dialogs, system tray, notifications) and verify cross-platform consistency. These tests ensure desktop-specific features work correctly on macOS, Windows, and Linux.

**Output**: Four integration test files covering file dialogs, menu bar, notifications, and cross-platform validation with proper cleanup and platform-specific handling.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
</execution_context>

<context>
@frontend-nextjs/src-tauri/tests/commands_test.rs
@frontend-nextjs/src-tauri/tests/menu_bar_test.rs
@frontend-nextjs/src-tauri/src/main.rs
@.planning/phases/097-desktop-testing/097-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Create integration test module structure</name>
  <files>frontend-nextjs/src-tauri/tests/integration/mod.rs</files>
  <action>
    Create frontend-nextjs/src-tauri/tests/integration/ directory and mod.rs:

    1. Create directory: frontend-nextjs/src-tauri/tests/integration/
    2. Create frontend-nextjs/src-tauri/tests/integration/mod.rs with:
       - Module docstring explaining integration test purpose
       - Module declarations for each integration test file
       - Common setup/teardown helpers

    Module structure:
    ```rust
    //! Integration tests for Tauri desktop application
    //!
    //! Tests for native API integration, system tray, notifications,
    //! and cross-platform consistency.

    mod file_dialog_integration;
    mod menu_bar_integration;
    mod notification_integration;
    mod cross_platform_validation;
    ```
  </action>
  <verify>ls -la frontend-nextjs/src-tauri/tests/integration/</verify>
  <done>Integration test module structure created</done>
</task>

<task type="auto">
  <name>Create file dialog integration tests</name>
  <files>frontend-nextjs/src-tauri/tests/integration/file_dialog_integration.rs</files>
  <action>
    Create frontend-nextjs/src-tauri/tests/integration/file_dialog_integration.rs:

    Test file dialog workflows:
    1. **File read/write workflows** (2-3 tests):
       - Test reading file content with temp files
       - Test writing file content to temp directory
       - Test nested directory creation and file write

    2. **Directory listing workflows** (2-3 tests):
       - Test listing directory with files and subdirectories
       - Test listing non-existent directory error handling
       - Test listing file as directory error handling

    3. **File metadata workflows** (1-2 tests):
       - Test reading file metadata (size, modified time)
       - Test entry iteration and metadata extraction

    Use patterns from existing commands_test.rs but with integration-level assertions (workflow sequences, not just unit operations).
    Always use std::env::temp_dir() for test files, clean up in test cleanup or with defer-style patterns.
  </action>
  <verify>cd frontend-nextjs/src-tauri && cargo test --test file_dialog_integration</verify>
  <done>file_dialog_integration.rs with 5-8 integration tests</done>
</task>

<task type="auto">
  <name>Create menu bar integration tests</name>
  <files>frontend-nextjs/src-tauri/tests/integration/menu_bar_integration.rs</files>
  <action>
    Create frontend-nextjs/src-tauri/tests/integration/menu_bar_integration.rs:

    Test menu bar system tray interactions:
    1. **Menu item structure** (2-3 tests):
       - Test menu item count and labels
       - Test menu event handler IDs
       - Test menu item enable/disable states

    2. **Menu event workflows** (2-3 tests):
       - Test quit event handler registration
       - Test show window event handler
       - Test custom menu item actions

    3. **System tray integration** (1-2 tests):
       - Test tray icon visibility (if accessible without GUI)
       - Test tray menu structure

    Note: Some GUI-dependent tests may require platform-specific conditional compilation (#[cfg(target_os)]).
    Reference existing menu_bar_test.rs patterns and extend with integration-level workflows.
  </action>
  <verify>cd frontend-nextjs/src-tauri && cargo test --test menu_bar_integration</verify>
  <done>menu_bar_integration.rs with 5-8 integration tests</done>
</task>

<task type="auto">
  <name>Create notification integration tests</name>
  <files>frontend-nextjs/src-tauri/tests/integration/notification_integration.rs</files>
  <action>
    Create frontend-nextjs/src-tauri/tests/integration/notification_integration.rs:

    Test notification system integration:
    1. **Notification structure** (2-3 tests):
       - Test notification builder (title, body, icon, sound)
       - Test notification sound validation ("default" vs "none")
       - Test notification category/identifier

    2. **Notification delivery** (1-2 tests):
       - Test notification send command structure
       - Test notification permission handling (if accessible)

    3. **Notification scheduling** (1-2 tests):
       - Test scheduled notification timestamp validation
       - Test notification cancellation workflow

    Note: Actual GUI notification delivery may be manual verification. Test command logic and response structures.
    Reference main.rs send_notification function for implementation details.
  </action>
  <verify>cd frontend-nextjs/src-tauri && cargo test --test notification_integration</verify>
  <done>notification_integration.rs with 4-7 integration tests</done>
</task>

<task type="auto">
  <name>Create cross-platform validation tests</name>
  <files>frontend-nextjs/src-tauri/tests/integration/cross_platform_validation.rs</files>
  <action>
    Create frontend-nextjs/src-tauri/tests/integration/cross_platform_validation.rs:

    Test cross-platform consistency:
    1. **Platform detection** (1-2 tests):
       - Test platform detection (macOS/Windows/Linux)
       - Test architecture detection (x64/arm64)
       - Use cfg!(target_os) and cfg!(target_arch)

    2. **Path separator consistency** (2-3 tests):
       - Test PathBuf handles both forward and backward slashes
       - Test file name extraction across platforms
       - Test parent directory resolution

    3. **Temp directory access** (1-2 tests):
       - Test std::env::temp_dir() is writable on all platforms
       - Test temp directory cleanup

    4. **Platform-specific features** (2-3 tests with #[cfg]):
       - Test macOS-specific features with #[cfg(target_os = "macos")]
       - Test Windows-specific features with #[cfg(target_os = "windows")]
       - Test Linux-specific features with #[cfg(target_os = "linux")]

    Use conditional compilation to skip tests on irrelevant platforms.
  </action>
  <verify>cd frontend-nextjs/src-tauri && cargo test --test cross_platform_validation</verify>
  <done>cross_platform_validation.rs with 6-10 integration tests</done>
</task>

</tasks>

<verification>
After completion:
1. Run `cd frontend-nextjs/src-tauri && cargo test --test integration` - all integration tests pass
2. Verify each integration test file exists (4 files)
3. Check tests use std::env::temp_dir() for test files
4. Verify cleanup patterns (defer or manual cleanup)
5. Run cargo test --all to ensure no regressions
</verification>

<success_criteria>
1. 4 integration test files created (file_dialog, menu_bar, notification, cross_platform)
2. 20-33 total integration tests across all files
3. All tests use temp directories with proper cleanup
4. Cross-platform tests use cfg!(target_os) for platform-specific logic
5. Tests follow existing patterns from commands_test.rs
6. No GUI-dependent tests that block CI execution
</success_criteria>

<output>
After completion, create `.planning/phases/097-desktop-testing/097-05-SUMMARY.md` with:
- Integration test breakdown by file
- Platform-specific test handling notes
- Cross-platform validation coverage
- Total test count and pass rate
</output>
