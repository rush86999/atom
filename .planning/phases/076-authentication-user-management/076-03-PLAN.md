---
phase: 076-authentication-user-management
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/e2e_ui/tests/test_auth_logout.py
autonomous: true

must_haves:
  truths:
    - "User can log out via dashboard user menu"
    - "Session is cleared after logout (JWT token removed)"
    - "User is redirected to login page after logout"
    - "User cannot access protected routes after logout"
  artifacts:
    - path: "backend/tests/e2e_ui/tests/test_auth_logout.py"
      provides: "Logout flow E2E tests"
      min_lines: 100
      exports: ["test_logout_via_user_menu", "test_logout_clears_session", "test_logout_redirects_to_login", "test_logout_blocks_protected_access"]
  key_links:
    - from: "backend/tests/e2e_ui/tests/test_auth_logout.py"
      to: "backend/tests/e2e_ui/pages/page_objects.py"
      via: "DashboardPage.logout() method"
      pattern: "dashboard\\.logout\\(\\)"
    - from: "backend/tests/e2e_ui/tests/test_auth_logout.py"
      to: "backend/tests/e2e_ui/fixtures/auth_fixtures.py"
      via: "authenticated_page fixture"
      pattern: "def test.*\\(authenticated_page"
---

<objective>
Test user logout flow via dashboard user menu with session cleanup verification.

**Purpose**: Validate complete logout workflow including menu interaction, session cleanup (JWT token removal), redirect to login page, and blocking of protected route access. Ensures users can securely terminate sessions.

**Output**: E2E test file `test_auth_logout.py` with 4 test cases covering logout scenarios.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/tests/e2e_ui/pages/page_objects.py
@backend/tests/e2e_ui/fixtures/auth_fixtures.py

Reference: DashboardPage has logout() method that clicks user profile then logout button.
</context>

<tasks>

<task type="auto">
  <name>Create logout flow E2E tests</name>
  <files>backend/tests/e2e_ui/tests/test_auth_logout.py</files>
  <action>
Create `backend/tests/e2e_ui/tests/test_auth_logout.py` with the following tests:

1. **test_logout_via_user_menu(authenticated_page)**
   - Navigate to dashboard (authenticated)
   - Verify dashboard is loaded (is_loaded() returns True)
   - Click user profile button (user_profile_button)
   - Verify logout button is visible
   - Click logout button
   - Verify redirect to login page
   - Verify login form is displayed

2. **test_logout_clears_session(authenticated_page)**
   - Navigate to dashboard
   - Verify JWT token exists in localStorage
   - Execute logout via DashboardPage.logout()
   - Check localStorage for auth_token
   - Verify token is None or empty string
   - Verify next-auth.session-token is also cleared

3. **test_logout_blocks_protected_access(authenticated_page)**
   - Navigate to dashboard
   - Execute logout
   - Attempt to navigate to /settings directly
   - Verify redirect to login page
   - Attempt to navigate to /projects directly
   - Verify redirect to login page

4. **test_logout_and_relogin_works(authenticated_page, db_session)**
   - Navigate to dashboard (authenticated)
   - Logout
   - Verify on login page
   - Login again using LoginPage with same credentials
   - Verify redirect to dashboard
   - Verify token is set again

**Use existing fixtures and Page Objects:**
- Import: `from tests.e2e_ui.pages.page_objects import LoginPage, DashboardPage`
- Import: `from tests.e2e_ui.fixtures.auth_fixtures import authenticated_page, test_user`
- Use DashboardPage.logout() for complete logout flow
- Use DashboardPage.user_profile_button and DashboardPage.logout_button for step-by-step

**Test pattern:**
```python
def test_logout_via_user_menu(authenticated_page):
    # Setup: Start on authenticated dashboard
    dashboard = DashboardPage(authenticated_page)
    dashboard.navigate()
    assert dashboard.is_loaded()

    # Action: Logout
    dashboard.logout()

    # Verify: Redirected to login
    login = LoginPage(authenticated_page)
    assert login.is_loaded()
```

**Token verification helper:**
```python
def get_auth_token(page):
    return page.evaluate("() => localStorage.getItem('auth_token')")

def assert_no_token(page):
    token = get_auth_token(page)
    assert token is None or token == ""
```

**DO NOT:**
- Create new fixtures (use authenticated_page)
- Modify logout flow (test existing behavior)
- Create new Page Objects (use DashboardPage.logout())
  </action>
  <verify>
Run: `pytest backend/tests/e2e_ui/tests/test_auth_logout.py -v`

Expected: 4 tests pass, logout flow verified
  </verify>
  <done>
User can log out, session cleared, redirect to login, protected routes blocked
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
- All tests use authenticated_page fixture
- DashboardPage.logout() method used for logout action
- Token removal verified via localStorage
- Redirect to login verified after logout
- Protected routes blocked after logout
- Re-login after logout works
</verification>

<success_criteria>
- 4 test functions created in test_auth_logout.py
- Logout via user menu works
- JWT token cleared from localStorage
- User redirected to login page
- Protected routes inaccessible after logout
- Re-login flow validated
</success_criteria>

<output>
After completion, create `.planning/phases/076-authentication-user-management/076-03-SUMMARY.md`
</output>
