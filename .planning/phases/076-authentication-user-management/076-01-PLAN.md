---
phase: 076-authentication-user-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/e2e_ui/tests/test_auth_login.py
autonomous: true

must_haves:
  truths:
    - "User can log in via email and password through the login form"
    - "User sees error message when providing invalid credentials"
    - "User is redirected to dashboard after successful login"
    - "Login form validation works for empty fields"
    - "Remember me checkbox persists session across browser restart"
  artifacts:
    - path: "backend/tests/e2e_ui/tests/test_auth_login.py"
      provides: "Login flow E2E tests"
      min_lines: 150
      exports: ["test_login_with_valid_credentials", "test_login_with_invalid_credentials", "test_login_with_empty_fields", "test_remember_me_persists_session"]
  key_links:
    - from: "backend/tests/e2e_ui/tests/test_auth_login.py"
      to: "backend/tests/e2e_ui/pages/page_objects.py"
      via: "LoginPage import"
      pattern: "from.*page_objects import LoginPage"
    - from: "backend/tests/e2e_ui/tests/test_auth_login.py"
      to: "backend/tests/e2e_ui/fixtures/auth_fixtures.py"
      via: "test_user fixture"
      pattern: "def test.*\\(test_user"
---

<objective>
Test user authentication flow via UI login form with happy path and error cases.

**Purpose**: Validate the complete login workflow including successful authentication, credential validation, error handling, and remember me functionality. Ensures users can securely authenticate and access protected routes.

**Output**: E2E test file `test_auth_login.py` with 5-6 test cases covering login scenarios.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/tests/e2e_ui/pages/page_objects.py
@backend/tests/e2e_ui/fixtures/auth_fixtures.py
@backend/tests/e2e_ui/tests/test_smoke.py

Reference pattern: test_smoke.py demonstrates fixture usage and Page Object interaction.
</context>

<tasks>

<task type="auto">
  <name>Create login flow E2E tests</name>
  <files>backend/tests/e2e_ui/tests/test_auth_login.py</files>
  <action>
Create `backend/tests/e2e_ui/tests/test_auth_login.py` with the following tests:

1. **test_login_with_valid_credentials(browser, db_session)**
   - Create a test user in database with known email/password
   - Navigate to login page (http://localhost:3001/login)
   - Use LoginPage.fill_email() and LoginPage.fill_password()
   - Click submit button
   - Verify redirect to dashboard (url contains /dashboard)
   - Verify JWT token is set in localStorage

2. **test_login_with_invalid_credentials(browser)**
   - Navigate to login page
   - Fill email with invalid address
   - Fill password with wrong password
   - Click submit button
   - Verify error message is displayed (get_error_text() not None)
   - Verify still on login page (not redirected)

3. **test_login_with_empty_fields(browser)**
   - Navigate to login page
   - Click submit without filling fields
   - Verify HTML5 validation or error message
   - Verify form does not submit

4. **test_remember_me_persists_session(browser, db_session)**
   - Create test user
   - Navigate to login page
   - Fill credentials
   - Check remember_me checkbox
   - Submit login
   - Verify token is set
   - Close browser context
   - Create new context and navigate to dashboard
   - Verify still authenticated (token persists)

5. **test_login_redirects_protected_route(browser, db_session)**
   - Create test user
   - Navigate directly to dashboard (not login page first)
   - Verify redirect to login page
   - Login with credentials
   - Verify redirect back to dashboard

**Use existing fixtures and Page Objects:**
- Import: `from tests.e2e_ui.pages.page_objects import LoginPage, DashboardPage`
- Import: `from tests.e2e_ui.fixtures.auth_fixtures import test_user`
- Use `db_session` fixture for database access
- Use `browser` fixture from pytest-playwright

**Test pattern:**
```python
def test_login_with_valid_credentials(browser, db_session):
    # Setup: Create test user
    user = create_test_user(db_session, "test@example.com", "password123")

    # Action: Login via UI
    login_page = LoginPage(browser.new_page())
    login_page.navigate()
    login_page.login("test@example.com", "password123")

    # Verify: Dashboard loaded
    assert "dashboard" in browser.url.lower()
```

**DO NOT:**
- Create new fixtures (use existing ones)
- Modify Page Objects (use existing LoginPage)
- Create new pages (write tests only)
  </action>
  <verify>
Run: `pytest backend/tests/e2e_ui/tests/test_auth_login.py -v`

Expected: 5 tests pass, all login scenarios validated
  </verify>
  <done>
User can log in via email/password, errors shown for invalid credentials, remember me persists session
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
- All tests use existing fixtures (test_user, db_session, browser)
- LoginPage Page Object used for all interactions
- Error cases tested (invalid credentials, empty fields)
- Happy path tested (valid credentials, redirect to dashboard)
- Remember me functionality verified
</verification>

<success_criteria>
- 5 test functions created in test_auth_login.py
- All tests use LoginPage Page Object methods
- Valid credentials login redirects to dashboard
- Invalid credentials show error message
- Remember me checkbox persists session
- Tests run without modifying fixtures or Page Objects
</success_criteria>

<output>
After completion, create `.planning/phases/076-authentication-user-management/076-01-SUMMARY.md`
</output>
