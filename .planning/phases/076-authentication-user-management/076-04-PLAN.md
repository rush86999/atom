---
phase: 076-authentication-user-management
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/e2e_ui/tests/test_settings_page.py
autonomous: true

must_haves:
  truths:
    - "User can access settings page when authenticated"
    - "User can update theme preference (light/dark mode)"
    - "User can toggle notification preferences"
    - "Settings are saved and persist across page refresh"
  artifacts:
    - path: "backend/tests/e2e_ui/tests/test_settings_page.py"
      provides: "Settings page E2E tests"
      min_lines: 130
      exports: ["test_access_settings_page", "test_update_theme_preference", "test_toggle_notifications", "test_settings_persist_across_refresh"]
  key_links:
    - from: "backend/tests/e2e_ui/tests/test_settings_page.py"
      to: "backend/tests/e2e_ui/pages/page_objects.py"
      via: "SettingsPage import"
      pattern: "from.*page_objects import SettingsPage"
    - from: "backend/tests/e2e_ui/tests/test_settings_page.py"
      to: "backend/tests/e2e_ui/fixtures/auth_fixtures.py"
      via: "authenticated_page fixture"
      pattern: "def test.*\\(authenticated_page"
---

<objective>
Test settings page access and preference updates for theme and notifications.

**Purpose**: Validate that authenticated users can access the settings page, modify theme and notification preferences, and have changes persist across page refreshes. Ensures user settings work correctly.

**Output**: E2E test file `test_settings_page.py` with 4-5 test cases covering settings scenarios.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/tests/e2e_ui/pages/page_objects.py
@backend/tests/e2e_ui/fixtures/auth_fixtures.py

Reference: SettingsPage has theme_toggle, notification checkboxes, and save_button methods.
</context>

<tasks>

<task type="auto">
  <name>Create settings page E2E tests</name>
  <files>backend/tests/e2e_ui/tests/test_settings_page.py</files>
  <action>
Create `backend/tests/e2e_ui/tests/test_settings_page.py` with the following tests:

1. **test_access_settings_page(authenticated_page)**
   - Navigate to /settings
   - Verify settings page loads (theme_toggle is visible)
   - Verify all sections are present:
     - Theme toggle section
     - Notifications section
     - Account section
   - Verify save button is visible

2. **test_update_theme_preference(authenticated_page)**
   - Navigate to settings
   - Get current theme (get_current_theme())
   - Toggle theme using toggle_theme()
   - Click save button
   - Wait for success message
   - Verify theme changed (get_current_theme() different from initial)
   - Reload page
   - Verify theme persists (still the changed theme)

3. **test_toggle_notifications(authenticated_page)**
   - Navigate to settings
   - Get initial email notification state
   - Toggle email notifications using set_email_notifications(True/False)
   - Toggle push notifications using set_push_notifications(True/False)
   - Click save button
   - Verify success message displayed
   - Verify checkbox states reflect changes

4. **test_settings_persist_across_refresh(authenticated_page)**
   - Navigate to settings
   - Change theme to dark
   - Enable email notifications
   - Save settings
   - Reload page
   - Verify theme is still dark
   - Verify email notifications still enabled

5. **test_unauthenticated_cannot_access_settings(browser)**
   - Create new page context (no auth token)
   - Navigate to /settings
   - Verify redirect to login page
   - Verify settings page not accessible

**Use existing fixtures and Page Objects:**
- Import: `from tests.e2e_ui.pages.page_objects import SettingsPage, LoginPage`
- Import: `from tests.e2e_ui.fixtures.auth_fixtures import authenticated_page`
- Use SettingsPage methods: get_current_theme(), toggle_theme(), set_email_notifications(), click_save()

**Test pattern:**
```python
def test_update_theme_preference(authenticated_page):
    # Setup: Navigate to settings
    settings = SettingsPage(authenticated_page)
    settings.navigate()

    # Get initial state
    initial_theme = settings.get_current_theme()

    # Action: Toggle theme
    settings.toggle_theme()
    settings.click_save()

    # Verify: Theme changed
    new_theme = settings.get_current_theme()
    assert new_theme != initial_theme
```

**Success message verification:**
```python
def verify_save_success(settings):
    settings.click_save()
    message = settings.get_success_message()
    assert message is not None
    assert "saved" in message.lower()
```

**DO NOT:**
- Create new fixtures (use authenticated_page)
- Modify settings UI (test existing components only)
- Create new settings options (test existing theme/notification only)
  </action>
  <verify>
Run: `pytest backend/tests/e2e_ui/tests/test_settings_page.py -v`

Expected: 5 tests pass, settings page functionality verified
  </verify>
  <done>
User can access settings, change theme, toggle notifications, settings persist
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
- All tests use authenticated_page fixture (except unauth test)
- SettingsPage Page Object used for all interactions
- Theme toggle tested with persistence
- Notification toggles tested with save
- Settings persist across page refresh
- Unauthenticated access blocked
</verification>

<success_criteria>
- 5 test functions created in test_settings_page.py
- Settings page accessible when authenticated
- Theme preference can be changed and saved
- Notification preferences can be toggled
- Settings persist across page reload
- Unauthenticated users redirected to login
</success_criteria>

<output>
After completion, create `.planning/phases/076-authentication-user-management/076-04-SUMMARY.md`
</output>
