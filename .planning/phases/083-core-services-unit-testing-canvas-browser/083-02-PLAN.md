---
phase: 083-core-services-unit-testing-canvas-browser
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_browser_tool.py
  - backend/tests/unit/test_browser_tool_governance.py
autonomous: true

must_haves:
  truths:
    - Browser automation tool tests cover CDP integration (Playwright session management)
    - Governance enforcement tested for browser operations (INTERN+ maturity required)
    - Page navigation, element interaction, screenshot capture all tested
    - Form filling, text extraction, script execution tested
    - Error handling tested for session failures, navigation errors, element not found
    - Session lifecycle (create, navigate, interact, close) fully tested
  artifacts:
    - path: backend/tests/unit/test_browser_tool.py
      provides: Browser automation unit tests with CDP integration
      min_lines: 1500
    - path: backend/tests/unit/test_browser_tool_governance.py
      provides: Governance tests for browser operations (INTERN+ required)
      min_lines: 500
  key_links:
    - from: test_browser_tool_governance.py
      to: core/agent_governance_service.py
      via: governance.can_perform_action("browser_navigate")
      pattern: governance\.can_perform_action.*browser_navigate
    - from: test_browser_tool_governance.py
      to: tools/browser_tool.py
      via: browser_create_session with governance checks
      pattern: browser_create_session.*governance
    - from: test_browser_tool.py
      to: tools/browser_tool.py
      via: BrowserSession.start() and BrowserSession.close()
      pattern: (await session\.start|await session\.close)
---

<objective>
Create comprehensive unit tests for browser automation tool functions covering CDP integration, session management, page navigation, element interaction, screenshots, form filling, and governance enforcement to achieve 90%+ coverage for browser_tool.py (currently at 9.92%).

Purpose: Browser automation is a critical feature with security implications (INTERN+ governance required) and external dependencies (Playwright CDP). Current tests (9.92%) do not cover governance paths, high-level browser functions (browser_create_session, browser_navigate, etc.), or error handling.

Output: 20+ new tests in test_browser_tool.py and new test_browser_tool_governance.py with 90%+ coverage target
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
@.planning/ROADMAP.md
@.planning/PROJECT.md

# Phase 82 patterns to follow
@.planning/phases/082-core-services-unit-testing-governance-episodes/082-01-SUMMARY.md
</execution_context>

<context>
@backend/tools/browser_tool.py
@backend/tests/unit/test_browser_tool.py
@backend/core/models.py (AgentExecution, AgentStatus)
@backend/core/agent_governance_service.py (governance patterns)

# Phase 083-01 canvas tool patterns for consistency:
- Governance tests in separate file (test_canvas_tool_governance.py)
- AsyncMock for all async methods
- FeatureFlags mocking for governance bypass scenarios
- AgentContextResolver mocking for agent resolution
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add browser session creation and governance tests</name>
  <files>backend/tests/unit/test_browser_tool_governance.py</files>
  <action>
Create new test file test_browser_tool_governance.py with governance-focused test classes:

1. TestBrowserCreateSessionGovernance class (10 tests):
   - STUDENT agent blocked from browser_create_session (INTERN+ required)
   - INTERN agent allowed for browser_create_session
   - SUPERVISED agent allowed for browser_create_session
   - AUTONOMOUS agent allowed for browser_create_session
   - Governance check uses correct action_type "browser_navigate"
   - Agent execution record created on successful session creation
   - Agent execution marked failed on governance block
   - Session creation returns session_id on success
   - Session creation failure includes governance reason in error
   - Outcome recorded for successful session creation
   - Outcome recorded for failed session creation

2. TestBrowserSessionUserValidation class (6 tests):
   - Session creation stores user_id correctly
   - Navigation validates user_id matches session user
   - Screenshot validates user_id matches session user
   - Form fill validates user_id matches session user
   - Click validates user_id matches session user
   - Cross-user session access returns error

3. TestBrowserSessionTimeout class (6 tests):
   - Session manager tracks last_used timestamp
   - Expired session (30+ minutes inactive) is cleaned up
   - Active session (<30 minutes) is not cleaned up
   - Session timeout configurable via session_timeout_minutes
   - Multiple expired sessions cleaned in single call
   - Cleanup returns count of expired sessions removed

Mock pattern:
- Mock AgentContextResolver.resolve_agent_for_request to return test agent
- Mock governance.can_perform_action for permission checks
- Mock get_db_session as context manager
- Mock ServiceFactory.get_governance_service
- Mock async_playwright for Playwright instances
- Mock ws_manager.broadcast if needed

Test file size target: 500+ lines
  </action>
  <verify>
pytest backend/tests/unit/test_browser_tool_governance.py -v
pytest backend/tests/unit/test_browser_tool_governance.py -k "TestBrowserCreateSessionGovernance" -v
  </verify>
  <done>
22 new governance tests added across 3 test classes (10 + 6 + 6)
Test file created with 500+ lines
All browser governance paths tested
  </done>
</task>

<task type="auto">
  <name>Task 2: Add browser navigation and interaction tests</name>
  <files>backend/tests/unit/test_browser_tool.py</files>
  <action>
Add new test classes to existing test_browser_tool.py after existing TestBrowserSessionLifecycle class:

1. TestBrowserNavigation class (10 tests):
   - browser_navigate successfully loads URL
   - browser_navigate returns page title
   - browser_navigate returns final URL (after redirects)
   - browser_navigate returns HTTP status
   - browser_navigate with wait_until="domcontentloaded"
   - browser_navigate with wait_until="networkidle"
   - browser_navigate handles invalid session_id
   - browser_navigate handles user_id mismatch
   - browser_navigate handles navigation timeout
   - browser_navigate updates session.last_used timestamp

2. TestBrowserScreenshots class (8 tests):
   - browser_screenshot returns base64 data by default
   - browser_screenshot saves to file when path provided
   - browser_screenshot with full_page=True captures entire page
   - browser_screenshot with full_page=False captures viewport only
   - browser_screenshot handles invalid session_id
   - browser_screenshot handles user_id mismatch
   - browser_screenshot returns PNG format
   - browser_screenshot returns size_bytes
   - browser_screenshot updates session.last_used timestamp

3. TestBrowserInteraction class (10 tests):
   - browser_click successfully clicks element
   - browser_click waits for element visibility
   - browser_click waits for post-click selector if specified
   - browser_click handles element not found
   - browser_click handles timeout
   - browser_click updates session.last_used timestamp
   - browser_fill_form fills multiple fields
   - browser_fill_form handles INPUT fields
   - browser_fill_form handles TEXTAREA fields
   - browser_fill_form handles SELECT dropdowns
   - browser_fill_form submits form when submit=True
   - browser_fill_form handles unsupported element types gracefully
   - browser_fill_form returns count of fields filled

4. TestBrowserDataExtraction class (8 tests):
   - browser_extract_text extracts full page text without selector
   - browser_extract_text extracts from specific elements with selector
   - browser_extract_text handles multiple matching elements
   - browser_extract_text returns text length
   - browser_get_page_info returns page title
   - browser_get_page_info returns current URL
   - browser_get_page_info returns cookies count
   - browser_execute_script executes JavaScript and returns result
   - browser_execute_script handles invalid session_id

Add tests after existing TestBrowserSessionLifecycle class.
Target: 1000+ new lines of test code
  </action>
  <verify>
pytest backend/tests/unit/test_browser_tool.py -v
pytest backend/tests/unit/test_browser_tool.py -k "TestBrowserNavigation or TestBrowserScreenshots or TestBrowserInteraction or TestBrowserDataExtraction" -v
  </verify>
  <done>
36 new tests added across 4 test classes (10 + 8 + 10 + 8)
test_browser_tool.py expanded by 1000+ lines
All browser navigation and interaction paths covered
  </done>
</task>

<task type="auto">
  <name>Task 3: Add browser session management and error handling tests</name>
  <files>backend/tests/unit/test_browser_tool.py</files>
  <action>
Add new test classes to test_browser_tool.py after TestBrowserDataExtraction class:

1. TestBrowserSessionManager class (10 tests):
   - get_session returns existing session by ID
   - get_session returns None for non-existent session
   - create_session generates unique session_id (UUID v4)
   - create_session stores session in sessions dict
   - create_session starts the browser session
   - close_session removes session from sessions dict
   - close_session calls session.close() method
   - close_session returns False for non-existent session
   - cleanup_expired_sessions removes timed-out sessions
   - Session timeout default is 30 minutes

2. TestBrowserCloseSession class (8 tests):
   - browser_close_session closes active session
   - browser_close_session removes session from manager
   - browser_close_session handles non-existent session
   - browser_close_session validates user_id
   - browser_close_session handles user_id mismatch
   - browser_close_session returns success=True on close
   - browser_close_session returns error when session not found
   - Multiple sessions can be closed independently

3. TestBrowserErrorHandling class (10 tests):
   - browser_navigate handles network errors
   - browser_navigate handles invalid URL format
   - browser_screenshot handles page screenshot failure
   - browser_click handles element click failure
   - browser_fill_form handles field fill failure
   - browser_fill_form handles submit button not found
   - browser_extract_text handles page read failure
   - browser_execute_script handles JavaScript syntax error
   - browser_create_session handles Playwright launch failure
   - browser_close_session handles session close failure
   - All errors return success=False with error message
   - Error paths log appropriate error messages

4. TestBrowserTypeSupport class (6 tests):
   - Chromium browser type launches correctly
   - Firefox browser type launches correctly
   - WebKit browser type launches correctly
   - Default browser type is chromium
   - Invalid browser type handled gracefully
   - Browser type passed to session.start() correctly

Add after TestBrowserDataExtraction class.
Target: 800+ new lines of test code
  </action>
  <verify>
pytest backend/tests/unit/test_browser_tool.py -v
pytest backend/tests/unit/test_browser_tool.py -k "TestBrowserSessionManager or TestBrowserCloseSession or TestBrowserErrorHandling or TestBrowserTypeSupport" -v
  </verify>
  <done>
34 new tests added across 4 test classes (10 + 8 + 10 + 6)
test_browser_tool.py expanded by 800+ lines
All session management and error paths covered
  </done>
</task>

</tasks>

<verification>
1. Run all browser tool tests: pytest backend/tests/unit/test_browser_tool.py -v
2. Run governance tests: pytest backend/tests/unit/test_browser_tool_governance.py -v
3. Verify test count: 60+ new tests added (22 + 36 + 34 = 92 tests)
4. Verify coverage: Run coverage report for browser_tool.py, target 90%+
5. Verify no regressions: All existing tests still pass
6. Verify governance paths: All maturity levels tested for browser operations
7. Verify session lifecycle: Create -> Navigate -> Interact -> Close fully tested
</verification>

<success_criteria>
1. 90%+ coverage achieved for browser_tool.py (up from 9.92%)
2. All CDP operations tested (navigation, screenshot, fill, click, extract)
3. Governance enforcement tested at all maturity boundaries (INTERN+ required)
4. Session management fully tested (create, get, close, cleanup)
5. User validation tested for all session operations
6. Error handling tested for all failure modes
7. Test files: test_browser_tool.py (1500+ lines), test_browser_tool_governance.py (500+ lines)
8. 92 new tests added across 10 test classes
</success_criteria>

<output>
After completion, create `.planning/phases/083-core-services-unit-testing-canvas-browser/083-02-SUMMARY.md`
</output>
