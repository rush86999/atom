---
phase: 083-core-services-unit-testing-canvas-browser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_canvas_tool.py
autonomous: true

must_haves:
  truths:
    - Canvas tool tests cover all presentation types (chart, markdown, form, status_panel, specialized)
    - Governance enforcement tested for all maturity levels (STUDENT blocked, INTERN approval, SUPERVISED supervision)
    - State management (canvas update, close) tested with session isolation
    - JavaScript execution security validated (AUTONOMOUS only, dangerous patterns blocked)
    - Audit trail creation verified for all canvas actions
    - Error handling tested for governance blocks, WebSocket failures, invalid inputs
  artifacts:
    - path: backend/tests/unit/test_canvas_tool.py
      provides: Comprehensive canvas tool unit tests
      min_lines: 1800
    - path: backend/tests/unit/test_canvas_tool_governance.py
      provides: Governance enforcement tests for canvas operations
      min_lines: 600
  key_links:
    - from: test_canvas_tool_governance.py
      to: core/agent_governance_service.py
      via: governance.can_perform_action() calls
      pattern: governance\.can_perform_action
    - from: test_canvas_tool_governance.py
      to: tools/canvas_tool.py
      via: FeatureFlags.should_enforce_governance('canvas')
      pattern: FeatureFlags\.should_enforce_governance
---

<objective>
Create comprehensive unit tests for canvas tool functions covering presentation types, governance enforcement, state management, JavaScript execution security, and error handling to achieve 90%+ coverage for canvas_tool.py (currently at 40.4%).

Purpose: Canvas presentation is a core user interaction mechanism with security implications (JavaScript execution) and governance requirements (agent maturity checks). Current tests (40.4%) do not cover governance enforcement paths, specialized canvas types, or error handling comprehensively.

Output: 25+ new tests in test_canvas_tool.py and new test_canvas_tool_governance.py with 90%+ coverage target
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
@.planning/ROADMAP.md
@.planning/PROJECT.md

# Phase 82 patterns to follow
@.planning/phases/082-core-services-unit-testing-governance-episodes/082-01-SUMMARY.md
</execution_context>

<context>
@backend/tools/canvas_tool.py
@backend/tests/unit/test_canvas_tool.py
@backend/core/models.py (AgentStatus, AgentExecution, CanvasAudit)
@backend/core/agent_governance_service.py (governance patterns)

# Phase 82 testing patterns (for consistency):
- AsyncMock for async method testing
- Mock query side_effect pattern for multiple DB calls
- Governance check tests verify both allowed and blocked paths
- Test organization: New test classes added after related existing classes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add governance enforcement tests for canvas presentations</name>
  <files>backend/tests/unit/test_canvas_tool_governance.py</files>
  <action>
Create new test file test_canvas_tool_governance.py with governance-focused test classes:

1. TestPresentChartGovernance class (8 tests):
   - STUDENT agent blocked from present_chart (governance enforcement)
   - INTERN agent allowed for present_chart
   - SUPERVISED agent allowed for present_chart
   - AUTONOMOUS agent allowed for present_chart
   - Agent execution record created on success
   - Agent execution marked failed on governance block
   - Outcome recording for successful chart presentation
   - Outcome recording for failed chart presentation

2. TestPresentFormGovernance class (8 tests):
   - STUDENT agent blocked from present_form (INTERN+ required)
   - INTERN agent allowed for present_form
   - SUPERVISED agent allowed for present_form
   - AUTONOMOUS agent allowed for present_form
   - Agent execution record created for form presentation
   - Agent execution marked failed on governance block
   - Outcome recording for successful form presentation
   - Canvas audit entry created with form metadata

3. TestPresentMarkdownGovernance class (6 tests):
   - STUDENT agent blocked from present_markdown
   - INTERN agent allowed for present_markdown
   - SUPERVISED agent allowed for present_markdown
   - AUTONOMOUS agent allowed for present_markdown
   - Agent execution record created for markdown
   - Canvas audit entry created with content length

4. TestUpdateCanvasGovernance class (6 tests):
   - STUDENT agent blocked from update_canvas (INTERN+ required)
   - INTERN agent allowed for update_canvas
   - SUPERVISED agent allowed for update_canvas
   - AUTONOMOUS agent allowed for update_canvas
   - Agent execution record created for canvas update
   - Audit entry created with update metadata

Mock pattern from Phase 082-01:
- Mock AgentContextResolver.resolve_agent_for_request to return agent with specific status
- Mock governance.can_perform_action to return {"allowed": True/False, "reason": "..."}
- Mock get_db_session as context manager yielding mock db session
- Mock ws_manager.broadcast as AsyncMock for WebSocket calls
- Mock FeatureFlags.should_enforce_governance('canvas') to return True

Test file size target: 600+ lines (6 classes x ~100 lines each)
  </action>
  <verify>
pytest backend/tests/unit/test_canvas_tool_governance.py -v
pytest backend/tests/unit/test_canvas_tool_governance.py -k "TestPresentChartGovernance or TestPresentFormGovernance" -v
  </verify>
  <done>
28 new governance tests added across 4 test classes (8 + 8 + 6 + 6)
Test file created with 600+ lines
All tests passing with proper governance enforcement mocking
  </done>
</task>

<task type="auto">
  <name>Task 2: Add specialized canvas and JavaScript execution tests</name>
  <files>backend/tests/unit/test_canvas_tool.py</files>
  <action>
Add new test classes to existing test_canvas_tool.py after TestPresentMarkdown class:

1. TestPresentSpecializedCanvas class (10 tests):
   - Present docs canvas with rich_editor component
   - Present email canvas with thread_view component
   - Present sheets canvas with data_grid component
   - Present orchestration canvas with kanban component
   - Present terminal canvas with output component
   - Present coding canvas with diff_view component
   - Invalid canvas type returns error
   - Invalid component type for canvas returns error
   - Invalid layout for canvas type returns error
   - Governance enforcement for specialized canvases (INTERN+)
   - Maturity level validation for canvas types
   - Agent execution record created for specialized canvas
   - Canvas audit entry created with canvas_type metadata

2. TestCanvasExecuteJavascript class (12 tests):
   - AUTONOMOUS agent can execute JavaScript
   - SUPERVISED agent blocked from JavaScript execution
   - INTERN agent blocked from JavaScript execution
   - STUDENT agent blocked from JavaScript execution
   - No agent_id returns error (security requirement)
   - Empty JavaScript code returns error
   - Dangerous pattern 'eval(' blocked
   - Dangerous pattern 'Function(' blocked
   - Dangerous pattern 'document.cookie' blocked
   - Dangerous pattern 'localStorage.' blocked
   - Dangerous pattern 'window.location' blocked
   - JavaScript execution creates audit entry with code
   - Agent execution record created for JavaScript
   - Successful execution returns canvas_id and javascript_length

3. TestCanvasStateManagement class (8 tests):
   - Update canvas with new data
   - Update canvas with new title
   - Update canvas with multiple fields
   - Update canvas with session isolation
   - Close canvas without session_id
   - Close canvas with session_id
   - Canvas update creates audit entry
   - Canvas close sends WebSocket message

4. TestCanvasErrorHandling class (10 tests):
   - present_chart handles WebSocket broadcast failure
   - present_markdown handles database commit failure
   - present_form handles agent resolution failure
   - update_canvas handles invalid canvas_id
   - canvas_execute_javascript handles timeout
   - present_specialized_canvas handles registry validation error
   - Audit creation failure logged but does not crash
   - Agent execution failure recorded correctly
   - Multiple error paths create proper audit trail
   - Feature flags disabled bypasses governance gracefully

Add tests after existing TestPresentMarkdown class in test_canvas_tool.py.
Target: 1000+ new lines of test code
  </action>
  <verify>
pytest backend/tests/unit/test_canvas_tool.py -v
pytest backend/tests/unit/test_canvas_tool.py -k "TestPresentSpecializedCanvas or TestCanvasExecuteJavascript or TestCanvasStateManagement" -v
  </verify>
  <done>
40 new tests added across 4 test classes (10 + 12 + 8 + 10)
test_canvas_tool.py expanded by 1000+ lines
All specialized canvas types covered
JavaScript execution security tests complete
State management and error handling covered
  </done>
</task>

<task type="auto">
  <name>Task 3: Add canvas audit and present_to_canvas wrapper tests</name>
  <files>backend/tests/unit/test_canvas_tool.py</files>
  <action>
Add new test classes to test_canvas_tool.py after TestCanvasErrorHandling class:

1. TestCanvasAuditEntry class (10 tests):
   - _create_canvas_audit with all parameters
   - _create_canvas_audit with minimal parameters
   - _create_canvas_audit includes canvas_type
   - _create_canvas_audit includes component_type
   - _create_canvas_audit includes session_id
   - _create_canvas_audit handles metadata dict
   - _create_canvas_audit handles governance_check_passed flag
   - Audit entry receives unique UUID
   - Database add/commit/refresh called in order
   - Exception returns None and logs error

2. TestPresentToCanvasWrapper class (10 tests):
   - present_to_canvas routes to chart type correctly
   - present_to_canvas routes to form type correctly
   - present_to_canvas routes to markdown type correctly
   - present_to_canvas routes to status_panel type correctly
   - present_to_canvas routes to docs specialized canvas
   - present_to_canvas routes to email specialized canvas
   - present_to_canvas routes to sheets specialized canvas
   - present_to_canvas returns error for unknown canvas_type
   - present_to_canvas passes session_id through to specialized functions
   - present_to_canvas passes agent_id through to specialized functions
   - present_to_canvas handles exceptions from specialized functions
   - present_to_canvas returns success=False on error with error message

3. TestStatusPanelPresentation class (6 tests):
   - present_status_panel with items list
   - present_status_panel with title
   - present_status_panel with session_id
   - present_status_panel with governance enabled
   - present_status_panel governance block for STUDENT
   - Status panel sends correct WebSocket message format
   - Status panel creates audit entry if governance enabled

Add after TestCanvasErrorHandling class.
Target: 600+ new lines of test code
  </action>
  <verify>
pytest backend/tests/unit/test_canvas_tool.py -v
pytest backend/tests/unit/test_canvas_tool.py -k "TestCanvasAuditEntry or TestPresentToCanvasWrapper or TestStatusPanelPresentation" -v
  </verify>
  <done>
26 new tests added across 3 test classes (10 + 10 + 6)
test_canvas_tool.py expanded by 600+ lines
Audit entry creation fully covered
present_to_canvas wrapper tested for all routes
Status panel presentation tested
  </done>
</task>

</tasks>

<verification>
1. Run all canvas tool tests: pytest backend/tests/unit/test_canvas_tool.py -v
2. Run governance tests: pytest backend/tests/unit/test_canvas_tool_governance.py -v
3. Verify test count: 60+ new tests added (28 + 40 + 26 = 94 tests)
4. Verify coverage: Run coverage report for canvas_tool.py, target 90%+
5. Verify no regressions: All existing tests still pass
6. Verify governance paths: All maturity levels (STUDENT, INTERN, SUPERVISED, AUTONOMOUS) tested
7. Verify security tests: JavaScript execution dangerous patterns blocked
</verification>

<success_criteria>
1. 90%+ coverage achieved for canvas_tool.py (up from 40.4%)
2. All presentation types tested (chart, markdown, form, status_panel, specialized)
3. Governance enforcement tested at all maturity boundaries
4. JavaScript execution security validated (AUTONOMOUS only, dangerous patterns blocked)
5. State management tested (update_canvas, close_canvas, session isolation)
6. Audit trail creation verified for all canvas actions
7. Error handling tested for all failure modes
8. Test files: test_canvas_tool.py (1800+ lines), test_canvas_tool_governance.py (600+ lines)
9. 94 new tests added across 11 test classes
</success_criteria>

<output>
After completion, create `.planning/phases/083-core-services-unit-testing-canvas-browser/083-01-SUMMARY.md`
</output>
