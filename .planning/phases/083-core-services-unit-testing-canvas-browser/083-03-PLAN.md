---
phase: 083-core-services-unit-testing-canvas-browser
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - backend/tests/unit/test_device_tool.py
autonomous: true

must_haves:
  truths:
    - Device capabilities tool tests cover camera, screen recording, location, notifications, command execution
    - Governance enforcement tested for all device operations (INTERN+, SUPERVISED+, AUTONOMOUS only)
    - Permission checks tested at maturity boundaries
    - WebSocket integration mocked appropriately for device communication
    - Audit trail creation verified for all device actions
    - Error handling tested for device offline, WebSocket unavailable, command whitelist violations
  artifacts:
    - path: backend/tests/unit/test_device_tool.py
      provides: Device capabilities unit tests with governance enforcement
      min_lines: 1200
  key_links:
    - from: test_device_tool.py
      to: core/agent_governance_service.py
      via: governance.can_perform_action() for device operations
      pattern: governance\.can_perform_action.*device_
    - from: test_device_tool.py
      to: tools/device_tool.py
      via: _check_device_governance() helper
      pattern: _check_device_governance
    - from: test_device_tool.py
      to: api/device_websocket.py
      via: send_device_command() and is_device_online() mocks
      pattern: (send_device_command|is_device_online)
---

<objective>
Create comprehensive unit tests for device capabilities tool functions covering camera access, screen recording, location services, notifications, command execution, governance enforcement at multiple maturity levels, and WebSocket integration to achieve 90%+ coverage for device_tool.py (currently at 9.73%).

Purpose: Device capabilities are security-sensitive features with tiered governance requirements (INTERN+ for camera/location/notifications, SUPERVISED+ for screen recording, AUTONOMOUS only for command execution). Current tests (9.73%) do not cover governance paths, WebSocket integration, or error handling.

Output: 15+ new tests in test_device_tool.py with 90%+ coverage target, including governance enforcement at all maturity levels
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
@.planning/ROADMAP.md
@.planning/PROJECT.md

# Phase 82 patterns to follow
@.planning/phases/082-core-services-unit-testing-governance-episodes/082-01-SUMMARY.md
# Phase 083-02 browser tool patterns for consistency
@.planning/phases/083-core-services-unit-testing-canvas-browser/083-02-PLAN.md
</execution_context>

<context>
@backend/tools/device_tool.py
@backend/core/models.py (DeviceAudit, DeviceNode, DeviceSession, AgentStatus)
@backend/core/agent_governance_service.py (governance patterns)
@api/device_websocket.py (WebSocket integration to mock)

# Phase 083 patterns to follow:
- Device operations have tiered governance (INTERN+, SUPERVISED+, AUTONOMOUS only)
- WebSocket communication must be mocked (send_device_command, is_device_online)
- Audit entry creation must be verified for all device actions
- FeatureFlags mocking for governance bypass scenarios
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add device camera, location, and notification tests with governance</name>
  <files>backend/tests/unit/test_device_tool.py</files>
  <action>
Create new test file test_device_tool.py with device capability test classes:

1. TestDeviceCameraSnap class (10 tests):
   - device_camera_snap successful with online device
   - device_camera_snap governance: STUDENT agent blocked (INTERN+ required)
   - device_camera_snap governance: INTERN agent allowed
   - device_camera_snap governance: SUPERVISED agent allowed
   - device_camera_snap governance: AUTONOMOUS agent allowed
   - device_camera_snap handles offline device (WebSocket not connected)
   - device_camera_snap handles WebSocket module unavailable
   - device_camera_snap creates audit entry on success
   - device_camera_snap creates audit entry on failure
   - device_camera_snap returns file_path and base64_data
   - device_camera_snap with custom camera_id parameter
   - device_camera_snap with custom resolution parameter

2. TestDeviceGetLocation class (8 tests):
   - device_get_location successful with online device
   - device_get_location governance: STUDENT agent blocked (INTERN+ required)
   - device_get_location governance: INTERN agent allowed
   - device_get_location handles offline device
   - device_get_location handles WebSocket unavailable
   - device_get_location returns latitude, longitude, accuracy
   - device_get_location returns altitude if available
   - device_get_location creates audit entry
   - device_get_location with high accuracy parameter
   - device_get_location with medium/low accuracy

3. TestDeviceSendNotification class (8 tests):
   - device_send_notification successful with title and body
   - device_send_notification governance: STUDENT agent blocked (INTERN+ required)
   - device_send_notification governance: INTERN agent allowed
   - device_send_notification handles offline device
   - device_send_notification with icon parameter
   - device_send_notification with sound parameter
   - device_send_notification creates audit entry
   - device_send_notification returns sent_at timestamp

Mock pattern:
- Mock send_device_command from api.device_websocket to return success responses
- Mock is_device_online to return True/False for online/offline tests
- Mock governance.can_perform_action for maturity checks
- Mock get_db_session as context manager
- Mock _create_device_audit to verify audit calls
- Mock WEBSOCKET_AVAILABLE flag for module availability tests

Test file size target: 600+ lines
  </action>
  <verify>
pytest backend/tests/unit/test_device_tool.py -v
pytest backend/tests/unit/test_device_tool.py -k "TestDeviceCameraSnap or TestDeviceGetLocation or TestDeviceSendNotification" -v
  </verify>
  <done>
26 new tests added across 3 test classes (10 + 8 + 8)
Test file created with 600+ lines
All INTERN+ device operations tested with governance
  </done>
</task>

<task type="auto">
  <name>Task 2: Add device screen recording and command execution tests with governance</name>
  <files>backend/tests/unit/test_device_tool.py</files>
  <action>
Add new test classes to test_device_tool.py after TestDeviceSendNotification class:

1. TestDeviceScreenRecordStart class (12 tests):
   - device_screen_record_start successful with SUPERVISED agent
   - device_screen_record_start governance: STUDENT agent blocked (SUPERVISED+ required)
   - device_screen_record_start governance: INTERN agent blocked (SUPERVISED+ required)
   - device_screen_record_start governance: SUPERVISED agent allowed
   - device_screen_record_start governance: AUTONOMOUS agent allowed
   - device_screen_record_start creates database session record
   - device_screen_record_start creates DeviceSession with "screen_record" type
   - device_screen_record_start validates duration (max enforced)
   - device_screen_record_start with audio_enabled=True
   - device_screen_record_start with custom resolution
   - device_screen_record_start creates audit entry on success
   - device_screen_record_start handles offline device
   - device_screen_record_start updates session status to failed on error

2. TestDeviceScreenRecordStop class (8 tests):
   - device_screen_record_stop successful for active session
   - device_screen_record_stop updates session status to "closed"
   - device_screen_record_stop updates session closed_at timestamp
   - device_screen_record_stop returns file_path
   - device_screen_record_stop returns duration_seconds
   - device_screen_record_stop handles non-existent session
   - device_screen_record_stop validates user_id matches session
   - device_screen_record_stop creates audit entry
   - device_screen_record_stop handles WebSocket unavailable gracefully

3. TestDeviceExecuteCommand class (12 tests):
   - device_execute_command successful with AUTONOMOUS agent
   - device_execute_command governance: STUDENT blocked (AUTONOMOUS only)
   - device_execute_command governance: INTERN blocked (AUTONOMOUS only)
   - device_execute_command governance: SUPERVISED blocked (AUTONOMOUS only)
   - device_execute_command governance: AUTONOMOUS agent allowed
   - device_execute_command validates command whitelist
   - device_execute_command blocks command not in whitelist
   - device_execute_command enforces timeout max (300 seconds)
   - device_execute_command with custom working_dir
   - device_execute_command with custom environment variables
   - device_execute_command returns exit_code, stdout, stderr
   - device_execute_command creates audit entry with command
   - device_execute_command handles offline device

Add after TestDeviceSendNotification class.
Target: 800+ new lines of test code
  </action>
  <verify>
pytest backend/tests/unit/test_device_tool.py -v
pytest backend/tests/unit/test_device_tool.py -k "TestDeviceScreenRecordStart or TestDeviceScreenRecordStop or TestDeviceExecuteCommand" -v
  </verify>
  <done>
32 new tests added across 3 test classes (12 + 8 + 12)
test_device_tool.py expanded by 800+ lines
All SUPERVISED+ and AUTONOMOUS-only operations tested
  </done>
</task>

<task type="auto">
  <name>Task 3: Add device audit, helper functions, and error handling tests</name>
  <files>backend/tests/unit/test_device_tool.py</files>
  <action>
Add new test classes to test_device_tool.py after TestDeviceExecuteCommand class:

1. TestDeviceAuditEntry class (10 tests):
   - _create_device_audit creates DeviceAudit with all parameters
   - _create_device_audit includes agent_id
   - _create_device_audit includes device_node_id
   - _create_device_audit includes action_type
   - _create_device_audit includes action_params
   - _create_device_audit includes success flag
   - _create_device_audit includes result_summary on success
   - _create_device_audit includes error_message on failure
   - _create_device_audit includes result_data
   - _create_device_audit includes file_path
   - _create_device_audit includes duration_ms
   - _create_device_audit includes session_id
   - _create_device_audit includes governance_check_passed
   - _create_device_audit generates unique UUID
   - _create_device_audit commits to database
   - _create_device_audit handles exception and logs error

2. TestDeviceGovernanceCheck class (8 tests):
   - _check_device_governance returns allowed=True when governance disabled
   - _check_device_governance bypasses with EMERGENCY_GOVERNANCE_BYPASS
   - _check_device_governance calls governance.can_perform_action
   - _check_device_governance returns governance_check_passed flag
   - _check_device_governance includes reason in result
   - _check_device_governance handles exception and fails open
   - _check_device_governance logs error on exception
   - _check_device_governance returns allowed=True on governance service failure

3. TestDeviceSessionManager class (8 tests):
   - get_device_session_manager returns singleton instance
   - DeviceSessionManager.create_session generates UUID
   - DeviceSessionManager.create_session stores session
   - DeviceSessionManager.get_session returns existing session
   - DeviceSessionManager.get_session returns None for non-existent
   - DeviceSessionManager.close_session removes session
   - DeviceSessionManager.close_session returns True for existing session
   - DeviceSessionManager.close_session returns False for non-existent session
   - DeviceSessionManager.cleanup_expired_sessions removes old sessions
   - Session timeout default is 60 minutes

4. TestDeviceHelperFunctions class (6 tests):
   - get_device_info returns device information for existing device
   - get_device_info returns None for non-existent device
   - get_device_info includes platform, status, capabilities
   - list_devices returns devices for user
   - list_devices filters by status when specified
   - list_devices returns empty list when no devices

5. TestDeviceExecuteWrapper class (8 tests):
   - execute_device_command routes to camera command type
   - execute_device_command routes to location command type
   - execute_device_command routes to notification command type
   - execute_device_command routes to command (shell) type
   - execute_device_command returns error for unknown command_type
   - execute_device_command handles exceptions from device functions
   - execute_device_command passes parameters correctly to device functions
   - execute_device_command includes execution_id in response

Add after TestDeviceExecuteCommand class.
Target: 700+ new lines of test code
  </action>
  <verify>
pytest backend/tests/unit/test_device_tool.py -v
pytest backend/tests/unit/test_device_tool.py -k "TestDeviceAuditEntry or TestDeviceGovernanceCheck or TestDeviceSessionManager or TestDeviceHelperFunctions or TestDeviceExecuteWrapper" -v
  </verify>
  <done>
40 new tests added across 5 test classes (10 + 8 + 8 + 6 + 8)
test_device_tool.py expanded by 700+ lines
All device audit, helper, and wrapper functions tested
  </done>
</task>

</tasks>

<verification>
1. Run all device tool tests: pytest backend/tests/unit/test_device_tool.py -v
2. Verify test count: 50+ new tests added (26 + 32 + 40 = 98 tests)
3. Verify coverage: Run coverage report for device_tool.py, target 90%+
4. Verify no regressions: All existing tests still pass
5. Verify governance paths: All maturity levels tested (INTERN+, SUPERVISED+, AUTONOMOUS only)
6. Verify audit trails: All device actions create audit entries
7. Verify WebSocket mocking: send_device_command and is_device_online properly mocked
</verification>

<success_criteria>
1. 90%+ coverage achieved for device_tool.py (up from 9.73%)
2. All device capabilities tested (camera, screen recording, location, notifications, command execution)
3. Tiered governance tested at all maturity boundaries:
   - INTERN+ for camera, location, notifications
   - SUPERVISED+ for screen recording
   - AUTONOMOUS only for command execution
4. Command whitelist validation tested
5. WebSocket integration properly mocked
6. Audit trail creation verified for all device actions
7. Error handling tested for offline devices and WebSocket failures
8. Test file: test_device_tool.py (1200+ lines)
9. 98 new tests added across 11 test classes
</success_criteria>

<output>
After completion, create `.planning/phases/083-core-services-unit-testing-canvas-browser/083-03-SUMMARY.md`
</output>
