---
phase: 226-llm-provider-registry
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/llm/byok_handler.py
  - backend/core/benchmarks.py
  - backend/core/llm_service.py
  - backend/tests/test_lux_bpc_integration.py
autonomous: true

must_haves:
  truths:
    - "LUX provider appears in COST_EFFICIENT_MODELS with model mapping"
    - "LUX has quality score (88) in MODEL_QUALITY_SCORES benchmarks"
    - "BYOKHandler initializes lux client with proper API key resolution"
    - "LUX appears in provider_priority lists for appropriate query complexities"
    - "LUX routing works for vision and computer use tasks"
  artifacts:
    - path: "backend/core/llm/byok_handler.py"
      provides: "LUX provider configuration and client initialization"
      contains: '"lux": {', 'COST_EFFICIENT_MODELS', 'providers_config'
    - path: "backend/core/benchmarks.py"
      provides: "LUX model quality score"
      contains: '"lux": 88'
    - path: "backend/core/llm_service.py"
      provides: "LUX in cognitive tier routing"
      contains: '"lux"'  # in COST_EFFICIENT_MODELS if separate
    - path: "backend/tests/test_lux_bpc_integration.py"
      provides: "Integration tests for LUX BPC routing"
      exports: ["test_lux_in_cost_efficient_models", "test_lux_quality_score", "test_lux_client_init"]
  key_links:
    - from: "backend/core/llm/byok_handler.py"
      to: "backend/core/lux_config.py"
      via: "lux_config.get_anthropic_key()"
      pattern: "lux_config\.get_anthropic_key\(\)"
    - from: "backend/core/llm/byok_handler.py"
      to: "backend/core/benchmarks.py"
      via: "get_quality_score()"
      pattern: "get_quality_score\("
---

<objective>
Integrate LUX computer use model into the BPC (Benchmark-Price-Capability) routing system in BYOKHandler.

Purpose: Enable LUX model selection in cognitive tier routing, support vision/computer use tasks, and ensure proper API key resolution through lux_config.

Output: Updated BYOKHandler with LUX provider configuration, quality score in benchmarks, and integration tests.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/226-llm-provider-registry/226-CONTEXT.md
@.planning/phases/226-llm-provider-registry/226-RESEARCH.md
@backend/core/llm/byok_handler.py
@backend/core/lux_config.py
@backend/core/benchmarks.py
@backend/ai/lux_model.py
@.planning/phases/68-byok-cognitive-tier-system/68-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add LUX provider configuration to BYOKHandler</name>
  <files>backend/core/llm/byok_handler.py</files>
  <action>
    Modify BYOKHandler to include LUX provider configuration:

    1. **Import lux_config** (add at top with other imports):
       from core.lux_config import lux_config

    2. **Add LUX to providers_config** in _initialize_clients method:
       providers_config = {
           # ... existing providers ...
           "lux": {"base_url": None},  # Uses Anthropic API, no custom base_url
       }

    3. **Add LUX to COST_EFFICIENT_MODELS** (after minimax entry):
       COST_EFFICIENT_MODELS = {
           # ... existing providers ...
           "lux": {
               QueryComplexity.SIMPLE: "lux-1.0",
               QueryComplexity.MODERATE: "lux-1.0",
               QueryComplexity.COMPLEX: "lux-1.0",
               QueryComplexity.ADVANCED: "lux-1.0",
           },
       }

    4. **Add LUX to static provider_priority lists** (position for vision tasks):
       In provider_priority dict or relevant static lists:
       - For vision/computer use tasks: ["lux", "openai", "anthropic"]
       - LUX should be prioritized for requires_vision=True requests

    5. **Update _initialize_clients to handle lux API key**:
       if provider_id == "lux":
           api_key = lux_config.get_anthropic_key() or self.byok_manager.get_api_key("lux")
           if api_key:
               self.clients["lux"] = Anthropic(api_key=api_key)  # Uses Anthropic client

    Reference: Existing minimax integration pattern (Phase 68-04) for model additions
  </action>
  <verify>grep -n '"lux":' backend/core/llm/byok_handler.py && grep -n "lux_config" backend/core/llm/byok_handler.py</verify>
  <done>LUX appears in providers_config, COST_EFFICIENT_MODELS, and client initialization</done>
</task>

<task type="auto">
  <name>Add LUX quality score to benchmarks</name>
  <files>backend/core/benchmarks.py</files>
  <action>
    Add LUX model quality score to MODEL_QUALITY_SCORES:

    1. **Locate MODEL_QUALITY_SCORES dict** in benchmarks.py
    2. **Add LUX entry** (score 88, between gemini-2.0-flash and deepseek-chat):
       MODEL_QUALITY_SCORES = {
           # ... existing scores ...
           "lux-1.0": 88,  # LUX Computer Use (Claude 3.5 Sonnet based)
       }

    3. **If MODEL_QUALITY_BY_PROVIDER exists**, add entry:
       MODEL_QUALITY_BY_PROVIDER = {
           # ... existing ...
           "lux": 88,
       }

    Score rationale: LUX uses Claude 3.5 Sonnet (quality ~90) for computer use tasks,
    with additional overhead for visual understanding. 88 is a conservative estimate
    between gemini-2.0-flash (86) and claude-3-5-sonnet (90).

    Reference: Phase 68-04 minimax integration (quality score 88)
  </action>
  <verify>grep -n '"lux"' backend/core/benchmarks.py</verify>
  <done>LUX has quality score 88 in MODEL_QUALITY_SCORES</done>
</task>

<task type="auto">
  <name>Create LUX BPC integration tests</name>
  <files>backend/tests/test_lux_bpc_integration.py</files>
  <action>
    Create comprehensive integration tests for LUX model routing:

    **Test file structure:**
    import pytest
    from core.llm.byok_handler import BYOKHandler, QueryComplexity
    from core.benchmarks import get_quality_score

    **Test cases:**
    1. test_lux_in_cost_efficient_models():
       Verify "lux" key exists in COST_EFFICIENT_MODELS
       Verify model mapping for all QueryComplexity levels

    2. test_lux_quality_score():
       Verify get_quality_score("lux-1.0") returns 88
       Verify score is between gemini-2.0-flash (86) and claude-3-5-sonnet (90)

    3. test_lux_client_initialization():
       Mock lux_config.get_anthropic_key() to return test key
       Initialize BYOKHandler
       Verify "lux" in handler.clients

    4. test_lux_client_fallback_to_byok():
       Mock lux_config to return None
       Mock byok_manager.get_api_key("lux") to return key
       Verify client initializes from BYOK

    5. test_lux_in_provider_priority():
       Verify LUX appears in provider_priority for vision tasks
       Verify position relative to other vision-capable providers

    6. test_lux_not_in_provider_priority_for_simple():
       Verify LUX is not prioritized for non-vision simple queries
       (costlier providers should be lower priority)

    7. test_lux_requires_vision_routing():
       Test get_ranked_providers with requires_vision=True
       Verify LUX appears in ranked options

    Use existing BYOKHandler test patterns from backend/tests/test_byok_handler.py
    Mock Anthropic client to avoid actual API calls
  </action>
  <verify>pytest backend/tests/test_lux_bpc_integration.py -v --collect-only</verify>
  <done>7+ tests defined, all collect successfully, run to verify they pass</done>
</task>

</tasks>

<verification>
After plan completion:
1. pytest backend/tests/test_lux_bpc_integration.py -v (all tests pass)
2. Verify LUX in COST_EFFICIENT_MODELS: python -c "from core.llm.byok_handler import COST_EFFICIENT_MODELS; print(COST_EFFICIENT_MODELS.get('lux'))"
3. Verify quality score: python -c "from core.benchmarks import get_quality_score; print(get_quality_score('lux-1.0'))"
4. Test BYOKHandler initialization: python -c "from core.llm.byok_handler import BYOKHandler; h = BYOKHandler(); print('lux' in h.clients)"
</verification>

<success_criteria>
1. LUX provider in COST_EFFICIENT_MODELS with lux-1.0 model mapping
2. Quality score 88 in MODEL_QUALITY_SCORES
3. BYOKHandler initializes lux client from lux_config or BYOK
4. All 7+ integration tests pass
5. LUX appears in vision task routing
</success_criteria>

<output>
After completion, create `.planning/phases/226-llm-provider-registry/226-02-SUMMARY.md`
</output>
