---
phase: 226-llm-provider-registry
plan: 03
type: execute
wave: 2
depends_on: ["226-01"]
files_modified:
  - backend/core/byok_endpoints.py
  - backend/api/provider_registry_routes.py
  - frontend-nextjs/components/DevStudio/BYOKManager.tsx
  - backend/tests/test_provider_registry_api.py
autonomous: true

must_haves:
  truths:
    - "API keys submitted via POST body (not URL query params)"
    - "Provider registry CRUD endpoints return provider lists with model counts"
    - "Manual sync endpoint triggers ProviderAutoDiscovery.sync_providers()"
    - "Frontend BYOKManager uses POST body for API key submission"
    - "Tests verify endpoint security, sync functionality, and CRUD operations"
  artifacts:
    - path: "backend/api/provider_registry_routes.py"
      provides: "REST API endpoints for provider registry"
      exports: ["/api/ai/providers/registry", "/api/ai/providers/registry/sync"]
    - path: "backend/core/byok_endpoints.py"
      provides: "Modified API key endpoints (POST body instead of query params)"
      contains: "async def add_api_key", "RequestModel"
    - path: "frontend-nextjs/components/DevStudio/BYOKManager.tsx"
      provides: "Secure API key submission via POST body"
      contains: "method: 'POST'", "body: JSON.stringify"
    - path: "backend/tests/test_provider_registry_api.py"
      provides: "API endpoint tests"
      exports: ["test_list_providers", "test_sync_providers", "test_add_key_via_post_body"]
  key_links:
    - from: "frontend-nextjs/components/DevStudio/BYOKManager.tsx"
      to: "/api/ai/providers/{provider}/keys"
      via: "fetch with POST body"
      pattern: "body: JSON\.stringify\(\{ api_key \}\)"
    - from: "backend/api/provider_registry_routes.py"
      to: "backend/core/provider_auto_discovery.py"
      via: "get_auto_discovery().sync_providers()"
      pattern: "get_auto_discovery\(\)"
---

<objective>
Create REST API endpoints for provider registry and fix API key security vulnerability (POST body instead of query params).

Purpose: Enable external access to provider registry, secure API key submission, and provide manual sync trigger for auto-discovery.

Output: New provider_registry_routes.py file, modified byok_endpoints.py for secure API keys, updated BYOKManager.tsx frontend, and API tests.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/226-llm-provider-registry/226-CONTEXT.md
@.planning/phases/226-llm-provider-registry/226-RESEARCH.md
@backend/core/byok_endpoints.py
@backend/core/provider_registry.py
@frontend-nextjs/components/DevStudio/BYOKManager.tsx
@.planning/phases/226-llm-provider-registry/226-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Fix API key security in byok_endpoints (POST body)</name>
  <files>backend/core/byok_endpoints.py</files>
  <action>
    Modify the add API key endpoint to accept POST body instead of query params:

    1. **Create Pydantic RequestModel** for API key submission:
       from pydantic import BaseModel

       class AddAPIKeyRequest(BaseModel):
           api_key: str
           key_name: str = "default"

    2. **Update POST endpoint** (replace existing query param version):
       @router.post("/api/ai/providers/{provider_id}/keys")
       async def add_api_key(
           provider_id: str,
           request: AddAPIKeyRequest,
           background_tasks: BackgroundTasks
       ):
           """Add API key for a provider (secure: uses POST body)"""

           # Validate api_key is not empty
           if not request.api_key or len(request.api_key) < 10:
               raise HTTPException(400, "Invalid API key")

           # Use request.api_key instead of query params
           key_hash = hashlib.sha256(request.api_key.encode()).hexdigest()
           # ... rest of encryption logic ...

    3. **Add request validation**:
       - api_key: min_length=10
       - key_name: max_length=100, alphanumeric + underscore only

    4. **Remove old query param handling**:
       Delete any code that accesses api_key from request.query_params

    Security note: API keys in POST body don't appear in browser history, logs, or analytics
    compared to query params which are commonly logged.
  </action>
  <verify>grep -n "class AddAPIKeyRequest" backend/core/byok_endpoints.py && grep -n "request: AddAPIKeyRequest" backend/core/byok_endpoints.py</verify>
  <done>AddAPIKeyRequest model exists, endpoint uses request body instead of query params</done>
</task>

<task type="auto">
  <name>Create provider registry REST API endpoints</name>
  <files>backend/api/provider_registry_routes.py</files>
  <action>
    Create new API router file for provider registry endpoints:

    **File structure:**
    from fastapi import APIRouter, Depends, HTTPException, BackgroundTasks
    from sqlalchemy.orm import Session
    from core.database import get_db
    from core.provider_registry import get_provider_registry
    from core.provider_auto_discovery import get_auto_discovery

    router = APIRouter()

    **Endpoints:**

    1. GET /api/ai/providers/registry
       - List all active providers with model counts
       - Query params: active_only (default=true), include_inactive (default=false)
       - Returns: {"success": true, "providers": [{provider_id, name, model_count, quality_score, supports_vision, ...}]}
       - Use ProviderRegistryService.list_providers()

    2. GET /api/ai/providers/registry/{provider_id}
       - Get single provider with models
       - Returns: {"success": true, "provider": {...}, "models": [...]}

    3. GET /api/ai/providers/registry/{provider_id}/models
       - List all models for a provider
       - Query params: supports_vision (bool), min_quality (int), max_cost (float)
       - Returns: {"success": true, "models": [...]}

    4. POST /api/ai/providers/registry/sync
       - Trigger manual sync from LiteLLM/OpenRouter
       - Runs in background task
       - Returns: {"success": true, "message": "Sync started", "sync_id": str}

    5. GET /api/ai/providers/registry/sync/status
       - Check if sync is running
       - Returns: {"success": true, "syncing": bool, "last_sync": timestamp}

    **Error handling:**
       - 404 for non-existent provider_id
       - 400 for invalid query params
       - 500 for database errors (log, don't expose details)

    Import patterns from existing routes in backend/core/api_routes.py
  </action>
  <verify>grep -n "router = APIRouter()" backend/api/provider_registry_routes.py && grep -n "def list_providers" backend/api/provider_registry_routes.py</verify>
  <done>All 5 endpoints defined with proper error handling</done>
</task>

<task type="auto">
  <name>Fix frontend API key submission (POST body)</name>
  <files>frontend-nextjs/components/DevStudio/BYOKManager.tsx</files>
  <action>
    Modify BYOKManager.tsx to submit API keys via POST body instead of query params:

    1. **Update handleAddKey function** (replace query param approach):
       const handleAddKey = async () => {
           if (!selectedProvider || !apiKey) {
               // ... existing validation ...
           }

           try {
               // SECURE: Use POST body instead of query params
               const response = await fetch(`/api/ai/providers/${selectedProvider}/keys`, {
                   method: 'POST',
                   headers: {
                       'Content-Type': 'application/json',
                   },
                   body: JSON.stringify({
                       api_key: apiKey,
                       key_name: keyName
                   })
               });

               const data = await response.json();
               // ... existing success/error handling ...
           } catch (error: any) {
               // ... existing error handling ...
           }
       };

    2. **Remove any query param construction**:
       Delete lines that build URLs like:
       `/api/ai/providers/${provider}/keys?api_key=${encodeURIComponent(apiKey)}`

    3. **Add TypeScript interface for request body** (if not present):
       interface AddApiKeyRequest {
           api_key: string;
           key_name: string;
       }

    Security impact: API keys no longer visible in:
    - Browser URL bar/history
    - Server access logs (query params often logged)
    - Analytics/tracking systems
  </action>
  <verify>grep -n "body: JSON.stringify" frontend-nextjs/components/DevStudio/BYOKManager.tsx && ! grep -n "api_key=\${" frontend-nextjs/components/DevStudio/BYOKManager.tsx</verify>
  <done>API key submission uses POST body, no query params contain api_key</done>
</task>

<task type="auto">
  <name>Create API endpoint integration tests</name>
  <files>backend/tests/test_provider_registry_api.py</files>
  <action>
    Create integration tests for provider registry API:

    **Test file structure:**
    import pytest
    from fastapi.testclient import TestClient
    from main import app
    from core.database import get_db
    from unittest.mock import patch, MagicMock

    **Test cases:**
    1. test_list_providers_success():
       Mock get_provider_registry().list_providers()
       GET /api/ai/providers/registry
       Verify 200, providers list returned

    2. test_get_provider_with_models():
       Mock registry.get_provider() with models
       GET /api/ai/providers/registry/openai
       Verify 200, models included

    3. test_get_provider_not_found():
       Mock registry.get_provider() to return None
       GET /api/ai/providers/registry/nonexistent
       Verify 404

    4. test_sync_providers_starts_background_task():
       Mock get_auto_discovery().sync_providers()
       POST /api/ai/providers/registry/sync
       Verify 200, "sync started" message

    5. test_add_api_key_via_post_body():
       Mock byok_manager.add_api_key()
       POST /api/ai/providers/openai/keys with JSON body
       Verify api_key extracted from body, not query params

    6. test_add_api_key_rejects_query_params():
       Attempt to add key via query params (old behavior)
       Verify 400 or rejection

    7. test_search_models_by_capability():
       Mock registry.search_models()
       GET /api/ai/providers/registry/openai/models?supports_vision=true
       Verify filters applied

    Use TestClient from fastapi.testclient
    Mock database sessions to avoid DB dependency
    Follow patterns from backend/tests/test_api_routes.py
  </action>
  <verify>pytest backend/tests/test_provider_registry_api.py -v --collect-only</verify>
  <done>7+ API tests defined, verify all pass with pytest</done>
</task>

</tasks>

<verification>
After plan completion:
1. pytest backend/tests/test_provider_registry_api.py -v (all tests pass)
2. Test provider list: curl http://localhost:8000/api/ai/providers/registry
3. Test sync: curl -X POST http://localhost:8000/api/ai/providers/registry/sync
4. Verify frontend: Open browser DevTools Network tab, add API key, verify POST body contains api_key (not URL)
5. Security check: grep -r "api_key=" frontend-nextjs/components/DevStudio/ (should have no matches in URL construction)
</verification>

<success_criteria>
1. API key submission uses POST body (no query params)
2. Provider registry endpoints return providers with model counts
3. Manual sync endpoint triggers ProviderAutoDiscovery
4. All 7+ API tests pass
5. Frontend BYOKManager submits secure POST requests
</success_criteria>

<output>
After completion, create `.planning/phases/226-llm-provider-registry/226-03-SUMMARY.md`
</output>
