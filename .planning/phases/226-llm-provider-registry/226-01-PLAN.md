---
phase: 226-llm-provider-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/models.py
  - backend/core/provider_registry.py
  - backend/core/provider_auto_discovery.py
  - backend/alembic/versions/001_add_provider_registry.py
  - backend/tests/test_provider_registry.py
autonomous: true

must_haves:
  truths:
    - "ProviderRegistry and ModelCatalog tables exist in database with proper indexes"
    - "Auto-discovery service can sync from DynamicPricingFetcher to database"
    - "ProviderRegistry CRUD endpoints return providers with model counts"
    - "Manual sync endpoint triggers background refresh from LiteLLM/OpenRouter"
    - "Tests verify upsert logic, provider-model relationships, and sync operations"
  artifacts:
    - path: "backend/core/models.py"
      provides: "ProviderRegistry and ModelCatalog SQLAlchemy models"
      contains: "class ProviderRegistry", "class ModelCatalog"
    - path: "backend/core/provider_registry.py"
      provides: "ProviderRegistry service with CRUD operations"
      exports: ["ProviderRegistryService", "get_provider_registry"]
    - path: "backend/core/provider_auto_discovery.py"
      provides: "Auto-discovery orchestration service"
      exports: ["ProviderAutoDiscovery", "get_auto_discovery"]
    - path: "backend/alembic/versions/001_add_provider_registry.py"
      provides: "Database migration for provider registry tables"
      contains: "def upgrade", "def downgrade"
    - path: "backend/tests/test_provider_registry.py"
      provides: "Unit tests for provider registry service"
      exports: ["test_create_provider", "test_sync_providers", "test_upsert_model"]
  key_links:
    - from: "backend/core/provider_auto_discovery.py"
      to: "backend/core/dynamic_pricing_fetcher.py"
      via: "get_pricing_fetcher()"
      pattern: "get_pricing_fetcher\(\)"
    - from: "backend/core/provider_registry.py"
      to: "core.models.ProviderRegistry"
      via: "SQLAlchemy ORM"
      pattern: "db.query\(ProviderRegistry\)"
---

<objective>
Create a persistent provider registry system that stores LLM providers and models in the database with auto-discovery from DynamicPricingFetcher.

Purpose: Enable persistent storage of provider/model metadata, support scheduled auto-discovery polling, and provide CRUD API endpoints for provider management.

Output: SQLAlchemy models (ProviderRegistry, ModelCatalog), ProviderRegistryService, ProviderAutoDiscovery service, Alembic migration, and unit tests.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/226-llm-provider-registry/226-CONTEXT.md
@.planning/phases/226-llm-provider-registry/226-RESEARCH.md
@backend/core/models.py
@backend/core/dynamic_pricing_fetcher.py
@backend/core/byok_endpoints.py
</context>

<tasks>

<task type="auto">
  <name>Create ProviderRegistry and ModelCatalog SQLAlchemy models</name>
  <files>backend/core/models.py</files>
  <action>
    Add two new SQLAlchemy models to the end of models.py (before existing relationships):

    1. **ProviderRegistry** model with fields:
       - provider_id (String, primary_key): e.g., "openai", "anthropic", "lux"
       - name (String, nullable=False): Human-readable name
       - description (String): Provider description
       - litellm_provider (String): For mapping to LiteLLM models
       - base_url (String): API endpoint
       - supports_vision (Boolean, default=False)
       - supports_tools (Boolean, default=False)
       - supports_cache (Boolean, default=False)
       - supports_structured_output (Boolean, default=False)
       - reasoning_level (Integer): 1-4 scale
       - quality_score (Float): Benchmark score (0-100)
       - is_active (Boolean, default=True)
       - discovered_at (DateTime, default=datetime.now(timezone.utc))
       - last_updated (DateTime, default=datetime.now(timezone.utc), onupdate=datetime.now(timezone.utc))
       - metadata (JSON): Freeform provider-specific data
       - Add Index on is_active for filtering active providers

    2. **ModelCatalog** model with fields:
       - model_id (String, primary_key): e.g., "gpt-4o", "lux-1.0"
       - provider_id (String, ForeignKey("provider_registry.provider_id")): Link to provider
       - name (String): Human-readable model name
       - description (String): Model description
       - input_cost_per_token (Float)
       - output_cost_per_token (Float)
       - max_tokens (Integer)
       - max_input_tokens (Integer)
       - context_window (Integer)
       - mode (String): "chat", "completion", "vision"
       - source (String): "litellm", "openrouter", "manual"
       - discovered_at (DateTime, default=datetime.now(timezone.utc))
       - last_updated (DateTime, default=datetime.now(timezone.utc), onupdate=datetime.now(timezone.utc))
       - metadata (JSON)
       - Add Index on provider_id for fast queries
       - Add relationship: provider = relationship("ProviderRegistry", backref="models")

    Use timezone-aware datetimes: from datetime import datetime, timezone
  </action>
  <verify>grep -n "class ProviderRegistry" backend/core/models.py && grep -n "class ModelCatalog" backend/core/models.py</verify>
  <done>Both models exist with all required fields, indexes, and foreign key relationship</done>
</task>

<task type="auto">
  <name>Create ProviderRegistryService with CRUD operations</name>
  <files>backend/core/provider_registry.py</files>
  <action>
    Create a new service file with ProviderRegistryService class:

    **Class structure:**
    - __init__(self, db_session): Accept database session dependency
    - create_provider(self, provider_data): Create new provider (upsert by provider_id)
    - get_provider(self, provider_id): Get single provider with model count
    - list_providers(self, active_only=True): List all providers with model counts
    - update_provider(self, provider_id, updates): Update provider fields
    - delete_provider(self, provider_id): Soft delete (set is_active=False)
    - create_model(self, model_data): Create or update model (upsert by model_id)
    - get_models_by_provider(self, provider_id): Get all models for a provider
    - search_models(self, filters): Search models by capabilities, cost, quality

    **Key methods:**
    - upsert_provider(provider_dict): Merge provider, update if exists
    - upsert_model(model_dict): Merge model, update if exists
    - get_provider_stats(provider_id): Return dict with model_count, avg_cost, capabilities

    **Singleton pattern:**
    _registry_instance: Optional[ProviderRegistryService] = None
    def get_provider_registry() -> ProviderRegistryService

    Use SQLAlchemy Session context: with get_db_session() as db:
    Handle duplicate key errors gracefully (update instead of insert)
  </action>
  <verify>grep -n "class ProviderRegistryService" backend/core/provider_registry.py && grep -n "def get_provider_registry" backend/core/provider_registry.py</verify>
  <done>ProviderRegistryService exists with all CRUD methods and singleton pattern</done>
</task>

<task type="auto">
  <name>Create ProviderAutoDiscovery service with sync logic</name>
  <files>backend/core/provider_auto_discovery.py</files>
  <action>
    Create auto-discovery service that bridges DynamicPricingFetcher to ProviderRegistry:

    **Class structure:**
    from core.dynamic_pricing_fetcher import get_pricing_fetcher
    from core.provider_registry import get_provider_registry

    class ProviderAutoDiscovery:
        def __init__(self):
            self.pricing_fetcher = get_pricing_fetcher()
            self.registry = get_provider_registry()

        async def sync_providers(self) -> Dict[str, int]:
            """Fetch latest pricing and sync to registry

            Returns: {"providers_synced": N, "models_synced": M}
            """
            # 1. Refresh pricing cache
            await self.pricing_fetcher.refresh_pricing(force=True)

            # 2. Extract unique providers from pricing_cache
            # Group models by litellm_provider
            # Create ProviderRegistry entries for new providers

            # 3. Upsert models to ModelCatalog
            # Map pricing data to model fields
            # Extract: input_cost_per_token, output_cost_per_token, max_tokens, mode

            # 4. Update provider metadata (model counts, capabilities)
            # Set supports_vision, supports_tools from pricing metadata

        async def sync_single_provider(self, provider_id: str) -> Dict:
            """Sync only models for a specific provider"""

        def _extract_provider_from_model(self, model_name: str, pricing: Dict) -> Dict:
            """Extract provider data from model pricing entry"""

        def _extract_model_from_pricing(self, model_name: str, pricing: Dict) -> Dict:
            """Extract model data from pricing entry"""

    **Singleton:**
    _auto_discovery_instance: Optional[ProviderAutoDiscovery] = None
    def get_auto_discovery() -> ProviderAutoDiscovery

    Import patterns from DynamicPricingFetcher: litellm_provider, mode, supports_cache
  </action>
  <verify>grep -n "class ProviderAutoDiscovery" backend/core/provider_auto_discovery.py && grep -n "async def sync_providers" backend/core/provider_auto_discovery.py</verify>
  <done>ProviderAutoDiscovery exists with sync_providers method and singleton</done>
</task>

<task type="auto">
  <name>Create Alembic migration and unit tests</name>
  <files>backend/alembic/versions/001_add_provider_registry.py, backend/tests/test_provider_registry.py</files>
  <action>
    **Alembic migration** (create new version file):
    - In backend/alembic/versions/ create: 001_add_provider_registry.py
    - Use alembic revision -m "add provider registry" if available, or create manually
    - upgrade(): Create provider_registry and model_catalog tables with indexes
    - downgrade(): Drop both tables
    - Follow existing migration patterns in backend/alembic/versions/

    **Unit tests** (test_provider_registry.py):
    1. test_create_provider(): Verify provider creation, field storage
    2. test_upsert_provider_updates_existing(): Verify update on duplicate provider_id
    3. test_create_model_with_relationship(): Verify foreign key to provider
    4. test_sync_providers_from_pricing_fetcher(): Mock DynamicPricingFetcher, call sync_providers
    5. test_list_providers_includes_model_count(): Verify aggregation
    6. test_search_models_by_capability(): Filter by supports_vision=True
    7. test_soft_delete_provider(): Verify is_active=False, models preserved

    Use pytest fixtures from existing test patterns:
    - db_session (in-memory SQLite)
    - mock_pricing_fetcher (Mock with pricing_cache dict)

    Run: pytest backend/tests/test_provider_registry.py -v
  </action>
  <verify>ls backend/alembic/versions/*provider_registry*.py && pytest backend/tests/test_provider_registry.py -v --collect-only</verify>
  <done>Migration file exists, tests pass with 7+ test cases</done>
</task>

</tasks>

<verification>
After plan completion:
1. Run alembic upgrade head to create tables
2. pytest backend/tests/test_provider_registry.py -v (all tests pass)
3. Verify provider_registry and model_catalog tables in schema
4. Test sync: python -c "from backend.core.provider_auto_discovery import get_auto_discovery; import asyncio; asyncio.run(get_auto_discovery().sync_providers())"
</verification>

<success_criteria>
1. ProviderRegistry and ModelCatalog tables created in database
2. ProviderAutoDiscovery.sync_providers() successfully populates tables from DynamicPricingFetcher
3. All 7+ unit tests pass
4. Migration can be applied and rolled back cleanly
5. Provider list endpoint returns providers with model counts
</success_criteria>

<output>
After completion, create `.planning/phases/226-llm-provider-registry/226-01-SUMMARY.md`
</output>
