---
phase: 124-episode-property-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/property_tests/episodes/test_episode_segmentation_invariants.py
  - backend/tests/property_tests/episodes/test_episode_retrieval_invariants.py
  - backend/tests/property_tests/episodes/test_episode_lifecycle_invariants.py
autonomous: true

must_haves:
  truths:
    - "All existing episode property tests pass with pytest"
    - "Segmentation invariants validated (time gaps > threshold trigger new episodes)"
    - "Retrieval invariants validated (feedback boosts score, recency matters)"
    - "Lifecycle invariants validated (active -> decayed -> consolidated -> archived transitions)"
    - "Hypothesis generates appropriate examples (200 for critical, 100 for retrieval, 50 for lifecycle)"
    - "No VALIDATED_BUG regressions (all documented bugs remain fixed)"
  artifacts:
    - path: "backend/tests/property_tests/episodes/test_episode_segmentation_invariants.py"
      provides: "Time gap, topic change, and task completion segmentation property tests"
      min_lines: 800
      contains: "TestTimeGapSegmentationInvariants, @settings(max_examples=200)"
    - path: "backend/tests/property_tests/episodes/test_episode_retrieval_invariants.py"
      provides: "Temporal, semantic, and feedback-boosted retrieval property tests"
      min_lines: 1000
      contains: "TestTemporalRetrievalInvariants, TestSemanticRetrievalInvariants, @settings(max_examples=100)"
    - path: "backend/tests/property_tests/episodes/test_episode_lifecycle_invariants.py"
      provides: "Decay, consolidation, and archival lifecycle property tests"
      min_lines: 400
      contains: "TestEpisodeDecayInvariants, TestEpisodeConsolidationInvariants, @settings(max_examples=50)"
  key_links:
    - from: "backend/tests/property_tests/episodes/test_episode_segmentation_invariants.py"
      to: "backend/core/episode_segmentation_service.py"
      via: "EpisodeBoundaryDetector.detect_time_gap()"
      pattern: "detect_time_gap.*time_gap.*threshold"
    - from: "backend/tests/property_tests/episodes/test_episode_retrieval_invariants.py"
      to: "backend/core/episode_retrieval_service.py"
      via: "retrieve_temporal(), retrieve_semantic(), retrieve_contextual()"
      pattern: "retrieve_(temporal|semantic|contextual)"
    - from: "backend/tests/property_tests/episodes/test_episode_lifecycle_invariants.py"
      to: "backend/core/episode_lifecycle_service.py"
      via: "decay_old_episodes(), consolidate_similar_episodes(), archive_to_cold_storage()"
      pattern: "(decay_old_episodes|consolidate_similar_episodes|archive_to_cold_storage)"
---

<objective>
Verify existing episode property tests meet Phase 124 success criteria and align max_examples settings with proven patterns from Phase 123.

**Purpose:** Phase 113 created extensive episode property tests (2358+ lines). Phase 124 must verify these tests meet success criteria: segmentation invariants, retrieval ranking invariants, and lifecycle transitions. Also ensure max_examples values align with roadmap requirements (100 for retrieval, 50 for lifecycle).

**Output:** Verified passing test suite with aligned settings, documented coverage gaps for expansion in Plan 02.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-episode-property-tests/124-RESEARCH.md
@.planning/phases/113-episodic-memory-coverage/113-RESEARCH.md
@.planning/phases/123-governance-property-tests/123-RESEARCH.md

# Existing property test files to verify
@backend/tests/property_tests/episodes/test_episode_segmentation_invariants.py
@backend/tests/property_tests/episodes/test_episode_retrieval_invariants.py
@backend/tests/property_tests/episodes/test_episode_lifecycle_invariants.py
</context>

<tasks>

<task type="auto">
  <name>Run existing episode property tests to verify they pass</name>
  <files>backend/tests/property_tests/episodes/</files>
  <action>
    Run all existing episode property tests to verify they pass.

    Command:
    ```bash
    cd /Users/rushiparikh/projects/atom/backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
      tests/property_tests/episodes/test_episode_segmentation_invariants.py \
      tests/property_tests/episodes/test_episode_retrieval_invariants.py \
      tests/property_tests/episodes/test_episode_lifecycle_invariants.py \
      -v --hypothesis-show-statistics
    ```

    Capture:
    - Total tests passed
    - Hypothesis statistics (examples generated per test)
    - Any failing tests or errors
    - Current max_examples settings used

    Expected outcome:
    - All tests pass (baseline from Phase 113)
    - Hypothesis shows example counts
  </action>
  <verify>pytest output shows all tests passing with Hypothesis statistics</verify>
  <done>All episode property tests pass, test counts and statistics documented</done>
</task>

<task type="auto">
  <name>Verify max_examples settings align with roadmap requirements</name>
  <files>backend/tests/property_tests/episodes/test_episode_segmentation_invariants.py
         backend/tests/property_tests/episodes/test_episode_retrieval_invariants.py
         backend/tests/property_tests/episodes/test_episode_lifecycle_invariants.py</files>
  <action>
    Check max_examples settings in all three property test files.

    Roadmap requirements (from Phase 124):
    - Segmentation invariants: max_examples=200 (critical invariant)
    - Retrieval invariants: max_examples=100 (roadmap requirement)
    - Lifecycle invariants: max_examples=50 (roadmap requirement)

    For each test file, grep @settings decorators:
    ```bash
    grep -n "@settings" backend/tests/property_tests/episodes/test_episode_segmentation_invariants.py
    grep -n "@settings" backend/tests/property_tests/episodes/test_episode_retrieval_invariants.py
    grep -n "@settings" backend/tests/property_tests/episodes/test_episode_lifecycle_invariants.py
    ```

    Document:
    1. Which tests use max_examples=200 (should be critical segmentation invariants)
    2. Which tests use max_examples=100 (should be retrieval invariants)
    3. Which tests use max_examples=50 (should be lifecycle invariants)
    4. Any misaligned settings that need updating

    Do NOT modify files yet - just document findings.
  </action>
  <verify>List of all @settings decorators with their max_examples values documented</verify>
  <done>max_examples settings audit complete, misalignments identified for fixing in Plan 02 if needed</done>
</task>

<task type="auto">
  <name>Verify segmentation invariants are covered</name>
  <files>backend/tests/property_tests/episodes/test_episode_segmentation_invariants.py</files>
  <action>
    Verify test_episode_segmentation_invariants.py covers all required segmentation invariants from Phase 124 success criteria:

    Required invariants:
    1. Time gap detection (gaps > threshold trigger new episodes)
    2. Topic change detection (semantic similarity triggers segmentation)
    3. Task completion detection (task_complete=True marks boundaries)
    4. Segment boundaries (disjoint, chronological, no overlaps)
    5. Information preservation (no events lost)
    6. Metadata integrity (required fields present)

    Check test class names and test methods:
    ```bash
    grep -E "class Test|def test_" backend/tests/property_tests/episodes/test_episode_segmentation_invariants.py
    ```

    Document coverage:
    - Which invariants have tests
    - Which invariants are missing (if any)
    - VALIDATED_BUG comments (regression prevention)
  </action>
  <verify>List of segmentation invariants with test coverage status (covered/missing)</verify>
  <done>Segmentation invariant coverage audit complete, gaps identified</done>
</task>

<task type="auto">
  <name>Verify retrieval ranking invariants are covered</name>
  <files>backend/tests/property_tests/episodes/test_episode_retrieval_invariants.py</files>
  <action>
    Verify test_episode_retrieval_invariants.py covers all required retrieval invariants from Phase 124 success criteria:

    Required invariants:
    1. Temporal retrieval (time filtering, chronological ordering)
    2. Semantic retrieval (similarity bounds, ranking order)
    3. Feedback boosting (positive feedback +0.2, negative feedback -0.3)
    4. Recency scoring (recent episodes ranked higher)
    5. Limit enforcement (result count <= limit)

    Check test class names and test methods:
    ```bash
    grep -E "class Test|def test_" backend/tests/property_tests/episodes/test_episode_retrieval_invariants.py
    ```

    Document coverage:
    - Which invariants have tests
    - Which invariants are missing (if any)
    - Feedback and recency boosting specifically verified
  </action>
  <verify>List of retrieval invariants with test coverage status (covered/missing)</verify>
  <done>Retrieval invariant coverage audit complete, gaps identified</done>
</task>

<task type="auto">
  <name>Verify lifecycle transition invariants are covered</name>
  <files>backend/tests/property_tests/episodes/test_episode_lifecycle_invariants.py</files>
  <action>
    Verify test_episode_lifecycle_invariants.py covers all required lifecycle transition invariants from Phase 124 success criteria:

    Required transitions:
    1. Active -> Decayed (importance decay over time)
    2. Decayed -> Consolidated (similar episodes merge)
    3. Consolidated -> Archived (cold storage movement)
    4. State machine integrity (no invalid transitions)
    5. Circular reference prevention (consolidation safety)

    Check test class names and test methods:
    ```bash
    grep -E "class Test|def test_" backend/tests/property_tests/episodes/test_episode_lifecycle_invariants.py
    ```

    Document coverage:
    - Which state transitions have tests
    - Which transitions are missing (if any)
    - Consolidation circular reference prevention verified
  </action>
  <verify>List of lifecycle transition invariants with test coverage status (covered/missing)</verify>
  <done>Lifecycle transition invariant coverage audit complete, gaps identified</done>
</task>

</tasks>

<verification>
1. All existing episode property tests pass (pytest exit code 0)
2. Hypothesis statistics show example counts matching max_examples settings
3. Segmentation invariants coverage documented (time gaps, topic changes, task completion, boundaries)
4. Retrieval invariants coverage documented (temporal, semantic, feedback boosting, recency)
5. Lifecycle invariants coverage documented (decay, consolidation, archival transitions)
6. max_examples settings audit complete (200 for critical, 100 for retrieval, 50 for lifecycle)
7. Coverage gaps documented for Plan 02 expansion
</verification>

<success_criteria>
1. All existing episode property tests pass without errors
2. Segmentation invariants validated (time gaps > threshold trigger new episodes)
3. Retrieval ranking invariants validated (feedback boosts score, recency matters)
4. Lifecycle transitions validated (active -> decayed -> consolidated -> archived)
5. max_examples settings documented (200 for critical, 100 for retrieval, 50 for lifecycle)
6. Coverage gaps identified for Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/124-episode-property-tests/124-01-SUMMARY.md` with:
- Test execution results (pass/fail counts)
- Hypothesis statistics (examples generated)
- max_examples settings audit
- Invariant coverage audit (segmentation, retrieval, lifecycle)
- Identified coverage gaps for Plan 02
</output>
