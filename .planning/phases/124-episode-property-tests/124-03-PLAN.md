---
phase: 124-episode-property-tests
plan: 03
type: execute
wave: 2
depends_on: ["124-01", "124-02"]
files_modified:
  - backend/tests/property_tests/episodes/
autonomous: true

must_haves:
  truths:
    - "All Phase 124 success criteria validated"
    - "Segmentation invariants validated (episodes segment on time gaps, topic changes)"
    - "Retrieval ranking invariants validated (feedback boosts score, recency matters)"
    - "Lifecycle transitions validated (active -> decayed -> consolidated -> archived)"
    - "max_examples settings correct (100 for retrieval, 50 for lifecycle)"
    - "Coverage gap filling tests added and passing"
    - "No regressions in existing tests"
  artifacts:
    - path: "backend/tests/coverage_reports/metrics/phase_124_property_tests_coverage.json"
      provides: "Coverage metrics for Phase 124 property tests"
      contains: "test_count, examples_generated, invariants_validated, coverage_percentage"
  key_links:
    - from: ".planning/phases/124-episode-property-tests/124-03-SUMMARY.md"
      to: ".planning/REQUIREMENTS.md"
      via: "PROP-02 requirement completion"
      pattern: "PROP-02.*complete"
    - from: ".planning/phases/124-episode-property-tests/124-03-SUMMARY.md"
      to: ".planning/ROADMAP.md"
      via: "Phase 124 completion"
      pattern: "Phase 124.*complete"
---

<objective>
Verify Phase 124 completion against success criteria and document final results.

**Purpose:** Plans 01 and 02 expanded episode property tests. Plan 03 runs final verification to confirm all Phase 124 success criteria are met: segmentation invariants validated, retrieval ranking invariants validated, lifecycle transitions validated, max_examples settings appropriate.

**Output:** Phase 124 completion summary with all success criteria validated.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-episode-property-tests/124-RESEARCH.md
@.planning/phases/124-episode-property-tests/124-01-SUMMARY.md
@.planning/phases/124-episode-property-tests/124-02-SUMMARY.md

# All episode property test files
@backend/tests/property_tests/episodes/
</context>

<tasks>

<task type="auto">
  <name>Run full episode property test suite with Hypothesis statistics</name>
  <files>backend/tests/property_tests/episodes/</files>
  <action>
    Run all episode property tests with Hypothesis statistics enabled.

    Command:
    ```bash
    cd /Users/rushiparikh/projects/atom/backend
    PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest \
      tests/property_tests/episodes/ \
      -v --hypothesis-show-statistics --tb=short \
      | tee /tmp/phase_124_test_output.txt
    ```

    Capture and document:
    - Total test count
    - Tests passed vs failed
    - Hypothesis statistics (examples generated per test)
    - Execution time
    - Any warnings or errors

    Parse output for key metrics:
    - Segmentation test count and examples
    - Retrieval test count and examples
    - Lifecycle test count and examples
  </action>
  <verify>pytest exit code 0, all tests passing, statistics captured</verify>
  <done>Full test suite executed with results documented</done>
</task>

<task type="auto">
  <name>Validate segmentation invariants success criterion</name>
  <files>backend/tests/property_tests/episodes/test_episode_segmentation_invariants.py</files>
  <action>
    Verify Phase 124 success criterion: "Property tests validate segmentation invariants (episodes segment on time gaps, topic changes)"

    For each required segmentation invariant:
    1. Time gap detection: grep test methods for "time_gap"
    2. Topic change detection: grep test methods for "topic_change"
    3. Task completion detection: grep test methods for "task_completion"

    Command:
    ```bash
    echo "=== Segmentation Invariant Coverage ==="
    echo "Time gap tests:"
    grep -c "def test.*time_gap" tests/property_tests/episodes/test_episode_segmentation_invariants.py || echo "0"
    echo "Topic change tests:"
    grep -c "def test.*topic" tests/property_tests/episodes/test_episode_segmentation_invariants.py || echo "0"
    echo "Task completion tests:"
    grep -c "def test.*task" tests/property_tests/episodes/test_episode_segmentation_invariants.py || echo "0"
    ```

    Document:
    - Which invariants have tests (names of test methods)
    - Whether tests use @example decorators for boundary cases
    - Whether max_examples=200 for critical time gap tests

    Required: All three invariants must have passing tests.
  </action>
  <verify>List of segmentation invariant tests with coverage confirmed</verify>
  <done>Segmentation invariants validated: time gaps, topic changes, task completion all have tests</done>
</task>

<task type="auto">
  <name>Validate retrieval ranking invariants success criterion</name>
  <files>backend/tests/property_tests/episodes/test_episode_retrieval_invariants.py</files>
  <action>
    Verify Phase 124 success criterion: "Property tests validate retrieval ranking invariants (feedback boosts score, recency matters)"

    For each required retrieval invariant:
    1. Feedback boosting: grep test methods for "feedback"
    2. Recency scoring: grep test methods for "recency"
    3. Combined feedback+recency: grep test methods for "combined"

    Command:
    ```bash
    echo "=== Retrieval Ranking Invariant Coverage ==="
    echo "Feedback tests:"
    grep -c "def test.*feedback" tests/property_tests/episodes/test_episode_retrieval_invariants.py || echo "0"
    echo "Recency tests:"
    grep -c "def test.*recency" tests/property_tests/episodes/test_episode_retrieval_invariants.py || echo "0"
    echo "Combined tests:"
    grep -c "def test.*combined" tests/property_tests/episodes/test_episode_retrieval_invariants.py || echo "0"
    ```

    Document:
    - Which invariants have tests (names of test methods)
    - Whether tests use @example decorators for boundary cases
    - Whether max_examples=100 for retrieval tests (roadmap requirement)

    Required: Feedback and recency must have tests.
  </action>
  <verify>List of retrieval ranking invariant tests with coverage confirmed</verify>
  <done>Retrieval ranking invariants validated: feedback boosts, recency scoring both have tests</done>
</task>

<task type="auto">
  <name>Validate lifecycle transitions success criterion</name>
  <files>backend/tests/property_tests/episodes/test_episode_lifecycle_invariants.py</files>
  <action>
    Verify Phase 124 success criterion: "Property tests validate lifecycle transitions (active -> decayed -> consolidated -> archived)"

    For each required lifecycle transition:
    1. Decay: grep test methods for "decay"
    2. Consolidation: grep test methods for "consolidat"
    3. Archival: grep test methods for "archiv"
    4. State transitions: grep test methods for "transition"

    Command:
    ```bash
    echo "=== Lifecycle Transition Invariant Coverage ==="
    echo "Decay tests:"
    grep -c "def test.*decay" tests/property_tests/episodes/test_episode_lifecycle_invariants.py || echo "0"
    echo "Consolidation tests:"
    grep -c "def test.*consolidat" tests/property_tests/episodes/test_episode_lifecycle_invariants.py || echo "0"
    echo "Archival tests:"
    grep -c "def test.*archiv" tests/property_tests/episodes/test_episode_lifecycle_invariants.py || echo "0"
    echo "Transition tests:"
    grep -c "def test.*transition" tests/property_tests/episodes/test_episode_lifecycle_invariants.py || echo "0"
    ```

    Document:
    - Which transitions have tests (names of test methods)
    - Whether state machine flow is validated
    - Whether max_examples=50 for lifecycle tests (roadmap requirement)

    Required: All three transitions must have tests.
  </action>
  <verify>List of lifecycle transition tests with coverage confirmed</verify>
  <done>Lifecycle transitions validated: decay, consolidation, archival all have tests</done>
</task>

<task type="auto">
  <name>Verify max_examples settings match roadmap requirements</name>
  <files>backend/tests/property_tests/episodes/</files>
  <action>
    Verify Phase 124 success criterion: "All episode property tests use appropriate max_examples (100 for retrieval, 50 for lifecycle)"

    Roadmap requirements:
    - Segmentation: max_examples=200 (critical invariant)
    - Retrieval: max_examples=100
    - Lifecycle: max_examples=50

    Audit all @settings decorators:
    ```bash
    echo "=== max_examples Settings Audit ==="
    echo "Segmentation (should be 200):"
    grep "@settings" tests/property_tests/episodes/test_episode_segmentation_invariants.py | grep -o "max_examples=[0-9]*" | sort | uniq -c

    echo "Retrieval (should be 100):"
    grep "@settings" tests/property_tests/episodes/test_episode_retrieval_invariants.py | grep -o "max_examples=[0-9]*" | sort | uniq -c

    echo "Lifecycle (should be 50):"
    grep "@settings" tests/property_tests/episodes/test_episode_lifecycle_invariants.py | grep -o "max_examples=[0-9]*" | sort | uniq -c
    ```

    Document:
    - Current max_examples distribution per file
    - Any settings that don't match roadmap requirements
    - Whether Plan 02 corrections were applied

    Required: Retrieval tests use 100, lifecycle tests use 50.
  </action>
  <verify>max_examples settings audit shows alignment with roadmap</verify>
  <done>max_examples settings verified: retrieval=100, lifecycle=50, segmentation=200</done>
</task>

<task type="auto">
  <name>Generate coverage report and create summary document</name>
  <files>
    backend/tests/coverage_reports/metrics/phase_124_property_tests_coverage.json
    .planning/phases/124-episode-property-tests/124-03-SUMMARY.md
  </files>
  <action>
    Generate coverage report for Phase 124 property tests and create summary document.

    Create coverage metrics JSON:
    ```bash
    mkdir -p /Users/rushiparikh/projects/atom/backend/tests/coverage_reports/metrics

    cat > /Users/rushiparikh/projects/atom/backend/tests/coverage_reports/metrics/phase_124_property_tests_coverage.json << 'EOF'
    {
      "phase": "124-episode-property-tests",
      "completed_at": "2026-03-02",
      "success_criteria": {
        "segmentation_invariants_validated": true,
        "retrieval_ranking_invariants_validated": true,
        "lifecycle_transitions_validated": true,
        "max_examples_aligned": true
      },
      "test_counts": {
        "segmentation_tests": <COUNT>,
        "retrieval_tests": <COUNT>,
        "lifecycle_tests": <COUNT>,
        "total_property_tests": <COUNT>
      },
      "hypothesis_examples": {
        "segmentation_total": <EXAMPLES>,
        "retrieval_total": <EXAMPLES>,
        "lifecycle_total": <EXAMPLES>
      },
      "max_examples_distribution": {
        "max_200": <COUNT>,
        "max_100": <COUNT>,
        "max_50": <COUNT>
      },
      "coverage_gaps_filled": {
        "edge_case_tests": <COUNT>,
        "feedback_recency_tests": <COUNT>,
        "state_transition_tests": <COUNT>
      }
    }
    EOF
    ```

    Fill in actual counts from test execution.

    Then create SUMMARY.md documenting:
    - Phase 124 completion status
    - Success criteria validation results
    - Test counts and Hypothesis statistics
    - Coverage gaps filled
    - Recommendations for future phases
  </action>
  <verify>Coverage JSON file exists with valid JSON, SUMMARY.md created</verify>
  <done>Coverage report generated, summary document created</done>
</task>

</tasks>

<verification>
1. All episode property tests pass (exit code 0)
2. Segmentation invariants validated (time gaps, topic changes have tests)
3. Retrieval ranking invariants validated (feedback, recency have tests)
4. Lifecycle transitions validated (decay, consolidation, archival have tests)
5. max_examples settings correct (100 for retrieval, 50 for lifecycle, 200 for critical segmentation)
6. Coverage report JSON created
7. Summary document created
</verification>

<success_criteria>
1. Property tests validate segmentation invariants (episodes segment on time gaps, topic changes)
2. Property tests validate retrieval ranking invariants (feedback boosts score, recency matters)
3. Property tests validate lifecycle transitions (active -> decayed -> consolidated -> archived)
4. All episode property tests use appropriate max_examples (100 for retrieval, 50 for lifecycle) - verified via --hypothesis-show-statistics
5. Coverage report and summary created
6. Phase 124 marked complete in ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/124-episode-property-tests/124-03-SUMMARY.md` with:
- Phase 124 completion confirmation
- All success criteria validated
- Test counts and Hypothesis statistics
- Coverage metrics JSON location
- Update REQUIREMENTS.md to mark PROP-02 complete
- Update ROADMAP.md to mark Phase 124 complete
</output>
