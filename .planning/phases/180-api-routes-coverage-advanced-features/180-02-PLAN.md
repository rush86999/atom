---
phase: 180-api-routes-coverage-advanced-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_artifact_routes_coverage.py
autonomous: true

must_haves:
  truths:
    - "All 5 artifact endpoints have success path tests"
    - "All 5 artifact endpoints have error path tests"
    - "Authentication dependency (get_current_user) is properly overridden"
    - "Database dependency (get_db) uses StaticPool pattern"
    - "Artifact versioning is tested (version records created on update)"
    - "75%+ line coverage achieved on artifact_routes.py"
  artifacts:
    - path: "backend/tests/api/test_artifact_routes_coverage.py"
      provides: "Artifact route test suite with 20+ tests"
      min_lines: 380
      exports: ["TestArtifactList", "TestArtifactSave", "TestArtifactUpdate", "TestArtifactVersions", "TestArtifactAuth", "TestArtifactErrorPaths"]
  key_links:
    - from: "test_artifact_routes_coverage.py"
      to: "backend/api/artifact_routes.py"
      via: "import router in TestClient app"
      pattern: "from api.artifact_routes import router"
    - from: "test_artifact_routes_coverage.py"
      to: "backend/core/database.py"
      via: "dependency_overrides[get_db]"
      pattern: "app.dependency_overrides[get_db] = override_get_db"
    - from: "test_artifact_routes_coverage.py"
      to: "backend/core/security_dependencies.py"
      via: "dependency_overrides[get_current_user]"
      pattern: "app.dependency_overrides[get_current_user] = override_get_current_user"
---

<objective>
Create comprehensive test coverage for artifact version control routes (artifact_routes.py).

**Purpose**: Validate artifact CRUD with version control, authentication, and database integration with 75%+ line coverage.

**Output**: test_artifact_routes_coverage.py with 20+ tests covering all 5 endpoints, versioning, auth, and error paths.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/180-api-routes-coverage-advanced-features/180-RESEARCH.md

# Source files to test
@backend/api/artifact_routes.py  # 130 lines, 5 endpoints
@backend/core/models.py  # Artifact, ArtifactVersion models

# Test patterns from Phase 178
@backend/tests/api/test_admin_system_health_routes.py  # StaticPool pattern, auth override
@backend/tests/api/test_auto_install_routes_coverage.py  # DB override pattern
</context>

<tasks>

<task type="auto">
  <name>Create artifact test fixtures with database</name>
  <files>backend/tests/api/test_artifact_routes_coverage.py</files>
  <action>
Create test fixtures at the top of the file:

1. **test_db()** - In-memory SQLite database with StaticPool (scope="function"):
   - Use sqlalchemy.create_engine with sqlite:///:memory:
   - Add connect_args={"check_same_thread": False}
   - Use poolclass=StaticPool (prevents locking issues)
   - Create all tables with Base.metadata.create_all(engine)
   - Create TestingSessionLocal with sessionmaker
   - Yield db, close in finally

2. **mock_user()** - Mock User object (NOT real model - avoids SQLAlchemy relationship issues):
   - Create MagicMock(spec=["id", "email", "created_at"])
   - Set id="test_user_1", email="test@example.com"

3. **authenticated_client()** - TestClient with auth and DB overrides:
   - Import router from api.artifact_routes
   - Create FastAPI app, include router
   - Override get_current_user to return mock_user
   - Override get_db to yield test_db
   - Return TestClient(app)
   - Clear dependency_overrides in cleanup

4. **unauthenticated_client()** - TestClient without auth override:
   - Create FastAPI app, include router
   - Override get_db to yield test_db
   - DO NOT override get_current_user (triggers 401)
   - Clear dependency_overrides in cleanup

5. **sample_artifact_data()** - Factory for valid ArtifactCreate dict

6. **sample_artifact()** - Artifact model fixture for database insertion

Reference Phase 178 StaticPool pattern. Use mock User to avoid relationship issues.
  </action>
  <verify>File created with 6+ fixtures, imports resolve, StaticPool configured</verify>
  <done>Fixtures provide test database and auth contexts for all tests</done>
</task>

<task type="auto">
  <name>Implement TestArtifactList class</name>
  <files>backend/tests/api/test_artifact_routes_coverage.py</files>
  <action>
Create TestArtifactList class with 4 tests:

1. test_list_artifacts_success - GET /api/artifacts/ with authentication
   - Insert 3 test artifacts into test_db
   - Verify response 200, list returned
   - Verify all artifacts present in response

2. test_list_artifacts_filter_by_session - GET /api/artifacts/?session_id=xyz
   - Insert artifacts with different session_id values
   - Verify only matching session_id artifacts returned

3. test_list_artifacts_filter_by_type - GET /api/artifacts/?type=chart
   - Insert artifacts with different types
   - Verify only matching type artifacts returned

4. test_list_artifacts_combined_filters - GET /api/artifacts/?session_id=xyz&type=chart
   - Insert artifacts with mixed session_id and type
   - Verify both filters applied correctly

5. test_list_artifacts_empty - GET /api/artifacts/ with empty database
   - Verify empty list returned, not error

Use authenticated_client fixture. Insert artifacts directly into test_db.
  </action>
  <verify>5 tests created, list endpoint filters tested</verify>
  <done>Artifact listing with all filter combinations tested</done>
</task>

<task type="auto">
  <name>Implement TestArtifactSave class</name>
  <files>backend/tests/api/test_artifact_routes_coverage.py</files>
  <action>
Create TestArtifactSave class with 4 tests:

1. test_save_artifact_success - POST /api/artifacts/ with valid data
   - Use sample_artifact_data fixture
   - Verify response 200
   - Verify artifact.id generated (UUID format)
   - Verify artifact.name matches request
   - Verify artifact.version == 1

2. test_save_artifact_with_agent_id - POST /api/artifacts/ with agent_id
   - Include agent_id in request
   - Verify agent_id saved correctly

3. test_save_artifact_with_metadata - POST /api/artifacts/ with metadata_json
   - Include metadata dict in request
   - Verify metadata saved correctly

4. test_save_artifact_minimal - POST /api/artifacts/ with only required fields
   - Omit optional fields (session_id, agent_id)
   - Verify artifact created with defaults

5. test_save_artifact_author_assigned - POST /api/artifacts/ checks author_id
   - Verify author_id set to authenticated user ID

Use authenticated_client. Query test_db to verify database state.
  </action>
  <verify>5 tests created, save endpoint tested with all field combinations</verify>
  <done>Artifact creation tested with all optional fields</done>
</task>

<task type="auto">
  <name>Implement TestArtifactUpdate class</name>
  <files>backend/tests/api/test_artifact_routes_coverage.py</files>
  <action>
Create TestArtifactUpdate class with 5 tests:

1. test_update_artifact_name - POST /api/artifacts/update with name change
   - Create artifact in test_db
   - POST update with new name
   - Verify name updated, version incremented to 2

2. test_update_artifact_content - POST /api/artifacts/update with content change
   - Create artifact in test_db
   - POST update with new content
   - Verify content updated, version incremented

3. test_update_artifact_metadata - POST /api/artifacts/update with metadata_json change
   - Create artifact in test_db
   - POST update with new metadata
   - Verify metadata updated, version incremented

4. test_update_creates_version_record - POST /api/artifacts/update checks version history
   - Create artifact with version=1, content="v1"
   - POST update to content="v2"
   - Verify artifact.version == 2
   - Verify ArtifactVersion record created in test_db
   - Verify version.version == 1 (old version stored)
   - Verify version.content == "v1" (old content stored)

5. test_update_multiple_fields - POST /api/artifacts/update with name, content, metadata
   - Create artifact in test_db
   - POST update with all fields changed
   - Verify all fields updated, version incremented
   - Verify single ArtifactVersion created

Test the critical versioning feature: update creates version record.
  </action>
  <verify>5 tests created, update and versioning tested</verify>
  <done>Artifact updates tested with version record creation</done>
</task>

<task type="auto">
  <name>Implement TestArtifactVersions class</name>
  <files>backend/tests/api/test_artifact_routes_coverage.py</files>
  <action>
Create TestArtifactVersions class with 3 tests:

1. test_get_artifact_versions_success - GET /api/artifacts/{id}/versions
   - Create artifact in test_db
   - Update artifact twice (creates 2 version records)
   - GET versions endpoint
   - Verify 2 versions returned
   - Verify versions ordered by version desc

2. test_get_artifact_versions_empty - GET /api/artifacts/{id}/versions with no updates
   - Create artifact, never update
   - Verify empty list returned

3. test_get_artifact_versions_content_preserved - GET versions checks content storage
   - Create artifact with content="original"
   - Update to content="updated"
   - GET versions endpoint
   - Verify version.content == "original" for version 1
   - Verify current artifact.content == "updated"

Test version history retrieval and content preservation.
  </action>
  <verify>3 tests created, version history retrieval tested</verify>
  <done>Artifact versioning endpoint tested</done>
</task>

<task type="auto">
  <name>Implement TestArtifactAuth class</name>
  <files>backend/tests/api/test_artifact_routes_coverage.py</files>
  <action>
Create TestArtifactAuth class with 2 tests:

1. test_list_requires_auth - GET /api/artifacts/ without authentication
   - Use unauthenticated_client (no get_current_user override)
   - Verify response 401 Unauthorized

2. test_save_requires_auth - POST /api/artifacts/ without authentication
   - Use unauthenticated_client
   - Verify response 401 Unauthorized

3. test_update_requires_auth - POST /api/artifacts/update without authentication
   - Use unauthenticated_client
   - Verify response 401 Unauthorized

4. test_versions_requires_auth - GET /api/artifacts/{id}/versions without authentication
   - Use unauthenticated_client
   - Verify response 401 Unauthorized

All artifact endpoints require authentication via get_current_user dependency.
  </action>
  <verify>4 tests created, authentication required for all endpoints</verify>
  <done>Authentication tested for all 5 endpoints</done>
</task>

<task type="auto">
  <name>Implement TestArtifactErrorPaths class</name>
  <files>backend/tests/api/test_artifact_routes_coverage.py</files>
  <action>
Create TestArtifactErrorPaths class with 5 tests:

1. test_save_missing_name - POST /api/artifacts/ without name field
   - Verify 422 validation error

2. test_save_missing_type - POST /api/artifacts/ without type field
   - Verify 422 validation error

3. test_update_artifact_not_found - POST /api/artifacts/update with invalid ID
   - Use non-existent artifact ID
   - Verify 404 not found error

4. test_update_empty_request - POST /api/artifacts/update with no changes
   - Create artifact, POST update with empty body
   - Verify validation or no change response

5. test_get_versions_not_found - GET /api/artifacts/{invalid_id}/versions
   - Use non-existent artifact ID
   - Verify empty list or 404 (check actual behavior)

6. test_update_invalid_id_format - POST /api/artifacts/update with malformed ID
   - Test error handling for invalid ID format

Cover validation errors (422), not found errors (404).
  </action>
  <verify>6 tests created, error paths tested for all endpoints</verify>
  <done>Comprehensive error testing validates API-03 requirement</done>
</task>

<task type="auto">
  <name>Verify artifact coverage and finalize</name>
  <files>backend/tests/api/test_artifact_routes_coverage.py</files>
  <action>
Run coverage verification:

```bash
cd /Users/rushiparikh/projects/atom/backend
pytest tests/api/test_artifact_routes_coverage.py --cov=api/artifact_routes --cov-report=term-missing --cov-report=json -v
```

Verify:
1. 20+ tests total
2. 75%+ line coverage achieved
3. All tests passing
4. No unexpected failures

If coverage <75%, add tests for missing lines:
- Test workspace_id="default" filtering
- Test updated_at timestamp behavior
- Test is_locked field (if present in model)
- Test additional edge cases

Add file header docstring with target, goal, endpoints covered.
  </action>
  <verify>Coverage report shows 75%+ on artifact_routes.py, all tests pass</verify>
  <done>Artifact routes have comprehensive test coverage meeting phase goal</done>
</task>

</tasks>

<verification>
After completion, verify:
1. File exists: backend/tests/api/test_artifact_routes_coverage.py
2. Test count: 20+ tests
3. Line coverage: 75%+ on api/artifact_routes.py
4. All tests pass: pytest exit code 0
5. StaticPool pattern used for test database
6. Authentication dependency properly overridden
7. Artifact versioning tested with database queries
</verification>

<success_criteria>
1. All 5 artifact endpoints have success path tests
2. All 5 artifact endpoints have error path tests
3. Authentication tested for all endpoints
4. Artifact version creation validated
5. 75%+ line coverage achieved on artifact_routes.py
6. All tests passing with pytest
</success_criteria>

<output>
After completion, create `.planning/phases/180-api-routes-coverage-advanced-features/180-02-SUMMARY.md` with:
- Number of tests created
- Coverage percentage achieved
- Any deviations from plan
- File with line count
</output>
