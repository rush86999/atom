---
phase: 180-api-routes-coverage-advanced-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_apar_routes_coverage.py
autonomous: true

must_haves:
  truths:
    - "All 14 APAR endpoints have success path tests"
    - "All 14 APAR endpoints have error path tests"
    - "PDF download endpoints (AP and AR) are testable without reportlab"
    - "Invoice status transitions are validated (draft → pending → approved → paid)"
    - "APAREngine is properly mocked with MagicMock"
    - "75%+ line coverage achieved on apar_routes.py"
  artifacts:
    - path: "backend/tests/api/test_apar_routes_coverage.py"
      provides: "APAR route test suite with 45+ tests"
      min_lines: 550
      exports: ["TestAPARSuccess", "TestAPARIntake", "TestAPARApproval", "TestARGenerate", "TestARSend", "TestARPaid", "TestAROverdue", "TestARPDFDownload", "TestAPARErrorPaths", "TestARPDFDownload", "TestARReminders", "TestARSummary", "TestAllInvoices"]
  key_links:
    - from: "test_apar_routes_coverage.py"
      to: "backend/api/apar_routes.py"
      via: "import router in TestClient app"
      pattern: "from api.apar_routes import router"
    - from: "test_apar_routes_coverage.py"
      to: "backend/core/apar_engine.py"
      via: "patch('api.apar_routes.apar_engine')"
      pattern: "patch('api.apar_routes.apar_engine')"
---

<objective>
Create comprehensive test coverage for AP/AR invoice automation routes (apar_routes.py).

**Purpose**: Validate accounts payable (AP) and accounts receivable (AR) invoice automation endpoints with 75%+ line coverage.

**Output**: test_apar_routes_coverage.py with 45+ tests covering all 14 endpoints, invoice lifecycle, PDF downloads, and error paths.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/180-api-routes-coverage-advanced-features/180-RESEARCH.md

# Source files to test
@backend/api/apar_routes.py  # 241 lines, 14 endpoints
@backend/core/apar_engine.py  # APAREngine service (mock target)

# Test patterns from Phase 177-179
@backend/tests/api/test_ai_workflows_routes_coverage.py  # AsyncMock pattern
@backend/tests/api/test_auto_install_routes_coverage.py  # Service mocking pattern
</context>

<tasks>

<task type="auto">
  <name>Create APAR test fixtures</name>
  <files>backend/tests/api/test_apar_routes_coverage.py</files>
  <action>
Create test fixtures at the top of the file:

1. **mock_apar_engine()** - MagicMock for APAREngine with:
   - intake_invoice returns APInvoice with id, vendor, amount, status, approved_by
   - approve_invoice returns APInvoice with status=approved
   - get_pending_approvals returns list of pending APInvoice objects
   - get_upcoming_payments returns list of upcoming APInvoice objects
   - generate_invoice returns ARInvoice with id, customer, amount, status
   - send_invoice returns ARInvoice with status=sent
   - mark_paid returns ARInvoice with status=paid
   - get_overdue_invoices returns list of overdue ARInvoice objects
   - get_all_invoices returns mixed list of APInvoice and ARInvoice objects
   - generate_reminder returns dict with reminder text
   - get_collection_summary returns dict with metrics
   - generate_invoice_pdf returns b"%PDF-1.4 fake pdf content" (avoid reportlab dependency)

2. **apar_client()** - TestClient with isolated FastAPI app:
   - Import router from api.apar_routes
   - Include router in new FastAPI app
   - Patch 'api.apar_routes.apar_engine' with mock_apar_engine
   - Return TestClient(app)

3. **sample_ap_intake_request()** - Factory for valid APIntakeRequest data

4. **sample_ar_generate_request()** - Factory for valid ARGenerateRequest data

5. **sample_ap_invoice()** - APInvoice fixture for invoice object assertions

6. **sample_ar_invoice()** - ARInvoice fixture for invoice object assertions

Reference Phase 177/179 fixture patterns. Use MagicMock (not AsyncMock) for APAREngine - all methods are synchronous.
  </action>
  <verify>File created with 6+ fixtures, all imports resolve</verify>
  <done>Fixtures provide deterministic mock responses for all APAREngine methods</done>
</task>

<task type="auto">
  <name>Implement TestAPARSuccess class</name>
  <files>backend/tests/api/test_apar_routes_coverage.py</files>
  <action>
Create TestAPARSuccess class with 7 happy path tests:

1. test_intake_ap_invoice_success - POST /apar/ap/intake with vendor, amount, due_date
   - Verify response 200, id returned, vendor matches, auto_approved flag correct

2. test_intake_ap_invoice_auto_approved - POST /apar/ap/intake with amount < 500
   - Verify auto_approved=True (threshold behavior)

3. test_intake_ap_invoice_manual_approval - POST /apar/ap/intake with amount > 500
   - Verify auto_approved=False (requires manual approval)

4. test_approve_ap_invoice_success - POST /apar/ap/{invoice_id}/approve
   - Verify status=approved, id returned

5. test_get_pending_approvals_success - GET /apar/ap/pending
   - Verify count returned, invoices list present

6. test_get_upcoming_payments_default - GET /apar/ap/upcoming (default 7 days)
   - Verify count, total_due calculated, invoices with due_date

7. test_get_upcoming_payments_custom_days - GET /apar/ap/upcoming?days=30
   - Verify custom days parameter works

Use sample_ap_intake_request fixture. Assert response.json()["success"] is True.
  </action>
  <verify>7 tests created, all endpoint paths correct (/apar/ap/*)</verify>
  <done>All AP success paths tested with proper assertions</done>
</task>

<task type="auto">
  <name>Implement TestARGenerate class</name>
  <files>backend/tests/api/test_apar_routes_coverage.py</files>
  <action>
Create TestARGenerate class with 4 happy path tests:

1. test_generate_ar_invoice_success - POST /apar/ar/generate with customer, amount
   - Verify response 200, id returned, customer matches

2. test_send_ar_invoice_success - POST /apar/ar/{invoice_id}/send
   - Verify status=sent, id returned

3. test_mark_ar_paid_success - POST /apar/ar/{invoice_id}/paid
   - Verify status=paid, id returned

4. test_get_overdue_invoices_success - GET /apar/ar/overdue
   - Verify count returned, invoices list with customer, amount

Use sample_ar_generate_request fixture. Test invoice lifecycle: generate → send → paid.
  </action>
  <verify>4 tests created, all endpoint paths correct (/apar/ar/*)</verify>
  <done>All AR success paths tested covering invoice lifecycle</done>
</task>

<task type="auto">
  <name>Implement TestARPDFDownload class</name>
  <files>backend/tests/api/test_apar_routes_coverage.py</files>
  <action>
Create TestARPDFDownload class with 4 tests:

1. test_download_ar_invoice_success - GET /apar/ar/{invoice_id}/download
   - Verify response 200
   - Verify content-type="application/pdf"
   - Verify "attachment" in content-disposition header
   - Verify b"fake pdf content" in response.content

2. test_download_ap_invoice_success - GET /apar/ap/{invoice_id}/download
   - Verify response 200, content-type, headers, PDF content

3. test_download_ar_invoice_not_found - Mock raises ValueError
   - Configure mock_apar_engine.generate_invoice_pdf.side_effect = ValueError("Invoice not found")
   - Verify response 404, error detail present

4. test_download_ap_invoice_reportlab_missing - Mock raises ImportError
   - Configure mock_apar_engine.generate_invoice_pdf.side_effect = ImportError("reportlab not installed")
   - Verify response 500, error detail mentions reportlab

Mock returns b"%PDF-1.4 fake pdf content" to avoid requiring reportlab installation.
  </action>
  <verify>4 tests created, PDF download paths covered, error paths tested</verify>
  <done>PDF download endpoints tested with mocked reportlab</done>
</task>

<task type="auto">
  <name>Implement TestARReminders and TestARSummary classes</name>
  <files>backend/tests/api/test_apar_routes_coverage.py</files>
  <action>
Create TestARReminders class with 3 tests:

1. test_send_reminder_success - POST /apar/ar/{invoice_id}/remind
   - Verify reminder dict returned

2. test_send_reminder_friendly_tone - POST /apar/ar/{invoice_id}/remind with tone query
   - Verify reminder contains friendly language

3. test_send_reminder_firm_tone - POST /apar/ar/{invoice_id}/remind with firm tone
   - Verify reminder contains firm language

Create TestARSummary class with 2 tests:

1. test_get_collection_summary_success - GET /apar/summary
   - Verify summary dict returned with metrics (total_outstanding, etc.)

2. test_get_collection_summary_empty - Mock returns empty summary
   - Verify zero values returned

Mock generate_reminder to return dict with reminder_text and tone fields.
  </action>
  <verify>5 tests created, reminders and summary endpoints covered</verify>
  <done>AR collections features tested</done>
</task>

<task type="auto">
  <name>Implement TestAllInvoices class</name>
  <files>backend/tests/api/test_apar_routes_coverage.py</files>
  <action>
Create TestAllInvoices class with 3 tests:

1. test_get_all_invoices_mixed - GET /apar/all
   - Mock get_all_invoices returns mix of APInvoice and ARInvoice
   - Verify response 200, count calculated correctly
   - Verify both "customer" (AR) and "vendor" (AP) fields present
   - Verify type field correctly identifies "AR" vs "AP"

2. test_get_all_invoices_ap_only - GET /apar/all with only AP invoices
   - Verify vendor fields present, customer fields null

3. test_get_all_invoices_ar_only - GET /apar/all with only AR invoices
   - Verify customer fields present, vendor fields null

Test uses hasattr(inv, "customer") logic from route to distinguish invoice types.
  </action>
  <verify>3 tests created, all invoices aggregation endpoint covered</verify>
  <done>Combined AP/AR endpoint tested with type discrimination</done>
</task>

<task type="auto">
  <name>Implement TestAPARErrorPaths class</name>
  <files>backend/tests/api/test_apar_routes_coverage.py</files>
  <action>
Create TestAPARErrorPaths class with 12 tests:

1. test_intake_invoice_missing_vendor - POST /apar/ap/intake without vendor
   - Verify 422 validation error

2. test_intake_invoice_negative_amount - POST /apar/ap/intake with negative amount
   - Verify error handling (may process or validate)

3. test_intake_invoice_invalid_date_format - POST /apar/ap/intake with invalid due_date
   - Verify error handling (datetime.fromisoformat may raise)

4. test_approve_invoice_not_found - POST /apar/ap/{invalid_id}/approve
   - Mock raises ValueError, verify 404 or error response

5. test_approve_invoice_already_approved - Approve already approved invoice
   - Verify idempotent or error response

6. test_generate_ar_missing_customer - POST /apar/ar/generate without customer
   - Verify 422 validation error

7. test_send_invoice_not_found - POST /apar/ar/{invalid_id}/send
   - Mock raises ValueError, verify error response

8. test_mark_paid_already_paid - Mark already paid invoice as paid
   - Verify idempotent or error response

9. test_get_pending_approvals_empty - Mock returns empty list
   - Verify count=0, empty invoices list

10. test_get_upcoming_payments_negative_days - GET /apar/ap/upcoming?days=-7
    - Verify error handling or zero results

11. test_get_overdue_invoices_empty - Mock returns empty list
    - Verify count=0, empty invoices list

12. test_send_reminder_not_found - POST /apar/ar/{invalid_id}/remind
    - Mock raises ValueError, verify error response

Cover validation errors, not found errors, edge cases.
  </action>
  <verify>12 tests created, all error paths covered</verify>
  <done>Comprehensive error testing validates API-03 requirement</done>
</task>

<task type="auto">
  <name>Verify APAR coverage and finalize</name>
  <files>backend/tests/api/test_apar_routes_coverage.py</files>
  <action>
Run coverage verification:

```bash
cd /Users/rushiparikh/projects/atom/backend
pytest tests/api/test_apar_routes_coverage.py --cov=api/apar_routes --cov-report=term-missing --cov-report=json -v
```

Verify:
1. 45+ tests total
2. 75%+ line coverage achieved
3. All tests passing
4. No unexpected failures

If coverage <75%, add tests for missing lines:
- Add parametrized tests for similar endpoints
- Test edge cases in invoice status transitions
- Test all exception paths

Add file header docstring with target, goal, endpoints covered.
  </action>
  <verify>Coverage report shows 75%+ on apar_routes.py, all tests pass</verify>
  <done>APAR routes have comprehensive test coverage meeting phase goal</done>
</task>

</tasks>

<verification>
After completion, verify:
1. File exists: backend/tests/api/test_apar_routes_coverage.py
2. Test count: 45+ tests
3. Line coverage: 75%+ on api/apar_routes.py
4. All tests pass: pytest exit code 0
5. APAREngine properly mocked
6. PDF downloads tested without reportlab dependency
</verification>

<success_criteria>
1. All 14 APAR endpoints have success path tests
2. All 14 APAR endpoints have error path tests
3. PDF download endpoints tested with mocked reportlab
4. Invoice status transitions validated
5. 75%+ line coverage achieved on apar_routes.py
6. All tests passing with pytest
</success_criteria>

<output>
After completion, create `.planning/phases/180-api-routes-coverage-advanced-features/180-01-SUMMARY.md` with:
- Number of tests created
- Coverage percentage achieved
- Any deviations from plan
- File with line count
</output>
