---
phase: 180-api-routes-coverage-advanced-features
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_deeplinks_coverage.py
autonomous: true

must_haves:
  truths:
    - "All 4 deep link endpoints have success path tests"
    - "All 4 deep link endpoints have error path tests"
    - "DEEPLINK_ENABLED feature flag tested (503 when disabled)"
    - "Async execute_deep_link function properly mocked with AsyncMock"
    - "Database dependency (get_db) uses StaticPool pattern"
    - "75%+ line coverage achieved on deeplinks.py"
  artifacts:
    - path: "backend/tests/api/test_deeplinks_coverage.py"
      provides: "Deep link route test suite with 35+ tests"
      min_lines: 650
      exports: ["TestDeepLinkExecute", "TestDeepLinkAudit", "TestDeepLinkGenerate", "TestDeepLinkStats", "TestDeepLinkFeatureFlag", "TestDeepLinkErrorPaths"]
  key_links:
    - from: "test_deeplinks_coverage.py"
      to: "backend/api/deeplinks.py"
      via: "import router in TestClient app"
      pattern: "from api.deeplinks import router"
    - from: "test_deeplinks_coverage.py"
      to: "backend/core/deeplinks.py"
      via: "patch('api.deeplinks.execute_deep_link')"
      pattern: "patch('api.deeplinks.execute_deep_link')"
    - from: "test_deeplinks_coverage.py"
      to: "backend/core/database.py"
      via: "dependency_overrides[get_db]"
      pattern: "app.dependency_overrides[get_db] = override_get_db"
---

<objective>
Create comprehensive test coverage for deep link execution and audit routes (deeplinks.py).

**Purpose**: Validate deep link execution, audit logging, generation, and statistics with 75%+ line coverage.

**Output**: test_deeplinks_coverage.py with 35+ tests covering all 4 endpoints, feature flag, async mocking, and error paths.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/180-api-routes-coverage-advanced-features/180-RESEARCH.md

# Source files to test
@backend/api/deeplinks.py  # 401 lines, 4 endpoints
@backend/core/deeplinks.py  # Deep link execution service (async, mock target)
@backend/core/models.py  # DeepLinkAudit model

# Test patterns from Phase 177-179
@backend/tests/api/test_ai_workflows_routes_coverage.py  # AsyncMock pattern
@backend/tests/api/test_auto_install_routes_coverage.py  # Service mocking pattern
</context>

<tasks>

<task type="auto">
  <name>Create deep link test fixtures</name>
  <files>backend/tests/api/test_deeplinks_coverage.py</files>
  <action>
Create test fixtures at the top of the file:

1. **test_db()** - In-memory SQLite database with StaticPool (scope="function"):
   - Use sqlalchemy.create_engine with sqlite:///:memory:
   - Add connect_args={"check_same_thread": False}
   - Use poolclass=StaticPool (prevents locking issues)
   - Create all tables with Base.metadata.create_all(engine)
   - Create TestingSessionLocal with sessionmaker
   - Yield db, close in finally

2. **mock_execute_deep_link()** - AsyncMock for execute_deep_link function:
   - Return dict with success=True, agent_id, agent_name, execution_id, resource_type, resource_id, action, source
   - Configure side_effect for error tests

3. **deeplink_client()** - TestClient with DB override:
   - Import router from api.deeplinks
   - Create FastAPI app, include router
   - Override get_db to yield test_db
   - Patch 'api.deeplinks.execute_deep_link' with mock
   - Return TestClient(app)
   - Clear dependency_overrides in cleanup

4. **sample_execute_request()** - Factory for valid DeepLinkExecuteRequest

5. **sample_generate_request()** - Factory for valid DeepLinkGenerateRequest

6. **sample_audit_entries()** - Factory for DeepLinkAudit database objects

7. **sample_execution_response()** - Expected execute response structure

8. **mock_generate_deep_link()** - Mock for generate_deep_link function
9. **mock_parse_deep_link()** - Mock for parse_deep_link function

Reference Phase 179 AsyncMock pattern. Use AsyncMock for execute_deep_link (async function).
  </action>
  <verify>File created with 9+ fixtures, AsyncMock configured, test_db with StaticPool</verify>
  <done>Fixtures provide deterministic async mock responses and test database</done>
</task>

<task type="auto">
  <name>Implement TestDeepLinkExecute class</name>
  <files>backend/tests/api/test_deeplinks_coverage.py</files>
  <action>
Create TestDeepLinkExecute class with 6 tests:

1. test_execute_deeplink_agent_success - POST /api/deeplinks/execute with atom://agent/{id}
   - Configure mock to return agent execution result
   - Verify response 200
   - Verify success=True, agent_id, agent_name, execution_id present

2. test_execute_deeplink_workflow_success - POST /api/deeplinks/execute with atom://workflow/{id}
   - Configure mock to return workflow trigger result
   - Verify resource_type="workflow", resource_id present

3. test_execute_deeplink_canvas_success - POST /api/deeplinks/execute with atom://canvas/{id}
   - Configure mock to return canvas result
   - Verify resource_type="canvas", action present

4. test_execute_deeplink_with_source - POST /api/deeplinks/execute with source parameter
   - Include source="external" in request
   - Verify source passed through to response

5. test_execute_deeplink_custom_params - POST /api/deeplinks/execute with query params
   - Use URL with query parameters (atom://agent/123?message=hello)
   - Verify parameters handled correctly

6. test_execute_deeplink_all_resource_types - Parametrized test for all 4 types
   - Use @pytest.mark.parametrize for agent, workflow, canvas, tool
   - Verify each resource type works

Use sample_execute_request fixture. Configure mock_execute_deep_link per test.
  </action>
  <verify>6 tests created, execute endpoint tested with all resource types</verify>
  <done>Deep link execution tested for all supported resource types</done>
</task>

<task type="auto">
  <name>Implement TestDeepLinkAudit class</name>
  <files>backend/tests/api/test_deeplinks_coverage.py</files>
  <action>
Create TestDeepLinkAudit class with 5 tests:

1. test_get_deeplink_audit_success - GET /api/deeplinks/audit
   - Insert 3 DeepLinkAudit entries into test_db
   - Verify response 200, list returned
   - Verify all entries present

2. test_get_audit_filter_by_user - GET /api/deeplinks/audit?user_id={id}
   - Insert entries with different user_id values
   - Verify only matching user_id entries returned

3. test_get_audit_filter_by_agent - GET /api/deeplinks/audit?agent_id={id}
   - Insert entries with different agent_id values
   - Verify only matching agent_id entries returned

4. test_get_audit_filter_by_resource_type - GET /api/deeplinks/audit?resource_type=agent
   - Insert entries with different resource_type values
   - Verify only matching resource_type entries returned

5. test_get_audit_pagination - GET /api/deeplinks/audit?limit=10&offset=5
   - Insert 20+ entries
   - Verify limit and offset applied correctly

6. test_get_audit_empty - GET /api/deeplinks/audit with no entries
   - Verify empty list returned

Use sample_audit_entries factory to create test data. Query test_db to verify state.
  </action>
  <verify>6 tests created, audit endpoint tested with all filters</verify>
  <done>Audit log retrieval tested with all filter combinations</done>
</task>

<task type="auto">
  <name>Implement TestDeepLinkGenerate class</name>
  <files>backend/tests/api/test_deeplinks_coverage.py</files>
  <action>
Create TestDeepLinkGenerate class with 5 tests:

1. test_generate_deeplink_agent - POST /api/deeplinks/generate with resource_type=agent
   - Use sample_generate_request with resource_type="agent"
   - Mock generate_deep_link to return "atom://agent/123"
   - Verify response 200, deeplink_url present
   - Verify URL format correct

2. test_generate_deeplink_workflow - POST /api/deeplinks/generate with resource_type=workflow
   - Use resource_type="workflow"
   - Verify deeplink_url contains workflow

3. test_generate_deeplink_canvas - POST /api/deeplinks/generate with resource_type=canvas
   - Use resource_type="canvas"
   - Verify deeplink_url contains canvas

4. test_generate_deeplink_tool - POST /api/deeplinks/generate with resource_type=tool
   - Use resource_type="tool"
   - Verify deeplink_url contains tool

5. test_generate_deeplink_with_parameters - POST /api/deeplinks/generate with parameters
   - Include parameters dict in request
   - Verify parameters included in generated URL

6. test_generate_deeplink_all_valid_types - Parametrized test for all 4 valid types
   - Use @pytest.mark.parametrize for agent, workflow, canvas, tool
   - Verify all types generate valid URLs

Mock generate_deep_link function. Verify deeplink_url format matches expected.
  </action>
  <verify>6 tests created, generate endpoint tested for all resource types</verify>
  <done>Deep link generation tested for all supported resource types</done>
</task>

<task type="auto">
  <name>Implement TestDeepLinkStats class</name>
  <files>backend/tests/api/test_deeplinks_coverage.py</files>
  <action>
Create TestDeepLinkStats class with 6 tests:

1. test_get_deeplink_stats_success - GET /api/deeplinks/stats
   - Insert mix of successful and failed DeepLinkAudit entries
   - Verify response 200, stats returned
   - Verify total_executions, successful_executions, failed_executions

2. test_get_stats_by_resource_type - GET /api/deeplinks/stats checks aggregation
   - Insert entries with different resource_type values
   - Verify by_resource_type dict has counts for each type

3. test_get_stats_by_source - GET /api/deeplinks/stats checks source breakdown
   - Insert entries with different source values
   - Verify by_source dict has counts for each source

4. test_get_stats_top_agents - GET /api/deeplinks/stats checks agent ranking
   - Insert entries with different agent_id values
   - Verify top_agents list sorted by execution count

5. test_get_stats_recent_activity - GET /api/deeplinks/stats checks time filters
   - Insert entries with different created_at timestamps
   - Verify last_24h_executions count
   - Verify last_7d_executions count

6. test_get_stats_empty - GET /api/deeplinks/stats with no audit entries
   - Verify all counts are zero
   - Verify by_resource_type has zero for all types

7. test_get_stats_no_agent_joins - GET /api/deeplinks/stats with no agents
   - Insert entries without associated AgentRegistry
   - Verify top_agents is empty list

Create DeepLinkAudit entries directly in test_db with various timestamps.
  </action>
  <verify>7 tests created, stats endpoint tested with all aggregations</verify>
  <done>Deep link statistics tested with all aggregation queries</done>
</task>

<task type="auto">
  <name>Implement TestDeepLinkFeatureFlag class</name>
  <files>backend/tests/api/test_deeplinks_coverage.py</files>
  <action>
Create TestDeepLinkFeatureFlag class with 4 tests:

1. test_execute_deeplink_disabled - POST /api/deeplinks/execute with DEEPLINK_ENABLED=false
   - Patch 'api.deeplinks.DEEPLINK_ENABLED' to False
   - Verify response 503 Service Unavailable
   - Verify error message mentions "disabled"

2. test_generate_deeplink_disabled - POST /api/deeplinks/generate with DEEPLINK_ENABLED=false
   - Patch 'api.deeplinks.DEEPLINK_ENABLED' to False
   - Verify response 503 Service Unavailable

3. test_audit_works_when_disabled - GET /api/deeplinks/audit with DEEPLINK_ENABLED=false
   - Patch DEEPLINK_ENABLED to False
   - Verify audit endpoint still works (returns 200)
   - Audit doesn't require feature flag

4. test_stats_works_when_disabled - GET /api/deeplinks/stats with DEEPLINK_ENABLED=false
   - Patch DEEPLINK_ENABLED to False
   - Verify stats endpoint still works (returns 200)

Feature flag behavior is critical - execute/generate blocked when disabled, read-only endpoints work.
  </action>
  <verify>4 tests created, feature flag tested for all affected endpoints</verify>
  <done>DEEPLINK_ENABLED feature flag behavior validated</done>
</task>

<task type="auto">
  <name>Implement TestDeepLinkErrorPaths class</name>
  <files>backend/tests/api/test_deeplinks_coverage.py</files>
  <action>
Create TestDeepLinkErrorPaths class with 10 tests:

1. test_execute_deeplink_invalid_url - POST /api/deeplinks/execute with malformed URL
   - Configure mock to raise DeepLinkParseException
   - Verify 422 validation error

2. test_execute_deeplink_security_violation - POST /api/deeplinks/execute with blocked resource
   - Configure mock to raise DeepLinkSecurityException
   - Verify 422 validation error

3. test_execute_deeplink_service_error - POST /api/deeplinks/execute with service failure
   - Configure mock execute_deep_link to return {"success": False}
   - Verify 422 validation error

4. test_execute_deeplink_unexpected_error - POST /api/deeplinks/execute with exception
   - Configure mock to raise generic Exception
   - Verify 500 internal error

5. test_generate_invalid_resource_type - POST /api/deeplinks/generate with invalid type
   - Use resource_type="invalid"
   - Verify 422 validation error with valid types listed

6. test_generate_missing_resource_type - POST /api/deeplinks/generate without resource_type
   - Verify 422 validation error

7. test_generate_missing_resource_id - POST /api/deeplinks/generate without resource_id
   - Verify 422 validation error

8. test_execute_missing_deeplink_url - POST /api/deeplinks/execute without deeplink_url
   - Verify 422 validation error

9. test_execute_missing_user_id - POST /api/deeplinks/execute without user_id
   - Verify 422 validation error

10. test_audit_limit_validation - GET /api/deeplinks/audit with limit > 1000
    - Use limit=2000 (exceeds max)
    - Verify 422 validation error

Cover validation errors (422), security errors (422), service errors (500).
  </action>
  <verify>10 tests created, all error paths covered</verify>
  <done>Comprehensive error testing validates API-03 requirement</done>
</task>

<task type="auto">
  <name>Verify deep link coverage and finalize</name>
  <files>backend/tests/api/test_deeplinks_coverage.py</files>
  <action>
Run coverage verification:

```bash
cd /Users/rushiparikh/projects/atom/backend
pytest tests/api/test_deeplinks_coverage.py --cov=api/deeplinks --cov-report=term-missing --cov-report=json -v
```

Verify:
1. 35+ tests total
2. 75%+ line coverage achieved
3. All tests passing
4. No unexpected failures

If coverage <75%, add tests for missing lines:
- Test edge cases in stats aggregation
- Test all exception paths in execute endpoint
- Test additional validation scenarios
- Test audit entry response model fields

Add file header docstring with target, goal, endpoints covered.
  </action>
  <verify>Coverage report shows 75%+ on deeplinks.py, all tests pass</verify>
  <done>Deep link routes have comprehensive test coverage meeting phase goal</done>
</task>

</tasks>

<verification>
After completion, verify:
1. File exists: backend/tests/api/test_deeplinks_coverage.py
2. Test count: 35+ tests
3. Line coverage: 75%+ on api/deeplinks.py
4. All tests pass: pytest exit code 0
5. execute_deep_link mocked with AsyncMock
6. DEEPLINK_ENABLED feature flag tested
7. Database queries tested for audit and stats
</verification>

<success_criteria>
1. All 4 deep link endpoints have success path tests
2. All 4 deep link endpoints have error path tests
3. Feature flag tested (503 when disabled)
4. Async execute_deep_link properly mocked
5. 75%+ line coverage achieved on deeplinks.py
6. All tests passing with pytest
</success_criteria>

<output>
After completion, create `.planning/phases/180-api-routes-coverage-advanced-features/180-03-SUMMARY.md` with:
- Number of tests created
- Coverage percentage achieved
- Any deviations from plan
- File with line count
</output>
