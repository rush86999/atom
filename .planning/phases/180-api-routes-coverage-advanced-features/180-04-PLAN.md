---
phase: 180-api-routes-coverage-advanced-features
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_integrations_catalog_coverage.py
autonomous: true

must_haves:
  truths:
    - "Both integration catalog endpoints have success path tests"
    - "Both integration catalog endpoints have error path tests"
    - "Database dependency (get_db) uses StaticPool pattern"
    - "Search functionality tested (ilike on name and description)"
    - "Filter parameters tested (category, popular)"
    - "75%+ line coverage achieved on integrations_catalog_routes.py"
  artifacts:
    - path: "backend/tests/api/test_integrations_catalog_coverage.py"
      provides: "Integration catalog test suite with 15+ tests"
      min_lines: 320
      exports: ["TestIntegrationsCatalog", "TestIntegrationDetails", "TestCatalogFilters", "TestCatalogSearch", "TestCatalogErrorPaths"]
  key_links:
    - from: "test_integrations_catalog_coverage.py"
      to: "backend/api/integrations_catalog_routes.py"
      via: "import router in TestClient app"
      pattern: "from api.integrations_catalog_routes import router"
    - from: "test_integrations_catalog_coverage.py"
      to: "backend/core/database.py"
      via: "dependency_overrides[get_db]"
      pattern: "app.dependency_overrides[get_db] = override_get_db"
    - from: "test_integrations_catalog_coverage.py"
      to: "backend/core/models.py"
      via: "IntegrationCatalog model"
      pattern: "IntegrationCatalog"
---

<objective>
Create comprehensive test coverage for integration catalog routes (integrations_catalog_routes.py).

**Purpose**: Validate integration catalog listing, search, filtering, and detail retrieval with 75%+ line coverage.

**Output**: test_integrations_catalog_coverage.py with 15+ tests covering all 2 endpoints, search, filters, and error paths.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/180-api-routes-coverage-advanced-features/180-RESEARCH.md

# Source files to test
@backend/api/integrations_catalog_routes.py  # 99 lines, 2 endpoints
@backend/core/models.py  # IntegrationCatalog model

# Test patterns from Phase 178
@backend/tests/api/test_admin_system_health_routes.py  # StaticPool pattern
@backend/tests/api/test_auto_install_routes_coverage.py  # DB override pattern
</context>

<tasks>

<task type="auto">
  <name>Create integration catalog test fixtures</name>
  <files>backend/tests/api/test_integrations_catalog_coverage.py</files>
  <action>
Create test fixtures at the top of the file:

1. **test_db()** - In-memory SQLite database with StaticPool (scope="function"):
   - Use sqlalchemy.create_engine with sqlite:///:memory:
   - Add connect_args={"check_same_thread": False}
   - Use poolclass=StaticPool (prevents locking issues)
   - Create all tables with Base.metadata.create_all(engine)
   - Create TestingSessionLocal with sessionmaker
   - Yield db, close in finally

2. **catalog_client()** - TestClient with DB override:
   - Import router from api.integrations_catalog_routes
   - Create FastAPI app, include router
   - Override get_db to yield test_db
   - Return TestClient(app)
   - Clear dependency_overrides in cleanup

3. **sample_integration()** - IntegrationCatalog fixture factory:
   - Accept id, name, description, category, popular flag
   - Return IntegrationCatalog instance with defaults

4. **sample_integrations()** - Factory for list of test integrations:
   - Create 5+ IntegrationCatalog instances
   - Mix categories (communication, email, productivity, etc.)
   - Mix popular flags

5. **integration_response_structure()** - Expected response fields:
   - List required fields (id, name, description, category, icon, color, authType, triggers, actions, popular)

Reference Phase 178 StaticPool pattern. IntegrationCatalog has no authentication requirement.
  </action>
  <verify>File created with 5+ fixtures, imports resolve, StaticPool configured</verify>
  <done>Fixtures provide test database and integration data factories</done>
</task>

<task type="auto">
  <name>Implement TestIntegrationsCatalog class</name>
  <files>backend/tests/api/test_integrations_catalog_coverage.py</files>
  <action>
Create TestIntegrationsCatalog class with 4 tests:

1. test_get_catalog_success - GET /api/v1/integrations/catalog
   - Insert 5 IntegrationCatalog entries into test_db
   - Verify response 200
   - Verify list returned
   - Verify all 5 integrations present

2. test_get_catalog_empty - GET /api/v1/integrations/catalog with no integrations
   - Verify empty list returned, not error

3. test_get_catalog_response_structure - GET /api/v1/integrations/catalog checks response format
   - Insert integration entries
   - Verify response has correct structure
   - Verify authType field (camelCase from auth_type)
   - Verify triggers and actions are lists

4. test_get_catalog_multiple_categories - GET /api/v1/integrations/catalog with mixed categories
   - Insert integrations from different categories
   - Verify all categories present in response

Use catalog_client fixture. Use sample_integrations factory for test data.
  </action>
  <verify>4 tests created, catalog listing tested</verify>
  <done>Integration catalog listing tested with various data scenarios</done>
</task>

<task type="auto">
  <name>Implement TestIntegrationDetails class</name>
  <files>backend/tests/api/test_integrations_catalog_coverage.py</files>
  <action>
Create TestIntegrationDetails class with 3 tests:

1. test_get_integration_details_success - GET /api/v1/integrations/catalog/{piece_id}
   - Create IntegrationCatalog with id="slack_1" in test_db
   - GET /api/v1/integrations/catalog/slack_1
   - Verify response 200
   - Verify id matches, name present

2. test_get_integration_details_all_fields - GET /api/v1/integrations/catalog/{id} checks response
   - Create integration with all fields populated
   - Verify all response fields present (id, name, description, category, icon, color, authType, triggers, actions, popular)

3. test_get_integration_details_response_format - GET /api/v1/integrations/catalog/{id} checks format
   - Verify authType is camelCase (from auth_type)
   - Verify triggers and actions are lists
   - Verify popular is boolean

Query test_db after test to verify database state unchanged.
  </action>
  <verify>3 tests created, details endpoint tested</verify>
  <done>Single integration details retrieval tested</done>
</task>

<task type="auto">
  <name>Implement TestCatalogFilters class</name>
  <files>backend/tests/api/test_integrations_catalog_coverage.py</files>
  <action>
Create TestCatalogFilters class with 5 tests:

1. test_filter_by_category - GET /api/v1/integrations/catalog?category=communication
   - Insert integrations with different categories
   - Verify only communication category integrations returned

2. test_filter_by_popular_true - GET /api/v1/integrations/catalog?popular=true
   - Insert mix of popular=True and popular=False
   - Verify only popular=True integrations returned

3. test_filter_by_popular_false - GET /api/v1/integrations/catalog?popular=false
   - Insert mix of popular flags
   - Verify only popular=False integrations returned

4. test_combined_filters - GET /api/v1/integrations/catalog?category=email&popular=true
   - Insert integrations with various categories and popular flags
   - Verify both filters applied (intersection)

5. test_filter_no_matches - GET /api/v1/integrations/catalog with filter matching nothing
   - Use category that doesn't exist
   - Verify empty list returned

6. test_filter_category_case_sensitive - GET /api/v1/integrations/catalog checks case
   - Test exact case match (database comparison is case-sensitive)

Use sample_integrations factory with varied categories and popular flags.
  </action>
  <verify>6 tests created, all filter parameters tested</verify>
  <done>Category and popular filters tested with combinations</done>
</task>

<task type="auto">
  <name>Implement TestCatalogSearch class</name>
  <files>backend/tests/api/test_integrations_catalog_coverage.py</files>
  <action>
Create TestCatalogSearch class with 4 tests:

1. test_search_by_name - GET /api/v1/integrations/catalog?search=slack
   - Insert integrations: "Slack", "Gmail", "Dropbox"
   - Verify only "Slack" returned (name.ilike match)

2. test_search_by_description - GET /api/v1/integrations/catalog?search=email
   - Insert integration with description="Email service integration"
   - Verify integration returned (description.ilike match)

3. test_search_case_insensitive - GET /api/v1/integrations/catalog?search=SLACK
   - Insert "Slack" integration
   - Verify search is case-insensitive (ilike)

4. test_search_partial_match - GET /api/v1/integrations/catalog?search=mail
   - Insert "Gmail", "Mailchimp"
   - Verify both returned (partial match)

5. test_search_no_results - GET /api/v1/integrations/catalog?search=nonexistent
   - Verify empty list returned

6. test_search_special_characters - GET /api/v1/integrations/catalog?search=C++
   - Test search with special characters in query

7. test_search_with_filters - GET /api/v1/integrations/catalog?search=slack&category=communication
   - Verify search and filter combined correctly

Search uses ilike on both name and description (OR condition).
  </action>
  <verify>7 tests created, search functionality tested comprehensively</verify>
  <done>ilike search on name and description tested</done>
</task>

<task type="auto">
  <name>Implement TestCatalogErrorPaths class</name>
  <files>backend/tests/api/test_integrations_catalog_coverage.py</files>
  <action>
Create TestCatalogErrorPaths class with 4 tests:

1. test_get_integration_not_found - GET /api/v1/integrations/catalog/{invalid_id}
   - Use non-existent integration ID
   - Verify response 404
   - Verify error detail mentions "Integration"

2. test_get_integration_empty_id - GET /api/v1/integrations/catalog/ with no ID
   - Verify response 404 (FastAPI path param missing)

3. test_filter_invalid_category - GET /api/v1/integrations/catalog?category=nonexistent
   - Use category that doesn't exist
   - Verify empty list (not error)

4. test_database_error_handling - Mock database error
   - Configure test_db.query to raise exception
   - Verify 500 internal error with error message
   - This tests the try/except block in catalog endpoint

5. test_search_sql_injection - GET /api/v1/integrations/catalog?search=<malicious>
   - Test SQL injection attempt is handled safely
   - ilike parameterized query should be safe

Cover 404 not found, 500 internal errors.
  </action>
  <verify>5 tests created, error paths tested</verify>
  <done>Comprehensive error testing validates API-03 requirement</done>
</task>

<task type="auto">
  <name>Verify integration catalog coverage and finalize</name>
  <files>backend/tests/api/test_integrations_catalog_coverage.py</files>
  <action>
Run coverage verification:

```bash
cd /Users/rushiparikh/projects/atom/backend
pytest tests/api/test_integrations_catalog_coverage.py --cov=api/integrations_catalog_routes --cov-report=term-missing --cov-report=json -v
```

Verify:
1. 15+ tests total
2. 75%+ line coverage achieved
3. All tests passing
4. No unexpected failures

If coverage <75%, add tests for missing lines:
- Test edge cases in ilike search
- Test exception handler in catalog endpoint
- Test response field mappings (auth_type → authType)
- Test additional error scenarios

Add file header docstring with target, goal, endpoints covered.
  </action>
  <verify>Coverage report shows 75%+ on integrations_catalog_routes.py, all tests pass</verify>
  <done>Integration catalog routes have comprehensive test coverage meeting phase goal</done>
</task>

</tasks>

<verification>
After completion, verify:
1. File exists: backend/tests/api/test_integrations_catalog_coverage.py
2. Test count: 15+ tests
3. Line coverage: 75%+ on api/integrations_catalog_routes.py
4. All tests pass: pytest exit code 0
5. StaticPool pattern used for test database
6. Search functionality tested (ilike on name and description)
7. Filter parameters tested (category, popular)
</verification>

<success_criteria>
1. Both 2 integration catalog endpoints have success path tests
2. Both 2 integration catalog endpoints have error path tests
3. Search tested with name and description matching
4. Filters tested (category, popular)
5. 75%+ line coverage achieved on integrations_catalog_routes.py
6. All tests passing with pytest
</success_criteria>

<output>
After completion, create `.planning/phases/180-api-routes-coverage-advanced-features/180-04-SUMMARY.md` with:
- Number of tests created
- Coverage percentage achieved
- Any deviations from plan
- File with line count
</output>
