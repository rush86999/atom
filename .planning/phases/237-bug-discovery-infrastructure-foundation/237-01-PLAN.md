---
phase: 237-bug-discovery-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/fuzzing/__init__.py
  - backend/tests/fuzzing/conftest.py
  - backend/tests/browser_discovery/__init__.py
  - backend/tests/browser_discovery/conftest.py
  - backend/tests/bug_discovery/TEMPLATES/.gitkeep
autonomous: true

must_haves:
  truths:
    - "Bug discovery tests integrate into existing tests/ directory (not separate /bug-discovery/)"
    - "Fuzzing tests have dedicated conftest.py with Atheris setup"
    - "Browser discovery tests have dedicated conftest.py with Playwright setup"
    - "TEMPLATES directory exists for documentation templates"
    - "New directories follow existing test structure patterns"
  artifacts:
    - path: "backend/tests/fuzzing"
      provides: "Fuzzing test directory with Atheris setup"
      contains: "conftest.py"
    - path: "backend/tests/browser_discovery"
      provides: "Browser discovery test directory with Playwright setup"
      contains: "conftest.py"
    - path: "backend/tests/bug_discovery/TEMPLATES"
      provides: "Documentation templates directory for bug discovery categories"
      min_lines: 1
  key_links:
    - from: "backend/tests/fuzzing/conftest.py"
      to: "backend/tests/e2e_ui/conftest.py"
      via: "Import existing auth and database fixtures"
      pattern: "from tests.e2e_ui.fixtures import"
    - from: "backend/tests/browser_discovery/conftest.py"
      to: "backend/tests/e2e_ui/conftest.py"
      via: "Import existing auth and database fixtures"
      pattern: "from tests.e2e_ui.fixtures import"
---

<objective>
Create bug discovery test directory structure integrated into existing tests/ with dedicated conftest.py files for fuzzing and browser discovery tests.

Purpose: Establish the foundation for bug discovery testing by creating dedicated directories that integrate with existing pytest infrastructure, following the pattern from research (no separate /bug-discovery/ directory per INFRA-01 requirement).

Output: New test directories (fuzzing/, browser_discovery/) with conftest.py files, TEMPLATES/ directory for documentation templates.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/237-bug-discovery-infrastructure-foundation/237-RESEARCH.md

# Infrastructure patterns from existing tests
@backend/tests/e2e_ui/conftest.py
@backend/tests/property_tests/conftest.py
@backend/pytest.ini

# Existing fixtures to reuse
@backend/tests/e2e_ui/fixtures/auth_fixtures.py
@backend/tests/e2e_ui/fixtures/database_fixtures.py
@backend/tests/e2e_ui/fixtures/api_fixtures.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fuzzing test directory with Atheris setup</name>
  <files>backend/tests/fuzzing/__init__.py, backend/tests/fuzzing/conftest.py</files>
  <action>
Create backend/tests/fuzzing/ directory with two files:

1. **backend/tests/fuzzing/__init__.py** (empty file, marks as Python package)

2. **backend/tests/fuzzing/conftest.py** with:
   - Atheris setup and teardown fixtures
   - Import existing fixtures from tests.e2e_ui.fixtures (auth_fixtures, database_fixtures)
   - Fuzzing-specific pytest hooks for timeout and crash detection
   - Fuzzing campaign fixtures (start, stop, monitor runs)

Key fixtures to create:
   - `atheris_fuzz_target`: Base fixture for Atheris fuzz targets
   - `fuzz_input_data`: Provider for random fuzz input
   - `fuzz_timeout`: Timeout fixture (default 300s for fuzzing)
   - Import: `from tests.e2e_ui.fixtures.auth_fixtures import authenticated_user`
   - Import: `from tests.e2e_ui.fixtures.database_fixtures import db_session`

DO NOT create separate auth/database fixtures - reuse existing ones from e2e_ui/fixtures/.
  </action>
  <verify>
    - Directory exists: `ls -la backend/tests/fuzzing/`
    - __init__.py exists and is empty
    - conftest.py exists with atheris imports and fixture definitions
    - `grep -r "from tests.e2e_ui.fixtures import" backend/tests/fuzzing/conftest.py` returns fixture imports
  </verify>
  <done>
    Fuzzing directory created with conftest.py that:
    - Imports existing fixtures (auth_fixtures, database_fixtures)
    - Defines Atheris-specific fixtures (fuzz_target, fuzz_timeout, fuzz_input)
    - No duplicate fixture definitions (all from e2e_ui/fixtures/)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create browser discovery directory with Playwright setup</name>
  <files>backend/tests/browser_discovery/__init__.py, backend/tests/browser_discovery/conftest.py</files>
  <action>
Create backend/tests/browser_discovery/ directory with two files:

1. **backend/tests/browser_discovery/__init__.py** (empty file, marks as Python package)

2. **backend/tests/browser_discovery/conftest.py** with:
   - Playwright setup following e2e_ui/conftest.py pattern
   - Import existing fixtures from tests.e2e_ui.fixtures (auth_fixtures, database_fixtures, page_objects)
   - Browser discovery-specific fixtures (exploration_agent, console_error_monitor, accessibility_checker)
   - pytest hooks for automatic screenshot/log capture on failures

Key fixtures to import from e2e_ui/fixtures/:
   - `authenticated_page`: API-first authenticated Playwright page
   - `db_session`: Database session with worker isolation
   - `test_user`: Test user creation

Key fixtures to create:
   - `exploration_agent`: Fixture for intelligent UI exploration
   - `console_monitor`: Fixture for capturing JavaScript console errors
   - `accessibility_checker`: Fixture with axe-core integration

DO NOT duplicate auth/database fixtures - import from e2e_ui/fixtures/.
  </action>
  <verify>
    - Directory exists: `ls -la backend/tests/browser_discovery/`
    - __init__.py exists and is empty
    - conftest.py exists with Playwright imports and fixture definitions
    - `grep "from tests.e2e_ui.fixtures import" backend/tests/browser_discovery/conftest.py` returns fixture imports
  </verify>
  <done>
    Browser discovery directory created with conftest.py that:
    - Imports existing fixtures (authenticated_page, db_session, test_user)
    - Defines browser discovery fixtures (exploration_agent, console_monitor, accessibility_checker)
    - No duplicate fixture definitions (all from e2e_ui/fixtures/)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create TEMPLATES directory for bug discovery documentation</name>
  <files>backend/tests/bug_discovery/TEMPLATES/.gitkeep</files>
  <action>
Create backend/tests/bug_discovery/TEMPLATES/ directory with placeholder file:

1. **backend/tests/bug_discovery/TEMPLATES/.gitkeep** (empty file to preserve directory in git)

This directory will be populated with documentation templates in Plan 02:
   - FUZZING_TEMPLATE.md
   - CHAOS_TEMPLATE.md
   - PROPERTY_TEMPLATE.md
   - BROWSER_TEMPLATE.md

For now, just create the directory structure with .gitkeep.
  </action>
  <verify>
    - Directory exists: `ls -la backend/tests/bug_discovery/TEMPLATES/`
    - .gitkeep file exists
  </verify>
  <done>
    TEMPLATES directory created in bug_discovery/ for documentation templates
  </done>
</task>

</tasks>

<verification>
1. **Directory structure verified**:
   - `backend/tests/fuzzing/` exists with conftest.py
   - `backend/tests/browser_discovery/` exists with conftest.py
   - `backend/tests/bug_discovery/TEMPLATES/` exists

2. **Fixture reuse verified**:
   - `grep -r "from tests.e2e_ui.fixtures import" backend/tests/fuzzing/conftest.py`
   - `grep -r "from tests.e2e_ui.fixtures import" backend/tests/browser_discovery/conftest.py`
   - No duplicate fixture definitions

3. **Integration verified**:
   - `pytest tests/fuzzing/ --collect-only` discovers new conftest.py
   - `pytest tests/browser_discovery/ --collect-only` discovers new conftest.py

4. **No separate /bug-discovery/ directory**:
   - All bug discovery tests are under tests/ (INFRA-01 requirement satisfied)
</verification>

<success_criteria>
1. New test directories integrate into existing tests/ structure
2. Fuzzing conftest.py imports existing fixtures (no duplication)
3. Browser discovery conftest.py imports existing fixtures (no duplication)
4. TEMPLATES directory created for documentation templates (Plan 02)
5. pytest discovery works for new directories
</success_criteria>

<output>
After completion, create `.planning/phases/237-bug-discovery-infrastructure-foundation/237-01-SUMMARY.md`
</output>
