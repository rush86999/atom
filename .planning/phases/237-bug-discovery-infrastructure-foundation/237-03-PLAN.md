---
phase: 237-bug-discovery-infrastructure-foundation
plan: 03
type: execute
wave: 2
depends_on: ["237-01", "237-02"]
files_modified:
  - .github/workflows/pr-tests.yml
  - .github/workflows/bug-discovery-weekly.yml
  - backend/pytest.ini
  - backend/tests/conftest.py
autonomous: true

must_haves:
  truths:
    - "Fast PR tests run in <10 minutes with 'fast or property' markers"
    - "Weekly bug discovery tests run in ~2 hours with 'fuzzing or chaos or browser' markers"
    - "Pytest markers configured for all bug discovery categories"
    - "pytest_exception_interact hook integrates bug filing"
    - "Separate CI workflows prevent PR pipeline bloat"
  artifacts:
    - path: ".github/workflows/pr-tests.yml"
      provides: "Fast PR test CI workflow (<10 minutes)"
      contains: "pytest tests/ -m \"fast or property\""
    - path: ".github/workflows/bug-discovery-weekly.yml"
      provides: "Weekly bug discovery CI workflow (~2 hours)"
      contains: "pytest tests/ -m \"fuzzing or chaos or browser\""
    - path: "backend/pytest.ini"
      provides: "Pytest configuration with bug discovery markers"
      contains: "markers ="
    - path: "backend/tests/conftest.py"
      provides: "Root conftest with bug filing pytest hook"
      contains: "pytest_exception_interact"
  key_links:
    - from: ".github/workflows/pr-tests.yml"
      to: "backend/pytest.ini"
      via: "Pytest marker-based test selection"
      pattern: "-m \"fast or property\""
    - from: ".github/workflows/bug-discovery-weekly.yml"
      to: "backend/pytest.ini"
      via: "Pytest marker-based test selection"
      pattern: "-m \"fuzzing or chaos or browser\""
    - from: "backend/tests/conftest.py"
      to: "backend/tests/bug_discovery/bug_filing_service.py"
      via: "Automatic bug filing on test failure"
      pattern: "BugFilingService"
---

<objective>
Create separate CI pipelines for fast PR tests (<10min) and weekly bug discovery tests (~2 hours) using pytest marker-based test separation.

Purpose: Enable fast PR feedback while running comprehensive bug discovery tests weekly, preventing CI pipeline bloat and ensuring developers get quick feedback on PRs (INFRA-02 requirement).

Output: Two GitHub Actions workflows (pr-tests.yml, bug-discovery-weekly.yml), updated pytest.ini with markers, root conftest.py with bug filing hook.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/237-bug-discovery-infrastructure-foundation/237-RESEARCH.md

# Existing CI workflows to reference
@.github/workflows/weekly-stress-tests.yml
@.github/workflows/automated-bug-filing.yml
@backend/pytest.ini
@backend/tests/conftest.py
@backend/tests/bug_discovery/bug_filing_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pytest.ini with bug discovery markers</name>
  <files>backend/pytest.ini</files>
  <action>
Update backend/pytest.ini to add bug discovery markers.

The pytest.ini already has markers for fuzzy, chaos, and stress. Add the following markers to the [pytest] markers section:

```ini
# Bug Discovery Markers (Phase 237)
fuzzing: Fuzzing tests using Atheris - run weekly
browser: Browser automation bug discovery - run weekly
discovery: General bug discovery tests - run weekly
```

DO NOT remove existing markers (unit, integration, property, invariant, contract, fast, slow, fuzzy, mutation, chaos, stress, etc.).

Also update the testpaths to include new bug discovery directories:
```ini
testpaths = tests tests/fuzzing tests/browser_discovery tests/e2e_ui/tests
```

Verify the addopts section includes marker-based test selection for CI:
```ini
# Note: CI pipelines use -m markers to separate fast vs slow tests
# PR tests: pytest -m "fast or property" (<10 minutes)
# Bug discovery: pytest -m "fuzzing or chaos or browser" (~2 hours)
```
  </action>
  <verify>
    - pytest.ini exists with new markers (fuzzing, browser, discovery)
    - `grep "fuzzing:" backend/pytest.ini` returns marker definition
    - `grep "browser:" backend/pytest.ini` returns marker definition
    - `grep "discovery:" backend/pytest.ini` returns marker definition
    - testpaths includes new directories
  </verify>
  <done>
    pytest.ini updated with bug discovery markers (fuzzing, browser, discovery) and testpaths
  </done>
</task>

<task type="auto">
  <name>Task 2: Create pr-tests.yml workflow for fast PR tests</name>
  <files>.github/workflows/pr-tests.yml</files>
  <action>
Create .github/workflows/pr-tests.yml with:

**Trigger:** pull_request, push to main

**Job:** fast-tests
- Runs on ubuntu-latest
- Python 3.11, Node.js 18
- Install backend dependencies
- Install Playwright browsers
- Run fast tests: `pytest tests/ -m "fast or property" -n auto --tb=short`
- Timeout: 10 minutes (fail if exceeded)

**Test Selection:**
```yaml
- name: Run fast PR tests
  run: |
    cd backend
    pytest tests/ -m "fast or property" -n auto --tb=short \
      --junitxml=pr-test-results.xml \
      --maxfail=20
  timeout-minutes: 10
```

**Fast tests include:**
- Unit tests (<0.1s per test)
- Property-based tests with Hypothesis (fast execution)
- Integration tests (<1s per test)
- NO fuzzing, chaos, browser discovery (these run weekly)

**On failure:**
- Upload test results as artifact
- Comment on PR with failure summary

**Structure:**
```yaml
name: Fast PR Tests

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

jobs:
  fast-tests:
    name: Fast Tests (<10 min)
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          cd backend
          pip install -r requirements.txt
          pip install -r requirements-testing.txt
          playwright install chromium

      - name: Run fast PR tests
        run: |
          cd backend
          pytest tests/ -m "fast or property" -n auto --tb=short \
            --junitxml=pr-test-results.xml
        timeout-minutes: 10

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: pr-test-results
          path: backend/pr-test-results.xml
```
  </action>
  <verify>
    - .github/workflows/pr-tests.yml exists
    - Contains `-m "fast or property"` test selection
    - Contains timeout-minutes: 10
    - Triggers on pull_request
  </verify>
  <done>
    pr-tests.yml created with fast test selection (<10 minutes, no bug discovery tests)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create bug-discovery-weekly.yml workflow</name>
  <files>.github/workflows/bug-discovery-weekly.yml</files>
  <action>
Create .github/workflows/bug-discovery-weekly.yml with:

**Trigger:**
- schedule: cron '0 3 * * 0' (Sunday 3 AM UTC weekly)
- workflow_dispatch: Manual trigger

**Job:** bug-discovery-tests
- Runs on ubuntu-latest
- Python 3.11, Node.js 18
- Install all dependencies including Atheris
- Run bug discovery tests: `pytest tests/ -m "fuzzing or chaos or browser" --tb=long`
- Timeout: 2 hours (bug discovery is comprehensive)

**Test Selection:**
```yaml
- name: Run bug discovery tests
  run: |
    cd backend
    pytest tests/ -m "fuzzing or chaos or browser" \
      --tb=long \
      --junitxml=bug-discovery-results.xml \
      --maxfail=50
  timeout-minutes: 120
```

**Bug discovery tests include:**
- Fuzzing tests with Atheris (coverage-guided)
- Chaos engineering tests (failure injection)
- Browser discovery tests (console errors, a11y, broken links)

**On failure:**
- Upload test results, screenshots, logs
- Trigger automated bug filing workflow

**Structure:**
```yaml
name: Weekly Bug Discovery Tests

on:
  schedule:
    - cron: '0 3 * * 0'  # Sunday 3 AM UTC
  workflow_dispatch:

jobs:
  bug-discovery:
    name: Bug Discovery Tests (~2 hours)
    runs-on: ubuntu-latest
    timeout-minutes: 150

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          cd backend
          pip install -r requirements.txt
          pip install -r requirements-testing.txt
          pip install atheris  # Fuzzing support
          playwright install chromium

      - name: Run bug discovery tests
        run: |
          cd backend
          pytest tests/ -m "fuzzing or chaos or browser" \
            --tb=long \
            --junitxml=bug-discovery-results.xml
        timeout-minutes: 120
        continue-on-error: true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: bug-discovery-results
          path: backend/bug-discovery-results.xml

      - name: Upload screenshots
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: bug-discovery-screenshots
          path: backend/tests/artifacts/screenshots/

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: bug-discovery-logs
          path: backend/tests/artifacts/logs/
```
  </action>
  <verify>
    - .github/workflows/bug-discovery-weekly.yml exists
    - Contains `-m "fuzzing or chaos or browser"` test selection
    - Contains timeout-minutes: 120
    - Triggers on schedule (weekly Sunday 3 AM UTC)
  </verify>
  <done>
    bug-discovery-weekly.yml created with bug discovery test selection (~2 hours, weekly schedule)
  </done>
</task>

<task type="auto">
  <name>Task 4: Update root conftest.py with bug filing hook</name>
  <files>backend/tests/conftest.py</files>
  <action>
Update backend/tests/conftest.py to add pytest_exception_interact hook for automatic bug filing.

Add the following function to the root conftest.py:

```python
# Bug discovery automatic bug filing hook
def pytest_exception_interact(node, call, report):
    """
    File GitHub Issue when bug discovery test fails.

    This hook automatically files bugs for tests marked with:
    - @pytest.mark.fuzzing
    - @pytest.mark.chaos
    - @pytest.mark.browser
    - @pytest.mark.discovery

    Skips bug filing for:
    - Local development (no GITHUB_TOKEN)
    - Expected failures (xfail markers)
    - Known flaky tests
    """
    from .bug_discovery.bug_filing_service import BugFilingService
    import os

    # Only file bugs for failed tests
    if report.failed:
        # Check if test is a bug discovery test
        item = node.obj
        if hasattr(item, 'pytestmark'):
            markers = [marker.name for marker in item.pytestmark]
            discovery_markers = ['fuzzing', 'chaos', 'browser', 'discovery']

            if any(marker in markers for marker in discovery_markers):
                # Check environment variables
                github_token = os.getenv("GITHUB_TOKEN")
                github_repository = os.getenv("GITHUB_REPOSITORY")

                if github_token and github_repository:
                    try:
                        service = BugFilingService(
                            github_token=github_token,
                            github_repository=github_repository
                        )

                        # Determine test type from markers
                        test_type = next(
                            (m for m in markers if m in discovery_markers),
                            'discovery'
                        )

                        # File bug with test metadata
                        service.file_bug(
                            test_name=node.name,
                            error_message=str(call.excinfo.value),
                            metadata={
                                "test_type": test_type,
                                "file_path": str(node.fspath),
                                "line_number": node.lineno,
                                "python_version": os.sys.version,
                            }
                        )
                    except Exception as e:
                        # Log error but don't fail the test run
                        print(f"Bug filing failed: {e}")
```

Also add imports at the top of conftest.py if not already present:
```python
import os
import sys
```

DO NOT modify existing fixtures or hooks in conftest.py. Only add the new pytest_exception_interact function.
  </action>
  <verify>
    - backend/tests/conftest.py contains pytest_exception_interact function
    - Function imports BugFilingService
    - Function checks for discovery markers (fuzzing, chaos, browser, discovery)
    - Function checks for GITHUB_TOKEN and GITHUB_REPOSITORY env vars
  </verify>
  <done>
    Root conftest.py updated with pytest_exception_interact hook for automatic bug filing
  </done>
</task>

</tasks>

<verification>
1. **pytest.ini updated:**
   - `grep "fuzzing:" backend/pytest.ini` returns marker
   - `grep "browser:" backend/pytest.ini` returns marker
   - testpaths includes new directories

2. **PR workflow created:**
   - .github/workflows/pr-tests.yml exists
   - Uses `-m "fast or property"` test selection
   - Timeout set to 10 minutes

3. **Bug discovery workflow created:**
   - .github/workflows/bug-discovery-weekly.yml exists
   - Uses `-m "fuzzing or chaos or browser"` test selection
   - Scheduled weekly (Sunday 3 AM UTC)

4. **Bug filing hook added:**
   - backend/tests/conftest.py contains pytest_exception_interact
   - Integrates with BugFilingService
   - Checks for discovery markers before filing

5. **Integration verified:**
   - PR workflow excludes bug discovery tests (fast feedback)
   - Bug discovery workflow runs comprehensive tests weekly
   - Bug filing happens automatically on failures
</verification>

<success_criteria>
1. Fast PR tests complete in <10 minutes (no bug discovery tests)
2. Weekly bug discovery tests run in ~2 hours (comprehensive coverage)
3. Pytest markers enable CI pipeline separation
4. Automatic bug filing integrated via pytest hook
5. Separate workflows prevent PR pipeline bloat (INFRA-02 satisfied)
</success_criteria>

<output>
After completion, create `.planning/phases/237-bug-discovery-infrastructure-foundation/237-03-SUMMARY.md`
</output>
