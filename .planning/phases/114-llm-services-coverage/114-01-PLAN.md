---
phase: 114-llm-services-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/llm/test_byok_handler_coverage.py
autonomous: true

must_haves:
  truths:
    - "Coverage report shows 40%+ coverage for llm/byok_handler.py (up from 8.72%)"
    - "Tests cover provider initialization and client setup"
    - "Tests cover query complexity analysis with all patterns"
    - "Tests cover provider ranking and optimal selection"
    - "Tests cover context window and truncation utilities"
  artifacts:
    - path: "backend/tests/unit/llm/test_byok_handler_coverage.py"
      provides: "Comprehensive coverage tests for BYOKHandler core functionality"
      min_lines: 800
  key_links:
    - from: "backend/tests/unit/llm/test_byok_handler_coverage.py"
      to: "backend/core/llm/byok_handler.py"
      via: "Direct method testing with mocked LLM clients and BYOK manager"
      pattern: "_initialize_clients|analyze_query_complexity|get_ranked_providers|get_optimal_provider"
---

<objective>
Add comprehensive tests for BYOKHandler core methods to reach 40%+ coverage.

**Purpose:** The BYOK handler is the central LLM routing service with only 8.72% coverage (72/654 lines). This plan focuses on testing core methods that don't require full LLM API calls: provider initialization, complexity analysis, provider ranking, and utility functions.

**Output:** 40+ new tests covering initialization, complexity analysis, provider selection, context window management, and utility methods
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source files
@backend/core/llm/byok_handler.py (1556 lines, 8.72% coverage = 72/654 lines)
@backend/tests/unit/test_byok_handler.py (existing 4113 lines, covers initialization and basic functionality)
@backend/tests/unit/test_byok_handler_expanded.py (existing 733 lines, covers expanded scenarios)

# Proven patterns from Phase 112-113
@backend/tests/unit/governance/test_agent_context_resolver.py (error handling with mock patches)
@backend/tests/unit/episodes/test_episode_segmentation_coverage.py (helper methods for test data)
@backend/tests/test_cognitive_tier_classification.py (parametrized tests for complexity levels)
</context>

<tasks>

<task type="auto">
  <name>Add tests for BYOKHandler provider initialization</name>
  <files>backend/tests/unit/llm/test_byok_handler_coverage.py</files>
  <action>
    Create test file backend/tests/unit/llm/test_byok_handler_coverage.py with comprehensive initialization tests:

    1. test_byok_handler_init_with_all_providers
       - Mock BYOK manager with is_configured=True for openai, deepseek, moonshot, deepinfra
       - Mock get_api_key to return test keys
       - Verify all clients initialized in self.clients dict
       - Verify all async_clients initialized in self.async_clients dict

    2. test_byok_handler_init_without_openai_package
       - Patch OpenAI to None
       - Verify handler initializes gracefully without crashing
       - Verify workspace_id still set

    3. test_initialize_clients_with_env_fallback
       - Mock byok_manager.is_configured to return False
       - Set OPENAI_API_KEY env var
       - Verify client initialized from env var
       - Verify logger.info called

    4. test_initialize_clients_skip_unconfigured
       - Mock is_configured False for deepseek
       - Mock no env var set
       - Verify deepseek not in clients dict
       - Verify other providers still initialized

    5. test_initialize_clients_handles_init_exception
       - Mock OpenAI() to raise Exception
       - Verify error logged
       - Verify other providers still initialized
       - Verify handler doesn't crash

    6. test_get_provider_fallback_order_with_primary
       - Mock available_providers = ["openai", "deepseek", "moonshot"]
       - Call _get_provider_fallback_order("deepseek")
       - Verify deepseek first in list
       - Verify remaining in priority order (openai, moonshot)

    7. test_get_provider_fallback_order_unavailable_primary
       - Mock available_providers excludes requested primary
       - Verify primary not in fallback list
       - Verify priority order maintained

    8. test_get_provider_fallback_order_empty_clients
       - Mock empty async_clients
       - Verify returns empty list
       - Verify handles gracefully

    Pattern from Phase 112: Use Mock with side_effect for return values, assert_called for verification
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_byok_handler_coverage.py -v -k "init"</verify>
  <done>All 8 tests pass, provider initialization coverage increases significantly</done>
</task>

<task type="auto">
  <name>Add tests for query complexity analysis</name>
  <files>backend/tests/unit/llm/test_byok_handler_coverage.py</files>
  <action>
    Add 12 tests for analyze_query_complexity method covering all complexity patterns:

    Create TestQueryComplexity class:

    1. test_analyze_complexity_simple_queries
       - Parametrize with ["hi", "summarize this", "what is AI", "list benefits"]
       - Verify returns QueryComplexity.SIMPLE

    2. test_analyze_complexity_moderate_queries
       - Parametrize with ["analyze this", "compare options", "explain in detail"]
       - Verify returns QueryComplexity.MODERATE

    3. test_analyze_complexity_technical_queries
       - Parametrize with ["calculate integral", "solve equation", "matrix transformation"]
       - Verify returns QueryComplexity.COMPLEX or higher

    4. test_analyze_complexity_code_queries
       - Parametrize with ["debug this function", "implement API endpoint", "```python```"]
       - Verify returns QueryComplexity.COMPLEX or higher

    5. test_analyze_complexity_advanced_queries
       - Parametrize with ["security audit", "distributed architecture", "reverse engineer"]
       - Verify returns QueryComplexity.ADVANCED

    6. test_analyze_complexity_with_code_blocks
       - Prompt with ```python...``` code block
       - Verify complexity_score increased by 3
       - Verify returns appropriate tier

    7. test_analyze_complexity_token_based_scoring
       - Test with 100 char prompt (~25 tokens) - should be SIMPLE
       - Test with 2000 char prompt (~500 tokens) - should be MODERATE+
       - Test with 8000 char prompt (~2000 tokens) - should be COMPLEX+

    8. test_analyze_complexity_with_task_type_code
       - Simple query with task_type="code"
       - Verify complexity increased appropriately

    9. test_analyze_complexity_with_task_type_chat
       - Moderate query with task_type="chat"
       - Verify complexity decreased by 1

    10. test_analyze_complexity_combined_patterns
       - Query with multiple patterns (code + advanced + long)
       - Verify correct cumulative scoring

    11. test_analyze_complexity_regex_word_boundaries
       - Query with "define" inside "definition" (should not match)
       - Verify word boundaries work correctly

    12. test_analyze_complexity_case_insensitive
       - Query with "DEBUG THIS FUNCTION" (uppercase)
       - Verify pattern matches case-insensitively

    Pattern from test_cognitive_tier_classification.py: Use pytest.mark.parametrize for multiple test cases
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_byok_handler_coverage.py::TestQueryComplexity -v</verify>
  <done>All 12 tests pass, complexity analysis coverage increases</done>
</task>

<task type="auto">
  <name>Add tests for provider ranking and optimal selection</name>
  <files>backend/tests/unit/llm/test_byok_handler_coverage.py</files>
  <action>
    Add 10 tests for get_ranked_providers and get_optimal_provider methods:

    Create TestProviderRanking class:

    1. test_get_optimal_provider_returns_tuple
       - Mock get_ranked_providers to return [("deepseek", "deepseek-chat")]
       - Verify returns (provider, model) tuple
       - Verify types are strings

    2. test_get_optimal_provider_fallback_to_default
       - Mock get_ranked_providers to return []
       - Mock self.clients with one provider
       - Verify returns (first_provider, "gpt-4o-mini")

    3. test_get_optimal_provider_no_providers_raises
       - Mock empty clients
       - Verify raises ValueError
       - Verify error message about BYOK keys

    4. test_get_ranked_providers_simple_complexity
       - Mock pricing fetcher with model data
       - Call with QueryComplexity.SIMPLE, prefer_cost=True
       - Verify budget models ranked first (deepseek, moonshot)

    5. test_get_ranked_providers_advanced_complexity
       - Call with QueryComplexity.ADVANCED
       - Verify premium models ranked first (openai, anthropic)

    6. test_get_ranked_providers_requires_tools
       - Call with requires_tools=True
       - Verify models from MODELS_WITHOUT_TOOLS excluded
       - Verify only tool-capable models returned

    7. test_get_ranked_providers_with_cognitive_tier
       - Call with cognitive_tier=CognitiveTier.STANDARD
       - Verify tier-appropriate models returned
       - Verify MIN_QUALITY_BY_TIER filter applied

    8. test_get_ranked_providers_cache_aware_routing
       - Mock pricing with cache support (anthropic: 0.9 hit rate)
       - Call with estimated_tokens=1000, workspace_id="test"
       - Verify cache-aware effective cost used for ranking

    9. test_get_ranked_providers_tenant_plan_filtering
       - Call with tenant_plan="free"
       - Verify models filtered by plan availability

    10. test_get_ranked_providers_empty_result_handling
       - Mock pricing fetcher with no models
       - Verify returns empty list
       - Verify handles gracefully

    Pattern from Phase 113: Mock complex dependencies (pricing fetcher, cache_router) with predefined return values
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_byok_handler_coverage.py::TestProviderRanking -v</verify>
  <done>All 10 tests pass, provider ranking coverage increases</done>
</task>

<task type="auto">
  <name>Add tests for context window and utility methods</name>
  <files>backend/tests/unit/llm/test_byok_handler_coverage.py</files>
  <action>
    Add 10 tests for utility methods covering context window, truncation, and helpers:

    Create TestUtilityMethods class:

    1. test_get_context_window_from_pricing
       - Mock pricing fetcher with max_input_tokens=128000
       - Verify returns 128000

    2. test_get_context_window_fallback_to_max_tokens
       - Mock pricing with max_tokens=4096 (no max_input_tokens)
       - Verify returns 4096

    3. test_get_context_window_defaults_by_model
       - Mock pricing fetcher to raise exception
       - Call with "gpt-4o"
       - Verify returns 128000 from CONTEXT_DEFAULTS

    4. test_get_context_window_conservative_default
       - Mock pricing failure, unknown model
       - Verify returns 4096 (conservative default)

    5. test_truncate_to_context_no_truncation_needed
       - Mock context window large
       - Call with short text
       - Verify returns unchanged text

    6. test_truncate_to_context_truncates_long_text
       - Mock context window small
       - Call with text exceeding limit
       - Verify returns truncated text with indicator

    7. test_truncate_to_context_with_reserve_tokens
       - Call with reserve_tokens=2000
       - Verify truncates earlier to reserve space

    8. test_get_available_providers
       - Mock initialized clients
       - Verify returns list of provider IDs
       - Verify excludes providers with no clients

    9. test_get_routing_info
       - Mock analyze_query_complexity
       - Mock get_optimal_provider
       - Verify returns dict with complexity, provider, model

    10. test_classify_cognitive_tier_delegates
       - Mock self.cognitive_classifier
       - Verify returns CognitiveTier from classifier
       - Verify task_type passed through

    Pattern from Phase 112: Test both success and failure paths for utility methods
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_byok_handler_coverage.py::TestUtilityMethods -v</verify>
  <done>All 10 tests pass, utility methods coverage increases</done>
</task>

</tasks>

<verification>
After completing all tasks, run coverage report:

```bash
cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_byok_handler_coverage.py --cov=core/llm/byok_handler --cov-report=term-missing --cov-report=json
```

Verify:
- New test file has 40+ tests
- Coverage for core/llm/byok_handler.py increased from 8.72% to 40%+
- All tests passing
- No test isolation issues
</verification>

<success_criteria>
1. 40+ new tests added in test_byok_handler_coverage.py
2. byok_handler.py coverage increased to 40%+ (from 8.72%)
3. All new tests passing
4. Coverage report shows progress toward 60% target (remaining coverage to be added in Plan 04)
</success_criteria>

<output>
After completion, create `.planning/phases/114-llm-services-coverage/114-01-SUMMARY.md` with coverage metrics achieved
</output>
