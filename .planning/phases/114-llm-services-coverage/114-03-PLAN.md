---
phase: 114-llm-services-coverage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/llm/test_canvas_summary_coverage.py
autonomous: true

must_haves:
  truths:
    - "Coverage report shows 70%+ coverage for llm/canvas_summary_service.py"
    - "Tests cover all 7 canvas types (generic, docs, email, sheets, orchestration, terminal, coding)"
    - "Tests cover summary generation with mocked BYOK handler"
    - "Tests cover caching behavior (hit/miss)"
    - "Tests cover metadata fallback on LLM failure"
  artifacts:
    - path: "backend/tests/unit/llm/test_canvas_summary_coverage.py"
      provides: "Comprehensive coverage tests for CanvasSummaryService"
      min_lines: 700
  key_links:
    - from: "backend/tests/unit/llm/test_canvas_summary_coverage.py"
      to: "backend/core/llm/canvas_summary_service.py"
      via: "Async testing with mocked BYOK handler and timeout simulation"
      pattern: "generate_summary|_build_prompt|_fallback_to_metadata|clear_cache|get_cache_stats"
---

<objective>
Add comprehensive tests for CanvasSummaryService to reach 70%+ coverage.

**Purpose:** The canvas summary service has existing tests (test_canvas_summary_service.py with 342 lines) but needs coverage for async generation paths, caching, timeout handling, and metadata fallback. This plan fills those gaps.

**Output:** 30+ new tests covering async generation, caching, fallback, and all canvas types
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source files
@backend/core/llm/canvas_summary_service.py (387 lines, 12 public methods)
@backend/tests/test_canvas_summary_service.py (342 lines, existing tests)
@backend/tests/test_canvas_summary_integration.py (254 lines, integration tests)

# Existing test patterns
@TestCanvasSummaryServiceInitialization
@TestPromptBuilding
@TestSummaryGeneration
@TestCachingBehavior
</context>

<tasks>

<task type="auto">
  <name>Add tests for summary generation with all canvas types</name>
  <files>backend/tests/unit/llm/test_canvas_summary_coverage.py</files>
  <action>
    Create test file backend/tests/unit/llm/test_canvas_summary_coverage.py with comprehensive async generation tests:

    Create TestSummaryGeneration class:

    1. test_generate_summary_generic_canvas
       - Mock byok_handler.generate_response AsyncMock
       - Call generate_summary("generic", {...})
       - Verify await completes
       - Verify correct system_instruction passed

    2. test_generate_summary_sheets_canvas
       - Mock byok_handler with sheets state
       - Verify sheets-specific prompt built
       - Verify revenue/metrics extracted

    3. test_generate_summary_orchestration_canvas
       - Mock workflow state with approval_amount
       - Verify workflow-specific prompt built
       - Verify stakeholders extracted

    4. test_generate_summary_terminal_canvas
       - Mock terminal state with command, exit_code
       - Verify command-specific prompt built
       - Verify exit_code extracted

    5. test_generate_summary_docs_canvas
       - Mock docs state with document_title
       - Verify docs-specific prompt built
       - Verify sections extracted

    6. test_generate_summary_email_canvas
       - Mock email state with to, subject
       - Verify email-specific prompt built
       - Verify recipients extracted

    7. test_generate_summary_coding_canvas
       - Mock coding state with language, line_count
       - Verify coding-specific prompt built
       - Verify language extracted

    8. test_generate_summary_with_agent_task
       - Call with agent_task="Review financials"
       - Verify task included in prompt
       - Verify context provided

    9. test_generate_summary_with_user_interaction
       - Call with user_interaction="submit"
       - Verify interaction included in prompt
       - Verify action captured

    10. test_generate_summary_invalid_canvas_type_raises
       - Call with canvas_type="invalid"
       - Verify raises ValueError
       - Verify error message lists valid types

    Pattern from Phase 113: Use AsyncMock for async BYOK handler, use pytest.mark.asyncio
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_canvas_summary_coverage.py::TestSummaryGeneration -v</verify>
  <done>All 10 tests pass, all canvas types generation covered</done>
</task>

<task type="auto">
  <name>Add tests for caching behavior</name>
  <files>backend/tests/unit/llm/test_canvas_summary_coverage.py</files>
  <action>
    Add 8 tests for caching mechanisms:

    Create TestCaching class:

    1. test_generate_summary_cache_hit
       - First call: mock byok_handler, capture summary
       - Second call: same inputs, mock not called
       - Verify returns cached summary
       - Verify byok_handler.generate_response called only once

    2. test_generate_summary_cache_miss_different_inputs
       - First call with canvas_state={...}
       - Second call with different canvas_state={...}
       - Verify byok_handler called twice
       - Verify different cache keys

    3. test_cache_key_includes_canvas_type
       - Same state, different canvas_type
       - Verify cache miss
       - Verify both summaries generated

    4. test_cache_key_includes_agent_task
       - Same state, different agent_task
       - Verify cache miss
       - Verify both summaries generated

    5. test_cache_key_includes_user_interaction
       - Same state, different user_interaction
       - Verify cache miss
       - Verify both summaries generated

    6. test_clear_cache_empties_cache
       - Generate summaries for 3 different canvases
       - Call clear_cache()
       - Verify _summary_cache is empty dict
       - Verify subsequent calls regenerate

    7. test_clear_cache_logs_count
       - Pre-fill cache with 5 items
       - Clear cache
       - Verify log shows 5 cleared

    8. test_get_cache_stats
       - Generate 3 summaries
       - Call get_cache_stats()
       - Verify cache_size=3
       - Verify tracked_sessions=3
       - Verify supported_canvas_types=7

    Pattern from Phase 112: Test cache hit/miss with call counting (assert_called_once vs assert_called_twice)
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_canvas_summary_coverage.py::TestCaching -v</verify>
  <done>All 8 tests pass, caching behavior fully covered</done>
</task>

<task type="auto">
  <name>Add tests for timeout and error handling</name>
  <files>backend/tests/unit/llm/test_canvas_summary_coverage.py</files>
  <action>
    Add 8 tests for timeout and error scenarios:

    Create TestErrorHandling class:

    1. test_generate_summary_timeout_raises
       - Mock byok_handler.generate_response to delay >2s
       - Call with timeout_seconds=1
       - Verify asyncio.TimeoutError raised
       - Verify warning logged

    2. test_generate_summary_custom_timeout
       - Mock slow byok_handler
       - Call with timeout_seconds=5
       - Verify completes within timeout
       - Verify summary returned

    3. test_generate_summary_llm_error_raises
       - Mock byok_handler to raise Exception
       - Verify exception propagates
       - Verify error logged

    4. test_generate_summary_with_json_serialization
       - Complex canvas_state with nested dicts, lists
       - Verify cache key generated without error
       - Verify JSON serialization works

    5. test_fallback_to_metadata_with_components
       - Mock canvas_state with components list
       - Verify fallback includes component types
       - Verify returns "Agent presented button, text on sheets canvas"

    6. test_fallback_to_metadata_with_critical_data
       - Mock state with workflow_id, revenue, amount
       - Verify fallback extracts critical fields
       - Verify formatted correctly

    7. test_fallback_to_metadata_empty_state
       - Call with empty canvas_state={}
       - Verify returns "Agent presented generic canvas"
       - Verify handles gracefully

    8. test_fallback_to_metadata_terminal_command
       - Mock state with command="pytest", exit_code=0
       - Verify fallback includes "command: pytest"
       - Verify captures action

    Pattern from Phase 113: Use asyncio.wait_for to test timeout, use pytest.raises for error paths
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_canvas_summary_coverage.py::TestErrorHandling -v</verify>
  <done>All 8 tests pass, timeout and error handling covered</done>
</task>

<task type="auto">
  <name>Add tests for utility and helper methods</name>
  <files>backend/tests/unit/llm/test_canvas_summary_coverage.py</files>
  <action>
    Add 8 tests for utility methods and helpers:

    Create TestUtilityMethods class:

    1. test_get_supported_canvas_types
       - Call get_supported_canvas_types()
       - Verify returns list of 7 types
       - Verify includes all expected types

    2. test_is_canvas_type_supported_valid
       - Parametrize all 7 valid types
       - Verify returns True for each

    3. test_is_canvas_type_supported_invalid
       - Call with "invalid_type"
       - Verify returns False

    4. test_get_total_cost_tracked
       - Generate 3 summaries
       - Call get_total_cost_tracked()
       - Verify returns sum of tracked costs
       - Verify includes all sessions

    5. test_get_canvas_prompt_instructions_valid
       - Parametrize all 7 canvas types
       - Verify returns non-None string for each
       - Verify instructions are type-specific

    6. test_get_canvas_prompt_instructions_invalid
       - Call with invalid canvas_type
       - Verify returns None
       - Verify handles gracefully

    7. test_calculate_semantic_richness
       - Call with rich summary (50-100 words, diverse vocabulary)
       - Verify score ≥0.8
       - Verify semantic variety detected

    8. test_detect_hallucination_basic
       - Mock summary with "I'm not sure" / "might be" patterns
       - Verify hallucination score calculated
       - Verify uncertainty detected

    Pattern from Phase 112: Test utility methods with both valid and invalid inputs
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_canvas_summary_coverage.py::TestUtilityMethods -v</verify>
  <done>All 8 tests pass, utility methods covered</done>
</task>

</tasks>

<verification>
After completing all tasks, run coverage report:

```bash
cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_canvas_summary_coverage.py --cov=core/llm/canvas_summary_service --cov-report=term-missing --cov-report=json
```

Verify:
- New test file has 30+ tests
- Coverage for core/llm/canvas_summary_service.py ≥70%
- All tests passing
- No test isolation issues
</verification>

<success_criteria>
1. 30+ new tests added in test_canvas_summary_coverage.py
2. canvas_summary_service.py coverage ≥70%
3. All 7 canvas types tested with generation
4. Caching behavior (hit/miss/clear/stats) fully tested
5. Metadata fallback on LLM failure tested
6. All new tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/114-llm-services-coverage/114-03-SUMMARY.md` with coverage metrics achieved
</output>
