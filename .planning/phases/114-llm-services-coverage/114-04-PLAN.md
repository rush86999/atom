---
phase: 114-llm-services-coverage
plan: 04
type: execute
wave: 2
depends_on:
  - "114-01"
  - "114-02"
  - "114-03"
files_modified:
  - backend/tests/unit/llm/test_byok_handler_coverage.py
  - backend/tests/unit/llm/test_cognitive_tier_coverage.py
  - backend/tests/unit/llm/test_canvas_summary_coverage.py
autonomous: true

must_haves:
  truths:
    - "All 3 LLM services achieve 60%+ coverage target"
    - "Gap analysis identifies remaining uncovered lines"
    - "Critical paths (multi-provider routing, token counting, tier escalation) fully tested"
    - "Edge cases and error handling paths covered"
  artifacts:
    - path: "backend/tests/unit/llm/test_byok_handler_coverage.py"
      provides: "Extended BYOK handler tests for gap closure"
      min_lines: 1000
    - path: "backend/tests/unit/llm/test_cognitive_tier_coverage.py"
      provides: "Extended cognitive tier tests for gap closure"
      min_lines: 800
    - path: "backend/tests/unit/llm/test_canvas_summary_coverage.py"
      provides: "Extended canvas summary tests for gap closure"
      min_lines: 900
  key_links:
    - from: "extended test files"
      to: "backend/core/llm/*.py"
      via: "Gap analysis driven additional tests"
      pattern: "Additional tests for remaining uncovered lines"
---

<objective>
Add gap-filling tests to achieve 60%+ coverage for all 3 LLM services.

**Purpose:** Plans 01-03 establish baseline coverage. This plan analyzes remaining uncovered lines and adds targeted tests to close gaps and reach the 60% target for all three services.

**Output:** 20+ additional tests targeting specific uncovered lines, all services at 60%+ coverage
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-llm-services-coverage/114-01-SUMMARY.md
@.planning/phases/114-llm-services-coverage/114-02-SUMMARY.md
@.planning/phases/114-llm-services-coverage/114-03-SUMMARY.md

# Source files
@backend/core/llm/byok_handler.py
@backend/core/llm/cognitive_tier_system.py
@backend/core/llm/canvas_summary_service.py

# Coverage gaps (to be identified after Plans 01-03)
# Plan tasks will be adjusted based on actual coverage reports
</context>

<tasks>

<task type="auto">
  <name>Run coverage analysis and identify gaps</name>
  <files>backend/tests/unit/llm/</files>
  <action>
    Run coverage reports for all three services to identify remaining uncovered lines:

    ```bash
    cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_byok_handler_coverage.py tests/unit/llm/test_cognitive_tier_coverage.py tests/unit/llm/test_canvas_summary_coverage.py --cov=core/llm/byok_handler --cov=core/llm/cognitive_tier_system --cov=core/llm/canvas_summary_service --cov-report=term-missing --cov-report=json:tests/coverage_reports/metrics/phase_114_plan04_coverage.json
    ```

    Document uncovered lines by service:
    1. byok_handler.py: List uncovered line numbers
    2. cognitive_tier_system.py: List uncovered line numbers
    3. canvas_summary_service.py: List uncovered line numbers

    Identify patterns:
    - Error handling paths (except blocks)
    - Edge cases (empty inputs, None values)
    - Conditional branches (if/else not yet tested)
    - Helper methods not yet called

    Create gap analysis summary in comments for next tasks
  </action>
  <verify>ls -la backend/tests/coverage_reports/metrics/phase_114_plan04_coverage.json && cat backend/tests/coverage_reports/metrics/phase_114_plan04_coverage.json | jq '.files | keys'</verify>
  <done>Coverage report generated, gaps identified by service and line number</done>
</task>

<task type="auto">
  <name>Add gap-filling tests for byok_handler.py</name>
  <files>backend/tests/unit/llm/test_byok_handler_coverage.py</files>
  <action>
    Based on coverage gaps identified in Task 1, add tests for uncovered lines:

    Common gap areas (adjust based on actual coverage report):

    Create TestGapFillingBYOK class:

    1. Test uncovered error paths in get_ranked_providers
       - Mock pricing fetcher failures
       - Test exception handling in provider ranking
       - Verify fallback behavior

    2. Test uncovered conditionals in analyze_query_complexity
       - Edge case scores at threshold boundaries
       - Pattern combinations not yet tested
       - Special character handling

    3. Test utility method uncovered branches
       - get_provider_comparison with specific model states
       - get_cheapest_models with pricing variations
       - classify_cognitive_tier with task_type variations

    4. Test _is_trial_restricted method if uncovered
       - Mock environment for trial mode
       - Verify restriction detection
       - Test unrestricted paths

    5. Test exception handlers not yet covered
       - Database session errors in tier service init
       - Client initialization failures
       - API key resolution failures

    Pattern from Phase 113: Target specific uncovered lines identified in coverage report
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_byok_handler_coverage.py::TestGapFillingBYOK -v --cov=core/llm/byok_handler --cov-report=term-missing</verify>
  <done>Additional tests added, byok_handler.py coverage increased to 60%+</done>
</task>

<task type="auto">
  <name>Add gap-filling tests for cognitive_tier_system.py</name>
  <files>backend/tests/unit/llm/test_cognitive_tier_coverage.py</files>
  <action>
    Based on coverage gaps identified in Task 1, add tests for uncovered lines:

    Create TestGapFillingCognitiveTier class:

    1. Test uncovered branches in _calculate_complexity_score
       - Specific pattern combinations not yet tested
       - Edge case score values at thresholds
       - Task type None handling

    2. Test uncovered branches in classify method
       - Token count exact threshold values
       - Complexity score exact threshold values
       - Tier mapping boundary conditions

    3. Test get_tier_models if branches uncovered
       - Invalid tier input
       - Empty tier mappings

    4. Test get_tier_description if uncovered
       - Tier enum edge cases
       - Description content validation

    5. Test regex pattern edge cases
       - Pattern matches at word boundaries
       - Case sensitivity variations
       - Special characters in prompts

    Pattern from Phase 112: Focus on branch coverage (if/else paths)
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_cognitive_tier_coverage.py::TestGapFillingCognitiveTier -v --cov=core/llm/cognitive_tier_system --cov-report=term-missing</verify>
  <done>Additional tests added, cognitive_tier_system.py coverage increased to 60%+</done>
</task>

<task type="auto">
  <name>Add gap-filling tests for canvas_summary_service.py</name>
  <files>backend/tests/unit/llm/test_canvas_summary_coverage.py</files>
  <action>
    Based on coverage gaps identified in Task 1, add tests for uncovered lines:

    Create TestGapFillingCanvasSummary class:

    1. Test uncovered branches in generate_summary
       - Cache hit path variations
       - Timeout variations
       - Error handling paths

    2. Test _build_prompt uncovered branches
       - Canvas state variations
       - Missing state fields
       - Special characters in state

    3. Test _fallback_to_metadata uncovered branches
       - Empty components list
       - No critical data fields
       - Mixed data scenarios

    4. Test utility methods if uncovered
       - get_canvas_prompt_instructions for all types
       - _calculate_semantic_richness edge cases
       - _detect_hallucination patterns

    5. Test error handlers
       - JSON serialization failures
       - Cache key collisions
       - State validation errors

    Pattern from Phase 113: Test both success and failure paths for comprehensive coverage
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_canvas_summary_coverage.py::TestGapFillingCanvasSummary -v --cov=core/llm/canvas_summary_service --cov-report=term-missing</verify>
  <done>Additional tests added, canvas_summary_service.py coverage increased to 60%+</done>
</task>

</tasks>

<verification>
After completing all tasks, run final coverage report:

```bash
cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_byok_handler_coverage.py tests/unit/llm/test_cognitive_tier_coverage.py tests/unit/llm/test_canvas_summary_coverage.py --cov=core/llm/byok_handler --cov=core/llm/cognitive_tier_system --cov=core/llm/canvas_summary_service --cov-report=term-missing --cov-report=json:tests/coverage_reports/metrics/phase_114_plan04_final.json
```

Verify:
- All 3 services show ≥60% coverage
- Coverage report shows "phase_114_plan04_final.json" created
- All tests passing
- Coverage gaps documented
</verification>

<success_criteria>
1. All 3 services achieve 60%+ coverage:
   - byok_handler.py ≥60%
   - cognitive_tier_system.py ≥60%
   - canvas_summary_service.py ≥60%
2. Gap analysis completed and documented
3. All new tests passing
4. Coverage report saved for Plan 05 verification
</success_criteria>

<output>
After completion, create `.planning/phases/114-llm-services-coverage/114-04-SUMMARY.md` with gap analysis and final coverage metrics
</output>
