---
phase: 114-llm-services-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/llm/test_cognitive_tier_coverage.py
autonomous: true

must_haves:
  truths:
    - "Coverage report shows 70%+ coverage for llm/cognitive_tier_system.py"
    - "Tests cover all 5 cognitive tiers (MICRO, STANDARD, VERSATILE, HEAVY, COMPLEX)"
    - "Tests cover token estimation and complexity scoring"
    - "Tests cover tier model recommendations"
    - "All helper methods (_estimate_tokens, _calculate_complexity_score) tested"
  artifacts:
    - path: "backend/tests/unit/llm/test_cognitive_tier_coverage.py"
      provides: "Comprehensive coverage tests for CognitiveClassifier"
      min_lines: 600
  key_links:
    - from: "backend/tests/unit/llm/test_cognitive_tier_coverage.py"
      to: "backend/core/llm/cognitive_tier_system.py"
      via: "Direct class testing with edge cases and boundary conditions"
      pattern: "classify|_calculate_complexity_score|_estimate_tokens|get_tier_models"
---

<objective>
Add comprehensive tests for CognitiveClassifier to reach 70%+ coverage.

**Purpose:** The cognitive tier system has existing tests from Phase 68 (2420 total lines across 4 test files) but may have coverage gaps in edge cases, boundary conditions, and helper methods. This plan fills those gaps to achieve 70%+ coverage.

**Output:** 30+ new tests covering edge cases, boundary conditions, and helper methods
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source files
@backend/core/llm/cognitive_tier_system.py (297 lines, 6 public methods)
@backend/tests/test_cognitive_tier_classification.py (455 lines, 90+ existing tests)
@backend/tests/test_cognitive_tier_api.py (485 lines, API tests)
@backend/tests/test_cognitive_tier_service.py (554 lines, service tests)
@backend/tests/test_cognitive_tier_e2e.py (926 lines, E2E tests)

# Existing test patterns
@TestCanvasSummaryServiceInitialization (init tests)
@TestPromptBuilding (prompt construction tests)
@TestSummaryGeneration (generation with mocks)
</context>

<tasks>

<task type="auto">
  <name>Add tests for cognitive tier boundary conditions</name>
  <files>backend/tests/unit/llm/test_cognitive_tier_coverage.py</files>
  <action>
    Create test file backend/tests/unit/llm/test_cognitive_tier_coverage.py with boundary condition tests:

    Create TestTierBoundaries class:

    1. test_classify_micro_tier_upper_boundary
       - Test with exactly 100 characters (~25 tokens)
       - Verify returns CognitiveTier.MICRO

    2. test_classify_micro_to_standard_transition
       - Test with 101 characters (~26 tokens, crosses threshold)
       - Verify returns CognitiveTier.STANDARD

    3. test_classify_standard_upper_boundary
       - Test with exactly 500 characters (~125 tokens)
       - Verify returns CognitiveTier.STANDARD

    4. test_classify_standard_to_versatile_transition
       - Test with 501 characters (~126 tokens)
       - Verify returns CognitiveTier.VERSATILE

    5. test_classify_versatile_upper_boundary
       - Test with exactly 2000 characters (~500 tokens)
       - Verify returns CognitiveTier.VERSATILE

    6. test_classify_versatile_to_heavy_transition
       - Test with 2001 characters (~501 tokens)
       - Verify returns CognitiveTier.HEAVY

    7. test_classify_heavy_upper_boundary
       - Test with exactly 5000 characters (~1250 tokens)
       - Verify returns CognitiveTier.HEAVY

    8. test_classify_heavy_to_complex_transition
       - Test with 5001 characters (~1251 tokens)
       - Verify returns CognitiveTier.COMPLEX

    9. test_classify_very_long_prompt_complex
       - Test with 21000 characters (~5250 tokens)
       - Verify returns CognitiveTier.COMPLEX

    10. test_classify_empty_prompt
       - Test with empty string ""
       - Verify returns CognitiveTier.MICRO

    Pattern from Phase 113: Test exact boundaries to ensure tier thresholds are correct
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_cognitive_tier_coverage.py::TestTierBoundaries -v</verify>
  <done>All 10 tests pass, tier boundary conditions covered</done>
</task>

<task type="auto">
  <name>Add tests for complexity scoring edge cases</name>
  <files>backend/tests/unit/llm/test_cognitive_tier_coverage.py</files>
  <action>
    Add 8 tests for _calculate_complexity_score method edge cases:

    Create TestComplexityScoring class:

    1. test_calculate_complexity_with_code_block_boost
       - Prompt with ```python code block
       - Verify +3 complexity added for code blocks
       - Verify tier appropriately elevated

    2. test_calculate_complexity_multiple_code_blocks
       - Prompt with multiple ``` blocks
       - Verify only +3 applied (code is code, not cumulative)

    3. test_calculate_complexity_task_type_adjustments
       - Parametrize task_type: ["code", "analysis", "reasoning", "agentic"]
       - Verify +2 adjustment applied for each
       - Verify tier elevated accordingly

    4. test_calculate_complexity_chat_task_type_reduction
       - Moderate prompt with task_type="chat"
       - Verify -1 adjustment applied
       - Verify tier reduced accordingly

    5. test_calculate_complexity_general_task_type_reduction
       - Moderate prompt with task_type="general"
       - Verify -1 adjustment applied

    6. test_calculate_complexity_unknown_task_type
       - Prompt with task_type="unknown"
       - Verify 0 adjustment (no crash)
       - Verify handles gracefully

    7. test_calculate_complexity_combined_patterns
       - Prompt with code + technical + advanced patterns
       - Verify scores accumulate correctly
       - Verify final tier reflects all patterns

    8. test_calculate_complexity_minimum_score_floor
       - Prompt with many simple patterns (negative weight)
       - Verify score never goes below -2
       - Verify minimum enforced

    Pattern from Phase 112: Test both positive and negative edge cases
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_cognitive_tier_coverage.py::TestComplexityScoring -v</verify>
  <done>All 8 tests pass, complexity scoring edge cases covered</done>
</task>

<task type="auto">
  <name>Add tests for token estimation and tier models</name>
  <files>backend/tests/unit/llm/test_cognitive_tier_coverage.py</files>
  <action>
    Add 8 tests for _estimate_tokens and get_tier_models methods:

    Create TestTokenEstimation class:

    1. test_estimate_tokens_empty_string
       - Call with ""
       - Verify returns 0

    2. test_estimate_tokens_heuristic
       - Test with known lengths
       - Verify 1 token ≈ 4 characters (len // 4)
       - Verify consistent application

    3. test_estimate_tokens_unicode
       - Test with emoji and unicode characters
       - Verify counts characters, not graphemes
       - Verify handles gracefully

    4. test_estimate_tokens_very_long
       - Test with 1M character string
       - Verify returns 250000
       - Verify no overflow

    Create TestTierModels class:

    5. test_get_tier_models_micro
       - Call with CognitiveTier.MICRO
       - Verify returns list of budget models
       - Verify includes "deepseek-chat", "gemini-3-flash"

    6. test_get_tier_models_standard
       - Call with CognitiveTier.STANDARD
       - Verify returns balanced models
       - Verify includes "gpt-4o-mini", "claude-3-haiku"

    7. test_get_tier_models_complex
       - Call with CognitiveTier.COMPLEX
       - Verify returns premium models
       - Verify includes "gpt-5", "o3", "claude-4-opus"

    8. test_get_tier_models_all_tiers
       - Parametrize all 5 tiers
       - Verify each returns non-empty list
       - Verify models appropriate for tier

    Pattern from Phase 113: Use parametrize for testing all enum values
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_cognitive_tier_coverage.py::TestTokenEstimation -v && pytest tests/unit/llm/test_cognitive_tier_coverage.py::TestTierModels -v</verify>
  <done>All 8 tests pass, token estimation and tier models covered</done>
</task>

<task type="auto">
  <name>Add tests for get_tier_description and edge cases</name>
  <files>backend/tests/unit/llm/test_cognitive_tier_coverage.py</files>
  <action>
    Add 6 tests for remaining uncovered methods and edge cases:

    Create TestTierDescriptions class:

    1. test_get_tier_description_all_tiers
       - Parametrize all 5 CognitiveTier values
       - Verify returns non-empty string
       - Verify matches TIER_THRESHOLDS description

    2. test_get_tier_description_content
       - Verify descriptions include token ranges
       - Verify descriptions include complexity info
       - Verify human-readable format

    Create TestClassifierEdgeCases class:

    3. test_classify_with_none_task_type
       - Call with task_type=None
       - Verify handles gracefully
       - Verify classification based only on prompt

    4. test_classify_whitespace_only
       - Call with "   " (whitespace)
       - Verify returns MICRO tier
       - Verify handles gracefully

    5. test_classify_special_characters
       - Call with "!@#$%^&*()_+"
       - Verify returns MICRO tier
       - Verify no regex crashes

    6. test_classify_with_newlines_tabs
       - Call with multi-line string with tabs
       - Verify token count includes whitespace
       - Verify classification accurate

    Pattern from Phase 112: Test defensive coding with None, empty strings, special characters
  </action>
  <verify>cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_cognitive_tier_coverage.py::TestTierDescriptions -v && pytest tests/unit/llm/test_cognitive_tier_coverage.py::TestClassifierEdgeCases -v</verify>
  <done>All 6 tests pass, remaining methods and edge cases covered</done>
</task>

</tasks>

<verification>
After completing all tasks, run coverage report:

```bash
cd backend && PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest tests/unit/llm/test_cognitive_tier_coverage.py --cov=core/llm/cognitive_tier_system --cov-report=term-missing --cov-report=json
```

Verify:
- New test file has 30+ tests
- Coverage for core/llm/cognitive_tier_system.py ≥70%
- All tests passing
- No test isolation issues
</verification>

<success_criteria>
1. 30+ new tests added in test_cognitive_tier_coverage.py
2. cognitive_tier_system.py coverage ≥70%
3. All 5 cognitive tiers tested with boundary conditions
4. All helper methods (_estimate_tokens, _calculate_complexity_score) tested
5. All new tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/114-llm-services-coverage/114-02-SUMMARY.md` with coverage metrics achieved
</output>
