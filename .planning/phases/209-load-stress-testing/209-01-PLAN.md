---
phase: 209-load-stress-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/load/locustfile.py
  - backend/tests/load/conftest.py
  - backend/tests/load/README.md
  - backend/tests/load/scenarios/__init__.py
autonomous: true

must_haves:
  truths:
    - "Locust test suite is runnable with 'locust -f tests/load/locustfile.py'"
    - "Test users can authenticate and make authenticated requests to API endpoints"
    - "Load test scenarios exist for 5+ critical endpoints from Phase 208 benchmarks"
    - "Load tests can run in headless mode for CI/CD integration"
  artifacts:
    - path: "backend/tests/load/locustfile.py"
      provides: "Main Locust file defining user behaviors and scenarios"
      min_lines: 150
    - path: "backend/tests/load/conftest.py"
      provides: "Shared fixtures for load testing (auth, test data)"
      min_lines: 80
    - path: "backend/tests/load/README.md"
      provides: "Documentation for running and interpreting load tests"
      min_lines: 100
  key_links:
    - from: "backend/tests/load/locustfile.py"
      to: "backend/api/health_routes.py"
      via: "HTTP GET /health/live requests"
      pattern: "client\.get\(['\"]\/health\/"
    - from: "backend/tests/load/locustfile.py"
      to: "backend/api/agent_governance_routes.py"
      via: "Governance check endpoint"
      pattern: "governance.*check"
---

<objective>
Create Locust load testing infrastructure with realistic user scenarios for critical endpoints identified in Phase 208 benchmarks.

Purpose: Load testing validates system behavior under concurrent user load (100-1000 users), identifies bottlenecks that only appear under concurrent access, and establishes capacity limits for production deployment.

Output: Runnable Locust test suite with 5-8 critical endpoint scenarios, authentication, and documentation.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/209-load-stress-testing/209-RESEARCH.md
@.planning/phases/208-integration-performance-testing/208-07-PERFORMANCE-METRICS.md
@backend/tests/integration/performance/conftest.py
@backend/requirements-testing.txt

# Critical Endpoints from Phase 208 Benchmarks
53 benchmarks documented across:
- Workflow: validation, execution, sorting (13 benchmarks)
- Episode: detection, creation, segmentation, retrieval (10 benchmarks)
- Governance: cache, checks, resolution, permissions (15 benchmarks)
- API Latency: health checks, agent endpoints (7 benchmarks)
- Database: agent queries, episode queries (8 benchmarks)

# Locust Configuration
- Locust 2.15.0+ already installed in requirements-testing.txt
- Default host: http://localhost:8000
- Web UI port: 8089
- Headless mode: --headless flag for CI/CD
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Locust infrastructure and shared fixtures</name>
  <files>backend/tests/load/conftest.py</files>
  <action>
Create shared fixtures for load testing in backend/tests/load/conftest.py:

1. Import required modules: pytest, random, uuid
2. Create fixture `load_test_config` returning dict with:
   - host: "http://localhost:8000"
   - default_wait_min: 1
   - default_wait_max: 3
   - auth_endpoint: "/api/v1/auth/login"
3. Create fixture `test_user_credentials` returning list of 10 test users with:
   - email: "load_test_{i}@example.com"
   - password: "test_password_123"
4. Create fixture `agent_ids` returning list of 100 test agent IDs
5. Create fixture `workflow_ids` returning list of 20 test workflow IDs
6. Add docstrings explaining each fixture's purpose

DO NOT create any LocustFile classes in conftest.py - those go in locustfile.py
  </action>
  <verify>
cat backend/tests/load/conftest.py | grep -E "(def load_test_config|def test_user_credentials|def agent_ids|def workflow_ids)" | wc -l
# Should output 4
  </verify>
  <done>
Shared fixtures exist with 4 fixture functions for load test configuration and test data
  </done>
</task>

<task type="auto">
  <name>Task 2: Create main Locust file with authentication</name>
  <files>backend/tests/load/locustfile.py</files>
  <action>
Create backend/tests/load/locustfile.py with:

1. Imports: from locust import HttpUser, task, between, events
2. Base class `AtomAPIUser(HttpUser)`:
   - wait_time = between(1, 3)
   - host = "http://localhost:8000"
   - on_start(self): calls self.login()
   - login(self): POST to /api/v1/auth/login, store token in self.token
3. Add health check task @task(1): GET /health/live
4. Add authentication error handling: if login fails, log warning but continue (some endpoints don't require auth)

Use the authentication pattern from research doc:
```python
def login(self):
    response = self.client.post("/api/v1/auth/login", json={
        "email": "load_test@example.com",
        "password": "test_password_123"
    })
    if response.status_code == 200:
        self.token = response.json().get("access_token")
```

DO NOT create endpoint-specific scenarios yet - that's Task 3
  </action>
  <verify>
grep -E "(class AtomAPIUser|def login|def on_start|@task)" backend/tests/load/locustfile.py | wc -l
# Should output at least 6 (class, login, on_start, 3+ task definitions)
  </verify>
  <done>
Base Locust user class exists with authentication, health check task, and wait times configured
  </done>
</task>

<task type="auto">
  <name>Task 3: Add endpoint-specific load test scenarios</name>
  <files>backend/tests/load/locustfile.py</files>
  <action>
Extend locustfile.py with endpoint-specific scenarios. Add these user classes:

1. `AgentAPIUser(HttpUser)` extending AtomAPIUser:
   - @task(5) list_agents: GET /api/v1/agents
   - @task(3) get_agent: GET /api/v1/agents/{random_id}
   - @task(1) create_agent: POST /api/v1/agents with test data
   - Use Authorization header if token exists

2. `WorkflowExecutionUser(HttpUser)`:
   - wait_time = between(2, 5) (longer for workflows)
   - @task(2) execute_workflow: POST /api/v1/workflows/test_workflow/execute
   - @task(1) list_workflows: GET /api/v1/workflows

3. `GovernanceCheckUser(HttpUser)`:
   - @task(4) check_permission: POST /api/agent-governance/check-permission
   - @task(2) get_cache_stats: GET /api/agent-governance/cache-stats

4. `EpisodeAPIUser(HttpUser)`:
   - @task(3) list_episodes: GET /api/v1/episodes
   - @task(2) get_episode: GET /api/v1/episodes/{random_id}

Use catch_response=True for validation:
```python
with self.client.get(url, catch_response=True) as response:
    if response.status_code == 200:
        response.success()
    else:
        response.failure(f"Unexpected status: {response.status_code}")
```

All classes should have on_start calling login().
  </action>
  <verify>
grep -E "class (AgentAPIUser|WorkflowExecutionUser|GovernanceCheckUser|EpisodeAPIUser)" backend/tests/load/locustfile.py | wc -l
# Should output 4
  </verify>
  <done>
Four user scenario classes exist covering agent, workflow, governance, and episode endpoints
  </done>
</task>

<task type="auto">
  <name>Task 4: Create load testing documentation</name>
  <files>backend/tests/load/README.md</files>
  <action>
Create backend/tests/load/README.md with:

1. Overview section: Explain what load tests are for
2. Prerequisites: Locust installed, running server
3. Running Locust:
   - Interactive mode: `locust -f tests/load/locustfile.py`
   - Headless mode: `locust -f tests/load/locustfile.py --headless -u 100 -r 10 -t 5m`
4. User Scenarios section: Document each user class and its tasks with weights
5. Critical Endpoints table:
   - Endpoint | Method | Purpose | Weight
   - /health/live | GET | Health check | 1
   - /api/v1/agents | GET | List agents | 5
   - /api/v1/agents/{id} | GET | Get agent | 3
   - /api/v1/workflows/{id}/execute | POST | Execute workflow | 2
   - /api/agent-governance/check-permission | POST | Governance check | 4
   - /api/v1/episodes | GET | List episodes | 3
6. Interpreting Results: Explain RPS, failure rate, response time percentiles
7. Troubleshooting: Common issues (server not running, auth failures, port conflicts)
8. CI Integration: How to run in GitHub Actions

Make it actionable and clear for someone who hasn't run load tests before.
  </action>
  <verify>
test -f backend/tests/load/README.md && grep -E "(Running Locust|User Scenarios|Critical Endpoints|Interpreting Results)" backend/tests/load/README.md | wc -l
# Should output 4 or more
  </verify>
  <done>
README exists with clear instructions for running load tests and interpreting results
  </done>
</task>

</tasks>

<verification>
Overall Phase Checks:
1. Locust can import the file: `python -c "from tests.load.locustfile import *"` (no errors)
2. Locust help works: `locust -f tests/load/locustfile.py --help`
3. All user classes are discoverable by Locust
4. Authentication flow is properly defined
5. At least 5 different endpoint scenarios are covered
</verification>

<success_criteria>
- Locust file imports without errors
- 4 user scenario classes defined (Agent, Workflow, Governance, Episode)
- Authentication flow handles failures gracefully
- README provides clear instructions for running load tests
- At least 5 critical endpoints from Phase 208 are covered
</success_criteria>

<output>
After completion, create `.planning/phases/209-load-stress-testing/209-01-SUMMARY.md` with:
- Number of user scenarios created
- Endpoints covered
- Any issues encountered (port conflicts, auth setup)
</output>
