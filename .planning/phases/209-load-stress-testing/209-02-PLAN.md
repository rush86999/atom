---
phase: 209-load-stress-testing
plan: 02
type: execute
wave: 2
depends_on: ["209-01"]
files_modified:
  - backend/tests/load/scenarios/agent_api.py
  - backend/tests/load/scenarios/workflow_api.py
  - backend/tests/load/scenarios/governance_api.py
  - backend/tests/load/scenarios/__init__.py
autonomous: true

must_haves:
  truths:
    - "Extended agent scenarios cover create, update, delete operations"
    - "Workflow scenarios include parallel execution and error handling"
    - "Governance scenarios test all maturity levels (STUDENT, INTERN, SUPERVISED, AUTONOMOUS)"
    - "Scenarios can be imported and used in locustfile.py"
  artifacts:
    - path: "backend/tests/load/scenarios/agent_api.py"
      provides: "Extended agent API load test scenarios"
      min_lines: 100
    - path: "backend/tests/load/scenarios/workflow_api.py"
      provides: "Workflow execution load test scenarios with error paths"
      min_lines: 80
    - path: "backend/tests/load/scenarios/governance_api.py"
      provides: "Governance check scenarios for all maturity levels"
      min_lines: 80
  key_links:
    - from: "backend/tests/load/scenarios/agent_api.py"
      to: "backend/api/agent_governance_routes.py"
      via: "Agent CRUD endpoint testing"
      pattern: "(POST|PUT|DELETE).*\/api\/v1\/agents"
    - from: "backend/tests/load/scenarios/governance_api.py"
      to: "backend/core/agent_governance_service.py"
      via: "Maturity level permission checks"
      pattern: "(STUDENT|INTERN|SUPERVISED|AUTONOMOUS)"
---

<objective>
Create extended load test scenarios for agent CRUD operations, workflow execution patterns, and governance maturity levels.

Purpose: Expand load test coverage beyond basic endpoints to include realistic user operations (create/update agents, execute workflows with errors, test all maturity levels). This validates performance under complex usage patterns.

Output: Modular scenario files that can be imported into locustfile.py, covering 8+ endpoint variations with realistic weights.
</object>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/209-load-stress-testing/209-01-PLAN.md
@.planning/phases/209-load-stress-testing/209-RESEARCH.md
@backend/api/agent_governance_routes.py
@backend/core/agent_governance_service.py
@backend/tests/load/locustfile.py

# Governance Maturity Levels
- STUDENT: Read-only, blocked from automated triggers
- INTERN: Proposal workflow, human approval required
- SUPERVISED: Real-time supervision, pause/correct/terminate
- AUTONOMOUS: Full execution, no oversight

# Scenario Modularity Pattern
Create separate scenario modules that can be imported into locustfile.py
Each scenario module defines task methods that can be mixed into user classes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create agent API CRUD scenarios</name>
  <files>backend/tests/load/scenarios/agent_api.py</files>
  <action>
Create backend/tests/load/scenarios/agent_api.py with:

1. Imports: from locust import task
2. Mixin class `AgentCRUDTasks` with tasks:
   - @task(5) list_agents: GET /api/v1/agents with pagination params
   - @task(3) get_agent: GET /api/v1/agents/{id} with random agent_id
   - @task(1) create_agent: POST /api/v1/agents with realistic payload:
     ```python
     {
         "name": f"Load Test Agent {random.randint(1, 10000)}",
         "category": "automation",
         "module_path": "test.module",
         "class_name": "TestAgent",
         "maturity": "AUTONOMOUS",
         "confidence_score": 0.9
     }
     ```
   - @task(1) update_agent: PUT /api/v1/agents/{id} with status update
   - @task(1) delete_agent: DELETE /api/v1/agents/{id} for test agents only

3. Add helper method `_get_auth_headers(self)` that returns Authorization header if self.token exists

4. Use catch_response for validation:
   - 200/201 for success
   - 404 is acceptable (agent not found)
   - 401 indicates auth issue
   - 500 should be marked as failure

5. Add docstring explaining the weight distribution (read-heavy is realistic)
  </action>
  <verify>
grep -E "(class AgentCRUDTasks|def list_agents|def get_agent|def create_agent|def update_agent|def delete_agent)" backend/tests/load/scenarios/agent_api.py | wc -l
# Should output 6 or more
  </verify>
  <done>
AgentCRUDTasks mixin exists with 5 task methods covering CRUD operations with realistic weights
  </done>
</task>

<task type="auto">
  <name>Task 2: Create workflow execution scenarios</name>
  <files>backend/tests/load/scenarios/workflow_api.py</files>
  <action>
Create backend/tests/load/scenarios/workflow_api.py with:

1. Imports: from locust import task
2. Mixin class `WorkflowExecutionTasks` with tasks:
   - @task(3) list_workflows: GET /api/v1/workflows
   - @task(2) execute_simple_workflow: POST /api/v1/workflows/simple/execute
   - @task(1) execute_parallel_workflow: POST /api/v1/workflows/parallel/execute
   - @task(1) execute_error_workflow: POST /api/v1/workflows/error-test/execute (tests error handling)
   - @task(1) get_workflow_status: GET /api/v1/workflows/{id}/status
   - @task(1) get_workflow_analytics: GET /api/v1/workflows/analytics

3. Helper method `_generate_workflow_input(self)` returning:
   ```python
   {
       "input_data": {"test_key": f"test_value_{random.randint(1, 1000)}"},
       "parameters": {"timeout": 30}
   }
   ```

4. Error scenario specifically tests:
   - Workflow that fails validation
   - Verify 400/422 response (not 500)
   - Tests graceful degradation

5. Add docstring explaining workflow execution patterns
  </action>
  <verify>
grep -E "(class WorkflowExecutionTasks|def list_workflows|def execute.*workflow|def get_workflow)" backend/tests/load/scenarios/workflow_api.py | wc -l
# Should output 5 or more
  </verify>
  <done>
WorkflowExecutionTasks mixin exists with 6 task methods covering execution and status checks
  </done>
</task>

<task type="auto">
  <name>Task 3: Create governance maturity level scenarios</name>
  <files>backend/tests/load/scenarios/governance_api.py</files>
  <action>
Create backend/tests/load/scenarios/governance_api.py with:

1. Imports: from locust import task
2. Mixin class `GovernanceCheckTasks` with tasks testing all maturity levels:
   - @task(4) check_student_permission: POST with maturity=STUDENT, action_complexity=1
   - @task(3) check_intern_permission: POST with maturity=INTERN, action_complexity=2
   - @task(2) check_supervised_permission: POST with maturity=SUPERVISED, action_complexity=3
   - @task(2) check_autonomous_permission: POST with maturity=AUTONOMOUS, action_complexity=4
   - @task(3) get_cache_stats: GET /api/agent-governance/cache-stats
   - @task(1) invalidate_cache: POST /api/agent-governance/cache/invalidate

3. Helper method `_build_governance_payload(self, maturity, complexity)` returning:
   ```python
   {
       "agent_id": f"agent_{random.randint(1, 1000)}",
       "action": f"test_action_{random.choice(['read', 'write', 'execute', 'admin'])}",
       "action_complexity": complexity,
       "agent_maturity": maturity,
       "context": {"load_test": True}
   }
   ```

4. Validate governance responses:
   - Check for "allowed" key in response
   - STUDENT should be blocked from complexity > 1
   - AUTONOMOUS should be allowed for all actions

5. Add docstring explaining maturity level testing strategy
  </action>
  <verify>
grep -E "(class GovernanceCheckTasks|def check.*permission|def get_cache_stats|def invalidate_cache)" backend/tests/load/scenarios/governance_api.py | wc -l
# Should output 5 or more
  </verify>
  <done>
GovernanceCheckTasks mixin exists with 6 task methods covering all maturity levels and cache operations
  </done>
</task>

<task type="auto">
  <name>Task 4: Update locustfile.py to use scenario mixins</name>
  <files>backend/tests/load/locustfile.py</files>
  <action>
Update backend/tests/load/locustfile.py to use the new scenario mixins:

1. Add imports at top:
   ```python
   from tests.load.scenarios.agent_api import AgentCRUDTasks
   from tests.load.scenarios.workflow_api import WorkflowExecutionTasks
   from tests.load.scenarios.governance_api import GovernanceCheckTasks
   ```

2. Update user classes to inherit from mixins:
   - class `AgentAPIUser(HttpUser, AgentCRUDTasks)`
   - class `WorkflowUser(HttpUser, WorkflowExecutionTasks)`
   - class `GovernanceUser(HttpUser, GovernanceCheckTasks)`
   - class `ComprehensiveUser(HttpUser, AgentCRUDTasks, WorkflowExecutionTasks, GovernanceCheckTasks)`

3. Remove duplicate task methods that are now in mixins

4. Add comment explaining ComprehensiveUser is for "power user" simulation

5. Keep on_start and login methods in each class (mixins don't define auth)

DO NOT remove the health check task from AtomAPIUser base class
  </action>
  <verify>
grep -E "(from tests.load.scenarios|class.*User)" backend/tests/load/locustfile.py | wc -l
# Should show imports and 4+ user classes
  </verify>
  <done>
Locust file updated with scenario imports, user classes inherit from mixins, no duplicate task methods
  </done>
</task>

</tasks>

<verification>
Overall Phase Checks:
1. All scenario modules import without errors: `python -c "from tests.load.scenarios.agent_api import *"`
2. Locust validates the file: `locust -f tests/load/locustfile.py --headless --help`
3. 8+ distinct endpoint operations are covered across all scenarios
4. All 4 maturity levels are tested in governance scenarios
5. Mixin pattern is correctly implemented (no duplicate code)
</verification>

<success_criteria>
- 3 scenario modules created (agent_api, workflow_api, governance_api)
- 8+ distinct endpoint operations covered
- All 4 maturity levels tested
- Locust file updated to use mixins without duplication
- Import chain works correctly (scenarios -> locustfile)
</success_criteria>

<output>
After completion, create `.planning/phases/209-load-stress-testing/209-02-SUMMARY.md` with:
- Number of scenario modules created
- Total endpoint operations covered
- Maturity level coverage confirmation
</output>
