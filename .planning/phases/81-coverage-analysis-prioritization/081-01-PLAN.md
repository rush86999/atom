---
phase: 81-coverage-analysis-prioritization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/coverage_reports/metrics/coverage.json
  - backend/tests/coverage_reports/html/
  - backend/tests/coverage_reports/coverage_report_generator.py
autonomous: true

must_haves:
  truths:
    - "Coverage report generated showing all backend files with current coverage percentage"
    - "HTML coverage report accessible at backend/tests/coverage_reports/html/index.html"
    - "Coverage JSON generated at backend/tests/coverage_reports/metrics/coverage.json"
    - "Report includes line coverage, branch coverage, and missing lines per file"
    - "Coverage report generation is automated via Python script"
  artifacts:
    - path: "backend/tests/coverage_reports/coverage_report_generator.py"
      provides: "Automated coverage report generation script"
      min_lines: 100
      contains: "generate_coverage_report", "coverage.json", "html"
    - path: "backend/tests/coverage_reports/metrics/coverage.json"
      provides: "Machine-readable coverage metrics for analysis"
      contains: "totals", "percent_covered", "files"
    - path: "backend/tests/coverage_reports/html/index.html"
      provides: "Human-readable HTML coverage report with drill-down"
    - path: "backend/tests/coverage_reports/COVERAGE_REPORT_v3.2.md"
      provides: "Comprehensive coverage report document for v3.2 milestone"
      min_lines: 200
      contains: "Coverage Summary", "Module Breakdown", "Gap Analysis"
  key_links:
    - from: "coverage_report_generator.py"
      to: "coverage.json"
      via: "pytest-cov --cov-report=json"
      pattern: "subprocess.run.*pytest.*--cov-report=json"
    - from: "coverage_report_generator.py"
      to: "html/index.html"
      via: "pytest-cov --cov-report=html"
      pattern: "--cov-report=html"
    - from: "coverage.json"
      to: "COVERAGE_REPORT_v3.2.md"
      via: "JSON parsing and markdown generation"
      pattern: "json.load.*coverage.json"
---

<objective>
Generate comprehensive coverage report for v3.2 milestone.

Purpose: Establish the baseline coverage for backend services before expanding test coverage. The report provides detailed metrics on current coverage levels, identifies gaps, and serves as the foundation for prioritizing high-impact files in subsequent plans.

Output: Coverage report generator script, updated JSON metrics, HTML report, and comprehensive markdown documentation.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@backend/pytest.ini
@backend/tests/coverage_reports/BASELINE_COVERAGE.md
@backend/tests/coverage_reports/aggregate_coverage.py
@backend/tests/coverage_reports/metrics/coverage.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create coverage report generator script</name>
  <files>
    backend/tests/coverage_reports/coverage_report_generator.py
  </files>
  <action>
    Create backend/tests/coverage_reports/coverage_report_generator.py with:

    1. Main function generate_coverage_report() that:
       - Runs pytest with coverage: pytest --cov=core --cov=api --cov=tools --cov-report=json --cov-report=html --cov-report=term-missing
       - Captures output to tests/coverage_reports/metrics/coverage.json
       - Generates HTML report to tests/coverage_reports/html/
       - Returns coverage metrics dictionary

    2. Helper functions:
       - parse_coverage_json(filepath): Load and parse coverage.json
       - get_file_coverage(coverage_data, file_path): Get coverage for specific file
       - get_module_summary(coverage_data, module_name): Get summary for core/api/tools
       - calculate_coverage_trend(current, baseline): Compare with baseline

    3. Report generation:
       - generate_markdown_report(coverage_data, output_path): Create COVERAGE_REPORT_v3.2.md
       - Include sections: Executive Summary, Module Breakdown, File-by-File Details, Coverage Distribution

    4. CLI entry point:
       ```python
       if __name__ == "__main__":
           import sys
           output_format = sys.argv[1] if len(sys.argv) > 1 else "all"
           generate_coverage_report(output_format=output_format)
       ```

    Use existing patterns from aggregate_coverage.py (already in coverage_reports/).
    DO NOT modify existing aggregate_coverage.py - create new file alongside it.
  </action>
  <verify>
    python backend/tests/coverage_reports/coverage_report_generator.py  # Runs without errors
    ls -la backend/tests/coverage_reports/metrics/coverage.json  # File exists and is valid JSON
    python -c "import json; data=json.load(open('backend/tests/coverage_reports/metrics/coverage.json')); print('files' in data)"  # True
  </verify>
  <done>
    coverage_report_generator.py exists with generate_coverage_report() function
    Script runs pytest with coverage and generates JSON + HTML reports
    Helper functions for parsing and analysis are implemented
  </done>
</task>

<task type="auto">
  <name>Task 2: Execute coverage report generation</name>
  <files>
    backend/tests/coverage_reports/metrics/coverage.json
    backend/tests/coverage_reports/html/index.html
  </files>
  <action>
    1. Run the coverage report generator:
       ```bash
       cd /Users/rushiparikh/projects/atom/backend
       python tests/coverage_reports/coverage_report_generator.py
       ```

    2. Verify outputs:
       - coverage.json exists in tests/coverage_reports/metrics/
       - HTML report generated in tests/coverage_reports/html/
       - Terminal output shows coverage summary

    3. Handle any errors:
       - If tests fail, note the failures but continue (coverage from passing tests still valuable)
       - If coverage.json is malformed, check pytest-cov version compatibility
       - Ensure PYTHONPATH includes backend directory

    4. Capture current coverage metrics:
       - Overall line coverage percentage
       - Number of files covered
       - Total lines vs covered lines
  </action>
  <verify>
    test -f backend/tests/coverage_reports/metrics/coverage.json  # File exists
    test -f backend/tests/coverage_reports/html/index.html  # HTML report exists
    python -c "import json; d=json.load(open('backend/tests/coverage_reports/metrics/coverage.json')); print(f\"Coverage: {d['totals']['percent_covered']:.2f}%\")"  # Shows coverage %
  </verify>
  <done>
    Coverage report successfully generated
    coverage.json contains valid coverage metrics
    HTML report is viewable in browser
  </done>
</task>

<task type="auto">
  <name>Task 3: Generate comprehensive markdown coverage report</name>
  <files>
    backend/tests/coverage_reports/COVERAGE_REPORT_v3.2.md
  </files>
  <action>
    Generate backend/tests/coverage_reports/COVERAGE_REPORT_v3.2.md with:

    1. Header section:
       - Title: "Coverage Report v3.2 - Baseline for Bug Finding & Coverage Expansion"
       - Generated date: 2026-02-24
       - Phase: 81-01

    2. Executive Summary:
       - Overall coverage percentage (from coverage.json totals.percent_covered)
       - Lines covered vs total lines
       - Number of files analyzed
       - Comparison to baseline (from BASELINE_COVERAGE.md 5.13%)

    3. Module Breakdown table:
       | Module | Files | Lines | Covered | Coverage |
       |--------|-------|-------|---------|----------|
       | core | ? | ? | ? | ?% |
       | api | ? | ? | ? | ?% |
       | tools | ? | ? | ? | ?% |

    4. Coverage Distribution:
       - 0% coverage: X files (Y%)
       - 1-20% coverage: X files (Y%)
       - 21-50% coverage: X files (Y%)
       - 51-70% coverage: X files (Y%)
       - 71-90% coverage: X files (Y%)
       - 90%+ coverage: X files (Y%)

    5. File-by-file details for top 50 largest files (by total lines):
       - Sort by uncovered lines descending (highest gap first)
       - Show: filename, total lines, covered, coverage %
       - Highlight files with >200 lines and <30% coverage

    6. Use the generate_markdown_report() function from coverage_report_generator.py
  </action>
  <verify>
    test -f backend/tests/coverage_reports/COVERAGE_REPORT_v3.2.md  # Report exists
    grep -q "Executive Summary" backend/tests/coverage_reports/COVERAGE_REPORT_v3.2.md  # Has summary section
    grep -q "Module Breakdown" backend/tests/coverage_reports/COVERAGE_REPORT_v3.2.md  # Has module breakdown
    grep -q "Coverage Distribution" backend/tests/coverage_reports/COVERAGE_REPORT_v3.2.md  # Has distribution
  </verify>
  <done>
    COVERAGE_REPORT_v3.2.md exists with all required sections
    Report includes executive summary, module breakdown, and file-by-file details
    Report is ready for use in Plan 02 (high-impact file identification)
  </done>
</task>

</tasks>

<verification>
Overall phase checks:
1. Coverage report generator script exists and is executable
2. Coverage JSON and HTML reports are generated successfully
3. Markdown report provides comprehensive coverage baseline
4. All metrics are accurate and consistent across JSON, HTML, and markdown
</verification>

<success_criteria>
1. Coverage report generator (coverage_report_generator.py) runs successfully
2. coverage.json contains valid metrics with totals.percent_covered
3. HTML report is viewable at backend/tests/coverage_reports/html/index.html
4. COVERAGE_REPORT_v3.2.md documents baseline coverage with module breakdown
5. Report serves as foundation for Plan 02 high-impact file identification
</success_criteria>

<output>
After completion, create .planning/phases/81-coverage-analysis-prioritization/081-01-SUMMARY.md with:
- Coverage metrics (overall %, lines covered/total)
- Module breakdown (core/api/tools coverage)
- Notable findings (files with 0% coverage, unexpected gaps)
- Comparison to baseline (5.13% from BASELINE_COVERAGE.md)
</output>
