---
phase: 253b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/core/governance/test_agent_context_resolver_coverage.py
  - backend/tests/core/llm/test_cognitive_tier_system_coverage.py
  - backend/tests/core/llm/test_cache_aware_router_coverage_extend.py
  - backend/tests/core/episodes/test_episode_lifecycle_service_coverage_extend.py
  - backend/tests/core/episodes/test_agent_graduation_service_coverage.py
autonomous: true
requirements: [COV-B-04]
user_setup: []

must_haves:
  truths:
    - "Agent context resolver fallback chain covers all 4 levels (explicit, session, system default, failure)"
    - "Cognitive tier classification uses token count and semantic complexity for tier assignment"
    - "Cache-aware router applies cached cost ratio when provider supports caching and prompt meets minimum tokens"
    - "Episode lifecycle applies decay formula: decay_score = max(0, 1 - (days_old / 180))"
    - "Agent graduation readiness combines episode count, intervention rate, and constitutional score"
    - "Coverage increases by +1-3 percentage points from 4.60% baseline"
  artifacts:
    - path: "backend/tests/core/governance/test_agent_context_resolver_coverage.py"
      provides: "AgentContextResolver coverage tests"
      min_lines: 250
      contains: "class TestAgentContextResolverCoverage"
    - path: "backend/tests/core/llm/test_cognitive_tier_system_coverage.py"
      provides: "CognitiveTier system coverage tests"
      min_lines: 300
      contains: "class TestCognitiveTierSystemCoverage"
    - path: "backend/tests/core/episodes/test_agent_graduation_service_coverage.py"
      provides: "AgentGraduationService coverage tests"
      min_lines: 400
      contains: "class TestAgentGraduationServiceCoverage"
  key_links:
    - from: "test_agent_context_resolver_coverage.py"
      to: "core/agent_context_resolver.py"
      via: "tests resolve_agent_for_request fallback chain"
      pattern: "def test_resolve_agent.*_explicit|session|default"
    - from: "test_cognitive_tier_system_coverage.py"
      to: "core/llm/cognitive_tier_system.py"
      via: "tests classify() with token and complexity patterns"
      pattern: "def test_classify.*tier"
    - from: "test_agent_graduation_service_coverage.py"
      to: "core/agent_graduation_service.py"
      via: "tests graduation criteria validation"
      pattern: "def test_graduation.*criteria"
---

<objective>
Create unit/integration tests for high-impact core services to increase backend coverage by +1-3 percentage points from 4.60% baseline.

Purpose: Progressive coverage expansion toward 80% target (COV-B-04). Wave 1 targets high-impact governance, LLM services, and episode services that are core to agent operations but have significant coverage gaps.

Target files:
1. Core governance: agent_context_resolver.py (minimal existing coverage)
2. LLM services: cognitive_tier_system.py (new), cache_aware_router.py (extend existing)
3. Episode services: episode_lifecycle_service.py (extend), agent_graduation_service.py (minimal)

Output: 5 new test files with ~53 tests total, increasing coverage by +1-3 percentage points.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/251-backend-coverage-baseline/251-01-SUMMARY.md
@backend/TESTING.md

# Baseline from Phase 251
Current backend coverage: 4.60% (5,070/89,320 lines)
Target: 80% coverage (COV-B-04)
Gap: ~71,450 lines
Approach: Progressive waves of +1-3 percentage points each

# Phase 253a Context (Property Tests - DO NOT DUPLICATE)
Phase 253a completed 38 property tests for data integrity:
- 20 episode data integrity tests (score bounds, timestamps, referential integrity)
- 19 skill execution data integrity tests (billing idempotence, status transitions)
These are property tests using Hypothesis. This phase (253b) focuses on unit/integration tests for coverage.

# Target File Analysis

## 1. agent_context_resolver.py (238 lines)
Current coverage: Minimal (only indirect tests)
Key methods to test:
- resolve_agent_for_request() - 4-level fallback chain
- _get_agent() - fetch by ID
- _get_session_agent() - session metadata lookup
- _get_or_create_system_default() - create Chat Assistant
- set_session_agent() - associate agent with session
- validate_agent_for_action() - wrapper for governance

## 2. cognitive_tier_system.py (147 lines)
Current coverage: None (new file)
Key classes/methods:
- CognitiveTier enum (MICRO, STANDARD, VERSATILE, HEAVY, COMPLEX)
- TIER_THRESHOLDS configuration
- CognitiveClassifier.classify() - token + complexity scoring
- _calculate_complexity_score() - pattern matching

## 3. cache_aware_router.py (308 lines)
Current coverage: ~40% (test_cache_aware_router_coverage.py exists)
Gap areas to extend:
- predict_cache_hit_probability() - historical lookup
- record_cache_outcome() - history tracking
- get_cache_hit_history() - analytics retrieval
- clear_cache_history() - workspace clearing

## 4. episode_lifecycle_service.py (436 lines)
Current coverage: ~30% (test_episode_lifecycle_coverage.py exists)
Gap areas to extend:
- decay_old_episodes() - decay score calculation
- consolidate_similar_episodes() - semantic clustering
- archive_to_cold_storage() - LanceDB archival
- update_importance_scores() - feedback-weighted scoring
- batch_update_access_counts() - bulk operations

## 5. agent_graduation_service.py (829 lines)
Current coverage: Minimal (only indirect tests)
Key methods to test:
- calculate_readiness_score() - graduation criteria validation
- _calculate_score() - weighted readiness formula
- _generate_recommendation() - human-readable output
- promote_agent() - maturity level update
- get_graduation_audit_trail() - comprehensive data retrieval
- calculate_supervision_metrics() - supervision-based metrics

# Existing Test Patterns

From backend/tests/core/governance/test_agent_governance_service_coverage.py:
```python
class TestAgentGovernanceServiceInit:
    def test_service_initialization(self, db_session):
        service = AgentGovernanceService(db_session)
        assert service.db is db_session

class TestRegisterOrUpdateAgent:
    def test_register_or_update_agent_new_agent(self, db_session):
        service = AgentGovernanceService(db_session)
        agent = service.register_or_update_agent(...)
        assert agent.name == "Test Agent"
```

From backend/tests/core/llm/test_cache_aware_router_coverage.py:
```python
class TestCalculateEffectiveCost:
    def test_cost_openai_with_cache_hit(self, router_with_pricing):
        cost = router_with_pricing.calculate_effective_cost(
            model="gpt-4o", provider="openai",
            estimated_input_tokens=2000, cache_hit_probability=0.9
        )
        assert cost < full_price
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AgentContextResolver coverage tests</name>
  <files>backend/tests/core/governance/test_agent_context_resolver_coverage.py</files>
  <read_first>backend/core/agent_context_resolver.py, backend/tests/core/governance/test_agent_governance_service_coverage.py</read_first>
  <behavior>
    Create backend/tests/core/governance/test_agent_context_resolver_coverage.py with tests for:

    1. Service Initialization (2 tests):
       - __init__ with db session
       - governance service initialization

    2. Agent Resolution - Fallback Chain (8 tests):
       - Level 1: Explicit agent_id found
       - Level 1: Explicit agent_id not found (fallback to session)
       - Level 2: Session agent found
       - Level 2: Session not found
       - Level 2: Session exists but no agent_id in metadata
       - Level 3: System default exists
       - Level 3: System default created (Chat Assistant)
       - Level 4: All fallbacks failed

    3. Session Agent Operations (3 tests):
       - set_session_agent with valid session and agent
       - set_session_agent with non-existent session
       - set_session_agent with non-existent agent

    4. Agent Validation (2 tests):
       - validate_agent_for_action with allowed action
       - validate_agent_for_action with blocked action
  </behavior>
  <action>
    Create backend/tests/core/governance/test_agent_context_resolver_coverage.py with:

    ```python
    """
    Coverage-driven tests for AgentContextResolver (target 70%+)

    Target file: core/agent_context_resolver.py (238 lines)
    """

    import pytest
    from unittest.mock import Mock, patch, MagicMock
    from datetime import datetime, timezone
    from uuid import uuid4

    from core.agent_context_resolver import AgentContextResolver
    from core.models import (
        AgentRegistry,
        AgentStatus,
        ChatSession,
        ChatMessage,
        User,
    )


    class TestAgentContextResolverInit:
        """Test AgentContextResolver initialization."""

        def test_service_initialization(self, db_session):
            """Cover lines 29-31: Service initialization with dependencies."""
            resolver = AgentContextResolver(db_session)
            assert resolver.db is db_session
            assert resolver.governance is not None


    class TestResolveAgentExplicitAgentId:
        """Test Level 1 fallback: Explicit agent_id in request."""

        def test_resolve_agent_with_valid_explicit_id(self, db_session):
            """Cover lines 66-71: Explicit agent_id found."""
            resolver = AgentContextResolver(db_session)

            # Create agent
            agent = AgentRegistry(
                id=str(uuid4()),
                name="TestAgent",
                category="test",
                module_path="test.module",
                class_name="TestAgent",
                status=AgentStatus.STUDENT.value,
                confidence_score=0.5
            )
            db_session.add(agent)
            db_session.commit()

            # Resolve with explicit agent_id
            resolved_agent, context = resolver.resolve_agent_for_request(
                user_id="user1",
                requested_agent_id=agent.id
            )

            assert resolved_agent is not None
            assert resolved_agent.id == agent.id
            assert "explicit_agent_id" in context["resolution_path"]

        def test_resolve_agent_with_invalid_explicit_id(self, db_session):
            """Cover lines 72-74: Explicit agent_id not found."""
            resolver = AgentContextResolver(db_session)

            resolved_agent, context = resolver.resolve_agent_for_request(
                user_id="user1",
                requested_agent_id="nonexistent-id"
            )

            assert resolved_agent is None
            assert "explicit_agent_id_not_found" in context["resolution_path"]


    class TestResolveAgentSessionFallback:
        """Test Level 2 fallback: Session context agent."""

        def test_resolve_agent_with_session_agent(self, db_session):
            """Cover lines 77-82: Session agent found."""
            resolver = AgentContextResolver(db_session)

            # Create agent
            agent = AgentRegistry(
                id=str(uuid4()),
                name="SessionAgent",
                category="test",
                module_path="test.module",
                class_name="SessionAgent",
                status=AgentStatus.INTERN.value,
                confidence_score=0.6
            )
            db_session.add(agent)
            db_session.flush()

            # Create session with agent_id in metadata
            session = ChatSession(
                id=str(uuid4()),
                user_id="user1",
                tenant_id="default",
                metadata_json={"agent_id": agent.id}
            )
            db_session.add(session)
            db_session.commit()

            # Resolve with session_id
            resolved_agent, context = resolver.resolve_agent_for_request(
                user_id="user1",
                session_id=session.id
            )

            assert resolved_agent is not None
            assert resolved_agent.id == agent.id
            assert "session_agent" in context["resolution_path"]

        def test_resolve_agent_session_not_found(self, db_session):
            """Cover lines 119-121: Session not found."""
            resolver = AgentContextResolver(db_session)

            resolved_agent, context = resolver.resolve_agent_for_request(
                user_id="user1",
                session_id="nonexistent-session"
            )

            assert resolved_agent is None
            assert "no_session_agent" in context["resolution_path"]

        def test_resolve_agent_session_no_metadata(self, db_session):
            """Cover lines 123-132: Session exists but no agent_id."""
            resolver = AgentContextResolver(db_session)

            session = ChatSession(
                id=str(uuid4()),
                user_id="user1",
                tenant_id="default",
                metadata_json=None
            )
            db_session.add(session)
            db_session.commit()

            resolved_agent, context = resolver.resolve_agent_for_request(
                user_id="user1",
                session_id=session.id
            )

            assert resolved_agent is None  # Falls through to system default


    class TestResolveAgentSystemDefault:
        """Test Level 3 fallback: System default Chat Assistant."""

        def test_resolve_agent_system_default_exists(self, db_session):
            """Cover lines 147-153: System default Chat Assistant exists."""
            resolver = AgentContextResolver(db_session)

            # Create Chat Assistant
            chat_assistant = AgentRegistry(
                id=str(uuid4()),
                name="Chat Assistant",
                category="system",
                module_path="system",
                class_name="ChatAssistant",
                status=AgentStatus.STUDENT.value,
                confidence_score=0.5
            )
            db_session.add(chat_assistant)
            db_session.commit()

            resolved_agent, context = resolver.resolve_agent_for_request(
                user_id="user1"
            )

            assert resolved_agent is not None
            assert resolved_agent.name == "Chat Assistant"
            assert "system_default" in context["resolution_path"]

        def test_resolve_agent_creates_system_default(self, db_session):
            """Cover lines 156-175: Create Chat Assistant if not exists."""
            resolver = AgentContextResolver(db_session)

            # Ensure no Chat Assistant exists
            db_session.query(AgentRegistry).filter(
                AgentRegistry.name == "Chat Assistant"
            ).delete()

            resolved_agent, context = resolver.resolve_agent_for_request(
                user_id="user1"
            )

            assert resolved_agent is not None
            assert resolved_agent.name == "Chat Assistant"
            assert resolved_agent.category == "system"
            assert "system_default" in context["resolution_path"]


    class TestSetSessionAgent:
        """Test set_session_agent method."""

        def test_set_session_agent_success(self, db_session):
            """Cover lines 180-218: Set agent on valid session."""
            resolver = AgentContextResolver(db_session)

            agent = AgentRegistry(
                id=str(uuid4()),
                name="TestAgent",
                category="test",
                module_path="test.module",
                class_name="TestAgent",
                status=AgentStatus.STUDENT.value
            )
            db_session.add(agent)
            db_session.flush()

            session = ChatSession(
                id=str(uuid4()),
                user_id="user1",
                tenant_id="default",
                metadata_json={}
            )
            db_session.add(session)
            db_session.commit()

            result = resolver.set_session_agent(session.id, agent.id)

            assert result is True
            db_session.refresh(session)
            assert session.metadata_json.get("agent_id") == agent.id

        def test_set_session_agent_session_not_found(self, db_session):
            """Cover lines 195-197: Non-existent session."""
            resolver = AgentContextResolver(db_session)

            result = resolver.set_session_agent("nonexistent", "agent-id")

            assert result is False

        def test_set_session_agent_agent_not_found(self, db_session):
            """Cover lines 199-206: Non-existent agent."""
            resolver = AgentContextResolver(db_session)

            session = ChatSession(
                id=str(uuid4()),
                user_id="user1",
                tenant_id="default"
            )
            db_session.add(session)
            db_session.commit()

            result = resolver.set_session_agent(session.id, "nonexistent-agent")

            assert result is False


    class TestValidateAgentForAction:
        """Test validate_agent_for_action method."""

        def test_validate_agent_allowed_action(self, db_session):
            """Cover lines 222-237: Allowed action for agent."""
            resolver = AgentContextResolver(db_session)

            agent = AgentRegistry(
                id=str(uuid4()),
                name="TestAgent",
                category="test",
                module_path="test.module",
                class_name="TestAgent",
                status=AgentStatus.AUTONOMOUS.value,
                confidence_score=0.95
            )
            db_session.add(agent)
            db_session.commit()

            result = resolver.validate_agent_for_action(agent, "read")

            assert result["allowed"] is True

        def test_validate_agent_blocked_action(self, db_session):
            """Cover lines 222-237: Blocked action for low-maturity agent."""
            resolver = AgentContextResolver(db_session)

            agent = AgentRegistry(
                id=str(uuid4()),
                name="TestAgent",
                category="test",
                module_path="test.module",
                class_name="TestAgent",
                status=AgentStatus.STUDENT.value,
                confidence_score=0.3
            )
            db_session.add(agent)
            db_session.commit()

            result = resolver.validate_agent_for_action(agent, "delete")

            assert result["allowed"] is False
    ```

    Total: 15 tests across 6 test classes
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/core/governance/test_agent_context_resolver_coverage.py -v --tb=short</automated>
  </verify>
  <done>
    - File backend/tests/core/governance/test_agent_context_resolver_coverage.py exists (min 250 lines)
    - Contains 6 test classes with 15 tests total
    - Tests cover: init, explicit agent fallback, session fallback, system default, set session agent, validation
    - Command `pytest tests/core/governance/test_agent_context_resolver_coverage.py -v` passes with 15 passed
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create CognitiveTier system coverage tests</name>
  <files>backend/tests/core/llm/test_cognitive_tier_system_coverage.py</files>
  <read_first>backend/core/llm/cognitive_tier_system.py, backend/tests/core/llm/test_cache_aware_router_coverage.py</read_first>
  <behavior>
    Create backend/tests/core/llm/test_cognitive_tier_system_coverage.py with tests for:

    1. CognitiveTier Enum (5 tests):
       - MICRO tier values
       - STANDARD tier values
       - VERSATILE tier values
       - HEAVY tier values
       - COMPLEX tier values

    2. TIER_THRESHOLDS Configuration (5 tests):
       - MICRO threshold validation
       - STANDARD threshold validation
       - VERSATILE threshold validation
       - HEAVY threshold validation
       - COMPLEX threshold validation

    3. CognitiveClassifier Initialization (2 tests):
       - __init__ creates compiled patterns
       - Compiled patterns are regex objects

    4. Classification by Token Count (5 tests):
       - <100 tokens -> MICRO
       - 100-500 tokens -> STANDARD
       - 500-2k tokens -> VERSATILE
       - 2k-5k tokens -> HEAVY
       - >5k tokens -> COMPLEX

    5. Classification by Semantic Complexity (6 tests):
       - Simple patterns -> lower tier
       - Moderate patterns -> mid tier
       - Technical/code patterns -> higher tier
       - Advanced patterns -> highest tier
       - Code block detection (```)
       - Combined token + complexity scoring

    6. Task Type Adjustments (4 tests):
       - "code" task increases tier
       - "analysis" task increases tier
       - "chat" task decreases tier
       - "general" task decreases tier

    7. Edge Cases (3 tests):
       - Empty prompt
       - Very long prompt (>10k tokens)
       - Unknown patterns default to STANDARD
  </behavior>
  <action>
    Create backend/tests/core/llm/test_cognitive_tier_system_coverage.py with:

    ```python
    """
    Coverage-driven tests for CognitiveTier System (target 80%+)

    Target file: core/llm/cognitive_tier_system.py (147 lines)
    """

    import pytest
    from core.llm.cognitive_tier_system import (
        CognitiveTier,
        CognitiveClassifier,
        TIER_THRESHOLDS,
    )


    class TestCognitiveTierEnum:
        """Test CognitiveTier enum values."""

        def test_micro_tier_value(self):
            """Cover line 18: MICRO tier enum value."""
            assert CognitiveTier.MICRO.value == "micro"

        def test_standard_tier_value(self):
            """Cover line 19: STANDARD tier enum value."""
            assert CognitiveTier.STANDARD.value == "standard"

        def test_versatile_tier_value(self):
            """Cover line 20: VERSATILE tier enum value."""
            assert CognitiveTier.VERSATILE.value == "versatile"

        def test_heavy_tier_value(self):
            """Cover line 21: HEAVY tier enum value."""
            assert CognitiveTier.HEAVY.value == "heavy"

        def test_complex_tier_value(self):
            """Cover line 22: COMPLEX tier enum value."""
            assert CognitiveTier.COMPLEX.value == "complex"


    class TestTierThresholds:
        """Test TIER_THRESHOLDS configuration."""

        def test_micro_threshold(self):
            """Cover lines 27-31: MICRO threshold configuration."""
            micro = TIER_THRESHOLDS[CognitiveTier.MICRO]
            assert micro["max_tokens"] == 100
            assert micro["complexity_score"] == 0
            assert "Simple queries" in micro["description"]

        def test_standard_threshold(self):
            """Cover lines 32-36: STANDARD threshold configuration."""
            standard = TIER_THRESHOLDS[CognitiveTier.STANDARD]
            assert standard["max_tokens"] == 500
            assert standard["complexity_score"] == 2

        def test_versatile_threshold(self):
            """Cover lines 37-41: VERSATILE threshold configuration."""
            versatile = TIER_THRESHOLDS[CognitiveTier.VERSATILE]
            assert versatile["max_tokens"] == 2000
            assert versatile["complexity_score"] == 5

        def test_heavy_threshold(self):
            """Cover lines 42-46: HEAVY threshold configuration."""
            heavy = TIER_THRESHOLDS[CognitiveTier.HEAVY]
            assert heavy["max_tokens"] == 5000
            assert heavy["complexity_score"] == 8

        def test_complex_threshold(self):
            """Cover lines 47-52: COMPLEX threshold configuration."""
            complex_tier = TIER_THRESHOLDS[CognitiveTier.COMPLEX]
            assert complex_tier["max_tokens"] == float("inf")
            assert complex_tier["complexity_score"] == float("inf")


    class TestCognitiveClassifierInit:
        """Test CognitiveClassifier initialization."""

        def test_init_creates_compiled_patterns(self):
            """Cover lines 107-112: Initialize with compiled regex patterns."""
            classifier = CognitiveClassifier()

            assert hasattr(classifier, "_compiled_patterns")
            assert len(classifier._compiled_patterns) == 5

            # Check patterns are compiled regex
            import re
            for name, (pattern, weight) in classifier._compiled_patterns.items():
                assert isinstance(pattern, re.Pattern)


    class TestClassifyByTokenCount:
        """Test classification based on token count."""

        def test_classify_micro_short_prompt(self):
            """Cover lines 114-123: <100 tokens -> MICRO."""
            classifier = CognitiveClassifier()
            tier = classifier.classify("hello")
            assert tier == CognitiveTier.MICRO

        def test_classify_standard_medium_prompt(self):
            """Cover lines 114-123: 100-500 tokens -> STANDARD."""
            classifier = CognitiveClassifier()
            # ~250 tokens (1000 chars / 4)
            prompt = "hello " * 200
            tier = classifier.classify(prompt)
            assert tier == CognitiveTier.STANDARD

        def test_classify_versatile_long_prompt(self):
            """Cover lines 114-123: 500-2k tokens -> VERSATILE."""
            classifier = CognitiveClassifier()
            # ~1250 tokens (5000 chars / 4)
            prompt = "analyze this data " * 1250
            tier = classifier.classify(prompt)
            assert tier == CognitiveTier.VERSATILE

        def test_classify_heavy_very_long_prompt(self):
            """Cover lines 114-123: 2k-5k tokens -> HEAVY."""
            classifier = CognitiveClassifier()
            # ~3000 tokens (12000 chars / 4)
            prompt = "detailed analysis " * 3000
            tier = classifier.classify(prompt)
            assert tier == CognitiveTier.HEAVY

        def test_classify_complex_huge_prompt(self):
            """Cover lines 114-123: >5k tokens -> COMPLEX."""
            classifier = CognitiveClassifier()
            # ~6000 tokens (24000 chars / 4)
            prompt = "complex task " * 6000
            tier = classifier.classify(prompt)
            assert tier == CognitiveTier.COMPLEX


    class TestClassifyBySemanticComplexity:
        """Test classification based on semantic patterns."""

        def test_classify_simple_keywords(self):
            """Cover lines 61-65, 139-140: Simple patterns lower tier."""
            classifier = CognitiveClassifier()
            tier = classifier.classify("hello, please summarize this briefly")
            assert tier == CognitiveTier.MICRO

        def test_classify_moderate_keywords(self):
            """Cover lines 66-70, 139-140: Moderate patterns mid tier."""
            classifier = CognitiveClassifier()
            tier = classifier.classify("analyze the pros and cons of this approach")
            assert tier in [CognitiveTier.STANDARD, CognitiveTier.VERSATILE]

        def test_classify_technical_keywords(self):
            """Cover lines 72-78, 139-140: Technical patterns higher tier."""
            classifier = CognitiveClassifier()
            tier = classifier.classify("calculate the integral of this function")
            assert tier in [CognitiveTier.VERSATILE, CognitiveTier.HEAVY, CognitiveTier.COMPLEX]

        def test_classify_code_keywords(self):
            """Cover lines 79-87, 139-140: Code patterns higher tier."""
            classifier = CognitiveClassifier()
            tier = classifier.classify("help me debug this async function")
            assert tier in [CognitiveTier.VERSATILE, CognitiveTier.HEAVY, CognitiveTier.COMPLEX]

        def test_classify_advanced_keywords(self):
            """Cover lines 88-95, 139-140: Advanced patterns highest tier."""
            classifier = CognitiveClassifier()
            tier = classifier.classify("design enterprise-scale distributed architecture")
            assert tier in [CognitiveTier.HEAVY, CognitiveTier.COMPLEX]

        def test_classify_code_block_detection(self):
            """Cover lines 141-142: Code block ``` increases complexity."""
            classifier = CognitiveClassifier()
            prompt = "```python\ndef complex_function():\n    pass\n```"
            tier = classifier.classify(prompt)
            # Code block should boost to at least VERSATILE
            assert tier in [CognitiveTier.VERSATILE, CognitiveTier.HEAVY, CognitiveTier.COMPLEX]


    class TestTaskTypeAdjustments:
        """Test task type adjustments to classification."""

        def test_task_type_code_increases_tier(self):
            """Cover lines 143-145: 'code' task type increases tier."""
            classifier = CognitiveClassifier()
            tier = classifier.classify("simple task", task_type="code")
            # Code task should bump from MICRO to STANDARD or higher
            assert tier != CognitiveTier.MICRO

        def test_task_type_analysis_increases_tier(self):
            """Cover lines 143-145: 'analysis' task type increases tier."""
            classifier = CognitiveClassifier()
            tier = classifier.classify("simple task", task_type="analysis")
            assert tier != CognitiveTier.MICRO

        def test_task_type_chat_decreases_tier(self):
            """Cover lines 103-104: 'chat' task type decreases tier."""
            classifier = CognitiveClassifier()
            # Without task type would be higher
            tier_with_chat = classifier.classify("help me analyze this data", task_type="chat")
            tier_without = classifier.classify("help me analyze this data")
            assert tier_with_chat.value == tier_without.value or tier_with_chat == CognitiveTier.MICRO

        def test_task_type_general_decreases_tier(self):
            """Cover lines 104-105: 'general' task type decreases tier."""
            classifier = CognitiveClassifier()
            tier = classifier.classify("analyze this", task_type="general")
            # General should potentially lower tier
            assert tier in [CognitiveTier.MICRO, CognitiveTier.STANDARD]


    class TestEdgeCases:
        """Test edge cases in classification."""

        def test_classify_empty_prompt(self):
            """Cover lines 114-123: Empty prompt defaults to MICRO."""
            classifier = CognitiveClassifier()
            tier = classifier.classify("")
            assert tier == CognitiveTier.MICRO

        def test_classify_very_long_prompt(self):
            """Cover lines 127-136: Very long prompt (>5k tokens)."""
            classifier = CognitiveClassifier()
            # Create prompt that would be >5000 tokens
            prompt = "word " * 25000  # ~25k chars = ~6.25k tokens
            tier = classifier.classify(prompt)
            assert tier == CognitiveTier.COMPLEX

        def test_classify_unknown_patterns(self):
            """Cover lines 114-123: Unknown patterns default to STANDARD."""
            classifier = CognitiveClassifier()
            # Random words that don't match patterns
            tier = classifier.classify("xyz abc 123 foo bar")
            # Should default based on token count alone (STANDARD for short)
            assert tier in [CognitiveTier.MICRO, CognitiveTier.STANDARD]
    ```

    Total: 30 tests across 7 test classes
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/core/llm/test_cognitive_tier_system_coverage.py -v --tb=short</automated>
  </verify>
  <done>
    - File backend/tests/core/llm/test_cognitive_tier_system_coverage.py exists (min 300 lines)
    - Contains 7 test classes with 30 tests total
    - Tests cover: enum values, thresholds, initialization, token classification, semantic complexity, task types, edge cases
    - Command `pytest tests/core/llm/test_cognitive_tier_system_coverage.py -v` passes with 30 passed
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend CacheAwareRouter coverage tests</name>
  <files>backend/tests/core/llm/test_cache_aware_router_coverage_extend.py</files>
  <read_first>backend/core/llm/cache_aware_router.py, backend/tests/core/llm/test_cache_aware_router_coverage.py</read_first>
  <behavior>
    Extend cache_aware_router.py coverage from ~40% to ~70% by adding tests for:

    1. Cache Hit Prediction (4 tests):
       - predict_cache_hit_probability with no history (returns 0.5)
       - predict_cache_hit_probability with history (returns actual rate)
       - predict with 100% hit rate
       - predict with 0% hit rate

    2. Cache Outcome Recording (4 tests):
       - record_cache_outcome with hit
       - record_cache_outcome with miss
       - multiple recordings update history correctly
       - workspace isolation in history tracking

    3. Cache History Analytics (3 tests):
       - get_cache_hit_history returns all history
       - get_cache_hit_history filters by workspace
       - history structure is correct [hits, total]

    4. Cache History Management (3 tests):
       - clear_cache_history clears all
       - clear_cache_history clears specific workspace
       - clear_history with empty workspace_id clears all

    5. Provider Capability Edge Cases (3 tests):
       - get_provider_cache_capability with unknown provider (defaults)
       - get_provider_cache_capability with "google" alias to gemini
       - Provider with fuzzy match returns gemini config
  </behavior>
  <action>
    Create backend/tests/core/llm/test_cache_aware_router_coverage_extend.py with:

    ```python
    """
    Extended coverage tests for CacheAwareRouter (extend from 40% to 70%+)

    Target file: core/llm/cache_aware_router.py (308 lines)
    Gap areas: lines 157-308 (cache history, analytics, edge cases)
    """

    import pytest
    from unittest.mock import MagicMock

    from core.llm.cache_aware_router import CacheAwareRouter


    class TestPredictCacheHitProbability:
        """Test predict_cache_hit_probability method (lines 157-193)."""

        @pytest.fixture
        def router(self):
            """Create router with mock pricing fetcher."""
            mock_pricing = MagicMock()
            return CacheAwareRouter(mock_pricing)

        def test_predict_with_no_history_returns_default(self, router):
            """Cover lines 183-193: No history returns 0.5 default."""
            probability = router.predict_cache_hit_probability("abc123", "default")

            assert probability == 0.5

        def test_predict_with_history_returns_actual_rate(self, router):
            """Cover lines 186-189: Returns hit rate from history."""
            # Set up history: 8 hits out of 10
            router.cache_hit_history["default:abc123"] = [8, 10]

            probability = router.predict_cache_hit_probability("abc123", "default")

            assert probability == 0.8

        def test_predict_with_100_percent_hit_rate(self, router):
            """Cover lines 186-189: Perfect cache hit rate."""
            router.cache_hit_history["default:perfect"] = [10, 10]

            probability = router.predict_cache_hit_probability("perfect", "default")

            assert probability == 1.0

        def test_predict_with_0_percent_hit_rate(self, router):
            """Cover lines 186-189: Zero cache hit rate."""
            router.cache_hit_history["default:miss"] = [0, 10]

            probability = router.predict_cache_hit_probability("miss", "default")

            assert probability == 0.0

        def test_prompt_hash_truncated_to_16_chars(self, router):
            """Cover line 184: First 16 characters of hash used as key."""
            # Record with one hash
            long_hash = "abc123def456xyz789more"
            router.cache_hit_history["default:abc123def456xyz7"] = [5, 10]

            # Query with different hash but same 16-char prefix
            probability = router.predict_cache_hit_probability("abc123def456xyz789different", "default")

            assert probability == 0.5


    class TestRecordCacheOutcome:
        """Test record_cache_outcome method (lines 195-228)."""

        @pytest.fixture
        def router(self):
            """Create router with mock pricing fetcher."""
            mock_pricing = MagicMock()
            return CacheAwareRouter(mock_pricing)

        def test_record_cache_hit(self, router):
            """Cover lines 221-223: Recording a cache hit."""
            router.record_cache_outcome("hash123", "workspace1", was_cached=True)

            assert router.cache_hit_history["workspace1:hash123"] == [1, 1]

        def test_record_cache_miss(self, router):
            """Cover lines 221-223: Recording a cache miss."""
            router.record_cache_outcome("hash456", "workspace1", was_cached=False)

            assert router.cache_hit_history["workspace1:hash456"] == [0, 1]

        def test_multiple_recordings_update_correctly(self, router):
            """Cover lines 218-223: Multiple recordings accumulate."""
            # Record 8 hits and 2 misses
            for _ in range(8):
                router.record_cache_outcome("hash789", "workspace2", was_cached=True)
            for _ in range(2):
                router.record_cache_outcome("hash789", "workspace2", was_cached=False)

            assert router.cache_hit_history["workspace2:hash789"] == [8, 10]

        def test_workspace_isolation_in_history(self, router):
            """Cover lines 184, 216: Different workspaces have separate histories."""
            router.record_cache_outcome("hash1", "workspace_a", was_cached=True)
            router.record_cache_outcome("hash1", "workspace_b", was_cached=False)

            assert router.cache_hit_history["workspace_a:hash1"] == [1, 1]
            assert router.cache_hit_history["workspace_b:hash1"] == [0, 1]


    class TestGetCacheHitHistory:
        """Test get_cache_hit_history method (lines 274-290)."""

        @pytest.fixture
        def router(self):
            """Create router with mock pricing fetcher."""
            mock_pricing = MagicMock()
            router = CacheAwareRouter(mock_pricing)
            # Set up history
            router.cache_hit_history["ws1:hash1"] = [8, 10]
            router.cache_hit_history["ws2:hash2"] = [5, 10]
            router.cache_hit_history["ws1:hash3"] = [2, 5]
            return router

        def test_get_history_returns_all(self, router):
            """Cover lines 289-290: Get all history when no filter."""
            history = router.get_cache_hit_history()

            assert len(history) == 3
            assert "ws1:hash1" in history
            assert "ws2:hash2" in history
            assert "ws1:hash3" in history

        def test_get_history_filters_by_workspace(self, router):
            """Cover lines 284-289: Filter by workspace_id."""
            history = router.get_cache_hit_history(workspace_id="ws1")

            assert len(history) == 2
            assert "ws1:hash1" in history
            assert "ws1:hash3" in history
            assert "ws2:hash2" not in history

        def test_history_structure_is_correct(self, router):
            """Cover lines 219-220: History structure is [hits, total]."""
            history = router.get_cache_hit_history()

            for key, value in history.items():
                assert isinstance(value, list)
                assert len(value) == 2
                assert isinstance(value[0], int)  # hits
                assert isinstance(value[1], int)  # total


    class TestClearCacheHistory:
        """Test clear_cache_history method (lines 292-307)."""

        @pytest.fixture
        def router(self):
            """Create router with mock pricing fetcher."""
            mock_pricing = MagicMock()
            router = CacheAwareRouter(mock_pricing)
            # Set up history
            router.cache_hit_history["ws1:hash1"] = [8, 10]
            router.cache_hit_history["ws2:hash2"] = [5, 10]
            router.cache_hit_history["ws1:hash3"] = [2, 5]
            return router

        def test_clear_all_history(self, router):
            """Cover lines 304-305: Clear all history when workspace_id is None."""
            router.clear_cache_history()

            assert len(router.cache_hit_history) == 0

        def test_clear_workspace_history(self, router):
            """Cover lines 299-303: Clear specific workspace history."""
            router.clear_cache_history(workspace_id="ws1")

            assert "ws1:hash1" not in router.cache_hit_history
            assert "ws1:hash3" not in router.cache_hit_history
            assert "ws2:hash2" in router.cache_hit_history  # Other workspace intact

        def test_clear_empty_workspace_does_nothing(self, router):
            """Cover lines 299-303: Clearing non-existent workspace is safe."""
            initial_count = len(router.cache_hit_history)

            router.clear_cache_history(workspace_id="nonexistent")

            assert len(router.cache_hit_history) == initial_count


    class TestProviderCapabilityEdgeCases:
        """Test get_provider_cache_capability edge cases (lines 230-272)."""

        @pytest.fixture
        def router(self):
            """Create router with mock pricing fetcher."""
            mock_pricing = MagicMock()
            return CacheAwareRouter(mock_pricing)

        def test_unknown_provider_returns_defaults(self, router):
            """Cover lines 267-272: Unknown provider returns no-cache defaults."""
            caps = router.get_provider_cache_capability("unknown_provider")

            assert caps["supports_cache"] is False
            assert caps["cached_cost_ratio"] == 1.0
            assert caps["min_tokens"] == 0

        def test_google_alias_to_gemini(self, router):
            """Cover lines 259-265: 'google' fuzzy matches to 'gemini'."""
            caps = router.get_provider_cache_capability("google")

            # Should return gemini config
            assert caps["supports_cache"] is True
            assert caps["cached_cost_ratio"] == 0.10
            assert caps["min_tokens"] == 1024

        def test_gemini_direct_lookup(self, router):
            """Cover lines 256-257: Direct 'gemini' lookup."""
            caps = router.get_provider_cache_capability("gemini")

            assert caps["supports_cache"] is True
            assert caps["cached_cost_ratio"] == 0.10
            assert caps["min_tokens"] == 1024
    ```

    Total: 17 tests across 5 test classes
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/core/llm/test_cache_aware_router_coverage_extend.py -v --tb=short</automated>
  </verify>
  <done>
    - File backend/tests/core/llm/test_cache_aware_router_coverage_extend.py exists (min 250 lines)
    - Contains 5 test classes with 17 tests total
    - Tests cover: cache hit prediction, outcome recording, history analytics, history clearing, provider edge cases
    - Command `pytest tests/core/llm/test_cache_aware_router_coverage_extend.py -v` passes with 17 passed
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create AgentGraduationService coverage tests</name>
  <files>backend/tests/core/episodes/test_agent_graduation_service_coverage.py</files>
  <read_first>backend/core/agent_graduation_service.py, backend/tests/core/episodes/test_episode_lifecycle_service_coverage.py</read_first>
  <behavior>
    Create backend/tests/core/episodes/test_agent_graduation_service_coverage.py with tests for:

    1. Service Initialization (1 test):
       - __init__ with db session and LanceDB handler

    2. Graduation Criteria Constants (3 tests):
       - INTERN criteria values
       - SUPERVISED criteria values
       - AUTONOMOUS criteria values

    3. Calculate Readiness Score (6 tests):
       - Ready agent meets all criteria
       - Not enough episodes fails
       - High intervention rate fails
       - Low constitutional score fails
       - Unknown maturity level returns error
       - Agent not found returns error

    4. Score Calculation (4 tests):
       - _calculate_score with perfect inputs
       - _calculate_score with minimum passing scores
       - _calculate_score with failing scores
       - Weight distribution verification (40% episodes, 30% intervention, 30% constitutional)

    5. Recommendation Generation (4 tests):
       - Ready agent gets positive recommendation
       - Very low score (<50) gets "significant training" message
       - Medium score (50-75) gets "more practice" message
       - High score (75-100) but not ready gets "address gaps" message

    6. Promote Agent (5 tests):
       - Successful promotion updates status
       - Promotion sets configuration metadata
       - Invalid maturity level returns False
       - Agent not found returns False
       - Updated_at timestamp is set

    7. Graduation Audit Trail (5 tests):
       - Returns agent info
       - Returns episode count
       - Calculates total interventions
       - Calculates average constitutional score
       - Groups episodes by maturity level
  </behavior>
  <action>
    Create backend/tests/core/episodes/test_agent_graduation_service_coverage.py with:

    ```python
    """
    Coverage-driven tests for AgentGraduationService (target 60%+)

    Target file: core/agent_graduation_service.py (829 lines)
    """

    import pytest
    from unittest.mock import Mock, patch, MagicMock
    from datetime import datetime, timezone
    from uuid import uuid4

    from core.agent_graduation_service import AgentGraduationService
    from core.models import (
        AgentRegistry,
        AgentStatus,
        Episode,
        EpisodeSegment,
        User,
        UserRole,
    )


    class TestAgentGraduationServiceInit:
        """Test AgentGraduationService initialization."""

        def test_service_initialization(self, db_session):
            """Cover lines 54-56: Service initialization with dependencies."""
            with patch('core.agent_graduation_service.get_lancedb_handler') as mock_lancedb:
                mock_lancedb.return_value = Mock()

                service = AgentGraduationService(db_session)

                assert service.db is db_session
                assert service.lancedb is not None


    class TestGraduationCriteria:
        """Test CRITERIA constants (lines 36-52)."""

        def test_intern_criteria(self):
            """Cover lines 37-40: INTERN graduation criteria."""
            from core.agent_graduation_service import AgentGraduationService
            criteria = AgentGraduationService.CRITERIA["INTERN"]

            assert criteria["min_episodes"] == 10
            assert criteria["max_intervention_rate"] == 0.5
            assert criteria["min_constitutional_score"] == 0.70

        def test_supervised_criteria(self):
            """Cover lines 42-45: SUPERVISED graduation criteria."""
            from core.agent_graduation_service import AgentGraduationService
            criteria = AgentGraduationService.CRITERIA["SUPERVISED"]

            assert criteria["min_episodes"] == 25
            assert criteria["max_intervention_rate"] == 0.2
            assert criteria["min_constitutional_score"] == 0.85

        def test_autonomous_criteria(self):
            """Cover lines 47-51: AUTONOMOUS graduation criteria."""
            from core.agent_graduation_service import AgentGraduationService
            criteria = AgentGraduationService.CRITERIA["AUTONOMOUS"]

            assert criteria["min_episodes"] == 50
            assert criteria["max_intervention_rate"] == 0.0
            assert criteria["min_constitutional_score"] == 0.95


    class TestCalculateReadinessScore:
        """Test calculate_readiness_score method (lines 58-106)."""

        @pytest.fixture
        def service(self, db_session):
            """Create service with mocked dependencies."""
            with patch('core.agent_graduation_service.get_lancedb_handler') as mock_lancedb:
                mock_lancedb.return_value = Mock()
                return AgentGraduationService(db_session)

        @pytest.mark.asyncio
        async def test_ready_agent_meets_all_criteria(self, service):
            """Cover lines 58-106: Agent ready for promotion."""
            agent = AgentRegistry(
                id=str(uuid4()),
                name="ReadyAgent",
                category="test",
                module_path="test.module",
                class_name="TestAgent",
                status=AgentStatus.INTERN.value,
                confidence_score=0.8,
                user_id="user1"
            )
            service.db.add(agent)
            service.db.commit()

            # Mock episode service to return ready
            with patch('core.agent_graduation_service.get_episode_service') as mock_get:
                mock_episode_service = MagicMock()
                mock_result = MagicMock()
                mock_result.to_dict.return_value = {
                    "ready": True,
                    "threshold_met": True,
                    "score": 85.0,
                    "episode_count": 15,
                    "intervention_rate": 0.3,
                    "constitutional_score": 0.85
                }
                mock_episode_service.get_graduation_readiness.return_value = mock_result
                mock_get.return_value = mock_episode_service

                result = await service.calculate_readiness_score(
                    agent_id=agent.id,
                    target_maturity="SUPERVISED"
                )

                assert result["ready"] is True
                assert result["score"] >= 80.0

        @pytest.mark.asyncio
        async def test_not_enough_episodes_fails(self, service):
            """Cover lines 94-99: Insufficient episodes."""
            agent = AgentRegistry(
                id=str(uuid4()),
                name="NewAgent",
                category="test",
                module_path="test.module",
                class_name="TestAgent",
                status=AgentStatus.INTERN.value,
                confidence_score=0.6
            )
            service.db.add(agent)
            service.db.commit()

            with patch('core.agent_graduation_service.get_episode_service') as mock_get:
                mock_episode_service = MagicMock()
                mock_result = MagicMock()
                mock_result.to_dict.return_value = {
                    "ready": False,
                    "threshold_met": False,
                    "score": 30.0,
                    "gaps": ["Need 15 more episodes"]
                }
                mock_episode_service.get_graduation_readiness.return_value = mock_result
                mock_get.return_value = mock_episode_service

                result = await service.calculate_readiness_score(
                    agent_id=agent.id,
                    target_maturity="SUPERVISED"
                )

                assert result["ready"] is False
                assert result["score"] < 50.0

        @pytest.mark.asyncio
        async def test_agent_not_found(self, service):
            """Cover lines 80-85: Agent not found returns error."""
            result = await service.calculate_readiness_score(
                agent_id="nonexistent",
                target_maturity="SUPERVISED"
            )

            assert "error" in result
            assert "not found" in result["error"].lower()

        @pytest.mark.asyncio
        async def test_unknown_maturity_level(self, service):
            """Cover lines 88-89: Unknown maturity level returns error."""
            agent = AgentRegistry(
                id=str(uuid4()),
                name="TestAgent",
                category="test",
                module_path="test.module",
                class_name="TestAgent",
                status=AgentStatus.INTERN.value
            )
            service.db.add(agent)
            service.db.commit()

            result = await service.calculate_readiness_score(
                agent_id=agent.id,
                target_maturity="INVALID"
            )

            assert "error" in result
            assert "unknown" in result["error"].lower()


    class TestCalculateScore:
        """Test _calculate_score method (lines 108-129)."""

        @pytest.fixture
        def service(self, db_session):
            """Create service instance."""
            with patch('core.agent_graduation_service.get_lancedb_handler'):
                return AgentGraduationService(db_session)

        def test_calculate_score_perfect_inputs(self, service):
            """Cover lines 108-129: Perfect scores give 100%."""
            score = service._calculate_score(
                episode_count=50,
                min_episodes=25,
                intervention_rate=0.0,
                max_intervention=0.2,
                constitutional_score=1.0,
                min_constitutional=0.85
            )

            assert score == 100.0

        def test_calculate_score_minimum_passing(self, service):
            """Cover lines 108-129: Minimum passing scores."""
            score = service._calculate_score(
                episode_count=25,
                min_episodes=25,
                intervention_rate=0.2,
                max_intervention=0.2,
                constitutional_score=0.85,
                min_constitutional=0.85
            )

            # All at minimum = 40 + 30 + 30 = 100
            assert score >= 95.0  # Allow small rounding differences

        def test_calculate_score_failing_inputs(self, service):
            """Cover lines 108-129: Failing scores give <50%."""
            score = service._calculate_score(
                episode_count=10,
                min_episodes=25,
                intervention_rate=0.5,
                max_intervention=0.2,
                constitutional_score=0.7,
                min_constitutional=0.85
            )

            assert score < 70.0

        def test_calculate_score_weight_distribution(self, service):
            """Cover lines 119-129: Verify 40/30/30 weight distribution."""
            # Episode score: 40% (perfect)
            # Intervention score: 30% (perfect)
            # Constitutional score: 30% (perfect)
            score = service._calculate_score(
                episode_count=100,
                min_episodes=10,
                intervention_rate=0.0,
                max_intervention=0.5,
                constitutional_score=1.0,
                min_constitutional=0.7
            )

            assert score == 100.0


    class TestGenerateRecommendation:
        """Test _generate_recommendation method (lines 131-141)."""

        @pytest.fixture
        def service(self, db_session):
            """Create service instance."""
            with patch('core.agent_graduation_service.get_lancedb_handler'):
                return AgentGraduationService(db_session)

        def test_recommendation_ready(self, service):
            """Cover lines 133-134: Ready agent gets positive message."""
            recommendation = service._generate_recommendation(
                ready=True,
                score=95.0,
                target="SUPERVISED"
            )

            assert "ready" in recommendation.lower()
            assert "95.0" in recommendation
            assert "supervised" in recommendation.lower()

        def test_recommendation_very_low_score(self, service):
            """Cover lines 136-137: Score <50 gets significant training message."""
            recommendation = service._generate_recommendation(
                ready=False,
                score=30.0,
                target="AUTONOMOUS"
            )

            assert "not ready" in recommendation.lower()
            assert "training" in recommendation.lower()

        def test_recommendation_medium_score(self, service):
            """Cover lines 137-138: Score 50-75 gets more practice message."""
            recommendation = service._generate_recommendation(
                ready=False,
                score=65.0,
                target="SUPERVISED"
            )

            assert "progress" in recommendation.lower()
            assert "practice" in recommendation.lower()

        def test_recommendation_high_score_not_ready(self, service):
            """Cover lines 138-140: Score 75-100 but not ready gets gaps message."""
            recommendation = service._generate_recommendation(
                ready=False,
                score=85.0,
                target="AUTONOMOUS"
            )

            assert "close to ready" in recommendation.lower()
            assert "gaps" in recommendation.lower()


    class TestPromoteAgent:
        """Test promote_agent method (lines 263-309)."""

        @pytest.fixture
        def service(self, db_session):
            """Create service instance."""
            with patch('core.agent_graduation_service.get_lancedb_handler'):
                return AgentGraduationService(db_session)

        @pytest.mark.asyncio
        async def test_promote_agent_success(self, service):
            """Cover lines 280-308: Successful promotion."""
            agent = AgentRegistry(
                id=str(uuid4()),
                name="PromoteMe",
                category="test",
                module_path="test.module",
                class_name="TestAgent",
                status=AgentStatus.INTERN.value,
                confidence_score=0.7
            )
            service.db.add(agent)
            service.db.commit()

            result = await service.promote_agent(
                agent_id=agent.id,
                new_maturity="SUPERVISED",
                validated_by="admin_user"
            )

            assert result is True
            service.db.refresh(agent)
            assert agent.status == AgentStatus.SUPERVISED.value
            assert agent.updated_at is not None

        @pytest.mark.asyncio
        async def test_promote_sets_configuration_metadata(self, service):
            """Cover lines 298-304: Promotion sets metadata."""
            agent = AgentRegistry(
                id=str(uuid4()),
                name="PromoteMeta",
                category="test",
                module_path="test.module",
                class_name="TestAgent",
                status=AgentStatus.SUPERVISED.value,
                configuration={}
            )
            service.db.add(agent)
            service.db.commit()

            await service.promote_agent(
                agent_id=agent.id,
                new_maturity="AUTONOMOUS",
                validated_by="admin_user"
            )

            service.db.refresh(agent)
            assert "promoted_at" in agent.configuration
            assert agent.configuration["promoted_by"] == "admin_user"

        @pytest.mark.asyncio
        async def test_promote_invalid_maturity(self, service):
            """Cover lines 291-293: Invalid maturity level."""
            agent = AgentRegistry(
                id=str(uuid4()),
                name="TestAgent",
                category="test",
                module_path="test.module",
                class_name="TestAgent",
                status=AgentStatus.INTERN.value
            )
            service.db.add(agent)
            service.db.commit()

            result = await service.promote_agent(
                agent_id=agent.id,
                new_maturity="INVALID_LEVEL",
                validated_by="admin"
            )

            assert result is False

        @pytest.mark.asyncio
        async def test_promote_agent_not_found(self, service):
            """Cover lines 284-286: Agent not found."""
            result = await service.promote_agent(
                agent_id="nonexistent",
                new_maturity="SUPERVISED",
                validated_by="admin"
            )

            assert result is False


    class TestGraduationAuditTrail:
        """Test get_graduation_audit_trail method (lines 311-378)."""

        @pytest.fixture
        def service(self, db_session):
            """Create service instance."""
            with patch('core.agent_graduation_service.get_lancedb_handler'):
                return AgentGraduationService(db_session)

        @pytest.mark.asyncio
        async def test_audit_returns_agent_info(self, service):
            """Cover lines 330-337, 369-370: Returns agent information."""
            agent = AgentRegistry(
                id=str(uuid4()),
                name="AuditAgent",
                category="test",
                module_path="test.module",
                class_name="TestAgent",
                status=AgentStatus.INTERN.value
            )
            service.db.add(agent)
            service.db.commit()

            trail = await service.get_graduation_audit_trail(agent.id)

            assert trail["agent_id"] == agent.id
            assert trail["agent_name"] == "AgentName"
            assert trail["current_maturity"] == "INTERN"

        @pytest.mark.asyncio
        async def test_audit_returns_episode_count(self, service):
            """Cover lines 339-343: Returns episode count."""
            agent = AgentRegistry(
                id=str(uuid4()),
                name="EpisodeAgent",
                category="test",
                module_path="test.module",
                class_name="TestAgent",
                status=AgentStatus.INTERN.value
            )
            service.db.add(agent)

            # Create episodes
            for i in range(5):
                episode = Episode(
                    id=str(uuid4()),
                    agent_id=agent.id,
                    user_id="user1",
                    workspace_id="default",
                    task_description=f"Task {i}",
                    status="completed",
                    started_at=datetime.now(timezone.utc)
                )
                service.db.add(episode)

            service.db.commit()

            trail = await service.get_graduation_audit_trail(agent.id)

            assert trail["total_episodes"] == 5

        @pytest.mark.asyncio
        async def test_audit_calculates_interventions(self, service):
            """Cover lines 344-345: Calculates total interventions."""
            agent = AgentRegistry(
                id=str(uuid4()),
                name="InterventionAgent",
                category="test",
                module_path="test.module",
                class_name="TestAgent",
                status=AgentStatus.SUPERVISED.value
            )
            service.db.add(agent)

            # Create episodes with interventions
            for i in range(3):
                episode = Episode(
                    id=str(uuid4()),
                    agent_id=agent.id,
                    user_id="user1",
                    workspace_id="default",
                    human_intervention_count=i + 1,
                    status="completed",
                    started_at=datetime.now(timezone.utc)
                )
                service.db.add(episode)

            service.db.commit()

            trail = await service.get_graduation_audit_trail(agent.id)

            assert trail["total_interventions"] == 6  # 1 + 2 + 3

        @pytest.mark.asyncio
        async def test_audit_calculates_constitutional_score(self, service):
            """Cover lines 347-351: Calculates average constitutional score."""
            agent = AgentRegistry(
                id=str(uuid4()),
                name="ScoreAgent",
                category="test",
                module_path="test.module",
                class_name="TestAgent",
                status=AgentStatus.SUPERVISED.value
            )
            service.db.add(agent)

            # Create episodes with scores
            scores = [0.8, 0.9, 1.0]
            for score in scores:
                episode = Episode(
                    id=str(uuid4()),
                    agent_id=agent.id,
                    user_id="user1",
                    workspace_id="default",
                    constitutional_score=score,
                    status="completed",
                    started_at=datetime.now(timezone.utc)
                )
                service.db.add(episode)

            service.db.commit()

            trail = await service.get_graduation_audit_trail(agent.id)

            assert trail["avg_constitutional_score"] == 0.9

        @pytest.mark.asyncio
        async def test_audit_groups_by_maturity(self, service):
            """Cover lines 353-365: Groups episodes by maturity level."""
            agent = AgentRegistry(
                id=str(uuid4()),
                name="GroupAgent",
                category="test",
                module_path="test.module",
                class_name="TestAgent",
                status=AgentStatus.INTERN.value
            )
            service.db.add(agent)

            # Create episodes at different maturity levels
            for maturity in ["INTERN", "SUPERVISED"]:
                for i in range(2):
                    episode = Episode(
                        id=str(uuid4()),
                        agent_id=agent.id,
                        user_id="user1",
                        workspace_id="default",
                        maturity_at_time=maturity,
                        status="completed",
                        started_at=datetime.now(timezone.utc)
                    )
                    service.db.add(episode)

            service.db.commit()

            trail = await service.get_graduation_audit_trail(agent.id)

            assert trail["episodes_by_maturity"]["INTERN"] == 2
            assert trail["episodes_by_maturity"]["SUPERVISED"] == 2
    ```

    Total: 23 tests across 7 test classes
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/core/episodes/test_agent_graduation_service_coverage.py -v --tb=short</automated>
  </verify>
  <done>
    - File backend/tests/core/episodes/test_agent_graduation_service_coverage.py exists (min 400 lines)
    - Contains 7 test classes with 23 tests total
    - Tests cover: init, criteria, readiness score, score calculation, recommendations, promotion, audit trail
    - Command `pytest tests/core/episodes/test_agent_graduation_service_coverage.py -v` passes with 23 passed
  </done>
</task>

<task type="auto">
  <name>Task 5: Generate Wave 1 coverage report</name>
  <files>backend/tests/core/governance/test_agent_context_resolver_coverage.py, backend/tests/core/llm/test_cognitive_tier_system_coverage.py, backend/tests/core/llm/test_cache_aware_router_coverage_extend.py, backend/tests/core/episodes/test_agent_graduation_service_coverage.py</files>
  <read_first>backend/TESTING.md, backend/tests/core/governance/test_agent_governance_service_coverage.py</read_first>
  <action>
    1. Run all Wave 1 tests to verify they pass:
       ```bash
       cd backend && python -m pytest \
         tests/core/governance/test_agent_context_resolver_coverage.py \
         tests/core/llm/test_cognitive_tier_system_coverage.py \
         tests/core/llm/test_cache_aware_router_coverage_extend.py \
         tests/core/episodes/test_agent_graduation_service_coverage.py \
         -v --tb=short
       ```

    2. Run coverage for Wave 1 target files:
       ```bash
       cd backend && python -m pytest \
         tests/core/governance/test_agent_context_resolver_coverage.py \
         tests/core/llm/test_cognitive_tier_system_coverage.py \
         tests/core/llm/test_cache_aware_router_coverage_extend.py \
         tests/core/episodes/test_agent_graduation_service_coverage.py \
         --cov=core/agent_context_resolver \
         --cov=core/llm/cognitive_tier_system \
         --cov=core/llm/cache_aware_router \
         --cov=core/agent_graduation_service \
         --cov-report=term-missing \
         --cov-report=html:.coverage_html_wave1
       ```

    3. Generate summary statistics:
       - Count total tests created (target: ~53 tests)
       - Count tests by category (governance: 15, llm: 30, episodes: 23)
       - Verify all tests pass
       - Check coverage increase (target: +1-3 percentage points from 4.60% baseline)

    4. Create Wave 1 summary document:
       ```bash
       # After verification, create summary
       cat > .planning/phases/253b-coverage-expansion-80pct/253b-01-COVERAGE.md << 'EOF'
       # Phase 253b-01 Coverage Wave 1 Report

       ## Tests Created
       - test_agent_context_resolver_coverage.py: 15 tests
       - test_cognitive_tier_system_coverage.py: 30 tests
       - test_cache_aware_router_coverage_extend.py: 17 tests
       - test_agent_graduation_service_coverage.py: 23 tests
       - **Total: 85 tests**

       ## Coverage Results
       [Fill in after running coverage]
       - agent_context_resolver.py: X% coverage
       - cognitive_tier_system.py: X% coverage
       - cache_aware_router.py: X% coverage
       - agent_graduation_service.py: X% coverage

       ## Overall Backend Coverage
       - Baseline (Phase 251): 4.60%
       - After Wave 1: X%
       - Increase: +X percentage points
       EOF
       ```

    5. Verify grep-verifiable acceptance criteria:
       ```bash
       # Check test files exist with correct content
       grep -l "class TestAgentContextResolverCoverage" backend/tests/core/governance/*.py
       grep -l "class TestCognitiveTierSystemCoverage" backend/tests/core/llm/*.py
       grep -l "class TestAgentGraduationServiceCoverage" backend/tests/core/episodes/*.py
       ```
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/core/governance/test_agent_context_resolver_coverage.py tests/core/llm/test_cognitive_tier_system_coverage.py tests/core/llm/test_cache_aware_router_coverage_extend.py tests/core/episodes/test_agent_graduation_service_coverage.py -v --tb=short | grep -E "(PASSED|FAILED|passed|failed|ERROR|test session)"</automated>
  </verify>
  <done>
    - All 85 tests pass (15 + 30 + 17 + 23)
    - Coverage report generated for target files
    - Summary document created at .planning/phases/253b-coverage-expansion-80pct/253b-01-COVERAGE.md
    - Overall backend coverage increased by +1-3 percentage points from 4.60% baseline
    - Grep-verifiable criteria met:
      - class TestAgentContextResolverCoverage exists
      - class TestCognitiveTierSystemCoverage exists
      - class TestAgentGraduationServiceCoverage exists
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code -> production code | Unit tests import and validate production service behavior without external dependencies |
| test fixtures -> database | Tests use isolated test database with rollback after each test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-253b-01 | Spoofing | mock dependencies | accept | Tests use unittest.mock for isolation; no external API calls |
| T-253b-02 | Information Disclosure | test error messages | accept | Test runs in isolated environment; no production data in tests |
| T-253b-03 | Denial of Service | test execution time | mitigate | Tests use minimal data; db_session fixtures clean up efficiently |
</threat_model>

<verification>
## Overall Phase Verification

1. Test files created for high-impact core services:
   - agent_context_resolver.py (15 tests)
   - cognitive_tier_system.py (30 tests)
   - cache_aware_router.py extension (17 tests)
   - agent_graduation_service.py (23 tests)

2. All tests use pytest with appropriate fixtures (db_session, mocks)

3. Coverage increased by +1-3 percentage points from 4.60% baseline

4. Tests cover:
   - Service initialization
   - Core business logic (fallback chains, classification, caching, graduation)
   - Edge cases (not found, invalid inputs, boundary conditions)
   - Data validation (criteria checks, score calculations)

## Requirement Coverage

- COV-B-04: Backend coverage reaches 80% - progressive waves - IN PROGRESS
  - Wave 1 (253b-01): +1-3 percentage points
  - Remaining waves will continue progressive expansion
</verification>

<success_criteria>
- [ ] 85 tests created (15 governance + 30 LLM + 17 cache + 23 graduation)
- [ ] All tests pass with `pytest tests/core/governance/test_agent_context_resolver_coverage.py tests/core/llm/test_cognitive_tier_system_coverage.py tests/core/llm/test_cache_aware_router_coverage_extend.py tests/core/episodes/test_agent_graduation_service_coverage.py -v`
- [ ] Coverage report shows +1-3 percentage point increase from 4.60% baseline
- [ ] Summary document created at 253b-01-COVERAGE.md
- [ ] COV-B-04 progressive wave 1 complete
</success_criteria>

<output>
After completion, create `.planning/phases/253b-coverage-expansion-80pct/253b-01-COVERAGE.md` with:
- Test count breakdown (15 + 30 + 17 + 23 = 85 tests)
- Coverage metrics for each target file
- Overall backend coverage percentage after Wave 1
- Verification that COV-B-04 wave 1 is complete
</output>
