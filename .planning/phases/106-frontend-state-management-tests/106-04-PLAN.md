---
phase: 106-frontend-state-management-tests
plan: 04
type: tdd
wave: 2
depends_on:
  - 106-01
  - 106-02
files_modified:
  - frontend-nextjs/tests/property/__tests__/state-transition-validation.test.ts
autonomous: true

must_haves:
  truths:
    - "State transition tests verify no unreachable states exist in chat, canvas, and auth state machines"
    - "FastCheck property tests validate state machine invariants with 50-100 examples"
    - "All valid transitions are documented and tested"
    - "Invalid transitions are prevented and tested"
  artifacts:
    - path: frontend-nextjs/tests/property/__tests__/state-transition-validation.test.ts
      provides: State transition validation tests (40+ property tests)
      min_lines: 600
  key_links:
    - from: state-transition-validation.test.ts
      to: tests/property/state-management.test.ts
      via: Existing property test patterns
      pattern: "fc.assert|fc.property|renderHook"
    - from: state-transition-validation.test.ts
      to: hooks/useWebSocket.ts, hooks/useCanvasState.ts, hooks/useChatMemory.ts
      via: State machine testing
      pattern: "isConnected|state|memories"
---

<objective>
Create FastCheck property tests validating state machine invariants and preventing unreachable states.

This plan uses TDD with FastCheck to validate state machine invariants for chat, canvas, and auth state management. Property tests systematically verify that:
1. No unreachable states exist (all states are reachable from initial state)
2. All valid transitions are allowed
3. Invalid transitions are prevented
4. State updates preserve invariants (immutability, idempotency)

This builds on existing property test patterns in state-management.test.ts but focuses specifically on state transition validation.

Output: 40+ property tests validating state machine correctness.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend-nextjs/tests/property/state-management.test.ts
@frontend-nextjs/hooks/useWebSocket.ts
@frontend-nextjs/hooks/useCanvasState.ts
@frontend-nextjs/hooks/useChatMemory.ts
@frontend-nextjs/package.json
</context>

<tasks>

<task type="auto">
  <name>RED: Create failing state transition property tests</name>
  <files>frontend-nextjs/tests/property/__tests__/state-transition-validation.test.ts</files>
  <action>
Create state-transition-validation.test.ts with property tests that will fail initially, then pass after implementation.

**Property Tests (RED phase - must fail first):**

1. **WebSocket State Machine Tests** (12 tests):
   - fc.property: WebSocket states only transition: disconnected -> connecting -> connected -> disconnected
   - fc.property: Cannot transition from connected directly to connecting (must disconnect first)
   - fc.property: isConnected boolean matches connection state (connected=true only in connected state)
   - fc.property: streamingContent only accumulates in connected state
   - fc.property: Messages are only received in connected state
   - fc.property: Subscriptions persist across reconnection
   - fc.property: Connection lifecycle is idempotent (connect->connect is same as connect)
   - fc.property: Disconnect is idempotent (disconnect->disconnect is no-op)
   - fc.property: Rapid connect/disconnect cycles settle to disconnected
   - fc.property: Auto-connect respects initial autoConnect value
   - fc.property: Token parameter is always present in WebSocket URL
   - fc.property: Channel subscriptions are queued until connected

2. **Canvas State Machine Tests** (10 tests):
   - fc.property: Canvas state is always null before first subscription callback
   - fc.property: allStates array grows monotonically (canvases added, never removed except explicitly)
   - fc.property: State updates preserve canvas_id immutability
   - fc.property: getState returns null for unregistered canvas
   - fc.property: getState returns valid state for registered canvas
   - fc.property: Multiple canvas subscriptions are independent
   - fc.property: Changing canvasId unsubscribes from previous, subscribes to new
   - fc.property: Unsubscribing stops state updates for that canvas
   - fc.property: Global subscription receives all canvas updates
   - fc.property: Canvas state contains required fields (canvas_type, canvas_id, timestamp)

3. **Chat Memory State Machine Tests** (10 tests):
   - fc.property: memories array grows monotonically (oldest first, newest at index 0)
   - fc.property: memories array is limited to contextWindow size
   - fc.property: Storing memory increments memory count (until limit)
   - fc.property: Clearing session resets memories to empty array
   - fc.property: memoryContext relevanceScore is always 0-1
   - fc.property: hasRelevantContext is false when relevanceScore <= 0.3
   - fc.property: isLoading is false after store completes (success or error)
   - fc.property: error is null on successful operation
   - fc.property: Disabled memory (enableMemory=false) prevents all operations
   - fc.property: Memory stats refresh does not affect memories array

4. **Auth State Machine Tests** (8 tests):
   - fc.property: Auth states: unauthenticated -> authenticated -> unauthenticated (no other transitions)
   - fc.property: loading is only true during state transitions
   - fc.property: session is null when status='unauthenticated'
   - fc.property: session is non-null when status='authenticated'
   - fc.property: error clears on successful state transition
   - fc.property: Login failure returns to unauthenticated with error set
   - fc.property: Logout from unauthenticated is no-op
   - fc.property: Session expiration transitions to unauthenticated

**Test Configuration:**
- numRuns: 50 for state machine tests (balanced coverage)
- seed: Fixed seeds for reproducibility
- Use fc.assert for property verification
- Use renderHook for hook testing
- Use act() for state transitions

**File Location:** frontend-nextjs/tests/property/__tests__/state-transition-validation.test.ts
  </action>
  <verify>
cd frontend-nextjs && npm test -- state-transition-validation --watchAll=false --passWithNoTests
  </verify>
  <done>
40+ property tests created, documenting expected state machine behavior
  </done>
</task>

<task type="auto">
  <name>GREEN: Ensure all property tests pass</name>
  <files>frontend-nextjs/tests/property/__tests__/state-transition-validation.test.ts</files>
  <action>
Run property tests and verify they pass. The tests validate existing state management code without modifications.

If any tests fail, document the failure in the test file as a TODO comment indicating a state machine bug to be fixed later.

**Expected Results:**
- Most property tests should pass (existing code is generally correct)
- Any failures indicate actual bugs in state management code
- Document failures with: // TODO: Fix state machine bug - [description]

**Failure Handling:**
- If test fails due to bug in hook code, document but don't fix (out of scope)
- If test fails due to incorrect test assumption, fix the test
- Goal: Verify state machine correctness, not fix all bugs

**Verification Command:**
```bash
cd frontend-nextjs
npm test -- state-transition-validation --watchAll=false
```

**File Location:** frontend-nextjs/tests/property/__tests__/state-transition-validation.test.ts (no changes, just verification)
  </action>
  <verify>
cd frontend-nextjs && npm test -- state-transition-validation --watchAll=false --passWithNoTests
  </verify>
  <done>
40+ property tests executed, results documented (pass/fail)
  </done>
</task>

</tasks>

<verification>
Run property tests and document results:

```bash
cd frontend-nextjs
npm test -- state-transition-validation --coverage --watchAll=false
```

Verify:
1. 40+ property tests created
2. Each test documents a specific state machine invariant
3. FastCheck configuration uses appropriate numRuns (50-100)
4. Tests cover all three state machines (WebSocket, Canvas, Auth)
5. Any failures are documented with TODO comments

Document unreachable states found (if any) and valid transitions verified.
</verification>

<success_criteria>
1. 40+ property tests created using FastCheck
2. Tests validate state machine invariants
3. No unreachable states found (or documented if found)
4. All valid transitions tested
5. Invalid transitions are prevented (verified or documented as bug)
6. Property tests use appropriate numRuns (50-100)
7. Test failures documented with TODO comments
</success_criteria>

<output>
After completion, create `.planning/phases/106-frontend-state-management-tests/106-04-SUMMARY.md` with:
- Test count (40+ property tests)
- State invariants validated
- Any unreachable states found
- Any state machine bugs discovered
</output>
