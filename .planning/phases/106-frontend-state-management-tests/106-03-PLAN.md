---
phase: 106-frontend-state-management-tests
plan: 03
type: execute
wave: 2
depends_on:
  - 106-01
  - 106-02
files_modified:
  - frontend-nextjs/tests/integration/__tests__/auth-state-management.test.tsx
  - frontend-nextjs/tests/integration/__tests__/session-persistence.test.tsx
autonomous: true

must_haves:
  truths:
    - "Auth state tests cover login/logout flows with useSession hook integration"
    - "Token refresh tests verify session persistence and automatic renewal"
    - "Session storage tests cover localStorage and session management"
    - "All auth tests verify proper cleanup and state transitions"
  artifacts:
    - path: frontend-nextjs/tests/integration/__tests__/auth-state-management.test.tsx
      provides: Auth state management tests (30+ tests)
      min_lines: 450
    - path: frontend-nextjs/tests/integration/__tests__/session-persistence.test.tsx
      provides: Session persistence tests (25+ tests)
      min_lines: 400
  key_links:
    - from: auth-state-management.test.tsx
      to: tests/integration/auth.test.tsx
      via: Existing auth integration test patterns
      pattern: "signIn|signOut|useSession"
    - from: session-persistence.test.tsx
      to: next-auth/react
      via: NextAuth session management
      pattern: "getSession|useSession|session"
---

<objective>
Create comprehensive tests for authentication state management covering login/logout, token refresh, and session persistence.

This plan tests the authentication state management using next-auth. The tests verify login/logout flows, session persistence across page reloads, token refresh behavior, and localStorage integration.

The existing auth.test.tsx covers basic integration flows but needs focused state management tests to achieve 50%+ coverage for auth-related code.

Output: 55+ tests across 2 files achieving 50%+ coverage for auth state management.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend-nextjs/tests/integration/auth.test.tsx
@frontend-nextjs/components/chat/ChatInterface.tsx
@frontend-nextjs/components/GlobalChatWidget.tsx
@frontend-nextjs/jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Create auth state management tests</name>
  <files>frontend-nextjs/tests/integration/__tests__/auth-state-management.test.tsx</files>
  <action>
Create auth-state-management.test.tsx with 30+ tests covering:

1. **useSession Hook Tests** (8 tests):
   - Returns null session when not authenticated
   - Returns session object when authenticated
   - Returns loading: true during session fetch
   - Returns loading: false after session fetch
   - Updates session when authentication changes
   - Provides update method for session refresh
   - Handles session expiration
   - Provides status: 'authenticated' | 'unauthenticated' | 'loading'

2. **Login State Tests** (7 tests):
   - Transitions from unauthenticated to authenticated on successful login
   - Stores session data after login
   - Sets loading state during login process
   - Handles login error with error state
   - Clears error state on next login attempt
   - Persists session to localStorage
   - Triggers useSession update after login

3. **Logout State Tests** (6 tests):
   - Transitions from authenticated to unauthenticated on logout
   - Clears session data on logout
   - Clears localStorage on logout
   - Sets loading state during logout process
   - Handles logout error gracefully
   - Redirects to home page after logout (router.push)

4. **Token Refresh Tests** (5 tests):
   - Automatically refreshes token before expiration
   - Updates session data after refresh
   - Handles token refresh failure
   - Retries token refresh on network error
   - Logs out on authentication failure during refresh

5. **Session State Transition Tests** (4 tests):
   - No unreachable states (no invalid state combinations)
   - loading cannot be true with status='authenticated'
   - error clears on successful authentication
   - session is null when status='unauthenticated'

**Test Patterns:**
- Mock next-auth/react: signIn, signOut, useSession, getSession
- Mock next/router: useRouter with push, pathname
- Mock localStorage: getItem, setItem, removeItem
- Use renderHook for useSession testing
- Test state transitions (before/after)
- Verify cleanup (localStorage cleared, etc.)

**File Location:** frontend-nextjs/tests/integration/__tests__/auth-state-management.test.tsx
  </action>
  <verify>
cd frontend-nextjs && npm test -- auth-state-management --coverage --watchAll=false --passWithNoTests
  </verify>
  <done>
30+ tests created, all passing, auth state management covered
  </done>
</task>

<task type="auto">
  <name>Create session persistence tests</name>
  <files>frontend-nextjs/tests/integration/__tests__/session-persistence.test.tsx</files>
  <action>
Create session-persistence.test.tsx with 25+ tests covering:

1. **localStorage Session Tests** (7 tests):
   - Session is stored to localStorage on login
   - Session is retrieved from localStorage on page load
   - Session persists across browser refresh
   - Multiple tabs share same session (storage event listener)
   - Session updates propagate to other tabs
   - Logout clears localStorage
   - Session expires based on timestamp

2. **Session Recovery Tests** (5 tests):
   - Recovers session from localStorage after refresh
   - Handles corrupted localStorage data gracefully
   - Handles missing localStorage gracefully
   - Validates session structure on recovery
   - Falls back to server session check if localStorage invalid

3. **Multi-Tab Synchronization Tests** (6 tests):
   - Login in one tab updates other tabs
   - Logout in one tab updates other tabs
   - Session refresh in one tab updates other tabs
   - Handles storage event for 'atom_session' key
   - Ignores storage events for other keys
   - Debounces rapid storage changes

4. **Session Expiration Tests** (4 tests):
   - Auto-logs out when session expires
   - Shows warning before session expiration
   - Refreshes session before expiration
   - Handles expired session on page load

5. **Security Tests** (3 tests):
   - Session token is not accessible to third-party scripts
   - XSS protection (session stored securely)
   - CSRF token included in requests

**Test Patterns:**
- Mock localStorage with storage event simulation
- Mock window.addEventListener for 'storage' events
- Simulate browser refresh with cleanup/reattachment
- Test cross-tab communication with storage events
- Use Jest fake timers for expiration testing

**File Location:** frontend-nextjs/tests/integration/__tests__/session-persistence.test.tsx
  </action>
  <verify>
cd frontend-nextjs && npm test -- session-persistence --coverage --watchAll=false --passWithNoTests
  </verify>
  <done>
25+ tests created, all passing, session persistence covered
  </done>
</task>

</tasks>

<verification>
Run coverage report to verify auth state coverage:

```bash
cd frontend-nextjs
npm test -- auth-state-management session-persistence --coverage --watchAll=false --passWithNoTests
```

Verify auth-related files show increased coverage:
- Components using useSession: increased coverage
- Auth API routes: increased coverage
- Session storage utilities: increased coverage

Verify no unreachable states in auth state machine.
</verification>

<success_criteria>
1. 55+ tests created across 2 files (30+ auth-state, 25+ session-persistence)
2. All tests passing (100% pass rate)
3. Auth state management covered with login/logout flows
4. Token refresh behavior verified
5. Session persistence across page reloads tested
6. Multi-tab synchronization verified
7. No unreachable auth states
</success_criteria>

<output>
After completion, create `.planning/phases/106-frontend-state-management-tests/106-03-SUMMARY.md` with:
- Test count (55+ tests)
- Auth state transitions covered
- Session persistence behaviors verified
- Any bugs or issues found
</output>
