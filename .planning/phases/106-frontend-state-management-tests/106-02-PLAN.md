---
phase: 106-frontend-state-management-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/components/canvas/__tests__/useCanvasState.test.tsx
autonomous: true

must_haves:
  truths:
    - "useCanvasState hook has 50%+ coverage including subscription lifecycle, API access, and state updates"
    - "Canvas state tests cover component registration, state updates, and accessibility API integration"
    - "Tests verify window.atom.canvas global API integration with proper initialization"
    - "Subscription cleanup verified on unmount to prevent memory leaks"
  artifacts:
    - path: frontend-nextjs/components/canvas/__tests__/useCanvasState.test.tsx
      provides: Canvas state hook tests (50+ tests)
      min_lines: 700
  key_links:
    - from: useCanvasState.test.tsx
      to: useCanvasState.ts
      via: import statement and renderHook calls
      pattern: "import.*from.*useCanvasState|renderHook.*useCanvasState"
    - from: useCanvasState.test.tsx
      to: components/canvas/types/index.ts
      via: CanvasStateAPI type imports
      pattern: "import.*CanvasStateAPI|CanvasStateChangeEvent"
---

<objective>
Create comprehensive tests for canvas state management using the useCanvasState hook.

This plan tests the useCanvasState hook which provides access to the canvas state API for components and AI agents. The hook integrates with the window.atom.canvas global API for state management and subscriptions.

The existing test file (canvas-state-hook.test.tsx) covers basic functionality but needs expansion to achieve 50%+ coverage with more edge cases, error scenarios, and accessibility API integration tests.

Output: 50+ tests achieving 50%+ coverage for useCanvasState hook.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend-nextjs/hooks/useCanvasState.ts
@frontend-nextjs/components/canvas/__tests__/canvas-state-hook.test.tsx
@frontend-nextjs/components/canvas/types/index.ts
@frontend-nextjs/jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Expand useCanvasState hook tests to 50+ coverage</name>
  <files>frontend-nextjs/components/canvas/__tests__/useCanvasState.test.tsx</files>
  <action>
Expand the existing canvas-state-hook.test.tsx file with additional tests to reach 50+ tests total (existing has ~30 tests, add 20+ more).

**New tests to add:**

1. **Canvas State Registration Tests** (5 tests):
   - Multiple canvases can be registered simultaneously
   - getState returns correct state for each canvas ID
   - getAllStates returns all registered canvases
   - Registering duplicate canvas ID updates existing entry
   - Unregistering canvas removes it from getAllStates

2. **State Update Tests** (6 tests):
   - State update triggers callback for specific canvas subscription
   - State update triggers callback for global subscription
   - Multiple rapid state updates are handled correctly
   - State update preserves canvas_type in event
   - State update includes timestamp
   - State update handles all 7 canvas types (generic, docs, email, sheets, orchestration, terminal, coding)

3. **Accessibility API Integration Tests** (5 tests):
   - window.atom.canvas.getState is accessible from hook
   - window.atom.canvas.getAllStates is accessible from hook
   - window.atom.canvas.subscribe is callable
   - window.atom.canvas.subscribeAll is callable
   - Hook methods work without accessibility API (graceful degradation)

4. **Subscription Lifecycle Tests** (4 tests):
   - Subscription is cleaned up when canvasId changes
   - Subscription is cleaned up on unmount
   - Multiple subscriptions can be active for different canvas IDs
   - Subscription callback receives correct state shape

5. **Error Handling Tests** (4 tests):
   - Handles missing window.atom gracefully
   - Handles missing window.atom.canvas gracefully
   - Returns empty array when getAllStates throws
   - Returns null when getState throws

6. **Edge Cases Tests** (6 tests):
   - Empty canvasId string is handled
   - Undefined canvasId is handled
   - Null canvasId is handled
   - Special characters in canvasId are handled
   - Very long canvasId is handled
   - Rapid canvasId changes don't cause memory leaks

**Existing test coverage to maintain:**
- Hook initialization tests (~15 tests)
- API method access tests (~8 tests)
- Cleanup and unmount tests (~7 tests)

**File Location:** frontend-nextjs/components/canvas/__tests__/useCanvasState.test.tsx (expand existing)
  </action>
  <verify>
cd frontend-nextjs && npm test -- canvas-state-hook --coverage --watchAll=false --passWithNoTests
  </verify>
  <done>
50+ tests total (existing + new), all passing, 50%+ coverage for useCanvasState hook
  </done>
</task>

</tasks>

<verification>
Run coverage report to verify 50%+ coverage:

```bash
cd frontend-nextjs
npm test -- canvas-state-hook --coverage --watchAll=false --passWithNoTests
```

Check coverage/coverage-summary.json for:
- hooks/useCanvasState.ts: 50%+

Verify all new tests follow existing patterns with renderHook and proper cleanup.
</verification>

<success_criteria>
1. 50+ tests total in useCanvasState test file
2. All tests passing (100% pass rate)
3. 50%+ coverage achieved for useCanvasState hook
4. Tests cover all 7 canvas types
5. Accessibility API integration verified
6. Subscription cleanup verified for memory leak prevention
</success_criteria>

<output>
After completion, create `.planning/phases/106-frontend-state-management-tests/106-02-SUMMARY.md` with:
- Test count (50+ tests)
- Coverage percentage
- Any bugs or issues found
- Canvas types covered
</output>
