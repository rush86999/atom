---
phase: 106-frontend-state-management-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-nextjs/hooks/__tests__/useWebSocket.test.ts
  - frontend-nextjs/hooks/__tests__/useChatMemory.test.ts
autonomous: true

must_haves:
  truths:
    - "useWebSocket hook has 50%+ coverage including connection lifecycle, message handling, streaming, and error states"
    - "useChatMemory hook has 50%+ coverage including store/retrieve/context operations and error handling"
    - "Chat state tests verify message streaming updates, context preservation, and error recovery"
    - "All state management tests use renderHook from React Testing Library with proper cleanup"
  artifacts:
    - path: frontend-nextjs/hooks/__tests__/useWebSocket.test.ts
      provides: useWebSocket hook tests (40+ tests)
      min_lines: 600
    - path: frontend-nextjs/hooks/__tests__/useChatMemory.test.ts
      provides: useChatMemory hook tests (35+ tests)
      min_lines: 500
  key_links:
    - from: useWebSocket.test.ts
      to: useWebSocket.ts
      via: import statement and renderHook calls
      pattern: "import.*from.*useWebSocket|renderHook.*useWebSocket"
    - from: useChatMemory.test.ts
      to: useChatMemory.ts
      via: import statement and renderHook calls
      pattern: "import.*from.*useChatMemory|renderHook.*useChatMemory"
---

<objective>
Create comprehensive tests for agent chat state management including useWebSocket and useChatMemory hooks.

This plan tests the core state management hooks that power the agent chat interface. The useWebSocket hook handles real-time communication with streaming content, while useChatMemory manages conversation context and persistence.

The tests follow React Testing Library best practices for hooks: renderHook for testing, act() for state updates, proper cleanup verification, and comprehensive edge case coverage.

Output: 75+ tests across 2 files achieving 50%+ coverage for both hooks.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend-nextjs/hooks/useWebSocket.ts
@frontend-nextjs/hooks/useChatMemory.ts
@frontend-nextjs/components/canvas/__tests__/canvas-state-hook.test.tsx
@frontend-nextjs/jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Create useWebSocket hook tests</name>
  <files>frontend-nextjs/hooks/__tests__/useWebSocket.test.ts</files>
  <action>
Create useWebSocket.test.ts with 40+ tests covering:

1. **Connection Lifecycle Tests** (10 tests):
   - Initializes with disconnected state
   - Connects when autoConnect is true
   - Does not auto-connect when autoConnect is false
   - Sets isConnected to true after successful connection
   - Sets isConnected to false after disconnect
   - Handles connection errors gracefully
   - Reconnects after disconnect
   - Cleans up WebSocket on unmount
   - Uses dev-token fallback when no session token
   - Constructs correct WebSocket URL with token parameter

2. **Message Handling Tests** (8 tests):
   - Receives and parses lastMessage from server
   - Handles malformed JSON messages (catch, no error thrown)
   - Handles streaming:update messages
   - Handles streaming:complete messages
   - Accumulates streaming content in Map by message ID
   - Removes completed streams from streamingContent Map
   - Handles message type field correctly
   - Preserves message timestamp

3. **Channel Subscription Tests** (6 tests):
   - Subscribes to channel via sendMessage
   - Unsubscribes from channel via sendMessage
   - Subscribes to initialChannels on connect
   - Handles subscription when not connected (no-op)
   - Handles unsubscribe when not connected (no-op)
   - Sends correct JSON format for subscribe/unsubscribe

4. **Streaming Content Tests** (6 tests):
   - Initializes with empty streamingContent Map
   - Adds delta content to existing stream
   - Creates new entry for new stream ID
   - Deletes stream entry when complete=true
   - Returns Map from streamingContent property
   - Handles multiple concurrent streams by ID

5. **State Management Tests** (5 tests):
   - Returns isConnected boolean
   - Returns lastMessage or null
   - Returns streamingContent Map
   - Returns subscribe function
   - Returns unsubscribe function

6. **Error Handling Tests** (5 tests):
   - Handles WebSocket onerror silently
   - Handles JSON parse errors in onmessage
   - Handles missing session token gracefully
   - Handles connection timeout (via onclose)
   - Does not throw on sendMessage when disconnected

**Test Patterns:**
- Use renderHook from @testing-library/react
- Use act() for state-triggering actions
- Mock WebSocket class: jest.fn(() => ({ readyState: 0, send: jest.fn(), close: jest.fn() }))
- Mock next-auth useSession: { data: { backendToken: 'test-token' } }
- Test cleanup with unmount callback
- Verify event listeners are added/removed

**File Location:** frontend-nextjs/hooks/__tests__/useWebSocket.test.ts
  </action>
  <verify>
cd frontend-nextjs && npm test -- useWebSocket.test --coverage --watchAll=false --passWithNoTests
  </verify>
  <done>
40+ tests created, all passing, 50%+ coverage for useWebSocket hook
  </done>
</task>

<task type="auto">
  <name>Create useChatMemory hook tests</name>
  <files>frontend-nextjs/hooks/__tests__/useChatMemory.test.ts</files>
  <action>
Create useChatMemory.test.ts with 35+ tests covering:

1. **Hook Initialization Tests** (6 tests):
   - Initializes with empty memories array
   - Initializes with null memoryContext
   - Initializes with null memoryStats
   - Initializes with isLoading false
   - Initializes with error null
   - Calls refreshMemoryStats on mount when enableMemory is true

2. **Memory Storage Tests** (8 tests):
   - Stores memory via API call
   - Adds new memory to local memories array
   - Limits memories to contextWindow size
   - Sets isLoading true during store
   - Sets error on store failure
   - Does not store when enableMemory is false
   - Refreshes stats after successful store
   - Handles network errors gracefully

3. **Memory Context Tests** (6 tests):
   - Retrieves memory context via API
   - Returns context with shortTermMemories
   - Returns context with longTermMemories
   - Returns context with relevanceScore
   - Returns disabled context when enableMemory is false
   - Returns empty context on error (not null)

4. **Session Management Tests** (5 tests):
   - Clears session memory via API call
   - Clears local memories array on clear
   - Clears memoryContext on clear
   - Refreshes stats after clear
   - Does nothing when enableMemory is false

5. **Memory Stats Tests** (4 tests):
   - Retrieves stats via API
   - Updates memoryStats state on success
   - Does not set error on stats failure
   - Returns null when enableMemory is false

6. **Derived State Tests** (3 tests):
   - hasRelevantContext is true when relevanceScore > 0.3 and has memories
   - hasRelevantContext is false when relevanceScore <= 0.3
   - contextRelevanceScore defaults to 0 when memoryContext is null

7. **Auto-Store Tests** (3 tests):
   - Auto-stores messages when autoStoreMessages is true
   - Does not auto-store when enableMemory is false
   - Calls storeMemory with correct metadata

**Test Patterns:**
- Use renderHook from @testing-library/react
- Mock fetch API for /api/chat/memory/* endpoints
- Use waitFor for async state updates
- Test loading states (isLoading before/after)
- Test error states (error message set correctly)
- Verify API call payloads (userId, sessionId, content)

**File Location:** frontend-nextjs/hooks/__tests__/useChatMemory.test.ts
  </action>
  <verify>
cd frontend-nextjs && npm test -- useChatMemory.test --coverage --watchAll=false --passWithNoTests
  </verify>
  <done>
35+ tests created, all passing, 50%+ coverage for useChatMemory hook
  </done>
</task>

</tasks>

<verification>
Run coverage report to verify 50%+ coverage for both hooks:

```bash
cd frontend-nextjs
npm test -- coverage --watchAll=false --passWithNoTests
```

Check coverage/coverage-summary.json for:
- hooks/useWebSocket.ts: 50%+
- hooks/useChatMemory.ts: 50%+

Verify all tests use renderHook properly with cleanup and act() for state updates.
</verification>

<success_criteria>
1. 75+ tests created across 2 files (40+ useWebSocket, 35+ useChatMemory)
2. All tests passing (100% pass rate)
3. 50%+ coverage achieved for useWebSocket hook
4. 50%+ coverage achieved for useChatMemory hook
5. All tests use renderHook with proper cleanup
6. WebSocket mock properly simulates connection lifecycle
</success_criteria>

<output>
After completion, create `.planning/phases/106-frontend-state-management-tests/106-01-SUMMARY.md` with:
- Test count (75+ tests)
- Coverage percentages for each hook
- Any bugs or issues found
- Files created list
</output>
