---
phase: 082-core-services-unit-testing-governance-episodes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/episodes/test_episode_segmentation_service.py
  - backend/core/episode_segmentation_service.py
autonomous: true

must_haves:
  truths:
    - "Episode segmentation tests cover time gap detection (30-minute threshold)"
    - "Topic change detection uses semantic similarity (< 0.75 threshold)"
    - "Task completion detection marks episode boundaries"
    - "Episode lifecycle tests cover creation, segmentation, archival"
    - "Canvas and feedback context extraction tested"
    - "Supervision episode creation tested"
  artifacts:
    - path: "backend/tests/unit/episodes/test_episode_segmentation_service.py"
      provides: "Comprehensive unit tests for EpisodeSegmentationService"
      min_lines: 1500
      exports: ["TestTimeGapDetection", "TestTopicChangeDetection", "TestEpisodeLifecycle", "TestCanvasContext", "TestSupervisionEpisodes"]
  key_links:
    - from: "backend/tests/unit/episodes/test_episode_segmentation_service.py"
      to: "backend/core/episode_segmentation_service.py"
      via: "import EpisodeSegmentationService, EpisodeBoundaryDetector"
      pattern: "from core.episode_segmentation_service import"
---

<objective>
Expand EpisodeSegmentationService unit tests to achieve 90%+ coverage including time gap detection, topic changes, task completion, canvas context extraction, and supervision episodes.

Purpose: EpisodeSegmentationService is critical for agent memory and learning. Comprehensive testing ensures episodes are segmented correctly, context is preserved, and edge cases (empty sessions, missing data) are handled gracefully.

Output: Enhanced test file with 25+ new tests covering previously untested paths.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/core/episode_segmentation_service.py
@backend/tests/unit/episodes/test_episode_segmentation_service.py

# Existing test patterns:
- Use pytest fixtures for mock_db and mock_lancedb
- Use patch for external dependencies (get_lancedb_handler, BYOKHandler, CanvasSummaryService)
- Test boundary conditions (threshold values, empty lists, None values)
- AsyncMock for async methods
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add episode creation and lifecycle tests</name>
  <files>backend/tests/unit/episodes/test_episode_segmentation_service.py</files>
  <action>
    Add comprehensive tests for create_episode_from_session covering:
    1. Successful episode creation with messages and executions
    2. Title generation from first user message (truncation at 50 chars)
    3. Description and summary generation
    4. Duration calculation from timestamps
    5. Topic extraction from message content (words > 4 chars)
    6. Entity extraction (emails, phone numbers, URLs)
    7. Importance score calculation (0.0 to 1.0 clamping)
    8. Maturity level retrieval from agent status
    9. Human intervention counting from metadata
    10. Minimum size enforcement (force_create bypass)

    Mock dependencies:
    - Mock ChatSession, ChatMessage, AgentExecution objects
    - Mock lancedb handler with embed_text returning embeddings
    - Mock BYOKHandler and CanvasSummaryService
    - Mock db.query for session lookups

    Location: Add new test class TestEpisodeCreation after TestSegmentCreation (after line 428)
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/episodes/test_episode_segmentation_service.py::TestEpisodeCreation -v</verify>
  <done>Episode creation paths tested, title/description/summary generation verified</done>
</task>

<task type="auto">
  <name>Task 2: Add canvas context extraction tests</name>
  <files>backend/tests/unit/episodes/test_episode_segmentation_service.py</files>
  <action>
    Add tests for canvas context methods covering:
    1. _fetch_canvas_context returns canvases ordered by created_at
    2. _extract_canvas_context builds correct structure (canvas_type, presentation_summary, visual_elements)
    3. Canvas metadata extraction (workflow_id, approval_status, revenue, etc.)
    4. User interaction mapping (submit, close, update, execute, present, approve, reject)
    5. Empty canvas list handling
    6. Critical data points extraction by canvas type (orchestration, sheets, terminal)
    7. _filter_canvas_context_detail with summary/standard/full levels
    8. _extract_canvas_context_llm with LLM-generated summary
    9. LLM timeout handling (2-second timeout)
    10. Fallback to metadata extraction on LLM failure

    Mock dependencies:
    - Mock CanvasAudit objects with canvas_type, action, audit_metadata
    - Mock CanvasSummaryService with generate_summary (AsyncMock)
    - Test timeout scenarios with AsyncMock side_effect

    Location: Add new test class TestCanvasContextExtraction after TestEpisodeCreation
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/episodes/test_episode_segmentation_service.py::TestCanvasContextExtraction -v</verify>
  <done>Canvas context extraction tested for all canvas types and detail levels</done>
</task>

<task type="auto">
  <name>Task 3: Add supervision episode and segment creation tests</name>
  <files>backend/tests/unit/episodes/test_episode_segmentation_service.py</files>
  <action>
    Add tests for supervision episode methods covering:
    1. create_supervision_episode with completed supervision session
    2. _format_agent_actions with task descriptions and status
    3. _format_interventions with timestamps and guidance
    4. _format_supervision_outcome with rating and feedback
    5. _extract_supervision_topics from agent name and task description
    6. _extract_supervision_entities (session, agent, supervisor IDs)
    7. _calculate_supervision_importance based on rating and intervention count
    8. _archive_supervision_episode_to_lancedb with metadata
    9. Segment creation with conversation, execution, intervention, reflection types
    10. _create_segments with boundary handling

    Mock dependencies:
    - Mock SupervisionSession with interventions list, supervisor_rating
    - Mock AgentExecution with task_description, status
    - Mock lancedb for archival
    - Mock db for segment storage

    Location: Add new test class TestSupervisionEpisodes after TestCanvasContextExtraction
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/episodes/test_episode_segmentation_service.py::TestSupervisionEpisodes -v</verify>
  <done>Supervision episode creation tested, all formatting methods verified</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. Test count increased by at least 25 new tests
2. Coverage for episode_segmentation_service.py increased to 90%+
3. All new tests pass: pytest backend/tests/unit/episodes/test_episode_segmentation_service.py -v
4. No regressions in existing tests
5. LanceDB and LLM services properly mocked

Coverage check:
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/episodes/test_episode_segmentation_service.py --cov=backend/core/episode_segmentation_service --cov-report=term-missing
</verification>

<success_criteria>
1. 25+ new tests added to test_episode_segmentation_service.py
2. Coverage of episode_segmentation_service.py reaches 90%+
3. Time gap, topic change, and task completion detection fully tested
4. Canvas context extraction tested for all canvas types
5. Supervision episode creation tested with intervention tracking
6. LLM service and LanceDB properly mocked
</success_criteria>

<output>
After completion, create `.planning/phases/082-core-services-unit-testing-governance-episodes/082-02-SUMMARY.md`
</output>
