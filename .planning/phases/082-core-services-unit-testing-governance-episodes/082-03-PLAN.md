---
phase: 082-core-services-unit-testing-governance-episodes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_byok_handler.py
  - backend/core/llm/byok_handler.py
autonomous: true

must_haves:
  truths:
    - "BYOK handler tests cover multi-provider routing (OpenAI, Anthropic, DeepSeek, Gemini)"
    - "Streaming responses tested with token-by-token emission"
    - "Error recovery covers provider failover and timeout handling"
    - "Token counting and cost attribution verified"
    - "Cognitive tier classification tested"
    - "Vision routing and coordinated vision tested"
  artifacts:
    - path: "backend/tests/unit/test_byok_handler.py"
      provides: "Comprehensive unit tests for BYOKHandler LLM integration"
      min_lines: 1500
      exports: ["TestProviderRouting", "TestStreaming", "TestErrorRecovery", "TestTokenCounting", "TestCognitiveTier"]
  key_links:
    - from: "backend/tests/unit/test_byok_handler.py"
      to: "backend/core/llm/byok_handler.py"
      via: "import BYOKHandler, QueryComplexity, CognitiveTier"
      pattern: "from core.llm.byok_handler import"
---

<objective>
Expand BYOKHandler unit tests to achieve 90%+ coverage including multi-provider routing, streaming responses, error recovery, token counting, cognitive tier classification, and vision support.

Purpose: BYOKHandler manages LLM provider routing, cost optimization, and streaming. Comprehensive testing ensures provider failover works, costs are tracked correctly, and streaming responses handle edge cases.

Output: Enhanced test file with 20+ new tests covering previously untested paths.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/core/llm/byok_handler.py
@backend/tests/unit/test_byok_handler.py

# Existing test patterns:
- Mock BYOKManager for provider key management
- Mock OpenAI clients for provider testing
- AsyncMock for streaming responses
- Patch external dependencies (get_byok_manager, get_pricing_fetcher, llm_usage_tracker)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add provider routing and cognitive tier tests</name>
  <files>backend/tests/unit/test_byok_handler.py</files>
  <action>
    Add comprehensive tests for provider selection covering:
    1. analyze_query_complexity classification (SIMPLE, MODERATE, COMPLEX, ADVANCED)
    2. Complexity scoring with length-based thresholds
    3. Vocabulary pattern matching (simple, moderate, technical, code, advanced)
    4. Code block detection (``` triggers complexity increase)
    5. Task type override behavior
    6. get_optimal_provider returns correct (provider, model) tuple
    7. get_ranked_providers returns prioritized list
    8. Cache-aware routing with estimated_tokens
    9. Cognitive tier classification (MICRO, STANDARD, VERSATILE, HEAVY, COMPLEX)
    10. classify_cognitive_tier wrapper method

    Test edge cases:
    - Empty prompt defaults to SIMPLE
    - Very long prompts (>2000 tokens) -> COMPLEX
    - Code patterns -> COMPLEX or ADVANCED
    - Task type "code" increases complexity

    Mock dependencies:
    - Mock pricing fetcher for BPC algorithm
    - Mock quality scores for model filtering
    - Mock cache router for cache hit prediction

    Location: Add new test class TestProviderRouting after existing routing tests (around line 200)
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/test_byok_handler.py::TestProviderRouting -v</verify>
  <done>All complexity levels tested, cognitive tier classification verified</done>
</task>

<task type="auto">
  <name>Task 2: Add streaming and error recovery tests</name>
  <files>backend/tests/unit/test_byok_handler.py</files>
  <action>
    Add tests for streaming and error handling covering:
    1. stream_completion yields tokens one by one
    2. Streaming tracks token count correctly
    3. Governance tracking creates AgentExecution records
    4. Stream completion updates execution status to "completed"
    5. Stream failure updates execution status to "failed"
    6. Provider failover on streaming errors
    7. generate_response provider fallback loop
    8. Trial restriction check (_is_trial_restricted)
    9. Budget enforcement (llm_usage_tracker.is_budget_exceeded)
    10. Free tier managed AI blocking

    Mock dependencies:
    - AsyncMock for streaming with async generator
    - Mock AgentExecution for governance tracking
    - Mock llm_usage_tracker for budget checks
    - Mock Workspace for trial status

    Test error scenarios:
    - API rate limit triggers provider fallback
    - All providers fail returns error message
    - Budget exceeded returns budget message
    - Trial expired returns trial message

    Location: Add new test class TestStreamingAndRecovery after TestProviderRouting
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/test_byok_handler.py::TestStreamingAndRecovery -v</verify>
  <done>Streaming tested with governance tracking, error recovery verified</done>
</task>

<task type="auto">
  <name>Task 3: Add token counting, vision, and cost attribution tests</name>
  <files>backend/tests/unit/test_byok_handler.py</files>
  <action>
    Add tests for token counting and vision support covering:
    1. get_context_window returns correct size for known models
    2. get_context_window defaults to 4096 for unknown models
    3. truncate_to_context preserves short text
    4. truncate_to_context truncates long text with indicator
    5. Token counting from response.usage (prompt_tokens, completion_tokens)
    6. Cost attribution via llm_usage_tracker.record
    7. Savings calculation (reference cost vs actual cost)
    8. Vision routing for image_payload (base64 and URL)
    9. Coordinated vision with non-vision reasoning models
    10. _get_coordinated_vision_description generates semantic description

    Mock dependencies:
    - Mock usage object with prompt_tokens, completion_tokens
    - Mock pricing fetcher for cost estimation
    - Mock vision model client for _get_coordinated_vision_description
    - Mock response with choices[0].message.content

    Test vision scenarios:
    - Image payload routes to vision-capable models
    - Reasoning models without vision get coordinated description
    - Vision-only models (Janus) selection

    Location: Add new test class TestTokenCountingAndVision after TestStreamingAndRecovery
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/test_byok_handler.py::TestTokenCountingAndVision -v</verify>
  <done>Token counting verified, vision routing tested, cost attribution confirmed</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. Test count increased by at least 20 new tests
2. Coverage for byok_handler.py increased to 90%+
3. All new tests pass: pytest backend/tests/unit/test_byok_handler.py -v
4. No regressions in existing tests
5. External services properly mocked (no real API calls)

Coverage check:
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/test_byok_handler.py --cov=backend/core/llm/byok_handler --cov-report=term-missing
</verification>

<success_criteria>
1. 20+ new tests added to test_byok_handler.py
2. Coverage of byok_handler.py reaches 90%+
3. Provider routing tested for all complexity levels
4. Streaming responses tested with governance tracking
5. Error recovery and provider failover verified
6. Vision routing and coordinated vision tested
7. Token counting and cost attribution verified
</success_criteria>

<output>
After completion, create `.planning/phases/082-core-services-unit-testing-governance-episodes/082-03-SUMMARY.md`
</output>
