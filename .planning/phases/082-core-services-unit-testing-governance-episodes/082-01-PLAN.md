---
phase: 082-core-services-unit-testing-governance-episodes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_agent_governance_service.py
  - backend/core/agent_governance_service.py
autonomous: true

must_haves:
  truths:
    - "Agent governance service tests cover lifecycle methods (registration, update, promotion)"
    - "Permission checks verify action complexity mapping and maturity requirements"
    - "Cache invalidation occurs on agent status changes"
    - "Error handling covers missing agents, permission denied, invalid states"
    - "Tests use mocks for external services (RBAC, WorldModel, GovernanceCache)"
  artifacts:
    - path: "backend/tests/unit/test_agent_governance_service.py"
      provides: "Comprehensive unit tests for AgentGovernanceService"
      min_lines: 1200
      exports: ["TestAgentRegistration", "TestPermissionChecks", "TestCacheInvalidation", "TestErrorHandling"]
  key_links:
    - from: "backend/tests/unit/test_agent_governance_service.py"
      to: "backend/core/agent_governance_service.py"
      via: "import AgentGovernanceService"
      pattern: "from core.agent_governance_service import AgentGovernanceService"
---

<objective>
Expand AgentGovernanceService unit tests to achieve 90%+ coverage of all code paths including lifecycle methods, permission checks, cache invalidation, and error handling.

Purpose: AgentGovernanceService is a critical security component that manages agent maturity, permissions, and governance. Comprehensive test coverage ensures agents cannot bypass maturity restrictions, cache works correctly, and edge cases are handled.

Output: Enhanced test file with 30+ new tests covering previously untested paths.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/core/agent_governance_service.py
@backend/tests/unit/test_agent_governance_service.py

# Existing test patterns to follow:
- Use pytest fixtures for mock database (mock_db)
- Use AsyncMock for async methods (_adjudicate_feedback, record_outcome)
- Patch external services (get_governance_cache, RBACService, WorldModelService)
- Test both success and failure paths
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add feedback adjudication tests with specialty matching</name>
  <files>backend/tests/unit/test_agent_governance_service.py</files>
  <action>
    Add comprehensive tests for _adjudicate_feedback method covering:
    1. Admin user feedback acceptance (WORKSPACE_ADMIN, SUPER_ADMIN roles)
    2. Specialty matching logic (user.specialty == agent.category case-insensitive)
    3. Non-trusted reviewer queuing (regular users without specialty match)
    4. WorldModelService.record_experience integration
    5. Feedback status transitions (PENDING -> ACCEPTED)
    6. Confidence score updates based on adjudication result
    7. Edge cases: None specialty, mismatched specialty, admin with no specialty

    Mock dependencies:
    - WorldModelService with AsyncMock for record_experience
    - User and AgentRegistry with appropriate role/specialty combinations
    - Verify ai_reasoning is set correctly for each case

    Location: Add new test class TestFeedbackAdjudication after TestFeedbackSubmission (after line 237)
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/test_agent_governance_service.py::TestFeedbackAdjudication -v</verify>
  <done>All adjudication paths tested, specialty matching verified, WorldModel integration mocked</done>
</task>

<task type="auto">
  <name>Task 2: Add cache invalidation and governance cache tests</name>
  <files>backend/tests/unit/test_agent_governance_service.py</files>
  <action>
    Add comprehensive cache invalidation tests covering:
    1. Cache invalidation on maturity transition (STUDENT -> INTERN -> SUPERVISED -> AUTONOMOUS)
    2. Cache invalidation on promote_to_autonomous call
    3. Cache HIT vs MISS behavior in can_perform_action
    4. Cache result structure includes all required fields
    5. Cache key format (agent_id:action_type)
    6. Multiple action types cached for same agent
    7. Cache update after confidence score change with status transition
    8. No cache invalidation when confidence changes but status stays same

    Mock dependencies:
    - patch('core.agent_governance_service.get_governance_cache')
    - Mock cache instance with get, set, invalidate methods
    - Verify invalidate called with correct agent_id
    - Verify set called with correct key and result structure

    Location: Add new test class TestCacheInvalidation after TestOutcomeRecording (after line 409)
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/test_agent_governance_service.py::TestCacheInvalidation -v</verify>
  <done>Cache invalidation verified on all status transitions, cache HIT/MISS paths tested</done>
</task>

<task type="auto">
  <name>Task 3: Add evolution directive validation tests (GEA guardrail)</name>
  <files>backend/tests/unit/test_agent_governance_service.py</files>
  <action>
    Add tests for validate_evolution_directive method covering:
    1. Hard danger phrase blocking ("ignore all rules", "bypass guardrails", etc.)
    2. Evolution depth limit (>50 entries triggers block)
    3. Domain noise pattern detection ("as an ai language model", etc.)
    4. Valid config approval (safe prompts, reasonable depth)
    5. Case-insensitive danger phrase matching
    6. Empty evolution_history handling
    7. Boundary conditions: exactly 50 history entries (allowed), 51 (blocked)
    8. Missing system_prompt handling (empty string, None)

    Use AsyncMock since validate_evolution_directive is async
    Test both返回True for safe configs and返回False for blocked configs

    Location: Add new test class TestEvolutionDirectiveValidation at end of file
  </action>
  <verify>PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/test_agent_governance_service.py::TestEvolutionDirectiveValidation -v</verify>
  <done>All GEA guardrail paths tested: danger phrases, depth limits, noise patterns</done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. Test count increased by at least 30 new tests
2. Coverage for agent_governance_service.py increased to 90%+
3. All new tests pass: pytest backend/tests/unit/test_agent_governance_service.py -v
4. No regressions in existing tests
5. Mock usage is appropriate (external services mocked, not real DB/API calls)

Coverage check:
PYTHONPATH=/Users/rushiparikh/projects/atom/backend pytest backend/tests/unit/test_agent_governance_service.py --cov=backend/core/agent_governance_service --cov-report=term-missing
</verification>

<success_criteria>
1. 30+ new tests added to test_agent_governance_service.py
2. Coverage of agent_governance_service.py reaches 90%+ (verify with --cov)
3. All adjudication paths tested (admin, specialty match, non-trusted)
4. Cache invalidation verified on all maturity transitions
5. GEA guardrail tests cover all danger phrases and limits
6. Tests use proper mocks (WorldModelService, GovernanceCache, RBACService)
</success_criteria>

<output>
After completion, create `.planning/phases/082-core-services-unit-testing-governance-episodes/082-01-SUMMARY.md`
</output>
