---
phase: 176-api-routes-coverage-auth-authz
plan: 04
type: execute
wave: 2
depends_on: ["176-01", "176-02", "176-03"]
files_modified:
  - backend/tests/api/test_permission_checks.py
  - backend/tests/api/conftest.py
autonomous: true

must_haves:
  truths:
    - "Permission check functions achieve 75%+ line coverage"
    - "RBACService.check_permission fully tested"
    - "require_permission dependency tested"
    - "WebSocket auth tested (get_current_user_websocket)"
  artifacts:
    - path: "backend/tests/api/test_permission_checks.py"
      provides: "Comprehensive permission check tests"
      min_lines: 350
    - path: "backend/tests/api/conftest.py"
      provides: "Permission fixtures (test_users_with_roles, mock_rbac_check)"
      min_lines: 40
  key_links:
    - from: "backend/tests/api/test_permission_checks.py"
      to: "backend/core/security_dependencies.py"
      via: "Test require_permission function"
      pattern: "from core.security_dependencies import require_permission"
    - from: "backend/tests/api/test_permission_checks.py"
      to: "backend/core/rbac_service.py"
      via: "Test RBACService.check_permission"
      pattern: "from core.rbac_service import RBACService"
---

<objective>
Comprehensive coverage for permission checking functions (security_dependencies.py and rbac_service.py).

Purpose: Achieve 75%+ line coverage on authorization and permission checking code. This covers require_permission dependency factory, RBACService.check_permission, WebSocket authentication, and role-based permission enforcement critical for API security.

Output: Test file with 350+ lines covering all permission checks with role testing, permission verification, and WebSocket auth.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-api-routes-coverage/167-01-PLAN.md
@.planning/phases/167-api-routes-coverage/167-01-SUMMARY.md
@backend/core/security_dependencies.py
@backend/core/rbac_service.py
@backend/core/models.py
@backend/tests/api/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Create permission check test fixtures</name>
  <files>backend/tests/api/conftest.py</files>
  <action>
Add permission check fixtures to backend/tests/api/conftest.py:

1. Create test_users_with_roles fixture:
   - Returns dict mapping UserRole to User objects
   - Create users for: GUEST, MEMBER, TEAM_LEAD, WORKSPACE_ADMIN, SUPER_ADMIN
   - Each user has proper id, email, role set

2. Create mock_rbac_check fixture:
   - Mock RBACService.check_permission
   - Track permission checks for verification
   - Return True/False based on test configuration

3. Create all_permissions fixture:
   - Returns list of all Permission enum values
   - Useful for parametrized tests

4. Create role_permission_mapping fixture:
   - Returns dict of {UserRole: Set[Permission]}
   - From ROLE_PERMISSIONS constant
   - For verifying permission inheritance

File modification: Append fixtures to existing conftest.py (do NOT replace).
  </action>
  <verify>
Run: python -c "from backend.tests.api.conftest import test_users_with_roles; print('Permission fixtures loaded successfully')"
Check: New fixtures exist and are importable
  </verify>
  <done>
Permission fixtures provide reusable User objects with all roles and permission verification helpers.
  </done>
</task>

<task type="auto">
  <name>Test RBACService.check_permission</name>
  <files>backend/tests/api/test_permission_checks.py</files>
  <action>
Create backend/tests/api/test_permission_checks.py with RBACService tests:

1. Test class TestRBACServiceCheckPermission:
   - test_guest_has_view_permissions: GUEST can agent:view, workflow:view
   - test_guest_cannot_run_agents: GUEST cannot agent:run
   - test_member_has_run_permissions: MEMBER can agent:run, workflow:run
   - test_member_cannot_manage: MEMBER cannot agent:manage, workflow:manage
   - test_team_lead_has_workflow_manage: TEAM_LEAD can workflow:manage
   - test_team_lead_cannot_agent_manage: TEAM_LEAD cannot agent:manage
   - test_workspace_admin_has_all_workspace_permissions: WORKSPACE_ADMIN can manage agents and users
   - test_workspace_admin_cannot_system_admin: WORKSPACE_ADMIN cannot system:admin
   - test_super_admin_has_all_permissions: SUPER_ADMIN has all permissions
   - test_string_role_conversion: Handle string role values
   - test_invalid_role_returns_false: Unknown role returns False
   - test_permission_enum_values: Verify all Permission enum values are valid

2. Use parametrized tests for permission matrix:
   - Test each role against each permission
   - 5 roles x 11 permissions = 55 test cases
   - Use pytest.mark.parametrize

3. Verify permission sets:
   - get_user_permissions returns correct set
   - SUPER_ADMIN returns all permissions
   - Other roles return subset

File size target: 150+ lines for RBACService tests.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_permission_checks.py::TestRBACServiceCheckPermission -v
Check: All RBAC tests pass, coverage of RBACService >75%
  </verify>
  <done>
RBACService.check_permission fully tested with role-permission matrix and edge cases.
  </done>
</task>

<task type="auto">
  <name>Test require_permission dependency</name>
  <files>backend/tests/api/test_permission_checks.py</files>
  <action>
Add require_permission dependency tests to existing test file:

1. Test class TestRequirePermissionDependency:
   - test_require_permission_with_valid_permission: User with permission passes
   - test_require_permission_with_invalid_permission: User without permission raises 403
   - test_require_permission_factory_pattern: Verify factory returns callable
   - test_require_permission_http_403: Verify HTTPException status code
   - test_require_permission_error_message: Verify error message includes permission name
   - test_require_permission_with_get_current_user: Verify dependency chaining works

2. Test with FastAPI TestClient:
   - Create test endpoint using require_permission
   - Test authenticated user with permission
   - Test authenticated user without permission
   - Test unauthenticated user

3. Create test endpoint:
   ```python
   @router.get("/test-permission")
   async def test_endpoint(
       user: User = Depends(require_permission(Permission.AGENT_RUN))
   ):
       return {"success": True}
   ```

4. Verify dependency injection:
   - get_current_user called before permission check
   - User passed to permission checker
   - Permission check raises correct exception

File size target: 100+ lines for require_permission tests.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_permission_checks.py::TestRequirePermissionDependency -v
Check: All require_permission tests pass, coverage >75%
  </verify>
  <done>
require_permission dependency fully tested with permission checks, error raising, and dependency chaining.
  </done>
</task>

<task type="auto">
  <name>Test WebSocket authentication</name>
  <files>backend/tests/api/test_permission_checks.py</files>
  <action>
Add WebSocket authentication tests to existing test file:

1. Test class TestWebSocketAuth:
   - test_get_current_user_websocket_valid_token: Valid token returns User
   - test_get_current_user_websocket_invalid_token: Invalid token returns None
   - test_get_current_user_websocket_missing_sub: Token without sub returns None
   - test_get_current_user_websocket_with_db: Token + db session returns User from DB
   - test_get_current_user_websocket_without_db: Token without db returns None
   - test_get_current_user_websocket_user_not_found: Valid token but user missing returns None
   - test_get_current_user_websocket_exception_handling: Exception returns None

2. Mock WebSocket connection:
   - Create mock WebSocket object
   - Mock query parameter access
   - Test with various token values

3. Mock decode_token:
   - patch("core.auth.decode_token")
   - Return valid payload for good tokens
   - Raise exception for invalid tokens

4. Test database integration:
   - Use db_session fixture
   - Create test users in DB
   - Verify user lookup works

File size target: 100+ lines for WebSocket auth tests.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_permission_checks.py::TestWebSocketAuth -v
Check: All WebSocket auth tests pass, coverage >75%
  </verify>
  <done>
WebSocket authentication fully tested with token validation, user lookup, and error handling.
  </done>
</task>

<task type="auto">
  <name>Test permission edge cases and security</name>
  <files>backend/tests/api/test_permission_checks.py</files>
  <action>
Add security and edge case tests:

1. Test class TestPermissionSecurity:
   - test_permission_escalation_prevented: User cannot escalate permissions
   - test_role_change_propagates: Role change immediately affects permissions
   - test_case_sensitive_permission_names: Permission names are case-sensitive
   - test_empty_permission_denied: Empty/None permission always denied
   - test_concurrent_permission_checks: Multiple simultaneous checks are thread-safe

2. Test class TestPermissionEdgeCases:
   - test_user_with_no_role: User without role has no permissions
   - test_user_with_invalid_role: Invalid role string handled gracefully
   - test_super_admin_bypass: SUPER_ADMIN bypasses all permission checks
   - test_permission_enum_completeness: All enum values have string values
   - test_role_permissions_immutability: ROLE_PERMISSIONS cannot be modified

3. Test security boundaries:
   - Verify no permission bypass for invalid roles
   - Verify SUPER_ADMIN cannot be impersonated
   - Verify permission checks cannot be skipped

4. Test error messages:
   - Verify error messages don't leak sensitive info
   - Verify permission names included in errors
   - Verify user IDs not in error messages

File size target: 100+ lines for security tests.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_permission_checks.py::TestPermissionSecurity -v && python -m pytest backend/tests/api/test_permission_checks.py::TestPermissionEdgeCases -v
Check: All security tests pass, edge cases verified
  </verify>
  <done>
Permission security and edge cases fully tested with escalation prevention, role handling, and error message verification.
  </done>
</task>

</tasks>

<verification>
1. Run all permission tests: python -m pytest backend/tests/api/test_permission_checks.py -v --cov=backend/core/security_dependencies --cov=backend/core/rbac_service --cov-report=term-missing
2. Verify coverage >75% for both security_dependencies.py and rbac_service.py
3. Confirm all test classes pass (TestRBACServiceCheckPermission, TestRequirePermissionDependency, TestWebSocketAuth, TestPermissionSecurity, TestPermissionEdgeCases)
4. Check total test count >25 tests
5. Verify file size >350 lines
</verification>

<success_criteria>
1. test_permission_checks.py created with 350+ lines, 25+ tests
2. security_dependencies.py achieves 75%+ line coverage
3. rbac_service.py achieves 75%+ line coverage
4. All permission check functions tested (check_permission, require_permission, get_current_user_websocket)
5. Role-permission matrix fully tested
6. Security edge cases tested (escalation, invalid roles, error messages)
</success_criteria>

<output>
After completion, create `.planning/phases/176-api-routes-coverage-auth-authz/176-04-SUMMARY.md` with:
- Coverage achieved for security_dependencies.py and rbac_service.py
- Total new test count and lines of test code
- Permission matrix coverage breakdown
- Any security concerns identified
</output>
