---
phase: 176-api-routes-coverage-auth-authz
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_auth_2fa_routes_enhanced.py
  - backend/tests/api/conftest.py
autonomous: true

must_haves:
  truths:
    - "2FA auth routes (api/auth_2fa_routes.py) achieve 75%+ line coverage"
    - "All 2FA endpoints tested (status, setup, enable, disable)"
    - "TOTP verification tested with pyotp mocking"
    - "Audit logging verified for 2FA enable/disable actions"
  artifacts:
    - path: "backend/tests/api/test_auth_2fa_routes_enhanced.py"
      provides: "Comprehensive 2FA routes TestClient tests"
      min_lines: 400
    - path: "backend/tests/api/conftest.py"
      provides: "2FA-specific fixtures (mock_totp, mock_audit_service)"
      min_lines: 30
  key_links:
    - from: "backend/tests/api/test_auth_2fa_routes_enhanced.py"
      to: "backend/api/auth_2fa_routes.py"
      via: "TestClient with FastAPI app including auth_2fa_routes router"
      pattern: "from api.auth_2fa_routes import router|TestClient\\(app\\)"
    - from: "backend/tests/api/test_auth_2fa_routes_enhanced.py"
      to: "backend/core/audit_service"
      via: "Mock audit_service.log_event for enable/disable actions"
      pattern: "mock.*audit_service|patch.*core.audit_service"
---

<objective>
Comprehensive TestClient-based coverage for 2FA authentication routes (auth_2fa_routes.py).

Purpose: Achieve 75%+ line coverage on two-factor authentication endpoints using FastAPI TestClient with proper TOTP mocking and audit logging verification. This file contains 4 endpoints (status, setup, enable, disable) critical for account security.

Output: Enhanced test file with 400+ lines covering all 2FA routes with TOTP verification, audit logging, and error cases.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-api-routes-coverage/167-01-PLAN.md
@.planning/phases/167-api-routes-coverage/167-01-SUMMARY.md
@backend/api/auth_2fa_routes.py
@backend/tests/api/test_auth_2fa_routes.py
@backend/tests/api/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Create 2FA-specific test fixtures</name>
  <files>backend/tests/api/conftest.py</files>
  <action>
Add 2FA-specific fixtures to backend/tests/api/conftest.py:

1. Create mock_totp fixture:
   - Mock pyotp.TOTP class
   - Mock verify() method to return True for valid codes, False for invalid
   - Mock provisioning_uri() to return fake otpauth:// URL
   - Accept parametrized codes for testing

2. Create mock_pyotp_random fixture:
   - patch("pyotp.random_base32")
   - Return deterministic secret for testing (e.g., "TEST_SECRET_32_CHARS")

3. Create user_with_2fa fixture:
   - Create User with two_factor_enabled=True
   - Set two_factor_secret to test value
   - Set two_factor_backup_codes to ["BACKUP-1234-5678"]

4. Create user_without_2fa fixture:
   - Create User with two_factor_enabled=False
   - No secret or backup codes set

5. Create mock_audit_log fixture:
   - Mock audit_service.log_event
   - Track calls for verification in tests
   - Return MagicMock for chaining

File modification: Append fixtures to existing conftest.py (do NOT replace).
  </action>
  <verify>
Run: python -c "from backend.tests.api.conftest import mock_totp, user_with_2fa; print('2FA fixtures loaded successfully')"
Check: New fixtures exist and are importable
  </verify>
  <done>
2FA conftest.py provides reusable fixtures for TOTP mocking and user state testing.
  </done>
</task>

<task type="auto">
  <name>Test 2FA status and setup endpoints</name>
  <files>backend/tests/api/test_auth_2fa_routes_enhanced.py</files>
  <action>
Create backend/tests/api/test_auth_2fa_routes_enhanced.py with status and setup tests:

1. Test class TestTwoFactorStatus:
   - test_status_when_disabled: GET /api/auth/2fa/status returns enabled=False
   - test_status_when_enabled: GET /api/auth/2fa/status returns enabled=True
   - test_status_requires_auth: 401 without Authorization header
   - test_status_response_format: Verify {"enabled": bool} structure

2. Test class TestTwoFactorSetup:
   - test_setup_generates_secret: POST /api/auth/2fa/setup generates new secret
   - test_setup_returns_otpauth_url: Response includes otpauth_url for QR code
   - test_setup_requires_auth: 401 without Authorization header
   - test_setup_saves_secret_to_user: Verify two_factor_secret set on user model
   - test_setup_when_already_enabled: 409 conflict when 2FA already enabled
   - test_setup_issuer_name: Verify "Atom AI (Upstream)" in provisioning_uri
   - test_setup_secret_length: Verify secret is 32 chars (base32)

3. Create FastAPI app in module:
   ```python
   from fastapi import FastAPI
   from api.auth_2fa_routes import router
   app = FastAPI()
   app.include_router(router)
   ```

4. Mock pyotp.random_base32:
   - Use mock_pyotp_random fixture
   - Return deterministic "JBSWY3DPEHPK3PXP" for testing

5. Use db_session for database operations
   - Create test users
   - Commit changes for setup
   - Clean up after tests

File size target: 150+ lines for status and setup tests.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_auth_2fa_routes_enhanced.py::TestTwoFactorStatus -v && python -m pytest backend/tests/api/test_auth_2fa_routes_enhanced.py::TestTwoFactorSetup -v
Check: All status/setup tests pass, coverage of these endpoints >75%
  </verify>
  <done>
2FA status and setup endpoints fully tested with secret generation, OTP URL creation, and conflict handling.
  </done>
</task>

<task type="auto">
  <name>Test 2FA enable endpoint</name>
  <files>backend/tests/api/test_auth_2fa_routes_enhanced.py</files>
  <action>
Add 2FA enable tests to existing test file:

1. Test class TestTwoFactorEnable:
   - test_enable_with_valid_code: POST /api/auth/2fa/enable success
   - test_enable_invalid_code: 400 validation error for wrong TOTP code
   - test_enable_requires_auth: 401 without Authorization header
   - test_enable_requires_setup: 400 when two_factor_secret not set
   - test_enable_sets_enabled_flag: Verify two_factor_enabled=True after success
   - test_enable_generates_backup_codes: Verify two_factor_backup_codes generated
   - test_enable_audit_log: Verify audit_service.log_event called
   - test_enable_already_enabled: 409 conflict when already enabled

2. Mock TOTP verification:
   - Use mock_totp fixture
   - Configure verify() to return True for "123456", False for others
   - Test with both valid and invalid codes

3. Verify audit logging:
   - Check audit_service.log_event called with:
     - event_type=AuditEventType.UPDATE.value
     - action="2fa_enabled"
     - security_level=SecurityLevel.HIGH.value
   - Verify user_id and user_email in log

4. Test backup code generation:
   - Verify two_factor_backup_codes is list
   - Verify at least one backup code generated
   - Verify backup code format

File size target: 150+ lines for enable tests.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_auth_2fa_routes_enhanced.py::TestTwoFactorEnable -v
Check: All enable tests pass, TOTP verification works, audit logging verified
  </verify>
  <done>
2FA enable endpoint fully tested with TOTP verification, backup code generation, and audit logging.
  </done>
</task>

<task type="auto">
  <name>Test 2FA disable endpoint</name>
  <files>backend/tests/api/test_auth_2fa_routes_enhanced.py</files>
  <action>
Add 2FA disable tests to existing test file:

1. Test class TestTwoFactorDisable:
   - test_disable_with_valid_code: POST /api/auth/2fa/disable success
   - test_disable_invalid_code: 400 validation error for wrong TOTP code
   - test_disable_requires_auth: 401 without Authorization header
   - test_disable_when_not_enabled: 400 validation error when 2FA not enabled
   - test_disable_clears_enabled_flag: Verify two_factor_enabled=False after success
   - test_disable_clears_secret: Verify two_factor_secret=None after success
   - test_disable_clears_backup_codes: Verify two_factor_backup_codes=None after success
   - test_disable_audit_log: Verify audit_service.log_event called
   - test_disable_audit_details: Verify action="2fa_disabled" in log

2. Test state changes:
   - Create user with 2FA enabled
   - Verify all 2FA fields set
   - Call disable endpoint
   - Verify all 2FA fields cleared

3. Verify audit logging:
   - Check audit_service.log_event called with:
     - event_type=AuditEventType.UPDATE.value
     - action="2fa_disabled"
     - security_level=SecurityLevel.HIGH.value
   - Verify description includes user email

4. Test idempotency:
   - Disable twice with valid code
   - Second call should fail (not enabled)

File size target: 120+ lines for disable tests.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_auth_2fa_routes_enhanced.py::TestTwoFactorDisable -v
Check: All disable tests pass, cleanup verified, audit logging confirmed
  </verify>
  <done>
2FA disable endpoint fully tested with TOTP verification, field cleanup, and audit logging.
  </done>
</task>

<task type="auto">
  <name>Test error paths and edge cases</name>
  <files>backend/tests/api/test_auth_2fa_routes_enhanced.py</files>
  <action>
Add comprehensive error path and edge case tests:

1. Test class TestTwoFactorErrorPaths:
   - test_database_error_on_setup: Mock db.commit() failure
   - test_database_error_on_enable: Mock db.commit() failure during enable
   - test_database_error_on_disable: Mock db.commit() failure during disable
   - test_pyotp_import_error: Handle missing pyotp library gracefully
   - test_concurrent_enable_requests: Multiple simultaneous enable requests

2. Test class TestTwoFactorEdgeCases:
   - test_very_long_secret: Handle 100+ character secrets
   - test_special_characters_in_backup_codes: Backup codes with special chars
   - test_enable_with_backup_code: Enable using backup code instead of TOTP
   - test_disable_with_backup_code: Disable using backup code
   - test_rate_limiting_codes: Test multiple failed code attempts
   - test_code_reuse_prevention: Same code cannot be used twice

3. Use monkeypatch for error injection:
   - Mock db.commit() to raise Exception
   - Mock pyotp.TOTP to raise exception
   - Verify proper error responses

4. Test security considerations:
   - Verify secrets not returned after enable
   - Verify backup codes only shown once
   - Verify audit logs include all security-relevant data

File size target: 100+ lines for error path tests.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_auth_2fa_routes_enhanced.py::TestTwoFactorErrorPaths -v && python -m pytest backend/tests/api/test_auth_2fa_routes_enhanced.py::TestTwoFactorEdgeCases -v
Check: All error path tests pass, security edge cases verified
  </verify>
  <done>
Error paths and edge cases fully tested with database failures, TOTP errors, and security considerations.
  </done>
</task>

</tasks>

<verification>
1. Run all enhanced 2FA tests: python -m pytest backend/tests/api/test_auth_2fa_routes_enhanced.py -v --cov=backend/api/auth_2fa_routes --cov-report=term-missing
2. Verify coverage >75% for auth_2fa_routes.py
3. Confirm all test classes pass (TestTwoFactorStatus, TestTwoFactorSetup, TestTwoFactorEnable, TestTwoFactorDisable, TestTwoFactorErrorPaths, TestTwoFactorEdgeCases)
4. Check total test count >25 tests
5. Verify file size >400 lines
</verification>

<success_criteria>
1. test_auth_2fa_routes_enhanced.py created with 400+ lines, 25+ tests
2. auth_2fa_routes.py achieves 75%+ line coverage
3. All 4 endpoints tested (status, setup, enable, disable)
4. TOTP verification tested with pyotp mocking
5. Audit logging verified for enable/disable actions
6. Error paths tested (database failures, validation errors)
</success_criteria>

<output>
After completion, create `.planning/phases/176-api-routes-coverage-auth-authz/176-02-SUMMARY.md` with:
- Coverage achieved for auth_2fa_routes.py
- Total new test count and lines of test code
- Endpoint coverage breakdown
- Any uncovered edge cases identified
</output>
