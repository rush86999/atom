---
phase: 176-api-routes-coverage-auth-authz
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_agent_control_routes.py
  - backend/tests/api/conftest.py
autonomous: true

must_haves:
  truths:
    - "Agent control routes (api/agent_control_routes.py) achieve 75%+ line coverage"
    - "All agent control endpoints tested (start, stop, restart, status, execute)"
    - "DaemonManager mocking tested for all operations"
    - "Error paths tested (already running, not running, invalid parameters)"
  artifacts:
    - path: "backend/tests/api/test_agent_control_routes.py"
      provides: "Comprehensive agent control routes TestClient tests"
      min_lines: 400
    - path: "backend/tests/api/conftest.py"
      provides: "Agent control fixtures (mock_daemon_manager, test_pid)"
      min_lines: 40
  key_links:
    - from: "backend/tests/api/test_agent_control_routes.py"
      to: "backend/api/agent_control_routes.py"
      via: "TestClient with FastAPI app including agent_control_routes router"
      pattern: "from api.agent_control_routes import router|TestClient\\(app\\)"
    - from: "backend/tests/api/test_agent_control_routes.py"
      to: "backend/cli/daemon.py"
      via: "Mock DaemonManager for start/stop/status operations"
      pattern: "mock.*DaemonManager|patch.*cli.daemon"
---

<objective>
Comprehensive TestClient-based coverage for agent control routes (agent_control_routes.py).

Purpose: Achieve 75%+ line coverage on Atom OS agent control endpoints using FastAPI TestClient with proper DaemonManager mocking. This file contains 5 endpoints (start, stop, restart, status, execute) critical for programmatic Atom OS control from external agents.

Output: Test file with 400+ lines covering all agent control routes with happy paths, error cases, and daemon lifecycle testing.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-api-routes-coverage/167-01-PLAN.md
@.planning/phases/167-api-routes-coverage/167-01-SUMMARY.md
@backend/api/agent_control_routes.py
@backend/cli/daemon.py
@backend/tests/api/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Create agent control test fixtures</name>
  <files>backend/tests/api/conftest.py</files>
  <action>
Add agent control-specific fixtures to backend/tests/api/conftest.py:

1. Create mock_daemon_manager fixture:
   - Mock cli.daemon.DaemonManager class
   - Mock is_running() to return False by default (configurable)
   - Mock get_pid() to return test PID (12345)
   - Mock start_daemon() to return test PID
   - Mock stop_daemon() with no return value
   - Mock get_status() to return status dict

2. Create test_daemon_status fixture:
   - Returns dict with typical status:
     ```python
     {
         "running": True,
         "pid": 12345,
         "uptime_seconds": 3600,
         "memory_mb": 256.5,
         "cpu_percent": 5.2,
         "status": "running"
     }
     ```

3. Create mock_running_daemon fixture:
   - Returns DaemonManager mock with is_running()=True
   - Configured for "already running" test scenarios

4. Create mock_stopped_daemon fixture:
   - Returns DaemonManager mock with is_running()=False
   - Configured for "not running" test scenarios

File modification: Append fixtures to existing conftest.py (do NOT replace).
  </action>
  <verify>
Run: python -c "from backend.tests.api.conftest import mock_daemon_manager, test_daemon_status; print('Agent control fixtures loaded successfully')"
Check: New fixtures exist and are importable
  </verify>
  <done>
Agent control conftest.py provides reusable fixtures for DaemonManager mocking and status testing.
  </done>
</task>

<task type="auto">
  <name>Test agent start endpoint</name>
  <files>backend/tests/api/test_agent_control_routes.py</files>
  <action>
Create backend/tests/api/test_agent_control_routes.py with start endpoint tests:

1. Test class TestAgentStart:
   - test_start_success: POST /api/agent/start with default params
   - test_start_with_custom_port: Start with port=9000
   - test_start_with_custom_host: Start with host="127.0.0.1"
   - test_start_with_workers: Start with workers=4
   - test_start_with_host_mount: Start with host_mount=True
   - test_start_with_dev_mode: Start with dev=True
   - test_start_already_running: 400 when DaemonManager.is_running()=True
   - test_start_returns_dashboard_url: Verify dashboard_url in response
   - test_start_invalid_port: 422 for port >65535 or <1
   - test_start_invalid_workers: 422 for workers >16 or <1
   - test_start_runtime_error: 500 on RuntimeError from start_daemon
   - test_start_io_error: 500 on IOError from start_daemon

2. Create FastAPI app in module:
   ```python
   from fastapi import FastAPI
   from api.agent_control_routes import router
   app = FastAPI()
   app.include_router(router)
   ```

3. Use parametrized tests for validation:
   - Port values: 1, 8000, 65535 (valid), 0, 65536, -1 (invalid)
   - Worker values: 1, 4, 16 (valid), 0, 17 (invalid)

4. Mock DaemonManager.start_daemon:
   - Use mock_daemon_manager fixture
   - Return test PID for successful starts
   - Raise RuntimeError/IOError for error tests

File size target: 150+ lines for start endpoint tests.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_agent_control_routes.py::TestAgentStart -v
Check: All start tests pass, coverage of start endpoint >75%
  </verify>
  <done>
Agent start endpoint fully tested with parameter validation, daemon lifecycle, and error handling.
  </done>
</task>

<task type="auto">
  <name>Test agent stop endpoint</name>
  <files>backend/tests/api/test_agent_control_routes.py</files>
  <action>
Add agent stop endpoint tests to existing test file:

1. Test class TestAgentStop:
   - test_stop_success: POST /api/agent/stop when running
   - test_stop_not_running: 400 when DaemonManager.is_running()=False
   - test_stop_calls_stop_daemon: Verify DaemonManager.stop_daemon() called
   - test_stop_response_format: Verify success=True, status="stopped"
   - test_stop_exception_handling: 500 on unexpected exception

2. Test idempotency:
   - test_stop_when_already_stopped: Handle multiple stop calls gracefully

3. Verify response structure:
   - success: True
   - status: "stopped"
   - message: Success message included

4. Use mock_daemon_manager fixture:
   - Configure is_running() for each test scenario
   - Verify stop_daemon() called exactly once

File size target: 80+ lines for stop endpoint tests.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_agent_control_routes.py::TestAgentStop -v
Check: All stop tests pass, coverage of stop endpoint >75%
  </verify>
  <done>
Agent stop endpoint fully tested with daemon lifecycle, error handling, and idempotency.
  </done>
</task>

<task type="auto">
  <name>Test agent restart and status endpoints</name>
  <files>backend/tests/api/test_agent_control_routes.py</files>
  <action>
Add restart and status endpoint tests to existing test file:

1. Test class TestAgentRestart:
   - test_restart_when_running: Stop then start successfully
   - test_restart_when_not_running: Start without stopping first
   - test_restart_with_new_config: Apply new port/host on restart
   - test_restart_was_running_true: Verify was_running=True in response when running
   - test_restart_was_running_false: Verify was_running=False when not running
   - test_restart_returns_new_pid: Verify new PID returned after restart
   - test_restart_exception_handling: 500 on unexpected exception

2. Test class TestAgentStatus:
   - test_status_when_running: GET /api/agent/status returns running status
   - test_status_when_not_running: GET /api/agent/status returns non-running status
   - test_status_includes_pid: Verify pid in status dict
   - test_status_includes_uptime: Verify uptime_seconds in status
   - test_status_includes_memory: Verify memory_mb in status
   - test_status_includes_cpu: Verify cpu_percent in status
   - test_status_exception_handling: 500 on get_status() exception

3. Use test_daemon_status fixture:
   - Return realistic status dict
   - Test all status fields present

4. Test restart timing:
   - Verify 2-second wait between stop and start
   - Use mock for time.sleep to speed up tests

File size target: 150+ lines for restart and status tests.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_agent_control_routes.py::TestAgentRestart -v && python -m pytest backend/tests/api/test_agent_control_routes.py::TestAgentStatus -v
Check: All restart/status tests pass, coverage of these endpoints >75%
  </verify>
  <done>
Agent restart and status endpoints fully tested with lifecycle management, status reporting, and error handling.
  </done>
</task>

<task type="auto">
  <name>Test agent execute endpoint</name>
  <files>backend/tests/api/test_agent_control_routes.py</files>
  <action>
Add execute endpoint tests to existing test file:

1. Test class TestAgentExecute:
   - test_execute_placeholder: POST /api/agent/execute returns placeholder response
   - test_execute_response_format: Verify success=True, result, note fields
   - test_execute_command_validation: Verify command field required
   - test_execute_timeout_validation: Verify timeout constraints (1-300 seconds)
   - test_execute_invalid_timeout: 422 for timeout <1 or >300

2. Test placeholder behavior:
   - Verify result="Command execution not yet implemented"
   - Verify note explains to use daemon mode instead
   - Test with various command strings

3. Test input validation:
   - Missing command field (422)
   - Invalid timeout values
   - Valid timeout boundary values (1, 300)

4. Document expected behavior:
   - Note: This endpoint is currently a placeholder
   - Future implementation will execute actual commands

File size target: 80+ lines for execute endpoint tests.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_agent_control_routes.py::TestAgentExecute -v
Check: All execute tests pass, coverage of execute endpoint >75%
  </verify>
  <done>
Agent execute endpoint fully tested with placeholder behavior and input validation.
  </done>
</task>

<task type="auto">
  <name>Test error paths and edge cases</name>
  <files>backend/tests/api/test_agent_control_routes.py</files>
  <action>
Add comprehensive error path and edge case tests:

1. Test class TestAgentControlErrorPaths:
   - test_start_daemon_permission_error: Handle permission errors
   - test_start_port_already_in_use: Handle port binding errors
   - test_stop_daemon_not_responding: Handle unresponsive daemon
   - test_status_corrupted_pid_file: Handle corrupted PID file
   - test_concurrent_start_requests: Multiple simultaneous start requests
   - test_concurrent_stop_requests: Multiple simultaneous stop requests

2. Test class TestAgentControlEdgeCases:
   - test_very_long_host_name: Handle 1000+ character host names
   - test_special_characters_in_command: Command with special chars
   - test_maximum_workers: Start with 16 workers (maximum)
   - test_minimum_workers: Start with 1 worker (minimum)
   - test_boundary_port_values: Test ports 1, 65535
   - test_restart_loop_protection: Prevent restart loops

3. Use monkeypatch for error injection:
   - Mock DaemonManager methods to raise exceptions
   - Mock file operations for PID file handling
   - Verify proper error responses

4. Test response consistency:
   - All error responses have consistent format
   - Error messages are descriptive
   - HTTP status codes are appropriate

File size target: 100+ lines for error path tests.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_agent_control_routes.py::TestAgentControlErrorPaths -v && python -m pytest backend/tests/api/test_agent_control_routes.py::TestAgentControlEdgeCases -v
Check: All error path tests pass, edge cases verified
  </verify>
  <done>
Error paths and edge cases fully tested with permission errors, port conflicts, and boundary conditions.
  </done>
</task>

</tasks>

<verification>
1. Run all agent control tests: python -m pytest backend/tests/api/test_agent_control_routes.py -v --cov=backend/api/agent_control_routes --cov-report=term-missing
2. Verify coverage >75% for agent_control_routes.py
3. Confirm all test classes pass (TestAgentStart, TestAgentStop, TestAgentRestart, TestAgentStatus, TestAgentExecute, TestAgentControlErrorPaths, TestAgentControlEdgeCases)
4. Check total test count >30 tests
5. Verify file size >400 lines
</verification>

<success_criteria>
1. test_agent_control_routes.py created with 400+ lines, 30+ tests
2. agent_control_routes.py achieves 75%+ line coverage
3. All 5 endpoints tested (start, stop, restart, status, execute)
4. DaemonManager mocking tested for all operations
5. Error paths tested (already running, not running, invalid parameters)
6. Input validation tested (port range, workers range, timeout constraints)
</success_criteria>

<output>
After completion, create `.planning/phases/176-api-routes-coverage-auth-authz/176-03-SUMMARY.md` with:
- Coverage achieved for agent_control_routes.py
- Total new test count and lines of test code
- Endpoint coverage breakdown
- Any uncovered edge cases identified
</output>
