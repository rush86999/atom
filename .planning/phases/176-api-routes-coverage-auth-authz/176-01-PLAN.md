---
phase: 176-api-routes-coverage-auth-authz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/api/test_auth_routes_enhanced.py
  - backend/tests/api/conftest.py
autonomous: true

must_haves:
  truths:
    - "Auth routes (api/auth_routes.py) achieve 75%+ line coverage"
    - "All mobile authentication endpoints tested (login, biometric, refresh, device management)"
    - "Error paths tested (invalid credentials, device not found, validation errors)"
    - "Tests use proper fixtures (db_session, test_user, mock services)"
  artifacts:
    - path: "backend/tests/api/test_auth_routes_enhanced.py"
      provides: "Comprehensive auth routes TestClient tests"
      min_lines: 500
    - path: "backend/tests/api/conftest.py"
      provides: "Auth-specific fixtures (mock_user, mock_mobile_device, test_token)"
      min_lines: 50
  key_links:
    - from: "backend/tests/api/test_auth_routes_enhanced.py"
      to: "backend/api/auth_routes.py"
      via: "TestClient with FastAPI app including auth_routes router"
      pattern: "from api.auth_routes import router|TestClient\\(app\\)"
    - from: "backend/tests/api/test_auth_routes_enhanced.py"
      to: "backend/core/auth.py"
      via: "Mock authenticate_mobile_user, create_mobile_token, verify_biometric_signature"
      pattern: "mock.*authenticate_mobile_user|patch.*core.auth"
---

<objective>
Comprehensive TestClient-based coverage for mobile authentication routes (auth_routes.py).

Purpose: Achieve 75%+ line coverage on mobile authentication endpoints using FastAPI TestClient with proper fixture isolation. This file contains 6 endpoints (login, biometric register/authenticate, refresh, device get/delete) critical for mobile app authentication.

Output: Enhanced test file with 500+ lines covering all auth routes with happy paths, error cases, and governance.
</objective>

<execution_context>
@/Users/rushiparikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rushiparikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/167-api-routes-coverage/167-01-PLAN.md
@.planning/phases/167-api-routes-coverage/167-01-SUMMARY.md
@backend/api/auth_routes.py
@backend/tests/api/test_auth_routes.py
@backend/tests/api/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Create auth-specific test fixtures</name>
  <files>backend/tests/api/conftest.py</files>
  <action>
Add auth-specific fixtures to backend/tests/api/conftest.py:

1. Create mock_mobile_device fixture:
   - Returns MagicMock MobileDevice with device_token, platform (ios/android), device_info
   - Set user_id, status="active", notification_enabled=True
   - Mock last_active and created_at timestamps

2. Create test_user_with_device fixture:
   - Creates User with id, email, hashed_password
   - Creates associated MobileDevice
   - Returns (user, device) tuple

3. Create mock_auth_service fixture:
   - Mock authenticate_mobile_user: returns dict with user, access_token, refresh_token
   - Mock create_mobile_token: returns tokens dict
   - Mock verify_biometric_signature: returns True for valid signatures
   - Mock get_mobile_device: returns device or None based on params

4. Create biometric_test_data fixture:
   - Returns dict with public_key, device_token, signature, challenge
   - Use fake keys for testing (no real crypto)

File modification: Append fixtures to existing conftest.py (do NOT replace entire file).
  </action>
  <verify>
Run: python -c "from backend.tests.api.conftest import mock_mobile_device, test_user_with_device; print('Auth fixtures loaded successfully')"
Check: New fixtures exist and are importable
  </verify>
  <done>
Auth conftest.py provides reusable fixtures for mobile authentication testing (user, device, tokens, biometric data).
  </done>
</task>

<task type="auto">
  <name>Test mobile login endpoint</name>
  <files>backend/tests/api/test_auth_routes_enhanced.py</files>
  <action>
Create backend/tests/api/test_auth_routes_enhanced.py with mobile login tests:

1. Test class TestMobileLogin:
   - test_login_with_valid_credentials: POST /api/auth/mobile/login success
   - test_login_invalid_email: 400/401 for wrong email
   - test_login_invalid_password: 400/401 for wrong password
   - test_login_missing_fields: 400 for missing email/password/device_token
   - test_login_creates_device: Verify MobileDevice record created
   - test_login_updates_device_info: device_info saved correctly
   - test_login_returns_tokens: Response includes access_token, refresh_token, expires_at
   - test_login_platform_validation: Accept only ios/android platforms

2. Create FastAPI app in module:
   ```python
   from fastapi import FastAPI
   from api.auth_routes import router
   app = FastAPI()
   app.include_router(router)
   ```

3. Use parametrized tests for validation:
   - Invalid platforms (web, desktop, invalid)
   - Missing required fields

4. Mock authenticate_mobile_user:
   - Use patch("core.auth.authenticate_mobile_user")
   - Return success dict for valid credentials
   - Return None for invalid

File size target: 150+ lines for mobile login tests alone.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_auth_routes_enhanced.py::TestMobileLogin -v
Check: All mobile login tests pass, coverage of login endpoint >75%
  </verify>
  <done>
Mobile login endpoint fully tested with happy paths, validation, error cases, and device creation.
  </done>
</task>

<task type="auto">
  <name>Test biometric registration and authentication</name>
  <files>backend/tests/api/test_auth_routes_enhanced.py</files>
  <action>
Add biometric authentication tests to existing test file:

1. Test class TestBiometricRegistration:
   - test_register_biometric_success: POST /api/auth/mobile/biometric/register
   - test_register_requires_auth: 401 without Authorization header
   - test_register_device_not_found: 404 for invalid device_token
   - test_register_saves_public_key: Verify device_info["biometric_public_key"] set
   - test_register_generates_challenge: Verify challenge token returned
   - test_register_enables_after_auth: biometric_enabled=False initially, True after auth

2. Test class TestBiometricAuthentication:
   - test_biometric_auth_success: POST /api/auth/mobile/biometric/authenticate
   - test_biometric_auth_device_not_found: 404 for invalid device_id
   - test_biometric_auth_not_registered: 400 when biometric_public_key missing
   - test_biometric_auth_invalid_signature: Returns success=False for bad signature
   - test_biometric_auth_returns_tokens: Valid auth returns access_token, refresh_token
   - test_biometric_auth_updates_last_used: Verify last_biometric_auth timestamp set

3. Mock verify_biometric_signature:
   - patch("core.auth.verify_biometric_signature")
   - Return True for valid signatures, False for invalid

4. Use db_session fixture for database operations
   - Create test User and MobileDevice records
   - Clean up after each test

File size target: 150+ lines for biometric tests.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_auth_routes_enhanced.py::TestBiometricRegistration -v && python -m pytest backend/tests/api/test_auth_routes_enhanced.py::TestBiometricAuthentication -v
Check: All biometric tests pass, coverage of biometric endpoints >75%
  </verify>
  <done>
Biometric registration and authentication fully tested with registration flow, signature verification, and token generation.
  </done>
</task>

<task type="auto">
  <name>Test token refresh and device management</name>
  <files>backend/tests/api/test_auth_routes_enhanced.py</files>
  <action>
Add token refresh and device management tests to existing test file:

1. Test class TestTokenRefresh:
   - test_refresh_valid_token: POST /api/auth/mobile/refresh success
   - test_refresh_expired_token: 401 for expired JWT
   - test_refresh_invalid_token: 401 for malformed JWT
   - test_refresh_wrong_token_type: 401 when type != "refresh"
   - test_refresh_returns_new_tokens: Response includes new access_token, refresh_token
   - test_refresh_validates_device: 400 if device not found/inactive

2. Test class TestDeviceManagement:
   - test_get_device_info: GET /api/auth/mobile/device success
   - test_get_device_not_found: 404 for invalid device_id
   - test_get_device_requires_auth: 401 without Authorization header
   - test_get_device_returns_correct_fields: device_id, platform, status, notification_enabled, last_active, created_at
   - test_delete_device: DELETE /api/auth/mobile/device success
   - test_delete_device_not_found: 404 for invalid device_id
   - test_delete_marks_inactive: Verify status="inactive", notification_enabled=False

3. Mock JWT decode:
   - patch("jose.jwt.decode") for token refresh tests
   - Return valid payload for good tokens
   - Raise JWTError for invalid tokens

4. Test database cleanup:
   - Use db_session fixture
   - Verify soft delete (status=inactive, not actual deletion)

File size target: 150+ lines for refresh and device tests.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_auth_routes_enhanced.py::TestTokenRefresh -v && python -m pytest backend/tests/api/test_auth_routes_enhanced.py::TestDeviceManagement -v
Check: All refresh/device tests pass, coverage of these endpoints >75%
  </verify>
  <done>
Token refresh and device management fully tested with JWT validation, device lifecycle, and error handling.
  </done>
</task>

<task type="auto">
  <name>Test error paths and edge cases</name>
  <files>backend/tests/api/test_auth_routes_enhanced.py</files>
  <action>
Add comprehensive error path and edge case tests:

1. Test class TestAuthErrorPaths:
   - test_database_connection_error: Mock DB failure, verify 500 response
   - test_concurrent_login_same_device: Multiple login requests handling
   - test_device_info_json_parsing: Invalid JSON in device_info field
   - test_missing_user_after_auth: Auth succeeds but user not found (race condition)
   - test_platform_case_sensitivity: Platform validation (iOS vs ios)

2. Test class TestAuthEdgeCases:
   - test_very_long_device_token: Handle 1000+ character device_token
   - test_special_characters_in_email: Email validation with +, ., -
   - test_unicode_in_device_info: Unicode characters in platform/device_info
   - test_multiple_devices_same_user: User with multiple registered devices
   - test_device_already_registered: Re-login with existing device_token

3. Use monkeypatch for error injection:
   - Mock db.commit() to raise Exception
   - Mock db.query() to return None
   - Verify error responses contain proper error messages

4. Test logging:
   - Mock logger to verify error logging occurs
   - Check for log entries on failed auth attempts

File size target: 100+ lines for error path tests.
  </action>
  <verify>
Run: python -m pytest backend/tests/api/test_auth_routes_enhanced.py::TestAuthErrorPaths -v && python -m pytest backend/tests/api/test_auth_routes_enhanced.py::TestAuthEdgeCases -v
Check: All error path tests pass, error responses are proper
  </verify>
  <done>
Error paths and edge cases fully tested with database failures, race conditions, validation errors, and special cases.
  </done>
</task>

</tasks>

<verification>
1. Run all enhanced auth tests: python -m pytest backend/tests/api/test_auth_routes_enhanced.py -v --cov=backend/api/auth_routes --cov-report=term-missing
2. Verify coverage >75% for auth_routes.py
3. Confirm all test classes pass (TestMobileLogin, TestBiometricRegistration, TestBiometricAuthentication, TestTokenRefresh, TestDeviceManagement, TestAuthErrorPaths, TestAuthEdgeCases)
4. Check total test count >30 tests
5. Verify file size >500 lines
</verification>

<success_criteria>
1. test_auth_routes_enhanced.py created with 500+ lines, 30+ tests
2. auth_routes.py achieves 75%+ line coverage
3. All 6 endpoints tested (mobile/login, biometric/register, biometric/authenticate, mobile/refresh, mobile/device GET, mobile/device DELETE)
4. Error paths tested (invalid credentials, device not found, validation errors, database failures)
5. All tests use proper fixtures (db_session, mock_auth_service, mock_mobile_device)
</success_criteria>

<output>
After completion, create `.planning/phases/176-api-routes-coverage-auth-authz/176-01-SUMMARY.md` with:
- Coverage achieved for auth_routes.py
- Total new test count and lines of test code
- Endpoint coverage breakdown
- Any uncovered edge cases identified
</output>
