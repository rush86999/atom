---
context: default
phase: R72-hermes-gap-closure
task: 9
total_tasks: 10
status: paused
last_updated: 2026-08-02T16:45:46Z
---

# R72 — Hermes gap-closure plan: paused at 8/10 workstreams (next: G)

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Test-infra patch targets that don't exist | `tests/core/agents/test_atom_meta_agent_react_loop.py` patches `core.atom_meta_agent.LLMService`, but that module has no such attribute → 33 pre-existing failures. NOT caused by R72 (verified via git stash). | advisory | Ignore these failures OR fix patch targets to `core.llm_service.get_llm_service` in a separate test-infra commit. |
| venv path | The venv is `backend/venv/bin/python`, NOT repo-root `venv/`. Run pytest from `backend/` with `PYTHONPATH=$PWD`. | advisory | Always `cd /Users/rushiparikh/projects/atom/backend` first. |
| BYOKHandler `get_ranked_providers` is a SYNC method returning `AwaitableResult` | `generate_structured_response` does `await self.get_ranked_providers(...)`. Test harnesses that mock it with a BARE LIST cause `TypeError: object list can't be used in 'await' expression` → handler bails before reaching instructor/cascade/MoA. Discovered as the root cause of pre-existing cascade B1–B8 failures + MoA M2/M8. Fixed by mocking `return_value=AwaitableResult(options)` + `itertools.cycle` for the tenant-plan DB lookup + `tenant.id` on the fixture. | blocking | Any BYOKHandler test that exercises `generate_structured_response` MUST wrap the options mock in `AwaitableResult` and feed every tenant-plan block (workspace, then tenant) via `itertools.cycle`. |
| Config flags already added | `hallucination_config.py` already has `is_moa_enabled/get_moa_samples`, `is_parallel_tools_enabled/get_max_parallel_tools`, `is_tool_cache_enabled/get_tool_cache_ttl`, `is_skill_injection_enabled` (all default-ON). Workstreams F/G/H pick up from here. | advisory | Do NOT re-add these flags. |

<current_state>
Executing the R72 gap-closure plan (10 workstreams A–J). 8 completed and committed:
- D (memory defaults ON): `9c67a3620`
- A (in-loop self-correction + ActionJudge wiring): `4c8fac36c`
- C (skill auto-injection): `07df489ec`
- E (FieldGuide snapshot): `07df489ec`
- H (tool-result memoization): `793289dd3`
- B (/learn endpoint): `e4fbe855d`
- I (channel binding): `54361bc59`
- F (Mixture-of-Agents): `0ebc610a1`

Working tree is CLEAN (only test byproducts: `backend/.planning/STATE.md` modified, `backend/atom_dev.db-journal` deleted, `backend/chat_sessions.json` modified — do NOT commit these).
</current_state>

<completed_work>

Completed Tasks (committed):
- Task F (MoA, THIS SESSION): `generate_structured_response` gained `provider_model`/`allow_moa` kwargs (byte-identical when unset) + MoA dispatch guard (`_moa_eligible`: COMPLEX/ADVANCED or code/analysis/reasoning, >=2 options, no vision/pin) + `generate_structured_moa` (N per-provider samples pinned `provider_model`, `allow_moa=False`, cascade per sample; one best-ranked aggregator; fallback best valid sample) + `SelfConsistencyVoter.is_irreversible` post-hoc audit + voter recursion guards (`allow_moa=False` at `self_consistency_voter.py` both sample call sites). Tests `tests/unit/llm/test_moa.py` M1–M11: 11/11. Fixed pre-existing test-infra break in `tests/unit/llm/test_cascade_routing.py` (AwaitableResult mock + cycle + tenant.id): now 9/9. BYOK handler/routes regression: 38 pass, 2 skipped. `main_api_app` imports clean.
- Prior sessions: D, A, C, E, H, B, I as listed above.
</completed_work>

<remaining_work>

- Task G (parallel tools, NEXT — NOT STARTED): `actions: Optional[List[ToolCall]]` on BOTH ReActStep copies (react_models.py:15 + atom_meta_agent.py local copy ~185); prompt-format doc for optional `actions` array (atom_meta_agent.py:954-959, generic_agent.py:374-379); parallel branch in both loops before single-action branch: per-tool `can_perform_action_async`, force HITL for complexity>1, all-or-nothing batch approval (`_wait_for_all_approvals`), `asyncio.gather(return_exceptions=True)` over `_execute_tool_with_governance(..., pre_approved=True)`, serialize `mcp_tool_search`, `_merge_parallel_observations`, one `AgentReasoningStep` per tool (same step_number), turn_fact once per batch; `pre_approved: bool = False` kwarg on `_execute_tool_with_governance`; generic_agent mirror (execute ~171, `_step_act(pre_approved=True)` ~515). Tests `tests/unit/test_parallel_tools.py` (P1–P10).
- Task J (docs, LAST): HERMES_COMPARISON.md gap matrix (mark 1-9 closed), CLAUDE.md env vars (ATOM_MOA_*, ATOM_PARALLEL_TOOLS, ATOM_MAX_PARALLEL_TOOLS, ATOM_SKILL_INJECTION_ENABLED, ATOM_TOOL_CACHE_*; TURN_FACT_* defaults), ENVIRONMENT_VARIABLES.md, CONTEXT_MEMORY.md defaults.
</remaining_work>

<decisions_made>

- MoA + in-loop parallel tools: full implementation, default ON (per user, locked).
- Durable-memory flags (`TURN_FACT_EXTRACTION_ENABLED`, `TURN_FACT_VECTOR_RECALL_ENABLED`): ON by default.
- Skill injection, tool cache: default ON. ActionJudge stays default OFF (`ATOM_SANDBOX_JUDGE_ENABLED`).
- Sequencing: Wave 1 (A,B,C,E,H,D) → Wave 2 (I) → Wave 3 (F,G) → J continuously. F/G land last, then full regression.
- MoA dispatch scope: only COMPLEX/ADVANCED or code/analysis/reasoning, >=2 providers, no vision, no pinned `provider_model` (byte-identical when unset).
</decisions_made>

<blockers>
- Pre-existing react-loop suite failures: 33 tests patch nonexistent `core.atom_meta_agent.LLMService`. Not caused by R72. Verify with `git stash` if in doubt.
- Pre-existing `tests/unit/llm/test_self_consistency_voter.py::test_C5_is_irreversible_detection` (asserts OLD value-matching; code intentionally name-matches per Bug #13) and `tests/unit/llm/test_provider_family_invariant.py::test_D1` (harness/await mismatch). Both fail at HEAD, git-stash verified; NOT caused by R72 F.
- `backend/atom_dev.db-journal` shows deleted (test byproduct); leave out of commits.
</blockers>

## Required Reading (in order)
1. `backend/core/hallucination_config.py` — flags already present for F/G/H (do not re-add)
1. `backend/core/atom_meta_agent.py` — ReActStep local copy (~185), `_execute_tool_with_governance` (~1098), `_wait_for_approval`, `can_perform_action_async`, loop (~668)
1. `backend/core/generic_agent.py` — execute loop (~171), `_step_act` (~515), `_react_step` (~374)
1. `backend/core/react_models.py` — `ReActStep` (~15, keep in sync with atom_meta_agent local copy)
1. `tests/unit/llm/test_cascade_routing.py` + `tests/unit/llm/test_moa.py` — the AwaitableResult harness pattern (blocking constraint above)

## Infrastructure State
- Venv: `backend/venv/bin/python` (Python 3.11). Run pytest from `backend/` with `PYTHONPATH=$PWD`.
- Recent commits on `main`: `bc48a1635` (HANDOFF state), `0ebc610a1` (F/MoA), `54361bc59` (I/channel).
- `main_api_app` imports clean after F.

<context>
Implementing the R72 Hermes gap-closure plan. 8/10 workstreams done and committed. The pattern that works: edit code + write compact async pytest (mock BYOKHandler internals via `AwaitableResult`/`itertools.cycle`, mock `_TURN_FACT_*` aliases) + verify with targeted pytest + `import main_api_app` check + commit per workstream. Pre-flight for G confirmed: `is_parallel_tools_enabled()` (default true) and `get_max_parallel_tools()` (default 4) already exist in hallucination_config.py:93-101. G is the last architectural wave; J (docs) last.
</context>

<next_action>
Start with Workstream G (parallel tools, default ON): add `actions: Optional[List[ToolCall]]` to BOTH ReActStep definitions (react_models.py + atom_meta_agent.py local copy), add the parallel branch to both agent loops with all-or-nothing HITL batch approval + `asyncio.gather` over `_execute_tool_with_governance(pre_approved=True)`, persist one AgentReasoningStep per tool, and write `tests/unit/test_parallel_tools.py` (P1–P10). Then run the react-loop regression and commit `feat(agent): in-loop parallel tool execution`.
</next_action>
