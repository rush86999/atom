---
context: default
phase: R72-hermes-gap-closure
task: 10
total_tasks: 10
status: paused
last_updated: 2026-08-02T17:30:00Z
---

# R72 — Hermes gap-closure plan: 9/10 workstreams done (next: J docs)

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Test-infra patch targets that don't exist | `tests/core/agents/test_atom_meta_agent_react_loop.py` patches `core.atom_meta_agent.LLMService`, but that module has no such attribute → 33 pre-existing failures. NOT caused by R72 (git-stash verified; identical failure counts before/after G). | advisory | Ignore these failures OR fix patch targets to `core.llm_service.get_llm_service` in a separate test-infra commit. |
| venv path | The venv is `backend/venv/bin/python`, NOT repo-root `venv/`. Run pytest from `backend/` with `PYTHONPATH=$PWD`. | advisory | Always `cd /Users/rushiparikh/projects/atom/backend` first. |
| BYOKHandler `get_ranked_providers` is a SYNC method returning `AwaitableResult` | Tests that mock it with a BARE LIST raise `TypeError: object list can't be used in 'await'`. Must wrap in `AwaitableResult` + `itertools.cycle` for tenant-plan DB lookup. | blocking | Any BYOKHandler test exercising `generate_structured_response` MUST use this harness. |
| Config flags already added | `hallucination_config.py` already has `is_parallel_tools_enabled/get_max_parallel_tools` (default ON / 4). Do NOT re-add. | advisory | Workstream G picks up from here. |

<current_state>
Executing the R72 gap-closure plan (10 workstreams A–J). 9 completed and committed:
- D (memory defaults ON): `9c67a3620`
- A (in-loop self-correction + ActionJudge wiring): `4c8fac36c`
- C (skill auto-injection): `07df489ec`
- E (FieldGuide snapshot): `07df489ec`
- H (tool-result memoization): `793289dd3`
- B (/learn endpoint): `e4fbe855d`
- I (channel binding): `54361bc59`
- F (Mixture-of-Agents): `0ebc610a1`
- G (parallel tools): `235237645`

Working tree has only test byproducts (`backend/.planning/STATE.md`, `backend/atom_dev.db-journal`, `backend/chat_sessions.json` — do NOT commit).
</current_state>

<completed_work>

Completed Tasks (committed):
- Task G (parallel tools, THIS SESSION): `actions: Optional[List[ToolCall]]` on both ReActStep copies (react_models.py + atom_meta_agent local copy); prompt-format docs; parallel branch in atom_meta_agent + generic_agent loops with all-or-nothing HITL batch approval (`_wait_for_all_approvals`), `asyncio.gather` over `_execute_tool_with_governance(pre_approved=True)` / `_step_act(pre_approved=True)`, `mcp_tool_search` serialized, `ATOM_MAX_PARALLEL_TOOLS` cap (default 4); one `AgentReasoningStep` per tool via shared `_persist_reasoning_step`; turn-fact once per batch (`dispatch_turn_fact`); flag-off degrades `actions` to first single action. Tests `tests/unit/test_parallel_tools.py` P1–P10 (10/10). `main_api_app` imports clean. Meta-agent suites identical to baseline (37 failed/24 errors pre-existing; zero new).
- Prior sessions: D, A, C, E, H, B, I, F as listed above.
</completed_work>

<remaining_work>

- Task J (docs, NEXT — NOT STARTED): `docs/architecture/HERMES_COMPARISON.md` gap matrix (mark gaps 1-9 closed); `CLAUDE.md` env vars (ATOM_MOA_*, ATOM_PARALLEL_TOOLS, ATOM_MAX_PARALLEL_TOOLS, ATOM_SKILL_INJECTION_ENABLED, ATOM_TOOL_CACHE_*; TURN_FACT_* defaults flipped true); `backend/docs/ENVIRONMENT_VARIABLES.md` (or wherever the env var reference lives); `docs/architecture/CONTEXT_MEMORY.md` defaults. Then full regression + mark R72 complete.
</remaining_work>

<decisions_made>

- MoA + in-loop parallel tools: full implementation, default ON (per user, locked).
- Durable-memory flags (`TURN_FACT_EXTRACTION_ENABLED`, `TURN_FACT_VECTOR_RECALL_ENABLED`): ON by default.
- Skill injection, tool cache: default ON. ActionJudge stays default OFF (`ATOM_SANDBOX_JUDGE_ENABLED`).
- Sequencing: Wave 1 (A,B,C,E,H,D) → Wave 2 (I) → Wave 3 (F,G) → J continuously.
- Parallel batch: all-or-nothing HITL; mcp_tool_search serialized; per-tool AgentReasoningStep; turn-fact once per batch.
</decisions_made>

<blockers>
- Pre-existing react-loop suite failures: 33 tests patch nonexistent `core.atom_meta_agent.LLMService`. Not caused by R72. Git-stash verified (identical counts before/after G).
- Pre-existing `test_C5_is_irreversible_detection` + `test_D1_provider_family_invariant`. Both fail at HEAD; not caused by R72.
- `backend/atom_dev.db-journal` shows deleted (test byproduct); leave out of commits.
</blockers>

## Required Reading (in order)
1. `backend/core/hallucination_config.py` — flags already present for F/G/H (do not re-add)
1. `docs/architecture/HERMES_COMPARISON.md` — gap matrix to mark closed in J
1. `CLAUDE.md` + `backend/docs/ENVIRONMENT_VARIABLES.md` — env-var reference to update in J

## Infrastructure State
- Venv: `backend/venv/bin/python` (Python 3.11). Run pytest from `backend/` with `PYTHONPATH=$PWD`.
- Recent commits on `main`: `235237645` (G/parallel), `f0c06db13` (wip R72 8/10), `0ebc610a1` (F/MoA).
- `main_api_app` imports clean after G.

<context>
Implementing the R72 Hermes gap-closure plan. 9/10 workstreams done and committed. The pattern that works: edit code + write compact async pytest + verify with targeted pytest + `import main_api_app` check + commit per workstream. G (parallel tools) landed at 235237645 with P1–P10 tests (10/10) and zero new regressions. Only J (docs) remains, then full regression.
</context>

<next_action>
Workstream J (docs, LAST): update HERMES_COMPARISON.md gap matrix (mark gaps 1-9 closed), CLAUDE.md env vars (ATOM_MOA_*, ATOM_PARALLEL_TOOLS, ATOM_MAX_PARALLEL_TOOLS, ATOM_SKILL_INJECTION_ENABLED, ATOM_TOOL_CACHE_*; TURN_FACT_* defaults), ENVIRONMENT_VARIABLES.md, CONTEXT_MEMORY.md defaults. Then run full regression and mark R72 complete.
</next_action>
