---
context: default
phase: test-infra-cleanup
task: 2
total_tasks: 5
status: in_progress
last_updated: 2026-08-03T00:04:27Z
---

# Test-infra cleanup + bug hunting (post-R72) — PAUSED

## BLOCKING CONSTRAINT — read before anything else

- [ ] **React-loop tests: do NOT just repatch the LLMService target.** Patching
  `core.atom_meta_agent.LLMService` → `core.service_factory.ServiceFactory.get_llm_service`
  clears the `AttributeError` but exposes deeper stale test logic
  (`NoneType.category` at `core/atom_meta_agent.py:603`) and **hangs the file**.
  The 33 tests in `tests/core/agents/test_atom_meta_agent_react_loop.py` must be
  **rewritten** against the current `AtomMetaAgent.execute()` ReAct-loop semantics —
  a mechanical one-line patch is strictly worse than the fast-failing AttributeError.

## Current state
- **Done this session (both pushed to `origin/main`):**
  - `39f2b40bd` test(infra): fix 76 pytest collection errors from dead-code cleanup
  - `4674ca56d` docs: correct CLAUDE.md #31 — ReDoS guard was archived, never production-wired
- **Working tree clean** for session changes. Pre-existing unrelated modified files:
  `.planning/STATE.md`, `backend/.planning/STATE.md`, `backend/chat_sessions.json`,
  `backend/atom_dev.db-journal` (deleted) — do NOT commit these.
- **Full suite collects cleanly now**: `pytest tests/ --collect-only` exit 0, 1511 nodes.

## Completed this session
1. **Test-infra cleanup (commit `39f2b40bd`)** — 76 collection errors fixed:
   - 69 pure-dead files: module-level `pytest.importorskip("module", reason="...")`
   - 5 mixed files: try/except ImportError + skipif/skipIf/fixture-skip on dead classes only
   - `test_webhook_processing.py`: `pytest.skip(allow_module_level=True)` (stripe_routes is a stub)
   - `test_data_processing_scenarios.py`: removed unused deleted-module import
2. **CLAUDE.md #31 doc fix (commit `4674ca56d`)** — Workflow ReDoS Guard investigation:
   - Verified `core/workflow_parameter_validator.py` was NEVER referenced by any non-test
     production file across all git history → cleanup's "zero references" was accurate
   - No live code compiles user-supplied regex → NOT a live ReDoS regression
   - Corrected the doc to describe the real live code (`ParameterValidator`, test-only)
3. **React-loop investigation** — 33 tests patch nonexistent `core.atom_meta_agent.LLMService`;
   confirmed stale; mechanical fix hangs → **reverted**, no commit.

## Remaining work
- Rewrite 33 stale react-loop tests against current `execute()` ReAct-loop semantics
- Repair 82 surfaced stale-test failures in live mixed-file classes:
  - `test_data_processing_scenarios.py` (48): requests `db_engine` fixture not in
    `tests/scenarios/conftest.py` (only in property_tests/regression/e2e conftests)
  - `test_mobile_canvas.py` (14): User fixture omits `role`; model `nullable=False` (`core/models.py:434`)
  - `test_skill_execution_error_paths.py` (13): old `CommunitySkillTool(name=...)` constructor
  - `test_business_intelligence.py` (3): mocked AI returns empty intents
  - `test_memory_backfill.py` (4): Redis-based job-queue tests need live Redis
- Full regression pass after collection fix

## Decisions made
- ReDoS guard removal = not a live regression (verified via git grep across all revisions)
- Reverted react-loop patch-target fix (hangs file; needs full rewrite, not one-liner)

## Resume instructions
Run `/gsd:resume-work` in a fresh session — it will load this handoff and `HANDOFF.json`.
Next action: pick up the react-loop test rewrite (33 tests) or the 82 surfaced stale-test failures.

<next_action>
Rewrite `backend/tests/core/agents/test_atom_meta_agent_react_loop.py` (33 tests) against current
`AtomMetaAgent.execute()` semantics, OR repair the 82 surfaced stale-test failures listed above.
</next_action>
